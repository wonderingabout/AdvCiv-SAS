AdvCiv-SAS C++ File Audit Album
Generated from local light-source snapshot only; no web/GitHub lookup used.

CURRENT PROGRESS SUMMARY
Files complete: 169 / 169
Files open: 0 / 169
Last completed file: ordinary queue 169 CvGameCoreDLL/_precompile.cpp (5 queued / 5 current physical lines) - CLOSED AT C031-WIP676 / NO NEW FINDING; F708 UNUSED
Current open file: none - all 169 queued .cpp files COMPLETE at C031-WIP676; post-pass mechanical invariant audit ACTIVE at C031-WIP891 / F708-F748 confirmed / F749 unused
Latest confirmed audit findings: F650-F678, F680-F748 / KI#972-KI#1069, plus F679 reopening KI#300. F708-F713 were promoted by the post-169-file reconciliation: F708 resolves the wrapped-iterator seam explicitly carried from Queue022/Queue025 to Queue167; F709 resolves the deliberately parked advc.106b Turn-Log NO_TECH counting heuristic after the substantive primary gameplay/state pass completed; F710 promotes the separately parked SAS Stalin Renaissance peace-intro audio-reference typo after full XML/audio/player-call-chain proof; F711 promotes the previously deferred SPaH hidden-randomization disclosure flag that is serialized/displayed but never set true; F712 promotes the deferred third-party resource-alert multiset duplicate-message root when identical deal multiplicity changes by more than one between observer checks; F713 promotes the inherited malformed CvMap initialization memory-log printf conversion that executes on every map init. F714-F748 were promoted by the subsequent chronological repair-history and mechanical-invariant audits. Exact historical ranges, duplicate reconciliations, retractions, reopened findings and unused IDs remain authoritative in the durable checkpoints below.
Next new root ID: F749 unused / provisional KI#1070 next

The durable checkpoints and current cursor below are authoritative for finding status; the initial C010 snapshot and later WIP appends preserve their own historical source identities.

METHOD
- Primary queue = every current .cpp file, sorted by current line count longest -> shortest.
- Associated headers/XML/history are inspected when needed to establish contracts, configuration semantics, ancestry, or data reality.
- Current-tree behavior is authoritative. Historical diffs are supporting archaeology, not a reason to report a defect that no longer survives.
- Do not promote tuning disagreements, theoretical unsupported configurations, dead paths, or already-fixed history into new findings.
- One root cause gets one finding even when several callers expose different symptoms.
- False positives are recorded when useful so they are not rediscovered later.
- The stronger-WIP priority reloop is complete through C031-WIP509, and the resumed ordinary numeric queue is now complete through Queue169 at C031-WIP676. The WIP509-WIP511 numbering bridges the retry fork by retaining the omitted ArithmeticTraits closure before the fuller CvInitCore baseline and confirmed finding.

WIP541/WIP542 FORK RECONCILIATION
---------------------------------
An interrupted alternate WIP541 recovery branch reconstructed the missing Queue-043 continuation,
closed Queue 044 without a finding and opened Queue 046. The intact WIP542 branch supplied the
actual physical WIP537-WIP540 stones, then continued through WIP541-WIP542 and confirmed F674-F675
while Queue 044 remained open. This intact chain is authoritative. Do not resume from the alternate
Queue-044 closure or its premature Queue-046 cursor; audit Queue 046 again in ordinary order after
the authoritative Queue-044 work closes. A later duplicate recovered-WIP541 submission expanded
the same stale closure but supplied no new durable root; its one-reference FP interpretation and
unsupported-timing dismissal of stale JoinGroup are superseded by WIP542's per-machine message-order
proof and simultaneous-turn invalidation evidence.

QUEUE (current .cpp files, longest -> shortest)
001  32205 CvGameCoreDLL/CvPlayerAI.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP65 / F321-F374]
002  28024 CvGameCoreDLL/CvUnitAI.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP87 / F375-F387]
003  22412 CvGameCoreDLL/CvGameTextMgr.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP150 / F388-F418, F420-F443; F419 DUPLICATE]
004  20675 CvGameCoreDLL/CvPlayer.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP276 / F444, F446-F520; F445 RETRACTED; F489 DUPLICATE; F709 POST-PASS CONFIRMED C031-WIP678]
005  19615 CvGameCoreDLL/CvCityAI.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP335]
006  13828 CvGameCoreDLL/CvCity.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP403]
007  12945 CvGameCoreDLL/CvUnit.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP427 / F593-F607]
008  10841 CvGameCoreDLL/CvGame.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP431 / F608]
009   8705 CvGameCoreDLL/CvPlot.cpp [COMPLETE ORIGINAL PASS / DEEP-METHOD RE-AUDIT CLOSED - C031-WIP435 / F609-F611; F611 REOPENS KI#351]
010   6854 CvGameCoreDLL/CvTeamAI.cpp [COMPLETE - SEE DURABLE CHECKPOINT]
011   6343 CvGameCoreDLL/SASGameRecordLog.cpp [COMPLETE - SEE DURABLE CHECKPOINT]
012   6326 CvGameCoreDLL/CvDLLWidgetData.cpp [COMPLETE - SEE DURABLE CHECKPOINT]
013   6190 CvGameCoreDLL/CvTeam.cpp [COMPLETE - SEE DURABLE CHECKPOINT]
014   5024 CvGameCoreDLL/WarUtilityAspect.cpp [COMPLETE - SEE C015 DURABLE CHECKPOINT]
015   4722 CvGameCoreDLL/CvSelectionGroup.cpp [COMPLETE - SEE C016 DURABLE CHECKPOINT]
016   4595 CvGameCoreDLL/CitySiteEvaluator.cpp [COMPLETE - SEE C017 DURABLE CHECKPOINT]
017   3700 CvGameCoreDLL/UWAIAgent.cpp [COMPLETE - SEE C018 DURABLE CHECKPOINT]
018   2965 CvGameCoreDLL/CvGameInterface.cpp [COMPLETE - SEE C030 DURABLE CHECKPOINT]
019   2810 CvGameCoreDLL/CvDLLButtonPopup.cpp [COMPLETE - SEE C031 DURABLE CHECKPOINT]
020   2599 CvGameCoreDLL/CvInfo_Unit.cpp [COMPLETE - C031-WIP437 / F612]
021   2503 CvGameCoreDLL/CyPlayer.cpp [COMPLETE - C031-WIP438]
022   2448 CvGameCoreDLL/TrueStarts.cpp [COMPLETE - SEE C031-WIP450 DURABLE CHECKPOINT]
023   2430 CvGameCoreDLL/InvasionGraph.cpp [COMPLETE - SEE C022 DURABLE CHECKPOINT]
024   2399 CvGameCoreDLL/CyCity.cpp [COMPLETE - SEE C031-WIP460 DURABLE CHECKPOINT]
025   2247 CvGameCoreDLL/StartingPositionIteration.cpp [COMPLETE - SEE C031-WIP457 DURABLE CHECKPOINT]
026   2132 CvGameCoreDLL/CvInitCore.cpp [COMPLETE - C031-WIP513 / F655-F656]
027   2099 CvGameCoreDLL/CvInfo_RandomEvent.cpp [COMPLETE - C031-WIP514]
028   2095 CvGameCoreDLL/CvXMLLoadUtilitySet.cpp [COMPLETE - C031-WIP516 / F657; F680 CROSS-FILE ACTIVATION DISCOVERED AT C031-WIP555]
029   2052 CvGameCoreDLL/CyEnumsInterface.cpp [COMPLETE - C031-WIP518 / F658]
030   1919 CvGameCoreDLL/CvInfo_Misc.cpp [COMPLETE - C031-WIP519 / NO NEW FINDING]
031   1812 CvGameCoreDLL/UWAICache.cpp [COMPLETE - SEE C020 DURABLE CHECKPOINT]
032   1704 CvGameCoreDLL/CyUnit.cpp [COMPLETE - C031-WIP520 / NO NEW FINDING]
033   1672 CvGameCoreDLL/CvInfo_Terrain.cpp [COMPLETE - C031-WIP522 / F659]
034   1637 CvGameCoreDLL/CvInfo_Civilization.cpp [COMPLETE - C031-WIP523 / NO NEW C++ FINDING]
035   1619 CvGameCoreDLL/CvInfo_GameOption.cpp [COMPLETE - C031-WIP524 / NO NEW FINDING]
036   1596 CvGameCoreDLL/CvMap.cpp [COMPLETE - SEE C031-WIP468 DURABLE CHECKPOINT]
037   1576 CvGameCoreDLL/CvPythonCaller.cpp [COMPLETE - SEE C025 DURABLE CHECKPOINT]
038   1555 CvGameCoreDLL/RiseFall.cpp [COMPLETE - C031-WIP531 / F660-F665]
039   1461 CvGameCoreDLL/CvGameCoreUtils.cpp [COMPLETE - SEE C023 DURABLE CHECKPOINT]
040   1454 CvGameCoreDLL/CvGlobals.cpp [COMPLETE - SEE C024 DURABLE CHECKPOINT]
041   1454 CvGameCoreDLL/CvSelectionGroupAI.cpp [COMPLETE - SEE C019 DURABLE CHECKPOINT]
042   1419 CvGameCoreDLL/CvDeal.cpp [COMPLETE - SEE C026 DURABLE CHECKPOINT]
043   1335 CvGameCoreDLL/ACOText.cpp [COMPLETE - C031-WIP540 / F666-F673]
044   1306 CvGameCoreDLL/CvMessageData.cpp [COMPLETE - C031-WIP543 / F674-F675]
045   1281 CvGameCoreDLL/CvMapGenerator.cpp [COMPLETE - SEE C021 DURABLE CHECKPOINT]
046   1268 CvGameCoreDLL/CyGame.cpp [COMPLETE - C031-WIP545 / NO NEW FINDING]
047   1214 CvGameCoreDLL/CvDllPythonEvents.cpp [COMPLETE - SEE C027 DURABLE CHECKPOINT]
048   1128 CvGameCoreDLL/CvInfo_Building.cpp [COMPLETE - C031-WIP548 / F676 CONFIRMED; NO INDEPENDENT F677]
049   1081 CvGameCoreDLL/CvReplayInfo.cpp [COMPLETE - C031-WIP551 / F677-F678; NO INDEPENDENT F679]
050   1067 CvGameCoreDLL/CyPlot.cpp [COMPLETE - C031-WIP553 / F679 REOPENS KI#300; NO INDEPENDENT F680]
051   1042 CvGameCoreDLL/CyTeam.cpp [COMPLETE - C031-WIP554 / NO NEW FINDING; F680 UNUSED]
052    973 CvGameCoreDLL/CvXMLLoadUtilityGet.cpp [COMPLETE - C031-WIP556 / F680 CONFIRMED; NO INDEPENDENT F681]
053    970 CvGameCoreDLL/EnumMapTest.cpp [COMPLETE - C031-WIP557 / NO NEW FINDING; F681 UNUSED]
054    844 CvGameCoreDLL/GroupPathFinder.cpp [COMPLETE - SEE C028 DURABLE CHECKPOINT]
055    837 CvGameCoreDLL/CyInfoInterface3.cpp [COMPLETE - C031-WIP558 / NO NEW FINDING; F681 UNUSED]
056    768 CvGameCoreDLL/CvInfo_Asset.cpp [COMPLETE - C031-WIP559 / NO NEW FINDING; F681 UNUSED]
057    713 CvGameCoreDLL/CombatOdds.cpp [COMPLETE - SEE C031-WIP471 DURABLE CHECKPOINT]
058    693 CvGameCoreDLL/ArmamentForecast.cpp [COMPLETE - SEE C031-WIP476 DURABLE CHECKPOINT]
059    687 CvGameCoreDLL/CyGlobalContext.cpp [COMPLETE - C031-WIP560 / NO NEW FINDING; F681 UNUSED]
060    686 CvGameCoreDLL/CvArea.cpp [COMPLETE - SEE C031-WIP479 DURABLE CHECKPOINT]
061    679 CvGameCoreDLL/CvEventReporter.cpp [COMPLETE - C031-WIP561 / NO NEW FINDING; F681 UNUSED]
062    673 CvGameCoreDLL/MilitaryAnalyst.cpp [COMPLETE - SEE C031-WIP482 DURABLE CHECKPOINT]
063    643 CvGameCoreDLL/CyInfoInterface1.cpp [COMPLETE - C031-WIP562 / NO NEW FINDING; F681 UNUSED]
064    623 CvGameCoreDLL/MilitaryBranch.cpp [COMPLETE - SEE C031-WIP484 DURABLE CHECKPOINT]
065    603 CvGameCoreDLL/CvStructs.cpp [COMPLETE - C031-WIP564 / F681 CONFIRMED; NO INDEPENDENT F682]
066    580 CvGameCoreDLL/CvInfo_Command.cpp [COMPLETE - C031-WIP565 / NO NEW FINDING; F682 UNUSED]
067    576 CvGameCoreDLL/CvInfo_Civics.cpp [COMPLETE - C031-WIP566 / NO NEW FINDING; F659 DEDUP; F682 UNUSED]
068    551 CvGameCoreDLL/KmodPathFinderLegacy.cpp [COMPLETE - SEE C031-WIP485 DURABLE CHECKPOINT]
069    543 CvGameCoreDLL/AdvCiv4lerts.cpp [COMPLETE - C031-WIP567 / NO NEW FINDING; F682 UNUSED]
070    494 CvGameCoreDLL/CvInfo_Base.cpp [COMPLETE - C031-WIP568 / NO NEW FINDING; F682 UNUSED]
071    491 CvGameCoreDLL/CyInfoInterface2.cpp [COMPLETE - C031-WIP569 / NO NEW FINDING; F682 UNUSED]
072    482 CvGameCoreDLL/CvStatistics.cpp [COMPLETE - SEE C031-WIP489 DURABLE CHECKPOINT]
073    476 CvGameCoreDLL/CyPlayerInterface1.cpp [COMPLETE - C031-WIP570 / NO NEW FINDING; F682 UNUSED]
074    475 CvGameCoreDLL/WarEvaluator.cpp [COMPLETE - C031-WIP491 / F650]
075    472 CvGameCoreDLL/SelfMod.cpp [COMPLETE - C031-WIP572 / F682 CONFIRMED; NO INDEPENDENT F683]
076    461 CvGameCoreDLL/CyCityInterface1.cpp [COMPLETE - C031-WIP573 / NO NEW FINDING; F683 UNUSED]
077    451 CvGameCoreDLL/StartPointsAsHandicap.cpp [COMPLETE - C031-WIP579 / F683-F687 CONFIRMED; NO INDEPENDENT F688]
078    435 CvGameCoreDLL/CvVirtualWrappers.cpp [COMPLETE - C031-WIP581 / NO NEW FINDING; F688 UNUSED]
079    432 CvGameCoreDLL/ScaledNumTest.cpp [COMPLETE - C031-WIP582 / NO NEW FINDING; F688 UNUSED]
080    428 CvGameCoreDLL/CyReplayInfo.cpp [COMPLETE - C031-WIP583 / NO NEW FINDING; F688 UNUSED]
081    425 CvGameCoreDLL/CvInfo_Tech.cpp [COMPLETE - C031-WIP584 / NO NEW FINDING; F688 UNUSED]
082    407 CvGameCoreDLL/CvFractal.cpp [COMPLETE - C031-WIP493 / F651]
083    403 CvGameCoreDLL/FProfiler.cpp [COMPLETE - C031-WIP585 / NO NEW FINDING; F688 UNUSED]
084    389 CvGameCoreDLL/CyUnitInterface1.cpp [COMPLETE - C031-WIP586 / NO NEW FINDING; F688 UNUSED]
085    375 CvGameCoreDLL/FFreeListTrashArray.cpp [COMPLETE - C031-WIP587 / NO NEW FINDING; F688 UNUSED]
086    374 CvGameCoreDLL/CvXMLLoadUtility.cpp [COMPLETE - C031-WIP588 / NO NEW FINDING; F688 UNUSED]
087    348 CvGameCoreDLL/CyMap.cpp [COMPLETE - C031-WIP589 / NO NEW FINDING; F688 UNUSED]
088    342 CvGameCoreDLL/CvInfo_Organization.cpp [COMPLETE - C031-WIP590 / NO NEW FINDING; F688 UNUSED]
089    338 CvGameCoreDLL/CvGameAI.cpp [COMPLETE - C031-WIP494]
090    330 CvGameCoreDLL/CyGameInterface.cpp [COMPLETE - C031-WIP592 / F688 CONFIRMED; NO INDEPENDENT F689]
091    328 CvGameCoreDLL/CySelectionGroup.cpp [COMPLETE - C031-WIP593 / NO NEW FINDING; F689 UNUSED]
092    327 CvGameCoreDLL/BBAILog.cpp [COMPLETE - SEE C029 DURABLE CHECKPOINT]
093    323 CvGameCoreDLL/CvPopupReturn.cpp [COMPLETE - C031-WIP594 / NO NEW FINDING; F689 UNUSED]
094    304 CvGameCoreDLL/CvPlotGroup.cpp [COMPLETE - C031-WIP595 / NO NEW FINDING; F689 UNUSED]
095    291 CvGameCoreDLL/CyGameTextMgr.cpp [COMPLETE - C031-WIP596 / NO NEW FINDING; F689 UNUSED]
096    286 CvGameCoreDLL/CvMemoryManager.cpp [COMPLETE - C031-WIP495]
097    282 CvGameCoreDLL/CvTalkingHeadMessage.cpp [COMPLETE - C031-WIP597 / NO NEW FINDING; F490/KI#813 DEDUP; F689 UNUSED]
098    281 CvGameCoreDLL/RFChapterScore.cpp [COMPLETE - C031-WIP602 / F689-F692 CONFIRMED; NO INDEPENDENT F693]
099    279 CvGameCoreDLL/CvDLLLogger.cpp [COMPLETE - C031-WIP605 / F693-F694 CONFIRMED; NO INDEPENDENT F695]
100    277 CvGameCoreDLL/CvInfo_Water.cpp [COMPLETE - C031-WIP606 / NO NEW FINDING; F695 UNUSED]
101    277 CvGameCoreDLL/RFChapter.cpp [COMPLETE - C031-WIP609 / F695-F696 CONFIRMED; NO INDEPENDENT F697]
102    273 CvGameCoreDLL/CyPlotInterface1.cpp [COMPLETE - C031-WIP610 / NO NEW FINDING; F697 UNUSED]
103    272 CvGameCoreDLL/CyGameCoreUtils.cpp [COMPLETE - C031-WIP611 / NO NEW FINDING; F697 UNUSED]
104    269 CvGameCoreDLL/CvString.cpp [COMPLETE - C031-WIP612 / NO NEW FINDING; F697 UNUSED]
105    265 CvGameCoreDLL/CvInfo_City.cpp [COMPLETE - C031-WIP613 / NO NEW FINDING; F697 UNUSED]
106    264 CvGameCoreDLL/CvDiploParameters.cpp [COMPLETE - C031-WIP614 / NO NEW FINDING; F697 UNUSED]
107    262 CvGameCoreDLL/FAStarFunc.cpp [COMPLETE - C031-WIP615 / NO NEW FINDING; F697 UNUSED]
108    258 CvGameCoreDLL/CvAgents.cpp [COMPLETE - C031-WIP498 / F652-F653]
109    246 CvGameCoreDLL/CvPopupInfo.cpp [COMPLETE - C031-WIP616 / NO NEW FINDING; F697 UNUSED]
110    240 CvGameCoreDLL/AgentIteratorTest.cpp [COMPLETE - C031-WIP499 / no new root]
111    240 CvGameCoreDLL/CvArtFileMgr.cpp [COMPLETE - C031-WIP618 / F697 CONFIRMED; NO INDEPENDENT F698]
112    237 CvGameCoreDLL/UWAI.cpp [COMPLETE - C031-WIP500 / no new root]
113    235 CvGameCoreDLL/CyTeamInterface.cpp [COMPLETE - C031-WIP619 / NO NEW FINDING; F698 UNUSED]
114    211 CvGameCoreDLL/CvInfo_Symbol.cpp [COMPLETE - C031-WIP620 / NO NEW FINDING; F698 UNUSED]
115    208 CvGameCoreDLL/CvMessageControl.cpp [COMPLETE - C031-WIP621 / NO NEW FINDING; F698 UNUSED]
116    205 CvGameCoreDLL/CyArea.cpp [COMPLETE - C031-WIP622 / NO NEW FINDING; F698 UNUSED]
117    204 CvGameCoreDLL/FAssert.cpp [COMPLETE - C031-WIP623 / NO NEW FINDING; F698 UNUSED]
118    204 CvGameCoreDLL/FDialogTemplate.cpp [COMPLETE - C031-WIP624 / NO NEW FINDING; F698 UNUSED]
119    191 CvGameCoreDLL/CyStructsInterface1.cpp [COMPLETE - C031-WIP625 / NO NEW FINDING; F698 UNUSED]
120    186 CvGameCoreDLL/AgentIterator.cpp [COMPLETE - C031-WIP501 / no new root]
121    177 CvGameCoreDLL/CvXMLLoadUtilityInit.cpp [COMPLETE - C031-WIP626 / NO NEW FINDING; F698 UNUSED]
122    175 CvGameCoreDLL/RFTotalScore.cpp [COMPLETE - C031-WIP630 / F698-F700 CONFIRMED; NO INDEPENDENT F701]
123    175 CvGameCoreDLL/TeamPathFinder.cpp [COMPLETE - C031-WIP502 / no new root]
124    171 CvGameCoreDLL/CyGlobalContextInterface4.cpp [COMPLETE - C031-WIP580 / NO NEW FINDING; F688 UNUSED]
125    166 CvGameCoreDLL/UWAIReport.cpp [COMPLETE - C031-WIP503 / no new root]
126    150 CvGameCoreDLL/CvDllTranslator.cpp [COMPLETE - C031-WIP633 / F701-F702 CONFIRMED; NO INDEPENDENT F703]
127    149 CvGameCoreDLL/CyMapGenerator.cpp [COMPLETE - C031-WIP634 / NO NEW FINDING; F703 UNUSED]
128    149 CvGameCoreDLL/ReproTest.cpp [COMPLETE - C031-WIP637 / F703-F704 CONFIRMED; NO INDEPENDENT F705]
129    145 CvGameCoreDLL/AIStrengthMemoryMap.cpp [COMPLETE - C031-WIP505 / F654]
130    137 CvGameCoreDLL/CvRandom.cpp [COMPLETE - C031-WIP506 / no new root]
131    134 CvGameCoreDLL/Shelf.cpp [COMPLETE - C031-WIP640 / F705-F706 CONFIRMED; NO INDEPENDENT F707]
132    127 CvGameCoreDLL/CvReplayMessage.cpp [COMPLETE - C031-WIP641 / NO NEW FINDING; F707 UNUSED]
133    124 CvGameCoreDLL/CyGlobalContextInterface1.cpp [COMPLETE - C031-WIP642 / NO NEW FINDING; F707 UNUSED]
134    101 CvGameCoreDLL/CvInfo_Build.cpp [COMPLETE - C031-WIP643 / NO NEW FINDING; F707 UNUSED]
135     96 CvGameCoreDLL/TSCProfiler.cpp [COMPLETE - C031-WIP644 / NO NEW FINDING; F707 UNUSED]
136     95 CvGameCoreDLL/CyArgsList.cpp [COMPLETE - C031-WIP645 / NO NEW FINDING; F707 UNUSED]
137     94 CvGameCoreDLL/CyMapInterface.cpp [COMPLETE - C031-WIP646 / NO NEW FINDING; F707 UNUSED]
138     91 CvGameCoreDLL/CvDLLInterfaceIFaceBase.cpp [COMPLETE - C031-WIP507 / no new root]
139     90 CvGameCoreDLL/CyPlayerInterface2.cpp [COMPLETE - C031-WIP647 / NO NEW FINDING; F707 UNUSED]
140     89 CvGameCoreDLL/CvCivilization.cpp [COMPLETE - C031-WIP648 / NO NEW FINDING; F707 UNUSED]
141     84 CvGameCoreDLL/CvDLLEntity.cpp [COMPLETE - C031-WIP649 / NO NEW FINDING; F707 UNUSED]
142     84 CvGameCoreDLL/CyArtFileMgr.cpp [COMPLETE - C031-WIP650 / NO NEW FINDING; F707 UNUSED]
143     81 CvGameCoreDLL/CySelectionGroupInterface.cpp [COMPLETE - C031-WIP651 / NO NEW FINDING; F707 UNUSED]
144     80 CvGameCoreDLL/CvDLLPython.cpp [COMPLETE - C031-WIP652 / NO NEW FINDING; F707 UNUSED]
145     80 CvGameCoreDLL/CyDeal.cpp [COMPLETE - C031-WIP653 / NO NEW FINDING; F707 UNUSED]
146     80 CvGameCoreDLL/CyMessageControl.cpp [COMPLETE - C031-WIP654 / NO NEW FINDING; F707 UNUSED]
147     79 CvGameCoreDLL/CvInfo_TrueStarts.cpp [COMPLETE - C031-WIP655 / NO NEW FINDING; F613-F623 DEDUP; F707 UNUSED]
148     79 CvGameCoreDLL/CyHallOfFameInterface.cpp [COMPLETE - C031-WIP657 / F707 CONFIRMED; NO INDEPENDENT F708]
149     78 CvGameCoreDLL/CvBugOptions.cpp [COMPLETE - C031-WIP658 / NO NEW FINDING; F708 UNUSED]
150     76 CvGameCoreDLL/PlayerHistory.cpp [COMPLETE - C031-WIP659 / NO NEW FINDING; F708 UNUSED]
151     74 CvGameCoreDLL/WarEvalParameters.cpp [COMPLETE - C031-WIP508 / no new root]
152     73 CvGameCoreDLL/BarbarianWeightMap.cpp [COMPLETE - C031-WIP660 / NO NEW FINDING; F708 UNUSED]
153     73 CvGameCoreDLL/CyGameCoreUtilsInterface.cpp [COMPLETE - C031-WIP661 / NO NEW FINDING; F708 UNUSED]
154     73 CvGameCoreDLL/CyGlobalContextInterface2.cpp [COMPLETE - C031-WIP662 / NO NEW FINDING; F708 UNUSED]
155     62 CvGameCoreDLL/CyAreaInterface.cpp [COMPLETE - C031-WIP663 / NO NEW FINDING; F708 UNUSED]
156     60 CvGameCoreDLL/CyGlobalContextInterface3.cpp [COMPLETE - C031-WIP664 / NO NEW FINDING; F708 UNUSED]
157     57 CvGameCoreDLL/CyGameTextMgrInterface.cpp [COMPLETE - C031-WIP665 / NO NEW FINDING; F708 UNUSED]
158     44 CvGameCoreDLL/CyCityInterface2.cpp [COMPLETE - C031-WIP666 / NO NEW FINDING; F708 UNUSED]
159     43 CvGameCoreDLL/CyMapGeneratorInterface.cpp [COMPLETE - C031-WIP667 / NO NEW FINDING; F708 UNUSED]
160     39 CvGameCoreDLL/CvHallOfFameInfo.cpp [COMPLETE - C031-WIP668 / F707/KI#1028 DEDUP; NO NEW F708]
161     38 CvGameCoreDLL/CvGameCoreDLL.cpp [COMPLETE - C031-WIP669 / NO NEW FINDING; F708 UNUSED]
162     31 CvGameCoreDLL/CyArtFileMgrInterface.cpp [COMPLETE - C031-WIP670 / NO NEW FINDING; F708 UNUSED]
163     25 CvGameCoreDLL/CyMessageControlInterface.cpp [COMPLETE - C031-WIP671 / NO NEW FINDING; F708 UNUSED]
164     24 CvGameCoreDLL/CyHallOfFameInfo.cpp [COMPLETE - C031-WIP672 / F707/KI#1028 DEDUP; NO NEW F708]
165     20 CvGameCoreDLL/CyRandomInterface.cpp [COMPLETE - C031-WIP673 / NO NEW FINDING; F708 UNUSED]
166     18 CvGameCoreDLL/ModName.cpp [COMPLETE - C031-WIP674 / NO NEW FINDING; F708 UNUSED]
167     16 CvGameCoreDLL/PlotRadiusIterator.cpp [COMPLETE - C031-WIP675 / F708 CONFIRMED POST-PASS AT C031-WIP677]
168     11 CvGameCoreDLL/ArithmeticTraits.cpp [COMPLETE - C031-WIP509 / no new root]
169      5 CvGameCoreDLL/_precompile.cpp [COMPLETE - C031-WIP676 / NO NEW FINDING; F708 UNUSED]

INITIAL C010 SOURCE IDENTITY
Branch: main
HEAD: 5286fc13797d9abc5cef6655b0977f48016f9494
Practical commit count: 6225
Base comparison: local AdvCiv 1.14 source archive

INITIAL C010 PROGRESS
Files complete at C010: 9 / 169
Files open at C010: 1 / 169
Current file at C010: 010 CvGameCoreDLL/CvTeamAI.cpp (6,854 lines) - OPEN
New current defect roots established by this file-oriented pass: 23 (provisional KI#330-KI#352)
Existing known defect roots materially broadened/reopened: 8 (KI#312, KI#21, KI#213, KI#48, KI#306, KI#70, KI#74, KI#11)

LOCAL RECONCILIATION AFTER C010-WIP7 IMPORT
- The original audit baseline above remains the source identity for ChatGPT-5.6-Sol's cumulative checkpoint. GPT-5.6-Sol reconciled the imported WIP7 against current local HEAD f0d50e744 after the F001-F031 repair track and the reopened KI#48.3/KI#70/KI#74 CvCityAI batch.
- The new findings F001-F004 and F012-F031 have been independently promoted and fixed as KI#330-KI#352 except F012/KI#333 and F015/KI#336, which remain pending. The reopened F007/KI#48.3, F009/KI#70 and F010/KI#74 roots are now fixed too. F032-F047 remain provisional while CvTeamAI.cpp is open.
- WIP7 was generated from the older 5286fc137 snapshot and therefore repeatedly embeds superseded WIP layers and recovery cursors. This is intentional loss-prevention evidence rather than the desired durable presentation; finalized C011 should consolidate the open-file work into one current section.
- WIP7 again imported the previously rejected F024/F026/F027 ancestry changes: local Civ4CE BtS contains the same End Score helper reset/outer reuse, successful-Python-found-value omission and host-team air-capacity argument. The independently promoted BtS ancestry for KI#345, KI#347 and KI#348 remains authoritative.
- Reconciliation also restores the seven-finding F017-F022/F024 count and the open-file cursor to file 010. The new F045-F047 findings survive an initial current-tree structural check but remain provisional pending independent promotion. (GPT-5.6-Sol)

LOCAL RECONCILIATION AFTER C010-WIP22 IMPORT
- ChatGPT-5.6-Sol's cumulative WIP13-WIP22 handoff was generated from practical 6239 / HEAD cdbf743dc. Current local HEAD is practical 6243 / 6512cf098 after independently reviewed repairs through KI#363; current source and reviewed Known Issues remain authoritative.
- F032-F042 have been promoted and fixed as KI#353-KI#363. The WIP20 root normalization is accepted: F043, F044 and F046 are manifestations of F037's single practical-1842 `AI_preDeclareWar` population substitution, not independent roots. Current work completes their compiled and runtime-smoke-tested common repair under KI#358; provisional KI#364, KI#365 and KI#367 remain reserved merged aliases rather than reviewed KI entries.
- WIP22 adds explicit method-inventory coverage and strengthens F042/F048-F052 without introducing F053 or changing the normalized root set. F045/KI#366, broadened F047/KI#368, F048/KI#369, F049/KI#370 and F050-F052/KI#371-KI#373 remain pending independent current-tree review. WIP22 is still an open-file recovery checkpoint, not durable C011 completion; its embedded older authority blocks and superseded intermediate wording remain evidence only. (GPT-5.6-Sol)

NUMBERING RECONCILIATION
- The parallel archaeology/repair track assigned KI#327-KI#329 before C008 was integrated into the repository.
- C008's six provisional roots were therefore shifted from KI#327-KI#332 to provisional KI#330-KI#335. Stable F-numbers remain the preferred album identifiers until each candidate is independently reviewed and promoted.
- C009 adds ten CvGame.cpp roots as stable F015-F024 with provisional KI#336-KI#345 aliases. These remain audit candidates in this Album; README_Known_Issues.md intentionally remains the independently reviewed/promoted issue ledger.
- C010 adds seven CvPlot.cpp roots as stable F025-F031 with provisional KI#346-KI#352 aliases; no F032 was established during closure.

FILE 001 - CvGameCoreDLL/CvPlayerAI.cpp - 32,289 lines - REVIEWED / NEW FINDINGS

F001 - provisional KI#330 - supported-config master/vassal tech-contact power-bias exemption depends on an unrelated toggle
Status: NEW CURRENT SAS DEFECT ROOT, static evidence strong.
Evidence:
- AI_doDiplo initializes bVassalsOrMaster=false.
- That relation flag is set only inside SAS_AI_DO_DIPLO_TECH_TRADE_MASTER_FROM_TO_VASSAL_CONTACT_PERCENT_OPTIMIZE.
- The separately configurable SAS_AI_DO_DIPLO_POWER_TECH_TRADE_OPTIMIZE later skips its power modifier only when !bVassalsOrMaster is false.
- XML documentation for the power-bias option explicitly promises that the bias is not applied between members of a master-vassal(s) locus, and both options are independently enable/disable toggles.
Consequence:
- With preferential master/vassal contact disabled but power-based tech contact enabled, master/vassal or sibling-vassal contacts are incorrectly treated as ordinary rivals for the power modifier.
- Defaults mask the problem because both options are enabled.
Repair direction:
- Compute the master/vassal-locus relationship independently of the preferential-contact feature toggle; use that relation both for the contact bonus and for the power-bias exemption as appropriate.

F002 - provisional KI#331 - restrictive AI canScrap policy silently vetoes callers that assume CvUnit::scrap succeeded
Status: NEW CURRENT SAS CONTRACT DEFECT ROOT, cross-file; first proven from CvPlayerAI.cpp/CvUnit.cpp/CvTeam.cpp. CvUnitAI.cpp may broaden the caller set during file 002.
Root contract:
- CvUnit::scrap() returns void and silently returns when canScrap()==false.
- SAS canScrap now rejects most AI units, especially in recommended obsolete-only mode.
- Several inherited callers were written when canScrap was essentially only a fighting-state gate and therefore treat the call as if removal succeeded.
Confirmed consequence A - AI_doMilitary financial disband quota:
- Candidate collection uses AI_disbandValue and does not filter canScrap().
- The loop calls scrap(), calls doDelayedDeath(), then increments iDisbandCount regardless of whether scrap() actually set delayed death.
- A rejected candidate therefore consumes the per-turn disband quota and can prevent a later genuinely eligible obsolete unit from being considered.
- Current data makes this practical: a unit can already be cheapened by AI_disbandValue when its ObsoleteTech is known while canScrap still rejects it until the configured obsolete-era delay has elapsed.
Confirmed consequence B - AI_doTurnUnitsPost upgrade cleanup:
- The cleanup path calls scrap(), doDelayedDeath(), then unconditionally sets bKilled=true.
- If canScrap rejects the unit, doDelayedDeath is a no-op because scrap() never set delayed death, but the still-living unit is treated as killed and skips the later ordinary upgrade path.
Confirmed consequence C - capitulation nuke disarm:
- CvTeam::setVassal inherited advc.143b disarms a capitulating team by calling scrap() on every nuke.
- Recommended SAS obsolete-only AI scrapping rejects units with ObsoleteTech=NONE before later UnitAI exceptions are reached.
- Current UNIT_TACTICAL_NUKE and UNIT_ICBM both use UNITAI_ICBM and ObsoleteTech=NONE, so AI capitulation can retain nukes while a human capitulation still removes them because the restrictive gates are AI-only.
Repair direction:
- Routine optional disband/cleanup callers should prefilter canScrap and only consume quota / mark killed after successful removal (or the API could explicitly return success).
- Mandatory game-rule actions such as capitulation disarm should use a forced/internal removal path that is not blocked by routine AI preservation policy.
- Do not globally weaken canScrap merely to satisfy mandatory callers.

F003 - KI#312 broadened - cluster-first tech redirect is bypassed by the independent high-research-progress gold-purchase fallback
Status: EXISTING KNOWN ROOT, not counted as a new defect.
Evidence:
- AI_doDiplo independently selects eBestReceiveTech and eBestProgressTech from the outsider partner.
- The SAS master/vassal cluster-first block can suppress eBestReceiveTech by setting it to NO_TECH when it believes a locus partner is an internal source.
- The later advc.550f fallback runs when eBestProgressTech != NO_TECH && eBestProgressTech != eBestReceiveTech and can immediately buy that progress tech from the same outsider for gold.
- Therefore setting eBestReceiveTech to NO_TECH can make the same outsider tech satisfy the fallback comparison rather than suppressing external acquisition consistently.
Classification:
- Keep this under KI#312 because that issue already covers the shared master/vassal internal-tech-supply predicate and the mismatch between merely-known / apparently-willing internal sources and legally/actually transferable techs.
- A centralized internal-supply/acquisition predicate should govern every external acquisition route that is intended to obey cluster-first policy.

FALSE POSITIVES / CLEAN TAIL CHECKS FROM FILE 001
- AI_eventValue recursion warning: current CIV4EventInfos graph mechanically checked (339 events, 62 Additional/Clear edges); no non-self cycle/SCC exists. Dormant warning, not a current-data bug.
- Negative-production-cost Great People cannot fall into the normal AI_disbandValue cost formula: productionCost < 0 returns MAX_INT first.
- Deterministic disband arithmetic remains bounded under current unit data; no new overflow issue established.
- AI_balanceDeal, AI_counterPropose, AI_techUnitValue, AI_techReligionValue, AI_getGreeting, AI_stopTradingTradeVal, AI_maxGoldTrade, AI_doAdvancedStart and numerous small tail diffs are caching/signature/enum refactors with no semantic defect found.
- AI_isVictoryValid contains an unused cached Space-strategy define while still calling the original getter in that one branch. This is an incomplete micro-optimization only; behavior is unchanged and it is not promoted.
- AI_religionValue current power weighting was checked against KI#309; the earlier bloc-power duplication is already fixed by weighting each known player by that player power.
- Worker-demand/helper changes inspected here map to already-known KI#192/KI#198 lineage; no new defect established in file 001.
- Culture-victory strategic additions have later repair lineage already captured by the prior archaeology (including the shorter-active-victory-countdown fix); no new surviving root established in this pass.


FILE 002 - CvGameCoreDLL/CvUnitAI.cpp - 28,009 lines - REVIEWED / EXISTING ROOT STRENGTHENED

RESULT
- No third new defect root was established in CvUnitAI.cpp.
- File 002 substantially strengthens provisional KI#331 by proving that the restrictive canScrap policy is structurally incompatible with several inherited CvUnitAI cleanup callers.
- Existing pending KI#213 remains a real cross-file architecture defect involving CvUnitAI::AI_bestCityBuild and the deliberately empty land-side CvCityAI best-build cache; this file review did not duplicate it.
- The major current Settler, Worker, city-assault, Great Person, Great General, transport, promotion, and naval changes otherwise map to already-documented fixes/policies or survive current-state checks.

F002 / provisional KI#331 - CvUnitAI caller expansion
Historical interaction:
- The important ancestry is a cross-commit contract change, not a changed caller. Base AdvCiv's canScrap was broadly permissive outside a narrow fighting-state restriction, so inherited `scrap(); return;` code effectively meant "remove this unit and stop."
- Practical 5945 introduced the recommended restrictive AI obsolete-only policy. Practical 5979 simplified that policy further, but the old CvUnitAI callers remained.
- This explains why the earlier commit-oriented archaeology could inspect practical 5945 locally and miss the later current-tree consequence: the defect lives in the interaction between a new callee contract and unchanged old callers.
- Current no-site Settler cleanup provides a useful internal contrast: `AI_scrapSettlerWithNoValidSite` deliberately uses `scrapInternal()` after its own guards because generic Settler scrap policy would otherwise block the mandatory cleanup.

Confirmed CvUnitAI consequences:
1. Worker self-cleanup no longer removes Workers under the recommended mode.
   - Current Worker unit variants use `UNITAI_WORKER` and `ObsoleteTech=NONE`.
   - The AI_workerMove cleanup branch can call `scrap(); return;` after productive actions fail.
   - canScrap rejects these Workers, so the call can only burn that Worker's turn; it can also prevent later stranded/safety fallback in that update.

2. Excess land Explorers can enter a repeated no-progress branch.
   - In inherited AI_exploreMove, an excess explorer that has positive ATTACK value converts to UNITAI_ATTACK; otherwise it calls `scrap(); return;`.
   - Current Scout/Explorer-style units explicitly disallow UNITAI_ATTACK, so the conversion fallback can be unavailable.
   - Scout/Explorer/Airship can remain blocked until their configured obsolete delay permits scrapping.
   - The current Robotic Recon Drone is stronger proof: it is an EXPLORE land unit, disallows ATTACK, has a positive production cost, and has `ObsoleteTech=NONE`. Once it reaches the excess-explorer cleanup with no earlier action, generic scrap has no future eligibility escape, so the inherited branch can repeatedly no-op and return.

3. Escort Sea cleanup can be impossible exactly when invoked.
   - The inherited inferior-escort cleanup branch is reached while `getCargo()>0`.
   - If role conversion fails, it executes `scrap(); return;`.
   - Recommended canScrap rejects units carrying cargo, so this removal is categorically vetoed at that call site.

4. Explore Sea cleanup can repeatedly no-op for cargo-capable ships.
   - Inherited obsolete-role and excess-explorer cleanup branches call `scrap(); return;` after failed role-conversion paths.
   - Recommended policy rejects any cargo-capable unit even when empty because `cargoSpace()>0`.
   - A cargo-capable ship assigned EXPLORE_SEA can therefore satisfy the caller's cleanup decision while being categorically ineligible for generic scrap.

5. Barbarian trapped-ship cleanup can no-op.
   - Inherited AI_barbAttackSeaMove periodically detects a ship that cannot generate a useful path and calls `scrap(); return;`.
   - A cargo-capable Barbarian ship such as a Galley is categorically rejected by the new policy, turning the intended cleanup into a lost turn whenever that branch fires.

6. Settler transport retirement after unloading can never use generic scrap in recommended mode.
   - Inherited AI_settlerSeaMove can unload a now-inferior old transport and then call `scrap(); return;`.
   - The transport remains cargo-capable even when empty, so canScrap rejects it solely from cargo capacity.

7. Doomed-city capturable-unit scorched-earth cleanup can report success without removing the unit.
   - AI_retreatToCity's inherited evacuation fallback can probabilistically scrap a capturable unit in an evacuating own city and immediately `return true`.
   - Settlers/Workers are representative capturable units and have no ObsoleteTech.
   - Recommended AI canScrap therefore rejects the removal, yet the caller reports that it handled the unit; the civilian can remain in the doomed city and be captured.

Together with file 001's financial-disband quota, upgrade-cleanup, and capitulation-nuke cases, these are one contract root rather than separate KIs.

Suggested root repair remains:
- Optional cleanup: check `canScrap()` before treating the decision as completed, and only consume quotas / return success / mark killed after actual removal.
- Mandatory removal: use an explicit forced/context-specific internal removal path.
- If practical, make the public scrap operation return success so callers cannot silently confuse a veto with removal.
- Do not weaken global canScrap just to accommodate mandatory callers.

MAJOR SUBSYSTEMS REVIEWED / NO ADDITIONAL ROOT

Settlers / founding:
- `AI_settleMove`, `AI_foundFirstCity`, `AI_found`, `AI_foundFollow`, `AI_unloadSettlerCargoForDestination`, and no-site cleanup were checked as a family.
- Current logic corresponds to KI#144, KI#179, KI#180, KI#185, KI#192, KI#194, and KI#215.
- Destination-aware Worker cargo accounting is internally consistent for the intended founding voyage; no deterministic multi-transport failure was established.
- The dangerous-unescorted and clearly-better-site founding guards return control to normal replanning rather than forcing a stale mission.

Worker job selection / mobility:
- `AI_workerMove`, `AI_bestCityBuild`, `AI_nextCityToImprove`, `AI_improveBonus`, `AI_improvePlot`, `AI_improveLocalPlot`, `AI_ferryWorkers`, `AI_findTransport`, `AI_handleStranded`, and `AI_pickupStranded` were reviewed together.
- The customized candidate pipeline initializes candidate/output state, rejects NO_BUILD before use in the important paths, sorts before expensive path/reservation checks, and revalidates the concrete selected job enough for the same-turn execution path.
- Completed-area Worker evacuation and Settler-transport reload prevention map cleanly to KI#192.
- Off-BFC sea-bonus handling and stale Work Boat skipping map to KI#157.
- `AI_betterPlotBuild` contains a superficially suspicious resource-route concept, but the earlier archaeology already chased the active resource-connection callers and proved current compensation. It remains a robustness/comment concern, not a current defect.
- `AI_improvePlot`'s direct "first buildable route" choice on an improved bonus could be tuned more elegantly, but no concrete correctness failure was established; not promoted.
- KI#59 remains the older empirical "partially improved" Worker-distribution note rather than a newly isolated static root.
- KI#213 remains pending and is explicitly not duplicated: the current Worker policy recognizes valid land jobs that the deliberately empty legacy city-side land cache can hide from Worker-demand consumers.

Worker XML/config parser and irrigation helpers:
- The file-scope no-bonus Worker branch parser and improvement-list caches were audited separately from member methods.
- All 28 current `SAS_WORKER_AI_BASE_BRANCH_*` definitions referenced by the code resolve to existing Build types.
- All 8 current configured Worker improvement-list / chain-start definitions inspected resolve to existing Improvement types.
- Parser null/invalid-token paths have runtime guards after assertions and do not index invalid Build values.
- Generic branch scoring bounds-checks BuildTypes before indexing its optional adjustment vector.
- Deterministic irrigation-chain search contains the fixed KI#214 state model: best cost is tracked per `(plot, steps)` rather than one cost per plot, with a bounded `(maxPlots + 1) * numPlots` state space and generation-stamp reset handling.

Promotion policy:
- `AI_promotionValue`'s large SAS hard-policy layer was reviewed as policy, not assumed defective because it is aggressive.
- Optional type lookups use sentinel-safe resolution and the outer promotion validity guard prevents missing optional promotion names from matching a real promotion.
- The large positive/negative policy sentinels are safely separated from ordinary promotion-value arithmetic in current use.
- This remains documented KI#47 behavior; no current all-options-blocked state or other correctness defect was proved.

City assault / combat organization:
- `AI_attackCityMove`, `AI_attackMove`, `AI_guardCity`, `AI_omniGroup`, `AI_pickTargetCity`, and the file-scope barbarian-expedition / targetless-peacetime helper family were reviewed together.
- Current changes map mainly to KI#104, KI#155, KI#156, KI#158, the KI#188 family, KI#190, and later diagnostic/hardening work.
- KI#217 is genuinely fixed in current source: `SAS_getBestUpgradeForLog(CvUnit const&)` derives the iterated unit's own `AI_getUnitAIType()` instead of reusing the attack-stack head role.
- Targetless-peacetime aggregate release and central merge caps now operate across same-plot ATTACK_CITY-led groups; the current selection-group no-progress tripwire repair (KI#319) also prevents role conversion/head detachment from falsely skipping the remaining old group.
- The KI#190 Space-capital emergency target is limited to an active launch countdown and a reachable current capital; pre-launch team-wide project progress is not incorrectly treated as capital-local.
- No new arithmetic/sentinel/target-lifetime defect was established in these current paths.

Great People / Great Generals:
- `AI_greatPersonMove`, `AI_generalMove`, `AI_join`, `AI_construct`, and `AI_lead` were checked against their documented issue lineage.
- Current Great-Person target continuity/action classification corresponds to KI#154, KI#167, KI#168, KI#169 and KI#170.
- Current Great-General travel/construction/join safety is the post-KI#204 hardened version, including the corrected inherited inverted safe-city test.
- `AI_generalMove` contains a redundant second Military Academy attempt after the first fails with no intervening state change. It is behaviorally inert and not promoted.

Naval / transport:
- `AI_assaultSeaMove` / `AI_assaultSeaTransport` include the KI#193 / KI#188.5 distant-disposable Barbarian-target gate.
- `AI_settlerSeaTransport` and the destination-aware unload helper preserve needed cargo while avoiding the KI#215 unrelated-cargo unload bug.
- The ordinary transport/stranded helper changes preserve their base fallback structure.
- The significant surviving naval concern from this file is the KI#331 scrap-contract interaction described above, not a separate target/pathfinding root.

Upgrade routing:
- `AI_upgrade(int iMaxUpgradePrice)` is the expected bool-return / price-cap extension; default/unbounded callers retain ordinary behavior.
- `AI_travelToUpgradeCity` now asks for the exact selected upgrade and releases units whose real current upgrade gate fails while preserving foreign-territory travel where `canUpgrade` can fail for positional reasons.
- This matches KI#188.3.3; no new current defect established.

FALSE POSITIVES / NON-FINDINGS RECORDED FOR FILE 002
- `AI_attackOdds` is not a giant changed hotspot. A brace-aware extractor proves the current 85-line member is behaviorally/textually unchanged from AdvCiv 1.14; the earlier rough parser had accidentally absorbed nearby file-scope helper code.
- `AI_betterPlotBuild` resource-route inversion was already investigated through real callers in the prior archaeology and remains compensated in current behavior.
- The second immediate `AI_construct(1)` in Great-General logic is redundant only.
- Current Worker XML branch/improvement strings contain no unresolved configured Build/Improvement identifiers.
- Promotion hard rules are intentionally strong policy; no evidence supports relabeling them as a defect.
- The generic partially-improved Worker-distribution KI#59 is not treated as a new precise static finding.
- No additional current CvUnitAI defect root was manufactured merely from a changed heuristic or tuning preference.


FILE 003 - CvGameCoreDLL/CvGameTextMgr.cpp - 22,412 lines - REVIEWED / NEW FINDING + EXISTING ROOT REOPENED

RESULT
- One new current defect root was established: provisional KI#332, a wrong civilization-context convention in multiple leader-hover callers.
- Existing KI#21 should be reopened/broadened: live-unit map help prints raw impassable terrain/feature flags without applying current passable-tech state, so it can contradict actual movement legality.
- The previously noted self-referential default-specialist cache is real source code but currently masked exactly by data and was already captured by the old archaeology; it is not counted again.
- All 34 current member functions with real executable/textual changes versus AdvCiv 1.14 were classified, and the file-scope/header surface was checked separately. No additional current defect root was established.

F004 - provisional KI#332 - leader hover callers pass literal civilization 1, so trait help can resolve another civilization's units/buildings
Status: NEW CURRENT SAS UI DEFECT ROOT, current-data proof strong.
Native contract:
- CvDLLWidgetData::parseLeaderHelp treats widget data2, when not -1, as a CivilizationTypes value and passes it to CvGameTextMgr::parseLeaderTraits.
- parseLeaderTraits forwards that civilization to parseTraits.
- parseTraits uses the supplied civilization to resolve trait fast-production UnitClass/BuildingClass entries to civilization-specific UnitTypes/BuildingTypes.
Current caller problem:
- Multiple Python WIDGET_PEDIA_JUMP_TO_LEADER callers pass literal data2=1 rather than the leader/player's civilization.
- Current civilization index 1 is CIVILIZATION_ARABIA, so those hovers resolve fast-production classes through Arabia regardless of the displayed leader.
- Current literal-1 callers occur in Scoreboard, Hall of Fame, Info Screen, Military Advisor, and several Sevopedia pages (Religion, Civic, Music).
Concrete current consequence:
- Shaka is Aggressive and Zulu; the Zulu Barracks-class replacement is the Ikhanda, and that class receives the Aggressive double-production trait effect.
- Arabia has no Barracks-class replacement. A literal-Arabia context therefore makes Shaka's trait hover identify ordinary Barracks instead of his actual Ikhanda for that fast-production line.
- Equivalent wrong-resolution opportunities exist for leaders whose civilization replaces other trait-affected classes.
Ancestry/accounting:
- The old commit-oriented archaeology had already source-proven this interaction while examining a later caller, but deliberately held it unnumbered pending firmer ancestry.
- Local Base AdvCiv 1.14 retains literal data2=1 in its inherited BUG Scoreboard and Sevopedia Index leader widgets under the same DLL contract. Practical 4732 is an early SAS-lineage occurrence, not the upstream birth of the convention. Current SAS inherited the Scoreboard case, independently replaced/corrected the Index path and later broadened the wrong convention across additional screens.
Repair direction:
- Callers that know the real player/civilization should pass that CivilizationTypes value.
- Generic leader contexts should reuse the same canonical leader-to-civilization selection used by the main Sevopedia Leader path.
- Do not blindly substitute -1: parseLeaderHelp currently uses data2==-1 to suppress leader hover help entirely.

F005 - KI#21 reopened/broadened - live-unit Cannot Enter help ignores current passable-tech state
Status: EXISTING KNOWN ROOT, not counted as a new defect.
Evidence:
- The actual-unit setUnitHelp path lists pUnit->getTerrainImpassable / getFeatureImpassable directly when building the map-view Cannot Enter line. These are raw unit-info restrictions.
- The detailed/basic Sevopedia unit help separately understands TerrainPassableTech / FeaturePassableTech and can render an Until Tech relationship.
- CvUnit::canMoveInto permits a raw impassable terrain/feature once the unit's team owns the configured passable tech; own-team cultural-border exceptions can also make the raw flag insufficient by itself.
Concrete current data:
- UNIT_WORKBOAT has TERRAIN_OCEAN marked impassable and TECH_ASTRONOMY as its TerrainPassableTech.
- After the Work Boat's team researches Astronomy, the engine allows Ocean movement while the live-unit hover can still print Cannot Enter: Ocean from the raw flag.
Classification:
- This is an incomplete dynamic-state part of KI#21's map-view Cannot Enter display feature, not a distinct root.
- An older archaeology note had concluded no current raw-impassable + pass-tech contradiction existed; current Work Boat XML disproves that old data conclusion and should supersede it.
Repair direction:
- Build live-unit restriction text from effective current movement state, or at minimum account for the team's passable tech before printing the raw restriction. Preserve the cultural-border nuance as applicable.

FALSE POSITIVES / NON-FINDINGS RECORDED FOR FILE 003
- The source contains `static const SpecialistTypes eDefaultSpecialist = (SpecialistTypes)eDefaultSpecialist;` instead of GC.getDEFAULT_SPECIALIST(). Static zero-initialization makes it enum 0; current enum 0 is SPECIALIST_CITIZEN, which is also the current default specialist. The typo is presently exactly masked and was already recorded by the old archaeology; not promoted again.
- The custom building-power help contains an explicit clean-or-dirty uncertainty branch. Engine tracing resolves the rule: area clean power overrides dirty-power health classification. Current XML has only Three Gorges Dam with area clean power, and it is not itself dirty, so the uncertain simultaneous area-clean+dirty branch is unreachable in current data. No new root.
- Static Coal Plant-style health prose can be context-approximate when a city is already area-clean-powered, but this is inherited/static-help behavior and no SAS-created correctness root was established.
- `buildBRepeatString` is coherent with repeatable-tech ownership semantics; current Tech XML contains no bRepeat=1 entries anyway.
- Civilopedia suppression of generic obsolete/replacement/requirement prose was checked against the dedicated Sevopedia panels; the corresponding relationships remain represented, including Manhattan Project project requirements.
- Worker-speed summary uses the same player-wide accumulator fed by current Tech/Civic/Building worker-speed modifiers; no category mismatch established.
- Zero-base collateral capability rows remain intentional KI#324/KI#324.2 behavior and were not reopened.
- Promotion icon-size plumbing, null guards, billboard colors, score/trade/event/espionage helpers, and numerous small changed methods are presentation/caching/hardening changes with no new semantic defect found.
- No additional file-scope SAS helper layer exists in CvGameTextMgr.cpp that would sit outside the member-function comparison.


FILE 004 - CvGameCoreDLL/CvPlayer.cpp - 20,672 lines - REVIEWED / CLEAN CURRENT

RESULT
- No new current defect root was established in CvPlayer.cpp.
- All 104 member-function pairs with executable/token-level differences versus AdvCiv 1.14 were classified; the only genuinely new CvPlayer member is the key-early-strategic-bonus helper.
- CvPlayer.h was compared at token level after stripping comments/formatting. Its only semantic addition versus AdvCiv 1.14 is the declaration of that helper; the many apparent header differences are signature collapsing/comment movement.
- File-scope additions were audited separately. They are the SASGameRecord diplomacy text/logging helpers and do not alter gameplay state.
- The file therefore closes as a useful clean result rather than manufacturing a finding from tuning, caching, logging, or unsupported old-save compatibility.

CROSS-CONTRACT / DATA CHECKS
1. Save read/write cleanup is internally paired for the supported current format.
   - CvPlayer::write still writes uiFlag 22.
   - Current CvPlayer::read consumes the same current-format fields that AdvCiv 1.14 read when uiFlag==22; removed branches are old migration/legacy representations.
   - Same-version serialization therefore remains paired. The project does not promise cross-version save compatibility, so accepting an older save without migration is not promoted as a supported-current defect.

2. Static GlobalDefine caching does not currently freeze a dynamically changed CvPlayer value.
   - A mechanical scan collected the GlobalDefine names read by current CvPlayer.cpp and compared them against current runtime setDefineINT/setDefineFLOAT/setDefineSTRING usage in C++ and Python/map scripts.
   - No overlap was found for the 103 define names used by this file.
   - The static-local caches therefore preserve current runtime semantics under today's code/configuration.

3. New key-early-strategic-bonus helper is explicit tuning, not a hidden contract mismatch.
   - getNumAvailableBonusesHaveAnyKeyEarlyStrategicBonuses checks Copper, Iron, Horse, Camel and Elephants through getNumAvailableBonuses.
   - All five define-resolved current BONUS_* names exist in CIV4BonusInfos.xml and the helper guards NO_BONUS.
   - getNumAvailableBonuses is capital-plot-group scoped; comments on both declaration and implementation explicitly call out that capital-network-only approximation.
   - Current callers use the helper to relax early siege composition/production caps. The approximation can make an isolated city's local options differ from the player-level signal, but that limitation is documented and intentional tuning rather than a newly proven correctness failure.

4. TechAcquisitionCause arguments added at CvPlayer call sites are bookkeeping context only.
   - Current CvTeam research helpers forward the cause to setHasTech when progress completes a technology.
   - CvTeam::setHasTech forwards it to CvEventReporter::techAcquired; CvEventReporter uses it for SASGameRecord provenance while preserving the original Python tech-acquired event signature.
   - No research/ownership legality branch depends on the cause enum.

5. Diplomacy trade-row art additions resolve against current data.
   - INTERFACE_TECH_VASSAL, INTERFACE_TECH_OPENBORDERS, INTERFACE_TECH_DEFENSIVEPACT and INTERFACE_TECH_PERMALLIANCE all exist in current CIV4ArtDefines_Interface.xml.
   - Target-team leader icons retain the pre-existing valid-team assumption and guard NO_PLAYER/dead leaders before dereferencing the player/leader art.

6. SASGameRecord hooks inspected from this file are observational.
   - updateSASGameRecordPlayerTurnState increments only GameRecord accounting arrays.
   - plot-change hooks buffer/log old-vs-new plot state after the gameplay mutation; they do not mutate the plot.
   - diplomacy helpers format already-supplied event context and call the logger.
   - technology acquisition source plumbing is likewise observational as described above.

EXISTING-FIX / INTENT MAPPINGS CONFIRMED
- canTrain ObsoleteTech enforcement maps to KI#93.
- Human-only production/Great-Person rounding removal maps to KI#67.
- Great-General safe-city birth selection maps to KI#204 and preserves SyncRandNum consumption before filtering.
- Defensive city-name lookup guards map to KI#161; the separate translated-name identity problem remains KI#325 rather than a new CvPlayer finding.
- getEventTriggerWeight now compares actual HandicapInfo iDifficulty values rather than enum-index*10. Current handicap values are 18,26,34,...,98, so the change is a real correctness improvement, not a regression.
- War-declaration cause arguments and gWarLogLevel substitutions are diagnostic/context plumbing; no changed war-legality path was found here.

FALSE POSITIVES / NON-FINDINGS RECORDED FOR FILE 004
- Free-promotion storage initially looked like a UnitCombat->UnitClass contract replacement because the rough method matcher paired same-named overloads incorrectly. AdvCiv 1.14 already has both UnitCombat and UnitClass free-promotion arrays/overloads. Current CvUnit initialization, random-event application, and save read/write preserve both systems. Parser-overload false hotspot only.
- CvPlayer::read's large line-count delta is dominated by deleting legacy uiFlag migration branches. Current uiFlag-22 layout remains paired with write; no same-version serialization defect established.
- Static define caching was checked against actual runtime define mutation rather than treated as a theoretical risk; no current overlap exists.
- getItemTradeString's treaty/target portrait additions were checked against current art definitions and existing target-team contracts; no bad art key or new invalid-player dereference established.
- getSASGameRecordDiploData1Text has initially confusing player naming, but logSASGameRecordDiploEventAction passes `getID()` as its first player. CONVERT therefore logs the requesting player's state religion and REVOLUTION the requesting player's favorite civic, matching handleDiploEvent semantics.
- The capital-network strategic-bonus helper has a consciously documented approximation. No deterministic wrong-state outcome strong enough to override that design intent was established in this file.
- No new defect was promoted merely because a current method differs substantially in formatting, caching, diagnostics, enum-source annotation, or an intentional AI/game-balance policy.


FILE 005 - CvGameCoreDLL/CvCityAI.cpp - 19,615 lines - REVIEWED / EXISTING ROOTS BROADENED OR REOPENED

RESULT
- No independent new defect root was established in CvCityAI.cpp; this file instead exposed several incomplete existing fixes/contracts that are better kept under their original KI families.
- Existing KI#213 is materially broadened from a Worker-demand reporting/cache mismatch into production, transport, relocation and evacuation consequences.
- Existing KI#48 should gain a focused follow-up (recommended KI#48.3): the building-value prefilter's growth signal subtracts unhealthiness twice and does not perform its own documented final zero clamp.
- Existing KI#306 is reopened/broadened: the government-center maintenance cache was fixed, but the sibling top-production-city cache retains the same insufficient within-turn invalidation model and can feed hard Wonder eligibility gates.
- Existing KI#70 is reopened: the forced-specialist sanitizer checks current assigned-specialist validity before deciding whether to trim an invalid forced count, allowing the historical invalid Artist/assert state to remain reachable.
- Existing KI#74 is reopened/broadened: AI_bestSpreadUnit still returns an enum comparison against NULL, so NO_UNIT (-1) is reported as Boolean success; caller guards mask much of the damage but do not repair the helper contract.
- Existing KI#11 is broadened: the current debug-map Actual/Standard Build Values display still calls AI_getImprovementValue, which now deliberately returns zero for every land plot.
- The large production, building, citizen, Worker, Settler, military, hurry/draft, process/project, improvement, helper and serialization surfaces otherwise survived the current-data/cross-contract review without another correctness root being manufactured.

F006 - KI#213 broadened - split Worker-demand architecture can now suppress or evacuate Workers, not merely undercount demand
Status: EXISTING KNOWN ROOT, materially stronger current consequences.
Root recap:
- Current land Worker planning is intentionally handled by CvUnitAI::AI_bestCityBuild rather than the old city-side land evaluator/cache.
- AI_updateWorkersHaveAndNeeded nevertheless still derives important demand signals from the old city-side best-build state plus partial compensating heuristics.
- A worked, unimproved no-bonus Flood Plains tile is a concrete current job that the custom Worker policy can improve but the city-side demand workaround can miss, yielding AI_neededWorkers(area)==0 despite useful land work remaining.
Newly proven consequences in the current tree:
1. Concrete CvCityAI::AI_chooseUnit can treat AI_neededWorkers(area)==0 as an area-usefulness veto and suppress Worker production.
2. CvUnitAI transport/relocation logic can use that same false zero to reject ferrying or unloading a Worker into the area.
3. Existing Workers in the area can be classified as belonging to a completed area and become candidates for the KI#192 completed-area evacuation machinery even though the custom land evaluator still sees useful work there.
Classification / repair direction:
- Keep one KI#213 root. The right repair is a shared authoritative Worker-job/demand predicate (or demand source) that sees the same land jobs as the custom Worker planner; patching each consumer independently would preserve the architectural split.

F007 - KI#48 broadened; recommended KI#48.3 - AI_buildingValue effective-food signal double-counts unhealthy-food loss
Status: EXISTING BUILDING-VALUE FAMILY, current implementation defect confirmed.
Current code:
- AI_buildingValue obtains `iFoodDifference = foodDifference(false, true)` and `iHealthLevel = goodHealth() - badHealth() + ...`.
- It then computes `iEffectiveFood = max(0, iFoodDifference) - max(0, -iHealthLevel)` and only later forces zero for nonpositive happiness surplus.
Engine contract:
- CvCity::foodDifference(..., true) subtracts foodConsumption().
- foodConsumption() already incorporates healthRate(), so unhealthy population has already reduced iFoodDifference before AI_buildingValue receives it.
- Subtracting max(0,-iHealthLevel) therefore charges the same unhealthiness a second time.
- The nearby worked example for a starving/unhealthy city explicitly says the result should be clamped to zero conceptually, but the implementation has no final `max(0, ...)` after the health subtraction and can leave iEffectiveFood negative.
Consequence:
- iEffectiveFood is not merely descriptive; it feeds hard force/skip gates for food-kept and health-building decisions. Thresholds can therefore flip because a mathematically wrong growth signal is used before the ordinary detailed building valuation runs.
Ancestry / classification:
- This logic was introduced in practical 4950 as part of the broad KI#48 building-value prefilter family. Treating it as KI#48.3 is cleaner than assigning a new root number.
Repair direction:
- Do not subtract current unhealthiness again from foodDifference when bIgnoreFood=false / food consumption is included. Derive the intended growth signal from one authoritative net-food quantity, then apply the documented nonnegative clamp and happiness gate.

F008 - KI#306 reopened/broadened - sibling top-hammer city cache can go stale during sequential city turns
Status: EXISTING CACHE-INVALIDATION ROOT; earlier repair was partial.
Current cache:
- AI_buildingValue keeps static per-player best/second/third base-production values keyed only by game turn and number of cities.
- Those rankings feed exact hard Wonder filters, including top-2/top-3 and +5-hammer boundary tests.
Concrete within-turn invalidation:
- Building completion can change another city's getBaseYieldRate(YIELD_PRODUCTION) without changing the turn or city count.
- Current Angkor Wat and Cristo Redentor each grant +1 production to Priest specialists through SpecialistYieldChanges.
- CvPlayer building processing applies those effects through changeSpecialistExtraYield(); that calls updateExtraSpecialistYield() across the player's cities, and CvCity::updateExtraSpecialistYield changes each affected city's base yield rate immediately when Priests are assigned.
- City turns/production choices are sequential. A top-HPT cache populated before such a completion can therefore be consumed later in the same player turn after one or more city production values have changed.
Consequence:
- Even a one-hammer stale ranking can cross the prefilter's exact top-city/+5 thresholds and change whether a later city is allowed to consider a Wonder.
Ancestry / classification:
- KI#306 fixed the same practical-5281 invalidation pattern for the government-center maintenance ranking cache while explicitly leaving the separate top-production cache in place. Reopen/broaden KI#306 rather than create a new cache root.
Repair direction:
- Avoid a turn+city-count cache for values that can change during city processing, or invalidate/recompute on relevant yield changes. A generation/revision counter for player city production state would be more precise than adding more ad hoc conditions.

F009 - KI#70 reopened - forced-specialist sanitizer can skip an already-invalid forced count
Status: EXISTING BASE/ADVCIV CITIZEN ROOT, current failure remains reachable.
Current sanitizer shape:
- AI_assignWorkingPlots enters its repair block only when `!isSpecialistValid(eSpecialist)`.
- Inside that block it trims assigned specialists above max capacity and separately trims forced specialists above max capacity.
Contract mismatch:
- isSpecialistValid(e, 0) tests whether the city's current assigned count can remain valid; it does not mean the independent forced-specialist target is <= capacity.
Concrete state:
- Artist assigned=0, max=0, forced=1: current assignment is valid, so the outer sanitizer condition is false and forced=1 is not trimmed.
- The later forced-specialist assignment pass sees the forced target and can assign the Artist anyway, after which the historical KI#70 debug assertion is reached because assigned=1 exceeds capacity=0.
- In non-assert builds this is not purely diagnostic: the invalid forced assignment can exist transiently.
Classification / repair direction:
- Reopen KI#70. Sanitize assigned count and forced target against capacity independently; do not gate forced-count repair on validity of the current assigned count.

F010 - KI#74 reopened/broadened - AI_bestSpreadUnit Boolean return still treats NO_UNIT as success
Status: EXISTING BASE/ADVCIV HELPER-CONTRACT ROOT, caller guards only mask it.
Current code:
- AI_bestSpreadUnit ends with `return (*eBestSpreadUnit != NULL);`.
- UnitTypes uses NO_UNIT=-1; NULL is integer/pointer zero in this comparison context.
- Therefore a caller that initializes `eBestSpreadUnit=NO_UNIT` and receives no candidate gets `(-1 != 0) == true`.
Current consequences / masking:
- Current production and governor callers contain assertions and explicit `eBestSpreadUnit != NO_UNIT` guards before actually trying to choose the unit, preventing the worst queue/action consequences.
- Those guards explain why the issue can look addressed, but the helper's Boolean contract itself remains false and the corresponding debug assertion remains reachable when no unit is selected but the helper reports success.
Classification / repair direction:
- Reopen KI#74 rather than add a new finding. The root fix is to compare against NO_UNIT (or return `iBestValue > 0` / an explicit success state consistently), then keep caller guards as defense in depth.

F011 - KI#11 broadened - land Actual/Standard Build Values debug help is structurally empty under the custom Worker evaluator split
Status: EXISTING DEBUG-MAP DISPLAY ROOT, no gameplay effect.
Evidence:
- Current CvCityAI::AI_getImprovementValue explicitly returns 0 (and NO_BUILD when requested) immediately for every non-water plot so the custom CvUnitAI land Worker policy has exclusive control.
- CvGameTextMgr's debug-map `Actual Build Values` and `Standard Build Values` paths still call AI_getImprovementValue for the working city and candidate improvements.
Consequence:
- On land, those diagnostics no longer describe the values driving the real Worker AI and can show empty/zero build-value information even when the custom Worker planner has a strong valid job.
Classification / repair direction:
- Keep under KI#11's existing inaccurate/weird debug-map display umbrella. If these diagnostics are retained, point them at the authoritative custom land evaluator (or clearly label the legacy water-only evaluator) rather than reviving the old land decision path.

AUDIT NOTE - direct Artist culture cache has an invalidation mismatch but is not promoted
- SAS_AI_directArtistCultureValuePercent caches one result per turn/player/city.
- AI citizen assignment can run before city turns and then again after that city's doCulture/doPlotCulture work in the same game turn, so the second call can reuse a pre-culture control/contestation verdict.
- A city's own culture update moves the relevant control tests toward more local control, so the demonstrated stale direction chiefly delays the intended Artist dampening until the next turn.
- This is a real cache-granularity/one-turn-lag observation, but no hard invariant or sufficiently serious current consequence was established. Keep as an audit note unless empirical evidence shows larger impact.

FALSE POSITIVES / NON-FINDINGS RECORDED FOR FILE 005
- Always-Peace ICBM production: apparent missing local guard is safely duplicated lower down. AI_nukeWeight returns zero when AI_isWarPossible() is false, so the high-priority nuke-production branch does not become reachable merely because its immediate block lacks `!bAlwaysPeace`.
- Concrete AI_chooseUnit can syntactically end in `return true` after a guarded pushOrder. Current two-argument callers always supply a real UnitAI and the substitution path does not clear a valid unit, so no supported current no-push/success path was established.
- A filtered diff made the small-city AI_jobChangeValue Artist restriction look as if it rejected every job. Full source retains `if (new_job.first)`, so the hard rejection applies only to specialist candidates; plots remain evaluable.
- Preferred non-combat food-production delegation was checked for mutual deferral. A candidate must complete the same unit no slower than the current city, and deterministic population/stagnation/tie ordering prevents a supported mutual-defer loop; no queue deadlock established.
- The `/*bFuture=*/true` label on getAdditionalHealthByBuilding is semantically wrong: the parameter is bAssumeStrategicBonuses. Current-data tracing does not turn that comment error into a separate gameplay defect. Furusiyya Maydan is the only building whose positive strategic BonusHealth can by itself flip intrinsic health classification, but its Stable gate already rejects it without Horse/Camel/Elephants; Hammam is health-positive without Marble; Factory/Industrial Park strategic effects are negative; resource-powered plants concern their required strategic resources.
- AI_strictAdditionalHappy was compared against CvCity::getAdditionalHappinessByBuilding and current building XML. It intentionally computes a narrower reliable subset for the hard prefilter; no current omitted effect was shown to violate that stated contract.
- All current hardcoded/configured type names used by the reviewed production/building helper layer resolve: the Horse/Camel/Elephants bonuses, Mounted Combat tech, Stable/Palace/Heroic Epic building classes, Renaissance era, six specialist IDs and Siege combat type.
- The large concrete AI_chooseUnit fallback/substitution helper has a stale cautionary comment about booleans not being recomputed after unit replacement. Current downstream naval/civilian booleans are computed after substitution; the earlier siege/defender booleans are consumed before substitution. No current stale-boolean action was established.
- Current culture-victory city rank/investment members are intentionally transient. They initialize to zero and are refreshed by CvPlayerAI's commerce-weight update before the relevant AI city-turn decisions; omission from CvCityAI serialization is not a same-version read/write mismatch.
- Wonder prefilter arithmetic was checked for zero/negative turn denominators. Building production modifiers are engine-clamped sufficiently that the constructed multiplier remains positive, and explicit max/validity guards cover the new estimates. Rival-tech rejection is an intentionally conservative policy approximation, not represented as canConstruct legality.
- AI_yieldValue, AI_permanentSpecialistValue, AI_buildUnitProb, AI_processValue, AI_minDefenders, hurry/draft, barbarian production, improvement valuation, culture pressure, specialist/yield helpers, threat/defender helpers and the remaining changed-method tail were reviewed for new sentinel/arithmetic/contract failures; changes that survived amount to policy, caching, diagnostics, or already-known issue lineage.
- Whole-file sentinel/static-cache scan found no additional enum-vs-NULL defect beyond KI#74 and no additional behavior-sensitive mutable static cache beyond the KI#306 top-hammer cache plus the low-impact Artist note above.
- No independent new CvCityAI defect root was manufactured merely because a heuristic is aggressive or could make a suboptimal strategic choice in an edge case.


FILE 006 - CvGameCoreDLL/CvCity.cpp - 13,834 lines - REVIEWED / NEW FINDING + EXISTING ROOTS CONFIRMED

RESULT
- One new current defect root was established: provisional KI#333, a priority-order regression between the emergency Port/water-hammer branch and the older emergency-defense branch in CvCity::doTurn.
- The large no-production / emergency-production additions otherwise map to existing KI#51, KI#310, KI#313 and related policy work or survive current cross-contract checks.
- Production overflow/completion GameRecord hooks were verified as observational/accounting-only and preserve inherited gameplay/event ordering.
- Current CvCity serialization is internally paired: a mechanical current read/write scan found 178 corresponding serialization operations in the same order.
- No additional current defect root was established in the remaining 32 changed member functions, anonymous helper block, header surface, configured type names, or static-define cache pass.

F012 - provisional KI#333 - emergency Port priority can preempt the documented stronger emergency-defense branch
Status: NEW CURRENT SAS AI-PRODUCTION PRIORITY DEFECT ROOT, birth/history/current-data evidence strong.
Birth and documented contract:
- Practical 5919 / bd2153482f introduced the mature-coastal emergency water-hammer / Port branch.
- The same birth commit's GlobalDefines documentation states that defense emergencies still run earlier in CvCity::doTurn so Castle/defense can go before Port when needed.
- The same practical's Main Changes Guide likewise says immediate danger / defense logic still runs earlier, allowing Walls/Castle to go first when needed.
Current control flow:
- CvCity::doTurn actually runs the emergency Harbor block, then the practical-5919 Port/water-hammer block, and only afterward enters the Walls/Castle emergency-defense block.
- The Port branch is gated by `!bDanger`, but not by `!bAtWar`, `!bEnemyStrong`, or the broader defense predicate.
- On success SASTryEmergencyBuilding pushes Port and sets `bEmergencyBuilding=true`.
- The later defense section is inside `if (!bEmergencyBuilding && !bHuman)`, so a successfully forced Port suppresses the entire defense branch for that turn/queue choice.
Concrete supported state:
- Mature ocean-coastal AI city, Navigation known, Port absent and constructible, sufficiently low production or food surplus, not locally flagged by AI_isDanger(), while its team is at war with a significantly stronger enemy (or otherwise satisfies the broader at-war defense predicate).
- Port passes its local gate and is forced first.
- The defense predicate would have requested Walls/Castle through bEnemyStrong / bAtWar, but is never evaluated because Port has already set bEmergencyBuilding.
- Current Port is a normal positive-cost Navigation building and Walls remains independently constructible in overlapping eras, so this is not a theoretical unsupported XML state.
Why the old commit-oriented pass missed it:
- The earlier archaeology inspected practical 5919 locally and correctly found the Port legality helper itself safe for mature ocean-coastal, non-danger cities.
- The defect is an insertion-order interaction with an older neighboring branch, exactly the kind of cross-commit/current-file interaction this second file-oriented pass is intended to expose.
Repair direction:
- Make the actual priority match the documented contract. The cleanest structure is to evaluate/force the broader emergency defense branch before Port, while retaining the explicit Harbor-first policy if that remains intentional; alternatively gate Port on the full defense-emergency predicate being false.
- Preserve the existing local-danger exclusion, but do not treat `!AI_isDanger()` as equivalent to `no strategic defense emergency`, because the defense branch deliberately also covers stronger enemies and ongoing wars without immediate local danger.

EXISTING ROOTS / CURRENT BEHAVIOR CONFIRMED IN FILE 006
- KI#51: the hard no-production safety net remains the intended workaround. Current fallback detection excludes a valid Process and is limited to genuinely empty or invalid queues.
- KI#310: current water-heavy fallback is repaired: if Harbor/Port/Lighthouse attempts all fail, control continues into the same restricted military fallback instead of ending without production.
- KI#313: the human convenience BFC Artist helper still lacks provenance for the forced-Artist increment; its cleanup can decrement a human-forced Artist that was not created by the helper. This is existing KI#313, not a new root.
- KI#44.5: the debug assertion in changeSpecialistCount fires on every positive default-Citizen addition, including legitimate inherited UI paths. This is intentionally broad instrumentation around the already-documented Citizen assignment/fallback family and is not counted again.
- KI#162 / KI#162.2 / KI#162.3: getProductionBarPercentages contains the expected owner/registration, active-player and typed-order hardening. The current typed production calculation is consistent with the inherited production-bar semantics; no new crash root was established.
- KI#161 / KI#161.2: setName contains the existing null/empty guard and acquisition-path duplicate-scan/past-name hardening. No additional current source-proven city-name root was established here.

FALSE POSITIVES / NON-FINDINGS RECORDED FOR FILE 006
- SASTryEmergencyBuilding calls `canConstruct(..., bIgnoreCost=true)` before clearing the queue, while pushOrder later performs normal canConstruct. In theory a future define retuned to a production-cost -1 class could make the helper clear the queue, fail to push, and still return true. All five current configured emergency classes map to ordinary positive-cost buildings for supported civilizations (Harbor, Port, Lighthouse, Walls, Castle; Barbarian Port maps to NONE), so no current supported defect is established. Keep as robustness/future-tuning debt only.
- Clearing an existing queue between SASTryEmergencyBuilding's legality precheck and pushOrder does not make a currently legal building less legal under present contracts; removal of making-count pressure can only relax relevant max-count constraints.
- The no-production unit fallback's concrete helper returns success only with valid Unit/UnitAI outputs chosen through canTrain. Its direct push is restricted away from UNITAI_SETTLE. Popping an invalid old head before push does not create a current canTrain failure path for the already selected unit.
- The human/AI Artist force is intentionally applied via the assign-work-dirty mechanism: a force added late in one city turn is consumed by the next player-level citizen assignment. The resulting timing is not promoted as a correctness defect; the provenance problem is already KI#313.
- The changeSpecialistCount default-Citizen assertion is not a new defect; see KI#44.5 above.
- popOrder / handleOverflow additions pass exact overflow results to GameRecord logging. The logging helpers mutate only GameRecord aggregates/log output, and their placement preserves inherited unit/building/project gameplay-event order. Logging a produced air unit before possible no-capacity destruction is intentional production accounting.
- handleOverflow still uses the inherited computeOverflow result for gameplay, setOverflowProduction and payOverflowGold. New output parameters split positive lost production from negative unused capacity for diagnostics only.
- Current read/write serialization was mechanically paired: 178 corresponding operations occur in the same order, including enum maps and the newer tail fields. Removal of legacy uiFlag branches therefore does not create a current same-version save mismatch; cross-version save compatibility remains intentionally unsupported.
- Current hardcoded/configured identifiers used by changed CvCity paths resolve (COLOR_ALT_HIGHLIGHT_TEXT, ERA_RENAISSANCE, EVENTTRIGGER_PARTISANS, SPECIALIST_ARTIST, UNITCOMBAT_SIEGE, and the configured Harbor/Port/Lighthouse/Walls/Castle building classes).
- Static caching introduced in changed CvCity methods was checked against current runtime define mutation. Current code mutates river-generation, camera/FOV and minimap defines at runtime, not the cached CvCity gameplay/UI values reviewed here.
- The anonymous addGoodOrBad/subtractGoodOrBad helper block is unchanged from AdvCiv 1.14.
- No additional enum-vs-NULL, changed-header ABI, division-by-zero, stale mutable-cache, or completion-lifetime defect survived the final whole-file smell pass.

FILE 007 - CvGameCoreDLL/CvUnit.cpp - 12,945 lines - REVIEWED / TWO NEW FINDINGS + KI#331 CONTRACT CONFIRMED

RESULT
- Two new current defect roots were established from the CvUnit/Battle-History surface: provisional KI#334 and provisional KI#335.
- KI#334 is an inherited AdvCiv hidden-information leak in Great-General death notifications that SAS practical 6123 unintentionally made persistent in replay/Timeline history.
- KI#335 is a SAS Military Advisor Battles subject-vs-observer visibility regression: vassal perspective can expose battle-location map knowledge held by the selected vassal but not by the real human/master observer.
- CvUnit also confirms the callee side of provisional KI#331: scrap() is a void wrapper that silently returns when canScrap() vetoes the unit, while multiple inherited callers assume successful removal.
- All 53 current CvUnit member functions with real token-level differences from AdvCiv 1.14 were classified. The changed header/nonmember surface and current XML/data reachability were checked separately.
- Current CvUnit serialization is internally paired: a mechanical scan found 86 read/write serialization operations in corresponding order.
- No additional current defect root survived the final sentinel/cache/lifetime/data-reachability pass.

F013 - provisional KI#334 - Great-General death notifications can reveal the killer civilization to observers that know only the victim; SAS Timeline persistence preserves the leak
Status: NEW CURRENT HIDDEN-INFORMATION DEFECT ROOT; inherited transient leak, SAS persistence amplification; source/visibility contract evidence strong.
Inherited notification contract failure:
- CvUnit::kill handles a Great-General death by notifying the owner, killer and eligible third parties.
- For a third-party observer, the branch checks only whether the observer has met the victim owner's team (or is a spectator).
- The actual TXT_KEY_MISC_GENERAL_KILLED text names both the victim civilization and the killer civilization: the General "has died in combat against %s3_Civ".
- There is no corresponding check that the observer has met the killer team before GET_PLAYER(ePlayer).getCivilizationShortDescription() is inserted.
- This victim-only gate and fully identified killer text are inherited from AdvCiv 1.14, so the transient on-screen leak predates SAS.
Normal diplomacy-event contrast:
- Comparable two-party public announcements use both-participant visibility. For example, CvTeam::announceWar suppresses the third-party announcement unless the observer has met both the aggressor and the target; permanent-alliance announcements follow the same principle.
- The Great-General branch therefore violates the surrounding information-visibility convention rather than merely choosing a different cosmetic message.
SAS persistence amplification:
- Practical 6123 / 574e50ba53, while fixing KI#204 Great-General AI/UI issues, added a coordinate-less REPLAY_MESSAGE_MAJOR_EVENT containing the same fully identified victim + killer text.
- The birth comment assumes the original notification already exposes the event safely to met observers, but that inherited notification itself has the victim-only visibility flaw above.
- Current Info Screen Timeline spoiler filtering can evaluate the replay message player (the victim) and optional plot visibility. This entry has the victim as iPlayer and no coordinates, and it carries no killer-team visibility metadata.
- Therefore an observer who has met the victim but not the killer can retain/read the killer civilization identity in replay/Timeline history as well as potentially receiving the transient notification.
Repair direction:
- Gate the fully identified third-party transient message on the observer knowing both victim and killer, or sanitize the unknown participant rather than naming it.
- Do not persist a fully identified killer under a victim-only replay record unless Timeline/replay has enough metadata to apply both-participant visibility. A sanitized replay variant or explicit visibility-aware metadata would be safer than relying on the victim player ID alone.
- Preserve spectator/debug behavior explicitly rather than weakening normal observer privacy.

F014 - provisional KI#335 - Military Advisor Battles vassal perspective exposes the vassal's hidden battle-location map knowledge to the human/master observer
Status: NEW CURRENT SAS ADVISOR VISIBILITY DEFECT ROOT; separate feature/fix surface from KI#224 and KI#232; current default configuration exposes it.
Shared advisor contract:
- SASUtils' advisor-perspective helper explicitly allows a master's vassal as a selectable perspective outside debug mode while promising that this does not reveal unrelated players or hidden map data.
- The Main Changes Guide states the same user-facing privacy intent for vassal advisor perspectives.
Birth/lineage:
- Practical 5757 / 62ceac98ee introduced the persistent Battles table with exact stored battle coordinates, plot-row camera targeting and debug logging.
- Practical 5758 / 4481a5877f then enabled the shared debug/vassal perspective selector for the Battles page (`bAllowVassalPerspective=True`) without adding an observer-team reveal filter to the pre-existing battle-location data.
- Later plot-context work additionally stores/renders terrain, feature, hill/peak and city context, amplifying the same location-information root.
Current behavior:
- drawBattleHistory reads SASBattleHistory entries for `self.iActivePlayer`, which can legitimately be a selected vassal rather than the real active human.
- Battle entries persist exact X/Y from the battle plot. Current plot-context display is enabled by default and derives/renders terrain, feature, hill/peak and city context for the stored location.
- The Plot column caches the exact coordinates; clicking it hides the advisor and calls CyCamera().JustLookAtPlot(CyMap().plot(iX,iY)) with no reveal test against the actual human observer team.
- The Battles LOG button is enabled by default and prints the stored location/context without an active-observer reveal check.
Reachability / scope correction:
- Master-vassal teams can possess different revealed-plot knowledge; vassalization does not merge the vassal's revealed map into the master's map. A vassal can therefore have fought on a plot that the human master has never revealed.
- CvTeam::setVassal DOES synchronize diplomatic `hasMet` relationships between master and vassal. Therefore this finding should NOT claim that selecting a vassal reveals an otherwise-unmet opponent civilization; the live defect is hidden map/location/context knowledge.
Classification relative to existing KIs:
- KI#224 covers the World Advisor's vassal-perspective hidden map/resource leak.
- KI#232 separately covers the Policy Advisor's corresponding hidden Holy City/HQ subject-vs-observer leak.
- Battles is another independently implemented advisor feature with its own persisted coordinates, render path, log path and camera action, so keep it as a separate root rather than stretching KI#224's World-Advisor fix surface.
Repair direction:
- Keep the selected vassal as the battle-history SUBJECT, but outside debug mode use the actual active human's team as the OBSERVER for map information.
- For battle plots the human observer has not revealed, hide/sanitize exact X/Y and terrain/feature/hill/city context, suppress those fields from LOG output, and disable the Plot camera action.
- Debug/spectator modes can retain unrestricted inspection explicitly.

KI#331 - CvUnit callee-side confirmation / robustness note
- Current CvUnit::scrap() remains a void wrapper: `if (!canScrap()) return; scrapInternal();`.
- The SAS obsolete-only AI policy contains early reject/allow paths before the inherited final plot-fighting guard. This is awkward defensive ordering, but no supported live caller was proved able to exploit that ordering as an independent gameplay defect: human manual deletion bypasses the AI policy, normal AI financial scrapping occurs in pre-turn processing, ordinary UnitAI cleanup runs through normal group updates, and delayed death itself will not complete while the unit is fighting.
- Keep the guard-placement cleanup as robustness debt when repairing KI#331, not as another KI. The substantive current root remains callers assuming a silent-veto void API succeeded.

FALSE POSITIVES / CLEAN TAIL CHECKS FROM FILE 007
- Battle History initially looked as if vassal perspective could expose an opponent civilization unknown to the human master. This was rejected after tracing CvTeam::setVassal: master/vassal `hasMet` relations are synchronized. KI#335 is deliberately limited to hidden map/location/context knowledge.
- Battle History funnels the DLL's `COMBAT_LIMIT` and true `WITHDRAWAL` non-lethal endings into one Retreated outcome. That would be semantically lossy if partial-lethal combat units existed, but current CIV4UnitInfos has zero combat-capable units with `0 < iCombatLimit < 100`: fighting units use combat limit 100, while 0 belongs to nonfighters. Current data therefore makes the COMBAT_LIMIT branch unreachable; future/mod-mod robustness only, no KI.
- canScrap's AI-policy early return before the inherited plot-fighting guard was traced through current callers and delayed-death scheduling; no supported current exploit was established. Keep as robustness debt under KI#331.
- canUpgrade's `bUpgradeCityKnown` extension is used with upgrade types established by getUpgradeCity and preserves the ordinary gold/ROI/other gates; this matches the KI#188.3.3 fix rather than creating a second upgrade-legality hole.
- getUpgradeCity's optional chosen-upgrade output is initialized to NO_UNIT and set on successful direct/best-city results; no uninitialized output path found.
- Current espionage GameRecord snapshots were compared with CvPlayer::doEspionageMission's actual calculations. Production destroyed, stolen gold, inserted culture, poison/unhappy/revolt values, player-anarchy duration and counterespionage duration use the corresponding engine calculations/game-speed scalers; no logging-value mismatch established.
- Air strike/interception, bombard/pillage, blockade, nuke, range/flanking/collateral combat, Great-Person actions, upgrade, move/setXY and combat-message changes are logging/accounting hooks, enum renames or cached constants after contract review; no independent gameplay mutation survived.
- maxCombatStr changes are the already-fixed KI#211/KI#212 lineage; no new combat-strength root established.
- Current read/write serialization was mechanically paired at 86 corresponding operations. Removal of old uiFlag branches is unsupported legacy-save cleanup, not a current same-version mismatch.
- All cached GlobalDefines used by changed CvUnit paths were checked against current runtime define mutation; no behavior-sensitive overlap was found.
- Header changes (testSpyIntercepted phase label, canUpgrade bUpgradeCityKnown, getUpgradeCity output, impassability wrappers, scrapInternal exposure) match their implementations/callers; no ABI/contract mismatch established.
- Whole-file sentinel/null/static/lifetime scan found no extra enum-vs-NULL defect, stale mutable cache or post-kill dereference in the changed surface.


FILE 008 - CvGameCoreDLL/CvGame.cpp - 10,841 lines - REVIEWED / TEN NEW FINDINGS

RESULT
- CvGame.cpp is closed after both a current-vs-AdvCiv semantic-diff pass and an independent whole-file inherited-code/invariant pass. The second layer was especially productive: several defects live in untouched inherited code and therefore were not natural targets for the older commit-oriented archaeology.
- Ten new current roots survived full reachability/history/data checks: F015-F024 (provisional KI#336-KI#345).
- The findings split into one SAS lifecycle/configuration defect (F015), one AdvCiv-replay x SAS-Timeline visibility interaction (F016), seven inherited starting/score/global-warming/vote/victory defects from AdvCiv/BtS lineage (F017-F022/F024), and one inherited BtS Lock Mods defect (F023).
- Current-format CvGame serialization was mechanically paired at 88 corresponding read/write operations in the same order; no same-version stream mismatch was found.
- Multiple attractive false positives were explicitly killed so they are not rediscovered later.

F015 - provisional KI#336 - dynamic map-heaviness fallback is computed before starting plots exist and therefore caches a neutral result
Status: NEW CURRENT SAS LIFECYCLE DEFECT ROOT; current default/support configuration reaches it.
Intended contract:
- SAS map-heaviness logic first classifies scripts listed explicitly as land-heavy or naval-heavy.
- Scripts intentionally absent from both lists fall back to a dynamic start-area classifier that counts major-civilization starting plots and classifies the map from those areas.
- Current XML deliberately leaves Big_and_Small, Hub, Medium_and_Small, RandomScriptMap, Shuffle and not_too_Big_or_Small unspecified so this dynamic fallback can decide them.
Current lifecycle failure:
- CvGame::initDiplomacy explicitly documents that the EXE invokes it after map generation but before assigning starting plots. recomputeMapnameHeaviness() is called at the end of that function.
- regenerateMap likewise clears/has no starts, generates the map, calls recomputeMapnameHeaviness(), and only later reaches setInitialItems()/starting-plot assignment.
- The dynamic branch therefore normally sees no major-civilization starting plots. With iMajorCivs <= 1 it leaves both heaviness flags false.
- Those booleans are then cached and serialized correctly, so the problem is computation timing rather than persistence corruption.
Lineage:
- Practical 5032 introduced the script-name cache/call sites.
- Practical 5402 added the start-area dynamic fallback and persistence without moving the computation to a post-start phase.
Consequence:
- Supported scripts intentionally delegated to dynamic classification normally remain neutral instead of receiving their intended land/naval heuristic classification. AI logic consuming these flags can therefore miss its map-specific tuning.
Repair direction:
- Split script-name classification from the start-area-dependent phase, or explicitly recompute the dynamic fallback after starting plots have been assigned (including scenario/regeneration lifecycle considerations).

F016 - provisional KI#337 - AP/UN passed-resolution replay entries bypass the live voting-member audience when surfaced through Timeline
Status: NEW CURRENT HIDDEN-INFORMATION INTERACTION; AdvCiv replay record plus SAS Timeline visibility model.
Live audience contract:
- CvGame::doVoteResults sends the detailed vote result only to voting members (with explicit spectator handling).
- Passed non-Secretary-General resolutions are also persisted as coordinate-less REPLAY_MESSAGE_MAJOR_EVENT entries with NO_PLAYER.
- The replay text can contain the vote source, Secretary-General team identity, vote totals and target civilization/city/resolution details.
Timeline interaction:
- CvReplayInfo::createInfo(activePlayer) copies the replay-message stream; its player argument does not filter individual messages by their original live audience.
- Current Info Screen Timeline spoiler filtering can test replay-player diplomacy, plot reveal, plot owner and the special unknown-player color.
- This entry has NO_PLAYER and no coordinates/owner metadata, so none of those generic gates can reconstruct the voting-member-only audience.
Consequence:
- A civilization that was not a voting member and therefore did not receive the live detailed result can later inspect that detailed record in Timeline.
Scope/control:
- This is not a blanket NO_PLAYER replay defect. The coordinate-less global-warming activation replay is deliberately broadcast live to all major civilizations, so global Timeline visibility is consistent there.
- F013/KI#334 Great-General replay leakage has a related metadata limitation but a different observer contract (met killer vs voting membership); keep roots separate unless a generalized replay-visibility architecture deliberately repairs both.
Repair direction:
- Persist enough audience/visibility metadata for vote-result replay entries, omit/sanitize private detail in the globally visible replay text, or teach Timeline a vote-source membership rule that can be evaluated for the observer.

F017 - provisional KI#338 - starting-location volatility safeguard tests the human's old start instead of the sorted site they are about to receive
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT; refactor regression proved by history.
Evidence:
- applyStartingLocHandicaps sorts actual starting sites into apStartingSitesByValue and ultimately assigns player at slot i to apStartingSitesByValue[i].
- The human volatility safeguard instead sets pStart from GET_PLAYER(ePlayer).getStartingPlot(), i.e. that player's pre-reassignment/original start.
- Its neighboring alternative/swap tests correctly inspect apStartingSitesByValue[iSwapPlayerIndex].
Lineage:
- Before practical 2708's starting-site refactor, the safeguard tested startPlots[i], the actual site associated with the slot being evaluated.
- The refactor changed storage/ordering but accidentally changed this one check to the player's old plot.
Consequence:
- A human can be assigned a volatile site without the safeguard noticing because their old site was safe, or can be swapped unnecessarily because their old site was volatile even though the new assigned slot is safe.
Repair direction:
- Use apStartingSitesByValue[i] for pStart so the safeguard evaluates the site the human is actually about to receive.

F018 - provisional KI#339 - starting-handicap collision randomization computes a random rotation and then never applies it
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT; practical tie/collision bias.
Evidence:
- sortByStartingLocHandicap separately processes human and AI agents, draws a random ordinal, walks to that agent and stores its actual source index in iRandOffset.
- The placement pass then iterates the source list from index 0 without using iRandOffset at all.
- The random draw therefore consumes map RNG but has no effect on ordering.
Lineage:
- Pre-refactor starting-site code used the random offset to rotate traversal, preventing fixed player-slot priority when equal/colliding StartingLocationPercent values mapped agents to the same positions.
- The extracted/refactored helper retained calculation of the offset but dropped the rotated traversal.
Consequence:
- Players/teams sharing the same starting-location handicap can resolve collisions deterministically according to input/player ordering rather than the intended randomized order.
Repair direction:
- Restore a rotation over the eligible source-agent order using the computed starting index (or directly shuffle/rotate the eligible list in an equivalent deterministic-map-RNG-safe way).

F019 - provisional KI#340 - team-start fallback uses compact alive-team count as an offset in the absolute team-ID space
Status: NEW CURRENT INHERITED BTS DEFECT ROOT; sparse/high team IDs make the intended random priority non-uniform.
Evidence:
- assignStartingPlots draws iRandOffset from MapRandNum(countCivTeamsAlive()).
- It then rotates through (i + iRandOffset) % MAX_CIV_TEAMS, which is the absolute team-ID domain rather than the compact list of alive teams.
Concrete example:
- If the two alive teams are IDs 5 and 20, countCivTeamsAlive()==2, so the only offsets are 0 and 1.
- Scanning absolute IDs from either 0 or 1 reaches team 5 before team 20; team 20 can never receive first-choice priority.
Reachability:
- Sparse/high IDs are supported in scenarios and current SAS code, and StartingPositionIteration deliberately falls back to this inherited assignment path for supported cases such as sufficiently large team games.
Repair direction:
- Build the actual alive-team list and randomize/rotate within that compact list; never mix a compact count with absolute team IDs.

F020 - provisional KI#341 - initScoreCalculation passes a civilization index to isCivilizationFreeTechs where the technology index is required
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT; practical 2011 enum-refactor regression, current-data consequence proved.
Evidence:
- initScoreCalculation loops over technologies and then playable civilizations to determine whether eLoopTech is a possible civilization free technology.
- Current code calls GC.getInfo(eLoopCivilization).isCivilizationFreeTechs(eLoopCivilization), passing the civilization enum as the tech argument.
- BtS/Civ4CE lineage passes the technology-loop index as intended.
Current-data proof:
- Current playable civilization/free-tech data contains no accidental civ-index/free-tech-index coincidence that rescues this predicate; the erroneous branch is false for every playable civilization.
Consequence:
- On an Ancient start, m_iInitTech receives 0 from this free-start-tech normalization instead of counting the current five distinct possible civilization starting technologies (each currently worth one technology-score point).
- This defeats the routine's explicit intent to normalize the immediate-retirement score for free starting technologies. Later start eras mask much of the effect because earlier-era technologies are counted through the separate era branch.
Repair direction:
- Pass eLoopTech to isCivilizationFreeTechs.

F021 - provisional KI#342 - global-warming target selection can accept the last rejected water/peak plot after exhausting its retry loop
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT; K-Mod control shows the lost exhaustion guard.
Evidence:
- getRandGWPlot tries up to 25 random plots for each pool candidate and accepts Ice or ordinary non-peak land.
- Unsuitable open water/peaks are simply retried. If all 25 random draws are non-null but unsuitable, pTestPlot still points to the final rejected plot when the loop exits.
- Current post-loop guard tests only pTestPlot == NULL, so the rejected plot proceeds into scoring/selection.
- eTerrain is declared outside the outer candidate-pool loop and can therefore also carry the previous candidate's terrain value into this exhausted candidate.
Lineage:
- K-Mod retained an explicit exhaustion test in addition to the null test.
- AdvCiv practical 1631 removed the loop-count guard on the theory that resetting pTestPlot made it redundant; resetting before a successful-but-unsuitable draw does not make it null after the final retry.
Consequence:
- On water/peak-heavy maps, a global-warming strike can be wasted on an unsuitable open-water/peak target, and stale eTerrain can mis-rank an exhausted candidate.
Repair direction:
- Track whether a suitable plot was actually found (or test retry exhaustion explicitly), and reset candidate-specific terrain state inside the outer pool iteration.

F022 - provisional KI#343 - multiple missing AP/UN voters receive staggered one-turn grace periods based on player iteration order
Status: NEW CURRENT INHERITED BTS MULTIPLAYER DEFECT ROOT; source contract explicit.
Evidence:
- doVoteResults comments that a player who has not voted should receive one more turn to submit a vote.
- On the first NO_PLAYER_VOTE it changes only that voter to NO_PLAYER_VOTE_CHECKED, sets bAllVoted=false and immediately breaks from the voter loop.
- On the next game turn, that first checked voter defaults to abstain, then the next still-missing voter receives the checked state; additional missing voters repeat the sequence.
Consequence:
- When several eligible voting members miss the same deadline, they do not receive the same one-turn grace period. Later players in iteration order can receive successively later deadlines.
Repair direction:
- In the first incomplete-vote pass, mark every currently missing eligible voter NO_PLAYER_VOTE_CHECKED, remember that voting is incomplete, and postpone processing until the next turn. Then all still-checked missing votes can default consistently.

F023 - provisional KI#344 - Lock Modified Assets can randomly generate an empty admin password and fail to lock WorldBuilder
Status: NEW CURRENT INHERITED BTS DEFECT ROOT; low-probability but direct option-contract failure.
Evidence:
- With GAMEOPTION_LOCK_MODS, CvGame::init generates seven arbitrary random char values from a range that includes zero, terminates the buffer, and supplies it as the admin password.
- If the first generated byte is NUL, the C string passed to setAdminPassword is empty.
- Current isDebugToolsAllowed(true) explicitly permits single-player WorldBuilder when the admin password is empty.
Consequence:
- A game started with Lock Modified Assets can therefore retain WorldBuilder/debug-tool access despite the option's stated purpose of assigning a random password to prevent it.
- Under the normal Windows/MSVC signed-char range (0..127), first-byte NUL is approximately a 1/128 event per locked game. A later NUL merely shortens the password; the first-byte case is the concrete contract failure.
Repair direction:
- Generate from a non-NUL printable alphabet/range or encode a random integer into a guaranteed nonempty textual password.

F024 - provisional KI#345 - victory scan clears the cumulative End Score flag on every victory/team test
Status: NEW CURRENT INHERITED BTS DEFECT ROOT; concrete Time-Victory tie behavior contradicts the outer scan's cumulative use.
Contract failure:
- CvGame::testVictory(VictoryTypes, TeamTypes, bool*) begins by assigning *pbEndScore=false on every call, then sets it true only for that individual victory if appropriate.
- The outer testVictory() passes the same bEndScore pointer through every team/victory pair, but later treats the final value as the cumulative question "does this game have an End Score victory?".
Current-data consequence:
- VICTORY_TIME has End Score enabled but is followed in XML iteration by non-End-Score Conquest, Domination, Cultural, Space and Diplomatic victories. Those later calls reset bEndScore back to false.
- Concrete trigger: when the Time Victory deadline is reached with the top teams tied, Time Victory correctly refuses to select a winner; the later non-End-Score calls then clear bEndScore, allowing the post-loop max-turn fallback to end the game anyway. The outer guard's intended cumulative meaning is therefore defeated.
Lineage:
- Local Civ4CE BtS 3.19 contains the same helper, per-call reset and outer reuse. The defect is therefore inherited from BtS itself rather than introduced by K-Mod; tracked K-Mod and AdvCiv retain it.
Repair direction:
- Preserve the helper's per-call output contract and OR each result into a separate cumulative flag in the outer caller, or change the helper to set-only semantics explicitly.

FALSE POSITIVES / CLEAN TAIL CHECKS FROM FILE 008
- Vote-source city coordinate leak candidate rejected: getVoteSourceCity(..., bDebug=true) still requires the observer to have revealed the city unless the game itself is actually in debug mode; the flag is not an unconditional reveal bypass.
- sortByStartingLocHandicap's all-human/all-AI zero-agent pass calls MapRandNum(0), but CvRandom explicitly defines zero as legal: it returns 0 without advancing RNG. No separate zero-range random defect.
- skipDuplicateNormalizationBonus appears able to divide by zero because the city-radius iterator includes the candidate plot, but all live callers invoke it while that candidate is bonus-free; the self plot therefore cannot pass the same-bonus test before the division.
- The two perAreaUnowned.find(...)->second dereferences are safe under the local construction contract: the map is populated for every current CvArea immediately before the consuming plot scan.
- randomBarbarianUnit's optional OR-resource/NO_TECH handling looked under-defensive, but current unit XML has no reachable unit configuration exercising the problematic path. Future/mod-mod robustness only.
- Global-warming ordinary probability/defence denominators and current XML values were checked; no second denominator/configuration root survived beside F021.
- clearSecretaryGeneral's apparently underinitialized target state does not escape through the negative processVote path; the relevant removal branch returns before target fields can be consumed.
- goodyHutEffectFactor can only reach its questionable denominator if the relevant GlobalDefine is retuned outside current supported/default values; record as tuning robustness debt, not a current KI.
- updateStartingPlotRange's denominator/range paths have current caller/data guards; no additional starting-distance arithmetic root established.
- Current-format CvGame serialization was mechanically paired at 88 corresponding read/write operations in the same order. Variable-length string vectors and conditional SPAH, Rise & Fall and event-trigger blocks are symmetric.
- Modern read() hard-coded option remap numbers were checked against the current enum: 17 New Random Seed, 18 Lock Mods, 19 Rise & Fall, 20 True Starts, 23 No Vassals and 27 No Espionage are all correct.
- Map-heaviness members are reset and serialized correctly. F015 is specifically a lifecycle/computation-order failure, not stale persistence.
- Cached CvGame gameplay defines were cross-checked against current runtime define mutation. The dynamically changed river-generation, camera/FOV and minimap values do not overlap these caches.
- Global warming's coordinate-less NO_PLAYER replay message is intentionally broadcast live to all major civilizations; unlike F016, its Timeline visibility matches its live audience.
- replaceCorporation iterator deletion, timer/active-player accesses, reviveActivePlayer, deal-iteration diagnostics and remaining FAssert(false) sites were traced to established iterator/UI/EXE contracts; no additional current defect survived.
- calculateSyncChecksum concerns map to already-known/fixed KI#326 lineage; no new synchronization root from this file.
- Constructor/reset/header/member ownership was checked: persistent enum maps/state have matching reset/read/write lifecycles, transient UI/cache state is reset rather than serialized, replay storage is cleared, and BarbarianWeightMap ownership/activity reset is coherent.
- Post-C008 commits touching CvGame.cpp were checked: practical 6216 only adjusts/comment-documents starting-distance work; practical 6221 is KI#325 past-city-name signature/locale work. Neither repairs or invalidates F015-F024.
- Final assertion/sentinel/container/iterator/enum/division scan found no additional live root. The remaining FAssert(false) sites are synchronization diagnostics or switch-default impossibilities under current enums.


FILE 009 - CvGameCoreDLL/CvPlot.cpp - 8,705 lines - REVIEWED / SEVEN NEW FINDINGS

RESULT
- CvPlot.cpp is closed after a changed-surface pass, an independent inherited whole-file pass, and final owner/team-sentinel, fixed-array, iterator/container, constructor/header, serialization, supported-caller and known-KI collision sweeps.
- Seven new current defect roots survive as F025-F031 / provisional KI#346-KI#352.
- Current CvPlot read/write serialization was mechanically revalidated at 60 corresponding operations in the same structural order.
- No F032 was established. Several genuine source defects remain recorded below as dormant/robustness debt because no supported current caller reaches the broken branch.

F025 - provisional KI#346 - dynamic feature passability changes leave PASSABLE_AREAS and plot-group connectivity stale
Status: NEW CURRENT DEFECT ROOT, inherited/AdvCiv topology interaction; supported global-warming trigger.
Evidence:
- AdvCiv's PASSABLE_AREAS behavior deliberately makes impassable shelf Ice part of water-area topology and performs an additional area calculation after Ice placement.
- CvPlot::setFeatureType() changes the feature, calls updateImpassable() and updateYield(), but does not recalculate/reassign CvArea topology and does not rebuild trade/resource plot-group connectivity.
- Ordinary current global warming can remove FEATURE_ICE through setFeatureType(NO_FEATURE).
- After the removal, the plot's direct passability is updated immediately while its old CvArea identity and existing plot groups can still describe the Ice barrier.
- isTradeNetwork() itself treats impassable feature state as relevant to connectivity, so the stale state is not merely diagnostic metadata.
Consequence:
- A melted channel can be traversable at plot level while naval/area-scoped AI and resource/trade-network logic still reason from the pre-melt disconnected topology.
- CvArea::canBeEntered, water-area unit/resource/city accounting and several naval/worker/assault decisions deliberately consume these area divisions.
Repair direction:
- Treat a feature change that changes effective passability as a topology invalidation and perform a safe deferred area/plot-group refresh after the mutation batch.
- Do not naively call wholesale recalculateAreas() inside every setFeatureType(); current area rebuild code has live-city bookkeeping caveats.
- If an incremental processArea(-1/+1) repair is considered, first fix or bypass the dormant processArea defects recorded below.

F026 - provisional KI#347 - lazy getFoundValue ignores a successful Python city-found-value override
Status: NEW CURRENT SUPPORTED-CONFIGURATION API DEFECT, inherited from BtS.
Evidence:
- CvPlot::getFoundValue() calls CvPythonCaller::AI_foundValue().
- If Python returns -1, the DLL computes and stores its own value in m_aiFoundValue.
- If Python returns a real override, the function never copies iValue into m_aiFoundValue; the cache remains -1.
- It then tests/returns the still--1 cache entry, fails to update the area's best-found value from the Python result, and repeats the callback on the next lazy read.
- Current SAS still registers/configures getCityFoundValue as a supported BUG/GameUtils callback; default is -1, but enabling the callback is an explicit supported configuration.
- Local Civ4CE BtS 3.19 source has the same lazy callback path and missing non--1 cache assignment; K-Mod and AdvCiv retain it through later wrappers/refactors.
Consequence:
- A valid Python override can be ignored specifically through lazy CvPlot::getFoundValue(), producing -1 instead of the supplied city-site value and leaving associated caches inconsistent.
Repair direction:
- In the non--1 branch, store iValue into m_aiFoundValue before area-best comparison and return.

F027 - provisional KI#348 - foreign friendly air units inherit the host city's enhanced air capacity
Status: NEW CURRENT GAMEPLAY DEFECT, inherited from BtS.
Evidence:
- CvPlot::airUnitSpaceAvailable(TeamTypes eTeam) correctly counts aircraft belonging to the requested eTeam.
- On a city plot it asks pCity->getAirUnitCapacity(getTeam()), where getTeam() is the plot/city owner's team, not eTeam.
- CvCity::getAirUnitCapacity(eTeam) is intentionally team-sensitive: the city team receives building-enhanced capacity, while a different friendly team receives only the base capacity.
- Current gameplay permits peaceful foreign-team air basing/rebasing, and ordinary callers query airUnitSpaceAvailable for the visiting team.
- Local Civ4CE BtS 3.19 source contains both the wrong plot-team argument and the team-sensitive city-capacity contract; K-Mod and AdvCiv retain them.
Consequence:
- Visiting allied/vassal/open-borders aircraft can be admitted using the host owner's Airport-enhanced capacity even though the underlying city API intentionally denies that expansion to foreign teams.
Repair direction:
- Pass the requested team through: pCity->getAirUnitCapacity(eTeam).

F028 - provisional KI#349 - CvPlot::debugStr returns a dangling c_str pointer
Status: NEW CURRENT DIAGNOSTIC/UNDEFINED-BEHAVIOR DEFECT, introduced by AdvCiv.
Evidence:
- debugStr() returns wchar const* and uses a static std::wostringstream.
- Every return expression is out.str().c_str().
- std::wostringstream::str() returns a temporary std::wstring; c_str() therefore points into a temporary destroyed at the end of the return expression.
- The static lifetime of the stream does not extend the lifetime of that temporary string.
- Current found-value/BBAI diagnostic paths consume debugStr when those logs are enabled.
Consequence:
- Diagnostic logging reads a dangling pointer and can produce stale/garbled text or, under undefined behavior, worse failures.
Repair direction:
- Prefer returning CvWString/std::wstring by value, or assign out.str() to storage whose lifetime survives the call before returning c_str().

F029 - provisional KI#350 - nuke effect reporting reveals the true owner of hidden-nationality units
Status: NEW CURRENT HIDDEN-INFORMATION DEFECT, inherited AdvCiv reporting behavior.
Evidence:
- nukeExplosion() records damaged/killed units in per-real-owner vectors and later formats observer messages with GET_PLAYER(eUnitOwner).getCivilizationAdjectiveKey().
- The observer gate checks whether each affected plot is visible, but does not apply the unit's observer-relative visual owner before naming the civilization.
- Current UNIT_PRIVATEER is hidden-nationality and always-hostile, is not nuke-immune, and getVisualOwner(observerTeam) intentionally conceals its real civilization under ordinary visibility.
- Such a unit can legitimately be damaged/killed by a nuclear blast.
Consequence:
- An observer who sees only an anonymous/Barbarian-looking Privateer can receive a nuke message naming its real civilization (for example, "Dutch unit destroyed"), revealing information the unit itself is designed to conceal.
Repair direction:
- Group/name affected units using observer-relative visual/combat ownership, or sanitize hidden-nationality entries rather than formatting them with the real owner's civilization adjective.
- Keep this separate from the Great-General identity leak: the feature, data path and repair surface are different.

F030 - provisional KI#351 - post-start river-edge edits leave several derived systems stale
Status: NEW CURRENT SUPPORTED-API DEFECT, inherited from BtS.
Evidence:
- The source immediately around fresh-water logic explicitly notes that changing river state requires updateIrrigated and CvCity::updateFreshWaterHealth.
- CvPlot::setNOfRiver() and setWOfRiver() can dynamically change that state, but neither performs those updates.
- The setters update river crossings, yields and river-edge area counts, and adjust plot-group bonus counts, but do not rebuild trade/resource plot-group connectivity even though river edges can create/remove network connections.
- Both setters remain publicly exposed through current CyPlot Python APIs; current SAS logging also explicitly supports rare post-start WorldBuilder/script/event river-edge edits.
- Initial map generation is not the problematic case because later initialization rebuilds derived state; the defect concerns post-start mutation.
Consequence:
- A runtime river edit can leave irrigation propagation/yields, city fresh-water health and river-based resource/trade connectivity inconsistent with the visible river.
Repair direction:
- When an edge actually changes after initialization, invalidate/recompute irrigation around the affected edge, update fresh-water health for affected cities and refresh plot-group connectivity for relevant players/teams.

F031 - provisional KI#352 - doCultureDecay reads one element past its city-radius array for Barbarian culture
Status: NEW CURRENT MEMORY-SAFETY/LOGIC DEFECT, inherited AdvCiv practical 1289.
Evidence:
- doCultureDecay() allocates bool abInRadius[MAX_CIV_PLAYERS].
- In the 48-civ DLL, valid array indices are civilization-player slots 0..47; BARBARIAN_PLAYER is 48 and MAX_PLAYERS is 49.
- The later FOR_EACH_ENUM(Player) loop spans the full PlayerTypes domain through MAX_PLAYERS and reads abInRadius[eLoopPlayer].
- Therefore a positive Barbarian-culture entry reaches abInRadius[48], one element past the stack array.
- Reachability is normal current gameplay, not a malformed save: Barbarian cities/adjacent plots receive ordinary plot culture, legal Barbarian/civilization city spacing permits overlapping/contested radius plots, and the decay branch can process such a plot.
- Historical intent around practical 1289 explicitly discussed excluding Barbarian tiles from the extra city-radius decay, so the mismatch is especially likely to be an array-domain implementation error.
Consequence:
- Release builds have undefined behavior. The likely visible symptom is nondeterministic application of accelerated culture decay to Barbarian culture on contested plots; the one-past-end read is also a memory-safety defect in its own right.
Repair direction:
- Either size the state to the full PlayerTypes domain (MAX_PLAYERS / PlayerMap<bool>) or explicitly skip BARBARIAN_PLAYER before indexing if the historical Barbarian exemption remains intended.

DORMANT / ROBUSTNESS DEFECTS AND KILLED CANDIDATES FROM FILE 009
- CvPlot::processArea contains two genuine source mistakes in a currently dormant negative/live-city path:
  - `pWaterArea->getCitiesPerPlayer(getOwner()) > 0, true` uses the comma operator and is always true. The intended expression is evidently `pWaterArea->getCitiesPerPlayer(getOwner(), true) > 0`.
  - The refactored building-area loop skips `iTotalChange <= 0`, suppressing removal effects; historical behavior indicates that only zero should be skipped.
  Current supported area-changing paths do not reach those malformed negative branches with a living city still needing that bookkeeping. Do not promote them as present gameplay roots, but fix/avoid them before using incremental processArea(-1) as part of F025.
- exclusiveRadius(ePlayer) can overwrite an inner-ring result (1) with a later same-owner outer-ring result (2) when city radii overlap. Its only logic consumer is currently dead/commented, so this is a dormant helper bug rather than a current KI.
- Deprecated isCityExternal(bool, TeamTypes) compares `eForTeam == NULL`; NO_TEAM is -1, so its old parameterized ActsAsCity path would dereference GET_TEAM(NO_TEAM). Current supported callers use the ordinary no-parameter city check and the function/header explicitly treat the parameterized path as deprecated/EXE compatibility, so this remains robustness debt.
- getNonObsoleteBonusType has a header default of NO_TEAM while its implementation asserts and dereferences a real team. No current zero-argument C++ caller was found and the Python wrapper requires the team argument; no live root established.
- changeBuildProgress and shouldDisplayBridge also assume valid player arguments, but all supported engine/Python call contracts found supply a real player; no sentinel bug was promoted from hypothetical invalid external input.
- The setFeatureType fresh-water facet is incomplete in general, but current dynamically changing feature data does not provide a supported post-start AddsFreshWater creation/removal path; it remains future/mod-mod robustness separate from F025.
- Hidden-resource random-event eligibility is intentional for the one matching current event ("A Man Named Jed", Oil), whose design is to discover Oil; no information-leak root.
- Culture-flip civilization names follow AdvCiv's established hasSeen/revealed-owner contract and do not require hasMet; no leak established.
- The hidden-nationality visibility contact path acts on visual owner and does not identify the concealed real owner; no defect there.
- getYieldWithBuild unowned-owner concern was killed by caller contracts: current callers operate on WorkablePlotIter/best-build plots with an owned/same-owner working city.
- canHavePotentialIrrigation(...NO_TEAM...) safely propagates the sentinel to code that handles it; no invalid team dereference.
- getPlotGroup(ePlayer) requires a real player, and current callers establish that contract.
- Sight-recursion pointer lifetime/NULL concern was killed: reaching the recursive branch establishes the candidate plot.
- Static GlobalDefine caches were cross-checked against current runtime define mutation; no cached CvPlot gameplay value overlaps the currently mutable river-generation/UI/camera/minimap define set.
- Fixed-array/domain scan found no second MAX_CIV_PLAYERS/MAX_CIV_TEAMS local-array mismatch beyond F031.
- Iterator erase patterns, constructor/member initialization, enum/container bounds and final division/sentinel scans produced no additional supported current root.

DEFERRED OBSERVATION POLICY
- Observations in later files are not counted until that file reaches its turn. File 010 is now active.

================================================================================
WORKING CHECKPOINT C010-WIP1 - FILE 010 CvTeamAI.cpp STILL OPEN
================================================================================

CHECKPOINT STATUS
- This is a cumulative RECOVERY / IN-PROGRESS checkpoint, not a completed-file checkpoint.
- Durable completed-file stone remains C010 = 9 / 169 .cpp files COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` (6,854 lines) remains OPEN.
- Do NOT rename this checkpoint to C011 or mark CvTeamAI.cpp complete merely because many findings already survive.
- Current semantic cursor: late whole-file closure pass after the major changed/inherited logic review.
- Remaining closure work: finish fresh arithmetic/default/assert/nextIndex/fixed-array scan; finish remaining public sentinel and relationship/cache lifecycle contracts; recheck worst-enemy lifecycle against existing KI lineage; perform complete Album/KI collision pass; reread F032-F042 as a set for exact reachability and repair wording.
- If those passes are exhausted cleanly, then and only then close CvTeamAI.cpp -> 10/169 -> freeze durable C011 -> open file 011 SASGameRecordLog.cpp.

CURRENT FILE-010 RESULT SO FAR
- Eleven current supported defect roots survive: F032-F042.
- Provisional KI aliases: KI#353-KI#363.
- The file remains OPEN; these findings are checkpointed so that chat/thread/network interruption cannot lose them.
- Most roots are inherited BBAI/K-Mod/AdvCiv survivors. The current SAS-specific semantic surface checked so far (tech-locus KI#312 repair, victory-denial peace hook, Barbarian AreaAI option, war diagnostics/logging, serialization cleanup) has not produced another new SAS regression.

F032 - provisional KI#353 - AI_diploVoteCounterCandidate compares vote count with team ID
Status: CONFIRMED CURRENT AI LOGIC DEFECT.
Evidence:
- AI_diploVoteCounterCandidate() tracks first/second vote totals, but a branch compares `iFirstMostVotes` (vote count) with `getID()` (TeamTypes numeric ID).
- Those values are unrelated domains.
- Concrete normal case: our team ID 0 has 100 votes, rival team 1 has 60, third team 2 has 20. When team 1 becomes the second-place candidate, the test `100 == 0` fails and the helper can return NO_TEAM rather than team 1 as the obvious counter-candidate.
- The result feeds diplomacy-victory strategy progress and UWAI military-victory utility.
Ancestry:
- AdvCiv practical 1832 introduced the comparison in this form.
- Later practicals 2579 and 2633 explicitly fixed neighboring diplomatic-victory vote calculations but left this domain mismatch intact.
Repair direction:
- Compare the proper candidate/team state rather than a vote total with a team ID; reconstruct the original intended first/second candidate logic explicitly.

F033 - provisional KI#354 - AI_haveSeenCities(eTeam) iterates our own team instead of target team
Status: CONFIRMED CURRENT / CONFIGURATION-REACHABLE WAR-PLANNING DEFECT.
Evidence:
- Helper contract is to determine whether cities belonging to `eTeam` have been seen/deduced.
- Current implementation uses `MemberIter(getID())`; the target `eTeam` argument is not used to select the city owners.
- K-Mod's original explicit loop correctly filtered players by `getTeam() == eTeam`.
- AdvCiv practical 1842 ("Rename WPAI to UWAI; revise loops in CvTeamAI") replaced the target population with the own-team iterator.
- A sibling practical-1842 own-team/target-team substitution in AI_isLandTarget() was later repaired by practical 1941 (`MemberIter(getID())` -> `MemberIter(eTarget)`), strongly corroborating this refactor class.
Reachability:
- Default UWAI masks the path for ordinary major-civ war selection.
- It is live through supported Aggressive AI / K-Mod behavior, `USE_KMOD_AI_NONAGGRESSIVE`, `DISABLE_UWAI`, and related background-UWAI modes.
Consequence:
- Once an AI has seen/deduced one of its own cities, total/limited/dogpile gates can act as though a proposed enemy city has been seen even when no target city is known.
Repair direction:
- Iterate `MemberIter(eTeam)` (or the exact equivalent target-team population) and preserve the original visibility/deduction semantics.

F034 - provisional KI#355 - AI_getRivalAirPower computes the rival-average denominator once per air class
Status: CONFIRMED CURRENT AI VALUATION DEFECT, inherited K-Mod.
Evidence:
- Routine promises enemy air power plus average rival air power.
- During K-Mod practical 199's rewrite from unit types to unit classes, `iTeamCount++` was placed inside the outer air-unit-class loop.
- Current SAS has five qualifying default air classes (Fighter, Jet Fighter, Bomber, Stealth Bomber, Guided Missile).
- With two eligible rival teams, the denominator can therefore become about 10 instead of 2.
- AdvCiv's later "avoid war / friendly rival" filtering can further aggravate the same root: skipped teams contribute no numerator while the denominator population can still include them.
Consequence:
- The average-rival-air component is systematically understated, approximately 5x in the straightforward current-data case.
- This feeds interceptor unit valuation, defensive-air production and air-defense building valuation.
Repair direction:
- Count eligible rival teams once per rival, with exactly the same population used by the numerator; accumulate unit-class air power inside that rival loop (or track unique included teams separately).

F035 - provisional KI#356 - AI_doWar subtracts total Dagger teammates from independent teammate populations
Status: CONFIRMED CURRENT MULTI-MEMBER-TEAM COUNTING DEFECT, inherited K-Mod/BBAI logic.
Evidence:
- Financial-trouble gate intends to reason about financially troubled NON-DAGGER teammates, but computes `iFinancialTroubleCount - iDaggerCount`.
- Dagger strategy and financial trouble are independent sets; a healthy Dagger member can cancel a different member's genuine financial trouble.
- The same invalid set-subtraction pattern is used for `GET_BETTER_UNITS`: `iGetBetterUnitsCount - iDaggerCount`, even though Dagger and GET_BETTER_UNITS are independently computed strategies and can coexist arbitrarily.
- Nearby comment says "half the non-Dagger teammates", but the threshold uses `getNumMembers()/2`, i.e. half of all teammates.
Concrete case:
- Two-member team: member A is healthy + Dagger; member B is financially troubled + non-Dagger. Intended troubled-non-Dagger count is 1/1; subtraction yields 1 - 1 = 0 and removes the intended opposition.
Repair direction:
- Count the actual intersections directly (`financialTrouble && !Dagger`, `GET_BETTER_UNITS && !Dagger`) and compare against the actual non-Dagger population.

F036 - provisional KI#357 - AI_warCommitmentCost passes accumulated output cost as raw war weariness
Status: CONFIRMED CURRENT REFACTOR REGRESSION.
Evidence:
- War-weariness section computes a raw `iTotalWW`.
- Current code calls `getModifiedWarWearinessPercentAnger(iWWCost)`, where `iWWCost` is the accumulating result/cost output, not raw war weariness.
- K-Mod practical 887 explicitly fixed this same routine to pass `iTotalWw`.
- AdvCiv practical 1842 changed the argument to `iWWCost` during iterator/refactor cleanup.
Consequence:
- First team member begins with `iWWCost == 0`, so its anger estimate can ignore nonzero actual war weariness.
- Later members are fed a feedback value measured in AI-cost units rather than war-weariness units.
- Commitment penalty can become detached from the actual war weariness that triggered the branch.
Repair direction:
- Restore the intended raw war-weariness argument (`iTotalWW` / exact current equivalent); keep the accumulated cost only as the output accumulator.

F037 - provisional KI#358 - AI_preDeclareWar changes "war despite tribute" memory audience to the wrong knowledge set
Status: CONFIRMED CURRENT PRACTICAL-1842 ITERATOR REGRESSION.
Evidence:
- Pre-refactor AdvCiv/K-Mod logic awarded the memory to AI players whose team had met the tribute-paying victim, subject to the master/vassal-locus exclusion.
- Practical 1842 replaced this with an iterator population based on knowledge/potential-enemy relationship to the attacker.
- These populations are not equivalent.
- The original advc.130o logic and XML frame this memory as knowledge that the VICTIM had paid tribute before being attacked.
Consequence:
- An AI that knows the attacker but has never met the tribute-paying victim can receive the memory.
- An AI that knows the victim but has not met the attacker can be omitted.
Repair direction:
- Restore the victim-contact population (plus intended master/vassal exclusions) explicitly; do not substitute the attacker's potential-enemy iterator.

F038 - provisional KI#359 - AI_acceptSurrender reverses the war-duration side of "might capitulate to another rival"
Status: CONFIRMED CURRENT PRACTICAL-1842 COMPARATOR/CONTROL REGRESSION.
Evidence:
- Before the refactor, code did:
  `if (atWarCounter < threshold) continue;`
  then tested overwhelming rival war success.
- Therefore the rival had to have been at war LONG ENOUGH before being treated as a plausible alternative capitulation master.
- Practical 1842 collapsed the logic to a positive condition using:
  `atWarCounter < threshold && overwhelmingWarSuccess`
- This reverses the duration side.
Consequence:
- `bMightCapToOther` can become true during an EARLY war, then cease becoming true once the exact duration threshold intended to make capitulation plausible has been reached.
- This feeds several later decisions about whether to accept surrender before another enemy does.
Repair direction:
- Restore the `>= threshold` meaning (or retain the original `continue if < threshold` structure for clarity).

F039 - provisional KI#360 - AI_knownTechValModifier includes capitulated teams only in the denominator
Status: CONFIRMED CURRENT PRACTICAL-1842 POPULATION MISMATCH.
Evidence:
- Before practical 1842, capitulated teams were excluded from BOTH `iCivsMet` and `iTechCivs`.
- After the refactor, `iCivsMet++` became unconditional while the numerator still excludes capitulated teams.
Consequence:
- Every known capitulated team increases the denominator but can never increase the tech-owning numerator.
- Technologies therefore look artificially rarer / more monopolized and more valuable.
- Modifier feeds ordinary AI technology-trade valuation and Great-Person bulb/discover valuation.
Repair direction:
- Use the same eligible civilization/team population for numerator and denominator, matching the pre-1842 logic.

F040 - provisional KI#361 - AI_getEnemyPowerPercent double-counts shared Defensive Pact allies
Status: CONFIRMED CURRENT INHERITED COALITION-POWER DEFECT.
Evidence:
- AI can hold several chosen-war plans simultaneously.
- Each prospective target's `getDefensivePower()` independently includes its Defensive Pact allies.
- If targets B and C both share pact ally D, summing target defensive power can effectively produce `(B + D) + (C + D)`.
- AdvCiv practical 1366 explicitly noticed this and left a FIXME; no later repair found.
Consequence:
- Shared allies can be counted multiple times in the "enemy power" total.
- AI_getEnemyPowerPercent feeds hard war thresholds, military-need logic, production pressure and newer SAS danger-sensitive weighting.
Repair direction:
- Build the union of hostile/committed defending teams once and sum each team exactly once, rather than summing overlapping per-target coalition totals.

F041 - provisional KI#362 - AI_randomCounterChange callers calculate accumulated relationship ceilings but cap only the 0-2 single-turn roll
Status: CONFIRMED CURRENT HELPER/CALLER CONTRACT DEFECT; wording corrected before durable C011.
Evidence:
- CvTeamAI Open Borders caller computes a long-term `iMax = 2 * OpenBordersAttitudeDivisor + 10`.
- CvPlayerAI Bonus Trade caller similarly computes a long-term cap from the Bonus Trade attitude divisor/change limit.
- Both pass this as `AI_randomCounterChange(iUpperCap, rProb)`.
- The helper applies `iUpperCap` only to the per-turn binomial increment, whose natural maximum is already 2.
- Therefore those meaningful long-term caps are effectively no-ops.
Important correction:
- Normal Open Borders deal start/end resets the OB counter to zero; the earlier working-note claim that excess OB state survives cancellation and decays afterward was WRONG and must not be repeated in C011.
Current live consequence:
- Bonus Trade does provide the strong supported consequence.
- Current default divisor 50 and +2 limit imply a calculated ceiling around 125.
- Sustained valuable imports can push the counter above that intended ceiling.
- When imports disappear, Bonus Trade counter decays rather than being immediately reset; excess hidden state can keep the capped +2 resource-trade attitude for extra turns.
Repair direction:
- Enforce a ceiling on the ACCUMULATED counter, e.g. cap the result after increment or constrain the increment to `max(0, ceiling - currentCounter)`.
- Keep Open Borders recorded as the same ineffective-cap caller contract without overstating its current post-cancellation impact.

F042 - provisional KI#363 - freeing a vassal fails to restore cached war-plan counts against the newly independent team
Status: CONFIRMED CURRENT CACHED-STATE LIFECYCLE DEFECT.
Evidence:
- War-plan-count optimization excludes vassal targets from `m_aiWarPlanCounts`.
- `CvTeam::setVassal(..., false, ...)` explicitly tries to re-add existing plans targeting the team being freed:
  conceptually `AI_updateWarPlanCounts(target, NO_WARPLAN, existingPlan)`.
- It performs that restoration BEFORE clearing the target's current vassal/master status.
- `AI_updateWarPlanCounts()` sees `target.isAVassal()` and immediately returns, making the intended restoration a no-op.
Reachable lifecycle:
- While V is a vassal, enemy E can be dragged into war with V and hold `WARPLAN_ATTACKED_RECENT`; the plan is intentionally excluded from E's cache during vassalage.
- If V is freed while the war continues, the now-countable existing plan is not restored.
- When the plan later transitions `ATTACKED_RECENT -> ATTACKED`, normal cache transition code can decrement the old type that was never incremented, producing a negative per-plan count while incrementing the new type.
Consequence:
- `AI_isAnyWarPlan()` and per-plan cached strategy queries can temporarily disagree with live plans.
- Negative cached counts can persist during the live session until reconstruction/repair.
Ancestry:
- AdvCiv practical 1677 introduced the war-plan-count optimization and this pre-status-change setVassal adjustment together.
- Later storage/serialization descendants did not correct the ordering.
Repair direction:
- Restore/reconstruct target war-plan counts only AFTER the team is no longer considered a vassal, or make the transition helper explicitly operate on the intended future status rather than consulting stale current state.

PRACTICAL-1842 FOCUSED REFRACTOR AUDIT - CLOSED AS A SUBPASS
- Practical 1842 generated five surviving current regressions in this file: F033, F036, F037, F038, F039.
- Its other iterator substitutions were expanded back to their old predicates and checked.
- AI_calculateAreaAIType's shift to KNOWN_POTENTIAL_ENEMY_OF does not lose a reachable pre-contact chosen-war target: normal `canEventuallyDeclareWar()` war-plan creation requires contact, including the relevant Always War path.
- Peace, worst-enemy, forgiveness, counter and timeout iterator conversions examined are population-equivalent or documented semantic changes.
- `AI_isLandTarget()` provides sibling evidence: practical 1842 also changed its target-player loop to our own team, and practical 1941 explicitly repaired that one while F033's analogous error remained.

SERIALIZATION / STORAGE / CURRENT SAS SURFACE
- CvTeamAI current-format read/write payload was mechanically paired in ordered structure, including war-plan/counter arrays, war success, trade/grant values, nuke vector, StrengthMemory and UWAI tail.
- Large SAS/base serialization diff is legacy-save cleanup rather than a current save-stream mismatch.
- `m_aiWarPlanCounts` uses char storage; legitimate current team counts cannot approach signed-char overflow.
- Diplomacy state counters use short storage; overflow requires >~32k continued turns, far beyond configured current game-speed horizons. Record as extreme-duration robustness only.
- Current SAS semantic additions inspected so far (KI#312 tech-locus repair, victory-denial peace policy, Barbarian AreaAI toggle, diagnostics/logging, serialization cleanup) preserve their intended contracts; no additional new SAS root established.

IMPORTANT KILLED / DORMANT / ROBUSTNESS CANDIDATES FOR FILE 010
- `scaled::hash(eTech, getLeaderID())` DOES incorporate the leader's capital plot through the hash helper; the tech-monopoly comment promising map-position variation is correct.
- `AI_getTechMonopolyValue()` recipient-team UU/UB averaging is intentional advc.550c behavior; `MemberIter::nextIndex()` yields the alive-member denominator after the loop. No divisor defect.
- Seller/recipient naval-context asymmetry in tech monopoly valuation remains heuristic/design territory; no source-backed correctness contradiction.
- `AI_chooseElection()` +20000 team-vote bonus does not accidentally choose Secretary-General election over victory: SG elections use a separate engine path and do not coexist in that VoteSelectionData list.
- `AI_getLatestVictoryVoteSource()` is correct: `CvGame::isDiploVote(eVoteSource)` means `getDiploVoteCount(eVoteSource) > 0`, i.e. the institution currently exists; it is not merely an XML category query.
- Missing `UNITAI_ATTACK_CITY_LEMMING` switch arm in AI_getTechMonopolyValue() is genuine enum incompleteness but dormant in current data: no current unit uses Lemming as `<DefaultUnitAI>`. Secondary/event-spawn usage does not reach this switch.
- `AI_getLowestVictoryCountdown()` ignoring countdown 0 is not currently proved wrong; victory resolution/reset consumes the 0 state in CvGame::testVictory before a persistent AI-observable state was established.
- `AI_forgiveEnemy()` appears to mutate shared `iDelta` across members, but current domain is only 0/-1 and the relevant war-success divisor is team-shared through era; no different current per-member result was established.
- `AI_enemyTradeResentmentFactor` apparent `NO_TEAM` worst-enemy call is protected by reachable condition: actual war enemy produces positive enmity and a valid worst enemy.
- `AI_surrenderTrade()` / `AI_acceptSurrender()` unusual `!isVassal(master) && canVassalRevolt(master)` use is intentional: canVassalRevolt also exposes raw land/population escape thresholds and can reject a prospective master the team is already strong enough to escape.
- `AI_finalizeInit()` rebuilds per-plan counts but does not first force `m_bAnyWarPlan=false`. This is defensive weakness, but the currently proved F042 lifecycle still updates the aggregate boolean correctly before serialization; no independent stale-true/no-plan current root established. Resetting false before reconstruction would still be a cheap robustness improvement.
- `AI_teamCloseness(..., bConsiderLandTarget, ...)` does not read the flag, but history shows AdvCiv intentionally removed that extra adjustment after moving land-connection influence into AI_playerCloseness(); stale API residue only.
- Raw EnumMap team getters would not accept NO_TEAM, but supported callers checked so far branch before indexing or establish real teams. No sentinel root from hypothetical invalid external calls.
- AI_mayAttack TeamTypes overload assumes a real defender team; normal plot/unit wrappers establish ownership/team first, and Always Hostile combat-owner logic explicitly does not support NO_TEAM.
- Minor-civ exclusion in war-plan-count cache does not currently expose an F042-style normal midgame status transition with existing plans; no sibling root established.
- Relationship transition pass so far: war/peace counters reset at the intended hooks; Open Borders deal start/end resets its OB counter; Defensive Pact/shared-war relationship memory deliberately decays/retains state. No F042-style ordering sibling established.
- `AI_calculateAreaAIType()` practical-1842 population concern killed by war-plan contact requirements.
- Known `AI_getEnemyPowerPercent()` Defensive Pact FIXME is F040 itself, not a second root.
- `nextIndex()` denominator scan so far: nearly all averages use explicit max(1,...) or have nonempty team-member contracts; the exceptional wrong-population averages already map to F034/F039.
- Public sentinel/default/assert/fixed-array sweep has not yet produced F043.

CURRENT FALSE-POSITIVE / CORRECTION DISCIPLINE
- Do not resurrect rejected candidates merely because they look odd in isolation.
- Do not use the superseded Open Borders post-cancellation decay example for F041.
- Do not split one root into several KIs merely because the same bad population/cap contract has multiple callers (F035, F041).
- Do not classify a deliberate K-Mod/AdvCiv AI heuristic as a defect without a broken contract, wrong-domain variable, set mismatch, lifecycle inconsistency, or other source/data evidence.
- F043 is currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP1
1. Continue fresh independent whole-file mechanical scan of CvTeamAI.cpp:
   - unchecked divisors / percent denominators
   - `continue` + later denominator population mismatches
   - FAssert/default switch reachability
   - TODO/FIXME comments not already owned
   - fixed enum-map / array domains
   - suspicious unused/mismatched parameters
2. Finish public NO_TEAM/NO_PLAYER/raw-map caller contracts, especially generic wrapper paths.
3. Finish relationship/cached-count lifecycle transitions (Open Borders / Defensive Pact / shared war / vassal / war-plan state), while avoiding already-killed duplicates.
4. Recheck worst-enemy lifecycle against current KI#208 repair lineage.
5. Complete current SAS-vs-AdvCiv semantic surface disposition.
6. Run complete Album / reviewed-KI collision check for F032-F042.
7. Reread all eleven findings for exact present reachability, ancestry, consequence and surgical repair wording.
8. Only if all remaining passes close:
   - mark file 010 CvTeamAI.cpp COMPLETE
   - total becomes 10 / 169
   - freeze durable C011
   - byte-validate C010->C011 reconstruction
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp` (6,343 lines).

END WORKING CHECKPOINT C010-WIP1
================================================================================
WORKING CHECKPOINT C010-WIP2 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP1.
- Durable completed-file checkpoint is STILL C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots remain F032-F042 / provisional KI#353-KI#363.
- No F043 has survived.
- Do NOT promote this checkpoint to C011.
- The purpose of WIP2 is to protect the substantial closure work completed after WIP1.

POST-WIP1 CLOSURE WORK

1. AI_preDeclareWar(..., eWarPlan, ...) unused parameter - CLOSED CLEAN / API RESIDUE
- Whole-file parameter-use scan showed `eWarPlan` is unused in AI_preDeclareWar.
- History confirms this is not a later accidental loss of behavior.
- Practical 1676 created AI_preDeclareWar by splitting AI/game-rule work out of CvTeam::declareWar.
- The new pre-declaration helper accepted the original declareWar eWarPlan parameter from birth but did not use it.
- AI_postDeclareWar does use eWarPlan to install/update the actual war-plan state.
- Therefore the pre-function parameter is harmless split-API symmetry/residue, not F043.

2. Other meaningful unused parameters - DISPOSED
- F033's unused `eTeam` in AI_haveSeenCities remains a real bug because the function's target population was accidentally replaced by own-team iteration.
- AI_reportSharedWarSuccess(..., eEnemy, ...) intentionally does not currently use eEnemy; source comments explicitly say that enemy does not currently matter because distress is aggregated. No defect.
- AI_forgiveEnemy(..., bFreed, ...) has documented historical residue; current behavior does not require the flag.
- AI_teamCloseness(..., bConsiderLandTarget, ...) historical check reconfirmed: AdvCiv deliberately removed the separate land-target bonus after moving land-connection influence into AI_playerCloseness. Stale API residue only.

3. Pre-contact At Peace counter - CLOSED CLEAN
- AI_doCounter increments AtPeaceCounter even before teams have met.
- This initially looked as though relationship history might begin too early.
- AdvCiv's attitude logic explicitly accounts for this inherited counter semantic by using:
  `min(AI_getAtPeaceCounter(...), AI_getHasMetCounter(...))`
  when computing the peace-attitude contribution.
- Other consumers checked either require an already-known/previous-war rival or intentionally use elapsed non-war time.
- Therefore AtPeace-before-contact is intentional inherited state semantics, not a bug.

4. Relationship / cached-counter lifecycle pass - NO NEW ROOT
- Open Borders:
  - setOpenBorders resets the OB counter to zero when status changes.
  - This is the important correction already incorporated into F041: do NOT claim excess OB counter survives cancellation.
- Defensive Pact:
  - pact status change triggers attitude refresh; the DP relationship counter is allowed to decay as relationship memory rather than hard-resetting.
- Shared War:
  - declaration/post-declaration and peace hooks reset/update the relevant counter consistently; post-war memory/decay behavior is intentional.
- War/peace:
  - underlying at-war state is changed before associated live-state count updates; no F042-style stale-status update ordering found.
- Vassal transition:
  - F042 remains the exceptional cache-order defect because its explicit war-plan re-addition is attempted while the target still reports current vassal status and is therefore rejected.
- Minor-civ cache exclusion:
  - no normal midgame status transition analogous to vassal freeing with pre-existing plans was established.
- No second F042-style lifecycle root found.

5. Worst-enemy lifecycle against KI#208 - CLOSED CLEAN
- Current KI#208 sticky-worst-enemy repair is present.
- After vassal/master status actually changes, CvTeam::setVassal performs broad third-party attitude refreshes.
- Those refreshes default to worst-enemy recalculation.
- No stale-worst-enemy sibling to F042 was established.

6. Practical 1842 focused iterator audit - RECONFIRMED EXHAUSTED
- Current survivors from practical 1842 remain exactly:
  F033, F036, F037, F038, F039.
- AI_isLandTarget's later practical-1941 repair remains strong sibling evidence for F033.
- Remaining 1842 iterator substitutions checked reproduce the former population or represent an intentional documented change.
- No additional practical-1842 root remains open.

7. FAssert / default-switch reachability pass - NO NEW CURRENT ROOT
- AI_getAttitudeWeight and other attitude switches cover all five valid AttitudeTypes:
  Furious, Annoyed, Cautious, Pleased, Friendly.
- Their default error/assert branches therefore remain unreachable for valid current enum values.
- AI_getTechMonopolyValue's default switch remains incomplete for UNITAI_ATTACK_CITY_LEMMING, but current XML has no unit whose DefaultUnitAI is Lemming. Secondary/event-spawn Lemming usage does not reach this switch.
- AI_estimateTotalYieldRate's unknown-yield branch is not newly reachable: live current callers use Production/Commerce; the standard yield domain is otherwise understood by the replacement estimator.
- No F043 from enum/default growth.

8. Divisor / `nextIndex()` pass - NO NEW ROOT
All `MemberIter::nextIndex()` sites were re-enumerated.
- AI_getCurrEraFactor explicitly rejects/deals with dead-team zero members.
- personality/team averages use max(1, count) or are reached only for an alive team with at least one member.
- AI_refuseToTalkWarThreshold lacks max(1), but its live caller path concerns a living at-war team and therefore has at least one member.
- AI_getTechMonopolyValue recipient average uses alive-member count correctly.
- F034 remains the intentional exception where the denominator POPULATION is wrong, not zero.
- F039 remains the other known population mismatch.
- No additional divide-by-zero or wrong-population denominator established.

9. Direct-array / vector indexing pass - NO NEW ROOT
Direct `[]` sites were enumerated.
- vote-selection index is bounded by the vote-options loop.
- resource `bonuses[eBonus]` is guarded against NO_BONUS and allocated to current BonusInfos size.
- air-unit vector loop uses its size.
- affected-team temporary array indexes are bounded by its constructed count.
- nuke-explosion vector serialization accesses `[0]` only after size/nonempty guards on both read and write.
- No current direct-index root.

10. Variable-domain comparison scan - F032 REMAINS UNIQUE
- Fresh grep/static pass searched integer-looking variables compared with team/player/enum identifiers/getters.
- F032's `iFirstMostVotes == getID()` remains the clear incompatible-domain comparison.
- No sibling vote-count/team-ID or similar enum/int comparison survived.
- An apparent AttitudeTypes-vs-int threshold comparison is intentional: personality refusal thresholds are stored as integer attitude enum thresholds and compared in that domain.

11. AI_doWar() complete linear reread - F035 ONLY
- Full AI_doWar range reread rather than grep-only review.
- F035 remains the only independent root.
- Other team-wide aggression/final-war flags are intentionally existential (`any member`) rather than pretending to form set intersections.
- The bad branches remain exactly the ones subtracting total Dagger count from independent FinancialTrouble / GET_BETTER_UNITS populations and comparing against the wrong population size.
- `iOurPower *= (100 - iEnemyPowerPercent)` may go non-positive when current enemy burden exceeds 100%, but that coherently expresses no spare power for another war rather than an arithmetic defect.
- No second control-flow root.

12. War-rand / surrender / war-value arithmetic reread - NO NEW ROOT
- AI_getWarRands denominator `2 + members + vassals` positive.
- AI_getWarThresholds member averages positive for live team.
- AI_getTotalWarOddsTimes100 guards divisor.
- AI_surrenderTrade mean/median populations retain live free/non-capitulated major-team support under reachable calls; averages guarded.
- AI_acceptSurrender's own war-weariness API use passes actual war-weariness state and is not another F036.
- AI_warSpoilsValue, AI_warDiplomacyCost, AI_startWarVal and AI_endWarVal denominator/range checks remained guarded or invariant-positive.
- "should consider area power" and similar comments remain explicit heuristic TODOs, not broken current contracts.

13. Smell / FIXME / TODO scan - NO NEW ROOT
Notable current comments were dispositioned:
- AI_getEnemyPowerPercent shared Defensive Pact FIXME = F040 itself.
- AI_knownTechValModifier minor-civ inclusion comment remains a design/heuristic question; no source-backed correctness contradiction.
- AI_endWarVal "should consider area power" is heuristic limitation.
- AI_doWar "make sure they share an area" is old strategic heuristic TODO; current total-war selection deliberately has nearby/distant passes and explicit nonlocal exceptions. Comment alone is not a correctness contract.
- AI_getTechMonopolyValue "relying only on AndTech seems questionable" is heuristic/mod-support completeness debt, not a current wrong-data contract.
- Old AI_isHasPathToEnemyCity comment explicitly marks deleted BBAI code as wrong; current replacement sets/iterates actual enemy teams and is not that old bug.

14. Current SAS-vs-Base semantic surface - RECHECKED CLEAN / KNOWN
Whitespace-insensitive current-vs-Base diff contains 39 hunks.
Substantive SAS/current changes already dispositioned include:
- diagnostics/logging
- Barbarian AreaAI option
- KI#312 tech-locus helpers and related valuation
- current tech-trade relative-strength/danger tuning
- victory-denial peace policy
- KI#208 worst-enemy repair
- war-plan GameRecord hooks
- serialization legacy-save cleanup
- AI_doWar logging/tuning
No additional new SAS-specific defect surfaced in this closure pass.

15. First semantic collision scan F032-F042 - CLEAN
Searched current reviewed README_Known_Issues.md and repository cpp_file_audit_album.txt by each finding's semantic terms.
- F032: nearby existing issue is the player-facing BUG diplomatic-vote prediction mismatch. That is a distinct UI approximation/spoiler problem; F032 is the private AI internal runner-up algorithm comparing vote count to team ID.
- F033 target-city visibility: no existing same-root AI_haveSeenCities issue found.
- F034 rival air denominator: no existing same-root finding.
- F035 Dagger/financial/get-better-units team population: no existing same-root finding.
- F036 warCommitmentCost war-weariness variable: no existing same-root finding.
- F037 war-despite-tribute observer population: no existing same-root finding.
- F038 acceptSurrender war-duration inversion: no existing same-root finding.
- F039 capitulated tech-rarity denominator: no existing same-root finding.
- F040 shared Defensive Pact enemy-power double count: no existing same-root finding.
- F041 relationship accumulated-counter cap contract: no existing same-root finding.
- F042 vassal-freeing war-plan cache restoration: no existing same-root finding.
- Final collision pass and wording reread still required before durable C011.

16. Header / member-storage / serialization invariant pass - NO NEW ROOT
- CvTeamAI.h EnumMap storage domains align with implementations.
- m_aiWarPlanCounts char capacity is far above legitimate current simultaneous count.
- short diplomacy counters overflow only after >~32k turns, far beyond every current configured game-speed horizon; extreme-duration robustness only.
- Current read/write payload remains structurally paired.
- Nuke vector raw storage has nonempty guards.
- Redundant `CvTeamAI::` qualification in an in-class declaration is a compile-accepted style oddity, not behavioral.
- No declaration/definition state mismatch found.

CURRENT FINDING SET AFTER WIP2
- F032 / KI#353 - diplo vote counter-candidate vote-count vs team-ID comparison
- F033 / KI#354 - AI_haveSeenCities iterates own team
- F034 / KI#355 - rival-air average denominator multiplied by air classes / mismatched eligible population
- F035 / KI#356 - AI_doWar invalid Dagger set subtraction / non-Dagger population
- F036 / KI#357 - warCommitmentCost passes iWWCost instead of iTotalWW
- F037 / KI#358 - war-despite-tribute memory wrong observer population
- F038 / KI#359 - acceptSurrender war-duration comparator inversion
- F039 / KI#360 - capitulated teams denominator-only in tech rarity
- F040 / KI#361 - shared DP ally double-count in enemy power
- F041 / KI#362 - accumulated relationship ceilings applied only to 0-2 per-turn roll
- F042 / KI#363 - vassal freeing fails to restore cached war-plan counts
- F043 remains UNUSED.

EXACT CURSOR AFTER C010-WIP2
Still required before CvTeamAI.cpp may close:
1. Finish second/deeper semantic collision check against older KI wording / Album aliases, not only obvious keyword matches.
2. Finish the last public sentinel/wrapper caller sweep if any generic TeamTypes paths remain unchecked.
3. Reread F032-F042 one by one against current source and historical control for:
   - present reachability
   - exact ancestry/classification
   - consequence without overstatement
   - surgical repair direction
4. Confirm no open candidate remains from the fresh mechanical sweep.
5. If all above close cleanly:
   - mark CvTeamAI.cpp COMPLETE
   - 10 / 169 complete
   - freeze durable C011
   - generate and byte-validate cumulative C010->C011 / prior-WIP review artifact as appropriate
   - open file 011 SASGameRecordLog.cpp
6. If handoff occurs first:
   - create C010-WIP3, never pretend file 010 is complete.

END WORKING CHECKPOINT C010-WIP2
================================================================================
WORKING CHECKPOINT C010-WIP3 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP2.
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots now extend through F044 / provisional KI#365.
- Do NOT promote this checkpoint to C011.
- Important correction: C010-WIP2's statement that the practical-1842 iterator subpass was exhausted with exactly F033/F036-F039 is SUPERSEDED. Renewed hunk-by-hunk review found F043 and F044 as additional surviving practical-1842 regressions.
- This checkpoint exists specifically so that the renewed 1842 findings and corrected audit state cannot be lost to thread/network interruption.

NEW CURRENT ROOT AFTER WIP2

F043 - provisional KI#364 - practical 1842 stopped clearing old arrogant-demand memories toward the declaring team's own master/vassal locus
Status: CONFIRMED CURRENT PRACTICAL-1842 POPULATION REGRESSION.

Original contract / ancestry:
- Practical 1260 (`ccf5d7b95...`) explicitly states in its log:
  "AI civs now forget all 'arrogant demands' when they declare any war. [advc.130o]"
- Pre-1842 declare-war logic iterated all living major players and, for a primary declaration, cleared each declaring member's `MEMORY_MADE_DEMAND` toward every such player.
- Practical 1842 replaced the broad player loop with:
  `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`
- `KNOWN_POTENTIAL_ENEMY_OF` requires contact and excludes players in the declaring team's same-master locus (master, vassals, sibling vassals).
- This silently narrowed a deliberately all-player memory reset.

Why excluded memories are not already guaranteed gone:
- `CvTeam::setVassal()` does clear `MEMORY_MADE_DEMAND` when a team becomes a vassal, but it clears the NEW VASSAL'S OWN memories toward others.
- It does not symmetrically clear a master's existing `MEMORY_MADE_DEMAND` about that new vassal.

Concrete supported sequence:
1. AI A accepts an arrogant demand from human H, so A stores `MEMORY_MADE_DEMAND(H)`.
2. H later becomes A's VOLUNTARY vassal.
3. A later declares an unrelated war on C.
4. The practical-1260 contract says A should now forget all old arrogant-demand memories.
5. Current practical-1842-era loop excludes H because H is in A's master/vassal locus.
6. A therefore retains the old negative demand memory.

Why the voluntary-vassal example is useful:
- Capitulation has special forced-attitude behavior that could temporarily mask some visible consequences.
- Voluntary vassalage gives the cleanest demonstration that this remains meaningful AI state.

Current consequence:
- `MEMORY_MADE_DEMAND` has real current negative attitude/decay values in leader data.
- UWAI / demand-war reasoning also consumes this memory count.
- The stale memory therefore remains behaviorally meaningful; it is not dead bookkeeping.

Collision:
- No existing reviewed KI / Album root with the same all-demand-reset population regression was found.

Repair direction:
- Separate the deliberately all-major-player `MEMORY_MADE_DEMAND` reset from the narrower declaration-observer loop.
- Do not rely on `KNOWN_POTENTIAL_ENEMY_OF` for the all-player forget operation.

F044 - provisional KI#365 - practical 1842 suppresses valid war-on-friend memories for observers inside the aggressor's master/vassal locus
Status: CONFIRMED CURRENT PRACTICAL-1842 OBSERVER-POPULATION REGRESSION.

Current structure:
- `AI_preDeclareWar()` now loops over:
  `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`
  before asking:
  `kPlayer.AI_disapprovesOfDoW(getID(), eTarget)`
  and applying `MEMORY_DECLARED_WAR_ON_FRIEND`.
- `KNOWN_POTENTIAL_ENEMY_OF` excludes players sharing the aggressor's current master locus.

Pre-1842 semantics:
- The old declare-war loop considered all living major third-party players.
- For the war-on-friend memory, it excluded the aggressor's OWN TEAM.
- It did NOT generally exclude observers merely because they shared the aggressor's master/vassal locus.
- It did reject cases where the VICTIM shared the observer's master locus, which is a different relationship.

Current helper confirms the broader intended semantic population:
- `CvPlayerAI::AI_disapprovesOfDoW(eAggressor, eVictim)` rejects:
  - self/aggressor/victim invalid cases,
  - capitulated victim,
  - observer not meeting victim,
  - observer already at war with victim,
  - victim sharing the observer's master locus,
  - and other specific partner/memory exceptions.
- It does NOT reject an observer merely for sharing a master with the AGGRESSOR.
- It deliberately does not require the observer to have met the aggressor, matching inherited BtS behavior.

Why the DoW popup's narrower audience does not make the memory change intentional:
- Practical 1416's declaration-warning popup excludes same-master observers for presentation.
- While a vassal relation is active, the vassal/master forced-attitude logic can make listing that observer as a currently disapproving "third party" misleading.
- That UI choice does not erase the underlying historical memory semantics.
- `MEMORY_DECLARED_WAR_ON_FRIEND` used to be stored and can matter later if the vassal relationship ends.

Concrete supported lifecycle:
1. AI B is a voluntary vassal / same-master-locus member of aggressor A.
2. B knows and likes outside civilization C.
3. A declares war on C.
4. Pre-1842 code could record `MEMORY_DECLARED_WAR_ON_FRIEND` in B about A.
5. Practical 1842's `KNOWN_POTENTIAL_ENEMY_OF(A)` prefilter excludes B before `AI_disapprovesOfDoW` is even asked.
6. The memory is therefore never created.
7. If B later becomes independent, `CvTeam::setVassal(...false)` does not clear this memory type; under the old behavior it could then contribute normally to B's attitude / war utility toward A.

Current consequence:
- Current leader data assigns real negative attitude weights to `MEMORY_DECLARED_WAR_ON_FRIEND` (often -100, stronger for some leaders) with decay.
- UWAI / WarUtility-related logic reads this memory as well.
- Thus suppressing it can alter post-vassalization diplomacy and strategic memory.

Collision:
- No same-root current KI / Album finding found.

Repair direction:
- Do not use the aggressor-potential-enemy population as the audience for war-on-friend memory.
- Restore all living major third-party observers and let `AI_disapprovesOfDoW` perform its intended semantic filtering, or build an iterator predicate exactly equivalent to that helper's requirements.
- Keep F044 separate from:
  F037 = tribute-memory observer visibility population,
  F043 = all-player arrogant-demand reset population.
  They originate from one overly broad loop replacement but break distinct contracts and have distinct consequences.

RENEWED PRACTICAL-1842 AUDIT - IMPORTANT CORRECTION
- WIP2's "practical 1842 focused iterator audit - closed/exhausted" statement is no longer valid.
- Renewed review mechanically re-opened all 166 `CvTeamAI.cpp` hunks from:
  `LLM_Helpers/context/commit_diffs/AdvCivPreSAS_1842_99e98d3130.diff`
- Current surviving regressions attributable to practical 1842 now include at least:
  - F033 - AI_haveSeenCities own-team substitution
  - F036 - warCommitmentCost wrong war-weariness argument
  - F037 - tribute-memory observer population
  - F038 - acceptSurrender duration inversion
  - F039 - known-tech denominator mismatch
  - F043 - all-demand-reset population narrowing
  - F044 - war-on-friend observer population narrowing
- The same practical also contained historical regressions later repaired by descendants, strengthening the need for careful hunk review:
  - AI_isLandTarget target-member loop was changed to own-team iteration in 1842; practical 1941 repaired it.
  - AI_getAttitudeVal was accidentally changed to return an AttitudeTypes-like enum value converted to int instead of the averaged raw attitude value; practical 1903 restored `iAttitudeVal / iCount`.
- These repaired siblings are historical evidence only, not new current findings.

RENEWED 1842 HUNK DISPOSITION SO FAR
- Hunk 21: F033 current survivor.
- Hunk 24: historical AI_isLandTarget own-team substitution; repaired practical 1941.
- Hunk 27: historical AI_getAttitudeVal wrong return-domain regression; repaired practical 1903.
- Hunk 30: contains three distinct current survivors:
  - F037 tribute-memory observer population
  - F043 all-player `MEMORY_MADE_DEMAND` reset narrowed
  - F044 war-on-friend observer population narrowed
- Hunk 45: F036 current survivor.
- Hunk 51: F039 current survivor.
- Hunk 74: F034 current survivor.
- Hunk 76: F038 current survivor.
- Large vassal/surrender, peace, worst-enemy, forgiveness, counter, AreaAI, tech-monopoly and water-area iterator rewrites checked so far are equivalent or intentional after expanding their iterator predicates.
- `AI_isWaterAreaRelevant`'s own-locus `NOT_A_RIVAL_OF` treatment and known-potential-enemy treatment remains coherent with its current bloc/visibility semantics.
- `AI_forgiveEnemy` narrowing reviewed so far does not establish an independent current lost-memory population.
- Known-potential-enemy war-plan loops remain protected by reachable war-plan contact requirements.
- Renewed 1842 audit is STILL OPEN: do not again call it exhausted until every high-risk iterator/population/control substitution is dispositioned.

OTHER POST-WIP2 CLOSURE RESULTS
- Public `NO_TEAM` / generic TeamTypes wrapper scan continued to come back clean:
  - supported generic plot/unit wrappers establish ownership/team before calling raw TeamTypes APIs,
  - optional "any team" helpers branch before indexing raw team EnumMaps,
  - no supported literal/current NO_TEAM path into the unsafe raw maps was established.
- Direct array/vector and `nextIndex()` denominator checks remain clean beyond already-owned F034/F039.
- F032-F042 were individually re-read substantially for ancestry, reachability and consequence; no collision found so far.
- F041 wording correction remains mandatory:
  - Open Borders resets its counter on agreement status change.
  - Do NOT use the old post-cancellation Open Borders decay example.
  - Strong current consequence remains Bonus Trade accumulated excess + later decay.
- No F043/F044 collision with existing reviewed KI / Album roots was found.

CURRENT FINDING SET AFTER WIP3
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP3
1. Continue renewed practical-1842 hunk-by-hunk audit, especially iterator/population substitutions not yet fully dispositioned.
2. Do not limit closure to 1842: continue independent current whole-file pass over remaining CvTeamAI methods.
3. Finish relationship/cache/sentinel/default/array/divisor checks not already closed.
4. Deep semantic collision and wording reread of F032-F044 against:
   - current source,
   - base AdvCiv 1.14,
   - relevant historical controls,
   - current Album,
   - reviewed README_Known_Issues.
5. Ensure each finding has:
   - current supported reachability,
   - exact inherited/SAS ancestry,
   - consequence stated without overreach,
   - surgical repair direction.
6. Confirm no candidate remains open or merely "probably clean."
7. If all closure work is genuinely exhausted:
   - mark file 010 CvTeamAI.cpp COMPLETE
   - total becomes 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.
8. If another handoff occurs first:
   - create C010-WIP4 and keep CvTeamAI.cpp OPEN.

END WORKING CHECKPOINT C010-WIP3
================================================================================
WORKING CHECKPOINT C010-WIP4 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP3.
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots now extend through F045 / provisional KI#366.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the post-WIP3 worst-enemy finding and the renewed practical-1842 progress before further investigation continues.

F045 - provisional KI#366 - incumbent worst enemy can remain inside the same master/vassal locus even though such teams are excluded from all new candidates
Status: CONFIRMED CURRENT WORST-ENEMY ELIGIBILITY / LIFECYCLE DEFECT.

Current structure:
- `AI_updateWorstEnemy()` treats the incumbent and replacement candidates differently.
- Existing `m_eWorstEnemy` is evaluated directly through `AI_enmityValue(m_eWorstEnemy)`.
- New/replacement candidates are searched through:
  `TeamIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`
- `KNOWN_POTENTIAL_ENEMY_OF` excludes teams sharing the same current master/vassal locus.
- `AI_enmityValue()` itself does not equivalently reject a voluntary vassal/master-locus incumbent.

History-dependent inconsistency:
1. Team A legitimately has team B as worst enemy while both are independent.
2. B later becomes A's VOLUNTARY vassal (or otherwise enters A's same-master locus).
3. Vassalization triggers broad attitude / worst-enemy refreshes.
4. `AI_updateWorstEnemy()` evaluates incumbent B directly, outside the candidate iterator's master-locus exclusion.
5. If B's retained enmity remains large enough, B can stay `m_eWorstEnemy`.
6. Starting from scratch in the exact same post-vassal state, B could never be chosen as a new worst enemy because the normal candidate population excludes it.

Concrete gameplay consequence:
- `AI_techTrade()` has an unconditional permission when WE are the recipient's vassal, but not the reverse master->vassal direction.
- It subsequently checks whether `AI_getWorstEnemy() == eToTeam` and may return `DENIAL_WORST_ENEMY`.
- Therefore an AI master can potentially refuse technology trade to its own voluntary vassal because that vassal remains a grandfathered stale worst enemy.
- Other worst-enemy consumers can inherit the same impossible-by-fresh-selection state.

Relation to existing KI#208:
- KI#208 is a different root: the previously repaired sticky-worst-enemy flag/order problem where later enemy-trade-memory processing could overwrite the need to recalculate worst enemy.
- F045 is candidate-eligibility asymmetry after a master/vassal relationship transition:
  incumbent evaluation bypasses the population restriction imposed on all replacements.
- Keep F045 separate from KI#208.

Ancestry:
- Practical 1842 moved the replacement scan to `KNOWN_POTENTIAL_ENEMY_OF` during the AgentIterator refactor.
- The incumbent remained evaluated outside that iterator population.
- No descendant repair for this incumbent/candidate asymmetry found so far.
- Later AdvCiv work continues to treat master/vassal status as relevant to worst-enemy semantics, reinforcing that same-locus teams should not normally be eligible.

Repair direction:
- Apply the same eligibility predicate to the incumbent before retaining it.
- If current `m_eWorstEnemy` is no longer a valid `KNOWN_POTENTIAL_ENEMY_OF` candidate (or exact intended equivalent), discard/recalculate it rather than grandfathering it.
- Prefer centralizing candidate eligibility so incumbent and replacement paths cannot diverge again.

POST-WIP3 RENEWED PRACTICAL-1842 PROGRESS
- Practical-1842 re-audit remains OPEN.
- Relationship-filter substitutions revisited after F043/F044:
  - `AI_forgiveEnemy()` changes examined so far do not establish an independent lost-memory population.
  - Large vassal/surrender iterator conversions examined preserve old master/vassal free-team cases or are intentional semantics.
  - War-value / future-enemy iterator conversions examined use `isFutureWarEnemy()` and related logic to improve sibling-vassal / Defensive Pact accounting; no accidental population loss established.
- F045 adds another practical-1842 survivor in the worst-enemy replacement population.
- Current practical-1842 survivors now include at least:
  F033, F036, F037, F038, F039, F043, F044, F045.
- `AI_isWaterAreaRelevant()` practical-1842 rewrite is the exact currently OPEN subpass:
  - own/master-locus cities are deliberately grouped together,
  - "other" cities become known potential enemies,
  - no-cheating `AI_deduceCitySite()` logic was added,
  - no defect has yet been classified there.
- Do not call practical 1842 exhausted until that and all remaining hunk populations are dispositioned.

CURRENT FINDING SET AFTER WIP4
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP4
1. Resume renewed practical-1842 audit at `AI_isWaterAreaRelevant()` and finish its exact pre/post population semantics.
2. Continue every remaining high-risk practical-1842 iterator/control substitution; do not assume the refactor is exhausted.
3. Continue independent whole-current-file audit after 1842:
   - remaining public sentinel/wrapper contracts
   - relationship and cached-state transitions
   - fixed-array / enum / divisor / default-assert reachability
   - TODO/FIXME and unused/mismatched parameter scan
4. Deep semantic collision + wording reread F032-F045 against current Album and reviewed KI ledger.
5. For every finding verify:
   - current supported reachability
   - exact ancestry/classification
   - consequence without overstatement
   - surgical repair direction
6. If another handoff occurs before complete:
   - freeze C010-WIP5 first.
7. Only when all closure work is genuinely exhausted:
   - mark CvTeamAI.cpp COMPLETE
   - total becomes 10 / 169
   - freeze durable C011
   - validate reconstruction byte-for-byte
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP4
================================================================================
WORKING CHECKPOINT C010-WIP5 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP4.
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots now extend through F046 / provisional KI#367.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the post-WIP4 renewed practical-1842 audit, including the cascading-war declared-war-memory defect and corrected observer-contact wording for F037/F044.

F037 / provisional KI#358 - WORDING BROADENING / CORRECTION
- Root remains the practical-1842 replacement of the original tribute-memory observer population with the broad outer:
  `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`
  keyed to the AGGRESSOR.
- Pre-1842 logic for the "war despite received tribute" memory explicitly required the OBSERVER to have met the tribute-paying VICTIM, plus its separate master/vassal exclusions.
- `KNOWN_POTENTIAL_ENEMY_OF(aggressor)` instead:
  - requires observer contact with the AGGRESSOR,
  - excludes observer if in aggressor's same-master locus,
  - does NOT itself guarantee observer contact with the tribute-paying victim.
- Therefore the refactor can both:
  1. suppress a formerly valid observer who knows the victim but has not met the aggressor, and
  2. admit an observer who knows the aggressor but not the victim, subject to the remaining inner checks.
- Keep this as F037: one observer-population regression in the tribute-memory contract.
- Repair should reconstruct the ORIGINAL victim-knowledge population explicitly rather than reuse the aggressor-potential-enemy iterator.

F044 / provisional KI#365 - WORDING BROADENING
- Root remains the practical-1842 outer `KNOWN_POTENTIAL_ENEMY_OF(aggressor)` filter applied before `AI_disapprovesOfDoW`.
- Earlier WIP wording emphasized observers in the aggressor's master/vassal locus.
- Current helper trace proves the regression is broader:
  `CvPlayerAI::AI_disapprovesOfDoW(eAggressor,eVictim)` deliberately does NOT require the observer to have met the aggressor:
  `//!kOurTeam.isHasMet(eAggressor) // Not checked by BtS either`
- The helper DOES require the observer to know the victim and applies the actual victim/friend/master-locus semantic filters.
- Pre-1842 all-major-player iteration therefore allowed an outside observer who knows/likes the victim but has not yet met the aggressor to store `MEMORY_DECLARED_WAR_ON_FRIEND`.
- Practical 1842 silently adds an aggressor-contact requirement and same-aggressor-locus exclusion before the helper gets a chance to decide.
- This can suppress historical diplomatic memory that was intended to become meaningful later once contact/status changes.
- Keep same-master-locus and no-aggressor-contact examples under the single F044 observer-population root.
- Repair should restore the broad third-party observer population and let `AI_disapprovesOfDoW` enforce its own semantic eligibility.

F046 - provisional KI#367 - practical 1842 can suppress direct victim `MEMORY_DECLARED_WAR` on cascading declarations before first contact
Status: CONFIRMED CURRENT PRACTICAL-1842 CONTACT/ORDERING REGRESSION.

Current declaration ordering:
- `CvTeam::declareWar(eTarget,...)` calls:
  1. `AI().AI_preDeclareWar(eTarget,...)`
  2. sets both teams' at-war flags
  3. performs other immediate war-state work
  4. only later calls `meet(eTarget, false)`
  5. later triggers Defensive Pact and vassal-alignment cascading wars.
- Thus AI_preDeclareWar executes with the PRE-WAR contact graph.

Why primary wars usually mask the defect:
- Normal voluntary war-plan / `canEventuallyDeclareWar()` logic requires `isHasMet(eTarget)`.
- Therefore the primary aggressor/victim pair is normally already known and passes `KNOWN_POTENTIAL_ENEMY_OF`.

Why cascading wars are different:
- Defensive Pact and vassal-alignment reactions queue new war declarations between teams that need not already have met each other.
- `triggerDefensivePacts` can queue e.g. third-party defender C -> original attacker A with `bPrimary=false`.
- `triggerWars` drains the queue by directly invoking `GET_TEAM(attacker).declareWar(defender,...)`; there is no separate pre-contact meeting before AI_preDeclareWar.
- The nested declaration itself only establishes contact later through `meet`.

Concrete supported example:
1. A declares war on B.
2. B has a Defensive Pact with C.
3. C has never met A.
4. The pact reaction queues C declaring war on A.
5. C's `declareWar(A)` enters `AI_preDeclareWar(A,...)` before C/A meet.
6. Practical-1842 outer iterator:
   `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(C)`
   excludes A because A has not met C yet.
7. Therefore the inner direct-victim branch:
   `if (kPlayer.getTeam() == eTarget) ... MEMORY_DECLARED_WAR ...`
   never sees A's members.
8. The declaration then establishes C/A contact, but no later code replays the missed victim memory.

Pre-1842 control:
- The original all-living-major-player loop did not require prior aggressor/victim contact before reaching the direct-victim `MEMORY_DECLARED_WAR` branch.
- Thus the victim correctly remembered that the newly encountered team had declared war on it.

Current consequence:
- `MEMORY_DECLARED_WAR` has current leader-memory XML values and real attitude/decay behavior.
- UWAI / WarUtility and UWAIAgent logic also read this memory family.
- Missing it on secondary/cascading declarations can therefore alter post-war attitude and strategic memory.

Scope:
- Distinct from F044:
  - F044 = third-party `MEMORY_DECLARED_WAR_ON_FRIEND` observer audience.
  - F046 = the DIRECT VICTIM's own `MEMORY_DECLARED_WAR`.
- Distinct from F037:
  - F037 = war-despite-tribute observer population.
- Distinct from F043:
  - F043 = all-player arrogant-demand reset narrowed by the same broad iterator.

Ancestry:
- Practical 1842 replaced the old broad per-player declaration-memory loop with `KNOWN_POTENTIAL_ENEMY_OF(getID())`.
- The outer filter now imposes a contact requirement before the declaration's own later `meet()` call can establish that contact.
- No descendant repair found so far.

Repair direction:
- Do not prefilter direct victim memory through an aggressor-contact iterator.
- The direct target/victim branch should explicitly process the target team's members regardless of prior contact when a valid declaration occurs.
- More generally, split AI_preDeclareWar's multiple distinct memory contracts into separately correct populations rather than driving all of them through one broad iterator.

POST-WIP4 PRACTICAL-1842 AUDIT PROGRESS
- `AI_isWaterAreaRelevant()` closes clean:
  - same-team / same-master-locus cities form the "our side" population via `NOT_A_RIVAL_OF`,
  - known outside-locus cities remain potential rivals regardless friendliness,
  - `AI_deduceCitySite()` is a deliberate anti-cheating refinement,
  - no lost legitimate current city population established.
- Large voluntary-vassal / surrender iterator rewrites were re-expanded literally:
  - `FREE_MAJOR_CIV` / enemy populations preserve the old intended free-team exceptions,
  - prospective-master enemy and "other war success" populations preserve the old exclusions,
  - threat loops preserve master/vassal cases.
- Worst-enemy side-effect loop narrowing to `OTHER_KNOWN_TO` closes clean:
  - rival-trade/grant values only exist meaningfully for contact-established third parties,
  - no stale unknown-team value population established.
- `AI_changeWarSuccess` major-civ restriction does not establish a lost current diplomatic-memory population for minor/barbarian teams.
- `AI_countWarPlans` / sneak-attack iterator rewrites remain contact-equivalent under reachable war-plan creation.
- `AI_doWar` practical-1842 target populations remain contact-equivalent apart from already-owned F033/F035 paths; dogpile-power no-cheat changes appear intentional.
- `AI_warDiplomacyCost` / `AI_startWarVal` future-enemy rewrites appear intentional refinements to vassal / Defensive Pact handling rather than regressions.
- Renewed practical-1842 survivor set now includes at least:
  F033, F036, F037, F038, F039, F043, F044, F045, F046.
- Practical 1842 is STILL OPEN until remaining hunks / same AI_preDeclareWar cascading semantics are fully dispositioned.

CURRENT FINDING SET AFTER WIP5
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP5
1. Continue `AI_preDeclareWar` / declaration-cascade audit:
   - check every remaining branch in the shared practical-1842 outer iterator for pre-contact or same-locus population loss,
   - check whether secondary vassal/Defensive Pact declarations create any additional distinct memory/state omission beyond F046.
2. Finish renewed practical-1842 hunk-by-hunk audit and only then re-mark that subpass exhausted.
3. Resume independent current whole-file closure:
   - final public sentinel/wrapper contracts,
   - arithmetic/divisor/default/assert/fixed-array pass,
   - relationship/cache transitions,
   - header/member/state invariants.
4. Run exact semantic collision search for F046 and renewed F037/F044 wording against current Album + reviewed Known Issues.
5. Reread F032-F046 one-by-one against current source/history:
   - current supported reachability,
   - exact ancestry,
   - consequence without overstatement,
   - surgical repair.
6. If another handoff occurs first:
   - freeze C010-WIP6.
7. Only when all remaining work is genuinely closed:
   - mark CvTeamAI.cpp COMPLETE
   - total becomes 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 SASGameRecordLog.cpp.

END WORKING CHECKPOINT C010-WIP5
================================================================================
WORKING CHECKPOINT C010-WIP6 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP5.
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots remain F032-F046 / provisional KI#353-KI#367.
- F047 is currently UNUSED; one vassal-war-plan storage/query candidate is OPEN and deliberately UNNUMBERED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects all post-WIP5 work and the exact open candidate before further proof.

POST-WIP5 PRACTICAL-1842 CLOSURE PROGRESS
- The four distinct contracts sharing practical-1842's broad `AI_preDeclareWar` outer iterator have been separated:
  - F037 = war-despite-tribute observer population.
  - F043 = all-player arrogant-demand reset.
  - F044 = third-party war-on-friend observer population.
  - F046 = direct victim's `MEMORY_DECLARED_WAR` on first-contact cascading declarations.
- Primary voluntary declarations normally require prior contact, so F046's first-contact omission is specifically relevant to cascading/secondary declarations such as Defensive Pact and vassal-alignment wars.
- Vassal-alignment first-contact cases are the same F046 root, not a new issue.
- Collision search for F046 against current Album / reviewed Known Issues found no same-root issue.

OTHER PRACTICAL-1842 HUNKS DISPOSITIONED AFTER WIP5
- `AI_countChosenWars()` population narrowing is technically different from old code but the function is unused in C++ and no current SAS Python consumer was found that gives the difference current gameplay/UI impact. API/history debt only.
- Global `AI_isSneakAttackPreparing(NO_TEAM)` / `AI_isSneakAttackReady(NO_TEAM)` scan narrowed to MAJOR_CIV:
  - live no-argument helpers are important,
  - but current legacy war selection only creates PREPARING_TOTAL / PREPARING_LIMITED plans against major civs,
  - minor/barbarian wars can have active/attacked plans but not the preparing state these helpers test,
  - no current lost preparing-plan signal established.
- `AI_changeWarSuccess` major-civ restriction:
  - shared-war success is a major-civ diplomacy/relationship mechanism,
  - minor/barbarian teams do not participate in the normal memory/attitude contracts,
  - no current diplomatic loss established.
- `AI_countEnemyCitiesByArea()` still comments that barbarian cities are included although its iterator now requires a known potential enemy.
  - Current game initialization makes non-major teams declare war on alive civs.
  - `declareWar()` establishes contact.
  - Therefore barbarian team contact exists before these area war-plan/city helpers matter.
  - Comment remains effectively true under current lifecycle.
- Area/enemy helper family generally closes on the same basis:
  - normal chosen/preparing plans are contact-gated,
  - actual barbarian/minor wars establish contact,
  - no hidden unmet-enemy population remains.

INDEPENDENT CURRENT-FILE CLOSURE PROGRESS AFTER WIP5
- Fresh trade-denial / swapped-TeamTypes argument pass:
  - tech trade, map trade, vassal trade, Open Borders, Defensive Pact and permanent alliance directions remain internally consistent.
  - own-vassal tech/map denial oddity remains a consequence of F045's otherwise-impossible stale own-vassal worst-enemy state, not a second trade root.
- Fresh divisor concern in `AI_getDiploDecay()` killed:
  - current XML grep appeared to have seven explicit `iAIMemoryRandPercent` tags for eight game speeds,
  - loader/default is 100 (`kElements.addInt(...,100)`), not zero,
  - missing explicit tag therefore cannot create division by zero.
- `DIPLOMACY_VALUE_REMAINDER` and similar config denominators checked against current XML remain positive; configuration robustness only.
- Current direct arrays/vectors, `nextIndex()` denominators, raw-team sentinel paths and enum/default scans remain clean beyond already-owned findings.
- Trade/worst-enemy small-helper scan has not produced another independent root.
- The whole-file audit is now sufficiently deep that remaining work is mainly the final inherited war-plan semantics + complete finding reread/collision pass.

OPEN UNNUMBERED CANDIDATE - possible F047
Title candidate:
- Legacy AI may store a chosen/preparing war plan on a VOLUNTARY VASSAL team while query/readiness APIs redirect the same target to its MASTER team.

Why it looks suspicious:
- Legacy `AI_doWar()` explicitly considers voluntary vassals as war targets.
- Target selection stores:
  `eBestTarget = eTarget` (the vassal team itself)
  and later calls:
  `AI_setWarPlan(eBestTarget, WARPLAN_PREPARING_TOTAL/LIMITED/DOGPILE...)`.
- `AI_isOkayVassalTarget(eTeam)` exists specifically to permit some voluntary-vassal targets while rejecting capitulated/inappropriate ones.
- There is no obvious target->master normalization at the selection call site.

But multiple query APIs redirect vassal targets to their master:
- `AI_isChosenWar(eIndex)` reads:
  `AI_getWarPlan(GET_TEAM(eIndex).getMasterTeam())`.
- `AI_isSneakAttackPreparing(eIndex)` likewise queries the target's master plan.
- `AI_mayAttack(eDefender)` gets defender master and requires an imminent plan against that master.
- `AI_updateWarPlanCounts()` intentionally ignores war plans whose target is currently a vassal.
- `AI_isAnyWarPlan()` and per-plan cache queries therefore do not count a vassal-stored plan.

Potential mismatch:
- Write key = vassal team.
- Several readiness/query/cache keys = master team.
- If no sibling/master plan is created elsewhere, legacy AI can select and store a vassal-target plan that later readiness/attack logic cannot observe through the normalized query APIs.

Reachability:
- Full UWAI returns before legacy AI_doWar selection.
- Legacy path remains reachable in supported configurations when UWAI is disabled / K-Mod-style or related background modes.
- Must still prove a complete supported behavioral failure before promotion.

Important reasons NOT to promote yet:
- `AI_doWar()`'s first live-plan loop reads raw `AI_getWarPlan(eLoopTeam)`, so the vassal-stored plan is not completely invisible.
- It may advance PREPARING -> active on the vassal team.
- Declaring war on the vassal can drag its master into war by game rules, so storing the raw plan on the vassal might be partially intentional.
- Need determine whether:
  1. a sibling plan is also created on the master,
  2. the vassal plan is later normalized,
  3. unit/city war-readiness consumers require the master-normalized APIs before actual declaration,
  4. current supported non-UWAI configuration actually reaches the bad combination.

Exact current source facts already established:
- `AI_doWar()` target selection:
  - iterates known potential enemy teams,
  - computes `eLoopMasterTeam = GET_TEAM(eTarget).getMasterTeam()`,
  - detects `bVassal`,
  - permits via `AI_isOkayVassalTarget(eTarget)`,
  - stores `eBestTarget = eTarget`,
  - calls `AI_setWarPlan(eBestTarget, ...)`.
- `AI_isOkayVassalTarget()`:
  - rejects capitulated target,
  - permits non-vassal directly,
  - for voluntary vassal evaluates area/adjacency relationship between vassal and master and can return true.
- Query redirects:
  - AI_isChosenWar(eIndex) -> target master
  - AI_isSneakAttackPreparing(eIndex) -> target master
  - AI_mayAttack(eDefender) -> defender master
- Cache:
  - AI_updateWarPlanCounts returns without counting vassal targets.
- Legacy `AI_doWar()` first plan-processing loop reads raw plans by actual team ID, so storage is still visible to that loop.

Exact proof tasks for possible F047
1. Trace every live caller of:
   - AI_isChosenWar
   - AI_isSneakAttackPreparing / Ready
   - AI_mayAttack
   - raw AI_getWarPlan
   in CvUnitAI / CvSelectionGroupAI / CvCityAI / CvTeamAI and related war execution.
2. Trace PREPARING_TOTAL/LIMITED stored on a vassal:
   - how/when it transitions,
   - what predicate starts actual attacks,
   - whether declaration ultimately targets vassal or master.
3. Search all `AI_setWarPlan` paths for automatic sibling/master plan creation.
4. Trace `CvTeam::declareWar` / vassal-war propagation to see whether plan reconciliation occurs.
5. Historical control:
   - compare K-Mod / pre-1842 / AdvCiv practical(s), especially `advc.104j`,
   - determine whether master redirects and vassal target selection were designed together or introduced separately.
6. Current configuration reachability:
   - prove supported legacy AI path can select a voluntary vassal while UWAI is disabled.
7. Collision search if confirmed.
8. Promote only if an actual current strategy/execution failure survives; otherwise record as intentional dual-key semantics or robustness debt.

CURRENT CONFIRMED FINDING SET AFTER WIP6
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 currently UNUSED pending proof above.

EXACT RECOVERY CURSOR AFTER C010-WIP6
1. Prove/kill the vassal-target war-plan storage/query candidate described above.
2. If confirmed:
   - collision search,
   - assign F047 / provisional KI#368,
   - verify ancestry and surgical repair.
3. Finish remaining practical-1842 hunk audit and only then mark that historical subpass exhausted.
4. Finish independent current whole-file closure.
5. Reread every confirmed F032-F046 (+ F047 if promoted) against:
   - current source,
   - Base AdvCiv 1.14,
   - historical controls,
   - Album,
   - reviewed Known Issues.
6. Ensure no open candidate remains.
7. If another handoff occurs first:
   - freeze C010-WIP7.
8. Only when genuinely complete:
   - mark CvTeamAI.cpp COMPLETE
   - total 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP6
================================================================================
WORKING CHECKPOINT C010-WIP7 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending C010-WIP6.
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed current roots now extend through F047 / provisional KI#368.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the fully proved legacy voluntary-vassal war-plan storage/query mismatch before the remaining file-closure pass.

F047 - provisional KI#368 - advc.104j master-normalized vassal war-plan queries but left legacy AI_doWar writing plans to the vassal team
Status: CONFIRMED CURRENT CONFIGURATION-REACHABLE WAR-PLANNING DEFECT.

Root:
- K-Mod's original representation was internally consistent:
  - war target selection could choose a voluntary vassal,
  - `AI_setWarPlan(vassal, ...)` stored the plan on that vassal team,
  - `AI_isChosenWar(vassal)` / `AI_isSneakAttackPreparing(vassal)` read the same vassal key.
- AdvCiv practical 1366 (`82ed992626...`, change `advc.104j`) changed the READ/query side to normalize vassal targets to their master:
  - `AI_isChosenWar(eIndex)` -> `AI_getWarPlan(GET_TEAM(eIndex).getMasterTeam())`
  - `AI_isSneakAttackPreparing(eIndex)` -> master-keyed plan
  - pathing / tactical readiness was likewise changed to use the target master.
- Practical 1366's own comment says:
  "Since a war plan against a master implies a war plan against its vassal, I don't think this function is relevant anymore."
  about `AI_isOkayVassalTarget`.
- Its commit log explicitly says:
  "Fixed issues that had prevented the AI from starting a war by invading a vassal. [advc.104j]"
- However, practical 1366 did NOT normalize the legacy `AI_doWar()` target-selection/write side.

Current legacy selection/write path:
- Legacy AI_doWar still iterates possible major targets and can select a VOLUNTARY VASSAL:
  - `eTarget = it->getID()`
  - `eLoopMasterTeam = GET_TEAM(eTarget).getMasterTeam()`
  - `bVassal = (eLoopMasterTeam != eTarget)`
  - if vassal, `AI_isOkayVassalTarget(eTarget)` can return true.
- It intentionally rejects capitulated vassals, but some voluntary vassals remain valid targets.
- If selected:
  - `eBestTarget = eTarget` remains the VASSAL team ID,
  - `AI_setWarPlan(eBestTarget, WARPLAN_PREPARING_TOTAL/LIMITED or DOGPILE...)`
    stores the plan on the VASSAL key.
- `AI_startWarVal(eTarget,...)` knows the target's master for coalition valuation but does not normalize the stored target.
- Search of current AI_setWarPlan paths found no automatic creation of a sibling plan on the target MASTER when legacy AI_doWar selects the vassal.
- AI_setWarPlan's own vassal propagation concerns OUR AI vassals inheriting our target; it does not normalize the ENEMY target to its master.

Why the plan is partly visible but tactically unusable:
- AI_doWar's first plan-maintenance loop reads raw:
  `AI_getWarPlan(eLoopTeam)`.
- Therefore a vassal-stored PREPARING plan can age and transition to WARPLAN_LIMITED / WARPLAN_TOTAL.
- But the APIs that permit/execute an undeclared attack against that target normalize to the target master:
  - `AI_isChosenWar(vassal)` -> master plan
  - `AI_isSneakAttackPreparing(vassal)` -> master plan
  - `AI_isSneakAttackReady(vassal)` -> master-keyed chosen/preparing tests
  - `AI_mayAttack(vassal)` -> defender master plan
  - `CvSelectionGroupAI::AI_isDeclareWar(vassal plot)` -> target master plan for limited-war semantics
  - GroupPathFinder `MOVE_DECLARE_WAR` handling -> AI_isSneakAttackReady(target)
  - `CvUnit::canMoveInto(..., bDeclareWar=true)` for AI requires AI_isSneakAttackReady(plot team)
  - `CvUnitAI::AI_considerDOW()` only declares when AI_isSneakAttackReady(plot team) is true.
- With only a vassal-keyed plan, all of those master-normalized readiness checks see NO_WARPLAN.
- The AI therefore cannot use the normal tactical border-crossing / path-DOW mechanism to execute the planned undeclared war.

Timeout consequence:
- After a PREPARING vassal-keyed plan matures to LIMITED/TOTAL, AI_doWar's raw plan loop still sees it.
- `AI_abandonWarPlanIfTimedOut()` checks whether any member has an enemy-target mission against the vassal team.
- Because master-keyed readiness prevents normal mission/path declaration setup, the plan can remain inactive.
- Once the state counter exceeds the normal abandon threshold, the vassal-keyed plan is cleared.
- Thus the legacy planner can select, prepare and then abandon a war against a voluntary vassal without the tactical layer ever recognizing it as an imminent war.

Current reachability:
- Full UWAI returns before the legacy target-selection half of AI_doWar.
- Legacy K-Mod/BBAI war planning remains a supported path when UWAI is disabled / legacy K-Mod-style configurations are used (the same configuration family relevant to F033).
- `canEventuallyDeclareWar(vassal)` deliberately calls `canChangeWarPeace(eTeam, true)`, so an outside team may declare war on a voluntary vassal; target reachability is not blocked by the core diplomacy API.
- AI_isOkayVassalTarget exists and remains called precisely to allow suitable voluntary-vassal targets.

Ancestry classification:
- K-Mod target storage/query was same-key and therefore did not have this particular split.
- AdvCiv practical 1366 / advc.104j introduced master normalization on the tactical/query side while leaving K-Mod's vassal-target selection/write side intact.
- This is best classified as an AdvCiv incomplete-fix / cross-contract regression surviving SAS.

Collision:
- Semantic searches for `AI_isOkayVassalTarget`, `advc.104j`, vassal war-plan/invasion/sneak-plan wording found no same-root current Album or reviewed Known-Issues entry.

Repair direction:
Preferred architecture:
1. Normalize the LEGACY target to the master before storing a chosen/preparing war plan:
   - if selection evaluates a voluntary vassal's geography/value, retain that as the tactical subtarget if useful,
   - but strategic war-plan key should be the master team, matching advc.104j's query/tactical contract.
2. Or centralize a canonical `warPlanTargetTeam(eTeam)` helper and use it consistently for BOTH writes and reads.
3. Reevaluate/remove `AI_isOkayVassalTarget()` from legacy strategic plan-key selection; practical 1366 already comments that the function became conceptually irrelevant once plans are master-keyed.
4. Preserve the intended ability to invade a vassal when that is the attractive front of a war against its master; do not simply ban vassal invasion.

CURRENT CONFIRMED FINDING SET AFTER WIP7
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP7
1. Finish renewed practical-1842 hunk audit; most remaining hunk populations have now closed clean, but do not stamp exhausted until mechanically complete.
2. Continue independent inherited/current CvTeamAI pass after F047:
   - oldest BtS/K-Mod control comparisons,
   - tiny helpers / trade denial / state setters,
   - raw plan-state/cached-state lifecycle,
   - remaining sentinel/divisor/default/assert/header invariants.
3. Deep semantic collision + final wording pass for F032-F047:
   - current supported reachability
   - exact ancestry/classification
   - consequence without overstatement
   - surgical repair direction
4. Recheck F047 specifically against the full legacy war execution lifecycle and any descendant changes before durable C011.
5. Confirm no candidate remains OPEN.
6. If another handoff occurs before completion:
   - freeze C010-WIP8 first.
7. Only when genuinely complete:
   - mark CvTeamAI.cpp COMPLETE
   - total 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 SASGameRecordLog.cpp.

END WORKING CHECKPOINT C010-WIP7
================================================================================
WORKING CHECKPOINT C010-WIP8 - RECONCILED TO CURRENT HEAD 6239
================================================================================

STATUS
- This cumulative recovery checkpoint is generated FROM the repository's reconciled
  `_1_AdvCiv-SAS/Docs/Source_Analysis/cpp_file_audit_album.txt`, not from the older standalone WIP7 file.
- Repository snapshot reconciled here:
  - Branch: main
  - HEAD: cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - Practical commit count: 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional current-file roots remain F032-F047 / provisional KI#353-KI#368.
- F048 is currently UNUSED.
- Do NOT promote this checkpoint to C011.

WHY THIS RECONCILIATION IS SAFE
- Current `CvGameCoreDLL/CvTeamAI.cpp` at practical 6239 is byte-for-byte identical to the original
  practical-6225 audit snapshot:
  SHA-256 = 0992277df3a76bcf98279cbdd49a901aad3591165e0c9ae2035dc278d9270382
- Therefore none of the file-010 findings F032-F047 has been silently changed by the intervening
  C009/C010 repair and documentation work.
- The current path-history index confirms the most recent source commit touching CvTeamAI.cpp is
  practical 6218, before the original practical-6225 file-010 audit baseline.
- Practical 6239 itself reconciled the repository Album through open C010-WIP7 and explicitly retained
  F045-F047 as structurally surviving but provisional pending durable C011 closure.

CURRENT REPOSITORY / REVIEW STATE
- README_Known_Issues.md currently promotes/fixes through KI#352 for the C010 CvPlot batch.
- KI#346-KI#352 have now been independently reviewed/fixed/documented:
  - KI#346 passable-area invalidation after Ice melt
  - KI#347 Python city-found-value override caching
  - KI#348 foreign-team city air capacity
  - KI#349 plot debug-string dangling pointer
  - KI#350 hidden-nationality nuke report leak
  - KI#351 runtime river-edit derived-state refresh
  - KI#352 Barbarian culture-decay array bound
- The repository Album intentionally retains F032-F047 only as provisional open-file findings.
- This preserves the intended architecture:
  reviewed/promoted/fixed issues -> README_Known_Issues.md
  still-open file-oriented investigation -> cpp_file_audit_album.txt / WIP checkpoint.

POST-WIP7 CLEAN-PROGRESS STATE PRESERVED HERE
After WIP7, before this practical-6239 reconciliation, the following closure work was completed without
establishing F048:

1. Practical-1842 hunk inventory / tail review
- Remaining `CvTeamAI.cpp` practical-1842 hunks were inventoried by function rather than only by suspicious region.
- Large tail families around relationship counters, team/personality averages, war-plan plumbing, religion,
  fair-trade helpers, AI_doWar, no-war rolls and tech-monopoly logic were largely resolving as population-equivalent
  or intentional.
- No additional practical-1842 survivor beyond F033/F036-F039/F043-F046 was promoted in that post-WIP7 slice.
- Renewed practical-1842 subpass is not yet stamped finally exhausted; explicit final disposition/collision reread remains.

2. Type / domain / enum / array / assert pass
- Fresh integer-vs-TeamTypes/PlayerTypes/enum comparison scan did not establish another incompatible-domain bug.
- F032 remains the unique confirmed vote-count/team-ID domain mismatch.
- Direct array/vector indexing remained guarded.
- Remaining FAssert/default branches examined were enum-complete or dormant under current supported data.
- No F048 from these mechanical passes.

3. TODO/FIXME disposition
- `AI_getEnemyPowerPercent()` shared Defensive Pact FIXME remains F040, not a second root.
- Barbarian AreaAI `// XXX does this ever happen?` branch is reachable heuristic logic, not a correctness defect.
- `AI_endWarVal` "should consider area power" is an explicit strategic-quality approximation.
- `AI_doWar` "make sure they share an area" is heuristic/design debt, not a proved invariant violation.
- No additional current correctness root from comment debt.

4. State-symmetry / lifecycle pass begun
- `AI_reset()` symmetrically clears this team's relationship arrays and reciprocal entries stored by other teams.
- at-war / at-peace / met / Open Borders / Defensive Pact / shared-war / war-success setter/reset invariants checked so far remain coherent.
- current read/write payload remains structurally paired.
- `AI_finalizeInit()` still does not force `m_bAnyWarPlan=false` before rebuilding per-plan counts, but no independent
  supported stale-true/no-plan lifecycle has been established; retain as robustness improvement only unless new evidence appears.
- This state-symmetry pass was the exact open whole-file closure area when WIP7 handoff occurred.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 currently UNUSED.

EXACT RECOVERY CURSOR AFTER RECONCILED C010-WIP8
1. Resume state-symmetry / relationship lifecycle audit:
   - cached plan counts / aggregate any-war-plan flag
   - relationship counters and reciprocal storage
   - worst-enemy eligibility / transition consistency
   - vassal / master / Defensive Pact / war-peace transitions
2. Finish explicit practical-1842 hunk dispositions and only then call that historical subpass exhausted.
3. Re-run the independent current whole-file tail after the new practical-6239 source reconciliation:
   - tiny helpers / trade denial / state setters
   - public sentinel/wrapper contracts
   - divisor/default/assert/fixed-array/header invariants
4. Deep final semantic collision + wording reread of F032-F047 against:
   - current practical-6239 source
   - Base AdvCiv 1.14
   - relevant K-Mod/BtS/Civ4CE controls
   - current repository Album
   - reviewed README_Known_Issues
5. Verify every finding for:
   - current supported reachability
   - exact ancestry classification
   - consequence without overstatement
   - surgical repair direction
6. Confirm no candidate remains open and no earlier WIP statement remains superseded/incorrect.
7. If another handoff occurs before file closure:
   - freeze C010-WIP9 FROM THIS RECONCILED WIP8/current repo lineage.
8. Only when genuinely complete:
   - mark CvTeamAI.cpp COMPLETE
   - total becomes 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP8
================================================================================
WORKING CHECKPOINT C010-WIP9 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP8.
- Authoritative repository snapshot remains:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots now extend through F048 / provisional KI#369.
- F049 currently UNUSED.
- Do NOT promote this checkpoint to C011.

F048 - provisional KI#369 - Permanent Alliance merge discards absorbed team's SharedWarSuccess toward third parties
Status: CONFIRMED CURRENT CROSS-FILE DIPLOMACY-STATE MERGE DEFECT, inherited AdvCiv.

Feature semantics:
- `advc.130m` introduced `SharedWarSuccess` as the actual-contribution component of the lasting shared-war relationship bonus.
- Current `CvPlayerAI::AI_getShareWarAttitude` consumes:
  - `AI_getShareWarCounter(eTeam)` for duration/cap, and
  - `AI_getSharedWarSuccess(eTeam)` for the other side's actual military contribution.
- The value therefore has current attitude and diplomacy consequences.

Permanent Alliance state merge:
- `CvTeam::addTeam(eTeam)` permanently absorbs `eTeam` into `getID()`.
- `CvTeam::shareCounters(eTeam)` merges the absorbed team's OWN relationship state toward every third-party team.
- It explicitly merges, among others:
  - WarWeariness
  - StolenVisibilityTimer
  - AtWarCounter
  - AtPeaceCounter
  - TurnsAtPeace
  - HasMetCounter
  - OpenBordersCounter
  - DefensivePactCounter
  - ShareWarCounter
  - WarSuccess
  - EnemyPeacetimeTradeValue
  - EnemyPeacetimeGrantValue
- For these own-side counters, the established `shareCounters` policy is generally to retain the larger/stronger of the two team values.
- `SharedWarSuccess` is absent from this merge.

Opposite-direction evidence makes the omission particularly clear:
- Later in the same `CvTeam::addTeam`, every third-party team `kOther` has its state TOWARD the merged team recomputed.
- That code explicitly includes:
  `kOther.AI_setSharedWarSuccess(getID(),
      (iOriginalTeamSize * kOther.AI_getSharedWarSuccess(getID()) +
       iSecondTeamSize * kOther.AI_getSharedWarSuccess(eTeam)) /
       getNumMembers());`
- Thus the merge handles SharedWarSuccess in the third-party -> merged-team direction,
  while failing to merge the merged-team -> third-party direction.
- After A absorbs B:
  - outsider C's memory of A+B's contribution is combined,
  - merged A's memory of C's contribution retains only old A's value,
  - B's accumulated history of C's shared-war contribution is silently discarded.

Historical evidence:
- AdvCiv `advc.130m` feature lineage introduced SharedWarSuccess and explicitly added the third-party-side Permanent Alliance merge.
- No corresponding addition to `shareCounters` was made.
- This is consistent with a field added after the older K-Mod counter-merging routine and omitted from one side of the merge contract.
- No descendant repair found.

Current reachability:
- `GAMEOPTION_PERMANENT_ALLIANCES` remains a visible supported game option.
- Current `CIV4TechInfos.xml` contains a live tech with `<bPermanentAllianceTrading>1`.
- Therefore `CvTeam::addTeam` / Permanent Alliance state merging remains supported current gameplay, not dormant compatibility code.

Current consequence:
- The absorbed alliance partner's accumulated shared-war contribution history toward third parties is lost at merge.
- `AI_getShareWarAttitude()` thereafter uses the surviving merged-team SharedWarSuccess, so the resulting relationship bonus can be lower than the state carried by the absorbed team.
- The relationship becomes directionally asymmetric despite explicit reverse-direction merging.

Collision:
- Current reviewed Known Issues / Album searches found no existing same-root Permanent Alliance + SharedWarSuccess merge issue.

Repair direction:
- Add `AI_getSharedWarSuccess` / `AI_setSharedWarSuccess` to `CvTeam::shareCounters`.
- The conservative policy should follow that function's established OWN-side merge semantics (normally retain the stronger/max state), while preserving the existing team-size-weighted reverse direction.
- The correctness requirement is that the absorbed team's SharedWarSuccess not be silently discarded; exact merge weighting can be reviewed alongside the surrounding Permanent Alliance policy.

POST-WIP8 STATE-SYMMETRY PROGRESS
- `CvTeamAI::AI_reset` remains symmetric:
  - resets all local team-indexed counters and war plans,
  - clears reciprocal entries stored by every other team,
  - includes SharedWarSuccess,
  - resets cached war-plan counts and aggregate `m_bAnyWarPlan`.
- `CvTeam::setVassal` transition review still leaves F042 as the specific war-plan cache ordering defect; no second counter-array transition omission established.
- `CvTeam::addTeam` / Permanent Alliance lifecycle review produced F048 above.
- `AI_updateWorstEnemy` remains F045 for incumbent eligibility; post-vassal attitude refresh itself is present.
- Open Borders, Defensive Pact, shared-war, war/peace and ordinary counter setter lifecycles remain coherent apart from F041's ineffective accumulated counter ceiling.
- `AI_finalizeInit` missing an initial `m_bAnyWarPlan=false` remains robustness debt only; no independent current stale-true/no-plan lifecycle established.

PRACTICAL-1842 CONTINUED DISPOSITION
- Hunk 46 / `AI_warDiplomacyCost()`:
  - outer `KNOWN_POTENTIAL_ENEMY_OF(aggressor)` does narrow out observers in the aggressor's same-master locus compared with pre-1842 code.
  - The old code already required aggressor+victim contact with the observer, so no new no-contact issue exists here.
  - `AI_disapprovesOfDoW` can semantically disapprove from a same-aggressor-locus observer, so the population difference is real.
  - However, `AI_warDiplomacyCost` is a prospective strategic heuristic, and the source does not yet establish that counting a currently subordinate same-locus observer is a required correctness contract rather than an intentional/refactor simplification.
  - HOLD BELOW THRESHOLD; do not promote as F049 without stronger design/behavior evidence.
- `AI_isWaterAreaRelevant()` rewrite remains clean so far:
  - own/master-locus cities intentionally grouped under NOT_A_RIVAL_OF,
  - outside known potential enemies counted with anti-cheating city-site deduction,
  - no lost current legitimate population established.
- Other already-dispositioned practical-1842 iterator families remain as recorded in WIP8.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP9
1. Continue explicit practical-1842 hunk disposition from the current generated diff;
   focus on remaining relation-predicate population changes (war diplomacy/value, surrender,
   declare-war trade, enemy-area/war-plan helpers, counter/final tail).
2. Continue independent state-symmetry/lifecycle audit:
   - Permanent Alliance remainder after F048
   - war-plan cache and plan-state transitions
   - worst-enemy transitions
   - reciprocal vs directional relationship storage
3. Finish whole-current-file tiny-helper/sentinel/divisor/default/assert/fixed-array/header pass.
4. Deep final semantic collision + wording reread F032-F048 against current practical-6239 source,
   Base AdvCiv 1.14 and historical controls.
5. Ensure every finding has current supported reachability, exact ancestry, bounded consequence and
   surgical repair wording.
6. If another handoff occurs before closure, freeze C010-WIP10.
7. Only when genuinely exhausted:
   - mark CvTeamAI.cpp COMPLETE
   - total 10 / 169
   - freeze durable C011
   - byte-validate reconstruction
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP9
================================================================================
WORKING CHECKPOINT C010-WIP10 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP9.
- Authoritative repository snapshot remains:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F048 / provisional KI#353-KI#369.
- F049 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the clean post-WIP9 closure progress and one important F047 wording correction before further investigation.

POST-WIP9 RECONCILIATION / CLEAN CLOSURE PROGRESS

1. Current source synchronization remains exact
- Current practical-6239 `CvTeamAI.cpp` is still byte-for-byte identical to the practical-6225 audit baseline.
- Therefore no F032-F048 source finding was silently altered by the intervening reviewed C009/C010 implementation work.
- The repository Album / Known-Issues architecture remains synchronized:
  - reviewed/promoted/fixed C010 CvPlot issues through KI#352 live in README_Known_Issues.md,
  - open CvTeamAI candidates remain provisional in the Album.

2. Permanent Alliance completeness after F048 - no second current root
- Continued `CvTeam::addTeam` / `CvTeam::shareCounters` review after F048.
- `StrengthMemory` is not merged, but K-Mod/AdvCiv history treats it as tactical/fog-of-war estimation state rather than a durable diplomatic relationship counter with an explicit inheritance contract.
- Merging it could be an AI enhancement, but no source-backed correctness requirement was established.
- The one-turn / transient nuke-target-memory vector is even weaker as a merge obligation.
- UWAI has its own team-merge integration hook.
- Lonely-state and other derived team state are recomputed / updated rather than simply copied.
- No second F048-like Permanent Alliance state-loss root established.

3. Practical-1842 continued hunk disposition - mostly clean
- `AI_isWaterAreaRelevant` continues to close clean:
  - own/same-master-locus cities grouped intentionally,
  - outside known potential enemies considered with `AI_deduceCitySite` anti-cheating logic.
- `AI_postDeclareWar`, peace/attitude refresh, surrender/vassal populations, war-value populations and recent SAS tech-locus work continue to resolve as equivalent or intentional after predicate expansion.
- The possible same-aggressor-locus omission in `AI_warDiplomacyCost` remains BELOW THRESHOLD:
  - population difference exists,
  - but the function is a prospective heuristic cost estimator,
  - no clear source contract yet requires pricing the displeasure of a currently subordinate same-locus observer.
- Do not promote this as F049 without stronger behavioral/design evidence.

4. F047 important correction - DO NOT overstate practical 1842
- A transient post-WIP9 thought suggested practical 1842 might further hide voluntary-vassal plans from legacy `AI_doWar()` maintenance by excluding the foreign vassal from `KNOWN_POTENTIAL_ENEMY_OF(ourTeam)`.
- That interpretation is WRONG.
- `KNOWN_POTENTIAL_ENEMY_OF(ourTeam)` excludes teams in OUR own master locus.
- A FOREIGN voluntary vassal belonging to another master remains an outside-locus potential enemy and can still be iterated.
- Therefore practical 1842 should NOT be cited as aggravating F047 by making the raw vassal-keyed plan invisible to AI_doWar's maintenance loop.
- The correct F047 root remains exactly the original WIP7 analysis:
  - legacy selection/write can store chosen/preparing plan on the voluntary VASSAL key,
  - advc.104j query/tactical APIs normalize that target to the MASTER key,
  - raw planner maintenance can still see/age the vassal-keyed plan,
  - tactical readiness/path/DOW consumers looking under the master key can fail to recognize it.
- Preserve this corrected wording in any durable C011 entry.

5. Fresh type/domain/mechanical pass - no F049
- Explicit hunk/function inventory of the remaining practical-1842 tail continued.
- Relationship counters, team/personality averages, war-plan plumbing, religion/fair-trade helpers,
  `AI_doWar`, no-war rolls and tech-monopoly logic largely resolved as population-equivalent or intentional.
- Fresh integer-vs-TeamTypes/PlayerTypes/enum comparison scan found no second incompatible-domain bug:
  F032 remains the unique confirmed vote-count/team-ID comparison.
- Direct array/vector indexing remained guarded.
- Remaining FAssert/default branches examined were enum-complete or dormant under current supported data.
- No new sentinel/divisor/default/assert/fixed-array root.

6. TODO/FIXME disposition - no new root
- `AI_getEnemyPowerPercent` shared Defensive Pact FIXME remains F040 itself.
- Barbarian AreaAI `// XXX does this ever happen?` branch is reachable heuristic logic.
- `AI_endWarVal` area-power comment is an explicit strategic-quality approximation.
- `AI_doWar` "make sure they share an area" comment is heuristic/design debt, not a proved invariant violation.
- No correctness root created from comment debt alone.

7. State-symmetry pass resumed - clean so far beyond owned findings
- `AI_reset()` symmetrically clears local and reciprocal relationship arrays.
- at-war / at-peace / has-met / Open Borders / Defensive Pact / shared-war / war-success setter/reset invariants remain coherent.
- Current read/write payload remains structurally paired.
- `AI_finalizeInit()` still does not force `m_bAnyWarPlan=false` before rebuilding per-plan counts.
  No independent supported stale-true/no-plan lifecycle has been proved; keep as robustness improvement only.
- Current open closure area remains state-symmetry / lifecycle plus final individual finding reread/collision.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP10
1. Resume state-symmetry / lifecycle pass:
   - cached plan counts and aggregate `m_bAnyWarPlan`,
   - relationship counters and reciprocal storage,
   - worst-enemy transition consistency,
   - vassal/master/Defensive-Pact/war-peace transitions,
   - Permanent Alliance remainder already mostly closed after F048.
2. Finish explicit practical-1842 hunk dispositions; do not stamp exhausted until mechanically complete.
3. Finish whole-current-file tiny-helper / sentinel / divisor / default / assert / fixed-array / header pass.
4. Deep final semantic collision + wording reread of F032-F048 against:
   - practical-6239 current source,
   - Base AdvCiv 1.14,
   - relevant K-Mod/BtS/Civ4CE controls,
   - repository Album,
   - reviewed README_Known_Issues.
5. Recheck F047 using the CORRECTED statement above; do not attribute foreign-vassal exclusion to practical 1842.
6. Verify every finding has:
   - current supported reachability,
   - exact ancestry classification,
   - bounded consequence,
   - surgical repair direction.
7. Confirm no candidate remains open and no older WIP statement remains superseded or overbroad.
8. If another handoff occurs before closure:
   - freeze C010-WIP11.
9. Only when genuinely exhausted:
   - mark CvTeamAI.cpp COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP10
================================================================================
WORKING CHECKPOINT C010-WIP11 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP10.
- Authoritative repository snapshot:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F048 / provisional KI#353-KI#369.
- F049 remains UNUSED at this exact checkpoint.
- A strong Permanent-Alliance/Open-Borders counter candidate is OPEN and deliberately UNNUMBERED pending final proof.
- Do NOT promote this checkpoint to C011.

POST-WIP10 CLEAN CLOSURE PROGRESS
1. Permanent Alliance completeness after F048
- Continued checking state merged/recomputed by `CvTeam::addTeam` and `shareCounters`.
- F048 remains the unique confirmed SharedWarSuccess own-side omission.
- `StrengthMemory` is unmerged but is tactical/fog-of-war estimation state rather than a durable diplomatic-history field with a source-backed merge contract.
- Transient nuke-target memory is even less suitable as a required inherited relationship field.
- UWAI has its own team-merge hook.
- Lonely/derived state is recomputed.
- No second confirmed root from those fields.

2. Practical-1842 / type-domain / comment-debt closure
- Remaining iterator/hunk families continued to resolve as equivalent, documented, unused or heuristic.
- F032 remains the unique confirmed incompatible vote-count/team-ID domain comparison.
- Direct array/vector indexing and remaining enum/default/assert checks remained guarded/dormant under current data.
- Existing FIXME/TODO comments did not produce another current correctness root:
  - Defensive-Pact double-count FIXME = F040.
  - Area-power / share-area comments = strategic heuristic debt.
- The possible same-aggressor-locus population narrowing in `AI_warDiplomacyCost` remains below threshold because no source contract proves that a currently subordinate observer's displeasure must be included in this prospective heuristic.

3. State-symmetry refinement of F045
- F045 is broader than only "master retains voluntary vassal as worst enemy":
  - pre-1842 `AI_enmityValue` explicitly excluded `isVassal(eEnemy)` (our master),
  - 1842 removed that local check under the assumption that candidate iteration enforces locus eligibility,
  - incumbent worst enemy is nevertheless evaluated outside the candidate iterator.
- Therefore repair should centralize incumbent/replacement eligibility for the whole same-master locus rather than patch only one master->vassal trade symptom.

OPEN UNNUMBERED CANDIDATE - possible F049
Candidate title:
- Permanent Alliance merge can discard the OUTSIDER'S Open Borders relationship counter toward the absorbed team.

Current source structure:
- `CvTeam::addTeam(eTeam)` permanently absorbs team B (`eTeam`) into surviving team A (`getID()`).
- Before the final counter merge, relationship propagation can create A<->C Open Borders when B already had Open Borders with third-party C.
- `CvTeam::setOpenBorders` resets the counter for the relationship endpoint whose status changes:
  `AI().AI_setOpenBordersCounter(eIndex, 0)`.
- Thus when A did not previously have Open Borders with C but B did:
  - creation of A->C Open Borders resets new A->C counter to 0,
  - creation of C->A Open Borders resets new C->A counter to 0,
  - old B->C and C->B counters remain attached to B until merge cleanup.

Own-side merge:
- `CvTeam::shareCounters(B)` explicitly merges B->C into surviving A->C using the routine's established stronger/max counter semantics.
- It includes `AI_getOpenBordersCounter` / `AI_setOpenBordersCounter`.
- Therefore B's own Open Borders history toward C is preserved on the surviving team's side.

Reverse third-party merge:
- Later in `CvTeam::addTeam`, each outsider `kOther` has many values TOWARD the merged team combined:
  - AtWarCounter
  - AtPeaceCounter
  - HasMetCounter
  - WarWeariness
  - DefensivePactCounter
  - ShareWarCounter
  - WarSuccess
  - SharedWarSuccess
  - espionage / peace / enemy-trade/grant state and related values.
- This reverse-direction block does NOT merge `kOther.AI_getOpenBordersCounter(...)`.
- Therefore C->A remains the newly reset 0 and old C->B relationship history is discarded.

Concrete supported sequence under investigation:
1. B and C have long-standing Open Borders, with a positive C->B OpenBordersCounter.
2. A and C do not have Open Borders.
3. A permanently allies with / absorbs B.
4. The merged alliance inherits B-C Open Borders.
5. A->C can preserve B's old counter through `shareCounters(B)`.
6. C->A is newly reset to 0 and no reverse merge restores old C->B.
7. C therefore loses accumulated Open Borders relationship history toward the absorbed alliance partner.

Current behavioral consumer to finish proving:
- `CvPlayerAI::AI_getOpenBordersAttitude` reads the team's OpenBordersCounter toward the other team, divides by the leader's OpenBordersAttitudeDivisor and clamps to the change limit.
- Current leader XML is believed to have nonzero Open Borders attitude divisors/change limits.
- Exact consumer/XML line proof still required before promotion.

Ancestry evidence already found:
- K-Mod's initial `CvTeam::shareCounters` includes OWN-side OpenBordersCounter merging.
- K-Mod's initial reverse `CvTeam::addTeam` third-party merge includes several relationship counters but omits OpenBordersCounter.
- Therefore this likely predates AdvCiv as a K-Mod Permanent-Alliance merge omission rather than a new SAS defect.
- Need check later K-Mod/AdvCiv team-merge commits for deliberate semantic change or repair.

Why this is probably distinct from existing roots:
- F041 = counter ceiling/growth helper contract.
- This candidate = Permanent Alliance transition discarding directional relationship history.
- F048 = SharedWarSuccess omitted on merged team's own side.
- Candidate concerns the opposite-direction Open Borders history and appears to have different ancestry.

Proof tasks before F049 promotion:
1. Confirm exact current `AI_getOpenBordersAttitude` use and current leader-data reachability.
2. Confirm exact `setOpenBorders` counter-reset ordering in the Permanent Alliance sequence.
3. Inspect later K-Mod / AdvCiv team-merge history for intended omission/repair.
4. Search Album + reviewed Known Issues for same-root Permanent Alliance/OpenBordersCounter issue.
5. Determine exact repair semantics:
   - adding a late reverse merge may still use newly reset A-side data,
   - preserving/cache old C->A/C->B values before relationship propagation may be required for exact member-weighted semantics.
6. Promote only if all checks survive.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 UNUSED pending candidate proof.

EXACT RECOVERY CURSOR AFTER C010-WIP11
1. Prove/kill Permanent Alliance reverse OpenBordersCounter candidate above.
2. If confirmed:
   - collision search,
   - assign F049 / provisional KI#370,
   - lock ancestry and conservative repair wording.
3. Finish state-symmetry / relationship lifecycle audit after the Permanent Alliance family.
4. Finish explicit practical-1842 hunk disposition.
5. Perform final finding-by-finding reread/collision F032-F048 (+F049 if promoted).
6. Confirm no candidate remains open.
7. If handoff occurs before closure, freeze C010-WIP12.
8. Only when genuinely complete:
   - mark CvTeamAI.cpp COMPLETE,
   - total 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP11
================================================================================
WORKING CHECKPOINT C010-WIP12 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP11.
- Authoritative repository snapshot:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots now extend through F049 / provisional KI#370.
- F050 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the fully proved Permanent-Alliance/OpenBordersCounter root and the post-WIP11 state/practical-1842 closure progress.

F049 - provisional KI#370 - Permanent Alliance reverse merge omits outsider OpenBordersCounter toward absorbed team
Status: CONFIRMED CURRENT K-MOD PERMANENT-ALLIANCE MERGE DEFECT.

Current relationship semantics:
- `CvPlayerAI::AI_getOpenBordersAttitude(ePlayer)` reads the directional TEAM counter:
  `GET_TEAM(getTeam()).AI_getOpenBordersCounter(TEAMID(ePlayer))`
  divided by the leader's OpenBordersAttitudeDivisor and clamped to the OpenBordersAttitudeChangeLimit.
- Current ordinary leader defaults use divisor 25 and change limit +2.
- Therefore OpenBordersCounter is active directional diplomatic relationship state, not diagnostic bookkeeping.

Permanent Alliance transition:
- `CvTeam::addTeam(eTeam)` permanently absorbs team B (`eTeam`) into surviving team A (`getID()`).
- During relationship propagation, if B has Open Borders with outsider C while A does not, the merge establishes:
  - A -> C Open Borders
  - C -> A Open Borders.
- `CvTeam::setOpenBorders(eIndex, true)` resets the directional OpenBordersCounter when the status changes.
- Thus the newly created A/C directions begin from zero while old B/C directional counters still exist on B until the later merge cleanup.

Own-side merge is present:
- `CvTeam::shareCounters(B)` explicitly includes:
  `AI_getOpenBordersCounter` / `AI_setOpenBordersCounter`.
- Consequently B -> C's old Open Borders history is preserved into surviving A -> C using the routine's established stronger/max own-side merge policy.

Reverse third-party merge is missing OpenBordersCounter:
- Later in `CvTeam::addTeam`, every outsider `kOther` has its relationship state TOWARD the merged team combined.
- This block merges:
  - AtWarCounter
  - AtPeaceCounter
  - HasMetCounter
  - WarWeariness
  - DefensivePactCounter
  - ShareWarCounter
  - WarSuccess
  - SharedWarSuccess
  - TurnsAtPeace
  - espionage and enemy peacetime trade/grant state
  - other neighboring relationship values.
- It does NOT merge `kOther.AI_getOpenBordersCounter(...)`.

Concrete supported consequence:
1. B and C have had Open Borders for 50+ turns; C -> B OpenBordersCounter is therefore positive and can contribute +1/+2 attitude.
2. A and C either:
   a. do not currently have Open Borders, or
   b. have a different, shorter Open Borders history.
3. A absorbs B through Permanent Alliance.
4. The new merged alliance inherits B-C Open Borders.
5. B -> C history is merged into A -> C by `shareCounters`.
6. C -> B history is never combined into C -> A.
   - If A lacked Open Borders, C -> A remains the newly reset zero.
   - If A already had Open Borders, only A's prior C -> A history survives instead of the team-size-weighted A/B history.
7. C can therefore abruptly lose part or all of its positive Open Borders attitude toward the newly merged alliance.

Historical evidence / ancestry:
- K-Mod practical 941 (`94d44a04a0...`, "Made some changes to how data is merged when two teams merge") deliberately reworked Permanent-Alliance state combining.
- It retained OpenBordersCounter in the absorbed-team -> outsider `shareCounters` merge.
- Its reverse outsider -> merged-team averaging block combines neighboring relationship counters but omits OpenBordersCounter.
- K-Mod practicals 943/944 and later AdvCiv team-merge descendants did not add the missing reverse merge.
- Best classification: inherited K-Mod directional Permanent-Alliance merge omission surviving AdvCiv/SAS.

Why this is distinct:
- F041 = relationship-counter growth/ceiling helper contract.
- F048 = SharedWarSuccess omitted on merged team's OWN side while reverse side is present.
- F049 = OpenBordersCounter omitted on the OUTSIDER -> merged-team side while own side is present.
- These are distinct state fields, directions and ancestry contracts.

Field-symmetry evidence:
- Comparing durable directional relationship fields confirms the Permanent Alliance merge is generally deliberate rather than broadly incomplete.
- `shareCounters` own-side relationship fields and reverse `addTeam` fields correspond closely.
- OpenBordersCounter is the conspicuous reverse-side omission.
- SharedWarSuccess is the converse own-side omission already owned by F048.
- Other unmerged state is transient/derived/tactical (e.g. StrengthMemory, recent nuke target memory) or has separate recomputation/merge machinery.

Collision:
- Current repository Album and reviewed README_Known_Issues contain no same-root Permanent Alliance + OpenBordersCounter issue.

Repair direction:
- Add reverse team-size-weighted OpenBordersCounter merging in `CvTeam::addTeam`, alongside HasMet/DefensivePact/ShareWar and the other directional relationship counters:
  conceptually:
  `kOther.AI_setOpenBordersCounter(getID(),
      (iOriginalTeamSize * kOther.AI_getOpenBordersCounter(getID()) +
       iSecondTeamSize * kOther.AI_getOpenBordersCounter(eTeam)) /
      getNumMembers());`
- No extra pre-reset cache appears required:
  - if A did not already have OB with C, A's prior directional counter was already zero by ordinary status-reset semantics;
  - if A already had OB, `setOpenBorders(true)` is a no-op and its old counter remains available;
  - B's old counter remains accessible until the merge block.
- Exact arithmetic should follow the existing reverse-block team-size-weighted policy.

POST-WIP11 STATE / PRACTICAL-1842 CLOSURE PROGRESS

1. State-field symmetry inventory
- Aside from already-owned:
  - F042 war-plan-count lifecycle,
  - F045 worst-enemy incumbent eligibility,
  - F048 SharedWarSuccess own-side merge,
  - F049 OpenBordersCounter reverse-side merge,
  the remaining CvTeamAI state examined has a symmetric transition/merge path or is intentionally transient/derived.
- WarPlan / WarPlanStateCounter are deliberately cleared/reconciled on team merge.
- `m_bAnyWarPlan` / cached per-plan counts are covered by F042; load-time reset wart remains robustness only.
- worst enemy is recomputed but F045's incumbent eligibility is the surviving defect.
- lonely state is derived/recomputed.
- StrengthMemory is tactical/fog-of-war estimation state without a durable relationship inheritance contract.
- recent nuke-target vector is transient.
- UWAI has an explicit team-merge hook.

2. Practical-1842 iterator inventory
- Every introduced iterator/predicate in the high-risk CvTeamAI regions has been mechanically enumerated.
- Remaining area/enemy, peace, surrender, vassal, declare-war trade, forgiveness, counter and tail substitutions are resolving as:
  - old-population equivalent,
  - intentional no-cheating / master-locus semantics,
  - unused helper changes,
  - or heuristic changes below correctness threshold.
- `AI_warDiplomacyCost` still has a real same-aggressor-locus population difference, but remains BELOW THRESHOLD:
  - pre-1842 code already required observer to know both aggressor and victim,
  - unlike actual-memory F044 there is no new no-contact semantic loss,
  - this is a prospective heuristic cost estimator,
  - no hard source contract proves subordinate same-locus observer displeasure must be priced.
- Do not promote without stronger evidence.

3. Adversarial reread of practical-1842 memory roots
- `AI_preDeclareWar` practical 1842 forced four different original contracts through one outer:
  `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`.
- Revalidation confirms these remain distinct current roots:
  - F037: victim-known "war despite tribute" observer reputation population.
  - F043: all-major-player arrogant-demand reset.
  - F044: helper-defined third-party war-on-friend observer population.
  - F046: direct victim `MEMORY_DECLARED_WAR` on first-contact cascading declarations.
- Current attitude/UWAI consumers still use these memory families; none is dead historical state.

4. F045 adversarial reread
- `CvTeam::setVassal` DOES trigger broad attitude / worst-enemy refresh after relationship transition.
- Therefore F045 is not a missing refresh.
- Root is specifically incumbent/replacement eligibility divergence inside `AI_updateWorstEnemy`:
  current incumbent is evaluated directly while replacements use same-master-locus-excluding iterator.
- Repair should centralize eligibility rather than add another transition refresh.

5. F047 corrected wording remains mandatory
- Do NOT claim practical 1842 hides foreign voluntary-vassal plans from raw AI_doWar maintenance.
- A foreign voluntary vassal remains outside OUR master locus and is still a known potential enemy.
- F047 remains the advc.104j / practical-1366 read-vs-write key split:
  legacy planner can write vassal-keyed plan while tactical/read APIs canonicalize to master key.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP12
1. Finish practical-1842 final explicit hunk disposition and decide whether that historical subpass can finally be marked exhausted.
2. Finish final tiny-helper / sentinel / divisor / default / assert / fixed-array / header pass.
3. Deep semantic collision + adversarial reread F032-F049 one by one against:
   - practical-6239 current source,
   - Base AdvCiv 1.14,
   - relevant K-Mod/BtS/Civ4CE controls,
   - repository Album,
   - reviewed README_Known_Issues.
4. Specifically verify no WIP wording remains superseded:
   - F041 no Open-Borders post-cancellation decay claim,
   - F047 no practical-1842 foreign-vassal exclusion claim,
   - WIP2/WIP3 "1842 exhausted" statements superseded by later findings.
5. Confirm no open candidate remains and no merely plausible heuristic is promoted.
6. If another handoff occurs before closure:
   - freeze C010-WIP13.
7. Only when genuinely complete:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP12
================================================================================
WORKING CHECKPOINT C010-WIP13 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP12.
- Authoritative repository snapshot:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F049 / provisional KI#353-KI#370.
- F050 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the post-WIP12 adversarial certification work before continuing.

POST-WIP12 ADVERSARIAL CERTIFICATION PROGRESS

1. F046 re-proved against exact current declaration ordering
Current `CvTeam::declareWar` ordering is:
- `AI_preDeclareWar(...)`
- set both teams at war
- establish first contact with `meet(...)`
- post-declaration processing
- trigger Defensive Pact / vassal cascading wars.

Cascading declarations still enter the normal `declareWar()` path.
Therefore a Defensive-Pact or vassal-alignment team that has never met the eventual victim can enter
`AI_preDeclareWar()` BEFORE first contact is created.

This re-proves F046:
- practical 1842's outer `KNOWN_POTENTIAL_ENEMY_OF(getID())` requires pre-existing contact,
- so the direct victim can be filtered out during a first-contact cascading declaration,
- the later `meet()` call does not replay the omitted `MEMORY_DECLARED_WAR`.
No later restoration path was found.

2. F047 re-proved through tactical DOW execution
Legacy `AI_doWar()` can still write a chosen/preparing plan on a voluntary VASSAL team ID.
The following current query/execution APIs normalize that target to its MASTER:
- `AI_isChosenWar(vassal)`
- `AI_isSneakAttackPreparing(vassal)`
- `AI_isSneakAttackReady(vassal)`
- `AI_mayAttack(vassal)`
- AI group/path `MOVE_DECLARE_WAR` readiness
- `CvUnit::canMoveInto(..., bDeclareWar=true)` AI readiness
- `CvUnitAI::AI_considerDOW()` readiness.

Direct AI declaration search found no general strategic bypass that repairs the mismatch.
A separate opportunistic "poach undefended units" declaration path exists, but it is independent no-plan behavior
and does not make the vassal-keyed strategic preparation visible to the master-normalized tactical layer.

Important wording correction retained:
- practical 1842 does NOT exclude a FOREIGN voluntary vassal from raw AI_doWar maintenance merely because it is a vassal.
- F047 remains the advc.104j / practical-1366 read-vs-write key split, not an 1842 foreign-vassal exclusion.

3. F048/F049 Permanent Alliance pair re-proved as conspicuous directional omissions
Exact source reconstruction confirms:
- `shareCounters()` deliberately contains OpenBordersCounter on absorbed-team -> outsider direction.
- reverse outsider -> merged-team block omits OpenBordersCounter = F049.
- reverse block explicitly contains SharedWarSuccess.
- `shareCounters()` omits SharedWarSuccess = F048.
Neighboring durable directional relationship state is substantially merged/recomputed, making these two omissions
stand out rather than reflecting an intentionally lossy merge design.

4. F042 re-proved against exact `setVassal` ordering
- `setVassal(..., false, ...)` attempts to re-add existing plans against the newly freed target while the target's
  `m_eMaster` / vassal status is still old.
- `AI_updateWarPlanCounts()` explicitly returns for current vassal targets.
- The intended restoration is therefore blocked by stale transition state exactly as F042 claims.

5. F045 re-proved as incumbent/replacement eligibility divergence
- `setVassal` DOES trigger broad attitude / worst-enemy refresh.
- Therefore F045 is not a missing-refresh bug.
- `AI_updateWorstEnemy()` evaluates the incumbent directly through `AI_enmityValue`,
  while replacement candidates pass the same-master-locus exclusion of `KNOWN_POTENTIAL_ENEMY_OF`.
- A voluntary master/vassal relation does not force the master's attitude toward the vassal to Friendly.
- A formerly valid worst enemy can therefore remain grandfathered despite being impossible to choose from scratch
  in the same post-vassal state.
- Repair should centralize worst-enemy eligibility for incumbent and replacements.

6. Recent-nuke-memory timing concern closed clean
- Birth history explicitly introduced recent nuke-target memory for same-turn repeated-nuke valuation.
- It is intentionally remembered only for one game turn.
- Its sole current consumer is the second/repeated-strike nuke valuation path.
- No persistence/merge defect established.

7. Practical-1842 mechanical hunk inventory effectively exhausted
- All 166 `CvTeamAI.cpp` hunks from practical 1842 have now been inventoried by affected function.
- Remaining iterator/control substitutions not already owned resolve as:
  - equivalent populations,
  - intentional no-cheating / master-locus behavior,
  - unused helper changes,
  - or heuristics below correctness threshold.
- The lingering same-aggressor-locus population difference in `AI_warDiplomacyCost` remains BELOW THRESHOLD:
  it is a prospective heuristic and no hard source contract proves that a subordinate observer's current displeasure
  must be included.
- Do not promote it without stronger evidence.

8. Final mechanical sanity progress
- Fresh type/domain scan did not produce F050.
- F032 remains the unique confirmed incompatible vote-count/team-ID comparison.
- Remaining direct arrays/vectors are guarded.
- Remaining FAssert/default branches are enum-complete or dormant under supported current data.
- TODO/FIXME comments map to already-owned findings or heuristic-quality debt.
- No new current root established.

9. Collision state
- Reconciled Known-Issues collision pass remains clean through F049.
- F045 remains distinct from KI#208:
  KI#208 = stale recalculation-flag/order repair;
  F045 = incumbent-vs-replacement eligibility after relationship transition.
- No existing reviewed KI found for F046-F049 roots.

SUPERSEDED WORDING TO AVOID IN DURABLE C011
- F041:
  DO NOT claim excess Open Borders counter survives agreement cancellation and then decays.
  OB status transition resets the OB counter.
  Strong current consequence is the Bonus Trade sibling's accumulated excess and later decay.
- F047:
  DO NOT claim practical 1842 excludes foreign voluntary vassals from raw planner maintenance.
  A foreign voluntary vassal is outside OUR master locus and remains a potential enemy.
  Root is the practical-1366/advc.104j strategic write key vs tactical/query master key split.
- Earlier WIP2/WIP3 statements saying practical 1842 had already been "exhausted" before F043-F046 were found
  are superseded by the renewed complete hunk inventory.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP13
1. Finish the final adversarial finding-by-finding reread, especially F042-F049:
   - current reachability
   - exact ancestry
   - bounded consequence
   - surgical repair wording.
2. Run one final independent whole-file sanity scan after the completed practical-1842 inventory:
   - state setter/getter symmetry
   - raw sentinel contracts
   - divisors/percent arithmetic
   - default/assert reachability
   - header/member/storage invariants.
3. Confirm no open candidate remains and no superseded WIP wording leaks into the durable summary.
4. If closure is not yet fully satisfying, freeze C010-WIP14 rather than forcing completion.
5. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP13
================================================================================
WORKING CHECKPOINT C010-WIP14 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP13.
- Authoritative repository snapshot:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F049 / provisional KI#353-KI#370.
- F050 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- Main change from WIP13: F047 is broadened into the full incomplete master/vassal war-plan
  canonicalization root rather than only the legacy vassal-write/master-read symptom.

F047 / provisional KI#368 - BROADENED
Revised root:
- `advc.104j` / practical 1366 established the conceptual rule:
  "a war plan against a master implies a war plan against its vassal."
- It canonicalized several important query/tactical paths to the target MASTER,
  but did not make war-plan target canonicalization universal.
- Current code therefore contains BOTH directions of key inconsistency:
  A) some writers/raw-maintenance paths use a VASSAL key while tactical/query readers normalize to MASTER;
  B) some normal master-keyed preparations are invisible to raw per-vassal area/enemy helper reads.

Manifestation A - legacy planner vassal-key write vs master-normalized tactical read
- Legacy `AI_doWar()` can select a suitable VOLUNTARY VASSAL as `eBestTarget`.
- It writes PREPARING/LIMITED/TOTAL/DOGPILE state on that vassal team ID.
- `AI_isChosenWar(vassal)`, `AI_isSneakAttackPreparing/Ready(vassal)`,
  `AI_mayAttack(vassal)` and AI border/path DOW execution normalize the target to the MASTER.
- No automatic sibling plan is created on the enemy master by that legacy selection/write path.
- Raw AI_doWar plan maintenance can still see/age the vassal-keyed plan;
  do NOT claim practical 1842 hides foreign vassals from that raw loop.
- The tactical layer can nevertheless fail to recognize the plan under the master key,
  leading to prepare/age/abandon behavior without normal strategic DOW execution.
- Reachable when UWAI is disabled / legacy K-Mod-style war planning is active.

Manifestation B - canonical master-key preparation omitted by raw vassal area/enemy reads
- This one is reachable even with normal/default UWAI-style strategic planning.
- A normal plan against independent master M is stored on M.
- `AI_setWarPlan(M, PREPARING_...)` propagates preparations to OUR AI vassals;
  it does NOT create plan entries on M's target vassals.
- Before actual war, therefore:
  `AI_getWarPlan(M) != NO_WARPLAN`
  but for target vassal V:
  `AI_getWarPlan(V) == NO_WARPLAN`.
- Current raw helper/AreaAI code still uses those per-team keys:
  - `AI_countEnemyPowerByArea` loops enemy players and includes area power only when
    `AI_getWarPlan(kEnemy.getTeam()) != NO_WARPLAN`.
  - `AI_countEnemyCitiesByArea` uses the same raw team-plan test.
  - `AI_countEnemyPopulationByArea` also has the same raw test (currently unused).
  - `AI_calculateAreaAIType`'s target and relative-enemy loops similarly skip teams whose raw
    `AI_getWarPlan(team) == NO_WARPLAN`.
- Consequently M's vassal forces/cities can be omitted during PREPARATION even though they will
  necessarily become enemies when war with M begins.

Concrete current consequences of manifestation B
- `AI_calculateAreaAIType` uses `AI_countEnemyPowerByArea` to choose defensive/massing/offensive
  posture and compares own area power against planned-enemy power.
- It also derives target/enemy-city presence from raw per-team plan loops.
- `CvPlayerAI::AI_getTotalFloatingDefendersNeeded` uses
  `AI_countEnemyCitiesByArea` while focusing on land war / sneak preparation.
- Several `CvUnitAI` naval/worker/transport decisions use `AI_countEnemyPowerByArea`.
- During preparation against a master with a substantial vassal presence in the area,
  the AI can therefore underestimate future enemy area power/city presence and underprepare
  defenses or choose the wrong AreaAI posture.
- Once the war is actually declared, `CvTeam::declareWar` cascades war to target vassals and
  `AI_postDeclareWar` creates raw war plans for those declarations; the omission is primarily
  a PRE-WAR planning-state defect.

Why this is the same root, not F050
- Both manifestations come from incomplete canonicalization after practical 1366 / advc.104j:
  some code treats the master as the canonical strategic war target while other reads/writes
  still use raw team IDs.
- Splitting by direction would duplicate the same invariant:
  every logical war-plan lookup/write should consistently map a vassal target to its master
  unless the code explicitly needs a tactical subtarget.
- F047 should therefore be renamed/reframed around inconsistent master/vassal war-plan keying,
  with both manifestations documented.

Historical control
- K-Mod was internally same-key for the relevant vassal target queries/writes.
- Practical 1366 changed `AI_isChosenWar` and `AI_isSneakAttackPreparing` to read the target master
  and modified tactical/path behavior under the explicit "master plan implies vassal plan" idea.
- The same practical left `AI_isOkayVassalTarget` in place and even commented that it no longer
  seemed conceptually relevant.
- Area enemy-count helpers and several raw plan loops were not comprehensively canonicalized.
- This supports classifying F047 as an AdvCiv incomplete-fix / cross-contract regression.

Revised repair direction
1. Introduce one canonical helper for strategic war-plan target keys, e.g. conceptually:
   `canonicalWarPlanTarget(eTeam) = GET_TEAM(eTeam).getMasterTeam()`
   where appropriate.
2. Use the canonical key consistently for strategic plan WRITES and READS.
3. Where tactical code intentionally wants a particular vassal front/subtarget, keep that separate
   from the strategic plan key rather than overloading `m_aeWarPlan`.
4. Audit area/enemy helper loops so a master-key plan includes the master's vassal members/cities/power
   exactly once during preparation.
5. Reassess/remove `AI_isOkayVassalTarget` from strategic key selection; practical 1366 already notes
   that a master-key plan should make direct vassal strategic targeting conceptually unnecessary.

OTHER POST-WIP13 CLOSURE RESULTS
- `AI_enemyTradeResentmentFactor` apparent NO_TEAM worst-enemy sentinel risk is now killed with timing evidence:
  `declareWar` -> queued cascade handling -> `triggerWars(true)` -> global AI attitude refresh occurs before the
  declaration stack returns to ordinary AI trade evaluation. A live war enemy therefore has a recalculated
  worst-enemy state before normal resentment callers execute.
- Current-vs-Base AdvCiv whitespace-insensitive semantic diff was re-enumerated (~43 hunks):
  substantive current SAS changes remain the already-reviewed Barbarian AreaAI option, KI#312 tech-locus repair,
  victory-denial peace hook, KI#208 worst-enemy repair, diagnostics/logging, and current-format serialization cleanup.
  No overlooked SAS-specific semantic root was found in that pass.
- Persistent member storage vs reset/read/write/cross-file transitions remains symmetric apart from already-owned:
  F042, F045, F048, F049.
- F050 remains UNUSED.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368 - BROADENED as above
- F048 / KI#369
- F049 / KI#370
- F050 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP14
1. Continue final independent caller-oriented pass over raw `AI_getWarPlan` uses:
   - distinguish intentional raw active-war state from places that should use the canonical master target,
   - broaden F047 further if same invariant; do not create duplicate roots.
2. Finish tiny-helper / trade-value / area-enemy / sentinel / divisor / assert / header checks.
3. Complete final adversarial reread F032-F049 with corrected F047 architecture.
4. Confirm practical 1842 final inventory has no unresolved candidate.
5. Confirm no superseded WIP wording remains in the eventual durable C011 synthesis.
6. If not fully satisfied, freeze C010-WIP15 rather than force completion.
7. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP14
================================================================================
WORKING CHECKPOINT C010-WIP15 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP14.
- Authoritative repository snapshot:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots now extend through F050 / provisional KI#371.
- F051 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects F050 and the post-WIP14 F047 broadening/caller verification before further closure work.

F050 - provisional KI#371 - K-Mod war-success coalition formulas exclude vassal battle results while including vassal power
Status: CONFIRMED CURRENT INHERITED K-MOD COALITION-AGGREGATION DEFECT.

Core state contract:
- Ordinary WarSuccess is stored directionally by ACTUAL team ID.
- Combat/city results call `AI_changeWarSuccess` on the actual attacker/defender/city-owner team:
  - defender-side success against actual attacker team,
  - attacker-side success against actual defender team,
  - city capture against actual city team.
- `CvTeam::setVassal()` does NOT transfer ordinary `AI_getWarSuccess` between vassal and master.
- Current `AI_countEnemyWarSuccess()` provides a useful internal control:
  it iterates `CIV_ALIVE, ENEMY_OF`, i.e. all alive enemy teams including vassals, and sums their
  actual WarSuccess against us.

K-Mod `AI_getWarSuccessRating()` population mismatch:
- Current function loops:
  `TeamAIIter<FREE_MAJOR_CIV, ENEMY_OF>`
  and therefore excludes all enemy vassal team IDs.
- For each free enemy master it reads only:
  - `AI_getWarSuccess(master)`
  - `master.AI_getWarSuccess(getID())`.
- It does not sum that master's vassal teams' WarSuccess.
- Yet its power terms use:
  - `iThisTeamPower = getPower(true)`
  - `iOtherTeamPower = master.getPower(true)`.
- `CvTeam::getPower(true)` explicitly adds all alive vassal players' power.
- Therefore the formula measures military POWER over master+vassal coalitions while measuring
  BATTLE RESULTS only over the free-master team IDs.

Symmetric own-side inconsistency:
- When this team is a free master, `getPower(true)` includes OUR vassals.
- WarSuccess earned by our vassal teams remains keyed to those vassal team IDs and is not folded into
  our own `AI_getWarSuccess(masterEnemy)` value.
- Thus combat by either side's vassals can disappear from the success term even while those units
  remain included in coalition power.

Concrete supported case:
1. Free team A is at war with free master M and M's vassal V.
2. Most/all actual fighting occurs between A and V.
3. A destroys many V units; V scores little or nothing.
4. The victories are recorded in `A.AI_getWarSuccess(V)`.
5. `AI_getWarSuccessRating()` never visits V because V is not FREE_MAJOR_CIV.
6. No ordinary WarSuccess transfer moves `A->V` into `A->M`.
7. M's `getPower(true)` nevertheless includes V's military power.
8. The rating can remain neutral/negative based largely on coalition power despite A actually
   winning the battles that the function contract says it is supposed to reflect.
- The reverse loss case is likewise ignored if V is the one defeating A.

Function contract / historical evidence:
- K-Mod practical 116 (`47f56c9c4e...`) explicitly rewrote the BBAI ratio and says:
  - it should "scale properly for wars of different sizes in different eras",
  - +99 means "we are soundly defeating our enemies",
  - zero is neutral.
- That rewrite explicitly filtered enemy teams with `!kLoopTeam.isAVassal()`
  while simultaneously using `getPower(true)`.
- K-Mod practical 455 later renamed the function to `AI_getWarSuccessRating`,
  explicitly stating that it "has nothing to do with capitulation" and is a general war-success rating.
- AdvCiv later refactors preserve the free-enemy population (`FREE_MAJOR_CIV, ENEMY_OF`);
  scaled WarSuccess conversion also preserves it.
- No descendant aggregation of vassal WarSuccess into the master was found.

Force-Peace sibling manifestation:
- K-Mod practical 86 rewrote Force-Peace vote decision similarly:
  - explicitly excludes vassal enemy teams,
  - uses free enemy master's `getPower(true)`,
  - reads only direct `ePeaceTeam <-> free master` WarSuccess.
- Thus the same coalition-population mismatch also exists in that K-Mod force-peace evaluator.
- Current AdvCiv comment notes UWAI can overwrite most K-Mod Force-Peace results when enabled,
  so F050 should not rely solely on this path for current impact.
- Treat it as a sibling manifestation of the same K-Mod aggregation design.

Current impact:
- `AI_getWarSuccessRating()` remains widely consumed by:
  - CvCityAI production / panic / wonder / unit decisions,
  - CvPlayerAI military/economic strategy,
  - CvUnitAI behavior,
  - UWAICache / UWAIAgent,
  - WarUtilityAspect,
  - CvTeamAI war decisions,
  - UI/debug help.
- Therefore omitted vassal battle results can affect default/current AI behavior even when full UWAI is enabled.

Why this is one root:
- K-Mod deliberately tried to avoid treating each vassal as an independent war by iterating only free teams.
- But it canonicalized only the POWER side of the coalition through `getPower(true)`;
  it never canonicalized/aggregated WarSuccess state to the same master coalition.
- The repair invariant is population consistency: coalition power and coalition battle success must cover
  the same master/vassal membership.

Collision:
- Semantic searches of current repository Album and reviewed README_Known_Issues found no same-root
  "vassal WarSuccess omitted from free-master coalition rating / Force Peace" issue.

Repair direction:
1. For each free enemy coalition master, aggregate directional WarSuccess across the master and all its vassals
   before applying coalition power.
2. If the rating concept includes our own vassal power through `getPower(true)`, likewise aggregate the
   WarSuccess earned/suffered by our vassal teams so numerator/state population matches own coalition power.
3. Prefer a centralized coalition/master-locus WarSuccess helper so Force-Peace and `AI_getWarSuccessRating`
   cannot independently repeat the same free-team/raw-team mismatch.
4. Avoid simply re-including every vassal as a separate top-level enemy while retaining `master.getPower(true)`,
   which would double-count coalition power; aggregate vassal battle results under the free master instead.

POST-WIP14 F047 BROADENING / CALLER VERIFICATION
- F047 remains ONE incomplete master/vassal war-plan canonicalization root, not F050:
  - legacy vassal-key strategic writes can be missed by master-normalized tactical/read APIs,
  - canonical master-key preparations can be missed by raw per-vassal area/enemy helpers.
- Additional current consequence confirmed:
  `AI_updateWarPlanCounts()` deliberately ignores plans whose RAW target is currently a vassal.
  Therefore manifestation A's legacy vassal-keyed plan may also leave:
  - `m_bAnyWarPlan == false`
  - per-plan cached counts == 0
  even though the raw planner contains a real preparation/war plan.
- Many production/strategy helpers consume `AI_isAnyWarPlan()`, so manifestation A is not limited to
  tactical border-crossing readiness.
- Conversely, master-key preparations make the global cache correct but still leave raw target-vassal
  AreaAI/enemy aggregators blind during the preparation phase.
- This reinforces one canonical-key invariant rather than creating extra roots.

OTHER POST-WIP14 CLOSURE RESULT
- Fresh free/master-only enemy aggregation audit identified F050 above.
- No other independent finding has yet emerged from that pass.
- F051 currently UNUSED.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368 - broadened incomplete war-plan canonicalization
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP15
1. Continue free/master-only team aggregation audit:
   - WarSuccess / war weariness / power / enemy-state helpers,
   - determine whether any sibling mismatch belongs under F050 or is intentional.
2. Finish final independent tiny-helper/caller/sentinel/divisor/assert/header pass.
3. Complete adversarial reread F032-F050:
   - exact current reachability,
   - ancestry classification,
   - bounded consequence,
   - surgical repair direction.
4. Verify practical-1842 inventory has no unresolved candidate and no superseded wording survives.
5. Deep collision search against reconciled Album + reviewed KI through current practical 6239.
6. If not fully satisfied, freeze C010-WIP16 rather than force C011.
7. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP15
================================================================================
WORKING CHECKPOINT C010-WIP16 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP15.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F050 / provisional KI#353-KI#371.
- F051 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the clean final-scan work performed after WIP15 before the next adversarial pass.

POST-WIP15 FINAL-SCAN PROGRESS

1. Quantitative claims revalidated against current practical-6239 XML/data
- F034:
  current XML has exactly five default air UnitClasses with DOMAIN_AIR and positive iAirCombat:
  Fighter, Jet Fighter, Bomber, Stealth Bomber, Guided Missile.
  Therefore the simple current-data example in which the rival-team denominator is inflated by roughly 5x
  remains accurate.
- F041 / F049:
  ordinary leader defaults remain:
  - BonusTradeAttitudeDivisor = 50
  - BonusTradeAttitudeChangeLimit = +2
  - OpenBordersAttitudeDivisor = 25
  - OpenBordersAttitudeChangeLimit = +2
  The numeric examples remain current.

2. F035 independence assumption revalidated
- GET_BETTER_UNITS strategy is generated from military-unit quality/availability conditions.
- DAGGER is generated separately from an aggression/early-rush strategic score.
- financial trouble is separately computed state.
- No implication relation was found among these sets.
- Therefore subtracting total Dagger count from FinancialTrouble / GET_BETTER_UNITS counts remains invalid
  set arithmetic, and F035 survives unchanged.

3. F041 birth history revalidated
- Practical 1386 introduced the randomized relationship-counter helper and the large Open Borders / Bonus Trade
  "cap" arguments in the same change.
- Helper semantics cap only the returned 0-2 per-turn binomial increment.
- The large caller caps were therefore ineffective from birth; this is not a later semantic drift.
- Preserve the corrected consequence:
  - Open Borders counter resets on agreement status changes;
  - do NOT claim OB excess survives cancellation;
  - Bonus Trade excess + later decay remains the strong current effect.

4. Division / modulo sweep
- Every syntactically unguarded division/modulo site in CvTeamAI was enumerated and checked against live caller/data invariants.
- Representative `AI_refuseToTalkWarThreshold()` member-count divisor is only reached for a live player's
  at-war team and is therefore nonzero.
- Demographic/yield/team-attitude averages have nonempty team or explicit positive denominator invariants.
- Variable percentage denominators are guarded.
- No F051 from divide-by-zero or denominator reachability.

5. Tiny-army `AI_getWarSuccessRating()` scale - held below threshold
- `militaryUnits * WAR_SUCCESS_ATTACKING / 5` can become very small/zero for tiny armies.
- K-Mod deliberately scaled WarSuccess sensitivity by army size and guards the eventual divisor with max(1,...).
- No source-backed correctness threshold says tiny wars must be smoothed differently.
- Keep as tuning territory, not a finding.

6. Sentinel / type-domain sweep
- Raw NO_TEAM / enum-map callers rechecked.
- APIs using NO_TEAM as "any team" branch before raw indexing.
- Integer-vs-team/player/enum comparisons were scanned again.
- F032 remains the unique confirmed incompatible vote-count/team-ID comparison.
- No new current sentinel/type root.

7. FAssert / default-switch sweep
- Live attitude/yield switches cover current valid enum domains.
- Missing UNITAI_ATTACK_CITY_LEMMING arm in tech-monopoly switch remains dormant because current XML has no
  Lemming `<DefaultUnitAI>`.
- Other default/assert branches are unreachable for supported current enum/data.
- No F051.

8. Unused-parameter / ignored-argument scan
- F033 remains the meaningful ignored target-team parameter caused by own-team iteration.
- `AI_forgiveEnemy(..., bFreed)` is explicitly documented in AdvCiv as unused/not needed.
- `AI_reportSharedWarSuccess(..., eEnemy, ...)` matches the aggregated-distress design; enemy identity is not
  currently semantically required.
- `AI_teamCloseness(..., bConsiderLandTarget, ...)` remains historical API residue after the effect moved to
  AI_playerCloseness.
- No new dropped-behavior root.

9. Comment debt / FIXME / TODO inventory
- Concrete Defensive Pact double-count FIXME = F040.
- Remaining XXX/TODO comments concern strategic approximations, old replaced code, or possible tuning improvements.
- No unowned explicit correctness FIXME remains.

10. Persistent member / reset-read-write-state pass
- CvTeamAI persistent member storage was re-enumerated against constructor/reset/read/write and major diplomacy transitions.
- Apart from already-owned lifecycle/merge roots:
  - F042 war-plan-count restoration,
  - F045 worst-enemy incumbent eligibility,
  - F048 SharedWarSuccess own-side PA merge,
  - F049 OpenBordersCounter reverse PA merge,
  remaining persistent state examined is symmetric, reconstructed, derived, or intentionally transient.
- Current read/write structure remains paired.
- `AI_finalizeInit()` not initially forcing `m_bAnyWarPlan=false` remains a robustness improvement only; no
  independent supported stale-true/no-plan lifecycle was established.

11. Free/master-only aggregation scan
- F050 was the surviving coalition-population defect:
  WarSuccessRating / Force-Peace free-master loops omit vassal battle results while power uses getPower(true).
- Possible capitulation/surrender sibling was NOT automatically folded in:
  surrender code contains explicit design comments about how vassal WarSuccess should affect master's
  capitulation chances, so that subsystem has separate intentional semantics and is below the current defect threshold.
- No additional coalition-aggregation root established after F050.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368 - broadened incomplete master/vassal war-plan canonicalization
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP16
1. Continue the final adversarial finding-by-finding reread F032-F050:
   try to KILL, narrow or merge findings rather than merely confirm them.
2. Complete one final independent caller-oriented pass over tiny helpers and state consumers.
3. Run deep semantic collision search against current reconciled Album + reviewed KI through practical 6239.
4. Ensure no superseded wording survives:
   - F041: no OB post-cancellation decay claim.
   - F047: no practical-1842 foreign-vassal exclusion claim.
   - old "1842 exhausted" WIP statements are historical/superseded only.
5. Confirm no open candidate remains.
6. If any uncertainty remains at handoff:
   freeze C010-WIP17 rather than forcing completion.
7. Only when genuinely satisfied the whole file is exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP16
================================================================================
WORKING CHECKPOINT C010-WIP17 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP16.
- Authoritative repository snapshot remains:
  - branch main
  - HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
  - practical 6239
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F050 / provisional KI#353-KI#371.
- F051 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the post-WIP16 ancestry corrections and narrowed repair guidance before further certification work.

F049 / provisional KI#370 - ANCESTRY CORRECTION
Correct classification:
- F049 is NOT introduced by K-Mod practical 941.
- The reverse Permanent-Alliance OpenBordersCounter omission is inherited from original Firaxis team-merge code.

Historical control:
- Civ4 / Warlords / BtS historical source snapshots already show the same directional asymmetry:
  - absorbed-team -> outsider `shareCounters()` preserves OpenBordersCounter,
  - outsider -> merged-team reverse averaging combines neighboring counters but omits OpenBordersCounter.
- K-Mod practical 941 ("Made some changes to how data is merged when two teams merge") refactored team-merge logic
  but preserved the pre-existing omission.
- K-Mod, AdvCiv and SAS therefore inherit this root from Firaxis/Civ4 lineage.

Current defect statement remains unchanged:
- when survivor A absorbs B and B has Open Borders history with outsider C,
  B->C can be preserved into A->C by `shareCounters`,
  while C->B is not merged into C->A;
- if A lacked Open Borders with C, creation of the inherited relationship resets C->A to zero and no reverse merge restores B's history;
- if A already had OB, only A's prior history survives instead of the combined team history.
- Current Open Borders attitude consumes that directional counter, so the outsider can lose relationship attitude history.

Repair remains:
- add reverse team-size-weighted OpenBordersCounter merging in `CvTeam::addTeam`,
  following the existing reverse relationship-counter policy.

F047 / provisional KI#368 - REPAIR SCOPE NARROWED
- Do NOT globally replace every raw `AI_getWarPlan(team)` with master-key lookup.
- After war begins, cascading declarations create genuine per-vassal raw war plans, and some code intentionally needs actual-team active-war state.
- The broken invariant is chiefly PRE-WAR / STRATEGIC canonicalization:
  - strategic plan writes/reads disagree on vassal vs master key,
  - `AI_isAnyWarPlan()` cache ignores raw vassal-key plans,
  - master-key preparations can leave target-vassal area/enemy helpers blind until actual war declaration.
- Preferred repair architecture:
  1. canonicalize strategic/preparation war-plan identity to the master team;
  2. keep tactical subtarget/front selection separate from the strategic key;
  3. preserve actual per-team active-war plans once war is declared;
  4. audit area/enemy preparation helpers to include the master's vassal coalition exactly once.

F050 / provisional KI#371 - SCOPE CHECK
- Additional FREE_MAJOR_CIV / ENEMY_OF loops were inspected.
- Most are correctly asking an independent-master question (e.g. which coalition master would enter/leave war).
- F050 remains distinct because its formulas mix populations:
  - WarSuccess from free-master team IDs only,
  - power through `getPower(true)` including vassals.
- Surrender/capitulation code has explicit separate comments/policies for vassal WarSuccess; do not broaden F050 into that subsystem without a direct contradiction.
- Current best root remains coalition-population consistency in `AI_getWarSuccessRating` plus the Force-Peace sibling.

POST-WIP16 ANCESTRY ANCHORS
- SharedWarSuccess feature lineage first appears in AdvCiv practical 1253 / advc.130m family; this supports F048's
  "new field omitted from one older merge side" classification.
- `AI_randomCounterChange` relationship-counter randomization/cap helper appears in practical 1386; F041's ineffective
  large caller caps are born together with the helper contract rather than later drift.
- `AI_updateWarPlanCounts` / cached plan-count optimization appears in practical 1677; F042's setVassal ordering defect
  belongs to that optimization lineage.
- F049 corrected to Firaxis/Civ4 ancestry as above.

POST-WIP16 CLEAN CERTIFICATION RESULTS
- Repeated persistent-member lifecycle scan found no fifth durable state omission beyond:
  F042, F045, F048, F049.
- Repeated divisor/denominator, NO_TEAM, enum/ID, direct-index, FAssert/default, unused-parameter and FIXME/TODO scans
  produced no F051.
- F032 remains the unique confirmed incompatible count-vs-team-ID comparison.
- Current practical-6239 CvTeamAI source remains unchanged from original practical-6225 audit baseline.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368 - broadened incomplete strategic master/vassal war-plan canonicalization
- F048 / KI#369
- F049 / KI#370 - FIRAXIS/CIV4 inherited ancestry
- F050 / KI#371
- F051 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP17
1. Continue ancestry/adversarial reread F032-F050, trying to kill/narrow/merge rather than merely affirm.
2. Verify birth/practical ancestry especially for:
   - F032/F033/F036-F039/F043-F046 practical-1842 family,
   - F040 inherited DP coalition logic,
   - F045 worst-enemy candidate iterator change,
   - F048 SharedWarSuccess feature merge,
   - F050 K-Mod war-success coalition rewrite.
3. Finish final caller-oriented pass over tiny helpers and state consumers.
4. Deep semantic collision search against reconciled Album + reviewed Known Issues through practical 6239.
5. Confirm no open candidate and no superseded wording.
6. If uncertainty remains, freeze C010-WIP18.
7. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP17
================================================================================
WORKING CHECKPOINT C010-WIP18 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP17.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots remain F032-F050 / provisional KI#353-KI#371.
- F051 is currently an OPEN UNNUMBERED declare-war-trade candidate.
- Do NOT promote this checkpoint to C011.

POST-WIP17 PROTECTED PROGRESS

F047 / provisional KI#368 - additional same-root raw-plan callers
The broadened incomplete master/vassal strategic war-plan canonicalization root now has additional
pre-war readers that expose the same mismatch:

1. `AI_isHasPathToEnemyCity()`
- Raw per-team `AI_getWarPlan(eLoopTeam)` filtering can omit cities belonging to a target master's
  vassal during preparation when the canonical strategic plan exists only on the master.
- This can make path-to-enemy-city reasoning understate the future coalition before declaration.

2. `AI_getRivalAirPower()`
- Enemy-air treatment checks raw `AI_getWarPlan(itRival->getID())`.
- During preparation against a master, air units belonging to the master's vassal can remain outside
  the "enemy" component because no raw plan exists on that vassal yet.
- They may still contribute to the broader rival average, but planned-coalition enemy weighting is incomplete.

3. `AI_getTechMonopolyValue(eTech,eTeam)`
- Military-tech monopoly valuation sets war-plan context through raw `AI_getWarPlan(eTeam)`.
- If `eTeam` is a foreign vassal of the planned master, the strategic coalition is logically part of the
  future war but the raw vassal key can report NO_WARPLAN during preparation.

These are NOT separate findings. They reinforce the same repair invariant:
- canonicalize strategic/preparation coalition identity,
- preserve true per-team active-war state after declaration,
- audit raw plan reads only where they are intended to represent preparation rather than actual team-local war state.

F050 / provisional KI#371 - additional population precision
- `AI_getWarSuccessRating()` mixes populations in one more term:
  - `iThisTeamPower = getPower(true)` includes our vassals,
  - but `iSuccessScale` is based on `getNumMilitaryUnits()` for this team itself.
- Therefore even the WarSuccess smoothing/normalization scale is narrower than the coalition whose power is
  used in the same formula.
- Keep this under F050; it is the same coalition-population consistency defect, not a separate root.
- The key repair remains to define one master/vassal coalition population and use it consistently for:
  WarSuccess earned/suffered, military scale and power.

OPEN UNNUMBERED CANDIDATE - possible F051
Candidate:
- Legacy `AI_declareWarTradeValLegacy()` may underprice a declaration of war against a VOLUNTARY VASSAL
  by valuing the named vassal rather than the master coalition that necessarily enters the war.

Current wrapper:
- `AI_declareWarTradeVal(eTarget,eSponsor)`:
  - with UWAI enabled, explicitly canonicalizes:
    `GET_TEAM(eSponsor).uwai().declareWarTradeVal(GET_TEAM(eTarget).getMasterTeam(), getID())`
    and comments that the target can be a voluntary vassal;
  - without UWAI, calls:
    `AI_declareWarTradeValLegacy(eTarget,eSponsor)`
    with the raw target ID.

Legacy value terms use raw target:
- target city count
- target total population
- target nukes
- `GET_TEAM(eWarTeam).getPower(true)`.
For a voluntary vassal, `getPower(true)` includes that vassal's own vassals (if any) but NOT its master.

Coalition contrast:
- `CvTeam::getDefensivePower()` canonicalizes through `getMasterTeam()` and includes the full same-master coalition.
- `AI_declareWarTrade` denial/power logic can therefore see the full master/vassal coalition while the legacy
  price formula appears to value only the named vassal.

War-propagation fact already established:
- declaring war on a voluntary vassal queues vassal-alignment war against its master / same-locus partners.
- Therefore accepting the trade entails war with more than the named vassal.

Historical anchor:
- AdvCiv practical 1263 / advc.104o split the old K-Mod/BtS valuation into `AI_declareWarTradeValLegacy`.
- The new UWAI branch already normalized a voluntary-vassal target to its master and explicitly documented that
  the raw target can be a voluntary vassal.
- Legacy branch intentionally preserved old raw-target valuation.

DO NOT PROMOTE YET.
Proof still required:
1. Show a supported non-UWAI current trade path can actually request/price war against a voluntary vassal.
2. Show legacy valuation materially omits the master coalition and no later floor/adjustment reliably compensates.
3. Determine whether the legacy formula's raw-front valuation was intentionally preserved or is simply inherited
   K-Mod/BtS behavior made inconsistent with AdvCiv's master-normalized war semantics.
4. Search current Album / reviewed KI for same-root collision.
5. If confirmed, assign F051 / provisional KI#372; otherwise record as heuristic approximation.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 currently OPEN / UNNUMBERED.

EXACT RECOVERY CURSOR AFTER C010-WIP18
1. Prove or kill the legacy voluntary-vassal declare-war-trade valuation candidate.
2. Continue final ancestry/adversarial reread F032-F050.
3. Finish caller-oriented raw war-plan audit under F047 without splitting duplicate roots.
4. Deep collision search against reconciled Album + reviewed KI through practical 6239.
5. If handoff occurs before closure, freeze C010-WIP19.
6. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP18
================================================================================
WORKING CHECKPOINT C010-WIP19 - CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP18.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Confirmed/provisional roots now extend through F052 / provisional KI#373.
- F053 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- F051 is a late cross-file CvPlayerAI regression discovered while proving the CvTeamAI war-trade caller contract.
- F052 is a cross-contract AdvCiv UWAI/TRADE_WAR feature mismatch.
- The earlier unnumbered "legacy voluntary-vassal price undercounts master" candidate is NOT independently current-reachable because F052's game-rule gate blocks vassal TRADE_WAR items; retain it only as a repair hazard if voluntary-vassal war trades are enabled in legacy mode.

F051 - provisional KI#372 - legacy AI joint-war proposal uses untouched -1 cached war price
Status: CONFIRMED CURRENT CONFIGURATION-REACHABLE CROSS-FILE REGRESSION.
Root file: `CvPlayerAI.cpp`, discovered through `CvTeamAI::AI_declareWarTradeVal` caller tracing.

Current control flow:
- `CvPlayerAI::AI_proposeWarTrade(eHireling)` initializes:
  `int iBestTeamPrice = -1;`
- UWAI target-selection branch:
  - evaluates candidate joint-war utility,
  - calls `AI_declareWarTradeVal`,
  - assigns `iBestTeamPrice = iTheirPrice` when choosing a target.
- Legacy/K-Mod branch:
  - independently chooses a legal target,
  - never assigns `iBestTeamPrice`.
- After either branch:
  `int iWeReceive = iBestTeamPrice;`
  so the legacy path enters deal balancing with -1.

Concrete no-tech consequence:
1. UWAI disabled / Aggressive AI (Legacy) active.
2. AI sponsor is at war and finds a legal AI hireling + target.
3. Legacy branch chooses `eBestTarget`.
4. No tradable tech is available, so `iHirelingReceives = 0`.
5. `iWeReceive = -1`.
6. Gold-balancing branches do not repair the sentinel.
7. Final deal test:
   `4 * iHirelingReceives > 3 * iWeReceive`
   becomes `0 > -3`, true.
8. AI implements a deal in which the hireling gives `TRADE_WAR` and the sponsor can give nothing.

With a tradable tech:
- positive `iHirelingReceives > -1`,
- because legacy `iBestTargetValue` remains 0, the code can enter the branch that asks the HIRELING to pay gold back
  to the sponsor, even though the sponsor is the one asking for military help.

Ancestry:
- Before AdvCiv practical 1341, the common post-selection path always called:
  `ourTeam.AI_declareWarTradeVal(eBestTeam, civ.getTeam())`
  after either UWAI or legacy target selection.
- Practical 1341 / `a5724ff581...`, title "Some small AI changes":
  - introduced `iBestTeamPrice = -1`,
  - cached/assigned the price only in the UWAI branch,
  - replaced the old common `AI_declareWarTradeVal(...)` call with `iBestTeamPrice`.
- The legacy branch was left without an assignment.
- Later refactors (including extraction to `AI_proposeWarTrade`) preserve the sentinel and branch structure.
- No descendant repair found.

Current supported reachability:
- AdvCiv explicitly supports disabling UWAI through:
  - Custom Game "Aggressive AI (Legacy)" / K-Mod mode,
  - `DISABLE_UWAI`,
  - related legacy testing configurations.
- `AI_proposeWarTrade` is called for AI-AI diplomacy regardless; it has explicit UWAI vs legacy branches.
- Therefore this is not dormant debug code.

Collision:
- No current reviewed Known Issue / Album root for `iBestTeamPrice` or free/inverted legacy AI joint-war compensation found.

Repair direction:
- Restore a valid legacy price before common deal balancing.
- Minimal safe repair:
  after `eBestTarget` is selected, if UWAI is disabled:
  `iBestTeamPrice = kOurTeam.AI_declareWarTradeVal(eBestTarget, kHireling.getTeam());`
  (or assign the price when selecting the legacy target).
- Keep the UWAI cached-price optimization.
- Add/assert `iBestTeamPrice >= 0` once `eBestTarget != NO_TEAM` before monetary/tech balancing so this sentinel cannot silently reappear.

F052 - provisional KI#373 - intended UWAI voluntary-vassal war-trade targets are blocked by inherited BtS TRADE_WAR gate
Status: CONFIRMED CURRENT ADV-CIV INCOMPLETE FEATURE / CROSS-CONTRACT DEFECT.

Inherited game-rule gate:
- Current `CvPlayer::canTradeItem(... TRADE_WAR ...)` requires:
  `!GET_TEAM(eTargetTeam).isAVassal()`.
- Thus ALL vassal teams, including voluntary non-capitulated vassals, are rejected before AI denial/value logic.

Historical lineage:
- Original Civ4 TRADE_WAR game-rule code did not have the universal target-vassal exclusion.
- Warlords used a narrower vassal restriction.
- BtS changed the target rule to reject any `isAVassal()` team.
- AdvCiv inherited that BtS gate unchanged through current practical 6239.

AdvCiv `advc.104o` later introduces an explicit contradictory target contract:
- In `CvPlayerAI::AI_proposeWarTrade`, UWAI target-selection comment says:
  an unpopular VOLUNTARY vassal "should be a valid target even if the master would not be."
- The code itself rejects only `kTarget.isCapitulated()` before testing `canTradeItem`, showing voluntary vassals are intended to remain candidates.
- `CvTeamAI::AI_declareWarTradeVal` UWAI branch explicitly comments:
  "`eWarTeam` can be a (voluntary) vassal"
  and canonicalizes the valuation target to `GET_TEAM(eTarget).getMasterTeam()`.
- `CvTeam::canEventuallyDeclareWar(eTarget)` calls `canChangeWarPeace(eTarget, true)`,
  which permits declaration against a FOREIGN voluntary vassal.
- `CvTeam::declareWar` contains the vassal-alignment cascade that brings the target's master/locus into the war.
- Therefore the underlying war system supports the state that the trade-item gate suppresses.

Concrete lost behavior:
1. Sponsor A is at war with voluntary vassal V (and therefore V's master M).
2. Potential hireling H dislikes V enough to accept a war trade against V but likes M too much to accept the same trade when M is named.
3. AdvCiv UWAI code explicitly intends V to be a valid target in this case.
4. `H.canTradeItem(A, TRADE_WAR(V), ...)` reaches the inherited BtS game-rule gate.
5. `V.isAVassal()` makes the item invalid before `AI_declareWarTrade` / UWAI target valuation runs.
6. The intended target choice is therefore impossible.

User-facing side:
- The same `canTradeItem` contract also prevents ordinary trade UI / AI deal machinery from constructing a war-trade item whose named target is a voluntary vassal.
- The strongest source-backed design contradiction, however, is AdvCiv's own UWAI AI-AI target comment and master-normalized valuation path.

Collision:
- No current reviewed KI / Album entry for the voluntary-vassal `TRADE_WAR` game-rule gate versus advc.104o intent found.
- Distinct from F047:
  F047 = strategic war-plan key canonicalization.
  F052 = diplomatic TRADE_WAR target availability.

Repair direction:
Conservative UWAI-preserving option:
- Relax the target-vassal gate only for VOLUNTARY/non-capitulated vassals when the relevant supported UWAI war-trade path is active.
- Continue rejecting capitulated-vassal target items if that remains the desired user/game-rule policy.
General option:
- Replace universal `!isAVassal()` with a deliberate capitulation/voluntary distinction (e.g. reject only capitulated),
  then ensure both UWAI and legacy valuation/denial paths are coalition-correct.

IMPORTANT REPAIR HAZARD - dormant legacy valuation mismatch
- The earlier unnumbered candidate about `AI_declareWarTradeValLegacy(vassal, sponsor)` underpricing the target master is
  NOT independently current-reachable through normal TRADE_WAR item construction because F052's gate blocks all vassal targets.
- If F052 is fixed by enabling voluntary-vassal targets in LEGACY/K-Mod mode too, this latent mismatch becomes reachable:
  - legacy value uses raw vassal cities/population/nukes/power,
  - `getPower(true)` on a vassal does not include its master,
  - declaring war on the vassal necessarily aligns the master,
  - embargo floor is also raw-target based and does not reliably restore coalition valuation.
- Therefore either:
  1. enable voluntary-vassal war-trade targets only in UWAI mode (whose valuation already canonicalizes to master), or
  2. canonicalize `AI_declareWarTradeValLegacy` to the master coalition at the same time.
- Do NOT assign a separate current finding to the dormant raw legacy price under the present gate.

CURRENT CONFIRMED / PROVISIONAL FILE-010 SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364
- F044 / KI#365
- F045 / KI#366
- F046 / KI#367
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 / KI#372 - late cross-file CvPlayerAI joint-war pricing regression
- F052 / KI#373 - blocked AdvCiv UWAI voluntary-vassal war-trade target feature
- F053 currently UNUSED.

EXACT RECOVERY CURSOR AFTER C010-WIP19
1. Continue final adversarial/caller pass after F051/F052:
   - inspect war-trade/peace-trade helper contracts for any same-root sibling,
   - do not duplicate F051/F052 by counting repair hazards as current defects.
2. Continue raw-plan caller audit under F047.
3. Finish ancestry table/reread F032-F052 and deep collision search.
4. Run final whole-file state/sentinel/divisor/assert/header sanity pass.
5. If uncertainty remains, freeze C010-WIP20.
6. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP19
================================================================================
WORKING CHECKPOINT C010-WIP20 - ROOT-NORMALIZED, CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending reconciled C010-WIP19.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Root normalization performed before durable C011:
  F043, F044 and F046 are now MERGED HISTORICAL ALIASES of F037 rather than independent roots.
- Confirmed/provisional independent roots are therefore:
  F032-F042, F045, F047-F052 (with gaps intentionally retained for traceability).
- Provisional KI#364, KI#365 and KI#367 are MERGED/RESERVED aliases under KI#358; they should not become
  separate reviewed Known-Issue entries unless later evidence establishes an independent root.
- F053 currently UNUSED.
- Do NOT promote this checkpoint to C011.

ROOT NORMALIZATION - F037 absorbs F043 / F044 / F046
Canonical root:
F037 / provisional KI#358
Practical-1842 `AI_preDeclareWar()` uses one attacker-relative `KNOWN_POTENTIAL_ENEMY_OF`
outer population for four inner memory contracts that originally had different populations.

Why one root:
- Pre-1842 code had one broad loop over every alive non-minor/major player.
- Each inner branch then applied its OWN semantic predicate:
  1. war-despite-tribute reputation:
     observer must know the tribute-paying VICTIM and satisfy separate master-locus conditions;
  2. `MEMORY_MADE_DEMAND` reset:
     declaring player forgets the memory toward ALL other living major players;
  3. direct victim `MEMORY_DECLARED_WAR`:
     target-team members are processed whenever a valid declaration occurs, including a first-contact cascade;
  4. third-party `MEMORY_DECLARED_WAR_ON_FRIEND`:
     `AI_disapprovesOfDoW()` itself defines the valid observer population and intentionally does not require
     prior contact with the aggressor.
- Practical 1842 replaced the common broad loop with:
  `PlayerIter<MAJOR_CIV, KNOWN_POTENTIAL_ENEMY_OF>(getID())`
  keyed to the AGGRESSOR before any of those inner branches execute.
- That single population substitution imposes:
  - aggressor contact,
  - exclusion of aggressor same-master-locus players,
  - and an attacker-relative population,
  on all four logically different contracts.
- Restoring the original broad major-player population (or splitting the four operations into correctly scoped loops)
  repairs all four manifestations together.
- Therefore counting four separate bugs would overstate one refactor root.

Canonical F037 manifestations to preserve:
A. Former F037 / KI#358 manifestation - war despite received tribute
- Observer population should be based on knowledge of the tribute-paying VICTIM plus the original locus condition.
- 1842 instead keys outer eligibility to knowledge/locus relative to the AGGRESSOR.
- Can both omit a valid victim-knowing observer and admit an attacker-knowing observer that lacks the victim-contact
  condition intended by the original code.

B. Former F043 / provisional KI#364 - all arrogant-demand reset
- Practical 1260 explicitly promised that AI civs forget ALL "arrogant demands" when declaring any war.
- Pre-1842 broad loop cleared `MEMORY_MADE_DEMAND` toward all other living major players.
- 1842 only clears players in the aggressor's current potential-enemy iterator.
- Existing demand memories toward current master/vassal-locus players can survive contrary to the feature contract.
- `setVassal()` does not symmetrically clear the master's memories about the new vassal.

C. Former F044 / provisional KI#365 - war-on-friend observer memory
- Pre-1842 broad population let `AI_disapprovesOfDoW()` enforce its own semantics.
- That helper deliberately does NOT require the observer to have met the aggressor and does not simply reject an
  observer for sharing the aggressor's master locus.
- 1842 silently removes those observers before the helper is called.
- Historical `MEMORY_DECLARED_WAR_ON_FRIEND` can therefore be omitted and later diplomacy/war utility altered.

D. Former F046 / provisional KI#367 - direct victim memory on first-contact cascading declaration
- `CvTeam::declareWar()` calls `AI_preDeclareWar()` BEFORE `meet()`.
- Defensive Pact / vassal-alignment cascades can declare war between teams that had not previously met.
- The 1842 outer contact filter can therefore remove the direct target's players before the
  `MEMORY_DECLARED_WAR` branch.
- Later contact establishment does not replay the missed memory.

Historical aliases / counting policy:
- F043, F044, F046 remain in WIP history for traceability but are RETIRED AS INDEPENDENT ROOTS.
- KI#364, KI#365, KI#367 are provisional merged aliases; do not create four separate KI entries from this root.
- Later F numbers and provisional KI aliases are NOT renumbered in WIP history, because the user/Codex have already
  received those identifiers.
- If reviewed Known-Issue numbering is deliberately compacted during implementation, do that explicitly and record
  the mapping rather than silently reusing provisional aliases.

Canonical repair direction for F037:
Preferred:
1. Do not drive all pre-declaration memory work through `KNOWN_POTENTIAL_ENEMY_OF(aggressor)`.
2. Restore/builder a broad living-major observer loop and keep each branch's own semantic predicate,
   matching the pre-1842 contract.
Or:
3. Split the four operations into dedicated loops/targeted team operations:
   - direct victim -> target-team members directly;
   - all-demand reset -> all other alive major players;
   - tribute reputation -> observers satisfying victim-contact/locus conditions;
   - war-on-friend -> broad observers, then `AI_disapprovesOfDoW`.
This is clearer and less likely to reintroduce cross-population coupling.

WHY OTHER CLUSTERS REMAIN SEPARATE
- F048 vs F049:
  both involve Permanent-Alliance merge completeness, but:
  - F048 = AdvCiv-added SharedWarSuccess omitted on merged team's own direction;
  - F049 = Firaxis-inherited OpenBordersCounter omitted on outsider->merged reverse direction.
  Different state fields, ancestry and merge statements; separate roots.
- F047 vs F050:
  both concern master/vassal coalitions, but:
  - F047 = strategic war-plan key canonicalization/read-write identity;
  - F050 = WarSuccess/military-scale/power population aggregation.
  Different state contracts and ancestry; separate roots.
- F051 vs F052:
  - F051 = practical-1341 legacy joint-war proposal leaves cached price at -1;
  - F052 = inherited BtS TRADE_WAR gate blocks AdvCiv's intended UWAI voluntary-vassal target.
  Different configuration paths and surgical fixes; separate roots.

CURRENT INDEPENDENT ROOT SET AFTER NORMALIZATION
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358 - CANONICAL AI_preDeclareWar population root
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364 - MERGED INTO F037
- F044 / KI#365 - MERGED INTO F037
- F045 / KI#366
- F046 / KI#367 - MERGED INTO F037
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 / KI#372
- F052 / KI#373
- F053 currently UNUSED.

Independent-root count represented by the above:
- 18 surviving independent roots:
  F032-F042 = 11,
  plus F045, F047, F048, F049, F050, F051, F052 = 7.
- F043/F044/F046 are manifestations/aliases, not additional roots.

POST-WIP19 NEGATIVE CONTROL
- Brokered peace does not show the F052 mirror:
  `AI_makePeaceTrade` deliberately rejects vassal peace targets and uses the independent/master team as the
  coalition representative.
- This strengthens F052's classification as a specific war-trade/advc.104o contract mismatch rather than a
  general "all diplomacy should name vassals" assumption.

EXACT RECOVERY CURSOR AFTER C010-WIP20
1. Continue final adversarial/root-normalization pass:
   - try to merge/kill/narrow any remaining findings rather than grow the count.
2. Finish finding-by-finding source/history/caller reread of the 18 independent roots.
3. Complete deep collision search against practical-6239 Album + reviewed Known Issues.
4. Re-run one last independent whole-file sentinel/divisor/state/header sanity scan.
5. Ensure durable synthesis never counts F043/F044/F046 independently and never repeats superseded WIP wording.
6. If not fully satisfied, freeze C010-WIP21.
7. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP20
================================================================================
WORKING CHECKPOINT C010-WIP21 - ROOT-NORMALIZED, CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending root-normalized C010-WIP20.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Independent surviving roots remain the 18-root normalized set:
  F032-F042, F045, F047-F052.
- F043/F044/F046 remain MERGED historical aliases of canonical F037.
- F053 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- Main purpose of WIP21: protect ancestry corrections and current supported-option evidence found after WIP20.

F032 / KI#353 - ANCESTRY CORRECTION
- Earlier WIPs incorrectly described practical 1832 as the birth of the vote-count/team-ID comparison.
- Practical 1832 merely MOVED the already-buggy helper from `WarAndPeaceAgent` into `CvTeamAI` nearly verbatim.
- The deleted pre-move implementation already contains the same incompatible comparison:
  conceptually `if (firstMostVotes == agentId)`.
- The same bug is visible in the initial tracked AdvCiv/WPAI import practical 1253 (`f6ff6a182f`, "Initial commit (cont.)").
- Durable classification:
  present from the initial tracked AdvCiv/WPAI code imported into this ancestry; later moved to CvTeamAI by practical 1832.
- Do NOT say practical 1832 introduced F032.

F035 / KI#356 - UPSTREAM BIRTH CONFIRMED
- The invalid Dagger set-subtraction logic already exists in the large BBAI/K-Mod baseline import
  `KMod_3_2414225e64.diff`.
- The comments themselves say "half the non-dagger teammates" while the implementation subtracts independent
  Dagger totals from FinancialTrouble / GET_BETTER_UNITS totals.
- Later K-Mod / AdvCiv changes largely preserve/reformat it.
- Durable classification: inherited BBAI/K-Mod baseline design defect, not an AdvCiv regression.

F040 / KI#361 - ANCESTRY PRECISION
- Shared Defensive Pact double-counting predates AdvCiv practical 1366.
- Pre-1366 chosen-war code already summed each chosen target's `getDefensivePower(getID())`; overlapping DP
  allies could therefore be counted more than once.
- Practical 1366 added vassal de-duplication and explicitly left the note that DP allies can still be double-counted.
- Durable classification:
  inherited BBAI/K-Mod coalition-summation defect, explicitly acknowledged but left unfixed by AdvCiv 1366.
- Do NOT describe 1366 as the birth of the bug.

F045 / KI#366 - ANCESTRY / SCOPE NUANCE
- Pre-1842 `AI_enmityValue()` explicitly rejected `isVassal(eEnemy)`, i.e. OUR MASTER.
- Practical 1842 removed that local check while moving candidate eligibility into
  `KNOWN_POTENTIAL_ENEMY_OF`, apparently relying on the iterator to enforce master-locus eligibility.
- Current `AI_updateWorstEnemy()` nevertheless evaluates the INCUMBENT directly through `AI_enmityValue`
  before iterating replacement candidates.
- Thus the historically strongest unambiguous regression case is:
  a former worst enemy later becomes OUR MASTER; the old value function would have rejected it, while the
  current incumbent path can bypass the iterator exclusion.
- Current candidate semantics are broader and exclude the whole same-master locus, so an own voluntary vassal
  can likewise become grandfathered after a relationship transition.
- Durable wording should focus on the CURRENT invariant:
  incumbent and replacement worst-enemy eligibility diverge.
- Use "our master" as the strongest historical-control example; do not overstate that pre-1842 code had exactly
  the same broad same-locus policy in every direction.

F048 / KI#369 - FEATURE-LINEAGE ANCHOR
- SharedWarSuccess is visible in the initial tracked AdvCiv/advc.130m implementation practical 1253.
- That implementation explicitly includes SharedWarSuccess in the outsider -> merged-team Permanent Alliance
  merge while the older own-side `shareCounters()` routine omits the new field.
- Durable classification:
  omission present in the initial tracked AdvCiv/advc.130m SharedWarSuccess implementation.
- Since the local tracked history begins around this import, avoid claiming practical 1253 is necessarily the
  first-ever external conception of the feature; it is the first tracked ancestry point and already contains the mismatch.

F050 / KI#371 - K-MOD BIRTH CONFIRMED
- K-Mod practical 116 (`47f56c9c4e...`) rewrote the BBAI war-success ratio to scale across war sizes/eras.
- The rewrite explicitly combines:
  - WarSuccess only from non-vassal/free enemy team IDs,
  - coalition power through `getPower(true)` including vassals.
- Later code preserves that population mismatch.
- Durable classification: K-Mod practical-116 coalition-aggregation design defect.

F051 / KI#372 - SUPPORTED LEGACY CONFIGURATION REACHABILITY CONFIRMED
Current option plumbing makes the legacy joint-war path an intentional supported game configuration:
- `CvGameAI::AI_sortOutUWAIOptions()` maps the normal Custom Game "Aggressive AI (Legacy)" option to
  `setUseLegacyAI(true)`.
- `GlobalDefines_devel.xml` documents:
  - `DISABLE_UWAI`
  - `UWAI_IN_BACKGROUND` (UWAI logs, K-Mod/legacy makes decisions).
- `GlobalDefines_advc.xml` exposes `USE_KMOD_AI_NONAGGRESSIVE`.
- UI text names the option "Aggressive AI (Legacy)".
- Therefore F051 is not developer-only or reachable only through unsupported source hacking.

POST-WIP20 ROOT-NORMALIZATION / COLLISION PROGRESS
- The four practical-1842 AI_preDeclareWar manifestations remain correctly merged under canonical F037.
- Other clusters were re-tested with the "would one surgical fix repair all manifestations?" rule:
  - F048/F049 remain separate: different directional fields, ancestry and statements.
  - F047/F050 remain separate: plan-key identity vs coalition WarSuccess aggregation.
  - F051/F052 remain separate: sentinel pricing regression vs item-validity/feature contract.
- Mechanical repository searches by both function names and semantic concepts have not revealed a reviewed KI
  collision for the normalized roots.
- Final deeper adjacent-KI reread is still required before durable C011.

CURRENT INDEPENDENT ROOT SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358 - canonical AI_preDeclareWar population root
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364 - MERGED alias of F037
- F044 / KI#365 - MERGED alias of F037
- F045 / KI#366
- F046 / KI#367 - MERGED alias of F037
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 / KI#372
- F052 / KI#373
- F053 currently UNUSED.

Independent surviving root count: 18.

EXACT RECOVERY CURSOR AFTER C010-WIP21
1. Continue ancestry/adversarial reread for all 18 independent roots.
2. Finish deep semantic collision inspection of likely adjacent reviewed Known Issues, not only grep-level search.
3. Continue final caller-oriented whole-file pass, especially:
   - raw strategic war-plan reads under F047,
   - master/free-team aggregation under F050,
   - war/peace brokerage symmetry around F051/F052.
4. Run one final independent state/sentinel/divisor/default/assert/header sanity scan.
5. Ensure durable synthesis contains:
   - corrected F032/F040/F045 ancestry wording,
   - F049 Firaxis ancestry,
   - F043/F044/F046 merged aliases only,
   - corrected F041 and F047 consequence wording.
6. If any uncertainty remains, freeze C010-WIP22.
7. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP21
================================================================================
WORKING CHECKPOINT C010-WIP22 - ROOT-NORMALIZED, CvTeamAI.cpp STILL OPEN
================================================================================

STATUS
- Cumulative recovery checkpoint extending root-normalized C010-WIP21.
- Authoritative source snapshot remains practical 6239:
  HEAD cdbf743dc6205df661a65e2b34cd8e09c8a90600
- Durable completed-file checkpoint remains C010 = 9 / 169 COMPLETE.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp` remains OPEN.
- Independent surviving roots remain the 18-root normalized set:
  F032-F042, F045, F047-F052.
- F043/F044/F046 remain MERGED historical aliases of canonical F037.
- F053 currently UNUSED.
- Do NOT promote this checkpoint to C011.
- This checkpoint protects the explicit function-coverage work and the post-WIP21 adversarial reproofs before further closure work.

POST-WIP21 EXPLICIT FUNCTION-COVERAGE PASS
- Parsed the current CvTeamAI implementation into an explicit method inventory rather than relying only on
  "interesting-region" coverage.
- Roughly 154 current CvTeamAI method definitions were inventoried.
- Helpers that had only been implicitly covered through callers are now being individually dispositioned.

Tiny/helper families explicitly reviewed so far:
- constructor / reset / doTurnPre / doTurnPost
- area-target updates
- financial-trouble / military-weight wrappers
- demographic and yield estimators
- primary-area helpers
- attitude / memory wrappers
- tech / map / vassal trade helpers
- trade-denial helpers
- counter / state wrappers
- master-planning helpers
- vote wrappers
- circumnavigation helpers
- no-war probability helpers
- Open Borders counter increment logic
- religious-victory proximity wrappers
- victory-source helpers
- peace trade value helpers
- enemy-danger-by-area helper
- current SAS KI#312 tech-locus helper trio

Specific clean dispositions:
1. `AI_countVSNonMembers`
- Intentionally counts PLAYER-level nonmembers for the diplomatic-victory obstacle metric.
- Its callers use alive civilization player counts too.
- No team/player-domain bug.

2. `AI_isAnyCloseToReligiousVictory`
- Straight any-member wrapper.
- No population mismatch.

3. Circumnavigation / map-trade helper
- Team-wide "can any member train a suitable sea unit?" logic is intentional for team map-trade valuation/refusal.
- No first-member or wrong-team defect.

4. `AI_isMasterPlanningLandWar`
- Inherited BBAI/K-Mod comment "Master has no presence here" can be imprecise when the master's AreaAI is merely NEUTRAL.
- Behavior is a stochastic production heuristic, not a hard zero-city/presence contract.
- Retain as comment/heuristic debt only.

5. `AI_refuseToTalkWarThreshold`
- Member-count divisor has no explicit max(1), but live caller concerns a living at-war team and therefore has at least one alive member.
- No current divide-by-zero.

6. `AI_noWarProbAdjusted`
- Raw attitude enum zero means ATTITUDE_FURIOUS in the current enum domain.
- No enum/sentinel defect.

7. `AI_countEnemyDangerByArea`
- PUF data passes a leader player ID, but PUF_isPotentialEnemy converts the supplied player through TEAMID.
- No TeamTypes/PlayerTypes mismatch.

8. `AI_makePeaceTradeVal`
- Counter/attitude averaging and denominators remain guarded / positive under live contracts.
- No new root.

9. `AI_makeUnwillingToTalk`
- Behavior matches its intended diplomacy-state contract.

10. Current SAS KI#312 tech-locus helpers
- Re-read under practical 6239; no new regression.

No F053 has emerged from the explicit helper-coverage pass so far.

F042 / KI#363 - TRANSITION DIRECTION REFINED
Exact `CvTeam::setVassal` ordering now proves a useful asymmetry:
- BECOMING a vassal:
  cache removal happens BEFORE `m_eMaster` is assigned.
  Target still appears independent, so `AI_updateWarPlanCounts` removes the old count correctly.
- BECOMING FREE:
  attempted cache restoration happens BEFORE `m_eMaster` is cleared.
  Target still appears a vassal, so `AI_updateWarPlanCounts` returns and the restoration is blocked.
- Therefore F042 is a clean one-direction transition-order bug, not a vague general vassal-cache lifecycle problem.

F048 / KI#369 - SEMANTIC REPROOF
- `AI_changeWarSuccess` reports contribution to allies through `AI_reportSharedWarSuccess`.
- `AI_reportSharedWarSuccess` stores directional, accumulating partner-contribution state with decay.
- `CvPlayerAI::AI_getShareWarAttitude` directly consumes
  `kOurTeam.AI_getSharedWarSuccess(eTeam)`.
- `CvTeam::addTeam` reverse outsider->merged-team merge explicitly combines SharedWarSuccess,
  while own-side `shareCounters` omits it.
- Thus the omitted state is unquestionably live relationship history after a Permanent Alliance merge.

F049 / KI#370 - SEMANTIC REPROOF
- `CvPlayerAI::AI_getOpenBordersAttitude` directly consumes:
  `GET_TEAM(getTeam()).AI_getOpenBordersCounter(TEAMID(ePlayer))`.
- own-side `shareCounters` preserves OpenBordersCounter;
  reverse outsider->merged-team `addTeam` merge omits it.
- Firaxis/Civ4 ancestry correction from WIP17 remains authoritative.

F050 / KI#371 - SEMANTIC REPROOF
- `CvTeam::getPower(true)` explicitly sums own members plus all vassal players.
- ordinary WarSuccess remains keyed to actual team IDs.
- `AI_getWarSuccessRating` reads only free enemy master-team WarSuccess while using coalition power.
- The population mismatch is therefore real and current.

F051 / KI#372 - EXACT BIRTH + CALLER REPROOF
- Practical 1341 / `a5724ff581...` exact diff was inspected again.
- Before the practical, the common post-target-selection path computed:
  `AI_declareWarTradeVal(eBestTeam, hirelingTeam)`
  after either UWAI or legacy selection.
- Practical 1341:
  - introduced `iBestTeamPrice = -1`,
  - assigned it only while selecting the UWAI target,
  - replaced the old common price call with that cached variable,
  - left the legacy target-selection branch without any assignment.
- Current `CvPlayerAI::AI_proposeWarTrade` preserves this.
- Supported reachability is explicit:
  - Custom Game "Aggressive AI (Legacy)" -> legacy AI,
  - DISABLE_UWAI,
  - UWAI_IN_BACKGROUND,
  - USE_KMOD_AI_NONAGGRESSIVE.
- The city-for-war fallback suspicion is KILLED:
  `kHireling.AI_cityTradeVal(city, kHireling.getID())` is the correct acquire-value contract when callee==recipient.

F052 / KI#373 - ADVERSARIAL FEATURE-CONTRACT REPROOF
Current game rule:
- `CvPlayer::canTradeItem(... TRADE_WAR ...)` universally rejects target teams with `isAVassal()`.

Current core-war negative control:
- An outside team CAN eventually declare war on a foreign voluntary vassal through
  `canEventuallyDeclareWar` / `canChangeWarPeace(..., true)`.
- Declaration then aligns the target's master/locus normally.
- So the intended voluntary-vassal target is a supported war state; only TRADE_WAR item validity blocks it.

Historical gate lineage:
- Original Civ4: no universal target-vassal TRADE_WAR exclusion.
- Warlords: narrower vassal restriction.
- BtS: universal `!target.isAVassal()` gate.
- AdvCiv inherits the BtS gate.

AdvCiv feature contradiction:
- Initial tracked AdvCiv/UWAI source practical 1253 already says an unpopular VOLUNTARY vassal
  should be a valid joint-war target even if the master would not be.
- The selection rejects only capitulated vassals before testing `canTradeItem`.
- Practical 1263 split legacy/UWAI valuation; UWAI path explicitly says `eWarTeam` can be a voluntary vassal and
  canonicalizes its war-trade value to the target MASTER.
- No later policy statement intentionally disabling this feature has been found.
- Therefore F052 remains a strong inherited BtS gate vs AdvCiv feature-contract mismatch.

Brokered-peace negative control:
- Peace brokerage intentionally uses the independent/master team as coalition representative.
- No mirror defect established.

DEEP COLLISION PASS PROGRESS
Current Known-Issue adjacency reviewed beyond simple grep:
- KI#208 worst-enemy recalculation flag is distinct from F045 incumbent eligibility.
- KI#327 rejected exact diplomatic-vote UI prediction is distinct from F032 private AI count-vs-ID logic.
- KI#108 vassal/surrender deal consistency is distinct from F051/F052 joint-war brokerage/item validity.
- KI#210 vote first-contact iterator issue is distinct from F046 manifestation of canonical F037.
- KI#309 religion-block power double-count is distinct from F040 coalition Defensive Pact double-count.
- KI#312 tech-locus repair is distinct from F047 war-plan canonicalization.
- KI#343 AP/voter-stagger issue is distinct from F032/F037 vote/diplomacy logic.
- No same-root reviewed KI collision found so far for F048-F052.

CURRENT INDEPENDENT ROOT SET
- F032 / KI#353
- F033 / KI#354
- F034 / KI#355
- F035 / KI#356
- F036 / KI#357
- F037 / KI#358 - canonical AI_preDeclareWar population root
- F038 / KI#359
- F039 / KI#360
- F040 / KI#361
- F041 / KI#362
- F042 / KI#363
- F043 / KI#364 - MERGED alias of F037
- F044 / KI#365 - MERGED alias of F037
- F045 / KI#366
- F046 / KI#367 - MERGED alias of F037
- F047 / KI#368
- F048 / KI#369
- F049 / KI#370
- F050 / KI#371
- F051 / KI#372
- F052 / KI#373
- F053 currently UNUSED.

Independent surviving root count: 18.

EXACT RECOVERY CURSOR AFTER C010-WIP22
1. Complete explicit function-name coverage:
   identify any CvTeamAI method still not individually or caller-wise dispositioned, especially tiny state getters/setters,
   vote helpers, trade wrappers, area/enemy helpers and war-plan utilities.
2. Continue caller-oriented raw strategic-war-plan audit under F047;
   fold same invariant into F047 rather than creating duplicate roots.
3. Continue F052 descendant/policy search for any explicit intentional statement disabling voluntary-vassal war trades;
   none found so far.
4. Finish ancestry/adversarial reread for all 18 independent roots.
5. Finish deep adjacent-KI semantic collision pass.
6. Run one final independent state/sentinel/divisor/default/assert/header sanity scan.
7. If uncertainty remains, freeze C010-WIP23.
8. Only when genuinely exhausted:
   - mark `CvTeamAI.cpp` COMPLETE,
   - total becomes 10 / 169,
   - freeze durable C011,
   - byte-validate reconstruction,
   - open file 011 `CvGameCoreDLL/SASGameRecordLog.cpp`.

END WORKING CHECKPOINT C010-WIP22

================================================================================
DURABLE CHECKPOINT C011 - FILE 010 CvGameCoreDLL/CvTeamAI.cpp COMPLETE
================================================================================

CURRENT SOURCE AUTHORITY
- Authoritative AdvCiv-SAS light-source snapshot for this closure:
  branch main
  practical 6244
  HEAD acf6f3baea1fb700645fcb5133e130b4e61e2316
- `CvTeamAI.cpp` current length: 6,916 lines.
- Local comparison material used only: current AdvCiv-SAS light source, AdvCiv 1.14 archive, Civ4CE source, and bundled local commit-diff history. No web lookup was needed.
- Commits practical 6240-6244 between the WIP22 cursor and this closure were reread. Their CvTeamAI-family changes are the reviewed KI#353-KI#363 repairs plus the KI#358 alias reconciliation; no unrelated intervening change invalidates the remaining roots.

DURABLE PROGRESS
- Files complete: 10 / 169.
- Files open after this handoff: 0 / 169.
- File 010 `CvGameCoreDLL/CvTeamAI.cpp`: COMPLETE for the file-oriented audit pass.
- Next queue entry when work resumes: file 011 `CvGameCoreDLL/SASGameRecordLog.cpp` (6,343 lines).
- Normalized roots attributable to the CvTeamAI-oriented pass: 18 independent roots total.
  - 11 are already reviewed/promoted/fixed in current source as F032-F042 / KI#353-KI#363.
  - 7 remain current pending candidates: F045, F047, F048, F049, F050, F051, F052.
- F043/F044/F046 remain merged historical aliases of F037 and do not increase the independent-root count.
- F053 remains UNUSED. The final coverage/sanity passes did not justify manufacturing an additional root.

EXPLICIT WHOLE-FILE COVERAGE CLOSURE
- A comment-stripped parser identifies 172 live out-of-line CvTeamAI definition bodies representing 169 unique method names; the only overload sets are `AI_isHasPathToEnemyCity`, `AI_isTerritoryAccessible`, and `AI_mayAttack`.
- The earlier rough WIP inventory was slightly inflated because it counted the three intentionally commented-out obsolete bodies `AI_calculateCapitalProximity`, `AI_calculatePlotWarValue`, and `AI_calculateBonusWarValue`. Their declarations remain in the header, but the source explicitly records that K-Mod obsoleted them and that their bodies were deleted in 2019. They are not live missing-definition defects because they are unused.
- `CvTeamAI.h` was cross-checked against the implementation. The remaining header-only AI methods are straightforward inline state/accessor helpers (`AI_getWarPlan`, counters, cached counts/flags, `AI_getTeam`, etc.) or the three obsolete declarations above.
- Reset/read/write symmetry was reread for all actual CvTeamAI state: war-plan counters, relation counters, WarSuccess, SharedWarSuccess, enemy trade/grant state, war-plan cache and raw plans, worst enemy, lonely flag, nuke-explosion memory, strength memory, and UWAI payload. No new serialization/reset asymmetry root was established.
- Tiny wrappers and helper families that were previously covered only implicitly were explicitly dispositioned, including initialization/turn hooks, demographic/yield estimators, area helpers, attitude/memory accessors, election/vote wrappers, tech-locus helpers, map/vassal/trade wrappers, relation counters, personality-average accessors, enemy-area helpers, peace/war valuation wrappers, no-war rolls, circumnavigation, master-planning helpers, religious-victory wrappers, plot defense, train-expectation, forgiveness/liberation, tech-monopoly value, and water-area relevance.
- A final independent sentinel/divisor/default/assert sweep did not establish another current defect. Apparent risky divisions are guarded directly, constructed from positive values, or protected by a live-team/game-data contract. Apparent enum/sentinel concerns similarly resolve to valid current domains.

CURRENT ROOT NORMALIZATION
- F032 / KI#353 - fixed: diplomatic-vote counter-candidate vote count vs team ID.
- F033 / KI#354 - fixed: war planning inspected our cities instead of the proposed target's cities.
- F034 / KI#355 - fixed: rival air-power averaging counted each rival once per qualifying air class.
- F035 / KI#356 - fixed: Dagger teammates were subtracted from unrelated economic/upgrade populations.
- F036 / KI#357 - fixed: war-commitment valuation passed accumulated AI cost as raw war weariness.
- F037 / KI#358 - fixed canonical root: practical-1842 `AI_preDeclareWar` population substitution.
- F038 / KI#359 - fixed: alternative-master capitulation minimum-war-duration comparison reversed.
- F039 / KI#360 - fixed: capitulated teams affected only the known-tech denominator.
- F040 / KI#361 - fixed: shared Defensive Pact coalition members could be counted repeatedly.
- F041 / KI#362 - fixed: relationship ceilings capped each random increment rather than accumulated state.
- F042 / KI#363 - fixed: freeing a vassal attempted cached war-plan restoration before independence was current.
- F043 / provisional KI#364 - MERGED alias of F037; reserved only as audit history.
- F044 / provisional KI#365 - MERGED alias of F037; reserved only as audit history.
- F045 / provisional KI#366 - PENDING current candidate; incumbent worst-enemy eligibility can diverge from replacement-candidate eligibility.
- F046 / provisional KI#367 - MERGED alias of F037; reserved only as audit history.
- F047 / provisional KI#368 - PENDING current candidate; strategic/preparation war-plan master/vassal key invariant is only partially canonicalized.
- F048 / provisional KI#369 - PENDING current candidate; Permanent Alliance own-side merge omits absorbed-team SharedWarSuccess toward third parties.
- F049 / provisional KI#370 - PENDING current candidate; Permanent Alliance outsider-side merge omits OpenBordersCounter toward the absorbed team.
- F050 / provisional KI#371 - PENDING current candidate; war-success rating combines direct-team success/unit scale with coalition power that includes vassals.
- F051 / provisional KI#372 - PENDING current candidate; supported legacy `AI_proposeWarTrade` selection leaves cached best-team price at -1.
- F052 / provisional KI#373 - PENDING current candidate; inherited BtS TRADE_WAR vassal gate blocks AdvCiv/UWAI's explicit voluntary-vassal joint-war target feature.
- F053 - UNUSED.

PENDING ROOT REPROOF - F045 / provisional KI#366
Incumbent worst-enemy eligibility can be grandfathered after a master/vassal-locus transition.
- `AI_updateWorstEnemy` seeds the contest with `AI_enmityValue(m_eWorstEnemy)` before entering the candidate iterator.
- Replacement candidates come from `TeamIter<MAJOR_CIV,KNOWN_POTENTIAL_ENEMY_OF>`, whose predicate requires contact and `!isSameMaster(...)`.
- `AI_enmityValue` itself does not impose that same master/locus eligibility rule.
- Therefore the incumbent can remain eligible after it ceases to satisfy the population from which every replacement is drawn; inertia then makes the stale incumbent slightly harder to dislodge.
- Historical control strengthens the root: before the practical-1842 iterator refactor, the local worst-enemy valuation path explicitly rejected the relevant vassal/master relationship. Practical 1842 moved candidate eligibility into the iterator but left the incumbent pre-evaluation outside it.
- KI#208 is distinct: KI#208 fixes whether a second worst-enemy recomputation is requested after trade-memory decay; it does not restore incumbent/candidate population equivalence.
Repair direction:
- Evaluate the incumbent only if it still satisfies the same current worst-enemy eligibility predicate as replacement candidates, or centralize that eligibility into `AI_enmityValue`/a shared helper so incumbent and challenger cannot diverge.

PENDING ROOT REPROOF - F047 / provisional KI#368
Strategic/preparation war-plan master/vassal key identity is incomplete; this is NOT a claim that every raw `AI_getWarPlan` access is wrong.
- The raw accessor is intentionally raw: `AI_getWarPlan(eIndex)` simply reads `m_aeWarPlan[eIndex]`.
- Several strategic helpers explicitly canonicalize a requested defender to its master before reading the plan: notably `AI_isChosenWar`, `AI_isSneakAttackPreparing`, and `AI_mayAttack`.
- `AI_updateWarPlanCounts` deliberately excludes target vassals from cached strategic plan counts.
- However `AI_setWarPlan` stores exactly the raw target supplied and propagates that same raw target to our AI vassals; it does not canonicalize the target locus.
- Supported legacy `AI_doWar` can evaluate a voluntary vassal as the preferred target while separately reasoning about its master, then call `AI_setWarPlan(eBestTarget, ...)` on that raw vassal. The resulting preparation plan is stored on a key that strategic master-normalizing helpers and cached-plan counts do not recognize.
- The opposite inconsistency also survives: when preparation is correctly represented on the master key, several raw per-team consumers can omit the master's vassal coalition during strategic preparation. Current examples include area-strategy target scans, path-to-enemy-city gating, rival air power, enemy power/city/population-by-area helpers, and tech-monopoly war-plan context.
- Historical control: AdvCiv practical 1366 explicitly changed chosen-war/sneak-preparation logic so that a war plan against a master implies the corresponding strategic plan against its vassals. That makes the current mixed raw/master treatment an architectural inconsistency rather than a new policy proposal.
- Active-war negative control matters: once war has actually begun, real per-team active-war state can legitimately be raw. Therefore a global blind replacement of all `AI_getWarPlan(eTeam)` calls with `AI_getWarPlan(master)` would be unsafe and could erase meaningful actual-team state.
Repair direction:
- Define one canonical identity for undeclared strategic/preparation plans, most naturally the target master/locus representative.
- Make legacy target selection/setter use obey that identity.
- Make preparation-phase coalition consumers expand the target locus exactly once when they need actual cities/units/power.
- Preserve raw per-team active-war plans after declaration and keep tactical subtarget/front selection separate from strategic plan identity.

PENDING ROOT REPROOF - F048 / provisional KI#369
Permanent Alliance own-side merge omits absorbed-team SharedWarSuccess toward third parties.
- `AI_changeWarSuccess` feeds `AI_reportSharedWarSuccess`, which stores directional, accumulating relationship state about a war ally's contribution.
- `CvPlayerAI::AI_getShareWarAttitude` consumes our team's `AI_getSharedWarSuccess(eOtherTeam)` directly.
- In `CvTeam::addTeam`, the reverse outsider->newly merged team direction explicitly combines each outsider's SharedWarSuccess toward both pre-merge teams.
- In the own-side `CvTeam::shareCounters(eTeam)` direction, neighboring relationship histories are merged, including WarSuccess, but SharedWarSuccess is absent.
- Hence one directional half of the same live relationship state survives the Permanent Alliance while the absorbed team's own SharedWarSuccess toward third parties is discarded.
- The SharedWarSuccess feature is already present in the earliest tracked AdvCiv/UWAI source lineage, so this is not a recent SAS-only expectation.
Repair direction:
- Merge own-side SharedWarSuccess under the same relationship-history policy chosen for the corresponding directional state, taking care not to double count accumulated contribution.

PENDING ROOT REPROOF - F049 / provisional KI#370
Permanent Alliance outsider-side merge omits OpenBordersCounter toward the absorbed team.
- Own-side `CvTeam::shareCounters` explicitly preserves the larger OpenBordersCounter when combining the two allied teams' histories toward outsiders.
- The reverse outsider->merged-team loop in `CvTeam::addTeam` combines AtWar, AtPeace, HasMet, WarWeariness, DefensivePact, ShareWar, WarSuccess, SharedWarSuccess, peace-turns, enemy-trade/grant state and espionage, but omits OpenBordersCounter.
- `CvPlayerAI::AI_getOpenBordersAttitude` directly consumes the team OpenBordersCounter, so the omitted directional state has live diplomacy consequences after a Permanent Alliance.
- The asymmetry is inherited through Firaxis/K-Mod lineage; it is not caused by the recent SAS counter-cap repair.
Repair direction:
- Add the outsider-side OpenBordersCounter merge using the same team-size-weighted policy as the neighboring directional counters in `addTeam`.

PENDING ROOT REPROOF - F050 / provisional KI#371
`AI_getWarSuccessRating` mixes incompatible team and coalition populations.
- Its success scale is based on the caller team's own members' military-unit count only.
- Direct WarSuccess is keyed to the actual free enemy master-team IDs in the loop.
- Yet both `iThisTeamPower = getPower(true)` and `iOtherTeamPower = enemy.getPower(true)` include vassal power.
- Thus direct-team WarSuccess and an own-team unit-count scale are weighted by broader coalition power; a large or successful vassal can change the rating without supplying corresponding success/unit-scale state to the same population.
- The mismatch originates in inherited K-Mod war-success-rating logic (introduced around practical 116 in the local ancestry), not in KI#361's separate duplicate-coalition enumeration root.
Repair direction:
- Choose a coherent population: either evaluate direct team success against direct team power/unit scale, or explicitly aggregate the same master/vassal coalition's success and scale alongside coalition power. Avoid fixing only one operand.

PENDING ROOT REPROOF - F051 / provisional KI#372
Supported legacy `CvPlayerAI::AI_proposeWarTrade` can enter deal balancing with a price of -1.
- Current code initializes `iBestTeamPrice = -1`.
- The UWAI target-selection branch assigns that variable when it selects a target.
- The supported legacy target-selection branch can select `eBestTarget` and update `iBestValue`, but never assigns `iBestTeamPrice`.
- After either branch, common deal construction initializes `iWeReceive = iBestTeamPrice`; therefore a valid legacy selection starts balancing from -1 rather than the selected target's declaration price.
- Exact ancestry identifies the regression at AdvCiv practical 1341: the old common post-selection `AI_declareWarTradeVal(...)` call was replaced by the cached UWAI price, but the legacy branch was not given a corresponding assignment.
- Reachability is supported rather than theoretical: current configuration paths include Legacy Aggressive AI and the documented UWAI disable/background/nonaggressive switches.
- A suspected city-for-war fallback argument problem was adversarially checked and rejected; `kHireling.AI_cityTradeVal(city, kHireling.getID())` uses the correct acquiring-player valuation contract.
Repair direction:
- On the legacy selection path, compute/cache the selected target's war price before common deal balancing, or restore a common post-selection price computation with the correct legacy/UWAI semantics.

PENDING ROOT REPROOF - F052 / provisional KI#373
Inherited BtS TRADE_WAR item validity blocks an explicit AdvCiv/UWAI voluntary-vassal joint-war feature.
- Current `CvPlayer::canTradeItem(... TRADE_WAR ...)` rejects every target for which `GET_TEAM(eTarget).isAVassal()` is true.
- Current UWAI `AI_proposeWarTrade` intentionally distinguishes capitulated from voluntary vassals and states that an unpopular voluntary vassal should be a valid target even when its master would not be; it rejects only capitulated vassals before calling `canTradeItem`.
- Current `CvTeamAI::AI_declareWarTradeVal` and `AI_declareWarTrade` explicitly master-normalize a voluntary-vassal target for the UWAI valuation/denial contract.
- Core war rules provide the negative control: a foreign voluntary vassal is a supported eventual declaration target and declaration then aligns the master/vassal locus. The blocker is the trade-item gate, not impossibility of the war state.
- Ancestry is coherent:
  - original Civ4 did not have the later universal target-vassal TRADE_WAR exclusion;
  - Warlords used a narrower restriction;
  - BtS introduced/inherited the universal `!target.isAVassal()` style gate;
  - AdvCiv practical 1253 already carries the UWAI comment/selection intent for voluntary vassals;
  - practical 1263 explicitly master-normalizes the UWAI war-trade valuation for such a target.
- The descendant/policy search found no later AdvCiv statement intentionally retracting the voluntary-vassal feature.
- Important repair hazard: simply relaxing the gate for every AI mode would expose the legacy valuation path, which values the raw target and can underprice the wider master coalition. This root should therefore be repaired together with mode-appropriate valuation/canonicalization rather than by deleting one condition blindly.
Repair direction:
- Permit the explicit voluntary-vassal joint-war target where the UWAI contract supports it, while retaining capitulated-vassal exclusions and ensuring the price/denial calculation represents the target master/locus.
- If legacy mode is also meant to support the feature, repair its coalition valuation first or at the same time.

F047 CALLER-ORIENTED WAR-PLAN AUDIT - CLOSING SCOPE
- The final raw-war-plan callsite pass confirms that F047 should stay one architectural root rather than spawn sibling findings for every consumer.
- Raw reads in actual-war/UWAI tactical state are often legitimate and must not be mechanically rewritten.
- The defect boundary is the undeclared strategic/preparation identity: code that asks "are we strategically preparing to attack this political locus?" already tends to normalize to the master, while code that consumes the locus's cities/power/air/path information often iterates actual member teams.
- That producer/consumer split explains both failure directions under one invariant and is the appropriate unit for later repair/review.

F052 POLICY / DESCENDANT SEARCH - CLOSED
- Local AdvCiv history was searched around the voluntary-vassal joint-war comments and master-normalized valuation.
- Those comments and semantics persist through descendants; no explicit reversal was found.
- Base AdvCiv 1.14 still contains the universal BtS-style `TRADE_WAR` vassal gate, so current SAS inherited both sides of the contradiction rather than introducing it locally.
- The candidate therefore remains strong enough for independent KI review rather than being downgraded to an uncertain policy suggestion.

DEEP KNOWN-ISSUE COLLISION PASS - CLOSED
- KI#208 vs F045: distinct worst-enemy recomputation flag vs incumbent eligibility population.
- KI#312 vs F047: distinct internal technology-supply locus contract vs war-plan target-locus identity.
- KI#309/KI#361 vs F050: distinct religion/Defensive-Pact coalition enumeration roots vs war-success population/scale mismatch.
- KI#108 vs F051/F052: distinct broader vassal/surrender deal consistency vs joint-war price initialization and item-validity contract.
- KI#327 vs F032: rejected exact UI vote prediction vs private AI counter-candidate bookkeeping defect.
- KI#210/KI#343 vs F037 aliases: distinct vote/contact/AP timing roots vs practical-1842 preDeclareWar population substitution.
- Current reviewed Known Issues through KI#363 were reread semantically; no reviewed same-root collision was found for F045 or F047-F052.

FINAL SANITY / NEGATIVE-CONTROL NOTES
- `AI_doTurnPost` loneliness arithmetic is not off by one: `getHasMetCivCount` excludes self and the `+1` condition accounts for that population difference.
- `AI_estimateDemographic` and yield estimators retain appropriate no-turn/range/data contracts.
- `AI_countVSNonMembers` is intentionally player-level and its callers use player-level diplomatic-victory populations.
- `AI_countEnemyDangerByArea` passes a leader player ID to a PUF that converts it through TEAMID; no TeamTypes/PlayerTypes mismatch.
- `AI_noWarProbAdjusted` attitude enum value zero is valid ATTITUDE_FURIOUS, not a sentinel.
- `AI_refuseToTalkWarThreshold`, `AI_warDiplomacyCost`, personality averages, Open Borders increment logic, Shared War distress, vote-victory counting and the reviewed war-planning divisions have guarded or live-positive denominators.
- `AI_doWar`'s power-threshold division is not reachable at a zero threshold because the preceding comparison cannot hold for a nonnegative enemy-power percentage.
- Nuke-explosion transient memory across a Permanent Alliance, `AI_isPushover` heuristic asymmetry, air-power default-unit approximation, and stochastic master-planning behavior were reconsidered but remain unproven/tuning/comment concerns, not promoted roots.

ANCESTRY / CURRENT-TREE FINAL DISPOSITION
- All 18 normalized roots were reread against current practical 6244 at least at their defining loci; F032-F042 are visibly repaired under KI#353-KI#363.
- F045 and F047-F052 still reproduce in current source and remain pending independent promotion/repair review.
- The seven pending roots are deliberately not written into `README_Known_Issues.md` by this audit checkpoint; the repository ledger remains the independently reviewed/promoted authority.
- No evidence justifies reopening a fixed KI#353-KI#363 root or splitting the merged F037 aliases.
- No F053 was established.

NEXT RECOVERY CURSOR AFTER DURABLE C011
1. Treat file 010 `CvTeamAI.cpp` as COMPLETE for audit coverage.
2. If the user/Codex wants implementation review first, promote/reject/merge F045 and F047-F052 individually before editing the repository; their repair hazards and negative controls are recorded above.
3. Otherwise continue the longest-file queue at file 011 `CvGameCoreDLL/SASGameRecordLog.cpp` (6,343 lines), using practical 6244 / HEAD acf6f3baea as the current source identity unless a newer light-source ZIP is supplied.
4. Keep F053 as the next unused stable audit finding number.

END DURABLE CHECKPOINT C011

================================================================================
C011-WIP01 — FILE 011 OPEN: SASGameRecordLog.cpp — FIRST DURABLE CHECKPOINT
================================================================================

SOURCE BASELINE
---------------
Authoritative working tree supplied by user:
  AdvCiv-SAS practical 6244 / HEAD acf6f3baea1fb700645fcb5133e130b4e61e2316
Primary file:
  CvGameCoreDLL/SASGameRecordLog.cpp (6343 lines)
Comparison material available locally:
  AdvCiv 1.14
  Civ4CE-main
Durable completed-file count remains 10 / 169. File 011 is OPEN.

AUDIT STATUS AT THIS CHECKPOINT
-------------------------------
The first broad structural pass now covers the logger/session/reset layer, buffered turn-flow state,
full-map revelation/resource hooks, geography/known-area/BFC machinery, ASCII-map helpers and
palette/resampling safety, initial context, substantial global/team/player snapshot machinery,
city happiness/health source reconstruction, the espionage snapshot, and nearby-enemy diagnostics.
Caller and engine-contract greps have been used where a helper's meaning depends on another class.
This is NOT a file-complete checkpoint; later player/city/unit/event-hook and final inventory passes
still remain.

NEW NORMALIZED ROOTS
--------------------

F053 / provisional KI#374 — nearby-enemy diagnostics pass the CENTER plot to CvUnit::isEnemy
instead of the nearby unit's ACTUAL plot; the same defect is duplicated in CvUnitAI diagnostics.

Primary evidence:
  SASGameRecordLog.cpp, countSASGameRecordVisibleEnemiesNearPlot (around current line 5419):

    for each pLoopPlot around kCenter
      for each pLoopUnit on pLoopPlot
        if (pLoopUnit == NULL ||
            !pLoopUnit->isEnemy(eTeam, kCenter) ||
            pLoopUnit->isInvisible(eTeam, false))
          continue;

The unit being classified is on pLoopPlot, but combat hostility is queried as though it occupied
kCenter. This matters because CvUnit::isEnemy(TeamTypes, CvPlot const&) deliberately routes through
getCombatOwner(eTeam, kPlot), and the supplied plot is semantically significant. Always-hostile /
hidden-nationality handling can change with the plot; in particular CvUnit::isAlwaysHostile(kPlot)
returns false on a friendly/base plot so that always-hostile units cannot attack into friendly cities.
Consequently a nearby unit can be classified according to the center city's diplomatic/combat-owner
context rather than its own location.

Affected GameRecord fields include:
  visibleEnemiesR2
  visibleCombatEnemiesR2
  nearestEnemyPlayer
  nearestEnemyUnit
  nearestEnemyDist

Independent duplicate:
  CvUnitAI.cpp contains SAS_countVisibleEnemiesNearPlot with the same
  pLoopUnit->isEnemy(eTeam, kCenter) call, used by SAS_logSettlerParking. Thus the BBAI/SAS settler
  diagnostic can be wrong for the same reason. This appears diagnostic-only, not a core AI decision.

Ancestry:
  Introduced in practical 6026 (commit 876158dff2..., "Add BBAI and SASGameSummary AI Settler
  security information") with the wrong plot argument already present; later carried into the
  GameRecord rename/expansion.

Repair direction:
  In both helpers, evaluate hostility at the loop/unit plot, e.g.
    pLoopUnit->isEnemy(eTeam, *pLoopPlot)
  subject to the local pointer/reference conventions. Do not globally replace other isEnemy calls:
  this root is specifically the nearby-unit helper using the wrong spatial context.

Impact:
  Diagnostic/logging accuracy; especially hidden-nationality/always-hostile edge cases. No evidence
  that this helper currently alters gameplay decisions.

Confidence: HIGH.


F054 / provisional KI#375 — the central structured GameRecord formatter silently truncates rows at
2047 bytes, while multiple machine-readable rows contain unbounded/large dynamic inventories.

Primary evidence:
  logSASGameRecordFormattedLine uses a single fixed char buf[2048], calls _vsnprintf with
  sizeof(buf)-1, then forcibly writes the final NUL. The KI#161.2-style hardening prevents an
  unterminated buffer/crash after MSVC truncation, but it cannot preserve content beyond the fixed
  capacity.

Why this is a data-integrity bug here rather than merely a cosmetic logger limitation:
  GameRecord rows are intended to be parsed as structured records, and several rows append lists
  whose size scales with mod/game state, including at least:
    GAME_RECORD_CITY_BUILDINGS ... buildings=%s
    GAME_RECORD_UNIT_COMPOSITION ... unitTypes=%s unitAI=%s
    GAME_RECORD_UNIT_PROMOTIONS ...
    city/unit composition and other type inventories
  A truncated row can therefore lose closing fields or cut a type/count token in half without an
  explicit truncation marker.

Current-tree scale check:
  166 BuildingInfos exist in the supplied SAS XML. Building type names alone average roughly 22
  characters and can reach roughly the high 30s. A mature/modded city inventory can therefore exceed
  the entire formatter capacity independently of the fixed fields surrounding it. Unit and promotion
  type inventories are also large enough to make the ceiling relevant.

Strong negative/control:
  Some intentionally huge coordinate/position rows are already manually chunked at roughly 1500
  bytes before logging. That shows the 2048-byte sink ceiling is real and was consciously avoided in
  selected producers, but the central formatter offers no equivalent protection to the many other
  dynamically growing records.

Bundled/sample logs checked so far do not prove a present truncation event (largest observed line in
one supplied/example log was below the ceiling); the defect is the reachable silent-loss condition,
not a claim that every normal game already hits it.

Ancestry:
  GameRecord inherited the fixed-buffer pattern when introduced around practical 6022; the earlier
  KI#161.2 hardening addressed NUL termination/safety, not structured-row completeness.

Repair direction:
  Prefer a central solution that preserves the complete formatted record (grow/retry or an equivalent
  old-MSVC-compatible formatting strategy). If that is awkward under the VC7.1-era va_list runtime,
  explicitly chunk all variable inventories and/or detect truncation and emit continuation records.
  Merely increasing 2048 to another arbitrary fixed number reduces but does not remove the invariant.

Impact:
  Structured log corruption/loss in sufficiently large rows; parsers can receive syntactically or
  semantically incomplete records.

Confidence: HIGH for the reachable bug; MEDIUM-HIGH for how often stock-sized games trigger it.


F055 / provisional KI#376 — ESPIONAGE stationarySpies/maxFortifyTurns are documented as
"stationary cost-reduction preparation" but count units whose fortify turns cannot represent an
active espionage discount, including home-idle spies and Great Spies.

Primary evidence:
  logSASGameRecordEspionage accepts a unit when either:
    pLoopUnit->isSpy()
    OR AI_getUnitAIType() == UNITAI_GREAT_SPY
  It then increments stationarySpies/maxFortifyTurns solely from getFortifyTurns() before checking
  foreign deployment/territory.

Engine semantics:
  CvUnit::doTurn increments FortifyTurns for every unmoved, non-cargo unit under the normal fortify
  mechanism; it is not an espionage-specific preparation counter.
  CvPlayer::getEspionageMissionCostModifier applies the stationary spy discount only when an actual
  pSpyUnit is supplied and uses that unit's current fortify turns at the mission location. A spy that
  accumulated fortify turns while sitting at home must move to reach a foreign target, which resets
  the preparation; those old home fortify turns therefore do not describe a usable mission discount.

Great Spy contradiction:
  Current UNIT_GREAT_SPY XML has bSpy=0 and its default AI is UNITAI_GREAT_SPY. The logger explicitly
  includes such Great-Spy-role units in stationarySpies/maxFortifyTurns even though they are not
  ordinary spy mission units receiving the normal stationary mission-cost discount.

This conflicts with the function's own explanatory contract, which describes the fields as
"stationary cost-reduction preparation," not merely "espionage-themed units that have stood still."

Ancestry:
  Introduced in practical 6046 (commit 559ee966cb..., espionage/army/GP diagnostic expansion) with
  this interpretation already present.

Repair direction:
  If the metric is meant to measure mission-discount preparation, restrict it to real isSpy() units
  that are stationary in a relevant foreign target context; continue reporting Great Spies through
  their separate Great-Spy/deployment metrics. Alternatively rename/re-document the metric if the
  desired meaning is only generic stationary espionage-role units, but that would abandon the current
  cost-reduction claim.

Impact:
  Diagnostic/logging accuracy; can make a player look as though it has prepared discounted espionage
  missions when it has only idle spies at home and/or stationary Great Spies.

Confidence: HIGH.


CHALLENGED BUT NOT PROMOTED
---------------------------

1. Shared player previous-state bValid / delta ordering:
   Checked. Level-2 player subrows all consume/update their own previous values before the common
   kPrevious.bValid flag is set at the end of logSASGameRecordPlayerSnapshot. No "first helper makes
   later helpers believe old state exists" bug in the fixed-level snapshot path.

2. City happiness / unhappiness / health source decomposition:
   Checked directly against CvCity::happyLevel, unhappyLevel, goodHealth and badHealth. The GameRecord
   helpers mirror the engine components essentially term-for-term. In particular, adding city and
   player ExtraHappiness/ExtraHealth is correct: their getters expose distinct stored fields.

3. globalWarming anger * 10:
   Not a units bug. CvCity::unhappyLevel contains K-Mod's explicit explanation: GwPercentAnger is a
   conventional percentage while Civ4's other anger-percent values are parts per thousand, so *10 is
   the intended conversion. GameRecord matches engine behavior.

4. tradeRoutes vs domesticTradeRoutes + foreignTradeRoutes:
   The city summary's tradeRoutes value comes from CvCity::getTradeRoutes(), i.e. route-slot capacity,
   while domestic/foreign counts enumerate populated getTradeCity slots. Therefore the three values
   need not add up in disorder/plunder/no-partner situations. This is potentially ambiguous naming,
   but "trade routes" is also the engine's established name for the capacity getter; no defect is
   promoted without a stronger parser/documentation contract.

5. revelation/resource hooks and known-area/BFC accounting:
   No independent defect found in the inspected paths. Full-map revelation callers tightly bracket
   the special state; ordinary plot reveal is invoked on the false->true transition; the old area-ID
   varargs issue in the bonus hook is already repaired in current source.

6. ASCII map arithmetic/palette/resampling:
   No root found in the inspected pass. Candidate divisors are guarded; palette validation accounts
   for derived aliases/player symbols; large map payloads are deliberately chunked in key producers.

7. direct literal log-format arity:
   Mechanical audit of 167 literal-format logSASGameRecord calls found zero format-specifier versus
   argument-count mismatches. This does not prove type correctness for every vararg, but it closes a
   broad class of accidental missing/extra arguments.

8. duplicate #include "CvInfo_Symbol.h":
   Harmless cleanup only; no normalized issue.


NEXT CURSOR
-----------
Continue file 011 without renumbering or reopening the above roots:
  - finish player bonus/economic/posture/worker/settler/city/unit snapshot semantics and engine
    reconciliation where labels reconstruct gameplay quantities;
  - finish later event hooks (combat/air/production/city acquisition/GP/victory/load) and caller timing;
  - explicit definition/header/caller disposition inventory;
  - final sentinel/divisor/index/state-lifetime/varargs-type sweep;
  - ancestry/KI collision check for any additional root;
  - only close SASGameRecordLog.cpp when the whole-file inventory is defensible.

F056 is presently UNUSED.


===============================================================================
C011-WIP02 -- FILE 011 SASGameRecordLog.cpp CONTINUATION CHECKPOINT
===============================================================================

STATUS
------
Source baseline remains practical 6244 / HEAD acf6f3baea... from the supplied
AdvCiv-SAS_light_source_20260824T132346.zip.

File 011 remains OPEN. Durable completed-file count remains 10/169.
This checkpoint intentionally freezes progress before the final closure tail.

Previously frozen roots F053-F055 remain live. The continuation work since
C011-WIP01 adds F056-F060. F061 remains UNUSED.

CURRENT NORMALIZED ROOT LEDGER
------------------------------

F053 / provisional KI#374 -- nearby-enemy diagnostics pass the wrong plot context to CvUnit::isEnemy

The helper that counts nearby visible enemies iterates a unit on pLoopPlot but
calls pLoopUnit->isEnemy(eTeam, kCenter), where kCenter is the city/settler
center plot rather than the unit's actual plot. CvUnit::isEnemy uses the supplied
plot to determine combat ownership / always-hostile behavior. Hidden-nationality
or otherwise always-hostile units can therefore be classified using the wrong
territorial context.

Affected diagnostic outputs include visibleEnemiesR2, visibleCombatEnemiesR2 and
nearestEnemy*; the same helper shape also exists in the CvUnitAI.cpp settler
logging path. This is a telemetry/diagnostic correctness issue, not gameplay AI
behavior by itself.

Confidence: HIGH.


F054 / provisional KI#375 -- fixed 2048-byte structured-row formatting ceiling can silently truncate rows

All structured rows are formatted through a fixed 2048-byte temporary buffer.
The KI#161.2 hardening guarantees termination / prevents unsafe overflow, but it
does not preserve data beyond the buffer; oversized rows are silently truncated.

This is materially relevant because several GameRecord rows intentionally contain
variable-length machine-readable inventories (CITY_BUILDINGS, unit type/AI
composition, promotion inventories, etc.). The supplied sample's longest observed
row is only about 1.64 KB, so that sample does NOT demonstrate truncation.
However a legality-aware upper-bound check makes the structural defect reachable:
SAS has 123 building classes, and a legal mature city can still carry roughly 117
distinct building entries after civilization UB mappings and ordinary national-
wonder constraints. The building type/count payload alone is roughly 2.8 KB for
several civs before fixed row fields are added.

The codebase itself contains a useful negative control: intentionally large map /
chunk payloads elsewhere are split around ~1500 bytes, showing the 2048 sink is a
real design ceiling rather than an unlimited formatter.

Confidence: HIGH for reachable structured-log truncation; NOT observed in the
provided sample run.


F055 / provisional KI#376 -- stationary-spy preparation metric counts states that do not represent usable mission discount

The espionage diagnostic describes stationarySpies/maxFortifyTurns as stationary
cost-reduction preparation, but it counts every unmoved non-cargo spy-like role
unit, including spies idling at home and Great-Spy-role units. The actual mission
stationary discount uses the acting ordinary spy's fortify turns in the relevant
mission context. UNIT_GREAT_SPY has bSpy=0, yet Great-Spy-role units are explicitly
included by this metric.

Thus the recorder can report substantial "stationary preparation" when no usable
ordinary spy discount is actually prepared against a target.

Ancestry: introduced in practical 6046 (559ee966cb...).

Confidence: HIGH.


F056 / provisional KI#377 -- attacker-loss combat is logged at attacker origin instead of attacked plot

CvUnit::updateCombat knows the actual combat target as pPlot. When the ATTACKER
dies, the combatResult callback runs while that losing attacker still resides on
its origin plot. logSASGameRecordCombatResult reconstructs battle location from
pLoser->plot(), so attacker-loss battles are recorded at the attacker start tile
rather than the tile attacked.

Defender-loss combat happens to be correct because the losing defender occupies
the battle target tile.

Downstream consequences include wrong battle coordinates and, critically, wrong
cityPlot classification for failed attacks against cities. That propagates into
interval cityBattleWins/Losses, per-war cityPlotWins*, Barbarian combat-location
rows, battle records and related diagnostic context.

Ancestry: present in the original practical 6083 GameRecord combat implementation;
later practical 6100/6103 flow and per-war summaries reused the derived cityPlot
and widened the diagnostic damage rather than creating the original error.

Confidence: HIGH.


F057 / provisional KI#378 -- battle-summary displayed interval does not track the real counter-reset boundary

GAME_RECORD_BATTLE_SUMMARY reconstructs its displayed start turn as
currentTurn - configuredInterval + 1 rather than remembering the turn at which its
battle counters were actually reset.

This is already wrong for the first new-game bucket: turn-0 combat can contribute
to the counters while a turn-10 summary can display range 1-10. It becomes more
obviously wrong when a save is loaded at an arbitrary turn or when victory forces
a snapshot/reset in the middle of the configured interval.

The newer production/military-flow bucket is a strong internal negative control:
it explicitly tracks g_iSASGameRecordFlowStartTurn to avoid exactly this class of
false interval label.

Confidence: HIGH.


F058 / provisional KI#379 -- session-local Golden Age / anarchy observations are emitted as unqualified lifetime totals

The recorder starts a fresh log session after loading a save and resets its local
arrays. totalGoldenAgeTurns and totalAnarchyTurns are among those reset values.
They then accumulate only turns observed since the new GameRecord session began,
but their names and emitted fields say totalGoldenAgeTurns / totalAnarchyTurns
without a logged/session validity qualifier.

Elsewhere the recorder is careful about this distinction: recorder-local city and
battle values are named loggedCities*/loggedBattle*, wars loaded midstream expose
unknown start state, and delta rows expose deltaValid=0 until a previous snapshot
exists. The GA/anarchy fields do not.

Therefore loading a turn-200 save can legitimately produce totalGoldenAgeTurns=0
for a civilization that had one or more earlier Golden Ages. Conservative repair
is schema/metadata/renaming (e.g. logged/session totals or validity/start fields),
unless true lifetime totals are deliberately persisted in save state.

Confidence: HIGH as telemetry/schema accuracy; not gameplay logic.


F059 / provisional KI#380 -- raw MAX_INT finite-production sentinels leak into structured city records

The city snapshot writes CvCity::getProductionTurnsLeft() and
CvCity::getProductionNeeded() directly as ordinary numbers. These getters use
MAX_INT (2147483647) as an internal sentinel when no finite answer exists.

Observed directly in the supplied GameRecord sample:
  * PROCESS production rows can emit productionTurns=2147483647 and
    productionNeeded=2147483647;
  * cities with no current production order can emit the same sentinel pair;
  * even ordinary UNIT/BUILDING production can emit productionTurns=2147483647
    during disorder/anarchy because getProductionDifference() is zero there,
    while productionNeeded remains finite.

So a machine-readable field that visually looks like a real ETA/requirement leaks
an implementation sentinel rather than an explicit unknown/infinite/not-applicable
state.

Ancestry: dates to practical 6022, the initial SASGameSummary city-detail row,
which already logged these getters directly.

Confidence: VERY HIGH; observed in supplied log and matched to engine getter contracts.


F060 / provisional KI#381 -- productionConversionX100 reports process output during disorder when CvCity::updateCommerce produces zero

The logger's process-conversion helper says it matches CvCity::updateCommerce, but
it computes process conversion whenever a process is selected and omits the
engine's !isDisorder() gate.

CvCity::updateCommerce zeros ordinary commerce and production-to-commerce process
conversion while the city is in disorder. A human city can retain a PROCESS while
in disorder because CvCity::doProduction handles isProductionProcess() before its
disorder early-return. Therefore the recorder can report Wealth/Research/Culture
conversion output that the city is not actually receiving on that turn.

Ancestry: introduced in practical 6075 / commit 22dc2f0abd... with the
productionConversionX100 field.

Confidence: HIGH.


F061 -- UNUSED


IMPORTANT NEGATIVE RESULTS / CHALLENGED CANDIDATES
--------------------------------------------------

* Shared per-player previous-state deltaValid ordering is correct. Level-2
  subrows consume/update their individual prior values before the common valid
  bit is set at the end of the snapshot.

* Player-history snapshot timing is valid: periodic recording occurs after each
  player has updated current-turn economy/industry/agriculture/power/culture/
  espionage history. Apparent missing-current-turn concerns are rejected.

* Golden Age/anarchy cumulative increment timing matches the turn played under
  those states; F058 is specifically the load-session lifetime-label problem,
  not an off-by-one decrement-timing bug.

* City happiness/unhappiness/health source decomposition matches CvCity engine
  accounting essentially term-for-term. The odd global-warming *10 is K-Mod's
  explicit percentage-to-parts-per-thousand conversion and is intentional.

* tradeRoutes is route-slot capacity while domesticTradeRoutes/foreignTradeRoutes
  count populated routes. This naming is somewhat ambiguous but follows the
  engine getter vocabulary; no defect promoted.

* War start/end hooks, spaceship launch/failure, capital victory-state logging,
  production overflow sampling and Great-Person action hooks are on defensible
  sides of the relevant state transitions.

* Combat Great-Person death is not double logged: lethal kill occurs while the
  unit is still in combat, suppressing the noncombat death hook; combatResult is
  the ordinary combat-death record.

* Buffered bombard sequences have a defensible flush/ordering mechanism and a
  recursion guard. No dropped/reordered-row root found.

* Production overflow is not double-counted. handleOverflow increments the
  interval overflow totals; completion hooks count completed objects and repeat
  overflow values only on detailed attribution rows.

* Worker buildTurnsLeft theoretically has a MAX_INT branch, but the recorder asks
  it only for a live currently executing build where the worker itself contributes
  work. No reachable zero-rate current-build state established; do not promote.

* Great-Person specialist indexing would be unsafe for NO_SPECIALIST in isolation,
  but the sole CvUnit::join caller reaches it only after canJoin validation rejects
  NO_SPECIALIST / invalid specialist state. Unreachable; rejected.

* visibleCombatEnemiesR2 excludes ordinary aircraft (base combat 0, air combat >0).
  After tracing its settler/worker diagnostic purpose, this is not strong enough
  to prove a bug: the field can reasonably mean nearby direct ground/naval danger,
  and ordinary air attacks cannot capture settlers. Keep as naming ambiguity only.

* researchTurns=-1 is explicitly AdvCiv's "infinitely many turns" contract;
  victory countdown -1 is the real inactive-state contract; vote-source timers
  are maintained nonnegative. No second accidental MAX_INT leak like F059 found.


MECHANICAL / STRUCTURAL CLOSURE STATE
-------------------------------------

Definition inventory:
  284 live top-level definitions / 280 unique callable names after reconciling
  constructors and intentional overloads.

Static helpers:
  209 static helpers; every one has at least one live use. No orphan helper found.

Header/definition/caller wiring:
  Every header-declared hook has a definition.
  Every externally intended hook checked has an external caller.
  The apparent exceptions are intentional:
    - two getter functions are consumed through header macros;
    - logSASGameRecordRunStatus is intentionally internal to snapshot generation.

Literal format arity:
  Whole CvGameCoreDLL scan (including generic calls in CvPlayer.cpp, CvUnit.cpp,
  CvDeal.cpp) found 172 literal logSASGameRecord call sites and ZERO
  format-specifier / argument-count mismatches.

Vararg type audit:
  NOT YET COMPLETE. The format corpus contains approximately 1675 %d, 390 %s,
  38 %S and 4 %u conversions. Argument counts are proven clean; the remaining
  important closure task is to finish narrow/wide/integer argument-type checking.

Index/enum/nullability sweep:
  Largely complete and currently clean. Public textual hook arguments checked are
  guarded or supplied from stable literal/XML-owned strings; mission-queue empty
  state is safely NO_MISSION; representative enum-indexed containers use valid
  engine enum state with expected NO_* guards.

Divisor/sentinel sweep:
  Largely complete. Percentage/division sites found so far are protected by
  max(1,...), positive construction or valid populated-record contracts. No new
  unsafe denominator promoted. No second accidental MAX_INT field beyond F059.

Known-Issue collision check:
  KI#374 through KI#381 are free in the current supplied tree.

Source modifications:
  NONE. Audit only.


EXACT NEXT CURSOR
-----------------
File 011 remains OPEN.

Resume from:
  1. finish vararg TYPE audit (%d / %s / %S / %u), not just count audit;
  2. finish any remaining event-hook mutation timing / string lifetime edge cases;
  3. final adversarial reread of F053-F060 against engine contracts and ancestry;
  4. recheck KI#374-KI#381 collision/current-source state;
  5. decide whether SASGameRecordLog.cpp can become durable C012 (11/169 files)
     or requires another WIP.

F061 remains UNUSED. Do not invent one merely to increase the finding count.


===============================================================================
C011-WIP03 -- FILE 011 SASGameRecordLog.cpp LATE-CLOSURE CHECKPOINT
===============================================================================

STATUS
------
Source baseline remains practical 6244 / HEAD acf6f3baea... from the supplied
AdvCiv-SAS_light_source_20260824T132346.zip.

File 011 remains OPEN while the final adversarial closure pass finishes.
Durable completed-file count remains 10/169.

C011-WIP02 is preserved byte-for-byte as the cumulative prefix. This append
records the work performed after WIP02: completion of the structured-varargs
type audit, static-state lifecycle closure, another adversarial reread of the
existing roots, and promotion of one late independent telemetry root, F061.
F062 is currently UNUSED.

NEW ROOT
--------

F061 / provisional KI#382 -- session rollover can discard pending buffered telemetry

The logger deliberately buffers some records until an ordering boundary rather
than writing them immediately:

* CITY_BOMBARD actions are accumulated in g_kSASGameRecordPendingCityBombard
  and emitted by flushSASGameRecordPendingCityBombard().
* batched plot-change coordinates are accumulated in
  g_aSASGameRecordPlotChanges with g_iSASGameRecordPendingPlotTurn.
* incremental revelation coordinates are accumulated in
  g_aaSASGameRecordRevealedPlots.

Ordinary ordering is sound: logSASGameRecord() flushes a pending bombard before
an unrelated row, flushSASGameRecordTurnChanges() flushes bombard plus the
turn-local coordinate buffers, and endGameTurn calls that turn flush.

The session-start paths are different. Both
startSASGameRecordLogForNewGame() and startSASGameRecordLogForLoadedSave() do:

    rollSASGameRecordLog(...);
    resetSASGameRecordState();

resetSASGameRecordState() then clears the pending bombard object, pending plot
turn, plot-change groups and revelation coordinate vectors. There is no flush
before that reset. Loading is particularly concrete because
CvGame::onAllGameDataRead() starts the new loaded-save log only after all saved
game state has already been read. Therefore an old-session pending buffer cannot
be reconstructed after the reset.

Reachable example: perform a city bombardment at GameRecord level 3 and quickload
before another ordinary GameRecord row/end-turn boundary. The bombard hook has
only accumulated the action in static pending state. The loaded-save start path
rolls to the new log and resets that state, so the final bombard sequence never
appears in the old session log. Batched map/revelation coordinates have the same
class of rollover loss when still pending.

This is recorder durability/history loss, not gameplay behavior. It should not
be overstated as corruption of already-written rows, and abrupt process exit may
need a different solution. The deterministic load/new-game rollover path is the
root established here.

Repair direction needs care: simply flushing after onAllGameDataRead is too late
for map-derived fields because the CvGame/CvMap object already represents the
newly loaded state. A robust fix should flush/finalize pending old-session
telemetry before destructive game-state replacement / before rolling the log, or
make each pending record self-contained enough to finalize safely at rollover.
CITY_BOMBARD itself is already largely self-contained; batched revelation rows
also derive revealedPctX100 at flush time, so those need special attention.

Ancestry: the pending plot-change architecture is already present when
SASGameRecord replaces/absorbs SASGameSummary at practical 6083
(cac3fa4d2b...). CITY_BOMBARD buffering is added at practical 6092
(c30e4e1ef0...) together with reset-time clearing and ordinary-row/end-turn
flushes, but no pre-session-rollover flush. Thus the common rollover gap is not
inherited BtS/AdvCiv gameplay code; it belongs to SAS telemetry history.

Confidence: HIGH for the deterministic pending-buffer loss; repair placement
requires design care.


VARARGS TYPE AUDIT -- COMPLETED
-------------------------------

A corrected whole-CvGameCoreDLL parser now recognizes comparison operators
without mistaking '<'/'>' expressions for C++ template nesting. Exact literal
logSASGameRecord corpus:

* 172 literal call sites
* 2,107 format conversions
* 1,675 %d
* 390 %s
* 38 %S
* 4 %u
* zero format/argument-count mismatches

The type-level follow-up is also clean:

* all 38 %S values are genuine wide-string inputs (CvWString/wchar data),
* all 4 %u values are unsigned values (CvRandom seed / uint timing values),
* no %d site passes a Cv object/reference/pointer in the manner of the historical
  pPlot->getArea() varargs crash; container .size() values are explicitly cast
  to int, and numeric/bool/enum values have valid default promotions,
* no %s/%S narrow/wide crossover was found; temporary CvString values whose
  GetCString() is passed remain alive through the complete call expression.

The old bonus-change CvArea& crash is therefore a useful negative control rather
than evidence of another surviving formatter-type bug. No F062 from varargs.


STATIC-STATE LIFECYCLE -- COMPLETED
-----------------------------------

All file-scope GameRecord state was inventoried again. Session identity/timing
state (timestamp/context, session and previous-snapshot timer, display-context
flag) is intentionally reset by rollSASGameRecordLog(). Recorder observation
state (battle/city totals, player/team/global previous snapshots, flow buckets,
war summaries, pending plot/revelation/bombard state, autoplay counters, etc.) is
reset by resetSASGameRecordState().

g_bSASGameRecordFlushingCityBombard is not persistent session data: it is a
short synchronous recursion guard set immediately around one synthetic write and
restored immediately afterward. No stale-session root was found there.

The one state-lifecycle failure is F061 above: valid pending observations are
cleared at rollover before being finalized.


F053-F060 FINAL-CHALLENGE NOTES SO FAR
--------------------------------------

F053 remains HIGH confidence. CvUnit::isEnemy explicitly documents that the
supplied plot controls combat ownership. getCombatOwner_bulk turns an
always-hostile unit into BARBARIAN_PLAYER when isAlwaysHostile(kPlot) is true,
while isAlwaysHostile(kPlot) deliberately becomes false in the unit owner's base
territory. Passing the observer center instead of pLoopPlot can therefore change
the answer. A concrete direction is an always-hostile/hidden-nationality unit on
its own base tile near a foreign observer center: the actual unit plot suppresses
always-hostile ownership, whereas the foreign center need not. The duplicated
CvUnitAI diagnostic helper has the same wrong-plot expression.

F054 remains HIGH confidence. The 2048-byte _vsnprintf sink silently truncates
rather than failing safely at the schema level. The current sample does not hit
it, but legality-aware city-building payloads can exceed it before fixed row
fields are added.

F055 remains HIGH confidence. The recorded field/comment specifically describes
stationary espionage cost-reduction preparation, but all fortified spy-like
units are counted regardless of deployment; ordinary mission cost reduction uses
the acting pSpyUnit's fortify turns, and UNIT_GREAT_SPY has XML bSpy=0 while this
metric explicitly includes UNITAI_GREAT_SPY.

F056 remains VERY HIGH confidence. CvUnit::updateCombat calls combatResult before
a surviving attacker advances. On attacker death, pLoser is therefore still on
the attacker's origin plot; on defender death, pLoser is on the attacked target.
Using pLoser->plot() cannot represent the battle tile symmetrically. This affects
coordinates and the derived cityPlot aggregates, plus detailed death/battle rows.
Simply substituting pWinner->plot() is not a universal repair because on a
defender death the winning attacker also has not advanced yet; the actual target
plot must be propagated/reconstructed from combat context.

F057 remains VERY HIGH confidence. Battle counters can be reset by a forced
victory snapshot mid-configured-interval and can begin observation only after an
arbitrary-turn save load, yet range start is always back-calculated from the
configured interval. The newer flow bucket's explicit g_iSASGameRecordFlowStartTurn
is the local correct-pattern control.

F058 remains HIGH confidence as a schema/session-accuracy root. The arrays are
reset on every loaded-save log and only count player turns observed afterward,
yet rows call them totalGoldenAgeTurns/totalAnarchyTurns without a session or
validity qualifier. Nearby recorder-local aggregates are explicitly named
logged* or carry validity/start metadata.

F059 remains VERY HIGH confidence and directly observed. CvCity exposes MAX_INT
for process/no-order production and for finite production with zero production
difference; GameRecord writes 2147483647 as an ordinary productionTurns /
productionNeeded value. The supplied example demonstrates the sentinel.

F060 remains HIGH confidence. getSASGameRecordCityProductionConversion claims to
match CvCity::updateCommerce but omits updateCommerce's !isDisorder() guard.
CvCity::doProduction handles a retained production process before its own
disorder early-return, so PROCESS+disorder is reachable while actual process
commerce is zero.


REJECTED / CLOSED LATE CANDIDATES
---------------------------------

* Ordinary CITY_BOMBARD ordering itself is sound: next ordinary rows and
  end-turn flushes preserve sequence order. F061 is specifically durability at
  session rollover, not ordinary ordering.
* Static logger state is otherwise partitioned correctly between roll/reset.
* Full-map revelation begin/end assertions are backed by the sole CvTeam caller,
  which supplies a real acquired map-visible tech and a valid civ team; no
  release-assert-only mutation/root found.
* Pending-turn equality is protected by prepareSASGameRecordTurnChanges(), which
  flushes an older pending turn before beginning the new one.
* Spaceship arrivalTurn = launchTurn + countdown survives timing reconstruction:
  the first countdown decrement is on the next game turn, so N reaches zero at
  launchTurn+N.
* Air-strike/interception/bomb target locations are supplied from the actual
  target context and do not share F056's lethal-ground-combat reconstruction.
* Player elimination reconciliation timing survives review.
* Worker build ETA, researchTurns=-1, victory countdown=-1, and other examined
  sentinels remain valid under their caller/engine contracts; F059 is still the
  distinct accidental raw MAX_INT exposure.


STRUCTURAL CLOSURE STATE
------------------------

* 284 live top-level definitions / 280 unique callable names reconciled.
* 209 static helpers, all with at least one live use.
* Every header-declared hook has a definition.
* Every externally intended hook has a live external caller; the two getter
  exceptions are consumed by header macros and logSASGameRecordRunStatus is
  intentionally internal.
* Whole-DLL literal structured-varargs audit complete as above.
* enum/index/nullability/sentinel/divisor scans have not produced another root.
* KI#374-KI#381 were previously confirmed free in the current Known Issues file.
  Current Known Issues ends at KI#363; therefore provisional KI#382 is also free.


CURRENT NORMALIZED ROOT LEDGER
------------------------------

F053 / provisional KI#374 -- wrong plot context in nearby-enemy isEnemy checks
F054 / provisional KI#375 -- fixed 2048-byte structured-row truncation ceiling
F055 / provisional KI#376 -- stationary-spy preparation counts ineligible state
F056 / provisional KI#377 -- attacker-loss combat uses origin as battle location
F057 / provisional KI#378 -- battle-summary range ignores actual reset boundary
F058 / provisional KI#379 -- session GA/anarchy observations labeled as totals
F059 / provisional KI#380 -- raw MAX_INT production sentinel leaks into rows
F060 / provisional KI#381 -- process conversion reported during city disorder
F061 / provisional KI#382 -- session rollover can discard pending buffered rows
F062 -- UNUSED


EXACT NEXT CURSOR
-----------------

SASGameRecordLog.cpp remains OPEN.
Resume at F062 UNUSED.

1. Finish the final adversarial challenge of F053-F061, especially whether F061
   should remain one common buffered-rollover root and whether any documented
   design contract intentionally permits dropping the old-session tail.
2. One last whole-file closure/sanity sweep for anything invalidated by the late
   F061 lifecycle observation; do not manufacture F062.
3. Reconfirm provisional KI#374-KI#382 numbering against the current tree.
4. If all roots survive and no uncovered structural family remains, freeze
   durable C012 and advance completed-file count to 11/169.
5. If a real uncertainty remains, freeze another WIP instead.

No repository source has been modified by this audit.
===============================================================================


===============================================================================
C012 -- FILE 011 SASGameRecordLog.cpp DURABLE CLOSURE
===============================================================================

STATUS
------
Source audited: practical 6244 / HEAD
acf6f3baea1fb700645fcb5133e130b4e61e2316 from the supplied
AdvCiv-SAS_light_source_20260824T132346.zip.

FILE 011 IS NOW COMPLETE for the current file-oriented audit.
Durable completed-file count advances from 10/169 to 11/169.

C011-WIP03 is preserved byte-for-byte as the cumulative prefix of this C012
artifact. No repository source was modified by the audit.

Nine independent current-tree SASGameRecord roots survive final adversarial
review: F053-F061. F062 remains UNUSED for the next file.

The current Known Issues document ends at KI#363. No headings KI#374-KI#382
exist, so all provisional numbers below remain collision-free in practical 6244.


FINAL NORMALIZED ROOT LEDGER -- FILE 011
----------------------------------------

F053 / provisional KI#374
Nearby-enemy diagnostics evaluate combat ownership at the observer center plot,
not the unit's actual plot.

countSASGameRecordVisibleEnemiesNearPlot iterates pLoopUnit on pLoopPlot but calls
pLoopUnit->isEnemy(eTeam, kCenter). CvUnit::isEnemy explicitly uses the supplied
plot to determine combat ownership. getCombatOwner_bulk can map an always-hostile
unit to BARBARIAN_PLAYER depending on isAlwaysHostile(kPlot), while
isAlwaysHostile(kPlot) deliberately becomes false in the unit owner's base
territory. Therefore replacing pLoopPlot by kCenter can change enemy status.

A concrete direction is an always-hostile/hidden-nationality unit standing on
its own team's base tile near a foreign center: its actual tile suppresses
always-hostile ownership, while the foreign center need not. The same wrong-plot
expression is duplicated in CvUnitAI.cpp's settler diagnostic helper.

Affected telemetry includes visibleEnemiesR2, visibleCombatEnemiesR2 and
nearestEnemy*; no direct gameplay AI behavior is changed by the GameRecord copy,
although the duplicated CvUnitAI diagnostic also becomes misleading.

Repair direction: call isEnemy(eTeam, *pLoopPlot) / use the unit's actual plot.
Confidence: HIGH.


F054 / provisional KI#375
The fixed 2048-byte structured formatter can silently truncate valid rows.

logSASGameRecordFormattedLine formats every structured row through a static
char[2048] using MSVC _vsnprintf. KI#161.2 correctly forces the final NUL byte,
preventing the historical unterminated-buffer/heap-crash class, but oversized
rows are still silently cut and then written as if complete.

This is reachable for variable-length machine-readable inventories. The supplied
example log does not hit the ceiling (largest observed row about 1.64 KB), which
is preserved as an important negative observation. However SAS has 123 building
classes; after civilization-specific building mappings and ordinary national-
wonder constraints, a legal mature city can still hold roughly 117 distinct
building entries. Their TYPE:count payload alone can be about 2.8 KB for several
civilizations before the fixed GAME_RECORD_CITY_BUILDINGS fields are included.
Other intentionally large map producers already chunk around ~1500 bytes, a
useful local control showing that bounded output is part of the logger design.

Repair direction: use a dynamically sized formatting path, or make every
unbounded structured payload use explicit chunking with part/parts metadata.
Confidence: HIGH (reachable structural data loss, not demonstrated in the sample).


F055 / provisional KI#376
The stationary espionage-preparation metric counts states that cannot provide the
claimed ordinary-spy stationary cost reduction.

logSASGameRecordEspionage describes foreign deployment, city infiltration and
"stationary cost-reduction preparation". It counts any included unit with
getFortifyTurns()>0, independent of where it is standing, and explicitly includes
UNITAI_GREAT_SPY. Actual espionage mission cost reduction uses the acting
pSpyUnit->getFortifyTurns(); a spy fortified at home is not presently positioned
to use that target discount, and UNIT_GREAT_SPY has XML bSpy=0 despite the
logger's UNITAI_GREAT_SPY inclusion.

Thus stationarySpies/maxFortifyTurns can report apparent mission preparation
that consists only of home-idle ordinary spies or Great Spies rather than a
usable stationary infiltration state.

Repair direction: define the metric explicitly and count only ordinary spy units
in an eligible foreign/mission context if it is intended to measure current
stationary mission discount. Alternatively rename/split the raw fortified-unit
metric so it does not claim cost-reduction readiness.
Ancestry: introduced in practical 6046 (559ee966cb...).
Confidence: HIGH.


F056 / provisional KI#377
Lethal attacker losses are recorded at the attacker's origin, not the battle
target, causing direction-dependent combat-location and city-battle accounting.

CvUnit::updateCombat has the true target as pPlot. When the attacker dies it
calls combatResult(pDefender, this) before the dead attacker has moved from its
origin plot. When the defender dies it calls combatResult(this, pDefender), and
the defender naturally resides on the target plot. logSASGameRecordCombatResult
then reconstructs the battle tile as pLoser->plot(). The same expression is
therefore correct only when the defender loses.

Consequences for attacker-loss combat include wrong x/y, failed attacks against a
city becoming cityPlot=0, wrong interval/per-war cityPlot wins/losses, and wrong
locations on detailed BARBARIAN_COMBAT / GAME_RECORD_BATTLE rows. Combat Great
General/Great Person death rows also read the losing unit's coordinates and can
inherit the same attacker-origin location.

Simply switching to pWinner->plot() is not a general repair because a winning
attacker also has not advanced at the defender-death callback. The actual combat
target plot (or equivalent attacker/defender context) should be propagated to the
GameRecord hook.

Ancestry: present in the original practical-6083 GameRecord combat implementation;
6100/6103 later reused the derived cityPlot in additional flow/war summaries.
Confidence: VERY HIGH.


F057 / provisional KI#378
GAME_RECORD_BATTLE_SUMMARY fabricates the displayed observation interval instead
of tracking the actual counter-reset boundary.

logSASGameRecordBattleBuckets derives startTurn as
currentTurn - configuredInterval + 1. But its counters can start/reset at other
boundaries: a loaded-save session can begin on an arbitrary turn, and victory can
force a full snapshot/battle-bucket reset in the middle of the configured
interval. The first new-game bucket also can contain turn-0 combat while a
turn-10 / interval-10 row displays range=1-10.

The newer production/military flow bucket is the local correct-pattern control:
it carries g_iSASGameRecordFlowStartTurn, initializes it to the actual log-session
turn, and advances it to iGameTurn+1 after a flush.

Repair direction: track an explicit battle-bucket start turn and update it at
every reset, analogous to the flow bucket.
Confidence: VERY HIGH.


F058 / provisional KI#379
Golden Age/anarchy recorder-session observations are emitted as unqualified
lifetime totals after a loaded save.

g_aiSASGameRecordTotalGoldenAgeTurns and
 g_aiSASGameRecordTotalAnarchyTurns are reset to zero whenever a new loaded-save
GameRecord session starts, then increment once for each player turn observed in
that session. Nevertheless GAME_RECORD_PLAYER and related action rows expose them
as totalGoldenAgeTurns / totalAnarchyTurns without a validity/session qualifier.
A turn-200 load can therefore report zero lifetime total despite earlier Golden
Ages/anarchy that occurred before the load.

This differs from nearby recorder-local quantities whose schema is explicit:
loggedBattle*, loggedCities*, deltaValid, unknown loaded-war start baselines, and
flow start turns.

Repair direction: rename/qualify as logged/session totals and expose session start
or validity, or persist/reconstruct true lifetime counters if lifetime semantics
are desired.
Confidence: HIGH (schema/telemetry accuracy, not gameplay logic).


F059 / provisional KI#380
Raw CvCity MAX_INT production sentinels leak into structured city rows as ordinary
numeric values.

GAME_RECORD_CITY writes CvCity::getProductionTurnsLeft() and
getProductionNeeded() directly. CvCity returns MAX_INT for no finite current
order / ORDER_MAINTAIN process, and getProductionTurnsLeft can also return MAX_INT
for an ordinary unit/building/project whenever current production difference is
zero, e.g. during disorder. The supplied example log directly contains
2147483647 in these fields, including process/empty-production states and finite
production under anarchy/disorder.

GAME_RECORD_BARBARIAN_CITY also writes getProductionTurnsLeft() directly and
shares the productionTurns sentinel exposure.

Repair direction: translate MAX_INT to an explicit schema sentinel such as -1
(or a textual state/validity field) and keep finite productionNeeded only where a
finite train/construct/create order exists.
Ancestry: dates to practical 6022's initial SASGameSummary city-detail row.
Confidence: VERY HIGH and directly observed.


F060 / provisional KI#381
productionConversionX100 reports process output during disorder although
CvCity::updateCommerce gives the city zero process commerce then.

getSASGameRecordCityProductionConversion says it matches CvCity::updateCommerce,
but if a process is selected it always multiplies production yield by the
production-to-commerce modifier. The real CvCity::updateCommerce performs both
ordinary commerce and process conversion only inside if (!isDisorder()).
CvCity::doProduction can retain a production process during disorder because the
process branch returns before doProduction's later disorder early-return.
PROCESS + disorder is therefore a reachable state with actual conversion zero
but positive GameRecord productionConversionX100.

Repair direction: return '-' / zero conversion while isDisorder(), mirroring the
engine contract the helper explicitly claims to reconstruct.
Ancestry: introduced practical 6075 (22dc2f0abd...).
Confidence: HIGH.


F061 / provisional KI#382
Starting a loaded/new game can discard still-buffered telemetry from the old log
session before it is ever written.

CITY_BOMBARD actions, batched plot-change coordinates and incremental revelation
coordinates are intentionally buffered. During ordinary play, the generic logger
and end-turn flush preserve ordering and eventually write them. However both
startSASGameRecordLogForNewGame() and startSASGameRecordLogForLoadedSave() roll the
log and then call resetSASGameRecordState(), which clears all pending buffers;
there is no pre-roll/pre-reset finalization.

Concrete deterministic example: perform a city bombard at log level 3, then
quickload before another ordinary GameRecord row/end-turn. The bombard exists
only in g_kSASGameRecordPendingCityBombard. CvGame::onAllGameDataRead starts the
new loaded-save log after the new game state has been read, and reset discards the
old pending action, so it never reaches the old session file. Pending map/reveal
batches have the same class of tail loss.

Project documentation describes SASGameRecord as a compact factual whole-game /
autoplay record, and load sessions deliberately roll to distinct loadN logs; no
contract was found that permits dropping the old branch tail. Victory explicitly
flushes pending turn changes before its final snapshot, reinforcing that pending
history is meant to be finalized at important boundaries.

Repair placement needs care. Flushing map-derived rows only after
onAllGameDataRead is too late because the game/map state already belongs to the
newly loaded save; either finalize the old session before destructive state
replacement/log roll or make buffered rows self-contained enough for safe late
finalization.

Ancestry: the pending plot architecture is already present when GameRecord is
introduced/renamed at practical 6083 (cac3fa4d2b...); CITY_BOMBARD buffering is
added at practical 6092 (c30e4e1ef0...) with normal-boundary flushes and
reset-time clearing but no session-rollover flush.
Confidence: HIGH for deterministic pending-buffer loss; repair architecture is
less trivial than the diagnosis.


FINAL WHOLE-FILE CLOSURE EVIDENCE
---------------------------------

1. Definition/header/helper inventory

* 284 live top-level definitions / 280 unique callable names reconciled.
* 209 static helpers; every one has at least one live use.
* Every SASGameRecordLog.h declaration has a definition.
* Every externally intended public hook has a live external caller. The two
  apparent getter exceptions are consumed through header macros;
  logSASGameRecordRunStatus is intentionally internal to snapshot generation.

No orphan hook/helper root remains.

2. Structured varargs audit

A corrected parser over the entire CvGameCoreDLL finds exactly:

* 172 literal logSASGameRecord call sites
* 2,107 conversions total
* 1,675 %d
* 390 %s
* 38 %S
* 4 %u
* zero format/argument-count mismatches

Type challenge also closes cleanly: all %S arguments are wide strings, all %u
arguments are unsigned values, narrow/wide %s crossover was not found, temporary
CvString lifetimes survive the full call expression, container .size() arguments
are explicitly int-cast, and no remaining %d expression passes a class/object
reference like the already-fixed CvArea& bonus-change crash.

3. Static state/reset lifecycle

All session identity/timing state is deliberately owned by rollSASGameRecordLog;
all recorder observation/counter/previous-state state is reset by
resetSASGameRecordState. The bombard flushing bool is a synchronous recursion
guard and cannot become ordinary stale session data. F061 is the one surviving
lifecycle defect: valid pending observations are cleared rather than finalized.

4. Index/enum/nullability/sentinel/divisor sweep

No additional live invalid index or caller-nullability root survived. Several
apparently unsafe helpers become safe under their sole validated callers (e.g.
Great-Person join specialist indexing). Direct division sites have positive
construction or explicit guards; landmass divisions run only for positive-sized
landmasses and team percentages guard world totals. Worker build ETA, research
-1, victory countdown -1 and vote timers follow valid engine contracts. F059 is
the distinct accidental raw MAX_INT schema leak.

5. Snapshot and event timing

Player-history timing, Golden Age/anarchy per-turn sampling, city health/happiness
reconstruction, production overflow ownership, war start/end, spaceship launch /
failure, capital victory-progress removal, Great-Person actions, air strike /
interception/bombing and player-elimination reconciliation were traced to their
mutation points. No additional event-order root survived. Ground lethal combat
location remains the asymmetric F056 exception.

6. Rejected semantic candidates retained as negative controls

* deltaValid common-bit ordering is correct.
* tradeRoutes capacity versus populated domestic/foreign route counts is naming
  ambiguity consistent with engine getter vocabulary, not a promoted defect.
* ordinary bombard sequence ordering/recursion is correct; only rollover
  durability is F061.
* aircraft omission from visibleCombatEnemiesR2 was not strong enough to call a
  bug under the settler/worker direct-danger diagnostic semantics.
* combat Great-Person deaths are not normally double logged.
* overflow totals are not double counted by completion rows.
* full-map revelation assertion contracts are guaranteed by the sole CvTeam
  caller in supported current flow.
* spaceship arrivalTurn arithmetic matches countdown timing.
* no second MAX_INT-like sentinel leak was established.

7. Base / ancestry classification

SASGameRecordLog is SAS-specific, so most roots are telemetry/schema issues rather
than inherited AdvCiv/Civ4CE behavior. Base/AdvCiv engine code was used to prove
contracts where necessary (CvUnit combat ownership, CvCity production/commerce,
combat callback timing). Snapshot commit-diff archaeology was used to date the
SAS regressions/features where useful. No root is being mislabeled as a base
AdvCiv gameplay bug merely because an engine getter participates in it.


CLOSURE DECISION
----------------

SASGameRecordLog.cpp is defensibly exhausted for this pass. The nine roots above
remain independently actionable after explicit attempts to disprove them; late
whole-file passes killed several weaker candidates instead of manufacturing
F062. No uncovered method/helper/caller/state/index/format/divisor/event family
remains that justifies keeping file 011 open.

FILE 011 STATUS: COMPLETE -- REVIEWED / NINE NEW SAS TELEMETRY FINDINGS
COMPLETED FILES: 11 / 169
NEXT ROOT ID: F062 (UNUSED)

NEXT FILE
---------

012  6326  CvGameCoreDLL/CvDLLWidgetData.cpp

Start the next file from its structure/public entry points/callers, then proceed
through tooltip/action/widget dispatch families and engine/UI contracts with the
same evidence standard. Do not carry SASGameRecord-specific hypotheses into the
new file unless a real caller/cross-file relationship requires it.

No repository source was modified.
===============================================================================


===============================================================================
C012-WIP01 -- FILE 012 IN PROGRESS: CvGameCoreDLL/CvDLLWidgetData.cpp
Source baseline: practical 6244 / HEAD acf6f3baea...
File size: 6,326 lines
Durable completed files entering this pass: 11 / 169
Next root entering this pass: F062
Repository source modified: NO
===============================================================================

RECOVERY CURSOR
---------------

CvDLLWidgetData.cpp is OPEN. Initial structural/SAS-delta pass completed; top-level
parse/execute/isLink dispatchers and current SAS-only changes have begun detailed
caller/ancestry validation. Resume with F063 unused after the confirmed F062 root,
then continue inherited helper/action/tooltip families, index/nullability/divisor
checks, header/caller inventory, and final root challenge before any C013 decision.

CONFIRMED CURRENT ROOT
----------------------

F062 / provisional KI#383
The SAS vassal-willingness helper refactor accidentally removed the existing
War-Trades hover explanation from WIDGET_LH_GLANCE.

Current CvForeignAdvisor.py deliberately selects WIDGET_LH_GLANCE for a Glance
matrix relation cell when Advisors__ShowGlanceWarTrades is enabled. The visible
fist icon itself is assembled in Python via AttitudeUtil.getAttitudeText(...,
AdvisorOpt.isShowGlanceWarTrades()). In base AdvCiv 1.14, the matching C++
WIDGET_LH_GLANCE tooltip path does two things:

  1. parseLeaderheadRelationsHelp(...)
  2. GAMETEXT.parseWarTradesHelp(szBuffer, data1, data2)

The second call supplies the textual explanation for the special fist state --
"willing to start war" against the relevant third party -- using the same
eligibility checks as the Python icon.

SAS current source still routes Show War Trades cells to WIDGET_LH_GLANCE, but
its C++ case now performs parseLeaderheadRelationsHelp(...) followed only by
parseVassalWillingnessHelp(...). The parseWarTradesHelp call is absent. Result:
the War Trades option can still show its fist icon while hovering that cell no
longer receives AdvCiv's dedicated war-trade explanation; the newly added vassal
willingness line replaced rather than augmented that portion of the tooltip.

Ancestry is exact:

* practical 5178 / 39fcce8ec0... added the vassal-willingness tooltip while
  preserving GAMETEXT.parseWarTradesHelp in WIDGET_LH_GLANCE.
* practical 5179 / daa528a557... refactored the repeated vassal code into
  CvDLLWidgetData::parseVassalWillingnessHelp and, in that refactor hunk, deleted
  GAMETEXT.parseWarTradesHelp without replacing it.
* AdvCiv 1.14 still contains the call, confirming this is a SAS regression rather
  than an upstream policy change.

Repair direction: restore GAMETEXT.parseWarTradesHelp for WIDGET_LH_GLANCE and
then append parseVassalWillingnessHelp; the two describe independent status icons
and are meant to coexist.
Confidence: VERY HIGH.

INITIAL NEGATIVE CONTROL
------------------------

The inherited WIDGET_MENU_ICON -> WIDGET_CONSCRIPT switch fallthrough looks
alarming in both parseHelp and executeAction and exists all the way back through
AdvCiv and vanilla Civ4CE. It was challenged before promotion:

* CvMainInterface.py explicitly hides MainMenuButton whenever the city screen is
  up, i.e. the ordinary state in which a city is selected and conscription UI is
  meaningful.
* Outside the city screen the button is shown only in ordinary map-interface
  state; the current UI contract does not establish a reachable selected-city
  list there that would make the fallthrough's selectedCitiesGameNetMessage
  actually draft a city.
* parseConscriptHelp likewise needs a head-selected city to add conscription help.

Therefore this is retained as suspicious inherited dead/redundant fallthrough,
not promoted as F063 without a reachable side effect. If later caller/UI evidence
shows selected cities can coexist with the visible MainMenuButton, reopen it.

INITIAL SAS-DELTA MAP
---------------------

Relative to current AdvCiv 1.14, substantive SAS changes in this file are small
compared with the 6.3k-line inherited body. Current differences are chiefly:

* cached immutable XML/global-define lookups for several hover helpers;
* optional whole-tooltip <font=N> wrapping through SAS_UI_FONT_HOVER;
* tech-advisor right-click bonus-ID fixes for reveal vs obsolete bonus widgets;
* Foreign Advisor vassal-willingness tooltip support;
* comment/signature formatting and the unified CvForeignAdvisor naming update.

This makes a two-track audit useful: first prove all SAS deltas, then inspect the
much larger inherited dispatch/tooltip body for older AdvCiv/BtS defects.

NEXT CURSOR
-----------

F063 is UNUSED.

Continue with:
1. finish SAS-only delta validation (font wrapper, cached defines, obsolete-bonus
   right-click routing, vassal helper bounds/semantics);
2. inspect executeAction / executeAltAction / isLink symmetry and widget caller
   construction in Python/C++;
3. audit action/mission help, trade-item help, scoreboard/contact cheat text,
   city/economic hover helpers, deal cancellation and city-trade decoding;
4. perform whole-file enum/index/null-pointer/divisor/fallthrough checks;
5. reconcile source definitions against CvDLLWidgetData.h and external callers;
6. challenge every promoted root against AdvCiv/Civ4CE ancestry and current UI
   reachability before closing file 012.
===============================================================================


================================================================================
C012-WIP02 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline: AdvCiv-SAS practical 6244 / HEAD acf6f3baea...
Durable completion remains 11/169 files. This file remains OPEN.
This checkpoint extends C012-WIP01; no source files were modified.

ROOT LEDGER AT THIS CHECKPOINT

F062 / provisional KI#383 — CONFIRMED — SAS-specific regression
WIDGET_LH_GLANCE lost AdvCiv/BULL parseWarTradesHelp when SAS refactored the
vassal-willingness hover into parseVassalWillingnessHelp. Practical 5178 still
called both; practical 5179 (daa528a557...) removed parseWarTradesHelp while
extracting the vassal helper. With BUG "Show War Trades" enabled, Python still
uses WIDGET_LH_GLANCE and can still draw the war-trade fist, but the specialized
"willing to start war" hover explanation is no longer appended. Repair should
restore GAMETEXT.parseWarTradesHelp(...) and then append vassal willingness;
the two statuses are independent.

F063 / provisional KI#384 — STRONG — inherited AdvCiv regression
CvDLLWidgetData::executeAltAction unconditionally executes:
    CvCivilization const& kActiveCiv = *GC.getGame().getActiveCivilization();
before switching on the widget type. CvGame::getActiveCivilization explicitly
returns NULL when getActivePlayer()==NO_PLAYER. The opening-menu Civilopedia is
an intended such state: Sevopedia stores iActivePlayer=-1, and CvGameTextMgr has
explicit comments/branches for "NULL when in opening-menu Civilopedia". The
right-click dispatcher contains many Pedia cases (tech/unit/project/religion/
corporation/etc.) that require no civilization context, but all now encounter
the unconditional dereference first. This widening dates to AdvCiv practical
1648 (553b1a0f8d...), when old per-case doPedia* helpers were replaced by direct
CvPythonCaller jumps and the active-civilization reference was hoisted for only
the WIDGET_TRAIN/WIDGET_CONSTRUCT class-to-civ mappings. Vanilla/Civ4CE keeps
civilization lookup local to those two helpers. Conservative repair: do not
hoist/dereference the pointer; resolve active civ lazily only in cases that need
it, with an opening-menu fallback/default-unit/default-building policy if those
cases can themselves appear there.

F064 / provisional KI#385 — CONFIRMED DIAGNOSTIC DRIFT — inherited, widened by SAS
parseScoreboardCheatText contains a duplicate reconstruction of the legacy
K-Mod/BBAI AI_doWar decision machinery, but it no longer matches the real
CvTeamAI::AI_doWar. Concrete mismatches include:
  * current SAS AI_doWar KI#356 counts non-Dagger members, non-Dagger financial
    trouble, and non-Dagger GET_BETTER_UNITS intersections explicitly; the
    tooltip still uses (financialTroubleCount-daggerCount) and
    (getBetterUnitsCount-daggerCount) against the total team population.
  * actual target loops require AI_haveSeenCities(eTarget); tooltip candidate
    generation does not.
  * actual target defensive power is getDefensivePower(getID()), excluding the
    evaluating team from defensive-pact coalition power; tooltip uses
    getDefensivePower() with no exclusion.
  * actual total-war comparison additionally scales target defensive power by
    2/3; the tooltip uses the unscaled value.
  * actual dogpile support iterates MAJOR_CIV enemies and filters unseen teams;
    the tooltip uses a broader CIV_ALIVE enemy set without the same knowledge
    filter.
Several of these discrepancies already exist in AdvCiv 1.14; SAS KI#356 makes
the first two gate formulas newly/starkly wrong as a mirror of current SAS AI.
This affects debug/diagnostic truth, not gameplay. Because the function presents
war percentages, possible passes and candidates specifically as AI-debug info,
its duplicate logic should either be synchronized with AI_doWar or replaced by
shared query/helper calculations so future AI repairs cannot silently drift.

F065 / provisional KI#386 — CONFIRMED — inherited BtS tooltip defect
parseActionHelp_Mission's MISSION_INFILTRATE branch searches selected units with:
    if (!kSelectedUnit.canEspionage(&kMissionPlot)) continue;
but the mission being explained is Infiltration and CvUnit::infiltrate gates on
canInfiltrate(plot()). CvUnit::canEspionage rejects !isSpy(), whereas SAS
UNIT_GREAT_SPY has bSpy=0 and iEspionagePoints=3000; canInfiltrate instead
requires positive espionage points and a foreign non-Barbarian city. Therefore
a normal Great Spy that can perform MISSION_INFILTRATE fails the tooltip's
predicate, so the intended "+N espionage" yield line is omitted. Civ4CE/BtS
contains the same canEspionage mistake and AdvCiv 1.14 retains it. Repair: use
canInfiltrate(&kMissionPlot, true) (matching the tooltip/test-visible context)
before getEspionagePoints.

REJECTED / DEFERRED IN THIS TRANCHE
- WIDGET_MENU_ICON -> WIDGET_CONSCRIPT missing breaks remain suspicious inherited
  fallthroughs, but MainMenuButton is hidden while city screen is up and no
  verified visible-menu + selected-city path has been established. Do not call
  this a bug without reachability evidence.
- Debug war-percentage division by zero: XML literal zero war-rands belong only
  to LEADER_BARBARIAN and fake LEADER_DEFAULTS. Extremely high vassal counts can
  theoretically integer-reduce max-war rand to zero under FINAL_WAR, but this
  is not being promoted independently absent a realistic/reproduced path.
- SAS bonus right-click data routing (reveal=data2, obsolete=data1) matches the
  actual CvTechChooser widget payloads and is accepted.
- SAS global hover-font wrapper and cached immutable defines have no demonstrated
  defect so far.

NEXT CURSOR
F066 UNUSED.
Continue inside parseActionHelp_Mission after MISSION_INFILTRATE and then audit
remaining action helpers, trade/deal/city/economic hover functions, dispatch
symmetry, enum/index/null/divisor/fallthrough/static-state sweeps, full
header/definition/caller inventory, ancestry and Known-Issue collision pass.
Keep CvDLLWidgetData.cpp open until those closure gates are complete.
================================================================================


================================================================================
C012-WIP03 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline: AdvCiv-SAS practical 6244 / HEAD acf6f3baea...
Durable completion remains 11/169 files. This file remains OPEN.
This checkpoint extends C012-WIP02; no source files were modified.

ROOT LEDGER AT THIS CHECKPOINT

F062 / provisional KI#383 — CONFIRMED — SAS-specific regression
Unchanged from WIP02: WIDGET_LH_GLANCE lost parseWarTradesHelp when practical
5179 refactored the vassal-willingness hover into parseVassalWillingnessHelp.

F063 / provisional KI#384 — STRONG — inherited AdvCiv regression
Unchanged from WIP02: executeAltAction hoists and dereferences
getActiveCivilization() before its widget switch, although opening-menu Pedia is
an explicit NO_PLAYER/NULL-active-civilization state and most right-click Pedia
cases need no civilization context. Introduced by practical 1648 refactor.

F064 / provisional KI#385 — CONFIRMED DIAGNOSTIC DRIFT — inherited, widened by SAS
Unchanged from WIP02: parseScoreboardCheatText's duplicated AI_doWar model has
multiple stale gates/power/knowledge semantics; KI#356 widened the mismatch.

F065 / provisional KI#386 — CONFIRMED — inherited BtS tooltip defect
Unchanged from WIP02: MISSION_INFILTRATE tooltip tests canEspionage instead of
canInfiltrate, suppressing the normal Great Spy infiltration EP-yield line.

F066 / provisional KI#387 — CONFIRMED — inherited AdvCiv regression
CvDLLWidgetData::parseTradeItem stores eWhoFrom as PlayerTypes but renders the
TRADE_PEACE, TRADE_WAR and TRADE_EMBARGO labels through:
    GET_TEAM(eWhoFrom).getName()
This treats the player ID as though it were a team ID. Vanilla/Civ4CE BtS uses
    GET_TEAM(GET_PLAYER(eWhoFrom).getTeam()).getName()
and AdvCiv originally preserved that semantics through its TEAMREF(eWhoFrom)
macro, where TEAMREF(ePlayer) was defined as GET_TEAM(TEAMID(ePlayer)).

The regression can be pinned exactly to AdvCiv practical 1679
(1c56cc6ac1...), whose broad TEAMREF-removal refactor mechanically changed:
    TEAMREF(eWhoFrom)
to:
    GET_TEAM(eWhoFrom)
for all three trade-item labels. That loses the required PlayerTypes ->
TeamTypes conversion. In common one-player-per-team games player IDs often
match team IDs and mask the problem. In real team games they need not: hovering
'Make peace with', 'Declare war on' or 'Stop trading with' trade items can name
the unrelated team whose team index happens to equal eWhoFrom, rather than the
from-player's actual team. This is tooltip/diplomacy UI truth, not gameplay
trade execution. Conservative repair: GET_TEAM(GET_PLAYER(eWhoFrom).getTeam())
(or TEAMID(eWhoFrom)) in all three labels.

ADDITIONAL REJECTED / CLEARED ITEMS SINCE WIP02
- MISSION_BUILD feature-tech lookup is safe for builds whose feature prereq is
  NO_TECH: CvTeam::isHasTech(NO_TECH) deliberately returns true, so the branch
  never calls GC.getInfo(NO_TECH).
- MISSION_BUILD getBuildTurnsLeft can theoretically return MAX_INT for zero
  future work rate, but no normal reachable build-action hover with that state
  has yet been established; do not conflate it with the proven GameRecord
  production sentinel root.
- MISSION_DISCOVER validates canDiscover before indexing the discovery tech;
  canDiscover explicitly rejects NO_TECH and zero research.
- MISSION_HURRY's production key is protected by the surrounding production-
  building/action semantics; no independent null-string defect established.
- The potential MISSION_LEAD queued-destination discrepancy is not promoted:
  the actual warlord experience action resolves the units present at execution
  time, and there is not yet a defensible contract that the hover should
  predict a queued destination stack.

ANCESTRY DETAIL FOR F066
Tracked commit-diff evidence is local in:
  LLM_Helpers/context/commit_diffs/AdvCivPreSAS_1679_1c56cc6ac1.diff
The same diff explicitly removes:
  #define TEAMREF(ePlayer) GET_TEAM(TEAMID(ePlayer))
and replaces the three
  TEAMREF(eWhoFrom)
expressions in CvDLLWidgetData with raw GET_TEAM(eWhoFrom). The prior practical
1417 trade-item refactor still used TEAMREF correctly, and Civ4CE/BtS retains
the original explicit GET_PLAYER(eWhoFrom).getTeam() conversion.

NEXT CURSOR
F067 UNUSED.
Continue after parseTradeItem through remaining city/economic/simple wrapper
helpers, then audit execute/click payload symmetry and top-level dispatcher
fallthroughs. After semantic passes, perform whole-file enum/index/null/divisor/
static-state and definition/header/caller closure inventories; re-challenge
F062-F066, check Known-Issue collisions, and only then decide durable C013.
================================================================================


================================================================================
C012-WIP04 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline synchronized to new light source: AdvCiv-SAS practical 6250 /
HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
Durable completion remains 11/169 files. This file remains OPEN.
This checkpoint corrects WIP03 after discovering the overloaded GET_TEAM
contract and reconciles the open audit against the user's newer source tree.
No repository source files were modified by this audit.

CURRENT SOURCE / REVIEW STATUS
------------------------------
The updated source has independently reviewed/staged fixes for:
  KI#383 / F062  WIDGET_LH_GLANCE War Trades hover restoration
  KI#384 / F063  opening-menu Pedia NULL active-civilization dereference
  KI#386 / F065  Great Spy Infiltration tooltip predicate
KI#385 / F064 remains deliberately pending for focused review because the
scoreboard cheat-war reconstruction is a broad diagnostic mirror of AI_doWar.

WIP03 CORRECTION — FORMER F066 / KI#387 IS RETRACTED
----------------------------------------------------
WIP03 claimed that parseTradeItem's GET_TEAM(eWhoFrom), where eWhoFrom is a
PlayerTypes value, treated the player's integer ID as a TeamTypes value. That
claim is false in current AdvCiv/SAS.

The relevant accessor is deliberately overloaded:
  CvGamePlay.h:
    getTeam(TeamTypes eTeam)      -> CvTeam::getTeam(eTeam)
    getTeam(PlayerTypes ePlayer)  -> CvTeam::getTeam(TEAMID(ePlayer))
  CoreAI.h has the analogous pair for CvTeamAI.
And GET_TEAM(x) expands through the overloaded CvGamePlay/CoreAI getter in this
translation-unit environment. Therefore GET_TEAM(eWhoFrom) with a statically
PlayerTypes expression already performs the required PlayerTypes -> TeamTypes
conversion. AdvCiv practical 1679's TEAMREF removal preserved semantics via this
overload; comparing the call text mechanically to vanilla BtS was insufficient.

The new staged explicit conversion:
  GET_TEAM(GET_PLAYER(eWhoFrom).getTeam())
is behaviorally equivalent and harmless, but it does NOT repair a team-game bug.
KI#387's current documentation therefore should be retracted/corrected rather
than preserved as a fixed defect. If the explicit conversion is retained, it is
only a readability/defensive-explicitness change.

This directly repeats the accessor lesson already recorded by rejected KI#311
in the completed historical_source_archaeology.txt sweep. KI#311 traced the AI
translation-unit form, CoreAI::getTeam(PlayerTypes), while this file uses the
parallel non-AI CvGamePlay::getTeam(PlayerTypes) overload. Both resolve through
TEAMID(ePlayer); KI#311 should therefore be cross-linked from KI#387 so either
overload family leads future audits to the shared negative-control evidence.

This is a useful negative control: ancestry can look compelling while a type-
overloaded API preserves the old semantics invisibly at the call site. Future
GET_TEAM(PlayerTypes) findings must inspect overload resolution before promotion.

ROOT LEDGER AFTER RECONCILIATION
--------------------------------
F062 / KI#383 — confirmed; fixed in updated staged source.
F063 / KI#384 — confirmed; fixed in updated staged source.
F064 / KI#385 — confirmed diagnostic drift; intentionally still pending.
F065 / KI#386 — confirmed; fixed in updated staged source.
F066 — UNUSED.
Former WIP03 F066 / provisional KI#387 — RETRACTED false positive.

ADDITIONAL REJECTIONS CARRIED FORWARD
-------------------------------------
- WIDGET_MENU_ICON -> WIDGET_CONSCRIPT fallthrough: no demonstrated harmful
  reachability; MainMenuButton display contract excludes a head-selected city.
- MISSION_BUILD NO_TECH feature lookup: safe through isHasTech(NO_TECH).
- MISSION_BUILD MAX_INT ETA: no normal reachable zero-work action context proved.
- MISSION_DISCOVER NO_TECH: rejected by canDiscover before indexing.
- MISSION_LEAD queued-destination mismatch: no defensible correctness contract.
- parsePowerRatioHelp negative espionage thresholds: intentional margin semantics.
- debug war-rand divide-by-zero: no ordinary major-civ XML-zero route established.

NEXT CURSOR
-----------
F066 UNUSED.
Continue after parseTradeItem through remaining city/economic/simple wrapper
helpers, then execute/click payload symmetry and dispatcher fallthroughs. After
semantic passes, perform whole-file enum/index/null/divisor/static-state and
header/definition/caller inventories; re-challenge F062-F065 against the updated
source and leave F064 clearly pending if no narrow safe repair emerges. Promote
no new root without caller/type-contract evidence.
================================================================================

================================================================================
C012-WIP05 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline: AdvCiv-SAS practical 6250 / HEAD
f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
Durable completion remains 11/169 files. This file remains OPEN.
No repository source files were modified by this audit.

This checkpoint preserves the WIP04 correction that former WIP03 F066/KI#387
was a false positive. Because the user's current staged Known-Issues document
still occupies KI#387 with that retracted claim, the two newly promoted roots
below use the next free provisional slots KI#388 and KI#389 rather than silently
reusing 387. If KI#387 is removed during reconciliation, numbering may of course
be compacted by the maintainer later.

NEW F066 / provisional KI#388 — INFO-SCREEN POWER HOVER IMPORTS SCOREBOARD-ONLY
EXPANSION STATE/TIMER SIDE EFFECT
-------------------------------------------------------------------------------
Current SAS adds WIDGET_POWER_RATIO cells to the Info Screen's custom Score tab:
  Assets/Python/Screens/CvInfoScreen.py
    SASTextScale.setTableTextLabel(...,
      WidgetTypes.WIDGET_POWER_RATIO, ePlayer, iPowerColorForWidget, ...)

CvDLLWidgetData::parseHelp handles WIDGET_POWER_RATIO in two independent ways:
  1. parsePowerRatioHelp(...) renders the intended ratio tooltip;
  2. the post-switch scoreboard expansion block unconditionally calls
       GET_PLAYER(getActivePlayer()).setScoreboardExpanded(true)
     for WIDGET_POWER_RATIO regardless of iData2.
The AdvCiv comment immediately above this special case explicitly says that
WIDGET_POWER_RATIO needs iData2 for another purpose and therefore this WidgetType
"must only" be used on the scoreboard.

SAS practical 5483 / commit e92a36c49dca11cdb4f28526d85887a2c63012f0
introduced the new Info Screen Score tab and reused WIDGET_POWER_RATIO there.
The commit's stated purpose is the sortable Score tab/power display; there is no
scoreboard-expansion intent.

The side effect is real rather than merely a dirty-bit nuisance. In
CvPlayer::setScoreboardExpanded, when the UI is focused (as with an advisor
screen) and Scores__ExpandOnHover is enabled, b=true can set
m_bScoreboardExpanded and schedules UPDATE_COLLAPSE_SCORE_BOARD. A collapse
callback while UI focus remains active calls setScoreboardExpanded(false), but
that focused branch does not clear the state (except diplomacy-specific logic);
it schedules another collapse callback and returns. Thus hovering an Info Screen
power cell can start/maintain the scoreboard's hidden expansion state and timer
loop until focus ends, and can leave the scoreboard briefly/stalely expanded as
the advisor closes before a later callback collapses it.

This is SAS-specific cross-caller contract drift: the tooltip widget was reused
outside the only context its C++ side-effect code says it supports. Gameplay is
not affected, but UI state/timer behavior is. Conservative repair directions
include a context-distinct power-ratio widget/helper or removing/guarding the
scoreboard-expansion side effect for non-scoreboard callers; simply using
WIDGET_GENERAL would avoid the side effect but would also lose the useful power
hover.

Negative control: BUG/Scoreboard.py is the intended original caller and uses
WIDGET_POWER_RATIO on the scoreboard, where expansion-on-hover is desired.

NEW F067 / provisional KI#389 — SPECIALIST "EXTRA SLOTS" REUSES A TECH-CONTEXT
SPECIAL-BUILDING WIDGET WITH NO_TECH, PRODUCING FALSE HOVER TEXT
-------------------------------------------------------------------------------
SAS practical 5331 / commit d0360f3c4757a6b7a13181029bddd8fa2474ec3d
added SevoPediaSpecialist's Extra Slots panel. For grouped special-building
sources (Temple/Cathedral/Monastery), it constructs the button as:
  WIDGET_HELP_SPECIAL_BUILDING, data1=-1, data2=iSpecialBuilding

But CvDLLWidgetData::parseSpecialBuildingHelp forwards data1 as a TechTypes:
  GAMETEXT.buildSpecialBuildingString(buffer,
      (TechTypes)data1, (SpecialBuildingTypes)data2)
and CvGameTextMgr::buildSpecialBuildingString is explicitly a tech-unlock
formatter. It emits "Can Construct ..." when TechPrereq == eTech and
"Any player can Construct ..." when TechPrereqAnyone == eTech.

Current SAS CIV4SpecialBuildingInfos.xml has Temple, Cathedral and Monastery
TechPrereqAnyone=NONE. NONE is -1, exactly the value supplied by the Specialist
page. Therefore the sentinel is not neutral: it matches TechPrereqAnyone and can
make an Extra Slots source button claim "Any player can Construct Temple /
Cathedral / Monastery". That text does not describe why the building class is
shown and is false/misleading in this context.

Controls: CvTechChooser.py and SevoPediaTech.py also use
WIDGET_HELP_SPECIAL_BUILDING, but correctly pass a real tech ID in data1. The
C++ helper therefore behaves according to its intended contract; the defect is
the SAS Specialist-page caller's widget/payload choice, discovered through this
C++ contract audit.

Conservative repair: do not feed NO_TECH to this tech-context widget for the
Extra Slots grouping. Use a neutral/non-tech widget if the generic special-
building icon is only representative, or introduce a dedicated Pedia/source
hover that describes the special-building family/slot relationship.

ROOT LEDGER AT THIS CHECKPOINT
------------------------------
F062 / KI#383 — confirmed; fixed in updated staged source.
F063 / KI#384 — confirmed; fixed in updated staged source.
F064 / KI#385 — confirmed diagnostic drift; intentionally still pending.
F065 / KI#386 — confirmed; fixed in updated staged source.
Former WIP03 F066 / KI#387 — RETRACTED false positive (GET_TEAM overload).
F066 / provisional KI#388 — promoted; InfoScreen WIDGET_POWER_RATIO triggers
  scoreboard-only expansion state/timer behavior.
F067 / provisional KI#389 — promoted; Specialist Extra Slots feeds NO_TECH to
  a tech-context special-building widget and gets false sentinel-matched hover.
F068 — UNUSED.

ADDITIONAL REJECTIONS / NEGATIVE CONTROLS
------------------------------------------
- WIDGET_MENU_ICON -> WIDGET_CONSCRIPT fallthrough remains rejected by the
  MainMenuButton display/selected-city contract.
- parsePowerRatioHelp's negative EP threshold remains intentional margin
  semantics; F066 concerns the independent post-switch scoreboard side effect.
- WIDGET_HELP_SPECIAL_BUILDING itself is not generically defective: its Tech
  Chooser and SevoPedia Tech callers supply real tech IDs and match its contract.
- Former parseTradeItem GET_TEAM(eWhoFrom) finding remains retracted: overload
  resolution already converts PlayerTypes to that player's TeamTypes.

NEXT CURSOR
-----------
F068 UNUSED.
Continue the remaining encoded-widget payload/caller audit (finance, city trade,
deal/advisor/Pedia helpers), mechanically reconcile parseHelp/executeAction/
executeAltAction/isLink switch cases and fallthroughs, then perform the full
whole-file enum/index/null/divisor/static-state and definition/header/caller
closure inventory. Re-challenge F062-F067 against the current practical-6250
source before deciding WIP vs durable C013.
================================================================================

================================================================================
C012-WIP06 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline remains AdvCiv-SAS practical 6250 / HEAD
f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
Durable completion remains 11/169 files. This file remains OPEN.
No repository source files were modified by this audit.

This checkpoint preserves WIP05 and records two important refinements from the
encoded-widget caller audit: F066 is broader than its first power-cell example,
and a new independent misleading-hover root F068 is promoted. Former WIP03
F066/KI#387 remains RETRACTED; the current source's explicit player->team
conversion is behaviorally redundant because GET_TEAM(PlayerTypes) already
performs TEAMID(ePlayer).

F066 / provisional KI#388 — BROADENED: INFO SCREEN SCORE TAB REUSES THREE
SCOREBOARD-ONLY EXPANSION WIDGETS, IMPORTING HIDDEN SCOREBOARD STATE/TIMERS
-------------------------------------------------------------------------------
WIP05 first isolated this through the custom Score tab's WIDGET_POWER_RATIO
cells. The caller audit now proves the same root at three independent Score-tab
cell families in Assets/Python/Screens/CvInfoScreen.py:

  active score cell:  WIDGET_SCORE_BREAKDOWN with data2=0
  power ratio cell:   WIDGET_POWER_RATIO
  trade-network cell: WIDGET_TRADE_ROUTES_SCOREBOARD

CvDLLWidgetData::parseHelp's post-switch expansion block treats all three as
scoreboard controls:
  - WIDGET_SCORE_BREAKDOWN expands when data2==0;
  - WIDGET_POWER_RATIO expands unconditionally;
  - WIDGET_TRADE_ROUTES_SCOREBOARD expands unconditionally.

The surrounding AdvCiv comment explicitly documents the scoreboard-only data
contract for this machinery (notably WIDGET_POWER_RATIO's occupied data2).
CvPlayer::setScoreboardExpanded then mutates the active player's scoreboard
expansion state and schedules UPDATE_COLLAPSE_SCORE_BOARD callbacks. While an
advisor owns UI focus, collapse attempts can reschedule rather than clear the
state, so hovering these Info Screen cells can create/maintain hidden scoreboard
state and timer churn until focus ends.

This is best kept as ONE cross-context root, not three findings: the custom Info
Screen Score tab reused scoreboard UI widgets for their useful text hovers but
also inherited their unrelated scoreboard-expansion behavior. The power cell
was simply the first observable instance. Repair should separate tooltip
semantics from scoreboard-only state mutation or guard the expansion side effect
to actual scoreboard context.

NEW F068 / provisional KI#390 — MILITARY-ADVISOR "TOTAL COST + SUPPLY" ROW USES
THE GLOBAL-INFLATED-EXPENSES TOOLTIP, SO ITS HOVER EXPLAINS UNRELATED COSTS
-------------------------------------------------------------------------------
The custom Military Advisor Summary tab displays:
  Total Cost + Supply = post-inflation Unit Cost + post-inflation Unit Supply.
The row binds:
  WIDGET_HELP_FINANCE_INFLATED_COSTS, data1=self.iActivePlayer, data2=1.

CvDLLWidgetData::parseFinanceInflatedCosts does NOT describe that subtotal. It
calls CvGameTextMgr::buildFinanceInflationString, which obtains:
  CvPlayer::calculatePreInflatedCosts()
and the engine definition is:
  calculateUnitCost() + calculateUnitSupply() + getTotalMaintenance()
    + getCivicUpkeep() + Python extraExpenses().
Thus the hover's pre-inflation base and inflation amount can include city
maintenance, civic upkeep and other expenses that are absent from the visible
"Total Cost + Supply" value.

The original/current Domestic Finance screen is the clean control: it attaches
WIDGET_HELP_FINANCE_INFLATED_COSTS to totalInflatedCosts / the overall Expenses
row, where the helper's global expense-base contract matches the displayed
number. The Summary tab's own introductory comment says rows bind the matching
finance widget "where applicable", and its legend defines Total Cost + Supply
specifically as Unit Cost + Unit Supply. Therefore this is not an intentional
whole-economy hover; it is a cross-caller widget mismatch introduced with the
Military Advisor Summary tab (initial implementation practical 5800).

This is diagnostic/UI accuracy only, not gameplay. Conservative repair: use no
hover on this subtotal, combine the already-correct Unit Cost and Away Supply
breakdowns in a dedicated helper, or add a purpose-specific Cost+Supply tooltip.
Do not use the global inflation/total-expenses helper for this row.

ADDITIONAL NEGATIVE CONTROL
---------------------------
- The Summary tab's WIDGET_HELP_FINANCE_UNIT_COST and
  WIDGET_HELP_FINANCE_AWAY_SUPPLY reuses are legitimate: their C++ helpers call
  the exact player Unit Cost and Unit Supply breakdown builders. The mismatch is
  specific to the combined Total Cost + Supply row.
- WIDGET_HELP_FINANCE_NUM_UNITS is intentionally disabled by AdvCiv and therefore
  produces no tooltip; using it on the Summary Total Units row is a no-op rather
  than a false breakdown, so it is not promoted as a separate bug at this point.

ROOT LEDGER AT THIS CHECKPOINT
------------------------------
F062 / KI#383 — confirmed; fixed in updated staged source.
F063 / KI#384 — confirmed; fixed in updated staged source.
F064 / KI#385 — confirmed diagnostic drift; intentionally still pending.
F065 / KI#386 — confirmed; fixed in updated staged source.
Former WIP03 F066 / KI#387 — RETRACTED false positive (GET_TEAM overload).
F066 / provisional KI#388 — broadened: Info Screen Score tab reuses scoreboard-
  only WIDGET_SCORE_BREAKDOWN / WIDGET_POWER_RATIO /
  WIDGET_TRADE_ROUTES_SCOREBOARD expansion widgets.
F067 / provisional KI#389 — Specialist Extra Slots misuses tech-context
  WIDGET_HELP_SPECIAL_BUILDING with NO_TECH.
F068 / provisional KI#390 — Military Summary Total Cost + Supply uses global
  inflated-expenses hover whose numbers include unrelated expenses.
F069 — UNUSED.

NEXT CURSOR
-----------
F069 UNUSED. Continue encoded-widget payload/caller audit, especially remaining
SAS-added advisor/Pedia help-widget reuses; then finish inherited C++ sections,
parse/execute/isLink symmetry, exact definition/header/caller inventory, and
whole-file enum/index/null/divisor/static-state closure. Re-challenge all live
roots before durable C013.
================================================================================

================================================================================
C012-WIP07 — file 012 CvGameCoreDLL/CvDLLWidgetData.cpp — INCOMPLETE CHECKPOINT
================================================================================
Baseline: AdvCiv-SAS practical 6250 / HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
Durable completion remains 11/169 files. This file remains OPEN.
No repository source files were modified by this audit.

This checkpoint preserves WIP06 and records one new inherited cross-file payload
root, F069. Former WIP03 F066/KI#387 remains RETRACTED; current normalized roots
are F062-F069 with the current F066 numbering beginning at KI#388.

NEW F069 / provisional KI#391 — OPTIONAL BUG DETAILED/WIDE TECH SPLASH PASSES
BUILD ID AS TECH ID AND HARD-CODES BUILD ID 1 TO WIDGET_HELP_IMPROVEMENT
-------------------------------------------------------------------------------
Both live optional BUG tech-splash implementations contain the same caller:

  Assets/Python/Contrib/TechWindow.py
  Assets/Python/Contrib/TechWindowWide.py

For a build enabled by the displayed technology whose BuildInfo has no
ImprovementType, they attach the build button as:

  WIDGET_HELP_IMPROVEMENT, j, 1

where j is the BuildTypes index.

The C++ widget contract is the opposite kind of payload:

  CvDLLWidgetData::parseBuildHelp
    data1 -> TechTypes
    data2 -> BuildTypes

and forwards them to:

  CvGameTextMgr::buildImprovementString(szBuffer, eTech, eBuild)

which tests the supplied build's technology prerequisite/feature technologies
against the supplied technology and, on a match, prints "Can build <Build>".

The normal/current CvTechChooser is the control: it passes (iTech, j), i.e. the
actual technology in data1 and actual BuildTypes index in data2.

Concrete current-SAS reachability:
  Build 0  BUILD_ROAD          -> TECH_THE_WHEEL
  Build 1  BUILD_RAILROAD      -> TECH_STEAM_POWER
  Build 18 BUILD_REMOVE_JUNGLE -> feature tech TECH_METAL_CASTING
  Build 19 BUILD_REMOVE_FOREST -> feature tech TECH_MASONRY
  Build 20 BUILD_SCRUB_FALLOUT -> feature tech TECH_ENVIRONMENTALISM

All five have ImprovementType=NONE and therefore use this erroneous TechWindow
branch. data2 is always 1, i.e. BUILD_RAILROAD. data1 becomes raw build IDs
0/1/18/19/20, which in current TechInfos correspond to unrelated technologies
(Mysticism, Meditation, Liberalism, Democracy, Corporation). None is Steam
Power, so buildImprovementString finds no match and the intended hover is empty.
The same payload also prevents executeAltAction's WIDGET_HELP_IMPROVEMENT path
from jumping anywhere useful because Build 1 has no ImprovementType.

This is not SAS-specific. The exact bad pair is already present when the BUG
TechWindow assets enter reachable K-Mod history in practical 4 / commit
593c549b51b5726302484ad8d0e08602b010d4a9 ("assets to go with the initial
commit"). Both Detailed and Wide variants contain it there. The UI remains live:
Assets/Config/TechWindow.xml exposes View = Original|Detailed|Wide and
CvScreensInterface.createTechSplash imports TechWindow/TechWindowWide when the
corresponding user option is selected.

Classification: inherited BUG/K-Mod UI tooltip/payload bug; no gameplay effect.
Conservative repair: pass self.iTech (or the displayed technology ID) as data1
and j as data2 in both files, matching CvTechChooser's established contract.

ADDITIONAL NEGATIVE CONTROLS / PROGRESS
---------------------------------------
- Shifted mission-help plot dereference is safe: CvSelectionGroup::lastMissionPlot
  falls back to the group's current plot when no queued movement target exists.
- WIDGET_HELP_OBSOLETE / _BONUS / _SPECIAL casts in CvDLLWidgetData look odd
  locally, but their CvGameTextMgr functions intentionally accept an int item ID
  and reinterpret it as BuildingTypes / BonusTypes / SpecialBuildingTypes; the
  current TechChooser callers therefore match the real callee contract.
- WIDGET_HELP_TERRAIN_TRADE's -1 hazard is not reached by current callers:
  terrain callers pass a real TerrainTypes index and river trade deliberately
  uses getNumTerrainInfos() as its sentinel, which parseTerrainTradeHelp handles.

ROOT LEDGER AT THIS CHECKPOINT
------------------------------
F062 / KI#383 — confirmed; fixed in updated source.
F063 / KI#384 — confirmed; fixed in updated source.
F064 / KI#385 — confirmed diagnostic drift; intentionally still pending.
F065 / KI#386 — confirmed; fixed in updated source.
Former WIP03 F066 / KI#387 — RETRACTED false positive (GET_TEAM overload).
F066 / KI#388 — Info Screen Score tab imports scoreboard-only expansion state.
F067 / KI#389 — Specialist Extra Slots misuses tech-context special-building help.
F068 / KI#390 — Military Summary Cost+Supply uses whole-expense inflation hover.
F069 / provisional KI#391 — BUG Detailed/Wide Tech Splash mis-encodes
  WIDGET_HELP_IMPROVEMENT payload; build-action hover is empty/wrong.
F070 — UNUSED.

KI#391 collision check against the practical-6250 docs/source: no existing hit.

NEXT CURSOR
-----------
F070 UNUSED. Continue the inherited parseActionHelp/mission/action payload audit
and mechanically enumerate remaining SAS-added/nonstandard widget callers.
Then finish parseHelp/executeAction/executeAltAction/isLink symmetry, reconcile
168 live C++ definitions vs 170 header declarations (doCancelCivics/applyCityEdit
currently appear dead), and perform whole-file enum/index/null/divisor/static-
state closure. Re-challenge F062-F069 before durable C013.
================================================================================


================================================================================
C012-WIP08 — FILE 012 CvDLLWidgetData.cpp — SAFETY CHECKPOINT
Baseline synchronized to AdvCiv-SAS practical 6250 / HEAD
f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
Status: INCOMPLETE. Durable completion remains 11/169. No source modified.
================================================================================

NORMALIZED ROOT LEDGER AT THIS CHECKPOINT

F062 / KI#383 — LH Glance vassal-helper refactor dropped parseWarTradesHelp.
  - SAS-specific regression; fixed in the current practical-6250 source.

F063 / KI#384 — executeAltAction opening-menu Pedia active-civilization dereference.
  - AdvCiv inherited; fixed in current source.

F064 / KI#385 — parseScoreboardCheatText is a stale duplicate of AI_doWar.
  - Diagnostic/debug-only drift; intentionally pending in current source.

F065 / KI#386 — MISSION_INFILTRATE hover tested canEspionage instead of canInfiltrate.
  - BtS/Civ4CE inherited; fixed in current source.

Former WIP03 F066 / KI#387 — RETRACTED FALSE POSITIVE.
  - GET_TEAM(PlayerTypes) is intentionally overloaded and performs TEAMID(ePlayer).
  - The current explicit player->team spelling is behaviorally harmless/readability-only;
    the old code did not confuse PlayerTypes with TeamTypes at runtime.

F066 / KI#388 — Info Screen Score tab reuses scoreboard-only expansion widgets.
  - CvInfoScreen.py uses WIDGET_SCORE_BREAKDOWN(data2=0), WIDGET_POWER_RATIO,
    and WIDGET_TRADE_ROUTES_SCOREBOARD.
  - CvDLLWidgetData::parseHelp treats these as scoreboard-hover controls and calls
    setScoreboardExpanded(true), importing hidden scoreboard state/timer behavior into
    an advisor screen. The C++ comments describe these widget uses as scoreboard-only.
  - Origin: SAS practical 5483 / e92a36c49...

F067 / KI#389 — SevoPedia Specialist Extra Slots misuses tech-context
WIDGET_HELP_SPECIAL_BUILDING with data1=-1.
  - buildSpecialBuildingString interprets data1 as TechTypes and compares it with both
    the normal prerequisite and TechPrereqAnyone.
  - Temple/Cathedral/Monastery special-building definitions use NONE (-1) for the
    anyone-tech prerequisite, so the sentinel can match and generate misleading
    "Any player can Construct ..." hover text instead of explaining slot provenance.
  - Origin: SAS practical 5331 / d0360f3c47...

F068 / KI#390 — Military Advisor "Total Cost + Supply" uses the wrong finance tooltip.
  - Cell displays post-inflation Unit Cost + Unit Supply only.
  - WIDGET_HELP_FINANCE_INFLATED_COSTS explains calculatePreInflatedCosts(), which also
    includes city maintenance, civic upkeep and Python extra expenses.
  - The normal Finance total-expense row is the correct control for that widget.
  - Origin: SAS practical 5800.

F069 / provisional KI#391 — BUG Detailed/Wide Tech Splash passes the wrong payload to
WIDGET_HELP_IMPROVEMENT for non-improvement build actions.
  - Bad caller shape in both TechWindow.py and TechWindowWide.py: (data1=j, data2=1),
    where j is BuildTypes.
  - C++ contract: data1 = TechTypes, data2 = BuildTypes.
  - In current XML build ID 1 is BUILD_RAILROAD; affected build buttons include Road,
    Railroad, Remove Jungle, Remove Forest and Scrub Fallout. The resulting hover asks
    whether Railroad is unlocked by unrelated tech IDs equal to those build indices,
    and therefore loses the intended build-help text.
  - Correct control: CvTechChooser supplies (iTech, iBuild).
  - Present since K-Mod practical 4 / 593c549b51...

F070 / provisional KI#392 — Detailed Tech Splash has an undefined yWidgetTypes symbol.
  Evidence / reachability:
  - Assets/Python/Contrib/TechWindow.py line 253 currently executes:
      ... yWidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT, eLoopUnit, 1, False
    when the researched technology enables a civilization unit.
  - No symbol/import/alias named yWidgetTypes exists anywhere else in the Python tree.
  - Assets/Python/Contrib/TechWindowWide.py uses the correct WidgetTypes symbol at the
    corresponding call; base AdvCiv also uses WidgetTypes.
  - CvScreensInterface.createTechSplash imports TechWindow when the user selects the
    live TechWindow "Detailed" view; TechWindow.xml exposes Original|Detailed|Wide.
    Thus a normal tech-acquisition splash that unlocks a unit can execute this branch
    and raise Python NameError, disrupting Detailed Tech Splash rendering.
  Ancestry:
  - Introduced by SAS practical 4847 / commit
    2973ba48f3bd65d0a85956ed5aee0457858dc621 during Ruff/Pylance cleanup.
  - The commit diff shows the prior correct two-line
      WidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT
    call collapsed into a one-line call with the accidental leading "y".
  - No KI#392 collision found in the current docs/source.
  Repair direction:
  - Replace yWidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT with
    WidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT; no broader behavior change is needed.

F071 UNUSED — next normalized root number.

ADDITIONAL CLOSURE / NEGATIVE-CONTROL RESULTS SINCE WIP07

1. Header inventory discrepancy is old dead declaration residue, not broken wiring.
   - Current CvDLLWidgetData.cpp: 168 live out-of-line definitions.
   - Header: 170 declarations.
   - The only declaration-only names are doCancelCivics() and applyCityEdit().
   - No callers/definitions exist in current CvGameCoreDLL, base AdvCiv or Civ4CE.
   - K-Mod practical 3 / KMod_3_2414225e64.diff already introduced these two header
     declarations without implementations. Keep as inherited dead residue unless a
     cleanup task is desired; do not classify as a runtime bug.

2. WIDGET_CHOOSE_EVENT active-player use is valid.
   - Its live caller is CvDLLButtonPopup's in-game triggered-event popup; this is not an
     opening-menu/Pedia path. The unguarded active player in parseEventHelp therefore
     has the intended caller contract.

3. Shifted mission plot dereference is valid.
   - CvSelectionGroup::lastMissionPlot() falls back to the group's current plot, so
     parseActionHelp_Mission does not obtain NULL merely because there is no queued
     shifted destination.

4. MISSION_BUILD / NO_TECH paths are sentinel-aware.
   - CvTeam::isHasTech(NO_TECH) intentionally succeeds for the no-prerequisite case.
   - The Sevopedia WIDGET_HELP_IMPROVEMENT NO_TECH caller is also intentional:
     CvGameTextMgr::buildImprovementString handles feature-specific prerequisite techs
     when BuildInfo::getTechPrereq()==NO_TECH.

5. MISSION_JOIN / HURRY and several other apparent null/sentinel cases remain protected
   by their can* caller predicates; no F071 was justified from them.

6. Scoreboard-expansion follow-up in progress.
   - WIDGET_PEDIA_JUMP_TO_TECH also participates in scoreboard expansion when data2==0.
   - The actual scoreboard deliberately passes data2=0.
   - Obvious non-scoreboard Tech/Pedia callers inspected so far pass -1 or 1, not zero.
   - Continue enumerating CONTACT_CIV / DEAL_KILL / PEDIA_JUMP_TO_TECH and other
     expansion-list callers before deciding whether any root exists beyond F066.

EXACT RECOVERY CURSOR
- Durable completion: 11/169.
- File 012 CvDLLWidgetData.cpp remains OPEN.
- Baseline: practical 6250 / HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314.
- Live normalized roots: F062-F070; former KI#387 retracted.
- Next root number: F071 UNUSED.
- Continue: finish hidden-scoreboard-expansion caller enumeration; compare remaining
  Detailed/Wide TechWindow payloads and identifiers; mechanically audit SAS-added
  Python widget payloads against CvDLLWidgetData contracts; finish inherited
  parseActionHelp/executeAction/executeAltAction/isLink symmetry; final enum/index/
  null/divisor/static-state/caller pass; adversarial reread F062-F070; decide C013 or
  another WIP.
================================================================================


================================================================================
C012-WIP09 — file 012 CvDLLWidgetData.cpp — INCOMPLETE SAFETY CHECKPOINT
Baseline: AdvCiv-SAS practical 6250 / HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314
Durable completion remains 11/169. File 012 remains OPEN. No source modified.
Prior cumulative prefix: C012-WIP08 preserved byte-for-byte.

NORMALIZED ROOT LEDGER AT THIS CHECKPOINT
- F062 / KI#383 — LH Glance vassal-helper refactor dropped parseWarTradesHelp. Fixed in current source.
- F063 / KI#384 — executeAltAction opening-Pedia active-civilization dereference. Fixed in current source.
- F064 / KI#385 — scoreboard cheat war-decision mirror drifted from actual AI_doWar. Pending intentionally.
- F065 / KI#386 — Infiltrate hover used canEspionage instead of canInfiltrate. Fixed in current source.
- Former WIP03 F066 / KI#387 — RETRACTED. GET_TEAM(PlayerTypes) already performs TEAMID conversion.
- F066 / KI#388 — Info Screen Score tab reuses scoreboard-only expansion widgets, importing scoreboard expansion/timer state.
- F067 / KI#389 — SevoPedia Specialist Extra Slots passes data1=-1 into tech-context WIDGET_HELP_SPECIAL_BUILDING, enabling NONE-tech sentinel matches and misleading hover text.
- F068 / KI#390 — Military Advisor Total Cost + Supply uses whole-expense WIDGET_HELP_FINANCE_INFLATED_COSTS and displays unrelated maintenance/upkeep/etc. in hover.
- F069 / KI#391 — BUG Detailed/Wide Tech Splash passes WIDGET_HELP_IMPROVEMENT as (BuildTypes j, 1) instead of (TechTypes tech, BuildTypes build), losing intended non-improvement build hover.
- F070 / KI#392 — Detailed Tech Splash uses undefined yWidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT and can NameError when a researched tech unlocks a unit.
- F071 / provisional KI#393 — AdvCiv multi-unit city-bombard previews sum selected units that cannot actually execute the mission.
- F072 / provisional KI#394 — AdvCiv Air Bomb mission prioritization does not filter per-unit target eligibility and can choose an invalid aircraft, causing a valid group mission to abort with no action.
- F073 UNUSED.

F071 / provisional KI#393 — MULTI-UNIT BOMBARD PREVIEWS COUNT INELIGIBLE UNITS

CvDLLWidgetData.cpp MISSION_BOMBARD help (current ~2914):
  for each UI-selected unit:
      iDamage += kSelectedUnit.damageToBombardTarget(kMissionPlot);

The preview does not require kSelectedUnit.canMove() or
kSelectedUnit.canBombard(kMissionPlot). By contrast, actual execution in
CvSelectionGroup::startMission repeatedly calls
AI_bestUnitForMission(MISSION_BOMBARD), and that helper explicitly skips units
that cannot move or fail canBombard(kAt).

Reachable example: a selected siege group contains one fresh siege unit and one
unit that has already spent its attack/bombard for the turn. At least one unit
can still bombard, so the action remains available. The tooltip sums nominal
damage from both; execution skips the spent unit. The preview can therefore
promise more city-defense reduction than the mission can deliver.

Sibling in CvGameTextMgr::getAirBombPlotHelp:
  - the helper first establishes that one best unit can air-bomb the hovered
    target;
  - for revealed city targets it then loops over every selected unit and adds
    airBombDefenseDamage without checking canMove/canAirBombAt(target).
Thus a fresh eligible aircraft plus an exhausted or out-of-range selected
companion can similarly overstate city-defense damage.

Concrete SAS XML range control: Fighter range 6 / bomb rate 8; Bomber range 8 /
bomb rate 16; Jet Fighter range 10 / bomb rate 12; Stealth Bomber range 12 /
bomb rate 20; Guided Missile range 5 / bomb rate 16. For a city 7 tiles away,
a Bomber can be valid while a selected Fighter is out of range; once the valid
best-unit gate passes, the city preview still adds the Fighter's nominal
airBombDefenseDamage.

Ancestry: the ground and air multi-unit previews were introduced together in
AdvCiv practical 2778 / commit 4098fbb24851e4cdc6f2f615f4458adedf97da75,
"Various UI improvements for bombard and air bomb missions." The same commit
also added per-unit eligibility-aware execution, making the omitted tooltip
filters especially clear.

Conservative repair direction:
  - ground preview: add damage only for units that can move and
    canBombard(kMissionPlot) (or otherwise mirror the actual mission selector);
  - air-city preview: add damage only for units that can move and
    canAirBombAt(kPlot).
Do not merely test bombard/air-bomb base rate: target/move-state eligibility is
the mismatch.

F072 / provisional KI#394 — AIR BOMB BEST-UNIT SELECTION CAN ABORT A VALID GROUP ORDER

Current CvSelectionGroupAI::AI_bestUnitForMission(MISSION_AIRBOMB, pMissionPlot):
  - common loop filters !pUnit->canMove();
  - city branch tests pUnit->canAirBomb(pMissionPlot), NOT canAirBombAt;
  - CvUnit::canAirBomb's pFrom parameter is explicitly unused and it tests only
    generic air-bomb ability / attack-spent state;
  - non-city branch has no target-eligibility test at all.

Actual group execution in CvSelectionGroup::startMission is:
  while ((pUnit = AI_bestUnitForMission(MISSION_AIRBOMB, &kMissionPlot)) != NULL &&
         pUnit->airBomb(kMissionPlot, ...))
      ...
CvUnit::airBomb begins with canAirBombAt(kTarget) and returns false if the chosen
unit cannot attack that target. Because the while condition then fails, the
mission loop stops instead of trying another eligible selected aircraft.

CvSelectionGroup::canDoMission(MISSION_AIRBOMB) proves normal reachability:
it returns true if ANY selected unit canAirBombAt(target), so invalid companion
units are allowed to remain in a group whose Air Bomb order is valid.

Concrete current-SAS example from CIV4UnitInfos.xml:
  UNIT_BOMBER: iBombRate=16, iAirRange=8, cost=180
  UNIT_JET_FIGHTER: iBombRate=12, iAirRange=10, cost=170
Against a city 9 tiles away, the Jet Fighter is target-valid and makes the
mission available; the Bomber is out of range. City prioritization nevertheless
uses nominal airBombDefenseDamage and can prefer the higher-rate Bomber. The
chosen Bomber's airBomb(target) then rejects range 9 and the while loop stops,
so the valid Jet Fighter never acts. The same eligibility omission exists for
non-city air-bomb targets.

Ancestry:
  - practical 2778 / 4098fbb248... introduced the smarter grouped Air Bomb
    ordering with the same generic canAirBomb test for city targets and no
    target filter in the non-city ordering;
  - practical 2784 / 160fa499a896cf595f74bcbde68b7ce41bedc466 factored the
    ordering into CvSelectionGroupAI::AI_bestUnitForMission and preserved the
    missing target-eligibility check exactly;
  - later bombard-priority work (e.g. practical 2809) retained it.

Conservative repair direction: in MISSION_AIRBOMB selection, reject any unit
that does not pUnit->canAirBombAt(*pMissionPlot) (using the proper current
signature/origin semantics) before ranking it. That aligns selection with
canDoMission, CvUnit::airBomb, and the target-specific mission contract.

NEGATIVE/CONTROL RESULTS ADDED SINCE WIP08
- General scoreboard-expansion caller enumeration found no new root beyond F066.
  Ordinary non-scoreboard callers pass the non-expanding data2 variants; the
  Info Screen Score tab remains the exceptional cross-context reuse.
- Detailed-vs-Wide TechWindow differential produced no further sibling mismatch
  beyond F069/F070.
- Shifted lastMissionPlot remains safe through its current-plot fallback.
- MISSION_BUILD NO_TECH and several JOIN/HURRY/Great-General caller-contract
  suspicions remain rejected.

EXACT NEXT CURSOR
File 012 remains open. F073 unused.
Continue inherited parseActionHelp_Mission and action/command help semantics;
then mechanical parseHelp/executeAction/executeAltAction/isLink payload symmetry,
remaining Python->C++ widget payloads, enum/index/null/divisor/state checks,
caller/header closure, final adversarial reread F062-F072, and decide durable
C013 vs another WIP.
================================================================================


================================================================================
C012-WIP10 — file 012 CvDLLWidgetData.cpp — INCOMPLETE SAFETY CHECKPOINT
Baseline: AdvCiv-SAS practical 6250 / HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314
Durable completion remains 11/169. File 012 remains OPEN. No source modified.
Prior cumulative prefix: C012-WIP09 preserved byte-for-byte.

NORMALIZED LEDGER DELTA SINCE WIP09
- F071 / provisional KI#393 is broadened, not split: the same causal UI-family
  defect also affects Paradrop interception help. Multi-unit special-mission
  previews choose/aggregate selected units without first filtering to the units
  that can actually execute the hovered mission at the target.
- F072 / provisional KI#394 unchanged and still independent: Air Bomb executor
  ranking itself lacks target-specific eligibility and can abort a valid group
  order by choosing an invalid aircraft first.
- F073 remains UNUSED.

F071 PARADROP SIBLING (ADDED AFTER WIP09)
CvGameTextMgr::getParadropPlotHelp chooses the selected unit with the greatest
interception-evasion value before applying target/move eligibility. It then
emits interception help only if that chosen unit canParadropAt the target.

This differs from actual group execution: CvSelectionGroupAI::AI_bestUnitForMission
(MISSION_PARADROP) first requires canMove and canParadropAt(target), then ranks
eligible units. Current SAS has UNIT_PARATROOPER with equal drop/evasion stats,
so a normal mixed-move-state selection is enough: one tied Paratrooper has
already moved, another is fresh. The fresh unit keeps the Paradrop order valid
and is the unit execution can use, but the help loop can retain the moved tied
unit and then suppress interception information when its final canParadropAt
check fails.

This is normalized under F071 because its cause and repair principle are the
same as the already-recorded Ground Bombard and Air Bomb preview cases: first
restrict the help population to mission-executable units at the target, then
aggregate/rank them. Do not create a separate finding merely for each mission.

REJECTED F073 CANDIDATE — MISSION_HEAL MAX_INT IS SEMANTICALLY REAL
parseActionHelp_Mission computes the group Heal ETA as the maximum healTurns()
of all selected units, and CvUnit::healTurns returns MAX_INT when a hurt unit's
net heal rate at the plot is <= 0. At first this looked like another raw sentinel
leak.

Engine semantics show otherwise:
- CvUnit::canHeal rejects healTurns()==MAX_INT, so such a unit alone cannot make
  the Heal mission available.
- If another selected unit can heal, the group can nevertheless enter
  ACTIVITY_HEAL.
- CvUnit::doTurn applies feature turn-damage and then doHeal to hurt stationary
  units; the group remains ACTIVITY_HEAL while any member remains hurt (for a
  human group, absent the separate sentry-danger wake condition).
- Therefore a hurt selected unit whose healing is exactly cancelled/exceeded by
  feature turn damage really can keep the group from ever becoming fully healed
  at that plot. MAX_INT is meaningful as an infinite group ETA in that state,
  not a finite-production-style sentinel masquerading as ordinary data.

Result: reject this candidate. F073 stays unused.

OTHER POST-WIP09 CONTROLS
- Airlift, Recon and Range Strike group execution re-validates target eligibility
  per selected unit; one invalid companion does not abort later valid units.
- Paradrop execution is properly eligibility-filtered; only its interception
  help selection shares F071's preview-population defect.
- The apparent WIDGET_HELP_OBSOLETE parse/alt payload mismatch is not a bug:
  buildObsoleteString takes an int item/building identifier despite the local
  TechTypes cast annotation, and the callers/alt-action consistently encode a
  BuildingTypes value.
- WIDGET_HELP_FREE_UNIT intentionally dual-encodes unit in data1 and tech in
  data2; parse and alt-action consume the appropriate half.

CURRENT-TREE RECONCILIATION AFTER WIP10
- The behaviorally redundant explicit player-to-team conversion described by
  the earlier checkpoints was removed during review. Current source retains
  GET_TEAM(eWhoFrom) plus a warning that CvGamePlay::getTeam(PlayerTypes)
  converts through TEAMID, cross-referencing the parallel CoreAI overload in
  rejected KI#311. KI#387 remains the local rejected false-positive record.
- Direct current-source checks support provisional KI#391's payload mismatch
  and confirm KI#392's undefined yWidgetTypes identifier. KI#393 and KI#394
  remain plausible, source-supported mission-family roots pending focused
  implementation review and runtime validation; none is promoted as fixed here.

EXACT NEXT CURSOR
File 012 remains open. F073 unused.
Continue the remaining generic vassal-tooltip scope challenge and encoded-widget
payload/dispatcher symmetry; then finish inherited action/command help,
enum/index/null/divisor/state and caller/header closure, adversarial reread
F062-F072, and decide durable C013 vs another WIP.
================================================================================


================================================================================
C012-WIP14 RECONSTRUCTED CONTINUATION — file 012 CvDLLWidgetData.cpp — OPEN
Authoritative current source: AdvCiv-SAS practical 6250 / HEAD f0ee784b6444e9a0ebedb67722fa4b89f9e29314
Reconstruction note: the attached C012-WIP10 artifact above is preserved exactly as the physical prefix. The original WIP11-WIP13 files were not re-uploaded into this new chat; their deltas below are reconstructed from the preserved previous-thread transcript and rechecked against the current practical-6250 source where relevant. This avoids pretending byte-for-byte possession of artifacts that are no longer mounted while preserving the audit state faithfully. No source files modified.

RECOVERED WIP11 DELTA
F073 / provisional KI#395 — BUG/AdvCiv Chevron angry-citizen overlay uses the specialist widget contract
Status: inherited current UI defect; source/caller contract strong.
Evidence:
- The Chevron city-specialist display creates the ordinary angry-citizen button with WIDGET_ANGRY_CITIZEN.
- It then draws a 10x10 DDS chevron directly over that angry-citizen button but tags the overlay WIDGET_CITIZEN with data1=j.
- The neighboring specialist chevrons intentionally use WIDGET_CITIZEN because they really are specialist overlays; the angry-citizen branch is the copy/paste outlier.
- addDDSGFC carries its own widget/data payload. Hovering the overlay therefore routes through parseCitizenHelp with j interpreted as a SpecialistTypes value; right-click routes to that specialist's Pedia instead of the angry-citizen contract.
Repair direction:
- Tag the angry-citizen chevron overlay with WIDGET_ANGRY_CITIZEN and the same payload convention as the underlying angry-citizen button, or make the overlay non-interactive if the engine permits that without breaking rendering.
Rejected neighboring smell:
- WIDGET_HELP_OBSOLETE's local TechTypes cast is misleading but not a runtime payload bug; buildObsoleteString consumes the integer as a Building ID, matching the caller/right-click contract.

RECOVERED WIP12 DELTA
F074 / provisional KI#396 — native Bonus/Tech trade-denial widgets lost their old Pedia fallback click/link behavior
Status: inherited AdvCiv native-widget migration regression; pre/post behavior source-provable.
Evidence:
- Before AdvCiv practical 1419, the Foreign Advisor used BugDll.widget(... fallback=WIDGET_PEDIA_JUMP_TO_BONUS/TECH ...). Because the custom enum was not native, the fallback widgets supplied ordinary clickable Pedia navigation and isLink behavior.
- Practical 1419 made WIDGET_PEDIA_JUMP_TO_BONUS_TRADE and WIDGET_PEDIA_JUMP_TO_TECH_TRADE native and added their richer denial-help parsing.
- The native migration did not add the corresponding executeAction/isLink Pedia behavior, so selecting the custom widget stops using the clickable fallback.
- WIDGET_DEAL_KILL is a clean migration control: when AdvCiv made that custom behavior native, it retained action/link machinery rather than assuming every rich-hover widget should be parse-only.
- The bonus import column may encode its BonusTypes payload with +1000; any native Pedia action needs to normalize that encoding before jumping.
Repair direction:
- Restore Pedia click/link semantics for the native trade-denial widgets while preserving their denial hover text and payload encoding.

RECOVERED WIP13 DELTA + POST-WIP13 NORMALIZATION
F075 / provisional KI#397 — practical-1481 native BULL hover migration dropped contact fallback actions
Status: inherited AdvCiv native-widget migration regression; two sibling manifestations, one root.
Manifestation A — WIDGET_LEADERHEAD_RELATIONS:
- Before practical 1481 (4a65bf5c0f...), callers were wrapped in BugDll.widgetVersion(... fallback=WIDGET_LEADERHEAD ...).
- The fallback's C++ alt action contacted the civilization.
- The Military/Foreign Advisor Python right-click handling still hides the advisor in preparation for that contact action.
- Practical 1481 made WIDGET_LEADERHEAD_RELATIONS native and added rich hover parsing but no matching executeAltAction. A right-click can therefore hide the advisor and then do nothing instead of contacting the civ.
Manifestation B — scoreboard trade-network widget:
- Before practical 1481, the scoreboard trade icon used BugDll.widget("WIDGET_TRADE_ROUTES", ..., fallback=WIDGET_CONTACT_CIV, rival, -1), inheriting ordinary click-to-contact behavior.
- Practical 1481 made the rich trade-routes hover native but added no execute action, dropping the fallback contact.
- Practical 3396 (cf04fab335...) later split the scoreboard form to WIDGET_TRADE_ROUTES_SCOREBOARD only to stop Foreign Advisor hovers from expanding the scoreboard; it did not restore or intentionally revoke contact behavior.
Repair direction:
- Restore the lost contact action(s) explicitly on the native widgets, preserving the current rich hover behavior and the later scoreboard-only expansion distinction.
Negative control:
- WIDGET_DEAL_KILL shows that native migration can and does retain explicit click/link semantics when intended.

POST-WIP13 STRICT-SWEEP CONTROL — WIDGET_MENU_ICON FALL-THROUGH REJECTED AS CURRENT BUG
- parseHelp has WIDGET_MENU_ICON fall through into WIDGET_CONSCRIPT, whose parser uses assign() and would overwrite "Main Menu" help with conscription help when a selected city exists.
- executeAction likewise calls doMenu() and then falls through to doConscript(). This shape is inherited and also present in the local controls.
- Current normal UI hides the Main Menu button while the city screen / selected-city context is active, which masks the harmful city-dependent side of the fall-through in supported ordinary use.
- Keep as ugly inherited control-flow / robustness debt, not F076, unless a normal reachable selected-city + visible-menu path is later proven.

F076 / provisional KI#398 — group Espionage can abort a valid order by trying the first movable non-Spy
Status: NEW INHERITED BTS GAMEPLAY / GROUP-MISSION CONTRACT DEFECT ROOT; current SAS reproduction and vanilla/BtS control proof strong.

Mission-availability contract:
- CvGame::canHandleAction delegates mission legality to the selected interface group's canStartMission.
- CvSelectionGroup::canDoMission(MISSION_ESPIONAGE) iterates the group and returns true when ANY unit satisfies canEspionage(target, bTestVisible) and, when required, canMove().
- Therefore a mixed same-domain group legitimately exposes/enables Espionage when one selected Spy can act, even if other selected units cannot.

Execution contract failure:
- CvSelectionGroup::startMission iterates units in group order, skipping only units that cannot move.
- MISSION_ESPIONAGE calls pUnit->espionage(...) on the first movable unit it reaches and then unconditionally jumps to exit_unit_loop, explicitly to allow one unit at a time to do espionage.
- CvUnit::espionage immediately returns false when canEspionage(plot()) is false; CvUnit::canEspionage rejects every non-Spy through !isSpy().
- Consequently the executor stops after a failed attempt by an ineligible movable unit rather than continuing until it reaches the Spy that made the group mission legal.

Concrete current SAS reproduction:
- CvUnit::canJoinGroup allows same-owner units on the same plot to group when their domains match; it does not forbid Spy + combat-unit groups.
- Current UNIT_AXEMAN is DOMAIN_LAND, DefaultUnitAI=UNITAI_ATTACK, bSpy=0.
- Current UNIT_SPY is DOMAIN_LAND, DefaultUnitAI=UNITAI_SPY, bSpy=1.
- CvSelectionGroup::addUnit orders units by AI_groupFirstVal (then second value). UNITAI_ATTACK has first value 13; UNITAI_SPY has 9. Thus a normal Axeman + Spy group deterministically puts the Axeman before the Spy.
- On a rival-owned plot where the fresh Spy is legally able to perform espionage and the fresh Axeman can move, canDoMission returns true because of the Spy.
- startMission reaches the Axeman first; Axeman::espionage returns false because it is not a Spy; the unconditional exit prevents the eligible Spy from acting. A valid Espionage order can therefore produce no espionage action/popup.

Help sibling:
- parseActionHelp_Mission(MISSION_ESPIONAGE) uses the head selected unit kUnit and passes only that unit to setEspionageMissionHelp.
- setEspionageMissionHelp immediately returns for !isSpy(). In the same Axeman + Spy ordering, the action is enabled by the Spy but Spy-specific mission help is omitted because the head is the Axeman.
- This reinforces the same group-selection assumption, though the executor failure is independently sufficient for F076.

Ancestry/control:
- The local Civ4CE BtS source has the same contract split: canDoMission iterates until any pLoopUnit->canEspionage succeeds, while execution attempts espionage on the current loop unit and then sets pUnitNode=NULL regardless of success to stop after one unit.
- The AdvCiv/SAS goto exit_unit_loop is a readability/control-flow rewrite of that inherited BtS behavior, not the birth of the defect.

Conservative repair direction:
- Preserve the "one Spy per Espionage command" rule, but exit the unit loop only after selecting/attempting an actually eligible Spy.
- With canMove already filtered by the outer loop, the MISSION_ESPIONAGE case can continue past units that fail canEspionage(plot()) and stop after the first eligible Spy performs/opens the espionage action.
- Keep the two-phase/popup semantics intact; do not allow multiple Spies to execute one user command.
- For help, derive the espionage-help subject from an eligible selected Spy rather than blindly from the group head.

CURRENT REJECTION / CLOSURE NOTES CARRIED FORWARD
- The two extra CvDLLWidgetData.h declarations doCancelCivics and applyCityEdit remain dead inherited header residue: no live definitions/callers established in current, base AdvCiv, or Civ4CE controls.
- Generic enum casts without reachable bad widget producers remain non-findings; theoretical forged payloads are not being promoted.
- WIDGET_EXAMINE_CITY's direct city lookup remains protected by ordinary live-owned-city callers; no supported stale-city path has been proven.
- parseTradeRoutes' player IDs remain valid across all live callers inspected.
- plot-list static iUnitsPerRow is refreshed on each forward-shift sequence rather than acting as a permanently stale layout cache.
- MISSION_BUILD worker ETA does not double-count workers already performing the same build: getBuildTurnsLeft accounts for active builders while widget help adds selected workers only when their current build differs.

NORMALIZED LEDGER / RECOVERY CURSOR
- Durable prior files: 11 / 169.
- File 012 CvGameCoreDLL/CvDLLWidgetData.cpp remains OPEN.
- F062-F075 recovered/normalized from WIP10 + preserved thread; F075 includes the scoreboard trade-network contact sibling.
- F076 / provisional KI#398 promoted in this checkpoint.
- F077 UNUSED.
- Next: finish remaining head-unit/special-mission help parity; strict enum/index/pointer/null/string/state inventory; reconcile 168 CPP definitions vs 170 header declarations; final custom-28-widget closure; adversarial reread F062-F076; then decide whether file 012 earns durable C013.
================================================================================

CURRENT-TREE RECONCILIATION OF WIP14 DELTA
- Current caller spot checks also support KI#395's angry-citizen overlay mismatch
  and the live widget families underlying KI#396-KI#397. KI#398's mixed-group
  Espionage availability/execution mismatch is source-strong and especially
  suitable for a focused reproduction, but all four remain provisional until
  their own implementation review. File 012 therefore remains OPEN; this WIP14
  reconstruction is preserved as evidence, not promoted as durable C013.


================================================================================
C012-WIP15 — file 012 CvDLLWidgetData.cpp — RESUMED CLOSURE CHECKPOINT
Authoritative source snapshot: user-supplied AdvCiv-SAS_light_source_20260825T074323.zip
Current reconciled source HEAD recorded by snapshot: 8eae1ddf6c0d2f6346d0184bec5e6a009edb4fb0
Durable completion remains 11/169. File 012 remains OPEN. No source modified.
This checkpoint appends to the current reconciled album, whose WIP14 reconstruction
already contains F076 / provisional KI#398. True next finding slot is F077.

RESUMED NORMALIZATION — F071 / provisional KI#393 BROADENED
F071's causal family is now stronger than the previously recorded Ground Bombard,
Air Bomb and Paradrop cases. Additional current-SAS mixed-selection reproductions
show that parseActionHelp_Mission sometimes derives mission help from the head
selected unit rather than from the unit(s) that actually make the group mission
executable:

1) MISSION_LEAD / Lead Troops
- A normal Axeman + Great General land group can be ordered with the Axeman first:
  UNITAI_ATTACK has group-first priority 13, while UNITAI_GREAT_GENERAL has 11.
- The Great General makes Lead valid, but the help path receives the head Axeman.
- The General-specific +20 XP / Leader-promotion explanation can therefore be
  omitted even though the selected group can execute Lead.

2) MISSION_DISCOVER
- A normal Axeman + Great Scientist group can have the Axeman as head.
- The main discovery-value calculation searches the selected units and can find
  the Scientist correctly, but the later discovery-path helper receives the head
  Axeman's UnitTypes value.
- The Axeman has no discovery flavor/base discovery role, so the actual Great
  Scientist bulb-path explanation can disappear from an otherwise valid action.

3) MISSION_PLUNDER
- Current UNIT_TRANSPORT uses UNITAI_ASSAULT_SEA (group-first 11) and bPillage=0.
- Current UNIT_DESTROYER uses UNITAI_RESERVE_SEA (group-first 2) and bPillage=1.
- In a Transport + Destroyer selection, the Destroyer can make Plunder/Blockade
  legal while the Transport remains the group head.
- Head-unit-only help can then emit the non-plundering unit's refusal/diagnostic
  text despite another selected unit being the actual executable subject.

Normalization: keep these under F071, not separate IDs. The common repair rule is
to derive special-mission help from the same eligibility population that can
execute the selected mission, then aggregate/rank only those eligible units.

F076 / provisional KI#398 — REPAIR MODEL DEEPENED
The WIP14 executor proof remains valid: a mixed Axeman + Spy group can expose
Espionage because the Spy passes canDoMission, then startMission reaches the
movable Axeman first, calls espionage(), receives false because it is not a Spy,
and exits before reaching the eligible Spy.

A naive repair of only that unit loop is NOT sufficient. The two-phase popup flow
also loses the acting Spy identity:
- CvUnit::espionage(NO_ESPIONAGEMISSION) queues BUTTONPOPUP_DOESPIONAGE without
  storing the invoking Spy ID.
- CvDLLButtonPopup::launchDoEspionagePopup later calls getHeadSelectedUnit() and
  uses that unit to test/build available espionage missions.
- In the concrete Axeman + Spy ordering, the head is still the Axeman; K-Mod's
  canDoEspionageMission path rejects the non-Spy, so a merely loop-fixed command
  can still open an empty/Cancel-only mission popup.
- launchDoEspionageTargetPopup likewise reacquires getHeadSelectedUnit() for the
  acting Spy; parseEspionageCostHelp also follows the head-selection identity.
- Popup return messages push the selection-group mission and do not carry a Spy
  unit ID that would repair this identity loss downstream.

Repair implication: F076 is an end-to-end selected-actor identity problem, not
only a missing `continue` in startMission. Preserve one-Spy-per-command and the
existing two-phase popup semantics, but either carry the chosen eligible Spy
through popup data or consistently resolve an eligible selected Spy at every
phase. Do not mark a patch complete if it only skips non-Spies in startMission.

STRICT-CLOSURE NOTES FROM THE RESUMED PASS
- CvDLLWidgetData.h's 170 declarations vs 168 out-of-line CPP definitions is now
  fully explained by doCancelCivics and applyCityEdit. Both are dead inherited
  declarations with no definition/caller found in current SAS, base AdvCiv or
  Civ4CE; this is residue, not broken wiring.
- The decimal city-trade widget encoding remains safe under SAS's 48-player DLL:
  it reserves two decimal digits per player (0..99), so player IDs 0..47 do not
  collide.
- WIDGET_MENU_ICON -> CONSCRIPT fall-through remains ugly inherited control flow,
  but the harmful selected-city context is masked by normal supported UI button
  visibility. Keep as robustness debt, not a promoted finding absent a normal
  reachable reproduction.
- The 28 AdvCiv/SAS custom widget types are now a bounded closure set. The known
  incomplete-native-migration roots remain F074/F075; the other custom entries
  checked so far have coherent parse/action ownership or are intentionally
  informational/Python-owned.

REJECTED AUDIT-TOOL ARTIFACT — DO NOT RESURRECT AS F077
An apparent duplicate parseCityNameHelp() call in WIDGET_CITY_NAME was caused by
my inspection command printing overlapping sed ranges ending/starting at source
line 105. Direct reading of the authoritative ZIP entry shows one call, as does
base AdvCiv. The extracted working copy's SHA-256 matches the ZIP entry exactly:
f03b1bafdf8f65742e1c18819946a65d5434a2c9662eaa6926606e2f141c1fa1.
There is no CITY_NAME duplication bug. F077 remains UNUSED.

CURRENT COMMAND/HELP CLOSURE STATE
The resumed pass has entered parseActionHelp's command family. Promotion and
Upgrade help currently match their selected-unit/all-unit contracts. Gift is
under parity review; Delete's expense projection remains coherent under current
SAS's disabled extra-cost callback and has not produced a new root. Load/Unload
and the remaining command action semantics still need their final execution
parity check.

EXACT NEXT CURSOR
11/169 durable · file 012 CvDLLWidgetData.cpp OPEN · authoritative 2026-08-25
source snapshot · F062-F076 normalized · F071 broadened with Lead/Discover/
Plunder · F076 popup actor-identity repair caveat recorded · F077 UNUSED.
Continue command/action parity (Gift/Delete/Load/Unload/Upgrade controls) ->
strict enum/index/pointer/null/string/state inventory -> remaining native/custom
widget closure -> final adversarial reread F062-F076 -> decide C013 vs WIP16.
================================================================================


================================================================================
C012-WIP16 — file 012 CvDLLWidgetData.cpp — CROSS-FILE LEAD ROOT CHECKPOINT
Authoritative source snapshot unchanged: AdvCiv-SAS_light_source_20260825T074323.zip
Current snapshot HEAD: 8eae1ddf6c0d2f6346d0184bec5e6a009edb4fb0
Durable completion remains 11/169. File 012 remains OPEN. No source modified.
Prior C012-WIP15 prefix preserved byte-for-byte.

F077 / provisional KI#399 — INHERITED BTS GREAT-GENERAL XP REMAINDER CAN BE LOST
Status: strong inherited gameplay/mechanics defect discovered while following
MISSION_LEAD help/execution from the open CvDLLWidgetData audit.

Current code contract
CvUnit::giveExperience():
1. canGiveExperience(plot) counts only eligible own units other than the Warlord.
2. getStackExperienceToGive(iNumUnits) computes a total XP pool from that eligible
   count.
3. iMinExperiencePerUnit = total / eligible count and iRemainder = total % count.
4. The award loop correctly grants XP only when:
      pUnit != this && same owner && pUnit->canAcquirePromotionAny()
   BUT the remainder index `i` is incremented outside that eligibility block.

Consequently every ineligible plot-list entry consumes a position in the
`i < iRemainder` test despite receiving no XP. The intended remainder points can
therefore disappear rather than being assigned to eligible recipients.

Concrete current-SAS reproduction without any exotic stack
- UNIT_GREAT_GENERAL has iLeaderExperience=20.
- Current GlobalDefinesAlt has WARLORD_EXTRA_EXPERIENCE_PER_UNIT_PERCENT=10 and
  WARLORD_MAXIMUM_EXTRA_EXPERIENCE_PERCENT=100.
- With 4 eligible recipients, getStackExperienceToGive computes:
      20 * (100 + (4-1)*10) / 100 = 26 XP.
  Intended split: 7 + 7 + 6 + 6 = 26.
- Current UnitTypes ordering places UNIT_GREAT_GENERAL at index 129 of 131;
  ordinary current combat recipients are lower UnitTypes (e.g. Axeman 30,
  Maceman 39, Rifleman 58, Infantry 61).
- CvPlot::addUnit orders same-owner/same-domain plot units through
  CvUnit::isBeforeUnitCycle; when unit types differ, higher UnitTypes come first.
  Therefore the Great General itself normally precedes ordinary land recipients.
- The loop sees the General first: it is excluded from XP, but the unconditional
  `i++` changes i from 0 to 1. The four eligible recipients then see i=1,2,3,4.
  With iRemainder=2, only the first recipient satisfies i<2. Actual award becomes
  7+6+6+6 = 25 XP: 1 XP silently vanishes.
- Additional ineligible units ordered before recipients (for example another
  non-recipient) can consume still more remainder positions. No such extra unit
  is required for the basic defect because the Warlord itself already does so.

Why this is not merely a tooltip discrepancy
getStackExperienceToGive returns the intended pool, and canGiveExperience supplies
its eligible denominator. The award loop then fails to distribute that pool.
This affects real unit experience and promotion progression. MISSION_LEAD help
can additionally advertise the nominal pool, but the mechanics loss is the
independent root and is sufficient for F077.

Ancestry/control
- AdvCiv 1.14 contains the same unconditional `i++` outside the eligibility block.
- The supplied Civ4CE/BtS implementation has the same structure, including the
  unconditional increment after each plot unit. This establishes the defect as
  inherited BtS behavior rather than an AdvCiv-SAS regression.
- K-Mod/AdvCiv increased Warlord scaling, which changes when/how much remainder
  exists, but did not introduce the indexing mistake itself.

Conservative repair direction
Advance the remainder-distribution index only when an eligible unit actually
receives experience, e.g. move `i++` into the eligibility branch after the award
(or use an explicitly named `iEligibleRecipient`). This preserves plot iteration
order and all existing total-XP/scaling rules while guaranteeing that exactly
`iRemainder` eligible recipients receive the +1 point.

NORMALIZATION NOTE
F077 is cross-file (CvUnit.cpp), but it was source-reached directly through the
CvDLLWidgetData MISSION_LEAD contract audit. Prior album findings already retain
cross-caller/executor roots when they are necessary to explain the widget-visible
behavior (e.g. F067/F072/F076), so preserving it here is consistent and useful.
It should also be carried into the eventual CvUnit.cpp audit as an already-known
root rather than rediscovered/double-counted there.

PARALLEL WIP15 RECONCILIATION
Two parallel threads produced different checkpoints named WIP15. The separately
downloaded C012-WIP15_RECONSTRUCTED used practical 6250 / HEAD f0ee784b... and
preserved the older WIP14 reconstruction as its prefix. The WIP15 embedded in
this WIP16 used the later reconciled 20260825T074323 snapshot / HEAD 8eae1ddf...
and retained intervening current-tree reconciliation, including the KI#311 /
KI#387 accessor cross-reference. Therefore WIP16's statement that its WIP15
prefix was preserved byte-for-byte refers to the WIP15 produced in the same
newer thread, not the parallel WIP15_RECONSTRUCTED file. Direct comparison of
the two downloaded full albums confirmed that they are not byte-identical and
first diverge in the earlier reconciled prefix rather than at the new checkpoint.

WIP16 remains authoritative for numbering, source state and continuation cursor.
The following useful closure evidence from the alternate WIP15 is retained here
so choosing WIP16 does not discard the parallel review:
- MISSION_PILLAGE was challenged and rejected as another F071 sibling. Its help
  uses the supplied unit chiefly for group-shared domain/team knowledge when
  choosing improvement-versus-route context; no concrete false current output
  was established from a non-pillaging group head.
- Corporation Spread was challenged and rejected as another F071 sibling.
  Spread cost and visible city-side failure reasons are player/city/corporation
  based rather than Executive-type dependent; no independent false output or
  failed-execution contract was established.
- The 170 header declarations versus 168 CPP definitions discrepancy resolves
  exactly to inherited dead declarations doCancelCivics and applyCityEdit in
  current SAS, AdvCiv 1.14 and Civ4CE/BtS; it is not missing current wiring.
- Cached GlobalDefines inspected in CvDLLWidgetData have no overlapping current
  runtime setDefine writer. The plot-list iUnitsPerRow static is refreshed at
  the forward-shift sequence before use rather than remaining stale.
- Route-movement preview divisors have positive adjusted movement costs and the
  power-ratio denominator is protected by max(1, power). The remaining raw
  war-rand divisions belong to already-normalized F064 rather than a new root.
- WIDGET_MENU_ICON falling through to Conscript is inherited but masked by the
  supported UI: Main Menu is hidden in the selected-city context required for
  the harmful path. It remains robustness debt absent a reachable reproduction.
- The normalized current-versus-AdvCiv semantic surface was accounted for; no
  large unexplained SAS C++ body remained. The native BUG/BULL fallback review
  found action-loss families only in existing F074/F075; neighboring Finance
  and Food widgets are intentionally hover-only, while WIDGET_DEAL_KILL is a
  clean control with explicit native action/link handling.
- Strict pointer/index checks retained WIDGET_EXAMINE_CITY, city-trade payloads,
  trade-route player pairs and generic Pedia enum casts as negative controls;
  no supported stale producer or concrete invalid current payload was proven.

This reconciliation adds no finding and leaves F078 unused.

CURRENT CURSOR
F062-F077 normalized; F078 UNUSED. File 012 remains OPEN.
Continue strict CvDLLWidgetData index/pointer/null/string/state closure and the
remaining command/native-widget contracts; then adversarially challenge the full
F062-F077 ledger and decide C013 vs another WIP. Do not let F077 substitute for
finishing CvDLLWidgetData itself.
================================================================================



===============================================================================
C013 -- FILE 012 CvDLLWidgetData.cpp DURABLE CLOSURE
===============================================================================

STATUS
------
Authoritative audit/implementation synchronization source:
AdvCiv-SAS_light_source_20260825T085034.zip.

Repository snapshot HEAD:
7aaf0b6aa9fc0a5d7e7ff5a4d21490e6ce94b1df (practical 6252).

The authoritative working tree also contains the then-staged KI#388-KI#390
implementation batch. Those staged changes are part of the source actually
reviewed for this closure rather than being treated as hypothetical patches.
The supplied bundled audit album, including its WIP15 divergence reconciliation
and authoritative WIP16 cursor, is preserved byte-for-byte as the prefix of this
C013 artifact.

FILE 012 IS NOW COMPLETE for the current file-oriented audit.
Durable completed-file count advances from 11/169 to 12/169.

No new independent root survived the final closure tranche. F078 therefore
remains UNUSED and becomes the next available root number for the next file.

No repository source was modified by this audit.


FINAL NORMALIZED ROOT LEDGER -- FILE 012 / CROSS-FILE ROOTS REACHED FROM IT
--------------------------------------------------------------------------

F062 / KI#383 -- FIXED
SAS WIDGET_LH_GLANCE vassal-willingness refactor dropped the inherited War Trades
explanation from the Glance relationship hover. Current synchronized tree already
contains the accepted repair.

F063 / KI#384 -- FIXED
Inherited opening-Pedia alt-click path could dereference an invalid active
civilization while no real game player existed. Current synchronized tree already
contains the accepted opening-menu guard.

F064 / KI#385 -- PENDING BY DESIGN
Scoreboard cheat/debug war-decision reconstruction has drifted from live
CvTeamAI::AI_doWar behavior. This remains diagnostic/debug debt requiring a
focused policy review rather than a rushed local patch. It is not merged with
any later divisor concern; the unguarded war-rand divisions encountered during
this file audit remain part of this already-normalized diagnostic family.

F065 / KI#386 -- FIXED
Inherited BtS Infiltrate help used the generic Espionage capability predicate
instead of the actual Infiltrate predicate. Current synchronized tree contains
the repair.

Former KI#387 -- RETRACTED FALSE POSITIVE
GET_TEAM(PlayerTypes) already performs PlayerTypes -> TeamTypes conversion through
its overload/TEAMID path. The previously suspected player/team confusion was not
a behavior bug. This retraction remains authoritative and must not be resurrected
from an older divergent checkpoint.

F066 / KI#388 -- FIXED/STAGED IN SYNCHRONIZED WORKING TREE
The custom Info Screen Score tab reused scoreboard-only widgets and imported
setScoreboardExpanded(true) state/timer behavior. The synchronized staged repair
separates Info-Screen power help and avoids scoreboard-expansion payloads for the
other Score-tab cells. The new WIDGET_POWER_RATIO_INFO_SCREEN contract was
included in the final custom-widget closure and is correctly hover-only.

F067 / KI#389 -- FIXED/STAGED IN SYNCHRONIZED WORKING TREE
Specialist Pedia Extra Slots passed data1=-1 to tech-context
WIDGET_HELP_SPECIAL_BUILDING, allowing the NO_TECH sentinel to match XML
TechPrereqAnyone=NONE and generate misleading construction help. The synchronized
repair uses a neutral widget for the grouped display.

F068 / KI#390 -- FIXED/STAGED IN SYNCHRONIZED WORKING TREE
Military Advisor Total Cost + Supply displayed the unit-cost/supply subtotal but
borrowed WIDGET_HELP_FINANCE_INFLATED_COSTS, whose help describes the player's
whole pre-inflation expense base. The synchronized repair adds a dedicated
WIDGET_HELP_FINANCE_UNIT_COST_AND_SUPPLY helper. Its parser/declaration/action
ownership was included in the final structural closure and is coherent.

F069 / KI#391 -- FIXED/COMMITTED
BUG Detailed/Wide Tech Splash supplied WIDGET_HELP_IMPROVEMENT as
(buildID, 1) for non-improvement builds although the C++ contract is
(techID, buildID), yielding empty/wrong help. The synchronized source contains
the committed repair.

F070 / KI#392 -- FIXED/COMMITTED
Detailed Tech Splash contained yWidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT and could
raise NameError when a researched tech unlocks a unit. The synchronized source
contains the committed repair.

F071 / provisional KI#393 -- RETAINED AFTER FINAL ADVERSARIAL REVIEW
Multi-unit mission help can describe/rank/aggregate the selection head or all
selected units instead of the subset that can actually execute the hovered
mission. Proven siblings remain:
- Ground Bombard can sum nominal bombard damage from spent/ineligible selected
  siege units while execution filters through canBombard.
- Air Bomb help can analogously overcount aircraft that cannot execute at the
  target.
- Paradrop interception help can select a moved/high-evasion or tied unit and
  suppress information although another selected Paratrooper can execute.
- Lead Troops help can use an ordinary combat head unit even when a selected
  Great General makes the action executable, omitting the General-specific XP /
  leader-promotion details.
- Discover computes the main bulb amount through an eligible unit search but
  later calls setDiscoverPathHelp with the selection-head unit type, so the path
  explanation can disappear in a mixed selection.
- Plunder/blockade can be executable through a selected pillaging naval unit
  while a non-plundering head unit supplies a false inability diagnostic.
MISSION_PILLAGE and Corporation Spread were explicitly challenged and rejected as
additional siblings; they are not needed to support this root.

F072 / provisional KI#394 -- RETAINED AFTER FINAL ADVERSARIAL REVIEW
AI_bestUnitForMission(MISSION_AIRBOMB) does not consistently filter candidates by
target eligibility. For city targets it uses generic canAirBomb rather than
canAirBombAt; for non-city targets it can likewise rank without the target test.
A chosen out-of-range/ineligible aircraft can then fail CvUnit::airBomb and stop
the group mission before a valid selected companion acts. The current SAS
Bomber-range-8 / Jet-Fighter-range-10 example against a city at distance 9
remains a concrete reproduction family.

F073 / provisional KI#395 -- RETAINED
The BUG/AdvCiv Chevron angry-citizen overlay places a hit-testable DDS over the
angry-citizen button but tags the overlay WIDGET_CITIZEN with a SpecialistTypes
payload. Hover/right-click over the chevron portion can therefore route to
specialist help/Pedia instead of angry-citizen behavior. The surrounding
specialist chevrons are the copy-pattern control; the angry case is the outlier.

F074 / provisional KI#396 -- RETAINED AFTER NATIVE-WIDGET CLOSURE
When AdvCiv made the Bonus/Tech trade-denial BULL widgets native, the old
BugDll.widget fallback to clickable Pedia-jump widgets stopped being used, but
the native migration supplied rich parseHelp handling without corresponding
executeAction/isLink behavior. Current Foreign Advisor constructors still use the
native trade widgets and no compensating Python handler was found. The old Pedia
interaction therefore remains lost.

F075 / provisional KI#397 -- RETAINED AFTER NATIVE-WIDGET CLOSURE
The practical-1481 native BULL-hover migration also dropped two contact fallbacks:
- WIDGET_LEADERHEAD_RELATIONS lost the old WIDGET_LEADERHEAD right-click contact
  action; current advisor Python can still hide the screen on right-click while
  C++ has no relations-widget executeAltAction.
- scoreboard WIDGET_TRADE_ROUTES[_SCOREBOARD] lost the old WIDGET_CONTACT_CIV
  ordinary click-to-contact action.
WIDGET_DEAL_KILL remains the clean migration control because its native action /
link behavior was explicitly supplied.

F076 / KI#398 -- RETAINED AND REPAIR SCOPE DEEPENED
Mixed-group Espionage can be enabled by a selected Spy while
CvSelectionGroup::startMission reaches a movable non-Spy first, calls its
espionage(...) method and unconditionally exits the unit loop after failure.
Furthermore a naive repair that merely skips non-Spies is insufficient:
BUTTONPOPUP_DOESPIONAGE does not retain the acting Spy ID, and both espionage
popup phases return to getHeadSelectedUnit(). The actor identity must survive the
whole popup/return path. Lead Troops is a strong local negative control: its
popup explicitly stores the Great General's unit ID and resolves that stored
actor later, demonstrating the appropriate engine pattern.

F077 / KI#399 -- RETAINED CROSS-FILE ROOT (CvUnit.cpp)
CvUnit::giveExperience correctly calculates Great-General total XP and remainder
from eligible recipients, but increments its remainder-distribution index for
every plot unit, including units that receive no XP. Ineligible entries therefore
consume remainder slots and silently destroy intended experience.

The ordinary current-SAS four-recipient reproduction remains deterministic:
20 base leader XP with current Warlord scaling yields 26 intended XP, nominally
7+7+6+6. Same-owner/same-domain plot ordering places the high-UnitTypes Great
General before ordinary land recipients; the General is excluded from the award
but still consumes the first unconditional index increment, producing 7+6+6+6
= 25 actual XP. Base AdvCiv 1.14 and Civ4CE/BtS preserve the same inherited loop
shape. Repair direction remains to advance the remainder index only when XP is
actually awarded to an eligible recipient.

F077 is a post-C007 cross-file amendment reached through MISSION_LEAD during this
widget audit. CvUnit.cpp was already durably audited earlier; do not rediscover or
double-count this root later merely because implementation review returns to
CvUnit.cpp.

F078 -- UNUSED
No additional independent root survived file-012 closure.


FINAL WHOLE-FILE CLOSURE EVIDENCE
---------------------------------

1. Current-source structural inventory

On the synchronized practical-6252 working tree (including staged KI#390):
- CvDLLWidgetData.cpp has exactly 169 out-of-line CvDLLWidgetData definitions.
- CvDLLWidgetData.h has exactly 171 method declarations.
- Every current definition has a header declaration.
- The only declarations without definitions are the same inherited dead residue:
  doCancelCivics and applyCityEdit.

The earlier 168/170 count was not instability: staged KI#390 added exactly one
new declaration and one matching definition, moving the pair to 169/171 while
leaving the same two inherited header-only declarations.

2. Complete custom WidgetTypes ownership inventory

The Civ4CE/BtS control has 163 WidgetTypes; the synchronized SAS tree has 193.
All 30 SAS/AdvCiv extras were explicitly mapped across parseHelp, executeAction,
executeAltAction and isLink rather than sampling the dispatcher. The 30 extras
are:

WIDGET_HELP_POLLUTION_OFFSETS
WIDGET_HELP_POLLUTION_SOURCE
WIDGET_HELP_SUSTAINABILITY_THRESHOLD
WIDGET_HELP_GW_RELATIVE_CONTRIBUTION
WIDGET_HELP_GW_INDEX
WIDGET_HELP_GW_UNHAPPY
WIDGET_HELP_GLOBAL_COMMERCE_MODIFIER
WIDGET_HELP_EXTRA_SPECIALIST_COMMERCE
WIDGET_RF_CIV_CHOICE
WIDGET_LH_GLANCE
WIDGET_SHOW_REPLAY
WIDGET_PEDIA_JUMP_TO_BONUS_TRADE
WIDGET_PEDIA_JUMP_TO_TECH_TRADE
WIDGET_TRADE_ROUTES
WIDGET_HELP_FINANCE_DOMESTIC_TRADE
WIDGET_HELP_FINANCE_FOREIGN_TRADE
WIDGET_HELP_FINANCE_SPECIALISTS
WIDGET_LEADERHEAD_RELATIONS
WIDGET_FOOD_MOD_HELP
WIDGET_TRADE_ROUTES_SCOREBOARD
WIDGET_EXPAND_SCORES
WIDGET_POWER_RATIO
WIDGET_GOLDEN_AGE
WIDGET_ANARCHY
WIDGET_CITY_TRADE
WIDGET_CYCLE_UNIT
WIDGET_EXAMINE_CITY
WIDGET_HELP_NO_FEAR_FOR_SAFETY
WIDGET_POWER_RATIO_INFO_SCREEN
WIDGET_HELP_FINANCE_UNIT_COST_AND_SUPPLY

No new incomplete native-action family survives beyond F074/F075. The two newly
staged KI#388/KI#390 widget types are intentionally hover-only and have coherent
parser ownership.

3. Help/command/executor parity closure

The large mission/action body was traced against actual unit/group execution
rather than judged from local casts or names. F071/F072/F076 are the surviving
execution/help families. Late command checks did not add F078:
- Gift help/execution uses canGift and revalidates individual units.
- Load deliberately requires all selected units to be loadable before issuing the
  AdvCiv group command; this is not an accidental partial-selection mismatch.
- Upgrade, Delete and Unload execution revalidate per unit.
- The human Delete path remains coherent under normal selected-unit contracts.
- Lead popups preserve the Great General actor ID, a negative control that both
  rejects a speculative Lead-popup root and strengthens F076's Espionage actor
  diagnosis.

4. Strict index / enum / pointer / null / sentinel / string / divisor sweep

No new reachable payload-safety root survived. Important negative controls:
- WIDGET_EXAMINE_CITY has a locally strong city-pointer assumption, but all live
  supported producers found pass a live owned city ID; no normal stale producer
  was established.
- city-trade decimal encoding reserves two decimal digits per player and is safe
  for SAS's 48-player IDs (0-47).
- parseTradeRoutes receives valid player pairs from every live caller found.
- parseTerrainTradeHelp lacks a local data2>=0 test, but every live constructor
  supplies a real terrain index 0..n-1 or exactly n for the deliberate River
  Trade sentinel. A forged -1 payload is not a supported reproduction.
- WIDGET_HELP_OBSOLETE's TechTypes-looking cast remains a type-annotation smell;
  the text-manager helper consumes the integer as the intended Building ID.
- plot-list iUnitsPerRow is refreshed at the forward-shift sequence before use.
- cached GlobalDefines inspected here have no current runtime setDefine writer.
- route/power/culture/finance divisors are structurally positive or guarded; the
  raw war-rand divisions remain inside already-normalized F064 diagnostic logic.
- Military Advisor minimap-highlight encoding uses data1=-owner, so player 0 is
  encoded as -0/0; decoding still works because the action path accepts <=0.
  The possible link-cursor asymmetry for player 0 does not break the action and
  was not promoted.
- generic enum casts were not treated as bugs without a concrete live producer
  capable of manufacturing the invalid payload.

5. Inherited control-flow and UI-state negative controls

- WIDGET_MENU_ICON falling through toward Conscript is ugly inherited control
  flow, but the harmful side is masked in supported UI because Main Menu is not
  exposed in the selected-city context needed for Conscript behavior. It remains
  robustness debt absent a reachable reproduction.
- Heal MAX_INT can represent a genuinely indefinitely healing group when feature
  turn damage exactly cancels healing; it is not a leaked finite ETA sentinel.
- broader vassal-willingness text on non-Glance relationship widgets remains
  observer-gated and truthful; broader presentation alone is not a bug.
- shifted mission lastMissionPlot is protected by its fallback to the group's
  current plot.
- MISSION_BUILD's NO_TECH paths use sentinel-aware engine helpers.
- no general scoreboard-expansion defect exists outside the already-normalized
  F066 cross-context Info-Screen users.

6. Current SAS versus base/controls accounted

The semantic current-versus-AdvCiv diff was inspected after normalization rather
than assuming every textual difference was suspicious. Remaining substantive SAS
regions are accounted for by known fixes/features (vassal willingness, opening-
Pedia repair, Infiltrate repair, hover-font wrapper, cached defines and the
staged KI#388/KI#390 widget separation) or previously normalized findings. No
large unexplained SAS-specific C++ behavior block remains.

Civ4CE/BtS was used as an independent control where useful, especially for the
native-widget set, inherited mission/action structures and Great-General XP loop;
it was never assumed automatically correct merely because it differs.

7. Final adversarial challenge of execution-sensitive retained roots

The late pass explicitly attempted to disprove F071, F072, F076 and F077 rather
than simply restating them:
- F071 retains exact current call sites where the head/all-selected population
  differs from the mission-executable population; spent/ineligible bombarders can
  still contribute nominal damage because damageToBombardTarget is not itself the
  full canBombard eligibility test.
- F072 retains the generic canAirBomb versus target-specific canAirBombAt mismatch
  and the executor's stop-on-failed-selected-unit behavior.
- F076 retains both the first-movable-unit abort and the second-stage actor-loss in
  popup construction; Lead's stored-unit-ID pattern is the negative control.
- F077 retains the unconditional remainder-index increment plus deterministic
  same-owner/same-domain Great-General ordering proof.

F073-F075 were likewise rechecked against current Python/C++ producers/actions;
no compensating Python action was found for F074, and the relations/scoreboard
contact fallbacks in F075 remain absent from the current C++ dispatchers.

8. Explicit rejected audit artifact

An apparent duplicate parseCityNameHelp call found during a prior WIP was caused
by overlapping source-inspection ranges printing the same boundary line twice.
The working copy was hash-checked against the ZIP entry and the source contains
only one call. It is permanently rejected and did not consume F077/F078.


CLOSURE DECISION
----------------

CvDLLWidgetData.cpp is defensibly exhausted for this file-oriented pass. The
surviving roots F062-F077 have been normalized, merged where causal siblings
belong together, and adversarially challenged. The late command, custom-widget,
pointer/index/null/state, divisor and current-versus-base closure passes did not
produce an independent F078.

FILE 012 STATUS: COMPLETE -- REVIEWED
COMPLETED FILES: 12 / 169
NEXT ROOT ID: F078 (UNUSED)

NEXT FILE
---------

013  6190  CvGameCoreDLL/CvTeam.cpp

Start from the current practical-6252 working tree, build an exact current/base /
Civ4CE structural and semantic delta, enumerate public/state-mutating team
contracts and callers, then follow war/peace, vassal, diplomacy, tech, map/reveal,
trade and victory/team-state paths with the same caller/executor evidence
standard. Carry F077 only as an already-known post-C007 cross-file amendment; do
not rediscover or double-count it.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP01 -- FILE 013 CvTeam.cpp -- PERMANENT-ALLIANCE STATE MERGE TRANCHE
Current source: practical 6252 / 08:50 synchronized working tree
Prior durable prefix: C013, preserved byte-for-byte
Status: CvTeam.cpp OPEN
Next root id after this checkpoint: F082 UNUSED
===============================================================================

RECOVERY / SYNCHRONIZATION
--------------------------

The 08:50 light-source ZIP and its bundled audit album are authoritative. C013
closed CvDLLWidgetData.cpp with F078 unused and began CvTeam.cpp. This WIP
continues that exact cursor; no divergent WIP15 reconstruction is involved.

The first CvTeam.cpp pass is intentionally centered on CvTeam::addTeam,
shareItems and shareCounters because Permanent Alliance merging has already
produced several source-proven directional-state defects (KI#369/KI#370). The
method was treated as a state migration: enumerate team-indexed and transient
team state, then prove whether both the survivor->outsider and outsider->survivor
directions retain semantically live values after absorbed players are reassigned.

F078 / provisional KI#400 -- inherited Permanent Alliance merge drops active
Counterespionage effects in both directions
--------------------------------------------------------------------------

STATE / CONTRACT
- CvTeam owns two directional arrays:
  m_aiCounterespionageTurnsLeftAgainstTeam and
  m_aiCounterespionageModAgainstTeam.
- The live Counterespionage mission in current SAS has exactly one nonzero
  definition: 10 turns and +100 modifier. CvPlayer::doEspionageMission applies
  both values to the acting team's entry against the target team.
- Espionage mission cost reads the target team's Counterespionage modifier
  against the acting spy's current team. The effect is therefore keyed strictly
  by current TeamTypes identity, not by player identity.
- An active effect cannot normally be started again against the same target while
  its turns-left entry is positive, so each directional team pair represents one
  live effect rather than an arbitrary stack.

MERGE FAILURE
- shareCounters explicitly preserves neighboring temporary/history state such as
  StolenVisibilityTimer, WarWeariness and AI relation counters, but it never
  transfers either Counterespionage array from the absorbed team to the survivor.
- CvTeam::addTeam's reverse outsider-direction merge likewise combines
  StolenVisibilityTimer and EspionagePointsAgainstTeam but never combines the
  outsider's Counterespionage entries against the two constituent teams.
- After absorbed players change from team B to survivor A, entries keyed to B are
  unreachable for ordinary espionage cost calculations.

CONCRETE REPRODUCTION
1. Team B performs Counterespionage against team C and has, e.g., 7 turns of the
   +100 effect remaining.
2. B enters a Permanent Alliance and is absorbed into A.
3. A had no corresponding effect against C.
4. Former B spies/territory now belong to team A, so all checks use A<->C.
5. B's still-positive B->C timer/modifier remain on the dead team entry and the
   +100 effect disappears immediately. The reverse C->B case is lost analogously.

CONTROL / ANCESTRY
- Base AdvCiv 1.14 and the supplied Civ4CE/BtS source contain the same omission.
- Firaxis already preserves StolenVisibilityTimer in this exact merge family,
  making Counterespionage the conspicuous neighboring temporary espionage state
  that was not migrated.

REPAIR DIRECTION
- Preserve the live pair in both directions when team identities collapse.
- Current data has only the single +100 mission, so choosing the longer remaining
  active effect is unambiguous in practical SAS. Do not sum durations/modifiers.
- If future XML adds different Counterespionage modifiers, copy a coherent
  timer+modifier pair rather than independently manufacturing maxima.

Classification: inherited BtS/Firaxis defect retained by AdvCiv/SAS.


F079 / provisional KI#401 -- AdvCiv Permanent Alliance resets inherited
first-contact dates used by Info Screen graphs
--------------------------------------------------------------------------

STATE / ORIGIN
- AdvCiv practical 2468 (f91c93bea834f12a3f5da1894d8afaa1a07b4cac,
  "Show score graph (Info screen) regardless of espionage") replaced the old
  boolean has-met state with m_aiHasMetTurn.
- makeHasMet records the current game turn in that array.
- CvInfoScreen uses getHasMetTurn to hide pre-contact score history and to gate
  partial score graphs for five turns after first contact.
- That introducing commit changed reset/serialization/makeHasMet and the Info
  Screen but did not update CvTeam::addTeam/shareCounters.

MERGE FAILURE
- addTeam propagates missing contacts by calling meet(eOther,false) before the
  absorbed players are reassigned.
- If B met C long ago but survivor A did not, A.meet(C) writes the alliance turn
  as A's new first-contact date; the reciprocal C->A entry is also written now.
- The old B<->C timestamps remain attached to the soon-dead B identity.
- Even when both constituent teams had met C, the merged team does not normalize
  the timestamp to the earliest inherited contact.
- The older AI_getHasMetCounter *is* merged, so the omission is specifically the
  later-added real timestamp used by the UI.

CONCRETE REPRODUCTION
- B and C first meet on turn 50.
- A has never met C.
- A absorbs B through a Permanent Alliance on turn 100.
- A and C become known through addTeam, but both relevant new A<->C timestamps
  become 100 rather than inheriting 50.
- Info Screen graph history that should be available through the allied team's
  prior knowledge is hidden as if first contact occurred on turn 100, and the
  five-turn partial-graph delay is restarted.

REPAIR DIRECTION
- For each inherited known relation, preserve the earliest nonnegative constituent
  has-met turn in both directions. A missing (-1) value must not beat a real date.

Classification: inherited AdvCiv regression introduced when advc.091 added the
first-contact timestamp without extending Permanent Alliance state migration.


F080 / provisional KI#402 -- AdvCiv Permanent Alliance can leave a live
Disengage deal without its enforcement flag
--------------------------------------------------------------------------

STATE / ORIGIN
- AdvCiv practical 1285 (8aaaae059d3db7d47d430c099ace4adc92e3afdb,
  "Temporary right of passage after making peace") introduced TRADE_DISENGAGE and
  m_abDisengage.
- The trade is a dual player-to-player CvDeal, while actual movement/diplomacy
  enforcement uses directional CvTeam::isDisengage flags.
- Starting the trade sets the team flag; ending it clears the flag and may bump
  units from territory that is no longer friendly.

MERGE FAILURE
- CvDeal::isBetween(TeamTypes,TeamTypes) derives the teams dynamically from the
  deal's stored players via TEAMID.
- Therefore a live B<->C Disengage deal automatically becomes an A<->C deal when
  B's player is reassigned to survivor team A during addTeam.
- addTeam explicitly transfers Open Borders, Defensive Pact, Force Peace and other
  team relations, but never transfers m_abDisengage from B<->C to A<->C.
- The practical-1285 implementation did add TRADE_DISENGAGE to the list of deals
  canceled *between the two merging teams*; it did not address outsider deals.
- A B<->C outsider Disengage deal is not killed by that merged-team cleanup: after
  reassignment it is an A<->C deal, not an A<->A deal.

CONSEQUENCE
- The deal remains present and continues counting down, but the team-level flag
  that grants its temporary right of passage is false on A<->C unless A happened
  to have an independent effect.
- Unit/plot/diplomacy code querying isDisengage therefore stops honoring the still
  live deal. At eventual deal termination, endTrade operates on the players'
  *current* teams and clears A<->C, further demonstrating that the deal itself is
  considered migrated even though its enforcement state was not.

REPAIR DIRECTION
- When an absorbed team has a live Disengage relation with an outsider, migrate
  the corresponding team relation to survivor<->outsider (both directions as
  required by the dual trade), or deliberately cancel the surviving outsider deal
  and enforce that as a documented policy. Silently keeping the deal while losing
  its state is inconsistent.

Classification: inherited AdvCiv defect introduced with advc.034.


F081 / provisional KI#403 -- Permanent Alliance can erase an in-flight
spaceship launch / victory countdown
--------------------------------------------------------------------------

STATE / REACHABILITY
- Permanent Alliance trading has no condition prohibiting the trade after a team
  has launched a spaceship; the human path can therefore reach this normally.
- launch() stores the actual in-flight state in m_aiVictoryCountdown and clears
  canLaunch for that victory.
- shareCounters transfers spaceship ProjectCount state (using the established
  max-of-two-teams rule). changeProjectCount subsequently recomputes canLaunch
  from the merged project requirements.
- addTeam/shareCounters never transfer m_aiVictoryCountdown.

CONCRETE REPRODUCTION
1. Team B launches and has 5 turns remaining before arrival.
2. B joins A via Permanent Alliance while the game is still running.
3. B's project/component counts are transferred to A.
4. B's countdown remains keyed to the dead B team and A retains its own default
   -1 (not launched) countdown.
5. The transferred projects can make A canLaunch again; the merged team has lost
   the existing ship-in-flight and must launch again from a fresh full delay (or
   can simply remain indefinitely in the not-launched state).

CONTROLS
- Current SAS, base AdvCiv 1.14 and supplied Civ4CE/BtS all omit countdown state
  from addTeam.
- CvPlayer::canPossiblyTradeItem(TRADE_PERMANENT_ALLIANCE) checks team/vassal/war/
  member/trading conditions but does not reject a live victory countdown.

TWO-LAUNCH COLLISION / REPAIR DIRECTION
- The merged team has only one countdown slot per victory. If either constituent
  has a live countdown, preserve it; if both do, preserve the minimum nonnegative
  value (earliest arrival), which is the least destructive representation of two
  already-launched team victories.
- While a countdown is active, keep canLaunch false; transferred project counts
  must not silently re-enable a second launch.

Classification: inherited BtS/Firaxis Permanent Alliance state-migration defect.


NEGATIVE CONTROLS / NEIGHBORING STATE CHECKED SO FAR
----------------------------------------------------

- m_abHasSeen does not immediately justify another root: addTeam copies absorbed
  revealed-map/city information to the survivor and explicitly meets inherited
  contacts, both of which invoke the existing identity-seen machinery.
- m_abJustDeclaredWar is reset each turn and war synchronization in addTeam runs
  through ordinary declaration paths where a new declaration is actually needed;
  no independent persistent-state loss has been established.
- KI#369 SharedWarSuccess and KI#370 reverse OpenBordersCounter are already fixed
  in the authoritative practical-6252 tree and were used as controls, not
  rediscovered.
- m_aiTurnsAtPeace is already merged in both the own and outsider directions by
  advc.130k / the existing SAS fixes.
- EspionagePointsAgainstTeam and EspionagePointsEver are explicitly summed during
  the merge; no loss found there.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F081
NEXT ROOT ID: F082 UNUSED

Next work:
1. Finish the remaining Permanent-Alliance state-migration matrix, including
   project-art/victory-adjacent, relation and scalar/team-derived state.
2. Adversarially challenge F078-F081 against trade/victory turn ordering and any
   compensating caller after addTeam.
3. Then leave addTeam/shareItems/shareCounters and continue CvTeam.cpp in source
   order: doTurn/reset state, war/peace queues and relation transitions, vassal
   state, tech/research processing, reveal/trade helpers, victory/project paths,
   serialization and final caller/index/divisor closure.
4. Freeze the next WIP early if another stable tranche emerges.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP02 -- CvTeam.cpp continued closure checkpoint
===============================================================================

AUTHORITATIVE SOURCE
--------------------
2026-08-25 08:50 light-source snapshot, practical 6252 / HEAD 7aaf0b6a... plus
its bundled/reconciled audit album and staged KI#388-KI#390 implementation.

This checkpoint is append-only relative to C013-WIP01. No repository source was
modified.


F081 / provisional KI#403 -- strengthening: even the SURVIVING launched team can
be made launchable again by the Permanent Alliance project transfer
--------------------------------------------------------------------------

The WIP01 root remains valid and is stronger than the absorbed-team reproduction
alone showed.

- CvPlayer::launch stores the in-flight state in m_aiVictoryCountdown and calls
  setCanLaunch(eVictory, false).
- shareCounters transfers a larger absorbed project count through
  changeProjectCount.
- changeProjectCount recomputes setCanLaunch(eVictory,
  GC.getGame().testVictory(eVictory, getID())) for every victory threshold touched.
- CvGame::testVictory checks the project/victory conditions but does not test an
  already-live victory countdown.

Therefore, if survivor A itself has already launched and B brings a higher count
of any victory-relevant spaceship component, the merge can set A.canLaunch back to
true while A's original countdown is still running. The problem is not only that
an absorbed B countdown is lost; transferred project state can also contradict a
survivor countdown already present.

Normalized repair requirement: after project counts have been merged, restore the
chosen live countdown (minimum nonnegative constituent countdown) and force
canLaunch=false for that victory whenever that countdown is active.


F082 / provisional KI#404 -- AdvCiv doubles the overflow portion in stored
completed-tech research progress
--------------------------------------------------------------------------

INTRODUCTION / INTENT
- AdvCiv practical 2082, commit 5a6bff0b8fbdb037f0c875aab7d3d6ed61beba09
  ("Some misc. refactoring") added the line while explicitly describing the work
  as cleaner/safer handling of research progress for already-discovered techs.
- The local source comment says: "Cleaner to subtract the overflow. Cf. comment in
  getResearchProgress."
- The adjacent getResearchProgress comment says that, once a tech is known, the
  stored value is the beakers the team put into it and can even be 0.

ACTUAL ARITHMETIC
Suppose the completion call has research progress P = C + O, where C is cost and
O is adjusted overflow. The code correctly grants player overflow from O, then:

    m_aiResearchProgress.add(eIndex,
        getResearchProgress(eIndex) - getResearchCost(eIndex));

EnumMap::add is ordinary arithmetic addition. Thus the stored value becomes:

    (C + O) + O = C + 2O

rather than subtracting O as the comment says. Vanilla/Civ4CE simply leaves the
old C+O value; this extra distortion is AdvCiv-specific.

REACHABILITY / IMPACT
- The wrong stored value is exposed through CyTeam::getResearchProgress and debug
  facilities.
- Most normal gameplay consumers first test isHasTech, which explains why this can
  remain quiet in ordinary play.
- Tech removal is nevertheless a supported engine operation (Advanced Start,
  regeneration/debug/WB-style paths use setHasTech(false)). Once a completed tech
  is removed, its stored progress becomes live research state again; an inflated
  >=cost value can therefore leak back into research/recompletion/overflow logic.

REPAIR DIRECTION
Subtract the adjusted overflow portion from the stored progress rather than add it
(or otherwise explicitly set the intended post-completion value). Keep repeat-tech
handling in mind; practical 2088 separately amended that family.

Classification: inherited AdvCiv arithmetic/bookkeeping defect introduced by
advc refactoring.


F083 / provisional KI#405 -- AdvCiv cached tech count increments when a tech is
REMOVED instead of decrementing
--------------------------------------------------------------------------

INTRODUCTION
- AdvCiv practical 2169, commit 91ebe9eaa0fac82efca5ba70c7dfb8b968c51be8
  ("Tweak revolt chance penalties, nationality bar help"), advc.101, introduced
  cached CvTeam::m_iTechCount and getTechCount().
- The cache is initialized/serialized and used specifically to avoid repeatedly
  counting known techs.

BUG
In the non-repeat branch of CvTeam::setHasTech:

    m_abHasTech.set(eTech, bNewValue);
    m_iTechCount++;

The cache increments for BOTH bNewValue=true and bNewValue=false.

A transition true -> false should decrease the count by one. Instead it increases
by one, leaving the cache two higher than the real number of known techs after a
single removal.

NORMAL SUPPORTED REPRODUCTION
Advanced Start explicitly supports removing a purchased/owned technology through
getAdvancedStartTechCost(..., false) and then:

    setHasTech(eTech, false, ...)

subject to dependency/unit/building checks. Thus this is not confined to hostile
or impossible calls. A remove/rebuy cycle can repeatedly increase the cache even
though the actual number of known technologies returns to its starting value.
Map-regeneration/initialization cleanup also contains false transitions and is an
additional cache-drift risk.

GAMEPLAY CONSUMERS
- CvCity::cultureStrength uses getTechCount()/numTechInfos to derive its era-like
  culture/revolt-strength factor.
- CvPlayerAI building valuation uses getTechCount() around the early-war pacing
  threshold (advc.131).
Therefore the stale cache can affect gameplay, not merely diagnostics.

REPAIR DIRECTION
Use a signed update corresponding to the state transition, e.g.

    m_iTechCount += (bNewValue ? 1 : -1);

with the existing early equality return guaranteeing that only real transitions
are counted. Preserve the existing special handling for repeatable techs.

Classification: inherited AdvCiv cached-state defect introduced with advc.101.


ADDITIONAL NEGATIVE CONTROLS / SECTIONS CLOSED IN THIS TRANCHE
--------------------------------------------------------------

- CvTeam::setVassal was reread through creation and liberation. KI#363 remains the
  genuine war-plan-cache defect already fixed in the authoritative tree; the
  reordered restoration now waits until m_eMaster is cleared. Contact/war
  alignment goes through meet / queueWar / triggerWars, and no second independent
  vassal-transition state loss was established in this pass.
- assignVassal/freeVassal deal plumbing is consistent with current master/vassal
  state; no new root promoted.
- changeProjectCount's project-art vector sizing is balanced. The project-art
  style chosen by an absorbed team may not be preserved through PA project-count
  maxing, but this remains cosmetic/visual state and does not meet the bug bar yet.
- VictoryCountdown / canLaunch remains normalized under F081, not split into
  multiple roots.
- Counterespionage setters, EspionagePointsAgainstTeam, ForceRevealedBonus and
  their ordinary update helpers did not expose an additional non-PA defect in
  this pass.
- processTech's map-centering flag is intentionally one-way-looking (also true for
  map-centering buildings); because map centering acts as a one-time/permanent UI
  capability and has multiple sources, no tech-removal bug is promoted from that
  shape without stronger behavioral evidence.
- Circumnavigation correctly rejects non-wrapping maps in
  CvGame::circumnavigationAvailable before CvTeam::testCircumnavigated runs.
- Current serialization remains symmetric under the project's explicit current-
  schema-only policy (KI#46); old uiFlag branches are not being rediscovered.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F083
NEXT ROOT ID: F084 UNUSED

Source-order progress now covers the Permanent-Alliance state matrix through the
main vassal transition and into project/victory, research acquisition,
espionage/reveal helpers, tech sharing and processTech. The file tail and a final
cross-caller/adversarial pass remain open.

Next work:
1. Continue from processTech / remaining late CvTeam helpers and serialization
   controls, then return to any source-order gaps not fully challenged.
2. Audit setHasTech/processTech false-transition behavior further now that F082 and
   F083 demonstrate this is a productive boundary, while avoiding duplicate roots
   for consequences of the same cache/arithmetic causes.
3. Continue current-vs-base/Civ4CE differential checks and caller reachability.
4. Freeze another WIP after the next stable tranche; do not declare C014 until the
   whole CvTeam.cpp file has survived an adversarial reread.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP03 -- CvTeam.cpp false-transition / Advanced Start checkpoint
===============================================================================

AUTHORITATIVE SOURCE
--------------------
2026-08-25 08:50 light-source snapshot, practical 6252 / HEAD 7aaf0b6a... plus
its bundled/reconciled audit album and staged KI#388-KI#390 implementation.

This checkpoint is append-only relative to C013-WIP02. No repository source was
modified.


F084 / provisional KI#406 -- Advanced Start can refund a map-visible technology
while retaining its irreversible full-map reveal
--------------------------------------------------------------------------

CURRENT SAS REPRODUCTION
- TECH_SATELLITES has iAdvancedStartCost=100 and bMapVisible=1.
- CvPlayer::getAdvancedStartTechCost allows removal/refund of an owned tech when
  no still-owned dependent tech and no placed unit/building requires it.
- CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_TECH,...,false) then calls
  CvTeam::setHasTech(eTech,false,...) and refunds the points.
- Acquiring a bMapVisible tech enters CvTeam::setHasTech's owned-tech branch and
  calls GC.getMap().setRevealedPlots(getID(), true, true), permanently revealing
  all plots to the team.
- The false transition has no inverse map-unreveal operation. This is sensible in
  isolation because plot knowledge can also be legitimate from exploration,
  visibility, map trades, espionage and other sources; blindly unrevealing the
  world would destroy independent knowledge.

Therefore Advanced Start supports the sequence:

    buy Satellites -> reveal full world -> refund Satellites -> keep full world

The source technology and its points are gone while the irreversible benefit
remains.

ANCESTRY / CONTROL
The same fundamental shape is present in the supplied Civ4CE/BtS control; this is
not introduced by recent SAS GameRecord plumbing.

REPAIR DIRECTION
Do not try to reverse per-plot knowledge. The conservative repair is to prohibit
Advanced Start removal/refund of a tech with bMapVisible once acquired (or more
generally mark irreversible tech effects nonrefundable).

A neighboring bMapCentering tech is also one-way in processTech, but current SAS
uses that as a UI/presentation capability and it does not independently meet the
bug bar established for the full-map reveal.

Classification: inherited BtS Advanced Start irreversible-effect/refund defect.


F085 / provisional KI#407 -- Advanced Start tech refund does not roll back
currentEra, allowing later-era mechanics to persist without the corresponding
techs
--------------------------------------------------------------------------

STATE TRANSITION
In CvTeam::setHasTech, acquiring any non-repeat tech with an era above a member's
current era does:

    if (kMember.getCurrentEra() < kTech.getEra())
        kMember.setCurrentEra((EraTypes)kTech.getEra());

There is no corresponding downward update on setHasTech(..., false). Search of the
current DLL shows that normal era advancement is otherwise not periodically
recomputed from the team's known-tech set; outside initialization/Python, this
acquisition path is what moves player era forward.

ADVANCED START REACHABILITY
Advanced Start explicitly permits reversible tech purchases. A player can buy a
later-era tech (after satisfying its prerequisites), thereby advance currentEra,
and then refund/remove that tech once no dependent owned content blocks removal.
The player remains in the higher era. With enough points the player can walk a
tech chain upward and refund it backward while retaining the highest reached era.

GAMEPLAY IMPACT
currentEra is not merely art/music state. Concrete current-SAS consumers include:
- CvUnit::getGreatWorkCulture multiplies Great Work culture by
  (owner.currentEra + 1).
- CvPlayer::doEvents uses the current era's EventChancePerTurn.
- CvPlayer::canBuild visibility/era tests depend on currentEra.
- Many AdvCiv/SAS AI valuation, military, worker, culture-victory and strategy
  heuristics depend directly on player currentEra.

Thus the Advanced Start refund can preserve real later-era mechanics after the
technology that caused the era transition has been removed.

ANCESTRY
The monotonic-era plus reversible-Advanced-Start-tech combination is inherited
from BtS/Civ4CE rather than introduced by SAS.

REPAIR DIRECTION
Keep ordinary gameplay era progression monotonic. On an Advanced Start tech
removal, recompute each affected team member's era from the highest remaining
known non-repeat technology (bounded by the game start era), or equivalently
prevent removal when it would leave currentEra unsupported. Recomputing only in
the Advanced Start removal path avoids making normal post-start eras regress.

Classification: inherited BtS Advanced Start cached/derived-state defect.


ADDITIONAL NEGATIVE CONTROLS / STRENGTHENING
--------------------------------------------

- F081 was challenged for an ordinary post-launch reproduction independent of
  Permanent Alliances. CvPlayer::canCreate rejects victory-prerequisite projects
  whenever that victory already has a nonnegative countdown, including the
  bContinue=true queue path. Ordinary city production therefore cannot normally
  finish more spaceship parts after launch and re-enable canLaunch. F081 remains
  correctly scoped to project-count state changes that bypass normal construction,
  especially Permanent-Alliance project transfer.
- Repeat technologies use a separate acquisition-count path and are not evidence
  that F083's ordinary cached total-tech-count bug should be broadened.
- Tech-reveal bonus visibility, routes, trading counters, commerce flexibility,
  improvement yields, obsolete-building state and the ordinary signed processTech
  effects are generally reversed through iChange. No duplicate root was created
  merely because tech removal is uncommon.
- First-to-discover religion/free-tech/free-unit perks do not create an Advanced
  Start rebuy exploit through this path because Advanced Start acquisition calls
  setHasTech with bFirst=false.
- Current SpecialBuilding XML has no live TechPrereqAnyone value other than NONE,
  so no current-SAS global-special-building refund exploit was promoted.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F085
NEXT ROOT ID: F086 UNUSED

Source-order progress now covers the main Permanent-Alliance merge family,
vassal/project/victory transitions, technology acquisition/research, espionage and
reveal helpers, Advanced Start false-transition interactions and processTech.
Serialization has been checked for current-schema read/write symmetry, but the
file is not closed.

Next work:
1. Continue from processTech into the remaining late helpers and serialization /
   spaceship utility tail, then backfill earlier source-order gaps.
2. Keep challenging false transitions and derived/cached team state, but distinguish
   irreversible-effect policy from actual reachable refund/state defects.
3. Reconcile remaining current-vs-base/Civ4CE semantic deltas and callers.
4. Perform a mechanical field/state-transition audit and adversarially challenge
   F078-F085 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP04 -- CvTeam.cpp Barbarian-contact / early-source checkpoint
===============================================================================

AUTHORITATIVE SOURCE
--------------------
2026-08-25 08:50 light-source snapshot, practical 6252 / HEAD 7aaf0b6a... plus
its bundled/reconciled audit album and staged KI#388-KI#390 implementation.

This checkpoint is append-only relative to C013-WIP03. No repository source was
modified.


F086 / provisional KI#408 -- Ancient-era Barbarian-contact threshold of zero
turns local contact into global contact and over-accelerates Barbarian research
--------------------------------------------------------------------------

CURRENT CODE / REACHABILITY
CvTeam::isInContactWithBarbarians is the advc.302 gate used by
CvTeam::doBarbarianResearch when deciding how many civilization teams should
contribute to Barbarian free research.

During the early phase, before the city-contact fallback is enabled, it sets:

    int iUnitThresh = GC.getGame().getCurrentEra();

and is meant to skip map areas in which the civilization has too little unit
presence:

    if (!bCheckCity && countNumUnitsByArea(*pArea) < iUnitThresh)
        continue;

It then accepts contact when the same area contains more than iUnitThresh
Barbarian units.

In the Ancient era, currentEra is 0. Therefore:

    ownUnits < 0

can never be true. No area is filtered by the civilization's own presence. The
function consequently scans every area on the map, and any Barbarian unit
anywhere satisfies:

    barbarianUnits > 0

Thus, during the period when the function is specifically supposed to localize
contact through nearby units, one remote Barbarian unit can make every Ancient-
era civilization count as being in contact with Barbarians.

GAMEPLAY EFFECT
CvTeam::doBarbarianResearch counts civilization teams that both know a technology
and return true from isInContactWithBarbarians. F086 therefore inflates the
contact-weighted Barbarian research rate during the early phase, defeating the
geographic/contact distinction introduced for advc.302. The later fallback that
counts a fraction of all known civilizations does not make this harmless; the
whole point of the earlier branch is that actual contact should matter more.

ANCESTRY / INTENT CONTROL
The zero-threshold shape is inherited from AdvCiv's Barbarian-contact feature,
not introduced by current SAS. The source comment immediately above the threshold
already notes that perhaps a threshold of 1 would have the same effect, which is
consistent with the intended threshold being a positive unit-presence test rather
than zero in the Ancient era.

REPAIR DIRECTION
Give the unit threshold an explicit positive floor while retaining later-era
scaling, e.g. conceptually:

    iUnitThresh = max(1, currentEra)

Then the early phase requires at least some civilization presence in an area and
more than that threshold of Barbarian units, instead of globalizing contact in
era 0.

Classification: inherited AdvCiv Barbarian-research/contact-localization defect.


ADDITIONAL NEGATIVE CONTROLS / SECTIONS CLOSED IN THIS TRANCHE
--------------------------------------------------------------

- AgentIterator::nextIndex was challenged because KI#207 established that it can
  be the wrong denominator when a caller applies an extra manual filter. That is
  not happening in the current CvTeam uses. MemberIter and TeamIter<CIV_ALIVE>
  either traverse exact cached sequences or account internally for predicate
  skips, so getCurrentEra, getHandicapType and both Barbarian-research alive-team
  divisors use valid counts. F086 is specifically the zero contact threshold.
- Permanent-Alliance field/state backfill did not establish another omitted live
  state beyond F078-F081. Tech and team-shared-building effects are deliberately
  cross-applied before player reassignment; project-making counts need their
  explicit merge because CvPlayer::setTeam does not transfer them automatically.
- The irreversible global Manhattan Project unlock is not a project-removal bug.
  Normal resetVictoryProgress only removes spaceship projects, while the
  Manhattan unlock is intentionally global/historical once completed.
- Cached war-enemy classification remains coherent when an existing wartime
  opponent changes vassal status: CvTeam::setVassal explicitly adjusts the
  cached vassal-war-enemy component on every team currently at war with it.
- Defensive-pact / peace-treaty first-list assumptions continue to be supported
  by the deal construction contract for those symmetric one-item trade deals.
- testCircumnavigated deliberately marks an already-complete very-early map as
  circumnavigated without awarding movement when the elapsed-turn gate is not
  met. Advanced Start full-map visibility can reach that policy, but this is an
  intentional early/scenario anti-reward shape, not promoted independently from
  F084.
- CvTeam::doTurn contains a source-authored "Known issue in networked
  multiplayer" around the advc.134a peace-offer staging workaround. It is being
  retained as an acknowledged limitation to investigate, not promoted as F087
  without a concrete reproducible state failure and repair contract.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F086
NEXT ROOT ID: F087 UNUSED

Source-order backfill now includes the early turn/war/contact aggregates,
Barbarian contact/research, team-size/iterator denominators, relation caches,
Permanent-Alliance field-state reconciliation, project/victory state, research
and technology transitions, espionage/reveal helpers, and most late helpers.
The file remains open.

Next work:
1. Continue war/peace/contact and other earlier source-order gaps, including the
   acknowledged advc.134a network-MP staging limitation only if concrete behavior
   can be proven.
2. Audit remaining unchanged inherited helpers as well as current-vs-base/Civ4CE
   semantic deltas; do not assume a quiet SAS diff means inherited code is safe.
3. Continue divisor/index/state-transition checks and caller reachability.
4. Adversarially challenge F078-F086 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP05 -- CvTeam.cpp Permanent-Alliance espionage self-target checkpoint
===============================================================================

AUTHORITATIVE SOURCE
--------------------
2026-08-25 08:50 light-source snapshot, practical 6252 / HEAD 7aaf0b6a... plus
its bundled/reconciled audit album and staged KI#388-KI#390 implementation.

This checkpoint is append-only relative to C013-WIP04. No repository source was
modified.


F087 / provisional KI#409 -- Permanent Alliance redirects absorbed-team
espionage against the survivor into the survivor's self-target EP, polluting
K-Mod's total-unspent denominator and AI sea-spy targeting
--------------------------------------------------------------------------

CURRENT CODE / REACHABILITY
CvTeam::shareItems merges the survivor A and absorbed team B's directed
espionage-point arrays by iterating every TeamTypes slot without excluding the
two constituent team IDs:

    setEspionagePointsAgainstTeam(eLoopTeam,
        GET_TEAM(eTeam).getEspionagePointsAgainstTeam(eLoopTeam) +
        getEspionagePointsAgainstTeam(eLoopTeam));

For the iteration where eLoopTeam == A (the surviving team's own ID), this
becomes conceptually:

    A.EPvsA = B.EPvsA + A.EPvsA

B.EPvsA is perfectly ordinary pre-alliance espionage accumulated against the
future ally. Once B's players are reassigned to team A, however, those points
have no valid external target. They have instead been stored as A's espionage
against itself.

The analogous A->B constituent slot is also obsolete after B disappears and
should not become meaningful merged espionage. Outsider-target slots are the
ones that should be combined.

SELF EP IS NOT DEAD STATE
K-Mod's CvTeam::getTotalUnspentEspionage sums:

    for (TeamIter<CIV_ALIVE> it; it.hasNext(); ++it)
        iTotal += getEspionagePointsAgainstTeam(it->getID());

TeamIter<CIV_ALIVE> is backed directly by CvAgents::CIV_ALIVE, whose cache
contains every alive non-Barbarian civilization team. There is no relation
predicate excluding the iterating team; AgentPredicates explicitly distinguishes
NOT_SAME_TEAM_AS / OTHER_KNOWN_TO when self is supposed to be excluded. Thus A's
new A->A EP is included in getTotalUnspentEspionage.

GAMEPLAY EFFECT
CvUnitAI::AI_specialSeaTransportSpy uses getTotalUnspentEspionage as the
denominator when measuring how much of the team's espionage stock is aimed at
each real target:

    iValue = 1000 * EPvsTarget / max(1, iTotalPoints);

For example, suppose before the alliance:

    A has 1000 EP against outsider C
    B has 1000 EP against A

After B is absorbed, the current merge leaves A with 1000 EP against C plus
1000 EP against itself. A's real C target therefore falls from a 100% share of
its useful unspent EP to only 50% in AI_specialSeaTransportSpy, despite no real
espionage having been redirected away from C. This depresses the base values used
to select and route sea-transported spies and can alter whether/where those
missions are pursued. SASGameRecord's total-unspent value is contaminated too.

Other espionage AI code often uses explicit OTHER/NOT_SAME team iterators and is
therefore protected; that is a useful control and confirms that the unfiltered
K-Mod total is semantically intended to represent useful rival-target EP rather
than self-target residue.

ANCESTRY
The bad constituent-slot merge is inherited. Vanilla/Civ4CE's shareItems loops
all MAX_TEAMS too and uses max rather than sum, so B->A EP can already be written
into A->A. Kek-Mod/AdvCiv practical 1400 ([dlph.26], "Permanent alliance
bugfixes and adjustments") changed espionage merging from max to sum but retained
the same missing constituent-team exclusion. Current AdvCiv/SAS inherits that
shape.

REPAIR DIRECTION
During Permanent-Alliance espionage merging:

1. Sum the two constituent teams' EP only for genuine outsider targets.
2. Discard/clear the survivor-self and absorbed-team target slots; espionage
   previously spent by A against B or B against A cannot remain useful after the
   two teams become one.
3. As defensive hardening, make getTotalUnspentEspionage explicitly exclude the
   team's own ID (e.g. a NOT_SAME_TEAM_AS iterator), so stale/corrupt self-target
   EP can never distort AI denominators again.

Do not redistribute mutual constituent espionage to unrelated rivals; there is no
source-supported target for such a transfer.

Classification: inherited BtS/K-Mod Permanent-Alliance espionage-state defect
with current K-Mod AI gameplay consequence.


ADDITIONAL NEGATIVE CONTROLS / SECTIONS CLOSED IN THIS TRANCHE
--------------------------------------------------------------

- The F087 effect is not caused by AgentIterator counting semantics. The
  CvAgents::CIV_ALIVE cache itself includes the survivor team; getTotalUnspent
  simply chooses an unfiltered iterator. This is distinct from the earlier
  KI#207 nextIndex/manual-filter bug.
- Outsider -> merged-team espionage is separately combined later in addTeam and
  is directionally correct. F087 concerns the constituent A/B target slots in
  the survivor's own espionage array, not the outsider reverse merge.
- The absorbed-team target slot A->B becomes irrelevant once B is dead and is
  normally excluded from alive-team totals; the harmful slot is specifically
  B->A being redirected into survivor A->A. Clearing both constituent slots is
  nevertheless the clean merge invariant.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F087
NEXT ROOT ID: F088 UNUSED

The Permanent-Alliance field/state matrix has now additionally covered directed
espionage-point target identity and the relevant K-Mod total/AI consumer. The
file remains open.

Next work:
1. Continue remaining war/peace/contact and source-order gaps, then unchanged
   inherited helpers and late state transitions.
2. Keep the advc.134a network-MP peace-offer limitation unpromoted unless a
   concrete reproducible failure and repair contract emerge.
3. Continue raw state/index/divisor/caller checks and current/base/Civ4CE
   comparisons.
4. Adversarially challenge F078-F087 before any C014 decision.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP06 -- CvTeam.cpp Advanced-Start dependency + hasSeen merge checkpoint
===============================================================================

AUTHORITATIVE SOURCE
--------------------
2026-08-25 08:50 light-source snapshot, practical 6252 / HEAD 7aaf0b6a... plus
its bundled/reconciled audit album and staged KI#388-KI#390 implementation.

This checkpoint appends to the user-received C013-WIP05 artifact. No repository
source was modified.


F088 / provisional KI#410 -- Advanced Start tech refunds validate only later
techs and the acting player's units/buildings, allowing live team/map
infrastructure dependencies to survive after their prerequisite tech is sold
-------------------------------------------------------------------------------

CURRENT CODE / REACHABILITY
CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_TECH) obtains the refund
price from getAdvancedStartTechCost(eTech, false), then removes the technology
from the entire team through:

    GET_TEAM(getTeam()).setHasTech(eTech, false, getID(), false, false);

and refunds the Advanced Start points.

The removal-side validator in CvPlayer::getAdvancedStartTechCost does check:

1. later team technologies that require eTech;
2. units belonging to *this player* whose unit definition requires eTech;
3. buildings in *this player's* cities whose building definition requires eTech.

Two live dependency classes are omitted.

A. OWNED IMPROVEMENTS / ROUTES
------------------------------
The validator never scans map infrastructure already purchased through the
technology. The Advanced Start add-cost helpers do enforce the build technology
when the infrastructure is placed, but removing the technology later does not
remove the resulting improvement/route and does not retroactively invalidate its
ordinary plot yield/movement effect.

Current SAS gives simple reachable examples:

- TECH_AGRICULTURE has an Advanced Start cost and enables BUILD_FARM and
  BUILD_PLANTATION. Farms/Plantations themselves have Advanced Start costs.
  Agriculture can therefore be bought, the infrastructure purchased, and the
  technology refunded while the placed infrastructure remains.
- TECH_THE_WHEEL enables BUILD_ROAD. A Road can be purchased and then the tech
  refunded while the physical route remains.
- Current SAS places BUILD_RAILROAD behind TECH_STEAM_POWER. Railroad likewise
  has an Advanced Start cost, so the same buy-place-refund sequence exists there.

Technology loss can remove some *technology-gated* secondary effects (for
example bonus revelation/connectivity through the normal team-tech machinery),
but it does not erase the infrastructure's own persistent map state and ordinary
route/improvement benefits. Thus the refund can retain value whose prerequisite
was paid only temporarily.

B. TEAMMATE UNIT / BUILDING DEPENDENCIES
----------------------------------------
Technology ownership is team-wide, but the unit/building validation uses only
FOR_EACH_UNIT(..., *this) and FOR_EACH_CITY(..., *this) for the player pressing
Refund. It never iterates the other members of that team.

Therefore, on a multi-player team, player B can satisfy the local refund check
while teammate A still owns units/buildings that require eTech; the subsequent
setHasTech(false) removes the technology from the whole team anyway. This is the
same incomplete dependency-validation root, not a separate finding.

CONTROL / ANCESTRY
------------------
Base AdvCiv 1.14 retains the same removal validator shape. The supplied
Civ4CE/BtS control likewise checks dependent team techs followed by the acting
player's units/cities and does not cover existing routes/improvements or the
other team members. This is therefore an inherited BtS Advanced Start contract
hole, not a SAS regression.

REPAIR DIRECTION
----------------
Keep the repair at the Advanced Start refund-validation boundary rather than
trying to destroy objects after team technology has already been removed:

1. For bAdd == false, validate unit/building dependencies across *all* living
   members of the team, because the state being removed is team-wide.
2. Reject removal while an owned improvement or route whose construction/build
   requires eTech remains on the map (using the actual build/prerequisite
   relationship rather than a hard-coded improvement/route list).
3. Preserve the existing later-tech dependency checks.

This also composes naturally with F084/F085: Advanced Start technology removal
needs to be treated as a reversible transaction only when all persistent state
that depends on that technology can remain internally valid.

Classification: inherited BtS Advanced Start dependency/refund-validation defect.


F089 / provisional KI#411 -- Permanent Alliance fails to merge K-Mod hasSeen
historical civilization-identity knowledge, so absorbed players can forget a
civilization identity they knew before joining the surviving team
-------------------------------------------------------------------------------

CURRENT STATE / CONTRACT
------------------------
K-Mod practical 728 (77a9b8d554...) introduced CvTeam::m_abHasSeen. Its commit
message explicitly describes this as broader than first contact: it tracks teams
that have "been seen" and can become true through "map trading and things like
that." The flag is deliberately historical/persistent and is used by AdvCiv/K-Mod
to decide whether a civilization's leader/civilization identity and player color
may be shown to an observer.

Current sources set hasSeen when, among other things:

- a team actually meets another team (makeHasMet/makeHasSeen);
- a revealed plot exposes an owner (CvPlot::setRevealedOwner);
- a city becomes revealed (CvCity::setRevealed).

PERMANENT-ALLIANCE OMISSION
---------------------------
CvTeam::addTeam unions actual first-contact state by calling meet for contacts
known to either constituent team, and later transfers revealed plot/city data.
However, addTeam/shareItems/shareCounters never union m_abHasSeen itself.

Current-map transfer often masks this omission: if the absorbed team currently
has a revealed city or a revealed owner that still matches the plot's current
owner, copying that information causes the survivor to call makeHasSeen again.
But hasSeen is historical precisely because that current evidence need not still
exist.

CONCRETE LOSS CASE
------------------
A normal sequence can therefore be:

1. Team B learns civilization C's identity without first contact, e.g. through
   map/city information; B.hasSeen(C) becomes true.
2. The particular city/territory through which C was identified later changes
   owner (or otherwise no longer supplies current C-owned revealed evidence),
   while C remains alive elsewhere and B still correctly retains historical
   hasSeen(C).
3. B forms a Permanent Alliance into surviving team A, which has never seen/met
   C.
4. The transferred current map can identify the *current* owner, but there is no
   code transferring historical B.hasSeen(C).
5. B's players are reassigned to team A. Their identity-visibility checks now
   consult A.hasSeen(C), which remains false. The former B player can therefore
   have C's name/civilization/color concealed again despite having known it
   immediately before the alliance.

The state loss is especially clear because the absorbed CvTeam object is no
longer used after the merge; leaving the bit only on B is equivalent to deleting
that historical knowledge.

ANCESTRY / CONTROL
------------------
The omission follows K-Mod's addition of m_abHasSeen: the K-Mod practical-728
change introduced/reset/serialized the field and hooked it to reveal/contact
paths, while the inherited Permanent-Alliance merger was not taught to combine
it. Current base AdvCiv 1.14 still has no hasSeen union in addTeam/shareItems or
shareCounters. Vanilla Civ4CE/BtS predates this K-Mod field and therefore cannot
serve as a field-level control.

REPAIR DIRECTION
----------------
During Permanent Alliance, union historical identity knowledge explicitly:

    if (absorbed.isHasSeen(eOther))
        survivor.makeHasSeen(eOther);

for relevant team IDs (and, if the disappearing team's state is needed during
pre-reassignment processing, preserve the symmetric temporary direction as
appropriate). Do not rely on current revealed ownership to reconstruct a
historical flag; that loses exactly the cases m_abHasSeen was introduced to
remember.

Classification: inherited K-Mod Permanent-Alliance state-merge omission with
observer-facing identity regression.


ADDITIONAL NEGATIVE CONTROLS / SECTIONS CLOSED IN THIS TRANCHE
--------------------------------------------------------------

- A mechanical current-schema serialization matrix compared all 72 live CvTeam
  data members declared in the persistent/transient state block against reset,
  read and write. Every persistent member has symmetric read/write handling.
  The only two members intentionally absent from serialization are advc.134a's
  m_eOfferingPeace and m_iPeaceOfferStage; both are transient EXE diplomacy-
  handshake state reset to NO_TEAM/0 rather than durable game state. No hidden
  save/load root was found there.
- The advc.134a source comment still acknowledges a networked-multiplayer peace-
  offer limitation, but no deterministic state failure + repair contract has yet
  been established. It remains unpromoted rather than turning an author warning
  into an unsupported finding.
- Defensive Pact/Open Borders/peace-treaty deal paths that look one-sided locally
  are constructed as symmetric/dual deal data where their callers require that
  contract; no additional relation-state root was established from those paths.
- hasSeen should *not* be inferred solely from hasMet: K-Mod intentionally made
  it broader than contact. Conversely, current-map transfer can legitimately
  re-create many hasSeen bits, which is why F089 is specifically about the
  historical residue that current revealed ownership cannot reconstruct.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F089
NEXT ROOT ID: F090 UNUSED

The source-order and state-matrix audit has now additionally covered Advanced
Start technology-refund dependency scope, all current CvTeam serialization
members, and K-Mod hasSeen identity state across Permanent Alliances. The file
remains open.

Next work:
1. Continue unchanged inherited war/peace/contact helpers and cached-counter
   transitions, then remaining source-order gaps.
2. Continue raw state/index/divisor/caller checks and current/base/Civ4CE
   comparisons; keep distinguishing inherited defects from SAS regressions.
3. Revisit the advc.134a network-MP staging warning only if a concrete reachable
   failure and repair can be proven.
4. Adversarially challenge F078-F089 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP07 -- CvTeam.cpp audit continuation
===============================================================================

Authoritative source remains the 2026-08-25 08:50 light-source snapshot,
practical 6252 / HEAD 7aaf0b6a... plus its bundled/reconciled audit album and
staged KI#388-KI#390 implementation.

This checkpoint appends directly to the user-received C013-WIP06 artifact. No
repository source was modified.


F090 / provisional KI#412 -- AdvCiv reparations announcements resolve
player-local city IDs through the giving team's leader, so a city ceded by a
nonleader teammate can be misnamed or omitted
-------------------------------------------------------------------------------

PAYLOAD CONTRACT
----------------
TRADE_CITIES stores the city ID belonging to the specific player who owns and
cedes the city. City IDs are player-local, not team-global. CvDeal::startTrade
uses the correct contract when executing a city trade:

    kFromPlayer.getCity(trade.m_iData)

where kFromPlayer is the actual player on the giving side of that deal item.

ADVCIV ANNOUNCEMENT MISMATCH
----------------------------
The advc.039 reparations-announcement path discards that player identity before
formatting the peace terms. CvTeam::tradeItemString receives only the giving
TeamTypes and, for TRADE_CITIES, resolves the same numeric city ID through:

    GET_PLAYER(kFrom.getLeaderID()).getCity(iData)

That is only correct if the giving player is the team leader. After a Permanent
Alliance, one team can contain several players with independent city-ID
namespaces, so identical numeric city IDs can refer to entirely different
cities.

CONCRETE REACHABLE CASE
-----------------------
1. Players A and B are members of the same team after a Permanent Alliance; A is
   the team's leader.
2. B participates in a later peace deal and cedes B's city ID 0.
3. A also owns its own unrelated city ID 0.
4. CvDeal executes the item correctly through B and transfers B's city.
5. The advc.039 announcement formatter later resolves numeric ID 0 through team
   leader A and can announce A's different city as the reparations city.
6. If A has no city with that local ID, the lookup fails and the city term can
   disappear from the formatted reparations instead.

The deal path is not leader-only: CvDeal participants are PlayerTypes, and the
peace-treaty/trade machinery remains reachable for individual members of a
multi-player team. Therefore this is not merely a malformed synthetic payload.

ANCESTRY / CONTROL
------------------
The mismatch is already present in the AdvCiv advc.039 implementation. The
ordinary CvDeal execution path provides the clean control because it retains the
specific giving player and resolves TRADE_CITIES correctly from that player's
city table.

The neighboring reparations payloads do not share this defect:
- technologies, civics and religions use global info IDs;
- maps are naturally team-level;
- the relevant target/team arguments are otherwise supplied consistently.
City IDs are exceptional because they are allocated independently per player.

REPAIR DIRECTION
----------------
Do not try to recover the city owner by scanning members of the giving team:
two teammates can legitimately own the same numeric city ID. Preserve the actual
reparations-giving PlayerTypes from CvDeal through the announcement path and use
that player when formatting TRADE_CITIES. TeamTypes can remain sufficient for
team-global trade-item kinds.

Classification: inherited AdvCiv advc.039 UI/reporting payload-identity defect.


ADDITIONAL NEGATIVE CONTROLS / SECTIONS CLOSED IN THIS TRANCHE
--------------------------------------------------------------

- Other trade-item formatting cases around tradeItemString were checked for the
  same player-vs-team identity loss. No comparable mismatch was established:
  their IDs are global or intentionally team-level.
- The BBAI Defensive Pact behavior-2 source warning remains unpromoted. Current
  SAS config uses behavior 0, and the stale warning alone is not enough evidence
  for a current reachable F091 without a deterministic failure + repair.
- advc.134a's networked-multiplayer peace-offer warning likewise remains an
  acknowledged source limitation rather than an audit root until a precise
  reachable state failure can be demonstrated.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F090
NEXT ROOT ID: F091 UNUSED

Next work:
1. Continue remaining aggregate/cached-counter and first-contact helpers, then
   remaining source-order gaps.
2. Continue raw state/index/divisor/caller checks and inherited/base/Civ4CE
   comparisons.
3. Revisit dormant source warnings only if normal current-SAS reachability and a
   concrete repair contract can be proven.
4. Adversarially challenge F078-F090 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP08 -- CvTeam.cpp audit continuation
===============================================================================

Authoritative source remains the 2026-08-25 08:50 light-source snapshot,
practical 6252 / HEAD 7aaf0b6a... plus its bundled/reconciled audit album and
staged KI#388-KI#390 implementation.

This checkpoint appends directly to C013-WIP07. No repository source was
modified.


F091 / provisional KI#413 -- AdvCiv forced-peace duration helper assumes a
peace treaty is the first item of the first deal list, so valid reparations
peace can be reported as having zero turns remaining
-------------------------------------------------------------------------------

ROOT
----
CvTeam::turnsOfForcedPeaceRemaining is meant to recover the remaining lifetime
of a mandatory peace treaty from live CvDeal objects. After confirming that the
team-level isForcePeace flag is active, it scans deals between the relevant
master teams but recognizes a treaty only when:

    d->getLengthFirst() > 0 &&
    d->getFirstList().head()->m_data.m_eItemType == TRADE_PEACE_TREATY

This makes physical list side and list order part of a logical team-level
forced-peace query.

NORMAL REACHABLE COUNTEREXAMPLE
-------------------------------
AdvCiv itself deliberately places peace treaties at the bottom of offer lists.
CvPlayer::updateTradeList removes/re-appends peacetime treaty items specifically
so that they are at the bottom for the diplomacy Python UI.

The ordinary AI peace/reparations path also constructs lists in which a giving
side may contain, for example:

    TRADE_GOLD / TRADE_TECHNOLOGIES / TRADE_CITIES ... TRADE_PEACE_TREATY

and then ensures the other side contains a treaty too before either opening the
human diplomacy popup or calling CvGame::implementDeal for AI-AI peace.
Consequently the resulting deal can have fully symmetric, valid forced peace
while the head of the first list is a reparations item rather than the treaty.

In that state isForcePeace(eOther) is true, but
turnsOfForcedPeaceRemaining(eOther) ignores the live treaty and returns 0.

GAMEPLAY / AI CONSEQUENCE
-------------------------
The helper is not cosmetic. UWAI uses the remaining duration to:
- delay concluding war preparations while more than three treaty turns remain;
- decide whether a switch to an alternative target can fit before forced peace
  ends;
- reduce war-preparation drive in proportion to the treaty time remaining.
CvPlayerAI also uses the helper to gate help/tribute diplomatic actions.

Returning zero therefore lets AI planning and diplomacy behave as though
mandatory peace has already expired even though CvTeam::canDeclareWar still
correctly blocks an actual declaration until the treaty ends.

ANCESTRY
--------
The list-head assumption is present in the advc.104 introduction of
turnsOfForcedPeaceRemaining at AdvCiv practical 1253 / f6ff6a182f.... A later
null/empty-list guard did not change the physical first-list/head requirement.
This is therefore an inherited AdvCiv helper defect rather than an SAS
regression.

REPAIR DIRECTION
----------------
Search both trade lists of each matching deal and recognize
TRADE_PEACE_TREATY anywhere in either list. The logical duration query should
not depend on which participant physically stores the treaty item or on its
position among reparations/annual items.

Do not merely move peace treaties to the head: AdvCiv intentionally keeps some
trade UI lists ordered with the treaty at the bottom, and the robust contract is
that the helper locate the item rather than impose UI list order on deal state.

Classification: inherited AdvCiv advc.104 forced-peace duration / deal-layout
contract defect.


ADDITIONAL CONTROLS / OPEN NEIGHBOR
-----------------------------------

- The cached major/minor/vassal war-count audit did not produce a minor-status
  sibling: CvInitCore explicitly treats changing minor-civ status after game
  start as unsupported, while setVassal already repairs the supported live
  vassal reclassification.
- CvPlayer::resetDualDeal also assumes first-list storage, but unlike F091 it
  scans the whole first list rather than only the head. Normal symmetric peace
  construction therefore still finds the treaty there. A separate defect would
  require proving a final live one-sided-treaty representation, so this remains
  under challenge rather than being promoted from the source comment alone.
- K-Mod/AdvCiv deliberately uses one-sided peace-treaty items transiently while
  counter-proposing because the EXE dislikes adding the item to both sides.
  Whether every accepted route later symmetrizes the team-level forced-peace
  state is still being traced. F091 does not depend on this unresolved question.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F091
NEXT ROOT ID: F092 UNUSED

Next work:
1. Finish the one-sided peace-treaty / resetDualDeal challenge only if a normal
   accepted-deal path can be proven; otherwise reject it explicitly.
2. Continue remaining war/peace/contact helpers, cached transitions and
   unchanged inherited source-order gaps.
3. Continue raw state/index/divisor/caller closure and current/base/Civ4CE
   controls.
4. Adversarially challenge F078-F091 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP09 -- CvTeam.cpp audit continuation
===============================================================================

Authoritative source remains the 2026-08-25 08:50 light-source snapshot,
practical 6252 / HEAD 7aaf0b6a... plus its bundled/reconciled audit album and
staged KI#388-KI#390 implementation.

This checkpoint appends directly to C013-WIP08. No repository source was
modified.


F092 / provisional KI#414 -- AdvCiv's optimized Tech Share first-contact scan
uses the reverse directional hasMet relation before it is initialized, making
The Internet's free-tech grant depend on which team initiates contact
-------------------------------------------------------------------------------

ROOT
----
CvTeam::meet establishes contact by calling makeHasMet in sequence:

    makeHasMet(eTeam, ...);
    GET_TEAM(eTeam).makeHasMet(getID(), ...);

Each makeHasMet sets only its own directional has-met turn and immediately calls
updateTechShare(). Current updateTechShare iterates:

    TeamIter<CIV_ALIVE,OTHER_KNOWN_TO> itOther(getID())

but AgentIterator's OTHER_KNOWN_TO predicate is deliberately directional in the
opposite direction:

    GET_TEAM(getTeam(kAgent)).isHasMet(m_eTeam)

Thus the first team's Tech Share scan runs before the second team has recorded
that it knows the first team. The newly encountered team is absent from that
scan even though the first team has already set its own hasMet(other) state.
The second team's makeHasMet then gets the reciprocal state and scans normally.

CURRENT-SAS REPRODUCTION
------------------------
The current XML has one Tech Share source, PROJECT_THE_INTERNET, with
<iTechShare>2</iTechShare> and <iMaxGlobalInstances>1</iMaxGlobalInstances>.
That makes a clean asymmetric reproduction:

- A owns The Internet and already knows C.
- C and newly encountered B both know technology X; A does not.
- B does not own The Internet (the project is globally unique).
- Contact with B should raise A's known holders of X from one player to two and
  immediately grant X.

If the contact path calls A.meet(B), A is processed first. Its updateTechShare
scan sees C but cannot yet see B through OTHER_KNOWN_TO, so it remains at 1/2
and X is not granted. B is processed second but has no Tech Share capability and
cannot compensate.

If the same first contact is reached with B.meet(A), B is processed first and A
second. By the time A scans, B's reciprocal has-met state exists, so A sees both
B and C and correctly receives X.

Both caller orientations occur naturally. Unit movement can call the observing
team's meet(movingTeam), while plot/territory visibility can call the owning
team's meet(viewingTeam). There is no invariant that the Internet owner is
always second.

WHY THE LOSS CAN PERSIST
------------------------
Tech Share updates are event-driven, not continuously recomputed. They run on
first contact, on Tech Share count changes, and when a team acquires a
technology (which asks other teams to update that particular tech). In the
reproduction B already had X before first contact, and B cannot build a second
Internet. Therefore no same-contact fallback necessarily rechecks X for A.
The missed grant can persist until some unrelated later event causes another
full Tech Share update.

ANCESTRY
--------
Vanilla BtS updateTechShare scans alive teams and checks the receiving team's own
relation:

    if (isHasMet((TeamTypes)iI))

By the time makeHasMet calls updateTechShare, that receiving-side relation has
already been set, so vanilla includes the newly met team.

AdvCiv practical 1837 / 74de2f9c0f... optimized the scan to
TeamIter<CIV_ALIVE,KNOWN_TO>. AgentIterator's KNOWN_TO relation uses the reverse
other.isHasMet(this) direction. Practical 1842 later changed KNOWN_TO to
OTHER_KNOWN_TO only to exclude self; it retained the same directional predicate
and therefore the ordering defect. Current SAS inherits it.

REPAIR DIRECTION
----------------
Prefer establishing both reciprocal has-met states before evaluating Tech Share.
A causal repair is to remove updateTechShare() from makeHasMet and, in meet,
call updateTechShare() for both teams only after both makeHasMet calls have
finished. This also gives Tech Share the natural logical contract: first make
contact symmetric, then evaluate effects that depend on contact.

Alternatively, updateTechShare could explicitly iterate from the receiving
team's own isHasMet relation, but keeping dependent updates after reciprocal
contact is less fragile and avoids similar ordering assumptions later.

Classification: inherited AdvCiv practical-1837 first-contact / AgentIterator
relation-direction regression.


ADDITIONAL CONTROLS / REJECTIONS
---------------------------------

- A possible stale m_abNoTradeTech state after tech removal/reacquisition was
  rejected for normal gameplay. The live ordinary tech-removal paths are
  Advanced Start and pregame True-Starts map regeneration. Advanced Start techs
  are self-purchased and do not set NoTradeTech; map regeneration rebuilds setup
  state. A stale true flag requires an external Python/WorldBuilder-style
  removal after a traded/stolen tech, so it is robustness/API residue rather
  than a normal-game F092 root.
- The earlier one-sided peace-treaty neighbor remains rejected as a separate
  finding because the final human-acceptance transformation is partly inside
  the closed EXE. F091 remains independently proven by ordinary symmetric
  reparations deals and does not depend on that representation.
- Team death/revival and setTeam member/alive-count transitions were re-read as
  controls. When an absorbed PA team reaches zero alive members, its dead-team
  war/war-plan cleanup operates on the obsolete team while the surviving team's
  already-propagated relations remain live; no independent root was established
  there.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F092
NEXT ROOT ID: F093 UNUSED

Next work:
1. Continue remaining member/death/revival, vote/trading-counter, first-contact
   and war-weariness helpers after the F092 contact-order root.
2. Continue circumnavigation and remaining unchanged inherited source-order
   sections, then raw field/index/divisor/caller closure.
3. Reconcile unusual helpers against base AdvCiv/Civ4CE and commit archaeology
   before promotion.
4. Adversarially challenge F078-F092 before any C014 decision.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP10 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

F093 / provisional KI#415 -- AdvCiv alive-only war-weariness iterator freezes
weariness against dead civilizations instead of applying BtS's explicit decay
-------------------------------------------------------------------------------

ROOT
----
CvTeam::doWarWeariness stores war weariness per opposing team. Vanilla BtS /
the local Civ4CE control iterate the full civilization-team domain and contain
an explicit stronger peace-decay condition for a dead target team. AdvCiv
practical 1837 (74de2f9c0f...) converted this loop to TeamIter<CIV_ALIVE> while
leaving the inherited !target.isAlive() branch in place. That branch is now
unreachable by construction.

CONSEQUENCE
-----------
When an opponent dies, stored war weariness against it no longer receives the
BtS dead-team decay; it freezes indefinitely while the target is absent. AdvCiv
has a real civilization/player revival path, so the stale value can later
become relevant again: it re-enters AI future-war cost evaluation and, upon a
renewed war, can again contribute to city war-weariness anger.

CONTROL / ANCESTRY
------------------
The Civ4CE/BtS implementation proves that dead teams were intentionally part of
the loop: it iterates all teams and explicitly tests !isAlive() in the decay
condition. Practical 1837's iterator optimization removed those teams without
removing or replacing that semantic requirement. This is therefore an
inherited AdvCiv practical-1837 iterator-regression family, independent of
F092's directional first-contact Tech Share regression from the same commit.

REPAIR DIRECTION
----------------
Restore iteration over the full civilization-team domain for stored nonzero war
weariness, or otherwise process dead targets explicitly so the inherited
strong dead-team decay remains reachable.

Classification: inherited AdvCiv practical-1837 iterator semantic regression.


CONTROLS / REJECTIONS SINCE WIP09
---------------------------------
- A possible stale m_abNoTradeTech flag after tech removal/reacquisition was
  rejected for normal gameplay. Ordinary removal paths do not naturally create
  the required traded/stolen-tech stale state.
- A turn-zero/very-early circumnavigation oddity remains unpromoted: K-Mod's
  source history explicitly changed the minimum elapsed-turn behavior, so the
  observed early-map behavior is consistent with deliberate policy rather than
  a source-proven defect.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F093
NEXT ROOT ID: F094 UNUSED

Next work:
1. Use AdvCiv practical 1837 as a focused differential oracle: compare every
   iterator substitution against the precise condition it removed, especially
   directional KNOWN_TO relations and ALIVE/MAJOR_CIV restrictions.
2. Continue remaining vote/trading/war/contact/circumnavigation source-order
   gaps, then raw field/index/divisor/caller closure.
3. Reconcile any candidate against base AdvCiv/Civ4CE and commit archaeology.
4. Adversarially challenge F078-F093 before any C014 decision.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP11 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

F094 / provisional KI#416 -- AdvCiv practical-1837 alive-only team-member
optimization skips dead teammates during tech acquisition, so revived members
can permanently miss era advancement and player-level tech effects
-------------------------------------------------------------------------------

ROOT
----
AdvCiv practical 1837 (74de2f9c0f...) replaced several explicit all-player
team loops in CvTeam::setHasTech / CvTeam::processTech with MemberIter. MemberIter
is an ALIVE,MEMBER_OF player iterator. Two of the replaced BtS loops
intentionally had no isAlive test:

1. setHasTech advanced every player assigned to the team to the acquired
   technology's era when needed.
2. processTech applied the technology's persistent per-player modifiers to
   every player assigned to the team.

After practical 1837, both operations silently exclude a dead member of a
still-alive multi-player team.

REACHABLE CONSEQUENCE
---------------------
A multi-member team can remain alive while one member is dead. The surviving
member can continue researching. If the dead member is later revived, the
player still belongs to the team and therefore shares its known technologies,
but CvPlayer::setAlive(true) does not replay the team's known technologies or
reconstruct the skipped player-level effects.

Current SAS contains many concrete player-level tech effects that can therefore
be missed while dead, including (examples, not an exhaustive list):
- Mathematics: +50% feature production;
- Currency / Corporation: +1 trade route;
- Electricity: +50% worker speed;
- Bioengineering: +3 health;
- AI: +50% worker speed, +1 health, +2 happiness;
- Naturalism: +1 specialist research.

The same optimization also means a dead member does not receive the normal
currentEra increase when a later-era team technology is acquired. On revival,
that player can therefore have the team's technology set but stale era and
stale persistent player modifiers.

CONTROL / ANCESTRY
------------------
The Civ4CE/BtS control loops all players and checks only player.getTeam()==this
for these persistent tech-acquisition effects; there is deliberately no alive
condition. Practical 1837 is the exact change that replaces those loops with
alive-only MemberIter. The real AdvCiv revival path (CvPlayer::setAlive(true),
including CvGame::reviveActivePlayer / verifyAlive) does not replay known team
technologies.

REPAIR DIRECTION
----------------
Restore the old semantic population for persistent team-tech effects: process
players assigned to the team even while dead (e.g. an appropriate EVER_ALIVE /
team-membership iteration where safe), both for current-era advancement and
processTech's persistent per-player modifiers. Alternatively, revival would
need an explicit complete replay/reconstruction of all team-tech effects, which
is broader and more fragile.

Classification: inherited AdvCiv practical-1837 alive-filter semantic
regression.


F095 / provisional KI#417 -- AdvCiv practical-1837 getDefensivePower refactor
can count the excluded attacker's own vassals as defenders of a Defensive-Pact
target
-------------------------------------------------------------------------------

ROOT
----
CvTeam::getDefensivePower(eExcludeTeam) is used by UWAI / legacy war evaluation
to estimate a target's defensive coalition while excluding the evaluating /
attacking team.

Before practical 1837, the function iterated teams. It skipped eExcludeTeam at
the team level, then added getPower(true) for the target master and its
Defensive-Pact partners. K-Mod's own comment records the important invariant:
vassals cannot themselves hold Defensive Pacts; only their master does.

Practical 1837 rewrote the function to iterate individual alive players. It now
skips a player only when player.getTeam()==eExcludeTeam, then derives that
player's master and counts the player whenever the target master is the same as,
or has a Defensive Pact with, that master.

REACHABLE CONSEQUENCE
---------------------
Let master A own vassal V and have a Defensive Pact with target T. Masters with
vassals are allowed to sign Defensive Pacts; only being a vassal blocks pact
signing. UWAI can also evaluate / scheme against a current DP partner because
canEventuallyDeclareWar does not treat a Defensive Pact as permanent forced
peace.

For T.getDefensivePower(A):
- old code skips A. V has no independent DP with T, so V is not counted;
- new code skips A's direct players, but V's players have master==A. Since T's
  master has a DP with A, every V player is added to T's defensive power.

Thus the attacker's own vassal military can be counted on the target's side.
This inflates target-power ratios used throughout UWAI / CvTeamAI and can make
wars against a DP partner look materially harder simply because the attacker
has vassals that would actually belong to the attacking bloc.

CONTROL / ANCESTRY
------------------
The exact team-to-player rewrite appears in AdvCiv practical 1837 /
74de2f9c0f.... The old implementation's team-level exclusion plus the K-Mod
"only our master will have defensive pacts" invariant avoids this case.
Current CvPlayer::canPossiblyTradeItem permits a non-vassal master to sign a DP
even when it owns vassals.

REPAIR DIRECTION
----------------
Exclude the attacking/master bloc before applying the defensive-coalition test,
not merely players whose direct team equals eExcludeTeam. For the principal
UWAI call contract, players whose master is the excluded attacking master must
not be counted as defenders of a DP target. Preserve careful behavior for any
rare callers that pass a vassal as eExcludeTeam rather than assuming every
caller is a free master.

Classification: inherited AdvCiv practical-1837 refactor semantic regression.


PRACTICAL-1837 CONTROLS / REJECTIONS SINCE WIP10
-------------------------------------------------
- setVassal's optimized KNOWN_POTENTIAL_ENEMY_OF(master) war-plan cleanup was
  challenged but not promoted. That branch explicitly clears plans the master
  may have forced upon the vassal; requiring the master to know the rival is
  coherent, while an independent vassal plan against a civ unknown to the
  master need not be erased.
- getVotes / isFullMember alive-member conversion was not promoted: dead
  players contribute no live voting population/cities, and no persistent
  state is skipped.
- getTypicalUnitValue, assets, units/cities/population/power-by-area and making
  counts are dynamic live-state aggregates; their old loops already filtered
  alive players where relevant, so MemberIter preserves the population.
- setOpenBorders attitude-cache updates remain a negative control: revival
  performs broader AI attitude recomputation, so the alive-only update does not
  establish a persistent missing-state root analogous to F094.
- verifySpyUnitsValidPlot's old loop already required isAlive, so MemberIter is
  semantically equivalent there.
- getHasMetCivCount / other KNOWN_TO helpers retain a transient directional
  asymmetry during the first half of CvTeam::meet, but the durable concrete
  Tech Share failure is already normalized as F092; no separate root has been
  manufactured without another persistent consequence.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F095
NEXT ROOT ID: F096 UNUSED

Next work:
1. Finish the practical-1837 iterator differential, especially relation filters
   and any old branches made impossible by ALIVE / MAJOR_CIV restrictions.
2. Resume remaining vote/trading/contact/war/circumnavigation source-order gaps,
   then raw field/index/divisor/caller closure.
3. Reconcile every surviving candidate against base AdvCiv/Civ4CE and commit
   archaeology before promotion.
4. Adversarially challenge F078-F095 before any C014 decision.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP12 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues directly from C013-WIP11. It physically anchors the
post-WIP11 Tech Share player-count finding and records the latest whole-file /
Permanent-Alliance negative controls before the audit continues from F097.

F096 / provisional KI#418 -- Kek-Mod player-count Tech Share counts dead team
members through getNumMembers(), allowing eliminated teammates to satisfy The
Internet threshold
-------------------------------------------------------------------------------

ROOT
----
Kek-Mod / AdvCiv changed Tech Share (currently The Internet in SAS) from
counting qualifying TEAMS to counting qualifying PLAYERS. The live
CvTeam::updateTechShare implementation does this by iterating alive known teams
and, when a team knows the candidate technology, adding that team's
getNumMembers():

    if (itOther->isHasTech(eTech))
        iCount += itOther->getNumMembers(); // kekm.38: was +1

CvTeam::getNumMembers() is total assigned team membership, not alive-member
count. A dead teammate remains a member of its surviving team, so a qualifying
alive team can contribute eliminated players to the Tech Share threshold.

CONCRETE SAS CASE
-----------------
The Internet is the current globally unique Tech Share source and uses
TechShare threshold 2. Let Internet owner A know team X. Team X has two members,
one alive and one eliminated, and knows technology T (technology ownership is
team-wide). A does not know T.

updateTechShare sees one alive qualifying team X, but adds
X.getNumMembers()==2. A therefore receives T immediately even though only one
living player / civilization on X currently knows it.

INTERNAL CONTROL
----------------
The same feature's AI project valuation counts known PLAYERS through an alive
player iterator (PlayerIter<MAJOR_CIV,OTHER_KNOWN_TO>). Thus the implementation
and its AI valuation disagree about whether eliminated teammates count. The AI
sees one living known player in the example above; the actual grant logic sees
two through getNumMembers().

ANCESTRY
--------
AdvCiv practical 3116 / 2cf365b350... is titled "Internet project counts
players, not teams" and changes the grant logic from +1 per team to
getNumMembers(). The commit did not substitute an alive-member count. This is
therefore inherited from the Kek-Mod / AdvCiv player-count implementation, not
an SAS regression.

REPAIR DIRECTION
----------------
Count living members of each qualifying team (e.g. getAliveCount() or an
alive-member iteration), matching the feature's player-count intent and its AI
valuation. Do not revert to team count merely to avoid the dead-member case.

Classification: inherited Kek-Mod / AdvCiv Tech Share counting defect.


POST-WIP11 CONTROLS / REJECTIONS
---------------------------------
- Practical-1837 iterator differential is effectively exhausted. Apart from
  F092/F093/F094/F095, the remaining substitutions preserve the old semantic
  population or affect dynamic live-state queries where alive filtering is
  appropriate.
- CvTeam::getName / getReplayName were challenged because the current callers
  do not manually set bFirst=false. Rejected: AdvCiv practical 2882 explicitly
  refactored setListHelp to take bool& and update bFirst internally, then
  deliberately removed those assignments.
- Counterespionage/stolen-visibility/turns-at-peace timers pause while the
  counterpart team is dead, but unlike F093 this alive-only policy already
  exists in the BtS control; no contrary inherited decay rule was made
  unreachable.
- Permanent-Alliance project counts do NOT sum duplicate projects. Live
  shareCounters computes a positive delta only when the absorbed team has the
  larger count, thereby setting the survivor to max(oldA, oldB). The older
  sum-looking version is commented out. Thus two constituent teams owning the
  same team-limited project do not by themselves push the merged count beyond
  MaxTeamInstances.
- Other nearby getNumMembers uses are not generalized into F096. PA weighting
  and research-cost team-size scaling have different semantics; F096 is strong
  because this feature was explicitly changed to count players who know a tech
  while its own AI valuation counts alive players.


CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F096
NEXT ROOT ID: F097 UNUSED

Next work:
1. Resume whole-file CvTeam.cpp closure after the practical-1837 differential:
   remaining relation/state helpers, signed/cached counters, project/victory
   state, raw field/index/divisor/caller audit.
2. Challenge any suspicious team/member count against its exact semantic
   population rather than treating getNumMembers / alive iterators generically.
3. Reconcile new candidates against base AdvCiv/Civ4CE and commit archaeology.
4. Adversarially reread F078-F096 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP13 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues directly from C013-WIP12. It physically anchors the
cross-file Domestic Advisor coordinate defect found while tracing
CvTeam::isMapCentering and records the latest Permanent-Alliance/state/refactor
controls before the audit continues from F098.

F097 / provisional KI#419 -- AdvCiv Customizable Domestic Advisor X-coordinate
wrapper returns Y, so both coordinate columns display Y once map centering is
unlocked
-------------------------------------------------------------------------------

ROOT
----
AdvCiv added map-centering-aware wrappers for the Customizable Domestic Advisor
coordinate columns so that city coordinates remain hidden until the active team
has obtained map centering. The X wrapper contains a direct copy/paste typo:

    def getX (self, city, szKey, arg):
        if PyPlayer(CyGame().getActivePlayer()).getTeam().isMapCentering():
            return city.getY()
        return "?"

The neighboring Y wrapper also returns city.getY(), as intended. Therefore,
once map centering is available, LOCATION_X and LOCATION_Y both display the
city's Y coordinate.

REACHABILITY / CURRENT SAS CONTROL
----------------------------------
CvCustomizableDomesticAdvisor registers both fields as live optional columns:

    LOCATION_X -> self.getX
    LOCATION_Y -> self.getY

Current SAS also has ordinary map-centering sources (notably TECH_PAPER and
BUILDING_STONEHENGE), so the true-coordinate branch is reachable. Before map
centering both wrappers intentionally return "?"; after unlocking, the X value
is simply wrong whenever city.getX()!=city.getY().

ANCESTRY
--------
AdvCiv practical 3061 / ace7bb9bf28989b2a30e9200cf376a4646099d6f,
"Fix info leaks in Customizable Domestic Advisor", introduced these wrappers.
The original patch already contained return city.getY() in getX. The later
merge-history copy preserves the same typo. This is therefore an inherited
AdvCiv Python/UI defect, not an SAS regression.

REPAIR DIRECTION
----------------
Change the successful branch of getX to:

    return city.getX()

No C++ behavior change is required.

Classification: inherited AdvCiv UI / coordinate-reporting bug, surfaced
cross-file through the CvTeam::isMapCentering audit.


POST-WIP12 CONTROLS / REJECTIONS
---------------------------------
- Permanent-Alliance m_aiExtraMoves merge was challenged because K-Mod itself
  says provenance cannot be distinguished reliably. Under the actual SAS/BtS
  source set, the live merge order is nevertheless coherent: shareItems first
  shares all technologies both ways, thereby equalizing additive technology
  DomainExtraMoves; the later max merge then only needs to preserve a unique
  constituent-only source such as circumnavigation. No F097 from extra moves.
- Tech bMapCentering is one-way in processTech and current TECH_PAPER is an
  Advanced Start removable map-centering technology. This remains a sibling
  challenge to the already-normalized Advanced Start irreversible-state family
  (especially F084/F085); it is not split into another root in this checkpoint.
- The apparent getName/getReplayName missing bFirst=false remains rejected:
  practical 2882 deliberately moved bFirst mutation into setListHelp(bool&).
- changeNoFearForSafetyCount's upper assertion was challenged against XML.
  Current SAS has exactly one NoFearForSafety technology (TECH_NATIONALISM), so
  a normal singleton team cannot accumulate multiple tech-derived increments.
- Permanent-Alliance project counts remain max-merged, not summed.
- Project-art provenance was challenged separately: when the absorbed team has
  more projects, newly created survivor m_aaiProjectArtTypes entries begin at
  -1 and absorbed default/per-instance art is not explicitly copied. However,
  default project-art selection crosses an exported/closed EXE contract and
  -1 is an explicit supported "undefined/default" state finalized at launch.
  A visible failure cannot be source-proved from the supplied trees, so this is
  retained only as an unresolved cosmetic-risk note, not F098.
- Remaining signed capability counters audited in this tranche continue to be
  driven by signed processTech(iChange) transitions; no F083-style fixed-sign
  sibling has survived so far.

CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F097
NEXT ROOT ID: F098 UNUSED

Next work:
1. Continue whole-file CvTeam.cpp closure: remaining relation/state helpers,
   persistent-field provenance, raw index/divisor/caller and sentinel checks.
2. Keep using base AdvCiv/Civ4CE differences only as leads, not proof.
3. Reconcile any new candidate against current SAS reachability and commit
   archaeology before promotion.
4. Adversarially reread F078-F097 before any C014 decision.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP14 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues directly from C013-WIP13. It physically anchors the
Permanent-Alliance TurnsAtPeace merge defect found during the final persistent-
field provenance/adversarial pass and normalizes several late controls before
the audit continues from F099.

F098 / provisional KI#420 -- AdvCiv exact TurnsAtPeace counter is merged
asymmetrically by Permanent Alliance: outsider direction uses a team-size-
weighted average while survivor direction takes the maximum
-------------------------------------------------------------------------------

ROOT
----
Change advc.130k introduced CvTeam::m_aiTurnsAtPeace as a deterministic,
team-level replacement for the randomized CvTeamAI at-peace counter specifically
because the latter was incorrectly being used for the sustained-peace trade-
route modifier. Outside Permanent Alliances, this state is maintained as a
bilateral exact counter: war declaration resets both directions to 0 and, while
both teams remain alive and at peace, each side increments its direction once
per turn.

Permanent Alliance merging handles the two directions differently.

For an outsider kOther looking toward the new combined team, CvTeam::addTeam
explicitly computes a team-size-weighted average:

    kOther.setTurnsAtPeace(getID(),
        (iOriginalTeamSize * kOther.getTurnsAtPeace(getID()) +
         iSecondTeamSize * kOther.getTurnsAtPeace(eTeam)) /
        getNumMembers());

But for the new combined team looking toward that same outsider,
CvTeam::shareCounters uses the older generic stronger-history/max pattern:

    if (kShareTeam.getTurnsAtPeace(eLoopTeam) > getTurnsAtPeace(eLoopTeam))
        setTurnsAtPeace(eLoopTeam, kShareTeam.getTurnsAtPeace(eLoopTeam));

That breaks the bilateral counter invariant immediately after a merge.

CONCRETE REPRODUCTION
---------------------
For equal-size constituent teams A and B and outsider C:

    A -> C TurnsAtPeace = 100
    B -> C TurnsAtPeace =   5

Before the alliance the reciprocal values are normally the same in each pair.
After A absorbs B:

    C -> (A+B) = (100 + 5) / 2 = 52   [reverse weighted merge]
    (A+B) -> C = max(100, 5) = 100    [shareCounters]

The same post-alliance bilateral relation therefore has two different values
for what advc.130k introduced as an exact turns-at-peace counter.

GAMEPLAY CONSEQUENCE
--------------------
CvCity::getPeaceTradeModifier reads the city owner's directional
getTurnsAtPeace(otherTeam) value when calculating the sustained-peace foreign-
trade modifier. The asymmetric PA merge can therefore give cities on opposite
sides of the same relationship different peace-history credit solely because
one direction was max-merged and the other weighted. WarUtilityAspect also
uses this counter as real inter-team peace-history state.

ANCESTRY / INTENT CONTROL
-------------------------
AdvCiv commit dc322128c7de2fce6befbc891d577c271340e95e, "Keep exact count of
turns at peace", introduced m_aiTurnsAtPeace because the randomized AI counter
must not drive the sustained-peace trade-route modifier. The new field was then
inserted into two pre-existing PA merge patterns with different policies:
shareCounters' own-side max pattern and addTeam's reverse team-size-weighted
pattern. No explicit rationale was found for making this exact bilateral state
asymmetric.

REPAIR DIRECTION
----------------
Merge both directions with the same explicit policy. The least invasive repair
is to use the same team-size-weighted average for the survivor's own direction
that addTeam already uses for the outsider direction, calculated before member
reassignment while both constituent team sizes are still known. An alternative
policy could be chosen deliberately, but it must be symmetric; the current
max-vs-weighted split cannot preserve the field's bilateral meaning.

Classification: inherited AdvCiv Permanent-Alliance history/state merge bug.


POST-WIP13 CONTROLS / NORMALIZATION
-----------------------------------
- Advanced Start / map-centering sibling strengthened: current TECH_PAPER is
  Advanced-Start refundable and has bMapCentering=1. processTech applies map
  centering only for iChange>0 and no normal false transition clears it.
  Buying Paper, enabling map centering, then refunding Paper retains the effect.
  This is normalized as another manifestation of the already-recorded Advanced
  Start irreversible/refundable-state family (alongside F084/F085), not split
  into a new root.
- PermanentWarPeace reverse-flag transfer was challenged. addTeam transfers the
  survivor-side flag but does not rewrite every outsider's stale absorbed-team
  slot. Normal behavior checks both orientations (our flag || their flag), and
  the transferred survivor-side flag preserves the restriction; no normal
  gameplay failure was source-proved. Rejected as F098.
- Permanent-Alliance ProjectMaking is summed while constituent cities may both
  be producing the same team-limited project. Normal production validation
  subsequently drops conflicting continuing queues and decrements ProjectMaking
  back into range. The choice of which queue survives can depend on city order,
  but invested production remains stored and no sufficiently irreversible
  failure was established. Retained as a quality-risk note only.
- m_aiProjectDefaultArtTypes / m_aaiProjectArtTypes provenance loss across PA
  remains an unresolved cosmetic-risk note because -1 is a supported default
  state and final art selection crosses the closed EXE contract.
- PA m_aiExtraMoves max merge remains rejected as a bug under current sources:
  shareItems first equalizes all tech-derived DomainExtraMoves; max then safely
  preserves the only constituent-specific source such as circumnavigation.
- setListHelp refactor control remains clean: practical 2882 intentionally made
  setListHelp(bool&) mutate bFirst, so getName/getReplayName need no manual
  bFirst=false assignment.
- Raw divisor, list-head and pointer closure remained quiet in this tranche;
  F091 continues to be the only source-proved positional trade-list misuse.
- canSeeTech and similar exposed helpers are less defensive than their comments
  imply, but all live source callers found pass valid team/player arguments; no
  forged Python/API sentinel case was promoted.

ADVERSARIAL STATUS
------------------
The reread has not retracted F078-F097 so far. In particular:
- F078/F081 remain reachable because PA trading has no Counterespionage or
  spaceship-countdown prohibition.
- F080's player-level Disengage deal survives team reassignment while the team-
  level enforcement flag does not transfer.
- F082/F083 remain direct arithmetic/signed-transition defects.
- F084/F085/F088 remain live through the ordinary Advanced Start removal path.
- F094 revival does not replay known-team-tech player modifiers.
- F095 still admits players whose direct team differs from the excluded attacker
  but whose master team is that attacker.
- F096 still counts dead roster members through getNumMembers in Tech Share.
- F097 remains the direct Domestic Advisor getX -> getY typo.

CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F098
NEXT ROOT ID: F099 UNUSED

Next work:
1. Finish adversarial reread of F078-F098 against current XML/callers and later
   refactors; retract or merge anything that no longer survives.
2. Complete final persistent-field/source-order/caller checklist, including the
   remaining team history/victory/espionage/relation state families.
3. Reconcile any new candidate against base AdvCiv/Civ4CE and ancestry before
   promotion.
4. Only if the entire closure remains coherent, decide whether CvTeam.cpp earns
   durable C014; otherwise freeze another WIP first.

No repository source was modified.
===============================================================================


===============================================================================
C013-WIP15 -- CvTeam.cpp audit continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues directly from C013-WIP14. It physically anchors the
stolen-visibility Permanent-Alliance lifecycle defect found during the final
espionage/persistent-field closure and preserves the current adversarial state
before the audit continues from F100.

F099 / provisional KI#421 -- Permanent Alliance leaves live teams' stolen-
visibility relation to the absorbed team active after that target dies, so the
old target's plot-level stolen visibility can become permanent
-------------------------------------------------------------------------------

ROOT
----
CvTeam::setStolenVisibilityTimer is not merely timer bookkeeping. Whenever a
team-target relation crosses zero, it adds or removes one
CvPlot::StolenVisibilityCount for the stealing team on every plot currently
visible to the target team:

    if (bOldStolenVisibility != isStolenVisibility(eIndex))
        for every plot visible to eIndex
            changeStolenVisibilityCount(getID(), active ? +1 : -1);

Permanent Alliance merging transfers the absorbed team's ordinary visibility
into the surviving team and merges stolen-visibility timers, but it never
performs the corresponding old-target -> 0 transition for live teams that had
stolen visibility against the absorbed team.

CONCRETE OUTSIDER REPRODUCTION
------------------------------
Let A absorb B while outsider C has an active stolen-visibility timer against B.
Before the alliance, C's C->B timer has already added +1 stolen-visibility count
to every plot B can currently see.

During CvTeam::addTeam:
1. B's ordinary visibility is copied into A.
2. The reverse outsider merge computes/sets a weighted C->A stolen-visibility
   timer from C->A and C->B.
3. C->B itself is never cleared through setStolenVisibilityTimer(B, 0).
4. B's players are now members of A and B is no longer an alive team.

CvTeam::doTurn decrements stolen-visibility timers only over
TeamIter<CIV_ALIVE,NOT_SAME_TEAM_AS>. C therefore never visits dead B again and
its old positive C->B timer freezes indefinitely.

When the new C->A timer later expires, setStolenVisibilityTimer(A,0) subtracts
only the plot-level +1 belonging to the new A relation. The old +1 originally
created by C->B remains on B's pre-alliance visible plots, leaving C with
permanent visibility there.

A weighted merged timer that rounds to zero makes the defect even simpler: no
new C->A relation need be created at all, yet the old C->B plot counts remain.

SURVIVOR-INTERNAL SIBLING
-------------------------
The cleanup also has to include survivor A's own A->B stolen-visibility
relation. After B's real sight has been copied into A, A no longer needs an
espionage relation against its new teammate. Leaving A->B active gives A an
extra plot-level stolen-visibility +1 tied to a dead target. As legitimate
merged sight later changes, that stale +1 can continue to reveal former-B plots.

WHY LATER RESET DOES NOT REPAIR IT
----------------------------------
CvTeam::reset does not call setStolenVisibilityTimer when clearing another
team's target slot. It directly executes:

    kLoopTeam.m_aiStolenVisibilityTimer.resetVal(getID());

Thus a later reset can erase the frozen timer value without invoking the
zero-transition that removes plot-level stolen-visibility counts. The leaked
plot state can therefore become permanently detached from its timer bookkeeping.

ANCESTRY / CONTROL
------------------
The underlying Permanent-Alliance stolen-timer merge exists in Civ4CE/BtS and
base AdvCiv as well. K-Mod/Kek-Mod later added a specifically documented fix for
"permanent espionage visibility" during Permanent Alliances, but that repair
only clears CvCity espionage visibility for the absorbed team through
CvCity::setEspionageVisibility. In the same merge machinery the stolen map-
visibility timers continue to be averaged/transferred without clearing the old
absorbed-target relation. No later current-source cleanup path was found.

REPAIR DIRECTION
----------------
The order matters because the old relation's plot counts must be removed without
throwing away legitimate merged sight:

1. Copy B's real visibility into survivor A as today.
2. Compute/set each live team's intended post-merge stolen-visibility timer
   against A, using the chosen PA merge policy while the old B timer is still
   available as input.
3. After that transfer, clear every live non-B team's stolen-visibility timer
   against B through setStolenVisibilityTimer(B, 0), not direct array reset.
   This includes survivor A itself and all outsiders such as C.

Calling the setter is essential: it subtracts the old B-derived plot-level
stolen counts. If the live team retains a merged A relation, A's legitimate
visibility/timer contribution remains independently represented.

Classification: inherited BtS/K-Mod Permanent-Alliance espionage-visibility
lifecycle bug.


POST-WIP14 CONTROLS / NORMALIZATION
-----------------------------------
- Repeat-tech Permanent-Alliance sharing was challenged because shareItems uses
  isHasTech while repeat technologies store counts separately and deliberately
  leave isHasTech false. Current SAS contains no bRepeat=1 technologies, so no
  live reproduction exists; retained as generic/modmod robustness only.
- The old Kek-Mod Permanent-Alliance "permanent espionage visibility" fix was
  traced and confirmed to address city espionage visibility only; it does not
  clear StolenVisibilityTimer relations or CvPlot stolen-visibility counts.
- CvPlayer::setTeam changes team aggregate membership/city/population/land state
  but performs no stolen-visibility cleanup.
- CvTeam::changeAliveCount(0) ends wars and related war-plan state but performs
  no stolen-visibility cleanup.
- CvTeam::doTurn's stolen-visibility decrement domain is explicitly alive target
  teams, proving that a surviving team's positive timer against absorbed/dead B
  does not naturally expire.

CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F099
NEXT ROOT ID: F100 UNUSED

Next work:
1. Continue the final persistent-field/source-order/caller closure from F100,
   with special attention to remaining espionage/victory/relation lifecycle
   state and any direct array resets that bypass stateful setters.
2. Finish adversarial reread of F078-F099 against current XML, current callers
   and later refactors; retract or merge anything that no longer survives.
3. Reconcile any new candidate against base AdvCiv/Civ4CE and ancestry before
   promotion.
4. Only after the closure remains coherent decide whether CvTeam.cpp earns C014;
   otherwise freeze another WIP first.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP16 -- FILE 013 CvTeam.cpp -- F100 PROJECT-INHERITANCE ANNOUNCEMENT ROOT
===============================================================================

This checkpoint continues directly from C013-WIP15. It physically anchors the
false project-completion/replay event emitted when a Permanent Alliance inherits
projects from the absorbed team, and preserves the current closure state before
continuing from F101.

F100 / provisional KI#422 -- Permanent-Alliance project inheritance reuses the
ordinary positive project-completion path, generating false duplicate project
completion announcements and replay events
-------------------------------------------------------------------------------

ROOT
----
CvTeam::shareCounters merges completed Project counts by raising the survivor to
the maximum constituent count. When the absorbed team has more copies of a
project, it calls the survivor's ordinary positive transition:

    int iDelta = kShareTeam.getProjectCount(eProject) - getProjectCount(eProject);
    if (iDelta > 0)
    {
        changeProjectCount(eProject, iDelta);
        GC.getGame().incrementProjectCreatedCount(eProject, -iDelta);
    }

The later incrementProjectCreatedCount compensation repairs the global numerical
"projects created" count. It cannot undo the other positive-completion side
effects that changeProjectCount has already emitted.

FALSE EVENT PATH
----------------
CvTeam::changeProjectCount(...,+N) enters the normal "a project was just
completed by this team" branch whenever the game is final-initialized and not in
WorldBuilder. It builds TXT_KEY_MISC_COMPLETES_PROJECT[_THE], permanently adds a
REPLAY_MESSAGE_MAJOR_EVENT attributed to the survivor's leader/team name, and
sends every living major player the corresponding
TXT_KEY_MISC_SOMEONE_HAS_COMPLETED[_THE] notification with the project-completed
sound.

Permanent Alliances occur during an ordinary final-initialized game, so this
branch is live during shareCounters.

CONCRETE CURRENT-SAS REPRODUCTION
---------------------------------
Current SAS has ordinary pre-alliance projects such as PROJECT_APOLLO_PROGRAM,
PROJECT_SDI, PROJECT_THE_INTERNET, PROJECT_MANHATTAN_PROJECT and spaceship
components. There is no Permanent-Alliance rule requiring the two teams to own
identical project sets.

Let survivor A lack Apollo while absorbed team B already owns Apollo. During
A.addTeam(B):
1. shareCounters runs while B is still a separate team and its players have not
   yet been reassigned to A.
2. B.ProjectCount(Apollo)=1 and A.ProjectCount(Apollo)=0, so shareCounters calls
   A.changeProjectCount(Apollo,+1).
3. changeProjectCount emits a fresh replay event/message saying team A completes
   Apollo, even though B completed that Apollo earlier and no new project was
   built at the alliance turn.
4. shareCounters then decrements the global ProjectCreatedCount by one, restoring
   only that numerical counter.
5. B's players are reassigned to A later in addTeam. The already-created replay
   record and notifications remain permanently false/duplicated.

The same mechanism applies to any project where the absorbed team has a higher
count than the survivor. Multi-copy spaceship projects can emit the completion
message once per positive merge call as well, regardless of how many copies were
actually inherited in iDelta.

ORDERING CONTROL
----------------
CvTeam::addTeam calls shareCounters(eTeam) before looping through the absorbed
team's players and calling CvPlayer::setTeam(getID()). Therefore getReplayName,
getName and getLeaderID inside the survivor's changeProjectCount describe the
surviving team at the moment the false completion event is created; they do not
somehow report the absorbed team as the historical builder.

ANCESTRY / CONTROL
------------------
The same underlying pattern is present in Civ4CE/BtS: shareCounters calls
changeProjectCount for inherited positive project deltas and only afterward
compensates ProjectCreatedCount; BtS changeProjectCount also emits replay and
major-event project-completion notifications for every positive change. Base
AdvCiv retains the pattern. This is therefore an inherited BtS Permanent-
Alliance transition bug, not an SAS regression.

REPAIR DIRECTION
----------------
Project inheritance needs the state/effect processing of a positive project
count change but must not masquerade as a newly completed project. A conservative
repair is to give changeProjectCount (or a dedicated PA-transfer helper) an
explicit "announce completion" / transfer-mode control and suppress replay/UI
completion events during shareCounters inheritance while retaining legitimate
state updates such as project count, project effects, Tech Share count, victory
re-evaluation and AI production invalidation. The existing global-created-count
compensation can remain or be folded into the same transfer-aware path.

Classification: inherited BtS Permanent-Alliance project-transfer reporting bug.


POST-WIP15 CONTROLS / NORMALIZATION
-----------------------------------
- Current Project XML was checked for a concrete live reproduction: Apollo, SDI,
  The Internet, Manhattan and spaceship components provide ordinary projects
  whose counts can differ between two teams before a Permanent Alliance.
- addTeam ordering was rechecked: shareCounters runs before absorbed players are
  reassigned, so the completion event is deterministically attributed to the
  survivor rather than being neutralized by the later team merge.
- Civ4CE/BtS changeProjectCount contains the same positive-project replay/message
  branch, while its shareCounters likewise compensates only the numerical global
  project-created count after inheritance. F100 is therefore not caused by an
  SAS-only announcement customization.
- The earlier project-count-limit concern remains rejected: current PA project
  counts are max-merged, not summed; the old sum implementation is commented out.

CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F100
NEXT ROOT ID: F101 UNUSED

Next work:
1. Continue the final stateful-setter/persistent-field/source-order/caller closure
   from F101, especially any transition helper whose ordinary setter owns
   secondary state but PA/death/reset bypasses it.
2. Finish adversarial reread of F078-F100 against current XML, current callers,
   and later AdvCiv/K-Mod refactors; retract or merge anything that fails.
3. Complete method/field/index/divisor/list assumptions and source-order coverage.
4. Only after those closure controls remain coherent decide whether CvTeam.cpp
   earns durable C014; otherwise freeze another WIP first.

No repository source was modified.
===============================================================================

===============================================================================
C013-WIP17 -- FILE 013 CvTeam.cpp -- F101 AUTOLOG NO_PLAYER TECH-EVENT ROOT
===============================================================================

This checkpoint continues directly from C013-WIP16. It physically anchors the
optional BUG Autolog integration defect exposed by Permanent-Alliance technology
sharing and preserves the closure state before continuing from F102.

F101 / provisional KI#423 -- Permanent-Alliance technology sharing fires the
legitimate techAcquired player sentinel NO_PLAYER, but BUG Autolog dereferences
that sentinel as a real player and raises a Python handler exception
-------------------------------------------------------------------------------

ROOT
----
CvTeam::shareItems shares technologies between the two constituent teams before
players are reassigned. For a technology known by one constituent but not the
other, it deliberately calls setHasTech with no unique discovering player:

    setHasTech(eTech, true, NO_PLAYER, true, false, false,
               TECH_ACQUISITION_TEAM_MERGE);

CvTeam::setHasTech then sends the normal Python techAcquired event. SAS adds
TechAcquisitionCause only to its GameRecord logging; CvEventReporter explicitly
keeps the original Python signature and forwards ePlayer unchanged.

The base CvEventManager handler documents the contract directly:

    # Note that iPlayer may be NULL (-1) and not a refer to a player object

and guards player-only behavior with iPlayer > -1.

BUG Autolog's parallel handler does not preserve that contract. With technology
logging enabled it reaches, in its player-specific traded-tech branch:

    zsCiv = gc.getPlayer(iPlayer).getName() + ...

without first excluding iPlayer == -1.

SENTINEL FAILURE
----------------
CyGlobalContext::getCyPlayer explicitly returns NULL when idx == NO_PLAYER.
Therefore gc.getPlayer(-1) is exposed to Python as None. Calling getName or
getCivilizationShortDescription on it raises a Python exception.

BugEventManager catches exceptions from individual event handlers and traces the
failure, so this is not asserted to crash the DLL/game process. The Autolog
techAcquired handler nevertheless fails, the intended log entry is lost, and a
Python error/trace is produced.

NORMAL PERMANENT-ALLIANCE REPRODUCTION
--------------------------------------
1. Enable BUG Autolog and technology logging (LogTech defaults true once logging
   itself is active).
2. During the active human's normal input phase, accept/form a Permanent Alliance
   with another team that knows at least one technology the survivor does not.
3. CvDeal processes TRADE_PERMANENT_ALLIANCE immediately and calls CvTeam::addTeam.
4. addTeam -> shareItems transfers the differing tech with ePlayer=NO_PLAYER and
   fires techAcquired before the absorbed players are reassigned.
5. Autolog receives iPlayer=-1.
6. When its bHumanPlaying branch is active, it tries gc.getPlayer(-1).getName()
   and the handler raises.

The bHumanPlaying reachability is not guessed from its name. Autolog sets it true
on EndGameTurn and the sequential-turn player lifecycle does not enter
CvPlayer::doTurn / the handler's BeginPlayerTurn transition until the player ends
its active input turn. Human diplomacy and CvDeal acceptance occur during that
input window, so a human-formed PA can hit the branch normally.

ANCESTRY / CONTROL
------------------
Base AdvCiv 1.14 retains both sides of the mismatch: PA shareItems calls
setHasTech(...,NO_PLAYER,...) and autologEventManager.onTechAcquired performs the
unguarded gc.getPlayer(iPlayer) access. The ordinary CvEventManager in the same
base tree already guards the documented -1 contract, providing a direct correct
control. This is therefore an inherited BUG/AdvCiv optional-Autolog integration
bug, not an SAS regression.

REPAIR DIRECTION
----------------
Handle NO_PLAYER explicitly in autologEventManager.onTechAcquired before any
player lookup. The smallest safe repair is to skip the player-specific traded-
tech formatting when iPlayer < 0. A richer repair can log a team/alliance
acquisition using iTeam instead; it must not synthesize an arbitrary player
because team merges deliberately have no unique discovering/trading player.

Classification: inherited BUG/AdvCiv optional Autolog sentinel-contract bug,
exposed by CvTeam Permanent-Alliance tech sharing.


POST-WIP16 CONTROLS / NORMALIZATION
-----------------------------------
- CvDllPythonEvents was traced to confirm that the techAcquired Python signature
  remains (tech, team, player, announce); SAS's new acquisition-cause enum is
  intentionally GameRecord-only and does not sanitize the player sentinel.
- CyGlobalContext::getCyPlayer was traced rather than assumed: NO_PLAYER returns
  NULL, establishing the Python None dereference concretely.
- BugEventManager's event dispatch was traced: handler exceptions are caught and
  logged, so F101 is described as a Python handler failure/error, not a guaranteed
  whole-game crash.
- Autolog.xml was checked: Autolog itself is optional/disabled by default, while
  LogTech defaults true conditional on logging being active. F101 is therefore a
  live optional-feature defect, not a default-every-game failure.
- The earlier first-contact half-initialized state was checked against current
  Python firstContact handlers; no additional live handler failure beyond F092's
  Tech Share timing was established.

CURRENT CURSOR
--------------

COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
NORMALIZED NEW ROOTS IN THIS FILE: F078-F101
NEXT ROOT ID: F102 UNUSED

Next work:
1. Continue final persistent-field/stateful-setter/source-order closure from F102.
2. Finish adversarial reread of F078-F101, including optional-feature/current-XML
   reachability and later-refactor controls.
3. Complete remaining method/index/divisor/list/caller assumptions.
4. Decide C014 only after the entire closure remains coherent; otherwise freeze
   another WIP first.

No repository source was modified.
===============================================================================

================================================================================
C013-WIP18 -- FILE 013 CvTeam.cpp -- ADVERSARIAL CORRECTION OF FORMER F101
================================================================================

PURPOSE
-------
Freeze the first correction produced by the final adversarial reread. WIP17
promoted an Autolog NO_PLAYER hypothesis as F101 / provisional KI#423. That
hypothesis is false because it stopped tracing the actor parameter one step too
early. The explicit NO_PLAYER supplied by Permanent-Alliance shareItems is
normalized inside CvTeam::setHasTech before the Python event is fired.

FORMER F101 / provisional KI#423 -- RETRACTED FALSE POSITIVE
------------------------------------------------------------
Incorrect WIP17 claim:
- Permanent-Alliance technology sharing calls setHasTech with ePlayer=NO_PLAYER.
- BUG Autolog's techAcquired handler does not guard iPlayer == -1.
- Therefore PA tech sharing was claimed to pass -1 to Autolog and trigger a
  Python None.getName()/None.getCivilizationShortDescription error.

Adversarial correction:
- CvTeam::shareItems really does call setHasTech(..., NO_PLAYER, ...).
- However CvTeam::setHasTech immediately performs:

      if (ePlayer == NO_PLAYER)
          ePlayer = getLeaderID();

  before either repeat-tech or ordinary-tech EventReporter calls.
- The later call is therefore:

      CvEventReporter::getInstance().techAcquired(
          eTech, getID(), ePlayer, ...);

  where ePlayer is already the valid team leader ID.
- CvEventReporter preserves that now-valid value when forwarding the ordinary
  Python techAcquired event.
- The unguarded BUG Autolog handler is thus not reached with -1 through this PA
  path. Its missing NO_PLAYER guard may still be weak defensive code, but this
  audit has no normal producer that sends NO_PLAYER through CvTeam::setHasTech.
- Base AdvCiv 1.14 has the same early NO_PLAYER -> getLeaderID normalization, so
  this is not an SAS regression hidden by later changes.

Conclusion:
- RETRACT former F101 / provisional KI#423 completely.
- Do not implement/document KI#423 as a PA Autolog bug.
- Reopen F101 as the next unused root ID, matching the audit convention used
  after the earlier former F066/KI#387 false positive.

WHY THE FALSE POSITIVE WAS USEFUL TO RECORD
-------------------------------------------
The raw shareItems call and Python handler look convincing in isolation. The
mistake was failing to carry the argument through the first lines of
CvTeam::setHasTech. Recording this correction prevents the same cross-file
NO_PLAYER hypothesis from being rediscovered later and strengthens the closure
method: producer -> immediate normalizer -> reporter -> Python consumer must all
be traced before promoting an integration root.

CURRENT NORMALIZED STATE
------------------------
- F078-F100 remain live normalized CvTeam.cpp findings.
- Former F101 / provisional KI#423 is RETRACTED FALSE POSITIVE.
- NEXT ROOT ID: F101 UNUSED.
- CvTeam.cpp remains OPEN; C014 has not been earned yet.
- Continue final stateful-setter/persistent-field/source-order/caller closure
  plus adversarial reread of F078-F100 from F101 unused.
- No repository source files modified.

LOCAL RECONCILIATION FOR THE NEXT-THREAD HANDOFF
------------------------------------------------
- This current repository album is authoritative over the downloaded C013-WIP18
  artifact.
- Preserve the corrected `WIDGET_MENU_ICON -> CONSCRIPT` spelling; the downloaded
  artifact's `CONCRIPT` is only a typo.
- Direct enumeration of current `CvEnums.h` confirms 193 WidgetTypes: 163 shared
  with the Civ4CE/BtS control plus exactly 30 AdvCiv/SAS additions. Preserve 193,
  not the downloaded artifact's incorrect 194.
- Live provisional roots remain F078-F100. Former F101 / provisional KI#423 is
  retracted and must not be implemented; F101 is the next unused root ID.
- CvTeam.cpp remains OPEN. Continue its final closure and adversarial reread before
  deciding whether the file earns durable C014.

Reconciled with the help of GPT-5.6-Sol, thanks.


===============================================================================
C013-WIP19 -- CvTeam.cpp final-closure continuation
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues from the reconciled repository WIP18 state. It does not
force C014. The purpose of this tranche is to preserve the post-WIP18 adversarial
corrections/strengthenings and the mechanical raw-risk/persistent-field closure
performed against the 2026-08-25 14:12 light-source snapshot before continuing
from F101 unused.

AUTHORITATIVE ROOT STATE
------------------------
- F078-F100 remain the live normalized CvTeam.cpp roots.
- Former F101 / provisional KI#423 (PA setHasTech(NO_PLAYER) -> BUG Autolog
  exception) remains RETRACTED. setHasTech normalizes NO_PLAYER to getLeaderID()
  before EventReporter dispatch.
- F101 is therefore UNUSED.
- No repository source files were modified.

F098 / KI#420 STRENGTHENING -- EXACT HasMetCounter IS A SECOND MANIFESTATION
----------------------------------------------------------------------------
WIP14 proved that Permanent Alliance merges the deterministic CvTeam
m_aiTurnsAtPeace relation asymmetrically: survivor->outsider takes max while
outsider->survivor takes a team-size-weighted average.

The final counter audit shows that CvTeamAI::m_aiHasMetCounter belongs to the same
root.

CvTeamAI::AI_doCounter explicitly distinguishes exact from randomized counters:

    /* No randomization for atWar and hasMet. These are used by the AI in
       several places that more or less assume an exact count. */

Both directions of a normal first-contact relation therefore start together and
advance once per turn. Yet PA uses the same max-versus-weighted split:

    shareCounters: survivor->C = max(A->C, B->C)
    addTeam:        C->survivor = weighted(C->A, C->B)

For equal-size A/B with 100 and 5 contact turns, the merged relation becomes
100 in one direction and about 52 in the other. This breaks the exact bilateral
contact-history invariant just as the TurnsAtPeace split does.

Internal controls strengthen the classification:
- AI_getAtPeaceCounter is deliberately randomized and may legitimately diverge,
  so it does NOT join F098.
- OpenBordersCounter, DefensivePactCounter and ShareWarCounter are likewise
  randomized/decaying state in advc.130k and do not carry an exact bilateral
  invariant.
- AI_getAtWarCounter is exact, but PA explicitly reconciles the survivor's value
  to the already-computed reverse weighted result; that neighboring code is a
  direct control showing how exact bilateral state can be kept symmetric.

Normalize F098/KI#420 as: Permanent Alliance asymmetrically merges exact bilateral
peace/contact history. Proven manifestations are m_aiTurnsAtPeace and
AI_getHasMetCounter.

F099 / KI#421 PRECISION CONTROL -- ORDINARY ELIMINATION DOES NOT REPRODUCE PA LEAK
---------------------------------------------------------------------------------
The stolen-visibility lifecycle root remains PA-specific rather than a generic
"target becomes dead" defect.

During ordinary elimination, target-team units/cities lose legitimate sight and
CvPlot::changeVisibilityCount continues to visit alive observing teams. If an
observer still has isStolenVisibility(target), those sight-loss transitions
subtract the observer's plot-level stolen-visibility counts. Thus the secondary
plot state naturally drains as the defeated target loses visibility.

Permanent Alliance is different: addTeam copies absorbed B's legitimate sight to
survivor A and then kills team identity B by member reassignment, without driving
B through the ordinary sight-loss transition and without clearing observer->B
through setStolenVisibilityTimer(B,0). B's old plot-level stolen counts can
therefore remain after the new observer->A timer later expires.

This negative control narrows rather than weakens F099.

F100 / KI#422 RECHECK
---------------------
The project-inheritance replay/message root remains direct and current-source
provable. changeProjectCount(+delta) still runs its normal positive completion
side effects before addTeam compensates only ProjectCreatedCount. The project-art
vector is NOT a memory-safety sibling: changeProjectCount pushes one -1 art slot
per inherited project and asserts vector-size/project-count balance. Thus the
source-proven root remains false duplicate completion/message/replay state, not an
out-of-bounds art-vector defect.

RAW DIVISOR / INDEX / LIST / POINTER CLOSURE
---------------------------------------------
A fresh mechanical sweep of CvTeam.cpp did not produce F101.

Divisors:
- current-era averaging explicitly returns NO_ERA when the member divisor is <=0;
- culture-ratio and espionage formulas check/clamp denominators;
- Barbarian research divides only after at least one qualifying/alive team exists;
- PA reverse weighting divides by the merged live team's member count;
- remaining divisions are constants or structurally nonzero current domains.
F082 remains the distinct research-progress arithmetic defect; no second divisor
root emerged.

Raw vector/index/list-head sites:
- addTeam apOther and member vectors are accessed only under size-bounded loops;
- reparations string vector is likewise size-bounded even if all strings filter
  out (the nonempty assertion is semantic, not required for safe indexing);
- turnsOfForcedPeaceRemaining checks getLengthFirst()>0 before head(), leaving
  F091 as a positional/semantic first-item bug rather than a null-head bug;
- allowDefensivePactsToBeCanceled has the same nonempty-list guard before head();
- all six queued-war queues are pushed together exclusively by queueWar and popped
  together exclusively by triggerWars, so testing attacking_queue.empty() is
  sufficient under the current encapsulated call graph;
- project-art getters/setters are count-indexed; changeProjectCount keeps the
  vector size synchronized for normal internal callers.

Random-member selector:
- getRandomMemberAlive calls SyncRandNum(iValid) without a local iValid==0 guard.
  All current C++ callers are UWAI paths whose agent/target iterators require live
  teams. PlayerIter<HUMAN,MEMBER_OF> itself also implies alive status. No normal
  zero-domain caller was established; retain the fallback FError as defensive
  rather than promote an API-forged case.

Leader-capital helper:
- getLeaderCapital dereferences getLeaderID() before its fallback member scan, but
  current source callers are live-team circumnavigation/trade-AI paths. No dead /
  NO_PLAYER caller was established.

Serialization:
- current CvTeam read/write field order remains paired. Apparent extras from a
  crude scan are the commented-out m_abVassal write and the manually symmetric
  project-art loop.
- old uiFlag migration branches are intentionally removed under KI#46 / current
  same-version-save-only policy; unsupported old-save compatibility is not a new
  CvTeam root.

PERSISTENT-FIELD / PA PROVENANCE CONTROLS
-----------------------------------------
The remaining serialized/transient fields were challenged for state that is
neither derived/recomputed nor explicitly migrated.

m_abJustDeclaredWar:
- PA does not explicitly inherit the absorbed team's flag.
- Its live consumers are AdvCiv's same-turn invasion move-spending option and the
  DoW-specific capturable-unit chance.
- Current SAS has SPEND_ALL_MOVES_ON_INVASION=0 and both
  BASE_UNIT_CAPTURE_CHANCE=50 and DOW_UNIT_CAPTURE_CHANCE=50.
- Therefore changing the flag has no current behavioral consequence. Keep this as
  config/modmod robustness only, not F101.

m_bMapCentering:
- The flag is one-way and PA does not explicitly max/copy it.
- In normal current SAS, Permanent Alliance trading is enabled by TECH_GAME_THEORY.
  Game Theory requires Corporation, whose current prerequisite chain runs through
  Paper; TECH_PAPER itself has bMapCentering=1.
- No current Python/gameplay path independently grants PA trading. Therefore every
  normally PA-eligible constituent already has the Paper map-centering state.
- A raw CyTeam API caller could manufacture a different state, but no normal
  current-SAS reproduction exists. Do not promote.

Repeat technologies / team-limited UnitClass/BuildingClass:
- Current SAS has no repeatable technologies, so generic repeat-count PA weakness
  remains dormant.
- Current SAS has no positive UnitClass/BuildingClass MaxTeamInstances; generic
  summed-count risk remains dormant. Projects are the live team-limited family
  and receive their dedicated max merge.

Vassal/master power and capitulation state:
- These values describe a vassal agreement at signing and PA trading excludes
  vassal teams. They therefore do not create a normal PA migration requirement.

Project default/art provenance:
- inherited counts create valid -1 project-art slots and -1 is a supported
  undefined/default art state. Loss of the absorbed team's cosmetic/default art
  choice remains a cosmetic/closed-EXE risk note only; no source-level gameplay
  failure has been established.

ITERATOR-PREDICATE CLOSURE
--------------------------
A mechanical sweep for alive/member iterator predicates versus contradictory
inner branches found no second practical-1837-style root beyond the already
normalized F093/F094 family.
- doWarWeariness still contains the known impossible !target.isAlive() branch
  under TeamIter<CIV_ALIVE> (F093).
- MemberIter's alive-only semantics remain the mechanism in F094's tech-player
  processing/revival defect.
- Other nearby alive checks are function/caller guards or apply to different
  objects, not impossible branches.

ADVERSARIAL STATUS
------------------
No live root was retracted in this tranche. The final reread also rediscovered
F082's suspicious "subtract overflow" line mechanically and correctly normalized
it back into existing F082 rather than double-counting it; this is a useful
control that the current audit ledger is preventing duplicate roots.

The exact-counter reread strengthens F098. F099's ordinary-death negative control
strengthens its PA-specific lifecycle classification. F100 survives its art-vector
challenge. Former F101/KI#423 remains retracted.

CURRENT CURSOR
--------------
COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
PHYSICAL CHECKPOINT: C013-WIP19
LIVE NORMALIZED ROOTS IN THIS FILE: F078-F100
RETRACTED: former F101 / KI#423
NEXT ROOT ID: F101 UNUSED

Remaining work before C014 can be earned:
1. Finish the method/source-order and remaining scalar/caller closure, with special
   attention to any stateful setter whose side effects could still be bypassed.
2. Finish the explicit adversarial F078-F100 reread against current callers/XML
   and any later SAS fixes, retracting or merging if needed.
3. Reconcile any surviving new candidate against base AdvCiv 1.14, Civ4CE/BtS and
   local history before promotion.
4. Declare C014 only if those passes are genuinely exhausted; another WIP is
   preferred to premature closure.

No repository source files were modified.
===============================================================================

===============================================================================
C013-WIP20 -- CvTeam.cpp adversarial-ledger / final-tail checkpoint
Date: 2026-08-25
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvTeam.cpp remains OPEN
===============================================================================

This checkpoint continues directly from the reconciled C013-WIP19 artifact and
records the explicit F078-F100 adversarial reread plus the remaining list-head /
source-order controls reached before a final C014 decision. It deliberately does
not invent F101 merely to increase the finding count.

AUTHORITATIVE ROOT STATE
------------------------
- F078-F100 remain live after the explicit adversarial reread.
- Former F101 / KI#423 remains RETRACTED; setHasTech normalizes NO_PLAYER to the
  team leader before EventReporter dispatch.
- F101 remains UNUSED.
- No repository source files were modified.

ADVERSARIAL F078-F100 RESULT
----------------------------
Every currently live root was re-challenged against the 2026-08-25 14:12 SAS
source, current XML/options where relevant, and the already-supplied AdvCiv 1.14
/ Civ4CE controls. No additional retraction survived.

F078 / KI#400 -- SURVIVES
Counterespionage turns+modifier remain keyed by TeamTypes and are not migrated in
either PA direction. Former-B players immediately use survivor A identity, so a
live B->C effect disappears; outsider C->B is likewise stranded. Current XML has
a live 10-turn/+100 Counterespionage mission, so this is not dormant capability.

F079 / KI#401 -- SURVIVES
m_aiHasMetTurn is genuine historical state consumed by the Info Screen graph /
partial-score-graph gates. The objection "A itself only met C at the alliance"
does not close the root: B's players become members of A, while the merged team
has only one team-level first-contact timestamp. Resetting that timestamp to the
alliance turn discards history possessed by members of the merged team. Both
A->C and C->A need the inherited earliest real date.

F080 / KI#402 -- SURVIVES
A live B<->C Disengage deal follows its players and therefore becomes A<->C after
B's player is reassigned, while the enforcement flag is TeamTypes-keyed and is
not migrated. The deal continuing while its team flag vanishes remains a direct
state/deal inconsistency.

F081 / KI#403 -- SURVIVES
Victory countdown/canLaunch is independent live team state. PA still has no
post-launch prohibition. Project transfer can both strand absorbed B's active
countdown and recompute survivor A's canLaunch from project counts even if A had
already launched. Ordinary construction's countdown guard is a useful control:
this is specifically a PA state-transfer path that bypasses it.

F082 / KI#404 -- SURVIVES
setResearchProgress still adds the overflow amount to an already-overflowed
stored progress value instead of reducing the completed-tech progress to that
overflow. The suspicious arithmetic is source-direct and independently
rediscovered by the final mechanical arithmetic pass; it remains normalized to
F082 rather than duplicated.

F083 / KI#405 -- SURVIVES
The non-repeat setHasTech transition still performs m_iTechCount++ for both
true->false and false->true. Advanced Start supplies a supported false transition,
and current gameplay consumers use the cached total. Save/load preserves the
bad cache rather than repairing it.

F084 / KI#406 -- SURVIVES
Current TECH_SATELLITES remains Advanced-Start refundable and bMapVisible. Full
plot revelation is irreversible, whereas the tech can be removed/refunded. The
repair remains refund-policy/validation rather than attempting to erase possibly
legitimate independent map knowledge.

F085 / KI#407 -- SURVIVES
Player currentEra remains monotonic in ordinary setHasTech acquisition and has
no inverse on Advanced Start tech removal. Current-era consumers are live
mechanics, not presentation only. Restricting any recomputation to the Advanced
Start removal transaction remains the least disruptive repair direction.

F086 / KI#408 -- SURVIVES
isInContactWithBarbarians still derives iUnitThresh directly from current era.
Era 0 therefore makes the own-unit filter `count < 0` impossible and the
barbarian-unit test `count > 0`, globalizing the intended local early contact
criterion as soon as any qualifying barbarian unit exists anywhere.

F087 / KI#409 -- SURVIVES
shareItems still processes the survivor team ID in the absorbed team's directed
EP array, converting ordinary B->A espionage into A->A self-target EP. K-Mod's
getTotalUnspentEspionage still includes self because it iterates CIV_ALIVE
without NOT_SAME_TEAM_AS, and live spy-transport AI uses that total as a target
share denominator. This is not harmless dead-slot residue.

F088 / KI#410 -- SURVIVES
Advanced Start removal validation is team-tech-wide at the mutation boundary but
still checks only the acting player's units/buildings and no already-purchased
map improvements/routes. Current Agriculture/The Wheel/etc. provide ordinary
buy-infrastructure-refund examples. The root remains incomplete dependency
validation, with teammate and infrastructure manifestations kept together.

F089 / KI#411 -- SURVIVES AND BROADENS BIDIRECTIONALLY
The historical K-Mod m_abHasSeen omission is real in BOTH observer directions.
The prior wording established that absorbed B's players can lose identity
knowledge of outsider C because A.hasSeen(C) is not unioned.

The reverse is equally direct: if outsider C historically saw B but never A,
then after B's player is reassigned to team A, identity-display checks for that
player consult C.hasSeen(A). addTeam never transfers C.hasSeen(B) into
C.hasSeen(A). Thus an outsider can also lose the identity of a civilization /
player it had already seen merely because that player joined the surviving team.
Current-map copying can reconstruct some cases but cannot reconstruct historical
residue after the revealing city/owner evidence has changed. Normalize F089 as
PA failure to union historical hasSeen identity knowledge in both directions.

F090 / KI#412 -- SURVIVES
TRADE_CITIES continues to carry a player-local city ID, while the reparations
announcement formatter resolves it through the giving team's leader after
player identity has been discarded. Multi-player teams created by PA make the
namespace collision reachable. Deal execution itself remains the clean control
because it uses the actual giving PlayerTypes.

F091 / KI#413 -- SURVIVES
turnsOfForcedPeaceRemaining still recognizes only a TRADE_PEACE_TREATY at the
head of the first list. AdvCiv deliberately keeps treaty items at the bottom of
ordinary offer lists, and valid reparations can precede them. The helper's live
AI callers therefore can see zero remaining forced-peace turns while the team
flag and deal still enforce peace.

F092 / KI#414 -- SURVIVES
meet still calls makeHasMet sequentially, and makeHasMet still triggers Tech
Share before reciprocal contact has been initialized. OTHER_KNOWN_TO examines
the reverse relation, preserving the initiator-dependent Internet grant. The
later firstContact callback reread found no second independent persistent root;
Tech Share remains the concrete gameplay manifestation.

F093 / KI#415 -- SURVIVES
The doWarWeariness loop remains CIV_ALIVE while retaining a branch explicitly
intended to apply accelerated decay to dead targets. The dead-target branch is
therefore unreachable after the AdvCiv iterator refactor, and old war-weariness
can remain frozen until revival. Neighboring dead-target timer pausing that was
already present in BtS is not folded into this regression.

F094 / KI#416 -- SURVIVES
Persistent tech effects and currentEra still use alive-only MemberIter even
though the pre-practical-1837 loops intentionally processed every player
assigned to the team. A dead member of a still-alive team can therefore miss
later team tech effects, and revival does not replay the team's known techs.

F095 / KI#417 -- SURVIVES
getDefensivePower(eExcludeTeam) still excludes only direct players of the
attacking team. A vassal whose master is eExcludeTeam can be counted through the
target master's Defensive Pact with that same master, putting the attacker's own
vassal power on the target side. Numerous UWAI/war-evaluation callers pass the
attacking team explicitly, so this is not a Python-only API corner.

F096 / KI#418 -- SURVIVES
Tech Share still counts getNumMembers() for each alive known team with the tech,
while getNumMembers includes dead assigned teammates. Current Internet threshold
is 2 players and the feature's AI valuation uses alive player iteration, leaving
a direct implementation/valuation semantic mismatch.

F097 / KI#419 -- SURVIVES
The Customizable Domestic Advisor X wrapper still returns city.getY() in its
map-centering-unlocked branch while Y also returns getY(). Current Paper /
Stonehenge map-centering sources make the branch reachable. This remains a
cross-file Python/UI root surfaced from the CvTeam map-centering audit.

F098 / KI#420 -- SURVIVES IN BROADENED FORM
Both deterministic m_aiTurnsAtPeace and exact AI_getHasMetCounter are merged
max-on-survivor versus weighted-on-outsider. AI_doCounter explicitly documents
HasMet and AtWar as exact; AtWar's explicit post-merge reconciliation is the
neighboring control. Randomized/decaying AtPeace/OpenBorders/DP/ShareWar counters
do not join this root.

F099 / KI#421 -- SURVIVES, PA-SPECIFIC
setStolenVisibilityTimer owns secondary per-plot visibility counts. PA transfers
B's real sight to A but never clears live observers' old ->B relation through the
setter before B dies by member reassignment. Normal civilization elimination is
a negative control because ordinary sight loss drains those plot counts. Direct
resetVal later can erase bookkeeping without removing leaked plot state.

F100 / KI#422 -- SURVIVES
PA project inheritance still calls ordinary positive changeProjectCount before
compensating only ProjectCreatedCount. The positive path can emit completion /
replay/message side effects for a project that B completed earlier. Project-art
vector sizing remains correct and is explicitly rejected as a memory-safety
sibling.

FINAL LIST-HEAD / SOURCE-ORDER CONTROL
--------------------------------------
The only other raw CvTeam.cpp `.head()` use is
allowDefensivePactsToBeCanceled(). Mixed dual deals are genuinely possible in
multiplayer (CvDeal::doTurn explicitly documents dual+GPT mixtures), so its
head-only DP recognition is generically fragile. However current SAS has
BBAI_DEFENSIVE_PACT_BEHAVIOR=0. In that mode triggerDefensivePacts first calls
kTarget.cancelDefensivePacts(), whose implementation scans the full first list
and kills every target DP deal before allowDefensivePactsToBeCanceled runs.
Therefore the head-only helper has no demonstrated current-SAS behavioral
consequence under the shipped configuration. Keep it as a configuration/modmod
robustness note, not F101. If BBAI behavior 1/2 is ever enabled, it deserves a
fresh focused audit.

The PA-Tech-Share intermediate-state candidate also remains rejected: Internet
thresholding counts players, so A and B as separate pre-merge teams contribute
exactly the same A-members+B-members total they contribute after merging. Any
dead-member inflation is already F096.

CURRENT CURSOR
--------------
COMPLETED FILES: 12 / 169
FILE 013: CvTeam.cpp OPEN
PHYSICAL CHECKPOINT: C013-WIP20
LIVE NORMALIZED ROOTS IN THIS FILE: F078-F100
RETRACTED: former F101 / KI#423
NEXT ROOT ID: F101 UNUSED

The remaining task before C014 is now narrowly the final whole-file method /
source-order exhaustion and one final ledger-integrity check. If that pass stays
quiet, C014 can be earned with F101 still unused; finding count is not a closure
criterion.

No repository source files were modified.
===============================================================================



===============================================================================
C014 -- FILE 013 CvTeam.cpp DURABLE CLOSURE
===============================================================================

STATUS
------
Authoritative current source reviewed for this closure:
AdvCiv-SAS_light_source_20260825T141228.zip.

Current CvTeam.cpp SHA-256 reviewed:
b4349d1515cf551f34c68c75eddf34f020d8b4fe22f4cf7faca9f7aa44fb0866

Independent local controls supplied and used where relevant:
- AdvCiv 1.14 (`AdvCiv-1.14 (1)(3).zip`)
- Civ4CE (`Civ4CE-main(3).zip`)

The complete C013-WIP20 artifact is preserved byte-for-byte as the prefix of this
C014 artifact. No repository source files were modified by this audit.

FILE 013 IS NOW COMPLETE for the current file-oriented audit.
Durable completed-file count advances from 12/169 to 13/169.

No new independent root survived the final whole-file exhaustion after WIP20.
F101 therefore remains UNUSED and becomes the next available root number for
file 014.

The former WIP17 F101 / provisional KI#423 remains RETRACTED FALSE POSITIVE and
must not be implemented or reused. `shareItems` passes NO_PLAYER into setHasTech,
but setHasTech normalizes NO_PLAYER to getLeaderID() before the event reporter,
so BUG Autolog does not receive player -1 through that path.


FINAL NORMALIZED ROOT LEDGER -- FILE 013 / CROSS-FILE ROOT REACHED FROM IT
--------------------------------------------------------------------------

F078 / KI#400 -- RETAINED
Permanent Alliance fails to migrate active Counterespionage state in either
relevant direction. The turns/modifier arrays are TeamTypes-keyed live gameplay
state and current espionage missions still create nonzero 10-turn/+100 effects.
Player reassignment cannot reconstruct them.

F079 / KI#401 -- RETAINED
Permanent Alliance does not union inherited first-contact dates
`m_aiHasMetTurn`. The surviving team can replace an absorbed constituent's older
real contact date with the alliance-turn contact created during `meet`, changing
partial Info-Screen graph/history visibility for players who are now members of
the same merged team. The correct invariant is the earliest real inherited date
for the merged bilateral relationship, not merely the survivor's personal date.

F080 / KI#402 -- RETAINED
A live Disengage deal can follow reassigned players through a Permanent Alliance
while the TeamTypes-keyed `m_abDisengage` enforcement flag is not migrated. The
deal and the rule-enforcement state can therefore disagree after the merge.

F081 / KI#403 -- RETAINED
Permanent Alliance does not coherently merge spaceship/victory launch state.
An absorbed team's active victory countdown can be lost, while inherited project
counts can also make the survivor launch-capable again despite an earlier launch.
The PA trade gate has no post-launch exclusion. This is persistent team gameplay
state, not merely announcement/UI state.

F082 / KI#404 -- RETAINED
`setResearchProgress` mishandles completed-tech overflow by adding the computed
overflow to the already-overflowed stored progress value. The final mechanical
arithmetic pass independently rediscovered the same suspicious line and
normalized it back into F082 rather than creating a duplicate root.

F083 / KI#405 -- RETAINED
The non-repeat `setHasTech` branch increments `m_iTechCount` on both false->true
and true->false transitions. Advanced Start supplies a supported tech-removal
transition, so refunds can increase the cached total-tech count instead of
reducing it. Current gameplay consumers use that cache and save/load preserves
it.

F084 / KI#406 -- RETAINED
Current `TECH_SATELLITES` is Advanced-Start refundable and has `bMapVisible=1`.
Buying it reveals the full map irreversibly; refunding/removing the tech cannot
separate that knowledge from other legitimate revelation sources. The clean
repair direction is therefore to prevent/refine the refund rather than attempt a
blind map rollback.

F085 / KI#407 -- RETAINED
Advanced Start tech removal does not lower player `currentEra`, although tech
acquisition raises it. The era value has live gameplay consumers. Any repair
should remain narrowly transaction-aware rather than making ordinary era state
non-monotonic globally.

F086 / KI#408 -- RETAINED
`isInContactWithBarbarians` derives its own-unit threshold directly from current
era. In ERA_ANCIENT that threshold is 0, making the own-unit test `< 0`
impossible while the barbarian-unit side becomes `> 0`. The intended local
contact criterion can therefore globalize as soon as any qualifying barbarian
unit exists anywhere.

F087 / KI#409 -- RETAINED
Permanent-Alliance espionage summation includes the absorbed team's directed EP
against the survivor and stores it as survivor self-target EP. K-Mod's
`getTotalUnspentEspionage()` includes self because its alive-team iteration does
not exclude the same team, and live spy-transport AI consumes that total as a
share denominator. This is behaviorally observable state, not harmless dead-slot
residue.

F088 / KI#410 -- RETAINED
Advanced Start tech-refund validation is incomplete at the team mutation
boundary. It checks only the acting player's unit/building dependencies and does
not cover teammates or already-purchased tech-dependent map infrastructure such
as improvements/routes. Current Agriculture/The Wheel-style dependencies give
ordinary reproductions. These manifestations remain one dependency-validation
root.

F089 / KI#411 -- RETAINED, BIDIRECTIONAL NORMALIZATION
Permanent Alliance fails to union K-Mod historical `m_abHasSeen` identity
knowledge in both directions.

1. Absorbed B's players can lose historical identity knowledge of outsider C
   because, after reassignment, their team identity queries use A.hasSeen(C).
2. Outsider C can likewise lose historical identity knowledge of B's player/
   civilization because that player now belongs to A and C.hasSeen(A) was never
   inherited from C.hasSeen(B).

Current map/revealed-state copying can reconstruct some present evidence, but it
cannot reconstruct arbitrary historical residue once the original revealing
city/ownership evidence has changed.

F090 / KI#412 -- RETAINED
Reparations city announcement formatting receives only TeamTypes and resolves a
player-local city ID through the team's leader. Permanent Alliance makes
multi-player teams normal, so a city belonging to a nonleader member can be
misnamed or omitted. Deal execution is the control: it retains the actual giving
PlayerTypes and resolves the local city namespace correctly.

F091 / KI#413 -- RETAINED
`turnsOfForcedPeaceRemaining` recognizes a peace treaty only when
`TRADE_PEACE_TREATY` is the first item of the first deal list. Valid AdvCiv deal
construction can place reparations/other items before the treaty. AI callers can
therefore observe zero remaining forced-peace turns while `isForcePeace` and the
actual deal still enforce peace.

F092 / KI#414 -- RETAINED
`meet` initializes contact directionally through two sequential `makeHasMet`
calls, and `makeHasMet` triggers Tech Share before reciprocal contact is set.
`OTHER_KNOWN_TO` tests the reverse relationship, so an Internet/Tech-Share grant
can depend on which direction executes first. The final callback audit found no
second independent persistent first-contact root; Tech Share remains the proven
manifestation.

F093 / KI#415 -- RETAINED
`doWarWeariness` now iterates `TeamIter<CIV_ALIVE>` while retaining an inner
branch explicitly intended for `!target.isAlive()`. That dead-target branch is
unreachable after the iterator refactor, so war weariness against a dead team can
remain frozen and later matter again if the team revives.

F094 / KI#416 -- RETAINED
Persistent tech/player effects and era propagation use alive-only `MemberIter`.
A dead member of a still-alive team can miss tech-derived player state acquired
while dead, and revival does not replay the team's known technologies. The
pre-practical-1837 full-player loop provides the historical semantic control.

F095 / KI#417 -- RETAINED
`getDefensivePower(eExcludeTeam)` excludes only players whose direct team equals
the attacking team. An attacker's vassal can still be counted on the target side
through master/Defensive-Pact logic. Live UWAI/war-evaluation callers pass the
attacker explicitly, so this is a real combat-evaluation accounting problem.

F096 / KI#418 -- RETAINED
Tech Share counts `getNumMembers()` for each alive known team possessing the
technology. `getNumMembers()` includes dead assigned teammates, allowing dead
players to satisfy the current Internet player threshold. The project's own AI
valuation uses alive-player iteration, giving a strong internal semantic control.

F097 / KI#419 -- RETAINED CROSS-FILE ROOT
The Python Customizable Domestic Advisor X-coordinate wrapper returns
`city.getY()` in its map-centering-enabled branch, while Y also returns getY().
Current Paper/Stonehenge map-centering makes the branch reachable. This root was
found through CvTeam's map-centering audit but belongs to the Python/UI source;
do not rediscover or double-count it when that file is audited later.

F098 / KI#420 -- RETAINED, BROADENED EXACT-HISTORY ROOT
Permanent Alliance asymmetrically merges exact bilateral relationship history.
Two proven manifestations now belong to the same causal root:
- `m_aiTurnsAtPeace`
- `AI_getHasMetCounter`

The survivor direction takes the stronger/max constituent history while the
outsider reverse direction receives a team-size-weighted merge. `AI_doCounter`
explicitly documents HasMet and AtWar as exact counters. AtWar is the decisive
neighboring control because PA explicitly reconciles its two directions after
the weighted merge; HasMet and TurnsAtPeace are not reconciled. Randomized or
decaying AtPeace/OpenBorders/DefensivePact/ShareWar counters are deliberately
excluded from F098.

F099 / KI#421 -- RETAINED, PA-SPECIFIC
`setStolenVisibilityTimer` owns secondary per-plot stolen-visibility counts.
Permanent Alliance transfers absorbed B's legitimate visibility into A but does
not clear live observer->B timers through the stateful setter before B becomes a
dead team through member reassignment. Those old plot counts can therefore
survive after the intended observer->A timer expires. Direct resetVal can later
erase the bookkeeping timer without removing the leaked plot state.

Ordinary civilization elimination is an important negative control: normal
unit/city sight loss drives `changeVisibilityCount` and drains observers' stolen
plot counts even as the target dies. The leak is therefore specifically the PA
state-transfer lifecycle, not generic dead-team timer pausing.

F100 / KI#422 -- RETAINED
Permanent-Alliance project inheritance calls ordinary positive
`changeProjectCount` when the absorbed team owns more copies. That normal
completion path can emit a new project-completion message/replay event for an
already-completed inherited project. The later `ProjectCreatedCount`
compensation repairs only the numerical global count; it cannot retract emitted
side effects. Project-art vectors remain size-consistent and were explicitly
rejected as a memory-safety sibling.

FORMER F101 / provisional KI#423 -- RETRACTED FALSE POSITIVE
The WIP17 Autolog hypothesis is authoritatively rejected. `shareItems` does call
`setHasTech(..., NO_PLAYER, ...)`, but `setHasTech` immediately replaces
NO_PLAYER with `getLeaderID()` before `CvEventReporter::techAcquired`. Python and
BUG Autolog therefore receive a valid player ID. This historical false
hypothesis remains in the cumulative prefix so it will not be rediscovered.

F101 -- UNUSED
No new independent root survived file-013 closure.


FINAL WHOLE-FILE CLOSURE EVIDENCE
---------------------------------

1. Permanent-Alliance transition matrix exhausted

The audit mechanically classified and challenged serialized/team-owned state
against addTeam/shareItems/shareCounters and player reassignment. This produced
or strengthened F078-F081, F087, F089 and F098-F100. Late candidate fields were
not promoted without live consequences:
- `m_abJustDeclaredWar` is not explicitly inherited, but current SAS config has
  SPEND_ALL_MOVES_ON_INVASION=0 and identical base/DoW capture chances (50/50),
  making the flag behaviorally inert in the shipped configuration.
- `m_bMapCentering` is not explicitly merged, but normal current PA eligibility
  arrives through Game Theory; the current prerequisite chain has already passed
  Paper, which grants map centering. No normal current-SAS PA constituent lacks
  the state.
- repeat-tech count migration is generically weak, but current SAS has no
  repeatable technology.
- summed team-limited UnitClass/BuildingClass counts are a generic modmod risk,
  but current SAS has no positive UnitClass/BuildingClass MaxTeamInstances.
  Projects are the live team-limited family and have their own merge path.
- project default/art provenance can lose cosmetic/default choices, but inherited
  count slots are valid and no source-level gameplay/memory fault was proved.

2. Stateful-setter / secondary-state matrix exhausted

Stolen visibility remains exceptional because its timer setter owns secondary
per-plot state. Neighboring war/open-borders/Defensive-Pact/force-peace and
counter arrays either contain their gameplay state directly or are reset in
initialization/transition contexts where no analogous unreconciled secondary
state was established.

The old Kek-Mod PA espionage fix was also used as a control: it clears city-level
espionage visibility but never performs the stolen-map-visibility target cleanup,
which is why F099 is a neighboring lifecycle omission rather than a repaired
rediscovery.

3. Relationship-setter/source-order pass exhausted

Late candidates were explicitly challenged rather than inferred from “setter has
side effects” alone:
- Open Borders propagation can reset OpenBordersCounter, but ordinary
  `setOpenBorders(false)` already resets that relation's counter; there is no
  legitimate fading nonzero closed-treaty history to preserve.
- Defensive Pact propagation can emit a new A-C relation event, but A-C genuinely
  becomes a newly established relation through the merger, unlike F100's already
  completed project. No false-event sibling was established.
- `allowDefensivePactsToBeCanceled` inspects only the head trade item and is
  generically fragile for mixed multiplayer dual deals. However shipped
  `BBAI_DEFENSIVE_PACT_BEHAVIOR=0` first calls `cancelDefensivePacts`, whose full
  first-list scan kills target DP deals before that helper can matter. Keep this
  as configuration/modmod robustness debt; re-audit if behavior 1/2 is enabled.
- PA tech sharing before member reassignment does not transiently inflate current
  Internet thresholds: Tech Share counts players, and separate A-members +
  B-members equals the final merged member count. Dead-member inflation remains
  exactly F096.

4. Tech acquisition/removal and Advanced Start one-way effects exhausted

Every significant one-way `setHasTech/processTech` effect was challenged against
supported tech removal and current XML:
- cached total-tech count -> F083
- full-map reveal -> F084
- player currentEra -> F085
- teammate/map-infrastructure dependency validation -> F088
- map centering was normalized as an irreversible discovery-style state rather
  than a new root; its PA omission is unreachable under current PA tech gating.
- `makeSpecialBuildingValid` is another one-way acquisition side effect, but
  current SAS XML contains no SpecialBuildingInfo with a live TechPrereqAnyone,
  so there is no shipped Advanced-Start reproduction.
- first-discovery religion/free-tech/free-unit effects do not create an Advanced
  Start refund sibling because the Advanced Start acquisition path uses
  `bFirst=false`.

5. Exact-counter / iterator-predicate pass exhausted

The counter audit distinguished exact bilateral histories from intentionally
randomized directional AI counters. That distinction broadened F098 but rejected
AI AtPeace/OpenBorders/DP/ShareWar siblings. Exact AtWar remains the local
positive control because PA explicitly reconciles it.

The iterator-predicate audit found the F093 dead-target contradiction and F094
alive-only member regression, but no second impossible-branch analogue survived.
First-contact callback/event consumers were also traced after F092; no second
persistent callback-side half-contact bug was found.

6. Divisor / raw index / list-head / queue / pointer closure

A fresh mechanical pass covered the remaining arithmetic and container-risk
sites:
- current-era averaging checks the member divisor and returns NO_ERA for dead
  teams;
- culture/espionage denominators are guarded or clamped;
- Barbarian research reaches division only with structurally positive live-team
  domains;
- PA weighted merges divide by the merged live team's positive member count;
- addTeam/member vectors are size-bounded;
- reparations string indexing is size-bounded;
- F091's treaty-head bug is semantic ordering, not a null-head problem;
- the six multi-war queues are pushed and popped only in lockstep through
  queueWar/triggerWars;
- `getRandomMemberAlive` lacks a local zero guard, but every current C++ caller
  found operates through UWAI/live-team domains; no normal zero-domain call was
  established;
- `getLeaderCapital` has a strong live-team assumption, but every current caller
  found is a live trade/circumnavigation path. No dead-team dereference root was
  established.

7. Building/improvement/helper warning blocks exhausted

K-Mod's source-authored “heaps of bugs” comment around bonus connection describes
the rationale for `doesImprovementConnectBonus`; current callers use the
centralized helper and no new divergence was found.

`updateActivePaths` and `isCityDefense` warning comments likewise describe known
path/UI or assertion limitations, not a new reachable gameplay defect.

`shareItems` team-shared-building ordering was challenged specifically: both
teams' technology sets are synchronized before the building loops, so apparent
cross-team obsolescence disagreement collapses. Building map-centering remains a
one-way “ever discovered/obtained” state in inherited code and no independent
current regression was established.

8. Vassal / war-transition tail exhausted

The final pass re-read `setVassal`, `assignVassal`, `freeVassal`, queueWar and
triggerWars against the synchronized current source. Existing SAS repairs (for
example the KI#363 war-plan-count restoration ordering) are already present.
No new master/vassal transition ordering root survived.

The queue family is internally encapsulated: all six parallel queues receive one
entry per `queueWar` invocation and are consumed/popped together in
`triggerWars`. Checking only attacking_queue.empty() therefore does not create a
current desynchronization path.

9. Serialization / initialization / final helper closure

Current CvTeam read/write ordering is paired. Apparent scan mismatches reduce to
commented-out old vassal storage and the deliberately manual symmetric project-
art loop. `changeProjectCount` maintains project-art vector cardinality.

The advc.134a peace-offer fields are intentionally transient and excluded from
serialization; their source comment already acknowledges the networked-
multiplayer limitation. No stronger concrete reproduction was established in
this file audit, so it remains an acknowledged limitation rather than a newly
numbered root.

Old uiFlag migration branches have deliberately been removed under the current
SAS same-version-save policy (KI#46). Unsupported historical save compatibility
was not counted as a new defect.

The final spaceship helper tail (`getCompletedSpaceshipProjects`,
`getProjectPartNumber`, `hasLaunched`) is consistent with its current project
call domains and did not produce an additional count/art/index root beyond F081
and F100.

10. Adversarial F078-F100 reread completed

Every live root was actively challenged against current callers, current XML /
GlobalDefines where relevant, and later SAS fixes. None of F078-F100 required
retraction. Several were narrowed or strengthened instead:
- F089 is explicitly bidirectional historical identity loss.
- F098 contains only exact bilateral histories; randomized counters are excluded.
- F099 is explicitly PA-specific; ordinary elimination is the negative control.
- F100 is announcement/replay state, not project-art memory corruption.

The process also caught its own WIP17 F101/KI#423 mistake before closure, which is
why preserving adversarial retractions in the cumulative audit remains useful.
A high finding count was never used as a closure criterion.


CLOSURE DECISION
----------------

CvTeam.cpp is defensibly exhausted for this file-oriented pass. The surviving
F078-F100 roots have been normalized, merged where manifestations share one
cause, checked for current configuration/XML reachability, and adversarially
re-read. The final method/source-order, setter-secondary-state, PA provenance,
tech-transition, iterator, divisor/index/list/pointer, vassal/war, serialization
and helper-tail passes produced no unresolved F101 candidate.

FILE 013 STATUS: COMPLETE -- REVIEWED
COMPLETED FILES: 13 / 169
NEXT ROOT ID: F101 (UNUSED)

NEXT FILE
---------

014  5002  CvGameCoreDLL/WarUtilityAspect.cpp

Start from the synchronized 2026-08-25 current SAS source and supplied AdvCiv
1.14 / Civ4CE controls. First build a structural/caller inventory and map the
WarUtilityAspect subclasses/dispatch into UWAI war evaluation, then audit each
aspect's arithmetic domains, sign/scale conventions, target/master/vassal
semantics, cached/current state assumptions and cross-aspect aggregation. Carry
F078-F100 only as existing CvTeam findings and F097 as an already-known
cross-file amendment; do not rediscover or double-count them.

No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP01 -- FILE 014 WarUtilityAspect.cpp OPEN -- FIRST PROMOTED ROOTS
===============================================================================

STATUS
------
Authoritative current source:
AdvCiv-SAS_light_source_20260825T141228.zip.

Current WarUtilityAspect.cpp SHA-256 reviewed:
6a2422bf343527c5f5f3eb4073b3c9717d05cb844806946c2b081622e41e59a9

Controls used locally:
- supplied AdvCiv 1.14 source
- supplied Civ4CE source where applicable (UWAI/WarUtilityAspect is AdvCiv-side)
- current SAS XML/GlobalDefines and AdvCiv historical git-log/manual material

The complete durable C014 artifact is preserved byte-for-byte as this WIP's
prefix. File 013 CvTeam.cpp remains durably COMPLETE; file 014 is OPEN.
No repository source files were modified.

STRUCTURAL MAP ESTABLISHED
--------------------------
WarEvaluator creates one instance of each WarUtilityAspect subclass, then loops
all alive members of the agent team:

    vector<WarUtilityAspect*> apAspects;
    fillWithAspects(apAspects);
    for (MemberIter itMember(m_kAgent.getID()); itMember.hasNext(); ++itMember)
        evaluate(itMember->getID(), apAspects);

The same aspect objects are deliberately retained across member evaluations;
WarUtilityAspect::m_iU is initialized to zero once and reset() does NOT clear it.
The final team utility then sums each aspect object's cumulative utility once.
This is consistent with AdvCiv's historical team-game design note that teammate
war utility is "already covered by adding up the war utility of all team members
in the end."

Current SAS differs semantically from AdvCiv 1.14 in this file mainly through
its faraway-target Risk::preEvaluate hard guard and its winning-war Distraction
cap. In addition, current SAS activates many non-100 UWAI aspect weights through
AI_Variables_GlobalDefines.xml; base AdvCiv 1.14 has all global aspect weights at
100 and therefore leaves F101 below numerically dormant.


F101 / provisional KI#424 -- CONFIRMED -- SAS-ACTIVATED INHERITED UWAI TEAM BUG
NON-100 WAR-UTILITY ASPECT WEIGHTS COMPOUND ACROSS TEAM MEMBERS INSTEAD OF
WEIGHTING EACH MEMBER ONCE
-------------------------------------------------------------------------------
WarUtilityAspect::evaluate(MilitaryAnalyst const&) adds the current member's raw
contributions to the aspect object's persistent m_iU and then applies the XML
weight to the WHOLE accumulated m_iU:

    scaled rXMLAdjust = getUWAI().aspectWeight(xmlID());
    if (m_iU != 0 && rXMLAdjust != 1)
        m_iU = (m_iU * rXMLAdjust).round();

reset() clears only transient player/rival pointers and helper state; it does not
clear m_iU. WarEvaluator then calls this same method once for each alive team
member on the same aspect objects. Therefore member N reweights every earlier
member's already-weighted contribution again.

For two team members whose raw contribution to a 300% aspect is +10 each:
- intended additive team result: (10 + 10) * 3 = +60;
- current member 1: 10 * 3 = 30;
- current member 2: (30 + 10) * 3 = 120.

With three equal +10 contributors at 300%, current result becomes 390 instead of
90; with four it becomes 1200 instead of 120. Weights below 100 repeatedly
suppress earlier members instead: at 50%, two +10 members yield about 7.5 (then
integer/fixed-point rounding) instead of 10. The result is also member-iteration
order dependent whenever member raw utilities differ: earlier players are
reweighted once for every later teammate.

This is strongly live in current SAS. Of 27 global UWAI aspect weights, 17 are
currently non-100, including:
- Loathing 70%
- Military Victory 105%
- Preservation of Partners 80%
- Reconquista 130%
- Rebuke 200%
- Fidelity 110%
- Hired Hand 0%
- Border Disputes 90%
- Preemptive War 125%
- King Making 105%
- Risk 105%
- Distraction 300%
- Ulterior Motives 200%
- Fair Play 125%
- Tactical Situation 140%
- Third Party Intervention 200%
- Dramatic Arc 10%.

Base AdvCiv 1.14 contains the same cumulative weighting code but currently has
all these global aspect weights at 100%, so the arithmetic defect is dormant in
that baseline. Historical AdvCiv notes nevertheless explicitly support additive
member aggregation, and older/modmod non-100 weights can activate the same root.

No aspect implementation reads prior m_iU to make a cross-member decision; apart
from the wrapper, implementations only add/subtract their result. A conservative
repair can therefore preserve cumulative storage while weighting only the
current member's delta, e.g. snapshot m_iU before preEvaluate/rival evaluation,
compute the raw delta, apply rXMLAdjust to that delta once, then add the weighted
delta back to the prior cumulative value. Returning that weighted delta would
also repair the secondary diagnostic issue that WarEvaluator's current
"Total utility for <player>" log becomes cumulative for the second and later
team members even at 100% weights.

This root affects normal starting-team games and teams enlarged by Permanent
Alliances; its magnitude grows rapidly with member count for weights far from
100%. It is gameplay-significant because the final WarEvaluator result directly
feeds UWAI war/peace/war-trade decisions.


F102 / provisional KI#425 -- CONFIRMED -- ADVCIV-SAS REGRESSION / TEAM-GAME BUG
FARAWAY-WAR HARD REJECT USES ONE MEMBER'S PLAYER-LOCAL DISTANCE/LIFT CACHE AS A
PLAN-KILLING TEAM-WIDE VETO
-------------------------------------------------------------------------------
The SAS KI#61 guard lives in Risk::preEvaluate. WarEvaluator invokes that
preEvaluate once per alive member of the agent team, with m_pAgentCache pointing
to the current member's UWAICache. The guard scans:

    ourCache().cityAt(i)
    kCity.getDistance()
    kCity.canReachByLand()
    ourCache().canTrainAnyCargo()

and returns -100000 on unreachable / too-far / no-lift conditions, explicitly
commented as:

    return -100000; // kill this (agent,target) war plan

WarEvalParameters::agent and target are TeamTypes, and the final war decision is
team-level. But UWAICache::City::updateDistance(eCacheOwner) explicitly computes
paths "for each city of the cache owner" and iterates FOR_EACH_CITY over that
single player. Its getDistance/canReachByLand values are therefore player-local,
not a minimum across the team. canTrainAnyCargo is likewise stored on the
current player's cache.

A normal two-member team can consequently have:
- member A bordering target C, cached land contact 5 turns -> guard passes;
- member B isolated from C, unreachable or >15 turns -> guard returns -100000.

B's inability to personally reach C then overwhelms the aggregate team utility
and rejects a war for which A has a perfectly valid nearby front. The analogous
naval case lets one teammate with no transport capacity veto the team even when
another teammate can conduct the invasion.

This conflicts with the surrounding UWAI team design. WarEvaluator intentionally
sums all members' ordinary gains/costs, and other aspects contain explicit team-
size normalization/coordination logic. KI#61's own documentation describes an
outer sanity bound on a prospective war TARGET before ordinary UWAI evaluates
power, utility, diplomacy and context; it does not state that every teammate must
personally be within the contact limit. A remote teammate's lower expected gain
or higher risk can be represented by that member's normal utility; inability to
attack is not by itself a reason to make the target categorically illegal for
other members.

The safest policy-level repair is to make the hard eligibility test team-aware
and execute/derive it once per team: accept the contact sanity gate when at least
one alive team member has the required land/naval reach (and, for genuinely
naval contact, appropriate lift capability), while rejecting only when the team
collectively lacks a qualifying route/capability. Exact implementation should
preserve the existing KI#183 peace-scenario exclusion, ongoing-war exclusion,
existing-plan tolerance and victory-denial bypass.

This root is SAS-specific: base AdvCiv 1.14 has no KI#61 hard-reject block.


F103 / provisional KI#426 -- CONFIRMED -- INHERITED ADVCIV SEMANTIC BUG
PARTNER TECH-TRADE UTILITY TESTS THE RIVAL'S WILLINGNESS USING THE AGENT'S
TECH-REFUSAL PERSONALITY THRESHOLD
-------------------------------------------------------------------------------
partnerUtilFromTech() intentionally tests both directions of potential future
tech trade:

    AttitudeTypes eOurAttitudeThresh = techRefuseThresh(eWe);
    AttitudeTypes eTheirAttitudeThresh = techRefuseThresh(eThey);

and rejects partner utility when either AI side's attitude is below its supposed
threshold. But the helper is:

    AttitudeTypes WarUtilityAspect::techRefuseThresh(PlayerTypes ePlayer) const
    {
        if (GET_PLAYER(ePlayer).isHuman())
            return ATTITUDE_FURIOUS;
        return (AttitudeTypes)(kOurPersonality.getTechRefuseAttitudeThreshold()+1);
    }

The ePlayer argument is honored for the human check but ignored for AI
personality lookup: kOurPersonality is always the current AGENT player's leader.
Thus the second call for eThey substitutes our leader's tech refusal threshold
for the rival's.

The live trade rule elsewhere in CvPlayerAI uses the prospective trading AI's
own personality:

    GC.getInfo(kPartner.getPersonalityType()).getTechRefuseAttitudeThreshold()

so this is not an intentional shared/team threshold policy.

Current SAS leader XML makes the difference very reachable: among parsed leader
entries, thresholds include NONE, ATTITUDE_FURIOUS, ATTITUDE_ANNOYED and
ATTITUDE_CAUTIOUS. For example, a permissive rival can be incorrectly treated as
requiring the agent's stricter attitude, or a strict rival can be treated as
more willing than it really is.

partnerUtilFromTech feeds multiple gameplay utility families, including
Assistance (value of preserving a partner), IllWill/lost-partner costs and the
cost assigned to diplomatic penalties. It can therefore over- or understate war
utility depending on the leader pairing.

Base AdvCiv 1.14 has the same helper and call pattern; this is inherited rather
than introduced by SAS. The direct repair is to read the personality of ePlayer
for the AI case, e.g. GC.getInfo(GET_PLAYER(ePlayer).getPersonalityType()), while
retaining the human special case.


ACTIVE CHALLENGE AFTER F103
---------------------------
F096/KI#418 from CvTeam.cpp established that CvTeam::getNumMembers includes dead
assigned teammates. WarUtilityAspect contains several team-size normalizations
using getNumMembers even though WarEvaluator's MemberIter explicitly iterates
ALIVE members and GreedForAssets::teamSizeMultiplier explicitly uses
getAliveCount(). This may be another manifestation of the existing F096 dead-
member semantic root rather than a new F104. It is under challenge; do not count
it yet.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP01
NEW LIVE ROOTS IN FILE 014: F101-F103
RETRACTED HISTORICAL SLOT: former F101 / provisional KI#423 remains retracted
NEXT ROOT ID: F104 UNUSED

Continue with the dead-member normalization challenge, then systematic helper /
aspect arithmetic-sign-state review. In particular keep F101's aggregation
semantics in mind when evaluating all later aspects so compounded weights are
not mistaken for independent aspect-formula defects.

No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP02 -- FILE 014 WARUTILITYASPECT.CPP CONTINUATION
===============================================================================
STATUS: WIP / append-only continuation of authoritative reconciled C014-WIP01.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NEXT UNUSED ROOT AFTER THIS CHECKPOINT: F107.
NO REPOSITORY SOURCE FILES MODIFIED.

F096 / KI#418 -- BROADENED MANIFESTATION -- DEAD ASSIGNED TEAM MEMBERS ARE USED
WHERE WARUTILITYASPECT SEMANTICS REQUIRE LIVING PARTICIPANTS
-------------------------------------------------------------------------------
The CvTeam.cpp audit had already established the root fact behind F096:
CvTeam::getNumMembers() counts assigned team slots including eliminated players,
whereas getAliveCount() counts living members. That finding was originally
promoted through Tech Share, whose threshold explicitly intends players that can
actually know/share the technology. At that time the audit deliberately refused
to generalize every getNumMembers call without a consumer-specific invariant.

WarUtilityAspect.cpp now supplies that invariant for several additional sites.
WarEvaluator evaluates an AI team by iterating its living MemberIter players and
adding those living members' aspect contributions. Nevertheless the common
normalizer divides the resulting team-on-team utility by:

    GET_TEAM(eOther).getNumMembers() * kOurTeam.getNumMembers()

An eliminated-but-still-assigned teammate therefore contributes no numerator
utility, because it is not evaluated, but continues inflating the denominator.
The team utility can remain permanently diluted simply because a dead historical
member slot still belongs to the team.

lossesFromFlippedTiles is an even more explicit local proof. Its comment says:

    Tiles are counted per team, but this function gets called once per rival player.

It consequently divides the team-level tile count by iRivalMembers, but defines
that as kTheirTeam.getNumMembers(). The actual aspect evaluation only visits
living rival players. A dead assigned rival therefore makes the per-live-player
share too small and also raises the early 4*iRivalMembers significance threshold.

The file contains a strong internal control: GreedForAssets::teamSizeMultiplier
explicitly uses:

    min(kOurTeam.getAliveCount(), kTheirTeam.getAliveCount())

when it means active participants in team-on-team warfare. GreedForVassals also
starts its current-vassal count with PlayerIter<ALIVE,VASSAL_OF>::count and then
adds getNumMembers for predicted new vassal teams, another mixed live/assigned
semantic seam worth repairing consistently.

This does NOT mean every getNumMembers in the file is automatically wrong. The
root is broadened only where surrounding iteration/comment semantics establish
that the quantity represents living participants. The direct repair is to use
getAliveCount (or the corresponding alive-player iterator count) at those sites,
with each remaining team-size use reviewed individually rather than globally
replaced.


F104 / provisional KI#427 -- CONFIRMED -- INHERITED ADVCIV OFF-BY-ONE
PARTNER TRADE UTILITY'S DECLARED FOUR-RESOURCE CAP ACTUALLY COUNTS ONLY THREE
-------------------------------------------------------------------------------
WarUtilityAspect::partnerUtilFromTrade() explicitly says:

    // Count only the first four resource trades

and sets:

    int const iMaxResourceTrades = 4;

However, for each qualifying resource deal it first increments iResourceTrades
and then skips when:

    if (iResourceTrades >= iMaxResourceTrades)
        continue;

Thus resource deals 1, 2 and 3 are valued, while deal 4 is already skipped. The
log message compounds the contradiction by saying that trades "in excess of 4"
are skipped even though the fourth itself is discarded.

This is a live utility input: partnerUtilFromTrade contributes to the value of
preserving a diplomatic/trading partner, including Assistance and IllWill costs.
A relationship with four ongoing qualifying resource deals is therefore valued
as though only three existed.

Base AdvCiv 1.14 contains the same code and comment, so this is inherited. The
minimal repair is to skip only when iResourceTrades > iMaxResourceTrades (or test
before increment with an equivalent >= condition), preserving exactly the first
four as documented.


F105 / provisional KI#428 -- CONFIRMED -- INHERITED ADVCIV BOUNDARY / DIVISOR BUG
DOMINATION WAR-UTILITY PROGRESS MISHANDLES EXACT POPULATION-THRESHOLD EQUALITY
-------------------------------------------------------------------------------
MilitaryVictory::progressRatingDomination computes:

    iPopToGo = iPopTarget - kOurTeam.getTotalPopulation();

and later treats population as already satisfied only when:

    if (iPopToGo < 0)

The real CvGame Domination victory check, however, rejects population only when:

    100 * teamPopulation < totalPopulation * adjustedPopulationPercent

so exact equality already satisfies the population requirement. Therefore
`iPopToGo == 0` is a normal boundary state in which the population half of the
victory condition is complete.

The utility code does not recognize that state. It first executes:

    rPopGained.decreaseTo(iPopToGo);

which clamps projected population gain to zero, then enters the non-negative
branch and can evaluate either:

    rPopGained / iPopToGo

or the two-component expression containing the same division. That is division
by zero in ScaledNum's MulDiv-backed arithmetic, and independently of the exact
platform error/sentinel behavior it is semantically wrong: a fully satisfied
population component is being represented as zero/error progress.

Current SAS has a live Domination victory, so this is reachable whenever a team
lands exactly on the population threshold while still needing land/cities and
WarUtility evaluates projected conquests. Base AdvCiv 1.14 has the same `< 0`
boundary and division structure, making the bug inherited.

The direct boundary repair is to treat `iPopToGo <= 0` as population already
satisfied and route equality through the existing completed-population logic,
thereby both preserving victory semantics and eliminating the zero divisor.


F106 / provisional KI#429 -- CONFIRMED -- INHERITED ADVCIV SCORE-COMPARISON BUG
KINGMAKING COMPUTES AN ADJUSTED BEST SCORE BUT COMPARES CANDIDATES USING RAW SCORE
-------------------------------------------------------------------------------
KingMaking::addLeadingPlayers first computes rBestScore from an adjusted score for
each free major player. In the midgame it deliberately adds half of estimated
commerce:

    // Count extra score for commerce so that players that are getting far
    // ahead in tech are identified as a threat

and can further multiply a peaceful overseas culture/space contender's score by
4/3. rBestScore therefore represents the maximum ADJUSTED contender score.

The second pass does not apply those adjustments to candidates. It compares:

    raw predicted/current game score / rBestScore

against 70% for humans or 75% for AI.

This defeats the stated purpose of the adjustments. A tech leader's commerce
bonus raises the denominator used against everybody but never raises that same
leader's numerator. In a sufficiently strong case, the player that established
rBestScore can fail its own 75% cutoff; the adjusted leader can therefore be
omitted from kLeading, and in the extreme the set can be empty despite the
nearby invariant comment that someone is normally always ahead.

The 4/3 overseas-victory adjustment has the same mismatch. Base AdvCiv 1.14
contains the same two-pass implementation, so the root is inherited.

The robust repair is to centralize the adjusted contender-score calculation and
use that same quantity both when deriving rBestScore and when testing each
candidate against the margin. This also prevents future changes to the first-pass
threat adjustment from silently diverging from the second pass again.


POST-CHECKPOINT AUDIT CURSOR
----------------------------
- File 014 remains OPEN; this is not C015.
- F101/KI#424 through F106/KI#429 are live file-014 roots, with former
  F101/KI#423 retained only as a retracted historical false positive.
- F096/KI#418 has been broadened with consumer-proven WarUtilityAspect dead-member
  manifestations; no duplicate finding number was created.
- Next unused root: F107.
- Continue with PublicOpposition, Revolts, UlteriorMotives, FairPlay,
  Bellicosity, TacticalSituation, ThirdPartyIntervention and DramaticArc, plus
  remaining player-vs-team / sign / divisor / state-freshness checks.
===============================================================================


===============================================================================
C014-WIP03 -- FILE 014 WARUTILITYASPECT.CPP CONTINUATION
===============================================================================
STATUS: WIP / append-only continuation of authoritative C014-WIP02.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NEXT UNUSED ROOT AFTER THIS CHECKPOINT: F111.
NO REPOSITORY SOURCE FILES MODIFIED.

F107 / provisional KI#430 -- CONFIRMED -- INHERITED ADVCIV SIDE-CONSTRUCTION BUG
PREEMPTIVEWAR BUILDS OUR PRESENT AND PREDICTED CITY BASES INCONSISTENTLY
-------------------------------------------------------------------------------
PreEmptiveWar::evaluate compares the rival side's projected city ratio with our
side's projected city ratio to estimate whether a currently threatening rival is
likely to become more dangerous over the long run.

The rival side is constructed consistently. For each rival player, teammate or
vassal, an existing vassal is included at half weight in BOTH current and
predicted cities, while a newly predicted capitulation is included at half
weight in predicted cities only.

Our side is not constructed symmetrically. It starts with team-wide city counts:

    scaled rOurPredictedCities = kOurTeam.getNumCities()
            - (int)militAnalyst().lostCities(eWe).size();
    scaled rOurCurrentCities = kOurTeam.getNumCities();

and then handles our vassals as follows:

    if (existing vassal)
        rOurCurrentCities += 0.5 * (cities - predicted losses);
    else if (new predicted capitulation)
        rOurPredictedCities += 0.5 * (cities - predicted losses);

An EXISTING vassal is therefore present in our current baseline but disappears
entirely from our predicted baseline. Even if the simulated war changes nothing
about that vassal, merely having the vassal can make our side appear to shrink
between present and future. This directly biases rTheirEdge upward and can create
or magnify PreEmptiveWar utility against the rival.

There is a related multi-member-team mismatch: the initial city base is the
whole team's kOurTeam.getNumCities(), but projected losses subtract only
lostCities(eWe) for the one player whose aspect is currently being evaluated.
Losses predicted for our living teammate therefore remain counted in the team
future-city numerator. Because WarEvaluator evaluates living team members
separately, this produces member-specific partial versions of what is otherwise
a team-level power forecast rather than one coherent future team city total.

Base AdvCiv 1.14 contains this present/predicted construction. The archaeology
shows the existing-vassal asymmetry is longstanding, not introduced by SAS. The
clean repair shape is to construct current and predicted "our side" from the same
participants and weights, applying predicted city changes only to the predicted
copy. Existing vassals should contribute to both; newly capitulating vassals only
to predicted. For multi-member teams, projected losses/gains should likewise be
aggregated consistently for the whole team if the base quantity is team-wide.


F108 / provisional KI#431 -- CONFIRMED -- INHERITED ADVCIV VARARGS UB
ULTERIORMOTIVES UWAI LOG FORMAT HAS THREE CONVERSIONS BUT ONLY TWO ARGUMENTS
-------------------------------------------------------------------------------
UlteriorMotives contains the following report call in a live UWAI logging path:

    log("Attitude level towards %s: %d, refusal thresh: %d",
            m_kReport.leaderName(eThey), towardThem());

The format string requires three values after the format string (%s, %d, %d),
but only two are supplied. CvString::formatv therefore reads a nonexistent third
vararg for the "refusal thresh" conversion when the report is enabled. This is
undefined behavior: the log can print arbitrary stack/register data and the call
has no valid C/C++ varargs contract.

A mechanical audit of every log(...) call in WarUtilityAspect.cpp found no other
format/argument-count mismatch, so this is a bounded defect rather than evidence
of a wider report-format family.

Base AdvCiv 1.14 has the same call. The direct repair is to pass the actual
DeclareWarRefuseAttitudeThreshold (or whatever threshold the report intended to
show) as the third argument, or remove the third conversion/text if the value is
not needed.


F109 / provisional KI#432 -- CONFIRMED -- INHERITED ADVCIV WRONG-OBJECT / WEIGHT
KINGMAKING VALUES A NEW RIVAL VASSAL FROM THE RIVAL'S ASSET POOL AND FULL WEIGHT
-------------------------------------------------------------------------------
KingMaking::theirRelativeLoss loops over the rival, its teammates, existing
vassals and teams predicted to capitulate to the rival. In the branch for a NEW
predicted vassal, eTheirAlly correctly identifies that future vassal:

    if (kCapitulationsAccepted.count(kTheirAllyTeam.getID()) > 0)
    {
        scaled rAssets;
        netLostRivalAssetScore(NO_PLAYER, &rAssets);
        scaled rTheirGain = rVassalFactor *
                remainingCityRatio(eTheirAlly) * rAssets;
        ...
    }

But netLostRivalAssetScore(NO_PLAYER, &rAssets) is a WarUtilityAspect helper
whose rival/victim is implicitly the current aspect opponent eThey. It does not
take eTheirAlly as the player whose assets should be measured. The branch thus
multiplies the CURRENT RIVAL player's asset pool by the NEW VASSAL's remaining-
city ratio. A small new vassal can inherit a large rival's asset value, or a large
new vassal can be valued from a small rival's asset pool.

The same branch also applies the wrong vassal weight. rVassalFactor is divided
by two only when kTheirAllyTeam.isAVassal() is true NOW. A team that is not yet a
vassal but is predicted by the military analysis to capitulate therefore enters
this future-vassal gain branch at full 1.0 weight. Nearby PreEmptiveWar provides
a strong internal policy control: it explicitly applies half weight when either
bNewVassal OR isAVassal() is true.

Both defects are present in base AdvCiv 1.14. The repair needs to obtain the
prospective vassal player's/team's own asset base and apply the intended vassal
factor to a predicted capitulation as well as an existing vassal.


F110 / provisional KI#433 -- CONFIRMED -- INHERITED ADVCIV PLAYER-LOCAL MODEL
KINGMAKING USES PLAYER QUANTITIES FOR TEAM-SHARED VICTORY STATE
-------------------------------------------------------------------------------
KingMaking stores likely winners as PlayerTypes and evaluates several victory
conditions with player-local quantities even though Civ4 victory ownership is
team-level. This produces at least two independently proven normal-game
manifestations in multi-member teams / Permanent Alliances.

1. SCORE / TIME VICTORY LEADERSHIP

The actual game score used for score/time victory is CvGame::getTeamScore. Civ4
constructs team score by summing the member player scores. KingMaking's
addLeadingPlayers instead ranks:

    m_kGame.getPlayerScore(ePlayer)

or, in the predicted case:

    militAnalyst().predictedGameScore(ePlayer)

The latter is also explicitly player-level: it begins from that player's game
score and stores a per-player simulation result.

A two-member team with player scores 600 + 600 therefore genuinely leads a solo
1000 team by 1200 to 1000, while KingMaking can identify the solo 1000 player as
the score leader and omit both 600-point members. The model can consequently
misidentify who is actually closest to winning a Time/score game.

2. PREDICTED DOMINATION SETBACK

CvPlayerAI's Domination victory-stage calculation derives the relevant progress
from TEAM population/land. KingMaking anyVictory therefore starts from a
team-derived AI_VICTORY_DOMINATION3/4 flag. When predicting whether military
losses thwart that victory, however, it computes:

    max(0, kPlayer.getTotalPopulation() - iLostPop)

and compares that INDIVIDUAL player's remaining population share against the
Domination target share, which is a team victory threshold.

A Permanent Alliance can remain above the Domination population threshold in
aggregate while each member individually is far below it. Losing a few cities
from one member can therefore make KingMaking falsely declare the team's
Domination trajectory thwarted even though the team's combined population still
satisfies it.

The shared root is not simply "replace PlayerTypes with TeamTypes everywhere":
KingMaking's culture/space/conquest heuristics have player-specific inputs, and
predictedGameScore also folds simulation details that require care when summed.
The repair should make each genuinely team-owned victory test team-aware, with a
proper predicted team-score/team-population estimator rather than blindly
summing helpers that may already contain vassal or simulation adjustments.


UNRESOLVED CHALLENGE AFTER F110
-------------------------------
KingMaking keeps m_winningPresent and m_winningFuture as std::set<PlayerTypes>
and later uses set size as the number of competing "winning civs", while also
checking eWe and kOurTeam.getLeaderID() in different places. This may cause
additional team-game double counting or self-recognition problems beyond the two
proven F110 manifestations. It remains under challenge; do not count it as F111
unless an independent behavioral consequence survives.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP03
LIVE FILE-014 ROOTS: F101-F110 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F111 UNUSED

Continue with the KingMaking PlayerTypes winner-set challenge, then complete the
remaining late-aspect / helper / player-vs-team / arithmetic-sign / state-
freshness audit. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP04 APPEND -- WarUtilityAspect.cpp continued adversarial / team audit
===============================================================================

F109 / provisional KI#432 -- BROADENED -- WRONG-OBJECT ASSET REUSE IS GENERAL
KINGMAKING THEIRRELATIVELOSS REVALUES eThey FOR TEAMMATES / VASSALS TOO
-------------------------------------------------------------------------------
The WIP03 wording proved the wrong-object bug in the NEW-vassal branch. A direct
reread shows that the same root is broader and more damaging than that wording.

KingMaking::theirRelativeLoss iterates eTheirAlly over all major players and
selects the current rival, its teammates, existing vassals and prospective new
vassals. But in the ordinary current-rival/team-member/existing-vassal branch it
again calls:

    scaled rAssets;
    scaled rLostAssets = netLostRivalAssetScore(
            NO_PLAYER, &rAssets, eIgnoreGains);

netLostRivalAssetScore has no eTheirAlly parameter. Its rival/victim is the
aspect's current eThey. Therefore every accepted loop entry reuses eThey's asset
pool and eThey's projected loss rather than measuring the actual loop player.

A two-player rival team A+B can consequently count A's assets/losses once for A
and then again for B. An existing vassal likewise contributes a half-weighted
copy of A rather than its own assets. The previously proven prospective-vassal
branch has the same wrong-object problem and additionally fails to apply the
future-vassal half weight because rVassalFactor is halved only for a team that
is already a vassal.

So F109 is best normalized as a general wrong-object construction in
KingMaking::theirRelativeLoss: the loop is over eTheirAlly, but asset/loss
queries remain pinned to eThey. The new-vassal weighting error is a coupled
second manifestation. Base AdvCiv 1.14 contains the same structure.

A repair should make the asset/loss helper capable of evaluating the actual
loop player (or factor the calculation into a helper that takes PlayerTypes),
and separately apply the intended vassal factor to both existing and predicted
vassals.


F110 / provisional KI#433 -- BROADENED -- PLAYER WINNER SETS DUPLICATE TEAM STATE
KINGMAKING REPRESENTS TEAM-SHARED VICTORY CANDIDACY AS set<PlayerTypes>
-------------------------------------------------------------------------------
The earlier two F110 manifestations remain valid: Score/Time leadership uses
player rather than team score, and predicted Domination setback uses individual
population against a team threshold. The winner-set challenge now proves that
the representation itself causes further multi-member-team distortions.

KingMaking stores likely present/future winners in:

    std::set<PlayerTypes> m_winningPresent;
    std::set<PlayerTypes> m_winningFuture;

but later treats set cardinality as the number of competing winning
civilizations/competitors:

    scaled rCatchUpVal = ... / m_winningPresent.size();
    int iWinningRivals = m_winningFuture.size();

and uses player membership tests such as:

    m_winningFuture.count(eThey)
    m_winningFuture.count(eWe)
    m_winningFuture.count(kOurTeam.getLeaderID())

For team-owned victory conditions, two members of one Permanent Alliance can
therefore occupy two set entries despite representing one winning team. This
can:

* inflate the number of alleged competing winners and lower the KingMaking
  competition/catch-up utility;
* allow essentially the same team-level winning state to be processed once per
  winning member as WarUtilityAspect evaluates player opponents;
* suppress preEvaluate's "we'll be the only winners" shortcut because our
  leader may be present while m_winningFuture.size() is 2 solely because our
  teammate is another member of the same winning team;
* make self-recognition depend on which member PlayerTypes happens to represent
  the team in a given helper.

These are not a new root beyond F110; they are additional consequences of the
same player-local KingMaking model for team-shared victory state. A robust
repair should distinguish truly player-owned race inputs from team-owned
victory candidacy and should count a winning team once.


F111 / provisional KI#434 -- CONFIRMED -- INHERITED ADVCIV WRONG-OBJECT TEST
DIPLOMACY/AP CONQUEST WEIGHT TESTS THE CONQUEROR'S FULL MEMBERSHIP, NOT OLD OWNER
-------------------------------------------------------------------------------
MilitaryVictory::progressRatingDiplomacy builds weighted conquests made by a
partner and then explicitly adjusts each conquered city based on its OLD owner:

    PlayerTypes const eCityOwner = kCacheCity.city().getOwner();
    // Weight for old owner
    if (!GET_TEAM(eCityOwner).isHuman())
    {
        /* AP: Conquests from non-full members are always worthwhile
           b/c, as full members, we can make their votes count twice. */
        if (bSecular || kThey.isFullMember(eVS))
        {
            AttitudeTypes const eOwnerTowardUs =
                    GET_PLAYER(eCityOwner).AI_getAttitude(eWe);
            ...
        }
    }

The comment, the eCityOwner local and the subsequent attitude lookup all refer
to the old owner. Yet the Apostolic-Palace membership branch tests kThey -- the
current rival/partner who conquered the city -- instead of eCityOwner.

That distinction is live precisely in the third-party-conquest case supported
by addConquestsByPartner: the conqueror and old owner need not have the same AP
membership status. Full membership changes the vote interpretation the comment
is trying to model, so the wrong-object test can either discount population that
should not be discounted or fail to discount population that already gave the
favorable/full-member vote contribution.

Base AdvCiv 1.14 has the same condition. For the non-secular AP branch, the test
should query the old owner, e.g. GET_PLAYER(eCityOwner).isFullMember(eVS), while
preserving kThey-based membership tests in the distinct code paths that really
do describe the conquering/new owner.


F112 / provisional KI#435 -- CONFIRMED -- INHERITED ADVCIV WRONG-TEAM DIVISOR
UWAI WAR-ANGER CACHE SPLITS AN ENEMY-TEAM CONTRIBUTION BY OUR TEAM SIZE
-------------------------------------------------------------------------------
UWAICache::updateWarAnger first attributes the owner's total war-weariness anger
to ENEMY TEAMS:

    aiAngerContrib[enemyTeam] = ourTeam.getWarWeariness(enemyTeam, true)

It then stores one cached anger value for each LIVING ENEMY PLAYER. The source
comment correctly states the required normalization:

    // Turn per-team into per-civ

but the division is:

    scaled(aiAngerContrib.get(itEnemy->getTeam()), iTotalAngerContribs) /
        GET_TEAM(kOwner.getTeam()).getNumMembers()

The contribution being split belongs to itEnemy's team, so dividing by the
OWNER team's member count is the wrong side of the relation. PublicOpposition
later consumes angerFromWarWeariness(eThey) once for each living enemy player,
and WarEvaluator separately evaluates every living agent member. Thus the wrong
denominator directly changes aggregate war utility rather than merely changing
how a report is displayed.

Concrete controls:

* solo owner vs a two-player enemy team: the same enemy-team contribution can be
  stored at full strength for both enemy players and then counted twice;
* two-player owner team vs solo enemy: each owner player's cached signal is
  divided by two even though there is only one enemy player representing that
  enemy-team contribution, so the aggregate signal can be halved;
* equal team sizes can accidentally hide the defect.

Because the consumer/player iterator is CIV_ALIVE, a dead assigned enemy member
should not dilute this split. The natural denominator is therefore the ENEMY
team's alive member count, not either side's getNumMembers(). This also avoids a
secondary F096 dead-member distortion.

Base AdvCiv 1.14 contains the identical wrong-owner-team denominator.


WIP04 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP04
LIVE FILE-014 ROOTS: F101-F112 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F113 UNUSED

Continue the remaining player-vs-team/pairwise audit, especially TacticalSituation,
FairPlay, late ThirdPartyIntervention/DramaticArc and shared helper contracts;
then run arithmetic/sign/state-freshness and adversarial ledger closure. No
repository source files were modified.
===============================================================================


===============================================================================
C014-WIP05 APPEND -- WarUtilityAspect.cpp wrong-ID / team-slot audit
===============================================================================

F113 / provisional KI#436 -- CONFIRMED -- BROAD INHERITED PLAYER/TEAM-ID MIXUP
WARUTILITYASPECT PASSES PlayerTypes DIRECTLY TO GET_TEAM; kTheirTeam MACRO IS WRONG
-------------------------------------------------------------------------------
WarUtilityAspect defines both a correct rival-team ID and a rival-team object:

    #define kThey           (getRivalPlayer())
    #define eThey           (kThey.getID())
    #define kTheirTeam      (GET_TEAM(eThey))
    #define eTheirTeam      (TEAMID(eThey))

The two last lines are NOT equivalent. eThey is PlayerTypes. GET_TEAM expects a
TeamTypes and indexes the global team array directly; TEAMID(eThey) instead asks
the player which team it currently belongs to.

There is no Civ4 invariant that PlayerTypes numerically equals the player's
TeamTypes. CvPlayer::setTeam explicitly changes the player's team association in
CvInitCore without changing the PlayerTypes ID, and the normal Permanent-
Alliance transition uses this mechanism. Configured team games and Shuffle
Teams can likewise assign player and team IDs independently.

Therefore, after player B joins survivor team A through a Permanent Alliance:

    eThey == B                    // still PlayerTypes B
    eTheirTeam == TEAMID(B) == A  // correct current team
    kTheirTeam == GET_TEAM(B)     // WRONG numeric team slot B

The old B team slot can by then have zero members and otherwise contain stale or
irrelevant team state. The macro is used throughout the file (43 source
occurrences including its definition), so this is not a single cosmetic lookup.
Representative affected consumers include:

* lossesFromFlippedTiles -- getNumMembers(); the obsolete team slot can contain
  zero members and become an actual divisor;
* tech/research state in Loathing-related asset logic;
* capitulation/vassal checks;
* AP/diplomatic membership decisions;
* HiredHand and Assistance team state;
* victory-stage and KingMaking checks;
* Risk war count / war success;
* IllWill, Affection and Distraction war/vassal state;
* FairPlay contact, war and shared-war state;
* ThirdPartyIntervention scheme/land-target and war-rand logic;
* DramaticArc vassal/team-size normalization.

The divisor manifestation is especially sharp. lossesFromFlippedTiles does:

    int const iRivalMembers = kTheirTeam.getNumMembers();
    ...
    r /= iRivalMembers;

If eThey is an absorbed PA member and its old numeric team slot has no members,
the same wrong-ID root can turn this into a divide-by-zero path when flipped
losses are evaluated.

The file also contains explicit non-macro instances of the same type error,
where locals that are demonstrably PlayerTypes are passed directly to GET_TEAM:

    GET_TEAM(eCityOwner)      // progressRatingDiplomacy / addConquestsByPartner
    GET_TEAM(ePartner)        // Fidelity
    GET_TEAM(eAlly)           // HiredHand
    GET_TEAM(eTheirAlly)      // KingMaking::theirRelativeLoss
    GET_TEAM(eVassal)         // Risk
    GET_TEAM(eTheirVassal)    // Affection

These can independently read the wrong team in ordinary configured team games
or after team reassignment. Some overlap already-proven semantic findings --
e.g. F111's old-owner AP block and F109's KingMaking loop -- but the ID-domain
error is broader and survives even after those wrong-object formulas are fixed.

Base AdvCiv 1.14 contains the same kTheirTeam/eTheirTeam macro pair and the same
style of direct PlayerTypes-to-GET_TEAM calls, so F113 is inherited rather than
introduced by current SAS. Reachable pre-SAS source history also contains the
pattern, making this a longstanding team-game/PA oversight.

Repair direction:

* define the rival object through the actual team association, e.g.

      #define kTheirTeam (GET_TEAM(eTheirTeam))

  or GET_TEAM(kThey.getTeam());
* replace every explicit GET_TEAM(ePlayerVariable) whose variable is
  PlayerTypes with GET_TEAM(TEAMID(ePlayerVariable)) / GET_PLAYER(...).getTeam()
  as appropriate;
* retain direct GET_TEAM calls only where the argument is genuinely TeamTypes;
* after repair, re-run the multi-member/PA normalization cases because F113 can
  currently mask or exaggerate several other UWAI findings.


WIP05 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP05
LIVE FILE-014 ROOTS: F101-F113 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F114 UNUSED

Continue the remaining wrong-object/team semantics and individual aspect audit,
then arithmetic/sign/state-freshness and adversarial ledger closure. No
repository source files were modified.
===============================================================================


===============================================================================
C014-WIP06 APPEND -- F113 cross-file UWAI type-domain expansion
===============================================================================

F113 / provisional KI#436 -- BROADENED -- UWAI-WIDE PLAYER/TEAM-ID LOOKUP FAMILY
WARUTILITYASPECT IS ONE MANIFESTATION; UWAICACHE AND UWAI::PLAYER SHARE THE ROOT
-------------------------------------------------------------------------------
The WIP05 root is substantially broader than WarUtilityAspect.cpp. A directed
cross-file type audit proves that the same PlayerTypes -> GET_TEAM misuse occurs
inside the per-player UWAI cache and per-player UWAI agent.

The crucial type boundary is explicit in the headers:

    UWAICache::m_eOwner       : PlayerTypes
    UWAI::Player::m_eAgent    : PlayerTypes
    UWAI::Team::m_eAgent      : TeamTypes

Therefore a textual grep for GET_TEAM(m_eAgent) cannot be fixed blindly: uses
inside UWAI::Team are type-correct, while uses inside UWAI::Player are not.
Likewise, GET_TEAM(m_eOwner) in UWAICache is wrong because m_eOwner is always a
player ID.

REACHABILITY IS NORMAL, NOT PA-ONLY
-----------------------------------
UWAI::Player::init(PlayerTypes ePlayer) does:

    m_eAgent = ePlayer;
    m_cache.init(ePlayer);

and UWAI::Player::turnPre() calls m_cache.update() every turn. Thus every player
has a player-owned cache; these aren't leader-only cache objects. CvPlayer::
setTeam independently rewrites GC.getInitCore().setTeam(getID(), eTeam) without
changing the player's ID. Consequently an ordinary configured multi-player team
already supplies a normal PlayerTypes != TeamTypes case for any member whose
player slot differs from the shared team slot; Permanent Alliances are merely a
particularly clear later-game way to create the same mismatch.

UWAICACHE GAMEPLAY MANIFESTATIONS
---------------------------------
UWAICache.cpp repeatedly indexes GET_TEAM directly with m_eOwner. Representative
live uses include:

* createTeamPathFinders / updateCities -- pathfinders and own/foreign-city
  classification are built from GET_TEAM(m_eOwner);
* goldPerProdVictory -- owner victory stage and rival iteration use that team;
* updateWarUtility / updateWarUtilityIgnDistraction -- leader gating and the
  team whose war state is evaluated are selected from that slot;
* leaderCache -- deciding whether this cache is the leader cache, and which
  player's cache to redirect to, both use GET_TEAM(m_eOwner);
* feature-tech, tech-trade, bonus-obsolescence/reveal checks -- use the wrong
  team's technology state;
* reportWarEnding -- owner war success, chosen-war state and at-war duration use
  the wrong team;
* reportCityCreated -- AI_deduceCitySite uses the wrong team;
* is/setReadyToCapitulate and onTeamLeaderChanged -- leader identity/routing are
  resolved through the wrong team slot.

This means a nonleader teammate's cache can be populated using another numeric
team slot's pathing, technology, war-history and leader-cache state. After a PA,
the wrong slot can instead be the absorbed player's old now-empty/dead team.

UWAI::PLAYER GAMEPLAY MANIFESTATIONS
------------------------------------
After UWAI::Player::Player() establishes m_eAgent as PlayerTypes, numerous
player-level methods likewise call GET_TEAM(m_eAgent) directly. Confirmed live
examples include:

* considerDemand -- reparationsToHuman is taken from numeric team slot m_eAgent;
* considerPlea -- force-peace, canDeclareWar and sneak-attack-ready state;
* willTalk / isPeaceDealPossible -- surrender, team-leader and peace-reluctance
  state;
* canTradeAssets -- AI_techTradeVal;
* tradeValUtilityConversionRate -- team yield-rate estimates;
* confidenceFromWarSuccess -- current-war counters and war success;
* warConfidenceAllies -- whether the player belongs to a multi-member team.

Several of these functions are called through GET_PLAYER(...).uwai() for the
individual player (e.g. CvPlayerAI offer/will-talk logic and InvasionGraph), so
the wrong lookups are not confined to a leader-only path.

INITIALIZATION SIBLING
----------------------
UWAI::initNewPlayerInGame(PlayerTypes eNewPlayer) currently does:

    GET_TEAM(eNewPlayer).uwai().init(TEAMID(eNewPlayer));
    GET_PLAYER(eNewPlayer).uwai().init(eNewPlayer);

The first line selects the team object by PlayerTypes but initializes that
object with the player's actual TeamTypes. If a dynamically created player's
IDs differ, this can initialize the wrong CvTeamAI::uwai() object as another
team. This is the same ID-domain defect at a state-transition boundary; the
ordinary per-player cache manifestations above do not depend on this dynamic-
player case and already prove the root's normal reachability.

ANCESTRY / CONTROL
------------------
Base AdvCiv 1.14 contains the same UWAICache GET_TEAM(m_eOwner) calls, the same
post-UWAI::Player-constructor GET_TEAM(m_eAgent) calls and the same
initNewPlayerInGame line. The family is inherited.

The important negative control is UWAI::Team. Its m_eAgent is declared
TeamTypes; direct GET_TEAM(m_eAgent) calls in that class are correct and must not
be mechanically replaced as though they were part of F113.

REPAIR/AUDIT SHAPE
------------------
Treat F113 as a typed audit, not a search-and-replace:

* WarUtilityAspect PlayerTypes locals/macros: resolve through TEAMID(player);
* UWAICache::m_eOwner: resolve team through TEAMID(m_eOwner) or
  GET_PLAYER(m_eOwner).getTeam();
* UWAI::Player::m_eAgent: same player-to-team conversion;
* UWAI::initNewPlayerInGame: select GET_TEAM(TEAMID(eNewPlayer));
* UWAI::Team::m_eAgent: leave direct GET_TEAM(m_eAgent) intact;
* audit other UWAI files by declared enum type, because both enum domains are
  integer-compatible at the call site and therefore compile silently.

This expansion does not consume F114; it is the same F113/KI#436 invariant and
explains why some team-game/PA symptoms can appear outside WarUtilityAspect.


WIP06 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP06
LIVE FILE-014 ROOTS: F101-F113 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F114 UNUSED

Resume the remaining WarUtilityAspect semantic/formula audit assuming the F113
ID-domain family is repaired; ThirdPartyIntervention's nonlinear player-power
threshold remains under challenge, not yet promoted. No repository source files
were modified.
===============================================================================


===============================================================================
C014-WIP07 APPEND -- F114 + F113 tail + adversarial controls
===============================================================================

F113 / provisional KI#436 -- ADDITIONAL EXPLICIT WARUTILITYASPECT SITE
ILLWILL::EVALANGEREDPARTNERS PASSES A PLAYERSET MEMBER DIRECTLY TO GET_TEAM
-------------------------------------------------------------------------------
The continuing typed sweep found another explicit same-root manifestation not
listed in WIP05/WIP06. IllWill::evalAngeredPartners obtains:

    PlyrSet const& kWeDecl = militAnalyst().getWarsDeclaredBy(eWe);

and iterates that set with PlyrSetIter. Therefore *it is PlayerTypes. It then
executes:

    if (GET_TEAM(*it).isAVassal() || eTheirTeam == TEAMID(*it))
        continue;

The TEAMID use on the second half is type-correct; the direct GET_TEAM(*it) on
the first half is not. In a configured team game or after reassignment, the
vassal filter can inspect the numeric team slot matching the target player's ID
rather than that player's actual team. This belongs under the already-broadened
F113 typed-audit family and consumes no new finding ID.

A useful control from the same file is GreedForVassals: its apparent
GET_TEAM(*it) use occurs under TeamSetIter, so *it is genuinely TeamTypes and
that call is correct. This reinforces that F113 repair must follow declared enum
types rather than textual patterns.


F114 / provisional KI#437 -- CONFIRMED -- FAIRPLAY COUNTS TARGET TEAMMATES AS
"OTHER ENEMIES" / "ALLIES" OF THE TARGET AND DILUTES THE DOGPILE PENALTY
-------------------------------------------------------------------------------
FairPlay::evaluate contains an early block whose stated purpose is:

    // Avoid big dogpiles (or taking turns attacking a single target)
    scaled rOtherEnemies; // Apart from us

It then loops:

    for (PlayerAIIter<FREE_MAJOR_CIV,KNOWN_POTENTIAL_ENEMY_OF>
            itOther(eOurTeam); ...)

and excludes only:

    if (kOther.getID() == eThey ||
        !kTheirTeam.isHasMet(kOther.getTeam()))
        continue;

This is player-local exclusion where the loop's semantic domain is third
parties relative to the TARGET SIDE. A second living member of eTheirTeam is a
known potential enemy of eOurTeam and has a different PlayerTypes ID from
eThey, so it passes the filter even though it can never be an independent enemy
of its own team.

Two concrete consequences survive independently of F113:

1. TARGET TEAMMATES INFLATE THE DENOMINATOR

The code increments iPotentialOtherEnemies immediately after the filter. Later:

    rFromOtherEnemies = ... /
        (sqrt(max(1, iPotentialOtherEnemies)) - 1/3);

Thus each teammate of the current target player is counted as another potential
third-party enemy, increasing the denominator and reducing the fairness cost of
dogpiling that team.

2. A TARGET TEAMMATE CAN BE CLASSIFIED AS AN "ALLY OF eThey"

If the teammate is not counted as an enemy, FairPlay evaluates:

    else if (kTheirTeam.AI_shareWar(kOther.getTeam()))

For a target teammate, kOther.getTeam() == eTheirTeam. CvTeamAI::AI_shareWar
loops the team's current enemies and returns true when one of them is at war
with the supplied team. Therefore AI_shareWar(ownTeam) is true whenever the
team has any war enemy. FairPlay can consequently log the target's own teammate
as:

    "An ally of eThey"

and subtract from rOtherEnemies. This does more than dilute the denominator: it
can directly reduce the perceived dogpile pressure merely because the target
has teammates.

The declared loop role gives a strong repair invariant. Independent third
parties must exclude the entire current target team, not just eThey. A nearby
internal control already implements that distinction correctly:
IllWill::nukeCost skips

    kThirdPlayer.getTeam() == eTheirTeam

when iterating independent third players.

Base AdvCiv 1.14 contains the same FairPlay filter and shared-war branch, so the
defect is inherited rather than SAS-specific.

Minimal repair shape:

    if (kOther.getTeam() == eTheirTeam ||
        !kTheirTeam.isHasMet(kOther.getTeam()))
        continue;

This naturally excludes eThey and every teammate before either
 iPotentialOtherEnemies or enemy/ally history is touched. Re-audit behavior
with F113's kTheirTeam lookup repaired, but F114 itself is independent of F113.


REJECTED / CONTROLLED LATE HYPOTHESES
-------------------------------------
AFFECTION ONE-SIDED TEAM NORMALIZATION -- REJECTED

Affection says pairwise affection needs a correction in team games, but applies
it only when kOurTeam.getNumMembers() > 1. At first glance a solo agent versus a
two-member target looks asymmetric. The WarEvaluator outer loop and the history
of the correction explain the asymmetry, however: a solo player can legitimately
carry separate pairwise friendship costs toward two distinct rival players,
whereas a multi-member AGENT team would otherwise duplicate the same team-war
decision once for every agent member. The pre-SAS commit that introduced the
normalization used the agent-team-size gate from inception. This is therefore
not strong enough for F115.

THIRDPARTYINTERVENTION ALL-ZERO DEFENSIVE-POWER DIVISOR -- REJECTED

preEvaluate computes:

    m_rLostDefPowRatio = rOurLostPow /
        max(m_rDefPow, m_rDefPow + rGainedPow);

without an explicit epsilon. When current, gained and lost defensive power are
all zero, ScaledNum ultimately reaches Win32 MulDiv with divisor zero. For the
signed fixed-point type used here, MulDiv returns its -1 error sentinel; the
immediately following

    m_rLostDefPowRatio.clamp(0, 0.5);

raises that tiny negative value to zero, and evaluate exits on
rOurLostPowRatio <= 0. Later ThirdParty power divisions require a path where
positive current or gained power has already supplied a denominator. So this is
untidy arithmetic but not a demonstrated live gameplay root.

THIRDPARTYINTERVENTION COLLECTIVE TEAM POWER -- STILL UNDER CHALLENGE

The nonlinear <=50% power cutoff is still computed from the current rival
player plus vassals, not rival teammates, despite team-level intervention
eligibility. Two individually sub-threshold teammates can therefore both yield
zero even if their collective team power would be dangerous. However,
WarEvaluator deliberately aggregates player-local contributions in several
team-game aspects, and the later team-size normalization complicates the
intended policy. Keep this candidate open; do not promote it without a harder
source/consumer invariant.


WIP07 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP07
LIVE FILE-014 ROOTS: F101-F114 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F115 UNUSED

Continue the remaining Risk/Effort/shared-helper and late-aspect
wrong-object/team-model/arithmetic/state-freshness pass under the assumption
that F113 is repaired. ThirdPartyIntervention collective-power semantics remain
under challenge. Do not declare C015 until the remaining formulas and an
adversarial F101-F114 reread genuinely close. No repository source files were
modified.
===============================================================================


===============================================================================
C014-WIP08 APPEND -- F115 + late-aspect controls / next DramaticArc challenge
===============================================================================

F115 / provisional KI#438 -- CONFIRMED -- THIRDPARTYINTERVENTION COMPUTES A
GAME-SPEED-ADJUSTED PEACE DURATION, THEN DISCARDs IT AND DECAYS FROM RAW TURNS
-------------------------------------------------------------------------------
ThirdPartyIntervention::evaluate contains a long-peace anti-stalemate decay:

    int iAtPeaceTurns = kOurTeam.AI_getAtPeaceCounter(eTheirTeam);
    int iThresh = (5 * PEACE_TREATY_LENGTH) / 3 - 1;
    if (iAtPeaceTurns > iThresh)
    {
        scaled rAtPeaceTurns = fixp(1.5) * iAtPeaceTurns /
            (per100(m_kSpeed.getGoldenAgePercent()) + fixp(0.5));
        scaled rMult = std::max(fixp(1/3.),
            1 - scaled(iAtPeaceTurns - iThresh, 30));
        ...
        rInterventionProb *= rMult;
    }

The local rAtPeaceTurns is never read after being computed. The actual gate and
linear decay both continue to use the raw iAtPeaceTurns.

This is unusually strong evidence of an accidental missing use rather than
merely an alternative heuristic:

1. THE UNUSED EXPRESSION HAS A CLEAR GAME-SPEED NORMALIZATION ROLE

The factor

    1.5 / (GoldenAgePercent / 100 + 0.5)

is exactly 1 on Normal speed (GoldenAgePercent=100), so the omission is hidden
there. It changes systematically on every other shipped SAS game speed:

    Nitro       55
    Turbo       60
    Quick       80
    Normal     100
    Epic       125
    Marathon   200
    Slow       300
    Very Slow  400

Thus the dead local is not redundant arithmetic. It computes a distinct
normalized peace duration everywhere except Normal.

2. THE DEAD LOCAL WAS PRESENT FROM THE ORIGINAL ASPECT INTRODUCTION

The pre-SAS commit that introduced ThirdPartyIntervention
(f39e13df87, "Add ThirdPartyIntervention war utility aspect", 2021-11-19)
already contains the exact sequence: compute rAtPeaceTurns, then ignore it and
use iAtPeaceTurns in the multiplier. Base AdvCiv 1.14 still contains the same
code. This is therefore an inherited construction error, not a SAS refactor
regression or an abandoned variable left by a later rewrite.

3. THE CURRENT RESULT IS SPEED-DEPENDENT IN THE WRONG UNIT

With the default PEACE_TREATY_LENGTH=10, iThresh is 15. At 31 RAW peace turns,
current code gives approximately:

    1 - (31 - 15) / 30 = 0.467

before the 1/3 floor -- on every game speed.

On Very Slow, however, the expression that the code itself computes gives:

    normalized peace turns ~= 1.5 * 31 / (4 + 0.5) ~= 10.3

so the normalized peace duration has not even reached the intended 15-turn
threshold yet. Current code has already cut intervention fear by more than half.
On faster-than-Normal modes the error reverses: the raw-turn schedule decays
more slowly than the prepared normalized schedule.

The semantic purpose is explicit in the surrounding comment:

    // Avoid stalemates: don't be afraid of interventions forever

That policy can reasonably scale with game speed, but the source already
contains the attempted scaling and then fails to consume it.

REPAIR BOUNDARY

Do not merely substitute rAtPeaceTurns into the existing multiplier while
leaving the raw-turn threshold unchanged. On slow speeds that could enter the
raw iAtPeaceTurns > iThresh block while normalized peace duration is still
below iThresh, producing a multiplier greater than 1 before clamping logic.
The coherent repair is to compute the normalized duration first and use the
same normalized unit for BOTH:

- the comparison to the long-peace threshold; and
- the excess duration used by the decay.

The exact scaling convention should be reviewed against current SAS turn-scaling
policy, but the current compute-and-discard implementation is demonstrably
self-contradictory.


ADDITIONAL REJECTED / CONTROLLED HYPOTHESES SINCE WIP07
--------------------------------------------------------
TACTICALSITUATION PER-RIVAL-MEMBER EVALUATION -- NOT PROMOTED

A repeated team-game execution initially looked like a possible duplicate-team
charge. Direct tracing shows TacticalSituation::evalEngagement is substantially
player-local on both sides: the current eWe member's military/cities are judged
against the current eThey player's military/cities, and WarEvaluator
intentionally evaluates living agent members separately. That does not furnish
the hard team-level invariant available for F112/F114. Keep the existing code
under ordinary heuristic scrutiny, but do not promote this repetition by
itself.

ASPECT-OBJECT STATE ACROSS AGENT MEMBERS -- NO SECOND F101 ROOT FOUND

WarUtilityAspect objects deliberately retain cumulative m_iU while evaluating
multiple living members. Their extra mutable state was checked for stale-member
leakage. The relevant fields are either overwritten per member (e.g.
MilitaryVictory, ThirdPartyIntervention, DramaticArc) or deliberately cumulative
with a safe uniqueness rule (e.g. Revolts::m_countedCities prevents a city from
being charged through multiple primary-area relations). No independent stale
member-state defect survived.

RISK / EFFORT LATE ARITHMETIC -- NO NEW ROOT SO FAR

The dense loss/power/vassal pass mostly produced further F113 typed-lookup
manifestations. Once those are conceptually repaired, the examined ratios have
positive structural domains or explicit clamps. No independent divisor/sign
root survived this pass.

THIRDPARTYINTERVENTION COLLECTIVE TEAM POWER -- STILL UNDER CHALLENGE

The earlier nonlinear <=50% military threshold remains suspicious because it
uses the current rival player's army plus vassals rather than that rival team's
teammates, even though intervention eligibility is team-level. Two individually
40%-strength teammates can each be thresholded to zero despite 80% collective
team power. However, WarEvaluator intentionally aggregates player-local terms in
some team-game calculations. F115 is independent of this question. Do not
promote the collective-power hypothesis without a stronger intended-aggregation
invariant.


NEXT LIVE CHALLENGE -- DRAMATICARC HASMET / TURNSATPEACE TOLERANCE
------------------------------------------------------------------
DramaticArc currently does roughly:

    int iAtPeace = getTurnsAtPeace(eSecond);
    // With tolerance b/c the has-met counter isn't exact
    if (abs(AI_getHasMetCounter(eSecond) - iAtPeace) >= 8)
        ...
    // else assume always at peace

The comment/history claim that HasMet is inexact conflicts with CvTeamAI's
counter implementation, which deliberately does NOT randomize HasMet (nor
AtWar) because callers assume an exact count. Historical archaeology shows that
this mistaken rationale was already present when DramaticArc was amended to use
exact TurnsAtPeace.

That comment error is not yet enough for F116. Need finish the lifecycle proof:
trace makeHasMet / war / peace reset and increment ordering, quantify the normal
difference after a short early war, and decide whether the 8-turn tolerance has
a defensible policy role (e.g. intentionally ignoring very short early wars) or
causes a demonstrably contradictory DramaticArc classification. Keep UNDER
CHALLENGE.


WIP08 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP08
LIVE FILE-014 ROOTS: F101-F115 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F116 UNUSED

Resume with the DramaticArc exact-counter lifecycle challenge, then return to
ThirdPartyIntervention collective-team-power only if a hard aggregation
invariant can be established. Continue remaining wrong-object/nonlinear-
threshold/shared-helper and adversarial F101-F115 closure before deciding C015.
No repository source files were modified.
===============================================================================

===============================================================================
C014-WIP09 APPEND -- F116 DramaticArc exact-counter lifecycle correction
===============================================================================

F116 / provisional KI#439 -- CONFIRMED -- DRAMATICARC'S 8-TURN "INEXACT HASMET"
TOLERANCE CAN MISCLASSIFY A RECENT SHORT WAR AS "ALWAYS AT PEACE"
-------------------------------------------------------------------------------
DramaticArc::preEvaluate currently tries to distinguish pairs that have always
been peaceful from pairs whose exact TurnsAtPeace counter was reset by war:

    int iAtPeace = itFirst->getTurnsAtPeace(eSecond);
    // With tolerance b/c the has-met counter isn't exact
    if (abs(itFirst->AI_getHasMetCounter(eSecond) - iAtPeace) >= 8)
        iLoopMinCounter = std::min(iLoopMinCounter, iAtPeace);
    // (else assume that they've always been at peace)

The stated premise is false. CvTeamAI::AI_doCounter explicitly documents the
opposite:

    // No randomization for atWar and hasMet. These are used by the AI in
    // several places that more or less assume an exact count.

HasMet is incremented deterministically once per alive-team turn after contact.
CvTeam's exact TurnsAtPeace is likewise incremented once per alive-team turn
whenever the pair is not at war, and declaration of war resets BOTH directions
to zero.

Thus the 8-turn tolerance is not compensating for randomized counter noise.
It can erase real, recent warfare from DramaticArc's global-tension history.

NORMAL REPRODUCTION

Take two teams that have not previously met, after DramaticArc's early-game
25-normalized-turn gate:

1. They meet through a declaration of war. The declaration resets exact
   TurnsAtPeace to 0 while HasMet begins its exact contact history.
2. They remain at war for e.g. 3 counted team turns.
3. They make peace and remain peaceful for e.g. 1-2 counted turns.

At that point, approximately:

    HasMet        = 4-5
    TurnsAtPeace  = 1-2
    difference    = 3   (< 8)

Current code therefore takes the ELSE path and "assume[s] that they've always
been at peace." iLoopMinCounter remains initialized to iElapsedTurns, so a
peace that began only one or two turns ago can be represented as roughly the
whole elapsed game age.

This directly contradicts DramaticArc's source/history purpose: to encourage
war when there "hasn't been any warfare for a long time" and discourage it when
there is too much. A real war that ended almost immediately before evaluation
must not be reclassified as no prior warfare merely because it lasted fewer
than eight turns.

COUNTER LIFECYCLE GIVES A CLEANER EXACT TEST

The two exact counters have a useful directional relation:

- TurnsAtPeace starts accumulating from game start even before two teams meet.
- HasMet starts accumulating only after first contact.
- In a never-at-war relationship, therefore, HasMet <= TurnsAtPeace.
- A declaration of war resets TurnsAtPeace to 0 but does not reset HasMet.
- After at least one counted war turn, HasMet > TurnsAtPeace; once peace resumes,
  both counters increment together, preserving that strict inequality.

So, assuming the separate PA counter-merge defect F098 is repaired, a much more
source-faithful discriminator is conceptually:

    if (AI_getHasMetCounter(eSecond) > iAtPeace)
        // Actual counted warfare has occurred: use exact iAtPeace.
    else
        // No counted war since contact: treating the pair as always peaceful is
        // consistent with the counter history.

A declaration and peace occurring entirely within one counter tick can leave
both counters equal; treating such a zero-count-duration war as negligible is a
small transition edge, not justification for an arbitrary eight-turn band.

HISTORY / ANCESTRY

The chronology makes the mistaken premise unusually clear:

- dc322128c7 (2022-02-15), "Keep exact count of turns at peace", introduced the
  exact CvTeam TurnsAtPeace state.
- 03f2a56482, shortly afterward, introduced DramaticArc using the RANDOMIZED AI
  AtPeace counter and a comment that "these counters aren't exact."
- 17652fd75b (2022-02-16), "Minor amendment to 'Dramatic Arc' commit", switched
  DramaticArc from AI_getAtPeaceCounter to exact getTurnsAtPeace but retained a
  tolerance because it claimed "the has-met counter isn't exact," reducing the
  threshold only from 10 to 8.
- The corresponding commit message states the same mistaken rationale: "since
  the has-met count is also randomized..." Yet CvTeamAI deliberately keeps
  HasMet non-randomized because callers assume an exact count.

Base AdvCiv 1.14 retains this exact code and rationale. F116 is therefore an
inherited logic defect, not a SAS regression.

REPAIR BOUNDARY

Remove the fictitious random-noise tolerance. Prefer a predicate based on the
actual exact-counter lifecycle (e.g. HasMet > TurnsAtPeace means counted war
history exists) or another explicit persisted war-history predicate if one is
chosen. Do not merely lower the threshold: any positive arbitrary tolerance can
still turn a genuine short recent war into "always at peace."

Keep F098 in mind when implementing/testing: Permanent-Alliance merging can
currently make exact HasMet/TurnsAtPeace histories asymmetric. F116 is
independent and should be evaluated with F098 conceptually repaired.


CONTROLS / STATUS SINCE WIP08
-----------------------------
THIRDPARTYINTERVENTION COLLECTIVE TEAM POWER -- STILL NOT PROMOTED

The nonlinear <=50% intervention-power threshold remains suspicious for
multi-member potential-intervening teams because it measures current rival
player + vassals, not rival teammates, before later member normalization. The
architecture intentionally aggregates player-local terms elsewhere, however,
and no hard source-level aggregation invariant has yet been established. Keep
under challenge; do not assign F117 from intuition alone.


WIP09 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP09
LIVE FILE-014 ROOTS: F101-F116 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F117 UNUSED

Resume remaining late-aspect/shared-helper wrong-object, team-vs-player,
nonlinear-threshold and sign/state-freshness review. Return to ThirdParty
collective teammate power only if a hard intended-aggregation invariant can be
established. Then adversarially challenge F101-F116 before deciding C015.
No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP10 APPEND -- F117 PublicOpposition scenario-state bug + F113 expansion
===============================================================================

F117 / provisional KI#440 -- CONFIRMED -- PUBLICOPPOSITION MIXES HYPOTHETICAL
NEW-WAR RELIGION ANGER WITH CURRENT-STATE ANGRY POPULATION
-------------------------------------------------------------------------------
PublicOpposition::evaluate intentionally runs against the MilitaryAnalyst's
simulated war state:

    if (kWe.isAnarchy() || !militAnalyst().isWar(eWe, eThey) || ...)
        return;

For every city it then estimates the religious anger attributable to THIS
simulated enemy:

    scaled rAngry = pCity->angryPopulation(...);
    rFaithAnger += std::min(rAngry,
        pCity->getReligionPercentAnger(eThey) * pCity->getPopulation() /
        GC.getPERCENT_ANGER_DIVISOR());

The two operands of that min are not from the same world state.

- CvCity::getReligionPercentAnger(PlayerTypes ePlayer) calculates the potential
  anger contributed by that specified player's state religion. It can therefore
  be used for a prospective enemy that we are not currently fighting.
- CvCity::angryPopulation(), however, is a CURRENT-city-state quantity.
  CvCity::unhappyLevel feeds it the no-argument getReligionPercentAnger(), whose
  implementation loops only PlayerIter<CIV_ALIVE,ENEMY_OF> -- actual current
  enemies. A merely simulated future enemy is absent from that total.

Thus PublicOpposition projects a hypothetical anger source, then caps it by a
consequence count that does not yet include that hypothetical source.

NORMAL NEW-WAR REPRODUCTION

Suppose a currently peaceful/content city has:

    current angryPopulation = 0

but a prospective target has a state religion present strongly enough in that
city that war with that target would create, say, 3 religiously angry citizens.
MilitaryAnalyst's war scenario makes isWar(eWe,eThey) true, and the explicit
getReligionPercentAnger(eThey) call correctly computes that prospective anger.
Current code then does conceptually:

    min(currentAngry = 0, hypotheticalFaithAnger = 3) = 0

So the aspect can assign ZERO religious public-opposition cost to a new war that
would itself create unhappy citizens.

SECOND MANIFESTATION -- REUSING THE SAME CURRENT ANGRY CITIZENS

WarEvaluator evaluates rival players separately. For existing/multiple enemies,
each rival's religious anger is independently capped by the same current
angryPopulation. A city with 5 actual angry citizens and two enemy-specific
religion contributions each large enough for 5 can therefore contribute 5+5
across two rival evaluations even though there are only 5 angry citizens total.
The same current unhappy population can be attributed repeatedly to different
wars instead of computing a scenario-level/marginal anger change.

This is not F112. F112 concerns how cached WAR-WEARINESS anger is split from a
team quantity into per-enemy-player contributions. F117 concerns religion anger
mixing a SIMULATED source with a CURRENT-state cap and independently reusing
that cap across rivals.

ANCESTRY / INTENT

Base AdvCiv 1.14 contains the same code. The initial PublicOpposition
implementation in reachable AdvCiv history already used the same

    min(current angryPopulation, getReligionPercentAnger(theyId) * pop / divisor)

construction. Later tuning did not repair the state mismatch.

The source itself labels the neighboring war-weariness input as a known issue
for new wars (cached WW is zero when not currently at war). By contrast, the
explicit per-player religion overload is plainly being used to estimate the
prospective-war contribution; capping it by current angryPopulation defeats
that projection in exactly the happy-city case above.

REPAIR BOUNDARY

Do not simply delete the cap without thinking through city happiness. The
quantity wanted is the MARGINAL/SCENARIO public opposition caused by the
simulated war set. A robust repair should evaluate total city unhappiness with
scenario religion-war anger (or reconstruct the relevant marginal angry
population) and apportion that finite result once. In particular:

- prospective religion anger must be allowed to CREATE angry citizens that are
  absent from current angryPopulation; and
- the same finite set of angry citizens must not be independently credited to
  multiple enemy players.

If a cheaper pairwise approximation is retained, use a shared/marginal cap
against the scenario total rather than current angryPopulation independently
for every rival.


F113 / provisional KI#436 -- MATERIAL EXPANSION -- THE PLAYER-ID-AS-TEAM-ID
FAMILY REACHES THE CORE MILITARY SIMULATION, NOT ONLY ASPECT/CACHE HELPERS
-------------------------------------------------------------------------------
WIP05-WIP07 established the same root in WarUtilityAspect, UWAICache,
UWAI::Player and initialization: a PlayerTypes value is used directly where a
TeamTypes identity/index is required. Further tracing now proves that the root
also penetrates MilitaryAnalyst and InvasionGraph -- the machinery that builds
almost every WarUtilityAspect scenario.

MILITARYANALYST STARTS FROM THE WRONG TEAM FOR A NONLEADER TEAM MEMBER

MilitaryAnalyst stores:

    PlayerTypes m_eWe;

but its constructor begins with:

    CvTeamAI const& kAgent = GET_TEAM(m_eWe);

WarEvaluator deliberately constructs one MilitaryAnalyst for EACH living agent
player. For an ordinary teammate or PA-absorbed player whose player slot differs
from the shared team slot, this selects the team whose NUMERIC TEAM ID equals
that player ID, not the player's actual TEAMID(m_eWe).

The same constructor repeatedly performs GET_TEAM(ePlayer) while ePlayer comes
from PlayerIter, and even uses GET_TEAM(*it1) where *it1 comes from PlyrSet.
These calls determine master/vassal sides, war allies, extra targets and whether
simulated declarations of war are already active. Thus F113 can corrupt the
scenario itself before the aspect formulas run.

NON-GET_TEAM MANIFESTATION: TEAM ARRAY WRITTEN WITH PLAYER INDEX

MilitaryAnalyst declares:

    std::vector<TeamSet> m_capitulationsAcceptedPerTeam;

and its public accessor takes TeamTypes:

    getCapitulationsAccepted(TeamTypes eMaster)

but prepareResults writes each Node's team-level capitulation output to:

    m_capitulationsAcceptedPerTeam[ePlayer]

where ePlayer is PlayerTypes. Elsewhere the same information is read via
TEAMID(ePlayer). When player ID != team ID, the result is stored under one
numeric slot and queried under another. This is the same semantic F113 root even
though GET_TEAM is not involved syntactically: player identity has been used as
a team-array index.

INVASIONGRAPH HAS THE SAME TYPE-DOMAIN ASSUMPTION IN LIVE SIMULATION LOGIC

InvasionGraph::Node explicitly stores BOTH:

    PlayerTypes m_ePlayer;
    PlayerTypes m_eAgent;

Yet live logic contains calls such as:

    GET_TEAM(m_ePlayer).isAtWar(...)
    GET_TEAM(m_eAgent).isAtWar(...)
    CvTeamAI const& kAttTeam = GET_TEAM(kAttacker.m_ePlayer);
    CvTeamAI const& kDefTeam = GET_TEAM(m_ePlayer);

and InvasionGraph::willingness(PlayerTypes eAggressor, PlayerTypes eTarget)
uses GET_TEAM(eAggressor) and GET_TEAM(eTarget) directly.

These affect target selection, current-war detection, master/vassal logic,
capitulation eligibility, sneak-attack classification, willingness and other
simulation state -- not merely log output.

This also supplied an important negative control for a BorderDisputes candidate:
InvasionGraph explicitly states that newly conquered cities cannot be attacked
by third parties or reconquered in the simulation, so per-player conquest sets
are mutually exclusive for a city; BorderDisputes' first matching conqueror is
not order-dependent under this model.

ANCESTRY / IMPLEMENTATION GUIDANCE

Base AdvCiv contains these same MilitaryAnalyst/InvasionGraph patterns. The F113
repair audit should therefore be SYSTEMATIC across the UWAI subsystem, not just
replace the WarUtilityAspect `kTheirTeam` macro.

Use actual declared/derived types:

- PlayerTypes p -> TEAMID(p), GET_PLAYER(p).getTeam(), then GET_TEAM(team)
- TeamTypes t   -> GET_TEAM(t) directly

Audit array/vector indices whose semantic domain is team as well as GET_TEAM
calls. Do not blind-replace every `GET_TEAM(variable)`: e.g. the UWAI::Team
agent member really is TeamTypes and several TeamSet iterators are already
correct. The bug is a TYPE/DOMAIN mismatch, not a macro-name problem.


CONTROLS / OPEN QUESTIONS SINCE WIP09
-------------------------------------
- AI_countVSNonMembers(VoteSourceTypes) counts living MAJOR PLAYERS. Therefore
  MilitaryVictory's per-player AP obstacle numerator and that denominator are
  in compatible units; no new root there.
- BorderDisputes' first-conqueror break is safe under InvasionGraph's explicit
  no-recapture/no-third-party-recapture simulation model.
- Effort's 15-raw-turn grace threshold followed by game-speed-scaled escalation
  is unit-inconsistent-looking, but both pieces were introduced together and
  there is no hard intent contradiction. Keep as design-review only unless
  stronger evidence appears; do not promote F118 from aesthetics.
- ThirdPartyIntervention collective teammate power remains under challenge;
  the <=50% cutoff is player-local before aggregation, but a hard intended team
  aggregation invariant is still required.


WIP10 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP10
LIVE FILE-014 ROOTS: F101-F117 (former F101/KI#423 remains retracted history)
NEXT ROOT ID: F118 UNUSED

Continue remaining late-aspect/shared-helper wrong-object, current-vs-simulated,
nonlinear-threshold, sign/divisor and team-model audit under F113 conceptually
repaired. Keep extending F113 rather than duplicating the same type-domain root.
Then adversarially challenge F101-F117 before deciding C015.
No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP11 CONTINUATION -- AUTHORITATIVE F113 RETRACTION + F118/F119
===============================================================================

IMPORTANT CORRECTION -- F113 / provisional KI#436 RETRACTED
------------------------------------------------------------
DO NOT IMPLEMENT KI#436 FROM WIP05-WIP10.

The broad PlayerTypes-as-TeamTypes GET_TEAM diagnosis was a false positive.
WarUtilityAspect.cpp includes CoreAI.h, which deliberately replaces GET_TEAM
with an overloaded accessor:

    __forceinline CvTeamAI& getTeam(TeamTypes eTeam)
    { return CvTeamAI::AI_getTeam(eTeam); }

    __forceinline CvTeamAI& getTeam(PlayerTypes ePlayer)
    { return CvTeamAI::AI_getTeam(TEAMID(ePlayer)); }

CvGamePlay.h provides the same overload pattern for non-AI CvTeam access.
Therefore expressions such as GET_TEAM(eThey), GET_TEAM(m_eOwner),
GET_TEAM(m_eAgent), GET_TEAM(eRival), GET_TEAM(eCityOwner), and the analogous
InvasionGraph/MilitaryAnalyst calls are type-resolved correctly whenever the
argument's declared type is PlayerTypes. They do NOT index the numeric team slot
whose integer happens to equal the player ID.

This mechanism is inherited in Base AdvCiv 1.14 and is intentional advc.003u.
Current SAS archaeology had independently reached the same conclusion for the
older KI#311 candidate: suspected PlayerTypes-as-TeamTypes GET_TEAM misuse was
rejected specifically because GET_TEAM(PlayerTypes) resolves through TEAMID.
That existing control should have been consulted earlier.

Consequently, all WIP05-WIP10 text describing the WarUtilityAspect kTheirTeam
macro, UWAICache GET_TEAM(m_eOwner), UWAI::Player GET_TEAM(m_eAgent),
initNewPlayerInGame GET_TEAM(eNewPlayer), MilitaryAnalyst GET_TEAM(m_eWe),
InvasionGraph GET_TEAM(m_ePlayer/m_eAgent), AI_paranoiaRating GET_TEAM(eRival),
and similar overload-backed calls as wrong-team lookups is RETRACTED.

Preserve those historical sections only so the same false hypothesis is not
rediscovered. F113 is no longer a live file-014 root. The provisional KI#436
number remains historical/retracted; later KI numbering is NOT renumbered.

ADVERSARIAL IMPACT CHECK
------------------------
Later findings survive this correction independently:
- F111/KI#434: the AP bug is the use of kThey.isFullMember(eVS) when the code is
  explicitly weighting the OLD OWNER eCityOwner. GET_TEAM(eCityOwner) itself is
  safe and is removed from the proof.
- F114 FairPlay target-teammate leakage, F115 discarded game-speed-normalized
  peace counter, F116 false HasMet-randomness tolerance, F117 hypothetical
  religion anger capped by current angryPopulation, and F118 negative revolt
  loss all have proofs independent of GET_TEAM overload semantics.
- F101-F112 likewise retain their independent aggregation/arithmetic/object/
  state proofs. No other live finding currently requires F113 to remain true.

F104 / provisional KI#427 BROADENED -- WRONG RESOURCE-COUNT UNIT TOO
-------------------------------------------------------------------
The existing off-by-one remains: comment and constant say first FOUR resource
trades, but the counter is incremented and then tested with >= 4, so the fourth
resource-deal is already skipped.

A second manifestation is now source-proven. partnerUtilFromTrade scans every
TradeData item in a deal but collapses resources to one boolean:

    bool bWeReceiveResource = false;
    ...
    if (pItem->m_eItemType == TRADE_RESOURCES)
        bWeReceiveResource = true;
    ...
    if (bWeReceiveResource)
        iResourceTrades++;

Thus iResourceTrades counts DEALS containing at least one resource, not resource
TRADE ITEMS. AdvCiv's AI counterproposal code can repeatedly insert multiple
TRADE_RESOURCES items into one CLinkList deal; CvDeal then processes and saves
each resource item separately. One bundled deal containing several resources
therefore consumes only one of the nominal four-resource slots and can let more
than four individual resource trades through.

Normalize F104 as: partnerUtilFromTrade's intended first-four-resource cap uses
both the wrong boundary and the wrong counting unit. Repair by counting actual
TRADE_RESOURCES items and accepting exactly the first four items, independent
of how they are bundled into deals. Preserve the existing per-deal AI_dealVal
semantics carefully when implementing; this finding is about the cap/count,
not an instruction to value a bundled deal multiple times naively.

F118 / provisional KI#441 -- REVOLTS CAN TURN NEGATIVE ASSET LOSS INTO WAR GAIN
--------------------------------------------------------------------------------
CONFIRMED, inherited Base AdvCiv.

Revolts::evaluate uses each threatened city's UWAICache::City::getAssetScore()
directly in the expected revolt-loss numerator:

    int const iCityAssets = pCacheCity->getAssetScore();
    iTotalAssets += iCityAssets;
    ...
    rLossesFromRevolts += iCityAssets * rLossMult;
    ...
    rRevoltCost = ... * rLossesFromRevolts / iTotalAssets;
    m_iU -= rRevoltCost.round();

Per-city asset score can be negative because CvPlayerAI::AI_assetVal subtracts
city/civic/other-city maintenance. This very file documents that domain in
Risk::evaluate:

    // Per-city score can be negative (maintenance)

and explicitly raises aggregated LOST assets to at least zero there. Revolts
has no corresponding protection.

Reachable sign failure: a valuable safe city keeps iTotalAssets positive, while
a revolt-prone high-maintenance city has negative asset score. Its contribution
makes rLossesFromRevolts negative; if the net expected revolt loss stays below
zero, rRevoltCost is negative and `m_iU -= rRevoltCost` ADDS positive war
utility. War is then rewarded for increasing revolt risk in our own city.

That is not a coherent 'good to shed maintenance' model. CvCity::doRevolt can
damage garrison units, impose occupation and, in non-flip revolts, later kill
population; the owner does not simply discard the maintenance liability when a
revolt occurs. The original Revolts implementation and Base AdvCiv retain the
same unguarded multiplication.

Repair guidance: revolt-threat value must not become negative. Prefer handling
negative per-city asset scores explicitly (e.g. nonnegative threatened-value
input) so a liability neither creates positive war utility nor cancels genuine
revolt exposure in other cities. A final aggregate clamp prevents the sign
inversion but can still let negative cities offset positive threatened value,
so it is the weaker repair.

F119 / provisional KI#442 -- TEAM-INDEXED CAPITULATION RESULTS WRITTEN BY PLAYER ID
------------------------------------------------------------------------------------
CONFIRMED, inherited Base AdvCiv. This is the REAL raw PlayerTypes/TeamTypes
index defect that had previously been hidden inside false F113.

MilitaryAnalyst declares:

    std::vector<TeamSet> m_capitulationsAcceptedPerTeam;

sizes it to MAX_CIV_TEAMS, and exposes:

    getCapitulationsAccepted(TeamTypes eMaster)
        -> m_capitulationsAcceptedPerTeam[eMaster]

All normal consumers therefore address this table by TEAM identity. However,
prepareResults loops PlayerTypes ePlayer and writes:

    pNode->getCapitulationsAccepted(
        m_capitulationsAcceptedPerTeam[ePlayer]);

There is no overloaded accessor here: the enum value is used directly as a
vector index.

InvasionGraph::Node::setCapitulated makes the intended semantics especially
clear. When a team accepts a capitulation, it inserts the capitulated team into
m_capitulationsAccepted of EVERY live member node of the master team. During
result preparation those duplicate member-node team results should be unioned
into the master's TEAM slot. Instead current code scatters them into the member
PLAYER slots.

Concrete configured-team reproduction:
- P0 belongs to Team1; P1 belongs to Team0 (or any mapping where player and team
  IDs differ).
- A capitulation accepted by Team1 is present in P0's node.
- prepareResults stores it in vector slot 0.
- consumers ask getCapitulationsAccepted(Team1), i.e. slot 1.
The result can therefore be absent from the actual master and, when another
team uses the numerically matching slot, appear under the wrong team.

A multi-member team whose member IDs do not include its team ID can leave its
true slot empty entirely. Even when one member ID happens to match, other member
writes can pollute unrelated team-number slots.

Base AdvCiv 1.14 has the identical line. Archaeology shows the older original
form already used `capitulationsAcceptedPerTeam[civId]`, so the defect is
inherited rather than SAS-created.

Surgical repair:

    pNode->getCapitulationsAccepted(
        m_capitulationsAcceptedPerTeam[TEAMID(ePlayer)]);

getCapitulationsAccepted(TeamSet&) inserts/merges into the destination set, so
multiple member nodes naturally union into the one proper master-team slot.
Audit other raw team-indexed arrays separately; unlike GET_TEAM(PlayerTypes),
raw operator[] has no type-aware conversion layer.

THIRDPARTY COLLECTIVE-POWER CANDIDATE -- STRONGER, STILL NOT PROMOTED HERE
-------------------------------------------------------------------------
The earlier team-power concern gained a hard internal inconsistency after the
GET_TEAM correction. ThirdPartyIntervention's probability path calls
AI_paranoiaRating(eThey,...). Its rival-team accessor is SAFE through the
PlayerTypes overload and AI_paranoiaRating then uses actual team power, team war
plans, team worst-enemy state, etc. Later ThirdPartyIntervention constructs
rTheirPow from only kThey's player-local ARMY cache plus vassals and applies the
nonlinear <=50% early return before any member aggregation.

Because a DoW by one member commits the full team, this may discard an already
team-level intervention probability merely because the currently evaluated
member alone is weak (e.g. two 40% teammates individually fail although their
team fields 80%). This is now a strong F120 candidate, but retain it under
challenge until the intended member-aggregation/team-size normalization is
fully reconciled with the original ThirdParty design.

WIP11 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP11
LIVE FILE-014 ROOTS: F101-F112, F114-F119
RETRACTED: former F101/KI#423; F113/KI#436
NEXT ROOT ID: F120 UNUSED

Continue ThirdParty collective-power aggregation challenge, remaining late
helper/aspect semantic/sign/state audit, then adversarially re-challenge all
live file-014 roots under the corrected GET_TEAM overload model before deciding
C015. No repository source files were modified.
===============================================================================

LOCAL RECONCILIATION AFTER C014-WIP11 IMPORT
--------------------------------------------
- ChatGPT-5.6-Sol's green-only C014-WIP02 through C014-WIP11 append continues the open `WarUtilityAspect.cpp` audit from an earlier light-source handoff. Current local authority is practical 6261 / HEAD 58563781c3 after the independently reviewed KI#400-KI#405 and KI#424-KI#426 repair commits; current source and reviewed Known Issues remain authoritative.
- F101-F103 / KI#424-KI#426 are already fixed, documented, compiled and exercised by ordinary and multi-member-team autoplays in current source. Their incoming audit text remains useful discovery evidence rather than a statement that those defects are still pending.
- WIP11 correctly retracts F113 / provisional KI#436 after tracing the `GET_TEAM(PlayerTypes)` overload through `TEAMID`, matching the existing KI#311/KI#387 false-positive controls. Preserve WIP05-WIP10's superseded hypothesis only as evidence against rediscovery; do not implement it.
- F096 / KI#418 remains pending and is credibly broadened by the new living-participant normalizers in `WarUtilityAspect.cpp`; no separate root was created for those manifestations.
- F104-F112 and F114-F119 / provisional KI#427-KI#435 and KI#437-KI#442 are new and collision-free against the current reviewed Known Issues ledger. They remain provisional because file 014 is still open and WIP11 explicitly requires a corrected-model adversarial pass before C015.
- The durable C013 queue remains the preferred independent repair source while C014 is open. C014 resumes from WIP11 with F120 unused. (GPT-5.6-Sol)

FOLLOW-UP REJECTION OF C013 F084-F085
-------------------------------------
- F084 / KI#406 and F085 / KI#407 correctly describe consequences inside `CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_TECH, ..., false)`, but their claimed shipped reproduction is unreachable. `CvTechChooser.py` exposes only `AddTechButton`, queries only `getAdvancedStartTechCost(..., true)` and sends only `bAdd=true`; Advanced Start AI also buys but never removes technologies. No other repository caller sends a technology-removal action.
- The prepared DLL changes were therefore withdrawn and KI#406-KI#407 were retained as rejected findings. Re-audit both together if a future modmod exposes Advanced Start technology refunds through a supported interface. (GPT-5.6-Sol)
- F083 / KI#405 remains valid independently: `CvGame::regenerateMap` removes all existing team technologies when True Starts is enabled before `setInitialItems` rebuilds them. The cache must decrement on that reachable false transition even though the Advanced Start Tech Chooser does not expose refunds. Its code comment and KI reproduction were corrected accordingly. (GPT-5.6-Sol)
- F088 / KI#410 is rejected with the same caller evidence as F084-F085. Its incomplete dependency checks matter only if a custom caller sends the dormant technology-removal action; no shipped human or AI path does so. (GPT-5.6-Sol)


===============================================================================
C014-WIP12 CONTINUATION -- RECONCILED CURRENT SOURCE + F120/F121
===============================================================================

RECONCILIATION AGAINST CURRENT SAS HEAD 403929fe27 / PRACTICAL 6263
-----------------------------------------------------------------
This continuation was performed from the fresh AdvCiv-SAS light-source snapshot
20260825T195953 rather than from the older WIP11 source copy.

Current source and the locally reconciled album are authoritative. In particular:
- F101-F103 / KI#424-KI#426 are already fixed, documented, compiled and autoplay-
  exercised in current source. Their earlier audit entries remain discovery and
  ancestry evidence rather than pending implementation instructions.
- F113 / provisional KI#436 remains authoritatively RETRACTED; GET_TEAM has the
  intended PlayerTypes overload through TEAMID.
- F104-F112 and F114-F119 remain provisional/pending in the reconciled album at
  this cursor; none of their source regions had been changed by the current HEAD.
- The current working tree also contains later CvTeam reconciliation/fix work,
  but no uncommitted WarUtilityAspect/MilitaryAnalyst/UWAICache edits that would
  invalidate the checks below.

F120 / provisional KI#443 -- FAIRPLAY NONZERO-START FIX DOUBLE-COUNTS LATER-ERA START POSITION
----------------------------------------------------------------------------------------------
CONFIRMED against current SAS and inherited Base AdvCiv.

FairPlay's early-human protection says:

    // All bets off by turn 100 ...
    int iTargetTurn = 100 + m_kGame.getStartTurn();
    ...
    int const iElapsed = (m_kGame.getElapsedGameTurns() / rTrainMod).uround();
    int iTurnsRemaining = iTargetTurn - iElapsed
            - GC.getInfo(eStartEra).getStartPercent();

The `+ getStartTurn()` term is NOT arbitrary dead code. Reachable AdvCiv history
contains a targeted 2021 fix for FairPlay when start turn > 0. The intent was to
avoid prematurely expiring the early-game protection in games whose game-turn
counter starts above zero.

The problem is that CvGame::setStartTurnYear proves getStartTurn contains the
ORDINARY LATER-ERA BASELINE too:

    iStartTurn = sum(game-speed turn increments);
    iStartTurn *= StartPercent(startEra);
    iStartTurn /= 100;

FairPlay then separately subtracts that same era's StartPercent. Thus the old
fix conflates two different quantities:
1. an exceptional/nonstandard extra start-turn offset that may deserve
   compensation; and
2. the normal calendar/game-progress baseline caused merely by selecting a
   later starting era, which FairPlay already tries to shorten through
   StartPercent.

CURRENT SAS NUMERIC CONTROL

Current CIV4GameSpeedInfo totals and CIV4EraInfos give on NORMAL:

    total scheduled turns = 526
    Industrial StartPercent = 50 -> baseline startTurn = 263
    Modern     StartPercent = 65 -> baseline startTurn = 341 (integer floor)
    Future     StartPercent = 80 -> baseline startTurn = 420 (integer floor)

At elapsed turn 0 and neutral map-density factor, current FairPlay therefore
starts with approximately:

    Industrial: 100 + 263 - 50 = 313 normalized fair-play turns remaining
    Modern:     100 + 341 - 65 = 376
    Future:     100 + 420 - 80 = 440

Yet the entire normal-speed game remaining after those baselines is only about:

    Industrial: 526 - 263 = 263 actual turns
    Modern:     526 - 341 = 185
    Future:     526 - 420 = 106

The supposed early-game protection can therefore remain positive for the entire
remaining later-era game even before considering map-density scaling. Slower
speeds make the absolute mismatch still larger because startTurn scales with
full game length while the separately subtracted StartPercent is only 0..100.

This is stronger than merely saying two coordinate systems look mixed: current
XML plus CvGame initialization gives a deterministic shipped reproduction.

ANCESTRY / REPAIR BOUNDARY

The historical nonzero-start fix was sensible for the case it targeted, so do
NOT simply delete all start-turn compensation. Repair should distinguish the
EXTRA start-turn component from the normal starting-era baseline, and compare
all terms in one normalized elapsed-time unit. One conceptual baseline is:

    normalEraStartTurn = totalScheduledTurns * StartPercent / 100
    extraStartOffset   = max(0, getStartTurn() - normalEraStartTurn)

then normalize only the extra component consistently with rTrainMod (or replace
the whole grace calculation with a direct normalized elapsed-since-start gate).
Preserve the existing crowded-map and era-advance modifiers after the timebase
is made coherent.


F121 / provisional KI#444 -- "LEAVE THE START ERA" USED AS A TEMPORARY STARTUP CLOCK CAN NEVER EXPIRE IN TERMINAL-ERA STARTS
---------------------------------------------------------------------------------------------------------------------------
CONFIRMED, inherited Base AdvCiv; two independent UWAI manifestations.

ROOT

Several UWAI systems use

    currentEra <= startEra

as a proxy for "we are still too early after game start to trust this signal."
That is a plausible settling heuristic when there is guaranteed to be a later
era transition. It is not a bounded time condition. If the game starts in the
last defined era, no later era exists and the temporary guard becomes permanent.

MANIFESTATION A -- KINGMAKING PERMANENTLY DISABLED

KingMaking::preEvaluate says:

    // Scoreboard ranks just aren't meaningful off the bat, even if the game
    // starts in the Modern era. Leaving m_winningPresent empty causes 0 utility.
    if (m_eGameEra <= m_kGame.getStartEra())
        return 0;

The comment explicitly describes a temporary "off the bat" grace period. Current
SAS's final defined era is ERA_FUTURE (StartPercent 80). A Future-era start has
no era after Future, so currentEra can never become greater than startEra.
KingMaking therefore remains disabled for the entire game -- including late
score/time/victory-stage situations the aspect exists specifically to police.

MANIFESTATION B -- LONG-TERM THREAT / PREEMPTIVEWAR PERMANENTLY ZERO

UWAICache::calculateThreatRating has the same proxy:

    // Can't reliably estimate yield rates in the early game
    if (GC.getGame().getCurrentEra() <= GC.getGame().getStartEra())
        return 0;

Threat ratings are refreshed from this function. PreEmptiveWar then begins from
ourCache().threatRating(eThey) and immediately returns when it is below 0.15.
Thus a terminal-era start permanently reports zero long-term threat and disables
PreEmptiveWar as well.

REACHABILITY / CONTROL

SAS defines ERA_FUTURE as an ordinary EraInfo and uses it throughout start-era
building/tech logic; there is no later EraInfo to advance into. Even apart from
the normal custom-game era selector, scenarios can set InitCore era directly.
The root therefore does not require a hypothetical future era or XML modmod.

The neighboring DramaticArc startup gate is a strong control: it uses normalized
elapsed turns (`elapsed * speedMult < 25`) and therefore expires even when the
game begins in the terminal era. That is the correct kind of bounded startup
clock for an "off the bat" concern.

REPAIR BOUNDARY

Do not special-case only ERA_FUTURE if avoidable. Replace these temporary
"must leave the start era" gates with a bounded normalized elapsed-time/game-
progress grace, optionally retaining an era-advance shortcut if desired. That
preserves the intent for Ancient/Modern starts and remains well-defined at the
terminal era.


CONTROLS / OPEN QUESTIONS AT WIP12
----------------------------------
- Revolts::m_countedCities does not leak between separate war evaluations:
  WarEvaluator constructs and destroys a fresh aspect set per war/peace
  scenario. The set intentionally persists only across member/rival passes in
  one scenario. No new root.
- ThirdPartyIntervention's player-local <=50% power gate remains under challenge.
  Its intervention probability is partly team-derived, but the original aspect
  was designed as pairwise member utility with later team-size normalization.
  No F122 without a harder invariant.
- FairPlay F120 is specifically a partial historical start-turn fix that
  overcompensates the normal later-era baseline; it is NOT a claim that all use
  of getStartTurn is wrong.

WIP12 CURRENT CURSOR
--------------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP12
CURRENT SOURCE BASE: practical 6263 / HEAD 403929fe27
ALREADY FIXED IN CURRENT SOURCE: F101-F103 / KI#424-KI#426
LIVE/PROVISIONAL FILE-014 ROOTS: F104-F112, F114-F121
RETRACTED: former F101/KI#423; F113/KI#436
NEXT ROOT ID: F122 UNUSED

Continue remaining helper/aspect parameter/wrong-object/sign/state/nonlinear-team
checks against CURRENT source, then adversarially re-challenge every still-live
file-014 root (especially shipped reachability and intended approximation) before
deciding whether C015 is earned. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP13 CONTINUATION -- F122 DURABLE + F096 LOATHING BROADENING
===============================================================================
STATUS: WIP / append-only continuation of authoritative reconciled C014-WIP12.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
SOURCE SNAPSHOT: AdvCiv-SAS practical 6263 / HEAD 403929fe273f0bce131fc6bfd2ea15d57f688e6c.
NEXT UNUSED ROOT AFTER THIS CHECKPOINT: F123.
NO REPOSITORY SOURCE FILES MODIFIED.

RECONCILIATION / AUTHORITY
--------------------------
- All 783,033 bytes of the supplied C014-WIP12 are preserved as this file's exact
  byte-for-byte prefix. The current source and independently reviewed Known Issues
  remain authoritative over older provisional wording.
- F113 / provisional KI#436 remains RETRACTED. CoreAI/CvGamePlay deliberately
  overload GET_TEAM(PlayerTypes) through TEAMID; do not reintroduce that diagnosis.
- Former F101 / provisional KI#423 remains RETRACTED.
- F101-F103 / KI#424-KI#426 are already fixed in the supplied source snapshot and
  remain historical discovery evidence in the cumulative album.
- F120/F121 remain the latest physically durable roots from WIP12. This checkpoint
  freezes the post-WIP12 F122 proof before continuing at F123.

F096 / KI#418 -- ADDITIONAL WARUTILITYASPECT MANIFESTATION -- LOATHING MIXES
LIVING-PLAYER COUNTS WITH ASSIGNED TEAM MEMBER COUNTS
-------------------------------------------------------------------------------
This remains the existing dead-roster semantic root, not a new F-number.

Loathing::evaluate() computes the number of relevant free civilization players
from living-player iteration:

    int iFreeCivPlayers = countFreeRivals<false>();

and its joint-war ally loop likewise uses PlayerIter<FREE_MAJOR_CIV,...>, i.e.
living free major civilizations. If our team is not a vassal, however, it then
adds:

    iFreeCivPlayers += kOurTeam.getNumMembers();

and finally says:

    // Count Loathing only once per team member
    ... / (iFreeCivDivisor * kOurTeam.getNumMembers())

WarEvaluator's outer contribution loop is MemberIter and therefore evaluates only
living members of the agent team. An eliminated-but-still-assigned teammate thus
has two independent effects despite contributing no Loathing evaluation of its
own: it increases the supposed number of free civilization players before the
sqrt divisor is computed, and it increases the final team-member divisor.

This gives Loathing a particularly clean F096 manifestation because both nearby
quantities are explicitly framed in PLAYER units and the actual evaluator consumes
living players. getAliveCount()/alive-player counting is the coherent denominator
here. This is additional evidence for the WIP02 F096 broadening; it does not imply
that every getNumMembers() in UWAI is automatically wrong.


F122 / provisional KI#445 -- TACTICALSITUATION CAN REUSE THE SAME AGENT UNIT
AGAINST MULTIPLE MEMBERS OF ONE ENEMY TEAM
-------------------------------------------------------------------------------
CONFIRMED against current SAS and inherited Base AdvCiv 1.14.

ROOT / EXACT LOCAL INVARIANT

TacticalSituation::evalEngagement() builds `weThreaten` from the current agent
player's groups. When it inspects threatened plots, it counts only units whose
combat owner is the CURRENT rival player eThey. It then explicitly enforces:

    // Count at most one enemy unit per unit of ours as "entangled",
    // i.e. count pairs of units.
    iTheirUnits = std::min(iTheirUnits, iOurUnits);

That cap is correct only inside one rival-player pass. WarUtilityAspect::evaluate
iterates every living `KNOWN_POTENTIAL_ENEMY_OF` player in the military analysis
and invokes the same TacticalSituation aspect once for each rival. Therefore the
agent-side `weThreaten` inventory and its per-plot iOurUnits cap are rebuilt and
reused independently for every living member of the same hostile team.

DETERMINISTIC TEAM-PARTITION REPRODUCTION

Let player A have one relevant unit able to attack a visible plot. The hostile
team has players B and C, with one unit from each on that same plot.

B pass:
    iOurUnits = 1
    B-owned units on plot = 1
    entangled = min(1, 1) = 1

C pass:
    the same A unit is rebuilt into `weThreaten`
    iOurUnits = 1
    C-owned units on plot = 1
    entangled = min(1, 1) = 1

Across the two rival-player evaluations, the single A unit has therefore supplied
two "pairs" despite the source-authored invariant saying to count at most one
enemy unit per unit of ours.

If the identical two hostile units are instead both owned by B, the one B pass is:

    iOurUnits = 1
    B-owned units = 2
    entangled = min(2, 1) = 1

Thus merely repartitioning an otherwise identical hostile team force between
teammate ownership changes TacticalSituation utility. Team warfare itself has not
changed, yet the nonlinear one-to-one cap is reset at the player boundary.

RELATED MANIFESTATION

The same structural boundary affects `iOurExposed`. For each current rival player,
TacticalSituation asks:

    kWe.AI_getPlotDanger(..., eThey)

and can count the same damaged agent unit as exposed independently to B and then C.
The entanglement invariant is the stronger exact proof, so F122 is normalized
around agent-side tactical quantities being independently consumable by several
members of one enemy team; it does not assert that every per-player Tactical term
must be team-aggregated.

ANCESTRY / NEGATIVE CONTROL

Base AdvCiv 1.14 contains the same player-filtered tactical code. Its history also
contains commit ac91311251477c426ac9d2230a9603b9d93a73b4, titled:

    Fix UWAI bug with counting of entangled units

The surviving multi-member-team case crosses the per-rival evaluation boundary
that the local cap cannot see. This historical concern about double-counting
strengthens the interpretation of the explicit current comment rather than merely
suggesting a different tuning preference.

REPAIR DIRECTION

Do not merely divide final TacticalSituation utility by enemy-team size: a linear
normalizer cannot restore a one-to-one relationship after the nonlinear per-player
cap has already been applied. Safer directions are to aggregate relevant hostile
units/danger across eTheirTeam before applying the caps, or to maintain shared
per-enemy-team tactical accounting so one agent unit cannot be consumed again by
another teammate pass. Preserve genuinely player-specific terms where ownership
matters.


NEGATIVE CONTROLS RETAINED BEFORE F123
--------------------------------------
- The broad claim "TacticalSituation should never run once per rival player" is
  NOT promoted. Several subterms are intentionally player-specific. F122 is the
  narrower cross-pass reuse defect proved by the one-pair invariant.
- ThirdPartyIntervention's player-local power cutoff remains under challenge; its
  pairwise architecture and later team-member normalization still leave intent
  ambiguous enough that it is not assigned a root here.
- The prior GreedForVassals zero-city/capital-null idea remains unpromoted under
  current SAS game rules; normal established zero-city majors are eliminated.
- Revolts' m_countedCities lifetime remains clean because aspect objects are fresh
  per war/peace scenario and persist only across the intended member/rival passes.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP13
LIVE NEW ROOT ADDED HERE: F122 / provisional KI#445
NEXT UNUSED ROOT: F123

Continue with Bellicosity's team-partition challenge first, then re-challenge the
ThirdPartyIntervention collective-power cutoff and the remaining late
aspect/helper sign/state/team semantics. Re-run the corrected GET_TEAM false-
positive control before any player/team-looking candidate is promoted.
===============================================================================

===============================================================================
C014-WIP14 APPEND -- F123 Bellicosity + F124 ThirdParty team-partition findings
===============================================================================

AUTHORITY / CONTINUATION
------------------------
This append continues directly from the physically verified C014-WIP13 built
from the user's reconciled practical-6263 / HEAD 403929fe273f0bce131fc6bfd2ea15d57f688e6c
snapshot. F123 and F124 were re-proved against that current SAS source and Base
AdvCiv 1.14 before promotion. The prior GET_TEAM(PlayerTypes) false-positive
family F113/KI#436 remains retracted and is not reused here.


F123 / provisional KI#446 -- BELLICOSITY'S ANTI-THIRD-PARTY LOSS CAP IS RESET
FOR EACH MEMBER OF ONE ENEMY TEAM
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv defect / team-partition nonlinearity.

CURRENT CODE / INTENDED SAFEGUARD

Bellicosity::evaluate() is invoked separately for each rival player. For every
relevant military branch it computes:

    rOurLostPow = militAnalyst().lostPower(eWe, eBranch);
    rOurMinusTheirLostPow +=
        min(2.35 * rOurLostPow,
            militAnalyst().lostPower(eThey, eBranch))
        - 0.75 * rOurLostPow;

The surrounding comment says that, if the rival loses much more than we do,
"chances are that a third party is causing this." Historical ancestry makes the
purpose even less ambiguous: AdvCiv commit ed1826faf199f4a18442c9196f181a46797e79c1
explicitly records:

    Fixed an issue with war utility from "bellicosity" counting war successes
    of allies.

That commit introduced the current min(2.35 * ourLost, theirLost) cap in place
of the former direct enemy-loss subtraction. Base AdvCiv 1.14 and current SAS
retain the same implementation.

ROOT

MilitaryAnalyst::lostPower(PlayerTypes, branch) is player-local. The eWe loss
therefore has one fixed value for the current agent member, while the generic
WarUtilityAspect rival loop calls Bellicosity once for every living rival player.
Against a multi-member hostile team, the same agent loss budget is consequently
reused once per teammate and the nonlinear anti-third-party cap is restarted at
each player boundary.

DETERMINISTIC PARTITION REPRODUCTION

Take one branch, current aggressive power 100 and agent loss 10.

If a one-player enemy side loses 20:

    min(23.5, 20) - 7.5 = 12.5

If an otherwise identical two-player enemy team has the same total 20 losses
split 10 + 10:

    B pass: min(23.5, 10) - 7.5 = 2.5
    C pass: min(23.5, 10) - 7.5 = 2.5
    total = 5

The war-side loss balance is identical, but merely repartitioning ownership
between teammates cuts the Bellicosity signal from 12.5 to 5.

The nonlinearity can bend the other way too. With total enemy loss 40:

    one player: min(23.5, 40) - 7.5 = 16
    20 + 20:    12.5 + 12.5 = 25

So this is not a missing constant team-size divisor. The result depends on where
the min cap is applied. The later per-rival glory-rate calculation can introduce
additional nonlinear saturation through decreaseTo(1).

WHY THE TEAM BOUNDARY MATTERS HERE

Bellicosity's own source describes a "good war" as one "that we win." The 2018
repair is specifically trying not to credit enemy losses caused by allies or
third parties. In a Civ4 team war, B and C are one war side; resetting the same
A-loss comparison separately for B and C defeats that safeguard. Player-local
loss storage is fine, but the side-level comparison cannot be reconstructed by
applying min/subtraction independently and summing afterward.

REPAIR DIRECTION

Aggregate the relevant lost power of living members on eTheirTeam (and whatever
other side membership the intended model explicitly wants) before applying the
anti-third-party cap against eWe's loss once for that hostile team. Ensure the
team is charged only once in this aspect. Preserve player-specific reachability
or personality conditions deliberately rather than blindly replacing every
PlayerTypes quantity with a team quantity.

Do not repair by dividing the final utility by team size: linear normalization
cannot undo the partition-sensitive min and later saturation.


F124 / provisional KI#447 -- THIRDPARTYINTERVENTION USES FULL RIVAL-TEAM POWER
TO ESTIMATE INTERVENTION PROBABILITY, THEN HARD-GATES DANGER ON ONE MEMBER'S ARMY
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team-partition defect. This is independent of F115's
game-speed peace-decay bug.

THE INTERNAL UNIT SWITCH

For a prospective third party eThey, ThirdPartyIntervention first estimates the
chance of intervention through:

    kWe.AI_paranoiaRating(eThey, iOurDefPow, false, true)

AI_paranoiaRating immediately resolves:

    CvTeamAI const& kRivalTeam = GET_TEAM(eRival);
    int const iTheirPower = kRivalTeam.getPower(true);

and uses that full team power together with team war state, enemy-power percent,
worst-enemy state and team no-war probability. ThirdPartyIntervention also uses
kTheirTeam's DogpileWarRand, LimitedWarRand and MaxWarRand.

After a positive intervention probability has been established, however, the
threat-strength model switches to:

    rTheirPow = kThey.uwai().getCache().getPowerValues()[ARMY]->power();

It then adds half the ARMY power of eTheirTeam's vassals, but never adds the army
power of eThey's ordinary teammates. UWAICache military branches are created for
one PlayerTypes owner, so this is genuinely player-local power rather than a
hidden team aggregate.

The player-local value is then subjected to the decisive nonlinear gate:

    rTheirPowToOurs = rTheirPow / rOurPow;
    if (rTheirPowToOurs <= 0.5)
        return;

Only much later, after all nonlinear ratio shaping, does the aspect divide cost
by a function of both teams' member counts.

TEAM-WAR CONTRACT

CvTeam::declareWar changes the at-war state of the declaring TeamTypes and the
target TeamTypes. The declaration is therefore not a war by one player while a
teammate remains neutral; all living members of the rival team are on that war
side. That matches the team-level power already used by AI_paranoiaRating.

DETERMINISTIC THRESHOLD REPRODUCTION

Let our relevant post-loss power be 100. Let a two-member potential-intervening
team have 40 ARMY power in B and 40 in C, no relevant vassals/losses, and hold
other player-local probability inputs equal/positive.

The paranoia path can see the rival team at power 80 for either member. Yet the
severity path evaluates:

    B: 40 / 100 = 0.40 -> return
    C: 40 / 100 = 0.40 -> return

Total intervention cost is therefore zero.

If the same team-wide 80 ARMY power is instead concentrated under B:

    B: 80 / 100 = 0.80 -> passes the 0.50 gate

and a nonzero cost can be produced. Team military capacity and the team-wide war
that would result are unchanged; only teammate ownership partition changed.

The later

    rCost *= 2;
    rCost /= (kOurTeam.getNumMembers() + kTheirTeam.getNumMembers());

cannot repair this because it executes only after the hard return. More generally,
the power-ratio transform is nonlinear, so summing separately transformed member
powers is not equivalent to transforming the combined intervening-side power.

ANCESTRY / INTENT CONTROL

The player-local power block, vassal addition, 0.5 floor and later member-count
normalization all date back to the original 2021 AdvCiv ThirdPartyIntervention
commit f39e13df870c036c2e5d0db26437254760898bdf. Later 2023 commits explicitly
retuned intervention probabilities and the power-ratio estimate but retained
this team/member boundary. Base AdvCiv 1.14 therefore inherits it unchanged.

That history cautions against replacing the whole pairwise model: intervention
probability intentionally contains player-specific attitude, memory, closeness,
victory-stage and capital-distance terms. F124 is narrower. Once the aspect asks
"how dangerous would this intervention be?", the hard power gate must represent
the team that will actually enter the war, especially because the immediately
preceding paranoia model already measures that team's collective power.

REPAIR DIRECTION

Keep genuinely player-specific probability/influence terms if desired, but build
the threat-strength ratio and its 0.5 gate from the combined living military
power of the intervening team, with vassal treatment reviewed once at team scope.
Then ensure the resulting team-level severity is not independently charged in
full once per teammate; a canonical team pass or another explicit aggregation is
safer than relying on the later linear member-count divisor.


CONTROLS BEFORE NEXT ROOT
-------------------------
- F115/KI#438 remains the independent ThirdPartyIntervention game-speed bug: a
  normalized peace-duration local is computed and discarded. F124 does not
  subsume it.
- F113/KI#436 remains retracted. GET_TEAM(eThey) is safe because the AdvCiv AI
  accessors overload PlayerTypes and resolve TEAMID(eThey); F124 does not depend
  on the rejected raw-array interpretation.
- The mere fact that ThirdPartyIntervention evaluates player pairs is NOT called
  defective. F124 is the narrower contradiction between its team-level danger /
  declaration model and the nonlinear player-local military gate.
- DramaticArc's existing F116 exact-counter bug remains independent. A possible
  additional team-partition effect in its per-player action/fairness gates is
  the next challenge; it is not promoted here.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP14
NEW ROOTS IN THIS APPEND: F123 / provisional KI#446; F124 / provisional KI#447
NEXT UNUSED ROOT: F125

Continue with DramaticArc's per-player action/fairness gates under a team-wide
war transition, then perform a full late-aspect/helper and live-ledger adversarial
closure before deciding whether WarUtilityAspect.cpp genuinely deserves C015.
===============================================================================


===============================================================================
C014-WIP15 CONTINUATION -- DRAMATICARC + DISTRACTION TEAM-WAR GATES
===============================================================================

CHECKPOINT BASIS
----------------
- This append continues the exact current-tree C014-WIP14 checkpoint from
  practical 6263 / HEAD 403929fe273f0bce131fc6bfd2ea15d57f688e6c.
- Current SAS source, local Base AdvCiv 1.14 and the reconciled album were
  rechecked locally. Civ4CE still supplies no UWAI/WarUtilityAspect comparison
  surface for these roots.
- F113/KI#436 remains authoritatively retracted. Nothing below depends on the
  rejected GET_TEAM(PlayerTypes) interpretation.


F096 / KI#418 -- ADDITIONAL WARUTILITYASPECT MANIFESTATIONS -- DEAD ASSIGNED
TEAM MEMBERS DILUTE THIRDPARTYINTERVENTION AND DRAMATICARC
-------------------------------------------------------------------------------
EXISTING ROOT broadened only; no new F-number.

The CvTeam audit established that getNumMembers() includes dead players still
assigned to the team, whereas the WarEvaluator/WarUtilityAspect evaluation
surface iterates living player members. WIP02 and WIP13 already proved this
mismatch in normalizeUtility, flipped-tile losses, GreedForVassals and Loathing.
Two more late-aspect uses have the same consumer-level proof:

ThirdPartyIntervention ends with:

    rCost *= 2;
    rCost /= (kOurTeam.getNumMembers() + kTheirTeam.getNumMembers());

DramaticArc ends with:

    rUtil /= scaled(kOurTeam.getNumMembers() + kTheirTeam.getNumMembers(), 2);

Both aspects are reached through the generic per-rival WarUtilityAspect loop,
which evaluates living rival players; WarEvaluator itself evaluates living
members of the agent team. A dead assigned teammate therefore contributes no
corresponding aspect pass or military/player state, but still enlarges these
normalization denominators and dilutes utility/cost.

This is the same F096 roster-cardinality contract already established elsewhere,
not a reason to replace every getNumMembers use mechanically. In particular,
consumer semantics still need to be proved case by case.


F125 / provisional KI#448 -- DRAMATICARC'S PLAYER-LOCAL SAFETY AND ACTION GATES
CAN LICENSE THE SAME TEAM-WIDE WAR THROUGH ANOTHER TEAMMATE
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team-partition defect, independent of F116's exact
peace-counter bug and F096's dead-member denominator distortion.

INTENDED ROLE / HISTORY CONTROL

AdvCiv commit 03f2a564827127bc9f32cfa85362ab42aad6f7ec introduced the
"Dramatic Arc" war-utility aspect to slightly encourage war after an unusually
quiet period and discourage it when there is too much warfare. Four days later,
commit 1c62194c840c55304b61e1e86a96618a8f299037 was explicitly titled:

    UWAI: be careful about attacking humans for dramatic reasons

and added the current human-capital-loss safeguard. Base AdvCiv 1.14 and current
SAS retain the same DramaticArc::evaluate implementation.

CURRENT GATES

For a simulated transition into war with eTheirTeam, each rival-player pass
starts from the same team-wide transition:

    bWillBeAtWar = militAnalyst().isWar(eWe, eThey);
    if (bWillBeAtWar == kOurTeam.isAtWar(eTheirTeam))
        return;

When positive dramatic utility would encourage a new war, however, fairness and
"enough action" are checked through the current rival player:

    iTheirLostCities = militAnalyst().lostCities(eThey).size();

    if (iTheirLostCities > 2 ||
        (kThey.isHuman() && iTheirLostCities == 1 && kThey.hasCapital() &&
         lostCities(eThey).count(kThey.getCapital()->plotNum()) > 0 && ...))
        return;

    if ((iTheirLostCities == 0 && conqueredCities(eWe).empty()) ||
        lostPower(eWe, ARMY) + lostPower(eThey, ARMY) < ourArmy / 20)
        return;

MilitaryAnalyst lostCities/lostPower are explicitly PlayerTypes result sets and
values. The generic aspect loop invokes this independently for every living
member of the hostile team.

MANIFESTATION A -- THE HUMAN-SAFETY COMMIT CAN BE BYPASSED BY THE HUMAN'S AI
TEAMMATE

Take a two-member target team B+C, where B is human and C is AI. The simulated
new war is a team-wide war against B+C. Suppose B is predicted to lose exactly
its capital under the conditions targeted by the 2022 safeguard.

B's pass sees kThey.isHuman() and returns, correctly refusing to award positive
"dramatic" utility for that dangerous war.

C's pass evaluates the same team-wide war transition but kThey.isHuman() is
false. The explicit human-protection gate disappears. If C's player-local action
checks pass, positive dramatic utility is still added for entering the war that
B's pass deliberately rejected.

The final team-size divisor can reduce the surviving C contribution, but it
cannot restore a safety return that was applied only to B.

MANIFESTATION B -- THE MINIMUM-ACTION GATE DEPENDS ON OWNERSHIP PARTITION

Let our current ARMY power be 100, so the minimum predicted combined-loss gate
is 5. Assume we conquer at least one target-team city, our own predicted ARMY
loss is 0, and the hostile team loses 8 ARMY power total.

If one teammate owns all 8 lost power, that member's pass clears the 5-point
threshold and can add dramatic utility.

If the same team-wide loss is split 4 + 4 between B and C, then both passes test
only their own 4 against the same threshold and both return. The military event
and war side are otherwise identical; merely repartitioning losses between
teammates changes whether the war is judged sufficiently "real" to encourage.

ROOT

DramaticArc wants to decide whether a TEAM-WIDE war transition is a fair and
substantive event before using it to steer global warfare tension. Its hard
safety/action gates are nevertheless scoped to whichever PlayerTypes happens to
be the current rival pass. A hard return is nonlinear; independently applying it
per teammate and then summing/dividing cannot reproduce one team-war decision.

REPAIR DIRECTION

Evaluate the "safe/fair/substantive enough to use for dramatic tension" predicate
once for the hostile team: aggregate relevant lost cities and ARMY loss across
living members, and preserve the human-safety intent if any affected team member
meets the protected human-capital condition. Then charge the team-wide dramatic
transition canonically once, or otherwise ensure the aggregate decision is not
reopened by another teammate pass.

Keep F116 separate: the 8-turn HasMet tolerance remains an exact-counter/time
bug even in one-player teams. Keep F096 separate: dead-member normalization is a
roster-cardinality defect, not the live-member nonlinear gate here.


F126 / provisional KI#449 -- DISTRACTION CHARGES THE SAME TEAM-WAR OPPORTUNITY
COST ONCE PER LIVING ENEMY TEAMMATE
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team/player aggregation defect. Current SAS's newer
winning-war cap can modulate the amount but did not create this root.

WHY BOTH TEAMMATES ARE EVALUATED

WarUtilityAspect::evaluate(MilitaryAnalyst const&) loops every living known
potential enemy player that is part of the military analysis. For a prospective
war against target team T, MilitaryAnalyst::doWePlanToDeclWar(ePlayer) returns
true whenever TEAMID(ePlayer) == WarEvalParameters::getTarget(); therefore every
living known member of T is inserted into ourFutureOpponents/theirSide and then
m_partOfAnalysis. For an existing war, living members are likewise included by
the currently-at-war construction.

Distraction::evaluate is thus called once for B and once for C when B+C are one
multi-member target team, provided the ordinary player-pair reachability guard is
satisfied for both.

THE COST IT COMPUTES IS LARGELY THE SAME TEAM-WAR COST

After the reachability check, the core inputs are team-scoped:

    iWarDuration = kOurTeam.AI_getAtWarCounter(eTheirTeam);

    rWarUtilityVsThem = normalizeUtility(
        ourCache().warUtilityIgnoringDistraction(eTheirTeam));

normalizeUtility's parameter is explicitly named rUtilityTeamOnTeam, and both B
and C have the same eTheirTeam. Alternative targets are iterated as TeamTypes;
their war plans, declaration legality, reachability and cached war utility are
team quantities too.

Most decisively, every qualifying actual alternative war plan adds a fixed:

    rDistractionCost += 5.5;

under the comment:

    The war against eThey distracts us from our war against eAltTarget.

For B and C on one target team, "the war against eThey" is the same Civ4 team
war. Yet the fixed 5.5 is added independently in B's pass and C's pass. There is
no final division or canonical-team guard before:

    m_iU -= rDistractionCost.round();

DETERMINISTIC DUPLICATION CONTROL

Choose a two-member hostile team B+C such that both player pairs are reachable,
no member is eliminated/almost-done, and our team has one qualifying alternative
team D with an existing AI war plan. Hold all other optional opportunity-cost
terms at zero if desired.

Against a one-player target team, that alternative plan contributes the fixed
5.5 distraction nudge once.

Against an otherwise identical two-player target team B+C:

    B pass: +5.5 distraction cost for the D war plan
    C pass: +5.5 distraction cost for the same D war plan

so the same opportunity cost becomes 11 before rounding. Team-size handling
inside normalizeUtility applies only to the cached team-on-team war-utility
inputs; it does not scale this fixed nudge or the final rDistractionCost.

The broader opportunity-cost branch has the same structural duplication because
its duration/current-target/alternative-target inputs are team-scoped and the
result is again subtracted per rival pass. Player-local reachability and the
"almost done" city multiplier can legitimately differ, but those differences do
not justify charging a common team-war opportunity cost repeatedly once both
passes clear their guards.

ANCESTRY / INTERNAL CONTROLS

Base AdvCiv 1.14 contains the same generic Distraction structure and fixed 5.5
actual-war-plan nudge. Historical AdvCiv work explicitly describes/tunes the
negative utility counted by "Distraction" and later refactors alternative war
targets to teams; the current source itself labels the cached utility input as
team-on-team.

Current SAS adds a winning-war cap using target TEAM city count, TEAM power and
TEAM war success. That addition further confirms the target-war quantity's team
scope, but the duplicated per-member charge predates SAS and survives even when
that cap never fires.

REPAIR DIRECTION

Compute Distraction once per hostile team (or nominate a canonical living member
for the team-level charge), while retaining player-pair reachability only as a
question of whether the war side is meaningfully reachable. Review the existing
player-local almost-done multiplier separately: elimination of one member can be
a real detail, but it should modulate one team-war distraction estimate rather
than create multiple copies of the common opportunity cost.

Do not "fix" this by dividing every per-rival result blindly by team size. Some
Distraction guards are player-specific, and the fixed/common team cost plus
nonlinear maxima/aggregation make canonical team-level evaluation clearer and
less partition-sensitive.


CONTROLS / NEXT CHALLENGE
-------------------------
- LoveOfPeace superficially has the same per-rival shape, but current SAS data
  leaves iLoveOfPeace at 0; do not promote a dormant mod-mod configuration path
  as a current defect merely for symmetry.
- Revolts' m_countedCities lifetime remains a closed false positive: aspect
  objects are fresh per WarEvaluator scenario, while the set intentionally
  persists only across rival/member passes inside that scenario.
- Distraction F126 is independent of the SAS winning-war cap. The inherited
  fixed 5.5 duplicate is sufficient proof by itself.
- F123 Bellicosity and F124 ThirdPartyIntervention remain separate nonlinear
  partition roots; they are not collapsed into a vague umbrella issue.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP15
NEW ROOTS IN THIS APPEND: F125 / provisional KI#448; F126 / provisional KI#449
EXISTING ROOT BROADENED: F096 / KI#418
NEXT UNUSED ROOT: F127

Continue the systematic team-war/per-rival pass across the remaining late
aspects and helpers, then re-challenge F101-F126 and the explicit false-positive
ledger before deciding whether file 014 can close durably.
===============================================================================


===============================================================================
C014-WIP16 CONTINUATION -- GLOBAL VICTORY CAPS + TEAM-KEYED HIRED-HAND OBLIGATION
===============================================================================

CHECKPOINT BASIS
----------------
- This append continues the exact C014-WIP15 current-tree checkpoint at practical
  6263 / HEAD 403929fe273f0bce131fc6bfd2ea15d57f688e6c.
- Current SAS and local Base AdvCiv 1.14 were compared directly. The relevant
  MilitaryVictory and HiredHand implementations are inherited unchanged.
- F113/KI#436 remains retracted; neither finding below uses that false diagnosis.


F127 / provisional KI#450 -- MILITARYVICTORY CAPS GLOBAL DOMINATION/DIPLO
PROGRESS SEPARATELY INSIDE EACH RIVAL-PLAYER PASS
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv global-threshold aggregation defect. This is distinct
from F105's exact Domination equality/divisor bug and F111's AP wrong-owner test.

ARCHITECTURE

MilitaryVictory is an ordinary WarUtilityAspect. For each living agent-team
member, WarUtilityAspect::evaluate(MilitaryAnalyst const&) calls
MilitaryVictory::evaluate() once for every living known rival player that is
part of the analysis. MilitaryVictory then computes progress toward the agent's
shared/global victory thresholds from the current eThey player's predicted
losses.

This player-by-player decomposition is harmless only while the progress function
is additive. The current implementation applies global completion caps BEFORE
rival contributions are combined.

DOMINATION -- THE SOURCE'S OWN COMMENT STATES THE INVARIANT

progressRatingDomination() uses the agent team's current global threshold:

    iPopToGo = iPopTarget - kOurTeam.getTotalPopulation();

It accumulates population gained from cities lost by the CURRENT rival player
and then explicitly says:

    // No use in population beyond the victory threshold
    rPopGained.decreaseTo(iPopToGo);

That is exactly the correct conceptual cap for the full military scenario: once
we have gained the remaining required population, additional projected conquest
cannot make us more than 100% complete on that Domination component.

But the cap is reset for every eThey pass. MilitaryVictory::evaluate then clamps
that rival's progress again to [0,1] and immediately converts it to utility.
There is no later aggregate cap across rivals.

DETERMINISTIC DOMINATION REPRODUCTION

Suppose the agent team needs only 5 effective population to satisfy the remaining
Domination population threshold, and land/city conditions are such that this
component controls progress.

If projected conquest from B supplies 6 population and C supplies none:

    B pass: rPopGained -> capped to 5 -> 100% progress
    C pass: 0

If the same military scenario instead takes cities worth 6 population from B
AND 6 from C:

    B pass: independently capped to 5 -> 100%
    C pass: independently capped to 5 -> 100%

The global threshold has been credited twice. This can arise from two enemy teams
or, especially cleanly, two members of one hostile team. Merely repartitioning
otherwise equivalent conquered cities between members can change how many times
the completion cap is reached.

DIPLOMACY HAS THE SAME ROOT

progressRatingDiplomacy() uses the global remaining vote requirement cached by
preEvaluate:

    m_iVotesToGo = kOurTeam.AI_votesToGoForVictory();

After accumulating expected population/votes from the CURRENT rival's losses it
caps:

    rPopGained.decreaseTo(m_iVotesToGo);
    rProgress = rPopGained / m_iVotesToGo;

Again, if only 5 votes remain and B's projected conquests provide 6 while C's
provide another 6, each rival pass can independently report 100% vote progress.
The same five-vote global deficit is completed twice.

The AP-obstacle subcomponent is player-count based and was already checked as a
separate control; F127 does not relabel that numerator/denominator as defective.
The defect is specifically the repeated GLOBAL vote/population completion cap.

AGENT-TEAM MEMBERS CAN ALSO REPEAT THE GLOBAL CAP

WarEvaluator reuses the aspect set while evaluating every living member of the
agent team. Domination and diplo victory are team-owned conditions, but each
agent member can contribute projected conquests through its own eWe pass. Thus
the same global remaining threshold can be independently capped not only across
rivals but also across agent-member/rival-member pairs when multiple teammates
contribute to the projected gains.

ANCESTRY / INTENT CONTROL

The original AdvCiv MilitaryVictory implementation already had this per-rival
architecture and per-rival [0,1] progress cap. Later commits increased the value
of military-victory progress and added capitulation contributions without moving
the global threshold cap outside the player pass. Base AdvCiv 1.14 and current
SAS remain identical here.

The comment "No use in population beyond the victory threshold" is the strongest
intent control: the implementation itself wants surplus progress discarded, but
its scope allows the same surplus boundary to reset once per player.

REPAIR DIRECTION

Aggregate projected Domination population/city progress and diplomatic-vote
progress across the entire relevant military scenario before applying the
remaining-threshold cap and converting progress to utility. A natural approach
is one pre/global computation per agent team (or a cumulative marginal-progress
budget shared across player passes), while leaving genuinely opponent-specific
costs such as nukes separately attributable if desired.

Do not simply divide final MilitaryVictory utility by rival/team size: the error
is a reset global cap, not a linear scale problem. Conquest progress should be
reviewed separately because eliminating distinct rivals can legitimately be
additive; F127 is already proven by Domination and Diplomacy without claiming
that every MilitaryVictory subcomponent must be globalized identically.


F128 / provisional KI#451 -- HIREDHAND COUNTS ONE TEAM-KEYED SPONSORSHIP /
OBLIGATION ONCE PER LIVING MEMBER OF THE SPONSORED-WAR TARGET TEAM
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team-duplication defect.

TEAM-KEYED CONTRACT

UWAICache exposes:

    int bountyAgainst(TeamTypes eTeam) const;
    PlayerTypes sponsorAgainst(TeamTypes eTeam) const;

HiredHand::evaluate therefore looks up the already-accepted sponsorship with:

    eSponsor = ourCache().sponsorAgainst(eTheirTeam);
    iOriginalUtility = ourCache().bountyAgainst(eTheirTeam);

This describes one payment/obligation for war against a target TEAM, not a
separate payment against every member player.

Once the sponsorship is found, the main branch adds:

    rUtility += eval(eSponsor, iOriginalUtility, 20);

HiredHand::eval in turn measures how long the hired side has fought through the
TEAM at-war counters against eTheirTeam. Its output therefore also describes the
same team-war obligation.

WHY IT IS DUPLICATED

HiredHand is an ordinary concernsOnlyWarParties aspect. Against a multi-member
hostile team B+C, both B and C are part of the analysis and the generic aspect
loop calls HiredHand for each living player.

For a continuing sponsored war, getWarsContinued(eWe).count(eThey) can be true
for both B and C. Then:

    B pass -> sponsorAgainst(team B+C), same bounty, same war counter -> obligation
    C pass -> sponsorAgainst(team B+C), same bounty, same war counter -> obligation

There is no member-count normalization or canonical-team guard before the two
results accumulate into m_iU.

DETERMINISTIC REPRODUCTION

Assume D paid us once to fight target team B+C; original sponsorship utility is
25 and the obligation ratio currently makes eval return, say, 20.

A one-player target team contributes +20 HiredHand utility.

With two living members B+C, holding all ordinary guards true:

    B pass: +20 for the D sponsorship against team B+C
    C pass: +20 for the same D sponsorship against team B+C
    total:  +40

No second payment or promise occurred. Only the target team's player count
changed.

SECOND INDEPENDENT MANIFESTATION -- HISTORICAL ROLE

The fallback scenario rule says that, if we have been at war since the start of
the game, we should play along for a while:

    if (kOurTeam.AI_getAtWarCounter(eTheirTeam) >=
        m_kGame.getElapsedGameTurns())
        rUtility += eval(NO_PLAYER, 50, 12);

That condition and eval timer are again entirely team-scoped. A scenario war
against B+C therefore receives the same historical-role obligation once on B's
pass and once again on C's.

The branch where WE hired an ally contains some player-specific memory detail
and should be reviewed on its own; F128 does not require it. The accepted
sponsorship branch and historical-role branch already prove the root.

ANCESTRY / INTERNAL CONTROL

The original AdvCiv HiredHand implementation already queried sponsorship by
TEAMID(theyId) and used the target team's at-war counter. Historical fixes later
adjusted obligation duration/payment behavior (including a bug where a hired AI
continued doggedly after the sponsor made peace) but retained the per-rival
aspect placement. Base AdvCiv 1.14 and current SAS HiredHand::evaluate are
textually identical.

REPAIR DIRECTION

Charge team-keyed HiredHand obligations once per hostile target team, e.g. by a
canonical living target member or an explicit per-team pass. Preserve truly
player-specific relationship/memory pieces separately where they matter. The
sponsorship bounty and historical-role timer themselves should not be multiplied
by the number of living players assigned to the enemy team.


CONTROLS / NEXT CHALLENGE
-------------------------
- GreedForCash supplies a useful positive control: its comment explicitly says
  "Only one team member will pay" and uses normalizeUtility(4), showing that this
  codebase does deliberately normalize a team-level fixed amount when evaluated
  through player passes.
- Rebuke explicitly documents that counting capitulation utility multiple times
  across team members is acceptable there because the underlying rebuke memory
  is player-pair specific. Do not generalize F128 to every repeated team call.
- MilitaryVictory's Conquest subcomponent remains under separate review; F127
  does not need it and should not overclaim it.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP16
NEW ROOTS IN THIS APPEND: F127 / provisional KI#450; F128 / provisional KI#451
NEXT UNUSED ROOT: F129

Continue the remaining per-rival/team-key audit (especially early aspects and
team-keyed state), then re-challenge F101-F128 before any file-closure decision.
===============================================================================

LOCAL RECONCILIATION AFTER C014-WIP16 IMPORT
--------------------------------------------
- ChatGPT-5.6-Sol's C014-WIP12 through C014-WIP16 append is compatible with current authority at HEAD 403929fe2 plus the staged KI#414/KI#418 Tech Share repairs. Its occasional double blank lines are preserved as received because this is an append-only evidence record, not normalized source code.
- F096 / KI#418 is now fixed in the staged current source by counting each qualifying team's living members. WIP13's broader living-participant analysis remains useful corroboration rather than a separate pending root.
- F120-F128 / provisional KI#443-KI#451 are collision-free against the current reviewed Known Issues ledger, but remain provisional because `WarUtilityAspect.cpp` is still open and WIP16 explicitly requires a final adversarial reread before file closure. No current-source implementation is promoted from them yet. (GPT-5.6-Sol)


===============================================================================
C014-WIP17 APPEND -- WarUtilityAspect.cpp / vassal-score team-domain audit
===============================================================================

F129 / provisional KI#452 -- VASSAL CURRENT-TECH SCORE IS TEAM-OWNED BUT CACHED
PER PLAYER AND SUMMED ONCE PER VASSAL MEMBER
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team-duplication defect.

ROOT CONTRACT

UWAICache stores the value through a player-keyed map:

    int vassalTechScore(PlayerTypes ePlayer) const;
    CivPlayerMap<int> m_aiVassalTechScore;

and updateVassalScore(PlayerTypes eRival) computes it by iterating every tech and
asking:

    GET_PLAYER(eRival).canTradeItem(m_eOwner,
        TradeData(TRADE_TECHNOLOGIES, eLoopTech), false)

then adding:

    GET_TEAM(m_eOwner).AI_techTradeVal(
        eLoopTech, TEAMID(eRival), true)

Despite the PlayerTypes cache key, this CURRENT-TECH quantity is team-owned for
the purpose being measured.

WHY TEAMMATES RECEIVE THE SAME TECH SCORE

The technology branch of CvPlayer::canTradeItem uses the source player's TEAM:

    kOurTeam.isHasTech(eTech)
    !kOurTeam.isNoTradeTech(eTech)

and the fixed recipient team's ownership/research legality. updateVassalScore
passes bTestDenial=false, so player-specific AI/human trade willingness is not
consulted. canPossiblyTradeItem(TRADE_TECHNOLOGIES) is likewise team/game-option
state.

Therefore, for two living players B and C on the same prospective vassal team,
and a fixed master player A:

    vassalTechScore(B) == vassalTechScore(C)

for the existing-tech component, because B and C expose the same team technology
inventory and the same source TeamTypes to AI_techTradeVal.

The actual trade confirms the unit of ownership. CvDeal executes a technology
item with:

    GET_TEAM(eToPlayer).setHasTech(...)

so the recipient TEAM acquires the technology. Once A's team receives Tech X
from member B, member C cannot independently provide a second copy of Tech X.

CONFIRMED CONSUMER A -- acceptVassal SUMS THE DUPLICATE PLAYER CACHES

UWAI::Team::acceptVassal explicitly iterates every member of eVassal:

    for (MemberIter itVassalMember(eVassal); itVassalMember.hasNext(); ++itVassalMember)
    {
        iResourceScore += leaderCache().vassalResourceScore(
                itVassalMember->getID());
        iTechScore += leaderCache().vassalTechScore(
                itVassalMember->getID());
    }

The resulting iTechScore is converted to utility as one of the benefits of
accepting that VASSAL TEAM.

If team B+C owns one tradable technology worth 1000 trade value to A:

    one-member team B:      tech score = 1000
    two-member team B+C:    B cache 1000 + C cache 1000 = 2000

Nothing about the technology inventory changed. Only the number of players
sharing that inventory changed.

The later rVassalUtility.decreaseTo(25) can mask sufficiently large totals, but
it does not repair the defect: below the cap, or when combined with other
benefits/costs near the acceptance threshold, team membership changes the
valuation of the same set of technologies.

CONFIRMED CONSUMER B -- GreedForVassals REUSES THE SAME CURRENT-TECH COMPONENT
ON EACH MEMBER PASS

GreedForVassals::evaluate is called through the ordinary per-rival aspect loop.
For each member of a newly capitulating target team it reads:

    scaled const rTechScore = ourCache().vassalTechScore(eThey);

and adds that score to the member's future research-income estimate before the
per-pass tech-trade cap/reduction. Thus the same already-owned team technologies
can also be credited once on B's pass and again on C's pass. The future research
income itself is player-local and may legitimately be additive; F129 concerns
the duplicated CURRENT technology inventory component.

RESOURCE SCORE IS A USEFUL NEGATIVE CONTROL

The neighboring vassalResourceScore is also cached per player, but its core
availability check uses:

    GET_PLAYER(eRival).getNumAvailableBonuses(eLoopBonus)

Resources are actually owned/connected per player. B and C can therefore bring
different tribute resources to the same team, so summing their resource scores
can be meaningful. That contrast reinforces that the technology score is stored
at the wrong ownership granularity rather than proving that every per-member
vassal quantity should become team-wide.

ANCESTRY

The original AdvCiv WarAndPeaceCache implementation already used a per-player
vassalTechScores[civId] cache and acceptVassal already summed the score once per
vassal member. Later EnumMap/refactor commits preserved the ownership model.
Base AdvCiv 1.14 and current SAS retain the same updateVassalScore,
GreedForVassals and acceptVassal behavior. This is inherited, not SAS-created.

REPAIR DIRECTION

Compute/cache the already-owned tradable technology value once per prospective
vassal TEAM (or deduplicate techs across member caches before summing). Keep
player-local future research income and player-owned resource availability
separate.

For acceptVassal, do not sum identical current-tech inventories across MemberIter.
For GreedForVassals, ensure the team-owned existing-tech benefit is charged once
per newly acquired vassal team while preserving player-local commerce/research
contributions as intended.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP17
NEW ROOT IN THIS APPEND: F129 / provisional KI#452
NEXT UNUSED ROOT: F130

Continue the remaining early-aspect/team-key and nonlinear-state audit, then
re-challenge F101-F129 before deciding whether WarUtilityAspect.cpp is ready for
C015 closure. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP18 APPEND -- WarUtilityAspect.cpp / Risk team-owned vassal-state costs
===============================================================================

F130 / provisional KI#453 -- RISK COUNTS TEAM-OWNED VASSAL/CAPITULATION COSTS
ONCE PER AGENT/RIVAL MEMBER PASS
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv team-duplication defect; two independent manifestations.

ROOT

Risk mixes ordinary player-local loss accounting with costs whose ownership and
state are explicitly TEAM-level. WarEvaluator, however, reuses the Risk aspect
while evaluating every living agent-team member, and WarUtilityAspect then calls
Risk::evaluate once for every relevant rival player. The team-owned pieces have
no normalization/canonical-member guard.

MANIFESTATION A -- ONE TEAM CAPITULATION GETS A FIXED 100 COST PER PLAYER PAIR

Risk::evaluate tests:

    militAnalyst().getCapitulationsAccepted(eTheirTeam).count(eOurTeam) > 0

This is one predicted capitulation of the AGENT TEAM to the RIVAL TEAM. The
condition is identical for every member pair A_i / B_j belonging to those two
teams.

When true, Risk halves that pair's ordinary loss estimate and then adds:

    int const iCostForCapitulation = 100;
    rUtility += iCostForCapitulation;

The source comment describes the value as an extra cost "because of
capitulation"; there is no player-pair memory or asset quantity behind the fixed
100.

DETERMINISTIC 2-v-2 REPRODUCTION

Let agent team A contain A1+A2 and rival team B contain B1+B2, with the military
analysis predicting that A capitulates to B. Assuming the capitulation-result
lookup is available (including after F119/KI#442's team-slot repair):

    A1 / B1 pass: +100
    A1 / B2 pass: +100
    A2 / B1 pass: +100
    A2 / B2 pass: +100

The same team capitulation therefore contributes +400 Risk cost before the
pair-local lost-asset pieces are considered. A one-player-vs-one-player mapping
contributes +100 for the same team-state event.

This is not repaired by the preceding `rUtility /= 2`: that division applies to
each pair's ordinary loss estimate before the fixed 100 is added.

F119 INTERACTION

F119/KI#442 established that MilitaryAnalyst currently writes the team-indexed
capitulation result through a raw PlayerTypes slot. That can hide, swap or lose
the predicted capitulation in some team-ID mappings. F130 is a separate consumer
bug: whenever the team result is visible under the requested slot -- and
certainly after F119 is repaired -- Risk multiplies one team capitulation by the
number of member pairs. Do not treat the F119 storage repair as fixing F130.

MANIFESTATION B -- PRE-EVALUATION RECOUNTS OUR TEAM'S SAME VASSAL LOSSES FOR EACH
AGENT MEMBER

Risk::preEvaluate has an explicit non-rival block:

    // Handle potential losses of our vassals here
    for (PlayerIter<ALIVE,VASSAL_OF> itVassal(eOurTeam); ...)

It computes the projected loss of each vassal of eOurTeam, including a material
loss cost up to 33 and a possible break-away cost, and returns the negative sum.

`preEvaluate()` is invoked by WarUtilityAspect::evaluate(MilitaryAnalyst const&)
for every agent player. WarEvaluator constructs the aspect set once, then calls:

    evaluate(itMember->getID(), apAspects)

for every living member of the agent team. There is no one-shot guard around the
vassal-loss block and no division by the agent team's participant count.

Thus even the simplest fixed material part duplicates:

    one agent member + same vassal loses 100% assets -> about 33 cost
    two agent members + same team-owned vassal loss -> about 33 + 33

Only the roster of the master team changed; the same vassal and same projected
vassal losses are being valued repeatedly.

The break-away subcomponent is additionally player-partition-sensitive because
its denominator uses eWe's player-local army cache/gained power, even though
breaking free is a vassal-vs-master TEAM relation. F130 does not need that harder
modeling question: the identical material-loss component already proves the
member duplication.

ANCESTRY / HISTORY CONTROL

AdvCiv history shows that the vassal-loss code was deliberately moved from the
ordinary per-rival Risk::evaluate path into Risk::preEvaluate (practical 1291,
"Toward v0.92") so it could be handled outside the current `they` rival. The
move introduced/retained the global vassal loop but did not add agent-team
normalization.

The fixed 100 capitulation cost also exists in the original AdvCiv UWAI lineage.
Base AdvCiv 1.14 retains both behaviors. Current SAS adds a one-shot team-level
faraway-target gate near the beginning of Risk::preEvaluate (KI#425), but the
inherited vassal-cost block below it remains unguarded; F130 is not SAS-created.

INTERNAL CONTROL

GreedForCash explicitly uses `normalizeUtility(4)` with the comment "Only one
team member will pay" when a fixed team-level payment is evaluated through the
same player/member architecture. Risk's fixed team capitulation cost has the
same ownership problem but no corresponding normalization.

REPAIR DIRECTION

Keep player-local lost-city/blockade/nuke Risk costs attributable to the relevant
player pair, but charge team-owned state changes once at team scope.

- The fixed capitulation penalty should be applied once for the predicted
  eOurTeam -> eTheirTeam capitulation, or shared through a team/member
  normalization that yields the same aggregate regardless of roster partition.
- Vassal material-loss/break-away pre-evaluation should likewise be computed once
  per master team (or normalized across agent members). For break-away danger,
  review whether master-side power should be combined team power rather than the
  current eWe player denominator.

Do not simply divide all of Risk by team sizes; ordinary asset and military
losses are player-owned and are intentionally accumulated across players.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP18
NEW ROOT IN THIS APPEND: F130 / provisional KI#453
NEXT UNUSED ROOT: F131

Continue the explicit team-owned/fixed-state audit and then adversarially
re-challenge the live F101-F130 ledger before any C015 closure. No repository
source files were modified.
===============================================================================


===============================================================================
C014-WIP19 APPEND -- WarUtilityAspect.cpp physical city-site identity audit
===============================================================================

F131 / provisional KI#454 -- CONFIRMED -- INHERITED ADVCIV DUPLICATE-PLOT VALUE
GREEDFORSPACE CAN COUNT ONE PHYSICAL CITY SITE ONCE PER ELIMINATED TEAMMATE
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv player-cache/team-aggregation defect.

ROOT

GreedForSpace values settlement opportunities that become available when a rival
player is eliminated. City-site recommendations are cached separately by
CvPlayerAI, but the object being valued is a physical map plot. Two teammates can
therefore recommend the same plot independently, while one physical plot can
ultimately host only one city.

Current GreedForSpace::evaluate nevertheless runs once per rival player and
counts each eliminated player's cached site list independently. There is no
cross-member plot-identity deduplication.

SOURCE CONTRACT -- CITY-SITE LISTS ARE PLAYER-LOCAL

CvPlayerAI::AI_updateCitySites scans every map plot and accepts a candidate when:

    iValue > iMinFoundValueThreshold && !AI_isPlotCitySite(kPlot)

AI_isPlotCitySite checks only this CvPlayerAI instance's m_aeAICitySites vector:

    std::find(m_aeAICitySites.begin(), m_aeAICitySites.end(),
              kPlot.plotNum())

The selected plot is then pushed only into that player's vector. Found values are
also stored/evaluated for getID(). There is no teammate-site reservation or
team-wide duplicate check in this pipeline.

Thus, in a supported team game, B1 and B2 can both have the same physical plot P
in their city-site lists. Shared team visibility can make this especially
natural; different player found-value rankings are not required for overlap.

GREEDFORSPACE CONSUMER

For the current rival eThey, GreedForSpace first requires:

    militAnalyst().isEliminated(eThey)

It then loops that player's recommendations and increments iTheirSites for each
visible/relevantly-visible site. The resulting count feeds:

    rExpectedSites = iOurSites + 0.63 * iTheirSites

and ultimately positive utility for the expansion space supposedly released by
eliminating that player.

WarUtilityAspect evaluates the same GreedForSpace object separately for every
relevant rival player. Therefore, if B1 and B2 are members of the same target
team, both are eliminated by the scenario, and both cached plot P as a city site:

    B1 pass: P contributes one orphaned site
    B2 pass: P contributes one orphaned site

The aggregate evaluation has valued two future settlement opportunities even
though P can be settled only once.

This is a physical-identity double count, not merely a debate over how much a
multi-member team should be worth. Reassigning the same plot recommendation from
one target-team member to two members can increase war utility without creating
any additional settleable land.

AUTO-RAZE / DISTINCT-SITE CONTROL

GreedForSpace also adds auto-razed cities conquered from the current rival to
its site count. Those city plots are owner-specific physical losses and should
remain additive when distinct. Likewise, genuinely different recommended plots
held by B1 and B2 represent different expansion opportunities.

The repair therefore should NOT simply divide all GreedForSpace utility by team
size. The duplicated object is the plot ID itself.

ANCESTRY / HISTORY CONTROL

Current SAS and Base AdvCiv 1.14 have normalized-identical implementations of
both GreedForSpace::evaluate and CvPlayerAI::AI_updateCitySites for this behavior.
The defect is inherited.

Practical 2949 ("Some minor tweaks to UWAI war evaluation") is a useful local
historical control. In one adjacent patch it expanded GreedForSpace's site scan
to respect visibility; immediately below, it changed GreedForCash from a raw
fixed +4 to:

    normalizeUtility(4) // Only one team member will pay

Thus the lineage was already correcting a team-duplication problem in the same
small aspect neighborhood, but GreedForSpace's player-local site lists remained
aggregated without physical-plot deduplication.

ADVERSARIAL NON-FINDING CONTROL -- OPEN BORDERS

The preceding sweep also challenged Assistance/IllWill's fixed Open Borders
partner value. That does not presently meet the F131 standard. The original UWAI
implementation treats the OB term as part of the value of a particular partner,
alongside that player's trade/technology/military utility; access to two
teammates' separate territories can plausibly carry more strategic value than
access to one. A team-to-team treaty alone is therefore insufficient evidence of
double counting. No finding is promoted for that family here.

REPAIR DIRECTION

When valuing space released by elimination of multiple members of one rival team,
collect the relevant recommended site plot IDs at the team/scenario level and
count their UNION. Preserve distinct sites and distinct auto-razed city plots.

A localized alternative is a per-evaluation set of already-counted plotNums that
persists across rival-member passes for the same target team, with careful reset
between independent WarEvaluator scenarios. Do not normalize by raw member count.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP19
NEW ROOT IN THIS APPEND: F131 / provisional KI#454
NEXT UNUSED ROOT: F132

Continue the remaining helper/pre-evaluation/team-state sweep and then run an
adversarial closure pass over the live C014 ledger before deciding whether file
014 is ready for durable C015. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP20 APPEND -- Revolts cross-rival accumulator/denominator audit
===============================================================================

F132 / provisional KI#455 -- CONFIRMED -- INHERITED ADVCIV ORDER-DEPENDENT RATIO
REVOLTS PERSISTS THE "COUNTED CITY" SET BUT RESETS ITS NUMERATOR/DENOMINATOR
ON EVERY RIVAL PASS, MAKING UTILITY DEPEND ON WHICH PLAYER CLAIMS A CITY FIRST
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv state-partition / player-iteration-order defect,
independent of F118/KI#441's negative-asset sign inversion.

ROOT

Revolts is evaluated once per relevant rival player. A member field:

    std::set<PlotNumTypes> m_countedCities;

persists across those rival calls, and the source explicitly says:

    // Count each city only once

However, the quantities used to turn those city risks into utility are LOCAL to
each individual Revolts::evaluate call:

    scaled rLossesFromRevolts;
    int iTotalAssets = 0;

For every newly encountered city, the current call adds its asset score to
`iTotalAssets` and its expected revolt loss to `rLossesFromRevolts`. At the end
of that one rival pass it charges:

    rRevoltCost = (1 + amortizationMultiplier) *
                  50 * rLossesFromRevolts / iTotalAssets;

The next rival pass preserves which cities have already been claimed through
m_countedCities, but resets both numerator and denominator to zero. The final
aspect therefore sums NORMALIZED RATIOS OF ARBITRARY FIRST-CLAIM SUBSETS rather
than normalizing the union of cities that the persistent set was designed to
count once.

WHY m_countedCities IS CROSS-RIVAL STATE

Within one rival pass, the code loops map areas and then our cities in each area.
A city belongs to only one CvArea, so the member set is not needed merely to stop
the same city recurring in that local area traversal. It matters when different
rivals regard the same area as primary and Revolts::evaluate is called again.

WarUtilityAspect keeps the same Revolts object through its rival-player loop;
resetRival() does not clear m_countedCities. The object itself is fresh for the
next independent WarEvaluator scenario, which is why the earlier lifetime audit
correctly rejected a cross-scenario leak. F132 concerns the intended intra-
scenario persistence combined with per-call reset of the ratio accumulators.

DETERMINISTIC PLAYER-ITERATION-ORDER REPRODUCTION

Let our threatened cities X and Y each have asset score 100, revolt probability
at or above the 10% cap, and no hopeless-revolt exclusion. Their individual loss
multipliers are therefore both 0.5.

Let two simultaneous war enemies B and C satisfy the ordinary Revolts gates such
that:

    B has a primary area containing X but not Y
    C has primary areas containing both X and Y

Both players and all military/game state can be held fixed. Only their numeric
player iteration order changes.

CASE 1 -- B is visited first

B pass:
    newly counted: X
    numerator = 50, denominator = 100 -> ratio 0.5

C pass:
    X is skipped by m_countedCities; newly counted: Y
    numerator = 50, denominator = 100 -> ratio 0.5

Aggregate normalized ratio contribution: 1.0 (before common multiplier).

CASE 2 -- C is visited first

C pass:
    newly counted: X + Y
    numerator = 100, denominator = 200 -> ratio 0.5

B pass:
    X already counted; no newly counted assets -> returns

Aggregate normalized ratio contribution: 0.5.

The same cities, same revolt probabilities, same enemies, same wars and same
primary-area relationships therefore produce roughly DOUBLE the Revolts cost
solely from PlayerTypes iteration order / which rival gets first claim on X.
That cannot be defended as a multi-front modeling choice.

F118 SEPARATION

F118/KI#441 established that negative per-city asset scores can make revolt loss
negative and hence create positive war utility. F132 remains even when every
asset score is strictly positive, as in the 100/100 reproduction above. A repair
for one does not repair the other.

ANCESTRY

The original AdvCiv UWAI introduction already contains the same structural
combination:

    double revoltLoss = 0;
    int totalAssets = 0;
    ...
    if (countedCities.count(c->plotNum()) > 0) continue;
    countedCities.insert(c->plotNum());
    ...
    uMinus = ... * revoltLoss / totalAssets;

and declares `countedCities` as a Revolts member set. Later refactors merely
converted the arithmetic to scaled/fixed-point and renamed the member.
Base AdvCiv 1.14 and current SAS retain the same behavior. F132 is inherited,
not SAS-created.

REPAIR DIRECTION

The normalization domain must match the uniqueness domain.

Preferred: evaluate the union of all relevant threatened own-city plot IDs once
per agent member/scenario, accumulate one nonnegative expected-loss numerator
and one corresponding asset denominator, then apply the Revolts formula once.
This can naturally be combined with the F118 repair.

If keeping per-rival callbacks, persistent cumulative numerator/denominator
state must be used in a way that changes the aspect by only the delta of the
single global ratio; simply retaining m_countedCities while restarting local
ratios is not sufficient.

Do not remove m_countedCities without replacement: that would reintroduce the
more obvious duplicate-city count when several enemies share a primary area.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP20
NEW ROOT IN THIS APPEND: F132 / provisional KI#455
NEXT UNUSED ROOT: F133

Continue the remaining arithmetic/state-domain sweep, with special attention to
member fields that intentionally persist across rival calls and to local caps or
normalizers whose state domain does not match that persistence. Then adversarially
re-challenge F122-F132 before any C015 closure. No repository source files were
modified.
===============================================================================


===============================================================================
C014-WIP21 APPEND -- WarUtilityAspect.cpp victory-domain broadening + advc.035 ratio audit
===============================================================================

F127 / provisional KI#450 -- AUTHORITATIVE BROADENING -- CONQUEST TOO
PLAYER ELIMINATION IS TREATED AS 100% CONQUEST PROGRESS EVEN WHEN THEIR TEAM
STILL BLOCKS THE ACTUAL TEAM-LEVEL CONQUEST VICTORY
-------------------------------------------------------------------------------
WIP16 established F127 from Domination and Diplomacy: global/team victory
completion caps are restarted inside each rival-player pass, so the same
remaining global threshold can be independently completed more than once.
The subsequent victory-boundary audit proves that Conquest belongs under the
same root too, for an even more direct ownership-domain mismatch.

ACTUAL VICTORY CONTRACT

CvGame::testVictory evaluates Conquest for TeamTypes eTeam. After requiring the
candidate team to own a city, it iterates every other ALIVE non-same team and
rejects victory while any non-vassal OTHER TEAM still owns at least one city:

    if (kVictory.isConquest())
    {
        if (kTeam.getNumCities() <= 0)
            return false;
        for (TeamIter<CIV_ALIVE,NOT_SAME_TEAM_AS> itOther(eTeam);
            itOther.hasNext(); ++itOther)
        {
            if (!itOther->isVassal(eTeam) && itOther->getNumCities() > 0)
                return false;
        }
    }

The blocker is therefore the opposing TEAM, not each member player separately.
Eliminating one member of a multi-member free team does not remove that team's
Conquest obstruction while another member still has cities.

MILITARYVICTORY MODEL

MilitaryVictory::progressRatingConquest() instead begins:

    if (militAnalyst().isEliminated(eThey) ||
        militAnalyst().getCapitulationsAccepted(eOurTeam).count(eTheirTeam))
    {
        log("They're conquered entirely");
        return 1;
    }

`eThey` / `isEliminated(PlayerTypes)` is player-local. Thus for target team B+C:

* military simulation predicts all B cities lost;
* C remains alive, free, and owns cities;
* actual CvGame::testVictory still sees team B+C as a Conquest blocker;
* B's MilitaryVictory pass nevertheless logs "They're conquered entirely" and
  returns the maximum 1.0 Conquest progress;
* C is then evaluated in its own player pass as well.

This is not merely a different heuristic scale: a state labeled 100% Conquest
progress by the aspect is explicitly insufficient for the engine's Conquest
victory condition.

`countFreeRivals<false>()` also iterates PlayerTypes rather than blocking teams,
which reinforces the player-domain approximation in the partial-progress path.
Distinct member losses can of course be genuinely incremental, so this does NOT
prove that all partial Conquest asset loss should simply be deduplicated. The
hard defect is that the full-completion condition and its surrounding progress
normalization do not respect the team-level victory blocker.

ANCESTRY

The original local UWAI introduction already contains:

    if(m->isEliminated(theyId) ||
       m->getCapitulationsAccepted(agentId).count(TEAMID(theyId))) {
        log("They're conquered entirely");
        return 1;
    }

so player elimination -> full Conquest progress existed from birth. Base
AdvCiv 1.14's current `progressRatingConquest` is textually identical to current
SAS for this behavior. Later numeric/scaled refactors did not repair the domain.

F127 NORMALIZED ROOT AFTER BROADENING

MilitaryVictory measures shared global/team victory progress through independent
rival-player passes and applies nonlinear completion/caps before those passes are
combined. Proven manifestations now are:

1. Domination: the same global remaining population/land threshold can be
   independently saturated on multiple rival-player passes;
2. Diplomacy: the same global remaining vote threshold can be independently
   saturated on multiple rival-player passes;
3. Conquest: eliminating one member is treated as full 1.0 progress although
   the member's still-free team can continue to block the actual victory.

Repair should move completion/saturation to the same team/global domain as the
victory condition, while preserving genuinely additive marginal gains from
separate players. Do not mechanically divide every player-local loss by team
size.


F133 / provisional KI#456 -- CONFIRMED -- INHERITED SUPPORTED-CONFIG RATIO DEFECT
EXCLUSIVE-RADIUS FLIPPED-TILE LOSS IS ADDED TO LOSS NUMERATOR AFTER THE TOTAL
ASSET DENOMINATOR IS FROZEN; LOSS RATIOS CAN EXCEED 100%
-------------------------------------------------------------------------------
Status: NEW CURRENT SUPPORTED-CONFIGURATION DEFECT. Shipped/default SAS sets
`OWN_EXCLUSIVE_RADIUS=0`, so defaults mask it; the option is nevertheless an
explicit maintained AdvCiv configuration, documented in GlobalDefines_advc.xml:

    [advc.035] If set to 1, tiles that are only within the radius of one city
    belong to the owner of that city regardless of culture. Recommended: 0 ...

The audit therefore treats value 1 as a supported non-default configuration,
not as an invented unsupported retuning.

ROOT ORDERING DEFECT

WarUtilityAspect::netLostRivalAssetScore accumulates city/nuke loss into
`rNetLoss` and city assets into `rTotal`. It then deliberately raises total to
cover non-city nuke loss:

    rNetLoss += lossesFromNukes(eThey, eTo);
    rTotal.increaseTo(rNetLoss);

After that point it computes blockade loss from `(rTotal - rNetLoss)`, adds the
blockade component, and only THEN adds advc.035 flipped-tile loss:

    rNetLoss += rFromBlockade;
    rNetLoss += lossesFromFlippedTiles(eThey, eTo); // advc.035
    if (prTotalScore != NULL)
        *prTotalScore = rTotal;

Thus flipped-tile loss contributes to the numerator returned to every caller,
but never to the `prTotalScore` denominator returned alongside it.

The mismatch is not bounded elsewhere. UWAICache::updateLostTilesAtWar counts
all contested plots that the owner team would lose under OWN_EXCLUSIVE_RADIUS;
it stores that integer with no upper cap. lossesFromFlippedTiles then uses:

    if (abs(iVictimLostTiles) <= 4 * iRivalMembers)
        return 0;
    r = scaled(125 * iVictimLostTiles,
               max(5, GET_PLAYER(eVictim).getTotalLand()));
    r /= iRivalMembers;

For a solo rival, just five qualifying lost tiles clear the threshold. On a
small empire the flipped-tile score can therefore be of order 125 while the
asset denominator is independently based on visible/deduced city asset scores.
There is no invariant requiring that city-based denominator to be >= the later
flipped-tile score.

EXPLICIT INTERNAL INVARIANT FAILURE -- Assistance

Assistance::assistanceRatio() is the clearest consumer:

    scaled rTheirAssets;
    scaled rTheirLostAssets = netLostRivalAssetScore(NO_PLAYER, &rTheirAssets);
    if (rTheirAssets <= 0)
        return 0;
    scaled r(rTheirLostAssets / rTheirAssets);
    r.increaseTo(0);
    FAssert(r <= 1);

The source explicitly asserts that this loss ratio cannot exceed 100%. With
advc.035 enabled, numerator-only flipped-tile loss invalidates that assertion.
In non-assert builds the value can continue above 1, so Assistance can value a
non-eliminated partner as losing more than 100% of the denominator used to
measure its assets. If capitulation is also predicted, the subsequent

    r += 0.5 * (1 - r)

operates on an already out-of-domain ratio rather than repairing the root.

OTHER CONSUMERS / CONSEQUENCE SCOPE

This shared helper/denominator mismatch is not Assistance-only:

* Loathing::lossRating uses `rTheirLostAssets / rTheirAssets` as one of its
  relative-loss terms; flipped tiles can inflate it beyond the asset-total
  domain that `rTheirAssets` purports to normalize.
* MilitaryVictory::progressRatingConquest uses the same pair; it later caps its
  partial ratio to 1, masking the overflow numerically but still saturating
  Conquest progress from a numerator that the denominator never included.
* IllWill's punishment ratio likewise uses the returned loss/assets pair,
  although its final utility is capped separately.
* ThirdParty/Risk-related helper consumers that ask for a total score inherit
  the same numerator/denominator inconsistency whenever this advc.035 component
  is included.

One root should repair the shared helper rather than patching the Assistance
assert alone.

ANCESTRY

The local advc.035 introducing diff added exactly:

    r += lossesFromFlippedTiles(theyId, to);
    if(returnTotal != NULL)
        *returnTotal = total;

with the flipped-tile addition inserted after `total` had already been fixed.
The helper introduced in that same commit already used the 125 * lostTiles /
landTiles weighting and per-rival-team-member split. Thus the domain mismatch
was introduced with advc.035 itself, not by SAS.

Base AdvCiv 1.14 retains the same ordering and Assistance assertion. Current SAS
also retains it; only formatting/modern accessor details differ in the helper.

REPAIR DIRECTION

If flipped-tile loss is to be represented in the same "fraction of total
assets lost" ratios, the denominator must be extended consistently with that
asset component before it is returned, or the tile effect must be modeled as a
separate bounded dimension rather than silently appended to only the numerator.
Whichever model is chosen, restore the explicit Assistance invariant that a
non-elimination loss ratio lies in [0,1].

Do not merely clamp Assistance to 1: that hides the assertion but leaves
Loathing, Conquest partial progress and other shared-helper consumers with the
same mismatched measurement domains.


ARITHMETIC / STATE-DOMAIN CLOSURE CONTROLS SINCE WIP20
-------------------------------------------------------
- Mechanical balanced-parser scan of every `log(...)` call in
  WarUtilityAspect.cpp found exactly one printf-style argument-count mismatch:
  the already-established F108/KI#431 UlteriorMotives call. No second varargs
  root was found.
- Affection's reciprocal-looking no-war calculation cannot reach a zero
  denominator in the active path because the no-war percentage is explicitly
  capped at 94 before exponentiation.
- Risk floors its asset denominator with scaled::epsilon(), and the ordinary
  vassal-relative-loss divisions inspected have positive floors/guards.
- The Domination exact-population zero boundary remains F105/KI#428; it was not
  duplicated under the denominator sweep.
- Derived-member lifetime audit found no additional cross-scenario leak:
  WarEvaluator creates a fresh aspect set per scenario. KingMaking clears its
  winner sets per preEvaluate, MilitaryVictory overwrites its vote state,
  IllWill resets its cost per rival, ThirdParty overwrites its pre-evaluation
  power fields, and DramaticArc resets tension. Revolts::m_countedCities is the
  intentional persistent exception whose incompatible local normalization is
  already F132/KI#455.
- LoveOfPeace would repeat its fixed per-war pacifism subtraction across rival
  teammates, but all current SAS leaders resolve to iLoveOfPeace=0 and AdvCiv
  documents the aspect as unused except by modmods. Unlike OWN_EXCLUSIVE_RADIUS,
  there is no current enabled/supported data reproduction to justify a new KI;
  keep it as dormant robustness information only.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP21
AUTHORITATIVE BROADENING IN THIS APPEND: F127 / provisional KI#450 (Conquest)
NEW ROOT IN THIS APPEND: F133 / provisional KI#456
NEXT UNUSED ROOT: F134

Continue exact assertion/boundary and pointer/cache-contract checks, then run an
adversarial family re-challenge of F122-F133 before deciding whether file 014 is
ready for durable C015 closure. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP22 APPEND -- WarUtilityAspect.cpp IllWill team-war capability audit
===============================================================================

F134 / provisional KI#457 -- CONFIRMED -- INHERITED PLAYER/TEAM POWER-DOMAIN DEFECT
ILLWILL PRICES TEAM-WIDE REVENGE/DOGPILE MILITARY RISK THROUGH ONE PLAYER'S
ARMY POWER, SO NONLINEAR THRESHOLDS DEPEND ON HOW A TEAM'S POWER IS PARTITIONED
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED DEFECT ROOT. The diplomatic motive represented by
IllWill can legitimately differ by player, but the military capability used to
cross its revenge/dogpile danger thresholds does not match the team-wide war
that those motives can trigger.

PLAYER-LOCAL POWER HELPER

IllWill::theirToOurPowerRatio() computes:

    scaled rOurPow = ourCache().getPowerValues()[ARMY]->power() +
            militAnalyst().gainedPower(eWe, ARMY);
    ...
    scaled rTheirPow = kThey.uwai().getCache().getPowerValues()[ARMY]->power() +
            militAnalyst().gainedPower(eThey, ARMY);
    ...
    return rTheirPow / scaled::max(10, rOurPow);

Both sides are therefore one PlayerTypes member at a time. The nearby comment
only says not to include vassals; it does not say that ordinary teammates should
be excluded from the military side.

The helper is then fed into NONLINEAR gates in evalRevenge:

    if (rTheirToOurPow > 1.45 && ...team war-success tests...)
        ... hopeless-stalemate cost ...

and, for prospective revenge after our simulated DoW:

    if (rTheirToOurPow > 0.7)
        rRevengeCost += 4;

    scaled rPowRatioMult = rTheirToOurPow - 1;
    rPowRatioMult.clamp(0, 1);
    rRevengeCost += rPowRatioMult * 28 * rAttitudeMult;

Because these thresholds/clamps are applied BEFORE separate rival-player
contributions are combined, evaluating members independently is not equivalent
to evaluating the military side that would actually fight the war.

DETERMINISTIC TEAM-PARTITION REPRODUCTION

Take an agent side with 100 relevant ARMY power and a prospective enemy TEAM
with two ordinary teammates B and C, each with 60 ARMY power after the military
analysis. Ignore human-confidence modifiers and give both the same ordinary
attitude for the control.

Current IllWill evaluates B and C separately:

    B : 60 / 100 = 0.60 -> below 0.70 -> no fixed revenge-danger cost
    C : 60 / 100 = 0.60 -> below 0.70 -> no fixed revenge-danger cost

The power-ratio multiplier is also clamped from (0.60 - 1) to zero on both
passes. Thus the military-capability part of prospective revenge contributes
zero.

Now place the exact same 120 enemy ARMY power in one member D of an otherwise
equivalent enemy team:

    D : 120 / 100 = 1.20 -> passes 0.70, adds the fixed 4,
                             and has a positive (1.20 - 1) multiplier.

The same team-side military capacity therefore changes IllWill solely because
it is split among teammates. Summing two player-local results cannot repair a
threshold that has already zeroed both members.

The ongoing-war 1.45 branch has the same mismatch and an especially clear
mixed-domain structure: the surrounding war-success conditions are explicitly
team-on-team (`kTheirTeam.AI_getWarSuccess(eOurTeam)`,
`kOurTeam.AI_getWarSuccess(eTheirTeam)`, and
`kTheirTeam.AI_getWarSuccessRating()`), while the decisive "they're far more
powerful than we are" gate is still the current rival player's army divided by
the current agent player's army.

TEAM-WAR CONTRACT / INTERNAL CONTROL

This is not merely a preference for different UWAI granularity.
CvTeam::declareWar accepts TeamTypes eTarget, gathers MemberIter populations
from BOTH teams and then executes:

    setAtWar(eTarget, true);
    kTarget.setAtWar(getID(), true);

A revenge war initiated by any one member therefore commits the member's
ordinary teammates and their military capacity too.

The inherited non-UWAI war/peace logic uses that same domain. For example,
CvTeamAI::AI_endWarVal compares:

    getPower(true)

against the opposing team's defensive power. CvTeamAI::AI_doWar likewise
computes `iOurPower = getPower(true)` and tests target-team defensive power when
selecting wars. CvTeamAI::AI_getAttitude(TeamTypes) separately averages the
member-to-member diplomatic values. Those functions provide a useful design
control: diplomatic motivation can be assembled from player attitudes while
military capability is team power.

IllWill currently preserves the player-specific motive (`towardUs()`, human
status, trade/partner consequences) but also narrows the capability signal to
that same player. It is the latter transition that creates the partition bug.

SECOND CONSUMER -- ANGERED PARTNERS

IllWill::evalAngeredPartners() reuses theirToOurPowerRatio() in the part that
models exposure to a dogpile after worsening a partner's attitude:

    ... + (bWillDisplease ? min(14.5, 9 * SQR(rTheirToOurPow)) : 0);

The relationship penalty itself is appropriately player-specific, but the
extra danger term is again the military ability of a TEAM-WIDE war response
measured through one member's army. The square makes this nonlinear too, so
member-by-member summation is not invariant under redistributing the same team
power. This is a second manifestation of the same helper-domain root, not F135.

ANCESTRY

The original local UWAI introduction already implemented evalRevenge with
player-local `ourCache` / `theirCache` ARMY power plus player-local gainedPower,
then applied the 0.7 revenge threshold. Later AdvCiv added the ongoing-war
hopeless-stalemate branch but reused the same player-local power ratio alongside
team war-success state.

Current SAS and Base AdvCiv 1.14 have byte-for-byte identical implementations
of all three relevant IllWill members:

* IllWill::evalRevenge
* IllWill::theirToOurPowerRatio
* IllWill::evalAngeredPartners

This is therefore inherited AdvCiv behavior, not a SAS regression.

REPAIR DIRECTION

Keep player-specific relationship/motivation inputs where they belong, but
compute the military-capability ratio over the alive ordinary members of the
relevant teams before applying the 0.70 / 1.45 thresholds and nonlinear squared
or clamped power terms. The existing comment intentionally excludes vassals;
that policy can remain unless separately redesigned.

Human-confidence adjustment needs care in mixed human/AI teams: aggregate the
physical/military side first and apply confidence consistently to the relevant
human-controlled component or team policy rather than accidentally multiplying
the entire mixed team because the current rival member happens to be human.

Do not merely normalize the finished per-player utility by member counts. Once
a nonlinear gate has independently converted two 0.60 members to zero, a later
divisor cannot reconstruct the 1.20 team-side threat.


ASSERTION / POINTER / OWNERSHIP-DOMAIN CONTROLS SINCE WIP21
------------------------------------------------------------
- The remaining FAssert/FAssertMsg sites were challenged against their producer
  contracts instead of being promoted merely because an assertion looked
  fragile. No independent assertion-only root was established in this pass.
- Risk's relative-vassal-loss assertion can see awkward asset-score edge cases,
  but release behavior explicitly clamps the ratio; no independent current
  correctness failure beyond already-recorded Risk roots was proved.
- A suspicious KingMaking wrong-object read was deliberately rechecked against
  the Album and is already F109/KI#432. It was not rediscovered as F134.
- GreedForVassals capital-pointer uses were traced through current player-alive /
  founded-city lifecycle. No supported current null-capital dereference was
  proved from the inspected path.
- Open-Borders/Assistance/Fidelity/Rebuke/BorderDisputes-style player-local
  relationship values remain defensibly per-partner unless a separate
  team-keyed or physical-identity invariant is present; they were not folded
  mechanically into the recent team-partition findings.
- Affection's existing team-game normalization is historical and internally
  deliberate. No contradiction strong enough for another root was established.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP22
NEW ROOT IN THIS APPEND: F134 / provisional KI#457
NEXT UNUSED ROOT: F135

Continue the pointer/cache-producer checks and exact state/boundary sweep, then
re-challenge F122-F134 as a family for overgeneralization or hidden shared roots
before deciding whether file 014 deserves durable C015 closure. No repository
source files were modified.
===============================================================================


===============================================================================
C014-WIP23 APPEND -- ThirdParty decisive-target scope + cache contract controls
===============================================================================

F135 / provisional KI#458 -- CONFIRMED -- INHERITED ADVCIV TARGET-SCOPE DEFECT
THIRDPARTY "WINNING DECISIVELY" SHORTCUT CAN FIRE WHILE TARGET TEAM SURVIVES
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED DEFECT ROOT. Static evidence strong. Independent
of F124/KI#447's later intervention-severity power gate and of F119/KI#442's
raw player/team vector-index bug.

ROOT CONTRACT

ThirdPartyIntervention::evaluate() contains an early-return shortcut documented
as:

    /*  When already at war and winning decisively, try to wrap it up
        before worrying about 3rd parties. */

The shortcut is enabled while the WarEvaluator is considering peace with its
specific TeamTypes target eTarget:

    (m_kParams.isConsideringPeace() && eTarget != NO_TEAM &&
    (militAnalyst().isEliminated(GET_TEAM(eTarget).getLeaderID()) ||
     !militAnalyst().getCapitulationsAccepted(eOurTeam).empty()))

Both predicates are only PROXIES for the stated condition. Neither actually
establishes that the evaluated target team has been decisively defeated.

MANIFESTATION A -- HYPOTHETICAL ELIMINATION OF THE CURRENT REAL TEAM LEADER

CvTeam::getLeaderID() simply returns the team's stored m_eLeader. The real game
updates that member through CvTeam::updateLeaderID when actual player state
changes. MilitaryAnalyst/InvasionGraph, however, is a hypothetical simulation;
it does not kill real CvPlayers and does not mutate CvTeam::m_eLeader.

MilitaryAnalyst::isEliminated accepts a PlayerTypes and tests exactly that
player's simulated city losses:

    InvasionGraph::Node const* pNode = m_pInvGraph->getNode(ePlayer);
    ...
    return (((int)lost.size()) == GET_PLAYER(ePlayer).getNumCities());

Therefore, for a current target team T={B,C} whose stored real leader is B, the
simulation can predict:

    B : all cities lost -> isEliminated(B) == true
    C : cities remain   -> target team T still survives and fights

while:

    GET_TEAM(T).getLeaderID() == B

still holds because the simulation never runs CvTeam::updateLeaderID on the
real team. The shortcut returns immediately and ThirdPartyIntervention assigns
zero intervention cost even though the target TEAM has not been wrapped up.

This is not merely a stale-leader corner case. Permanent/multi-member teams are
represented player-by-player in InvasionGraph and MilitaryAnalyst; individual
members can be eliminated independently. F127's independently proven Conquest
mismatch already provides another control that one player's elimination does
not mean the whole non-vassal team has ceased to block team-level state.

Moreover, InvasionGraph's capitulation test itself requires the defending real
team to have at most one currently alive member (`kDefTeam.getAliveCount() <=
1`). Thus a two-alive-member target team can reach precisely the state above --
one member predicted eliminated while another survives -- without target-team
capitulation rescuing the intended shortcut premise.

MANIFESTATION B -- ANY CAPITULATION TO OUR TEAM, NOT THE TARGET'S CAPITULATION

The second predicate is broader still:

    !militAnalyst().getCapitulationsAccepted(eOurTeam).empty()

The result is a TeamSet of every simulated team whose capitulation was accepted
by eOurTeam. It is not filtered by eTarget.

MilitaryAnalyst explicitly includes wars other than the evaluated target war.
Its constructor gathers every player currently at war into `currentlyAtWar`,
constructs the InvasionGraph from those wars, and, in the WAR scenario of a
peace evaluation, continues the existing wars. In the recursive PEACE scenario
it removes the evaluated target side while unrelated current wars remain.

Consequently a concrete solo-agent control is sufficient and avoids any
interaction with F119's multi-member result-storage defect:

    A is currently at war with target team T and unrelated enemy team U.
    WarEvaluator is considering peace with T.
    MilitaryAnalyst predicts U capitulating to A.
    T itself remains undefeated / does not capitulate.

Then:

    getCapitulationsAccepted(Ateam) == { U }

is nonempty, so ThirdPartyIntervention returns under the comment that A is
"winning decisively" and should "wrap it [the target war] up." U's unrelated
capitulation does not establish that premise for T.

This manifestation survives a correct F119 repair. F119 changes which TeamTypes
slot the accepted-capitulation set is stored in; F135 is that the correctly
retrieved set is queried with `.empty()` instead of asking whether the relevant
target team is the capitulating team.

HISTORY / ANCESTRY

Reachable AdvCiv history practical 3485 / commit f39e13df87 is titled:

    Add ThirdPartyIntervention war utility aspect

and introduces this exact early-return condition, including both the current
real target leader's PlayerTypes elimination and the unfiltered nonempty
capitulations-accepted set. No later history correction found in the local
reachable diffs changes the scope of this shortcut.

Base AdvCiv 1.14 contains the same block verbatim. Current SAS also retains it
unchanged. This is therefore inherited from the aspect's introduction, not a
SAS regression.

WHY THIS IS ONE ROOT AND NOT TWO

Both manifestations violate the same documented predicate: "the evaluated
TARGET war is already being won decisively." The first substitutes one current
representative player for the target team; the second substitutes any accepted
capitulation for the target team's capitulation. Either can make the shortcut
true while the target remains an active opponent.

The repair should therefore make the shortcut target-scoped rather than patch
its two syntax shapes independently.

REPAIR DIRECTION

For the elimination side, require that the evaluated target team is actually
eliminated in the simulated state -- e.g. all relevant alive target-team member
nodes are eliminated -- rather than asking about the real team's pre-simulation
leader player.

For capitulation, test the evaluated target specifically, e.g. the target's
simulated capitulation state or membership of eTarget in the accepted set,
rather than `.empty()` on all capitulations accepted by eOurTeam.

Keep the existing high-level policy if desired: when the TARGET really is
nearly/fully defeated, suppressing concern about third-party intervention can
still be intentional. F135 is about the predicates failing to prove that
condition.

Do not fold this into F124/KI#447. F124 remains the separate later nonlinear
power-threshold domain mismatch for the prospective third party eThey; fixing
that power calculation does nothing to this early target-war shortcut.


CACHE / POINTER / SCENARIO CONTRACT CONTROLS SINCE WIP22
---------------------------------------------------------
- InvasionGraph::Node documents that city plots placed in its conquest/loss
  result sets are guaranteed to exist, at the time of the call, in the agent's
  UWAICache. Node::isValidTarget additionally rejects a target city when
  `m_kAgentCache.lookupCity(kCacheCity.getID()) == NULL`. This validates the
  otherwise alarming unchecked `ourCache().lookupCity(*it)` dereferences in
  WarUtilityAspect consumers of MilitaryAnalyst conquest/loss sets; no null
  pointer root was manufactured from those sites.
- UWAICache::updateCities provides the broader known/deduced-city population,
  while InvasionGraph imposes the stronger per-target cache-membership contract.
  The producer/consumer chain is therefore explicit rather than accidental.
- GreedForVassals' capital dereferences remain guarded by either direct
  hasCapital checks or a preceding city-count/eliminated/capitulated filter;
  no supported current null-capital path was established.
- The Risk break-away player-local power denominator was rechecked and is
  already recorded as a secondary team-partition manifestation under
  F130/KI#453; it was not rediscovered as a new root.
- F135 deliberately does not rely on F119/KI#442. Its unrelated-capitulation
  reproduction can use a solo agent team, and its target-leader manifestation
  concerns the PlayerTypes-vs-target-TeamTypes predicate before result storage.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP23
NEW ROOT IN THIS APPEND: F135 / provisional KI#458
NEXT UNUSED ROOT: F136

Continue the remaining scenario-state / ownership-domain sweep and then perform
an explicit adversarial normalization pass over F122-F135 (shared-root check,
repair-independence check, and current-reachability challenge) before deciding
whether file 014 is ready for durable C015 closure. No repository source files
were modified.
===============================================================================


===============================================================================
C014-WIP24 APPEND -- partner-tech ownership + recent-root normalization matrix
===============================================================================

F136 / provisional KI#459 -- CONFIRMED -- INHERITED TEAM-TECH PARTNER DUPLICATION
PARTNERUTILFROMTECH COUNTS THE SAME TEAM-OWNED FORESEEABLE TECHNOLOGY TRADES
ONCE PER FRIENDLY TEAMMATE
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED DEFECT ROOT. Narrowly scoped to the team-owned
technology opportunity inside the partner helper; does NOT claim that trade,
resource, military, attitude or commerce partner value is generally identical
between teammates.

TEAM-OWNED INPUT INSIDE A PLAYER-PARTNER HELPER

WarUtilityAspect::partnerUtilFromTech() is called with the current rival player
eThey, and appropriately contains some player-specific inputs such as human
status, pairwise attitude and estimated player commerce. But its concrete
near-future technology inventory is read from the two TEAMS:

    bool bWeHaveIt = (kOurTeam.isHasTech(eTech) ||
            kOurTeam.getResearchProgress(eTech) > 0);
    bool bTheyHaveIt = (kTheirTeam.isHasTech(eTech) ||
            (bCanSeeResearch && kTheirTeam.getResearchProgress(eTech) > 0));

    if (bWeHaveIt && !bTheyHaveIt)
        iWeCanOffer++;
    else if (bTheyHaveIt && !bWeHaveIt)
        iTheyCanOffer++;

and then:

    scaled rNearFutureTrades = std::min(iWeCanOffer, iTheyCanOffer);
    ...
    r += fixp(10/3.) * scaled::min(3, rNearFutureTrades);

For ordinary teammates B and C, `kTheirTeam` is the exact same CvTeam. Known
technologies and research progress are team state. With identical visibility /
no hidden-progress complication, the `iWeCanOffer`, `iTheyCanOffer` and
`rNearFutureTrades` sets/counts are therefore the same on B's and C's passes.

REAL TRADE EFFECT IS TEAM-OWNED TOO

CvPlayer::canTradeItem(TRADE_TECHNOLOGIES) checks source and recipient through
team state:

    kOurTeam.isHasTech(eTech)
    !kOurTeam.isNoTradeTech(eTech)
    !kToTeam.isHasTech(eTech)

and CvDeal::startTrade applies a technology trade with:

    GET_TEAM(eToPlayer).setHasTech(..., TECH_ACQUISITION_TRADE);

Thus one actual trade through member B changes the recipient TEAM's technology
ownership. If B and teammate C both currently provide the same technology X,
X is not two independently consumable trade opportunities: after it is traded
through either member, the shared team-tech difference is gone.

The denial implementation reinforces that technology willingness itself has a
team contract. CvPlayer::getTradeDenial routes TRADE_TECHNOLOGIES to:

    kOurTeam.AI_techTrade(eTech, TEAMID(eWhoTo));

and CvTeamAI::AI_techTrade applies team attitude plus member-personality rules.
This does not mean every player-facing approximation inside partnerUtilFromTech
must be discarded; it does make the team ownership of the technology inventory
non-optional.

CONCRETE DUPLICATION THROUGH ILLWILL

A team-wide declaration of war against T={B,C} causes MilitaryAnalyst to mark
war against both living members, and WarUtilityAspect evaluates IllWill once for
B and once for C. In `IllWill::evalLostPartner`, each new-war member contributes:

    partnerUtilFromTrade() +
    (bTheyCapitulated ? 0 : partnerUtilFromTech()) +
    partnerUtilFromMilitary();

Choose B and C as equally friendly AI teammates, both visible, with equal
player-local commerce for the simple control, and suppose the shared T tech
inventory versus our team yields three foreseeable near-future trades.

Then B's `partnerUtilFromTech` receives the bonus for those same three shared
team-tech differences, and C's pass receives it again. The war does end the
technology partnership with T, but it does not destroy two copies of each team
technology or two independent chances to exchange the same three differences.

Even without requiring the helper's commerce-derived base term to be treated
team-wide, the `rNearFutureTrades` component is deterministically duplicated.
That is sufficient for the root.

SECOND CONSUMERS

The same shared helper also feeds:

* Assistance: expected loss of a trading partner is weighted by
  `sqrt(assistanceRatio) * partnerUtilFromTech()`. When multiple members of one
  partner team are damaged, the same team technology inventory can be valued
  repeatedly even though surviving teammates may preserve the team-tech trade
  channel.
* IllWill::evalAngeredPartners: the cost assigned to alienating a prospective
  partner includes `partnerUtilFromTech()` once per current rival player.

These are manifestations of the helper's ownership mismatch, not additional
F-numbers.

NEGATIVE CONTROL -- PARTNERUTILFROMTRADE IS NOT THE SAME ROOT

Do not globalize the entire partner-value family. `partnerUtilFromTrade()`
walks CvDeals between the two actual players, player-owned bonus trades and
trade routes whose partner city owner is exactly eThey. Those quantities can
legitimately differ and add across teammates. F104's resource-item counting bug
is separate.

Likewise, player-specific relationship/willingness and commerce contribution in
partnerUtilFromTech can remain relevant. F136 is specifically that a shared
technology inventory / set of foreseeable tech differences is converted into a
per-player reusable opportunity.

RELATION TO F129 / KI#452

F129 established another team-tech duplication in a DIFFERENT producer and
consumer chain: UWAICache stores prospective-vassal current-tech value in a
PlayerTypes-keyed `m_aiVassalTechScore`, then acceptVassal sums that score over
vassal members.

F136 shares the technology-ownership principle but not the repair locus:

* repairing/rekeying `m_aiVassalTechScore` does not change partnerUtilFromTech;
* repairing partnerUtilFromTech does not change the vassal-score cache or
  acceptVassal member sum.

Under the Album's one-practical-root rule these therefore remain separate roots,
with the shared domain fact recorded as a cross-control rather than merging two
independent implementations.

RELATION TO F103 / KI#426

F103's current SAS repair fixes a concrete old wrong-object bug where the helper
ignored its ePlayer argument and used the AGENT leader's tech-refusal threshold
for the rival too. F136 does not retract that repair: selecting the queried
player's personality is strictly more coherent than substituting our leader.

The full live tech denial is team-level and can be stricter than the helper's
pairwise approximation in permanent teams. That is useful robustness context,
but F136 does not require promoting a second willingness-threshold defect; its
proof uses equally friendly teammates so willingness cannot explain the
inventory duplication.

ANCESTRY

The original local AdvCiv/UWAI introduction of partnerUtilFromTech already
iterated team technology ownership/research progress while exposing the result
through a rival-player partner call. The same near-future-trade construction
survives through Base AdvCiv 1.14.

Current SAS changed `techRefuseThresh` for F103/KI#426 but did not alter the
team-tech inventory counting or add a canonical partner-team allocation. The
F136 root is therefore inherited rather than created by the recent SAS repair.

REPAIR DIRECTION

Separate the helper's genuinely team-owned technology opportunity from its
player-specific relationship/economic modifiers.

A robust design could compute foreseeable shared technology differences once
per partner team, then assign/weight that team opportunity according to whether
there is at least one viable trading contact / the team's actual denial model.
Alternatively, keep player callbacks but maintain a per-team allocation so the
same current/progress tech differences cannot be consumed again by the next
teammate pass.

Do not simply divide the entire partnerUtilFromTech result by team size. Team
members can differ in attitude, human status, commerce contribution and contact
viability; only the shared inventory/opportunity needs identity-aware handling.


ADVERSARIAL NORMALIZATION PASS -- F122 THROUGH F136
----------------------------------------------------
Purpose: challenge the recent high-density team/state findings for accidental
splitting of one code defect, generic "team games are different" reasoning, or
repairs that would automatically fix several numbered roots.

RESULT: no retraction or mandatory merge established. The roots fall at distinct
state owners / nonlinear operations:

F122 / KI#445 -- TacticalSituation
    Shared scarce object: one agent tactical unit is independently consumed by
    per-rival entanglement caps. Repair locus is TacticalSituation engagement
    accounting / shared unit identity.

F123 / KI#446 -- Bellicosity
    Shared budget: the same agent lost-power budget is independently reused in
    the historical anti-third-party `min` safeguard. Repair locus is
    Bellicosity's loss aggregation before the cap.

F124 / KI#447 -- ThirdPartyIntervention severity
    Domain switch: intervention probability is team-aware, then the nonlinear
    50% severity gate uses one prospective intervener member's ARMY. Repair
    locus is the later threat-strength ratio/gate.

F125 / KI#448 -- DramaticArc
    Team-wide tension transition is licensed through player-local human-safety
    and minimum-action gates. Repair locus is DramaticArc's gate aggregation.

F126 / KI#449 -- Distraction
    Team-war opportunity cost / fixed current-war-plan nudge is charged once per
    target member. Repair locus is canonical hostile-team evaluation of the
    Distraction cost.

F127 / KI#450 -- MilitaryVictory
    Global/team victory completion budgets are reset per rival player; Conquest
    also confuses one player elimination with team-level victory progress.
    Repair locus is scenario/global victory-progress aggregation.

F128 / KI#451 -- HiredHand
    Sponsor/bounty/history obligation is keyed by TeamTypes but consumed per
    target player. Repair locus is team-keyed obligation accounting.

F129 / KI#452 -- vassal current-tech cache
    Team-owned existing technology value is cached under PlayerTypes and summed
    over prospective vassal members. Repair locus spans UWAICache vassal-tech
    storage and its explicit member-summing consumers.

F130 / KI#453 -- Risk
    Team-owned capitulation/vassal state costs are emitted through agent/rival
    member passes, including a fixed 100 capitulation cost. Repair locus is Risk
    team-state charging; ordinary player asset losses must remain separate.

F131 / KI#454 -- GreedForSpace
    Physical identity: the same plot can exist in two player city-site lists and
    be counted twice. Repair locus is plot-ID union/deduplication, not team-size
    normalization.

F132 / KI#455 -- Revolts
    Persistent uniqueness state (`m_countedCities`) is combined with reset local
    numerator/denominator state, causing player-iteration-order dependence.
    Repair locus is ratio/uniqueness lifetime alignment.

F133 / KI#456 -- shared loss helper
    Supported OWN_EXCLUSIVE_RADIUS tile loss augments only the numerator after
    total assets are frozen. Repair locus is the shared asset-measurement domain,
    independent of team-member iteration.

F134 / KI#457 -- IllWill
    Player-local ARMY capability crosses nonlinear revenge/dogpile thresholds
    for a team-wide war. Repair locus is IllWill's military-capability helper;
    diplomatic motive remains player-specific.

F135 / KI#458 -- ThirdPartyIntervention decisive-target shortcut
    Target-scoping failure: one pre-simulation leader player's elimination or
    ANY capitulation to us is accepted as decisive victory over eTarget. Repair
    locus is the early target-war shortcut. It is in the same aspect as F124 but
    a correct F124 power repair cannot affect this branch.

F136 / KI#459 -- partnerUtilFromTech
    Team-owned technology differences are exposed as reusable per-player partner
    opportunities. Repair locus is the shared partner-tech helper and its
    allocation; F129's vassal-tech cache repair cannot affect it.

WHY THESE ARE NOT ONE GENERIC ARCHITECTURE KI

WarUtilityAspect intentionally uses pairwise player evaluation. Numerous clean
controls in the same file genuinely require player ownership: resource deals,
trade routes, attitudes, city assets, nuke source/victim accounting, distinct
settlement plots, etc. Replacing the base loop with one TeamIter would break
valid semantics.

The recent findings survive only where an additional invariant identifies a
more specific ownership/normalization domain -- a unique physical object, an
explicit TeamTypes-keyed state, a team/global victory threshold, a team-wide war
capability, a persistent uniqueness set, or an explicit target-scoped comment.
Their repairs consequently remain local and independent rather than one common
change to WarUtilityAspect::evaluate.

CURRENT-REACHABILITY CHALLENGE

The team-partition findings use ordinary supported multi-member teams/Permanent
Alliances, not malformed IDs or dead code. Where a finding instead requires a
nondefault rule, it is called out explicitly (F133: documented
OWN_EXCLUSIVE_RADIUS=1). LoveOfPeace remains deliberately unpromoted because
current SAS data sets its personality value to zero throughout; that contrast
continues to enforce the current-reachability bar.

No F122-F136 root was retracted by this normalization pass. This is not a claim
that every possible numerical reproduction is equally severe; it is a claim
that each numbered root has an independent current code condition and repair
locus that survives the challenge.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP24
NEW ROOT IN THIS APPEND: F136 / provisional KI#459
NEXT UNUSED ROOT: F137

Continue the last untouched-helper / scenario-state sweep with particular care
not to promote merely approximate per-player modeling. Then re-run method/header
inventory, Base ancestry and live-ledger collision checks before deciding on
C015 closure. No repository source files were modified.
===============================================================================

LOCAL RECONCILIATION AFTER C014-WIP24 IMPORT

- ChatGPT-5.6-Sol's C014-WIP17 through C014-WIP24 append is compatible with current authority at HEAD 007ac4a3d plus the staged KI#416/KI#417 repairs. The received append remains verbatim as evidence.
- F094 / KI#416 and F095 / KI#417 are now fixed in the staged current source. Their repairs restore dead-teammate technology state and exclude the attacker's master/vassal bloc from target defensive power; the new WarUtility findings do not supersede either repair.
- F129-F136 / provisional KI#452-KI#459 are collision-free against the current reviewed Known Issues ledger, but remain provisional while `WarUtilityAspect.cpp` is open. No implementation is promoted from WIP17-WIP24 in this reconciliation. (GPT-5.6-Sol)


===============================================================================
C014-WIP25 CONTINUATION -- PREEMPTIVEWAR TEAM-THREAT OWNERSHIP + CLOSURE PASS
===============================================================================
STATUS: WIP / append-only continuation of authoritative C014-WIP24.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

F137 / provisional KI#460 -- CONFIRMED -- INHERITED TEAM-THREAT DUPLICATION
PREEMPTIVEWAR CONSUMES A TEAM-OWNED THREAT / SIDE FORECAST ONCE PER RIVAL PLAYER
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT. Static proof strong.

ROOT CONTRACT

PreEmptiveWar::evaluate() is entered through WarUtilityBroaderAspect's ordinary
per-rival-player callback. WarUtilityAspect::evaluate(MilitaryAnalyst const&)
therefore invokes it once for each known potential-enemy PLAYER, including every
living member of the same rival team.

Its first substantive input looks player-keyed:

    scaled const rCurrThreat = ourCache().threatRating(eThey);

but UWAICache::calculateThreatRating(PlayerTypes eRival) ultimately returns:

    return teamThreat(TEAMID(eRival));

apart from an individual rival-cache reachability early return. teamThreat then
uses the rival TEAM's attitude, long-term TEAM/master-side power, victory stage,
and alternative-target count. Thus, whenever two teammates B and C can both
reach our capital, threatRating(B) == threatRating(C): they expose the same
TeamBC threat through two PlayerTypes slots.

PREEMPTIVEWAR THEN RECONSTRUCTS THE SAME TEAM SIDE

The rest of the calculation does not make that shared threat player-specific.
For the current eThey it builds the rival side by looping all major players and
accepting anyone whose:

    kTheirAlly.getMasterTeam() == GET_PLAYER(eThey).getMasterTeam()

(or who is a newly accepted vassal of eTheirTeam).

For teammates B and C, getMasterTeam() and eTheirTeam are identical. Therefore
both callbacks aggregate the same current/predicted B+C+vassal city side. Within
one MilitaryAnalyst evaluation eWe is fixed too, so the current/predicted agent
side, rTheirEdge, threat-change transform, rival-team victory-stage modifier,
and agent distrust factor are also identical.

DETERMINISTIC REPRODUCTION

Take one agent player A and a two-member rival team B+C. Assume:

* B and C are both known potential enemies and both can reach A's capital;
* teamThreat(TeamBC) is above the 0.15 gate;
* the simulated scenario changes TeamBC's side enough for rTheirEdge != 0.

B pass:

    rCurrThreat = teamThreat(TeamBC)
    rival city side = B + C + their vassals
    -> utility X

C pass:

    rCurrThreat = teamThreat(TeamBC)
    rival city side = B + C + their vassals
    -> the same utility X

The aspect total receives 2X. Moving the same players into one-member teams, or
merely changing team membership while holding the combined military/economic
side fixed, changes the long-term-threat utility because the same TeamBC threat
is exposed once per member.

PLAYER-SPECIFIC REACHABILITY DOES NOT RESCUE THE MODEL

calculateThreatRating has one player-local gate before returning teamThreat: it
checks whether the particular rival player's cache says our capital is
reachable. This can make one teammate's slot zero and another's nonzero, but it
does not justify counting the common team threat twice when both are reachable.
It also exposes the deeper ownership mismatch: a team capability is cached under
PlayerTypes after filtering it through one member's reachability.

A robust repair should decide team reachability/capability explicitly (e.g. any
material member can project force) and expose/canonicalize the resulting threat
at team scope, rather than relying on whichever player slot is being consumed.

ANCESTRY

This is inherited, not SAS-created.

* Current SAS and Base AdvCiv 1.14 have normalized-identical
  PreEmptiveWar::evaluate().
* Current SAS and Base AdvCiv 1.14 also have normalized-identical
  UWAICache::calculateThreatRating and UWAICache::teamThreat.
* The original local UWAI introduction (reachable-history practical 1253,
  f6ff6a182f) already created a player-indexed threatRatings[civId] entry whose
  calculateThreatRating(civId) returned teamThreat(TEAMID(civId)), and the
  simultaneously introduced PreEmptiveWar iterated rival players while
  rebuilding their master-team/vassal city side.

So the player/team ownership mismatch exists from the feature's birth.

RELATION TO F107 / KI#430

Keep separate.

F107 proves that PreEmptiveWar constructs present/predicted sides
inconsistently: existing vassals disappear from our predicted baseline and a
team-wide current-city base subtracts only the current agent member's losses.
Correcting those baselines does NOT prevent a correct team forecast from being
consumed once for B and again for teammate C.

Conversely, evaluating/caching the long-term threat once per rival team does NOT
repair F107's existing-vassal and teammate-loss asymmetries inside that one team
forecast. The two roots therefore have different invariants and repair loci.

RELATION TO F121 / KI#444

F121's terminal-start manifestation forces calculateThreatRating to zero forever
while currentEra <= startEra. That startup-clock bug can hide F137 in a
terminal-era-start test, but fixing the clock merely allows the duplicated team
threat to become active. The roots are independent.

REPAIR DIRECTION

Prefer a team-owned long-term threat object/calculation and consume its change
once per distinct rival team (or otherwise allocate it exactly once), while
preserving genuinely player-specific diplomatic/economic quantities elsewhere.
If the reachability precondition remains, aggregate it at the same team scope
rather than using one player's cache as a hidden switch for a teamThreat value.
Do not simply divide every PreEmptiveWar result by team size without first
fixing F107's side construction and deciding how team reachability should work.


CLOSURE-PASS NOTE -- DERIVED MEMBER STATE
-----------------------------------------
The first derived-member lifetime inventory after WIP24 did NOT establish a
second root:

* MilitaryVictory::preEvaluate overwrites m_iVotesToGo/m_bEnoughVotes for each
  agent member from the same agent-team victory state.
* KingMaking::preEvaluate explicitly clears both winner sets on every member
  call before repopulating them.
* Risk::m_bSASContactGateEvaluated is intentionally scenario-wide after
  KI#425/F102 so the team-wide hard contact gate fires only once.
* IllWill::preEvaluate/evaluate overwrite the scratch scalar state needed for
  each member before use.
* ThirdPartyIntervention and DramaticArc overwrite their precomputed scalars on
  every member preEvaluate call.
* Revolts::m_countedCities is deliberately scenario-wide; its mismatch with
  reset per-call numerator/denominator state is already F132/KI#455.

This closes the obvious derived-state-lifetime surface without manufacturing a
new finding.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP25
NEW ROOT IN THIS APPEND: F137 / provisional KI#460
NEXT UNUSED ROOT: F138

Continue the untouched helper/scenario-state sweep and method/header inventory,
then run Base-ancestry/current-ledger collision closure before deciding whether
file 014 is ready for durable C015. No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP26 CONTINUATION -- TACTICAL OPERATIONAL READINESS TARGET-TEAM DUPLICATION
===============================================================================
STATUS: WIP / append-only continuation of authoritative C014-WIP25.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

F138 / provisional KI#461 -- CONFIRMED -- INHERITED TARGET-TEAM COST DUPLICATION
TACTICALSITUATION::evalOperational CHARGES ONE AGENT READINESS DEFICIT ONCE PER TARGET MEMBER
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT. Static proof strong.

EXPLICIT INTENT / DOMAIN

TacticalSituation::evalOperational begins with an unusually clear scope comment:

    Evaluate our readiness only for the target that we want to invade
    (not its vassals or allies from def. pact)

and enforces:

    if (m_kParams.getTarget() != eTheirTeam)
        return;

The quantity being priced is therefore the current AGENT PLAYER's operational
readiness for one target TEAM/war plan: attacker count, cargo capacity, escorts,
preparation time and whether the planned target requires naval deployment.

PER-PLAYER CALLBACK STILL REPEATS THAT TEAM COST

TacticalSituation itself remains a normal WarUtilityAspect. Its evaluate() is
called once for every rival player that belongs to the military analysis:

    if (!militAnalyst().isWar(eWe, eThey)) return;
    if (kOurTeam.isAtWar(eTheirTeam)) evalEngagement();
    else evalOperational();

For a new planned war against a multi-member target team B+C, both B and C are
part of the simulated enemy side. Since both have eTheirTeam == target, both
callbacks enter evalOperational.

Within a fixed agent-member MilitaryAnalyst evaluation, almost every material
input is then the same on both passes:

* our attacker UNITAI inventory;
* our logistics/cargo power;
* our sea escort inventory;
* our city count;
* total/limited and naval/land war mode;
* m_kParams.getPreparationTime();
* the initial preparation-time configuration;
* whether our army can bombard.

The target-player inputs do not prevent a deterministic duplicate. Teammates
share team technologies and can trivially be in the same era; choose B and C
both with at least one reachable city and the same current-era factor. Then
rTargetAttackers, readiness, passed preparation portion and the final
`iUnreadinessCost` are identical.

DETERMINISTIC REPRODUCTION

Agent A plans war against TeamBC, which has two living members B and C. A is at
60% operational readiness and has progressed far enough through preparation for
an unreadiness cost X > 0. Both B and C have reachable cities and are in the
same era.

B callback:

    target == TeamBC -> evalOperational
    A readiness = 60%
    -> m_iU -= X

C callback:

    target == TeamBC -> evalOperational
    same A readiness = 60%
    -> m_iU -= X again

The scenario is one war preparation by A against one target team, but its
readiness deficit is charged 2X solely because TeamBC has two players.

This differs from genuinely additive rival-player quantities: A does not have a
second copy of its attack stack, transport capacity or elapsed preparation just
because another teammate owns cities on the same target team.

AGENT TEAM MEMBERS ARE A DIFFERENT DIMENSION

WarEvaluator also evaluates each living AGENT team member separately. That is
not the root here: A and teammate A2 own different unit inventories, so separate
readiness assessments for A and A2 can be meaningful. F138 concerns repeated
charging of one fixed agent member's readiness across multiple members of the
SAME target team.

ANCESTRY

Current SAS and Base AdvCiv 1.14 have normalized-identical
TacticalSituation::evalOperational(). Reachable history shows the original UWAI
introduction (practical 1253 / f6ff6a182f) already implemented the same design:

    if(params.targetId() != TEAMID(theyId)) return;
    ... compute our attackers/cargo/escort/preparation readiness ...
    u -= unreadiness cost

inside the ordinary per-rival-player TacticalSituation callback. The root is
therefore inherited from the feature's birth, not caused by SAS.

RELATION TO F122 / KI#445

Keep separate despite sharing TacticalSituation.

F122 concerns evalEngagement during an EXISTING war: the same physical agent
unit can be consumed independently by rival-specific threat/entanglement caps.
Its repair needs shared physical-unit accounting across rival engagement passes.

F138 concerns evalOperational before the target war exists: one aggregate
agent-force/preparation deficit is charged once per target-team member. Its
repair can simply canonicalize/aggregate the target-team readiness calculation
once for each agent member.

Fixing the engagement unit-identity accounting does not touch evalOperational;
canonicalizing pre-war readiness does not fix ongoing-war unit reuse.

REPAIR DIRECTION

Evaluate operational readiness once per agent member for the configured target
TEAM, not once per target player. Target-specific era/reachability inputs should
also be aggregated at team scope (e.g. relevant/reachable target members) rather
than choosing one arbitrary member if the calculation is canonicalized.

Do not divide the whole TacticalSituation aspect by target team size: the
engagement branch contains genuinely player-owned enemy units/cities and needs
its own F122 identity-aware repair.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP26
NEW ROOT IN THIS APPEND: F138 / provisional KI#461
NEXT UNUSED ROOT: F139

Continue the remaining helper/aspect inventory and full method/header closure,
then run Base-ancestry/current-ledger collision checks before deciding on C015.
No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP27 CONTINUATION -- ILLWILL WAR-ON-FRIEND TEAM-EVENT COUNTING + CLOSURE CONTROLS
===============================================================================
STATUS: WIP / append-only continuation of authoritative C014-WIP26.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

F139 / provisional KI#462 -- CONFIRMED -- INHERITED TEAM-EVENT / PLAYER-SET COUNTING DEFECT
ILLWILL::evalAngeredPartners CAN COUNT ONE DECLARATION-OF-WAR-ON-FRIEND MEMORY ONCE PER VICTIM-TEAM MEMBER
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV DEFECT ROOT. Static producer/consumer proof strong.

ROOT CONTRACT

IllWill::evalAngeredPartners estimates the diplomatic penalty that an outside
player eThey will assign to the AGENT PLAYER eWe for wars newly declared in the
MilitaryAnalyst scenario. The actual relation event being modeled is
MEMORY_DECLARED_WAR_ON_FRIEND.

MilitaryAnalyst, however, represents declarations through player sets:

    PlyrSet const& kWeDecl = militAnalyst().getWarsDeclaredBy(eWe);

PlayerResult::setDoWBy accepts PlayerTypes, and the war-scenario construction
crosses the declaring side with the opposing side. Thus, if A declares war on a
two-member TeamBC, A's player result contains both B and C even though Civ4 has
one team declaration against TeamBC.

IllWill then loops those PLAYER entries and converts each one back to its team:

    for (PlyrSetIter it = kWeDecl.begin(); it != kWeDecl.end(); ++it)
    {
        if (GET_TEAM(*it).isAVassal() || eTheirTeam == TEAMID(*it))
            continue;
        if (kThey.AI_disapprovesOfDoW(eOurTeam, TEAMID(*it)))
        {
            ...
            rPenalties += rPenaltyPerDoW;
        }
    }

For B and C, TEAMID(*it) is the same TeamBC. The same observer predicate and
same fixed -1/-2 memory-attitude weight can therefore be added twice.

REAL DIPLOMACY PATH IS THE DECISIVE CONTROL

CvTeamAI::AI_preDeclareWar receives one `TeamTypes eTarget`. For each living
member of the aggressor team it loops the outside observers once and does:

    if (kPlayer.AI_disapprovesOfDoW(getID(), eTarget))
        kPlayer.AI_rememberEvent(kOurMember.getID(),
                MEMORY_DECLARED_WAR_ON_FRIEND);

A declaration A -> TeamBC therefore gives qualifying observer D one
war-on-friend memory toward A for that victim TEAM. The real code does not loop
B and C and record the same memory twice merely because TeamBC has two players.

This makes F139 stronger than a generic team-game weighting disagreement:
IllWill is trying to predict a concrete diplomacy-memory effect and can predict
more events than the real declaration path creates.

DETERMINISTIC TWO-MEMBER REPRODUCTION

Let A be the current agent player. TeamBC contains living B and C. Outside
player D likes TeamBC enough that:

    D.AI_disapprovesOfDoW(TeamA, TeamBC) == true

and D's leader uses the usual -1 war-on-friend attitude weight.

Military analysis for A's single declaration on TeamBC stores:

    getWarsDeclaredBy(A) = { B, C }

D's IllWill pass then evaluates:

    B entry -> AI_disapprovesOfDoW(TeamA, TeamBC) -> rPenalties += 1
    C entry -> AI_disapprovesOfDoW(TeamA, TeamBC) -> rPenalties += 1

so the forecast is two relation points. The real AI_preDeclareWar call for that
same TeamBC victim records one MEMORY_DECLARED_WAR_ON_FRIEND event toward A.
The downstream partner-loss, dogpile-danger and diplomatic-victory costs are all
scaled from the inflated rPenalties value.

AGENT TEAM MEMBERS ARE NOT THE DUPLICATE

CvTeamAI::AI_preDeclareWar deliberately records the observer memory separately
toward every living member of the AGGRESSOR team. WarEvaluator likewise evaluates
each agent member separately. Therefore A and teammate A2 can legitimately each
incur one memory with D.

The defect is only the VICTIM side: within A's one member evaluation, B and C
are two PlayerTypes representations of one target-team declaration.

CASCADING / DEFENSIVE-PACT WARS

MilitaryAnalyst can also add declarations against Defensive-Pact allies. Those
are distinct victim teams and correspond to additional real team declarations;
they must remain separately chargeable. The correct aggregation is therefore
not "one penalty total" but "one penalty per DISTINCT victim TeamTypes".

A practical repair is to collect/deduplicate TEAMID(*it) from kWeDecl before
calling AI_disapprovesOfDoW and adding rPenaltyPerDoW. Player-local report names
can use a canonical/representative member only for logging.

ANCESTRY

The original local UWAI introduction (practical 1253 / f6ff6a182f) already
implemented IllWill::evalAngeredPartners by iterating
`m->getWarsDeclaredBy(weId)`, which was a PlayerTypes set, and adding one fixed
penalty for every qualifying entry. Later advc.130h replaced the direct
player-attitude test with AI_disapprovesOfDoW(agentId, TEAMID(*it)), making the
team-domain contract even more explicit without deduplicating the player set.

Current SAS and Base AdvCiv 1.14 have normalized-identical
IllWill::evalAngeredPartners implementations for this behavior. The real
AI_preDeclareWar memory path likewise remains team-targeted. F139 is inherited
from UWAI's player-set representation of declarations, not SAS-created.

RELATION TO F134 / KI#457 AND F136 / KI#459

Keep separate.

* F134 concerns IllWill::theirToOurPowerRatio: team-wide revenge/dogpile military
  capability is measured through one player's army before nonlinear thresholds.
* F136 concerns partnerUtilFromTech: the same team-owned technology opportunity
  can be valued once per teammate.
* F139 exists even if military capability and partner-tech value are both fixed:
  the input relation-penalty COUNT itself can contain two predicted memory hits
  for one real team declaration.

REPAIR DIRECTION

Within evalAngeredPartners, transform the PlayerTypes declaration set into the
set of distinct victim teams before applying the war-on-friend observer test and
memory-attitude penalty. Preserve separate penalties for genuinely separate
victim teams (including cascading Defensive-Pact declarations), and preserve
per-agent-member evaluation because the real memory is stored separately toward
each aggressor player.


F114 / provisional KI#437 -- ADVERSARIAL REVALIDATION -- REMAINS LIVE
-------------------------------------------------------------------------------
The closure pass briefly challenged F114's assumption that a target teammate
passes FairPlay's:

    kTheirTeam.isHasMet(kOther.getTeam())

filter. A shallow read of CvTeam::reset would suggest a team does not meet
itself because m_aiHasMetTurn defaults to -1. The complete initialization trace
proves otherwise and therefore REVALIDATES F114 rather than retracting it.

CvGame::initDiplomacy explicitly executes for every team:

    itTeam->meet(itTeam->getID(), false);

CvTeam::meet then calls makeHasMet on that same team object; the first call sets:

    m_aiHasMetTurn.set(eOther, iGameTurn);

with eOther == self. Hence isHasMet(self) is true in an initialized game. A
second living member C of target TeamBC therefore is not filtered by the
has-met condition merely because C shares eTheirTeam with B.

The second F114 manifestation also survives: CvTeamAI::AI_shareWar(ownTeam)
iterates the team's current enemies and returns true when an enemy is at war
with the supplied own team. Therefore a target teammate that reaches the branch
can indeed be classified as an "ally" of eThey while the target team is at war.

No album correction is required: WIP07's F114 diagnosis remains authoritative.
This note is retained so the tempting but incomplete "teams do not meet
self" counterargument is not rediscovered later.


METHOD / HEADER CLOSURE CONTROL -- NO UNINVENTORIED SAS SEMANTIC SURFACE
-------------------------------------------------------------------------------
A brace-aware inventory found 83 actual Class::member definitions in both
current SAS WarUtilityAspect.cpp and Base AdvCiv 1.14 (plus the overload of the
WarUtilityAspect entry point handled separately by the inventory). No SAS-only
aspect/helper implementation is hiding outside the reviewed class surface.

Token-level body comparison after stripping comments/formatting shows the
meaningful current SAS-vs-Base implementation changes remain the already-known
localized areas:

* WarUtilityAspect::evaluate(MilitaryAnalyst const&) -- F101/KI#424 member-delta
  XML-weight repair already present in current SAS;
* WarUtilityAspect::techRefuseThresh -- F103/KI#426 repair already present;
* Risk::preEvaluate -- SAS hard contact gate plus F102/KI#425 team-wide one-shot
  repair;
* Distraction::evaluate -- later SAS winning-war cap/policy addition, which does
  not create F126's inherited team-cost duplication.

The header token diff is correspondingly narrow: current SAS adds only
Risk::m_bSASContactGateEvaluated and its constructor initialization relative to
Base 1.14. Constructor/member lifetime review found no uninitialized derived
state escaping into use: IllWill scratch values are overwritten before use,
MilitaryVictory/KingMaking/ThirdParty/DramaticArc pre-state is reset per member,
and Revolts' intentionally persistent set is already normalized under F132.

This substantially closes the possibility of an unreviewed SAS-only helper or
header-state regression in file 014. Remaining work is semantic/adversarial
closure of the inherited aspect formulas and ledger, not discovery of hidden
methods.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP27
NEW ROOT IN THIS APPEND: F139 / provisional KI#462
F114/KI#437: REVALIDATED, NOT RETRACTED
NEXT UNUSED ROOT: F140

Continue the untouched-helper/scenario-state and current-ledger collision pass,
then adversarially reread the live F101-F139 root set (with F113 still the sole
current file-014 retracted ID) before deciding whether a durable C015 is earned.
No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP28 CONTINUATION -- SAME-ROOT NORMALIZATION BEFORE FINAL CLOSURE
===============================================================================
STATUS: WIP / append-only continuation of authoritative C014-WIP27.
COMPLETED FILES: 13 / 169.
CURRENT FILE: 014 CvGameCoreDLL/WarUtilityAspect.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

This pass deliberately challenged three already-live roots whose later source
inspection exposed additional manifestations. All three survive as BROADENINGS
of their existing root; no F140 is spent here.


F124 / provisional KI#447 -- AUTHORITATIVE BROADENING -- THREAT-STRENGTH RATIO
IS PLAYER-LOCAL ON THE AGENT SIDE TOO
-------------------------------------------------------------------------------
WIP14 established F124 from the potential intervener side: ThirdPartyIntervention
estimates intervention probability with rival TEAM power/state, then switches to
one rival PLAYER's cached ARMY before the nonlinear 0.50 severity gate. The final
closure pass proves that the denominator has the symmetric team/member mismatch.

PROBABILITY PATH ALREADY USES AGENT TEAM POWER

For the paranoia branch, ThirdPartyIntervention computes:

    int iOurDefPow = kOurTeam.getDefensivePower();
    ...
    kWe.AI_paranoiaRating(eThey, iOurDefPow, false, true);

So its intervention-probability model explicitly supplies the full agent TEAM's
defensive power.

SEVERITY PATH SWITCHES TO CURRENT AGENT PLAYER

preEvaluate() instead initializes:

    m_rDefPow = ourCache().getPowerValues()[ARMY]->power();

`ourCache()` belongs to current PlayerTypes eWe. The subsequent ally loop is:

    PlayerAIIter<MAJOR_CIV,OTHER_KNOWN_TO> itAlly(eOurTeam)

and AgentIterator's `OTHER_KNOWN_TO` predicate explicitly requires:

    getTeam(kAgent) != m_eTeam

(the enum comment likewise says "Not incl. the first agent's team, but incl. its
vassals"). Therefore an ordinary teammate A2 on eOurTeam is categorically NOT
added to m_rDefPow. Vassals and qualifying defensive-pact teams can be added at
half weight; same-team members cannot.

Later evaluate() uses this player-local value as:

    scaled rOurPow = m_rDefPow;
    ...
    scaled rTheirPowToOurs = rTheirPow / rOurPow;
    if (rTheirPowToOurs <= fixp(0.5))
        return;

This is the same nonlinear gate already proven under F124.

DETERMINISTIC AGENT-PARTITION REPRODUCTION

Let agent TeamA have two ordinary members A1 and A2 with 40 relevant ARMY power
each (80 combined), and let potential intervener B have 30 relevant ARMY power.
Ignore vassals/losses and hold the probability path positive.

On A1's aspect pass:

    rOurPow = 40
    rTheirPowToOurs = 30 / 40 = 0.75 -> passes the 0.50 gate

On A2's pass, the same calculation occurs again. But the team that B would have
to fight has 80 combined power:

    30 / 80 = 0.375 -> should fail the aspect's own "too weak to matter" gate

Conversely, concentrating the same team power into one member changes the
nonlinear result despite identical war-side military capacity. The later linear
member-count divisor cannot undo a gate that has already passed/returned.

This is especially strong because the preceding paranoia calculation already
uses `kOurTeam.getDefensivePower()`: the same aspect first asks how likely B is
to intervene against the TEAM, then judges how dangerous that intervention is
against only the current member's army.

ANCESTRY / REPAIR NORMALIZATION

The original ThirdPartyIntervention introduction already initialized m_rDefPow
from current-player cache power and iterated OTHER_KNOWN_TO for allies; Base
AdvCiv 1.14 retains the same boundary. Later power-projection retuning did not
change it.

F124 is therefore best normalized as one root: ThirdPartyIntervention's
nonlinear threat-strength ratio is built from member-local ordinary-team power
on BOTH sides despite modelling a team-wide intervention. Keep genuinely
player-specific probability/attitude inputs if desired, but construct the
severity power ratio from the actual war sides before the nonlinear floor/gate.


F122 / provisional KI#445 -- AUTHORITATIVE BROADENING -- PHYSICAL AGENT EXPOSURE
AND EVACUATION CAN ALSO BE REUSED ACROSS TARGET-TEAM MEMBER PASSES
-------------------------------------------------------------------------------
WIP13's decisive F122 proof remains the explicit one-agent-unit/one-entangled-
enemy cap. Two neighboring TacticalSituation::evalEngagement quantities share
the same physical-asset reuse boundary and should be handled by the same repair.

1. `iOurExposed`

For every current rival player eThey, the function walks the SAME current agent
groups and asks:

    kWe.AI_getPlotDanger(kGroupPlot, 1, false, iGroupSize, false, eThey)

CvPlayerAI::AI_countDangerousUnits honors that final PlayerTypes filter by
counting only units whose combat owner equals eAttackPlayer. Thus a damaged A
unit threatened by both B and teammate C can be counted as exposed in B's pass
and then the exact same physical A unit can be counted again in C's pass.

2. `iOurEvac`

`evacPop(eWe, eThey)` walks the SAME physical cities of current agent eWe. For an
evacuating city it again uses player-filtered plot danger:

    kOwner.AI_getPlotDanger(..., eInvader) >= 2

The source comment says this filter exists only to avoid attributing evacuation
to an unrelated third party. Against two members B and C of the SAME hostile
team, one physical evacuating city/population can satisfy the danger threshold
for both passes and be charged twice.

These do not need separate roots: they are the same F122 state-domain defect as
entanglement -- agent-side physical tactical assets are independently consumable
inside rival-player callbacks even though the assets themselves cannot be lost,
exposed, evacuated or paired twice merely because enemy ownership is partitioned
between teammates.

Repair should aggregate/deduplicate the relevant target-team danger/engagement
before applying physical-agent-asset caps. Do not simply globalize every
TacticalSituation term; genuinely player-owned enemy units/cities remain valid
inputs.


F127 / provisional KI#450 -- AUTHORITATIVE BROADENING -- CONQUEST FULL COMPLETION
CAN ALSO REPEAT ACROSS AGENT-TEAM MEMBERS
-------------------------------------------------------------------------------
WIP21 proved that `progressRatingConquest()` treats elimination of one rival
PLAYER as full Conquest progress even while that player's TEAM can remain an
actual CvGame::testVictory blocker.

There is an additional aggregation manifestation under the already-established
F127 global/team-victory root. Its first condition is:

    if (militAnalyst().isEliminated(eThey) || ...)
        return 1;

The `isEliminated(eThey)` result is independent of which living agent-team member
currently owns eWe. WarEvaluator evaluates the shared MilitaryVictory aspect for
A1 and then A2. If B is predicted eliminated, both agent-member passes can reach
that same unconditional 1.0 completion state for the same team-owned Conquest
objective.

WIP16 already proved this general repeated-agent-member completion-budget problem
for Domination/Diplomacy; this note simply brings Conquest's later WIP21
broadening under the same normalization. No new root is needed.

The repair remains scenario/team-level victory-progress aggregation before any
single global completion cap/full-completion state is converted to utility.


CLOSURE CONTROL
---------------
These broadenings were intentionally NOT promoted as F140:

* F122 already owns cross-rival reuse of physical agent tactical assets;
* F124 already owns the player/team boundary inside ThirdPartyIntervention's
  nonlinear threat-strength ratio;
* F127 already owns repeated/global team-victory completion state inside
  player/member callbacks.

Keeping one repair locus per root is more useful than increasing the finding
count.

CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP28
BROADENED ROOTS: F122/KI#445, F124/KI#447, F127/KI#450
NEXT UNUSED ROOT: F140

Continue the final untouched-helper/scenario-contract and current Known-Issues
semantic-collision sweep. Then adversarially reread live F101-F139 before any
C015 closure. No repository source files were modified.
===============================================================================

LOCAL RECONCILIATION AFTER C014-WIP28 IMPORT

- ChatGPT-5.6-Sol's C014-WIP25 through C014-WIP28 append is compatible with current authority at HEAD a95f6eec8 plus the staged KI#420/KI#421 repairs. The received append remains verbatim as evidence.
- F098 / KI#420 and F099 / KI#421 are now fixed in the staged current source. Their repairs restore symmetric exact Permanent-Alliance history and statefully retire absorbed-team stolen-visibility relations; the new `WarUtilityAspect.cpp` findings do not supersede either repair.
- F137-F139 / provisional KI#460-KI#462 are collision-free against the current reviewed Known Issues ledger. WIP28 broadens F122/KI#445, F124/KI#447 and F127/KI#450 without allocating another root. All remain provisional while `WarUtilityAspect.cpp` is open; no implementation is promoted from WIP25-WIP28 in this reconciliation. (GPT-5.6-Sol)


===============================================================================
C014-WIP29 APPEND -- current-6267 reconciliation + GreedForVassals team-state audit
===============================================================================

CURRENT-SOURCE RECONCILIATION
-----------------------------
This continuation rebases the open file-014 audit onto the user's fresh
AdvCiv-SAS light-source snapshot `20260825T231107`:

    branch: main
    HEAD: fe0e4abbe4ef49a910bff90d7db2f02143855c53
    practical commit count: 6267

The repository-side audit album has already reconciled C014-WIP25 through
C014-WIP28 while the independently reviewed CvTeam repair track advanced through
KI#420/KI#421. The WarUtilityAspect candidates remain provisional rather than
silently fixed.

Most importantly, current practical-6267 `CvGameCoreDLL/WarUtilityAspect.cpp` is
byte-for-byte identical to the practical-6263 / 20260825T195953 copy used during
the WIP12-WIP28 investigation. Thus no live file-014 candidate has changed shape
merely because the surrounding CvTeam work advanced. All findings are still
re-challenged against the current tree and current Known-Issues ledger before
closure; this identity check is not a substitute for that semantic review.


F140 / provisional KI#463 -- CONFIRMED -- INHERITED ADVCIV TEAM-STATE DUPLICATION
GREEDFORVASSALS CHARGES ONE CAPITULATED-VASSAL DIPLOMATIC DOWNSIDE PER MEMBER
-------------------------------------------------------------------------------
CONFIRMED inherited AdvCiv player-loop/team-vassal-state defect.

ROOT

`GreedForVassals::evaluate()` is entered through the ordinary per-rival-player
WarUtilityAspect loop. It first decides whether the CURRENT RIVAL TEAM becomes a
new vassal of the agent team:

    TeamSet const& kNewVassals =
        militAnalyst().getCapitulationsAccepted(eOurTeam);

    if (kNewVassals.count(eTheirTeam) <= 0 || ...)
        return;

After valuing member-local research income, resources, trade routes and military
support, the function unconditionally applies:

    if (!kWe.AI_atVictoryStage(AI_VICTORY_CONQUEST3))
        rUtility -= 5; // To account for the diplo penalty from capitulated vassals

The condition that creates this cost is one `TeamTypes` capitulation relation,
but the cost is emitted from a `PlayerTypes` callback. If target team V contains
living members V1 and V2 and V capitulates to eOurTeam, both member evaluations
see the same `kNewVassals.count(V)` result:

    V1 pass: -5
    V2 pass: -5

Hence one capitulated-vassal team relation contributes a fixed -10 downside
solely because the vassal team contains two players. With one living member the
same relation contributes -5. The final `max(0, rUtility)` can mask the extra
subtraction when a member's benefit is already small, but it does not repair the
ownership error; whenever both passes remain above zero, the duplicated fixed
cost survives directly, and near zero it can change whether an otherwise useful
member contribution is discarded entirely.

HISTORICAL INTENT IS EXPLICITLY TEAM-STATE-LIKE

The original UWAI introduction did not merely contain an unexplained magic -5.
Its comment was:

    To account for downsides of vassals, in particular wars with third parties
    that dislike the vassal

Practical 2261 later shortened this to today's
"diplo penalty from capitulated vassals" while adding love-of-peace handling;
the -5 itself was preserved. Base AdvCiv 1.14 still has the same fixed cost in
the same per-rival-player callback.

Both stated downsides are consequences of acquiring a VASSAL TEAM, not of
receiving the same team relation once for every player stored on that team.

REAL DIPLOMACY CONTROL -- RIVAL-VASSAL ATTITUDE ITERATES VASSAL TEAMS

The live attitude system gives a particularly strong ownership-domain control.
`CvPlayerAI::AI_getRivalVassalAttitude(ePlayer)` computes dislike of another
player's vassal bloc by iterating:

    for (TeamIter<CIV_ALIVE,VASSAL_OF> itVassal(TEAMID(ePlayer)); ...)

and, for each capitulated vassal TEAM, derives one score from:

    itVassal->getNumCities()
    itVassal->getCapitulationTurn()

That `rCapitulatedScore` is then used in the rival-vassal attitude modifier. If
one capitulated team owns eight cities, repartitioning those same eight cities
between one or two player members does not produce a second TeamIter entry or a
second capitulation relation. The score remains based on the same vassal team's
aggregate city count and capitulation age.

This team granularity is not a late SAS reinterpretation. K-Mod's original
`AI_getRivalVassalAttitude` also queried the target player's TEAM and its vassal
count/power; AdvCiv later refined the formula while retaining team-level vassal
ownership.

REAL GAME-STATE CONTROL -- VASSALAGE ITSELF IS ONE CvTeam RELATION

`CvTeam::setVassal(TeamTypes eMaster, ...)` changes one team's `m_eMaster`,
records one team capitulation/voluntary-vassal event, and updates wars/war plans
through TeamTypes state. A multi-member team does not call `setVassal` once for
each player member. The forced-war liability highlighted by the original UWAI
comment therefore also originates from one team relation.

WHY THIS IS NOT F129 AND NOT A BLANKET VASSAL NORMALIZATION

F129/KI#452 concerns a different object: an already-owned TECHNOLOGY SET is
team-owned but cached/credited once per vassal player. Fixing that cache does not
touch this fixed -5 line.

Conversely, many neighboring GreedForVassals benefits really can be member-local:

* future commerce/research capacity can differ by player;
* connected resource availability is player-owned (the negative control already
  recorded under F129);
* trade-route city counts and military branches are player-local contributions.

Those quantities should not be blindly divided by team size. F140 is only the
fixed downside whose own condition/comment and the real diplomacy/vassal systems
identify it as one team-level capitulated-vassal state.

RELATION TO F130 / WHY A SEPARATE ROOT IS USEFUL

F130/KI#453 proves the same broad architectural hazard inside `Risk`: fixed
capitulation cost and master-team vassal losses are re-emitted through member
callbacks. F140 is not merged into F130 because the two defects have independent
repair loci and independent modeled quantities:

* F130 is Risk's own loss/capitulation accounting;
* F140 is GreedForVassals' approximation of the diplomatic/forced-war downside
  of ACQUIRING a vassal team.

Repairing Risk cannot change GreedForVassals, and repairing the latter cannot
change Risk. This follows the C014-WIP24 normalization rule that similar
player/team smells remain separate when their state owners and nonlinear/fixed
operations are independently emitted.

ANCESTRY

The fixed vassal-downside subtraction is present in the original UWAI lineage.
Practical 2261 changed only the surrounding comment/peace-personality treatment,
and later scaled-number/refactor commits retained the behavior. Current SAS and
Base AdvCiv 1.14 still contain the same per-member emission. F140 is therefore
inherited, not SAS-created.

REPAIR DIRECTION

Charge the fixed capitulated-vassal diplomatic/forced-war downside once per newly
acquired VASSAL TEAM, or apportion one team-level cost across whatever living
member callbacks are retained so the aggregate is roster-partition invariant.

Do not normalize the whole GreedForVassals result. Keep genuinely player-local
benefits additive, and handle F129's duplicated current-tech inventory at its own
ownership boundary.


CURRENT-6267 COLLISION / CONTROL NOTES
--------------------------------------
- Current `WarUtilityAspect.cpp` is unchanged from the WIP28 source base; no
  F101-F139 root is retired merely by the practical-6267 rebase.
- The reviewed Known-Issues ledger has no existing independently promoted root
  that owns GreedForVassals' fixed per-member capitulated-vassal downside.
  Provisional KI#463 is collision-free after KI#460-KI#462.
- The nearby `iOurVassalPlayers` calculation still contains the already-recorded
  F096/KI#418 live-vs-assigned-member denominator seam for predicted new vassal
  teams. That is not F140.
- BorderDisputes' capitulation branch is not generalized into F140: its
  `AI_getCloseBordersAttitude(eThey)` input is genuinely player-to-player and can
  differ between members. Rebuke likewise contains an explicit source comment
  permitting repeated member-pair memory value. These are useful controls
  against blanket team normalization.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP29
CURRENT SOURCE: practical 6267 / HEAD fe0e4abbe4ef49a910bff90d7db2f02143855c53
NEW ROOT IN THIS APPEND: F140 / provisional KI#463
NEXT UNUSED ROOT: F141

Continue the final current-ledger semantic-collision pass and adversarial reread
of F101-F140. Revisit only untouched helper/scenario-state seams with a hard
ownership/state invariant; a clean closure is preferable to manufacturing F141.
No repository source files were modified.
===============================================================================


===============================================================================
C014-WIP30 APPEND -- C++ polymorphic-lifetime closure + F141
===============================================================================

F141 / provisional KI#464 -- CONFIRMED -- INHERITED ADVCIV C++ LIFETIME DEFECT
WARUTILITYASPECT HAS NO VIRTUAL DESTRUCTOR BUT DERIVED ASPECTS ARE DELETED THROUGH BASE POINTERS
--------------------------------------------------------------------------------
STATUS
------
NEW CURRENT INHERITED C++ DEFECT ROOT. Static language-contract evidence is
conclusive; practical severity depends on compiler/optimizer/runtime because the
operation is undefined behavior, but current derived classes contain non-trivial
members whose destruction genuinely matters.

ROOT CONTRACT
-------------
`WarUtilityAspect` is a polymorphic base class. Its current declaration contains
multiple virtual functions, including:

    virtual int evaluate(MilitaryAnalyst const& kMilitaryAnalyst);
    virtual UWAI::AspectTypes xmlID() const=0;
    virtual void evaluate()=0;
    virtual int preEvaluate();
    virtual bool concernsOnlyWarParties() const;

but it declares no destructor at all. There is no
`virtual ~WarUtilityAspect()` in the header, cpp file, or any derived class.
Consequently the compiler supplies a non-virtual implicit base destructor.

`WarEvaluator::fillWithAspects` then allocates concrete derived objects and
stores them behind base pointers:

    vector<WarUtilityAspect*> apAspects;
    ...
    v.push_back(new GreedForAssets(params));
    ...
    v.push_back(new KingMaking(params));
    ...
    v.push_back(new Revolts(params));
    ...

After evaluation, `WarEvaluator::evaluate` destroys every object through that
base pointer:

    delete apAspects[i];

For a delete-expression whose static pointee type is a base class but whose
dynamic object is derived, the base destructor must be virtual. Here it is not.
Deleting any of these derived aspect objects through `WarUtilityAspect*` is
therefore undefined behavior in C++.

THIS IS NOT MERELY A STYLE/WARNING ISSUE
----------------------------------------
Two current derived classes make the missing derived destruction concretely
important:

`KingMaking` owns:

    std::set<PlayerTypes> m_winningFuture;
    std::set<PlayerTypes> m_winningPresent;

and `Revolts` owns:

    std::set<PlotNumTypes> m_countedCities;

Their implicit derived destructors must destroy those sets. With a non-virtual
base destructor, deleting the objects through `WarUtilityAspect*` does not have
a valid language-level path that guarantees those derived destructors run.
Under the ordinary implementation exhibited by a local C++03 compiler, only the
base destructor is called, so dynamically allocated set nodes owned by the
derived subobject are not released. The C++ rule itself is stronger: because the
program has undefined behavior at the delete-expression, no particular runtime
consequence should be assumed or relied upon.

The base class also owns a `std::vector<PlotNumTypes>`, but that does not rescue
the hierarchy: ordinary implementations can still destroy the base subobject
while skipping the derived subobject, and the standard already makes the whole
operation undefined when the base destructor is non-virtual.

LOCAL COMPILER CORROBORATION -- ILLUSTRATIVE, NOT THE PROOF
-----------------------------------------------------------
A minimal C++03 reproduction with a polymorphic base lacking a virtual
destructor and a derived class owning `std::set<int>` was compiled locally with:

    g++ -std=c++03 -Wall -Wextra -Wdelete-non-virtual-dtor

GCC 14.2 emits:

    warning: deleting object of polymorphic class type 'Base' which has
    non-virtual destructor might cause undefined behavior

and the illustrative run prints only the base destructor, not the derived
one. This corroborates the practical mechanism but is intentionally not used as
a claim about the exact Visual C++ 2003 manifestation; undefined behavior permits
implementation-dependent outcomes.

CURRENT CALL/LIFETIME REACHABILITY
----------------------------------
This delete path is not dormant. Every uncached `WarEvaluator::evaluate` scenario
constructs the aspect vector, evaluates it for the agent-team members, and then
loops over the vector and executes `delete apAspects[i]` for every concrete
aspect. War evaluation is a core UWAI path, so the invalid polymorphic deletion
is repeatedly reachable during normal AI play.

The non-trivial derived sets allocate only when populated, so a visible leak from
those containers can vary by game state. `Revolts::m_countedCities` is populated
when relevant cities are processed; `KingMaking`'s winner sets are populated once
that aspect becomes active. The source-level undefined behavior, however, exists
for every derived-object deletion regardless of whether those sets happen to be
empty in a particular call.

ANCESTRY -- INHERITED FROM THE ORIGINAL advc.104/UWAI CLASS INTRODUCTION
------------------------------------------------------------------------
Base AdvCiv 1.14 has the same defect:

* `WarUtilityAspect.h` declares the same polymorphic base without a virtual
  destructor;
* `WarEvaluator::fillWithAspects` allocates derived aspects into
  `vector<WarUtilityAspect*>`;
* `WarEvaluator::evaluate` executes `delete apAspects[i]` through those pointers;
* no `~WarUtilityAspect`, `~KingMaking` or `~Revolts` declaration exists.

The original practical-1253 / advc.104 introduction already created the hierarchy
this way. Its newly added `WarEvaluator::evaluate` constructed
`vector<WarUtilityAspect*> aspects`, filled it with `new DerivedAspect(...)`, and
then executed:

    delete aspects[i];

while the simultaneously introduced `WarUtilityAspect` declaration contained
virtual methods but no virtual destructor. The original `Revolts` class already
owned a `std::set<int> countedCities`, so a non-trivial derived destructor was
required from the beginning. Later KingMaking evolution added further `std::set`
members without changing the base destruction contract.

A scan of the retained current commit-diff corpus finds no later
`~WarUtilityAspect` addition/removal or manual compensating derived-destructor
scheme. Current source and Base 1.14 therefore preserve the original lifetime
bug.

COLLISION / ROOT ACCOUNTING
---------------------------
No current reviewed Known-Issues entry or prior album root owns the missing
virtual destructor / delete-through-base contract. Searches for virtual
/destructor/delete-through-polymorphic-base wording found no collision.

This is independent of every WarUtilityAspect semantic/modeling finding F101-
F140. Repairing the destructor cannot change any utility arithmetic, player/team
normalization or scenario-state predicate; conversely none of those repairs makes
`delete apAspects[i]` language-defined.

REPAIR DIRECTION
----------------
Give the polymorphic base a public virtual destructor. The project remains on an
old C++ toolchain, so a conservative C++03-compatible declaration is preferable,
for example:

    public:
        virtual ~WarUtilityAspect() {}

or declare it in the header and define an empty destructor in the cpp file.

It must be public because `WarEvaluator`, an unrelated caller, deletes through
`WarUtilityAspect*`. Do not use C++11-only `= default` merely for this repair.
Once the base destructor is virtual, the implicit derived destructors run in the
proper order and clean their standard-library members before the base subobject
is destroyed.

Do not paper over the issue by manually clearing the current `std::set` members:
that would leave the delete-expression undefined for every derived class and
would regress again when another derived class gains a non-trivial member.


EFFORT STRONGEST-RIVAL POWER CHALLENGE -- NOT PROMOTED
------------------------------------------------------
The closure pass also challenged `Effort::preEvaluate`, which estimates the
future usefulness of our surviving military by taking the maximum
`CvPlayer::getPower()` among known potential-enemy players. A hostile side with
one 120-power player therefore yields a different benchmark from an otherwise
similar two-member team split 60+60.

This is structurally player-partition-sensitive, but the source describes the
quantity as the power of our "most dangerous rival," and unlike F124/F134 there
is no hard nonlinear war-legality/capability predicate or direct real-game team
contract proving that this heuristic was intended to use combined team power.
Independently controlled civ/player armies can reasonably be treated as separate
post-war rivals even when teamed. No contradictory historical repair/comment has
been found.

Therefore this remains a DESIGN/MODELING REVIEW NOTE, not F142 (nor the former
F141 candidate). The audit should not convert every player-power approximation
into a team-power correctness defect merely because team partition can change a
heuristic.


C++ LIFETIME / OWNERSHIP CLOSURE CONTROLS
-----------------------------------------
- The missing virtual destructor is the only delete-through-polymorphic-base
  contract established in this file hierarchy. Searches found no custom derived
  destructors whose logic would mask or replace it.
- The unchecked `ourCache().lookupCity(...)` dereferences remain covered by the
  InvasionGraph/MilitaryAnalyst producer contract already recorded in WIP23;
  no competing pointer-lifetime root was established.
- Derived aspect members that intentionally persist across agent-member/rival
  callbacks were rechecked in WIP25. The only proven persistent/local partition
  defect remains F132 (`Revolts` numerator/denominator reset around
  `m_countedCities`); the destructor issue here concerns object destruction only
  after the entire scenario evaluation.
- The current practical-6267 `WarUtilityAspect.cpp`/header hierarchy remains
  source-compatible with the older WIP evidence; F141 is inherited rather than
  introduced by the recent SAS reconciliation.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP30
CURRENT SOURCE: practical 6267 / HEAD fe0e4abbe4ef49a910bff90d7db2f02143855c53
NEW ROOT IN THIS APPEND: F141 / provisional KI#464
NEXT UNUSED ROOT: F142

Continue the final semantic-collision and adversarial closure of F101-F141,
including one last scan of ownership/destruction and untouched helper contracts.
Do not force C015 merely because the method inventory is complete; if the
remaining passes stay clean, record the controls and then close file 014. No
repository source files were modified.
===============================================================================

LOCAL RECONCILIATION AFTER C014-WIP30 IMPORT

- ChatGPT-5.6-Sol's C014-WIP29 through C014-WIP30 append is compatible with current authority at HEAD 98142d25a plus the reviewed KI#430/KI#431 worktree repairs. The received append remains verbatim as evidence despite identifying its older practical-6267 / HEAD fe0e4abbe source baseline.
- F104-F106 / KI#427-KI#429 are fixed at current HEAD. F107-F108 / KI#430-KI#431 are fixed in the reviewed worktree: PreEmptiveWar now constructs matching team/vassal city snapshots, and UlteriorMotives supplies the missing UWAI-report vararg. The new findings do not supersede these repairs.
- F140-F141 / provisional KI#463-KI#464 are collision-free against the current reviewed Known Issues ledger and remain provisional while `WarUtilityAspect.cpp` is open. No implementation is promoted from WIP29-WIP30 in this reconciliation. (GPT-5.6-Sol)


===============================================================================
C014-WIP31 APPEND -- final denominator/API pass + exclusive-radius domain audit
===============================================================================

RECONCILIATION / SCOPE
----------------------
This continuation remains grounded in the reconciled practical-6267 tree:

    HEAD fe0e4abbe4ef49a910bff90d7db2f02143855c53

`WarUtilityAspect.cpp` is still byte-for-byte identical to the earlier source
underlying WIP28-WIP30. Current source and the reviewed Known-Issues ledger remain
authoritative; no repository source file was modified by this audit.

The closure pass resumed from F142 unused. Several superficially dangerous
construction/division paths were challenged before promoting anything:

* The base/derived constructor initializer order is unusual -- `m_kAgentTeam`
  appears early in the initializer list although it is declared after protected
  state -- but C++ declaration-order initialization remains safe here. None of
  the members initialized before `m_kAgentTeam` depends on it, `m_iU` is
  initialized before use, and `reset()` initializes the per-evaluation scratch
  state. No second construction-order root accompanies F141.
* ThirdPartyIntervention can start from zero defensive ARMY power, but the
  MilitaryAnalyst gained/lost-power contract prevents a reachable positive-cost
  divide-by-zero state through that route. If both current and gained defensive
  power are zero, lost power is zero and evaluation exits; positive projected
  gain raises `m_rDefPow` before later divisions. No F142 there.
* A second ThirdParty denominator challenge involving
  `confidenceAgainstHuman()` also collapses in the current implementation:
  `CvPlayerAI::confidenceAgainstHuman()` returns 1. The multiplication therefore
  cannot zero the denominator under current code/data.
* The remaining Risk/victory/partner ratio paths inspected in this pass have
  explicit positive floors, producer guarantees, or map directly to established
  F105/F127/F133 rather than defining another root.

These controls matter because the finding below is not being inferred from a
scary division alone. It has an explicit ownership-domain contradiction and a
partition-invariant reproduction.


F142 / provisional KI#465 -- CONFIRMED -- INHERITED SUPPORTED-CONFIG TEAM/PLAYER DOMAIN DEFECT
EXCLUSIVE-RADIUS LOST-TILE CACHE IS TEAM-WIDE BUT IS NORMALIZED BY EACH RIVAL
PLAYER'S LAND BEFORE THE INTENDED TEAM-SIZE SPLIT; THE SAME PHYSICAL TILE LOSS
CHANGES WITH TEAM ROSTER PARTITION
-------------------------------------------------------------------------------
Status: NEW CURRENT SUPPORTED-CONFIGURATION DEFECT ROOT. Shipped/default SAS uses
`OWN_EXCLUSIVE_RADIUS=0`, but, as established independently for F133, value 1 is
an explicitly documented and maintained AdvCiv configuration rather than an
invented unsupported retuning.

This root is adjacent to F133 because both involve advc.035 flipped-tile loss,
but the defects and repair loci are independent. F133 concerns the OUTER
`netLostRivalAssetScore` numerator/denominator ordering. F142 is already present
INSIDE `lossesFromFlippedTiles` even if that outer ordering is repaired.

PRODUCER CONTRACT -- THE STORED LOST-TILE COUNT IS TEAM-WIDE
-------------------------------------------------------------
`UWAICache::updateLostTilesAtWar()` begins from the cache owner's TEAM:

    CvTeam const& kOwnerTeam = GET_TEAM(m_eOwner);

For every other major team it computes contested plots through:

    contestedPlots(apFlipped, kOwnerTeam.getID(), itOther->getID());

and counts a plot according to its current TEAM owner and the war relation of
those two teams:

    TeamTypes ePlotTeam = apFlipped[j]->getTeam();
    if (ePlotTeam == (kOwnerTeam.isAtWar(itOther->getID()) ?
        itOther->getID() : kOwnerTeam.getID()))
    {
        iLost++;
    }

The resulting value is stored by opposing TeamTypes:

    m_aiPlotsLostAtWar.set(itOther->getID(), iLost);

Nothing in this computation depends on which particular `m_eOwner` player inside
`kOwnerTeam` owns a city, plot, army or share of the team's land. Consequently,
two player caches belonging to the same team store the SAME team-wide
`numPlotsLostAtWar(otherTeam)` value for the same opposing team.

This is not an inferred interpretation. The original advc.035 implementation
introduced the backing array as:

    int lostTilesAtWar[MAX_CIV_TEAMS]; // per team

and current storage remains indexed by TeamTypes.

CONSUMER CONTRACT -- THE AUTHOR ALREADY TRIES TO APPORTION A TEAM QUANTITY
-------------------------------------------------------------------------
`WarUtilityAspect::lossesFromFlippedTiles(eVictim, eTo)` retrieves that cached
team quantity through the current victim player's cache and accumulates it for
relevant war teams. It then says explicitly:

    int const iRivalMembers = kTheirTeam.getNumMembers();
    ...
    // Tiles are counted per team, but this function gets called once per rival player.
    r /= iRivalMembers;

The original advc.035 comment was equivalent and even more direct:

    // Tiles are counted per team, but this is a per-civ function.

So the `/ iRivalMembers` is not a speculative team-game redesign. It is the
original implementation's own normalization contract: one team-wide lost-tile
signal is being consumed once per rival player and therefore needs apportioning.

THE DOMAIN MISMATCH OCCURS BEFORE THAT SPLIT
--------------------------------------------
Before dividing by the member count, however, the helper normalizes the shared
team-wide lost-tile count by the CURRENT RIVAL PLAYER'S own land:

    int iVictimLandTiles = GET_PLAYER(eVictim).getTotalLand();
    r = scaled(125 * iVictimLostTiles,
               std::max(5, iVictimLandTiles));
    r /= iRivalMembers;

`CvPlayer::getTotalLand()` is player-local. By contrast, the matching team-domain
quantity exists directly:

    int CvTeam::getTotalLand(bool bCheckVassals) const
    {
        ...
        return m_iTotalLand + iVassalLand;
    }

and with `bCheckVassals=false` it returns the team's stored aggregate
`m_iTotalLand`.

The code therefore performs:

    (team-wide lost tiles / player-local land) / team members

rather than either of the ownership-consistent alternatives:

* player-local lost tiles / player-local land, or
* team-wide lost tiles / team-wide land, apportioned across per-player callbacks.

DETERMINISTIC EQUAL-PARTITION REPRODUCTION
------------------------------------------
Hold the physical/team state constant:

* the victim side controls 100 total land tiles;
* it would lose 10 contested tiles under OWN_EXCLUSIVE_RADIUS;
* no other inputs change.

Case A -- one-player victim team:

    lost-tile cache = 10
    player land     = 100
    team members    = 1

    score = 125 * 10 / 100 / 1 = 12.5

Case B -- the SAME 100 team land split evenly between teammates B and C:

Each player's UWAICache still stores the SAME team-wide lost count 10, because
`updateLostTilesAtWar` depends only on Team B+C versus the opposing team. Each
player owns 50 land:

    B pass = 125 * 10 / 50 / 2 = 12.5
    C pass = 125 * 10 / 50 / 2 = 12.5

The aspect is evaluated once for B and once for C, so aggregate utility becomes:

    12.5 + 12.5 = 25

The explicit `/ 2` normalization has not normalized the team partition at all;
the halved player-land denominator cancels it. Merely splitting the same team's
land between two members doubles the resulting total flipped-tile severity.

With an even n-member partition, the same algebra makes each member reproduce the
solo severity and the aggregate grows approximately n-fold. With uneven land
shares, the sum instead depends on a harmonic-like combination of the members'
land counts and can be dominated by a small-land teammate -- even though the
numerator is still the same undivided team-wide lost-tile count for every member.

THE SIGNIFICANCE GATE IS PARTITION-SENSITIVE TOO
-------------------------------------------------
Before scoring, the helper also uses:

    if (std::abs(iVictimLostTiles) <= 4 * iRivalMembers)
        return 0;

Again `iVictimLostTiles` is one team-wide physical count. Changing only the
number of player members changes whether the same lost plots are considered
significant at all.

For example, five team-wide lost plots:

* one-member team: 5 > 4, so the loss is scored;
* two-member team: 5 <= 8, so BOTH per-player calls return zero.

Thus roster partition can not only multiply the score after the gate; it can
switch the entire team-wide physical loss on or off before scoring.

ANCESTRY -- PRESENT FROM advc.035 BIRTH; BASE 1.14 IDENTICAL
------------------------------------------------------------
The retained practical-1286 / advc.035 introducing diff adds all components of
the defect together:

* `lostTilesAtWar[MAX_CIV_TEAMS] // per team`;
* team-vs-team contested-plot counting;
* `victimLandTiles = GET_PLAYER(victimId).getTotalLand()`;
* division by `teamSz` with the explicit comment that tiles are counted per team
  while the helper is per civ.

Current SAS and Base AdvCiv 1.14 `lossesFromFlippedTiles` are normalized-text
identical. This is therefore an inherited original-design defect, not an SAS
regression.

SUPPORTED-CONFIGURATION REACHABILITY
------------------------------------
Current GlobalDefines_advc.xml documents OWN_EXCLUSIVE_RADIUS directly:

    [advc.035] If set to 1, tiles that are only within the radius of one city
    belong to the owner of that city regardless of culture. Recommended: 0 ...

The implementation remains wired through CvPlot, CvCity, CitySiteEvaluator,
CvPlayerAI, UWAICache and WarUtilityAspect. As with F133, recommended/default 0
masks the defect in ordinary SAS play, but 1 remains a maintained supported
AdvCiv configuration. This meets the album's supported-config bar.

INDEPENDENCE FROM F133
----------------------
F133 repairs the fact that `netLostRivalAssetScore` freezes `rTotal` before
adding flipped-tile loss to `rNetLoss`. One possible F133 repair is to extend the
outer returned denominator so numerator and denominator remain comparable.

That does NOT change the value returned by `lossesFromFlippedTiles` itself. The
same team-wide 10 lost tiles would still produce 12.5 for a solo 100-land team
and aggregate 25 for two 50-land teammates before entering the repaired outer
asset accounting. F142 therefore survives a complete F133 outer-denominator
repair.

Conversely, repairing F142's ownership domain would not stop F133 from adding the
now-correct flipped-tile score to the numerator after freezing its outer asset
denominator. Both roots must be addressed independently.

COLLISION / ROOT ACCOUNTING
---------------------------
The current reviewed Known-Issues ledger reaches KI#426; no reviewed entry owns
this internal team-wide-lost-tiles / player-land normalization defect. Searches
for OWN_EXCLUSIVE_RADIUS, flipped/lost tiles and the specific helper establish
F133's outer-denominator issue but no existing root for this partition error.

F142 is also not another generic F096 dead-member issue. The equal two-LIVING-
member reproduction above already fails. Using `getNumMembers()` can add an F096
secondary distortion when dead assigned members exist, but replacing it with an
alive count alone does not fix the team/player land mismatch.

REPAIR DIRECTION
----------------
Preserve one ownership domain from producer through normalization. Two coherent
architectures are possible:

1. Keep the current TEAM-WIDE cache:
   * normalize the lost-tile count against the victim TEAM's land (normally
     `GET_TEAM(eVictim).getTotalLand(false)` or the equivalent team-domain
     quantity intended by the model),
   * apply the significance threshold in that same team domain,
   * then apportion the resulting team-level utility across the LIVING rival
     members because the aspect still receives one callback per player.

2. Change the producer to attribute lost plots to individual players, then use
   each player's own lost-tile numerator with that same player's land denominator
   and remove the artificial team-wide split.

The first is much smaller and preserves advc.035's explicit "tiles counted per
team" design. Whatever exact tuning is chosen, do not merely replace
`getNumMembers()` with an alive count, and do not treat an F133 outer-denominator
repair as sufficient.


CURRENT CURSOR
--------------
COMPLETED FILES: 13 / 169
FILE 014: CvGameCoreDLL/WarUtilityAspect.cpp OPEN
PHYSICAL CHECKPOINT: C014-WIP31
CURRENT SOURCE: practical 6267 / HEAD fe0e4abbe4ef49a910bff90d7db2f02143855c53
NEW ROOT IN THIS APPEND: F142 / provisional KI#465
NEXT UNUSED ROOT: F143

Continue the final mechanical/API and semantic closure pass, then adversarially
re-read F101-F142 against current practical-6267 source and the reviewed KI
ledger before deciding whether C014 has earned durable C015 closure. A clean
remaining surface is preferable to manufacturing F143. No repository source
files were modified.
===============================================================================


===============================================================================
C014-WIP32 APPEND -- FINAL ADVERSARIAL / MECHANICAL CLOSURE PASS
===============================================================================

SOURCE AUTHORITY / RECONCILIATION
---------------------------------
This closure pass remains grounded in the fresh reconciled AdvCiv-SAS light-
source snapshot supplied after C014-WIP28:

    practical commit count: 6267
    HEAD: fe0e4abbe4ef49a910bff90d7db2f02143855c53

Current WarUtilityAspect.cpp is byte-for-byte the same current file used for the
WIP29-WIP31 investigation. The reviewed Known Issues ledger is authoritative for
implementation status: KI#424-KI#426 / F101-F103 have been independently
reviewed, fixed and documented in the current source. Provisional KI#427-KI#465
remain unused by the reviewed ledger and therefore remain collision-free aliases
for the still-pending file-014 findings.

The purpose of this append is NOT to create another finding. It records the last
adversarial challenge of the complete F101-F142 ledger, the repaired-source
regression check, final API/registry/mechanical controls, and the evidence for
closing WarUtilityAspect.cpp without manufacturing F143.


CURRENT ROOT-STATUS NORMALIZATION
---------------------------------
The stable file-014 sequence F101-F142 contains 42 numerical slots.

* F113 / provisional KI#436 is authoritatively RETRACTED. CoreAI.h and
  CvGamePlay.h deliberately overload GET_TEAM for both TeamTypes and PlayerTypes;
  the PlayerTypes overload converts through TEAMID. Preserve the old WIP05-WIP10
  hypothesis only as anti-rediscovery evidence.
* The remaining 41 roots are valid discoveries.
* F101-F103 / KI#424-KI#426 are valid discoveries that are already FIXED in
  practical 6267 after independent review.
* F104-F112 and F114-F142 are 38 valid roots that still survive in practical
  6267 and remain pending independent review/implementation.
* Existing F096 / KI#418 was materially broadened by this file's alive-member
  normalizer audit; that is an existing cross-file root, not another file-014
  number.
* F143 remains UNUSED.

This distinction is important for C015: the album records what file 014 found,
not merely what remains broken at the instant of closure.


F119 -> F114 ADVERSARIAL REREAD -- ALL REMAIN LIVE
---------------------------------------------------
F119 / KI#442 -- team-indexed capitulation vector written by PlayerTypes

The raw storage mismatch survives exactly. MilitaryAnalyst declares
`vector<TeamSet> m_capitulationsAcceptedPerTeam`, sizes it by MAX_CIV_TEAMS and
exposes it through a TeamTypes accessor. prepareResults nevertheless loops
PlayerTypes ePlayer and writes directly to:

    m_capitulationsAcceptedPerTeam[ePlayer]

Unlike the retracted F113 GET_TEAM syntax, raw std::vector::operator[] has no
type-aware overload that can convert PlayerTypes through TEAMID. InvasionGraph
copies a master team's accepted capitulations into each live member node, so
those player-node sets must be unioned into TEAMID(ePlayer), not scattered into
player-numbered slots. Current source and Base AdvCiv 1.14 still contain the
same line.

F118 / KI#441 -- negative revolt asset value can become positive war utility

Revolts still multiplies a city's possibly-negative cache asset score by a
positive revolt-loss multiplier. Risk in the same file explicitly documents
that per-city asset score can be negative because of maintenance and prevents a
negative expected LOSS from becoming a gain. Revolts has no such protection;
with positive total assets elsewhere, a negative threatened city can make
`rLossesFromRevolts` negative and `m_iU -= rRevoltCost` increase war utility.
The event being modeled is revolt damage/occupation/population harm, not the
voluntary shedding of a maintenance liability. F118 remains independent of
F132's later subset/denominator-order problem.

F117 / KI#440 -- PublicOpposition mixes hypothetical religion anger with current
angryPopulation

The scenario deliberately calls `getReligionPercentAnger(eThey)` for a rival
that the military simulation may make into a FUTURE enemy. The cap is still the
real current city's `angryPopulation`, whose religion-anger construction only
includes ACTUAL current enemies. A currently content city can therefore cap a
hypothetical new-war religion anger contribution to zero. Conversely, separate
enemy passes can reuse the same current angry citizens. The scenario/current-
state domain mismatch remains direct and does not depend on F113.

F116 / KI#439 -- DramaticArc's eight-turn HasMet tolerance masks real short wars

The source still comments that the HasMet counter is "not exact", while
CvTeamAI::AI_doCounter explicitly treats HasMet and atWar as exact counters.
TurnsAtPeace is reset by war and then counts exact peaceful turns. A short recent
war can leave HasMet-TurnsAtPeace < 8 and therefore be classified as if the pair
had always been peaceful. The direct counter lifecycle remains a stronger
contract than any approximate-tuning defense of the hard 8.

F115 / KI#438 -- computed game-speed-normalized peace duration is discarded

ThirdPartyIntervention still computes `rAtPeaceTurns` from GoldenAgePercent and
then never reads it. The long-peace gate and decay continue to use raw
`iAtPeaceTurns`. The normalization equals raw turns only at Normal speed and was
present, unused, from the original aspect introduction. The final dead-local
scan found no second comparable computed-but-unused normalization variable.

F114 / KI#437 -- FairPlay includes target teammates as alleged third parties

The adversarial self-contact challenge remains resolved in FAVOR of F114.
CvGame::initDiplomacy establishes self-contact through `meet(self,false)`, so a
second player on eTheirTeam passes FairPlay's `kTheirTeam.isHasMet(...)` test.
The filter excludes only player ID `eThey`, not the whole eTheirTeam. The target
teammate can therefore inflate the potential-other-enemy denominator and, when
the target team has a war, satisfy `AI_shareWar(ownTeam)` and be logged/subtracted
as an "ally" of its own team. The entire target team must be excluded from the
independent-third-party loop.

No root in F114-F119 was merged or retracted during this challenge.


F112 -> F104 ADVERSARIAL REREAD -- ALL REMAIN LIVE
---------------------------------------------------
F112 / KI#435 -- wrong side used for war-anger per-civ normalization

UWAICache::updateWarAnger still builds the contribution by ENEMY TeamTypes:

    aiAngerContrib[enemyTeam] = ourTeam.getWarWeariness(enemyTeam, true)

and immediately comments:

    // Turn per-team into per-civ

but divides each enemy-team contribution by the OWNER team's getNumMembers().
The consumer is per living enemy player. A solo owner versus two-player enemy
therefore duplicates the same team contribution, while a two-player owner versus
solo enemy can halve it. This is an explicit producer/comment/denominator
contract, not merely a preferred team-game model. The natural split is across
living members of the enemy team; F096 additionally owns dead-member dilution.

F111 / KI#434 -- AP/Diplomacy city-conquest weighting tests wrong player

The loop stores `eCityOwner` and labels the block "Weight for old owner"; its
attitude lookup also uses eCityOwner. The full-membership test in the middle
still asks `kThey.isFullMember(eVS)`, i.e. the conqueror/current rival. Third-
party conquest makes these players genuinely different. The wrong-object test
survives unchanged.

F110 / KI#433 -- KingMaking models team-owned victory candidacy with player state

The direct controls remain CvGame team score and team-owned victory conditions.
KingMaking ranks current/predicted player scores for Time/score leadership and
uses individual population to decide whether a team-derived Domination
trajectory has been thwarted. Its set<PlayerTypes> winner representation can
also count two members of one winning team as two competitors. This is not a
blanket request to replace all player inputs: culture and several heuristics are
legitimately player-local. The proven team-owned victory-state portions remain
mis-modeled.

F109 / KI#432 -- theirRelativeLoss iterates allies but reuses current rival

`theirRelativeLoss` still loops `eTheirAlly`, but both ordinary ally/vassal and
new-vassal branches call `netLostRivalAssetScore(...)`, whose implicit victim is
current aspect opponent eThey and which has no eTheirAlly parameter. The loop
therefore reuses eThey's assets/losses for teammates and vassals. Predicted new
vassals also retain the coupled full-weight-vs-half-weight error. The wrong-
object construction is unchanged.

F108 / KI#431 -- varargs mismatch

The live call remains:

    log("Attitude level towards %s: %d, refusal thresh: %d",
        m_kReport.leaderName(eThey), towardThem());

Three conversions, two arguments. A balanced scan of every log(...) call in
WarUtilityAspect.cpp found no second format/argument-count mismatch. F108 remains
one bounded inherited C/C++ varargs defect.

F107 / KI#430 -- PreEmptiveWar present/predicted side construction mismatch

The rival side still includes an existing vassal at half weight in both current
and predicted states. Our side initializes a team-wide current and predicted city
base, then adds an existing vassal only to `rOurCurrentCities`; it adds only a NEW
predicted capitulation to `rOurPredictedCities`. Thus an unchanged existing
vassal disappears from our future side. The team-wide base also subtracts losses
of only current eWe. F137 later proves an independent repeated team-threat
problem; repairing F137 does not repair this present-vs-future construction.

F106 / KI#429 -- adjusted best score compared against raw candidates

The first pass still adds commerce/tech strength and possible 4/3 peaceful-
overseas-victory weighting before updating `rBestScore`. The second pass still
divides raw predicted/current player score by that adjusted rBestScore. The
player that established the adjusted maximum can therefore fail its own 70/75%
cutoff. This candidate was briefly rediscovered late in the audit and correctly
recognized as F106 rather than assigned a new number.

F105 / KI#428 -- exact Domination population threshold equality

The real CvGame victory test fails population only when:

    100 * teamPopulation < totalPopulation * adjustedPopulationPercent

so equality satisfies the population requirement. WarUtility instead treats the
component as completed only for `iPopToGo < 0`; equality (`iPopToGo == 0`) enters
the unfinished branch and divides by iPopToGo. ScaledNum's Win32 MulDiv backend
means the release symptom need not be a native integer trap; that nuance does not
repair the semantic boundary. Equality belongs in the already-satisfied branch.

F104 / KI#427 -- first-four resource cap has both boundary and unit errors

Current code still says "Count only the first four resource trades", increments
before testing `>= 4`, and therefore keeps only three resource-containing deals.
It also reduces all resource TradeData items in one deal to one boolean
`bWeReceiveResource`, even though Civ4 can bundle and process several
TRADE_RESOURCES items in one deal. The cap therefore both rejects the fourth
resource-containing deal and counts bundled resource ITEMS in the wrong unit.
No current-source change from KI#426's technology threshold repair touches this
trade-deal defect.

No root in F104-F112 was merged or retracted during this challenge.


F103 -> F101 CURRENT-SOURCE REGRESSION CHECK -- FIXES HOLD
----------------------------------------------------------
These are no longer pending current defects, but their discovery evidence remains
part of file 014 and their fixes were re-read against the same team/member
contracts that exposed them.

F101 / reviewed KI#424

WarUtilityAspect::evaluate now snapshots `iPriorUtility`, computes this member's
raw delta, applies the XML aspect weight only to `iMemberUtility`, restores:

    m_iU = iPriorUtility + iMemberUtility

and returns only that member's weighted contribution. Earlier members are not
reweighted again. This matches the reused-aspect-object team architecture and
also fixes the per-player diagnostic total.

F102 / reviewed KI#425

Risk owns one `m_bSASContactGateEvaluated` latch per aspect/scenario. The hard
contact gate executes once and scans every alive MemberIter of eOurTeam. Land
contact can come from any member; naval qualification requires the SAME member
to provide both a reachable route and cargo-building ability. Collective failure
produces one hard reject. The original one-remote-member team veto is gone while
KI#183/ongoing-war/victory-denial behavior remains around it.

F103 / reviewed KI#426

`techRefuseThresh(PlayerTypes ePlayer)` now binds `CvPlayerAI const& kPlayer =
GET_PLAYER(ePlayer)` and reads `GC.getInfo(kPlayer.getPersonalityType())` for the
AI threshold. The human special case remains. The rival no longer inherits the
agent's personality threshold.

The current README_Known_Issues.md independently records these three as fixed and
documents compiled/autoplay regression coverage. No regression was found in this
closure reread.


F120 -> F142 POST-WIP31 ADVERSARIAL STATUS
------------------------------------------
The newer block had already received repeated pairwise/team/state challenges
through WIP12-WIP31. The final closure pass rechecked the producer/consumer
contracts rather than merely rereading the prose. No root was retracted or
merged.

Key hard controls that remain decisive:

* F120/KI#443 FairPlay start-turn: CvGame::setStartTurnYear creates the ordinary
  later-era getStartTurn baseline from total game turns and StartPercent, while
  FairPlay separately subtracts StartPercent. The historical nonzero-start fix
  therefore conflates extra start delay with ordinary later-era position.
* F121/KI#444 terminal-era startup clock: KingMaking and UWAICache threat use
  `currentEra <= startEra` as a TEMPORARY early-game reliability gate. Future is
  terminal, so the gate has no exit for a Future-era start.
* F122/KI#445 TacticalSituation: one physical agent unit/city can be reused in
  rival-player-specific engagement, exposure and evacuation accounting across
  teammates. The repair locus is physical asset deduplication, not global
  teamification of every tactical input.
* F123/KI#446 Bellicosity: the historical anti-third-party loss budget is the
  same agent loss amount but resets separately for each target member.
* F124/KI#447 ThirdParty severity: intervention probability already uses team
  danger/power, while the nonlinear <=50% severity gate is built from ordinary
  member-local ARMY power on BOTH war sides. Ordinary teammates are omitted.
* F125/KI#448 DramaticArc: a human member's explicit safety return can be bypassed
  by the AI teammate pass for the same team-wide war; the too-little-action gate
  has the same player partition.
* F126/KI#449 Distraction: team-war duration/alternative-war opportunity cost,
  including a fixed +5.5 actual-war-plan nudge, can be recharged per target
  member. The later winning-war SAS cap does not normalize that inherited root.
* F127/KI#450 MilitaryVictory: Domination/Diplomacy global remaining thresholds
  are independently capped/completed per player/member pass. Conquest also
  returns 1 when one rival PLAYER is eliminated although CvGame::testVictory
  requires every other non-vassal TEAM to have zero cities; full completion can
  repeat across agent members too.
* F128/KI#451 HiredHand: sponsorAgainst/bountyAgainst and obligation age are
  explicitly target-TeamTypes state but are consumed once per target member.
* F129/KI#452 vassal current-tech score: technology ownership/transfer is team-
  owned; member caches can hold the same current technology value and
  acceptVassal explicitly sums them. Resource score is a negative control because
  resource availability is player-owned.
* F130/KI#453 Risk: team-owned capitulation/vassal costs are emitted through
  member/rival callbacks. The fixed 100 capitulation cost gives an exact 2-v-2
  multiplication reproduction.
* F131/KI#454 GreedForSpace: AI city sites are stored independently per player and
  teammates can recommend the exact same plotNum; one physical plot then becomes
  two orphaned expansion sites if both members disappear. Distinct plots remain
  legitimately additive.
* F132/KI#455 Revolts: persistent m_countedCities spans rival passes but numerator
  and denominator reset locally each evaluate call. Overlapping primary areas
  make the result dependent on which rival player is iterated first.
* F133/KI#456 supported OWN_EXCLUSIVE_RADIUS=1: netLostRivalAssetScore freezes the
  returned total-assets denominator before adding flipped-tile loss to the
  numerator; Assistance's explicit `FAssert(r <= 1)` can then be violated.
* F134/KI#457 IllWill: revenge military capability is tested by nonlinear
  thresholds using current player army power despite a revenge declaration being
  a team war. Real CvTeamAI war valuation uses team power; attitude/motivation
  can remain player-specific.
* F135/KI#458 ThirdParty decisive-target shortcut: one proxy tests only the
  target team's pre-simulation leader PLAYER; the other accepts any capitulation
  anywhere in the simulated network, not necessarily the current target.
* F136/KI#459 partnerUtilFromTech: near-future technology opportunity is derived
  from team ownership/research and one tech trade changes the whole team, yet the
  same opportunity can be counted for B and teammate C. Player-owned trade-route,
  resource and deal terms are negative controls.
* F137/KI#460 PreEmptiveWar: player-keyed threatRating resolves to the same
  teamThreat for teammates, and the aspect reconstructs the same full master-
  team/vassal city side again, producing an exact duplicate team-threat utility.
  This is independent of F107's present/predicted side construction.
* F138/KI#461 Tactical evalOperational: the branch explicitly evaluates OUR
  readiness for the one target TEAM but is entered once for each reachable target
  member. The same preparation/readiness cost can therefore repeat.
* F139/KI#462 IllWill evalAngeredPartners: MilitaryAnalyst records wars-declared-
  by as PlayerTypes, so one target-team DoW yields B and C; the real
  AI_preDeclareWar path creates one declared-war-on-friend memory per victim
  TEAM. Deduplication must be by victim team, while genuinely separate cascaded
  victim teams remain separate.
* F140/KI#463 GreedForVassals: the fixed -5 capitulated-vassal diplomatic/forced-
  war downside represents one team vassal relationship. Real rival-vassal
  attitude iterates vassal teams and their aggregate cities, but the fixed proxy
  is charged once per vassal member. Player-owned vassal benefits stay additive.
* F141/KI#464 C++ lifetime: WarUtilityAspect is polymorphic but has no virtual
  destructor; WarEvaluator allocates derived aspects and deletes them through
  WarUtilityAspect*. KingMaking/Revolts own std::set members requiring real
  derived destruction. This is standard C++ undefined behavior and survives from
  the original advc.104 hierarchy. C++03-compatible repair: public
  `virtual ~WarUtilityAspect() {}`.
* F142/KI#465 supported OWN_EXCLUSIVE_RADIUS=1: lost tiles are explicitly cached
  per TEAM and then, inside a per-player helper, normalized by current PLAYER land
  before dividing by team size. Equal partition of the same physical team land
  doubles aggregate severity; the significance threshold is roster-partition
  sensitive too. F133's outer denominator repair does not fix this internal
  producer/denominator domain mismatch.

The low-level ScaledNum check changed only consequence wording for zero divisors;
it did not invalidate any of these semantic boundaries.


FINAL MECHANICAL / API / REGISTRY CONTROLS
------------------------------------------
1. CURRENT-vs-BASE MEMBER SURFACE

A normalized member-definition comparison found 84 paired current/Base
WarUtilityAspect.cpp member definitions. Only four current methods have genuine
executable semantic divergence from Base AdvCiv 1.14:

* WarUtilityAspect::evaluate -- reviewed KI#424 weighting fix;
* Risk::preEvaluate -- SAS faraway-target policy, including reviewed KI#425
  team-aware latch/current repair;
* techRefuseThresh -- reviewed KI#426 personality fix;
* Distraction::evaluate -- documented SAS winning-war Distraction cap.

The apparent fifth difference in MilitaryVictory::addConquestsByPartner was
formatting/whitespace only. Thus there is no unexplained SAS-local executable
WarUtilityAspect regression hiding outside the already reviewed/customized
methods. The inherited findings deliberately remain visible because current vs
Base identity cannot disprove an inherited defect.

2. HEADER / DERIVED STATE LIFETIME

The derived-state member audit covered MilitaryVictory vote state, KingMaking
winner sets, Risk's SAS latch, IllWill scratch scalars, Revolts::m_countedCities,
ThirdParty precomputed power state and DramaticArc tension state. State that
persists across member/rival callbacks is either intentional scenario-wide state
or already owned by a finding (especially F132). No uninitialized/stale derived
scalar root survived. Constructor declaration order also does not create a
second lifetime defect next to F141.

3. POINTER / CACHE PROVENANCE

InvasionGraph explicitly guarantees that city IDs exported in its conquest/loss
results correspond to cities present in the agent UWAICache; target selection
checks cache lookup before inserting relevant targets. The alarming unchecked
`ourCache().lookupCity(...)` consumers therefore have a producer contract. No
new dangling/null current root was established.

4. CONTAINER / ITERATOR / STATIC CACHE PASS

No independent iterator invalidation or ownership bug survived. The static
aspectName buffer is consumed/copied at relevant diagnostic sites. Static SAS
GlobalDefine reads in this file have no overlap with runtime setDefine* setters
in the current repository, so caching them does not currently freeze a supported
runtime mutation. The cached Domination VictoryTypes selection is over fixed game
configuration, while the source correctly avoids statically caching the dynamic
adjusted domination population target.

5. LOG / VARARGS PASS

Balanced parsing of every log(...) call found exactly one format/argument-count
mismatch: F108. No second varargs root exists in file 014.

6. DEAD-LOCAL PASS

Only four plausible one-use local declarations surfaced mechanically. The
meaningful unused computation is F115's rAtPeaceTurns. `conqFriendly` and
`conqPleased` are side-effect-free CitySet leftovers that were unused from the
original UWAI implementation while real logic uses weightedConq; they are dead-
code cleanup, not gameplay defects. The fourth hit was a parser/regex false
positive.

7. ASSERTION / DIVISION / BOUNDARY PASS

The remaining assert/denominator candidates either have producer guarantees,
positive floors/release clamps, map to existing roots (notably F105/F133), or
collapse under actual branch reachability. ThirdParty's all-zero defensive-power
expression is a useful control: ScaledNum/MulDiv can produce its error sentinel,
but the immediately clamped nonpositive ratio makes evaluate return before later
power divisions. No new F143 boundary was established.

8. ASPECT REGISTRY / ENUM / XML CONTRACT

A final registry audit mechanically found:

    UWAI::AspectTypes enum entries:       29 unique
    concrete WarUtilityAspect xmlID IDs:  29 unique
    WarEvaluator constructed subclasses: 29 unique

Every enum entry has exactly one xmlID; there are no missing/extra/duplicate IDs,
and every concrete aspect is constructed exactly once (Distraction conditionally
omitted exactly as the existing NUM_ASPECTS assertion expects). All 29 generated
`UWAI_WEIGHT_<ASPECT>` GlobalDefine tags exist in current XML. Construction order
does not need to equal enum order because each aspect resolves its weight/name
through its explicit xmlID and per-aspect reporting carries its own name.

9. CURRENT REVIEWED-KI COLLISION PASS

README_Known_Issues.md currently contains reviewed headings through KI#426 (with
historical numbering gaps). Exact helper/aspect searches for the F104-F142
surfaces find no reviewed owner for these roots; provisional aliases KI#427-
KI#465 do not occur in the reviewed ledger. Existing Distraction/faraway-war
entries describe different SAS policies and were explicitly separated from
F126/F102 during root normalization. No pending F104-F142 root was found to be a
renamed older KI.

10. NEGATIVE CONTROLS PRESERVED

Important rejected hypotheses remain useful closure evidence:

* F113 GET_TEAM(PlayerTypes) broad family -- false because of overloads;
* Affection one-sided team normalization -- defensible pairwise design;
* BorderDisputes first-conqueror order -- conquest sets mutually exclusive under
  InvasionGraph's no-recapture model;
* Open-Borders partner values -- partner-specific territory/trade value can be
  legitimately additive;
* LoveOfPeace repeated fixed cost -- current SAS data has iLoveOfPeace=0 and the
  dormant modmod-facing hook does not meet the current-data finding bar;
* Effort strongest individual rival benchmark -- remains a defensible heuristic,
  lacking a hard conserved team-state contract;
* Loathing war-declaration duplication -- player/nemesis-specific vengefulness,
  unlike F139's exact one-memory-per-victim-team contract;
* GreedForAssets third-party defensibility -- player-specific geography and
  independently controlled armies leave insufficient invariant evidence;
* partnerUtilFromMilitary, SuckingUp and Fidelity -- explicit player-owned/
  player-attitude quantities provide positive controls against blanket
  "teamification";
* ThirdParty all-zero defensive power -- branch/clamp reachability collapses it;
* numerous assertion-only, tuning and current-data-dormant seams -- not promoted.

These controls are part of why the dense team/player finding family is not being
extended merely by analogy.


C014-WIP32 CLOSURE DECISION
---------------------------
After the full method/header/helper surface, Base/current semantic comparison,
producer/consumer and ownership-domain passes, assertion/division/log/dead-local
mechanical sweeps, registry/XML check, reviewed-KI collision scan and an
adversarial reread of the complete live finding ledger, NO independent F143 has
been established.

The remaining surface is now better supported as clean/intentional/covered than
as merely unchecked. F101-F103's current repairs survive regression review;
F104-F112 and F114-F142 survive adversarial review; F113 remains retracted.

WarUtilityAspect.cpp is therefore READY FOR DURABLE FILE-014 CLOSURE. A separate
C015 closure append may now mark 14 / 169 files complete and advance the queue to
current file 015, CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines in practical
6267), without implying that provisional KI#427-KI#465 have already been
independently reviewed or implemented.

No repository source files were modified by this audit pass.
===============================================================================


===============================================================================
C015 DURABLE CHECKPOINT -- FILE 014 CLOSED / FILE 015 QUEUED
===============================================================================

SOURCE IDENTITY AT CLOSURE
--------------------------
Branch: main
HEAD: fe0e4abbe4ef49a910bff90d7db2f02143855c53
Practical commit count: 6267
Base comparison: local AdvCiv 1.14 source archive
Current .cpp queue: 169 files, sorted by current line count longest -> shortest


FILE 014 -- CvGameCoreDLL/WarUtilityAspect.cpp -- 5,024 lines -- REVIEWED / CLOSED
---------------------------------------------------------------------------------
The long C014 investigation is now durably closed after C014-WIP32's final
adversarial/mechanical pass.

Stable file-014 root accounting:

* 41 valid F-roots remain in the historical discovery ledger:
  F101/KI#424-F112/KI#435 and F114/KI#437-F142/KI#465.
* F101-F103 / reviewed KI#424-KI#426 are already independently reviewed, fixed,
  documented and regression-checked in practical 6267.
* F104-F112 and F114-F142 = 38 valid roots still surviving in practical 6267 and
  awaiting independent review/implementation. Their KI#427-KI#465 aliases are
  provisional audit aliases only; the reviewed README_Known_Issues ledger does
  not yet contain those numbers.
* F113 / provisional KI#436 is RETRACTED. GET_TEAM(PlayerTypes) is intentionally
  overloaded and converts through TEAMID. Preserve its old WIP evidence only to
  prevent rediscovery.
* The earlier former F101 / provisional KI#423 hypothesis is also retracted
  history and is not part of the 41 valid roots above.
* Existing F096 / KI#418 was materially broadened by file-014 alive-member
  normalizer consumers; it remains one existing root rather than another F.
* F143 was never allocated.

This closure does NOT assert that every pending candidate should be implemented
unchanged. The album is the investigative handoff. Current source and the
independently reviewed Known Issues ledger remain implementation authority, and
me/Codex review can accept, merge, refine or reject a provisional root as new
runtime/testing evidence warrants.


WHY FILE 014 IS CLOSED
----------------------
Closure is evidence-based rather than time-based. The final passes covered:

* all current member definitions and associated WarUtilityAspect.h state;
* normalized current-SAS vs Base AdvCiv executable differences;
* player/team ownership and per-member/per-rival aggregation domains;
* current-vs-simulated state and target-scope predicates;
* all explicit team-size/living-member normalizers;
* global victory completion/cap semantics;
* denominator, boundary, sign and fixed-point arithmetic seams;
* C/C++ varargs format/argument counts;
* base-pointer/derived-object lifetime and destruction;
* derived member initialization and cross-member lifetime;
* raw pointer/cache-city producer contracts;
* STL container/iterator and static-local/cache behavior;
* computed-but-unused locals and author Tbd./assertion comments;
* supported nondefault configuration reachability;
* aspect enum/xmlID/construction/XML-weight registry integrity;
* current reviewed Known-Issues semantic collisions;
* original/pre-SAS ancestry where needed to distinguish inherited roots from SAS
  regressions;
* adversarial rereads of every live F101-F142 root and multiple negative controls.

The final registry check found 29 unique AspectTypes, 29 unique concrete xmlIDs,
29 unique constructed aspect subclasses and all 29 generated UWAI_WEIGHT_* XML
defines. The final current-vs-Base member comparison found no unexplained SAS-
local WarUtilityAspect executable difference beyond the reviewed/customized
methods already accounted for. The log scan found only F108's one varargs
mismatch; the dead-local scan found only F115's meaningful unused normalization;
the final normalizer scan found no independent issue beyond existing roots such
as F096 and F142.

Repeated attempts to establish F143 instead produced clean controls or mapped
back to an existing root. That is the desired stopping criterion.


IMPORTANT RETRACTION / CONTROL REMINDERS FOR IMPLEMENTATION REVIEW
-----------------------------------------------------------------
* Do NOT implement F113/KI#436 from WIP05-WIP10. GET_TEAM(PlayerTypes) is safe in
  this codebase through CoreAI/CvGamePlay overloads.
* F114/KI#437 remains live despite a temporary adversarial concern: CvGame's
  initialization deliberately establishes self-contact, so the target teammate
  can pass FairPlay's HasMet condition.
* Do not blanket-convert every player-local utility to team-level. Positive
  controls in Affection, Open Borders/partner utility, partner military value,
  SuckingUp, Fidelity and Loathing demonstrate legitimate player-specific state.
* F133/F142 require OWN_EXCLUSIVE_RADIUS=1, which is recommended off but remains
  an explicitly documented/maintained AdvCiv configuration. Preserve that
  supported-config classification when reviewing priority.
* F141/KI#464 is ordinary C++ correctness, not a modeling preference: deleting
  concrete aspect objects through WarUtilityAspect* without a virtual base
  destructor is undefined behavior. Use a C++03-compatible public virtual
  destructor if accepted.
* F105's exact zero-divisor runtime symptom should not be overstated: ScaledNum
  uses Win32 MulDiv semantics. The root is the incorrect exact-threshold state
  and division path, not a promise of one specific crash behavior.


PROGRESS AFTER C014 CLOSURE
---------------------------
Files complete: 14 / 169
Files open: 0 / 169
Last completed file: 014 CvGameCoreDLL/WarUtilityAspect.cpp -- 5,024 lines

NEXT QUEUED FILE:

    015 CvGameCoreDLL/CvSelectionGroup.cpp -- 4,722 lines

Following files by current practical-6267 line-count queue begin:

    016 CvGameCoreDLL/CitySiteEvaluator.cpp -- 4,595 lines
    017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines
    018 CvGameCoreDLL/CvGameInterface.cpp -- 2,965 lines
    019 CvGameCoreDLL/CvDLLButtonPopup.cpp -- 2,814 lines
    020 CvGameCoreDLL/CvInfo_Unit.cpp -- 2,599 lines

C015 is a durable boundary checkpoint. File 015 has NOT yet been audited in this
continuation; start it from current practical 6267 on the next audit pass rather
than carrying WarUtilityAspect hypotheses forward by analogy.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


================================================================================
C015-WIP01 APPEND -- FILE 015 CvSelectionGroup.cpp OPENING / CURRENT-DIFF SURFACE
================================================================================

SOURCE RECONCILIATION
---------------------
Current source authority remains the user's reconciled 2026-08-25T23:11 light-source
snapshot:

- Branch: main
- HEAD: fe0e4abbe4ef49a910bff90d7db2f02143855c53
- Practical commit count: 6267
- Base comparison: local AdvCiv 1.14 source archive
- Civ4CE BtS/Warlords/vanilla source is locally available for ancestry/cross-controls.

Durable C015 closed file 014 WarUtilityAspect.cpp. File 015 is now:

    CvGameCoreDLL/CvSelectionGroup.cpp -- 4,722 lines -- OPEN

No repository source files were modified by this audit.


MECHANICAL CURRENT-vs-BASE OPENING INVENTORY
--------------------------------------------
A comment/string-aware brace parser was used so braces in comments or string literals do
not cause the rough-function-extractor expansion that can otherwise make a small change
look like a giant method diff.

- Current CvSelectionGroup.cpp member definitions: 133
- Base AdvCiv 1.14 member definitions: 132
- Current-only member: getEspionageUnit()
- Method names with any normalized executable/token difference or new definition: 11

    clearUnits
    continueMission_bulk
    deleteUnitNode
    doTurn
    getEspionageUnit
    groupBuild
    groupPathTo
    groupRoadTo
    read
    startMission
    updateTimers

The corrected parser then proves that groupPathTo, groupRoadTo and updateTimers have ZERO
executable/token difference after comments/formatting are stripped. They were false change
signals from the earlier rough inventory and therefore leave only eight methods with real
current-source semantic/instrumentation differences plus the current-only helper.

CvSelectionGroup.h was independently token-compared after stripping comments/formatting.
Its ONLY semantic addition versus Base AdvCiv 1.14 is:

    CvUnit const* getEspionageUnit() const;

No layout-changing data member was added; the existing sizeof(CvSelectionGroup)==80 ABI
assert remains untouched.


CHANGED-METHOD CLASSIFICATION -- NO F143 ESTABLISHED
----------------------------------------------------

1. clearUnits / deleteUnitNode -- EXISTING KI#163 HARDENING

Current clearUnits deliberately no longer tears a whole group down by repeatedly invoking
the normal single-unit deleteUnitNode callback path. It directly resets group-owned
activity/automation/mission state and then clears m_units once. deleteUnitNode separately
adds a null-node guard while preserving its ordinary single-unit-removal behavior.

This is exactly the independently documented KI#163 rare group-teardown crash hardening.
The Known Issues entry explicitly describes the same re-entrancy/lifetime concern and the
same direct-full-teardown repair. Do not duplicate it as a new file-015 finding.

2. getEspionageUnit / startMission MISSION_ESPIONAGE -- EXISTING KI#398 FIX

Current source adds getEspionageUnit(), which returns the first selected unit that can
currently perform Espionage. startMission's MISSION_ESPIONAGE case also skips a selected
unit that cannot currently perform Espionage before allowing the eligible Spy to try.

This is exactly independently reviewed/fixed KI#398: inherited BtS group-Espionage actor
identity could expose a valid Spy mission but let an earlier movable non-Spy abort it.
Callers in help/popup code use the same centralized actor helper. No new root here.

3. doTurn -- WORKER DIAGNOSTICS + SAFE STATIC DEFINE CACHE

The material additions are level-3 Worker diagnostics around turn-boundary group state and
a static-local cache of MIN_TIMER_UNIT_DOUBLE_MOVES. The diagnostic reads/logs state but
does not mutate gameplay state.

A repository-wide runtime-define-setter scan found no setter for
MIN_TIMER_UNIT_DOUBLE_MOVES. Current runtime setDefineINT/FLOAT/STRING calls modify other
names (camera/minimap/map-generation defines). Caching this define therefore does not
currently freeze a dynamically mutated supported value. No behavioral defect established.

4. groupBuild -- SAFE COLOR LOOKUP HOIST

Current source caches GC.getColorType("WHITE") in a static local and uses the result in the
same UI message. This is a lookup of fixed XML type identity, not a mutable GlobalDefine.
No gameplay/UI semantic change established.

5. continueMission_bulk -- THREE DISTINCT CLASSES, ALL ACCOUNTED FOR SO FAR

(a) Level-3 Worker execution diagnostics are observational.

(b) The stale AI build-target guard is already present in the local AdvCiv 1.14 comparison
source and is independently documented as KI#176 in SAS history. MISSION_BUILD stores a
build type but no target coordinates, so non-route builds are canceled if the group is no
longer on AI_getMissionAIPlot. This prevents a stale queued build from executing on the
wrong physical plot.

(c) Current SAS additionally preserves a queued follow-up after a MISSION_BUILD consumes
all Worker movement:

    if (missionData.eMissionType != MISSION_BUILD || readyForMission())
        activateHeadMission();
    else setActivityType(ACTIVITY_MISSION);

Practical 6018 introduced this deliberately after observed Berlin/Beijing/Cologne feature-
removal follow-ups were discarded: immediate activation deleted the queued improvement,
while simply doing nothing left the group AWAKE and exposed it to AI turn-start cleanup.
The current branch instead keeps the waiting follow-up in MISSION activity until a later
turn. The original short-circuit semantics around groupBuild remain preserved, and current
history documents this as an empirically validated Worker reliability change. No current
contract failure has been established from it in this opening pass.

6. read -- REMOVED LEGACY SAVE MIGRATION ONLY

write() still writes uiFlag=3 and serializes, in order:

    ID, mission timer, force-update, owner, activity, automation,
    unit list, known-enemy list, current MissionData queue.

Current read() consumes exactly that current-format sequence. Relative to Base 1.14 it
removes only old uiFlag migration branches (pre-knownEnemies saves, MissionDataLegacy,
and pre-v3 MOVE_ROUTE_TO conversion).

This matches the project's already-established serialization policy from earlier file
reviews: same-version current read/write pairing is authoritative; cross-version save
migration is not a supported requirement. No same-version misalignment established.

7. groupPathTo / groupRoadTo / updateTimers -- FALSE CHANGE SIGNALS

The robust token/body comparison finds no executable difference at all. They remain Base
AdvCiv 1.14 behavior in current source after comment/format normalization.


OPENING NEGATIVE CONTROLS
-------------------------
- CvSelectionGroup header ABI/layout has not changed; getEspionageUnit is declaration-only.
- No MIN_TIMER_UNIT_DOUBLE_MOVES runtime setter exists in current C++/Python/map-script
  sources, so its static-local cache preserves current semantics.
- Worker logging branches are gated diagnostics and have no intended state mutations.
- Same-version CvSelectionGroup serialization is paired at uiFlag 3.
- KI#163, KI#176 and KI#398 are explicitly collision-mapped rather than rediscovered.


NEXT AUDIT CURSOR
-----------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP01
NEW FILE-015 ROOTS: none yet
NEXT ROOT ID: F143 UNUSED

Next pass: audit the large inherited/current-common mission/group surface rather than
stopping at the small SAS diff. Priority order is startMission/continueMission_bulk and
canDoMission as one availability/execution contract, then group movement/attack/build,
linked-list/group lifetime helpers, mission queue/timers, path/cargo helpers, and finally
serialization/header/caller closure. Compare Civ4CE BtS and local ancestry when they offer
an independent contract or fix control; do not promote tuning, dormant unsupported states,
or already-reviewed KI roots merely because the inherited code is old.


================================================================================
C015-WIP02 APPEND -- F143 / Sea Patrol mixed-group persistence contract
================================================================================

F143 / provisional KI#466 -- CONFIRMED -- INHERITED ADVCIV SEA-PATROL GROUP DEFECT
MIXED SEA GROUP CAN ACCEPT PATROL, THEN AN INELIGIBLE MEMBER WAKES THE WHOLE GROUP
--------------------------------------------------------------------------------
Status: NEW CURRENT DEFECT ROOT, inherited from the interaction created by AdvCiv
advc.004k. Static reachability/data evidence strong; no runtime game test performed by
this audit.

Mission-availability contract:

CvSelectionGroup::canDoMission(MISSION_SEAPATROL) treats the order as valid when at least
one member can Sea Patrol. With the ordinary canStartMission path bCheckMoves is false, so
once a qualifying member is found the function returns true immediately. An earlier
non-qualifying member is tolerated so long as it can still move.

    if (!bValid && pUnit->canSeaPatrol(pPlot, true))
    {
        if (!bCheckMoves)
            return true;
        bValid = true;
    }
    if (!pUnit->canMove())
        return false;

CvSelectionGroup::startMission then handles MISSION_SEAPATROL as a WHOLE-GROUP activity:

    setActivityType(ACTIVITY_PATROL);
    bDelete = true;

Unlike MISSION_AIRPATROL and MISSION_PARADROP, Sea Patrol does not collect unsuitable
members into apUnitsLeftBehind and does not split them from the group. The mission node is
deleted, but the mixed selection group remains intact in ACTIVITY_PATROL.

Turn-boundary contradiction:

CvUnit::isSeaPatrolling() is group-activity based. Every sea-domain member of such a group
therefore reports itself as Sea Patrolling regardless of whether that unit individually
satisfies CvUnit::canSeaPatrol:

    getGroup()->getActivityType() == ACTIVITY_PATROL &&
    getDomainType() == DOMAIN_SEA

CvUnit::doTurn then contains AdvCiv's advc.004k validity cleanup:

    if (isSeaPatrolling() && !canSeaPatrol())
        getGroup()->setActivityType(ACTIVITY_AWAKE);

The default canSeaPatrol call here deliberately ignores the current activity, but still
rejects a sea unit that cannot fight, is OnlyDefensive, is on an invalid patrol plot, etc.
Consequently one ineligible companion wakes the ENTIRE selection group, terminating the
persistent patrol of the member that made the group command legal in the first place.

Current-data supported reproduction:

- CvUnit::canJoinGroup permits units to join when owner, plot and DOMAIN match; it does not
  require common mission capability.
- UNIT_DESTROYER is DOMAIN_SEA, combat-capable and not OnlyDefensive, and can satisfy Sea
  Patrol on a normal valid water plot near a guardable owned water improvement.
- UNIT_TRANSPORT is DOMAIN_SEA but current XML has bOnlyDefensive=1, so canSeaPatrol rejects
  it.
- UNIT_WORKBOAT is also DOMAIN_SEA and has iCombat=0, so canSeaPatrol rejects it.

Thus a human can form e.g. Destroyer + Transport (or Destroyer + Work Boat), with both
members still able to move, on a valid patrol plot. The qualifying Destroyer makes
MISSION_SEAPATROL available and startMission sets the whole mixed group to PATROL. At the
next unit turn, the Transport/Work Boat satisfies isSeaPatrolling() by group activity but
fails canSeaPatrol(), so its doTurn changes the whole group's activity to AWAKE. The valid
Destroyer silently loses its patrol after only that turn boundary.

Ancestry/control:

- Civ4CE/BtS already uses the broad group-availability shape where one qualifying unit can
  expose Sea Patrol.
- AdvCiv practical 3866 / ffbe05255f, title "Usability improvements for Sea Patrol"
  (advc.004k), introduced the per-unit doTurn wake when a patrolling unit no longer passes
  canSeaPatrol and changed the availability query to ignore the current patrol activity
  appropriately when displaying the command.
- The commit's stated intent was to make Sea Patrol available only when there is actually
  something to guard and to improve patrol visibility/usability. It did not add mixed-
  member separation or change the inherited any-member group availability rule.
- Base AdvCiv 1.14 and current SAS practical 6267 both retain the resulting interaction.
- No existing KI or earlier audit finding mentioning Sea Patrol/ACTIVITY_PATROL collides
  with this root.

Why this is not merely a UI/tuning preference:

The same group-level API first says the order is executable and places the group in a
persistent activity, then a member that was explicitly tolerated by that availability
contract invalidates the persistent state for the qualifying member. Repartitioning the
same selected ships into two groups makes the valid warship patrol persist; grouping them
causes it to stop despite a successful accepted command.

Repair direction:

Reconcile mission availability, group activity and turn-boundary cleanup. Plausible
repairs include:

1. At mission start, split unsuitable Sea Patrol members from the patrol group, analogous
   to Air Patrol's apUnitsLeftBehind handling; or
2. Require every member of a group to be individually Sea-Patrol-capable before accepting
   the group mission (less friendly for mixed escort groups); or
3. Keep mixed groups intact, but make the doTurn cleanup wake the group only when NO member
   remains able to Sea Patrol rather than when the current member cannot.

Any repair should preserve advc.004k's intended wake when the patrol as a whole is no
longer meaningful (e.g. no reachable guarded improvement), rather than simply deleting the
validity cleanup.


WIP02 CURRENT CURSOR
--------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP02
NEW FILE-015 ROOTS: F143 / provisional KI#466
NEXT ROOT ID: F144 UNUSED

Continue the canDoMission/startMission/continueMission contract audit, especially all
mixed-group persistent activities and executor cases with early exit/whole-group state.
Then move through group movement/attack/build, cargo/amphibious handling, linked-list and
split/merge lifetime, timers/pathing, and caller/header closure. Do not generalize F143 to
missions that explicitly split unsuitable units or whose effects are legitimately
per-member.

===============================================================================
C015-WIP03 APPEND -- CvSelectionGroup.cpp persistent blockade lifecycle
===============================================================================

F144 / provisional KI#467 -- CONFIRMED -- INHERITED ADVCIV BLOCKADE GROUP DEFECT
ONE FORMER BLOCKADER BECOMING INVALID WAKES/CANCELS STILL-VALID GROUP BLOCKADERS
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED DEFECT ROOT. Independent of F143/KI#466.

Root contract:
- Blockade state is legitimately unit-local (`CvUnit::m_bBlockading`), while the
  waiting/activity state is group-local (`ACTIVITY_PLUNDER`).
- `CvUnit::doTurn` was changed by advc.033 so a blockading unit that can no longer
  legally plunder clears its own flag and wakes its group:

      if (isBlockading())
      {
          if (canPlunder(kPlot))
              collectBlockadeGold();
          else
          {
              setBlockading(false);
              getGroup()->setActivityType(ACTIVITY_AWAKE);
          }
      }

- `CvSelectionGroup::setActivityType` always calls `setBlockading(false)` before
  changing activity. The group helper applies that call to EVERY member.
- Therefore one former blockader becoming invalid does not merely wake/remove that
  unit's blockade; it clears every still-valid member's blockade too.

Concrete supported current-data reproduction:
1. Human groups a Destroyer and Privateer on the same valid water plot during war.
   `CvUnit::canJoinGroup` requires only same owner/plot/domain; hidden nationality
   and blockade eligibility are not grouping restrictions.
2. Current UNIT_DESTROYER and UNIT_PRIVATEER are both DOMAIN_SEA and bPillage=1.
   During the war both can pass `canPlunder` and MISSION_PLUNDER calls `plunder()`
   on each member, setting each unit's own m_bBlockading=true.
3. Peace is then made while the group remains on a legal neutral/unowned blockade
   plot. `CvTeam::makePeace` calls `updatePlunder(-1,false)` before changing war
   state and `updatePlunder(1,false)` afterward, but these calls only rebuild the
   blockade-count effects; they do NOT clear m_bBlockading or separate the group.
4. After peace, Destroyer can no longer plunder because it is not a pirate and the
   owner team has no wars. Privateer remains eligible because current XML gives it
   bHiddenNationality=1 and bAlwaysHostile=1; `canPlunder` deliberately exempts a
   pirate from the no-war rejection (subject to the ordinary plot restrictions).
5. When the Destroyer reaches CvUnit::doTurn, advc.033 detects that its existing
   blockade is no longer legal. It clears the Destroyer's local flag, then wakes
   the whole selection group.
6. The group wake calls CvSelectionGroup::setBlockading(false), which clears the
   still-valid Privateer's blockade as well. If the Privateer had not been grouped
   with the Destroyer, its blockade would legally persist after peace.

The result therefore depends on grouping unrelated persistent blockaders: one unit
whose legal state changes can terminate another member's still-valid blockade.
This is not merely visual/activity UI state; the Privateer's actual blockade flag
and plot blockade effects are removed.

Ancestry:
- Civ4/BtS already has unit-local blockade state and group PLUNDER activity.
- AdvCiv change advc.033 (visible in the local practical-1407 ancestry diff and in
  Base AdvCiv history) added the `canPlunder` recheck in CvUnit::doTurn and, on
  failure, `setBlockading(false); group->setActivityType(ACTIVITY_AWAKE);`.
- The corresponding history explicitly says non-Privateers can no longer blockade
  when at peace with everyone and "blockading units are awoken if a blockade is no
  longer allowed" so forgotten ships stop blockading after war.
- That intent is sound for the invalid unit, but the inherited group-wide wake
  also clears other members whose blockade remains allowed.
- Base AdvCiv 1.14 and practical-6267 SAS retain this interaction.

Why this is separate from F143/KI#466:
- F143 Sea Patrol accepts a mixed group where some members were NEVER individually
  eligible, represents patrol through one group activity, and then lets an
  ineligible companion interpret itself as patrolling and wake everybody.
- F144 Blockade starts with multiple individually valid per-unit blockades. The
  failure occurs later when eligibility diverges and one unit's correct local
  invalidation is escalated into group-wide blockade cancellation.
- Fixing Sea Patrol mission admission/splitting does not repair this blockade
  lifecycle, and fixing this lifecycle does not repair F143's initial mixed-group
  patrol contract.

Repair direction:
- Preserve advc.033's requirement that an invalid former blockader stop blockading.
- Do not wake/clear the whole PLUNDER group merely because one member becomes
  invalid while another member can still legally maintain a blockade.
- One conservative shape is: clear the invalid unit's local blockade, then wake the
  group only if no remaining member is still blockading/can legally plunder;
  otherwise separate the invalid unit or preserve the valid members' PLUNDER state.
- Be careful that `setActivityType(ACTIVITY_AWAKE)` itself clears every member's
  blockade, so simply moving that call without changing its condition is not
  sufficient.

CURRENT FILE-015 CURSOR AFTER WIP03
----------------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP03
NEW FILE-015 ROOTS: F143/KI#466, F144/KI#467
NEXT ROOT ID: F145 UNUSED

Continue the persistent-activity pipeline (Sleep/Fortify/Heal/Sentry/Plunder/
SeaPatrol/AirPatrol) and then the wider canDoMission/startMission/continueMission
availability/executor contract. Preserve one-root-per-cause discipline; do not
assume every group-level activity issue is F143/F144.
No repository source files were modified.

LOCAL RECONCILIATION AFTER C015-WIP03 IMPORT

- ChatGPT-5.6-Sol's C014-WIP31/WIP32, durable C015 checkpoint and C015-WIP01 through WIP03 append remains verbatim as evidence. It is compatible with current authority at HEAD 619d87112 plus the reviewed KI#434/KI#435 worktree repairs, despite using the older practical-6267 / HEAD fe0e4abbe source baseline.
- The durable closure's retraction of F113 / provisional KI#436 is accepted: `GET_TEAM(PlayerTypes)` deliberately resolves through `TEAMID`, matching the existing KI#311/KI#387 and AGENTS control. The reviewed KI#434 code/documentation has been corrected not to repeat the superseded WIP05 hypothesis; its independent old-owner AP-membership defect remains fixed.
- F104-F108 / KI#427-KI#431 are fixed at current HEAD. F111-F112 / KI#434-KI#435 are fixed in the reviewed worktree. F109-F110 / provisional KI#432-KI#433 remain pending independent review; the C014 closure inventory does not itself promote an implementation.
- F142 / provisional KI#465 and the new file-015 F143-F144 / provisional KI#466-KI#467 are collision-free against the current reviewed Known Issues ledger and remain provisional. `WarUtilityAspect.cpp` is durably closed as an audit surface, while `CvSelectionGroup.cpp` remains open at C015-WIP03. No implementation is promoted from the imported append in this reconciliation. (GPT-5.6-Sol)


================================================================================
C015-WIP04 APPEND -- CvSelectionGroup.cpp COMMAND_LOAD shared-capacity contract
================================================================================

F145 / provisional KI#468 -- CONFIRMED -- INHERITED ADVCIV GROUP-LOAD DEFECT
THE ALL-SELECTED LOAD PRECHECK REUSES THE SAME FREE CARGO SLOT FOR EACH UNIT
----------------------------------------------------------------------------
Status: NEW CURRENT INHERITED DEFECT ROOT. Static UI/executor reproduction strong;
no runtime game test performed by this audit.

Intended AdvCiv contract is explicit:

CvSelectionGroup::canDoCommand normally accepts a group command when ANY selected
unit can execute it. `COMMAND_LOAD` is deliberately special-cased by advc.123c:

    if (pUnit->canDoCommand(...))
    {
        if (eCommand != COMMAND_LOAD)
            return true;
    }
    /* Normally, a group can do a command if any unit can do it,
       but in the case of loading, it seems easier to make an exception than
       to have the load command fail for some of the selected units. */
    else if (eCommand == COMMAND_LOAD)
        return false;
    ...
    return (eCommand == COMMAND_LOAD);

Thus `COMMAND_LOAD` is accepted only if every selected/group unit individually
passes `CvUnit::canDoCommand(COMMAND_LOAD)`.

The individual check is not a group-capacity reservation:

- CvUnit::canDoCommand(COMMAND_LOAD) calls `canLoadOntoAnyUnit(getPlot(), ...)`.
- `canLoadOntoAnyUnit` returns true as soon as it finds one transport for which
  `canLoadOnto` succeeds.
- `canLoadOnto` in turn tests the transport's CURRENT
  `cargoSpaceAvailable(specialCargo, domainCargo)`.
- Repeating that independent query for the next selected unit does not reserve
  the slot that the previous unit would consume.

Therefore several selected cargo units can all answer "yes" by pointing at the
same one remaining cargo slot.

Concrete supported reproduction:

1. Put one friendly sea transport on a plot with exactly one compatible free
   cargo slot (for example a normal multi-slot transport already carrying all
   but one of its capacity).
2. Put two movable compatible land units on the same plot and select/group both.
3. Before execution, unit A's `canLoadOntoAnyUnit` sees the one free slot and
   returns true. Unit B independently sees that SAME unchanged slot and also
   returns true. The special group `COMMAND_LOAD` test therefore succeeds even
   though the transport cannot carry both selected units.
4. The UI handles generic Load through `BUTTONPOPUP_LOADUNIT`. With only one
   eligible transport, `launchLoadUnitPopup` auto-selects that transport and
   sends `GAMEMESSAGE_DO_COMMAND, COMMAND_LOAD_UNIT` without requiring a choice.
5. `CvGame::selectionListGameNetMessage(GAMEMESSAGE_DO_COMMAND, ...)` iterates
   every selected unit and sends the command sequentially.
6. Unit A executes first and fills the final slot. Unit B then revalidates
   `COMMAND_LOAD_UNIT`; `cargoSpaceAvailable` is now zero, so its command fails.

The result is a partial load from a command whose AdvCiv group-level exception
was introduced specifically to avoid a load command failing for some selected
units.

The same root can occur with multiple transports or special-cargo constraints:
checking that each selected unit has *some* individually legal destination does
not prove that a simultaneous assignment of all selected units to the finite
transport slots exists. The one-transport/one-free-slot/two-unit example is the
minimal deterministic reproduction.

Popup-target contract strengthens the proof:

`launchLoadUnitPopup` lists a candidate transport when

    pSelectionGroup->canDoCommand(COMMAND_LOAD_UNIT,
        pUnit->getOwner(), pUnit->getID())

is true. Unlike the special generic `COMMAND_LOAD` branch,
`COMMAND_LOAD_UNIT` uses the ordinary ANY-member group rule. Thus even when the
Load button's preliminary test happens to have enough aggregate capacity spread
across several ships, the popup can still offer a particular transport that can
load only a subset of the selection and then issue that target command to all
selected units.

Ancestry/control:

- Vanilla/BtS/Civ4CE already has the ordinary per-unit loading/execution model.
- AdvCiv advc.123c added the current all-selected `COMMAND_LOAD` exception and
  the move-left loading restriction. The local pre-SAS history contains the
  exact introducing comment quoted above.
- Base AdvCiv 1.14 and practical-6267 SAS retain the independent per-unit
  precheck plus ordinary ANY-member `COMMAND_LOAD_UNIT` popup filtering.
- The earlier C006/CvDLLWidgetData closure had recorded "Load deliberately
  requires all selected units to be loadable" as a negative control. F145 does
  not contradict the local statement that each unit is checked; it overturns
  the stronger implicit conclusion that those independent checks guarantee an
  all-units load. Shared finite cargo capacity was not reserved in that earlier
  pass.
- No reviewed KI or earlier F-root found in the current ledger covers this
  shared-capacity feasibility failure.

Why this is not merely UI preference:

The source states the desired group semantic explicitly: avoid a Load command
that succeeds for only part of the selected units. The implemented precondition
is mathematically insufficient for that stated goal because each unit is tested
against the same unconsumed capacity. The deterministic one-slot/two-unit case
violates the comment without relying on AI tuning, unusual XML or stale state.

Repair direction:

- Make group Load feasibility capacity-aware rather than checking each unit in
  isolation. For a generic Load command, reserve compatible transport slots
  while validating the selected units (potentially an assignment/matching
  problem when transports have different special/domain cargo restrictions).
- For `COMMAND_LOAD_UNIT`, offer/accept a target transport for a multi-unit
  selection only when that transport can accommodate every selected unit that
  the command is supposed to load; alternatively make partial loading explicit
  in UI/semantics instead of claiming the all-selected invariant.
- Preserve advc.123c's separate rule that land cargo must have movement left at
  command time, including its first-check/after-move nuance.


WIP04 CURRENT CURSOR
--------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP04
NEW FILE-015 ROOTS: F143/KI#466, F144/KI#467, F145/KI#468
NEXT ROOT ID: F146 UNUSED

Mission availability/executor and persistent-activity families have now been
systematically classified. Continue through movement/cargo/amphibious group
contracts, especially `setTransportUnit`, `groupAmphibMove`, `groupMove`, cargo
move readiness and split/merge behavior. Then audit linked-list/group lifetime,
mission timers/pathing and serialization before adversarial closure.
No repository source files were modified.



===============================================================================
C015-WIP05 APPEND -- CvSelectionGroup.cpp cargo/lifetime controls + Spy wake root
===============================================================================

CURRENT CURSOR / SOURCE AUTHORITY
---------------------------------
- Current source remains reconciled AdvCiv-SAS practical 6267 / HEAD
  fe0e4abbe4ef49a910bff90d7db2f02143855c53.
- FILE 015 remains CvGameCoreDLL/CvSelectionGroup.cpp -- OPEN.
- WIP04 remains authoritative through F145 / provisional KI#468.
- This append establishes F146 / provisional KI#469 and records several
  deliberately rejected neighboring cargo/split/merge hypotheses. F147 is unused.


F146 / provisional KI#469 -- CONFIRMED -- INHERITED K-MOD / ADVCIV UI-LIFECYCLE DEFECT
SLEEPING-SPY MAX-BONUS WAKE TESTS ONLY THE GROUP HEAD, SO A GROUPED NON-HEAD SPY NEVER WAKES
-----------------------------------------------------------------------------------------
Root contract:
- K-Mod practical 1243 / commit 85efabbf9f is explicitly titled
  "Sleeping spies now wake when they reach their maximum bonus."
- Its CvSelectionGroup::doTurn implementation deliberately checks the wake before
  CvUnit::doTurn increments fortify turns, so a sleeping Spy at
  MAX_FORTIFY_TURNS - 1 is awakened exactly as it reaches the capped stationary
  espionage preparation bonus.
- Current Base AdvCiv 1.14 and SAS retain the same implementation and even the
  explicit limitation in the source comment: "I'm only checking the head unit."

Current implementation:

    if (isHuman() && getActivityType() == ACTIVITY_SLEEP)
    {
        if (pHeadUnit->isSpy() && pHeadUnit->getPlot().getTeam() != getTeam())
        {
            if (pHeadUnit->getFortifyTurns() ==
                GC.getDefineINT(CvGlobals::MAX_FORTIFY_TURNS) - 1)
            {
                setActivityType(ACTIVITY_AWAKE);
            }
        }
    }

Why a real Spy can be outside that test:
- CvUnit::canJoinGroup permits same-owner, same-plot units of the same domain to
  group; it has no Spy-vs-combat-unit exclusion.
- MISSION_SLEEP is a group mission exposed when ANY member passes canSleep(). A
  Spy is not fortifyable and therefore canSleep(); startMission then sets the
  WHOLE selection group to ACTIVITY_SLEEP.
- CvSelectionGroup::addUnit orders members by descending AI_groupFirstVal.
  UNITAI_SPY has first value 9. Current UNIT_SWORDSMAN / UNIT_MACEMAN default to
  UNITAI_ATTACK_CITY, whose first value is 13-19 depending combat capability;
  UNIT_AXEMAN defaults to UNITAI_ATTACK and likewise outranks a Spy in ordinary
  cases. Thus an escort can deterministically be the group head while the Spy is
  a later member.
- Such same-owner land units can coexist and group in a foreign open-borders plot.

Concrete reproduction:
1. Human Spy and Swordsman/Maceman share a foreign open-borders plot and are
   grouped. The attack-city escort sorts ahead of UNITAI_SPY and is group head.
2. The Spy makes MISSION_SLEEP available; the command sets the whole group to
   ACTIVITY_SLEEP.
3. Each stationary, non-cargo unit independently increments its fortify turns in
   CvUnit::doTurn, and setFortifyTurns caps the value at MAX_FORTIFY_TURNS.
4. The espionage mission cost modifier uses the ACTING Spy's own fortify turns:

       iModifier *= 100 - pSpyUnit->getFortifyTurns() *
           ESPIONAGE_EACH_TURN_UNIT_COST_DECREASE;

5. At the turn boundary where the Spy has MAX_FORTIFY_TURNS - 1, the group-level
   convenience check looks only at the non-Spy escort head. It therefore does not
   wake the group.
6. The Spy's own doTurn increments/caps it at the maximum stationary bonus, but
   the group remains asleep. The advertised K-Mod wake behavior can remain absent
   indefinitely until some unrelated wake condition occurs.

This is not an espionage-mission actor-selection duplicate:
- KI#398 repairs choosing/reacquiring the acting Spy for MISSION_ESPIONAGE and its
  popup chain.
- F146 occurs before any espionage mission is selected and concerns the persistent
  sleeping-group lifecycle after a non-head Spy reaches its stationary bonus.

Ancestry:
- K-Mod practical 1243 / 85efabbf9f introduced the feature and the head-only test
  together.
- Base AdvCiv 1.14 retains it; current SAS retains it unchanged apart from nearby
  logging/formatting changes.

Consequence:
- The human convenience feature fails for a supported mixed land group, so the
  player may leave a fully prepared Spy asleep instead of receiving the intended
  wake/reminder at maximum discount.
- This is primarily a UI/usability defect rather than an AI-strength defect, but
  its promised behavior and deterministic current-data reproduction are strong.

Repair direction:
- While a human group is ACTIVITY_SLEEP, scan all members for a Spy in foreign
  territory whose fortify turns equal MAX_FORTIFY_TURNS - 1; wake the group when
  any such Spy reaches the threshold.
- Alternatively split/wake only the qualifying Spy if preserving the escort's
  sleep is preferable, but that is a larger behavior change.
- Preserve the pre-CvUnit::doTurn timing so the notification/wake still occurs
  exactly as the qualifying Spy reaches the capped bonus.


CARGO / AMPHIBIOUS / SPLIT-MERGE NEGATIVE CONTROLS
---------------------------------------------------
1. getLandCargoGroups duplicate group pointers are inefficient but currently inert.
   - The helper appends one group pointer per land cargo UNIT rather than
     deduplicating groups. handleBoarded can therefore put the same group into
     aAwake more than once and eventually call mergeIntoGroup on itself.
   - CvUnit::joinGroup immediately returns when the destination is the unit's
     existing group.
   - mergeIntoGroup's only other potentially behavioral branch changes UnitAI when
     a source member has greater AI_groupFirstVal than the target head. addUnit
     maintains descending AI_groupFirstVal ordering, so no member of a group can
     outrank its own head under the tested invariant.
   - Result: self-merge is wasted traversal / robustness cleanup, not a current
     gameplay defect. Do not allocate F147 for it.

2. setTransportUnit split-by-head-capacity did not yield a shipped-data defect.
   - The only C++ selection-group caller is CvUnitAI::AI_load.
   - Current AI_load callers are land-unit loading paths targeting ordinary
     land-domain transports. Those ships use SpecialCargo=NONE and accept the
     relevant land members, so the feared case where splitGroup selects members
     incompatible with the head-compatible capacity could not be established in
     current supported data/callers.
   - Specialized Carrier/Missile cargo restrictions apply to DOMAIN_AIR and are
     not reached by this land AI_load family.

3. groupAmphibMove already deduplicates cargo groups.
   - K-Mod explicitly repaired the old per-cargo-carrying-unit repetition by
     collecting distinct CvSelectionGroup pointers with std::find.
   - It requires each cargo group canAllMove before issuing the landing mission.
     Ordinary groupMove then peels units that cannot enter the actual destination
     into behind-groups, which is the intended general movement behavior rather
     than a stale-pointer/cargo-group failure.

4. canCargoAllMove matches its current transport-AI consumers.
   - It checks every LAND cargo unit whose transport belongs to this entire sea
     selection group, not merely cargo of one head ship; the K-Mod comment states
     that this whole-group replacement was intentional.
   - Current ferry/assault/missionary/spy-sea callers use it as a readiness gate
     before moving/landing the transport group. No contradictory per-ship contract
     was established.

5. splitGroup / mergeIntoGroup linked-list mutation is structurally safe in the
   reviewed core paths.
   - FOR_EACH_UNIT_VAR_IN caches the next CLinkList node before executing the loop
     body specifically so deleting/moving the current unit is safe.
   - splitGroup reserves representation for the old head AI in the remainder and
     the requested new head AI in the split group; current ppOtherGroup callers
     either initialize their pointer or do not request it.
   - No current iterator invalidation, empty-remainder dereference, or head-loss
     root was established in this pass.


C015-WIP05 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp -- OPEN
PHYSICAL CHECKPOINT: C015-WIP05
LIVE FILE-015 ROOTS: F143-F146 / provisional KI#466-KI#469
NEXT ROOT ID: F147 UNUSED

Continue with group attack/movement contracts, then top-of-file turn/timer/sentry
state, mission-queue lifetime, pathing/static pathfinder ownership, serialization,
and finally a full file-015 adversarial closure. No repository source files were
modified.


===============================================================================
C015-WIP06 APPEND -- CvSelectionGroup.cpp sentry actor-semantics audit
===============================================================================

F147 / provisional KI#470 -- CONFIRMED -- INHERITED BTS/ADVCIV SENTRY HOSTILITY-ACTOR MISMATCH
SENTRY ALERT BORROWS ONE MAX-RANGE UNIT'S ALWAYS-HOSTILE SEMANTICS FOR THE WHOLE GROUP
----------------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED GAMEPLAY/UI-LIFECYCLE DEFECT ROOT. Static/current-data proof strong.

Root contract:
- CvSelectionGroup::sentryAlert first scans every member only to choose ONE pHeadUnit:
  the member with greatest visibilityRange()+1.
- It then uses that same unit both as the LOS/range representative and as the combat-
  hostility actor in every visible-enemy test:

      if (!kPlot.isVisibleEnemyUnit(pHeadUnit, pLoopUnit))
          continue;

- The LOS/range optimization is valid for ordinary same-plot group members: canSeePlot
  uses the shared team plus range, and invisibility visibility is team-scoped.
- Enemy classification is NOT necessarily shared. CvPlot::isVisibleEnemyUnit(attacker,
  potentialEnemy) passes attacker->isAlwaysHostile(plot) into PUF_isEnemy. Thus an
  always-hostile/hidden-nationality member can legally treat a neutral foreign unit as
  an enemy while an ordinary teammate does not.

Current supported reproduction:
1. Current UNIT_PRIVATEER is DOMAIN_SEA, UNITCOMBAT_NAVAL, bHiddenNationality=1 and
   bAlwaysHostile=1.
2. Current UNIT_DESTROYER is DOMAIN_SEA and ordinary (both flags 0).
3. CvUnit::canJoinGroup imposes no hidden-nationality/always-hostile compatibility
   rule; same owner, plot and domain are sufficient here. A Privateer and Destroyer can
   therefore legally share a human selection group.
4. Current PROMOTION_SENTRY is available to UNITCOMBAT_NAVAL and gives
   iVisibilityChange=+1. Give it to the ordinary Destroyer while the Privateer lacks it.
   sentryAlert then deterministically selects the Destroyer as pHeadUnit because it has
   the strictly greatest visibilityRange().
5. Put the group on MISSION_SENTRY in neutral/open sea and let a visible ship belonging
   to a team with which the owner is at peace newly enter even the Privateer's ordinary
   visibility radius.
6. For the chosen Destroyer, isAlwaysHostile is false, so PUF_isEnemy reduces to actual
   team war state and says the neutral ship is not an enemy.
7. For the Privateer on the same tile, isAlwaysHostile is true outside a friendly/base
   plot; PUF_isEnemy therefore regards the same foreign combat owner as hostile even
   without a declared war. That ship is a legally attackable hostile target for one
   member of the sentry group.
8. Because sentryAlert tests hostility only through the max-range Destroyer, the group
   remains asleep instead of waking for the Privateer's newly visible enemy.

Why this is not a generic visibility-range problem:
- All members occupy the same plot/team. Using the largest range for geometrical LOS is
  a valid optimization and does not need to be replaced with N complete radius scans.
- Invisible detection is also team visibility in the called path.
- The mismatched quantity is specifically the UNIT-local attacker hostility semantics
  (notably isAlwaysHostile / hidden-nationality combat behavior). The scan actor was
  selected for range, a property unrelated to that combat relationship.

Ancestry:
- Local Civ4CE BtS CvSelectionGroup::sentryAlert already chooses one maximum-range unit
  and calls pPlot->isVisibleEnemyUnit(pHeadUnit). Thus the root predates AdvCiv.
- AdvCiv advc.004l replaced the immediate boolean scan with known-enemy identity
  tracking so units already in range would not wake Sentry repeatedly, but retained the
  same one-unit hostility actor. Base AdvCiv 1.14 and current SAS therefore preserve the
  inherited mismatch.
- Current Known Issues contains hidden-nationality/always-hostile issues in other
  producer contexts, but no collision with this sentry group-actor contract was found.

Consequence:
- A legal heterogeneous sentry group can fail to wake when a newly visible unit is an
  enemy for an always-hostile member but not for whichever member happened to have the
  greatest visibility range.
- Promotion assignment can change this behavior without changing the Privateer's own
  visibility or hostility: adding Sentry (+1 visibility) to the ordinary escort can make
  that escort become the sole hostility representative for the entire stack.

Repair direction:
- Preserve the max-range geometry optimization, but determine whether a visible
  potential enemy is hostile to ANY relevant group member rather than borrowing the
  max-range unit's isAlwaysHostile semantics for everybody.
- A cheap implementation can separate LOS/team-invisibility filtering from hostility:
  only members whose own visibility range reaches the candidate plot need be considered
  for member-specific PUF/isVisibleEnemyUnit hostility, or otherwise derive the union
  of actual group combat-hostility semantics carefully.
- Keep advc.004l knownEnemies tracking group-wide after that corrected predicate.

CONTROLS / NON-FINDINGS FROM THE SAME PASS
------------------------------------------
- sentryAlert's single maximum-range member is sufficient for ordinary LOS geometry:
  all group members share plot and team; canSeePlot receives only that plot/team/range.
- Invisible-unit visibility in CvPlot::isVisibleEnemyUnit ultimately uses the viewer
  team, so a dedicated see-invisible member contributes to team visibility; the defect
  above is not that the max-range member personally lacks the detection promotion.
- The doTurn Sentry-Heal wake test's pHeadUnit->canSentryHeal(plot()) initially looks
  like another head-only actor bug, but current canSentryHeal is purely
  !GET_TEAM(getTeam()).isRevealedCityHeal(plot). Same team + same plot makes its result
  identical for every group member.
- groupAttack's bBlitz / !bBlitz logic is intentional inherited K-Mod behavior. AdvCiv
  merely rewrote K-Mod's `bNoBlitz = !isBlitz() || !isMadeAttack()` as the algebraically
  equivalent `bBlitz = isBlitz() && isMadeAttack(); ... !bBlitz` when advc.164 expanded
  Blitz counts. No new F-root was established there.

WIP06 CURRENT CURSOR
--------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP06
CURRENT FILE-015 NEW ROOTS: F143-F147 / provisional KI#466-KI#470
NEXT ROOT ID: F148 UNUSED

Continue attack/movement and top-of-file timer/sentry lifecycle closure, then mission queue/
path/build/serialization and remaining inherited surface. No repository source files were modified.

LOCAL RECONCILIATION AFTER C015-WIP06 IMPORT

- ChatGPT-5.6-Sol's C015-WIP04 through WIP06 append remains verbatim as evidence. It is compatible with current authority at HEAD 619d87112 plus the reviewed KI#434/KI#435 worktree repairs, despite using the older practical-6267 / HEAD fe0e4abbe source baseline.
- F111-F112 / KI#434-KI#435 are fixed in the reviewed worktree, compiled and exercised by a full Huge multi-team autoplay through turn 500. The new `CvSelectionGroup.cpp` findings do not supersede those repairs; the prior WIP03 reconciliation and F113/provisional KI#436 retraction remain authoritative.
- F145-F147 / provisional KI#468-KI#470 are collision-free against the current reviewed Known Issues ledger. They remain provisional while file 015 is open at C015-WIP06; no implementation is promoted from this imported append. (GPT-5.6-Sol)

LOCAL RECONCILIATION AFTER KI#432 REVIEW

- F109 / KI#432 has now been independently revalidated against current source and AdvCiv 1.14. The reviewed worktree repair makes the asset-loss helper's evaluated player explicit, uses each Kingmaking loop participant's own assets and losses, normalizes culture-flipped tiles by that victim's team and gives both existing and predicted vassals half weight. The DLL compiled successfully, and a Huge mixed-team autoplay with two live vassal relationships completed with a Space Race victory on turn 329.
- F110 / provisional KI#433 remains live and intentionally separate: converting Kingmaking's player winner sets and genuinely team-owned Score/Time and Domination quantities requires a larger focused model refactor. C015-WIP06 and its provisional F145-F147 / KI#468-KI#470 queue remain otherwise unchanged. (GPT-5.6-Sol)

LOCAL RECONCILIATION AFTER KI#433 REVIEW

- F110 / KI#433 has now been independently revalidated against current source and AdvCiv 1.14. The reviewed worktree repair stores each winning team once, aggregates adjusted Score/Time contenders and predicted Domination population at team scope, adds team-shared predicted-vassal score once, and evaluates the coalition-wide rival state once through its team leader. Player-local Culture, Space, Conquest and Diplomacy inputs remain player-scoped before inserting their shared winning team. The DLL compiled successfully, and a full Huge UWAI autoplay with five two-player starting teams plus solo teams completed normally when Frederick's solo team won a Space Race victory on turn 427.
- F109-F110 / KI#432-KI#433 are therefore both resolved in the reviewed worktree as separate commits. C015-WIP06 and provisional F145-F147 / KI#468-KI#470 remain otherwise unchanged. (GPT-5.6-Sol)

===============================================================================
C015-WIP07 CONTINUATION -- CURRENT PRACTICAL 6270 RECONCILIATION + F148
===============================================================================

RECONCILIATION AGAINST CURRENT SAS HEAD 94e2d279e8 / PRACTICAL 6270
-------------------------------------------------------------------
- This continuation switches the active source base to the fresh
  AdvCiv-SAS_light_source_20260826T082633 snapshot supplied after the user and
  Codex reconciled C015-WIP06 into the repository album.
- Current repository identity is main / HEAD
  94e2d279e8e473896374d06a9a55cbd57fb9b89b / practical commit count 6270.
- The reviewed Known Issues ledger now contains independently repaired
  KI#434-KI#435; those repairs do not alter CvSelectionGroup.cpp.
- The imported album keeps F143-F147 / provisional KI#466-KI#470 as provisional
  file-015 findings. Current CvSelectionGroup.cpp still contains the same relevant
  mission/activity code as the WIP06 source surface.
- The only tracked working-tree source/config change in this light snapshot is
  diagnostic logging configuration in GlobalDefines_advciv_sas.xml; it does not
  affect the findings below. Current source remains authoritative. (GPT-5.6-Sol)


F148 / provisional KI#471 -- CONFIRMED -- INHERITED ADVCIV GROUP-ACTOR DEFECT
AIR-GROUP REBASE CLEARS LIVE RECON VISIBILITY ONLY FOR THE HEAD AIRCRAFT
-------------------------------------------------------------------------------
Status:
- NEW CURRENT INHERITED DEFECT ROOT.
- Static/lifecycle evidence strong; current-data reproduction is supported.
- Introduced by the AdvCiv advc.029 rebase/recon cleanup itself, not by SAS.

Intended behavior / history:
- AdvCiv practical 1282 / commit 20703b98c917750d406f388e169309be3aa251b9
  introduced the CvSelectionGroup::startMission hook.
- Its v0.91 commit message explicitly says:
    "Tiles made visible by recon are fogged when the recon unit is rebased."
- The source comment likewise distinguishes an air attack from a rebase and says
  that the recon plot should be cleared for rebase.

Current implementation:
- When an air selection group's MISSION_MOVE_TO destination is recognized as a
  revealed air base, startMission executes:
      getHeadUnit()->setReconPlot(NULL);
- No loop clears the other air units in the group.

Why the head-only assumption is invalid:
1. Multiple same-owner air units can legally share one CvSelectionGroup.
   CvUnit::canJoinGroup requires compatible owner/plot/domain but does not impose
   a one-aircraft-per-group restriction.

2. Recon state is per UNIT, not per selection group.
   CvUnit::recon calls:
      setReconPlot(pPlot);
   and each CvUnit stores its own recon coordinates / m_iLastReconTurn.

3. A live recon plot has gameplay state, not merely presentation state.
   CvUnit::setReconPlot removes/adds:
      changeReconCount(...)
      changeAdjacentSight(getTeam(), RECON_VISIBILITY_RANGE, ..., this, true)
   Thus a stale non-head recon plot continues to reveal map tiles.

4. Recon deliberately survives into the next player turn.
   CvUnit::doTurnPost clears only when:
      GC.getGame().getGameTurn() > m_iLastReconTurn
   CvPlayer::doTurn calls unit doTurnPost at the END of that player's turn.
   Therefore a recon performed on turn T remains live throughout turn T+1 until
   that next turn ends unless an earlier action such as rebase explicitly clears it.

5. A grouped rebase moves every eligible air unit, not only the head.
   CvSelectionGroup::groupPathTo treats DOMAIN_AIR as a direct destination move,
   then groupMove loops the group's movable members and calls each unit's move().
   CvUnit::move does NOT call setReconPlot(NULL).

Concrete supported reproduction:
- On turn T, aircraft A and aircraft B perform separate Recon missions (they may
  target different plots). Both now have independent live recon plots.
- Those recon effects survive through the beginning of turn T+1 by design.
- On turn T+1, place/join A and B into the same air selection group. Same-owner,
  same-plot, same-domain grouping is legal.
- Give the group a MOVE_TO order to another revealed valid air base while both
  aircraft can rebase.
- startMission clears the recon plot of whichever aircraft happens to be the
  group head.
- groupPathTo/groupMove then rebases both aircraft.
- The non-head aircraft's old recon plot is never cleared by the rebase path.
  Its old remote tiles therefore remain visible until end-of-turn doTurnPost,
  despite that aircraft having already relocated to another base.

Why ordinary end-of-turn cleanup does not rescue the contract:
- The advc.029 rebase hook exists precisely to remove recon visibility WHEN the
  recon aircraft rebases, rather than letting the effect linger until the normal
  lifetime expires.
- Delayed cleanup at the end of T+1 therefore still violates the feature's stated
  behavior and can leave stale remote military/map visibility during the rest of
  the player's active turn after the aircraft has moved elsewhere.

Ancestry:
- The head-only cleanup was introduced with the advc.029 fix itself in practical
  1282; the original patch already used getHeadUnit()->setReconPlot(NULL).
- Later AdvCiv refactors changed the air-base predicate but retained the same
  head-only actor.
- Current SAS inherits that structure unchanged in this path.

Relation to existing findings:
- This is distinct from KI#398 / the Espionage actor repair: that issue selected
  the wrong executor for a one-spy mission; F148 is per-unit persistent map
  visibility that must be cleared for every aircraft participating in a group rebase.
- It is also distinct from F146's head-only sleeping-Spy wake convenience: both
  expose a head-only assumption, but one repairs group wake semantics and the
  other repairs per-unit recon visibility ownership.
- No collision with the current reviewed Known Issues ledger was found.

Repair direction:
- When an air MISSION_MOVE_TO is identified as a rebase, clear recon state for
  every group member that will actually participate in that rebase, not merely
  getHeadUnit().
- A conservative implementation can iterate current group members and apply
  setReconPlot(NULL) to each air unit that is movable / valid for the same
  destination. Clearing all group's air recon plots at accepted rebase start is
  also semantically close to the existing head-only behavior, but participation
  matching is preferable if immobile/left-behind aircraft can coexist.
- Preserve the distinction between rebase and air attack; ordinary MOVE_TO air
  attacks should not prematurely clear recon through this hook.


WIP07 CURRENT CURSOR
--------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP07
CURRENT FILE-015 NEW ROOTS: F143-F148 / provisional KI#466-KI#471
NEXT ROOT ID: F149 UNUSED

Continue mission timer/queue/path/build and the remaining inherited
CvSelectionGroup surface, then adversarially re-challenge the file-015 ledger.
No repository source files were modified by this audit.


===============================================================================
C015-WIP08 APPEND -- F148 participation broadening + head/mutation controls
===============================================================================

F148 / provisional KI#471 -- PARTICIPATION BROADENING
------------------------------------------------------
The head-only rebase cleanup has a second deterministic manifestation that
strengthens the repair requirement.

CvSelectionGroup::canDoMission(MISSION_MOVE_TO) deliberately treats DOMAIN_AIR
differently from ordinary groups:

    if (pUnit->getDomainType() != DOMAIN_AIR)
        bValid = true; // air units don't have to move as a group
    if (pUnit->canMove() != bValid)
        return !bValid;

For an air group, bValid remains false; encountering any movable aircraft can
therefore make the mission available even if another aircraft in the same group
(including the head) has no moves. groupPathTo/groupMove later moves only the
members that can actually participate.

Consequently the current accepted-rebase hook:

    getHeadUnit()->setReconPlot(NULL);

is wrong in both directions when the head is not a participant:

- an immobile head aircraft can have its still-live recon plot cleared even
  though it does NOT rebase and remains at the old air base;
- a movable non-head aircraft that DOES rebase retains its own old recon plot
  and stale remote sight until normal end-of-turn cleanup.

This makes the preferred F148 repair more precise: clear recon state for the
aircraft actually participating in the rebase (or otherwise reconcile cleanup
with the same participation predicate used by group movement), rather than
blindly clearing either only the head or every aircraft regardless of movement.


HEAD-UNIT / GROUP-REPRESENTATIVE INVENTORY -- NO ADDITIONAL ROOT
-----------------------------------------------------------------
A full current CvSelectionGroup.cpp getHeadUnit() inventory was re-challenged
after F146/F148.

Clean / intentional representative uses:
- doTurn Worker logging: diagnostic only.
- showMoves: head-unit hostility can be presentation-ambiguous for mixed
  always-hostile groups, but this controls move display/timing only; no hard
  gameplay-hostility contract was established, so no F149.
- playActionSound: one representative action sound for the selection group.
- groupCycleDistance: explicitly compares group heads for UI cycling/order.
- getX/getY/plot/area/getDomainType/getHeadOwner/getHeadTeam: grouping contracts
  make these properties shared by all legal group members.
- setTransportUnit: the earlier special-cargo/head-capacity concern remains
  unreachable in shipped callers/data; current AI_load family uses ordinary
  land cargo transports.
- isAmphibPlot: head is used only to establish the sea-domain group contract;
  same-domain grouping makes that safe.
- split/merge/regroup bookkeeping: head identity is intentionally the group AI
  representative and was already checked for ordering/lifetime safety.

The K-Mod Spy wake (F146) and advc.029 recon cleanup (F148) remain the only
current head-only shortcuts in this file for which the queried state is
demonstrably UNIT-local while the code assumes the head represents all members.


MUTATING-EXECUTOR LIFETIME CONTROL -- GOLDEN AGE
-------------------------------------------------
The startMission individual-unit loop was re-audited for actions that can delete
OTHER members rather than merely the current unit.

Golden Age is the strongest apparent case:
- CvUnit::goldenAge calls CvPlayer::killGoldenAgeUnits(this).
- killGoldenAgeUnits can select another Great Person, including another member
  of the same selection group.
- FOR_EACH_UNIT_VAR_IN would be unsafe if that action synchronously removed a
  cached future linked-list node.

However, killGoldenAgeUnits uses pBestUnit->kill(true), and CvUnit::kill with
bDelay=true only calls startDelayedDeath() after finishMoves and RETURNS.
It does not remove the unit from its group/list at that point. The initiating
unit likewise uses kill(true). Actual list removal occurs later through
doDelayedDeath()/kill(false), where VAR traversal safely removes the current
node.

Therefore the outer startMission traversal does not retain a pointer to a freed
future node in this path. No new iterator-lifetime root was established.


C015-WIP08 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP08
CURRENT FILE-015 NEW ROOTS: F143-F148 / provisional KI#466-KI#471
NEXT ROOT ID: F149 UNUSED

Continue mission timer/queue/build and the middle group-predicate/path surface,
then serialization and full file-015 adversarial closure. No repository source
files were modified by this audit.


===============================================================================
C015-WIP09 APPEND -- F149 amphibious attempted-vs-successful cargo contract
===============================================================================

F149 / provisional KI#472 -- CONFIRMED -- CURRENT SAS AMPHIBIOUS GROUP DEFECT
MOVABLE BUT ILLEGAL CARGO CAN CONSUME A DEFERRED LANDING WITHOUT ANY UNIT LANDING
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT in inherited K-Mod/BtS group-amphib execution.
- Current SAS data supplies a deterministic supported reproduction.
- The structural executor behavior is inherited; SAS's deliberate land-unit
  feature-impassability makes the false-positive "landing attempted" state easy
  to reach in current gameplay.

Relevant K-Mod intent:
- K-Mod practical 461 / 7200489b5d added special handling so amphibious landings
  can execute even if the TRANSPORT group itself has no moves. The commit's
  example is a transport already on the coast: ordering it onto land should
  unload cargo despite the ship having no movement left.
- Practical 462 / a83a53016a moved that handling into startMission because the
  first implementation did not work.
- The resulting behavior intentionally preserves the transport MOVE_TO order
  when no cargo group is currently ready to land: groupAmphibMove returns false,
  so the transport mission is not consumed and can be retried on a later turn.
- K-Mod practical 428 / 85a83db596 later deduplicated cargo groups, but retained
  the same success accounting described below.

Current executor:
- groupAmphibMove gathers distinct land cargo groups.
- For every cargo group with canAllMove()==true it executes:
      kCargoGroup.pushMission(MISSION_MOVE_TO, target, ...);
      bLanding = true;
- It does NOT establish that the cargo group can legally enter/attack the target,
  nor does pushMission return whether the pushed mission actually moved/landed.
- Its return contract is documented as:
      "Returns true if attempted an amphib landing..."
- startMission interprets a true return by setting bDelete=true for the transport
  group's MOVE_TO mission. continueMission/groupPathTo likewise treat the true
  result as reason to stop the enclosing transport movement.

Why canAllMove is insufficient:
- It tests movement-point readiness only.
- canStartMission(MISSION_MOVE_TO, ..., bCheckMoves=false) deliberately accepts a
  different destination without checking path/terrain legality.
- readyForMission/canDoMission for MOVE_TO likewise checks movement-point
  uniformity, not destination entry legality.
- The pushed cargo mission therefore can start synchronously and only later have
  groupPathTo/pathfinding reject the destination.

Concrete current SAS reproduction:
1. Put at least two LAND cargo groups aboard one adjacent sea-transport group.
2. Cargo group A is a current Chariot (or Catapult/Trebuchet) with movement
   points remaining.
3. Cargo group B is an ordinary infantry-type land unit that is legally able to
   enter the target coastal Forest/Jungle, but has already spent its moves.
4. Target a coastal Forest/Jungle outside our own cultural borders.
5. Current SAS UNIT_CHARIOT (also Egyptian War Chariot, Catapult and Trebuchet)
   has FEATURE_FOREST and FEATURE_JUNGLE marked impassable with no passable tech;
   CvUnit::canMoveInto rejects those features outside own-team borders.
6. The transport destination/path is nevertheless a legitimate amphibious
   landing target because cargo B can legally enter it. This is exactly the kind
   of heterogeneous cargo group for which the group path/landing machinery must
   distinguish individual cargo groups.
7. groupAmphibMove skips B because B.canAllMove()==false.
8. A.canAllMove()==true, so it pushes MOVE_TO on A and immediately sets
   bLanding=true.
9. A's pushed mission starts immediately for a manual/non-appended command, but
   its own groupPathTo cannot generate a legal path into the impassable feature;
   its cargo mission is therefore canceled without landing.
10. groupAmphibMove has already returned true. The enclosing transport mission
    is consequently consumed/deleted even though NO cargo unit landed.
11. On the following turn cargo B would again have movement and could legally
    land, but the transport's deferred landing order has been lost.

Partition/control:
- If cargo A is absent, the same state with only immobile-but-legal cargo B makes
  groupAmphibMove return false. The transport landing mission is retained and can
  succeed when B regains moves.
- Adding the independently ILLEGAL but currently movable cargo A therefore
  changes "wait until a legal cargo group can land" into "consume the landing
  order now although nobody lands." That is not a tuning choice; it is a
  false-success signal in the executor contract.

Current-data proof:
- Base AdvCiv 1.14 has no land UnitInfo FeatureImpassable entries, while current
  SAS deliberately defines Forest/Jungle impassability for Chariot, Egyptian War
  Chariot, Catapult and Trebuchet. Thus the concrete feature reproduction is a
  current SAS-supported state even though the groupAmphibMove machinery itself
  is inherited.
- The structural issue is more general than those XML additions: any movable
  cargo group whose destination mission later fails can set the false landing
  result before execution success is known.

Relation to earlier findings:
- Distinct from F145/KI#468. F145 is shared transport LOAD capacity being tested
  independently before sequential loading. F149 concerns the later UNLOAD /
  amphibious side, where a pushed-but-illegal cargo mission is counted as a
  successful reason to consume the enclosing transport order.
- Distinct from the earlier cargo-group deduplication negative control: K-Mod's
  deduplication prevents the same group from being pushed multiple times, but
  does not validate whether a unique group can actually execute the landing.

Repair direction:
- Do not set/return the group-amphib "landing happened / consume enclosing order"
  signal merely because a cargo MOVE_TO mission was pushed.
- Prevalidate that a candidate cargo group can legally enter/attack the landing
  tile under the same movement/attack assumptions that its mission will use, or
  otherwise obtain an execution-success result from the cargo action.
- Preserve K-Mod's important deferred-order behavior: if no CURRENTLY READY and
  LEGALLY LANDING cargo group exists, leave the transport MOVE_TO queued so a
  temporarily immobile legal group can land on a later turn.
- Be careful with mixed attack/nonattack cargo; the repair should use the actual
  amphibious move/attack legality rather than simply checking movement points.


C015-WIP09 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP09
CURRENT FILE-015 NEW ROOTS: F143-F149 / provisional KI#466-KI#472
NEXT ROOT ID: F150 UNUSED

Continue path/cache reentrancy, mission/build queue ownership, timers,
serialization, and remaining inherited helpers; then adversarially re-challenge
the file-015 ledger. No repository source files were modified by this audit.


===============================================================================
C015-WIP10 APPEND -- F150 Air Patrol moveless-participant regression
===============================================================================

F150 / provisional KI#473 -- CONFIRMED -- INHERITED K-MOD REGRESSION
IMMOBILE UNSUITABLE AIRCRAFT IS NOT SPLIT FROM A NEW AIR-PATROL GROUP
-------------------------------------------------------------------------------
Status:
- NEW CURRENT INHERITED DEFECT ROOT.
- Introduced by K-Mod practical 1205 / 7027bf8ed2 during startMission
  reorganization; current SAS and Base AdvCiv inherit it.
- Current Fighter/Bomber data provides a deterministic human reproduction.

Mission contract:
- CvSelectionGroup::canDoMission(MISSION_AIRPATROL) returns true when a member
  canAirPatrol and (when moves are checked) that eligible member can move.
- The source comment explicitly says:
      "this mission will automatically ungroup any unsuitable units."
- startMission implements that concept through apUnitsLeftBehind and later splits
  those units into a separate group.

Historical control:
- K-Mod practical 732 / 58beb5a942 (2012-05-09), which rewrote mission readiness,
  explicitly had:
      case MISSION_PARADROP:
      case MISSION_AIRPATROL:
          bAbandonMoveless = true;
  Thus a unit with no moves was put into units_left_behind for BOTH missions.
- The same implementation separately checked movable Air Patrol members with
  canAirDefend and also put unsuitable ones into units_left_behind.
- K-Mod practical 1205 / 7027bf8ed2 (2015-09-26), commit message
  "Reorganised CvSelectionGroup::startMission for consistency", retained the
  per-movable-unit Air Patrol canAirDefend split but changed the moveless
  abandonment switch to:
      case MISSION_PARADROP:
          bAbandonMoveless = true;
  Air Patrol silently disappeared from that first-stage split.
- The commit itself says the reorganization was for consistency and that the
  author suspected it fixed an older bug but no longer remembered which one;
  there is no stated design decision to retain immobile non-interceptors in an
  Intercept group.
- AdvCiv's later advc.004c restructuring deleted the generic
  bAbandonMoveless helper and explicitly replicated moveless handling in its
  Paradrop block, while retaining the inherited Air Patrol per-movable check.
  It therefore preserves rather than creates the K-Mod 1205 regression.

Current executor:
1. A valid Air Patrol command sets the WHOLE selection group to
   ACTIVITY_INTERCEPT.
2. The individual-unit loop begins with:
       if (!pUnit->canMove())
           continue;
3. Only after that skip does MISSION_AIRPATROL run:
       if (!pUnit->canAirDefend(plot()))
           apUnitsLeftBehind.push_back(pUnit);
4. Therefore an unsuitable aircraft with no moves is never considered for the
   split.
5. apUnitsLeftBehind is later detached, but the skipped immobile aircraft stays
   in the original ACTIVITY_INTERCEPT group.

Concrete current-data reproduction:
- Current UNIT_FIGHTER:
    Domain=DOMAIN_AIR
    iInterceptionProbability=100
- Current UNIT_BOMBER:
    Domain=DOMAIN_AIR
    iInterceptionProbability=0
- Same-owner air units on the same plot may legally share one selection group.
- Let the Bomber perform an action first so it cannot move; keep a Fighter in
  the same group with movement remaining.
- Air Patrol remains available because the Fighter canAirPatrol and can move.
- Issue Air Patrol.
- startMission sets ACTIVITY_INTERCEPT for the whole group.
- The Fighter is a valid defender.
- The Bomber hits the upfront !canMove() continue and never reaches the
  !canAirDefend split.
- No later turn-start code repairs the composition. On the next turn the Bomber
  regains movement points, but ACTIVITY_INTERCEPT is a waiting activity and the
  group remains on patrol; the Bomber has maxInterceptionProbability()==0 and
  can never be selected as an interceptor.
- The Bomber therefore remains hidden/parked inside the intercept group until
  the player manually wakes or separates the group.

Why ordinary intercept logic does not rescue this:
- CvUnit::bestInterceptor requires canAirDefend and therefore simply ignores the
  Bomber; it does not split or wake it.
- CvSelectionGroup::doTurn has no "invalid Air Patrol member" reconciliation
  equivalent to Sea Patrol's per-unit invalidity wake.
- setActivityType(ACTIVITY_INTERCEPT) is group-wide and remains unchanged on
  later turns.

Relation to other file-015 roots:
- Distinct from F143/KI#466. F143 Sea Patrol accepts a mixed group and then an
  ineligible companion wakes/cancels the valid patrol on the next unit turn.
  F150 Air Patrol was explicitly designed to split unsuitable members, but one
  class of unsuitable member (no moves at command time) bypasses the split and
  becomes trapped in the valid patrol group.
- Distinct from F146/KI#469, which is a head-only Spy convenience wake.
- No current reviewed Known-Issues collision found.

Repair direction:
- Restore the original K-Mod participation contract: for MISSION_AIRPATROL,
  put nonparticipating/moveless members into apUnitsLeftBehind as well as movable
  members that fail canAirDefend.
- Prefer one explicit Air Patrol participation loop that classifies ALL group
  members before/while setting Intercept, so future outer canMove shortcuts
  cannot bypass the split.
- Preserve the valid K-Mod behavior that one movable eligible Fighter may enter
  patrol even when other selected aircraft cannot act; those others should be
  left behind rather than block the command.


POST-F149 MISSION / PATH / LIFETIME CONTROLS
---------------------------------------------
- Mission timer / mission queue ownership: no stale mission-node dereference
  established. deleteMissionQueueNode deactivates the head before deletion;
  K-Mod deliberately leaves next activation to callers. continueMission_bulk
  refreshes pHeadMission after groupPathTo and before later queue use.
- Build-completion cross-group mission deletion is plot-unit-list safe and
  getMissionType/getMissionData1 are sentinel-safe on an empty queue.
- CvSelectionGroup::doTurn caches pHeadUnit before calling each CvUnit::doTurn,
  but CvUnit::doTurn only uses delayed death for turn-damage death and does not
  synchronously unlink/regroup/upgrade the head. Actual removal occurs later in
  doDelayedDeath; no dangling cached-head root established.
- Shared GroupPathFinder composition caching remains a robustness candidate:
  setGroup does not notice group membership changes and canReuseInitialPathData
  intentionally admits that its movement-state key is incomplete; add/remove/
  join do not invalidate the finder. However, a shipped same-group reuse sequence
  producing a stale returned path has NOT yet been proven, so F151 is not spent
  on that concern.
- Serialization read/write remains paired for current uiFlag=3. Removed pre-v3
  migration branches are old-save compatibility, which this project does not
  promise; no current-format serialization defect established.


C015-WIP10 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP10
CURRENT FILE-015 NEW ROOTS: F143-F150 / provisional KI#466-KI#473
NEXT ROOT ID: F151 UNUSED

Continue remaining build/route/group predicates, path-cache reachability,
commands/UI cache, timers/serialization, and inherited-tail surface; then
adversarially re-challenge the file-015 ledger. No repository source files were
modified by this audit.


===============================================================================
C015-WIP11 APPEND -- F151 upgrade action-cache representative defect
===============================================================================

F151 / provisional KI#474 -- CONFIRMED -- INHERITED BTS ACTION-CACHE DEFECT
UPGRADE CACHE COLLAPSES SAME-TYPE UNITS DESPITE PER-UNIT UPGRADE PRICE/ELIGIBILITY
-------------------------------------------------------------------------------
Status:
- NEW CURRENT INHERITED DEFECT ROOT.
- Present already in BtS/Civ4CE; Base AdvCiv and current SAS inherit the same
  one-representative-per-UnitType action-cache design.
- Current SAS PROMOTION_LEADER gives a deterministic same-type eligibility
  difference through its 100% upgrade discount.

Cache contract:
- CvGame::setupActionCache is DllExport and forwards to the currently selected
  CvSelectionGroup. CvGame::canHandleAction is likewise DllExport and accepts the
  bUseCache fast-path flag used by the executable/UI action-update path.
- CvSelectionGroup::setupActionCache scans units that are isReadyForUpgrade(),
  but stores only the FIRST unit encountered for each UnitTypes value:
      if (eUnitType == m_aDifferentUnitCache[i]->getUnitType())
          bFound = true;
      ...
      if (!bFound)
          m_aDifferentUnitCache.push_back(pUnit);
- CvSelectionGroup::canEverDoCommand(COMMAND_UPGRADE,...,bUseCache=true) then
  tests only those cached representatives.
- If none of the representatives can execute the requested target upgrade, it
  returns false immediately. CvSelectionGroup::canDoCommand therefore never
  reaches its later full FOR_EACH_UNIT_IN scan.

Why UnitType is not a sufficient cache key:
- CvUnit::canUpgrade first requires isReadyForUpgrade, but then, for a real
  (bTestVisible=false) command, also compares the OWNER'S gold against this
  individual unit's upgradePrice(eUnit).
- upgradePrice subtracts:
      (iPrice * getUpgradeDiscount()) / 100
  where getUpgradeDiscount() is per-unit accumulated state from promotions.
- Two units with identical UnitType, owner and plot can therefore have different
  real upgrade affordability.

Concrete current-data reproduction:
1. Select/group two human units of the same upgradeable UnitType on the same
   owned-team plot, both with movement available.
2. Unit A is an ordinary copy.
3. Unit B is otherwise the same type but carries PROMOTION_LEADER from a Great
   General attachment.
4. Current CIV4PromotionInfos.xml defines:
      PROMOTION_LEADER
      iUpgradeDiscount = 100
5. Therefore, for the same target upgrade:
      B.upgradePrice(target) = 0
   while A retains the ordinary positive upgrade cost (absent another discount).
6. Choose a treasury below A's ordinary price; zero gold itself is sufficient.
   A cannot perform the real upgrade because canUpgrade fails the affordability
   check, while B can.
7. If setupActionCache encounters A first for this UnitType, it stores A and
   suppresses B as a duplicate type.
8. Cached canEverDoCommand(COMMAND_UPGRADE,target,...,true) tests A, gets false,
   exhausts the one-element type representative set and returns false.
9. canDoCommand returns false before its ordinary full-group scan can reach B.
   The action fast path can therefore disable/hide a valid free upgrade present
   in the selected group.
10. If group ordering instead makes B the representative, the same selected set
    returns true. Cache correctness consequently depends on which same-type unit
    happens to be retained.

Independent current-data control:
- Reviewed SAS KI#220 already documents and fixes another UI/statistics defect
  whose reproduction is precisely that a combat unit carrying PROMOTION_LEADER
  can validly upgrade for ZERO gold. That independently confirms the free-upgrade
  state is current and intentional rather than hypothetical XML.
- isReadyForUpgrade does not erase the distinction; it checks movement and
  own-team plot only, which A and B share.

Ancestry:
- Civ4CE BtS already contains the same setupActionCache:
  one ready unit per UnitType.
- BtS canEverDoCommand(COMMAND_UPGRADE,bUseCache) likewise tests only those
  representatives and returns false when none can perform the target upgrade.
- Base AdvCiv 1.14 retains the same design; current SAS changes are formatting,
  constness and unrelated guards only.
- This is therefore inherited from Firaxis BtS rather than introduced by K-Mod,
  AdvCiv or SAS.

Relation to existing issues:
- Distinct from KI#220: that issue was Military Summary statistics dropping
  zero-cost upgrades from a Python sample; F151 is the engine/UI action-cache
  feasibility result for the selected group.
- Distinct from AI upgrade-routing/ROI issues (KI#188.x / KI#217): this fast path
  concerns action availability and collapses same-type HUMAN units before normal
  unit-by-unit command testing.
- No collision with the current reviewed Known-Issues ledger found.

Repair direction:
- Do not collapse real COMMAND_UPGRADE feasibility solely by UnitType.
- Simplest correctness-first repair: cache every isReadyForUpgrade unit and let
  canEverDoCommand test all cached ready candidates.
- If retaining compression for UI performance, the key must include every
  per-unit property that can change real upgrade legality/price; in practice that
  is brittle (upgrade discount now, and potentially other per-unit state later),
  so retaining all ready units is safer.
- Preserve the purpose of the action cache (avoid repeated busy/readiness work)
  without changing the result of the uncached full-group command test.


PATHFINDER COMPOSITION-CACHE STATUS -- STILL NOT PROMOTED
---------------------------------------------------------
The shared GroupPathFinder concern remains a real robustness smell but still
does not meet the current-root bar:
- setGroup/cache reuse does not key on group membership;
- add/remove/join do not reset the global pathfinder;
- GroupStepMetric itself warns that its reuse condition misses significant
  group changes.
However, current human movement uses the explicitly cleared alternate finder,
and no deterministic shipped AI sequence has yet been established where the same
group pointer changes composition and then reuses a stale main-finder destination
without an intervening reset/rebind. Keep this as a closure challenge rather than
spending F152 from API possibility alone.


C015-WIP11 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP11
CURRENT FILE-015 NEW ROOTS: F143-F151 / provisional KI#466-KI#474
NEXT ROOT ID: F152 UNUSED

Continue build/route/group predicates, action/interface surfaces, remaining
inherited tail and path-cache reachability; then adversarially re-challenge
F143-F151 before file closure. No repository source files were modified.


LOCAL RECONCILIATION AFTER C015-WIP11 IMPORT

- ChatGPT-5.6-Sol's C015-WIP07 through WIP11 append remains verbatim as evidence. Its practical-6270 source baseline predates the reviewed KI#432/KI#433 commits and the current KI#436-KI#439 worktree review, but those changes do not modify `CvSelectionGroup.cpp` or invalidate the appended file-015 analysis.
- F113 / KI#436 is now documented in Known Issues as the same rejected overloaded-`GET_TEAM(PlayerTypes)` false positive already controlled by KI#311 and KI#387. F114-F116 / KI#437-KI#439 are independently reviewed and repaired in the current worktree. The DLL compiled successfully, and a full Huge Quick mixed-team UWAI autoplay recorded several four- and five-turn wars ending in peace before completing normally with a Space Race victory on turn 268; their C014 evidence remains authoritative and separate from this imported file-015 continuation.
- F148-F151 / provisional KI#471-KI#474 are collision-free against the current reviewed Known Issues ledger. Together with F143-F147 / provisional KI#466-KI#470, they remain provisional while file 015 is open at C015-WIP11; no implementation is promoted from the imported append. (GPT-5.6-Sol)


===============================================================================
C015-WIP12 APPEND -- F152 same-group pathfinder membership invalidation defect
===============================================================================

F152 / provisional KI#475 -- CONFIRMED -- INHERITED ADVC/K-MOD PATH-CACHE DEFECT,
CURRENT SAS SAME-TURN SETTLER-ESCORT TRIGGER
GROUP MEMBERSHIP CAN CHANGE MOVEMENT LEGALITY WITHOUT INVALIDATING REUSABLE PATH DATA
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT.
- The reusable GroupPathFinder architecture and missing membership invalidation
  are already present in Base AdvCiv 1.14's K-Mod-derived pathfinder line.
- Current SAS supplies a deterministic same-turn trigger through its Settler
  city-escort attachment and a shipped equal-move restrictive escort (Chariot).
- Civ4CE/BtS uses the older executable FAStar pathfinder; this audit does not
  establish the same node-cache defect there, so ancestry is pinned no earlier
  than the AdvCiv/K-Mod-derived GroupPathFinder implementation.

Broken cache contract:
- CvSelectionGroup::generatePath uses the shared reusable GroupPathFinder and
  binds it through GroupPathFinder::setGroup(*this, flags, maxPath).
- setGroup resets when the CvSelectionGroup POINTER changes, or when relevant
  movement flags change. It does not detect a changed member list inside the
  same group object.
- CvSelectionGroup::addUnit and removeUnit mutate m_units without calling
  invalidateGroupPaths. CvUnit::joinGroup reaches exactly those operations.
- invalidateGroupPaths is currently used when the emptied group is killed, not
  whenever a surviving group's membership changes.
- GroupStepMetric::canReuseInitialPathData itself warns that its reuse key does
  not catch every significant change. In the current implementation it accepts
  reuse when recalculated initialMoves equals the cached start-node moves.
- Membership can change much more than that key: feature/terrain legality,
  all-terrain movement, movement costs, combat/danger inputs and group strength,
  while leaving the minimum current moves unchanged.

Why stale nodes alter later results:
- KmodPathFinder caches initialized nodes across generatePath calls with the same
  start. resetNodes is deliberately skipped when the cached start data is judged
  reusable.
- processChild calls GroupStepMetric::canStepThrough ONLY for an UNINITIALIZED
  child node. Already-initialized children are reused without rerunning current
  group movement legality.
- Conversely, a newly examined impossible intermediate tile is permanently set
  PATHNODE_CLOSED, with the source comment explicitly assuming that it can never
  become enterable on a later generatePath call (except as destination).
- That assumption is valid only while the movement-relevant group state remains
  equivalent. Changing members of the same group violates it.
- The destination fast path is even clearer: if the destination node was already
  initialized, generatePath immediately reuses it as m_pEndNode; an already-found
  cached path is not reconstructed under the new member set before success is
  reported.

Deterministic current-SAS same-turn reproduction:
1. Use an AI UNIT_SETTLER standing in one of its own cities, unescorted and with
   its full 2 moves. Give the player at least one currently considered city site
   reachable only by crossing a neutral/foreign Forest or Jungle intermediate
   tile.
2. Keep at least three healthy local defenders so the shipped default
   SAS_AI_SETTLER_ATTACH_CITY_ESCORT_MIN_HEALTHY_CITY_DEFENDERS_LEFT = 2 permits
   one defender to be reassigned. Make the best eligible local escort a healthy
   UNIT_CHARIOT with its full moves.
3. Current UNIT_SETTLER has iMoves=2 and no feature-impassability. During
   AI_settleMove, before AI_found is called, the city-site scan invokes
       generatePath(kSite, MOVE_NO_ENEMY_TERRITORY, true)
   on the Settler-only group. The neutral Forest/Jungle route is legal and its
   nodes/destination are cached in the shared finder.
4. AI_settleMove then calls AI_found with the SAME movement flags.
5. At the start of AI_found, current SAS calls SAS_tryAttachCityEscortToSettler
   while the Settler is unescorted in its own city. The helper chooses a healthy
   defender by base combat strength and joins it directly to pSettlerGroup.
6. Current UNIT_CHARIOT is a valid canDefend escort candidate, has iMoves=2, and
   is feature-impassable in Forest and Jungle outside its own team's territory.
   UNITAI_SETTLE's group-first value remains above UNITAI_ATTACK, so the Settler
   also remains group head after attachment.
7. Because both fully ready units have 2 moves, the group's minimum moves after
   attachment still equals the cached Settler-only start-node moves. Same group
   pointer + same start + same relevant flags + same initialMoves therefore pass
   the existing reuse checks despite the new movement restriction.
8. AI_found immediately pathfinds the same city-site set again. The cached route
   or destination reached while the group was Settler-only can be reported as
   reachable even though the newly attached Chariot makes the required neutral
   Forest/Jungle step illegal.

Concrete gameplay consequence:
- AI_found trusts the stale successful generatePath result when selecting
  pBestFoundPlot / pBestPlot and then returns true after pushing its
  MISSION_MOVE_TO for the cached end-turn plot.
- AI mission commands execute immediately. CvSelectionGroup::groupPathTo uses
  the separately cleared alternate finder, now with the actual Settler+Chariot
  composition. If the stale selected step/target requires the impassable feature
  and has no legal alternate route, this fresh execution path correctly fails.
- continueMission_bulk marks the MOVE_TO done on that no-path result and removes
  the mission. But AI_found has already returned true to AI_settleMove, whose
  caller returns for the turn. Thus the cache mismatch can deterministically
  produce a selected Settler target/move that immediately evaporates and wastes
  that AI update/turn instead of evaluating the post-escort group coherently.
- Even where a fresh alternate route exists, the main-finder result can still
  supply stale path turns/end-turn choice and therefore distort site comparison
  before the execution finder repairs the route. The root is incorrect reusable
  path state, not merely the final mission failure shape.

Reverse-direction risk:
- The same cache hole also supports restrictive -> permissive membership changes:
  an intermediate node closed under a departed restrictive member can remain
  PATHNODE_CLOSED after the remaining group becomes able to traverse it.
- A previous attempted Catapult/Swordsman AI_guardCity reproduction used this
  direction, but it is deliberately NOT used as the current proof: current SAS
  now forbids Catapults from UNITAI_COLLATERAL and their ATTACK_CITY ordering no
  longer guarantees the restrictive non-head unit is the one ejected.
- The shipped Settler+Chariot attachment above is simpler, current-tree exact and
  immediate; no inter-group scheduling assumption is required.

Ancestry/control comparison:
- Base AdvCiv 1.14 has the same GroupStepMetric reuse warning, same setGroup
  pointer/flag reset architecture, and no addUnit/removeUnit membership
  invalidation. The structural defect is therefore inherited by SAS.
- Current SAS's same-turn trigger is custom: AI_settleMove first pathfinds, then
  AI_found may attach a defender directly to that still-live Settler group and
  pathfind again.
- Civ4CE/BtS instead routes CvSelectionGroup::generatePath through the executable
  FAStar interface and an explicit bReuse parameter. Without visibility into an
  equivalent cached-node contract, do not backdate F152 to Firaxis.

Relation to other file-015 roots:
- Distinct from F149 / KI#472 amphibious execution: F149 consumes a transport
  landing mission after treating a pushed but individually illegal cargo move as
  a successful landing attempt.
- Distinct from F151 / KI#474 action cache: F151 collapses same-UnitType units
  despite per-unit upgrade price; F152 is reusable map-path state surviving a
  changed member set.
- The current Settler escort hook belongs to KI#179, but KI#179's intended
  behavior (attach a spare defender before exposed founding) is valid. It merely
  exposes the inherited path-cache contract because attachment happens between
  two reusable path searches.
- No current reviewed Known-Issues collision for provisional KI#475 found.

Repair direction:
- Invalidate shared pathfinder state whenever membership of a surviving
  CvSelectionGroup actually changes, rather than trying to enumerate every
  movement/combat property in GroupStepMetric::canReuseInitialPathData.
- addUnit/removeUnit (or the single lower ownership boundary reached by both)
  are the natural correctness boundary. Invalidation should occur only when the
  membership mutation really happens, and must cover both shared pathfinders if
  either can still be bound to the group.
- Review clearUnits/full teardown separately: kill already invalidates before the
  group object is deleted, but normal surviving-group add/remove is the missing
  case.
- A narrower Settler-only reset would hide this reproduction but leave all other
  regroup/split/merge callers vulnerable to the same stale-node assumption.


C015-WIP12 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP12
CURRENT FILE-015 NEW ROOTS: F143-F152 / provisional KI#466-KI#475
NEXT ROOT ID: F153 UNUSED

F152 is now promoted from the WIP11 closure challenge. Continue the remaining
build/route/group predicates, group mutation/split/merge edge cases, action and
interface surfaces, serialization/timers and inherited tail; then perform an
adversarial F143-F152 closure pass before declaring file 015 complete. No
repository source files were modified by this audit.


===============================================================================
C015-WIP13 APPEND -- F153 cross-transport cargo group wrongly boarded as a whole
===============================================================================

SOURCE RECONCILIATION AT THIS CONTINUATION
-----------------------------------------
- Current uploaded AdvCiv-SAS light source is practical 6272 / HEAD
  038724dba92df55c2b5268de4341f72ab2ac47c2. CvSelectionGroup.cpp remains
  4,722 lines and is unchanged by the unrelated reviewed worktree edits.
- Base AdvCiv 1.14 and Civ4CE are used locally for ancestry/control comparison.
- C015-WIP12 remains an exact byte prefix of this checkpoint. No repository
  source file is modified by the audit.


F153 / provisional KI#476 -- CONFIRMED -- INHERITED ADVC.075 CARGO-CYCLING DEFECT
SUPPORTED K-MOD CROSS-TRANSPORT CARGO GROUPS RECEIVE ONE TRANSPORT GROUP'S
ACTIVITY_BOARDED STATE AS A WHOLE
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT.
- Introduced by Base AdvCiv's advc.075 boarded/cargo-cycling convenience layer,
  interacting with a grouping state explicitly supported by K-Mod.
- Current SAS inherits the same helper logic essentially unchanged.
- Civ4CE/BtS has no ACTIVITY_BOARDED / resetBoarded / handleBoarded layer, so
  this audit does not backdate the defect to Firaxis.

Broken ownership/state contract:
- CvSelectionGroup::startMission calls resetBoarded() for a HUMAN SEA group
  before dispatching the mission switch. This is not limited to ship movement;
  MISSION_SKIP and other accepted ship orders pass through the same call.
- resetBoarded() calls getLandCargoGroups(), then changes every returned
  selection group's activity from ACTIVITY_AWAKE to ACTIVITY_BOARDED.
- getLandCargoGroups() correctly filters individual cargo units by carrier:
      pUnit->getTransportUnit()->getGroup() == this
  but then returns pUnit->getGroup(), i.e. the cargo unit's entire LAND
  CvSelectionGroup.
- That selection group is not required to consist solely of cargo carried by
  this SEA selection group.
- CvUnit::canJoinGroup only requires compatible owner, plot and domain (plus the
  force-separate guard). It has no same-transport or same-transport-group rule.
  Human GAMEMESSAGE_JOIN_GROUP reaches CvUnit::joinGroup directly, so land cargo
  aboard different carriers on the same plot can legitimately share one group.
- Activity is selection-group-wide. ACTIVITY_BOARDED is a waiting activity;
  isCycleGroup excludes waiting groups from normal automatic unit cycling.
  resetBoarded therefore applies ship A's "cargo should become inactive" state
  to unrelated cargo on ship B whenever those cargo units share a group.

Minimal deterministic human reproduction -- no ship movement required:
1. Put two separate human sea transport selection groups, ship A and ship B, on
   the same coastal water tile. Give each at least one land cargo unit.
2. Manually select/group cargo A and cargo B. Same owner, same plot and both
   DOMAIN_LAND satisfy canJoinGroup, so they form one AWAKE cargo group despite
   being carried by different transport groups.
3. Select only ship A and give it MISSION_SKIP (or any other legal order that
   reaches startMission). Ship B receives no order and need not move.
4. startMission on ship A calls resetBoarded before the mission switch.
5. getLandCargoGroups sees cargo A because cargo A's carrier belongs to ship A's
   group, but pushes cargo A's SHARED selection-group pointer.
6. resetBoarded sets that whole shared group to ACTIVITY_BOARDED. Cargo B is now
   BOARDED solely because ship A received an order, although cargo B's own
   carrier ship B remained untouched.
7. With normal unit cycling enabled, the shared cargo group is a waiting group
   and is excluded from ordinary cycling. If ship B stays put, there is no
   generic per-turn BOARDED wake that restores cargo B; advc.075 normally wakes
   boarded cargo through the carrier-side movement/arrival handling. The player
   can therefore silently lose the expected prompt for otherwise idle cargo on
   an unrelated stationary transport. Manual selection still remains possible.

Why this contradicts the feature's own intent:
- Base AdvCiv history for advc.075 says: "When a ship is given an order, units
  in cargo that are awaiting orders are treated as if they had just boarded the
  ship" and become inactive until that ship reaches another destination.
- The current implementation identifies an affected cargo MEMBER correctly by
  its actual carrier, but stores the resulting inactivity on that member's whole
  selection group. Cargo on a different ship is therefore swept into the state
  transition even though that ship was not given the order.

K-Mod cross-transport grouping is a supported state, not a fabricated API case:
- K-Mod's changelog explicitly says cargo units stay grouped when their
  transports move unless the transports carrying parts of the group are
  separated, and its amphibious AI groups cargo before landing.
- CvUnit::setXY contains corresponding live logic: after a transport moves its
  cargo, it detects cargo groups that include units not transported by this
  transport group and separates the left-behind members.
- That separation deliberately preserves the cargo group's old activity when it
  is not ACTIVITY_MISSION. Thus, if resetBoarded has already assigned the shared
  group ACTIVITY_BOARDED, cargo on another carrier can be split off and retain
  the wrongly inherited BOARDED state. This corroborates the ownership mismatch
  but is not required by the simpler MISSION_SKIP reproduction above.

Ancestry:
- K-Mod commit/changelog 58faf9a7 (2011-12-16) explicitly supports cargo staying
  grouped across transport movement until the relevant transports separate.
- Base AdvCiv introduced ACTIVITY_BOARDED on 2019-02-18 to wake cargo when a ship
  reaches land, then advc.075 on 2019-02-20 added reset-on-ship-order / ship-first
  cycling behavior. A 2019-03-16 refinement groups awoken cargo for convenience.
- Base AdvCiv 1.14 contains the same resetBoarded/getLandCargoGroups contract.
- Civ4CE has no ACTIVITY_BOARDED or corresponding helpers. F153 is therefore an
  AdvCiv-layer defect exposed by a legitimate K-Mod grouping invariant.

Relation to earlier cargo observations:
- During this audit getLandCargoGroups was already noted to append duplicate
  CvSelectionGroup pointers when multiple cargo members belong to the same group.
  That is at most redundant traversal here; it is NOT the F153 root.
- Deduplicating those pointers would still set the same mixed-carrier cargo
  group to ACTIVITY_BOARDED once, leaving cargo B wrongly affected.
- The root is scope: a per-carrier/per-transport-group event is represented by a
  state transition on a cargo selection group that may span other carriers.

Repair direction:
- Preserve advc.075's intended convenience, but apply "boarded because THIS sea
  group got an order" only to cargo actually carried by that sea group.
- When an affected cargo unit belongs to a selection group containing members
  carried by other transport groups, partition/split the relevant cargo before
  applying ACTIVITY_BOARDED, or otherwise represent the state at the correct
  carrier/member scope.
- Review handleBoarded symmetrically: it also consumes cargo-group pointers and
  can wake/merge a whole mixed-carrier group based on one sea group's arrival.
  A repair should avoid transferring wake/merge effects to unrelated carriers.
- Do not treat pointer deduplication as the fix; it may be a small cleanup but
  does not restore the ownership contract.


NEIGHBOR CONTROL -- INITIAL MIXED PLUNDER ADMISSION IS NOT A NEW ROOT
---------------------------------------------------------------------
A mixed group such as a Destroyer that can plunder plus a Transport that cannot
can pass group MISSION_PLUNDER because canDoMission is any-member, after which
startMission puts the whole selection group in ACTIVITY_PLUNDER while only the
eligible unit gets unit-local m_bBlockading state.

This looks superficially similar to F143/F144, but current evidence does NOT
justify a new F154 or a broadening of those roots:
- the ineligible escort does not later cancel the eligible unit's blockade;
- it has no stale unit-local blockading state to reconcile;
- keeping a manually grouped escort waiting with the blockading group is a
  plausible and internally consistent group-activity semantic.
F143 remains specifically the Sea Patrol member that later wakes/cancels the
valid patrol; F144 remains the destructive divergence after a formerly valid
blockader loses legality and group wake clears still-valid blockaders.


C015-WIP13 CURRENT CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP13
CURRENT FILE-015 NEW ROOTS: F143-F153 / provisional KI#466-KI#476
NEXT ROOT ID: F154 UNUSED

Resume with the remaining boarded/handleBoarded symmetry and inherited tail,
then run the adversarial current-snapshot verification matrix across F143-F153.
If those roots survive and no uncovered contract family remains, close file 015
without manufacturing F154. No repository source files were modified.


C015-WIP14 APPEND -- F154 BtS cross-transport cargo automation scope leak
===============================================================================

F154 / provisional KI#477 -- CONFIRMED -- INHERITED FIRAXIS BTS DEFECT
STOPPING AUTOMATION ON ONE TRANSPORT GROUP CANCELS AUTOMATION FOR CARGO CARRIED
BY ANOTHER TRANSPORT GROUP WHEN THEIR LAND CARGO SHARES A SELECTION GROUP
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT.
- Present already in supplied Civ4CE/BtS; absent from the supplied Civ4 and
  Warlords CvSelectionGroup::setAutomateType implementations.
- Base AdvCiv 1.14 and current SAS inherit the BtS cargo-cancellation block.
- Distinct from F153 / KI#476: F153 is AdvCiv's later ACTIVITY_BOARDED/cycling
  layer. F154 predates that feature and corrupts AutomateTypes state instead.

Broken ownership/state contract:
- CvSelectionGroup::setAutomateType(NO_AUTOMATE) contains the BtS convenience:
      // If canceling automation, cancel on cargo as well.
- It scans units on the transport group's plot and correctly identifies an
  affected cargo MEMBER by checking:
      pCargoUnit->getTransportUnit()->getGroup() == this
- It then applies both NO_AUTOMATE and ACTIVITY_AWAKE to:
      pCargoUnit->getGroup()
  i.e. the member's entire LAND selection group.
- CvUnit::canJoinGroup does not require cargo members to have the same carrier
  or even carriers in the same sea selection group. Same owner, plot and domain
  are sufficient (plus the force-separate guard).
- Human grouping reaches CvUnit::joinGroup through the normal
  GAMEMESSAGE_JOIN_GROUP path. Thus land cargo carried by separate transport
  groups on the same plot can legitimately share one selection group.
- The per-transport event "automation on sea group A was canceled" therefore
  mutates automation state belonging to cargo on sea group B as well.

Current-SAS deterministic human reproduction:
1. Put two separate human transport selection groups, ship A and ship B, on the
   same city/coastal plot. Load one land combat cargo unit on each.
2. Group cargo A and cargo B through normal human selection/grouping. Their
   different carriers do not prevent canJoinGroup.
3. Set the shared cargo group to AUTOMATE_EXPLORE. Current CvUnit::canAutomate
   explicitly permits DOMAIN_LAND combat units to Explore even while cargo; it
   has no isCargo exclusion. Loading has already finishMoves()'d the units, so
   assigning automation need not make them act immediately.
4. Automate ship A (e.g. Explore) so its sea group has a non-NO automate type.
   Ship B remains a separate transport group and need receive no matching order.
5. Issue COMMAND_STOP_AUTOMATION (or another path that genuinely transitions
   ship A from an automate type to NO_AUTOMATE) on ship A.
6. ship A's setAutomateType(NO_AUTOMATE) finds cargo A by its actual carrier,
   then calls setAutomateType(NO_AUTOMATE) on cargo A's SHARED land group.
7. Cargo B, whose carrier ship B was untouched, has its AUTOMATE_EXPLORE state
   silently canceled solely because it was grouped with cargo A.
8. This is persistent gameplay state, not presentation only: unloading wakes a
   cargo group through CvUnit::setTransportUnit(NULL) but does NOT clear or
   restore its automate type. Without F154, cargo B could retain its automation
   for later use after unloading; after ship A's cancellation it cannot.

Why ordinary human reachability is supported:
- CvGame::selectUnit / selectGroup and CvDLLWidgetData::doCreateGroup ultimately
  issue GAMEMESSAGE_JOIN_GROUP; CvNetJoinGroup::Execute calls CvUnit::joinGroup.
- canJoinGroup has no carrier-affinity test. The required mixed-carrier land
  group therefore does not rely on a private or debug-only API state.
- K-Mod additionally treats cross-transport cargo grouping as meaningful and
  contains explicit separation logic when transport groups carrying different
  members later move apart. That strengthens present-day reachability, although
  the underlying F154 cancellation block already exists in Firaxis BtS.

Ancestry/control comparison:
- Supplied Civ4CE/BtS CvSelectionGroup::setAutomateType (around lines 3643-3674)
  contains the same "if canceling automation, cancel on cargo as well" loop and
  the same pCargoUnit->getGroup() state mutation.
- Supplied Warlords and original Civ4 setAutomateType implementations merely set
  the automate type, clear the mission queue and wake the group; they have no
  cargo-propagation loop.
- Base AdvCiv 1.14 carries the BtS loop essentially unchanged. Current SAS does
  too. F154 therefore originates in Firaxis BtS, not K-Mod/AdvCiv/SAS.
- BtS movement tended to split cargo groups more aggressively when transports
  moved; K-Mod later preserves such grouping when possible. That can change how
  often the mixed-carrier state survives movement, but does not remove the
  stationary/manual-group reproduction or the original scope mismatch.

Relation to F153 / KI#476:
- Both defects expose the same underlying data-model fact: a land cargo
  CvSelectionGroup is not owned by one carrier/sea group.
- They remain separate roots because different code, state and ancestry are
  involved. F153 applies AdvCiv ACTIVITY_BOARDED wake/cycle semantics from one
  ship order/arrival; F154 applies Firaxis BtS automation cancellation from one
  sea group's automate transition.
- A common repair helper may be sensible, but fixing only resetBoarded /
  handleBoarded would leave F154 untouched, and fixing only setAutomateType would
  leave F153 untouched.

Repair direction:
- Propagate NO_AUTOMATE/AWAKE only to cargo actually carried by the sea selection
  group whose automation is being canceled.
- If such cargo shares a land selection group with members carried by other sea
  groups, partition/split at the carrier/transport-group boundary before making
  a group-wide state transition, or otherwise apply state at an appropriately
  scoped representation.
- Reuse the same carrier-partition invariant considered for F153 if practical;
  pointer deduplication alone is again insufficient.
- Preserve the intended BtS convenience that canceling a transport's automation
  also cancels automation of ITS cargo; the defect is only the spillover to
  another transport group's cargo.


C015-WIP14 INTERIM CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP14
CURRENT FILE-015 NEW ROOTS: F143-F154 / provisional KI#466-KI#477
NEXT ROOT ID: F155 UNUSED

Continue the remaining tail/caller sweep and the adversarial current-snapshot
verification matrix. Do not close file 015 until all F143-F154 roots have been
rechallenged and the remaining helper families have explicit dispositions. No
repository source files were modified.


===============================================================================
C015-WIP15 APPEND -- F155 live groupMove traversal invalidated by move side effects
===============================================================================

F155 / provisional KI#478 -- CONFIRMED -- INHERITED WARLORDS/BTS DEFECT;
EXPLICITLY FIXED BY K-MOD 1.46 BUT STILL PRESENT IN ADVCIV/SAS DIVERGENT GROUPMOVE
POTENTIAL USE-AFTER-FREE / CTD WHEN GROUPMOVE PREFETCHES A FUTURE LIVE LIST NODE
AND MOVING THE CURRENT UNIT INDIRECTLY REGROUPS THAT FUTURE UNIT
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT, crash-class rather than merely tactical/UI behavior.
- Current SAS and Base AdvCiv 1.14 traverse the live CvSelectionGroup unit list
  inside groupMove while CvUnit::move can run reentrant game-state code that
  changes membership of that same group.
- The concrete conquest/culture-bump mechanism is already present in supplied
  Warlords and BtS, so the reproduction is inherited Firaxis behavior at least
  from Warlords onward.
- K-Mod commit 1129c1e740 (2016-09-28), included in the K-Mod 1.46 history,
  explicitly fixed this exact class by making groupMove use a copied list of
  units rather than the live list. Current AdvCiv/SAS groupMove still uses live
  traversal because this function has a deliberately divergent advc.001 path.

Current-SAS vulnerable traversal:
- CvSelectionGroup::groupMove performs two no-city-capture stages and traverses
  the group through:
      FOR_EACH_UNIT_VAR_IN(pUnit, *this)
- LinkedListTraversal.h implements the VAR macro by obtaining the CURRENT unit
  and then prefetching the NEXT CLLNode before entering the loop body:
      pUnit = ::getUnit(node->m_data)
      node = CLinkList<IDInfo>::static_next(node)
      // body may now call pUnit->move(...)
- This safely permits deleting pUnit's CURRENT node in the body. It does NOT
  protect the already-prefetched NEXT node from being deleted indirectly.
- AdvCiv's own advc.003s manual documents exactly that limitation: after
  nextUnitNode has been called, the saved next node and the unit represented by
  that node must not be deleted. Otherwise the saved CLLNode pointer dangles.

Concrete current-SAS reentrant invalidation chain:
1. groupMove begins iterating selection group G and resolves current unit A.
2. The traversal macro prefetches the linked-list node for next unit B before
   executing A's loop body.
3. A can move into and capture an enemy city through pUnit->move(*pPlot, true).
4. CvUnit::move -> CvUnit::setXY processes the city conquest and calls
   CvPlayer::acquireCity(...).
5. The conquest branch of acquireCity calls:
      GC.getMap().verifyUnitValidPlot();
   after the culture/ownership transition.
6. CvMap::verifyUnitValidPlot visits plots. CvPlot::verifyUnitValidPlot can bump
   units that are no longer legal on their plots because borders/territory have
   just changed. Current K-Mod-derived code records the affected selection
   groups and then calls regroupSeparatedUnits() on them.
7. If still-unmoved future member B of G is one of the units displaced by that
   conquest/culture update, CvSelectionGroup::regroupSeparatedUnits removes B
   from G with CvUnit::joinGroup(...), thereby deleting B's old linked-list node.
8. But groupMove's traversal has already saved a pointer to exactly that node as
   its NEXT node. When the for-loop condition resumes, it dereferences the
   dangling CLLNode pointer to obtain node->m_data.
9. Result is undefined behavior: depending on allocator/memory reuse this can
   skip/corrupt traversal or produce a use-after-free crash-to-desktop.

Why this is not merely a theoretical mutation warning:
- K-Mod's retained commit history contains commit 1129c1e740 with subject:
      Fixed potential pointer invalidation in CvSelectionGroup::groupMove
  and explains the mechanism directly:
      The selection group can be messed up when units move. eg. If a city is
      captured, the change in culture could bump the remaining units.
  Its stated repair is to use a copied list of units rather than the live list.
- K-Mod 1.46 changelog separately records:
      Fixed potential crash in CvSelectionGroup::groupMove
- This independently identifies the same function, same state change and same
  failure class that current static tracing reaches.
- AdvCiv's own linked-list refactor documentation further says the mutable
  prefetch idiom remains unsafe when a different, already-prefetched node is
  deleted as a side effect. The current groupMove call chain supplies exactly
  that prohibited mutation.

Ancestry/control comparison:
- Supplied original Civ4 groupMove already uses the same Firaxis idiom:
      pLoopUnit = ::getUnit(pUnitNode->m_data);
      pUnitNode = nextUnitNode(pUnitNode);
      pLoopUnit->move(...);
  However, supplied original Civ4 acquireCity does not show the conquest-time
  global verifyUnitValidPlot call used by this deterministic reproduction.
  F155 is therefore not backdated to Vanilla solely from loop shape.
- Supplied Warlords keeps the same live traversal AND its conquest branch calls
  GC.getMapINLINE().verifyUnitValidPlot(). CvPlot::verifyUnitValidPlot can move
  invalid units with jumpToNearestValidPlot. Thus the relevant reentrant
  membership-change family is present by Warlords.
- Supplied BtS keeps the same live traversal and conquest-time map verification.
- K-Mod later diagnosed the pointer invalidation explicitly and fixed groupMove
  by traversing a copied list.
- Base AdvCiv history says K-Mod 1.46 changes were broadly merged, including a
  rare CTD fix, but Base AdvCiv 1.14/current SAS groupMove still contains the
  older live traversal. The source itself explains why this function diverged:
  its advc.001 Gunship/no-city-capture staging comment says K-Mod 1.45 rewrote
  groupMove but merging that rewrite would also require reconciling the Gunship
  city-capture fix. Whatever other k146 code was merged, the copied-list safety
  property is absent from this current function.

Current implementation makes the old hazard especially explicit:
- Warlords/BtS used a hand-written while loop that prefetched nextUnitNode before
  calling move. AdvCiv refactored that shape into FOR_EACH_UNIT_VAR_IN.
- The macro improves readability and makes deletion of the CURRENT node safe,
  but it intentionally retains the same next-node-prefetch contract; it cannot
  make deletion of a FUTURE prefetched node safe.
- Therefore the modern macro is not a repair for the K-Mod 1.46 bug.

Repair direction:
- Restore the K-Mod 1.46 safety invariant: do not iterate the live selection-
  group CLinkList across calls to CvUnit::move that can reenter membership-
  mutating code.
- Snapshot the units/identities to be considered before movement and iterate
  that snapshot. Because reentrant effects can move, regroup or potentially
  destroy future members, an IDInfo-style snapshot with a fresh ::getUnit lookup
  and current-group/current-validity check before acting is safer than assuming
  raw pointers necessarily remain usable.
- Preserve current AdvCiv/SAS two-stage no-city-capture ordering and advc.153
  behind-group behavior while changing only the iteration ownership/lifetime
  contract.
- Do not rely on FOR_EACH_UNIT_VAR_IN as the fix: its documented guarantee covers
  deletion of the current node, not deletion of the prefetched next node by a
  nested side effect.

Distinctness:
- F155 is independent of F152 pathfinder invalidation. F152 concerns stale path
  cache state after legitimate group membership changes; F155 concerns unsafe
  linked-list traversal while the membership change occurs reentrantly.
- It is also independent of mission/cargo group-state roots F143-F154. Fixing
  any of those does not make groupMove traversal lifetime-safe.


C015-WIP15 INTERIM CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP15
CURRENT FILE-015 NEW ROOTS: F143-F155 / provisional KI#466-KI#478
NEXT ROOT ID: F156 UNUSED

Continue the remaining helper/tail disposition and the adversarial current-tree
verification matrix across F143-F155. File 015 should close only after every
root has survived that challenge and the remaining exported/helper families
have been explicitly inspected. No repository source files were modified.


===============================================================================
C015-WIP16 APPEND -- F156 outer updateTimers traversal invalidated by combat regroup
===============================================================================

F156 / provisional KI#479 -- CONFIRMED -- INHERITED BASE ADVCIV INTERACTION DEFECT
POTENTIAL USE-AFTER-FREE / CTD WHEN UPDATETIMERS PREFETCHES A FUTURE UNIT NODE,
UPDATECOMBAT ADVANCES THE WINNER, AND ADVC.153 SPLITS THAT FUTURE UNIT FROM GROUP
-------------------------------------------------------------------------------
Status:
- NEW CURRENT DEFECT ROOT, crash-class linked-list lifetime failure.
- Distinct from F155 / KI#478. F155 is the live traversal INSIDE groupMove while
  CvUnit::move can reentrantly remove a future node. F156 is the caller one
  level above: updateTimers keeps its own prefetched future node across
  CvUnit::updateCombat, and a perfectly normal groupMove membership change can
  delete that node before updateTimers resumes.
- Supplied stock BtS has the same next-node prefetch in updateTimers but breaks
  immediately after updating one fighting unit, so the prefetched node is never
  consumed after updateCombat. K-Mod deliberately disabled that break so that
  several queued combats in one selection group can be updated.
- Current deterministic reproduction additionally uses Base AdvCiv change
  advc.153: on a normal MANUAL HUMAN combat advance, non-attacking members are
  deliberately split from the attacker's original group rather than advancing
  with it. The proven current root is therefore classified conservatively as a
  Base AdvCiv inherited interaction defect, rather than backdating the exact
  failure to K-Mod from architecture alone.

Vulnerable outer traversal:
- Current CvSelectionGroup::updateTimers uses:
      FOR_EACH_UNIT_VAR_IN(pUnit, *this)
      {
          if (pUnit->isInCombat())
          {
              ...
              pUnit->updateCombat();
              bCombat = true;
              //break; // disabled by K-Mod.
          }
      }
- LinkedListTraversal.h's mutable traversal obtains the current unit and
  PREFETCHES the next CLLNode before entering the body. As documented for F155,
  this supports deletion of the CURRENT node but not deletion of the already-
  saved NEXT node by a nested side effect.
- Because K-Mod removed the old break, updateTimers will attempt to resume the
  loop through that saved next-node pointer after updateCombat returns.

Deterministic current-SAS reproduction:
1. Create a manually controlled human land selection group G with at least two
   ordinary city-capturing combat units. Arrange attacker A as the group/best
   attacker and B immediately after it in G's unit list. A and B need not have
   different unit types; choosing A clearly stronger avoids attacker-selection
   ambiguity.
2. With Quick Combat off, order G to attack an adjacent visible enemy unit on a
   non-city plot that has no other visible enemy defender. Stack Attack can be
   OFF; this is the simplest control because groupAttack starts A's visible
   combat and then stops issuing further attacks.
3. The visible fight remains active across timer ticks. On the finishing tick,
   CvSelectionGroup::updateTimers resolves current unit A while its traversal
   has already prefetched B's live group-list node.
4. CvUnit::updateCombat clears A/defender combat state, kills the defeated final
   defender and computes bAdvance. With no visible enemy defenders remaining it
   executes:
       getGroup()->groupMove(pPlot, true, bAdvance ? this : NULL, true);
5. For a victorious ordinary attacker that can advance, groupMove first moves A
   to the target. Choose an ordinary non-city target so no conquest/culture
   invalidation from F155 is required for this reproduction.
6. Current groupMove's advc.153 state is:
       bool const bAIControl = isAIControlled();
       bool const bGroupAdvance = (!bCombat || bAIControl);
   For a manually controlled human group, isAIControlled() is false; with
   bCombat=true, bGroupAdvance is therefore false.
7. In stage 0, ordinary B is consequently taken through the left-behind branch
   and pUnit->joinGroup(NULL, true) (or joins an already-created behind group).
   CvUnit::joinGroup removes B from original G; CvSelectionGroup::removeUnit ->
   deleteUnitNode -> CLinkList::deleteNode physically deletes B's OLD node.
8. Control returns from groupMove and then updateCombat to the OUTER
   updateTimers loop. Its traversal variable still points at exactly that freed
   old B node. With the K-Mod break disabled, loop continuation dereferences the
   dangling CLLNode to obtain its IDInfo.
9. Result is undefined behavior / use-after-free: allocator state can turn it
   into stale traversal, memory corruption or a crash-to-desktop.

Why the normal combat path satisfies groupMove's safety assertions:
- updateCombat reaches the advancement code only after the finishing combat
  timer has elapsed and it has called setAttackPlot(NULL), setCombatUnit(NULL)
  and pDefender->setCombatUnit(NULL).
- Thus the simple two-unit reproduction no longer has an active combat object
  keeping G busy when groupMove executes; its FAssert(!isBusy()) does not rule
  out this path.
- The dangerous removal is also not an exotic nested ownership event. It is the
  intended advc.153 group-splitting branch itself.

Ancestry/control comparison:
- Supplied Civ4CE/BtS updateTimers manually performs the same structural
  prefetch:
      pLoopUnit = ::getUnit(pUnitNode->m_data);
      pUnitNode = nextUnitNode(pUnitNode);
      ... pLoopUnit->updateCombat();
  but then immediately executes:
      break;
  Therefore stock BtS does not return to the prefetched node through this
  sequence.
- Current source retains a K-Mod comment explaining why that break was disabled:
  groupAttack was changed so multiple units in one group can be queued for
  combat, and updating only the first can otherwise cause an infinite loop.
  Restoring the BtS break is therefore not a correctness repair.
- Base AdvCiv history records commit 9ffb0d869861f50784f03f968fc8acd6a471460f
  (2021-01-28), "Merge branch split-groups-after-missions," as change advc.153.
  Current groupMove explicitly tags bGroupAdvance and the human left-behind
  partition branches with advc.153.
- I have not established from the supplied historical material that pre-advc.153
  K-Mod itself had an equally deterministic future-node deletion during this
  exact updateTimers call chain. The defensible provenance is therefore:
  BtS prefetch + K-Mod no-break are prerequisites, while current proven
  membership deletion is supplied by Base AdvCiv advc.153; SAS inherits it.

Why F155's repair is insufficient:
- F155 can be fixed perfectly by snapshotting the iteration INSIDE groupMove.
  groupMove must still legitimately remove B from G to implement advc.153.
- F156's dangling node was saved by updateTimers BEFORE entering updateCombat /
  groupMove. No internal groupMove iterator repair can revoke that outer saved
  pointer.
- Both traversal boundaries therefore need their own lifetime-safe strategy.

Repair direction:
- Preserve K-Mod's ability to update multiple queued combats; do not simply
  restore the stock-BtS break.
- Do not hold live m_units CLLNode pointers across updateCombat or
  updateAirCombat, because those calls can cause group membership changes.
- Snapshot unit identities before the traversal, then fresh-lookup each unit
  before updating it. Skip an identity if the unit no longer exists or no
  longer belongs to this CvSelectionGroup. This makes ownership explicit and
  also tolerates other nested combat-side membership/death effects.
- A copied-ID traversal is preferable to raw unit pointers because combat can
  destroy units as well as regroup them.
- Re-test queued same-group combats after the repair so the original K-Mod
  anti-infinite-loop purpose remains intact.

Negative controls examined while establishing F156:
- The MISSION_GOLDEN_AGE unit loop initially looked similar because
  killGoldenAgeUnits can consume several Great People. Those calls use
  CvUnit::kill(true), i.e. startDelayedDeath; the group-list nodes are not
  physically removed until later doDelayedDeath -> kill(false). It therefore
  does not reproduce this future-node invalidation during startMission.
- MISSION_AIRLIFT physically removes the acting unit from its old selection
  group because airlift -> setXY uses bGroup=false. That removes the CURRENT
  node, which the mutable iterator permits. removeUnit also clears the old
  mission queue, but current startMission checks
      getNumUnits() == 0 || headMissionQueueNode() == NULL
  after each action and breaks before a next iteration can dereference the
  cleared queue. No separate current-tree crash root was promoted there.


C015-WIP16 INTERIM CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP16
CURRENT FILE-015 NEW ROOTS: F143-F156 / provisional KI#466-KI#479
NEXT ROOT ID: F157 UNUSED

Continue the remaining mutation-capable mission/action loops, exported/helper
families and the adversarial current-snapshot closure matrix. Do not close file
015 merely because F156 is now settled. No repository source files were
modified.


C015-WIP17 APPEND -- reconciled-authority merge + mutation/lifetime closure pass
===============================================================================

AUTHORITY / MERGE STATUS
------------------------
This checkpoint deliberately starts from the user-provided 2026-08-26 12:27:36
light-source reconciled cpp_file_audit_album.txt as its exact byte prefix. That
reconciled authority ends at C015-WIP13 / F153. The locally farther C015-WIP14,
WIP15 and WIP16 append sections were then carried forward unchanged, preserving
F154-F156 without replacing any reconciled earlier text.

Current-source synchronization was checked rather than assumed. The latest
light-source CvGameCoreDLL/CvSelectionGroup.cpp and the source snapshot used for
C015-WIP16 have the same SHA-256:

    089ef460132b4d6862a369dbe1297c48d5c48a5a0d22c5aebeff76667fc4cc4f

A byte-for-byte diff is empty. Therefore no current implementation change has
silently invalidated the F154-F156 proofs or their line-level contracts since
WIP16. The latest reconciled Known Issues ledger also contains no collision for
provisional KI#466 through KI#479.

No repository source file was modified by this audit pass.


POST-F156 MUTATION / LIFETIME CLOSURE CONTROLS
----------------------------------------------
The remaining mutable-selection-group traversals were challenged specifically
for the lifetime pattern behind F155 and F156: the traversal macro prefetches a
live next CLLNode before entering the body, so deletion of the CURRENT node is
supported, while reentrant deletion of a FUTURE prefetched node is unsafe.
Several superficially dangerous sites close as negative controls:

1. CvSelectionGroup::doTurn
   - CvUnit::doTurn does not perform immediate kill(false), joinGroup, setXY or
     transport regrouping in the reviewed current implementation.
   - Unit death arising from turn processing is delayed and physically removed
     later through doDelayedDeath.
   - The saved pHeadUnit used by the surrounding turn-boundary logic therefore
     is not invalidated by a normal member's doTurn call in this path.
   - No F157 established.

2. CvSelectionGroup::doDelayedDeath
   - CvUnit::doDelayedDeath -> kill(false) removes the CURRENT unit-list node;
     that is the exact mutation the mutable traversal macro is designed to
     tolerate.
   - kill(false) recursively kills cargo before removing the carrier, so a
     same-group future cargo node would have been dangerous in principle.
     Current shipped SAS data closes that route concretely: every cargo-capable
     unit is DOMAIN_SEA, carrying DOMAIN_LAND or DOMAIN_AIR cargo, while
     CvUnit::canJoinGroup requires the selection group to have the same domain.
     Thus a transport and its carried units cannot occupy the same selection
     group whose future node is being traversed here.
   - No F157 established.

3. startMission action loop
   - MISSION_FOUND, religion/corporation spread, Great Person consumption and
     related consuming actions use delayed death during this loop; they do not
     physically delete a future selection-group node before loop continuation.
   - MISSION_GOLDEN_AGE was rechecked because killGoldenAgeUnits can consume
     several Great People. The unit kills are likewise delayed, matching the
     WIP16 negative control.
   - MISSION_AIRLIFT can physically move/ungroup the acting unit through setXY,
     but that is deletion of the CURRENT iteration node. Its old mission queue
     can also be cleared; startMission rechecks group emptiness/head mission and
     breaks before another action iteration can consume invalid queue state.
   - MISSION_PARADROP chooses the acting unit afresh through the mission helper;
     the earlier mutable pass only collects immobile units. No retained future
     group-list node crosses the paradrop setXY call.
   - No F157 established.

4. continueMission post-action split
   - The apCanMove vector is formed only under canAnyMove() && !canAllMove(), so
     apCanMove[0] is not an empty-vector access.
   - Members are subsequently regrouped from the saved unit pointers; no
     reviewed operation in that small split sequence kills an unprocessed unit
     as a side effect. This remains a valid raw-pointer use in the current
     shipped path rather than another linked-list-prefetch root.
   - No F157 established.

5. CvSelectionGroup::groupBuild
   - CvUnit::build may consume a build unit when the BuildInfo isKill flag is
     set, but it calls kill(true): delayed death. The live traversal therefore
     does not lose a future CLLNode during the build loop.
   - The later "stop other workers" pass traverses the plot, not this group's
     list, and deletes a MISSION QUEUE node from another group; it does not
     remove the iterated plot unit from that plot list.
   - No F157 established.

6. CvSelectionGroup::setTransportUnit
   - Loading explicitly expects setTransportUnit to ungroup the CURRENT unit;
     the loop breaks immediately after each successful load and restarts from
     the group's new head. This avoids consuming the prefetched next node.
   - Unloading clears the transport relation but current CvUnit::setTransportUnit
     does not ungroup the unloaded unit, so the unloading traversal remains
     structurally stable.
   - No F157 established.

7. CvSelectionGroup::unloadAll
   - Each transport's CvUnit::unloadAll first snapshots its cargo into a
     std::vector<CvUnit*> and only then clears transport links. Cargo units are
     cross-domain members of other selection groups; the outer selection-group
     traversal over the sea carriers does not delete a future carrier node.
   - No F157 established.

8. mergeIntoGroup / splitGroup / regroupSeparatedUnits
   - mergeIntoGroup removes the CURRENT member through joinGroup; if changing a
     UnitAI can regroup that current unit, it breaks and restarts deliberately.
   - splitGroup's mutable pass likewise joins only its CURRENT pLoopUnit to the
     split group. Mutable traversal's current-node deletion contract covers it.
   - regroupSeparatedUnits also moves only its CURRENT pLoopUnit. The retained
     head unit is deliberately left at the head plot and is not one of the
     members being removed in that loop.
   - No future-prefetched-node deletion comparable to F155/F156 was established.

9. setActivityType / NotifyEntity / airCircle / setBlockading
   - The group-level mutable loops call graphics/entity notification or update
     unit-local blockade accounting. Reviewed CvUnit implementations do not
     regroup, move or synchronously destroy selection-group members.
   - No F157 established.

10. Mission-queue ownership helpers
    - clearMissionQueue deactivates the current head mission before clearing the
      queue. deactivateHeadMission changes activity/timer state but does not
      recursively activate a replacement mission.
    - deleteMissionQueueNode deliberately leaves next-head activation to its
      caller; this avoids an immediate nested startMission while a caller still
      owns a live queue-node pointer.
    - continueMission copies MissionData before mutation-capable movement/combat
      work and explicitly refreshes headMissionQueueNode after groupAttack and
      after groupPathTo where the queue may have changed.
    - No new queue-node UAF root was established in this closure pass.


BOARDED / CARGO TAIL RECHECK
----------------------------
handleBoarded's duplicate cargo-group pointers remain a robustness/performance
quirk rather than a separate root:
- getLandCargoGroups can push the same selection-group pointer once per cargo
  unit carried by this sea group;
- after an earlier merge, the emptied old group object survives until delayed
  group death, and mergeIntoGroup on the already-target group is harmless
  because CvUnit::joinGroup immediately returns when the requested group is the
  unit's existing group;
- deduplicating these pointers would reduce redundant work but would NOT repair
  F153, whose defect is the cross-carrier scope of ACTIVITY_BOARDED itself.

The F154 automation sibling remains independently live for the same structural
reason: setAutomateType(NO_AUTOMATE) selects cargo by THIS transport group, then
writes automation/activity to the cargo UNIT'S WHOLE selection group. Fixing
only handleBoarded cannot fix that inherited BtS scope leak.


ADVERSARIAL CURRENT-SNAPSHOT ROOT RECHECK
-----------------------------------------
Because CvSelectionGroup.cpp is byte-identical to the WIP16-reviewed snapshot,
F143-F156 were re-challenged primarily for hidden reconciliation collisions and
for neighboring controls that could collapse two roots into one. None was
retracted:

- F143/KI#466 Sea Patrol mixed-group persistence: still distinct initial
  admission/turn-cleanup contract.
- F144/KI#467 Blockade group divergence: still distinct destructive wake after
  a previously valid blockader loses legality.
- F145/KI#468 group-load shared capacity: still a pre-execution aggregate
  capacity defect, not a transport/cargo activity-state issue.
- F146/KI#469 Spy sleep/wake UI lifecycle: still head-only participant logic.
- F147/KI#470 sentry hostility-actor mismatch: no mutation/lifetime overlap.
- F148/KI#471 recon cleanup group-actor defect: still unit-local recon state
  cleaned through the wrong group actor.
- F149/KI#472 amphibious attempted-vs-successful landing: remains execution
  success/mission-consumption logic, not path-cache or cargo-state scope.
- F150/KI#473 Air Patrol moveless participant regression: remains K-Mod split
  eligibility behavior and is not subsumed by F146/F148.
- F151/KI#474 upgrade action-cache representative defect: still per-unit upgrade
  price collapsed by one UnitType representative.
- F152/KI#475 reusable GroupPathFinder membership invalidation: still requires
  membership-change invalidation even if F155/F156 live-iterator repairs are
  implemented; those fixes do not invalidate cached path state.
- F153/KI#476 ACTIVITY_BOARDED cross-transport scope leak: still AdvCiv-specific
  and distinct from automation state.
- F154/KI#477 automation cross-transport scope leak: still inherited from BtS
  and distinct from F153.
- F155/KI#478 groupMove live traversal invalidation: still the inner traversal
  boundary and independently requires lifetime-safe iteration.
- F156/KI#479 updateTimers combat-regroup invalidation: still the outer caller
  traversal boundary; repairing groupMove alone leaves its saved next node
  dangling when legitimate advc.153 regrouping occurs.

No current-source contradiction, Known-Issues ID collision, or shared repair
was found that would justify retracting or merging any of these roots.


C015-WIP17 INTERIM CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP17
CURRENT FILE-015 NEW ROOTS: F143-F156 / provisional KI#466-KI#479
NEXT ROOT ID: F157 UNUSED

The mutation/lifetime family is now substantially closed beyond F155/F156, and
the current-snapshot root matrix has survived reconciliation. Continue with a
final exported/caller/API contract sweep (especially externally exposed list /
mission helpers and any semantics whose correctness depends on callers rather
than local memory safety), then challenge top-of-file sentry/turn lifecycle and
the remaining canDoMission/interface predicate families once more. If those
surfaces close without a deterministic new root, file 015 is approaching a
legitimate durable close; do not manufacture F157 merely to advance the ID.


C015-WIP18 APPEND -- F157 Firaxis Python queue-data ownership contract
======================================================================

F157 / provisional KI#480 -- CONFIRMED -- INHERITED FIRAXIS VANILLA PYTHON API DEFECT
--------------------------------------------------------------------------------------

Summary:
The Python-exposed selection-group API getMissionFromQueue advertises Boost.Python
`manage_new_object` ownership even though CySelectionGroup forwards a pointer to
MissionData stored inside CvSelectionGroup's live CLinkList mission node. A Python
caller therefore receives an owning wrapper for memory that was not allocated as a
standalone object. When the wrapper is released, Boost's owning-holder policy deletes
that interior pointer, creating invalid-free / heap-corruption / crash risk. The same
Firaxis pattern also exists in CyCity::getOrderFromQueue for OrderData, establishing a
systemic queue-data binding root rather than an isolated SAS wrapper typo.

Current selection-group chain:

    // CvSelectionGroup.cpp
    MissionData* CvSelectionGroup::getMissionFromQueue(int iIndex) const
    {
        CLLNode<MissionData>* pMissionNode = m_missionQueue.nodeNum(iIndex);
        if (pMissionNode != NULL)
            return &(pMissionNode->m_data);
        return NULL;
    }

    // CySelectionGroup.cpp
    MissionData* CySelectionGroup::getMissionFromQueue(int iIndex)
    {
        return m_pSelectionGroup ?
            m_pSelectionGroup->getMissionFromQueue(iIndex) : NULL;
    }

    // CySelectionGroupInterface.cpp
    .def("getMissionFromQueue", &CySelectionGroup::getMissionFromQueue,
         python::return_value_policy<python::manage_new_object>(), ...)

The pointer returned to Python is therefore `&pMissionNode->m_data`: storage owned by
the CLinkList node, not by a `new MissionData` expression.

Why `manage_new_object` is not a harmless annotation:
- Boost.Python's manage_new_object policy routes pointer returns through an owning
  holder (`make_owning_holder`). The holder constructs an owning smart pointer from
  the returned raw pointer.
- Destruction of that Python-owned holder consequently performs deletion of the
  returned C++ pointer.
- That policy is correct for nearby wrappers such as CySelectionGroup::getHeadUnit,
  where the Cy wrapper actually executes `new CyUnit(...)` before returning.
- It is false for getMissionFromQueue, which forwards internal queue storage without
  allocating a new MissionData object.
- Queue mutation creates a second lifetime problem even before Python destruction:
  clearing/deleting that mission node invalidates the borrowed interior pointer while
  Python may still retain its wrapper.

Systemic Firaxis sibling:

    // CvCity.cpp
    OrderData* CvCity::getOrderFromQueue(int iIndex) const
    {
        CLLNode<OrderData>* pOrderNode = m_orderQueue.nodeNum(iIndex);
        if (pOrderNode != NULL)
            return &pOrderNode->m_data;
        return NULL;
    }

    // CyCity.cpp
    OrderData* CyCity::getOrderFromQueue(int iIndex)
    {
        return m_pCity ? m_pCity->getOrderFromQueue(iIndex) : NULL;
    }

    // CyCityInterface1.cpp
    .def("getOrderFromQueue", &CyCity::getOrderFromQueue,
         python::return_value_policy<python::manage_new_object>(), ...)

Thus the MissionData defect is not best explained as a K-Mod/AdvCiv/SAS regression;
Firaxis used the same ownership pattern for both exposed linked-list data structs.

Reachability / severity qualification:
- No current shipped SAS Assets/Python or PrivateMaps Python source calls
  getMissionFromQueue or getOrderFromQueue. Current UI code instead uses scalar-safe
  helpers such as getMissionType/getMissionData1/getMissionData2.
- Therefore this is NOT being promoted as a normal current-gameplay CTD reproduction.
  It is a dormant but real public-Python-API memory-ownership defect: a Python mod,
  event script, console command or future SAS code using the advertised API can trigger
  the invalid ownership contract directly.
- The bundled Python API documentation exposes both methods as returning MissionData /
  OrderData objects, so treating them as unsupported private internals would be wrong.

Minimal deterministic API reproduction shape:
1. Obtain a CySelectionGroup with at least one queued mission.
2. In Python, call `m = group.getMissionFromQueue(0)` and retain or release the result.
3. The binding constructs an owning Python holder around the address of the queue
   node's embedded MissionData.
4. When that owning wrapper is destroyed, its ownership policy attempts to delete the
   non-standalone pointer. Alternatively, clear the mission queue while retaining `m`;
   the Python object now refers to storage whose containing CLinkList node is gone.
5. Either path violates C++ object ownership/lifetime and can manifest as heap
   corruption or a crash; exact allocator timing need not be deterministic for the
   ownership bug itself to be deterministic.

Ancestry:
- Supplied Civ4CE Vanilla, Warlords and BtS all contain the same
  `return &(pMissionNode->m_data)` implementation and the same
  `return_value_policy<manage_new_object>` Python binding.
- Base AdvCiv 1.14 retains both unchanged, and current SAS retains them.
- F157 therefore originates in Firaxis Vanilla Civ4, not K-Mod, AdvCiv or SAS.

Repair direction:
- Keep the public Python behavior value-like and make the ownership policy truthful.
  In CySelectionGroup::getMissionFromQueue, obtain the internal MissionData pointer and
  return `new MissionData(*pMissionData)` when non-null. Then the existing
  manage_new_object policy correctly owns an independent heap object whose lifetime is
  unrelated to later mission-queue mutation.
- Apply the same repair to CyCity::getOrderFromQueue:
  return `new OrderData(*pOrderData)` for a valid queue entry.
- This is preferable to switching the binding to reference_existing_object: a borrowed
  reference would remain vulnerable to queue-node deletion and would expose writable
  Python fields directly onto live DLL queue state.
- The existing scalar mission getters remain useful and require no ownership change.
- Because this root spans CvSelectionGroup plus its Cy wrapper and has a proven CvCity
  sibling, implementation/review should search for any other `manage_new_object`
  bindings that forward pointers into container-owned POD storage before declaring the
  pattern globally closed.

Relation to earlier file-015 roots:
- F157 is independent of F155/F156. Those are C++ live-CLLNode traversal UAFs during
  nested group mutation; F157 is a cross-language ownership error at the Python ABI.
- It is independent of F151's action cache and all mission semantic roots F143-F150.
- A copied MissionData Python result also avoids stale queue-node lifetime by design;
  it does not alter mission execution or queue semantics inside CvSelectionGroup.


C015-WIP18 INTERIM CURSOR
-------------------------
COMPLETED FILES: 14 / 169
FILE 015: CvGameCoreDLL/CvSelectionGroup.cpp (4,722 lines) -- OPEN
PHYSICAL CHECKPOINT: C015-WIP18
CURRENT FILE-015 NEW ROOTS: F143-F157 / provisional KI#466-KI#480
NEXT ROOT ID: F158 UNUSED

Continue the final exported/caller sweep, including a bounded repository-wide check of
other owning Python pointer-return bindings so F157's scope is neither understated nor
needlessly generalized. Then finish the remaining sentry/turn and mission/interface
predicate closure. File 015 remains open until those surfaces receive explicit
adversarial dispositions; do not manufacture F158 if they close cleanly.


===============================================================================
C016 DURABLE CHECKPOINT -- FILE 015 CLOSED / FILE 016 QUEUED
===============================================================================

SOURCE IDENTITY AT CLOSURE
--------------------------
Branch: main
HEAD: 74a53043ed15c4e896fb99c4fe912d01593bdfc2
Practical commit count: 6274
Base comparison: local AdvCiv 1.14 source archive
Additional ancestry: local Civ4CE Vanilla / Warlords / BtS source archive
Current .cpp queue: 169 files, sorted by current line count longest -> shortest

The current CvGameCoreDLL/CvSelectionGroup.cpp is 4,722 lines and has SHA-256:

    089ef460132b4d6862a369dbe1297c48d5c48a5a0d22c5aebeff76667fc4cc4f

It is byte-identical to the source snapshot used for the late WIP15-WIP18
adversarial passes. The user-provided 2026-08-26T12:27:36 reconciled album is an
exact byte prefix of C015-WIP17, and WIP17 is an exact byte prefix of WIP18;
this durable C016 checkpoint appends closure only and preserves that merged
history.

No repository source files were modified by this audit pass.


FILE 015 -- CvGameCoreDLL/CvSelectionGroup.cpp -- 4,722 lines -- REVIEWED / CLOSED
---------------------------------------------------------------------------------
The C015 investigation is now durably closed after WIP18's final exported/Python
ownership sweep. The file was not closed merely because a time budget expired:
the remaining surfaces have explicit positive or negative controls, and the
adversarial root matrix was rerun against the exact current source snapshot.

Stable file-015 root accounting:

* 15 valid new roots were established: F143-F157.
* Their provisional audit aliases are KI#466-KI#480.
* None of KI#466-KI#480 collides with the latest independently reviewed
  README_Known_Issues ledger in the supplied 6274 light source.
* These aliases remain provisional investigative handoff identifiers. Me/Codex
  review can accept, refine, merge or reject implementation proposals as runtime
  evidence warrants; closure means the source surface has been audited, not that
  every candidate must be implemented unchanged.
* No F158 was allocated.


FILE-015 ROOT LEDGER AT CLOSURE
-------------------------------
F143 / provisional KI#466
    Inherited AdvCiv Sea Patrol mixed-group persistence defect.

F144 / provisional KI#467
    Inherited AdvCiv Blockade group divergence / destructive wake defect.

F145 / provisional KI#468
    Inherited AdvCiv group-load shared-capacity defect.

F146 / provisional KI#469
    Inherited K-Mod / AdvCiv Spy sleep-wake UI-lifecycle defect.

F147 / provisional KI#470
    Inherited BtS / AdvCiv sentry hostility-actor mismatch.

F148 / provisional KI#471
    Inherited AdvCiv recon cleanup group-actor defect.

F149 / provisional KI#472
    Current SAS amphibious attempted-vs-successful landing contract defect,
    made concretely reachable by current SAS feature-impassable units.

F150 / provisional KI#473
    Inherited K-Mod Air Patrol moveless-participant regression.

F151 / provisional KI#474
    Inherited BtS upgrade action-cache representative defect.

F152 / provisional KI#475
    Inherited AdvCiv/K-Mod reusable GroupPathFinder membership-invalidation
    defect.

F153 / provisional KI#476
    Inherited AdvCiv advc.075 ACTIVITY_BOARDED cross-transport cargo-group scope
    defect.

F154 / provisional KI#477
    Inherited Firaxis BtS cross-transport cargo automation scope defect.

F155 / provisional KI#478
    Inherited Warlords/BtS live groupMove traversal invalidated by reentrant move
    side effects; K-Mod 1.46 explicitly fixed the historical crash, but the
    current divergent AdvCiv/SAS groupMove retains a proven reachable variant.

F156 / provisional KI#479
    Inherited Base AdvCiv interaction defect: outer updateTimers traversal can
    retain a prefetched unit-list node while combat completion invokes advc.153
    groupMove regrouping that deletes that future node.

F157 / provisional KI#480
    Inherited Firaxis Vanilla Python API ownership defect: getMissionFromQueue
    returns container-owned MissionData interior storage while the Boost.Python
    binding declares manage_new_object. CyCity::getOrderFromQueue has the same
    systemic OrderData sibling. Current shipped SAS Python does not call either,
    so this is a dormant public-API memory-lifetime defect rather than a normal
    gameplay CTD path.


WHY FILE 015 IS CLOSED
----------------------
The closure pass covered the full 134-current-member definition surface plus
associated header/Cy-wrapper contracts, with special attention to the mutation,
mission and group-state areas most likely to hide reentrant defects:

* construction/reset/kill and pathfinder ownership;
* sentry known-enemy bookkeeping and turn/post-turn lifecycle;
* unit turn processing, combat timers and delayed death;
* mission push/pop/start/continue/update and mission-timer ownership;
* all current mission families, including movement, route, amphibious landing,
  air patrol/recon/rebase/airlift/paradrop, blockade/sea patrol, build, Great
  Person and espionage actions;
* command/action cache and interface-mode availability/target predicates;
* group movement, combat, route/build execution and live-list mutation;
* cargo capacity, load/unload, carrier/cargo activity and automation semantics;
* split/merge/regroup membership mutation;
* activity/automation state transitions;
* current GroupPathFinder reuse/invalidation assumptions;
* selection-group/unit-list and mission-queue helper ownership;
* serialization and current uiFlag state;
* exported DLL node helpers and their stock ABI ancestry;
* CySelectionGroup public Python wrappers and a bounded repository-wide
  manage_new_object comparison;
* Vanilla/Warlords/BtS, K-Mod changelog/history and Base AdvCiv ancestry where
  they materially distinguish provenance;
* current SAS XML/data reachability for cargo domains, feature impassability,
  upgrade discounts and other reproductions;
* latest reviewed Known-Issues collision checks;
* adversarial reread of every live F143-F157 root and neighboring hypotheses.

The final mutation/lifetime challenge explicitly rejected plausible false
positives in doTurn, doDelayedDeath, startMission consumption, airlift/paradrop,
groupBuild, setTransportUnit, unloadAll, merge/split/regroup,
setActivityType/NotifyEntity/airCircle/setBlockading and mission-queue deletion.
Those controls are important: F155/F156 are not a blanket claim that mutable
traversal is unsafe; their distinguishing condition is deletion of a FUTURE
prefetched CLLNode across a nested call.

Likewise the final mission/interface pass did not promote every any-member group
predicate into a defect. Mixed-group semantics were only promoted where a later
executor/lifecycle supplied a deterministic contradictory contract (e.g.
F143/F144/F148/F150), while benign mixed waiting/escort behavior remained a
negative control.

The raw external node helpers remain stock DLL ABI surfaces whose external EXE
caller source is unavailable in the supplied material. Their mere exposure is
not a finding. No local caller sequence established a lifetime violation beyond
F155/F156, so no speculative F158 was spent there.

F157's broader owning-binding check also remains bounded: the normal
manage_new_object pattern returns separately allocated Cy* wrapper objects. The
identified queue-data exceptions are MissionData and OrderData, both forwarding
addresses into CLinkList node storage. That is one systemic Firaxis ownership
root, not evidence that all owning Python bindings are broken.

With those controls recorded, the remaining file-015 surface is better supported
as intentional/covered than merely unchecked. CvSelectionGroup.cpp is therefore
READY FOR DURABLE CLOSURE.


PROGRESS AFTER FILE-015 CLOSURE
-------------------------------
Files complete: 15 / 169
Files open: 0 / 169
Last completed file: 015 CvGameCoreDLL/CvSelectionGroup.cpp -- 4,722 lines

NEXT QUEUED FILE:

    016 CvGameCoreDLL/CitySiteEvaluator.cpp -- 4,595 lines

Following files by the current line-count queue begin:

    017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines
    018 CvGameCoreDLL/CvGameInterface.cpp -- 2,965 lines
    019 CvGameCoreDLL/CvDLLButtonPopup.cpp -- 2,814 lines
    020 CvGameCoreDLL/CvInfo_Unit.cpp -- 2,599 lines

C016 is a durable boundary checkpoint. File 016 has NOT yet been audited in this
continuation. Start CitySiteEvaluator.cpp from the current practical-6274 source
on the next pass rather than carrying CvSelectionGroup hypotheses forward by
analogy.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C016-WIP01 INTERIM CHECKPOINT -- FILE 016 CitySiteEvaluator.cpp OPEN
===============================================================================

SOURCE IDENTITY
---------------
Branch: main
HEAD: 74a53043ed15c4e896fb99c4fe912d01593bdfc2
Practical commit count: 6274
Current file: CvGameCoreDLL/CitySiteEvaluator.cpp -- 4,595 lines
Current file SHA-256:

    ed0688350c62cba526a5d5427c97364e5bdf29ea35c0d71fd41cae675f5da665

Base comparison: local AdvCiv 1.14 source archive
Additional ancestry: local Civ4CE Vanilla / Warlords / BtS source archive and
reachable K-Mod / AdvCiv / SAS commit-history mirror.

This checkpoint is append-only over durable C016. No repository source files were
modified by this audit pass.


F158 / provisional KI#481 -- inherited AdvCiv isRemovableFeature current-research regression
---------------------------------------------------------------------------------------------
ROOT
AIFoundValue::isRemovableFeature incorrectly handles a feature-removing Build whose
normal build prerequisite and feature-removal prerequisite are different technologies.
After first testing whether the team already has both technologies, its fallback for
"CurrentResearch should be good enough" requires one player's single current research
to equal BOTH eTech1 and eTech2 simultaneously:

    if (kMember.getCurrentResearch() == eTech1 &&
        kMember.getCurrentResearch() == eTech2)
        return true;

For distinct prerequisites this can never succeed. The intended state -- one
prerequisite already known and the other being researched -- is therefore classified
as not removable even though the feature is about to become removable.

SHIPPED REPRODUCTION
Current BuildInfos supplies ordinary distinct-tech cases. For example, BUILD_FARM has
TECH_AGRICULTURE as its normal build prerequisite while Forest removal requires
TECH_MASONRY and Jungle removal requires TECH_METAL_CASTING. A team that already knows
Agriculture and is currently researching Masonry therefore fails the fallback for a
Forest Farm: currentResearch cannot simultaneously be Agriculture and Masonry.
Mine and other feature-removing build paths provide the same shape.

CONSEQUENCE
isRemovableFeature feeds the central city-site plot evaluation and the longer-horizon
evaluateWorkablePlot path. During the affected research turn(s), removable Forest /
Jungle can be treated as persistent or unavailable for improvement, changing feature
yield/health handling, improvement potential and found-value comparisons. This is a
scoring correctness regression, not merely a diagnostic mismatch.

ANCESTRY
The pre-refactor AdvCiv AI_foundValue logic used the correct independent condition for
each prerequisite: each tech could be either already known OR the player's current
research. AdvCiv commit

    078fc9bd7c7962bdd944073c372ffd2e5853b16e
    2019-12-15 -- "Move AI_foundValue computation into a new class"

split the logic into AIFoundValue::isRemovableFeature and introduced the impossible
same-current-research conjunction. Base AdvCiv 1.14 retains it and current SAS inherits
it unchanged.

REPAIR DIRECTION
Test the two prerequisites independently. Conceptually, each prerequisite should pass
when NO_TECH, already held by the team, or currently researched by the relevant team
member. Do not require one current-research slot to equal two distinct technologies.
Keep team/member semantics consistent with the old known-good implementation.


F159 / provisional KI#482 -- inherited AdvCiv regression makes K-Mod other-island starting badness branch unreachable
-----------------------------------------------------------------------------------------------------------------------
ROOT
AIFoundValue::adjustToStartingSurroundings currently wraps the nearby-plot evaluation
in:

    if ((p.isWater() || p.isArea(kArea)) && it.currPlotDist() <= iRange)

but later, inside that already-filtered block, retains K-Mod's intended distinction:

    if (p.isWater() || p.isArea(kArea))
        iR += iTempValue;
    else if (iTempValue >= 13)
        iGreaterBadTile++; // add at least 1 badness point for other islands.

For a land plot on another area, the outer condition has already rejected the plot.
The inner "other islands" else branch can therefore never execute.

INTENDED K-MOD CONTRACT
K-Mod commit

    51e5ee601626cd188f6beb21a0c0ea65de0bd471
    2015-01-24 -- "Land on other islands is now counted as \"bad\" for starting locations."

explicitly commented out the old outer same-area gate. Its diff also moved the yield
addition behind an inner area test: nearby other-island land should NOT contribute its
yields to the capital, because the capital cannot work it, but sufficiently good land
on that inaccessible island SHOULD increase the bad-tile count because it consumes
nearby geographic space without helping the starting city.

REGRESSION ANCESTRY
AdvCiv's 2017 initial import continuation

    f6ff6a182f5187541fff6e5bbbe12f9573b3778c
    2017-04-16 -- "Initial commit (cont.)"

restored the old outer same-area condition while retaining K-Mod's inner other-island
branch and comment. Base AdvCiv 1.14 and current SAS still have this contradictory
combination.

CONSEQUENCE
Starting-location surroundings can undercount inaccessible nearby land on a different
island/area. Such plots neither add usable yields nor the K-Mod badness point they were
specifically meant to add, so starts near tight coast/island boundaries can be rated
better than the intended K-Mod heuristic.

REPAIR DIRECTION
Restore K-Mod's structure: let the distance-qualified nearby plot reach the scoring
body regardless of land area, add iTempValue only for water or same-area land, and use
the existing other-island branch to add badness for sufficiently valuable but
unworkable land.


F160 / provisional KI#483 -- inherited Warlords/BtS bonus-improvement scan ignores actual plot domain; water Oil can use land Well timing/yields
-----------------------------------------------------------------------------------------------------------------------------------------------
ROOT
AIFoundValue::getBonusImprovement receives the actual CvPlot& p, but its Build /
Improvement scan never verifies that a candidate improvement can exist on that plot.
It checks bonus validity/trade, technology nearness, feature-removal technology and
Fort exclusion, but not water/land domain (nor a stronger plot-validity predicate).

CURRENT SAS DETERMINISTIC REPRODUCTION
Oil has two connecting improvements in current XML:

* IMPROVEMENT_WELL: bWater=0; Oil trade improvement; +2 production, +3 commerce;
  BUILD_WELL requires TECH_COMBUSTION.
* IMPROVEMENT_OFFSHORE_PLATFORM: bWater=1; Oil trade improvement; matching current
  Oil improvement yield; its build requires the later TECH_MARINE_TECHNOLOGY.

Well appears before Offshore Platform in the Build/Improvement iteration. Oil itself is
revealed and city-trade enabled at Combustion. Therefore, for a WATER Oil plot after
Combustion but before Marine Technology, getBonusImprovement can select the land-only
Well, set bCanImprove/bCanImproveSoon from Well's tech path, and calculate Well as the
chosen bonus improvement even though a Well cannot be built on that water tile.

This is not caused by SAS's recent decision to continue scanning after an available
improvement. In current data Well and Offshore Platform have tied Oil improvement
yields, and Well is encountered first, so Base AdvCiv's old early-available shortcut
also lands on Well. The deeper defect is the missing plot/domain validity check.

CONSEQUENCE
The timing flags are materially wrong even though current Well and Offshore Oil yield
bonuses happen to tie. calculateSpecialYieldModifier sees the water Oil as currently
or soon improvable and avoids the intended delayed-improvement discount. The
longer-horizon evaluateWorkablePlot path likewise can skip its later-era devaluation
because bCanSoonImproveBonus was derived from the illegal Well path. This can overrate
water Oil before Offshore Platforms are technologically available.

The file itself contains a useful negative control: evaluateWorkablePlot later asks

    GC.getInfo(eBonus).getTechImprove(p.isWater())

when it needs eventual bonus-improvement timing. CvBonusInfo::getTechImprove(bool
bWater) is explicitly domain-aware, demonstrating that land and water Oil technology
paths are expected to differ.

ANCESTRY
The same shape is present in supplied Firaxis Warlords CvPlayerAI::AI_foundValue:
it loops all improvements and picks the first isImprovementBonusMakesValid(eBonus)
without checking the resource plot's domain. BtS retains it. Supplied Vanilla Civ4
does not contain this same eBonusImprovement found-value path, so the root is best
classified as inherited Warlords/BtS rather than Vanilla.

K-Mod strengthened the resource check (notably BonusTrade), and AdvCiv later refactored
it. AdvCiv commit

    891273d300ade2b2adc69bb9cc0a907c74e59267
    2019-12-21 -- "AI city placement tweaks"

added/used the actual plot parameter while fixing resource tech/yield details, but the
scan still did not acquire a domain/plot-legality test. Base AdvCiv 1.14 and current SAS
therefore retain the Firaxis root.

REPAIR DIRECTION
At minimum reject improvement candidates whose water/land domain disagrees with p.
A stronger solution can use the existing SAS bonus-specific plot/domain helper or an
appropriate plot-validity predicate, provided it does not accidentally require current
technology when this routine intentionally predicts near-future improvements.

RELATED NON-FINDING / LATENT SIBLING
Current SAS calculateBonusConnectionEra performs a similar all-Build scan without plot
domain filtering, so water Oil can nominally inherit Well's earlier Industrial-era
connection path there too. However, the current connection-era adjustment only acts on
positive explicit health/happiness/building-effect non-yield value. Current Oil has
zero health and happiness and no positive counted building health/happiness effect, so
this sibling has no demonstrated shipped scoring consequence at present. It is kept as
a latent robustness concern rather than a separate root.


F161 / provisional KI#484 -- current SAS contested-future-BFC helper filters rival cities by the candidate site's unrelated area
--------------------------------------------------------------------------------------------------------------------------------
ROOT
SAS's isPlotInKnownRivalFutureBFC(CvPlot const& p) is asked whether the SPECIFIC bonus
plot p is threatened by a known rival future BFC. Its rival-city loop nevertheless
contains:

    if (!pCity->isArea(kArea))
        continue;

AIFoundValue::kArea is not p's area. It is permanently bound in the constructor to the
land area of the NEW candidate city site kPlot currently being evaluated. The helper is
called while scanning bonus plots around all EXISTING own cities. Candidate site and
existing bonus can therefore be on entirely different continents.

DETERMINISTIC CROSS-AREA REPRODUCTION
1. Existing own city A is on land area A. An unowned Maize lies in A's future BFC.
2. Known rival city R, also on area A, has that same Maize within CITY_PLOTS_RADIUS;
   the Maize is genuinely contested and ownership is not reliable.
3. Evaluate a new city candidate B on a different land area B containing another
   Maize (or otherwise causing the same bonus type to be scored).
4. isBonusOwnedOrClaimedByFutureBFC(Maize) finds A's Maize and calls
   isPlotInKnownRivalFutureBFC on that plot.
5. Rival R is skipped solely because R is not on candidate B's kArea.
6. The helper falsely reports the A-area Maize unopposed; the caller then treats Maize
   as already reliably covered.

CONSEQUENCE
This is more than the flat diversity nudge. In current evaluate(), a falsely
"already covered" bonus loses the configured
SAS_EVALUATE_UNOWNED_BONUS_EXTRA_VALUE (currently 250). The same helper is also used by
nonYieldBonusValue to initialize bSurplus; false surplus status zeroes the candidate
copy's explicit health/happiness and counted building-effect value. Overseas/colony
site comparisons can therefore be distorted based on a rival-city filter tied to an
unrelated candidate continent.

INTRODUCTION / INTENT
The helper was introduced in SAS commit

    6341d3d556a7f580e3664b8631d52446c2c7c4da
    2026-07-11 -- "(Fixed/Improved) City-site bonus valuation used broad trade-style
    AI_bonusVal instead of settlement-specific long-term value and other related AI
    bonus settler valuation issues (KI#178)"

whose commit message explicitly says to avoid over-counting reliable future-BFC
coverage WHILE PRESERVING value for contested bonuses. The current area filter violates
that exact contract.

REPAIR DIRECTION
Do not filter rival cities by AIFoundValue::kArea here. The radius-2 test already
localizes the scan to cities that can actually threaten p. Replacing kArea with
p.getArea() is also unsafe for coastal resources: a water bonus' area differs from the
land area containing the coastal rival city that can claim it. Removing the unrelated
area filter is the straightforward correctness-first repair.


F162 / provisional KI#485 -- current SAS future-BFC coverage result is order-dependent because the first matching bonus returns immediately
------------------------------------------------------------------------------------------------------------------------------------------
ROOT
isBonusOwnedOrClaimedByFutureBFC asks an empire-wide existential question: is this
bonus type already owned or reliably covered by ANY existing city's future BFC?
However, once it encounters the first matching unowned/self-owned bonus plot, it does:

    return !isPlotInKnownRivalFutureBFC(*pLoopPlot);

A contested first copy therefore returns false immediately and prevents the search
from discovering a later safely claimable copy of the same bonus type.

DETERMINISTIC REPRODUCTION
1. The player does not yet have an actually available Maize.
2. Existing city A has an unowned Maize in its future BFC, but that plot is also inside
   a known rival city's future BFC and is therefore contested.
3. Existing city B has another unowned Maize in its future BFC with no known rival
   future-BFC contest; this second copy is reliable long-term coverage.
4. If the A/contested occurrence is encountered first in city/BFC iteration,
   isBonusOwnedOrClaimedByFutureBFC returns false immediately and never examines B.
5. If the safe occurrence is encountered first, the same empire state returns true.

CONSEQUENCE
Found value depends on city/BFC iteration order rather than resource-coverage state.
The false result can add the +250 unowned-resource diversity value to another Maize and
can preserve long-term non-yield Maize value even though a separate safe existing-city
copy already satisfies the helper's intended definition of reliable coverage.

INTRODUCTION / INTENT
This is independent of F161 but originates in the same KI#178 helper introduced by
SAS commit 6341d3d556a7f580e3664b8631d52446c2c7c4da. F161 asks the wrong rivals because
of unrelated area state; F162 stops searching after one contested occurrence even when
that individual contest test itself is correct.

REPAIR DIRECTION
A contested matching plot should mean "this occurrence does not establish reliable
coverage; keep searching", not "the whole empire has no reliable copy". Return true
when any safe qualifying copy is found; after exhausting all copies, return false.


F163 / provisional KI#486 -- current SAS future-BFC helper counts temporary trade imports as owned despite explicit opposite contract
------------------------------------------------------------------------------------------------------------------------------------
ROOT
The first line of isBonusOwnedOrClaimedByFutureBFC is:

    if (kPlayer.getNumAvailableBonuses(eBonus) > 0)
        return true;

But CvPlayer::getNumAvailableBonuses reads the capital plot group's bonus count, and
CvPlot::updatePlotGroupBonus explicitly ADDS getBonusImport at the capital. The DLL
also defines:

    getNumTradeableBonuses(eBonus) =
        getNumAvailableBonuses(eBonus) - getBonusImport(eBonus)

so imported bonuses are demonstrably included in getNumAvailableBonuses.

This directly contradicts the active CitySiteEvaluator comment, XML define comment,
Main Changes Guide and KI#147/KI#178 documentation, all of which say that trade imports
DO NOT count as owned here because they are temporary/non-permanent.

DETERMINISTIC REPRODUCTION
1. The player owns no Wheat and has no existing-city future-BFC Wheat that would count
   as reliable coverage.
2. The player temporarily imports Wheat in a resource trade deal connected to its
   capital network.
3. getNumAvailableBonuses(Wheat) becomes positive because imports are included in the
   capital plot-group bonus count.
4. Evaluate a post-capital candidate containing Wheat.
5. isBonusOwnedOrClaimedByFutureBFC returns true immediately because of the import.

CONSEQUENCE
The candidate Wheat can lose the configured +250 unowned-bonus diversity value, and
nonYieldBonusValue can set bSurplus and suppress the new permanent copy's explicit
health/happiness/building-effect settlement value. A cancellable trade deal is thus
allowed to erase precisely the permanent-resource value the SAS feature was designed
to preserve.

INTRODUCTION / REGRESSION ANCESTRY
KI#147's original SAS implementation (commit 24cf2b8853..., practical 5929) did NOT use
getNumAvailableBonuses. It scanned the player's cities with AI_countNumBonuses, which
did not convert temporary imports into owned map copies.

KI#178 commit

    6341d3d556a7f580e3664b8631d52446c2c7c4da

replaced that local scan with isBonusOwnedOrClaimedByFutureBFC and introduced the
getNumAvailableBonuses early return while retaining the explicit "Trade imports do not
count" contract. F163 is therefore a current SAS regression introduced by KI#178, not
an inherited AdvCiv defect.

REPAIR DIRECTION
Use an owned/permanent resource test that excludes getBonusImport. At minimum, do not
use raw getNumAvailableBonuses as the ownership predicate when its imported component
is explicitly outside this feature's semantics. Preserve the helper's later
existing-city/future-BFC coverage logic for not-yet-connected owned/claimable copies.


C016-WIP01 NEGATIVE CONTROLS / OPEN QUESTIONS
---------------------------------------------
* The earlier static Palace-type suspicion was rejected for current shipped data. The
  SAS Barbarian Palace maps to the Palace class but is deliberately not a capital
  building, and no deterministic wrong-capital valuation contract was established.
* countBadTiles has a small inherited logging typo: its diagnostic uses member
  iUnrevealedTiles before the caller copies the freshly computed local iUnrevealed into
  that member. This can log 0 unrevealed tiles when there are some, but no gameplay
  scoring consequence has been established; it remains secondary rather than F164.
* estimateImprovementProduction retains a TODO about water improvements. Current water
  improvements are resource-specific and resource improvement production is handled
  elsewhere, so shipped XML does not currently establish a separate missing-water
  production root there.
* calculateBonusConnectionEra shares F160's missing domain filter, but current Oil --
  the only shipped land/water multi-improvement bonus found -- has no positive explicit
  non-yield health/happiness/building value for that timing path. Keep this as a latent
  sibling unless a current consequential caller/data state is found.
* SAS's removal of Base AdvCiv's early "prefer currently available build regardless of
  yield" shortcut inside getBonusImprovement is NOT itself F160: current multi-path Oil
  data still selects Well first because Well and Offshore Platform tie on the compared
  improvement yields. The inherited domain omission is the proven root.


C016-WIP01 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN
PHYSICAL CHECKPOINT: C016-WIP01
CURRENT FILE-016 NEW ROOTS: F158-F163 / provisional KI#481-KI#486
NEXT ROOT ID: F164 UNUSED

Resume with the remaining KI#178/KI#187 bonus helpers and then the broader evaluate()
custom scoring surface. In particular, challenge building-effect eligibility,
connection timing, special-yield aggregation, sumUpPlotValues, deadlocked-bonus logic,
first-city/custom low-food gates and all area/culture assumptions. Keep the three
KI#178 helper roots separate unless implementation review later prefers one combined
patch: F161 is wrong area scope, F162 is premature existential termination, and F163 is
trade-import contamination.

Do not close file 016 until the early evaluate() custom logic and final deadlock /
starting-location tail have received the same adversarial pass. Do not manufacture
F164 merely to extend the ledger.
===============================================================================


LOCAL RECONCILIATION AFTER KI#465-KI#467 REVIEW

- F142 / KI#465 has been independently revalidated and fixed at team scope: one team-owned flipped-tile count now uses one fixed significance threshold, victim-team non-vassal land and living-member apportionment. The default `OWN_EXCLUSIVE_RADIUS=0` masks this supported configuration, so the repair remains source-verified.
- F143 / KI#466 has been independently revalidated and fixed: an individually ineligible companion wakes a Sea Patrol group only when no group member remains able to patrol. A direct mixed Battleship/Transport attempt did not expose the Sea Patrol command, so the narrow repair remains source-verified.
- F144 / KI#467 has been independently revalidated and fixed: clearing one invalid blockader preserves `ACTIVITY_PLUNDER` while another group member remains both blockading and legally able to plunder. The exact diplomatic transition remains source-verified.
- The compiled DLL completed an autoplay normally. C016-WIP01 remains verbatim evidence, and F158-F163 / provisional KI#481-KI#486 remain unchanged and provisional while `CitySiteEvaluator.cpp` is open. (GPT-5.6-Sol)


LOCAL RECONCILIATION AFTER KI#468-KI#471 REVIEW

- F145 / KI#468 has been independently revalidated and documented as pending. Its all-selected Load contract requires capacity-aware assignment across potentially heterogeneous transports; an apparently simple count or first-fit repair could still permit partial loading or reject a feasible assignment.
- F146 / KI#469 has been independently revalidated and fixed by scanning every sleeping-group member for a foreign-territory Spy reaching K-Mod's pre-unit-turn preparation threshold.
- F147 / KI#470 has been independently revalidated and fixed by retaining maximum-range LOS geometry while testing unit-local hostility through every group member that can personally see the candidate plot.
- F148 / KI#471 has been independently revalidated and fixed by clearing old recon visibility for every movable aircraft that can participate in the accepted rebase, rather than the potentially immobile head alone.
- The repaired DLL compiled successfully, and a Large Pangaea autoplay completed normally. The exact KI#469-KI#471 transitions remain source-verified because their manual setups were not exercised. C016-WIP01 and F158-F163 / provisional KI#481-KI#486 remain unchanged and provisional. (GPT-5.6-Sol)
===============================================================================


===============================================================================
C016-WIP02 APPEND -- 2026-08-26 -- CitySiteEvaluator.cpp continued
===============================================================================

Authority for this append: reconciled AdvCiv-SAS light source 20260826T150848.
C016-WIP01 is preserved byte-for-byte as the prefix of this file.

This pass continued through the custom first-city health/culture/BFC logic and the
resource-duplicate valuation path. Five further roots crossed the proof bar. A tempting
tiny-lake Lighthouse/Harbor candidate and an old special-food arithmetic regression
were both rejected as current findings after deeper caller/helper checks.


F164 / provisional KI#487 -- rival culture-pressure gate ignores real cross-area culture solely because the rival city tile is on another land area
----------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
The current SAS replacement for AdvCiv's adjustToCivSurroundings culture-pressure
logic scans known foreign cities, but begins each city with:

    if (!pCity->isArea(kArea)) continue;

kArea is the land area of the candidate settlement tile. This assumes a foreign city
on another land area cannot exert relevant culture pressure on the candidate.

That assumption is false in the actual culture engine. CvCity::doPlotCultureTimes100
explicitly permits culture to spread onto a plot in another area when the culture
distance is at most:

    eCultureLevel + 1

(the broader same-area range is larger). Thus a coastal city on one island can exert
real plot culture across a narrow channel onto another island.

DETERMINISTIC REPRODUCTION
1. Put a candidate land city site on island A.
2. Put a revealed/known rival city on nearby island B, so the city tiles have different
   CvArea IDs but are only two culture-distance steps apart.
3. Give the rival city culture level 2 or greater.
4. CvCity::doPlotCultureTimes100 can legally spread that city's culture onto the
   candidate-side plots because the cross-area distance is <= cultureLevel + 1.
5. CitySiteEvaluator nevertheless skips the rival city before even calculating its
   pressure because !pCity->isArea(kArea).

For the current simple pressure formula, a level-2 city at distance 2 has r=5 and would
produce pressure 10 + 90*(5-2)/5 = 64. With ordinary SOFT/HARD settings this is enough
to cause a substantial down-weight. The area filter instead forces its contribution to
zero.

CONSEQUENCE
Narrow-channel / island-chain sites can be rated as culturally safe even while an
existing known rival city can already project culture across the water toward them.
Depending on distance and XML thresholds, a site that should receive a medium penalty
or even rejection can receive none from this gate.

ANCESTRY / INTENT
The area restriction is inherited conceptually from K-Mod's 2012 foreign-proximity
found-value work and Base AdvCiv's adjustToCivSurroundings, which likewise considered
only same-area cities. SAS later disabled that complex routine and migrated a simpler
culture-pressure subset inline, carrying the same-area restriction with it.

The culture engine itself is the stronger authority for reachability and explicitly
contains special cross-area handling. This is therefore a long-standing geographic
assumption exposed by the simplified SAS gate, not merely a tuning disagreement.

REPAIR DIRECTION
Do not equate different land-area IDs with zero possible culture pressure. Preserve a
cheap same-area fast path if desired, but for a different-area rival city allow the
narrower range actually supported by CvCity::doPlotCultureTimes100 (culture level + 1)
rather than applying the full same-area range blindly. The no-cheat
AI_deduceCitySite/KNOWN_TO checks should remain intact.


F165 / provisional KI#488 -- SAS cautious-health score drops all Forest/Jungle health whenever the plot contains a visible bonus
-------------------------------------------------------------------------------------------------------------------------------
ROOT
The current cautious-health accumulator is located in the `else` of:

    if (eBonus != NO_BONUS)
        ... resource valuation ...
    else
        if (eFeature == Jungle/Forest)
            iCautiousHealthPercent += feature health percent;

So a Forest or Jungle contributes to the first/later-city bad-health dampener only when
that plot has NO visible bonus. A bonus does not itself remove the feature and does not
make its health effect disappear.

CURRENT SHIPPED DATA
SAS XML permits many bonuses on Forest and/or Jungle plots. Examples include current
food, luxury and strategic resources such as Pig/Deer on Forest and Rice/Banana,
Dye/Gems/Elephants/Spices on Jungle-related placements (exact map generation also
depends on terrain/feature rules). The important property is shipped, ordinary
feature+bonus coexistence: the feature health modifier remains real while the
CitySiteEvaluator accumulator ignores it.

DETERMINISTIC EFFECT
With the current comments' own scale, Jungle contributes -50 and Forest +50 to the
quick health estimate. Seven ordinary Jungle plots produce -350 and can cross a -300
start threshold. If one of those Jungle plots contains a bonus, the current accumulator
sees only -300. Conversely, a resource on Forest can erase +50 of mitigating health.
The site can therefore cross a configured penalty threshold in either direction merely
because a resource marker shares the feature.

CONSEQUENCE
The start/later bad-health dampener is not estimating BFC feature health as documented.
Resource-rich jungle starts can be treated as healthier than the same actual feature
layout without the resource, while resource-bearing Forest can fail to offset Jungle.
Because the dampener is thresholded and multiplicative, the discrepancy can be much
larger than 50 raw health-percent points once it changes whether the penalty runs.

INTRODUCTION / INTENT
This is SAS-specific custom logic. The active comments describe the accumulator simply
in terms of Jungle and Forest health (e.g. 7 Jungle = -350; 8 Jungle + 2 Forest = -300)
and do not document bonus plots as exempt. The exclusion comes from control-flow
placement rather than from bRemovableFeature, improvement legality, or a rule that the
feature will actually be removed.

REPAIR DIRECTION
Accumulate the relevant Forest/Jungle feature health independently of whether eBonus is
NO_BONUS. If the intended model is instead "health after likely improvements," that
needs an explicit feature-removal/improvement test; bonus presence alone is not such a
test.


F166 / provisional KI#489 -- SAS raw zero-city start-phase gates accidentally reintroduce a first-Barbarian-city distinction AdvCiv explicitly suppresses
-------------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
AIFoundValue construction deliberately normalizes Barbarian city count:

    // Barbarians shouldn't distinguish between earlier and later cities
    iCities = (bBarbarian ? 5 : kPlayer.getNumCities());

Current SAS evaluate() later introduces a separate raw state:

    int const iNumCities = kPlayer.getNumCities();
    bool const bStartPhase = (iNumCities == 0);

and uses bStartPhase for custom capital/first-city rules, including the hard minimum
`iGoodEnoughFirstCityBFCTiles` gate, the stricter starting very-bad-tile allowance and
the harsher starting health dampener.

REACHABILITY
The Barbarian player can genuinely pass through this evaluator with zero cities.
CvGame's Barbarian-city creation/site-selection path constructs/evaluates city sites for
BARBARIAN_PLAYER before its first city exists. Thus this is not merely a theoretical
state reachable only through WorldBuilder or API misuse.

DETERMINISTIC BEHAVIOR DIFFERENCE
Evaluate the same ordinary Barbarian candidate once when the Barbarian player has zero
cities and again after it has one unrelated city, with the local map state otherwise
unchanged. On the first call bStartPhase is true and the candidate can be hard-rejected
for failing the capital-only good-enough BFC count or can receive the stricter start
health/very-bad penalties. Once any Barbarian city exists, those exact SAS gates switch
to the later-city settings.

CONSEQUENCE
The first Barbarian city is subject to a unique capital-like quality policy despite the
inherited AdvCiv design explicitly preventing Barbarians from distinguishing early vs
later cities. This can suppress or reshape initial Barbarian settlement in ways that
disappear immediately after city #1 and can interact strongly with the hard first-city
BFC return-0 gate.

ANCESTRY / REGRESSION CLASS
The `iCities = 5` Barbarian normalization is inherited AdvCiv behavior (advc.108).
The conflicting bStartPhase gates are later SAS custom additions. This is therefore a
SAS regression against an explicit inherited contract rather than a Base AdvCiv bug.

REPAIR DIRECTION
Derive these first-city-only SAS gates from the evaluator's already-normalized notion of
early/late city state, or explicitly exclude bBarbarian from bStartPhase. Do not change
ordinary player first-city behavior while fixing the Barbarian special case.


F167 / provisional KI#490 -- first-city preliminary BFC hard gates count plots before the established usability/ownership filter rejects them
-------------------------------------------------------------------------------------------------------------------------------------------
ROOT
SAS practical/commit 7a6be1b482c631ab46fe891e44fdedacd5e3871b
(2026-06-28, "Replace Settler/Found AI hardcoded enough good enough first-city plots
with a dynamic one") added XML-aware preliminary BFC counters. During the CityPlot
loop it now evaluates revealed non-home plots and increments, among other state:

    iGoodEnoughFirstCityBFCTiles
    iVeryBadBFCTiles
    iLowFoodLocationScore

BEFORE calling:

    isUsablePlot(...)

The latter is the established filter that decides whether the candidate may actually
count on the plot after existing-city radius ownership/sharing/steal/reservation rules.
If isUsablePlot returns false, normal found-value processing immediately `continue`s --
but the preliminary first-city counters have already been changed.

WHY EXISTING CITY RADIUS STATE IS PRESENT
When a city is founded, CvPlot::setPlotCity iterates its entire CityPlot radius and
increments each plot's `cityRadiusCount` / player city-radius count immediately. This is
not limited to plots already culturally owned or currently workable. Therefore a plot
inside a revealed rival city's BFC can participate in the preliminary SAS classification
while the later usability filter knows it belongs to an existing competing radius.

DETERMINISTIC REPRODUCTION
1. Evaluate a first-city candidate whose BFC overlaps the inner/radius claims of a
   revealed rival city.
2. Put a strong revealed Grass/resource or otherwise "good enough" tile in that rival
   claimed portion at a CityPlot index reached during the preliminary pass.
3. SAS_isGoodEnoughFirstCityBFCPlot counts it toward the configured hard minimum.
4. isUsablePlot then rejects that same plot because the candidate must not count on
   working/stealing it under the established overlap rules.
5. The unusable plot can nevertheless be the tile that raises
   `iGoodEnoughFirstCityBFCTiles + iUnrevealedTiles` to the minimum and prevents a hard
   return 0.

The reverse direction also exists: unusable rival/overlap plots can feed preliminary
very-bad or low-food counters and penalize/reject a candidate even though the main plot
evaluator intentionally discards those tiles.

CONSEQUENCE
The new gate is documented and named in terms of "usable" / "good enough" BFC slots,
but its hard acceptance decision can depend on plots the evaluator itself later says
are unusable. First-city acceptance can therefore depend on rival-overlap geometry in
the wrong direction and disagree internally within one loop iteration.

REPAIR DIRECTION
Run the ownership/sharing/reservation usability decision before mutating the preliminary
first-city counters, or factor the relevant non-yield usability predicate into the
preliminary classification. Take care not to lose intentional accounting for fogged
plots (`iUnrevealedTiles`) or the home plot.


F168 / provisional KI#491 -- same-type resource empire value is assigned to the first CityPlot copy, making found value depend on arbitrary BFC enumeration order
-----------------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
nonYieldBonusValue receives the BFC's partial `aiBonusCount`. Current SAS does:

    bool bSurplus = isBonusOwnedOrClaimedByFutureBFC(eBonus);
    if (paiBonusCount != NULL)
    {
        if ((*paiBonusCount)[eBonus] > 0)
            bSurplus = true;
        (*paiBonusCount)[eBonus]++;
    }
    int const iEffectiveHappyHealthValue = (bSurplus ? 0 : iHappyHealthValue);

Thus, when the empire does not already have reliable coverage, the FIRST BFC copy of a
bonus type receives the once-per-type empire health/happiness/building value; every
later copy receives zero for that component.

But each copy's non-yield result is also adjusted by that individual plot's culture
modifier. The first copy need not be the safest, most culturally reliable or otherwise
best representative of the resource type. CityPlot iteration order, not resource-state
semantics, chooses the representative.

DETERMINISTIC ROTATION REPRODUCTION
Take a candidate BFC with two otherwise ordinary Rice plots. Let one be a reliably
claimable 100%-culture-modifier copy and the other a contested/less reliable 50% copy.
Assume the empire has no existing reliable Rice. Under the current Granary-style
health/building setup, Rice's explicit once-per-type value can be about 300 before
culture adjustment.

Layout A: the 50% copy occupies the earlier CityPlot index. It receives the once-per-type
value and contributes about 150; the later 100% copy is surplus and contributes 0.

Layout B: rotate/rearrange the equivalent BFC so the 100% copy occupies the earlier
CityPlot index. It receives about 300; the later 50% copy gets 0.

The two local resource tile yields are still evaluated independently. The difference in
empire-wide resource value is created solely by enumeration order.

ANCESTRY
This is older than KI#178. Base AdvCiv's nonYieldBonusValue already documents
`aiBonusCount` as "only a partial count" and gives the first duplicate more value while
dividing later copies. The shape is present in the BBAI-derived/K-Mod found-value line
rather than in the supplied Civ4CE/BtS reference implementation's simpler bonus value.

SAS KI#178 (commit 6341d3d556a7f580e3664b8631d52446c2c7c4da) did not invent the
ordering dependency, but amplified it: later duplicates' explicit health/happiness
settlement value is now exactly zero rather than merely diminished.

CONSEQUENCE
Geometrically/resource-equivalent candidate sites can receive materially different
found values depending on which same-type copy happens to occupy the earlier CityPlot
index. The error is strongest when duplicate copies differ in culture reliability.

REPAIR DIRECTION
Compute the once-per-bonus-type empire value outside arbitrary per-plot first-hit
assignment, or accumulate/select the best eligible representative (e.g. highest
reliability) before applying the marginal value. Keep local tile yield values per plot.


C016-WIP02 NEGATIVE CONTROLS / OPEN QUESTIONS
---------------------------------------------
* A suspected tiny-lake Lighthouse/Harbor yield-credit defect was RETRACTED. In AdvCiv,
  CvPlot::isCoastalLand(-1) converts -1 to MIN_WATER_SIZE_FOR_OCEAN, so the relevant
  `bCoastal` condition already excludes tiny lakes. The initially proposed reproduction
  cannot occur in this codebase; KI#490 was therefore left unused until F167 above.
* AdvCiv commit 891273d300's old special-food arithmetic can apply a culture/reliability
  modifier twice to `iSpecialFoodPlus` (e.g. old `(4-2)*50% = 1` versus later
  `(4*50%-2)*50% = 0`). This is a real historical arithmetic regression in isolation,
  but SAS has disabled the principal special-yield/food-adjustment consumers. The
  surviving sea-production cap saturates before this value changes its result in the
  relevant range, and ordinary bonus sites already make iResourceValue positive for
  the remaining "nothing special" gate. No current consequential F169 was established.
* estimateImprovementProduction's retained warning about not checking water
  improvements was challenged. Current shipped water improvements are resource-specific
  Fishing/Whaling/Offshore types and resource improvement production is handled through
  the resource path; no ordinary non-resource water-improvement production root was
  established.
* adjustToBonusCount explicitly receives only the partial aiBonusCount built by the
  evaluator. Its mismatch with physical resource count remains suspicious, but the
  function's purpose is heuristic anti-greed shaping rather than a clear exact-count
  contract. Do not spend a finding without a deterministic wrong-state consequence.


C016-WIP02 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN
PHYSICAL CHECKPOINT: C016-WIP02
CURRENT FILE-016 NEW ROOTS: F158-F168 / provisional KI#481-KI#491
NEXT ROOT ID: F169 UNUSED

Resume with adjustToStartingChoices / adjustToCitiesPerArea / adjustToBonusCount and the
remaining deadlocked-bonus / starting-location / logging / evaluateWorkablePlot tail.
Challenge exact geographic/ownership assumptions and any stateful first-hit logic, but
keep the old special-food double-modifier regression as dormant unless a live SAS
consumer with a material result is demonstrated. Finish with an adversarial F158-Fxxx
closure pass before closing file 016.
===============================================================================


F169 / provisional KI#492 -- StartingPositionIteration workable-plot precompute runs a discarded full city-site evaluation for every candidate plot
-----------------------------------------------------------------------------------------------------------------------------------------
ROOT
StartingPositionIteration precomputes a map-wide workable-plot value table through:

    CitySiteEvaluator yieldEval(..., bNormalize=true);
    ...
    rYieldVal = yieldEval.evaluateWorkablePlot(kPlot);

CitySiteEvaluator::evaluateWorkablePlot first chooses a hypothetical city center and
then constructs:

    AIFoundValue foundVal(*pBestCityPlot, *this);

AIFoundValue's constructor unconditionally finishes with:

    m_iResult = evaluate();

Only after that complete city-site evaluation has run does the wrapper call the
separate:

    foundVal.evaluateWorkablePlot(kPlot)

and return that result. The constructor's m_iResult is never read in this path.

WHY THE FULL EVALUATION IS NOT INITIALIZATION FOR THE WORKABLE PATH
The mutable intermediate state produced by evaluate() -- aiCitySiteRadius,
abOwnCityRadius, abFlip, bFirstColony and iUnrevealedTiles -- is not consumed by
AIFoundValue::evaluateWorkablePlot or the helpers it calls. The fields that path does
need (player/team/era/candidate-area/coastal context, city count, etc.) are initialized
before the constructor invokes evaluate(). Thus the expensive full evaluation is pure
discarded work here rather than a hidden prerequisite.

REACHABILITY / SCALE
This is the normal StartingPositionIteration constructor path. SPI loops over essentially
the whole map, skipping only plots that are not potential city work, and performs this
wrapper call once per remaining plot to build yieldValues/yieldsPerArea. Each successful
hypothetical city center therefore triggers a full found-value pass over a BFC and its
associated overlap/resource/culture/health/modifier logic merely so that the object can
then run the independent workable-plot calculation.

ANCESTRY
The redundant shape was introduced with AdvCiv's StartingPositionIteration work in
practical 2280 / commit a70e4ce3a1 (2020-05-28). The initial implementation already
constructed AIFoundValue in CitySiteEvaluator::evaluateWorkablePlot and then ignored
get()/m_iResult. Base AdvCiv 1.14 and current SAS retain that contract.

CONSEQUENCE
Starting-position iteration pays thousands of avoidable full city-site evaluations on
ordinary maps. This is a pure performance defect: removing the discarded evaluation
should not change the returned workable-plot value.

REPAIR DIRECTION
Allow AIFoundValue construction without immediately running evaluate(), or factor the
immutable helper context used by evaluateWorkablePlot into a lightweight path. Preserve
normal CitySiteEvaluator::evaluate behavior while ensuring the SPI wrapper initializes
only the state its workable-plot calculation actually consumes.


F170 / provisional KI#493 -- first-colony landmass bonus reads unrevealed foreign city count despite AdvCiv's explicit no-cheat area-size adjustment
--------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
adjustToCitiesPerArea applies up to a x2 value modifier when:

    if (kArea.getNumCivCities() <= 0)

and describes this as the bonus for being the first civilization to colonize the
landmass. CvArea::getNumCivCities() is simply total cities minus Barbarian cities; it has
no team-visibility or city-deduction filter.

The same block then sizes that bonus from:

    kArea.getNumRevealedTiles(eTeam) // advc.031: Don't cheat

So the amount of land considered is deliberately knowledge-limited, while the preceding
question "does another civilization already have a city here?" still uses omniscient
map state.

REACHABILITY
adjustToCitiesPerArea is called unconditionally near the end of ordinary AIFoundValue::
evaluate. It is not restricted to all-seeing map generation. For a player with existing
cities evaluating its first colony on another sufficiently revealed continent, a rival
city can exist elsewhere in that same CvArea while remaining entirely unknown to the
player/team.

DETERMINISTIC INFORMATION-LEAK REPRODUCTION
Take two game states with identical evaluated site, revealed tiles, known cities and all
other information available to the evaluating team. In state A, no other civilization
has a city anywhere on that landmass. In state B, an unknown rival city exists beyond
fog on the same landmass.

State A enters the first-colony modifier and can receive as much as x2 on a large,
sufficiently revealed area. State B receives no such modifier solely because
getNumCivCities sees the hidden city. The AI's found value therefore changes based on
information it cannot know.

ANCESTRY
K-Mod practical/history commit 7cfc27cd59 changed the BtS pArea->getNumCities()==0
bonus into a size-scaled "first on a new continent" bonus, still using omniscient city
count. AdvCiv later changed the city test to civilian-only and, importantly, changed the
area-size input to getNumRevealedTiles(eTeam) with the explicit "Don't cheat" comment,
but did not knowledge-limit the city-existence half. Base AdvCiv 1.14 and SAS retain
the mixed contract.

CONSEQUENCE
A hidden rival colony can remove a very large expansion bonus and alter site ranking.
This is not merely imperfect forecasting; two states identical from the AI's point of
view can produce different found values.

REPAIR DIRECTION
Determine whether the area contains a known/deducible non-Barbarian rival city (plus
our/team cities as appropriate) using the same knowledge policy as the rest of
CitySiteEvaluator, rather than raw CvArea city totals. Keep the revealed-area sizing and
do not reveal a hidden city merely to decide whether the player is "first."


F171 / provisional KI#494 -- advc.181 visibility-hardening defeats isDeadlockedBonus's explicit adjacent-revealed fog allowance, causing false deadlock penalties
------------------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
isDeadlockedBonus deliberately allows a potential future city center around a visible
resource to proceed when either the center itself is revealed OR it is merely adjacent
to revealed terrain:

    if (!isRevealed(kOtherSite) &&
        !kOtherSite.isAdjacentRevealed(eTeam))
        continue;

The comment says this is because the evaluator needs enough information to know there is
land. But the very next test is now:

    kPlayer.canFound(kOtherSite, false, kSet.isAllSeeing())

For an ordinary non-all-seeing evaluation, bIgnoreFoW is false. Since advc.181,
CvPlot::canFound immediately returns false whenever the candidate center itself is not
revealed to eTeam. Consequently the `isAdjacentRevealed` alternative above can never
make an unrevealed center count as a viable site.

DETERMINISTIC FALSE-DEADLOCK REPRODUCTION
Let a visible bonus just outside the candidate city's BFC have two otherwise valid
future city centers within its radius:

1. one revealed center lies within MIN_CITY_RANGE of the candidate. canFound succeeds,
   so bNeverFound becomes false, but this site would be blocked by founding the current
   city;
2. a second same-area center lies farther than MIN_CITY_RANGE and is actual valid land,
   but its center tile is still fogged while adjacent terrain is revealed.

The explicit precheck admits center #2, but advc.181 canFound rejects it solely because
that center is unrevealed. The loop therefore ends with !bNeverFound && !bCanFound and
classifies the resource as deadlocked. Revealing only center #2, with no geography or
ownership change, makes canFound succeed and the deadlock disappear.

CONSEQUENCE
adjustToBonusCount applies:

    iValue = (2 * iValue) / (2 + iDeadLockCount);

so one false deadlock cuts the site to 2/3 value; multiple ones are even harsher. The
result depends on a visibility detail the helper's own adjacent-revealed policy was
written to tolerate.

ANCESTRY / REGRESSION
The adjacent-revealed allowance predates advc.181. AdvCiv practical 3597 / commit
c12bd6b877 ("Fix map knowledge leaked by unit action recommendations") changed this
call to the new fog-aware CvPlayer::canFound overload. The same commit added
CvPlot::canFound's explicit `!isRevealed(eTeam) -> false` guard. It fixed information
leaks elsewhere but unintentionally made the older adjacent-revealed branch in
isDeadlockedBonus ineffective. Base AdvCiv 1.14 and current SAS inherit the mismatch.

REPAIR DIRECTION
Give isDeadlockedBonus a no-cheat prospective-site predicate that preserves its intended
adjacent-revealed terrain allowance without consulting hidden ownership/cities. Simply
switching back to omniscient canFound would reintroduce the information leak advc.181
was designed to remove; the fix should explicitly separate known terrain/founding
legality from hidden-state restrictions.


C016-WIP03 NEGATIVE CONTROLS / OPEN QUESTIONS
---------------------------------------------
* The possible F170 about StartingPositionIteration using one representative player's
  civilization for the shared workable-plot table was rejected. SPI explicitly says the
  representative player is not irrelevant because starting tech state already affects
  valuation, and Base AdvCiv's pre-KI#178 workable-resource path was already
  player/civilization-sensitive through inherited bonus valuation. SAS KI#178 makes the
  approximation more visible through civilization-specific building effects, but no
  exact per-player SPI contract was established.
* The normalization-only hardcoded sea-food building approximation was checked against
  current BUILDINGCLASS_HARBOR data. The normal Harbor and Barbarian Harbor are the only
  shipped implementations and share the relevant +1 sea-food / ocean-size behavior; no
  current-data civ-specific mismatch was established.
* calculateBonusConnectionEra shares F160's plot/domain-blind improvement scan and can
  see the land Well/Combustion path for water Oil before the water Offshore Platform /
  Marine Technology path. Current Oil has no direct happy/health value and no shipped
  bonus-building happy/health effect that makes this timing result materially feed the
  active KI#187 non-yield settlement value, so keep this as a dormant sibling rather
  than spending another root unless current data makes the timing consequential.


C016-WIP03 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN
PHYSICAL CHECKPOINT: C016-WIP03
CURRENT FILE-016 NEW ROOTS: F158-F171 / provisional KI#481-KI#494
NEXT ROOT ID: F172 UNUSED

Resume after the deadlocked-bonus fog-contract trace. Continue the remaining logging /
evaluateWorkablePlot and custom-vs-Base adversarial pass, then re-challenge the earlier
isSiteValid/overlap/bad-tile ownership and no-cheat assumptions before deciding whether
file 016 can close. Prefer rejecting acknowledged heuristic approximations unless a
reachable wrong-state contract can be demonstrated.
===============================================================================


F172 / provisional KI#495 -- calculateCultureModifier leaks a completely unrevealed second rival city through area city totals
--------------------------------------------------------------------------------------------------------------------------------
ROOT
calculateCultureModifier starts from a revealed foreign-owned plot, but one of its
opposing-culture multipliers asks:

    p.getArea().getCitiesPerPlayer(getRevealedOwner(p)) > 1

when we have no cities in that area. CvArea::getCitiesPerPlayer is an omniscient total;
it does not filter the rival's cities by reveal state or AI_deduceCitySite.

DETERMINISTIC INFORMATION-LEAK REPRODUCTION
Hold the evaluated plot, its revealed owner, all visible culture clues and the entire
player-visible map state fixed. In state A the rival has exactly one city in that land
area. In state B it has a second city elsewhere in the same area, completely unknown to
the evaluating team. State B alone multiplies the foreign culture-rate estimate by 1.5.
The same contested tile therefore receives a different culture modifier solely from a
hidden city.

ANCESTRY
This particular `citiesPerPlayer > 1` clause was added during AdvCiv's 2019
AIFoundValue/CitySiteEvaluator refactor. The older inline culture block had the rival
capital special case but not this hidden second-city multiplier. Base AdvCiv 1.14 and
current SAS retain it.

CONSEQUENCE
Culture reliability is applied to plot yield and bonus valuation. A hidden remote city
can therefore alter settlement ranking for a known contested tile without providing any
new information to the AI.

REPAIR DIRECTION
Base this multiplier on known/deducible rival cities in the relevant area, or remove the
city-count proxy. Do not use raw CvArea city totals in a knowledge-limited evaluation.


F173 / provisional KI#496 -- KI#178 future-BFC bonus ownership check reads live actual plot owner after requiring only team-revealed resource knowledge
--------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
SAS KI#178's isBonusOwnedOrClaimedByFutureBFC deliberately reasons about resources that
are known to the player's team. It starts by requiring a team-revealed bonus, but then
uses:

    pLoopPlot->getOwner()

rather than the team-visible/revealed owner. Plot ownership can change while a plot is
fogged, whereas CvPlot maintains a separate revealed-owner value specifically for
knowledge-limited consumers.

DETERMINISTIC INFORMATION-LEAK REPRODUCTION
Take a known resource in the future BFC of one of our existing cities, leave the plot
under fog, and let rival culture gain or lose current ownership without the evaluating
team seeing the plot again. The actual-owner branch inside the helper changes even
though the team's remembered owner and resource knowledge do not.

CONSEQUENCE
The same hidden ownership transition can alter both the +250 unowned-resource diversity
nudge and the long-term health/happiness/building value path that asks whether the
empire already safely owns or claims that bonus.

ANCESTRY
This is SAS-specific to the KI#178 helper introduced in practical 6025 / commit
6341d3d556. The helper's comments frame the test in terms of known/claimed resources,
so omniscient ownership is not an intentional all-seeing contract.

REPAIR DIRECTION
Use the same revealed-owner helper/policy as the surrounding CitySiteEvaluator code.
Current ownership may be used only when all-seeing or currently visible under an
explicit knowledge rule.


F174 / provisional KI#497 -- raw city-radius counters leak undeduced hidden city existence throughout ordinary site evaluation
-----------------------------------------------------------------------------------------------------------------------------
ROOT
CvPlot::isCityRadius() and getCityRadiusCount() are global map-state counters. When a
city is founded, setPlotCity updates the radius count for the full BFC immediately;
there is no per-team visibility component. CitySiteEvaluator nevertheless reads those
raw counters in several ordinary non-all-seeing paths, including overlap/flip setup,
bad-tile rescue, isTooManyBadTiles, isUsablePlot and isDeadlockedBonus.

This is independent of revealed plot ownership. A city can remain entirely undeduced
while its BFC has already changed the raw city-radius counters consumed here.

DETERMINISTIC INFORMATION-LEAK REPRODUCTION
Hold all terrain, revealed ownership/culture and known city sites fixed. Add an enemy
city wholly under fog such that one or more already-revealed candidate-BFC plots fall in
its radius, but leave too few ownership clues for AI_deduceCitySite. The corresponding
isCityRadius/getCityRadiusCount reads change immediately. Depending on the path, the
plot can become taken/shared, stop rescuing a bad-site test, alter flip assumptions, or
make a nearby resource cease to count as deadlockable. None of those changes require
the AI to have learned that the city exists.

ANCESTRY / WHY THIS LOOKS LIKE AN OMISSION
AdvCiv practical/commit ca3e110fd5 is explicitly titled "AI city site eval respects fog
of war wrt. tile ownership." It systematically replaced raw owner/team reads with
revealed-state helpers and added/used AI_deduceCitySite, but raw city-radius counters in
the same evaluator survived that cleanup. Base AdvCiv 1.14 and SAS therefore retain a
second channel through which hidden city existence leaks back in.

CONSEQUENCE
Two states that are identical under the evaluator's intended map-knowledge contract can
produce different found values, including hard site rejection, solely because an
undeduced city exists in fog.

REPAIR DIRECTION
Introduce a knowledge-limited "known/deducible city radius" predicate for this evaluator
(or derive radius membership only from own/team cities plus known/deducible foreign
cities). Do not simply remove the radius checks; they serve valid overlap/sharing and
deadlock purposes once the city that creates the radius is knowable.


F175 / provisional KI#498 -- calculateCultureModifier reads exact hidden working-city identity, geometry and capital status even when that city is not deducible
-------------------------------------------------------------------------------------------------------------------------------------------------------
ROOT
Even if the raw city-radius leak in F174 is repaired, calculateCultureModifier has an
independent hidden-city channel:

    CvCity* pForeignCity = p.getWorkingCity();

It then uses that actual city's exact area relationship / position and capital status to
modify the assumed foreign culture pressure. The preceding site-usability logic can
explicitly decline to deduce a foreign city, but this routine receives the hidden
working-city pointer anyway.

ISOLATED REPRODUCTION
Use a revealed foreign-owned plot with too little surrounding ownership information for
AI_deduceCitySite to identify the underlying rival city. Keep the player's revealed map
state identical, but place the actual hidden working city adjacent to the plot in one
state and two plots away in another. The geometry-dependent culture-rate multiplier can
differ (including the x1.8 branch) despite the city location remaining unknowable.
Changing whether that hidden city is the capital provides another independent hidden
input.

ANCESTRY
AdvCiv's older culture-value work introduced a raw getWorkingCity()->isCapital() special
case, and later geometry refinements made the hidden pointer matter still more. The
subsequent fog-of-war cleanup that added revealed-owner and city-deduction safeguards
did not knowledge-limit this working-city access. Base AdvCiv 1.14 and SAS retain it.

CONSEQUENCE
Known rival ownership on a tile can reveal information about the exact hidden city that
works/claims it through settlement scoring, and can change plot/bonus value without a
corresponding observation.

REPAIR DIRECTION
Only use a foreign-city pointer when that city is revealed/deducible to the evaluating
team; otherwise use a conservative owner-only culture estimate that does not depend on
hidden city coordinates or capital status.


F176 / provisional KI#499 -- live plot-culture amounts and culture ranking are read through fog after ownership was made knowledge-limited
----------------------------------------------------------------------------------------------------------------------------------------
ROOT
calculateCultureModifier correctly obtains the foreign player from getRevealedOwner,
but then reads current map state directly:

    p.getCulture(ePlayer)
    p.getCulture(getRevealedOwner(p))

and related overlap logic uses culture-derived getSecondOwner / findHighestCulturePlayer
on the live plot. isUsablePlot requires only that the plot has ever been revealed; it
does not require active visibility.

CvPlot stores remembered owner/improvement/route information per team, but not a
per-team snapshot of exact plot culture. The UI likewise only exposes exact plot culture
percentages while the plot is actively visible. Plot culture can continue changing
while the tile is fogged.

DETERMINISTIC INFORMATION-LEAK REPRODUCTION
Reveal a contested rival-owned tile, move vision away, then let either side's culture
accumulate while the plot remains fogged. The team's remembered ownership/map state can
remain unchanged while the live getCulture values and possibly second/highest culture
player change. Re-evaluating a settlement site therefore changes its culture modifier
or flip inference solely from hidden culture growth.

ANCESTRY
The 2021 AdvCiv fog-of-war cleanup ca3e110fd5 explicitly replaced raw owner/team reads
inside calculateCultureModifier with revealed-owner helpers, but left the raw culture
amounts and culture-ranking queries untouched. This is therefore a close sibling of
F174/F175 in the same incomplete knowledge-boundary cleanup, not a SAS invention.

CONSEQUENCE
Yield and resource reliability on already-seen but currently fogged plots can track
exact hidden culture progress. Two player-indistinguishable states can receive different
found values.

REPAIR DIRECTION
For non-visible plots, avoid exact live culture ratios/rankings unless a deliberate
knowledge snapshot exists. Use revealed ownership plus a conservative/stale proxy; keep
raw culture for all-seeing or actively visible evaluation.


F177 / provisional KI#500 -- SAS runtime first-settler wrapper disables allSeeing but leaves starting-location-only omniscient helpers active
----------------------------------------------------------------------------------------------------------------------------------------
ROOT
SAS practical 6008 introduced SAS_evaluateFirstCityFoundValue specifically to retain
starting-capital weights while removing map-generation omniscience during live first
settler movement:

    CitySiteEvaluator kEvaluator(kOwner, -1, true);
    kEvaluator.setAllSeeing(false);

The commit message explicitly says runtime first-city evaluation should use only
revealed BFC terrain, bonuses and improvement potential, with all-seeing evaluation kept
for map generation. But `bStartingLoc=true` still activates older code that treats
"starting location" itself as an omniscient map-generation mode rather than consulting
isAllSeeing.

LIVE MANIFESTATION 1 -- hidden land-area boundary/topology (default configuration)
evaluate() unconditionally calls adjustToLandAreaBoundary whenever isStartingLoc().
With the recommended/default PASSABLE_AREAS=1, that routine scans raw plots out to range
4, tests hidden impassable peak chains / representative areas, and sums complete
CvArea::getNumTiles sizes. No reveal or allSeeing guard exists. Two first-settler
positions with identical revealed BFC information can therefore receive different
scores because of unrevealed mountain/land-area topology outside what the settler knows.

LIVE MANIFESTATION 2 -- hidden resources on Balanced/HIGH normalization games
AIFoundValue::getBonus contains an older normalization rule:

    if (kSet.isStartingLoc() || kSet.isNormalizing())
        if (getStartingPlotNormalizationLevel() > NORMALIZE_LOW)
            return p.getBonusType();

Balanced maps explicitly set the game's normalization level to HIGH during map
generation, and CvGame::initDiplomacy deliberately preserves HIGH rather than resetting
it. Thus the new runtime wrapper's setAllSeeing(false) still returns raw hidden bonuses
on Balanced games simply because isStartingLoc remains true.

ADDITIONAL START-MODE OMNISCIENCE
The first-city evaluateSeaAccess path also consults full-map starting-plot counts and
findBiggestArea(true)/complete water-area topology. This is the same mode-boundary root:
starting-capital weighting and map-generation knowledge are still coupled in inherited
helpers.

ANCESTRY / REGRESSION
The omniscient helpers predate SAS and were valid when starting-location evaluation was
an all-seeing map-generation operation. SAS practical 6008 / commit a5f183a489
introduced a new runtime use and assumed setAllSeeing(false) was sufficient to make that
mode knowledge-limited. It fixed the range-6 starting-surroundings adjustment explicitly,
but did not audit every other `isStartingLoc()` path.

CONSEQUENCE
The first Settler's new supposedly no-cheat roaming/site ranking can still react to
hidden map geometry in ordinary default games and hidden bonuses in HIGH/Balanced games.
That can directly alter which capital site it walks toward or founds.

REPAIR DIRECTION
Separate "use starting-capital weights" from "map-generation/normalization omniscience."
Gate genuinely omniscient start-only helpers on isAllSeeing (or an explicit map-generation
mode), not merely isStartingLoc. Audit every isStartingLoc branch reached by the runtime
wrapper rather than patching only the already-disabled starting-surroundings modifier.


F178 / provisional KI#501 -- foreign-city culture pressure uses the live hidden culture level after a city is merely known/deduced
----------------------------------------------------------------------------------------------------------------------------------
ROOT
The current SAS culture-pressure gate says "No-cheat preserved" and skips cities that
cannot be known/deduced, but for every admitted city it computes:

    int const r = pCity->getCultureLevel() + 3;

A CvCity stores only per-team revealed and espionage-visibility booleans; there is no
per-team remembered CultureLevel value. A city that was seen earlier can remain revealed
while currently fogged, and its actual culture level can rise in the meantime.

DETERMINISTIC REPRODUCTION
Take a known/deduced rival city under fog and a candidate at distance 3. With culture
level 1, r=4 and the current linear pressure is 32, below the configured SOFT=35 and thus
ignored. If the hidden city grows to culture level 2, r=5 and pressure becomes 46,
triggering a substantial soft multiplier (78% with current tuning). No new observation
of the city is required for that ranking change.

ANCESTRY
This exact knowledge blind spot is inherited rather than created by the SAS replacement:
Base AdvCiv's preceding foreign-proximity code also used
`pLoopCity->getCultureLevel()+3` during ordinary runtime evaluation, and K-Mod's
AI_deduceCitySite itself uses the city's current culture level while deciding whether
its site can be inferred. SAS practical 4962 replaced the old proximity formula with the
simpler pressure gate but retained the live exact level while explicitly describing the
new gate as no-cheat.

CONSEQUENCE
A known but fogged rival city can silently expand the range/strength of settlement
pressure and flip a candidate from unpenalized to strongly down-weighted (or eventually
hard rejected) based on hidden current culture progress.

REPAIR DIRECTION
For a city that is not currently visible, use only a knowledge-limited/stale culture
range estimate rather than live getCultureLevel. The same principle should be reviewed
inside AI_deduceCitySite, but that cross-file helper issue should not be multiplied into
another CitySiteEvaluator finding unless independently audited.


C016-WIP04 NEGATIVE CONTROLS / OPEN QUESTIONS
---------------------------------------------
* F172-F178 are intentionally split by independent hidden-state channels and repair
  surfaces: area city totals, actual owner, raw city-radius counters, hidden working-city
  pointer, live plot culture, runtime starting-mode omniscience, and live city culture
  level respectively. Symptoms that share one of those roots are grouped rather than
  assigned extra IDs.
* F177 absorbs the newly found first-city evaluateSeaAccess full-map starting-plot /
  water-area queries because they arise from the same practical-6008 mode split; do not
  spend another root merely for each `isStartingLoc()` helper that remained omniscient.
* The shared StartingPositionIteration representative-player concern remains rejected
  from WIP03: SPI was already player-sensitive before SAS KI#178 and does not promise an
  exact per-civilization table.
* The countBadTiles diagnostic still appears to print member iUnrevealedTiles before the
  caller stores the freshly computed local iUnrevealed value. This is a real tiny
  logging defect inherited from Base AdvCiv, but remains unnumbered while gameplay /
  knowledge-boundary roots are still being audited.
* NEXT OPEN SEAM: ordinary non-all-seeing evaluation still uses complete CvArea topology
  and getNumTiles in a few non-starting paths (notably inaccessible-resource adjustment,
  small-area bad-tile logic and canal/waterbody valuation). Determine whether these are
  intended strategic approximations or additional hidden-map leaks before spending F179.


C016-WIP04 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN
PHYSICAL CHECKPOINT: C016-WIP04
CURRENT FILE-016 NEW ROOTS: F158-F178 / provisional KI#481-KI#501
NEXT ROOT ID: F179 UNUSED

Resume with the ordinary-runtime complete-area/topology audit (`bEasyAccess`,
nonYieldBonusValue getNumTiles, isTooManyBadTiles and evaluateSeaAccess canal sizes).
Then re-run the remaining custom-vs-Base tail and adversarial F158-Fxxx closure pass.
Keep map-generation/all-seeing uses separate from ordinary AI knowledge, and prefer a
single root for repeated manifestations of one missing knowledge boundary.
===============================================================================


===============================================================================
C016-WIP05 APPEND -- 2026-08-26
CitySiteEvaluator.cpp continued adversarial tail / data-contract pass
===============================================================================

F179 / provisional KI#502 -- ordinary runtime site evaluation reads complete hidden CvArea topology and size
----------------------------------------------------------------------------------------------------------
ROOT
Several ordinary non-all-seeing found-value paths use the complete CvArea graph or
getNumTiles rather than team-revealed geography. Unlike the map-generation-only
starting-location omniscience grouped under F177, these branches also run while AI
players evaluate later city sites with fog of war still active.

The codebase itself establishes a contrary knowledge contract nearby: settlement-area
logic sometimes substitutes getNumRevealedTiles(eTeam), explicitly with comments such
as "Don't cheat." CvArea::getNumTiles and area connectivity have no per-team remembered
or revealed-state equivalent behind them.

CLEAN DETERMINISTIC REPRODUCTION -- CANAL VALUE
Use an ordinary later-city candidate touching two revealed water tiles / water areas.
Keep every plot currently revealed to the evaluating team unchanged, but extend one or
both water bodies only through unrevealed terrain. evaluateSeaAccess tests the complete
sizes of those CvArea objects against MIN_WATER_SIZE_FOR_OCEAN. Merely making the hidden
continuations large enough can turn the canal branch on and add substantial found value
(up to 270 with the current formula), although the player has learned nothing new.

OTHER LIVE MANIFESTATIONS OF THE SAME ROOT
* An inaccessible resource's long-term non-yield value uses the complete size of its
  other land area and changes between the 45% and 60% cases around the <=2-tile test.
* Water-resource bEasyAccess can become true because the globally connected water area
  contains one of our cities, even if the channel establishing that connection is still
  wholly under fog.
* The inherited bad-tile rescue / small-area logic also branches on full candidate-area
  size rather than known area extent.

ANCESTRY
An area-size dependency is already present in Firaxis BtS found-value logic; K-Mod and
AdvCiv add further full-area / water-connectivity heuristics. Base AdvCiv 1.14 and SAS
therefore inherit a family of ordinary-runtime hidden-topology inputs rather than one
single recent SAS regression.

CONSEQUENCE
Two maps that are identical under the evaluating team's map knowledge can rank the same
city site differently solely because unrevealed terrain completes, enlarges or connects
an area behind fog.

REPAIR DIRECTION
For ordinary non-all-seeing evaluation, derive area-size / connectivity proxies from
team-revealed plots where the heuristic is intended to model player knowledge. Preserve
raw CvArea topology for map-generation/all-seeing evaluation. Treat the repeated size
and connectivity symptoms as one knowledge-boundary root rather than patching only the
canal formula.


F180 / provisional KI#503 -- practical 5976 low-food bonus-improvement helper omits already-available irrigated Farm food
-------------------------------------------------------------------------------------------------------------------------
ROOT
Practical 5976 introduced CvPlot::SAS_getBonusImprovementFoodChange so the new dynamic
low-food-BFC score could include food from the resource's bonus-specific improvement.
The helper manually sums:

    base improvement food
    + bonus-specific improvement food
    + river-side improvement food
    + hill improvement food
    + feature-removal terrain/feature adjustments

but it never adds CvImprovementInfo::getIrrigatedYieldChange.

That differs from Civ4's normal CvPlot::calculateImprovementYieldChange, which adds the
improvement's irrigated yield whenever isIrrigationAvailable() is true.

CURRENT SHIPPED DATA / DETERMINISTIC REPRODUCTION
IMPROVEMENT_FARM currently has:

    IrrigatedYieldChange: +1 food
    bRequiresIrrigation: 1

and is the bonus-trade improvement for Maize (+2 improvement food), Rice (+1 food,
+1 production) and Wheat (+2 food). Put any of those visible resources on an already
irrigable flat plot -- most simply a riverside / otherwise fresh-water plot. Civ4's
actual Farm yield receives the +1 irrigated food immediately, but
SAS_getBonusImprovementFoodChange omits it.

For example, a fresh-water Maize Farm contributes one less potential food to
SAS_getLowFoodEnvironmentScore than the same improvement contributes through
calculateImprovementYieldChange. This does not depend on assuming a future irrigation
chain or future Civil Service; irrigation is already available in the reproduction.

INTERNAL INCONSISTENCY
SAS_isVeryBadBFCPlot evaluates improvement potential through
calculateImprovementYieldChange and therefore can count this same already-available +1
irrigated Farm food. The newer low-food scorer evaluates the same plot through the
manual helper and misses it. A single plot can therefore have two different notions of
its current improvement food inside the same first-city preliminary pass.

ANCESTRY / INTENT
This begins in SAS practical 5976 / commit f3137a536b (2026-06-28), whose commit message
says the hardcoded low-food heuristic is being replaced by a dynamic one based on actual
yields so that XML changes and food bonuses are reflected automatically. The helper was
introduced in that commit with the irrigated-yield omission already present.

The maintained GlobalDefines comment likewise defines the low-food score in terms of
natural food, visible bonus food and "food from its bonus-specific improvement." Farm's
current +1 irrigated food is part of that improvement's actual yield when irrigation is
available.

CONSEQUENCE
Already-irrigable Maize/Rice/Wheat plots are understated by exactly 1 potential food.
Because each missing food increases iLowFoodLocationScore by 1 and the non-coastal gate
penalizes totals above the configured threshold (currently 5, with -75 base value), the
omission can move an otherwise equivalent candidate across the low-food penalty boundary
or make that penalty stronger.

REPAIR DIRECTION
At minimum, include kImprovement.getIrrigatedYieldChange(YIELD_FOOD) when
isIrrigationAvailable() is true. If the intended contract is later broadened from
currently available yield to fully long-term potential, decide separately whether
canHavePotentialIrrigation should also be credited; that broader design choice is not
needed to fix the deterministic current-irrigation mismatch.


C016-WIP05 NEGATIVE CONTROLS / TAIL CLOSURES SO FAR
---------------------------------------------------
* calculateBonusConnectionEra has the same plot/domain-blind Well vs Offshore Platform
  sibling already recorded under F160. Do not spend another root ID for it.
* The shared SPI representative-player/civilization concern remains rejected. Base
  AdvCiv's workable-plot valuation was already player-sensitive, and SPI explicitly
  accepts a representative player rather than promising a per-civilization exact table.
* MinRivalRange's raw hidden-unit scan is not an ordinary live-settler leak in current
  internal use. Its non-default use is Barbarian-city placement / matching logging,
  where the spacing condition is a global generation rule; live AI site evaluation uses
  the default -1.
* The KI#178 building-effect tech-horizon filter currently covers the tech prerequisite
  forms exposed by CvBuildingInfo / CvSpecialBuildingInfo; no shipped prerequisite path
  was found that bypasses it.
* BONUS_DRAMA and BONUS_MUSIC lack ordinary non-Fort plot trade improvements, but they
  are manufactured/non-map resources in current data. They do not establish a runtime
  violation of calculateBonusConnectionEra's effective plot-bonus input domain.
* Generic no-bonus water plots do not become falsely improvable through resource water
  improvements: canHaveImprovement rejects Fishing Boats / Whaling Boats / Offshore
  Platform without their required bonus.
* The strengthened SAS home-resource penalty and the later inherited starting-on-resource
  multiplier are intentionally layered. Base AdvCiv already applied both kinds of
  penalty, and KI#25 deliberately made SAS avoid settling directly on resources more
  aggressively. No clean double-charge regression was established.
* A normalization-only water-access concern remains too dependent on SPI's deliberately
  approximate long-horizon contract to promote.
* The inherited countBadTiles diagnostic still deterministically logs member
  iUnrevealedTiles before the caller copies the freshly computed local iUnrevealed into
  it. It remains a valid tiny logging defect held in reserve while the gameplay tail is
  still being closed.


C016-WIP05 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN
PHYSICAL CHECKPOINT: C016-WIP05
CURRENT FILE-016 NEW ROOTS: F158-F180 / provisional KI#481-KI#503
NEXT ROOT ID: F181 UNUSED

Resume with the remaining author-warning / assertion / plot-quality / logging tail and
then perform a full adversarial F158-F180 closure pass. Recheck any apparent mismatch
against shipped XML and actual helper semantics before promoting it. If no additional
gameplay/data-contract root survives, decide whether the deterministic countBadTiles
logging bug merits F181 before closing file 016.
===============================================================================


===============================================================================
C016-WIP06 APPEND -- 2026-08-26
CitySiteEvaluator.cpp final inherited production-proxy / diagnostic pass
===============================================================================

F181 / provisional KI#504 -- inherited AdvCiv medium-term production estimate mistakes "already owns one" for "can build it"
---------------------------------------------------------------------------------------------------------------------------
ROOT
AIFoundValue::estimateImprovementProduction is documented as estimating how much
production an unimproved non-resource plot might gain in the medium term. Once the
player has more than two cities, however, its first filter for every improvement is:

    if (kPlayer.getImprovementCount(eLoopImprovement) <= 0)
        continue;

The introducing AdvCiv comment explicitly calls this a fast way to check whether the
player can build the improvement. Existing empire improvement count is not a capability
test: an improvement may be newly buildable before the first copy exists, and a retained
or captured copy may exist when its build prerequisite is not currently available.

CURRENT SHIPPED DATA / DETERMINISTIC REPRODUCTION
Use an empire with at least three cities that has just acquired TECH_MINING and has not
yet completed its first Mine. IMPROVEMENT_MINE has +2 production and ordinary valid
hill plots can now legally receive it, but getImprovementCount(IMPROVEMENT_MINE) is 0,
so estimateImprovementProduction skips Mine entirely. Completing the first unrelated
Mine anywhere in the empire makes the same candidate plots suddenly gain the medium-term
Mine production estimate despite no change to the candidate terrain or the player's
technology.

The opposite error is possible in scenarios/captured territory: possessing a retained
improvement can make this first proxy pass even though ownership of that improvement does
not establish that the player currently has its matching Build/tech capability. The later
p.canHaveImprovement check tests plot legality, not build-tech availability, so it does
not repair the capability mismatch.

ANCESTRY
This proxy was introduced by AdvCiv practical 1275 / commit 71fa4eef1a (v0.90rc found-
value retuning). Its introducing comment already acknowledges that the check is "not a
perfectly safe way" to determine buildability. The 2019 AIFoundValue extraction moved it
into estimateImprovementProduction unchanged. Base AdvCiv 1.14 and current SAS retain it.

CONSEQUENCE
Future-site production value can depend on whether the empire happens already to own one
copy of an improvement rather than on whether that improvement can actually be built.
The first copy of a newly unlocked improvement can therefore produce a discontinuous,
unrelated jump in settlement ranking; retained/captured improvements can cause the
opposite false-positive capability assumption.

REPAIR DIRECTION
Replace the getImprovementCount capability proxy with a cheap test for at least one
matching currently available Build/tech, then retain p.canHaveImprovement for the
candidate plot's terrain/feature legality. Cache/build a per-player set of currently
buildable improvement types if the old loop-cost concern still matters.


F182 / provisional KI#505 -- inherited AdvCiv AIFoundValue refactor logs zero unrevealed tiles from countBadTiles
---------------------------------------------------------------------------------------------------------------
ROOT
countBadTiles receives the current unrevealed count through its local reference parameter
`iUnrevealed`, increments that value during the BFC scan, and then logs:

    logBBAI("Bad tiles: %d known bad, %d unrevealed",
            iBadTiles, iUnrevealedTiles)

The second argument is the AIFoundValue member, not the freshly computed local/reference
value. The member is initialized to 0 in the constructor. In evaluate(), the caller only
copies the returned local value into the member after countBadTiles has already finished
and emitted its log:

    int iUnrevealed = 0;
    iBadTiles = countBadTiles(..., iUnrevealed, ...);
    iUnrevealedTiles = iUnrevealed;

DETERMINISTIC REPRODUCTION
Enable found-value logging and evaluate any candidate whose BFC contains at least one
unrevealed plot. Inside countBadTiles, the `iUnrevealed > 0` condition correctly selects
the two-number logging branch, but the printed unrevealed value is still the pre-call
member value 0. Thus e.g. three truly unrevealed BFC tiles are logged as:

    Bad tiles: <known> known bad, 0 unrevealed

The gameplay count returned through the reference and later stored in iUnrevealedTiles is
correct; this finding is diagnostic-only.

ANCESTRY
The bad argument is present in the extracted countBadTiles added by AdvCiv commit
078fc9bd7c (2019-12-15, "Move AI_foundValue computation into a new class"). Base AdvCiv
1.14 and current SAS retain the same line.

CONSEQUENCE
Found-value diagnostics systematically lie about unrevealed BFC count exactly in the
cases where the special fog/first-colony logic is most important. This can mislead manual
AI-site debugging even though settlement scoring itself receives the correct count.

REPAIR DIRECTION
Log the local/reference `iUnrevealed` value inside countBadTiles. The member should remain
the post-call stored value used by later evaluation stages.


C016-WIP06 NEGATIVE CONTROLS / FINAL CLOSURE PASS STATUS
--------------------------------------------------------
* F180 remains narrowly scoped to the missing already-active irrigated Farm food. The
  rest of SAS_getBonusImprovementFoodChange's feature-removal arithmetic matches the
  way its caller combines it with calculateNatureYield for current shipped data.
* SAS_getBonusSpecificBuild's domain cache does not create another current same-domain
  multi-improvement choice root; current resource-improvement data has no shipped
  same-domain alternative requiring a separate selection fix. Water Oil remains covered
  by F160's older plot/domain-blind AIFoundValue scan.
* calculateBonusConnectionEra's omission of a single explicit route-tech prerequisite
  is not promoted. Connection can legitimately come from a city, river/coastal network,
  or a later route, so no deterministic one-tech repair follows from that observation.
* The inherited calculateBuildingYields +1-water-food approximation still matches the
  current civilization-specific Harbor-class implementations and their ocean-size gate;
  the earlier tiny-lake hypothesis remains rejected.
* estimateImprovementProduction's water TODO remains a non-finding for shipped data:
  current water improvements are bonus-specific and bonus production/yields are handled
  on the separate resource path.
* isRemovableFeature's deferred feature-production estimate can intentionally value
  eventual chopping even before current removal capability; do not merge that heuristic
  with F158's distinct broken current-research eligibility test.


C016-WIP06 INTERIM CURSOR
-------------------------
COMPLETED FILES: 15 / 169
FILE 016: CvGameCoreDLL/CitySiteEvaluator.cpp (4,595 lines) -- OPEN, FINAL CLOSURE PASS
PHYSICAL CHECKPOINT: C016-WIP06
CURRENT FILE-016 NEW ROOTS: F158-F182 / provisional KI#481-KI#505
NEXT ROOT ID: F183 UNUSED

Resume with one final adversarial F158-F182 closure pass: challenge duplicated roots,
repair boundaries, remaining active asserts/TODOs, and current shipped XML semantics.
If no additional independent gameplay/data-contract defect survives, close file 016 at
the next durable checkpoint rather than manufacturing F183.
===============================================================================


===============================================================================
DURABLE CHECKPOINT C017 -- FILE 016 CLOSED
2026-08-26
===============================================================================

FILE CLOSED
-----------
016 CvGameCoreDLL/CitySiteEvaluator.cpp -- 4,595 lines

FINAL FILE-016 FINDING RANGE
----------------------------
F158-F182 / provisional KI#481-KI#505
F183 remains UNUSED.

FINAL ROOT LEDGER
-----------------
F158 / KI#481
    Inherited AdvCiv isRemovableFeature current-research regression: after the
    AIFoundValue refactor, the same current research must equal both distinct
    build and feature-removal tech prerequisites instead of testing each
    prerequisite independently as known-or-currently-researched.

F159 / KI#482
    Inherited AdvCiv regression makes K-Mod's explicit other-island starting-
    surroundings badness branch unreachable by restoring an outer same-area /
    water filter that K-Mod had deliberately removed.

F160 / KI#483
    Inherited Warlords/BtS bonus-improvement selection is blind to the actual
    plot domain/legality; water Oil can inherit land Well timing/yields and the
    same root also contaminates related connection-era reasoning.

F161 / KI#484
    SAS future-BFC resource helper filters rival contesting cities against the
    unrelated candidate site's land area, so cross-area evaluation can make an
    existing contested resource look safely claimed.

F162 / KI#485
    The same SAS future-BFC helper returns on the first matching copy, making an
    empire-wide existential resource-coverage question depend on city/BFC
    iteration order.

F163 / KI#486
    The future-BFC ownership helper uses available-bonus state that includes
    temporary imports despite the feature's explicit contract that imported
    resources are not permanent owned coverage.

F164 / KI#487
    Rival culture-pressure gating discards real cross-water pressure solely
    because the rival city tile is in another land area, despite Civ4 culture
    being able to reach nearby plots across a narrow channel.

F165 / KI#488
    SAS cautious-health scoring drops Forest/Jungle health whenever a plot has
    a visible bonus, even though many shipped resources legitimately coexist
    with those features and the health effect remains real.

F166 / KI#489
    SAS raw zero-city start-phase gates accidentally reintroduce a special
    first-Barbarian-city state that AdvCiv explicitly normalizes away.

F167 / KI#490
    Practical-5978 preliminary first-city BFC hard gates count good/bad/low-food
    plots before the established usability/ownership filter can reject those
    same plots, allowing unusable rival/reserved tiles to affect acceptance.

F168 / KI#491
    Same-type resource empire value is awarded to the first CityPlot copy and
    suppressed for later copies, so different culture reliability on duplicate
    resources makes found value depend on arbitrary BFC enumeration order.

F169 / KI#492
    StartingPositionIteration's workable-plot precompute constructs AIFoundValue
    for every candidate plot; the constructor performs a complete city-site
    evaluate whose result/state is then discarded before the independent
    evaluateWorkablePlot path runs.

F170 / KI#493
    First-colony landmass value uses complete CvArea non-Barbarian city count,
    so a wholly unrevealed rival colony can remove the "first civ here" bonus
    despite the nearby area-size term explicitly using revealed tiles to avoid
    cheating.

F171 / KI#494
    advc.181's later canFound fog hardening defeats isDeadlockedBonus's explicit
    adjacent-revealed fog allowance, allowing a valid but still fogged alternate
    city site to be rejected and a resource falsely classified as deadlocked.

F172 / KI#495
    calculateCultureModifier reads complete rival city totals for an area; an
    unknown second rival city can apply the 1.5 opposing-culture multiplier in
    an otherwise identical player-visible state.

F173 / KI#496
    SAS KI#178 first requires a team-revealed bonus, then reads the plot's live
    actual owner rather than revealed owner; resource claim/contest value can
    therefore change from ownership movement entirely under fog.

F174 / KI#497
    Raw isCityRadius/getCityRadiusCount state leaks undeduced hidden city
    existence through multiple ordinary site-evaluation paths after AdvCiv's
    ownership fog-of-war cleanup converted neighboring owner reads but left the
    city-radius counters knowledge-blind.

F175 / KI#498
    calculateCultureModifier separately reads the exact hidden working-city
    pointer, geometry, area and capital status even when that city is not
    deducible; repairing F174 alone does not remove this channel.

F176 / KI#499
    Live CvPlot culture amounts / second-owner / highest-culture ranking are
    consumed on merely revealed but currently fogged plots, so hidden culture
    growth changes found value without updated player information.

F177 / KI#500
    SAS runtime first-settler evaluation disables allSeeing but keeps
    isStartingLoc semantics; multiple starting-only helpers still use that mode
    as permission for map-generation omniscience (normalization-level hidden
    bonuses, full land-boundary topology, starting-plot/water-area queries).

F178 / KI#501
    Foreign-city pressure uses a known/deduced city's current hidden
    getCultureLevel; culture growth under fog changes the pressure radius and
    strength without a remembered/stale per-team culture-level state.

F179 / KI#502
    Ordinary later-city evaluation reads complete hidden CvArea topology/size
    for canal value, access and small-area heuristics. Two identical revealed
    maps can rank a site differently based only on unrevealed area continuation
    or connectivity.

F180 / KI#503
    Practical-5976 SAS_getBonusImprovementFoodChange manually reconstructs
    bonus-improvement food but omits Farm's already-active IrrigatedYieldChange;
    currently irrigable Maize/Rice/Wheat can be understated by 1 food in the
    first-city low-food environment score.

F181 / KI#504
    Inherited AdvCiv medium-term production estimation uses
    getImprovementCount>0 as a proxy for whether an improvement can be built.
    Newly unlocked improvements are ignored until the first copy exists, while
    retained/captured copies can create the opposite false capability signal.

F182 / KI#505
    Inherited AdvCiv AIFoundValue refactor logs member iUnrevealedTiles from
    inside countBadTiles before the caller stores the freshly computed local
    unrevealed count, so positive unrevealed counts are deterministically logged
    as zero. Gameplay data is correct; the defect is diagnostic-only.


WHY FILE 016 IS CLOSED
----------------------
The closure pass covered the complete active CitySiteEvaluator/AIFoundValue surface,
including the current SAS additions in CvPlot that directly feed the new first-city
plot-quality helpers. The audit specifically challenged:

* constructor/settings modes and normalizing/starting/runtime-first-settler boundaries;
* site validity, overlap, city-radius ownership/sharing/stealing and planned-site state;
* bad/unrevealed/inner-ring tile accounting and first-colony selection;
* feature removal, chop production and current/future tech eligibility;
* bonus discovery, improvement selection, connection timing and improvement yields;
* nature/special/building/resource health/happiness valuation;
* KI#178 future-BFC ownership, duplicate handling and civilization building effects;
* culture claim/flip/foreign-city geometry and fog-of-war knowledge boundaries;
* first-city low-food, very-bad, minimum-good-slot and cautious-health gates;
* home-resource settlement penalties and current shipped bonus/improvement XML;
* production potential, fresh water, sea access, canals and complete-area heuristics;
* normalization workable-plot precomputation and StartingPositionIteration contract;
* starting surroundings/choices/land-boundary normalization helpers;
* city-per-area, resource-count and deadlocked-resource adjustments;
* logging, assertions, TODO/warning comments and diagnostic-only code paths;
* Base AdvCiv/K-Mod/Firaxis ancestry and current SAS commit-diff history where needed;
* current shipped XML legality and negative controls for every promoted data-contract
  claim rather than relying only on generic mod-mod possibilities.

The final adversarial reread rejected or bounded several plausible but unsupported
follow-ups:

* the shared SPI representative-player table was already player-sensitive in Base
  AdvCiv and does not promise exact per-civilization valuation;
* the apparent tiny-lake Lighthouse/Harbor mismatch was false because AdvCiv's
  isCoastalLand(-1) resolves to MIN_WATER_SIZE_FOR_OCEAN;
* the historical double special-food culture modifier remains mostly dormant after SAS
  disabled its meaningful consumers and therefore was not assigned an ID;
* calculateBonusConnectionEra has no single universally required route-tech input because
  city/river/coast/route connectivity are alternative mechanisms;
* current water-improvement data does not make estimateImprovementProduction's water TODO
  a live non-resource production omission;
* the home-resource penalty's negative coefficients make its `iValue += r` sign correct,
  while bonus-specific-only lost-yield weighting is an explicit tuning choice rather than
  an accidental omission;
* the remaining logPlot "soon" wording/assertion oddity under hidden starting bonuses is
  downstream of F177's runtime-starting-mode leak rather than an independent gameplay
  root;
* no additional independent active assert/TODO/data-contract defect survived current XML
  and helper-semantics checking.

With those negative controls recorded, continuing to mine CitySiteEvaluator.cpp would now
be more likely to split existing roots or elevate tuning disagreements than to discover a
new independent correctness issue. F183 is therefore deliberately left unused.


PROGRESS AFTER FILE-016 CLOSURE
-------------------------------
Files complete: 16 / 169
Files open: 0 / 169
Last completed file: 016 CvGameCoreDLL/CitySiteEvaluator.cpp -- 4,595 lines

NEXT QUEUED FILE:

    017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines

Following files by the current line-count queue begin:

    018 CvGameCoreDLL/CvGameInterface.cpp -- 2,965 lines
    019 CvGameCoreDLL/CvDLLButtonPopup.cpp -- 2,814 lines
    020 CvGameCoreDLL/CvInfo_Unit.cpp -- 2,599 lines

C017 is a durable boundary checkpoint. Start UWAIAgent.cpp from the reconciled 15:08
source on the next pass. Do not carry CitySiteEvaluator hypotheses forward by analogy;
new findings require their own live caller/data proof.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP01 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source: reconciled AdvCiv-SAS light source 2026-08-26 15:08
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017
New finding range in this append: F183-F184 / provisional KI#506-KI#507
Next finding number: F185 UNUSED

F183 / provisional KI#506
-------------------------
TITLE
    reviewWarPlans retains superseded cross-pass state when a plan change forces
    the deliberate retry loop.

STATUS
    CONFIRMED. Inherited UWAI/AdvCiv defect; present before the 2021 UWAI style
    rewrite and retained by Base AdvCiv 1.14 and current SAS.

ROOT
    reviewWarPlans intentionally repeats its complete review whenever reviewPlan
    changes a target plan, because that change can alter evaluation of the other
    plans. However, state intended to describe the final reviewed plan set is
    initialized before the do/while retry loop and then only accumulated:

        bool bScheme = true;
        bool bAllNaval = true, bAnyNaval = false;
        bool bAllLand = true, bAnyLand = false;
        do
        {
            ...
            if (iU < -100 && kAgent.isAtWar(eTarget))
                bScheme = false;
            ...
            if (aPlans[i].bNaval)
            {
                bAnyNaval = true;
                bAllLand = false;
            }
            else
            {
                bAnyLand = true;
                bAllNaval = false;
            }
        } while (bPlanChanged);

    abTargetDone deliberately removes a target after that target changes, but the
    classifications and bad-war state learned from earlier, superseded passes are
    never removed.

REACHABLE MULTI-PASS SHAPE
    Pass 1 can review target A successfully and classify it as land, then target B
    changes and triggers a retry. The retry exists precisely because B's change can
    alter the simulated context and therefore A's utility/plan character. On pass 2,
    A can then itself be canceled/switched and become done. A is omitted thereafter,
    but its pass-1 land bit remains latched. If the final surviving positive plans are
    all naval, bAllNaval is still false and the final alignAreaAI(true) call is lost.

    The same root is even more direct for bScheme: pass 1 can observe a current war
    below -100 utility and set bScheme=false. If reviewPlan then succeeds in ending
    that war and the review repeats, the final plan set can contain no disastrous
    current war at all, yet bScheme remains false and doWar skips scheme() for the
    whole turn.

CONSEQUENCE
    * AreaAI alignment can describe a superseded intermediate review pass rather
      than the final surviving plan set, despite the trailing source comment saying
      this review is the "last word on AreaAI".
    * A war that is ended during review can leave stale bScheme=false behind and
      suppress all new-war scheming until the next team turn.

ANCESTRY
    The pre-2021 UWAI implementation already initializes allNaval/anyNaval and
    allLand/anyLand outside the retry loop. AdvCiv's 2021 commit 4ae382e5db
    ("Revised all code in the UWAI component") mechanically retained that placement;
    current Base AdvCiv 1.14 and SAS still do so. Current SAS also retains bScheme
    outside the loop, exposing the same stale-pass contract on whether scheming is
    allowed.

REPAIR DIRECTION
    Recompute all state that is meant to describe the current/final pass at the start
    of each do/while iteration. The simplest correctness-first repair is to use
    per-pass bScheme/bAllNaval/bAnyNaval/bAllLand/bAnyLand values and preserve only
    the values from the final no-change pass. Do not merely clear the four AreaAI
    flags while leaving bScheme monotonic across superseded passes.

NEGATIVE CONTROLS
    This is not based on a hypothetical retry: reviewWarPlans explicitly changes a
    plan, marks that target done, logs "repeating review", and re-runs WarEvaluator
    for the remaining plans. Nor does the bug require the changed target itself to be
    classified; a plan classified before the changed target can become invalid on a
    later pass, which is exactly the dependency the retry is meant to handle.


F184 / provisional KI#507
-------------------------
TITLE
    Brokered-peace low-score relaxation is dead because makePeaceTrade immediately
    returns DENIAL_VICTORY after setting bNoDenial=true.

STATUS
    CONFIRMED. AdvCiv regression introduced by ca3cdbcdce (2020-05-17), retained by
    Base AdvCiv 1.14 and current SAS.

ROOT
    makePeaceTrade contains an explicit low-score exception when the enemy already
    wants peace but our own reluctance is moderately high:

        if (iEnemyReluct <= 0)
        {
            if (iAgentReluct < 55)
                bNoDenial = true;
            else
            {
                ...
                if (rGameEra > 0 && rScoreRatio < ...)
                {
                    // We're not doing well in score; the broker might be doing much better.
                    if (iAgentReluct < 70)
                        bNoDenial = true;
                    else return DENIAL_TOO_MUCH;
                }
                return DENIAL_VICTORY;
            }
        }
        ...
        if (bNoDenial)
            return NO_DENIAL;

    For the intended low-score case with iAgentReluct in [55,69], the function sets
    bNoDenial=true and then unconditionally returns DENIAL_VICTORY before the later
    NO_DENIAL branch can run.

INTRODUCING-COMMIT PROOF
    AdvCiv commit ca3cdbcdce, dated 2020-05-17 and titled
    "UWAI: Tweak brokered-peace denial reason", introduced this exact branch. Its
    commit message says:

        Civs that are clearly not winning the game shouldn't respond
        "we'd rather win this game".

    The diff added bNoDenial plus the low-score test, including the assignment
    bNoDenial=true for reluctance below 70, but also added the unconditional
    DENIAL_VICTORY return immediately after the test. The resulting control flow
    therefore contradicts the stated purpose of the introducing commit.

DETERMINISTIC REPRODUCTION STATE
    During brokered peace:
    * enemy reluctance <= 0;
    * agent reluctance is 55 through 69;
    * game era factor > 0;
    * the agent's score ratio passes the explicit "not doing well" threshold;
    * enemy willingness-to-talk and ordinary trade legality otherwise permit the
      later NO_DENIAL path.

    The low-score branch sets bNoDenial but returns DENIAL_VICTORY anyway. The broker
    is refused specifically with the victory-denial reason that this commit intended
    to suppress for a clearly trailing civ.

CONSEQUENCE
    Brokered peace is refused when the function's own score policy says it should be
    allowed. At minimum, the denial reason is wrong; because bNoDenial is the actual
    gate for NO_DENIAL, the misplaced return also blocks the peace trade itself in
    the moderate-reluctance low-score window.

REPAIR DIRECTION
    After the low-score branch succeeds with iAgentReluct < 70, fall through to the
    later bNoDenial handling. Preserve DENIAL_TOO_MUCH for the >=70 low-score case and
    DENIAL_VICTORY for the cases that fail the low-score exception.

NEGATIVE CONTROL
    This is not inferred merely from indentation or naming: the tracked introducing
    diff and its commit message establish that the low-score exception was intended
    to change behavior, and the current unconditional return makes the successful
    half of that new branch dead.


CURRENT FILE-017 CURSOR
-----------------------
Completed surfaces so far:
* Team lifecycle / doWar entry and vassal/human handling;
* reviewWarPlans retry semantics and final AreaAI/scheming aggregates;
* current emergency-peace / victory-denial review additions;
* preparation conclude/abandon/switch state transitions;
* current target-selection/local-preference/direct-war additions;
* canSchemeAgainst and reachability/direct-war helper contracts;
* joint-war and brokered-war trade entry paths;
* makePeaceTrade through the newly confirmed low-score control-flow defect.

Rejected / bounded candidates so far:
* nearest-city -1 sentinel cannot pass the required live city/land-target contracts;
* victory-denial non-preparing plan during forced peace has inherited imminent-plan
  semantics and is not by itself a broken state machine;
* locally self-contradictory empty-enemy assertions in acceptVassal/uEndAllWars have
  caller contracts that make the empty branch an invariant trap rather than a normal
  valid path;
* current deterministic preparation-age/severity/switch defines match their XML
  contracts and are not promoted merely because older UWAI used probabilities.

NEXT
    Resume at F185 UNUSED. Continue the peace/reparation/vote/vassal trade tail,
    respondToRebuke and reachability/report helpers, then the UWAI::Player half of
    the file. Revisit all F183-Fxxx roots during the final adversarial closure pass.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP02 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source: reconciled AdvCiv-SAS light source 2026-08-26 15:08
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior checkpoint: C017-WIP01
New finding range in this append: F185-F186 / provisional KI#508-KI#509
Next finding number: F187 UNUSED

F185 / provisional KI#508
-------------------------
TITLE
    peaceThreshold reads the target team's war-success entry against itself instead
    of the target's war success against the agent.

STATUS
    CONFIRMED. AdvCiv regression introduced by the 2021 UWAI rewrite
    4ae382e5db (2021-03-20, "Revised all code in the UWAI component"), retained
    by Base AdvCiv 1.14 and current SAS.

ROOT
    The current threshold contains:

        (2 * kAgent.AI_getWarSuccess(eTarget) +
         4 * kTarget.AI_getWarSuccess(eTarget)) /
        GC.getWAR_SUCCESS_CITY_CAPTURING()

    The first term is our success against the target and is correct. The second term
    is supposed to represent the target's success against us, but eTarget is the
    target team's own ID. CvTeamAI::AI_getWarSuccess is a direct per-opponent EnumMap
    lookup, so kTarget.AI_getWarSuccess(eTarget) reads the target's self slot rather
    than the war-success entry for kAgent.

HISTORY PROOF
    The pre-rewrite implementation used the correct IDs:

        agent.AI_getWarSuccess(targetId) +
        2 * target.AI_getWarSuccess(agentId)

    divided by 0.5 * WAR_SUCCESS_CITY_CAPTURING. Algebraically, that is the same
    intended weighting as the current (2 * ours + 4 * theirs) / city-capture form.

    The tracked 4ae382e5db rewrite diff mechanically changes the correct
    target.AI_getWarSuccess(agentId) call into
    kTarget.AI_getWarSuccess(eTarget). A later ScaledNum conversion changes the
    arithmetic representation but preserves the wrong argument. This therefore has
    a precise regression point rather than being an ambiguous design choice.

DETERMINISTIC CONSEQUENCE
    During a human/AI or AI/AI war where the opponent has accumulated meaningful war
    success, the target's self slot remains the ordinary zero value while the actual
    target-vs-agent slot is positive. peaceThreshold therefore omits the weighted
    enemy-success contribution. The agent can require a more negative war utility
    before seeking peace than intended, especially when the opponent is winning
    strongly through captures and combat.

    Current SAS emergency-war diagnostics elsewhere in the same file correctly read
    the reciprocal term as kTarget.AI_getWarSuccess(kAgent.getID()), providing an
    additional live same-codebase control for the intended indexing.

REPAIR DIRECTION
    Replace the reciprocal term with:

        kTarget.AI_getWarSuccess(kAgent.getID())

    Preserve the current weighting and scaling; only the opponent index is wrong.

NEGATIVE CONTROL
    This is not a question of whether war success is symmetric. Each team's map is
    explicitly indexed by the opponent team, and the old implementation proves that
    the second term was meant to read the target's entry for the agent.


F186 / provisional KI#509
-------------------------
TITLE
    endWarVal's "no payment possible" shortcut ignores legal city reparations and
    can collapse a non-square human peace settlement to free peace.

STATUS
    CONFIRMED. Inherited AdvCiv defect whose original simplification explicitly
    acknowledged city payment but was never updated when AdvCiv's city-trade and
    peace-negotiation machinery became capable of using cities as reparations.
    Retained by Base AdvCiv 1.14 and current SAS.

ROOT
    When the AI already wants peace, endWarVal avoids computing the human side's war
    utility if the human team cannot possibly trade gold or technologies:

        if (iAIReluct <= 0)
        {
            bool bCanTrade = false;
            ...
            if (humanMember.canPossiblyTradeItem(aiMember, TRADE_GOLD) ||
                humanMember.canPossiblyTradeItem(aiMember, TRADE_TECHNOLOGIES))
                bCanTrade = true;
            ...
            if (!bCanTrade)
                return 0;
        }

    City payment is omitted. But city trading is independent of the gold- and
    technology-trading advances/options: canPossiblyTradeItem(TRADE_CITIES) only
    requires that the recipient can receive a city and that Max City Elimination is
    disabled. Actual city legality is then checked by canTradeItem/canTradeCityTo.

    The same live peace implementation explicitly supports city reparations.
    CvPlayerAI::AI_negotiatePeace asks the payer for TRADE_CITIES when gold and tech
    do not close the valuation gap, searches for a legal city, values it, and inserts
    TRADE_CITIES into the peace offer. During war AI_cityTrade returns NO_DENIAL for
    an otherwise legal city; human city offers are likewise allowed through the
    normal legality path.

INTRODUCING-HISTORY PROOF
    The original AdvCiv shortcut, introduced in the v0.82-era change
    0c3f38b7c2 (2017-04-21), contains this explicit comment:

        Should ideally also check if human could give the AI a city.

    It then dismisses the omission on the assumption that a human probably would not
    give up a city pre-Alphabet/pre-Currency. The associated release note describes
    the intended simplification directly: in the early game, when tech and gold
    trading are unavailable, the AI makes peace with a human for free even if it
    believes the human is more eager to end the war.

    Later refactors removed that caveat from the source comment without adding the
    missing city capability check. AdvCiv's 2020 city-trade work (advc.ctr, including
    fd5d755af0) also strengthened the AI's willingness/ability to use cities as
    payment, and current AI_negotiatePeace actively selects cities, but the old
    early-return condition still tests only gold and technologies.

DETERMINISTIC REPRODUCTION SHAPE
    Use a human/AI war at a stage or under options where neither side has gold or
    technology trading, while at least one city can legally be included in a peace
    settlement. A particularly direct legal city state is a wartime
    return/liberation or another non-capital city satisfying canTradeCityTo; city
    trading itself requires neither Alphabet-style tech trading nor Currency-style
    gold trading.

    Let iAIReluct <= 0 so the shortcut runs, and choose war utilities far enough apart
    that the full endWarVal logic would assign reparations if payment is possible.
    Because only gold/tech capability is tested, endWarVal returns 0 before reading
    iHumanUtility. The reciprocal human/AI valuation reaches the same shortcut, so
    the utility difference can collapse to a square/free peace even though
    AI_negotiatePeace is capable of balancing that difference with a city.

CONSEQUENCE
    Human-AI peace valuation can ignore a real, legal asset class and waive
    reparations merely because gold and technology trading are unavailable. This can
    benefit either side depending on which side would otherwise owe value; most
    importantly, it makes the result contradict the current deal constructor's own
    ability to use cities as reparations.

REPAIR DIRECTION
    Include TRADE_CITIES in the capability test that decides whether payment can
    exist. This should remain a cheap possibility test; actual city eligibility,
    denial, and value should continue to be handled by the existing downstream
    canTradeItem / AI_negotiatePeace / canTradeAssets logic.

    Do not simply remove the shortcut unless desired for policy reasons: its purpose
    is still valid when no compensating asset class is possible at all.

NEGATIVE CONTROLS
    * This is not based on hypothetical city trading: the current peace-negotiation
      helper has an explicit TRADE_CITIES fallback and inserts the selected city into
      the resulting peace deal.
    * canPossiblyTradeItem(TRADE_CITIES) does not depend on gold/tech trading, so
      there are ordinary rules states where the omitted asset class exists while both
      tested classes are unavailable.
    * The original source author explicitly identified city payment as the omitted
      case; subsequent city-trade improvements make the old approximation less, not
      more, defensible.


CURRENT FILE-017 CURSOR
-----------------------
Completed / newly challenged since C017-WIP01:
* peaceThreshold reciprocal war-success weighting and its 2021 refactor ancestry;
* endWarVal human-AI reparations arithmetic and the early no-payment shortcut;
* current AI_negotiatePeace gold/tech/city balancing path;
* canPossiblyTradeItem/canTradeItem/canTradeCityTo city-payment legality;
* AdvCiv city-trade history around the old early-game free-peace simplification;
* UWAI::Player willTalk cache wrapper / explicit cache callers (no reachable nested
  disable-after-enable defect established; rejected);
* emergency-peace fresh-war prioritization and deterministic preparation scaling
  (current behavior matches explicit policy/define contracts; rejected as defects).

NEXT
    Resume at F187 UNUSED. Continue the remaining peace/reparation/vote/vassal and
    Player-tail audit, with a targeted pass for exact team/player argument mistakes
    similar to F185. Then return through the larger plan/review/scheme machinery and
    run an adversarial F183-Fxxx closure pass before closing UWAIAgent.cpp.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP03 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source: reconciled AdvCiv-SAS light source 2026-08-26 15:08
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior checkpoint: C017-WIP02
New finding range in this append: F187 / provisional KI#510
Next finding number: F188 UNUSED

F187 / provisional KI#510
-------------------------
TITLE
    canTradeAssets tests only the first arbitrarily enumerated tradable cities under
    its 1/6-city cap, so it can falsely conclude that a human cannot afford a deal
    even when one later city within the same cap would suffice.

STATUS
    CONFIRMED. Inherited AdvCiv defect introduced with the 2018 willingness-to-talk
    city-payment cap (bb7b6925d1, 2018-04-06), retained by Base AdvCiv 1.14 and
    current SAS.

ROOT
    UWAI::Player::canTradeAssets caps city payment to:

        iCityLimit = ceil(kHuman.getNumCities() / 6)

    which implements the intended rule that the AI should not demand more than one
    sixth of the human player's cities for peace. But it then walks the human's cities
    in raw FOR_EACH_CITYAI iteration order, accepts the first legally tradable cities,
    and stops as soon as iCities reaches iCityLimit:

        if (iCities >= iCityLimit)
            break;
        ...
        if (kHuman.canTradeItem(... TRADE_CITIES ...))
        {
            iCities++;
            iTotalTradeVal += AI_cityTradeVal(*pCity);
            if (iTotalTradeVal >= iTargetTradeVal)
                return true;
        }

    The source itself leaves the warning:

        Tbd.: Shouldn't check the cities in an arbitrary order

    The limit is therefore applied to arbitrary city IDs, not to the best set of at
    most ceil(numCities/6) legally tradable cities.

HISTORY / INTENDED-CONTRACT PROOF
    AdvCiv commit bb7b6925d1666957c0cc1239e8e8df7c63a5df09
    (2018-04-06, "Small misc. changes; no DLL committed for now.") introduces the
    city cap. Its UWAI release-note bullet states the intended behavior directly:

        AI not willing to talk when it would demand more than 1/6 of the human
        player's cities, rounded up, for peace.

    That is a quantity cap, not an arbitrary-city-order cap. A deal that can be paid
    with one sufficiently valuable city out of six satisfies the documented rule
    regardless of that city's creation/iteration ID.

DETERMINISTIC REPRODUCTION SHAPE
    Give the human six cities, so iCityLimit is exactly 1. Suppose two non-capital
    cities are legally tradable:

        earlier city in iteration order: trade value 200
        later city in iteration order:   trade value 500
        remaining required asset value:  400

    canTradeAssets examines the 200-value city first, increments iCities to 1, fails
    to reach 400, and stops before seeing the 500-value city. It returns false as if
    satisfying the deal would require more than the allowed one city.

    The actual CvPlayerAI::AI_negotiatePeace city-reparation path does not share this
    restriction: when city payment is useful it scans all of the payer's cities for a
    legal city fitting the valuation gap. A later city can therefore be a legitimate
    one-city settlement asset even though canTradeAssets has already refused to talk.

CONSEQUENCE
    Human-AI willingness to talk and other callers that use canTradeAssets can depend
    on arbitrary city iteration/order rather than on whether the documented maximum
    number of cities can actually cover the requested value. The false negative can
    block a peace negotiation or affordability path that a legal one-city payment
    would satisfy.

    The issue also affects other canTradeAssets callers (including sponsored-war
    affordability) whenever the permitted city count is smaller than the number of
    legal cities and an early low-value city exhausts the count before a later useful
    city is examined.

REPAIR DIRECTION
    Preserve the ceil(numCities/6) quantity cap, but evaluate the best/useful legally
    tradable city values within that cap instead of taking the first city IDs. A
    small collected-and-ranked list is adequate because the cap and city count are
    tiny. The helper only needs an upper-bound/existence answer; actual deal
    construction can continue to use AI_negotiatePeace's own city-selection policy.

    Do not remove the 1/6 cap: that policy is intentional. The defect is the arbitrary
    ordering used to decide what fits within it.

NEGATIVE CONTROLS
    * The result does not require speculative city trading; current peace negotiation
      explicitly scans legal TRADE_CITIES candidates.
    * The finding does not assume the most expensive city must be demanded. It only
      requires existence of a later legal city whose value can satisfy the target
      while the first legal city cannot.
    * The introducing code's own Tbd comment acknowledges the arbitrary-order
      weakness; the commit/release wording confirms that the intended restriction is
      number of cities, not city ID order.


CURRENT FILE-017 CURSOR
-----------------------
Completed / newly challenged since C017-WIP02:
* UWAI::Player::canTradeAssets city-payment cap and all principal callers;
* current AI_negotiatePeace city selection versus the estimator's arbitrary subset;
* introducing AdvCiv bb7b6925d1 history and the explicit 1/6-city contract;
* team/member affordability approximations remain under review rather than being
  promoted merely because team games require simplification.

NEXT
    Resume at F188 UNUSED. Continue the sponsored-war / joint-war / team-member
    affordability and remaining peace/vassal/Player helper surfaces, then return to
    the larger reviewWarPlans/scheme machinery for the final adversarial closure
    pass. Promote only caller-reachable contract failures; leave explicit strategic
    approximations as non-findings unless a deterministic contradiction is proved.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C017-WIP04 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source: reconciled AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- OPEN
Physically durable prior checkpoint: C017-WIP03
New finding range in this append: F188 / provisional KI#511
Next finding number: F189 UNUSED

F188 / provisional KI#511
-------------------------
TITLE
    Imminent TOTAL-war target switching temporarily rewrites the plan to
    WARPLAN_PREPARING_LIMITED, so the alternative is evaluated as a limited war and
    a successful switch can permanently downgrade TOTAL to PREPARING_LIMITED.

STATUS
    CONFIRMED. Inherited AdvCiv defect introduced by commit
    e04adaf0a58e53f474e9c6ca4e8eaa6ce7eb19cf (2018-07-31), "UWAI can switch to a
    different target when war remains imminent". Retained by Base AdvCiv 1.14 and
    current SAS. Current SAS KI#189 makes the comparison deterministic and therefore
    makes the inherited state-loss path easier to encounter, but did not create it.

ROOT
    reviewPlan handles a non-war imminent plan (WARPLAN_LIMITED or WARPLAN_TOTAL) by
    temporarily changing the target's plan before calling considerSwitchTarget:

        kAgent.AI_setWarPlanNoUpdate(eTarget, WARPLAN_PREPARING_LIMITED);
        bool const bSwitch = !considerSwitchTarget(eTarget, iU, 0);
        ...

    The temporary plan is hardcoded to PREPARING_LIMITED even when the saved eWP is
    WARPLAN_TOTAL.

    considerSwitchTarget is not merely a predicate. It reads the current target plan:

        WarPlanTypes const eWP = kAgent.AI_getWarPlan(eTarget);

    and uses that value both to evaluate every alternative target:

        int iAltU = eval.evaluate(eWP, iTurnsRemaining);

    and, after a successful switch, to install the new target plan:

        kAgent.AI_setWarPlan(eBestAltTarget, eWP);

    Therefore the temporary rewrite changes both the comparison semantics and the
    persistent result of a successful switch.

HISTORY / INTENDED-CONTRACT PROOF
    The introducing 2018 commit added this exact mechanism and describes it as a
    workaround because considerSwitchTarget had been written for preparation plans:

        "considerSwitchTarget was written under the assumption that no war is
        imminent. Could be difficult to rectify; instead, change the war plan
        temporarily."

    The implementation then always chose WARPLAN_PREPARING_LIMITED. There is no
    corresponding reason to erase TOTAL vs LIMITED: considerSwitchTarget already
    accepts the current preparation plan generically and WarEvaluator can evaluate
    either PREPARING_LIMITED or PREPARING_TOTAL.

DETERMINISTIC REPRODUCTION SHAPE
    1. Agent has an imminent WARPLAN_TOTAL against target A and positive utility.
    2. The imminent-plan branch reaches target switching.
    3. reviewPlan temporarily changes A to WARPLAN_PREPARING_LIMITED.
    4. Alternative target B is legal and sufficiently more attractive to satisfy the
       switch threshold.
    5. considerSwitchTarget reads PREPARING_LIMITED, evaluates B under that plan, then
       clears A and assigns PREPARING_LIMITED to B.

    The original TOTAL character is restored only when there is no switch (or while
    running in the background). On a real successful switch it is lost.

CONSEQUENCE
    * A TOTAL-war plan can choose its replacement target using LIMITED-war utility,
      which can change which target wins the comparison.
    * If the switch succeeds, the new target receives WARPLAN_PREPARING_LIMITED rather
      than WARPLAN_PREPARING_TOTAL. The AI has therefore changed both target and war
      type even though this path is intended only to reconsider the target.
    * SAS's deterministic KI#189 target comparison makes this inherited defect more
      reproducible because the old preliminary random roll no longer suppresses many
      switch attempts.

REPAIR DIRECTION
    Preserve the original plan character in the temporary preparation mapping:

        WARPLAN_TOTAL   -> WARPLAN_PREPARING_TOTAL
        WARPLAN_LIMITED -> WARPLAN_PREPARING_LIMITED

    Then call considerSwitchTarget exactly as today. If no switch occurs, restore the
    saved imminent plan as before. If a switch occurs, the helper naturally installs
    the matching preparation type on the new target.

NEGATIVE CONTROLS
    * This is not an argument that an imminent plan must stay "imminent" after a
      target change. Re-entering preparation for the new target is coherent; the bug
      is loss of TOTAL vs LIMITED intent.
    * Background simulation already restores eWP after the helper, so the persistent
      downgrade concerns the real successful-switch path.
    * The defect predates SAS; KI#189 changes reachability/frequency, not the faulty
      mapping itself.

F189 DISPOSITION -- AI_negotiatePeace success vs actual war end
---------------------------------------------------------------
STATUS
    REJECTED AS A NEW FINDING after the current-source contract pass.

WHY IT LOOKED SUSPICIOUS
    considerPeace now logs both:

        bPeace    = AI_negotiatePeace(...)
        bWarEnded = !kAgent.isAtWar(eTarget)

    but still returns !bPeace rather than !bWarEnded. A previous SAS victory-denial
    safety guard in CvDeal could reject an AI-AI treaty after AI_negotiatePeace had
    already returned true, producing exactly bPeace == true && bWarEnded == false.

CURRENT-SOURCE CLOSURE
    The 20:49 source has closed that concrete mismatch at both earlier layers:

    * UWAI::Team::considerPeace skips ordinary peace when either team satisfies the
      shared configured victory-threat predicate.
    * CvPlayerAI::AI_negotiatePeace repeats the same shared pre-check for non-UWAI AI
      callers before constructing or implementing the deal.
    * CvDeal retains the final guard as defense in depth for human/unusual trade paths.

    For the AI-AI AI_negotiatePeace path, the selected gold/tech/city items are checked
    before implementation and CvDeal performs its full canTradeItem preflight before
    mutating the deal. No other stable current path was established where the helper
    returns true yet an AI-AI peace deal is silently rejected.

    Human peace offers intentionally remain different: AI_negotiatePeace returns true
    when the diplomacy offer is sent, before the human accepts it, so bWarEnded is
    expected to remain false. Returning based on bWarEnded would therefore be wrong
    for that branch. The separate diagnostic remains useful, but it is not itself a
    correctness defect.

CURRENT FILE-017 CURSOR
-----------------------
Completed / newly challenged since C017-WIP03:
* F188 imminent TOTAL-war target-switch plan preservation -- CONFIRMED;
* AI_negotiatePeace return-value vs actual-war-end contract -- current concrete
  victory-denial mismatch CLOSED by shared pre-checks; no additional F189 promoted;
* 2021 wrong reciprocal-ID refactor sweep -- no sibling to F185 established.

NEXT
    Resume at F189 UNUSED. Continue through UWAIAgent.cpp from the remaining
    peace/capitulation/vassal and scheme/plan machinery, then run an adversarial
    closure pass across the whole file before deciding whether file 017 can close.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C017-WIP05 APPEND -- 2026-08-26 -- UWAIAgent.cpp continuing audit
===============================================================================

AUTHORITATIVE SOURCE
--------------------
Reconciled AdvCiv-SAS light source snapshot 2026-08-26 20:49.
C017-WIP04 is preserved byte-for-byte before this append.

F189 / provisional KI#512 -- scheme can start LIMITED war from TOTAL-only positive utility at the exact zero boundary
---------------------------------------------------------------------------------------------------------------
STATUS
    CONFIRMED inherited AdvCiv defect.

CURRENT CODE / ROOT
    UWAI::Team::scheme evaluates both preparation types and then selects which plan
    character to carry forward:

        bool bTotal = false;
        if (iLimitedU < 0 && iTotalU > 0)
            bTotal = true;
        if (iLimitedU < 0 && iTotalU < 0) // Only relevant for logging
            bTotal = (iTotalU > iLimitedU);
        if (iLimitedU > 0 && iTotalU > 0)
        {
            ... personality-weighted total-vs-limited choice ...
        }
        int iU = std::max(iLimitedU, iTotalU);

    The three conditions leave one consequential sign combination uncovered:

        iLimitedU == 0 && iTotalU > 0

    bTotal therefore remains at its default false, while iU takes the positive total
    utility. The target survives the `iU <= 0` rejection and can be selected, but the
    resulting plan is WARPLAN_PREPARING_LIMITED (or the corresponding direct LIMITED
    plan in the SAS victory-denial path).

WHY ZERO IS A REAL INPUT
    WarEvaluator utility is an ordinary integer sum/difference, initialized from zero
    and returned directly. Zero is not a sentinel and requires no exceptional game
    state. Thus an exact neutral limited-war evaluation with a positive total-war
    evaluation is a legitimate boundary case.

HISTORY / REGRESSION PROOF
    Before AdvCiv commit 0fb74a859b720ab67c1148573fa5b9ab32770bf0
    (2018-08-31, "Various minor changes"), scheme simply used:

        bool total = (uTotal > uLimited);

    so `uLimited == 0 && uTotal > 0` correctly selected total war.

    That commit's log explicitly describes the UWAI change as:

        "Increased the impact of AI leader preferences for limited vs. total war."

    It replaced the simple comparison with the current sign-partitioned / weighted
    selector, but used strict `< 0` and `> 0` tests and omitted the neutral-limited,
    positive-total boundary. AdvCiv 1.14 retains the same conditions, so this is an
    inherited upstream defect rather than a SAS regression.

DETERMINISTIC REPRODUCTION SHAPE
    1. A legal scheming target evaluates to exactly 0 utility for
       WARPLAN_PREPARING_LIMITED.
    2. The same target evaluates to positive utility for WARPLAN_PREPARING_TOTAL.
    3. No other gate removes the candidate and its final drive wins its selection roll.
    4. bTotal is still false even though the only positive evaluated plan was TOTAL.
    5. scheme therefore installs a LIMITED plan whose own evaluated utility was 0.

CONSEQUENCE
    * The AI can start the wrong war type at an exact but ordinary utility boundary.
    * Its positive drive is supplied entirely by the TOTAL evaluation while its stored
      plan character is LIMITED.
    * Follow-up preparation/review logic then evaluates and develops the LIMITED plan,
      so this is not merely a misleading log label.

NARROW REPAIR DIRECTION
    Include the neutral limited case when total war is positive, e.g. conceptually:

        if (iLimitedU <= 0 && iTotalU > 0)
            bTotal = true;

    Keep the personality-weighted branch for the genuinely competing case where both
    utilities are positive. No broader change to the existing max-utility drive rule
    is required to fix this finding.

NEGATIVE CONTROL / SCOPE
    The broader question whether `iU = max(iLimitedU, iTotalU)` should always be tied
    to the personality-selected plan when both utilities are positive is intentionally
    NOT folded into F189. That may be deliberate separation of overall war drive from
    war-type preference. F189 needs no such interpretation: at the omitted zero
    boundary the selected LIMITED type has no positive utility at all.

CURRENT FILE-017 CURSOR
-----------------------
* C017-WIP05 physically durable.
* F183-F189 / provisional KI#506-KI#512 durable.
* F190 UNUSED.
* scheme() sign/selection boundary pass completed through F189; broader personality
  weighting semantics remain unnumbered unless a stronger contract proof appears.
* Continue through declareWarTrade / makePeaceTrade / vote / vassal / Player helpers,
  then perform a whole-file adversarial closure pass before closing UWAIAgent.cpp.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C017-WIP06 APPEND -- 2026-08-26 -- UWAIAgent.cpp continuing audit
===============================================================================

AUTHORITATIVE SOURCE
--------------------
Reconciled AdvCiv-SAS light source snapshot 2026-08-26 20:49.
C017-WIP05 is preserved byte-for-byte before this append.

F190 / provisional KI#513 -- isLandTarget leaks one member's no-ocean-cargo distance exemption into later team members
---------------------------------------------------------------------------------------------------------------------
STATUS
    CONFIRMED inherited AdvCiv team-game defect.

CURRENT CODE / ROOT
    UWAI::Team::isLandTarget keeps a single mutable distance limit outside its loop
    over the agent team's player members:

        int iDistLimit = getUWAI().maxLandDist();
        for (MemberAIIter itMember(m_eAgent); itMember.hasNext(); ++itMember)
        {
            UWAICache const& kCache = itMember->uwai().getCache();
            // Sea route then unlikely to be much slower
            if (!kCache.canTrainDeepSeaCargo())
                iDistLimit = MAX_INT;
            ...
            if (kCacheCity.canReachByLand())
            {
                ...
                if (kCacheCity.getDistance() <= iDistLimit)
                    return true;
            }
        }

    Once an earlier member cannot train deep-sea cargo, iDistLimit becomes MAX_INT
    for the remainder of the team-member loop. It is never restored for a later
    member that *can* train deep-sea cargo.

PER-PLAYER CONTRACT PROOF
    `canTrainDeepSeaCargo()` is a UWAICache property. UWAICache::updateTransports
    computes it from that cache owner's own civilization units and
    `kOwner.canTrain(eUnit)`, so it is deliberately player-specific.

    A sibling consumer, InvasionGraph::Node::canReachByLand, applies the same rule
    locally to the cache being queried:

        pCacheCity->getDistance() <= getUWAI().maxLandDist() ||
        !m_kCache.canTrainDeepSeaCargo()

    This is strong internal evidence for the intended semantics: the unlimited-land
    exemption belongs to the particular player whose cache lacks ocean-capable cargo,
    not to all later members of that player's team.

DETERMINISTIC REPRODUCTION SHAPE
    Consider a multi-player team with members A then B in MemberAIIter order:

    1. A cannot train deep-sea cargo, so the shared iDistLimit becomes MAX_INT.
    2. A has no target city that is reachable by land, so A does not itself establish
       the target as a land target.
    3. B can train deep-sea cargo.
    4. B has a target city reachable by land, but its distance exceeds maxLandDist.
       With B's own transport capability, that route should fail the land-distance
       test so a sea approach can remain preferable.
    5. Because A's MAX_INT leaked forward, B's distant route passes and the team is
       classified as having a land target.

    Reordering otherwise equivalent team members so B is visited before A removes
    that classification. The result is therefore iteration-order dependent.

CONSEQUENCE
    AI_isLandTarget feeds UWAI and other team war decisions, including whether war
    evaluation treats a target as naval/land. In team games the leak can classify a
    distant target as land for the wrong member, changing preparation assumptions and
    war valuation merely because another earlier teammate lacks deep-sea cargo.

HISTORY
    The shared mutable limit is already present in AdvCiv practical commit
    f6ff6a182f5187541fff6e5bbbe12f9573b3778c (2017-04-16, "Initial commit
    (cont.)"), which introduced the early WarAndPeaceAI/UWAI implementation. AdvCiv
    1.14 still has the same loop and current SAS inherited it unchanged.

NARROW REPAIR DIRECTION
    Keep the base configured limit immutable and derive a member-local limit, e.g.:

        int const iBaseDistLimit = getUWAI().maxLandDist();
        for (... each member ...)
        {
            UWAICache const& kCache = ...;
            int const iMemberDistLimit = (kCache.canTrainDeepSeaCargo() ?
                    iBaseDistLimit : MAX_INT);
            ... compare this member's cached city distance to iMemberDistLimit ...
        }

    Equivalently, inline the per-cache condition as InvasionGraph already does.

NEGATIVE CONTROLS
    * If the no-cargo member itself has a reachable-by-land target city, treating its
      route as unlimited is intentional; F190 does not challenge that policy.
    * Single-player teams are unaffected because there is no later teammate to inherit
      the modified limit.
    * This is not about team technology sharing. The relevant cache flag explicitly
      depends on each player owner's `canTrain` result and is consumed per owner in
      InvasionGraph.

CURRENT FILE-017 CURSOR
-----------------------
* C017-WIP06 physically durable.
* F183-F190 / provisional KI#506-KI#513 durable.
* F191 UNUSED.
* Vote joint-war duplicate-team candidate rejected: WarEvalParameters stores war
  allies in std::set<TeamTypes>, so player iteration cannot multiply a team's effect.
* Continue remaining vote/vassal/Player-helper tail and then whole-file adversarial
  closure.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C017-WIP07 APPEND -- 2026-08-26 -- UWAIAgent.cpp continuing audit
===============================================================================

AUTHORITATIVE SOURCE
--------------------
Reconciled AdvCiv-SAS light source snapshot 2026-08-26 20:49.
C017-WIP06 is preserved byte-for-byte before this append.

F191 / provisional KI#514 -- emergency-peace path calls ScaledNum::uround on a peace threshold that is explicitly allowed to be negative
------------------------------------------------------------------------------------------------------------------------------------
STATUS
    CONFIRMED SAS regression; debug-assert / signed-rounding contract defect.

CURRENT CODE / ROOT
    UWAI::Team::considerPeace obtains the signed peace threshold and, for the
    preferred emergency-peace target, forces iU below it so normal negotiation code
    cannot early-out:

        scaled rPeaceThresh = peaceThreshold(eTarget);
        ...
        if (bEmergencyPeace)
        {
            ...
            iU = std::min(iU, rPeaceThresh.uround() - 100);
        }

    `ScaledNum::uround()` is not the signed rounding API. Its implementation says
    explicitly:

        BOOST_STATIC_ASSERT(bSIGNED); // Use round() instead
        ...
        FAssert(m_i >= 0 && ...);

    Therefore calling uround() requires a nonnegative scaled value even though the
    type itself is signed.

NEGATIVE-THRESHOLD CONTRACT PROOF
    peaceThreshold() is intentionally a signed threshold, not a nonnegative amount:

        scaled r = fixp(-7.5); // To give it time

    It can receive further negative adjustments for total war, having been attacked,
    and leader pride. Most decisively, for an AI target it finishes with:

        if (!kTarget.isHuman())
            iR = std::min(0, iR);

    Thus inter-AI peace thresholds are explicitly constrained to be nonpositive, not
    nonnegative. Negative values are ordinary and expected. Emergency peace is not
    human-only, so a negative rPeaceThresh can reach uround() directly.

DETERMINISTIC FAILURE SHAPE
    1. An AI team is in SAS emergency-peace mode and the reviewed AI opponent is the
       preferred emergency-peace target.
    2. peaceThreshold(eTarget) is negative -- for example its normal -7.5 starting
       bias remains negative after the other terms.
    3. considerPeace enters bEmergencyPeace and calls rPeaceThresh.uround().
    4. A debug build violates `FAssert(m_i >= 0)` inside ScaledNum::uround().

    No unusual corrupt state is needed; the caller feeds a value whose legal range
    contradicts the rounding helper's documented/asserted precondition.

RELEASE-BUILD NOTE
    With assertions compiled out, the signed arithmetic still executes, but uround()
    uses the positive-value rounding formula. Around negative fractional values this
    can differ from round() (for example by one integer). Because SAS then subtracts
    100 solely to force the utility clearly below the threshold, that one-point
    rounding difference is unlikely to alter normal release gameplay here. The main
    concrete consequence is therefore the invalid API use and debug assertion, not a
    claim of a large release-balance effect.

HISTORY
    The line is absent from base AdvCiv and was introduced by SAS practical commit
    bf56a70088f859b853c9e72df7fcb8350672d364 (2025-09-17,
    "Seemingly tremendously improved multi-war threat management by adding sanity
    \"emergency peace\" pre-checks in UWAI::Team::considerPeace..."). The introducing
    diff contains the same call:

        iU = std::min(iU, rPeaceThresh.uround() - 100);

    The defect therefore originates in the SAS emergency-peace addition rather than
    inherited UWAI.

NARROW REPAIR DIRECTION
    Use the signed rounding helper already used elsewhere for this exact threshold:

        iU = std::min(iU, rPeaceThresh.round() - 100);

    No emergency-peace policy or threshold tuning needs to change.

RECONCILIATION NOTE -- F185 STILL LIVE IN THE 20:49 SOURCE
    The current diagnostics correctly log reciprocal target war success as:

        kTarget.AI_getWarSuccess(kAgent.getID())

    but the actual inherited peaceThreshold() formula still contains the F185 typo:

        kTarget.AI_getWarSuccess(eTarget)

    This is the already-durable F185 / KI#508 root, not a new finding. Its presence in
    the reconciled source simply means the documented fix has not yet reached this
    snapshot (or is still awaiting the user's/Codex implementation pass).

NEGATIVE CONTROL / SCOPE
    Other uround() calls in this file are not automatically F191 siblings. Most
    consume explicitly nonnegative prices, utilities clamped with max(0,...), or
    positive weighting factors. They require individual sign proof before any finding
    can be promoted.

CURRENT FILE-017 CURSOR
-----------------------
* C017-WIP07 physically durable.
* F183-F191 / provisional KI#506-KI#514 durable.
* F192 UNUSED.
* Whole-file adversarial closure remains OPEN.
* Next: mechanically review the remaining uround()/uceil() signed-value call sites,
  then resume cross-function state/sign/caller-contract closure before deciding
  whether UWAIAgent.cpp can be closed.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C017-WIP08 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source for this continuation: AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017-WIP07
New finding in this append: F192 / provisional KI#515
Existing roots materially clarified here: F183 / KI#506 and F189 / KI#512
Next finding number: F193 UNUSED

F183 / provisional KI#506 -- REPAIR-BOUNDARY CLARIFICATION
----------------------------------------------------------
The earlier root remains confirmed, but the closure pass found an important nuance in
its proposed repair. Merely resetting bScheme and the four land/naval aggregate flags
at the start of each do/while retry is not sufficient.

reviewWarPlans uses abTargetDone to prevent a target whose plan already changed from
being reviewed a second time. Some plan changes remove or redirect the target, where
that omission is appropriate; however, other false-return paths leave the same target
with a live changed plan. In particular, considerConcludePreparations can replace
WARPLAN_PREPARING_LIMITED/TOTAL with WARPLAN_LIMITED/TOTAL and return false, and the
SAS victory-denial review path can declare the war and return false. The target is then
marked done and omitted from later retry evaluations even though its final live war
plan should still contribute to the final AreaAI / bad-war summary.

Therefore a per-retry reset alone can trade the original stale-bit defect for a new
omission: the final no-change pass would describe only targets that were not marked
done, rather than all surviving current plans.

CORRECTNESS-FIRST REPAIR DIRECTION
    Keep the retry/mutation machinery and abTargetDone semantics for reviewPlan, but
    derive bScheme and the four AreaAI aggregate flags from a distinct final read-only
    evaluation of all surviving current non-vassal plans after the mutation loop has
    stabilized (or implement equivalent summary-only evaluation for done targets).
    That final pass must not call reviewPlan again. This matches the trailing contract
    that reviewWarPlans is the "last word on AreaAI" and also lets bScheme describe
    the actual final current-war set rather than any superseded pass.

This clarification strengthens F183; it is not a new root.


F189 / provisional KI#512 -- ZERO-BOUNDARY CLARIFICATION
---------------------------------------------------------
The earlier confirmed scheme() zero-boundary root is broader once the current SAS
victory-denial boost is considered.

The inherited 2018 selector currently partitions only strict signs:

    if (iLimitedU < 0 && iTotalU > 0)
        bTotal = true;
    if (iLimitedU < 0 && iTotalU < 0)
        bTotal = (iTotalU > iLimitedU);
    if (iLimitedU > 0 && iTotalU > 0)
        ... leader-preference comparison ...

The already-recorded case iLimitedU == 0 && iTotalU > 0 starts LIMITED even though
TOTAL is the only positive evaluation. Current SAS exposes the opposite missed zero
boundary too: when iTotalU == 0 and iLimitedU < 0, bTotal remains false; the later
victory-denial utility boost can lift max(iLimitedU, iTotalU) above zero and make the
candidate actionable, at which point the AI starts LIMITED even though TOTAL was the
strictly better underlying plan. The wrong bTotal also selects the limited-plan naval
flag for the direct-war eligibility gate.

CORRECTNESS-FIRST REPAIR DIRECTION
    Handle all cases with at least one nonpositive utility by selecting the plan with
    the larger evaluated utility, e.g. conceptually:

        if (iLimitedU <= 0 || iTotalU <= 0)
            bTotal = (iTotalU > iLimitedU);
        else
            ... existing both-positive leader-preference comparison ...

    Preserve the existing both-positive preference logic; history shows that policy
    was the purpose of the 2018 change. This is a repair-boundary clarification of
    F189, not a second finding.


F192 / provisional KI#515
-------------------------
TITLE
    SAS victory-denial target selection can install a direct WARPLAN_LIMITED/TOTAL
    while a peace treaty still makes immediate declaration illegal.

STATUS
    CONFIRMED. AdvCiv-SAS regression introduced by commit 4210d5586a
    (2026-07-14), KI#184 victory-denial direct-war work; absent from Base AdvCiv 1.14.

ROOT
    Ordinary UWAI deliberately allows planning during temporary force peace:
    canSchemeAgainst calls canEventuallyDeclareWar, and CvTeam::canEventuallyDeclareWar
    does not reject isForcePeace. The separate CvTeam::canDeclareWar check does reject
    force peace. scheme() therefore attenuates target drive by treaty time remaining
    rather than excluding the target entirely.

    KI#184 computes:

        bool const bVictoryDenialDirect =
            (iVictoryDenialBoost > 0 &&
             isSASVictoryDenialDirectWarAllowed(...));

    isSASVictoryDenialDirectWarAllowed checks victory urgency, target power, distance
    and naval/land access, but not kAgent.canDeclareWar(eTarget). A treaty-time target
    can consequently enter TargetData with bDirect=true and a positive adjusted drive.

    Once selected, scheme() builds eWP from bDirect, so that target receives
    WARPLAN_LIMITED or WARPLAN_TOTAL. The execution branch is:

        if (aTargets[i].bDirect &&
            aTargets[i].iVictoryDenialBoost > 0 &&
            kAgent.canDeclareWar(eTarget))
        {
            kAgent.declareWar(eTarget, false, eWP);
        }
        else kAgent.AI_setWarPlan(eTarget, eWP);

    When force peace is the only blocker, canDeclareWar is false and the shared else
    installs that direct plan instead of the preparation plan that treaty-time UWAI
    normally uses.

REACHABILITY
    This does not require an impossible API combination. CvTeam::canChangeWarPeace
    and canEventuallyDeclareWar permit a future war despite an ordinary temporary
    treaty, while CvTeam::canDeclareWar separately rejects isForcePeace. scheme() even
    computes rPeacePortionRemaining specifically to leave a reduced, nonzero chance
    of beginning preparations during that period. A strong KI#184 utility boost can
    therefore make such a victory threat win the target-selection roll before the
    treaty expires.

CONSEQUENCE
    * A feature whose comments describe conversion to "immediate" war can create an
      imminent direct war plan without declaring war at all.
    * The same code then suppresses showWarPrepStartedMsg because bDirect is true; its
      comment says direct victory-denial wars "are already declared", which is false
      on this reachable branch.
    * The target bypasses the normal PREPARING plan state while declaration is still
      prohibited. CvTeamAI::AI_mayAttack prevents treaty violation, so this is not an
      illegal attack, but it is still the wrong strategic state and can enter the
      inherited imminent-plan timeout/review path rather than ordinary preparation.

ANCESTRY / INTENT EVIDENCE
    Commit 4210d5586a introduced both the direct-plan conversion and the guarded
    declareWar call. Its source comments say qualifying targets are converted to
    "immediate limited/total war" and that direct victory-denial wars "are already
    declared". The fallback-to-AI_setWarPlan branch existed for ordinary preparations
    and was reused without converting eWP back to PREPARING when declaration is
    temporarily impossible.

REPAIR DIRECTION
    Keep the victory-denial utility pressure during force peace, but make bDirect true
    only when an actual declaration is legal now (equivalently, fall back to the
    corresponding PREPARING_LIMITED/PREPARING_TOTAL plan when canDeclareWar is false).
    This preserves treaty-time preparation and the KI#184 urgency boost while making
    the "direct" state mean what its declaration/message logic assumes it means.

NEGATIVE CONTROLS
    * This is distinct from the previously rejected general observation that direct
      plans can coexist briefly with force peace: considerConcludePreparations
      intentionally allows an existing preparation to become direct within a narrow
      <=3-turn treaty window. F192 is about fresh KI#184 selection marking a target
      direct for any treaty duration without first establishing that declaration is
      legal now.
    * CvTeamAI::AI_mayAttack does prevent units from violating the treaty. The finding
      is therefore not "AI attacks through force peace"; it is the direct-vs-preparing
      state mismatch and the resulting wrong review/message contract.


NEGATIVE CONTROLS SINCE C017-WIP07
-----------------------------------
* acceptVassal's apparently contradictory empty-new-enemy assertion is protected by
  CvTeamAI::AI_vassalTrade, which calls UWAI acceptance only after finding at least one
  new major-civ war for the prospective master using the same enemy class;
* warConfidenceLearned's sign-sensitive bIgnoreDefOnly equality is intentional: the
  v0.86 history explicitly repaired ignored past-war success while retaining the rule
  that defensive victories should not easily embolden future aggression;
* amendTensions' late-era ask-for-help multiplier can become nonpositive, but this is
  inherited original-UWAI policy and ScaledNum::randSuccess intentionally converts
  nonpositive probabilities to no contact;
* the local-target nearest-city sentinel -1 is guarded in gameplay paths by mutual
  UWAI land-target tests (zero-city teams cannot satisfy UWAI::Team::isLandTarget),
  while the victory-direct helper explicitly rejects negative distance;
* no additional signed-rounding sibling of F191 survived: the other uround() sites in
  this file have nonnegative inputs at their call sites.


C017-WIP08 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, WHOLE-FILE CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP08
CURRENT FILE-017 ROOTS: F183-F192 / provisional KI#506-KI#515
NEXT ROOT ID: F193 UNUSED

Resume by continuing the SAS-modified-region vs inherited-region closure pass, with
special attention to KI#184/KI#189 state transitions, caller legality gates, sentinel
contracts and final-plan aggregation. If no additional independent defect survives,
perform one final adversarial cross-function pass before closing file 017 rather than
manufacturing F193.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP09 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source for this continuation: AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017-WIP08
New finding in this append: F193 / provisional KI#516
Next finding number: F194 UNUSED

F193 / provisional KI#516
-------------------------
TITLE
    Inverted human-master test prevents AI vassals from timing out the very
    human-instructed war preparations that UWAI says should expire after 20 turns.

STATUS
    CONFIRMED. Inherited AdvCiv defect present from the initial UWAI integration,
    commit f6ff6a182f (2017-04-16, "Initial commit (cont.)"), and unchanged in
    Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
    UWAI::Team::doWar contains the explicit contract:

        /* Non-human vassals abandon human-instructed war prep. after 20 turns.
           Humans can have war preparations from AI Auto Play that they should
           also abandon ... */

    but gates the timeout with:

        (kAgent.isHuman() || !GET_TEAM(kAgent.getMasterTeam()).isHuman())

    doWar enters this block only for a human team or a vassal. For the exact first
    case named by the comment -- a non-human vassal whose master is human -- both
    operands are false, so the timeout can never run. Conversely, a non-human
    vassal of an AI master passes the negated second operand.

REACHABILITY / CALLER CONTRACT
    This is not a dead theoretical combination. CvDiplomacy.py retains K-Mod's UI:

        "Allow masters to tell their vassals to prepare for war"

    and exposes that option to a human master whose AI vassal is currently at peace.
    USER_DIPLOCOMMENT_WARPLAN_TARGET sends DIPLOEVENT_SET_WARPLAN, and
    CvPlayer::handleDiploEvent directly executes:

        kOurTeam.AI_setWarPlan((TeamTypes)iData1, (WarPlanTypes)iData2, false);

    Thus a human master can install PREPARING_LIMITED/PREPARING_TOTAL on an AI
    vassal exactly as the UWAI timeout comment describes.

INTERNAL CROSS-CHECK
    The master synchronization immediately below reinforces the intended split:

        else if (kMaster.AI_getWarPlan(eTarget) != NO_WARPLAN &&
                 !kMaster.isHuman()) // Human master will have to instruct us
            kAgent.AI_setWarPlan(eTarget, WARPLAN_PREPARING_LIMITED);

    AI masters continuously propagate their own preparations to AI vassals; human
    masters deliberately do not, because the human must issue the instruction.
    Therefore the expiring/manual preparation is specifically the human-master case,
    while the AI-master case is maintained automatically.

CURRENT WRONG-SIDE CONSEQUENCE
    For an AI-master vassal older than 20 turns, the current condition does enter:

        considerAbandonPreparations(eTarget, -1, 0);

    With iU=-1 and iTurnsRemaining=0, that helper deterministically clears the
    preparation. If the AI master still has a war plan, the synchronization block
    immediately afterward reinstalls WARPLAN_PREPARING_LIMITED. CvTeamAI::AI_setWarPlan
    resets the target's war-plan state counter to 0 whenever the plan value changes.
    The inverted gate can therefore periodically clear/reinstall/reset an AI-master
    vassal's inherited preparation while leaving a stale human-instructed preparation
    untouched indefinitely.

ANCESTRY / INTENT EVIDENCE
    The condition and the explanatory comment entered together in the initial UWAI
    AdvCiv commit f6ff6a182f on 2017-04-16. Later refactors retained the same boolean
    logic. The adjacent master-sync comment and the pre-existing diplomacy UI both
    independently agree with the prose contract, not with the negated condition.

REPAIR DIRECTION
    The narrow repair is to remove the negation from the master-human test:

        kAgent.isHuman() || GET_TEAM(kAgent.getMasterTeam()).isHuman()

    This keeps the AI-AutoPlay human-team timeout, enables the advertised timeout for
    AI vassals manually instructed by human masters, and avoids independently timing
    out the preparations that AI masters continuously propagate to their vassals.

NEGATIVE CONTROLS
    * The condition is safe for an ordinary non-vassal human: short-circuiting on
      kAgent.isHuman() means getMasterTeam() need not establish vassal semantics.
    * This is not merely a stale comment. The K-Mod/AdvCiv diplomacy path still lets a
      human master issue the preparation, and the adjacent synchronization code still
      explicitly treats human-master instructions as manual rather than inherited.
    * The finding does not require changing the 20-turn policy itself; only which
      master type is eligible for that already-documented policy is inverted.


NEGATIVE CONTROLS SINCE C017-WIP08
-----------------------------------
* respondToRebuke(false) does not become F193: UWAI has deliberately represented the
  non-preparation rebuke response as a direct WARPLAN_LIMITED since its first AdvCiv
  integration; the older generic CvPlayerAI comment saying "immediately declares war"
  is not enough to prove that UWAI's distinct war-plan contract is defective;
* isPeaceDealPossible's apparently reversed endWarVal direction is consistent with
  endWarVal's human/AI normalization: the helper computes the human-side payment need
  and then tests exactly the human assets that could satisfy it;
* Civ4CE-main in the supplied snapshot contains no UWAI/UWAIAgent/WarEvaluator
  implementation, so it cannot provide an independent later implementation or fix for
  file 017; Base AdvCiv 1.14 plus the supplied AdvCiv/SAS history mirrors remain the
  applicable ancestry controls here.


C017-WIP09 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, FINAL WHOLE-FILE CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP09
CURRENT FILE-017 ROOTS: F183-F193 / provisional KI#506-KI#516
NEXT ROOT ID: F194 UNUSED

Resume with a final mechanical/adversarial cross-function sweep: state variables whose
lifetime spans loops/retries, signed/sentinel arithmetic, team-vs-player identity,
war-plan transition legality, trade-estimator vs deal-construction contracts, and
historical TODO/Hack/assert assumptions. Close file 017 only if that sweep produces no
additional independent defect; do not manufacture F194 merely to extend the ledger.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C017-WIP10 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source for this continuation: AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017-WIP09
New finding in this append: F194 / provisional KI#517
Next finding number: F195 UNUSED

F194 / provisional KI#517
-------------------------
TITLE
    doWarReport's background-mode toggle does not update UWAI::Team's cached
    background state, so the supposedly diagnostic report run can mutate real war
    plans, peace, capitulation and declarations.

STATUS
    CONFIRMED. Inherited AdvCiv developer/debug-path defect. The cached Team
    background state dates to the initial UWAI integration; doWarReport was later
    added by AdvCiv commit a8a7e75c6c ("Some bugfixes (in K-Mod, Kek-Mod, AdvCiv
    code)") without synchronizing that cache. The same mismatch survives in Base
    AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
    UWAI::Team::reset stores background mode in a member cache:

        m_bInBackground = getUWAI().isEnabled(true);

    and every Team-side mutation guard reads only that cache:

        bool isInBackground() const { return m_bInBackground; }

    doWarReport is documented as:

        "Runs 'scheme' as if UWAI was running in the background ... Intended for
         debugging."

    but implements the temporary mode only through the global UWAI object:

        bool bInBackground = getUWAI().isEnabled(true);
        getUWAI().setInBackground(true);
        setForceReport(true);
        doWar();
        setForceReport(false);
        getUWAI().setInBackground(bInBackground);

    It never updates or resets this Team's m_bInBackground around doWar().

REACHABILITY / STATE PROOF
    doWarReport first requires getUWAI().isEnabled() with the default argument. By
    UWAI::isEnabled's contract, that is true only when UWAI is fully enabled and the
    global m_bInBackground is false. In that normal mode, Team::reset has cached
    m_bInBackground=false. doWarReport then changes the global value to true but
    leaves the Team cache false, and immediately invokes doWar().

    This is precisely the mode in which the documented manual debugging recipe is
    intended to be used: insert a doWarReport call, load the save to inspect, trigger
    the call, read the report, then remove the instrumentation.

CONSEQUENCE
    doWar and its subroutines currently contain many side-effect gates of the form:

        if (!isInBackground())

    Because the stale Team cache remains false, those branches are active during the
    report run. Reachable effects include, depending on the evaluated state:

    * canceling or changing existing war plans;
    * switching preparation targets;
    * setting new preparation/direct plans;
    * declaring an eligible direct victory-denial war;
    * negotiating/implementing peace;
    * capitulation and voluntary-vassal actions;
    * spectator war-plan messages associated with those real state changes.

    At the same time the global UWAI mode has been changed to background, so helpers
    that consult the global state can be evaluating under background assumptions while
    Team-side code still commits the result as though this were a real synchronized
    doWar turn. A diagnostic call can therefore perturb the very save state it is
    supposed to inspect.

ANCESTRY / INTENT EVIDENCE
    The original Team cache comment says the background value is cached during reset
    rather than queried repeatedly. Later commit a8a7e75c6c added doWarReport and its
    explicit "as if ... running in the background" contract, but toggled only the
    global object. Subsequent renames/refactors preserved both pieces independently;
    the 2021 UWAI rewrite still assigns m_bInBackground only in Team::reset.

REPAIR DIRECTION
    Keep global and Team-local background state synchronized for the whole report
    scope. A narrow repair can save m_bInBackground, set it true alongside
    getUWAI().setInBackground(true), then restore both after doWar(); alternatively,
    call/reset the Team cache after each global mode change. Preserve setForceReport
    independently, since forcing output and suppressing mutations are separate jobs.

    Do not merely change the comment or remove the global toggle: WarEvaluator and
    other UWAI code can consult the global background mode, while Team mutation gates
    consult the cached member. Both views need to agree.

SEVERITY / SCOPE
    This is developer-facing rather than ordinary autonomous gameplay: doWarReport is
    a manual diagnostic helper and has no normal production caller in the supplied
    source. It is nevertheless a real correctness defect because its documented use
    can alter the loaded save while investigating it, potentially invalidating the
    report/reproduction being debugged.

NEGATIVE CONTROLS
    * This is not the disabled-UWAI case. doWarReport returns immediately unless
      getUWAI().isEnabled() is true, which strengthens the proof: its reachable entry
      mode starts with the global and Team cache both false, then only the global flag
      is flipped.
    * setForceReport only overrides report cadence; it does not alter
      m_bInBackground and therefore cannot suppress state mutation.
    * Merely restoring the global flag at function exit does not undo war-plan,
      diplomacy or declaration mutations that already occurred during doWar().


NEGATIVE CONTROLS SINCE C017-WIP09
-----------------------------------
* KI#184.2's shared SAS_UWAI_VICTORY_DENIAL_DIRECT_MAX_DISTANCE is intentionally
  interpreted in two different distance representations: nearest-city plot distance
  in target selection and cached path/travel turns in WarUtilityAspect's contact
  guard. Commit 85b3818317 and the XML/source comments explicitly document this dual
  use, so the unit difference is policy rather than an accidental mismatch;
* the preferred-local-target and "roll only best eligible target" passes both use the
  same aAdjustedDrives values after AI_isAvoidWar hesitation and repeat the same local
  skip predicate; no preselection/eligibility drift survived that cross-check;
* the 2019 human-peace iDelta==0 arithmetic guard remains present after the 2021 UWAI
  rewrite, and the later capitulation skip-probability fixes inspected in ancestry are
  likewise still represented in current code;
* the remaining direct AI_setWarPlan / declareWar transition sweep found no new
  independent gameplay root beyond F188, F192 and F193 already recorded.


C017-WIP10 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, FINAL CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP10
CURRENT FILE-017 ROOTS: F183-F194 / provisional KI#506-KI#517
NEXT ROOT ID: F195 UNUSED

Resume with one final closure pass over remaining helper return contracts, arithmetic
boundaries, report/cache lifetime and current-vs-base differences. If that produces no
additional independent defect, close file 017 with F195 unused and advance the album;
do not manufacture a finding merely to avoid an unused ID.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP11 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source for this continuation: AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017-WIP10
New finding in this append: F195 / provisional KI#518
Next finding number: F196 UNUSED

F195 / provisional KI#518
-------------------------
TITLE
    AdvCiv's 2021 random-team-member diplomacy change left amendTensions with its
    old leader-only assertion, so valid nonleader diplomats trip a debug assertion.

STATUS
    CONFIRMED. Inherited AdvCiv regression introduced by commit f016996eb6
    (2021-03-27), "Let UWAI negotiate with a random team member"; present in Base
    AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
    UWAI::Player::amendTensions begins with:

        FAssert(GET_PLAYER(eHuman).isHuman());
        FAssert(GET_TEAM(m_eAgent).getLeaderID() == m_eAgent);

    That second invariant was originally valid: initial UWAI scheme() contacted the
    human through leaderUWAI().amendTensions(...).

    AdvCiv commit f016996eb6 deliberately changed war-related diplomacy away from
    team leaders. In scheme(), it replaced the leader call with:

        PlayerTypes eTargetPlayer = GET_TEAM(eTarget).getRandomMemberAlive(true);
        CvPlayerAI& kAgentPlayer = GET_PLAYER(kAgent.getRandomMemberAlive(false));
        ...
        kAgentPlayer.uwai().amendTensions(eTargetPlayer);

    but did not remove or adapt amendTensions' leader-only assertion.

REACHABILITY / INTENT PROOF
    CvTeam::getRandomMemberAlive explicitly documents its purpose:

        "pick members for conducting war-related diplomacy -- instead of using the
         team leaders"

    The introducing commit is titled "Let UWAI negotiate with a random team member"
    and says "... instead of the team leader. Human members strictly preferred."
    Thus selecting a nonleader is not an accidental caller state; it is the feature.

    On any non-human team with more than one alive AI member, scheme() can select a
    nonleader whenever a human target survives the target checks, the preparation
    roll fails, utility is <=23 and contact is possible. Current SAS's KI#207 repair
    to getRandomMemberAlive also makes the eligible-member selection genuinely
    uniform instead of relying on the older broken iterator-index comparison, so the
    nonleader path is straightforwardly reachable.

CONSEQUENCE
    In debug/assert-enabled builds, a legitimate multi-player-team diplomacy path can
    stop on a false assertion even though the caller is behaving exactly as the 2021
    feature intended. This can disrupt team-game/autoplay debugging and misleadingly
    suggest state corruption.

    In release builds the assertion is normally absent and the function proceeds.
    The called CvPlayerAI diplomacy helpers (AI_demandTribute, AI_askHelp,
    AI_contactReligion, AI_contactCivics) operate on the selected player and do not
    impose a team-leader requirement, reinforcing that the stale assertion rather
    than the random-member caller is the inconsistent piece.

ANCESTRY
    * 2017 initial UWAI: amendTensions asserts that its agent is the team leader;
      scheme calls leaderUWAI(), so the invariant is valid.
    * 2021-03-27 f016996eb6: scheme is intentionally changed to a random alive team
      member and calls that member's uwai().amendTensions; the assertion is untouched.
    * Base AdvCiv 1.14 and current SAS both retain the random-member caller plus the
      obsolete leader-only assertion.

REPAIR DIRECTION
    Remove the leader-only assertion (or replace it with an invariant that actually
    matches the random-member contract, such as ordinary alive/team membership if a
    defensive assertion is still desired). Do not revert scheme() to the team leader:
    the commit history and CvTeam helper comment explicitly establish random-member
    diplomacy as intentional.

NEGATIVE CONTROLS
    * This is not a claim that every amendTensions call must use a nonleader; single-
      member teams and random selection of the leader remain fine. The defect is that
      a valid nonleader selection contradicts a leftover assertion.
    * doWar returns early for human teams, so the agent-side scheme path here concerns
      AI-only teams; getRandomMemberAlive(false) may select any alive AI member.
    * The human target side is separately constrained by getRandomMemberAlive(true),
      matching amendTensions' first assertion that eHuman is human.


NEGATIVE CONTROLS / CLOSURE WORK SINCE C017-WIP10
--------------------------------------------------
* The final FAssert/Tbd/Hack sweep was reopened rather than closed when the
  amendTensions caller/assertion mismatch appeared; F195 is therefore an additional
  independent root rather than a split of F183-F194.
* The remaining contact helpers called by amendTensions were inspected and do not
  require the acting AI player to be its team's leader.


C017-WIP11 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, FINAL WHOLE-FILE CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP11
CURRENT FILE-017 ROOTS: F183-F195 / provisional KI#506-KI#518
NEXT ROOT ID: F196 UNUSED

Resume by continuing the whole-file assertion/caller and team-game contract sweep,
particularly other places changed by the 2021 random-team-member diplomacy commit and
any remaining player-vs-team assumptions. Then re-run the arithmetic/sentinel and
background-state closure passes before closing file 017; do not manufacture F196.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C017-WIP12 -- FILE 017 OPEN
=======================================

AUDIT STATE
-----------
Authoritative source for this continuation: AdvCiv-SAS light source 2026-08-26 20:49
Current file: 017 CvGameCoreDLL/UWAIAgent.cpp -- 3,700 lines -- OPEN
Physically durable prior boundary: C017-WIP11
New finding in this append: F196 / provisional KI#519
Next finding number: F197 UNUSED

F196 / provisional KI#519
-------------------------
TITLE
    Human-capitulation readiness persists across peace, so a later war can inherit
    an obsolete considerPeace authorization and bypass UWAI's AI-turn surrender gate.

STATUS
    CONFIRMED. Inherited AdvCiv lifecycle defect dating to the initial UWAI
    integration; present in Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
    UWAI uses the leader cache's m_readyToCapitulateTo set as a temporal authorization
    for surrender to a human master. considerCapitulation deliberately sets the bit
    before AI_surrenderTrade and leaves it set when capitulation is genuinely accepted:

        if (bHumanMaster)
            leaderCache().setReadyToCapitulate(eMaster, true);
        ...
        if (eDenial != NO_DENIAL)
        {
            ...
            if (bHumanMaster)
                leaderCache().setReadyToCapitulate(eMaster, false);
            return true;
        }

    The accompanying comment on the denial clear states the purpose explicitly:

        "To ensure that the capitulation decision is made on an AI turn;
         so that tryFindingMaster and AI_doSplit are called by considerPeace."

    CvTeamAI::AI_surrenderTrade enforces that contract for a human prospective master:

        /* Make sure that UWAI::Team::considerPeace has been called before surrendering. */
        if (getUWAI().isEnabled() && kMasterTeam.isHuman() &&
            !uwai().leaderUWAI().getCache().isReadyToCapitulate(eMasterTeam))
        {
            return DENIAL_RECENT_CANCEL;
        }

    The problem is lifecycle reset. reviewWarPlans clears the bit for a human target
    only inside its target loop, after an earlier no-war-plan return:

        if (!kAgent.AI_isAnyWarPlan())
            return true;
        ...
        if (kTarget.isHuman())
            leaderCache().setReadyToCapitulate(eTarget, false);

    If an authorized war ends before a later review, neither Team::reportWarEnding,
    UWAICache::reportWarEnding, CvTeamAI::AI_preMakePeace nor AI_postMakePeace clears
    m_readyToCapitulateTo. AI_preDeclareWar / AI_postDeclareWar do not clear it either.
    Consequently the old authorization can survive peace and enter a later war.

REACHABILITY / CONSEQUENCE
    A concrete lifecycle is:

    1. During war A against human team H, considerCapitulation reaches NO_DENIAL and
       leaves ReadyToCapitulate(H) true so a human capitulation deal can be offered.
    2. H does not accept that capitulation; war A later ends by ordinary peace.
    3. AI_postMakePeace resets war success and both war plans, but the readiness bit
       survives. If there are no other plans, reviewWarPlans' early return also leaves
       it untouched.
    4. H later starts war B. Before the victim AI gets its next considerPeace pass,
       the stale true bit can satisfy AI_surrenderTrade's explicit temporal gate once
       the other surrender criteria become true.

    This is not a claim that the AI necessarily capitulates immediately at zero war
    success. AI_surrenderTrade still has the ordinary power, war-success, city and
    diplomatic gates, and war success was reset at the old peace. The concrete defect
    is that the old war's authorization is reused in the new war. For example, a human
    can declare war and inflict enough losses/city captures during the same human turn
    for the normal surrender criteria to become true before the victim's AI turn.

    UWAI::Player::willTalk makes that stale authorization more consequential because
    surrender is tested before the one-turn refusal-to-talk gate:

        if (GET_TEAM(m_eAgent).AI_surrenderTrade(TEAMID(eToPlayer)) == NO_DENIAL)
            return 1;
        if (iAtWarCounter <= 1 || ...)
            return -1;

    Thus, once the normal surrender checks pass, the stale bit can make the AI willing
    to talk/capitulate before considerPeace has run in the current war -- precisely
    what the original AI-turn safeguard says it is intended to prevent.

PERSISTENCE / RESET PROOF
    Exhaustive setReadyToCapitulate call-site search in the current source found only:

    * reviewWarPlans: clear per human target during a review;
    * considerCapitulation: set before the human-master surrender check;
    * considerCapitulation: clear again on denial;
    * UWAICache implementation / leader migration plumbing.

    There is no peace-end or war-start clear. UWAICache::reportWarEnding clears bounty
    and sponsor state and records past-war success, but not readiness.

    m_readyToCapitulateTo is also written to and restored from the savegame stream.
    Therefore this is persistent cache state, not temporary stack/turn state; saving
    during the stale interval does not repair it.

ANCESTRY
    The initial AdvCiv UWAI integration introduced the same combination of behaviors:

    * the no-war-plan early return in reviewWarPlans;
    * the per-human readiness clear inside the later target loop;
    * set true before a human-master surrender check;
    * clear on denial specifically to keep the decision on an AI turn;
    * the AI_surrenderTrade readiness gate.

    Later UWAI rewrites/refactors preserve the lifecycle. Searches of current Base
    AdvCiv 1.14 and SAS show no later war-end/new-war clear.

REPAIR DIRECTION
    Clear this authorization when the corresponding war ends, rather than waiting for
    a future review. A file-017-local repair fits naturally in UWAI::Team::reportWarEnding:

        if (GET_TEAM(eEnemy).isHuman())
            leaderCache().setReadyToCapitulate(eEnemy, false);

    before or after forwarding reportWarEnding to member caches. That lifecycle point
    is called from CvTeamAI::AI_preMakePeace while the old war context still exists and
    is semantically exact: authorization produced for that war expires with that war.

    Clearing during an actual capitulation is harmless: AI_surrenderTrade has already
    approved the deal and the relationship is transitioning to vassalage. A clear on
    new declaration can be useful defense-in-depth, but it is weaker as the primary
    repair because the stale state should not survive the preceding peace at all.

    Merely moving the reviewWarPlans clear ahead of the no-war-plan early return is
    also insufficient: there can still be a peace-to-new-DoW window before that team's
    next review.

NEGATIVE CONTROLS
    * The finding does not remove or weaken the normal capitulation criteria; it only
      restores the intended current-war/AI-turn authorization lifetime.
    * The persistent bit is intentionally kept true after NO_DENIAL while the current
      war remains live so a human can receive/accept the capitulation; clearing it at
      every function exit would break that feature.
    * The correct boundary is therefore war lifetime, not function lifetime.


NEGATIVE CONTROLS / CLOSURE WORK SINCE C017-WIP11
--------------------------------------------------
* The rest of AdvCiv's 2021 random-member diplomacy conversion was checked after F195.
  Player-level peace and vassal deal routines intentionally operate on selected team
  members; no second leader-only contract mismatch was proven.
* A suspected random-member contact-timer bypass was rejected. Contact timers are
  player-pair state in ordinary non-UWAI diplomacy too, so different team members are
  intentionally able to have independent diplomatic cadence.
* scheme()'s INT_MIN total-war sentinel was traced through later comparisons/boosts;
  when total war is not evaluated, the sentinel does not enter the arithmetic path
  used to boost or divide the selected limited-war utility.
* Historical UWAI sign/epsilon/OOS bug-fix sites were rechecked. The known fixes are
  retained; the reciprocal-ID sweep found no sibling to the already-recorded F185.
* Tail confidence clamps, report allocation/close paths and force-report ownership
  were rechecked; no independent root beyond F194 was found there.


C017-WIP12 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, FINAL WHOLE-FILE CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP12
CURRENT FILE-017 ROOTS: F183-F196 / provisional KI#506-KI#519
NEXT ROOT ID: F197 UNUSED

Resume with a focused lifecycle/cache sibling sweep: persistent current-war state in
UWAICache and UWAIAgent that acts as an authorization or eligibility cache, especially
state cleared by reportWarEnding / new-war hooks. Then do one final mechanical closure
pass. If no independent root survives, close file 017 with F197 unused; do not invent
one merely to avoid an unused ID.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C017-WIP13 APPEND -- F197 / provisional KI#520
===============================================================================

F197 / provisional KI#520 -- INHERITED ADVCIV DEBUG ASSERTION DEFECT:
UWAI brokered-peace valuation asserts that war duration is strictly positive even
though a valid brokered-peace valuation can occur on the declaration turn with an
at-war counter of 0; the formula itself is already safe for 0.

LOCATION / LIVE CODE
    CvGameCoreDLL/UWAIAgent.cpp, UWAI::Team::makePeaceTradeVal:

        int iWarDuration = kAgent.AI_getAtWarCounter(eEnemy);
        FAssert(iWarDuration > 0);
        /*  warDuration could be as small as 1 I think. Then the mark-up is
            +175% in the Ancient era. None for a Renaissance war after 15 turns. */
        scaled rTimeModifier = (fixp(5.5) - kAgent.AI_getCurrEraFactor() / 2) /
                scaled(iWarDuration + 1).sqrt();

    The asserted invariant is stronger than both the actual trade API contract and
    the arithmetic requirement. The expression is perfectly defined at 0 because it
    takes sqrt(iWarDuration + 1).

REACHABILITY / CALLER CONTRACT
    CvTeamAI::AI_makePeaceTradeVal requires only that the two relevant teams are at
    war before forwarding to UWAI. It has no minimum-war-age precondition.

    TRADE_PEACE likewise has no age gate. CvPlayer::canTradeItem accepts a peace-
    brokerage item whenever the AI's team is at war with that third team and the
    ordinary contact/team-status conditions hold. CvPlayer::getTradeDenial forwards
    the item to CvTeamAI::AI_makePeaceTrade; UWAI::Team::makePeaceTrade contains no
    minimum at-war-counter check either.

    The normal human-AI trade table exposes third-party TRADE_PEACE items from the AI
    side when they are otherwise tradeable. If the AI's makePeaceTrade denial returns
    NO_DENIAL, AI_considerOffer proceeds to AI_dealVal, whose TRADE_PEACE case calls
    AI_makePeaceTradeVal and therefore reaches this assertion.

ZERO-COUNTER LIFECYCLE
    CvTeamAI::AI_doCounter increments AI_getAtWarCounter only during CvTeam::doTurn
    when the teams are already at war. An ended war has its counter reset to 0 in
    AI_postMakePeace. A declaration later in a turn does not pre-increment that
    counter; AI_postDeclareWar resets peace/share-war state and installs war plans,
    but leaves the new war duration at its legitimate initial value 0.

    Therefore a war declared after the team's counter update has a real interval in
    which isAtWar(eEnemy) is true while AI_getAtWarCounter(eEnemy) == 0. A broker can
    reach diplomacy in that interval (particularly straightforward in human / network
    turn-order cases). There is no denial-side age rule that excludes it.

    NO_DENIAL at age 0 is also not forbidden by UWAI semantics. makePeaceTrade bases
    the decision on the two teams' reluctanceToPeace values and contact state, not on
    iWarDuration. For example, a modestly reluctant aggressor (<55) and an enemy whose
    reluctance is nonpositive satisfies the normal no-denial branch; defensive-pact,
    vassal/cascade and otherwise unattractive fresh wars provide ordinary ways for
    immediate peace utility to be low or negative.

ANCESTRY / INTERNAL CONTROL
    This assertion and the accompanying "could be as small as 1" comment date to the
    initial AdvCiv UWAI integration (practical commit 1253, f6ff6a182f, 2017-04-16)
    and remain in Base AdvCiv 1.14 and current SAS.

    The legacy CvTeamAI::AI_makePeaceTradeVal path is useful independent evidence that
    the public valuation contract never required a positive age: it computes with

        AI_getAtWarCounter(ePeaceTeam) + 10

    and therefore naturally accepts a 0-turn war. UWAI changed the pricing formula but
    did not add a caller-side minimum-age rule.

CONSEQUENCE / SEVERITY
    In assertion-enabled builds, a legitimate same-round brokered-peace valuation can
    trip FAssert(iWarDuration > 0). This is a debug/developer correctness defect.

    This is NOT a claim of a release-build divide-by-zero or invalid square root. The
    current formula is safe at 0 and produces a defined, deliberately high early-war
    markup. With assertions compiled out, the mathematical path continues normally.

REPAIR DIRECTION
    The narrow repair is to relax/remove the invalid positive-age assertion. If an
    invariant check is desired, the actual domain is nonnegative:

        FAssert(iWarDuration >= 0);

    No denominator clamp or behavioral age gate is required; adding one would alter
    valid pricing semantics rather than merely repair the assertion contract.

NEGATIVE CONTROLS
    * No TRADE_PEACE_TREATY/direct bilateral-peace behavior is implicated here; this
      finding concerns the third-party TRADE_PEACE brokerage valuation.
    * No claim is made that every declaration-turn brokerage is accepted. The defect
      needs only one legal NO_DENIAL state; makePeaceTrade has no rule excluding age 0.
    * The arithmetic itself is already robust at zero, so this root is intentionally
      classified as assertion/debug-path scope rather than inflated into a gameplay
      crash or numeric corruption finding.


ADDITIONAL CLOSURE WORK SINCE C017-WIP12
----------------------------------------
* Remaining UWAICache lifecycle siblings were checked after F196. Sponsorship/bounty
  is explicitly cleared by reportWarEnding; lost-plot, war-anger and undistraction
  state are refreshed rather than acting as one-shot current-war authorization.
* canBeHiredAgainst is deliberately hysteretic/serialized, but cannot authorize an
  illegal already-at-war or force-peace declaration: CvTeamAI::AI_declareWarTrade
  applies the declaration-legality gates before consulting UWAI hireability.
* Background-mode branches that return simulated "changed" decisions while suppressing
  mutation were revisited. No independent caller contract failure beyond the already-
  recorded F194 cached-background mismatch has yet been proven.


C017-WIP13 INTERIM CURSOR
-------------------------
COMPLETED FILES: 16 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- OPEN, FINAL WHOLE-FILE CLOSURE PASS
PHYSICAL CHECKPOINT: C017-WIP13
CURRENT FILE-017 ROOTS: F183-F197 / provisional KI#506-KI#520
NEXT ROOT ID: F198 UNUSED

Resume with F198 unused. Finish the remaining assertion/Tbd/Hack census and the
SAS-vs-Base modified-hunk comparison, then perform a final function/return-state sweep.
If no independent root survives, close file 017 with F198 unused rather than inventing
one.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C017-WIP13 IMPORT

- C017-WIP13 has been imported without rewriting its append-only evidence. F197 / provisional KI#520 is collision-free against the current Known Issues ledger and remains provisional pending independent review. Its own cursor still leaves `UWAIAgent.cpp` open for one final whole-file closure pass with F198 unused; the next audit continuation should close that boundary before advancing to file 018.
- F132 / KI#455 has been independently revalidated and fixed. `Revolts` now forms one scenario-wide union of threatened agent cities in `preEvaluate`, with one matching expected-loss numerator and asset denominator per agent member; the removed persistent member set can no longer partition the ratio by rival order or leak city identities into another agent-member evaluation.
- F133 / KI#456 has been independently revalidated and fixed. Exclusive-radius land now has one explicit player-apportioned asset-category total shared by the flipped-tile numerator and `netLostAssetScore` denominator instead of comparing tile losses against city assets alone.
- The repaired Debug-opt DLL completed a default Huge mixed-team autoplay normally through a turn-357 Space victory. With `OWN_EXCLUSIVE_RADIUS=1`, it also completed a Large mixed-team autoplay normally through a turn-469 Space victory; exact Revolts and asset-ratio inputs remain source-verified because UWAI reporting was not inspected.
- F137-F138 / KI#460-KI#461 have been independently revalidated and fixed. `PreEmptiveWar` now consumes one shared rival-team threat forecast per agent member through any-member reachability; `TacticalSituation` charges one operational-readiness deficit per agent member and configured target team after aggregating the target's reachable-member era and reachability. The repaired Debug-opt DLL completed a Huge Normal Pangaea autoplay with 16 players across 11 teams, including five two-player teams, normally through a turn-372 Space victory.
- F149-F157 / KI#472-KI#480 have all been independently reviewed and fixed. The accepted repairs cover amphibious mission success, Air Patrol participation, heterogeneous upgrade-action caching, reusable group-path invalidation, cross-transport cargo state, reentrant `groupMove`/combat-timer node lifetime and value-copy ownership for the two Firaxis Python queue getters. Runtime tests directly exercised KI#473 and KI#479; the remaining narrow transitions received compiled autoplay coverage and source verification as recorded in Known Issues.
- KI#475's first implementation exposed and then fixed two additional independent crashes. KI#475.2 restored a destroyed-GroupPathFinder guard during save teardown after PDB-assisted diagnosis of the SAS follow-up regression. KI#475.3 initializes dead-at-load player UWAI before Base AdvCiv's revival update; the former reproduction save and WorldBuilder revival route then completed without crashing. These decimal continuation issues are implementation findings rather than replacements for an album root number.
- KI#480's repaired Debug-opt DLL compiled and completed an autoplay normally. Current shipped SAS Python does not call either queue getter, so its corrected public-API ownership transition remains source-verified.
- Current source and Known Issues are authoritative for these reviewed states; the preserved C014-C017 checkpoints remain verbatim investigation evidence. (GPT-5.6-Sol)


===============================================================================
DURABLE CHECKPOINT C018 -- FILE 017 CLOSED / PRIORITY QUEUE OVERRIDE ACTIVE
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot for closure:
    AdvCiv-SAS_light_source_20260827T000854.zip
Closed file:
    queue 017  CvGameCoreDLL/UWAIAgent.cpp  (3,700 lines)
Prior physical checkpoint:
    C017-WIP13

SOURCE RECONCILIATION
---------------------
The current UWAIAgent.cpp is byte-for-byte identical to the 2026-08-26 20:49 light
snapshot used for the late C017 investigation:

    SHA-256 c9ed9ac4c3a75595e21fa505a38a17517ea1a27bc6513bb45038a027ff23298c

Therefore the final closure pass did not have to reconcile any intervening source edit
inside file 017. The newer 00:08 bundle is nevertheless authoritative for repository-
wide state, the queue, Known Issues, and implementation/test archaeology.

FILE-017 FINAL FINDING RANGE
----------------------------
F183-F197 / provisional KI#506-KI#520.
F198 remains UNUSED.

No additional root survived the final closure pass. In particular, the pass rechecked:

* every remaining FAssert/Tbd/Hack site against its caller invariant rather than
  treating unusual comments or debug guards as defects by themselves;
* all current SAS-vs-Base UWAIAgent.cpp modified regions, with special attention to
  the KI#184/KI#189 victory-denial, deterministic preparation and target-selection
  state transitions;
* all public UWAIAgent.h Team/Player helper contracts against their external callers;
* reciprocal player/team IDs, including war-success and at-war-counter lookups;
* NO_TEAM / NO_PLAYER / NO_WARPLAN sentinels before getter and logging use;
* vector indexing and first-element accesses after their population gates;
* ScaledNum round/uround domains, logarithm/square-root inputs and division
  denominators;
* loop-carried state, retry-pass state and current-war/cache lifetimes;
* state read after AI_setWarPlan, declareWar, AI_negotiatePeace, capitulation and
  vassal-deal transitions;
* report allocation/close paths, forced-report lifetime and background simulation;
* the 2021 random-team-member diplomacy conversion and its remaining player-pair
  contact timers;
* historical AdvCiv UWAI fixes for war-trade sign, peace epsilon/zero-delta handling,
  target-switch arithmetic, OOS-safe closeness and related historically fragile code.

FINAL NEGATIVE CONTROLS / REJECTIONS
------------------------------------
The following late candidates were intentionally NOT assigned F198:

1. Background review helpers can report a simulated plan change while suppressing the
   real mutation. That is part of the normal background-evaluation design; the proven
   defect remains F194, where doWarReport toggles only the global background flag while
   Team::m_bInBackground stays stale and therefore fails to suppress those mutations.

2. The 2021 random-member peace/vassal diplomacy conversion does not create a
   team-global contact-timer bypass. The underlying diplomacy system intentionally
   stores CONTACT_PEACE_TREATY timers per player pair, and ordinary non-UWAI diplomacy
   can likewise contact another member of the same enemy team while one pair is on
   cooldown. F195 remains the isolated stale leader-only amendTensions assertion.

3. scheme's sorted TargetData/aAdjustedDrives metadata stays index-aligned through the
   preferred-local-target prepass, best-eligible-target pass and final selection. The
   best-target logging also initializes eBestEligibleTarget before any dereference in
   the non-best-only mode.

4. scheme's skipped-total INT_MIN value is never fed into the later victory boost,
   divisor or ScaledNum arithmetic. It participates only in comparisons/logging until
   iU takes max(iLimitedU, iTotalU). The real zero-partition plan-type defect remains
   F189; no independent sentinel root survives.

5. considerSwitchTarget's ratio denominator is positive whenever an alternative
   target exists. Its padding lifts the smaller utility to 20 when necessary, while a
   no-padding path requires the selected alternative to exceed a current utility of at
   least 20.

6. The apparently strict confidenceFromWarSuccess reciprocal at-war-counter assertion
   is not promoted. War declaration/peace reset reciprocal counters together, ordinary
   team-turn ordering explains the documented small skew, and permanent-alliance
   merging explicitly re-synchronizes the counterpart counters. No reachable >2 skew
   was established.

7. tradeValJointWar's non-human-ally assertion is preserved. Both normal AI diplomacy
   and Rise & Fall replacement valuation call it with an AI ally; no human-ally caller
   was found.

8. uEndAllWars/acceptVassal empty-vector assertions remain caller-protected rather than
   hidden crashes. Vote validity and AI_vassalTrade respectively establish the
   applicable enemy set before the UWAI entry points are used.

9. getSASBBAINearestCityDistance returning -1 for no city pair does not create a
   preferred-local phantom target because the mutual AI_isLandTarget eligibility gate
   rejects the no-source-city case before the distance threshold is decisive.

10. The diagnostic WAR_TARGET_PLAN_SET row can also be emitted after the direct
    declaration branch. The label/comment is somewhat broader than the nearby wording,
    but the declaration really does install that war plan; this is logging naming,
    not a gameplay/state root.

11. report ownership is balanced: every path after startReport in doWar reaches
    closeReport, and the Team destructor remains a final safety delete. setForceReport
    has no independent persistent-state leak beyond F194's background-cache mismatch.

12. Civ4CE supplied with this audit contains no UWAI/UWAIAgent/WarEvaluator
    implementation, so there is no independent CE-side implementation or later UWAI
    fix to compare for file 017. Base AdvCiv 1.14 and the bundled AdvCiv history mirrors
    remain the appropriate ancestry controls here.

C018 CLOSURE JUDGMENT
---------------------
UWAIAgent.cpp is CLOSED for this first-pass album. This does not mean the file is
proven bug-free; it means the present deep pass has exhausted the concrete roots that
can be supported by current code, caller contracts, lifecycle analysis and available
history without manufacturing tuning disagreements or speculative edge cases.

The high number of roots in this file also supports the newer investigation method:
repeated physical WIPs, narrow candidate proof, caller/lifecycle tracing and historical
comparison exposed substantially more than a quick linear pass would have.

PRIORITY-QUEUE POLICY FROM CURRENT ALBUM
----------------------------------------
The original line-count queue remains preserved for identity/reference, but next-file
selection now follows the explicit "<-(priority next wip...)" markers in the current
album rather than raw source size.

The explicitly ordered first group is:

    priority 1st: queue 041  CvGameCoreDLL/CvSelectionGroupAI.cpp  (1,454 lines)
    priority 2nd: queue 031  CvGameCoreDLL/UWAICache.cpp           (1,812 lines)
    priority 3rd: queue 045  CvGameCoreDLL/CvMapGenerator.cpp      (1,281 lines)

After those, continue through the other files marked "priority next wip" in the current
queue before returning to ordinary pending files. Once those never-passed priority
files have received the stronger WIP method, perform a second deep sweep of the older
important files that were marked priority despite already being COMPLETE, especially:

    CvPlayerAI.cpp, CvUnitAI.cpp, CvPlayer.cpp, CvCityAI.cpp,
    CvCity.cpp, CvUnit.cpp, CvGame.cpp and CvPlot.cpp.

Those second sweeps are not predicated on finding more issues. Their value is that the
old passes predate the stronger checkpoint/proof methodology, while these files are
large and gameplay-critical enough that sparse historical findings are worth
revalidating rather than assuming completeness.

CURRENT DURABLE CURSOR
----------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017: CvGameCoreDLL/UWAIAgent.cpp -- CLOSED AT C018
PHYSICAL CHECKPOINT: C018
FILE-017 ROOTS: F183-F197 / provisional KI#506-KI#520
NEXT ROOT ID: F198 UNUSED
NEXT SELECTED FILE (PRIORITY OVERRIDE):
    queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp (1,454 lines)

Start the next WIP from F198 unused. Do not renumber the original queue; identify the
file as queue 041 while the C018-WIPxx checkpoint series records investigation order.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP01 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- FIRST DURABLE HANDOFF
===============================================================================

SOURCE / CURSOR
---------------
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T000854.zip

Priority override is active. This is queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp,
selected ahead of the old size-order queue because it is marked priority next WIP.
Base AdvCiv 1.14 and Civ4CE are being used as local comparison controls where the
implementation overlaps.

F198 / provisional KI#521 -- CONFIRMED
--------------------------------------
Inherited AdvCiv debug assertion defect: a legal human BOMBARD mission against an
undefended city can trip AI_bestUnitForMission's iDefenders > 0 assertion.

Root:
    CvSelectionGroupAI::AI_bestUnitForMission(MISSION_BOMBARD)

The human-only branch computes the number of defending units on the bombard target
and then asserts:

    FAssertMsg(iDefenders > 0, "AI bombards undefended city");

That invariant is false for a legal human command. The full caller/legality chain was
checked rather than inferred from the local function:

1. CvSelectionGroup::canDoMission(MISSION_BOMBARD) exposes the mission when any
   movable group unit satisfies CvUnit::canBombard.
2. CvUnit::canBombard requires positive bombard rate, attacks remaining, no cargo,
   and a non-null bombardTarget. It does not require a defending unit.
3. CvUnit::bombardTarget accepts an adjacent CvCity when CvCity::isBombardable does.
4. CvCity::isBombardable also has no defender requirement. In particular, its
   advc.004c human behavior deliberately permits legal bombardment of a city whose
   defenses can still be reduced/kept at zero even when no unit defends it.
5. CvSelectionGroup's MISSION_BOMBARD executor calls
   AI_bestUnitForMission(MISSION_BOMBARD) for the human command too.
6. Therefore a human can legally issue Bombard with iDefenders == 0 and reach this
   human-only assertion.

The assertion text itself is additional evidence of a stale invariant: it says
"AI bombards undefended city" despite sitting in the else branch of if (!isHuman()).

Release-build arithmetic is already safe. The later attacker/defender ratio uses
std::max(iDefenders, 1), so zero defenders do not create a divide-by-zero. This root
should therefore remain scoped as a debug/assertion correctness defect, not described
as a release crash or as invalid Bombard gameplay.

Ancestry/control:
- The block comes from AdvCiv's advc.004c mission-selection rewrite and survives in
  Base AdvCiv 1.14.
- The local Civ4CE branch does not carry this AdvCiv selector/assertion rewrite.
- No existing KI#521 collision was found in the reconciled album/Known Issues at the
  time of promotion.

Narrow repair boundary:
- Remove or relax the iDefenders > 0 assertion for the human path.
- Do NOT add a defender requirement to canBombard/isBombardable; undefended-city
  bombardment is intentionally legal for humans.
- Existing std::max(iDefenders, 1) arithmetic protection can remain.

NEGATIVE CONTROLS COMPLETED BEFORE THIS HANDOFF
-----------------------------------------------
- KI#319 no-progress tripwire: challenged the omitted group-size/composition state.
  AI_counterMove -> AI_separate looked promising, but AI_separate removes the head
  first into a fresh group, so the old group's head ID changes/disappears and the
  tripwire correctly detects progress. Other split/load cases either replace the
  head similarly or queue a mission. No generic "snapshot group size too" root.
- SAS low-power / obsolete-sacrifice attacker ordering: the low-power mode is used by
  the real post-declaration attack loop, so its inherited canMoveInto(..., true)
  legality does not accidentally need the potential-enemy mode. iUnitOdds == -1
  after choosing a sacrifice is also not consumed incorrectly by the caller.
- AI_getMissionAIUnit stale ID warning: normal FFreeList slot recycling does not
  alias the stale ID because the full ID contains a generation component and getAt
  verifies the complete ID. Exact aliasing would require an extreme generation wrap,
  not ordinary unit replacement.
- AI_getBombardTurns: a mixed ignore-building-defense / ordinary-siege estimate can
  mathematically over-credit normal siege, but current SAS has no land/sea bombard
  unit with ignore-building-defense; the live trigger is BBAI's explicitly synthetic
  global air-bomber-support approximation. No correctness root promoted from that
  deliberately approximate helper.
- AI_isDeclareWar initial caller/legality pass found no concrete contract defect.
- AI_ejectBestDefender still contains the later AdvCiv precision repair and no current
  zero/selection defect has yet been proved.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP01
QUEUE-041 ROOTS SO FAR: F198 / provisional KI#521
NEXT ROOT ID: F199 UNUSED

Continue from AI_bestUnitForMission's remaining mission contracts, then AI_isFull,
AI_ejectBestDefender, combat odds/strength helpers, serialization symmetry and the
historical Base/CE fix checklist. Do not close queue 041 merely because one linear
pass is clean.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP02 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- SECOND DURABLE HANDOFF
===============================================================================

SOURCE / CURSOR
---------------
Authoritative source snapshot remains:
    AdvCiv-SAS_light_source_20260827T000854.zip

C018-WIP01 is an exact byte prefix of this checkpoint. Investigation continued from
F199 unused, with particular attention to the SAS AI_update no-progress tripwire,
transport/cargo state transitions, combat/group-attack state, mission references and
historical K-Mod/AdvCiv safeguards.

F199 / provisional KI#522 -- CONFIRMED
--------------------------------------
AdvCiv-SAS regression in the KI#319/5093 no-progress safeguard: legitimate AI sea-
transport unloads can be mistaken for "no progress" because the tripwire does not
observe cargo-state changes, so the transport receives MISSION_SKIP instead of
immediately reconsidering its next action.

Root:
    CvSelectionGroupAI::AI_update

Current tripwire snapshots only:
- head unit ID;
- head UnitAI role;
- head x/y;
- head remaining moves;
- selection-group mission-queue length.

If all those values are unchanged after CvUnitAI::AI_update, the group is not busy and
can still move, the safeguard pushes MISSION_SKIP and breaks the update loop.

That predicate misses an inherited and explicitly intentional class of progress:
unloading cargo at the destination without pushing a mission.

The cleanest concrete path is CvUnitAI::AI_specialSeaTransportSpy:

    if (atPlot(pTargetPlot))
    {
        getGroup()->unloadAll();
        getGroup()->setActivityType(ACTIVITY_AWAKE);
        // no actual mission pushed, but we need to rethink our next move.
        return true;
    }

AI_spySeaMove calls AI_specialSeaTransportSpy while carrying UNITAI_SPY cargo and
returns after this helper returns true. Control then returns through CvUnitAI::AI_update
to CvSelectionGroupAI::AI_update.

The unload is real state progress but does not alter any field currently observed by
the SAS tripwire:

1. CvSelectionGroup::unloadAll iterates the group's transports and calls
   CvUnit::unloadAll.
2. On a legal coastal-land destination, the Spy cargo can unload. Its
   setTransportUnit(NULL) call decrements the old transport's cargo count through
   changeCargo(-1).
3. This does not move the sea transport, consume the transport's remaining moves,
   change its UnitAI role or head identity, or push a mission on the transport group.
4. The helper explicitly leaves the transport group ACTIVITY_AWAKE.
5. Therefore, if the transport still has movement left, !isBusy() and
   readyToMove(true) remain true while every currently snapshotted field compares
   equal.
6. The tripwire consequently pushes MISSION_SKIP and breaks, suppressing the immediate
   same-turn reconsideration that the inherited helper explicitly requests.

This is not limited to the Spy helper. Current CvUnitAI retains the same missionless
unload + ACTIVITY_AWAKE + return-true pattern in multiple transport paths, including:
- AI_transportGoTo;
- the at-destination AI_settlerSeaTransport branches (now using SAS's
  AI_unloadSettlerCargoForDestination helper);
- AI_settlerSeaFerry;
- AI_specialSeaTransportMissionary;
- AI_specialSeaTransportSpy.

Historical intent is unusually strong. K-Mod commit bc00e144af6b2cf937eee0a7dea246907c048652
(practical 849), titled "Told the AI to explicitly wake their group in a few places.",
added setActivityType(ACTIVITY_AWAKE) to six such at-destination unload branches. The
Spy branch already carried the literal explanation that there is "no actual mission
pushed" because the unit needs to rethink its next move. In inherited pre-SAS
CvSelectionGroupAI::AI_update, the still-awake/movable group therefore naturally
iterates again after the cargo transition.

The conflicting SAS safeguard arrived much later in commit
1798ac76f438e0c42602efb5e09819192efc06b2 (practical 5093), whose no-progress
predicate initially observed only head geometry/moves and mission-queue length.
KI#319, fixed in commit d0ff436c1dc604225935d2b64951124f5c679923 (practical 6211),
correctly expanded that predicate with head identity and UnitAI role after a different
legitimate state transition exposed the same design weakness. Cargo progress remains
unobserved, so F199 is a second concrete incomplete-predicate case in the same
safeguard family rather than a new inherited transport bug.

Reachability is concrete rather than theoretical. AI_specialSeaTransportSpy searches
coastal land owned by known civs and explicitly values a target city when not at war;
a sea transport can legally occupy an accessible coastal city, arrive with Spy cargo,
unload there, and retain movement. The helper's own atPlot branch exists specifically
for that reached-destination state.

Narrow repair boundary:
- Preserve the no-progress safeguard; it fixes a real inherited Scout/Explorer loop.
- Count cargo-state change as progress. A direct low-risk repair is to snapshot the
  selection group's total getCargo() value at loop entry and require it to be
  unchanged before pushing MISSION_SKIP.
- Prefer group cargo rather than only pHeadUnit->getCargo(), because a multi-transport
  group can unload cargo from a non-head transport as well.
- The post-action getCargo() comparison can remain late in the conjunction so the
  extra group scan is only performed after the cheaper identity/role/geometry/move/
  queue tests have already matched; the initial cargo snapshot still costs one small
  group scan per loop iteration.
- Do NOT "fix" the transport helpers by removing ACTIVITY_AWAKE or forcing them to
  consume their turn; K-Mod deliberately added the wake-up behavior so they can
  reconsider immediately after unloading.

No existing KI#522 or F199 collision was found in the current reconciled snapshot at
promotion time.

NEGATIVE / CLOSURE WORK SINCE C018-WIP01
----------------------------------------
1. AI_isFull special-cargo logic survived caller/XML validation. Assault-sea groups
   intentionally ignore special-cargo ships when ordinary assault transports are
   available, while UNITAI_MISSILE_CARRIER_SEA groups correctly wait for their
   SPECIALUNIT_MISSILE capacity. Zero-capacity matching escorts report full and do
   not create a false capacity requirement.

2. A broader KI#319 "group size must be snapshotted" theory was rejected. In
   AI_counterMove -> AI_separate, the head is separated first so the old group gets a
   different/no head and the current ID test notices progress. In the newer
   AI_guardCity defender-ejection path, detaching only non-head defenders makes
   AI_guardCity return false so the acting head continues inside the same
   CvUnitAI::AI_update rather than prematurely returning to the group tripwire.

3. AI_isGroupAttack was also tested as an omitted tripwire field. When groupAttack
   requeues an attack because the destination is already fighting, CvUnit::attack
   leaves an attackPlot; CvUnit::isInCombat and CvSelectionGroup::isBusy therefore
   stay true. The tripwire is gated by !isBusy(), so K-Mod's requeued-attack state is
   not converted to MISSION_SKIP.

4. AI_getMissionAIUnit's stale IDInfo warning does not alias ordinary recycled unit
   slots. FFreeList full IDs include a generation component and getAt validates the
   complete ID. A practical replacement unit in the same slot therefore does not
   resolve through the stale reference; only extreme generation wraparound remains
   the acknowledged theoretical case.

5. AI_getBombardTurns contains deliberately approximate mixed-defense/air-support
   math, but current SAS has no live land/sea bombard unit with ignore-building-
   defense set. Its relevant branch is BBAI's explicitly synthetic global air-bomber
   support estimate; no correctness root was promoted from that heuristic.

6. The exported AI_isDeclareWarExternal(CvPlot* pPlot = NULL) boundary is safe.
   AdvCiv's pointer-to-reference refactor added an explicit pPlot == NULL return-false
   guard in the external wrapper in the same historical change, so the legacy default
   argument cannot dereference NULL there.

7. SAS low-power/sacrifice attack ordering, AI_sumStrength legality filtering,
   AI_getWeightedOdds normalization and serialization symmetry were challenged and no
   independent current root has yet survived. The historical K-Mod group-attack,
   already-fighting, amphibious and pre-declaration movement safeguards examined so
   far also remain present.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP02
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
NEXT ROOT ID: F200 UNUSED

Continue through the remaining path/mission helpers, combat/defender and mutable-state
contracts, assertions, Base AdvCiv/K-Mod historical-fix checklist and CE comparison.
Do not close queue 041 until the stronger whole-file closure sweep is complete.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP03 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- THIRD DURABLE HANDOFF
===============================================================================

SOURCE / CURSOR
---------------
Authoritative source snapshot remains:
    AdvCiv-SAS_light_source_20260827T000854.zip

C018-WIP02 is an exact byte prefix of this checkpoint. Investigation continued from
F200 unused, finishing the previously open AI_sumStrength/pre-war legality challenge
before returning to the inherited attack-odds weighting contract.

F200 / provisional KI#523 -- CONFIRMED
--------------------------------------
Inherited AdvCiv arithmetic/contract defect in CvSelectionGroupAI::AI_getWeightedOdds:
the function explicitly tries to remove the personality AttackOddsChange before
applying nonlinear opportunity/value adjustments, but it subtracts that bonus from an
attack-odds value that has ALREADY been clamped to the public 1..99 range. Once the
upper clamp has fired, subtraction cannot reconstruct the pre-personality combat odds.

Root:
    CvSelectionGroupAI::AI_getWeightedOdds

Cross-function source of the lossy value:
    CvUnitAI::AI_attackOdds
    CvUnitAI::LFBgetBetterAttacker

The AdvCiv source states the intended contract directly:

    "We shouldn't adjust the odds based on an optimistic estimate
     (increased by AttackOddsChange) ...
     I'm subtracting the AttackOddsChange temporarily;
     adding them back in after the adjustments are done."

Current code therefore does, conceptually:

    adjustedAndClamped = clamp(rawOdds + attackOddsChange, 1, 99);
    supposedRawOdds    = adjustedAndClamped - attackOddsChange;
    weighted           = AI_opportuneOdds(supposedRawOdds, defender);
    result             = clamp(weighted + attackOddsChange, 1, 99);

Both producers of iUnitOdds perform the upper clamp before returning it:
- CvUnitAI::AI_attackOdds adds AI_getAttackOddsChange(), then range(..., 1, 99);
- with default LFB combat-odds selection, CvUnitAI::LFBgetBetterAttacker likewise adds
  AI_getAttackOddsChange() and clamps to 1..99 before storing iAIAttackOdds.

Thus the subtraction is invertible only when rawOdds + attackOddsChange <= 99. Once
that sum exceeds 99, information has been destroyed. For example:

    true raw odds          = 99
    AttackOddsChange       = +16
    producer returns       = clamp(115, 1, 99) = 99
    AI_getWeightedOdds uses= 99 - 16 = 83   // not 99

+16 is reachable for ordinary current SAS leaders: e.g. Ragnar and Napoleon have
BaseAttackOddsChange 6 and AttackOddsChangeRand 8; CvPlayerAI::AI_doTurn can therefore
set 6 + 7 + floor(7/2) = 16. The Barbarian setting can reach an even larger +23.
LFB_ENABLE and LFB_USECOMBATODDS both default enabled, so the lossy LFB producer is the
normal current path rather than a disabled compatibility branch.

This is behaviorally material because AI_opportuneOdds is intentionally nonlinear in
its input. It applies production-value, LFB-value, defensive-plot, wound/heal and city-
opportunity adjustments using iOdds and (100-iOdds). Adding the personality bonus back
at the end therefore does NOT undo feeding the wrong 83 instead of the true 99 into
those calculations.

A deterministic arithmetic example shows that this can change an attack decision,
not merely the displayed score. Take a land attacker with:
- true raw odds 99;
- AttackOddsChange +16;
- production cost 200 against a 50-cost defender;
- a defender plot with defenseModifier 200;
- one defender in a city; and no other differing opportunity modifiers.

The current reconstruction feeds 83 into AI_opportuneOdds:
- production term: x = 83*17*2/(200+50+20) = 10, giving -15;
- defensive-plot term: -(17*200/300) = -11;
- one-city-defender opportunity bonus: +17/3 = +5;
- 83 - 15 - 11 + 5 = 62;
- add personality bonus back: 62 + 16 = 78.

With the intended true raw 99:
- the production x term truncates to 0;
- the defensive-plot penalty truncates to 0;
- the one-defender city bonus truncates to 0;
- adding +16 afterward clamps back to 99.

So the same genuinely near-certain combat can be weighted 78 instead of 99. That is
large enough to fail live AI_anyAttack thresholds such as 90 despite the underlying
combat odds being essentially certain. The exact numerical scenario is illustrative;
the defect exists for every upper-clamped value because the lost excess cannot be
recovered and the downstream transform is nonlinear.

This is inherited unchanged from base AdvCiv 1.14; current SAS did not introduce the
root. The nearby advc.114b comment itself is strong provenance evidence because the
implementation violates the stated purpose of that change.

Narrow repair boundary:
- Do NOT try a different subtraction after the public/clamped value; the missing raw
  odds are mathematically unrecoverable once the clamp has fired.
- Give AI_getWeightedOdds access to the pre-AttackOddsChange odds BEFORE the final
  1..99 clamp. A small helper or optional parameter in the underlying attack-odds
  calculation that suppresses the personality adjustment until after
  AI_opportuneOdds would preserve the intended advc.114b design.
- Keep the final AttackOddsChange addition and final 1..99 clamp unless deliberately
  redesigning the personality behavior; the bug is specifically the lossy attempted
  inverse before the nonlinear opportunity adjustment.
- The LFB and non-LFB producers need consistent treatment because both currently add
  and clamp the personality bonus before returning iUnitOdds.

No existing KI#523 or F200 collision was found in the reconciled album at promotion
time.

NEGATIVE / CLOSURE WORK SINCE C018-WIP02
----------------------------------------
1. The previously open AI_sumStrength(bCheckCanAttack=true) pre-war siege hypothesis
   was rejected after completing the reachability proof. AI_stackAttackCity first
   requires CvSelectionGroup::canMoveOrAttackInto(..., bDeclareWar=true). Against
   ordinary rival defenders before the DoW, the final unit-level hasDefender enemy
   check prevents that path from reaching AI_compareStacks in the proposed state.
   Once war has actually begun, the defenders are visible enemies and AI_sumStrength
   performs its target-specific canMoveInto filter. The initially proposed inflated
   pre-war siege ratio therefore does not occur through that caller.

2. That rejection exposed a separate inherited CvUnit::canMoveInto potential-war
   semantic oddity around the K-Mod bDeclareWar path and its final bTestEnemy defender
   query. It belongs to CvUnit.cpp and has not been promoted or assigned an F/KI ID in
   this queue-041 audit. Preserve it as a candidate for the later deep CvUnit resweep
   rather than scope-creeping file 041.

3. Current SAS genuinely has combat-limited siege (Catapult/Trebuchet 75%,
   Cannon/Hwacha/Houfnice 80%, Field Gun/Self-Propelled Artillery/Levitation Artillery
   85%), so the rejected AI_sumStrength hypothesis was tested against live data rather
   than dismissed because combat limits were absent.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP03
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
NEXT ROOT ID: F201 UNUSED

Continue with the remaining inherited attack/mission/path helpers, assertions, mutable-
state contracts, Base AdvCiv/K-Mod historical-fix checklist and CE/BtS comparison.
Do not close queue 041 until a final whole-file adversarial sweep is complete.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C018-WIP03 IMPORT

- C018 closes `UWAIAgent.cpp`, bringing the first-pass audit to 17 complete files. C018-WIP01 through WIP03 open priority queue file 041, `CvSelectionGroupAI.cpp`, with F198-F200 / provisional KI#521-KI#523 and F201 unused. The progress summary and queue statuses above now reflect this current cursor; the original numeric queue order remains unchanged.
- F158-F167 / KI#481-KI#490 have been independently reviewed and fixed, with their ancestry and repair boundaries recorded in current Known Issues. F168 / provisional KI#491 remains pending because eliminating its duplicate-resource CityPlot enumeration-order dependency requires broader two-pass resource aggregation rather than a narrow first-hit patch.
- The Debug-opt DLL containing the KI#487-KI#490 repairs compiled successfully, and the requested Large Archipelago, Ancient-era, Normal-speed autoplay completed normally. The narrow cross-area culture, feature+bonus health, first-Barbarian-city and unusable-overlap transitions remain source-verified.
- F169-F196 / provisional KI#492-KI#519 remain pending independent review and implementation. Closing files 016 and 017 records completion of their audit passes, not completion of every resulting repair.
- F197 / KI#520 was independently reviewed and fixed by accepting the valid zero declaration-turn war counter in its debug assertion while preserving the existing pricing arithmetic. Its repaired Debug-opt DLL completed an autoplay normally; the exact third-party peace-brokerage transition remains source-verified.
- C018-WIP03 is retained as append-only investigation evidence. Its new F198-F200 findings remain provisional until independent source review and implementation; current source and Known Issues remain authoritative where the older light-source snapshot differs. (GPT-5.6-Sol)


===============================================================================
C018-WIP04 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- FOURTH DURABLE HANDOFF
===============================================================================

SOURCE / CURSOR RECONCILIATION
------------------------------
Investigation has now been reconciled against the newer user-supplied snapshot:
    AdvCiv-SAS_light_source_20260827T084943.zip

Local comparison controls remain:
    AdvCiv-1.14(2).zip
    Civ4-K-Mod-master-1.46.zip
    Civ4CE-main(7).zip

C018-WIP03 remains a byte-for-byte prefix of this checkpoint. No prior F198-F200 root
was invalidated by the newer SAS snapshot. Queue 041 remains OPEN while the final
whole-file closure sweep continues.

F200 / provisional KI#523 -- SAME ROOT, LOWER-CLAMP PROOF ADDED
---------------------------------------------------------------
The lossy AI_attackOdds round-trip documented in C018-WIP03 fails at the lower clamp
as well as the upper clamp. This strengthens F200; it is not a new F201 root.

AI_getWeightedOdds contains an explicit advc.114b safety rule that is intended to use
the actual/unbiased combat odds:

    if (iOdds == 0 && getNumUnits() < 3)
        return 0;

However, the quantity received from CvUnitAI::AI_attackOdds has already had
AI_getAttackOddsChange applied and has already been clamped to the public 1..99 range.
Therefore a genuinely rounded raw 0% result behaves inconsistently:

    raw odds = 0, AttackOddsChange = 0
    producer = clamp(0 + 0, 1, 99) = 1
    subtraction in AI_getWeightedOdds = 1 - 0 = 1

The explicit iOdds == 0 safeguard can never activate in that state. With a positive
optimism bonus, the same raw 0 can sometimes survive the attempted inverse, e.g.
0 + 6 -> 6, then 6 - 6 -> 0. The supposedly personality-independent small-stack
safety rule can therefore itself depend on personality merely because the clamp is
applied before the attempted subtraction.

Repair implication is now stronger than in WIP03: AI_getWeightedOdds needs the
pre-personality, pre-final-1..99-clamp odds. Repairing only upper saturation would
leave the explicit sub-1%/rounded-0 safety contract broken.

F201 / provisional KI#524 -- CONFIRMED
--------------------------------------
SAS KI#319 no-progress tripwire can misclassify AdvCiv's deliberate non-head bombard
unit split as "no progress" and force MISSION_SKIP on the surviving assault stack.

Primary root:
    CvSelectionGroupAI::AI_update

Cross-file trigger:
    CvUnitAI::AI_bombardCity

Immediate callers confirmed:
    CvUnitAI::AI_attackCityMove
    CvUnitAI::AI_attackCityLemmingMove

The current SAS tripwire snapshots only the original group's:
- head unit ID;
- head UnitAI role;
- head x/y;
- head moves left;
- mission queue length.

If all remain unchanged after CvUnitAI::AI_update returns, while the group is still
ready and not busy, SAS pushes MISSION_SKIP and breaks.

AdvCiv advc.004c deliberately changed K-Mod's bombard behavior. K-Mod pushed
MISSION_BOMBARD on the whole assault group. AdvCiv instead selects one unit:

    CvUnit* pBombardUnit = AI_getGroup()->AI_bestUnitForMission(MISSION_BOMBARD);
    ...
    if (AI_getUnitAIType() == UNITAI_ATTACK_CITY)
        pBombardUnit->joinGroup(NULL);
    pBombardUnit->getGroup()->pushMission(MISSION_BOMBARD, ...);
    return true;

The selected bombard unit is therefore split into its own group before the bombard
mission is issued. pushMission starts an AI mission immediately; the detached unit
bombards and spends its own movement through CvUnit::bombard. The original group does
not receive that mission.

This becomes a KI#319 false positive when pBombardUnit is NOT the original group head:
1. The non-head bombard unit leaves the original group.
2. CvSelectionGroup::removeUnit/deleteUnitNode changes membership and clears/wakes the
   old group, but the old head survives unchanged.
3. The detached unit's new group executes MISSION_BOMBARD and spends that unit's moves.
4. AI_bombardCity returns true; AI_attackCityMove / AI_attackCityLemmingMove return
   immediately; CvUnitAI::AI_update then returns false to CvSelectionGroupAI::AI_update.
5. The original group can still be awake, missionless, non-busy and ready to move.
6. Its head ID/role/x/y/moves and mission queue length can all equal their snapshots.
7. SAS therefore pushes MISSION_SKIP on the surviving original group solely because
   the tripwire does not observe that getNumUnits() decreased.

Base AdvCiv 1.14 has no SAS tripwire at this point. After exactly the same non-head
split/bombard, its CvSelectionGroupAI::AI_update while-loop tests readyToMove again and
can immediately reconsider the surviving stack. Thus the behavior suppressed by SAS
is not speculative extra processing; it is the inherited control flow that
advc.004c's split leaves available.

NON-HEAD REACHABILITY IS ORDINARY, NOT A CONTRIVED POINTER STATE
---------------------------------------------------------------
Current group ordering and current SAS XML provide a concrete normal composition:

CvUnitAI::AI_groupFirstVal:
    UNITAI_ATTACK_CITY + bombardRate() > 0  -> 19
CvUnitAI::AI_groupSecondVal:
    base combat strength

Current generic siege includes:
    Catapult   -- DefaultUnitAI ATTACK_CITY, strength 5, bombard 8
    Trebuchet  -- DefaultUnitAI ATTACK_CITY, strength 4, bombard 16

For an ATTACK_CITY group containing both, the Catapult can be the group head because
both have first-value 19 and the Catapult has higher second-value/base strength.
AI_bestUnitForMission(MISSION_BOMBARD), however, does not simply choose the head. Its
bombard priority increases with useful bombard damage and with (1 - attack odds),
while both examples share the same basic collateral dimensions. A Trebuchet's doubled
bombard rate and lower combat odds can therefore make it the selected bombard unit
while the Catapult remains the head.

The broader code explicitly anticipates mixed assault stacks as well: the
ATTACK_CITY movement logic contains the inherited BBAI grouping path for a stack led
by a bombard unit to join another stack "probably not lead by a bombard unit." More
generally, multiple bombard-capable members with different promotions/strengths make
selection of a non-head unit routine; AI_bestUnitForMission exists precisely to pick
a mission unit independently of head identity.

PROVEN STATE DETAILS
--------------------
- CvUnitAI::AI_update asserts that the acting unit is the group head, dispatches
  UNITAI_ATTACK_CITY to AI_attackCityMove and UNITAI_ATTACK_CITY_LEMMING to the
  corresponding lemming routine, then normally returns false.
- Both attack-city routines return immediately when AI_bombardCity returns true.
- CvUnit::joinGroup(NULL) creates/rejoins a separate group by default
  (bRejoin = true).
- Removing a non-head does not set AI_forceSeparate; joinGroup sets that flag only
  when the removed unit was the old head and the replacement head has a different
  UnitAI role.
- The old group's head identity, role, location and moves therefore need not change.
- pushMission on the detached AI group is started immediately; MISSION_BOMBARD calls
  CvUnit::bombard, which sets made-attack and consumes one movement denominator from
  the detached bombard unit only.
- The original group's mission queue remains empty after the split and can still pass
  readyToMove(true).

BEHAVIORAL EFFECT
-----------------
At minimum, the SAS tripwire can consume the surviving attack-city stack's turn after
one non-head siege piece was detached to bombard, instead of letting inherited AdvCiv
re-enter the still-ready group. That can suppress additional same-turn bombard/
reassessment/attack behavior by the rest of the stack and alter city-assault tempo.
The exact next inherited action is intentionally recalculated after the first
bombard; the bug is that SAS prevents that recalculation by mistaking membership
change for zero progress.

REPAIR BOUNDARY
---------------
F201 needs a group-composition progress dimension. The narrow repair is to snapshot:

    const int iNumUnitsBefore = getNumUnits();

and require getNumUnits() == iNumUnitsBefore before the no-progress MISSION_SKIP.

This should be combined with, not substituted for, F199's group-total getCargo()
snapshot:
- F199: cargo can change while membership count stays constant;
- F201: membership can change while cargo and every current head snapshot stay
  constant.

A more general state-signature redesign is possible, but the conservative repair is
to add these two proven missing dimensions rather than guess at every mutable group
field.

No existing KI#524 collision was found in the current reconciled album or Known
Issues snapshot at promotion time.

NEGATIVE / CLOSURE WORK SINCE C018-WIP03
----------------------------------------
1. AI_getBestGroupSacrifice's bPotentialEnemy parameter looks stale because its
   inherited canMoveInto legality test does not use the parameter. The actual
   groupAttack sacrifice path invokes it after hostility is established, and SAS's
   obsolete-sacrifice subscan is likewise live on that post-DoW path. This remains a
   dead/legacy API awkwardness, not an F root.

2. AI_isForceSeparate was challenged as another omitted KI#319 snapshot field.
   Internal paths that set it true arise when join/split operations remove or change
   the old head; the existing head-ID/role snapshots observe those transitions. The
   arbitrary direct setter is an EXE-facing wrapper rather than a normal same-head
   CvUnitAI action. No independent tripwire failure survived.

3. A single Settler unloading itself from a transport can change transport linkage
   without necessarily changing the transport group's total cargo snapshot, but
   AI_settleMove does not return at that transition. It continues its decision tree;
   exits issue a mission/move/group transition or reach its explicit final
   MISSION_SKIP. No KI#319 sibling was found there.

4. Delayed death is not an invisible tripwire transition. CvUnit::kill(true) calls
   finishMoves before marking delayed death, and upgrades explicitly resolve the old
   unit's delayed death. Movement/head readiness therefore changes before the
   no-progress test can misclassify it.

5. Worker ACTIVITY_AWAKE / mission-AI clearing recovery paths continue into normal
   worker decisions rather than returning a missionless success. No independent
   activity-only tripwire root survived.

6. Python unit-AI override is not a reachable current-SAS explanation. The current
   PythonCallbackDefines.xml has USE_AI_UNIT_UPDATE_CALLBACK = 0 and BUG's default
   AI_unitUpdate result is false. A hypothetical enabled extension could mutate
   arbitrary state, but widening a current repair around a disabled hook would be
   speculative.

7. Loading onto transports remains covered by existing snapshots: relevant paths
   either split/change the old head/group identity or call finishMoves. The cargo
   unloadAll family remains the distinctive count-stable cargo mutation behind F199.

8. SAS low-power / obsolete-sacrifice attacker ordering was rechecked against its
   current define values (60% health gate; 90/60/25/70 odds/deficit/value thresholds).
   The previously suspected int-percent multiplication remains far below overflow in
   reachable current unit values; no arithmetic root was promoted.

9. The earlier pre-war AI_sumStrength combat-limit hypothesis remains rejected. It
   was not revived during this pass.

10. AI_follow, stored MissionAI IDInfo handling, AI_compareStacks internal callers,
    AI_ejectBestDefender and the remaining separation helpers examined so far have
    not yielded another independent queue-041 root.

FUTURE-FILE NOTE -- NOT QUEUE 041 / NOT ASSIGNED
-------------------------------------------------
A suspicious precedence expression was noticed in CvUnitAI::AI_airStrike while doing
cross-file state-transition greps:

    int iBestValue = iThreshold + isSuicide() &&
        getUnitInfo().getProductionCost() > 0 ?
        getUnitInfo().getProductionCost() * 5 / 6 : 0;

Normal C++ precedence appears capable of grouping this as a conditional based on
(iThreshold + isSuicide()) rather than adding a conditional suicide-unit cost term.
This has NOT been audited to the promotion bar and belongs to the future CvUnitAI.cpp
resweep. Do not consume a queue-041 F/KI ID for it and do not treat this note as a
confirmed bug.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP04
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
NEXT ROOT ID: F202 UNUSED

Next: continue the final whole-file adversarial closure pass, with special attention
to any other same-head missionless state transition, group-composition changes not
covered by F201, and inherited K-Mod/AdvCiv/CE divergence. If no F202 survives, leave
it unused and close queue 041 only after the remaining historical/diff checklist is
exhausted.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C018-WIP04 IMPORT

- C018-WIP04 has been imported without rewriting its append-only evidence. It strengthens F200 / provisional KI#523 with the lower-clamp proof and adds F201 / provisional KI#524; both remain pending independent review and implementation. The progress summary now records queue 041 open at WIP04 with F202 unused.
- F180-F182 / KI#503-KI#505 have been independently reviewed and fixed. The accepted repairs add currently available irrigated Farm food to SAS low-food valuation, replace AdvCiv's existing-improvement-count capability proxy with current Build/feature technology and plot legality, and make `countBadTiles` log its freshly computed unrevealed count.
- The repaired Debug-opt DLL compiled successfully, and a full Continents autoplay completed normally. KI#503-KI#504's exact transitions remain source-verified; KI#505 additionally remains directly observable when found-value logging evaluates a BFC with unrevealed plots.
- Current source and Known Issues remain authoritative for reviewed implementation state. C018-WIP04 remains durable investigation evidence for the still-provisional queue-041 findings. (GPT-5.6-Sol)


===============================================================================
C018-WIP05 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- FIFTH DURABLE HANDOFF
===============================================================================

SOURCE / CURSOR RECONCILIATION
------------------------------
Authoritative SAS source remains the user-supplied snapshot:
    AdvCiv-SAS_light_source_20260827T084943.zip

Local comparison controls remain:
    AdvCiv-1.14(2).zip
    Civ4-K-Mod-master-1.46.zip
    Civ4CE-main(7).zip

C018-WIP04 is an exact byte prefix of this checkpoint. Investigation continued from
F202 unused, with a second-proof pass over queued group attacks and a systematic
search for synchronous non-head actions that can defeat the SAS KI#319 no-progress
tripwire. The earlier WIP04 rejection of AI_getBombardTurns' synthetic global air
support was also reopened against all live callers and current SAS air-unit data.

F202 / provisional KI#525 -- CONFIRMED
--------------------------------------
AdvCiv-SAS KI#319 regression: consuming a stale queued group-attack flag can be a
legitimate state transition that leaves every currently snapshotted field unchanged.
The tripwire can therefore push MISSION_SKIP on a still-movable group immediately
after K-Mod's queued-combat retry state has served its purpose.

Primary root:
    CvSelectionGroupAI::AI_update

Cross-file mechanics:
    CvSelectionGroup::groupAttack
    CvSelectionGroup::updateTimers
    CvUnit::updateCombat
    CvSelectionGroup::groupMove

IMPORTANT CORRECTION TO THE EARLY WIP HYPOTHESIS
-------------------------------------------------
The first exploratory explanation was wrong in one important respect and is explicitly
superseded here: a unit waiting to attack while another combat occupies the source or
destination plot IS busy. CvUnit::attack stores an attack plot; CvUnit::isAttacking
therefore returns true, CvUnit::isInCombat includes isAttacking, and
CvSelectionGroup::isBusy sees that combat state. The KI#319 tripwire is gated by
!isBusy(), so it cannot misfire merely while the queued attacker is waiting.

The surviving defect occurs later, after the wait has resolved.

REACHABLE STATE SEQUENCE
------------------------
1. CvSelectionGroup::groupAttack tries to attack a defended adjacent target while
   another combat is already using the source or destination plot.
2. K-Mod intentionally still calls pBestAttackUnit->attack so the unit receives an
   attackPlot and will retry through combat-timer processing. For an AI stack with
   more than one unit it also calls AI_queueGroupAttack(targetX, targetY), setting
   m_bGroupAttack = true.
3. While attackPlot remains set, the group is busy. This part is safe and is NOT the
   KI#319 failure.
4. The preceding combat can eliminate the final defender before the waiting attacker's
   CvUnit::updateCombat retry runs.
5. CvSelectionGroup::updateTimers later calls updateCombat on the waiting attacker.
   With pDefender == NULL, updateCombat:
       - clears the unit's attackPlot/combat unit;
       - calls getGroup()->groupMove(pPlot, true, ..., true) onto the now-vacant tile;
       - clears the normal mission queue;
       - returns.
6. Neither setAttackPlot(NULL), groupMove nor clearMissionQueue cancels
   CvSelectionGroupAI::m_bGroupAttack. The queued group-attack state therefore
   survives after the group has already advanced onto its old attack target.
7. groupMove's bEndMove=true does not call finishMoves. It controls move animation;
   CvUnit::move still charges ordinary plot movement cost. A unit with more than one
   move can remain movable after the automatic advance.

The remaining-movement condition is live in current SAS rather than theoretical.
Current land attackers include, among others:
    SELF_PROPELLED_ARTILLERY -- 2 moves, UNITAI_ATTACK_CITY
    LEVITATION_ARTILLERY    -- 3 moves, UNITAI_ATTACK_CITY
and multiple 2+ move UNITAI_ATTACK units. On an ordinary one-move-cost advance, at
least one unit can therefore remain ready after the queued combat resolves.

THE KI#319 FALSE POSITIVE
-------------------------
On the next CvSelectionGroupAI::AI_update for that still-movable group:

1. The while condition admits the group because AI_isGroupAttack is still true and
   the group is no longer busy (and in the demonstrated fast-unit case it is also
   still movable).
2. SAS takes its current no-progress snapshot BEFORE consuming the queued attack:
       head ID / UnitAI / x / y / head moves / mission-queue length.
3. The queued-attack branch executes:
       AI_cancelGroupAttack();
       groupAttack(oldTargetX, oldTargetY, MOVE_DIRECT_ATTACK, ...);
4. The group is already standing on oldTargetX/Y after the no-defender auto-advance.
   groupAttack therefore fails immediately at its non-air adjacency guard because
   stepDistance(currentPlot, oldTargetPlot) == 0 rather than 1.
5. No unit moves, no mission is queued, and the head identity/role/plot/moves remain
   exactly as snapshotted. The only intentional state change is
       AI_isGroupAttack(): true -> false.
6. If any unit can still move, !isBusy() && readyToMove(true) is true. Because the
   SAS tripwire does not snapshot the queued-attack flag, it classifies the iteration
   as no progress and pushes MISSION_SKIP.

Base AdvCiv 1.14 has the same K-Mod queued-combat machinery but not the SAS KI#319
tripwire. After the stale retry is consumed, its while-loop can continue and let the
still-ready group choose a normal action. SAS therefore suppresses inherited
same-turn reassessment solely because the progress predicate omits a live AI state
transition.

REPAIR BOUNDARY
---------------
F202 needs the queued group-attack state in the no-progress signature, e.g. snapshot:

    const bool bGroupAttackBefore = AI_isGroupAttack();

and require:

    AI_isGroupAttack() == bGroupAttackBefore

before applying the fallback MISSION_SKIP.

This is independent of the already-proved KI#319 dimensions:
- F199 / KI#522: group cargo can change with all current head snapshots stable;
- F201 / KI#524: group membership can change with all current head snapshots stable;
- F202 / KI#525: queued group-attack state can be intentionally consumed with all
  current head/membership/cargo state otherwise stable.

No existing KI#525 or F202 collision was found in the reconciled album / Known Issues
snapshot at promotion time.


F203 / provisional KI#526 -- CONFIRMED
--------------------------------------
AdvCiv-SAS KI#319 regression: a synchronous immediate mission can deliberately spend
movement on a NON-HEAD member, change the game world, and return with an empty mission
queue while the head remains completely unchanged. The current head-centric progress
snapshot then pushes MISSION_SKIP at exactly the point K-Mod explicitly intended the
AI group to reconsider its action.

Primary root:
    CvSelectionGroupAI::AI_update

Concrete cross-file trigger:
    CvUnitAI::AI_pillageRange
    CvSelectionGroup::pushMission / startMission / continueMission
    CvSelectionGroupAI::AI_bestUnitForMission(MISSION_PILLAGE)
    CvUnit::pillage

PILLAGE HAS AN EXPLICIT K-MOD SAME-TURN RECONSIDERATION CONTRACT
---------------------------------------------------------------
For MISSION_PILLAGE, CvSelectionGroup::startMission repeatedly asks
AI_bestUnitForMission for the best movable pillager. After a successful AI pillage it
contains the explicit K-Mod rule:

    // AI groups might want to reconsider their action after pillaging
    if (!isHuman() && canAllMove())
        break;

That is not an inferred preference. K-Mod deliberately stops the immediate pillage
loop while the whole group can still move so normal unit AI can reconsider what to do
with the remaining movement.

The mission lifecycle is synchronous for an AI command:
1. CvUnitAI::AI_pillageRange, when already standing on its chosen enemy pillage plot,
   calls getGroup()->pushMission(MISSION_PILLAGE, ...) and returns true.
2. pushMission inserts the AI mission with immediate activation.
3. startMission executes the pillage before pushMission returns.
4. CvUnit::pillage changes the route/improvement and charges exactly one
   MOVE_DENOMINATOR of movement to the selected unit.
5. If all members can still move, the K-Mod break above stops further immediate
   pillaging so the group can reconsider.
6. Because the group is not busy, startMission enters continueMission. The immediate
   MISSION_PILLAGE is a one-step mission; continueMission marks it done and deletes
   the mission node.
7. Control therefore returns to CvUnitAI with the mission queue back at its original
   length (normally zero).

NON-HEAD REACHABILITY IN THE CURRENT 08:49 SAS SNAPSHOT
-------------------------------------------------------
The first exploratory example incorrectly described Catapult and Horse Archer as both
UNITAI_ATTACK. Current XML explicitly prevents Catapult from using UNITAI_ATTACK.
That stale role claim is discarded here.

The actual current mixed-role route is ordinary and explicitly supported by K-Mod:
- Catapult:
      DefaultUnitAI = UNITAI_ATTACK_CITY
      1 move, strength 5, collateral damage, bombard 8
      AI_groupFirstVal as ATTACK_CITY + bombard = 19
- Horse Archer:
      DefaultUnitAI = UNITAI_ATTACK
      2 moves, strength 6, withdrawal 20
      AI_groupFirstVal as ATTACK + withdrawal = 14

CvUnitAI::AI_attackMove for a UNITAI_ATTACK unit repeatedly calls
AI_omniGroup(UNITAI_ATTACK_CITY, ...). AI_canGroupWithAIType permits this outside the
small newly-acquired-city exception, AI_allowGroup selects a target whose head really
is UNITAI_ATTACK_CITY, and AI_omniGroup can merge the caller's group into that target
group. The member UnitAI roles are not homogenized merely by this merge.

Once merged, CvSelectionGroup::addUnit ordering makes the Catapult the group head
(first-value 19 versus Horse Archer 14). CvUnitAI::AI_attackCityMove consequently
drives the mixed group and contains live AI_pillageRange paths in enemy territory.

AI_bestUnitForMission(MISSION_PILLAGE) is intentionally independent of head identity
and strongly favors fast units. Its priority is multiplied by movesLeft after other
adjustments. On an eligible enemy improvement:
- the 2-move non-head Horse Archer can therefore be selected ahead of the 1-move
  Catapult head;
- Horse Archer pillages and spends one movement denominator, leaving one move;
- the Catapult has not moved;
- canAllMove() remains true, activating K-Mod's explicit "reconsider" break.

THE KI#319 FALSE POSITIVE
-------------------------
Before the head's CvUnitAI::AI_update call, SAS snapshots:
    head ID / UnitAI / x / y / HEAD moves / mission-queue length.

After the synchronous non-head pillage returns:
- the route/improvement has changed;
- Horse Archer movement has decreased;
- group membership is unchanged;
- cargo is unchanged;
- queued-group-attack state is unchanged;
- Catapult head ID, role, x/y and moves are unchanged;
- the transient MISSION_PILLAGE has already been deleted, so queue length is again
  the same as before;
- the group is awake/non-busy and can still move.

Thus every field in the current KI#319 predicate compares equal. SAS immediately
pushes MISSION_SKIP and ends the group's turn, overriding the inherited K-Mod comment
and control flow that deliberately left the group movable so it could reconsider.

This is an independent missing state dimension. F199 cargo, F201 membership and F202
queued-attack snapshots would all still compare equal in the pillage case.

REPAIR BOUNDARY
---------------
The no-progress signature needs to observe movement progress by the group as a whole,
not only movesLeft() on the current head. A conservative low-cost dimension is an
aggregate of member remaining movement before and after the attempted AI action, e.g.
a sum of movesLeft() across current group members, combined with F201's explicit
membership-count check.

Conceptually:

    iGroupMovesBefore = sum(movesLeft() for every member)

and the fallback MISSION_SKIP is allowed only if the corresponding aggregate is still
identical afterward.

The exact implementation can be kept local to the KI#319 guard. There is no need to
change MISSION_PILLAGE itself; its K-Mod reconsideration behavior is coherent. The SAS
tripwire is the layer suppressing it.

MISSION_PILLAGE is the clean proof because K-Mod states the intended reconsideration
in a comment and current mixed groups make non-head selection ordinary. Other
immediate missions selected through AI_bestUnitForMission can be checked as same-root
siblings, but they should not receive separate F numbers merely for exercising the
same omitted non-head-movement dimension.

No existing KI#526 or F203 collision was found in the reconciled album / Known Issues
snapshot at promotion time.


KI#319 PROGRESS-SIGNATURE STATUS AFTER F199/F201/F202/F203
----------------------------------------------------------
The investigation no longer supports treating KI#319 progress as a head-only concept.
Four independent, concretely reachable state dimensions have now crossed the bar:

    F199 / KI#522  group cargo total          -- getCargo()
    F201 / KI#524  group membership           -- getNumUnits()
    F202 / KI#525  queued group-attack state  -- AI_isGroupAttack()
    F203 / KI#526  non-head movement progress -- aggregate member movesLeft()

These should be combined with the already-existing head ID / UnitAI / plot / head
moves / queue-length checks. A small local "progress signature" helper would be a
reasonable cleanup if implementation review prefers it, but the audit does not need a
large generic state hash: the four additions above each have a proved caller and can
be added conservatively.


REOPENED NEGATIVE CONTROL -- GLOBAL AIR SUPPORT IN AI_getBombardTurns
---------------------------------------------------------------------
WIP04 had rejected AI_getBombardTurns' synthetic global air-bomber support as an
intentional BBAI approximation rather than a correctness root. The full-history pass
reopened that judgment because the helper's comment says it approximates how many
turns "this group" would need, while its air support test is completely global:

    GET_PLAYER(getOwner()).AI_isDomainBombard(DOMAIN_AIR)

AI_isDomainBombard is AI_calculateTotalBombard(DOMAIN_AIR, 1) > 0.
AI_calculateTotalBombard counts owned unit classes across the entire civilization; it
has no target, range, area, air-base or mission-legality test. Any one air-bombard
unit anywhere therefore gives every evaluated land group:
- a synthetic bombard rate of 16; and
- bIgnoreBuildingDefense = true.

Current SAS confirms the locality mismatch is live:
    Fighter         bomb rate  8, air range  6
    Jet Fighter     bomb rate 12, air range 10
    Bomber          bomb rate 16, air range  8
    Stealth Bomber  bomb rate 20, air range 12
    Guided Missile  bomb rate 16, air range  5

A Fighter on another continent can therefore count as immediate support for a city it
cannot target. Quantitatively, even without invoking building-defense suppression,
an 80-defense city with no local group bombard can be estimated at roughly 80 turns
without the synthetic air flag but 5 turns with it. In AI_attackCityMove's projected
post-bombard formula, an immediate stack comparison of 80 can consequently be lifted
to about 112, enough to cross the ordinary 110 attack-city readiness ratio. The same
helper also affects AI_pickTargetCity valuation and AI_bombardCity's bombard threshold.

Despite that material effect, this pass STILL does not promote the seam to an F root.
The fixed +16 and empire-wide presence test are visibly inherited BBAI/K-Mod synthetic
strategic support rather than an accidental local coding error. A distant aircraft
may be intended as a crude proxy for air power that can be rebased by the time a land
stack reaches its target. The final direct city assault also rechecks real current
stack strength rather than blindly attacking on the projected value.

Without a save/log case showing the approximation causing a pathological target or
siege decision, changing it would be an AI-design refinement (e.g. target-local
reachable air support), not a sufficiently objective queue-041 defect under this
audit's current bar. The deeper caller/range analysis is preserved here so the seam
does not need to be rediscovered later.

Ancestry: the global-presence + fixed-16 behavior is inherited from BBAI through
K-Mod. AdvCiv moved the helper from CvSelectionGroup to CvSelectionGroupAI and used
the equivalent AI_isDomainBombard wrapper; it did not make the air-support test local.


ADDITIONAL CLOSURE / NEGATIVE CONTROL NOTES
-------------------------------------------
- clearMissionQueue does not call AI_cancelGroupAttack. This is a key F202 detail,
  not an accidental omission in the proof.
- CvUnit::setAttackPlot(NULL) likewise only clears attack coordinates / air-combat
  state; it does not clear the selection group's queued group-attack flag.
- CvSelectionGroup::groupMove's bEndMove flag controls ExecuteMove animation after
  movement. It does not call finishMoves, so F202's fast-unit residual-movement case
  survives.
- The current Catapult XML role correction above supersedes the exploratory
  "Catapult supports UNITAI_ATTACK" statement. Current Catapult explicitly places
  UNITAI_ATTACK in NotUnitAIs. The mixed ATTACK_CITY-head + ATTACK-member route is
  instead directly supported by AI_attackMove -> AI_omniGroup(UNITAI_ATTACK_CITY).
- No new root is being manufactured from the global-bomber approximation merely
  because its estimate is coarse.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP05
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
NEXT ROOT ID: F204 UNUSED

Next exact cursor: challenge the remaining immediate AI_bestUnitForMission mission
family as same-root coverage for F203 (without duplicating IDs), then finish the
full-history / mutable-state closure sweep for CvSelectionGroupAI.cpp. In particular,
look for any state transition that (a) intentionally asks the same group to rethink,
(b) leaves all now-proved KI#319 signature dimensions unchanged, and (c) is reachable
in current SAS. If no F204 survives, leave it unused and close the file only after the
history/diff checklist is exhausted.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP06 -- PRIORITY FILE 041 CvSelectionGroupAI.cpp -- CLOSURE PASS CONTINUES
===============================================================================

SOURCE / CURSOR
---------------
Authoritative current source snapshot remains:
    AdvCiv-SAS_light_source_20260827T084943.zip

This append continues directly from C018-WIP05. F198-F203 / provisional
KI#521-KI#526 remain frozen. The post-WIP05 pass challenged the remaining KI#319
state dimensions, the current SAS-only attacker/mission changes, and selected
K-Mod / AdvCiv / Civ4CE historical controls before promoting any new root.


F204 / provisional KI#527 -- CONFIRMED
--------------------------------------
Inherited AdvCiv debug assertion / diagnostic contract defect: Debug Mode combat
help explicitly accepts a selected DOMAIN_AIR group, then its Shift-hover stack
comparison calls AI_compareStacks -> AI_sumStrength even though AdvCiv explicitly
asserts that AI_sumStrength does not support DOMAIN_AIR.

Root boundary:
    CvSelectionGroupAI::AI_compareStacks / AI_sumStrength
Caller exposing the false invariant:
    CvGameTextMgr::setCombatPlotHelp debug Shift-hover diagnostics

CURRENT SOURCE CONTRACT
-----------------------
CvGameTextMgr::setCombatPlotHelp deliberately accepts air selections:

    switch (kSelectionList.getDomainType())
    {
    ...
    case DOMAIN_AIR:
        bValid = true;
        break;
    ...
    }

For a visible defended target that the selected aircraft can attack, the helper
successfully obtains an air pAttacker and pDefender. In Debug Mode while Shift is
held it later executes:

    szTempBuffer.Format(L"\\nStack Compare Value = %d",
        kSelectionList.AI_compareStacks(pPlot, false, true));

AI_compareStacks preserves the group's domain when it is DOMAIN_AIR and passes that
domain to AI_sumStrength. AdvCiv's AI_sumStrength begins with:

    FAssert(eDomainType != DOMAIN_AIR && eDomainType != DOMAIN_IMMOBILE);
    // advc: Air combat strength isn't counted

Therefore a legal debug/developer UI path reaches a false assertion invariant:

    debug/assert-enabled DLL
      + in-game Debug Mode
      + Shift combat hover
      + selected Fighter/Bomber/etc.
      + valid defended air target
        -> AI_compareStacks(DOMAIN_AIR)
        -> AI_sumStrength(DOMAIN_AIR)
        -> FAssert fires.

This is not hypothetical domain data. Current SAS aircraft such as Fighter,
Jet Fighter, Bomber and Stealth Bomber use DOMAIN_AIR, iCombat=0 and a separate
positive iAirCombat value. The combat-help domain switch explicitly treats them as
valid rather than rejecting the UI path first.

RELEASE-BUILD CONSEQUENCE / SCOPE
---------------------------------
This should be described narrowly as a debug/assertion and diagnostic defect, not a
release gameplay crash. Without assertions, current AdvCiv/SAS AI_sumStrength also
filters out units for which canFight() is false; canFight() is based on baseCombatStr
and current aircraft have iCombat=0. The resulting air "Stack Compare Value" is
therefore not a meaningful air-power comparison even when execution continues.

Normal strategic AI callers examined in this file use land/sea groups and do not
rely on DOMAIN_AIR AI_compareStacks. The confirmed defect is the debug UI exposing a
function whose AdvCiv contract explicitly excludes air.

ANCESTRY / CONTROLS
-------------------
- K-Mod and Civ4CE already carry the debug "Stack Compare Value" combat-help family.
- K-Mod's older AI_sumStrength still contains an air branch, although it ultimately
  relies on the same land-style currEffectiveStr machinery.
- AdvCiv practical 1941 (60e9fadec3, "Some misc. refactoring, fixed assertion.")
  explicitly introduced:

      FAssert(eDomainType != DOMAIN_AIR && eDomainType != DOMAIN_IMMOBILE);

  and removed the old air branch with the comment that air combat strength is not
  counted / currEffectiveStr is 0 for air units.
- That AdvCiv change did not correspondingly gate the existing debug combat-help
  Stack Compare diagnostic for DOMAIN_AIR selections.
- Current SAS inherits both sides of that mismatch unchanged in substance.
- No existing KI#527 or F204 collision was found in the reconciled album / Known
  Issues snapshot at promotion time.

NARROW REPAIR BOUNDARY
----------------------
The smallest safe repair is at the diagnostic boundary: do not call/display the
land/sea Stack Compare value for DOMAIN_AIR selections (or display an explicit N/A).
A broader alternative would be to implement a genuinely air-aware stack-strength
metric using air combat strength, but merely removing the assertion while retaining
AI_sumStrength's current air filtering would only hide the contract problem and keep
showing a meaningless diagnostic.


POST-WIP05 KI#319 CLOSURE / NEGATIVE CONTROLS
----------------------------------------------
The no-progress tripwire was challenged against additional mutable state rather than
blindly expanding its snapshot.

1. forceUpdate -- REJECTED AS AN OMITTED PROGRESS DIMENSION
   Current source has only one setForceUpdate(true) producer, in
   CvSelectionGroup::doTurn before AI_update. AI_update clears that flag at loop
   entry. No current CvUnitAI decision silently sets it again while returning through
   the KI#319 guard, so snapshotting forceUpdate would be redundant in current SAS.

2. Activity / MissionAI changes -- NO INDEPENDENT ROOT
   Worker wake/recovery paths that clear retreat/hold state fall through into normal
   worker decision logic in the same CvUnitAI call. Sea-transport wake-and-return
   paths with a meaningful same-group state change are the cargo-unload family already
   covered by F199. Direct MissionAI mutations examined either accompany ordinary
   mission/group progress or operate on another joining group.

3. Immediate best-unit missions -- SAME-ROOT COVERAGE FOR F203
   MISSION_BOMBARD, MISSION_AIRBOMB and MISSION_PARADROP can also act with members
   selected independently of the head. Their successful paths spend member action /
   movement and/or alter membership/plot state. They reinforce the need for F203's
   group-wide movement observation (plus F201 membership) rather than creating a new
   omitted state dimension. MISSION_PILLAGE remains the strongest proof because K-Mod
   explicitly says the still-movable AI group should reconsider afterward.

4. Global declaration-of-war state -- REJECTED AFTER CALLER TRACE
   AI_considerDOW can change team state without itself moving a group, so it was a
   plausible nonlocal progress class. Its live callers were traced. They continue in
   the same move routine after the declaration and subsequently issue an action/skip,
   queue movement, or encounter the normal bump/immobilization effects. No current
   route was found where declaration alone returns to the KI#319 no-progress test
   with every local signature field unchanged while same-turn reconsideration is
   still required.

5. MISSION_SKIP fallback cargo semantics -- CAUTION, NOT A NEW ROOT
   The fallback mission has inherited semantics beyond a bare finishMoves in some
   cargo contexts, but no case was proved where a *correctly detected* no-progress
   state is uniquely harmed by those semantics. The known false positives should be
   fixed at the KI#319 progress predicate rather than by redefining MISSION_SKIP.


SAS LOW-POWER ATTACKER ORDER -- AIR-DOMAIN NEGATIVE CONTROL
------------------------------------------------------------
A real metric mismatch was found but is dormant under current normal AI grouping, so
it is deliberately NOT F205.

AI_currEffectiveStr calls the land-style currEffectiveStr/maxCombatStr path. The
latter returns zero when baseCombatStr()==0. Current SAS aircraft confirm the mismatch:

    Fighter          iCombat 0, iAirCombat 12
    Jet Fighter      iCombat 0, iAirCombat 24
    Bomber           iCombat 0, iAirCombat 16
    Stealth Bomber   iCombat 0, iAirCombat 20
    Guided Missile   iCombat 0, iAirCombat 40

AI_currEffectiveStr ultimately clamps its result to at least 1, so SAS's custom
bPreferLowPower branch would not meaningfully rank different aircraft by their true
air strength if several AI aircraft were in one attacking selection group.

Reachability review prevents promotion:
- CvPlayer::initUnit / CvUnit::setXY creates each new unit in its own selection group.
- AI_attackAirMove / AI_defenseAirMove / AI_carrierAirMove / AI_missileAirMove do not
  use AI_group or AI_omniGroup to merge aircraft attackers.
- Carrier loading changes transport linkage, not selection-group membership.
- AI_setUnitAIType separates the unit from its group on role conversion.
- SAS's low-power mode is disabled for human-owned groups.

Thus normal current AI air attacks are effectively singleton-group selections; there
is no attacker-order choice for the bad metric to corrupt. Preserve this as a dormant
robustness hazard if future SAS AI work ever groups aircraft, not as a current root.

The low-power branch's current numeric ranking itself also remained bounded: the
AI_currEffectiveStr contribution is capped at 25,000 and the tactical
bombard/collateral -1,000,000 offset cannot approach signed underflow. No arithmetic
root survived there.


OTHER POST-WIP05 NEGATIVE CONTROLS
----------------------------------
- AI_bestUnitForMission's SAS KI#394 Air Bomb repair was rechecked. Requiring
  canAirBombAt(target) before ranking is symmetric for city and improvement targets
  and prevents the inherited invalid-first-aircraft selection without blocking a
  valid group member. No regression found.
- AI_attackOdds' 100 return for a defenderless plot remains a strategic "nothing to
  fight" convention for its live group-level bombard caller, not proof that an
  attacker exists; attack legality is handled elsewhere.
- AI_compareStacks integer scaling still has only a theoretical extreme-stack
  overflow ceiling. Current per-unit effective strength is capped and no ordinary
  SAS-reachable stack size near the dangerous range was established.
- AI_bestUnitForMission(MISSION_BOMBARD) deriving bombardTarget through a group member
  remains safe because actual per-unit canBombard filtering is applied before the
  selected unit acts.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP06
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
NEXT ROOT ID: F205 UNUSED

Next exact cursor: continue the final history/function closure from F205 unused.
Challenge the remaining AI_getBombardTurns/path/MissionAI/fullness/ejection/serialization
surfaces and any unreviewed substantive historical hunk, with special attention to
cross-file callers whose domain or ownership assumptions are broader than the local
helper's contract. Do not consume F205 merely to fill an ID. If the remaining checks
close cleanly, freeze the rejection checklist and close queue 041 before moving to the
next priority file (queue 031 UWAICache.cpp).

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP07 APPEND — QUEUE 041 CvSelectionGroupAI.cpp CONTINUED
===============================================================================

This checkpoint continues from C018-WIP06. F198/KI#521 through F204/KI#527
remain frozen. The post-WIP06 pass completed the all-Gunship / undefended-city
strength seam, expanded the historical AI_update analysis, validated additional
helper assumptions against current SAS XML and callers, and confirmed one new
inherited debug assertion defect.


F205 / provisional KI#528 -- CONFIRMED
--------------------------------------
Inherited AdvCiv debug assertion contract defect: a legal human Bombard mission
against an undefended enemy city reaches AI_bestUnitForMission(MISSION_BOMBARD),
whose human-only branch falsely asserts that the city must have at least one
defender.

Root boundary:
    CvSelectionGroupAI::AI_bestUnitForMission(MISSION_BOMBARD)
Legal caller:
    CvSelectionGroup::startMission for an ordinary human Bombard command
Underlying legality contract:
    CvUnit::canBombard -> CvUnit::bombardTarget -> CvCity::isBombardable

CURRENT SOURCE CONTRADICTION
----------------------------
AI_bestUnitForMission first resolves the bombard target and counts defenders:

    iDefenders = pMissionPlot->plotCount(
            PUF_canDefendEnemy, getOwner(), false);

For an AI group it may use AI_isEvacuating. For a human group it instead enters:

    else
    {
        FAssertMsg(iDefenders > 0, "AI bombards undefended city");
        ...
    }

The asserted invariant is not part of Bombard legality. CvUnit::canBombard only
requires a positive bombard rate, remaining attack capacity, non-cargo status and a
non-NULL bombardTarget. bombardTarget selects an adjacent city for which
CvCity::isBombardable(this) is true.

CvCity::isBombardable deliberately does NOT require a defending unit. In current
AdvCiv/SAS it returns true for an enemy city with positive defense and additionally
contains human-only support for bombarding a defenseless city so recovered defense
can be kept at zero:

    /* Allow bombarding defenseless cities (to keep their def at 0), ... */
    (getTotalDefense(false) > 0 && pUnit->isHuman() && !isBombarded())

The normal mission UI / mission execution path preserves that contract:

    CvSelectionGroup::canDoMission(MISSION_BOMBARD)
      -> any movable member with pUnit->canBombard(*pPlot) makes the mission legal

    CvSelectionGroup::startMission()
      -> MISSION_BOMBARD
      -> AI().AI_bestUnitForMission(MISSION_BOMBARD)

Thus a human siege unit adjacent to an enemy city that has bombardable defense but
zero defenders is a supported mission state and reaches the assertion directly.

CONSEQUENCE / SEVERITY
----------------------
This is a debug/assert-enabled DLL defect, not a release-gameplay crash. In a debug
build the legal human Bombard command can fire the assertion. In a release build the
surrounding calculations remain bounded:
- the attacker/defender ratio uses std::max(iDefenders, 1) where division needs a
  nonzero denominator;
- the bombard-priority denominator is 15 + min(iDefenders, collateralMaxUnits), so
  zero defenders is safe;
- empty-target attack odds may drive some ordering terms to zero, but selection still
  has a valid finite priority and Bombard itself remains legal.

The finding should therefore be described narrowly as a false debug invariant / bad
assertion on a legal human mission, analogous in severity class (not root cause) to
F204's legal debug UI -> unsupported air stack-comparison assertion.

ANCESTRY
--------
- AdvCiv's human-only ability to bombard defenseless cities predates the assertion;
  it is already present in the early advc.004c history (practical 1253).
- AdvCiv practical 2778 (4098fbb248, "Various UI improvements for bombard and air
  bomb missions") further preserves/extends the human Bombard UI contract.
- AdvCiv practical 2809 (1ce5042732, "Smarter code for order of group bombardment")
  introduced AI_bestUnitForMission's current bombard-order logic and the assertion:

      FAssertMsg(iDefenders > 0, "AI bombards undefended city");

  This was added inside the explicit isHuman() branch despite the already-supported
  human undefended-city bombard case.
- Current SAS inherits both the mission legality and the false assertion unchanged in
  substance.

NARROW REPAIR BOUNDARY
----------------------
Remove or relax the iDefenders > 0 assertion. Do not forbid the human mission in
canBombard/isBombardable: AdvCiv deliberately supports it, and the surrounding
ordering math is already zero-defender-safe. No broader behavior change is required
for this finding.


POST-WIP06 REJECTIONS / CLOSURE EVIDENCE
-----------------------------------------

1. K-Mod undefended-city AI_sumStrength + no-city-capture units -- REJECTED

K-Mod change 522 deliberately skips target-specific canMoveInto filtering in
AI_sumStrength(..., bCheckCanAttack=true) when the target has no visible defender.
That means a no-city-capture unit such as a Gunship can contribute nominal attack
strength against an empty enemy city.

The local oddity is real, but no distinct wrong decision survived the caller trace:
- an all-Gunship/no-capture group cannot actually enter an empty enemy city;
  CvUnit::canMoveInto rejects isNoCityCapture() against an enemy city, while the
  attack alternative has no defender to attack;
- AI_stackAttackCity checks group canMoveOrAttackInto before committing the assault;
- if a mixed stack has at least one real capturer, the destination is legal and
  AdvCiv's groupMove explicitly moves capture-capable units first, then advances
  no-city-capture units such as Gunships afterward;
- AI_attackCityMove's readiness logic also requires a meaningful city-capture-capable
  fraction before treating a city-assault stack as ready;
- UNITAI_ATTACK groups may still approach a city as offensive support, but that
  behavior exists independently of this empty-city strength exception and is part of
  K-Mod's broader city-target logic;
- AI_stackVsStack only evaluates plots with enemies, so the empty-target exemption is
  irrelevant there.

Therefore the extra Gunship strength against an empty city is a coarse K-Mod
estimate, not a demonstrated independent defect. F205 was left unused until the
separate bombard assertion crossed the bar.


2. AI_getBombardTurns global air-support approximation -- REJECTED AGAIN

The earlier reopened BBAI seam remains deliberately unpromoted. AI_getBombardTurns
uses owner-wide AI_isDomainBombard(DOMAIN_AIR) and can therefore grant a land group a
synthetic air bombard rate / building-defense bypass even when the aircraft is
nowhere near the target. Caller math proves that this can bias projected target and
approach values, but the final assault rechecks real stack strength and the code is
visibly written as a strategic synthetic approximation inherited from BBAI.

Without save/log evidence showing a pathological strategic consequence, retain this
as an intentional coarse heuristic rather than manufacturing a root.


3. AI_isFull assumptions validated against current SAS XML

The two-pass special-cargo code assumes that units with SpecialCargo are actual cargo
hulls. Current CIV4UnitInfos.xml was checked mechanically. Every non-NONE
SpecialCargo unit has positive cargo capacity:

    Missile Cruiser     SPECIALUNIT_MISSILE   iCargo 4
    Attack Submarine    SPECIALUNIT_PEOPLE    iCargo 1
    Carrier             SPECIALUNIT_FIGHTER   iCargo 3

There is no current special-cargo / zero-capacity unit that can inflate
specialCargoCount independently of cargoCount and falsify the >= comparison. Caller
review in assault-sea and missile-carrier AI also remains consistent with the old
intended two-pass behavior. No root.


4. MissionAI-only progress -- NO NEW KI#319 SIBLING

Direct AI_setMissionAI producers were revisited. Relevant current-unit paths do not
produce a missionless false-return state requiring an independent MissionAI snapshot:
- AI_upgrade returns success/abort semantics after replacing/splitting the acting
  unit and, when needed, assigns MISSIONAI_GROUP to the new unit's new group;
- worker retreat/HOLD recovery clears MissionAI and wakes the group but falls through
  to ordinary worker decision logic; its fallback ultimately takes an action or
  queues MISSION_SKIP;
- assault-transport cancellation of MISSIONAI_GROUP acts on *other* joining groups;
- the current transport's unload-and-wake return is the cargo-state family already
  captured by F199.

MissionAI therefore remains metadata accompanying already-observed progress, not an
independent missing dimension under current callers.


5. Remaining assertion inventory mostly proved sound

After F204 and F205, the other live assertions in CvSelectionGroupAI.cpp were checked
against their producer/caller contracts:
- AI_getBestGroupAttacker attack odds are positive because the public unit odds path
  clamps attack odds to at least 1;
- AI_sacrificeValue is non-negative by its producer contract;
- bMaxSurvival implies !bSacrifice and human ownership at every current caller (the
  mode is used by human combat-help / Alt-key selection paths);
- owner/head assertions are reached on owned nonempty groups;
- the AI_isDeclareWar default switch has no uncovered current UnitAITypes (42/42 were
  enumerated earlier in this audit).

The two legal-caller contradictions found in this file are therefore specifically
F204's air debug Stack Compare and F205's human undefended-city Bombard assertion.


AI_UPDATE / KI#319 HISTORICAL CONTRACT — STRONGER CONTEXT
----------------------------------------------------------
AdvCiv practical 2219 (5e2d002497, "Fix false positive 'unit stuck in loop'") is an
important upstream signal. It changed the permanent AI_update fallback from:

    6 * (era + 1) + range(getNumUnits(), 4, 14)

to:

    6 * (era + 1) + max(getNumUnits(), 4)

because legitimate work by larger groups could require more loop iterations than the
old capped term allowed. Current SAS preserves this uncapped unit-count contribution.

SAS history then shows the same contract evolving again:
- SAS practical 5093 introduced KI#319's no-progress tripwire using only head x/y,
  head moves and mission-queue length after a Scout/Explorer spin was observed;
- SAS practical 6211 (KI#319 follow-up) added head identity and UnitAI role because a
  legitimate AI_setUnitAIType/head-replacement transition proved that the first
  signature was incomplete;
- the current audit has now confirmed further legitimate state dimensions:

      F199  group cargo count / unload progress
      F201  group membership count / non-head split progress
      F202  queued group-attack state
      F203  non-head member movement consumed by synchronous immediate missions

This lineage strongly argues against tightening the legacy attempt ceiling or merely
adding a one-off field for each testcase. The robust repair direction is a compact
*group progress signature*: preserve the already-tracked head identity/role/position,
then include group membership, cargo, queued group-attack state and a group-wide
movement/action summary sufficient to notice non-head work. The inherited attempt
ceiling should remain the ultimate fallback for genuinely unproductive loops.

This is architectural repair guidance only; no repository patch was made by this
audit checkpoint.


HISTORY / CONTROL STATUS SINCE WIP06
-------------------------------------
Additional substantive ancestry was sampled rather than treating all 139 touching
commits as equally meaningful:
- K-Mod 325's debug-only head-team strength-memory crash fix remains preserved in the
  later code lineage;
- K-Mod 636/661/825 group-head/pathfinder cleanups are represented by later current
  forms; no reverted behavioral regression was found;
- AdvCiv 1366's master-team war-plan lookup is still present in AI_isDeclareWar;
- AdvCiv 1395's AI_isFull restructuring was behavior-preserving and the resulting
  assumptions were validated against current SAS XML above;
- AdvCiv 1687's AI_currEffectiveStr stack-strength refactor remains the source of the
  explicit air-domain limitation already captured by F204's downstream debug caller;
- AdvCiv 2505's human attacker tie-break fix is present;
- AdvCiv 2676's const-cache strength-memory refactor remains consistent with current
  AI_compareStacks;
- AdvCiv 2809 is the introducing change for F205;
- AdvCiv 4035's fast-human-pillage skip list remains present and is compatible with
  F203's *AI* same-turn reconsideration contract;
- SAS practical 4416's precision repair in AI_ejectBestDefender is preserved.

Civ4CE was also used as an independent older-control check on the tail helpers. It
retains the older BBAI/K-Mod forms of AI_isDeclareWar, AI_isFull,
AI_getMissionAIUnit and AI_ejectBestDefender; no CE-only downstream fix was found that
invalidates the current classifications. CE predates AdvCiv's AI_bestUnitForMission
bombard-order code, so it naturally does not contain F205's assertion.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP07
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
NEXT ROOT ID: F206 UNUSED

Next exact cursor: continue the final queue-041 closure from F206 unused. Finish the
remaining substantive historical-hunk checklist and adversarial caller pass over
AI_isDeclareWar, path/stranded helpers, MissionAI/fullness/ejection/serialization and
any cross-file UI helper whose domain or mission legality is broader than the local
helper contract. Specifically continue looking for *independent* state dimensions or
legal-caller assertion/ownership/domain mismatches rather than variants already
covered by F199-F205. If no further defect survives, freeze the final rejection list
and close queue 041 before moving to the next priority file.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP08 APPEND — QUEUE 041 CvSelectionGroupAI.cpp CONTINUED (UNFINISHED)
===============================================================================

This is deliberately an unfinished durable handoff. C018-WIP07 remains an exact
byte prefix. F198/KI#521 through F205/KI#528 remain frozen. The post-WIP07 pass
strengthened F205, formally rejected the obsolete-sacrifice potential-enemy seam,
expanded the exact-path ancestry inventory, and confirmed one new SAS/inherited
contract-interaction defect. Queue 041 remains OPEN.


F205 / provisional KI#528 -- SAME ROOT, RELEASE-BUILD CONSEQUENCE ADDED
------------------------------------------------------------------------
WIP07 correctly proved the false debug assertion in the human branch of
AI_bestUnitForMission(MISSION_BOMBARD): a legal human Bombard of an undefended city
can reach

    FAssertMsg(iDefenders > 0, "AI bombards undefended city");

The deeper mission-order trace shows that the same bad assumption also has a release
build consequence; F205 is therefore stronger than the WIP07 description "debug
assertion only."

For an undefended target, CvUnitAI::AI_attackOdds returns 100 immediately because
there is no defender. The smart human bombard ordering then computes roughly:

    scaled rOdds = per100(pUnit->AI_attackOdds(pMissionPlot, false));
    rPriority *= (1 - rOdds);

Thus rOdds == 1 and EVERY eligible bombarder has the promotion/intent-derived
priority multiplied by zero. The code that AdvCiv practical 2809 added specifically
to derive a sensible human group-bombard order therefore collapses to equal zero
priority in exactly the supported undefended-city case.

This is consequential because CvSelectionGroup::startMission(MISSION_BOMBARD)
repeatedly asks AI_bestUnitForMission for the next unit and executes that unit's
bombard until the mission can no longer continue. Group iteration order can therefore
determine which bombarders spend their attack/movement state and which bombard
capacity is wasted once city defense reaches the stopping condition.

REVISED CONSEQUENCE / REPAIR BOUNDARY
-------------------------------------
F205 should no longer be described as debug-only:
- debug DLL: legal human undefended-city Bombard reaches the false assertion;
- release DLL: the same state zeros the intended smart bombard priorities and
  degenerates selection to iteration/tie order.

The root remains the same, so no new F-number is consumed. A correct repair must
handle iDefenders == 0 as a supported human bombard state rather than merely deleting
the assertion while leaving rOdds == 1 to erase the priority signal. One narrow
option is to bypass/neutralize the attack-odds attenuation when there is no defender;
the exact patch is left to implementation review.


POST-WIP07 REJECTION — SACRIFICE bPotentialEnemy MISMATCH
---------------------------------------------------------
The inherited AI_getBestGroupSacrifice helper and SAS's obsolete-sacrifice subscan
both call canMoveInto(..., attack=true) without forwarding bPotentialEnemy. This is
locally suspicious because AI_getBestGroupAttacker itself does use bPotentialEnemy
for ordinary attacker eligibility.

The exact live caller contract makes the omission consequence-neutral:

1. CvSelectionGroup::groupAttack first probes the prospective target through
   AI_getBestGroupAttacker(pDestPlot, true, ...), but this call uses the default
   bSacrifice=false.
2. groupAttack then requires pDestPlot->hasDefender(...) to establish a real enemy.
3. Only inside the subsequent real-combat loop does it call AI_getBestGroupAttacker
   with bSacrifice=true; that call explicitly passes bPotentialEnemy=false.
4. Human combat-help/selection callers that use the sacrifice/max-survival modes also
   pass false.

Therefore no reachable sacrifice-enabled caller relies on potential-war legality.
The stale/over-general bPotentialEnemy parameter in AI_getBestGroupSacrifice is API
awkwardness, not F206. Do not revive this candidate unless a new caller is added that
combines bSacrifice=true with bPotentialEnemy=true.


HISTORY INVENTORY CLARIFICATION
-------------------------------
The exact current light-source mirror was recounted by path. There are 134 reachable
commit-diff records whose patch actually touches
CvGameCoreDLL/CvSelectionGroupAI.cpp. The earlier working note saying 139 was a rough
inventory and should not be used as the exact-path count.

The newly inspected substantive hunks continue to preserve their intended later
forms: AdvCiv attacker tie-breaking / sacrifice refactors, AI_follow/path reset,
impassable separation, city-defense evaluation in AI_ejectBestDefender, collateral
weighting, and K-Mod separation/pathfinder changes did not expose a reverted fix.


F206 / provisional KI#529 -- CONFIRMED
--------------------------------------
AdvCiv-SAS / inherited-contract interaction: CvSelectionGroupAI::AI_isDeclareWar
assumes that ANY cargo aboard a UNITAI_ASSAULT_SEA group is invasion cargo and
therefore authorizes war. K-Mod/AdvCiv also allow assault transports to rescue any
stranded land-unit role. SAS KI#193 then added a below-threshold opportunistic launch
whose documentation says "military cargo" but whose implementation checks only
`iCargo > 0`. A rescued noncombat Missionary or Great Person can therefore make an
assault transport evaluate an invasion with zero actual attack strength. With Open
Borders to an imminent sneak-attack target, the pre-war amphibious path can be legal,
AI_considerDOW can declare war, and the post-DoW path can then become impossible.
The AI has declared war but cannot execute the assault that triggered it.

ROOT / INTERACTION BOUNDARY
---------------------------
Current queue-041 contract:

    CvSelectionGroupAI::AI_isDeclareWar
      case UNITAI_ASSAULT_SEA:
          return hasCargo();

Cross-file reachability that violates the inherited assumption:

    CvUnitAI::AI_handleStranded
    CvUnitAI::AI_pickupStranded
    CvUnitAI::AI_assaultSeaMove
    CvUnitAI::AI_assaultSeaTransport
    CvUnitAI::AI_transportGoTo / AI_considerPathDOW

SAS activation:

    SAS_AI_ASSAULT_SEA_OPPORTUNISTIC_TARGET_ENABLE = 1

The define and source comment say that "military cargo below those broad
army-assembly thresholds" should receive the target-specific launch test. The actual
gate is:

    if (bOpportunisticTargetEnable && iCargo > 0 &&
        iCargo < iTargetReinforcementSize)
        AI_assaultSeaTransport(bNoWarPlans);

`iCargo` is total cargo, not military/capture-capable cargo.

CONCRETE REACHABILITY PROOF
---------------------------
1. Noncombat units really can become stranded.

   AI_missionaryMove and AI_greatPersonMove both call AI_handleStranded after their
   normal useful actions / retreat attempts fail. AI_handleStranded treats access to
   an enemy city as proof of being non-stranded only for:

       (canFight() || isSpy())

   A Missionary is neither. If it cannot reach an owned city in its land area, it can
   therefore advertise MISSIONAI_STRANDED at a coastal pickup plot.

2. Assault transports really can rescue those roles.

   AI_assaultSeaMove contains generic calls to:

       AI_pickupStranded()

   with NO_UNITAI filtering. AI_pickupStranded only requires the target group to have
   MISSIONAI_STRANDED; it does not require a military UnitAI. Normal Galley/Galleon/
   Transport assault ships have SpecialCargo=NONE and DomainCargo=DOMAIN_LAND, so
   current Missionaries / Great People (DOMAIN_LAND, SPECIALUNIT_PEOPLE) are loadable.

3. The "inappropriate cargo" purge does not remove them.

   At the top of AI_assaultSeaMove the special cleanup only detects:

       getUnitAICargo(UNITAI_SETTLE) > 0 ||
       getUnitAICargo(UNITAI_WORKER) > 0

   Missionary and Great Person cargo survives into normal assault logic.

4. SAS KI#193 makes a single rescued civilian sufficient to enter target evaluation.

   Base K-Mod/AdvCiv broad invasion thresholds are normally several units
   (AI_neededCityAttackers has a base around four before further scaling). The SAS
   opportunistic branch intentionally bypasses those broad thresholds for a small
   capable military load, but tests only total iCargo. Thus a single rescued
   Missionary can call AI_assaultSeaTransport.

5. AI_assaultSeaTransport computes zero real attack strength but does not reject an
   undefended target for that reason.

   Cargo attack strength is accumulated only after:

       if (!pLoopUnit->canAttack() || pLoopUnit->isDead())
           continue;

   so a Missionary contributes zero landed/amphibious attack strength. However the
   defender-strength rejection executes only when iEnemyDefenders > 0. An undefended
   revealed coastal city can therefore remain a candidate with zero attack strength.

6. Open Borders makes the PRE-WAR amphibious destination legal to the civilian.

   GroupPathFinder validates an amphibious destination through the cargo group's
   canMoveOrAttackInto. Before war, a Missionary can ordinarily enter the rival city
   under Open Borders. Open Borders does not prevent canDeclareWar, and
   CvTeamAI::AI_mayAttack accepts an imminent undeclared war plan. Thus the target can
   pass both strategic and path legality while the teams are still at peace.

7. AI_considerPathDOW deliberately declares war at the amphibious target.

   AI_transportGoTo calls AI_considerPathDOW for MOVE_DECLARE_WAR paths.
   AI_considerDOW contains a special rule that if the group destination is an
   amphibious plot, "lets just assume that our cargo will need a DoW!" If the target
   team is sneak-attack-ready and canDeclareWar succeeds, it calls declareWar.

   CvSelectionGroupAI::AI_isDeclareWar compounds the same assumption for the assault
   ship by treating mere hasCargo() as sufficient war intent.

8. The same destination becomes illegal AFTER the DoW.

   Once war is declared, the foreign city is now an enemy city and the Open Borders
   relationship is gone. A Missionary cannot attack, and non-attack movement into an
   enemy city is rejected. AI_transportGoTo regenerates the path after declaring war;
   that regeneration can therefore fail and AI_assaultSeaTransport returns false.

The observable semantic failure is consequently stronger than "civilian cargo is
valued poorly": the transport can trigger a real war declaration and then discover
that the cargo cannot perform the landing that caused the declaration.

ANCESTRY / REGRESSION CLASSIFICATION
------------------------------------
- K-Mod already has the broad UNITAI_ASSAULT_SEA -> hasCargo() declaration rule.
- K-Mod and AdvCiv already let an assault transport fall back to generic
  AI_pickupStranded(), so the assumption that assault cargo is necessarily military
  was never perfectly encoded.
- In ordinary upstream flow, however, a single rescued civilian is far below the
  normal reinforcement/invasion threshold and normally does not launch an assault.
- SAS practical 6063 / KI#193 deliberately added the below-threshold launch path to
  let a *small military cargo* attack a target it can actually beat, but used total
  iCargo rather than a military/capture-capable cargo count. That turns the inherited
  loose cargo assumption into a much more directly reachable current-SAS defect.
- Civ4CE retains the old `UNITAI_ASSAULT_SEA` `hasCargo()` declaration rule; no
  downstream CE repair for this interaction was found. CE does not contain SAS
  KI#193's opportunistic activation path.

CONSEQUENCE / SEVERITY
----------------------
This is gameplay-visible and can affect diplomacy/war timing rather than merely a
log or assertion:
- a rescued civilian can be mistaken for the "military cargo" promised by KI#193;
- an undefended coastal city can pass assault target selection with zero attack
  strength;
- an imminent sneak war can be declared when the transport reaches the amphibious
  target window;
- after declaration, the civilian cargo cannot enter/attack the city, so the target
  path can disappear immediately after the irreversible diplomatic action;
- the AI may therefore start a war without executing the naval assault that justified
  that declaration, while also carrying a valuable civilian in the assault fleet.

The scenario is conditional (notably stranded civilian rescue + an imminent war and
path legality such as Open Borders), but every link is supported by current code and
normal engine states. No save-file reproduction is required to establish the
contract failure, though a targeted debug save would be useful implementation-side
confirmation.

REPAIR BOUNDARY
---------------
Do not fix this merely by special-casing Missionaries. The broken assumption is "any
cargo == invasion cargo."

Implementation review should require actual assault-capable cargo at one or both of
these boundaries:

1. SAS KI#193 opportunistic launch: replace total `iCargo > 0` as the capability gate
   with a military/capture-capable cargo measure and refuse zero landed attack
   strength before target selection; and/or
2. CvSelectionGroupAI::AI_isDeclareWar UNITAI_ASSAULT_SEA: do not authorize a DoW
   solely from hasCargo(); require cargo whose post-DoW movement/attack role can
   actually support the intended invasion.

A robust target-local repair should ensure the cargo can execute the post-DoW
amphibious destination, not merely enter the same city pre-war under Open Borders.
Keep generic stranded rescue available; rescuing civilians is useful and is not the
bug.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP08 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528 (release consequence strengthened here)
    F206 / provisional KI#529
NEXT ROOT ID: F207 UNUSED

Next exact cursor: continue queue-041 closure from F207 unused. Challenge F206 once
more against actual post-DoW amphibious movement and any alternate cargo cleanup path;
then continue the remaining behavior-changing history checklist, especially older
AI_isDeclareWar / naval-path / mission-order changes not yet explicitly classified.
Also continue the final KI#319 progress-signature closure for any nonlocal world/team
state transition not already covered by F199/F201/F202/F203. If no new independent
root survives, freeze the final rejection list and close queue 041.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP09 APPEND — QUEUE 041 CvSelectionGroupAI.cpp CONTINUED (UNFINISHED)
===============================================================================

This is deliberately another unfinished durable handoff. C018-WIP08 remains an exact
byte prefix. F198/KI#521 through F206/KI#529 remain frozen. The post-WIP08 pass
finished the post-declaration movement proof for F206, rejected another apparent
bombard-turn arithmetic problem, substantially closed the remaining KI#319 mutable-
state classes, and continued the behavior-changing K-Mod/AdvCiv ancestry checklist.
Queue 041 remains OPEN and F207 remains UNUSED.


F206 / provisional KI#529 -- POST-DoW FAILURE MECHANICS HARDENED
----------------------------------------------------------------
WIP08 proved the full pre-war reachability chain for rescued civilian cargo aboard a
UNITAI_ASSAULT_SEA transport and showed that AI_considerPathDOW can declare war for
that prospective amphibious landing. The remaining challenge was whether the engine
might preserve enough movement/path legality after the declaration that the
Missionary/Great Person could still execute the landing, which would weaken the
finding to merely an odd declaration trigger.

The current SAS implementation closes that objection:

1. CvTeam::declareWar removes bilateral deals BEFORE switching the teams to war.

   The function iterates all deals between the two teams and kills them at the top of
   the declaration sequence. Open Borders therefore disappears before setAtWar is
   installed.

2. declareWar then invalidates/rebuilds movement state.

   After the diplomatic/war state changes it verifies unit-valid plots, updates
   active paths, and calls CvSelectionGroup::resetPath(). The pre-war path cannot be
   treated as authoritative after the declaration.

3. AI_considerDOW itself resets the unit pathfinder immediately after declareWar.

   Its successful declaration branch does:

       kOurTeam.declareWar(...);
       getPathFinder().reset();
       return true;

4. AI_transportGoTo explicitly regenerates the assault path after a DoW.

   Immediately after AI_considerPathDOW returns true:

       if (!canMove())
           return true;
       if (!generatePath(kTargetPlot, eFlags, false))
           return false;

   Therefore the transport does not blindly execute the Open-Borders path that was
   valid before the declaration.

5. The rescued Missionary has no legal post-war amphibious destination mode.

   After Open Borders is gone and the city is hostile, ordinary non-attack entry is
   rejected for an enemy city. The Missionary cannot use the attack alternative
   either because it cannot attack. Thus the regenerated amphibious destination can
   fail immediately after the irreversible declaration.

This makes F206 a genuine declaration-then-no-executable-assault contract failure,
not merely a target-scoring curiosity. The repair recommendation from WIP08 stands:
capability must be based on actual military/capture-capable cargo (ideally target-
local post-DoW landing legality), while generic civilian stranded rescue should
remain available.


POST-WIP08 REJECTION — "ZERO BOMBARD RATE DIVISION" IN AI_getBombardTurns
---------------------------------------------------------------------------
AI_getBombardTurns uses max(1, iTotalBombardRate) in the denominator. At first glance,
a stack with no usable bombard unit therefore appears to receive a finite bombard-
turn estimate instead of an explicit infinity/failure state.

The live caller math makes the encoding intentional/consequence-safe:

- the important city-attack caller caps the returned estimate at 20 turns;
- the projected-defense formula credits progressively more defense removal only for
  smaller bombard-turn counts;
- a capped value of 20 corresponds to essentially NO useful projected defense
  removal and the target-value path also penalizes the remaining/max defense.

So the max(1, rate) denominator is functioning as an "effectively cannot bombard"
large-turn sentinel rather than pretending that defenses will disappear. This is not
F207.

The separate global-anywhere-bomber approximation remains the earlier recorded
heuristic rejection: it can bias strategic estimates but is visibly inherited BBAI
coarse modeling, and the final assault gate rechecks real current strength.


POST-WIP08 KI#319 PROGRESS-SIGNATURE CLOSURE
--------------------------------------------
The remaining mutable-state classes were challenged against the actual tripwire
entry contract rather than simply enumerating every field in CvSelectionGroup.

The tripwire can act only when the group is not busy and readyToMove(true). Current
readyToMove semantics require:

    (canAnyMove()) &&
    (isForceUpdate() ||
        (headMissionQueueNode() == NULL && getActivityType() == ACTIVITY_AWAKE)) &&
    !isBusy() && !isCargoBusy()

The remaining candidate classes therefore close as follows:

- activity changes: self-protecting. A non-AWAKE activity prevents readyToMove unless
  forceUpdate is active; wake-up paths that return immediately are already represented
  by the F199 cargo-unload family or continue into another decision in the same call.
- forceUpdate: no hidden in-loop progress source. Current SAS has only one
  setForceUpdate(true) producer, in CvSelectionGroup::doTurn before AI_update begins;
  AI_update consumes/clears it and UnitAI decision code does not silently set it back.
- delayed death: already protected by finishMoves / explicit delayed-death handling.
- head identity / UnitAI / position / head moves: covered by current KI#319 snapshots.
- membership: F201.
- cargo count: F199.
- queued group-attack state: F202.
- non-head member action/movement during a synchronous immediate mission: F203.
- MissionAI-only changes: reviewed live callers either accompany queue/activity/action
  state or are intentionally persistent cross-turn metadata (e.g. STRANDED/EXPLORE),
  not an independent false-return ready-group transition.
- global/team state such as declaration of war: all live AI_considerDOW callers traced
  so far proceed into movement/action, path invalidation/bumping, or another decision;
  no DoW-only local-no-change return reaches the tripwire independently.

This is strong evidence that F199/F201/F202/F203 form a coherent and close-to-complete
family of KI#319 blind spots rather than an unbounded list of arbitrary group fields.

Repair guidance remains architectural: use a compact GROUP progress signature rather
than continuing to add one testcase-specific field at a time. At minimum it should
cover head identity/role/position, group size/cargo, queued group-attack state, and a
small aggregate/hash of member identity + movement/action state. Keep the inherited
attempt ceiling as the ultimate safety fallback.


POST-WIP08 HISTORY / WHOLE-FILE CLOSURE PROGRESS
------------------------------------------------
The exact current light-source history inventory remains 134 reachable commit-diff
records whose patch actually touches CvGameCoreDLL/CvSelectionGroupAI.cpp.
Behavior-changing clusters are being separated from merge/style/refactor-only hunks.

The stack-strength lineage reviewed after WIP08 remains coherent:
- K-Mod visibility/memory/head-team changes (including the 323/325 cluster),
- K-Mod move/attack filtering redesign around AI_compareStacks/AI_sumStrength (346),
- AdvCiv migration to AI_currEffectiveStr (1687).

No second independent root appeared there. The air-domain exclusion is already the
specific debug/UI mismatch frozen as F204.

AI_getWeightedOdds was also reread as a whole after F200. No second independent
zero/empty/value defect has survived yet; the important lossy pre-/post-personality
clamp problem remains F200.

The historically fragile AI_update lineage remains fully relevant context: upstream
fixes repeatedly preserve legitimate extra loop iterations and non-obvious state
changes. AdvCiv practical 2219 explicitly fixed a false-positive "unit stuck in loop"
case by raising the attempt allowance with std::max(getNumUnits(), 4). This further
supports broadening SAS KI#319's definition of progress rather than tightening the
loop or assuming only the head unit can make meaningful progress.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP09 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529 (post-DoW path failure mechanically strengthened here)
NEXT ROOT ID: F207 UNUSED

Next exact cursor: continue queue-041 closure from F207 unused. The remaining work is
primarily the last behavior-changing history clusters, the remaining AI_isDeclareWar
role assumptions, and a final function-by-function adversarial pass over helpers not
yet challenged from an independent caller angle. The KI#319 state-class sweep is now
near closure; do not invent another sibling unless a distinct legitimate ready-group
state transition survives the full caller proof.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP10 CONTINUATION — queue 041 CvSelectionGroupAI.cpp still OPEN
ChatGPT-5.6-Sol audit continuation, 2026-08-27
===============================================================================

This is an intentionally unfinished durable checkpoint. C018-WIP09 is preserved
byte-for-byte as the prefix of the cumulative WIP10 file. No repository source files
were modified by ChatGPT during this audit.


F206 / provisional KI#529 — SAME-ROOT BLAST RADIUS: AI_enemyTargetMissions
---------------------------------------------------------------------------
WIP08-WIP09 proved that UNITAI_ASSAULT_SEA::AI_isDeclareWar uses hasCargo() as a
proxy for having invasion-capable cargo, while generic stranded rescue can place a
noncombat Missionary or Great Person aboard that transport. The same capability
assumption also leaks into strategic mission counting.

CvPlayerAI::AI_enemyTargetMissions scans selection groups whose mission plot belongs
to a prospective enemy team and counts a group when either war already exists or:

    pLoopSelectionGroup->AI_isDeclareWar(*pMissionPlot)

is true. When counted, it adds BOTH:

    pLoopSelectionGroup->getNumUnits();
    pLoopSelectionGroup->getCargo();

Therefore a rescued civilian cargo unit can be counted as committed military pressure
against the target team merely because the transport head is UNITAI_ASSAULT_SEA and
hasCargo() is true. This is not a separate root from F206: the same invalid assumption
("any assault-sea cargo is invasion-capable cargo") authorizes both the premature DoW
path and the strategic target-mission count.

Current known consumers include CvTeamAI war/counteroffensive logic. Treat this as
additional F206/KI#529 blast radius, not F207.


F207 / provisional KI#530 — HUMAN SMART BOMBARD COUNTS FOREIGN PEACEFUL UNITS AS ATTACKERS
------------------------------------------------------------------------------------------------
ROOT / ANCESTRY
---------------
Inherited from AdvCiv, introduced by practical commit 2809
("Smarter code for order of group bombardment"). K-Mod and Civ4CE do not contain
this AdvCiv smart group-bombard selector. Current AdvCiv 1.14 retains the same logic.

LIVE CODE
---------
In CvSelectionGroupAI::AI_bestUnitForMission(MISSION_BOMBARD), the human-only
"easy city capture" precheck intentionally scans the whole current plot rather than
only the selected group:

    int iAttackers = 0;
    FOR_EACH_UNIT_IN(pUnit, kAt)
    {
        if (!pUnit->canBombard(kAt) &&
            pUnit->canMoveOrAttackInto(*pMissionPlot))
        {
            iAttackers++;
            if (iAttackers >= 2 * iDefenders)
                break;
        }
    }

There is NO owner/player/team filter on pUnit.

WHY canMoveOrAttackInto IS NOT AN ATTACKER FILTER
-------------------------------------------------
CvUnit::canMoveOrAttackInto is:

    canMoveInto(kPlot, false, bDeclareWar) ||
    canMoveInto(kPlot, true, bDeclareWar, ...)

The first half is ordinary movement, not attack capability. Therefore a unit owned by
an unrelated third civilization can satisfy this predicate merely because it is at
peace with the target and Open Borders allows it to enter the target city normally.
It need not be at war with the target and need not be capable of helping the human
capture the city.

A legal state is:

- human A is at war with target B and is bombarding a defended B city;
- third-party C is at peace with both A and B and has the necessary Open Borders to
  share A's siege tile and to enter B's city;
- a non-bombarding C unit is on A's tile;
- for C, B's defenders/city are not enemies, so ordinary peaceful canMoveInto can
  succeed and canMoveOrAttackInto returns true;
- AdvCiv increments A's iAttackers for C's unit.

This directly contradicts the quantity being estimated: how many of the human's units
can help capture the city immediately.

CONCRETE THRESHOLD FLIP
-----------------------
The false count can change bEasyCityCapture, not merely an unused diagnostic.
For example, with one visible defender and a current selected-group stack comparison
of about 200%:

- one genuine human non-bombarding attacker gives iAttackers=1;
- the later test is roughly 1/1 * 2.0 = 2.0, below the >2.5 immediate-conquest gate;
- one peaceful foreign unit falsely counted as an attacker raises iAttackers to 2;
- the same test becomes 2/1 * 2.0 = 4.0 and bEasyCityCapture becomes true.

The foreign unit therefore flips a live control-flow decision.

PLAYER-VISIBLE BOMBARD-ORDER CONSEQUENCE
----------------------------------------
When bEasyCityCapture is true, every bombard candidate receives an extra factor:

    rPriority *= per100(pUnit->currHitPoints());

That can reverse which of the human's siege units performs the bombard action.
Current SAS Catapult bombard rate is 8, so a city with 8 defense remaining can be
reduced to zero by whichever Catapult is selected first; later candidates then need
not spend their action.

A representative legal ordering boundary uses two otherwise comparable Catapults:

- one damaged ordinary Catapult that the normal priority logic prefers to spend on
  bombardment because its attack odds are poorer;
- one full-health City Raider Catapult, whose +city-attack promotion is intentionally
  penalized by the human-intent bombard formula so it can be preserved for combat.

For representative resulting attack odds of 10% vs 30%, the relative pre-common-factor
priorities with 8 non-wasted bombard damage are approximately:

    damaged ordinary, no easy-capture flag:
        1.00 * (1 - .10) = .90
    full-health City Raider I, no easy-capture flag:
        .75 * (1 - .30) = .525

so the damaged ordinary unit is selected first. At 50% HP, a falsely enabled
bEasyCityCapture multiplies the first value by .50:

        .90 * .50 = .45 < .525

and the City Raider is selected first instead. Because one Catapult can remove the
remaining 8 defense, the false foreign attacker can determine which human siege unit
spends its action that turn.

The exact odds are state-dependent; the important proof is structural: the HP factor
is applied only when bEasyCityCapture is true, and there are ordinary legal priority
states on opposite sides of that multiplier boundary. The foreign unit can therefore
alter real mission ordering.

INTENT / REPAIR BOUNDARY
------------------------
Scanning the whole tile is defensible: other selection groups belonging to the SAME
human player can also attack after the bombardment, so restricting the estimate to
this one group would lose useful information. What is not defensible is counting units
the human does not own and cannot order, especially units whose predicate succeeds
through peaceful movement rather than attack.

Narrow repair: require pUnit->getOwner() == getOwner() before counting it as an
attacker, then retain the existing move-or-attack legality test for that owner's units.
A more explicit attack/capture-capability predicate could be reviewed separately, but
ownership alone fixes the demonstrated foreign-unit false positive without discarding
the intended whole-tile same-player information.

SEVERITY
--------
Human-control / mission-order defect, modest but real. It does not create an illegal
attack or crash; it can make AdvCiv's "smarter" group bombard order depend on neutral
third-party units and choose a different human unit to consume the bombard action.

STATUS: CONFIRMED as F207 / provisional KI#530.


POST-WIP09 HISTORY NOTE — practical 2809 intent
------------------------------------------------
The commit title is explicitly "Smarter code for order of group bombardment." It
introduced bEasyCityCapture, the whole-plot iAttackers scan, and the human promotion-
based bombard priority together. Later refactors retained this code. This supports the
interpretation that the whole-plot scan is meant to inform HUMAN bombard sequencing,
not to estimate unrelated civilizations' possible movement into the target.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP10 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
NEXT ROOT ID: F208 UNUSED

Next exact cursor: continue queue-041 closure from F208 unused. Resume the remaining
behavior-changing AdvCiv/K-Mod history clusters and final function-by-function caller
challenge. The KI#319 progress-state family is near closure; AI_isDeclareWar has one
confirmed capability leak (F206); human smart bombard now has the undefended-target
root F205 and foreign-attacker root F207. Do not merge these roots: F205 concerns the
no-defender assumption and 100%-odds collapse, whereas F207 concerns ownership and
peaceful movement contaminating the defended-city easy-capture estimate.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP11 CONTINUATION — queue 041 CvSelectionGroupAI.cpp still OPEN
ChatGPT-5.6-Sol audit continuation, 2026-08-27
===============================================================================

This is an intentionally unfinished durable checkpoint. C018-WIP10 is preserved
byte-for-byte as the prefix of the cumulative WIP11 file. No repository source files
were modified by ChatGPT during this audit.


POST-WIP10 SAS-DELTA CLOSURE NOTES
-----------------------------------
The remaining behavior-changing SAS hunks were challenged as a closed set rather than
through random current-source rereads.

- KI#319 tripwire work (SAS practical 5093 and follow-up 6211): the already frozen
  F199/F201/F202/F203 family remains the meaningful set of demonstrated missing
  progress dimensions. The remaining mutable state classes (activity, forceUpdate,
  MissionAI persistence, delayed death, force separation, etc.) have not produced an
  additional independent ready-group false positive after full caller tracing.
- Low-power attack ordering (5286): current numeric/tie ranges are safe. The apparent
  air-strength mismatch is real at the metric level because ordinary air units use
  iAirCombat rather than iCombat, but normal AI air attackers remain singleton groups;
  therefore low-power member ordering is moot for them under current grouping logic.
- Obsolete-sacrifice refinement (6097): the apparent bPotentialEnemy mismatch in its
  extra canMoveInto test is semantically dead on the reachable sacrifice-enabled path.
  groupAttack first establishes a real enemy and invokes sacrifice selection with
  bPotentialEnemy=false. Current arithmetic bounds were also previously checked safe.
- Mission eligibility fix (6256 / KI#394): current Air Bomb ranking now uses
  canAirBombAt(target), matching execution/help eligibility and closing the inherited
  invalid-first-aircraft problem without exposing a new asymmetry.
- Defender-ejection precision fix (4416): the earlier pointer/lifetime/caller audit
  remains clean; the changed precision/division math stays comfortably inside current
  reachable ranges.

An inherited BtS/K-Mod bNoBlitz seam was also challenged because SAS's low-power
selector depends on it. The flag is a stack-attack policy decided by the initial probe:
a normal stack pass avoids repeatedly reusing already-used Blitz units, whereas a
continuation already led by a used Blitz attacker permits the Blitz follow-up path.
The same structure exists in BtS/Civ4CE and K-Mod. SAS changes candidate ordering
inside that policy but does not create it. Rejected as F208.


F208 / provisional KI#531 — HUMAN SMART BOMBARD COUNTS LOADED CARGO AS CITY DEFENDERS
-------------------------------------------------------------------------------------
ROOT / ANCESTRY
---------------
Inherited from AdvCiv practical 2809, "Smarter code for order of group bombardment."
The same commit introduced the human bEasyCityCapture precheck, including this raw
plot defender count. K-Mod/Civ4CE do not contain this AdvCiv smart group-bombard
selector. Current AdvCiv 1.14 retains the same code.

LIVE CODE
---------
CvSelectionGroupAI::AI_bestUnitForMission(MISSION_BOMBARD) computes:

    iDefenders = pMissionPlot->plotCount(
            PUF_canDefendEnemy, getOwner(), false);

For the human branch, iDefenders controls both the attacker-count gate and the final
immediate-conquest test that decides bEasyCityCapture.

WHY PUF_canDefendEnemy IS TOO BROAD HERE
----------------------------------------
PUF_canDefendEnemy is essentially:

    PUF_canDefend(pUnit) && PUF_isEnemy(...)

and PUF_canDefend calls CvUnit::canDefend(). CvUnit::canDefend checks combat ability
and domain validity on the unit's current plot, but it does NOT reject isCargo().

By contrast, CvPlot::getBestDefender — the actual combat defender-selection path —
explicitly does:

    if (kUnit.isCargo())
        continue;

before considering a unit as defender.

Therefore a loaded LAND combat unit aboard a transport in an enemy coastal city can
satisfy PUF_canDefendEnemy on that land city tile even though, while still cargo, it
cannot be selected to defend the city in combat.

REACHABILITY
------------
Cargo is not automatically detached merely because its transport is in a friendly
city. CvUnit::setXY retains the transport link whenever the transport remains on the
same destination plot; unloading is an explicit operation (or occurs in other
specific transport-handling paths). Thus a coastal city can legally contain an enemy
transport with loaded land military cargo while the human bombards that city.

The earlier naval-domain suspicion is explicitly rejected: an ordinary SEA or AIR
unit in the land city fails CvUnit::canDefend's domain check, so ships/aircraft by
themselves are not the false defenders. The demonstrated false population is loaded
same-domain land combat cargo.

INDEPENDENT CONTROL-FLOW CONSEQUENCE
------------------------------------
This local count defect can flip bEasyCityCapture even if the separate cross-file
AI_localDefenceStrength cargo issue is left exactly as current code computes it.

Example legal state:

- target coastal city has 1 genuine unloaded land defender;
- the city also contains 1 enemy loaded land combat unit aboard a transport;
- the human bombard tile has 2 genuine non-bombarding attackers;
- the current AI_compareStacks result (including whatever current strength accounting
  produces) is about 200%, i.e. rStackCmp ~= 2.0.

Actual combat-defender population is 1. With the correct count:

    2 attackers / 1 defender * 2.0 = 4.0 > 2.5

and the >1.5 stack-ratio condition is also met, so bEasyCityCapture becomes true.

The current raw PUF count reports 2 defenders because the loaded cargo is included:

    2 attackers / 2 reported defenders * 2.0 = 2.0 <= 2.5

so bEasyCityCapture remains false. The loaded unit cannot defend the city, yet it
changes the smart-bombard control-flow decision.

PLAYER-VISIBLE ORDERING EFFECT
------------------------------
As established for F207, bEasyCityCapture is not merely diagnostic. When true, each
bombard candidate receives:

    rPriority *= per100(pUnit->currHitPoints());

That extra HP factor can reverse the order of two otherwise legal siege candidates.
When the target has only enough defense left for one bombard action (e.g. current SAS
Catapult bombard damage matching the remaining defense), the false defender count can
therefore decide which human siege unit consumes its action and which remains free.

DISTINCT FROM F205 AND F207
---------------------------
- F205/KI#528 is the no-real-defender assumption: legal undefended bombard asserts in
  debug and release smart priority collapses through the 100%-attack-odds convention.
- F207/KI#530 contaminates iAttackers with foreign units because the whole-plot scan
  has no owner filter and accepts peaceful canMoveOrAttackInto movement.
- F208/KI#531 contaminates iDefenders with loaded combat cargo that actual defender
  selection explicitly excludes.

These are independent populations and independently demonstrated control-flow flips.

CROSS-FILE FOLLOW-UP — DO NOT MERGE INTO F208
----------------------------------------------
During the proof, CvPlayerAI::AI_localDefenceStrength was found to filter canFight(),
domain, death, etc. but not isCargo(). It can therefore also count loaded same-domain
combat cargo as defensive strength, and AI_compareStacks consumes that routine.
That is broader than this local human-bombard count and belongs to the later
CvPlayerAI.cpp audit/resweep. Preserve it as a cross-file candidate; do not claim F208
fixes the strength-estimation root.

REPAIR BOUNDARY
---------------
The bEasyCityCapture defender population should follow actual combat-defender
eligibility at least far enough to exclude loaded cargo (and should ideally be derived
from the same defender semantics used by combat rather than an unrelated raw PUF
count). A minimal demonstrated repair is to exclude isCargo() from the counted units.
Visibility can be reviewed separately; current CvUnit source notes that there are no
invisible defenders that can defend, so invisibility is not needed to prove this root.

SEVERITY
--------
Human-control / mission-order defect, modest but real. It does not permit illegal
combat; it can cause AdvCiv's smart group-bombard ordering to preserve/spend the wrong
siege unit because a loaded enemy unit that cannot defend is treated as a defender.

STATUS: CONFIRMED as F208 / provisional KI#531.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP11 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
NEXT ROOT ID: F209 UNUSED

Next exact cursor: continue queue-041 closure from F209 unused. Preserve the newly
opened cross-file candidate that CvPlayerAI::AI_localDefenceStrength appears to count
loaded same-domain combat cargo as defensive strength, but defer ownership of that
root to the CvPlayerAI audit/resweep. Continue the remaining substantive AdvCiv/K-Mod
history clusters and the final function-by-function caller/contract closure. Do not
manufacture another queue-041 root merely because the remaining surface is small.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP12 CONTINUATION — queue 041 CvSelectionGroupAI.cpp still OPEN
ChatGPT-5.6-Sol audit continuation, 2026-08-27
===============================================================================

This is an intentionally unfinished durable checkpoint. C018-WIP11 is preserved
byte-for-byte as the prefix of the cumulative WIP12 file. No repository source files
were modified by ChatGPT during this audit.


POST-WIP11 CLOSURE / ANCESTRY NOTES
------------------------------------
The remaining work continues to be closure-oriented rather than finding-oriented.
F209 remains unused.

1. AI_follow mutable-list crash lineage: CLOSED / REJECTED AS CURRENT BUG
-----------------------------------------------------------------------
The old AdvCiv crash lineage around AI_follow was rechecked against current SAS.
AdvCiv practical 1282/1287 added defensive assertions around mutation of the group
while iterating its linked list. The subsequent K-Mod 1.46 merge (AdvCiv practical
1288 / <k146>) replaced that fragile traversal with the durable pattern still present
in current SAS: snapshot original members as a std::vector<IDInfo>, reacquire each
unit by ID, verify that the unit still belongs to this group before acting, and reset
the path whenever a follower acts and may mutate group composition.

The historical crash mechanism is therefore not merely presumed absent; current SAS
contains the upstream structural fix written for it. No F209.

2. SAS WAR_ATTACK_ORDER diagnostics: NO SIMULATION / RNG SIDE EFFECT FOUND
-------------------------------------------------------------------------
The logging-only attacker diagnostics were challenged under the invariant that
raising gWarLogLevel must not change simulation state or RNG sequence. The extra
computations traced through attack-odds math, AI_sacrificeValue,
AI_currEffectiveStr, collateral estimation, defender lookup and LFB relative-value
rating. No synchronization RNG draw or gameplay-state mutation was found in the
logging-only work. It can add CPU/log I/O cost but does not currently establish an
OOS or attacker-selection side effect. No F209.

3. Human equal-value attacker tie-break history: INTENTIONAL LATER ADVCIV FIX
----------------------------------------------------------------------------
An older AdvCiv version guarded the human AI_sacrificeValue tie-break with an
odds-threshold condition. This was not accidentally lost: AdvCiv practical 2505,
"Fix attacker selection in combat odds display," deliberately removed the threshold
so equal-value human candidates use the sacrifice-value tie-break consistently in
the combat-odds display/selection path. Current SAS matches that later intended
behavior. No F209.

4. Loaded cargo in collateral estimation: REJECTED AS SIBLING OF F208
---------------------------------------------------------------------
During F208 follow-up, K-Mod's estimateCollateralWeight was noticed to count
canDefend() units without excluding isCargo(). That is NOT the same defect as F208.
Actual CvUnit::collateralCombat also chooses collateral victims through canDefend()
without excluding loaded cargo; loaded combat cargo can therefore legitimately be
hurt by collateral even though CvPlot::getBestDefender explicitly excludes cargo
from primary-defender selection.

This sharpens F208's boundary: cargo is wrong when AdvCiv 2809 counts units that can
prevent immediate city capture, but the same cargo population can be relevant to
collateral valuation. No additional root.

5. Sea weighted-odds !hasCargo() exemption: REJECTED AS COARSE K-MOD HEURISTIC
-----------------------------------------------------------------------------
AI_getWeightedOdds applies its special sea-stack adjustment only when !hasCargo().
Because cargo can include aircraft/missiles as well as invasion troops, this was
explicitly challenged after F206 exposed a different dangerous hasCargo capability
proxy. Here, however, the branch is an acknowledged K-Mod naval-combat heuristic,
not a movement/war-legality gate; its factor can raise or lower the adjusted odds
based on group/defender counts. No defensible invariant or concrete wrong action was
established for carrier/missile cargo. Preserve as coarse tuning, not F209.


F207 / KI#530 STRENGTHENING — OWNER FILTER ALONE IS NOT ENOUGH
---------------------------------------------------------------
F207 was originally proved through a foreign/neutral unit sharing the bombard tile:
AdvCiv 2809 scans the whole plot, does not restrict iAttackers to the human owner, and
uses canMoveOrAttackInto(target) so a third-party unit can count merely through
peaceful Open Borders entry.

A deeper current-SAS check proves a same-owner false-attacker case too. Spy units are
DOMAIN_LAND, have zero ordinary combat strength and rival-territory/invisibility
semantics that let them coexist with enemy units and enter hostile territory through
the ordinary movement half of canMoveOrAttackInto. Therefore a human Spy on the
bombard tile can increment iAttackers against a defended enemy city despite having no
ability to remove the defender.

The root is consequently broader than a missing owner filter:

    AdvCiv 2809 uses destination-entry eligibility as a proxy for immediate assault
    capability.

REPAIR BOUNDARY UPDATE
----------------------
A same-owner filter remains necessary for the already-proved foreign-unit case, but
it is not sufficient. The attacker census must also require actual combat/assault
eligibility against the defended target rather than generic canMoveOrAttackInto.
F205 separately owns the zero-defender smart-bombard case, so the defended-target
predicate can be made attack-specific without conflating those roots.

This strengthening belongs to existing F207/KI#530. F209 remains unused.


SEPARATION / ASSAULT CALLER PASS — CURRENT CURSOR
-------------------------------------------------
The final caller-contract pass has resumed in CvUnitAI::AI_assaultSeaMove around:

- AI_separateImpassable
- AI_separateEmptyTransports
- AI_separateAI
- cached iCargo / iEscorts / bFull recomputation after group mutation
- the subsequent assault/reinforcement/retreat transitions.

Current observations so far:

- after AI_separateImpassable, K-Mod explicitly recomputes bEmpty, bFull, iCargo and
  iEscorts because group composition may have changed;
- AI_separateEmptyTransports removes only empty UNITAI_ASSAULT_SEA hulls, so cargo
  total is unchanged for a surviving loaded head; call sites that may detach the
  current empty assault head explicitly test the new singleton group's cargo and
  reset cached cargo/escort state before continuing;
- AI_separateAI callers in the assault-sea routine are followed either by an
  immediate return/mission decision or by recomputation of the specific escort count
  that subsequent logic uses.

No stale-cache, dangling-group or wrong-head defect has crossed the F209 bar yet.
Continue this pass rather than assuming it is closed.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP12 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
NEXT ROOT ID: F209 UNUSED

Next exact cursor: continue the separation/assault caller-contract pass from F209
unused, then finish the remaining substantive AdvCiv/K-Mod history clusters and final
function-by-function closure. Preserve the deferred CvPlayerAI loaded-cargo defensive
strength candidate for its owning file. Do not manufacture a new queue-041 finding if
the remaining surfaces close cleanly.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP13 — UNFINISHED QUEUE-041 CLOSURE PASS
===============================================================================

STATUS
------
Queue 041 CvSelectionGroupAI.cpp remains OPEN.
F198 through F208 remain unchanged.
F209 remains UNUSED.
This checkpoint preserves verified post-WIP12 closure work and a deferred cross-file
CvUnitAI caller candidate; it does not promote a new queue-041 root.

1. SEPARATION HELPERS — MUTATION SAFETY RECONFIRMED
---------------------------------------------------
AI_separate, AI_separateNonAI, AI_separateAI, AI_separateImpassable and
AI_separateEmptyTransports all iterate through FOR_EACH_UNIT_VAR_IN. The macro
pre-advances the linked-list node before executing the loop body specifically so
removing/joinGroup(NULL)-ing the current unit cannot invalidate traversal. This
matches the old manual K-Mod pattern that advanced nextUnitNode before detaching.
No helper-local invalidation defect found.

The remaining interesting state issue belongs to a caller rather than these helpers.

DEFERRED CROSS-FILE CANDIDATE — CvUnitAI::AI_assaultSeaMove stale bFull
-----------------------------------------------------------------------
Do NOT assign a queue-041 F-number to this unless later evidence moves the fault into
CvSelectionGroupAI. Preserve it for the owning CvUnitAI.cpp audit/resweep.

A concrete caller-side stale-cache sequence exists:

- AI_assaultSeaMove caches bFull near entry as
      getGroup()->getCargo() > 0 && AI_getGroup()->AI_isFull().
- A legal group can contain one fully loaded UNITAI_ASSAULT_SEA transport plus one
  empty UNITAI_ASSAULT_SEA transport. The group-wide AI_isFull result is false because
  the empty hull is not full.
- When bAttack is already true, a later AI_separateEmptyTransports call can detach the
  empty hull. The surviving loaded group is now actually full, but this call site does
  not recompute the cached bFull value.
- If the immediately attempted AI_assaultSeaTransport action fails, later logic can
  reach the `else if (!bFull)` pickup branch using the stale false value.
- CvUnitAI::AI_pickup does not test free cargo capacity / isFull at entry; it only
  rejects units with nominal cargoSpace() <= 0 and can issue MISSIONAI_PICKUP wait or
  movement based on pickup counts. Therefore the now-full transport group can waste
  a decision/turn pursuing cargo it cannot load.

Related caller-side question to preserve for CvUnitAI.cpp: AI_assaultSeaMove may also
unloadAll() early under threat without immediately recomputing cached bEmpty/bFull if
its attack/retreat/safety attempts all fail. iCargo is calculated afterward, but the
booleans can remain stale. This related route is NOT yet proved; preserve it as a
follow-up candidate rather than a finding.

2. LATE ADVCIV HISTORY CLUSTERS — CLOSED
----------------------------------------
The remaining small behavior-history touches checked after WIP12 do not expose a
surviving queue-041 regression:

- AdvCiv practical 3856 migrates AI_getWeightedOdds to CvPlot::DefenderFilters while
  retaining the same effective enemy/potential-enemy/visibility/can-attack flags.
- AdvCiv practical 4185 deliberately replaces raw collateralDamage with the
  promotion-aware AI_collateralDmgFactor in both attacker ranking and human bombard
  ordering. Current SAS retains the paired change coherently.
- Practical 1647 is const-correctness for the old AI_isControlled helper; 1861 is
  formatting; 2033 adds repro-test write instrumentation; 2240 removes the old
  AI_isControlled wrapper after caller refactoring; 2676 threads the later
  strength-memory/cache API through AI_compareStacks. No contradictory current
  caller contract found.
- Practical 2612 changes defender-ejection city-defense detection to the team's
  isCityDefense predicate so rival forts/Open-Borders city-defense semantics are
  handled consistently. Current SAS retains it.
- Practical 2698 is signature formatting only.
- AdvCiv 1499/1618/2518/2551/2879 are predominantly style/type/pathfinder refactors
  in this file; the current K-Mod-1.46 vector-safe AI_follow structure and resetPath
  behavior preserve their final intended semantics.
- AdvCiv 2983 moves bombard-turn/path/stranded helpers into CvSelectionGroupAI rather
  than changing their substantive behavior; those helpers have already received
  dedicated caller/history checks in this audit.
- AdvCiv 4188 only adds the stale-ID caution comment to AI_getMissionAIUnit. The
  actual generation-tagged FFreeList ID implementation was already traced; ordinary
  slot reuse resolves to NULL rather than an unrelated replacement unit.

3. REMAINING SMALL K-MOD HISTORY TOUCHES — CLOSED
-------------------------------------------------
The previously unclassified small K-Mod touches were also checked explicitly:

- K-Mod 98 removes a redundant noDefensiveBonus filter in AI_ejectBestDefender;
  effective strength already incorporates the defensive effect.
- K-Mod 241/353/372/381/825 are successive pathfinder-reset experiments/refactors in
  AI_update/AI_follow. Their final lineage is superseded by the K-Mod 1.46 vector-safe
  follower loop already verified in WIP12.
- K-Mod 268 only improves the old loop assertion message.
- K-Mod 292 moves AI_compareStacks to AI_localDefenceStrength; the later visibility,
  memory and DefenderFilters evolution has already been traced.
- K-Mod 378 is comment/cleanup around sleeping AI force-update behavior.
- K-Mod 480 comments out an assertion invalidated when automated-unit AI moved into
  the automoves phase; current behavior matches that intentional lifecycle change.
- K-Mod 543 switches visible-enemy detection from group owner to head owner, which is
  the appropriate source for a potentially ownerless group wrapper and is retained.
- K-Mod 1081 adds first-strike strength and 1200 corrects estimateCollateralWeight to
  take the attacking team. The current helper signature still expects attacking team
  first; current SAS calls with getTeam(), so this lineage remains coherent.
- K-Mod 1148 fixes only a redundant self-assignment typo in the collateral-weight
  initialization and has no semantic delta.

4. ASSERTION / TAIL CHECKS — NO NEW ROOT
----------------------------------------
The AI_follow `originalGroup.size() == getNumUnits()` assertion is evaluated
immediately after building the vector, before any follower action can split/kill a
member. The later loop then reacquires each ID and checks group identity, so the
assert does not conflict with the mutable-group behavior it is meant to protect.

MissionAI serialization remains symmetric. AI_ejectBestDefender's current arithmetic
also has positive bounded divisors: its city-attack denominator is at least 50 and
its level denominator at least 2. No divide-by-zero or helper-local arithmetic root
found.

5. SAS-DELTA CLOSURE PROGRESS
-----------------------------
The branch-history list confirms a finite set of meaningful SAS changes in this file.
Behavior-changing items already challenged include:

- 4416 defender-ejection precision repair;
- 5093/6211 KI#319 no-progress tripwire lineage (source of F199/F201/F202/F203);
- 5286 low-power attacker ordering;
- 6094 WAR_ATTACK_ORDER diagnostics (read-only/RNG-neutral under audit);
- 6097 obsolete-sacrifice preference;
- 6256 Air Bomb mission-eligibility correction.

The intervening 5095/5304/5306/5469/5961/5978-style changes in this file are assertion
presentation, comment cleanup or signature formatting rather than separate behavior
changes. 6162 is the upstream 1.14 merge that reapplies the defender-ejection
precision fix. No unclassified SAS gameplay hunk has yet produced F209.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP13 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
NEXT ROOT ID: F209 UNUSED

Next exact cursor: finish the current-SAS delta check and the final public-function /
caller matrix, then decide whether queue 041 can close with F209 unused. Preserve the
CvUnitAI::AI_assaultSeaMove stale-bFull candidate for its owning file; do not promote
it into queue 041 merely because it was discovered while auditing separation helpers.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP14 -- UNFINISHED STEP CHECKPOINT
Queue 041: CvGameCoreDLL/CvSelectionGroupAI.cpp
Continuation after C018-WIP13
===============================================================================

F209 / provisional KI#532 -- SAS low-power attacker ordering invokes ordinary
land/sea effective-strength logic during normal AI air strikes
-------------------------------------------------------------------------------
STATUS: CONFIRMED SOURCE-LEVEL DEFECT; SAS REGRESSION INTRODUCED BY PRACTICAL 5286
SEVERITY/CONSEQUENCE: deterministic debug assertion during normal AI air strikes
with the optimization enabled; release builds currently degrade the air unit's
low-power rank to the helper minimum (1), which is normally behavior-neutral only
because ordinary AI aircraft groups are singletons.

ROOT
----
SAS practical 5286 added the optional bPreferLowPower parameter to
CvSelectionGroupAI::AI_getBestGroupAttacker and changed CvSelectionGroup::groupAttack
to pass `true` for the real AI attack loop. For non-human groups while
SAS_AI_GETBESTGROUPATTACKER_LOW_POWER_ATTACK_ORDER_OPTIMIZE is enabled, this causes
the selector to call:

    kLoopUnit.AI_currEffectiveStr(...)

for every eligible attacker.

AI_currEffectiveStr is the ordinary land/sea combat-strength estimator. It calls
CvUnit::currEffectiveStr -> currCombatStr/maxCombatStr, and maxCombatStr immediately
returns 0 when baseCombatStr() == 0. AI_currEffectiveStr then asserts:

    FAssertMsg(iCombatStrengthPercent > 0, "Non-combat unit?");

Current SAS aircraft use the separate air-combat strength family. Verified current
XML examples:

    UNIT_FIGHTER          DOMAIN_AIR  iCombat=0  iAirCombat=12
    UNIT_JET_FIGHTER      DOMAIN_AIR  iCombat=0  iAirCombat=24
    UNIT_BOMBER           DOMAIN_AIR  iCombat=0  iAirCombat=16
    UNIT_STEALTH_BOMBER   DOMAIN_AIR  iCombat=0  iAirCombat=20

The SAS define is currently enabled (`...LOW_POWER_ATTACK_ORDER_OPTIMIZE = 1`).

REACHABLE NORMAL-AI EXECUTION CHAIN
-----------------------------------
This is not limited to a debug UI helper and does not require a multi-air group:

1. CvUnitAI::AI_airStrike selects a real troop air-strike target. For an air strike
   rather than MISSION_AIRBOMB, it calls pushGroupMoveTo(target).
2. CvUnit::pushGroupMoveTo queues MISSION_MOVE_TO with the default movement flags.
   AI missions execute immediately.
3. CvSelectionGroup::continueMission_bulk sees a same-turn MISSION_MOVE_TO without
   MOVE_NO_ATTACK and first calls groupAttack(...).
4. groupAttack explicitly permits DOMAIN_AIR. Its initial attacker probe uses the
   inherited/default bPreferLowPower=false and can correctly identify the aircraft:
   canAirAttack() uses airBaseCombatStr(), and canMoveInto(target,true) delegates the
   air attack legality to canAirStrike(target).
5. Once a defender is confirmed, the real attack loop calls:

       AI_getBestGroupAttacker(..., !bMaxSurvival, bMaxSurvival, true)

   The final `true` is the SAS bPreferLowPower parameter introduced by practical 5286.
6. For an AI owner and the enabled define, bUseLowPower becomes true. Even a singleton
   Fighter/Bomber therefore enters the low-power branch and calls
   AI_currEffectiveStr, reaching the `"Non-combat unit?"` assertion because ordinary
   baseCombatStr is zero.

WHY THE EARLIER "DORMANT FOR SINGLETON AIR GROUPS" REJECTION WAS INCOMPLETE
---------------------------------------------------------------------------
An earlier closure pass correctly observed that normal AI aircraft are effectively
singleton groups, so the *ordering choice* among multiple air attackers is normally
irrelevant. That did not make the new branch dormant: the selector still computes the
low-power key for the one aircraft before returning it. Therefore the wrong metric is
executed and its assertion is live even with exactly one unit.

This WIP explicitly supersedes that earlier rejection. The singleton observation
still bounds the current release-build gameplay impact; it does not remove the debug
contract failure.

RELEASE BUILD BEHAVIOR
----------------------
With assertions compiled out, ordinary effective strength starts at 0 for these air
units. The subsequent calculation remains at 0 in the normal aircraft case, and
AI_currEffectiveStr finally returns std::max(1, iR), collapsing the air attacker's
low-power strength rank to 1 rather than using iAirCombat.

For the currently normal singleton AI air group this does not change which unit is
selected, so the demonstrated present gameplay consequence is the debug assertion.
If AI multi-air groups are ever introduced/reached, the same defect would make their
low-power ordering semantically meaningless because ordinary aircraft strengths
collapse to the helper minimum instead of reflecting air combat strength.

ANCESTRY / ATTRIBUTION
----------------------
Base AdvCiv 1.14 does not have the low-power branch and does not pass the new
bPreferLowPower=true argument from groupAttack. SAS practical 5286 explicitly:

- added the bPreferLowPower parameter;
- enabled bUseLowPower for non-human groups under the SAS define;
- added AI_currEffectiveStr as the low-power primary key; and
- changed the real groupAttack loop to pass `true`.

Therefore this is a SAS regression rather than an inherited AdvCiv/K-Mod air-combat
assertion path.

F204/KI#527 remains a separate root. F204 is an inherited debug combat-help/Shift-hover
diagnostic that calls AI_compareStacks/AI_sumStrength for DOMAIN_AIR despite that
stack-strength routine explicitly excluding air. F209 is normal AI runtime introduced
by SAS 5286 and directly invokes AI_currEffectiveStr from the new low-power attacker
ordering. The callers, ownership and repair boundaries differ even though both expose
the broader fact that ordinary effective-strength helpers do not model air combat.

REPAIR BOUNDARY
---------------
The minimal safe repair for the current feature is to disable the low-power ordering
path for DOMAIN_AIR (at group or candidate level) and fall back to inherited air
attacker selection. If low-power ordering for grouped aircraft is intentionally wanted
in the future, it needs an air-specific strength metric based on air combat strength;
feeding iAirCombat aircraft through ordinary currEffectiveStr is not valid.

This finding does NOT require changing the inherited assertion in AI_currEffectiveStr:
that assertion correctly catches callers feeding a non-ordinary-combat unit into a
helper whose contract assumes positive ordinary combat strength.

POST-WIP13 CLOSURE STATE
------------------------
The current-SAS delta pass is now even more informative: practical 5286 contains one
confirmed domain-contract regression (F209) despite the rest of its low-power ordering
arithmetic/tie logic surviving prior review. Continue checking the remaining public
function/caller matrix rather than assuming this finding closes queue 041 by itself.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP14 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
    F209 / provisional KI#532
NEXT ROOT ID: F210 UNUSED

Next exact cursor: continue the final public-function/caller + remaining substantive
history closure from F210 unused. Revisit practical 5286 only for independent roots;
do not split the same air-domain mismatch into another finding. Keep the deferred
CvUnitAI::AI_assaultSeaMove stale-bFull candidate assigned to the later CvUnitAI audit.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP15 -- UNFINISHED STEP CHECKPOINT
Queue 041: CvGameCoreDLL/CvSelectionGroupAI.cpp
Continuation after C018-WIP14
===============================================================================

POST-F209 REPAIR-BOUNDARY REFINEMENT -- WAR_ATTACK_ORDER AIR DIAGNOSTICS
-------------------------------------------------------------------------------
The post-WIP14 domain audit rechecked the SAS WAR_ATTACK_ORDER diagnostics in light of
F209/KI#532. Those diagnostics are still deterministic/read-only and do not consume
synchronized RNG or mutate simulation state, as established earlier. However, their
candidate-detail logging also calls AI_currEffectiveStr(), so an air attack would
reach the same invalid ordinary-combat metric if that diagnostic scan were entered.

This is NOT an independent root. The entire level-3 candidate scan is gated by
bUseLowPower. Therefore the existing minimal F209 repair -- do not enable the SAS
low-power ordering path for DOMAIN_AIR unless an air-specific metric exists -- also
prevents the diagnostic scan from feeding aircraft into AI_currEffectiveStr. Do not
allocate a second finding for the logging symptom.


F210 / provisional KI#533 -- inherited LFB attacker/odds path treats aircraft as
ordinary zero-strength combatants, corrupting grouped-air attacker selection
-------------------------------------------------------------------------------
STATUS: CONFIRMED SOURCE-LEVEL DEFECT; INHERITED FROM BETTER BTS AI / K-MOD AND
PRESENT IN ADVCIV 1.14
SEVERITY/CONSEQUENCE: player-visible grouped-air attacker-order/help error; inherited
odds collapse also makes the intended <68% sacrifice threshold fire for aircraft
regardless of their real iAirCombat strength. Current AI aircraft are normally
singletons, which limits autonomous-AI ordering damage, but AdvCiv explicitly supports
human grouped-air stack execution and air-strike help through this selector.

ROOT
----
CvSelectionGroupAI::AI_getBestGroupAttacker explicitly accepts DOMAIN_AIR attackers:

    if (kLoopUnit.getDomainType() == DOMAIN_AIR)
        bCanAttack = kLoopUnit.canAirAttack();

but, unless SAS's newer low-power branch is active, it then sends those aircraft
through the inherited Lead-From-Behind path:

    kLoopUnit.LFBgetBetterAttacker(...)

LFBgetBetterAttacker -> CvUnit::LFBgetAttackerRank ->
pDefender->LFBgetDefenderOdds(pAttacker). LFBgetDefenderOdds decides whether it has a
usable attacker by evaluating ordinary combat strength:

    iAttStrength = pAttacker->currCombatStr();
    if (iAttStrength > 0)
        bUseAttacker = true;

Current aircraft have ordinary iCombat=0 and store their real strength in iAirCombat.
Thus Fighter/Jet Fighter/Bomber/Stealth Bomber all supply currCombatStr()==0 here.
LFB consequently takes its defender-only fallback and never incorporates the
attacker's air strength into the combat-odds ranking.

The sibling unit-level path has the same domain mistake. CvUnitAI::AI_attackOdds is
explicitly air-aware in its old/fallback calculation:

    iOurStrength = (getDomainType() == DOMAIN_AIR ?
            airCurrCombatStr(NULL) : currCombatStr());

but the default LFB-enabled branch runs before that fallback and calls generic
calculateCombatOdds(*this, *pDefender). CombatOdds::setupCombatantsImpl initializes
its attacker with ordinary currCombatStr(), so an aircraft contributes zero ordinary
strength and the calculation immediately yields 0 attack odds. After personality
adjustment and the public clamp, ordinary air AI_attackOdds therefore collapses to the
minimum (typically 1%, or only the leader AttackOddsChange above that) instead of
reflecting iAirCombat.

ANCESTRY CROSS-CHECK
--------------------
Civ4CE/BtS's pre-LFB CvUnitAI::AI_attackOdds reaches the explicit DOMAIN_AIR
`airCurrCombatStr` branch and therefore distinguishes aircraft by air strength.
K-Mod's Better BTS AI / Lead-From-Behind integration inserts the generic combat-odds
branch ahead of that air-aware calculation. AdvCiv 1.14 retains the same structure.
This is therefore inherited rather than introduced by SAS.

The current XML confirms the domain split rather than relying on a hypothetical unit:

    UNIT_FIGHTER          iCombat=0  iAirCombat=12  cost=140  interception=100
    UNIT_JET_FIGHTER      iCombat=0  iAirCombat=24  cost=170  interception=100
    UNIT_BOMBER           iCombat=0  iAirCombat=16
    UNIT_STEALTH_BOMBER   iCombat=0  iAirCombat=20

CONCRETE GROUPED-AIR CONSEQUENCE
--------------------------------
AdvCiv explicitly supports grouped human aircraft. CvSelectionGroup::groupAttack has
advc.004c logic described as "Treat air combat like stack attack unless intercepted",
and CvGameTextMgr::getAirStrikePlotHelp asks the selected group's
AI_getBestGroupAttacker for the aircraft whose interception/help should be shown.
Therefore multi-air selection is a live supported caller, not merely a future AI
possibility.

Consider a full-health human group containing a Fighter and Jet Fighter attacking the
same weak ground defender. Both use UNITCOMBAT_AIR_FIGHTER, so the inherited LFB
fallback sees the same defender-side ordinary-strength context for them while seeing
neither 12 nor 24 air strength. More importantly, its returned iUnitOdds collapses to
about the minimum and therefore satisfies AdvCiv's explicit sacrifice gate:

    if (bSacrifice && iUnitOdds < 68)
        pBestUnit = AI_getBestGroupSacrifice(...);

AI_getBestGroupSacrifice's DOMAIN_AIR value does not use iAirCombat either. For these
two default units it rewards interception and divides by production cost. With equal
100% interception and health, the cheaper Fighter receives the higher sacrifice value
(roughly 128*200/140 versus 128*200/170 before common value adjustments), so the
weaker Fighter is selected as the expendable attacker.

This is not what the <68% threshold intends. As a sanity comparison, the pre-LFB
air-aware AI_attackOdds formula on a simple unmodified strength-3 ground defender
would heavily distinguish the units and put both comfortably above the sacrifice
threshold (approximately 97% for air strength 12 and 99% for air strength 24 under
that legacy heuristic). The LFB path instead erases the air-strength distinction and
forces the sacrifice mode through its bogus near-minimum odds.

The same selector is used by getAirStrikePlotHelp, so the displayed interception/help
can also be based on the wrong member of a selected multi-air group.

RELATIONSHIP TO F209 / KI#532
-----------------------------
F209 is a SAS practical-5286 regression: the new low-power ordering feeds aircraft
into AI_currEffectiveStr and asserts because ordinary combat strength is zero.
F210 is older and independent: the inherited LFB selection/odds machinery also uses
ordinary strength where DOMAIN_AIR requires air strength.

The interaction is important for implementation review. Merely fixing F209 by making
bUseLowPower false for DOMAIN_AIR causes the real air attack loop to fall back to the
inherited LFB path, where F210 remains. Therefore F209's repair is necessary but does
not make aircraft selection/odds correct by itself.

REPAIR BOUNDARY
---------------
A robust repair needs an explicitly air-aware path rather than passing aircraft
through ordinary LFB combat strength:

1. CvUnitAI::AI_attackOdds should not call generic land-combat calculateCombatOdds for
   DOMAIN_AIR. It should use an air-aware estimate (the existing legacy air branch is
   the obvious baseline, though its heuristic can be reviewed separately).
2. CvSelectionGroupAI::AI_getBestGroupAttacker should not call
   LFBgetBetterAttacker for DOMAIN_AIR unless LFB itself is taught to use air combat
   strength. Once AI_attackOdds is air-correct, selecting air candidates by that value
   (with the existing human/sacrifice policy layered afterward) is safer than invoking
   an LFB routine whose attacker-rank contract assumes ordinary combat strength.

Do not "fix" this by giving aircraft fake ordinary iCombat values; the engine
intentionally separates ordinary and air combat strength and many other callers rely
on that distinction.


POST-WIP14 HISTORY / CLOSURE NOTES
----------------------------------
- Rechecking the old force-update lineage did not expose a new root: current
  CvSelectionGroup::readyToMove explicitly treats isForceUpdate() as sufficient to
  bypass the normal awake/empty-mission readiness condition, so the later K-Mod
  semantics supersede the older doForceUpdate shape safely.
- The remaining K-Mod/AdvCiv attacker-history review continues to support the already
  classified roots rather than showing a reverted fix. F210 is the first new
  independent inherited air-selection contract found in this final matrix pass.
- The deferred CvUnitAI::AI_assaultSeaMove stale-bFull candidate still belongs to its
  owning CvUnitAI audit and is not consumed as a queue-041 finding.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP15 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
    F209 / provisional KI#532
    F210 / provisional KI#533
NEXT ROOT ID: F211 UNUSED

Next exact cursor: continue the final public-function/caller and remaining substantive
K-Mod/AdvCiv history matrix from F211 unused. In particular, challenge the remaining
air-aware/non-air-aware caller boundaries now that F209/F210 show two independent
domain assumptions, but do not split additional symptoms of those same roots into new
findings. Close queue 041 only after the finite helper/caller matrix and meaningful
history hunks are fully reconciled.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP16 -- UNFINISHED STEP CHECKPOINT
Queue 041: CvGameCoreDLL/CvSelectionGroupAI.cpp
Continuation after C018-WIP15
===============================================================================

F207 / KI#530 STRENGTHENING -- "ATTACKER" CENSUS ALSO COUNTS UNITS THAT CANNOT
ACT THIS TURN
-------------------------------------------------------------------------------
The final AI_bestUnitForMission(MISSION_BOMBARD) caller/predicate pass strengthened
F207's root again. The human easy-city-capture precheck scans every unit on the plot
and counts a unit when:

    !pUnit->canBombard(kAt) &&
    pUnit->canMoveOrAttackInto(*pMissionPlot)

Earlier proof already established two false-positive classes:
- foreign third-party units can satisfy the ordinary-movement half through Open
  Borders with the target, despite not participating in the human player's war;
- same-owner Spies can satisfy ordinary movement through hostile territory/cities
  while having zero combat strength.

There is a third independent false-positive mode inside the SAME census root:
canMoveOrAttackInto is destination/legal-entry logic and does not test whether the
unit can currently act. CvUnit::canMove() separately rejects exhausted movement,
dead units and immobile-timer units; canMoveInto does not consume that contract.
Therefore a same-owner ordinary combat unit that has already exhausted all movement
can still be counted toward iAttackers and can spuriously flip bEasyCityCapture even
though it cannot take part in the immediate assault this turn.

This reinforces the correct F207 repair boundary. An owner-only filter is necessary
but not sufficient. For the defended-city branch, the census needs to represent
units that the human actually controls AND that can currently act AND that can
actually perform the hostile attack/assault against the target. Generic
canMoveOrAttackInto is too broad because its ordinary-movement half and its lack of a
current-movement check answer a different question.

Do NOT allocate a new root for this symptom; it is further proof of F207's existing
"destination entry used as immediate assault capability" defect.


F208 / KI#531 BOUNDARY CHECK -- INVISIBLE DEFENDER LEAK IS DORMANT IN CURRENT SAS
-------------------------------------------------------------------------------
The same human smart-bombard defender census uses:

    pMissionPlot->plotCount(PUF_canDefendEnemy, getOwner(), false)

where PUF_canDefendEnemy combines canDefend + enemy semantics but does not apply
PUF_isVisible. CvPlot::getNumVisibleEnemyDefenders, by contrast, explicitly adds a
visibility predicate. Thus the local census would count an invisible combat-capable
defender if current game data allowed one to defend the target land city.

Current SAS XML was enumerated rather than assuming such a unit exists. Among 131
UnitInfo entries, the only unit with a real non-NONE Invisible type is
UNIT_ATTACK_SUBMARINE (INVISIBLE_SUBMARINE), DOMAIN_SEA, iCombat=24. There is no
combat-capable invisible DOMAIN_LAND unit. Spies are special-entry/invisible units
but have zero combat and do not satisfy canDefend. A sea submarine cannot defend a
land city under the domain check.

Therefore this is a future-data/API robustness hazard, NOT a current information-leak
finding and not F211. Keep it as a boundary note beside F208; F208's live current-data
proof remains loaded land combat cargo, which canDefend() counts even though primary
getBestDefender() explicitly rejects isCargo().


F204 / KI#527 BOUNDARY CHECK -- GROUP PATHFINDER DOES NOT ADD A NORMAL AIR CALLER
-------------------------------------------------------------------------------
The final caller matrix noticed two textual AI_sumStrength references in
GroupPathFinder and challenged whether F204's explicit DOMAIN_AIR assertion could be
reached during normal air pathfinding.

One reference is inside an old fully commented-out experimental path-validity block.
The live reference is in the MOVE_ATTACK_STACK cost path, which immediately asserts:

    FAssert(kGroup.getDomainType() == DOMAIN_LAND);

MOVE_ATTACK_STACK is itself AI-only there. Thus normal air pathfinding does not feed
an aircraft group into AI_sumStrength. F204 remains the inherited debug combat-help
caller mismatch, not a broader pathfinding defect.


CURRENT-SAS DELTA / CLOSURE STATUS
----------------------------------
A whitespace-insensitive current SAS vs AdvCiv 1.14 comparison confirms that the
behavioral SAS deltas in this file are finite and already explicitly under audit:
- KI#319 no-progress tripwire changes;
- practical-5286 low-power attacker ordering and its diagnostics;
- practical-6097 obsolete-sacrifice refinement;
- KI#394 MISSION_AIRBOMB target-validity repair;
- UnitAI enum renames / nonbehavioral signature formatting.

No additional hidden SAS behavior hunk appeared in that mechanical comparison.
Remaining uncertainty is primarily inherited K-Mod/AdvCiv caller/history closure.

CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP16 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
    F209 / provisional KI#532
    F210 / provisional KI#533
NEXT ROOT ID: F211 UNUSED

Next exact cursor: continue the finite inherited public-function/caller and
K-Mod/AdvCiv history matrix from F211 unused. Preserve root cohesion: further bad
"attacker census" cases belong to F207; current smart-bombard defender-census cases
that describe units unable to serve as primary defenders belong to F208. Do not
promote dormant future-data hazards without current reachability.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP17 -- UNFINISHED STEP CHECKPOINT
Queue 041: CvGameCoreDLL/CvSelectionGroupAI.cpp
Continuation after C018-WIP16
===============================================================================

F211 / provisional KI#534 -- inherited planned-war mission census can call
AI_isDeclareWar on a supported empty selection group and fire a false debug assert
--------------------------------------------------------------------------------
STATUS: CONFIRMED SOURCE-LEVEL DEBUG-CONTRACT DEFECT; RELEASE BUILD RETURNS SAFELY.
INHERITED ROOT: BtS AI_isDeclareWar null-head assertion + BBAI/K-Mod target-mission
census / group-separation lifecycle; present in K-Mod 1.46 and AdvCiv 1.14.
CURRENT SAS DID NOT INTRODUCE THE ROOT.

LOCAL CONTRACT MISMATCH
-----------------------
Current CvSelectionGroupAI::AI_isDeclareWar begins with:

    FAssert(getHeadUnit() != NULL);

but later deliberately reacquires the head and handles its absence:

    CvUnit const* pHeadUnit = getHeadUnit();
    if (pHeadUnit == NULL)
        return false;

The later guard is not dead release-only paranoia. A selection group can legitimately
remain allocated for a bounded interval after its last unit has left.

SUPPORTED EMPTY-GROUP LIFECYCLE
-------------------------------
The final lifecycle trace established all of the following in current SAS:

1. CvUnit::joinGroup removes a unit from its old group through removeUnit(). It does
   not kill the old selection group when that removal empties it.

2. CvSelectionGroup::removeUnit -> deleteUnitNode clears the mission command queue,
   but clearMissionQueue() does NOT clear the AI mission metadata
   (m_eMissionAIType / m_iMissionAIX / m_iMissionAIY / m_missionAIUnit).
   Therefore an emptied group can retain the MissionAI plot that it had while live.

3. If removing a non-human group's head exposes a new head with a different UnitAI,
   CvUnit::joinGroup marks the old group AI_isForceSeparate().

4. CvPlayerAI::AI_unitUpdate has an explicit prepass over the group cycle which calls
   AI_separate() on force-separated groups. AI_separate() rejoins every remaining
   unit into a fresh group. The old group can consequently become empty while staying
   registered in the player's selection-group/group-cycle containers.

5. This is an expected enough state that K-Mod's movement-priority path explicitly
   handles a null head by returning MAX_INT, while CvSelectionGroupAI::AI_update
   returns false immediately when getNumUnits() == 0. Neither path destroys the
   object at that point.

6. Destruction is deferred to CvSelectionGroup::doDelayedDeath(). In particular,
   CvPlayer::doTurnUnits starts by iterating all groups and calling doDelayedDeath(),
   which kills groups whose unit count is now zero.

The empty group is therefore a supported transient object, not merely an impossible
state after memory corruption.

LIVE PRE-WAR CALL PATH
----------------------
The first direct-caller pass almost rejected the candidate because the two obvious
current AI_enemyTargetMissions calls are war-only:

- CvTeamAI::AI_endWarVal asserts isAtWar(eTeam);
- the AI_surrenderTrade use occurs in its bWar/capitulation branch.

For both, AI_enemyTargetMissions short-circuits at:

    ::atWar(getTeam(), pMissionPlot->getTeam()) ||
        pLoopSelectionGroup->AI_isDeclareWar(*pMissionPlot)

so AI_isDeclareWar is not evaluated for an already-active war.

However AdvCiv's optimized boolean wrapper AI_isAnyEnemyTargetMission() is also live.
Its important caller is CvTeamAI::AI_abandonWarPlanIfTimedOut, which is invoked for
WARPLAN_LIMITED / WARPLAN_DOGPILE / WARPLAN_TOTAL targets specifically while the team
is NOT yet at war. It asks whether any member has an active mission toward eTarget so
that a still-active invasion plan is not abandoned.

For such a target, ::atWar is false. If an empty group's retained MissionAI plot is
owned by eTarget, AI_enemyTargetMissions therefore evaluates AI_isDeclareWar on that
empty group and trips the leading debug assertion. The later null-head guard would
otherwise return false, and the zero-unit group cannot contribute a positive mission
count.

TURN ORDER MAKES THE WINDOW REAL
--------------------------------
The final ordering proof removes the remaining lifetime ambiguity:

- K-Mod/AdvCiv player activation calls CvTeam::doTurn for the team's leader BEFORE
  that player's doTurnUnits.
- CvTeam::doTurn -> AI_doTurnPost -> AI_doWar.
- AI_doWar can reach AI_abandonWarPlanIfTimedOut and therefore
  AI_isAnyEnemyTargetMission / AI_enemyTargetMissions for a planned, not-yet-declared
  target.
- only AFTER CvTeam::doTurn returns does player activation call CvPlayer::doTurnUnits,
  whose opening doDelayedDeath pass deletes empty groups.

Thus a force-separated group emptied during the prior movement phase can survive
until the next team's war-planning pass and be observed before the normal empty-group
cleanup.

ANCESTRY
--------
Civ4CE/BtS already has the apparently contradictory AI_isDeclareWar shape: a leading
FAssert(getHeadUnit() != NULL), followed later by a null-safe head check. It does not
have BBAI's AI_enemyTargetMissions function.

K-Mod 1.46 imports BBAI's AI_enemyTargetMissions and uses it directly in the pre-war
WARPLAN_LIMITED/DOGPILE and WARPLAN_TOTAL timeout branches while !isAtWar(target).
K-Mod also has the team-doTurn-before-player-doTurnUnits ordering and the deferred
empty-group doDelayedDeath cleanup. Therefore the bad interaction is already present
in K-Mod, not introduced by AdvCiv's later bool wrapper.

AdvCiv 1.14 retains the same underlying census and lifecycle; its
AI_isAnyEnemyTargetMission optimization only changes the maximum count requested.
Current SAS inherits the same contract mismatch.

CONSEQUENCE / SEVERITY
----------------------
This is a debug-build false assertion, not demonstrated release gameplay corruption.
In release builds AI_isDeclareWar reaches its null check and returns false. The empty
group has zero units/cargo, so it cannot increase the mission count either.

The practical cost is nevertheless real for source debugging: a valid transient group
state can stop an assert-enabled run inside normal AI war-plan maintenance. Given this
audit's repeated use of debug assertions to catch genuine state corruption, preserving
assert signal quality is worthwhile.

REPAIR BOUNDARY
---------------
The narrowest semantically exact repair is in CvPlayerAI::AI_enemyTargetMissions:
ignore empty groups before reading their stale MissionAI target or consulting
AI_isDeclareWar, e.g. alongside the skip-group check:

    if (pLoopSelectionGroup == pSkipSelectionGroup ||
        pLoopSelectionGroup->getNumUnits() <= 0)
    {
        continue;
    }

That is behavior-neutral for valid live counts because a zero-unit group cannot add
units or cargo, and it prevents obsolete AI mission metadata from entering the war
intent classifier.

A broader alternative is to make AI_isDeclareWar's existing null-head release guard
precede or replace the assertion. The caller-side filter is preferable as the first
repair because AI_enemyTargetMissions is a census of actual units and has no reason to
inspect an empty group's stale target at all.

STATUS: CONFIRMED as F211 / provisional KI#534.


WEIGHTED-ODDS FOLLOW-UP -- SUSPECTED DOUBLE ADJUSTMENT REJECTED
---------------------------------------------------------------
The WIP16-era final matrix had one independent-looking concern in
AI_getWeightedOdds(): it subtracts GET_PLAYER(...).AI_getAttackOddsChange() from the
selected group attack odds. That would be wrong if Lead From Behind's alternate
attacker selector returned raw odds without first adding the leader's optimism bonus.

The exact LFB path closes the concern. CvUnitAI::LFBgetBetterAttacker converts its
combat odds to AI odds and then explicitly executes:

    iAIOdds += GET_PLAYER(getOwner()).AI_getAttackOddsChange();

before clamping and returning iAIAttackOdds. Therefore AI_getWeightedOdds removes an
adjustment that really is present. No independent root is created; F200's already
classified clamp/weight issue remains the relevant weighted-odds finding.

F212 is NOT spent here.


FINITE LOW-MENTION PUBLIC-HELPER PASS -- NO NEW ROOT SO FAR
-----------------------------------------------------------
The function-name coverage matrix identified several public helpers with little or no
literal-name discussion in the album. Their current definitions and all current-tree
callers were enumerated rather than assuming that low textual coverage meant low risk.

AI_init / AI_uninit
-------------------
AI_init delegates to AI_reset; AI_uninit is empty; AI_reset clears all mission-AI,
force-separate and queued-group-attack state. Constructor/destructor use the same
lifecycle. No allocation/resource ownership is hidden here and no independent defect
was found.

AI_isHasPathToAreaEnemyCity / AI_isHasPathToAreaPlayerCity
-----------------------------------------------------------
Current live uses are in CvUnitAI city-assault/stranded handling. The player-specific
helper explicitly returns false for getNumUnits() <= 0 before dereferencing area(), so
it already contains the exact empty-group guard missing from F211's mission census.
For live groups it restricts candidate cities to the group's area, calls generatePath
with the requested movement flags and max-turn bound, and rechecks iPathTurns when a
bound is supplied.

The enemy wrapper applies bMajorOnly as documented and requires
GET_TEAM(getTeam()).AI_mayAttack(candidate team) before testing pathability. Current
callers invoke it through a live CvUnitAI group. No null-group, wrong-team or max-turn
contract defect survived this pass.

AI_isStranded
-------------
This K-Mod helper is intentionally a mission-state query:

    AI_getMissionAIType() == MISSIONAI_STRANDED

The current callers use that marker to order movement priority, preserve stranded
state in unit behavior/help, and let transports discover groups advertised for pickup.
The actual determination / coast-routing work is in CvUnitAI::AI_handleStranded; the
helper is not pretending to recompute geographical strandedness. No defect found in
this accessor contract.

AI_getHeadUnit
--------------
Both AdvCiv const/non-const wrappers read headUnitNode and return NULL when no node
exists. All current external callers were enumerated. The direct dereference in
CvUnitAI's transport-load remainder path is preceded by:

    pRemainderGroup == NULL || pRemainderGroup->getNumUnits() <= 0

so a head must exist under the normal group invariant. Other callers either test NULL
or use a group already anchored by a live acting unit. The new SAS game-record caller
also explicitly null-checks the group before asking for the head.

No new F212 root emerged from these low-mention helpers.


CURRENT WIP CURSOR
------------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP17 (unfinished)
QUEUE-041 ROOTS SO FAR:
    F198 / provisional KI#521
    F199 / provisional KI#522
    F200 / provisional KI#523
    F201 / provisional KI#524
    F202 / provisional KI#525
    F203 / provisional KI#526
    F204 / provisional KI#527
    F205 / provisional KI#528
    F206 / provisional KI#529
    F207 / provisional KI#530
    F208 / provisional KI#531
    F209 / provisional KI#532
    F210 / provisional KI#533
    F211 / provisional KI#534
NEXT ROOT ID: F212 UNUSED

Next exact cursor: continue queue-041 proof-of-closure from F212 unused. Finish the
remaining finite public helper/header/exported-wrapper matrix and the last meaningful
K-Mod/AdvCiv behavioral-history hunks. In particular, do not reopen the direct
AI_enemyTargetMissions war-only callers (they short-circuit safely); F211 is owned by
the pre-war mission-census path. Continue challenging serialization/reset/group-state
contracts and any remaining helper whose callers have not yet been mechanically
enumerated. Close queue 041 only if that finite pass leaves no unresolved substantive
seam; do not manufacture F212 merely to extend the finding count.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP18 APPEND -- QUEUE 041 CvSelectionGroupAI.cpp -- LATEST-SOURCE RECONCILIATION
===============================================================================

This is deliberately another unfinished durable handoff. C018-WIP17 remains an exact
byte prefix. The purpose of this append is to reconcile the audit against the newer
13:41 light-source snapshot before queue closure, record which early findings have
already been implemented, and correct one finding-ledger duplication discovered only
when comparing the frozen roots side by side.

LATEST SOURCE IDENTITY
----------------------
Reconciliation source:
    AdvCiv-SAS_light_source_20260827T134157.zip

Repository state captured by that ZIP:
    branch: main
    HEAD: 61e14a5a947574a86bab81ad012addfa7097cc8d
    practical commit count: 6292
    origin/main ahead/behind: 0 / 0

The ZIP also captures staged changes on top of HEAD in:
    CvGameCoreDLL/CvSelectionGroupAI.cpp
    _1_AdvCiv-SAS/Docs/README_Known_Issues.md
    _1_AdvCiv-SAS/Docs/Source_Analysis/cpp_file_audit_album.txt

This matters because C018 began against an earlier source snapshot while the user and
Codex/GPT implementation side continued reviewing and applying findings in parallel.
The audit therefore must distinguish "confirmed against the source that exposed it"
from "still present in the newest supplied source."

EXACT STAGED CvSelectionGroupAI CHANGESET IN THE 13:41 SNAPSHOT
---------------------------------------------------------------
The staged diff for this file is small and finite: +7 / -3 lines. It implements three
C018 findings:

1. F198 / KI#521
   - removes the stale human-path iDefenders > 0 assertion in
     AI_bestUnitForMission(MISSION_BOMBARD);
   - does not alter legal Bombard gameplay.

2. F199 / KI#522
   - snapshots getCargo() before CvUnitAI::AI_update;
   - requires cargo to remain unchanged before the SAS no-progress tripwire can push
     MISSION_SKIP.

3. F201 / KI#524
   - snapshots getNumUnits();
   - requires group membership count to remain unchanged before classifying the
     update as no progress.

The reconciled README_Known_Issues.md contains durable reviewed/fixed entries 521,
522 and 524 matching those three code changes. No durable entries for the later
provisional KI numbers from this queue are present in the supplied 13:41 Known Issues
file; those later KI labels therefore remain audit-side provisional identifiers, not
repository numbering that must be preserved at all costs.

FINDING-LEDGER CORRECTION -- F198 AND F205 WERE ACCIDENTALLY DOUBLE-PROMOTED
---------------------------------------------------------------------------
Comparing the frozen finding texts side by side exposed a bookkeeping error in C018:

    F198/KI#521 promoted the false human undefended-city Bombard assertion.
    F205/KI#528 later promoted the very same assertion again.

WIP08 then explicitly said that F205's deeper release-build consequence was the
"same root" and that "no new F-number is consumed," even though F205 had already been
assigned. That is internally inconsistent and should not be carried into closure.

There is, however, a real *second faulty behavior* in the same supported
undefended-city state, and it survives the KI#521 assertion repair:

    scaled rOdds = per100(pUnit->AI_attackOdds(pMissionPlot, false));
    rPriority *= (1 - rOdds);

With no defender, CvUnitAI::AI_attackOdds returns 100. Therefore rOdds == 1 and every
eligible Bombarder's otherwise meaningful human ordering priority is multiplied by
zero. All candidates tie at zero and AI_bestUnitForMission falls back to group/unit
iteration order. This is distinct from the now-fixed false assertion: deleting the
assertion does not repair the release ordering collapse.

Least-destructive ledger repair for the cumulative album:

- F198 / KI#521 remains exactly the false debug assertion root and is now FIXED in
  the supplied 13:41 source.
- F205 is RETRACTED AS A DUPLICATE ASSERTION PROMOTION and RE-SCOPED, without changing
  its F-number, to the independent release-build human Bombard ordering collapse at
  `rPriority *= (1 - rOdds)` for iDefenders == 0.
- F205's ancestry/scenario evidence remains useful, but future summaries must not say
  that F198 and F205 are two separate copies of the same assertion defect.
- Later F206-F211 identifiers do not need renumbering.

This correction is preferable to pretending the duplication never happened, and it
also reflects the current source cleanly: KI#521's assertion fix is present while the
zero-priority release behavior remains present.

13:41 FINDING-VS-SOURCE MATRIX
------------------------------
The newest supplied code was mechanically checked against every C018 root.

F198 / KI#521 -- IMPLEMENTED / FIXED IN CURRENT STAGED SOURCE
    False `iDefenders > 0` human Bombard assertion removed.

F199 / KI#522 -- IMPLEMENTED / FIXED IN CURRENT STAGED SOURCE
    AI_update no-progress signature now includes total group cargo.

F200 / provisional KI#523 -- SOURCE PATTERN STILL PRESENT
    AI_getWeightedOdds still receives the public/clamped attacker odds and subtracts
    AI_getAttackOddsChange only afterward. LFBgetBetterAttacker still adds the player
    adjustment and clamps to 1..99 before returning iBestOdds. The lossy-clamp contract
    challenged by F200 has not been changed in this source.

F201 / KI#524 -- IMPLEMENTED / FIXED IN CURRENT STAGED SOURCE
    AI_update no-progress signature now includes group membership count.

F202 / provisional KI#525 -- SOURCE PATTERN STILL PRESENT
    AI_update still consumes AI_isGroupAttack()/AI_cancelGroupAttack() before the
    no-progress comparison without snapshotting queued group-attack state. The new
    cargo/membership fields do not observe this transition.

F203 / provisional KI#526 -- SOURCE PATTERN STILL PRESENT
    AI_update still observes only head-unit moves, not a group-wide movement/action
    summary. A synchronous immediate mission spending a non-head member's movement can
    therefore remain invisible to the tripwire when all newly added membership/cargo
    fields also stay unchanged.

F204 / provisional KI#527 -- SOURCE PATTERN STILL PRESENT
    Debug Shift-hover still calls kSelectionList.AI_compareStacks(pPlot,...), which
    reaches AI_sumStrength; AI_sumStrength's air-domain limitation/assertion remains.
    No domain gate was added at the debug combat-help caller.

F205 / provisional KI#528 -- RE-SCOPED RELEASE ROOT STILL PRESENT
    The duplicate assertion aspect is retired. The surviving root is the undefended-
    city Bombard priority collapse described above; the current source still executes
    `rPriority *= (1 - rOdds)` and AI_attackOdds returns 100 when no defender exists.

F206 / provisional KI#529 -- SOURCE CONTRACT STILL PRESENT
    AI_isDeclareWar still returns hasCargo() for UNITAI_ASSAULT_SEA. Current
    CvUnitAI.cpp still permits generic stranded-unit pickup by assault transports and
    retains the SAS small-cargo/opportunistic assault machinery used in the proof.
    No military/capture-capable-cargo qualification was added at this contract.

F207 / provisional KI#530 -- SOURCE PATTERN STILL PRESENT
    Human smart-Bombard iAttackers still scans every unit on the bombarder's current
    plot and increments for `!canBombard && canMoveOrAttackInto(target)` without an
    owner filter, without canMove/current-action eligibility, and without a stronger
    hostile-assault-role predicate. The strengthened F207 census root survives.

F208 / provisional KI#531 -- SOURCE PATTERN STILL PRESENT
    iDefenders still uses raw
        plotCount(PUF_canDefendEnemy, getOwner(), false)
    and therefore retains the loaded-cargo defender-census mismatch proved earlier.

F209 / provisional KI#532 -- SOURCE PATTERN STILL PRESENT
    bUseLowPower is still enabled from bPreferLowPower + AI ownership + SAS define,
    with no DOMAIN_AIR exclusion. The low-power branch still calls
    CvUnitAI::AI_currEffectiveStr for every eligible attacker, and WAR_ATTACK_ORDER
    candidate diagnostics remain under the same low-power gate.

F210 / provisional KI#533 -- SOURCE PATTERN STILL PRESENT
    CvUnitAI::AI_attackOdds still enters LFB/calculateCombatOdds before its explicit
    DOMAIN_AIR airCurrCombatStr fallback. CvUnit::LFBgetDefenderOdds still tests the
    attacker's ordinary currCombatStr(), which is zero for aircraft, and therefore
    falls back to its non-air strength path. The grouped-air selection/odds root has
    not been changed.

F211 / provisional KI#534 -- SOURCE PATTERN STILL PRESENT
    CvPlayerAI::AI_enemyTargetMissions still enumerates groups without first skipping
    getNumUnits() <= 0, obtains mission/plot state, and can call
    AI_isDeclareWar(*pMissionPlot). AI_isDeclareWar still begins with the non-null-head
    assertion and only afterward safely returns false for a null head. No census-side
    empty-group filter has been added.

The matrix is intentionally a source-state reconciliation, not an assertion that every
unimplemented provisional root must be accepted or patched. Implementation review can
still reject or reshape any provisional finding. The audit's job here is only to say
accurately whether the source condition that was proved still exists.

FINAL HELPER / WRAPPER / HISTORY PASS SINCE WIP17
-------------------------------------------------
The finite tail pass has not produced F212 so far.

- Exported virtual wrappers were compared with the original BtS/Civ4CE virtual API.
  Raw pointer parameters in AI_attackOddsExternal and AI_ejectBestDefenderExternal do
  not imply a new nullability contract: those original virtuals did not have nullable
  defaults. AI_isDeclareWar was the notable optional/null original, and AdvCiv's
  external wrapper explicitly guards NULL before forwarding to the reference-based
  implementation. Rejected as F212.

- AI_setMissionAI / AI_getMissionAIPlot state was checked for invalid-coordinate
  lifetime. The setter stores either coordinates from a real CvPlot or the invalid
  sentinel; plotSoren handles the sentinel as NULL. Current map plots do not disappear
  during normal runtime. No supported dangling-coordinate state was proved.

- AI MissionAIUnit serialization remains paired owner/id state; the older stale-ID
  concern remains bounded by FFreeList generation-tagged IDs as proved earlier.

- AI-owned state fields are reset and read/write symmetrically: mission type/coords,
  force-separate, mission unit owner/id and queued group-attack state/coords. No save
  asymmetry survived the pass.

- The exact tracked history inventory still contains 134 commit diffs that genuinely
  patch CvSelectionGroupAI.cpp. Remaining behavior/bugfix-shaped K-Mod and AdvCiv
  hunks were reconciled against their current descendants; no additional old fix was
  found partially reverted outside already-classified roots.

- AI_getBombardTurns' owner-wide synthetic bomber support was challenged again and
  remains rejected as a new root. The approximation is real and can over-credit
  distant air support, but it is visibly an inherited strategic heuristic and the
  album already established that final assault decisions recheck real strength. Do
  not manufacture F212 from this without runtime evidence of a pathological decision.

CURRENT CURSOR
--------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP18
CURRENT LEDGER:
    F198 / KI#521 -- fixed in latest staged source
    F199 / KI#522 -- fixed in latest staged source
    F200 / provisional KI#523 -- source pattern survives
    F201 / KI#524 -- fixed in latest staged source
    F202 / provisional KI#525 -- source pattern survives
    F203 / provisional KI#526 -- source pattern survives
    F204 / provisional KI#527 -- source pattern survives
    F205 / provisional KI#528 -- assertion duplicate retired; re-scoped release
                                 ordering root survives
    F206 / provisional KI#529 -- source pattern survives
    F207 / provisional KI#530 -- source pattern survives
    F208 / provisional KI#531 -- source pattern survives
    F209 / provisional KI#532 -- source pattern survives
    F210 / provisional KI#533 -- source pattern survives
    F211 / provisional KI#534 -- source pattern survives
NEXT ROOT ID: F212 UNUSED

Exact continuation cursor: finish the latest-source closure pass by challenging the
re-scoped F205 once more against actual mission iteration/termination semantics, then
complete the remaining public/helper caller matrix and closure bookkeeping. If no
independent defect survives, close queue 041 with F212 unused rather than inventing a
finding merely to extend the sequence.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C018-WIP19 APPEND -- QUEUE 041 CvSelectionGroupAI.cpp CONTINUED
===============================================================================

This append continues exactly from C018-WIP18. It finishes the re-scoped F205 proof,
closes two deliberately reopened historical checks, and promotes one new inherited
root after a full current-runtime + K-Mod ancestry proof. Queue 041 remains OPEN;
F213 is unused.

F205 / PROVISIONAL KI#528 -- RE-SCOPED RELEASE ORDERING ROOT CONFIRMED
---------------------------------------------------------------------
WIP18 intentionally left one final challenge open: after retiring F205's accidental
duplicate promotion of the KI#521 assertion, is the remaining undefended-city
Bombard priority collapse actually capable of changing supported gameplay, or is it
merely a meaningless tie?

It is gameplay-relevant.

Current AI_bestUnitForMission(MISSION_BOMBARD) builds a meaningful per-unit priority
from human intent, bombard damage, wasted bombard damage and other factors, then does:

    scaled rOdds = per100(pUnit->AI_attackOdds(pMissionPlot, false));
    rPriority *= (1 - rOdds);

CvUnitAI::AI_attackOdds returns 100 when the plot has no defender. Therefore, on a
legal human Bombard command against an undefended city, every otherwise-differentiated
candidate receives a final priority of zero. Because replacement requires
`rPriority > rMaxPriority`, the selector keeps the first eligible unit in group-list
order instead of the unit selected by the intended Bombard priority model.

The decisive supported state is the human zero-current-defense maintenance Bombard:

- CvCity::isBombardable deliberately allows a human Bombard at current defense 0 when
  total defense is still positive and the city has not yet been bombarded this turn.
- This is meaningful, not UI residue: CvCity::changeDefenseModifier marks the city as
  bombarded even when the effective defense change is zero, suppressing defense
  recovery for that turn.
- CvSelectionGroup::startMission repeatedly selects a Bombard unit and calls
  CvUnit::bombard.
- The first selected unit pays its attack/movement cost; the city becomes bombarded.
- The city's special zero-defense human isBombardable allowance then becomes false,
  so the group mission terminates after that one unit.

Thus the collapsed selector decides which single siege/bombard unit pays the action
cost. Group-list order is not Bombard intent; a stronger or more valuable unit can be
spent while a more appropriate unit remains unused. The effect is deterministic for
a given group ordering, not merely cosmetic.

Ancestry makes the root unusually clear. AdvCiv practical commit 2809,
"Smarter code for order of group bombardment", introduced the modern richer priority
calculation and the attack-odds multiplier. Before that change the undefended state
still retained differentiation based on actual/wasted bombard and unit preservation.
The multiplier therefore regressed precisely the ordering that the commit was trying
to improve. Later AdvCiv changes alter individual factors but leave this root intact.

Ledger boundary after reconciliation:

- F198/KI#521 = false debug assertion for legal undefended human Bombard; fixed in the
  supplied 13:41 source.
- F205/provisional KI#528 = independent release-build ordering collapse caused by
  multiplying the Bombard priority by zero when AI_attackOdds returns 100 for an
  undefended target; still present in the supplied source.

The clean repair boundary is to avoid using ordinary attack odds as a zeroing factor
when there is no defender. Exact implementation should be reviewed with the intended
human Bombard ordering semantics; deleting or special-casing the no-defender factor is
safer than reviving the old assertion.

HISTORICAL REOPEN -- K-MOD 271 PRESERVED
-----------------------------------------
K-Mod practical 271, "Fixed a rare infinite-loop bug, indirectly caused by the K-Mod
change in groupAttack", was reopened because its commit number had not been explicitly
classified in the durable tail despite the current behavior having been inspected.

Its CvSelectionGroupAI fix changed the AI_update head check from merely testing
`isDelayedDeath()` to actively executing `doDelayedDeath()` before continuing. Current
SAS preserves that behavior:

    CvUnitAI* pHeadUnit = AI_getHeadUnit();
    if (pHeadUnit == NULL || pHeadUnit->doDelayedDeath())
        break;

The companion group-timer change that updates all queued combats rather than breaking
after the first fighting unit is also preserved in current CvSelectionGroup. No
partial reversion or new root was found. Rejected as F212.

F212 / PROVISIONAL KI#535 -- K-MOD 846 REINTRODUCED A K-MOD 428 AMPHIBIOUS
GROUP-ATTACK CANCELLATION WINDOW
--------------------------------------------------------------------------------
STATUS: CONFIRMED inherited K-Mod regression; present in final K-Mod 1.46, AdvCiv 1.14
and current SAS.

ROOT
----
A turn-start `isForceUpdate()` flag can survive on a land cargo group until after its
transport has already ordered that cargo group to begin an immediate amphibious
attack. The first attack queues K-Mod's deferred group-attack continuation, but the
cargo group's later AI_update sees the *old* force-update flag and cancels the *new*
queued attack before executing it.

This is exactly the class of failure K-Mod practical 428 explicitly fixed, but K-Mod
practical 846 later reshaped force-update semantics in a way that reopened it.

CURRENT-RUNTIME PROOF
---------------------
1. CvSelectionGroup::doTurn is run for AI land groups at turn activation, including
   loaded cargo groups. In current source there is exactly one live producer of
   `setForceUpdate(true)`: this turn-start path.

   For an AI-controlled group whose activity is not ACTIVITY_MISSION, doTurn does:

       setForceUpdate(true);
       clearMissionQueue();
       AI().AI_cancelGroupAttack();

   Therefore any pre-existing queued group attack is already canceled here. The force
   flag that remains is only a request that the group receive at least one AI update.

2. The loaded sea transport normally moves before its land cargo group. Current
   CvPlayerAI::AI_movementPriority gives a non-special loaded sea transport priority
   3, whereas ordinary land combat cargo is much later (bombard/collateral around
   11/12, ordinary fighting groups roughly around the 65-based land scale). This is
   the exact ordering anticipated by K-Mod's surviving comment:

       "this is required for amphibious assaults; during which a low priority group
        can be ordered to attack before its turn."

3. When the transport reaches an adjacent amphibious target,
   CvSelectionGroup::groupAmphibMove obtains the cargo groups and pushes
   MISSION_MOVE_TO directly onto each eligible cargo group.

4. CvSelectionGroup::pushMission starts AI commands immediately:

       insertAtEndMissionQueue(mission, !bAppend || isAIControlled());

   The cargo group's own AI_update has therefore not run yet and its turn-start
   force-update flag is still true, but the transport can already make the cargo
   attack.

5. The MISSION_MOVE_TO execution reaches groupAttack against the adjacent defended
   target. For ordinary AI combat, stack-attack player option handling is false, so
   groupAttack issues one attack and, when multiple units remain and
   MOVE_SINGLE_ATTACK is not set, records the continuation through:

       AI().AI_queueGroupAttack(targetX, targetY);

   AI_queueGroupAttack only sets m_bGroupAttack and its target coordinates. It does
   not clear m_bForceUpdate.

6. Combat resolution clears the ordinary mission queue. The queued group-attack
   state is therefore the dedicated mechanism that tells the AI to continue the
   multi-unit assault after the first combat.

7. AI_movementPriority gives a group with AI_isGroupAttack() priority -1, explicitly
   so this deferred amphibious continuation can run promptly. Whether combat resolved
   synchronously or the global update waits for the combat to stop, the cargo group
   eventually reaches its own CvSelectionGroupAI::AI_update with:

       isForceUpdate() == true
       AI_isGroupAttack() == true

8. Current AI_update begins with:

       if (isForceUpdate())
           AI_cancelGroupAttack();

   It therefore deletes the freshly queued amphibious continuation before reaching
   the loop whose first condition is supposed to execute it:

       while ((AI_isGroupAttack() && !isBusy()) || readyToMove())

   The remaining cargo units do not receive the intended forced follow-through from
   that queued state. The turn-start signal has incorrectly overridden newer combat
   state created later in the same movement phase.

WHY THIS IS NOT A SPECULATIVE ORDERING
--------------------------------------
The current priority model strongly favors the loaded sea transport over normal land
cargo, so the transport-before-cargo sequence is ordinary behavior rather than a rare
scheduler coincidence. K-Mod practical 428 itself documents the same requirement:
a low-priority cargo group can be ordered to attack before its own turn, hence a
queued group attack must receive top movement priority afterward.

ANCESTRY -- K-MOD 428 -> K-MOD 846 REGRESSION
---------------------------------------------
K-Mod practical 428 / commit 85a83db596 is titled:

    "Fixed three bugs from the original BtS code which could sometimes cause the
     AI's amphibious group-attacks to be canceled prematurely."

Among its fixes:

- AI_movementPriority began returning -1 for AI_isGroupAttack(), specifically for
  amphibious assaults whose cargo may be ordered to attack before its turn.
- CvSelectionGroup::doTurn stopped leaving a deferred force-update flag and instead
  called doForceUpdate immediately. The source comment says this prevents the forced
  update from interrupting amphibious assaults.
- AI_update's force-update handling was centralized through that immediate cleanup.

K-Mod practical 846 / commit 5348a1d503 later intentionally changed the model:

- turn start once again does `setForceUpdate(true)` and performs mission/group-attack
  cleanup explicitly;
- readyToMove treats force update as sufficient to enter AI processing;
- doForceUpdate is disabled;
- AI_update cancels a group attack when it sees isForceUpdate, without first clearing
  the flag;
- the flag is cleared only after entering the AI_update loop, or in the end cleanup
  path when the group cannot enter the loop.

The full 846 commit message says force update should now merely signal that every AI
group's update function runs at least once per turn, and that AI_cancelGroupAttack and
clearMissionQueue are called explicitly rather than being directly associated with
force update. Yet the new AI_update entry retained exactly such an association:
`isForceUpdate() -> AI_cancelGroupAttack()`.

No later K-Mod change repairs this interaction. Final K-Mod 1.46 retains the same
turn-start flag, immediate amphibious cargo command, queued group-attack priority and
AI_update-entry cancellation. AdvCiv and current SAS inherit that lineage.

SCOPE / CONSEQUENCE
-------------------
The root applies when a transport orders a multi-unit cargo group into an amphibious
attack before that cargo group's own turn/update and the first combat leaves a queued
follow-through to the same target. Instead of continuing the stack attack as K-Mod's
queue mechanism intends, the stale turn-start force-update marker can cancel it.

Practical consequence: an AI naval invasion can spend only the first attack from a
cargo group at that stage instead of following through with the remaining attackers,
weakening or delaying amphibious assaults. Exact visible severity depends on combat
resolution, remaining defenders/attackers and later AI opportunities, but the state
transition itself is deterministic once the above conditions hold.

REPAIR BOUNDARY
---------------
The strongest narrow observation is that current source has only one live
`setForceUpdate(true)` producer, and that producer already explicitly cancels any
pre-existing group attack at turn start. Therefore the extra AI_update-entry
cancellation is redundant for old turn-start group-attack state but destructive for a
new group attack queued later in the movement phase.

A natural repair candidate is consequently to remove the AI_update-entry
`AI_cancelGroupAttack()` association with isForceUpdate, letting the loop consume a
fresh AI_isGroupAttack state. Because the loop clears forceUpdate before executing
its first action, the end-of-function force-update cleanup then does not cancel that
fresh attack. This also matches K-Mod 846's stated conceptual contract that force
update is merely an update-request signal rather than group-attack cleanup.

An alternative local repair would be to clear the stale force-update flag when
AI_queueGroupAttack records a newer continuation. That is more local but couples the
queue setter to turn-update state; review against the external wrapper and any future
force-update producers would be prudent. Do not patch blindly; the root proof is
stronger than any particular implementation recommendation.

Historical classification:

    introduced fix lineage: K-Mod practical 428 / 85a83db596
    regression lineage:     K-Mod practical 846 / 5348a1d503
    present in:              final K-Mod 1.46, AdvCiv 1.14, current SAS
    SAS-specific:            no

REMAINING HISTORY / CLOSURE STATUS
----------------------------------
The deliberately reopened bugfix-shaped history is now mostly exhausted:

- K-Mod 271 delayed-death/groupAttack infinite-loop fix: preserved.
- K-Mod 323/325 visibility-memory/head-team AI_compareStacks fix: preserved.
- K-Mod 344 explorer declare-war guard: preserved.
- K-Mod 372 path-reset lineage: superseded coherently by later vector-safe follower
  handling already audited.
- K-Mod 428 amphibious group-attack fix: not simply "preserved"; its movement-priority
  half survives, but the force-update half was later undermined by K-Mod 846, yielding
  F212.
- K-Mod 449 disabled compare-ratio experiment: no live behavior.
- K-Mod 466 const-only MissionAI accessor change: no behavior.
- K-Mod 512 force-separation return-value lineage: current behavior coherent.

The queue is intentionally not declared closed in this checkpoint. F212 was found
precisely because the final historical challenge was allowed to continue after the
file already looked nearly finished. The remaining task is to challenge F212's repair
boundary/caller assumptions once more, finish the last public/helper/history closure
bookkeeping and then either close queue 041 or promote another root only if one is
independently proved.

CURRENT CURSOR
--------------
COMPLETED FIRST-PASS FILES: 17 / 169
FILE 017 UWAIAgent.cpp: CLOSED AT C018
CURRENT FILE: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp -- OPEN
PHYSICAL CHECKPOINT: C018-WIP19
NEW SINCE WIP18:
    F205 / provisional KI#528 -- re-scoped release Bombard-order root now fully
                                 confirmed by mission termination + AdvCiv 2809 ancestry
    F212 / provisional KI#535 -- confirmed inherited K-Mod 846 regression of K-Mod
                                 428 amphibious group-attack continuation protection
NEXT ROOT ID: F213 UNUSED

Exact continuation cursor: re-challenge F212 against all force-update and queued-
group-attack producers/consumers (the current source has only the turn-start true
producer and groupAttack gameplay queue producer), then finish the finite remaining
history/public-helper closure matrix. If clean, close queue 041 with F213 unused.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C018-WIP20 -- QUEUE 041 FINAL CLOSURE PASS
CvGameCoreDLL/CvSelectionGroupAI.cpp
Continuation after C018-WIP19
===============================================================================

SOURCE / METHOD RECONCILIATION
------------------------------
Authoritative current SAS source for this final pass:
    AdvCiv-SAS_light_source_20260827T134157.zip

C018-WIP19 is an exact byte prefix of this checkpoint. The final pass deliberately
re-challenged F212, finished the current-SAS-vs-AdvCiv semantic delta, checked the
public/header/EXE-wrapper surface, rechecked every persisted CvSelectionGroupAI field,
and exhausted the remaining assert/default/low-mention helper matrix before closure.

No F213 was promoted merely to show progress.


F212 / PROVISIONAL KI#535 -- FINAL RE-CHALLENGE AND REPAIR-BOUNDARY REFINEMENT
-----------------------------------------------------------------------------
F212 remains CONFIRMED. The re-challenge strengthened the state-contract proof and
found a second place where the same stale turn-start force-update marker can outrank a
newer queued group attack.

Current CvPlayerAI::AI_unitUpdate performs this force-separation pre-pass before
building its movement-priority list:

    if (pLoopSelectionGroup->AI_isForceSeparate())
    {
        if (pLoopSelectionGroup->isForceUpdate() ||
            !pLoopSelectionGroup->AI_isGroupAttack())
        {
            pLoopSelectionGroup->AI_separate();
        }
    }

Thus the stale force-update state can defeat fresh group-attack state in TWO places:

1. If AI_isForceSeparate() becomes true after the initial pre-pass -- e.g. combat
   removes a group head and exposes a different UnitAI -- the next AI_unitUpdate
   pre-pass can split the group despite its newly queued attack because the stale
   isForceUpdate() makes the condition true.

2. If the group survives that pre-pass, CvSelectionGroupAI::AI_update still begins by
   canceling AI_isGroupAttack() whenever isForceUpdate() is true.

The state provenance is unusually strong. A complete current-tree grep finds only one
live setForceUpdate(true) producer:

    CvSelectionGroup::doTurn

That turn-start producer already calls clearMissionQueue() and AI_cancelGroupAttack().
Therefore a later state satisfying both:

    isForceUpdate() == true
    AI_isGroupAttack() == true

contains group-attack state that was created AFTER the old turn-start cleanup. The
queued attack is newer information than the force-update marker.

The current tree likewise has only the ordinary groupAttack gameplay producer of
AI_queueGroupAttack, plus the preserved external ABI wrapper. groupAttack queues the
continuation after the first attack when multiple attackers remain and the attack is
not a single-attack command.

The movement scheduler reinforces rather than weakens the amphibious proof. Loaded
sea transports have very high movement priority and can issue immediate cargo missions
before ordinary land cargo receives its own AI_update. If combat makes the game busy,
the next AI_unitUpdate rebuilds priorities; AI_isGroupAttack then gives the cargo group
priority -1, but the force-separation pre-pass runs before sorting and the AI_update
entry cancellation remains afterward. The queued continuation is therefore exposed to
both stale-force-update consumers.

K-Mod 428 explicitly introduced the top-priority queued attack and immediate
force-update handling to prevent premature amphibious cancellation. K-Mod 846 later
states that force update should merely ensure each AI group receives an update at
least once and that AI_cancelGroupAttack / clearMissionQueue are now called explicitly
"without being directly associated with force update." Its new AI_update entry
nonetheless retained exactly such a direct association. Final K-Mod 1.46, AdvCiv 1.14
and current SAS retain it.

REPAIR BOUNDARY REFINED
-----------------------
The WIP19 suggestion to remove only the AI_update-entry AI_cancelGroupAttack is not
sufficient, because the AI_unitUpdate force-separation pre-pass can still destroy the
fresh attack.

Two repair families deserve review:

A. Make both consumers respect newer queued attack state -- i.e. do not let a stale
   force-update marker override AI_isGroupAttack in either the force-separation
   pre-pass or AI_update entry.

B. Clear the stale force-update marker when AI_queueGroupAttack records a newer attack
   continuation.

B is compact and naturally orders the two pieces of state, but should not be applied
blindly. CvUnitAI::AI_evacuateCity uses !isForceUpdate() as a proxy for whether a group
has already received its normal AI update this turn; clearing the bit early can change
that heuristic before AI_update has actually executed. A is therefore semantically
more conservative unless caller review proves B harmless.

The root is stronger than any particular patch recommendation. Keep F212 scoped to the
stale-force-update/newer-group-attack ordering contract and review both cancellation
sites together.


CURRENT SAS VS ADVCIV 1.14 -- FINITE SEMANTIC DELTA EXHAUSTED
-------------------------------------------------------------
A function-body comparison with comments/formatting removed reduces the current SAS
behavioral delta in CvSelectionGroupAI.cpp to exactly three substantive functions:

1. AI_update
   - KI#319 / no-progress tripwire state snapshot and stuck-loop diagnostics.
   - The live correctness weaknesses are already owned by F199/F201/F202/F203.
   - KI#522 and KI#524 repairs present in the 13:41 snapshot broaden the snapshot with
     cargo/group-size/head-role state but do not yet implement F202/F203.

2. AI_getBestGroupAttacker
   - trailing internal bPreferLowPower parameter;
   - low-power attacker ordering;
   - obsolete-sacrifice refinement;
   - WAR_ATTACK_ORDER diagnostics.
   - The air-domain contract root is already F209, with inherited LFB air behavior
     separately owned by F210.

3. AI_bestUnitForMission
   - KI#521 removal of the false human undefended-city Bombard assertion;
   - KI#394 target-specific Air Bomb eligibility repair.
   - Remaining Bombard ordering/census problems are already owned by F205/F207/F208.

No fourth hidden SAS behavior cluster exists in this file under the current snapshot.
The constructor difference produced by the crude extractor was only a signature/comment
parsing artifact.

Header comparison adds only the internal trailing bPreferLowPower default argument and
formatting changes. No CvSelectionGroupAI data member was added. The existing class-size
BOOST_STATIC_ASSERT is unchanged.


EXTERNAL / VIRTUAL ABI CLOSURE
------------------------------
CvVirtualWrappers and CvSelectionGroup's preserved EXE-facing virtual declarations were
rechecked against the internal API.

- AI_getBestGroupAttackerExternal keeps the original EXE signature and forwards without
  the new bPreferLowPower argument, so the internal trailing default remains false for
  external calls. The SAS ordering mode is not accidentally exposed through an ABI
  mismatch.
- AI_isDeclareWarExternal preserves the original nullable plot contract by returning
  false for NULL before calling the newer reference-based internal implementation.
- AI_attackOddsExternal, AI_getBestGroupAttackerExternal and
  AI_ejectBestDefenderExternal still require the same non-null plot arguments as their
  original interfaces; adding speculative null semantics is not justified.
- compare/sum-strength legacy arguments intentionally remain compatibility shims around
  the K-Mod-reduced internal APIs.
- No virtual slot/signature drift or newly reachable low-power behavior was found.

Rejected as F213.


PERSISTED STATE / RESET / ACCESSOR CLOSURE
------------------------------------------
All eight CvSelectionGroupAI-owned persisted values are covered symmetrically:

    m_iMissionAIX
    m_iMissionAIY
    m_bForceSeparate
    m_eMissionAIType
    m_missionAIUnit.eOwner / iID
    m_bGroupAttack
    m_iGroupAttackX
    m_iGroupAttackY

AI_reset initializes every field. read and write consume/produce the same fields in the
same order. m_missionAIUnit validates its owner after deserialization. No source delta
alters the serialization layout.

AI_getMissionAIPlot is safe for the intentional INVALID_PLOT_COORD sentinel:
CvMap::plotSoren returns NULL for that sentinel before asserting ordinary coordinates.
The earlier stale MissionAIUnit concern remains rejected for normal unit-ID recycling
because FFreeList full IDs contain generation state and lookup validates the full ID.

AI_cancelGroupAttack intentionally clears only the boolean; stale attack coordinates
are inert while m_bGroupAttack is false and are replaced by AI_queueGroupAttack before
reuse.

No independent state/serialization root survived. F213 remains unused.


ASSERT / DEFAULT / DIAGNOSTIC CLOSURE
-------------------------------------
1. AI_isDeclareWar's switch was mechanically checked against the current UnitAITypes
   enum: all 42 current enum values have explicit cases. Its default FAssert(false) is
   not hiding a current enum-expansion defect. The empty-group leading assertion is
   already independently owned by F211.

2. Every live AI_bestUnitForMission caller passes exactly one of the four implemented
   mission types:
       MISSION_PILLAGE
       MISSION_BOMBARD
       MISSION_AIRBOMB
       MISSION_PARADROP
   The unsupported-mission FErrorMsg has no current live unsupported caller.

3. The SAS stuck-loop diagnostic moved its FAssertMsg inside #ifdef _DEBUG. This does
   not disable an assert-enabled non-debug configuration: FAssert.h defines
   FASSERT_ENABLE only when _DEBUG itself is defined. Outside _DEBUG the old assert was
   already a no-op. The new placement preserves debug behavior while avoiding expensive
   diagnostic formatting until the assertion condition actually fails.

Rejected as F213.


NO-PROGRESS HIDDEN-STATE RE-CHALLENGE
-------------------------------------
The final pass explicitly challenged whether the KI#319 progress signature also needed
MissionAI, activity or force-separate state in order to avoid another independent
false Skip.

No current supported path crossed the finding bar:

- force-separate is normally created by a head/group-membership change, which is already
  visible through head identity and/or group size, and AI_unitUpdate has a dedicated
  force-separation pre-pass before AI_update;
- direct MissionAI changes observed in live unit-AI paths either accompany mission-queue
  work, group/head changes, an action that returns out of the loop, or ultimately push
  an explicit Skip themselves;
- activity wake/reset paths likewise do not supply a demonstrated "only hidden state
  changed, then the tripwire suppresses required continued work" sequence.

This does NOT prove the snapshot is future-proof. F202 and F203 already establish the
broader lesson that a hand-selected progress signature can miss valid group state.
Future producers that mutate only these omitted fields should be reviewed against that
same root family rather than assigned a new finding automatically.

No F213.


AI_getBombardTurns GLOBAL-BOMBER APPROXIMATION -- REJECTED AS DEFECT
--------------------------------------------------------------------
AI_getBombardTurns grants a synthetic air contribution whenever the owner has any air
bombard unit globally. A bomber elsewhere on the map can therefore shorten an unrelated
land group's projected bombard horizon and make building defense look ignorable.

This is coarse, but the complete caller/ancestry check does not support classifying it
as a correctness defect:

- the helper explicitly documents an approximate number of bombard turns;
- even a group with zero real bombard capability receives a finite estimate rather
  than an "impossible" sentinel;
- callers use the result as a strategic heuristic/value modifier, not as a legality or
  reachability guarantee;
- the global bHasBomber term is deliberate BBAI/K-Mod ancestry rather than an accidental
  SAS mutation.

Record as an intentional coarse strategic approximation / possible tuning weakness,
not F213.


OTHER FINAL NEGATIVE CONTROLS
-----------------------------
- AI_bestUnitForMission MISSION_BOMBARD cannot dereference a missing pTargetCity for a
  valid candidate: CvUnit::canBombard itself requires a non-null bombardTarget.
- MISSION_PARADROP's pMissionPlot dereference has only non-null live callers; Air Bomb
  now has an explicit target/null eligibility gate.
- AI_isFull's live gameplay callers are transport/carrier contexts; ordinary zero-cargo
  groups do not gain a demonstrated behavioral effect from the helper's generic
  cargoSpace==0 "full" semantics.
- AI_ejectBestDefender denominators remain strictly positive and its current callers
  preserve the required target-plot contract.
- AI_isHasPathToAreaPlayerCity explicitly rejects empty groups before area/path use;
  the enemy-city wrapper preserves its bMajorOnly and AI_mayAttack contracts.
- AI_isStranded remains an intentional MissionAI marker query; determination is owned
  by CvUnitAI::AI_handleStranded.
- AI_compareStacks / AI_sumStrength received no additional normal air caller beyond the
  already-classified F204 debug-help mismatch; GroupPathFinder's live strength path is
  explicitly land-only.
- Lead-From-Behind weighted-odds adjustment was rechecked: LFBgetBetterAttacker really
  adds AI_getAttackOddsChange before AI_getWeightedOdds subtracts it. F200 remains about
  loss of information through clamping, not a nonexistent double subtraction.

No new independent root emerged.


QUEUE 041 CLOSURE JUDGMENT
--------------------------
CvGameCoreDLL/CvSelectionGroupAI.cpp is CLOSED for this first-pass album.

This does not assert that the file is mathematically bug-free. It means the current
source, associated header and wrappers, all current SAS behavioral deltas, persisted
state, current callers, relevant cross-file lifecycle contracts, available Civ4CE/
K-Mod/AdvCiv ancestry and the deliberately reopened bugfix history have been exhausted
to the point where another finding would currently require speculation, tuning
preference or unsupported future data.

The closure standard was intentionally strict. The file appeared nearly finished at
F210, but continued caller/lifecycle/history work still found F211 and F212. After
F212, the audit again challenged the exact same classes of assumptions before closing;
those final challenges produced bounded rejections rather than a manufactured F213.

QUEUE-041 ROOT LEDGER
---------------------
F198 / KI#521   - human undefended-city Bombard false debug assertion
                  [implemented/fixed in current staged source]
F199 / KI#522   - no-progress tripwire misses missionless cargo unload
                  [implemented/fixed in current staged source]
F200 / KI#523   - weighted odds cannot undo AttackOddsChange after 1..99 clamp
F201 / KI#524   - no-progress tripwire misses deliberate non-head bombard split
                  [implemented/fixed in current staged source]
F202 / KI#525   - no-progress tripwire misses queued group-attack state consumption
F203 / KI#526   - no-progress tripwire misses non-head synchronous mission progress
F204 / KI#527   - debug Shift combat help feeds DOMAIN_AIR into land-only strength API
F205 / KI#528   - human Bombard order collapses all priorities to zero when undefended
                  (re-scoped release-ordering root; distinct from fixed F198 assert)
F206 / KI#529   - ASSAULT_SEA declare-war intent uses hasCargo as hostile-assault proxy
F207 / KI#530   - human smart-Bombard attacker census uses legal-entry proxy rather
                  than owner/current-action/real-assault eligibility
F208 / KI#531   - human smart-Bombard defender census counts loaded cargo as defenders
F209 / KI#532   - SAS low-power ordering sends air units into land-only effective-str API
F210 / KI#533   - inherited LFB odds/attacker selection is air-blind
F211 / KI#534   - planned-war mission census can query AI_isDeclareWar on supported
                  transient empty groups and fire a false debug assertion
F212 / KI#535   - K-Mod 846 stale force-update semantics can cancel a newer queued
                  amphibious group-attack continuation, regressing K-Mod 428 protection

F213 remains UNUSED.

CURRENT DURABLE CURSOR
----------------------
COMPLETED FIRST-PASS FILES: 18 / 169
FILE 017 CvGameCoreDLL/UWAIAgent.cpp: CLOSED AT C018
FILE 018 (investigation order; queue 041) CvGameCoreDLL/CvSelectionGroupAI.cpp:
    CLOSED AT C019
PHYSICAL WIP CHECKPOINT: C018-WIP20
DURABLE CLOSURE CHECKPOINT: C019
QUEUE-041 ROOTS: F198-F212 / provisional KI#521-KI#535
NEXT ROOT ID: F213 UNUSED

NEXT SELECTED FILE -- PRIORITY OVERRIDE
---------------------------------------
Per the reconciled album's explicit priority markers, do not continue numerically to
queue 042. The next never-passed priority file is:

    priority 2nd: queue 031 CvGameCoreDLL/UWAICache.cpp (about 1,812 lines)

After queue 031, priority 3rd remains queue 045 CvGameCoreDLL/CvMapGenerator.cpp unless
a newer reconciled album changes the priority annotations.

Exact next cursor: begin queue 031 UWAICache.cpp from F213 unused. Establish its local
SAS-vs-AdvCiv delta first, then map cache lifetime/invalidation/read-write or turn-key
contracts and UWAI callers before promoting any root. Preserve the same slow WIP
method: findings are optional; proof and clean rejections are not.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP01 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Current file:
    queue 031  CvGameCoreDLL/UWAICache.cpp  (1,806 physical lines in snapshot)
Prior durable boundary:
    C019 -- queue 041 CvSelectionGroupAI.cpp CLOSED
Completed first-pass files entering this queue: 18 / 169
New finding in this append:
    F213 / provisional KI#536
Next finding number:
    F214 UNUSED

QUEUE-031 INITIAL DELTA CLASSIFICATION
--------------------------------------
A normalized function-body comparison was performed before the inherited audit so that
CRLF/comment/formatting churn in the raw diff would not be mistaken for SAS behavior.
Current SAS and Base AdvCiv 1.14 each contain the same 61 UWAICache.cpp function
definitions. Only two function bodies differ behaviorally:

    UWAICache::calculateThreatRating
    UWAICache::updateWarAnger

Both differences are already-owned reviewed repairs rather than new queue-031 roots:

* updateWarAnger is the F112 / KI#435 repair. AdvCiv divided an enemy-team war-weariness
  contribution by the cache owner's team size even though the team contribution is
  consumed once per living enemy player. Current SAS divides by that enemy team's
  living-member count.

* calculateThreatRating is the F121 / KI#444 bounded startup-clock repair. AdvCiv used
  currentEra <= startEra as an unbounded startup guard; a terminal-era start can never
  leave the starting era. Current SAS preserves the era shortcut but bounds it by 25
  normalized startup turns.

The current UWAICache.h normalized diff is formatting-only. No SAS data member or public
cache ABI change was found at queue entry.


F213 / provisional KI#536
-------------------------
TITLE
    Full UWAICache reset does not clear m_readyToCapitulateTo, so a new game or loaded
    save can inherit human-capitulation authorization from an earlier in-memory game.

STATUS
    CONFIRMED. Inherited AdvCiv/UWAI cache-reset and save-load defect dating to the
    initial 2017 WarAndPeaceAI cache integration; present in Base AdvCiv 1.14 and current
    AdvCiv-SAS.

ROOT
    m_readyToCapitulateTo is persistent temporal authorization state. It is serialized:

        pStream->Write(m_readyToCapitulateTo.size());
        ...
        pStream->Write(*it);

    and restored by insertion:

        int iSize;
        pStream->Read(&iSize);
        for (int i = 0; i < iSize; i++)
        {
            TeamTypes eMaster;
            pStream->Read((int*)&eMaster);
            ...
            m_readyToCapitulateTo.insert(eMaster);
        }

    read() explicitly calls the cache's full reset first:

        m_eOwner = ...;
        // Important to set owner first b/c clear uses it
        clear();

    However, UWAICache::clear(false) resets/deallocates every neighboring persistent
    team cache family -- past war score, bounty, sponsor, hireability, military branches,
    etc. -- without ever clearing m_readyToCapitulateTo. init() and uninit() also call
    this same full clear(). Therefore the set is neither replacement-read state nor a
    clean new-game state.

SAVE/OBJECT-REUSE PROOF
    This is not dependent on allocator reuse speculation. UWAICache.h documents the
    intended object lifecycle explicitly: CvPlayer objects are reset and reused when
    returning to the opening menu or loading a savegame; read() is supposed to reset
    the cache data members, while init() resets them for a new game. CvPlayerAI allocates
    m_pUWAI once in its constructor and deletes it only in the CvPlayerAI destructor.
    UWAI::Player::init/read simply delegate into the existing m_cache object.

    Consequently a supported sequence is:

    1. In game A, AI player P's leader cache obtains ReadyToCapitulate(H)=true for human
       team H.
    2. Return to menu and start game B, or load save B in the same Civ4 process.
    3. The existing P cache is reset/reused. init() or read() calls clear(false), but
       the old H entry survives.
    4. For a loaded save whose serialized ready set does NOT contain H, read() inserts
       only the save's actual entries but never erases the stale H entry. The resulting
       state is effectively OLD_SET union SAVED_SET instead of SAVED_SET.

    Thus the exact same save can yield a different m_readyToCapitulateTo state when
    loaded in a fresh process versus after another game. A new game can likewise begin
    with authorization inherited from the previous game occupying that player slot.

CONSEQUENCE
    CvTeamAI::AI_surrenderTrade deliberately treats this bit as a gate proving that
    UWAI::Team::considerPeace has authorized capitulation to a human master. A stale true
    can therefore satisfy that temporal safeguard even though the current game/save never
    granted it. The ordinary power, war-success, city and diplomacy surrender checks still
    apply; this finding does NOT claim unconditional immediate capitulation. The defect is
    state contamination / save-load nondeterminism in an authorization bit whose explicit
    purpose is to prevent capitulation before the current AI-turn decision path runs.

RELATION TO F196 / KI#519
    F196 is a different lifecycle root: within one continuous game, a valid authorization
    from war A survives ordinary peace and can be reused in later war B because war end
    does not clear it.

    F213 is the stronger full-reset/save-load boundary: even a new game or an unrelated
    loaded save can inherit authorization from an earlier in-memory game because clear()
    itself fails to reset the serialized set. Fixing either root alone does not prove the
    other fixed. They may share the same state variable but violate different lifecycle
    contracts and need separate regression tests.

ANCESTRY
    The initial AdvCiv WarAndPeaceAI integration commit (practical 1253,
    f6ff6a182f, 2017-04-16) introduced the same combination in WarAndPeaceCache:

    * readyToCapitulate is serialized;
    * read() calls clear() and then inserts each loaded master team;
    * init() calls clear();
    * clear(false) resets neighboring persistent data but never clears
      readyToCapitulate.

    Later renaming/refactoring from WarAndPeaceCache to UWAICache preserves the omission.
    Base AdvCiv 1.14 and current SAS still have it unchanged.

REPAIR DIRECTION
    Reset this set only on a FULL cache reset, alongside the state that is intentionally
    not recomputed by update():

        if (!bBeforeUpdate)
        {
            ...
            m_readyToCapitulateTo.clear();
            ...
        }

    Do not blindly clear it in the common portion of clear(), because update() calls
    clear(true) every player/team turn and the readiness authorization is intentionally
    allowed to survive ordinary cache recomputation long enough to serve the human
    capitulation gate. F196 separately argues for clearing the corresponding target on
    war end.

NEGATIVE CONTROLS
    * This is not caused by old save versions. The set has been serialized since the
      initial cache implementation, and current version-9 reads have the same union
      behavior.
    * std::set suppresses duplicate entries, so the bug is not duplicate growth; it is
      retention of stale team IDs absent from the incoming save/new game.
    * The neighboring m_bOffensiveTrait / m_bDefensiveTrait fields are not analogous:
      init() immediately recomputes them and read() overwrites them from the stream.
    * clear(true) intentionally preserves several asynchronously maintained fields;
      the defect is specifically that clear(false), whose documented purpose is a full
      reset for init/read/uninit, also preserves this serialized authorization set.


OTHER QUEUE-ENTRY WORK / NEGATIVE CONTROLS
------------------------------------------
* createMilitaryBranches/deleteMilitaryBranches and city-list ownership were traced
  through init, read, uninit and the destructor. read() calls clear(false), which deletes
  the old military branch objects before createMilitaryBranches; no duplicate-vector
  allocation root was found there.
* m_abPlayerLocated exists in the header but has no live references in current SAS or
  Base AdvCiv 1.14. Its lack of reset/serialization is dead state, not a gameplay cache
  defect.
* m_aiReachableCities' own-player value is initialized from getNumCities in clear(),
  while incremental City::add only adjusts foreign owners. Current consumers query
  foreign/target players rather than relying on the cache owner's own slot, so no live
  incremental own-city count defect has been promoted.


C019-WIP01 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP01
CURRENT QUEUE-031 ROOTS: F213 / provisional KI#536
NEXT ROOT ID: F214 UNUSED

Resume with the inherited 59-function cache audit. Prioritize:

1. fields intentionally preserved by clear(true) versus all mutation/reset producers;
2. City incremental add/remove/ownership-change invariants and m_cityList/m_cityMap
   synchronization;
3. MilitaryBranch/non-naval-unit count lifecycle, including revival/new-player paths;
4. team-leader migration (addTeam/onTeamLeaderChanged) and team-only state ownership;
5. read/write version symmetry and City pointer reconstruction after load;
6. cache update ordering relative to CvTeamAI war planning and mid-turn reports.

Do not duplicate the already-owned UWAICache manifestations from C014-C017, especially
F112/KI#435, F121/KI#444, F129/KI#452, F137/KI#460, F142/KI#465 and F196/KI#519. F214
is optional; preserve clean rejections rather than manufacturing another root.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP02 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP01
New finding in this append:
    F214 / provisional KI#537
Next finding number:
    F215 UNUSED

F214 / provisional KI#537
-------------------------
TITLE
    MilitaryBranch typical-unit refresh is sticky: when a branch loses every currently
    trainable candidate, the cache keeps its old unit and can forecast military capacity
    that no longer exists (current concrete case: losing Uranium retains nuclear buildup).

STATUS
    CONFIRMED CURRENT CROSS-FILE CACHE-REFRESH DEFECT. Root is inherited from the initial
    2017 UWAI/MilitaryBranch integration and is unchanged in Base AdvCiv 1.14 and current
    AdvCiv-SAS. Reached from queue 031 through UWAICache::updateTypicalUnits; the local
    implementation lives in queue 064 MilitaryBranch.cpp and must be treated as an
    already-owned root when that file is audited later.

ROOT
    UWAICache::update() calls updateTypicalUnits() every ordinary cache refresh:

        updateTypicalUnits();

    and UWAICache::updateTypicalUnits() asks every persistent MilitaryBranch object to
    refresh itself:

        for (size_t i = 0; i < m_militaryPower.size(); i++)
            m_militaryPower[i]->updateTypicalUnit();

    However, MilitaryBranch::updateTypicalUnit() does not invalidate m_eTypicalUnit or
    m_rTypicalPower before looking for the best CURRENTLY trainable candidate. It merely
    overwrites them when a candidate beats the default-zero rBestValue. If no candidate
    survives the trainability/domain/utility checks, the previous turn's unit and power
    remain cached.

    NuclearArsenal::updateTypicalUnit() independently repeats the same pattern rather
    than calling the base implementation. It even returns immediately when the owner has
    no capital without clearing the old nuke first.

    Therefore these functions implement "replace if a new candidate exists" rather than
    the required refresh/replacement semantics "compute the best current candidate, or
    NO_UNIT if there is none."

CURRENT-SAS NUCLEAR REPRODUCTION
    This is live under the supplied current XML, not merely a future-mod robustness case.
    Both current nuclear units require Uranium:

        UNIT_TACTICAL_NUKE  -> BonusType BONUS_URANIUM
        UNIT_ICBM           -> BonusType BONUS_URANIUM

    and both require Fission + Rocketry. CvCity::canTrain delegates to CvPlot::canTrain;
    with bTestVisible=false, CvPlot::canTrain explicitly rejects a unit whose required
    BonusType is not connected/available in that city.

    Supported sequence:

    1. Player P has Fission/Rocketry and Uranium. Its capital can train an ICBM or
       Tactical Nuke. UWAICache::update -> NuclearArsenal::updateTypicalUnit stores a
       nuclear m_eTypicalUnit and matching m_rTypicalPower.
    2. P then loses its last connected Uranium (e.g. improvement/resource connection is
       lost or the supplying trade ends) while remaining otherwise alive and developed.
    3. On the next cache update, the Nuclear branch scans the same civ units. The capital
       canTrain checks now reject both nukes.
    4. No candidate enters the rLoopValue > rBestValue assignment, but the routine never
       first sets m_eTypicalUnit=NO_UNIT. The old nuke therefore survives indefinitely
       while Uranium remains unavailable.

    This is a post-refresh stale value, not merely the expected one-turn interval between
    an asynchronous resource change and the next UWAICache update.

DIRECT WAR-EVALUATION CONSEQUENCE
    InvasionGraph::Node::initMilitary() copies the current cached MilitaryBranch objects,
    including the stale NuclearArsenal, into the simulation.

    ArmamentForecast then deliberately reserves an offensive nuclear buildup share
    (20 percent in current AdvCiv/SAS) and documents the intended safeguard:

        // Undone below if unable to build nukes

    The safeguard is exactly the typical-unit test shared by all branches:

        if (m_kMilitary[i]->getTypicalUnit() == NO_UNIT)
        {
            rSurplus += rBranchPortions[i];
            rBranchPortions[i] = 0;
        }

    Because the stale Nuclear branch still reports the old nuke, the safeguard does not
    fire. The forecast later calls getTypicalCost/getTypicalPower and converts the
    nuclear branch's production share into predicted extra nuclear military power even
    though no city can currently train a nuke.

    This can feed InvasionGraph/MilitaryAnalyst/WarEvaluator with an overstated armament
    trajectory and therefore alter war/peace utility decisions. It does not require the
    player to have zero existing nukes; the defect is specifically that FUTURE production
    is forecast from a unit that is no longer trainable.

BROADER SAME-ROOT MANIFESTATIONS
    The stale-refresh contract is not nuclear-specific.

    * For sea branches the base update uses CvTeamAI::AI_isExpectingToTrain, which scans
      actual cities and requires a qualifying coastal city that can train the unit. A civ
      that loses its last useful coastal city can therefore have no current Fleet or
      Logistics candidate while retaining the old cached ship/transport. ArmamentForecast
      can then continue assigning naval production to capacity the civ can no longer
      build, and UWAICache::shipSpeed can continue using a stale Logistics transport.

    * Any branch whose last trainable candidate disappears through a resource/prerequisite
      change has the same sticky-unit behavior. Nuclear is the clearest current-data case
      because both current nuke types share the same removable Uranium requirement.

    These are manifestations of one refresh-invalidation root, not additional finding IDs.

ANCESTRY / INTENT PROOF
    Practical AdvCiv commit 1253 (f6ff6a182f, 2017-04-16), the initial WarAndPeace/UWAI
    cache integration, already contains both sides of the contradiction:

    * MilitaryBranch::updateTypicalUnit and NuclearArsenal::updateTypicalUnit assign the
      stored typical unit only after finding a better candidate and never clear it at
      refresh start.
    * The initial ArmamentForecast comments that its 25-percent nuclear share is
      "Undone below if unable to build nukes" and implements that by testing whether the
      branch's typical unit is NULL.

    Thus the cache value was intended from its introduction to represent CURRENT
    buildability, but the invalidation case was omitted. Later refactors changed names,
    containers and numeric types but preserve the sticky refresh. Base AdvCiv 1.14 and
    current SAS remain behaviorally the same here.

REPAIR BOUNDARY
    At minimum, each typical-unit refresh must begin from an invalid/no-candidate state:

        m_eTypicalUnit = NO_UNIT;
        m_rTypicalPower = 0;

    before scanning candidates. This must also be done in NuclearArsenal's independent
    override (or centralized in a helper that every override invokes), and before its
    early no-capital return.

    Do not reset m_rTotalPower or m_iUnits: those are separately maintained counts of the
    military actually possessed, not trainability estimates.

    Fleet has one related derived-cache detail that must not be missed when implementing
    the general repair. Fleet::updateTypicalUnit currently updates m_bCanBombard only if
    getTypicalUnit()!=NO_UNIT. Once the typical unit is correctly invalidated, a no-candidate
    Fleet must also leave m_bCanBombard false rather than retaining the old ship's flag.
    Army::updateCanBombard/updateCanSoften already recompute their booleans to a false
    fallback on every refresh.

    Queue 064 MilitaryBranch.cpp should revisit implementation mechanics and any additional
    derived-field siblings, but must not allocate a new root for this already-proven
    typical-unit invalidation defect.

NEGATIVE CONTROLS
    * This is not a serialization-only issue. A correctly serialized old typical unit
      should be refreshed on the next UWAICache update; the bug is that refresh itself
      cannot express "no candidate now."
    * Existing nuclear power is tracked separately by m_rTotalPower and unit reports.
      Clearing the typical unit does not erase or undercount already-built nukes.
    * The issue does not depend on tech loss, which ordinary Civ4 rarely permits. Current
      SAS Uranium availability can change during normal play while the nuclear techs and
      player remain valid.
    * A branch changing from one trainable unit to another is normally safe because a new
      candidate overwrites the old value. The failing transition is specifically
      SOME_CANDIDATE -> NO_CURRENT_CANDIDATE.


PRESERVED-FIELD MATRIX -- INTERIM NOTES
---------------------------------------
* The earlier onTeamLeaderChanged / m_abCanBeHiredAgainst seam remains under review and is
  NOT F215 at this checkpoint. A newly selected AI team leader can carry its old team's
  hireability bits through a Permanent Alliance because onTeamLeaderChanged does not reset
  that map despite its comment saying CanBeHired should start false. However, the actual
  declareWarTrade path still performs legality/attitude checks and a fresh WarEvaluator
  before accepting a sponsored war. The stale true bit may therefore be chiefly an
  alert/evaluation-suppression timing discrepancy rather than a demonstrated incorrect
  war authorization. Do not promote without a stronger consequence proof.

* F214 is distinct from F213. F213 is a full-reset/read replacement failure in a serialized
  capitulation-authorization set. F214 is an ordinary per-turn refresh failure in a
  trainability-derived military estimate.


C019-WIP02 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP02
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
NEXT ROOT ID: F215 UNUSED

Resume the inherited cache-lifecycle matrix from the intentionally preserved fields.
Finish the team-leader migration/canHire check without forcing a root; then continue city
incremental add/remove/ownership synchronization, military unit-count reports and
read/write pointer reconstruction. When queue 064 MilitaryBranch.cpp is eventually audited,
mark F214 as already owned before looking for any distinct sibling.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP03 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP02
New finding in this append:
    F215 / provisional KI#538
Next finding number:
    F216 UNUSED

F215 / provisional KI#538
-------------------------
TITLE
    Team-leader migration leaves CanBeHired state in the promoted player's private cache;
    a new leader can inherit default/stale TRUE hireability and immediately allow a
    sponsored war that the migration contract says must start disabled.

STATUS
    CONFIRMED INHERITED UWAI TEAM-STATE MIGRATION DEFECT. Current in Base AdvCiv 1.14
    and AdvCiv-SAS. Especially clear when an AI team leader dies and an existing AI
    teammate becomes leader; Permanent Alliance leader replacement is another supported
    path. The defect predates SAS and is rooted in the interaction between advc.104t
    leader-cache migration and the later war-trade-alert/hireability cache.

ROOT
    UWAICache stores some team-level UWAI state in only the current team leader's player
    cache. UWAICache::leaderCache() redirects accesses to that current leader. The header
    explicitly documents that this team data has to be moved whenever the team's leader
    changes.

    CvTeam::updateLeaderID() detects a leader change and calls onTeamLeaderChanged on the
    NEW leader's UWAICache:

        if (m_eLeader != eFormerLeader && getUWAI().isEnabled() &&
            GET_PLAYER(m_eLeader).isAlive())
        {
            GET_PLAYER(m_eLeader).uwai().getCache().onTeamLeaderChanged(eFormerLeader);
        }

    UWAICache::onTeamLeaderChanged copies selected team-level state from the former
    leader, including m_aiWarUtilityIgnoringDistraction and human-target capitulation
    readiness, then ends with the explicit contract comment:

        /*  (CanBeHired and ReadyToCapitulate will get updated soon enough.
            Start out as false for the new leader.) */

    However it never modifies m_abCanBeHiredAgainst.

    That omission is not neutral. m_abCanBeHiredAgainst is declared as

        CivTeamMap<bool,void*,true> m_abCanBeHiredAgainst;

    so its enum-map default is TRUE. A normal reset() also restores TRUE, not false.
    Moreover, while a player is a non-leader, the live team hireability state is updated
    in the current leader's cache; the subordinate player's own map can therefore remain
    at its old/default contents. Promoting that player makes this previously private map
    the new live leader cache without the intended false initialization.

CONCRETE CURRENT RUNTIME PATH -- LEADER DEATH
    This does not require the more complicated Permanent Alliance case.

    1. Two AI players share a team. Player A is the current team leader; player B is a
       surviving teammate.
    2. UWAI war planning updates hireability through the team leader cache. Player B's
       private m_abCanBeHiredAgainst does not represent the team's current values. In a
       clean/default cache its entries are TRUE.
    3. Player A is eliminated while B remains alive. Normal player/team lifecycle calls
       CvTeam::updateLeaderID(), which promotes B and invokes B's
       UWAICache::onTeamLeaderChanged(A).
    4. onTeamLeaderChanged copies some team data but never clears B's CanBeHired map.
       Consequently B becomes the authoritative leader with default/stale TRUE entries
       even where the former team state was false and despite the function's own
       "Start out as false" contract.

    Permanent Alliance formation supplies another supported path: CvTeam::addTeam saves
    the absorbed team's old leader, moves its players to the surviving team, and the
    repeated setTeam/updateLeaderID sequence can select an absorbed AI player as the new
    surviving-team leader (e.g. the lower player id when both candidates are otherwise
    equal). That player's old-team hireability bits then become the merged team's live
    bits unless explicitly reset.

GAMEPLAY CONSEQUENCE -- NOT ONLY AN ALERT/CPU THROTTLE
    canBeHiredAgainst is documented as only a necessary condition, but it is a real
    branch controlling whether sponsored-war evaluation is allowed.

    UWAI::Team::declareWarTrade uses the bit for a human sponsor in a large game:

        if (!kSponsor.isHuman() || kSponsor.getHasMetCivCount() < 8 ||
            leaderCache().canBeHiredAgainst(eTarget))
        {
            ... WarEvaluator ...
            if (iU > iUtilityThresh)
                return NO_DENIAL;
        }

    Therefore FALSE does not merely suppress an alert message: for the gated large-game
    human-sponsor case it prevents this path from returning NO_DENIAL at all. A stale
    TRUE left in the promoted leader's private cache permits a fresh WarEvaluator and can
    immediately make the sponsored declaration tradeable. Had onTeamLeaderChanged
    obeyed its stated false-initialization contract, that same request would remain
    denied until normal UWAI war planning recomputed hireability for the new leader.

    The later legality/attitude/power checks in CvTeamAI::AI_declareWarTrade still run,
    so this does not bypass core war legality. The bug is narrower and deterministic:
    the leader transition can incorrectly bypass UWAI's deliberate large-game
    hireability gate and change diplomatic deal availability/timing.

ANCESTRY / INTENT PROOF
    The history separates the pieces unusually well.

    * Practical AdvCiv 1268 (e11525d303) introduced onTeamLeaderChanged for team-level
      WarAndPeace/UWAI state before the hireability cache existed.
    * Practical AdvCiv 1343 (f79b7f7d5f), titled
      "Tried to reduce the flickering of the war trades alert", introduced hireAgainst,
      serialized it, initialized/reset it TRUE in that version, and made
      declareWarTrade condition its large-game human evaluation on canBeHiredAgainst.
      That commit did not extend the already-existing leader-migration routine.
    * Practical AdvCiv 2920 (4ae382e5db), "Revised all code in the UWAI component",
      contains the modern onTeamLeaderChanged form and added the explicit comment that
      CanBeHired should start FALSE for the new leader, but still added no assignment.
    * The current CivTeamMap conversion preserves TRUE as the map's default and the
      omission survives unchanged in Base AdvCiv 1.14 and current SAS.

    Thus both historical behavior and a later explicit maintenance comment support the
    same contract: the newly authoritative leader must not inherit arbitrary private
    hireability state.

REPAIR BOUNDARY
    In UWAICache::onTeamLeaderChanged, explicitly initialize the NEW leader's
    m_abCanBeHiredAgainst entries to false as part of the leader migration, before they
    can be queried as authoritative team data.

    A plain

        m_abCanBeHiredAgainst.reset();

    is NOT sufficient because this map's declared default is true. Use an explicit false
    fill or set the relevant major-civ team entries false in the migration loop.

    Do not copy the former leader's CanBeHired bits blindly. The source comment's design
    is specifically to start false and allow normal UWAI war planning to recompute the
    new leader's willingness soon afterward; this also avoids carrying old personality/
    planning-derived suppression state through the leadership transition.

    This repair is independent of F213/KI#536. F213 concerns full reset/read replacement
    of m_readyToCapitulateTo across games/loads. F215 concerns transfer of a team-level
    runtime cache when the authoritative PLAYER cache changes within a live game.

NEGATIVE CONTROLS / SCOPE
    * CvTeamAI::AI_declareWarTrade still performs canDeclareWar, attitude, nukes and
      other checks before UWAI::Team::declareWarTrade. F215 does not permit an illegal
      declaration; it can change a normally legal sponsored war from gated/denied to
      evaluable/acceptable too early.
    * In small games or for AI sponsors, declareWarTrade deliberately does not consult
      CanBeHired. The concrete effect is the large-game human-sponsor branch for which
      the cache was introduced.
    * If the promoted player's private bit happens already to be false, that target is
      unaffected. The defect is that private/default history is allowed to determine
      new team state at all.
    * The explicit true default is not itself a defect during ordinary cache init; that
      behavior came from the alert-throttling design. The defect is failing to perform
      the special FALSE initialization required by leader migration.


CITY / POINTER LIFECYCLE PASS -- INTERIM NEGATIVE CONTROLS
-----------------------------------------------------------
* cacheCitiesAfterRead is called only after all CvPlayer/CvCity objects have been read,
  and each serialized UWAICache::City then resolves its plot index back to the plot's
  current CvCity. No ordinary pre-city-load dereference was found.
* City destruction removes the UWAICache entry after CvPlot::setPlotCity(NULL), but the
  CvCity object itself is still alive at that point. remove() dereferences its cached
  CvCity pointer only for owner metadata, then erases/deletes the cache object before
  the CvCity is destroyed; no dangling-pointer use was established there.
* Acquisition creates a new city object after the old city has gone through the kill /
  reportCityDestroyed path, and then notifies all major AI caches through
  reportCityCreated. This is consistent with the source comment that ownership changes
  are represented as destruction + creation and avoids an in-place owner mutation in
  the cached City object.
* m_aiReachableCities intentionally does not increment same-team member cities in add();
  current numReachableCities consumers are target/enemy reachability logic, so no
  teammate-count consumer has yet been established. Keep this as a negative control
  unless a same-team caller appears later.


C019-WIP03 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP03
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
NEXT ROOT ID: F216 UNUSED

Resume the inherited cache-lifecycle matrix at city synchronization and asynchronous
unit reporting. In particular: prove add/remove/map-vector replacement invariants across
founding, conquest, raze and visibility/deduction transitions; reconcile m_iNonNavalUnits
and MilitaryBranch reportUnit callbacks with unit conversion/capture/gift/upgrade paths;
then finish read/write reconstruction and the remaining preserved-state fields. Do not
spend F216 on an intentional approximation or merely stale comment.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP04 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP03
New finding in this append:
    F216 / provisional KI#539
Next finding number:
    F217 UNUSED

F216 / provisional KI#539
-------------------------
TITLE
    Permanent Alliance cache merge iterates the already-merged live team and applies
    the absorbed leader's history back onto absorbed members; the absorbed leader
    self-adds and doubles its own past-war score.

STATUS
    CONFIRMED INHERITED ADVCIV/UWAI REGRESSION. Current in Base AdvCiv 1.14 and
    AdvCiv-SAS. Introduced by the AgentIterator conversion in practical AdvCiv commit
    1818 (854cb8058c, "Use AgentIterators in all UWAI classes"), which changed the
    membership source at exactly the point where CvTeam::addTeam has already reassigned
    the absorbed players to the surviving team.

ROOT
    CvTeam::addTeam saves the absorbed team's old leader id, then reassigns every player
    on the absorbed team to the surviving team:

        PlayerTypes eTeamLeader = GET_TEAM(eTeam).getLeaderID();
        for (int i = 0; i < MAX_PLAYERS; i++)
        {
            if (GET_PLAYER((PlayerTypes)i).getTeam() == eTeam)
                GET_PLAYER((PlayerTypes)i).setTeam(getID());
        }
        updateLeaderID();

    Only much later, after those player-team identities have changed, the UWAI merge is
    invoked:

        if (getUWAI().isEnabled())
            AI().uwai().addTeam(eTeamLeader);

    UWAI::Team::addTeam then uses the CURRENT team membership:

        for (MemberAIIter it(m_eAgent); it.hasNext(); ++it)
            it->uwai().getCache().addTeam(eOtherLeader);

    and UWAICache::addTeam obtains the absorbed leader's cache and adds its stored
    history into the recipient cache:

        UWAICache& kOther = GET_PLAYER(eOtherLeader).uwai().getCache();
        ...
        m_aiPastWarScore.add(eTeam, kOther.m_aiPastWarScore.get(eTeam));

    Because the former absorbed leader is now itself a MemberAIIter member of m_eAgent,
    one loop iteration has

        &it->uwai().getCache() == &kOther

    and consequently performs, for every iterated target team,

        ownPastWarScore += ownPastWarScore;

    All other absorbed-team members are likewise treated as if they had belonged to the
    original surviving team and receive the absorbed leader's history, even though that
    was not the pre-regression membership boundary.

CONCRETE GAMEPLAY CONSEQUENCE
    m_aiPastWarScore is live decision state, not archival statistics. It feeds:

        UWAICache::pastWarScore
          -> UWAI::Player::confidenceFromPastWars
          -> UWAI::Player::warConfidenceLearned
          -> InvasionGraph attack/defense confidence

    confidenceFromPastWars converts the score through a square-root confidence term.
    For example, an absorbed leader with pastWarScore -100 against a rival has roughly
    85% past-war confidence from this component. The self-add changes the stored score
    to -200 and lowers that component to about 79%. Positive history is amplified in the
    same way. Larger accumulated scores can create still larger changes until the
    function's final confidence clamp applies.

    Therefore a Permanent Alliance can instantaneously rewrite the absorbed leader's
    learned military confidence merely because its player object changed teams. The
    same cache also merges sponsored-war bounty/sponsor state; the post-1818 live-team
    loop can apply absorbed-leader data to absorbed teammates that the original merge
    loop did not visit. The past-war self-double is the simplest deterministic proof and
    is sufficient for the root.

ANCESTRY / REGRESSION PROOF
    The old and new implementations make the accidental membership expansion unusually
    clear.

    * The initial WarAndPeace/UWAI implementation used a Team::members vector. Its
      Team::addTeam loop was:

          for (size_t i = 0; i < members.size(); i++)
              GET_PLAYER(members[i]).warAndPeaceAI().getCache().addTeam(otherLeaderId);

      CvTeam::addTeam invoked this merge and only AFTERWARD called the global UWAI
      update. Thus, at the merge call, Team::members still described the surviving
      team's PRE-MERGE members. The absorbed leader was not visited and could not
      self-add.

    * Practical AdvCiv commit 1818 (854cb8058c), titled
      "Use AgentIterators in all UWAI classes", replaced that cached-vector loop with:

          for (MemberIter it(agentId); it.hasNext(); ++it)
              it->warAndPeaceAI().getCache().addTeam(otherLeaderId);

      In the same conversion series, the old post-addTeam global UWAI refresh was
      removed because AgentIterators made cached membership unnecessary.

    * But CvTeam::addTeam already changes each absorbed player's CvPlayer::team before
      calling UWAI::Team::addTeam. The new live iterator therefore sees the merged
      membership, not the old surviving-team membership that the replaced vector had
      represented at this call site.

    * Later UWAI renames/revisions retain the dynamic MemberIter/MemberAIIter loop and
      the current bug survives unchanged in Base AdvCiv 1.14 and SAS.

    This is therefore not an ambiguous choice about how alliance history ought to be
    blended. The AgentIterator refactor changed the actual recipient set and introduced
    a self-add that the immediately preceding implementation could not perform.

REPAIR BOUNDARY
    Restore the original PRE-MERGE recipient boundary. Do not merely special-case
    eOtherLeader: a multi-member absorbed team would still have its other absorbed
    members incorrectly visited by the live post-merge iterator.

    The cleanest structural repair is to perform the UWAI cache merge while the
    surviving team's membership still has its pre-merge identity (before the absorbed
    players' setTeam loop), or otherwise explicitly preserve/pass the original survivor
    member set to UWAI::Team::addTeam. UWAICache::addTeam itself can then retain its
    existing per-recipient merge logic.

    If moving the call earlier, re-check the now-soon-to-disappear absorbed-team key in
    the TeamIter<MAJOR_CIV> loop; the essential invariant is that only ORIGINAL survivor
    members receive eOtherLeader's cache contribution. Avoid a repair based solely on
    current MemberAIIter membership after setTeam, because the information about which
    players belonged to which side has already been erased.

    Do not 'fix' this by clearing or averaging all past-war scores on a Permanent
    Alliance. reportWarEnding explicitly treats past-war interpretation as player-level
    state; F216 is about applying the other leader's contribution to the wrong recipient
    set, not about redesigning that heuristic.

NEGATIVE CONTROLS / SCOPE
    * Original surviving-team members are legitimate recipients of the absorbed
      leader's cache contribution; their addTeam call is not the defect.
    * The absorbed leader's self-add is deterministic whenever a relevant nonzero
      m_aiPastWarScore exists. No save/load timing, RNG or UI action is required.
    * F215/KI#538 is a distinct Permanent-Alliance/leader-change root: F215 concerns
      failure to false-initialize team-level CanBeHired when the authoritative leader
      cache changes. F216 concerns the recipient set used when merging per-player
      history/sponsorship caches.


UNIT REPORTING / CONVERSION TIMING -- REJECTED F216 CANDIDATE
--------------------------------------------------------------
The earlier apparent temporary double count during CvUnit::convert does not justify a
separate root:

* initUnit reports the replacement unit immediately and convert ends by kill(true) on
  the source, so there is indeed a short staging interval before the old unit's
  reportUnitDestroyed callback.
* Human COMMAND_UPGRADE and COMMAND_GIFT both return through CvUnit::doCommand, which
  immediately calls getGroup()->doDelayedDeath().
* The normal AI upgrade path explicitly calls doDelayedDeath immediately after upgrade.
* AI_doSplit can call gift() directly, but this occurs inside AI_doTurnUnitsPre; after it
  returns, CvPlayer::doTurnUnits immediately performs doDelayedDeath on every group
  before normal group turns. No intervening UWAI cache consumer was found in that path.
* CyUnit::convert exposes raw conversion to Python, but current SAS source contains no
  Python caller of that wrapper. Do not promote a hypothetical external-mod misuse as a
  current SAS/AdvCiv cache bug.

Thus the +1-before--1 interval is supported delayed-death staging on live current paths,
not F216. F216 is the independent Permanent Alliance history merge regression above.


C019-WIP04 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP04
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
NEXT ROOT ID: F217 UNUSED

Continue the inherited cache-lifecycle matrix from the war-state merge cluster. Challenge
reportWarEnding / sponsorship / addTeam state for any distinct replacement-vs-merge root,
then finish serialization-version symmetry, update ordering, city reachability/deduction
refresh and remaining low-mention helpers. Keep F215 and F216 separate despite both being
visible during Permanent Alliances.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP05 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP04
New finding in this append:
    F217 / provisional KI#540
Next finding number:
    F218 UNUSED

F217 / provisional KI#540
-------------------------
TITLE
    UWAICache identifies Fallout as the first feature with <= -50 health; SAS Jungle is
    now first and also -50, so UWAI believes Fallout can be scrubbed at Metal Casting
    instead of Environmentalism and understates nuclear-war damage.

STATUS
    CONFIRMED SAS CROSS-LAYER REGRESSION exposing a brittle inherited AdvCiv/UWAI data
    assumption. Base BtS/AdvCiv 1.14 data satisfy the old uniqueness assumption because
    FEATURE_FALLOUT is the only feature at <= -50 health. Current SAS no longer does.

ROOT
    UWAICache::updateCanScrub does not identify Fallout by type. It searches FeatureTypes
    in enum/XML order and caches the first sufficiently unhealthy feature:

        static FeatureTypes eFallout = NO_FEATURE;
        if (eFallout == NO_FEATURE)
        {
            FOR_EACH_ENUM(Feature)
            {
                if (GC.getInfo(eLoopFeature).getHealthPercent() <= -50)
                {
                    eFallout = eLoopFeature;
                    break;
                }
            }
        }

    It then asks whether the owner knows any BuildInfo feature-removal tech for that
    selected feature:

        TechTypes eFeatureTech = GC.getInfo(eLoopBuild).getFeatureTech(eFallout);
        if (eFeatureTech != NO_TECH && GET_TEAM(m_eOwner).isHasTech(eFeatureTech))
            m_bCanScrub = true;

    In current SAS CIV4FeatureInfos.xml the relevant order/health values are:

        0 FEATURE_ICE           0
        1 FEATURE_JUNGLE      -50
        2 FEATURE_OASIS         0
        3 FEATURE_FLOOD_PLAINS -50
        4 FEATURE_FOREST       +50
        5 FEATURE_FALLOUT      -75

    Thus the static variable named eFallout is actually initialized to FEATURE_JUNGLE
    on its first call and remains that value for the process lifetime.

CURRENT SAS DATA PROOF
    CIV4BuildInfos.xml gives Jungle-removal FeatureTech TECH_METAL_CASTING on the normal
    improvement builds and BUILD_REMOVE_JUNGLE. Real FEATURE_FALLOUT removal uses
    TECH_ENVIRONMENTALISM, including BUILD_SCRUB_FALLOUT and the improvement builds.

    Therefore once the player knows Metal Casting, updateCanScrub sets:

        m_bCanScrub = true

    despite the player still lacking Environmentalism and being mechanically unable to
    scrub Fallout. The cache is cleared/recomputed each update, but the same wrong
    FeatureTypes singleton is selected each time, so the error persists from Metal
    Casting until real Fallout cleanup finally becomes available.

GAMEPLAY / UWAI CONSEQUENCE
    canScrubFallout is consumed by nuclear-war utility code, not just UI.

    WarUtilityAspect::lossesFromNukes adjusts the expected asset loss per nuclear hit:

        rHits * (kVictimCache.canScrubFallout() ?
                 rLossRate - 0.05 : rLossRate + 0.05) * rScorePerCity

    With the false TRUE state, a non-agent victim uses roughly 0.30 instead of 0.40 from
    the normal 0.35 baseline (25% lower damage estimate), while the agent/victim case
    uses roughly 0.40 instead of 0.50 from the 0.45 baseline (20% lower).

    The peaceful-victory nuclear penalty similarly uses 0.35 population-loss weighting
    when canScrubFallout is true versus 0.45 when false. Thus Metal Casting can make
    UWAI materially less pessimistic about nuclear damage many eras before the actual
    Fallout-removal tech.

ANCESTRY / DATA-CONTRACT PROOF
    The helper dates to the initial 2017 WarAndPeace/UWAI integration, practical AdvCiv
    1253 (f6ff6a182f). Its original code used the same <= -50 search and even documented
    the assumption through the assertion message:

        "Fallout feature not found; should have -50 health"

    Under Base BtS and Base AdvCiv 1.14 XML this happened to be unambiguous:

        FEATURE_FALLOUT  -50

    is the only feature at or below -50. Jungle is -25 and Flood Plains is above the
    threshold. So this is not presented as a live Base AdvCiv defect under its own data.

    SAS practical commit 4876 (4622d268ac) changed FEATURE_JUNGLE health from -25 to
    -50 as part of the Jungle rebalance. Since Jungle precedes Fallout in FeatureTypes,
    that change immediately invalidated the inherited sentinel heuristic. SAS practical
    commit 4905 (6c08e62699) later also changed Flood Plains from -40 to -50 and Fallout
    from -50 to -75, further demonstrating that health magnitude is no longer a unique
    identifier. The bug is therefore a cross-layer integration failure between an
    inherited code assumption and legitimate SAS balance-data changes.

REPAIR BOUNDARY
    Identify the exact Fallout feature rather than inferring identity from health.
    Current SAS already uses the appropriate pattern elsewhere, e.g. CvUnitAI caches:

        static const FeatureTypes eFeatureFallout =
            (FeatureTypes)GC.getInfoTypeForString("FEATURE_FALLOUT");

    UWAICache::updateCanScrub should use the same exact FeatureTypes lookup (cached once)
    and then retain its existing scan for a known FeatureTech that removes that feature.
    An exact BUILD_SCRUB_FALLOUT lookup would also be possible, but the existing helper
    is intentionally asking whether ANY available build can remove Fallout, so exact
    feature identity preserves that broader contract better.

    Do not repair this by changing Jungle/Flood-Plains health back solely to protect the
    heuristic; those are independent SAS balance choices. Do not change the comparison
    from <= -50 to a different magic threshold either: Fallout identity should not depend
    on unrelated feature balance values.

NEGATIVE CONTROLS / SCOPE
    * Base AdvCiv 1.14 and the supplied Civ4CE/BtS data have only FEATURE_FALLOUT at
      <= -50, so their current data do not trigger F217 even though they carry the brittle
      helper.
    * The error does not grant Workers a mechanically legal Fallout-removal action.
      Worker/build legality still uses actual BuildInfo prerequisites. F217 changes UWAI
      strategic valuation because the cache reports a false capability.
    * F214/KI#537 is unrelated despite also involving capability caching: F214 is stale
      MilitaryBranch typical-unit state after capability is LOST; F217 freshly
      recomputes the wrong capability from a misidentified feature each turn.


C019-WIP05 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP05
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
NEXT ROOT ID: F218 UNUSED

Resume after updateCanScrub from the remaining low-mention/update helpers. Finish
serialization/version symmetry and city reachability/deduction refresh, then challenge
updateTransports, target-mission/threat/vassal-score inputs and full update ordering. Keep
Base-AdvCiv latent assumptions separate from SAS-live regressions like F217.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP06 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP05
New finding in this append:
    F218 / provisional KI#541
Next finding number:
    F219 UNUSED

F218 / provisional KI#541
-------------------------
TITLE
    UWAICache::updateTransports uses player-level canTrain, which ignores city/resource
    prerequisites; SAS obsoletes every resource-free assault transport by Combustion, so
    an Oil-less civ is cached as able to train ocean transports although no city can,
    producing false overseas reachability and defeating the SAS no-lift war-target gate.

STATUS
    CONFIRMED SAS CROSS-LAYER REGRESSION exposing an inherited UWAI cache assumption.
    The player-level capability test dates to the initial 2017 WarAndPeace/UWAI cache.
    Base AdvCiv's standard unit progression masks the clean resource-loss reproduction
    because its resource-free Galleon never becomes mechanically obsolete; current SAS's
    explicit ObsoleteTech chain removes that fallback.

ROOT
    UWAICache::updateTransports runs before city-distance caching and currently does:

        CvPlayerAI const& kOwner = GET_PLAYER(m_eOwner);
        CvCivilization const& kOwnerCiv = kOwner.getCivilization();
        for (int i = 0; i < kOwnerCiv.getNumUnits(); i++)
        {
            UnitTypes const eUnit = kOwnerCiv.unitAt(i);
            CvUnitInfo const& kUnit = GC.getInfo(eUnit);
            if (kUnit.getUnitAIType(UNITAI_ASSAULT_SEA) && kOwner.canTrain(eUnit))
            {
                m_bHaveAnyTransports = true;
                if (!kOwner.AI_isAnyImpassable(eUnit))
                    m_bHaveDeepSeaTransports = true;
            }
        }

    The public accessors are explicitly named canTrainAnyCargo and
    canTrainDeepSeaCargo. However CvPlayer::canTrain is only the player/global layer. It
    checks the civilization's unit mapping, technology requirements (including SAS
    ObsoleteTech), state religion, and global/team/player class limits. It does NOT
    inspect a city's plot-group resources or whether any city is geographically able to
    train a sea unit.

    Actual city trainability is stricter:

        CvCity::canTrain(...)
            -> GET_PLAYER(getOwner()).canTrain(...)
            -> getPlot().canTrain(...)

    and CvPlot::canTrain explicitly checks both the AND resource and every
    PrereqOrBonus against pCity->hasBonus(...), rejecting the unit if its required bonus
    is unavailable. It also enforces city/sea placement constraints. Thus
    CvPlayer::canTrain(eUnit) == true is not evidence that ANY owned city can actually
    train eUnit.

CURRENT SAS DATA PROOF
    Current SAS has exactly three UnitInfo entries carrying UNITAI_ASSAULT_SEA:

        UNIT_GALLEY
            PrereqTech   TECH_SAILING
            ObsoleteTech TECH_ASTRONOMY
            bonuses      none

        UNIT_GALLEON
            PrereqTech   TECH_ASTRONOMY
            ObsoleteTech TECH_COMBUSTION
            bonuses      none

        UNIT_TRANSPORT
            PrereqTech   TECH_COMBUSTION
            ObsoleteTech NONE
            PrereqOrBonus BONUS_OIL (the only non-NONE entry)
            impassable terrain/features: none

    Therefore take an ordinary coastal civilization that has learned Combustion, owns
    no Oil, and has no already-built lift that should be considered under a separate
    operational-capacity policy:

        * UNIT_GALLEY fails CvPlayer::canTrain because Astronomy is already known and
          SAS marks it obsolete there.
        * UNIT_GALLEON fails CvPlayer::canTrain because Combustion is known and SAS
          marks it obsolete there.
        * UNIT_TRANSPORT PASSES CvPlayer::canTrain because the player-level function
          sees Combustion and no obsolete-tech/global-limit failure.
        * UNIT_TRANSPORT FAILS every CvCity::canTrain in an Oil-less empire because
          CvPlot::canTrain sees its sole PrereqOrBonus BONUS_OIL and requires a city
          with that connected bonus.

    updateTransports nevertheless sets both:

        m_bHaveAnyTransports = true;
        m_bHaveDeepSeaTransports = true;

    because Transport has no impassable terrain. The cache has therefore asserted an
    immediate training capability that no city possesses.

GAMEPLAY / UWAI CONSEQUENCE
    This state is consumed before and during war evaluation, not only exposed through a
    diagnostic getter.

    1. UWAICache::City::updateDistance reads both cargo booleans. When
       bAnyTransports is true and one of the cache owner's cities is coastal, it is
       allowed to search a naval route to a target. Because the false Transport state
       also sets bDeepSeaTransports, it uses the ANY_WATER pathfinder rather than the
       shallow-water finder. A successful route can set bReachBySea and contribute a
       finite m_iDistance. Thus a target that would otherwise have no supported sea
       route can be cached as reachable solely because the Oil requirement was skipped.

    2. UWAI::Team::isLandTarget and InvasionGraph::Node::canReachByLand consume
       canTrainDeepSeaCargo when deciding whether long land routes may instead be
       treated as naval/overseas problems. The false flag can therefore alter scenario
       classification and invasion reachability assumptions.

    3. Current SAS WarUtilityAspect::Risk::preEvaluate uses
       canTrainAnyCargo explicitly as its pre-war naval lift gate. For each team member
       it records a cargo-qualified contact distance only when:

           bool const bMemberCanTrainCargo = kMemberCache.canTrainAnyCargo();

       and later defines bNoLift from whether any such route exists. Because the same
       false transport flag can already have made an overseas city reachable in the
       cache, an Oil-less post-Combustion coastal civ can satisfy both the route and
       cargo-building parts of this test. The hard reject intended for a naval target
       with no trainable lift can therefore be bypassed.

    A concrete reproduction does not require loss of an existing transport: a civ can
    simply research/acquire Combustion while lacking Oil and have no pre-built
    Galleons. At that instant the SAS ObsoleteTech transition removes its last
    resource-free trainable assault transport, while updateTransports still reports the
    Oil-gated Transport as trainable.

ANCESTRY / ACTIVATION BOUNDARY
    The underlying player-level assumption is old. Initial practical AdvCiv commit 1253
    (f6ff6a182f, "Initial commit (cont.)") introduced WarAndPeaceCache::updateTrainCargo
    with the same essential test:

        if(info.getUnitAIType(UNITAI_ASSAULT_SEA) && owner.canTrain(ut))
            trainAnyCargo = true;

    Later UWAI revisions renamed/restructured this as updateTransports but retained
    CvPlayer::canTrain. Practical AdvCiv 2920 (4ae382e5db, "Revised all code in the
    UWAI component") shows that direct continuity.

    Base AdvCiv 1.14 also gives UNIT_TRANSPORT an Oil PrereqOrBonus, but its Galley and
    Galleon have no ObsoleteTech mechanism/tag and remain player/city-trainable once
    unlocked. Hence an Oil-less post-Combustion standard Base-AdvCiv civ can still
    genuinely train the resource-free Galleon, so this exact all-transports-untrainable
    resource reproduction is masked under base data.

    SAS practical commit 5321 (6bd3873c83) changed the naval progression so Galley
    obsoletes at Astronomy and Galleon (including the then-current Carrack replacement)
    obsoletes at Combustion. Current SAS later removed that Carrack replacement, leaving
    exactly the three assault-sea UnitInfos listed above. This makes the inherited
    distinction between player-level and city-level trainability gameplay-live for an
    ordinary civ as soon as Combustion is known without Oil.

REPAIR BOUNDARY
    If the cached contract remains "can train cargo," require at least one actual owned
    city for which CvCity::canTrain(eUnit) succeeds before setting either transport
    capability bit. updateTransports already runs before cached-city reconstruction, so
    it can iterate the owner's real CvCity list directly; it does not need a UWAICache
    City object. There are only a few assault-sea candidates and the scan can stop on the
    first qualifying city/unit, so the extra legality checks are bounded.

    This is preferable to hand-checking only Oil or player-global bonus counts: city
    trainability also captures disconnected plot-group resources, coast/sea placement,
    civilization-specific replacements and other local prerequisites. It restores the
    meaning promised by canTrainAnyCargo/canTrainDeepSeaCargo instead of adding another
    partial approximation.

    Do NOT automatically fold already-owned obsolete transports into this narrow repair.
    Existing operational lift is a legitimate separate design question because some
    callers may care about what the player can use now rather than what it can produce.
    The inherited cache and SAS contact-gate variable are explicitly framed as TRAINING
    capability, and broadening them to "can train or already owns" would change that
    contract. Review that policy independently if desired rather than using it to leave
    the proven false canTrain result in place.

NEGATIVE CONTROLS / SCOPE
    * The fault is not CvCity/CvPlot resource legality; those functions correctly reject
      an Oil-less Transport. The incorrect abstraction boundary is UWAICache using only
      CvPlayer::canTrain to answer an any-city training question.
    * Existing Base AdvCiv still has a coarse player-level transport capability helper,
      including questions such as landlocked empires. F218 does not claim every such
      approximation is newly caused by SAS. Its concrete current-SAS root is the
      resource-gated-only post-Combustion transport chain and the resulting false
      training/reachability state.
    * A civ that currently has a city with Oil and can actually train Transport is not a
      reproduction; city-level and player-level capability agree there.
    * A pre-Combustion civ that can still train the resource-free Galleon is likewise
      not the clean F218 reproduction even without Oil.
    * F214/KI#537 is distinct: F214 preserves a stale MilitaryBranch unit after a valid
      capability is lost. F218 freshly recomputes TRUE every turn from an insufficient
      player-level legality test.
    * F217/KI#540 is also distinct: F217 misidentifies an XML feature by health/order;
      F218 identifies the right unit but asks the wrong layer of the training API.


C019-WIP06 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP06
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
NEXT ROOT ID: F219 UNUSED

Continue after updateTransports. Finish target-mission/threat/vassal-score input and
update-order matrices, serialization/version fallbacks and remaining City helper/cache
contracts. Keep inherited coarse heuristics separate from demonstrated current-data
correctness failures. Revisit existing-vs-trainable transport capacity only as a separate
policy question; do not weaken F218's proven canTrain mismatch on that basis.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP07 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T134157.zip
Prior physical checkpoint:
    C019-WIP06
New finding in this append:
    F219 / provisional KI#542
Next finding number:
    F220 UNUSED

F219 / provisional KI#542
-------------------------
TITLE
    UWAICache::goldPerProdVictory checks peaceful-victory stage 3 before stage 4
    even though the victory-stage hash is cumulative, making both stage-4 branches
    unreachable and feeding systematically wrong production opportunity costs into
    UWAI war evaluation.

STATUS
    CONFIRMED inherited AdvCiv/UWAI logic bug. The reversed stage test is present in
    the initial 2017 WarAndPeace cache implementation and survives current SAS.

ROOT
    Current UWAICache::goldPerProdVictory determines the cache owner's peaceful-victory
    stage as follows:

        int iOwnerVictoryStage = 0;
        if (kOwnerTeam.AI_anyMemberAtVictoryStage(
                AI_VICTORY_CULTURE3 | AI_VICTORY_SPACE3))
            iOwnerVictoryStage = 3;
        else if (kOwnerTeam.AI_anyMemberAtVictoryStage(
                AI_VICTORY_CULTURE4 | AI_VICTORY_SPACE4))
            iOwnerVictoryStage = 4;

    The rival loop repeats the same ordering:

        int iRivalVictoryStage = 0;
        if (itRival->AI_anyMemberAtVictoryStage3())
            iRivalVictoryStage = 3;
        else if (itRival->AI_anyMemberAtVictoryStage4())
            iRivalVictoryStage = 4;

    However CvPlayerAI::AI_updateVictoryStageHash constructs each victory strategy
    cumulatively. For Space, for example, a stage-4 result first executes:

        m_eVictoryStageHash |= AI_VICTORY_SPACE3;

    and only inside that stage-3 block then executes:

        m_eVictoryStageHash |= AI_VICTORY_SPACE4;

    Culture is constructed the same way. CvPlayerAI::AI_atVictoryStage simply tests
    whether any requested bit is present, and CvTeamAI::AI_anyMemberAtVictoryStage3/4
    delegate to those player tests. Therefore a Space4/Culture4 player or team is also
    necessarily Space3/Culture3 for these predicates.

    The first branch is thus TRUE whenever the second branch could be TRUE. Both
    `else if (...stage4...)` branches in goldPerProdVictory are unreachable for valid
    current stage-4 states.

GAMEPLAY CONSEQUENCE
    goldPerProdVictory is one lower-level component of m_rGoldPerProduction, exposed as
    goldValueOfProduction(). WarUtilityAspect::Effort reads that value directly when
    converting projected production/unit losses and wartime opportunity cost into UWAI
    utility. The function intentionally raises the value of production for peaceful
    victory pursuit and lowers it when rivals are as close or closer to victory:

        scaled r = iOwnerVictoryStage + 1;
        ...
        if (iRivalVictoryStage >= iOwnerVictoryStage)
            r--;
        if (iRivalVictoryStage > iOwnerVictoryStage)
            r--;

    Misclassifying stage 4 as stage 3 therefore changes real war evaluation.

    Clean examples:

    * Owner at peaceful stage 4, no stage-3/4 rival:
          intended base = 5
          current base  = 4
      Production is undervalued despite the owner being at the most urgent peaceful
      victory stage, making diversion into war cheaper than intended.

    * Owner at peaceful stage 4, rival at stage 3:
          intended: owner 4 vs rival 3 => 5 (rival is behind; no deduction)
          current:  owner 3 vs rival 3 => 4 - 1 = 3
      The combination of demoting the owner and falsely treating the rival as tied can
      lower the victory-production valuation by two full points.

    * Owner at peaceful stage 3, rival at stage 4:
          intended: 4 - 1 (rival >= owner) - 1 (rival > owner) = 2
          current:  rival stage 4 is seen as stage 3, so result = 3
      The extra urgency adjustment for a rival who is strictly closer to victory is
      never applied.

    updateGoldPerProduction takes the maximum of this term and its other production-
    valuation terms, so another term can mask F219 in a particular position; the bug
    does not guarantee a changed final ratio every turn. But whenever the victory term
    is controlling or near-controlling, the erroneous stage classification feeds
    WarUtilityAspect::Effort and can alter war-plan utility.

ANCESTRY
    Initial practical AdvCiv/WarAndPeace commit 1253 (f6ff6a182f, "Initial commit
    (cont.)") introduced WarAndPeaceCache::goldPerProdVictory with the same ordering:

        if (... AI_VICTORY_CULTURE3 ... || ... AI_VICTORY_SPACE3 ...)
            ourVictLevel = 3;
        else if (... AI_VICTORY_CULTURE4 ... || ... AI_VICTORY_SPACE4 ...)
            ourVictLevel = 4;

    and likewise checked the rival's level-3 helper before its level-4 helper. Later
    WarAndPeace/UWAI refactors preserved that logic essentially verbatim. F219 is
    therefore inherited from the original UWAI cache design rather than introduced by
    SAS.

REPAIR BOUNDARY
    Test stage 4 before stage 3 in both places:

        if (... CULTURE4 | SPACE4 ...)
            iOwnerVictoryStage = 4;
        else if (... CULTURE3 | SPACE3 ...)
            iOwnerVictoryStage = 3;

    and:

        if (itRival->AI_anyMemberAtVictoryStage4())
            iRivalVictoryStage = 4;
        else if (itRival->AI_anyMemberAtVictoryStage3())
            iRivalVictoryStage = 3;

    No change to victory-stage construction is needed; cumulative lower-stage bits are
    widely intentional. The local consumer simply has to query from highest stage to
    lowest.

NEGATIVE CONTROLS / SCOPE
    * This is not the retracted F113/KI#436 PlayerTypes/GET_TEAM false positive;
      overloaded GET_TEAM(PlayerTypes) resolves through TEAMID correctly. F219 depends
      only on the order of cumulative stage predicates.
    * Military and Diplomacy victory stages are intentionally not used for the owner's
      peaceful-production premium. The bug is specifically the unreachable stage-4
      classification within the peaceful Space/Culture logic and the generic rival
      stage comparison used by that same helper.
    * Another gold/production heuristic may dominate the final max in
      updateGoldPerProduction, so F219 is an input correctness defect rather than a
      claim that every stage-4 turn necessarily changes the final war utility.


C019-WIP07 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP07
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
NEXT ROOT ID: F220 UNUSED

Continue with the unfinished target-mission/threat/vassal-score input and cross-player
freshness matrix, serialization/version fallbacks and remaining City helper/cache
contracts. In particular, determine whether calculateThreatRating's rival-cache
reachability read is merely accepted one-turn cache staging or a demonstrable player/
team-order correctness defect before considering F220.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C019-WIP08 CONTINUATION -- THREAT CACHE FRESHNESS REJECTION + TEAM NUKE SCOPE
===============================================================================
STATUS: WIP / append-only continuation of authoritative C019-WIP07.
COMPLETED FIRST-PASS FILES: 18 / 169.
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

CROSS-PLAYER THREAT-REACHABILITY FRESHNESS -- NOT PROMOTED
----------------------------------------------------------
The unfinished WIP07 question about calculateThreatRating reading the rival player's
cached City::canReach state does not presently cross the finding bar.

Current turn orchestration intentionally refreshes each team's UWAI member caches at
that team's turn start:

    CvTeamAI::AI_doTurnPre()
      -> UWAI::Team::turnPre()
      -> each UWAI::Player::turnPre()
      -> UWAICache::update()

and CvPlayer city creation/acquisition/destruction additionally broadcasts incremental
city-cache maintenance to all major players. In a sequential-turn game, a rival cache
can therefore be one refresh behind changes made after that rival's own turn-start
cache build (for example, end-of-turn research that changes naval reachability), but
that is part of the broader per-turn cache staging model rather than a unique broken
invariant in calculateThreatRating.

There is historical evidence that the authors were conscious of genuinely unsafe
cross-cache startup state: practical commit 2751 added bPlayerHistAvailable specifically
because other civs' caches can be unready at scenario/turn-0 startup. Separate special
paths also force cache updates when a new player's first city appears so higher-ID
rivals can evaluate it immediately. Those safeguards do not establish a requirement
that all foreign derived fields be transactionally recomputed after every mid-turn
tech/resource mutation.

A stale reachability bit could certainly make long-term threat react one opposing turn
later in some player-order situations, but I have not proved a stronger contract or a
persistent/order-dependent state corruption analogous to F213-F219. Treat this as an
accepted cache-latency limitation unless a concrete gameplay trace later proves that a
consumer requires same-slice foreign reachability freshness.

F220 / provisional KI#543 -- CONFIRMED
UWAI LONG-TERM NUCLEAR DETERRENCE CHECKS ONLY THE CURRENT PLAYER EVEN THOUGH THE
THREAT AND ANY RESULTING WAR ARE TEAM/MASTER-SIDE OBJECTS
-------------------------------------------------------------------------------
TITLE
    UWAICache::teamThreat applies its nuclear deterrent only when m_eOwner personally
    owns a nuke. A teammate (or master-side partner) can possess the retaliation
    capability that necessarily participates in any team war, yet the owner's threat
    cache ignores it and overstates the rival's long-term threat.

STATUS
    CONFIRMED inherited AdvCiv/UWAI team-scope defect. The per-player nuke test is
    present from the initial 2017 WarAndPeace cache implementation and survives Base
    AdvCiv 1.14 and current SAS.

ROOT CONTRACT
    calculateThreatRating(PlayerTypes eRival) is player-indexed only for the rival
    reachability gate. Its substantive threat value is then:

        return teamThreat(TEAMID(eRival));

    teamThreat itself is overwhelmingly team/master-side:

    * rival attitude is AI_getAttitude(eOwnerTeam);
    * rival victory pressure is queried through team-wide any-member stages;
    * rival long-term power is computed for the rival team/master side;
    * owner long-term power is computed from the OWNER TEAM'S MASTER TEAM, including
      allied/vassal-side military branches and defensive-pact support as applicable;
    * alternative targets are counted as teams;
    * the modeled future conflict can only be declared against the owner team, not an
      isolated member player.

    Yet the nuclear term alone switches ownership domain:

        // Nuclear deterrent
        if (GET_PLAYER(m_eOwner).getNumNukeUnits() > 0)
            rDiploFactor -= fixp(0.2);

    Thus the model asks whether the one cache-owning PLAYER has a nuke instead of
    whether the owner war side has nuclear retaliation capability.

DETERMINISTIC TEAM REPRODUCTION
    Consider a normal two-member team A+B and rival team R:

    * A owns zero nuclear units;
    * B owns at least one nuclear unit;
    * R otherwise has a positive teamThreat against TeamAB;
    * both A and B are alive normal teammates.

    Any war R declares on A is a war against TeamAB. B joins automatically and B's
    nukes are available against the common enemy. The deterrent therefore exists for
    the whole target team.

    Nevertheless:

        A.uwai().cache.teamThreat(R)

    receives NO -0.2 nuclear deterrent because A personally has zero nukes, while:

        B.uwai().cache.teamThreat(R)

    receives the deterrent solely because B happens to own the units.

    The probability proxy for one indivisible team war therefore changes according to
    which teammate's cache happens to represent the same team-side future conflict.

LIVE CONSUMER / WHY LATER AGGREGATION DOES NOT REPAIR IT
    threatRating feeds WarUtilityAspect::PreEmptiveWar. WarEvaluator explicitly
    evaluates the aspect once for every alive member of the agent team, binding
    ourCache() to that member's UWAICache each time.

    The recent F137/KI#460 repair correctly consumes each RIVAL TEAM'S common threat
    forecast only once per agent member, taking the maximum threatRating over that
    rival team's members. It does not and cannot repair F220's OWNER-SIDE mismatch:
    the A pass and B pass still begin from different teamThreat values solely because
    one teammate owns the team's nukes.

    PreEmptiveWar then compares team-wide current/predicted city sides and converts the
    threat into actual war utility. The per-player nuke omission can therefore raise
    the pre-emptive-war pressure contributed by a teammate whose partner supplies the
    real retaliation capability.

ENGINE NEGATIVE CONTROL / INTENDED DOMAIN EVIDENCE
    Civ4/AdvCiv already exposes exactly the team-level nuclear capability primitive:

        int CvTeam::getNumNukeUnits() const

    which sums nuclear units across the team's members and its vassals. Other
    team-war/diplomacy code uses that team-level API for nuclear deterrence; e.g.
    CvTeamAI::AI_declareWarTrade checks:

        if (GET_TEAM(eTarget).getNumNukeUnits() > 0)
            return DENIAL_POWER_THEM;

    i.e. "don't want to get nuked" is correctly a property of the target team, not of
    a singled-out target player. This is a strong negative control for teamThreat's
    otherwise isolated player-level check.

ANCESTRY
    Initial practical AdvCiv/WarAndPeace commit 1253 (f6ff6a182f, "Initial commit
    (cont.)") introduced the long-term threat calculation with the same structure and
    the same line:

        // Nuclear deterrent
        if(GET_PLAYER(ownerId).getNumNukeUnits() > 0)
            diploFactor -= 0.2;

    Later UWAI refactors renamed the owner field and converted doubles to scaled, but
    retained the player-owned nuclear test verbatim in substance. Base AdvCiv 1.14 and
    current SAS remain the same here. F220 is therefore inherited from the feature's
    original ownership model rather than introduced by SAS.

REPAIR BOUNDARY
    The deterrent should use the same owner-side scope as the future war/power model.
    At minimum, ordinary teammate nuclear capability must count. Because teamThreat's
    owner power is based on the owner's master side, the most internally consistent
    repair should deliberately decide whether master/vassal nuclear capability belongs
    in the deterrent as well, rather than merely replacing GET_PLAYER(m_eOwner) with an
    arbitrary teammate.

    CvTeam::getNumNukeUnits() already aggregates team members and vassals. If the
    intended scope is the full master side used by longTermPower, prefer querying the
    owner's master team through that API. If design intent is deliberately narrower,
    at least query the owner team so normal teammate nukes cannot disappear.

    Do NOT alter the rival-player reachability gate as part of F220; that is the
    separate cache-staging/modeling question rejected above.

RELATION TO EXISTING ROOTS
    * F137/KI#460 was a rival-side multiplicity defect: the same rival-team threat was
      exposed through several rival PlayerTypes and consumed repeatedly. Its repair
      deduplicates the rival team per agent member. F220 instead says the supposedly
      team-owned threat value itself changes with WHICH OWNER MEMBER'S cache computes
      it. The fixes are independent.
    * F134/KI#457 found another team-war capability modeled with current-player army
      power inside IllWill. That supports the ownership principle but is a different
      consumer and quantity; fixing revenge capability there does not change
      UWAICache::teamThreat's nuclear deterrent.
    * The cross-player reachability freshness question above remains unpromoted; F220
      does not depend on cache age. A fully current cache still produces the wrong
      deterrent for teammate-owned nukes.


C019-WIP08 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP08
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
NEXT ROOT ID: F221 UNUSED

Continue the remaining threat/vassal-score inputs, serialization/version fallbacks,
City helper/cache contracts and historical bugfix hunks. Cross-player foreign-cache
freshness has been challenged and rejected absent a stronger same-slice contract; do
not re-promote it merely because ordinary per-turn derived state can lag.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C019-WIP09 CONTINUATION -- TARGET-MISSION CARGO MULTIPLICITY + SIBLING CENSUS
===============================================================================
STATUS: WIP / append-only continuation of authoritative C019-WIP08.
COMPLETED FIRST-PASS FILES: 18 / 169.
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

F221 / provisional KI#544 -- CONFIRMED
TARGET-MISSION CENSUSES COUNT BOARDED LAND CARGO TWICE BECAUSE TRANSPORT CARGO
AND THE PERSISTENT BOARDED CARGO SELECTION GROUPS ARE BOTH ENUMERATED
-------------------------------------------------------------------------------
TITLE
    UWAICache::updateTargetMissionCount adds both a group's own units and its cargo,
    but K-Mod deliberately keeps loaded land units in their own selection groups.
    Those boarded groups remain enumerable and follow the transport plot. In enemy
    territory the same physical cargo can therefore contribute once through the
    transport's getCargo() count and again through its own boarded group.

STATUS
    CONFIRMED inherited BBAI/K-Mod representation mismatch, copied into AdvCiv UWAI.
    The current SAS cache reproduces the same multiplicity. The ancestral
    CvPlayerAI::AI_enemyTargetMissions census has the same root and must be reviewed
    together with the cache repair.

CURRENT CACHE CENSUS
    UWAICache::updateTargetMissionCount(PlayerTypes ePlayer) iterates every owner
    selection group and, when the group's MissionAI plot or fallback current plot is
    owned by ePlayer, counts:

        iMissions += pGroup->getNumUnits();
        iMissions += pGroup->getCargo();

    The helper explicitly says it is based on CvPlayerAI::AI_enemyTargetMissions.

BOARDED CARGO REMAINS A SEPARATE ENUMERATED GROUP
    FOR_EACH_GROUPAI is a raw traversal of CvPlayerAI::AI_firstSelectionGroup /
    AI_nextSelectionGroup. It has no activity-type filter. ACTIVITY_BOARDED groups
    therefore remain in this census.

    K-Mod loading clears the cargo group's executable mission queue but deliberately
    leaves the unit/group object alive and sets:

        getGroup()->setActivityType(ACTIVITY_BOARDED);

    The cargo is not merged into the transport selection group.

BOARDED GROUP FOLLOWS THE TRANSPORT PLOT
    CvUnit::setXY contains K-Mod cargo-preservation logic. When a transport moves, it
    finds units whose getTransportUnit() is that transport and recursively calls:

        pLoopUnit->setXY(iX, iY, bGroup, false);

    while retaining their cargo group IDs. Thus a boarded land group's plot() really
    follows the transport's current tile.

MISSIONAI IS NOT REQUIRED FOR THE DUPLICATE
    updateTargetMissionCount deliberately falls back to the group's current plot:

        pMissionPlot = pGroup->AI_getMissionAIPlot();
        if (pMissionPlot == NULL)
            pMissionPlot = pGroup->plot();

    Consequently the duplicate does not depend on stale MissionAI state surviving
    loading. If an invasion fleet ends its turn loaded on an enemy-owned coastal tile,
    both the transport group and the boarded cargo groups can satisfy the same owner
    test through their current plot alone.

CACHE-UPDATE TIMING MAKES THE STATE OBSERVABLE
    UWAI refresh runs from CvTeamAI::AI_doTurnPre -> UWAI::Team::turnPre ->
    UWAI::Player::turnPre -> UWAICache::update, before that turn's unit movement.
    Therefore a fleet that ended the preceding turn loaded in enemy coastal water is
    sampled exactly while:

      * the transport group still reports its loaded units through getCargo();
      * each boarded land unit still belongs to its own enumerable selection group;
      * those boarded groups are at the transport's enemy-owned plot.

DETERMINISTIC MINIMAL REPRODUCTION
    Consider one assault transport carrying two land units, all ending the previous
    turn on a tile owned by enemy player E.

    The transport selection group contributes:

        getNumUnits() = 1
        getCargo()    = 2
        ----------------
                        3

    The two physical land units still exist in boarded cargo group(s). Through the
    current-plot fallback those group(s) contribute the same two land units again.

        nonduplicated physical census = 3
        current cached census         = 5

    The exact group partition of the two cargo units does not matter: whether they are
    together or in separate boarded groups, their aggregate second contribution is 2.

LIVE BEHAVIORAL THRESHOLD
    InvasionGraph::Node::findTarget consumes UWAICache::targetMissionCount and begins
    with:

        int iMaxMissions = 3;

    A mission-derived target is considered only when:

        iMissions > iMaxMissions

    Therefore the ordinary one-transport/two-cargo example crosses a real branch:

        intended/nonduplicated 3 -> no mission-determined target
        current duplicated      5 -> target may be determined from unit missions

    This can change which current war opponent UWAI assumes the army is committed to,
    and therefore changes the invasion simulation rather than merely a displayed or
    unused statistic.

ANCESTRAL SIBLING -- CvPlayerAI::AI_enemyTargetMissions
    The BBAI/K-Mod function that inspired the cache contains the same accounting:

        iCount += pLoopSelectionGroup->getNumUnits();
        iCount += pLoopSelectionGroup->getCargo();

    and traverses the same persistent selection groups with the same MissionAI/current
    plot fallback. K-Mod 1.46 retains this BBAI-era census while also retaining the
    later K-Mod cargo-group representation in which loaded units remain in their own
    groups. AdvCiv/SAS inherit both sides.

    Current AdvCiv/SAS live callers use AI_enemyTargetMissions for team-level war
    reasoning, including estimates of attackers/missions in enemy territory and the
    related peace/capitulation logic. Therefore repairing only UWAICache would leave
    the ancestral live census with the same multiplicity defect.

ROOT CONTRACT
    A target-mission census is an estimate of committed physical units. One physical
    unit must contribute at most once to a given target census. Current code violates
    that invariant specifically for transported land cargo by exposing one physical
    unit through two simultaneous representations:

      1. transport-side cargo count;
      2. boarded cargo selection-group unit count.

REPAIR BOUNDARY
    Do NOT simply delete getCargo() from the transport count. Cargo-side counting is
    useful when boarded land groups have no meaningful MissionAI target; the transport
    is often the only object carrying the invasion destination context.

    Instead choose exactly one representation for loaded units. A conservative family
    of repairs is:

      * count transport cargo through the transport group;
      * skip units/groups whose members are currently cargo when evaluating their own
        boarded group contribution, or otherwise subtract cargo already represented by
        its transport;
      * preserve ordinary non-cargo group counting and the MissionAI/current-plot
        fallback for untransported units.

    Apply the same physical-unit uniqueness rule to both
    UWAICache::updateTargetMissionCount and CvPlayerAI::AI_enemyTargetMissions so their
    intentionally overlapping semantics do not diverge.

    Be careful with mixed cargo groups: K-Mod's movement code can separate groups whose
    members are not all transported by the same transport. Prefer counting at a level
    that can distinguish actually loaded units rather than blindly dropping an entire
    group solely because its activity is ACTIVITY_BOARDED.

RELATION TO OTHER ROOTS
    * F211/KI#534 concerned empty transient groups reaching AI_isDeclareWar's debug
      precondition. updateTargetMissionCount explicitly skips zero-unit groups, so F221
      is independent and occurs with perfectly valid non-empty boarded groups.
    * The ordinary foreign-cache freshness question rejected before F220 is unrelated;
      F221 is reproducible even with a perfectly current cache.
    * This finding does not require stale MissionAI state. The current-plot fallback is
      sufficient, although retained MissionAI state can provide an additional route.

FOLLOW-UP REJECTION -- reportWarEnding POINTER EXCLUSIVITY
----------------------------------------------------------
The apparent if (pWeReceive) / else if (pWeGive) asymmetry in
UWAICache::reportWarEnding is not a defect. CvTeamAI::AI_preMakePeace deliberately
calls one side as (pReparations, NULL) and the opposing side as (NULL, pReparations).
The two lists are an intentionally mutually exclusive API representation of which side
received the peace concessions. Do not spend F222 on this local shape.

HISTORICAL CLOSURE PROGRESS
---------------------------
A mechanical history pass found 142 tracked AdvCiv/SAS commits whose diff actually
contains UWAICache.cpp or its former WarAndPeaceCache.cpp name. Reopened bugfix-shaped
hunks checked so far remain present in current code rather than exposing another root:

    1909  old UWAI city-cache out-of-bounds/hole handling
    2644  teammate-owned cities valid in the cache
    2706  human-player vs human-team update distinction
    2921  revised-UWAI mix-up corrections
    2929/2931 deployment-distance / transport-destination corrections
    3044  new-player history and vassal-score consistency fixes

The older city-sorting, cache-initialization and distance-0 team-city repairs checked
in the same pass likewise survive in current source. Continue the finite historical
matrix rather than assuming every old repair is clean, but do not reopen these without
new contradictory evidence.

C019-WIP09 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP09
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544 (with CvPlayerAI::AI_enemyTargetMissions sibling)
NEXT ROOT ID: F222 UNUSED

Continue remaining reportSponsoredWar/bounty semantics, City::updateDistance adversarial
cases, goldPerProd* arithmetic beyond F219, remaining historical cache bugfix commits,
and the final member/producer/consumer matrix. One not-yet-proved sponsorship seam is
whether a deal containing multiple TRADE_WAR items causes reportSponsoredWar to assign
the entire compensation list independently to each target; first prove that such a
multi-war deal is legal/reachable and that the bounty state has a live consumer before
considering F222.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C019-WIP10 CONTINUATION -- GOLD-PER-PRODUCTION SITE-WEIGHT CONVERSION REGRESSION
===============================================================================
STATUS: WIP / append-only continuation of authoritative C019-WIP09.
COMPLETED FIRST-PASS FILES: 18 / 169.
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp OPEN.
NO REPOSITORY SOURCE FILES MODIFIED.

SPONSORED-WAR MULTI-TARGET SEAM -- NOT PROMOTED
-----------------------------------------------
The WIP09 cursor asked whether one compensation list could be copied into several
sponsored-war targets when a single deal contains multiple TRADE_WAR items.

The generic deal representation can contain multiple trade items and CvDeal would call
reportSponsoredWar once per TRADE_WAR item. However, UWAI's own deal valuation has an
explicit supported-policy guard:

    // advc.104o: Can't be hired for more than one war at a time
    int iWars = 0;
    ...
    case TRADE_WAR:
        if (getUWAI().isEnabled())
        {
            iWars++;
            if (iWars > 1)
                iValue += 1000000; // More than they can pay
        }

Thus the ordinary UWAI trade-acceptance layer intentionally makes the exact multi-war
sponsorship deal effectively unaffordable. Do not spend F222 merely because CvDeal's
reporting loop is structurally capable of receiving such a list through forced or
external deal construction.

A separate possible approximation remains: a single sponsored-war deal can in
principle bundle unrelated reciprocal trade items, while reportSponsoredWar values the
entire received list rather than computing the war item's marginal payment. HiredHand
would then remember some value that economically belongs to other items. The original
implementation already described the whole received list as the sponsorship and even
accepted overpayment as increasing later obligation. Without a sharper supported
reproduction/contract showing that bundled-item marginal attribution is required, keep
this as a modeling limitation rather than F222.

F222 / provisional KI#545 -- CONFIRMED
UWAI REVISION MIS-PARENTHESIZED THE SETTLER ADJUSTMENT IN goldPerProdSites,
DIVIDING THE FIXED SITE WEIGHT BY 10 AND SYSTEMATICALLY UNDERVALUING EXPANSION
-------------------------------------------------------------------------------
TITLE
    AdvCiv practical commit 2920's scaled-number rewrite changed the algebra of
    UWAICache::goldPerProdSites. The old expression divided only the settler count by
    10; the rewritten expression divides the entire site-weight-plus-settler term by
    10. Good expansion sites are therefore valued far below the pre-rewrite model.

STATUS
    CONFIRMED inherited AdvCiv regression introduced by practical commit 2920 /
    4ae382e5db ("Revised all code in the UWAI component"). Base AdvCiv 1.14 and
    current SAS retain the changed expression.

PRE-2920 CONTRACT / INTENT CONTROL
    Immediately before the large UWAI rewrite, goldPerProdSites used ordinary double
    arithmetic:

        double weights[sitesMax] = { 1, 0.8, 0.6, 0.4, 0.2 };
        ...
        sites += foundVals[i] * (weights[i] + settlers / 10.0) / 2500;

    The comment explains the small settler-dependent addition:

        Once the settlers have been used, the found value of more remote sites
        may increase a bit

    Algebraically the intended weight is therefore:

        siteWeight + settlers / 10

    The 1.0/0.8/... fixed site ranking is not itself supposed to be divided by 10.

2920 REGRESSION
    The scaled rewrite converted the same block to:

        scaled arSiteWeights[] = { 1, fixp(0.8), fixp(0.6), fixp(0.4), fixp(0.2) };
        ...
        rSiteScore +=
            (aiFoundVals[i] * (arSiteWeights[i] + iSettlers) / 10) / 2500;

    ScaledNum's integer overloads have normal numeric semantics:

        r + int  -> add ScaledNum(int)
        r / int  -> divide the whole scaled value by int

    so the rewritten coefficient is:

        (siteWeight + settlers) / 10

    rather than:

        siteWeight + settlers / 10

    This is not a representation trick; the formulas are mathematically different.

DETERMINISTIC NUMERIC PROOF
    With zero settlers, the best site's fixed weight is 1.0.

    Old coefficient:

        1.0 + 0 / 10 = 1.0

    Current coefficient:

        (1.0 + 0) / 10 = 0.1

    For a reference found value of 2500 (the function's own normalizing divisor):

        old site-score contribution     = 2500 * 1.0 / 2500 = 1.0
        current site-score contribution = 2500 * 0.1 / 2500 = 0.1

    The simplest expansion site is therefore undervalued by exactly a factor of 10.

    The error remains for nonzero settlers. Because the loop starts at iSettlers, the
    corresponding coefficients are for example:

        settlers 1, weight 0.8:
            old     = 0.8 + 0.1 = 0.9
            current = (0.8 + 1) / 10 = 0.18

        settlers 2, weight 0.6:
            old     = 0.6 + 0.2 = 0.8
            current = (0.6 + 2) / 10 = 0.26

    Thus the conversion does not merely shift a corner case; it systematically shrinks
    the city-site opportunity term throughout the normal settler range.

DOWNSTREAM EFFECT
    goldPerProdSites returns:

        min(4.5, 4.5 * rSiteScore / iCities)

    and updateGoldPerProduction begins with:

        m_rGoldPerProduction = max(goldPerProdBuildings(), goldPerProdSites());

    before applying amortization and the process/floor/victory lower bounds.

    For one city, one 2500-value unclaimed site and zero settlers, the site component
    was designed to reach the 4.5 cap:

        old:     rSiteScore = 1.0 -> 4.5 gold per production
        current: rSiteScore = 0.1 -> 0.45 gold per production

    The later global floor of 1 can mask part of the numerical collapse when no other
    peaceful use dominates, but it cannot restore the intended expansion opportunity
    signal. In ordinary early states where building demand is between those values,
    current code chooses the building term whereas the original model would choose the
    stronger expansion-site term.

LIVE WAR-UTILITY CONSUMER
    WarUtilityAspect::Effort reads:

        scaled rGoldPerProduction = ourCache().goldValueOfProduction();

    and multiplies wartime build-up/lost production by that value before converting it
    to utility. Therefore F222 systematically makes war production look cheaper in
    game states where good expansion sites should be the dominant peaceful alternative.
    It is not dead cache arithmetic or a display-only discrepancy.

ANCESTRY
    Practical commit 1253 introduced the original site-opportunity model. Practical
    commit 1358 refined it to weighted found values and introduced the exact expression:

        foundVals[i] * (weights[i] + settlers / 10.0) / 2500

    That double-arithmetic form survives later pre-revision commits.

    Practical commit 2920 / 4ae382e5db ("Revised all code in the UWAI component")
    changes the old line directly to:

        (aiFoundVals[i] * (arSiteWeights[i] + iSettlers) / 10) / 2500

    The same rewritten expression is reproduced in later history snapshots and remains
    in Base AdvCiv 1.14 and current SAS. No subsequent fix restores the original
    parentheses/algebra.

REPAIR BOUNDARY
    Preserve the pre-2920 numeric formula while using scaled arithmetic. For example:

        rSiteScore += aiFoundVals[i] *
                (arSiteWeights[i] + scaled(iSettlers, 10)) / 2500;

    or an exactly equivalent scaled expression such as:

        rSiteScore += aiFoundVals[i] *
                (10 * arSiteWeights[i] + iSettlers) / 10 / 2500;

    Prefer the first form because it mirrors the documented original intent and makes
    the "settlers / 10" adjustment visually explicit.

    Do not change the five fixed site weights, the settler-based starting index, the
    Barbarian-city contribution, city-count cap or rGoldPerProductionCap as part of
    F222; none of those is needed to repair the proven conversion regression.

RELATION TO F219
    F219/KI#542 found a separate ordering bug in goldPerProdVictory where cumulative
    stage-3 bits shadow stage 4. F222 is another updateGoldPerProduction input but is
    algebraically independent: fixing victory-stage detection does not alter site
    opportunity valuation, and restoring the site formula does not alter victory-stage
    logic.

C019-WIP10 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP10
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544 (with CvPlayerAI::AI_enemyTargetMissions sibling)
    F222 / provisional KI#545
NEXT ROOT ID: F223 UNUSED

Continue from F223 unused. Prioritize the remaining goldPerProd* arithmetic and
City::updateDistance contracts while the historical before/after context is hot, then
finish the remaining UWAICache member/producer/consumer and historical-fix matrix.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

C019-WIP11 APPEND -- QUEUE 031 CONTINUATION FROM F223 UNUSED
=======================================================================

F223 / provisional KI#546 -- CONFIRMED
UWAI SCALED-NUMBER REWRITE MIS-PARENTHESIZED THE HIREABILITY PERSISTENCE
PROBABILITY, COLLAPSING THE ANTI-FLICKER HYSTERESIS BY ROUGHLY TWO ORDERS
OF MAGNITUDE
-------------------------------------------------------------------------------
TITLE
    AdvCiv practical commit 2920 converted UWAICache::updateCanBeHiredAgainst
    from double to scaled arithmetic but changed:

        1 - (threshold - utility) / 100.0

    into:

        scaled(1 - (threshold - utility), 100)

    The latter means (1 - delta) / 100, not 1 - delta / 100. The branch is
    specifically the persistence branch for a target that is ALREADY marked
    hireable, so the conversion defeats the hysteresis introduced to reduce
    flickering of human war-trade availability.

STATUS
    CONFIRMED inherited AdvCiv regression introduced by practical commit 2920 /
    4ae382e5db ("Revised all code in the UWAI component"). Current Base AdvCiv
    1.14 and current SAS retain the erroneous expression. No later repair was
    found; practical commit 3209 only replaced the direct ScaledNum random call
    with SyncRandSuccess while preserving the same probability.

ORIGINAL CONTRACT / INTENT CONTROL
    Practical commit 1343 / f79b7f7d5f is titled:

        Tried to reduce the flickering of the war trades alert

    and its commit message says UWAI should be more reluctant to change its
    willingness to be hired for war at a human player's request in large games.

    That commit introduced the hireAgainst cache and the exact persistence
    formula:

        double pr = -1;
        if (!hireAgainst[tId])
            pr = std::min(0.58, (u - 1.5 * thresh) / 100.0);
        else
            pr = 1 - (thresh - u) / 100.0;
        hireAgainst[tId] = ::bernoulliSuccess(pr, ...);

    Thus the two branches intentionally have different roles:

      * FALSE -> TRUE is conservative/probabilistic and capped at 58%;
      * TRUE -> TRUE has hysteresis: at threshold it persists with probability
        1.0 and only fades gradually as utility falls below the threshold.

2920 REGRESSION
    Practical commit 2920 rewrote the latter as:

        scaled rProb = -1;
        if (!m_abCanBeHiredAgainst.get(eTeam))
            rProb = std::min(fixp(0.58),
                    (iWarUtility - fixp(1.5) * iUtilityThresh) / 100);
        else
            rProb = scaled(1 - (iUtilityThresh - iWarUtility), 100);

    The first branch preserves the old arithmetic. The second does not.

    Let:

        delta = iUtilityThresh - iWarUtility

    Intended pre-2920 probability:

        1 - delta / 100 = (100 - delta) / 100

    Current probability:

        (1 - delta) / 100

    The fixed 1.0 baseline was accidentally turned into 0.01.

DETERMINISTIC NUMERIC PROOF WITH THE CURRENT UWAI THRESHOLD
    Current UWAIAgent.cpp defines:

        int const iWarTradeUtilityThresh = -37;

    If a target is already marked hireable and its current war utility is
    exactly the threshold (-37):

        delta = -37 - (-37) = 0

        intended: 1 - 0 / 100 = 1.00 = 100% persistence
        current:  scaled(1 - 0, 100) = 0.01 = 1% persistence

    If utility is ten points below threshold (-47):

        delta = 10

        intended: 1 - 10 / 100 = 0.90 = 90%
        current:  (1 - 10) / 100 = -0.09

    ScaledNum::randSuccess returns false immediately for probabilities <= 0, so
    the current branch clears hireability with certainty rather than preserving
    it 90% of the time.

    Even ten points ABOVE threshold (-27):

        delta = -10

        intended: 1 - (-10) / 100 = 1.10 -> randSuccess always true
        current:  (1 - (-10)) / 100 = 0.11 = 11%

    Thus even a target comfortably above the nominal threshold usually loses an
    already-established hireability flag under current code.

LIVE UPDATE CADENCE
    For ordinary non-human, non-vassal teams, UWAI::Team::doWar calls scheme()
    whenever reviewWarPlans permits scheming. scheme() evaluates each eligible
    target and then executes:

        bool const bCanHireOld = kCache.canBeHiredAgainst(eTarget);
        kCache.updateCanBeHiredAgainst(eTarget, iU, iWarTradeUtilityThresh);
        bool const bCanHireNew = kCache.canBeHiredAgainst(eTarget);

    Therefore this is normal recurring war-planning state, not a dead helper or
    one-time initialization path.

LIVE DIPLOMACY CONSEQUENCE
    UWAI::Team::declareWarTrade uses canBeHiredAgainst as an actual evaluation
    gate for a human sponsor who has met at least eight civilizations:

        if (!kSponsor.isHuman() || kSponsor.getHasMetCivCount() < 8 ||
            leaderCache().canBeHiredAgainst(eTarget))
        {
            ... WarEvaluator ...
            if (iU > iUtilityThresh)
                return NO_DENIAL;
        }

    If the cache flag is false in that large-game human-sponsor case, the fresh
    WarEvaluator block is skipped and NO_DENIAL cannot be returned through it;
    the function falls through to refusal reasons such as DENIAL_TOO_MANY_WARS.

    F223 therefore causes exactly the user-visible availability instability that
    practical commit 1343 was designed to dampen. This is not merely an internal
    probability discrepancy.

RANDOM-HELPER CONTRACT
    Current SyncRandSuccess(rProb) expands to ScaledNum::randSuccess on the sync
    RNG. ScaledNum::randSuccess explicitly implements:

        if (m_i <= 0)
            return false;
        if (m_i >= SCALE)
            return true;

    so the numeric examples above have deterministic endpoint behavior. There is
    no hidden clamping layer that restores the intended 0.90/1.00 probabilities.

ANCESTRY
    * Practical commit 1343 / f79b7f7d5f introduces the hireability cache and
      the correct double expression `1 - (thresh - u) / 100.0`, explicitly to
      reduce war-trade alert flickering.
    * The same expression survives subsequent pre-revision refactors, including
      the AgentIterator-era code.
    * Practical commit 2920 / 4ae382e5db changes that exact line to
      `scaled(1 - (iUtilityThresh - iWarUtility), 100)` during the comprehensive
      UWAI double-to-ScaledNum rewrite.
    * Later history snapshots retain the expression. Practical commit 3209 only
      changes the random invocation from `rProb.bernoulliSuccess(...)` to
      `SyncRandSuccess(rProb)`; it does not change the arithmetic.
    * Base AdvCiv 1.14 and current SAS still contain the 2920 expression.

REPAIR BOUNDARY
    Restore the pre-2920 algebra while keeping scaled arithmetic. The direct form
    is:

        rProb = 1 - scaled(iUtilityThresh - iWarUtility, 100);

    or any exactly equivalent expression such as:

        rProb = scaled(100 - (iUtilityThresh - iWarUtility), 100);

    Prefer the first because it mirrors the original formula and makes the
    hysteresis baseline explicit.

    Do NOT change the FALSE -> TRUE branch, its 58% cap, the -37 trade utility
    threshold, the large-game sponsor gate, or scheme cadence as part of F223;
    none of those is needed to repair the proven conversion error.

RELATION TO F215 AND F222
    F215/KI#538 concerns incorrect initialization/migration of CanBeHired state
    when the team leader changes. F223 is independent: even with perfectly reset
    state, normal per-turn persistence arithmetic is wrong.

    F222/KI#545 is a different arithmetic regression from the same practical
    commit 2920, in goldPerProdSites. Their shared commit ancestry justifies a
    broader audit of 2920 numeric conversions, but the roots and consumers are
    distinct and should remain separate findings.

C019-WIP11 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP11
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544 (with CvPlayerAI::AI_enemyTargetMissions sibling)
    F222 / provisional KI#545
    F223 / provisional KI#546
NEXT ROOT ID: F224 UNUSED

Continue from F224 unused. Keep the practical-2920 before/after arithmetic scan hot:
mechanically challenge every remaining UWAICache numeric conversion for altered
parentheses, percentage baselines, integer/scaled division or rounding. Then return to
remaining City::updateDistance, member/producer/consumer and historical-fix closure.
Do not manufacture F224 if the remaining conversions are algebraically equivalent.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C019-WIP11 IMPORT

- C018-WIP05 through WIP20 and C019-WIP01 through WIP11 have been imported without rewriting their append-only evidence. C019 closes queue 041 `CvSelectionGroupAI.cpp`, bringing the first-pass audit to 18 complete files, and queue 031 `UWAICache.cpp` is now open at WIP11 with F213-F223 / provisional KI#536-KI#546 and F224 unused. The live summary and queue statuses above now reflect that cursor.
- C019's closure review explicitly confirms that the current staged source implements F198 / KI#521, F199 / KI#522 and F201 / KI#524. The later findings do not invalidate those repairs: F205 / provisional KI#528 is a distinct release-mode human Bombard ordering defect, while F202-F203 identify additional no-progress signature gaps not covered by cargo and group-size snapshots.
- F200 / provisional KI#523 and F202-F212 / provisional KI#525-KI#535 remain pending independent review and implementation. F213-F223 / provisional KI#536-KI#546 from the open UWAICache audit likewise remain provisional.
- A separate deterministic Debug-opt replay promoted KI#524.2-KI#524.6 after this checkpoint's queue-041 work. Its successive matching PDBs exposed four unsafe consumers before a Python exception identified the common producer: inherited `CvRandomEventInterface.applyPartisans1` passed `NO_UNIT` to `CvPlayer::initUnit`, which allocated a reset/unplaced unit-container entry before failing. Current source and Known Issues document the independently reviewed repairs and successful replay; these decimal KI entries are not new C019 F-roots.
- Current source and Known Issues remain authoritative for reviewed implementation state; the imported checkpoints preserve ChatGPT-5.6-Sol's newer investigation and negative controls for the next batches. (GPT-5.6-Sol)


===============================================================================
CHECKPOINT C019-WIP12 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical/reconciled checkpoint:
    C019-WIP11 plus the local reconciliation already appended by the implementation side
New finding in this append:
    F224 / provisional KI#547
Next finding number:
    F225 UNUSED

SOURCE RECONCILIATION
---------------------
The 20:51 source snapshot's CvGameCoreDLL/UWAICache.cpp is byte-for-byte unchanged
from the earlier 13:41 snapshot used for C019-WIP01..WIP11. The later deterministic
Debug-opt crash-chain repairs recorded as KI#524.2-KI#524.6 therefore do not silently
alter any F213-F223 UWAICache root. Current source remains authoritative for reviewed
implementation state; this checkpoint continues the investigation from F224 unused.

F224 / provisional KI#547
-------------------------
TITLE
    Mid-turn city conquest keeps the detailed UWAICache city set current but leaves
    m_rTotalAssets at its pre-conquest value, so live human-turn war/peace evaluation
    combines new city ownership with an old asset denominator.

STATUS
    CONFIRMED inherited AdvCiv/UWAI cache-coherency defect. Current in Base AdvCiv
    1.14 and current AdvCiv-SAS. The mid-turn ownership callback was introduced in
    practical AdvCiv commit 1289 (ed1826faf1); practical commit 2605 (f788c46a21,
    "Various optimizations for UWAICache") refactored it into the modern destroy/create
    callbacks. Neither form refreshes the cache owner's total-asset aggregate.

CURRENT ROOT
    UWAICache::update() performs the normal complete city rebuild and then computes:

        updateCities(...);
        sortCitiesByAttackPriority();
        updateTotalAssetScore();

    updateTotalAssetScore() derives m_rTotalAssets from the current cached cities owned
    by m_eOwner (plus the Palace/national-wonder adjustment).

    City ownership changes, however, are deliberately propagated during a turn. The
    modern callbacks are:

        UWAICache::reportCityDestroyed(CvCity const& kCity) { remove(kCity); }

    and:

        void UWAICache::reportCityCreated(CvCity& kCity)
        {
            ...
            if (kCity.getTeam() == TEAMID(m_eOwner) ||
                GET_TEAM(m_eOwner).AI_deduceCitySite(kCity))
            {
                add(kCity);
                sortCitiesByAttackPriority();
            }
        }

    remove/add keep m_cityList, m_cityMap and m_aiReachableCities coherent; creation
    also restores the attack-priority ordering. But neither callback updates
    m_rTotalAssets.

    A mechanical current-source caller check finds updateTotalAssetScore() called only
    by the ordinary per-turn UWAICache::update(). There is no second city-acquisition
    hook that restores the aggregate later in the same human turn.

SUPPORTED MID-TURN LIFECYCLE
    CvCity::kill notifies every major AI cache through AI_cityKilled while the old
    CvCity object is still alive. CvPlayer::acquireCity later constructs the replacement
    city and notifies every major cache through AI_cityCreated. This deliberately gives
    UWAICache a current ownership/city-set view without waiting for another team turn.

    The source comment states the reason explicitly:

        "It's important that the cache keep up with ownership changes
         so that the AI is aware of conquests made throughout a human turn."

    Full UWAI cache refresh, by contrast, occurs from UWAI::Team::turnPre /
    UWAI::Player::turnPre at the relevant team's next turn. Thus a human can capture an
    AI city and negotiate/evaluate peace before the victim's next cache refresh.

MIXED-SNAPSHOT FAILURE
    After such a conquest:

      * the lost city has been removed from the victim's UWAICache city set;
      * the newly acquired city has been inserted into the conqueror's city set;
      * target ownership, reachability and attack ordering reflect the new position;
      * the victim's m_rTotalAssets still includes its pre-conquest asset baseline;
      * the conqueror's m_rTotalAssets still excludes the newly acquired city.

    This is stronger than ordinary one-turn heuristic staleness: the same cache
    intentionally updates one representation of city ownership immediately while
    retaining a derived aggregate computed from the old representation.

LIVE WAR-EVALUATION CONSUMERS
    totalAssetScore() is read directly by several current WarUtilityAspect paths,
    including Risk, GreedForAssets, Loathing/Rebuke-related ratios, nuclear-loss
    valuation, vassal-loss normalization and blockade/loss normalization.

    Human-AI peace valuation calls UWAI::Team::reluctanceToPeace / endWarVal, which
    constructs a fresh WarEvaluator. Therefore an immediate post-conquest diplomacy
    query can consume the already-updated city list together with stale total assets;
    no later per-turn cache update is required to reach the consumer.

NUMERIC CONSEQUENCE
    Consider a victim whose pre-conquest cached total assets are 100 and whose captured
    city contributes 30 to that position. After the city callbacks, the current cached
    city set represents roughly 70 assets, but totalAssetScore() remains 100.

    If a continuation scenario in WarUtilityAspect::Risk predicts another 20 asset
    points lost, its principal normalization is approximately:

        stale current:   400 * 20 / 100 = 80
        coherent state:  400 * 20 /  70 = 114.3

    The exact final war utility also includes blockade, nuclear and other terms, but the
    denominator discrepancy itself is deterministic. Losing a valuable city can
    therefore leave the AI materially under-normalizing subsequent danger until its
    next turn. The reverse stale direction occurs for the conqueror, whose aggregate
    does not yet include its newly acquired city.

ANCESTRY / INTENT
    Practical commit 1289 (ed1826faf1) introduced reportCityOwnerChanged during the
    early WarAndPeaceCache implementation. Its source comment already says:

        "I didn't think I'd need to update the city cache during turns, so this
         is awkward to write ..."

    That callback immediately replaced the cached city owner representation and
    reachable-city counts, but did not update totalAssets.

    Practical commit 2605 (f788c46a21, "Various optimizations for UWAICache") replaced
    the in-place owner-change routine with the modern remove/add lifecycle and
    strengthened the comment to the current human-turn-conquest wording. The same
    commit retained updateTotalAssetScore as an ordinary update-only operation.

    The omission therefore predates SAS and survives both the original callback and its
    later pointer/lifecycle cleanup.

REPAIR BOUNDARY
    Keep m_rTotalAssets coherent only when the cache owner's own city set changes.

    A narrow repair can recompute updateTotalAssetScore after the old-owner cache has
    removed one of m_eOwner's cities, and after the new-owner cache has added/sorted a
    city now owned by m_eOwner. The callbacks already run for every major cache, so do
    not recompute the aggregate unnecessarily for observers whose own city holdings did
    not change.

    For reportCityCreated, perform the refresh after add() and
    sortCitiesByAttackPriority(), because updateTotalAssetScore relies on own-team cities
    sorting to the end. For destruction, removal preserves the existing order of the
    remaining list, so recomputation after remove is sufficient.

    Do not turn F224 into a full mid-turn UWAICache::update. Other per-turn heuristic
    fields intentionally have their normal refresh cadence; the proven root is the
    derived city-asset aggregate becoming inconsistent with the city set that these
    special conquest callbacks explicitly maintain.

NEGATIVE CONTROLS / SCOPE
    * The detailed City object added for the conqueror computes a fresh per-city asset
      score. The defect is not failure to construct the new City; it is failure to fold
      the changed own-city set back into m_rTotalAssets.
    * Reachable-city counts are independently adjusted by add/remove and are not part of
      F224.
    * The cache-owner extinction guard in reportCityCreated is unrelated; a player with
      no cities has no useful continuing war evaluation to repair there.
    * One-turn drift in values such as goldPerProduction is not automatically promoted
      under F224. The stronger contract here comes from m_rTotalAssets being directly
      derived from the exact city list that the conquest callbacks intentionally keep
      current during a human turn.

DEFERRED OLD-SAVE COMPATIBILITY NOTE -- NOT F225
-------------------------------------------------
The City distance serialization history contains a real but very old compatibility
quirk, retained as a closure note rather than spending F225 at this point.

Practical commit 1287 (ad5faf8aa2) introduced the first City save-version encoding.
The pre-version format had written raw `distance`. The new decoder always performs:

    iSaveVersion = iTmp / 10000;
    m_iDistance = (iTmp % 10000) - 1;

For an old version-0 raw distance d, this restores d-1. In particular, the old
unreachable sentinel -1 becomes -2.

Practical commit 1326 (5e6aa1c42d) explicitly acknowledged this supported state by
changing the writer to:

    FAssertMsg(distance >= -1 && distance < 10000,
        "-2 is OK if loaded from an old savegame version");
    distance = ::range(distance, -1, 9999);

The release clamp lets -2 be re-saved as the canonical -1 value, but the assertion
condition itself still rejects the value its message calls OK. Current read only
rebuilds CvCity pointers after all players load; normal cache recomputation happens
later at team turnPre, so the compatibility value can persist for a while after load.

This is a genuine old-format off-by-one/debug-assert compatibility issue, but it affects
only saves from the initial unversioned UWAI format and is far less relevant to current
SAS gameplay than F224. Keep it available for later cleanup/closure review rather than
promoting it as F225 without a reason to prioritize ancient-save compatibility.

C019-WIP12 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP12
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537 (cross-file MilitaryBranch refresh root)
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544 (with CvPlayerAI::AI_enemyTargetMissions sibling)
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
NEXT ROOT ID: F225 UNUSED

Resume from F225 unused. Continue the finite low-mention UWAICache producer/consumer
matrix, especially remaining City lifecycle/updateDistance invariants, sponsorship and
war-history fields, pathfinder/reset ownership, and historical bugfix hunks not yet
explicitly closed. Keep the pre-version City distance issue as a deferred compatibility
note unless a stronger current consequence appears. Do not manufacture F225 merely to
advance the checkpoint number.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP13 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical checkpoint:
    C019-WIP12
New finding in this append:
    F225 / provisional KI#548
    (Implementation-side reconciliation may reasonably place this as a KI#475.3
     follow-up, e.g. KI#475.4, because the current crash repair exposes the remaining
     military-cache reconstruction half of the same revival lifecycle.)
Next finding number:
    F226 UNUSED

LOW-MENTION MILITARY-COUNT NOTE -- NOT A FINDING
------------------------------------------------
The remaining m_iNonNavalUnits population mismatch was challenged before promoting
F225. reportUnit deliberately excludes units whose XML DefaultUnitAI is
UNITAI_EXPLORE ("exclude Recon"), while the ARMY MilitaryBranch still includes
combat-capable land/air recon power. HomeGuard::initTotals therefore derives its
guard-unit fraction from a count population with Recon removed and applies that fraction
to an Army-power pool that still includes Recon.

Current SAS has several military-production, non-sea, default-Explore combat units
(Scout, Explorer, Airship and Robotic Recon Drone), so the asymmetry is live. However,
the exact split and the "exclude Recon" comment are already present in the initial UWAI
implementation, and no concrete incorrect branch decision beyond a coarse HomeGuard
allocation approximation has yet been proved. Keep this as a tuning/contract note rather
than spending an F-root unless a deterministic wrong decision is later found.

F225 / provisional KI#548
-------------------------
TITLE
    Reviving a player after units already exist initializes an empty UWAI military
    inventory and never reconstructs those units, so the revived civ is modeled with
    zero military and later unit destruction can drive incremental cache counts/power
    negative.

STATUS
    CONFIRMED inherited AdvCiv revival-cache reconstruction defect, still present after
    the current SAS KI#475.3 crash repair. KI#475.3 correctly fixes the dead-at-load
    owner/branch initialization crash; F225 is the remaining logical reconstruction
    half of the same lifecycle rather than a reason to revert that repair.

CURRENT SUPPORTED REPRODUCTION / ORDER
    A dead player can be revived by receiving a unit (the exact KI#475.3 WorldBuilder
    reproduction used a Robotic Tank).

    CvPlayer::initUnit allocates and fully initializes the unit without setting the
    player alive. CvUnitAI::finalizeInit calls CvUnit::finalizeInit, which reports the
    newly created unit to UWAI:

        kOwner.uwai().getCache().reportUnitCreated(getUnitType());

    This occurs while the player is still dead.

    Later CvGame::testAlive calls CvPlayer::verifyAlive. For a dead player:

        if (getNumCities() > 0 || getNumUnits() > 0)
            setAlive(true);

    The already-created unit is therefore the reason revival occurs; the creation
    callback necessarily precedes setAlive(true).

DEATH / REVIVAL CACHE LIFECYCLE
    On elimination AdvCiv deliberately calls:

        AI().uwai().uninit();

    UWAICache::uninit performs a full clear(). Full clear resets
    m_iNonNavalUnits and deletes all MilitaryBranch objects. This is the historical
    reason for the advc.104r comment "UWAI data gets deleted upon death."

    A player dead when a save is loaded is even less initialized: CvPlayerAI::read
    correctly skips player UWAI deserialization for a dead major, leaving the cache
    constructor state (m_eOwner == NO_PLAYER, no branch vector). This was the immediate
    KI#475.3 crash root.

    Current SAS correctly repairs that crash by calling, on revival:

        getUWAI().initRevivedPlayerInGame(getID());
        getUWAI().processNewPlayerInGame(getID());

    initRevivedPlayerInGame calls Player::init -> UWAICache::init. UWAICache::init:

        m_eOwner = eOwner;
        clear();
        updateTraits();
        createMilitaryBranches();

    The full clear sets m_iNonNavalUnits = 0 and deletes any branch data; the subsequent
    createMilitaryBranches creates brand-new branches with zero unit count and zero
    total power.

LOST CREATION CALLBACK
    In the dead-at-load case the revival-causing unit's earlier reportUnitCreated cannot
    be retained meaningfully because the cache was never deserialized/initialized.
    In a dead-during-session case it can increment residual scalar state before revival,
    but UWAICache::init then clears that state anyway.

    In both cases, once current revival initialization completes, the physically
    existing revival-causing unit is absent from the new military inventory.

    This is not repaired by processNewPlayerInGame:

        GET_PLAYER(eNewPlayer).uwai().getCache().update(true);
        for (TeamAIIter<MAJOR_CIV> it; it.hasNext(); ++it)
            it->uwai().turnPre();

    UWAICache::update rebuilds cities, transports, typical trainable units, threat,
    vassal/adjacency/asset/economic fields, etc. It does NOT enumerate owned units to
    rebuild MilitaryBranch::m_iUnits / m_rTotalPower or m_iNonNavalUnits. Those fields
    are intentionally maintained through reportUnitCreated/reportUnitDestroyed
    callbacks.

    Team::turnPre -> Player::turnPre -> m_cache.update() repeats the same kind of update
    and likewise cannot reconstruct the missing unit inventory.

CURRENT-SAS CONCRETE EXAMPLE
    The exact KI#475.3 WorldBuilder reproduction used UNIT_ROBOTIC_TANK. Current XML
    gives it:

        Domain             DOMAIN_LAND
        DefaultUnitAI      UNITAI_ATTACK
        bMilitaryProduction 1
        iCombat            56
        iPower             56

    It therefore should increment m_iNonNavalUnits and contributes nonzero ARMY power.

    After current revival initialization, however:

        actual units owned:        1 Robotic Tank
        cached m_iNonNavalUnits:   0
        cached ARMY units/power:   0 / 0  (apart from any subsequently created units)

    processNewPlayerInGame immediately runs team turnPre for all major teams. A fresh
    WarEvaluator/InvasionGraph can therefore consume this state in the same revival
    processing sequence.

LIVE CONSUMER
    InvasionGraph::Node::initMilitary copies present military directly from the cache:

        vector<MilitaryBranch*> const& kCacheMilitary = m_kCache.getPowerValues();
        pHomeGuard->initTotals(m_kCache.numNonNavalUnits(),
            kCacheMilitary[ARMY]->power() - kCacheMilitary[NUCLEAR]->power());

    It then copies the ARMY/FLEET/LOGISTICS/CAVALRY/NUCLEAR branch totals. The revived
    player can consequently be modeled as having no present army despite physically
    owning the unit that caused its revival.

DETERMINISTIC LATER UNDERFLOW
    CvUnit::kill/destruction always reports:

        kOwner.uwai().getCache().reportUnitDestroyed(getUnitType());

    reportUnitDestroyed calls reportUnit(eUnit, -1). If the revived Tank was never
    replayed into the freshly initialized cache, its later destruction has no matching
    retained +1:

        m_iNonNavalUnits: 0 -> -1

    Every MilitaryBranch that can employ the unit similarly applies -1 to a branch that
    started at zero, making its m_iUnits and m_rTotalPower negative. The ordinary
    incremental contract therefore becomes permanently unbalanced, not merely stale
    for one turn.

ANCESTRY
    Practical AdvCiv commit 2010 / 56af3a8dfa introduced explicit dead-player revival
    support. Its CvPlayer::setAlive addition already states:

        // advc.104r> (UWAI data gets deleted upon death)
        if (getUWAI.isEnabled())
            getUWAI.processNewCivInGame(getID());

    Thus the inherited design already knew that elimination deletes UWAI state, but
    revival only called the "process/update" stage. That stage did not recreate or scan
    the deleted military inventory.

    The later scaled/UWAI rewrite renamed this to processNewPlayerInGame without
    changing that reconstruction contract.

    Base AdvCiv 1.14 therefore contains the deeper omission. For a player dead during
    the running session, m_eOwner can remain valid but the MilitaryBranch vector deleted
    by uninit is not rebuilt by processNewPlayerInGame. For a player dead when a save
    loads, the skipped deserialization additionally leaves m_eOwner == NO_PLAYER; that
    second condition is what made the current WorldBuilder reproduction crash before
    KI#475.3.

    SAS practical commit 6283 / 150aff570a (KI#475.3) correctly adds
    initRevivedPlayerInGame before processNewPlayerInGame, restoring the owner and branch
    objects. It converts the inherited crash/uninitialized-object failure into a safe
    but still incomplete zero-inventory state. F225 therefore extends rather than
    contradicts KI#475.3.

REPAIR BOUNDARY
    After revival-specific UWAICache::init has created a clean cache and MilitaryBranch
    vector, rebuild the incremental military inventory from the player's units that
    already exist.

    A narrow repair can iterate the revived player's current units once and replay the
    equivalent of reportUnitCreated(unit.getUnitType()) before
    processNewPlayerInGame/its immediate war-evaluation refresh.

    This restores:
      * m_iNonNavalUnits;
      * MilitaryBranch unit counts;
      * MilitaryBranch present-power totals.

    Do not make ordinary per-turn UWAICache::update scan every unit merely to repair
    this exceptional lifecycle. Incremental creation/destruction reporting is otherwise
    intentional and cheaper.

    Do not move or duplicate team initialization into the player repair. KI#475.3's
    distinction remains correct: CvTeam::changeAliveCount initializes a team returning
    from zero alive members, while a surviving teammate's team UWAI must remain intact.

    If a dedicated "rebuild unit-derived military cache" helper is introduced, it
    should run only after full player-cache initialization and before consumers, and it
    should start from known zero branch/count state to avoid double-reporting.

NEGATIVE CONTROLS / SCOPE
    * New colonial players are not the same lifecycle. They receive normal player UWAI
      initialization before their later processNewPlayerInGame stage; units created
      after initialization can be tracked incrementally.
    * F225 does not reopen KI#475.3's owner/cache crash repair. The initialization call
      is necessary.
    * Typical-unit refresh is not a reconstruction of owned-unit power; it scans what
      unit type is representative/trainable. F214 concerns stale typical-unit identity
      and is independent.
    * Generic CvPlayer power is updated correctly by CvUnit::finalizeInit; F225 is
      specifically the differentiated UWAI MilitaryBranch/non-naval cache.
    * The earlier conversion +1/-1 staging concern remains rejected: ordinary unit
      conversion paths resolve delayed death and preserve a matching incremental pair.
      Revival is different because a full cache clear occurs between the +1 creation
      event and all later consumers/destruction.

IMPLEMENTATION-SIDE NUMBERING NOTE
    Album continuity uses F225 / provisional KI#548. Because this was found by extending
    the exact KI#475.3 revival lifecycle and the practical repair likely belongs beside
    that crash fix, the implementation/reconciled Known Issues side may prefer a decimal
    follow-up such as KI#475.4. Preserve the root and proof; the public KI number can be
    reconciled independently.

C019-WIP13 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP13
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548 (implementation-side likely KI#475.3 follow-up)
NEXT ROOT ID: F226 UNUSED

Resume from F226 unused. Continue the remaining low-mention UWAICache producer/consumer
matrix: sponsorship/war-history fields, pathfinder/reset ownership, remaining City
incremental/updateDistance contracts, serialization/version fallbacks and historical
bugfix hunks. Keep the Recon/nonNaval denominator asymmetry as an intentional/coarse
heuristic note unless a deterministic wrong decision is proved. Keep the ancient
pre-version City-distance issue deferred unless a stronger current consequence appears.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP14 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical checkpoint:
    C019-WIP13
New finding in this append:
    F226 / provisional KI#549
Next finding number:
    F227 UNUSED

F226 / provisional KI#549
-------------------------
TITLE
    City::updateDistance conflates "a sea route exists" with "sea is the fastest
    pairwise route": a valid naval route from a secondary/non-primary colony can be
    ignored because a shorter local land path is present even though that land path is
    explicitly forbidden from establishing strategic reachability, causing the target
    city to be cached as wholly unreachable.

STATUS
    CONFIRMED inherited UWAI reachability/route-selection invariant mismatch. Present
    from the initial 2017 WarAndPeaceCache implementation and still present in current
    AdvCiv 1.14 and AdvCiv-SAS. Later practical commit 2767 broadened qualifying land
    reach from the capital to any primary area and therefore reduced the incidence, but
    did not remove the underlying mismatch. Practical 2929 added a sea-search
    optimization that gives the same mismatch an additional skip path when the
    non-qualifying land route is already short.

CURRENT ROOT
    UWAICache::City::updateDistance tracks two logically distinct things:

      1. iPairwDuration -- the best/shortest deployment duration found from a source
         city to the target;
      2. m_bReachByLand / bReachBySea -- whether at least one strategically acceptable
         route exists so that the target is not globally classified unreachable.

    A land path from any qualifying source city is allowed to populate
    iPairwDuration. But only a source in an AI primary area establishes
    m_bReachByLand:

        if (landFinder.generatePath(...))
        {
            iPairwDuration = ...;
            if (kCacheOwner.AI_isPrimaryArea(pCity->getArea()))
                m_bReachByLand = true;
            ...
        }

    This primary-area restriction is deliberate. AI_isPrimaryArea currently means a
    non-water area with either more than two of the player's cities or the capital.
    It prevents a small/peripheral colony near a rival from making that rival look like
    a normal land target for the whole empire.

    The sea branch, however, uses the existing iPairwDuration as both a performance
    shortcut and the condition for setting the *existence* flag:

        if (bAnyTransports &&
            kStart.isCoastalLand(-1) &&
            iPairwDuration > iSeaPenalty + 2)
        {
            ...
            if (iPathLength > 0)
            {
                int iPathTurns = ...;
                if (iPathTurns < iPairwDuration)
                {
                    iPairwDuration = iPathTurns;
                    bReachBySea = true;
                }
            }
        }

    Thus a legal sea path sets bReachBySea only when it also beats the local land
    duration. If the local land path came from a non-primary area, that land path can
    suppress the only flag that would make the target strategically reachable even
    though the same land path is explicitly not allowed to set m_bReachByLand.

    The final gate then discards every collected duration:

        if ((!m_bReachByLand && !bReachBySea) || aiPairwDurations.empty())
            return; // m_iDistance remains -1

SUPPORTED CURRENT MAP STATE
    A straightforward current-data state is enough:

      * the cache owner has an inland capital/main area, so no main-area naval launch
        route establishes reach to the target;
      * it also owns exactly two substantial cities on a secondary landmass. With only
        two cities and no capital there, AI_isPrimaryArea is false for that area;
      * at least one of those secondary cities is coastal and passes updateDistance's
        population/production filters;
      * an enemy coastal city is on that same secondary landmass;
      * a normal land path from the colony to the enemy exists and is short;
      * the player genuinely has cargo transports and a valid water path also exists
        from that colony to the enemy, but the naval trip is slower than the local land
        trip.

    For example, a local land duration of 4 and a legal naval duration of 6 produces:

        land path found:
            iPairwDuration = 4
            m_bReachByLand = false      // secondary area

        naval path:
            either not searched if the sea-penalty shortcut deems it unable to improve,
            or searched but 6 < 4 is false
            bReachBySea = false

        final:
            !m_bReachByLand && !bReachBySea
            => return with m_iDistance == -1

    Yet the target has a mechanically valid sea route from a city that the same
    function accepts as a naval source.

DESIGN NEGATIVE CONTROL
    This is not merely disagreement with the deliberate primary-area policy.

    The sea branch contains no AI_isPrimaryArea restriction. A secondary coastal city
    *is allowed* to establish bReachBySea when its sea route happens to be faster than
    its local land route. Therefore current policy already says that a peripheral port
    may establish naval reachability.

    The defect is that the exact same valid sea route stops counting as reachability
    solely because a shorter land path is also present -- even though that shorter land
    path has independently been declared insufficient to establish strategic land
    reachability.

    Route existence and route-speed selection are separate contracts; the implementation
    accidentally couples them.

LIVE CONSEQUENCES
    City::canReach is simply:

        return (m_iDistance >= 0);

    UWAICache::add increments m_aiReachableCities for foreign owners only when
    kCacheCity.canReach() is true. Therefore the false -1 propagates to the player/team
    reachability API:

        UWAI::Player::canReach(eTarget)
            -> cache.numReachableCities(eTarget) > 0

        UWAI::Team::canReach(eTarget)
            -> any member cache has numReachableCities(targetMember) > 0

    InvasionGraph::Node::targetCity stops at the first cached city that is not reachable
    and never chooses such a city as a target. Reachability counts also feed current
    WarUtilityAspect paths such as operational Readiness and elimination/hopeless-war
    logic.

    A valid target can therefore disappear from UWAI's target set because the legal sea
    route was slower than a deliberately non-qualifying local land route.

ANCESTRY
    The invariant mismatch already exists in practical initial AdvCiv commit 1253 /
    f6ff6a182f, in WarAndPeaceCache::City::updateDistance.

    The original comments are particularly diagnostic:

        "reachByLand refers to our (AI) capital. This is to ensure that the
         AI can still detect the need for a naval assault when it has a
         colony near the target civ."

    and immediately afterward:

        "// Reach by sea from c (whereas reachBySea refers to any city of agent)"

    Yet the original sea code sets the global reachBySea flag only inside:

        if (pwd < 0 || d < pwd)
        {
            pwd = d;
            reachBySea = true;
        }

    Thus from the initial implementation a non-capital colony could have a valid land
    path that does not establish reachByLand, while a valid but slower sea path also
    fails to establish reachBySea.

    Practical 2663 removed reachBySea from the serialized/external City interface and
    kept it only as a local updateDistance reachability flag; this does not change the
    root.

    Practical 2767 ("UWAI: Relax conditions for enemy cities being land targets")
    deliberately broadens qualifying land reach from only the capital to any
    AI_isPrimaryArea and adds the separate capital-area flag. That reduces the number of
    source cities exposed to the mismatch, but secondary areas still retain it.

    Practical 2929 ("Fix some issues with the calc of UWAI deployment distances") adds:

        iPairwDuration > iSeaPenalty + 2

    to avoid naval pathfinding when sea cannot be much faster. That is a valid speed
    optimization only when the existing land route already satisfies the reachability
    contract. For a secondary-area land route it can now prevent the sea existence
    check entirely.

REPAIR BOUNDARY
    Separate "valid sea route exists" from "sea is the duration chosen for this source."

    In particular:

      * when a coastal source's land route does NOT itself establish strategic land
        reachability (e.g. source area is non-primary), do not let the
        iPairwDuration/sea-penalty optimization skip the naval existence test solely
        because that local land route is short;
      * once a valid water path is found, let that fact establish bReachBySea
        independently of whether iPathTurns is faster than the current
        iPairwDuration;
      * continue to replace iPairwDuration with iPathTurns only when the sea route is
        actually faster. The shorter local land duration may still contribute to the
        weighted deployment estimate once the target is known to be strategically
        reachable; this is consistent with the existing design that allows secondary
        city durations to influence the aggregate without making them land targets.

    One narrow implementation shape is to track whether the current land route
    qualifies for m_bReachByLand, force the sea-path existence test for a non-qualifying
    coastal source, set bReachBySea whenever the water finder succeeds, and keep the
    `iPathTurns < iPairwDuration` comparison only around the duration replacement.

    Do not "repair" F226 by simply allowing every secondary-area land path to set
    m_bReachByLand. Practical 2767 and its downstream InvasionGraph/ArmamentForecast
    changes show that the primary-area distinction is deliberate.

NEGATIVE CONTROLS / SCOPE
    * If any qualifying primary-area source has a land path, m_bReachByLand is already
      true and the target remains reachable; F226 concerns cases where only
      non-qualifying land paths coexist with a legal naval route.
    * If the sea route is already faster than the local land route, current code sets
      bReachBySea and no false-unreachable result occurs.
    * If no valid sea route exists, preserving distance=-1 under the primary-area policy
      is intentional.
    * F218 is independent: it concerns falsely claiming transport trainability. F226
      assumes genuine cargo capability and a water path that the current water finder
      can actually generate.
    * The earlier primary-area-only land-reachability concern remains rejected as an
      intentional strategic heuristic. F226 is the interaction between that deliberate
      heuristic and the unrelated fastest-route test.

C019-WIP14 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP14
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
NEXT ROOT ID: F227 UNUSED

Resume from F227 unused. Continue the finite low-mention UWAICache producer/consumer
matrix and the remaining historical bugfix checklist, especially sponsorship/war-history
state, City construction/updateAssetScore and target-value synchronization, remaining
serialization fallbacks, and any incremental state whose producer can disappear between
full updates. Preserve the intentional primary-area policy; F226 only separates route
existence from fastest-route selection.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP15 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical checkpoint:
    C019-WIP14
New finding in this append:
    F227 / provisional KI#550
Next finding number:
    F228 UNUSED

F227 / provisional KI#550
-------------------------
TITLE
    Mid-turn city destruction removes an entry from UWAICache's stateful
    area-clustering attack order without re-sorting the survivors, so a raze can leave
    the next invasion target in an order that a fresh cache build would never produce.

STATUS
    CONFIRMED inherited AdvCiv UWAICache lifecycle/refactor defect. Current in Base
    AdvCiv 1.14 and AdvCiv-SAS. The modern destroy/create callback split was introduced
    by practical AdvCiv commit 2605 / f788c46a21 ("Various optimizations for
    UWAICache"). A capture that creates a replacement city eventually re-sorts on the
    create half; a pure destruction/raze has no create half and therefore leaves the
    greedily sorted vector stale until the next full cache update.

CURRENT ROOT
    sortCitiesByAttackPriority is not an ordinary fixed-key sort. It is a greedy
    selection pass whose score for each candidate depends on the area of the city
    selected immediately before it:

        if (i > 0 && pPrevArea != NULL &&
            !m_cityList[j]->city().isArea(*pPrevArea) &&
            rPriority.isPositive())
        {
            rPriority /= 2;
        }

    This deliberately clusters consecutive invasion targets in the same area.

    The modern destruction callback is:

        void reportCityDestroyed(CvCity const& kCity) { remove(kCity); }

    remove() erases the City object from m_cityMap and m_cityList but performs no
    sortCitiesByAttackPriority() afterward.

    The creation callback, by contrast, explicitly does:

        add(kCity);
        sortCitiesByAttackPriority();

    Thus a normal ownership transfer that destroys the old CvCity and creates a new
    CvCity is eventually re-sorted, but a razed/disbanded city is not.

DELETION DOES NOT PRESERVE THIS SORT
    Consider three currently reachable foreign cities with raw attack priorities:

        A1 = 100   area A
        A2 =  60   area A
        B1 =  90   area B

    A fresh greedy sort gives:

        i=0: no area penalty -> choose A1 (100)
        i=1: previous area A
             A2 = 60
             B1 = 90 / 2 = 45
             -> choose A2
        result: A1, A2, B1

    If A1 is then destroyed, vector erasure produces:

        A2, B1

    But a fresh sort of the surviving set must begin with raw priorities because
    position zero has no previous-area penalty:

        B1 = 90 > A2 = 60
        -> correct fresh order: B1, A2

    Therefore simple erasure is not order-preserving under the algorithm's own
    comparison semantics.

SUPPORTED HUMAN-TURN REPRODUCTION
    CvCity::kill notifies every major AI cache through AI_cityKilled before deleting the
    CvCity object, so a raze reaches reportCityDestroyed/remove immediately.

    A particularly ordinary path is:
      1. human captures an enemy city;
      2. acquisition creates the replacement CvCity and reportCityCreated re-sorts;
      3. human chooses to raze that newly acquired city;
      4. the new city is killed and reportCityDestroyed removes it;
      5. no replacement city is created, so no later reportCityCreated re-sort occurs;
      6. the rest of the human turn continues with a stale attack-order vector.

    This is exactly within the special mid-turn cache contract documented beside these
    callbacks:

        "It's important that the cache keep up with ownership changes
         so that the AI is aware of conquests made throughout a human turn."

LIVE CONSUMER
    InvasionGraph::Node::targetCity() iterates m_kCache.cityAt(i) in cached order and
    returns the first reachable, target-owned city that has not already been lost in the
    simulation.

    Thus the stale A2,B1 order above can make military analysis select A2 as the next
    target whereas a cache rebuilt from the same post-raze game state selects B1.

    Human-AI peace and other immediate war evaluations construct fresh WarEvaluator /
    InvasionGraph objects during the human turn, before the affected AI's next
    UWAICache::update. The stale vector is therefore behaviorally observable rather
    than merely an internal ordering discrepancy that is always repaired before use.

ANCESTRY
    The initial practical 1289 ownership-change callback updated/replaced one cached
    city and called sortCitiesByAttackPriority() at the end.

    Practical 2605 / f788c46a21 refactored the lifecycle to modern pointer-safe
    reportCityDestroyed/reportCityCreated callbacks. The old replacement callback's
    final unconditional sort was moved to reportCityCreated:

        add(c);
        sortCitiesByAttackPriority();

    while reportCityDestroyed became remove-only.

    This split is appropriate for captures because the creation callback follows the
    destruction callback and performs the final sort. It accidentally drops the old
    re-sort guarantee for destruction with no replacement. F227 is therefore best
    classified as an inherited AdvCiv 2605 lifecycle/refactor edge, not SAS-specific.

REPAIR BOUNDARY
    Re-establish the ordering invariant after a successful mid-turn removal when no
    later creation can be assumed.

    The simplest safe repair is to call sortCitiesByAttackPriority() after remove()
    actually erased a cached City, either from reportCityDestroyed or as part of a
    removal helper whose callers require current ordering.

    Do not rebuild City distances/asset scores merely for F227; the proven invariant is
    only the ordering of the surviving already-cached entries.

    Be aware that every major AI cache receives AI_cityKilled, so a blind re-sort after
    every notification has some cost. A small refinement could make remove return
    whether an entry was actually erased and sort only then. This remains far cheaper
    than a pathfinding/cache rebuild and occurs only on city destruction.

    F224 is independent: it concerns the cache owner's aggregate total assets not being
    reconciled with the changed own-city set. A practical implementation can update the
    aggregate and restore the sort in the same destruction callback, but the invariants
    are separate.

NEGATIVE CONTROLS / SCOPE
    * A standard city ownership transfer that creates a replacement is eventually
      sorted by reportCityCreated; F227's clearest live case is destruction/raze with no
      creation half.
    * Removing an element preserves the broad partition of reachable/own/unreachable
      priorities well enough that targetCity's break-on-invalid assertion is not the
      root. The defect is which valid target comes first.
    * If the removed city was late enough that the survivor order coincidentally
      remains equal to a fresh greedy sort, no behavioral difference occurs. The
      A1/A2/B1 construction proves that preservation is not guaranteed.
    * Capital relocation and other expensive city-derived changes after a conquest are
      separate cache-cadence questions. F227 requires only the existing cached
      priorities and the sort's own stateful area penalty.

SPONSORED-WAR MIXED-BUNDLE REJECTION -- NOT F228
-------------------------------------------------
The previously open reportSponsoredWar mixed-bundle seam was not promoted.

Human-AI trade-list filtering prevents the strongest reciprocal-overvaluation cases:
TRADE_WAR is nonannual, while Open Borders, Defensive Pact and Permanent Alliance are
annual dual items; at peacetime the trade UI hides annual/nonannual mixing once an item
from the opposite class is on the offer.

The remaining legal nonannual compensation items (gold, technology, city, maps, etc.)
are all things the sponsor can genuinely transfer to the hireling as part of the same
deal. If the hireling also gives an unrelated nonannual item back, whether its later
sense of obligation should remember gross received payment or marginal/net surplus is
a policy question, not a demonstrated contract violation. reportSponsoredWar's own
comment describes remembering the utility/deal value of the payment and even explicitly
allows overpayment to increase reluctance to end the war.

More than one TRADE_WAR item in a normal UWAI-valued deal remains intentionally blocked
by AI_dealVal's +1,000,000 second-war price. Do not spend an F-root on these bundle
shapes without a stronger supported contract.

INSPECTION CORRECTION -- NO SAVE-STREAM DEFECT
----------------------------------------------
During this pass an inspection command printed source ranges 1-220 and 220-360
consecutively. Their one-line overlap made the line at 220,
m_aiVassalTechScore.read(pStream), appear twice in terminal output.

Direct inspection of the actual current file and normalized comparison with Base
AdvCiv 1.14 prove that the version-9 sequence is correct:

    m_aiVassalTechScore.read(pStream);
    m_aiVassalResourceScore.read(pStream);
    m_aiAdjLandPlots.read(pStream);

There is no duplicate read, no enum-map stream shift and no finding. Preserve this
negative control so the false terminal-artifact suspicion is not rediscovered later.

HISTORICAL / LOW-MENTION CLOSURE PROGRESS
-----------------------------------------
* Practical 2751 ("UWAI: Fix crash in games that start with free cities") remains
  intact. Current update still suppresses threat and war-utility evaluation when player
  history/yield estimates are unavailable, now additionally honoring bNewPlayer.
* update's major dependency order is coherent: transport capability precedes City
  distance construction; typical-unit refresh precedes threat calculations consuming
  typical power/cost; all ordinary cache inputs precede updateWarUtility.
* create/delete/reset TeamPathFinders ownership remains balanced; no escaping reference
  was found.
* calculateFocusOnPeacefulVictory, updateTraits, updateAdjacentLand and the currently
  unimplemented relative-navy cache did not expose a new correctness root in this pass.
* The broader capital-relocation/full-distance refresh question remains a cache-cadence
  limitation rather than F228 at this stage. Unlike F224/F227, repairing it would imply
  substantial pathfinding/target-value recomputation and no explicit contract has yet
  been proved that those expensive derived values must be transactionally refreshed
  during the human turn.

C019-WIP15 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP15
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
NEXT ROOT ID: F228 UNUSED

Resume from F228 unused. Continue the finite low-mention UWAICache producer/consumer
matrix and remaining historical bugfix checklist, with special attention to
war-history/sponsorship state, City target/asset synchronization, old-version read
fallbacks and incremental fields whose producer disappears between full updates.
Do not promote the capital-relocation cache-cadence question without a stronger
transactional contract.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP16 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical checkpoint:
    C019-WIP15
New finding in this append:
    F228 / provisional KI#551
Next finding number:
    F229 UNUSED

AGGRESSIVE / LEADERSHIP TRAIT RECHECK -- NOT F228
--------------------------------------------------
The low-mention updateTraits helper initially looked stale under current SAS XML:
Base AdvCiv Aggressive grants Combat I, while SAS practical 4856 changes its free
promotion to Leadership. updateTraits recognizes an "offensive trait" only when one of
the trait's free promotions has positive generic combat percent.

Current Leadership has:
    iCombatPercent = 0
    iCityDefense   = 0
    iExperiencePercent = 100

so current Aggressive leaders correctly no longer trip m_bOffensiveTrait.

This is not a correctness regression. The only live hasOffensiveTrait consumer in
InvasionGraph explicitly models the old direct Combat-I effect by adding +10% to
simulated army power. Leadership no longer grants that immediate +10% combat strength;
continuing to classify it as the old offensive-trait bonus would overstate current SAS
army strength. Leadership's faster experience gain may deserve separate long-term
modeling, but that is an AI-enhancement/design question rather than a stale cache bug.

The m_bDefensiveTrait flag is used elsewhere (e.g. distrustRating), and current
Protective/City Garrison data still trips its intended city-defense test. No F-root.

PERMANENT-ALLIANCE STALE CITY-OBJECT RECHECK -- NOT F228
---------------------------------------------------------
After a Permanent Alliance, existing UWAICache::City objects can temporarily retain
their pre-merge `m_iDistance` classification, so a newly absorbed teammate's cities can
still look "foreign" (`distance > 0`) to a survivor member's cache until its next normal
refresh.

This is real staging, but no current wrong target result was proved:
InvasionGraph re-checks the cached city's live owner/team/war-opponent status before
accepting it as a military target, and totalAssetScore only sums cities actually owned
by m_eOwner. The stale teammate entries may be scanned past but do not become valid
enemy targets under the tested consumers. Do not promote a full post-PA City-cache
rebuild without a stronger behavioral contract.

F228 / provisional KI#551
-------------------------
TITLE
    Permanent Alliance migration is one-way for persistent team-keyed UWAI history:
    members of the surviving team inherit the absorbed leader's history, but outsider
    caches that store past-war or sponsored-war state *against the absorbed team* are
    never remapped to the surviving team. A paid hireling can therefore forget its
    live war obligation immediately when its target joins a Permanent Alliance, and
    outsiders permanently forget war-experience history against the absorbed half.

STATUS
    CONFIRMED inherited AdvCiv/UWAI Permanent-Alliance state-migration defect.
    The one-way merge is present from the initial WarAndPeace/UWAI integration.
    Current AdvCiv 1.14 and AdvCiv-SAS retain it.

PERSISTENT STATE
    UWAICache::clear(true) deliberately preserves:
        m_aiPastWarScore
        m_aiBounty
        m_aeSponsorPerTarget
    because these are event-maintained historical/obligation fields rather than
    ordinary per-turn derived cache values.

    reportWarEnding updates m_aiPastWarScore and clears sponsorship for the specific
    enemy TeamTypes key.

    reportSponsoredWar stores:
        m_aiBounty[eTarget]
        m_aeSponsorPerTarget[eTarget]

    HiredHand and confidenceFromPastWars later query these fields using the *current*
    target TeamTypes.

CURRENT PA MERGE
    CvTeam::addTeam(eAbsorbedTeam) saves the absorbed team's leader id, transfers the
    absorbed players onto the surviving team and merges the core team relations.

    In particular it explicitly propagates differing wars in both directions through
    queueWar/triggerWars. If the absorbed team B was at war with outsider C and the
    surviving team A was not, C becomes at war with A as part of the alliance merge.

    Near the end of CvTeam::addTeam current code calls only:

        AI().uwai().addTeam(eTeamLeader);

    UWAI::Team::addTeam then iterates the *members of the newly merged surviving team*:

        for (MemberAIIter it(m_eAgent); it.hasNext(); ++it)
            it->uwai().getCache().addTeam(eOtherLeader);

    UWAICache::addTeam copies the absorbed leader's own persistent history into each
    surviving-team member:

        m_aiPastWarScore.add(eTeam, kOther.m_aiPastWarScore.get(eTeam));

    and chooses/merges the absorbed leader's sponsorship/bounty for each target.

    No corresponding loop visits major-civ players outside the alliance and remaps
    values whose *key* is the absorbed TeamTypes to the surviving TeamTypes.

SUPPORTED SPONSORED-WAR REPRODUCTION
    Let:
        A = team that will survive the Permanent Alliance
        B = team that will be absorbed
        C = AI hireling outside the alliance
        D = sponsor

    1. D pays C to declare war on B.

       CvDeal reports the sponsorship to C's cache:
           sponsorAgainst[B] = D
           bountyAgainst[B]  > 0

    2. Later A and B form a Permanent Alliance.

       CvTeamAI::AI_permanentAllianceTrade does not require the two teams to have
       identical current wars. CvTeam::addTeam explicitly contains the machinery for
       transferring differing wars.

    3. Because B was at war with C, the core team merge makes C at war with surviving
       team A as well.

    4. The UWAI addTeam call updates A/B's newly merged member caches only. C's cache
       remains:
           sponsorAgainst[B] = D
           bountyAgainst[B]  > 0

       while:
           sponsorAgainst[A] = NO_PLAYER
           bountyAgainst[A]  = 0
       in the clean reproduction where C had no prior sponsorship against A.

    5. C subsequently considers peace with the war opponent that now actually exists,
       team A.

       HiredHand::evaluate uses:
           eTheirTeam = A
           ourCache().sponsorAgainst(eTheirTeam)
           ourCache().bountyAgainst(eTheirTeam)

       and therefore sees no sponsorship.

    The paid-war obligation can vanish immediately solely because the target team
    changed identity through a Permanent Alliance. The payment, sponsor, hireling and
    continuing physical war all still exist.

LIVE CONSEQUENCE
    HiredHand is specifically designed to make an AI "inclined to fight for 20 turns"
    after being hired. It turns the remembered original payment utility into positive
    utility for continuing the war.

    Under F228 that utility disappears for the transferred war against A. C can become
    willing to make peace substantially earlier than it would have if B had remained a
    standalone team, despite D having paid for the same continuing conflict.

PAST-WAR HISTORY SIBLING UNDER THE SAME ROOT
    m_aiPastWarScore is likewise keyed by target TeamTypes and preserved across turns.

    UWAI::Player::confidenceFromPastWars(eTarget) reads exactly:
        m_cache.pastWarScore(eTarget)

    and maps a negative history into reduced future war confidence (first lost-war scale
    roughly -15%, bounded over repeated history).

    If outsider C had a strongly negative past-war score against B but no history
    against A, B's absorption makes all future evaluations target A. C then reads
    pastWarScore[A] and the B history is orphaned under the dead TeamTypes key.

    This is especially diagnostic because UWAICache::addTeam already treats
    `m_aiPastWarScore` as persistent team-related data that *must* be merged for the
    surviving alliance members. The omission is the reverse direction: outsiders'
    histories about the absorbed target are never merged/remapped.

ANCESTRY
    The initial practical AdvCiv WarAndPeace implementation (1253 /
    f6ff6a182f) already defines WarAndPeaceCache::addTeam as a one-way operation:
    it obtains the absorbed team's leader cache and adds that leader's past-war and
    sponsorship data to the surviving cache.

    CvTeam::addTeam then invokes WarAndPeaceAI::Team::addTeam only for the surviving
    team. There is no outsider reverse migration.

    Practical 1268 changes the API from an absorbed TeamTypes to the saved absorbed
    leader PlayerTypes so the source cache remains accessible after player reassignment;
    it preserves the same one-way semantics.

    At that time CvTeam::addTeam also called getWPAI.update() after the merge. That
    function merely rebuilt the global lists of alive major teams/civs and team-member
    vectors. It did not rewrite any UWAICache/WarAndPeaceCache history maps, so it did
    not repair the outsider direction.

    Practical 1818 replaces cached team-member vectors with live AgentIterators and
    removes the old global registry refresh. F216 is a separate regression caused by
    that live iterator including the absorbed leader itself. F228 predates 1818: the
    missing outsider-key migration exists from the original design.

    Current CvTeam::addTeam contains extensive reverse-direction migration for core
    bilateral counters/relations (war counters, contact history, war success,
    espionage, etc.), which further demonstrates the general team-identity contract.
    UWAI's persistent outsider maps remain an exception.

REPAIR BOUNDARY
    During Permanent Alliance absorption, before the absorbed team identity becomes
    unusable as a live target, migrate persistent outsider cache entries keyed by
    eAbsorbedTeam to eSurvivingTeam.

    At minimum cover the fields that UWAICache::addTeam itself classifies as persistent
    team-related history:
        * m_aiPastWarScore
        * m_aiBounty
        * m_aeSponsorPerTarget

    For each major-civ player outside the merged team:
      * combine pastWarScore[absorbed] into pastWarScore[survivor] using the same
        additive policy already used when survivor-side member histories are merged;
      * if sponsorship against the absorbed team exists, preserve it under the
        surviving target. If both keys already contain sponsorships, use a coherent
        conflict policy (the existing UWAICache::addTeam policy chooses the higher
        bounty when both have sponsors);
      * clear/reset the absorbed-team entries afterward if practical, to avoid retaining
        dead-key duplicate history.

    Keep the direction conceptually separate from survivor-member `addTeam` to avoid
    reintroducing F216 self-addition. A dedicated "target team absorbed" helper on
    outsider caches would make the two directions explicit.

    Derived per-turn maps (threat, reachable cities, vassal scores, war utility, etc.)
    need not all be folded into F228; they have normal refresh semantics or live-team
    checks. The proven root concerns persistent event-maintained history/obligation
    that otherwise never migrates itself.

NEGATIVE CONTROLS / SCOPE
    * If C already has a sponsorship against surviving A, the conflict needs a merge
      policy; the clean reproduction intentionally starts with none.
    * If C's war against B ends normally before the alliance, reportWarEnding can clear
      the B sponsorship. F228 concerns a war transferred by the alliance itself.
    * The second HiredHand branch ("we've hired someone") also uses current-team keys,
      but the direct loss of C's own paid obligation is sufficient proof.
    * F216 is different: it concerns self-add/doubling inside the surviving merged
      team's member loop after the AgentIterator refactor. F228 is the missing outsider
      target-key direction and exists even in the original vector-based implementation.
    * The temporarily stale UWAICache::City team classification after a PA is not part
      of F228; no concrete wrong target was proved from it.

C019-WIP16 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP16
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
NEXT ROOT ID: F229 UNUSED

Resume from F229 unused. Continue the literal low-mention function/producer/consumer
coverage matrix and remaining historical bugfix checklist. Persistent team-keyed PA
migration is now owned by F228; do not broaden it automatically to all per-turn derived
maps. Revisit only if another event-maintained field is proved to have the same orphaned
target-team lifetime.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C019-WIP17 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-27
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260827T205116.zip
Prior physical checkpoint:
    C019-WIP16
New material in this append:
    F228 same-root refinement: ReadyToCapitulate target-team migration
    F229 / provisional KI#552
Next finding number:
    F230 UNUSED

F228 SAME-ROOT REFINEMENT -- READY-TO-CAPITULATE TARGET KEY
-----------------------------------------------------------
F228's persistent outsider target-team migration root also covers
m_readyToCapitulateTo.

This state is not an ordinary per-turn heuristic. It is an event-maintained team-level
handshake used specifically for AI capitulation to a human master. Non-leader member
queries redirect to the current team leader's cache, and AI_surrenderTrade refuses a
human master with DENIAL_RECENT_CANCEL unless the current leader cache says the team is
ready.

Supported target-absorption reproduction:

    A = surviving team in a Permanent Alliance
    B = absorbed human team
    C = outside AI team currently at war with B

Before the alliance, C's UWAI has already decided to capitulate to the human side:

    C.readyToCapitulateTo contains B

The core Permanent-Alliance merge can transfer C's war with B onto surviving team A.
Because B's human member is moved onto A, A is now a human team. The physical war and
the already-made UWAI capitulation decision therefore concern the same human side, now
under TeamTypes A.

But C's persistent set is untouched by UWAI::Team::addTeam:

    contains(B) == true
    contains(A) == false

A subsequent AI_surrenderTrade(A) asks isReadyToCapitulate(A), which returns false and
can therefore produce DENIAL_RECENT_CANCEL solely because the prospective master was
renumbered by the Permanent Alliance.

This belongs under F228 rather than a new F-number. The same proven invariant applies:
persistent event-maintained state whose key denotes a target team must survive
absorption of that target team.

F228's outsider migration repair boundary should therefore include, at minimum:

    m_aiPastWarScore
    m_aiBounty
    m_aeSponsorPerTarget
    m_readyToCapitulateTo

For readiness, the natural merge policy is set-union under the surviving human target:
if an outsider was ready to capitulate to either side and the merged surviving team is
human, preserve readiness under the surviving TeamTypes. Remove the absorbed dead-key
entry afterward when practical.

Do not apply F228 mechanically to m_aiWarUtilityIgnoringDistraction or ordinary
threat/reachability maps. Those are derived/reset by normal update cadence; F228 is
about state that otherwise has no producer capable of recreating the lost historical
meaning after the target TeamTypes disappears.

COMPACT-MAP BOUNDS PASS -- NEGATIVE CONTROLS
--------------------------------------------
Several current UWAICache integer maps use compact short storage:

    CivPlayerMap<int,short> m_aiReachableCities
    CivPlayerMap<int,short> m_aiTargetMissions
    CivTeamMap<int,short>   m_aiPlotsLostAtWar
    CivTeamMap<int,short>   m_aiBounty

EnumMap's normal compactValue path is a plain cast unless ENUM_MAP_EXTRA_ASSERTS is
enabled; that macro is currently commented out. Therefore each such field needs its
own producer-range argument rather than assuming the template protects it.

No new root is promoted for the first three at this checkpoint:

* Default SAS48 world dimensions are 99x70 terrain cells, and current AdvCiv map reset
  uses two plots per cell for ordinary world-size dimensions: 198x140 = 27,720 plots.
  Thus even an intentionally absurd "every plot lost against one rival" upper bound
  stays below signed-short max on the default largest SAS world size.
  Custom map scripts that override grid dimensions can use a different scale and remain
  a future robustness concern, but no current F-root is assigned solely on that basis.

* m_aiReachableCities is bounded more tightly than the total plot count under ordinary
  city-placement rules, so the default SAS48 geometry is also a strong negative control.

* m_aiTargetMissions counts units/cargo rather than plots and therefore lacks the same
  mathematical map bound. A gigantic unit population could in principle exceed 32767,
  but no supported current-SAS reproduction near that threshold has been established.
  Keep this as robustness only unless concrete unit-count evidence appears.

m_aiBounty is materially different and is F229 below.

F229 / provisional KI#552
-------------------------
TITLE
    Sponsored-war bounty is compacted from int utility to signed short without a range
    check; a sufficiently large legal positive overpayment wraps negative and makes
    reportSponsoredWar erase the sponsorship entirely.

STATUS
    CONFIRMED AdvCiv enum-map compression regression. The sponsored-war value was an int
    in the original UWAI/WarAndPeace cache. Practical AdvCiv commit 3183 /
    59a3e3190061 ("Rewrite of enum map classes") changed m_aiBounty from
    CivTeamMap<int> to CivTeamMap<int,short> as part of a nominally non-functional
    compact-storage rewrite. Current AdvCiv 1.14 and AdvCiv-SAS retain the short.

CURRENT STORAGE / CAST CONTRACT
    UWAICache.h:

        CivTeamMap<int,short> m_aiBounty;

    EnumMap's compact conversion is:

        #ifdef ENUM_MAP_EXTRA_ASSERTS
            return safeCast<ToType>(fValue);
        #else
            return (ToType)fValue;
        #endif

    ENUM_MAP_EXTRA_ASSERTS is currently disabled/commented out. Thus assigning an int
    outside signed-short range performs the target compiler's narrowing conversion
    without assertion or clamping. On the supported MSVC/Windows target, signed short
    is 16 bit and the practical result is low-16-bit/two's-complement wrapping.

SUPPORTED PAYMENT PATH
    CvPlayer::canTradeItem(TRADE_GOLD) imposes no special diplomatic upper bound on a
    specified human gold amount:

        if (getGold() >= item.m_iData)
            bValid = true;

    The amount is an int. A human sponsor can therefore intentionally overpay for a war
    declaration up to their actual treasury.

    This is not contrary to UWAI's intended contract. reportSponsoredWar explicitly
    comments:

        "As it is now, paying more than the AI demands makes it a bit
         more reluctant to end the war."

    Large positive overpayment is therefore a supported semantic direction, not an
    invalid-input case.

VALUE PIPELINE
    reportSponsoredWar stores:

        m_aiBounty.set(eTarget,
            tradeValToUtility(kOwner.AI_dealVal(eSponsor, kWeReceive)).round());

    For received TRADE_GOLD, current CvPlayerAI::AI_dealVal applies
    AI_goldTradeValuePercent(), which returns 200:

        iValue += (iGold * 200) / 100;

    UWAI::Player::tradeValToUtility then multiplies by:

        3 * speedFactor /
        (max(10, estimated commerce) + 2 * max(1, estimated production))

    The rate is positive and can be as high as 0.25 when the denominator is at its
    minimum 12. At that upper rate:

        65,536 gold
        -> AI_dealVal 131,072
        -> utility 32,768

    already exceeds SHRT_MAX by one. A slightly larger example such as utility 35,000
    narrows to a negative signed-short value on the target platform.

    Even for stronger economies with a lower conversion rate, there is no short-based
    cap on the legal human payment; a sufficiently large positive treasury/payment can
    reach the same storage boundary.

IMMEDIATE BEHAVIORAL FAILURE
    The wrapped value is consumed immediately by the same function:

        if (m_aiBounty.get(eTarget) > 0)
            m_aeSponsorPerTarget.set(eTarget, eSponsor);
        else
        {
            m_aiBounty.set(eTarget, 0);
            m_aeSponsorPerTarget.set(eTarget, NO_PLAYER);
        }

    Therefore a positive payment that crosses the compact boundary can become negative,
    fail the >0 test and be normalized to:

        bountyAgainst(target)  = 0
        sponsorAgainst(target) = NO_PLAYER

    A smaller overpayment below the boundary is remembered correctly, while a larger
    overpayment can make the AI remember no sponsorship at all.

LIVE CONSEQUENCE
    WarUtilityAspect::HiredHand reads sponsorAgainst/current-target and bountyAgainst
    to keep a paid AI inclined to continue the sponsored war for roughly 20 turns.
    F229 therefore removes the intended obligation immediately at deal creation.

    This is the opposite of the source comment's monotonic intent: overpaying should
    increase reluctance to end the war, not erase the obligation.

ANCESTRY
    The original WarAndPeace/UWAI cache stored sponsorship value in a full int.

    Practical 3183 / 59a3e3190061 ("Rewrite of enum map classes") changes, in
    UWAICache.h:

        CivTeamMap<int> m_aiBounty;
        ->
        CivTeamMap<int,short> m_aiBounty;

    The commit message characterizes the broad XML/serialization changes as
    non-functional and introduces compact enum-map storage generally. No saturation
    policy was added around reportSponsoredWar.

    Current save version 9 serializes the compact short representation, so the narrowed
    value is also the persistent representation once it has survived the immediate
    sign check (values that wrap nonpositive are cleared before that).

REPAIR BOUNDARY
    Keep sponsored-war obligation monotonic under large positive payment.

    Lowest-risk repair while preserving current save width:
      * compute the bounty utility in a full int/scaled temporary;
      * clamp/saturate it explicitly to [0, SHRT_MAX] before assigning m_aiBounty.

    Saturation is semantically reasonable here: HiredHand only needs a bounded positive
    measure of the original obligation, and any payment above the cap can simply mean
    "maximal remembered bounty." It preserves the current version-9 short save format
    and prevents overpayment from becoming less obligation.

    Alternative:
      * restore m_aiBounty to full int;
      * but then bump/adapt UWAICache save serialization so current version-9 short
        saves remain readable and new saves have an unambiguous width.

    Do not enable ENUM_MAP_EXTRA_ASSERTS globally as the sole repair. That would turn a
    deterministic release logic bug into an assertion but would not define safe release
    behavior.

    Do not clamp only after m_aiBounty.set; by then narrowing has already lost the sign
    and magnitude. Clamp the full-width temporary before compact assignment.

NEGATIVE CONTROLS / SCOPE
    * This is not the same as F228. F228 loses a correctly stored bounty when a target
      team identity is absorbed; F229 can erase sponsorship at the moment the deal is
      recorded, without any team merge.
    * The earlier mixed-bundle sponsorship question remains rejected. F229 needs only a
      single ordinary WAR declaration compensated by legal gold.
    * Extremely huge gold can encounter unrelated int-arithmetic limits in other trade
      formulas at much larger amounts. F229's signed-short boundary occurs far earlier
      and is independently sufficient.
    * Compact short storage is not automatically defective. Default-SAS48 plot/city
      geometry gives useful bounds for other fields; each compact field must be judged
      by its producer domain.

C019-WIP17 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP17
CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
NEXT ROOT ID: F230 UNUSED

Resume from F230 unused. Continue the remaining literal low-mention
UWAICache producer/consumer matrix and historical fix checklist. In particular:
    * finish totalAssetScore positivity/denominator assumptions using the 3038/3044
      historical evidence before promoting anything;
    * finish remaining old-version read defaults and compact-map producer bounds;
    * continue City asset/target-value and report-state lifecycle checks.
Clean rejections are the preferred outcome when a contract survives.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C019-WIP18 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this continuation:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip

Prior reconciled physical checkpoint:
    C019-WIP17

New root in this append:
    F230 / provisional KI#553

Next root:
    F231 UNUSED

PURPOSE OF THIS WIP
-------------------
Resume the C019-WIP17 cursor rather than reopening already-audited CvTeamAI.cpp.
The reconciled album has already advanced to queue 031 UWAICache.cpp.

This pass concentrated on:
    * totalAssetScore positivity and denominator assumptions, especially the historical
      3038 / 3044 fixes;
    * remaining old-version UWAICache read defaults and migrations;
    * compact EnumMap producer bounds after F229;
    * City asset / target-value / report-state lifecycle seams.

One new root is promoted below. Several tempting seams survive the audit and are
recorded as clean rejections / non-promotions rather than manufactured findings.


F230 / provisional KI#553
-------------------------
TITLE
    Pre-v4 past-war save conversion scales only teams alive at load; legacy history
    against a dead rival remains at the old granularity and becomes mis-scaled if that
    rival is later revived.

STATUS
    CONFIRMED inherited AdvCiv backward-save / revival compatibility regression.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

CURRENT CODE
    UWAICache::read restores the legacy pre-v9 past-war array first:

        if (iSaveVersion >= 9)
            m_aiPastWarScore.read(pStream);
        else m_aiPastWarScore.readArray<int>(pStream);

    It then performs the pre-v4 granularity migration only for teams that are alive at
    load time:

        if (iSaveVersion < 4)
        {
            for (TeamIter<CIV_ALIVE> it; it.hasNext(); ++it)
                m_aiPastWarScore.multiply(it->getID(), 100);
        }

    The two operations have different domains. readArray<int> restores the serialized
    CivTeamMap slots; the migration subsequently touches only the currently living
    subset.

ORIGINAL COMPATIBILITY CONTRACT
    AdvCivPreSAS commit 1378, 2348464189, introduced save version 4 specifically for
    the increased granularity of pastWarScore.

    Its migration was deliberately slot-complete:

        if(savegameVersion < 4)
            for(int i = 0; i < MAX_CIV_TEAMS; i++)
                pastWarScores[i] *= 100;

    The same change made the live consumer interpret the stored value in hundredths
    and changed new reportWarEnding contributions to roughly +/-100 instead of +/-1.

    Therefore the migration contract was not "convert currently relevant teams."
    It was "convert every serialized legacy team slot."

REGRESSION POINT
    AdvCivPreSAS commit 1818, 854cb8058c,
        "Use AgentIterators in all UWAI classes"
    replaced the all-slot loop with:

        for (TeamIter<CIV_ALIVE> it; it.hasNext(); ++it)
            pastWarScores.multiply(it->getID(), 100);

    That iterator substitution is harmless for many live-game loops, but not for a
    save-format migration whose input contains values for identities that can be dead
    at load time.

WHY THE SKIPPED VALUE IS REALLY STILL DESERIALIZED
    Current EnumMap::readArray reads the full stored key range one value at a time and
    calls setUnsafe for each key. It does not filter the data according to current
    alive status.

    Therefore a dead team's pre-v4 pastWarScore can be present in m_aiPastWarScore
    immediately after deserialization and still escape the required x100 conversion.

SUPPORTED OBSERVABLE STATE
    1. Team A and team B fight and finish a war in a save-format version older than 4.
       A therefore has a nonzero legacy pastWarScore entry for B.
    2. B is later eliminated before that game is saved.
    3. The old save is loaded by current AdvCiv / AdvCiv-SAS.
    4. readArray restores B's serialized legacy score, but TeamIter<CIV_ALIVE> skips B,
       so B's entry remains in old +/-1-scale granularity rather than the new
       +/-100-scale granularity.
    5. B is later revived. AdvCiv explicitly supports player revival; current
       CvPlayer::setAlive(true) reports playerRevived and refreshes UWAI state.
    6. Other players can again evaluate B, and confidenceFromPastWars(B) consumes A's
       still-mis-scaled history.

    Revival is not merely hypothetical dead data. AdvCivPreSAS commit 2010,
    56af3a8dfa, "Fix issues with AI Auto Play ending upon player defeat", added the
    explicit playerRevived lifecycle, and current SAS retains and extends that path.

LIVE CONSEQUENCE
    UWAI::Player::confidenceFromPastWars currently does:

        scaled rPastWarScore = per100(m_cache.pastWarScore(eTarget));
        int iSign = (rPastWarScore < 0 ? -1 : 1);
        scaled r = 1 + iSign * rPastWarScore.abs().sqrt() * fixp(0.15);
        r.clamp(fixp(0.5), fixp(1.5));

    For the simplest old score of +1:
        correct migration: +1 -> +100 -> per100 = 1
        confidence term:   1 + sqrt(1) * 0.15 = 1.15

        skipped migration: +1 remains +1 -> per100 = 0.01
        confidence term:   1 + sqrt(0.01) * 0.15 = 1.015

    Likewise for -1:
        correct: 0.85
        skipped: 0.985

    The stored score is therefore 100x too small. Because confidence uses sqrt, the
    displacement from neutral is 10x too small for the same legacy score before clamp.

WHY THIS IS NOT JUST A DEAD-SLOT CLEANLINESS ISSUE
    If the skipped team remained permanently dead, the stale scale would normally be
    behaviorally irrelevant. The explicit revival lifecycle makes the old key live
    again without rerunning the save-version migration. The defect therefore becomes
    observable precisely in the compatibility state that the current reader claims to
    support.

REPAIR DIRECTION
    For iSaveVersion < 4, multiply every valid CivTeam key represented by the legacy
    serialized map, not TeamIter<CIV_ALIVE>.

    Conceptually:

        if (iSaveVersion < 4)
        {
            FOR_EACH_ENUM(CivTeam)
                m_aiPastWarScore.multiply(eLoopCivTeam, 100);
        }

    Use the project's exact all-CivTeam enum iteration spelling after compile review.
    An explicit 0 .. MAX_CIV_TEAMS-1 loop is also semantically faithful to the original
    migration if that is safer for this old-format branch.

    No save-version bump should be needed: this repairs the read conversion of an
    already-supported old format.

NEGATIVE CONTROLS / SCOPE
    * Current-format saves are unaffected.
    * Pre-v4 entries for teams alive at load are converted correctly.
    * A skipped dead entry matters only if it contains legacy history and later becomes
      behaviorally reachable again, e.g. through revival.
    * This is separate from F228. F228 concerns one-way migration of persistent
      team-keyed history during a Permanent Alliance team absorption. F230 concerns a
      backward-save conversion whose domain was accidentally narrowed.
    * This is also separate from F225 / KI#548, which concerns reconstruction of the
      revived player's own military cache from already-existing units.


REMAINING C019-WIP17 CURSOR -- RESULTS SO FAR
---------------------------------------------

1. totalAssetScore positivity / denominator seam -- NOT PROMOTED
    updateTotalAssetScore starts from a +5 Palace baseline and then adds own-city asset
    scores plus national-wonder contributions.

    Individual per-city AI_assetVal(..., true) scores can indeed be negative because
    maintenance is subtracted. Historical commit 3038, 23545fcd6e,
        "Fix two minor issues with UWAI asset score values"
    explicitly notes that a per-city score can be negative and defensively clamps a
    vassal-loss calculation.

    However, the historical totalAssetScore == 0 observation was subsequently traced
    to an incomplete new-player refresh. Commit 3044, 8a2a75e1e0,
        "Fix two minor inconsistencies in the UWAI cache"
    changed UWAI::processNewPlayerInGame from a partial typical-unit refresh to a full
    cache.update(true), and its comment says the bad total "Had not worked correctly
    when called from UWAI::processNewPlayerInGame. Should be fixed now".

    Risk::evaluate still divides by totalAssetScore + epsilon rather than max(1,...),
    so a genuinely nonpositive aggregate would be dangerous. But this audit has not
    established a supported normal full-refresh state that makes the aggregate
    nonpositive. Mathematical possibility from a negative constituent is not enough.

    RESULT:
        Keep as a robustness seam / historical check, not a new root.

2. remaining compact EnumMap producer bounds -- NO NEW ROOT AFTER F229
    F229 remains the concrete compact-type defect because a legal full-width human
    sponsored-war payment can cross signed-short range before assignment to m_aiBounty.

    The other reviewed compact fields have substantially tighter domains in the
    supported default geometry, or require only an extreme theoretical count with no
    supported reproduction established here.

    In particular, the prior WIP17 bound for the default SAS48 standard map remains
    useful: 198 * 140 = 27,720 plots, below signed-short max 32,767. That gives a real
    default-world upper bound for plot-loss counts and an even tighter practical bound
    for reachable-city counts.

    targetMissions could only overflow a signed short through an extreme number of
    simultaneously counted units / missions; no supported practical state was proved.

    RESULT:
        No F230-style "short is suspicious, therefore bug" finding is manufactured.

3. remaining old-version read defaults -- CLEAN EXCEPT F230
    * pre-v3 m_abCanBeHiredAgainst omission inherits the map's intended true reset from
      clear(), so old saves receive the correct default.
    * pre-v5 m_bFocusOnPeacefulVictory omission inherits false from clear(), matching
      the feature's introduction semantics.
    * pre-v1 relative-navy reconstruction calls updateRelativeNavyPower(), but the
      feature remains explicitly "Not implemented; work in progress" and has no live
      consumer found in the current source.
    * City pre-v4 capital-area fallback from reach-by-land is deliberate compatibility
      behavior; no defect established.
    * pre-v3 / pre-v1 discarded City fields are consumed only to preserve stream
      alignment; no live state was found to depend on them.

4. City construction / target-value initialization -- CLEAN
    City construction computes distance/reachability and asset score before passing
    the cache-city pointer into AI_targetCityValue. The fields needed by the current
    target-value path are therefore initialized first.

    No use-before-initialization root was found in this pass.

5. reportCityCreated human-cache asymmetry -- OBSERVED, NOT PROMOTED YET
    Full updateCities includes a foreign city for a human cache without requiring
    AI_deduceCitySite, while reportCityCreated requires same-team or deducible city and
    has no corresponding human override.

    City transfer also reports destruction of the old city before creation of the
    replacement, so a human cache can transiently lose a newly transferred foreign
    city until a full refresh when it is not deducible.

    However, the report callbacks are explicitly motivated by keeping AI caches aware
    of changes during a human turn, and this pass has not yet proved a live
    post-transfer-before-refresh consumer for the human cache whose behavior is wrong.

    RESULT:
        Preserve as an open cadence/contracts question for F231+, not a finding.

6. F224 refinement -- NO NEW ROOT
    Normal city founding also calls AI_cityCreated for major caches, so the same
    reportCityCreated path that does not recompute m_rTotalAssets is reached outside
    conquest as well.

    This broadens the producer side of F224's stale-total-assets mechanism, but the
    strongest already-proved behavioral reproduction remains the mid-turn
    conquest/diplomacy path recorded under F224. Do not duplicate it as a new root.


C019-WIP18 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP18

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553

NEXT ROOT ID: F231 UNUSED

Resume from F231 unused.

Best next seam:
    * prove or reject the reportCityCreated human-cache asymmetry by tracing a concrete
      live consumer and turn ordering after foreign city creation / transfer;
    * continue remaining City asset / target-value refresh paths and persistent
      report-state lifecycle;
    * if those contracts survive, close the low-mention producer/consumer matrix and
      decide whether UWAICache.cpp is ready for its first-pass durable checkpoint.

Clean rejection remains preferable to inventing another issue.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C019-WIP18 IMPORT
---------------------------------------------
- C019-WIP12 through WIP18 have been imported without rewriting their append-only evidence. The open queue-031 `UWAICache.cpp` audit now reaches F230 / provisional KI#553, with F231 unused.
- Current source and Known Issues implement independently reviewed F213/KI#536, F214/KI#537, F217/KI#540, F218/KI#541 and F219/KI#542. The current staged batch also implements F222/KI#545 and F223/KI#546, restoring their pre-practical-2920 arithmetic; a full Peirce SAS24 autoplay completed successfully.
- F215-F216 / provisional KI#538-KI#539, F220-F221 / provisional KI#543-KI#544 and F224-F229 / provisional KI#547-KI#552 remain pending independent review. F225 may instead become a KI#475.3 decimal follow-up when implemented; preserve its revival-cache proof independently of final public numbering.
- F230 / provisional KI#553 and C019-WIP12's ancient-save note concern backward compatibility only and are intentionally not promoted: AdvCiv-SAS supports only saves created by the current mod version. Current source and Known Issues remain authoritative for implementation state. (GPT-5.6-Sol)


===============================================================================
CHECKPOINT C019-WIP19 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this continuation:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip

Prior reconciled physical checkpoint:
    C019-WIP18

New roots in this append:
    F231 / provisional KI#554
    F232 / provisional KI#555

Same-root refinements in this append:
    F216 / provisional KI#539 -- PA merge target-domain loss for dead/revivable teams
    F229 / provisional KI#552 -- pre-v9 old-save full-int bounty narrows on read

Next root:
    F233 UNUSED

PURPOSE OF THIS WIP
-------------------
Continue the queue-031 UWAICache.cpp audit from F231 unused, with emphasis on the
remaining City/report-state lifecycle, incremental versus full-cache invariants,
backward-save decoding, and low-mention persistent-state producer/consumer paths.

This pass promotes two distinct roots:
    * F231: incremental city callbacks leave deployment geometry and reachable-city
      counts stale for surviving cached foreign cities after the cache owner's own
      city set changes;
    * F232: the first City-subrecord versioning retrofit decodes version-0 raw
      distances with the versioned "-1" transform, shifting every legacy distance.

It also strengthens two existing roots without consuming new root numbers:
    * F216's Permanent Alliance migration needs a full persistent target-team domain,
      not TeamIter<MAJOR_CIV>;
    * F229 also affects pre-v9 saves because old full-width bounty values are narrowed
      into the new short-backed map during deserialization.

Several plausible seams were checked and rejected or left open rather than being
manufactured into findings.


F231 / provisional KI#554
-------------------------
TITLE
    Incremental city create/destroy callbacks update only the changed cache entry;
    deployment geometry and reachable-city counts for surviving foreign cached cities
    remain stale when the cache owner's own city set changes.

STATUS
    CONFIRMED inherited AdvCiv UWAI incremental-cache consistency defect.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

CORE FULL-REFRESH CONTRACT
    A cached foreign City's deployment geometry is not a property of the target city
    alone. City::updateDistance computes it from the CACHE OWNER'S current city set.

    For every relevant owner city it can use:
        * land reachability to the target;
        * coastal access and naval paths;
        * source-city population / production filters;
        * primary-area status;
        * the current capital, which receives extra weight;
        * whether the current capital has a land route to the target.

    It derives and stores:
        m_iDistance
        m_bReachByLand
        m_bCapitalArea

    A full updateCities pass constructs every foreign City from this current geometry.
    add(City&) also increments m_aiReachableCities for every foreign cached City whose
    canReach() is true.

INCREMENTAL CONTRACT IS NARROWER
    Current reportCityCreated:

        if (kCity.getTeam() == TEAMID(m_eOwner) ||
            GET_TEAM(m_eOwner).AI_deduceCitySite(kCity))
        {
            add(kCity);
            sortCitiesByAttackPriority();
        }

    add(CvCity&) creates pathfinders and constructs ONLY kCity.

    Current remove(CvCity const&) removes ONLY the matching cached City. It decrements
    m_aiReachableCities only when the removed city itself is a reachable FOREIGN city.

    Neither callback recomputes m_iDistance / m_bReachByLand / m_bCapitalArea for
    surviving foreign cached cities. Nor does it reconstruct m_aiReachableCities from
    those surviving entries.

WHY THE CACHE OWNER'S OWN CITY CHANGES INVALIDATE SURVIVING TARGETS
    Any own city-set change can change foreign-target deployment geometry.

    Examples:
        * Founding or conquering a coastal city can make previously unreachable
          overseas targets reachable by sea.
        * Losing or razing the only useful coastal launch city can make previously
          reachable overseas targets unreachable.
        * Adding/removing a large productive source city changes the weighted
          deployment-distance average.
        * Losing the capital and selecting a new capital can change
          m_bCapitalArea / canReachByLandFromCapital for many existing targets.
        * The capital itself receives duplicate weight in the distance sample, so a
          capital move can change m_iDistance even when broad reachability remains.

    Thus this is not merely "the new/removed city entry is stale." Existing target
    entries are derived from a source set that has changed underneath them.

CAPITAL-LOSS CALLBACK ORDERING GIVES A CLEAN REPRODUCTION
    CvCity::kill currently notifies every major AI cache first:

        for (PlayerAIIter<MAJOR_CIV> it; it.hasNext(); ++it)
            it->AI_cityKilled(*this);

    The owner's city object is deleted later:

        kOwner.deleteCity(getID());

    Only after that deletion does the old-capital branch call:

        kOwner.findNewCapital();

    Therefore reportCityDestroyed is too early even if it wanted to recompute the
    owner's deployment geometry: at callback time the old city still exists and an old
    capital is still the capital. No second UWAI geometry refresh follows
    findNewCapital.

    City acquisition later reports the newly created replacement through
    AI_cityCreated, but reportCityCreated again constructs only that one city and sorts.
    It does not rebuild the surviving foreign targets from the new owner-city set.

NORMAL FOUNDING HAS THE SAME STRUCTURAL ISSUE
    CvPlayer::found also notifies major AI caches through AI_cityCreated after creating
    the new city.

    For the founding player's own cache, that newly existing source city can alter
    deployment distance/reachability to all previously cached foreign cities, yet only
    the founded city's own cache record is constructed.

    So the root is broader than conquest/capital loss. Capital capture is simply the
    clearest high-impact demonstration.

STALE m_aiReachableCities IS LIVE STATE, NOT DISPLAY-ONLY METADATA
    m_aiReachableCities is reconstructed by full update logic from each cached foreign
    city's canReach state, but incremental own-city changes do not rebuild it.

    Multiple live consumers use it as a hard gate, including:
        * UWAI::Team::canReach;
        * UWAI::Player::canReach;
        * WarUtilityAspect::GreedForCash;
        * WarUtilityAspect::TacticalSituation::evalOperational;
        * WarUtilityAspect::LoveOfPeace;
        * loss / elimination war-utility logic;
        * CvPlayerAI diplomacy demand/tribute reachability checks when UWAI is active.

    Therefore, for example, loss of the only practical coastal launch city can leave
    the AI behaving as though an overseas rival still has reachable cities until the
    next full owner-cache update. The inverse can happen after gaining a launch city.

STALE PER-CITY GEOMETRY ALSO HAS DIRECT WAR-MODELING CONSUMERS
    InvasionGraph deliberately works with the analyzed player's own cache:

        m_kCache(GET_PLAYER(m_ePlayer).uwai().getCache())

    Its target selection stops when cached cities are unreachable or classified as
    own-team:

        if (!kCacheCity.canReach() || kCacheCity.isOwnTeamCity())
            break;

    Its land-reach calculations directly consult:
        canReachByLandFromCapital()
        canReachByLand()
        getDistance()

    ArmamentForecast likewise consults canReachByLandFromCapital() and getDistance()
    when deciding naval versus land armament.

    Thus stale fields influence invasion topology, deployment timing and armament
    forecasts independently of the aggregate reachable-city count.

SUPPORTED SAME-TURN BEHAVIORAL REPRODUCTION
    A strong ordering is:

        1. During the human player's turn, a human captures/razes a victim AI's sole
           useful coastal source city or its capital.
        2. AI_cityKilled causes the victim cache to remove the changed city, but all
           surviving foreign cached targets retain pre-loss geometry.
        3. For a capital, findNewCapital runs only after that callback.
        4. The human then negotiates peace during the same turn, before the victim's
           next turnPre/full cache update.
        5. CvPlayerAI::AI_dealVal for an at-war human deal reaches
           UWAI::Team::endWarVal / reluctanceToPeace.
        6. WarEvaluator -> MilitaryAnalyst -> InvasionGraph consumes the victim's
           stale deployment geometry and/or reachable-city state.

    This establishes a live consumer between mutation and the next full refresh.

HISTORICAL ANCESTRY
    AdvCivPreSAS commit 2605, f788c46a21,
        "Various optimizations for UWAICache"
    introduced the split city-killed / city-created callback architecture.

    Its own comment states:

        "City ownership changes result in the creation of a new city.
         It's important that the cache keep up with ownership changes
         so that the AI is aware of conquests made throughout a human turn."

    That goal explains the incremental updates, but the implementation updates the
    changed target entry rather than all cache entries whose deployment geometry is
    derived from the cache owner's mutable city set.

    AdvCivPreSAS commit 2606, 1fdeb92b6f,
        "Minor tweaks to UWAI distance computation"
    added the human full-refresh city-location override discussed separately below;
    it did not add geometry reconstruction to incremental city callbacks.

BASELINE COMPARISON
    Current base AdvCiv 1.14 retains the same incremental add/remove behavior.
    K-Mod and Civ4CE do not carry this UWAI cache layer, so they do not provide a
    direct equivalent implementation to compare.

DISTINCTION FROM EXISTING ROOTS
    F224:
        stale total-asset denominator after create/destroy.
        F231 concerns per-target path/deployment geometry and aggregate reachability.

    F227:
        stale stateful attack ordering / area-clustering after remove-only destruction.
        F231 persists even if the vector is perfectly resorted because the underlying
        surviving target geometry itself is outdated.

    F226:
        path-choice logic inside one City::updateDistance calculation.
        F231 concerns failing to rerun that calculation after its source-city inputs
        change.

REPAIR DIRECTION
    The important ordering requirement is that surviving target geometry must be
    recomputed AFTER the owner-city mutation is stable.

    In particular, recomputing inside the existing AI_cityKilled callback is
    insufficient because CvCity::kill invokes it before deleteCity and, for a capital,
    before findNewCapital.

    A robust design could introduce a post-city-set-change cache refresh/helper that:
        * rebuilds or updates pathfinders for foreign target teams;
        * reruns City::updateDistance for surviving foreign cached Cities belonging to
          the player whose own source-city set changed;
        * reconstructs m_aiReachableCities from the resulting canReach values;
        * refreshes any derivative target ordering/value state whose inputs changed;
        * resorts after those values are stable.

    A whole cache.update() would be simple but may be unnecessarily expensive and may
    touch unrelated cadence/persistent state. A targeted geometry/reachability helper
    looks preferable if it can be kept lifecycle-safe.

    The destruction path needs a post-delete/post-findNewCapital hook or equivalent.
    The creation path already fires after the new city exists, so it can invoke the
    targeted owner-geometry refresh from there.

    Compile/runtime review is needed before choosing exact hook placement.


F232 / provisional KI#555
-------------------------
TITLE
    Version-0 UWAICache::City save records wrote raw distance, but the versioned reader
    unconditionally subtracts 1; every legacy distance is shifted and distance 1 is
    misclassified as an own-team city.

STATUS
    CONFIRMED inherited AdvCiv backward-save decoder defect.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

CURRENT FORMAT
    Current City::write folds the City-subrecord version into distance:

        int iDistance = ::range(m_iDistance, -1, 9999);
        pStream->Write(iDistance + 1 + 10000 * iSaveVersion);

    Current iSaveVersion is 4.

    Current City::read always decodes:

        pStream->Read(&iTmp);
        iSaveVersion = iTmp / 10000;
        m_iDistance = (iTmp % 10000) - 1;

ORIGINAL VERSION-0 FORMAT
    The history is explicit.

    Before City subrecord versioning, the writer and reader simply did:

        stream->Write(distance);
        ...
        stream->Read(&distance);

    AdvCivPreSAS commit 1287, ad5faf8aa2,
        "Test version incl. DLL and manual; needs some more work."
    introduced version 1 and changed writing to:

        stream->Write(distance + 1 + 10000 * savegameVersion);

    But its new reader immediately changed to:

        savegameVersion = tmp / 10000;
        distance = (tmp % 10000) - 1;

    with no version-0 branch.

    Therefore the new encoding transform was retroactively applied to values that had
    been stored in the old raw encoding.

EXACT LEGACY SHIFT
    For a version-0 record, abs(distance) values are below 10000, so:

        iSaveVersion = rawDistance / 10000 = 0
        decodedDistance = rawDistance - 1

    Therefore:
        old -1 -> -2
        old  0 -> -1
        old  1 ->  0
        old  N -> N - 1

    The current writer's assertion message even acknowledges one symptom:

        "-2 is OK if loaded from an old savegame version"

    but the compatibility shift is not confined to the -1 sentinel. Every legacy
    distance is displaced.

BEHAVIORAL CONSEQUENCE: FOREIGN DISTANCE 1 BECOMES "OWN TEAM"
    Current:

        bool isOwnTeamCity() const { return (m_iDistance == 0); }

    A perfectly ordinary legacy foreign target with deployment distance 1 therefore
    loads as distance 0 and is falsely classified as an own-team city.

    attackPriority returns the special own-team priority:

        if (m_iDistance < 0)
            return -1;
        else if (isOwnTeamCity())
            return -2;

    Current InvasionGraph target iteration checks the same classification and can break
    upon encountering an own-team/unreachable cached city.

    Thus this is not merely a one-turn numeric underestimate: a foreign target can
    cross a semantic sentinel boundary and disrupt target enumeration/modeling.

OTHER POSITIVE DISTANCES ARE ALSO TOO SMALL
    Legacy distance N > 1 becomes N-1. Consumers that directly use getDistance for
    deployment timing, land-reach thresholds, invasion modeling and armament
    forecasting therefore receive systematically understated values until refresh.

LOAD-TIME CACHE RELINK DOES NOT RECOMPUTE DISTANCE
    After UWAICache has been deserialized, CvPlayerAI::read eventually calls:

        kPlayer.uwai().getCache().cacheCitiesAfterRead();

    cacheCitiesAfterRead only relinks each cached City to its CvCity:

        for (...)
            cityAt(i).cacheCvCity();

    It does not rerun updateDistance, rebuild reachable-city counts or resort by newly
    recomputed geometry.

    CvGameAI::read likewise does not perform an immediate full UWAI cache update.

    The normal complete recomputation occurs at a later UWAI player turnPre/cache
    update. Therefore the bad decoded distance is genuinely live between load and that
    refresh.

SUPPORTED OBSERVABILITY
    A manual save can be loaded into an active turn. Diplomacy and UWAI war-evaluation
    entry points can be invoked before the affected cache owner's next turnPre.

    In particular, the distance-1 -> 0 semantic collision can be consumed by
    InvasionGraph / war-evaluation machinery in that interval.

HISTORICAL / BASELINE COMPARISON
    The defect originates in the very first City-subrecord versioning retrofit and is
    retained in current base AdvCiv 1.14 and current SAS.

    This is an AdvCiv backward-save issue; K-Mod/Civ4CE do not provide the same UWAI
    City-subrecord format.

REPAIR DIRECTION
    Decode the raw version-0 representation separately:

        pStream->Read(&iTmp);
        iSaveVersion = iTmp / 10000;
        if (iSaveVersion <= 0)
            m_iDistance = iTmp; // legacy record stored raw distance
        else
            m_iDistance = (iTmp % 10000) - 1;

    Keep the existing version-gated field consumption after distance:
        * capitalArea for v4+;
        * discarded reachBySea for versions <3;
        * discarded canDeduce for version 1.

    Verify signed integer division/modulo behavior for the legacy -1 sentinel under the
    VC2003 compiler/runtime during implementation review. The intended semantic rule is
    still simple: version-0 is raw; versioned records use the +1/+10000 packing.

    No new save-version bump should be required because this is a read-side correction
    for an already-supported historical format.

DISTINCTION FROM F230
    F230 concerns a top-level pre-v4 pastWarScore scale migration whose iteration
    domain was narrowed to alive teams.

    F232 concerns the nested City record's original encoding transition and an
    unconditional arithmetic transform. They are independent backward-save bugs.


F216 / provisional KI#539 -- SAME-ROOT REFINEMENT
--------------------------------------------------
Permanent Alliance cache merge also narrows the persistent TARGET-team domain to
currently alive major teams.

Current UWAICache::addTeam(PlayerTypes eOtherLeader) merges persistent values with:

    for (TeamIter<MAJOR_CIV> itTeam; itTeam.hasNext(); ++itTeam)
    {
        TeamTypes eTeam = itTeam->getID();
        m_aiPastWarScore.add(eTeam, kOther.m_aiPastWarScore.get(eTeam));
        ...
    }

Agent predicate/iterator definitions confirm that MAJOR_CIV implies ALIVE and maps to
the major-alive collection.

Persistent war history and related team-keyed state can nevertheless exist for an
eliminated major rival, and AdvCiv supports revival. If such a rival is dead when the
Permanent Alliance is formed, the absorbed member's persistent history versus that
rival is not merged into the surviving cache. A later revival makes the omitted key
behaviorally relevant again.

This belongs under F216 rather than a new root because F216 already establishes that
addTeam's Permanent Alliance migration is structurally using the wrong iteration
contract.

A correct migration needs BOTH:
    1. the pre-merge recipient membership boundary, so the absorbed leader does not
       merge itself into itself and double its score (original F216);
    2. a full persistent CivTeam target-key domain, not an alive-only iterator, so
       historical state against dead/revivable rivals is not discarded.

F228 remains distinct: it concerns outsider caches whose stored references point to
the absorbed team. This F216 refinement concerns state copied FROM the absorbed
member into the surviving alliance cache.


F229 / provisional KI#552 -- BACKWARD-SAVE REFINEMENT
------------------------------------------------------
The bounty int-to-short regression also affects pre-v9 save migration.

AdvCivPreSAS commit 3183, 59a3e31900,
    "Rewrite of enum map classes"
did two relevant things together:
    * changed m_aiBounty from a full int map to CivTeamMap<int,short>;
    * bumped the UWAICache save version to 9 for the new enum-map save behavior.

Current backward reader for old saves uses:

    if (iSaveVersion >= 9)
        m_aiBounty.read(pStream);
    else m_aiBounty.readArray<int>(pStream);

The old serialized value is therefore read as full-width int, but assignment into the
current map ultimately compacts it into the short backing type.

With the normal non-extra-assert compact path, a legal old bounty above signed-short
range can wrap rather than being range-preserved.

This is especially damaging because the sponsor is deserialized separately from the
old full-width sponsor map. A loaded cache can therefore contain:

    sponsorAgainst(target) != NO_PLAYER
    bountyAgainst(target) <= 0

Current HiredHand logic asserts the inverse relationship:

    FAssert((eSponsor == NO_PLAYER) == (iOriginalUtility <= 0));

and in release behavior only honors the sponsored-war obligation when the bounty is
positive.

So the backward-save manifestation can:
    * trip the sponsor/bounty invariant in debug;
    * silently erase the practical paid-war obligation in release.

Do not allocate a new root. This is the same representability defect as F229, reached
through the old-save reader rather than reportSponsoredWar's live assignment.

Repair choices remain:
    * restore a full-width bounty representation; or
    * for old-save migration, read to temporary full-width storage and explicitly
      saturate/range-check before assigning compact storage, while keeping the sponsor
      invariant consistent.


OPEN QUESTION -- HUMAN FULL-REFRESH / INCREMENTAL-CREATE ASYMMETRY
------------------------------------------------------------------
NOT PROMOTED AS F233 IN THIS WIP.

Full updateCities deliberately gives a human cache omniscient city-location treatment:

    if (same team ||
        bHuman ||
        kCacheTeam.AI_deduceCitySite(*pCity))
    {
        add(...);
    }

Historical commit 2606 explains the intent:
    "Assume that human can locate all cities of known civs"

Incremental reportCityCreated does NOT carry the bHuman override. It adds a foreign
new city only if the cache owner's team can deduce the site.

Therefore a newly founded unseen foreign city can be absent from the human player's
cache until the next human full refresh even though the full-refresh contract says it
belongs there.

There is a plausible cross-cache consequence because InvasionGraph deliberately uses
the analyzed player's own cache. An AI evaluating a human's possible operations can
therefore model the human from a cache that temporarily omits a city the full-refresh
policy would include.

However:
    * this is a narrow cadence/fairness asymmetry;
    * the callback comment is primarily concerned with keeping AI aware of HUMAN
      conquests during a human turn;
    * no sufficiently strong direct behavioral reproduction distinct from F231 has yet
      been established.

Keep this as the leading F233 candidate, but do not promote it merely from mismatch.


OTHER CHECKS / NEGATIVE CONTROLS THIS PASS
------------------------------------------

1. updateTraits offensive/defensive else-if -- CURRENT DATA DOES NOT TRIGGER IT
    The code marks a trait as offensive when a trait-granted free promotion has
    positive generic combat, otherwise checks city defense through else-if.

    A promotion with both positive combat and city defense would therefore set the
    offensive flag but miss the defensive flag.

    Current SAS trait/free-promotion XML was checked. No currently trait-granted free
    promotion has both positive generic combat and positive city defense.

    RESULT:
        Generic mod-robustness smell only. No current-data root.

2. m_abPlayerLocated -- DEAD MEMBER / CLEANUP ONLY
    No live uses were found.

3. updateRelativeNavyPower -- EXPLICITLY UNIMPLEMENTED / NO LIVE CONSUMER
    It remains a work-in-progress field and does not support a current root.

4. updateAdjacentLand / updateLostTilesAtWar / longTermPower / updateVassalScore
    Reviewed in the remaining low-mention sweep. No supported new defect established.

5. reportWarEnding reparations direction
    Caller supplies received/given reparations to the two team caches in the intended
    direction. The function's if/else-if is not a lost-state bug.

6. City::updateDistance capital pointer
    Normal cache lifecycle structurally gives an owner with cities a capital. The
    destruction callback occurs before old-capital deletion, while creation occurs
    after city creation. No supported null-capital crash state was established here.

7. updateCanBeHiredAgainst
    Reviewed caller/leader-cache usage; no new root established.

8. reportWarEnding score accumulation
    Persistent score is full-width integer. No realistic supported overflow root was
    established.

9. cvCityById
    Used for simulated city-loss IDs while underlying actual cities remain present.
    No null/stale identity root established in the inspected consumers.


HISTORICAL "KNOWN WEAK SPOTS" SWEEP
-----------------------------------
The audit also checked older commits whose titles explicitly mention UWAI crashes,
distance corrections or cache inconsistencies. These are valuable negative controls
because several queue-031 findings arose when later refactors narrowed an older
compatibility contract.

Reviewed examples:

    AdvCivPreSAS 1909, 8095a0a0a3
        "Fix array index out of bounds in UWAI code"
        The old owner-change vector implementation could leave null slots. Current
        erase-based city vector does not retain that specific problem.

    AdvCivPreSAS 2706, a42eb4f3a6
        "Fix minor issue with UWAI in team games"
        Corrected human detection from team-level to cache-owner-player-level.
        Current code retains the player-specific behavior.

    AdvCivPreSAS 2751, 674f019680
        "UWAI: Fix crash in games that start with free cities"
        Added first-turn history availability guarding. Current code retains and later
        strengthens that logic for new players.

    AdvCivPreSAS 2921, 79134e4532
        "Correct mix-ups in revised UWAI code"
        Corrected several revised-UWAI mistakes including teamThreat arithmetic and
        naval/path details. Current source retains the inspected corrections.

    AdvCivPreSAS 2929, 6763e87bca
        "Fix some issues with calc of UWAI deployment distances"

    AdvCivPreSAS 2931, 4a10bee17a
        "Amend UWAI deployment distances fix"
        Current code retains the historical path direction/sentinel corrections.
        The still-live path-choice issue from this area is already F226; no duplicate
        root is created here.

    AdvCivPreSAS 3044, 8a2a75e1e0
        "Fix two minor inconsistencies in the UWAI cache"
        Current processNewPlayerInGame retains the full cache.update(true) correction
        that fixed the historical totalAssetScore==0 observation.

RESULT
    No additional regression was promoted from this historical sweep.
    This increases confidence that F231/F232 are distinct surviving gaps rather than
    duplicates of already-fixed historical bugs.


C019-WIP19 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP19

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553
    F231 / provisional KI#554
    F232 / provisional KI#555

SAME-ROOT REFINEMENTS ADDED THIS WIP:
    F216:
        addTeam target-domain should include persistent dead/revivable CivTeam keys.
    F229:
        pre-v9 full-int bounty can narrow/wrap during old-save deserialization.

NEXT ROOT ID: F233 UNUSED

Resume from F233 unused.

Best next seams:
    * prove or reject the human full-refresh / reportCityCreated asymmetry with a
      concrete cross-cache consumer and turn-order reproduction;
    * finish the remaining low-mention producer/consumer matrix for persistent
      report-state and City cache fields;
    * re-check whether any city-set mutation paths bypass both incremental callbacks
      and full refresh;
    * inspect any remaining save-version transitions for the same "new representation
      decoded over old domain" pattern exposed by F230/F232/F229 refinement;
    * if those contracts survive, decide whether UWAICache.cpp is ready for its
      first-pass durable close and then move to the next priority queue file.

Clean rejection remains preferable to inventing F233.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C019-WIP20 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this continuation:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip

Prior reconciled physical checkpoint:
    C019-WIP19

New roots in this append:
    F233 / provisional KI#556
    F234 / provisional KI#557

Next root:
    F235 UNUSED

PURPOSE OF THIS WIP
-------------------
Continue the queue-031 UWAICache.cpp audit from F233 unused.

This pass first resolved the full-refresh / incremental human-city membership mismatch,
then continued through remaining cache construction, save-layout, setup and callback
invariants. A second root emerged from the interaction between the older
setFoundedFirstCity full refresh and the later reportCityCreated callback.

The two promoted roots are behaviorally distinct:
    * F233 is a missing foreign-city entry in HUMAN caches because incremental creation
      does not mirror the full-refresh known-city policy.
    * F234 is a duplicate same-city entry in the FIRST-CITY owner's cache because a
      full refresh and an incremental add both insert the same plot. The vector/map
      ownership split can later become a real dangling-pointer / use-after-free path.

No F235 is manufactured. The Advanced Start multi-city gap and branch-layout concerns
are recorded below as non-promotions / robustness seams.


F233 / provisional KI#556
-------------------------
TITLE
    Incremental reportCityCreated omits the full-refresh human known-city rule; later
    AI military analysis can model a human from a stale human cache that is missing a
    newly founded foreign city.

STATUS
    CONFIRMED inherited AdvCiv UWAI incremental-cache policy mismatch.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

FULL-REFRESH POLICY
    UWAICache::update first iterates only major teams KNOWN_TO the cache owner's team.
    For each such team, updateCities applies:

        if (eTeam == kCacheTeam.getID() ||
            // Assume that human can locate all cities of known civs
            bHuman || kCacheTeam.AI_deduceCitySite(*pCity))
        {
            add(...);
        }

    Therefore the intended human policy is:
        * same-team cities are included;
        * for a FOREIGN TEAM ALREADY KNOWN to the human, every city is included even
          when AI_deduceCitySite would currently fail.

    The "known civ" scope is important: bHuman is not global omniscience over unmet
    teams. update() reaches updateCities only through TeamIter<MAJOR_CIV,KNOWN_TO>.

INCREMENTAL POLICY
    reportCityCreated instead uses:

        if (kCity.getTeam() == TEAMID(m_eOwner) ||
            GET_TEAM(m_eOwner).AI_deduceCitySite(kCity))
        {
            add(kCity);
            sortCitiesByAttackPriority();
        }

    There is no human override corresponding to updateCities.

    Thus a newly founded city of an already-known foreign civ can belong in the human
    cache under the full-refresh contract but be skipped by the incremental callback.

HISTORICAL ORIGIN IS A TWO-COMMIT CONTRACT SPLIT
    AdvCivPreSAS commit 2605, f788c46a21,
        "Various optimizations for UWAICache"
    introduced the current reportCityCreated-style callback with:
        same team OR AI_deduceCitySite
    and no human override.

    The very next commit, AdvCivPreSAS 2606, 1fdeb92b6f,
        "Minor tweaks to UWAI distance computation"
    changed full updateCities from:
        same team OR AI_deduceCitySite
    to:
        same team OR human OR AI_deduceCitySite
    with the explicit comment:
        "Assume that human can locate all cities of known civs"

    It also removed the older human-location check from City::updateDistance.

    reportCityCreated was not updated to match the new membership policy.

WHY A STALE HUMAN CACHE IS USED BY AI CODE
    InvasionGraph::Node deliberately models each player from THAT PLAYER'S cache:

        m_kCache(GET_PLAYER(m_ePlayer).uwai().getCache())

    while retaining the evaluating agent's own cache separately:

        m_kAgentCache(GET_PLAYER(m_eAgent).uwai().getCache())

    The source comment explicitly says:
        "agent cheats by using info from other players' caches"

    and the agent cache is then used to sanitize target cities that the evaluating AI
    itself should not know about.

    findBestTarget iterates m_kCache.cityAt(i). Therefore a city absent from the
    modeled player's cache cannot be selected/modelled at all, even if the evaluating
    agent's own fresh cache knows that city.

SUPPORTED TURN-ORDER REPRODUCTION
    Choose three major players:
        H = human being modeled
        A = known foreign rival of H
        B = later-turn AI doing military analysis

    1. H's turn begins. H's UWAICache receives its normal full update. At this time
       the cache obeys the intended human policy: all cities of known civs are eligible
       independent of AI_deduceCitySite.

    2. Later in the same round, A founds a new city C.

    3. CvPlayer::found broadcasts AI_cityCreated(C) to the major AI-player objects.

    4. H knows A, but choose C so that H's team cannot currently deduce the exact site.
       H's reportCityCreated therefore skips C even though the next full updateCities
       would include it solely because H is human.

    5. Still later in the round, B's turn starts. B gets its own normal full cache
       refresh. Choose an ordinary state where B can see/deduce C, so B's own cache
       correctly contains C.

    6. B performs a military analysis that includes H as a modeled belligerent
       (for example H and A are at war).

    7. The InvasionGraph node for H binds m_kCache to H's stale cache and
       m_kAgentCache to B's fresh cache.

    8. C is absent from H's target list before the agent-cache visibility sanitizer
       even gets a chance to approve it.

    The mismatch can therefore change B's model of which city H would target, the
    invasion topology and downstream war-utility estimates.

    A clean illustrative state is for C to be H's only / clearly best newly reachable
    city of A. The omission then changes modeled target selection directly.

WHY THIS IS DISTINCT FROM F231
    F231:
        entries for surviving foreign cities exist but their distance/reachability
        geometry is stale after the cache owner's own city set changes.

    F233:
        the newly created foreign city entry is entirely ABSENT from the human cache
        because full refresh and incremental creation disagree about membership.

    Correcting F231's geometry recomputation does not create the missing F233 entry.

BASELINE
    Current base AdvCiv 1.14 retains the same updateCities/reportCityCreated mismatch.
    K-Mod and Civ4CE do not carry this UWAI cache architecture.

REPAIR DIRECTION
    reportCityCreated should mirror the membership contract of full updateCities, not
    merely append `|| isHuman()` to the current condition without regard to contact.

    Conceptually:
        * same-team city: eligible;
        * foreign city:
            - foreign team must be known/met by the cache owner's team, matching the
              TeamIter<...,KNOWN_TO> outer scope of full update;
            - then human OR AI_deduceCitySite decides location eligibility.

    In other words, preserve BOTH parts of the full-refresh policy:
        KNOWN foreign team
        AND (human OR deducible city site).

    Use TEAMID(m_eOwner) / the project's type-safe owner-team form in the repair.
    Do not copy the current `GET_TEAM(m_eOwner)` PlayerTypes-vs-TeamTypes misuse;
    that broad type-domain family is already tracked separately under F113 / KI#436.

    The eventual city-callback repair should also be reconciled with F224, F231,
    F227 and F234 so that one lifecycle helper does not fix membership while leaving
    total assets, surviving-target geometry, ordering or uniqueness stale.


F234 / provisional KI#557
-------------------------
TITLE
    First-city full refresh plus reportCityCreated inserts the same city twice; the
    city vector and plot-ID map diverge, and a same-round later capture can leave a
    dangling cached City that sortCitiesByAttackPriority dereferences.

STATUS
    CONFIRMED inherited AdvCiv cache-uniqueness / memory-safety regression.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

THE TWO INDEPENDENT INSERTION PATHS
    CvPlayer::initCity finishes CvCity::init and then calls:

        setFoundedFirstCity(true);

    The advc.104 comment says this was deliberately moved out of CvCity::init so that
    the new city is already fully initialized.

    On the false -> true transition, setFoundedFirstCity performs:

        if (!isBarbarian() && getParent() == NO_PLAYER &&
            getUWAI().isEnabled() && GC.getGame().isFinalInitialized())
        {
            AI().uwai().getCache().update();
        }

    The comment explains why:
        "So that rivals (with higher ids) can immediately evaluate war plans
         against this player"

    A full update clears the city cache and updateCities inserts the owner's newly
    created city.

    CvPlayer::found then continues its post-init founding work. For an ordinary
    non-Advanced-Start founding it later broadcasts:

        for (PlayerAIIter<MAJOR_CIV> it; it.hasNext(); ++it)
            it->AI_cityCreated(*pCity);

    This includes the founding player's own AI object/cache.

    reportCityCreated sees a same-team city and calls add(kCity) again.

WHY NORMAL FIRST-TURN FOUNDING IS INSIDE THE CONDITION
    CvGame setup calls setInitialItems and only afterward calls:

        setFinalInitialized(true);

    Standard start settlers therefore found their first playable city after final
    initialization is true. This is not limited to a WorldBuilder or load-only path.

    The same structural duplication can also occur whenever an alive cityless player
    acquires its first city during a finalized game: acquireCity calls initCity
    (triggering the first-city full update) and later broadcasts AI_cityCreated for the
    replacement city.

ADVANCED START IS AN EXCEPTION, NOT A DEFENSE
    CvPlayer::found intentionally skips the AI_cityCreated broadcast while
    getAdvancedStartPoints() >= 0. Therefore this exact duplicate insertion is avoided
    for Advanced Start placement. That does not protect ordinary first-city founding.

ADD DOES NOT ENFORCE UNIQUE PLOT IDS
    UWAICache::add(City&) does:

        m_cityList.push_back(&kCacheCity);
        m_cityMap.insert(std::make_pair(kCacheCity.getID(), &kCacheCity));

    std::map::insert does not replace an existing key.

    Its return value is ignored and there is no assertion that insertion succeeded.

    After the second insertion:
        m_cityList contains TWO separately allocated UWAICache::City objects for the
        same plot;
        m_cityMap still points to the FIRST object only.

    numCities() therefore sees a duplicate that lookupCity(plot) cannot expose.

THE TWO CACHED COPIES CAN ALSO REPRESENT DIFFERENT SNAPSHOTS
    setFoundedFirstCity's full update occurs inside initCity.

    CvPlayer::found continues afterward, including such work as adding free
    start-era buildings before the AI_cityCreated broadcast.

    Therefore the first map-owned cache record can be an earlier snapshot while the
    later vector-only duplicate is constructed from the more-final city state.

    This further violates the intended "one cache City per plot" identity invariant.
    F224 already covers stale total-asset accounting from incremental city lifecycle;
    F234 is not allocated merely for that stale-value consequence.

REMOVE DELETES ONLY THE MAP-OWNED COPY
    UWAICache::remove finds a cached City exclusively through m_cityMap:

        posMap = m_cityMap.find(kCity.plotNum());
        pCacheCity = posMap->second;

    It erases that map entry, finds exactly that pointer in m_cityList, deletes it and
    erases that one vector element.

    If F234's duplicate exists, the second vector-only City survives.

    UWAICache.h states the intended destruction contract directly:

        // No checks upfront; make sure we're not keeping any dangling pointer.
        void reportCityDestroyed(...) { remove(kCity); }

    F234 violates that contract.

CONCRETE DANGLING-POINTER / USE-AFTER-FREE REPRODUCTION
    A supported sequential state can be constructed without corrupt data:

    1. Player P begins a finalized normal game/turn with no city and at least two ways
       to obtain cities in the same turn (e.g. two settlers, or founding plus another
       ordinary acquisition).

    2. P founds first city C1.
       setFoundedFirstCity(true) full-refreshes P's cache and inserts C1 once.
       found -> AI_cityCreated(C1) inserts C1 a second time.
       The vector now contains duplicate wrappers for C1; the map owns only the first.

    3. P founds/acquires another city C2 before P's next turnPre/full cache refresh.
       P can therefore survive the later loss of C1.

    4. A later-turn rival Q captures C1 during the same round.

    5. The old CvCity::kill broadcasts AI_cityKilled before deleting the old CvCity.
       P's reportCityDestroyed/remove removes ONLY the map-owned first C1 wrapper.
       The vector-only duplicate remains.

    6. CvCity::kill later executes kOwner.deleteCity(getID()), destroying the old
       CvCity object. The surviving duplicate UWAICache::City now holds m_pCity
       pointing at freed city storage.

    7. acquireCity creates Q's replacement city on the same plot and eventually
       broadcasts AI_cityCreated(kNewCity) to all major caches.

    8. P still owns C2, so reportCityCreated does not take the zero-city early return.
       The captured plot was revealed to P as its own former city, so
       AI_deduceCitySite succeeds for the replacement city. P adds the new foreign
       cache record and immediately calls sortCitiesByAttackPriority().

    9. sortCitiesByAttackPriority iterates every vector entry. Its area-clustering
       expression calls:

           m_cityList[j]->city().isArea(*pPrevArea)

       before checking whether the priority is positive.

       The surviving duplicate's city() dereferences the freed m_pCity.

    Result:
        a direct use-after-free / possible crash during the capture callback.

    Even when freed storage happens still to look readable in release, behavior is
    undefined and the vector/map invariants remain corrupted until a full cache clear.

WHY A SECOND CITY MATTERS IN THE CLEAN CRASH REPRO
    If C1 is P's only city, reportCityCreated for P returns when getNumCities() <= 0,
    and acquireCity later eliminates/uninitializes P. That can remove the stale wrapper
    before this particular sort path dereferences it.

    Keeping C2 makes P remain a live cache owner, so the replacement-city callback
    proceeds and provides the immediate dereference.

    The requirement is narrow but fully supported. Nothing in ordinary game rules or
    cache code forbids obtaining two cities in one turn before the next UWAI refresh.

HISTORICAL REGRESSION CHAIN
    AdvCivPreSAS commit 1494, 59ebdf364a, moved setFoundedFirstCity to
    CvPlayer::initCity and added the full UWAI cache update specifically so later-ID
    rivals could immediately evaluate the newly established player.

    AdvCivPreSAS commit 2605, f788c46a21,
        "Various optimizations for UWAICache"
    later introduced the generalized AI_cityCreated/reportCityCreated callback and
    added the ordinary-founding broadcast in CvPlayer::found.

    The older first-city full refresh remained in place.

    Thus each mechanism is locally understandable, but their composition inserts the
    first city twice into the owner's cache.

BASELINE
    Current base AdvCiv 1.14 retains:
        * setFoundedFirstCity -> cache.update();
        * ordinary found/acquire -> AI_cityCreated broadcast;
        * reportCityCreated -> unconditional add for eligible city;
        * add -> vector push plus unchecked std::map::insert;
        * remove -> delete only the one pointer reached through the map.

    This is therefore inherited rather than SAS-created.

REPAIR DIRECTION
    Enforce the one-plot/one-cache-City invariant centrally.

    Do not merely hide this case by skipping the cache owner in the founding broadcast:
    the later callback can contain a more-final city snapshot than the early
    setFoundedFirstCity refresh, and incremental creation is needed for other caches.

    Safer architecture:
        * before incrementally inserting a city, detect an existing entry for the same
          plot;
        * explicitly replace/refresh the existing cache record exactly once, keeping
          vector and map ownership synchronized;
        * correctly undo/reapply reachable-city accounting when replacing a foreign
          entry;
        * assert successful unique insertion in debug builds so future duplicate paths
          cannot silently split vector/map ownership;
        * then perform the broader F224/F231/F227 refresh work in a lifecycle-safe
          order.

    A central helper that owns add/replace/remove invariants is preferable to fixing
    only the current first-city caller.

    Because F231 requires post-owner-city-set geometry rebuilding and F224 requires
    aggregate asset refresh, implementation review should solve these city lifecycle
    roots together rather than stacking independent partial callback patches.

DISTINCTION FROM F227
    F227 is stale ordering after a legitimate cached city is removed.

    F234 corrupts cache identity ownership itself: two wrappers exist for one plot,
    only one is represented by the map, and removal can leave a dangling wrapper.
    Resorting after remove cannot safely fix F234 because sorting may be the operation
    that dereferences the dangling copy.


ADVANCED START MULTI-CITY CALLBACK GAP -- NOT PROMOTED AS F235
--------------------------------------------------------------
CvPlayer::found skips AI_cityCreated while Advanced Start points are active.

At first glance this means every Advanced Start city can be absent from incremental
UWAI caches.

The first Advanced Start city, however, flips setFoundedFirstCity and therefore
triggers the older full cache.update() path once final initialization is active.
Historical commit 1494 added that refresh intentionally.

Only additional Advanced Start cities placed after foundedFirstCity is already true
lack both:
    * the first-city full refresh;
    * the normal AI_cityCreated broadcast.

CvPlayerAI::AI_doAdvancedStart can indeed place multiple cities in a loop, so a
partial own cache is structurally possible during setup.

Why this WIP does NOT promote it:
    * first-round/team startup ordering limits normal UWAI war-evaluation consumers;
    * setTurnActive does not run the usual team doTurn path while elapsed game turns
      are zero;
    * UWAI scenario readiness and later per-turn refreshes repair the cache before many
      relevant consumers;
    * a behavioral reproduction that requires a particular first-contact / player-ID
      ordering is possible-looking but not yet established strongly enough to call a
      defect.

Keep as a setup robustness/cadence seam for F235+ if later evidence supplies a clean
consumer before refresh.


MILITARY-BRANCH SAVE-LAYOUT CONCERN -- REJECTED
------------------------------------------------
UWAICache serializes the m_militaryPower vector by current branch count without writing
an outer branch count/version. That would be fragile if the branch roster had ever
changed after UWAI saves existed.

Historical review found the six-branch roster stable since initial UWAI integration:
    HOME_GUARD
    ARMY
    FLEET
    LOGISTICS
    CAVALRY
    NUCLEAR

No add/remove/reorder transition requiring an outer migration was found.

MilitaryBranch later versioned its own record and, importantly, its reader DOES
explicitly distinguish raw version-0 data before applying the newer packed encoding.

That is a useful negative control for F232: MilitaryBranch contains the v0 guard that
the older UWAICache::City distance retrofit omitted.

RESULT:
    No F235.


PACKED VERSION-SENTINEL RANGE CHECK -- CLEAN
--------------------------------------------
The principal folded version encodings were checked against current SAS data/domain
sizes:

    UWAICache owner:
        base 100
        SAS current maximum civ-player domain is 48
        => no owner/version collision.

    MilitaryBranch typical unit:
        base 1000
        current SAS unit-info count is far below 1000
        => no unit/version collision.

    UWAICache::City distance:
        base 10000
        current writer explicitly clamps the current encoded distance to [-1,9999]
        and normal UWAI deployment-distance limits are far below the boundary
        => no modern current-format collision.

This does not alter F232: F232 is the missing raw-v0 decoding branch, not a current
sentinel-capacity problem.


ADDITIONAL LOW-MENTION / CALLBACK SWEEP
---------------------------------------

1. Unit create/destroy military cache callbacks -- NO NEW ROOT
    CvUnit creation updates owner power/assets and then calls reportUnitCreated.
    CvUnit kill reverses the owner power/assets and calls reportUnitDestroyed before
    the unit object is deleted.

    The inspected ownership/capture lifecycle did not expose a queue-031 cache-count
    imbalance independent of already-tracked initialization/revival issues.

2. clear/read/createMilitaryBranches lifecycle -- CLEAN IN THIS PASS
    read sets the owner before clear, clear(false) deletes any prior branch state, and
    read later reconstructs the fixed branch roster before consuming branch records.
    No duplicate branch allocation path was established.

3. updateLostTilesAtWar early return -- CLEAN
    clear(true) resets the map before the update. If OWN_EXCLUSIVE_RADIUS is disabled,
    returning early therefore leaves the intended zero state rather than stale values.

4. updateWarAnger anarchy early return -- CLEAN
    m_arWarAnger is reset by the enclosing full cache update before this function.
    Returning during anarchy does not preserve old anger values.

5. City::updateAssetScore -- NO NEW LOCAL ROOT
    The negative per-city score possibility and aggregate denominator implications were
    already examined in WIP18. No additional independent asset-score calculation defect
    was established here.

6. City::updateDistance -- NO NEW ROOT BEYOND F226/F231/F232
    The detailed path body was reread after those findings. Remaining concerns map to:
        F226 -- which sea/land route is chosen;
        F231 -- failing to rerun geometry after source-city-set mutation;
        F232 -- legacy distance decoding.
    No additional independent path arithmetic defect was promoted.

7. zero-city cache-owner guard in reportCityCreated -- PURPOSEFUL ENOUGH TO KEEP
    The foreign-target distance calculation is fundamentally based on the cache
    owner's source cities. A zero-city player's cache has special lifecycle constraints.
    No independent team-game defect was proved from this early return in this WIP.


C019-WIP20 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP20

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553
    F231 / provisional KI#554
    F232 / provisional KI#555
    F233 / provisional KI#556
    F234 / provisional KI#557

NEXT ROOT ID:
    F235 UNUSED

Resume from F235 unused.

BEST NEXT SEAMS
---------------
    * finish the Advanced Start / first-turn / scenario cadence proof and either reject
      it durably or promote only if a concrete pre-refresh consumer is established;
    * inspect any remaining add/remove identity assumptions now that F234 exposes the
      vector/map uniqueness contract explicitly;
    * perform one final call-site matrix for reportCityCreated/reportCityDestroyed and
      setFoundedFirstCity so unusual city creation paths (colonies, gifts, liberation,
      WorldBuilder, resurrection) are classified rather than assumed;
    * close the residual persistent-state producer/consumer table;
    * if no further root survives, prepare the first-pass durable close for
      UWAICache.cpp and advance to the next priority queue file.

Do not close merely because the root count is high. Likewise, do not invent F235 if
the remaining lifecycle seams are only theoretical.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C019-WIP21 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this continuation:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip

Prior physical checkpoint:
    C019-WIP20

New roots in this append:
    F235 / provisional KI#558
    F236 / provisional KI#559

Same-root refinement in this append:
    F234 / provisional KI#557 -- duplicate first-city wrappers survive save/load

Next root:
    F237 UNUSED

PURPOSE OF THIS WIP
-------------------
Continue queue 031 UWAICache.cpp from F235 unused.

This pass concentrated on derived cache state that is assumed immutable, knowledge /
revelation transitions that do not create a city object, remaining city identity
lifecycle after F234, and the residual leader/team-transition matrix.

Two new roots survive the proof standard:
    * F235: cached trait personality flags are not invalidated when True Starts changes
      the leader after UWAI initialization;
    * F236: contact / revelation of pre-existing foreign cities does not incrementally
      refresh the city cache, so reachability can remain zero and immediate sponsored
      war offers can be falsely denied.

F234 is also strengthened: its duplicate vector/map identity divergence is serialized,
so save/load does not heal the cache and can preserve the later dangling-entry hazard.

The remaining leader/team-transition anomalies inspected in this pass predominantly map
onto existing F213/F215/F216/F228/F230 contracts. No F237 is manufactured.


F235 / provisional KI#558
-------------------------
TITLE
    UWAICache trait flags are initialized and serialized but never refreshed; True
    Starts can change the player's leader after UWAI initialization and leave the
    previous leader's offensive/defensive military personality cached indefinitely.

STATUS
    CONFIRMED inherited AdvCiv integration/lifecycle defect.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

CACHED DERIVED STATE
    UWAICache::init does:

        m_eOwner = eOwner;
        clear();
        updateTraits();
        createMilitaryBranches();

    updateTraits derives two booleans from the cache owner's CURRENT traits and their
    free promotions:

        m_bOffensiveTrait = false;
        m_bDefensiveTrait = false;

        ...
        if (promotion.getCombatPercent() > 0)
            m_bOffensiveTrait = true;
        else if (promotion.getCityDefensePercent() > 0)
            m_bDefensiveTrait = true;

    The header describes the intended meanings as:
        offensive:
            any trait granting an unconditional combat bonus through a free promotion;
        defensive:
            any trait boosting city defense through a free promotion.

REGULAR CACHE REFRESH DOES NOT RECOMPUTE THEM
    UWAICache::update(bool) calls clear(true) and refreshes cities, transports,
    missions, typical units, threat, vassal scores, geometry, economy, war anger,
    fallout capability and war utility.

    It never calls updateTraits().

    clear(true) deliberately does not reset the two trait flags either.

    Therefore these booleans behave as immutable identity-derived cache state after
    init.

SAVE/LOAD ALSO TRUSTS THE CACHED DERIVATION
    UWAICache::write serializes:

        m_bOffensiveTrait
        m_bDefensiveTrait

    UWAICache::read reads the serialized booleans directly.

    It does not recompute them from the player's current leader/traits after load.

    This makes the cache vulnerable both to runtime leader replacement and to future
    XML reinterpretation of traits/promotions across compatible save versions.

SUPPORTED NORMAL-GAME INVALIDATION: TRUE STARTS
    The important point is that AdvCiv itself has a normal game feature that changes
    leaders AFTER player AI/UWAI initialization.

    CvPlayerAI::AI_init initializes UWAI:

        if (isEverAlive() && !isBarbarian() && !isMinorCiv())
            m_pUWAI->init(getID());

    At this point updateTraits has cached the leader/trait assignment that existed
    during player initialization.

    Later, CvGame::setInitialItems performs starting-plot assignment and then:

        if (isOption(GAMEOPTION_TRUE_STARTS))
        {
            TrueStarts ts;
            ts.changeCivs();
            ...
        }

    TrueStarts::changeCivs chooses the selected leader and calls:

        itPlayer->changeCiv(...);
        itPlayer->changeLeader(eLeader, ...);

    Thus the supported startup sequence can replace the leader after UWAICache::init.

CHANGELEADER CORRECTLY UPDATES REAL PLAYER TRAITS, BUT NOT UWAI DERIVED FLAGS
    CvPlayer::changeLeader:

        processTraits(-1);
        set new leader;
        processTraits(1);
        changePersonalityType();

    so the player's actual game traits and ordinary AI personality become correct.

    The old blanket AI reinitialization is intentionally disabled:

        /* Re-initializing the AI here was probably always a bad idea,
           but definitely mustn't re-initialize UWAI. Instead call
           AI_updatePersonality from setPersonalityType. */
        // AI().AI_init();

    That is sensible: reinitializing UWAI wholesale would destroy persistent state.

    But no targeted UWAICache::updateTraits call replaces the stale derived booleans.

    Subsequent normal UWAICache::update calls still do not recompute them, so the
    mismatch is not a one-turn transient. It can persist for the entire game.

HISTORICAL INTEGRATION CHAIN
    AdvCivPreSAS commit 1766, a26d4f8362,
        "Repair the ChangePlayer component"
    deliberately removed/rejected full AI reinitialization from changeLeader and
    moved the ordinary personality refresh into AI_updatePersonality.

    At that point avoiding UWAI reinitialization was correct and necessary.

    Later AdvCivPreSAS commit 3579, 4977b513da,
        "Merge true_starts into 1.05"
    added the ordinary startup True Starts path that can call changeLeader after the
    earlier UWAI initialization.

    The later integration therefore invalidated the implicit assumption behind the
    separately cached trait flags without adding a targeted invalidation/refresh.

LIVE CONSUMERS
    The flags are not decorative.

    UWAI::Player::distrustRating:
        if (m_cache.hasDefensiveTrait())
            iR += 30;

    InvasionGraph:
        * attacker hasOffensiveTrait -> roughly +0.1 army modifier;
        * defender hasOffensiveTrait -> roughly +0.1 army modifier;
        * defender hasDefensiveTrait -> +0.3 city-defender modifier.

    CvCityAI::AI_calculateSettlerPriority:
        the defensive UWAI trait branch adds a settler-priority adjustment.

    Therefore stale flags can change diplomacy/war confidence, invasion force
    estimates and expansion prioritization.

CURRENT SAS DATA GIVES A CLEAN DEFENSIVE REPRODUCTION
    Current SAS TRAIT_PROTECTIVE grants CITY_GARRISON1.
    That promotion has positive city-defense percentage and no generic combat
    percentage, so updateTraits sets m_bDefensiveTrait = true.

    Current SAS TRAIT_AGGRESSIVE has been reworked to grant LEADERSHIP rather than
    COMBAT1. Leadership has neither generic combat nor city-defense percentage, so
    current SAS does not classify Aggressive as offensive through this cache.

    Hence a simple current-SAS True Starts reproduction is:

        initial temporary leader: Protective
        True Starts selected leader: non-Protective

        actual player traits after changeLeader:
            non-Protective

        cached UWAICache flag:
            m_bDefensiveTrait remains true

    Result:
        phantom Protective-style UWAI modeling can persist indefinitely.

    Reverse ordering gives a missing defensive-trait model.

BASE ADVCIV EFFECT IS BROADER
    Base AdvCiv 1.14 retains the same lifecycle.

    Its ordinary Aggressive trait uses COMBAT1, whose generic combat modifier makes
    updateTraits classify it as offensive. Thus base AdvCiv can stale BOTH the
    offensive and defensive cached models after a True Starts leader reassignment.

SECONDARY SAVE/XML MANIFESTATION
    Because the booleans are serialized as derived data and trusted on load, an
    otherwise compatible save can also retain a flag computed under an older XML
    interpretation.

    For example, a save written when a trait granted a generic-combat promotion can
    retain offensive=true even if the current compatible data no longer give that
    trait such a promotion.

    This is secondary to the fully supported True Starts reproduction, but reinforces
    that these fields should be treated as derivable state rather than immutable
    serialized truth.

NOT THE ELSE-IF DATA SMELL
    updateTraits contains:

        if (combat > 0)
            offensive = true;
        else if (cityDefense > 0)
            defensive = true;

    A single trait-granted promotion with BOTH bonuses would therefore fail to set the
    defensive flag.

    Current SAS trait/free-promotion XML was checked and does not presently contain
    such a triggering promotion. That generic robustness concern is NOT promoted as a
    separate finding.

REPAIR DIRECTION
    Do not restore whole AI/UWAI reinitialization in changeLeader.

    Prefer targeted invalidation/recomputation:
        * expose a UWAICache helper for leader/trait identity change; and
        * call updateTraits after processTraits(1) / leader replacement is complete.

    As a defensive backstop, updateTraits is cheap enough to consider during every
    ordinary UWAICache::update as well.

    Save reading should also consider consuming the serialized legacy booleans for
    stream compatibility, then recomputing the current derived values from actual
    traits after the player/leader state is available.

    Exact load ordering should be checked before implementing that read-side hardening.


F236 / provisional KI#559
-------------------------
TITLE
    Contact and map-revelation transitions do not refresh UWAICache city knowledge;
    newly known/deducible pre-existing cities can remain absent with zero cached
    reachability, causing false immediate DENIAL_NO_GAIN for sponsored-war trades.

STATUS
    CONFIRMED inherited AdvCiv incremental-cache coverage defect.
    Present in current base AdvCiv 1.14 and current AdvCiv-SAS.

FULL-REFRESH KNOWLEDGE CONTRACT
    UWAICache::update rebuilds cached cities only for currently known major teams:

        for (TeamIter<MAJOR_CIV,KNOWN_TO> it(...))
            updateCities(it->getID(), ...);

    For an AI owner, updateCities includes an existing foreign city only when:

        kCacheTeam.AI_deduceCitySite(*pCity)

    AI_deduceCitySite immediately returns true for a city whose site is revealed:

        if (kCity.isRevealed(getID()))
            return true;

    Thus, after contact/revelation, a fresh cache can legitimately contain pre-existing
    cities that were absent at the preceding refresh.

WHAT THE INCREMENTAL LAYER COVERS
    UWAICache has an add(CvCity&) helper capable of constructing one cache City with
    fresh pathfinders and reachability.

    Historical/current comment:

        "Only for updates triggered by unit movement of other players.
         Caller is assumed to check visibility and civ status"

    In current source, however, the live route to this helper is reportCityCreated.

    reportCityCreated handles a city OBJECT being created / ownership-recreated. It
    does not handle an already-existing city becoming known, revealed or deducible.

FIRST CONTACT HAS NO UWAI INVALIDATION
    CvTeam::meet / makeHasMet establishes reciprocal contact, Tech Share, attitudes,
    vassal/master propagation and related diplomacy state.

    No UWAICache city refresh/invalidation is performed.

    Therefore a team that was not KNOWN_TO the cache owner at the previous full update
    remains completely absent from the cached city list after first contact, even when
    the contact movement also reveals one of its cities.

MAP REVELATION HAS THE SAME ROOT
    TRADE_MAPS loops the map and calls:

        kPlot.setRevealed(kToPlayer.getTeam(), true, false,
                kFromPlayer.getTeam(), false);

    CvPlot::setRevealed propagates city revelation through:

        pCity->setRevealed(eTeam, true);

    CvCity::setRevealed updates seen/attitude/visibility state but does not notify UWAI.

    Therefore an AI can ALREADY know rival team T at its last cache refresh, omit T's
    hidden/non-deducible city C, then receive a map that makes C revealed/deducible.
    C remains missing until a later full cache rebuild.

    This is the same invariant failure as first contact:
        world/knowledge state changed such that a pre-existing city now satisfies the
        full-refresh inclusion rule, but no incremental cache transition mirrors it.

REACHABILITY CONSEQUENCE
    m_aiReachableCities is populated only from cached City entries.

    UWAICache::add(City&) increments the foreign owner's count when the added City is
    reachable.

    If a newly known/revealed city is never added, the foreign player's count can
    remain zero even though a fresh cache would add a reachable target.

    UWAI::Team::canReach uses that count directly:

        if (itAgent->uwai().getCache().
                numReachableCities(itTarget->getID()) > 0)
            return true;

    There is no fresh path or visibility calculation in this gate.

HIGH-CONFIDENCE NORMAL TURN REPRODUCTION
    Use three teams:
        A = AI that may be hired for war
        T = target
        H = human sponsor

    H already knows both A and T.

    1. At the start of A's sequential turn, A's team turnPre performs the normal UWAI
       cache refresh.
       A has not yet met T, so T is outside the KNOWN_TO updateCities domain and A has
       no cached T cities / zero reachable-city count for T.

    2. Later in A's same active turn, one of A's units moves adjacent to a T city.
       Normal visibility/contact processing makes A meet T and reveals/deduces that
       same-land reachable city.

    3. CvTeam::meet performs no UWAI cache refresh. The existing T city was not
       created, so reportCityCreated is not called either.

    4. A's turn ends. Before A's NEXT turnPre, H's sequential turn begins.

    5. H asks A to declare war on T.
       CvPlayer::canTradeItem accepts the TRADE_WAR item when both A and H have met T
       and A can legally declare war.

    6. CvPlayer::getTradeDenial reaches:
           CvTeamAI::AI_declareWarTrade
           -> UWAI::Team::declareWarTrade

       Choose ordinary attitude/war-state conditions that pass the preceding refusal
       gates.

    7. UWAI::Team::declareWarTrade begins with:

           if (!canReach(eTarget))
               return DENIAL_NO_GAIN;

       A's stale cache still reports zero reachable T cities.

       The function returns DENIAL_NO_GAIN BEFORE constructing/evaluating the
       WarEvaluator for the sponsored war.

    8. A fresh UWAICache at this moment would iterate newly known T, include the
       revealed same-land city and give a positive reachable-city count.

    Therefore the denial is caused specifically by stale cache knowledge, not by an
    actually unreachable target or ordinary attitude/power refusal.

TURN ORDER SUPPORT
    CvPlayer::setTurnActive(true) performs:
        AI_updateCacheData;
        for elapsed turns > 0 and the team leader:
            GET_TEAM(getTeam()).doTurn();
        then:
            doTurnUnits();

    CvTeam::doTurn reaches the UWAI team/player turn-pre refresh before the subsequent
    unit movement that can establish new contact.

    Hence the required "refresh first, contact later in same AI turn" ordering is
    normal sequential-turn behavior.

MAP-TRADE VARIANT
    A second ordinary reproduction does not require a new team:

        * A already knows T at A's last full update.
        * T's relevant city was still hidden/non-deducible, so it was omitted.
        * A receives a map later in the round that reveals that existing city.
        * no UWAICache callback adds it;
        * immediate diplomacy/war evaluation before A's next refresh still sees the old
          reachability set.

    This broadens the repair contract beyond merely hooking CvTeam::meet.

HISTORICAL CONTEXT
    AdvCivPreSAS commit 2605, f788c46a21,
        "Various optimizations for UWAICache"
    introduced the incremental add/remove City layer and changed city ownership updates
    to reportCityCreated/reportCityDestroyed so AI caches could follow conquests during
    a human turn.

    That optimization solves world-object mutation, but the cached city's inclusion
    condition also depends on mutable KNOWLEDGE state. No corresponding transition was
    added for already-existing cities becoming known/deducible.

    Commit 2606, 1fdeb92b6f,
        "Minor tweaks to UWAI distance computation"
    later strengthened the full-refresh human-known-city policy but likewise did not
    add a revelation/contact invalidation path.

DISTINCTION FROM F233
    F233:
        a NEW foreign city of an ALREADY KNOWN civ is omitted from a HUMAN cache
        because reportCityCreated lacks the full-refresh human override.

    F236:
        a PRE-EXISTING city becomes newly eligible for an AI cache because contact or
        revelation/knowledge changed, but no city-creation callback occurs at all.

    Their producers and repair hooks are different, so keep separate roots.

DISTINCTION FROM F231
    F231 concerns cached cities that ARE PRESENT but whose deployment geometry and
    aggregate reachability become stale when the cache owner's source-city set changes.

    F236 concerns cities that SHOULD NOW EXIST in the cache but are still entirely
    ABSENT because knowledge changed.

REPAIR DIRECTION
    A robust fix needs a knowledge-change invalidation policy, not only a first-contact
    special case.

    Candidate architecture:
        * provide a targeted "refresh known cities for team" / city-knowledge-dirty
          helper;
        * after reciprocal CvTeam::meet is stable, refresh newly eligible cities for
          affected major-player caches;
        * for bulk map revelation, avoid an expensive UWAI rebuild per plot. Mark the
          recipient team/player caches dirty while plots are revealed and flush once
          after TRADE_MAPS finishes;
        * rebuild/recompute m_aiReachableCities and attack ordering consistently with
          any newly inserted cities;
        * ensure required pathfinders use the now-current contact/revelation state.

    Alternatively, force an appropriate full cache update before diplomacy consumers
    use reachability, but broad full updates during map trades/contact may be more
    expensive and may disturb unrelated cadence/persistent state.

    Exact hook/batching design should be reviewed before implementation.


F234 / provisional KI#557 -- SAME-ROOT SAVE/LOAD REFINEMENT
------------------------------------------------------------
The duplicate first-city vector/map divergence is SAVE-PERSISTENT.

F234 already established the runtime sequence:
    * first-city setFoundedFirstCity(true) performs a full owner-cache update;
    * CvPlayer::found later broadcasts AI_cityCreated for the same city;
    * reportCityCreated -> add(CvCity&) appends a second City wrapper;
    * m_cityMap::insert for the duplicate plot key fails silently;
    * m_cityList contains two allocated wrappers while m_cityMap owns/addresses only
      the first.

This pass checked serialization.

UWAICache::write serializes:
    pStream->Write(numCities());
    for (int i = 0; i < numCities(); i++)
        m_cityList[i]->write(pStream);

numCities() is the VECTOR size.

Therefore both duplicate wrappers are written to a manual save.

UWAICache::read then reconstructs all serialized vector entries:

    for (...)
    {
        m_cityList.push_back(new City);
        m_cityList[i]->read(pStream);
        m_cityMap.insert(std::make_pair(m_cityList[i]->getID(), m_cityList[i]));
    }

Again, duplicate map insertion is not checked. The vector is rebuilt with both
wrappers while only the first duplicate key wins the map entry.

cacheCitiesAfterRead subsequently does:

    for (...)
        cityAt(i).cacheCvCity();

Both wrappers relink to the SAME surviving real CvCity. It does not deduplicate or
rebuild the map/vector uniqueness invariant.

Consequences:
    * save/load does not heal F234;
    * the divergent vector/map representation can survive arbitrarily long through a
      manual save;
    * later removal still deletes only the map-owned first wrapper, leaving the
      duplicate vector wrapper able to become stale/dangling after the real city dies;
    * the crash/UAF window is therefore not restricted to the original founding round.

Keep this under F234. No new root number is needed.


ADVANCED START FOLLOW-UP -- REJECTED
-------------------------------------
A suspicious callback asymmetry was checked more fully:

    CvPlayer::found does not broadcast AI_cityCreated while Advanced Start points are
    active.

The first Advanced Start city can still trigger the first-city full refresh through
setFoundedFirstCity. Later Advanced Start cities can therefore be temporarily absent.

However, for a normal AI activation:
    * AI_doAdvancedStart executes before the normal elapsed-turn team doTurn;
    * team doTurn performs UWAI turnPre/full cache refresh before ordinary unit/war
      planning.

No concrete normal supported consumer was established between the later Advanced Start
city creation and that corrective refresh. Initial elapsed-turn-zero ordering likewise
did not establish a meaningful pre-refresh war consumer.

RESULT:
    Keep as a lifecycle oddity / negative control.
    Do not promote a root without a behaviorally live window.


RESIDUAL LEADER/TEAM-TRANSITION MATRIX
---------------------------------------

1. onTeamLeaderChanged and canBeHired -- ALREADY F215
    The function comments that CanBeHired / ReadyToCapitulate will be updated and
    should start false for the new leader.

    But m_abCanBeHiredAgainst is a default-true CivTeamMap, and the new leader's
    existing cache can contain its own prior state.

    This is precisely the already-recorded team-leader migration defect F215.
    Do not duplicate it as F237.

2. onTeamLeaderChanged persistent target-domain narrowing -- NO NEW ROOT
    The function copies selected leader-level state over TeamIter<MAJOR_CIV>, i.e.
    living major teams.

    Dead/revivable target-domain concerns are structurally similar to the already
    recorded migration/history roots F216/F228/F230.

    For warUtilityIgnoringDistraction specifically, ordinary cache refresh recomputes
    needed free-major targets; no independent durable behavioral reproduction was
    established here.

3. addTeam persistent target domain -- remains F216 refinement
    The absorbed leader's persistent team-keyed history needs a full persistent
    CivTeam target domain, as recorded in WIP19.

4. outsider references after Permanent Alliance -- remains F228
    No second independent migration mechanism was found in this pass.

5. ready-to-capitulate reset/reuse -- remains F213
    Current SAS source contains the custom full-clear fix recorded under F213.


OTHER NEGATIVE CONTROLS / SATURATION NOTES
-------------------------------------------

1. Trait free-promotion double-property else-if
    As noted above, current SAS data do not trigger the "combat AND city-defense on the
    same trait-granted promotion" case. No standalone root.

2. True Starts and military branches
    MilitaryBranch objects are owner-bound rather than permanently leader/civ-bound,
    and updateTypicalUnit reads the owner's CURRENT civilization/unit set during normal
    cache refresh. No independent persistent True Starts branch-identity root was
    established.

3. City revelation functions
    CvPlot::setRevealed and CvCity::setRevealed update the normal map/city seen state
    and relevant attitudes/UI, but no hidden UWAICache invalidation call was found.
    This supports F236 rather than creating another root.

4. add(CvCity&) live callers
    Current direct incremental City addition flows through reportCityCreated; no
    separate contact/map-reveal caller was found.

5. Save-version top-level residual scan
    No additional independent old-format transition survived beyond:
        F229 -- compact bounty + pre-v9 narrowing;
        F230 -- pre-v4 pastWarScore alive-only conversion;
        F232 -- raw City v0 distance decoded with versioned -1 transform.

6. Packed version sentinels
    Current range checks from WIP20 remain clean.

7. City callback identity
    F234 now covers duplicate first-city identity and its save persistence.
    Remaining ordinary found/acquire/kill paths inspected in this pass did not establish
    a different vector/map identity failure.

SATURATION ASSESSMENT
    UWAICache.cpp is now substantially closer to first-pass closure:
        * ordinary refresh fields have been swept;
        * compact representations have been bounded;
        * save-version transitions have been inspected historically;
        * city create/destroy and knowledge/revelation lifecycles have been traced;
        * player revival, team merging, team-leader migration and True Starts identity
          transitions have been checked;
        * many apparent anomalies now resolve to existing roots rather than new ones.

    Because F235 and F236 are both substantial new lifecycle roots found in this pass,
    keep the file OPEN for one more residual pass instead of declaring closure in the
    same checkpoint. A clean next pass is preferable to a premature durable close.


C019-WIP21 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP21

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553
    F231 / provisional KI#554
    F232 / provisional KI#555
    F233 / provisional KI#556
    F234 / provisional KI#557
    F235 / provisional KI#558
    F236 / provisional KI#559

SAME-ROOT REFINEMENT ADDED THIS WIP:
    F234:
        duplicate vector wrappers are serialized and survive read/cacheCitiesAfterRead.

NEXT ROOT ID:
    F237 UNUSED

Resume from F237 unused.

BEST NEXT SEAMS
---------------
    * one final knowledge-transition check around vassal/master propagated contact and
      whether F236's repair needs to dirty all member/master caches rather than only the
      directly meeting teams;
    * finish the last report-state producer/consumer matrix for sponsorship,
      capitulation and war-ending state across death/revival/team transitions;
    * re-read the City nested read/write record once more alongside the original
      unversioned format to ensure F232 is the only nested compatibility defect;
    * inspect the remaining UWAICache public accessors against every reset/update
      cadence to catch any field whose consumer contract assumes a fresher cadence than
      its producer provides;
    * if those survive cleanly, convert C019 into a durable first-pass close, increment
      completed files to 19 / 169, determine the next marked priority queue file from
      the reconciled album, and continue there.

Clean rejection remains preferable to inventing F237.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C019-WIP22 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this continuation:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*

Prior reconciled physical checkpoint:
    C019-WIP21

New roots in this append:
    F237 / provisional KI#560
    F238 / provisional KI#561
    F239 / provisional KI#562

Cross-file deduplication:
    Apparent F240 capitulation-lifetime candidate == already-recorded F196 / KI#519.
    F240 remains UNUSED.

Same-root refinements:
    F231:
        surviving cached target m_iTargetValue can also go stale across capital/city
        lifecycle changes; a newly selected capital is the cleanest example.
    F236:
        contact propagation through master/vassal meet chains broadens the cache-
        invalidation repair domain beyond the directly meeting pair.
    F239:
        the scenario startup isReady gate provides a second narrower path by which
        sponsorship can be recorded but normal peace can skip reportWarEnding cleanup.

PURPOSE OF THIS WIP
-------------------
Continue the final residual UWAICache sweep from F237 unused, especially the actual-unit
military inventory, HomeGuard/Army population contracts, sponsorship lifetime, and
cross-file producer/consumer assumptions.

This pass deliberately cross-checked candidate roots against the earlier audit album.
That caught one strong-looking issue -- stale human-capitulation authorization across
peace -- as an existing F196 rather than allocating a duplicate F240.


F237 / provisional KI#560
-------------------------
TITLE
    MilitaryBranch::Army is documented to include air units, but the incremental
    actual-unit census rejects ordinary aircraft because it tests ground getCombat();
    fighters, bombers and guided missiles therefore contribute zero cached Army power.

STATUS
    CONFIRMED inherited AdvCiv/UWAI military-cache accounting defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    The mismatch is already present in the original UWAI MilitaryBranch design.

DECLARED BRANCH CONTRACT
    MilitaryBranch.h describes Army explicitly:

        "Includes air and siege units."

    It also explains that Army tracks the full trained force before HomeGuard is split
    out.

    MilitaryBranch::updateTypicalUnit makes the intended distinction especially clear:

        // Siege and air units count for power but aren't typical
        if (kUnit.getCombat() <= 0 || ... ||
            kUnit.getDomainType() == DOMAIN_AIR ||
            kUnit.getDomainType() == DOMAIN_IMMOBILE)
        {
            continue;
        }

    In other words:
        * air units are deliberately excluded from selecting the representative
          "typical" Army unit;
        * they are nevertheless supposed to count in ACTUAL Army power.

ACTUAL REPORTING CONTRACT
    UWAICache::reportUnit forwards each unit creation/destruction to every military
    branch except HomeGuard.

    MilitaryBranch::reportUnit then admits a unit only when:

        if ((GC.getInfo(eUnit).getCombat() > 0 ||
             GC.getInfo(eUnit).isNuke()) &&
            isValidDomain(eUnit))
        {
            ...
            m_rTotalPower += rPowChange;
            m_iUnits += iChange;
        }

    Army::isValidDomain correctly allows:

        DOMAIN_LAND
        DOMAIN_AIR
        DOMAIN_IMMOBILE

    but the preceding getCombat() gate is ground-combat strength. Civ4 aircraft use
    iAirCombat/getAirCombat instead; ordinary aircraft normally have iCombat == 0.

CURRENT SAS DATA PROOF
    Current CIV4UnitInfos.xml contains these non-nuclear air combatants:

        UNIT_FIGHTER
            DOMAIN_AIR
            iCombat       0
            iAirCombat   12
            iPower       12
            bMilitaryProduction 1

        UNIT_JET_FIGHTER
            iCombat       0
            iAirCombat   24
            iPower       24

        UNIT_BOMBER
            iCombat       0
            iAirCombat   16
            iPower       16

        UNIT_STEALTH_BOMBER
            iCombat       0
            iAirCombat   20
            iPower       20

        UNIT_GUIDED_MISSILE
            iCombat       0
            iAirCombat   40
            iPower        6

    All pass Army's domain test and have positive XML power, but all fail the
    getCombat() > 0 / isNuke() admission gate.

    Tactical Nukes and ICBMs are admitted only through the explicit isNuke exception;
    that exception does not help ordinary air combatants.

LIVE CONSEQUENCE
    The Army branch is not a display statistic. Many WarUtilityAspect calculations use
    the present cached:

        getPowerValues()[ARMY]->power()

    directly for relative military strength, risk, tactical situation, intervention,
    vassal and other war-utility calculations.

    InvasionGraph::Node::initMilitary likewise copies the actual cached Army branch as
    the starting present force for its war simulation.

    Consequently a late-game empire can own a substantial Fighter/Bomber/Guided
    Missile force whose positive military power is completely absent from these cached
    Army totals.

HOME-GUARD SIDE EFFECT
    UWAICache::m_iNonNavalUnits DOES count those aircraft because its predicate is based
    on non-sea + bMilitaryProduction (excluding Recon), not getCombat().

    HomeGuard::initTotals also counts UNITAI_DEFENSE_AIR in its guard numerator.

    The current system can therefore have:
        denominator/headcount: includes aircraft
        Army power pool:       excludes ordinary aircraft

    For defensive Fighters this can additionally cause their headcount to raise the
    HomeGuard ratio while their own power is missing, shifting unrelated ground Army
    power into HomeGuard. Offensive aircraft can move the ratio in the other direction.
    This is a consequence of the same missing-air Army census, not a separate root.

ANCESTRY
    The historical MilitaryBranch implementation already used:

        (u.getCombat() > 0 || u.getNukeRange() >= 0)

    for actual unit-power updates while the contemporaneous Army class comment said:

        "Includes air force."

    The later scaled/UWAI rewrite changed names/types but preserved that predicate.
    Base AdvCiv 1.14 retains it.

    Thus this is not a SAS unit-data regression; current SAS aircraft merely make the
    inherited contract failure concrete.

REPAIR DIRECTION
    Separate "is a military unit with branch power" from "has ground iCombat".

    For the generic actual-unit census, an admission check needs to include valid air
    combatants as well as ground combatants and nukes, while still letting each
    branch's unitPower/isValidDomain reject units that do not belong.

    Conceptually an admission predicate could include:
        getCombat() > 0
        OR getAirCombat() > 0
        OR isNuke()

    but implementation review should check unusual domains/special units rather than
    mechanically adopting that exact expression.

SAVE / MIGRATION WARNING
    MilitaryBranch actual unit count and total power are serialized.

    Existing saves produced by old code contain Army totals that OMIT existing ordinary
    aircraft. If patched code merely starts applying +1/-1 callbacks to aircraft after
    loading such a save, destroying or upgrading an aircraft that existed at save time
    will subtract power/count that was never present in the serialized Army total.

    Therefore the safe repair should also rebuild present MilitaryBranch inventories
    from the owner's actually existing units after loading/migration, or otherwise
    explicitly invalidate/reconstruct those totals.

    This overlaps naturally with F225's already-needed "rebuild unit-derived military
    cache" helper for revived players. A common reconstruction helper may repair both
    lifecycle problems cleanly without turning ordinary per-turn update into an
    expensive full unit scan.

DISTINCTION FROM F225
    F225:
        revival creates a fresh zero inventory and fails to replay units that already
        physically exist.

    F237:
        even during normal continuous play, ordinary aircraft are never admitted into
        Army actual-unit power in the first place.

    A shared reconstruction helper can be part of both repairs, but the admission
    predicate still has to be corrected or future aircraft creations remain missing.


F238 / provisional KI#561
-------------------------
TITLE
    HomeGuard computes one guard ratio from a unit-count population that includes
    nuclear weapons, then uses that same ratio to partition Army POWER after nuclear
    power has been removed; adding nukes can manufacture deployable conventional Army
    power without changing the conventional force.

STATUS
    CONFIRMED inherited UWAI military-population mismatch.
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    The same structure is present in the original UWAI implementation.

CURRENT FLOW
    UWAICache::reportUnit increments m_iNonNavalUnits for a unit when:

        domain != DOMAIN_SEA
        bMilitaryProduction
        default UnitAI != UNITAI_EXPLORE

    No conventional-combat or non-nuclear check is applied.

    Current SAS ICBMs and Tactical Nukes satisfy the count predicate:
        ICBM          DOMAIN_IMMOBILE, bMilitaryProduction=1
        Tactical Nuke DOMAIN_AIR,      bMilitaryProduction=1

    Nukes are also intentionally counted in Army:
        Army::unitPower comment: "(Do include nukes in army)"
        MilitaryBranch::reportUnit admits nukes explicitly.

    They additionally have a separate NUCLEAR branch.

INCONSISTENT HOME-GUARD SPLIT
    InvasionGraph::Node::initMilitary calls:

        rGuardRatio = pHomeGuard->initTotals(
            m_kCache.numNonNavalUnits(),
            kCacheMilitary[ARMY]->power() -
            kCacheMilitary[NUCLEAR]->power());

    Thus the ratio denominator is a headcount that includes nukes, while the power pool
    being apportioned explicitly excludes all Nuclear-branch power.

    That one rGuardRatio is then reused to split both Army unit count and conventional
    Army power.

DETERMINISTIC EXAMPLE
    Suppose an empire has:
        5 conventional guard units
        5 conventional offensive units
        total conventional Army power P
        no nukes

    Then:
        non-naval headcount = 10
        guard count          = 5
        guard ratio          = 0.50
        HomeGuard power      = 0.50 P
        deployable conventional power ~= 0.50 P

    Now add 10 ICBMs, while leaving every conventional unit unchanged.

    Nuclear power is explicitly subtracted before the HomeGuard power split, so the
    conventional power pool remains P.

    But:
        non-naval headcount = 20
        guard count          = 5
        guard ratio          = 0.25

    The simulation now assigns:
        HomeGuard power      = 0.25 P
        conventional deployable share ~= 0.75 P

    Merely stockpiling immobile strategic missiles has made another quarter of the
    unchanged conventional military appear available for invasion.

WHY THIS IS DISTINCT FROM F237
    Fixing F237 so Fighters/Bombers enter Army power does not remove nuclear weapons
    from the headcount population while Nuclear power is excluded from the HomeGuard
    power pool.

    F238 therefore survives an F237 repair.

WHY A ONE-LINE "DON'T COUNT NUKES" FIX IS NOT AUTOMATICALLY SAFE
    The same rGuardRatio is also applied to:

        kCacheMilitary[ARMY]->numUnits()

    and Army's unit count currently includes nuclear units.

    Removing nukes only from m_iNonNavalUnits without changing the Army-headcount split
    can therefore create a different mismatch.

    The robust design likely needs explicit conventional non-naval headcount for the
    HomeGuard POWER split, and possibly separate handling for the simulated Army unit
    count instead of forcing one ratio to represent both populations.

ANCESTRY / DESIGN CONTEXT
    The original UWAI InvasionGraph already:
        * counted non-naval units;
        * subtracted Nuclear power;
        * applied one guard ratio to Army count and conventional Army power.

    Later rewrites preserve this arithmetic.

    The HomeGuard comment acknowledges that unit-count allocation is approximate, but
    F238 is stronger than "garrisons may be cheaper": the count and power populations
    are structurally different, and adding a unit whose entire power is deliberately
    excluded changes the partition of unchanged conventional power.


F239 / provisional KI#562
-------------------------
TITLE
    Team elimination ends wars through raw setAtWar(false) rather than makePeace;
    UWAICache::reportWarEnding is bypassed, so sponsored-war bounty/sponsor state can
    survive target death and later revival into an unrelated new war.

STATUS
    CONFIRMED inherited AdvCiv/UWAI war-lifecycle cleanup defect.
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.

NORMAL WAR-END CONTRACT
    CvTeam::makePeace invokes:

        AI().AI_preMakePeace(eTarget, pReparations);

    When UWAI is enabled/ready, CvTeamAI::AI_preMakePeace forwards both sides to:

        UWAI::Team::reportWarEnding

    which forwards to every living team member's UWAICache::reportWarEnding.

    That cache function unconditionally begins:

        // Forget sponsorship once a war ends
        m_aiBounty.set(eEnemy, 0);
        m_aeSponsorPerTarget.set(eEnemy, NO_PLAYER);

    These clears occur BEFORE any significance/duration test for past-war learning.
    Therefore the intended sponsorship lifetime is explicitly "until this war ends",
    including an otherwise insignificant/short peace.

ELIMINATION BYPASSES THAT CONTRACT
    When a team's alive count reaches zero, CvTeam::changeAliveCount ends its wars
    directly:

        kLoopTeam.setAtWar(getID(), false);
        setAtWar(kLoopTeam.getID(), false);
        ... clear war plans ...

    It does not call makePeace, AI_preMakePeace or UWAI::Team::reportWarEnding.

    The eliminated player's own UWAI cache is later uninitialized, but the SURVIVING
    enemy/hireling caches are not. Their bounty/sponsor entries keyed by the now-dead
    target remain.

REVIVAL MAKES THE STALE KEY LIVE AGAIN
    AdvCiv supports dead-player/team revival.

    Current SAS revival correctly initializes and refreshes UWAI caches (including the
    KI#475.3/F225 work), but ordinary cache.update/clear(true) deliberately preserves
    asynchronously maintained:

        m_aiBounty
        m_aeSponsorPerTarget

    A later declaration of war against the revived target does not clear those fields
    either.

    Therefore the old sponsorship can survive:
        paid war A
        -> target elimination
        -> target revival
        -> unrelated war B

LIVE CONSUMER
    WarUtilityAspect::HiredHand, when considering peace in war B, reads:

        ourCache().sponsorAgainst(eTheirTeam)
        ourCache().bountyAgainst(eTheirTeam)

    If the stored sponsor is still alive and sufficiently liked, HiredHand treats the
    old payment as an obligation to continue fighting.

    Its duration calculation uses the CURRENT war's at-war counters, so revival/new-war
    state does not naturally age out the old obligation. The old bounty can become
    behaviorally strong again as if sponsor S had paid for war B.

SUPPORTED SEQUENCE
    1. Sponsor S pays AI/team H to declare war on target team T.
    2. H fights T.
    3. T's final living member is eliminated.
    4. changeAliveCount removes H-T war with raw setAtWar(false); H's cache never
       receives reportWarEnding(T).
    5. H retains sponsor S and bounty against dead team T.
    6. T is later revived.
    7. H and T enter a later unrelated war.
    8. H considers peace. HiredHand sees S's ancient sponsorship and adds utility to
       continue the new war.

    Ordinary HiredHand conditions still apply; this finding does not claim every such
    revived war is forcibly prolonged. The defect is reuse of a paid-war contract whose
    documented cleanup boundary -- the end of the sponsored war -- was skipped.

PAST-WAR HISTORY REFINEMENT
    The same elimination path also bypasses the normal reportWarEnding computation of
    m_aiPastWarScore.

    If T never returns, history keyed to T is behaviorally irrelevant. Upon revival,
    however, confidenceFromPastWars(T) can again be consulted, and the decisive prior
    war ending may be absent from learned confidence.

    Keep this under F239 as an additional consequence of bypassing the normal UWAI
    war-end event; the sponsored-war stale contract is already the strongest
    deterministic proof.

SCENARIO-START SIBLING PATH
    There is a second, narrower cleanup asymmetry.

    CvDeal records TRADE_WAR sponsorship whenever UWAI is ENABLED:

        bool const bReportUWAI = getUWAI().isEnabled();
        ...
        reportSponsoredWar(...);

    This does not require UWAI::isReady().

    CvTeamAI::AI_declareWarTrade likewise routes to UWAI declareWarTrade when enabled;
    it has no top-level isReady guard.

    But AI_preMakePeace reports the war ending only when:

        getUWAI().isEnabled() && getUWAI().isReady()

    UWAI::isReady deliberately returns false during the first two elapsed turns of a
    scenario.

    Therefore an otherwise valid sponsored war recorded during that startup window can
    end by ordinary makePeace while its bounty/sponsor cleanup is skipped.

    This is the same lifetime contract violation as F239, reached through a different
    bypass, so do NOT allocate a new root. A robust repair should distinguish
    "war-evaluation history needs ready caches" from "transient paid-war obligation must
    be cleared whenever the war ends."

REPAIR DIRECTION
    Ensure every semantically real war termination clears transient UWAI war-contract
    state for surviving caches, including elimination.

    Do not simply call the current full reportWarEnding blindly from a state where its
    war-success/reparation assumptions are invalid; an explicit lifecycle helper may be
    cleaner:
        * always clear bounty/sponsor at war termination;
        * separately record past-war success only when the required history/context is
          valid.

    For elimination, if pastWarScore learning should be preserved, invoke that part
    while the old war-success/context still exists and before low-level state is erased.

    For scenario startup, do not gate sponsorship cleanup itself on isReady merely
    because the war-evaluation/history component is not ready.


F196 / KI#519 -- CROSS-FILE REVALIDATION, NOT F240
---------------------------------------------------
While auditing m_readyToCapitulateTo from UWAICache.cpp, a seemingly strong new root was
reconstructed:

    * considerCapitulation sets ReadyToCapitulate(H)=true for a human master and leaves
      it true after a successful AI-turn decision;
    * ordinary peace does not clear it;
    * reviewWarPlans clears it only after an early "no war plans" return;
    * a later unrelated war can therefore reuse the old authorization and bypass
      AI_surrenderTrade's "considerPeace must have run this AI turn" gate.

Before assigning F240, the earlier audit album was searched.

This exact lifecycle is already:
    F196 / provisional KI#519
    "Human-capitulation readiness persists across peace, so a later war can inherit an
     obsolete considerPeace authorization and bypass UWAI's AI-turn surrender gate."

The earlier entry contains the same war-A -> peace -> war-B reproduction, the same
willTalk ordering consequence and the same recommended reportWarEnding clear.

RESULT
    Independently revalidated from the cache side.
    No duplicate root.
    F240 remains UNUSED.

This also reinforces why F213 is distinct:
    F196 = ordinary live-game war lifetime;
    F213 = full cache reset / save-load object reuse.
Current SAS already carries the F213 full-reset fix; F196 remains the separate lifecycle
finding in the album.


F231 / KI#554 -- TARGET-VALUE SAME-ROOT REFINEMENT
---------------------------------------------------
F231 already proves that incremental city create/destroy callbacks fail to recompute
surviving cached foreign-city deployment geometry when the cache owner's city set
changes.

The residual pass found another derived field at the same invalidation boundary:
    City::m_iTargetValue

City construction computes m_iTargetValue once via:

    GET_PLAYER(eCacheOwner).AI_targetCityValue(kCity, false, true, this);

AI_targetCityValue depends on dynamic city/strategic properties including:
    * whether kCity is the capital;
    * war-plan type;
    * national wonders;
    * ownership/cultural adjacency;
    * owner victory stages;
    * Space-victory capital status;
    * city defensive strength;
    * cached distance/reachability.

When an enemy capital is destroyed/captured, the existing cache callbacks remove the old
capital BEFORE CvCity::kill later calls the victim's findNewCapital().

The surviving cached wrapper for the newly selected capital is not reconstructed.
It therefore retains the target value computed when that city was NOT the capital,
despite AI_targetCityValue having several explicit capital-specific branches.

No new root:
    This is another surviving-City derived value invalidated by the same incomplete
    incremental city lifecycle as F231.

Repair implication:
    F231's targeted post-city-set-change reconstruction should reconsider target value
    as well as distance/reachability/reachable-city totals before final resorting.


F236 / KI#559 -- PROPAGATED-CONTACT REPAIR-DOMAIN REFINEMENT
-------------------------------------------------------------
F236 establishes that newly knowable existing foreign cities are not inserted into
UWAICache when contact/map knowledge changes.

The final contact sweep confirms that a repair cannot assume only the directly meeting
two teams are affected.

CvTeam::meet can propagate knowledge through master/vassal relationships, and vassal
setup synchronizes third-party contacts through additional meet calls.

Thus one diplomacy/contact event can make cities newly eligible for caches belonging to
multiple related teams/members.

No new root:
    This broadens F236's invalidation set. A repair should dirty/rebuild every cache
    whose effective known-team/city-deduction set changes after propagation, rather
    than updating only the initiating pair.


OTHER RESIDUAL CHECKS / NEGATIVE CONTROLS
------------------------------------------

1. Human-capitulation lifetime candidate
    Deduplicated to F196 as described above.

2. Sponsorship in multi-member teams
    CvDeal records the payment in the specific signing player's cache, and
    WarEvaluator evaluates HiredHand once per agent-team member, summing those member
    contributions. This produces one paid obligation for the player who actually
    received the deal rather than automatically multiplying it by team size.

    A signing player's later elimination while teammates survive raises a policy
    question about whether the team's moral/strategic obligation should transfer, but
    no clear original contract proving such transfer was found. Do not promote.

3. MilitaryBranch::canEmploy
    canEmploy still uses getCombat()>0, but the only current live caller found is
    ArmamentForecast's Cavalry check against Army's typical unit. Army typical units
    intentionally exclude air/immobile units, so this does not create a second F237
    path with current callers.

4. Recon mismatch in m_iNonNavalUnits
    The earlier C019 note remains: combat Recon is excluded from the non-naval
    denominator while Army can include it. This is an explicit original "exclude Recon"
    approximation. No stronger deterministic decision error was established here.
    F237/F238 are structurally sharper and do not convert this old note into a root.

5. Packed MilitaryBranch save version
    Current typical-unit packing uses a 1000 enum stride. Current SAS has far fewer
    than 1000 UnitTypes. The version-0 reader also has an explicit raw-value branch,
    unlike F232's City decoder. No current compatibility root.

6. Apparent duplicate legacy m_aiAdjLandPlots read
    Reopened against authoritative source and original enum-map history.
    The source contains one legacy read, not two; the earlier visual duplication came
    from overlapping/combined output. Rejected.

7. Ordinary unit conversion staging
    Remains the prior F216 negative control. No live consumer was found between the
    balanced +1/-1 conversion callbacks on supported normal upgrade/gift paths.

8. Full old-save top-level field matrix
    No new independent transition survived beyond F229/F230/F232 and F229's pre-v9
    narrowing refinement.


C019-WIP22 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP22

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553
    F231 / provisional KI#554
    F232 / provisional KI#555
    F233 / provisional KI#556
    F234 / provisional KI#557
    F235 / provisional KI#558
    F236 / provisional KI#559
    F237 / provisional KI#560
    F238 / provisional KI#561
    F239 / provisional KI#562

CROSS-FILE REVALIDATION:
    F196 / provisional KI#519 -- stale ReadyToCapitulate across peace/new war.
    Not renumbered here.

NEXT ROOT ID:
    F240 UNUSED

Resume from F240 unused.

BEST NEXT SEAMS
---------------
    * finish sponsorship / past-war / hireability lifecycle coverage for target death,
      sponsor death, surviving teammates, revival and scenario-start readiness;
    * finish present-military census invariants, especially ownership transfer and
      whether any branch/count population can become unbalanced independently of F225,
      F237 and F238;
    * challenge remaining cache accessors against their producer cadence, with
      cross-file deduplication against earlier UWAIAgent/CvTeamAI roots before assigning
      F240;
    * re-open historical commits that explicitly fixed UWAICache/MilitaryBranch issues
      and verify later refactors did not narrow those contracts;
    * if the residual pass stays clean, decide whether UWAICache.cpp is finally ready
      for a durable first-pass close (19 / 169) and then continue with the next marked
      priority file rather than manufacturing F240.

Clean rejection remains preferable to inventing F240.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C019-WIP23 -- FILE 019 OPEN / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked locally:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    _0_Common_Docs/AdvCiv_Base_Doc/manual.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C019-WIP22

New root:
    F240 / provisional KI#563

Next root:
    F241 UNUSED

PURPOSE
-------
Preserve the resolved TeamPathFinder/maxLandDist investigation before continuing the
remaining queue-031 closure pass.

The finding below is unusually well supported by the historical transition:
the pre-refactor code explicitly treated maxLandDist as a travel-duration horizon and
expanded the pathfinder's tile-count upper bound by estimated movement speed; the
TeamPathFinder refactor retained a speed-expanded SEA length cap but fed raw
maxLandDist into the LAND path-length cap even though TeamPathFinder explicitly
documents that max path length is number of plots, not travel turns.


F240 / provisional KI#563
-------------------------
TITLE
    The 2020 TeamPathFinder refactor lost the movement-speed expansion of UWAI's LAND
    search horizon: maxLandDist is a travel-duration threshold, but it is now also used
    as a raw tile-count cap, so road/rail land targets beyond that many plots can be
    classified as naval or unreachable even when deployment takes far fewer turns.

STATUS
    CONFIRMED inherited AdvCiv/UWAI refactor regression.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Introduced by AdvCivPreSAS commit 2604, 0866f78275,
        "Use TeamPathFinder for UWAI distance computations".

CURRENT CODE
    UWAICache::resetTeamPathFinders currently sets:

        int const iSeaLimit = getUWAI().maxSeaDist() * shipSpeed();
        int const iLandLimit = getUWAI().maxLandDist();

        landFinder.reset(..., iLandLimit);
        anyWaterFinder.reset(..., iSeaLimit);
        shallowWaterFinder.reset(..., iSeaLimit);

    TeamPathFinder's class contract explicitly says:

        "The length of a path is the number of plots entered (not counting the start),
         i.e. the speed (base moves) of the units doesn't matter and no travel time
         (number of turns) is computed."

    StepMetricBase/KmodPathFinder enforce that limit on PathNode::getPathLength().
    TeamStepMetric initializes path length to 0 and increments it uniformly by one per
    entered plot.

    Therefore a LAND finder reset with maxLandDist=N cannot return any route entering
    more than N plots, regardless of the route movement cost.

BUT LAND PATH COST IS EXPLICITLY ROUTE-AWARE
    TeamStepMetric<LAND>::cost starts at MOVE_DENOMINATOR, but when both plots have
    usable non-enemy routes it substitutes the route movement cost plus the team's
    route movement modifier.

    Current SAS route data, for example:
        Road:
            iMovement = 30
            Engineering change = -10
        Railroad:
            iMovement = 20

    Thus a long route can have travel cost far below one turn per entered plot.

    City::updateDistance consumes precisely that route-aware path cost as estimated
    deployment duration:

        iPairwDuration =
            landFinder.getPathCost() / MOVE_DENOMINATOR

    and later stores a weighted travel-duration estimate in m_iDistance.

    So two different metrics are currently being mixed:
        search admissibility: raw number of entered plots <= maxLandDist
        resulting distance: route-aware movement duration in turns

HISTORICAL CONTRACT BEFORE THE REFACTOR
    Immediately before commit 2604, City::measureDistance explicitly distinguished the
    travel horizon from the pathfinder's step-count horizon.

    It first chose:
        maxDist = maxLandDist or maxSeaDist

    and, in later eras, deliberately increased maxDist by 4/3 so remote targets
    remained targetable.

    It then computed:
        stepDist = geometric step distance
        speedEstimate = estimateMovementSpeed(...)

    and rejected only when:

        stepDist / speedEstimate > maxDist

    Most importantly, its call into the old FAStar-based team pathfinder said:

        /* Path distance counts each step as 1 move; upper bound needs to
           account for faster movement. */
        ceil(maxDist * speedEstimate)

    That comment is the exact old invariant that commit 2604 lost.

OLD LAND SPEED MODEL
    The deleted estimateMovementSpeed function returned 1 for primitive unroaded land,
    but deliberately increased land speed as infrastructure improved:

        Classical+:
            up to roughly 2x from roads;
            Fast Roads multiplied that further.

        Industrial / railroad age:
            roughly 4.5x expected land speed.

        Future:
            another factor of two before the railroad multiplier.

    Therefore the old LAND pathfinder could search many more than maxLandDist tiles
    when the estimated travel time was still within maxLandDist turns.

    For Industrial-era land, the old path-length upper bound could be on the order of:
        maxDist * 4.5
    and maxDist itself was raised by 4/3 from era 4 onward.

THE REFACTOR CHANGE
    Commit 2604 replaced that code with TeamPathFinder.

    SEA received:

        seaLimit = maxSeaDist * shipSpeed

    LAND received:

        landLimit = maxLandDist

    The new TeamPathFinder had already documented that max path length is plot count,
    not movement duration. Its LAND cost function also already modeled routes.

    This is exactly the situation in which the old comment said the upper bound must
    be expanded for faster movement.

    Commit 2605 then deleted estimateMovementSpeed/measureDistance as obsolete.

    Commit 2606 made "Minor tweaks to UWAI distance computation", changing
    maxLandDist to maxSeaDist - 1 and adjusting the final cached-distance clamp, but it
    left the raw LAND path-length cap unchanged. No later historical change found in
    the reachable history redefined maxLandDist as a geometric tile radius.

CURRENT SEMANTIC EVIDENCE THAT maxLandDist IS STILL A TURN/DURATION THRESHOLD
    Current consumers compare m_iDistance -- which City::updateDistance derives from
    path COST / MOVE_DENOMINATOR -- against maxLandDist.

    Examples:
        ArmamentForecast:
            if target distance > maxLandDist -> naval armament

        UWAI::Team::isLandTarget:
            cached travel distance <= maxLandDist

        InvasionGraph::Node::canReachByLand:
            cached distance <= maxLandDist unless no deep-sea cargo

    Therefore maxLandDist still acts as a strategic deployment-duration threshold in
    current code. It has not become a pure geometric path-length constant.

SUPPORTED REPRODUCTION SHAPE
    On a common wrapped map with dimensions <=100, current:
        maxSeaDist = 15
        maxLandDist = 14

    Consider a same-area enemy city whose shortest land route from every significant
    source city enters 16 plots.

    Suppose roughly four of those steps are in enemy territory, where routes are
    intentionally ignored, and twelve are on usable Road/Railroad infrastructure.

    With MOVE_DENOMINATOR-scale full-cost enemy steps and route cost around one third of
    a full move, the route-aware deployment cost is roughly:

        4 full-cost steps + 12 * (1/3) move
        ~= 8 turns

    That is comfortably below the 14-turn strategic horizon.

    Current behavior:
        landFinder max path length = 14 plots
        16-plot path is not returned at all
        -> m_bReachByLand remains false for that source.

    Pre-2604 behavior:
        the step-count upper bound was multiplied by estimated land movement speed;
        such a route was within the search horizon and its duration was then reduced
        according to infrastructure-era movement speed.

    No exotic map size or overflow is required; distances moderately above 14 plots
    are ordinary on Standard-and-larger maps.

BEHAVIORAL CONSEQUENCES
    If every significant owner city is beyond the raw tile cap:

    * A same-continent coastal target can be treated as naval because the valid fast
      land path is never found; ArmamentForecast can consequently prepare naval rather
      than land armament.

    * A landlocked target can be left with m_iDistance == -1 if no sea/mixed path
      supplies an alternative, causing the cache and UWAI reachability logic to treat
      a strategically nearby-by-rail rival as unreachable.

    * m_aiReachableCities can consequently undercount reachable foreign cities.

    * InvasionGraph land topology and deployment modeling use
      canReachByLand/canReachByLandFromCapital/getDistance and inherit the false
      negative.

    * UWAI::Team::isLandTarget can fail to recognize a long-but-fast same-continent
      target as a land target.

    This can be especially visible in the industrial/modern game, exactly when roads
    and railroads make tile count diverge most from travel duration.

CROSS-FILE SAME-ROOT SIBLING: UWAIAgent::alignAreaAI
    The same commit 2604 changed an "easily reachable target" check from:

        measureDistance(..., DOMAIN_LAND, ..., &d);
        round(d / estimateMovementSpeed(...)) <= 8

    to:

        TeamPathFinder<LAND> pf(..., 8);
        if (pf.generatePath(...))
            ...

    The old test was explicitly <=8 estimated travel turns.
    The new test is <=8 entered plots.

    Therefore a 9+ tile capital-to-target route that takes <=8 turns because of roads
    is no longer considered "easily reachable" in this alignment decision.

    This is the same refactor invariant loss, not a second root number. It lives in
    UWAIAgent.cpp, whose first-pass audit is already closed, so preserve it here as a
    cross-file F240 scope refinement rather than inventing a duplicate finding.

BASELINE COMPARISON
    Base AdvCiv 1.14 retains:
        iSeaLimit = maxSeaDist * shipSpeed
        iLandLimit = maxLandDist

    and the same TeamPathFinder length contract.

    SAS inherited the behavior. K-Mod/Civ4CE do not carry the same UWAI deployment
    cache/refactor and therefore do not provide a direct equivalent.

WHY THIS IS NOT MERELY "PATHFINDING IS APPROXIMATE"
    UWAI deliberately approximates terrain and route movement, but the specific
    invariant here was explicit before the refactor:

        the geometric path upper bound must account for movement speed.

    The replacement pathfinder actually improves route-cost modeling, yet the raw
    tile-count cap prevents that better model from ever evaluating long fast routes.

    The asymmetric SEA treatment strengthens the proof: sea path length is still
    multiplied by ship speed precisely because TeamPathFinder max length counts plots.
    LAND lacks the corresponding expansion despite route-aware movement.

REPAIR DIRECTION
    Do not simply remove all path limits; deployment pathfinding is a documented
    performance hotspot, especially in very large/high-player-count games.

    Preserve a computational tile-count horizon that is large enough to admit paths
    whose route-aware travel COST can still fall within the intended strategic
    duration.

    Possible implementation directions:
        * derive a conservative LAND step cap from maxLandDist and plausible current
          land route speed, analogous to the old maxDist * speedEstimate;
        * or extend the pathfinder so this use can impose a cost/duration ceiling
          rather than a raw step ceiling, while retaining a separate generous
          geometric safety cap.

    City::updateDistance already uses path cost to compute deployment duration, so once
    the path is admitted it has the correct metric available.

    For alignAreaAI, merely enlarging the path-length cap is not sufficient: the old
    semantic test was <=8 travel turns. After finding a sufficiently generous land
    route, compare route-aware path cost against an 8-turn threshold.

    Any repair should be benchmarked on 36/48-civ large maps because commit 2604's main
    purpose was a major UWAI pathfinding performance improvement.

NEGATIVE CONTROLS / SCOPE
    * Primitive unroaded paths, where cost is about one move per entered plot, are
      largely unaffected; the metrics coincide there.
    * Paths already <=maxLandDist tiles are not rejected by this specific defect.
    * Enemy-owned route segments intentionally receive full movement cost in
      TeamStepMetric and are not evidence for F240.
    * F226 is different: it concerns which available path is selected / how path
      direction and route ownership affect one City::updateDistance computation.
      F240 concerns a valid fast path being excluded before its cost can be evaluated.
    * F231 is different: it concerns failure to recompute cached geometry after the
      cache owner's city set changes.
    * F190 is different: it concerns a team-member loop leaking the no-ocean-cargo
      max-distance exemption into later members.
    * This does not claim that every route beyond maxLandDist plots should be used;
      only that raw tile count cannot stand in for the documented movement-duration
      horizon when route-aware cost says the path is strategically short.


OTHER CLOSURE CHECKS COMPLETED BEFORE F240
------------------------------------------
The pass leading into F240 also revalidated several apparent seams as clean:

* Advanced Start city founding:
    CvPlayer::found suppresses incremental AI_cityCreated while Advanced Start points
    remain active, but ordinary UWAI war-planning turnPre is itself unavailable during
    setup/turn-zero initialization. The first real cache.update reconstructs setup
    cities before normal UWAI planning consumes the cache. Not a root.

* GET_TEAM(m_eOwner) / leaderCache:
    An intermediate suspicion that PlayerTypes was being used as a raw TeamTypes index
    was rejected. The project intentionally provides GET_TEAM(PlayerTypes) semantics
    through TEAMID. The earlier album had already retracted old F113/KI#436; that
    retraction remains correct.

* reportWarEnding reparations if/else-if:
    AI_preMakePeace intentionally calls one side with (received,NULL) and the other
    with (NULL,given). The mutually exclusive branches encode direction and do not
    discard a simultaneous second list.

* updateCanBeHiredAgainst:
    Although setter routing and updater storage look asymmetric, normal UWAI::Team
    scheduling updates this leader-only state on the team leader's cache. No new root.

* m_iNonNavalUnits zero-power junk:
    Current SAS unit XML was checked after F237/F238. No additional ordinary
    zero-ground/zero-air/non-nuclear bMilitaryProduction units were found slipping into
    the denominator beyond the already documented air/nuclear accounting roots.

* m_aiWarUtilityIgnoringDistraction MIN_INT sentinel:
    Current consumers either query targets expected to have been evaluated, normalize
    vassal/master/current-war targeting appropriately, or clamp the sentinel
    conservatively. No favorable-war decision from an uncomputed sentinel was proved.

* ScaledNum rewrite archaeology:
    The old updateWarAnger wrong-team-size divisor was independently rediscovered in
    the historical 2920 rewrite but is already F112/KI#435 and is fixed in current
    SAS. Other suspicious algebra changes mapped onto existing F219/F222/F223 or
    historically corrected follow-up commits. No duplicate root was allocated.


C019-WIP23 INTERIM CURSOR
-------------------------
COMPLETED FIRST-PASS FILES: 18 / 169
CURRENT FILE: queue 031 CvGameCoreDLL/UWAICache.cpp -- OPEN
PHYSICAL CHECKPOINT: C019-WIP23

CURRENT QUEUE-031 ROOTS:
    F213 / provisional KI#536
    F214 / provisional KI#537
    F215 / provisional KI#538
    F216 / provisional KI#539
    F217 / provisional KI#540
    F218 / provisional KI#541
    F219 / provisional KI#542
    F220 / provisional KI#543
    F221 / provisional KI#544
    F222 / provisional KI#545
    F223 / provisional KI#546
    F224 / provisional KI#547
    F225 / provisional KI#548
    F226 / provisional KI#549
    F227 / provisional KI#550
    F228 / provisional KI#551
    F229 / provisional KI#552
    F230 / provisional KI#553
    F231 / provisional KI#554
    F232 / provisional KI#555
    F233 / provisional KI#556
    F234 / provisional KI#557
    F235 / provisional KI#558
    F236 / provisional KI#559
    F237 / provisional KI#560
    F238 / provisional KI#561
    F239 / provisional KI#562
    F240 / provisional KI#563

NEXT ROOT:
    F241 UNUSED

Resume from F241 unused.

BEST NEXT WORK
--------------
    * continue the mechanical every-function closure matrix for UWAICache.cpp;
    * challenge the remaining historical bugfix transitions, especially pathfinder and
      save/lifecycle changes after 2604;
    * finish sponsorship/hireability/player-death negative controls;
    * cross-check any plausible root against the whole earlier album before numbering
      F241;
    * if the residual matrix remains clean, decide whether queue 031 is finally ready
      to close as first-pass file 19 / 169.

Parked for future queue 064 MilitaryBranch.cpp:
    canKnowTypicalUnit prerequisite check appears inverted:
        if (kTypicalUnit.getPrereqAndTech() != NO_TECH) // Warrior
            return true;
    Current XML Warrior has no prerequisite while advanced typical units do.
    Do not consume a queue-031 root for that implementation-level queue-064 candidate.

Clean rejection remains preferable to inventing F241.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
DURABLE CHECKPOINT C020 -- FILE 019 COMPLETE / QUEUE 031 UWAICache.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this close:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material used across the queue-031 pass:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    _0_Common_Docs/AdvCiv_Base_Doc/manual.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C019-WIP23

DURABLE RESULT
--------------
    COMPLETED FIRST-PASS FILES: 19 / 169

    queue 031
    CvGameCoreDLL/UWAICache.cpp
    STATUS: COMPLETE -- FIRST-PASS DURABLE CLOSE

    Queue-031 roots:
        F213 / provisional KI#536
        through
        F240 / provisional KI#563

    NEXT GLOBAL ROOT ID:
        F241 UNUSED

This close follows the album's established meaning of COMPLETE:
    * the file has received the systematic first-pass method;
    * local SAS/base/history differences and important cross-file contracts were
      challenged;
    * findings were promoted only with a supported producer -> state -> consumer path,
      or an equally strong save/lifetime invariant;
    * attractive but unsupported suspicions were recorded as negatives or parked for
      the owning file;
    * COMPLETE does not mean mathematically proven bug-free and does not prevent later
      cross-file archaeology from adding a sibling/refinement.


QUEUE-031 FINDING INDEX
-----------------------

F213 / provisional KI#536
    Full UWAICache reset did not clear m_readyToCapitulateTo, allowing a new game or
    loaded save in the same process to inherit human-capitulation authorization.
    Current staged SAS source contains the reviewed full-reset repair.

F214 / provisional KI#537
    MilitaryBranch typical-unit refresh is sticky when no currently trainable
    replacement exists; stale capability can survive loss of prerequisites.

F215 / provisional KI#538
    Team-leader migration fails to initialize/migrate CanBeHired according to its
    explicit "new leader starts disabled" contract.

F216 / provisional KI#539
    Permanent Alliance addTeam migration iterates an already-merged membership state
    and can self-add/double absorbed-leader history.
    Later refinement: the persistent TARGET-team domain also needs dead/revivable team
    keys rather than only currently alive major teams.

F217 / provisional KI#540
    Fallout discovery by "first feature with <= -50 health" aliases SAS Jungle after
    Jungle became -50 health, causing wrong scrub-tech/nuclear-damage reasoning.

F218 / provisional KI#541
    Transport capability can be cached from player-level trainability despite actual
    city/resource prerequisites, producing false ocean-lift reachability in SAS.

F219 / provisional KI#542
    goldPerProdVictory checks peaceful-victory stage 3 before cumulative stage 4,
    making stage-4 valuation branches unreachable.

F220 / provisional KI#543
    Nuclear deterrence in teamThreat checks only the cache owner's personal nukes,
    ignoring retaliation capability supplied by a teammate/master-side partner.

F221 / provisional KI#544
    Target-mission counting can count loaded cargo both through the transport and
    through the cargo unit's own still-enumerable selection group.

F222 / provisional KI#545
    ScaledNum rewrite changed goldPerProdSites parentheses, dividing the whole
    site-weight-plus-settler expression by 10 instead of only the settler term.

F223 / provisional KI#546
    ScaledNum rewrite changed CanBeHired hysteresis probability from
        1 - delta / 100
    to
        (1 - delta) / 100,
    collapsing the intended persistence anti-flicker behavior.

F224 / provisional KI#547
    Incremental city conquest/destruction keeps the detailed city set current but
    leaves m_rTotalAssets stale until the next full refresh.

F225 / provisional KI#548
    Revived players receive a fresh empty UWAI military inventory but existing
    physical units are not replayed into it; later destruction can even drive counts
    and power negative.

F226 / provisional KI#549
    City::updateDistance can treat the existence of a shorter locally valid land path
    as reason not to retain a valid sea path even when that land path is forbidden from
    establishing strategic reachability.

F227 / provisional KI#550
    Incremental city destruction erases an entry from the stateful attack-priority
    ordering without re-sorting surviving cities.

F228 / provisional KI#551
    Permanent Alliance migration is one-way for persistent team-keyed history:
    outsider caches that reference the absorbed team are not remapped to the survivor.

F229 / provisional KI#552
    Sponsored-war bounty is narrowed from full int utility to signed short without
    representability checking; large legal payments can wrap and erase sponsorship.
    Later refinement: pre-v9 old-save full-int bounty values can narrow during read.

F230 / provisional KI#553
    Pre-v4 pastWarScore migration multiplies only teams alive at load, although the
    original migration converted every serialized team slot; dead/revivable rival
    history can remain at 1/100 scale.

F231 / provisional KI#554
    Incremental own-city-set changes update only the changed cache entry and leave
    surviving foreign targets' deployment geometry and reachable-city aggregate stale.
    Later refinement: target value can also go stale, especially when a new capital is
    selected after the old capital is lost.

F232 / provisional KI#555
    Version-0 City records stored raw distance, but the versioned reader always applies
    the new "-1" unpacking transform; legacy distance 1 becomes the own-team sentinel 0.

F233 / provisional KI#556
    Incremental reportCityCreated omits the full-refresh human known-city rule; an AI
    can temporarily model a human through a human cache missing a newly founded city.

F234 / provisional KI#557
    First-city full refresh followed by the ordinary city-created callback can insert
    the same city twice, diverging vector and ID-map ownership and enabling a dangling
    cached City after same-round destruction.

F235 / provisional KI#558
    Cached offensive/defensive trait flags are not refreshed when True Starts changes
    the player's leader after UWAI initialization.

F236 / provisional KI#559
    Contact/map-revelation transitions can make pre-existing cities newly knowable
    without adding them to the cache, producing false zero reachability.
    Later refinement: master/vassal contact propagation broadens the invalidation set.

F237 / provisional KI#560
    Army is explicitly documented to include air units, but actual-unit accounting
    admits ground getCombat()>0 or nukes only; ordinary Fighters/Bombers/Guided
    Missiles contribute zero cached Army power.

F238 / provisional KI#561
    HomeGuard uses a headcount denominator that includes nuclear weapons to partition
    Army power after nuclear power has been removed; adding nukes can manufacture
    deployable conventional power.

F239 / provisional KI#562
    Team elimination ends war through raw setAtWar(false), bypassing the normal
    reportWarEnding sponsorship cleanup; stale paid-war state can survive target death,
    revival and a later unrelated war.
    A scenario-start isReady gate supplies a narrower sibling cleanup bypass.

F240 / provisional KI#563
    TeamPathFinder refactor changed LAND's search horizon from a movement-speed-expanded
    travel-duration search into raw maxLandDist plot count, while route-aware path cost
    is still interpreted as deployment turns. Long fast road/rail targets can therefore
    be excluded before their low travel cost is evaluated.
    Same-root UWAIAgent::alignAreaAI sibling: old <=8 estimated-turn test became <=8
    entered plots.


FINAL CLOSURE MATRIX
--------------------

A. OBJECT LIFETIME / RESET / SAVE LAYOUT
    Challenged:
        init
        uninit
        clear
        write
        read
        cacheCitiesAfterRead
        military-branch allocation/deallocation
        city-wrapper allocation/deallocation

    Root-owned:
        F213  full-reset capitulation authorization
        F225  revived-player military reconstruction
        F229  compact bounty save narrowing
        F230  old pastWarScore conversion domain
        F232  old City distance decoder
        F234  duplicate City ownership/save ramifications

    Clean controls:
        * m_readyToCapitulateTo existed in the original WarAndPeace cache save layout;
          its unconditional current read is not a missing version gate.
        * pre-v3 CanBeHired omission inherits the intended true reset from clear.
        * pre-v5 peaceful-victory focus omission inherits false, matching introduction.
        * the legacy relative-navy field is explicitly unfinished and has no live
          consumer found.
        * discarded old City fields are consumed for stream alignment only.
        * current MilitaryBranch packed typical-unit save encoding has an explicit
          version-0 raw-value branch; no F232-like decoder transition was found.
        * the apparent duplicated legacy m_aiAdjLandPlots read was an overlapping dump
          artifact; authoritative source contains one read.

B. FULL UPDATE PIPELINE / CACHED ECONOMIC VALUES
    Challenged:
        update
        updateTotalAssetScore
        updateGoldPerProduction
        goldPerProdBuildings
        goldPerProdSites
        goldPerProdProcess
        goldPerProdVictory
        calculateFocusOnPeacefulVictory

    Root-owned:
        F219  victory-stage ordering
        F222  site-value ScaledNum algebra
        F224  incremental total-asset staleness

    Clean controls:
        * per-city asset values can be negative, but the historical observed
          totalAssetScore==0 lifecycle bug was specifically repaired by the 3044 full
          new-player cache update; no supported normal fully refreshed nonpositive
          aggregate was proved.
        * goldPerProdProcess' current process-conversion valuation matches the later
          historical refactor/introduction contract.
        * no additional peaceful-victory stage/default defect survived challenge.

C. CITY SET / ID MAP / INCREMENTAL LIFECYCLE
    Challenged:
        updateCities
        add(City&)
        add(CvCity&)
        remove
        reportCityCreated
        sortCitiesByAttackPriority
        cvCityById

    Root-owned:
        F224  total-assets cadence
        F227  stale attack ordering after erase
        F231  surviving target geometry/reachability/target-value invalidation
        F233  human incremental known-city asymmetry
        F234  duplicate city insertion
        F236  newly knowable pre-existing city omission

    Clean controls:
        * ordinary creation/acquisition/destruction reach the expected callback family.
        * Advanced Start deliberately suppresses incremental city-created callbacks
          during setup; the first real UWAI turn-pre performs a full cache update before
          normal war planning consumes setup state.
        * City construction initializes distance and asset score before current
          AI_targetCityValue use of the passed cache-city wrapper.
        * losing knowledge was not shown to create a symmetric problem because revealed
          map/city knowledge does not simply revert in the same manner.

D. DEPLOYMENT / PATHFINDERS / REACHABILITY
    Challenged:
        createTeamPathFinders
        deleteTeamPathFinders
        resetTeamPathFinders
        City::updateDistance
        pathfinder-era historical changes 2604/2605/2606/2929/2931

    Root-owned:
        F226  valid sea-route loss caused by shorter unusable land path
        F231  failure to recompute geometry after source-city-set changes
        F240  movement-duration versus raw-step search horizon

    Clean controls:
        * historical path-direction/sentinel corrections from 2929/2931 remain present.
        * enemy-owned route segments intentionally use full movement cost.
        * primitive unroaded paths are not evidence for F240 because tile and duration
          metrics approximately coincide there.
        * no null-capital crash state was established in supported cache lifecycle.

E. UNIT / MILITARY INVENTORY / TRANSPORTS
    Challenged:
        createMilitaryBranches
        updateTypicalUnits
        updateTransports
        reportUnitCreated
        reportUnitDestroyed
        reportUnit
        m_iNonNavalUnits producer/consumer population

    Root-owned:
        F214  sticky typical-unit capability
        F218  false trainable transport capability
        F225  missing revival reconstruction
        F237  omitted ordinary air-unit Army power
        F238  nuclear-inclusive headcount / nuclear-exclusive power partition

    Clean controls:
        * normal creation/destruction/upgrade/ownership-transfer paths balance the
          incremental +1/-1 bookkeeping while the cache already exists.
        * the generic reconstruction hole is therefore lifecycle-specific (F225), not
          evidence of a third ordinary transfer imbalance.
        * Army's inclusion of nuclear power itself is intentional; several comments and
          consumers explicitly rely on it. F238 is the narrower population mismatch.
        * current SAS unit XML did not reveal an additional ordinary
          zero-ground/zero-air/non-nuclear military-production junk population in
          m_iNonNavalUnits beyond the already classified air/nuclear issues.

F. WAR UTILITY / THREAT / TARGET MISSIONS / VASSAL VALUES
    Challenged:
        updateWarUtility
        updateWarUtilityIgnDistraction
        updateWarAnger
        updateTargetMissionCounts
        updateTargetMissionCount
        updateThreatRatings
        calculateThreatRating
        teamThreat
        longTermPower
        updateVassalScores
        updateVassalScore
        updateAdjacentLand
        updateLostTilesAtWar
        updateRelativeNavyPower

    Root-owned:
        F220  teammate nuclear deterrence omission
        F221  duplicate loaded-cargo target missions

    Cross-file existing root:
        F112 / KI#435
            updateWarAnger ScaledNum rewrite used owner-team member count where the
            enemy-team member count was required. Independently rediscovered from the
            historical diff; current SAS already contains the reviewed repair.

    Clean controls:
        * m_aiWarUtilityIgnoringDistraction uses MIN_INT as its default sentinel, but no
          caller was proved to interpret an uncomputed sentinel as a favorable war
          result; current callers normalize/clamp or query evaluated targets.
        * updateRelativeNavyPower remains explicitly unimplemented/WIP with no live
          consumer found.
        * historical long-term threat scaling changes remain present.
        * player-specific adjacency/vassal/threat values are intentionally summed or
          evaluated per team member by higher-level war-utility code rather than being
          required to represent a whole team in one cache.

G. DIPLOMACY / SPONSORSHIP / WAR-END STATE
    Challenged:
        updateCanBeHiredAgainst
        setCanBeHiredAgainst
        leaderCache
        reportSponsoredWar
        reportWarEnding
        addTeam
        onTeamLeaderChanged
        isReadyToCapitulate
        setReadyToCapitulate

    Root-owned:
        F215  CanBeHired leader migration
        F216  PA merge membership/domain
        F228  outsider PA history remapping
        F229  compact bounty
        F230  old past-war migration
        F239  elimination/startup war-end cleanup bypass

    Cross-file existing root:
        F196 / KI#519
            ReadyToCapitulate can survive ordinary peace into a later unrelated war.
            Reconstructed again from this cache side and deliberately NOT duplicated.

    Clean controls:
        * reportWarEnding's reparations if/else-if is directional by caller contract:
          one cache receives (received,NULL), the opponent receives (NULL,given).
        * team-leader death calls updateLeaderID/onTeamLeaderChanged before the former
          leader's uwai().uninit(), so authoritative leader-only state is still
          available for migration. F215 is therefore a missing-field/initialization
          problem, not wholesale post-clear copying.
        * a dead sponsor makes HiredHand's stale sponsor ID inert while dead; whether a
          revived sponsor should resume an already-paid still-live obligation is a
          policy question, not a proved lifecycle contract violation like F239.
        * sponsorship disappearing with the particular signing player in a surviving
          multi-member team also lacks a clear source contract requiring transfer to a
          teammate; not promoted.
        * updateCanBeHiredAgainst's local storage is reached through normal team-leader
          scheduling; no independent wrong-cache updater was established.
        * GET_TEAM(PlayerTypes) is an intentional project overload that resolves through
          TEAMID. The earlier F113/KI#436 player/team-ID suspicion remains RETRACTED.

H. TRAITS / SCRUB / SMALL HELPERS
    Challenged:
        updateTraits
        updateCanScrub
        shipSpeed
        helper getters/setters and bulk wrappers

    Root-owned:
        F217  scrub-feature aliasing
        F235  trait cache not refreshed after leader replacement

    Clean controls:
        * updateTraits uses offensive `if` / defensive `else if`; this would miss a
          trait-granted promotion carrying BOTH positive generic combat and city
          defense, but current SAS trait/free-promotion XML contains no such promotion.
          Mod-robustness smell only.
        * current shipSpeed consumers and era gates did not establish a new independent
          cache defect.

I. NESTED UWAICache::City
    Challenged:
        constructor
        cacheCvCity
        write
        read
        attackPriority
        updateDistance
        updateAssetScore

    Root-owned:
        F226  route-type reachability choice
        F227  ordering invalidation
        F231  derived-state invalidation
        F232  legacy distance read
        F234  duplicate wrapper ownership
        F240  raw-step LAND search horizon

    Clean controls:
        * remaining version-gated City fields consume the intended historical layouts.
        * attackPriority's own-team/unreachable sentinel ordering is internally
          consistent with the vector sort direction; no independent sort-sign bug.
        * no additional updateAssetScore arithmetic transition survived comparison.

J. HISTORICAL REGRESSION ARCHAEOLOGY
    Post-2604 substantive UWAICache history was walked as a finite closure checklist.

    Verified retained or already classified:
        2604 TeamPathFinder refactor                  -> F240
        2605 incremental cache optimizations         -> F224/F227/F231/F233/F234 family
        2606 distance tweaks                         -> retained; no new root
        2706 team-game human detection               -> retained
        2719 custom-era handling                     -> retained
        2751 free-city scenario crash repair         -> retained
        2767 enemy cities as land targets            -> retained; save transition clean
        2839 war-utility pushover adjustment          -> retained
        2920 ScaledNum rewrite                       -> F219/F222/F223 + historical F112
        2921 immediate corrections                   -> retained
        2929/2931 deployment-distance fixes          -> retained; F226/F240 distinct
        2947 obsolete building/resource handling     -> retained
        3044 new-player cache inconsistency repair   -> retained
        3183 enum-map rewrite                        -> F229 save refinement / F230
        3496/3518 process/effort valuation           -> retained
        3662 war-success ScaledNum conversion        -> no new divergence established
        4071 long-term threat scaling                -> retained

    This archaeology is important closure evidence because multiple queue-031 findings
    were caused by a later refactor narrowing or algebraically changing an older
    explicit contract.


PARKED FOR ITS OWNING FILE -- DO NOT COUNT AS QUEUE-031 ROOT
-------------------------------------------------------------
queue 064 CvGameCoreDLL/MilitaryBranch.cpp

Strong candidate discovered while following F237:

    bool MilitaryBranch::canKnowTypicalUnit(...) const
    {
        ...
        if (kTypicalUnit.getPrereqAndTech() != NO_TECH) // Warrior
            return true;
        ...
    }

Current SAS XML:
    Warrior has NO prerequisite tech.
    Advanced typical units generally DO have prerequisite techs.

The condition therefore appears opposite to the comment/design: it immediately declares
tech-requiring typical units knowable and leaves the no-tech Warrior to fall through
technology/visibility uncertainty checks.

Potential consequence:
    getTypicalPower(observer) / getTypicalCost(observer) may reveal hidden advanced
    military capability that the observer should only estimate at 80%/85%.

This is implementation-level MilitaryBranch behavior and queue 064 already owns that
file. Preserve the candidate there rather than assigning queue-031 F241.


DURABLE C020 CURSOR
-------------------
COMPLETED FIRST-PASS FILES:
    19 / 169

JUST CLOSED:
    queue 031
    CvGameCoreDLL/UWAICache.cpp
    24 queue-local roots:
        F213 through F240
    NEXT ROOT:
        F241 UNUSED

NEXT FILE -- PRIORITY OVERRIDE
------------------------------
Do not continue numerically to queue 032/042.

The reconciled album's explicit priority order remains:

    priority 1st: queue 041 CvGameCoreDLL/CvSelectionGroupAI.cpp
                  COMPLETE -- C019 durable checkpoint

    priority 2nd: queue 031 CvGameCoreDLL/UWAICache.cpp
                  COMPLETE -- C020 durable checkpoint

    priority 3rd: queue 045 CvGameCoreDLL/CvMapGenerator.cpp
                  PENDING

Therefore the next first-pass file is:

    queue 045
    CvGameCoreDLL/CvMapGenerator.cpp
    approximately 1,281 lines in the queue snapshot

Exact next cursor:
    begin queue 045 CvMapGenerator.cpp from F241 unused.

Recommended opening method:
    1. establish current SAS-vs-Base-AdvCiv semantic delta;
    2. inventory functions and high-risk map-generation state;
    3. trace RNG/determinism, plot/terrain assignment, normalization, starting-plot and
       feature/bonus lifecycle contracts into CvMap/CvPlot/CvGame where needed;
    4. compare K-Mod/Civ4CE only where they retain a cognate implementation;
    5. search bundled AdvCiv history for fixes/refactors touching CvMapGenerator.cpp;
    6. promote F241 only if a concrete supported failure survives those controls.

Clean rejection remains preferable to inventing F241.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP01 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison material:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip

Historical material:
    LLM_Helpers/context/commit_diffs/*
    LLM_Helpers/context/commit_diffs/PATH_HISTORY_INDEX.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior durable checkpoint:
    C020 -- queue 031 UWAICache.cpp COMPLETE

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp
    1,281 current SAS source lines

New roots:
    F241 / provisional KI#564
    F242 / provisional KI#565

Next root:
    F243 UNUSED

OPENING METHOD / SURFACE MAP
----------------------------
Current CvMapGenerator.cpp public/protected implementation families:

    GetInstance
    canPlaceBonusAt
    canPlaceGoodyAt
    addGameElements
    addLakes
    addRivers
    doRiver
    addRiver
    addFeatures
    addBonuses
    addUniqueBonusType
    addNonUniqueBonusType
    placeGroup
    addGoodies
    eraseRivers
    eraseFeatures
    eraseBonuses
    eraseGoodies
    generateRandomMap
    generatePlotTypes
    generateTerrain
    afterGeneration
    setPlotTypes
    getRiverValueAtPlot
    calculateNumBonusesToAdd
    findStandardClimate
    findStandardSeaLevel

Initial high-risk contracts:
    * synchronized MapRand consumption and repeat/new-game determinism;
    * Python default-implementation versus full-override transitions;
    * temporary map-script global-define mutations;
    * river IDs / route termination / recursion;
    * area recalculation after lakes/plot-type changes;
    * OneArea resource area-selection, count caps, clustering and range rules;
    * per-player / eligible-tile bonus quantity arithmetic;
    * starting-map climate/sea-level option normalization;
    * world-size-sensitive goody/resource rules;
    * SAS toggles that reconstruct BTS alternatives.

Current SAS semantic delta from Base AdvCiv is intentionally concentrated:
    * XML-tunable AdvCiv/BTS alternatives for river routing, OneArea distribution,
      tie randomization, clustering, resource player-count scaling, desert lakes and
      peak replacement;
    * KI#202 fix clearing areas_tried before AdvCiv's uncapped second OneArea pass;
    * KI#243/KI#244 Water.py define snapshot/use/restore around addRivers;
    * small static-define lookup caches;
    * signature-collapse/refactoring-only changes.

The opening pass first checked whether those recent statics reintroduced AdvCiv's old
"return to menu -> OOS" map-state bug. Historical commits 2601/2682 with that title
actually concern normalization-level state in CvGame/CvMapGenerator, not cached map
generation defines. The river defines that ARE intentionally mutated by Water.py are
now deliberately non-static and restored by KI#243. No F241 was allocated from the
static-cache suspicion.


F241 / provisional KI#564
-------------------------
TITLE
    AdvCiv's placeGroup extraction dropped OneArea-specific placement bounds: grouped
    copies can exceed the calculated global bonus target and the first-pass per-area
    cap, and current range-2 OneArea Elephants can spill outside the selected area.

STATUS
    CONFIRMED inherited AdvCiv resource-placement regression.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Introduced by practical commit 1268 / e11525d303,
        v0.89rc (advc.129 group-placement refactor).

ORIGINAL BTS / K-MOD CONTRACT
    In K-Mod and Civ4CE/BtS, addUniqueBonusType selects pBestArea, places a center
    resource there, then iterates the XML GroupRange.

    Crucially, every grouped-tile attempt is guarded by BOTH:

        GC.getMap().getNumBonuses(eBonusType) < iBonusCount

    and:

        pLoopPlot->area() == pBestArea

    Thus:
        * the global calculated target is a hard upper bound even inside the final
          cluster;
        * the OneArea cluster stays within the area that the area-selection algorithm
          chose.

    The outer center loop separately stops when the same global target is reached.

ADVCIV EXTRACTION
    Practical commit 1268 extracted the duplicated group-placement block into:

        placeGroup(..., int limit = 100)

    The non-OneArea caller was converted to:

        iBonusCount -= placeGroup(..., iBonusCount);

    preserving its remaining-count upper bound.

    But the OneArea caller became:

        placeGroup(eBonusType, *pPlot, bIgnoreLatitude);

    which uses the default limit 100.

    The extracted helper also collected every canPlaceBonusAt-eligible plot in group
    range without carrying forward:

        pLoopPlot->area() == pBestArea

    from the old OneArea block.

    The practical commit's release note describes the intended change only as:
        randomize eligible group-tile order and reduce placement probability after
        each placed resource so very large clusters become less likely.

    It does not describe intentionally relaxing target counts or allowing a selected
    OneArea cluster to cross area boundaries.

CURRENT GLOBAL-TARGET OVERSHOOT
    Current addUniqueBonusType still has an explicit global target:

        int const iTarget = calculateNumBonusesToAdd(eBonus);

    and the outer placement loop runs only while:

        kMap.getNumBonuses(eBonus) < iTarget

    But after placing the center:

        kRandPlot.setBonusType(eBonus);
        iAdded++;
        iAdded += placeGroup(eBonus, kRandPlot, bIgnoreLatitude);

    placeGroup receives no remaining-global-target limit.

    Therefore, when the center brings the resource count to iTarget, the helper can
    still place one or more grouped copies afterward.

    This is not merely a theoretical zero-target corner:
        calculateNumBonusesToAdd returns at least 1,
        and several current OneArea bonuses have GroupRand 50.

    A target of 1 can therefore become 2+ whenever the final center gets a successful
    group roll.

    K-Mod/Civ4CE stop the group loop as soon as the target is reached.

CURRENT FIRST-PASS PER-AREA CAP OVERSHOOT
    AdvCiv later added a first-pass area cap:

        iAreaLimit = min(2, 3 * areaTiles) + areaTiles / 25

    and tracks:

        iAdded++

        iAdded += placeGroup(...)

    The center-loop condition checks:

        iAdded < iAreaLimit

    only BEFORE placing the center/cluster.

    Because placeGroup again receives its default 100 limit, the last allowed cluster
    can push iAdded beyond iAreaLimit.

    The fact that grouped copies are added into iAdded strongly indicates the cap is
    meant to represent resource instances, not merely cluster centers.

    SAS KI#202 documentation likewise describes the first pass as applying AdvCiv's
    per-area cap before the uncapped second pass.

CURRENT SAME-AREA LOSS IS REACHABLE IN SAS
    Current default AdvCiv-clustering placeGroup builds a PlotCircle around the center
    and checks canPlaceBonusAt, but does NOT require a grouped plot to share the
    center/pBestArea.

    SAS's optional BTS clustering branch, by contrast, explicitly has:

        if (pPlot == NULL ||
            (kBonus.isOneArea() && !pPlot->sameArea(kCenter)))
        {
            continue;
        }

    That branch is a useful internal control: the missing restriction is specific to
    the default AdvCiv path.

    Current SAS XML includes:

        BONUS_ELEPHANTS
            bArea       = 1
            GroupRange  = 2
            GroupRand   = 17

    With range 2, a center near a narrow water gap can have an otherwise eligible land
    tile from another CvArea inside its group circle. The default AdvCiv helper can
    place the grouped Elephant there even though the area-selection/cap logic selected
    and charged pBestArea.

    This matters even though AdvCiv intentionally distributes an old "OneArea" resource
    type across multiple suitable areas: distribution is supposed to occur through the
    explicit area-selection loop. A group spill bypasses:
        * pBestArea selection;
        * areas_tried;
        * suitability ranking;
        * the selected area's cap accounting versus the destination area.

HISTORICAL REINFORCEMENT
    Practical commit 2364 / 2ff9f8bbb3 later substantially reworked OneArea
    distribution and introduced the current per-area cap, but retained the helper call
    without a remaining-count or same-area constraint.

    The commented-out old inline code preserved beside the current call still shows
    both original invariants:
        getNumBonuses(eBonus) < target
        pLoopPlot->area() == pBestArea

    Practical commits 2996/3005 later fixed a separate cluster bug in placeGroup
    (missing canPlaceBonusAt recheck after shuffling) but did not restore these
    OneArea-specific constraints.

    Practical commit 3006 then gave Elephants GroupRange 2, making the missing same-area
    guard materially easier to reach.

BASELINES
    K-Mod:
        strict global target guard inside every grouped placement;
        group member must belong to pBestArea.

    Civ4CE/BtS:
        same two invariants.

    Base AdvCiv 1.14:
        current helper extraction behavior; inherits F241.

    SAS:
        inherits F241 in the default AdvCiv clustering mode.
        SAS's BTS-alternative group branch happens to restore the same-area check, but
        the OneArea caller still does not pass a global remaining-count limit.

REPAIR DIRECTION
    The OneArea caller should pass the actual number of grouped placements still
    permitted by ALL active hard bounds.

    At minimum:
        remainingTarget =
            iTarget - kMap.getNumBonuses(eBonus)

    During the capped first pass, also respect:
        remainingArea =
            iAreaLimit - iAdded

    A combined group limit can use the smaller positive remainder.

    The helper additionally needs an area restriction for this caller, e.g. either:
        * an optional required-area parameter;
        * a OneArea-aware `sameArea(kCenter)` rule when invoked by addUniqueBonusType;
        * or an equivalent caller-specific eligibility predicate.

    Be careful not to impose the OneArea same-area rule on non-OneArea resources.

    Preserve the randomized iteration and exponentially decreasing group probability;
    those are the intended advc.129 behavior.

SCOPE / NEGATIVE CONTROLS
    * addNonUniqueBonusType already passes its remaining target count to placeGroup and
      does not share the global-target overshoot.
    * OneArea resources with GroupRange 0 cannot exhibit the group-specific failure.
    * GroupRange 1 normally cannot bridge two areas if area connectivity already joins
      all immediately adjacent eligible land; current Elephants' range 2 supplies the
      concrete cross-area case.
    * KI#202 is different: it fixes areas_tried not being reset for the second pass.
      F241 concerns what one selected area's cluster is allowed to place.
    * This is not a request to abolish AdvCiv's intentional multi-area distribution of
      bArea resources; it restores the selected-cluster boundaries that the area
      distribution algorithm itself assumes.


F242 / provisional KI#565
-------------------------
TITLE
    AdvCiv's OneArea suitability scan hardcodes latitude checking even when the active
    map script explicitly says bonus placement must ignore latitude, so area ranking is
    based on eligibility rules different from both target counting and actual placement.

STATUS
    CONFIRMED inherited AdvCiv map-script compatibility / resource-distribution defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Introduced with the suitability weighting in practical commit 2364 /
        2ff9f8bbb3, "Tweak placement of area-unique resources."

THREE RELEVANT CONTRACTS
    addUniqueBonusType obtains:

        bool const bIgnoreLatitude =
            GC.getPythonCaller()->isBonusIgnoreLatitude();

    Actual center placement correctly calls:

        canPlaceBonusAt(..., bIgnoreLatitude)

    calculateNumBonusesToAdd also correctly obtains the same Python flag and counts
    possible plots through:

        kPlot.canHaveBonus(eBonus, bIgnoreLatitude)

    Thus both:
        * how many copies should exist;
        * which tiles can actually receive them;

    honor the map script's latitude policy.

SUITABILITY SCAN USES DIFFERENT RULES
    AdvCiv's added area suitability scan instead does:

        if (kTestPlot.isArea(kLoopArea) &&
            canPlaceBonusAt(eBonus, ..., false, false))
        {
            iEligible++;
        }

    The second false intentionally skips expensive range checks.
    The FIRST false is bIgnoreLatitude and is hardcoded.

    Therefore the area selector can score an area as latitude-unsuitable even though
    the active script says latitude is irrelevant and actual placement will accept the
    same plots.

SUPPORTED SHIPPED MAP SCRIPTS
    Current SAS ships multiple scripts that explicitly return True from
    isBonusIgnoreLatitude while delegating many or all bonus types back to the default
    C++ addBonusType path, including:

        Wheel.py
        Hub.py
        Ring.py
        BTG_Grid.py
        BTG_Cross.py

    Fantasy_Realm.py also returns True and uses default C++ placement for bonus classes
    depending on its resource option.

    This is therefore not an unsupported custom-script hypothetical.

CURRENT ONEAREA DATA MAKES LATITUDE MATERIAL
    Many current bArea resources have real latitude constraints, e.g.:

        Banana      max 70
        Cattle      max 65
        Deer        min 45
        Pig         max 70
        Rice        max 55
        Sheep       max 60
        Silk        max 49
        Spices      max 45
        Grapes      max 55
        Elephants   min 40

    On an ignore-latitude map, those limits are deliberately not supposed to affect
    bonus placement.

DETERMINISTIC RANKING DISTORTION
    The suitability term is:

        rSuitability = iEligible / iNumTiles;
        rSuitability = (rSuitability + 2) / 3;
        iValue = round(rSuitability * iValue);

    Consider two otherwise equivalent candidate areas on a script that returns
    bIgnoreLatitude=true.

    If the hardcoded latitude-aware scan sees:
        Area A: 0% latitude-eligible
        Area B: 100% latitude-eligible

    it assigns diluted suitability approximately:
        A: 2/3
        B: 1

    even though under the script's actual placement rule both are 100% eligible with
    respect to latitude.

    Area B can therefore win pBestArea solely because of a restriction that the map
    script explicitly disabled.

    Since areas_tried and the first-pass caps determine where copies are distributed,
    this changes the landmasses receiving the resource, not merely an internal score.

HISTORICAL ORIGIN
    K-Mod/BtS did not have this suitability weighting.

    Practical commit 2364 introduced the scan with the hardcoded:

        false, false

    arguments and described the work as improving distribution of area-unique
    resources. No accompanying rationale says that the new ranking should intentionally
    re-enable latitude on scripts that disable it.

    Current Base AdvCiv retains the mismatch.

REPAIR DIRECTION
    Use the already-computed map-script policy:

        canPlaceBonusAt(
            eBonus,
            kTestPlot.getX(),
            kTestPlot.getY(),
            bIgnoreLatitude,
            false)

    preserving `bCheckRange=false` as the intended performance shortcut.

    This aligns:
        target-count eligibility,
        area suitability,
        final placement

    without adding expensive range checks to the suitability scan.

SCOPE / NEGATIVE CONTROLS
    * Scripts using the normal latitude policy are unaffected.
    * Bonus types with 0..90 latitude range are unaffected by this particular mismatch.
    * This does not claim that AdvCiv's diluted suitability formula is itself wrong.
    * F241 concerns cluster bounds after pBestArea is selected; F242 concerns the rules
      used to rank/select pBestArea.
    * The recent SAS map-generation toggles did not introduce F242; they inherit the
      AdvCiv branch and merely make alternative behavior selectable.


OPENING NEGATIVE CONTROLS SO FAR
---------------------------------
1. Historical "OOS after returning to main menu" commits 2601/2682
    Those CvMapGenerator hunks removed an eager reset of starting-plot normalization
    from generateRandomMap; the persistent state was CvGame normalization level, not
    local map-generator define values.

    Current SAS static caches in addLakes therefore do not by themselves recreate that
    exact historical defect.

2. Water.py temporary river defines
    SAS KI#243 explicitly snapshots the three mutable default-river defines before the
    Python callback, reads their possibly changed values after it, uses them for this
    generation, and restores the globals on both Python-handled and C++-handled exits.

    This avoids the later static-cache regression that had prevented Water's temporary
    settings from taking effect and contaminated subsequent map generations.

3. addLakes super-Huge overflow fix
    Base AdvCiv practical commit 4030 changed the expected-lake adjustment to divide
    the scaled candidate ratio before multiplying by LAKE_PLOT_RAND. Current SAS
    retains that order. No regression found in the opening comparison.

4. canPlaceBonusAt unique-range fix
    Base AdvCiv practical commit 2366 repaired the per-bonus unique range that an
    earlier AdvCiv change had ignored. Current SAS retains separate BonusClass range
    and Bonus unique range checks.

5. placeGroup shuffled revalidation
    Base AdvCiv practical commits 2996/3005 repaired a v0.98 cluster failure by
    re-running canPlaceBonusAt on the shuffled candidate immediately before placement.
    Current SAS retains that recheck.


C020-WIP01 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565

NEXT ROOT:
    F243 UNUSED

Resume from F243 unused.

Best next seams:
    * complete addUnique/addNonUnique/placeGroup target/count/area/RNG matrix;
    * audit addLakes expected-count arithmetic and area-recalc lifecycle beyond the
      already-retained super-Huge overflow fix;
    * audit addRivers/doRiver/addRiver recursion, river IDs, temporary define restoration
      and synchronized RNG;
    * inspect the 91-path historical commit list by substantive change rather than
      blindly reading formatting/merge diffs;
    * challenge generateRandomMap's disabled climate/sea-level option normalization and
      Python override ordering;
    * compare current SAS toggled BTS alternatives against actual K-Mod/Civ4CE behavior.

Clean rejection remains preferable to inventing F243.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


LOCAL RECONCILIATION AFTER C019 DURABLE CLOSE AND C020-WIP01 IMPORT
------------------------------------------------------------------
- The append-only album now closes queue 031 `UWAICache.cpp` with F213-F240 / provisional KI#536-KI#563 and opens priority queue 045 `CvMapGenerator.cpp` with F241-F242 / provisional KI#564-KI#565.
- Current source and Known Issues independently implement F213-F229 / KI#536-KI#552. F230/KI#553 concerns old-save conversion and remains intentionally unpromoted under AdvCiv-SAS's current-version-save-only policy. The current KI#538-KI#539 batch includes C019's later F216 refinement: Permanent Alliance history now uses both the original survivor-member recipient boundary and the full persistent civilization-team target-key domain.
- F229/KI#552's live full-width-to-short saturation remains implemented. Its newly recorded pre-v9 read refinement is intentionally not added because old save formats are unsupported. F232/provisional KI#555 is rejected for the same policy reason; F231 and F233-F240 / provisional KI#554 and KI#556-KI#563 remain pending independent review.
- C020-WIP01's F241-F242 / provisional KI#564-KI#565 are newly imported map-generator findings and remain pending independent review. Current source, Known Issues and this later reconciliation are authoritative for implementation state. (GPT-5.6-Sol)

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP02 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    _SNAPSHOT_CONTEXT/pending_upstream/*
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C020-WIP01

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New roots:
    F243 / provisional KI#566
    F244 / provisional KI#567

Next root:
    F245 UNUSED


F243 / provisional KI#566
-------------------------
TITLE
    AdvCiv snapshots every lake-eligible plot before placing any lakes; unlike BtS,
    eligibility is therefore not re-evaluated after earlier successful lakes turn into
    water, so adjacent candidates can both become random lakes even though the later
    one would have become coastal and ineligible in the original algorithm.

STATUS
    CONFIRMED inherited AdvCiv map-generation semantic regression.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Introduced by the AdvCiv desert-lake rework merged at practical 3898 /
        40bd5f76d9
    (upstream change "Make desert lakes less likely (standard map generator)").

BTS / K-MOD / CIV4CE CONTRACT
    The original loop processes plots sequentially:

        for every map plot:
            if !water && !coastalLand && !river:
                roll lake chance
                if success:
                    setPlotType(PLOT_OCEAN)

    setPlotType defaults to immediate area recalculation in these implementations.

    Therefore lake placement changes the eligibility of plots that have not yet been
    visited.

    CvPlot::isCoastalLand(0) returns true for ordinary land with any adjacent
    non-impassable water plot. Consequently, if candidate B is adjacent to earlier
    candidate A and A becomes a lake first, B is coastal by the time B is visited and
    BtS/K-Mod do not roll/place a lake on B.

ADVCIV CHANGE
    The desert-lake change wanted to make desert candidates less likely while preserving
    the overall expected number of lakes.

    To know how many candidates are desert, it first scans the ENTIRE map and stores:

        std::vector<std::pair<CvPlot*,bool> > apbCandidates;

    using:

        if (!p.isWater() && !p.isCoastalLand() && !p.isRiver())
            apbCandidates.push_back(...);

    Only after this complete snapshot does it perform the random rolls.

    The introduction diff even comments:

        "(Same condition as in BtS)"

    but the timing of that condition is no longer the same. It is evaluated once
    against the original water layout rather than immediately before each plot's roll.

    The initial AdvCiv version still converted successful candidates immediately inside
    the second loop. That does NOT restore BtS semantics because later candidates are
    never rechecked for coastal status after the conversion.

LATER BATCH-RECALC OPTIMIZATION
    Practical 4030 later accumulated successful candidates in apLakes and called
    setPlotType only afterward, recalculating areas on the last conversion.

    That makes the batching more explicit, but it is NOT the origin of F243. The
    eligibility loss already exists as soon as the candidate set is precomputed in the
    earlier desert-lake change.

CURRENT SAS
    Current SAS retains the precomputed candidate vector and later apLakes batch.

    SAS optionally restores BtS peak replacement through
        SAS_MAP_LAKE_PEAK_REPLACEMENT_ENABLE,
    but that toggle does not alter the frozen coastal-eligibility snapshot.

SUPPORTED REPRODUCTION
    Let A and B be adjacent inland land plots that initially satisfy:
        !isWater
        !isCoastalLand
        !isRiver
        and, under current default AdvCiv behavior, !isPeak.

    Assume A precedes B in the candidate traversal and both legal synchronized lake
    rolls succeed.

    BtS/K-Mod/Civ4CE:
        1. A is processed and becomes PLOT_OCEAN.
        2. When B is reached, B.isCoastalLand() now sees adjacent water A.
        3. B is skipped.
        Result: one random lake tile.

    Current AdvCiv/SAS:
        1. A and B were both inserted into apbCandidates before any water conversion.
        2. Their rolls are performed from that frozen eligibility set.
        3. Both successful candidates are put in apLakes / converted.
        Result: a two-tile random lake.

    No invalid XML, custom map or runtime mutation is needed; only a legal map seed
    whose synchronized rolls succeed for two adjacent candidates.

    With BtS's standard LAKE_PLOT_RAND of 160 the event is uncommon for any given pair,
    but a map contains many candidate pairs and reproducibility requires only one legal
    RNG state, not a high probability.

BROADER EFFECT
    Chains of adjacent candidates can form multi-tile randomly generated inland lakes.
    The specific original guard was intended to reject land that had become coastal
    after a prior lake placement.

    This also changes synchronized RNG consumption relative to the original:
        BtS does not even perform B's lake roll once A has made B coastal;
        AdvCiv still performs B's pre-scheduled roll.
    That is not an OOS within one DLL version, but it is further proof that the
    precomputation is not behaviorally equivalent to BtS.

WHY THIS IS NOT MERELY "ADVANCED LAKE VARIETY"
    The introducing change is documented as a probability adjustment for desert lakes
    while preserving expected lake count; it explicitly labels its eligibility
    predicate "Same condition as in BtS."

    No historical rationale found says the change intentionally allowed adjacent random
    lakes or intentionally froze coastal eligibility.

    The semantic difference is therefore collateral to the candidate-counting
    refactor, not the stated feature.

REPAIR DIRECTION
    Preserve the desert-candidate statistics without freezing dynamic coastal
    eligibility.

    A straightforward semantic repair can still precompute the initial candidate set
    for probability normalization, but immediately before accepting each successful
    candidate re-check the dynamic placement conditions that prior lake placements can
    invalidate.

    Because current code defers plot conversion until after all rolls, merely calling
    p.isCoastalLand() during the roll loop is insufficient: earlier selected lakes are
    not water yet.

    Options include:
        * convert each accepted lake immediately, restoring sequential semantics, then
          optimize the expensive area recalculation separately;
        * or retain batched conversion while maintaining the selected-lake set and
          reject a candidate adjacent to any already selected lake, in addition to the
          original preexisting-water/coastal eligibility.

    If batching conversions without recalculating areas, take care with helpers that
    consult CvArea state. An explicit selected-lake adjacency test is the least
    ambiguous way to preserve the particular BtS `isCoastalLand(0)` invariant while
    keeping one final area rebuild.

    Preserve the intentionally adjusted desert probability and expected-count formula.

BASELINE
    Base AdvCiv 1.14:
        inherits F243.

    K-Mod 1.46:
        retains sequential immediate eligibility/placement.

    Civ4CE BtS:
        retains sequential immediate eligibility/placement.

SCOPE / NEGATIVE CONTROLS
    * A lone lake candidate is unaffected.
    * Candidate pairs that are not adjacent are unaffected by this particular dynamic
      coastal-state issue.
    * River status is already established before addLakes and does not change during
      the lake loop, so the live invalidated predicate is specifically water/coastal
      eligibility.
    * The 4030 overflow fix for super-Huge expected-lake arithmetic remains correct and
      is independent of F243.
    * This finding does not assert that multi-tile lakes are inherently bad map design;
      it identifies an unintended behavioral change relative to the algorithm whose
      probability distribution the AdvCiv change says it is preserving.


F244 / provisional KI#567
-------------------------
TITLE
    eraseRivers removes river edges but leaves per-plot river IDs and the map's next
    river ID intact; regenerateGameElements can therefore regenerate against invisible
    old-river barriers, and AdvCiv's short river-ID counter can accumulate toward
    overflow across repeated regenerations.

STATUS
    CONFIRMED inherited Civ4/BtS river-regeneration lifecycle defect.
    Present in BtS/Civ4CE and K-Mod.
    AdvCiv partially mitigates the first post-start call by clearing plot river IDs at
    initial game setup, but repeated `regenerateGameElements` calls recreate the defect.
    Current AdvCiv/SAS additionally store IDs in signed short.

PUBLIC API CONTRACT
    CyMap exposes:

        regenerateGameElements()

    with the Python-interface description:

        "used to regenerate everything but the terrain and height maps"

    Its implementation does:

        eraseRivers();
        eraseFeatures();
        eraseBonuses();
        eraseGoodies();
        addGameElements();

    This is therefore a real supported regeneration lifecycle, not a private dead
    helper.

WHAT eraseRivers ACTUALLY CLEARS
    CvMapGenerator::eraseRivers iterates plots and clears only visible river-edge flags:

        if (kPlot.isNOfRiver())
            kPlot.setNOfRiver(false, NO_CARDINALDIRECTION);
        if (kPlot.isWOfRiver())
            kPlot.setWOfRiver(false, NO_CARDINALDIRECTION);

    CvPlot::setNOfRiver(false, ...) and setWOfRiver(false, ...) update edge state,
    river crossings, yields, area river-edge counts and graphics.

    They do NOT call setRiverID(-1).

    CvPlot::m_iRiverID therefore survives eraseRivers.

    CvPlot::erase, by contrast, explicitly performs:
        setNOfRiver(false, ...)
        setWOfRiver(false, ...)
        setRiverID(-1)

    confirming that river ID is separate lifecycle state.

WHY STALE IDS ALTER THE NEXT GENERATED NETWORK
    doRiver assigns a fresh river ID for a newly started river and immediately does:

        short iOtherRiverID = pStartPlot->getRiverID();
        if (iOtherRiverID != -1 && iOtherRiverID != iThisRiverID)
            return; // Another river already exists here

    During normal river generation every traversed river corner receives iThisRiverID.

    After eraseRivers:
        * its river edges are gone;
        * but those old corners still retain the old ID.

    A replacement river uses a new ID. If its traversal reaches one of those invisible
    old-ID corners, doRiver stops as though another river still occupies the corner.

    The erased network therefore remains as hidden barriers constraining the supposedly
    regenerated network.

BTS / K-MOD
    BtS/Civ4CE and K-Mod have the same basic eraseRivers implementation and doRiver
    old-ID collision check.

    Their initial river IDs persist after normal generation, so even the first
    post-generation `regenerateGameElements` call can regenerate against stale IDs.

ADVCIV PARTIAL MITIGATION AND REPEATED-CALL REPRODUCTION
    Practical 2172,
        10f79cd394
        "Clear river ids at game start, store as short int."
    added to CvGame::setInitialItems:

        // River ids shouldn't be used after map generation
        for every plot:
            setRiverID(-1);

    Thus, once an ordinary AdvCiv game has completed initial setup, the original
    generation's plot IDs are cleared.

    However, `regenerateGameElements` later:
        1. builds a fresh river network through addRivers;
        2. assigns new per-plot river IDs;
        3. does NOT run CvGame::setInitialItems afterward.

    Therefore:

        first post-start regenerateGameElements:
            starts from the initial -1 plot IDs and can generate normally;
            leaves newly generated IDs behind.

        second regenerateGameElements:
            eraseRivers removes the first regenerated network's edges but not IDs;
            the second regenerated network encounters those stale IDs and is blocked/
            truncated wherever its fresh river ID meets them.

    That is a deterministic lifecycle-state violation even though the exact visual
    difference depends on the synchronized river routing outcome.

NEXT-RIVER-ID COUNTER IS ALSO NOT RESET
    CvMap::m_iNextRiverID is initialized to 0 by CvMap reset/rebuild.

    eraseRivers/regenerateGameElements never reset it.

    Every new doRiver start consumes:

        getNextRiverID();
        incrementNextRiverID();

    even if that candidate then immediately returns because of a stale different ID.

    In BtS/K-Mod this is an int and is mostly a cleanliness issue.

    AdvCiv practical 2172 compacted both:
        CvPlot::m_iRiverID
        CvMap::m_iNextRiverID
    to signed short, specifically because river IDs were considered map-generation-only
    data.

    Repeated element regeneration can therefore monotonically push the short counter
    toward 32767 and eventual wrap/sentinel/collision behavior.

    A single normal SAS map-generation pass was not shown to exceed short range on
    supported world sizes, so do NOT split this into a separate overflow root.
    It is a lifecycle-amplification consequence of failing to reset river generation
    state when explicitly regenerating rivers.

FULL MAP REGENERATION IS A NEGATIVE CONTROL
    CvGame::regenerateMap performs:

        kMap.erasePlots();

    and CvPlot::erase explicitly clears setRiverID(-1).

    It then rebuilds the map generation flow.

    Therefore F244 is specifically about the lighter
        CyMap::regenerateGameElements
    lifecycle, not a claim that full WorldBuilder map regeneration always carries stale
    per-plot IDs.

REPAIR DIRECTION
    `eraseRivers` / the regenerate-elements lifecycle should clear ALL state belonging
    to the erased river-generation graph, not merely visible edge flags.

    At minimum:
        for every plot:
            clear N/W river edges as today;
            setRiverID(-1);

    Because m_iNextRiverID has no semantic reason to preserve uniqueness across fully
    erased river generations and is short in AdvCiv, also reset it to the generation
    baseline (normally 0) before adding the replacement rivers.

    This may be best represented by a small CvMap river-generation-state reset helper
    rather than exposing raw counter mutation.

    Do not clear river IDs during a live ordinary partial edge edit; this fix belongs to
    the explicit whole-river-network erase/regenerate operation.

HISTORICAL CONTEXT
    AdvCiv 2172 explicitly says:

        "River ids shouldn't be used after map generation"

    and labels m_iRiverID:
        "Only used during map gen."

    `regenerateGameElements` starts a new map-generation phase for rivers, so it should
    establish the same clean river-ID precondition as original generation.

SCOPE / NEGATIVE CONTROLS
    * Current setNOfRiver/setWOfRiver not clearing riverID is reasonable for ordinary
      single-edge mutation; those setters alone are not the root.
    * Full CvPlot::erase clears riverID correctly.
    * First post-start AdvCiv regenerate is partially protected by setInitialItems'
      initial ID clear; second and later regenerations are not.
    * This is not an OOS claim. It is deterministic stale state inside one generated
      game/map.
    * No separate F245 is allocated for theoretical short overflow without a practical
      one-pass reproduction; preserve it under F244's repeated-regeneration lifecycle.


OTHER RIVER/LAKE CHECKS IN THIS PASS
------------------------------------
1. CvPlot::isCoastalLand semantics
    Current default iMinWaterSize is 0; any adjacent non-impassable water plot therefore
    makes ordinary land coastal. This confirms F243's adjacent-lake reproduction.

2. `setPlotType(PLOT_OCEAN)` default behavior
    BtS/K-Mod call it with default bRecalculate=true, so an earlier successful lake is
    fully visible to the eligibility check of later sequential plots.

3. Current lake batch area rebuild
    Current AdvCiv/SAS uses false/false for every accepted lake except the last and
    recalculates on the final conversion. No independent stale-area consumer was found
    between those conversions; F243 arises earlier from the frozen candidate set.

4. River ID clearing at ordinary startup
    AdvCiv's explicit post-normalization setRiverID(-1) remains present and explains why
    F244's strongest AdvCiv reproduction is repeated element regeneration rather than
    necessarily the first call.

5. `CvGame::regenerateMap`
    Uses CvPlot::erase through map erase and therefore clears river IDs. Negative
    control retained.


C020-WIP02 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567

NEXT ROOT:
    F245 UNUSED

Resume from F245 unused.

Best next work:
    * finish addRivers/doRiver/addRiver/getRiverValueAtPlot recursion, RNG and
      source-density contracts;
    * inspect substantive historical river changes, especially 2172, 2298 and later
      RNG/refactor commits, for regressions not already covered by F244;
    * finish addLakes arithmetic and map-recalc negatives;
    * continue generateRandomMap climate/sea-level normalization and Python override
      ordering;
    * audit calculateNumBonusesToAdd, addGoodies and remaining count/range arithmetic;
    * compare SAS's selectable BTS branches against actual K-Mod/Civ4CE semantics;
    * cross-check the entire earlier album before assigning F245.

Clean rejection remains preferable to inventing F245.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP03 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    _SNAPSHOT_CONTEXT/pending_upstream/*
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C020-WIP02

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New root:
    F245 / provisional KI#568

Next root:
    F246 UNUSED


F245 / provisional KI#568
-------------------------
TITLE
    AdvCiv's OneArea "small islands only after two-thirds of the target exists" guard
    can deadlock resource placement at zero when every land area is below
    4 * NUM_CITY_PLOTS; the nominally uncapped second pass repeats the same bootstrap
    exclusion and cannot recover.

STATUS
    CONFIRMED inherited AdvCiv resource-distribution defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS when the AdvCiv OneArea
    distribution path is enabled.

    Introduced by practical commit 1674 / f25a2b91ab,
        "Relax single-landmass restrictions for resource placement"
        [advc.129]

CURRENT CODE
    addUniqueBonusType calculates:

        int const iTarget = calculateNumBonusesToAdd(eBonus);

    and later, while scoring candidate areas:

        int iAddedTotal = kMap.getNumBonuses(eBonus);
        if (bSASAdvCivOneAreaBonusDistribution &&
            iAddedTotal * 3 < 2 * iTarget &&
            iNumTiles < 4 * NUM_CITY_PLOTS)
        {
            continue;
        }

    NUM_CITY_PLOTS is 21 in the current DLL, so the hard small-area cutoff is:

        4 * 21 = 84 tiles.

    calculateNumBonusesToAdd ends with:

        return std::max(1, iBonusCount);

    Therefore every bonus type reaching this function has a positive target even if
    the density calculation happened to round to zero.

BOOTSTRAP FAILURE
    At the start of placement for a normal absent bonus:

        iAddedTotal = 0

    For every positive target:

        0 * 3 < 2 * iTarget

    is true.

    Therefore every area smaller than 84 tiles is unconditionally skipped until at
    least two-thirds of the target has ALREADY been placed elsewhere.

    If every land area on the map is smaller than 84 tiles:
        * every candidate area is skipped;
        * pBestArea remains NULL;
        * the placement while-loop breaks;
        * the target remains completely unmet.

THE SECOND PASS DOES NOT FIX THIS
    AdvCiv has two passes because its first pass imposes a per-area resource cap.

    Current SAS correctly includes KI#202:

        if (iPass > 0)
            areas_tried.clear();

    allowing the second pass to revisit first-pass areas.

    The second pass also disables the per-area cap.

    However, it executes the SAME area-selection small-island guard.

    When the first pass placed zero copies:

        iAddedTotal is still 0

    so every <84-tile area is skipped again.

    Thus:
        capped first pass -> zero
        uncapped second pass -> zero

    Clearing areas_tried cannot break the bootstrap deadlock.

HISTORICAL INTENT MAKES THE FAILURE ESPECIALLY CLEAR
    The introducing commit message says:

        "And I've added a limit for the number of resources of one type to be placed
         on a landmass: Can only place up to
         2 + ((number of tiles on the landmass) / 25).
         Excess resources are placed elsewhere; that can include small islands.
         (K-Mod had prevented single-landmass resources from getting placed on small
         islands - so islands resembling the Spice Islands had not been possible.)"

    The explicit design goal was therefore to ALLOW excess resources to reach small
    islands.

    Yet the new guard requires a large-enough area to bootstrap at least two-thirds of
    the target before any small island can even compete.

    In an all-small-island world, the prerequisite can never become true.

    This is not an intentional "prefer large landmasses" result; it is a self-blocking
    implementation of that preference.

BROADER UNDERFILL CASE
    The complete-zero case is the cleanest proof, but the defect is not limited to maps
    with literally no large area.

    Suppose one >=84-tile landmass exists, but it cannot supply two-thirds of iTarget
    because:
        * terrain/feature eligibility is sparse;
        * latitude removes many otherwise valid plots;
        * unique/range restrictions crowd out remaining copies;
        * the first-pass area cap and later physical eligibility limit placement.

    As long as:

        iAddedTotal * 3 < 2 * iTarget

    all otherwise-valid small islands remain excluded.

    Once every viable large area has been tried, pBestArea can again become NULL and
    the pass can terminate under target even though legal small-island plots exist.

SHIPPED-MAP SUPPORT
    This is not restricted to malformed custom maps.

    Current SAS ships Archipelago.py, including a "Tiny Islands" mode whose map
    generation uses high continent grain and delegates ordinary default bonus
    placement back to the DLL.

    Islands.py and other island-heavy scripts provide similar supported map shapes.

    A legal generated map in which all landmasses are below 84 tiles is therefore
    within the intended supported map space.

CURRENT SAS CONTENT AFFECTED
    Current SAS CIV4BonusInfos.xml has many bArea=1 resources, including:

        BONUS_BANANA
        BONUS_MAIZE
        BONUS_CATTLE
        BONUS_DEER
        BONUS_PIG
        BONUS_RICE
        BONUS_SHEEP
        BONUS_WHEAT
        BONUS_DYE
        BONUS_INCENSE
        BONUS_ELEPHANTS
        BONUS_SILK
        BONUS_SPICES
        BONUS_SUGAR
        BONUS_GRAPES

    So F245 is capable of suppressing broad food/luxury/resource variety on sufficiently
    fragmented maps, not merely one obscure strategic resource.

K-MOD / BTS BASELINE
    K-Mod's older OneArea selection logic does NOT hard-exclude areas below
    4 * NUM_CITY_PLOTS.

    It computes a base value from:

        iNumTiles / iNumUniqueBonusesOnArea

    and only grants the additional random size bonus when the area is large enough.

    Small areas therefore rank worse on average but remain eligible.

    BtS/Civ4CE likewise do not contain AdvCiv's hard "must already have 2/3 target"
    bootstrap exclusion.

HISTORICAL CONTINUITY
    Practical 1674 introduced the guard together with the two-pass/per-area-cap design.

    Practical 2364 later substantially reworked OneArea suitability/ranking but retained
    the same guard.

    Current Base AdvCiv 1.14 retains it.

    SAS KI#202 correctly repairs a separate inherited two-pass problem by clearing
    areas_tried before pass two, but deliberately leaves this selection rule unchanged;
    F245 therefore remains after KI#202.

REPAIR DIRECTION
    Preserve AdvCiv's preference against consuming tiny islands too early, but make it
    a preference rather than a bootstrap dead end.

    Viable directions include:

        1. Skip small islands only while at least one not-yet-tried viable large area
           remains.

        2. Allow the uncapped second pass to consider small islands regardless of the
           two-thirds threshold.

        3. Rank small islands with a penalty rather than `continue`-excluding them.

        4. If a pass finds no selectable large area while the target is unmet, retry the
           area selection with the small-island exclusion relaxed.

    Option 2 is particularly close to the original two-pass comment:
        first pass controls distribution;
        second pass exists specifically to avoid ending with too few resources.

    Any repair should still respect:
        * actual `canPlaceBonusAt` eligibility;
        * F241's restored cluster global/area bounds;
        * F242's map-script latitude policy;
        * AdvCiv's intended distribution of formerly single-landmass resource types.

DISTINCTION FROM EXISTING QUEUE-045 ROOTS
    F241:
        selected-area cluster can exceed target/cap or spill outside the selected area.

    F242:
        area suitability is scored with the wrong latitude policy.

    F245:
        potentially suitable small areas are never allowed to enter the competition
        until an externally impossible bootstrap count is reached.

    The fixes are therefore complementary rather than duplicates.

NEGATIVE CONTROLS / SCOPE
    * If a large-enough area can provide at least two-thirds of the target, the guard
      eventually opens and small islands become eligible.
    * If `SAS_MAP_ADVCIV_ONE_AREA_BONUS_DISTRIBUTION_ENABLE` is disabled, SAS takes its
      BTS-style path and this specific AdvCiv bootstrap guard is disabled.
    * The existence of `std::max(1, iBonusCount)` is not itself a bug; it simply makes
      the zero-placement contradiction exact.
    * This does not claim every tiny-island map should meet every XML target perfectly;
      it proves that valid small-area placement opportunities are rejected solely
      because the algorithm requires copies to exist before it permits the only areas
      capable of creating those copies.


ADDITIONAL C020-WIP02 -> WIP03 NEGATIVE CONTROLS
------------------------------------------------

1. addRivers / doRiver ID consumption
    A fresh river ID is consumed before some starts are rejected. In normal one-shot
    generation that is harmless uniqueness sparsity.

    Under repeated regenerateGameElements it amplifies F244's never-reset short counter,
    so preserve it under F244 rather than allocate a separate root.

2. AdvCiv river-bank terrain scoring change
    Historical practical 2298 changed river direction scoring to account for terrain on
    both banks. Current code retains the intended form; no later sign/direction
    regression was established.

3. SAS river-routing randomness profile
    SAS exposes the AdvCiv routing algorithm and direction-randomness knobs separately.
    Documentation explicitly states that the BTS-style routing profile should pair the
    BTS routing branch with the BTS randomness value. No hidden "one toggle claims to
    reproduce all BTS river semantics" contract was found; not a root.

4. Local river RNG seed arithmetic
    Some coordinate/hash arithmetic can overflow 32-bit signed int under abstract C++
    rules. Under the actual target VC2003/32-bit environment it yields deterministic
    machine wrapping, and no supported cross-peer divergence or map corruption was
    established. Portability smell only.

5. addLakes expected-count arithmetic
    Base AdvCiv practical 4030 fixed the super-Huge overflow/order issue by dividing the
    scaled candidate ratio before multiplying by LAKE_PLOT_RAND. Current SAS retains
    the corrected arithmetic.

6. Batched lake area recalculation
    Current AdvCiv/SAS delays area recalculation until the final accepted lake. No live
    intermediate consumer was found between those conversions. The confirmed semantic
    issue is F243's frozen dynamic eligibility, not an independent stale-CvArea use.

7. calculateNumBonusesToAdd
    The sublinear eligible-tile formula, per-player scaling and later player-count
    historical adjustments were checked against their introducing commits and remain
    consistent with documented intent.

    No zero-divisor route was found in the normal XML/map contract.

8. addGoodies
    Current SAS has one normal goody improvement (Goody Hut), with positive
    TilesPerGoody and range.

    Existing spacing/world-size/rounding changes trace to documented historical tweaks.
    No practical short/int range failure or pass-order defect was established.

9. canPlaceBonusAt
    Current unique-range repair from practical 2366 remains present.

    Existing bonus occupancy is rejected by canHaveBonus, so the OneArea suitability
    scan does not count already occupied plots as eligible.

    MinLandPercent arithmetic retains its denominator guard.

10. addFeatures
    The default feature loop cannot simply overwrite a feature placed by an earlier
    entry because current canHaveFeature checks the existing feature state.

    Current positive default-appearance features do not establish a conflicting
    replacement case.

11. generateRandomMap disabled climate/sea-level options
    Practical 4242 moved normalization of disabled map options before Python
    beforeGeneration / plot / terrain generation.

    CvMap::getClimate and getSeaLevel read InitCore, so shipped scripts such as Donut
    that disable a climate option but later use map climate during terrain generation
    receive the normalized standard value in time.

    Tectonics explicitly normalizes/overrides its own relevant climate behavior.
    No current early-hook consumer requiring the old value was established.

12. findStandardClimate / findStandardSeaLevel
    Current XML supplies a usable unique standard climate/sea-level entry under the
    helper's criteria. No unsupported -1 result in normal data was established.

13. generateTerrain Python result contract
    The strict expectation that a Python override return one terrain result per plot is
    consistent with the inherited map-script contract. Malformed custom-script output
    is not promoted as a DLL bug.

14. setPlotTypes area recalculation
    Areas are recalculated after plot-type assignment. Subsequent shallow/deep water
    terrain assignment does not change land/water CvArea connectivity, so no missing
    second area rebuild was found.

15. addBonuses randomized tie ordering
    Current SAS uses CvRandom::shuffle(vector), whose vector overload initializes the
    vector with iota before shuffling. The apparently default-zero vector contents are
    therefore not shuffled duplicates; no bug.

16. static local map-generation define caches
    The known Water.py-mutated river defines are intentionally NOT left in immutable
    statics; KI#243 snapshots/reads/restores them around the Python hook.

    No shipped runtime mutator was found for the other reviewed cached map-generation
    defines. The old "return to main menu -> OOS" map-generator history concerned
    normalization/game state, not these local immutable options.

17. selectable BTS OneArea distribution
    Disabling SAS's AdvCiv OneArea distribution removes F245's bootstrap guard and
    returns to a BTS/K-Mod-style area-value path.

    F241's OneArea grouped-copy global-target issue remains separately relevant because
    its caller-side group limit is independent of the area-selection toggle.

18. eraseBonuses and `m_aebBalancedBonuses`
    eraseBonuses clears map bonus instances but does not directly clear the game's
    balanced-bonus bookkeeping set.

    The set's current consumers/producers were traced into CvGame normalization and
    TrueStarts. Under a normal regenerateGameElements call with unchanged map script /
    options, the same resource-handling policy is reused, and no supported stale-policy
    transition was established.

    Preserve as a lifecycle seam to challenge if a legal same-map script-option change
    can be shown; do not promote from stale-looking storage alone.


SUBSTANTIVE HISTORY CHECKED SO FAR
----------------------------------
CvMapGenerator-affecting history reviewed includes:

    1674  small-island / OneArea distribution       -> F245
    2172  river-ID lifecycle/short storage          -> F244 context
    2298  river terrain-bank scoring                -> retained
    2364  OneArea suitability                       -> F242; retains F245
    2366  unique bonus range                        -> retained
    2368  resource density                          -> retained
    2375  goody spacing                             -> retained
    2534  pass-specific river source ranges         -> retained
    2852  rounding-helper refactor                  -> retained
    2986  bonus-generation refactor                 -> no new root yet
    2996/3005 group placement revalidation          -> retained; F241 distinct
    3006  Elephant group-range change               -> strengthens F241
    3209  RNG simplification                        -> no new divergence yet
    3712/3713 balanced resources                    -> under lifecycle review
    3912  bonus RNG-term refactor                   -> no new divergence established
    3919  player-count scaling                      -> retained
    4030  lake expected-count overflow              -> retained
    4242  disabled climate/sea-level normalization  -> retained
    SAS 6114 XML-tunable AdvCiv/BTS alternatives    -> no new SAS-only root yet
    SAS 6117 KI#202 areas_tried reset               -> retained
    SAS 6212 Water.py river define restoration      -> retained


C020-WIP03 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568

NEXT ROOT:
    F246 UNUSED

Resume from F246 unused.

Best next work:
    * finish regeneration lifecycle for bonuses/features/goodies and balanced-resource
      bookkeeping, but require a supported policy-changing state before promoting;
    * continue addRiver/doRiver/addRiver/getRiverValueAtPlot source-density, recursive
      stopping and RNG contracts;
    * finish SAS 6114 selectable-BTS comparison against actual K-Mod/Civ4CE behavior;
    * inspect remaining substantive history rather than formatting-only path changes;
    * perform a whole-file producer/RNG/state inventory before considering queue 045
      complete;
    * cross-check the full album before allocating F246.

Clean rejection remains preferable to inventing F246.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

LOCAL RECONCILIATION AFTER C020-WIP03 IMPORT AND KI#564-KI#565 REVIEW
--------------------------------------------------------------------
- C020-WIP02 and C020-WIP03 are preserved as ChatGPT-5.6-Sol's append-only investigation. They add F243-F245 / provisional KI#566-KI#568; all three remain pending independent review and implementation.
- Current source and Known Issues now implement F241/KI#564 by bounding grouped OneArea copies by the remaining global and active per-area allowances and restoring the same-landmass invariant in AdvCiv clustering. They implement F242/KI#565 by using the map script's latitude policy during both area ranking and placement.
- Current-build Huge Hub, Wheel, BTG Grid and Archipelago maps generated successfully. BTG Grid saved/reloaded successfully, and Archipelago with Tiny Islands completed a turn-101 autoplay. The exact hidden counts, boundaries and rankings remain source-verified.
- Current source, Known Issues and this reconciliation are authoritative for implementation state; the earlier checkpoint's pending label for F241-F242 records its state before local review. (GPT-5.6-Sol)


===============================================================================
CHECKPOINT C020-WIP04 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    _1_AdvCiv-SAS/Docs/README_Known_Issues.md
    current SAS XML/resource data

Prior physical checkpoint:
    C020-WIP03

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New root:
    F246 / provisional KI#569

Next root:
    F247 UNUSED


F246 / provisional KI#569
-------------------------
TITLE
    SAS KI#202 lets the uncapped OneArea pass revisit capped areas, but the inherited
    area-score denominator still assumes the current resource is absent and adds
    "plus this one"; on a revisited area the resource is already counted, so it is
    double-counted and can make an otherwise viable recovery area score zero.

STATUS
    CONFIRMED AdvCiv-SAS follow-up regression exposed by the correct KI#202 repair.
    Current Base AdvCiv 1.14 does not have this exact reachable revisit state because
    its broken pass-two `areas_tried` lifetime prevents first-pass selected areas from
    being revisited.

    Introduced into reachable SAS behavior by:
        SASBranch practical 6117 / 91454e007c
        "(Fixed) Let AdvCiv's uncapped OneArea-resource pass revisit capped landmasses
         (KI#202)"

    KI#202 itself remains a valid fix for the inherited Base AdvCiv bug. F246 is a
    second stale assumption that became observable once that intended second-pass
    behavior was restored.

CURRENT AREA SCORE
    In addUniqueBonusType, each candidate area uses:

        // number of unique bonuses starting on the area, plus this one
        int iNumUniqueBonusesOnArea =
            1 + kLoopArea.countNumUniqueBonusTypes();

    and in the AdvCiv branch:

        iValue = (
            (iNumTiles * (iTarget - iAddedTotal)) / iTarget
            + MapRandNum(3 * NUM_CITY_PLOTS)
        ) / iNumUniqueBonusesOnArea;

    Before the suitability scan, current code has:

        if (iValue <= iBestValue) // iBestValue initially 0
            continue;

    Thus integer truncation to zero is not just a slight ranking penalty:
    that area cannot become pBestArea at all in that selection round.

WHY "+ THIS ONE" WAS ORIGINALLY VALID
    countNumUniqueBonusTypes() loops all bonus types and counts a OneArea bonus whenever:

        getNumBonuses(eBonus) > 0

    Base AdvCiv's `areas_tried` set is declared outside the new two-pass loop and is
    never cleared.

    Therefore, once an area was selected during pass one and received at least one copy
    of eBonus, Base AdvCiv could never examine that area again in pass two.

    Any area that Base AdvCiv newly considered for eBonus could therefore normally be
    treated as:
        existing unique bonus types + the new eBonus being proposed

    and the unconditional +1 matched that traversal contract.

WHAT KI#202 CORRECTLY CHANGED
    KI#202 observed that the documented uncapped second pass could not do its job:
    first-pass areas capped by AdvCiv remained in `areas_tried` and could not be
    revisited when valid plots remained.

    SAS now correctly does:

        if (iPass > 0)
            areas_tried.clear();

    This restores the documented second-pass purpose:
        revisit an area capped during pass one and finish the target without the
        first-pass per-area cap.

    But that means a pass-two candidate can already contain eBonus.

AREA COUNTER IS UPDATED IMMEDIATELY
    CvPlot::setBonusType updates both map and area counters synchronously:

        getArea().changeNumBonuses(getBonusType(), 1);
        GC.getMap().changeNumBonuses(getBonusType(), 1);

    CvArea::countNumUniqueBonusTypes then checks those live counters:

        if (getNumBonuses(eLoopBonus) > 0 &&
            GC.getInfo(eLoopBonus).isOneArea())
        {
            iCount++;
        }

    Therefore, when pass two revisits a first-pass area containing eBonus:

        countNumUniqueBonusTypes()

    ALREADY includes eBonus.

    The inherited unconditional `1 +` counts it twice.

DETERMINISTIC ZERO-SCORE REPRODUCTION
    Consider an entirely legal pass-two state:

        iTarget = 10
        iAddedTotal = 9
        candidate area size = 50 tiles
        current area contains eBonus plus four other OneArea bonus types
        so countNumUniqueBonusTypes() = 5
        synchronized MapRandNum(3 * NUM_CITY_PLOTS) returns 0

    The F245 small-island gate is already open:
        9 * 3 = 27
        2 * 10 = 20
        therefore the `< 2 * target` condition is false.

    The remaining-count size term is:

        (50 * (10 - 9)) / 10
        = 5

    Correct denominator:
        5 existing unique OneArea types, already including eBonus
        => 5 / 5 = 1

    Current SAS denominator:
        1 + 5
        => 5 / 6 = 0 by integer division

    Since iBestValue starts at zero:

        if (iValue <= iBestValue)
            continue;

    the current code rejects the area before its actual placement suitability is even
    evaluated.

    If other areas are exhausted, invalid, tried or also zero-scoring, pBestArea stays
    NULL and the supposedly uncapped recovery pass terminates at 9/10 despite a valid
    remaining placement on this revisited area.

CURRENT DATA MAKES TARGET 10 PLAUSIBLE
    Several current OneArea luxury resources use:

        iPlayer = 35
        iTilesPer = 0
        iConstAppearance = 50
        random appearance terms 25 + 25

    including Dye, Incense, Silk, Spices and Sugar.

    On a 48-player game, AdvCiv's sublinear player-count component is approximately:

        (48 * 0.35)^0.85 * 4/3
        ~= 14.67
        -> rounded around 15 before the appearance multiplier.

    The appearance multiplier can range through roughly 50..98 percent, yielding
    ordinary target values in the high single digits / low teens. A target of 10 is
    therefore a supported current-data state, not an invented XML value.

    The zero-score mechanism is broader than exactly target 10; that example merely
    provides compact integer arithmetic.

WHY THIS IS SAS-ONLY IN CURRENT REACHABILITY
    Base AdvCiv 1.14 has the same unconditional:

        1 + countNumUniqueBonusTypes()

    but its inherited `areas_tried` bug means a first-pass selected area is excluded
    from pass two.

    Thus the stale assumption and the pass-two inability accidentally mask each other.

    SAS KI#202 correctly removed the masking bug. That exposed the denominator's
    original assumption.

    This is a classic follow-up compatibility issue:
        repair A enables a code path that inherited expression B had implicitly assumed
        was unreachable.

RELATION TO KI#202
    KI#202 should NOT be reverted.

    Its documented purpose is sound:
        "Let AdvCiv's uncapped OneArea-resource pass revisit capped landmasses."

    Reverting the clear would restore the larger inherited underfill defect.

    F246 instead updates the area-scoring expression to match the newly restored
    lifecycle.

REPAIR DIRECTION
    Count eBonus exactly once.

    Conceptually:

        int iNumUniqueBonusesOnArea =
            kLoopArea.countNumUniqueBonusTypes();

        if (kLoopArea.getNumBonuses(eBonus) <= 0)
            iNumUniqueBonusesOnArea++;

    CvArea publicly exposes getNumBonuses(BonusTypes), so the condition is available
    without rescanning plots.

    This preserves the original intended "+ this one" behavior on fresh areas while
    avoiding a phantom extra type on pass-two revisits.

    A defensive lower bound may be reasonable if future data/script behavior makes the
    divisor unexpectedly zero, although for eBonus under consideration the conditional
    increment should guarantee at least one.

    Implementation review should apply F242's corrected latitude policy and F241/F245
    fixes independently; they affect different stages of the same area-selection/
    placement pipeline.

DISTINCTION FROM OTHER QUEUE-045 ROOTS
    F241:
        grouped copies violate selected-area/global/per-area placement bounds.

    F242:
        suitability scans use the wrong map-script latitude policy.

    F245:
        small areas can be hard-excluded before two-thirds of target exists, creating
        a bootstrap deadlock.

    F246:
        an area that KI#202 deliberately made revisitable can be penalized by counting
        eBonus twice in the unique-resource denominator and can truncate to zero.

    All can coexist; one repair does not subsume the others.

NEGATIVE CONTROLS / SCOPE
    * On pass one, before an area has received eBonus, the original +1 remains correct.
    * On a fresh pass-two area that never received eBonus in pass one, +1 remains
      correct.
    * If the current expression stays safely above zero, F246 may only alter relative
      ranking rather than cause underfill; the zero-score reproduction proves a harder
      behavioral consequence exists.
    * `CvPlot::setBonusType` updates area counts immediately, so there is no delayed
      counter update that would preserve the old assumption.
    * The BTS-style SAS path uses only one pass and therefore does not reach the
      KI#202 revisit state.
    * No prior album root covered this double-count; it is a new follow-up of the
      recent SAS KI#202 repair.


ADDITIONAL NEGATIVE CONTROLS IN THIS PASS
-----------------------------------------
1. Water.py mutable river defines
    Search across current shipped PrivateMaps confirms Water.py is the only shipped
    script presently mutating the three default-river globals handled by KI#243:
        PLOTS_PER_RIVER_EDGE
        RIVER_SOURCE_MIN_RIVER_RANGE
        RIVER_SOURCE_MIN_SEAWATER_RANGE

    No second shipped mutator was found that the current snapshot/use/restore logic
    fails to cover.

2. mapDefaultOptionAvailability output initialization
    The PythonCaller helper initializes/assigns both output booleans; the
    generateRandomMap normalization path is not consuming uninitialized values.

3. Python plot/terrain override result sizes
    PythonCaller validates the returned list size against numPlots before treating the
    override as successful. Malformed custom Python output is not silently accepted by
    the C++ caller.

4. getRiverValueAtPlot wrap handling
    `plotValidXY` / map plot access accounts for wrapping. The neighbor loop does not
    establish a simple map-edge omission on wrapped worlds.

5. zero river randomness
    CvRandom::get(0) returns zero in the target implementation. A zero configurable
    river-direction randomness value does not itself create a divide/modulo failure.

6. current goody-count storage
    With present supported SAS48 map sizes and current Goody Hut spacing/count data,
    the short-backed per-improvement count does not approach signed-short range.

7. Planet Generator default delegation
    The shipped Planet Generator's generateRandomMap/addBonuses hooks call
    allowDefaultImpl rather than fully swallowing the C++ default path. It does not
    establish a current shipped bypass of the corresponding default bookkeeping.

8. `eraseBonuses` / balanced-resource bookkeeping
    The stale-looking `m_aebBalancedBonuses` state remains under review, but no legal
    same-map policy-changing regeneration transition has yet been proved. Do not
    promote from storage appearance alone.

9. repeated addLakes through regenerateGameElements
    `regenerateGameElements` does not convert previously generated lake water back to
    land before calling addLakes again. This is semantically suspicious given the
    public "regenerate everything but terrain and height maps" description, but whether
    generated lakes are considered retained height-map topology rather than erasable
    game elements still needs historical/API contract proof. No root assigned yet.


C020-WIP04 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568
    F246 / provisional KI#569

NEXT ROOT:
    F247 UNUSED

Resume from F247 unused.

Best next work:
    * finish whole-regeneration lifecycle: generated lakes, balanced-resource markers,
      bonus/feature/goody state and map-script override contracts;
    * challenge river recursion/source density and short river-ID bounds once more,
      keeping theoretical overflow under F244 unless a supported independent state is
      proved;
    * continue historical pre/post-1674 resource-generation changes for assumptions
      exposed by the two-pass rewrite;
    * compare all SAS selectable BTS branches with actual K-Mod/Civ4CE semantics;
    * perform a function-by-function RNG/state/producer-consumer closure matrix before
      deciding whether queue 045 can close;
    * cross-check the complete earlier album before assigning F247.

Clean rejection remains preferable to inventing F247.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP05 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    current shipped PrivateMaps
    LLM_Helpers/context/commit_diffs/*

Prior physical checkpoint:
    C020-WIP04

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New root:
    F247 / provisional KI#570

Next root:
    F248 UNUSED


F247 / provisional KI#570
-------------------------
TITLE
    AdvCiv's CvPythonCaller refactor discards the numeric result of
    getRiverAltitude when a map script calls allowDefaultImpl; shipped Inland Sea and
    Great Plains deliberately use that hybrid callback to add directional altitude to
    the DLL score, so their custom river guidance is silently ignored.

STATUS
    CONFIRMED inherited AdvCiv Python-callback semantics regression.
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    Introduced by AdvCivPreSAS practical 1648 / 553b1a0f8d,
        "Moved DLL-to-Python calls and logging into separate classes."

    Directly affects shipped current SAS maps:
        PrivateMaps/Inland_Sea.py
        PrivateMaps/Great_Plains.py

ORIGINAL BTS / K-MOD CALLBACK CONTRACT
    Original getRiverValueAtPlot initializes:

        long result = 0;

    calls Python getRiverAltitude, and then distinguishes two cases.

    Full Python override:
        if Python did NOT call allowDefaultImpl:
            return result directly.

    Default/hybrid mode:
        if Python called allowDefaultImpl (or did not override):
            continue DLL scoring with:

                iSum = result;

            then add the terrain/adjacency/default-river terms.

    Therefore a map script can intentionally do BOTH:

        CyPythonMgr().allowDefaultImpl()
        return someAltitudeModifier

    and the numeric Python value becomes an additive modifier to the normal C++ river
    score.

    K-Mod 1.46 and Civ4CE/BtS retain this exact behavior.

THE 1648 REFACTOR
    Practical 1648 moved the callback into CvPythonCaller::riverValue.

    getRiverValueAtPlot was rewritten to:

        bool bOverride = false;
        int pyValue = GC.getPythonCaller()->riverValue(..., bOverride);
        if (bOverride)
            return pyValue;
        int iSum = pyValue; // Add to value from Python

    The caller comment explicitly preserves the old additive contract.

    But the new helper did:

        bOverride = isOverride();
        if (!bOverride)
            return -1;

    Therefore every allowDefaultImpl callback lost its actual numeric lResult before
    control returned to the caller.

    This was a refactoring regression: the pre-refactor call site retained `result`
    even when Python requested the default implementation.

2298 CHANGED THE SENTINEL, NOT THE LOST SEMANTICS
    Practical 2298 / 893807eb97,
        "River placement accounts for terrain on both banks equally"
    changed the helper's non-override/failure return from -1 to 0:

        if (!bOverride)
            return 0;

    This removed the uniform -1 offset from default river scores, but it still discards
    a legitimate numeric hybrid result.

    The same commit keeps:

        int iSum = pyValue; // Add to value from Python

    and its documented purpose concerns symmetrical terrain-bank weighting and stronger
    attraction to water, not removing map-script altitude modifiers.

    Current source retains this mismatch.

SHIPPED INLAND SEA REPRODUCTION
    Current PrivateMaps/Inland_Sea.py:

        def getRiverAltitude(argsList):
            pPlot = argsList[0]
            map = CyMap()

            CyPythonMgr().allowDefaultImpl()

            return (
                abs(pPlot.getX() - map.getGridWidth()/2) +
                abs(pPlot.getY() - map.getGridHeight()/2)
            ) * 20

    This is exactly the old supported hybrid contract:
        keep the DLL's generic terrain routing,
        but add a strong map-specific radial altitude term.

    Rivers choose the LOWEST candidate score.

    The returned value is therefore intended to make positions nearer the Inland Sea
    center more attractive and guide rivers toward the central sea.

    Current AdvCiv/SAS:
        allowDefaultImpl => bOverride=false
        CvPythonCaller::riverValue returns 0
        the radial term is completely lost

    Result:
        Inland Sea's custom river-altitude guidance is not used by the DLL.

SHIPPED GREAT PLAINS REPRODUCTION
    Current PrivateMaps/Great_Plains.py likewise does:

        CyPythonMgr().allowDefaultImpl()

    and then returns a coordinate-dependent score based on the generated Mississippi
    path and the candidate's x/y position.

    The function contains several nonzero branches, e.g. terms based on:
        y * 0.5
        (iW - x) * 0.38
        plus/minus y-dependent components

    Those values are intended to steer default tributary rivers relative to the
    Mississippi.

    Current AdvCiv/SAS again substitutes zero for the returned value, silently removing
    that directional guidance.

PLANET GENERATOR NEGATIVE CONTROL
    Current Planet_Generator_0_68.py defines getRiverAltitude only as a default stub:

        CyPythonMgr().allowDefaultImpl()

    with no meaningful numeric return.

    It does not supply the direct behavioral reproduction; Inland Sea and Great Plains
    do.

CURRENT SAS ROUTING TOGGLE DOES NOT MASK THE ROOT
    F247 occurs before either routing profile's C++ scoring terms.

    With SAS_MAP_ADVCIV_RIVER_ROUTING_ENABLE=1:
        lost Python modifier + AdvCiv symmetrical score

    With it disabled:
        lost Python modifier + retained BTS-style default score

    Thus selecting the BTS routing profile does not restore the original Python hybrid
    callback behavior.

BEHAVIORAL CONSEQUENCE
    `doRiver` evaluates candidate directions through getRiverValueAtPlot and chooses the
    minimum score.

    Removing a coordinate-dependent additive term can change which cardinal direction
    wins at many river corners, changing:
        * river courses;
        * freshwater coverage;
        * floodplain eligibility;
        * later feature/resource/start-normalization interactions;
        * synchronized MapRand consumption downstream through the changed generated map.

    This is deterministic within current AdvCiv/SAS; it is not an OOS claim.

BASELINE
    K-Mod:
        preserves additive hybrid result.

    Civ4CE/BtS:
        preserves additive hybrid result.

    Base AdvCiv:
        inherits the CvPythonCaller regression.

    SAS:
        inherits it and ships affected copies of Inland Sea and Great Plains.

REPAIR DIRECTION
    Restore all three semantic states cleanly:

        1. full Python override:
               return Python value directly;

        2. allowDefaultImpl + numeric Python result:
               return that numeric value to be added to DLL score;

        3. absent/default stub/no meaningful numeric modifier:
               additive value 0.

    Do not merely change current `return 0` to return a sentinel-initialized lResult:
    Planet Generator and missing-function/default paths should not accidentally inject
    -1 or a Python-None conversion.

    The pre-refactor code used `long result = 0`, which is a useful semantic reference.
    A robust CvPythonCaller helper can initialize the result to zero for this callback
    and preserve a successfully returned numeric value even when `isOverride()` is
    false.

    Keep the existing negative-result validation for a true full override unless map
    script compatibility review establishes a broader contract.

    Compile/runtime validation should compare river layouts on:
        Inland Sea
        Great Plains
    against current behavior and, ideally, K-Mod/BtS semantics with the same map seed.

DISTINCTION FROM OTHER QUEUE-045 ROOTS
    F244:
        river-ID lifecycle across regenerateGameElements.

    F247:
        Python altitude modifier lost before the river scoring algorithm sees it.

    F247 is independent of river IDs, source density, route randomness and the
    AdvCiv/BTS routing toggle.

NEGATIVE CONTROLS / SCOPE
    * A getRiverAltitude that fully overrides the DLL without allowDefaultImpl still
      returns its value correctly.
    * A script with no custom altitude term is unaffected.
    * The bug is not caused by SAS's recent river tunability work; provenance is the
      older AdvCiv CvPythonCaller refactor.
    * Current getRiverValueAtPlot's 4x4 AdvCiv neighborhood traversal was re-opened
      during this pass and confirmed correct; iX/iY are recomputed from iDX/iDY before
      plotValidXY. No duplicate F248 from that false alarm.


ADDITIONAL NEGATIVE CONTROLS
----------------------------
1. Balanced-resource marker persistence through eraseBonuses
    AdvCiv practical 3713 explicitly serializes m_aebBalancedBonuses because:
        "Player might save on turn 0, then reload and regenerate the map.
         Therefore this info needs to be saved."

    This establishes that the marker is policy state intended to survive map-element
    regeneration. eraseBonuses not clearing it is therefore not forgotten cleanup.

2. Full addBonuses Python override marker
    A generic third-party full-phase override would bypass per-type setBonusBalanced
    marking and remains a compatibility seam.

    Current shipped SAS search found only Planet Generator defining addBonuses, and it
    calls CyPythonMgr().allowDefaultImpl(), so current default per-type processing still
    runs. No shipped supported reproduction was established; not promoted.

3. getRiverValueAtPlot neighborhood traversal false alarm
    Full function inspection confirms:
        iX = kPlot.x - 1 + iDX
        iY = kPlot.y + 1 - iDY
    on every nested-loop iteration before plotValidXY.

    The function does score the intended 4x4-minus-corners neighborhood.


C020-WIP05 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568
    F246 / provisional KI#569
    F247 / provisional KI#570

NEXT ROOT:
    F248 UNUSED

Resume from F248 unused.

Best next work:
    * finish Python map callback semantic comparison for riverStartCardinalDirection,
      canPlaceBonusAt, canPlaceGoodyAt and full-phase overrides to see whether 1648
      introduced sibling regressions;
    * finish regenerateGameElements generated-lake semantics before deciding whether the
      monotonic lake behavior is a supported API defect or an inherited limitation;
    * continue river recursion/source-density/ID bounds;
    * finish older resource-generation refactor archaeology;
    * build the function-by-function state/RNG/callback closure matrix before queue-045
      completion;
    * cross-check the whole album before assigning F248.

Clean rejection remains preferable to inventing F248.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP06 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    current SAS XML/resource data
    current shipped PrivateMaps

Prior physical checkpoint:
    C020-WIP05

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New root:
    F248 / provisional KI#571

Next root:
    F249 UNUSED


F248 / provisional KI#571
-------------------------
TITLE
    AdvCiv's OneArea area-value formula can truncate a fresh, legally placeable area's
    score to zero when only a small part of the target remains; zero is treated as
    "no candidate area", so the two-pass recovery can probabilistically terminate below
    target even after the small-island gate has opened.

STATUS
    CONFIRMED inherited AdvCiv resource-placement arithmetic/termination defect.
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS when the AdvCiv OneArea
    distribution path is enabled.

    Introduced by practical 1674 / f25a2b91ab,
        "Relax single-landmass restrictions for resource placement."

    Distinct from:
        F245 -- hard bootstrap exclusion of small areas before 2/3 of target exists;
        F246 -- SAS-only pass-two revisit double-count of eBonus in the unique-type
                denominator after KI#202.

CURRENT FORMULA
    For each candidate area, current AdvCiv scoring computes:

        iAddedTotal = kMap.getNumBonuses(eBonus);

        iValue = (
            (iNumTiles * (iTarget - iAddedTotal)) / iTarget
            + MapRandNum(3 * NUM_CITY_PLOTS)
        ) / iNumUniqueBonusesOnArea;

    and immediately does:

        if (iValue <= iBestValue) // iBestValue initially 0
            continue;

    The expensive suitability/eligible-plot scan happens only AFTER this check.

    Therefore iValue == 0 means:
        * the area is not checked for actual legal placement at all;
        * it cannot become pBestArea;
        * if every remaining candidate has value 0 or is exhausted, pBestArea stays
          NULL and the placement while-loop breaks.

WHY THE NEW "REMAINING/TARGET" TERM CREATES ZERO FOR VALID FRESH AREAS
    Before practical 1674, K-Mod's fresh-area score was essentially:

        iNumTiles / iNumUniqueBonusesOnArea

    plus a random size term only for large areas.

    AdvCiv 1674 changed the deterministic size component to:

        (iNumTiles * remainingCopies) / iTarget

    specifically to:
        "Decrease the impact of iNumTiles when approaching the target resource count"

    Integer division can reduce this whole size component to zero even though the area
    contains a perfectly valid free plot.

    The subsequent random term can legally also be zero.

SUPPORTED CLEAN REPRODUCTION
    Consider a current supported state with:

        iTarget      = 10
        iAddedTotal  = 9
        remaining    = 1

        one untried 5-tile island is the only area with a legal remaining plot
        for eBonus;

        eBonus is not yet present on that island;
        there are no other OneArea resource types on it, so:
            countNumUniqueBonusTypes() = 0
            "plus this one" denominator = 1

    The F245 hard small-island gate is OPEN:

        iAddedTotal * 3 = 27
        2 * iTarget     = 20
        27 < 20 is false

    So this candidate is allowed to reach the score computation.

    Deterministic size term:

        (5 * (10 - 9)) / 10
        = 5 / 10
        = 0

    `MapRandNum(3 * NUM_CITY_PLOTS)` has range 0..62 and can legally return 0.

    On that synchronized result:

        iValue = (0 + 0) / 1
               = 0

    The code executes:

        if (iValue <= iBestValue) // 0 <= 0
            continue;

    It never asks whether the island actually has a valid placement.

    If no other area can supply the final copy:
        pBestArea remains NULL;
        the current pass breaks at 9/10.

THE SECOND PASS CAN FAIL THE SAME WAY
    The two-pass comment says:

        "Two passes - just to make sure that the new per-area limit doesn't
         lead to fewer resources overall."

    In Base AdvCiv:
        the tiny island was not selected, so it was never inserted into areas_tried.
        Pass two can therefore consider it again.

    In SAS:
        KI#202 clears areas_tried before pass two, so it can also be reconsidered after
        exhausted first-pass areas have been processed.

    The area-value RNG is rolled anew.

    A second legal low roll of 0 reproduces exactly the same iValue==0 exclusion.

    The second pass can therefore also end at 9/10 despite a legal remaining plot.

    This is probabilistic with respect to the synchronized map seed, but fully
    deterministic for any seed producing those legal rolls. A supported bug does not
    require the bad random branch to be common.

CURRENT TARGET 10 IS NOT INVENTED XML
    As documented under F246, several current OneArea resources use approximately:

        iPlayer = 35
        iConstAppearance = 50
        random appearance terms = 25 + 25

    and current SAS supports 48-player maps.

    AdvCiv's current player-scaling plus appearance multiplier naturally yields
    high-single-digit / low-teen targets for such resources; target 10 is an ordinary
    current-data possibility.

    The arithmetic flaw is not tied to exactly 10. Any state with:

        iNumTiles * remaining < iTarget

    gives a zero deterministic size component, and a sufficiently low random term can
    still make the final integer-divided score zero.

WHY THE AREA CAN BE LEGALLY PLACEABLE
    The zero precheck happens BEFORE the practical-2364 suitability scan.

    Therefore nothing in the zero result proves:
        iEligible == 0
        or that canPlaceBonusAt would fail.

    A tiny fresh island can contain one legal resource tile and still be discarded
    solely by score truncation.

    In fact, with 5 tiles and no existing OneArea types, K-Mod's corresponding fresh
    area score is:

        5 / 1 = 5

    so the same legally usable island is not zeroed by the older algorithm.

HISTORICAL INTENT
    Practical 1674's explicit purpose was:
        * cap copies per landmass;
        * place excess resources elsewhere, including small islands.

    Its second pass was added:
        "just to make sure that the new per-area limit doesn't lead to fewer resources
         overall."

    Turning a legally usable final island into "no candidate" because the near-target
    size weight rounded to zero conflicts with that recovery purpose.

    The historical comment only says to DECREASE the influence of area size near the
    target, not to make valid areas disappear from consideration.

BASELINE
    Base AdvCiv 1.14:
        inherits F248.

    SAS:
        inherits it when
        SAS_MAP_ADVCIV_ONE_AREA_BONUS_DISTRIBUTION_ENABLE=1.

    K-Mod:
        does not have the AdvCiv remaining/target size multiplier. A fresh area with at
        least one free tile cannot reach the specific 5/1 -> 0 arithmetic above.

    Civ4CE/BtS:
        likewise do not contain AdvCiv's two-pass remaining-target weighting.

REPAIR DIRECTION
    Ranking weight zero should not be conflated with "no legal area exists."

    Viable repairs include:

        * clamp the pre-suitability candidate weight to at least 1 for areas that are
          otherwise eligible for consideration;

        * better: perform/retain an explicit iEligible > 0 test and ensure every area
          with at least one legal placement receives a positive ranking value;

        * use non-truncating/scaled arithmetic for the remaining-target size factor,
          then round only after suitability/ranking has been combined;

        * if no pBestArea is found while target is unmet, perform a final fallback over
          areas with actual eligible plots rather than terminating solely on score 0.

    Simply removing the:
        `if (iValue <= iBestValue) continue`
    optimization without changing the later `if (iValue > iBestValue)` selection does
    not solve zero-valued areas.

    Keep the intended near-target reduction in SIZE INFLUENCE; only remove the
    unintended transition from "low ranking" to "not a candidate."

RELATION TO F245 / F246
    F245:
        before 2/3 target, small areas are hard-skipped by an explicit `continue`.
        F248 occurs AFTER that threshold has opened.

    F246:
        on a SAS pass-two revisit, eBonus is already counted by
        countNumUniqueBonusTypes and the unconditional `+1` can turn a positive score
        into zero.
        F248 needs no revisit and no double-count. Its numerator is already zero on a
        fresh legal area.

    Fixing F246's denominator therefore does not repair F248.

NEGATIVE CONTROLS / SCOPE
    * Areas whose final score stays positive are not excluded by this specific root.
    * A failed target due solely to there being no legal resource plot is not F248.
    * The BTS-style SAS OneArea branch does not use the remaining/target AdvCiv formula.
    * F248 does not claim the full target must always be reachable physically; it proves
      a state where the only remaining legal placement is skipped solely because a
      ranking calculation rounded to zero.


REPEATED-LAKE REGENERATION CANDIDATE -- NOT PROMOTED
-----------------------------------------------------
The prior WIP05 cursor had a possible F248 candidate around:

    CyMap::regenerateGameElements()
        eraseRivers
        eraseFeatures
        eraseBonuses
        eraseGoodies
        addGameElements()
            addRivers
            addLakes
            ...

There is no eraseLakes, so random lake plot-type conversions accumulate across repeated
calls.

The Python API description says:
    "used to regenerate everything but the terrain and height maps"

This is semantically awkward because:
    * lakes are explicitly re-added by addGameElements;
    * but random lakes are represented by PLOT_OCEAN plot-type changes, which can also
      be interpreted as part of the retained height/topology map.

No shipped caller or historical statement was found that unambiguously promises repeated
calls should remove/re-roll prior random lake plot types.

Therefore:
    * keep this as an inherited API/lifecycle ambiguity;
    * do NOT assign a finding from monotonic lake accumulation without stronger contract
      evidence.

F248 is instead assigned to the independent confirmed OneArea zero-score defect above.


ADDITIONAL CLOSURE WORK SINCE WIP05
-----------------------------------
1. CvPythonCaller 1648 sibling callback sweep
    F247 remains unusually specific.

    canPlaceBonusAt / canPlaceGoodyAt:
        old contract uses Python result only for a full override; allowDefaultImpl
        intentionally resumes C++ and does not add a numeric modifier.

    riverStartCardinalDirection:
        original BtS/K-Mod contract also ignores the returned direction when Python
        chooses allowDefaultImpl. No F247-like additive hybrid semantics existed.

    phase callbacks addRivers/addLakes/addFeatures/addBonuses/addBonusType/addGoodies:
        control-flow override/default semantics broadly match the pre-refactor contract.

    No additional 1648 sibling root promoted.

2. SAS_Simple_Flat_Grass.py optional callback assertions
    The intentionally minimal shipped script omits some optional generation callbacks.

    CvPythonCaller::addFeatures is unusually reached with assert-success defaults and may
    emit debug/assert diagnostics before falling back to C++.

    Release gameplay still has a sensible fallback and the implementation belongs to
    CvPythonCaller.cpp, not queue 045. Preserve as a future owning-file robustness seam,
    not F249.

3. Simple Flat Grass plot/terrain fallback
    Missing Python generatePlotTypes makes C++ fill PLOT_LAND.
    Changing reset ocean plots to land assigns LAND_TERRAIN through CvPlot::setPlotType.
    This explains the script's flat-grass result; no map-corruption root.

4. OneArea fresh-area zero score
    Cross-album search found no earlier root covering the inherited numerator truncation.
    F246 explicitly concerns the newly reachable SAS revisit double-count and does not
    subsume F248.


C020-WIP06 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568
    F246 / provisional KI#569
    F247 / provisional KI#570
    F248 / provisional KI#571

NEXT ROOT:
    F249 UNUSED

Resume from F249 unused.

Best next work:
    * finish addUniqueBonusType adversarial arithmetic after F245/F246/F248, especially
      selection termination versus actual iEligible availability;
    * finish river recursion/source-density and regenerated-state closure after F244/
      F247;
    * finish generateRandomMap/findStandardClimate/findStandardSeaLevel data assumptions;
    * inspect remaining substantive historical CvMapGenerator commits not yet assigned
      or cleanly rejected;
    * compare SAS selectable BTS branches against K-Mod/Civ4CE line-by-line where their
      comments claim equivalent behavior;
    * build a complete function-by-function RNG/state/Python-callback closure matrix
      before declaring queue 045 complete;
    * cross-check the full album before assigning F249.

Clean rejection remains preferable to inventing F249.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP07 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    current shipped PrivateMaps
    current SAS XML / GlobalDefines

Prior physical checkpoint:
    C020-WIP06

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New roots in this append:
    NONE

Next root:
    F249 UNUSED

PURPOSE
-------
Preserve the residual no-new-root work after F248 before the final whole-file
closure/historical sweep.

This pass deliberately challenged the remaining OneArea selection math, shipped Python
resource delegation, SAS's selectable BTS river profile, river recursion/source passes,
small helpers and configurable define lifetimes. The attractive seams inspected so far
either reduce to F241-F248, require malformed/unsupported third-party script behavior,
or survive as intentional/documented approximations.


ONEAREA RESIDUAL MATRIX -- NO F249
----------------------------------
1. Pre-suitability score pruning is mathematically safe.

    Current AdvCiv first computes a pre-suitability iValue, then skips the expensive
    suitability scan when:

        iValue <= iBestValue

    The later suitability multiplier is:

        ((iEligible / iNumTiles) + 2) / 3

    and therefore lies in [2/3, 1].

    It can only preserve or REDUCE the preliminary score; it cannot make an area whose
    preliminary value is already <= the current post-suitability best become the new
    best.

    Therefore this optimization is not independently discarding a possible winner.

2. An area with zero actually eligible plots can still receive a positive diluted
   suitability score, be selected, then fail to place a center.

    This wastes one selection attempt and inserts the area into areas_tried, but the
    next loop iteration can continue to another area.

    With the current OneArea resource set, no supported dynamic event inside
    addUniqueBonusType was found that would turn such an area from invalid to valid
    later in the same placement call.

    RESULT:
        inefficiency / noisy ranking, not a proved underfill root beyond the already
        recorded F245/F248 termination cases.

3. Shipped Python `addBonusType` hybrid paths were checked for a possible
   "partially place this bonus, then allow default C++ placement" state.

    Current scripts inspected include:
        Arboria
        Balanced
        Boreal
        Great_Plains
        Oasis
        Rainforest
        Fantasy_Realm
        Planet_Generator_0_68

    In their shipped branches, custom placement and allowDefaultImpl/default placement
    are mutually exclusive for a given resource/configuration.

    No current shipped state was found in which C++ addUniqueBonusType begins with an
    arbitrary partial set created by Python and therefore violates an additional
    preexisting-count assumption.

4. CyMapGenerator exposes addUniqueBonusType/addNonUniqueBonusType to Python, and a
   third-party caller could invoke them against an already partially populated map.

    No shipped current caller was found doing so.

    CvWBDesc's Randomize Resources flow clears existing bonuses before calling the
    whole addBonuses pipeline.

    Do not promote generic externally-misused public API behavior without a supported
    current caller.


SAS SELECTABLE BTS RIVER PROFILE -- CLEAN SO FAR
-------------------------------------------------
The retained BTS/K-Mod source traversal was compared directly against the SAS branch
used when:

    SAS_MAP_ADVCIV_RIVER_ROUTING_ENABLE = 0

The important source-generation structure matches:

    * deterministic map-index traversal rather than AdvCiv's shuffled traversal;
    * four source passes;
    * source-river and seawater range thresholds;
    * relaxed thresholds after the first pass;
    * final sparse/random source pass;
    * inherited river-start/addRiver handling.

The current SAS documentation also intentionally separates:
    routing algorithm/profile
from:
    direction-score randomness amplitude.

It explicitly says to pair the BTS routing branch with:

    SAS_MAP_RIVER_DIRECTION_RANDOMNESS = 10

for the complete BTS-style routing profile.

The normal SAS default uses 20 together with the AdvCiv routing branch.

RESULT:
    No misleading "one toggle claims to restore every BTS river characteristic"
    contract and no SAS-only equivalence root.

Historical practical 2534 fixed a bug where later source passes accidentally kept using
the original full source/seawater ranges. Current code uses the pass-specific loop
variables and retains that correction.


RIVER RECURSION / SOURCE-DENSITY CHECKS -- NO NEW ROOT YET
-----------------------------------------------------------
1. Current doRiver remains structurally aligned with K-Mod/BtS after accounting for:
       * AdvCiv short river IDs;
       * CvPythonCaller wrapper;
       * AdvCiv/SAS direction scoring;
       * style/refactor changes.

2. Python `getRiverStartCardinalDirection` values are checked only for non-negative
   success before becoming a CardinalDirectionTypes value.

   A malformed third-party map script returning an integer beyond the enum range could
   therefore violate the recursion contract.

   Current shipped implementations checked return valid cardinal enum values:
       Team_Battleground
       Inland_Sea
       Great_Plains
   while Planet Generator delegates to default.

   No supported current root.

3. A fresh river ID can be consumed before a start later aborts.

   During a normal one-shot generation this creates only gaps in the ID sequence.

   Across repeated regenerateGameElements calls it accelerates F244's never-reset short
   counter; retain that as an F244 consequence rather than allocating F249.

4. Single-pass signed-short river-ID exhaustion was challenged against supported SAS48
   map dimensions.

   A practical normal-generation path to >32767 distinct started river IDs was not
   established. Repeated whole-element regeneration remains the supported amplification
   under F244.

5. Local coordinate/hash arithmetic used for deterministic river-direction noise can
   overflow signed 32-bit arithmetic under abstract modern-C++ rules.

   The target VC2003/32-bit runtime uses deterministic machine wrapping, and no
   supported peer divergence, memory error or gameplay corruption was established.

   Portability concern only for a future engine/toolchain port.


COUNT / SMALL-HELPER NEGATIVE CONTROLS
--------------------------------------
1. addGoodies
    Current SAS has one ordinary Goody Hut improvement with positive TilesPerGoody and
    range. The short-backed per-improvement count remains safely below signed-short
    bounds on supported SAS48 map dimensions.

2. canPlaceGoodyAt
    Current spacing and world-size checks trace to documented inherited behavior.
    No current negative-range or divide-by-zero path.

3. calculateNumBonusesToAdd
    Current sublinear player-count scaling, appearance randomness, eligible-tile
    component and minimum-one behavior were compared to the substantive historical
    changes.

    The minimum-one result is inherited from BtS/K-Mod and is not itself an AdvCiv
    regression.

4. canPlaceBonusAt
    Practical 2366's per-bonus unique-range fix remains present.
    MinLandPercent retains its denominator guard.
    Existing bonus occupancy is rejected by canHaveBonus.
    No independent range/count root after F241/F242.

5. addFeatures
    canHaveFeature prevents the ordinary XML-order loop from simply overwriting an
    already placed feature with a later one. No current positive-probability feature
    combination established a replacement bug.

6. generateRandomMap climate/sea-level normalization
    Practical 4242's early normalization of disabled options remains before Python
    beforeGeneration/plot/terrain generation.

    Shipped scripts traced that consult map climate/sea level therefore see a valid
    standard option when the setting is disabled.

7. findStandardClimate / findStandardSeaLevel
    Current XML supplies usable standard entries satisfying the helpers' selection
    criteria. No normal-data NO_* return path established.

8. generatePlotTypes / generateTerrain Python result validation
    CvPythonCaller validates the returned list length against map numPlots before the
    C++ caller treats an override as successful. A malformed short/long returned list
    is not silently indexed as valid C++ map data.

9. setPlotTypes
    Areas are recalculated after land/water plot-type assignment. Subsequent shallow/
    deep-water TERRAIN assignment does not change land/water area connectivity, so no
    missing second area rebuild was established.


MAP-SCRIPT / GLOBAL-DEFINE LIFETIME CHECKS
-------------------------------------------
1. Current shipped PrivateMaps were searched for runtime mutations of the three river
   globals involved in Water.py:
       PLOTS_PER_RIVER_EDGE
       RIVER_SOURCE_MIN_RIVER_RANGE
       RIVER_SOURCE_MIN_SEAWATER_RANGE

    Water.py is the shipped mutator found.

    SAS KI#243 snapshots the originals, lets Python alter them, consumes the active
    values for this generation, and restores them afterward.

2. No second shipped mutator was found that makes the current restoration helper
   incomplete.

3. The other reviewed static local map-generation define caches are not currently
   mutated by shipped map scripts.

    The old AdvCiv "return to main menu -> OOS" map-generation history concerned
    persistent starting-plot normalization/game state, not these immutable local
    configuration reads.

4. Planet Generator's default-generation hooks call allowDefaultImpl rather than
   swallowing the C++ phase. No current shipped full-addBonuses override bypassing
   balanced-resource bookkeeping was established.


REPEATED-LAKE REGENERATION -- REMAINS AMBIGUOUS, NOT F249
----------------------------------------------------------
CyMap::regenerateGameElements erases:
    rivers
    features
    bonuses
    goodies

and then addGameElements also calls addLakes.

Previously generated random lakes are plot-type changes and are not erased, so
subsequent calls can retain old lakes and add more.

This remains internally awkward:
    * if lakes are regenerated elements, old generated lakes should seemingly be
      removed/re-rolled;
    * if lake plot types belong to retained height/topology, addLakes seemingly should
      not keep adding new ones.

However, the public description says regeneration retains terrain and height maps, and
random lake plot type can plausibly be classified as retained topology.

No shipped repeated caller or historical statement was found that unambiguously
promises replacement of prior random lakes.

RESULT:
    inherited API/lifecycle ambiguity only.
    Keep unnumbered unless stronger contract evidence emerges.


SUBSTANTIVE HISTORY RETAINED / CLASSIFIED SO FAR
-------------------------------------------------
Already classified or verified in current code:

    1268 placeGroup extraction                  -> F241
    1627/1629 bonus tie randomization           -> retained
    1648 CvPythonCaller refactor                -> F247
    1674 OneArea two-pass rewrite               -> F245 / F248
    2172 river ID compaction/startup clear      -> F244 context
    2298 both-bank river scoring                -> retained
    2364 OneArea suitability weighting          -> F242
    2366 unique bonus range                     -> retained
    2368 bonus density                          -> retained
    2375 goody spacing                          -> retained
    2534 river pass-specific ranges             -> retained
    2601/2682 normalization/OOS lifecycle       -> retained
    2852 rounding refactor                      -> retained
    2986 bonus-generation refactor              -> no new root
    2996/3005 cluster revalidation              -> retained
    3006 Elephant GroupRange=2                  -> strengthens F241
    3208/3209 shuffle/RNG API changes           -> no range divergence established
    3712/3713 Balanced-resource state           -> retained/intentionally persistent
    3898 desert-lake candidate change           -> F243
    3912 bonus RNG expression refactor          -> no semantic divergence established
    3919 sublinear player scaling               -> retained
    4030 super-Huge lake arithmetic             -> retained
    4242 disabled climate/sea normalization     -> retained

    SAS 6114 selectable map-generation profiles -> no SAS-only profile mismatch found
                                                   so far
    SAS 6117 KI#202 pass-two revisit            -> valid fix; exposes F246
    SAS 6212 Water define restore               -> retained


C020-WIP07 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568
    F246 / provisional KI#569
    F247 / provisional KI#570
    F248 / provisional KI#571

NEXT ROOT:
    F249 UNUSED

Resume from F249 unused.

BEST NEXT WORK
--------------
    * finish direct line-by-line K-Mod/Civ4CE comparisons for the remaining functions
      not already root-owned or explicitly challenged;
    * complete the post-1253 historical path list, concentrating only on substantive
      semantic commits rather than formatting;
    * build a final function-by-function RNG/state/Python-callback closure matrix;
    * re-open any apparent issue against the full earlier album before allocating F249;
    * if the remaining matrix stays clean, decide whether queue 045 is ready for its
      durable first-pass close as file 20 / 169.

Clean rejection remains preferable to inventing F249.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C020-WIP08 -- FILE 020 OPEN / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    LLM_Helpers/context/commit_diffs/*
    current SAS XML / GlobalDefines
    CvPlot/CvMap/CvArea lifecycle consumers

Prior physical checkpoint:
    C020-WIP07

Current file:
    priority 3rd / queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

New root:
    F249 / provisional KI#572

Next root:
    F250 UNUSED


F249 / provisional KI#572
-------------------------
TITLE
    AdvCiv's 4030 "recalc only once" lake batching sets every accepted lake except the
    last to water with bRecalculate=false, but setting bRecalculate=true on the last
    lake normally updates only THAT plot's area rather than rebuilding the map; earlier
    lake plots can therefore remain water tiles still attached to their old land
    CvArea during feature/resource generation and starting-plot normalization.

STATUS
    CONFIRMED inherited AdvCiv map-generation lifecycle regression.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.

    Introduced by practical 4030 / 1b2adf4aea:
        "Fix overflow in lake generation for super-Huge maps"

    The commit's overflow correction is valid; the regression is in the additional
    unrelated `advc.opt` batching optimization introduced by the same commit.

CURRENT LAKE BATCH
    Current addLakes first gathers all successful lake candidates in:

        std::vector<CvPlot*> apLakes;

    and then performs:

        // <advc.opt> Recalc only once
        for (size_t i = 0; i < apLakes.size(); i++)
        {
            bool bRecalc = (i + 1 == apLakes.size());
            apLakes[i]->setPlotType(PLOT_OCEAN, bRecalc, bRecalc);
        }

    Therefore:
        every accepted lake except the final vector element:
            setPlotType(PLOT_OCEAN, false, false)

        final accepted lake:
            setPlotType(PLOT_OCEAN, true, true)

THE COMMENT'S "RECALC ONLY ONCE" ASSUMPTION IS FALSE
    CvPlot::setPlotType does NOT interpret bRecalculate=true as:
        "perform a whole-map CvArea rebuild."

    When land/water state changes and bRecalculate=true, it first attempts to determine
    whether the changed plot can be moved between areas incrementally.

    A whole:

        GC.getMap().recalculateAreas();

    occurs only when the local topology test determines that multiple preexisting areas
    merge/split or another exceptional condition requires it.

    Otherwise, setPlotType performs only a LOCAL transfer for the changed plot:

        CvArea* pOldArea = area();
        setArea(NULL);
        ...
        if (pNewArea == NULL)
        {
            pNewArea = GC.getMap().addArea();
            pNewArea->init(isWater());
        }
        setArea(pNewArea);

    Thus the final lake can be fully repaired while earlier batched lake plots are never
    touched.

WHAT bRecalculate=false LEAVES BEHIND
    For an earlier accepted lake A:

        A.setPlotType(PLOT_OCEAN, false, false)

    does change important plot-level state:
        * m_ePlotType becomes PLOT_OCEAN;
        * water terrain is assigned if necessary;
        * map land-plot count changes;
        * yields/path groups/etc. are updated.

    But the entire area-membership block is guarded by:

        if (bRecalculate)

    so A never calls setArea.

    A therefore remains attached to the SAME CvArea object it belonged to as land.

    `erase()` does not rescue this: although land->water setPlotType calls erase(),
    CvPlot::erase clears city/unit/bonus/improvement/route/feature/river state but does
    not clear or replace the plot's CvArea pointer.

DETERMINISTIC TWO-LAKE REPRODUCTION
    Let A and B be two nonadjacent inland lake candidates that both receive legal
    successful synchronized lake rolls.

    This reproduction does NOT depend on F243's adjacent-candidate eligibility issue.

    apLakes order:
        A first
        B last

    1. A:
        setPlotType(PLOT_OCEAN, false, false)

        A becomes a water plot but remains a member of its old land CvArea L.

    2. B:
        setPlotType(PLOT_OCEAN, true, true)

    Choose the ordinary case in which B is an isolated inland conversion surrounded by
    one connected land area and adjacent to no prior water area.

    Inside setPlotType:
        * no two different water areas are found to merge;
        * the local direction-transition test does not require a whole-map rebuild;
        * bRecalculateAreas remains false.

    B is therefore detached from its old land area and assigned a new water CvArea
    incrementally.

    The promised whole-map "one recalc" NEVER occurs.

    Result immediately after addLakes:
        B: water plot in correct water area
        A: water plot still in old land area

BASELINE
    K-Mod and Civ4CE/BtS place every successful lake immediately with:

        pLoopPlot->setPlotType(PLOT_OCEAN);

    using the default bRecalculate=true each time.

    Every converted plot therefore receives normal area migration/recalculation.

    The stale-earlier-lake state begins with AdvCiv's 4030 batching optimization.

HISTORICAL PROOF OF INTRODUCTION
    The practical-4030 diff explicitly changes:

        p.setPlotType(PLOT_OCEAN);

    into:

        apLakes.push_back(&p);

    followed by:

        // <advc.opt> Recalc only once
        ...
        bool bRecalc = (i + 1 == apLakes.size());
        apLakes[i]->setPlotType(PLOT_OCEAN, bRecalc, bRecalc);

    The same commit changes CvPlot::processArea optimizations but does not redefine
    setPlotType's bRecalculate contract into a guaranteed wholesale rebuild.

    Therefore the optimization's assumption is visible directly in its introducing
    diff.

LIVE CONSUMERS RUN BEFORE ANY LATER STARTUP REPAIR
    addGameElements order is:

        addRivers();
        addLakes();
        addFeatures();
        addBonuses();
        addGoodies();
        afterGeneration();

    There is no CvMap::recalculateAreas call between addLakes and these later phases.

    Full map generation then proceeds into starting-plot assignment and normalization
    before CvGame::setInitialItems eventually performs the optional PASSABLE_AREAS
    whole-area rebuild.

    Therefore stale area state is consumed during:
        * feature generation;
        * resource target/placement logic;
        * goody generation;
        * map-script afterGeneration;
        * starting-plot assignment;
        * starting-plot normalization.

DIRECT SEAFOOD CONSEQUENCE WITH CURRENT SAS XML
    CvPlot::canHaveBonus uses the plot's current area for minimum-area eligibility:

        if (kBonus.getMinAreaSize() != -1)
        {
            if (getArea().getNumTiles() < kBonus.getMinAreaSize())
                return false;
        }

    Current SAS water resources include:

        BONUS_MOLLUSCS
            iMinAreaSize = 10
            TERRAIN_COAST

        BONUS_FISH
            iMinAreaSize = 10
            TERRAIN_COAST, TERRAIN_OCEAN

        BONUS_WHALE
            iMinAreaSize = 10
            TERRAIN_COAST, TERRAIN_OCEAN

    A newly created isolated random lake is physically a one-tile water body and should
    fail this minimum-water-area requirement.

    But stale lake A still returns its old LAND area's tile count.

    If L has, for example, 40 tiles:

        A.isWater() == true
        A.getArea().isWater() == false
        A.getArea().getNumTiles() ~= 40

    `canHaveBonus` can therefore accept a coast-compatible seafood type on A even though
    the true water area size is one.

    addBonuses runs immediately after addLakes, so this is a live placement path.

    If later startup code eventually rebuilds areas correctly, the seafood already
    placed on A is not automatically removed merely because the repaired one-tile water
    area would now fail iMinAreaSize=10.

    The transient stale area can therefore leave a permanent illegal map-generation
    result.

isLake IS ALSO WRONG DURING THE WINDOW
    CvPlot::isLake is:

        return (area() == NULL ? false : getArea().isLake());

    CvArea::updateLake immediately returns without setting lake=true for a land area.

    Stale A therefore reports:
        A.isWater() == true
        A.isLake() == false

    even though it is physically an isolated random lake tile.

    Feature/map-script logic using isLake can consequently see contradictory topology.

AREA COUNTERS ALSO REMAIN STALE
    Because A never calls setArea:
        * old land CvArea's tile count still includes A;
        * its land/water classification remains land;
        * any per-area resource, starting-plot, yield or topology accounting tied to
          processArea remains associated with the wrong area.

    The direct seafood reproduction is sufficient on its own; these are additional
    consequences rather than separate roots.

EVENTUAL setInitialItems RECALC DOES NOT NEGATE THE BUG
    Current default AdvCiv/SAS has:

        PASSABLE_AREAS = 1

    and setInitialItems eventually does:

        if (PASSABLE_AREAS)
            GC.getMap().recalculateAreas(false);

    This can repair raw area membership later.

    However its ordering is:

        assignStartingPlots();
        normalizeStartingPlots();
        ...
        recalculateAreas(false);

    Therefore:
        * feature/bonus/goody generation has already completed;
        * starting sites have already been selected and normalized.

    Any decisions or illegal resource placements made from stale area state remain.

    Moreover PASSABLE_AREAS=0 is a documented supported BtS-style configuration, in
    which this later AdvCiv area rebuild is not guaranteed to occur at all.

DISTINCTION FROM F243
    F243:
        AdvCiv snapshots lake eligibility before any lake conversion, so adjacent
        candidates can both be selected when BtS would make the later one coastal.

    F249:
        given any batch containing at least two accepted lakes, earlier conversions
        can retain their former LAND CvArea because the last conversion's
        bRecalculate=true is only local.

    F249 works with nonadjacent lakes and survives a hypothetical F243 selection fix
    unless area rebuilding is independently repaired.

RELATION TO F244
    F244 concerns stale river-generation IDs across erase/regenerate lifecycle.

    F249 concerns stale CvArea membership during ordinary initial lake generation.

    They are independent state families.

REPAIR DIRECTION
    If the goal is genuinely:
        "recalculate areas once after all accepted lakes are converted"

    make that explicit rather than relying on bRecalculate=true for the last plot.

    Conceptually:

        for every accepted lake:
            setPlotType(PLOT_OCEAN, false, false);

        if (!apLakes.empty())
            GC.getMap().recalculateAreas();

    Then perform any required graphics/water-terrain refresh as appropriate for the map
    generation lifecycle.

    A full area rebuild is especially appropriate here because multiple arbitrary
    land->water conversions can:
        * split land areas;
        * create multiple new water areas;
        * merge into existing water areas.

    A single local update for the final changed plot cannot reconstruct the earlier
    topology.

    If performance motivates avoiding a wholesale rebuild, then every changed plot's
    area membership must instead be correctly updated incrementally; simply skipping
    area processing on all but the last plot is not safe.

    Implementation review should also consider F243 simultaneously:
        * F243 needs sequential/dynamic lake eligibility or an equivalent selected-lake
          adjacency constraint;
        * F249 needs correct final CvArea topology for all accepted lakes.

    One carefully designed batched-lake helper could repair both without restoring
    repeated expensive per-lake whole-map recalculation.

NEGATIVE CONTROLS / SCOPE
    * If zero or one lake is accepted, F249 does not arise from this batching.
    * If the final lake happens to force GC.getMap().recalculateAreas(), all earlier
      stale area pointers are repaired and this specific execution avoids F249.
    * The existence of such lucky final topologies does not make the optimization safe;
      the ordinary isolated-final-lake case performs only local migration.
    * K-Mod/BtS sequential placement is a direct negative control.
    * Later PASSABLE_AREAS rebuilding can repair state but occurs after consumers and
      does not roll back placements/normalization decisions already made.


C020-WIP08 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    19 / 169

CURRENT FILE:
    queue 045 CvGameCoreDLL/CvMapGenerator.cpp -- OPEN

QUEUE-045 ROOTS:
    F241 / provisional KI#564
    F242 / provisional KI#565
    F243 / provisional KI#566
    F244 / provisional KI#567
    F245 / provisional KI#568
    F246 / provisional KI#569
    F247 / provisional KI#570
    F248 / provisional KI#571
    F249 / provisional KI#572

NEXT ROOT:
    F250 UNUSED

Resume from F250 unused.

Best next work:
    * finish the whole-file function-by-function RNG/state/Python-callback closure
      matrix now that the late lake-area lifecycle root is accounted for;
    * explicitly re-open every `setPlotType(..., false)` / delayed-recalc map-generation
      optimization in current CvMapGenerator history to ensure F249 has no sibling;
    * finish direct baseline comparisons for eraseFeatures/eraseBonuses/eraseGoodies and
      afterGeneration interactions;
    * cross-check any candidate against F241-F249 and the full earlier album before
      assigning F250;
    * if the residual matrix remains clean, consider a durable queue-045 close as
      first-pass file 20 / 169 rather than manufacturing F250.

Clean rejection remains preferable to inventing F250.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
DURABLE CHECKPOINT C021 -- FILE 020 COMPLETE / QUEUE 045 CvMapGenerator.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this close:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material used across the queue-045 pass:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    current shipped PrivateMaps
    current SAS XML / GlobalDefines
    LLM_Helpers/context/commit_diffs/*
    LLM_Helpers/context/commit_diffs/PATH_HISTORY_INDEX.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C020-WIP08

DURABLE RESULT
--------------
    COMPLETED FIRST-PASS FILES: 20 / 169

    queue 045
    CvGameCoreDLL/CvMapGenerator.cpp
    STATUS: COMPLETE -- FIRST-PASS DURABLE CLOSE

    Queue-045 roots:
        F241 / provisional KI#564
        through
        F249 / provisional KI#572

    NEXT GLOBAL ROOT ID:
        F250 UNUSED

This close follows the album's established meaning of COMPLETE:
    * current SAS source was compared with Base AdvCiv, K-Mod and Civ4CE where cognate
      code exists;
    * historical semantic changes were inspected by introducing/refactoring commit,
      not only by current line shape;
    * RNG, map-script callback, resource-count, river, lake, regeneration and CvArea
      lifecycle contracts were challenged;
    * attractive but unsupported candidates were explicitly rejected;
    * findings were cross-checked against earlier album roots before numbering;
    * COMPLETE remains first-pass completeness, not a proof that later cross-file
      archaeology can never add a refinement.


RECOVERED WIP06-WIP08 VALIDATION
--------------------------------
During the final continuation, orphaned physical checkpoints C020-WIP06 through WIP08
were found in /mnt/data from interrupted/retried execution attempts that had never
surfaced to the user.

They were NOT trusted blindly.

F248 and F249 were independently reopened against the authoritative source and current
CvPlot/CvArea semantics.

Validation result:
    F248 CONFIRMED:
        near-target OneArea preliminary score can truncate to zero before actual
        eligibility is checked, causing legal areas to become "no candidate."

    WIP07 repeated-lake-regeneration candidate remains REJECTED/AMBIGUOUS:
        regenerateGameElements retains previous lake topology while addLakes can add
        more; awkward API semantics, but no sufficiently unambiguous current contract
        requiring replacement of old random lakes was established.

    F249 CONFIRMED:
        lake batching's final bRecalculate=true call does not guarantee a whole-map area
        rebuild; earlier setPlotType(...,false,false) lakes can retain land CvArea
        membership through feature/bonus/goody/start normalization consumers.

Therefore C020-WIP08 is accepted as the valid predecessor to this durable close.


QUEUE-045 FINDING INDEX
-----------------------

F241 / provisional KI#564
    AdvCiv placeGroup extraction dropped OneArea-specific global/area group bounds:
    grouped copies can exceed the calculated target/per-area cap, and current range-2
    Elephants can spill outside the selected CvArea.

F242 / provisional KI#565
    OneArea suitability scoring hardcodes latitude checking even when the active map
    script explicitly requests bonus placement to ignore latitude.

F243 / provisional KI#566
    AdvCiv snapshots all lake candidates before any conversion, so adjacent candidates
    can both become lakes even though sequential BtS/K-Mod would make the later plot
    coastal and ineligible after the first lake.

F244 / provisional KI#567
    eraseRivers clears visible river edges but leaves per-plot river IDs and the map's
    next river-ID counter; repeated regenerateGameElements can regenerate against hidden
    old-river barriers and monotonically advance the short ID counter.

F245 / provisional KI#568
    OneArea small-island guard requires two-thirds of target to already exist before
    any <4*NUM_CITY_PLOTS area may compete; all-small-island maps can therefore
    bootstrap to zero and the second pass repeats the same deadlock.

F246 / provisional KI#569
    SAS KI#202 correctly allows pass-two OneArea revisits, but inherited
    `1 + countNumUniqueBonusTypes()` double-counts the resource on a revisited area and
    can truncate an otherwise viable recovery area's score to zero.

F247 / provisional KI#570
    CvPythonCaller refactor discards the numeric getRiverAltitude result when a script
    calls allowDefaultImpl; shipped Inland Sea and Great Plains deliberately use this
    hybrid additive callback, so their directional river guidance is lost.

F248 / provisional KI#571
    OneArea near-target area-value arithmetic can truncate a fresh legally placeable
    area's preliminary score to zero before suitability/eligibility is checked; zero is
    treated as "no candidate" and can terminate both recovery passes below target.

F249 / provisional KI#572
    AdvCiv 4030 batches accepted lakes with setPlotType(...,false,false) for every lake
    except the last and assumes the last bRecalculate=true call performs a whole-map
    recalculation. It normally performs only local area migration, leaving earlier lake
    plots attached to land CvAreas through resource/start-generation consumers.


FINAL FUNCTION / STATE CLOSURE MATRIX
-------------------------------------

A. Singleton / public placement helpers
    GetInstance
        clean singleton lifecycle for this file's scope.

    canPlaceBonusAt
        Root-owned:
            F241 group-context constraints indirectly rely on this helper.
            F242 caller latitude-policy mismatch.

        Retained historical fixes:
            practical 2366 per-bonus unique range;
            bonus-class unique range;
            MinLandPercent denominator guard;
            occupied-plot rejection through canHaveBonus;
            AdvCiv tightly packed cluster rule when its profile is enabled.

        Clean:
            Python allowDefaultImpl semantics are intentionally full-override-or-default
            here; no F247-like additive numeric contract.

    canPlaceGoodyAt
        Current goody XML/ranges, world-size adjustment, Python override contract and
        occupancy checks survive review.
        No current supported count/range failure.

B. addGameElements / lake pipeline
    addGameElements
        Order:
            rivers -> lakes -> features -> bonuses -> goodies -> afterGeneration

        This ordering provides live consumers for F249's stale CvArea state.

    addLakes
        Root-owned:
            F243 frozen candidate eligibility.
            F249 incorrect delayed area recalculation assumption.

        Retained:
            practical 4030 arithmetic order that avoids super-Huge overflow;
            intended desert-lake probability adjustment;
            SAS profile toggles for desert weighting / peak replacement.

        Rejected:
            repeated regenerateGameElements retaining previous lakes is an inherited
            API/topology ambiguity, not numbered without stronger replacement contract.

C. river generation
    addRivers
        Root-owned:
            F244 regeneration river-ID lifecycle.
            F247 Python additive altitude loss via downstream scoring.

        Retained:
            practical 2534 pass-specific source/seawater ranges;
            practical 2298 both-bank terrain scoring;
            Water.py KI#243 snapshot/use/restore of three mutable river globals;
            selectable SAS AdvCiv/BTS traversal profile and documented separate
            direction-randomness setting.

        Clean:
            no second shipped PrivateMaps mutator of the three river defines;
            zero configured direction randomness is accepted by CvRandom::get(0);
            normal one-shot river-ID range did not establish signed-short exhaustion.

    doRiver
        Root-owned:
            F244 stale ID collision on regenerated river graph.

        Clean:
            ordinary recursion/cardinal transitions remain cognate with inherited code;
            malformed out-of-range third-party Python enum returns are outside current
            shipped supported data.

    addRiver
        Fallback freshness helper retains inherited two-pass cardinal search semantics.
        No independent root established.

    getRiverValueAtPlot
        Root-owned:
            F247 additive Python result loss.

        Clean:
            4x4-minus-corners AdvCiv traversal recomputes X/Y correctly;
            wrap/map-edge access uses plotValidXY;
            deterministic local river RNG does not establish a target-runtime OOS.

D. feature generation
    addFeatures
        Existing-feature rejection in canHaveFeature prevents simple XML-order
        overwrites.
        No current positive-probability conflicting feature chain established.

    eraseFeatures
        Full feature removal is appropriate for regenerateGameElements.
        No forgotten separate feature-generation state found in this file.

E. resource generation dispatcher
    addBonuses
        AdvCiv randomized placement-order ties and SAS selectable BTS tie behavior were
        compared against intended profiles.
        Python per-type handling and balanced-resource marking retain current shipped
        contracts.

        No shipped full-phase addBonuses override that silently bypasses balancing state
        was found; Planet Generator delegates through allowDefaultImpl.

    addUniqueBonusType
        Root-owned:
            F241 target/area/group spill.
            F242 latitude-policy ranking.
            F245 small-island bootstrap deadlock.
            F246 pass-two revisit denominator.
            F248 zero-valued candidate termination.

        Retained:
            KI#202 areas_tried reset is a valid repair and should not be reverted;
            two-pass/per-area cap design and suitability dilution otherwise match
            documented AdvCiv intent.

    addNonUniqueBonusType
        Passes remaining global target into placeGroup and does not share F241's
        OneArea caller-side global overshoot.
        No independent count under/overflow root.

    placeGroup
        Root-owned:
            F241 OneArea context lost by extraction.

        Retained:
            2996/3005 canPlaceBonusAt revalidation after shuffled candidates;
            intended randomized circular AdvCiv grouping and exponentially declining
            group probability;
            SAS BTS branch same-area restriction for OneArea resources.

F. goody generation
    addGoodies
        Current one normal goody type, area counts, spacing and TilesPerGoody arithmetic
        remain within supported SAS48 bounds.
        short goodiesPerArea storage is nowhere near signed-short range.

    eraseGoodies
        removeGoody internally checks whether an improvement is a goody.
        No unrelated improvements are erased.

G. regeneration erasers
    eraseRivers
        Root-owned:
            F244 incomplete river graph state reset.

    eraseFeatures
    eraseBonuses
    eraseGoodies
        No additional independent stale state proved.

        m_aebBalancedBonuses is intentionally policy state that historical AdvCiv
        explicitly serialized so it survives save/reload/regeneration; not forgotten
        eraseBonuses cleanup.

H. top-level random map generation
    generateRandomMap
        practical 4242 disabled climate/sea-level normalization occurs before
        beforeGeneration / plot / terrain generation.

        Shipped Planet Generator's generateRandomMap calls allowDefaultImpl; no current
        shipped full override exposed a missing post-override computeShelves contract.

        Starting normalization profile assignment for the Balanced custom option remains
        current intended behavior.

    generatePlotTypes
        Python result length validated by CvPythonCaller.
        Missing override fallback to PLOT_LAND explains Simple Flat Grass behavior.

    setPlotTypes
        All plots use setPlotType(...,false,false) intentionally, followed by explicit
        whole-map recalculateAreas(). This is a strong negative control for F249:
        this batch correctly performs the wholesale rebuild that addLakes only assumes.

    generateTerrain
        Terrain vector size validated.
        Water/land connectivity is already established by plot types; ordinary
        shallow/deep terrain assignment does not require a second area rebuild.

    afterGeneration
        Python phase hook then map statistics.
        No independent file-local state contract found.

I. resource quantity helper
    calculateNumBonusesToAdd
        Current eligible-tile count, sublinear scaling, player-count scaling, appearance
        randomness and min-one inherited contract reviewed.

        Retained history:
            2368 density changes;
            3912 RNG expression refactor;
            3919 player-count scaling.

        No current divisor-zero/overflow or semantic refactor regression beyond
        queue-045 roots.

J. standard climate/sea helpers
    findStandardClimate
    findStandardSeaLevel
        Current XML contains viable standard entries.
        practical 4242 caller usage remains valid.
        No normal-data NO_CLIMATE/NO_SEALEVEL route established.


HISTORICAL CLOSURE
------------------
Substantive CvMapGenerator history was reviewed rather than treating all 90+ path
touches equally.

Classified/retained examples include:

    1268  placeGroup extraction                  -> F241
    1627/1629 placement-order randomization      -> retained
    1648  CvPythonCaller refactor                -> F247
    1674  OneArea two-pass rewrite               -> F245 / F248
    2172  river ID compaction/startup clear      -> F244 context
    2298  both-bank river scoring                -> retained
    2361  placement/helper refactor               -> retained
    2364  OneArea suitability weighting          -> F242
    2366  bonus unique range                     -> retained
    2368  resource density                       -> retained
    2375  goody spacing                          -> retained
    2534  river pass-specific source ranges      -> retained
    2535  uninitialized addRivers bValid fix     -> retained
    2601/2682 normalization/OOS lifecycle        -> retained
    2852  rounding refactor                      -> retained
    2986  style/refactor changes                 -> retained
    2996/3005 cluster revalidation               -> retained
    3006  Elephant GroupRange=2                  -> strengthens F241
    3208/3209 RNG/shuffle API refactor           -> no range change established
    3712/3713 balanced-resource persistence      -> retained/intended
    3898  desert-lake candidate snapshot         -> F243
    3912  bonus RNG expression refactor          -> retained
    3919  sublinear player scaling               -> retained
    4030  super-Huge lake arithmetic + batching  -> arithmetic retained; batching F249
    4242  disabled climate/sea normalization     -> retained

    SAS 5043/5083 static define optimizations     -> current river-mutator problem
                                                   already repaired under KI#243
    SAS 6114 selectable AdvCiv/BTS profiles       -> no independent profile mismatch
    SAS 6117 KI#202 pass-two revisit              -> valid fix; exposes F246
    SAS 6212 Water define restoration             -> retained


PARKED OWNING-FILE SEAMS -- NOT QUEUE-045 ROOTS
-----------------------------------------------
CvPythonCaller.cpp:
    Minimal SAS_Simple_Flat_Grass.py omits some optional map callbacks.
    CvPythonCaller::addFeatures is unusually invoked with assert-success defaults and
    may emit debug/assert diagnostics before valid C++ fallback.
    Release gameplay has a fallback and no current corruption was proved.
    Revisit in queue 037 CvPythonCaller.cpp.

MilitaryBranch.cpp:
    Previously parked from queue 031:
        canKnowTypicalUnit prerequisite test appears inverted relative to its Warrior
        comment/current XML.
    Revisit in queue 064.


DURABLE C021 CURSOR
-------------------
COMPLETED FIRST-PASS FILES:
    20 / 169

JUST CLOSED:
    queue 045
    CvGameCoreDLL/CvMapGenerator.cpp

QUEUE-045 ROOTS:
    F241 through F249
    provisional KI#564 through KI#572

NEXT ROOT:
    F250 UNUSED


NEXT FILE -- REMAINING PRIORITY MARKERS
---------------------------------------
The explicit first/second/third priority sequence is now complete:

    priority 1st: queue 041 CvSelectionGroupAI.cpp  COMPLETE C019
    priority 2nd: queue 031 UWAICache.cpp           COMPLETE C020
    priority 3rd: queue 045 CvMapGenerator.cpp      COMPLETE C021

Per the earlier durable album policy:
    "After those, continue through the other files marked 'priority next wip' in the
     current queue before returning to ordinary pending files."

Remaining PENDING priority-marked files in queue order include:

    queue 023 CvGameCoreDLL/InvasionGraph.cpp
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp
    queue 040 CvGameCoreDLL/CvGlobals.cpp
    queue 042 CvGameCoreDLL/CvDeal.cpp
    queue 092 CvGameCoreDLL/BBAILog.cpp

Therefore the next selected never-passed priority file is:

    queue 023
    CvGameCoreDLL/InvasionGraph.cpp
    approximately 2,430 lines in the album snapshot

Exact next cursor:
    begin queue 023 InvasionGraph.cpp from F250 unused.

Recommended opening method:
    1. establish current SAS-vs-Base-AdvCiv semantic delta and historical path list;
    2. inventory graph nodes/edges, ownership/lifetime and simulated-force state;
    3. trace cached city/reachability dependencies, especially interactions with
       queue-031 findings without duplicating them;
    4. audit graph mutation, node indices, land/naval topology, path/deployment
       arithmetic, recursive traversal and elimination/conquest simulations;
    5. compare K-Mod/Civ4CE only where cognate code exists;
    6. cross-check existing roots F190/F220/F231/F237/F238/F240 and other UWAI findings
       before allocating F250.

Clean rejection remains preferable to inventing F250.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C021-WIP01 -- FILE 021 OPEN / QUEUE 023 InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    Civ4-K-Mod-master-1.46(2).zip
    Civ4CE-main(9).zip
    current SAS victory-stage helpers
    LLM_Helpers/context/commit_diffs/*
    LLM_Helpers/context/commit_diffs/PATH_HISTORY_INDEX.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior durable checkpoint:
    C021 -- queue 045 CvMapGenerator.cpp COMPLETE

Current file:
    remaining priority-marked queue 023
    CvGameCoreDLL/InvasionGraph.cpp
    approximately 2,430 lines

SAS VS BASE
-----------
Current SAS InvasionGraph.cpp has no material local behavioral delta from Base AdvCiv
1.14. The visible SAS differences are signature-collapse/comment annotations and nearby
cross-file behavior supplied through SAS-modified caches/helpers.

Therefore queue-023 roots should normally be classified as inherited AdvCiv/UWAI unless
a SAS cross-file dependency is specifically required.

New root:
    F250 / provisional KI#573

Next root:
    F251 UNUSED


F250 / provisional KI#573
-------------------------
TITLE
    InvasionGraph's getConquestStage/getDominationStage helper checks cumulative victory
    bits from stage 1 upward, so every active stage 2-4 is reported as stage 1; the
    simulated-capitulation gate can therefore allow surrender to an attacker whose
    Conquest strategy is actually more advanced than its Domination strategy.

STATUS
    CONFIRMED inherited UWAI logic defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    The stage helper originates in the original UWAI lineage and survives the 2920
    rewrite as a local InvasionGraph helper.

CURRENT HELPER
    InvasionGraph.cpp declares:

        #define DECL_GET_VICTORY_STAGE(MixedCase, UPPER_CASE) \
            int get##MixedCase##Stage(PlayerTypes ePlayer) \
            { \
                CvPlayerAI const& kPlayer = GET_PLAYER(ePlayer); \
                if (kPlayer.AI_atVictoryStage(AI_VICTORY_##UPPER_CASE##1)) \
                    return 1; \
                if (kPlayer.AI_atVictoryStage(AI_VICTORY_##UPPER_CASE##2)) \
                    return 2; \
                if (kPlayer.AI_atVictoryStage(AI_VICTORY_##UPPER_CASE##3)) \
                    return 3; \
                if (kPlayer.AI_atVictoryStage(AI_VICTORY_##UPPER_CASE##4)) \
                    return 4; \
                return 0; \
            }

    and instantiates it for:
        Domination
        Conquest

VICTORY-STAGE HASH IS CUMULATIVE
    CvPlayerAI::AI_updateVictoryStageHash constructs each strategy cumulatively.

    For Conquest:
        if stage >= 1:
            set CONQUEST1
            if stage > 1:
                set CONQUEST2
                if stage > 2:
                    set CONQUEST3
                    if stage > 3:
                        set CONQUEST4

    Domination is built the same way.

    AI_atVictoryStage merely returns whether the requested bit intersects the player's
    victory-stage hash.

    Therefore:
        Conquest2 implies Conquest1
        Conquest3 implies Conquest2 and Conquest1
        Conquest4 implies Conquest3/2/1

    and likewise for Domination.

    As a result, the first stage-1 test is true for EVERY active stage 1 through 4.
    The stage-2/3/4 branches are unreachable whenever the strategy is active.

CURRENT SAS INTERNAL CONTROL
    SAS already has a general helper in CvGameCoreUtils.cpp for this exact bitfield:

        getSASVictoryStageLevel(...)

    It checks:
        stage4
        stage3
        stage2
        stage1

    and current SAS uses:
        getSASConquestVictoryStageLevel
        getSASDominationVictoryStageLevel

    in several diagnostics/AI paths.

    This is strong internal confirmation that extracting a numeric level from the
    cumulative bitfield requires highest-to-lowest testing.

LIVE CONSUMER
    The only current InvasionGraph use is a simulated capitulation gate in
    Node::applyStep:

        if (... &&
            getConquestStage(kAttacker.m_ePlayer) <=
            getDominationStage(kAttacker.m_ePlayer))
        {
            ...
            possibly setCapitulated(...)
        }

    The policy is clearly comparative: do not expect a victim to capitulate when the
    attacker's Conquest-victory pursuit is more advanced than its Domination pursuit.

    This makes sense because a highly Conquest-driven attacker has stronger incentive
    to eliminate rivals outright, whereas Domination benefits more naturally from
    vassalized land/population contribution.

DETERMINISTIC MISCLASSIFICATION
    Consider an attacker currently at:

        Conquest stage 3
        Domination stage 2

    Such simultaneous lower-stage strategy flags are supported; the calculations are
    separate. The global "only one level-4 strategy" rule does not prevent stage-2/3
    coexistence.

    Intended numeric comparison:
        3 <= 2
        false

    Current helper:
        getConquestStage -> 1
        getDominationStage -> 1

    Current comparison:
        1 <= 1
        true

    Therefore the current simulation may enter its capitulation model when the intended
    relative-stage condition says not to.

    Another supported shape is:
        Conquest stage 4
        Domination stage 2 or 3

    Conquest4 includes Conquest1, so current again reports Conquest=1. Domination2/3
    also reports 1. The relative strategic distinction is erased.

    If only one of the two strategies is active, the zero-vs-one distinction usually
    still preserves the broad ordering. F250 specifically matters when both are active
    at different positive levels.

BEHAVIORAL CONSEQUENCE
    Once the erroneous gate opens, the remaining surrender conditions can cause
    InvasionGraph to mark the simulated defender as capitulated:

        * enough cities conquered by this attacker;
        * defender not human;
        * last living member of defender team;
        * no defender nukes;
        * no vassal blocker;
        * threshold based on current cities/personality/war state.

    Simulated capitulation then:
        * treats the defender as eliminated for further losses;
        * records capitulation acceptance on master-side nodes;
        * feeds MilitaryAnalyst result sets;
        * affects WarUtilityAspect consumers that value expected capitulation,
          vassal gains/losses and target-war outcomes.

    Thus F250 can make an aggressive conquest-focused war look as though it ends in
    vassalization when the helper's own intended stage comparison would reject that
    assumption.

ANCESTRY
    The original WarAndPeace/UWAI code already had getConquestStage and
    getDominationStage helpers that tested stage 1 before higher stages.

    Practical 2920 / 4ae382e5db,
        "Revised all code in the UWAI component"
    moved/recreated these as the local DECL_GET_VICTORY_STAGE helpers in
    InvasionGraph.cpp and preserved the same ordering.

    No later historical correction found reverses the tests.

    Base AdvCiv 1.14 retains current behavior.

RELATION TO F219 / KI#542
    F219 proved the same underlying fact -- victory-stage bits are cumulative -- in
    UWAICache::goldPerProdVictory, where stage 3 was checked before stage 4.

    F250 is NOT merged into F219 because:
        * separate file and repair locus;
        * different helper;
        * different strategy types (Conquest/Domination);
        * different behavioral output (simulated capitulation versus production value).

    F219 is useful as a cross-file semantic control, not the owner of this code.

REPAIR DIRECTION
    Base-compatible repair:
        test 4 -> 3 -> 2 -> 1 in DECL_GET_VICTORY_STAGE.

    Current SAS can avoid repeating the logic entirely by using:
        getSASConquestVictoryStageLevel(kPlayer.AI_getVictoryStageHash())
        getSASDominationVictoryStageLevel(kPlayer.AI_getVictoryStageHash())

    if those helpers are appropriate at this dependency layer.

    Preserve the existing comparative policy unless design review intentionally changes
    when conquest-focused attackers should accept capitulation.

NEGATIVE CONTROLS / SCOPE
    * Stage construction itself is intentionally cumulative and widely consumed that
      way. Do not change the hash construction.
    * If Conquest and Domination are at the same actual positive level, collapsing both
      to 1 does not change this particular <= comparison.
    * If one strategy is zero and the other positive, current generally retains the
      zero-vs-one ordering; the strongest failure is different positive levels.
    * The capitulation simulation has additional gates, so F250 does not imply every
      affected war will capitulate; it makes one prerequisite incorrectly permissive.


DORMANT 2920 TEAM-CAPITULATION REGRESSION -- NOT F251
------------------------------------------------------
The same 2920 rewrite contains a striking reversed condition in
Node::setCapitulated.

Pre-2920 functional logic:
    for another living member of the capitulating team:
        if teammate HAS already capitulated:
            continue  // avoid recursive loop
        teammate.setCapitulated(master)

2920/current logic:
        bool bHasCapitulated = teammate.hasCapitulated();
        if (!bHasCapitulated)
        {
            FAssert(!bHasCapitulated);
            continue;
        }
        teammate.setCapitulated(master);

The rewrite therefore skips exactly the teammate it says it intends to recursively
mark, while the assertion became tautological.

However the current ONLY live setCapitulated call in Node::applyStep requires:

        kDefTeam.getAliveCount() <= 1

before simulated capitulation can occur.

PlayerIter<MAJOR_CIV> iterates living major players. Under that caller gate there is no
second living defender-team member for the broken recursive branch to affect.

Result:
    real code defect / future-robustness regression,
    but no current supported behavioral reproduction.
    Do NOT allocate F251 unless another live caller or reachable multi-member
    capitulation path is found later.

This negative also clarifies F119/KI#442:
    setCapitulated still inserts the capitulated TEAM into every living MASTER-team
    member's m_capitulationsAccepted set, so F119's separate player-indexed result
    storage defect remains valid. The broken defender-teammate recursion does not
    invalidate F119.


OPENING GRAPH / CYCLE CONTROLS
------------------------------
1. `scaled` default construction
    ScaledNum() initializes m_i to 0. breakCycle's:
        scaled rSumOfEnemyThreat;
    is therefore zero-initialized, not undefined.

2. breakCycle NULL preliminary step
    step() returns NULL only when a non-clash node can no longer obtain targetCity().
    Immediately after prepareForSimulation, findAndLinkTarget/findTarget select targets
    through nearly the same reachable/known city predicates.

    No independent normal-state route to "all targeting nodes have NULL steps" has yet
    been proved. Existing UWAICache stale-order/geometry findings can create
    inconsistencies, but those remain owned by F227/F231/F233/F236 rather than a new
    InvasionGraph root.

3. resolveLosses actual-step dereference
    Preliminary and actual step calls occur without an intervening graph/city mutation
    for the selected invader. step's NULL condition is targetCity absence, independent
    of the changed army-portion arguments. No direct null-dereference root established.

4. SAS-vs-Base semantic delta
    Current InvasionGraph.cpp has no meaningful local SAS behavior change. Queue-023
    opening findings should therefore be presumed inherited unless a SAS dependency is
    specifically demonstrated.


C021-WIP01 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573

NEXT ROOT:
    F251 UNUSED

Resume from F251 unused.

Best next work:
    * continue the 2920 "Revised all UWAI" side-by-side semantic diff, especially
      condition/sign/argument-order changes that were not covered by the immediate
      2928 logical-mixup fix;
    * audit findTarget/findBestTarget/targetCity/m_iCacheIndex consistency and graph
      edge mutation independently of known UWAICache staleness roots;
    * audit applyStep city-loss/conquest/capitulation bookkeeping, team membership and
      result ownership while cross-checking F119/F130/F135/etc.;
    * audit resolveLosses/simulateComponent/breakCycle time/threat arithmetic and graph
      mutation invariants;
    * inspect historical bugfix commits 2928, 2943, 3045, 3443/3451/3477, 3946 for
      later regressions;
    * preserve clean negatives rather than manufacturing F251.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C021-WIP02 -- FILE 021 OPEN / QUEUE 023 InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T000835.zip

Comparison/history material checked:
    AdvCiv-1.14(4).zip
    current AdvCiv/UWAI history and earlier album roots
    LLM_Helpers/context/commit_diffs/*
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt

Prior physical checkpoint:
    C021-WIP01

Current file:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

New root:
    F251 / provisional KI#574

Next root:
    F252 UNUSED


F251 / provisional KI#574
-------------------------
TITLE
    A node simulated as eliminated or capitulated does not remove its outgoing invasion
    edge; recursive loss resolution can therefore let a defeated civ attack its own
    target later in the same graph component.

STATUS
    CONFIRMED inherited UWAI graph-lifecycle defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    The core lifetime mismatch is present from the initial InvasionGraph design.

GRAPH ORDER THAT MAKES THE DEFECT LIVE
    InvasionGraph edges point:
        attacker -> primary target

    resolveLossesRec resolves incoming attackers recursively BEFORE resolving the
    current node itself:

        for each player in m_targetedBy:
            attacker.resolveLossesRec();

        resolveLosses();

    Consider an ordinary acyclic component:

        A -> B -> C

    C is the sink.
    C.resolveLossesRec therefore calls:
        B.resolveLossesRec
            -> A.resolveLossesRec
            -> B.resolveLosses()    // A attacks B
        -> C.resolveLosses()        // B attacks C

    This ordering is explicitly part of the graph simulation; it is not an unusual
    re-entry or external callback.

HOW B BECOMES SIMULATED-DEAD
    During B.resolveLosses, an A attack can successfully conquer one of B's cached
    cities.

    applyStep then records:

        addCityLoss(kStepCity);

    and if the simulated city losses equal B's actual city count:

        if (m_cityLosses.size() == iCurrentCities)
            setEliminated(true);

    Simulated capitulation likewise calls:

        setCapitulated(...)
            -> setEliminated(true);

    `setEliminated` is only:

        void setEliminated(bool b) { m_bEliminated = b; }

    It does not remove B's outgoing primary-target edge.

    `setCapitulated` likewise marks the node eliminated but does not call
    changePrimaryTarget(NULL).

OUTGOING EDGE THEREFORE SURVIVES
    B's existing primary target is C.

    B was inserted into:
        C.m_targetedBy

    when the edge B -> C was established.

    Neither setEliminated nor setCapitulated removes B from C.m_targetedBy.

    applyStep has two later edge-removal checks, but both are based on ARMY power being
    fully exhausted:

        if (our Army power <= our lost Army power)
            changePrimaryTarget(NULL);

    A civ can lose all simulated cities or capitulate while retaining substantial Army
    power. In that common shape the B -> C link survives.

DOWNSTREAM C THEN INVOKES THE DEAD B
    After B.resolveLossesRec returns, C.resolveLossesRec continues and calls
    C.resolveLosses().

    C's m_targetedBy still contains B.

    C.resolveLosses iterates those invaders and calls:

        B.step(...)

    The first thing Node::step checks is only whether a non-clash attack can find a
    target city:

        pCacheCity = targetCity();
        if (pCacheCity == NULL && !bClashOnly)
            return NULL;

    It does NOT reject:
        isEliminated()
        hasCapitulated()

    B's primary target is still C, real map cities remain physically present because
    InvasionGraph is only a simulation, and B's cache still contains C's target cities.

    Therefore B.step can construct a normal attack step despite B having already been
    simulated as completely eliminated or capitulated.

SUPPORTED REPRODUCTION
    1. Three simulated war parties form A -> B -> C.
    2. B has enough remaining Army power that its army is not completely destroyed.
    3. A's attack in B.resolveLosses takes B's final simulated city, or causes B to
       capitulate.
    4. B is marked eliminated.
    5. B -> C is not unlinked.
    6. Control returns to C.resolveLosses.
    7. C still regards B as an incoming attacker.
    8. B.step uses B's surviving military and C's still-existing real/cache city data.
    9. C can suffer predicted losses/conquests from a civ that the same simulation has
       already declared defeated.

    No stale external cache or another known finding is needed.

CAPITULATION VARIANT
    setCapitulated explicitly comments:

        "Treat capitulated as eliminated, i.e. assume no further losses."

    Yet the same outgoing-edge survival lets a capitulated node remain an attacker of a
    third party later in the component.

    This is especially contradictory because the comment immediately states that the
    real rules would make the vassal join its master's wars but that such follow-up is
    intentionally NOT modeled. The current graph nevertheless lets the node continue
    its pre-capitulation offensive.

BEHAVIORAL CONSEQUENCES
    The defeated node can:
        * inflict additional military losses;
        * conquer cities;
        * alter later simulated elimination/capitulation;
        * affect graph distraction and clash state;
        * change MilitaryAnalyst's final lost-power/city-loss/conquest results;
        * feed downstream WarUtilityAspect utilities.

    This can make third-party war projections materially too favorable or too damaging
    depending on which side the zombie attacker belongs to.

WHY ARMY-ELIMINATION CLEANUP DOES NOT FIX IT
    Current applyStep correctly removes a primary target when a node's Army branch is
    completely exhausted.

    Simulation elimination and capitulation are different predicates:
        city count / surrender state, not zero Army power.

    Surviving mobile military is exactly what makes the stale edge behaviorally active.

ANCESTRY / HISTORICAL CONTROL
    The initial InvasionGraph implementation already represented elimination as a
    boolean setter without automatic edge cleanup.

    Later practical 1726 / 04eceb5fab,
        "Bugfix in UWAI invasion graph"
    corrected an edge-removal bug when the ATTACKER's Army was eliminated:

        wrong:
            changePrimaryTarget(NULL);          // on defender/this

        fixed:
            attacker.changePrimaryTarget(NULL);

    This historical fix is useful evidence that outgoing-edge removal must occur on the
    node whose offensive has ended and is an explicit graph-lifecycle operation.

    No later commit found adds the corresponding cleanup when the node itself becomes
    eliminated/capitulated through city-loss/surrender simulation.

DISTINCTION FROM EARLIER ROOTS
    F227/F231/F233/F236:
        concern UWAICache ordering/geometry/knowledge state.
        F251 reproduces with a perfectly current cache.

    F250:
        controls whether simulated capitulation is entered based on victory stages.
        F251 occurs after either a legitimate capitulation OR complete simulated city
        elimination and therefore survives an F250 fix.

    F119:
        concerns team-indexed storage of capitulation results in MilitaryAnalyst.
        F251 concerns graph-edge lifetime before result extraction.

REPAIR DIRECTION
    When a node transitions to a terminal simulated state, remove its outgoing edge
    immediately.

    A natural invariant is:

        eliminated/capitulated node => m_pPrimaryTarget == NULL

    `setEliminated(true)` could enforce:
        if (m_pPrimaryTarget != NULL)
            changePrimaryTarget(NULL);

    but implementation review should ensure that all existing callers can safely mutate
    the reverse adjacency set at that point.

    Alternatively, call changePrimaryTarget(NULL) explicitly in the city-elimination and
    capitulation transitions.

    Adding an `isEliminated() || hasCapitulated()` guard to step() is a useful defensive
    backstop, but by itself leaves stale reverse adjacency and component/threat state.
    Repairing the edge invariant is preferable; a guard can be added as robustness.

NEGATIVE CONTROLS / SCOPE
    * Losing a city without elimination/capitulation does not necessarily imply that the
      attacker should permanently abandon all offense; F251 is scoped to terminal
      simulated state.
    * If Army power is also fully exhausted, the existing Army-elimination cleanup can
      happen to remove the outgoing edge and mask F251.
    * updateTargets between simulation phases can later clear/reassign terminal nodes,
      but F251 occurs inside one resolveLossesRec component before such a phase boundary.
    * The real game's CvPlayer/CvTeam elimination callbacks are irrelevant here; this is
      wholly simulated InvasionGraph state.


OTHER CHECKS SINCE WIP01
-------------------------
1. m_iCacheIndex cross-opponent cursor suspicion -- REJECTED
    addConquest advances one node-global cache cursor past the conquered city, which at
    first looks capable of skipping higher-ranked cities of another opponent.

    During one simulation phase a node does not normally retarget from a defeated
    primary target to a different opponent; target removal produces NULL and retargeting
    occurs through updateTargets at the phase transition.

    Each simulate() call invokes prepareForSimulation(), which resets m_iCacheIndex=0.
    Existing simulated city losses are still filtered by targetCity/hasLost.

    Therefore the cursor does not establish an independent cross-opponent permanent
    skip under the current phase contract.

2. phase-one -> phase-two conquest persistence
    m_conquests/m_cityLosses intentionally survive across phases while m_iCacheIndex
    resets. targetCity consults the target owner's hasLost set, so already simulated
    lost cities are not attacked again. No duplicate conquest root.

3. breakCycle ScaledNum initialization
    Default ScaledNum is zero-initialized; no uninitialized rSumOfEnemyThreat.

4. reversed teammate-capitulation recursion
    Remains a real dormant 2920 regression, but current only caller requires defender
    team aliveCount <= 1, so no living teammate exists to exercise the broken recursive
    branch. Not numbered.


C021-WIP02 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574

NEXT ROOT:
    F252 UNUSED

Resume from F252 unused.

Best next work:
    * continue applyStep/resolveLosses terminal-state and reverse-adjacency invariants;
    * inspect phase transition preservation/reset fields (`m_bHasClashed`,
      m_arLostPower, m_rProductionInvested, conquests/losses, distractions);
    * continue 2920 semantic-diff review and later 2928/3045/3477 fix history;
    * audit cycle breaking when graph mutation removes edges during recursion;
    * audit team/player ownership in accepted capitulations and city-loss/conquest
      extraction without duplicating F119/F130-family roots;
    * promote F252 only with an independent supported producer -> state -> consumer path.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C021-WIP03 -- FILE 021 OPEN / QUEUE 023 InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Prior physical checkpoint:
    C021-WIP02

New roots:
    NONE

Next root:
    F252 UNUSED

PURPOSE
-------
Preserve the post-F251 phase-state / retargeting checks before continuing deeper into
InvasionGraph.cpp.


PHASE-STATE / RETARGETING CHECKS
--------------------------------

1. `isContinuedWar` cross-opponent m_conquests candidate -- NOT PROMOTED
    Current:

        bool Node::isContinuedWar(Node const& kOther) const
        {
            return (actual teams are at war && m_conquests.empty());
        }

    m_conquests is node-global rather than target-specific. Therefore if phase 1
    produces a conquest against A and phase 2 updateTargets retargets the same node to
    C, `isContinuedWar(C)` is false even when the node was already at war with C in the
    real game.

    This initially looked like a target-domain leak because the deployment-time shortcut:

        if (isContinuedWar(kDefender))
            iDeployTurns /= 2;

    is described as accounting for units already deployed in an ongoing real war.

    However the alternative interpretation is coherent:
        * a conquest against A proves that the simulated force has already spent time/
          position on another front;
        * the one-time "already deployed" advantage may intentionally be considered
          consumed once the simulated campaign has begun;
        * switching to C can reasonably require fresh deployment even if war with C
          already existed.

    No historical comment or predecessor was found requiring the discount to be
    per-opponent after simulated conquests.

    RESULT:
        coarse approximation, not a demonstrated correctness defect.
        Keep F252 unused.

2. m_iCacheIndex phase reset -- CLEAN
    prepareForSimulation resets m_iCacheIndex=0 on every simulate() call.

    m_cityLosses and m_conquests intentionally persist from phase 1 into phase 2, but
    targetCity filters previously lost cities through the target owner's hasLost set.

    Therefore:
        * phase-two retargeting does not inherit the phase-one cache cursor;
        * already simulated conquered cities are not simply attacked a second time.

3. terminal node state across phase boundary
    F251 is specifically a SAME-PHASE graph-edge lifetime bug.

    MilitaryAnalyst calls updateTargets between the preparation phase and phase 2.
    findAndLinkTarget sees isEliminated()/hasCapitulated() and clears terminal nodes'
    primary targets there.

    Thus phase transition happens to clean the stale edge; it does not repair the
    A->B->C same-component attack that occurs before the boundary.

4. m_arLostPower / m_conquests / m_cityLosses persistence -- INTENTIONAL
    These represent cumulative simulated physical losses/results across phases and are
    deliberately not reset by prepareForSimulation.

    ArmamentForecast adds new production to the military branches between loss phases.

5. m_rTempArmyLosses reset -- INTENTIONAL/PLAUSIBLE
    Temporary clash damage is documented as sidelined until the end of a simulation
    phase. Resetting it at prepareForSimulation matches that stated lifetime.

6. distraction fields reset -- INTENTIONAL/PLAUSIBLE
    m_rDistractionByConquest / m_rDistractionByDefense model current-front allocation
    inside one graph-loss phase. Reassignment/updateTargets between phases supplies a
    natural reset boundary.

7. m_bHasClashed persists -- INTENTIONAL/PLAUSIBLE
    A physical clash already simulated in phase 1 should not be assumed never to have
    occurred merely because phase 2 begins. No contradictory consumer was found.

8. m_rEmergencyDefPow phase reset -- OPEN APPROXIMATION, NOT A ROOT
    Emergency defenders are accumulated after city losses because ordinary armament
    forecasting does not perfectly model units trained while an invader heals/moves.

    prepareForSimulation resets this auxiliary power at the next simulation phase.
    Those defenders could conceptually survive into phase 2, but phase boundaries also
    run additional ArmamentForecast production and represent a coarse time-model reset.

    No clean no-double-count/no-loss contract was found that proves the reset wrong.
    Preserve as an approximation question only.


HISTORICAL CONTROLS
-------------------
* Practical 1726 ("Bugfix in UWAI invasion graph") fixed removal of the WRONG node's
  primary target when the attacker's Army was eliminated. This reinforces F251's
  graph-edge ownership invariant but does not supply another phase-state root.

* Practical 3045 clamps negative simulated power/loss inputs and fixes defender fleet
  power floor. Current source retains those corrections.

* Practical 3477's uniform-garrison threat mode remains current and is specifically
  used by breakCycle so lightly defended cities do not artificially inflate threat.
  No regression established in this pass.


C021-WIP03 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574

NEXT ROOT:
    F252 UNUSED

Resume from F252 unused.

Best next work:
    * continue the 2920 side-by-side rewrite review, especially arithmetic/condition
      changes not corrected by 2928;
    * audit target/link mutation in cycles and after failed attacks;
    * audit city-loss/conquest result ownership and node/team aggregation against
      existing F119/F130/F135 family roots;
    * audit `step` military-power transformations and branch accounting without
      duplicating queue-031 F237/F238 or parked MilitaryBranch findings;
    * inspect historical 2943/3045/3477/3946 changes as regression controls;
    * clean rejection remains preferable to inventing F252.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C021-WIP04 -- FILE 021 OPEN / QUEUE 023 InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Source re-anchor:
    AdvCiv-SAS_light_source_20260828T134631.zip
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(3).zip
    Civ4CE-main(10).zip

Prior physical checkpoint:
    C021-WIP03

New roots:
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577

Next root:
    F255 UNUSED

PURPOSE
-------
Continue queue 023 from F252 unused, concentrating on the post-2920 semantic migration,
`step` branch accounting / query purity, resolveLosses scheduling, and result ownership.
The current SAS file was normalized against Base AdvCiv 1.14 first: apart from already
reconciled SAS changes (notably KI#561 in initMilitary and signature/comment cleanup),
the relevant InvasionGraph behavior below is inherited. K-Mod 1.46 and Civ4CE do not
contain the UWAI InvasionGraph class, so they provide no direct cognate control here.


F252 / provisional KI#575
-------------------------
TITLE
    Successful conquest makes the ATTACKER leave Army power behind, but converts the
    unit count with the DEFENDER node's typical Army power; the attacker's remaining
    simulated offensive force therefore depends on the victim's representative unit.

STATUS
    CONFIRMED inherited UWAI/AdvCiv ownership defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Present already in the first tracked AdvCiv/UWAI import (practical 1253).

CURRENT PRODUCER -> STATE -> CONSUMER PATH
    Node::applyStep is invoked on the DEFENDER node. It explicitly recovers the
    attacker separately:

        Node& kAttacker = *m_kOuter.m_nodeMap[kStep.getAttacker()];

    On a successful city conquest the source then says:

        // The conquerer leaves part of his army behind to protect the city
        scaled rUnitsLeftBehind = 2 + GET_PLAYER(m_ePlayer).AI_getCurrEraFactor();
        ...
        scaled rPowLeftBehind = rUnitsLeftBehind *
                m_military[ARMY]->getTypicalPower(TEAMID(m_eAgent));
        rPowLeftBehind.decreaseTo(kAttacker.m_military[ARMY]->power()
                - kAttacker.m_arLostPower[ARMY]);
        kAttacker.m_arLostPower[ARMY] += rPowLeftBehind;
        kAttacker.m_arShiftedPower[ARMY] += rPowLeftBehind;

    `m_military` without a qualifier belongs to `this`, i.e. the DEFENDER.

MILITARYBRANCH OWNERSHIP PROOF
    MilitaryBranch stores:

        PlayerTypes m_eOwner;
        scaled m_rTypicalPower;

    and its constructor is owner-specific. updateTypicalUnit scans the civilization and
    trainability of m_eOwner, then caches that owner's representative unit/power.
    getTypicalPower merely returns that receiver object's m_rTypicalPower (possibly
    reduced for an observer that cannot know the typical unit).

    Therefore:

        m_military[ARMY]->getTypicalPower(...)

    in defender::applyStep is unambiguously the DEFENDER's representative Army power,
    while:

        kAttacker.m_military[ARMY]->getTypicalPower(...)

    is the attacker branch that matches the source comment and the state being reduced.

LIVE EFFECT
    rPowLeftBehind is not only logged. It is added to the attacker's m_arLostPower, so
    that power becomes unavailable to subsequent SimulationStep calculations in the
    same and later phases. It is simultaneously recorded in m_arShiftedPower so the
    final public lostPower result does not misreport the garrison as combat casualties.

    Thus the wrong receiver changes the simulated invasion trajectory:

      * advanced/strong attacker vs weak victim:
            weak defender typical power -> too little attacker Army shifted away ->
            attacker can unrealistically snowball through more cities;

      * weak/obsolete attacker vs advanced victim:
            strong defender typical power -> too much attacker Army shifted away ->
            attacker can be clipped to its entire remaining Army and stop early.

    The error is especially artificial because the victim's unit technology -- not the
    conqueror's actual/representative troops -- controls how much of the conqueror's
    force disappears from its offensive pool.

ADJACENT OWNERSHIP SMELL
    The unit-count term also reads:

        GET_PLAYER(m_ePlayer).AI_getCurrEraFactor()

    i.e. the DEFENDER's era factor despite describing how many conqueror units are left
    behind. This is very likely the same receiver-context slip, and practical 1253
    already used the defender's `id` for both current era and typical Army power.

    The typical-power receiver alone proves F252. Treat switching the era-factor owner
    to kAttacker as part of implementation review rather than requiring that secondary
    point in order for the root to stand.

ANCESTRY
    Initial tracked practical 1253 already contains:

        int nUnitsLeftBehind = 2 + GET_PLAYER(id).getCurrentEra();
        ...
        double powLeftBehind = military[ARMY]->getTypicalUnitPower()
                * nUnitsLeftBehind;
        ...
        attacker.lostPower[ARMY] += powLeftBehind;

    where `id` / `military` are the defender Node and `attacker` is separate.

    The 2920 ScaledNum rewrite preserves the same ownership. No later targeted
    InvasionGraph repair changes it. Base AdvCiv 1.14 still has the same structure.

REPAIR DIRECTION
    At minimum, derive the representative power from the force being shifted:

        kAttacker.m_military[ARMY]->getTypicalPower(TEAMID(m_eAgent))

    Review the adjacent era-factor receiver at the same time. A more ambitious model
    could use the attacker's surviving actual average Army-unit power, but that is a
    design change; correcting the owner of the existing typical-unit model is surgical.


F253 / provisional KI#576
-------------------------
TITLE
    Node::step is used as a speculative/query calculation, but a predicted successful
    city attack permanently halves the defender's emergency-defense state INSIDE step;
    discarded priority/cycle probes therefore weaken later real simulations merely by
    being evaluated.

STATUS
    CONFIRMED inherited UWAI/AdvCiv query-side mutation defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Present already in the first tracked AdvCiv/UWAI import (practical 1253).

ROOT
    Node::step(...) is declared const and conceptually builds/returns a SimulationStep.
    Most of its outputs are correctly written only into that temporary object.

    In the successful city-attack branch, however, it contains the persistent write:

        /* Assume that not all emergency defenders were able to reach this city
           (i.e. their true power is actually 2 * emergencyDefPow) */
        kDefender.m_rEmergencyDefPow /= 2;

    kDefender is reached through the graph's Node pointer, so the const qualifier on
    the attacking Node does not prevent this mutation.

SPECULATIVE CALL SITE A -- INVASION PRIORITY
    resolveLosses first evaluates EVERY current invader with the defending Army ignored:

        apSteps[*it] = ...->step(0, ...);

    The resulting steps are only probes used to calculate duration/threat and pick the
    next invader. They are then all deleted:

        SAFE_DELETE(apSteps[it->getID()]);

    If one of those provisional attacks predicts city success, merely computing its
    priority halves m_rEmergencyDefPow even though no attack has been selected/applied.

    This can happen once the defender has already lost a city in the phase, because
    applyStep intentionally accumulates emergency power after a successful city loss:

        m_rEmergencyDefPow += typicalHomeGuardPower * duration / (2 or 3);

    On the next resolveLosses iteration the query-side halving is live.

SPECULATIVE CALL SITE B -- CYCLE THREAT ESTIMATION
    breakCycle likewise calls:

        pStep = m_nodeMap[*it]->step(0, 1, false, true);

    solely to measure a threat value, then deletes pStep. A successful probe can thus
    alter the persistent defender state while the graph is only deciding which edge/
    clash should break a cycle.

NON-APPLIED ACTUAL STEP PATH
    The selected step is computed again at resolveLosses::1962 before the time-horizon
    check. Therefore even a step later deleted because:

        Simulation step exceeds time limit; not executed

    may already have halved m_rEmergencyDefPow. The world-state mutation can survive an
    explicitly NON-EXECUTED attack.

DETERMINISTIC MULTIPLICATIVE CONSEQUENCE
    Let the defender have emergency power E after its first simulated city loss.

    On the next city-resolution iteration with one invader whose provisional and actual
    attacks both predict success:

        priority probe: E -> E/2       (probe discarded)
        actual step:    E/2 -> E/4     (before applyStep)

    Only then is the actual conquest applied and new emergency power accumulated.

    With multiple successful provisional invaders, the same state can be halved once
    per discarded probe before the chosen actual attack is even evaluated. Outcome can
    therefore depend on how many candidates were inspected and their iteration order,
    not just on attacks that occurred.

DISTINCTION FROM C021-WIP03'S EMERGENCY-POWER RESET QUESTION
    WIP03 left `prepareForSimulation` resetting m_rEmergencyDefPow between phases as a
    coarse-model approximation, not a root.

    F253 is independent and much stronger: within ONE phase, a function used for
    counterfactual measurement changes persistent combat state even when its result is
    thrown away or rejected by the time limit.

ANCESTRY
    Practical 1253 already has `defender.emergencyDefPow /= 2` inside the step function,
    while the same initial implementation also uses step objects as disposable
    resolveLosses probes. The 2920 rewrite carries this forward as
    `kDefender.m_rEmergencyDefPow /= 2`. Base AdvCiv 1.14 remains identical here.

REPAIR DIRECTION
    Keep Node::step side-effect-free with respect to persistent graph state.

    If halving emergency defense is still desired after an ACTUALLY APPLIED successful
    city conquest, move that state transition to applyStep (or encode it as an explicit
    SimulationStep state delta and apply it only there). It must not execute during
    threat/duration probes or during a step rejected by the time horizon.

    A useful regression invariant is:
        calling step(...) and deleting the result without applyStep must leave every
        Node's persistent simulation state byte-for-byte/field-for-field unchanged.


F254 / provisional KI#577
-------------------------
TITLE
    resolveLosses treats zero-defense probe duration as an optimistic lower bound and
    aborts the whole defender when that bound or one selected actual step misses the
    horizon; naval probes can be LONGER than the real repelled landing, and one blocked
    invader does not prove that other concurrent invaders are out of time.

STATUS
    CONFIRMED inherited UWAI/AdvCiv time-horizon scheduling defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Core logic and even the unresolved per-node-time-limit comment are present in the
    first tracked practical 1253 implementation.

MANIFESTATION A -- THE DOCUMENTED "LOWER BOUND" IS NOT A LOWER BOUND FOR NAVAL WAR
    resolveLosses creates priority probes with:

        step(0, attackerPortion)

    where the first argument is the DEFENDER Army portion. step deliberately reuses that
    same portion for the defending Fleet in a naval battle:

        rDefFleetPow = (defender fleet - losses) * confidence *
                rArmyPortionDefender;

    Therefore every priority probe has rDefFleetPow == 0.

    For a viable attacking fleet (rFleetPow > 1), the probe necessarily wins the naval
    battle and proceeds to the landing/city-siege path. That path can include deployment,
    healing and bombardment turns.

    The REAL selected step has a nonzero defender share. Its defender fleet can instead
    repel the landing. On that branch step terminates immediately with:

        setDuration(iDeployTurns + (bSneakAttack ? 0 : 2));
        return;

    while still recording genuine Fleet/Logistics and potentially drowned-Army losses.

    Hence a feasible shape is:

        priority probe, defender fleet forced to zero:
            landing succeeds -> city siege -> duration 13

        real step, defender fleet participates:
            landing repelled -> duration 7

    Yet resolveLosses states:

        "Shortest duration is an optimistic lower bound ...
         If the shortest duration exceeds the time limit, all actual durations ..."

    and executes:

        if (iShortestDuration > iTimeLimit)
            break;

    With a 10-turn horizon the example above is therefore discarded before the actual
    7-turn naval combat is ever computed. Real in-horizon fleet/logistics/army losses
    and the failed invasion can disappear from the analysis.

MANIFESTATION B -- ONE SELECTED ACTUAL OVERFLOW ABORTS OTHER INVADERS
    Even when the shortest probe is inside the horizon, resolveLosses recomputes only
    that selected invader with its assigned defender share. If its actual cumulative
    duration exceeds the limit, current code does:

        delete &kNextStep;
        log("Simulation step exceeds time limit; not executed");
        break;

    The `break` abandons ALL remaining invaders targeting this defender.

    That inference is not valid. Candidate ordering was based on each invader's
    zero-defense probe, while the actual-duration increase depends on that invader's
    defender share, route (land/naval), target city, bombardment/cavalry path and prior
    simulated turns. A second invader can still have an actual step within the horizon.

    The source itself flags this area as unresolved:

        "Per-node time limit. Not sure if really needed. Would have to
         toggle isTargeting and unset m_pPrimaryTarget as well in the
         else branch (instead of breaking)."

    The comment has survived since the first tracked implementation.

WHY THIS IS NOT JUST A LONG-HORIZON APPROXIMATION
    The first manifestation can suppress a combat that the same step model says occurs
    BEFORE the horizon. This is not merely choosing not to model events after the
    horizon; it is an invalid bound used to decide whether to evaluate the event at all.

    The second manifestation is a scheduling/selection problem among independent
    concurrent invaders: one candidate being too late does not make every other
    candidate too late.

F253 INTERACTION
    Before F253 is repaired, even a provisional/actual step rejected by these horizon
    branches may already mutate emergency-defense power. F253 and F254 therefore amplify
    each other, but remain independent:

        F253 = speculative calculation mutates persistent state;
        F254 = invalid horizon bound / global abort can skip valid combat.

REPAIR DIRECTION
    Do not rely on the current zero-defender step duration as a universal lower bound.
    A safe lower bound would have to remain <= every possible actual branch (e.g. a
    carefully defined deployment-only bound), or the code can compute actual candidates
    before concluding that no step fits.

    When one selected actual step exceeds the per-node horizon, do not `break` the whole
    defender's loop merely for that reason. Mark/exclude that invader locally for this
    resolveLosses pass and continue considering the others, without necessarily changing
    its persistent graph target. Exact scheduling policy deserves implementation review,
    but the current global inference is unsound.

ANCESTRY
    The initial practical 1253 InvasionGraph already contains:
        * priority `step(0, ...)` calls;
        * the "optimistic lower bound" comment and global shortest-duration break;
        * the later actual-step time check;
        * the comment questioning the per-node limit;
        * `break` when the selected actual step exceeds it.

    The 2920 rewrite largely renames/types this logic rather than creating it. Later
    2928/2943/3045/3477/3946 repairs do not remove the horizon assumptions.


ADDITIONAL CONTROLS / NON-ROOTS THIS PASS
----------------------------------------
1. Capitulation result ownership after F119/KI#442 -- CURRENT SAS CLEAN AT PRODUCER
    MilitaryAnalyst::prepareResults now unions each node's accepted-capitulation set into:

        m_capitulationsAcceptedPerTeam[TEAMID(ePlayer)]

    Current SAS therefore contains the already-reconciled F119 repair. No new queue-023
    result-index root was found here.

2. setCapitulated teammate recursion inversion -- STILL DORMANT UNDER LIVE CALLER
    The post-2920 branch remains backwards (`if (!bHasCapitulated) continue;`), but the
    only live caller requires the surrendering REAL team to have `getAliveCount() <= 1`.
    No living teammate can reach that recursion under the current gate. Preserve as a
    dormant regression, not F255.

3. Existing later-history controls remain retained
    * practical 2928 fixes the attacker-area `!bNaval || bClashOnly` condition;
    * practical 3045 retains nonnegative simulated-loss/fleet clamps;
    * practical 3477 retains uniform-garrison threat calculation for cycle breaking;
    * practical 3946 retains the human rally multiplier.

4. K-Mod / Civ4CE direct comparison
    Neither source contains UWAI InvasionGraph.cpp/.h. No cognate implementation was
    available there to confirm or refute F252-F254; ancestry classification therefore
    rests on current Base AdvCiv and the local AdvCiv/UWAI history.


C021-WIP04 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577

NEXT ROOT:
    F255 UNUSED

Resume from F255 unused.

Best next work:
    * continue the 2920 old/new side-by-side arithmetic/condition audit, now with special
      attention to hidden writes inside nominally computational helpers;
    * challenge F252-F254 against exact call/lifetime assumptions before durable closure;
    * continue cycle mutation and two-node/multi-node clash accounting after removing
      F253's speculative-state contamination from the mental model;
    * inspect SimulationStep power-loss bookkeeping and naval branch dimensional/portion
      arithmetic, while treating the source's already-explicit Logistics FIXME as known
      approximation rather than manufacturing a duplicate root;
    * continue target-city/cache progression and failure/horizon retarget semantics;
    * clean rejection remains preferable to inventing F255.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C021-WIP05 CONTINUATION -- QUEUE 023 InvasionGraph.cpp
GPT-5.6-Sol, 2026-08-28
===============================================================================

This continuation freezes the verbal C021-WIP04 -> WIP05 handoff and resumes the
same queue-023 first pass. No source files were modified.


F255 / provisional KI#578
-------------------------
TITLE
    Two-node cycle clash stores the LOSER'S army portion as the WINNER'S
    conquest-distraction portion; asymmetric outside pressure is transferred to the
    wrong civilization after the clash.

STATUS
    CONFIRMED inherited UWAI/AdvCiv ownership/accounting defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Present from the first tracked UWAI/AdvCiv InvasionGraph implementation.

CURRENT PRODUCER -> STATE -> CONSUMER PATH
    breakCycle computes two independent portions for a two-node cycle:

        rFirstArmyPortion  = portion of first node's own army available for clash
        rSecondArmyPortion = portion of second node's own army available for clash

    and invokes:

        kFirst.clash(rFirstArmyPortion, rSecondArmyPortion)

    Node::clash names those values correspondingly:

        clash(scaled rArmyPortion, scaled rTargetArmyPortion)

    It then executes the symmetric combat by asking the target node to construct the
    clash step:

        SimulationStep& kClashStep =
                *kTarget.step(rArmyPortion, rTargetArmyPortion, true);
        applyStep(kClashStep);

    The arguments are therefore still:
        rArmyPortion       = THIS node's own committed portion
        rTargetArmyPortion = TARGET node's own committed portion

    But the post-clash distraction bookkeeping swaps ownership:

        if (kClashStep.isAttackerSuccessful())
        {
            // kTarget (the step attacker) won
            kTarget.m_rDistractionByConquest = rArmyPortion;       // WRONG owner
            m_rDistractionByDefense = rTargetArmyPortion;
        }
        else
        {
            // this node won
            m_rDistractionByConquest = rTargetArmyPortion;         // WRONG owner
            kTarget.m_rDistractionByDefense = rArmyPortion;
        }

    m_rDistractionByConquest is later consumed when computing how much of a node's Army
    is available to defend against a third party. It is therefore not merely a log or
    naming error.

WHY THE PORTIONS ARE NOT INTERCHANGEABLE
    They are only equal in a symmetric two-node cycle. breakCycle derives each from that
    node's own outside threats and clamps each independently. For example:

        A can devote 80% of its Army to B
        B can devote 35% of its Army to A

    If A wins, the surviving Army continuing the conquest of B is A's 80% commitment.
    Current bookkeeping instead records 35% as A's conquest distraction. A third-party
    attacker against A will therefore be allowed to use too much of A's Army for
    defense. If the asymmetry is reversed, A can instead be made too distracted.

ANCESTRY
    The original tracked implementation documents the same parameter ownership directly
    (armyPortion1 = this node, armyPortion2 = target node) yet performs the same swapped
    winner assignment. The later 2920 type/renaming rewrite preserves it. Base AdvCiv
    1.14 and SAS remain equivalent here.

REPAIR DIRECTION
    Preserve ownership when assigning the winner's conquest distraction:

        if target wins:
            kTarget.m_rDistractionByConquest = rTargetArmyPortion
            this->m_rDistractionByDefense    = rArmyPortion

        if this wins:
            this->m_rDistractionByConquest   = rArmyPortion
            kTarget.m_rDistractionByDefense  = rTargetArmyPortion

    Recheck the exact intended meaning of m_rDistractionByDefense at implementation time,
    but the currently documented ownership of both clash portions makes the conquest-side
    swap itself unambiguous.


F256 / provisional KI#579
-------------------------
TITLE
    Practical 2923 changed the ally-confidence army-portion adjustment from assignment
    to multiplication, accidentally squaring every partial portion at neutral confidence.

STATUS
    CONFIRMED Base AdvCiv regression, inherited by AdvCiv-SAS.
    Regression boundary: practical 2923, commit 9ee1db509d
    ("Minor adjustments to the revised UWAI code").

CURRENT CODE
    The comment says the absent portion -- 1 minus the supplied portion -- should be
    adjusted for confidence in allies:

        /* Adjust the portion of the army that is assumed to be absent,
           i.e. 1 minus portion */

        rArmyPortionDefender *= scaled::max(0,
                1 - (1 - rArmyPortionDefender) * rConfAlliesAtt);
        rArmyPortionAttacker *= scaled::max(0,
                1 - (1 - rArmyPortionAttacker) * rConfAlliesDef);

    Let p be the incoming army portion and c the relevant ally-confidence factor.
    The intended expression described by the comment and used immediately before 2923 is:

        p' = max(0, 1 - (1 - p) * c)

    Current code instead computes:

        p' = p * max(0, 1 - (1 - p) * c)

NEUTRAL-CONFIDENCE INVARIANT
    With c = 1, confidence is neutral and should leave p unchanged:

        intended: p' = 1 - (1-p) = p
        current:  p' = p * p = p^2

    Examples:
        p = 0.50 -> 0.25
        p = 0.70 -> 0.49
        p = 0.90 -> 0.81

    Only the trivial endpoints 0 and 1 survive unchanged.

HISTORY / REGRESSION BOUNDARY
    * the pre-rewrite UWAI implementation used assignment;
    * practical 2920's revised/scaled implementation still used assignment;
    * practical 2923 changes ONLY these operators from `=` to `*=` while retaining the
      same explanatory comment and the same right-hand expression;
    * current Base AdvCiv 1.14 and SAS retain the 2923 multiplication.

    The local tracked diff is explicit:

        - rArmyPortionDefender = scaled::max(...)
        + rArmyPortionDefender *= scaled::max(...)

        - rArmyPortionAttacker = scaled::max(...)
        + rArmyPortionAttacker *= scaled::max(...)

EFFECT
    The distortion is concentrated in partial allocations -- exactly the multi-front and
    cycle cases where army portions carry the most information. It can substantially
    understate Army/Fleet participation before the actual combat model is even entered,
    and F255 can then mis-attribute the already-distorted portion after a cycle clash.

REPAIR DIRECTION
    Restore assignment (`=`) for both expressions, matching the original semantics,
    practical 2920, and the still-present comment. Add a regression invariant that
    neutral ally confidence maps every p in [0,1] to itself.


F257 / provisional KI#580
-------------------------
TITLE
    Open-field/cycle Army-clash casualties are stored in confidence-scaled units because
    combat/trait modifiers are divided back out but rConfAtt/rConfDef are not; naval and
    city-siege paths explicitly undo confidence and establish the intended unit convention.

STATUS
    CONFIRMED inherited UWAI/AdvCiv dimensional/accounting defect.
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    Present in the initial tracked UWAI implementation.

PRODUCER
    The Army clash first constructs perceived/modified combat powers:

        rArmyPowModified    = rArmyPow    * rArmyModAttCorr * rConfAtt;
        rDefArmyPowModified = rDefArmyPow * rArmyModDefCorr * rConfDef;

    clashLossesWinnerLoser and clashLossesTemporary operate on those modified powers, so
    their returned quantities are in the same modified/confidence-scaled power space.

CONVERSION BACK TO BRANCH POWER -- CURRENT CLASH PATH
    After the clash, current code removes only the combat modifier:

        rLossesLoser  /= rArmyModDefCorr; // or attacker corr depending on winner
        rLossesWinner /= rArmyModAttCorr;
        kStep.reducePower(..., ARMY, rLosses...);

    Temporary losses likewise divide only by the corresponding Army modifier:

        kStep.setTempLosses(rTempLosses / rArmyMod...Corr);

    The confidence multiplier remains embedded.

INTERNAL CONTROL -- OTHER PATHS IN THE SAME FUNCTION
    Naval losses explicitly divide by confidence when converting perceived battle losses
    back to stored branch power:

        rLossesAtt = rrLosses... / rConfAtt;
        rLossesDef = rrLosses... / rConfDef;

    Later city-siege losses likewise divide by both the Army correction and confidence,
    e.g. denominator `(rArmyModAttCorr * rConfAtt)`.

    The source comment immediately above the Army clash also says:

        "These bonuses are removed again (through division) when applying losses"

    Confidence is part of the multiplication that created the modified power, but is not
    removed in this one path.

WHY THIS IS NOT JUST 'CONFIDENCE SHOULD AFFECT EXPECTED LOSSES'
    Confidence already affects which side appears stronger and therefore the clash result
    in the perceived-power space. Once a returned quantity is converted into the stored
    `m_arLostPower` unit, leaving the epistemic multiplier in the measurement changes the
    branch-power unit itself.

    For the same physical force and same combat modifier, merely changing rConf from
    1.0 to 1.5 can inflate the recorded branch-power loss; changing it to 0.7 can shrink
    it. Naval and siege code deliberately undo exactly that scaling.

CONSUMERS
    SimulationStep losses are applied directly to m_arLostPower in applyStep. Those
    cumulative losses reduce Army/Cavalry availability in later simulated attacks and
    can trigger the "Army eliminated" target unlink. Temporary losses additionally reduce
    within-phase Army availability. The error therefore propagates beyond reporting.

REPAIR DIRECTION
    Convert clash losses and temporary losses back with both factors, consistently with
    the naval and city-siege branches:

        / (rArmyMod...Corr * rConf...)

    Review the cavalry proportional loss after this conversion, but do not apply a second
    confidence correction there because Cavalry loss is derived from the corrected Army
    loss.


F258 / provisional KI#581
-------------------------
TITLE
    City-defense trait bonus is read from the graph-wide evaluating agent instead of the
    defending player, giving or denying +30% city-defense modifier based on the wrong civ.

STATUS
    CONFIRMED inherited UWAI/AdvCiv owner-identity defect.
    Present in current Base AdvCiv 1.14 and current AdvCiv-SAS.
    Present in the first tracked UWAI implementation under the older Protective-trait name.

CURRENT CODE
    During city-garrison strength calculation:

        MilitaryBranch const& kDefHomeGuard =
                *kDefender.m_military[HOME_GUARD];
        ...
        scaled rCityDefenderMod = ...;
        if (GET_PLAYER(kDefender.m_eAgent).uwai().getCache().hasDefensiveTrait())
            rCityDefenderMod += 0.3;

    But Node construction assigns:

        m_eAgent(kOuter.m_eAgent)

    i.e. every node receives the SAME analyst/evaluating player ID. `m_eAgent` is not the
    node's owner. The defender's owner is `kDefender.m_ePlayer`.

MANIFESTATIONS
    * Agent has Defensive trait, actual defender does not:
        every simulated defender can receive the +30% city-defense adjustment.
    * Agent lacks Defensive trait, a third-party/target defender has it:
        that defender receives no trait adjustment.
    * Only when defender == agent (or both happen to have the same trait state) is the
      result accidentally correct.

    rCityDefenderMod subsequently enters the garrison/city-defense power calculation and
    can alter whether the attacker succeeds, losses, threat and duration.

ANCESTRY
    The first tracked UWAI implementation used the same wrong identity via its graph-wide
    `defender.weId` and checked `hasProtectiveTrait()`. The later rewrite renamed the
    field to `m_eAgent`; the mod's later trait abstraction renamed the cached trait concept
    to Defensive, but ownership remained unchanged. This is not introduced by SAS.

REPAIR DIRECTION
    Query the defender owner:

        GET_PLAYER(kDefender.m_ePlayer).uwai().getCache().hasDefensiveTrait()

    A regression test should evaluate A attacking B from the viewpoint of C and vary
    Defensive trait independently on B and C; only B's trait may change city-defense power.


ADDITIONAL CONTROLS / REJECTIONS BETWEEN WIP04 AND WIP05
---------------------------------------------------------
1. SimulationStep omitted scaled initializers -- SAFE
    m_rThreat and m_rTempLosses are not named in the constructor initializer list, but
    `scaled`/ScaledNum has a zeroing default constructor. Paths that do not call the
    corresponding setter therefore observe zero rather than indeterminate memory.
    No F-number assigned.

2. GET_TEAM(PlayerTypes) suspicion -- SAFE IN ADVCIV/SAS
    Calls such as GET_TEAM(m_ePlayer) are not raw indexing of a team array by PlayerTypes.
    AdvCiv's CoreAI helper deliberately overloads GET_TEAM(PlayerTypes) and converts via
    TEAMID. This is the already-known false-positive family behind the retracted
    F113/KI#436 concern; do not recycle it in queue 023.

3. Garrison-only priority probe allegedly zeroing garrison -- CURRENT CODE SAFE
    A short-lived historical revision around practical 2943 capped too much, but the
    subsequent revised-UWAI merge leaves full garrison power in place and caps only the
    additional defender-advantage component. Current `step(0, ...)` still ignores the
    mobile defender Army as intended while retaining garrisons. No F-number assigned.

4. isContinuedWar uses global m_conquests.empty() rather than opponent-specific conquest
    history -- NOT PROMOTED
    Cross-front state flow is real, but UWAI uses one aggregate Army pool and the code can
    plausibly intend the initial "already deployed" advantage to be consumed once the
    aggregate Army has achieved any conquest. This is too model-policy-dependent for the
    audit's bug threshold without stronger contradictory evidence.


C021-WIP05 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581

NEXT ROOT:
    F259 UNUSED

Resume from F259 unused.

Best next work:
    * continue SimulationStep naval/logistics and city-siege loss dimensional accounting;
    * continue 2920/2923 migration arithmetic/condition audit beyond the already-proven
      portion regression, especially branches whose comments encode an invariant;
    * inspect cycle break/multi-node retarget state after F255 and speculative mutation
      after F253 are mentally corrected;
    * inspect target-city/cache progression and post-failure/horizon semantics;
    * trace all SimulationStep result ownership and phase-lifetime consumers;
    * retain the explicit Logistics power FIXME as a documented approximation unless an
      independent arithmetic/owner-state defect is found;
    * F259 stays unused until a new root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C021-WIP06 CONTINUATION -- QUEUE 023 InvasionGraph.cpp
GPT-5.6-Sol -- 2026-08-28
===============================================================================

F259 / provisional KI#582
-------------------------
TITLE
    MaxWarMinAdjacentLand personality scaling can push the clash meeting point past the
    defender, producing a negative defender deployment distance; debug logging then
    violates scaled::uround's non-negative contract and release arithmetic can boost
    defender Army power above 100% instead of applying a deployment penalty.

STATUS
    CONFIRMED Base AdvCiv/UWAI regression introduced in practical 1341
    (a5724ff58159f2c073fe9b3b55ddcc30210dbf13, "Some small AI changes").
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.

PRODUCER / CURRENT CLASH GEOMETRY
    For bClashOnly, current code begins with one fixed clash distance for each side:

        rDeploymentDistAttacker = rClashDist;
        rDeploymentDistDefender = rClashDist;

    It then allocates the attacker's share by Army-power ratio, multiplies THAT spatial
    share by the evaluating leader's MaxWarMinAdjacentLand personality factor, and
    subtracts the inflated result from the defender's fixed distance:

        rDeploymentDistAttacker *= rArmyPow / (rArmyPow + rDefArmyPow);
        rDeploymentDistAttacker *= rMaxWarMinAdjLandFactor;
        rDeploymentDistDefender -= rDeploymentDistAttacker;

    `rMaxWarMinAdjLandFactor` is derived from an XML value documented as [0,4]. Current
    SAS XML actually uses all values 0,1,2,3,4. The resulting factors are approximately:

        XML 0 -> 4/6 = 0.667
        XML 1 -> 1.000
        XML 2 -> 7/6 = 1.167
        XML 3 -> 8/6 = 1.333
        XML 4 -> 9/6 = 1.500

NEGATIVE-DISTANCE THRESHOLDS
    Let s = attackerArmy / (attackerArmy + defenderArmy). The defender distance becomes
    negative whenever s * factor > 1.

        factor 1.50  -> s > 2/3 -> attacker Army > 2x defender Army
        factor 4/3   -> s > 3/4 -> attacker Army > 3x defender Army
        factor 7/6   -> s > 6/7 -> attacker Army > 6x defender Army

    These are reachable power ratios, not mathematical edge cases. Multiple shipped
    leader personalities use XML 3 and 4.

CONSUMERS / TWO CONCRETE FAILURE MODES
    1. Debug-domain violation
       The result is logged through:

           rDeploymentDistDefender.uround()

       ScaledNum::uround explicitly documents that it works only on non-negative values
       and asserts `m_i >= 0`. A sufficiently one-sided clash therefore trips the debug
       invariant solely because the meeting-point arithmetic crossed below zero.

    2. Release semantic inversion
       The defender deployment modifier is then:

           max(100 - 2 * rDeploymentDistDefender, 50) / 100

       A negative distance makes `100 - 2*negative` GREATER than 100, so the defender's
       Army power is multiplied above its original value. Moving the estimated meeting
       point beyond the defender's origin therefore becomes a combat-power bonus.

ANCESTRY
    The first tracked UWAI code split the fixed clash distance by Army-power share without
    this personality multiplier, keeping both sides' spatial portions within the original
    distance. Practical 1341 introduced MaxWarMinAdjacentLand scaling and specifically
    added the multiplier between the attacker's share calculation and subtraction from
    the defender. The later scaled-number rewrite preserved the arithmetic. Practical
    2923 changed relevant logging to uround, making the pre-existing negative-domain
    state explicitly assertable. Current Base AdvCiv and SAS retain it.

REPAIR DIRECTION
    Keep physical meeting geometry bounded and non-negative. The cleanest interpretation
    is to split rClashDist by Army-power share first:

        meetingAtt = clashDist * attackerShare;
        meetingDef = clashDist - meetingAtt;

    and apply MaxWarMinAdjacentLand only to the attacker's deployment penalty/time if the
    personality effect is still desired. If personality is intended to shift the meeting
    point itself, cap the spatial attacker share at rClashDist before deriving the
    defender share. Merely changing uround to round would hide the debug symptom while
    retaining the >100% defender-power inversion and is not a sufficient repair.


F260 / provisional KI#583
-------------------------
TITLE
    Attacker battle-area weighting scales total Army power but not its overlapping
    Cavalry sub-branch, breaking Cavalry<=Army and allowing inflated Cavalry availability
    and losses; the corresponding defender omission was explicitly fixed in practical
    1366 but the attacker sibling remains.

STATUS
    CONFIRMED inherited AdvCiv/UWAI branch-accounting omission.
    Attacker area weighting introduced in practical 1289 (ed1826faf1...) with the omission.
    Defender-side sibling fixed in practical 1366
    (82ed992626a03f21b42345058cfdea526337641e).
    Present in Base AdvCiv 1.14 and current AdvCiv-SAS.

BRANCH INVARIANT BEFORE AREA WEIGHTING
    CAVALRY is an overlapping subset/sub-branch of ARMY. Current step() explicitly clamps
    both sides before battle-area weighting:

        rCavPow.decreaseTo(rArmyPow);
        rDefCavPow.decreaseTo(rDefArmyPow);

    This encodes the expected Cavalry<=Army invariant for the force represented by the
    step.

PRODUCER -- ASYMMETRIC AREA FILTER
    `rAreaWeightAtt` and `rAreaWeightDef` estimate the fraction of each civ's military
    actually available in the battle area. Current attacker path:

        if (rAreaWeightAtt < 1)
            rArmyPow *= rAreaWeightAtt;

    Current defender path:

        if (rAreaWeightDef != 1)
        {
            rDefArmyPow *= rAreaWeightDef;
            rDefCavPow *= rAreaWeightDef;
        }

    The attacker Cavalry value is therefore left at whole-force scale while its enclosing
    Army value is reduced to local-area scale.

CONCRETE INVARIANT BREAK
    Example before weighting:
        Army = 100, Cavalry = 80, areaWeightAtt = 0.5

    Current result:
        Army = 50, Cavalry = 80

    This is impossible under the model's own overlapping-branch interpretation and
    directly undoes the earlier `rCavPow.decreaseTo(rArmyPow)` safety clamp.

DOWNSTREAM CONSUMERS
    * Open-field/cycle loss partition derives Cavalry ratios from this unweighted Cavalry
      against weighted Army. A clamp to 1 prevents an unbounded ratio there, but can make
      all local Army losses appear Cavalry-heavy.
    * City combat uses:

          rLossesAttArmy * rCavPow / rArmyPow

      without a ratio clamp. Once rCavPow > rArmyPow, a 20 Army-power casualty estimate
      can record more than 20 Cavalry power of casualties. Later cumulative branch clamps
      do not restore the per-step dimensional consistency and can prematurely exhaust the
      Cavalry branch for subsequent simulation.
    * Naval landing first scales Army and Cavalry together by the landing ratio, but later
      attacker area weighting can recreate the mismatch.

HISTORY / STRONG INTERNAL CONTROL
    Practical 1289 introduced area weighting and initially multiplied only Army power on
    BOTH attacker and defender sides. Practical 1366 later added exactly:

        targetCavPow *= areaWeightDef;

    to the defender path. Its commit message explicitly says UWAI fixed a v0.93 bug where
    military power was "grossly underestimated" for defenders with cities on several
    landmasses and many Mounted/fast units. That establishes that overlapping Cavalry has
    to follow the same area projection as Army. No corresponding `cavPow *= areaWeightAtt`
    was added, and the attacker omission remains in Base AdvCiv 1.14 and SAS.

REPAIR DIRECTION
    Apply the same battle-area factor to the attacker Cavalry sub-branch whenever Army is
    filtered:

        rArmyPow *= rAreaWeightAtt;
        rCavPow  *= rAreaWeightAtt;

    Reapplying `rCavPow.decreaseTo(rArmyPow)` afterward is a cheap defensive invariant,
    though mathematically it should become redundant once both values are scaled by the
    same non-negative factor.


ADDITIONAL CONTROL / REJECTION AFTER WIP05
------------------------------------------
Target-city cache cursor across phase-II retargeting -- REJECTED
    `findBestTarget()` searches the cache independently while `targetCity()` advances a
    monotonic `m_iCacheIndex`, which initially suggested that cities skipped while fighting
    one opponent might become unreachable after phase-II retargeting. Lifecycle tracing
    disproved that path: `prepareForSimulation()` resets m_iCacheIndex to 0 at the start of
    every simulate() phase, and a node does not retarget across opponents inside a single
    simulate() call after advancing the cursor. Phase-II therefore begins with a fresh
    cursor and explicitly skips already-lost cities. The nearby assertion remains useful
    diagnostics but does not establish the proposed cross-opponent failure. No F-number.


C021-WIP06 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583

NEXT ROOT:
    F261 UNUSED

Resume from F261 unused.

Best next work:
    * continue attacker/defender overlapping-branch invariants beyond the now-proven
      area-weight omission, especially loss partition after cavalry-specialized attacks;
    * inspect smaller historical InvasionGraph bugfix commits (1335/1358/1387/1395/1726/
      1879/2767 and the post-2920 cleanups) for one-sided fixes whose sibling survives;
    * continue SimulationStep naval/logistics, siege and clash dimensional accounting;
    * continue cycle/retarget/result ownership and horizon-state consumers;
    * keep documented approximations (notably Logistics cargo-vs-power) separate from
      independently provable owner/arithmetic/state defects;
    * F261 stays unused until another root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C021-WIP07 CONTINUATION -- QUEUE 023 InvasionGraph.cpp
GPT-5.6-Sol -- 2026-08-28
===============================================================================

F261 / provisional KI#584
-------------------------
TITLE
    Attacker Logistics losses in naval combat retain an inverse confidence factor while
    defender Logistics losses are confidence-neutral; confidence therefore changes the
    physical amount of transport capacity destroyed on the attacking side.

STATUS
    CONFIRMED inherited AdvCiv/UWAI dimensional-accounting asymmetry.
    Present in the first tracked UWAI import (practical 1253), Base AdvCiv 1.14 and
    current AdvCiv-SAS. Practical 1335 fixed a separate defender Logistics-owner error
    but did not repair this confidence mismatch.

MODEL / UNITS
    MilitaryBranch::Logistics::power() is explicitly cargo capacity, not combat power.
    Fleet power, by contrast, is a combat-power rating. The existing source FIXME already
    acknowledges that taking a Logistics/Fleet ratio is a rough dimensional approximation;
    that approximation is NOT this finding. F261 concerns an additional confidence factor
    that appears on only one side of an otherwise symmetric conversion.

PRODUCER -- ATTACKER
    Current naval setup uses confidence-scaled Fleet power:

        rFleetPow = physicalFleetPower * rConfAtt * rArmyPortionAttacker;

    but attacker Logistics portion is computed as:

        rLogisticsPortion = rCargoCap / rFleetPow;

    with no compensating rConfAtt in the numerator. Fleet losses returned by
    clashLossesWinnerLoser are in confidence-scaled Fleet-power units and are subsequently
    converted back to physical Fleet power by division by rConfAtt. The attacker transport
    loss paths then effectively apply confidence twice in opposite places:

        win path:
            rrLossesWL.first * rLogisticsPortion / rConfAtt

        loss path:
            rLossesAtt * rLogisticsPortion
            where rLossesAtt = rrLossesWL.second / rConfAtt

    Since rLogisticsPortion itself already contains 1/rConfAtt through rFleetPow, physical
    Logistics losses retain an extra inverse-confidence factor.

INTERNAL CONTROL -- DEFENDER
    Defender setup explicitly includes confidence in the numerator:

        rLogisticsPortionDef = physicalCargoCapacity * rConfDef / rDefFleetPow;

    Because rDefFleetPow already contains rConfDef, this ratio is confidence-neutral.
    Later division of confidence-scaled Fleet casualties by rConfDef therefore converts
    defender cargo losses back to physical capacity without leaving a confidence factor.

ALGEBRAIC CONSEQUENCE
    Holding the physical Fleet/cargo composition and battle casualty fraction fixed:

        defender physical Logistics loss  ~ actual cargo casualty fraction
        attacker physical Logistics loss  ~ actual cargo casualty fraction / rConfAtt

    Thus an otherwise identical attacking force can lose roughly twice as much physical
    transport capacity at rConfAtt=0.5 as at rConfAtt=1, while rConfAtt=1.5 produces only
    about two-thirds as much transport loss. Confidence is epistemic/perceptual in this
    simulation and is explicitly divided back out of physical Fleet losses; it should not
    silently change the measurement unit of the transport casualties on only one side.

ANCESTRY
    The first tracked UWAI import already computes attacker logisticsPortion as
    cargoCap/fleetPow while fleetPow contains confAtt, and later divides the loss by
    confAtt. Practical 1335 repairs a concrete neighboring defender bug:

        military[LOGISTICS] -> defender.military[LOGISTICS]

    in logisticsPortionTarget, but preserves the defender-side *confDef and leaves the
    attacker omission unchanged. The 2920 scaled-number rewrite preserves the asymmetry.

REPAIR DIRECTION
    Preserve the existing cargo-vs-Fleet approximation if desired, but make the confidence
    conversion symmetric. For example:

        rLogisticsPortion = rCargoCap * rConfAtt / rFleetPow;

    Do this in the ratio only; do not globally confidence-scale rCargoCap, because that
    variable later represents actual landing capacity. Then both sides convert perceived
    Fleet casualties to physical cargo losses under the same dimensional convention.


C021-WIP07 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584

NEXT ROOT:
    F262 UNUSED

Resume from F262 unused.

Immediate next work:
    * finish challenge of repeat-trip cargo multiplier ordering relative to naval transport
      casualties; prove or reject before assigning F262;
    * continue naval/logistics casualty and drowned-Army dimensional accounting;
    * continue SimulationStep branch invariants, result ownership and return-path state;
    * continue history-driven one-sided-fix search without assuming symmetry alone is a bug;
    * F262 stays unused until another root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C021-WIP08 CONTINUATION -- queue 023 InvasionGraph.cpp
===============================================================================

F255 / provisional KI#578 -- ADDENDUM: BOTH CYCLE-DISTRACTION ASSIGNMENTS SWAP OWN ARMY PORTIONS
-----------------------------------------------------------------------------------------------
The earlier F255 wording emphasized the winner's conquest distraction. A deeper consumer trace
shows that the same ownership swap affects the loser's defense distraction too, so the root is
broader but remains one bug.

MODEL / CONTRACT
    InvasionGraph.h defines the fields as temporary distraction of a node's OWN military effort:

        m_rDistractionByConquest  -- defense less effective while this node tries to conquer
                                     cities of a third player
        m_rDistractionByDefense   -- conquest less effective while this node fends off a
                                     third player

    breakCycle/clash parameters likewise represent the portions of the two nodes' respective
    armies committed to the mutual clash:

        rArmyPortion        = this node's own committed Army portion
        rTargetArmyPortion  = target node's own committed Army portion

CURRENT PRODUCER
    clash calls the target as the SimulationStep attacker:

        SimulationStep& kClashStep =
            *kTarget.step(rArmyPortion, rTargetArmyPortion, true);

    If kTarget wins, current code writes:

        kTarget.m_rDistractionByConquest = rArmyPortion;
        m_rDistractionByDefense = rTargetArmyPortion;

    If this node wins, current code writes:

        m_rDistractionByConquest = rTargetArmyPortion;
        kTarget.m_rDistractionByDefense = rArmyPortion;

    In both branches each surviving distraction field receives the OTHER node's committed
    portion rather than its owner's portion.

DOWNSTREAM CONSEQUENCE
    The conquest-distraction field reduces the node's later defensive availability, while
    m_rDistractionByDefense reduces that node's later attacking portion (e.g. the invader
    portion in resolveLosses). Thus asymmetric outside pressure is transferred between the two
    civs whichever side wins the clash; this is not limited to the winner bookkeeping described
    in WIP05.

REPAIR DIRECTION
    Keep the clash result logic, but assign each node's own committed portion to its own
    distraction field. This is an extension of F255/KI#578, not a new root/F-number.


F262 / provisional KI#585 -- REPEAT-TRIP CARGO MULTIPLIER IS APPLIED BEFORE NAVAL TRANSPORT LOSSES
--------------------------------------------------------------------------------------------------
TITLE
    Sunk transports can still contribute to post-battle repeat-trip landing capacity because
    the cargo relay multiplier is applied to the pre-battle transport pool and losses are
    subtracted only afterward.

STATUS
    CONFIRMED inherited UWAI/AdvCiv ordering/accounting bug.
    Present in the first tracked UWAI import (practical 1253), Base AdvCiv 1.14 and current
    AdvCiv-SAS.

MODEL / UNITS
    MilitaryBranch::Logistics::unitPower() returns getCargoSpace(); InvasionGraph.h explicitly
    states that Logistics "power" is cargo capacity. Therefore both rCargoCap and
    rLogisticsLosses below are expressed in the same one-trip cargo-capacity unit.

CURRENT PRODUCER / ORDERING
    Before the naval battle is resolved, current code computes base usable cargo capacity C:

        rCargoCap = 0.73 * (LOGISTICS power - lost LOGISTICS power);

    It then immediately applies the post-battle repeat-trip factor M:

        // Cargo units can go multiple times once the naval battle is won
        rCargoCap *= 1 + 1 / max(2, rReinforcementDist);

    Only after battle casualties have been calculated is destroyed transport capacity L
    subtracted:

        rCargoSize = rCargoCap - rLogisticsLosses;

    Hence the current effective landing capacity is:

        M * C - L

    But the source comment itself says the extra trips happen *once the naval battle is won*.
    Transports destroyed in that battle cannot perform those later relay trips. Physical
    surviving transport capacity should therefore be formed first, then allowed to repeat:

        M * (C - L)

ERROR MAGNITUDE
    Current overstatement is exactly:

        L * (M - 1)

    M can reach 1.5, so as much as half of the cargo capacity destroyed in the naval battle can
    effectively reappear in the subsequent landing calculation.

    Example: C=10, M=1.5, L=4

        current:       10 * 1.5 - 4 = 11
        survivor-first:(10 - 4) * 1.5 = 9

    Two units of effective landing capacity are supplied by transports that were already sunk.

CONSUMER PATH
    rCargoSize -> rLandingRatio = rCargoSize / rArmySize
               -> attacker Army/Cavalry landing power
               -> city invasion odds/result and subsequent losses/conquest state.

    The distortion can therefore change whether the simulated naval invasion succeeds; it is
    not only a reporting discrepancy in "surviving cargo".

ANCESTRY / CONTROL
    The first tracked UWAI implementation already has the same semantic sequence and the same
    explanatory comment: multiply cargo capacity for repeat trips first, then later compute
    cargoSize = cargoCap - logisticsLosses. The 2920 scaled-number rewrite preserves it.
    Base AdvCiv 1.14 and SAS remain equivalent here.

    This is separate from F261/KI#584. F261 concerns the confidence unit leaked into attacker
    Logistics casualty magnitude; F262 remains present even if L itself is calculated perfectly.

REPAIR DIRECTION
    Keep physical one-trip cargo capacity separate until naval losses are known. For example,
    retain a repeat-trip factor and calculate the landing capacity as conceptually:

        rCargoSize = max(0, rCargoCap - rLogisticsLosses);
        rCargoSize *= rRepeatTripFactor;

    or equivalently defer the existing multiplication until after loss subtraction. Do not
    multiply rLogisticsLosses a second time; it already denotes physical one-trip capacity
    destroyed in the battle.


C021-WIP08 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578  (scope extended in WIP08)
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585

NEXT ROOT:
    F263 UNUSED

Resume from F263 unused.

Immediate next work:
    * continue naval/logistics casualty and drowned-Army dimensional accounting;
    * challenge emergency-defender power/loss attribution against its intended virtual-unit
      model before considering any new root;
    * continue SimulationStep branch invariants/result ownership and return-path state;
    * inspect one-sided historical bugfixes for omitted symmetric/adjacent cases, but promote
      only where a live producer -> state -> consumer failure survives;
    * F263 stays unused until another root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C021-WIP09 CONTINUATION -- queue 023 InvasionGraph.cpp
===============================================================================

F263 / provisional KI#586 -- FAILED INVADER IS NOT REMOVED AND CAN ATTACK AGAIN
-------------------------------------------------------------------------------
TITLE
    resolveLosses says a failed attacker receives no further step, but leaves that invader
    in the live targeting set; the same failed force can therefore attack again, while its
    previous threat is simultaneously counted as `rPastThreat` and reduces the defending
    Army assigned to the retry.

STATUS
    CONFIRMED inherited UWAI/AdvCiv control-flow/state defect.
    Present in the first tracked UWAI import (practical 1253), Base AdvCiv 1.14 and current
    AdvCiv-SAS. The relevant comment and loop structure have survived essentially unchanged.

INTENDED CONTRACT -- SOURCE STATES IT EXPLICITLY
    After applying an actual step, resolveLosses says:

        /* If attack fails, the attacker doesn't get another step. However,
           the defending army portion assigned to that attacker mustn't
           be assumed to instantaneously redeploy to another front. */

    It then stores the failed attacker's threat in `rPastThreat` so that the defender
    remains partly tied down for the immediately following invasion resolution.

LIVE CONTROL FLOW
    At the top of every do/while iteration, resolveLosses walks ALL entries still in
    `m_targetedBy` and constructs a new preliminary step for each:

        for (... it = m_targetedBy.begin(); it != m_targetedBy.end(); ++it)
            apSteps[*it] = node[*it]->step(...);

    An ordinary failed city attack does not remove the invader from that set.

    `applyStep` only unlinks the attacker here when its Army is completely eliminated:

        if (attacker Army power <= attacker lost Army power)
            kAttacker.changePrimaryTarget(NULL);

    The generic failure path does not call changePrimaryTarget(NULL), does not clear a
    local active flag and does not otherwise exclude `eNextInvader` from later iterations.
    The target city also remains unconquered, so `targetCity()` can simply return it again.

PRODUCER -> STATE -> CONSUMER FAILURE
    Suppose attacker A assaults defender D and the actual step fails without eliminating A.

    1. `applyStep(kNextStep)` records the casualties but leaves A -> D live.

    2. Because the step failed:

           rPastThreat = rNextInvaderThreat;

       Let that prior threat be T_A.

    3. The do/while loop repeats. A is still in D.m_targetedBy, so A receives another
       preliminary step and contributes a fresh threat T'_A to `rSumOfThreats`.

    4. A can be selected as `eNextInvader` again and the actual defender-Army portion is:

           T'_A / (rSumOfThreats + rPastThreat + m_rDistractionByConquest)

       For the simplest one-invader case this is approximately:

           T'_A / (T'_A + T_A)

       rather than 1. The same failed attacker is therefore not merely retried; its OLD
       threat is treated as if it belonged to another front and diverts defending Army
       away from its OWN retry.

MULTI-INVADER CONSEQUENCE
    The same double representation occurs with several attackers: after A fails, A remains
    a live contributor to the new `rSumOfThreats` while T_A is also carried separately as
    `rPastThreat`. This contradicts the comment's model, where A is supposed to receive no
    second step and only its already-committed defensive distraction should carry over for
    the next OTHER invasion.

DOWNSTREAM EFFECT
    A first failed assault can therefore be followed by another simulated assault by the
    same force within the same horizon. Because the retry can receive a smaller defender
    Army portion, a force that was repelled once can become more likely to conquer the same
    city on its retry. Repeated failures also consume `aiTurnsSimulated[A]` until the time
    horizon finally stops them; ordinary failure itself does not.

ANCESTRY
    Practical 1253 already contains all three necessary pieces together:
        * the do/while reconstruction of steps from the unchanged `targetedBy` set;
        * the explicit comment that a failed attacker "doesn't get another step";
        * `pastThreat = nextInvaderThreat` with no removal/exclusion of that invader.

    Later refactors, including the revised-UWAI/scaled-number work, preserve the behavior.
    Base AdvCiv 1.14 and current SAS are equivalent in this block.

DISTINCTION FROM OTHER QUEUE-023 ROOTS
    * F251 concerns an ELIMINATED/CAPITULATED node retaining its outgoing edge and later
      attacking a different downstream target during recursive graph resolution.
    * F263 concerns a still-alive attacker whose ordinary FAILED city assault is explicitly
      supposed to end its attacks for this loss-resolution pass but does not.
    * F254 concerns time-horizon pruning. F263 remains live with a generous horizon.

REPAIR DIRECTION
    Enforce the existing stated contract by excluding the failed invader from subsequent
    step generation in this resolveLosses pass while retaining its threat solely as the
    carry-over distraction for the next other attacker. This can be done with explicit
    local active/finished state; alternatively unlinking the failed attacker's target may
    fit the intended graph semantics, but should be reviewed against phase-II retargeting.

    Crucially, do not leave the same invader simultaneously represented both in the live
    threat sum and in `rPastThreat`.


CONTROL / NON-PROMOTION: EMERGENCY HOME-GUARD LOSS ATTRIBUTION
--------------------------------------------------------------
The city-defense formula combines baseline HOME_GUARD power, emergency defender power and
recorded Home-Guard losses, and a successful city defense loss is then stored through the
HOME_GUARD branch while emergency power is also reduced. This can look dimensionally odd,
particularly once emergency power becomes large. However, the original UWAI implementation
contains the same virtual-defender model and the adjacent comment indicates intentional
coarse handling of emergency units that could/could not reach the city. No clean invariant
was found that uniquely determines how casualties should be split between baseline and
virtual emergency defenders. This remains a modeling question, not F264.


C021-WIP09 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F263 / provisional KI#586

NEXT ROOT:
    F264 UNUSED

Resume from F264 unused.

Immediate next work:
    * continue resolveLosses scheduling/failed-step state after F263, including threat
      carry-over and multi-invader ordering without duplicating F254;
    * continue naval drowned-Army / Logistics dimensional checks after F261/F262;
    * continue history-driven checks around one-sided UWAI fixes and SimulationStep state;
    * retain emergency-defender loss attribution as a non-promoted modeling question unless
      a stronger invariant/consumer proves a concrete defect;
    * F264 stays unused until another independent root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C021-WIP10 CONTINUATION -- AUTHORITATIVE F263 RETRACTION + queue-023 controls
===============================================================================

IMPORTANT CORRECTION -- F263 / provisional KI#586 RETRACTED
------------------------------------------------------------
DO NOT IMPLEMENT KI#586 FROM C021-WIP09.

The WIP09 diagnosis that an ordinary failed city attacker remains linked and can
receive another step was a false positive caused by reading the Army-elimination
cleanup at the end of Node::applyStep without carrying the immediately preceding
success/failure branch through to its `else`.

Current Node::applyStep is explicit:

    if (kStep.isAttackerSuccessful())
    {
        ...
    }
    // Remove link from graph
    else kAttacker.changePrimaryTarget(NULL);

That `else` belongs to `if (kStep.isAttackerSuccessful())`, not to either of the
later Army-elimination tests. Therefore EVERY applied unsuccessful step -- an
ordinary failed city attack as well as an unsuccessful clash -- immediately
unlinks the attacker from its current primary target. changePrimaryTarget(NULL)
removes the attacker's player ID from the defender's `m_targetedBy` set.

Consequently, the WIP09 producer -> state -> consumer chain does not exist:

* after A's ordinary failed attack, A -> D is removed;
* the next resolveLosses iteration does NOT construct another preliminary A step;
* A does NOT contribute a fresh threat to `rSumOfThreats` while its old threat is
  carried as `rPastThreat`;
* the source comment "If attack fails, the attacker doesn't get another step" is
  in fact implemented by applyStep.

`rPastThreat` therefore has the intended role: for one following resolution it
keeps part of D's Army tied to the just-failed attack even though that attacker
has been removed from the live incoming-edge set.

The first tracked UWAI implementation also contains this generic failure unlink;
the supposed inherited defect was an audit-reading error, not a source defect.

Preserve C021-WIP09's F263 section only as rejected-history evidence so that the
same hypothesis is not rediscovered. F263 is no longer a live queue-023 root.
The provisional KI#586 number remains historical/retracted; later KI numbering
is NOT renumbered.

ADVERSARIAL IMPACT CHECK
------------------------
F250-F262 do not depend on F263 and remain live. In particular:
* F251 is about a successfully eliminated/capitulated defender retaining its OWN
  outgoing edge; it does not rely on ordinary attack failure.
* F254 is about horizon pruning before an actual step is applied.
* F261/F262 are local naval Logistics arithmetic roots.


CONTROL / NON-PROMOTION -- findBestTarget/targetCity INVALID-SUFFIX BREAK IS SAFE
---------------------------------------------------------------------------------
Both findBestTarget and targetCity stop when they encounter an unreachable or
own-team city because the cache is sorted by attackPriority. UWAICache proves the
required ordering invariant:

    unreachable city -> attackPriority() == -1
    own-team city     -> attackPriority() == -2
    reachable rival   -> attackPriority() >= 0

sortCitiesByAttackPriority is descending selection sort (with only a positive
rival's cross-area priority diluted by 1/2, which remains nonnegative). Thus a
negative-priority own/unreachable city cannot precede a reachable rival city.
The `break` is valid; no F264 here.


CONTROL / NON-PROMOTION -- NAVAL LOGISTICS LIMIT * AREA AVAILABILITY
--------------------------------------------------------------------
A tempting arithmetic objection is that a naval landing can first be reduced by
cargo support and later again by `rAreaWeightAtt`, so the two factors multiply.
History makes that insufficient for a bug finding. Practical 1289 deliberately
introduced the battle-area projection AFTER the existing Logistics-supported
landing calculation and explicitly described two separate constraints:

* the naval attack can focus "as many as are supported by LOGISTICS";
* independently, the AI "struggles badly with deploying units from different
  areas."

Practical 1371 retained that separation while changing the human/AI weighting.
Multiplying two explicitly distinct availability approximations may be crude, but
is compatible with the stated model. Without a stronger invariant or concrete
state/unit mismatch, do not promote this as F264.


C021-WIP10 CURRENT CURSOR
-------------------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585

RETRACTED QUEUE-023 ROOT:
    F263 / provisional KI#586 -- audit false positive; ordinary failure DOES unlink attacker

NEXT ROOT:
    F264 UNUSED (next provisional KI would be KI#587; do not reuse KI#586)

Resume from F264 unused.

Immediate next work:
    * continue naval drowned-Army / Logistics dimensional checks after F261/F262;
    * continue SimulationStep/result ownership and branch-subset invariants;
    * inspect one-sided historical fixes for omitted sibling paths;
    * keep emergency-defender loss attribution and sequential naval/area weighting as
      non-promoted modeling questions unless a harder invariant proves a defect;
    * F264 stays unused until another independent live root survives challenge.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================


C021-WIP11 CONTINUATION -- queue 023 InvasionGraph.cpp
=======================================================

F264 / provisional KI#587 -- COASTAL DEFENDER FLEET LOSSES DO NOT REDUCE ITS OVERLAPPING LOGISTICS CAPACITY
------------------------------------------------------------------------------------------------------------
TITLE
    A defender can lose transport ships in the coastal-bombardment sea battle, but that
    step records only FLEET loss and deliberately records zero LOGISTICS loss. Because
    LOGISTICS is persistent graph state and overlaps FLEET for proper transports, the same
    defender can later launch a naval invasion with cargo capacity belonging to transports
    that the preceding simulation step has already destroyed as Fleet power.

STATUS
    CONFIRMED inherited UWAI/AdvCiv overlapping-branch state omission.
    Present from the initial tracked InvasionGraph import, practical 1253
    (f6ff6a182f...), and still present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    This is separate from the explicitly acknowledged cargo-capacity-vs-combat-power
    approximation and from F261's confidence conversion error.

BRANCH MODEL / HARD OVERLAP
    MilitaryBranch.h defines FLEET as any sea ship with combat strength, including
    transports, and LOGISTICS as ships transporting Army units. Therefore a proper
    combat-capable transport belongs to BOTH branches:

        FLEET       -> combat-power representation
        LOGISTICS   -> cargo-capacity representation

    MilitaryBranch::Fleet::unitPower uses the ship's military power, whereas
    MilitaryBranch::Logistics::unitPower returns getCargoSpace(). The units differ, but
    both branches describe the same physical transport hulls.

PRODUCER -- COASTAL BATTLE
    In Node::step, a land-reachable city attack can still trigger a coastal sea battle
    when the attacker can bombard from sea. The source explicitly says:

        "Only defender is assumed to bring cargo units into battle."

    The attacker removes its Logistics component from the Fleet estimate (approximately),
    whereas the defender uses its entire remaining FLEET power, therefore including
    combat-capable transports.

    The calculated sea-battle casualties are then recorded only as:

        kStep.reducePower(attacker, FLEET, rLossesAtt);
        kStep.reducePower(defender, FLEET, rLossesDef);

    No LOGISTICS loss is recorded. The nearby rationale is:

        "Losses in logistics don't matter b/c no naval landing is attempted,
         and the cost (of having to rebuild units) is already included in the fleet losses."

    That rationale is local to the present step and does not hold for the persistent
    graph state consumed by later steps.

CONCRETE HARD CASE
    Let defender D's naval force consist entirely of proper combat-capable transports
    (e.g. the model-equivalent of Galleons/Transports), so every ship contributes to both
    FLEET and LOGISTICS.

    A coastal battle can record positive FLEET loss for D. With no non-transport combat
    ship available to absorb that loss, the model has necessarily destroyed some of the
    physical transports represented by LOGISTICS as well. Yet D's m_arLostPower[LOGISTICS]
    remains unchanged, so all original cargo capacity survives in simulation state.

PRODUCER -> STATE -> CONSUMER PATH
    A simple acyclic component is enough:

        A -> D -> T

    where A attacks a land-reachable coastal city of D and D's own primary target T
    requires a naval landing.

    resolveLossesRec is post-order over incoming edges:

        T.resolveLossesRec()
            -> D.resolveLossesRec()
                -> A.resolveLossesRec()
                -> D.resolveLosses()      [A attacks D first]
            -> T.resolveLosses()          [D attacks T afterward]

    Thus A's coastal attack on D is applied BEFORE D's later outgoing attack on T.
    D's FLEET losses persist in m_arLostPower[FLEET], but the omitted Logistics losses do
    not. When D later performs a naval step, its landing capacity is recomputed from:

        D.m_military[LOGISTICS]->power() - D.m_arLostPower[LOGISTICS]

    and therefore still includes the cargo capacity of transports already represented as
    destroyed by the earlier FLEET loss.

WHY THE EXISTING COMMENT DOES NOT MAKE THIS SAFE
    It is reasonable not to care about cargo capacity for the *current* land attack, and
    the FLEET loss does already charge rebuilding cost. But SimulationStep losses are also
    availability state for later graph steps. Cost accounting cannot make a destroyed
    transport available for a subsequent landing. This is the same reason overlapping
    CAVALRY/ARMY branches are updated together elsewhere: overlapping branches must remain
    mutually consistent when the same physical unit is lost.

    The pre-existing FIXME about LOGISTICS using cargo slots while FLEET uses corrected
    combat power makes the exact proportional conversion approximate; it does not justify
    an exact zero LOGISTICS update in the all-transport hard case.

HISTORY
    Practical 1253 introduces the coastal battle with both comments above and with FLEET-
    only losses. The behavior survives later cleanup/scaled rewrites (including 2920/2954)
    and remains in Base AdvCiv 1.14 and SAS. No K-Mod/Civ4CE cognate exists because UWAI's
    InvasionGraph is not present there.

REPAIR DIRECTION
    When the coastal defender's Fleet loss can include proper transports, propagate an
    estimated overlapping loss into LOGISTICS as well. Reusing a defender-side cargo/Fleet
    ratio analogous to the full naval-battle path is the smallest structural repair, while
    retaining the existing documented dimensional approximation. Clamp the resulting
    Logistics loss to remaining cargo capacity.

    A more exact long-term repair would track transport combat power and cargo capacity
    separately so the same physical transport casualties can update both branch measures
    without dividing unlike units.

INTERACTIONS / NON-DUPLICATION
    * F261: confidence leaks into attacker Logistics casualty conversion in the full naval
      battle. F264 exists even at confidence 1 and concerns the coastal-battle path where
      defender Logistics casualties are omitted entirely.
    * F262: repeat-trip capacity is applied before transport losses during an actual naval
      landing. F264 concerns stale Logistics state created by an earlier coastal battle.
    * The longstanding "cargo capacity vs Fleet power" FIXME is an approximation/control;
      F264 is the independently provable persistence failure when destroyed overlapping
      transports remain fully available in a later step.


C021-WIP11 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F265 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#588.)

Resume from F265 unused.

Best next work:
    * continue overlapping-branch loss invariants after naval/coastal combat, especially
      drowned Army/Cavalry and remaining Fleet/Logistics state;
    * continue city-combat loss conversion and emergency-defender state beyond F253;
    * inspect SimulationStep query-vs-apply ownership/mutation and cumulative branch caps;
    * continue historical one-sided bugfix audit (especially 1335/1358/1387/1395/1726/
      1879/2767 and post-2920 cleanup commits) for surviving sibling omissions;
    * keep explicit modeling approximations separate from hard owner/state/arithmetic
      contradictions.

No repository source files modified. Audit album only.


C021-WIP12 CONTINUATION -- queue 023 InvasionGraph.cpp
=======================================================

F265 / provisional KI#588 -- DEPLOYMENT FILTER SCALES ARMY BUT NOT ITS OVERLAPPING CAVALRY SUB-BRANCH
-----------------------------------------------------------------------------------------------------
TITLE
    Deployment-distance attenuation reduces an attacker's enclosing ARMY power but leaves
    its overlapping CAVALRY power unchanged. This can immediately violate Cavalry<=Army
    and later make a city-combat step record more Cavalry casualty power than total Army
    casualty power.

STATUS
    CONFIRMED inherited UWAI/AdvCiv overlapping-branch projection omission.
    Present in the initial tracked InvasionGraph import (practical 1253, f6ff6a182f...)
    and still present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    Independent from F260, which concerns the later attacker battle-area filter.

BRANCH INVARIANT / SOURCE MODEL
    MilitaryBranch.h explicitly states:

        "All Cavalry is also included in Army."

    Node::step itself enforces the resulting subset invariant before deployment:

        rCavPow.decreaseTo(rArmyPow);

    Thus CAVALRY is not an independent pool that may exceed ARMY; it is an overlapping
    description of the fast/mounted subset of that same Army force.

PRODUCER -- DEPLOYMENT ATTENUATION
    After naval handling and immediately before battle-area weighting, current code applies
    deployment-distance availability as:

        rDefArmyPow *= rDefDeploymentMod;
        rArmyPow *= scaled::max(
                100 - 1.55 * rDeploymentDistAttacker.pow(1.15), 50) / 100;

    Neither rCavPow nor rDefCavPow is changed in this block.

    The attenuation is not merely a combat-strength modifier. The source comment says the
    attacking reduction models produced units becoming available with a delay and the AI
    forming smaller stacks / units arriving in a trickle. In other words it is an
    availability projection of the represented force. Leaving its overlapping Cavalry
    subset at pre-projection scale breaks the branch representation.

CONCRETE HARD CASE
    Take an Army consisting entirely of units that qualify for CAVALRY:

        before deployment filter: Army = 100, Cavalry = 100

    A long deployment can hit the explicit 50% lower bound for Army availability:

        after current filter: Army = 50, Cavalry = 100

    The simulation now claims that the available subset contains twice as much Cavalry
    power as total Army power even though every Cavalry unit is included in Army.

DOWNSTREAM CONSUMER -- CITY LOSSES
    The open-field clash path computes/clamps a Cavalry ratio, which masks some of the
    damage there. City combat later does not clamp the ratio and records:

        kStep.reducePower(attacker, ARMY, rLossesAttArmy);
        kStep.reducePower(attacker, CAVALRY,
                rLossesAttArmy * rCavPow / rArmyPow);

    Under the pure-Cavalry example above, 10 Army power of losses becomes 20 Cavalry power
    of losses. The same issue exists on both successful and failed city-assault branches.
    applyStep eventually caps cumulative branches independently at their original totals,
    but that does not restore per-step overlap consistency and can exhaust the Cavalry
    branch too early for later simulated combat.

WHY "CAVALRY DEPLOYS FASTER" DOES NOT SAVE THE CURRENT ARITHMETIC
    The model does recognize a cavalry-focused attack separately: bCavalryAttack shortens
    deployment duration and reduces attacker deployment distance. That can reasonably make
    a cavalry-heavy force suffer a smaller availability penalty. But leaving CAVALRY wholly
    unfiltered while reducing its enclosing ARMY uniformly cannot represent faster Cavalry
    correctly: in the pure-Cavalry limit, reducing Army necessarily reduces the same
    physical units represented by Cavalry. A different mobility factor would still have to
    preserve Cavalry<=Army after projection.

HISTORY
    The first tracked UWAI InvasionGraph import already had:

        targetArmyPow *= defDeploymentMod;
        armyPow *= max(... deploymentDistAttacker ...);

    while Cavalry was already an overlapping Army branch and city/clash losses were already
    derived from cavPow / armyPow. No corresponding Cavalry deployment projection appears
    in that initial implementation. Later cavalry-specific deployment tweaks (e.g.
    practical 1341's shorter distance/duration for a cavalry attack) change the distance
    input but do not repair the branch mismatch. The scaled rewrite and current AdvCiv/SAS
    preserve the omission.

REPAIR DIRECTION
    The hard live consumer is the ATTACKER path: project rCavPow whenever the attacker's
    enclosing rArmyPow receives the deployment-availability factor. For example:

        scaled rAttDeploymentMod = ...;
        rArmyPow *= rAttDeploymentMod;
        rCavPow  *= rAttDeploymentMod;

    and defensively re-establish `rCavPow.decreaseTo(rArmyPow)` before any unbounded city-
    loss ratio is consumed.

    Be more careful on the defender side. Current city-attack flow later deliberately
    divides rDefArmyPow by rDefDeploymentMod when the defending army is treated as having
    retreated to the city (deployment distance 0). Therefore blindly multiplying
    rDefCavPow by rDefDeploymentMod here without undoing the same factor later would
    CREATE a new mismatch. If defender Cavalry is also projected for clash consistency,
    mirror every later defender-ARMY undo on CAVALRY as well.

    If a deliberate mobility advantage for Cavalry is desired, model it with a distinct
    bounded factor while preserving Cavalry<=Army in the represented force. Simply
    clamping the final casualty ratio would hide one symptom while leaving attacker
    availability inconsistent.

INTERACTIONS / NON-DUPLICATION
    * F260 is the later attacker battle-area omission. It has a strong one-sided historical
      control: practical 1366 repaired the defender sibling but not attacker Cavalry.
      F265 is an independent earlier deployment-distance transformation already wrong in
      the initial UWAI import.
    * F259 concerns a negative defender deployment distance caused by personality scaling.
      F265 remains at entirely valid non-negative distances and even at the ordinary 50%
      deployment lower bound.


CONTROL / REJECTION -- ARMY PORTION NEED NOT SCALE LOGISTICS CAPACITY
---------------------------------------------------------------------
    A candidate F265 was initially considered because rArmyPortionAttacker/Defender is
    applied to ARMY, CAVALRY and FLEET, while LOGISTICS cargo capacity is not multiplied by
    that portion. Historical/source semantics do not establish this as a bug. The portion
    represents combat forces committed to the current front; a smaller committed Army can
    plausibly concentrate all available transports on carrying that selected subset.
    The initial UWAI implementation already describes cargo capacity separately from the
    Army/Fleet commitment fraction. Without a hard reuse/ownership contradiction, matching
    the Fleet multiplier mechanically would be an unjustified symmetry assumption.
    No F-number assigned for this candidate.


C021-WIP12 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F266 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#589.)

Resume from F266 unused.

Best next work:
    * continue drowned-Army/Cavalry and Fleet/Logistics overlap conversions after naval
      combat, challenging exact unit dimensions and confidence/portion interactions;
    * continue city-combat and emergency-defender state beyond F253, especially overlap
      ratios after deployment/area filtering and cumulative branch caps;
    * inspect SimulationStep ownership/query-vs-apply semantics and any player-ID fallback
      that can silently charge a third player as defender;
    * continue historical one-sided bugfix audit for surviving siblings rather than
      assuming apparent asymmetry is erroneous;
    * keep coarse UWAI modeling choices separate from hard owner/state/unit contradictions.

No repository source files modified. Audit album only.


C021-WIP13 CONTINUATION -- queue 023 InvasionGraph.cpp
=======================================================

F266 / provisional KI#589 -- HOME_GUARD CAP DISCARDS CASUALTIES INFLICTED ON VIRTUAL EMERGENCY DEFENDERS
--------------------------------------------------------------------------------------------------------
TITLE
    City defense can include positive m_rEmergencyDefPow beyond the original HOME_GUARD
    branch, and step() computes HOME_GUARD casualties from that emergency-augmented pool.
    applyStep then caps cumulative HOME_GUARD loss to the ORIGINAL branch power only.
    Once that base cap is reached, casualties inflicted on emergency defenders can vanish
    from persistent availability state and the same virtual defenders can fight a later
    invader again at full strength.

STATUS
    CONFIRMED inherited UWAI/AdvCiv state-accounting defect.
    Present from the initial tracked InvasionGraph import (practical 1253,
    f6ff6a182f...) and still present in Base AdvCiv 1.14 and current AdvCiv-SAS.
    This supersedes the earlier WIP09 CONTROL / NON-PROMOTION on emergency Home-Guard loss
    attribution: the exact split between baseline and emergency casualties remains a
    modeling choice, but the generic base-branch cap supplies a new hard loss-discard
    path that the earlier control had not established.

PRODUCER -- EMERGENCY DEFENDERS ARE REAL AVAILABILITY IN THE NEXT STEP
    After a successful city loss, applyStep creates an auxiliary defender pool:

        m_rEmergencyDefPow += typicalHomeGuardPower * duration / (2 or 3);

    The stated rationale is that these are defenders trained while the invader heals and
    approaches its next target and therefore not already covered by ordinary forecasting.

    A later city-defense calculation explicitly includes them in available garrison power:

        rPowerPerGarrison = 0.5 *
            (HOME_GUARD.power() + m_rEmergencyDefPow
             - m_arLostPower[HOME_GUARD]) / remainingCities;

    Thus emergency power is not merely a reporting bonus; it supplies units/power that can
    absorb casualties in the simulated defense.

LOSS PRODUCER
    Both successful and failed city assaults can store HOME_GUARD loss based on
    rGuardPowUnmodified, which was computed from the emergency-augmented garrison pool.
    For example the failed-assault path records:

        reducePower(defender, HOME_GUARD,
            rGuardPowUnmodified * 0.75 * rThreat);

    No separate field in SimulationStep says which part of that casualty came from the
    baseline guard versus the emergency defenders.

STATE CONSUMER / HARD CAP
    applyStep handles every branch generically:

        m_arLostPower[eBranch] += rLostPowerDef;
        m_arLostPower[eBranch].decreaseTo(m_military[eBranch]->power());

    For HOME_GUARD, m_military[HOME_GUARD]->power() is the original/base branch and does
    NOT include m_rEmergencyDefPow. The auxiliary emergency pool itself is not reduced on
    an ordinary failed city attack.

CONCRETE NO-AMBIGUITY CASE
    Suppose D has:

        base HOME_GUARD power = 10
        already recorded HOME_GUARD loss = 10
        emergency defender power = 20

    D can still defend a city with positive garrison power because the availability
    formula contains `10 + 20 - 10 = 20` before distribution.

    A later failed assault computes, say, +8 HOME_GUARD casualties from those defenders.
    applyStep performs:

        lostHG = 10 + 8 = 18
        lostHG.decreaseTo(baseHG = 10)  -> 10

    Therefore the entire +8 loss disappears. m_rEmergencyDefPow remains 20, so the next
    city-defense calculation again sees all 20 emergency power. With the base branch
    already exhausted, there is no plausible baseline unit to which the discarded loss
    can be reassigned; it necessarily came from power that the model nevertheless leaves
    available.

SAME-PHASE PRODUCER -> STATE -> LATER CONSUMER
    Multiple invaders targeting one defender are resolved sequentially inside the same
    Node::resolveLosses() loop, so no phase reset is needed:

        A -> D
        B -> D
        C -> D

    One valid sequence is:
        1. A successfully takes a city from D; applyStep adds emergency defender power.
        2. B later assaults D and inflicts HOME_GUARD casualties against the augmented
           pool. If the base cap is reached, some/all of B's new casualty is discarded.
        3. B's failed attack is unlinked, while C remains in D.m_targetedBy.
        4. C's later step recomputes garrison power using the unchanged emergency pool and
           therefore fights defenders that B's applied step had already modeled as lost.

    This is a persistent same-phase availability error, distinct from the separate
    question of whether emergency defenders should survive across prepareForSimulation.

RELATION TO F253
    F253 is the query-side mutation `m_rEmergencyDefPow /= 2` performed inside speculative
    step() calls for predicted successful attacks. F266 exists independently on an
    ordinary FAILED assault, where that halving does not execute. Fixing F253 by moving
    success-only emergency mutation into applyStep therefore does not repair F266.

HISTORY
    Practical 1253 introduced both mechanisms together:
        * emergencyDefPow is reset/accumulated separately and added to HOME_GUARD defense;
        * applyStep caps every lostPower branch to `military[branch]->power()`.

    The initial city-loss code already derives HOME_GUARD casualties from the augmented
    garrison. The later revised-UWAI/scaled refactors preserve both the auxiliary pool and
    the original-branch cap. Base AdvCiv 1.14 and SAS retain the same structural mismatch.

REPAIR DIRECTION
    Track consumption of emergency defender power explicitly when HOME_GUARD casualties
    exceed the remaining baseline Home Guard represented by the branch. One conservative
    approach is to split the applied casualty into:

        remainingBaseHG = max(0, baseHG - lostHG);
        lossFromBase    = min(stepHGLoss, remainingBaseHG);
        lossFromEmergency = stepHGLoss - lossFromBase;

    then add lossFromBase to m_arLostPower[HOME_GUARD] and subtract/clamp
    lossFromEmergency from m_rEmergencyDefPow. The exact proportional split could be made
    more sophisticated, but it must preserve total defender availability.

    Avoid merely increasing the generic m_arLostPower[HOME_GUARD] cap to
    `base + emergency` without reviewing MilitaryAnalyst::getLostPower / war-utility
    reporting: emergency defenders are simulated extra production, so counting their
    deaths as losses of the original baseline branch may distort cost accounting even if
    it fixes availability.


C021-WIP13 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F267 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#590.)

Resume from F267 unused.

Best next work:
    * continue emergency-defender accounting after SUCCESSFUL assaults, keeping F253's
      query mutation separate from F266's loss-cap discard;
    * inspect remaining Army/Cavalry overlap ratios after temporary-loss and city-loss
      conversions for hard subset violations not already covered by F260/F265;
    * inspect SimulationStep's two-party storage contract and all caller ownership paths;
    * continue historical one-sided bugfix audit and remaining naval/city arithmetic;
    * reject coarse modeling choices unless a producer/state/consumer contradiction exists.

No repository source files modified. Audit album only.


C021-WIP14 CONTINUATION -- queue 023 InvasionGraph.cpp
=======================================================

F267 / provisional KI#590 -- PARTIAL CITY LOSS DOES NOT STOP THE DEFENDER'S THIRD-PARTY OFFENSIVE DESPITE EXPLICIT MODEL INVARIANT
------------------------------------------------------------------------------------------------------------------------------
TITLE
    After a node loses a city to a third civilization, applyStep explicitly says that
    its own offensive stops and its whole Army becomes available for defense. The code
    only clears m_rDistractionByConquest; it does not remove the node's outgoing edge.
    In ordinary A -> B -> C post-order resolution, a surviving B can therefore first
    use its Army to defend against A and then still execute B -> C over a fresh local
    invasion horizon in the same simulation phase.

STATUS
    CONFIRMED inherited UWAI/AdvCiv graph-state defect.
    Present from the initial tracked InvasionGraph import (practical 1253,
    f6ff6a182f...) and still present in Base AdvCiv 1.14 and current AdvCiv-SAS.

    This is independent of F251. F251 requires B to become terminal
    (eliminated/capitulated) while retaining its outgoing edge. F267 needs only one
    successful city loss: B can remain alive, non-capitulated and retain substantial
    Army power.

SOURCE-STATED INVARIANT
    On a successful city attack, the defender executes:

        addCityLoss(kStepCity);
        ...
        /* Assume that own offensive stops upon losing a city to a third civ,
           and whole army becomes available for defense. ... */
        m_rDistractionByConquest = 0;

    The header defines m_rDistractionByConquest as temporary defensive distraction
    while trying to conquer a third player. Setting it to zero therefore does implement
    the second half of the comment: all surviving mobile Army may be assigned to
    defense against the invader.

    It does NOT implement the first half: m_pPrimaryTarget and the corresponding reverse
    m_targetedBy entry are untouched.

LIVE GRAPH ORDER
    Use an ordinary acyclic component:

        A -> B -> C

    C is the sink. resolveLossesRec processes incoming nodes recursively before the
    current node:

        C.resolveLossesRec()
            B.resolveLossesRec()
                A.resolveLossesRec()
                B.resolveLosses()      // A attacks B
            C.resolveLosses()          // B attacks C

    Suppose A conquers one B city successfully, but B still has other cities and
    surviving Army power.

    During B.resolveLosses(), B:
        * records the city loss;
        * remains non-eliminated/non-capitulated;
        * sets m_rDistractionByConquest = 0, making its Army fully available for defense;
        * does NOT call B.changePrimaryTarget(NULL).

    The later Army-elimination cleanup does not fire unless B's Army branch has actually
    been exhausted.

OUTGOING OFFENSIVE THEREFORE REMAINS LIVE
    B -> C is still represented by both:
        B.m_pPrimaryTarget == &C
        C.m_targetedBy contains B

    Control returns to C.resolveLossesRec(), which invokes C.resolveLosses(). B remains
    an incoming invader and B.step(...) constructs its ordinary attack against C.

    No eliminated-node loophole, stale city cache or phase retarget is required.

TIME / DOUBLE-COMMIT CONSEQUENCE
    The implementation has a field intended to carry an attacker's already simulated
    war time:

        m_iWarTurnsSimulated

    Successful city attacks add their duration to that field, but resolveLosses' time
    test still has the corresponding use commented out:

        // + m_iWarTurnsSimulated

    and the header still says that the field is currently not used.

    Each defender's resolveLosses() creates its own fresh aiTurnsSimulated vector.
    Consequently, time B spent being attacked/defending in B.resolveLosses is not charged
    against B when B is later treated as the invader in C.resolveLosses.

    In the A -> B -> C sequence, B can therefore receive the full defensive availability
    intended by the source comment against A and then still receive a fresh local horizon
    for its surviving Army to attack C. This is not merely a stale label: it changes
    subsequent losses, possible conquests and capitulations.

WHY THIS IS STRONGER THAN A MODELING-PREFERENCE CLAIM
    A model could reasonably decide that B survives A's attack and later resumes an
    offensive against C. That, however, is not the policy encoded/documented here:

        "own offensive stops upon losing a city to a third civ"

    The code then explicitly performs the paired state transition that frees the whole
    Army for defense. There is no state or delay condition that represents a later
    resumption. Leaving B -> C live therefore contradicts the declared transition and
    also grants the outgoing attack a separate unused horizon.

ANCESTRY
    Practical 1253, the initial tracked InvasionGraph import, already contains the exact
    comment and only:

        distractionByConquest = 0;

    It does not unlink the defender's own primary target. The per-node time accounting is
    already marked "currently not used" in that first implementation, and the optional
    `warTimeSimulated` contribution to the time-limit expression is commented out there
    as well.

    The 2920 revised-UWAI rewrite and later formatting/refactor commits preserve the same
    behavior. Base AdvCiv 1.14 and current SAS remain equivalent here.

DISTINCTION FROM F251
    F251:
        terminal node (eliminated/capitulated) remains an attacker.

    F267:
        surviving node loses only part of its empire to a third-party invasion; the
        source explicitly says its offense stops, but its outgoing edge remains active.

    Repairing F251 by enforcing `terminal => no outgoing edge` does not repair F267.

REPAIR DIRECTION
    On a successful city loss to a THIRD PARTY, implement the stated offensive-stop
    transition by removing the defender's outgoing edge when it points elsewhere:

        if (m_pPrimaryTarget != NULL &&
            m_pPrimaryTarget->m_ePlayer != kAttacker.m_ePlayer)
        {
            changePrimaryTarget(NULL);
        }

    The exact condition should be reviewed around mutual-target/cycle cases; the source
    specifically says "third civ", so blindly cancelling a defender's edge against the
    very attacker that just took its city may change intended two-party-war behavior.

    Alternatively, if the intended modern model is actually "pause, defend, then resume",
    the comment/state should be rewritten and elapsed-time accounting must prevent a
    fresh full-horizon offensive. Merely leaving the current edge live while also claiming
    the whole Army for defense produces the unsupported hybrid behavior.

NEGATIVE CONTROLS / SCOPE
    * A simple ordinary failed attack is already handled by the attacker's failure unlink
      (the reason F263 was retracted); F267 is success-on-the-defensive-side state.
    * Full city elimination/capitulation belongs to F251; F267 reproduces with B alive.
    * If B's Army is also entirely destroyed, existing Army-elimination cleanup masks
      F267 by removing B's outgoing edge.
    * This does not assert that every real civilization historically stops all operations
      after one city loss. It is a consistency defect against the simulation policy that
      InvasionGraph itself states and partially applies.


ADDITIONAL CONTROL / F257 ADDENDUM
----------------------------------
During the continued clash audit, the confidence-scaled clash loss is also subtracted from
local Army/Cavalry availability before the permanent conversion documented by F257.
That initially looked like a second downstream consequence. In current code, however,
that local subtraction occurs on the clash-only path, and the clash path returns without
using the modified local values for a subsequent city assault. The durable F257 effect
therefore remains the stored permanent/temporary loss conversion; no separate root is
created from the local subtraction.


C021-WIP14 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589
    F267 / provisional KI#590

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F268 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#591.)

Resume from F268 unused.

Best next work:
    * continue surviving-node edge/distraction semantics after successful defensive city
      loss, especially m_rDistractionByDefense and m_bHasClashed interactions;
    * continue Army/Cavalry/Home-Guard overlap invariants against practical 1335/1366
      and later one-sided bugfixes;
    * inspect remaining SimulationStep result ownership and branch-loss conversions;
    * continue capitulation/result propagation and dormant teammate recursion only if a
      live multi-member-team caller can be proved;
    * keep historical/intentional horizon approximations distinct from hard state bugs.

No repository source files modified. Audit album only.


===============================================================================
CHECKPOINT C021-WIP15 -- FILE 023 OPEN / InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol
Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T134631.zip
Prior physical checkpoint:
    C021-WIP14
New finding in this append:
    F268 / provisional KI#591
Next finding number:
    F269 UNUSED

F268 / provisional KI#591 -- LAND REACHABILITY BOOLEAN IS COMBINED WITH A MIXED LAND/SEA DISTANCE AND CAN TURN SEA TRAVEL TIME INTO LAND-INVASION TIME
----------------------------------------------------------------------------------------------------------------------------------------
TITLE
    UWAICache::City deliberately records land-route existence separately from one
    numeric deployment distance chosen from the fastest available land/sea route.
    InvasionGraph::Node::canReachByLand combines the land-existence boolean with that
    mixed numeric distance as though it were a land-only distance. A target with a
    valid but long land route and a much shorter sea route can therefore be classified
    as a land invasion while using the sea trip's duration and bypassing the Fleet /
    Logistics battle model required to achieve that duration.

STATUS
    CONFIRMED inherited UWAI/AdvCiv representation/consumer mismatch.
    Present from the initial tracked UWAI cache/InvasionGraph implementation and still
    present in Base AdvCiv 1.14 and current AdvCiv-SAS.

    This is distinct from F226/KI#549 and F240/KI#563:
      * F226 concerns whether a valid sea route establishes reachability at all when a
        shorter NON-QUALIFYING local land route exists. SAS already carries KI#549.
      * F240 concerns a valid fast land route being excluded by TeamPathFinder's raw
        plot-count search horizon before its route-aware cost can be evaluated.
      * F268 reproduces after BOTH routes have been found and the land route genuinely
        qualifies. The defect is that the cache retains only the fastest mixed numeric
        duration, while downstream code needs a land-only duration.

CURRENT PRODUCER CONTRACT
    UWAICache::City::updateDistance documents m_iDistance as a weighted estimate of
    deployment duration. For each significant source city it first evaluates land:

        if (landFinder.generatePath(...))
        {
            iPairwDuration = land path cost / MOVE_DENOMINATOR;
            bQualifyingLandRoute = AI_isPrimaryArea(sourceArea);
            if (bQualifyingLandRoute)
                m_bReachByLand = true;
            if (source is capital)
                m_bCapitalArea = true;
        }

    Current SAS's KI#549 repair correctly keeps strategic land reachability separate
    from route-duration selection.

    A valid sea route then does:

        bReachBySea = true;
        iPathTurns = sea penalty + water-path turns;
        if (iPathTurns < iPairwDuration)
            iPairwDuration = iPathTurns;

    Therefore, when BOTH a qualifying land route and a faster sea route exist:

        m_bReachByLand / m_bCapitalArea  <- land route exists
        m_iDistance                     <- faster sea duration

    This producer behavior is coherent for a generic "best deployment duration" cache.
    The mismatch arises when consumers ask for a LAND-specific duration that the cache
    does not retain.

CURRENT INVASIONGRAPH CONSUMER
    Node::canReachByLand is:

        return (bFromCapital ?
                pCacheCity->canReachByLandFromCapital() :
                pCacheCity->canReachByLand()) &&
               (pCacheCity->getDistance() <= getUWAI().maxLandDist() ||
                !m_kCache.canTrainDeepSeaCargo());

    Node::step then determines the invasion mode from that helper:

        bNaval = !canReachByLand(target, false);

    and, for an ordinary city attack, computes attacker deployment distance from the
    SAME mixed cache value:

        rDeploymentDistAttacker = min(maxSeaDist, pCacheCity->getDistance());

SUPPORTED CURRENT STATE
    A simple coastal target is sufficient:

        qualifying land route: 12 turns
        valid sea route:        5 turns
        maxLandDist:            >= 5 but < 12 (or simply >=12; the duration error
                                still reproduces)

    updateDistance records:

        canReachByLand == true
        getDistance()  ~= 5

    InvasionGraph therefore gets:

        canReachByLand(target) == true   // boolean supplied by the land route
        bNaval == false                  // ordinary land invasion
        deployment distance ~= 5         // number supplied by the sea route

    The graph has thus consumed a SEA travel duration as LAND deployment time. Because
    bNaval is false, the attack also bypasses the naval Fleet clash and Logistics/cargo
    limitations that would be required to realize that five-turn route.

MAX-LAND-DIST MISCLASSIFICATION
    The same mixed metric is used inside canReachByLand's maxLandDist gate. If the
    actual qualifying land route is outside the intended land-duration threshold but
    the sea route is short, the SEA duration can make the LAND test pass.

    This is stronger than the broad design criticism "UWAI should sometimes choose sea
    even when land is possible": the implementation is pairing one route's boolean
    with a different route's duration.

CROSS-FILE SAME-ROOT CONSUMER: ArmamentForecast.cpp
    ArmamentForecast classifies target-specific buildup as naval with:

        if (!pTargetCity->canReachByLandFromCapital() ||
            pTargetCity->getDistance() > getUWAI().maxLandDist())
            bNavalArmament = true;

    The same supported state therefore lets a capital-qualified but long land route be
    classified as non-naval because the single getDistance() value was shortened by a
    faster sea route. This can bias forecast production toward land rather than naval
    preparation for the very route supplying the short duration.

    Keep this under F268 rather than assigning a second root: both consumers fail for
    the same missing land-specific cached metric.

ANCESTRY / INTENT EVIDENCE
    The initial tracked UWAI cache already separated a reachByLand boolean from one
    numeric pairwise duration and replaced that duration with sea travel when sea was
    faster. The original InvasionGraph helper simultaneously combined reachByLand with
    getDistance <= maxLandDist. The representation mismatch is therefore inherited,
    not SAS-specific.

    Practical 2767 broadened land reachability from capital-only to primary areas and
    introduced/retained canReachByLandFromCapital, but still used the same one numeric
    distance. The 2920 revised-UWAI rewrite preserves the contract.

    Base AdvCiv 1.14 retains both current consumers:
      * InvasionGraph::Node::canReachByLand uses canReachByLand* + getDistance;
      * ArmamentForecast uses canReachByLandFromCapital + getDistance.

    Most diagnostic of all, the AdvCiv manual's own UWAI TODO states:

        InvasionGraph::Node::step sets isNaval solely from land reachability;
        UWAICache::City does not store separate distances via land and sea;
        add a separate land-only distance (suggested name: distByLand).

    That TODO identifies the exact missing representation needed by the current
    consumers and strongly supports treating this as an unfinished known design defect,
    not a speculative reinterpretation.

REPAIR DIRECTION
    Preserve at least one separate land-only deployment metric in UWAICache::City.
    For example:

        m_iDistance         = best/mixed deployment duration (existing semantics)
        m_iDistanceByLand   = weighted qualifying land-only duration, -1 if unavailable

    Then:
      * InvasionGraph land reachability / maxLandDist tests use m_iDistanceByLand;
      * a land invasion uses m_iDistanceByLand for its deployment duration;
      * a naval invasion uses a sea-appropriate distance and executes Fleet/Logistics
        modeling;
      * ArmamentForecast's target-specific naval/land classification uses the land-only
        metric rather than the mixed best-route value.

    A narrow patch that merely removes getDistance <= maxLandDist from
    canReachByLand() is NOT sufficient: step() would still feed the mixed sea duration
    into a land attack.

    Conversely, changing getDistance() globally to land-only would lose the generic
    fastest-route information used elsewhere. The missing abstraction should be added
    rather than silently changing the existing metric's meaning.

NEGATIVE CONTROLS / SCOPE
    * If land is actually the fastest route, getDistance agrees with the land route and
      F268 does not reproduce.
    * If no qualifying land route exists, bNaval normally remains true; that is outside
      this root (subject to independent reachability issues such as F226).
    * KI#549/F226 does not repair F268. Current SAS correctly lets a valid sea route set
      bReachBySea independently while still replacing iPairwDuration only when faster;
      F268 is precisely the later need to remember BOTH route metrics.
    * F240 can make land reachability falsely absent before F268 is reached; F268 needs
      the land path to have been found successfully.
    * This does not claim that UWAI must always choose the objectively fastest travel
      mode. It claims only that once a LAND mode is selected, the duration and
      maxLandDist decision cannot safely come from an unidentified mixed/sea route.


C021-WIP15 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589
    F267 / provisional KI#590
    F268 / provisional KI#591

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F269 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#592.)

Resume from F269 unused.

Best next work:
    * continue surviving-node edge/distraction semantics, especially successful defense
      and multi-invader state transitions after F267;
    * continue Army/Cavalry/Home-Guard overlap invariants against practical 1335/1366
      and later one-sided fixes;
    * inspect remaining SimulationStep result ownership / branch-loss conversions and
      city-success/failure bookkeeping;
    * keep F226/F240/F268 route/cache boundaries distinct while checking any remaining
      InvasionGraph getDistance consumers;
    * keep deliberate UWAI approximations and manual TODOs below the bug bar unless a
      concrete producer -> state -> consumer contradiction can be shown.

No repository source files modified. Audit album only.


C021-WIP16 ADDENDUM / CONTINUATION
=================================

F268 SAME-ROOT ADDENDUM -- clashDistance ALSO CONSUMES THE MIXED ROUTE METRIC
-----------------------------------------------------------------------------
    The final F268 pass found one more current InvasionGraph consumer that should stay
    under F268 rather than receive a separate root.

    Node::clashDistance estimates where two mutually targeting armies meet by averaging
    the cached target-city distances:

        return scaled(pTargetCity->getDistance() +
                      pOtherTargetCity->getDistance(), 2);

    Those getDistance() values have the same mixed best-land/sea semantics established
    in F268. A graph clash that is being modeled as an overland Army encounter can
    therefore inherit a sea-shortened target-city duration when both land and sea routes
    exist. That can shorten the nominal clash distance and feeds directly into the
    deployment-distance arithmetic in step(..., bClashOnly=true).

    This is not a new root. It is another consumer showing why the missing abstraction
    is a route-specific land distance rather than a narrow fix in canReachByLand().


F269 / PROVISIONAL KI#592 -- SIMULATED CITY LOSSES DO NOT REDUCE PER-AREA CITY COUNTS,
SO LATER AREA WEIGHTS AND GARRISON RALLY SOURCES CAN INCLUDE ALREADY-CONQUERED CITIES
----------------------------------------------------------------------------------------
CLASSIFICATION:
    Inherited UWAI simulation-state bug / stale spatial inventory.

STATUS:
    LIVE. Confirmed in current SAS and Base AdvCiv 1.14; introduced with the area-aware
    availability/rally logic in practical 1289. The source has carried explicit Fixme
    comments acknowledging both manifestations since their introduction.

ROOT CAUSE
    InvasionGraph maintains simulated conquest state separately from the real map:

        iRemainingCitiesDef = iDefCities - kDefender.m_cityLosses.size();
        iRemainingCitiesAtt = iAttCities - m_cityLosses.size();

    but when it needs the number of those remaining cities in a particular CvArea it
    reads:

        pBattleArea->getCitiesPerPlayer(player)

    CvArea::getCitiesPerPlayer returns m_aiCitiesPerPlayer, an ACTUAL game-state city
    count. That map counter is changed by real CvCity/CvPlot ownership/lifecycle code;
    InvasionGraph::addCityLoss/addConquest only update graph-local containers and do not
    mutate CvArea or real city ownership.

    Therefore a city that has already been conquered inside the military simulation is
    removed from the graph-wide remaining-city denominator but remains present in every
    per-area numerator/rally count.

CURRENT CONSUMER 1 -- MOBILE ARMY AREA WEIGHT
    Current step() computes, for a land battle/clash:

        rAreaWeightAtt *= scaled(
            pBattleArea->getCitiesPerPlayer(m_ePlayer),
            iRemainingCitiesAtt);

        rAreaWeightDef *= scaled(
            pBattleArea->getCitiesPerPlayer(kDefender.m_ePlayer),
            iRemainingCitiesDef);

    The source itself says immediately above the attacker expression:

        // Fixme(?): getCitiesPerPlayer should be reduced based on lost cities

    The same stale numerator affects the defender even though the comment is written
    only on the attacker block.

SUPPORTED DEFENDER EXAMPLE
    D begins the simulation with four actual cities:

        battle/island area: D1, D2
        another area:       D3, D4

    A conquers D1 in the simulation. No real CvCity ownership changes, so:

        kDefender.m_cityLosses.size()                  = 1
        iRemainingCitiesDef                            = 3
        pBattleArea->getCitiesPerPlayer(D)             = 2   [stale actual map count]
        simulated remaining cities in pBattleArea      = 1   [only D2]

    On A's subsequent attack on D2, current raw area share is therefore:

        current: 2 / 3
        simulated reality represented by graph state: 1 / 3

    The defender clamp is [1/3, 1], so this example is not masked: current code assigns
    roughly twice the local mobile-Army share that its own simulated city distribution
    implies. rDefArmyPow and the overlapping rDefCavPow are then multiplied by that
    overstated weight and flow into city-defense/clash outcomes.

    The same producer mismatch exists on the attacker side when the attacker has
    simulated city losses in pBattleArea.

CURRENT CONSUMER 2 -- GARRISON RALLY CAP
    Later in the same city-defense calculation, the number of garrisons allowed to
    rally from nearby cities is capped by:

        iRallyBound = min(
            pBattleArea->getCitiesPerPlayer(kDefender.m_ePlayer) - 1,
            iRallyBound);
        iRallyBound = min(iRemainingCitiesDef - 1, iRallyBound);

    and the source states:

        /* -1: garrison of c already counted as local.
           Fixme(?): Should subtract lost cities in pBattleArea. */

    In the four-city example above, while D2 is attacked:

        current local-other-city cap = 2 - 1 = 1
        simulated local-other-city cap = 1 - 1 = 0

    Thus the already-conquered D1 can still authorize one additional rallied garrison
    into D2. The global iRemainingCitiesDef cap does not repair the spatial error because
    D3 and D4 still exist elsewhere.

LIVE SEQUENTIAL PATH
    This does not require a contrived retarget or phase-reset state:

      1. A -> D attacks D1 successfully.
      2. applyStep records D1 in D.m_cityLosses and in A.m_conquests.
      3. A's primary-target edge remains live after success (unless an independent
         terminal condition removes it).
      4. addConquest advances A's cache cursor; targetCity() skips cities already lost
         by D and can return D2.
      5. The next step against D2 computes iRemainingCitiesDef from m_cityLosses, while
         pBattleArea->getCitiesPerPlayer(D) still counts D1 and D2.

    Hence both stale consumers are reachable during ordinary repeated conquest of one
    defender.

ANCESTRY / INTENT EVIDENCE
    Practical 1289 (ed1826faf1) introduced the area-availability logic and already wrote:

        Fixme(?): getCitiesPerPlayer should be reduced based on lost cities

    in the attacker area-weight block. The same commit introduced the local rally cap
    with:

        Fixme(?): Should subtract lost cities in battleArea.

    The new code simultaneously introduced/used remainingCitiesAtt/Def, i.e. it already
    understood that total city counts must be simulation-adjusted but lacked an
    equivalent per-area simulated count.

    Base AdvCiv 1.14 retains both Fixme comments and both stale CvArea consumers.
    Current SAS retains them unchanged in substance.

    CvArea::getCitiesPerPlayer itself returns m_aiCitiesPerPlayer. Its mutation API,
    changeCitiesPerPlayer, is called by actual city/plot/map lifecycle code; there is no
    InvasionGraph simulation hook that changes it. This confirms that the numerator is
    not secretly updated when addCityLoss is called.

WHY ONE ROOT, NOT TWO
    The area-weight overstatement and phantom rally source have the same missing state:
    there is no "remaining simulated cities of player P in area A" count. Treat them as
    two consumers of F269 rather than assigning separate findings.

DISTINCT FROM NEARBY ROOTS
    * Not F260/F265: those concern overlapping CAVALRY failing to track reductions to
      ARMY after area/deployment filtering. F269 says the AREA FILTER ITSELF receives a
      stale city distribution before any Cavalry subset issue is considered.
    * Not F266: F266 concerns HOME_GUARD losses against virtual emergency-defender power
      being clipped/discarded. F269 can create a garrison rally source from a city that
      the graph already regards as conquered even with emergency power = 0.
    * Not F267: no A->B->C outgoing-offensive state is needed. One attacker repeatedly
      conquering cities from one defender is sufficient.
    * Not a real-map ownership bug: InvasionGraph intentionally does not mutate the
      actual game map. The bug is that a real-map spatial aggregate is consumed without
      subtracting the graph-local simulated losses that its own total denominator does
      subtract.

REPAIR DIRECTION
    Compute a simulation-adjusted per-area city count for each node/player, e.g.:

        remainingCitiesInArea(player, area) =
            area.getCitiesPerPlayer(player)
            - number of that node's m_cityLosses whose cached/real city lies in area

    Use that adjusted count consistently for:
      * attacker land/clash rAreaWeightAtt numerator;
      * defender rAreaWeightDef numerator;
      * defender local-other-city iRallyBound cap.

    Clamp the adjusted count to a sane nonnegative range and preserve the existing
    total-remaining-city denominator/clamps.

    Do NOT mutate CvArea or CvCity ownership during the simulation merely to make these
    reads work; the graph is intentionally hypothetical and other systems must continue
    to see the real game state.

NEGATIVE CONTROLS / BOUNDS
    * Before any simulated loss for that player, the actual per-area count and simulated
      per-area count coincide; F269 does not reproduce.
    * If all lost cities are in OTHER areas, the battle area's numerator is still valid.
    * Some map distributions/capital multipliers/clamps can hide the numerical
      difference, but the 2-local + 2-remote example above survives the defender 1/3
      floor and the rally cap independently.
    * The existing iRemainingCitiesDef/Att values correctly adjust the GLOBAL totals;
      replacing them is not the repair. The missing adjustment is spatial/per-area.


C021-WIP16 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589
    F267 / provisional KI#590
    F268 / provisional KI#591
    F269 / provisional KI#592

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F270 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#593.)

Resume from F270 unused.

Best next work:
    * continue successful-defense / multi-invader state and remaining city-combat
      bookkeeping after F269;
    * continue Army/Cavalry/Home-Guard overlap invariants against historical one-sided
      fixes, without splitting same-root consequences from F260/F265/F266;
    * inspect any remaining uses of actual map city/area state inside hypothetical
      simulation state for a concrete downstream contradiction;
    * keep F268's remaining mixed-distance consumers as same-root addenda unless they
      expose an independent producer/state defect;
    * preserve current conservative standard: controls/retractions are preferred over
      manufacturing a new root.

No repository source files modified. Audit album only.


C021-WIP17 ADDITIONAL CONTROL -- PHASE-BOUNDARY EMERGENCY DEFENDERS
===================================================================
F270 remains UNUSED.  The following initially looked like a persistence bug but does
not presently meet the audit bar after tracing the full ArmamentForecast timing model.

CANDIDATE TESTED
    Node::prepareForSimulation() resets:

        m_rEmergencyDefPow = 0;

    at the start of every InvasionGraph::simulate() call. MilitaryAnalyst can call
    simulate twice (preparation phase, then main horizon), while simulated city losses
    and normal MilitaryBranch power persist. At first sight this seems to make defenders
    explicitly "trained" after a phase-I city loss disappear at the phase-II boundary.

WHY THIS IS NOT PROMOTED
    InvasionGraph::simulate intentionally splits ordinary armament production into:

        1. initial build-up for the first half of the phase;
        2. loss simulation over the full phase horizon;
        3. concurrent build-up for the second half, committed to m_military only after
           the loss simulation.

    ArmamentForecast's class contract is explicit: it predicts how many units are
    TRAINED, ignoring losses; its predicted branch power is written permanently into
    m_military. Thus the second-half forecast can represent wartime-trained defenders in
    the persistent branch total even though those units were unavailable to the generic
    loss simulation while that simulation was running.

    The emergency-defender comment is consistent with a TEMPORAL bridge for that
    approximation rather than an independently additive permanent production pool:

        "Defenders trained while the war is conducted are generally covered by
         ArmamentForecast, including defenders hurried just prior to an attack, but not
         emergency defenders trained while the invading army heals and approaches its
         next target."

    In other words, m_rEmergencyDefPow lets some wartime training influence a later city
    assault INSIDE the same loss simulation even though the concurrent ArmamentForecast
    is only applied afterward. Carrying the same auxiliary pool into the next simulate()
    call, on top of the permanently added concurrent build-up, could double-count that
    training.

HISTORY / CONTROL
    The first tracked UWAI import (practical 1253) already combines all three pieces:
      * emergencyDefPow reset in prepareForSimulation;
      * the same comment describing ordinary wartime defenders as principally covered
        by ArmamentForecast and emergency defenders as a timing gap;
      * the simulate ordering of initial build-up -> losses -> concurrent build-up.

    The current 2920/scaled rewrite preserves this model. No later change found so far
    establishes emergencyDefPow as a separately produced permanent branch that ought to
    survive a phase boundary.

BOUND / RELATION TO F266
    This does NOT retract F266. F266 has a different hard same-phase path: emergency
    defender power that is actively used during a loss simulation can suffer calculated
    HOME_GUARD casualties that are then discarded by the base-branch cumulative loss cap
    and immediately be reused by a later invader. That does not depend on whether the
    auxiliary pool is intentionally phase-local.

AUDIT RESULT
    REJECT / CONTROL for now. Do not assign F270 merely because emergencyDefPow is reset
    between simulation phases. Reopen only if a producer or result consumer demonstrates
    that emergencyDefPow is intended to be additional permanent training beyond the
    concurrent ArmamentForecast rather than a within-phase timing proxy.


C021-WIP17 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589
    F267 / provisional KI#590
    F268 / provisional KI#591
    F269 / provisional KI#592

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F270 UNUSED
    (KI#586 remains retired with retracted F263; next provisional KI is KI#593.)

Resume from F270 unused.

Best next work:
    * finish the practical-2920 old/new operator, condition, polarity and ownership
      comparison, ignoring mechanical double->scaled/member-renaming noise;
    * continue residual target/result and branch-subset invariants;
    * prefer controls/retractions or saturation over forcing F270.

No repository source files modified. Audit album only.


===============================================================================
CHECKPOINT C021-WIP18 -- FILE 023 OPEN / CONVERGENCE + SATURATION PASS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Latest source snapshot re-anchored for this pass:
    AdvCiv-SAS_light_source_20260828T134631.zip
Comparison controls retained:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(3).zip
    Civ4CE-main(10).zip
    local AdvCiv pre-SAS commit-diff mirror, especially practical 1253 and 2920+

F270 remains UNUSED. KI#593 remains the next provisional KI number.

PURPOSE OF THIS PASS
--------------------
This pass was intentionally a convergence/saturation pass rather than a search for an
issue count. The remaining current InvasionGraph.cpp state was inventoried by producer
and consumer, and the large practical-2920 rewrite was re-read through operator/
condition/ownership-focused comparisons rather than raw textual diff noise.

No new independent hard producer -> state -> consumer contradiction crossed the audit bar.
The absence of F270 is therefore meaningful evidence of convergence, not an unfinished
search caused by staying only in previously reviewed regions.


CONTROL -- PHASE-II TARGET REFRESH CALLER CONTRACT
---------------------------------------------------
A possible stale-target path was tested around addFutureWarParties' early return and the
two-phase MilitaryAnalyst flow.

The live caller contract closes the suspected gap:
    * a prospective war with a preparation phase supplies the relevant declaring side;
      addFutureWarParties then calls updateTargets();
    * when the evaluated war is already active, WarEvaluator collapses preparation time
      so that the problematic two-phase prospective-war state is not produced.

No F270 assigned.


CONTROL -- 75%-OF-AVERAGE POPULATION RALLY THRESHOLD USES ACTUAL CITY COUNT DELIBERATELY
-----------------------------------------------------------------------------------------
The city-rally heuristic uses actual getNumCities() rather than the simulation-adjusted
remaining-city count. That initially resembled F269's stale real-map spatial inventory.
History argues against treating this particular denominator as an omission:

    * the first tracked UWAI implementation already maintained a separate simulated
      remaining-city value while deliberately using actual getNumCities() for the
      population-average threshold;
    * practical 1289 made remainingCitiesDef explicit and still retained the actual
      city-count denominator for this heuristic.

This is best read as a stable pre-war/current-map city-importance heuristic. No F270.


CONTROL -- PRACTICAL-2920 CLASH-AREA CAPITAL WEIGHT PRESERVES PREDECESSOR SEMANTICS
-----------------------------------------------------------------------------------
The pre-rewrite clashArea code had two owner tests under `if (capital)`, while the revised
code combines them with `||` and comments "Count it double". This is equivalent: the
iterated city has only one owner, so the two predecessor increments could never both
fire. The capital contributes its ordinary city count plus one extra weight in both
versions. No operator/polarity regression here.


CONTROL -- PRACTICAL-2920 `healDuration = 2` -> `iHealTurns += 2` IS EQUIVALENT
--------------------------------------------------------------------------------
The operator-focused rewrite scan surfaced the apparent assignment-to-addition change in
the prior-conquest deployment block. `iHealTurns` is initialized to zero inside each
step and has no earlier producer before this block. Consequently `+= 2` and predecessor
`= 2` are equivalent in the live flow. No F270.


CONTROL -- CONQUEST OCCUPATION DETACHMENT DOES NOT REQUIRE A SEPARATE CAVALRY LOSS ROOT
---------------------------------------------------------------------------------------
After a successful conquest, applyStep adds `rPowLeftBehind` to ARMY lost/shifted power
without adding a parallel CAVALRY value. This was tested as a possible independent
subset violation.

It does not presently justify F270:
    * the detachment is explicitly SHIFTED/UNAVAILABLE Army power, not a casualty;
    * every subsequent step reconstructs Army and Cavalry availability and enforces
      `rCavPow.decreaseTo(rArmyPow)` before combat;
    * getLostPower subtracts the Army shift so the occupation detachment is not exported
      as a casualty;
    * no independent current consumer was found that uses the unprojected Cavalry total
      after the Army detachment while bypassing the step-level availability clamp.

A future model could track which unit subtype remains as occupation troops, but current
representation is not a hard independent bug. Do not split from F260/F265 merely because
CAVALRY is an overlapping branch.


CONTROL / EXISTING-ROOT BOUND -- DEFENDER CAVALRY AND DEPLOYMENT-UNDO PATH
---------------------------------------------------------------------------
The defender's deployment-distance modifier initially scales ARMY but not CAVALRY; later,
when a city attack bypasses a second open-field clash, the code divides ARMY by that
modifier again because the defending army is assumed to have retreated to the city.

This was re-challenged as a possible F270. It should remain inside the already documented
F265 repair bounds rather than become another root:
    * F265's hard live consumer is the ATTACKER deployment projection, where Army is
      attenuated and the unattenuated Cavalry subset can exceed it and enter unbounded
      city-loss ratios;
    * F265 already warns that a defender-side repair must mirror the later Army undo on
      Cavalry too, or it would create a new mismatch;
    * current defender city flow does not provide a cleaner independent root than the
      attacker-side F265 contradiction.

No F270 assigned.


CONTROL -- `rPastThreat` IS INTENTIONALLY ONE-ITERATION CARRYOVER
-----------------------------------------------------------------
resolveLosses keeps only the immediately prior failed attack's threat rather than a sum
of all past failed attackers. This can look like forgotten cumulative state. The source
comment, however, explicitly describes threat "that carries over from one iteration to
the next," and the timing model permits an older defensive allocation to redeploy while
the next invasion is being resolved. Without a hard overlapping-time counterexample,
accumulating every prior failure would be an unjustified model change. No F270.


STATE/FUNCTION COVERAGE CHECK
-----------------------------
The convergence pass explicitly re-inventoried all persistent Node simulation state and
its writes/reads in current SAS:

    m_pPrimaryTarget / m_targetedBy
        target discovery, retargeting, failure unlink, cycle break, terminal-state paths;
        live hard defects already represented by F251/F267 and related controls.

    m_iCacheIndex
        reset, targetCity scan, addConquest advance; phase-lifecycle cross-target failure
        hypothesis already rejected because each phase resets the cursor.

    m_arLostPower / m_arShiftedPower
        all step application, branch caps, left-behind detachment, exported lostPower;
        live hard defects represented by F252/F257/F264/F266 where applicable.

    m_rEmergencyDefPow
        reset, city-defense contribution, successful-step mutation, production after loss;
        F253 covers query-side mutation; F266 covers same-phase casualty discard/reuse;
        phase-boundary persistence hypothesis remains rejected/control.

    m_rTempArmyLosses
        phase reset, clash producer, attacker availability consumer; no independent
        uncovered owner/lifetime path found beyond F257 confidence conversion.

    m_rDistractionByConquest / m_rDistractionByDefense
        phase reset, cycle producer, multi-invader consumers, city-loss transition;
        F255 and F267 cover the hard mismatches found; global continued-war behavior
        remains below the bug bar as an aggregate-army approximation.

    m_bHasClashed
        producer after applied combat and city-rally consumer; persistent across phases
        consistently with prior simulated combat. No new hard contradiction found.

    m_conquests / m_cityLosses
        target filtering, production portion, area/rally consumers, result export;
        F269 covers the missing per-area simulated inventory; stable real-map population
        heuristic was separately rejected above.

    m_capitulationsAccepted / m_bCapitulated / m_bEliminated
        capitulation/elimination production and MilitaryAnalyst result export; F251 covers
        live stale-edge terminal behavior. The 2920 teammate recursion polarity remains a
        dormant regression because the current capitulation caller requires a last living
        team member; no separate live root promoted.

    m_iWarTurnsSimulated
        successful-city producer and currently commented-out consumer; its absence from
        live timing contributes to F267's proven fresh-horizon path but is not split into
        a generic "unused field" finding.

    SimulationStep ownership/result fields
        every current reducePower caller is one of the step attacker/defender pair;
        omitted scaled members zero-initialize safely; no third-party storage path found.

Major pure/plumbing functions (constructors, add/remove opponents, result getters,
changePrimaryTarget, findCycle/findSink, simulateArmament/simulateLosses) were also
rechecked for ownership/lifetime assumptions while tracing the states above.


POST-2920 HISTORY SATURATION CONTROL
------------------------------------
The substantive later InvasionGraph changes were re-enumerated as intent controls:
    practical 2923  -- contains the confirmed F256 compound-assignment regression;
    practical 2928  -- repairs the `!isNaval && !clashOnly` area condition to `||`;
    practical 2943/2954 -- temporary defender-advantage/garrison mistake resolved by
                           the later merge into the current "cap bonus only" logic;
    practical 3045  -- nonnegative defender-Fleet / reducePower protections;
    practical 3477  -- intentional uniform-garrison cycle-threat probe;
    practical 3662  -- scaled representation conversion for willingness arithmetic;
    practical 3946  -- deliberate human rally multiplier.

Current SAS differs materially from Base AdvCiv 1.14 in this file only through the
already-known KI#561 Home-Guard/nuclear headcount repair plus formatting/comment changes.
K-Mod/Civ4CE still provide no cognate UWAI InvasionGraph implementation to compare.


SATURATION ASSESSMENT
---------------------
Queue 023 has now had:
    * current SAS vs Base semantic comparison;
    * initial-UWAI ancestry checks;
    * focused practical-2920 semantic migration review;
    * later repair-history reverse audit;
    * graph topology / mutation / recursive-order review;
    * target-cache / reachability / land-vs-sea review;
    * Army/Cavalry/Home-Guard and Fleet/Logistics overlapping-branch review;
    * phase/timing/horizon review;
    * capitulation/elimination/result ownership review;
    * persistent-state producer -> consumer inventory;
    * multiple explicit false-positive challenges, including retraction of F263.

The latest convergence slices have yielded controls rather than independent roots.
This is strong evidence that InvasionGraph.cpp is close to saturation. Keep the file OPEN
for one final root-overlap / repair-scope reconciliation pass before declaring it COMPLETE;
do not invent F270 merely to close with a new number.


C021-WIP18 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    20 / 169

CURRENT FILE:
    queue 023 CvGameCoreDLL/InvasionGraph.cpp -- OPEN, FINAL CONVERGENCE PASS

LIVE QUEUE-023 ROOTS:
    F250 / provisional KI#573
    F251 / provisional KI#574
    F252 / provisional KI#575
    F253 / provisional KI#576
    F254 / provisional KI#577
    F255 / provisional KI#578
    F256 / provisional KI#579
    F257 / provisional KI#580
    F258 / provisional KI#581
    F259 / provisional KI#582
    F260 / provisional KI#583
    F261 / provisional KI#584
    F262 / provisional KI#585
    F264 / provisional KI#587
    F265 / provisional KI#588
    F266 / provisional KI#589
    F267 / provisional KI#590
    F268 / provisional KI#591
    F269 / provisional KI#592

RETRACTED:
    F263 / provisional KI#586 -- audit false positive; ordinary failed city attack does
    unlink the attacker through applyStep's failure `else`.

NEXT ROOT:
    F270 UNUSED
    KI#586 remains retired; next provisional KI is KI#593.

Exact next cursor:
    * perform final overlap/root-boundary review of F250-F269, especially naval roots
      F261/F262/F264 and overlapping-branch roots F260/F265/F266;
    * verify repair scopes don't accidentally conflict with intentional abstractions;
    * if no independent contradiction survives, promote queue 023 to COMPLETE without
      allocating F270 and select the next remaining priority-marked file from the
      reconciled queue.

No repository source files modified. Audit album only.
===============================================================================

===============================================================================
DURABLE CHECKPOINT C022 -- FILE 021 COMPLETE / QUEUE 023 InvasionGraph.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot for this close:
    AdvCiv-SAS_light_source_20260828T134631.zip

Comparison/history material used across the queue-023 pass:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(3).zip
    Civ4CE-main(10).zip
    current SAS source / XML / GlobalDefines
    LLM_Helpers/context/commit_diffs/*
    LLM_Helpers/context/commit_diffs/PATH_HISTORY_INDEX.txt
    _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt
    Base AdvCiv manual / UWAI notes where they document route-distance intent

Prior physical checkpoint:
    C021-WIP18

DURABLE RESULT
--------------
    COMPLETED FIRST-PASS FILES: 21 / 169

    queue 023
    CvGameCoreDLL/InvasionGraph.cpp
    STATUS: COMPLETE -- FIRST-PASS DURABLE CLOSE

    Live queue-023 roots:
        F250 / provisional KI#573
        F251 / provisional KI#574
        F252 / provisional KI#575
        F253 / provisional KI#576
        F254 / provisional KI#577
        F255 / provisional KI#578
        F256 / provisional KI#579
        F257 / provisional KI#580
        F258 / provisional KI#581
        F259 / provisional KI#582
        F260 / provisional KI#583
        F261 / provisional KI#584
        F262 / provisional KI#585
        F264 / provisional KI#587
        F265 / provisional KI#588
        F266 / provisional KI#589
        F267 / provisional KI#590
        F268 / provisional KI#591
        F269 / provisional KI#592

    Retracted / retired:
        F263 / provisional KI#586 -- RETRACTED audit false positive.
        Ordinary failed city attacks do unlink the attacker through applyStep's
        failure `else kAttacker.changePrimaryTarget(NULL);`.
        KI#586 remains retired and must not be implemented or reused.

    NEXT GLOBAL ROOT ID:
        F270 UNUSED
        next provisional KI: KI#593

FINAL ROOT-OVERLAP / INDEPENDENCE REVIEW
----------------------------------------
The final convergence pass challenged the live findings against one another by asking
whether each root can reproduce while the nearest-looking sibling is neutralized. No
root needed to be merged or retracted beyond the already-retired F263.

Naval accounting cluster:

    F261 versus F262:
        DISTINCT.
        F261 is produced by asymmetric confidence conversion of attacker LOGISTICS
        casualties. It reproduces with repeat-trip amplification neutralized.

        F262 is produced by operation ordering: pre-battle cargo capacity is multiplied
        for repeat trips before destroyed transport capacity is subtracted. It reproduces
        at confidence 1, where F261 disappears.

    F264 versus F261/F262:
        DISTINCT.
        F264 lives on the coastal-bombardment / no-landing defender path. Transport hulls
        may contribute to and be removed from FLEET while LOGISTICS is left unchanged,
        allowing stale cargo capacity to survive into a later outgoing invasion.
        It does not require the attacker LOGISTICS confidence conversion of F261 or the
        repeat-trip ordering of F262.

Overlapping-branch cluster:

    F260 versus F265:
        DISTINCT.
        F260 is the later battle-area projection: attacker ARMY is area-weighted while
        overlapping CAVALRY is not. It reproduces with deployment attenuation neutral.

        F265 is the earlier deployment-distance attenuation: attacker ARMY is reduced
        while overlapping CAVALRY is not. It reproduces with battle-area weight 1.
        Its repair scope remains deliberately attacker-focused; changing defender
        CAVALRY requires mirroring the later defender-ARMY retreat-to-city undo.

    F266 versus F260/F265:
        DISTINCT.
        F266 concerns HOME_GUARD plus the separate virtual emergency-defense pool and
        the cumulative base-branch loss cap. It needs neither CAVALRY nor the Army
        availability transforms of F260/F265.

Cycle / confidence cluster:

    F255 versus F256:
        DISTINCT.
        F255 swaps ownership of already-computed `this`/target Army portions when writing
        cycle distraction state. It reproduces even if those portions were calculated
        correctly.

        F256 changes the portion values themselves through the practical-2923 `*=`
        regression. It reproduces before any F255 distraction assignment is consumed.

    F257 versus F261:
        DISTINCT.
        Both expose confidence-space conversion errors, but in separate branch/state
        systems: F257 converts open-field/cycle ARMY clash losses back to persistent
        physical Army power without undoing confidence, whereas F261 converts attacker
        transport/LOGISTICS casualties. Either can reproduce with the other branch absent.

Deployment / route / horizon cluster:

    F254 versus F268:
        DISTINCT.
        F254 is a resolveLosses time-horizon error after a genuinely naval step exists:
        the defender-zero preliminary probe is not guaranteed to be a lower-duration
        bound and `break` can erase an in-horizon real combat.

        F268 occurs earlier, while classifying the route: a land-existence boolean is
        combined with a best-route distance that may belong to sea travel, turning sea
        duration into a nominal land invasion and bypassing naval modeling entirely.
        F254 reproduces with route classification already correct; F268 reproduces before
        the F254 probe/horizon logic matters.

    F259 versus F265:
        DISTINCT.
        F259 is the clash-distance split itself going negative on the defender side when
        attacker personality weighting expands one half beyond the fixed total; this can
        violate `uround` assumptions and boost defender power above 100%.
        F265 needs only an ordinary positive attacker deployment attenuation and the
        missing CAVALRY sibling transform.

Graph lifecycle / target cluster:

    F251 versus F267:
        DISTINCT.
        F251 requires a terminal/eliminated/capitulated node that retains an outgoing
        edge and can act as a zombie attacker.

        F267 requires a still-surviving node after a partial third-party conquest. The
        source says its offensive stops and its Army returns to defense, but its outgoing
        edge remains live and can later receive a fresh local horizon. No terminal-node
        condition is needed.

Ownership / observer cluster:

    F252 versus F258:
        DISTINCT.
        F252 calculates attacker's post-conquest occupation power from defender-owned
        representative Army power / adjacent defender-era quantity.
        F258 reads the evaluating graph agent's Defensive trait when modifying a different
        defender's city defense. Separate producer sites, state and consumers.

Spatial-state cluster:

    F268 versus F269:
        DISTINCT.
        F268 is missing route-type-specific distance state (`distByLand`) and contaminates
        route classification / clash distance / ArmamentForecast.
        F269 is missing simulated per-area city inventory after hypothetical conquests and
        contaminates area availability, rally caps and clash-area selection.

Speculative-state / emergency cluster:

    F253 versus F266:
        DISTINCT.
        F253 is a query-side mutation: speculative `step()` halves another node's
        emergency-defense power even when the step is discarded.
        F266 reproduces using actually-applied sequential assaults: casualties inflicted
        on emergency defenders can be clipped away by the HOME_GUARD base-power cap.

No additional independent producer -> state -> consumer contradiction survived the final
pass. In particular, the following late candidates remained controls rather than roots:
    * omitted `scaled` initializers are zero-safe by type construction;
    * player-typed GET_TEAM calls are safe through the overload / TEAMID conversion;
    * phase-boundary emergency-defense reset is consistent with ArmamentForecast timing;
    * LOGISTICS need not mechanically receive the same front portion as ARMY/FLEET;
    * conquest occupation shifts do not create a separate persistent CAVALRY consumer
      because each later step reconstructs availability and clamps CAVALRY <= ARMY;
    * defender CAVALRY deployment asymmetry is already a repair-boundary concern of F265,
      not a second independent current root;
    * stable real-map population average is historically intentional city-importance
      modeling, unlike F269's concrete stale per-area inventory contradiction;
    * the inverted teammate recursion in setCapitulated remains dormant under the live
      `getAliveCount() <= 1` capitulation caller contract;
    * post-2920 operator/condition archaeology found the known F256 regression and known
      later upstream repairs, but no further live semantic migration bug.

SATURATION / COMPLETE RATIONALE
-------------------------------
Queue 023 received substantially more than a simple current-file read:
    * current SAS was compared against Base AdvCiv 1.14 and the tracked UWAI/AdvCiv
      introducing/refactoring history;
    * K-Mod/Civ4CE were checked for cognate controls (no cognate UWAI InvasionGraph exists);
    * graph ownership, edge lifecycle, post-order resolution, target refresh, phase resets,
      capitulation/elimination and two-node cycles were traced end to end;
    * every persistent Node/SimulationStep state member had write sites and downstream
      consumers inventoried;
    * ARMY/HOME_GUARD/CAVALRY/FLEET/LOGISTICS overlap and conversion invariants were traced;
    * naval landing, coastal battle, city siege, open-field clash, deployment and horizon
      paths were challenged numerically and dimensionally;
    * the large practical-2920 semantic migration and post-2920 repair commits were audited
      separately from simple renaming/scaled-conversion noise;
    * attractive candidates were repeatedly rejected, and F263 was publicly/durably
      retracted when a missed control-flow `else` disproved it;
    * final overlap review found all remaining live roots independently reproducible.

Therefore queue 023 is sufficiently saturated for the album's established
FIRST-PASS COMPLETE meaning. COMPLETE does not assert that later cross-file archaeology can
never refine or broaden a root; it means there is no justification for keeping this file
open merely in hope of manufacturing F270.

NEXT FILE -- REMAINING PRIORITY MARKERS
---------------------------------------
The next remaining PENDING priority-marked file in the reconciled priority queue is:

    queue 039
    CvGameCoreDLL/CvGameCoreUtils.cpp
    approximately 1,461 lines in the album snapshot

Later remaining priority-marked files after that:
    queue 040 CvGameCoreDLL/CvGlobals.cpp
    queue 042 CvGameCoreDLL/CvDeal.cpp
    queue 092 CvGameCoreDLL/BBAILog.cpp

Exact next cursor:
    begin queue 039 CvGameCoreUtils.cpp from F270 UNUSED / KI#593 next.

Recommended opening method:
    1. establish current SAS-vs-Base-AdvCiv semantic delta and path history;
    2. inventory utility contracts and all high-fanout helpers before judging local code;
    3. prioritize helpers whose return values affect combat odds, pathing, plot/city/unit
       selection, hashing/indexing, serialization-facing IDs or deterministic sync;
    4. compare K-Mod/Civ4CE cognate helpers where available;
    5. treat generic utility cleanup/style/performance as controls unless a concrete
       producer -> consumer behavior or safety failure is established.

No repository source files were modified by GPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
CHECKPOINT C022-WIP01 -- FILE 022 OPEN / QUEUE 039 CvGameCoreUtils.cpp
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

Authoritative source snapshot:
    AdvCiv-SAS_light_source_20260828T134631.zip

Comparison material opened:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(3).zip
    Civ4CE-main(10).zip (BtS CvGameCoreUtils cognate)
    LLM_Helpers/context/commit_diffs/PATH_HISTORY_INDEX.txt
    SASBranch / AdvCivPreSAS path-history diffs for CvGameCoreUtils.cpp

QUEUE TRANSITION
----------------
Queue 023 InvasionGraph.cpp was durably closed COMPLETE in C022 after the final
root-independence/saturation pass. F270 remains unused; KI#593 is next.

Selected next remaining priority-marked file:
    queue 039
    CvGameCoreDLL/CvGameCoreUtils.cpp
    1,461 lines in current SAS

STATUS:
    OPEN -- INITIAL CONTRACT / DELTA INVENTORY

CURRENT ROOT CURSOR:
    F270 UNUSED
    next provisional KI: KI#593

OPENING SAS-vs-BASE DELTA
-------------------------
A whitespace-insensitive SAS-vs-AdvCiv-1.14 comparison reduces the apparent 375-line
size growth to a fairly bounded semantic delta:

    1. SAS-added `handicapFromDifficulty` helper.

    2. A large SAS diagnostic / victory-denial helper block:
         getSASDiploEventType
         getSASMemoryType
         getSASTradeItemType
         getSASWarPlanType
         getSASGameType
         getSASGameMode
         getSASCalendarType
         getSASWarDeclarationCause
         getSASTechAcquisitionCause
         getSASAutoPlayEndCause
         getSASVictoryStageLevel and per-victory wrappers
         getSASTeamMaxVictoryStage
         getSASTeamSpaceVictoryStage
         getSASTeamSpaceshipPartsBuilt / Required / Percent
         getSASVictoryDelayTurnsFromNormalGameSpeed
         getSASLeadingSpaceshipPartsBuilt
         isSASTeamStage3SpaceVictoryThreat
         isSASUWAIVictoryDenialPeaceThreat
         getSASTeamStage3SpaceLeaderPartGap

    3. Minor formatting-only signature collapses.

    4. `baseYieldToSymbol` caches MAX_YIELD_STACK in a local static const.
       The function is documented unused in current BtS/AdvCiv; no live issue inferred.

    5. `getTurnMonthForGame` hoists the current CvGameSpeedInfo and increment count;
       behavior is equivalent for valid game-speed XML. CALENDAR_WEEKS caches the
       WEEKS_PER_MONTHS define in a local static const.

    6. SAS UnitAI enum renames in getUnitAIString.

Everything else in the old high-fanout utility/PUF region is essentially Base AdvCiv
rather than a broad SAS rewrite. Therefore the audit should treat the file as two layers:

    A. SAS-added helper contracts and their cross-file consumers;
    B. inherited AdvCiv/K-Mod/vanilla utility and PUF contracts, compared against K-Mod
       and Civ4CE for missed fixes.

CROSS-FILE DEDUP CONTROL -- F250 ALREADY OWNS THE BROKEN LOCAL INVASIONGRAPH EXTRACTOR
---------------------------------------------------------------------------------------
The current shared helper in this file:

    getSASVictoryStageLevel(...)

correctly checks cumulative victory-stage bits highest-to-lowest (stage4 -> stage1).

Queue 023's F250 is NOT a defect in this shared helper. F250 owns a separate old local
InvasionGraph macro that checks `AI_atVictoryStage(stage1)` first and therefore collapses
all cumulative stages 1..4 to numeric 1.

This file is useful internal control evidence for F250, but the helper itself must not be
rediscovered/re-numbered as F270.

SAS-ADDED HELPER FIRST CONTROLS
-------------------------------
`handicapFromDifficulty`:
    * introduced because SAS handicap `iDifficulty` values are scores, no longer safely
      reconstructible as enum-index * 10 after adding Rookie / Deity+ levels;
    * current callers pass averaged/rounded difficulty scores from CvGame/CvTeam;
    * nearest-score lookup is coherent for current ordered XML;
    * exact midpoint ties choose the earlier XML enum because comparison uses `<`, but no
      caller-visible contract requiring upward tie-breaking has been established.
      CONTROL for now; do not promote.

Spaceship helpers:
    * current SAS XML has each spaceship project's `iMaxTeamInstances` equal to its Space
      `VictoryThreshold`, so summing max-team instances currently matches full ship parts;
    * a hypothetical mod where these diverge would make `getSASSpaceshipPartsRequired`
      less generic, but current SAS does not reproduce such a mismatch;
    * therefore no live F270 on this basis.

`getSASVictoryDelayTurnsFromNormalGameSpeed`:
    * deliberately does NOT static-cache game-speed output, avoiding stale speed if a new
      game is started in the same Civ4 process;
    * current victory-denial defines are 20 Normal-speed turns, and all shipped
      VictoryDelayPercent values produce exact integer scaled windows for 20;
    * helper rounds to nearest whereas CvGame::victoryDelay uses integer truncation, but
      no current shipped threshold divergence results from the configured value.
      Keep as a precision/design control unless a live boundary consumer is found.

`getSASUWAIVictoryDenialPeaceThreat`:
    * parameter is TeamTypes and current UWAIAgent calls pass `kAgent.getID()` where
      kAgent is CvTeamAI; no player/team type confusion there;
    * CvDeal/CvTeamAI callers likewise supply TeamTypes;
    * no ownership mismatch found in opening pass.

INHERITED HIGH-FANOUT OPENING CONTROL
-------------------------------------
PUF enemy/team predicates were compared structurally with K-Mod and Civ4CE cognates.
AdvCiv deliberately rewrote several owner/team names and hidden-nationality visibility
rules; the apparently reversed K-Mod variable names in `PUF_canDeclareWar` are not an
AdvCiv polarity bug. Current AdvCiv/SAS semantics explicitly ask whether input `iPlayer`
can declare on the unit's combat owner and call:

    GET_TEAM(eOurTeam).canDeclareWar(eOtherTeam)

which matches that contract.

No F270 from the initial PUF comparison.

HIGH-FANOUT / SAFETY INVENTORY STARTED
--------------------------------------
Priority groups for the next continuation:

    1. contestedPlots
       - dedup key, second-owner/team assumptions, exclusive-radius consumers.

    2. estimateDirection / cardinal conversion
       - wrap semantics, zero displacement, enum validity.

    3. hotkey parsing
       - boundary lengths / unknown-key fallback, mostly UI safety.

    4. estimateCollateralWeight
       - combat-owner/team filtering, collateral immunity/protection denominator and
         caller assumptions in CvPlayerAI/CvSelectionGroupAI/CvUnitAI.

    5. PUF_* predicates
       - combat owner, hidden nationality, invisibility, coexistence and assume-peace
         semantics; these are high fanout and need caller contracts, not local reading.

    6. intHash / deterministic scaled RNG
       - signed arithmetic / player-capital salt / determinism and possible high-player
         or index assumptions; distinguish theoretical C++ overflow from actual supported
         compiler/runtime behavior.

    7. getTurnMonthForGame / getTurnYearForGame
       - game-speed increment boundaries, BC negative-month division, nondefault calendars,
         and current XML ranges; compare K-Mod/Civ4CE and actual text-manager consumers.

OPENING RESULT
--------------
No new root allocated yet.

    F270 remains UNUSED.

The queue-039 first pass has nevertheless established a clean current delta and prevented
an immediate cross-file duplicate of F250. Continue slowly through the high-fanout helper
contracts before deciding whether any inherited or SAS-added behavior reaches the bug bar.

C022-WIP01 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    21 / 169

CURRENT FILE:
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp -- OPEN

NEXT ROOT:
    F270 UNUSED
    next provisional KI#593

Exact next cursor:
    * trace `contestedPlots` callers / coordinate dedup contract;
    * trace `estimateCollateralWeight` producer-consumer dimensional assumptions;
    * audit PUF caller semantics around combat-owner/hidden-nationality edge cases;
    * inspect intHash history and ScaledNum deterministic RNG use;
    * compare calendar behavior with K-Mod/Civ4CE and shipped game-speed XML;
    * keep F250 as an already-owned cross-file control, not a queue-039 rediscovery.

No repository source files modified. Audit album only.
===============================================================================


===============================================================================
C022-WIP02 — QUEUE 039 CvGameCoreUtils.cpp / HIGH-FANOUT HELPER PASS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol
Status: UNFINISHED WIP; queue 039 remains OPEN.
Repository source files modified: NONE.

This checkpoint preserves the first confirmed queue-039 root plus controls from the
contested-plot, direction, hash and calendar opening pass. It appends to C022-WIP01;
all earlier findings remain unchanged.

F270 / provisional KI#593
K-MOD-INHERITED CROSS-FILE BUG — CvPlayerAI::AI_localAttackStrength passes the
EVALUATING/DEFENDING team as estimateCollateralWeight's ATTACK team
--------------------------------------------------------------------------------
STATUS: CONFIRMED.
OWNING FILE FOR SHARED HELPER: CvGameCoreDLL/CvGameCoreUtils.cpp.
FAULTY LIVE CALLER: CvGameCoreDLL/CvPlayerAI.cpp::AI_localAttackStrength.
LINEAGE: K-Mod inherited; wrong caller semantics originate when collateral weighting
was introduced into AI_localAttackStrength (K-Mod practical 448) and become explicit
under the current helper contract at K-Mod practical 1200.

CURRENT PRODUCER CONTRACT
-------------------------
Current CvGameCoreUtils.cpp declares:

    int estimateCollateralWeight(CvPlot const* pPlot,
            TeamTypes eAttackTeam, TeamTypes eDefenseTeam)

`eAttackTeam` is not merely a label. The helper uses it to:

    * decide which plot team should receive defensive bonuses when eDefenseTeam is not
      supplied;
    * pass the attacking team into CvPlot::defenseModifier;
    * exclude the attacking team's own units from the population sampled for collateral
      immunity / Collateral Damage Protection.

This is the same semantic contract introduced by K-Mod practical 1200 when the helper
changed from a one-team `eDefenceTeam` API to explicit attacker + optional defender.

FAULTY CALLER
-------------
Current CvPlayerAI::AI_localAttackStrength is documented as:

    "Total attack strength of units (potentially) hostile to us ..."

and accepts `eAttackTeam`, defaulting to NO_TEAM. Its unit loop then counts either:

    kUnit.getTeam() == eAttackTeam

or, for NO_TEAM, teams currently at war with `getTeam()`.

Despite this, the collateral scale is initialized as:

    estimateCollateralWeight(pTargetPlot, getTeam())

(or the NULL-plot equivalent).

Here `getTeam()` is the CvPlayerAI object whose local danger/offense is being queried.
For the very common hostile/default mode it is therefore the DEFENDING/evaluating team,
not the team(s) whose attacking units are being valued.

END-TO-END WRONG-STATE PATH
---------------------------
A common concrete case is our city/stack being evaluated against hostile siege units:

    pTargetPlot->getTeam() == getTeam()
    eAttackTeam == NO_TEAM (or a known enemy team)
    actual collateral dealers == hostile team(s)

The helper receives our team as `eAttackTeam` and therefore:

1. Plot defensive bonus is evaluated for the wrong sides.

   With no explicit eDefenseTeam, current helper computes:

       ePlotBonusTeam =
           (pPlot->getTeam() == eAttackTeam ? NO_TEAM : pPlot->getTeam());

   On our own plot, the wrong `eAttackTeam == getTeam()` makes ePlotBonusTeam NO_TEAM.
   Current CvPlot::defenseModifier immediately returns after terrain/feature/hills when
   eDefender == NO_TEAM, before improvement/city defense. Thus the collateral scale for
   enemy attackers can omit the target city's defensive bonus entirely.

2. Resistance is sampled from the wrong unit population.

   The helper excludes:

       if (eAttackTeam != NO_TEAM && pUnit->getTeam() == eAttackTeam)
           continue;

   With the erroneous `eAttackTeam == getTeam()`, that skips our own defenders — exactly
   the units whose collateral immunity/protection should reduce the expected collateral
   inflicted by the hostile siege force. Other co-located units can instead influence
   the sample when eDefenseTeam is unspecified.

3. The bad scale is consumed for every hostile collateral attacker.

   AI_localAttackStrength passes `iBaseCollateral` into:

       kUnit.AI_currEffectiveStr(..., true, iBaseCollateral, ...)

   AI_currEffectiveStr adds an explicit collateral-strength term proportional to
   `iBaseCollateral`, number of possible collateral targets and the attacker's
   collateral factor. The wrong side assignment therefore changes the returned enemy
   local-attack estimate rather than merely affecting a debug/display value.

CALLER IMPACT
-------------
AI_localAttackStrength is high fanout. Current callers use it for, among other things:

    * city safety / danger and evacuation-strength comparisons;
    * unit/group danger and attack/retreat decisions;
    * team surrender / power comparisons;
    * local enemy-offense estimates;
    * UI/debug strength displays.

Many calls use the default `eAttackTeam = NO_TEAM`, i.e. specifically "hostile to us"
mode. Other calls pass a known enemy team. Calls that explicitly ask a player object to
estimate its own team's attack can happen to make `getTeam()` equal the actual attacker,
but that does not rescue the general helper contract.

The error is not monotonic. For example:

    * omitting real city defense can undervalue the relative benefit of enemy collateral;
    * excluding defenders with high collateral protection/immunity can overvalue it.

So this can move tactical strength estimates in either direction depending on the plot
and defending stack.

HISTORY / REGRESSION BOUNDARY
-----------------------------
K-Mod practical 448 (`3db35153a5...`, "Increased the AI's estimate of collateral damage
when calculating attack strength") first introduced estimateCollateralWeight and wired
it into AI_localAttackStrength. At that point the helper took only an eDefenceTeam, but
AI_localAttackStrength chose that team from the plot's relation to `getTeam()` rather
than from the method's `eAttackTeam` mode. Thus the caller was already incapable of
representing both hostile-vs-us and own-vs-target modes correctly.

K-Mod practical 1200 (`8b46440a9c...`, "estimateCollateralWeight now takes collateral
resistance into account") changed the helper to the current explicit contract:

    estimateCollateralWeight(pPlot, eAttackTeam, eDefenceTeam = NO_TEAM)

and changed AI_localAttackStrength to:

    estimateCollateralWeight(pTargetPlot, getTeam())

This makes the ownership mismatch unambiguous: the new first argument is explicitly the
collateral DEALER, while the caller supplies the CvPlayerAI evaluator.

K-Mod 1.46 still contains the same call. Base AdvCiv 1.14 still contains it. Current SAS
still contains it. This is inherited K-Mod behavior, not a SAS regression.

CROSS-FILE / DUPLICATE CHECK
----------------------------
The current CvSelectionGroupAI callers also pass `getTeam()`, but there `getTeam()` is
the team of the group whose ATTACK strength is being evaluated, so those callers match
the helper's K-Mod-1200 contract. CvUnitAI's NULL-plot caller likewise uses its actual
team as attacker.

This explains why the earlier queue-041 history note saying the current helper/caller
lineage was coherent was valid for that file's attacking-group call sites yet did not
close this CvPlayerAI cross-file mode. No prior album root owns the AI_localAttackStrength
argument-role mismatch. F270 is therefore independent rather than a rediscovery.

REPAIR DIRECTION
----------------
Do not blindly replace the second helper argument with a single hard-coded team. This
method supports two materially different modes:

    * default/hostile mode: attackers are enemy team(s), evaluator's team is defender;
    * explicit own-offense mode: eAttackTeam can equal getTeam(), while the target plot's
      opposing team is the defender.

The repair should derive the helper's attacking and defending roles from `eAttackTeam`
and the target context, e.g. conceptually:

    attacker = eAttackTeam (NO_TEAM is acceptable for generic multiple-hostile mode)
    defender = known target/evaluator defender when one can be identified

and pass both explicitly. In the common NO_TEAM hostile-to-us mode, supplying
`eDefenseTeam = getTeam()` is important so the resistance sample is restricted to the
actual defending side even though no single enemy attack team is known. In a known
attacker-vs-owned-target case, the plot/evaluator team is defender; in an own-attack
against an enemy-owned target, the target plot team is defender.

Any patch should re-test both categories rather than merely changing `getTeam()` to
`eAttackTeam`, because the helper's optional defender parameter also controls city
bonus and collateral-resistance sampling.

SEVERITY / CONFIDENCE
---------------------
Confidence: HIGH.
Severity: gameplay-relevant AI valuation bug; magnitude situational but high-fanout.
The helper itself is approximately modeling collateral, but the attacker/defender role
mismatch is an argument-contract error independent of approximation quality.


OPENING CONTROLS / NON-FINDINGS SINCE WIP01
===========================================

A. contestedPlots coordinate dedup key — CONTROL
------------------------------------------------
`contestedPlots` uses `x * 1000 + y` as a local dedup key. This would collide if the
supported Y dimension reached 1000, but current shipped/custom SAS map dimensions are
far below that regime (current XML maxima inspected are ~99x70 before any normal map
script scaling). No supported-map collision was established. Keep as a theoretical
future-proofing concern, not F271.

B. estimateDirection zero displacement — CONTROL
-------------------------------------------------
The helper returns NO_DIRECTION/-1 for zero displacement. For every nonzero displacement
one of the eight direction dot products is positive, so no invalid "best direction"
state was found. No root.

C. intHash signed-overflow / capital salt — CONTROL FOR NOW
-----------------------------------------------------------
`intHash` intentionally folds inputs with multiplier 31 and optionally salts by the
player capital plot. The capital term is documented as range diversification and its
callers use the result as deterministic pseudo-random material. Although signed-overflow
portability is theoretically undesirable under modern C++ rules, the target Civ4 MSVC
runtime has stable wrap-style behavior and no supported current cross-build/OOS failure
was proved. Do not promote merely on language-lawyer UB.

D. BC fractional-year conversion — STRONG LOW-SEVERITY CANDIDATE, NOT YET NUMBERED
-----------------------------------------------------------------------------------
`getTurnYearForGame` currently returns:

    getTurnMonthForGame(...) / max(1, GC.getNumMonthInfos())

For negative month counts not divisible by 12, C++ integer division truncates toward
zero instead of flooring. Example with 12 months:

    -48000 months -> -4000
    -47999 months -> -3999   // C++ truncation, but still February 4000 BC

AdvCiv practical 4317 explicitly fixed the sibling negative-modulo problem in
CvGameTextMgr::setDateStr using `intdiv::umodulo`, noting ordinary `%` fails for BC
calendar dates; the year division was left unchanged. K-Mod and Civ4CE also retain
ordinary division.

Current shipped CALENDAR_DEFAULT game-speed BC increments were checked and are divisible
by 12 up to AD, so normal default games do not reproduce this. However Civ4 supports
CALENDAR_MONTHS/SEASONS/WEEKS, WorldBuilder Calendar/StartYear scenarios and custom
increment XML, where fractional-year BC dates are supported. Preserve this for final
queue-039 disposition; it may warrant a low-severity F271 if the supported scenario API
is judged sufficient, but do not promote merely because the arithmetic is theoretically
wrong outside current default speed data.


C022-WIP02 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    21 / 169

CURRENT FILE:
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp -- OPEN

QUEUE-039 ROOTS SO FAR:
    F270 / provisional KI#593

NEXT ROOT:
    F271 UNUSED
    next provisional KI#594

Next exact cursor:
    * finish estimateCollateralWeight caller/population audit, especially NO_TEAM and
      neutral/hidden-nationality/coexistence cases without duplicating F270;
    * finish PUF combat-owner/hidden-nationality caller semantics;
    * decide final status of supported fractional-BC year conversion;
    * continue intHash/scaled deterministic RNG and calendar boundaries;
    * compare any surviving inherited helper candidate against K-Mod/Civ4CE and current
      callers before allocating F271.

No repository source files modified. Audit album only.
===============================================================================


===============================================================================
C022-WIP03 — QUEUE 039 CvGameCoreUtils.cpp / CALENDAR + PUF PASS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol
Status: UNFINISHED WIP; queue 039 remains OPEN.
Repository source files modified: NONE.

F271 / provisional KI#594
CIV4/K-MOD/ADVCIV-INHERITED BUG — getTurnYearForGame truncates negative fractional
BC years toward zero instead of flooring them
--------------------------------------------------------------------------------
STATUS: CONFIRMED (LOW SEVERITY, SUPPORTED NONDEFAULT CALENDARS / SCENARIOS).
OWNING FILE: CvGameCoreDLL/CvGameCoreUtils.cpp.

CURRENT CODE
------------
Current SAS returns:

    getTurnMonthForGame(iGameTurn, iStartYear, eCalendar, eSpeed) /
        max(1, GC.getNumMonthInfos())

from getTurnYearForGame.

For positive dates ordinary integer division gives the desired containing year. For a
negative month count that is not exactly divisible by the number of months in a year,
C++ integer division truncates toward zero rather than toward negative infinity.

With 12 months, for example:

    -48000 / 12 = -4000                     // January 4000 BC, fine
    -47999 / 12 = -3999 in C++             // February is still in 4000 BC

The most obvious era-boundary example is:

    turn-month -1  -> C++ year 0

CvGameTextMgr::setYearStr deliberately represents year 0 as 1 AD (there is no displayed
year zero). Under CALENDAR_MONTHS this can therefore label the month immediately before
the era boundary as "December, 1 AD", followed by "January, 1 AD" one turn later,
instead of December 1 BC followed by January 1 AD.

SUPPORTED INPUT / NOT MERELY HYPOTHETICAL XML
----------------------------------------------
This reproducer does not require an invented calendar enum:

    CALENDAR_DEFAULT
    CALENDAR_BI_YEARLY
    CALENDAR_YEARS
    CALENDAR_TURNS
    CALENDAR_SEASONS
    CALENDAR_MONTHS
    CALENDAR_WEEKS

are first-class CalendarTypes in CvEnums and exposed through CyEnumsInterface/Python.
WorldBuilder scenario serialization explicitly writes and reads both:

    Calendar=<calendar type>
    StartYear=<integer>

and CvGame/CvInitCore expose the selected calendar/start year. Thus a BC scenario using
CALENDAR_MONTHS, CALENDAR_SEASONS or CALENDAR_WEEKS is a supported Civ4 configuration,
not just malformed mod data.

Current shipped SAS CALENDAR_DEFAULT game-speed increments were separately checked:
the BC increments currently in the normal game-speed XML are multiples of 12 until AD,
so ordinary default games avoid fractional BC years and do not normally display the bug.
That bounds severity but does not invalidate the supported scenario/nondefault-calendar
path.

DOWNSTREAM EFFECTS
------------------
1. Date/year display and save-format text
   CvGameTextMgr::setYearStr calls getTurnYearForGame. CALENDAR_MONTHS, SEASONS and
   WEEKS then combine that wrong year with their month/season/week label.

2. Public game/Python API
   CvGame::getGameTurnYear -> getTurnYearForGame, and CyGame exposes getGameTurnYear.
   Scenario scripts therefore receive the same off-by-one BC year for fractional years.

3. Logs / diagnostics
   SASGameRecordLog, BBAILog, CitySiteEvaluator and other reporting paths record
   getGameTurnYear and therefore inherit the wrong BC year.

4. Small real gameplay timing effect
   CvCity::setNumRealBuilding stores GC.getGame().getGameTurnYear as the building's
   original time. CvCity::getBuildingCommerceByBuilding later compares the current
   getGameTurnYear against that stored year for CommerceChangeDoubleTime (1000 years in
   current building XML for applicable culture commerce). A building constructed during
   a fractional BC year can therefore have the year-based doubling threshold shifted by
   one calendar year in such a scenario. This is small compared with the display/API
   defect but proves that the helper is not presentation-only.

HISTORY / CONTROL
-----------------
Civ4CE's BtS source and K-Mod 1.46 both retain the same ordinary `/ numMonths` year
conversion. Base AdvCiv 1.14 likewise retains it (with only a divisor max guard).
Current SAS inherits the same behavior.

The diagnosis is strengthened by AdvCiv practical 4317, "Fix month display for BC game
dates." That fix explicitly notes that ordinary C++ `%` "doesn't work for negative
dates, i.e. BC" and replaces the month-index modulo in CvGameTextMgr with
`intdiv::umodulo`. The mathematically paired year quotient was not changed, leaving the
same negative-number issue in division form.

REPAIR DIRECTION
----------------
Use floor division by the positive number of months rather than C++ truncation. The
existing positive-remainder helper makes the relation explicit:

    n = max(1, GC.getNumMonthInfos())
    m = getTurnMonthForGame(...)
    year = (m - intdiv::umodulo(m, n)) / n

For nonnegative m this is equivalent to current division. For negative fractional years
it returns the containing BC year. A dedicated signed floor-div helper would also be
reasonable if useful elsewhere.

Regression tests should include at least:

    * CALENDAR_MONTHS, StartYear=-1: Jan through Dec 1 BC, then Jan 1 AD;
    * a much earlier negative year such as -4000 with turn-month +1;
    * positive/AD dates unchanged;
    * CALENDAR_DEFAULT current shipped speeds unchanged.

SEVERITY / CONFIDENCE
---------------------
Confidence: HIGH.
Severity: LOW overall because shipped default game speeds avoid fractional BC years;
real correctness bug for supported nondefault calendars/scenarios, public year API and
small year-based building-commerce timing.


POST-F270 COLLATERAL POPULATION CONTROL
=======================================
After separating F270's definite caller role mismatch, the helper's broader
`eDefenseTeam = NO_TEAM` population behavior was challenged independently.

When no defender team is supplied, estimateCollateralWeight counts canDefend units that
are not on the supplied attack team. In a plot shared with a neutral third party this
can sample a unit that actual CvUnit::collateralCombat would not necessarily hit, since
actual victim selection uses unit-level isEnemy. However:

    * the helper explicitly documents itself as a rough estimate;
    * multiple allied enemy teams can legitimately contribute collateral victims;
    * hidden-nationality / always-hostile combat can make simple diplomatic team filters
      misleading;
    * callers lacking an explicit eDefenseTeam intentionally use the generic mode.

No independent hard contract comparable to F270's wrong attacker argument was proved.
Do not split this into F272 merely because a neutral-coexistence edge can make the rough
sample imperfect. A future improvement could pass explicit defender information where
known and/or use attacker-relative combat-enemy semantics, but that is tuning unless a
specific caller invariant is established.


PUF REFACTOR PASS — CURRENT CONTROL STATE
=========================================
The high-fanout PUF enemy/combat-owner helpers were compared against K-Mod and the
AdvCiv practical-2731 refactor. So far the apparently unusual cases are deliberate:

    * PUF_isPotentialEnemy / PUF_isEnemy use the querying player's team as `eOurTeam`
      and pUnit->getCombatOwner(eOurTeam), matching hidden-nationality/always-hostile
      perspective semantics;
    * PUF_canDeclareWar asks whether the querying player can declare on the unit's
      combat owner; AdvCiv's swapped variable names corrected the readability of the
      inherited K-Mod orientation rather than reversing behavior;
    * canCoexistWithEnemyUnit exclusions are intentional for spies/coexisting units;
    * PUF_isPlayer's viewer-aware hidden-nationality/invisibility branch is part of
      advc.061 and has no current NO_PLAYER caller that violates its refactored enum
      assertion contract.

No PUF root has crossed F272 yet. Continue caller-specific checks rather than treating
hidden-nationality complexity itself as a defect.


C022-WIP03 CURSOR
-----------------
COMPLETED FIRST-PASS FILES:
    21 / 169

CURRENT FILE:
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp -- OPEN

QUEUE-039 ROOTS SO FAR:
    F270 / provisional KI#593
    F271 / provisional KI#594

NEXT ROOT:
    F272 UNUSED
    next provisional KI#595

Next exact cursor:
    * continue PUF caller-specific hidden-nationality/coexistence checks;
    * inspect PUF_isEnemyCityAttacker transport/cargo proxy against its city-safety and
      city-trade consumers without confusing deliberate fog-of-war conservatism with a
      hard bug;
    * finish intHash / scaled hash caller and compiler-determinism history;
    * finish remaining calendar boundary arithmetic beyond the now-owned F271 root;
    * compare any candidate with K-Mod/Civ4CE and album duplicates before F272.

No repository source files modified. Audit album only.
===============================================================================


C022-WIP04 CONTINUATION — QUEUE 039 CONTROL FREEZE
====================================================
Status: queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp remains OPEN.
Next unused root: F272. Next provisional KI: KI#595.

PUF / TRANSPORT / STRING-HELPER CONTROLS FROZEN
-----------------------------------------------
1. PUF callback argument-type audit:
   Despite generic int callback slots and some legacy/misleading names, traced live callers
   supply the semantic type expected by each predicate. In particular,
   PUF_isOtherTeam expects a PlayerTypes value and callers pass players; PUF_isCombatTeam
   expects TeamTypes and callers pass teams. No live player/team reinterpretation root was
   established.

2. PUF_isEnemyCityAttacker transport proxy:
   Its treatment of generic cargo transports as potential city threats is longstanding and
   intentional-looking, not a hard defect. The helper was introduced to classify units that
   can endanger a city, and cargo vessels can carry city attackers even when the hull itself
   cannot attack cities. Current consumers are conservative city-safety/trade gates. Do not
   make F272 merely from empty transports satisfying this proxy without an independent
   contract contradiction.

3. eAssumePeaceTeam hidden-nationality edge:
   The hypothetical-peace parameter could deserve care with always-hostile/hidden-nationality
   combat ownership, but every live isVisibleEnemyCityAttacker caller currently passes
   NO_TEAM. This is dormant in current SAS and is not F272.

4. setListHelp(NULL, ...) concern:
   The live NULL-start calls resolve to the CvWStringBuffer overload. CvWStringBuffer::append
   explicitly ignores a NULL wchar pointer. No current NULL-start call into the less-tolerant
   CvWString overload was found. This is safe by current caller/overload contract.

5. Debug enum/string helpers:
   Preliminary automated enum-vs-switch comparison suggests some MissionTypes/MissionAITypes
   lack dedicated symbolic names, but these diagnostic helpers have numeric UNKNOWN fallbacks.
   This remains under challenge; omission alone is not a gameplay/root finding.

No repository source files modified. Audit album only.


===============================================================================
C022-WIP05 — QUEUE 039 CvGameCoreUtils.cpp / SATURATION CONTROLS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

STATUS:
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp -- OPEN
    F270 / provisional KI#593 -- LIVE
    F271 / provisional KI#594 -- LIVE
    F272 UNUSED
    next provisional KI: KI#595

CONTROLS / NON-FINDINGS ADDED SINCE WIP04
=========================================

A. `handicapFromDifficulty` exact-midpoint tie -- CONTROL
---------------------------------------------------------
The SAS helper scans handicap XML in enum order and updates only on strictly smaller
absolute distance. Therefore an exact midpoint between two current scores chooses the
earlier/lower entry; e.g. Noble 50 + Prince 58 average to 54 and map to Noble.

This differs from some pre-SAS averaging callers, but there is no single inherited tie
contract to preserve:

  * AdvCiv `CvGame::initGameHandicap` averaged enum-derived difficulty and used
    `intdiv::round`, so an exact adjacent .5 generally rounded upward;
  * AdvCiv `CvGame::updateAIHandicap` averaged enum IDs with `intdiv::uround`, likewise
    generally choosing the upper adjacent enum on a .5;
  * AdvCiv `CvTeam::getHandicapType`, however, deliberately used integer division and
    therefore chose the lower entry for the comparable midpoint.

SAS commit 5970 replaced these assumptions because Rookie/Deity+ and XML `iDifficulty`
scores make enum-index arithmetic invalid. "Nearest XML score, earlier/lower on an exact
tie" is internally coherent, and the old callers themselves disagreed about tie direction.
Do not promote F272 merely from the changed tie-break outcome.

B. live-unit selection-group assumptions in PUF helpers -- CONTROL
-------------------------------------------------------------------
`PUF_isAirIntercept`, `PUF_isMissionAIType`, `PUF_isGroupHead` and the group-head helper
family dereference a unit selection group without local NULL guards. This initially looks
fragile because `CvUnit::getGroup()` can technically return NULL for an invalid group ID.

Lifecycle tracing does not establish a live bug:

  * normal live units are attached/re-attached to selection groups;
  * `CvUnit::isGroupHead()` itself unconditionally dereferences `getGroup()`, making group
    existence a broad core live-unit invariant rather than a special PUF assumption;
  * the deliberate `joinGroup(NULL, false, false)` no-rejoin path occurs during
    `CvUnit::kill`, after the unit is removed from the map and immediately before
    `unitLost`/deletion, so normal plot-count PUF callers cannot encounter that detached
    dying unit as a live plot unit.

No transient live caller violating the invariant was proved. Keep as contract control.

C. `PUF_isFiniteRangeAndNotJustProduced` uses `> 1` -- CONTROL
-------------------------------------------------------------
Despite the helper name, it excludes units whose creation turn is either the current turn
or the immediately preceding game turn. The sole current gameplay use is the inherited
BBAI/K-Mod transport-pickup valuation, whose own comment says it wants units that "have
had a chance to move since being built." The condition is inherited together with that
caller. Without proving the intended turn-order window is only one global turn, the name
alone is insufficient evidence of an off-by-one defect. Do not allocate F272 on this basis.

D. `setListHelp` integer-list-ID sentinel -- CONTROL
---------------------------------------------------
The int-reference overload treats `iLastListID == iListID` as a continuation and otherwise
emits `szStart`. Many callers initialize `iLast = 0`, which would be wrong if the first
actual list ID could also be zero.

All current caller families inspected either explicitly skip zero-valued modifiers before
calling the helper or sit inside an outer nonzero-value condition. Negative nonzero values
also correctly differ from the zero sentinel. Thus current first-item formatting is safe;
no uninitialized/zero-key caller was found.

E. hotkey description mapping -- CONTROL
----------------------------------------
The legacy mapping declares `iNumKeyBoardMappings = 108` and contains exactly 108 mapping
initializers. The longest `KB_*` token is well below the fixed 25-TCHAR storage capacity.
A scan of all current SAS XML found 49 distinct nonempty `<HotKey>` codes, and every one is
present in the mapping. No current hotkey-description omission or array-count mismatch.

F. `applyColorToString` self-format alias -- CONTROL
----------------------------------------------------
The helper calls `s.Format(..., s.GetCString())`. `CvWString::Format` first formats into a
separate local `std::wstring result` and only assigns `*this = result` afterward, so the
source pointer is not invalidated before formatting consumes it. No self-alias corruption.

G. debug Mission string omission -- DIAGNOSTIC CLEANUP, NOT F272
----------------------------------------------------------------
Current `getMissionTypeString` lacks an explicit `MISSION_RANGE_ATTACK` case while that
mission is live (`CvUnitAI::AI_rangeAttack` queues it). Debug plot help therefore falls
back to `UNKOWN_MISSION(<numeric id>)` rather than the symbolic mission name. The numeric
fallback remains unique and no gameplay state depends on the string. K-Mod, AdvCiv and
Civ4CE carry the same omission. Useful cleanup for diagnostics, but below the current root
bar unless an independent diagnostic failure is demonstrated.

H. `intHash` / coordinate-key controls remain unchanged
-------------------------------------------------------
No supported deterministic OOS failure was established from signed 32-bit polynomial
wrap on the fixed legacy MSVC/Win32 target, and `contestedPlots`' x*1000+y key remains far
outside the collision regime for all shipped SAS world sizes. Both remain future-proofing
notes rather than live roots.

CURRENT ASSESSMENT
==================
The inherited utility/PUF body has now had argument types, hidden-nationality ownership,
group lifecycle, list formatting, hotkey data, direction helpers, hash callers, calendar
boundaries, and K-Mod/Civ4CE cognates challenged. The late pass is increasingly yielding
controls rather than uncovered state/consumer contradictions.

F272 remains deliberately UNUSED.

Next cursor:
    * finish SAS victory-helper evolution / caller-unit consistency;
    * finish Civ4CE explicit-fix divergence scan for surviving utility helpers;
    * re-check queue-039 live roots F270/F271 for overlap/repair bounds;
    * if no independent hard root emerges, perform a closure/saturation pass rather than
      manufacturing F272.

No repository source files modified. Audit album only.
===============================================================================


===============================================================================
C022-WIP06 — QUEUE 039 CvGameCoreUtils.cpp / FINAL SATURATION CONTROLS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

STATUS:
    queue 039 CvGameCoreDLL/CvGameCoreUtils.cpp -- OPEN, closure challenge underway
    F270 / provisional KI#593 -- LIVE
    F271 / provisional KI#594 -- LIVE
    F272 UNUSED
    next provisional KI: KI#595

ADDITIONAL CONTROLS / CROSS-CHECKS
==================================

I. SAS victory-denial helper evolution and callers -- CONTROL
--------------------------------------------------------------
The post-6037 helper chain was traced through the current callers rather than assuming that
later refactoring kept all thresholds aligned.

`getSASVictoryStageLevel` itself correctly checks cumulative victory bits from stage 4 down
to stage 1. This strengthens the earlier F250 boundary: F250 belongs to InvasionGraph's
separate ancient local macro, not this shared helper.

The team-level stage, spaceship-parts and stage-3-Space helpers are internally coherent for
current SAS XML:
    * the current Space component maxima total 16 parts;
    * current VictoryThreshold values match the component maxima for the complete ship;
    * lower VictoryMinThreshold values intentionally permit an incomplete launch, whereas
      the stage-3 emergency heuristic explicitly measures progress toward the full ship
      (the source comment itself reasons in terms of 8/16 parts).

`getSASVictoryDelayTurnsFromNormalGameSpeed` intentionally does not cache the game-speed
result. Current victory-denial Normal-speed countdown defines are 20 turns, and every
shipped VictoryDelayPercent maps 20 to an exact integer, so its nearest-integer scaling and
`CvGame::victoryDelay`'s truncating arithmetic do not currently diverge on those gates.

`isSASUWAIVictoryDenialPeaceThreat` returns early before writing optional diagnostic output
pointers when the feature is globally disabled. The current `CvDeal` caller is safe:
    * disabled => both team calls return false and the deal helper returns before reading
      any output variables;
    * enabled => both calls populate their outputs before the logging path.
No asymmetric enable exists because the define is global/static.

The no-output callers in CvPlayerAI/UWAIAgent consistently ask whether either party is a
configured victory threat. CvTeamAI's older brokered-peace denial block duplicates the same
countdown/stage4/stage3-Space criteria with the same game-speed scaling. No one-sided
threshold/unit drift was found.

II. PUF refactor history / old targeted fixes -- CONTROL
--------------------------------------------------------
AdvCiv practical 2731's large PUF rewrite was re-read as an old->new semantic migration.
The important enemy/potential-enemy/can-declare-war changes make the querying player's role
explicit and add enum contracts; current live callers supply the expected PlayerTypes or
TeamTypes roles. No new polarity/owner regression survived the caller audit.

The AdvCiv practical-1624 change titled "Fixed a minor issue with the
Own-Exclusive-Radius option" was also checked because `contestedPlots` belongs to that
feature family. The behavioral fix is in CvPlot::calculateCulturalOwner (handling newly
expanded zero-culture city radius), not an unexamined mutation of `contestedPlots`.
Therefore it does not expose an adjacent unresolved utility-root here.

III. `PUF_isEnemyCityAttacker` against current cargo XML -- CONTROL
-------------------------------------------------------------------
The historical design treats a generic cargo transport as a conservative city threat even
when the hull itself is not a land attacker. Current SAS unit XML supports that intended
proxy rather than exposing a cargo-domain counterexample:

    generic (`SpecialCargo == NONE`) cargo units with positive cargo space:
        Galley, Galleon, Transport, Privateer
        -> all use DomainCargo = DOMAIN_LAND

    Missile Cruiser, Attack Submarine and Carrier use a non-NONE SpecialCargo and are
    therefore excluded from the helper's generic-transport shortcut.

Thus the shortcut does not currently classify an air-only/missile-only special transport as
an invasion transport. Its empty-transport conservatism remains intentional-looking city
safety/trade policy, not F272.

IV. Civ4CE / K-Mod independent comparison -- CONTROL
----------------------------------------------------
Civ4CE's BtS CvGameCoreUtils retains the legacy calendar quotient behavior later inherited
through K-Mod/AdvCiv, so it corroborates F271 ancestry rather than supplying a missing fix.
Its PUF family is substantially older/simpler than AdvCiv's viewer-aware refactor and does
not expose a CE-only correction that current AdvCiv/SAS lacks.

The K-Mod->AdvCiv history was separately reviewed around the high-value utility changes:
    * K-Mod 448 introduces collateral weighting;
    * K-Mod 1200 explicitly defines the helper's first team argument as the collateral
      attacking team, strengthening F270's wrong-role caller proof;
    * later AdvCiv PUF and list-help refactors preserve the currently audited semantics.
No additional K-Mod correction was found stranded on the other side of an AdvCiv rewrite.

V. remaining helper inventory -- SATURATION CONTROL
----------------------------------------------------
The current file has now been covered function-family by function-family:
    * contestedPlots / Own-Exclusive-Radius consumers;
    * color/string and direction helpers;
    * handicap XML-score mapping;
    * hotkey mapping against current XML;
    * SAS diagnostic enum-name helpers;
    * victory-stage / spaceship / countdown helpers and all behavior callers;
    * estimateCollateralWeight and all important caller roles;
    * setTradeItem / all setListHelp overload contracts;
    * PUF ownership, combat-owner, hostility, visibility, group, mission and cargo helpers;
    * baseYieldToSymbol (unused);
    * intHash determinism/overflow context;
    * calendar month/year conversion;
    * debug Direction/Activity/Mission/MissionAI/UnitAI strings.

Late candidates that remain below the root bar include:
    * contestedPlots' `x * 1000 + y` future-proofing key: no collision on shipped SAS map
      dimensions;
    * signed `intHash` overflow: formally non-portable C++, but deterministic under the
      fixed legacy MSVC/Win32 target and no current cross-compiler/OOS consumer proved;
    * missing MISSION_RANGE_ATTACK symbolic debug name: numeric UNKNOWN fallback preserves
      identity and no gameplay state depends on it;
    * exact handicap-score midpoint chooses the lower XML entry: coherent tie policy, and
      inherited pre-SAS callers themselves disagreed on tie direction;
    * live-unit selection-group dereferences in PUFs: supported by a system-wide live-unit
      group invariant; the no-rejoin NULL transition occurs only during unit destruction;
    * `PUF_isFiniteRangeAndNotJustProduced`'s >1 age: inherited together with a caller that
      explicitly wants units to have had a chance to move;
    * setListHelp integer ID zero sentinel: every current caller suppresses zero-valued
      entries before invoking it;
    * applyColorToString self-formatting: CvWString::Format uses a separate temporary result;
    * generic collateral population approximations beyond F270: rough-estimate policy with
      no additional caller invariant violation established.

F272 remains deliberately UNUSED. Next step is a final F270/F271 independence and repair-
boundary review. If that passes and no omitted current function family remains, queue 039
is a good candidate for FIRST-PASS COMPLETE without manufacturing a third root.

No repository source files modified. Audit album only.
===============================================================================


===============================================================================
C023 DURABLE CHECKPOINT — QUEUE 039 CvGameCoreUtils.cpp FIRST-PASS COMPLETE
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

DURABLE STATUS
--------------
Queue 039:
    CvGameCoreDLL/CvGameCoreUtils.cpp
    FIRST-PASS COMPLETE

COMPLETED FIRST-PASS FILES:
    22 / 169

LIVE ROOTS OWNED / ESTABLISHED DURING QUEUE 039:
    F270 / provisional KI#593
    F271 / provisional KI#594

NEXT ROOT NUMBER:
    F272 UNUSED
    next provisional KI#595

No F272 was allocated merely to increase the finding count.

FINAL ROOT INDEPENDENCE / REPAIR-BOUNDARY REVIEW
=================================================

F270 / KI#593 -- collateral attacker-team role in AI_localAttackStrength
-----------------------------------------------------------------------
Root:
    `estimateCollateralWeight` defines its first team argument as the team dealing
    collateral damage. `CvPlayerAI::AI_localAttackStrength`, however, hardwires the
    evaluating player's `getTeam()` into that role while the actual candidate attackers
    are selected by `eAttackTeam` (or by all teams currently at war when eAttackTeam is
    NO_TEAM).

Why this is independently live:
    * the helper uses the supplied attack team in defender-bonus context and when
      excluding the attacking side from the resistance sample;
    * explicit enemy-team calls therefore evaluate hostile collateral using the defender's
      team identity;
    * explicit own-team offense calls and default all-hostiles calls demonstrate that this
      cannot be repaired safely by blindly swapping one hardcoded team for another.

History:
    K-Mod 448 introduced collateral weighting into local attack-strength valuation.
    K-Mod 1200 made the helper contract explicit as attacking team first, defender team
    optional, while the local-attack-strength caller retained the evaluator-team argument.
    The mismatch survives K-Mod -> AdvCiv -> SAS. Selection-group callers are not affected
    by the same root because there `getTeam()` genuinely is the attacking group team.

Repair boundary:
    * when `eAttackTeam` is explicit, collateral attacker semantics must use that team;
    * when AI_localAttackStrength is aggregating all hostile teams (`NO_TEAM`), there is no
      single truthful attack-team identity, so use an explicitly generic/per-team policy
      rather than silently treating the evaluator as the attacker;
    * defender-team sampling must likewise follow whether the evaluator is defending the
      target, attacking it, or merely estimating nearby hostile strength.
Do not mechanically replace `getTeam()` with `eAttackTeam` in every mode without preserving
these caller semantics.

F271 / KI#594 -- negative fractional BC year division
-----------------------------------------------------
Root:
    `getTurnMonthForGame` can produce negative month counts not divisible by the number of
    months per year for supported fractional-year calendars. `getTurnYearForGame` converts
    months to years with ordinary signed C++ integer division, which truncates toward zero
    rather than flooring the calendar year.

Concrete supported example:
    With a monthly calendar around the BC/AD boundary, turn-month -1 represents the final
    month of 1 BC. `-1 / 12` becomes 0 in C++, and the year formatter intentionally renders
    year 0 as 1 AD. The chronology can therefore display a month of 1 AD before January
    1 AD. CALENDAR_MONTHS, CALENDAR_SEASONS and CALENDAR_WEEKS are first-class Civ4
    calendar modes and WorldBuilder scenarios serialize Calendar/StartYear, so this is not
    merely an impossible custom-mod input.

Non-display consumer:
    `CvGame::getGameTurnYear()` feeds the building-age comparison used for the 1000-year
    building commerce/culture doubling mechanic, so a fractional BC scenario can also shift
    that threshold by a calendar year.

History:
    Civ4/K-Mod/AdvCiv and Civ4CE retain the truncating quotient. AdvCiv separately repaired
    the sibling negative `%` problem in `CvGameTextMgr::setDateStr` by introducing
    `intdiv::umodulo`, but the quotient half remained unchanged.

Repair boundary:
    Let `m = getTurnMonthForGame(...)` and `n = max(1, GC.getNumMonthInfos())`.
    A clean integer floor quotient is:

        (m - intdiv::umodulo(m, n)) / n

    For nonnegative and exact-year dates this is identical to existing behavior. For a
    negative fractional year it subtracts the positive calendar remainder before exact
    division, giving the mathematically correct floor year.

F270 vs F271:
    DISTINCT / INDEPENDENT.
    F270 is a combat-AI caller-role bug in collateral valuation. F271 is calendar arithmetic
    used by date/API/gameplay year consumers. Either reproduces with the other subsystem
    absent; their repairs share no state or contract.

SATURATION / COMPLETE RATIONALE
===============================
Queue 039 received a full high-fanout utility audit rather than a local syntax scan:

    * current SAS-vs-Base AdvCiv semantic delta was bounded;
    * K-Mod introducing history and Civ4CE's independent BtS implementation were used as
      controls where relevant;
    * all current helper families were inventoried and caller-traced;
    * collateral weighting was traced through both the helper and every meaningful caller
      role, revealing F270 while preventing duplicate attribution from queue 041;
    * calendar month/year arithmetic was tested numerically across the BC/AD boundary and
      through non-display consumers, establishing F271;
    * PUF player/team/combat-owner/hidden-nationality/visibility/group/mission contracts
      were checked against actual caller argument types and live-unit lifecycle;
    * generic transport threat semantics were checked both historically and against current
      SAS cargo XML;
    * setListHelp overloads were checked against NULL handling and integer-list sentinels;
    * intHash/compiler determinism, map-coordinate dedup keys, direction helpers, hotkey
      mapping, color formatting and diagnostic enum strings were challenged;
    * SAS victory-stage/spaceship/countdown helpers were followed through their commit
      evolution and behavior callers, including duplicated peace-denial logic;
    * attractive edge cases were retained as explicit controls when they failed to establish
      a current producer -> state -> consumer contradiction.

Late convergence produced controls rather than unexplored code. No independent third root
survived the final history/caller/invariant pass. Queue 039 therefore meets the album's
FIRST-PASS COMPLETE standard without consuming F272.

NEXT PRIORITY FILE
==================
The next remaining priority-marked file in the reconciled queue is:

    queue 040
    CvGameCoreDLL/CvGlobals.cpp
    approximately 1,454 lines in the album snapshot

Later priority-marked files include queue 042 CvDeal.cpp, queue 037 CvPythonCaller.cpp,
queue 045 CvMapGenerator.cpp, queue 054 GroupPathFinder.cpp and queue 092 BBAILog.cpp;
queue 031 UWAICache.cpp also remains separately OPEN from its earlier checkpoint.

Exact next cursor:
    begin queue 040 CvGameCoreDLL/CvGlobals.cpp
    from F272 UNUSED / provisional KI#595 next.

Recommended opening method:
    1. bound SAS-vs-Base AdvCiv changes and path history;
    2. classify global ownership/lifetime, XML-info registration, cached define/info-type
       lookups and DLL init/reset contracts before judging individual getters;
    3. prioritize stale static/cache state across game reloads, index/type mismatches,
       allocation/reset symmetry and data-driven array bounds;
    4. compare K-Mod/Civ4CE where their global-container lifecycle can falsify a candidate;
    5. preserve performance/style cleanup as controls unless caller-visible state or safety
       failure is proved.

No repository source files modified. Audit album only.
===============================================================================


===============================================================================
C023-WIP01 — QUEUE 040 CvGlobals.cpp OPENING LIFETIME / TYPE-CONTRACT PASS
===============================================================================
Date: 2026-08-28
Model: GPT-5.6-Sol

QUEUE STATUS
------------
queue 040
CvGameCoreDLL/CvGlobals.cpp
OPEN

NEXT ROOT:
    F272 UNUSED
    next provisional KI#595

No repository source files modified. Audit album only.

I. SAS-vs-Base AdvCiv semantic delta — BOUNDED / CONTROL
--------------------------------------------------------
Normalized current SAS vs Base AdvCiv 1.14 comparison is very small. Current SAS mainly:
    * reformats several signatures/comments;
    * replaces terse missing-GlobalDefine assertions with richer diagnostics;
    * retains the Base AdvCiv global/cache/info-array implementation otherwise.

This makes inherited CvGlobals lifetime, cache and XML registration contracts the primary
queue-040 target rather than a broad SAS rewrite.

II. SetGlobalDefines -> cacheGlobals -> m_pPythonCaller — OPEN LIFECYCLE CANDIDATE
----------------------------------------------------------------------------------
CvXMLLoadUtility::SetGlobalDefines() finishes with:

    GC.cacheGlobals();

CvGlobals::cacheGlobals() performs full integer/float cache initialization and then:

    m_pPythonCaller = new CvPythonCaller();
    CvEventReporter::getInstance().initPythonCallbackGuards();

m_pPythonCaller is deleted in CvGlobals::uninit(), but cacheGlobals() itself does not first
free an existing object.

Important narrowing:
    * K-Mod/Civ4CE also called cacheGlobals() from runtime define setters;
    * AdvCiv intentionally replaced those full recaches with targeted cacheGlobalInts /
      cacheGlobalFloats updates before introducing/retaining the Python-caller allocation;
    * therefore current runtime setDefineINT/FLOAT does NOT itself repeat the allocation.

The remaining question is whether the EXE can invoke SetGlobalDefines() multiple times in a
single CvGlobals lifetime (e.g. supported XML reload). That call is external to the DLL-side
source available here. Without proving repeat execution, this is not F272.

III. info/type-map reset orchestration — CONTROL / EXTERNAL CONTRACT
-------------------------------------------------------------------
CleanUpGlobalVariables() calls deleteInfoArrays(), which deletes info objects/string arrays
and clears m_typesMap through clearTypesMap(). m_infosMap is instead cleared by the separately
exported CvGlobals::infoTypeFromStringReset().

The latter contains the inherited diagnostic:

    FErrorMsg("Just to see if and when CvGlobals::infoTypeFromStringReset is ever called");

This looks awkward, but the function is DllExport and therefore can be part of the EXE's XML
reload orchestration. A stale removed-info mapping would be real if arrays were reloaded
without this callback, but the available DLL source cannot establish that omission. Keep as
an external-lifecycle control; do not infer F272 from absence of an in-DLL caller.

IV. CvAgents ownership — REAL CLEANUP OMISSION, CURRENT IMPACT NOT YET PROVED
----------------------------------------------------------------------------
CvGlobals::init() allocates:

    m_agents = new CvAgents(MAX_PLAYERS, MAX_TEAMS);

CvGlobals::uninit() frees CvPlayer/CvTeam statics but never deletes m_agents. The CvAgents
object therefore remains allocated and retains vectors of pointers to player/team instances
that have just been freed.

This is a genuine ownership asymmetry, but no supported same-DLL init->uninit->init cycle has
yet been established. If uninit occurs only at process/DLL shutdown, the OS reclaims the
allocation and no later consumer dereferences the stale cache. Keep below gameplay-root bar
unless a re-init path is proved.

V. getDirectionTypes indexed overload — INHERITED TYPE-SIGNATURE MISMATCH / LATENT CONTROL
-----------------------------------------------------------------------------------------
Neighboring accessors are:

    getAutomateTypes(AutomateTypes e)
    getDirectionTypes(AutomateTypes e)

The second clearly ought, by semantic type, to take DirectionTypes. This exact copy/paste
signature exists in Civ4CE and K-Mod and survives Base AdvCiv/SAS.

However:
    * current DLL source uses only the pointer-returning getDirectionTypes() overload when
      loading/deleting the array;
    * no indexed current DLL caller was found;
    * K-Mod/Civ4CE exported the indexed overload, whereas current AdvCiv/SAS no longer marks
      it DllExport.

So the signature is incorrect as an API contract but no current caller-visible failure has
been proved. Keep as latent cleanup/control, not F272.

VI. define-cache update preliminaries — CONTROL
-----------------------------------------------
ReadGlobalDefines writes directly into FVariableSystem during startup, then cacheGlobals()
initializes the cached arrays. Runtime setDefineINT/FLOAT uses targeted update helpers in
current AdvCiv/SAS, so the old K-Mod/CE full-cache recache behavior no longer causes repeated
m_pPythonCaller allocation.

cacheGlobalFloats' startup recursion through updateCityCamDist is bounded: the outer call
stores the new FIELD_OF_VIEW before updateCityCamDist -> setDefineFLOAT triggers the inner
cacheGlobalFloats call, so the inner pass observes unchanged FoV and does not recurse again.

NEXT CURSOR
===========
Continue queue 040 from F272 unused / KI#595 next:
    * full allocation/deallocation and info-string-array symmetry;
    * cached define enum/update coverage and pre-cache setter assumptions;
    * XML array counts/index invariants and info/type registration;
    * infosReset/display-language lifecycle;
    * independent K-Mod/Civ4CE divergence/fix scan;
    * only return to SetGlobalDefines/m_agents lifecycle if a supported same-DLL reinit path
      is established.
===============================================================================


===============================================================================
C023-WIP02 — QUEUE 040 CvGameCoreDLL/CvGlobals.cpp CONTINUATION
Date: 2026-08-28
Model: GPT-5.6-Sol

QUEUE STATUS
------------
queue 040
CvGameCoreDLL/CvGlobals.cpp
OPEN

NEXT ROOT:
    F272 UNUSED
    next provisional KI#595

No repository source files modified. Audit album only.

VII. FootstepAudio "ByTag" lookup — REJECTED / NAMING CONTROL
-------------------------------------------------------------
CvGlobals::getFootstepAudioTypeByTag(strTag) searches m_paszFootstepAudioTypes rather than
m_paszFootstepAudioTags. The name initially suggests an array mix-up, but shipped XML proves
the current semantics are correct:
    * unit-art AudioRunTypeLoop / AudioRunTypeEnd fields contain tokens such as
      LOOPSTEP_TANK / ENDSTEP_TANK;
    * those tokens are entries in FootstepAudioTypes;
    * the parallel FootstepAudioTags array maps the resulting index to the actual AS3D sound
      script identifier.
So this function is really "type-index from XML type token" despite its confusing name.
No F272.

VIII. AnimationOperator indexed accessor — LATENT TYPE/API MISMATCH, NO LIVE FAILURE PROVED
------------------------------------------------------------------------------------------
AnimationOperatorTypes enum constants start at ANIMOP_FIRST = 1001, while
m_paszAnimationOperatorTypes is a normal 0..count-1 array. Yet
CvGlobals::getAnimationOperatorTypes(AnimationOperatorTypes e) directly indexes that array.
Passing a real ANIMOP_* enum constant would therefore assert/go out of bounds.

However, the live CyGlobalContext wrapper accepts a plain Python integer index and casts that
0..count-1 value to AnimationOperatorTypes before calling the accessor. No current C++/Python
caller was found passing an actual 1001+ ANIMOP_* constant. This is an inherited/domain-type
API defect, but presently latent; do not allocate F272 without a live caller-visible failure.

IX. lazy plot-group FAStar geometry — DORMANT CONTROL
-----------------------------------------------------
CvGlobals::getPlotGroupFinder() lazily initializes an FAStar instance with current map width,
height and wrap flags and does not locally reinitialize it for a later map geometry change.
That would be dangerous if used across regenerated maps.

Current AdvCiv/SAS CvPlotGroup::recalculatePlots, however, has the old plot-group-finder calls
commented out and uses the replacement DFS implementation. No other live DLL caller was found;
the getter itself carries an old diagnostic wondering whether it is called. Therefore the
stale-geometry concern is dormant in current code, not F272.

X. CvXMLLoadUtility raw global pointer — EXTERNAL LIFETIME CONTROL
-----------------------------------------------------------------
AdvCiv stores the current CvXMLLoadUtility* in CvGlobals so optional XML and throne-room XML
can be loaded lazily. CvXMLLoadUtility's destructor contains an explicit comment about the
EXE's unusual ownership/lifetime and warns against deleting the global pointer from the popup
path because that could double-delete.

Normal lazy consumers (CvGame reset optional load; throne-room late load) necessarily rely on
this utility surviving the initial XML phase. Available DLL source does not prove an interval
where the EXE deletes the utility while later retaining/using GC.m_pXMLLoadUtility. Treat this
as an externally-owned raw-pointer seam, not a use-after-free finding without lifecycle proof.

XI. playable-civilization counters and reload — SAME EXTERNAL-RELOAD FAMILY
---------------------------------------------------------------------------
LoadPreMenuGlobals increments m_iNumPlayableCivilizationInfos and
m_iNumAIPlayableCivilizationInfos; constructor initializes them to zero and cleanup does not
locally reset them before a hypothetical second full pre-menu load. That would double counts
on an in-process XML reload. But, like repeated cacheGlobals/PythonCaller allocation, the
available DLL source does not establish a supported second LoadPreMenuGlobals invocation in
one CvGlobals lifetime. Keep under the same external-reload candidate family, not F272.

XII. info-vector/string-array ownership inventory — CLEAN SO FAR
----------------------------------------------------------------
The advc.enum DO_FOR_EACH_INFO_TYPE list drives the ordinary info-vector deletion/reset paths.
CvWorldInfo is intentionally handled separately and is not also present in the macro, so there
is no double deletion there. Manual global string arrays (entity event, animation operator,
function, flavor, art style, city size, contact, diplomacy power, automate, direction,
FootstepAudio types/tags) are separately deleted by deleteInfoArrays(). No current loaded info
family has yet been found omitted from the corresponding cleanup family; Quest remains an
unused/commented legacy case rather than a live missing vector.

XIII. NUM_* macro apparent self-recursion — REJECTED / BUILD-MODE CONTROL
------------------------------------------------------------------------
CvGlobals.h defines, under `#ifndef _USRDLL`, macros such as:

    #define NUM_DIRECTION_TYPES (GC.getNumDirections())

while CvGlobals.cpp defines:

    int CvGlobals::getNumDirections() const { return NUM_DIRECTION_TYPES; }

This looks recursively self-referential in a textual read. It is not recursive in the DLL
build: `_USRDLL` is defined while compiling CvGameCoreDLL, so the compatibility macros are not
defined there and the getter returns the underlying enum constant. The macros exist for the
non-DLL/EXE side of the shared header. No F272.

NEXT CURSOR
===========
Continue queue 040 from F272 unused / KI#595 next:
    * cached GlobalDefines enum/tag coverage and update-before-cache contracts;
    * m_infosMap/type registration collision/index invariants using current XML;
    * array/count and getter bound invariants (including Color replay remapping);
    * independent CE/K-Mod/AdvCiv targeted-fix divergence;
    * remaining init/uninit member-ownership symmetry;
    * only promote external reload/lifetime candidates if a supported re-entry path is proved.
===============================================================================


===============================================================================
C023-WIP03 — QUEUE 040 CvGameCoreDLL/CvGlobals.cpp
F272 / provisional KI#595 CONFIRMED
Date: 2026-08-28
Model: GPT-5.6-Sol

F272 / provisional KI#595 — FOREIGN-REPLAY COLOR REMAP WRAPS BACK INTO THE
"SKIPPED" SYSTEM-COLOR PREFIX
--------------------------------------------------------------------------------
Status: CONFIRMED; inherited AdvCiv feature bug (advc.106i), optional/config-gated.
Primary file: CvGameCoreDLL/CvGlobals.cpp
Same-root consumer/input contract: CvGameCoreDLL/CvReplayInfo.cpp

Current CvGlobals::getColorInfo(ColorTypes eColor) contains an out-of-range replay fallback:

    if (eColor >= getNumColorInfos())
    {
        FAssert(m_bHoFScreenUp || eColor < getNumColorInfos());
        // +7: Skip colors from COLOR_CLEAR to COLOR_LIGHT_GREY
        eColor = (ColorTypes)((eColor + 7) % getNumColorInfos());
    }

The stated invariant is explicit: remapped foreign colors should skip indices 0..6
(COLOR_CLEAR through COLOR_LIGHT_GREY). The arithmetic does not preserve that invariant after
one wrap of the normal color table.

CURRENT SAS COUNTEREXAMPLE
--------------------------
Current CIV4ColorVals.xml contains 128 ColorVal entries:
    0 COLOR_CLEAR
    1 COLOR_ALPHA_GREY
    2 COLOR_WHITE
    3 COLOR_BLACK
    4 COLOR_DARK_GREY
    5 COLOR_GREY
    6 COLOR_LIGHT_GREY
    7 COLOR_RED
    ...

CvReplayInfo::read accepts a foreign-mod player color through:

    checkBounds(info.m_eColor, 0, GC.getNumColorInfos() - 1)

and checkBounds deliberately doubles its upper bound when HOF_DISPLAY_OTHER_MOD_REPLAYS is
enabled. With 128 local colors, foreign replay IDs 0..254 are therefore accepted.

For the accepted high foreign IDs:
    249 -> (249 + 7) % 128 = 0  -> COLOR_CLEAR
    250 -> 1                    -> COLOR_ALPHA_GREY
    251 -> 2                    -> COLOR_WHITE
    252 -> 3                    -> COLOR_BLACK
    253 -> 4                    -> COLOR_DARK_GREY
    254 -> 5                    -> COLOR_GREY

Thus six IDs that the parser intentionally accepts as valid foreign extra colors are mapped
straight into the very prefix that getColorInfo says it is skipping. COLOR_CLEAR is especially
bad for replay/Hall-of-Fame display because the mapped marker/color can become transparent.

PRODUCER -> STATE -> CONSUMER
-----------------------------
1. HOF_DISPLAY_OTHER_MOD_REPLAYS enables permissive loading of replay data produced by another
   mod.
2. CvReplayInfo::checkBounds allows foreign player-color IDs up to twice the local normal upper
   bound.
3. CvReplayInfo stores the accepted foreign m_eColor unchanged.
4. Hall-of-Fame/replay UI consumes those stored ColorTypes (e.g. color markers, graph/minimap
   layers); m_bHoFScreenUp deliberately permits CvGlobals::getColorInfo to receive an ID beyond
   the local array.
5. getColorInfo remaps it with `(eColor + 7) % N`.
6. For the final accepted IDs of the doubled range, the result is 0..5, violating the explicit
   skip-system-colors contract.

This is config-gated in current SAS because HOF_DISPLAY_OTHER_MOD_REPLAYS defaults to 0, but it
is a real defect in the supported/configurable compatibility feature, not merely hypothetical
bad input.

ANCESTRY / BOUNDARY
-------------------
K-Mod does not contain this remapping feature; it simply asserts a normal in-range ColorTypes.
AdvCiv practical 1430 (8377887745-family history around advc.106i introduction; exact tracked
file AdvCivPreSAS_1430_efd11afbeb.diff) introduced the foreign-replay fallback already as:

    // +7: Skip COLOR_CLEAR to COLOR_LIGHT_GREY
    e = (ColorTypes)((e + 7) % getNumColorInfos());

Later AdvCiv changes only refactored/renamed the accessor and comment. Base AdvCiv 1.14 and
current SAS retain the same arithmetic, while CvReplayInfo retains the generous doubled bound.
Therefore this is inherited from AdvCiv's advc.106i replay-compatibility feature, not SAS.

REPAIR DIRECTION
----------------
Map out-of-range IDs into the intended usable suffix [7, N) rather than shifting then taking
modulo N. For N > 7 and eColor >= N, a direct form is conceptually:

    7 + ((eColor - 7) % (N - 7))

(or equivalently offset relative to N). This guarantees every fallback result is >= 7 and < N.
Keep a defensive fallback/assert for pathological N <= 7 even though current assets have 128.

Do not merely lower CvReplayInfo's generous upper bound enough to hide the arithmetic problem;
that would unnecessarily reject foreign colors that can safely be represented through a
correct suffix remap.

SEVERITY / SCOPE
----------------
Low-to-moderate UI/compatibility severity; optional because display of other mods' replays is
disabled by default. No normal current-game ColorTypes are affected because the remap branch
runs only for eColor >= local color count.

DUPLICATE CHECK
---------------
No earlier album root was found for replay extra-color remapping/getColorInfo. This is separate
from replay-format/range changes such as SAS KI#166 and from queue-039 calendar/HOF-adjacent
utilities.

QUEUE STATUS AFTER F272
-----------------------
queue 040 remains OPEN.
F272 assigned / provisional KI#595.
Next unused root: F273.
Next provisional KI: KI#596.

NEXT CURSOR
===========
Continue CvGlobals.cpp from F273 unused / KI#596 next:
    * finish cached GlobalDefines current-XML type/coverage audit (initial type scan clean);
    * info-type map uniqueness/current XML registration;
    * manual/dynamic info-array count and bounds contracts;
    * remaining init/uninit ownership with a live-consumer requirement;
    * CE/K-Mod/AdvCiv targeted divergence scan;
    * revisit external reload leaks only if a supported re-entry path is established.
===============================================================================


===============================================================================
C023-WIP04 — QUEUE 040 CvGameCoreDLL/CvGlobals.cpp
F273 / provisional KI#596 CONFIRMED + QUEUE-041 CONTROL CORRECTION
Date: 2026-08-28
Model: GPT-5.6 Sol

F273 / provisional KI#596 — SAS RICH GLOBALDEFINE DIAGNOSTICS ACCIDENTALLY
DISABLE THE INHERITED ASSERTION IN THE OPTIMIZED ASSERT BUILD
--------------------------------------------------------------------------------
Status: CONFIRMED; SAS-specific diagnostic/build-configuration regression.
Primary file: CvGameCoreDLL/CvGlobals.cpp
Same-root sibling: CvGameCoreDLL/CvSelectionGroupAI.cpp
Build contract: CvGameCoreDLL/Project/Makefile + CvGameCoreDLL/FAssert.h

ROOT CONTRACT
-------------
AdvCiv's build system intentionally supports two assertion-capable configurations:

    Debug  = _DEBUG (which in FAssert.h also defines FASSERT_ENABLE)
    Assert = Release flags + /DFASSERT_ENABLE, without _DEBUG

The Makefile is explicit:

    Debug_DEFINES=/DWIN32 /D_WINDOWS /D_USRDLL /D_DEBUG
    Release_DEFINES=/DWIN32 /D_WINDOWS /D_USRDLL /DNDEBUG
    ...
    !IF "$(TARGET)" == "Assert"
    CFLAGS  = $(Release_CFLAGS) /DFASSERT_ENABLE

and FAssert.h independently gates the assertion macros on FASSERT_ENABLE:

    #ifdef _DEBUG
        #define FASSERT_ENABLE
    #endif
    ...
    #ifdef FASSERT_ENABLE
        #define FAssert(...)
        #define FAssertMsg(...)
    #else
        #define FAssert(...) (void)0
        #define FAssertMsg(...) (void)0
    #endif

Therefore `_DEBUG` is a sufficient condition for assertions, but it is NOT the assertion
feature flag. `FASSERT_ENABLE` is the actual contract and can intentionally be enabled in an
optimized/non-debug DLL.

PRIMARY FAILURE: CvGlobals GlobalDefine accessors
-------------------------------------------------
Base AdvCiv retains a cheap FAssert/FAssertMsg after the GlobalDefine lookup. Current SAS
replaced those plain assertions with richer lazy diagnostics in all three accessors:

    CvGlobals::getDefineINT
    CvGlobals::getDefineFLOAT
    CvGlobals::getDefineSTRING

The lookup status itself is correctly compiled whenever FASSERT_ENABLE is active, e.g.:

    #ifdef FASSERT_ENABLE
    bool bSuccess =
    #endif
    getDefinesVarSystem()->GetValue(...);

but the new diagnostic/assert block is guarded by `_DEBUG` instead:

    #ifdef _DEBUG
        if (!bSuccess) { ... FAssertMsg(...); }
    #endif

(or the FLOAT equivalent with CAMERA_MIN_DISTANCE exempted).

That changes the inherited build behavior:

    Build      Base AdvCiv                 current SAS
    -----      -----------                 -----------
    Debug      assertion active            richer assertion active
    Assert     assertion active            assertion SILENTLY REMOVED
    Release    assertion compiled out      assertion compiled out

The Assert target exists specifically to preserve assertions while running release-like code.
Missing/mistyped GlobalDefines therefore lose the inherited detection exactly in that useful
configuration. The lookup still falls back to the normal default, so this is a diagnostic
regression rather than a gameplay-state defect by itself; however it can materially hide XML
or refactor mistakes from the Assert DLL (the richer INT diagnostic was added precisely after
such a define typo was caught during SAS development).

REPAIR DIRECTION — CvGlobals
----------------------------
Gate the rich failure-only diagnostic on `FASSERT_ENABLE`, not `_DEBUG`:

    #ifdef FASSERT_ENABLE
        if (!bSuccess) { ... FAssertMsg(...); }
    #endif

and analogously for FLOAT/STRING. This keeps:

    * Debug: rich assertion, unchanged;
    * Assert: rich assertion restored;
    * Release: no assertion/formatting machinery;
    * normal successful lookups: no expensive message formatting, because the existing
      failure condition remains outside/around the formatting work.

No need to revert to the old uninformative Base assertion merely to fix the build guard.

SAME-ROOT SIBLING: CvSelectionGroupAI::AI_update
-------------------------------------------------
The same SAS optimization pattern was applied to AdvCiv's inherited stuck-loop assertion.
Base AdvCiv executes:

    FAssertMsg(iAttempts != iMaxAttempts, "Unit stuck in a loop");

outside `_DEBUG`, so it remains active in the Assert target. SAS moved the richer version
inside:

    #ifdef _DEBUG
        const bool bAssertCondition = (iAttempts != iMaxAttempts);
        if (!bAssertCondition) { ... FAssertMsg(...rich state...); }
        iMaxAttempts += 4; // restore Debug-only early-trigger adjustment
    #endif

The result is the same root: the Assert DLL no longer has the inherited stuck-loop assertion.

IMPORTANT REPAIR BOUNDARY FOR THE SIBLING
-----------------------------------------
Do NOT blindly replace that entire `_DEBUG` block with `FASSERT_ENABLE`: the
`iMaxAttempts += 4` line repairs an earlier `_DEBUG`-only `iMaxAttempts -= 4` adjustment and
must remain Debug-only. A safe conceptual split is:

    #ifdef FASSERT_ENABLE
        // lazy rich assertion block
    #endif
    #ifdef _DEBUG
        iMaxAttempts += 4; // restore only the Debug-only subtraction
    #endif

This restores Assert-build detection without changing its normal iteration limit.

CORRECTION TO THE ALREADY-CLOSED QUEUE 041 AUDIT
-------------------------------------------------
The queue-041 closure currently contains this rejected-control statement:

    "This does not disable an assert-enabled non-debug configuration: FAssert.h defines
     FASSERT_ENABLE only when _DEBUG itself is defined. Outside _DEBUG the old assert was
     already a no-op."

That statement is WRONG because it considered only FAssert.h's automatic `_DEBUG` enablement
and missed the Makefile's separate Assert target (`Release_CFLAGS /DFASSERT_ENABLE`).

Amend that old control as follows:

    * the expensive-formatting concern was valid;
    * moving message construction behind the failure condition was good;
    * moving the assertion itself behind `_DEBUG` was not equivalent to the inherited
      behavior and disabled it in the optimized Assert build;
    * reopen that rejected diagnostic candidate only as SAME-ROOT evidence for F273, not as
      a new independent numbered root and not as a retraction of queue-041's established
      gameplay findings.

ANCESTRY / INDEPENDENCE
-----------------------
Base AdvCiv has the plain assertions and the two-build assertion contract. The loss occurs in
SAS's richer diagnostic replacements, so F273 is SAS-specific. It is independent of F272:
F272 is an inherited Hall-of-Fame/replay color-remapping arithmetic bug; F273 is a build-guard
diagnostic regression affecting assertion coverage.

QUEUE STATUS AFTER F273
-----------------------
queue 040 remains OPEN while the final saturation pass finishes.
F272 / provisional KI#595 confirmed in WIP03.
F273 / provisional KI#596 confirmed here.
Next unused root: F274.
Next provisional KI: KI#597.

NEXT CURSOR
===========
Continue queue 040 CvGlobals.cpp from F274 unused / KI#597 next:
    * finish last init/uninit ownership and exported-reset/lazy-loader controls;
    * finish getter/setter/cache invalidation coverage, including less-obvious runtime define
      mutation paths;
    * compare remaining CvGlobals divergences with Base AdvCiv, K-Mod and Civ4CE for explicit
      fixes rather than stylistic differences;
    * re-challenge array/count/info-map bounds and any compile-time feature branches;
    * close queue 040 if no independent live root survives; do not allocate F274 merely for
      finding-count symmetry.

No repository source files were modified.
===============================================================================


===============================================================================
C023-WIP05 — QUEUE 040 CvGameCoreDLL/CvGlobals.cpp SATURATION CONTINUATION
Date: 2026-08-28
Model: GPT-5.6-Sol

QUEUE STATUS
------------
queue 040
CvGameCoreDLL/CvGlobals.cpp
OPEN

CONFIRMED ROOTS SO FAR:
    F272 / provisional KI#595 — foreign-replay color remap can wrap into reserved colors 0..6
    F273 / provisional KI#596 — SAS _DEBUG guard removes inherited assertions from Assert target

NEXT ROOT:
    F274 UNUSED
    next provisional KI#597

No repository source files modified. Audit album only.

I. CURRENT RUNTIME GlobalDefine MUTATION / CACHE INVALIDATION — CLEAN CONTROL
---------------------------------------------------------------------------
Enumerated every literal setDefineINT/FLOAT/STRING call in the current SAS source/assets and
matched it against CvGlobals' cached define machinery.

Current integer mutations are:
    * MINIMAP_RENDER_SIZE in CvReplayScreen.py;
    * RIVER_SOURCE_MIN_RIVER_RANGE, RIVER_SOURCE_MIN_SEAWATER_RANGE and
      PLOTS_PER_RIVER_EDGE in Water.py / CvMapGenerator.cpp.

MINIMAP_RENDER_SIZE is in DO_FOR_EACH_GLOBAL_DEFINE and setDefineINT's pinpoint update writes
the new value into m_aiGlobalDefinesCache. The three map-generator river controls are not in
the enum cache and their consumers use string getDefineINT, so the changed FVariableSystem
value is read directly. Their restore path is therefore coherent too.

Current float mutations are FIELD_OF_VIEW, CAMERA_MIN_DISTANCE, CAMERA_START_DISTANCE and
CAMERA_CITY_ZOOM_IN_DISTANCE. setDefineFLOAT refreshes cacheGlobalFloats, except the deliberate
CAMERA_START_DISTANCE path that passes bUpdateCache=false and immediately calls
cacheGlobalFloats(false) itself. No stale current cached value was found.

No live setDefineSTRING caller was found.

II. WAR_SUCCESS_CITY_CAPTURING LOWER-BOUND ASYMMETRY — LATENT ROBUSTNESS CONTROL
-------------------------------------------------------------------------------
Full cache initialization special-cases WAR_SUCCESS_CITY_CAPTURING with iLower = 1 because the
source explicitly says 0 can crash AI code. A later runtime setDefineINT of that same name would
use cacheGlobalInts(szChangedDefine, iNewValue), which stores iNewValue directly and therefore
does not reapply the >=1 normalization.

This is a real API robustness asymmetry, but current SAS contains no runtime setter for this
define. XML startup goes through the normalized full-cache path. Do not allocate F274 without
a supported/live mutation path; record the invariant for any future generic define editor.

III. CvGlobals init/uninit / CvAgents — OWNERSHIP OMISSION STILL BELOW ROOT BAR
------------------------------------------------------------------------------
Rechecked the whole init/uninit pair and CvAgents implementation.

CvGlobals::init allocates m_agents after CvPlayer/CvTeam statics and CvAgents stores vectors of
raw player/team pointers plus installs itself into AgentIteratorBase's global cache pointer.
CvGlobals::uninit frees CvPlayer/CvTeam statics but does not delete m_agents.

So the ownership omission is genuine, and the leaked object contains stale pointers during
teardown. init/uninit are DllExport, but the current DLL source contains no second local init
call, no post-uninit AgentIterator consumer, and no proof that the Civ4 EXE performs a supported
same-loaded-DLL reinit cycle rather than unloading the DLL/process. Keep as cleanup/lifecycle
debt rather than inventing a live F274.

IV. CvXMLLoadUtility RAW POINTER — HISTORICALLY ACKNOWLEDGED EXTERNAL-LIFETIME CONTROL
-------------------------------------------------------------------------------------
CvGlobals retains m_pXMLLoadUtility for lazy optional/throne-room XML. CvXMLLoadUtility's own
destructor explicitly comments that the EXE appears to destroy it only when exiting from the
opening menu and that deleting it from another path risks double deletion. Current source does
not clear GC.m_pXMLLoadUtility in that destructor.

The pointer can therefore look dangling in isolation, but its only lazy consumers are meaningful
before that menu-exit teardown. No local post-destruction consumer is proved. Preserve as an
external-lifetime seam, not F274.

V. DISABLED XML BINARY INFO CACHE — CURRENTLY NON-LIVE
------------------------------------------------------
The readInfoArray/writeInfoArray branch in CvGlobals is guarded by ENABLE_XML_FILE_CACHE and
current CvInfo_Base.h defines ENABLE_XML_FILE_CACHE 0. AdvCiv history also records that this
cache was effectively unused for mods. Compilation-repair history for this branch does not make
it a current runtime root.

VI. CAMERA_START_DISTANCE STATIC BASELINE — INTENTIONAL, NOT A CACHE BUG
------------------------------------------------------------------------
The current source comment questions the function-local static
m_fCAMERA_START_DISTANCE_Override. History resolves the intent: the automatic/default-camera
feature deliberately captures the original XML CAMERA_START_DISTANCE before the function starts
overwriting that same GlobalDefine. BUG's "Custom (XML)" option calls updateCameraStartDistance
with bReset=true specifically to restore this captured XML baseline. Re-reading the already
mutated define instead would lose the original custom XML value.

The static is therefore the persistence mechanism for the baseline, not accidental stale caching.
No F274.

VII. HISTORY / SIBLING-FIX STREAM — NO NEW ROOT IN THIS SLICE
-------------------------------------------------------------
Revisited the targeted CvGlobals repair history rather than only current text:
    * memory-corruption fixes around async-random ownership/type;
    * startup-audio EVENT_MESSAGE_TIME early-cache fix;
    * Cppcheck fixes around optional/throne-room assert-only temporaries;
    * release-build/XML-cache compilation repairs;
    * menu-background invalid-profile guard;
    * camera-distance evolution;
    * replay/color compatibility introduction.

The optional/throne-room functions are particularly useful as a control for F273: they already
gate assertion-only state with FASSERT_ENABLE, demonstrating the project's intended feature flag.
No adjacent uncorrected live root was found in those repair families beyond already-confirmed
F272/F273.

NEXT CURSOR
===========
Continue queue 040 from F274 unused / KI#597 next:
    * finish external accessor/bounds adapters (main-menu, landscape, direction/string arrays);
    * finish info registration/reset/count symmetry with current XML and loader call order;
    * finish independent CE/K-Mod divergence scan for explicit behavioral fixes;
    * final function-family inventory and two-root independence/repair-boundary check;
    * if those remain controls, close queue 040 with F272/F273 and leave F274 unused.
===============================================================================


================================================================================
C023-WIP06 — QUEUE 040 CvGlobals.cpp — FIXED-GLOBAL-TYPE COUNT RECHECK
================================================================================

Status entering this slice:
- Queue 040 CvGlobals.cpp: OPEN.
- F272 / KI#595: confirmed (foreign-replay color remap can wrap into reserved 0..6 colors).
- F273 / KI#596: confirmed (SAS rich GlobalDefine diagnostics are incorrectly gated by _DEBUG rather than FASSERT_ENABLE; same-root CvSelectionGroupAI correction recorded in WIP04).
- F274 / KI#597: UNUSED.

CONTROL / CORRECTION — FunctionTypes does NOT numerically match GlobalTypes.xml, but this is a stock dormant asymmetry rather than a current SAS defect

The earlier broad fixed-string-array control was too strong when it said that every inherited fixed GlobalTypes count matched its C++ enum. Rechecking the current enum against an independent Civ4CE BtS GlobalTypes.xml exposed one exception:

Current SAS CvEnums.h contains six FunctionTypes:

    FUNC_NOINTERP,
    FUNC_LINKEY,
    FUNC_BEZKEY,
    FUNC_TCBKEY,
    FUNC_EULERKEY,
    FUNC_STEPKEY,

so NUM_FUNC_TYPES is 6 in the DLL build.

CvXMLLoadUtility::SetGlobalTypes() deliberately calls:

    int iEnumVal = NUM_FUNC_TYPES;
    SetGlobalStringArray(&GC.getFunctionTypes(),
            "Civ4Types/FunctionTypes/FunctionType",
            &iEnumVal, true);

With bUseEnum == true, SetGlobalStringArray does not replace iEnumVal with the number of XML nodes. It allocates exactly *iNumVals CvString objects, reads siblings until there are no more, and breaks. Therefore a five-entry FunctionTypes XML section leaves the sixth allocated CvString default/empty rather than overflowing memory.

Independent Civ4CE comparison proves this is not a SAS or AdvCiv regression:
- Civ4CE's stock/BtS CvEnums.h also has the same six FunctionTypes including FUNC_STEPKEY.
- Civ4CE's loader uses the same NUM_FUNC_TYPES + bUseEnum=true loading pattern.
- Civ4CE's BtS GlobalTypes.xml contains only five FunctionType entries: NOINTERP, LINKEY, BEZKEY, TCBKEY and EULERKEY.
- A scan of Civ4CE BtS assets found no STEPKEY / FUNC_STEPKEY reference.

Current SAS evidence is equally non-live:
- The current light-source XML snapshot contains no STEPKEY / FUNC_STEPKEY reference.
- Current C++ references to FUNC_STEPKEY are only the enum declaration itself.
- getFunctionTypes is present in CvGlobals and the CyGlobalContext wrapper, but no current SAS Python caller of getFunctionTypes was found.
- Thus the empty sixth string is not consumed by current repository code/assets, and no current type lookup requires a FUNC_STEPKEY string mapping.
- Commit history shows FUNC_STEPKEY predates SAS and AdvCiv; it is already present in the imported K-Mod/BtS enum lineage.

Disposition:
- Correct the earlier blanket "all fixed counts match" wording: all checked fixed arrays match except this longstanding FunctionTypes 6-enum/5-XML asymmetry.
- Do NOT allocate F274. The actual load is memory-safe, the asymmetry is inherited from stock BtS/Civ4CE, and no current consumer reaches the empty FUNC_STEPKEY slot.
- Keep as latent API/data-contract debt only: a future Python/C++ caller asking GC.getFunctionTypes(FUNC_STEPKEY), or future XML relying on that token through this mapping, would get an empty/unregistered sixth string unless GlobalTypes.xml were extended.

RELATED ASSERT-BUILD CONTROL — duplicate-info-type detection is deliberately/inheritedly _DEBUG-only, but current XML has no trigger

CvGlobals::setInfoTypeFromString contains an additional duplicate-type assertion under #ifdef _DEBUG rather than FASSERT_ENABLE. This superficially resembles F273, but it is not the same SAS regression:
- commit history shows the _DEBUG guard already existed in pre-SAS AdvCiv;
- SAS did not replace an inherited FAssert-visible-in-Assert-build check here;
- the current non-text XML Type scan found 3,869 Type tokens and zero duplicate tokens across files and zero duplicates within a file.
Therefore there is no current duplicate-map overwrite trigger and no independent F274. It remains inherited diagnostic coverage debt if the project ever wants all assertion diagnostics available in the optimized Assert target.

Current queue state after WIP06:
- Queue 040 CvGlobals.cpp: OPEN, now at final closure/repair-boundary inventory.
- F272 / KI#595: confirmed.
- F273 / KI#596: confirmed.
- F274 / KI#597: still UNUSED.
- No repository source files modified.


===============================================================================
C024 DURABLE CLOSURE - QUEUE 040 CvGameCoreDLL/CvGlobals.cpp
Date: 2026-08-28
Model: GPT-5.6-Sol
===============================================================================

FIRST-PASS STATUS
-----------------
queue 040 CvGameCoreDLL/CvGlobals.cpp: CLOSED.
Files complete: 23 / 169.
Closed roots:
    F272 / provisional KI#595
    F273 / provisional KI#596
Next unused root:
    F274 / provisional KI#597

No F274 was allocated merely to make the finding count larger. The final passes continued to
produce bounded lifecycle/API/data-contract controls rather than another independent live root.

F272 FINAL REPAIR-BOUNDARY CHECK
--------------------------------
Current SAS has N = 128 ColorInfos and the foreign-replay compatibility path accepts foreign
player-color IDs through 254 when HOF_DISPLAY_OTHER_MOD_REPLAYS is enabled. The current remap:

    (eColor + 7) % N

was intended to skip reserved/system colors 0..6 but maps foreign IDs 249..254 to 0..5.

The narrow suffix-domain repair:

    7 + ((eColor - 7) % (N - 7))

for N > 7 was exhaustively compared across the current accepted foreign range 128..254:

    * 121 already-good mappings, IDs 128..248, remain EXACTLY unchanged;
    * only the six bad mappings change:
          249: 0 -> 7
          250: 1 -> 8
          251: 2 -> 9
          252: 3 -> 10
          253: 4 -> 11
          254: 5 -> 12
    * the repaired result range is 7..127, so the promised reserved prefix is actually skipped.

Thus the repair can preserve every current mapping that already satisfied the contract while
correcting only the proven wrap-back cases. Keep a defensive N <= 7 fallback/assert so the
modulo denominator can never become zero or negative in malformed/future data.

F273 FINAL REPAIR-BOUNDARY / SIBLING CHECK
------------------------------------------
Re-proved the project build contract rather than relying on the prior album statement:

    Debug  -> _DEBUG, which causes FAssert.h to enable FASSERT_ENABLE
    Assert -> release-like flags + /DFASSERT_ENABLE, WITHOUT _DEBUG
    Release-> neither assertion mode

Therefore SAS's richer getDefineINT/FLOAT/STRING diagnostics must be guarded by
FASSERT_ENABLE, not _DEBUG, if they are to preserve Base AdvCiv's optimized Assert-build
coverage.

The already-closed queue-041 CvSelectionGroupAI::AI_update has the same SAS regression root.
Its rich stuck-loop diagnostic can also be guarded by FASSERT_ENABLE, but its
`iMaxAttempts += 4` must remain under _DEBUG because it compensates an earlier _DEBUG-only
`iMaxAttempts -= 4`.

A useful independent in-tree model is CvGame::updateMoves:

    int iMaxUnitUpdateAttempts = 18;
    FAssertMsg(...);              // assertion remains available in Assert
    #ifdef _DEBUG
        iMaxUnitUpdateAttempts += 4; // Debug-only behavioral adjustment
    #endif

This is exactly the separation the CvSelectionGroupAI sibling needs.

A targeted repository-wide scan of assertions near _DEBUG/custom SAS diagnostic blocks found
no second SAS replacement of an inherited always-assert-capable check beyond the already-known
CvGlobals and CvSelectionGroupAI cases. Other hits were inherited Debug-only invariant sweeps
or deliberately SAS-added diagnostic probes that have no Base assertion contract to preserve.
The inherited CvGlobals::setInfoTypeFromString duplicate-type assertion is also _DEBUG-only,
but it predates SAS and current XML contains no duplicate Type trigger; it remains diagnostic
coverage debt, not F273 ancestry and not F274.

FINAL HIGH-FANOUT CONTROLS / REJECTIONS
---------------------------------------
1. GlobalDefine mutation and caches
   Every current literal setDefineINT/FLOAT/STRING path was matched against CvGlobals caching.
   MINIMAP_RENDER_SIZE receives the intended targeted integer-cache update; river generation
   controls are uncached and read directly; current camera/FoV float mutations refresh the
   float cache or deliberately defer and then refresh it. No current stale-cache path survived.

   WAR_SUCCESS_CITY_CAPTURING is clamped to >=1 during full caching because 0 can crash AI,
   while a hypothetical targeted runtime setter would bypass that clamp. Current repository
   code has no such setter, so this is latent API robustness debt, not F274.

2. Type/info maps and current XML
   A current non-text XML scan found 3,869 <Type> tokens and zero duplicate tokens, both within
   individual files and across files. The fixed GlobalTypes string categories likewise contain
   no current cross-category duplicate token that would silently overwrite m_typesMap.

3. FunctionTypes count correction
   The earlier blanket statement that every fixed GlobalTypes count matches its C++ enum was
   corrected. FunctionTypes has six C++ values including FUNC_STEPKEY but stock/current XML has
   five strings. Civ4CE independently reproduces the same six-enum/five-XML contract and the
   same NUM_FUNC_TYPES loader behavior. The sixth CvString remains default-empty safely, and
   neither current SAS nor CE assets/code use FUNC_STEPKEY. Longstanding dormant stock contract
   debt; no F274.

4. Direction / animation string accessor shapes
   getDirectionTypes takes AutomateTypes instead of DirectionTypes, a genuine copy/paste API
   mismatch, but there is no current indexed DLL/Python caller exposing it. AnimationOperator
   constants start at 1001 while the string accessor operationally expects a 0-based array index;
   current CyGlobalContext passes ordinary indices and no C++ caller passes real 1001+ constants.
   Both remain latent API defects rather than current roots.

5. Footstep audio
   getFootstepAudioTypeByTag is badly named but correct. Unit-art RunLoopSoundTag/RunEndSoundTag
   values such as LOOPSTEP_TANK are FootstepAudioTypes tokens; the resulting index is then used
   to fetch the parallel actual sound-script tag such as AS3D_LOOPSTEP_TANK.

6. CvAgents ownership
   CvGlobals::init allocates m_agents and uninit does not delete it. CvAgents retains player/team
   pointers, so this is a real shutdown ownership leak/stale-pointer object. However, no local
   same-loaded-DLL second init or post-uninit consumer could be established. Without the EXE
   lifecycle proof needed to turn it into a live re-entry failure, it stays cleanup debt.

7. cacheGlobals / CvPythonCaller / XML-loader lifetime
   cacheGlobals allocates m_pPythonCaller without first deleting an existing one, and CvGlobals
   retains a raw CvXMLLoadUtility pointer for lazy loading. Both are potentially dangerous under
   an undocumented repeated EXE bootstrap/reload cycle, but current DLL source does not prove
   that cycle. CvXMLLoadUtility's own destructor history explicitly discusses EXE ownership and
   double-deletion risk. Do not infer a root from external lifecycle speculation.

8. Lazy throne-room sequencing
   AdvCiv advc.003v deliberately introduced the lazy-load call specifically in exported
   getNumThroneRoomInfos(), with the comment "Load it as late as possible." LoadThroneRoomInfo
   atomically loads camera, room and style arrays and guards repeat loading with bThroneRoomLoaded.
   Other exported room/style accessors do not independently force the load, so this relies on the
   known EXE enumeration order. No current DLL-side caller violates that order and this exact
   sequencing is the original AdvCiv design; keep as an external-engine contract, not F274.

9. Main-menu / landscape exported bounds
   getMainMenus repairs a saved index >= current menu count because switching to a mod with fewer
   menu backgrounds is the known producer. It does not repair a negative index, but current UI
   producers emit valid nonnegative indices and the Python wrapper checks both bounds. Negative
   handling would harden corrupted/external profile input; no current producer was found.

   setActiveLandscapeID has no local bounds check and the derived plot-cell getters index by the
   active ID. It is EXE-owned state with no DLL setter caller; no invalid current producer was
   proved. Add an assertion if desired for robustness, but not as F274.

10. Plot-group FAStar
    getPlotGroupFinder contains an advc.test FError and lazily initializes geometry once, which
    initially raised stale-map-size concerns. AdvCiv explicitly marks it unused inside the DLL
    after CvPlotGroup::recalculatePlots stopped using it. No current internal consumer exists;
    exported legacy contract only, so no live stale-geometry root.

11. XML binary cache
    The binary info-cache read/write machinery is behind ENABLE_XML_FILE_CACHE == 0 in current
    SAS. Historical compile fixes for the branch do not make it a current runtime issue.

12. Camera baseline
    The function-static CAMERA_START_DISTANCE override is intentional: it preserves the original
    XML baseline while runtime camera logic overwrites the GlobalDefine, enabling the BUG
    "Custom (XML)" reset. Treating the static as stale and replacing it with repeated define reads
    would destroy the very baseline it is meant to preserve.

13. History / independent implementation scan
    AdvCiv repair streams touching CvGlobals were challenged for adjacent misses: async-random
    memory corruption/type ownership, startup audio EVENT_MESSAGE_TIME caching, optional/throne
    assertion-variable compile fixes, XML-info refactors/cache compilation, invalid menu-profile
    guard, camera evolution and replay compatibility. K-Mod/Civ4CE comparisons did not expose an
    additional surviving current root. The normalized SAS-vs-Base semantic delta in CvGlobals is
    tiny; beyond F273 it is predominantly comments/formatting rather than a broad SAS rewrite.

FUNCTION-FAMILY SATURATION
--------------------------
The final inventory covered constructor/init/uninit ownership; external engine pointers;
pathfinder globals; replay color access; lazy optional/throne loading; active landscape and
playable-civ state; fixed global string arrays; footstep mappings; GlobalDefine integer/float
caches and runtime setters; camera updates; hardcoded EXE define-pointer fallback; type/info
maps and reset seams; XML binary cache helpers; enum/count adapters; profiler/graphics globals;
and the remaining exported integer-index accessors.

No unexamined CvGlobals.cpp function family remains that plausibly hides an independent current
root under the audit standard. F272 and F273 are independent: replay-color arithmetic versus
SAS assertion-build gating. Queue 040 therefore closes with those two roots and F274 unused.

NEXT CURSOR - PRIORITY QUEUE 037
================================
Open queue 037 CvGameCoreDLL/CvPythonCaller.cpp (1,576 queued lines) from:

    F274 UNUSED
    provisional KI#597 next

Priority rationale already exists in the reconciled album. Begin with callback lifetime/guard
contracts, Python exception/error-return semantics, callback argument/return conversions,
missing/optional function handling, recursion/reentrancy, interpreter/bootstrap assumptions,
and SAS-vs-Base/K-Mod/Civ4CE divergence before promoting any root. Associated
CvPythonCaller.h and Python bridge/call sites should be inspected as needed.

No repository source files were modified.
===============================================================================

===============================================================================
C024-WIP01 CONTINUATION - QUEUE 037 CvPythonCaller.cpp
===============================================================================
Source basis: current local AdvCiv-SAS light-source snapshot 20260828T215559,
plus local AdvCiv 1.14, K-Mod 1.46 and Civ4CE snapshots. No web lookup used.
Repository source files were not modified.

OPEN FILE STATE
---------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp (1,576 queued lines) - OPEN
F274 / provisional KI#597 - CONFIRMED PROVISIONAL ROOT
F275 unused / provisional KI#598 next

F274 / provisional KI#597
-------------------------
TITLE: USE_DO_GOODY_CALLBACK can null-dereference when a goody hut is removed by plot ownership

CURRENT CODE ROOT
-----------------
CvPythonCaller::doGoody accepts a nullable unit pointer:

    bool CvPythonCaller::doGoody(CvPlot const& kPlot, CvUnit const* pUnit,
        PlayerTypes ePlayer) const

and its own adjacent comment explicitly preserves the old contract:

    // Note: pUnit can be NULL, but pyUnit can still be initialized (and is expected by Python).

but current AdvCiv/SAS then does:

    CyUnit* pyUnit = new CyUnit(*pUnit);

Dereferencing pUnit is undefined/crashing when pUnit == NULL.

LIVE NULL CALLER
----------------
This is not merely a misleading nullable signature. CvPlot::setOwner still contains a live call:

    if (isOwned())
    {
        if (isGoody())
            GET_PLAYER(getOwner()).doGoody(this, NULL);
        ...
    }

CvPlayer::doGoody immediately calls the wrapper before ordinary hut handling:

    if (GC.getPythonCaller()->doGoody(*pPlot, pUnit, getID()))
        return;

Therefore, when USE_DO_GOODY_CALLBACK is enabled, an owned goody plot that reaches this
ownership path invokes CvPythonCaller::doGoody with NULL and crashes while constructing the
CyUnit Python wrapper. Typical exposure is a goody hut being absorbed/removed through plot
ownership or culture rather than being entered by a unit.

CONFIGURATION / SEVERITY BOUNDARY
---------------------------------
Current Assets/XML/PythonCallbackDefines.xml sets:

    USE_DO_GOODY_CALLBACK = 0

so stock SAS defaults do not execute the bad dereference. This remains a real supported callback
configuration defect rather than a default-game crash: the define exists specifically to enable
this Python override, and enabling it exposes the live NULL caller immediately when the
ownership-hut path occurs.

ANCESTRY / INDEPENDENT CONTROL
------------------------------
K-Mod 1.46 uses the safe nullable-pointer form directly in CvPlayer::doGoody:

    CyUnit kGoodyUnit(pUnit);

CyUnit has a pointer constructor:

    CyUnit::CyUnit(CvUnit* pUnit) : m_pUnit(pUnit) {}

so NULL is a deliberate representable Python-wrapper state.

AdvCiv practical 2066, commit ffe04c3efd, is titled "Avoid some const_casts". Its CvPythonCaller
change is exactly:

    - CyUnit* pyUnit = new CyUnit(const_cast<CvUnit*>(pUnit));
    + CyUnit* pyUnit = new CyUnit(*pUnit);

The surrounding comment saying pUnit can be NULL was left unchanged. Thus the root is narrowly
identified: a const-cast cleanup replaced the nullable pointer constructor with the reference
constructor and broke the preserved NULL contract. Current Base AdvCiv 1.14 still contains the
same regression; SAS inherits it unchanged.

REPAIR BOUNDARY
---------------
The repair must preserve both existing modes:

1. non-NULL unit: Python receives a CyUnit wrapper for that unit;
2. NULL unit: Python still receives a CyUnit Python object whose internal unit pointer is NULL,
   matching BtS/K-Mod and the explicit comment/contract.

The smallest historical repair is to restore the pointer constructor (requiring the old
const_cast because the wrapper stores CvUnit*), e.g. the exact pre-2066 form. An alternative
without dereferencing NULL is to explicitly choose default/null CyUnit construction for the NULL
case and the reference constructor for non-NULL. Do not "fix" this by refusing the callback when
pUnit is NULL; that would change the inherited Python callback contract on the ownership path.

ROOT INDEPENDENCE
-----------------
F274 is independent of queue-040 F272/F273. It is a nullable Python-wrapper construction
regression in AdvCiv's callback refactor/const cleanup, not replay-color arithmetic or assertion
build gating.

EARLY QUEUE-037 CONTROLS / COVERAGE
-----------------------------------
- Normalized SAS-vs-Base AdvCiv semantic delta in CvPythonCaller.cpp is very small. The current
  substantive SAS changes are already-reviewed repairs such as the int AI_foundValue contract,
  riverValue hybrid/default handling and minStartingDistanceMod lower-bound restoration.
- The four central call(...) overloads initialize m_bLastCallSuccessful on both the module-missing
  and actual-call paths; no stale-success carryover was found in those wrappers in this opening
  pass.
- The F274 trigger is not inferred from the signature alone: the live CvPlot::setOwner NULL caller,
  current callback define, CyUnit constructors, K-Mod predecessor and precise AdvCiv regression
  commit were all checked locally before promotion.

NEXT CURSOR
-----------
Continue queue 037 from F275 unused / provisional KI#598 next. Audit all Python-object wrapper
constructions for nullable/lifetime mismatches; then callback result/default semantics,
isOverride/pythonUsingDefaultImpl state, exceptional or missing-function paths, random-event
object mutation, map-script vector/enum bounds, and practical-history sibling regressions.
===============================================================================

===============================================================================
C024-WIP02 CONTINUATION - QUEUE 037 CvPythonCaller.cpp
===============================================================================
OPEN FILE STATE
---------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp - OPEN
F274 / provisional KI#597 - confirmed in WIP01
F275 / provisional KI#598 - CONFIRMED PROVISIONAL ROOT
F276 unused / provisional KI#599 next

F275 / provisional KI#598
-------------------------
TITLE: CvPythonCaller::canBuild rejects positive Python override values above 1 despite the
       inherited callback contract

CURRENT CODE
------------
CvPythonCaller::canBuild contains the preserved contract comment:

    "Returning -1 means ignore; 0 means Build cannot be performed; 1 or greater means it can"

but after handling -1 it does:

    bOverride = true;
    FAssertBounds(-1, 2, lResult);
    return toBool(lResult);

CvPythonCaller::toBool is:

    static bool toBool(long l) { FAssert(l >= 0); return (l == TRUE); }

Therefore a valid positive callback result such as 2 has two wrong consequences:

- Assert DLL: FAssertBounds(-1, 2, 2) fires because the upper bound is exclusive;
- Release DLL: the assertion disappears, but toBool(2) returns false because 2 != TRUE (1).

The wrapper thus interprets 2+ as "cannot build" even though its own inherited contract says
1 or greater means the build can be performed.

PRE-REFACTOR / K-MOD CONTROL
----------------------------
The practical-1648 source immediately before extraction into CvPythonCaller used:

    long lResult = 0;
    callFunction(..., "canBuild", ..., &lResult);
    if (lResult >= 1)
        return true;
    else if (lResult == 0)
        return false;

Current K-Mod 1.46 retains the same >= 1 interpretation. Civ4CE's stock BtS CvGameUtils.py also
documents the same callback contract: -1 ignore, 0 deny, 1 or greater allow.

ANCESTRY
--------
The mismatch was introduced directly by AdvCiv practical 1648 (commit 553b1a0f8d, "Moved
DLL-to-Python calls and logging into separate classes"). The new wrapper copied the correct
contract into a comment but immediately narrowed it to {-1,0,1} through FASSERT_BOUNDS and then
used the exact-one helper. Base AdvCiv 1.14 still contains the regression; SAS inherits it.

CONFIGURATION BOUNDARY
----------------------
USE_CAN_BUILD_CALLBACK is 0 in current SAS defaults, as are all 54 callback defines cached by
CvPythonCaller. The defect becomes live when the supported canBuild callback is enabled and the
Python override returns a positive value greater than 1. This is therefore configuration-gated,
not a default-gameplay regression.

REPAIR BOUNDARY
---------------
Preserve all three documented states:

    -1      -> no Python override; continue C++ canBuild logic
     0      -> Python override denies build
    >= 1    -> Python override allows build

A narrow repair should therefore return lResult >= 1 once lResult != -1 and remove/relax the
incorrect exclusive upper-bound assertion. Do not change CvPythonCaller::toBool globally on the
strength of this finding: several other inherited callbacks historically used exact == 1, and
queue 037 is auditing those contracts individually rather than assuming one universal Python
truthiness convention.

ADDITIONAL CONTROLS
-------------------
- The callback-define table itself is internally complete: all 54 DO_FOR_EACH_CALLBACK_DEFINE
  entries have exactly one matching USE_*_CALLBACK integer define in current
  PythonCallbackDefines.xml. Eight additional USE_* event callbacks belong to the separate
  CvDllPythonEvents path, so they are not omissions from this table. All 62 current callback
  defines are 0 by default.
- K-Mod controls show that many UI callbacks really did use exact == 1, so the existence of
  CvPythonCaller::toBool is not itself enough to report a broad defect. F275 is promoted because
  canBuild has a specific explicit >= 1 contract and direct pre-refactor comparison.
- `updateColoredPlots` is the only wrapper-local `long lResult;` without initialization. Its
  Python function is expected to exist and the pre-refactor K-Mod code also depended on a
  successful call after setting the local result to 0. Current uninitialized-on-Python-error
  behavior is being retained as an error-path robustness lead, not yet F276.

NEXT CURSOR
-----------
Continue from F276 unused. Highest-value active checks:
- doPillageGold's negative/error sentinel versus K-Mod's old `lPillageGold < 0` fallback;
- callback call-failure defaults and uninitialized return storage;
- random-event mutable EventTriggeredData and pointer-output refresh consistency;
- map-script `isOverride()`/usingDefaultImpl and result validation;
- history commits that fixed post-1648 callback regressions, looking for unfixed siblings.
===============================================================================

===============================================================================
C024-WIP03 CONTINUATION - QUEUE 037 CvPythonCaller.cpp
===============================================================================
OPEN FILE STATE
---------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp - OPEN
F274 / provisional KI#597 - confirmed in WIP01
F275 / provisional KI#598 - confirmed in WIP02; SAME-ROOT MAP-SCRIPT SIBLINGS ADDED BELOW
F276 / provisional KI#599 - CONFIRMED PROVISIONAL ROOT
F277 unused / provisional KI#600 next

F275 SAME-ROOT SIBLING EVIDENCE
-------------------------------
The exact-one narrowing introduced by practical 1648 is not limited to canBuild. The two map
placement callbacks were also moved behind CvPythonCaller::toBool by the same refactor:

    CvPythonCaller::canPlaceBonusAt -> canPlaceItemAt
    CvPythonCaller::canPlaceGoodyAt -> canPlaceItemAt

Current canPlaceItemAt validates only negativity and then does:

    if (lResult < 0)
    {
        ...
        bOverride = false;
        return false;
    }
    return toBool(lResult);

Before practical 1648, both CvMapGenerator callbacks did this directly:

    if (result >= 0)
        return result;

where the function return type is bool. C++ therefore treated every nonzero positive long as
true. Practical 1648 replaced that implicit nonzero truth conversion with toBool(result), which
returns true only for exactly 1. Hence a custom map callback that historically returned 2 (or
another positive integer) changes from true to false after the refactor.

This is recorded as SAME-ROOT SUPPORT for F275 rather than a new finding. The canBuild case is the
strongest proof because its Python documentation explicitly says "1 or greater"; the canPlace*
case has direct predecessor behavior but no equally explicit current Python contract. Current SAS
ships only one script defining those hooks, Planet_Generator_0_68.py, and it delegates to the
default implementation rather than returning a custom positive integer, so no shipped default
trigger was found. The evidence nevertheless confirms that toBool cannot safely be assumed to
preserve every pre-refactor callback contract.

F276 / provisional KI#599
-------------------------
TITLE: Enabled doPillageGold callback suppresses the inherited C++ gold fallback on negative or
       failed Python results

CURRENT CODE ROOT
-----------------
CvPythonCaller::doPillageGold initializes a distinct error sentinel but returns true whenever the
callback define is enabled:

    int const iError = MIN_INT;
    ARGSLIST(iError);
    ...
    call("doPillageGold", argsList, lResult);
    ...
    if (lResult != iError)
        iPillageGold = toInt(lResult);
    else FAssert(lResult != iError);
    return true;

Its caller in CvUnit treats the wrapper's bool as "Python supplied the pillage result":

    int iPillageGold = 0;
    if (!GC.getPythonCaller()->doPillageGold(*this, kPlot, iPillageGold))
    {
        // K-Mod C++ fallback calculation
        ...
    }

Consequences when USE_DO_PILLAGE_GOLD_CALLBACK is enabled:

1. Python returns -1 (or any other negative value except MIN_INT): the wrapper copies the negative
   value to iPillageGold and returns true. The C++ fallback is skipped; the later `> 0` test then
   awards no pillage gold.
2. The Python call fails and leaves the MIN_INT initializer unchanged: the wrapper asserts in an
   assertion build but still returns true. In Release, iPillageGold remains the caller's 0 and the
   C++ fallback is again skipped, yielding no pillage gold.

PRE-REFACTOR / K-MOD CONTRACT
------------------------------
The practical-1648 diff shows the exact displaced K-Mod logic:

    long lPillageGold = -1;
    int iPillageGold = -1;
    if (GC.getUSE_DO_PILLAGE_GOLD_CALLBACK())
    {
        ... callFunction(..., &lPillageGold);
        iPillageGold = (int)lPillageGold;
    }
    if (lPillageGold < 0)
    {
        // K-Mod. C version of the original python code
        ... calculate pillage gold in C++ ...
    }

Thus negative values were explicitly the fallback state, not merely an accidental result of a
particular Python implementation. Initialization to -1 also meant a failed/no-result Python call
fell through to the C++ calculation.

The stock BtS/Civ4CE CvGameUtils.doPillageGold implementation returns an ordinary nonnegative gold
amount, so default Python does not intentionally exercise the negative sentinel. The regression is
therefore a callback/error-path compatibility defect, not a default-config gameplay problem.
Current SAS sets USE_DO_PILLAGE_GOLD_CALLBACK = 0 by default.

SIBLING CONTROL: captureGold PRESERVES ITS ERROR FALLBACK
---------------------------------------------------------
The same practical-1648 refactor created CvPythonCaller::captureGold with an almost identical
MIN_INT sentinel, but that wrapper correctly distinguishes failure:

    if (lResult != iError)
    {
        iCaptureGold = toInt(lResult);
        return true;
    }
    else FAssert(lResult != iError);
    return false;

Its caller performs the C++ capture-gold calculation when false is returned. This sibling design
supports the interpretation that doPillageGold's unconditional `return true` is an accidental
loss of fallback semantics rather than an intentional policy that callback enablement must suppress
C++ fallback after Python failure.

ANCESTRY
--------
The root is introduced directly by AdvCiv practical 1648 (commit 553b1a0f8d), which extracted the
old CvUnit Python call into CvPythonCaller. Later AdvCiv history moves/reformats the pillage caller
but does not repair CvPythonCaller::doPillageGold's result contract. Base AdvCiv 1.14 and current
SAS retain it.

REPAIR BOUNDARY
---------------
Preserve the inherited and current successful modes:

- callback disabled -> return false so C++ computes pillage gold;
- callback enabled and Python returns a valid nonnegative amount -> write it, return true;
- callback enabled but Python returns a negative sentinel or the call fails -> return false and
  leave iPillageGold untouched so the existing C++ fallback runs.

A narrow repair can therefore treat `lResult < 0` as "no valid Python amount" and return false.
That naturally includes the MIN_INT call-failure initializer and restores K-Mod's old any-negative
fallback. Do not blindly clamp negative Python results to zero; zero is a valid explicit override
meaning no pillage gold, while a negative result historically requested/defaulted to C++ logic.

ROOT INDEPENDENCE
-----------------
F276 is independent of F274's nullable CyUnit construction and F275's positive-boolean narrowing.
It is a lost fallback/sentinel contract in the output-valued pillage callback, though all three
were exposed by auditing the practical-1648 extraction boundary.

ADDITIONAL CONTROLS / COVERAGE
-------------------------------
- History grep found no later CvPythonCaller doPillageGold repair after practical 1648; later
  commits touching the call site reorganize CvUnit pillaging but retain the wrapper contract.
- `captureGold` provides an internal same-refactor control showing that an output callback can and
  should return false when its Python result remains at an error sentinel.
- `updateColoredPlots` still has an uninitialized `long lResult` unlike the pre-refactor `= 0`;
  this remains a separate failure-path robustness lead, not folded into F276 without further
  sibling/error-contract review.

NEXT CURSOR
-----------
Continue from F277 unused / provisional KI#600 next. Highest-value active checks:
- determine whether updateColoredPlots's uninitialized result is an isolated failure-path defect
  or part of a wider practical-1648 default-initialization regression;
- systematically compare all map-script callback result/default/override semantics against the
  pre-1648/Civ4CE implementations, especially enum and pointer-return outputs;
- audit random-event mutable EventTriggeredData copy-back and output-pointer refresh contracts;
- inspect central call(...) exception/missing-module behavior and callback recursion guards;
- use later AdvCiv callback-fix commits (1662, 1677, 1850, 1917, 1966, 2460, 2531, 2571) as
  repair-boundary probes for unfixed siblings.
===============================================================================

===============================================================================
C024-WIP04 CONTINUATION - QUEUE 037 CvPythonCaller.cpp
===============================================================================
OPEN FILE STATE
---------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp - OPEN
F274 / provisional KI#597 - confirmed
F275 / provisional KI#598 - confirmed
F276 / provisional KI#599 - confirmed
F277 / provisional KI#600 - CONFIRMED PROVISIONAL ROOT
F278 unused / provisional KI#601 next

F277 / provisional KI#600
-------------------------
TITLE: CvPythonCaller::addFeatures falsely asserts when a valid map script omits the optional
       Python hook and falls back to C++ feature generation

CURRENT CODE ROOT
-----------------
Most optional map-generation phase wrappers explicitly suppress call-success assertions because
absence of the Python override is a normal signal to run the inherited C++ implementation:

    bool CvPythonCaller::addBonuses() const
    {
        call("addBonuses", m_python.getMapScriptModule(), false);
        return isOverride();
    }

    bool CvPythonCaller::addGoodies() const
    {
        call("addGoodies", m_python.getMapScriptModule(), false);
        return isOverride();
    }

    bool CvPythonCaller::addLakes() const
    {
        call("addLakes", m_python.getMapScriptModule(), false);
        return isOverride();
    }

    bool CvPythonCaller::addRivers() const
    {
        call("addRivers", m_python.getMapScriptModule(), false);
        return isOverride();
    }

But addFeatures uniquely leaves the default bAssertSuccess=true:

    bool CvPythonCaller::addFeatures() const
    {
        call("addFeatures", m_python.getMapScriptModule());
        return isOverride();
    }

The central no-result call wrapper does:

    m_bLastCallSuccessful = m_python.callFunction(...);
    FAssert(!bAssertSuccess || m_bLastCallSuccessful);

Thus a selected map module that simply omits addFeatures produces an assertion in an
assertion-enabled DLL even though `isOverride()` then correctly returns false and
CvMapGenerator::addFeatures continues with the normal C++ fallback.

LIVE CURRENT SAS TRIGGER
------------------------
Current SAS ships 50 PrivateMaps scripts. A direct current-data scan finds exactly one without a
`def addFeatures(` callback:

    PrivateMaps/SAS_Simple_Flat_Grass.py

This is not an accidental empty/broken script. It is a deliberately minimal playable SAS map
wrapper. Its only wildcard import is SAS_WorldSizeUtils, whose exported functions are world-size
helpers and do not define addFeatures. Therefore selecting this current map script reaches the
missing-hook path.

The practical consequence is build-dependent:

- Debug / Assert DLL: the missing optional hook makes m_bLastCallSuccessful false and triggers the
  FAssert in CvPythonCaller::call;
- Release DLL: the assertion is absent; isOverride() is safely false and CvMapGenerator performs
  its inherited C++ feature placement as intended.

This is therefore a false-positive diagnostic/assertion regression on a supported current SAS map,
not a Release-gameplay failure.

PRE-REFACTOR CONTRACT
---------------------
Immediately before practical 1648, CvMapGenerator::addFeatures used:

    if (gDLL->getPythonIFace()->callFunction(mapModule, "addFeatures", NULL))
    {
        if (!gDLL->getPythonIFace()->pythonUsingDefaultImpl())
            return;
    }
    // C++ feature generation follows

A missing/failed Python function therefore did not assert; it simply left the function in the C++
fallback path. Practical 1648 replaced that block with:

    if (GC.getPythonCaller()->addFeatures())
        return;

and the newly extracted wrapper accidentally retained call()'s assertion-on-success default.

INTERNAL / HISTORY CONTROL
--------------------------
The neighboring addBonuses/addGoodies/addLakes/addRivers wrappers created in the same extraction
all pass `false`, matching their optional/fallback nature. This makes addFeatures the local outlier.

AdvCiv practical 1662 provides an independent meaning-of-the-flag control: it explicitly changed
applyEvent to pass `false` because “Many of the apply functions named in XML don't exist.” That
history confirms callFunction failure for a missing Python function is exactly the condition the
bAssertSuccess parameter is meant to tolerate when absence is valid.

Base AdvCiv 1.14 currently bundles eight PrivateMaps scripts and all eight define addFeatures, so
the inherited wrapper bug is latent under Base's shipped map set. SAS's new minimal map script
makes the defect live without changing CvPythonCaller itself.

REPAIR BOUNDARY
---------------
Make addFeatures consistent with the other optional map phase hooks:

    call("addFeatures", m_python.getMapScriptModule(), false);

Do not force SAS_Simple_Flat_Grass.py to define a dummy function merely to satisfy the wrapper;
omitting the hook is an inherited valid way to request/default to C++ generation. Preserve
isOverride()/pythonUsingDefaultImpl behavior for scripts that do implement addFeatures.

ROOT INDEPENDENCE
-----------------
F277 is separate from F273. F273 accidentally removes legitimate assertions from the optimized
Assert build by gating richer diagnostics with `_DEBUG`; F277 is the opposite kind of diagnostic
contract error: it emits an assertion on a valid optional-callback fallback. The repairs and code
roots are independent.

ADDITIONAL RANDOM-EVENT CONTROL
-------------------------------
The doEventTrigger pointer-output refresh was rechecked against practical 1648 and later practical
1966 (“Fix recently introduced bug in random event processing”). Practical 1966 added guards for
invalid city/unit IDs. Its pOtherPlayerCity assignment can leave the pre-call pointer unchanged
when Python changes the other-city ID to INVALID_INDEX, whereas pre-1648 getCity(-1) would normally
clear it. Current shipped random-event Python was searched for mutations of the actual triggered
data. Several callbacks mutate city/player/plot fields, but no current can-trigger callback was
found that changes an initially valid other-player-city ID to -1. Keep this as a mutation/output
consistency control, not F278 at present.

NEXT CURSOR
-----------
Continue from F278 unused / provisional KI#601. Revisit updateColoredPlots's uninitialized
failure result; audit all map enum/index return bounds and missing/default behavior; then move
through event mutation, ordinary callback return sentinels and later practical-history repairs.
===============================================================================

===============================================================================
C024-WIP05 CONTINUATION - QUEUE 037 CvPythonCaller.cpp
===============================================================================
OPEN FILE STATE
---------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp - OPEN
F274 / provisional KI#597 - confirmed
F275 / provisional KI#598 - confirmed
F276 / provisional KI#599 - confirmed
F277 / provisional KI#600 - confirmed
F278 / provisional KI#601 - CONFIRMED PROVISIONAL ROOT
F279 unused / provisional KI#602 next

F278 / provisional KI#601
-------------------------
TITLE: CvPythonCaller::updateColoredPlots consumes an uninitialized Python result when the
       callback call fails, losing the inherited deterministic C++ fallback

CURRENT CODE ROOT
-----------------
CvPythonCaller::updateColoredPlots is the only function in the current file that declares a bare
uninitialized result variable:

    bool CvPythonCaller::updateColoredPlots() const
    {
        long lResult;
        call("updateColoredPlots", lResult);
        return toBool(lResult);
    }

The central call wrapper records Python call success separately:

    m_bLastCallSuccessful = m_python.callFunction(..., &lResult);
    FAssert(!bAssertSuccess || m_bLastCallSuccessful);

and CvDLLPythonIFaceBase exposes callFunction as a bool-returning interface with an output pointer;
there is no DLL-side contract that the output is written when the call returns false. Therefore,
if Python fails before producing a long result, updateColoredPlots still evaluates the untouched
stack value through toBool().

The behavior depends on build/error handling:

- Debug / Assert: the central FAssert reports the failed call, but if execution continues/Ignore is
  chosen then the indeterminate result is still consumed;
- Release: the assertion vanishes and the uninitialized value is consumed immediately. It can
  spuriously return true and skip all inherited C++ colored-plot generation, or false by accident.

This is undefined behavior in C++, not merely an arbitrary fallback choice.

PRE-REFACTOR / K-MOD CONTROL
----------------------------
Immediately before AdvCiv practical 1648 extracted this callback, CvGame::updateColoredPlots used:

    long lResult = 0;
    gDLL->getPythonIFace()->callFunction(PYGameModule,
            "updateColoredPlots", NULL, &lResult);
    if (lResult == 1)
        return;

Thus a failed/no-result Python call deterministically left zero in place and continued into the
normal C++ colored-plot implementation. Current K-Mod 1.46 retains that same `lResult = 0`
initialization, and BUG's current default for updateColoredPlots is false, matching the fallback.

ANCESTRY
--------
The bug is introduced directly by AdvCiv practical 1648 (553b1a0f8d, "Moved DLL-to-Python calls
and logging into separate classes"). The same commit removes `long lResult = 0` from
CvGameInterface.cpp and creates CvPythonCaller::updateColoredPlots with `long lResult;`.
Base AdvCiv 1.14 retains the regression; SAS inherits it unchanged.

TRIGGER / SEVERITY BOUNDARY
---------------------------
The normal SAS PYGameModule and CvGameInterface.updateColoredPlots function exist, and BUG supplies
an ordinary false default, so normal successful calls do not exercise the bad value. The live
trigger is a Python call failure/exception/no-result path (for example while developing/reloading
Python or after a script error). updateColoredPlots is invoked repeatedly by ordinary UI/game
state changes, so once the Python side is broken the path is reachable without special XML callback
enablement.

This is therefore best classified as an error-path robustness/undefined-behavior regression rather
than a default healthy-script gameplay defect. It is still stronger than a mere assertion issue:
Release builds can make a control-flow decision from indeterminate storage.

REPAIR BOUNDARY
---------------
Restore the inherited false/default state locally:

    long lResult = false; // or = 0

before the call. Do not attempt to solve this by globally changing call() to overwrite all output
arguments on failure; other wrappers intentionally use distinct sentinels such as MIN_INT, -1,
NO_TECH and caller-provided defaults. updateColoredPlots simply needs its own historical default
restored.

ROOT INDEPENDENCE
-----------------
F278 is independent of F274-F277. It is an uninitialized-output regression on Python call failure,
not nullable wrapper construction (F274), numeric boolean-contract narrowing (F275), lost negative
fallback sentinel (F276), or optional map-hook assertion policy (F277).

ADDITIONAL CONTROL
------------------
A file-wide declaration scan found no second bare `long lResult;` in current CvPythonCaller.cpp.
All other ordinary long-result wrappers initialize through ARGSLIST(default) or explicit sentinel/
default variables. Therefore F278 is a localized extraction omission, not evidence that every
callback return is uninitialized on failure.

NEXT CURSOR
-----------
Continue from F279 unused / provisional KI#602. Audit map-script enum/index/vector return bounds and
hybrid/default semantics; then random-event mutable output refresh, ordinary callback sentinel/
truthiness contracts, and later practical-history fixes. Keep malformed-script-only bounds issues
as controls unless current SAS assets or a supported fallback path make them live.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP06 — F279 / provisional KI#602 confirmed
===============================================================================

F279 / provisional KI#602
-------------------------
TITLE
Active EVENT_FARM_BANDITS_2 names a nonexistent PythonHelp function, while
CvPythonCaller::eventHelp treats Python-help dispatch as mandatory and asserts on failure.

CURRENT DATA / CODE
-------------------
Current SAS CIV4EventInfos.xml contains this active (not commented-out) field:

    <Type>EVENT_FARM_BANDITS_2</Type>
    ...
    <PythonHelp>getHelpFarmBandit2</PythonHelp>

A comment-aware scan of the complete current random-event Python namespace finds no definition of
`getHelpFarmBandit2` in Assets/Python/EntryPoints/CvRandomEventInterface.py.

The wrapper is strict:

    CvWString CvPythonCaller::eventHelp(EventTypes eEvent,
            EventTriggeredData const* pTriggered) const
    {
        char const* szHelp = GC.getInfo(eEvent).getPythonHelp();
        if (std::strlen(szHelp) <= 0)
            return L"";
        CvWString szResult;
        CyArgsList argsList;
        argsList.add(eEvent);
        argsList.add(m_python.makePythonObject(pTriggered));
        m_bLastCallSuccessful = m_python.callFunction(PYRandomEventModule, szHelp,
                argsList.makeFunctionArgs(), &szResult);
        FAssert(m_bLastCallSuccessful);
        if (std::wcslen(szResult) == 0)
            return L"";
        szResult.insert(0, NEWLINE);
        return szResult;
    }

CvGameTextMgr::setEventHelp unconditionally appends eventHelp(...) while constructing an event
choice's displayed help, so this is a display-time callback contract rather than an apply-event-only
path.

REACHABILITY
------------
EVENT_FARM_BANDITS_2 is one of the three choices of the active
EVENTTRIGGER_FARM_BANDITS trigger. Current XML gives that trigger:

    <iPercentGamesActive>85</iPercentGamesActive>
    <iWeight>200</iWeight>
    ...
    <Events>
        <Event>EVENT_FARM_BANDITS_1</Event>
        <Event>EVENT_FARM_BANDITS_2</Event>
        <Event>EVENT_FARM_BANDITS_3</Event>
    </Events>

and the recurring EVENTTRIGGER_FARM_BANDITS_AGAIN also includes the same event choices. Thus the
missing callback is not dead/commented XML.

The choice's actual effects are already represented in ordinary EventInfo fields (not Python):
among them iUnitImmobileTurns=2 and iFood=-5. There is no PythonCallback or PythonCanDo attached to
the event. That makes the dangling PythonHelp look like stale data rather than evidence that a
missing custom behavior needs to be reconstructed.

OBSERVED CONTRACT CONSEQUENCE
-----------------------------
When help for EVENT_FARM_BANDITS_2 is built:

- Assert/Debug builds: the failed call reaches `FAssert(m_bLastCallSuccessful)`, producing a false
  assertion on a valid shipped random event.
- Release builds: the failed call yields no custom Python help; eventHelp returns an empty string
  after the failed dispatch. The generic C++ help for the XML-defined effects is still constructed.

This is therefore primarily a shipped-data/diagnostic defect rather than a gameplay-state defect,
but it is live under ordinary random-event play and stronger than a malformed-mod-only input.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same active XML PythonHelp token and likewise has no corresponding
Python definition. K-Mod also contains the same XML token and no definition. The mismatch is
therefore inherited rather than SAS-created.

A first raw-text namespace scan appeared to find additional missing hooks such as
canTriggerIndependentFilms/getHelpIndependentFilms. That was a false positive because SAS preserves
their removed values inside XML comments. Re-running the scan after stripping XML comments proves
that all active trigger eligibility hooks resolve:

- EventTrigger PythonCanDo: 72 unique, 0 missing.
- EventTrigger PythonCanDoCity: 9 unique, 0 missing.
- EventTrigger PythonCanDoUnit: 1 unique, 0 missing.
- EventInfo PythonCanDo: 31 unique, 0 missing.
- EventInfo PythonExpireCheck: 6 unique, 0 missing.

The only active EventInfo PythonHelp name missing from current random-event Python is
`getHelpFarmBandit2`.

The EventInfo PythonCallback namespace has 11 missing apply* names. Those are a separate, known
contract: AdvCiv practical 1662 explicitly changed applyEvent dispatch to `bAssertSuccess=false`
because many XML-named apply functions legitimately do not exist. They do not strengthen F279.

REPAIR BOUNDARY
---------------
The narrowest evidence-supported repair is data-side:

    <PythonHelp/>

for EVENT_FARM_BANDITS_2.

Do not invent a new Python function merely to satisfy the stale name unless historical upstream
material establishes an intended extra help string. All currently visible gameplay effects of the
choice are already encoded in XML and therefore described through the ordinary event-help path.

A broader wrapper change that suppresses all eventHelp failures would hide genuinely misspelled
PythonHelp names elsewhere and is not justified by this single bad shipped entry.

ROOT INDEPENDENCE
-----------------
F279 is independent of F274-F278. Its root is an active shipped XML -> Python symbol mismatch. It
does not depend on nullable CyUnit construction, truth-value narrowing, sentinel fallback,
optional-map-hook assertion policy, or uninitialized result storage.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 — UNUSED / provisional KI#603 next

NEXT CURSOR
-----------
Continue with the ordinary callback return/default contracts and output-vector ownership:
upgradePrice/experienceNeeded fallback sentinels, enum-return wrappers, generatePlotTypes /
generateTerrainTypes vector assumptions, and central call/isOverride reentrancy semantics. Then
cross-check the remaining practical history fixes and later SAS-only deltas before judging
saturation. Do not manufacture F280 if those branches close as controls.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP07 — F279 ancestry refinement + F280 / provisional KI#603 confirmed
===============================================================================

F279 ANCESTRY REFINEMENT
------------------------
The WIP06 wording needs one important distinction.

The dangling shipped XML token

    <PythonHelp>getHelpFarmBandit2</PythonHelp>

is inherited: Base AdvCiv and K-Mod contain the same active token and no matching random-event
Python definition.

However, K-Mod's pre-extraction event-help path did NOT assert that the Python dispatch succeeded.
It simply called:

    gDLL->getPythonIFace()->callFunction(
        PYRandomEventModule, kEvent.getPythonHelp(), ..., &szHelp);

and then appended the resulting help string.

AdvCiv practical 1648 moved this into CvPythonCaller::eventHelp and added:

    m_bLastCallSuccessful = m_python.callFunction(...);
    FAssert(m_bLastCallSuccessful);

Therefore F279 has two layers:

1. inherited BtS/K-Mod shipped-data defect: one active PythonHelp symbol has no implementation;
2. AdvCiv practical-1648 policy regression: the old silent missing-help mismatch becomes a repeatable
   Assert/Debug failure when that shipped event choice's help is built.

The repair recommendation remains data-side `<PythonHelp/>`, because the event's actual effects are
already encoded generically in XML and suppressing all eventHelp dispatch failures would hide real
future misspellings.


F280 / provisional KI#603
-------------------------
TITLE
Practical 1648 changed the getUnitCostMod application threshold from `> 1` to `> 0`, so callback
result 1 now reduces unit production cost to 1% instead of preserving the inherited no-override
behavior.

CURRENT CODE
------------
CvPythonCaller::unitCostMod returns the callback integer:

    int CvPythonCaller::unitCostMod(PlayerTypes ePlayer, UnitTypes eUnit) const
    {
        if (!isUse(GET_UNIT_COST_MOD))
            return 0;
        ARGSLIST(0);
        argsList.add(ePlayer);
        argsList.add(eUnit);
        call("getUnitCostMod", argsList, lResult);
        return toInt(lResult);
    }

CvPlayer::getProductionNeeded(UnitTypes) then does:

    int iPyMod = GC.getPythonCaller()->unitCostMod(getID(), eUnit);
    if (iPyMod > 0)
    {
        iProductionNeeded *= iPyMod;
        iProductionNeeded /= 100;
    }
    return std::max(1, iProductionNeeded);

So a result of exactly 1 applies a 1% multiplier. Integer division plus the final clamp means many
ordinary units collapse to 1 hammer.

INHERITED CONTRACT / REGRESSION PROOF
-------------------------------------
Immediately before practical 1648, the same CvPlayer code was:

    long lResult = 0;
    gDLL->getPythonIFace()->callFunction(
        PYGameModule, "getUnitCostMod", ..., &lResult);

    if (lResult > 1)
    {
        iProductionNeeded *= lResult;
        iProductionNeeded /= 100;
    }

K-Mod 1.46 independently retains the same `if (lResult > 1)` contract.

The building-cost sibling also used `> 1` before extraction and CURRENT SAS still preserves it:

    int iPythonModifier = GC.getPythonCaller()->buildingCostMod(*this, eBuilding);
    if (iPythonModifier > 1)
    {
        iProductionNeeded *= iPythonModifier;
        iProductionNeeded /= 100;
    }

Thus the unit-side `> 0` is not a general redesign of the cost callbacks. It is an isolated
extraction-time threshold drift.

BUG/PYTHON DEFAULT CROSS-CHECK
------------------------------
BUG/BugGameUtils.py declares:

    self._setDefault("getUnitCostMod", -1)
    self._setDefault("getBuildingCostMod", -1)

The C++ wrapper's local failure/disabled default of 0 is behaviorally harmless under the inherited
`> 1` consumer contract: -1, 0 and 1 are all non-overrides. The strong bug is therefore not the
wrapper's 0 versus BUG's -1 in isolation; it is that the CURRENT unit consumer accidentally made
1 active.

TRIGGER / SEVERITY
------------------
Current SAS ships:

    USE_GET_UNIT_COST_MOD_CALLBACK = 0

so ordinary default SAS gameplay does not trigger F280. This is a callback/modding-configuration
compatibility regression.

When the callback is enabled, however, result 1 is not malformed according to the inherited C++
interface in the sense relevant here: old K-Mod/AdvCiv explicitly accepted it as a non-applying
result. Any existing callback/mod that returns 1 therefore experiences a drastic behavior change
after the extraction.

REPAIR BOUNDARY
---------------
Restore the inherited consumer threshold only:

    if (iPyMod > 1)

This exactly matches:
- pre-practical-1648 AdvCiv,
- K-Mod 1.46,
- the current building-cost sibling.

Do not change the meaning of values >=2, and do not globally reinterpret all Python integer
callbacks.

Optionally, the wrapper's local default could later be documented against BUG's -1 default, but
changing it is unnecessary for F280 because both values remain inert once the proper `> 1` guard is
restored.

ROOT INDEPENDENCE
-----------------
F280 is independent of F274-F279. It is a percentage-application threshold regression at the
CvPythonCaller consumer boundary. It is not the canBuild truthiness narrowing of F275: here the
callback's numeric value is itself a percentage modifier, and the regression is specifically the
consumer changing an inherited threshold.

ADDITIONAL CONTROLS FROM THIS PASS
----------------------------------
1. BUG game-callback default audit:
   The canonical BUG defaults and CvPythonCaller local defaults line up semantically across the
   ordinary boolean/sentinel callbacks inspected. `getUnitCostMod` / `getBuildingCostMod` are the
   only conspicuous textual -1-vs-0 pair; under the proper >1 consumer contract that textual
   difference does not itself change behavior.

2. Ordinary fallback sentinels:
   `upgradePrice` and `experienceNeeded` return -1 when disabled/failed, and both CvUnit consumers
   explicitly treat `<0` as "use native C++ computation". No lost fallback there.

3. Recommended UI enums:
   recommended tech/unit/building wrappers initialize to NO_TECH/NO_UNIT/NO_BUILDING and the popup
   consumers fall back to native AI recommendations. Out-of-range non-sentinel values remain a
   malformed-Python robustness issue only; no current shipped callback produces them.

4. PrivateMaps custom-option namespace:
   46 shipped scripts define getNumCustomMapOptions; every one of those 46 also defines both
   getCustomMapOptionDefault and getCustomMapOptionDescAt. The four scripts without the count
   function are no-option/special cases handled by the tolerant count wrapper. No second F277-style
   optional-hook assertion was found there.

5. PYGameModule entry-point namespace:
   All 67 literal current game-module callback names used by CvPythonCaller resolve in the current
   CvGameInterface entry-point module. There is no second current shipped missing-symbol analogue
   to the random-event PythonHelp mismatch.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
Continue the practical-1648 old-vs-extracted semantic table, especially:
- city/player/unit boolean callback truthiness where old code used !=0 or >0;
- output/sentinel callbacks and call-failure behavior;
- generatePlotTypes/generateTerrainTypes output ownership and required-hook policy;
- central m_bLastCallSuccessful / pythonUsingDefaultImpl sequencing and reentrancy;
- later practical fixes as sibling-invariant clues.

Treat truthiness broadening/narrowing as a control unless the old contract is explicit or a current
callback can exercise the differing value. Do not allocate F281 merely because the file has already
yielded many extraction regressions.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP08 — F277 map-default siblings + F281 remains unused
===============================================================================

F277 SAME-ROOT EXPANSION — SAS_Simple_Flat_Grass intentionally relies on native/default plot and terrain generation too
---------------------------------------------------------------------------------------------------------------
The WIP04 F277 root originally covered the uniquely strict `addFeatures()` wrapper:

    call("addFeatures", m_python.getMapScriptModule());

which falsely asserts when the supported current SAS map `SAS_Simple_Flat_Grass.py` omits that
optional hook and correctly falls back to the native C++ feature generator.

A deeper current-map audit shows that the same supported map intentionally omits two earlier map
generation callbacks as well:

    generatePlotTypes
    generateTerrainTypes

The map file contains only:
- a description callback;
- compact almost-all-land `getGridSize`;
- an import from callback-free `SAS_WorldSizeUtils`.

This is deliberate architecture, not an accidentally truncated script. Current project comments
and history describe it as a playable "simple flat-grass sandbox map" / "almost all land" wrapper.
Before the helper split, the predecessor playable `SAS_WorldSizes.py` likewise exposed only the
description/grid-size callbacks.

WHY THE MINIMAL MAP STILL WORKS
-------------------------------
CvMapGenerator has a native fallback when Python does not provide an overriding plot list:

    if (!GC.getPythonCaller()->generatePlotTypes(paiPlotTypes, iNumPlots))
    {
        for (...)
            paiPlotTypes[i] = PLOT_LAND;
    }

`CvPlot::setPlotType(PLOT_LAND, ...)` then assigns the configured LAND_TERRAIN whenever the plot's
terrain is still NO_TERRAIN:

    if (getTerrainType() == NO_TERRAIN ||
        GC.getInfo(getTerrainType()).isWater() != isWater())
    {
        setTerrainType(isWater() ? ... : eLAND_TERRAIN, ...);
    }

Thus the native plot fallback itself supplies the intended all-land/default-land-terrain baseline.
The later no-override `generateTerrainTypes` path can legitimately leave that terrain in place.
This locally explains the documented flat-grass sandbox behavior without inventing a missing
Python generator.

ADVCIV DIAGNOSTIC REGRESSION
----------------------------
Despite those inherited fallback semantics, practical 1648 introduced hard diagnostic assertions
inside the extracted wrappers:

    if (!isOverride())
    {
        FErrorMsg("Map script has to override generatePlotTypes and mustn't call usingDefaultImpl");
        return false;
    }

and:

    if (!isOverride())
    {
        FErrorMsg("Map script has to override generateTerrainTypes and mustn't call usingDefaultImpl");
        return false;
    }

`FErrorMsg` is simply `FAssertMsg(false, msg)`, so an assertion-enabled build flags the map even
though the C++ fallback path is coherent and, for this current supported SAS script, intentional.

Pre-practical-1648 K-Mod/AdvCiv did not assert either condition. It simply tested:

    callFunction(...) && !pythonUsingDefaultImpl()

and otherwise:
- generated all PLOT_LAND for plot types;
- left terrain unchanged for terrain generation.

Therefore practical 1648 converted normal inherited default-implementation/fallback states into
false diagnostics, just as the strict `addFeatures` extraction did for a missing optional hook.

ROOT CLASSIFICATION
-------------------
Do NOT allocate F281 for these two additional assertions.

They are best recorded as same-root expansion of F277:
"map-script fallback/default implementation is valid, but extracted CvPythonCaller diagnostics
treat fallback as an error."

The concrete code sites differ:
- generatePlotTypes hard FError on !isOverride;
- generateTerrainTypes hard FError on !isOverride;
- addFeatures uses call()'s default bAssertSuccess=true;

but all three are exercised by the same supported deliberately-minimal current SAS map and all
three were non-asserting fallback paths before the extraction.

REPAIR BOUNDARY
---------------
F277's original narrow repair remains valid for addFeatures:

    call("addFeatures", mapModule, false);

For generatePlotTypes / generateTerrainTypes, the evidence says that !isOverride itself cannot
universally be diagnosed as an error, because current SAS intentionally supports a map relying on
those fallbacks. Remove/downgrade those FErrorMsg calls (or otherwise exclude the legitimate
default/fallback state) while retaining:
- the result-size diagnostics when an actual overriding Python result has the wrong size;
- the native all-land plot fallback;
- the current terrain already established through setPlotType.

Do not add dummy Python generators merely to silence the wrapper. That would duplicate native
behavior and obscure the intended minimal-script contract.

ADDITIONAL TRUTHINESS CONTROL
-----------------------------
A current shipped-Python scan was performed for literal integer returns from game callbacks whose
practical-1648 extraction moved them through CvPythonCaller::toBool (exactly TRUE == 1).

No current shipped callback implementation was found returning a literal positive integer >1 in
those boolean roles. Therefore the historical nonzero/positive -> exact-one narrowing remains:
- a confirmed F275 root where canBuild's own interface explicitly documents >=1 as yes;
- same-root compatibility evidence for canPlaceBonusAt/canPlaceGoodyAt, whose old C++ bool return
  accepted any nonzero positive result;
- a control for generic boolean callbacks without a current differing-value trigger or explicit
  broader contract.

Do not create F281 merely from theoretical `return 2` behavior in ordinary boolean callbacks.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional; same-root map fallback expansion above
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
Continue practical-1648 semantic comparison and then central wrapper-state/lifetime review:
- m_bLastCallSuccessful / pythonUsingDefaultImpl sequencing under nested/reentrant Python calls;
- vector-return ownership/size behavior and map result enum bounds;
- bCheckExists module-load behavior;
- remaining later AdvCiv callback-fix commits as sibling clues;
- constructor/destructor callback-cache lifetime and GlobalDefine recache/update interactions if any.

Close differences as controls unless there is a locally provable call/data path. No need to force
F281 despite the high finding count already produced by practical 1648.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP09 — central-state / mutability / fallback-policy controls
===============================================================================

F281 remains UNUSED / provisional KI#604 next.

RANDOM-EVENT MUTABILITY — CONTROL, NOT A NEW ROOT
-------------------------------------------------
`CvPythonCaller::doEventTrigger` takes:

    EventTriggeredData const& kTriggered

yet passes `&kTriggered` to Python and then deliberately rereads fields with the comment:

    // Python may have changed kTriggered

At first glance this looks like mutation through a const C++ view. The live caller establishes the
important ownership fact, however:

    EventTriggeredData* pTriggerData = addEventTriggered();
    ...
    GC.getPythonCaller()->doEventTrigger(getID(), *pTriggerData, ...)

The underlying object is genuinely mutable and owned by CvPlayer's event-trigger store. The const
reference is merely the wrapper function's local C++ view; it is not an object originally declared
const. Current random-event Python demonstrably uses this historical mutation channel, and the C++
caller explicitly rereads the resulting IDs/types afterward.

`CvDLLPythonIFaceBase::makePythonObject` is a templated non-owning Boost.Python wrapper creator.
Passing `&kTriggered` deduces a const-qualified pointer at that call site, but this interface has
longstanding binding machinery for EventTriggeredData and current shipped hooks successfully mutate
fields exposed through the Python class. No locally demonstrated corruption or mutation of a truly
const object was found.

Classification:
- const-correctness / API-shape debt;
- historical mutable Python data contract;
- not F281 absent a concrete call path with genuinely const storage.

CENTRAL `m_bLastCallSuccessful` / `isOverride` REENTRANCY — CONTROL
-------------------------------------------------------------------
All current uses of `isOverride()` consume the state immediately after the specific wrapper call
whose override status they query. There is no intervening CvPythonCaller invocation on the C++ path.

A nested Python -> DLL -> Python call can temporarily modify the shared member while the outer
`m_python.callFunction(...)` is executing, but the outer statement is:

    m_bLastCallSuccessful = m_python.callFunction(...);

so the outer call's success value is assigned only after the RHS returns, overwriting any temporary
nested value before the outer caller reaches `isOverride()`.

`pythonUsingDefaultImpl()` remains EXE-owned/opaque state, but K-Mod and pre-extraction code used
the same immediate "callFunction, then pythonUsingDefaultImpl" protocol. No current local
counterexample was found.

No F281.

NULLABLE CY-WRAPPER FAMILY — F274 IS LOCALIZED
-----------------------------------------------
A constructor/use scan found no second live F274-style dereference of a nullable raw gameplay
pointer.

Notably:
- `doGoody` remains the exceptional bad case: it dereferences nullable `CvUnit const* pUnit` into a
  reference-taking CyUnit constructor.
- `cannotHandleActionOverride(CvPlot* pPlot, ...)` passes `pPlot` to the pointer-taking `CyPlot`
  constructor and therefore preserves nullability rather than dereferencing it.
- Other reference-taking Cy wrappers are reached through caller contracts that supply live objects.

This strengthens F274's precision rather than broadening it.

ENABLED-CALL FAILURE DEFAULTS FOR POSITIVE PERMISSION CALLBACKS — CONTROL
--------------------------------------------------------------------------
Practical 1648 changed several wrappers from old direct-call local result `0` to an initialized
`true` result before dispatch, including:
- `canPickPlot`
- `citiesDestroyFeatures`
- `canRazeCity`
- `canDeclareWar`

Therefore an enabled callback that *fails to execute* can now fall back permissively where the old
direct code failed closed.

However, BUG/BugGameUtils.py independently declares the canonical callback defaults as True for
all four:
- canPickPlot -> True
- citiesDestroyFeatures -> True
- canRazeCity -> True
- canDeclareWar -> True

The refactor therefore appears to align call-failure/default initialization with each callback's
normal Python/native default, rather than accidentally choosing arbitrary true values. Healthy
callback behavior is unchanged.

This is materially different from:
- F276, where a failed/negative pillage callback suppresses an explicit C++ native calculation;
- F278, where a failed callback leaves an indeterminate stack value;
- F280, where a valid inherited numeric return changes meaning.

Without a healthy shipped trigger or explicit inherited requirement that exceptions must deny the
action, this fail-open-on-error policy change is ambiguous/arguably deliberate graceful
degradation. Keep it as a compatibility/error-policy control, not F281.

PRACTICAL-1648 / LATER-FIX ORACLE PASS — CURRENT STATUS
--------------------------------------------------------
Later AdvCiv repairs were used as sibling-invariant clues. Important examples remain:
- practical 1662: missing random-event apply functions are intentionally tolerated;
- 1677: saved-game custom-map-option / wrap assertion handling;
- 1917: explorer-placement inversion correction;
- 1966: invalid random-event object IDs;
- 2019: map scripts can perform terrain-related work in nonstandard phases;
- 2460 / 2531: map-module existence and load-if-necessary handling;
- later map-option availability fixes.

Current neighboring paths were checked against these repaired invariants. No new independent root
has emerged from them beyond F274-F280 and the F277 same-root fallback expansion.

MAP/STARTING-LOCATION RETURN CONTROLS
-------------------------------------
- `riverStartCardinalDirection`: shipped map implementations either return valid cardinal values or
  request the default implementation. No current out-of-range positive enum return.
- `findStartingArea`: NULL is the established signal to run native area selection.
- `findStartingPlot`: NULL is the established native fallback; Tectonics' special -10 case is
  explicitly known/handled diagnostically.
- `isBonusIgnoreLatitude`: preserves the old peculiar -1/default-implementation semantics.
- `minStartingDistanceMod`: current SAS already contains the earlier KI#285 repair restoring the
  inherited negative range down to -100.
- human explorer placement: current SAS already contains the later inversion repair.

No F281 from these paths.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional; map-default siblings recorded
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
Finish the remaining high-fanout branches rather than starting another broad reread:
1. vector/list output ownership and size/value validation (`generatePlotTypes`,
   `generateTerrainTypes`, `getGridSize`);
2. module existence / bCheckExists / saved-game behavior and no-argument `call` overload parity;
3. callback-cache/global-define lifetime versus any live runtime mutations;
4. constructor/destructor and every remaining direct return family for missing initialization;
5. independent CE/K-Mod divergence and final function-family coverage inventory.

If these remain controls, proceed to repair-boundary checks for F274-F280 and close queue 037
without allocating F281.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP10 — remaining map/module/cache controls; F281 remains unused
===============================================================================

F281 remains UNUSED / provisional KI#604 next.

GETGRIDSIZE / WORLD-SIZE SENTINEL — CONTROL
-------------------------------------------
Every current shipped PrivateMaps `getGridSize` implementation was enumerated. The common
NO_WORLDSIZE convention is `return []`; `Planet_Generator_0_68.py` is the conspicuous exception:

    if (argsList[0] == -1): # (-1,) is passed to function on loads
        return (-1,-1)

At first glance this looks dangerous because CvPythonCaller::mapGridDimensions accepts any result
with size >=2 and only *asserts* positivity before assigning the dimensions.

The live DLL caller bounds the issue:

    if (pInitInfo != NULL)
        use pInitInfo dimensions;
    else
    {
        WorldSizeTypes eWorldSize = GC.getInitCore().getWorldSize();
        if (eWorldSize != NO_WORLDSIZE)
            ... mapGridDimensions(eWorldSize, ...);
    }

Thus the local DLL path never calls `mapGridDimensions` with NO_WORLDSIZE/-1. Saved-map reset uses
pInitInfo dimensions instead. Planet Generator's comment evidently describes an external
EXE/Python callback circumstance, not this C++ wrapper call.

Current shipped normal-world-size returns are positive tuples or helpers that resolve to positive
dimensions. No live F281.

The wrapper still has malformed-script robustness debt:
- it accepts vectors with >2 values whereas the old/stock code required exactly 2;
- positivity is Assert-only, so a malformed override can install <=0 dimensions in Release.
Both are inherited/third-party malformed-input concerns with no shipped trigger found.

FINDSTARTINGAREA -1 COLLAPSE — CONTROL
--------------------------------------
Pre-practical-1648 code explicitly accepted a Python override result of -1:

    if (result == -1 || m.getArea(result) != NULL)
    {
        areas_by_value.push_back(std::make_pair(result,1));
        return areas_by_value;
    }

Current CvPythonCaller maps -1 to GC.getMap().getArea(-1) == NULL, and the caller interprets NULL as
"no Python area override", proceeding into native area selection.

This is a semantic drift, but downstream inspection weakens rather than strengthens it as a live
bug. Current AdvCiv's `findStartingAreas`/`findStartingPlot` multi-area algorithm filters plots by
exact area id from `areasByValue`. An overriding singleton `(-1,1)` would match no real plot and can
drive starting-plot search to "Could not find starting plot." It is therefore not credible evidence
that current AdvCiv intentionally uses overriding -1 to mean "all areas."

Current shipped scripts support that interpretation:
- scripts that return -1 do so together with `CyPythonMgr().allowDefaultImpl()`;
- ordinary overriding scripts return a real area id;
- no shipped script was found intentionally overriding with -1.

Keep the old explicit allowance as historical API oddity / third-party compatibility seam. Do not
allocate F281 without evidence of a real map that depended on overriding -1.

RUNTIME USE_*_CALLBACK FLAG REFRESH — CONTROL
----------------------------------------------
K-Mod's older generic `CvGlobals::setDefineINT` called full `cacheGlobals()`. This incidentally
meant a runtime:

    gc.setDefineINT("USE_..._CALLBACK", ...)

would refresh the corresponding cached callback guard.

However the loss of that dynamic behavior is NOT a practical-1648 CvPythonCaller regression.
AdvCiv practical 1631 ("Performance optimizations for access to XML data") had already changed
setDefineINT to pinpoint `cacheGlobalInts(szChangedDefine, iNewValue)`. Its changed-define branch
explicitly returned before the callback-flag reload block, with the source comment:

    return; // Don't bother checking the Python callback flags

Practical 1648 later removed those old CvGlobals callback fields and had CvPythonCaller /
CvDllPythonEvents snapshot the flags in arrays. That preserves practical 1631's already-static
assumption rather than newly changing a supported runtime contract.

Current repository scan finds no `setDefineINT`/Python write to any `USE_*_CALLBACK` define.
Current live setDefine uses concern camera/minimap/river-generation values, not callback guards.

Classification:
- K-Mod-to-AdvCiv modding compatibility change;
- explicitly assumed static by pre-CvPythonCaller AdvCiv;
- no current SAS trigger;
- not F281.

MODULE EXISTENCE / SAVED-GAME PATH — CLEAN CONTROL
---------------------------------------------------
The four private `call` overloads all use the same bCheckExists protocol. When requested they call:

    m_python.moduleExists(szModuleName, true)

and set m_bLastCallSuccessful=false without dispatch if the module cannot be loaded.

History establishes why this exists:
- practical 2460 added module-existence checks to prevent a Python error when the Settings tab
  examines a save whose original map script is no longer installed;
- practical 2531 changed bLoadIfNecessary from false to true to recover cleanly after Python reload
  errors when possible.

Current consumers line up with that policy:
- numCustomMapOptions tolerates absence and returns initialized 0;
- customMapOptionDefault asserts success only for a non-saved game; saved games may return
  NO_CUSTOM_MAPOPTION, and CvMap::getNonDefaultCustomMapOptionDesc explicitly treats negative
  default as "couldn't be loaded (script may have been removed)";
- customMapOptionDescription independently tolerates failure for saved games and returns an empty
  string;
- CvInitCore::refreshCustomMapOptions checks `gDLL->pythonMapExists` before querying a script.

No stale `m_bLastCallSuccessful` use was found after the module-missing branch.

MAP LATITUDE / WRAP PAIRING — CONTROL
--------------------------------------
Practical 1648 changed latitude handling from an inherited all-or-nothing pair to independent
top/bottom application. That is a semantic difference if a third-party map overrides only one
latitude function. Current shipped map scan finds:
- 16 maps defining getTopLatitude;
- exactly the same 16 defining getBottomLatitude;
- no one-sided latitude implementation.

Wrap behavior deliberately remains all-or-nothing. Current shipped scan likewise finds the same
41 scripts on both getWrapX and getWrapY.

No current trigger and no stronger inherited contract evidence: no F281.

MAP PLOT-PERCENT / DEFAULT-OPTION OUTPUTS — CONTROL
----------------------------------------------------
All current getNumPlotsPercent implementations were inspected. Their normal world-size paths return
positive percentage values; none returns zero/negative for a supported world size. The wrapper
initializes to 100 and its caller consumes iModifier only when isOverride() is true.

Current `isClimateMap` / `isSeaLevelMap` implementations return ordinary 0/1 / False/True values.
The wrapper's default `1` corresponds to "option flexible" and is a safe permissive fallback if a
hook is absent/fails.

No F281.

VECTOR OUTPUT OWNERSHIP — CURRENT CALLERS CLEAN
-----------------------------------------------
`generateTerrainTypes` receives a fresh empty local vector from CvMapGenerator::generateTerrain on
every call. Its `reserve` therefore cannot preserve stale elements from an earlier generation.
Wrong output size is diagnosed and rejected before the caller indexes the vector.

`generatePlotTypes` similarly checks exact output size before copying into the caller-owned array.
On failure the caller overwrites every plot entry with PLOT_LAND.

Neither wrapper individually validates each returned enum value. A malformed third-party map could
therefore supply an invalid PlotTypes/TerrainTypes integer. No current shipped trigger has been
identified; this remains robustness debt, not F281.

CALLBACK-GUARD STORAGE LIFETIME — CLEAN LOCALLY
-----------------------------------------------
CvPythonCaller owns exactly one `bool[NUM_CALLBACK_DEFINES]` allocation and deletes it in its
destructor. Macro-generated string and enum counts are asserted equal. Current callback guard
values are read after GlobalDefines have loaded, when CvGlobals::cacheGlobals constructs the
CvPythonCaller.

The broader question of whether CvGlobals::cacheGlobals can repeat in one global lifetime was
already audited under queue 040 and not locally proven; do not duplicate that speculative lifecycle
seam as a queue-037 root.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional; same-root map fallback expansion
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
Complete the last function-family/repair-boundary closure:
- inspect early UI/popup wrappers for argument/module/lifetime drift;
- finish city/player/unit callback consumer parity against practical 1648/K-Mod where not already
  covered;
- inspect every direct enum/int result for a current range/sentinel trigger;
- independent CE/K-Mod divergence pass for the confirmed roots and any surviving candidates;
- final coverage inventory + root-independence/repair-boundary table.

If those remain clean, close queue 037 with F274-F280 and leave F281 unused.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP11 — F275 same-root expansion + UI/screen/callback-namespace controls
===============================================================================

F281 remains UNUSED / provisional KI#604 next.

F275 SAME-ROOT EXPANSION — practical 1648 narrowed several inherited Python truth contracts to exact 1
----------------------------------------------------------------------------------------------------
F275 was promoted from the strongest explicit contract:

    canBuild:
        -1 = ignore / no Python override
         0 = cannot build
         1 or greater = can build

Current CvPythonCaller::toBool instead returns true only for exactly TRUE/1:

    static bool toBool(long l)
    {
        FAssert(l >= 0);
        return (l == TRUE);
    }

A complete practical-1648 old-condition audit now proves that canBuild was not the only callback
whose inherited consumer accepted a broader Python truth value.

Pre-extraction callbacks whose healthy-result semantics were broader than exact 1 include:

1. canBuild
   old:
       if (lResult >= 1)
           return true;
   current:
       return toBool(lResult);
   This remains F275's strongest contract because the accompanying Python interface comment
   explicitly documents "1 or greater" as the affirmative result.

2. isPlayerResearch
   old:
       if (lResult == 0)
           return false;
       // any nonzero result allowed research
   current:
       return toBool(lResult);

3. canDeclareWar
   old:
       if (lResult == 0)
           return false;
       // any nonzero result allowed declaration
   current:
       return toBool(lResult);

4. unitCannotMoveInto
   old:
       if (lResult != 0)
           return true;
   current:
       return toBool(lResult);

5. cannotSpreadReligion
   old:
       if (lResult > 0)
           return true;
   current:
       return toBool(lResult);

6. CvGame::isVictory
   old:
       if (lResult == 0)
           return false;
       // any nonzero result was true
   current:
       return toBool(lResult);

7. CvGame::isVictoryTest
   old:
       if (lResult == 0)
           return false;
       // any nonzero result was true
   current:
       return toBool(lResult);

8. canPickPlot / canPickRevealedPlot
   old:
       if (lResult == 0)
           ...
       // any nonzero result was affirmative
   current:
       return toBool(lResult);

9. map canPlaceBonusAt / canPlaceGoodyAt
   old:
       if (result >= 0)
           return result;
   The C++ return type was bool, so every positive integer converted to true.
   current:
       validates the nonnegative override, then returns toBool(lResult);
   Thus positive values >1 changed from true to false.

10. canRazeCity has a particularly instructive split inherited contract.
    The old acquireCity call site used:
        if (lResult == 1)
    whereas CvPlayer::canRaze used:
        if (lResult == 0)
            return false;
    so positive values >1 were accepted by the ordinary canRaze query but not by the acquisition
    branch. Practical 1648 unified both callers through one exact-1 CvPythonCaller::canRaze wrapper.
    This preserves acquireCity's old condition but narrows CvPlayer::canRaze.

These are best treated as SAME-ROOT F275 evidence rather than independent findings. The common
refactor mechanism is that practical 1648 imposed one exact-1 boolean conversion onto callbacks
whose inherited call sites did not all share exact-1 semantics.

WHY THIS IS NOT A CLAIM THAT ALL PYTHON BOOLS MUST ACCEPT ANY NONZERO VALUE
---------------------------------------------------------------------------
The same practical-1648 audit also finds many inherited callbacks that already used `== 1`.
Those are semantically preserved by toBool and should not be swept into F275 merely because Python
itself normally treats nonzero integers as truthy.

Examples whose old condition was already exact 1 include:
- citiesDestroyFeatures;
- canTrain / cannotTrain;
- canConstruct / cannotConstruct;
- canCreate / cannotCreate;
- canMaintain / cannotMaintain;
- doGrowth / doCulture / doPlotCulture / doProduction / doReligion / doGreatPeople / doMeltdown;
- AI_chooseProduction;
- skipProductionPopup / showExamineCityButton / skipResearchPopup / showTechChooserButton;
- doReviveActivePlayer / doHolyCity;
- createBarbarianCities / createBarbarianUnits;
- cannotSelectionListMove / cannotSelectionListGameNetMessage;
- cannotHandleAction;
- cannotDoControl;
- cannotFoundCity;
- canFoundCitiesOnWater;
- canResearch;
- canDoCivic / cannotDoCivic;
- doGold / doResearch;
- AI_doDiplo / AI_doWar;
- doCombat;
- isActionRecommended;
- AI_unitUpdate.

This distinction matters: F275 is an inherited-contract-preservation issue, not a proposal to make
every numeric Python callback use generic Python truthiness.

RANDOM-EVENT WRAPPERS CONFIRM THAT EXACT 1 IS NOT A UNIVERSAL BRIDGE CONTRACT
------------------------------------------------------------------------------
Current CvPythonCaller itself deliberately preserves nonzero truth semantics for several
random-event callbacks, e.g. by returning:

    (lResult != 0)

rather than toBool(lResult). Existing source comments even question whether the usual `== 1`
conversion would be suitable there.

This is useful internal evidence that the bridge does not have one universal "only integer 1 is
true" convention. Each callback's inherited/native contract matters.

CURRENT-DATA / SEVERITY BOUNDARY
--------------------------------
All 54 CvPythonCaller USE_*_CALLBACK GlobalDefines were checked against current SAS XML:
- 54 expected;
- all 54 exist exactly once;
- all are integer 0/1 values;
- all 54 are currently 0.

Therefore F275 and several neighboring guarded-callback regressions are not active under SAS's
default configuration. They remain real compatibility/configuration bugs because the callback API
is intentionally retained and old K-Mod/AdvCiv semantics are externally observable once a flag is
enabled.

No current shipped healthy callback was found returning literal positive integer >1 in one of the
broader-boolean roles. Do not exaggerate default-gameplay severity.

REPAIR BOUNDARY FOR F275
------------------------
A global change to toBool is unsafe because numerous callbacks intentionally inherited exact-1
semantics.

Repair must therefore preserve the affected callback's own old contract, for example:
- canBuild: explicit >=1 handling while keeping -1 as no override;
- inherited nonzero callbacks: `lResult != 0` where compatibility is desired;
- inherited positive callbacks: `lResult > 0`;
- canPlaceBonusAt / canPlaceGoodyAt: nonnegative validation plus normal C++ bool conversion
  semantics (or explicit `lResult != 0`);
- canRaze needs deliberate consideration because its two old call sites were not themselves
  semantically identical.

The minimum F275 repair can target canBuild alone if scope is deliberately limited to the explicit
documented regression, while the sibling list above should be reviewed as compatibility follow-up.
Do not blindly broaden toBool globally.


UI / SCREEN CALLBACK NAMESPACE — CLEAN CURRENT-DATA CONTROL
-----------------------------------------------------------
The remaining early CvPythonCaller UI/popup family was audited against current Python rather than
assuming names were valid.

Literal PYScreensModule callbacks used by CvPythonCaller:
- WorldBuilderGetHighlightPlot
- pediaMain
- pediaShowHistorical
- refreshMilitaryAdvisor
- showForeignAdvisorScreen
- showHallOfFame
- showInfoScreen

All seven exist in the current `Assets/Python/EntryPoints/CvScreensInterface.py`.

The dynamic call sites were then resolved from all current C++ callers:
- showPythonScreen("X") -> showX;
- callScreenFunction("...");
- jumpToPedia(..., "Type") -> pediaJumpToType;
- pedia main/history/back/forward calls;
- camera-start helper.

32 resolved current screen function names were checked and all 32 exist. No current shipped
missing-symbol sibling to F279 was found in the screen interface.

POPUP MODULE DEFAULTING — APPARENT BUG REJECTED
------------------------------------------------
`Assets/Python/Contrib/TriggerEvent.py` appears at first to create a popup with:

    setOnClickedPythonCallback("triggerRandomEvent")

without setting a Python module. CvPythonCaller::onOKClicked defaults such popups to
PYScreensModule, which would look wrong if TriggerEvent expected its own module.

Current BUG configuration resolves the contract explicitly:

    Assets/Config/Trigger Event.xml
        <export function="triggerRandomEvent" to="CvScreensInterface"/>

Thus the callback is intentionally exported into the default screen module. Random-event popups that
need CvRandomEventInterface explicitly set their Python module. No bug.

`onFocus` similarly uses the screen module, and current focus callback setters resolve to functions
available there. K-Mod used the same module convention.

POPUP CALLBACK STRING LIFETIME — CLEAN
--------------------------------------
`CvPopupInfo` callback getters return `const CvString&`. CvPythonCaller::onOKClicked obtains
`.c_str()` and uses it while the owning popup object remains alive for the duration of the call.
No temporary-string dangling pointer was found.

WORLD-BUILDER HIGHLIGHT EXTRACTION — IMPROVEMENT, NOT REGRESSION
-----------------------------------------------------------------
Pre-extraction code tested only whether the returned coordinate vector was nonempty and then indexed
both element 0 and element 1. A one-element Python result could therefore read out of bounds.

Current CvPythonCaller requires at least two elements before constructing the plot. Practical 1648
improved this malformed-result boundary.

SEND-EMAIL RESULT INITIALIZATION — IMPROVEMENT
-----------------------------------------------
The old sendEmail path used an uninitialized local result. The extracted wrapper initializes the
result before dispatch. This is the opposite of F278 and is useful evidence that the uninitialized
updateColoredPlots result is an isolated extraction omission rather than a broad intentional policy.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional
F275 / KI#598 — confirmed provisional; same-root truth-semantics expansion above
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional; same-root map fallback expansion
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
Finish the remaining practical-1648 parity table, with special attention to:
- mixed/sentinel numeric callbacks;
- direct enum return values and current range guarantees;
- any old result comparison not reproduced by the extracted consumer.

Then perform the independent K-Mod/Civ4CE comparison and final function-family inventory.
If no independent root emerges, run repair-boundary/root-independence checks and close queue 037
without allocating F281.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP12 — F274 ancestry correction; F275 bound detail; independent-history controls
===============================================================================

F281 remains UNUSED / provisional KI#604 next.

F274 ANCESTRY CORRECTION — practical 2066, not practical 1648, introduced the NULL dereference
------------------------------------------------------------------------------------------------
Earlier queue-037 notes correctly identified the live defect but were too broad about its origin.

Practical 1648's initial CvPythonCaller extraction preserved the nullable-pointer construction:

    // Note: pUnit can be NULL, but pyUnit can still be initialized (and is expected by Python).
    CyUnit* pyUnit = new CyUnit(const_cast<CvUnit*>(pUnit));

That is behaviorally consistent with the older K-Mod/CvPlot path, which constructed a CyUnit from
the nullable CvUnit pointer. The CyUnit pointer-taking constructor is capable of representing NULL.

The actual regression was introduced later by AdvCiv practical 2066, commit ffe04c3efd,
whose title is:

    Avoid some const_casts

Its diff is unusually self-documenting:

    // Note: pUnit can be NULL, but pyUnit can still be initialized (and is expected by Python).
-   CyUnit* pyUnit = new CyUnit(const_cast<CvUnit*>(pUnit));
+   CyUnit* pyUnit = new CyUnit(*pUnit);

The comment explicitly preserves the nullable contract while the new reference-taking expression
dereferences that nullable pointer before the CyUnit constructor is entered.

Therefore F274's precise ancestry is:
- K-Mod / pre-2066 AdvCiv: nullable pointer preserved;
- practical 1648: extraction still preserved it;
- practical 2066: const-correctness cleanup changed pointer construction to reference construction
  and created the NULL dereference;
- current SAS inherits the practical-2066 regression.

This strengthens confidence in the root and narrows review of the repair. Restoring the
pointer-taking construction (or adding an equivalent const-pointer CyUnit overload) preserves the
old nullable Python-object contract without redesigning doGoody.

A later AdvCiv fix is a useful sibling control: practical 2571 changed
cannotHandleActionOverride's plot plumbing to a pointer precisely where a plot can be NULL and uses
the pointer-taking CyPlot constructor. That neighboring repair reinforces that optional gameplay
objects should stay pointer-shaped across the Python wrapper boundary.

F275 EXTRA CONTRACT DETAIL — documented result 2 also trips FAssertBounds
---------------------------------------------------------------------------
Current canBuild contains:

    FAssertBounds(-1, 2, lResult);
    if (lResult == -1)
        return false;
    bOverride = true;
    return toBool(lResult);

`FAssertBounds(lower, upper, value)` uses a half-open upper bound. Thus this check accepts:
    -1, 0, 1

but rejects:
    2

That matters because the inherited/Python-facing canBuild contract explicitly says:
    -1 = ignore/no override
     0 = cannot build
     1 or greater = can build

So a perfectly valid affirmative callback result of 2 has two regressions:
1. Assert/Debug: it trips the bounds assertion;
2. all builds: exact-1 toBool converts it to false.

This is still the same F275 root, not a separate diagnostic finding. A repair that restores >=1
truth semantics must also widen/remove the incompatible half-open bounds assertion.

INDEPENDENT K-MOD / CIV4CE CONFIRMATION OF THE SEVEN ROOTS
-----------------------------------------------------------
An independent pass through K-Mod 1.46 and current Civ4CE was used to challenge the proposed
ancestry/semantics rather than relying only on AdvCiv history.

F274:
- K-Mod doGoody passes the nullable CvUnit pointer into CyUnit instead of dereferencing it.
- practical-1648 AdvCiv initially did the same after extraction.
- practical 2066 is therefore the pinpoint introduction.

F275:
- K-Mod and Civ4CE CvPlot::canBuild both retain:
      if (lResult >= 1) return true;
      else if (lResult == 0) return false;
  independently confirming the broader documented affirmative range.

F276:
- K-Mod initializes pillage Python result to -1 and executes the native C++ calculation whenever
  the callback remains negative.
- Current extracted wrapper's "enabled means handled" behavior therefore loses a concrete inherited
  fallback contract.

F277:
- K-Mod and Civ4CE CvMapGenerator test whether generatePlotTypes / generateTerrainTypes /
  addFeatures are actually supplied/overridden and otherwise continue through native generation.
- They do not treat ordinary native/default fallback itself as an assertion failure.
- This independently supports the current minimal SAS map's use of fallback.

F278:
- K-Mod and Civ4CE initialize the updateColoredPlots Python result to 0 before dispatch.
- Current CvPythonCaller is the divergent uninitialized form.

F279:
- K-Mod carries the same active EVENT_FARM_BANDITS_2 XML PythonHelp token with no matching Python
  implementation, confirming the stale data is inherited.
- K-Mod's old event-help call path did not assert dispatch success; the repeatable assertion layer
  is therefore an AdvCiv extraction-policy change.

F280:
- K-Mod and Civ4CE both retain:
      if (lResult > 1)
  for getUnitCostMod.
- Current SAS's building-cost sibling also retains >1.
- The unit-side >0 condition is consequently isolated threshold drift, not a broad callback-policy
  redesign.

POST-1648 HISTORY ORACLE — LATER FIXES MOSTLY EXPLAIN EXISTING ROOTS / CONTROLS
-------------------------------------------------------------------------------
Every later AdvCiv commit materially touching CvPythonCaller found in the local commit-diff mirror
was used as a "what did the author later discover?" oracle.

Relevant repairs / changes include:
- practical 1662: tolerate missing random-event apply functions instead of asserting them;
- practical 1677: saved-game custom-map-option / wrap handling becomes tolerant where scripts may
  be unavailable;
- practical 1850: no-assert numCustomMapOptions because a legitimate map lacks the hook;
- practical 1917: repair the human-explorer-placement truth inversion;
- practical 1966: reject/handle invalid random-event object IDs and sentinels;
- practical 2019: acknowledge that Planet Generator may do terrain work during addFeatures;
- practical 2066: introduces F274 while removing const_casts;
- practical 2460: add map-module existence protection;
- practical 2503: changes map-generation assertions from one assert macro to FErrorMsg, but does not
  resolve F277's underlying false-diagnostic fallback policy;
- practical 2531: module-existence checking may load the module if necessary;
- practical 2571: pointer-shaped cleanup of a separately nullable CvPlot path;
- later mechanical/refactor commits do not expose an adjacent unrepaired root.

SAS later independently repaired several old callback-semantics defects elsewhere:
- KI#304: found-value callback integer/sentinel contract;
- KI#285: negative minStartingDistanceMod range;
- KI#570: river override/default hybrid semantics.

Those later repairs are useful saturation evidence. They explain several suspicious historical
differences that otherwise might look like new queue-037 findings, while leaving F274-F280 as
independent surviving roots.

DIRECT ENUM / SENTINEL RETURN FAMILY — NO F281 SO FAR
------------------------------------------------------
The remaining raw enum/int return wrappers were compared to their consumers and inherited behavior.

- recommendedTech / recommendedUnit / recommendedBuilding:
  initialize to NO_* sentinels; popup consumers fall back to native AI recommendations.
- AI_chooseTech:
  initializes to NO_TECH; caller has a native fallback.
- conscriptUnitOverride:
  uses NO_UNIT as no-override sentinel; raw malformed positive IDs were also raw-cast historically.
- customMapOptionDefault:
  uses NO_CUSTOM_MAPOPTION when the module/default cannot be loaded in the allowed saved-game path.
- riverStartCardinalDirection:
  starts from NO_CARDINALDIRECTION and supports default implementation; malformed positive enum
  values remain third-party robustness debt, not a current regression.
- findStartingPlot:
  preserves the known special -10/default convention and native fallback.
- findStartingArea:
  old -1 acceptance remains a historical API seam, but current downstream multi-area logic and all
  shipped scripts argue against treating an overriding -1 as a useful supported result.
- upgradePrice / experienceNeeded:
  negative means native computation; current consumers preserve it.
- random-event eligibility/expiry:
  current wrappers deliberately retain nonzero truth semantics rather than toBool.

No current shipped invalid enum return was found. Missing range validation therefore remains
malformed-Python robustness debt unless a concrete current callback is found.

NULLABLE PYTHON-OBJECT CONSTRUCTION — F274 REMAINS LOCALIZED
-------------------------------------------------------------
The wrapper's Cy* construction family was rechecked after the practical-2066 discovery.

The significant nullable sites are:
- doGoody: BAD — pointer is explicitly nullable but current code dereferences it (F274).
- cannotHandleActionOverride: SAFE — nullable CvPlot* is passed to a pointer-taking CyPlot
  constructor after practical 2571.
- eventHelp: live caller CvGameTextMgr checks pTriggeredData and returns before invoking eventHelp
  when NULL.
- random-event city/unit objects: validity checks and NO_* sentinel handling introduced by later
  repairs prevent the formerly invalid-ID construction cases.
- ordinary city/player/unit callbacks take references whose callers supply live objects.

No second live F274-style optional-pointer dereference was established.

CURRENT QUEUE STATE
-------------------
queue 037 CvGameCoreDLL/CvPythonCaller.cpp — OPEN
F274 / KI#597 — confirmed provisional; ancestry corrected to practical 2066
F275 / KI#598 — confirmed provisional; broader truth-semantics siblings; result 2 also asserts
F276 / KI#599 — confirmed provisional
F277 / KI#600 — confirmed provisional; same-root native-map-fallback expansion
F278 / KI#601 — confirmed provisional
F279 / KI#602 — confirmed provisional
F280 / KI#603 — confirmed provisional
F281 — UNUSED / provisional KI#604 next

NEXT CURSOR
-----------
The substantive semantic/history audit is now close to saturation.

Before closure:
1. finish direct current-data checks for map cardinal/default-option enum returns;
2. inspect doPlayerScore and any remaining initialized-output/error-sentinel wrappers against their
   pre-extraction behavior;
3. run a final direct-call / call-overload coverage inventory so no small callback family was
   skipped;
4. perform explicit root-independence and repair-boundary checks for F274-F280;
5. determine the next reconciled priority queue from the album rather than guessing it.

If no independent root survives those checks, close queue 037 with F274-F280 and leave F281 unused.
===============================================================================


===============================================================================
QUEUE 037 CONTINUATION — CvPythonCaller.cpp
C024-WIP13 — final current-data/output/inventory controls; closure challenge
===============================================================================

F281 remains UNUSED / provisional KI#604 next.

SHIPPED RIVER-CARDINAL RETURN DOMAIN — CLEAN
--------------------------------------------
Exactly four current PrivateMaps define getRiverStartCardinalDirection:

1. Great_Plains.py
   Returns CARDINALDIRECTION_EAST or CARDINALDIRECTION_WEST.

2. Inland_Sea.py
   Returns NORTH/SOUTH/WEST/EAST on exhaustive ordinary branches.

3. Team_Battleground.py
   Returns EAST/WEST for the left-vs-right layouts, NORTH/SOUTH for the top-vs-bottom layout,
   and calls CyPythonMgr().allowDefaultImpl() for the remaining layout.

4. Planet_Generator_0_68.py
   Calls CyPythonMgr().allowDefaultImpl() unconditionally.

Thus the current wrapper's lack of an explicit upper-bound enum check has no shipped invalid-value
trigger. Negative override results are rejected and default-implementation paths leave the native
cardinal-direction selection untouched.

CUSTOM-MAP DEFAULT DOMAIN — STOCK RANDOM -1 CONVENTION, NOT INVALID CURRENT DATA
---------------------------------------------------------------------------------
A static literal-table audit was possible for 38 current map scripts whose
getNumCustomMapOptionValues / getCustomMapOptionDefault functions use directly recoverable option
dictionaries.

Every ordinary default lies within:
    0 <= default < numValues

except six defaults whose value is -1:
- Tilted_Axis option 0;
- Ring options 1 and 2;
- Wheel option 1;
- Hub options 1 and 2.

Every one of those six options is independently marked:
    isRandomCustomMapOption(...) == true

This is the longstanding map-script convention for a random default, not evidence of a malformed
positive CustomMapOptionTypes value. The current CvMap display helper also deliberately treats a
negative default as a no-description/default-like state.

No F281.

doPlayerScore OUTPUT SENTINEL — CURRENT FAILURE BEHAVIOR IS A FALLBACK IMPROVEMENT
----------------------------------------------------------------------------------
Current wrapper:

    int const iError = MIN_INT;
    ARGSLIST(iError);
    ...
    call("calculateScore", argsList, lResult);
    if (lResult != iError)
        iScore = toInt(lResult);
    else FAssert(...);

At first glance this differs substantially from the pre-practical-1648 path, where the score result
was initialized to 0 and the Python result was returned directly.

The surrounding practical-1648 change is decisive: the same refactor moved the normal CvGameUtils
score calculation into C++. CvPlayer::calculateScore now computes a complete native score first and
passes it by reference to doPlayerScore only as an optional callback override.

Therefore:
- callback disabled -> preserve native C++ score;
- callback succeeds -> replace it with Python result;
- callback fails / does not write the MIN_INT sentinel -> assert diagnostically and preserve the
  already-computed native score.

This is coherent graceful fallback, not an output-sentinel regression. It differs from F276 because
pillage's caller explicitly expects "not handled" to trigger a native calculation that current
doPillageGold wrongly suppresses.

OTHER OUTPUT/SENTINEL WRAPPERS — CLEAN
--------------------------------------
Final direct comparison reconfirmed:
- captureGold:
  MIN_INT means wrapper did not produce a valid override; caller can keep its fallback/current
  value. Zero remains a valid explicit capture-gold result.
- upgradePrice:
  negative result means use native C++ price, preserving inherited behavior.
- experienceNeeded:
  negative result means native C++ XP calculation, preserving K-Mod behavior.
- AI_chooseTech / recommendations / conscript:
  NO_* sentinels preserve native-selection fallbacks.
- mapGridDimensions:
  output vector is checked for at least two elements before indexing; live DLL callers do not pass
  NO_WORLDSIZE.
- mapPlotsPercent:
  starts at 100 and only installs the result when an override exists.
- latitude/wrap/default-option outputs:
  current shipped scripts use paired hooks where pairing matters; no one-sided current-data trigger.
- riverValue / minStartingDistance / foundValue:
  previously suspicious extraction semantics have already been repaired by later AdvCiv/SAS work
  (including the current KI#570/KI#285/KI#304 lineage).
- findStartingPlot:
  preserves its special default/sentinel behavior.
- random-event booleans:
  deliberately retain nonzero truth semantics.

No independent F281 emerged.

ASSERT / HARD-DIAGNOSTIC INVENTORY — ALL CURRENT HITS CLASSIFIED
-----------------------------------------------------------------
Every FAssert/FError site in CvPythonCaller.cpp was rechecked:

- callback-tag array size:
  compile/static configuration invariant; clean.
- WB highlight short vector:
  current code is safer than old size>0/index-two behavior.
- multiplayer popup OOS warning:
  inherited/intentional.
- central call bAssertSuccess:
  expected strictness for mandatory callbacks; F277/F279 are the proved cases where the chosen
  policy conflicts with supported/current optional data.
- Pitboss email:
  inherited K-Mod had both call-success and zero-result assertions; current initialized result is
  safer, not weaker.
- eventHelp:
  F279.
- doPlotCulture ePlayer:
  caller contract; no NO_PLAYER producer found.
- captureGold / doPillageGold / doPlayerScore error sentinels:
  capture/score fallback clean; doPillageGold is F276.
- canBuild bounds:
  F275, including the documented valid value 2 assertion.
- water-city plot assertion:
  caller supplies water plot.
- custom-map description saved-game exception:
  deliberately repaired in later AdvCiv history.
- grid positive dimensions:
  malformed-script robustness only; current normal returns positive and the DLL excludes
  NO_WORLDSIZE.
- map-generation no-override FErrors:
  F277.
- wrong-size plot/terrain generation:
  valid diagnostic when an actual overriding map returns the wrong list length.
- canPlaceItem/river/cardinal negative checks:
  compatible with current shipped return domains; no invalid current trigger.
- findStartingArea / findStartingPlot:
  current shipped sentinel/default patterns understood; no independent root.
- callback output sentinels:
  classified above.

There is no remaining unclassified assertion/error site.

ALLOCATED CY* OBJECT OWNERSHIP — BALANCED
-----------------------------------------
A mechanical scan finds 36 explicit `new Cy...` allocations in CvPythonCaller.cpp:
- 22 pyCity;
- 9 pyPlot;
- 4 pyUnit;
- 1 pyGroup.

The corresponding delete counts match exactly for each variable family. Dispatch failure does not
bypass those deletes in the current straight-line wrappers.

This does not cure F274: its failure occurs while evaluating `*pUnit` before the CyUnit object can
be constructed. But no second allocation/deletion imbalance or early-return leak was established.

PYTHON CALL OVERLOAD / MAP OPTIONALITY INVENTORY
-------------------------------------------------
All four private `call` overloads were compared:
- args + result;
- result only;
- args only;
- no args/no result.

They consistently:
1. optionally check/load module existence;
2. set m_bLastCallSuccessful=false when required module is absent;
3. otherwise assign the actual callFunction success result;
4. assert only according to bAssertSuccess.

Every map-script wrapper that is known/allowed to be optional passes bAssertSuccess=false EXCEPT
`addFeatures`, already captured by F277. The two direct vector-return map calls bypass `call`, but
their false-diagnostic `!isOverride()` policy is also already included in F277.

No second optional-map-hook assertion root remains.

FUNCTION-FAMILY COVERAGE INVENTORY
----------------------------------
The current .cpp contains 125 CvPythonCaller method definitions including constructor, four private
call overloads and isOverride. All substantive families have now received at least one direct
contract/current-data/history pass:

- construction / callback guard cache / lifetime;
- screen/popup/Pedia/UI dispatch;
- WorldBuilder / colored plots;
- central call overloads and success/default state;
- random-event trigger/help/apply/expiry/data mutation;
- city train/construct/create/maintain/cost/turn hooks;
- city capture/raze;
- plot/build/goody/founding;
- player research/civics/cost/expenses/found value/AI tech/diplo;
- team war/organization-tech callbacks;
- unit/group combat/action/movement/spread/pillage/upgrade/XP/AI update;
- map module existence/custom options/grid/plot percentage/latitude/wrap/default availability;
- map plot/terrain generation and bonus/goody/feature/lake/river hooks;
- map river direction/value/start placement/distance/latitude;
- game score/revive/holy-city/barbarian/victory callbacks.

The direct `long lResult` inventory also reconfirms that updateColoredPlots is the sole bare
uninitialized result local; every other result is initialized through ARGSLIST or an explicit
assignment.

CURRENT ROOT-INDEPENDENCE CHECK
-------------------------------
F274-F280 remain independent roots:

F274 — nullable C++ pointer destroyed at Python-wrapper construction boundary.
F275 — broader inherited numeric truth contract narrowed to exact integer 1.
F276 — negative/error pillage result no longer signals native fallback.
F277 — legitimate map-script native/default fallback diagnosed as failure.
F278 — uninitialized Python result consumed on failed colored-plot dispatch.
F279 — active shipped XML names nonexistent PythonHelp; AdvCiv asserts its failed help dispatch.
F280 — unit-cost percentage application threshold changed from >1 to >0.

None requires another finding's condition to reproduce, and no single repair below fixes two of
them accidentally.

REPAIR-BOUNDARY CHECK
---------------------
F274:
- restore pointer-shaped CyUnit construction for nullable pUnit (or add a const-pointer constructor);
- do not change non-null doGoody Python argument shape.

F275:
- restore each affected callback's inherited truth contract where chosen;
- canBuild's minimum repair is >=1 affirmative plus a compatible range assertion;
- do NOT globally redefine toBool because many inherited callbacks intentionally used ==1.

F276:
- if the enabled Python pillage callback fails or returns a negative result, return false so the
  caller executes native C++ pillage-gold calculation;
- preserve 0 and positive values as explicit Python overrides.

F277:
- missing/default map hooks should return "not overridden" without a false diagnostic;
- keep diagnostics for an actual overriding plot/terrain list with the wrong length;
- addFeatures should use non-asserting optional dispatch.

F278:
- initialize updateColoredPlots result to 0, restoring K-Mod/Civ4CE/pre-extraction deterministic
  fallback behavior.

F279:
- narrow data repair: clear EVENT_FARM_BANDITS_2's stale PythonHelp token unless historical evidence
  establishes intended custom help;
- do not globally silence valid event-help misspelling diagnostics.

F280:
- restore `if (iPyMod > 1)` at the unit-cost consumer;
- preserve all values >=2 exactly as before and keep <=1 as non-overrides.

All seven boundaries preserve already-correct sibling behavior.

CLOSURE CHALLENGE
-----------------
At this point:
- every method family is covered;
- every hard diagnostic is classified;
- every explicit allocation is ownership-balanced;
- current map/event callback namespaces were checked;
- callback guard configuration was checked against current XML;
- practical-1648 semantic extraction was compared against old call-site conditions;
- later AdvCiv repairs were used as sibling-invariant clues;
- independent K-Mod/Civ4CE behavior was used to challenge all seven roots;
- current shipped enum/default/sentinel data were checked where the wrapper relies on them;
- all seven repairs have narrow, independent boundaries.

No credible independent F281 remains.

The next step is therefore to promote queue 037 to FIRST-PASS COMPLETE with F274-F280, leave F281
unused, and select the next still-PENDING `priority next wip` queue from the live queue table.
===============================================================================


===============================================================================
C025 DURABLE — QUEUE 037 CLOSED; QUEUE 042 OPENED
===============================================================================

QUEUE 037 — CvGameCoreDLL/CvPythonCaller.cpp
STATUS: FIRST-PASS COMPLETE

CONFIRMED ROOTS
F274 / KI#597 — nullable CvUnit pointer destroyed at Python-wrapper construction boundary in doGoody; pinpoint introduction practical 2066.
F275 / KI#598 — inherited numeric truth contract narrowed to exact integer 1; canBuild explicitly documents 1 or greater and current bounds also reject 2.
F276 / KI#599 — negative/error Python pillage result no longer signals native fallback.
F277 / KI#600 — legitimate native/default map-script fallback diagnosed as error; supported SAS_Simple_Flat_Grass relies on it.
F278 / KI#601 — updateColoredPlots consumes the only bare uninitialized long result in the wrapper on failed dispatch.
F279 / KI#602 — active EVENT_FARM_BANDITS_2 names nonexistent getHelpFarmBandit2; extracted strict help assertion exposes the inherited mismatch.
F280 / KI#603 — unit-cost callback threshold drifted from inherited >1 to >0.
F281 remains UNUSED.

SATURATION
125 method definitions covered; every hard diagnostic classified; Cy* allocation ownership checked; current callback namespaces/data checked; practical-1648 and later-history parity reviewed; K-Mod/Civ4CE independently challenged all roots; repair boundaries/root independence verified.

PROGRESS: 24 / 169 first-pass files complete.

-------------------------------------------------------------------------------
QUEUE 042 — CvGameCoreDLL/CvDeal.cpp
STATUS: OPEN
F281 UNUSED / provisional KI#604 next.

Opening focus: deal lifetime/ownership; cancellation/doTurn/verify; startTrade/endTrade symmetry and directionality; peace/vassal/permanent-alliance state transitions; persistence; AI/diplomacy side effects; SAS-vs-Base/K-Mod/Civ4CE/history comparison; previous-album archaeology.

No repository source files modified.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP01 — F281 / provisional KI#604 confirmed
===============================================================================

F281 / provisional KI#604
-------------------------
TITLE
CvDeal::isPeaceDeal scans only the first saved trade list, so an ordinary one-sided AI reparations
peace treaty saved only in the second list is never recognized as a peace deal and can remain
force-peace indefinitely.

CURRENT CODE
------------
Current CvDeal::isPeaceDeal is:

    bool CvDeal::isPeaceDeal() const
    {
        FOR_EACH_TRADE_ITEM(getFirstList())
        {
            if (pItem->m_eItemType == getPeaceItem())
                return true;
        }
        return false;
    }

It never examines getSecondList().

This matters because current AdvCiv/SAS does NOT require the peace treaty item to be stored on both
sides. CvPlayerAI::AI_negotiatePeace explicitly says:

    AI_counterPropose (and the EXE) don't like peace treaties on both sides

and deliberately inserts the treaty on only one side.

The relevant branch is:

    if (weGive.getLength() <= 0 &&
        (theyGive.getLength() > 0 || iTheirBenefit > iOurBenefit))
    {
        theyGive.insertAtEnd(peaceTreaty);
    }
    else
        weGive.insertAtEnd(peaceTreaty);

For AI-AI peace it then calls:

    GC.getGame().implementDeal(getID(), eOther, weGive, theyGive);

`CvGame::implementAndReturnDeal` preserves this orientation:

    pDeal->init(..., eWho, eOtherWho);
    pDeal->addTradeItems(kOurList, kTheirList, ...);

and CvDeal processes/saves kFirstList as the first player's gives list and kSecondList as the second
player's gives list.

LIVE SECOND-LIST-ONLY CONSTRUCTION
----------------------------------
The normal branch:

    if (iTheirBenefit > iOurBenefit)
        // "They need to give us sth."

can add reparations to `theyGive`, leaving `weGive` empty. The treaty-placement rule then inserts
TRADE_PEACE_TREATY into `theyGive`, not `weGive`.

During CvDeal::addTradeItems:
- one-shot reparations such as gold/tech/city are applied but not saved;
- TRADE_PEACE_TREATY returns bSave=true.

A perfectly ordinary resulting persistent deal can therefore be:

    firstList  = empty
    secondList = [TRADE_PEACE_TREATY]

with team-level setForcePeace already enabled.

FAILURE CHAIN
-------------
1. The treaty begins normally and setForcePeace is true.
2. CvGame::verifyDeals calls CvDeal::verify at game/deal turn boundaries.
3. CvDeal::verify is supposed to expire peace deals once their minimum duration becomes cancelable:

       if (... || (isCancelable(NO_PLAYER) && isPeaceDeal()))
           kill();

4. After PEACE_TREATY_LENGTH turns:
       isCancelable(NO_PLAYER) == true
   but:
       isPeaceDeal() == false
   because the treaty is only in secondList.

5. The deal is therefore not killed.
6. CvDeal::endTrade(TRADE_PEACE_TREATY), the normal teardown that executes:

       GET_TEAM(eFromPlayer).setForcePeace(TEAMID(eToPlayer), false);

   never runs for this expiry.
7. No general time-based auto-clear exists in CvTeam::setForcePeace; the flag is persistent state.
   Thus the two teams can remain unable to declare war indefinitely unless some unrelated later
   path destroys/changes the relationship.

SECONDARY CONSEQUENCES
----------------------
Because the same deal is misclassified:
- CvDeal::doTurn treats it as a non-peace annual-ish deal after it becomes cancelable and can feed
  its list through peacetime trade/grant accounting.
- CvPlayer::isTradingWithTeam / stopTradingWithTeam / isAnyDealTooRecentToCancel and AI trade-value
  consumers also use isPeaceDeal as an exclusion. The permanent-force-peace lifetime error is the
  strongest concrete consequence; these are same-root downstream misclassifications.

ANCESTRY / REGRESSION
---------------------
K-Mod 1.46 scans BOTH lists in CvDeal::isPeaceDeal.

Earlier AdvCiv used a combined `headTradesNode()/nextTradesNode()` traversal, likewise scanning both
lists.

During the later list-interface refactor visible in practical-count 2998 / commit
8c36049d431fd085eac8269bef81722061b53a11, this changed from:

    for (... = headTradesNode(); ... = nextTradesNode(...))

to:

    FOR_EACH_TRADE_ITEM(getFirstList())

while preserving the "simplified" label. The second-list half of the logical combined traversal was
lost.

Base AdvCiv 1.14 still has the same first-list-only implementation, so SAS inherits this regression.

INDEPENDENT EXISTING-ALBUM EVIDENCE
-----------------------------------
Earlier F091 / KI#413 in CvTeam.cpp independently established that live forced-peace treaty
representation cannot assume first-list/head placement. That helper originally recognized only a
first-list/head treaty and was repaired in current SAS to:
- filter relevant master teams;
- scan the entire first list;
- if not found, scan the entire second list.

Current CvTeam::turnsOfForcedPeaceRemaining therefore already encodes the correct representation
invariant that CvDeal::isPeaceDeal still violates.

F091 is NOT the same root:
- F091 caused the *reported remaining duration* to be wrong while the live deal still existed;
- F281 prevents the actual deal from being recognized for expiry/peace exclusions and can leave
  force-peace state persistent indefinitely.

REPAIR BOUNDARY
---------------
Restore logical both-list scanning, e.g.:

    FOR_EACH_TRADE_ITEM(getFirstList())
        if (pItem->m_eItemType == getPeaceItem())
            return true;
    FOR_EACH_TRADE_ITEM(getSecondList())
        if (pItem->m_eItemType == getPeaceItem())
            return true;
    return false;

or introduce a shared both-list helper if desired.

Do not restrict the repair to head items or reparations ordering. The representation contract is
simply "the peace item may live in either list."

This restores K-Mod / older AdvCiv behavior and matches the already-repaired KI#413 helper.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 — UNUSED / provisional KI#605 next

CONTROLS ESTABLISHED SO FAR
---------------------------
- startTrade/endTrade switch asymmetry is expected: one-shot trades do not need endTrade; persistent
  resource/GPT/vassal/OB/DP/peace/disengage items are saved and torn down.
- endTeamTrade's deletion-while-iterating initially looks dangerous, but the free-list iterator
  advances its integer cursor before returning each object, and the same pattern is inherited from
  K-Mod/AdvCiv. No iterator invalidation root established.
- serialization order is symmetric in the initial pass.
- SAS-vs-Base semantic delta in CvDeal is small outside game-record logging, tech-acquisition cause,
  already-known reparations identity fix, and victory-denial peace guard.

NEXT CURSOR
-----------
Continue queue 042 from F282 unused:
1. challenge all isPeaceDeal downstream consumers with both-list representation after F281;
2. continue startTrade/endTrade persistent-state symmetry, especially vassal/team-expanded deals;
3. verify addTradeItems early-return/partial-state behavior and permanent-alliance sequencing;
4. audit cancellation/kill recursion and endTeamTrade nested deletions more deeply;
5. persistence/load state, player/team death, vassal cascade and embargo interactions;
6. history-guided AdvCiv fixes plus independent K-Mod/Civ4CE divergence scan;
7. current SAS logging/peace-guard additions for any behavior-changing side effect.

Do not allocate F282 unless an independent root survives caller/history/current-data checks.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP02 — F282 / provisional KI#605 confirmed
===============================================================================

F282 / provisional KI#605
-------------------------
TITLE
Vassal-break memory duration logic became non-additive when addEndTradeMemory changed from
AI_changeMemoryCount to AI_setMemoryCount, so "2x10" / "3x10" intended memories collapse to a
single fixed count.

CURRENT CODE
------------
The vassal/surrender endTrade branch contains:

    addEndTradeMemory(eFromPlayer, eToPlayer, TRADE_VASSAL);
    if(!bDeniedHelp) // Master remembers for 2x10 turns
        addEndTradeMemory(eFromPlayer, eToPlayer, TRADE_VASSAL);
    else /* Vassal remembers for 3x10 turns (and master for 1x10 turns, ...) */
    {
        for(int i = 0; i < 3; i++)
            addEndTradeMemory(eToPlayer, eFromPlayer, TRADE_VASSAL);
    }

But addEndTradeMemory currently does:

    itRememberingMember->AI_setMemoryCount(
        itEndingMember->getID(),
        eMemory,
        bHalve ? 1 : 2);

So repeated calls are idempotent:
- two calls set 2, then set 2 again -> final count 2;
- three calls set 2 three times -> final count 2.

The comments and historical behavior require those repetitions to represent different durations.

HISTORY / REGRESSION PROOF
--------------------------
The original advc.143 implementation introduced the same repeated-call structure while
addEndTradeMemory used additive memory semantics:

    AI_rememberEvent(...)

Later, before practical-count 1497, the helper had been converted to:

    AI_changeMemoryCount(..., 2)

to match the faster memory-decay scale. Thus:
- one remembered event added 2;
- "2x10" used two calls -> +4;
- "3x10" used three calls -> +6.

Practical-count 1497 / commit 20e0f168829b6befe2de4dc2f77f123c877c1bc0
("Many style changes; header inclusions; embargo bugfix") changed:

    AI_changeMemoryCount(..., 2)

to:

    AI_setMemoryCount(..., 2)

without changing the repeated-call logic.

That turns the intended additive repetitions into repeated assignment of the same value.

Later practical-count 3118 added the `bHalve` parameter and retained the setter:

    eMemory, bHalve ? 1 : 2

with the comment that memories are "Twice remembered ... b/c now twice as fast forgotten."
That confirms count 2 represents one ordinary remembered event on the new decay scale, not the
whole intended 2x/3x duration.

LIVE CONSEQUENCE
----------------
When a vassal relationship ends:
- normal/non-denied-help case intends the master to remember the former vassal for 2x10 turns,
  but the repeated setter leaves only one-event count 2;
- denied-help case intends the former vassal to remember the master for 3x10 turns,
  but three setter calls still leave only count 2.

This shortens the intended diplomatic memory and can affect future vassal/capitulation willingness.
Current CvTeamAI explicitly consults MEMORY_CANCELLED_VASSAL_AGREEMENT in surrender/vassal trade
logic, so the memory has live strategic consumers despite its XML attitude weight being 0.

TEAM-PROXY INTERACTION / REPAIR BOUNDARY
----------------------------------------
Vassal/open-border/team trades are expanded into per-member proxy CvDeal objects through
startTeamTrade. When a top-level team trade ends, endTeamTrade kills sibling proxies with
`kill(false)`.

For OB/DP, cancellation memory is already gated by `if (bTeam)`. The vassal branch is not similarly
gated, so every proxy currently also executes the setter. Because it is a setter, these repetitions
do not multiply the value; they merely rewrite the same count.

This matters for the repair:
- blindly reverting `AI_setMemoryCount` to `AI_changeMemoryCount` would make every proxy additive
  and could grossly overcount memory on multi-member teams;
- the safe repair should preserve "exactly once per logical team-trade teardown."

Evidence-supported repair shapes include:
1. gate vassal cancellation-memory assignment behind `bTeam`, matching OB/DP's logical-team-trade
   treatment, then set the intended final count directly (2x or 3x on the current doubled-decay
   scale);
2. or refactor addEndTradeMemory to accept an explicit desired count and ensure proxy `bTeam=false`
   teardown does not add another logical memory event.

The exact numeric final counts should follow the current decay convention:
- one ordinary event = 2;
- intended 2x duration = 4;
- intended 3x duration = 6;
unless review of current decay code establishes a later scale change.

Do NOT simply replace setMemoryCount with changeMemoryCount without addressing proxy deals.

ROOT INDEPENDENCE
-----------------
F282 is independent of F281.
- F281 is a both-list peace-deal classification/lifetime bug.
- F282 is an additive-to-assignment regression in diplomatic memory duration during vassal teardown.

CONTROLS FROM THIS BRANCH
-------------------------
- The initial suspicion that team-expanded vassal proxy deals directly multiply current memory was
  false under current code because the helper uses a setter.
- The deletion-while-iterating pattern in endTeamTrade remains consistent with the free-list iterator
  contract: iteration position is advanced before each returned object; same architecture exists in
  K-Mod.
- `kill(false)` for deal objects is used here to suppress recursive team-wide teardown; the only
  unrelated current deal `kill(false)` found is disengagement cancellation, which has no vassal
  memory path.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 — UNUSED / provisional KI#606 next

NEXT CURSOR
-----------
Continue from F283 unused:
1. finish vassal/surrender teardown state and memory consumers;
2. inspect addTradeItems partial-application and early-return safety;
3. permanent-alliance / vassal sequencing and team merge interactions;
4. resource/GPT verify cancellation edge cases;
5. embargo/war/peace one-shot side effects and deal-value ordering;
6. save/load and dead-player/team references;
7. history-guided CvDeal fixes and Civ4CE divergence;
8. current SAS logging and victory-denial guard for behavior-changing side effects.

Do not force F283 if remaining branches close as controls.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP03 — F283 / provisional KI#606 confirmed
===============================================================================

F283 / provisional KI#606
-------------------------
TITLE
CvPlayer::resetDualDeal scans only a deal's first trade list, so a valid second-list-only peace
treaty cannot be prolonged; a new overlapping treaty is created and the older treaty can later
clear force-peace while the newer treaty is still supposed to be active.

CURRENT CALL PATH
-----------------
CvDeal::startTrade handles TRADE_PEACE_TREATY as follows:

    if (GET_TEAM(eFromPlayer).isForcePeace(kToPlayer.getTeam()))
    {
        if (kFromPlayer.resetDualDeal(eToPlayer, TRADE_PEACE_TREATY))
        {
            ...
            break;
        }
    }
    GET_TEAM(eFromPlayer).setForcePeace(kToPlayer.getTeam(), true);
    bSave = true;

The purpose is explicit: if the same dual agreement already exists, reset its initial turn instead
of creating a second overlapping persistent agreement.

Current CvPlayer::resetDualDeal:

    bool CvPlayer::resetDualDeal(PlayerTypes ePlayer, TradeableItems eDealType)
    {
        int iGameTurn = GC.getGame().getGameTurn();
        FOR_EACH_DEAL_VAR(d)
        {
            if (d->isBetween(getID(), ePlayer))
            {
                FOR_EACH_TRADE_ITEM(d->getFirstList())
                {
                    if (pItem->m_eItemType == eDealType)
                    {
                        d->setInitialGameTurn(iGameTurn);
                        return true;
                    }
                }
            }
        }
        return false;
    }

It never scans getSecondList().

LIVE REPRESENTATION
-------------------
F281 independently proves a normal live path where AI peace negotiation stores
TRADE_PEACE_TREATY only in the SECOND list:
- AI_negotiatePeace deliberately places the treaty on only one side because the EXE/counterproposal
  path dislikes treaties on both sides;
- reparations orientation can leave firstList empty and secondList containing the only saved treaty.

This is not malformed deal data.

FAILURE CHAIN
-------------
Assume an existing still-active treaty:

    deal A:
        firstList  = empty
        secondList = [TRADE_PEACE_TREATY]
        initialTurn = T0

At T1 < T0 + PEACE_TREATY_LENGTH, another trade legitimately includes/prolongs a peace treaty.

1. Team state already has isForcePeace == true.
2. startTrade calls resetDualDeal.
3. resetDualDeal scans only deal A.firstList, misses the treaty and returns false.
4. startTrade falls through:
       setForcePeace(..., true); // already true
       bSave = true;
5. A second persistent treaty, deal B, is saved with initialTurn = T1.

Now assume F281 itself has been repaired so both deals are recognized correctly.

At T0 + PEACE_TREATY_LENGTH:
6. deal A becomes cancelable and verify kills it.
7. endTrade(TRADE_PEACE_TREATY) executes:
       setForcePeace(..., false);
8. deal B is still younger and should keep peace forced until:
       T1 + PEACE_TREATY_LENGTH
   but forcePeace has already been cleared.

Thus F283 can cause the opposite lifetime failure from F281:
- F281 can make peace last indefinitely;
- F283 can make a renewed peace treaty end too early.

ROOT INDEPENDENCE FROM F281
---------------------------
This is a distinct root, not merely a second symptom of isPeaceDeal.

If F281 is fixed and F283 is not:
- second-list treaties expire normally;
- resetDualDeal still fails to find them;
- overlapping deals are still created;
- expiration of the older deal still clears the shared team flag prematurely.

If F283 is fixed but F281 is not:
- renewal finds and resets a second-list treaty correctly;
- but isPeaceDeal still fails to expire it later.

Both repairs are therefore required.

WHY OPEN BORDERS / DEFENSIVE PACT USUALLY MASK THE HELPER BUG
--------------------------------------------------------------
resetDualDeal is also used for TRADE_OPEN_BORDERS and TRADE_DEFENSIVE_PACT.

Those team agreements are materialized by startTeamTrade(..., bDual=true), which creates proxy
deals with the item in BOTH lists. Therefore the first-list-only search happens to find them.

Peace treaties are the live exceptional "dual state, one-sided saved representation" case.

ANCESTRY
--------
The first-list assumption predates the later list-interface refactor.

Practical count 1270 / commit beb230a3ba introduced advc.032 `resetPeaceTreaty`.
Its original implementation already:
- iterated deals between the two players;
- scanned only `d->getFirstTrades()`;
- reset the deal turn when it found TRADE_PEACE_TREATY.

Practical count 2998 / commit 8c36049d431fd085eac8269bef81722061b53a11 generalized:

    resetPeaceTreaty -> resetDualDeal(ePlayer, eDealType)

and reused it for OB/DP, but preserved the first-list-only loop.

So unlike F281, whose both-list combined traversal was specifically lost in a later refactor,
F283 is a longstanding advc.032 representation assumption from the helper's introduction.

K-Mod does not provide this AdvCiv-specific renewal helper; its both-list deal representation still
serves as a useful baseline, but the bug is specifically in AdvCiv's prolong-existing-dual logic.

REPAIR BOUNDARY
---------------
For a deal between the requested players, reset the initial turn if eDealType occurs in EITHER
trade list.

For example:
    scan d->getFirstList();
    scan d->getSecondList();
    if found:
        d->setInitialGameTurn(iGameTurn);
        return true;

Do not require the item to be on the side corresponding to the caller. The agreements using this
helper are team-level/dual state; the stored list side is representational.

This repair:
- preserves existing OB/DP behavior;
- handles one-sided peace treaties;
- prevents overlapping peace deals caused solely by list orientation.

RELATED BUT NOT PROMOTED YET
----------------------------
CvTeam::addTeam's permanent-alliance cleanup also scans only each internal deal's first list when
looking for OB/DP/peace/vassal/surrender/disengagement items.

A second-list-only peace treaty can therefore be missed when two teams merge. PA trade legality
does not explicitly prohibit force-peace, so the state is plausible, including human-driven trade.
However the strongest currently proven consequence is a stale internal deal/state-accounting seam,
not yet a comparably crisp independent gameplay failure. Keep this candidate open; do not allocate
F284 from it without a stronger downstream consequence.

HISTORY-GUIDED CONTROLS
-----------------------
- practical counts 1577/1578 repaired AI cancellation/renegotiation and trade-table handling but do
  not repair the first-list peace representation assumptions.
- practical 2485 fixes repeated loud cancellation sounds when war kills multiple deals; current
  bNoSound handling remains coherent.
- practical 2765 force-cancels GPT deals under a specific broke-player condition; this is policy
  logic, not yet evidence of a general GPT balance invariant.
- startTeamTrade persistent representation clarified: the initiating item with m_iData<=0 creates
  per-member m_iData=1 proxy deals; the original initiating deal item itself is not saved. Exactly
  one proxy cancellation normally enters endTrade with bTeam=true; nested sibling kills use
  bTeam=false. This supports F282's proposed "once per logical team teardown" repair boundary.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 — UNUSED / provisional KI#607 next

NEXT CURSOR
-----------
Continue from F284 unused:
1. finish the CvTeam::addTeam first-list-only PA cleanup challenge;
2. inspect all reset/restart/prolong deal paths for overlapping shared team-state flags;
3. history-guided city-trade / implied-peace / cancellation commits (1519, 2691, 3043, 3275);
4. verify vassal proxy teardown with F282 repair boundary and loss/help detection ordering;
5. partial addTradeItems application, permanent-alliance team merge and one-shot sequencing;
6. resource/GPT validation and dead-player/team teardown;
7. current SAS game-record/victory-denial additions;
8. full K-Mod/Civ4CE divergence and function-family saturation.

Do not force F284 if these resolve as controls.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP04 — F282/F283 repair-boundary refinements; F284 remains unused
===============================================================================

F284 remains UNUSED / provisional KI#607 next.


F282 SAME-ROOT EXPANSION — setter conversion broke all cancellation-memory accumulation
----------------------------------------------------------------------------------------
The WIP02 proof used the strongest self-documenting symptom:
- vassal teardown comments say "2x10 turns" and "3x10 turns";
- repeated calls became repeated `AI_setMemoryCount(..., 2)` assignments;
- therefore 2x/3x collapsed to a single event.

The practical-1497 diff establishes a broader same-root semantic regression.

Before practical count 1497, shared addEndTradeMemory ended with:

    AI_changeMemoryCount(..., eMemory, 2);

Practical 1497 changed ONLY that update primitive to:

    AI_setMemoryCount(..., eMemory, 2);

The helper is shared by:
- cancelled Open Borders;
- cancelled Defensive Pacts;
- cancelled vassal agreements.

Therefore the regression is not limited to the explicit repeated vassal calls.

Under the old additive contract:
- a new cancellation while an older same-type memory remains ADDS the new event;
- two remembered cancellations can produce e.g. 4;
- a later cancellation never shortens the memory already accumulated.

Under the current setter:
- every ordinary cancellation overwrites the entire count with 2;
- if the old count is 1, it becomes 2;
- if the old count is 4 or 6, a fresh cancellation REDUCES it to 2.

Practical 3118 later added per-event halving:

    AI_setMemoryCount(..., bHalve ? 1 : 2);

This can be even more counterintuitive when an AI ends the agreement:
an already-large memory can be overwritten downward to 1 merely because the latest cancellation is
supposed to count half as much.

The practical-3118 intent is "halve the cancellation memory [for this cancellation]," not
"replace all accumulated past cancellation memory with a one-unit total." The old additive helper
and the unchanged repeated-event structure make that distinction especially clear.

Thus F282's root should be stated broadly as:

    practical 1497 accidentally changed addEndTradeMemory from an additive event operation into
    absolute assignment, breaking both repeated-event accumulation and the explicitly coded
    2x/3x vassal-duration multipliers.

REPAIR REFINEMENT
-----------------
The repair still must account for team-proxy deals.

Important representation clarification:
- startTeamTrade called from an initiating m_iData<=0 team agreement creates per-member m_iData=1
  proxy CvDeals;
- the initiating item itself is NOT saved in the original deal;
- the persistent agreement is represented by the proxy deals;
- when one proxy is logically canceled, its endTrade runs with bTeam=true and endTeamTrade kills
  the remaining proxies with kill(false);
- those nested proxy endTrade calls therefore have bTeam=false.

A safe additive repair can consequently use bTeam as the logical-event gate:
- perform cancellation-memory event accounting only on the one bTeam=true teardown;
- suppress memory additions for proxy bTeam=false teardown;
- add the intended event amount rather than assigning the total.

On the current doubled-decay scale:
- ordinary event: +2;
- halved event: +1;
- intended 2x vassal memory: +4 total for that logical event;
- intended 3x vassal memory: +6 total.

This preserves historical accumulation across independent cancellations while avoiding MxN proxy
multiplication.

Do NOT simply replace setMemoryCount with changeMemoryCount inside the current helper while leaving
proxy bTeam=false calls active.


F283 REPAIR-BOUNDARY EXPANSION — team scope, not merely both-list scope
------------------------------------------------------------------------
WIP03 proved that resetDualDeal's first-list-only scan misses a second-list-only peace treaty and
can create overlapping treaties whose expiries fight over one shared forcePeace flag.

A deeper representation audit shows that "scan both lists" is necessary but NOT sufficient.

The agreements using resetDualDeal are team-level state:
- Open Borders;
- Defensive Pact;
- Peace Treaty / forcePeace.

Yet resetDualDeal searches only:

    d->isBetween(getID(), ePlayer)

i.e. one specific PLAYER pair.

MULTI-MEMBER PEACE FAILURE
--------------------------
Suppose teams A and B each have multiple members.

At T0:
- A1 and B1 create the live peace treaty;
- team-level forcePeace(A,B) becomes true;
- persistent treaty may be stored in a deal A1<->B1.

At T1:
- A2 and B2 legitimately include/prolong TRADE_PEACE_TREATY.

This is reachable because:
    canPossiblyTradeItem(TRADE_PEACE_TREATY)
        -> canChangeWarPeace(otherTeam)

and canChangeWarPeace deliberately does NOT reject temporary forcePeace. The startTrade renewal
branch exists specifically to handle an already-force-peace state.

A2.resetDualDeal(B2, PEACE_TREATY):
- searches only deals between A2 and B2;
- cannot see A1<->B1 even if that older treaty is in its first list;
- returns false;
- creates another team-level treaty.

The shared boolean forcePeace then has multiple player-level lifetime owners. When the older deal
expires first it clears the shared flag while the newer deal is still in duration.

This failure is independent of the first-vs-second-list bug.

OPEN BORDERS / DEFENSIVE PACT REPRESENTATION
----------------------------------------------
OB/DP are expanded by startTeamTrade into all member-pair proxy deals, so resetDualDeal usually does
find a proxy for the invoking pair. However it resets only THAT proxy's initial turn, not the
parallel proxy deals for the same team-level agreement.

Under ordinary legality:
- canPossiblyTradeItem currently rejects a new OB/DP item while the state is already active;
- therefore the renewal branches are less routinely reachable than peace renewal.

Still, if a forced/nonstandard path reaches those explicit prolong branches, resetting one proxy
does not coherently prolong the entire team agreement. The other proxies retain their older
cancelability age and any one proxy cancellation tears down the whole team trade.

This is same-root representation evidence, not a new finding.

CORRECT F283 REPAIR CONTRACT
----------------------------
For a TEAM-LEVEL dual agreement, renewal must operate on the logical team agreement, not one
player/list location.

At minimum for TRADE_PEACE_TREATY:
- search deals between the two teams, not merely the two players;
- search both saved lists;
- locate the already-live logical treaty and reset its initial turn instead of creating another.

For OB/DP if the generic helper remains shared:
- either reset every proxy deal belonging to that logical team agreement to the same current turn;
- or refactor renewal around a single logical team-trade helper rather than pretending one
  player-pair proxy owns the agreement.

A patch that only adds a second-list loop fixes the WIP03 example but leaves the multi-member
peace-overlap bug intact.


FIRST-LIST PEACE SIBLINGS — CONTROLS, NOT F284
-----------------------------------------------
A repository-wide scan found two further first-list-only cleanup sites:

1. CvTeam::addTeam permanent-alliance internal-deal cleanup.
2. CvTeam::setVassal cleanup of defensive-pact / peace-treaty deals involving the new vassal.

Both can miss a second-list-only peace treaty.

They should be reviewed when F281/F283 are implemented because they violate the same representation
fact. However current downstream consequences are weaker:
- after a permanent alliance the two players become same-team and the old inter-team forcePeace
  relation is no longer strategically meaningful;
- vassalization separately clears forcePeace with the master and aligns third-party wars/peace;
- DP is dual and therefore present in firstList.

The missed treaty can become stale bookkeeping and can later execute same-team/no-op teardown, but
no independent default-gameplay state corruption stronger than F281/F283 has yet been established.
Do not allocate F284 merely for code-pattern symmetry.


OTHER HISTORY-GUIDED CONTROLS
-----------------------------
- practical 1519 corrected city transfer semantics in peace deals (cede vs gift); current code
  preserves that.
- practical 2691 made implied peace treaties visible/explicit in trade-table handling; current
  sequencing is coherent.
- practical 3275 deferred implied treaty signing until the complete bundle has been applied, avoiding
  unilateral/double-treaty sequencing; current code preserves that.
- map-trade profiling shortcut is compile/profile-only.
- practical 1577/1578 cancellation repairs concern AI renegotiation/list orientation and the human
  resource-cancel trade-table path; current deal list accessors used there are orientation-aware.
- practical 2485 repeated cancellation sound suppression remains coherent.
- practical 2765 GPT insolvency cancellation is policy-specific; no sign/inversion issue found.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional; broadened additive-memory root
F283 / KI#606 — confirmed provisional; team-scope + both-list renewal root
F284 — UNUSED / provisional KI#607 next

NEXT CURSOR
-----------
Continue the independent saturation search:
1. remaining endTrade/startTrade state flags and overlapping-owner risks;
2. permanent-alliance reentrancy when the current deal contains additional persistent items;
3. kill/announce bForce first-list-only diagnostics (likely same representation control);
4. dead-player/team teardown and save/load invariants;
5. current SAS logging/victory-denial code for accidental state effects;
6. AI cancellation consumers, especially mixed deals and team-proxy cancelability;
7. independent Civ4CE/K-Mod behavior for any surviving candidates;
8. final function-level inventory before closure.

Do not allocate F284 unless a genuinely independent root survives these checks.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP05 — F284 / provisional KI#607 confirmed
===============================================================================

F284 / provisional KI#607
-------------------------
TITLE
Eliminating one player from a still-alive multi-member team tears down the entire surviving teams'
Open Borders / Defensive Pact / Disengagement agreement because a player-pair proxy deal owns
team-level state and killAllDeals cancels it with team-wide teardown.

LIVE REPRESENTATION
-------------------
AdvCiv represents several team-level agreements through player-pair proxy CvDeals.

CvDeal::startTeamTrade:

    for (MemberIter itFromMember(eFromTeam); ...)
    {
        for (MemberIter itToMember(eToTeam); ...)
        {
            TradeData item(eItem, 1);
            ...
            GC.getGame().implementDeal(itFromMember->getID(), itToMember->getID(),
                    ourList, theirList);
        }
    }

`MemberIter` is explicitly the ALIVE/MEMBER_OF iterator:
    PlayerIter<ALIVE, MEMBER_OF>.

For a dual Open Borders / Defensive Pact / Disengagement agreement between:
    Team A = {A1, A2}
    Team B = {B1}
the persistent representation therefore includes live proxies:
    A1 <-> B1
    A2 <-> B1

while the actual diplomatic state is stored on CvTeam:
    CvTeam::setOpenBorders
    CvTeam::setDefensivePact
    CvTeam::setDisengage

PLAYER-DEATH PATH
-----------------
CvPlayer::setAlive(false) does NOT require the whole team to die.

It:
1. sets m_bAlive = false;
2. decrements CvTeam::m_iAliveCount;
3. only CvTeam::changeAliveCount reaching 0 marks the team as having no living members / performs
   whole-team death cleanup;
4. then, for every individual defeated player, calls:

       killAllDeals();

CvPlayer::killAllDeals:

    FOR_EACH_DEAL_VAR(pLoopDeal)
    {
        if (pLoopDeal->involves(getID()))
            pLoopDeal->kill();
    }

Thus A1 can die while A2 remains alive and Team A remains alive, and A1's proxy deal is still
destroyed through ordinary `kill()` with bKillTeam=true.

CURRENT endTrade CONSEQUENCE
----------------------------
CvDeal::endTrade computes:

    bool const bAlive = (GET_PLAYER(eFromPlayer).isAlive() &&
                         GET_PLAYER(eToPlayer).isAlive());

and disables attitude updates if either player is dead.

But for these team-level items it still changes the team state:

TRADE_OPEN_BORDERS:
    GET_TEAM(eFromPlayer).setOpenBorders(TEAMID(eToPlayer), false);
    if (bTeam)
        endTeamTrade(TRADE_OPEN_BORDERS, ...);

TRADE_DEFENSIVE_PACT:
    GET_TEAM(eFromPlayer).setDefensivePact(TEAMID(eToPlayer), false);
    if (bTeam)
        endTeamTrade(TRADE_DEFENSIVE_PACT, ...);

TRADE_DISENGAGE:
    GET_TEAM(eFromPlayer).setDisengage(TEAMID(eToPlayer), false);
    if (bTeam)
        endTeamTrade(TRADE_DISENGAGE, ...);

So when A1 dies:
1. A1<->B1 proxy is killed.
2. endTrade sees A1 dead but still clears Team A/Team B's team-level agreement.
3. because bKillTeam/bTeam is true, endTeamTrade locates the remaining A2<->B1 proxy and kills it
   too.
4. A2 and B1 are both alive and their teams remain alive, but their agreement has been destroyed
   solely because teammate A1 was eliminated.

This is a direct live state consequence, not merely stale bookkeeping.

INTERNAL PRECEDENT — VASSAL TEARDOWN ALREADY RECOGNIZES THE DISTINCTION
-----------------------------------------------------------------------
The neighboring vassal/surrender branch begins:

    if (!bAlive)
        break;

before:
    setVassal(..., false, ...)
    endTeamTrade(...)

Thus when an individual member dies, deleting that member's vassal proxy does NOT itself dissolve
the team-level vassal relation. Surviving proxies and team state remain; whole-team death has its
own CvTeam::changeAliveCount cleanup.

That is exactly the ownership distinction missing from OB/DP/disengagement.

ANCESTRY
--------
The broad proxy-teardown behavior is inherited from BtS/K-Mod/Civ4CE:
- K-Mod and Civ4CE also clear Open Borders, Defensive Pact and Peace Treaty team state when a deal
  involving a dead player is ended.

AdvCiv practical count 1885 / commit 3c99f281643deb793534c983d463d7c009aae181
("Fix some assertions on player death") introduced the current `bAlive` handling.

The source comment says:

    Skip some steps when a trade ends through the defeat of one party -
    to avoid failed assertions

That commit:
- disables attitude updates on death;
- suppresses OB/DP cancellation memories on death;
- and, importantly, adds `if (!bAlive) break;` to VASSAL/SURRENDER.

It does NOT suppress team-state teardown for OB/DP.

So AdvCiv partially recognized that defeat-driven proxy deletion is not an ordinary diplomatic
cancellation, but the team-state lifetime distinction was applied only to vassalage.

A later player-death fix (practical 2437) deliberately moves `killAllDeals()` until after
`m_bAlive=false`, with the explicit comment:

    killAllDeals relies on bAlive=false.

That confirms death is intentionally observable inside endTrade; the current omission is not caused
by accidental call ordering.

SEVERITY / REACHABILITY
-----------------------
This requires a team with multiple living players:
- preconfigured multiplayer/team game;
- a larger team created through game mechanics such as Permanent Alliance can also create
  multi-member teams.

Individual player elimination while teammates survive is supported directly by CvTeam's
AliveCount; only count 0 triggers whole-team death handling.

Open Borders and Defensive Pact are ordinary persistent team agreements. Therefore this is a live
gameplay bug in supported team configurations, though less common than standard one-player teams.

Disengagement is AdvCiv-specific but uses the same proxy/team-state pattern and same failure.

PEACE TREATY — RELATED BUT REPAIR-DIFFERENT
-------------------------------------------
TRADE_PEACE_TREATY is also team-level forcePeace state, and player death currently clears it
unconditionally.

However peace treaties are NOT represented as the same all-member MxN proxy set. If the signing
player dies while the team survives:
- simply skipping setForcePeace(false) would delete the only timer-bearing CvDeal;
- forcePeace could then have no deal left to expire it later.

Therefore peace has the same ownership mismatch in spirit but requires migration/re-homing or a
team-level timer solution, not the simple surviving-proxy repair below.

Do not fold peace into the minimal F284 patch unless its lifetime is explicitly preserved.

REPAIR BOUNDARY
---------------
For proxy-backed team agreements (at minimum OB / DP / DISENGAGE), distinguish:

    a participant player died
from:
    one of the participant teams is now dead.

If one player is dead but BOTH teams remain alive:
- remove only deal/proxy state involving the dead player;
- do NOT clear the CvTeam-level agreement;
- do NOT call endTeamTrade to kill the surviving player-pair proxies;
- do NOT add cancellation memory (already suppressed today).

If an entire team is dead:
- normal whole-team cleanup may clear the team-state relation as appropriate.

The existing VASSAL/SURRENDER death branch is the strongest local model, but the condition should
be team-aware if generalized.

Do NOT merely call kill(false) from killAllDeals:
`bTeam=false` suppresses endTeamTrade, but OB/DP/disengage endTrade still unconditionally clears the
team-level flag before the bTeam check. The state teardown itself must be skipped for a dead
player whose team still survives.

ROOT INDEPENDENCE
-----------------
F284 is independent of:
- F281 (peace-deal list classification/expiry);
- F282 (diplomatic-memory accumulation);
- F283 (renewal/reset scope and overlapping agreements).

F284 is an ownership/lifetime mismatch between individual-player deal cleanup and team-level
agreement state.

RELATED CONTROL
---------------
The earlier concern about CvTeam::addTeam / CvTeam::setVassal first-list-only cleanup remains below
the independent finding bar. F284 instead has a direct default code path where surviving proxy
owners are actually destroyed.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 — UNUSED / provisional KI#608 next

NEXT CURSOR
-----------
Continue from F285 unused:
1. challenge the PEACE_TREATY-on-player-death subcase separately; determine whether existing code
   can re-home a treaty/timer or whether current behavior is intentionally specified anywhere;
2. inspect team-member revival/new proxy creation: team agreements existing while a member is dead
   may need proxy materialization when that player revives;
3. finish Permanent Alliance / team membership-change lifetime paths;
4. dead-player resource/GPT cleanup versus team-state cleanup;
5. remaining kill/verify/cancel consumers and save/load invariants;
6. current SAS logging/victory-denial modifications;
7. independent Civ4CE/K-Mod divergence and final function-family saturation.

Do not force F285 if these branches resolve as controls.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP06 — F285 / provisional KI#608 confirmed + saturation controls
===============================================================================

F285 / provisional KI#608
-------------------------
TITLE
AI_stopTradingTrade checks only the AI team's own gives-list for BOTH the peace treaty and annual
reparations, so a normal one-sided saved treaty on the opposite list can make the AI accept an
embargo it cannot actually fulfill.

CURRENT DENIAL CODE
-------------------
CvPlayerAI::AI_stopTradingTrade contains the AdvCiv reparations safeguard:

    // Avoid canceling reparations
    if (GET_TEAM(getTeam()).isForcePeace(eTradeTeam))
    {
        FOR_EACH_DEAL(d)
        {
            if (!d->isBetween(getTeam(), eTradeTeam))
                continue;
            bool bPeaceTreaty = false;
            bool bAnnualPayment = false;
            FOR_EACH_TRADE_ITEM(d->getGivesList(getTeam()))
            {
                TradeableItems eType = pItem->m_eItemType;
                if (eType == TRADE_PEACE_TREATY)
                    bPeaceTreaty = true;
                if (eType == TRADE_RESOURCES ||
                    eType == TRADE_GOLD_PER_TURN)
                {
                    bAnnualPayment = true;
                }
            }
            if (bPeaceTreaty && bAnnualPayment)
                return DENIAL_RECENT_CANCEL;
        }
    }

`CvPlayer::getTradeDenial` really uses this for TRADE_EMBARGO:

    case TRADE_EMBARGO:
        return AI().AI_stopTradingTrade((TeamTypes)kTrade.m_iData);

So failure of this check changes whether the AI can agree to a stop-trading request.

LIVE SAVED DEAL ORIENTATION
---------------------------
A normal bilateral peace bundle can contain:
- peace-treaty entries on both incoming offer lists;
- GPT/resources as reparations from one side.

CvDeal processes the first side first. The first TRADE_PEACE_TREATY:
- enables team forcePeace;
- is saved.

When the second peace-treaty item is later processed:
- forcePeace is already active;
- startTrade calls resetDualDeal;
- if that finds the first saved treaty, the duplicate treaty item is not saved.

Thus an ordinary persisted deal can naturally have:
    firstList  = [TRADE_PEACE_TREATY, ...]
    secondList = [TRADE_GOLD_PER_TURN, ...]

or the mirror orientation.

No malformed deal construction is required.

CONCRETE FAILURE
----------------
Choose the orientation where:
- the AI is the SECOND player/team;
- the saved peace treaty is in the FIRST list;
- the AI pays GPT reparations in the SECOND list.

Then:
- `d->isPeaceDeal()` is already true even under current unfixed F281, because the treaty happens to
  be in firstList;
- `d->getGivesList(getTeam())` for the AI sees the GPT reparations but NOT the treaty;
- AI_stopTradingTrade therefore fails to set bPeaceTreaty and does NOT return DENIAL_RECENT_CANCEL.

Now suppose the AI also has any other ordinary cancellable trade with the embargo target
(e.g. a resource trade established separately). That makes:
    canStopTradingWithTeam(target) == true
and TRADE_EMBARGO can be offered.

If the AI accepts:
- the broker/requester gets the accepted-stop-trading diplomatic outcome;
- the target receives the corresponding hired-embargo memory;
- CvPlayer::stopTradingWithTeam executes.

But stopTradingWithTeam deliberately preserves peace deals:

    if (... && !pLoopDeal->isPeaceDeal() && !pLoopDeal->isDisengage())
        pLoopDeal->kill();

Therefore:
- the unrelated resource deal is canceled;
- the peace+GPT reparations deal is deliberately NOT canceled;
- the AI keeps paying the target despite having agreed/rewarded the request to stop trading.

This is the exact inconsistency the reparations safeguard exists to prevent.

HISTORY / REGRESSION PROOF
--------------------------
The safeguard was introduced by advc.130f, practical count 1383 / commit d4fd8b6277.
The original code explicitly examined both trade lists while determining whether a peace deal
contained annual reparations.

A later traversal refactor (practical count 1626) converted that to the old combined
headTradesNode()/nextTradesNode() traversal, still covering BOTH lists and allowing the
peace-treaty item and annual item to reside on opposite sides of the same deal.

During practical-count 2998's list-interface refactor, the combined traversal was replaced with:

    FOR_EACH_TRADE_ITEM(d->getGivesList(getTeam()))

This changed the semantic domain from "all items in this deal" to "only items given by the AI team."
The peace marker can live on the opposite side, so the representation invariant was lost.

Base AdvCiv 1.14 inherits this regression.

RELATION TO F281
----------------
F285 is independent of F281.

The concrete orientation above deliberately puts the treaty in FIRST list, so current
CvDeal::isPeaceDeal() already returns true:
- stopTradingWithTeam correctly preserves the reparations deal;
- only the AI denial precheck is wrong.

Fixing F281 does not fix F285 because AI_stopTradingTrade still scans only the AI gives-list for
the treaty marker.

Conversely, fixing F285 does not repair F281's second-list treaty expiry bug.

REPAIR BOUNDARY
---------------
The intended modern condition is more precise than simply restoring the old combined "any annual
item anywhere" scan.

The reason for denial is:
    "I am currently obliged to GIVE annual reparations under a peace treaty to the embargo target."

Therefore for each deal between the teams:
1. detect the peace treaty anywhere in the deal (both saved lists, or `d->isPeaceDeal()` after F281
   is fixed);
2. inspect the AI team's own gives-list for:
       TRADE_RESOURCES
       TRADE_GOLD_PER_TURN
3. if both conditions hold, return DENIAL_RECENT_CANCEL.

Conceptually:

    bool bPeaceTreaty = scanBothListsForPeace(*d); // or fixed d->isPeaceDeal()
    if (!bPeaceTreaty)
        continue;

    FOR_EACH_TRADE_ITEM(d->getGivesList(getTeam()))
    {
        if (pItem->m_eItemType == TRADE_RESOURCES ||
            pItem->m_eItemType == TRADE_GOLD_PER_TURN)
        {
            return DENIAL_RECENT_CANCEL;
        }
    }

This:
- handles treaty marker on either side;
- refuses only when the AI itself owes annual reparations;
- does not unnecessarily refuse because the OTHER side pays GPT/resources.

If implementation is deliberately independent of F281, scan both lists locally for the peace item.

ROOT INDEPENDENCE
-----------------
F285 root:
    reparations embargo-denial lost whole-deal treaty visibility during list-interface refactor.

Independent of:
- F281 deal classification/expiry;
- F282 memory accumulation;
- F283 renewal ownership;
- F284 player-death/team-proxy ownership.

ADDITIONAL CONTROLS FROM THIS SLICE
-----------------------------------
1. Empty persistent deal / CvDeal::doTurn head dereference:
   REJECTED. CvGame::implementAndReturnDeal checks both saved list lengths after addTradeItems.
   If both are empty it immediately kills the deal and returns NULL. Ordinary one-shot/rejected
   bundles do not survive as empty CvDeals.

2. Team-proxy implementation-time attitude/value multiplication:
   REJECTED. AI_processTradeValue calls AI_dealVal(..., bIgnoreAnnual=true), and OB/DP/disengage/
   vassal proxy items are annual for that calculation. Pure dual deals are also suppressed in
   CvDeal::doTurn. MxN proxy count does not multiply ordinary trade-history value.

3. Resource verify `< 0`:
   CLEAN. After one exported copy is consumed, zero can be a valid "last copy is being exported"
   state. Negative indicates oversubscription/lost source and is the proper cancellation threshold.

4. Permanent Alliance mixed-bundle reentrancy:
   BELOW BUG BAR. AI-generated PA offers are PA-only and current PA legality requires one-member
   teams. Human/EXE mixed-bundle behavior is not locally established strongly enough for F286.

5. First-list cleanup siblings in CvTeam::addTeam / CvTeam::setVassal:
   remain representation debt/control. No independent state consequence stronger than F281/F283
   established.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional; team-scoped renewal contract
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 — UNUSED / provisional KI#609 next

NEXT CURSOR
-----------
Continue from F286 unused:
1. fold practical-2658 diplo-vote multi-proxy renewal evidence into F283 explicitly;
2. challenge peace-treaty participant-death ownership/timer separately from F284;
3. player revival / newly alive team member and preexisting team-agreement proxy materialization;
4. remaining stopTrading / cancelability / verify consumers and list orientation assumptions;
5. save/load and dead-player/team references;
6. current SAS logging/victory-denial modifications;
7. full CvDeal method-family inventory and independent K-Mod/Civ4CE divergence;
8. root-independence + repair-boundary closure challenge.

Do not allocate F286 unless an independent root survives.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP07 — F283/F284 lifetime refinements; F286 remains unused
===============================================================================

F286 remains UNUSED / provisional KI#609 next.


F283 SAME-ROOT EXPANSION — practical 2658 vote-prolongation proves all team proxies must share age
--------------------------------------------------------------------------------------------------
Practical count 2658 explicitly added the `bProlong` paths to Open Borders and Defensive Pact so
diplomatic votes can reset the turns before those agreements become cancelable.

Current CvGame::processVote:

    if (kVote.isOpenBorders())
        itFirst->signOpenBorders(itSecond->getID(), true);
    if (kVote.isDefensivePact())
        itFirst->signDefensivePact(itSecond->getID(), true);

CvTeam::signOpenBorders / signDefensivePact call implementDeal using the two TEAM LEADERS. Their
startTrade paths contain:

    if (isOpenBorders(...))
    {
        if (kFromPlayer.resetDualDeal(eToPlayer, TRADE_OPEN_BORDERS))
            break;
    }

and the analogous DP path.

But the persistent OB/DP representation is an MxN set of player-pair proxy deals created by
startTeamTrade for every living member pair. resetDualDeal:
- searches only deals between the two leader players;
- scans only the first list;
- resets only the first matching proxy;
- then returns under the source assumption "at most one deal of eDealType."

For a multi-member team agreement, that assumption is false by construction.

Consequently a vote intended to "prolong the Open Borders / Defensive Pact agreement" resets only
the leader-leader proxy. Other proxies retain their older initial turns. Once one older proxy is
cancelable, canceling it can endTeamTrade the entire logical agreement even though the diplomatic
vote intended to refresh its minimum duration.

This is especially strong F283 evidence because practical 2658 is the feature that introduced the
renewal helper for OB/DP. The current player-pair implementation cannot fulfill that feature's
team-level intent on multi-member teams.

F283 repair contract therefore remains:
- locate the logical TEAM agreement;
- inspect both saved lists;
- update every proxy deal that owns the same team-level OB/DP lifetime, or replace proxy-local ages
  with one logical agreement age;
- for peace, locate the treaty across the two teams rather than one particular player pair.

A both-list-only patch remains incomplete.


F284 SAME-ROOT EXPANSION — PEACE_TREATY has the same player-owner/team-state mismatch, but no proxy fallback
-----------------------------------------------------------------------------------------------------------
WIP05 proved the clean proxy-backed failure for OB/DP/DISENGAGE:
individual player death calls killAllDeals even while teammates survive, and deleting one proxy
clears team-level state plus kills surviving proxies.

TRADE_PEACE_TREATY has the same ownership mismatch:
- forcePeace is stored on CvTeam;
- the treaty timer/lifetime is stored in a CvDeal owned by two Players;
- unlike OB/DP, peace is normally represented by ONE saved treaty item rather than an MxN proxy set.

If the player owning that treaty dies while his team still has living members:
- setAlive(false) calls killAllDeals;
- endTrade(TRADE_PEACE_TREATY) unconditionally calls setForcePeace(..., false);
- the surviving teammate's team therefore loses the rest of the temporary peace period early.

This is the same conceptual F284 root:
    an individual-player deal lifetime is allowed to destroy team-level diplomatic state even
    though the participant team itself survives.

However its minimal repair differs from proxy-backed agreements.

For OB/DP/DISENGAGE:
- surviving member-pair proxy deals remain, so defeat cleanup can simply avoid team-wide teardown
  and let the surviving proxies continue owning the state.

For PEACE_TREATY:
- the dying player's deal may be the ONLY timer-bearing owner.
- merely skipping setForcePeace(false) and deleting the deal would leave forcePeace with no treaty
  left to expire it later.

Therefore a complete F284 repair should either:
1. re-home/migrate the surviving treaty duration to a deal between living members of the same two
   teams before deleting the dead player's deal; or
2. move temporary-peace lifetime ownership to team-level state independent of a particular
   player's CvDeal.

Do not patch peace by merely adding the OB/DP death guard.

The live consequence strengthens F284 but does not justify a separate F286: both cases arise from
the same player-object ownership being used to control team-object state during individual defeat.


REVIVAL / NEWLY ALIVE MEMBER — CONTROL
--------------------------------------
A revived player on an already-living team does not need a new proxy merely to USE existing
team-level Open Borders / Defensive Pact / Disengagement:
- the actual permission/state flags live on CvTeam;
- revived player units inherit those team relations immediately.

The absence of a newly materialized player-pair proxy can affect who carries cancellation age and
how later renewal/cancellation is represented. That is already F283's logical-team-agreement versus
player-proxy lifetime problem, not an independent root.

No separate F286 from revival.


EMPTY DEAL / TURN PROCESSING — CONTROL
--------------------------------------
CvDeal::doTurn dereferences firstList.head() in its pure-dual fast-path condition, which initially
looked unsafe for an empty persistent CvDeal.

The allocation wrapper closes that path:
CvGame::implementAndReturnDeal calls addTradeItems, then if BOTH saved lists are empty it
immediately kills the deal and returns NULL.

One-shot-only bundles and rejected/blocked bundles therefore do not survive as empty CvDeals into
the turn loop. No F286.


PROXY TRADE-VALUE MULTIPLICATION — CONTROL
-------------------------------------------
startTeamTrade creates MxN proxy CvDeals, but implementation-time AI trade-value accounting does
not multiply the ordinary attitude value per proxy:
- AI_processTradeValue uses AI_dealVal(..., bIgnoreAnnual=true);
- OB / DP / disengagement / vassal items are treated as annual for this purpose and skipped;
- CvDeal::doTurn separately skips pure one-item dual deals.

No F286.


CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional; now includes diplo-vote proxy-age proof
F284 / KI#607 — confirmed provisional; now includes peace participant-death lifetime
F285 / KI#608 — confirmed provisional
F286 — UNUSED / provisional KI#609 next

NEXT CURSOR
-----------
Continue the saturation challenge:
1. remaining startTrade/endTrade item symmetry and shared-state owner multiplicity;
2. stopTradingWithTeam / cancellation / announceCancel list-orientation siblings;
3. save/load and deal-ID/player-ID lifetime guarantees;
4. mixed persistent deals and verify behavior;
5. SAS-specific logging and victory-denial early-return paths;
6. full K-Mod/Civ4CE structural divergence for CvDeal;
7. complete method/assert/comment inventory and root-independent repair review.

Do not allocate F286 merely for robustness or another manifestation of F281-F285.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP08 — F286 / provisional KI#609 confirmed
===============================================================================

F286 / provisional KI#609
-------------------------
TITLE
A human mixed Permanent Alliance + city/implied-peace bundle can delete the CvDeal from inside
CvDeal::addTradeItems and then CvGame::implementAndReturnDeal immediately dereferences the freed
pointer.

SUPPORTED TRIGGER
-----------------
Current CvPlayer::updateTradeList explicitly says:

    if (!isHuman() || !GET_PLAYER(eOtherPlayer).isHuman())
        ...
        // mixed annual/nonannual items restricted
    // everything allowed in human-human trades

Permanent Alliance is a normal inventory item when:
- GAMEOPTION_PERMANENT_ALLIANCES / PA trading is available;
- both teams are non-vassals, at peace, distinct;
- both teams currently have exactly one member.

A non-liberation city offer at peacetime is one of the current items that implies temporary peace.
Current updateTradeList therefore appends explicit TRADE_PEACE_TREATY entries to BOTH human offer
lists for display/implementation:

    TradeData item(TRADE_PEACE_TREATY);
    ...
    item.m_iData = eForcePeaceItemType;
    apOffersVar[i]->insertAtEnd(item);

Thus a supported human-human bundle can contain, for example:
- first side: city + permanent alliance + appended peace treaty;
- second side: appended peace treaty
(or equivalent orientation/order supplied by the trade screen).

No malformed direct-DLL call is needed.

EXECUTION CHAIN
---------------
CvGame::implementAndReturnDeal:

    CvDeal* pDeal = addDeal();
    pDeal->init(...);
    pDeal->addTradeItems(kOurList, kTheirList, !bForce);
    if (pDeal->getLengthFirst() <= 0 && pDeal->getLengthSecond() <= 0)
    {
        pDeal->kill();
        return NULL;
    }
    return pDeal;

Inside addTradeItems:

1. Trade items are processed before the Permanent Alliance merge.

2. The first explicit TRADE_PEACE_TREATY encountered:
       setForcePeace(..., true);
       bSave = true;
   and is inserted into the current CvDeal's saved list.

3. When the opposite-side peace-treaty item is later processed, forcePeace is already true.
   resetDualDeal can find the just-created current deal and the already-saved treaty, so the
   duplicate treaty normally is not saved. The current deal nevertheless contains at least one
   saved TRADE_PEACE_TREATY.

4. TRADE_PERMANENT_ALLIANCE itself sets:
       bAlliance = true
   and is not saved.

5. After both item loops:
       if (bAlliance)
           GET_TEAM(...).addTeam(...);

CvTeam::addTeam reassigns the absorbed team's players into the surviving team and then iterates
deals that are now internal to the merged team:

    if (!pLoopDeal->isBetween(getID(), getID()))
        continue;

    FOR_EACH_TRADE_ITEM(pLoopDeal->getFirstList())
    {
        if (eType == TRADE_OPEN_BORDERS ||
            eType == TRADE_DEFENSIVE_PACT ||
            eType == TRADE_PEACE_TREATY ||
            ...)
        {
            pLoopDeal->kill();
            break;
        }
    }

The currently executing CvDeal now has:
- both players on the same merged team;
- a saved first-list peace treaty.

Therefore addTeam can call `kill()` on the very `this` whose addTradeItems stack frame is active.

IMMEDIATE DELETION / UAF PROOF
------------------------------
CvDeal::kill -> killSilent -> GC.getGame().deleteDeal(getID()).

CvGame::deleteDeal:
    m_deals.removeAt(iID);

FFreeListTrashArray::removeAt:
    SAFE_DELETE(m_pArray[iIndex].pData);

So the CvDeal object is actually destroyed before CvTeam::addTeam returns.

CvDeal::addTradeItems then returns through a member function whose `this` is already freed.
Even if its final bBumpUnits block happens not to dereference members in the concrete path, the
caller unconditionally does:

    pDeal->getLengthFirst()
    pDeal->getLengthSecond()

immediately after addTradeItems returns.

That is an unambiguous use-after-free.

ANCESTRY
--------
The dangerous interaction is AdvCiv-specific.

Practical count 2691 / commit 195785f015f5e71cffa4b6c52e2307ad8cc08a94:
    "Show implied peace treaties on the trade table"

added explicit PEACE_TREATY entries to human-visible offers for city/war items that imply temporary
peace. Its own comment says that, for deals involving humans, the game will attempt to implement
the treaty twice and considers that harmless.

Before that display change, the underlying city trade could merely set an internal
`bPeaceTreatyImplied`/sign-treaty path; a PA bundle takes the `bAlliance` branch and does not
materialize an explicit saved treaty that addTeam's internal-deal cleanup can recognize.

Practical 3275 later deferred implied-treaty signing until all bundle items had been applied to
avoid two unilateral treaties, but explicit treaty items added by the human trade table remain
processed/saved before the PA merge. It does not close this self-deletion path.

K-Mod/Civ4CE do not contain AdvCiv's same implied-peace-display mechanism in this form, so they do
not independently reproduce this exact trigger.

WHY CURRENT PREVALIDATION DOES NOT SAVE IT
------------------------------------------
addTradeItems validates each individual item through canTradeItem before applying the bundle.
There is no bundle-level rule forbidding Permanent Alliance + city/explicit implied peace for
human-human trades.

updateTradeList deliberately skips the annual/nonannual combination restriction when both players
are human ("everything allowed in human-human trades").

TRADE_PERMANENT_ALLIANCE canPossiblyTradeItem checks the two teams are single-member BEFORE the
merge; that condition is satisfied at validation time.

So all items can pass individually while their combined execution causes reentrant deletion.

SEVERITY
--------
High when triggered:
- heap object is freed while its method remains active;
- caller immediately dereferences the dangling CvDeal*;
- outcome can be crash, assertion, allocator-dependent corruption or apparently surviving stale
  memory depending on build/runtime.

The trigger is specialized (human-human PA enabled + mixed implied-peace bundle) but uses supported
trade-screen mechanics.

REPAIR BOUNDARY
---------------
The core invariant should be:

    CvTeam::addTeam must not delete the CvDeal that is currently implementing the Permanent Alliance
    until addTradeItems / implementAndReturnDeal have completed using that object.

Evidence-supported repair shapes include:

1. Preferred transaction/order repair:
   - do not persist an explicit implied peacetime PEACE_TREATY inside a deal that also executes a
     Permanent Alliance, because after the teams merge the treaty is meaningless anyway;
   - still display it to humans if needed, but suppress its saved lifetime when PA is in the same
     bundle.

2. Deferred cleanup:
   - let addTeam identify internal deals for cleanup but defer deletion of the currently executing
     deal until implementAndReturnDeal is done with it.

3. Bundle pre-normalization:
   - strip/ignore redundant PEACE_TREATY items from PA bundles before startTrade saves them.

A weak patch such as "don't dereference pDeal after addTradeItems" is insufficient unless ownership
and return semantics are redesigned, because `addTradeItems` itself may continue after deletion and
the function still conceptually returns through a dead `this`.

Do NOT remove all addTeam internal-deal cleanup: stale OB/DP/vassal/etc. agreements between newly
same-team players still need normalization.

ROOT INDEPENDENCE
-----------------
F286 is independent of F281-F285.

It is a reentrant object-lifetime/use-after-free bug caused by:
- explicit implied treaty materialization in a PA bundle;
- addTeam's internal-deal cleanup;
- immediate free-list deletion;
- post-call use of the CvDeal pointer.

It reproduces even if:
- F281 both-list peace classification is fixed;
- F282 memory accumulation is fixed;
- F283 team-level renewal is fixed;
- F284 player-death ownership is fixed;
- F285 embargo reparations detection is fixed.

CONTROLS / CAVEATS
------------------
- A pure PA offer does not save any item in the implementing CvDeal and is safely discarded as an
  empty deal after the merge.
- PA + ordinary one-shot items without an explicit saved internal team-state item do not
  automatically trigger this exact self-delete path.
- The strongest current trigger is PA + non-liberation city, because updateTradeList itself inserts
  explicit peace treaty items for human-visible implied peace.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional
F287 — UNUSED / provisional KI#610 next

NEXT CURSOR
-----------
Continue from F287 unused:
1. audit every addTeam/setVassal/declareWar/makePeace reentrant deal deletion path for another
   active-CvDeal lifetime hazard;
2. inspect mixed persistent bundles whose startTrade side effects can invalidate later items;
3. finish verify/isCancelable cancellation semantics, especially shared team-state items;
4. read/write/save-load deal IDs and enum/list invariants;
5. SAS-specific victory-denial early return and game-record logging for any partial side effects;
6. remaining first-list/list-orientation consumers in CvDeal/CvPlayerAI/CvTeam;
7. full function/assert/history/K-Mod/CE saturation and repair-boundary challenge.

Do not allocate F287 merely because F286 is high severity.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP09 — F287 / provisional KI#610 confirmed
===============================================================================

F287 / provisional KI#610
-------------------------
TITLE
Automatic invalidation of one annual item kills the entire mixed peace deal, so a broke GPT payer
can terminate the mandatory peace treaty before PEACE_TREATY_LENGTH expires.

CURRENT CODE
------------
CvDeal::verify:

    if (!verify(getFirstPlayer(), getSecondPlayer()) ||
        !verify(getSecondPlayer(), getFirstPlayer()) ||
        (isCancelable(NO_PLAYER) && isPeaceDeal()))
    {
        kill();
    }

The directional helper checks persistent annual items regardless of deal age.

For GPT:

    if (pItem->m_eItemType == TRADE_GOLD_PER_TURN)
    {
        if (kGiver.getCommercePercent(COMMERCE_GOLD) >= 100 &&
            kGiver.getGold() < pItem->m_iData && !kGiver.isAnarchy() &&
            kGiver.calculateGoldRate() < 0)
        {
            return false;
        }
    }

A false result kills the entire CvDeal immediately.

If that same persistent deal also contains TRADE_PEACE_TREATY, kill -> endTrade processes the
treaty and executes:

    GET_TEAM(eFromPlayer).setForcePeace(TEAMID(eToPlayer), false);

There is no independent remaining-turn field inside CvTeam::forcePeace; `isForcePeace` is simply
the stored team boolean. The CvDeal is the timer/lifetime owner.

SUPPORTED MIXED DEAL
--------------------
GPT is explicitly tradeable during war/peace negotiation:
- TRADE_GOLD_PER_TURN canPossiblyTradeItem only requires gold trading;
- canTradeItem itself accepts any nonnegative GPT amount;
- it does not require an existing peacetime trade network.

Human peace settlements can therefore legitimately contain:
    TRADE_PEACE_TREATY + TRADE_GOLD_PER_TURN reparations

in one persistent CvDeal.

The first PEACE_TREATY entry is saved as the temporary-peace lifetime item. GPT is also persistent
and saved in the same deal.

FAILURE CHAIN
-------------
Example:
- turn T0: peace signed with 10-turn treaty and 8 GPT reparations;
- T1/T2/... before T0+PEACE_TREATY_LENGTH: payer reaches 100% gold slider, cannot cover the GPT
  payment, is not in anarchy and still has a negative gold rate;
- CvGame::verifyDeals -> CvDeal::verify;
- GPT condition returns false;
- CvDeal::kill executes immediately despite `turnsToCancel() > 0`;
- GPT payment ends;
- TRADE_PEACE_TREATY teardown clears team forcePeace;
- the teams can resume war before the mandatory treaty duration has elapsed.

The normal treaty age protection:

    (isCancelable(NO_PLAYER) && isPeaceDeal())

only governs scheduled treaty expiry. It does NOT protect a peace deal from the earlier
resource/GPT invalidation branches.

ANCESTRY
--------
The general "invalid resource item kills whole deal" architecture is inherited from BtS/K-Mod/
Civ4CE.

The particularly common GPT trigger is AdvCiv-specific.

Practical count 2765 / commit c66c29025a3be4912b019a7b6128889ae07aacac:
    "Force-cancel gold-per-turn deals when broke"

added exactly the GPT false-return block to CvDeal::verify.

The patch did not special-case mixed peace deals or preserve the remaining treaty duration.
Therefore practical 2765 made ordinary GPT peace reparations capable of terminating temporary
peace early.

K-Mod/Civ4CE do not contain this GPT insolvency branch, though their inherited resource-invalid
whole-deal behavior shows the broader structural hazard.

RESOURCE SIBLING
----------------
Resource invalidation has the same whole-deal teardown behavior:
- lost trade network;
- oversubscribed resource;
- obsolete resource.

A resource + peacetime temporary treaty bundle could therefore exhibit the same lifetime problem.
For wartime peace negotiations, resource trading is often unavailable because canTradeNetworkWith
fails while enemies are at war, so GPT is the cleaner supported/default reproduction.

Treat resource invalidation as same-root structural evidence; F287 does not depend on proving a
wartime resource reparations case.

WHY THIS IS NOT NORMAL CANCELLATION
-----------------------------------
Manual cancellation is intentionally blocked while:
    turnsToCancel() > 0.

The GPT verify branch bypasses isCancelable entirely. So this is not simply "the payer is allowed
to cancel because insolvent." The engine itself tears down an uncancelable treaty as a side effect
of an annual payment failure.

REPAIR BOUNDARY
---------------
When an individual annual component of a deal becomes impossible during an uncancelable peace
period, the temporary peace lifetime must survive.

Possible evidence-supported designs:

1. Remove/end only the invalid annual item from the deal while retaining TRADE_PEACE_TREATY and its
   original initial turn.

2. If deal-level killing is retained, immediately re-home/recreate the treaty with the exact
   remaining duration before clearing forcePeace.

3. Split peace treaty lifetime from annual-payment deal lifetime when the bundle is implemented.

For the narrow GPT repair:
- cancel the impossible GPT obligation;
- do not clear temporary peace before its existing expiry.

Do NOT merely skip GPT verification while peace is active if that would leave an impossible payment
continuing indefinitely. The payment and the treaty have different failure/lifetime semantics.

ROOT INDEPENDENCE
-----------------
F287 is independent of F281-F286.

Even with:
- correct both-list isPeaceDeal;
- correct renewal;
- correct team-member death ownership;
- correct reparations embargo denial;
- no PA self-deletion;

an insolvent GPT component can still make `verify()` kill the whole still-young peace deal.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional
F287 / KI#610 — confirmed provisional
F288 — UNUSED / provisional KI#611 next

NEXT CURSOR
-----------
Continue from F288 unused:
1. inspect whether resource invalidation / deal verification can remove individual items safely or
   exposes another independent list-iteration bug;
2. finish reentrant deletion through setVassal / declareWar / makePeace / stopTrading;
3. verify cancellation announcements and bForce first-list orientation assumptions;
4. save/load / free-list deal ID and dead-player references;
5. SAS victory-denial/logging modifications for transaction atomicity;
6. remaining list-orientation consumers across CvDeal/CvPlayer/CvPlayerAI/CvTeam;
7. full function-family, history and K-Mod/Civ4CE saturation.

Do not force F288 if the remaining paths collapse into F281-F287 controls.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP10 — F286 transaction-lifetime expansion + method-family controls
===============================================================================

F288 remains UNUSED / provisional KI#611 next.


F286 SAME-ROOT EXPANSION — vassalization can self-delete the active transaction too
------------------------------------------------------------------------------------
WIP08 proved a high-confidence self-deletion path through Permanent Alliance.

The same transaction-lifetime invariant is violated by `CvTeam::setVassal(true)` under a supported
human peacetime mixed bundle.

SUPPORTED BUNDLE
----------------
At peacetime:
- human-human diplomacy permits mixed trade classes;
- a non-liberation city offer causes updateTradeList to append explicit PEACE_TREATY entries to
  both offer lists;
- voluntary TRADE_VASSAL is tradeable when neither team is already a vassal and the two sides are
  not at war.

Thus a supported bundle can contain:
    non-liberation city + voluntary vassal + displayed implied peace treaty.

EXECUTION
---------
addTradeItems deliberately processes non-vassal items in pass 0 and vassal items in pass 1.

Therefore before TRADE_VASSAL is executed:
- city transfer has occurred;
- the first explicit PEACE_TREATY has been saved in the current CvDeal.

TRADE_VASSAL then calls:

    startTeamTrade(...);
    GET_TEAM(eFromPlayer).setVassal(eToTeam, true, false);

Inside setVassal(true), current code iterates global deals involving the new vassal and kills
Defensive Pact / Peace Treaty deals whose FIRST list contains such an item:

    FOR_EACH_DEAL_VAR(pLoopDeal)
    {
        ...
        FOR_EACH_TRADE_ITEM(pLoopDeal->getFirstList())
        {
            if (pItem->m_eItemType == TRADE_DEFENSIVE_PACT ||
                pItem->m_eItemType == TRADE_PEACE_TREATY)
            {
                pLoopDeal->kill();
                break;
            }
        }
    }

The currently executing deal is already in the global free-list container and can satisfy this
test. Its first-list displayed implied treaty was saved before the vassal pass.

`kill()` therefore immediately SAFE_DELETEs the active CvDeal through `deleteDeal/removeAt`, exactly
as in the PA path.

Control then returns to the active `startTrade` / `addTradeItems` stack and ultimately to
implementAndReturnDeal, which dereferences the dangling pDeal pointer.

ROOT CLASSIFICATION
-------------------
Do not allocate F288.

The PA and vassal paths are the same root:
    a state transition invoked while applying a bundle is allowed to synchronously delete the
    CvDeal transaction object that is still executing that bundle.

The exact cleanup walker differs (`CvTeam::addTeam` vs `CvTeam::setVassal`), but the object-lifetime
contract and use-after-free consequence are identical.

This broadens the F286 repair requirement:
- transaction execution must survive all state-transition cleanup it invokes;
- implied/internal treaty normalization cannot synchronously destroy the active transaction.

WAR-BRIBE REENTRANCY CHALLENGE — CLEAN UNDER NORMAL DENIAL
-----------------------------------------------------------
TRADE_WAR calls `CvTeam::declareWar` synchronously, and defensive-pact cascades can themselves
declare additional wars and kill deals. This initially suggested another possible active-transaction
deletion.

A concrete dangerous shape would be:
- B brokers A into declaring war on C;
- B has a Defensive Pact with C;
- C's pact then causes B to declare war on A;
- B-vs-A war cleanup could kill the active A<->B bribe deal.

The trade denial closes the normal path.

`CvTeamAI::AI_declareWarTrade(eTarget=C, eSponsor=B)` begins with:

    if (GET_TEAM(eTarget).isVassal(eSponsor) ||
        GET_TEAM(eTarget).isDefensivePact(eSponsor))
        return DENIAL_JOKING;

TRADE_WAR can only be given by a non-human team through normal trade legality, and addTradeItems
prevalidates with denial checks unless the caller explicitly forces the deal.

Therefore the DP-with-sponsor cascade cannot occur through an ordinary accepted war-bribe deal.
A forced/malformed direct implementation could bypass this, but that is below the finding bar.

No F288.


`doTurn` ONE-ITEM DUAL FAST PATH — CLEAN
----------------------------------------
The condition:

    getLengthSecond() == getLengthFirst() &&
    getLengthSecond() <= 1 &&
    isDual(getFirstList().head()->m_data.m_eItemType)

looks as though it could skip peacetime-value processing based only on the first-side item even if
the second-side item were non-dual.

Current persistence semantics prevent the concerning normal shape:
- initial OPEN_BORDERS / DEFENSIVE_PACT / DISENGAGE items with m_iData<=0 are not saved in the
  initiating mixed deal; they create separate generated team-proxy deals;
- those generated dual proxies contain the same dual item on both sides;
- PEACE_TREATY is saved but `isPeaceDeal()` excludes the whole deal before this shortcut;
- PERMANENT_ALLIANCE is not saved;
- vassal items are not `isDual`.

So the live one-item-each dual case is the intended pure proxy agreement. No hidden GPT/resource
mixed deal is skipped by this shortcut.

No F288.


SMALL METHOD / ACCESSOR FAMILY — CONTROLS
-----------------------------------------
`isVassalTrade`
    whole-list scan; orientation-independent.

`isUncancelableVassalDeal`
    current generated vassal proxy representation gives exactly one vassal/master team side;
    getGivesList(team) contract is satisfied.

`isVassalTributeDeal`
    the static all-resources predicate is vacuously true for an empty list, but the persistent
    instance form cannot become a meaningful empty deal because empty CvDeals are deleted by
    implementAndReturnDeal. No live queue-042 root established.

`isDisengage`
    requires exactly one item on each side and generated disengagement proxies are dual, so the
    first-list head is guaranteed.

`getGivesList/getReceivesList(TeamTypes)`
    assert that exactly one participant belongs to the queried team. Same-team ordinary deals can
    exist after Permanent Alliance, but current team-overload callers first scope deals to two
    distinct relevant teams; no live assertion trigger found.

`isAllDual`
    empty-list true is mathematically vacuous but persistent empty deals are removed immediately;
    display/filter callers therefore see nonempty deals.

`isAnnual/isDual/hasData/isEndWar`
    current callers and trade categories line up with the persistence/valuation semantics examined;
    no omitted current trade enum found.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional; transaction-lifetime root now includes PA + vassal paths
F287 / KI#610 — confirmed provisional
F288 — UNUSED / provisional KI#611 next

NEXT CURSOR
-----------
Continue saturation from F288 unused:
1. cancellation-message `bForce` and other first-list-only representation consumers;
2. resource/GPT individual invalidation repair feasibility and iteration safety for F287;
3. serialization/load/free-list identity and same-team post-PA deals;
4. stopTrading / force-peace / cancellation age call graph after F281/F283 fixes;
5. SAS victory-denial guard completeness and atomicity;
6. remaining history commits touching CvDeal;
7. independent final K-Mod/Civ4CE function-family comparison and root repair boundaries.

Do not manufacture F288 if these resolve as controls/same-root consequences.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP11 — F287 supported tribute/resource expansion + history-oracle controls
===============================================================================

F288 remains UNUSED / provisional KI#611 next.


F287 SAME-ROOT EXPANSION — advc.104m deliberately creates peace+resource/GPT tribute deals
-------------------------------------------------------------------------------------------
WIP09 proved the common supported wartime peace-settlement case:
    TRADE_PEACE_TREATY + TRADE_GOLD_PER_TURN

and showed that practical 2765's broke-GPT verifier can kill the whole still-young deal and thereby
clear temporary forcePeace before PEACE_TREATY_LENGTH.

The history-guided advc.104m pass proves that resource invalidation is not merely a theoretical
structural sibling. AdvCiv deliberately creates PEACETIME help/tribute deals in which an annual
resource can share the same persistent CvDeal with a temporary peace treaty.

HISTORY
-------
Practical count 2070 / commit 507332fd0f introduced:
    "Peace treaty in tribute/help deals; no treaty for vassals."

Its design is explicit:
- at peacetime, CvPlayer::buildTradeTable inserts a hidden TRADE_PEACE_TREATY so the EXE doesn't
  discard the treaty from tribute/help requests;
- AI_askHelp and AI_demandTribute add peace-treaty items to both sides when tradeable;
- the accepted help/tribute bundle is therefore meant to grant a minimum temporary peace period.

Current source preserves this architecture.

CURRENT RESOURCE TRIGGER
------------------------
Current CvPlayerAI::AI_demandTribute supports DEMAND_BONUS.

That branch can insert one or several:
    TradeData(TRADE_RESOURCES, eBonus)

into `humanGives`.

After the requested value is accepted, current code adds:
    weGive      += TRADE_PEACE_TREATY
    humanGives  += TRADE_PEACE_TREATY

when both sides can trade the treaty.

On implementation:
- the first treaty entry is saved as the temporary-peace lifetime owner;
- the demanded resources are persistent and saved in the same CvDeal;
- the duplicate treaty entry is normally collapsed through the existing treaty-reset logic.

The resulting live deal can therefore be:
    peace treaty + one/multiple demanded resources.

If, before the treaty's minimum duration expires, one demanded resource:
- becomes unavailable/oversubscribed;
- loses its trade network;
- becomes obsolete for either side;

CvDeal::verify(direction) returns false for that resource and the OUTER verify kills the ENTIRE
deal.

kill/endTrade then clears TRADE_PEACE_TREATY forcePeace immediately.

Thus a resource promised under an AdvCiv tribute-protected temporary peace can disappear and
prematurely end the very peace guarantee advc.104m added.

CURRENT GPT TRIBUTE SIBLING
---------------------------
AI_demandTribute also supports DEMAND_GOLD_PER_TURN.

That creates exactly the practical-2765 F287 trigger at peacetime:
    demanded GPT + advc.104m temporary treaty.

If the human payer later meets the "100% gold, can't cover payment, negative gold rate, not anarchy"
condition, verify returns false and kills the whole protected tribute deal.

This is independent support for the same root in addition to wartime reparations.

REPAIR IMPLICATION
------------------
F287 should therefore be framed generally:

    Failure of one persistent annual obligation is allowed to destroy a still-uncancelable
    temporary-peace lifetime that shares the same CvDeal.

The correct boundary must preserve the treaty while ending only the failed annual obligation, or
otherwise re-home the exact remaining treaty duration.

This is not a niche wartime-reparations-only problem.


PRACTICAL 1690 PEACE-CANCELLATION ORACLE — CURRENTLY CLEAN
-----------------------------------------------------------
Practical 1690 ("Fix recently introduced bug with peace treaty cancellation") repaired
CvDeal::isCancelable(NO_PLAYER).

The buggy form performed:
    involves(NO_PLAYER)
    isUncancelableVassalDeal(NO_PLAYER)

before calculating age. `verify()` legitimately calls isCancelable(NO_PLAYER) for global
peace-treaty expiry, so that broke expiry.

Current code preserves the fix:
- player-involvement and uncancelable-vassal checks are inside:
      if (eByPlayer != NO_PLAYER)
- then turnsToCancel is evaluated.

`isEverCancelable` is player-specific and all current callers pass a real player.
No new F288 from this history stream.


PRACTICAL 1882 PERMANENT-ALLIANCE ORACLE — CURRENT GUARD PRESERVED
-------------------------------------------------------------------
Practical 1882 ("Fix some recently introduced problems with Perm. Alliances") added an important
same-team guard in `isUncancelableVassalDeal`:

    if (TEAMID(getFirstPlayer()) == TEAMID(getSecondPlayer()))
        return false;

because the team-oriented gives-list helper cannot represent an internal same-team deal.

Current CvDeal preserves that guard.

This does not repair F286: F286 occurs earlier, while addTeam/setVassal synchronously deletes the
active transaction object. But the historical guard confirms that same-team deals can temporarily
survive team merging and that later deal code must be careful with team-oriented accessors.

No independent F288.


PRACTICAL 1997 OPEN-BORDERS ORACLE — CURRENT SENTINEL PRESERVED
---------------------------------------------------------------
Practical 1997 ("Open Borders deals had no effect") corrected team-trade startup tests from:

    trade.m_iData == 0

to:

    trade.m_iData <= 0

after the default TradeData value had changed to -1.

The same correction was applied to:
- TRADE_VASSAL / TRADE_SURRENDER;
- TRADE_OPEN_BORDERS;
- TRADE_DEFENSIVE_PACT;
- TRADE_DISENGAGE.

Current startTrade still uses the <=0 contract. Generated proxy items use positive m_iData=1 and
therefore save rather than recursively spawn another team trade.

No F288.


PRACTICAL 1577/1578 AI-CANCELLATION ORACLE — CURRENT ORIENTATION API IS COHERENT
---------------------------------------------------------------------------------
Those consecutive fixes introduced/cleaned the orientation-aware AI cancellation pipeline:
- ensure the deal is actually between the AI and the queried player before cancellation logic;
- derive our/their gives lists using deal orientation;
- treat resource denial specially;
- preserve renegotiation versus outright cancellation.

Current AI_checkCancel uses:
    kDeal.getGivesList(ePlayer)
    kDeal.getGivesList(getID())

after its caller has ensured:
    pLoopDeal->isBetween(getID(), eOther)

and cancellation eligibility.

The old flip/orientation duplication is gone without reintroducing a first-vs-second assumption.
No current list-orientation regression emerged there.


CANCELLATION `bForce` FIRST-LIST DUAL SCAN — REAL SAME-FAMILY DIAGNOSTIC DEBT, NOT F288
----------------------------------------------------------------------------------------
Current CvDeal::kill decides whether cancellation UI should be forced/play the cancellation sound.

For dual items it scans ONLY getFirstList():

    FOR_EACH_TRADE_ITEM(getFirstList())
        if (isDual(...))
            bForce = true;

Resource-network failure is separately checked across both lists.

History proves this was another practical-2998 list-refactor loss:
- before practical 2998, kill used combined headTradesNode()/nextTradesNode(), scanning BOTH lists
  for duals;
- practical 2998 changed the dual test to firstList only, while explicitly preserving both-list
  resource scans.

Therefore a second-list-only peace treaty (normal representation already proved for F281) can be
announced as a non-forced/silent cancellation even though TRADE_PEACE_TREATY is dual.

This is a genuine UI/diagnostic consequence of the same representation/refactor family, but:
- it does not control gameplay state;
- current F281 can prevent the ordinary scheduled expiry of the same second-list treaty in the
  first place;
- when other teardown paths kill it, the main gameplay consequences are already captured by the
  stronger lifetime findings.

Record as repair-adjacent debt. When practical-2998 list-orientation bugs are corrected, restore
dual visibility across both lists here too. Do not allocate F288 merely for notification priority.


SERIALIZATION / FREE-LIST IDENTITY — CLEAN
------------------------------------------
CvDeal::write/read are symmetric:
    ID
    initial game turn
    first player
    second player
    first list
    second list

CvGame serializes the entire `FFreeListTrashArray<CvDeal>` through
Read/WriteStreamableFFreeListTrashArray.

The free-list stream includes:
- slot count;
- last index;
- free-list head/count;
- current ID;
- every next-free index;
- live object count.

Each CvDeal then reloads its serialized ID, and `FFreeListTrashArray::load` places the object back
into the slot encoded by that ID after asserting it is within the serialized container state.

No queue-042 save/load ID drift or "deal list reloads but ID does not" defect found.


CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional; transaction-lifetime root
F287 / KI#610 — confirmed provisional; now includes supported advc.104m resource/GPT tribute paths
F288 — UNUSED / provisional KI#611 next

NEXT CURSOR
-----------
Continue final saturation:
1. finish practical-history oracles touching CvDeal after 2070 and before/after the 2998 list
   refactor, especially any cancel/tribute/peace fixes that imply adjacent assumptions;
2. inspect SAS victory-denial early return against every normal wartime deal-construction path;
3. finish stopTrading / cancellation age / same-team post-PA consumers;
4. challenge static isVassalTributeDeal empty-list semantics in its AI callers;
5. complete K-Mod/Civ4CE differential and full method/assert/comment inventory;
6. root-independence + repair-boundary closure challenge.

Do not manufacture F288 if the remaining branches resolve as controls or consequences of F281-F287.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP12 — ancestry corrections/refinements for F281/F285; F288 remains unused
===============================================================================

F288 remains UNUSED / provisional KI#611 next.


AUDIT CORRECTION — F281 was introduced by practical 2623, not practical 2998
-------------------------------------------------------------------------------
Earlier queue-042 WIPs correctly proved F281's current behavior and repair boundary but attributed
the first-list-only regression too late.

The local reachable-history mirror gives a precise introduction point.

Before practical 2623, current-era AdvCiv's CvDeal::isPeaceDeal used the combined traversal:

    for (CLLNode<TradeData> const* pNode = headTradesNode(); pNode != NULL;
         pNode = nextTradesNode(pNode))
    {
        if (pNode->m_data.m_eItemType == getPeaceItem())
            return true;
    }

`headTradesNode()/nextTradesNode()` were specifically the combined first-then-second deal traversal.

Practical count 2623 / commit 72a2889d97a5962c893a97d778a45622a84b8829
Title:
    "Use CLinkList traversal macros throughout the DLL"

mechanically changed this exact function to:

    FOR_EACH_TRADE_ITEM(getFirstList())
    {
        if (pItem->m_eItemType == getPeaceItem())
            return true;
    }

and thereby silently dropped the second list.

Practical 2998 later carries/repeats the same modern-list form on a merged/divergent history path,
but it is NOT the earliest introduction.

Older ancestry is additionally reassuring:
- before the combined helper simplification, CvDeal::isPeaceDeal explicitly scanned first list and
  then second list;
- practical 1626 simplified those two loops into the combined headTradesNode traversal while
  preserving both-list semantics;
- K-Mod likewise scans both sides.

So the corrected F281 ancestry is:

    BtS/K-Mod/older AdvCiv: both lists
    practical 1626: still both lists, via combined traversal
    practical 2623: traversal-macro conversion accidentally narrows to first list only
    current Base AdvCiv/SAS: inherits that regression

The F281 gameplay proof and repair remain unchanged.


F285 HISTORY REFINEMENT — blind spot existed initially, was fixed, then reintroduced directionally
-------------------------------------------------------------------------------------------------
Earlier WIP wording over-credited practical 2998 as the source of the AI reparations-embargo
visibility regression. The history is more informative.

1. advc.130f introduction — practical 1383
------------------------------------------
The original "Avoid canceling reparations" safeguard iterated BOTH trade lists, but evaluated each
list separately:

    for each list:
        bPeaceTreaty = false;
        bAnnualPayment = false;
        scan that list;
        if (bPeaceTreaty && bAnnualPayment)
            deny embargo;

Therefore its initial design already missed a deal where:
    peace treaty marker is on one list
    annual payment is on the opposite list.

So the representation hazard existed from the feature's introduction.

2. practical 1618 — blind spot temporarily removed
---------------------------------------------------
Practical 1618 replaced the per-list logic with one combined traversal:

    bPeaceTreaty = false;
    bAnnualPayment = false;
    for (pNode = d->headTradesNode(); pNode != NULL;
         pNode = d->nextTradesNode(pNode))
    {
        ...
    }

Now treaty and annual payment could be detected anywhere in the same deal, including opposite
lists. This temporarily fixed the cross-list correlation problem.

However, that whole-deal test was directionally overbroad: it could also deny the AI merely because
the OTHER side was making an annual payment under the treaty.

3. practical 2574 / commit 3db68c8950b7f878f94ab2965b8c60878b84ea78
----------------------------------------------------------------------------------
Title:
    "Fix a minor issue with AI refusal of trade embargo"

This changed the combined traversal from:

    d->headTradesNode() / nextTradesNode()

to:

    d->headGivesNode(getTeam()) / nextGivesNode(..., getTeam())

i.e. only items GIVEN BY THE AI.

That correctly made `bAnnualPayment` directional: the AI should refuse because IT owes reparations,
not because the other party does.

But it also made `bPeaceTreaty` directional, even though the peace marker is deal metadata whose
saved side is representational. The opposite-list treaty-marker blind spot was therefore
reintroduced.

A corresponding `getGivesList(getTeam())` conversion appears on the later/divergent traversal-macro
history path (practical 2620), and current Base AdvCiv/SAS retain the gives-side-only form.

Practical 2998 is therefore not the semantic introduction of F285; it only preserves the already
directional traversal in the newer list API.

CORRECT MODERN REPAIR, NOW EXPLAINED BY HISTORY
-----------------------------------------------
The history itself separates the two concepts that current code incorrectly binds to one scan:

- PEACE-TREATY EXISTENCE:
    whole-deal property; inspect both lists / fixed `d->isPeaceDeal()`.

- ANNUAL REPARATION OWED BY THIS AI:
    directional property; inspect `d->getGivesList(getTeam())` for RESOURCES/GPT.

That combines:
- practical 1618's correct whole-deal treaty visibility;
- practical 2574's correct payer-directionality.

This is stronger than simply reverting to any one historical implementation.

F285's current reproduction and independence from F281 remain unchanged:
choose an orientation with treaty in first list and AI GPT in second list; current isPeaceDeal
already sees the treaty, while AI_stopTradingTrade fails its own denial.


RELATED CANCELLATION bForce LINEAGE — CONTROL ONLY
---------------------------------------------------
The cancellation-notification `bForce` dual-item scan has a similar traversal history but remains
below the numbered-finding bar.

- advc.106j originally used first-list-oriented logic.
- practical 1626 broadened the dual/resource traversal to the combined headTradesNode sequence.
- practical 2623 converted the dual detection back to first-list-only while separately preserving
  resource-network checks across both lists.

So a normal second-list-only peace treaty can again be missed by the "force cancellation
notification/sound" classification.

That is real diagnostic/UI debt and should be considered alongside F281's list repair, but it does
not independently corrupt diplomatic state. Keep it unnumbered unless a stronger gameplay
dependency is established.


CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional; ancestry corrected to practical 2623
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional; history refined through 1383 -> 1618 -> 2574
F286 / KI#609 — confirmed provisional
F287 / KI#610 — confirmed provisional
F288 — UNUSED / provisional KI#611 next

NEXT CURSOR
-----------
Continue final saturation:
1. close static isVassalTributeDeal empty-list semantics against all callers;
2. prove or reject SAS victory-denial guard bypass through any normal wartime implementDeal path;
3. inspect TRADE_PEACE / TRADE_EMBARGO reentrant state transitions for active-deal deletion;
4. finish same-team post-PA accessor/cancellation consumers;
5. complete current-source FAssert/FError/TODO/comment inventory;
6. final K-Mod/Civ4CE differential + root-independence/repair-boundary challenge.

If those remain controls, close queue 042 with F281-F287 and leave F288 unused.
===============================================================================


===============================================================================
QUEUE 042 CONTINUATION — CvDeal.cpp
C025-WIP13 — F288 / provisional KI#611 confirmed
===============================================================================

F288 / provisional KI#611
-------------------------
TITLE
A supported human Permanent Alliance + voluntary-vassal mixed bundle can make the surviving team
its OWN vassal (`m_eMaster == getID()`), leaving persistent team-state corruption.

SUPPORTED BUNDLE / PREVALIDATION
--------------------------------
Current diplomacy deliberately allows arbitrary mixed item classes when BOTH players are human:

    if (!isHuman() || !GET_PLAYER(eOtherPlayer).isHuman())
    {
        ... annual/nonannual hiding ...
    }
    // "everything allowed in human-human trades"

The trade inventory independently exposes both:
    TRADE_VASSAL
    TRADE_PERMANENT_ALLIANCE

when their ordinary individual conditions hold.

At peacetime between two separate one-member free teams:
- TRADE_VASSAL can pass because neither side is already a vassal and vassal-state trading is
  available;
- TRADE_PERMANENT_ALLIANCE can pass because neither side is a vassal, the teams are distinct and
  at peace, PA trading is available, and each team has one member.

`updateTradeList` has special mutual hiding for:
- vassal vs the OTHER side's vassal item;
- wartime vassal/surrender vs an end-war item;

but there is no PA-vassal incompatibility rule.

`CvDeal::addTradeItems` then validates every item while the two teams are still separate. Thus both
items can pass the bundle's prevalidation.

EXECUTION ORDER
---------------
The two-pass implementation intentionally postpones vassal/surrender items.

TRADE_PERMANENT_ALLIANCE:
- processed in pass 0;
- startTrade itself does nothing;
- `bAlliance = true`.

TRADE_VASSAL:
- processed in pass 1;
- startTeamTrade creates the ordinary team-vassal proxy deal(s);
- `GET_TEAM(eFromPlayer).setVassal(eToTeam, true, false)` establishes the vassal relation.

Only AFTER both item passes does addTradeItems execute the alliance:

    if (bAlliance)
    {
        if (eFirstTeam < eSecondTeam)
            GET_TEAM(eFirstTeam).addTeam(eSecondTeam);
        else if (eSecondTeam < eFirstTeam)
            GET_TEAM(eSecondTeam).addTeam(eFirstTeam);
    }

The lower team ID survives the Permanent Alliance.

CONCRETE CORRUPTION ORIENTATION
-------------------------------
Let:
    Team M = lower-ID surviving team
    Team V = higher-ID absorbed team

and let V give TRADE_VASSAL to M in the same bundle that contains the PA.

Immediately before addTeam:
    V.m_eMaster == M
    M.m_eMaster == NO_TEAM

Then:
    M.addTeam(V)

reaches AdvCiv's optimized single-master PA migration:

    if (GET_TEAM(eTeam).isAVassal())
    {
        bool bCapitulated = GET_TEAM(eTeam).isCapitulated();
        setVassal(getMasterTeam(), false, bCapitulated);
        setVassal(GET_TEAM(eTeam).getMasterTeam(), true, bCapitulated);
    }

Substitute the live values:

1. Survivor M is free, so:
       M.getMasterTeam() == M

2. First call:
       M.setVassal(M, false, ...)

   `M.isVassal(M)` is false while m_eMaster==NO_TEAM, so the early equality check sees:
       false == false
   and returns immediately. This does NOT clear anything.

3. Absorbed V's master is M:
       V.getMasterTeam() == M

4. Second call:
       M.setVassal(M, true, ...)

   This proceeds and writes:
       M.m_eMaster = M

The surviving team has become its own vassal.

WHY THE SELF-VASSAL DOES NOT SELF-CORRECT
-----------------------------------------
`setVassal(M,true)` updates the Agent caches as a real vassal relation, including inserting Team M
into the VASSAL_ALIVE cache for master M.

Its later "all our vassals now become free" loop can encounter the self relation. However
`freeVassal(M)` frees vassalage by finding and killing an actual TRADE_VASSAL/SURRENDER deal between
master M and vassal M.

At that point the real vassal proxy is still between distinct player teams M and V; there is no
M<->M vassal deal. Therefore `freeVassal(M)` does not clear M.m_eMaster.

The same loop subsequently frees the real V->M proxy relation, but that changes V's master state,
not M's newly-created self-master state.

Later, V's players are reassigned to M. V's alive count falls to zero.

CvTeam::changeAliveCount(0) frees living teams whose:
    kLoopTeam.isVassal(V)

But M now has:
    M.m_eMaster == M

not V. Therefore absorbed-team death cleanup also does not clear the self-vassal.

There is no later `m_eMaster = NO_TEAM` normalization in addTeam.

The corrupted state survives the merge.

MIRROR ORIENTATION — IMPORTANT BOUNDARY
----------------------------------------
If instead the LOWER-ID survivor was the vassal of the HIGHER-ID absorbed master immediately before
the PA:
    survivor.m_eMaster == absorbedTeam

the `GET_TEAM(eTeam).isAVassal()` branch is skipped because the absorbed master is free.

When absorbedTeam later reaches alive-count zero, CvTeam::changeAliveCount sees the survivor as a
living vassal of the dead absorbed team and calls:
    survivor.setVassal(absorbedTeam, false, ...)

So that mirror orientation happens to clean up.

F288 is therefore orientation-dependent on team IDs and which side gives voluntary vassalage, but
the bad orientation is fully supported and deterministic.

LIVE CONSEQUENCES
-----------------
Self-vassal is not cosmetic.

`isAVassal()` is:
    return (m_eMaster != NO_TEAM);

so it becomes true.

`getMasterTeam()` returns:
    m_eMaster == NO_TEAM ? getID() : m_eMaster

and therefore still returns M, hiding the corruption from some equality tests while the vassal flag
remains set.

Examples of broad consequences:
- CvTeam::canChangeWarPeace immediately returns false when `isAVassal()`;
- many diplomatic/trade gates reject vassals;
- population/land functions with vassal-aware accounting take the `isAVassal()` branch and can
  halve the team's own totals in those contexts;
- AI/iterator predicates classify the team as a vassal;
- CvAgents' VASSAL_ALIVE relation cache contains the team under itself;
- the relation is ordinary CvTeam state and is serialized, so the corruption can survive saves.

Additional downstream behavior may be worse, but these are already sufficient to establish a live
state bug.

ANCESTRY
--------
The human-human "everything allowed" mixed-trade behavior is inherited from K-Mod.

K-Mod's older vassal representation used a per-team boolean relation matrix. Its PA merge logic
iterated OTHER teams and explicitly excluded both merging team IDs while migrating vassal
relations. A direct vassal relation between the two teams being merged therefore was not converted
into a self-vassal relation.

AdvCiv later replaced the relation matrix with a single `m_eMaster`.

Practical count 1840 / commit 8cd5a498cfb33f035393333903c604d49410673c
Title:
    "Bugfixes: Vassals of Permanent Alliance; AgentIterator"

introduced the current single-master PA migration block:

    if (GET_TEAM(eTeam).isAVassal())
    {
        ...
        setVassal(getMasterTeam(), false, bCapitulated);
        setVassal(GET_TEAM(eTeam).getMasterTeam(), true, bCapitulated);
    }

This correctly handles an absorbed team that is vassal of some THIRD master, but it does not
special-case the master being the very team that is absorbing it.

Base AdvCiv 1.14 contains the same logic; SAS inherits it.

ROOT INDEPENDENCE
-----------------
F288 is independent of F281-F287.

It requires neither:
- list-orientation peace classification;
- cancellation-memory accumulation;
- deal-renewal lifetime;
- player-death proxy ownership;
- embargo reparations detection;
- active-deal self-deletion;
- annual-item verification.

Its root is bundle-level compatibility plus PA vassal-state migration:
    individually valid PA + vassal items create a transient direct master/vassal relation between
    the two teams immediately before those same teams merge.

REPAIR BOUNDARY
---------------
The cleanest repair is to reject/normalize the contradictory bundle BEFORE either state transition:

    A Permanent Alliance and a VASSAL/SURRENDER relation between the same two teams should not both
    be implemented as independent items in one transaction.

Possible approaches:
1. bundle validation:
   reject PA+VASSAL/SURRENDER combinations;
2. normalization:
   if PA is present, ignore/drop direct vassal/surrender items between the merging teams;
3. defensive addTeam hardening:
   when absorbed team's master is `getID()`, do NOT call `setVassal(getID(), true)`;
   likewise explicitly clear any direct survivor<->absorbed vassal relation before merge.

A robust repair should ideally do both:
- prevent contradictory trade construction;
- make addTeam resilient to an externally forced/directly constructed equivalent state.

Do not merely add `FAssert(eMaster != getID())` to setVassal: Release would still corrupt state, and
other legitimate callers should not have to absorb a malformed PA transaction after the fact.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional
F287 / KI#610 — confirmed provisional
F288 / KI#611 — confirmed provisional
F289 — UNUSED / provisional KI#612 next

CONTROLS CLOSED IN THIS SLICE
-----------------------------
- Static `isVassalTributeDeal(empty)` is vacuously true, but its AI_considerOffer call path requires
  positive received value from the tested list; an empty list cannot satisfy that gate. Persistent
  empty CvDeals are deleted immediately. No live empty-list trigger.
- Normal TRADE_PEACE / TRADE_WAR / TRADE_EMBARGO trade-table target generation excludes the
  negotiating partner's team and uses a third-team iterator. Ordinary brokered state changes
  therefore do not target the active deal partner and synchronously delete the active broker deal.
- Random-event BonusGift implementDeal calls currently come from Brothers In Need triggers whose
  other-player-war requirement is false; those resource gifts therefore occur at peacetime and do
  not bypass the SAS wartime victory-denial peace guard.
- Normal AI wartime peace reaches AI_negotiatePeace with explicit PEACE_TREATY. Direct helper
  signPeaceTreaty likewise supplies PEACE_TREATY; OB/DP/disengage and voluntary-vassal direct paths
  are peacetime. No normal wartime implementDeal route without the guard's treaty marker was found.
- PA + resource/GPT can leave a persistent deal between now-same-team players, but players retain
  separate gold/resource economies; no independent corruption was established from that alone.

NEXT CURSOR
-----------
Continue final saturation from F289 unused:
1. challenge PA+SURRENDER and any other contradictory human mixed-bundle pairs; classify under F288
   unless the root/consequence is genuinely independent;
2. finish current-source FAssert/FError/TODO and all ~40 CvDeal method-family inventory;
3. inspect post-PA same-team deal consumers and cancellation accessors one last time;
4. final independent K-Mod/Civ4CE differential;
5. explicit repair-boundary/root-independence table for F281-F288;
6. if no F289 survives, close queue 042 and advance to priority queue 047 CvDllPythonEvents.cpp.

Do not manufacture F289 merely because this file has been unusually productive.
===============================================================================


================================================================================
WORKING CHECKPOINT C025-WIP14 - CvDeal.cpp STILL OPEN
================================================================================

STATUS
------
- Cumulative continuation of C025-WIP13.
- Queue 042 `CvGameCoreDLL/CvDeal.cpp` remains OPEN.
- F281-F288 remain confirmed provisional.
- F289 / provisional KI#612 is now CONFIRMED.
- F290 is UNUSED / provisional KI#613 next.
- No repository source files modified.

F289 / PROVISIONAL KI#612
=========================
VOLUNTARY VASSAL SPONSOR CAN HIRE AN AI TO DECLARE WAR ON THE SPONSOR'S OWN
MASTER / SAME-MASTER COALITION, IMMEDIATELY MAKING THE SPONSOR AN ENEMY OF THE
HIRED TEAM

SUMMARY
-------
The brokered-war trade contract checks the relationship between the HIRELING and
the proposed target, but incompletely checks the relationship between the
SPONSOR and that target.

`CvTeamAI::AI_declareWarTrade(eTarget,eSponsor,...)` rejects:

    GET_TEAM(eTarget).isVassal(eSponsor)
    GET_TEAM(eTarget).isDefensivePact(eSponsor)

but does not reject the reverse master/vassal direction:

    GET_TEAM(eSponsor).isVassal(eTarget)

nor, more generally, a target in the sponsor's own master/vassal locus.

That leaves a supported live configuration in which a voluntary vassal asks an
AI civ to declare war on the vassal's own master. The requested declaration
then invokes the normal vassal-alignment cascade, which causes the hired AI to
declare war on the sponsor as well.

The accepted diplomatic transaction therefore destroys the diplomatic
relationship it was negotiated through and turns the sponsor into an enemy of
the team it just hired.

This is an independent diplomatic target-validity defect.

It ALSO provides a new concrete trigger for F286: the nested war declaration
kills the current sponsor/hireling CvDeal while `CvDeal::addTradeItems` is still
executing, after which `CvGame::implementAndReturnDeal` can dereference the
freed deal. That lifetime consequence remains under F286 rather than being
double-counted here.

LIVE CONSTRUCTION
-----------------
Let:

    A = AI hireling team
    B = voluntary-vassal sponsor team
    C = B's master team

Initial state:

    B.isVassal(C) == true
    B.isCapitulated() == false
    A is at peace with B and C
    A has met B and C
    A can otherwise declare war on C
    attitudes / power allow A to accept B's war request

B opens diplomacy with A and asks:

    A: declare war on C

This is not an artificial direct-C++ construction. Current trade-table and
trade-validity code can expose/accept it.

TRADE-TABLE REACHABILITY
------------------------
For an AI player, `CvPlayer::buildTradeTable` handles TRADE_PEACE, TRADE_WAR and
TRADE_EMBARGO by iterating every major third team except:

    - the AI's own team, through NOT_SAME_TEAM_AS;
    - the negotiating partner's exact team, through:

          if (itThirdTeam->getID() == TEAMID(eOtherPlayer))
              continue;

If B is a voluntary vassal of C, C is a distinct third team.

Therefore C is enumerated as a possible TRADE_WAR target while A negotiates
with B.

There is no corresponding exclusion for:

    itThirdTeam->getMasterTeam() == GET_TEAM(B).getMasterTeam()

so the sponsor's own master remains a candidate.

STRUCTURAL TRADE VALIDITY
-------------------------
`CvPlayer::canPossiblyTradeItem(... TRADE_WAR ...)` rejects the sponsor only
when the sponsor is CAPITULATED:

    return (!kOurTeam.isHuman() && !kOurTeam.isAVassal() &&
            !kToTeam.isCapitulated());

Here:
- kOurTeam = A, the AI hireling;
- kToTeam = B, the sponsor.

B is a voluntary vassal, not capitulated, so the broker role remains allowed.

Current SAS `CvPlayer::canTradeItem(... TRADE_WAR ...)` then checks the TARGET:

    bool const bValidTargetStatus =
        (!kTargetTeam.isAVassal() ||
         (getUWAI().isEnabled() && !kTargetTeam.isCapitulated()));

    if (bValidTargetStatus &&
        kOurTeam.isHasMet(eTargetTeam) &&
        kToTeam.isHasMet(eTargetTeam) &&
        kOurTeam.canDeclareWar(eTargetTeam))
    {
        bValid = true;
    }

For target C, the sponsor's FREE master:
- C is not a vassal, so target status passes even without UWAI;
- A and B naturally know C in the relevant configuration;
- A canDeclareWar(C) can be true.

Nothing here checks that B and C belong to the same master/vassal coalition.

AI DENIAL IS ASYMMETRIC
-----------------------
`CvPlayer::getTradeDenial(TRADE_WAR)` routes to:

    kOurTeam.AI_declareWarTrade(eTarget, TEAMID(eWhoTo))

so A evaluates C as target and B as sponsor.

The opening special relationship check is:

    if (GET_TEAM(eTarget).isVassal(eSponsor) ||
        GET_TEAM(eTarget).isDefensivePact(eSponsor))
        return DENIAL_JOKING;

For target C and sponsor B:
- `C.isVassal(B)` is false;
- master/vassal is not represented as a defensive pact;
- the REVERSE fact `B.isVassal(C)` is never tested.

The remainder of AI_declareWarTrade can therefore return NO_DENIAL when
attitude, war-load and power conditions are favorable.

This is not merely a trade-table cosmetic: the AI can genuinely accept.

WHY `canDeclareWar(C)` DOES NOT SAVE THE CONTRACT
-------------------------------------------------
`CvTeam::canDeclareWar(C)` answers whether A itself can legally enter war
against C.

It checks:
- team inequality / alive status;
- existing war;
- contact;
- permanent-war/peace constraints;
- force peace;
- game options;
- Python vetoes.

It does not and should not know that B is the sponsor of the proposed trade.

Thus A can perfectly legally declare war on C even though B, the broker asking
for that declaration, is C's voluntary vassal.

The missing invariant is specifically a WAR-TRADE sponsor/target relationship
constraint.

SYNCHRONOUS SELF-DEFEATING OUTCOME
----------------------------------
`CvDeal::startTrade(TRADE_WAR)` executes:

    GET_TEAM(eFromPlayer).declareWar(eAttackedTeam, ... eToPlayer);

For this example:

    A.declareWar(C, ..., sponsor=B)

`CvTeam::declareWar` sets A<->C at war, then iterates third teams.

For B:

    B.isVassal(C) == true

so it queues:

    queueWar(A, B, ..., WAR_DECLARATION_VASSAL_ALIGNMENT)

The function then calls:

    triggerWars(true);

BEFORE returning to `CvDeal::startTrade`.

`triggerWars` drains the queue synchronously and invokes:

    A.declareWar(B, ...)

That secondary declaration kills all active A<->B deals at the opening of
`CvTeam::declareWar`.

Therefore the sponsor who hired A to attack C immediately becomes an enemy of
A because B is C's vassal.

The diplomatic action defeats its own negotiating relationship in the same
call stack.

F286 SAME-ROOT LIFETIME EXPANSION
---------------------------------
The secondary A->B declaration also kills the CURRENT in-progress sponsor /
hireling deal.

That means:

    CvDeal::addTradeItems
      -> startTrade(TRADE_WAR)
        -> A.declareWar(C)
          -> triggerWars
            -> A.declareWar(B)
              -> kill all A<->B deals
                 -> current CvDeal deleted
      -> returns into methods still executing through `this`

and finally:

    CvGame::implementAndReturnDeal
        pDeal->getLengthFirst()
        pDeal->getLengthSecond()

can dereference a freed `pDeal`.

This is additional concrete evidence for F286's transaction-lifetime root.
Do NOT create another lifetime finding for the same deletion mechanism.

BROADER SAME-MASTER-LOCUS CASE
------------------------------
The clean invariant is broader than merely:

    sponsor.isVassal(target)

Current SAS, after KI#373 / F052 repair work, can also permit a VOLUNTARY
VASSAL as a TRADE_WAR target under full UWAI.

That means sibling voluntary vassals under the same master can also become
named sponsor/target pairs in configurations where the item otherwise passes.

War against one sibling aligns the common master and then the other sibling,
again turning the sponsor into the hireling's enemy.

The robust relation test is therefore the coalition identity:

    GET_TEAM(eTarget).getMasterTeam() ==
        GET_TEAM(eSponsor).getMasterTeam()

`CvTeam::getMasterTeam()` returns the team's own ID for a free team, so this
single comparison covers:
- sponsor -> own master;
- sponsor -> sibling vassal;
- other teams inside the same master/vassal locus.

Exact sponsor==target is already excluded by normal third-team generation and
caller assertions, but a structural guard can remain explicit if desired.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same asymmetric AI denial:

    target.isVassal(sponsor) || target.isDefensivePact(sponsor)

K-Mod and Civ4CE likewise carry the inherited target-is-vassal-of-sponsor
check without the reverse/same-master-locus normalization.

Therefore the master-target form of F289 is inherited rather than SAS-created.

Current SAS's KI#373 / F052 voluntary-vassal target support can make the
broader sibling-vassal form reachable under UWAI, but that is an expansion of
the same underlying missing sponsor/target coalition check, not a separate
root.

RELATION TO F052 / KI#373
-------------------------
F052 was the opposite contract problem:

    intended FOREIGN voluntary-vassal targets were universally blocked.

Its repair deliberately made a voluntary vassal target legal under supported
UWAI conditions.

F289 does NOT argue that voluntary vassals should again be universally banned.

Instead, it distinguishes:
- FOREIGN voluntary vassal: valid supported war-trade target;
- sponsor's own master/vassal coalition: invalid self-defeating target.

Repairing F289 with a same-master-locus test preserves F052's intended foreign
voluntary-vassal feature.

ROOT INDEPENDENCE
-----------------
F289 is independent of F281-F288 as a gameplay/diplomacy defect.

Even if F286 is repaired perfectly so current deals cannot self-delete:
- B can still request war on its own master C;
- A can still accept;
- normal vassal alignment still makes A and B enemies immediately.

So the semantic defect survives the lifetime repair.

Conversely, rejecting malformed sponsor/target war trades would remove this
particular F286 trigger but would NOT fix F286's other confirmed self-deletion
paths (e.g. mixed Permanent Alliance state transitions).

The roots are therefore separate:
- F286 = transaction object lifetime under nested state changes;
- F289 = missing coalition constraint on brokered-war target semantics.

REPAIR BOUNDARY
---------------
A robust repair should enforce the sponsor/target coalition invariant at the
structural trade layer, not only as AI preference.

For TRADE_WAR, reject a target when:

    GET_TEAM(eTarget).getMasterTeam() ==
        kToTeam.getMasterTeam()

where kToTeam is the sponsor / negotiating partner.

This belongs naturally in `CvPlayer::canTradeItem(TRADE_WAR)` because
`canTradeItem(..., bTestDenial=false)` is widely used as game-rule validity and
trade-table construction.

Then harden `CvTeamAI::AI_declareWarTrade` with the same relationship rule
(or assert that structural callers already excluded it), so direct AI callers
cannot reintroduce the contradiction.

Do NOT repair by:
- banning all voluntary-vassal targets again;
- checking only `sponsor.isVassal(target)`, which misses sibling vassals;
- relying only on `DENIAL_JOKING`, because structural / forced callers may use
  `canTradeItem` without denial testing;
- suppressing the vassal-alignment war cascade, which is correct war-state
  behavior once a valid declaration occurs.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional
F287 / KI#610 — confirmed provisional
F288 / KI#611 — confirmed provisional
F289 / KI#612 — confirmed provisional
F290 — UNUSED / provisional KI#613 next

NEXT CURSOR
-----------
Continue saturation rather than searching for a tenth root by count:
1. finish CvDeal assertion/TODO/method-family inventory;
2. recheck PA/post-merge same-team deal consumers;
3. challenge brokered TRADE_PEACE / TRADE_EMBARGO relationship symmetry using
   the same sponsor/master lens, but keep them controls unless a distinct live
   failure survives;
4. do final K-Mod/Civ4CE differential and historical repair-boundary pass;
5. build an explicit F281-F289 root-independence/repair table;
6. if F290 does not survive, close queue 042 and advance to priority queue 047
   `CvDllPythonEvents.cpp`.

Do not manufacture F290.
===============================================================================


================================================================================
WORKING CHECKPOINT C025-WIP15 - CvDeal.cpp STILL OPEN
================================================================================

STATUS
------
- Cumulative continuation of C025-WIP14.
- Queue 042 `CvGameCoreDLL/CvDeal.cpp` remains OPEN.
- F281-F289 remain confirmed provisional.
- F289 repair boundary is REFINED below: canonical master coalitions and their
  Defensive Pacts matter, not only an exact shared master.
- F290 / provisional KI#613 is now CONFIRMED as a low-severity
  diagnostic/state-cleanup defect.
- F291 is UNUSED / provisional KI#614 next.
- No repository source files modified.

F289 REPAIR-BOUNDARY REFINEMENT
===============================
THE SPONSOR/TARGET TEST MUST CANONICALIZE BOTH SIDES TO THEIR MASTER TEAMS
BEFORE CHECKING DEFENSIVE-PACT RELATIONSHIPS

WIP14 correctly established that a brokered war must not target the sponsor's
own master/vassal locus. A further cascade check shows that an exact
same-master comparison is necessary but not sufficient.

Additional live construction:

    A = AI hireling
    B = voluntary-vassal sponsor
    M = B's master
    C = proposed foreign target

with:

    B.isVassal(M) == true
    M.isDefensivePact(C) == true
    B and C are not directly in a Defensive Pact
    A is otherwise legally able/willing to declare war on C

Current `AI_declareWarTrade(C,B,...)` checks:

    C.isVassal(B)
    C.isDefensivePact(B)

Both can be false.

But when A executes the bought declaration against C:
1. C's Defensive Pact causes M to enter war against A;
2. B is M's vassal;
3. vassal alignment then brings B into war against A;
4. the sponsor B has again become an enemy of the civilization it just hired;
5. the current A<->B implementing deal can again be killed synchronously,
   expanding F286's trigger set.

The same issue generalizes when the named target is itself a voluntary vassal:
the meaningful target coalition is its master team, not merely the nominal
target team.

Therefore the robust F289 relation is approximately:

    TeamTypes const eSponsorMaster =
        GET_TEAM(eSponsor).getMasterTeam();
    TeamTypes const eTargetMaster =
        GET_TEAM(eTarget).getMasterTeam();

    if (eSponsorMaster == eTargetMaster ||
        GET_TEAM(eSponsorMaster).isDefensivePact(eTargetMaster))
    {
        reject;
    }

The exact implementation should preserve the normal symmetric DP invariant and
should be applied at the structural TRADE_WAR validity boundary as well as
AI denial hardening.

This refinement remains F289, not F290: it is the same missing
"sponsor must not buy a war that necessarily places the sponsor's coalition at
war with the hireling" contract.

F290 / PROVISIONAL KI#613
=========================
PERMANENT-ALLIANCE DEAL CLEANUP LOSES THE OLD TEAM IDENTITIES AFTER PLAYER
REASSIGNMENT; A VALID PRE-ALLIANCE DEFENSIVE PACT IS ENDED AS A SELF-PACT,
TRIGGERING ADVCIV/SAS ASSERTIONS

SEVERITY / SCOPE
----------------
Low severity, but confirmed.

The strongest current consequence is diagnostic:
- a normal Permanent Alliance formed while a Defensive Pact is active can hit
  `FAssert(getID() != eIndex)` in `CvTeam::setDefensivePact`;
- this assertion is active in both Debug and the project's separate optimized
  Assert configuration.

Release builds normally avoid a direct gameplay failure from the old DP bits
because the absorbed team is dead and alive-team iterators exclude it.
The cleanup also writes cancellation memory among the now-permanently allied
players, but same-team attitude is forced to 100 and a Permanent Alliance does
not later split, so that pollution is currently strategically inert.

This finding should therefore be treated as a real teardown-identity /
diagnostic defect, not overstated as a release gameplay corruption.

LIVE / INTENDED REACHABILITY
----------------------------
A Defensive Pact can legitimately still be active when a Permanent Alliance is
signed.

`CvPlayer::canPossiblyTradeItem(TRADE_PERMANENT_ALLIANCE)` requires:
- neither team is a vassal;
- distinct teams;
- no war;
- PA trading enabled;
- one member on each team.

It does NOT require the teams to lack Open Borders or a Defensive Pact.

More strongly, AI PA acceptance explicitly uses alliance history:

    AI_getDefensivePactCounter(eWithTeam) +
    AI_getShareWarCounter(eWithTeam) >= 40

and the Defensive Pact counter is incremented while:

    isDefensivePact(eOther)

is true.

Thus "active DP -> later PA" is not a synthetic malformed state; it is one of
the ordinary relationship histories the PA AI is designed around.

HOW THE OLD DP IS STORED
------------------------
Signing a DP calls:

    startTeamTrade(TRADE_DEFENSIVE_PACT, fromTeam, toTeam, true)

which creates persistent team-proxy `CvDeal`s for player pairs with a
Defensive-Pact item in both sides of each proxy deal.

The old pact therefore has live deals to clean up when the PA merges the teams.

THE K-MOD ORDERING CHANGE
-------------------------
BtS / Civ4CE cancel the inter-team OB/DP/peace/vassal deals before the members
of the absorbed team are reassigned.

K-Mod practical history commit 386 deliberately moved the entire cancellation
block *after* reassignment:

    "When forming a permanent alliance, deals are now canceled /after/ the team
     numbers have been reassigned. This is to prevent the new allies from being
     bumped..."

and its source comment says:

    "I've done this so that when open-borders is canceled,
     it doesn't bump our new allies out of our borders."

After the move, K-Mod identifies the old inter-team deals by the fact that both
participants are now on the surviving team.

AdvCiv and SAS inherit this ordering.

IDENTITY LOSS IN CURRENT ADDTEAM
--------------------------------
Current `CvTeam::addTeam(eTeam)` eventually does:

    for (...) {
        if (GET_PLAYER(i).getTeam() == eTeam)
            GET_PLAYER(i).setTeam(getID());
    }

Only afterward does it scan:

    if (!pLoopDeal->isBetween(getID(), getID()))
        continue;

and kill old OB / DP / peace / vassal / surrender / disengage deals.

That is enough to recognize which deals became internal, but it destroys the
old team identity needed by `CvDeal::endTrade`.

`CvDeal` stores player IDs, not historical team IDs.

When `kill()` later runs `endTrade`, every team lookup is based on the players'
CURRENT teams.

DEFENSIVE-PACT ASSERTION
------------------------
For an old M<->V DP proxy, after V's player has been reassigned to M:

    TEAMID(eFromPlayer) == M
    TEAMID(eToPlayer)   == M

`CvDeal::endTrade(TRADE_DEFENSIVE_PACT)` executes:

    GET_TEAM(eFromPlayer).setDefensivePact(
        TEAMID(eToPlayer), false);

which has become:

    M.setDefensivePact(M, false);

Current AdvCiv/SAS `CvTeam::setDefensivePact` begins:

    FAssert(getID() != eIndex); // advc

so the ordinary PA cleanup now calls a function under a condition that the
function explicitly asserts must not happen.

The subsequent equality early return does not save the diagnostic contract:
the assertion occurs first.

ASSERT BUILD IS REAL
--------------------
This is not Debug-only in the project.

The existing project configuration has an optimized Assert target using
`/DFASSERT_ENABLE` without `_DEBUG`, and `FAssert` is compiled whenever
FASSERT_ENABLE is present.

So a normal PA+DP teardown can fire in:
- Debug;
- optimized Assert.

This complements the earlier F273 finding: F273 found valid assertions that SAS
accidentally suppressed in the Assert configuration; F290 is the opposite
diagnostic problem, a valid game transition reaching an assertion whose
precondition was invalidated by teardown ordering.

ANCESTRY / COMBINATION
----------------------
Civ4CE retains the older ordering: relevant deals are killed before player-team
reassignment, so `endTrade` still sees the real old team pair.

K-Mod commit 386 introduced the post-reassignment cancellation specifically to
avoid Open-Borders unit bumping. K-Mod's `setDefensivePact` has only enum-range
assertions and no `getID() != eIndex` assertion, so the self-target teardown is
less visible there.

AdvCiv practical commit 1842 later added:

    FAssert(getID() != eIndex); // advc

to `CvTeam::setDefensivePact` while retaining K-Mod's post-reassignment PA
cleanup.

The confirmed false assertion is therefore an AdvCiv/SAS interaction between:
1. K-Mod's intentional PA cleanup ordering; and
2. AdvCiv's later self-team DP invariant assertion.

RELEASE-BUILD STATE EFFECT
--------------------------
With assertions disabled, `M.setDefensivePact(M,false)` ordinarily sees the
self DP bit already false and returns without touching the original M<->V
relation.

So the old DP bits keyed to absorbed team V are not actually cleared through
this path.

At that point V's final live member has already been moved to M, and
`CvPlayer::setTeam` has reduced V's alive count to zero.

Current live consumers such as `getDefensivePactCount` iterate ALIVE major
teams, which excludes V. A newly reused team is initialized/reset before normal
use.

Therefore no present release gameplay consequence from those stale DP bits has
been proved, and this checkpoint deliberately does not claim one.

SAME-TEAM CANCELLATION MEMORY
-----------------------------
AdvCiv's expanded `endTrade` also calls `addEndTradeMemory` when an OB or DP
team trade ends and both players are alive.

After PA reassignment, both the "ending" and "remembering" player team
iterators refer to the same newly merged team. This can write
MEMORY_CANCELLED_OPEN_BORDERS / MEMORY_CANCELLED_DEFENSIVE_PACT among the
permanent allies (including self-pairs through the nested member loops).

This is genuine teardown pollution, but currently inert for diplomacy:
`CvPlayerAI::AI_getAttitudeVal` returns 100 immediately for another player on
the same team, and a PA is irreversible.

It is evidence that the old identity is broadly lost during deal teardown, not
a separate finding.

WHY THIS IS NOT JUST "REMOVE THE ASSERT"
----------------------------------------
The assert is exposing a real mismatch:
- the deal being canceled semantically belonged to old teams M and V;
- the generic endTrade routine is being asked to interpret it after both
  participants have become M.

Deleting the assert would merely hide that mismatch and preserve the stale
old-team relation / same-team memory behavior.

ROOT / REPAIR BOUNDARY
----------------------
The repair should preserve the old team identities while ending the old
team-level agreement, or explicitly tear down the old M<->V team state rather
than asking generic `CvDeal::endTrade` to rediscover the old teams from players
whose team IDs have already changed.

Do not simply restore the entire pre-K-Mod cancellation block wholesale:
K-Mod moved it for a real reason -- canceling Open Borders before reassignment
can bump the soon-to-be allies out of each other's territory.

Safer repair families include:
- keep the old team IDs in a specialized PA teardown path;
- separate Open-Borders's unit-bump-sensitive ordering from DP/peace/etc.;
- explicitly clear the old M<->V relation after reassignment while suppressing
  generic same-team endTrade side effects, then delete the proxy deals.

The important invariant is:
    "tearing down an agreement keyed to old teams must not infer those old team
     IDs from participants after their team ownership has been rewritten."

ROOT INDEPENDENCE
-----------------
F290 is independent of F288.

F288:
- requires a mixed PA+vassal bundle;
- corrupts the surviving team into a self-vassal;
- is a release gameplay/state bug.

F290:
- needs only an ordinary pre-existing DP plus a later PA;
- arises when old team-proxy deals are canceled after participant reassignment;
- currently has its strongest consequence in Assert/Debug diagnostics.

Repairing the mixed-bundle self-vassal issue does not change the old DP teardown
path.

F290 is also independent of F286: no self-deletion/UAF is required. The old DP
proxy being canceled is intentionally being deleted; the problem is the team
identity used while ending it.

CURRENT QUEUE STATE
-------------------
queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
F281 / KI#604 — confirmed provisional
F282 / KI#605 — confirmed provisional
F283 / KI#606 — confirmed provisional
F284 / KI#607 — confirmed provisional
F285 / KI#608 — confirmed provisional
F286 / KI#609 — confirmed provisional
F287 / KI#610 — confirmed provisional
F288 / KI#611 — confirmed provisional
F289 / KI#612 — confirmed provisional; repair boundary refined above
F290 / KI#613 — confirmed provisional; low-severity diagnostic/state-cleanup
F291 — UNUSED / provisional KI#614 next

NEXT CURSOR
-----------
1. Continue final queue-042 saturation without trying to fill F291.
2. Finish the assertion/TODO/method-family inventory in CvDeal itself.
3. Challenge the remaining PA cleanup siblings (OB/peace/disengage) only for a
   concrete independent live consequence; otherwise keep them under F290.
4. Recheck second-list / list-orientation asymmetries against F281/F285/F287 so
   siblings are not double-counted.
5. Produce an explicit F281-F290 root-independence / repair-boundary table.
6. If no independent F291 survives, close queue 042 and advance to priority
   queue 047 `CvDllPythonEvents.cpp`.

Do not manufacture F291.
===============================================================================

================================================================================
C025-WIP16 ADDENDUM — queue 042 CvDeal.cpp
F291 / provisional KI#614 confirmed; F292 unused
================================================================================

F291 / provisional KI#614
Title:
  Multiple civic changes in one diplomacy deal are executed as separate forced
  revolutions, overcharging anarchy and shortening the post-anarchy revolution
  cooldown.

Root / why this is one defect:
  A diplomacy bundle may contain one TRADE_CIVIC item for each different civic
  option/column. CvDeal::startTrade nevertheless executes every TRADE_CIVIC
  independently. Each item reconstructs the player's *current* CivicMap, changes
  one option, and calls CvPlayer::revolution(aeNewCivics, true) immediately.
  Thus one logical multi-civic diplomatic demand/exchange is fragmented into N
  separate revolutions instead of one CivicMap revolution.

Current execution path:
  CvDeal::addTradeItems traverses each trade list item-by-item and calls
  startTrade for every non-vassal item.

  CvDeal::startTrade(TRADE_CIVIC):
      CivicMap aeNewCivics;
      kFromPlayer.getCivics(aeNewCivics);
      eCivic = (CivicTypes)trade.m_iData;
      aeNewCivics.set(option(eCivic), eCivic);
      kFromPlayer.revolution(aeNewCivics, true);

  CvPlayer::revolution:
      iAnarchyLength = getCivicAnarchyLength(kNewCivics);
      if (iAnarchyLength > 0)
          changeAnarchyTurns(iAnarchyLength);  // additive
      ...
      setRevolutionTimer(iAnarchyLength + getMinTurnsBetweenRevolutions());
                                              // absolute overwrite

  CvPlayer::changeAnarchyTurns:
      m_iAnarchyTurns += iChange;

Why multiple civic items are a supported live state rather than a malformed list:
  * CvPlayer::buildTradeTable exposes all individually tradeable civic items.
  * CvPlayerAI::AI_checkCivicReligionConsistency explicitly rejects a second
    TRADE_CIVIC only when its CivicOptionType duplicates a civic already present.
    Civics from different columns are therefore deliberately permitted.
  * Both AI_considerOffer and the AI counterproposal path call that consistency
    check.
  * CvPlayer::updateTradeList explicitly says "everything allowed in human-human
    trades"; its AI-side filtering does not impose a one-TRADE_CIVIC total rule.
  * AdvCiv's own AI_anarchyTradeVal comment explicitly discusses "multiple
    TRADE_CIVIC and TRADE_RELIGION items ... on the table." So the codebase itself
    recognizes multi-item ideology trades as a real state.

Locally provable arithmetic:
  CvPlayer::getCivicAnarchyLength sums the iAnarchyLength of all changed civic
  columns and, if at least one changed, adds:
    * BASE_CIVIC_ANARCHY_LENGTH once; and
    * the city-count/world-size anarchy overhead once.
  It then applies modifiers/speed/start-era scaling and caps that one revolution.

  In the deal path, however, each TRADE_CIVIC sees only one changed column,
  because the previous item has already committed its civic before the next item
  reconstructs the CivicMap. The fixed base/city overhead is therefore charged
  once per traded civic instead of once per logical multi-civic revolution.

Current SAS data makes this live without exotic configuration:
  * 25 civic entries; every current <iAnarchyLength> is 1.
  * Five distinct civic options/columns:
      GOVERNMENT, LEGAL, LABOR, ECONOMY, RELIGION.
  * Standard world: iNumCitiesAnarchyPercent = 8.
  * Normal speed: iAnarchyPercent = 100.
  * Ancient start era: iAnarchyPercent = 100.
  * SAS does not override the inherited stock values:
      BASE_CIVIC_ANARCHY_LENGTH = 1
      MAX_ANARCHY_TURNS         = 100
      MIN_REVOLUTION_TURNS      = 5

Concrete current-data examples (ordinary player, no no-anarchy trait/modifier):
  Standard / Normal / Ancient, 5 cities:
    city overhead = floor(5 * 8 / 100) = 0

    One civic:
      1 civic + 1 base = 2 turns.

    Two civic columns as one CivicMap revolution:
      1 + 1 civics + 1 base = 3 turns.

    Current deal execution:
      2 + 2 = 4 turns.
    => 1 extra anarchy turn.

  Standard / Normal / Ancient, 13 cities:
    city overhead = floor(13 * 8 / 100) = 1

    Two civics combined:
      1 + 1 civics + 1 base + 1 city overhead = 4 turns.

    Current deal execution:
      (1 + 1 base + 1 city) + (1 + 1 base + 1 city) = 6 turns.
    => 2 extra anarchy turns.

  The current five-column data can magnify this further. At 13 cities, changing
  all five columns in one normal CivicMap revolution costs:
      5 civic + 1 base + 1 city = 7 turns.
  The deal path instead applies five 3-turn revolutions:
      5 * 3 = 15 turns.
  => 8 extra anarchy turns from the same accepted bundle.

Secondary timer consequence:
  Each forced revolution also executes
      setRevolutionTimer(iAnarchyLength + getMinTurnsBetweenRevolutions())
  rather than adding/preserving the timer for the already accumulated anarchy.
  Therefore the *last* civic item overwrites the timer while the anarchy turns
  from all earlier civic items remain additive.

  Example, 5 cities, two civics:
    accumulated anarchy after the deal = 4
    final revolution timer             = 2 + 5 = 7

  Both counters are decremented each player turn. When the four inflated anarchy
  turns have elapsed, only about three revolution-timer turns remain. A single
  proper combined revolution would be:
    anarchy = 3
    revolution timer = 3 + 5 = 8
  and would still leave the intended five-turn post-anarchy cooldown after the
  three anarchy decrements.

  With enough separately executed civic items, the overwritten timer can reach
  zero while the accumulated anarchy is still running. canRevolution is still
  blocked by isAnarchy, but once anarchy ends the normal post-anarchy cooldown
  can be partly or wholly gone. Thus the defect both lengthens the immediate
  forced anarchy and weakens the subsequent revolution lockout.

Order sensitivity / future-proofing:
  Current SAS gives all civics iAnarchyLength=1, so the final timer is currently
  insensitive to which civic happens to be last. Mods with unequal civic
  anarchy lengths make the final revolution timer depend on trade-list order,
  another sign that item-by-item execution is the wrong state boundary.

Ancestry:
  The per-item TRADE_CIVIC -> revolution(...) behavior is inherited. K-Mod and
  Civ4CE/BtS contain the same basic execution shape, and Base AdvCiv 1.14 still
  does too. This is not a SAS-specific regression.
  AdvCiv later added explicit multi-civic consistency/value code (advc.132) but
  did not aggregate execution. Its AI valuation comment acknowledges multiple
  TRADE_CIVIC items as a real trade-table state.

Why religion is NOT needed to establish F291:
  TRADE_RELIGION also has its own forced conversion/anarchy path, and a bundle
  containing both religion and civics may raise a broader design question about
  whether two distinct ideology systems should share one anarchy overhead.
  F291 does not rely on that judgment. Multiple civic *columns* are represented
  by one CivicMap and ordinary CvPlayer::revolution already defines the native
  combined-operation semantics unambiguously.

Repair boundary:
  Keep normal/manual CvPlayer::revolution semantics unchanged.
  In deal implementation, collect all TRADE_CIVIC items given by each player
  into that player's one CivicMap and execute one forced revolution per player,
  rather than one forced revolution per item.

  Important boundaries:
    * single-civic deals must behave exactly as before;
    * the two players' civic sets must remain independent;
    * retain the existing up-front whole-bundle canTradeItem validation;
    * preserve per-item logging/AI trade-value bookkeeping if desired, but do not
      let those force separate state transitions;
    * set the AI civic timer once after the combined forced civic change;
    * do not "fix" this by changing changeAnarchyTurns or revolution globally;
    * do not automatically merge TRADE_RELIGION into the same state transition
      unless that broader behavior is separately designed and reviewed.

Root independence:
  F291 is independent of the earlier queue-042 roots:
    F281/F283/F285  = peace-treaty list/lifetime/orientation
    F282            = end-trade memory accumulation
    F284            = player-death vs team-agreement lifetime
    F286            = active-deal reentrant self-deletion/UAF
    F287            = mixed annual verification killing protected peace
    F288            = PA + vassal self-vassal survivor
    F289            = brokered-war master-locus relation
    F290            = PA teardown after team identity reassignment
  F291 is instead a transaction aggregation/state-transition granularity error
  in forced civic execution.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281 / provisional KI#604 — confirmed
  F282 / provisional KI#605 — confirmed
  F283 / provisional KI#606 — confirmed
  F284 / provisional KI#607 — confirmed
  F285 / provisional KI#608 — confirmed
  F286 / provisional KI#609 — confirmed
  F287 / provisional KI#610 — confirmed
  F288 / provisional KI#611 — confirmed
  F289 / provisional KI#612 — confirmed
  F290 / provisional KI#613 — confirmed
  F291 / provisional KI#614 — confirmed
  F292 / provisional KI#615 — UNUSED / next

Next saturation cursor:
  Re-run the remaining CvDeal transaction families specifically for:
    * other repeated one-shot item classes whose per-item state mutation has a
      non-linear/fixed-cost contract;
    * trade-list ordering dependence after one item mutates preconditions used
      by a sibling item;
    * endTrade/startTeamTrade identity changes not already absorbed by F286/F290;
    * final history/K-Mod/Civ4CE divergence scan for fixes absent from AdvCiv-SAS.
  If those close as controls, close queue 042 rather than allocating F292 merely
  to fill a number.

No repository source files modified.

================================================================================
C025-WIP17 ADDENDUM — queue 042 CvDeal.cpp
F292 / provisional KI#615 confirmed; F293 unused
================================================================================

F292 / provisional KI#615
Title:
  A mixed city + resource deal can pass prevalidation and then invalidate its
  own resource export when the city transfer removes the giver's last copy.

Root:
  CvDeal::addTradeItems validates every offered item independently against the
  pre-deal world state, then mutates the world item-by-item. A TRADE_CITIES item
  can immediately change plot ownership and rebuild plot groups, including the
  giver's connected bonus counts. A sibling TRADE_RESOURCES item is nevertheless
  accepted based only on the *old* resource count and startTrade does not
  revalidate it after the city transfer.

Clean source-proven live producer:
  Human-human trade intentionally permits annual and nonannual items in one
  bundle. CvPlayer::updateTradeList wraps the annual/nonannual compatibility
  filter in:

      if (!isHuman() || !GET_PLAYER(eOtherPlayer).isHuman())
          ...

  with the explicit comment:
      "everything allowed in human-human trades"

  Therefore a multiplayer/hotseat deal can contain both TRADE_CITIES
  (nonannual) and TRADE_RESOURCES (annual). This finding does not need to assume
  that the ordinary human-AI peacetime UI permits the same mixture.

Concrete trigger:
  Let player A and player B be human players on different teams with a valid
  trade network.

  A has a non-capital city C that passes the normal city-trade rules. C can be,
  for example, a sufficiently eligible/contested city (current SAS requires at
  least CITY_TRADE_CULTURE_THRESH = 10% recipient culture outside liberation
  shortcuts).

  A has exactly one connected copy of bonus X, and transferring C to B removes
  that copy from A. The simplest deterministic geometry is that C itself is
  founded on bonus X; the city tile connects that resource for A before the
  transfer and belongs to B afterward.

  A places both:
      TRADE_CITIES(C)
      TRADE_RESOURCES(X)
  in the same accepted bundle.

Why both pass the existing preflight:
  CvDeal::addTradeItems first loops over all items and calls canTradeItem before
  executing any of them.

  At that time:
    * canTradeItem(TRADE_CITIES) sees C still owned by A and accepts it if the
      normal city-trade conditions pass.
    * canTradeItem(TRADE_RESOURCES) sees X still non-obsolete on both teams and
      A's getNumTradeableBonuses(X) > 0. AdvCiv's current bCanTradeAll=true path
      allows the last available copy to be offered.

  There is no bundle-level "what bonuses remain after the city gifts?" check.

Execution:
  CvDeal::addTradeItems later calls startTrade item-by-item.

  TRADE_CITIES:
      pCity->doTask(TASK_GIFT/TASK_CEDE, eToPlayer)

  CvPlayer::acquireCity(..., bUpdatePlotGroups=true, ...)
      ...
      kGame.updatePlotGroups();

  The old owner's bonus network is therefore recomputed immediately after the
  city changes hands.

  TRADE_RESOURCES:
      kFromPlayer.changeBonusExport(X, +1);
      kToPlayer.changeBonusImport(X, +1);
      bSave = true;

  startTrade does not repeat canTradeItem. Consequently, regardless of which of
  the two sibling items happens to execute first, the final state can contain
  one export backed by zero remaining copies:
    * city first: local copy becomes 0, then export bookkeeping pushes available
      trade state below zero;
    * resource first: export consumes the one copy, then the city transfer and
      plot-group rebuild remove that physical copy underneath the export.

Why this is not merely theoretical bookkeeping:
  CvDeal::verify explicitly treats a resource giver with
      getNumTradeableBonuses(eBonus) < 0
  as invalid and returns false; CvDeal::verify() then kills the deal.

  Thus the engine itself recognizes the final state produced by this accepted
  bundle as an impossible/oversubscribed resource trade.

Irreversible transaction consequence:
  The city transfer is one-shot and is not stored in the persistent deal, so it
  cannot be rolled back when verify later kills the deal.

  Any annual consideration saved in the same human-human deal can disappear
  with the invalid resource item (resource/GPT and other persistent components),
  while ownership of C remains changed. Even a deal whose only persistent item
  is X visibly accepts a resource promise that is invalid essentially from its
  creation and is removed at the next deal verification.

  AI_dealVal also values TRADE_RESOURCES and TRADE_CITIES as separate trade
  items. Where a producer permits such a bundle, the two pieces therefore enter
  valuation independently even though the post-city resource promise can be
  impossible. The guaranteed current producer established here is human-human;
  no single-player human-AI exploit is required for F292.

Relationship to F287:
  F287 is a *teardown granularity* bug: later failure of one annual component
  kills an entire mixed persistent deal, including a still-protected peace
  treaty.

  F292 is a *transaction precondition* bug: the deal is accepted even though
  another item in that same transaction deterministically destroys the resource
  condition needed by the annual item.

  They are independent:
    * repairing F287 to remove only the failed annual component would still leave
      F292's resource consideration invalid immediately after the accepted city
      transfer;
    * rejecting/repairing F292 does not prevent ordinary later resource/GPT
      failures from exercising F287.

Ancestry:
  The relevant shape is inherited:
    * Base AdvCiv 1.14, K-Mod and Civ4CE/BtS all retain the per-item city/resource
      execution model.
    * Their updateTradeList implementations also retain the explicit
      human-human "everything allowed" exception.
  SAS did not introduce the root.

Rejected sibling that helped bound this finding:
  A superficially similar "trade a tech that obsoletes the resource received
  from the other side" case is already blocked. Resource canTradeItem checks
  bonus obsolescence for *both* giver and recipient; the tech giver already owns
  the obsoleting tech before the bundle, so that resource cannot pass the
  initial resource check. F292 therefore stays focused on state mutations whose
  relevant postcondition is not represented by an already-existing predeal
  state, with city ownership/resource supply being the proven case.

Repair boundary:
  The deal must not commit a one-shot city transfer while relying on an annual
  resource promise that the same transaction will make unsustainable.

  Conservative options for implementation review:
    1. Add a bundle-consistency preflight for TRADE_CITIES + TRADE_RESOURCES from
       the same giver, predicting the giver's post-transfer resource
       availability before committing any item.
    2. If exact post-city plot-group simulation is too invasive (city transfer
       can change more than the city tile), prohibit same-giver city + resource
       mixtures unless a safe conservative test proves the resource remains.
    3. A broader transactional redesign could stage/validate state transitions,
       but simply revalidating after the city has already moved is too late
       because city ownership has no rollback here.

  Do not "repair" this only in CvDeal::verify; that merely changes how the
  already-broken accepted transaction is torn down.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F292 / provisional KI#604-KI#615 — confirmed
  F293 / provisional KI#616 — UNUSED / next

Next saturation cursor:
  Continue the repeated TRADE_WAR / TRADE_PEACE / TRADE_EMBARGO mutation-order
  challenge, then inspect remaining one-shot + persistent cross-item pairs and
  final Base/K-Mod/Civ4CE history divergence. Avoid allocating F293 unless an
  independent producer + consequence survives those checks.

No repository source files modified.

================================================================================
C025-WIP18 ADDENDUM — queue 042 CvDeal.cpp
F293 / provisional KI#616 confirmed; F294 unused
================================================================================

F293 / provisional KI#616
Title:
  Brokered peace/war attitude bookkeeping keeps only the last political target,
  making otherwise identical multi-target deals semantically order-dependent.

Root:
  CvDeal::addTradeItems scans both offered lists before implementation and stores
  political context in only two scalar TeamTypes:

      TeamTypes eWarTradeTarget = NO_TEAM;
      TeamTypes ePeaceTradeTarget = NO_TEAM;

  Every TRADE_WAR simply overwrites eWarTradeTarget and every TRADE_PEACE simply
  overwrites ePeaceTradeTarget. The resulting single last-seen target of each
  class is then passed to AI_processTradeValue for BOTH trade directions.

  AI_processTradeValue evaluates the value of the complete item list and forwards
  that same scalar target context to CvPlayerAI::AI_processPeacetimeValue, which
  in turn asks each third-party team for
  CvTeamAI::AI_enemyTradeResentmentFactor(...).

  The latter has target-specific branches:
    * eWarTradeTarget == observer makes the observer count the source as sharing
      its war;
    * ePeaceTradeTarget == observer/master-locus suppresses rival-trade anger;
    * a peace target liked by the observer can also suppress anger.

  Therefore a list containing more than one brokered peace or war target cannot
  be represented correctly by the scalar metadata. Which target happens to be
  encountered last controls the attitude treatment of the entire deal value.

Why multi-target political lists are a supported/reachable state:
  * CvPlayer::buildTradeTable creates a distinct TRADE_PEACE / TRADE_WAR /
    TRADE_EMBARGO inventory item for every eligible third team.
  * CvPlayer::updateTradeList does not impose a one-item-per-political-heading
    restriction.
  * CvPlayerAI::AI_checkCivicReligionConsistency exists specifically to reject
    duplicate/incompatible civic/religion items; there is no analogous
    one-TRADE_PEACE check.
  * CvPlayerAI::AI_dealVal simply sums every TRADE_PEACE item:
        iValue += kOurTeam.AI_makePeaceTradeVal(target, eFromTeam);
    with no count limit.
  * The same function explicitly counts TRADE_WAR items and, under UWAI, adds an
    unaffordable 1,000,000 value when iWars > 1, with the source comment
        "Can't be hired for more than one war at a time"
    This is particularly strong evidence that multiple political items can
    coexist in an offered list; the code had to impose policy in valuation rather
    than relying on the trade representation to make a second item impossible.
    (Legacy/non-UWAI valuation does not even apply that multi-war policy.)
  * Human trade-table proposals are the clean producer for multiple brokered
    peace items; the DLL has no bundle-consistency rejection for that case.

Concrete order-sensitive brokered-peace example:
  Let:
    H = sponsor / paying player
    B = AI player being paid to make peace
    E = independent third team currently at war with B
    D = another third team currently at war with B

  Suppose E regards B as its worst enemy and H is not itself at war with E.
  H constructs one deal in which:
    H gives economic consideration to B; and
    B gives TRADE_PEACE(E) plus TRADE_PEACE(D).

  Both peace items are independently valid and AI_dealVal values both.

  When B receives H's consideration, B's AI_processTradeValue records rival-trade
  consequences for third parties. For observer E:

    CvTeamAI::AI_enemyTradeResentmentFactor(
        eTo = B,
        eFrom = H,
        ePeaceTradeTarget = <last TRADE_PEACE seen>)

  contains:
      if (ePeaceTradeTarget == getMasterTeam())
          return 0;

  For an independent E, getMasterTeam() == E.

  Hence:
    list order [PEACE(D), PEACE(E)]
      -> scalar ePeaceTradeTarget = E
      -> E correctly sees that H paid its enemy B to make peace with E
      -> target-specific rival-trade resentment is suppressed.

    list order [PEACE(E), PEACE(D)]
      -> scalar ePeaceTradeTarget = D
      -> E's own peace settlement is invisible to the context calculation
      -> provided D does not independently trigger the "liked peace target"
         exemption, E can apply positive rival-trade resentment to H for the
         value of the same economic/political bundle.

  The set of players, teams, items and values is identical. Only list order /
  last-target selection changes the diplomatic-memory result.

  Even if a particular UI path happens to insert targets in a stable enum order,
  the semantic defect remains: one valid peace target receives the intended
  exemption while another valid target is discarded solely because the metadata
  has room for one TeamTypes value. There is no normalization that defines the
  last target as semantically privileged.

War-side sibling:
  eWarTradeTarget has the same scalar overwrite architecture. The observer-side
  factor treats
      isAtWar(eFrom) || eWarTradeTarget == observer
  as "the source also shares our war." With several TRADE_WAR items, only the
  last target can receive that target-specific treatment.

  UWAI deliberately makes a second hired war economically impossible through
  AI_dealVal's +1,000,000 guard, so current default/full-UWAI exposure is much
  narrower on the war side. The inherited legacy/non-UWAI path does not have
  that valuation block. This sibling therefore strengthens the root but is not
  needed to establish F293; multiple TRADE_PEACE is sufficient.

Additional asymmetry:
  addTradeItems derives ePeaceTradeTarget/eWarTradeTarget from BOTH lists, then
  passes the same final pair of targets to AI_processTradeValue for each side.
  Thus the metadata is deal-global while the value being processed is
  direction/list-specific. That was an intentional approximation for a single
  brokered political item, but it becomes undefined once several distinct
  targets exist.

Ancestry / history:
  This shape is AdvCiv-specific rather than a SAS regression.

  AdvCiv practical 1361
  "Rival-trade penalties, war trades on Glance tab, misc. changes"
  (advc.130p) introduced the scalar warTradeTarget / peaceTradeTarget variables
  in CvDeal::addTrades while refactoring fair-trade/rival-trade bookkeeping.

  The introduction comment explicitly explains that brokered TRADE_PEACE cannot
  simply be filtered out because other items may be traded alongside it, and the
  target was added so rival-trade penalties could react to whom peace was being
  brokered with. The design handled the one-target case, but the scan used simple
  assignment and never acquired a multi-target representation.

  Current Base AdvCiv 1.14 retains the same scalar target contract. SAS inherits
  it unchanged apart from unrelated game-record/victory-denial instrumentation.
  K-Mod/BtS do not contain this later AdvCiv target-sensitive rival-trade
  machinery in the same form.

Why this is independent of earlier queue-042 findings:
  * F289 is relationship validation for a single brokered WAR target: a voluntary
    vassal sponsor may hire war against its own master/master-locus.
  * F293 assumes every individual TRADE_PEACE/WAR item is valid. Its problem is
    lossy representation of multiple valid political targets during subsequent
    attitude bookkeeping.
  * F282 concerns additive vs absolute cancellation-memory updates.
  * F291/F292 concern bundle execution/precondition aggregation, not third-party
    rival-trade memory context.

Repair boundary:
  Do not merely choose first target instead of last; that preserves the same
  lossy/order-dependent root.

  The robust design needs multi-target context. Two increasingly exact options:

  1. Minimal deterministic representation:
     collect brokered peace/war targets in a set/small vector and teach
     AI_enemyTradeResentmentFactor (or a wrapper) to reason over the whole set.
     This removes arbitrary last-item privilege, though care is still needed
     because an observer may be affected differently by different targets.

  2. Semantically cleaner repair:
     partition the recorded deal value into target-neutral consideration and
     target-specific political-item value, then apply each peace/war target's
     resentment adjustment to the appropriate component. This avoids letting one
     target-specific exemption suppress or amplify the value attributable to
     unrelated political items in the same bundle.

  Whichever repair is chosen:
    * one-TRADE_PEACE / one-TRADE_WAR behavior should remain unchanged;
    * direct peace between the two negotiating teams (bPeace) is a separate
      dimension and must remain separate;
    * reparations/fair-trade suppression rules from advc.130p must be preserved;
    * do not solve this by globally forbidding multiple TRADE_PEACE unless that
      is an explicit gameplay-policy choice, because current valuation supports
      and prices each brokered peace separately;
    * UWAI's explicit one-hired-war policy can remain as policy even if the
      underlying bookkeeping becomes capable of multiple war targets.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F293 / provisional KI#604-KI#616 — confirmed
  F294 / provisional KI#617 — UNUSED / next

Final saturation cursor:
  * finish the remaining trade-type classification/accessor/serialization
    inventory;
  * challenge any remaining cross-item coalition cascade that changes state,
    rather than merely value;
  * complete the targeted historical repair-stream ledger, explicitly marking
    each old CvDeal fix as preserved, absorbed by F281-F293, or a control;
  * if no independent F294 survives, close queue 042 cleanly and move to the
    next reconciled priority queue.

No repository source files modified.

================================================================================
C025-WIP19 ADDENDUM — queue 042 CvDeal.cpp / cross-file accessor consumer
F294 / provisional KI#617 confirmed; F295 unused
================================================================================

F294 / provisional KI#617
Title:
  Embargo valuation scans the hired player's whole team instead of the hired
  player's own deals, overpricing teammate trade that will survive the embargo
  and violating CvDeal's player-accessor contract on multi-member teams.

Root:
  CvPlayerAI::AI_stopTradingTradeVal describes its contract directly:

      "This player pays for the embargo and ePlayer stops trading
       with eTradeTeam."

  The actual embargo machinery has the same player-specific contract.

  CvPlayer::isTradingWithTeam documents:
      "- It checks if this player is trading with any member of eToTeam.
         Likewise, an embargo will affect only the enacting player - but the
         entire target team."

  CvPlayer::stopTradingWithTeam therefore filters:
      if (!pLoopDeal->isBetween(getID(), eTeam))
          continue;

  i.e. only deals involving the specific enacting player are candidates for
  cancellation.

  AI_stopTradingTradeVal instead scans:
      if (!pLoopDeal->isBetween(TEAMID(ePlayer), eTradeTeam))
          continue;

  That is a team-vs-team filter. On a team with more than one player it admits
  persistent deals owned by ePlayer's teammates even though those deals will not
  be canceled when ePlayer executes the embargo.

Concrete live topology:
  Let A1 and A2 be two living players on Team A (e.g. after a Permanent Alliance
  or in a preconfigured multi-player team), and let T be the embargo target team.

  A1 is the player being asked to stop trading with T. A1 has some valid
  outgoing trade to T, so:
      A1.canStopTradingWithTeam(T) == true.

  A2 independently has a valuable persistent resource/GPT deal with a member of
  T. Such player-level deals remain normal after the players share Team A.

  AI_stopTradingTradeVal(T, A1):
    * pLoopDeal(A2,T) passes isBetween(Team A, T);
    * its incoming consideration can therefore be added to A1's embargo price.

  Actual execution:
      A1.stopTradingWithTeam(T)

  only visits deals satisfying isBetween(A1,T), so A2's deal survives.

  Result: the sponsor can be charged for trade value that the hired player does
  not give up and that the embargo does not cancel.

CvDeal accessor-contract failure introduced on top of the semantic root:
  Current AdvCiv/SAS refactored the duplicated orientation branches into:

      pLoopDeal->getReceivesList(TEAMID(ePlayer))
      pLoopDeal->getOtherPlayer(ePlayer)
      pLoopDeal->getGivesList(ePlayer)

  The first call is deliberately team-scoped, but the latter two require the
  exact player to participate:

      CvDeal::getOtherPlayer(PlayerTypes ePlayer):
          FAssert(ePlayer == getFirstPlayer() ||
                  ePlayer == getSecondPlayer());

      CvDeal::getGivesList(PlayerTypes eGivingPlayer):
          same exact-participant assertion.

  Therefore, for an admitted A2<->T deal while ePlayer=A1:
    * Debug/Assert DLLs trip getOtherPlayer(A1);
    * if execution continues, getGivesList(A1) trips too.

Release behavior is not merely "assert removed":
  getOtherPlayer uses:
      return (ePlayer == getFirstPlayer() ?
              getSecondPlayer() : getFirstPlayer());

  When A1 is absent, it always falls through to getFirstPlayer.

  Thus result quality becomes deal-orientation dependent:
    * if A2 is first, getOtherPlayer(A1) returns A2 itself and A1.AI_dealVal is
      asked to value target-supplied items as though teammate A2 supplied them;
    * if T's player is first, the returned "other" happens to be the target
      participant by accident.

  getGivesList(A1) has the analogous absent-player fall-through to the second
  list, so the 5-vs-3 gift/trade multiplier can likewise inspect the wrong side
  depending on orientation.

  The underlying overcount exists even if those fall-through results happened
  to be benign: the teammate deal should not have entered this loop at all.

Direct normal embargo consequence:
  CvPlayerAI::AI_dealVal prices each TRADE_EMBARGO by:
      AI_stopTradingTradeVal(targetTeam, eFromPlayer)

  so this inflated/misoriented result feeds ordinary trade valuation and can
  alter whether/how much an AI sponsor is willing to pay for the embargo.

War-bribe lower-bound sibling:
  CvTeamAI::AI_declareWarTradeVal deliberately computes an embargo lower bound:

      "an embargo ... would only compel one player to stop trading with the
       (whole) target team, so we need to sum up the embargo trade values of
       the members of this team."

  It loops each hireling-team member that canStopTradingWithTeam(eTarget) and
  calls:
      sponsorLeader.AI_stopTradingTradeVal(eTarget, member, true);

  That design confirms the unit of cancellation/value is one player at a time.

  With a multi-member team, however, every one of those per-player calls scans
  the whole team's T-deals. If A1 and A2 each have cancelable player-level trade
  with T, A1's call can count A2's deal and A2's call can count A1's deal; the
  sum can therefore count the same trade losses repeatedly. This can inflate
  the lower bound used for a declaration-of-war bribe in addition to the direct
  embargo-price error.

Ancestry:
  The semantic player-vs-team mismatch is inherited from BtS/K-Mod/Civ4CE.

  Their AI_stopTradingTradeVal loops classify a deal as relevant whenever a
  participant's TEAM equals ePlayer's team. That already counts teammate-owned
  deals on multi-member teams, despite stopTradingWithTeam being player-scoped.

  AdvCiv practical 1352
  "Bugs fixed in AI war trade behavior; tech cost adjustments"
  (advc.130f/advc.001q) improved the inherited scan by adding the missing
  opposite-side target-team condition. Before that, BtS effectively checked only
  whether one participant belonged to ePlayer's team; the AdvCiv source comment
  records this explicitly:
      "BtS had only checked for ePlayer's team"

  That repair correctly stopped unrelated third-team deals from entering the
  price, but retained TEAMID(ePlayer) rather than the exact ePlayer identity.

  AdvCiv practical 1618
  "FOR_EACH macros everywhere; changes to AI class design."
  later rewrote the two orientation branches with the new CvDeal accessors.
  The refactor preserved the team-level filter but then called exact-player
  getOtherPlayer/getGivesList, turning the older semantic mismatch into a
  deterministic assertion-contract violation as well.

  Current Base AdvCiv 1.14 retains this shape. SAS inherits it; SAS's nearby
  local change merely caches GET_PLAYER(ePlayer) as kPlayer.

Why multi-member teams are not exotic malformed state:
  Permanent Alliance is a shipped trade item and deliberately combines players
  onto one team; scenario/team-game setups can also begin with multiple players
  sharing a team. Player-level resource/GPT deals remain represented by their
  individual player IDs. The embargo APIs themselves are explicitly documented
  around this distinction, so the code cannot assume one player per team here.

Independence from earlier findings:
  * F285 is an embargo-DENIAL/list-orientation bug involving peace reparations.
    F294 can reproduce with ordinary resource/GPT deals and no peace treaty.
  * F290 concerns old team identities while tearing down agreements during a
    Permanent Alliance merge. F294 applies after a multi-member team already
    exists and concerns current player-owned trade valuation.
  * F284 concerns a player death tearing down team-owned agreements. F294 is the
    inverse scope error: team-wide scanning of player-owned persistent deals.
  * F293 concerns lossy multi-target political attitude context, not deal owner
    identity.

Repair boundary:
  For the persistent ordinary-deal loop in AI_stopTradingTradeVal, use the same
  player scope as the operation being valued:

      if (!pLoopDeal->isBetween(ePlayer, eTradeTeam))
          continue;

  Once that invariant is established, use player-scoped list accessors
  consistently:
      getReceivesList(ePlayer)
      getOtherPlayer(ePlayer)
      getGivesList(ePlayer)

  This simultaneously:
    * excludes teammate deals that survive ePlayer's embargo;
    * satisfies getOtherPlayer/getGivesList's exact-participant contracts;
    * removes orientation-dependent Release fall-through.

  Important boundary:
  Keep the separate Open Borders / Defensive Pact terms under review as
  team-level relationship costs. Those relationships are genuinely stored and
  torn down at team scope through their proxy/agreement machinery. Do not
  mechanically convert every TEAMID(ePlayer) use in the function to player
  scope; F294 is specifically the ordinary persistent-deal inventory whose
  actual cancellation path is player-specific.

  The war-bribe member-sum should then naturally aggregate disjoint per-player
  trade losses instead of recounting the full team inventory for every member.
  Its team-level OB/DP contribution may still deserve independent heuristic
  review, but that is not required to repair the proven F294 root.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F294 / provisional KI#604-KI#617 — confirmed
  F295 / provisional KI#618 — UNUSED / next

Remaining saturation cursor:
  * challenge the team-level OB/DP contribution in the multi-member war-bribe
    member sum without assuming it is a bug merely because it is repeated;
  * finish the historical repair-stream ledger and remaining classification /
    serialization / cancellation controls;
  * if no independent F295 survives, close queue 042 cleanly rather than
    manufacturing another root.

No repository source files modified.

================================================================================
C025-WIP20 ADDENDUM — queue 042 CvDeal.cpp / war-trade embargo lower bound
F295 / provisional KI#618 confirmed; F296 unused
================================================================================

F295 / provisional KI#618
Title:
  Multi-member war-bribe embargo lower bound counts shared team-level
  Open-Borders / Defensive-Pact loss once per player even though the first
  embargo tears the shared agreement down for the whole team.

Root:
  CvTeamAI::AI_declareWarTradeVal deliberately constructs an embargo-equivalent
  lower bound by summing AI_stopTradingTradeVal for every member of the hireling
  team that can stop trading with the target:

      int iEmbargoTradeVal = 0;
      for (MemberIter itMember(getID()); itMember.hasNext(); ++itMember)
      {
          if (itMember->canStopTradingWithTeam(eTarget))
          {
              iEmbargoTradeVal += sponsorLeader.AI_stopTradingTradeVal(
                      eTarget, itMember->getID(), true);
          }
      }
      iTradeVal = max(iTradeVal, 0.83 * iEmbargoTradeVal);

  This decomposition is valid for independent player-owned resource/GPT deals:
  an embargo by A1 cancels A1's deals, and a separate embargo by A2 cancels A2's
  deals.

  It is not additive for Open Borders or Defensive Pact. Those are team-level
  relationships represented by replicated player-pair proxy deals. Killing one
  qualifying proxy with normal bKillTeam=true tears down the relationship for
  the entire pair of teams and deletes all sibling proxies.

Source-proven shared state:
  CvDeal::startTeamTrade(TRADE_OPEN_BORDERS / TRADE_DEFENSIVE_PACT, ...)
  creates a proxy deal for every member-pair.

  CvDeal::endTrade for either item:
      GET_TEAM(eFromPlayer).setOpenBorders/DefensivePact(..., false);
      if (bTeam)
          endTeamTrade(...);

  CvDeal::endTeamTrade then finds all sibling proxies between those teams and
  kills them with kill(false).

  CvPlayer::stopTradingWithTeam kills a qualifying player deal with ordinary:
      pLoopDeal->kill();
  i.e. bKillTeam defaults true.

  Therefore the first member embargo that kills an OB/DP proxy removes the
  shared team relation and every other member's corresponding proxy. A second
  member does not independently lose another copy of that relationship.

How AI_stopTradingTradeVal nevertheless prices it on every member call:
  For each ePlayer member it derives:
      CvTeam const& kTeam = GET_TEAM(ePlayer);

  and applies the same team-level Open-Borders state:
      if (kTeam.isOpenBorders(eTradeTeam))
      {
          iModifier += 50;
          ...
      }

  followed by the same team-level Defensive-Pact state:
      if (GET_TEAM(ePlayer).isDefensivePact(eTradeTeam))
          iValue *= 3;

  All players on the same team therefore carry identical OB/DP relationship
  state into every pre-cancellation member valuation.

Concrete two-member Open-Borders case:
  Let A1 and A2 share Team A, and T be the proposed war target.

  Team A has Open Borders with T and no other ordinary trade is required.

  startTeamTrade has created OB proxies involving both A1 and A2, so both can
  satisfy canStopTradingWithTeam(T).

  AI_declareWarTradeVal computes:
      stopTradeVal(T, A1, true)
    + stopTradeVal(T, A2, true)

  Both calls independently include the Open-Borders modifier.

  But actual embargo execution is sequential state mutation:
      A1.stopTradingWithTeam(T)
        -> kills A1's OB proxy with bKillTeam=true
        -> setOpenBorders(T,false)
        -> endTeamTrade removes all remaining A<->T OB proxies.

  At that point A2 has no second Open-Borders relation left to sacrifice.
  The lower-bound sum has counted one shared loss twice.

  With N hireling-team members the same shared relationship contribution can be
  repeated N times.

Defensive-Pact magnification:
  Defensive Pact is also one team-level relation with replicated proxies, but
  AI_stopTradingTradeVal applies:
      iValue *= 3
  for every member call while the pact is present.

  Thus the same shared DP loss can magnify every component of the N-member sum,
  even though the first actual cancellation removes the pact for the whole team.
  This makes the non-additivity substantially larger than the OB-only case.

Why this is independent of F294:
  F294 is the ordinary persistent-deal inventory scope error inside
  AI_stopTradingTradeVal:
      isBetween(TEAMID(ePlayer), eTradeTeam)
  admits teammate-owned resource/GPT deals and can violate exact-player
  CvDeal accessor contracts.

  Repairing F294 to:
      isBetween(ePlayer, eTradeTeam)
  correctly makes player-owned annual losses disjoint across member calls.

  F295 survives unchanged after that repair because the Open-Borders modifier
  and Defensive-Pact multiplier are explicit team-state terms outside the
  ordinary-deal loop. Every member call still sees the same shared team
  relationship before any hypothetical cancellation has been applied.

History / introduced intent:
  AdvCiv practical 2930, commit
      a16555656d83d7bc92067d6c1156ce0303d1f69d
  "Fix minor issue with war trade val computation"
  changed the prior leader-only embargo lower bound into the current
  member-summed form.

  Commit message:
      "The lower bound based on the embargo trade value hadn't been calculated
       correctly at all. [advc.104o]"

  The introduced source comment is explicit about the model:
      "Don't charge much less than for an embargo. An embargo (weirdly)
       would only compel one player to stop trading with the (whole) target team,
       so we need to sum up the embargo trade values of the members of this team."

  That rationale correctly identifies the player scope of ordinary embargo
  cancellation. It does not account for the fact that OB/DP are team-level
  exceptions whose first member cancellation removes the relationship globally.

  No later substantive repair to that member-sum pattern was found in current
  Base AdvCiv 1.14; SAS inherits it. K-Mod/Civ4CE do not contain this later
  AdvCiv member-summed lower-bound design in the same form.

Cross-check against normal relationship valuation:
  CvTeamAI::AI_openBordersTradeVal(eTeam) returns:
      getNumCities() + target.getNumCities()

  CvTeamAI::AI_defensivePactTradeVal(eTeam) returns the same team quantity * 3.

  Both are naturally team-scoped and priced once for the relationship, which is
  consistent with the actual state lifetime and inconsistent with treating OB/DP
  as N independently cancelable player assets.

Why this is not merely a heuristic disagreement:
  AI_stopTradingTradeVal is heuristic, but F295 does not assert that its absolute
  OB/DP price is objectively correct or incorrect. The provable defect is the
  decomposition boundary used by AI_declareWarTradeVal:
    * the comment says member values are summed to model the losses of multiple
      player-scoped embargoes;
    * ordinary player deals are disjoint under that decomposition;
    * shared team agreements are not disjoint;
    * the code nevertheless evaluates their same pre-state contribution once in
      every member summand.

  Therefore the result changes with the number of players sharing the same team
  even when the team-level relationship being sacrificed is unchanged.

Gameplay consequence:
  The inflated sum is multiplied by 0.83 and used as a lower bound for
  AI_declareWarTradeVal. On multi-member hireling teams, especially with a
  Defensive Pact to the target, this can make a war bribe more expensive or
  unattainable because a shared relationship cost is counted repeatedly.

Repair boundary:
  Do not divide the whole embargo sum by team size. Player-owned resource/GPT
  losses really are additive and should still be summed per member.

  The robust decomposition is:
    1. Sum each member's player-owned cancelable ordinary-deal loss, using the
       exact-player scope required by F294.
    2. Evaluate shared team-level relationship loss (Open Borders / Defensive
       Pact and any analogous team agreement) once for the hireling team.
    3. Combine those once before applying the 0.83 embargo lower bound.

  Implementation could expose a flag/helper that suppresses shared OB/DP terms
  in per-player AI_stopTradingTradeVal calls, then add one team-level component;
  or separate AI_stopTradingTradeVal into player-deal and team-relationship
  components explicitly.

  Preserve:
    * normal one-player-team behavior;
    * ordinary direct TRADE_EMBARGO valuation, where one player's embargo really
      can terminate the shared relation and should still charge for doing so;
    * the member sum for genuinely separate player-owned trades;
    * UWAI/legacy base war valuation before the embargo lower bound.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F295 / provisional KI#604-KI#618 — confirmed
  F296 / provisional KI#619 — UNUSED / next

Remaining saturation cursor:
  * finish the old-CvDeal repair-stream ledger and verify that each substantive
    historical fix is either preserved, absorbed by F281-F295, or explicitly
    rejected as a control;
  * rerun the complete method/classification/accessor/serialization inventory
    against the final finding set so no family was skipped;
  * re-challenge the last cross-item state transitions only where one item can
    invalidate a sibling after whole-bundle prevalidation;
  * if no independent F296 survives, close queue 042 rather than inventing one.

No repository source files modified.

================================================================================
C025-WIP21 ADDENDUM — queue 042 CvDeal.cpp / annual-deal attitude-cache timing
F296 / provisional KI#619 confirmed; F297 unused
================================================================================

F296 / provisional KI#619
Title:
  Annual-deal rival-trade attitude refresh is deferred for only one deal
  direction; the other direction updates third-party attitude caches mid-loop,
  making later resentment depend on CvDeal orientation / deal iteration order.

Root:
  AdvCiv practical 3309 added a bUpdateAttitude switch specifically to defer
  annual-deal attitude-cache refreshes until CvGame::doDeals has processed all
  deals. The current CvDeal::doTurn applies that switch only to the SECOND trade
  direction:

      if (getLengthSecond() > 0)
      {
          ...
          GET_PLAYER(getFirstPlayer()).AI_processPeacetimeTradeValue(
                  getSecondPlayer(), iValue);
          // or AI_processPeacetimeGrantValue(..., iValue)
          // ^ default bUpdateAttitude == true
      }

      if (getLengthFirst() > 0)
      {
          ...
          GET_PLAYER(getSecondPlayer()).AI_processPeacetimeTradeValue(
                  getFirstPlayer(), iValue, false);
          // grant path also passes false explicitly
      }

  The comment immediately below says the intended opposite:
      "we do not update attitudes of particular players here,
       but instead update all of them at the game turn boundary
       [advc: i.e. in CvGame::doDeals]."

  CvGame::doDeals indeed does:
      verifyDeals();
      FOR_EACH_DEAL_VAR(pLoopDeal)
          pLoopDeal->doTurn();
      CvPlayerAI::AI_updateAttitudes();

  Therefore first-direction annual value processing is the lone premature cache
  refresh inside the deal loop.

Why default true actually updates third-party caches:
  AI_processPeacetimeTradeValue / GrantValue forwards bUpdateAttitude into
  AI_processPeacetimeValue.

  For each third-party team that resents the trade:
      kThirdParty.AI_changeEnemyPeacetimeTradeValue(
          TEAMID(eFromPlayer), ..., bUpdateAttitude);

  CvTeamAI::AI_setEnemyPeacetimeTradeValue then does:
      if (bUpdateAttitude)
          AI_updateAttitude(eIndex, false);

  CvTeamAI::AI_updateAttitude iterates the member players and recomputes their
  cached m_aiAttitude toward the affected team.

  The false parameter there suppresses worst-enemy recomputation only; it does
  NOT suppress the attitude-cache refresh itself.

Why a later annual deal can observe that premature refresh:
  CvPlayerAI::AI_processPeacetimeValue calculates each new rival-trade increment
  through:
      kThirdParty.AI_enemyTradeResentmentFactor(
          eTo, eFrom, ...);

  AI_enemyTradeResentmentFactor reads cached attitude values:
      AI_getAttitude(...)
      AI_getAttitudeVal(eFrom)
      AI_getAttitudeVal(AI_getWorstEnemy())

  and builds:
      iAttitudeDiff =
          AI_getAttitudeVal(eFrom)
        - ...
        - AI_getAttitudeVal(AI_getWorstEnemy())
        + ...
      r = iAttitudeDiff / 5
  (clamped to the documented bounds).

  Meanwhile AI_updateAttitude includes AI_getRivalTradeAttitude, and
  AI_getRivalTradeAttitude is directly derived from the just-changed
  EnemyPeacetimeTrade/Grant values. A sufficiently large increment, or any
  increment crossing an existing rounding threshold, can therefore change the
  cached integer attitude that the next resentment-factor calculation reads.

Clean supported two-deal reproducer:
  Let:
    E = an AI third party
    W = E's current worst-enemy team
    X = another civ/team

  Suppose there are two mature cancelable annual deals whose recurring value
  includes X giving something valuable to W. Multiple persistent deals between
  the same player pair are supported by CvGame's deal container; AdvCiv's
  AI_foldDeals only folds the narrow AI-AI opposite GPT-for-single-resource case,
  and human-involved/resource arrangements remain ordinary separate deals.

  Deal D1 is stored:
      firstPlayer  = W
      secondPlayer = X
  with X's annual item in secondList.

  D1::doTurn therefore executes the FIRST processing branch:
      W.AI_processPeacetime*(X, value)
  with default bUpdateAttitude=true.

  Because W is E's worst enemy, E can record RivalTrade resentment against X for
  trading with W. That increment immediately recomputes E's cached attitude
  toward X.

  Deal D2, processed later in FOR_EACH_DEAL order, again evaluates X trading
  with W. Its call to:
      E.AI_enemyTradeResentmentFactor(W, X, ...)
  now sees the post-D1 cached E->X attitude rather than the turn-boundary cache.
  Since the factor explicitly uses attitude(X) relative to attitude(worstEnemy),
  D1 can alter D2's resentment multiplier.

Orientation proof:
  Store the economically same X->W annual item with participants reversed:
      firstPlayer  = X
      secondPlayer = W
  and X's item in firstList.

  W receives exactly the same recurring value, but it is now processed by the
  SECOND CvDeal::doTurn branch:
      W.AI_processPeacetime*(X, value, false)

  No E->X cache refresh occurs until the global CvGame::doDeals update.
  Therefore a subsequent deal in the same deal loop can receive a different
  rival-trade multiplier solely from which endpoint happened to occupy
  CvDeal::firstPlayer.

  This is not theoretical undefined list ordering: CvGame::implementAndReturnDeal
  preserves the caller's eWho/eOtherWho identities directly as first/second
  players, so storage orientation is part of deal construction rather than a
  canonical ordering by player/team ID.

Magnitude / threshold note:
  AI attitude is integer-cached, while rival-trade value uses a nonlinear
  diminishing-return calculation and rounding. Not every small annual increment
  changes the cached integer attitude. F296 does not require every deal to show
  the effect; an existing RivalTrade value near an attitude rounding boundary,
  or a sufficiently valuable recurring increment, makes the feedback live.
  Once the cached value changes, AI_enemyTradeResentmentFactor uses it
  immediately before the intended global refresh point.

History:
  This is an AdvCiv practical-3309 regression/partial fix, commit:
      a8192ca7faab182fa7dfa589c097c83a23f07509
      "Fix timing of attitude cache update during turn sequence"

  Commit message:
      "Needs to happen after each player turn; after the team turn isn't enough.
       [advc.001]

       There were also some unnecessary updates, in part due to change
       [advc.130p] (I think)."

  The commit:
    * added bUpdateAttitude to AI_processPeacetimeTradeValue,
      AI_processPeacetimeGrantValue and downstream enemy-value setters;
    * changed CvGame::doDeals from updating only direct trade participants to a
      global all-major-civ attitude refresh after all CvDeal::doTurn calls;
    * explicitly passed false in CvDeal::doTurn's SECOND-direction trade/grant
      calls;
    * did NOT change the symmetric FIRST-direction calls, which therefore kept
      the new default=true;
    * updated the local comment to explicitly identify CvGame::doDeals as the
      deferred refresh point.

  Base AdvCiv 1.14 still has this exact one-sided false/default-true shape.
  SAS inherits it unchanged.
  K-Mod predates the bUpdateAttitude mechanism; its doTurn comment already said
  attitudes should be updated at the game-turn boundary, but its enemy-value
  setters updated caches immediately. Practical 3309 was AdvCiv's attempt to
  repair/refine that timing and left one half behind.
  Civ4CE follows the older BtS/K-Mod-style path and does not contain this
  partial AdvCiv switch in the same form.

Why this is independent of F293:
  F293 is lossy scalar representation of multiple brokered political targets
  during initial trade-value processing.
  F296 concerns recurring annual CvDeal::doTurn processing with no brokered
  peace/war target at all; its defect is one-sided cache-update timing.

Why this is independent of F282:
  F282 is cancellation-memory accumulation (setter vs additive changer).
  F296 modifies neither cancellation nor memory. It changes third-party cached
  attitude timing while annual trade/grant values continue to accumulate.

Repair boundary:
  Make the two annual directions symmetric and honor practical 3309's own stated
  game-turn-boundary contract:

      GET_PLAYER(getFirstPlayer()).AI_processPeacetimeTradeValue(
          getSecondPlayer(), iValue, false);

      GET_PLAYER(getFirstPlayer()).AI_processPeacetimeGrantValue(
          getSecondPlayer(), iValue, false);

  The already-correct second-direction calls remain false.

  Do NOT globally change the default bUpdateAttitude to false. Other callers
  intentionally process immediate events outside the CvGame::doDeals batch,
  including unit gifts and newly implemented diplomacy, and rely on the normal
  default update semantics.

  Keep CvGame::doDeals' final CvPlayerAI::AI_updateAttitudes() call; it is the
  synchronization point practical 3309 established precisely so all recurring
  deal-value changes are visible together after the batch.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F296 / provisional KI#604-KI#619 — confirmed
  F297 / provisional KI#620 — UNUSED / next

Remaining saturation cursor:
  * finish the substantive CvDeal history ledger after practical 3309 and prove
    no later repair stream leaves another adjacent invariant;
  * complete the transaction-order matrix (tech / civic / religion /
    war / peace / embargo / city / PA / vassal) against F286/F288-F292;
  * re-run small method, serialization and cancellation-family inventory against
    the final finding map;
  * if no independent F297 survives, close queue 042 cleanly rather than
    manufacturing one.

No repository source files modified.

================================================================================
C025-WIP22 ADDENDUM — queue 042 CvDeal.cpp
Final saturation controls; F294 same-root extension; F297 still unused
================================================================================

State entering this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
  F281-F296 / provisional KI#604-KI#619 — confirmed
  F297 / provisional KI#620 — UNUSED / next
  24 / 169 first-pass files complete until this queue closes.

F294 same-root extension — embargo denial also uses the wrong team scope
-----------------------------------------------------------------------

The final accessor-consumer sweep found a second live consumer of the same
player-vs-team embargo-scope invariant already established by F294.

CvPlayer::stopTradingWithTeam is explicitly player-scoped:
    if (!pLoopDeal->isBetween(getID(), eTeam))
        continue;

Only deals belonging to the particular player who enacts the embargo are
cancelled. This is also stated by the isTradingWithTeam source comment:
    "an embargo will affect only the enacting player - but the entire target
     team."

F294 already established that CvPlayerAI::AI_stopTradingTradeVal violates this
contract by selecting ordinary deals with:
    pLoopDeal->isBetween(TEAMID(ePlayer), eTradeTeam)

The denial path CvPlayerAI::AI_stopTradingTrade contains the same scope error
when trying to protect peace reparations:

    if (GET_TEAM(getTeam()).isForcePeace(eTradeTeam))
    {
        FOR_EACH_DEAL(d)
        {
            if (!d->isBetween(getTeam(), eTradeTeam))
                continue;
            ...
            FOR_EACH_TRADE_ITEM(d->getGivesList(getTeam()))
            {
                if (type == TRADE_PEACE_TREATY)
                    bPeaceTreaty = true;
                if (type == TRADE_RESOURCES || type == TRADE_GOLD_PER_TURN)
                    bAnnualPayment = true;
            }
            if (bPeaceTreaty && bAnnualPayment)
                return DENIAL_RECENT_CANCEL;
        }
    }

On a multi-member Team A, player A1 can therefore refuse an embargo against T
because teammate A2 has a reparations deal with T, even though
A1.stopTradingWithTeam(T) would not cancel A2's player-owned annual payment.

This does not warrant a new finding number:
  * the underlying invariant is identical to F294 — valuation/denial asks what
    one player loses by stopping trade, but searches the whole team;
  * exact-player scoping is the same repair boundary;
  * the previously established F285 list-orientation problem in reparations
    recognition is separate and still needs its own treaty-anywhere correction.

F294 repair review should therefore cover BOTH:
  1. AI_stopTradingTradeVal ordinary-deal inventory:
         isBetween(ePlayer, eTradeTeam)
     plus player-scoped getGivesList/getReceivesList/getOtherPlayer;
  2. AI_stopTradingTrade reparations-preservation scan:
         isBetween(getID(), eTradeTeam)
     and then recognize the peace marker anywhere in that exact player's deal
     while checking annual reparations actually given by getID() (F285 boundary).

Do not replace these with TEAMID/getTeam merely because the embargo target is a
team. Target scope is team-wide; enacting-owner scope is player-specific.

Final accessor / serialization / lifecycle controls
----------------------------------------------------

The small-method surface has now been rerun against the full queue-042 finding
map rather than merely assumed safe.

* CvDeal::write / read:
  ID, initial turn, first player, second player, first list and second list are
  serialized/deserialized in matching order. No missing current member.

* reset / uninit:
  both trade lists are cleared and all serialized scalar members reset. There is
  no extra owned heap state in CvDeal.

* getOtherPlayer(PlayerTypes), getGivesList(PlayerTypes),
  getReceivesList(PlayerTypes):
  exact-player assertions are intentional and useful. The F294 violation is in
  a caller that admits teammate deals before invoking these accessors.

* getGivesList(TeamTypes), getReceivesList(TeamTypes):
  deliberately require exactly one endpoint to belong to the queried team.
  Current live team-typed consumers were enumerated:
    - CvPlayerAI embargo valuation / denial: F294 family;
    - CvTeam::freeVassal: team distinction is valid before the vassal proxy is
      killed;
    - CvDeal::isUncancelableVassalDeal: explicitly returns before the accessor
      when both endpoints have become the same team.
  No additional independent same-team consumer survived beyond F290/F294.

* isDisengage:
  its first-list-only item check is safe for the real representation.
  Disengagement is created through startTeamTrade(..., bDual=true), so each
  proxy contains one TRADE_DISENGAGE item in both lists. The item also lies
  beyond NUM_TRADEABLE_ITEMS and is not an ordinary voluntary trade-table
  choice.

* isVassalTributeDeal(CLinkList*):
  returns true for an empty list vacuously, but meaningful callers additionally
  require the opposite/value conditions that make an empty persistent deal
  irrelevant. CvGame::implementAndReturnDeal immediately kills a newly-created
  CvDeal whose two saved lists are both empty.

* CvDeal::doTurn empty-list head access:
  for a persistent deal both lists cannot be empty under the normal creation /
  deletion lifecycle. The apparently unsafe getFirstList().head() expression is
  therefore protected by representation plus short-circuiting for one-sided
  deals. No live null-head root established.

* isAllDual / isAnnual / isDual / hasData / isEndWar:
  every current TradeableItems family has been reconciled. PA's "annual"
  classification does not create a recurring PA deal because the PA item itself
  is not saved; the classification is used by generic trade/UI/value machinery.
  No independent state-lifetime defect established there.

Representation-debt siblings absorbed by existing findings
-----------------------------------------------------------

Several final first-list-only observations are real asymmetries but not new
roots:

1. CvDeal::kill determines "forced" cancellation sound/visibility by looking for
   dual items only in getFirstList(). Normally this is sufficient because OB,
   DP and disengagement proxies are dual. A legitimate one-sided
   TRADE_PEACE_TREATY in the second list is the exception and can therefore miss
   the dual-item bForce path. This is a diagnostic/UI sibling of F281's
   one-sided peace-treaty representation, not a new gameplay root.

2. CvTeam::setVassal still contains first-list peace-treaty cleanup inherited
   from the same representation assumption. This belongs in the F281/F283
   repair review; fixing only CvDeal::isPeaceDeal or resetDualDeal would leave
   another consumer of the outdated "peace marker lives in first list" model.

3. Other first-list-only checks for OB/DP/disengagement remain safe because
   startTeamTrade creates those proxy items symmetrically or in a fixed
   orientation known to the consumer.

Rejected transaction candidate — Open Borders + Disengagement
--------------------------------------------------------------

A synthetic bundle containing TRADE_OPEN_BORDERS and TRADE_DISENGAGE would be
order-sensitive: setOpenBorders(true) cancels disengagement, then a later
TRADE_DISENGAGE start could restore the disengage flag without rechecking OB.

This is not a normal accepted trade state:
  * TRADE_DISENGAGE is declared after NUM_TRADEABLE_ITEMS;
  * ordinary trade-table iteration does not offer it;
  * its live producer is CvTeam::makePeace -> signDisengage ->
    startTeamTrade(TRADE_DISENGAGE, ..., true);
  * capitulation suppresses the disengagement producer;
  * while the negotiating teams are still at war, OB/DP cannot independently
    prevalidate.

No F297.

Rejected transaction candidate — brokered peace with master + vassal
--------------------------------------------------------------------

Raw TRADE_PEACE inventory construction iterates third teams and can enumerate a
vassal at the game-rule level, so a master+vassal pair initially looked capable
of being redundantly priced: CvTeam::makePeace(master) cascades peace to the
master's vassals.

The AI denial contract closes the producer:
    CvTeamAI::AI_makePeaceTrade:
        if (GET_TEAM(ePeaceTeam).isAVassal())
            return DENIAL_VASSAL;

TRADE_PEACE itself is only offered by nonhuman givers
(CvPlayer::canPossiblyTradeItem returns false when the giving team is human).
AI_considerOffer applies getTradeDenial to every item the AI gives.

Therefore an accepted ordinary deal cannot contain the redundant
TRADE_PEACE(master) + TRADE_PEACE(vassal) pair. No F297.

Tech / resource / civic / religion transaction controls retained
-----------------------------------------------------------------

* Tech + resource self-obsolescence:
  resource prevalidation checks bonus obsolescence for BOTH giver and recipient;
  if one side already owns the obsoleting tech needed for the cross-item
  scenario, the resource item is rejected before implementation.

* Tech that newly enables another item:
  whole-bundle prevalidation occurs before the tech is transferred, so the
  newly-enabled civic/tech/etc. item fails conservatively rather than being
  accepted and then invalidated.

* Same-column multiple civics and duplicate religions:
  AI_checkCivicReligionConsistency explicitly rejects these accepted-offer
  states.

* Different civic columns:
  deliberately supported and therefore remain the exact F291 case.

* Multiple GOLD / GPT instances:
  normal trade-table representation exposes one editable item of each kind
  rather than an arbitrary duplicated-item producer. No independent normal
  duplicate-obligation root established.

Historical repair-stream ledger — final high-value status
----------------------------------------------------------

The substantive CvDeal history is now mostly classified:

* practical 1285 / advc.034 — temporary disengagement after peace:
  current producer/dual proxy lifetime checked; no new adjacent root.

* practical 1287 / advc.039 — reparations-aware peace:
  current announcement/player-local city identity includes SAS KI#412;
  no new queue-042 root beyond existing peace/list findings.

* practical 1290 / advc.036 / advc.133 — cancellation/update refinements:
  current attitude/cancellation descendants checked; F282/F287/F296 capture
  the surviving independent failures found in those families.

* practical 1361 / advc.130p — rival-trade refactor:
  F293 is the surviving lossy scalar political-target root.

* practical 1376 / advc.106j — forced cancellation notifications:
  resource scan covers both lists; dual scan relies on symmetric representation,
  with one-sided peace the F281 sibling noted above.

* practical 1385 / advc.001v — forced religion conversion:
  current TRADE_RELIGION calls convert(..., true); repair preserved.

* practical 1497 — embargo / trade-memory refactor:
  F282 captures additive-memory regression; F294/F285 cover current embargo
  owner/list scope defects.

* practical 1519 — city peace acceptance:
  current city/reparations handling preserves the intended direction and SAS
  KI#412 player-local city identity correction.

* practical 1577/1578 — deal cancellation:
  current cancellation paths were re-audited through verify/kill/endTeamTrade;
  surviving independent roots are already F284/F286/F287 rather than an
  unnumbered adjacent bug.

* practical 1690 — peace treaty cancellation:
  current isCancelable(NO_PLAYER) repair retained; F281 is a separate
  one-sided treaty-recognition defect.

* practical 1882 / 1885 — PA / player death:
  current descendants are exactly the families challenged by F284/F288/F290;
  no additional independent lifetime root yet found.

* practical 1997 — default m_iData team-trade fix:
  current <=0 startTeamTrade condition preserved for relevant team items.

* practical 2070 / 2658 / 2691 / 3275 — implied/prolongable/displayed treaties:
  together establish the logical-treaty renewal representation; F283 and the
  F281 sibling controls capture the surviving mismatch.

* practical 2350 — UWAI reporting only when enabled:
  current addTradeItems uses bReportUWAI = getUWAI().isEnabled() before
  reportSponsoredWar; preserved.

* practical 2485 — declaration-war cancellation sound:
  current first cancellation keeps sound, subsequent kills suppress it;
  preserved.

* practical 2765 — broke GPT forced cancellation:
  current verify predicate retained; its whole-deal teardown interaction is
  already challenged under F287.

* practical 2930 / advc.104o — member-summed embargo lower bound:
  F295 is the surviving shared-OB/DP non-additivity root.

* practical 3118 — halved cancellation memory:
  current setter-based memory mechanism remains the F282 root.

* practical 3309 — deferred attitude-cache refresh:
  F296 is the surviving one-direction-only application.

* Current Base AdvCiv 1.14 normalized comparison found no later upstream CvDeal
  repair omitted by SAS. Material SAS deltas are current instrumentation,
  victory-denial guard, KI#412 reparations identity and related logging /
  acquisition annotations rather than a missing 1.14 bugfix stream.

Remaining single candidate before closure
-----------------------------------------

The only transaction candidate still under active challenge is multiple
TRADE_WAR items under the supported legacy/K-Mod-war-AI configuration where
UWAI is disabled.

Full UWAI explicitly makes a second hired war unaffordable:
    "Can't be hired for more than one war at a time"
    iWars > 1 -> +1,000,000 value.

When UWAI is disabled, legacy valuation can accept multiple war items. If two
targets have a Defensive Pact, the first primary hired declaration can cause the
second target to enter the war through the DP cascade before its own prevalidated
TRADE_WAR executes. The second CvTeam::declareWar then returns because the teams
are already at war, while surrounding sponsorship-memory code still runs.

This is suspicious but not yet promoted:
  * the sponsor explicitly requested both war states;
  * both requested final war relationships do exist;
  * it remains to prove that the second item is semantically supposed to buy a
    distinct primary declaration rather than merely guarantee the war state;
  * full UWAI intentionally prohibits the multi-war bundle, narrowing exposure
    to the supported legacy/K-Mod-war-AI mode.

F297 remains UNUSED pending that last contract check.

Next cursor:
  1. Finish the legacy multi-war/Defensive-Pact contract challenge against
     denial, valuation, war-plan and sponsorship-memory semantics.
  2. Run final independence/repair-boundary review across F281-F296.
  3. If no independent F297 survives, close queue 042 as the next durable
     checkpoint, update first-pass progress to 25/169, and open the next
     reconciled priority queue (CvDllPythonEvents.cpp / KI#620 next).

No repository source files modified.

================================================================================
C025-WIP23 ADDENDUM — queue 042 CvDeal.cpp / legacy multi-war bundle
F297 / provisional KI#620 confirmed; F298 unused
================================================================================

F297 / provisional KI#620
Title:
  With UWAI disabled, a multi-TRADE_WAR bundle can charge separately for a
  Defensive-Pact ally whose entry into the war is already priced and triggered
  by the first hired declaration.

Supported configuration / producer:
  AdvCiv deliberately supports two war/peace AI modes.

  Current XML documents:
    * normal/default UWAI;
    * K-Mod war/peace AI when "Aggressive AI (K-Mod)" is selected;
    * USE_KMOD_AI_NONAGGRESSIVE / DISABLE_UWAI as additional supported switches.

  TRADE_WAR inventory construction creates one item for each eligible third
  team when the giver is AI. A human sponsor can therefore place more than one
  war item from the same AI hireling onto the diplomacy table.

  AI_considerOffer checks getTradeDenial independently for every item against
  the unchanged pre-deal world state.

  In the legacy/K-Mod branch, AI_declareWarTrade refuses a hired war when:
      getNumWars() > 0
  but if the hireling is initially at peace, two different TRADE_WAR items in
  the same proposed list each see getNumWars()==0 and can each pass.

  Full UWAI has an additional bundle-level policy in AI_dealVal:
      // "Can't be hired for more than one war at a time"
      iWars++;
      if (iWars > 1)
          iValue += 1000000;
  Crucially, that guard is inside:
      if (getUWAI().isEnabled())
  so the supported legacy/K-Mod-war-AI mode has no equivalent multi-item guard.

Concrete Defensive-Pact trigger:
  Let:
    H = human sponsor;
    B = AI hireling, currently at peace with everyone relevant and using the
        legacy/K-Mod war AI (UWAI disabled);
    A and D = two independent teams with an active Defensive Pact.

  Assume B is individually willing and able to declare war on both A and D and
  H can afford the quoted bundle.

  H asks B for:
      TRADE_WAR(A)
      TRADE_WAR(D)

  Both items can pass getTradeDenial before the deal because B still has zero
  current wars and canDeclareWar each target.

Why the price is provably overlapping rather than merely "two requested wars":
  CvTeamAI::AI_declareWarTradeValLegacy(A,H) explicitly prices the consequences
  of A's Defensive Pacts.

  When A currently has no wars, the function:
      iValue *= 2;
      for each eligible kDPTeam:
          if master(DPTeam).isDefensivePact(master(A)):
              iValue += DPTeam.numCities * 30;
              iValue += DPTeam.totalPopulation * 20;

  Thus the price of the FIRST item, TRADE_WAR(A), already charges B for D being
  pulled into the conflict through A's Defensive Pact.

  AI_dealVal then encounters TRADE_WAR(D) and adds a second full:
      AI_declareWarTradeValLegacy(D,H)

  D's own valuation can analogously include A as its Defensive-Pact ally.
  The same coalition-war consequence is therefore priced again.

Execution confirms that the second primary declaration is redundant:
  CvDeal::startTrade(TRADE_WAR(A)):
      B.declareWar(A, ..., sponsor=H)

  CvTeam::declareWar(A) triggers A's Defensive Pacts. D queues and executes a
  declaration of war against B through WAR_DECLARATION_DEFENSIVE_PACT.

  By the time CvDeal reaches TRADE_WAR(D), B and D are already at war.

  CvDeal still calls:
      B.declareWar(D, ..., sponsor=H)

  but CvTeam::declareWar begins with:
      if (isAtWar(eTarget))
          return;

  Hence the second purchased "declare war on D" does not perform a second
  primary sponsored declaration at all. Its war state already exists because
  the first purchased declaration caused the DP cascade.

Surrounding bookkeeping makes the mismatch visible:
  Although CvTeam::declareWar(D) returns immediately, CvDeal::startTrade
  continues after that call:
    * bPeaceTreatyImplied is set;
    * every member of D receives MEMORY_HIRED_WAR_ALLY against H;
    * DECLARED_WAR_RECENT helper memories may also be updated.

  The actual B<->D war, however, was created by D's Defensive-Pact declaration,
  with the DP cascade's war cause and no primary sponsored B->D declaration.
  Thus transaction bookkeeping can describe H as having hired a declaration
  that the declaration function itself never executed.

Why the final symmetric "at war" relation does not make the price correct:
  If one TRADE_WAR(A) already:
    1. creates B<->A war,
    2. causes the A-D Defensive Pact to create B<->D war, and
    3. explicitly charges extra in AI_declareWarTradeValLegacy(A,H) for D's
       expected DP participation,

  then adding TRADE_WAR(D) cannot legitimately charge the same DP consequence a
  second time merely because the final relation is symmetric.

  The legacy price function itself is the source contract that resolves the
  ambiguity: DP allies are included in the price of the primary target.

Ancestry:
  This is inherited from BtS/K-Mod legacy diplomacy and exposed by AdvCiv's
  dual-AI-mode architecture.

  Civ4CE/BtS:
    * AI_dealVal sums every TRADE_WAR separately;
    * AI_declareWarTradeVal adds cities/population of Defensive-Pact allies;
    * AI_declareWarTrade tests current/predeal war-plan state item-by-item.
    No bundle-level multi-war normalization.

  K-Mod:
    * retains per-item TRADE_WAR valuation;
    * its legacy war-trade value explicitly includes Defensive-Pact allies;
    * its denial refuses when already at war, but independent predeal item
      checks do not mutate state.

  AdvCiv practical/WPAI/UWAI work added the explicit policy:
      "Can't be hired for more than one war at a time"
  as early as the initial AdvCiv/UWAI integration (advc.104o), but deliberately
  gates the million-point second-war rejection on UWAI being enabled.

  Current Base AdvCiv 1.14 and SAS retain that conditional guard unchanged.
  Thus default UWAI avoids the accepted bundle, while the supported legacy mode
  still exposes the inherited transaction/valuation mismatch.

Relationship to F293:
  F293 concerns multiple brokered political targets losing all but the last
  target in rival-trade ATTITUDE metadata.

  F297 does not depend on that metadata. Even if F293 is repaired with a full
  target set, legacy AI_dealVal still charges overlapping war consequences and
  execution still finds the second target already at war.

Relationship to F289:
  F289 is a game-rule/relationship validation hole for a single brokered war
  involving the sponsor's master locus.

  F297 uses two individually valid independent targets and a Defensive Pact.
  The defect is cross-item consequence overlap after whole-bundle prevalidation.

Relationship to F292:
  Both are transaction-level "individually valid before implementation" bugs,
  but the invalidated invariant differs:
    * F292: a city transfer removes physical resource supply required by a
      persistent sibling resource item;
    * F297: the first hired war creates the second requested war as a DP cascade
      while its valuation already includes that cascade, making the second
      priced primary declaration redundant.

Repair boundary:
  The narrowest repair is to apply AdvCiv's already-stated policy
      "Can't be hired for more than one war at a time"
  regardless of whether UWAI is enabled.

  Concretely, the AI_dealVal iWars guard can be made unconditional (or an
  equivalent bundle-consistency denial can reject the second TRADE_WAR) while
  leaving the choice of legacy vs UWAI single-war pricing intact.

  This preserves:
    * all current one-war legacy/K-Mod trade valuation;
    * all current full-UWAI behavior;
    * the existing explicit design policy rather than inventing a new rule.

  If multiple hired wars are intentionally desired in the future, a larger
  repair is required:
    * compute the closure/union of vassal and Defensive-Pact war consequences
      for the whole bundle;
    * price each resulting war consequence once;
    * distinguish primary sponsored declarations from alliance/cascade wars;
    * execute/report sponsorship consistently with that staged result.

  Merely revalidating the second item after executing the first is insufficient
  for AI fairness because the sponsor has already been accepted at a price that
  included both independently computed items.

Severity:
  Moderate, configuration-gated.
  Default UWAI avoids accepted multi-war bundles through the million-point
  guard. The bug remains live in the explicitly supported legacy/K-Mod war AI
  configuration and can overprice a human war bribe while recording sponsorship
  semantics inconsistent with the declaration path.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F297 / provisional KI#604-KI#620 — confirmed
  F298 / provisional KI#621 — UNUSED / next

Final closure cursor:
  * run the independence/root-merging review across F281-F297;
  * ensure each proposed repair does not accidentally fix or invalidate another
    finding in a way that means two numbers are actually one root;
  * finish the last no-finding historical/refactor entries and team-proxy
    lifecycle controls;
  * if no independent F298 survives, close queue 042 and advance first-pass
    progress to 25/169 rather than searching indefinitely.

No repository source files modified.

================================================================================
C025-WIP24 ADDENDUM — queue 042 CvDeal.cpp / incompatible team-status bundle
F298 / provisional KI#621 confirmed; F299 unused
================================================================================

F298 / provisional KI#621
Title:
  A voluntary-vassal + Defensive-Pact bundle can be accepted and valued while
  both teams are free, but the mandatory vassal-last execution immediately
  destroys the just-signed Defensive Pact and records cancellation effects.

Root:
  Bundle legality is checked item-by-item against the unchanged pre-deal state.
  TRADE_VASSAL and TRADE_DEFENSIVE_PACT are individually legal between two free,
  peaceful teams, but they are mutually incompatible final states.

  CvDeal then deliberately executes vassal/surrender items in a second pass.
  Therefore a bundled Defensive Pact is signed first and the voluntary-vassal
  transition is applied afterward. CvTeam::setVassal explicitly cancels every
  Defensive-Pact deal involving the new vassal.

  The accepted transaction thus contains an item whose own later sibling is
  guaranteed to undo it before addTradeItems finishes.

Reachable trade-table state:
  CvPlayer::buildTradeTable exposes TRADE_VASSAL and TRADE_DEFENSIVE_PACT as
  ordinary basic trade items when their individual canTradeItem checks pass.

  Pre-deal:
    * TRADE_VASSAL is possible at peace when neither side is already a vassal
      and the prospective master has vassal-state trading.
    * TRADE_DEFENSIVE_PACT is possible when both teams are free (not vassals),
      at peace, satisfy the DP trading/war-state rules and can sign a pact.

  CvPlayer::updateTradeList contains no vassal-vs-DP incompatibility rule.

  Its vassal special cases hide:
    * another vassal/surrender item from the opposite side; and
    * an end-war item while the negotiating teams are at war.

  They do not hide TRADE_DEFENSIVE_PACT.

  The generic peacetime mixing filter also does not help:
    CvDeal::isAnnual(TRADE_VASSAL)          == true
    CvDeal::isAnnual(TRADE_DEFENSIVE_PACT) == true

  so both remain in the same annual class.

  Human-human diplomacy is the strongest unconditional producer because
  updateTradeList explicitly says:
      "everything allowed in human-human trades"

  The state is not limited to human-human games. An AI can also individually
  return NO_DENIAL for a voluntary-vassal relation and for a Defensive Pact
  (e.g. sufficiently favorable relationship with Open Borders and compatible
  war state), so human-AI negotiation has no backend bundle-consistency rule
  that would make the pair intrinsically impossible.

Whole-bundle prevalidation:
  CvDeal::addTradeItems first checks every item with canTradeItem before mutating
  anything.

  At that moment both teams are still free, so:
    canTradeItem(TRADE_VASSAL)          can pass;
    canTradeItem(TRADE_DEFENSIVE_PACT) can pass.

  No second validation is performed after another item changes team status.

Deterministic current execution:
  AdvCiv replaced K-Mod's list-reordering approach with explicit two-pass
  execution:

      for (int iPass = 0; iPass < 2; iPass++)
      {
          ...
          if (isVassal(pItem->m_eItemType) == (iPass == 0))
              continue;
          startTrade(...);
      }

  Consequently all non-vassal items run in pass 0 and vassal/surrender items in
  pass 1 regardless of list insertion order.

  For a same-direction bundle:
      TRADE_DEFENSIVE_PACT
      TRADE_VASSAL

  pass 0:
    CvDeal::startTrade(TRADE_DEFENSIVE_PACT)
      -> startTeamTrade(..., bDual=true)
      -> creates persistent DP proxy deal(s)
      -> setDefensivePact(otherTeam, true)

  pass 1:
    CvDeal::startTrade(TRADE_VASSAL)
      -> startTeamTrade(TRADE_VASSAL,...)
      -> setVassal(master, true, false)

  CvTeam::setVassal, once the new vassal state is current, iterates live deals
  involving the vassal and explicitly kills any first-list item equal to:
      TRADE_DEFENSIVE_PACT
      TRADE_PEACE_TREATY

  DP proxies are dual, so the first-list test finds the just-created pact.

  kill -> CvDeal::endTrade(TRADE_DEFENSIVE_PACT):
      setDefensivePact(master,false);
      endTeamTrade(...) removes sibling proxies.

  Final state of the accepted bundle:
      vassal relation = present
      Defensive Pact  = absent

  The pact that was just agreed to does not survive the implementation of the
  very deal that contained it.

Immediate cancellation side effects:
  This is not merely a display inconsistency.

  DP teardown occurs while both players remain alive. endTrade therefore calls
  addEndTradeMemory for the Defensive Pact. With no explicit canceling player,
  the cancellation-memory path can record
      MEMORY_CANCELLED_DEFENSIVE_PACT
  between the newly-created master/vassal participants.

  Thus a single accepted transaction can:
    1. sign a Defensive Pact;
    2. immediately cancel it because vassalage was also accepted; and
    3. leave diplomatic cancellation memory for that synthetic cancellation.

  Depending on UI/notification circumstances, the separate DP proxy teardown can
  also produce normal cancellation handling even though no player subsequently
  chose to break the pact.

AI valuation consequence:
  AI_considerOffer / AI_dealVal prices the proposed item list before execution
  with bIgnoreAnnual=false.

  AI_dealVal has independent cases:
      TRADE_VASSAL:
          iValue += AI_vassalTradeVal(...)
      TRADE_DEFENSIVE_PACT:
          iValue += AI_defensivePactTradeVal(...)

  There is no bundle-level "DP will disappear after vassalization" adjustment.

  Therefore, in an AI-involved proposal that otherwise clears the individual
  denials, the Defensive Pact can affect whether the bundle is considered fair
  even though implementation guarantees that it will not exist afterward.
  A human can be asked to compensate for a benefit the accepted transaction
  immediately removes, or the AI can count a DP benefit on the receiving side
  that likewise never survives.

  F298 does not rely on finding one exact leader/power configuration where both
  AI denials happen to clear; the human-human producer alone establishes the
  accepted-invalid transaction. The AI valuation path establishes the additional
  consequence when such an AI combination is individually acceptable.

Ancestry:
  The underlying incompatibility is inherited from BtS/K-Mod diplomacy:
    * the trade table permits vassal and DP items independently;
    * vassal establishment cancels Defensive Pacts;
    * there is no whole-bundle vassal-vs-DP exclusion.

  BtS/Civ4CE executes ordinary items in list order, so the final malformed result
  can depend on whether vassal or DP happens to execute first:
    * DP then vassal -> pact is canceled;
    * vassal then DP -> startTrade can sign a pact after the vassal transition
      because the item is not revalidated.

  K-Mod deliberately changed deal implementation so vassal items are moved to
  the end ("Vassal deals need to be implemented last, so that master/vassal
  power is set correctly"). Its second-list movement code had a copy/paste
  issue; AdvCiv replaced the approach with the current two-pass execution.

  In current Base AdvCiv 1.14 and SAS the result is therefore deterministic:
  non-vassal DP first, vassal last, immediate DP cancellation.

Why this is independent of F288:
  F288 is the distinct Permanent-Alliance + vassal combination:
    * PA execution is deferred until after the vassal pass;
    * merging the teams can turn the surviving team into its own vassal;
    * final persistent team state is corrupted (`m_eMaster == getID()`).

  F298 requires no Permanent Alliance and creates no self-vassal.
  It instead accepts a DP whose final state is guaranteed to be absent and
  triggers cancellation side effects.

  Rejecting only PA+vassal (the narrow F288 repair) leaves F298 untouched.
  Rejecting only vassal+DP leaves F288 untouched.

Why this is independent of F282:
  F282 concerns whether cancellation-memory changes are additive vs absolute.
  Even if F282 is repaired perfectly, F298 still performs an artificial DP
  cancellation and writes whatever correctly-implemented cancellation memory
  policy endTrade specifies.

Why this is independent of F292:
  Both involve whole-bundle prevalidation followed by cross-item mutation, but
  the violated contracts are different:
    * F292: city ownership removes physical resource supply required by an
      annual resource obligation;
    * F298: vassal team status is structurally incompatible with a DP that the
      same bundle has just established.

Repair boundary:
  The safest narrow rule is to reject mutually incompatible team-status items in
  the same negotiated bundle before any item is committed.

  For current F298:
    * TRADE_VASSAL (peacetime voluntary vassal) and
      TRADE_DEFENSIVE_PACT between the negotiating teams must not coexist in one
      accepted bundle.

  Apply the rule in backend bundle validation, not only in updateTradeList, so
  script/EXE/direct callers cannot bypass it.

  The UI should also hide the counterpart for clarity:
    * once a vassal item is offered, hide DP;
    * once DP is offered, hide vassal.

  Preserve:
    * ordinary single voluntary-vassal deals;
    * ordinary single Defensive Pacts;
    * the deliberate vassal-last execution required for power/war alignment;
    * capitulation handling (TRADE_SURRENDER occurs during war, where DP cannot
      independently prevalidate anyway).

  Do not merely suppress DP cancellation inside CvTeam::setVassal. A master and
  its vassal are not supposed to retain an ordinary Defensive Pact; the error is
  accepting the incompatible bundle, not the cleanup that enforces the final
  vassal invariant.

Queue state after this checkpoint:
  queue 042 CvGameCoreDLL/CvDeal.cpp — still OPEN
  F281-F298 / provisional KI#604-KI#621 — confirmed
  F299 / provisional KI#622 — UNUSED / next

Final saturation cursor:
  * rerun the remaining vassal-status pairings (OB, treaty, disengage, PA) only
    for independent final-state contradictions:
      - PA already F288;
      - peace/list lifetime already F281/F283/F287 family;
      - disengage is not voluntarily tradeable;
      - OB is not canceled by setVassal and needs separate proof before any
        claim;
  * finish F281-F298 root-independence / repair-boundary review;
  * complete the remaining historical/refactor entries that changed CvDeal only
    mechanically or diagnostically;
  * if no independent F299 survives, close queue 042 and advance to queue 047
    rather than continuing to mine a saturated file.

No repository source files modified.

================================================================================
C025-WIP25 ADDENDUM — queue 042 CvDeal.cpp
F298 strengthened: dual-DP orientation can leave a one-way pact bit with no backing deal
F299 remains unused
================================================================================

F298 SAME-ROOT REFINEMENT
-------------------------
WIP24 proved the basic accepted-invalid transaction:

    voluntary vassal + Defensive Pact

Both items can pass whole-bundle prevalidation while the teams are still free.
The DP is incompatible with the final vassal state, and CvTeam::setVassal removes
Defensive Pact deals when the vassal relation is established.

The final sequencing is more dangerous than WIP24's simplified "all non-vassal
items, then all vassal items" summary, because current AdvCiv/SAS does NOT stage
the two outer offer lists globally.

Actual CvDeal::addTradeItems order:
    firstList pass 0  — all non-vassal items
    firstList pass 1  — all vassal/surrender items
    secondList pass 0 — all non-vassal items
    secondList pass 1 — all vassal/surrender items

This matters for dual team agreements.

NORMAL DEFENSIVE-PACT REPRESENTATION REALLY IS DUAL
----------------------------------------------------
TRADE_DEFENSIVE_PACT is classified by CvDeal::isDual.

More importantly, DLL-owned normal producers physically put the item into BOTH
outer offer lists.

CvTeam::signDefensivePact:

    TradeData item(TRADE_DEFENSIVE_PACT);
    ...
    ourList.insertAtEnd(item);
    theirList.insertAtEnd(item);
    GC.getGame().implementDeal(..., ourList, theirList, ...);

CvPlayerAI::AI_doDiplo does the same for an AI Defensive-Pact proposal:

    TradeData item(TRADE_DEFENSIVE_PACT);
    ...
    weGive.insertAtEnd(item);
    theyGive.insertAtEnd(item);

and then either opens the diplomacy screen with those two lists or directly
implements them.

CvDeal::doTurn also explicitly documents that in multiplayer, "dual deals can be
mixed with e.g. gold per turn"; current trade-table filtering says
"everything allowed in human-human trades."

Thus two-sided outer-list representation is not merely a theoretical meaning of
the enum flag. It is the normal contract used by the DLL's own DP constructors.
The exact EXE mechanics for every human-edited mixed proposal remain opaque, so
the stronger branch below is not needed to establish F298; it is an additional
normal-representation consequence when the accepted bundle contains the usual
dual DP pair.

DANGEROUS ORIENTATION
---------------------
Let:
    A = first player/team, offering voluntary vassalage to B
    B = second player/team

Accepted outer bundle:
    firstList:
        TRADE_DEFENSIVE_PACT
        TRADE_VASSAL
    secondList:
        TRADE_DEFENSIVE_PACT

All three items pass the initial canTradeItem checks while A and B are still
separate free teams.

Current execution:

1. firstList pass 0 — first TRADE_DEFENSIVE_PACT
   startTrade calls:
       startTeamTrade(DP, A, B, true);
       A.setDefensivePact(B, true);

   startTeamTrade creates the ordinary persistent dual DP proxy CvDeal(s) with
   m_iData=1 saved on both sides.

2. firstList pass 1 — TRADE_VASSAL
   A.setVassal(B, true, false).

   CvTeam::setVassal scans live deals involving the new vassal and kills
   TRADE_DEFENSIVE_PACT proxies.

   CvDeal::killSilent processes BOTH saved proxy lists:
       A.setDefensivePact(B, false);
       B.setDefensivePact(A, false);

   So the pact is fully cleared at this point.

3. secondList pass 0 — second TRADE_DEFENSIVE_PACT
   This outer item was prevalidated BEFORE vassalization and is not revalidated
   here.

   startTrade sees no current DP flag, so it attempts:
       startTeamTrade(DP, B, A, true);

NESTED PROXY CREATION IS CORRECTLY REJECTED — BUT THE FLAG SET IS NOT
----------------------------------------------------------------------
startTeamTrade creates its proxy through a fresh:

    CvGame::implementDeal(...)

with bForce=false.

That nested CvDeal::addTradeItems therefore re-runs canTradeItem NOW, after A has
become B's vassal.

CvPlayer::canPossiblyTradeItem(TRADE_DEFENSIVE_PACT) requires:
    !ourTeam.isAVassal()
    !toTeam.isAVassal()

so the nested persistent DP proxy is rejected. implementAndReturnDeal sees both
saved lists empty, kills the empty deal and returns NULL.

However CvDeal::startTrade ignores whether startTeamTrade actually succeeded and
immediately executes:

    GET_TEAM(eFromPlayer).setDefensivePact(kToPlayer.getTeam(), true);

There is no return status from startTeamTrade and no post-state legality check.

CvTeam::setDefensivePact itself only asserts that the two team IDs differ; it
does NOT prohibit master/vassal status.

Therefore the final state can be:

    A.isVassal(B)             == true
    A.isDefensivePact(B)      == false
    B.isDefensivePact(A)      == true
    backing DP CvDeal/proxy   == NONE

This is stronger than "the pact is immediately canceled":
- the symmetric team relation becomes one-way;
- the surviving true bit has no CvDeal lifetime owner;
- normal deal cancellation / turnsToCancel / scoreboard-deal lookup cannot own
  or expire it;
- it exists despite the exact post-vassal canTradeItem check having rejected the
  corresponding persistent agreement.

The mirror orientation can instead end with the DP removed. Thus one logical
mixed vassal+dual-DP transaction can have orientation-sensitive final state.

WHY THIS REMAINS F298, NOT F299
-------------------------------
The root is unchanged:
    the bundle accepts a DP and a vassal transition whose final states are
    incompatible, then mutates them sequentially without a bundle-level
    consistency contract.

The one-way no-proxy DP is a stronger consequence of that same incompatibility
plus current per-list vassal-last sequencing. A backend rule that forbids
TRADE_VASSAL + TRADE_DEFENSIVE_PACT in one accepted bundle prevents BOTH the
WIP24 immediate-cancel branch and this WIP25 asymmetric-bit branch.

Do not allocate a separate finding merely because the same invalid bundle has a
second execution orientation.

REPAIR-BOUNDARY REFINEMENT
--------------------------
F298 should be repaired before startTrade mutates either relation.

Required backend invariant:
    an accepted transaction between two teams must not contain voluntary
    TRADE_VASSAL and TRADE_DEFENSIVE_PACT as coexisting requested final states.

UI hiding is useful but not sufficient.

Do NOT repair only by:
- changing the list pass order;
- making vassal items globally last;
- suppressing CvTeam::setVassal's DP cancellation;
- making startTeamTrade forced;
- or teaching setDefensivePact to silently accept the incompatible relation.

A defensive secondary improvement would also be reasonable:
    startTeamTrade could return success/failure and startTrade should not mutate
    the team-level flag when proxy creation was rejected.

That would prevent this particular orphan one-way bit under other future
mid-transaction invalidations, but it would NOT make vassal+DP a coherent
bundle. The primary repair remains bundle incompatibility rejection.

ANCESTRY
--------
Current Base AdvCiv 1.14 has the same:
- first-list two-pass loop followed by second-list two-pass loop;
- DP startTeamTrade followed by unconditional setDefensivePact(true);
- vassal cleanup of DP proxy deals.

So this stronger F298 consequence is inherited by SAS from current AdvCiv.

K-Mod's older "move vassal to end" implementation is different and even contains
the second-list copy/paste defect that AdvCiv comments on; Civ4CE/BtS use older
item-order behavior. The exact one-way/no-proxy sequence above is therefore most
directly a current AdvCiv/SAS execution-shape consequence, while the underlying
vassal+DP incompatibility is older.

OTHER VASSAL-STATUS PAIRING CONTROL — OPEN BORDERS
---------------------------------------------------
Vassal + Open Borders initially looks analogous but is NOT interchangeable with
F298.

CvTeam::setVassal does not cancel Open Borders, and Open Borders can still have
a distinct economic role after vassalization:
    CvTeam::isFreeTrade = isOpenBorders || global free-trade option.

AdvCiv explicitly removed/commented older unconditional vassal shortcuts from
trade-route eligibility; peaceful territorial entry and international trade
routes are not the same contract.

Therefore a master/vassal Open-Borders agreement is not proven to be an
impossible final state merely because movement access is partly implied by
vassalage. No F299 from the OB pairing.

Queue state:
  queue 042 CvGameCoreDLL/CvDeal.cpp — OPEN
  F281-F298 / provisional KI#604-KI#621 — confirmed
  F299 / provisional KI#622 — UNUSED / next

Next:
  * complete F281-F298 independence/root-merging and repair-interaction review;
  * finish the remaining no-finding vassal/treaty/disengage and team-proxy
    controls;
  * inspect remaining current CvDeal assertions/TODOs/history entries one final
    time against the completed map;
  * if no independent F299 survives, close queue 042 at 25/169 and move to the
    next reconciled priority queue.

No repository source files modified.

===============================================================================
C026 DURABLE — QUEUE 042 CvDeal.cpp CLOSED; QUEUE 047 NEXT
===============================================================================

QUEUE 042 — CvGameCoreDLL/CvDeal.cpp
STATUS: FIRST-PASS COMPLETE

CONFIRMED ROOTS
---------------
F281 / provisional KI#604
  isPeaceDeal scans only the first saved list. A legitimate one-sided peace
  treaty in the second list is not recognized as a peace deal, breaking expiry
  and related logical-treaty behavior.

F282 / provisional KI#605
  addEndTradeMemory uses absolute AI_setMemoryCount where the surrounding
  cancellation design requires additive accumulation; repeated/weighted
  cancellation memory is collapsed or overwritten.

F283 / provisional KI#606
  resetDualDeal renewal is player-pair / first-list scoped instead of logical
  team-agreement scoped, so one proxy can be prolonged while sibling proxies
  retain different ages; one-sided peace representation is also missed.

F284 / provisional KI#607
  death of one player on a still-alive multi-member team can destroy a
  player-pair proxy and thereby tear down the whole surviving team's OB/DP/
  disengagement agreement.

F285 / provisional KI#608
  reparations-aware embargo denial checks the wrong list/orientation for the
  peace marker and can approve an embargo that would cancel protected
  reparations.

F286 / provisional KI#609
  addTradeItems can be synchronously re-entered through team/state transitions
  that delete the currently executing CvDeal, after which the caller continues
  through a freed object. Confirmed trigger families include mixed PA/state
  mutations; relation bugs such as F289 can expand the trigger surface.

F287 / provisional KI#610
  failure of one annual component in verify kills the entire mixed persistent
  deal, including a still-mandatory peace treaty. Resource/GPT failure can
  therefore terminate protected peace early.

F288 / provisional KI#611
  a supported Permanent-Alliance + voluntary-vassal bundle can merge team
  identity after vassal establishment and leave the surviving team as its own
  vassal.

F289 / provisional KI#612
  brokered-war validity is not checked against the sponsor's canonical master
  coalition. A voluntary vassal can hire war against its own master, or against
  a coalition whose Defensive Pact necessarily pulls its master/sponsor into
  war with the hireling.

F290 / provisional KI#613
  Permanent-Alliance cleanup cancels old team-proxy agreements only after
  participant team reassignment; generic endTrade therefore reconstructs the
  wrong old team identity. A valid pre-PA Defensive Pact reaches
  setDefensivePact(self,false), tripping the AdvCiv/SAS self-team assertion and
  polluting same-team teardown state.

F291 / provisional KI#614
  multiple TRADE_CIVIC items from different civic columns are executed as
  separate forced revolutions instead of one CivicMap revolution. Fixed/city
  anarchy overhead is charged repeatedly and the revolution timer is overwritten
  per item.

F292 / provisional KI#615
  a city + resource bundle can pass whole-bundle prevalidation while the city
  still supplies the giver's last copy, then the city transfer removes that
  supply before/under the resource obligation. The annual promise becomes
  invalid while the one-shot city transfer cannot be rolled back.

F293 / provisional KI#616
  brokered peace/war rival-trade bookkeeping stores only one scalar
  ePeaceTradeTarget/eWarTradeTarget. Multi-target political bundles therefore
  discard all but the last target and make third-party resentment depend on
  item order.

F294 / provisional KI#617
  embargo valuation and reparations-preservation denial use team-wide ordinary
  deal scans even though an embargo cancels only the enacting player's deals.
  Multi-member teams can therefore price/refuse based on teammate-owned trades;
  the valuation path also violates exact-player CvDeal accessor contracts.

F295 / provisional KI#618
  the war-bribe embargo lower bound sums one embargo value per team member, but
  every member summand also contains the same shared team-level Open Borders /
  Defensive Pact loss. The first real embargo tears that shared relation down,
  so the relationship cost is counted repeatedly.

F296 / provisional KI#619
  practical 3309's deferred annual-deal attitude refresh is applied only to one
  CvDeal direction. The opposite direction keeps default
  bUpdateAttitude=true, letting later deals in the same doDeals batch observe
  prematurely refreshed third-party attitude caches.

F297 / provisional KI#620
  with UWAI disabled (supported legacy/K-Mod war AI), several TRADE_WAR items
  can be accepted together. A Defensive-Pact ally is already priced and pulled
  into war by the first purchased declaration, yet a second TRADE_WAR on that
  ally is independently priced and sponsorship-bookkept even though the actual
  declaration returns immediately because the war already exists.

F298 / provisional KI#621
  voluntary vassalage and Defensive Pact are individually valid predeal but
  mutually incompatible final team states. Current per-list vassal-last
  execution can first create a DP, vassalize and remove it, then process the
  opposite dual DP item after vassalization. Nested proxy creation correctly
  fails the new vassal legality check, but outer startTrade unconditionally sets
  the team DP bit anyway, allowing a one-way master/vassal DP flag with no
  backing CvDeal.

F299 / provisional KI#622 remains UNUSED.

ROOT-INDEPENDENCE / REPAIR-BOUNDARY REVIEW
------------------------------------------

The final review intentionally grouped findings by architecture and then applied
a stricter test: two findings merge only when the narrow repair for one
necessarily removes the other's proven trigger/consequence. A broad redesign
that happens to fix several roots is not enough to collapse them.

Peace/list representation:
  F281 vs F283:
    scanning both lists in isPeaceDeal fixes F281 but does not make renewal
    team-scope or reset all proxies; F283 survives.

  F281 vs F285:
    fixing generic peace recognition does not repair AI_stopTradingTrade's
    exact-owner/list interpretation of reparations; F285 survives.

  F283 vs F285:
    team-wide renewal age repair does not change embargo denial orientation;
    neither narrow repair subsumes the other.

Team-proxy / lifetime:
  F283 vs F284:
    renewal-age synchronization does not stop player death from destroying a
    surviving team's agreement. A death-lifetime repair does not fix renewal.

  F284 vs F290:
    preventing one dead player's proxy from tearing down a living team relation
    does not preserve old team identities during PA merge teardown. Conversely,
    a PA-specific old-ID teardown does not fix player-death ownership.

Permanent Alliance:
  F286 vs F288:
    reentrancy/self-deletion hardening leaves the PA+vassal self-vassal final
    state live; rejecting PA+vassal does not cure other synchronous self-delete
    paths.

  F288 vs F290:
    PA+vassal incompatibility is unrelated to old team-proxy identity when an
    ordinary pre-existing DP is cleaned during a normal PA.

  F288 vs F298:
    both are bundle-state incompatibilities, but one is PA+vassal/self-master
    and one is vassal+DP/orphan DP state. Rejecting either pair leaves the other.

Transaction consistency:
  F291:
    needs aggregation of several civic columns into one revolution; unrelated to
    city supply, coalition-war closure or team-status compatibility.

  F292:
    needs post-city resource consistency/staging. Ordinary later resource
    failure still exercises F287 even after F292 is rejected.

  F297:
    needs one-hired-war policy or union/closure pricing for a multi-war bundle;
    resource/civic/team-status fixes do not touch it.

  F298:
    needs explicit vassal+DP incompatibility plus optional defensive handling of
    failed nested proxy creation. Merely reordering items is insufficient.

Embargo:
  F285:
    protected-reparations recognition/ownership.

  F294:
    one-player embargo owner scope versus whole-team ordinary-deal scan.

  F295:
    non-additive shared team-level OB/DP contribution inside a per-player member
    sum. It survives even after F294's ordinary-deal filter becomes exact-player.

Political sponsorship:
  F289:
    structural sponsor/target coalition validity for a single hired war.

  F293:
    lossy multiple political-target metadata for rival-trade attitude.

  F297:
    multi-war pricing/execution closure in legacy war AI.

  None is removed by the narrow repair for either neighbor.

Annual/value bookkeeping:
  F282:
    cancellation-memory accumulation.

  F296:
    within-batch attitude-cache refresh timing.

  They share no mutable counter or repair point.

FINAL SATURATION CONTROLS
-------------------------

1. Permanent Alliance + Open Borders / Defensive Pact
   PA can legitimately supersede these weaker bilateral relationships. OB/DP
   are dual and their immediate reciprocal value is not a one-sided extra
   promise. The problematic part of post-merge proxy cleanup is already F290.
   No independent F299.

2. Generic startTeamTrade failure followed by unconditional team-state setter
   This is a real robustness shape for OB/DP/disengage, but a separate normal
   trigger was not established beyond F298:
     * vassal+DP is F298;
     * vassal+OB remains a coherent potentially economically meaningful state;
     * disengagement is not an ordinary voluntary trade-table item;
     * surrender+DP cannot prevalidate between the warring negotiating teams.
   No F299 from the helper pattern alone.

3. Vassal + Open Borders
   Not proven incompatible. Current free-trade/trade-route semantics retain a
   role for Open Borders after vassalization. No F299.

4. Master + vassal as two brokered-peace targets
   Raw inventory can suggest the shape, but AI_makePeaceTrade returns
   DENIAL_VASSAL for the vassal target; a human cannot be the TRADE_PEACE giver.
   No accepted normal bundle established.

5. Open Borders + Disengagement
   Synthetic ordering is contradictory, but TRADE_DISENGAGE lies beyond
   NUM_TRADEABLE_ITEMS and is produced through the makePeace/signDisengage
   machinery rather than ordinary voluntary negotiation. No normal mixed
   producer.

6. Tech + resource self-obsolescence
   Resource legality checks obsolescence for both sides before implementation,
   so the candidate is preblocked.

7. Tech enabling a later sibling item
   Whole-bundle prevalidation fails conservatively before the tech transfer
   rather than accepting a newly legal sibling.

8. Duplicate civic/religion items
   same-column civic duplicates and multiple religions are rejected by
   AI_checkCivicReligionConsistency. Different civic columns are deliberately
   allowed and are exactly F291.

9. Multiple GOLD/GPT items
   normal trade-table representation exposes one editable entry rather than an
   arbitrary duplicate-obligation producer. No supported duplicate root found.

10. PA + OB/DP teardown after team reassignment
    old agreement cancellation assertions/state pollution remain F290; no
    separate final-state root emerged.

11. Vassal/surrender + remaining team-status pairings
    PA is F288; DP is F298; disengage lacks a voluntary producer; surrender+DP
    fails prevalidation at war; treaty/list issues are already F281/F283/F287.

12. Serialization / reset / ownership
    CvDeal writes and reads ID, initial turn, both players and both lists
    symmetrically. reset/uninit clear both lists and serialized scalars. No
    missing owned member or independent persistence bug.

13. Accessors
    exact-player and exact-one-team assertions are valid contracts. Confirmed
    violations are caller-scope/identity defects already represented by
    F290/F294.

14. Small classifiers
    isVassalTrade, isVassalTributeDeal, isDisengage, isAllDual, isAnnual,
    isDual, hasData and isEndWar were reconciled against current item
    representation. The empty-list vacuous tribute helper has no live persistent
    empty-deal consequence.

15. kill / endTeamTrade list orientation
    normal OB/DP/disengagement team proxies are dual and satisfy the current
    orientation assumptions. The one-sided exception is peace-treaty
    representation and remains under F281/F283 sibling evidence rather than a
    new root.

HISTORY / INDEPENDENT COMPARISON CLOSURE
----------------------------------------

The final history sweep classified every substantive CvDeal repair stream
encountered. High-value streams include:
  1285 disengagement;
  1287 reparations;
  1290 cancellation/update handling;
  1361 rival-trade context -> F293;
  1376 cancellation notification;
  1385 forced religion conversion;
  1497 embargo/cancellation memory -> F282/F285/F294 family;
  1519 peace-city handling;
  1577/1578 cancellation repairs -> challenged by F284/F286/F287;
  1690 peace cancellation -> preserved, independent F281 remains;
  1882/1885 PA/player-death repairs -> F284/F288/F290 descendants;
  1997 team-trade m_iData/default repair -> preserved;
  2070 peace in tribute/help -> represented in peace-family tests;
  2350 UWAI reporting gate -> preserved;
  2485 cancellation sound -> preserved;
  2658 OB/DP prolongation -> F283 sibling evidence;
  2691/3275 implied/displayed peace treaty work -> F281/F283 representation;
  2765 broke-GPT cancellation -> F287 interaction;
  2930 member-summed embargo floor -> F295;
  3118 cancellation-memory halving -> F282;
  3309 attitude-cache timing -> F296.

Remaining CvDeal-touching history entries were mechanical, API renames,
iterator/refactor changes, diagnostics/UI-only changes, merge commits, or
already-preserved semantic adjustments. Particular rechecks included:
  1263 capitulated-vassal embargo sponsor exception;
  1267 makePeace helper rename;
  1270/1274/1280 war/embargo memory gating;
  1282/1318 cancellation message formatting;
  1301 significant-tech helper extraction;
  1362/1363/1367 trade-value/memory refinements;
  1386/1387 refactors;
  2571 prior isVassalTributeDeal copy-paste fix;
  2764/2785 style/comment changes;
  2920/2954 UWAI call typo/merge;
  3043 profiling-only map-trade skip;
  3099/3109 iterator conversions;
  3392 merge of already-audited changes.

No surviving adjacent invariant from those entries justified F299.

Current Base AdvCiv 1.14 was normalized against SAS for CvDeal.cpp. No later
upstream CvDeal bugfix was found missing from SAS. The material SAS differences
are current logging/game-record instrumentation, victory-denial peace guard,
KI#412 reparations giver identity and related local annotations rather than an
omitted 1.14 repair.

K-Mod and Civ4CE/BtS were used independently to challenge the roots and repair
boundaries. Several findings are inherited older behavior; several arise from
AdvCiv combinations/refactors; none of the final eighteen depended solely on a
SAS textual oddity without a live contract.

SATURATION DECISION
-------------------
Coverage is broad enough to close rather than continue rereading:
  * every CvDeal method family covered;
  * all TradeableItems classifications reconciled;
  * start/end symmetry and both outer list directions audited;
  * whole-bundle prevalidation versus mutation matrix audited;
  * team-proxy lifetime across cancellation, death, vassalization and PA audited;
  * annual doTurn/verify behavior audited;
  * player/team accessor contracts and all high-risk consumers challenged;
  * history-guided sibling invariants exhausted;
  * Base AdvCiv, K-Mod and Civ4CE comparisons completed;
  * final repair/root-independence review completed.

No credible independent F299 survived.

QUEUE 042 CLOSED with:
  F281-F298 confirmed provisional
  F299 UNUSED

PROGRESS
--------
25 / 169 first-pass files complete.

NEXT QUEUE
----------
Queue 047 — CvGameCoreDLL/CvDllPythonEvents.cpp
STATUS: OPEN / next reconciled priority queue
F299 UNUSED / provisional KI#622 next.

Opening audit focus:
  * DLL -> Python event dispatch contracts and argument packing;
  * optional vs required Python callbacks;
  * object lifetime / Cy wrapper ownership;
  * null object and enum/sentinel conversion;
  * return-value / error handling;
  * event ordering and save/load / initialization boundaries;
  * multiplayer/determinism implications;
  * SAS-vs-Base AdvCiv normalized delta;
  * K-Mod/Civ4CE comparison;
  * AdvCiv history-guided repair streams and previous-album archaeology.

No repository source files modified.
===============================================================================

===============================================================================
C026-WIP01 ADDENDUM — queue 047 CvDllPythonEvents.cpp
F299 / provisional KI#622 confirmed; F300 unused
===============================================================================

QUEUE 047 — CvGameCoreDLL/CvDllPythonEvents.cpp
STATUS: OPEN
F299 / provisional KI#622 — CONFIRMED
F300 / provisional KI#623 — UNUSED / next
Progress remains 25 / 169 until queue 047 closes.

F299 / provisional KI#622
Title:
  BUG's live event-manager override discards the consumable-event return value,
  so the DLL never learns that Python consumed a keyboard or mouse event.

Why this belongs in the queue-047 integration audit:
  CvDllPythonEvents.cpp is the native half of the event ABI. For keyboard and
  mouse input it is not a fire-and-forget notification: postEvent deliberately
  retrieves the Python return value and reportKbdEvent/reportMouseEvent return
  it to the exported CvEventReporter input hooks.

  The C++ side is internally coherent. The live Python event manager breaks the
  other half of that contract.

Native contract:
  CvDllPythonEvents::postEvent:

      long lResult = -1;
      bool bOK = gDLL->getPythonIFace()->callFunction(
          PYEventModule, "onEvent",
          eventData.makeFunctionArgs(), &lResult);

      return (bOK && lResult == 1);

  reportKbdEvent(...) and reportMouseEvent(...) directly return postEvent(...).

  CvEventReporter exposes both as DllExport bool methods and documents:

      // Returns true if the event is consumed by Python
      bool CvEventReporter::mouseEvent(...)
      bool CvEventReporter::kbdEvent(...)

  Thus the engine-facing interface explicitly expects Python's consumed/not-
  consumed result to propagate through this chain.

Stock Python contract:
  Assets/Python/EntryPoints/CvEventInterface.py:

      def onEvent(argsList):
          ...
          return getEventManager().handleEvent(argsList)

  Stock CvEventManager.handleEvent correctly preserves the handler result:

      ret = 0
      if self.EventHandlerMap.has_key(tag):
          fxn = self.EventHandlerMap[tag]
          ret = fxn(argsList[1:idx])
      return ret

BUG override:
  The live manager is BugEventManager.BugEventManager:

      eventManager = BugEventManager.BugEventManager()

  Its handleEvent override currently does:

      def handleEvent(self, argsList):
          self.bDbg, self.bMultiPlayer, self.bAlt, self.bCtrl, \
              self.bShift, self.bAllowCheats = argsList[-6:]
          self._dispatchEvent(argsList[0], argsList[1:-6])

  The return value of _dispatchEvent is discarded.

  Yet _dispatchEvent itself explicitly returns the selected dispatcher:

      return EVENT_FUNCTION_MAP.get(
          eventType,
          BugEventManager._handleDefaultEvent)(
              self, eventType, argsList)

  and both consumable native input events are intentionally mapped to:

      "kbdEvent":   BugEventManager._handleConsumableEvent
      "mouseEvent": BugEventManager._handleConsumableEvent

  _handleConsumableEvent has the intended semantics:

      result = eventHandler(argsList)
      if result > 0:
          return result
      ...
      return 0

  So the framework computes the consumed result correctly and then drops it one
  call frame before CvEventInterface.onEvent returns to C++.

Live current triggers:
  This is not a dead API path.

  BugEventManager.onKbdEvent returns 1 for a registered BUG shortcut. Current
  SAS assets ship multiple shortcut registrations, including:
      Alt M               — Reminder
      Ctrl X / Ctrl F10   — Strategy Overlay visibility
      Alt X / Alt F10     — Strategy Overlay edit mode
      Alt Ctrl O / Alt J  — BUG Options
      Alt Ctrl F1         — BUG Help
      Shift M             — friendly-move display toggle
      Ctrl Z              — debug mode toggle
      Alt Shift M         — save-map shortcut
      Ctrl Shift E        — trigger-event popup

  The handler also returns 1 for several built-in debug/utility key
  combinations.

  CvEventManager.onMouseEvent, which remains registered through BUG's handler
  map, likewise returns 1 for:
      * handled Ctrl+Alt / Ctrl+Shift debug clicks; and
      * screen Back / Forward events when CvScreensInterface consumes them.

Observable contract consequence:
  Inside Python, _handleConsumableEvent still stops later Python handlers after
  the first positive result, so the shortcut itself can appear to work.

  Across the DLL boundary, however, BugEventManager.handleEvent implicitly
  returns None. CvEventInterface.onEvent therefore returns None rather than the
  integer 1 that postEvent is waiting for.

  Consequently postEvent cannot satisfy:
      bOK && lResult == 1

  and CvEventReporter::kbdEvent/mouseEvent cannot report the consumed state back
  through their exported bool contract.

  The engine can therefore continue native/interface processing for an input
  that Python has already treated as consumed. The exact visible follow-on
  depends on the key/button and current screen, but the contract violation is
  deterministic and current shortcuts produce the positive result needed to
  exercise it.

Why this is not merely "BUG handles consumption internally":
  BUG's internal early-return solves only multiple Python handlers reacting to
  the same input.

  The exported C++ comment and postEvent implementation define a second,
  engine-facing purpose for the return value. If the intended contract ended
  inside Python, reportKbdEvent/reportMouseEvent would not need to return
  postEvent, and CvEventReporter would not expose/document the bool as consumed
  by Python.

Ancestry:
  The same missing return is present in:
      * Base AdvCiv 1.14 BugEventManager.py;
      * the supplied K-Mod BugEventManager.py.

  Their CvDllPythonEvents::postEvent implementations also retrieve lResult and
  require lResult == 1 in exactly the same way.

  Stock CvEventManager (including current SAS/Base) still returns ret correctly.
  The loss occurs specifically in the BUG/K-Mod event-manager override.

  Civ4CE/BtS does not contain this bundled BUG manager in the supplied tree, so
  the cross-layer regression is from the BUG/K-Mod integration lineage rather
  than the original stock CvEventManager contract.

Repair:
  Minimal and local:

      def handleEvent(self, argsList):
          self.bDbg, self.bMultiPlayer, self.bAlt, self.bCtrl, \
              self.bShift, self.bAllowCheats = argsList[-6:]
          return self._dispatchEvent(argsList[0], argsList[1:-6])

  This restores the return value that _dispatchEvent and
  _handleConsumableEvent already compute.

Repair boundary / compatibility:
  * Do not change CvDllPythonEvents::postEvent; its exactly-1 contract matches
    stock Civ4 and the existing input handler convention.
  * Do not make all default event handlers return 1. Ordinary notifications
    should remain unconsumed.
  * Existing default dispatchers commonly return None; returning that through
    handleEvent is harmless for fire-and-forget events and preserves current
    behavior there.
  * OnSave has its own special dispatcher returning a string, which is another
    reason the generic wrapper should preserve rather than discard dispatcher
    return values. Queue 047 does not rely on OnSave for F299's proof.
  * Current consumable handlers return 1, so no broader "any positive integer"
    policy change is needed on the C++ side.

Severity:
  Low-to-moderate UI/input integration defect. Python shortcuts still execute,
  which can hide the problem, but the native engine is never told they consumed
  the input. Depending on overlap with native screen/key handling this can
  permit duplicate/fall-through actions.

QUEUE-047 CONTROLS ESTABLISHED SO FAR
-------------------------------------
1. SAS normalized delta:
   The only functional SAS-vs-Base-AdvCiv-1.14 addition in
   CvDllPythonEvents.cpp is reportUnitCaptured(eOldOwner,eOldUnitType,pNewUnit)
   for SAS Military Advisor battle history.

   Its current caller creates pCapturedUnit successfully before firing the
   event. Python's live BUG override expects exactly those three arguments and
   only reads/records the new unit. No duplicate native/Python source for this
   event was found.

2. Native event-name coverage:
   Every literal native event name emitted by CvDllPythonEvents.cpp has a
   corresponding current CvEventManager event entry/handler. No misspelled or
   unregistered current event name found.

3. plotFeatureRemoved ABI mismatch — real but latent in shipped SAS:
   C++ emits:
       (plot, city, feature)
   while CvEventManager expects:
       (plot, feature, city)

   This inherited mismatch exists in Base AdvCiv, K-Mod and Civ4CE/BtS.
   Current SAS has no BUG/autolog/EventSigns/other listener for
   plotFeatureRemoved; the sole stock handler is an empty stub that does not use
   any argument. Therefore no shipped current consequence was established and
   it remains an ABI robustness/modding control, not F300.

4. Object-lifetime timing checked so far:
   * unitKilled fires before delayed/immediate removal;
   * unitLost fires after map/group detachment but before deleteUnit, and the
     object remains valid for owner/type reads during synchronous Python call;
   * unitCaptured fires only after successful captured-unit init;
   * cityLost fires before deleteCity;
   * cityRazed fires before disband/kill;
   * cityAcquired fires on the newly-created city before optional raze/AI
     disposal;
   * greatPersonBorn fires only with a live unit and non-null city.
   No post-delete CyUnit/CyCity wrapper established yet.

5. Callback guards:
   Eight CvDllPythonEvents callback defines are cached:
       ON_UPDATE
       ON_UNIT_SET_XY
       ON_UNIT_SELECTED
       ON_UNIT_CREATED
       ON_UNIT_LOST
       ROUTE_BUILT
       UNIT_MOVE
       UNIT_BUILD_IMPROVEMENT

   Current SAS XML defines all eight as 0. Their static caching was an intentional
   AdvCiv optimization already challenged in earlier queues; no runtime setter
   for these USE_* flags was established.

6. Cross-language side notes, not queue findings:
   * stock CvEventManager.onCorporationRemove references undefined iReligion
     when its optional corporation/religion logging branch is enabled;
   * stock onSelectionGroupPushMission references undefined pHeadUnit when the
     corresponding stock logging flag is enabled.
   These are Python handler defects, not native argument-packing mismatches.

NEXT AUDIT CURSOR
-----------------
F300 / provisional KI#623 unused.

Continue:
  * finish every C++ argument order/arity pairing against all live BUG/autolog/
    Civ4lerts listeners, not only the stock handler;
  * finish nullable CyPlot/CyCity/CyUnit and call-site lifetime proof;
  * audit reportSelectionGroupPushMission count/array contract and group
    mutation/lifetime;
  * inspect generic-event void* / tuple ownership and return/error handling;
  * history-guide the AdvCiv 1648 callback-guard/event-class split and all
    remaining CvDllPythonEvents-touching commits;
  * compare K-Mod/Civ4CE functional divergences for missing fixes;
  * keep plotFeatureRemoved as control unless a current shipped consumer is
    found;
  * do not allocate F300 without a live independent trigger/consequence.

No repository source files modified.
===============================================================================

===============================================================================
C026-WIP02 ADDENDUM — queue 047 CvDllPythonEvents.cpp
Deep ABI/lifetime/guard controls; F300 remains unused
===============================================================================

QUEUE 047 — CvGameCoreDLL/CvDllPythonEvents.cpp
STATUS: OPEN
F299 / provisional KI#622 — confirmed in WIP01
F300 / provisional KI#623 — UNUSED / next
Progress remains 25 / 169 until queue 047 closes.

This checkpoint deliberately contains NO new numbered finding. The investigation
since WIP01 was primarily adversarial falsification of several plausible
cross-language defects.

FULL NAMED-EVENT ARITY / REGISTRATION SWEEP
-------------------------------------------
Current CvDllPythonEvents.cpp emits 72 literal named events through postEvent.

The stock CvEventManager.EventHandlerMap contains a handler entry for every one
of those 72 names. No native event name is currently missing from the Python
event registry.

A second current-SAS scan found 85 direct addEventHandler/setEventHandler
registrations for those native event names across BUG and Contrib modules.

For every listener whose function body is locally resolvable, the direct
argsList unpack/index contract agrees with the native arity.

The six registrations not automatically resolved by the first static pass were
AIAutoPlay bound methods. Manual/local resolution confirms their live contracts:
  kbdEvent         -> six native event args;
  BeginPlayerTurn  -> two args;
  EndPlayerTurn    -> two args;
  victory          -> two args.
The commented-out EndGameTurn/GameStart registrations are not live.

No current arity mismatch survived.

plotFeatureRemoved remains the sole semantic-order mismatch found:
  native:
      reportPlotFeatureRemoved(CvPlot* pPlot, FeatureTypes eFeature, CvCity* pCity)
      emits:
          pPlot
          pCity
          eFeature

  stock Python expects:
          pPlot
          iFeatureType
          pCity

This is inherited in Base AdvCiv, K-Mod and Civ4CE/BtS.

Current SAS listener search still finds only the stock handler, whose body merely
assigns the three variables and performs no operation. No BUG, autolog,
Civ4lerts, EventSigns or SAS listener consumes plotFeatureRemoved arguments.
Therefore this remains a real ABI/modding robustness defect but does NOT satisfy
the current shipped consequence standard for F300.

CALLBACK-GUARD SATURATION CHECK
-------------------------------
AdvCiv practical 1648 / advc.003y moved eight USE_* event callback defines into
CvDllPythonEvents and cached them in m_abUseCallback:

    ON_UPDATE
    ON_UNIT_SET_XY
    ON_UNIT_SELECTED
    ON_UNIT_CREATED
    ON_UNIT_LOST
    ROUTE_BUILT
    UNIT_MOVE
    UNIT_BUILD_IMPROVEMENT

The introducing source comment says these callbacks were selected because they
were unused, unlike callbacks needed by Autolog/EventSigns/Civ4lerts.

Current SAS XML still defines all eight corresponding USE_* flags as 0.

A current Python registration/name sweep found no later non-stock listener for
the guarded native events:
    Update
    unitSetXY
    unitSelected
    unitCreated
    unitLost
    routeBuilt
    unitMove
    unitBuildImprovement

The only superficially similar search hits were "gameUpdate", which is a
different generic BUG event and is not controlled by ON_UPDATE.

Therefore practical 1648's old optimization has not become stale in current
SAS. No F300.

The m_abUseCallback array is NULL-safe before initialization through isUse().
initCallbackGuards would replace the allocation without deleting the old array
if called repeatedly, but queue 040 already challenged the surrounding
cacheGlobals/SetGlobalDefines lifecycle and could not establish a supported
second initialization within one CvGlobals/DLL lifetime. Do not manufacture a
duplicate leak finding here without a proven re-init producer.

TRAILING EVENT-CONTEXT FLAGS
----------------------------
postEvent appends:
    debugMode
    false               // bMultiPlayer
    alt
    ctrl
    shift
    cheats/debug allowed

bMultiPlayer being hardcoded false is inherited from BtS/K-Mod/Civ4CE and looks
odd by name. Current Python only assigns it to
BugEventManager.bMultiPlayer; no current Python consumer reads that attribute.
Consequently there is no shipped behavioral consequence to promote.

The cheat/debug flag differs intentionally from older K-Mod:
AdvCiv/SAS also treats game debug mode as allowing the event-manager debug
actions. Current BUG code relies on isCheatsEnabled / debug mode separately in
ways consistent with this change. No mismatch found.

CYARGSLIST CAPACITY / TEMP ARRAY OWNERSHIP
-----------------------------------------
CyArgsList::MAX_CY_ARGS == 20.

A direct count of every native packet shows the maximum current packet is
reportMouseEvent:
    8 entries before postEvent
  + 6 context flags
  = 14 total tuple arguments.

reportKbdEvent is 13 total; all others are <= 12.
No current overflow of CyArgsList's fixed argument array.

reportSelectionGroupPushMission:
    iNumUnits = pSelectionGroup->getNumUnits()
    aiUnitIds = new int[iNumUnits]
    traverse same selection-group unit list
    eventData.add(aiUnitIds, iNumUnits)
    delete[] aiUnitIds

CyArgsList::add(const int*, length) constructs a fresh Python list and converts
each integer before postEvent. The Python packet therefore does not retain the
temporary C++ array.

Current AdvCiv/SAS uses delete[] correctly. K-Mod fixed the older scalar-delete
bug ("Memory leak"); Civ4CE/BtS retains the older delete form. No current SAS
root.

The count and traversal occur synchronously with no Python callback between
them, so no supported mutation window was found that would make getNumUnits and
the node traversal diverge.

COMBATDETAILS TEMPORARY-LIFETIME CANDIDATE — REJECTED
------------------------------------------------------
This was the strongest lifetime concern after WIP01.

Native generic combat events pass pointers to CombatDetails values that can be
stack locals in CvUnit::resolveCombat. Current autolog deliberately stores the
received Python CombatDetails objects in self.WdlAttacker/self.WdlDefender and
can read them later from selectionGroupPushMission, after resolveCombat has
returned.

That looks like a dangling-reference bug if makePythonObject exposes a borrowed
reference to the native CombatDetails.

The Boost.Python conversion contract shows that it does not.

CvDLLPythonIFaceBase::makePythonObject<T>(T* pObj) constructs a
boost::python::object from the raw pointer.

Boost.Python arg_to_python selects:
    pointer_deep_arg_to_python<T*>
for raw pointers.

The distinct shallow/reference path is:
    pointer_shallow_arg_to_python
which uses make_reference_holder and is selected for pointer/reference wrapper
types, not an ordinary raw T*.

pointer_deep_arg_to_python passes the pointee through the registered pointee
converter. CombatDetails is registered as a normal:
    python::class_<CombatDetails>("CombatDetails")
with no noncopyable/reference-holder restriction.

The corresponding class converter is the copy-constructor/value-holder path
(class_cref_wrapper -> make_instance). Thus the Python CombatDetails object owns
a copied CombatDetails value rather than borrowing the stack address.

This is consistent with the pervasive Civ4 pattern:
    CyUnit* wrapper = new CyUnit(pUnit);
    makePythonObject(wrapper);
    postEvent(...);
    delete wrapper;
The Python object survives deletion of the temporary wrapper because the wrapper
value was converted/captured independently; a CyUnit copy itself contains the
underlying CvUnit pointer, whereas CombatDetails is a self-contained value
record.

Therefore autolog retaining the CombatDetails Python objects across the later
mission event is safe with respect to the C++ CombatDetails stack lifetime.
No F300.

SAS battle-history handlers are even more conservative: they immediately copy
the relevant CombatDetails fields into plain integer tuples rather than retaining
the wrapper.

NULLABLE WRAPPER / CALL-SITE CONTROLS
-------------------------------------
Additional current handler checks:

* greatPersonBorn:
  current stock handler explicitly checks pUnit.isNone() and pCity.isNone()
  before dereferencing.

* nukeExplosion:
  CyUnit can wrap a null pNukeUnit, but the current stock handler logs only the
  plot and does not dereference the optional unit.

* goodyReceived:
  stock/autolog listeners do not dereference a possibly-null pUnit. The unit
  naming extension only performs unit naming when the goody type itself grants
  a unit and searches the player's resulting units rather than relying on a null
  wrapper.

* plotFeatureRemoved:
  pCity is documented as nullable in Python. The only current handler does no
  work; argument ordering remains the already-recorded latent ABI control.

* cityAcquired:
  CvPlayer::acquireCity creates/initializes kNewCity, updates visibility and plot
  groups, then emits cityAcquired(&kNewCity) before the subsequent raze/disband/
  keep decision. The Python CyCity therefore references a live city throughout
  the synchronous callback.

* combatResult:
  both winner and loser still exist when CvEventReporter::combatResult is fired;
  lethal kill/removal occurs afterward. Current Python reads/copies the required
  data synchronously.

The earlier WIP01 timing controls for unitKilled, unitLost, unitCaptured,
cityLost, cityRazed, cityAcquiredAndKept and unit gifts remain intact.

GENERIC-EVENT SHAPE CONTROLS
----------------------------
Native reportGenericEvent nests the caller-created Python tuple as one event
argument. Stock generic-event consumers intentionally unpack through:
    genericArgs = argsList[0][0]

Current native generic callers:
    gameUpdate
    AutoPlayComplete
    SwitchHotSeatPlayer
    combatLogHit
    combatLogCalc
    sasBattleHistoryCombatDetails
    sasBattleHistoryCombatRetreat

The combat/gameUpdate consumers are written for this nesting.

SwitchHotSeatPlayer has an old cross-source shape inconsistency:
  native genericEvent -> nested ePlayer tuple;
  BUG's own fireEvent path -> flat ePlayer argument.

Current listeners do not interpret the native nested value in a way that
produces a shipped failure:
  Civ4lerts resets state and ignores it;
  Reminder clears reminder text;
  BUG screen reset listeners ignore it;
  StrategyOverlay forwards the value, but the live DotMap switch handler simply
  hides the layer and does not inspect the player ID.

Keep as an integration/API control, not F300.

F299 RETURN-VALUE ROOT — ADDITIONAL BOUNDARY EVIDENCE
------------------------------------------------------
BugEventManager has special dispatchers not only for kbdEvent/mouseEvent, but
also:
    OnPreSave
    OnSave
    OnLoad
    PreGameStart

_handleOnSaveEvent explicitly returns the concatenated strings returned by
OnSave handlers.

BugEventManager.handleEvent currently discards _dispatchEvent's result for all
of them, reinforcing that the missing return is a generic wrapper defect, not a
keyboard-specific coding convention.

Current SAS registers only the stock OnSave handler, which returns the empty
string, while BUG's real persistent BugData save runs during OnPreSave through
CyGame.setScriptData. Therefore no additional current save-data consequence is
needed or claimed beyond F299's live keyboard/mouse proof.

The minimal F299 repair remains:
    return self._dispatchEvent(...)

Do not alter individual dispatchers to manufacture return values they do not
naturally have.

BASE / K-MOD / CIV4CE COMPARISON STATUS
---------------------------------------
Normalized SAS vs Base AdvCiv 1.14 CvDllPythonEvents.cpp still has one functional
addition only:
    reportUnitCaptured(old owner, old unit type, new unit)
for SAS Military Advisor battle history.

No upstream Base-1.14 event fix is missing from SAS.

K-Mod divergence is dominated by later AdvCiv refactors:
    callback guard relocation/caching;
    enum-cast cleanup;
    interface wrapper modernization;
    debug-mode cheat allowance;
    const/ID helper cleanups;
    selectionGroupPushMission delete[] fix lineage.

No K-Mod-only fix absent from current SAS has survived so far.

Civ4CE/BtS retains older event code, including the plotFeatureRemoved order
mismatch and old selection-group array deletion form; no current CE correction
was found that SAS lacks.

History touch-set for CvDllPythonEvents.cpp has been enumerated. The main semantic
stream under active final review is practical 1648 (callback split), now strongly
controlled above; remaining touches are primarily refactor/rename/include/enum
cleanup, debug-key support, K-Mod's array-delete fix, and SAS unitCaptured.

NEXT CURSOR
-----------
F300 remains UNUSED.

Continue with:
  1. finish current wrapper retention search for CyUnit/CyCity/CyPlot objects,
     especially destructive events, proving listeners copy IDs/data rather than
     keeping dead native-object pointers;
  2. finish reportGenericEvent PyObject/reference-ownership reasoning as far as
     the exposed Python-interface contract permits;
  3. finish event-name semantic-order review for non-stock live listeners, not
     only arity;
  4. finish the full CvDllPythonEvents history ledger and compare every
     substantive change against current SAS;
  5. inspect exported keyboard/mouse call sites enough to bound F299's exact
     engine fall-through consequence without overclaiming opaque EXE behavior;
  6. if all remaining paths close, keep F300 unused and move toward queue-047
     saturation rather than inventing a second root.

No repository source files modified.
===============================================================================

===============================================================================
C026-WIP03 ADDENDUM — queue 047 CvDllPythonEvents.cpp
Post-WIP02 corrections frozen; wrapper/history/semantic-order pass advancing
===============================================================================

QUEUE 047 — CvGameCoreDLL/CvDllPythonEvents.cpp
STATUS: OPEN
F299 / provisional KI#622 — CONFIRMED
F300 / provisional KI#623 — UNUSED / next
Progress remains 25 / 169 until queue 047 closes.

This checkpoint first freezes the corrections that were reached after physical
WIP02 in the previous thread, then records the new-source verification and the
next audit slice. It deliberately does NOT allocate F300.

POST-WIP02 CORRECTION — BACK/FORWARD MOUSE NAVIGATION
-----------------------------------------------------
WIP01 had included screen Back / Forward events among the live mouse paths that
return 1 and therefore prove F299. That was too broad.

Current SAS CvEventManager.onMouseEvent delegates EventBack/EventForward to:
    CvScreensInterface.handleBack(screens)
    CvScreensInterface.handleForward(screens)

Both helpers perform their navigation but explicitly return 0. The same return-0
behavior is inherited in Base AdvCiv, K-Mod and Civ4CE/BtS.

Therefore Back/Forward must NOT be used as positive-consumption evidence for
F299. They remain a separate suspicious API/control case: the action is handled
but reports 0. No local evidence establishes a harmful engine follow-on, so this
is not promoted to F300.

F299 remains firmly proven by the numerous live keyboard/shortcut and debug
mouse paths that explicitly return 1.

POSITIVE-RESULT WIDTH CONTROL
-----------------------------
BUG's _handleConsumableEvent treats any result > 0 as consumed, whereas C++
postEvent recognizes exactly lResult == 1.

Current live consumers were checked: keyboard shortcut/debug handlers return
1/0, current debug mouse handling returns 1, and Back/Forward returns 0. No live
consumer returning 2+ was found. Therefore no independent current mismatch is
established from the >0 versus ==1 distinction.

DESTRUCTIVE / NULLABLE WRAPPER RETENTION — DEEPER CURRENT-SAS PASS
------------------------------------------------------------------
The remaining lifetime search was extended through the active BUG/Contrib
listeners for combatResult, unitKilled/unitLost/unitCaptured/unitGifted,
cityRazed/cityAcquired/cityAcquiredAndKept/cityLost, greatPersonBorn,
nukeExplosion and goodyReceived.

No shipped listener was found retaining a soon-invalid CyUnit/CyCity/CyPlot
wrapper past the synchronous callback in these destructive paths.

Notable controls:

* Civ4lerts city state:
  onCityLost/onCityAcquiredAndKept pass the live city to alert objects, but the
  alert state reduces it immediately to getCityId(city) == (owner, cityID) plus
  scalar population/anger/test state. The CyCity object itself is not retained.

* MoreCiv4lerts/autolog city loss/raze/acquire handlers read the city
  synchronously and retain no city wrapper.

* greatPersonBorn autolog does not perform the stock handler's explicit
  isNone() checks, but the only current native producer in CvPlayer fires the
  event only under `if (pCity != NULL)`, after the Great Person unit has been
  successfully created. The apparently weaker listener therefore has no
  reachable nullable producer in current SAS.

* unitCaptured's current SAS listeners consume/copy capture data synchronously;
  the new captured unit has already been successfully created when the event is
  emitted.

No F300 from wrapper retention or great-person nullability.

SEMANTIC-ORDER SWEEP — DIRECT CURRENT LISTENERS
-----------------------------------------------
The direct BUG/Contrib registrations were re-read for semantic argument order,
not merely arity. Representative same-shaped packets all agree with the native
producer order:

    firstContact                 team1, team2
    changeWar                    bWar, team, rivalTeam
    vassalState                  master, vassal, bVassal
    playerChangeStateReligion    player, newReligion, oldReligion
    cityAcquired                 oldOwner, newOwner, city, conquest, trade
    improvementDestroyed         improvement, owner, x, y
    unitPillage                  unit, improvement, route, owner
    goodyReceived                player, plot, unit, goodyType
    religionSpread/remove        religion, owner, city
    corporationSpread/remove     corporation, owner, city
    techAcquired                 tech, team, player, announce
    selectionGroupPushMission    owner, mission, count, unit-ID list

No second same-arity/order mismatch was found. plotFeatureRemoved remains the
already-recorded inherited exception (native plot, city, feature vs Python plot,
feature, city) with no current argument-using listener.

HISTORY LEDGER — SUBSTANTIVE STREAMS NARROWED
---------------------------------------------
The path-history index lists 20 pre-SAS/SAS/K-Mod touches after the initial
K-Mod import. Most are mechanically classifiable and have now been separated
from the true semantic streams.

Substantive streams checked/controlled:

1. K-Mod practical 539 — reliable modifier-key state
   postEvent changed from gDLL->altKey/ctrlKey/shiftKey to K-Mod's native key
   helpers. Current SAS still calls GC.altKey()/ctrlKey()/shiftKey(), whose
   CvGlobals helpers use GetKeyState(...). The fix is preserved.

2. K-Mod practical 1231 — selection-group temporary array deletion
   changed scalar delete to delete[]. Current SAS retains delete[]. Already
   controlled in WIP02.

3. AdvCiv practical 1648 — DLL-to-Python/logging split and callback guards
   This is the major behavioral refactor. Its cached eight callback defines were
   already exhaustively rechecked in WIP02 and remain aligned with current
   unused Python events.

4. AdvCiv practical 1328 — multiplayer/debugging change
   CvDllPythonEvents only changed the trailing allow-cheats/debug flag from
   `gDLL->getChtLvl() > 0` to also accept game debug mode. Current SAS preserves
   this behavior. It does not alter event shape/ownership/consumption.

5. SAS practical 5765 — unitCaptured event
   Rechecked against the current producer and live BUG override; current
   lifetime, order and arity are coherent.

The remaining path-history touches are dominated by enum-to-int cast cleanup,
header/include cleanup, inline/getter renames, constness or merge duplication.
No lost historical event fix has surfaced from those classes so far.

GENERIC-EVENT OWNERSHIP — CURRENT BOUND
---------------------------------------
reportGenericEvent receives a tuple returned by an inner CyArgsList::
makeFunctionArgs() and inserts that PyObject pointer as one element of the outer
event packet. CyArgsList::add(void*) deliberately stores a PyObject pointer
without creating a second wrapper, matching the same ownership convention used
throughout the DLL for makePythonObject results and nested argument packets.

The concrete MakeFunctionArgs implementation is engine-side through the pure
virtual Python interface and is not present in the supplied SDK source. Local
source therefore cannot prove its exact steal/incref sequence line-by-line.
However, the generic-event nesting idiom is inherited from BtS/K-Mod and is used
repeatedly by current native callers; no current double-free, dangling tuple or
retained nested-tuple consequence has been established.

Keep this as a bounded ownership control unless a concrete current failure can
be tied to the opaque engine-side MakeFunctionArgs implementation; do not invent
one from missing implementation detail.

CURRENT CURSOR
--------------
F300 remains UNUSED.

Still to finish before queue-047 saturation:
  1. complete the last non-direct/XML-configured listener semantic-order checks;
  2. finish the history ledger's remaining small touches explicitly enough to
     classify each as mechanical or behaviorally controlled;
  3. finish generic-event producer sequencing/retention checks around the seven
     current native generic-event names;
  4. bound F299's engine-facing consequence to the exported consumed/not-
     consumed contract without claiming opaque EXE behavior we cannot prove;
  5. if these remain clean, close queue 047 with F299 only and leave F300 unused.

No repository source files modified.
===============================================================================


===============================================================================
C027 DURABLE — QUEUE 047 CvDllPythonEvents.cpp CLOSED; QUEUE 054 NEXT
===============================================================================

QUEUE 047 — CvGameCoreDLL/CvDllPythonEvents.cpp
STATUS: FIRST-PASS COMPLETE

CONFIRMED ROOT
--------------
F299 / provisional KI#622
  BUG's live event-manager override drops the return value from
  `_dispatchEvent`. Native CvDllPythonEvents::postEvent deliberately retrieves
  CvEventInterface.onEvent's Python return and recognizes integer 1 as
  "consumed"; CvEventReporter::kbdEvent/mouseEvent export that bool with the
  explicit contract "Returns true if the event is consumed by Python."

  CvEventInterface.onEvent correctly returns `getEventManager().handleEvent`,
  and BUG's `_handleConsumableEvent` correctly stops at and returns the first
  positive keyboard/mouse-handler result. But BugEventManager.handleEvent calls
  `_dispatchEvent(...)` without returning it. A live Python handler can
  therefore consume the event internally while the event-interface boundary
  returns None instead of 1, making the native consumed predicate false.

  Current SAS has multiple live consumers that really return 1, including BUG
  keyboard shortcuts and debug mouse/keyboard paths. The minimal repair is:

      def handleEvent(self, argsList):
          self.bDbg, self.bMultiPlayer, self.bAlt, self.bCtrl, self.bShift, self.bAllowCheats = argsList[-6:]
          return self._dispatchEvent(argsList[0], argsList[1:-6])

  Do not change CvDllPythonEvents::postEvent. Its exactly-1 test is consistent
  with the stock event-interface contract and current positive consumers.

  Ancestry: the missing BUG wrapper return is inherited by Base AdvCiv and the
  K-Mod BUG integration. Civ4CE/BtS stock CvEventManager itself preserves its
  computed return. This is not a SAS-only regression.

ENGINE-FACING CONSEQUENCE BOUND
-------------------------------
The audit deliberately stops at the strongest locally provable boundary:

  * Python's documented entry point says to return 1 when an event is consumed.
  * BugEventManager's consumable dispatcher can and currently does compute 1.
  * the wrapper drops that value and returns None;
  * CvDllPythonEvents::postEvent asks the Python interface for a long result and
    returns true only when that result equals 1;
  * CvEventReporter::kbdEvent/mouseEvent directly return this bool and document
    it as the Python-consumed state.

The proprietary EXE-side caller is not present in the supplied SDK material, so
this checkpoint does NOT invent a more specific engine fall-through behavior
(e.g. a particular second action or UI side effect). The proven defect is the
broken exported consumed/not-consumed contract itself.

FINAL XML / NON-DIRECT LISTENER SWEEP
-------------------------------------
The last registration class left open at WIP03 was parsed from Assets/Config.
There are 13 explicit `<event>` registrations whose type is a native event,
covering six native event types:

  BeginPlayerTurn
    BUG Core -> BugUtil.doHotSeatCheck
    Better Espionage -> SpyUtil.onBeginPlayerTurn

  GameStart
    All Eras Dawn of Man / BUG Data / Better Espionage / Strategy Overlay /
    CvTechChooser

  ModNetMessage
    Strategy Overlay -> CvStrategyOverlay.onModNetMessage

  OnPreSave
    Strategy Overlay -> CvStrategyOverlay.onPreSave

  SwitchHotSeatPlayer
    BUG Core -> BugUtil.onSwitchHotSeatPlayer
    Strategy Overlay -> CvStrategyOverlay.onSwitchHotSeatPlayer

  gameUpdate
    BUG Core -> BugUtil.doDeferredCalls
    OOSLogger -> OOSLogger.onGameUpdate

Semantic use agrees with the native packets:
  * BeginPlayerTurn is (gameTurn, player);
  * ModNetMessage is five integers;
  * GameStart and OnPreSave have no event-specific arguments;
  * gameUpdate and SwitchHotSeatPlayer use the inherited generic nested-tuple
    representation.

The generic XML listeners either ignore the payload or, where the stock handler
needs it, use the established nested unpacking. No second semantic-order defect.

The `<events module=...>` manager declarations were also followed through their
Python registration methods during the broader direct-registration sweep; no
additional native arity/order mismatch survived.

GENERIC-EVENT PRODUCER / RETENTION CLOSURE
------------------------------------------
All seven current native generic-event names were rechecked from producer to
listener:

  gameUpdate
    Payload: one turn-slice integer. It is emitted from CvGame::updateUnprofiled
    when not between turns and from the active-player message-processing bridge
    after temporarily clearing the between-turn state. Current BUG/DealUtil/
    OOS listeners do not retain or misinterpret the integer; the stock generic
    handler uses the inherited nested unpack.

  AutoPlayComplete
    Payload: one turn-slice integer. AIAutoPlay's current completion handler does
    not consume the payload; no lifetime/order issue.

  SwitchHotSeatPlayer
    Native payload: one old-human PlayerTypes value nested by genericEvent.
    BUG's separately fired hot-seat event uses a flat player value, so the two
    producers are ABI-inconsistent. Current listeners on the native path either
    ignore the value, assign it without interpreting it, reset state, or hide
    the Strategy Overlay. No current consequence survives, so this remains a
    control rather than F300.

  combatLogHit
    Payload: attacker CombatDetails, defender CombatDetails, hit-side flag,
    damage. The CombatDetails raw-pointer conversion creates Python-owned value
    copies; no stack pointer is retained.

  combatLogCalc
    Payload: attacker CombatDetails, defender CombatDetails, odds. Same copied
    value lifetime; stock and autolog consumers use the established nested
    unpack.

  sasBattleHistoryCombatDetails
    Payload: two copied CombatDetails values plus attacker/defender pre-fight XP
    and Great-General flags. SASBattleHistory immediately reduces the values to
    integer tuples/context; no borrowed C++ gameplay object is retained.

  sasBattleHistoryCombatRetreat
    Payload: eight integers (owners, unit types, plot coordinates, ending
    strengths). SASBattleHistory converts/stores scalars only.

Thus the only uninspectable ownership detail is the proprietary engine-side
implementation of MakeFunctionArgs itself. No current caller-side dangling,
double-free or retained-wrapper consequence was established.

HISTORY LEDGER — COMPLETE CLASSIFICATION
----------------------------------------
Every CvDllPythonEvents.cpp path-history touch after the initial import is now
classified.

Behaviorally meaningful streams:
  K-Mod 539   — replace unreliable gDLL modifier-key helpers with native/GC key
                 state helpers. Preserved in current SAS.
  K-Mod 1231  — selection-group temporary unit-ID array uses delete[]. Preserved.
  AdvCiv 1328 — allow game debug mode in the trailing cheats/debug event flag.
                 Preserved; no packet-shape change.
  AdvCiv 1648 — split DLL-to-Python event reporting and cache eight unused
                 callback guards. All eight current XML flags remain 0 and no
                 current non-stock listener was added behind a disabled guard.
  SAS 5765    — add unitCaptured(oldOwner, oldUnitType, newUnit). Current native
                 producer, wrapper lifetime and live Python consumer agree.

Mechanical/refactor streams explicitly checked:
  AdvCiv 1253 — comment-only follow-up on the already-correct delete[].
  AdvCiv 1469 / 1548 / 1615 / 1678 — include/header revisions.
  AdvCiv 1552 / 1563 / 1564        — exported/inline getter renames.
  AdvCiv 1591                       — spacing only.
  AdvCiv 1744                       — constness/null-style loop cleanup.
  AdvCiv 1752                       — callback-macro naming cleanup.
  AdvCiv 1953                       — inline IDInfo accessor refactor plus explicit
                                      NULL style; no event semantic change.
  AdvCiv 2043                       — getInterfaceIFace -> UI alias.
  AdvCiv 2204                       — comments/line formatting.
  AdvCiv 2918 / merge copies 2936 and 2998
                                    — remove redundant enum-to-int casts; argument
                                      identity/order unchanged.
  merge/import duplicates           — no independent semantic edit.

No historical event repair was found missing from current SAS.

SAS / BASE / K-MOD / CIV4CE COMPARISON CLOSURE
-----------------------------------------------
Whitespace-normalized current SAS vs Base AdvCiv 1.14
CvDllPythonEvents.cpp has exactly one functional SAS addition:
`reportUnitCaptured`. That addition was audited above and is coherent.

The K-Mod key-state and delete[] fixes remain present. Civ4CE/BtS provides a
useful older control: it still has the pre-K-Mod modifier-key path and the old
scalar selection-array delete, confirming that SAS has not regressed those
known fixes.

No later comparison-source event fix was found absent from SAS.

FINAL CONTROLS / REJECTED CANDIDATES
------------------------------------
1. plotFeatureRemoved inherited ABI order error
   Native emits (plot, city, feature), stock Python names (plot, feature, city).
   This is a genuine API/modding defect inherited through BtS/Civ4CE, K-Mod and
   AdvCiv, but current SAS has only the empty stock handler and it uses none of
   the arguments. Keep as documented debt; no current F300.

2. generic SwitchHotSeatPlayer flat-vs-nested producer inconsistency
   Real interface inconsistency, but all current native-path listeners are
   value-insensitive. No current F300.

3. Back/Forward mouse handling
   CvScreensInterface.handleBack()/handleForward() perform navigation but return
   0 in SAS, Base AdvCiv, K-Mod and Civ4CE. Do not use these as F299 proof; no
   locally supported harmful native fall-through was established.

4. BUG positive result vs native exactly-1
   `_handleConsumableEvent` accepts any positive handler result; postEvent tests
   exactly 1. All current positive consumers found return 1, not 2+. No second
   defect.

5. temporary CombatDetails lifetime
   Rejected: Boost.Python's registered raw-pointer conversion makes value copies.

6. destructive CyUnit/CyCity/CyPlot retention
   Rejected after current listener sweep. Destructive handlers consume objects
   synchronously or reduce them to owner/ID/scalar state.

7. Great Person nullable city
   Rejected: sole current native producer guards pCity != NULL before event
   emission and fires after successful unit creation.

8. callback guards
   Eight cached USE_* guards remain aligned with currently unused callbacks;
   no stale optimization.

9. trailing bMultiPlayer=false
   Odd inherited state, but current Python only stores the attribute and no
   current consumer reads it.

10. CyArgsList capacity
    MAX_CY_ARGS is 20; largest native packet is mouse input at 14 total elements
    after the six context flags.

11. selection-group temporary array
    Python list conversion happens before delete[] and count/traversal have no
    intervening callback mutation window.

12. generic tuple ownership
    engine MakeFunctionArgs implementation is opaque in supplied SDK; inherited
    idiom plus complete current producer-lifetime review gives no evidence of a
    current dangling/double-free issue. Missing implementation is not evidence
    of a bug.

SATURATION DECISION
-------------------
Queue 047 is saturated enough to close:
  * every CvDllPythonEvents method family was read;
  * 72 literal native named events were reconciled with the stock handler map;
  * 85 direct BUG/Contrib registrations plus unresolved AIAutoPlay methods were
    checked for arity;
  * semantic order was checked separately for same-shaped packets;
  * explicit XML/config registrations were parsed and reconciled;
  * generic-event producers/listeners and lifetime were exhausted;
  * wrapper/null/destructive-event lifetimes were challenged;
  * callback guards, fixed CyArgsList capacity and temporary allocations were
    checked;
  * complete path history was classified;
  * normalized Base AdvCiv and independent K-Mod/Civ4CE comparisons were done;
  * attractive but consequence-free ABI debt was kept as controls rather than
    promoted merely to fill F300.

No credible independent F300 survived.

QUEUE 047 CLOSED with:
  F299 / provisional KI#622 CONFIRMED
  F300 / provisional KI#623 UNUSED

PROGRESS
--------
26 / 169 first-pass files complete.

NEXT QUEUE
----------
Queue 054 — CvGameCoreDLL/GroupPathFinder.cpp
STATUS: NEXT reconciled priority queue
F300 remains unused / provisional KI#623 next if a genuine independent root is
established there.

Opening audit focus:
  * path-node lifetime and parent/edge validity;
  * source/destination and invalid-plot sentinel handling;
  * movement-cost arithmetic / overflow / signedness;
  * cache/reset/reuse contracts;
  * team/player/domain visibility and passability assumptions;
  * callback/function-pointer contracts;
  * SAS-vs-Base AdvCiv normalized delta;
  * K-Mod/Civ4CE comparison where counterparts exist;
  * path-history repair streams and cross-file callers.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP01 — QUEUE 054 GroupPathFinder.cpp OPEN — FIRST DEEP PASS
===============================================================================

STATUS
------
Queue 054 remains OPEN.
F300 / provisional KI#623 remains UNUSED.
Progress remains 26 / 169 complete.

CURRENT SOURCE / COMPARISON SET
-------------------------------
Current SAS truth:
  AdvCiv-SAS_light_source_20260829T125810.zip
  CvGameCoreDLL/GroupPathFinder.cpp: 844 queued lines

Local comparison / archaeology:
  Base AdvCiv 1.14
  K-Mod 1.46
  Civ4CE BtS source
  current SAS KmodPathFinder.h and KmodPathFinderLegacy.cpp
  reachable AdvCiv / K-Mod commit-diff mirror and anonymized logs

No web lookup used. No repository source files modified.

SAS-vs-BASE NORMALIZED DELTA
----------------------------
Whitespace/signature-normalized comparison against Base AdvCiv 1.14 found only
one functional current-SAS delta inside GroupPathFinder.cpp itself:

  GroupPathFinder::generatePath(CvPlot const& kTo)

SAS keeps the inherited assertion that setGroup must first establish a group,
but additionally checks pGroup == NULL and returns false in release builds
instead of dereferencing pGroup through getPlot. This is an already-applied SAS
hardening fix, not a new queue-054 finding.

The remaining apparent source differences are formatting / collapsed signatures.
Thus a new root in this queue is expected to be inherited unless archaeology
proves otherwise.

CACHE / GROUP-IDENTITY BOUNDARY
-------------------------------
GroupPathFinder is a process-global reusable finder shared by selection groups.
setGroup performs a full reset when the group pointer changes, and conditionally
when path-relevant MovementFlags change. The start-plot change inside the generic
KmodPathFinder also resets cached nodes.

The current SAS membership invalidations from earlier F152 / KI#475 are present:
  * addUnit invalidates the reusable main finder when this group is cached;
  * deleteUnitNode does the same;
  * full-list teardown invalidates while m_pPathFinder exists, with the later
    KI#475.2 map-teardown null guard;
  * group kill invalidates both main and alternate finder pointers.

Do NOT duplicate that already-fixed root in queue 054.

Normal promotion is also not a surviving stale-cache path. CvUnit::promote calls
CvSelectionGroup::resetPath immediately after setHasPromotion, preserving the
K-Mod safeguard. A direct external setHasPromotion mutation could bypass this,
but no shipped producer/consequence sufficient for F300 has been established.

MOVE_DECLARE_WAR REUSE OPTIMIZATION
-----------------------------------
setGroup intentionally avoids resetting when MOVE_DECLARE_WAR is the only flag
change and the group team is not sneak-attack ready. This initially looked risky
because the flag participates in both movement legality and path cost.

Current controls:
  * for human groups, MOVE_DECLARE_WAR is deliberately not used for intermediate
    canStepThrough legality; destination legality may still use it;
  * AI war-route cost / bIgnoreRoutes differences are tied to
    AI_isSneakAttackReady(target); the no-argument AI_isSneakAttackReady check in
    setGroup forces a reset if any such target state is live;
  * the remaining human-only cost difference is the advc.035 exclusive-radius
    flip bias, but current GlobalDefines_advc.xml has OWN_EXCLUSIVE_RADIUS = 0.

Decision so far: no current F300. If OWN_EXCLUSIVE_RADIUS were enabled, this
optimization would need re-review because toggling MOVE_DECLARE_WAR can then
change human path costs without triggering the sneak-attack reset. That is a
disabled-configuration caveat, not a shipped defect.

HEURISTIC ADMISSIBILITY — CURRENT SAS MOVEMENT DATA
----------------------------------------------------
Sea default heuristic weight is one MOVE_DENOMINATOR, under the source comment
that water has no routes and no promotions that reduce water movement cost.
The current SAS XML preserves that assumption:
  * PROMOTION_NAVIGATOR / naval movement promotions add movement points rather
    than reducing per-tile water movement cost;
  * the only current iMoveDiscountChange=1 promotion is
    PROMOTION_MOBILITY_COST, restricted to armor / mounted land combat classes;
  * current naval combat classes do not receive raw movement-cost discounts or
    water double-move promotions that undercut the denominator lower bound.

Land heuristic weight uses KmodPathFinder::minimumStepCost(baseMoves), whose
initialization caps both route minima at MOVE_DENOMINATOR/2 and then takes the
minimum of base and scaled route costs. Current terrain/double-move discounts do
not undercut this intentionally conservative lower bound.

World-wrap handling is also correct at this layer: heuristic stepDistance uses
CvMap::xDistance/yDistance, which apply map wrapping before taking the Chebyshev
step distance.

Decision: no heuristic-admissibility root established.

2024 DANGER-AVOIDANCE REPAIR — VERIFIED BOUNDARY
------------------------------------------------
AdvCiv practical 4196 / b3de867844 (“Fix danger avoidance checks in pathfinder”)
is present in current SAS. It split canStepThrough into:
  1. a path-independent predicate that may permanently close a node; and
  2. a path-data predicate for the AI danger check, which may reject only the
     particular partial path while leaving the node UNINITIALIZED so another
     parent can try it.

This directly repairs the older K-Mod/AdvCiv failure where first reaching a
dangerous tile from a slow direction could permanently rule out a later roaded
route that crosses the dangerous tile without ending the turn there. The legacy
verification finder was updated in parallel.

The current path-data predicate rejects a dangerous plot when:
  (pathTurns > 1 || moves == 0)
  && AI-controlled
  && !MOVE_IGNORE_DANGER
  && (!canFight || MOVE_AVOID_DANGER)
  && !alwaysInvisible.

The Base AdvCiv manual explicitly records that the 2024 repair removed the known
failure-to-find mode while retaining a Tbd caveat: GroupStepMetric::cost still
uses path history and can generate occasional suboptimal danger-averse paths.
That documented limitation by itself is not a new F300.

REMAINING DANGER REPARENTING QUESTION — OPEN, NOT A FINDING
------------------------------------------------------------
A narrower algorithmic boundary remains under challenge.

KmodPathFinder::processChild applies the path-data canStepThrough predicate only
when a node is first initialized. If that opened node later receives a cheaper
parent, processChild calls updatePathData again but does NOT re-run the
path-dependent danger predicate. forwardPropagate likewise recalculates
moves/path turns and descendant costs without revalidating path-dependent
canStepThrough.

Therefore the code shape permits this state transition in principle:
  safe/opened node (first path: first turn with moves remaining)
    -> cheaper reparent
    -> updated node state has moves == 0 or later path turn
    -> the same dangerous plot would now fail canStepThrough(path data)
    -> node/descendant tree is nevertheless retained and propagated.

This is stronger than the already-documented “cost may be suboptimal” caveat if
it can lead to an actually returned unsafe MOVE_AVOID_DANGER path. It is NOT yet
F300 because a reachable game-state/cost ordering and returned-path consequence
still need proof. If those cannot be established, record it as a theoretical
algorithmic boundary/control only.

OPEN-BORDERS / WORLD-STATE CACHE CHALLENGE — OPEN, NOT A FINDING
----------------------------------------------------------------
A second cache boundary is being tested adversarially.

CvTeam::declareWar explicitly calls CvSelectionGroup::resetPath after movement
permissions/costs change. CvTeam::setOpenBorders does not reset the reusable
K-Mod GroupPathFinder. AdvCiv practical 4324 / 751d73e5f7 (“Update pathfinder
waypoints after signing OB”) adds updateActivePaths for the active-player UI,
but its own comment states that resetting the K-Mod/original finder did not fix
the waypoint problem. That commit is about UI waypoint refresh, not proof that
gameplay cache reuse is semantically valid across Open Borders changes.

At the generic A* level, a foreign-border tile previously rejected by the
path-independent canStepThrough predicate is cached CLOSED. After Open Borders
makes the tile traversable, a same-group/same-start/same-flags call does not
have an intrinsic world-state generation check. A previously CLOSED intermediate
node is not reopened, so stale cache state could in principle block a newly
legal path until some other reset occurs.

However, the normal shipped reachability is not yet proven:
  * the finder is global across all groups, so a different group path call resets
    it before reuse;
  * AI diplomacy happens at the end of CvPlayer::doTurn, before doTurnUnits, and
    the cached group immediately before diplomacy is often from another context;
  * human direct movement uses interface/final-path machinery rather than this
    AI-only reusable generatePath path in the ordinary case;
  * no shipped Python CySelectionGroup.generatePath consumer was found.

Thus the stale-OB cache shape is real, but F300 remains unused unless a normal
same-group producer sequence and current consequence can be proved.

NULL / GETTER PRECONDITION CONTROL
----------------------------------
The SAS release guard in generatePath(kTo) prevents its former null-group crash.
Other path getters such as getPathFirstPlot/getPathEndTurnPlot assume successful
path generation. Current C++ call sites are generally success-guarded, and no
shipped Python consumer was found that makes the getter precondition a current
reachable crash. Keep as API contract/control unless a concrete caller disproves
that later in the queue.

CURRENT CURSOR
--------------
Queue 054 OPEN.
F300 / provisional KI#623 UNUSED.

Next deep checks:
  * prove or reject the danger-node reparenting unsafe-path scenario by bounding
    GroupStepMetric::cost and constructing/rejecting a reachable movement state;
  * finish Open-Borders/world-state cache producer sequencing rather than merely
    observing missing invalidation;
  * classify the remaining historical GroupPathFinder/KmodPathFinder repair
    stream (K-Mod 354-359, 825, 1022/1024, AdvCiv 2587-2602, 2805,
    3216/3226, 4196, 4324) against current code;
  * inspect arithmetic/overflow, node state transitions and max-path behavior;
  * compare any surviving candidate against K-Mod/Civ4CE ancestry before
    allocating F300.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP02 ADDENDUM — queue 054 GroupPathFinder.cpp
Danger-reparent concrete proof + OB sequencing closure work
===============================================================================

QUEUE 054 — CvGameCoreDLL/GroupPathFinder.cpp
STATUS: OPEN
F300 / provisional KI#623 — STILL UNALLOCATED pending final consequence/repair-boundary review
Progress remains 26 / 169.

CORRECTION TO THE EARLY DANGER-PRODUCER NARROWING
-------------------------------------------------
The path-dependent danger predicate is not limited to callers that explicitly
set MOVE_AVOID_DANGER. It applies whenever:

  (pathTurns > 1 || moves == 0)
  && AI-controlled
  && !MOVE_IGNORE_DANGER
  && (!group.canFight() || MOVE_AVOID_DANGER)
  && !alwaysInvisible
  && AI_isAnyPlotDanger(plot).

Therefore every ordinary AI-controlled non-fighting group (Workers, Settlers,
missionaries, executives, many Great People, etc.) is danger-sensitive even
under NO_MOVEMENT_FLAGS. The earlier grep showing only a few explicit
MOVE_AVOID_DANGER producers is useful for fighting groups but does not bound the
noncombat surface.

The explicit MOVE_AVOID_DANGER producers themselves are narrow. Current direct
producers are first-city settler scouting and explore routines. Explore groups
also receive the 2/3 unexplored-tile cost modifier, which removes the particular
4/3 deep-fog asymmetry discussed below. The important current surface is instead
ordinary noncombat AI pathing.

CONCRETE REPARENT STATE — CURRENT SAS CAN INVERT SAFE VS UNSAFE PATH DATA
-------------------------------------------------------------------------
The theoretical WIP01 state can be instantiated with current SAS movement data.
No invented unit or hypothetical XML value is needed.

Current XML contains the live Dutch Worker replacement:

  UNIT_NETHERLANDS_SWIFT_WORKER
    class       UNITCLASS_WORKER
    combat      NONE / iCombat 0
    domain      DOMAIN_LAND
    default AI  UNITAI_WORKER
    iMoves      3

CIV4CivilizationInfos maps the Dutch UNITCLASS_WORKER replacement to this unit.
It has no flat/ignore-terrain movement flag. A singleton full-move group
therefore begins with 3 * MOVE_DENOMINATOR = 180 movement points.

Current routes:

  ROUTE_ROAD      iMovement 30, iFlatMovement 30
  ROUTE_RAILROAD  iMovement 20, iFlatMovement 6

For a 3-move unit, a revealed railroad step costs
min(20, 6 * 3) = 18; a revealed road step costs 30.

CvPlot::movementCost deliberately returns exactly MOVE_DENOMINATOR = 60 when
the destination plot has never been revealed and bAssumeRevealed is false,
which is how GroupStepMetric calls it.

GroupStepMetric::cost then applies:
  * unexplored destination from a revealed predecessor: modifier 3/3;
  * unexplored destination from an also-unrevealed predecessor, for a normal
    non-explorer: modifier 4/3.

Thus an unrevealed -> unrevealed step has the same movement-point effect on the
unit (60) but carries 80 units of movement weight in A* cost.

A fully constructible two-corridor local state can therefore have, from the
same full-move start S to the same unrevealed intermediate D:

  first/safe corridor:
    3 revealed railroad steps + first fog step + deep-fog step
    actual movement spent = 18 + 18 + 18 + 60 + 60 = 174
    final moves at D = 6
    weighted movement component = 18 + 18 + 18 + 60 + 80 = 194

  later/unsafe corridor:
    4 revealed road steps + first fog step
    actual movement spent = 30 + 30 + 30 + 30 + 60 = 180
    final moves at D = 0
    weighted movement component = 180

The small PATH_STEP/symmetry/no-route weights cannot erase a 14*1024 movement-
weight gap.

EXACT A* ORDERING CONSTRUCTION
------------------------------
The ordering can be made explicit rather than assumed. One non-wrapped local
coordinate arrangement is:

  S = (10,5)

  safe corridor:
    (10,5) -> (9,6) -> (8,7) -> (8,8) -> U=(9,9) -> D=(10,10)

  cheaper-later corridor:
    (10,5) -> (11,6) -> (12,7) -> (12,8) -> R=(11,9) -> D=(10,10)

  choose a safe final destination beyond D, e.g. toward (10,12), and make
  irrelevant cross-links impassable. U and R have equal Chebyshev heuristic
  distance to that destination.

With current PATH_* weights and current land heuristic weight 18, the exact
known/heuristic/total costs along the two designated corridors are:

  SAFE
    (9,6):  known  18445, h 110592, total 129037, moves 162
    (8,7):  known  36890, h  92160, total 129050, moves 144
    (8,8):  known  55332, h  73728, total 129060, moves 126
    U(9,9): known 116785, h  55296, total 172081, moves  66
    D:      known 198717, h  36864, total 235581, moves   6

  LATER CHEAPER ROUTE
    (11,6): known  30732, h 110592, total 141324, moves 150
    (12,7): known  61466, h  92160, total 153626, moves 120
    (12,8): known  92194, h  73728, total 165922, moves  90
    R(11,9):known 122924, h  55296, total 178220, moves  60
    D:      known 184376, h  36864, total 221240, moves   0

Therefore the generic A* processing order can be:
  * process the safe-prefix parents;
  * process U before R and initialize/open D with moves=6;
  * D is expensive enough to remain OPEN;
  * process R before D;
  * R offers a strictly cheaper path to D;
  * processChild reparents D and updatePathData changes D from moves=6 to
    moves=0;
  * the path-dependent canStepThrough predicate is NOT called after this
    reparent;
  * D therefore remains an OPEN node even though, if tested now, the same
    predicate would reject it whenever AI_isAnyPlotDanger(D) is true.

The 2024 repair's intended invariant is therefore still violated after a
cheaper-parent transition. This is no longer merely a theoretical possibility
from source shape; current SAS movement/XML values admit a concrete cost and
processing order.

DANGER ON AN UNREVEALED D IS REACHABLE FOR AI
----------------------------------------------
The fog-cost construction does not imply that AI_isAnyPlotDanger(D) must be
false. CvPlayerAI::AI_countDangerousUnits deliberately allows AI danger checks
against non-visible attacker plots through AI_cheatDangerVisibility, and that
helper deterministically returns a 50% hash result based on plot coordinates,
game turn and player ID. A hidden hostile unit adjacent to D can therefore make
AI_isAnyPlotDanger(D) true while D itself remains unrevealed and path-independent
canMoveThrough(D) remains legal.

This is an intentional AdvCiv AI information rule; the reparent defect does not
require inventing visibility semantics outside current code.

CURRENT CALLER CONSEQUENCE — IMPORTANTLY MORE CONSERVATIVE THAN "WORKER DIES"
-----------------------------------------------------------------------------
Do NOT overstate the consequence as "the Worker necessarily ends its turn on
D." MISSION_MOVE_TO execution uses CvSelectionGroup::getClearPathFinder and
recomputes a final path for each physical step. A later per-step recomputation
can reject the danger and prevent an actual unsafe landing.

There is nevertheless a current shipped consequence before physical movement.
SAS AI_nextCityToImprove does:

  GroupPathFinder& pf = CvSelectionGroup::pathFinder();
  pf.setGroup(*getGroup(), NO_MOVEMENT_FLAGS);

then, for every same-land-area city, asks AI_bestCityBuild for its best job and
uses:

  if (!pf.generatePath(*pPlot)) continue;
  int const pathTurns = pf.getPathTurns();
  score -= pathTurnPenalty * pathTurns;

There is no requirement that every intermediate plot be revealed. Thus a normal
AI Worker path evaluation can receive TRUE plus path-turn data from the
reparented tree even though the returned partial path contains a dangerous D
at which the path data says the group ends its turn and the path-dependent
validity predicate would now return false.

That can make a job count as pathable and affect cross-city target ranking. The
subsequent movement mission may then replan, change route or fail after movement
has begun. This is precisely a CvUnitAI-versus-CvSelectionGroup consistency
boundary; practical 4196's own commit message says the repair was intended to
avoid rare worker path failures/suboptimal paths and possible loop-like
inconsistencies.

So the strongest proven current consequence at this checkpoint is:
  * generatePath can return success / pathTurns for a partial-path tree that no
    longer satisfies GroupStepMetric's own path-dependent danger contract;
  * current worker AI consumes exactly that success/pathTurns for decisions;
  * final movement has a later revalidation layer, so actual unsafe end-turn is
    not yet claimed.

This is likely sufficient for a correctness finding, but F300 is deliberately
left unallocated until the remaining ancestry/repair-boundary and final-movement
review is complete.

ANCESTRY — STRONG SO FAR
------------------------
Base AdvCiv 1.14 has the same GroupStepMetric split and the same generic
processChild/forwardPropagate behavior. SAS's functional delta in
GroupPathFinder.cpp remains only its already-known null-group release guard, so
this reparent issue is not introduced by that SAS C++ file.

K-Mod's original custom pathfinder has the same generic shape:
  * pathValid_source is checked only when a child node is first put on the
    stack/opened;
  * pathValid_source contains the noncombat path-data danger check;
  * later cheaper-parent replacement calls pathAdd(... ASNC_PARENTADD_UP ...)
    and ForwardPropagate but does not call pathValid_source again.

Thus the reparent-invalidates-danger-state pattern is inherited from K-Mod.
AdvCiv practical 4196 substantially improved it by preventing a first
path-dependent rejection from permanently closing the plot, but did not add
revalidation on later reparent/propagation.

The practical-4196 source comment itself contains a remarkably relevant caveat
at the split point:

  the second predicate "merely invalidates the path we've taken so far" and
  "it's possible we might have to(?)" check further paths reaching the child.

Its commit message says the author had convinced himself the change was
"strictly more correct than before," while explicitly retaining concern about
path-data-dependent cost and CvUnitAI/CvSelectionGroup inconsistencies. The
current reparent construction lives exactly on that remaining boundary.

Civ4CE/BtS exposes the same path-data danger condition in its pathValid callback,
but the supplied CE source does not expose the EXE's internal FAStar reparent
algorithm. Do not claim the exact reparent defect for stock BtS solely from that
callback.

OPEN-BORDERS CACHE SEQUENCING — LEANING REJECTED
-------------------------------------------------
The WIP01 stale-CLOSED-node shape remains true in abstract, but normal producer
sequencing now strongly argues against a current finding.

Sequential turns:
  * AI unit movement occurs before that AI's end-of-turn diplomacy.
  * signing/canceling OB through AI diplomacy therefore occurs after the team's
    units have used their movement AI;
  * before the same group normally paths again, another group/player or the next
    game-turn cache reset replaces/resets the global finder state.

Simultaneous turns:
  * CvGame::doUpdateCacheOnTurn resets the global selection-group pathfinder at
    the game-turn boundary;
  * CvPlayer::doTurn (including AI diplomacy) happens before doTurnUnits;
  * traced AI_doTurnPre/AI_doDiplo paths do not populate the main reusable
    selection-group finder for that player's later unit group; where player-AI
    code needs temporary group paths, it uses a separate/clear finder.
  * the first actual unit-group caller therefore starts from a fresh or
    different-group cache and setGroup resets as needed.

Automatic deal processing also occurs after the game-turn cache reset. War
transitions have their own explicit reset. Human direct movement uses the
interface/final path layer in the ordinary case; automated human groups act in
their normal unit phase rather than repeatedly around a manual diplomacy change.

Manual automation-toggle / scripting sequences could manufacture same-group
reuse across an OB transition, but no ordinary shipped producer sequence has
survived. Unless a remaining edge disproves this, close it as a cache-theory
control rather than F300.

CURRENT CURSOR
--------------
Queue 054 OPEN.
F300 / provisional KI#623 still deliberately UNALLOCATED at this physical WIP.

Next:
  * finish final-movement / evaluator consequence classification for the
    danger-reparent invariant;
  * decide whether that current contract violation merits F300 and define the
    repair boundary without prescribing an unsafe one-line fix;
  * finish the remaining K-Mod/AdvCiv history classification;
  * close the Open-Borders control unless a real same-group transition appears;
  * then continue arithmetic/overflow, max-path and node-state saturation.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP03 CONTINUATION — QUEUE 054 / GROUPPATHFINDER.CPP
2026-08-29
===============================================================================

F300 / KI#623 — CONFIRMED
-------------------------
Title:
Reparented GroupPathFinder nodes are not revalidated after path-dependent
movement state changes, allowing danger-invalid AI noncombat paths to remain
accepted and scored.

Status after the WIP02 adversarial review: CONFIRMED.

WHY THIS NOW CROSSES THE FINDING THRESHOLD
------------------------------------------
All four required links are now proven in current shipped source rather than
inferred from a suspicious code shape:

1. Contract violation.
   StepMetricBase documents that the path-dependent canStepThrough(p,w) part
   must describe whether a step can be added to the current partial path w.
   GroupStepMetric's danger rule depends exactly on path state: path turns and
   remaining moves.

   KmodPathFinder::processChild checks that path-dependent predicate when a node
   is first initialized, but when an already-known node receives a cheaper
   parent it calls updatePathData after removing the old parent and DOES NOT
   call canStepThrough(plot,node) again. forwardPropagate can then change the
   same moves/path-turn data in descendants without revalidating them either.

2. Concrete current-SAS state.
   WIP02 gives an exact current-data counterexample using the live
   UNIT_NETHERLANDS_SWIFT_WORKER (3 moves), current road/railroad movement
   values, current fog movement cost and current unexplored-tile cost modifier.
   A node D can be opened through a path with 6 moves left (danger predicate
   accepts it), then reparented to a strictly cheaper path with 0 moves left
   (danger predicate would reject it) while remaining open. The A* ordering and
   costs were enumerated numerically rather than hand-waved.

3. Reachable danger and shipped consumer.
   AI danger can be true on the unrevealed construction through AdvCiv's
   deterministic cheat-danger visibility for hidden nearby hostile units.
   Current SAS AI_nextCityToImprove uses the reusable GroupPathFinder under
   NO_MOVEMENT_FLAGS for ordinary noncombat Workers, treats generatePath==true
   as pathable, consumes getPathTurns in cross-city job scoring, and does not
   require intermediate plots to be revealed. Thus the invalid tree changes a
   real shipped AI decision interface.

4. Ancestry/history corroboration.
   The generic cheaper-parent/ForwardPropagate shape is inherited from K-Mod.
   Base AdvCiv 1.14 still has it. AdvCiv practical 4181 added an explicit
   "Danger-averse unit failed to move?" diagnostic/skip workaround for fresh
   mission failures involving path-data danger, and practical 4196 then split
   path-independent from path-dependent danger so first-arrival rejection would
   no longer permanently close a node. Practical 4196's own source comment
   explicitly wondered whether further paths reaching the child might also need
   checking. Its commit message retained concern about path-data-dependent cost
   and CvUnitAI/CvSelectionGroup inconsistencies. The cheaper-reparent path is
   the remaining mutation boundary that 4196 did not cover.

PROVEN CONSEQUENCE / NON-CLAIMS
-------------------------------
Proven current consequence:
  * generatePath can report success and path-turn data for a parent tree whose
    current path-dependent GroupStepMetric danger predicate is false;
  * current Worker target evaluation consumes that result for pathability and
    scoring, so an actually danger-invalid route can affect selected work;
  * later mission execution can therefore have to choose a different route,
    fail/replan, or skip after the evaluator had treated the target as pathable.

Do NOT claim from this source proof alone:
  * that a Worker must actually end its turn on the dangerous plot;
  * that the Worker must die;
  * that every such evaluation visibly fails a mission.

CvSelectionGroup::groupPathTo uses the clear/final finder and movement missions
recalculate as physical steps execute. That later layer can catch the danger.
The defect is therefore a pathfinder/evaluator consistency and AI-targeting
correctness defect, with mission churn/wasted movement as a plausible downstream
manifestation, not a proven guaranteed death path.

SEVERITY / SCOPE
----------------
Recommended severity: low-to-moderate AI pathfinding correctness, rare-state
but deterministic once the required map/path/danger configuration occurs.

Exposure is not limited to explicit MOVE_AVOID_DANGER users: the path-dependent
danger rule applies to ordinary AI-controlled groups that cannot fight, so
Workers, Settlers, missionaries, executives and many Great People are in the
logical surface. The exact WIP02 reproducible cost inversion uses the current
Dutch 3-move Worker. SAS's movement additions can broaden the set of reachable
movement states, but the algorithmic root is inherited rather than introduced
by SAS GroupPathFinder.cpp.

REPAIR BOUNDARY — DO NOT APPLY A NAIVE ONE-LINE RECHECK
-------------------------------------------------------
A correct repair has to preserve the A* tree invariant, not merely add

    if (!canStepThrough(kChildPlot, kChild)) return;

AFTER mutating kChild.

Why that naive repair is unsafe/incomplete:
  * the old parent/path may remain valid. A proposed cheaper parent that produces
    danger-invalid path data should not destroy the old valid route;
  * processChild currently changes cost and removes the old-parent relationship
    before updatePathData, so post-mutation rejection would require rollback;
  * forwardPropagate can change moves/pathTurns for every descendant and can
    therefore invalidate their path-dependent predicates too. Checking only the
    direct child leaves the same invariant violation in grandchildren.

Robust repair families to evaluate during implementation review:
  A. Compute prospective child path data before committing the reparent, reject
     a path-dependent-invalid proposed parent while preserving the old tree,
     then recursively revalidate any descendant whose propagated path data
     changes; invalid descendants must be safely detached/reopened/re-discovered.
  B. Conservatively invalidate/re-discover the affected subtree whenever a
     reparent changes path-dependent data in a danger-sensitive metric.
  C. More fundamentally, make the path state that affects legality part of node
     identity (multiple states per plot). This is algorithmically cleanest but
     is a much larger change.

The current OpenList/NodeMap design does not provide a trivial generic subtree
rollback primitive, so implementation should be reviewed as an algorithmic
change. Prefer a correctness-first localized solution plus reproducible test
scenario over a superficially tiny patch.

ANCESTRY CLASSIFICATION
-----------------------
  K-Mod: root shape inherited. Original pathValid_source contains the path-data
         danger rule; cheaper-parent replacement uses pathAdd(PARENTADD_UP) and
         ForwardPropagate without rerunning pathValid_source.
  AdvCiv: practical 4196 repairs first-arrival closure by splitting the predicate
          but leaves reparent/propagation without path-dependent revalidation.
  SAS:    current GroupPathFinder.cpp functional delta from Base 1.14 is not the
          cause. Current SAS content provides a concrete 3-move-worker state and
          a current Worker evaluator consumer.
  BtS/CE: callback exposes a similar path-data danger rule, but supplied source
          does not expose the EXE FAStar reparent internals. Do not attribute the
          exact reparent defect to stock BtS without engine evidence.

NUMBERING AFTER PROMOTION
-------------------------
F300 / KI#623 CONFIRMED.
F301 / provisional KI#624 remains UNUSED.
Queue 054 remains OPEN pending saturation.

OPEN-BORDERS CONTROL
--------------------
No ordinary shipped same-group reuse sequence has emerged across an Open Borders
state change. Sequential/simultaneous turn ordering, game-turn pathfinder reset,
group switching and final-path separation all make the abstract stale-cache
shape hard to reach normally. Keep this as a documented control unless a new
producer sequence is proven; do not allocate F301 merely because setOpenBorders
lacks an explicit pathfinder reset.

NEXT SATURATION WORK
--------------------
  * finish arithmetic/overflow bounds under current maximum world size and live
    path-cost multipliers;
  * inspect max-path and OPEN/CLOSED/reparent state invariants, folding any
    path-data mutation consequence into F300 where it is the same root rather
    than multiplying findings;
  * finish the relevant K-Mod/AdvCiv pathfinder-history classification and
    verify current code retains the known direction/min-step/reset fixes;
  * close Open Borders as a control if no current same-group producer appears;
  * only then decide whether queue 054 closes with F300 as its sole finding or
    whether a genuinely independent F301 survives.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP04 CONTINUATION — QUEUE 054 / GROUPPATHFINDER.CPP
2026-08-29
===============================================================================

CORRECTION TO THE EARLY MAX-PATH PRODUCER HYPOTHESIS
----------------------------------------------------
The initially suspected CvGameInterface sitePath sequence (broad city-site
search followed by a smaller goody-hut range using the same local finder) is
NOT a valid current producer and must not be used as evidence.

The first path-generation phase requires pHeadSelectedUnit->isFound(), whereas
the later goody phase requires the same head unit to canFight(). Current Settler
content is noncombat, so the two phases cannot populate/reuse the same finder in
that way. This is a rejected candidate/control.

A stronger defect was found that needs no prior cache reuse at all.

F301 / KI#624 — CONFIRMED
-------------------------
Title:
A cheaper over-limit route can overwrite a valid bounded route in
KmodPathFinder, causing generatePath to return false even though a path within
its requested max-turn limit exists.

ROOT / CONTRACT FAILURE
-----------------------
StepMetricBase documents m_iMaxPath as a hard search bound:

    KmodPathFinder will only consider paths of this length or shorter.

and explicitly says that KmodPathFinder, not the path-dependent step checker,
handles that length limit.

The implementation enforces the bound only incompletely:

  * processNode filters OPEN *parents* by
        node.getPathLength() <= m_stepMetric.getMaxPath();
  * processChild may expand a parent whose path length is exactly maxPath;
  * GroupStepMetric::updatePathData increments the child's path turns when that
    parent has zero moves, so the child can become maxPath+1;
  * processChild has no max-path check on that newly-computed child state;
  * if the child is the destination, m_pEndNode is set regardless of its new
    path length;
  * an existing destination/node can then be reparented to a cheaper route that
    has MORE path turns, again without checking the bound;
  * only after the search terminates does generatePath test
        m_pEndNode->getPathLength() <= maxPath.

The one-node-per-plot representation therefore optimizes cost across both
in-bound and out-of-bound routes, even though an out-of-bound route is not a
legal candidate for a bounded search. A cheaper illegal state can replace and
destroy the only stored legal state for the same plot.

CONCRETE CURRENT-SAS REPRODUCTION CLASS
---------------------------------------
Current shipped data contains a direct normal caller/unit combination:

  * UNIT_SELF_PROPELLED_ARTILLERY has iMoves=2, bPillage=1 and
    DefaultUnitAI=UNITAI_ATTACK_CITY.
  * AI_attackCityMove always includes MOVE_AVOID_ENEMY_WEIGHT_2 in eMoveFlags.
  * when a nearby target city is too strong and at step distance <=2, it calls
        AI_choke(1, false, eMoveFlags)
  * AI_choke scans enemy worked plots and calls
        generatePath(plot, eFlags, true, &iPathTurns, iRange)
    with iRange=1.

A minimal ordinary local state can therefore use one 2-move self-propelled
artillery (120 movement points), all relevant plots revealed, no defenders on
the candidate worked tile, and an established war/war plan.

Use these local coordinates solely to make the cost accounting deterministic:

    S = (10,10)  start, own plot, no route
    A = (11,10)  own plot, no route
    B = (12,11)  own plot, railroad
    D = (12,10)  enemy worked plot, railroad

D may be worked by the nearby too-strong target city at e.g. (11,8), which is
within two steps of S and can work D, while B remains a normal contested-border
own plot. Configure no other enemy-owned tile adjacent to B; other alternate
corridors can simply be impassable/blocked. D itself has no visible defender,
which is exactly the kind of plot AI_choke evaluates.

Current route data:
  * ordinary flat movement step = 60 points;
  * railroad iMovement=20, iFlatMovement=6;
  * for a baseMoves=2 unit, railroad-to-railroad movement therefore costs
        min(20, 6*2) = 12 points.

Current land heuristic weight for baseMoves=2 is 12:
  * minimum route movement cost after tech modifiers = 20;
  * minimum flat route value = 6;
  * minimumStepCost(2) = min(20, 2*6) = 12.

With current GroupPathFinder weights, no combat defenders, no defensive bonus
on Self-Propelled Artillery, and MOVE_AVOID_ENEMY_WEIGHT_2, the two routes are:

VALID <=1-turn route S -> A -> D
  S -> A:
    60*1024 movement + step/symmetry/no-route terms = 61,451.
  A -> D:
    consumes the remaining 60 movement points;
    base/symmetry = 61,450;
    +9 territory;
    +1,400 no-defensive-bonus end-turn defence preference;
    +280 enemy-territory aggregate defence preference;
    subtotal 63,139;
    ending the turn on an owned war-plan target applies WEIGHT_2 => 126,278.
  Total valid known cost = 187,729.
  D has pathTurns=1 and is a valid result for iMaxPath=1.

CHEAPER >1-turn route S -> A -> B -> D
  S -> A = 61,451.
  A -> B:
    consumes the remaining 60 movement points and ends turn on B;
    before enemy-adjacency weighting = 62,852;
    B is own territory but adjacent to D, an enemy wartime plot, so
    WEIGHT_2 adjacency applies *4/3 => 83,802 (integer arithmetic).
  B known cost = 145,253.
  B is adjacent to D and its A* total is 145,253 + 12,288 heuristic
    = 157,541, still well below the already-found valid D cost 187,729.
  B is therefore processed.
  B -> D:
    B has zero moves, so updatePathData begins turn 2;
    railroad movement costs only 12 points, leaving 108;
    because moves remain, none of the end-turn defence/enemy-avoid weighting is
    applied;
    exact step cost = 12,298.
  New D known cost = 157,551, strictly cheaper than 187,729.
  processChild therefore reparents D from the valid S-A-D route to S-A-B-D and
  updatePathData changes D from pathTurns=1 to pathTurns=2.

At that point the sole stored D node is cheaper but illegal for maxPath=1.
processNode excludes it because pathTurns=2; no alternative valid D state was
preserved. The final generatePath check sees 2>1 and returns FALSE.

Therefore the bounded caller can receive "no path within one turn" even though
the pathfinder itself had already found S-A-D within one turn.

CURRENT SHIPPED CONSEQUENCE
---------------------------
AI_choke is a live current bounded evaluator. In the configuration above the
candidate enemy worked plot is skipped at the generatePath test even though it
is genuinely reachable within the requested turn. This can change attack-city
stack choking/positioning decisions.

The root is broader than AI_choke: many current AI routines pass finite
max-turn limits to the same pathfinder. The exact reproduction above is enough
to establish shipped consequence; do not claim every bounded caller is affected
without its own state proof.

ANCESTRY
--------
K-Mod:
  * practical 354 ("Wrote the K-Mod pathfinder, and enabled it") introduced the
    custom pathfinder/max-path design;
  * practical 355 enabled iMaxPath for the custom finder;
  * the original ProcessNode filters parents by m_iData2 <= iMaxPath but its
    child/reparent code never rejects a cheaper child state for exceeding the
    bound. It can therefore overwrite the destination exactly as above.
  * K-Mod also ships UNIT_MOBILE_ARTILLERY with 2 moves, UNITAI_ATTACK_CITY,
    pillaging enabled, MOVE_AVOID_ENEMY_WEIGHT_2 in attack-city movement, and
    the same AI_choke(1,false,...) call class.

Base AdvCiv 1.14:
  * retains the same generic max-path structure and the same 2-move Mobile
    Artillery / bounded attack-city caller class.
  * the later template/refactor work (including practical 2588) preserved this
    behavior rather than introducing the defect.

SAS:
  * inherits the algorithm. Self-Propelled Artillery is the current SAS
    counterpart with the same relevant 2-move attack-city/pillage role.
  * the only functional SAS-vs-Base edit in GroupPathFinder.cpp remains the
    already-present null-group guard; it is not causal.

BtS/Civ4CE:
  * supplied stock-style source uses the EXE FAStar pathfinder rather than this
    K-Mod custom implementation. Do not attribute the exact custom reparent bug
    to stock BtS/CE without engine evidence.

WHY THIS IS INDEPENDENT OF F300
-------------------------------
Both defects are exposed by cheaper-parent replacement, but they violate
separate contracts:

  F300: path-dependent legality (danger) can become false after path-state
        mutation and is not revalidated.
  F301: the generic hard path-length limit is not enforced when child/reparent
        state is committed, so an illegal >max state can replace a legal <=max
        state even when every step is otherwise valid.

F301 exists with no danger predicate at all. Keep separate numbering.

REPAIR BOUNDARY
---------------
A final `if (endNode.pathLength <= max)` cannot repair the root; that check is
already present and merely detects the corruption after the valid state has
been discarded.

At minimum, bounded legality must be checked prospectively BEFORE committing a
new/cheaper parent:
  * a new child whose candidate path length exceeds max must not become the
    stored goal/path state for this bounded search;
  * an existing valid node must not be reparented onto an over-limit cheaper
    path while destroying its old legal parent.

The generic design makes a robust patch subtler than a direct child guard:
forwardPropagate can change descendants' path lengths after an ancestor is
reparented. A descendant that was <=max can therefore become >max too. A safe
implementation needs either prospective subtree handling/rollback, conservative
subtree invalidation and rediscovery, or path state rich enough to retain
multiple cost/turn states for one plot. This overlaps the architectural pressure
seen in F300 but is a distinct max-bound invariant.

Do not prescribe a tiny patch until implementation review proves that it also
handles descendant propagation and preserves old still-valid bounded states.

NUMBERING / QUEUE STATE
-----------------------
F300 / KI#623 CONFIRMED — danger legality after reparent/propagation.
F301 / KI#624 CONFIRMED — max-path legal state overwritten by cheaper over-limit state.
F302 / provisional KI#625 UNUSED.
Queue 054 remains OPEN; progress remains 26/169 files complete.

ARITHMETIC / OVERFLOW SATURATION — CURRENT PARTIAL RESULT
--------------------------------------------------------
The arithmetic pass has not produced F302 so far.

Current XML/runtime bounds established:
  * largest raw unit iMoves = 8 (Destroyer);
  * current positive movement promotions are only +1 each;
  * current tech domain movement additions are +1 and total only a few points;
  * an intentionally conservative late naval stack gives roughly <=13 base
    moves before conversion by MOVE_DENOMINATOR=60 (~780 movement points);
  * SpeedBonuses are empty in current unit XML;
  * PATH_DAMAGE_WEIGHT=0 in the supplied BtS core defines and current SAS has no
    Python caller of the exposed unbounded plot-extra-cost mutator;
  * current largest SAS world size previously established is 99x70 = 6,930 plots.

Ordinary movement arithmetic is therefore far below signed-32-bit range.
MOVE_ATTACK_STACK and repeated enemy-adjacency weighting are the meaningful
large per-step terms and still require final worst-case bounding. Group-strength
terms can theoretically scale with arbitrarily huge unit stacks, so distinguish
"C++ has no mathematical hard cap" from a current reachable shipped overflow.
No overflow finding is allocated without a realistic/current producer and
consequence.

OPEN-BORDERS CONTROL
--------------------
Still no normal same-group stale-cache producer has survived. Keep rejected
unless later saturation discovers one.

NEXT CURSOR
-----------
  * finish signed-32-bit bounds for MOVE_ATTACK_STACK, defence/group-size and
    repeated adjacency multipliers;
  * finish OPEN/CLOSED and forward-propagation state saturation, folding max
    limit consequences into F301 and danger consequences into F300 rather than
    multiplying findings;
  * finish remaining K-Mod/AdvCiv history checks and stale-cache controls;
  * then decide whether queue 054 closes with F300+F301 only or whether a truly
    independent F302 survives.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP05 — post-WIP04 continuation checkpoint
Queue 054: CvGameCoreDLL/GroupPathFinder.cpp
Status at checkpoint creation: OPEN
Progress: 26 / 169 files complete
F300 / KI#623 CONFIRMED
F301 / KI#624 CONFIRMED
F302 / provisional KI#625 UNUSED — mixed-group iWorstMovesLeft candidate under proof
===============================================================================

POST-WIP04 SATURATION / CONTROLS
--------------------------------
1. Signed-int arithmetic has still not earned a finding. Ordinary movement is
   comfortably bounded by current XML. MOVE_ATTACK_STACK is the dominant large
   per-step term, but current reachable overflow still lacks a realistic producer
   and consequence. Keep "no saturation arithmetic" as technical debt unless a
   concrete shipped state proves otherwise.

2. The OPEN/CLOSED history pass strengthens F300 rather than creating a new root.
   If a previously processed/CLOSED node is reparented with changed moves/path
   data, forward propagation updates existing descendants but the parent is not
   reopened to reconsider adjacent children previously rejected by path-dependent
   danger. Fold this into F300's repair boundary: changed CLOSED state may require
   re-expansion, not merely direct-child revalidation.

3. The old canReuseInitialPathData warning about movement changes is not itself a
   current tech-bonus bug. A domain +move tech changes maxMoves(), therefore also
   movesLeft = maxMoves - movesUsed; the start-node reuse check sees the mismatch
   and resets.

4. Engineering/route-speed changes remain a stale-cache control, not a finding.
   Route movement cost can change without movesLeft changing, and processTech /
   changeRouteChange has no obvious pathfinder reset. However no normal current
   same-AI-group reuse sequence across that state transition has yet been proven.
   Human final movement uses the cleared execution finder. Do not allocate F302.

F302 CANDIDATE — iWorstMovesLeft overwritten by highest-cost unit
-----------------------------------------------------------------
Current GroupStepMetric::cost computes two logically distinct aggregates inside
FOR_EACH_UNIT_IN:

    iWorstMovesLeft = min(iWorstMovesLeft, iMovesLeft);

then, if this unit has the largest movement-cost contribution:

    if (iCost > iWorstCost)
    {
        iWorstCost = iCost;
        iWorstMovesLeft = iMovesLeft;
    }

The second assignment destroys the true minimum remaining movement whenever the
unit with the largest weighted movement cost is not the unit with the least
moves remaining. Yet the later `if (iWorstMovesLeft <= 0)` block is supposed to
mean "the GROUP ends its turn on kTo". updatePathData independently calculates
true group remaining moves and retains the minimum, so cost and path data can
classify the same node differently.

CURRENT-SAS NUMERIC STATE
-------------------------
Current UNIT_GERMAN_HUSSAR:
  * iMoves=3;
  * UNITCOMBAT_MOUNTED_RANGED;
  * DefaultUnitAI=UNITAI_ATTACK;
  * free PROMOTION_RETREAT1.
PROMOTION_RETREAT2 is available from Retreat I, and PROMOTION_MOBILITY_COST is
available from Retreat II to UNITCOMBAT_MOUNTED_RANGED; it supplies
`iMoveDiscountChange=1`.

On a terrain/feature step with nominal movement cost 3 and no route:
  * an ordinary 1-move unit pays its capped 60 points, has 0 left and movement
    weight 60*1024 = 61,440;
  * the 3-move Hussar with Mobility (Cost) reduces raw cost 3->2, pays 120 of
    180 points, has 60 left, and movement weight 120*1024 = 122,880.

If the exhausted slow member is encountered first, iWorstMovesLeft correctly
becomes 0. When the Hussar is then processed, its larger iCost wins and the code
incorrectly overwrites iWorstMovesLeft back to 60. The later end-turn block is
therefore skipped even though updatePathData correctly stores group moves=0.

ORDERING REACHABILITY — FIRST GATE PASSED
-----------------------------------------
This does NOT depend on arbitrary insertion order. CvSelectionGroup::addUnit
keeps m_units sorted descending by CvUnitAI::AI_groupFirstVal and then
AI_groupSecondVal. FOR_EACH_UNIT_IN walks that stored order from head to tail.

A normal 1-move siege unit assigned UNITAI_ATTACK_CITY with bombardRate>0 gets
AI_groupFirstVal() = 19. The German Hussar assigned UNITAI_ATTACK has withdrawal
and gets AI_groupFirstVal() = 14. Therefore the slow bombard siege unit is
naturally before the Hussar in the exact iteration order needed by the numeric
counterexample.

Current examples include 1-move Catapult/Trebuchet/Cannon-style attack-city
siege classes and the 3-move German Hussar. Still prove actual grouping behavior
rather than assuming any two compatible land units will normally share a group.

POTENTIAL CONSEQUENCE BOUNDARY
------------------------------
When the overwrite changes 0 -> positive, GroupStepMetric::cost skips the whole
end-turn section, including:
  * PATH_TERRITORY_WEIGHT;
  * feature/plot extra-cost damage weighting when enabled;
  * defense-position preferences;
  * enemy-territory aggregate defense penalties;
  * MOVE_AVOID_ENEMY_WEIGHT_2 / _3 ending-turn and adjacent-enemy multipliers.

This can therefore change route ordering substantially, not merely a tiny tie.
But F302 remains UNUSED until a normal current group/caller and actual route
choice/evaluation consequence are demonstrated, followed by ancestry comparison.

NEXT CURSOR
-----------
  * prove current AI grouping can naturally combine the ordered slow attack-city
    siege + promoted fast Hussar (or find an even simpler current pairing);
  * build a route pair where the wrong end-turn classification changes the chosen
    path or bounded evaluator result;
  * compare exact line/behavior in K-Mod and Base AdvCiv; determine whether SAS
    movement-content changes merely expose an inherited root or introduce it;
  * only then decide F302/KI#625.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP06 ADDENDUM — 2026-08-29
queue 054: CvGameCoreDLL/GroupPathFinder.cpp
F300 / KI#623 CONFIRMED
F301 / KI#624 CONFIRMED
F302 / KI#625 CONFIRMED in this addendum
F303 / provisional KI#626 UNUSED
progress remains 26/169; queue 054 remains OPEN
===============================================================================

CORRECTION TO WIP05 F302 CANDIDATE
----------------------------------
The WIP05 numeric description omitted an important precondition. In
GroupStepMetric::cost:

    int const iMaxMoves = (iCurrMoves > 0 ? iCurrMoves : pGroupUnit->maxMoves());

Therefore mixed units do NOT receive their individual maxMoves while the parent
node still has positive GROUP moves; they all use the same iCurrMoves then. The
0 -> positive overwrite demonstrated below occurs specifically when the parent
node has group moves == 0, i.e. on the first step of a newly simulated turn.
That state is ordinary on any multi-turn mixed-stack path. The candidate survives
with that narrower and correct statement.

F302 / KI#625 — CONFIRMED
-------------------------
Title: GroupStepMetric::cost can erase the group's true minimum moves-left and
skip end-turn danger/defense costs for mixed-mobility stacks

CURRENT ROOT
------------
Current SAS does this inside the per-unit cost loop:

    iWorstMovesLeft = std::min(iWorstMovesLeft, iMovesLeft);

    ...

    if (iCost > iWorstCost)
    {
        iWorstCost = iCost;
        iWorstMovesLeft = iMovesLeft;
    }

The first assignment has group semantics: `iWorstMovesLeft` becomes the minimum
remaining movement of ANY member, which is what determines whether the GROUP
must end its turn on kTo. The second assignment silently changes the meaning to
the remaining movement of whichever member happens to have the largest weighted
movement cost.

Those need not be the same member. When a slow member is exhausted but a faster
member has a larger movement-cost contribution and still has movement left, the
second assignment changes `iWorstMovesLeft` from 0 back to a positive value.
The later `if (iWorstMovesLeft <= 0)` then skips every cost that is supposed to
apply when the group ends the turn on kTo.

`updatePathData` does not make this mistake. In the non-uniform-cost branch it
reconstructs the current turn separately for every unit and stores the minimum
remaining movement. Thus the path node can correctly say `moves == 0` while the
cost function that created that step has incorrectly treated it as a non-end-turn
step.

CURRENT NORMAL MIXED-GROUP STATE
--------------------------------
A current German Hussar is a particularly clear shipped exposure:
  * UNIT_GERMAN_HUSSAR has iMoves=3, DefaultUnitAI=UNITAI_ATTACK,
    UNITCOMBAT_MOUNTED_RANGED and free PROMOTION_RETREAT1;
  * PROMOTION_RETREAT2 is normally reachable from Retreat I;
  * PROMOTION_MOBILITY_COST is normally reachable from Retreat II for
    UNITCOMBAT_MOUNTED_RANGED and has iMoveDiscountChange=1.

A normal one-move bombard siege member such as a Catapult can lead the same
city-assault selection group. The required iteration order is NOT synthetic:
`CvSelectionGroup::addUnit` sorts members by descending `AI_groupFirstVal` and
then `AI_groupSecondVal`, and FOR_EACH_UNIT_IN walks that stored order.
  * bombard-capable UNITAI_ATTACK_CITY -> groupFirst 19;
  * ordinary UNITAI_ATTACK German Hussar -> groupFirst 14 with withdrawal,
    otherwise 13.
So the slow siege member naturally precedes the Hussar.

Nor is the mixed group itself script-only. UNITAI_ATTACK movement normally calls
`AI_omniGroup(UNITAI_ATTACK_CITY, ...)` in the relevant war movement logic and
can merge into a city-assault stack; the attack-city head retains the higher
priority after the merge.

EXACT MOVEMENT DIVERGENCE AT A SIMULATED TURN BOUNDARY
-------------------------------------------------------
At a parent node whose stored group moves are 0, consider moving onto a revealed
jungle hill without a usable incoming route. Current movement denominator is 60.
The feature costs 2 and hills add 1, so raw terrain cost is 3.

One-move siege:
  base/max movement = 60;
  regular cost caps at baseMoves=1 -> 60;
  moves left = 0;
  movement weight = 60 * 1024 = 61,440.

Three-move Hussar with Mobility Cost:
  raw terrain cost 3 -> 2 after iMoveDiscountChange=1;
  movement cost = 120;
  max movement = 180;
  moves left = 60;
  movement weight = 120 * 1024 = 122,880.

Iteration therefore does:
  1. slow siege: `iWorstMovesLeft` becomes 0;
  2. Hussar: its larger `iCost` wins and the nested assignment overwrites
     `iWorstMovesLeft` back to 60.

The path node produced by updatePathData correctly stores group moves=0.
GroupStepMetric::cost nevertheless behaves as if the group still has movement.

EXACT CURRENT ROUTE-CHOICE INVERSION
------------------------------------
This can reverse the A* path choice, not merely omit a cosmetic preference.
A legal local corridor can be built from ordinary map states; all non-corridor
exits may simply be impassable peaks. Shifted coordinates used for exact current
AI symmetry weights:

  current source S = (11,10)
  common boundary P = (10,10)

  dangerous branch:
    D1=(9,9) jungle hill, own railroad, adjacent to one enemy-at-war owned plot
    D2=(8,8) railroad
    T =(7,9) railroad destination

  safe branch:
    A=(9,11) ordinary flat, no route
    B=(8,11) railroad
    C=(7,10) railroad
    T=(7,9)

The graph geometry has no unintended cross-edge among the two branches; other
exits can be peaks. S->P is one ordinary common step that exhausts the 1-move
siege and gives P stored group moves=0. Thus the bug is live on the next step.
All plots are revealed and this is non-exploration AI movement.

Current exact constants used:
  PATH_MOVEMENT_WEIGHT=1024
  PATH_STEP_WEIGHT=7
  PATH_STRAIGHT_WEIGHT=2
  PATH_NOROUTE_WEIGHT=2
  PATH_DEFENSE_WEIGHT=7
  railroad movement=20, flat movement=6
  both example units can fight; with no defensive bonus on the relevant tile,
  the two-unit end-turn defense term is 1,400.

For P->D1:
  buggy movement + exact symmetry = 122,891;
  because the overwrite leaves `iWorstMovesLeft=60`, the entire end-turn block
  is skipped.

With the overwrite removed, the true group minimum remains 0:
  122,891 + 1,400 defense = 124,291;
  D1 is adjacent to one enemy-at-war owned plot and
  MOVE_AVOID_ENEMY_WEIGHT_2 applies 4/3;
  exact integer result = 165,721.

The two following railroad steps cost 18,444 each (movement plus exact current
symmetry), giving:
  BUGGY dangerous branch P->D1->D2->T = 159,779
  CORRECT dangerous branch             = 202,609

Safe branch exact current costs:
  P->A ordinary end-turn step = 62,852
  A->B ordinary end-turn step = 62,850
  B->C railroad step          = 18,444
  C->T railroad step          = 18,442
  total safe branch           = 162,588

Therefore current buggy code chooses the dangerous branch:
  159,779 < 162,588.
With correct group end-turn accounting it chooses the safe branch instead:
  162,588 < 202,609.

The common S->P prefix costs 62,850 and does not affect the inversion:
  full BUGGY dangerous path = 222,629
  full safe path            = 225,438
  full CORRECT dangerous    = 265,459.

The corresponding path lengths are 3 turns for the buggy dangerous route and
4 turns for the safe route. `updatePathData` supplies those turn counts using the
true group movement state.

CURRENT SHIPPED DECISION CONSEQUENCE
------------------------------------
This does not require claiming that the stack blindly follows the stale route on
a later turn. Current ATTACK_CITY AI consumes the result as an evaluator before
movement:

  * AI_attackCityMove uses MOVE_AVOID_ENEMY_WEIGHT_2 consistently in eMoveFlags;
  * it calls AI_pickTargetCity(eMoveFlags, ...);
  * AI_pickTargetCity scans candidate enemy cities with `generatePath` and the
    returned `iPathTurns`;
  * iPathTurns affects several target filters/strength calculations and, most
    directly, city score is divided by `8 + SQR(iPathTurns)`.

Thus the exact route inversion above changes one candidate from the correct
4-turn travel estimate to a buggy 3-turn estimate. The final denominator changes
from 24 to 17, a ~41% relative scoring swing before considering the other
path-turn-dependent branches. Two otherwise close city candidates can therefore
be ranked differently solely because the cost function failed to recognize that
one mixed-stack member ends its turn on D1.

This is a current AI decision consequence. Later mission movement can recalculate
its route; no claim of guaranteed unit death or guaranteed entry onto D1 is
needed or justified.

ANCESTRY / ORIGIN
-----------------
Base AdvCiv 1.14 still contains the same contradictory assignment in
GroupPathFinder.cpp. Current K-Mod 1.46 contains the same root in pathCost in
CvGameCoreUtils.cpp.

The local K-Mod history makes the origin unusually clear:

  * K-Mod practical 237 changed the path-cost unit selector from the old
    `iCost < iWorstCost` / minimum-cost logic to `iCost > iWorstCost`, retaining
    the coupled assignments of `iWorstCost` and `iWorstMovesLeft`.
  * K-Mod practical 238 then refactored movement accounting and explicitly added
    `iWorstMovesLeft = std::min(iWorstMovesLeft, iMovesLeft)` before the
    max-iCost selector, but retained the older nested
    `iWorstMovesLeft = iMovesLeft` inside that selector.

Practical 238 is therefore the clearest point where the present contradiction
became explicit: calculate the true group minimum, then overwrite it with the
moves of the highest-cost unit.

Stock BtS/Civ4CE has an older, materially different pathCost selection scheme;
the exact current root should be classified as inherited K-Mod behavior rather
than a Firaxis/Civ4CE defect. SAS movement/unit changes make a concrete exposure
easy to construct but did not introduce the code root.

REPAIR BOUNDARY
---------------
Unlike F300/F301, this root appears locally repairable. The later end-turn block
needs the GROUP minimum, and that minimum has already been accumulated by:

    iWorstMovesLeft = std::min(iWorstMovesLeft, iMovesLeft);

`iWorstCost` independently needs the largest per-unit weighted movement cost.
There is no reason for selecting a new iWorstCost to change the already-computed
group minimum. The minimal candidate repair is therefore to remove only:

    iWorstMovesLeft = iMovesLeft;

from the `if (iCost > iWorstCost)` block.

Do not remove the preceding std::min accumulation. Current `iWorstMaxMoves` has
already been retired/commented out in AdvCiv, so no paired max-moves state needs
to be preserved in current SAS. A review/test should still compare path costs for
uniform groups, mixed 1-/3-move stacks on terrain and railroad, and AI avoid-enemy
routing before implementation is considered complete.

F302 DECISION
-------------
All promotion gates now pass:
  [x] exact source-level invariant violation;
  [x] current shipped unit/promotion/group state;
  [x] deterministic normal group iteration order;
  [x] normal AI grouping reachability;
  [x] exact route-cost inversion under current constants;
  [x] current AI consumer whose target score changes with the wrong path turns;
  [x] Base/K-Mod ancestry and local historical origin;
  [x] bounded repair boundary.

Promote as F302 / KI#625.

NEXT CURSOR
-----------
F303 / provisional KI#626 remains UNUSED.
Continue queue-054 saturation rather than searching for a quota:
  * finish arithmetic/overflow bounds and decide whether merely theoretical;
  * return to Engineering route-cost cache staleness only if a normal same-group
    producer/reuse sequence can be proven;
  * inspect remaining GroupStepMetric cost approximations for independent live
    consequences, folding mixed-stack end-turn consequences into F302;
  * finish OPEN/CLOSED, reset/invalidation, max-path and history inventory;
  * then decide whether queue 054 closes with F300-F302 or a truly independent
    F303 survives.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP07 ADDENDUM — 2026-08-29
queue 054: CvGameCoreDLL/GroupPathFinder.cpp
F300 / KI#623 CONFIRMED
F301 / KI#624 CONFIRMED
F302 / KI#625 CONFIRMED
F303 / KI#626 CONFIRMED in this addendum
F304 / provisional KI#627 UNUSED
progress remains 26/169; queue 054 remains OPEN
===============================================================================

F303 / KI#626 — CONFIRMED
-------------------------
Title: GroupStepMetric::cost uses the group's minimum moves as every unit's
remaining moves and can invent premature end-turn penalties for mixed-speed
stacks

INDEPENDENCE FROM F302
----------------------
F302 is the nested assignment that destroys the true minimum after it has been
computed. Removing that one line does NOT fix this finding.

F303 occurs earlier. Whenever the parent node has positive group movement,
GroupStepMetric::cost does:

    int const iMaxMoves = (iCurrMoves > 0 ? iCurrMoves : pGroupUnit->maxMoves());

`iCurrMoves` is only the movement remaining to the SLOWEST / limiting group
member. It is not the actual remaining movement of every unit. Thus a faster
member can be evaluated as if it has far fewer moves left than it really has.

Current `updatePathData` explicitly knows that this shortcut is wrong for mixed
movement costs. Its non-uniform branch reconstructs the current turn for each
unit separately and stores the actual group minimum. The path node can therefore
carry correct movement/path-turn data while the cost function uses fictitious
per-unit movement state for the same step.

SIMPLE CURRENT RAILROAD EXPOSURE
--------------------------------
No special promotion is needed. A normal mixed stack containing:
  * a 1-move bombard siege member (e.g. Catapult), and
  * current UNIT_GERMAN_HUSSAR with iMoves=3
is enough. Such ATTACK units can normally merge into an ATTACK_CITY stack, as
already proven in the F302 grouping pass.

On current Railroad:
  * one-move unit: flat route cost min(20, 6*1) = 6 movement points;
  * three-move Hussar: flat route cost min(20, 6*3) = 18 movement points.
With full movement they have 60 and 180 points respectively, so BOTH can move
exactly ten railroad tiles in one turn.

`updatePathData` reconstructs that correctly.

But after each step the node stores only the group minimum, which is controlled
by the one-move unit: 54, 48, 42, 36, 30, 24, 18, 12, 6, 0.
`cost` reuses each of those values as the Hussar's own `iMaxMoves`.
Consequences in current cost:
  * on rail step 8, parent group moves=18; Hussar is falsely treated as having
    only 18, pays 18 and is computed at 0 although it actually has 54 before the
    step and 36 afterward;
  * step 9 starts from group moves=12, so Hussar movement weight is further
    reduced to 12*1024 even though its real step contribution remains 18*1024;
  * step 10 starts from group moves=6 and similarly uses only 6*1024.

Hence step 8 and 9 are falsely classified as group end-turn steps; step 10 is the
real end of the slow unit's turn. This remains true after the F302 overwrite is
removed because the per-unit `iMovesLeft` inputs themselves are already wrong.

EXACT CURRENT ROUTE INVERSION
-----------------------------
A current, ordinary railroad corridor can turn that approximation into the
opposite route choice. Non-corridor exits may be impassable peaks, so no synthetic
API behavior is needed.

Coordinates (all corridor plots are own revealed railroad):

Direct 9-step route:
  S=(10,10)
  (10,9) -> (10,8) -> (9,7) -> (10,6) -> (11,6) -> (12,7) ->
  (13,8) -> D=(14,9) -> T=(15,10)

Safe 11-step detour:
  S=(10,10)
  (11,11) -> (10,12) -> (9,12) -> (8,12) -> (9,13) -> (10,13) ->
  (11,12) -> (12,12) -> (13,12) -> (14,11) -> T=(15,10)

The two internal corridors have no cross-adjacency. Let D, the eighth direct
rail tile, be adjacent to three enemy-at-war owned off-corridor plots (they can
be owned peaks). The safe false-end tiles are not adjacent to enemy territory.
T is an own staging city. All other setup is ordinary and revealed.

Use current `AI_moveToStagingCity` flags: MOVE_AVOID_ENEMY_WEIGHT_3.
Exact current AI symmetry weights are included below.

TRUE PER-UNIT MOVEMENT COSTING:
  direct 9 rails, no real end turn yet = 165,992;
  safe 11 rails, with real end turn on rail 10 = 204,272.
So the intended movement/end-turn logic strongly prefers the 9-step direct path.
It reaches T in path turn 1 with 6 group movement points left.

CURRENT BUGGY COSTING:
  direct exact edge costs =
    18,442 + 18,441 + 18,444 + 18,444 + 18,440 + 18,446 + 18,445 +
    66,973 + 13,702
    = 209,777.

The 66,973 step is the falsely exhausted eighth railroad tile after the normal
1,400 two-unit defense end-turn term and three successive x3/2 adjacent-enemy
weights. The ninth step is also falsely treated as an end turn and, because the
shared group minimum is only 12, its movement component is underweighted.

  safe exact current edge costs =
    18,444 + 18,444 + 18,441 + 18,440 + 18,444 + 18,440 + 18,444 +
    19,841 + 13,698 + 7,558 + 18,446
    = 188,640.

Thus current code chooses the 11-step detour:
  188,640 < 209,777,
while correct per-unit state chooses the 9-step direct route:
  165,992 < 204,272.

CURRENT SHIPPED AI CONSEQUENCE
------------------------------
`AI_moveToStagingCity` is a normal current ATTACK_CITY path. It:
  * scans own cities;
  * calls `generatePath(city, MOVE_AVOID_ENEMY_WEIGHT_3, ..., &iPathTurns)`;
  * divides city value by `5 + iPathTurns`;
  * stores `getPathEndTurnPlot()` for the selected city;
  * pushes a group move with the same MOVE_AVOID_ENEMY_WEIGHT_3 flags.

`AI_attackCityMove` reaches AI_moveToStagingCity through several ordinary paths,
including while waiting for joiners and later staging/retreat logic.

For the concrete corridor:
  * correct direct route is pathTurns=1 and reaches staging city T this turn;
  * buggy selected detour is pathTurns=2 because its tenth railroad step really
    exhausts the slow unit and step 11 starts a new turn;
  * staging-city score is therefore divided by 7 instead of 6;
  * getPathEndTurnPlot returns the detour's tenth tile instead of T;
  * the mission push uses the same flags/path-cost rule, so the group can spend
    the turn following that longer detour and stop short of a city it could have
    reached during the current turn.

This is a direct current AI routing and decision consequence, not merely a
slightly inaccurate internal cost estimate.

ANCESTRY / HISTORY
------------------
Current K-Mod 1.46 has the same shared-parent-moves pathCost calculation. Base
AdvCiv 1.14 inherits it. Current SAS did not introduce this root.

K-Mod practical 1017 is particularly revealing. Its title is:
  "Fixed a BtS pathfinding bug which caused inaccurate path turns."
It changes pathAdd/update-path-data specifically because the old shared group
movement shortcut is wrong for groups where one unit has more moves and a higher
movement cost, explicitly naming mixed 1-/2-move railroad groups as the obvious
example. It reconstructs each unit's moves through the current turn to fix path
turns.

But practical 1017 does not make the equivalent correction in pathCost. Thus the
current finder has accurate path data from the K-Mod fix while its step-cost
function still reasons from the older shared-minimum approximation. AdvCiv later
ported/refactored both pieces without reconciling that mismatch.

Stock BtS/Civ4CE uses the older pathCost/pathAdd family; its path-data behavior is
materially different. The current mismatch is best classified as an inherited
K-Mod incomplete-fix boundary rather than a SAS regression.

REPAIR BOUNDARY
---------------
This is not safely repaired by only changing an end-turn `if` or by F302's
one-line deletion. The cost function also underweights the fast unit's movement
component on later railroad steps (12*1024, 6*1024 in the example). It needs the
actual per-unit movement available at kFrom.

The information cannot in general be recovered from `iCurrMoves` alone; that node
field intentionally stores only the group's minimum. A robust repair therefore
needs one of:
  * reconstruct each unit's movement since the current turn boundary, sharing or
    refactoring the non-uniform logic already used by updatePathData;
  * cache sufficient per-unit/current-turn state in path nodes; or
  * restructure processChild so cost can evaluate against prospective exact path
    data without breaking reparent handling.

Be careful with the third option: for an existing node being considered through a
new parent, its currently stored path data belongs to the OLD parent until after
`iNewCost` wins. Simply reading kChild's existing moves from cost would therefore
be wrong for reparent candidates.

Performance matters because GroupStepMetric::cost is hot. A practical repair may
want a fast uniform-cost path and only reconstruct when group members' movement
costs/max movement can diverge, analogous to updatePathData's existing fast path.

F303 DECISION
-------------
All finding gates pass:
  [x] exact independent source invariant;
  [x] ordinary current mixed-speed group with no special promotion required;
  [x] exact railroad movement-state divergence;
  [x] exact current route-cost inversion;
  [x] current ATTACK_CITY staging caller with changed pathTurns/end-turn plot;
  [x] Base/K-Mod ancestry and the upstream incomplete-fix history;
  [x] repair boundary distinguished from F302.

Promote as F303 / KI#626.

NEXT CURSOR
-----------
F304 / provisional KI#627 remains UNUSED.
Continue queue 054 saturation:
  * treat any other shared-per-unit movement consequences as F303 unless a truly
    independent invariant is found;
  * finish signed-int cost/known-cost overflow reachability;
  * finish Engineering/route-change cache invalidation reachability;
  * finish reset/reuse/world-state and OPEN/CLOSED inventory;
  * inspect the remaining cost-function approximations and history commits;
  * only then decide whether queue 054 closes with F300-F303 or a real F304 exists.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP08 ADDENDUM — 2026-08-29
queue 054: CvGameCoreDLL/GroupPathFinder.cpp
F300 / KI#623 CONFIRMED
F301 / KI#624 CONFIRMED
F302 / KI#625 CONFIRMED
F303 / KI#626 CONFIRMED
F304 / provisional KI#627 UNUSED
progress remains 26/169; queue 054 remains OPEN
===============================================================================

POST-WIP07 SATURATION / CONTROL LEDGER
--------------------------------------
No new finding is allocated in this checkpoint. The following candidates were
challenged specifically to avoid manufacturing F304.

1. SIGNED-INT PATH-COST OVERFLOW — OPEN BUT CURRENTLY WEAK
----------------------------------------------------------
The ordinary movement terms are far below signed-int range. MOVE_ATTACK_STACK is
the dominant one-step term: at the current attack-ratio floor its raw combat-ratio
penalty is on the order of 2.4 million before the remaining step terms. Even eight
successive MOVE_AVOID_ENEMY_WEIGHT_3 adjacent-enemy multipliers leave a worst
single step on the order of 60 million, below INT_MAX.

Cumulative known-cost overflow is mathematically possible after many such extreme
steps, because the generic finder stores known/total costs in signed int and does
not saturate addition. That is not yet a current finding: the proof currently
requires a long succession of maximally hostile / defended enemy plots. The live
MOVE_ATTACK_STACK producers are being checked for iMaxPath bounds and practical
search topology before deciding whether this is reachable enough to matter.

2. ENGINEERING / ROUTE-SPEED CACHE INVALIDATION — CONTROL SO FAR
----------------------------------------------------------------
Engineering changes road movement cost through processTech -> changeRouteChange
without changing a group's current movesLeft value and without an obvious direct
GroupPathFinder reset. In isolation this is a real cache-generation blind spot:
canReuseInitialPathData watches start/group movement state rather than global
route-speed generation.

Two plausible same-turn triggers were tested and rejected:

* Goody-hut Engineering:
  Engineering can receive goody research in current SAS and can in principle be
  completed from the hut when sufficiently close to completion. However, the unit
  must first MOVE onto the hut. The next reusable group-path call therefore has a
  different start plot, which makes setGroup / initial-data reuse reset the node
  tree. Execution movement also uses the always-cleared alternate finder.

* Pillaging a route:
  AI pillaging can execute synchronously and return to AI_update while the group
  remains on the same plot, so this is a better world-mutation shape. But pillage
  consumes one full movement denominator. On the next reusable-path call the
  cached start-node moves no longer equal the group's actual moves, and
  canReuseInitialPathData resets the node tree.

Thus the abstract invalidation gap remains documented, but neither current trigger
produces a stale same-group/same-start/same-moves reuse sequence. No F304.

3. EXPLICIT HEURISTIC-WEIGHT REUSE — CURRENT-CALLER CONTROL
-----------------------------------------------------------
KmodPathFinder::setGroup does not compare iHeuristicWeight when deciding whether
cached nodes may remain, and generatePath only recomputes OPEN-node heuristics when
the destination changes. A hypothetical same-group/same-destination caller that
changes only the explicit heuristic weight could therefore retain stale heuristic
values.

Current call-site sweep does not expose that API weakness:
  * explicit non-default weights are used with freshly constructed/local or
    always-cleared finders and remain constant for that finder;
  * the reusable AI GroupPathFinder uses the default derived heuristic.

So this is generic future-mod/API debt, not a shipped F304.

4. setGroup MUTABLE-INPUT LEDGER
--------------------------------
The four important mutable dimensions have now been challenged independently:
  * group identity / start data — normal reset/reuse boundary works, including the
    current SAS KI#475 membership invalidations;
  * move flags — relevant semantic changes are either stripped because the metric
    does not consume them, trigger reset, or (MOVE_DECLARE_WAR special case) are
    safe under current OWN_EXCLUSIVE_RADIUS=0 configuration;
  * max path — NOT safe; this is already F301;
  * explicit heuristic weight — generic API weakness, but current callers safe as
    described above.

5. DESTINATION / CLOSED DEAD-END BEHAVIOR — INTENTIONAL CONTROL
---------------------------------------------------------------
A node that was CLOSED because it could not be used as an intermediate step may
later be attached when that same plot becomes the destination. This initially
looks suspicious, but it matches the interface split:
  * canStepThrough controls intermediate traversal;
  * isValidDest is freshly checked for the requested destination.
The destination may therefore legally be entered even when traversal beyond it is
forbidden. No finding.

6. DECLARATION-OF-WAR WORLD MUTATION — RESET PRESENT
----------------------------------------------------
Actual declaration of war explicitly resets the global selection-group pathfinder
before post-DOW pathing. Therefore the most important diplomacy transition that
changes enemy-territory legality/cost does not preserve stale reusable nodes.
The earlier Open-Borders invalidation theory remains an architectural control but
still lacks a normal same-group reuse sequence.

7. OPEN/CLOSED / FORWARD-PROPAGATION FOLLOW-UP
----------------------------------------------
The generic finder explicitly admits that forwardPropagate can produce a POSITIVE
cost delta in descendants even though the reparented node itself received a
cheaper parent. Path-history-dependent end-turn / defensive / territorial costs
can make a descendant's continuation worse under the new movement state.

This is being challenged as a possible deeper plot-only-state compression issue:
reparenting one plot discards its old movement/path-history state, and descendants
are recomputed only through the retained child tree rather than reopening every
alternative edge. For danger legality, this is already part of F300's repair
boundary. It should NOT receive F304 unless an independent current cost/result
failure can be demonstrated without relying on F300 or F303.

NEXT CURSOR
-----------
F304 / provisional KI#627 remains UNUSED.
Continue:
  * bound every live MOVE_ATTACK_STACK producer before closing overflow;
  * try to prove or reject an independent positive-forward-propagation / plot-state
    result failure using current GroupStepMetric values and a shipped caller;
  * finish remaining K-Mod/AdvCiv history and arithmetic/sentinel checks;
  * merge any shared-movement consequences into F302/F303 rather than duplicating;
  * only then decide queue-054 saturation/closure.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP09 ADDENDUM — 2026-08-29
queue 054: CvGameCoreDLL/GroupPathFinder.cpp
F300 / KI#623 CONFIRMED
F301 / KI#624 CONFIRMED
F302 / KI#625 CONFIRMED
F303 / KI#626 CONFIRMED
F304 / KI#627 CONFIRMED in this addendum
F305 / provisional KI#628 UNUSED
progress remains 26/169; queue 054 remains OPEN
===============================================================================

F304 / KI#627 — CONFIRMED
-------------------------
Title: MOVE_DECLARE_WAR route simulation ignores only the destination endpoint,
so pre-war AI pathing can count a future-enemy route when moving OUT of enemy
territory and underestimate invasion travel time

ROOT / EXACT SOURCE DEFECT
--------------------------
AdvCiv practical 4277 (`aa6bef8e31`, title "AI war-declaring moves anticipate
loss of Open Borders") added `bIgnoreRoutes` to GroupStepMetric::cost and
updatePathData so that a path computed before a declaration of war would not rely
on the prospective enemy's roads.

Current SAS / Base AdvCiv cost logic decides this only from `kTo`:

    TeamTypes const eToPlotTeam = kTo.getTeam();
    bool const bIgnoreRoutes = ((eFlags & MOVE_DECLARE_WAR) &&
            eToPlotTeam != NO_TEAM &&
            GET_TEAM(eTeam).AI_isSneakAttackReady(eToPlotTeam));

and then passes that single bool to:

    kTo.movementCost(*pGroupUnit, kFrom, false, bIgnoreRoutes)

The same destination-only rule is used by updatePathData, both in the uniform
cost path and in its per-unit reconstruction loop.

But CvPlot::movementCost can use a route only when BOTH endpoints have a valid
route:

    if (kFrom.isValidRoute(&kUnit, ...) &&
        isValidRoute(&kUnit, ...) &&
        ... && !bIgnoresRoute)

Before war, a prospective enemy's route can still be valid through Open Borders.
Therefore a step whose FROM plot belongs to the future war target but whose TO
plot belongs to a friendly / neutral-access team is not ignored by advc.001t.
The route segment is counted cheaply even though the FROM endpoint becomes an
enemy route immediately after the very DOW that MOVE_DECLARE_WAR is simulating.

This is directional: target-owned kTo is handled; target-owned kFrom is not.

SIMPLE CURRENT ONE-MOVE REPRODUCTION
------------------------------------
No SAS-only unit or unusual promotion is needed. Current Catapult, Trebuchet and
Cannon are 1-move UNITAI_ATTACK_CITY defaults, and ordinary attack-city stacks
can consist entirely of 1-move non-enemy-route land units.

Use a normal revealed road corridor:

    S -> E -> N -> C

where:
  * E is owned by prospective war target team A and has a Road;
  * N is a routed plot owned by us or a third party whose route is currently
    usable to us;
  * C is the target city of team A;
  * the team is sneak-attack ready against A;
  * the path is evaluated before war with MOVE_DECLARE_WAR;
  * no unit in the group has isEnemyRoute().

Current Road movement is 30 movement points before Engineering; one base land
move is 60 movement points.

PRE-DOW PATHFINDER ESTIMATE:
  1. S -> E:
     kTo is target-owned A, so bIgnoreRoutes=true. Ordinary movement consumes the
     full 60. Stored group moves become 0. Path turn 1 ends at E.

  2. E -> N:
     kTo is not A, so bIgnoreRoutes=false. Before war, E's A-owned route is still
     valid under current diplomatic access and N's route is valid too. movementCost
     therefore uses the Road cost 30. A new simulated turn starts with 60 and
     leaves 30 movement.

  3. N -> C:
     kTo is target-owned A, so bIgnoreRoutes=true. Ordinary movement cost is 60;
     the remaining 30 are exhausted/clamped to 0, but the step still belongs to
     the same simulated turn. Pathfinder result: C is reached in PATH TURN 2.

POST-DOW REAL MOVEMENT:
  * AI_considerPathDOW declares war before the group executes the invasion path;
  * after war, E is enemy-owned;
  * for ordinary units `CvPlot::isValidRoute` now rejects E's route;
  * E -> N therefore costs the full 60 and consumes all of turn 2;
  * N -> C cannot occur until turn 3.

Thus the exact pre-war route evaluation says 2 turns for a path that becomes
3 turns as soon as its predicted DOW actually occurs.

This is not merely a different aesthetic path preference. It directly violates
the stated purpose of practical 4277: anticipating the loss of Open Borders in
war-declaring movement.

CURRENT SHIPPED PRODUCER
------------------------
CvUnitAI::AI_attackCityMove builds:

    MOVE_AVOID_ENEMY_WEIGHT_2 |
    (bReadyToAttack ? (MOVE_ATTACK_STACK | MOVE_DECLARE_WAR) : 0)

for a ready city-assault stack and passes those flags to AI_pickTargetCity.
AI_pickTargetCity considers players whose teams satisfy `AI_mayAttack`, including
prospective targets before the actual DOW, and calls generatePath with those
flags.

Therefore the exact pre-war state above is a normal current evaluator path, not a
script/force-only call.

CURRENT DECISION CONSEQUENCE
----------------------------
AI_pickTargetCity consumes `iPathTurns` repeatedly:
  * AI_cityTargetStrengthByPath(..., iPathTurns);
  * visible-defense radius/logic changes with path time;
  * the `iPathTurns > 2` enough-offence-en-route branch changes exactly across
    this 2 -> 3 error;
  * most directly, final city target value is divided by:

        8 + SQR(iPathTurns)

A false 2-turn estimate uses denominator 12. The real 3-turn post-DOW route uses
17. Holding the preceding factors equal, the bad estimate therefore values that
city by 17/12 ~= 1.417 times the intended distance-adjusted value: about 41.7%
higher.

This is enough to change which prospective enemy city a ready assault stack
selects.

POST-DOW REPATH DOES NOT REPAIR TARGET SELECTION
------------------------------------------------
AI_goToTargetCity later calls AI_considerPathDOW. If that declares war, the code
explicitly regenerates the movement path after the DOW but comments:

    "but don't bother recalculating the best destination"

So execution can discover the slower post-war route only AFTER AI_pickTargetCity
has already ranked/selected the target using the underestimated pre-war path
turns. Repathing movement does not undo the target-selection consequence.

ANCESTRY / HISTORY
------------------
K-Mod's generic pathfinder does not contain this anticipatory enemy-route
suppression at all; it has the broader older pre-DOW route-assumption behavior.

AdvCiv practical 4277 (`aa6bef8e31`, 2025-01-10 in the reachable history) added:
  * the bIgnoreRoutes parameter to CvPlot::movementCost;
  * the destination-team-only route suppression in GroupStepMetric::cost;
  * matching destination-team-only suppression in updatePathData;
  * post-DOW path regeneration changes in AI_goToTargetCity.

Base AdvCiv 1.14 retains that exact destination-only implementation. Current SAS
inherits it. The precise F304 root is therefore best classified as an incomplete
Base AdvCiv repair of the older K-Mod behavior, not a SAS regression.

SAS-SPECIFIC REPAIR AMPLIFIER: `isEnemyRoute`
----------------------------------------------
Practical 4277 left this source comment:

    "Not bothering with CvUnit::isEnemyRoute. AI won't have a DoW group of those."

That assumption is no longer safe in current SAS.

Current `PROMOTION_LOGISTICS`:
  * has `<bEnemyRoute>1`;
  * requires only Combat I OR City Raider I;
  * has TechPrereq TECH_THE_WHEEL;
  * is available to UNITCOMBAT_GUN (among other combat classes).

Current Musketman, for example, is UNITCOMBAT_GUN and explicitly allows
UNITAI_ATTACK_CITY. Current promotion AI valuation also gives an enemy-route
promotion +20 value for UNITAI_ATTACK and UNITAI_ATTACK_CITY.

CvPlot::isValidRoute itself says:

    if (!isOwned() || pUnit->isEnemyRoute())
        return true;

Thus current SAS really can produce prospective-DOW attack-city units whose enemy
route remains usable after war.

REPAIR BOUNDARY
---------------
A naive patch such as:

    ignore routes if futureEnemy(kFrom) || futureEnemy(kTo)

would fix the ordinary-unit underestimation above, but it would create the
opposite error for Logistics / other `isEnemyRoute()` units that legitimately can
use those routes after war.

The robust semantic question is per UNIT and per ENDPOINT:

  "Would this unit be able to use the route on BOTH kFrom and kTo after the
   prospective MOVE_DECLARE_WAR diplomacy transition?"

Both GroupStepMetric::cost and updatePathData must use the same simulated
post-DOW rule. In particular, updatePathData's non-uniform per-unit reconstruction
must not use one group-global ignore bool if units in a mixed stack differ in
`isEnemyRoute()` capability.

A repair should preferably centralize the predicted-post-DOW route-validity rule
so path cost and movement/path-turn reconstruction cannot diverge. It should test:
  * ordinary non-enemy-route units crossing A -> friendly/third-party routes;
  * ordinary entry into A territory;
  * Logistics/isEnemyRoute units;
  * mixed groups where only some members have enemy-route access;
  * one-move and multi-move groups;
  * post-DOW regenerated path turns against pre-DOW MOVE_DECLARE_WAR estimates.

F304 DECISION
-------------
All promotion gates now pass:
  [x] exact endpoint-asymmetric source defect;
  [x] ordinary current 1-move attack-city stack state;
  [x] exact 2-turn pre-DOW vs 3-turn post-DOW reproduction;
  [x] current MOVE_DECLARE_WAR producer before target selection;
  [x] direct current target-value/path-strength consequence;
  [x] post-DOW movement repath shown not to recompute target selection;
  [x] precise K-Mod -> AdvCiv practical 4277 -> Base 1.14 -> SAS ancestry;
  [x] repair boundary checked against current SAS Logistics/isEnemyRoute.

Promote as F304 / KI#627.

POST-WIP08 SATURATION CONTROLS / CORRECTIONS
--------------------------------------------
1. Signed-int path-cost overflow remains UNNUMBERED.
   One extreme MOVE_ATTACK_STACK step remains below INT_MAX. Cumulative overflow
   lacks saturation protection in principle, and unbounded target-city searches
   exist, but a current construction still requires pathological chains of
   maximally overdefended / enemy-adjacent plots or absurd group-strength totals.
   That is insufficient for F305 under the current audit standard.

2. Positive forwardPropagate / plot-only path-state compression remains bounded
   as known upstream debt unless a sharper independent current failure is found.
   K-Mod source already acknowledges that a cheaper reparent can make descendant
   continuation cost increase; current AdvCiv documentation likewise records
   remaining path-data-dependent / cache-order suboptimality. Danger legality is
   already inside F300's repair boundary; hard max-path overwrite is F301; mixed
   movement cost errors are F302/F303. Do not duplicate these as F305.

3. Engineering route-speed stale-cache theory remains a control. Goody completion
   changes the start plot before reuse; pillaging changes moves-left before reuse.
   Neither preserves the required same-group/same-start/same-moves stale tree.

4. Explicit heuristic-weight reuse remains generic API debt only. Current reusable
   AI finder calls do not change only the heuristic weight for the same cached
   group/destination.

NEXT CURSOR
-----------
F305 / provisional KI#628 remains UNUSED.
Continue queue-054 saturation:
  * finish every remaining MAX_INT / sentinel / arithmetic edge in
    GroupPathFinder.cpp and KmodPathFinder implementation;
  * finish modern history sweep after practical 4277 and check for any later
    attempted repair of route simulation / reparent / max-path behavior;
  * inspect remaining assumptions/comments in GroupStepMetric::cost,
    updatePathData, setGroup and generatePath;
  * merge consequences into F300-F304 wherever they share those roots;
  * if nothing independent survives, close queue 054 with F305 unused and advance
    to the next priority queue file.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP11 ADDENDUM -- QUEUE 054 `GroupPathFinder.cpp`
F305 / KI#628 CONFIRMED -- CORRECTED PRODUCER/DESTINATION PROOF
===============================================================================

CORRECTION TO THE SUPERSEDED WIP10 PROOF
----------------------------------------
WIP10 used an unrevealed sea destination D when describing AI_explore as the
current producer. That destination shape is NOT valid for CvUnitAI::AI_explore:
AI_plotValid on DOMAIN_SEA calls CvUnit::isRevealedValidDomain, which returns
false for an unrevealed plot. Therefore the WIP10 producer wording was too broad.

Do not use WIP10's unrevealed-D reproduction as the durable F305 proof.

The underlying heuristic defect survives without that assumption. The corrected
current producer uses a REVEALED sea destination at the explored/fog boundary and
unrevealed intermediate water on the cheaper alternative route. The full exact
A* failure remains and is documented below.

F305 / KI#628 -- SEA-EXPLORER HEURISTIC OVERSTATES DISCOUNTED FOG STEPS,
SO A* CAN TERMINATE ON A MORE EXPENSIVE REVEALED-WATER PATH
-------------------------------------------------------------------------------
STATUS: CONFIRMED
CLASSIFICATION: inherited K-Mod pathfinder defect; present in Base AdvCiv 1.14
and current SAS. K-Mod practical 1188 made the pre-existing full-denominator sea
heuristic inadmissible when it introduced the 2/3 exploration discount.

ROOT
----
GroupPathFinder::setGroup chooses the default sea heuristic weight as one full
movement denominator:

    if (kGroup.getDomainType() == DOMAIN_SEA)
        iHeuristicWeight = GC.getMOVE_DENOMINATOR(); // 60

GroupStepMetric::heuristicCost therefore estimates:

    stepDistance * PATH_MOVEMENT_WEIGHT * 60

or 61,440 per remaining geometric step at PATH_MOVEMENT_WEIGHT=1024.

GroupStepMetric::cost, however, applies K-Mod's exploration modifier to every
step whose DESTINATION is unrevealed when the group is AUTOMATE_EXPLORE or an AI
UNITAI_EXPLORE / UNITAI_EXPLORE_SEA group:

    iExploreModifier = 2; // thirds
    ...
    iCost = (iCost * iExploreModifier) / 3;

CvPlot::movementCost deliberately returns one movement denominator (60) for an
unrevealed destination when the pathfinder does not assume revelation. Therefore
an ordinary sea-explorer step into fog has movement component:

    60 * 1024 * 2 / 3 = 40,960

plus small positive step/symmetry/no-route terms. The heuristic still assumes
61,440 for the same geometric step. It overestimates future path cost by roughly
50% on such steps and is inadmissible.

KmodPathFinder::processNode relies on admissibility for early stopping. Once the
destination has a finite known cost it processes only OPEN nodes satisfying:

    node.m_iKnownCost + node.m_iHeuristicCost < destination.m_iKnownCost

An overestimated heuristic can therefore hide a cheaper path behind an OPEN node
and cause the search to terminate on a more expensive route.

CURRENT LIVE SEA-EXPLORER CONTENT
---------------------------------
Current SAS explicitly permits UNITAI_EXPLORE_SEA for:
  * UNIT_GALLEY -- 3 moves;
  * UNIT_GALLEON -- 4 moves;
  * UNIT_TRIREME -- 3 moves;
  * UNIT_FRIGATE -- 5 moves;
  * UNIT_DESTROYER -- 8 moves;
  * UNIT_ATTACK_SUBMARINE -- 6 moves.

Thus the discounted cost is live in normal AI naval exploration.

CORRECTED EXACT CURRENT GALLEY COUNTEREXAMPLE
---------------------------------------------
Use one ordinary unpromoted 3-move Galley with UNITAI_EXPLORE_SEA. No enemies,
features, routes or special movement modifiers are required. Use ordinary
zero-defence unowned water.

Choose start S=(10,20) and REVEALED water destination D=(40,20) on a sufficiently
wide local region. D is an exploration-edge plot: at least one adjacent detour
water tile remains unrevealed, so AI_explore can give D exploration value even
though D itself is revealed.

Provide two cardinal water corridors:

Direct corridor:
  * 30 eastward water steps along y=20;
  * every direct tile, including D, is revealed.

Detour corridor:
  * S -> (10,19) -> (10,18);
  * east along y=18 to (40,18);
  * then (40,19) -> D;
  * 34 total steps;
  * the first 15 detour tiles are revealed;
  * the deeper middle/far detour tiles are unrevealed;
  * D itself is revealed.

All steps are cardinal, so the sea-isthmus diagonal restriction is irrelevant.
The two horizontal corridors are two rows apart. The exact simulation also
included their normal diagonal adjacency at the vertical arms; those extra
neighbors do not rescue the search.

The first unrevealed detour tile is (24,18), Chebyshev distance 14 from S. A
3-move sea unit has:

    AI_searchRange(4) = 4 * baseMoves = 12

so the local region through distance 13 can already be explored and
AI_exploreSeaMove's earlier AI_exploreRange(4) pass can legitimately find nothing
new before the later map-wide AI_explore call. This is compatible with normal
current visibility; the proof does not require fog adjacent to the Galley.

Exact current GroupStepMetric totals are:

    direct 30-step fully-revealed corridor = 1,857,620
    detour 34-step partly-fog corridor      = 1,727,738

The longer detour is cheaper by 129,882 according to the pathfinder's OWN metric.

A* ORDER / EARLY-STOP FAILURE
-----------------------------
The first detour node is revealed, with:

    known g     = 61,450
    heuristic h = 30 * 61,440 = 1,843,200
    total f     = 1,904,650

The direct corridor OPEN nodes stay below that. The largest f-cost of a direct
node before D is only 1,856,200.

The custom pathfinder therefore expands the full direct corridor first. It then
discovers D through that route at known cost 1,857,620. At the next processNode,
the destination cost becomes the stopping ceiling. The first detour node is still
OPEN at f=1,904,650, which is above the ceiling, so the search terminates without
expanding any detour node.

A direct simulation of current KmodPathFinder processNode/processChild behavior on
this exact corridor graph produced:

    returned destination known cost = 1,857,620
    returned route length           = 30 steps
    direct nodes processed          = 30
    detour nodes processed          = 0
    first detour node state         = OPEN

Yet independently evaluating the valid detour with the same current cost function
gives 1,727,738.

This is a direct A* optimality failure, not tie-breaking and not merely a different
path-turn preference.

CURRENT PRODUCER
----------------
CvUnitAI::AI_exploreSeaMove is the normal move routine for UNITAI_EXPLORE_SEA.
It tries AI_exploreRange(4) and later AI_explore.

AI_explore only path-targets sea plots accepted by AI_plotValid; for DOMAIN_SEA
that means a revealed-valid-domain destination. The corrected D satisfies that.
D also has exploration value because an adjacent water plot remains unrevealed.
The ordinary 1-in-4 sampling gate for otherwise non-goody explore candidates can
therefore cause AI_explore to call generatePath(D, AI_exploreFlags(), ...).

For an AI UNITAI_EXPLORE_SEA head, GroupStepMetric::cost automatically applies the
2/3 modifier on the unrevealed intermediate detour steps. Thus the corrected
revealed-D state is a current shipped producer of the faulty A* comparison.

CURRENT EXECUTION CONSEQUENCE
-----------------------------
If D is selected as the exploration target, AI_explore queues MISSION_MOVE_TO D.
CvSelectionGroup::groupPathTo does not trust/reuse the evaluator tree; it obtains
the CLEARED alternate GroupPathFinder, sets the same group/flags, and generates a
fresh execution path.

That fresh finder receives the same default sea heuristic and reproduces the same
optimality failure. At the current state it therefore chooses the 1,857,620
revealed corridor instead of the 1,727,738 partly-fog corridor that
GroupStepMetric itself says is preferable.

Subsequent movement can reveal new tiles and trigger later replanning, but the
current path/next-step decision has already been made from a nonminimum route. In
addition, AI_explore uses the path turns returned by this nonminimum tree in its
target-value denominator, so the defect can affect target ranking as well as route
selection.

ANCESTRY / HISTORY
------------------
K-Mod practical 366 (`f6aac75c78`) introduced XML-derived admissible heuristic
weights. Sea groups retained one full movement denominator:

    iHeuristicWeight = GC.getMOVE_DENOMINATOR();

with an assumption only about no sea roads / no promotions reducing sea movement
cost. At that point K-Mod had not yet added this exploration discount.

K-Mod practical 1188 (`caebff59e1`), titled:

    "Adjusted pathfinding cost for exploring units."

later introduced `iExploreModifier = 2` for unrevealed exploration steps and
multiplied path movement cost by it. It simultaneously standardized unrevealed
CvPlot movement cost to one denominator so pathCost could apply the weighting.
It did NOT lower/rederive the already-existing sea heuristic.

Current K-Mod retains both pieces. Base AdvCiv 1.14 and current SAS retain the same
combination. Therefore practical 1188 is the precise inherited root: a legitimate
new cost reduction broke the heuristic lower-bound invariant.

The current generic StepMetricBase comment explicitly warns that the custom
pathfinder may not cope with an inadmissible heuristic. F305 provides an exact
current case where early stopping indeed returns a nonminimum path.

REPAIR BOUNDARY
---------------
Under current SAS rules there are no sea routes and no promotion that reduces raw
water movement cost. A sea-explorer fog step can therefore have movement weight
as low as:

    60 * 2 / 3 = 40

before positive small weights. The default heuristic for an exploring sea group
must be <= 40 per geometric step to preserve admissibility.

Safe approaches include:
  * conservative/simple: use 40 as the default sea heuristic weight for ALL sea
    groups. This sacrifices some search efficiency for non-explorers but avoids
    mode-transition/cache complications;
  * role-sensitive: use 40 for AUTOMATE_EXPLORE / AI UNITAI_EXPLORE_SEA and 60
    otherwise, but then changing the derived heuristic weight on a reused finder
    must force reset/recalculation. Queue-054 saturation already proved that a
    same-destination generatePath call does not automatically recompute cached OPEN
    heuristics merely because m_stepMetric receives a different weight.

Any future sea route or sea movement-cost discount must be included in the lower
bound as well.

F305 DECISION
-------------
All promotion gates pass after the destination correction:
  [x] exact sea heuristic-overestimate invariant violation;
  [x] live current UNITAI_EXPLORE_SEA units;
  [x] realistic revealed sea destination accepted by AI_plotValid;
  [x] nearest fog placed beyond the earlier AI_exploreRange(4) search boundary;
  [x] exact all-cardinal current cost counterexample;
  [x] direct simulation of current A* returning the more expensive path;
  [x] current AI_explore producer;
  [x] fresh execution finder uses the same faulty heuristic;
  [x] K-Mod practical 366 -> 1188 ancestry;
  [x] Base AdvCiv 1.14 and current SAS retain the defect;
  [x] repair boundary checked against heuristic-weight cache behavior.

F305 / KI#628 remains CONFIRMED.

POST-F305 SATURATION CONTROLS
-----------------------------
1. Land exploration does not share this current lower-bound failure. The land
   heuristic uses the globally conservative route-derived minimum step cost
   (railroad-scale), far below a 2/3 unrevealed normal-terrain movement step.

2. AdvCiv's OWN_EXCLUSIVE_RADIUS flip modifier is another cost-reducing modifier,
   but the feature is disabled in current SAS. Its revealed-tile branch is also
   mutually exclusive with the unrevealed-explorer branch. No current F306 from
   that feature.

3. GroupPathFinder::getPathEndTurnPlot is release-unsafe if called without a valid
   m_pEndNode because it dereferences before its later null assertion. Current
   shipped C++ callers sampled in this saturation pass are guarded by successful
   generatePath paths, and current shipped Python contains no use of the exposed
   wrapper. Keep as API robustness debt unless a current unguarded producer is
   proven.

NEXT CURSOR
-----------
F306 / provisional KI#629 remains UNUSED.
Queue 054 remains OPEN at 26 / 169 files complete.

Continue:
  * resolve MOVE_SAFE_TERRITORY / MOVE_NO_ENEMY_TERRITORY destination semantics
    against current callers before treating the destination exception as a bug;
  * complete remaining MAX_INT/sentinel and accessor-call sweep;
  * finish remaining historical assumptions/comments and compare current Base/
    K-Mod behavior;
  * fold duplicate consequences into F300-F305;
  * if no independent root survives, close queue 054 with F306 unused.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP12 — QUEUE 054 GROUPPATHFINDER.CPP — POST-F305 SATURATION CHECKPOINT
===============================================================================
Checkpoint status:
  queue 054 GroupPathFinder.cpp      OPEN
  progress                           26 / 169 files complete
  F300 / KI#623                      CONFIRMED
  F301 / KI#624                      CONFIRMED
  F302 / KI#625                      CONFIRMED
  F303 / KI#626                      CONFIRMED
  F304 / KI#627                      CONFIRMED
  F305 / KI#628                      CONFIRMED (use corrected WIP11 proof)
  F306 / provisional KI#629          UNUSED

Purpose of this checkpoint:
  Preserve the negative/control evidence accumulated after corrected F305 before
  the final queue-054 saturation pass. None of the candidates below currently
  justify allocating F306.

CONTROL 1 — MOVE_SAFE_TERRITORY / MOVE_NO_ENEMY_TERRITORY DESTINATION EXCEPTION
-------------------------------------------------------------------------------
Current GroupStepMetric::canStepThrough applies MOVE_SAFE_TERRITORY and
MOVE_NO_ENEMY_TERRITORY to plots that are treated as intermediate/transit plots.
GroupStepMetric::isValidDest does not repeat those tests and instead validates the
final plot through canMoveOrAttackInto (plus the separate destination danger and
area rules).

That shape initially looked like a possible policy leak: a plot forbidden as an
intermediate can still be accepted when it is the destination. The deeper contract
and ancestry pass instead support intentional transit semantics:

* KmodPathFinder StepMetricBase explicitly specifies that canStepThrough should
  check whether a plot can be entered assuming it is NOT the destination, whereas
  destination validity is a separate isValidDest contract. The same distinction is
  important for cross-destination node caching.
* Stock BtS's legacy path-validity design applies these movement flags from the
  source/intermediate side of a step and keeps pathDestValid separate. K-Mod and
  AdvCiv preserve that conceptual split.
* Current AdvCiv's CvUnitAI::AI_settleMove source comment describes the old BtS
  MOVE_SAFE_TERRITORY behavior as preventing "passage through foreign (open)
  borders". That wording describes transit, not a hard final-destination ownership
  rule.
* K-Mod/current exploration deliberately combines MOVE_NO_ENEMY_TERRITORY with
  ordinary destination evaluation. Its diplomacy fixes focus on avoiding accidental
  declarations while traversing toward a target; they do not establish a rule that
  a combat-capable explorer already at war may never finish a move on an empty
  enemy-owned endpoint.

Therefore the endpoint exception is internally consistent with the inherited
movement-flag contract. Do not "fix" it merely because the flag name sounds global.
No F306.

CONTROL 2 — getPathEndTurnPlot RELEASE NULL ROBUSTNESS
------------------------------------------------------
Current AdvCiv/SAS GroupPathFinder::getPathEndTurnPlot starts with:

    GroupPathNode* pNode = m_pEndNode;
    FAssert(pNode->getPathLength() == 1 || pNode->m_pParent != NULL);

so a caller that invokes the accessor without a successful path can dereference
NULL in a release build before the later pNode != NULL assertion/check loop.
K-Mod's older pointer-return implementation was more forgiving because its first
assert guarded `!node` and its return expression could yield NULL.

This is genuine API-hardening debt, but the shipped-consequence gate has not been
met:

* Current shipped Python contains no call to the exposed getPathEndTurnPlot
  wrapper.
* Current C++ call sites sampled in the full sweep are inside successful
  generatePath branches, boolean-success conditions, or loops whose candidate was
  admitted only after generatePath succeeded.
* getFinalMoves already handles a missing end node explicitly; getEndNode's current
  use in AI_considerPathDOW follows a successful generatePath.
* Current SAS has additionally added a release guard to the no-from-argument
  GroupPathFinder::generatePath overload when no group has been set, preventing a
  separate null-group crash path that Base AdvCiv still relies on an assertion for.

Without a shipped unguarded producer, keep this as API robustness debt rather than
F306.

CONTROL 3 — ROUTE / WORLD-STATE CACHE INVALIDATION BLIND SPOTS
--------------------------------------------------------------
The reusable pathfinder cannot intrinsically detect every world-state change that
alters movement cost while leaving group/start/flags/moves unchanged. Several such
shapes were challenged, but no normal current producer survived:

* Engineering changes road movement cost through route-change tech processing and
  does not itself reset the group finder. Ordinary research/trade turn sequencing
  did not yield same-group/same-start/same-moves reuse across the change.
* A goody hut can contribute enough research to complete Engineering mid-unit
  action, but acquiring the hut requires the unit to move onto it first. The next
  path query therefore has a different start plot and the reusable tree resets.
* Pillaging a route can mutate movement cost synchronously on the same plot and AI
  can loop into another update, but pillaging consumes a full movement denominator.
  The cached start-node moves no longer match the group's current moves, so
  canReuseInitialPathData resets the tree before reuse.
* Open Borders similarly lacks an explicit generic pathfinder invalidation, but
  sequential/simultaneous diplomacy and unit-turn ordering did not produce a
  normal same-group cache-reuse sequence before another reset boundary.

These remain architectural invalidation limitations to keep in mind, but no F306
without a current producer/consequence.

CONTROL 4 — EXPLICIT HEURISTIC-WEIGHT CACHE INPUT
--------------------------------------------------
setGroup can receive an explicit heuristic weight, but heuristic weight is not one
of the values used to decide whether cached nodes must be reset. A same-group,
same-destination future API caller could therefore reuse OPEN nodes with stale
heuristics if only that weight changes.

Current shipped call sites do not exercise that unsafe combination: explicit
non-default weights are used by fresh/local/cleared finders or remain constant for
that finder, while the reusable AI group finder uses the default derived weight.
Thus generic API debt only. F301 already covers the max-path input, which DOES have
an actual current reuse/commit failure.

CONTROL 5 — SIGNED-INT / MAX_INT ARITHMETIC
-------------------------------------------
No credible current signed-int overflow has been proven.

* Ordinary one-step movement terms are far below INT_MAX.
* Even the dominant current MOVE_ATTACK_STACK ratio penalty plus all eight possible
  adjacent-enemy multipliers remains only in the low tens of millions for one
  step, below signed 32-bit overflow.
* Cumulative overflow is mathematically possible only after long chains of those
  near-worst combat-cost plots; the shortest constructions found require dozens of
  consecutively massively overdefended hostile tiles, or equivalently absurd group
  strength/unit-count intermediates. This is not a credible shipped scenario under
  the audit's consequence standard.
* A newly initialized node's `iNewCost - MAX_INT` delta stays within signed range
  for the positive current step costs, and a newly created node has no descendants
  to propagate that sentinel delta through.
* Current largest declared SAS map dimensions (99 x 70 = 6,930 plots) do not create
  a NodeMap allocation/indexing overflow issue.

The code lacks formal saturating arithmetic, so retain this as robustness debt,
not F306 unless a substantially shorter normal producer is found.

CONTROL 6 — BROAD PATH-HISTORY / forwardPropagate LIMITATION
-------------------------------------------------------------
K-Mod's generic finder and AdvCiv's documentation already acknowledge that the path
cost function depends on path data and that reparenting / cached query order can
occasionally produce suboptimal paths. K-Mod source explicitly notes that a cheaper
parent can, counterintuitively, make a descendant more expensive and that there was
no obvious generic solution.

Do not allocate F306 merely by rediscovering that broad known limitation. The
numbered findings F300, F301 and F303 are narrower because they prove sharper
current contract failures (danger legality, hard max-path loss, and concrete mixed
movement cost reconstruction). A new finding here would need a similarly distinct
current failure, not another generic manifestation of the known compression issue.

OTHER CURRENT CONTROLS
----------------------
* Land exploration heuristic remains conservative despite the 2/3 fog discount;
  its route-derived minimum is railroad-scale and still below a fog step.
* OWN_EXCLUSIVE_RADIUS can lower cost but is disabled in current SAS; no live
  heuristic violation from it.
* Actual declaration of war resets the group pathfinder, so stale post-DOW cache is
  not the root of F304; F304 is specifically the pre-DOW simulation's endpoint
  asymmetry.
* Promotions reset group paths; current SAS KI#475 additionally invalidates the
  main finder across group membership changes.
* Domain +move technology changes alter maxMoves and therefore movesLeft, which the
  initial-data reuse check notices and resets.

NEXT CURSOR
-----------
F306 / provisional KI#629 remains UNUSED.
Queue 054 remains OPEN at 26 / 169 files complete.

Continue with:
  * remaining mutable-world-state cache inputs (health/combat strength, revelation,
    defender appearance/removal) and normal sequencing;
  * recursive forwardPropagate robustness / cycle/depth bounds, without duplicating
    the known broad limitation;
  * remaining source comments/assumptions/TODO/MAX_INT and modern-history pass;
  * final getPathEndTurnPlot/caller safety spot-checks;
  * if no independent root survives, close queue 054 with F306 unused and produce
    the next durable album rather than manufacturing another issue.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP13 — QUEUE 054 GROUPPATHFINDER.CPP — FINAL CACHE/DEAD-END CONTROLS
===============================================================================
Checkpoint status:
  queue 054 GroupPathFinder.cpp      OPEN
  progress                           26 / 169 files complete
  F300 / KI#623                      CONFIRMED
  F301 / KI#624                      CONFIRMED
  F302 / KI#625                      CONFIRMED
  F303 / KI#626                      CONFIRMED
  F304 / KI#627                      CONFIRMED
  F305 / KI#628                      CONFIRMED (use corrected WIP11 proof)
  F306 / provisional KI#629          UNUSED

CONTROL 7 — AUTOMATION / UnitAI STATE IS NOT A CURRENT CACHE ROOT
-----------------------------------------------------------------
GroupStepMetric cost/legality depends on group automation and UnitAI role, while
those values are not explicit fields in GroupPathFinder::setGroup's cache identity.
The transition paths were therefore traced rather than assumed safe.

* CvUnitAI::AI_setUnitAIType changes the role and then calls joinGroup(NULL). Current
  SAS KI#475 invalidates the reusable finder on selection-group membership change;
  any resulting new group also has a different group pointer and forces reset.
* CvSelectionGroup::setAutomateType itself does not reset the pathfinder, and
  CvUnit::automate can immediately call AI_update on the same group. Structurally
  this is therefore a state mutation that the cache identity does not encode.
* However, a normal nonautomated human group does not populate the reusable main
  finder in the live pre-automation paths inspected. The current MOVE_MAX_MOVES
  uses that can serve human-side evaluation use a temporary/cleared finder.
* Automation-type transitions that can reuse the main finder either preserve the
  same Explore cost semantics or cause the next query to use movement flags that
  differ from the previous automation's query, which makes setGroup reset.

Thus the generic cache key is not a complete value-function key, but no current
producer/consequence was established for automation or UnitAI changes. No F306.

CONTROL 8 — MAIN GROUP FINDER IS GLOBAL/STATIC, NOT PER GROUP
-------------------------------------------------------------
CvSelectionGroup::m_pPathFinder is one shared static reusable GroupPathFinder.
setGroup resets its nodes whenever the group pointer differs. Consequently a large
class of stale-world-state scenarios cannot survive ordinary inter-group AI work:
when another selection group queries the main finder, the previous group's tree is
discarded.

The meaningful reuse surface is therefore primarily repeated queries by the SAME
group (multi-target evaluators, same-group follow-up queries, same group/flags with
changed destination). This structural fact is important context for F300/F301 and
for rejecting several speculative stale-world-state candidates.

CONTROL 9 — STATIC DEAD-END NODE LATER USED AS DESTINATION IS INTENTIONAL AND SAFE
---------------------------------------------------------------------------------
A tempting candidate was that a plot first encountered as an illegal intermediate
might become CLOSED with no path, then be mistaken for an already-reached endpoint
on a later cached query because generatePath assigns m_pEndNode for any initialized
destination node.

Exact KmodPathFinder::processChild ordering disproves that candidate for the static
canStepThrough case:

1. updatePathData initializes the candidate path data.
2. New node known cost is initially MAX_INT.
3. If StepMetric::canStepThrough(kChildPlot) is false, state is set CLOSED so the
   node will never be expanded THROUGH as an intermediate.
4. Execution intentionally CONTINUES. processChild computes the finite candidate
   cost, installs the parent link, updates known cost, and links the child into the
   parent's child list.

That is exactly the intended cross-destination contract: a defended/otherwise
transit-forbidden plot may have a valid path TO it even though it may not be
expanded beyond. When a later cached query makes it the destination, its cached
parent chain and finite cost are legitimate.

The path-dependent danger check is different: failure there returns before the node
is opened/closed and leaves the node UNINITIALIZED. Such a parentless MAX_INT node
therefore is not mistaken for a cached endpoint by generatePath.

The initially plausible false-success / getPathEndTurnPlot crash sequence is thus
rejected. No F306.

CONTROL 10 — MOVE_DECLARE_WAR FLAG-ONLY CACHE OPTIMIZATION IS ENFORCED BELOW
----------------------------------------------------------------------------
GroupPathFinder::setGroup deliberately avoids a reset when only MOVE_DECLARE_WAR
changes and the acting team is not AI_isSneakAttackReady. The comment says that in
that state the flag is effectively no different.

Lower-level tracing confirms this is not merely an assumption: AI
CvUnit::canMoveInto/canMoveOrAttackInto with bDeclareWar=true still requires the
team to be sneak-attack ready (and the group's AI_isDeclareWar predicate) before
future-enemy traversal becomes legal. Therefore adding MOVE_DECLARE_WAR while the
team is not sneak-ready does not change AI intermediate legality. No stale-CLOSED
root from this optimization.

CONTROL 11 — HUMAN DIRECT-ATTACK TINY COST CANNOT BE AN INTERMEDIATE HEURISTIC EDGE
------------------------------------------------------------------------------------
The human direct-attack fast path can return PATH_STEP_WEIGHT (7), far below the
normal GroupPathFinder heuristic lower bound. This is nevertheless safe for current
path structure: the special edge is restricted to the initial adjacent visible
attack, canStepThrough rejects a defended enemy plot as an ordinary intermediate,
and non-air groupAttack does not use such an attack edge as part of a longer route.
It can therefore only be the destination edge and does not invalidate A* lower-bound
reasoning for remaining path steps.

CONTROL 12 — SPEND_ALL_MOVES_ON_INVASION SIMULATION HAZARD IS DORMANT
---------------------------------------------------------------------
CvPlot::movementCost has an optional SPEND_ALL_MOVES_ON_INVASION branch that uses
the LIVE unit's current movesLeft. That would be suspect when GroupPathFinder is
simulating an invasion step on a future turn, because simulated remaining movement
can differ from the live unit object. Current SAS and Base AdvCiv both configure
SPEND_ALL_MOVES_ON_INVASION = 0, so this branch is dormant in the shipped rules.
Retain as configuration-sensitive debt only.

CROSS-FILE SIDE NOTE — SAS SETTLER LEVEL-2 LOGGING CAN USE STALE/NULL PATH END
-----------------------------------------------------------------------------
The full getPathEndTurnPlot caller inventory found a real SAS diagnostic hazard,
but not a GroupPathFinder gameplay root suitable for F306.

SAS_shouldDelayFoundInPlaceForBetterReachableSite repeatedly calls generatePath for
candidate sites and stores the best site's plot/value/path-turn scalar data, but it
does not preserve that best query's pathfinder tree/end-node. A later candidate can
replace the current end-node with another site's path or can fail and leave no end
node, while the helper can still return true because an EARLIER best site exists.
Three level-2 Settler diagnostic lines then call getPathEndTurnPlot on the current
finder state. With SAS_BBAI_SETTLER_LOG_LEVEL >= 2 this can therefore log the wrong
candidate's end-turn plot, or hit GroupPathFinder's release-unsafe null dereference
if the final later query failed.

The default log level is 0 and gameplay behavior already uses the stored best-site
values rather than that diagnostic endpoint. Preserve for the appropriate SAS
CvUnitAI/Settler logging review; do not allocate F306 in queue 054 for it.

NEXT CURSOR
-----------
F306 / provisional KI#629 remains UNUSED.
Queue 054 remains OPEN at 26 / 169 files complete.

Continue with the final single-group multi-target / generic-node invariant sweep,
remaining KmodPathFinder history/deltas, and exact queue-close decision. If no
independent current root survives, close queue 054 with F306 unused rather than
manufacturing another finding.

No repository source files modified.
===============================================================================

===============================================================================
C027-WIP14 — QUEUE 054 GROUPPATHFINDER.CPP — POST-WIP13 SATURATION CONTROLS
===============================================================================
Checkpoint status:
  queue 054 GroupPathFinder.cpp      OPEN
  progress                           26 / 169 files complete
  F300 / KI#623                      CONFIRMED
  F301 / KI#624                      CONFIRMED
  F302 / KI#625                      CONFIRMED
  F303 / KI#626                      CONFIRMED
  F304 / KI#627                      CONFIRMED
  F305 / KI#628                      CONFIRMED (use corrected WIP11+ proof)
  F306 / provisional KI#629          UNUSED

CONTROL 13 — CLOSED-NODE NON-REOPEN DOES NOT JUSTIFY A NEW ROOT BY ITSELF
-------------------------------------------------------------------------
The final generic KmodPathFinder sweep challenged a CLOSED node receiving a cheaper
parent without being reopened for fresh neighbor expansion. This is potentially
harmful only when the step metric depends on path history/state (or when the
heuristic is inadmissible), because ordinary path-independent A* with the remaining
current lower-bound assumptions should not discover a genuinely better route to an
already processed node later.

That exceptional family is already explicitly acknowledged by upstream code/history:
* KmodPathFinder::forwardPropagate says that a cheaper path to a parent can make a
  descendant more expensive because GroupStepMetric cost depends on path history,
  and says there is no obvious generic solution.
* AdvCiv practical 4196, "Fix danger avoidance checks in pathfinder", explicitly
  says rare failures may remain because GroupStepMetric uses path data and notes
  that further paths reaching a child may have to be reconsidered.

The sharper current consequences from this state-compression family are already
numbered as F300 (path-dependent danger legality is not revalidated/re-expanded
across reparent/propagation) and F303 (cost uses the wrong per-unit movement state).
F305 is the separate current inadmissible-heuristic root. Do not allocate F306 merely
for the broad CLOSED/non-reopen limitation already described by those roots/history.

CONTROL 14 — NAVAL MOVEMENT HAS NO SECOND LIVE SUB-HEURISTIC COST CHANNEL
-------------------------------------------------------------------------
F305 prompted a full check of current naval movement-cost reducers, not only raw
MoveDiscountChange.
* Current nonempty promotion FeatureDoubleMoves belongs to Woodsman-style land use.
* Current HillsDoubleMove belongs to a land archer/recon promotion.
* No current naval combat-class promotion grants terrain/feature double movement.
* The current raw move-discount promotion is restricted to land combat classes.
* There are no water routes and current unit XML has no nonempty SpeedBonuses.

Therefore ordinary sea movement cannot fall below one movement denominator through
another current movement ability. The live sea heuristic violation remains narrowly
the 2/3 unrevealed exploration discount recorded in F305 (plus optional disabled
rule configurations already controlled elsewhere). No F306.

CONTROL 15 — OLD baseMoves / maxMoves CACHE COMMENT HAS NO CURRENT SHIPPED ROOT
-------------------------------------------------------------------------------
GroupStepMetric::canReuseInitialPathData compares only initial moves, while an old
K-Mod comment worries that max/base moves may also need explicit checking.
Current state transitions were traced:
* domain +move technology and circumnavigation change maxMoves and therefore
  movesLeft (CvUnit::movesLeft = maxMoves - moves used), so initial-data reuse
  detects the difference and resets;
* promotions that alter unit movement explicitly reset the group pathfinder;
* current unit XML has no SpeedBonuses that could make baseMoves vary with local
  bonus availability while leaving group moves unchanged;
* the shipped debug CyTeam.changeExtraMoves path likewise changes maxMoves and hence
  movesLeft, which triggers the same start-data reset.

No normal current transition was established where the group's heuristic-relevant
baseMoves changes while same-group/same-start initial moves remain equal and cached
nodes are reused. Retain the comment as generic modding/API caution, not F306.

CONTROL 16 — PATH INFORMATION ACCESSORS / SHIPPED CALLERS
---------------------------------------------------------
The remaining path information interface was inventoried after the earlier
getPathEndTurnPlot control.
* AI_considerPathDOW calls getEndNode only after its own successful generatePath.
* Main C++ getPathFirstPlot/getPathEndTurnPlot/getFinalMoves uses are attached to
  successful generatePath branches or saved success booleans in the paths inspected.
* CyUnit/CySelectionGroup expose path plot accessors to Python, but current shipped
  SAS Python has no caller of these accessors.

The accessor implementation remains fragile as an API contract (especially
getPathEndTurnPlot's release null dereference), but the only concrete current SAS
unguarded/stale use found is the level-2 Settler diagnostic side note already
recorded in WIP13. No GroupPathFinder gameplay F306 from the accessor surface.

CONTROL 17 — ALTERNATE PATHFINDER LIFETIME/RESET CONTRACT HOLDS
---------------------------------------------------------------
CvSelectionGroup::m_pAltPathFinder is intentionally shared as allocation-saving
scratch state. getClearPathFinder unconditionally calls reset() before returning it.
Normal group death calls invalidateGroupPaths(), invalidating both main and alternate
finder group pointers. SAS clearUnits intentionally invalidates only the main finder
while a live groupMove may still consult its alternate execution path; if the group
then dies, kill() performs the two-finder invalidation before deletion.

Thus the deliberate KI#475 alternate-finder exception does not leave a stale group
pointer available on the next scratch-finder use. No F306.

CONTROL 18 — STRUCTURAL NODE / OPEN-LIST BOUNDS
-----------------------------------------------
* Current largest declared SAS map is 99 x 70 = 6,930 plots, far below PlotNumTypes
  and allocation arithmetic limits used by NodeMap.
* A plot has at most NUM_DIRECTION_TYPES (8) adjacent children; m_apChildren is
  exactly that size and m_iNumChildren is ample.
* OpenList::open is reached only for a newly UNINITIALIZED valid node; an existing
  OPEN/CLOSED node is not opened a second time, so the vector does not acquire
  duplicate node entries through normal processChild flow.
* Positive step costs and the strict cheaper-parent relation maintain an acyclic
  parent tree. Consequently path length is bounded by the finite plot set and does
  not threaten its int storage.
* Recursive forwardPropagate can be deep in theory, but a credible current crash
  would require an extremely pathological many-thousand-node descendant chain and
  late reparent; no shipped construction survived the consequence standard.

No F306 from structural storage/recursion bounds.

CONTROL 19 — IGNORED CACHE FLAGS ARE REALLY PATHFINDER-IRRELEVANT
-----------------------------------------------------------------
setGroup ignores differences in MOVE_DIRECT_ATTACK, MOVE_SINGLE_ATTACK and
MOVE_NO_ATTACK when comparing flags. A full GroupStepMetric search confirms none of
those flags participates in isValidStep, canStepThrough, isValidDest, cost,
updatePathData or heuristic calculation. They affect later mission/execution logic.
Excluding them from the reusable path tree's cache identity is therefore sound.

HISTORY / DELTA SATURATION
--------------------------
Current SAS KmodPathFinder.h is semantically Base AdvCiv 1.14; visible differences
are formatting/signature collapsing. GroupPathFinder.cpp's only substantive current
SAS-vs-Base behavioral delta is the already-known SAS release guard against calling
generatePath(kTo) before a group has been set. Modern upstream pathfinder work
(practical 4196 danger split, 4229 route-flag cleanup, 4277 pre-DOW route simulation)
is present; no later upstream generic correction was found missing from SAS.

NEXT CURSOR
-----------
F306 / provisional KI#629 remains UNUSED.
Queue 054 remains OPEN at 26 / 169 files complete.

Only the final queue-close reconciliation remains unless a genuinely independent
root appears during the last remaining source/history sanity checks. Prefer closing
queue 054 with F306 unused over turning documented path-history approximation,
optional disabled configuration, or API-hardening debt into a manufactured finding.

No repository source files modified.
===============================================================================


===============================================================================
C028 DURABLE — QUEUE 054 GROUPPATHFINDER.CPP — CLOSED
===============================================================================
Closure status:
  investigation file                   027
  queue                                054 CvGameCoreDLL/GroupPathFinder.cpp
  queued current lines                 844
  queue state                          COMPLETE
  progress after closure               27 / 169 files complete
  F300 / KI#623                        CONFIRMED
  F301 / KI#624                        CONFIRMED
  F302 / KI#625                        CONFIRMED
  F303 / KI#626                        CONFIRMED
  F304 / KI#627                        CONFIRMED
  F305 / KI#628                        CONFIRMED
  F306 / provisional KI#629            UNUSED / next available root

FINAL FINDING SET
-----------------
F300 / KI#623 — PATH-DEPENDENT DANGER IS NOT REVALIDATED AFTER REPARENT / PROPAGATION
  Reparenting changes GroupPathNode movement/path-turn state through updatePathData,
  but the path-dependent danger predicate is not rerun. forwardPropagate can change
  the same state recursively in descendants, and CLOSED nodes are not reopened for
  fresh expansion after such state mutation. A concrete current noncombat-worker
  case proves generatePath/path-turn evaluation can consume a route that no longer
  satisfies its own danger rule. Inherited from K-Mod; AdvCiv practical 4196 fixed
  the first-arrival form but explicitly left residual path-data concerns. Repair
  must preserve an older valid parent when a prospective reparent is invalid and
  account for descendant/CLOSED-node revalidation or rediscovery.

F301 / KI#624 — MAX-PATH BOUND IS NOT ENFORCED WHEN CHILD/PARENT STATE IS COMMITTED
  KmodPathFinder may initialize or reparent a destination through a path longer than
  iMaxPath. A valid bounded parent/path can then be overwritten by a cheaper
  over-limit route; the final length check returns false even though a legal bounded
  route had already existed. Current attack-city/mobile-artillery AI_choke(1) gives
  a shipped bounded producer. Inherited K-Mod generic-pathfinder root.

F302 / KI#625 — MAX-COST UNIT OVERWRITES TRUE GROUP MINIMUM MOVES LEFT
  GroupStepMetric::cost first computes iWorstMovesLeft as the minimum across group
  members, then overwrites it with iMovesLeft whenever a later unit sets iWorstCost.
  Mixed-speed/mixed-cost stacks can therefore skip real end-turn territory/defense/
  danger weights. Exact current siege + German Hussar route math reverses path
  choice and changes AI_pickTargetCity scoring. K-Mod practical 237/238 ancestry.
  Local repair: keep the independent group-minimum accumulation and remove the
  coupled iWorstMovesLeft assignment inside the max-cost branch.

F303 / KI#626 — COST FABRICATES PER-UNIT REMAINING MOVES FROM SHARED GROUP MINIMUM
  On parent nodes with positive group moves, GroupStepMetric::cost feeds the same
  iCurrMoves to every unit even when their true per-unit remaining movement differs
  because movement costs are non-uniform. updatePathData already contains K-Mod's
  practical-1017 per-unit replay to avoid exactly this movement-state error. A
  current siege + 3-move Hussar railroad case reverses a staging route and can make
  AI_moveToStagingCity spend an extra turn instead of reaching the city immediately.
  Independent of F302; repair needs real per-unit turn movement reconstruction or
  richer cached node state.

F304 / KI#627 — PRE-DOW ROUTE SIMULATION CHECKS ONLY THE DESTINATION ENDPOINT
  AdvCiv practical 4277 tries to anticipate loss of Open Borders when a path will
  declare war, but bIgnoreRoutes is derived only from kTo's team. A step from a
  future-enemy road out to a friendly/third-party road can therefore use the enemy
  route before war even though it will be unusable after the declaration. A normal
  1-move attack-city stack can be scored as 2 path turns when execution becomes 3,
  materially overvaluing AI_pickTargetCity. Base AdvCiv 1.14 inherits this incomplete
  repair. SAS also has AI-usable PROMOTION_LOGISTICS enemy-route capability, so a
  robust fix must simulate post-DOW route validity per unit at BOTH endpoints rather
  than blindly ignoring all prospective-enemy routes.

F305 / KI#628 — SEA EXPLORATION HEURISTIC OVERESTIMATES DISCOUNTED FOG STEPS
  Sea groups default to one full movement denominator as the A* heuristic weight,
  but K-Mod practical 1188 later made unrevealed exploration steps cost only 2/3.
  The resulting sea heuristic is inadmissible for UNITAI_EXPLORE_SEA/
  AUTOMATE_EXPLORE. Corrected current reproduction uses a REVEALED edge destination
  (sea AI cannot choose an unrevealed destination): exact A* simulation returns a
  more expensive revealed route while leaving the genuinely cheaper partially-fog
  detour unexpanded. Physical groupPathTo uses a freshly cleared finder with the same
  heuristic, so execution reproduces the suboptimal route. K-Mod practical 366 +
  1188 ancestry; current K-Mod, Base AdvCiv and SAS all inherit it.

WHY F306 REMAINS UNUSED
-----------------------
The final saturation pass challenged, among other things:
* static dead-end nodes later becoming destinations;
* CLOSED-node cheaper-parent/non-reopen behavior;
* MOVE_SAFE_TERRITORY / MOVE_NO_ENEMY_TERRITORY destination semantics;
* ignored cache flags (DIRECT_ATTACK / SINGLE_ATTACK / NO_ATTACK);
* MOVE_DECLARE_WAR-only cache changes;
* automation and UnitAI changes;
* max/base movement changes and old canReuseInitialPathData caveat;
* Engineering / Open-Borders / pillage / route-world-state mutation;
* explicit heuristic-weight reuse;
* alternate scratch-finder lifetime;
* getPathEndTurnPlot/getEndNode/getPathFirstPlot caller contracts;
* sea raw discounts plus terrain/feature double-move promotion channels;
* dormant SPEND_ALL_MOVES_ON_INVASION;
* signed-int path-cost overflow;
* NodeMap sizing, child capacity, OPEN-list uniqueness, parent cycles and recursion;
* normalized SAS-vs-Base semantic deltas and all modern upstream pathfinder commits.

These checks either close cleanly under current callers/configuration, are generic
API/modding hardening only, require pathological unsupported states, are disabled by
current configuration, or fall into the already-documented path-history/state
compression family represented more sharply by F300/F303. None justified a
separate current shipped F306.

CROSS-FILE SIDE NOTE RETAINED
-----------------------------
The SAS Settler helper SAS_shouldDelayFoundInPlaceForBetterReachableSite can retain
best-site scalar data while later generatePath calls replace/fail the current finder
state. Three level-2 diagnostic logging calls then use getPathEndTurnPlot and can log
another candidate's path end or null-dereference when the last query failed. Default
Settler log level is 0 and gameplay uses stored best values. Preserve this for the
CvUnitAI/Settler logging review; it is not a queue-054 gameplay root.

FINAL DELTA / ANCESTRY SATURATION
---------------------------------
After stripping comments and whitespace, current SAS KmodPathFinder.h is semantically
identical to Base AdvCiv 1.14. Current SAS GroupPathFinder.cpp differs behaviorally
from Base only through SAS's already-added release null-group guard in
GroupPathFinder::generatePath(kTo). No hidden SAS-local behavioral fork remains
unclassified, and no later relevant upstream generic fix in the supplied history is
missing from current SAS.

NEXT CURSOR
-----------
Queue 054 is CLOSED.
Progress is 27 / 169 files complete.
F306 / provisional KI#629 remains the next unused root ID.
The audit album marks queue 092 CvGameCoreDLL/BBAILog.cpp (327 lines) as the next
priority file.

No repository source files modified.
===============================================================================

===============================================================================
C028-WIP01 — QUEUE 092 BBAILOG.CPP — F306 FIXED-NAME SESSION UTC + FIRST CONTROLS
===============================================================================
Checkpoint status:
  queue 092 CvGameCoreDLL/BBAILog.cpp     OPEN
  progress                                27 / 169 files complete
  F306 / KI#629                           CONFIRMED
  F307 / provisional KI#630               UNUSED

SCOPE / ANCESTRY START
----------------------
Base AdvCiv 1.14 BBAILog.cpp contains the inherited fixed 2048-byte formatter,
network-player/fixed BBAI filename logic and deal-cancel helper. Current SAS adds
XML-backed category levels, timestamped per-new/load filenames, new/load/game-state
metadata, central mod identity/settings rows, and KI#161.2 truncation termination
hardening. The first SAS-specific lifecycle defect below was introduced when the
per-new/load metadata/session rollover was added; it is not a Base AdvCiv/K-Mod
behavioral root.

F306 / KI#629 — FIXED-NAME BBAI MODE REUSES STALE FIRST-SESSION UTC METADATA
---------------------------------------------------------------------------
Current state:
  g_szSASBBAILogTimestamp is lazy/cached process-global state.
  getSASBBAILogTimestamp() creates it only while empty.

rollSASBBAILog(szContext) refreshes that timestamp ONLY inside:
  if (isSASBBAILogTimestampedFilenameEnabled())

But the supported XML mode SAS_BBAI_LOG_USE_TIMESTAMPED_FILENAME=0 is documented as
preserving fixed-name BBAI.log behavior; the new/load metadata rows still advertise
UTC identity in that mode:
  BBAI_NEW_GAME_INITIALIZING utc=...
  BBAI_NEW_GAME_STARTED utc=...
  BBAI_SAVE_LOADED utc=...

Concrete process sequence with BBAI logging enabled and timestamped filename=0:
  1. First new/load calls rollSASBBAILog, which does nothing.
  2. First metadata call lazily creates timestamp T1.
  3. A later load/new in the SAME Civ4 process again calls rollSASBBAILog, which
     still does nothing.
  4. getSASBBAILogTimestamp returns cached T1, not the actual new/load time T2.

Thus subsequent session headers appended to fixed BBAI.log carry stale UTC from the
first logged state in the process. This directly contradicts the practical-6011
new/load identification purpose: startSASBBAILogForLoadedSave explicitly says the
loaded state is identified through UTC, and the XML option describes only the
filename choice, not a metadata-freshness opt-out.

Ancestry/history:
* Pre-SAS/Base fixed-name BBAI had no per-session UTC row, so there was no stale-UTC
  metadata contract to violate.
* SAS practical 6011 (f544db1d5e) introduced rollSASBBAILog, per-new/load metadata,
  and the current conditional timestamp refresh together. This is the root.
* Current SASGameRecordLog demonstrates the intended separation: its roll helper
  ALWAYS refreshes the session timestamp, while only sequence/context filename state
  is conditional on SAS_GAME_RECORD_LOG_USE_TIMESTAMPED_FILENAME.

Impact / severity boundary:
* Diagnostic/report correctness only; no gameplay-state corruption.
* Default timestamped mode=1 is correct. The bug affects the explicitly supported
  fixed-name opt-out mode=0, especially repeated save/new tests without restarting
  Civ4—the exact workflow practical 6011 sought to identify reliably.
* The wrong UTC can make later fixed-file sessions look as if they began at the
  first session's time and undermines chronology when BBAI.log contains many runs.

Repair boundary:
Always refresh g_szSASBBAILogTimestamp at every roll/new/load. Keep filename
sequence/context conditional if fixed-name behavior must remain BBAI.log/BBAI%d.log.
In outline:
  g_szSASBBAILogTimestamp = createSASBBAILogTimestamp();
  if (isSASBBAILogTimestampedFilenameEnabled()) { ++sequence; set context; }
Optionally clear stale context when fixed-name mode is selected, though the static
filename branch ignores it today. No save format change is needed.

CONTROL 1 — CATEGORY MASTER-ENABLE COMPLETENESS IS CLEAN
---------------------------------------------------------
Mechanical current-source reconciliation found 17 BBAI log-level getters and all 17
appear in isSASBBAILogEnabled(). Every g*LogLevel macro in BBAILog.h maps to the
expected getter. Therefore newly separated WAR / MILITARY_PRODUCTION /
OVERSEAS_TRANSPORT / GREAT_GENERAL / SETTLER / WORKER / WORKER_SEA etc. categories
cannot be silently suppressed merely because their getter was omitted from the
master OR.

CONTROL 2 — XML SETTING CACHE IS INTENTIONAL
---------------------------------------------
The XML-backed getters are static-cached on first use. The current GlobalDefines SAS
documentation explicitly tells users to restart Civ4 after changing these BBAI
settings, so lack of hot reload is intentional rather than a stale-setting bug.

OPEN CONTROL — FIXED 2048-BYTE logBBAI FORMATTER
-------------------------------------------------
KI#161.2 correctly forces a terminator after MSVC 7.1 _vsnprintf truncation, but the
sink still silently loses content beyond 2047 bytes. This is not yet a numbered
finding in queue 092:
* longest current static structured format literals are large (~1.2 KB) but do not
  alone prove overflow;
* several dynamic list producers are deliberately capped (e.g. Settler escort pool
  logs at most 10 units);
* potentially unbounded wide strings remain to be proven, notably deal text and
  other CvWStringBuffer output.
Promote only if a current normal producer can exceed the sink with a meaningful
logging consequence; otherwise retain as known formatter debt already bounded by
KI#161.2.

CROSS-FILE CONTROL — INHERITED CvWStringBuffer VARARGS ODDITY
-------------------------------------------------------------
CvPlayer::onTurnLogging contains inherited `logBBAI("%S", szBuffer)` even though
CvWStringBuffer has no conversion operator; a nearby later call correctly uses
`szBuffer.getCString()`. K-Mod and Base AdvCiv contain the same direct-object call.
On Civ4's 32-bit MSVC ABI it appears to work accidentally because the first object
member is the wchar buffer pointer and it is the only variadic argument, but this is
non-portable / undefined C++ rather than a clean `%S` contract. Preserve as
cross-file/portability debt; no current shipped failure has been established, so do
not allocate F307 merely from the source shape.

NEXT CURSOR
-----------
Queue 092 remains OPEN at 27 / 169 files complete.
F306 / KI#629 is confirmed.
F307 / provisional KI#630 remains unused.

Continue with:
  * prove/reject current >2047-byte BBAI producers (deal text, dynamic buffers,
    largest structured rows);
  * new/load/header lifecycle ordering, network-player filename behavior and
    timestamp/context collision states;
  * varargs/format specifier/null/string ownership sweep;
  * category gate/settings completeness and all SAS-vs-Base behavioral deltas;
  * BBAILog history saturation, especially practicals 5903/5925/5981/6011 and later
    category/mod-context changes.

No repository source files modified.
===============================================================================

===============================================================================
C028-WIP02 — QUEUE 092 BBAILOG.CPP — FORMAT/LIFECYCLE/HISTORY SATURATION
===============================================================================
Checkpoint status:
  queue 092 CvGameCoreDLL/BBAILog.cpp     OPEN
  progress                                27 / 169 files complete
  F306 / KI#629                           CONFIRMED
  F307 / provisional KI#630               UNUSED

F306 STATUS
-----------
F306 remains unchanged and fully confirmed: supported fixed-name mode
SAS_BBAI_LOG_USE_TIMESTAMPED_FILENAME=0 refreshes neither
`g_szSASBBAILogTimestamp` nor session context in `rollSASBBAILog`, so later new/load
headers in the same Civ4 process retain the first logged session's UTC. Current
SASGameRecordLog provides the internal control: it refreshes UTC unconditionally
and gates only filename sequence/context on timestamped-filename mode.

CONTROL 3 — 2048-BYTE FORMATTER CEILING HAS NO CURRENT NORMAL >2047 PRODUCER
----------------------------------------------------------------------------
KI#161.2's fixed 2048-byte `_vsnprintf` sink still silently truncates a row longer
than 2047 bytes, but a current-source producer sweep did not establish a normal
BBAI row that reaches the ceiling:
* the longest structured format literal is about 1.2 KB; pessimistic numeric-width
  expansion remains below the sink before bounded string fields;
* the current BBAI_GAME_SETTINGS row remains about 1,050 bytes even under a deliberately
  pessimistic construction with all 28 current GAMEOPTION_* type names enabled and a
  100-character map-script name (current longest map-script filename is only 27);
* score help is one fixed localized score-breakdown structure rather than an
  inventory/list whose size scales without a small bound;
* the player team/met/vassal/war status buffer only enumerates MAX_CIV_TEAMS IDs;
* persisted CvDeal lists retain only ongoing trade items (resource/GPT and treaty/
  relationship items), not arbitrary lists of every technology/city/map/gold item,
  so deal-cancel text cannot scale with all one-shot tradeables;
* large SAS diagnostic lists examined in the current tree frequently impose their
  own small caps (e.g. Settler escort-pool diagnostic lists).

Therefore keep the fixed sink as formatter robustness/technical debt. KI#161.2
correctly prevents unterminated-truncation crashes; there is no evidence gate for a
separate current F307 based only on possible silent truncation. The independent
SASGameRecord formatter's later KI#375 dynamic grow-and-retry repair does not by
itself prove the same practical overflow exists in BBAI.

CONTROL 4 — LITERAL FORMAT/ARGUMENT COUNT SWEEP IS CLEAN
---------------------------------------------------------
A mechanical current-source sweep initially appeared to find one sea-spy call with
three conversions and ten arguments, but that was a parser false positive caused by
parentheses/apostrophes in a trailing `//` comment confusing the call boundary.
After stripping comments while preserving strings and source structure, the sweep
reconciled 816 literal-format `logBBAI` calls with ZERO conversion-count mismatches.
Record the corrected result, not the discarded false positive.

CONTROL 5 — LOAD HEADER ORDERING IS VALID FOR CHECKED METADATA
--------------------------------------------------------------
`startSASBBAILogForLoadedSave()` runs at the top of `CvGame::onAllGameDataRead`, so
several apparently-late compatibility/cache states were challenged explicitly:
* playersEverAlive: BBAI calls `countCivPlayersEverAlive()`, which rescans loaded
  player `isEverAlive()` state; it does NOT read the immediately-afterward repaired
  `m_iCivPlayersEverAlive` cache. The old-save compatibility assignment therefore
  does not make the header report zero/stale player history.
* map heaviness: `m_bLandHeavyMapname` and `m_bNavalHeavyMapname` are serialized
  directly in `CvGame::read` and written in `CvGame::write`. Supported current saves
  therefore already contain the actual cached DLL classification when the early
  BBAI_SAVE_LOADED row is emitted; no post-load recomputation is required.
* map dimensions, game setup enums and player data are already part of the loaded
  state at onAllGameDataRead. No current delayed-finalization dependency was found.

Do not manufacture a BBAI ordering bug from truly old pre-field saves whose overall
CvGame serialization layout is no longer a supported current uiFlag format.

CONTROL 6 — RNG IDENTITY ROW IS READ-ONLY
------------------------------------------
`BBAI_GAME_RNG` calls `CvRandom::getSeed()` on map and sync RNGs. The getter simply
returns `m_uiRandomSeed`; it neither advances nor reseeds the generator. Logging RNG
identity therefore cannot perturb deterministic game state.

CONTROL 7 — SESSION ROLLOVER BOUNDARY HAS NO PRE-ROLL BBAI PRODUCER FOUND
-------------------------------------------------------------------------
New game starts the BBAI session after `CvGame::reset` and before map generation;
loaded games roll after serialized data is read. A current source sweep found no
BBAI producer inside reset/uninit/save-read paths that can lazily create a timestamp
and context-less log before the intended roll. ReproTest's `ReproTest: reloading`
line belongs to the quick-load control flow and does not establish a serialization-
time pre-roll writer.

CONTROL 8 — logBBAICancel CALLER CONTRACT IS CLEAN
--------------------------------------------------
All five current callers are within CvPlayerAI deal-cancellation/overdraft logic and
pass `getID()` after the surrounding contract has established that the deal is
between this player and the counterpart. Logging occurs while the deal is live and,
where the deal is then killed, before `kill()`. No NO_PLAYER or post-destruction
lifetime path was found.

CONTROL 9 — INHERITED CvWStringBuffer ELLIPSIS PASS REMAINS PORTABILITY DEBT
----------------------------------------------------------------------------
CvPlayer::onTurnLogging still contains inherited `logBBAI("%S", szBuffer)` for a
`CvWStringBuffer` object. `CvWStringBuffer` is a non-trivial 12-byte class on the
Civ4 ABI, has no conversion operator, and its first member is the backing `wchar*`.
Passing such an object through C-style ellipsis is not a clean portable C++ contract.
K-Mod and Base AdvCiv ship the same call, while a nearby later call correctly uses
`szBuffer.getCString()`.

The Civ4 32-bit MSVC ABI/layout plausibly explains why the inherited call works in
practice (the first object word is the expected wide-string pointer), and no current
wrong-output/crash evidence was established. Keep as portability/undefined-behavior
debt for the CvPlayer/BBAI call-site review rather than allocating F307 from source
shape alone. A local cleanup to `.getCString()` would nevertheless be prudent.

ANCESTRY SATURATION SO FAR
--------------------------
Older AdvCiv BBAILog history is retained:
* practical 1577: deal-cancellation logging;
* practical 2269: suppress `gDLL->logMsg` timestamps;
* practical 2690: network-MP active-player filename for OOS debugging;
* practical 3579: corrected old `BBAILog.log` typo back to `BBAI.log`.
SAS history classification:
* 5903: XML-backed cached levels;
* 5925: per-launch UTC filename;
* 5981: KI#161.2 terminator hardening;
* 6011: per-new/load roll + identity/settings metadata (F306 root);
* 6019: map-heaviness metadata only;
* 6326: mod-context metadata only;
* intervening later commits primarily add/rename diagnostic categories/settings and
  have not exposed a missing master-enable entry or stale upstream safety repair.

REMAINING CURSOR
----------------
Queue 092 remains OPEN at 27 / 169 complete.
F306 / KI#629 confirmed; F307 / KI#630 unused.
Remaining work before closure:
* finish network active-player/session filename and same-second/collision boundaries;
* finish null/string/encoding ownership/specifier review beyond literal arg counts;
* inspect any BBAI caller/configuration that can mutate logging identity mid-session;
* normalize current SAS-vs-Base semantic deltas and complete remaining history
  commit classification;
* only then either establish a genuinely independent F307 or close queue 092 with
  F306 as its sole finding.

No repository source files modified.
===============================================================================

===============================================================================
C028-WIP03 ADDENDUM — QUEUE 092 CATEGORY-ISOLATION MATRIX / F307 STILL UNUSED
===============================================================================

CATEGORY-ISOLATION CONTRACT
---------------------------
SAS split the inherited monolithic BBAI logging into independently configurable
PLAYER/TEAM/WAR/CITY/MILITARY_PRODUCTION/CITIZEN/UNIT/OVERSEAS_TRANSPORT/
GREAT_GENERAL/SETTLER/FOUND/EVACUATION/WORKER/WORKER_SEA/MAP/DEAL_CANCEL/CULTURE
levels. `logBBAI()` itself only performs the global "any BBAI category enabled"
sink gate, so category-specific callers/helpers must own their individual level
checks. Current CvPlayerAI conquest/raze comments explicitly recognize this rule.

A raw grep for helpers with direct `logBBAI` calls is NOT evidence of leakage:
many helper bodies are intentionally unguarded and every entry point is caller-
gated. The current caller-chain matrix was therefore traced rather than counted.

CONTROL 10 — CvUnitAI HELPER/CALLER CATEGORY MATRIX IS CLEAN
------------------------------------------------------------
The remaining apparently-unguarded UnitAI helper bodies all resolve to explicit
category gates:
* `logSASRetreatUnitResult` -> `bLogRetreatResult`, derived from Evacuation level;
* `logSASSafetyUnitResult` -> Evacuation >= 3;
* `SAS_logSettlerMissionDecision` -> Settler >= 2 at every action path; calls that
  look bare in grep output sit inside enclosing Settler >= 2 blocks;
* `logSASEvacuationUnitDecision` -> Evacuation >= 2;
* `logSASWorkerSeaMoveDetail` -> Worker Sea >= 2 for work-boat decisions, or
  Evacuation >= 3 for the intentionally shared retreat/safety diagnostics;
* `logSASCultureGreatArtistDecision` -> `bLogCultureGreatArtistDecision`, which is
  Culture >= 3 and UNITAI_GREAT_ARTIST;
* first-city BFC diagnostics -> `bLogSettlerAILevel3` = Settler >= 3;
* missing-war-city expedition diagnostic -> Unit >= 2 before helper entry;
* attack-defense reinforcement diagnostic -> direct gameplay call is Unit >= 2;
  the second apparently bare call is nested inside `SAS_logAttackCityParking`, and
  all parking helper calls are under `bLogAttackCityParking` = Unit >= 2 plus the
  intended parking context;
* barbarian attack-city opportunity -> `(Unit >= 2 || Player >= 2)` intentionally;
* large attack-city stack -> `bLogLargeAttackCityStack` = Unit >= 2;
* crowded peaceful attack-city diagnostics -> Unit >= 2;
* assault-transport failure -> Overseas Transport >= 2;
* Great-General join diagnostics -> Great General >= 1/2/3 as appropriate.
No normal UnitAI helper was found that can execute solely because an unrelated BBAI
category turns on the global sink.

CONTROL 11 — CvCityAI / CvCity / CvPlot / GROUP DIAGNOSTICS ARE CATEGORY-GATED
-------------------------------------------------------------------------------
* Military-production concrete reject and fallback rows use Military Production
  level 2/3 booleans.
* Worker-Sea choose/target/improvement-loss diagnostics use Worker Sea levels.
* Settler production decision helper calls use Settler levels.
* ordinary city push/finish/hurry/conscript rows remain under City levels.
* land-worker improvement-overwrite and worker group/mission execution rows use
  Worker levels.
No cross-category sink activation was found in these files.

CONTROL 12 — FOUND/MAP/TRUE-STARTS CALL CHAINS ARE CLEAN
--------------------------------------------------------
* `CitySiteEvaluator::log` is now self-guarded by Found level > 0; its known callers
  also gate it. `evaluateWithLogging` falls back to ordinary evaluation when Found
  logging is disabled.
* Barbarian city chooser detailed candidate rows are inside Found > 0. The final
  "Barbarian city created" row intentionally accepts either Player or Found logging,
  matching its shared gameplay/found-site diagnostic purpose.
* map normalization rows are inside Map logging gates.
* TrueStarts `IFLOG` remains its own map logging gate (and current macro additionally
  contains the inherited/current `&& false`, so those rows cannot leak at runtime).

CONTROL 13 — WAR/UWAI/TEAM/DEAL CALL CHAINS ARE CLEAN
-----------------------------------------------------
* WarEvaluator helper bodies are unguarded by design, but suspicious-peace entry is
  enabled only through a War-level path and high-utility diagnostics derive from
  War >= 3.
* UWAIAgent war-target helper entry points are War-level gated.
* CvSelectionGroupAI attack-order diagnostics are War >= 3.
* CvTeam/CvTeamAI war-plan/peace rows are War or Team gated according to purpose.
* CvDeal trade rows use Team, while explicit war-deal rows use War.
No monolithic-era normal war/team call was found that leaks when only an unrelated
new SAS category is enabled.

CONTROL 14 — CvPlayer / CvPlayerAI INHERITED LOGGING REMAINS PLAYER/CULTURE/WORKER-SEA GATED
---------------------------------------------------------------------------------------------
* `CvPlayer::onTurnLogging` is wholly partitioned by Player > 0 / Player >= 2;
  the inherited score-help varargs wart therefore does not bypass the Player category.
* strategy-change macros explicitly require Player >= 2.
* crush/attack-city conversion rows require Player >= 2.
* civic decision/candidate diagnostics derive from Player >= 2/3.
* culture stage and Great-Person-weight diagnostics derive from Culture levels.
* `AI_logWorkerSeaAudit` has an unguarded helper body but both normal and Barbarian
  callers invoke it only at Worker Sea >= 3.
* conquest/raze/liberation rows use the explicit player-category boolean introduced
  after SAS's category split.
No current PlayerAI category leak survived caller tracing.

CONTROL 15 — DEBUG/GLOBAL DIAGNOSTICS ARE NOT A NORMAL CATEGORY LEAK
--------------------------------------------------------------------
ReproTest BBAI output is compile-time `ENABLE_REPRO_TEST` instrumentation and uses the
legacy/global diagnostic gate intentionally. It is not a normal release gameplay
category producer and is not used to manufacture F307.

CONTROL 16 — NEW-GAME COMPLETE MAP METADATA IS EMITTED AT THE LATE HEADER
-------------------------------------------------------------------------
`startSASBBAILogForNewGame()` intentionally logs only `BBAI_NEW_GAME_INITIALIZING`
plus mod/settings before map generation. It does NOT call the complete game-state
row there. `logSASBBAINewGameStarted()` is emitted from the exported gameStart event
and calls `logSASBBAIGameState` only after generated state exists. Map heaviness is
recomputed after map generation and again in `initFreeUnits` after starting plots so
script-list and dynamic start-area classifications are finalized before gameplay.
The source comments explicitly define this two-stage contract. Thus the initial
pre-map false/default heaviness cache is not logged as complete new-game metadata.

CATEGORY-ISOLATION RESULT
-------------------------
No shipped normal caller has yet been found that emits an unrelated category solely
because `logBBAI`'s global "any category enabled" gate is true. The SAS category split
therefore survives the completed helper/caller matrix. Keep F307 / KI#630 UNUSED.

REMAINING CURSOR AFTER WIP03
----------------------------
Queue 092 remains OPEN at 27 / 169 complete.
F306 / KI#629 confirmed; F307 / KI#630 unused.
Remaining closure work is now BBAILog-local rather than broad call-site category
reconciliation:
* same-second and cross-process filename collision semantics;
* network active-player identity changes and file naming boundaries;
* cached XML logging settings / whether any supported runtime mutation exists;
* residual string/null/encoding/filename ownership and history delta classification;
* final root-merging and closure decision.

No repository source files modified.
===============================================================================


===============================================================================
C029 DURABLE — QUEUE 092 BBAILOG.CPP CLOSED / F306 SOLE ROOT
===============================================================================

FINAL STATUS
------------
Queue 092 CvGameCoreDLL/BBAILog.cpp is FIRST-PASS COMPLETE.
Progress advances to 28 / 169 files complete.
F306 / KI#629 is CONFIRMED.
F307 / provisional KI#630 remains UNUSED and carries forward to the next file.

CONFIRMED ROOT
--------------
F306 / KI#629 — fixed-name BBAI mode reuses stale first-session UTC metadata.

`rollSASBBAILog` refreshes `g_szSASBBAILogTimestamp` only when
`SAS_BBAI_LOG_USE_TIMESTAMPED_FILENAME` is enabled. In the explicitly supported
fixed-name mode (`=0`), later new games and loaded saves continue appending to the
fixed BBAI filename as intended, but their `BBAI_NEW_GAME_INITIALIZING` /
`BBAI_NEW_GAME_STARTED` / `BBAI_SAVE_LOADED` identity rows obtain UTC from the
cached first session instead of the session currently being identified.

This is SAS-specific lifecycle behavior introduced with the per-new/load identity
metadata/session rollover work (practical 6011). The newer SASGameRecord rollover
provides the clean precedent: sample/refresh the session UTC on every roll and gate
only filename context/sequence on the timestamped-filename option. The BBAI repair
should follow that separation.

FINAL SATURATION / CONTROL LEDGER
---------------------------------
The following suspicious boundaries were explicitly challenged and did not justify
F307:

1. CATEGORY ISOLATION
   The 17 independently configurable SAS BBAI categories all participate in the
   master `isSASBBAILogEnabled` OR. A full helper/caller matrix found no normal
   shipped category-specific row that leaks merely because an unrelated category is
   enabled. Apparently bare helper bodies are caller-gated; intentional shared rows
   (e.g. Player-or-Found Barbarian-city context) remain shared by design.

2. XML SETTING LIFETIME
   BBAI master/level/interval/filename getters intentionally cache their XML values.
   `GlobalDefines_advciv_sas.xml` explicitly documents: "restart Civ4 to apply
   changes." Current shipped Python never writes `SAS_BBAI_*` defines through the
   generic `setDefine*` API. The static caches therefore match the supported config
   contract rather than defeating runtime settings.

3. SESSION/FILENAME UNIQUENESS
   Timestamped mode uses a process-local monotonically increasing `newN` / `loadN`
   context in addition to second-resolution UTC, so repeated rolls within the same
   Civ4 process remain distinct even in the same second. A collision would require
   separate OS processes producing the same player/timestamp/context tuple; that is
   external multi-process filename debt, not a normal queue-092 gameplay/logging
   failure.

4. NETWORK ACTIVE-PLAYER SUFFIX
   The inherited `BBAI<activePlayer>` network filename is intended for OOS debugging
   on one PC. The active network slot lives in CvInitCore and survives CvGame reset;
   ordinary network turns do not rotate active player like Hotseat/PBEM. The active-
   player-changing paths found are Hotseat/PBEM handoff, single-player reload,
   Rise&Fall/control transfer, AI Auto Play/debug-style transitions, or human-player
   conversion rather than normal network progression. No supported ordinary network
   sequence was established that silently splits one BBAI session across suffixes.

5. NEW/LOAD HEADER ORDERING
   New-game rollover intentionally occurs before map generation and emits only the
   initializing/settings/mod rows; complete game-state metadata is delayed until
   `gameStart`, after map/player initialization and map-heaviness recomputation.
   Load rollover occurs after deserialization. `playersEverAlive` is obtained by the
   live counting getter, and cached land/naval map classifications are serialized.
   No stale/default header field survived the ordering audit.

6. RNG OBSERVATION
   Header RNG calls use `CvRandom::getSeed`, a read-only getter. Logging session
   identity does not perturb deterministic RNG state.

7. FIXED 2048-BYTE FORMATTER
   KI#161.2 correctly guarantees a trailing NUL after MSVC 7.1 `_vsnprintf`
   truncation. Silent truncation remains possible in principle, but no credible
   current BBAI producer exceeding 2047 bytes was proven. Pessimistic all-current-
   game-options settings rows stay around ~1.05 KB; persisted deal text is limited to
   saveable ongoing trade items; other whole dynamic buffers are bounded. Keep as
   formatter ceiling/debt rather than F307.

8. FORMAT CONTRACT SWEEP
   A comments-stripped mechanical pass reconciled 816 current literal-format
   `logBBAI` calls with zero conversion-count mismatches. The initial sea-spy apparent
   mismatch was a parser false positive caused by a trailing comment and was
   explicitly rejected.

9. INHERITED CvWStringBuffer VARARGS CALL
   One inherited score logger passes a `CvWStringBuffer` object through C-style
   ellipsis where `%S` expects a wide-string pointer; standard C++ does not make that
   portable, while Civ4's fixed 32-bit MSVC ABI/layout plausibly explains why it has
   shipped working from K-Mod/Base AdvCiv. No current wrong-output/crash evidence was
   established. Preserve as portability/UB debt, not F307.

10. logBBAICancel CONTRACT
    Every current caller supplies a real canceling player and logs before deal kill
    where applicable. Temporary wide strings live through the synchronous formatter
    call. No NO_PLAYER or stale-object path survived.

11. STRING/OWNERSHIP / THREADING BOUNDARIES
    Mod/type strings come from stable owned storage; temporary CvString pointers are
    consumed within the same full expression. BBAI logging is game-thread diagnostic
    code; the static formatter buffer and C runtime `gmtime` use do not gain a current
    concurrent producer in the shipped DLL. Raw mod-context escaping remains less
    parser-robust than SASGameRecord but current Windows path constraints/current
    branded values do not establish a shipped ambiguity.

12. HISTORY / ANCESTRY
    Older AdvCiv logger behavior remains intact: deal cancellation (1577), no EXE
    log timestamps (2269), per-active-player network filename (2690), and corrected
    `BBAI.log` spelling (3579). SAS changes 5903/5925/5981/6011/6019/6326 and later
    category additions were individually reconciled. No later upstream safety repair
    was found missing from current SAS.

ROOT-MERGING / CLOSURE
----------------------
The remaining irregularities are controls, portability debt, optional-configuration
concerns, or external multi-process limitations. None supplies an independent
current producer -> violated contract -> shipped consequence chain strong enough for
F307. Queue 092 therefore closes with F306 as its sole numbered root.

NEXT CURSOR
-----------
Priority override has now been exhausted: all files previously marked
"priority next wip" have received the stronger pass. Resume ordinary pending queue
order from the first remaining file:

    queue 018
    CvGameCoreDLL/CvGameInterface.cpp
    approximately 2,965 lines in the current album snapshot

Exact next root cursor:
    F307 UNUSED / provisional KI#630 next.

No repository source files modified.
===============================================================================

===============================================================================
C029-WIP01 — QUEUE 018 DEEP RE-AUDIT CHECKPOINT
===============================================================================
FILE
----
queue 018
CvGameCoreDLL/CvGameInterface.cpp
approximately 2,965 lines
STATUS: OPEN
F307 / provisional KI#630: UNUSED
Progress remains 28 / 169 until queue 018 closes.

ROUTING CORRECTION / PRIORITY PLAN
----------------------------------
The C029 closure statement saying the priority override was exhausted was incorrect.
The reconciled album explicitly marks queues 001-009 as "<-(priority next wip)":
these were completed before the later, deeper iterative WIP methodology became the
standard. Queue 010 is the first file already covered by that stronger WIP-era pass.

Correct continuation order is therefore:

    018 CvGameInterface.cpp (current file)
    019 CvDLLButtonPopup.cpp
    001 CvPlayerAI.cpp      deep re-loop
    002 CvUnitAI.cpp        deep re-loop
    003 CvGameTextMgr.cpp   deep re-loop
    004 CvPlayer.cpp        deep re-loop
    005 CvCityAI.cpp        deep re-loop
    006 CvCity.cpp          deep re-loop
    007 CvUnit.cpp          deep re-loop
    008 CvGame.cpp          deep re-loop
    009 CvPlot.cpp          deep re-loop
    then resume ordinary pending order at queue 020.

This routing supersedes C029's "priority override exhausted" note.

QUEUE-018 METHOD / SOURCE BASIS
-------------------------------
Current SAS is authoritative. Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS are used
for ancestry and contract comparison. The queue is being re-read sequentially rather
than only diff-hunted. Suspicious shapes require a current producer, violated
contract, consequence and ancestry before receiving F307.

CURRENT CONTROL LEDGER
----------------------
1. CONTROL ENUM / ADVISOR REMAP — REJECTED AS F307
   SAS removed/remapped the old Religion/Corporation/Finance control slots and added
   Foreign-Diplomacy/World-Advisor controls, shifting later numeric ControlTypes
   relative to Base AdvCiv. Current SAS CvEnums.h and CIV4ControlInfos.xml nevertheless
   match exactly, 63/63 entries in order (Base 64/64; stock/Civ4CE 63/63).

   The EXE-facing exported path is canHandleAction(int iAction) / handleAction(int
   iAction); XML-loaded ActionInfo indices map back to DLL-owned CvControlInfo /
   ControlTypes indices. doControl(ControlTypes) itself is not an EXE export. Popup
   control IDs are written/consumed inside the same runtime and Python receives the
   current enums through CyEnumsInterface. No live references to the removed constants
   remain. The shifted numbering is therefore internally coherent.

2. FINAL-INITIALIZATION / NULL-SELECTION GUARDS — CONTROL
   SAS practical 5475 added !isFinalInitialized() guards after a reproducible
   post-load -> main-menu Sevopedia/action crash. Practical 5383 added selection-list
   null guards in action/highlight paths. K-Mod already comments that direct selection
   cycling can crash before initialization and its delayed helper guards the same
   boundary. The SAS changes are consistent crash hardening, not a gameplay regression.

3. CROSS-FILE ROOTS — DO NOT RENUMBER HERE
   - Existing F151/KI#474: same-UnitType action-cache representative can hide a valid
     free Great-General upgrade. CvGameInterface is the exported UI consumer, but the
     root is already allocated elsewhere.
   - Existing F278/KI#601: updateColoredPlots reaches CvPythonCaller::updateColoredPlots,
     whose uninitialized Python-call result root belongs to CvPythonCaller.cpp.
   - Existing F145/KI#468: all-selected Load shared-capacity defect executes through
     selectionListGameNetMessage but belongs to CvSelectionGroup logic.

4. CITY-SCREEN DOUBLE-CLICK NULL DEREFERENCE — OPEN HARDENING CONTROL, NOT F307
   Current AdvCiv/SAS handleCityScreenPlotDoublePicked has:

       if (pCity != NULL && pCity->plot() == pPlot) ...
       else if (pPlot != NULL &&
                pCity->getCityPlotIndex(*pPlot) == NO_CITYPLOT) ...

   Therefore pCity == NULL && pPlot != NULL would dereference null. Stock/K-Mod guarded
   pCity. AdvCiv practical 2416 explicitly hardened the adjacent single/right-click
   city-screen callbacks with pCity/pPlot null rejection; practical 2741 later
   re-enabled double-click outside-radius exit without carrying that guard forward.

   This is a persuasive defensive-robustness oversight, but no local evidence yet
   proves the EXE can invoke this city-screen export with non-null pPlot and null
   pCity after selection/screen teardown. Keep unnumbered unless a real producer is
   established. A one-line early null guard would still be prudent hardening.

5. CITY-SCREEN RIGHT-CLICK HEAD-CITY DEREFERENCE — SAME HARDENING CONTROL
   The callback derives pCityPlot from isCityScreenUp() ? getHeadSelectedCity()->plot()
   : NULL. Some DLL paths defensively null-check the selected city; other current UI/
   Python code treats city-screen-up as implying a selected city. No normal violating
   lifecycle state is proven. Keep with the same defensive hardening family.

6. handleDiplomacySetAIComment GET_TEAM(eOtherPlayer) — FALSE ALARM
   Although eOtherPlayer is PlayerTypes, AdvCiv deliberately overloads GET_TEAM/helper
   access for both PlayerTypes and TeamTypes. The player overload resolves via that
   player's actual team. The apparent Player-ID-as-Team-ID mismatch is not a bug.

7. UPDATECOLOREDPLOTS BOUNDED PATHFINDER REUSE — PRIOR CONTROL CONFIRMED
   Its city-site search uses max 7 and its goody search 3-5 with one reusable finder,
   but current founder is a noncombat Settler while the goody branch requires the same
   head unit to canFight(). The two phases cannot execute for the same selected head
   unit, confirming the earlier F301-side rejection.

8. SELECTION / NETWORK DISPATCH — CLEAN SO FAR
   selectionListMove, selectionListGameNetMessage, recursive automation regrouping,
   DOW-popup move replay, city-order modifiers, rapid cycling and stale-ID/null
   assumptions were challenged. Current oddities either preserve established K-Mod/
   AdvCiv behavior or rely on maintained UI-list invariants. No F307 survived.

9. FLYOUT / HIGHLIGHT / NUKE / SEA-PATROL REGION — CLEAN SO FAR
   startFlyoutMenu's pPlot and selection assumptions remain EXE/UI preconditions with
   no null producer proven. getPlotHighlightColor contains SAS's selection null guard.
   AdvCiv practical 3685's nuke-AoE/right-click behavior is present. No independent
   root established.

10. SOUNDTRACK / DISPLAY / UI-TAIL PRELIMINARY CONTROLS
    getNextSoundtrack inherited filtering/fallback remains coherent. Waiting/end-turn
    display logic matches K-Mod/AdvCiv. getEndTurnState contains K-Mod's active-team
    pitboss guard. Other active-player/team accessors still require a complete lifecycle
    producer audit before closure.

11. HUMAN DIRECT ATTACK / PATHFINDER INTERACTIONS
    Any GroupPathFinder-specific path/cost roots already allocated as F300-F305 must
    stay merged there unless this file supplies a genuinely separate interface root.

CURRENT EXACT CONTINUATION
--------------------------
Continue sequentially through the remaining CvGameInterface.cpp surface, especially:
- doControl / quick-save-load / advisor / retirement / world-builder control flow;
- flyout and exported pointer preconditions;
- widget/active-player/soundtrack/display callbacks in init/teardown/pitboss states;
- onGraphicsInitialized, city-bar/camera and all city-screen callbacks;
- full DllExport pointer/null precondition inventory;
- AdvCiv/K-Mod/Civ4CE history comparison for any upstream fix lost by SAS.

Specific unresolved shapes to challenge without presuming defects:
- cheatSpaceship NO_TECH handling (debug-only; verify current project XML);
- shouldCenterMinimap/getNextSoundtrack/onGraphicsInitialized active player/team guards;
- canDoPing NO_PLAYER contract;
- startFlyoutMenu null plot contract;
- the city-screen double/right-click lifecycle candidates above.

No repository source files modified.
===============================================================================

================================================================================
C029-WIP02 APPEND -- QUEUE 018 CvGameInterface.cpp DEEPER INTERFACE/HISTORY PASS
================================================================================

STATUS
------
Queue 018 remains OPEN.
F307 / provisional KI#630 remains UNUSED.
Files complete remain 28 / 169.
No repository source files were modified.

ROUTING REMINDER
----------------
After queue 018 closes: queue 019 CvDLLButtonPopup.cpp, then deep-method re-loop
queues 001 through 009, then resume ordinary pending queue 020.

SAS-vs-BASE FUNCTION-LEVEL SEMANTIC INVENTORY
---------------------------------------------
A comment/string-aware balanced-brace extractor compared the 69 current CvGame::
definitions in CvGameInterface.cpp with Base AdvCiv 1.14 after stripping comments
and whitespace. Only 16 functions have executable/token differences:

    canDoControl
    canHandleAction
    cheatSpaceship
    doControl
    getGlobeLayers
    getPlotHighlightColor
    handleAction
    isSoundtrackOverride
    onCityScreenChange
    setupActionCache
    updateBlockadedPlots
    updateColoredPlots
    updateNukeAreaOfEffect
    updateSeaPatrolColors
    updateSelectionList
    updateTestEndTurn

Classification so far:

* canDoControl/doControl -- intended SAS advisor/control remap; enum/XML/action
  contract reconciled 63/63 in WIP01.
* canHandleAction/handleAction/setupActionCache/getPlotHighlightColor/
  updateSelectionList/updateTestEndTurn -- current SAS crash hardening around
  final initialization and null selection groups. Historical K-Mod and SAS crash
  evidence supports these guards; no suppressed normal path found.
* cheatSpaceship -- only adds explicit TECH_ACQUISITION_DEBUG provenance to the
  expanded setHasTech API. Current spaceship XML was independently checked and
  all spaceship projects have real TechPrereq values, so no NO_TECH edge.
* getGlobeLayers/isSoundtrackOverride/updateBlockadedPlots/updateNukeAreaOfEffect/
  updateSeaPatrolColors -- lookup/define hoists only; no current live-setting
  contract was found for these GlobalDefine values.
* updateColoredPlots -- lookup hoists plus deliberate removal of old worker
  recommendation circles. City-site/goody path logic otherwise preserves Base.
* onCityScreenChange -- city-camera displacement moved from fixed 0.23/0.03 to
  SAS integer defines; null selection/north-plot guards retained.

This sharply reduces the unknown SAS-local surface. The remaining queue audit is
therefore primarily inherited K-Mod/AdvCiv interface correctness plus historical
repair reconciliation, not an unclassified large SAS fork.

K-MOD PITBOSS / LIFECYCLE HISTORY CONTROL
-----------------------------------------
K-Mod practical 773 ("Fixed some Pitboss compatibility bugs") changed only one
CvGameInterface.cpp callback: getEndTurnState. It added the explicit
getActiveTeam()!=NO_TEAM guard before GET_TEAM because PitBoss can invoke that
callback without an active team. Current SAS retains the guard exactly in
substance.

No corresponding historical evidence has been found that the other soundtrack,
display, minimap or city-screen callbacks are invoked in the same headless state.
Therefore their inherited active-player/team assumptions are not being promoted
merely by analogy to the one observed PitBoss defect.

OTHER UPSTREAM FIXES RECONCILED
-------------------------------
* AdvCiv practical 2636 game-start action-recommendation fix remains present:
  updateColoredPlots returns while getTurnSlice()<=0, before player options have
  been initialized.
* practical 2725 suppresses Waiting Others while the active slot is not human;
  current code retains this AI-Auto-Play behavior.
* practical 2827 guards getCycleButtonUnit against NO_PLAYER / nonhuman active
  player; present.
* practical 3191 handles player options as human-only during AI Auto Play;
  updateColoredPlots keeps the corresponding human-state condition.
* practical 3917 calls onActiveSelection from updateSelectionList; present.
* practical 4063 cycle-order refresh work remains present.
* K-Mod practicals 306/392/481/497/699/706/712/724/734/739/835/1042/1049
  previously catalogued in WIP01 remain represented in the current control/
  selection path.

CAN-DO / DO-CONTROL MATCHED PASS -- NO F307
--------------------------------------------
canDoControl and doControl were reviewed as a pair. Current execution branches
that need optional selection/capital/city state either have matching enable-state
contracts or local null checks. The SAS advisor control cases resolve to live
Python entrypoints already reconciled in WIP01.

Controls that look odd but are not current findings:
* CONTROL_SELECT_HEALTHY selects hurt units. This is inherited stock/K-Mod
  historical naming/behavior, not a SAS regression.
* NEXTCITY/PREVCITY both call selectLookAtCity(true) when no city is selected.
  The parameter is "bAdd", not traversal direction; stock/Civ4CE have the same
  calls.
* doControl is not an EXE export; current external routes are action dispatch,
  CyGame and known DLL/UI call sites. canHandleAction/handleAction carry the
  final-init guards added for teardown crashes.

SELECTION / UNIT-LIST PASS -- CONTROL
-------------------------------------
selectionListMove, selectionListGameNetMessage, selectedCitiesGameNetMessage,
cyclePlotUnits, getPlotUnits and cycleSelectionGroups were reviewed for stale ID,
mutation-during-iteration and ownership assumptions.

* Selected-city iteration explicitly re-resolves IDs and skips null/wrong-owner
  cities.
* Selected-unit message iteration is inherited K-Mod UI-list behavior. It advances
  the list node before sending commands where mutation is possible. No producer of
  a stale live-selection node was established; do not infer a finding from the
  absence of an extra ::getUnit null check alone.
* cyclePlotUnits has no pPlot null guard, but all current DLL callers null-check the
  plot; the Python wrapper is currently unused by shipped Python. This is API
  hardening debt only.
* getPlotUnits handles pPlot==NULL. Its active-team visibility assumption remains
  an EXE/game-UI contract; no main-menu producer was found.

FLYOUT BUILD/APPLY TIME-GAP PASS -- NO F307
-------------------------------------------
startFlyoutMenu and applyFlyoutMenu were checked specifically for stale pointers
between menu construction and execution.

* applyFlyoutMenu resolves the plot coordinates afresh and returns if the plot is
  unavailable.
* City actions re-resolve getPlotCity and re-check active-player ownership.
* FLYOUT_SELECT_UNIT stores only an ID and re-resolves it from the current active
  player; a disappeared unit is ignored.
* FLYOUT_MOVE_TO re-enters selectionListMove rather than retaining a pathfinder or
  selected-unit pointer from menu construction.
* Wake/Fortify/Sleep walk the current plot units at application time. Optimistic
  commands can become illegal after menu construction, but mission/command
  execution revalidates legality; no use-after-free contract was found.

CITY-SCREEN CALLBACK CONTROL (STILL UNNUMBERED)
------------------------------------------------
The strongest unnumbered hardening candidate remains
handleCityScreenPlotDoublePicked:

    if (pCity != NULL && pCity->plot() == pPlot) ...
    else if (pPlot != NULL && pCity->getCityPlotIndex(*pPlot) == NO_CITYPLOT) ...

pCity==NULL with nonnull pPlot would dereference null. Stock/K-Mod guarded pCity,
AdvCiv practical 2416 explicitly hardened adjacent click callbacks, and practical
2741 later added this double-click behavior without carrying the same guard.

However, no local evidence proves that the EXE emits this city-screen callback
with a nonnull plot after the selected city has already been cleared. The normal UI
contract still appears to pair an up city screen with a selected city. Therefore it
remains defensive hardening debt, not F307.

The neighboring right-click head-selected-city dereference is kept in the same
control bucket for the same reason.

NEXT
----
Continue queue-018 history/export saturation:
1. classify remaining AdvCiv bugfix commits touching this file for lost/partial
   repairs;
2. finish DllExport callback preconditions and active-player/team lifecycle;
3. inspect loadBuildQueue / soundtrack / initSelection / display and city-screen
   tail contracts;
4. re-check current-vs-Base deltas for any dynamic-setting or initialization
   assumption not yet classified;
5. close cleanly with F307 unused if no current producer+contract+consequence root
   survives.

================================================================================
C029-WIP03 APPEND -- QUEUE 018 CvGameInterface.cpp HISTORY / EXPORT SATURATION
================================================================================

STATUS
------
Queue 018 remains OPEN.
F307 / provisional KI#630 remains UNUSED.
Files complete remain 28 / 169.
No repository source files were modified.

ROUTING REMINDER
----------------
After queue 018: queue 019 CvDLLButtonPopup.cpp, then deep-method re-loop queues
001 through 009, then resume ordinary pending queue 020.

ADDITIONAL ADVCIV BUGFIX-HISTORY RECONCILIATION
-----------------------------------------------
A title-indexed pass over every K-Mod/AdvCiv history diff touching
CvGameInterface.cpp was used to prioritize explicit fix/crash/bug commits rather
than relying only on current-source shape.

Previously catalogued fixes remain present. Newly rechecked examples include:
* AdvCiv 1383 DOW-move handling for enemy+neutral/third-party stacks remains in
  selectionListGameNetMessage.
* 1385 restored previous/next-city selection after an earlier procedure crash;
  current calls match the subsequently-working form.
* 1387's debug-only guard belongs to cheatSpaceship, not loadBuildQueue, and
  remains present.
* 1448 multiplayer pre-build modifier transmission remains represented by the
  current sendPushMission bModified argument.
* 1449's selection-message ownership/structure cleanup survives.
* 1680 blockade-range repair survives through the later blockadeRange helper.
* 1773 hurt-unit selection refactor survives.
* 2092 city-cycle forward/backward fix survives.
* 2210 unit-model display uses getHeadSelectedCity rather than merely
  isCityScreenUp, preserving the old 3D-model repair.
* 2636 game-start recommendation early return survives.
* 2725 AI-Auto-Play waiting-text suppression survives.
* 2827 NO_PLAYER/nonhuman cycle-button guard survives.
* 3296 Alt+Z player switching uses changeHumanPlayer as intended.
* 3765 plot-list shift reset, 3917 active-selection callback and 4063 cycling
  refresh changes all remain present.
No old explicit bugfix has yet been found to have been silently lost by SAS.

KI#5383 ACTION-GUARD CHALLENGE -- REJECTED
------------------------------------------
A potentially serious SAS-local concern was tested: the null-selection-list guard
added after the CvSelectionGroup::plot crash might have disabled control-only
ActionInfo entries (advisor hotkeys, options, save/load) whenever no unit group is
selected.

The current ordering disproves this. canHandleAction first performs:

    if (kAction.getControlType() != NO_CONTROL && canDoControl(...))
        return true;

Only after that does it require a head selected unit and selection list for
mission/command/interface-mode actions. Therefore control-only actions never reach
the new selection-list null guard. handleAction likewise executes doControl before
checking the selection list for interface-mode processing. The KI#5383 guard is
narrowly placed around unit-group-dependent behavior; no regression established.

CIV4CE INDEPENDENT COMPARATOR PASS
----------------------------------
Civ4CE's BtS CvGameInterface.cpp was compared specifically on the exported/tail
contracts being challenged. It does not supply an independent modern guard that
would overturn current classifications:
* city-screen double click preserves stock's pCity!=NULL outer guard;
* neighboring city callbacks retain stock-style checks;
* soundtrack, initSelection, canDoPing, minimap/display callbacks keep the same
  ordinary active-player/team assumptions as stock BtS;
* its getEndTurnState is stock-like and lacks K-Mod's later PitBoss repair.

This supports two separate conclusions:
1. K-Mod's explicit PitBoss guard is a targeted observed engine-state repair and
   remains a better lifecycle oracle than speculative null guarding everywhere.
2. AdvCiv practical 2741 really did weaken the stock/K-Mod double-click null shape,
   but current local material still does not prove the EXE invokes that export with
   pCity==NULL and pPlot!=NULL during normal play. Keep as hardening debt, not F307.

NUKE AOE VISUAL-STATE CHALLENGE -- OPEN CONTROL, NO FINDING
-----------------------------------------------------------
AdvCiv practical 3685 introduced AREA_BORDER_LAYER_NUKE. updateNukeAreaOfEffect
returns before clearing that layer when no selected nuke exists; the only explicit
clear of AREA_BORDER_LAYER_NUKE in current source is inside this function after the
selected-nuke check. This creates a theoretical stale-overlay concern if selection
changes before the interface invokes the usual "still-selected nuke + null center"
clear path.

No normal UI transition ordering proving such a stale overlay has yet been found.
The common interface-mode transition can call getNewHighlightPlot/updateNukeArea-
OfEffect while the nuke remains selected, which clears the layer. Therefore this
remains an inherited visualization control unless a reproducible selection/mode
sequence is established.

STATIC DEFINE HOISTS -- CONTROL
-------------------------------
SAS made SHOW_UNIT_LAYER_OPTIONS and VICTORY_SOUNDTRACK_AVAILABLE function-local
statics and similarly cached the two SAS city-camera percentage defines. Current
source contains no runtime setter for those defines, and the relevant values are
XML/configuration policy rather than BUG options advertised as live toggles. The
BUG EnlargeHUD choice remains queried dynamically. No supported live-setting
contract is currently violated.

CURRENT CONTINUATION
--------------------
Finish inherited/export saturation rather than chase already-rejected SAS deltas:
* active-player/team assumptions in loadBuildQueue, soundtrack, initSelection,
  minimap/display and onGraphicsInitialized;
* remaining DOW/selection-list message edge contracts;
* nuke-overlay transition ordering if local evidence can prove or kill it;
* city-screen double/right-click lifecycle producer proof;
* final whole-file source/history/control merge.

Promote F307 only with current producer + violated contract + concrete consequence.
Otherwise close queue 018 cleanly with F307 unused and advance to queue 019.

================================================================================
C029-WIP04 APPEND -- QUEUE 018 CvGameInterface.cpp LIFECYCLE / TAIL SATURATION
================================================================================

STATUS
------
Queue 018 remains OPEN for the final whole-file merge/closure decision.
F307 / provisional KI#630 remains UNUSED.
Files complete remain 28 / 169.
No repository source files were modified.

SOURCE / CONTINUITY
-------------------
Authoritative current tree for this pass:
    AdvCiv-SAS_light_source_20260829T201730

Comparators used locally:
    AdvCiv 1.14
    K-Mod 1.46
    Civ4CE BtS source
    current reachable-history cache / commit diffs

This append continues C029-WIP03 rather than restarting queue 018.
Routing remains:
    018 -> 019 -> deep-method re-loop 001 through 009 -> 020.

ACTIVE-PLAYER / TEAM EXPORTED TAIL -- CONTROL CLOSED
---------------------------------------------------
The remaining soundtrack, selection, ping, display, minimap and end-turn exports
were compared directly with Base AdvCiv, K-Mod and Civ4CE rather than being judged
from raw GET_PLAYER/GET_TEAM dereferences alone.

Current SAS is semantically identical to Base AdvCiv 1.14 for:
* getNextSoundtrack
* getSoundtrackSpace
* initSelection
* canDoPing
* shouldDisplayEndTurn
* shouldDisplayWaitingOthers
* shouldDisplayWaitingYou
* shouldDisplayEndTurnButton
* shouldCenterMinimap
* getEndTurnState

The ordinary active-player/team assumptions in these functions are also inherited
from stock/K-Mod/Civ4CE. K-Mod practical 773 is the important lifecycle oracle:
it specifically fixed getEndTurnState because PitBoss can have NO_TEAM and left the
other ordinary live-game callbacks under their existing active-player contracts.
Current SAS retains that exact NO_TEAM guard.

Therefore the absence of generic NO_PLAYER checks in every one of these callbacks
is not promoted by itself. A producer that actually invokes one in a no-active-
player state would still be actionable, but no such current producer was found.

loadBuildQueue was challenged separately. SAS now dereferences
*getActiveCivilization(), but stock/K-Mod/Civ4CE also require a valid active
civilization in this exported build-queue callback. No SAS path broadens that
producer to menus/PitBoss/no-active-player state. Keep as an EXE/UI precondition,
not F307.

ON-GRAPHICS-INITIALIZED ACTIVE-PLAYER CHALLENGE -- CONTROL CLOSED
----------------------------------------------------------------
CvGlobals::SetGraphicsInitialized(true) immediately calls
CvGame::onGraphicsInitialized(), whose current camera-start repair dereferences the
active player.

History matters here:
* AdvCiv practical 2209 introduced the GET_PLAYER(getActivePlayer()).getNumUnits()
  camera-start repair into onGraphicsInitialized.
* Later camera-distance work kept that contract, and practical 4268 merely moved
  the function into CvGameInterface.cpp.
* SAS's temporary city-billboard experiment touched this callback and was later
  reverted; it did not alter the active-player assumption.

No local call path was found that sets graphics initialized true while the game has
NO_PLAYER. The callback has also been a long-standing normal new-game/load boundary,
whereas K-Mod separately hardened lifecycle callbacks that really did fail in
PitBoss/no-active-team state. Without a current violating producer, this remains a
maintained engine lifecycle contract rather than a crash finding.

CURRENT SAS-vs-BASE SEMANTIC DELTA INVENTORY -- FULLY CLASSIFIED
---------------------------------------------------------------
A fresh function-body comparison (comments/whitespace ignored) finds 15 current
CvGame definitions in CvGameInterface.cpp with semantic SAS-vs-Base AdvCiv deltas:

    updateColoredPlots
    updateBlockadedPlots
    updateSelectionList
    updateTestEndTurn
    canHandleAction
    setupActionCache
    handleAction
    canDoControl
    getGlobeLayers
    getPlotHighlightColor
    updateNukeAreaOfEffect
    updateSeaPatrolColors
    cheatSpaceship
    isSoundtrackOverride
    onCityScreenChange

All 15 are now classified:
* updateColoredPlots: cached color lookups plus deliberate removal of old debug AI
  improvement-replacement circles; no gameplay control flow is lost.
* updateBlockadedPlots / updateNukeAreaOfEffect / updateSeaPatrolColors: color lookup
  hoists only.
* updateSelectionList / updateTestEndTurn: teardown/final-initialization crash guards
  already reconciled as valid lifecycle hardening.
* canHandleAction / setupActionCache / handleAction / getPlotHighlightColor: null
  selection-list/final-init guards added for known UI teardown/selection crashes;
  control-only actions are processed before the selection-list gate, so advisors,
  options and save/load are not suppressed by the repair.
* canDoControl: advisor/control remap already proven coherent through enum, XML,
  ActionInfo and Python entrypoints.
* getGlobeLayers / isSoundtrackOverride: function-local static XML-define hoists;
  current shipped source contains no runtime setter/live BUG-option contract for
  SHOW_UNIT_LAYER_OPTIONS or VICTORY_SOUNDTRACK_AVAILABLE.
* cheatSpaceship: explicit TECH_ACQUISITION_DEBUG provenance only; current spaceship
  project data also provides real prerequisite techs.
* onCityScreenChange: hardcoded AdvCiv camera weights replaced by SAS XML defines;
  EnlargeHUD itself remains dynamically queried, while the SAS percentage defines
  are configuration-time values.

Thus no unclassified SAS-local semantic delta remains in this file.

DOW / SELECTION-LIST MESSAGE EDGE PASS -- CONTROL CLOSED
--------------------------------------------------------
selectionListMove and selectionListGameNetMessage were reread together with the
BUTTONPOPUP_DECLAREWARMOVE reply path in CvDLLButtonPopup.cpp solely to close the
queue-018 continuation contract (not to pre-audit queue 019).

Current protections:
* selectionListMove requires a current head unit owned by the active player.
* selectionListGameNetMessage repeats that head-unit/ownership precondition.
* the initial DOW popup stores coordinates from an already-resolved current map plot.
* on popup reply, CvDLLButtonPopup re-resolves the current head unit; if it is gone,
  the move is not replayed.
* the replay then returns through selectionListGameNetMessage with MOVE_DECLARE_WAR,
  so the current selection is checked again rather than retaining an old unit
  pointer from popup construction time.

The coordinates remain valid because map dimensions do not change while this popup
is pending. No current stale-pointer / invalid-plot consequence survives here.
Popup-specific diplomacy/state assumptions remain for the full queue-019 audit.

NUKE AOE LAYER -- STRUCTURALLY PLAUSIBLE, STILL UNNUMBERED
----------------------------------------------------------
The deeper pass strengthens the shape but still does not prove the EXE ordering.

Current asymmetry:
* updateSeaPatrolColors clears AREA_BORDER_LAYER_PATROLLED before checking whether
  the selected unit is currently patrolling, and updateSelectionList explicitly
  clears that layer when all units become unselected.
* updateNukeAreaOfEffect first obtains the head selected unit and returns immediately
  when it is NULL or no longer a nuke; only after that does it clear
  AREA_BORDER_LAYER_NUKE.
* AREA_BORDER_LAYER_NUKE has no other explicit clear site in current C++.

Current NUKE InterfaceMode XML has bHighlightPlot=1. While the nuke remains selected,
leaving the targeting mode can reach a non-highlight-mode getNewHighlightPlot call
and clear the layer. The dangerous ordering would instead be:
    nuke radius painted -> selection changes to NULL/non-nuke first -> cleanup call
at which point updateNukeAreaOfEffect returns before clearing.

The DLL itself allows unit-cycle controls while not focused and those controls call
UI.selectUnit without an explicit DLL-side interface-mode reset. That makes the
ordering worth a practical UI test, but selectUnit and interface-mode transition
ordering live in the EXE-facing interface; local source still cannot establish
whether the EXE resets the mode / requests cleanup before or after changing the head
unit.

Therefore this remains a strong visualization-hardening candidate, not F307.
A robust local hardening would simply clear AREA_BORDER_LAYER_NUKE before the
selected-nuke early return. Suggested manual reproduction if desired later:
1. select a nuclear unit;
2. enter Nuke mode and hover a legal target until the yellow AOE is painted;
3. cycle/select a different unit (and separately test firing/consuming the nuke);
4. observe whether the yellow AOE ever remains stuck afterward.

CITY-SCREEN DOUBLE/RIGHT-CLICK LIFECYCLE -- STATUS UNCHANGED
-------------------------------------------------------------
The strongest crash-shaped candidate remains AdvCiv practical 2741's double-click
callback:

    if (pCity != NULL && pCity->plot() == pPlot) ...
    else if (pPlot != NULL && pCity->getCityPlotIndex(*pPlot) == NO_CITYPLOT) ...

Stock/K-Mod/Civ4CE guard pCity, and practical 2416 explicitly hardened the adjacent
single/right-click callbacks before practical 2741 added this new branch without the
equivalent guard. A pCity==NULL, pPlot!=NULL callback would therefore crash.

The final local lifecycle pass still does not establish such an EXE producer. The
callback's semantics identify pCity as the city currently being worked/viewed, and
normal city-screen operation pairs that callback with a selected city. CvCity::kill
clears selected cities before destruction, but no evidence shows the EXE then emits
this city-screen callback with the old screen context but null pCity.

The right-click callback's
    isCityScreenUp() ? getHeadSelectedCity()->plot() : NULL
has the same unsupported teardown-state dependency. It could cheaply use pCity->plot()
after the existing pCity guard, but no normal screen-up/no-head-city producer was
proven.

Both remain worthwhile defensive hardening, neither is numbered under the current
producer+contract+consequence standard.

FINAL MERGE STATE
-----------------
The major queue-018 branches now reconcile as follows:
* SAS semantic deltas: saturated / classified, no new root.
* inherited selection/action/network/DOW code: saturated to current local evidence,
  no new root.
* active-player/team/PitBoss/display lifecycle: saturated, targeted upstream guard
  retained, no new root.
* explicit historical K-Mod/AdvCiv bugfixes: no silently-lost repair found.
* city callback null shape: real hardening defect shape, producer unproven.
* nuke AOE stale-layer shape: real cleanup asymmetry, EXE ordering unproven.

F307 therefore remains deliberately unused at this checkpoint.

EXACT NEXT CONTINUATION
-----------------------
Only the final whole-file closure reconciliation remains for queue 018:
1. merge C029-WIP01 through WIP04 controls against the 66 current CvGame function
   definitions and prior album roots so nothing was skipped or double-numbered;
2. make the final keep/reject decision on the two unnumbered hardening candidates
   using the same conservative producer standard;
3. if no new concrete evidence appears, close queue 018 cleanly with F307 unused;
4. open queue 019 CvDLLButtonPopup.cpp;
5. after queue 019, jump to the deep-method re-loop at queue 001 as recorded above.

Prefer a clean no-finding closure over promoting either candidate from source shape
alone.

===============================================================================
C029-WIP05 APPEND -- QUEUE 018 CvGameInterface.cpp WHOLE-FILE FUNCTION LEDGER
===============================================================================
PURPOSE
-------
This checkpoint performs the final whole-file accounting pass promised by WIP04.
It does not reopen already-reconciled branches merely because they are inherited.
The goal is to prove that every current CvGame function definition in this physical
file belongs to an audited branch before making the final queue-018 closure decision.

BOOKKEEPING CORRECTION -- 69 DEFINITIONS, NOT 66
-------------------------------------------------
A fresh brace-balanced definition extraction over the current 2026-08-29 SAS source
finds 69 `CvGame::` definitions in CvGameInterface.cpp. WIP04's "66 current CvGame
function definitions" line was therefore an extraction/accounting undercount.

A fresh comments/whitespace-normalized comparison against supplied Base AdvCiv 1.14
finds 16 current SAS-semantic function differences, not 15. The omitted function from
the WIP04 written 15-name delta list was `doControl`. This is only a ledger omission:
`doControl` had already received the same deep advisor/control-remap, save/load,
world-builder, city/unit cycling, popup and action-contract audit as `canDoControl`.
No previously unaudited SAS behavior was discovered by correcting the counts.

The 16 SAS-vs-Base semantic-delta definitions are:
    updateColoredPlots
    updateBlockadedPlots
    updateSelectionList
    updateTestEndTurn
    canHandleAction
    setupActionCache
    handleAction
    canDoControl
    doControl
    getGlobeLayers
    getPlotHighlightColor
    updateNukeAreaOfEffect
    updateSeaPatrolColors
    cheatSpaceship
    isSoundtrackOverride
    onCityScreenChange

All 16 remain classified exactly as in WIP01-WIP04: deliberate visualization/UI
changes, initialization/null crash hardening, the proven-safe control/advisor remap,
configuration-time static define/color lookup hoists, explicit debug tech provenance,
or the still-unnumbered Nuke-layer cleanup asymmetry. There is no unclassified
SAS-local semantic delta left in queue 018.

69-DEFINITION AUDIT LEDGER
--------------------------
The complete current function surface reconciles into these already-audited groups.
Names are listed explicitly so later re-audits can tell that absence from a prose
example did not mean absence from the pass.

A. VISUALIZATION / SELECTION UPDATE ENTRYPOINTS (4)
    updateColoredPlots
    updateBlockadedPlots
    updateSelectionList
    updateTestEndTurn

The visualization layers, selected-city/unit node handling, final-initialization
teardown guards, worker recommendation removal, city-site/goody overlays and end-turn
selection updates were reviewed in WIP01-WIP04. No new root.

B. PLOT / CITY / UNIT CYCLING AND NETWORK SELECTION DISPATCH (11)
    getPlotUnits
    cycleCities
    getNextGroupInCycle
    getCycleButtonUnit
    cycleSelectionGroups
    cycleSelectionGroups_delayed
    cyclePlotUnits
    selectCity
    selectionListMove
    selectionListGameNetMessage
    selectedCitiesGameNetMessage

Fresh closure challenge:
* `getPlotUnits` tolerates NULL pPlot and only walks live plot unit nodes; its active
  team/player values are observation/filter inputs.
* `cycleCities` has a raw active-player fallback, but current control producers reach
  it only from live city-selection controls; no shipped Python producer invokes it
  during init/teardown despite its historical Python exposure.
* `getCycleButtonUnit` explicitly rejects NO_PLAYER / AI Auto Play before entering the
  shared group-cycle helper. `cycleSelectionGroups_delayed` independently carries the
  historical isFinalInitialized + NO_PLAYER guard because this helper really is called
  during new-game setup.
* `cyclePlotUnits` requires a caller-supplied plot and all shipped current callers pass
  a resolved non-NULL selection plot. No current producer passes NULL.
* selection/network dispatch re-resolves current selected objects and ownership. The
  DOW popup replay edge was separately reconciled in WIP04.
* `selectedCitiesGameNetMessage` resolves each selected city ID before sending its net
  message and skips stale/non-active-player city IDs. Current callers use task/order
  messages; no retained raw city pointer is passed through the network layer.

No current producer -> violated contract -> consequence chain survives in this group.

C. ACTION / CONTROL DISPATCH (5)
    canHandleAction
    setupActionCache
    handleAction
    canDoControl
    doControl

This group includes the corrected 16th SAS semantic delta (`doControl`). The final
pairwise check confirms:
* control actions are handled before the selection-list-dependent mission/interface
  paths, so the recent NULL selection-list repair does not disable advisors/options/
  save-load controls;
* the 63/63 current ControlTypes enum <-> CIV4ControlInfos.xml ordering is exact;
* the EXE-facing path is ActionInfo-index based rather than a hardcoded shifted
  ControlTypes numeric contract;
* removed Religion/Corporation/Finance controls and new Foreign-Diplomacy/World
  controls have matching current Python entrypoints; Espionage intentionally routes
  through the compatibility screen router;
* control enable predicates and execution-side pointer assumptions were read as pairs.

No F307.

D. RETIRE / WORLD-BUILDER / GLOBE CONTROL (5)
    retire
    enterWorldBuilder
    exitToMenu
    setGlobeView
    getGlobeLayers

`retire` and `enterWorldBuilder` are reached behind their `canDoControl` contracts;
popup integer control IDs are produced and consumed within the same current enum/XML
runtime. `exitToMenu` and `setGlobeView` are thin interface calls. `getGlobeLayers`'s
only SAS semantic delta is the already-classified configuration-time static define
hoist. No current runtime define setter was found for SHOW_UNIT_LAYER_OPTIONS.

E. FLYOUT / HIGHLIGHT / AREA-LAYER SURFACE (6)
    startFlyoutMenu
    applyFlyoutMenu
    getNewHighlightPlot
    getPlotHighlightColor
    updateNukeAreaOfEffect
    updateSeaPatrolColors

Flyout construction/execution was challenged for the menu-construction -> later-click
time gap. Execution re-resolves plots, cities and unit IDs and checks active ownership;
no construction-time gameplay pointer is retained into applyFlyoutMenu. The EXE flyout
producer supplies a plot; no shipped Python producer exists for a NULL-plot call.

The Nuke AOE cleanup asymmetry remains exactly as WIP04 recorded: structurally worth
hardening/testing, but the EXE's selection/interface-mode transition ordering is still
missing, so no current stale-layer producer is proven. Sea Patrol explicitly clears its
layer and does not establish the Nuke ordering by analogy alone.

F. BUILD-QUEUE / CHEAT / EXE DATA HELPERS (7 definitions; 6 logical names)
    loadBuildQueue
    cheatSpaceship
    getUnitDomain
    getBuildingArtInfo
    isWaterBuilding
    getWidgetShow(BonusTypes)
    getWidgetShow(ImprovementTypes)

`loadBuildQueue` dereferences getActiveCivilization by design, but its EXE build-queue
callback contract is an active-civilization UI operation; no current shipped Python
producer calls it in a NO_PLAYER state. The raw Unit/Building/Bonus/Improvement enum
helpers are likewise EXE query helpers whose callers supply typed current-info IDs.
There is no current source producer of NO_* sentinel IDs into them. `cheatSpaceship`
remains debug-only and current spaceship projects all carry real prerequisite techs.

G. ACTIVE-PLAYER / SOUNDTRACK / DISPLAY CALLBACKS (17)
    nextActivePlayer
    getNextSoundtrack
    getSoundtrackSpace
    isSoundtrackOverride
    initSelection
    canDoPing
    shouldDisplayReturn
    shouldDisplayEndTurn
    shouldDisplayWaitingOthers
    shouldDisplayWaitingYou
    shouldDisplayEndTurnButton
    shouldDisplayFlag
    shouldDisplayUnitModel
    shouldShowResearchButtons
    shouldCenterMinimap
    getEndTurnState
    onGraphicsInitialized

Fresh closure challenge concentrated on raw GET_PLAYER(getActivePlayer()) /
GET_TEAM(getActiveTeam()) uses. These callbacks are overwhelmingly unchanged from Base
AdvCiv and are live-game display/audio/selection callbacks; ancestry alone is not the
reason for rejection, but it supplies useful lifecycle evidence:
* K-Mod's observed PitBoss failure in `getEndTurnState` received the targeted NO_TEAM
  guard, and current SAS retains it exactly.
* no supplied upstream history shows the other display callbacks being called in the
  same no-active-team PitBoss state; there is also no current SAS/Python producer that
  newly broadens their lifecycle.
* shipped Python calls `shouldShowResearchButtons` from CvMainInterface during ordinary
  interface updates. No shipped Python producer was found for `nextActivePlayer`,
  `loadBuildQueue`, `cycleCities`, or the city-screen double-click callback.
* `onGraphicsInitialized` is invoked only on CvGlobals' false->true graphics flag
  transition. The callback and its active-player camera workaround have existed across
  long AdvCiv history; no local path setting graphics initialized true with NO_PLAYER
  was established.
* `canDoPing` receives the pinging player through the stock/K-Mod EXE callback contract;
  no SAS producer supplies NO_PLAYER.

Therefore raw active-player syntax alone still does not justify F307.

H. CITY-BAR / CITY-SCREEN / DIPLOMACY / SCREEN-STATE CALLBACKS (14)
    setCityBarWidth
    handleCityScreenPlotPicked
    handleCityScreenPlotDoublePicked
    handleCityScreenPlotRightPicked
    handleCityPlotRightPicked
    handleMiddleMouse
    handleDiplomacySetAIComment
    setHelpTextAreaWidth
    setDawnOfManShown
    isAboutToShowDawnOfMan
    setScreenDimensions
    getScreenWidth
    getScreenHeight
    onCityScreenChange

The simple screen-state setters/getters and city-bar entity reload are bounded. The
single-click city-screen callback explicitly rejects NULL city/plot. The right-click
callback has the previously recorded screen-up/head-city assumption. The double-click
callback retains the strongest crash-shaped hardening candidate in the file: practical
2741 can dereference pCity when pCity==NULL && pPlot!=NULL. Stock/K-Mod/Civ4CE guard the
city pointer, and practical 2416 hardened neighboring callbacks. However no current
EXE transition producing that argument pair has been established locally, so the
producer criterion remains missing.

`handleDiplomacySetAIComment`'s apparently suspicious GET_TEAM(eOtherPlayer) remains a
confirmed false alarm because AdvCiv's overloaded team helper accepts PlayerTypes and
resolves through the player's team.

FULL-SURFACE RESULT AT WIP05
----------------------------
All 69 current `CvGame::` definitions now belong to an explicit audit group. Correcting
the earlier 66/15 accounting to 69/16 did not uncover a skipped semantic function.

Remaining unnumbered items are still only:
1. city-screen double-click NULL hardening -- violated shape proven, EXE producer not;
2. Nuke AOE layer cleanup ordering -- cleanup asymmetry proven, EXE ordering not.

Neither meets the established current producer + violated contract + consequence
threshold. F307 / provisional KI#630 therefore remains UNUSED at WIP05.

EXACT NEXT CONTINUATION
-----------------------
Do one final root-merging pass against prior album findings / known issues so neither
candidate is a duplicate manifestation of an already-numbered root. If no overlap or
new producer evidence changes the result, close queue 018 cleanly with no new finding,
advance progress to 29 / 169, and open queue 019 CvDLLButtonPopup.cpp. Preserve the
routing after queue 019: deep re-loop 001 -> 009, then resume at 020.

No repository source files modified.
===============================================================================



================================================================================
C030 DURABLE -- QUEUE 018 CvGameInterface.cpp CLOSED / QUEUE 019 OPENED
================================================================================

STATUS
------
Queue 018 CvGameCoreDLL/CvGameInterface.cpp -- DEEP PRIORITY AUDIT COMPLETE.
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp -- OPEN.
Files complete: 29 / 169.
F307 / provisional KI#630 remains UNUSED and carries forward to queue 019.
No repository source files were modified.

WHY QUEUE 018 CLOSES WITHOUT A NEW NUMBERED ROOT
------------------------------------------------
The final C029-WIP05 function-ledger and root-merge passes saturated the current
CvGameInterface.cpp surface without producing a current producer + violated
contract + concrete shipped consequence chain that survives challenge.

The closure deliberately keeps the result modest.  Several source shapes are
worth defensive hardening, but the audit standard is not "a pointer could be
null if an undocumented caller misbehaves" or "an overlay might persist under
an engine ordering we cannot establish."  Those remain controls until a real
producer/order is demonstrated.

FINAL WHOLE-FILE RECONCILIATION
-------------------------------
1. Current SAS contains 69 CvGame:: definitions in CvGameInterface.cpp.  The
   earlier WIP04 note saying 66 was an extraction/accounting undercount; WIP05
   rebuilt the ledger brace-by-brace and reconciled all 69 definitions.

2. Current SAS has 16 comments/whitespace-normalized semantic function deltas
   versus Base AdvCiv 1.14, not the 15 listed in WIP04.  The missing ledger name
   was doControl().  doControl had already been audited deeply together with
   canDoControl, so this corrected bookkeeping did not expose an unaudited
   behavior branch.

3. All 16 SAS semantic deltas are classified.  They reduce to the already
   reviewed advisor/control remap, final-initialization/null crash guards,
   cached color/config lookups, intentionally removed worker recommendation
   visualization, explicit debug tech-acquisition provenance, and camera/UI
   tuning.  No unclassified SAS-local semantic branch remains.

4. The inherited helper pass separately challenged functions that do not differ
   from Base but contain stronger-looking engine assumptions: city/unit cycle
   callbacks, build-queue and raw-info query helpers, active-player display and
   soundtrack callbacks, flyout inputs, city-screen callbacks, and graphics
   initialization.  No current SAS/Python producer was found that violates
   those engine-facing preconditions.

5. K-Mod's PitBoss history remains an important negative control.  The callback
   actually observed upstream in a no-active-team state, getEndTurnState(), was
   specifically hardened with a NO_TEAM check, and that fix survives in SAS.
   There is still no evidence justifying speculative blanket NO_PLAYER findings
   for the rest of the live-interface display tail.

6. Selection/network and delayed flyout execution remain safe against stale raw
   object retention.  Stored IDs/coordinates are re-resolved, ownership/current
   selection is checked on execution, and network task receivers resolve the
   current city/unit state again.  Optimistic UI commands can become illegal
   after construction, but the game/network receivers revalidate them rather
   than dereferencing construction-time pointers.

7. The advisor/control enum remap remains strongly controlled.  Current SAS
   ControlTypes and CIV4ControlInfos ordering are synchronized; the EXE-facing
   action path resolves through ActionInfo, while control sub-IDs remain
   DLL/XML-owned.  The Espionage control is a deliberate compatibility route
   into the merged Foreign/Diplomacy advisor rather than a stale screen entry.

8. Prior-root merge found no duplicate root hiding behind the remaining
   visualization/callback controls.  In particular, old F278/KI#601 is about
   CvPythonCaller::updateColoredPlots consuming an uninitialized Python result
   after callback failure; it is a different root and does not convert any of
   the queue-018 controls below into a current finding.

UNNUMBERED CONTROL A -- CITY-SCREEN DOUBLE-CLICK NULL HARDENING
---------------------------------------------------------------
Current AdvCiv/SAS handleCityScreenPlotDoublePicked can dereference pCity in the
second branch when pCity == NULL and pPlot != NULL.  Stock/K-Mod/Civ4CE guard
pCity, and AdvCiv practical 2416 tightened the neighboring city-screen callbacks
before practical 2741 re-enabled double-click behavior without carrying an
analogous guard forward.

That ancestry makes a defensive guard attractive, for example rejecting either
NULL input before entering the body.  The missing audit fact is still the EXE
producer: local source/history does not demonstrate that normal play can invoke
this exported callback with a live plot and no city after city-screen selection
teardown.  Therefore this remains hardening debt, not F307.

UNNUMBERED CONTROL B -- NUKE AOE BORDER-LAYER CLEANUP ORDER
-----------------------------------------------------------
updateNukeAreaOfEffect obtains the selected unit and returns when no nuke is
selected before it clears AREA_BORDER_LAYER_NUKE.  updateSeaPatrolColors has the
stronger cleanup pattern: clear its layer first, then decide whether anything
should be repainted.  No other explicit Nuke-layer clear was found in current
C++.

The asymmetry is real and inherited from AdvCiv's introduction of the feature;
current SAS color/cache edits did not create it.  A stale yellow radius is
structurally possible only if the engine changes selection/interface state and
then invokes this callback after the selected nuke has disappeared or ceased to
be the head unit, without another engine-level layer clear first.  That event
ordering cannot be established from the available DLL/Python source.

Useful empirical test retained for later if desired:
- select a nuke;
- enter its nuke targeting mode and hover a legal target so the yellow radius is
  visibly painted;
- switch/cycle directly to a non-nuke or consume/fire the nuke;
- observe whether the old yellow radius remains until another redraw/mode
  transition.
A reproducible persistent overlay would supply the missing current producer /
consequence evidence and justify reopening this control.  Without it, do not
spend F307.

FINAL QUEUE-018 FINDING DECISION
--------------------------------
No new numbered root.
F307 remains unused.
Provisional KI#630 remains available for the next defensible root.

NEXT CURSOR / ROUTING
---------------------
Open queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp (2,810 queued lines) using the
same deep WIP method.  The DECLAREWARMOVE popup path was touched only as a
cross-file continuation contract while queue 018 was open; it has NOT been
pre-audited as queue 019 and must receive its own source/ancestry/delta and
inherited-saturation passes.

After queue 019 closes, do NOT resume at queue 020 yet.  Re-audit queues 001
through 009 in order with the stronger WIP/checkpoint methodology introduced
later in the project.  Only after that re-loop should the ordinary cursor resume
at queue 020.

ROUTE:
    018 CLOSED -> 019 OPEN -> 001 -> 002 -> 003 -> 004 -> 005 -> 006 -> 007
    -> 008 -> 009 -> 020

END C030 DURABLE


================================================================================
C030-WIP02 APPEND -- QUEUE 019 CvDLLButtonPopup.cpp OPENING / F307 CONFIRMED
================================================================================

STATUS
------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp -- OPEN.
Files complete remains 29 / 169.
F307 / provisional KI#630 -- CONFIRMED.
F308 / provisional KI#631 -- CONFIRMED.
No repository source files were modified.

OPENING SURFACE / DELTA MAP
---------------------------
Current SAS CvDLLButtonPopup.cpp contains 43 CvDLLButtonPopup:: definitions.  Base
AdvCiv 1.14 contains the same 43-function surface.  K-Mod has the same functional
surface plus its older launchPythonScreen helper, which AdvCiv later folded away.

After stripping comments and whitespace, only six current SAS functions differ
semantically from Base AdvCiv 1.14:
  * OnOkClicked
  * launchButtonPopup
  * launchRazeCityPopup
  * launchDoEspionagePopup
  * launchDoEspionageTargetPopup
  * launchEventPopup

The first-pass classification of those six is deliberately narrow:
  * OnOkClicked: PolicyAdvisorScreen rename for the Civics/policy UI path.
  * launchButtonPopup: KI#203 optional AI-Auto-Play suppression of only
    BUTTONPOPUP_TEXT while autoplay is active.
  * launchRazeCityPopup: static cache of PLAYER_ALWAYS_RAZES_CITIES.
  * launchDoEspionagePopup / launchDoEspionageTargetPopup: KI#398 fix that resolves
    the eligible selected Spy rather than assuming the arbitrary selection head is
    the Spy.
  * launchEventPopup: static warning-color cache only on the SAS-vs-Base delta;
    however the inherited Base AdvCiv body contains the new F307 root below.

The Auto Play BUTTONPOPUP_TEXT branch survives its opening challenge: the
OnOkClicked BUTTONPOPUP_TEXT case performs no gameplay/session action, current
producers use it as informational notices, and no shipped runtime setter changes
SAS_AIAUTOPLAY_AUTO_DISMISS_INFORMATIONAL_POPUPS_ENABLE after its static cache has
been initialized.  Do not reopen KI#203 merely because the popup is skipped.

F307 / KI#630 -- EVENT-POPUP "REVALIDATION" RE-RANDOMIZES AND LEAKS A SECOND TRIGGER RECORD
--------------------------------------------------------------------------------------------
ROOT
~~~~
Current Base AdvCiv / SAS launchEventPopup begins with the already-existing human
EventTriggeredData selected at the end of the previous turn:

    EventTriggeredData* pTriggeredData =
        kActivePlayer.getEventTriggered(info.getData1());

It then tries to "Double-check trigger conditions" through:

    if (kActivePlayer.initTriggeredData(pTriggeredData->m_eTrigger) == NULL)
    {
        kActivePlayer.deleteEventTriggered(pTriggeredData->getID());
        ...
        return false;
    }

initTriggeredData is NOT a pure predicate.  It is the constructor/randomizer used by
CvPlayer::doEvents to create a fresh candidate trigger.  On a successful call it:
  * may choose religion, corporation, plot, building and other-player context with
    SyncRandNum;
  * calls addEventTriggered(), inserting a NEW live record into m_eventsTriggered;
  * calls the trigger's Python doEventTrigger/can-do hook on that new record;
  * may consume additional SyncRandNum for trigger text and world-news text;
  * returns the newly inserted EventTriggeredData pointer.

The current function contains seven distinct SyncRandNum call sites before/after
allocation depending on trigger shape.  launchEventPopup tests only whether that
returned pointer is NULL and immediately discards the pointer when it is non-NULL.
It never deletes the newly-created validation record.

CURRENT PRODUCER
~~~~~~~~~~~~~~~~
The producer is ordinary shipped random-event flow, not a hypothetical caller:
  1. CvPlayer::doEvents creates candidate EventTriggeredData through
     initTriggeredData.
  2. For the selected human event, CvPlayer::trigger(const EventTriggeredData&)
     queues BUTTONPOPUP_EVENT carrying kData.getID().
  3. Human event choice is deliberately delayed until the popup is shown (often the
     next player turn).
  4. The EXE asks CvDLLButtonPopup::launchButtonPopup to render BUTTONPOPUP_EVENT,
     which dispatches launchEventPopup.
  5. launchEventPopup retrieves the original record by the queued ID and then calls
     initTriggeredData a second time merely to decide whether the old popup should
     still be shown.

Thus every successfully revalidated human random-event popup reaches the root.

CONCRETE CURRENT CONSEQUENCES
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A. Persistent logical-state leak in single-player too

On successful revalidation, the second EventTriggeredData remains in
CvPlayer::m_eventsTriggered.  This is an FFreeListTrashArray game-state container,
not a temporary heap object owned by launchEventPopup.  The normal doEvents candidate
cleanup deletes only the candidate pointers that doEvents itself retained in its
local vector.  There is no generic sweep of anonymous m_eventsTriggered entries.
The orphan therefore survives until an explicit delete by ID or player reset; no
such ID is retained here.

m_eventsTriggered is serialized by CvPlayer::write / restored by CvPlayer::read, so
these validation records persist into saves.  This is a logical game-state/save
leak, not merely a lost C++ pointer.

B. Rendering a UI popup mutates synchronized event/RNG state

initTriggeredData can consume synchronized random numbers while selecting trigger
context and text.  It also runs the trigger's Python predicate on the newly-created
record.  A delayed popup launch should be a validation/display operation, but the
current code can therefore advance gameplay RNG and execute trigger-construction
logic merely because the popup is being rendered.

C. Multiplayer desynchronization risk is structural, not just theoretical API debt

Most shipped event triggers are allowed in multiplayer: current XML has 197
EventTriggerInfo entries, of which 184 have bSinglePlayer=0.  The revalidation is in
CvDLLButtonPopup, i.e. the local UI popup-launch path, while the state it modifies
(m_eventsTriggered and SyncRand) is gameplay/synchronized state.  CvPlayer's own
save code explicitly treats displayed popups as potentially different between
network machines.  Therefore invoking synchronized random/event construction from
this UI-local renderer creates a direct OOS hazard: the client that renders its
human player's event can advance/add state that another peer has no reason to
construct at that moment.

The single-player orphan-record consequence already proves F307 even without an
empirical OOS reproduction; the network consequence raises the repair priority.

D. The supposed check does not actually validate the original event instance

The revalidation call passes only pTriggeredData->m_eTrigger and leaves all context
parameters at their defaults.  initTriggeredData is therefore free to pick a NEW
city/plot/unit/religion/corporation/other-player instance.  Success means "some new
instance of this trigger can be constructed now," not necessarily "the original
queued event instance is still valid."  launchEventPopup later tests event choices
against the old pTriggeredData, which catches some stale cases, but the advertised
trigger-condition recheck is semantically mismatched even apart from the leak/RNG
mutation.

ANCESTRY
~~~~~~~~
This is inherited from Base AdvCiv, not a SAS regression.

* K-Mod and Civ4CE/BtS launchEventPopup do not call initTriggeredData while rendering
  the event popup.
* Pre-SAS AdvCiv practical 4304 / commit 154ae6bd22 (2025-01-26), titled
  "Cancel random event popup if trigger conditions no longer met," introduced the
  exact call.
* The commit message explains the legitimate motivation: random events trigger at
  the end of a human player's turn, but the choice popup may not be handled until
  the next turn, so trigger conditions can become stale meanwhile.  It explicitly
  describes the new operation as "re-checking" conditions.
* Current Base AdvCiv 1.14 and current SAS both retain the side-effectful
  implementation.

The root is therefore the use of a state-creating/randomizing initializer as a
predicate in the UI renderer, not the valid goal of stale-event revalidation.

REPAIR BOUNDARY
~~~~~~~~~~~~~~~
Preferred repair: launchEventPopup must use a side-effect-free validation path for
the ORIGINAL pTriggeredData.  Refactor the relevant condition checks out of
initTriggeredData into a pure helper (or introduce a dedicated
canStillTrigger(EventTriggeredData const&) style helper) that:
  * does not allocate/add EventTriggeredData;
  * does not consume SyncRandNum;
  * does not reroll city/plot/unit/religion/corporation/player context;
  * does not invoke a construction-time Python hook merely to display the popup;
  * evaluates the old queued instance where instance-specific context matters.

A minimal containment patch that merely stores the new pointer and immediately
calls deleteEventTriggered(newId) would stop the persistent orphan, but it is NOT a
complete repair: it would still consume synchronized RNG / call trigger Python and
would still validate a freshly rerolled instance instead of the queued one.

If a pure refactor is undesirable in the first implementation pass, removing the
4304 reinitialization check and relying on the existing per-event canDoEvent(old
pTriggeredData) availability pass is safer for state/RNG integrity than retaining a
UI-local randomized constructor.  That fallback gives up some of 4304's intended
trigger-level stale-condition coverage, so a pure old-instance validator remains the
better final design.

FAST EMPIRICAL CONFIRMATION (OPTIONAL, NOT REQUIRED FOR ROOT PROOF)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
For a human random event, log/getNumEventsTriggered immediately before and after
launchEventPopup.  With successful 4304 revalidation, the count should increase by
one before the player has chosen any event outcome.  Saving immediately after the
popup is shown should preserve that extra record because m_eventsTriggered is in the
player save stream.

For MP/OOS validation, a two-human network test with random events enabled can log
m_eventsTriggered count and SyncRand seed/call state per peer around the first human
event popup.  The source already establishes the state mutation; this test would
only measure how quickly the divergence becomes externally visible.

NUMBERING / DOC NOTE
~~~~~~~~~~~~~~~~~~~~
F307 is now CONFIRMED and consumes provisional KI#630.  The current repository
README_Known_Issues.md still contains the earlier provisional KI#630 text saying
F307 was unassigned during queue 018.  That source document was intentionally not
modified during the audit; implementation/reconciliation should replace that stale
provisional disposition with this queue-019 root.

QUEUE 019 CONTINUATION
----------------------
Do not close the file at the first finding.  Continue the launch/reply time-gap
matrix, especially:
  * OnOkClicked vote/event/vassal/colony/religion IDs and receiver revalidation;
  * popup focus/relaunch cancellation after state changes;
  * city/unit ordinal popups (LOADUNIT / LEADUNIT) and whether modal UI guarantees
    the selection/plot cannot change between construction and response;
  * event-popup history beyond practical 4304 for partial follow-up fixes;
  * inherited active-player/PitBoss/network assumptions;
  * Python popup/screen ownership and callback contracts;
  * final function-by-function saturation of all 43 definitions.

F308 / provisional KI#631 is now CONFIRMED below.  F309 / provisional KI#632 remains
UNUSED until an independent root survives the same producer + contract + consequence threshold.

END C030-WIP02-PREAPPEND

C030-WIP02 ADDENDUM -- QUEUE 019 SECOND ROOT + TIME-GAP/HISTORY CONTROLS
=======================================================================

CURRENT STATE AT THIS CHECKPOINT
--------------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remains 29 / 169.
F307 / provisional KI#630 -- CONFIRMED.
F308 / provisional KI#631 -- CONFIRMED.
F309 / provisional KI#632 -- UNUSED.
No repository source files were modified.

TIME-GAP / RECEIVER CONTROLS CLOSED SO FAR
------------------------------------------
The launcher/reply pass has eliminated several tempting stale-popup candidates:

* Vassal tribute: the master popup lists a currently tradeable resource and the
  human-vassal grant popup can be delayed, but the eventual implement-deal path
  reaches CvDeal::addTradeItems(..., bCheckAllowed=true), which re-runs
  canTradeItem before applying the deal.  Losing/trading the resource while the
  popup is outstanding therefore rejects the stale deal instead of creating an
  invalid resource agreement.

* Diplomatic vote: a stale vote ID is rejected by the receiver when the
  VoteTriggeredData no longer exists.  The popup does not retain a raw pointer.

* Free colony: positive popup buttons carry an area ID, and CvPlayer::splitEmpire
  resolves the area again and rechecks both canSplitEmpire() and canSplitArea()
  before mutating the empire.  A stale area choice is therefore receiver-safe.

* Spaceship launch: K-Mod's old protection remains in launchLaunchPopup
  (!kTeam.canLaunch(eVictory) => cancel); CvPlayer::launch independently repeats
  canLaunch at execution.  The old K-Mod "launch popup after launch became
  impossible" defect has not regressed.

* Raze city: the old AdvCiv crash repair remains present.  When keeping a captured
  city would change its liberation target, eFutureLiberationPlayer is explicitly
  checked for NO_PLAYER before GET_PLAYER dereference.  The launcher also resolves
  the captured city ID afresh before use.

* Popup sound/relaunch tracking from AdvCiv practical 2453 remains structurally
  intact: only a successful launch plays AS2D_IF_BUTTONPOPUP_UP, while every launch
  attempt reports ButtonPopupLaunched so CvPlayer can distinguish relaunches.  No
  SAS delta was found in this ownership/lifecycle contract.

LOADUNIT / LEADUNIT ORDINAL TIME-GAP NOTE
-----------------------------------------
Both popups historically encode a displayed choice as an ordinal (1st eligible,
2nd eligible, ...), then re-enumerate the current plot when the player clicks.
That is weaker than storing a stable ID.  For LOADUNIT, however, the actual command
includes both transport owner and ID and the popup is an immediate modal choice;
no ordinary local mutation path was found that changes the active player's own
candidate ordering while the popup is open.  A same-team remote transport can move
in simultaneous multiplayer, but that presently establishes only a narrow stale
ordinal/no-op-or-retarget race, not a stronger general root.  Keep as hardening
unless runtime evidence makes it important.  A stable owner+unit-ID button payload
would be cleaner.

The LEADUNIT path has a separate, fully proved cross-player alias root below; do not
merge that with the generic ordinal observation.

F308 / KI#631 -- LEAD-UNIT POPUP CAN LIST TEAMMATE UNITS AND RESOLVE THEIR PLAYER-LOCAL ID AS A DIFFERENT OWN UNIT
-----------------------------------------------------------------------------------------------------------------
ROOT
~~~~
The Great General / Warlord action already defines its candidate-owner invariant in
CvUnit::canLead(pPlot, -1):

    if (pUnit != this && pUnit->getOwner() == getOwner() &&
        pUnit->canPromote(leaderPromotion, getID()))
    {
        iUnits++;
    }

Only SAME-OWNER units are therefore legitimate targets of this Great General.

CvDLLButtonPopup::launchLeadUnitPopup does not preserve that invariant.  It walks
EVERY unit on the selected group's plot and accepts solely:

    if (pUnit->canPromote((PromotionTypes)info.getData1(), info.getData2()))

The BUTTONPOPUP_LEADUNIT OnOk handler repeats the same owner-blind enumeration.
FOR_EACH_UNIT_VAR_IN on a CvPlot resolves the plot's full IDInfo unit list; it does
not filter by player.

WHY canPromote DOES NOT SAVE THIS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
For a leader promotion, CvUnit::canPromote(ePromotion, iLeaderUnitId) resolves the
leader through the CANDIDATE unit's owner:

    CvUnit* pWarlord = GET_PLAYER(getOwner()).getUnit(iLeaderUnitId);
    ...
    return pWarlord->getUnitInfo().getLeaderPromotion() == ePromotion;

Civ4 unit IDs are player-local.  CvPlayer::getUnit(int iID) is simply
m_units.getAt(iID), and each CvPlayer owns its own m_units array.

Therefore info.getData2() -- the active player's Great General ID -- has a different
meaning when canPromote is invoked on a teammate's unit.  If the teammate happens
to own its own Great General / Warlord at the same numeric player-local ID, then a
teammate combat unit on the shared plot can pass canPromote and appear as a legal
button even though the active Great General cannot legitimately lead that unit.

This is not an invented hostile-input scenario.  Team/Permanent-Alliance players
can normally stack units on the same plot, and independent per-player unit ID
sequences naturally overlap.  The action itself proves that foreign targets are
not intended because CvUnit::canLead(..., -1) explicitly filters to getOwner().

CLICK-TIME ID ALIAS / CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The popup button is only an ordinal.  Once that ordinal reaches the foreign
teammate unit, OnOk sends:

    selectionListGameNetMessage(..., MISSION_LEAD, pUnit->getID());

MISSION_LEAD carries ONLY the numeric unit ID -- no owner.  The selected active
Great General eventually executes CvUnit::lead(iData1), whose explicit target path
resolves:

    CvUnit* pUnit = GET_PLAYER(getOwner()).getUnit(iUnitId);

That is now the ACTIVE GREAT GENERAL OWNER'S namespace, not the teammate namespace
whose unit generated the button.

The receiver-side invariant is weaker than it should be too.  CvUnit::canLead(pPlot,
explicitId) gets the same-owner unit by ID and tests canPromote, but does NOT verify
that the target is actually at pPlot.  CvUnit::lead then promotes that resolved own
unit and kills the Great General.

Concrete outcomes after clicking a falsely-listed teammate button are therefore:
  * if the active player has no unit with that numeric ID, the command silently
    fails;
  * if that ID resolves to an ineligible own unit, it silently fails;
  * if that ID resolves to an eligible own unit, the Great General can be attached
    to the WRONG own unit; because explicit canLead lacks an at-plot test, that own
    unit may even be somewhere else on the map.

A normal shared-team setup can therefore turn a UI target described as teammate
unit X into a mission against unrelated active-player unit Y solely because their
player-local integer IDs coincide.

CURRENT PRODUCER / REACHABILITY
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A normal supported sequence is sufficient:
  1. Human A and teammate B (multiplayer team, Permanent Alliance, etc.) have units
     on the same plot.
  2. A has a Great General with local unit ID L and at least one legitimate own
     lead target on that plot, so the Lead action is enabled through the correct
     canLead(...,-1) owner-filtered test.
  3. B independently has a Warlord/Great-General-type unit whose player-local ID is
     also L.  Player-local ID overlap is valid and expected.
  4. A teammate combat unit on the shared plot calls canPromote(..., L); because its
     getOwner() is B, it resolves B's Warlord L and can pass the leader-promotion
     test.
  5. launchLeadUnitPopup therefore displays B's unit alongside A's own legitimate
     targets.
  6. Clicking B's displayed unit sends only B-target numeric ID T.
  7. A's Great General resolves T as A.getUnit(T), potentially acting on a different
     unit.

No EXE timing assumption, malformed network packet or unsupported mod configuration
is needed.

ANCESTRY
~~~~~~~~
This is inherited Firaxis BtS behavior retained by Civ4CE, K-Mod, Base AdvCiv 1.14
and current AdvCiv-SAS.  The supplied Civ4CE BtS and K-Mod launchLeadUnitPopup and
BUTTONPOPUP_LEADUNIT reply code have the same owner-blind canPromote scan.  The
same-generation CvUnit::canLead(...,-1) already contains the intended same-owner
filter, so the inconsistency is historical rather than a SAS design choice.

REPAIR BOUNDARY
~~~~~~~~~~~~~~~
Minimal current-producer repair:
  * in BOTH launchLeadUnitPopup and the BUTTONPOPUP_LEADUNIT OnOk re-enumeration,
    reject candidates whose owner is not the selected/active Great General owner
    before calling canPromote.
  * preferably resolve the intended Great General by active-player + info.getData2()
    first and use its owner as the authoritative owner boundary.

Stronger UI repair:
  * stop encoding lead targets as ordinals; store a stable same-owner target unit ID
    (e.g. ID+1 if button 0 must remain Cancel), resolve it afresh on click and
    revalidate owner/promotion/co-location.  This also removes the generic ordinal
    time-gap weakness.

Defense-in-depth outside this queue:
  * CvUnit::canLead(pPlot, explicitId) should require the resolved target to be at
    pPlot.  That makes the mission receiver uphold the spatial contract even if a
    stale/malformed UI mission supplies a wrong same-owner ID.  The ordinary AI/UI
    callers already choose co-located targets, so this should be a contract guard,
    not a gameplay-policy change.

NUMBERING
~~~~~~~~~
F308 is CONFIRMED and consumes provisional KI#631.  F309 / provisional KI#632 is
now the next unused root slot.

F307 REPRODUCIBILITY STRENGTHENING
----------------------------------
The event leak is repeatable more than once for the same original event.  For
pick-city triggers, the event popup can include "Examine City".  Its OnOk path
selects the city and queues a fresh BUTTONPOPUP_EVENT containing the SAME original
trigger ID.  Relaunching that popup re-enters launchEventPopup and calls
initTriggeredData again, creating another anonymous validation EventTriggeredData
and consuming another construction/randomization pass.  Repeated Examine City
round-trips can therefore increase m_eventsTriggered repeatedly before the player
chooses an event result.  This is useful for a fast WorldBuilder/debug confirmation
of F307 and further confirms that the leaked record is tied to rendering/relaunch,
not to one-time event generation.

CONTINUATION FROM C030-WIP02
----------------------------
Queue 019 remains open.  Continue with:
  * remaining launcher/OnOk stable-ID and receiver-revalidation matrix;
  * OnFocus cancellation/relaunch behavior and the old AdvCiv practical 1276 popup
    minimization/relaunch contract;
  * Python popup callback ownership and button-count assumptions;
  * R&F popup dispatch/defeat-history reconciliation;
  * inherited active-player/PitBoss assumptions;
  * final all-43-function saturation and merge check.

Do not manufacture F309.  The generic LOADUNIT/LEADUNIT ordinal observation remains
unnumbered unless a stronger independent current consequence is proved beyond the
now-separated F308 owner/ID-alias root.

END C030-WIP02

C030-WIP03 APPEND -- QUEUE 019 DEEPER DELAYED-POPUP / VOTE-LIFETIME PASS
============================================================================
Physical continuation from C030-WIP02.  No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29/169.

DORMANT SDK DEFECT -- DESTROY-UNIT ESPIONAGE POPUP PASSES UNIT TYPE WHERE UNIT ID IS REQUIRED (NOT NUMBERED)
-----------------------------------------------------------------------------------------------------------
A source-contract mismatch in launchDoEspionageTargetPopup initially looked strong:
for missions with getDestroyUnitCostFactor() > 0, the popup enumerates a concrete
pTargetUnit but calls canDoEspionageMission and getEspionageMissionCost with
pTargetUnit->getUnitType() as iExtraData.  Downstream CvPlayer espionage code treats
that same iExtraData as a TARGET PLAYER-LOCAL UNIT ID.  The actual popup button, by
contrast, correctly carries pTargetUnit->getID().

That generic SDK code is therefore inconsistent and could display the wrong
eligibility/cost (or alias another target-player unit ID) in a mod that enables a
Destroy Unit espionage mission.  The shape is inherited unchanged from Firaxis BtS
through Civ4CE, K-Mod, Base AdvCiv and current SAS.

It does NOT meet the current-tree producer standard, however.  Current shipped SAS
CIV4EspionageMissionInfo.xml explicitly sets iDestroyUnitCostFactor=0 and
iBuyUnitCostFactor=0 for every EspionageMissionInfo.  CvEspionageMissionInfo also
initializes both fields to 0 before XML loading.  Base AdvCiv and the supplied
Civ4CE data likewise have no nonzero Destroy Unit mission.  Therefore the branch
cannot currently be selected from shipped data.  Keep this as dormant generic SDK
repair debt; do NOT assign F309 to it.

F309 / KI#632 -- HUMAN "NONE" ELECTION CHOICE LEAKS VoteSelectionData FOREVER
----------------------------------------------------------------------------
ROOT
~~~~
CvGame::doVoteSelection creates a VoteSelectionData in m_voteSelections whenever a
human Secretary-General is asked which UN/AP resolution to propose, and queues a
BUTTONPOPUP_CHOOSEELECTION carrying that selection's ID.

launchChooseElectionPopup adds one button for every current aVoteOptions entry and
then a final "None" button.  Current AdvCiv conditionally labels that final button
"None (causes an early election)", making the no-proposal path explicit.  Its button
ID is GC.getNumVoteInfos(), deliberately outside the 0..aVoteOptions.size()-1 choice
range.

BUTTONPOPUP_CHOOSEELECTION OnOk does this:

    VoteSelectionData* pData = kGame.getVoteSelection(info.getData1());
    if (pData != NULL &&
        pPopupReturn->getButtonClicked() < (int)pData->aVoteOptions.size())
    {
        sendChooseElection(pPopupReturn->getButtonClicked(), info.getData1());
    }

Thus an ordinary resolution choice sends the synchronized CvNetChooseElection
message.  The "None" button fails the range test and sends NOTHING.

OWNERSHIP / LIFETIME CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
CvNetChooseElection::Execute calls CvGame::setVoteChosen(iSelection, iVoteId).
That routine is also the normal owner cleanup path:

    VoteSelectionData* pVoteSelectionData = getVoteSelection(iVoteId);
    if (pVoteSelectionData != NULL)
        addVoteTriggered(*pVoteSelectionData, iSelection);
    deleteVoteSelection(iVoteId);

Repository-wide current-source search found no other ordinary deletion/consumer for
an already-created human VoteSelectionData.  deleteVoteSelection is called when
addVoteSelection itself produces no options, and by setVoteChosen after a response;
there is no later vote-cycle sweep of abandoned selection objects.

So clicking the supported "None" button leaves its VoteSelectionData live in
m_voteSelections indefinitely.  Each later no-proposal election can add another
one.

This is persistent rather than a transient heap allocation.  m_voteSelections is
an FFreeListTrashArray: live entries remain occupied until removeAt/delete is called.
CvGame::write serializes m_voteSelections through
WriteStreamableFFreeListTrashArray, which writes flist.getCount() and invokes
VoteSelectionData::write for every live entry.  CvGame::read reconstructs those
entries.  Every skipped proposal therefore accumulates runtime state and permanent
save payload until game reset.

CURRENT PRODUCER / REACHABILITY
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
No unusual timing or malformed UI input is required:
  1. A normal UN/AP vote-source cycle reaches doVoteSelection with a human
     Secretary-General and at least one valid proposal.
  2. addVoteSelection creates a live VoteSelectionData and the engine displays the
     CHOOSEELECTION popup.
  3. The human clicks the shipped "None" / "None (causes an early election)" button.
  4. OnOk deliberately suppresses sendChooseElection because that button ID is
     outside aVoteOptions.size().
  5. The selection has no remaining owner/cleanup path and remains serialized.

The behavior can repeat every applicable vote cycle.  AdvCiv's own advc.178 UI text
explicitly documents "None" as an intended gameplay choice, so this is not an
unsupported close/cancel state.

ANCESTRY
~~~~~~~~
The same out-of-range "None" button plus range-gated OnOk structure is present in
the supplied Civ4CE BtS source and K-Mod and survives in Base AdvCiv/current SAS.
The leak is therefore inherited Firaxis/BtS behavior, not a SAS regression.
AdvCiv practical advc.178 later clarified the gameplay meaning by relabeling the
button "None (causes an early election)" when applicable, but did not add cleanup.

REPAIR BOUNDARY
~~~~~~~~~~~~~~~
Do NOT simply call deleteVoteSelection locally from CvDLLButtonPopup::OnOk in a
network game: m_voteSelections is synchronized serialized game state, so a UI-local
mutation risks OOS.

The existing synchronized receiver already supports a clean no-choice sentinel:
CvGame::addVoteTriggered(kData, iChoice) returns NULL when iChoice == -1, while
CvGame::setVoteChosen still unconditionally deleteVoteSelection(iVoteId).
Therefore the narrow repair is to send the ordinary choose-election network message
with selection -1 when the final None/Early-Election button is clicked.  All peers
then execute the same no-resolution cleanup through setVoteChosen.

For robustness, OnOk should distinguish valid proposal indices, the dedicated
None-button ID, and malformed/negative values explicitly rather than relying only on
"button < vector size".

NUMBERING
~~~~~~~~~
F309 is CONFIRMED and consumes provisional KI#632.
F310 / provisional KI#633 is now the next unused root slot.

VOTE STALE-ID CONTROL
---------------------
BUTTONPOPUP_DIPLOVOTE sends a vote-trigger ID that can in principle become stale
while a popup is delayed.  The receiver CvGame::castVote first resolves
getVoteTriggered(iVote) and immediately returns if the object no longer exists.
This path is therefore receiver-safe against deletion of a stale vote object.  A
normal vote writes through setPlayerVote only after the trigger has been resolved.

CONTINUATION FROM C030-WIP03
----------------------------
Queue 019 remains OPEN.  Continue with the remaining all-43-function saturation:
  * OnFocus cancel/relaunch behavior, especially choose-tech and production;
  * Python popup callback ownership and popup data contracts;
  * R&F custom popup dispatch/history and active-player assumptions;
  * admin/details/network/PBEM popup lifecycle;
  * launcher/reply stable-ID matrix not already closed;
  * final history-led merge/duplicate check before queue closure.

F307/KI#630, F308/KI#631 and F309/KI#632 are confirmed.  F310/KI#633 remains
UNUSED.  Continue to avoid manufacturing a finding from unsupported popup states.

END C030-WIP03



C030-WIP04 APPEND -- QUEUE 019 TEAM-VOTE ELIGIBILITY / REPLY-TIME REVALIDATION PASS
=====================================================================================
Physical continuation from C030-WIP03. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307 / provisional KI#630 -- CONFIRMED (event-popup reinitialization/leak).
F308 / provisional KI#631 -- CONFIRMED (lead-unit cross-player ID alias).
F309 / provisional KI#632 -- CONFIRMED (human None election selection leak).
F310 / provisional KI#633 -- CONFIRMED below.
F311 / provisional KI#634 -- UNUSED.

RE-VERIFICATION OF C030-WIP03 ROOTS
-----------------------------------
Before consuming another number, the three newly recorded WIP03 roots were checked
again against the current 2026-08-29 SAS source rather than accepted merely because
they appeared in the previous checkpoint.

* F307: CvPlayer::initTriggeredData is demonstrably a state-creating/randomizing
  constructor. It can consume SyncRandNum while selecting religion, corporation,
  plot, building, player/city and text context; then addEventTriggered inserts a new
  EventTriggeredData; the Python doEventTrigger hook runs on it. launchEventPopup
  calls that constructor only as a boolean test and discards the successful pointer.
  The ordinary doEvents -> trigger -> BUTTONPOPUP_EVENT producer therefore reaches
  the root. F307 remains confirmed.

* F308: launchLeadUnitPopup and its reply enumerate every unit on the selection
  plot with pUnit->canPromote(..., info.getData2()) and do not require the candidate
  to share the Great General's owner. CvUnit::canPromote resolves the leader ID in
  the CANDIDATE unit owner's player-local unit namespace. CvUnit::canLead(-1), by
  contrast, explicitly requires pUnit->getOwner()==getOwner(). The reply then sends
  only the selected pUnit->getID(), and the actual Great General later resolves that
  ID in ITS OWN owner's namespace. The cross-player alias/root is real; F308 remains
  confirmed.

* F309: CvGame::setVoteChosen is the synchronized normal ownership/cleanup path for
  VoteSelectionData and unconditionally calls deleteVoteSelection after optionally
  addVoteTriggered. The shipped None/Early-Election button deliberately falls
  outside the choice vector and BUTTONPOPUP_CHOOSEELECTION sends no network message
  for it. Repository-wide deletion search still shows no later generic cleanup of
  that already-created human selection. F309 remains confirmed.

F310 / KI#633 -- STALE TEAM-VOTE BALLOTS CAN ELECT A TEAM THAT IS NO LONGER ELIGIBLE
-----------------------------------------------------------------------------------
ROOT
~~~~
BUTTONPOPUP_DIPLOVOTE builds a team-vote ballot from the CURRENT eligibility set:

    if (!GC.getGame().isTeamVoteEligible(itTeam->getID(), eVoteSource))
        continue;
    popupAddGenericButton(..., itTeam->getID(), ...);

The button payload is therefore a TeamTypes value that was eligible when that local
popup was rendered. On reply, however, CvDLLButtonPopup::OnOkClicked sends that
stored team ID without any current eligibility check:

    sendDiploVote(info.getData1(),
        (PlayerVoteTypes)pPopupReturn->getButtonClicked());

CvNetDiploVote::Execute forwards it to CvGame::castVote. castVote does safely reject
a deleted vote-trigger ID, but for a still-live team vote it accepts any positive
team-shaped PlayerVoteTypes and writes it with setPlayerVote. It does NOT call
isTeamVoteEligible for the chosen team.

The final result path has the same gap. CvGame::findHighestVoteTeam iterates every
alive civ team and simply counts votes whose stored value equals that team ID. It
does not skip teams that have ceased to satisfy isTeamVoteEligible. Thus even votes
cast before the state change remain capable of making an ineligible team the winner.

CURRENT PRODUCER / DELAY WINDOW
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This does not depend on an invented EXE callback race.

CvGame::doTurn increments the game turn and then calls doDiploVote BEFORE activating
players for the new round in simultaneous-turn multiplayer. doDiploVote first
processes old results and then creates the next vote selection/trigger. Human voters
receive BUTTONPOPUP_DIPLOVOTE at that point.

Current doVoteResults deliberately gives missing human voters a full grace turn:
first-pass NO_PLAYER_VOTE entries become NO_PLAYER_VOTE_CHECKED and result processing
continues only on a later turn. Therefore a live VoteTriggeredData and unanswered
human ballot are explicitly designed to survive while ordinary gameplay continues.
Candidate state can change during that supported interval.

Two concrete eligibility transitions are available in current gameplay:

1. Apostolic Palace/full-membership change

isTeamVoteEligible rejects a non-force candidate that is no longer a full member.
CvTeam::isFullMember in turn requires every member player to remain a full member;
CvPlayer::isFullMember depends on the vote-source state religion/civic and loyal
membership. A candidate team that changes out of the required state religion/civic,
or otherwise loses full membership, therefore ceases to be a valid candidate while
already-issued ballots can still contain its team ID.

2. Capitulation -- particularly explicit in AdvCiv

advc.014 modifies isTeamVoteEligible so that, when compared with a non-capitulated
full-member alternative, a capitulated candidate's ranking votes are treated as 0.
CvTeam::setVassal also calls CvGame::updateSecretaryGeneral when capitulation occurs,
with the explicit comment "Early re-election if vote source owner capitulates".
updateSecretaryGeneral clears an incumbent Secretary General if it is capitulated.

So AdvCiv expressly intends capitulation to invalidate/replace that team in the
normal candidate machinery. A team can nevertheless capitulate after the ballot is
created, remain alive, and then be selected through an old popup or retain already
cast votes. findHighestVoteTeam will still count those votes because it tests only
alive-team identity, not current eligibility.

CONSEQUENCES
~~~~~~~~~~~~
A. Secretary-General election can undo AdvCiv's immediate capitulation cleanup

If the incumbent/candidate capitulates, advc.014 may clear its old Secretary General
outcome immediately. The still-live election can later choose the same now-ineligible
team through stale votes; doVoteResults then sets the team-vote outcome back to that
team. CvGame::getSecretaryGeneral only requires the passed outcome team to be alive,
so the stale result can restore the capitulated team as Secretary General until some
later updateSecretaryGeneral trigger clears it again.

B. Diplomatic-victory vote can award victory to an ineligible candidate

Team-vote result processing uses findHighestVoteTeam and the ordinary vote threshold.
CvGame::testVictory's diplomatic-vote condition accepts a team when a victory vote's
stored outcome equals that team. There is no final isTeamVoteEligible filter there.
Consequently a stale candidate that receives enough old/stale votes can satisfy the
vote-victory outcome even though it would not appear on a freshly generated ballot.

C. The problem is not limited to a voter clicking an old button

Even if every voter answered before the eligibility transition, those stored team
IDs are not normalized or invalidated when the candidate later becomes ineligible.
The result aggregator itself therefore needs a current-eligibility guard; UI-only
revalidation is insufficient.

ANCESTRY
~~~~~~~~
The result-side shape is inherited rather than SAS-local. K-Mod already combines an
isTeamVoteEligible candidate filter (including full-membership eligibility) with a
findHighestVoteTeam that counts every alive team and does not recheck eligibility.
Base AdvCiv/current SAS retain that architecture.

AdvCiv's advc.014 later added the capitulation-specific candidate-ranking rule and
immediate Secretary-General cleanup without adding reply/result revalidation. That
makes the stale-state contradiction especially explicit in AdvCiv, but the broader
"eligibility can change while a vote is live" architecture predates SAS.

This is distinct from:
  * F022 / KI#343, which concerns how missing simultaneous voters receive their grace
    turn; and
  * F032 / KI#353, which concerns AI_diploVoteCounterCandidate comparing a vote count
    with a team ID.
Neither existing root covers stale candidate eligibility at reply/result time.

REPAIR BOUNDARY
~~~~~~~~~~~~~~~
The authoritative fix belongs in synchronized game-state handling, not only in the
local popup renderer.

1. Result aggregation: findHighestVoteTeam (or an equivalent team-vote result pass)
   should ignore teams that are no longer isTeamVoteEligible for the vote source.
   This is essential because votes cast while the team was eligible can outlive the
   eligibility transition.

2. Receiver hardening: CvGame::castVote should reject or convert to abstention a
   positive team-vote choice that is no longer an eligible team for the trigger's
   vote source. This prevents a stale local popup/network message from injecting a
   newly invalid choice.

3. UI defense-in-depth: launch/focus/reply can refresh or suppress stale team choices,
   but this is not sufficient on its own because eligibility can change after some
   votes have already been recorded.

Care is needed for the existing vassal-master auto-vote behavior in addVoteTriggered:
that path intentionally calls AI_diploVote for a capitulated human vassal when the
master is eligible and the vassal team itself is not. Receiver validation should
therefore validate the CHOSEN candidate team, not require the voter's own team to be
eligible.

NUMBERING
~~~~~~~~~
F310 is CONFIRMED and consumes provisional KI#633.
F311 / provisional KI#634 is now the next unused root slot.

HUMAN-VASSAL TRIBUTE STALE-STATE CANDIDATE -- STILL UNNUMBERED
--------------------------------------------------------------
The earlier vassal-tribute delayed-popup concern remains worth retaining, but F310
is not being spent on it. A demand/grant popup can theoretically outlive the vassal
relationship; stale Yes is sent through implementDeal (whose receiver revalidates
trade legality), while stale No goes through CvNetChangeWar and CvTeam::declareWar,
which does not itself rerun canDeclareWar. Master capitulation can also free its
existing vassals through CvTeam::setVassal.

However, the exact local popup/modal scheduling needed to demonstrate the stale No
button in a supported human-vassal sequence is less direct than the team-vote grace
window above. Keep this as a delayed-state hardening candidate/control and do not
manufacture F311 from it without a concrete scheduling/reproduction chain.

CONTINUATION FROM C030-WIP04
----------------------------
Queue 019 remains OPEN. Continue with:
  * all remaining launcher/reply stable-ID and current-state revalidation pairs;
  * OnFocus cancellation/relaunch semantics (choose-tech, production and other
    mandatory prompts);
  * Python popup ownership/callback contracts;
  * R&F custom popup dispatch/history and active-player assumptions;
  * admin/details/network/PBEM popup lifetime paths;
  * complete all-43-function saturation and ancestry merge before closure.

F307/KI#630 through F310/KI#633 are confirmed. F311/KI#634 remains UNUSED.
The post-019 route remains the deep-method re-loop at queue 001, then 002-009,
then resume ordinary pending order at 020.

END C030-WIP04


C030-WIP05 APPEND -- QUEUE 019 VASSAL-TRIBUTE LIVE-RELATIONSHIP PASS
====================================================================
Physical continuation from C030-WIP04. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307 / provisional KI#630 -- CONFIRMED.
F308 / provisional KI#631 -- CONFIRMED.
F309 / provisional KI#632 -- CONFIRMED.
F310 / provisional KI#633 -- CONFIRMED.
F311 / provisional KI#634 -- CONFIRMED below.
F312 / provisional KI#635 -- UNUSED.

F311 / KI#634 -- STALE HUMAN VASSAL-TRIBUTE POPUP CAN DECLARE WAR AFTER THE VASSAL RELATION HAS ENDED
------------------------------------------------------------------------------------------------------
ROOT
~~~~
launchVassalGrantTributePopup correctly checks the relationship when the popup is
first rendered:

    CvPlayer& kMaster = GET_PLAYER((PlayerTypes)info.getData1());
    if (!GET_TEAM(getActiveTeam()).isVassal(kMaster.getTeam()))
        return false;

It then presents Yes / No. BUTTONPOPUP_VASSAL_GRANT_TRIBUTE OnOk does not repeat
that relationship check.

Yes builds a one-sided TRADE_RESOURCES offer and sends implementDeal to the old
master. No does this unconditionally:

    sendChangeWar(TEAMID((PlayerTypes)info.getData1()), true);

The popup therefore stores the semantic proposition "this player is my master and
is currently demanding tribute" only at launch time. Reply-time code treats that
relationship as immutable while the popup is visible.

SUPPORTED CURRENT PRODUCER -- SIMULTANEOUS MULTIPLAYER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The missing transition is available through the ordinary AI diplomacy loop, not a
contrived direct setVassal call.

CvPlayerAI::AI_doDiplo is a two-pass contact loop: AI targets are processed first,
human targets second. Current AdvCiv/SAS shuffles the target order inside each pass.
For a human vassal H of AI master A, the human pass can execute this shipped branch:

    if (GET_TEAM(ePlayer).isVassal(getTeam()) ...)
        ...
        addPopup(BUTTONPOPUP_VASSAL_GRANT_TRIBUTE, getID(), eBestGiveBonus)

So A can place a live tribute demand on H's UI.

Later in the SAME human-target pass, a different human C can be processed. A is
allowed to offer itself as a voluntary vassal through TRADE_VASSAL when the usual
AI_vassalTrade checks succeed. Neither canPossiblyTradeItem(TRADE_VASSAL) nor
AI_vassalTrade forbids A merely because A currently has vassals of its own.

If H precedes C in the shuffled human order, A can therefore create H's tribute
popup and then open an OFFER_VASSAL negotiation with C. In simultaneous network
multiplayer H and C have independent local UI: H's immediate popup can remain open
while C accepts A's offer.

The accepted TRADE_VASSAL is synchronized through ordinary deal execution.
CvDeal::startTrade calls:

    GET_TEAM(eFromPlayer).setVassal(kToPlayer.getTeam(), true, false);

for A becoming C's vassal. CvTeam::setVassal expressly iterates A's existing
vassals and calls freeVassal on each of them before aligning the new master relation.
H therefore ceases to be A's vassal while H's already-launched popup still carries
A and the old bonus IDs.

There is no OnFocus handler for either vassal-tribute popup and no relation-change
hook that invalidates an already-displayed BUTTONPOPUP_VASSAL_GRANT_TRIBUTE. The
launch-time guard cannot help once the popup is already on H's client.

CONCRETE CONSEQUENCES
~~~~~~~~~~~~~~~~~~~~~
A. Stale No starts a war that the current UI would never offer as "refusing tribute"

CvNetChangeWar::Execute forwards the message directly to:

    GET_TEAM(m_ePlayer).declareWar(m_eRivalTeam, false, NO_WARPLAN);

CvTeam::declareWar does NOT rerun canDeclareWar. It returns only if the teams are
already at war and then performs the full war transition, including deal killing,
war-state changes and vassal/defensive-pact cascades.

Thus H can click "No" on a demand from a civilization that is no longer H's master
and thereby declare war on former master A (and potentially A's new master coalition)
under a popup whose gameplay premise has disappeared. A freshly launched tribute
popup would have been rejected by launchVassalGrantTributePopup instead.

This is not merely a stale label: a high-impact synchronized state transition is
executed from obsolete relationship state.

B. Stale Yes can become an ordinary gift or a false acceptance message

The Yes path sends a resource deal to old master A without checking current
vassalage. CvGame/CvDeal does revalidate trade legality, which limits corruption:
if the resource trade is no longer legal, the deal can fail. But if an ordinary
resource gift remains legal after independence, the stale demand becomes a free
voluntary gift to the ex-master.

Additionally, OnOk immediately posts TXT_KEY_VASSAL_GRANT_TRIBUTE_ACCEPTED to A
regardless of whether synchronized deal validation ultimately accepts the trade.
So stale Yes can also produce an acceptance message for a deal that was rejected.
The forced stale-war path is the stronger root consequence.

WHY THE EXISTING LAUNCH GUARD IS NOT ENOUGH
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
If the relationship changes BEFORE H's popup is rendered, the launcher safely
returns false. F311 concerns the supported simultaneous-network ordering where the
popup has already been rendered on H's client and a different human client then
causes the synchronized vassal transition. Per-client immediate UI does not freeze
remote human diplomacy/game-state changes.

The same broader risk can arise from other synchronized master/vassal restructuring,
but the AI A / human H / human C sequence above is sufficient current producer proof.

ANCESTRY
~~~~~~~~
This is inherited from K-Mod / Base AdvCiv, not SAS-local.

The supplied K-Mod source already contains:
  * the two-pass AI_doDiplo human-contact machinery;
  * the AI-master BUTTONPOPUP_VASSAL_GRANT_TRIBUTE producer;
  * the launch-time isVassal guard;
  * an OnOk No branch that sends change-war without revalidation; and
  * voluntary-vassal offers in the same human-contact pass.

Base AdvCiv/current SAS retain that interaction, with later refactors but no
reply-time relationship check. Current SAS changes in queue 019 did not create it.

REPAIR BOUNDARY
~~~~~~~~~~~~~~~
1. BUTTONPOPUP_VASSAL_GRANT_TRIBUTE OnOk must re-resolve the stored master player and
   require the active team to STILL be that master's vassal before either Yes or No
   has gameplay effect. If the relation is stale, dismiss silently (or show a
   neutral "demand no longer applies" notice).

2. Recheck the stored bonus/trade item before the Yes path's success message. The
   authoritative synchronized implementDeal validation should remain in place, but
   local UI should not announce acceptance before knowing the premise still holds.

3. Defense-in-depth: CvNetChangeWar / its higher-level user-input producer should not
   be treated as a general permission bypass. Rechecking canDeclareWar for ordinary
   user-initiated declaration messages would contain stale/malformed UI sources,
   provided special forced-war callers that intentionally bypass normal permission
   are kept on an explicit separate path/cause.

The narrow queue-019 fix is the reply-time isVassal revalidation; broader network
war-command hardening belongs to the message/team audit if pursued.

NUMBERING
~~~~~~~~~
F311 is CONFIRMED and consumes provisional KI#634.
F312 / provisional KI#635 is now the next unused root slot.

RECONCILIATION WITH THE EARLIER WIP04 TRIBUTE NOTE
--------------------------------------------------
WIP04 deliberately left tribute unnumbered because it had not yet established a
supported way for a relationship to change AFTER the popup had actually launched.
The AI_doDiplo two-human simultaneous-network sequence above supplies that missing
producer. Therefore this WIP supersedes WIP04's "hardening candidate" status and
promotes the same root to F311; it is not a second tribute issue.

CONTINUATION FROM C030-WIP05
----------------------------
Queue 019 remains OPEN. Continue with F312 UNUSED through:
  * OnFocus and popup relaunch/cancel semantics;
  * LOADUNIT and remaining ordinal/time-gap controls;
  * Python popup callbacks and ownership;
  * details/admin/PBEM/PitBoss/session popups;
  * Rise & Fall custom popup contracts;
  * remaining stable-ID / receiver-revalidation pairs;
  * final all-43-function and history/ancestry saturation.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP05


======================================================================
C030-WIP06 -- QUEUE 019 LOADUNIT RETARGET ROOT + RELAUNCH CONTROL
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F312 are now CONFIRMED in queue 019.
F313 / provisional KI#636 is UNUSED.

This WIP supersedes the earlier generic LOADUNIT hardening note in WIP05. The
missing ingredient there was a deterministic current candidate-order mutation,
not merely the abstract fact that a teammate can move while the popup is open.
The plot's actual insertion order now supplies that proof.

AUTO-PLAY BUTTONPOPUP_TEXT / POPUP-RELAUNCH COUNTER CONTROL
-----------------------------------------------------------
Current SAS has an early launchButtonPopup return while AI Auto Play is active:

    if (bSASAutoDismissAutoPlayInformationalPopups &&
        kGame.getAIAutoPlay() > 0 && eType == BUTTONPOPUP_TEXT)
    {
        ...
        return false;
    }

AdvCiv's normal launch tail always calls:

    kPlayer.reportButtonPopupLaunched();

whether or not the popup actually launches. CvPlayer::killAll uses this callback
to count down m_iButtonPopupsRelaunching, whose only purpose is suppressing the
popup-up sound while already displayed popups are recreated. Thus the SAS early
return is structurally asymmetric: if a BUTTONPOPUP_TEXT were itself one of the
already displayed popups being relaunched, its attempt would not decrement the
counter.

Do NOT promote this to a numbered root at present:

* new BUTTONPOPUP_TEXT popups created while Auto Play is already running are
  rejected by this same early branch before becoming displayed relaunch entries;
* the remaining producer would require a text popup already displayed before Auto
  Play becomes active and then a killAll-triggering civic/religion/research change
  while it remains in the displayed set; that normal UI sequence has not been
  established locally;
* even if reproduced, the counter only gates popup audio. One leaked count would
  suppress the next popup-up sound and then the next ordinary launch report would
  decrement the counter again. No gameplay state or popup decision is lost.

This is worth a tiny robustness cleanup -- report the skipped launch, or move the
Auto Play suppression through the common tail -- but it does not meet the album's
root threshold without a stronger producer/consequence.

F312 / KI#635 -- LOAD-TRANSPORT POPUP ORDINAL CAN RETARGET TO A NEW TEAMMATE TRANSPORT IN SIMULTANEOUS MP
----------------------------------------------------------------------------------------------------------
ROOT
~~~~
BUTTONPOPUP_LOADUNIT does not encode the transport that each displayed button
represents. The launcher enumerates the currently eligible transports and stores
only an ordinal 1, 2, 3, ... as the button ID:

    int iCount = 1;
    FOR_EACH_UNIT_VAR_IN(pUnit, *pPlot)
    {
        if (!pSelectionGroup->canDoCommand(COMMAND_LOAD_UNIT,
            pUnit->getOwner(), pUnit->getID()))
        {
            continue;
        }
        ...
        popupAddGenericButton(..., iCount, WIDGET_GENERAL);
        iCount++;
    }

When the human later clicks, OnOkClicked does not resolve a stored owner+unit ID.
It enumerates the CURRENT plot again, decrements the old ordinal, and sends the
load command for whichever transport now occupies that position:

    int iCount = pPopupReturn->getButtonClicked();
    FOR_EACH_UNIT_VAR_IN(pUnit, pSelectionGroup->getPlot())
    {
        if (pSelectionGroup->canDoCommand(COMMAND_LOAD_UNIT,
            pUnit->getOwner(), pUnit->getID()))
        {
            iCount--;
            if (iCount == 0)
            {
                selectionListGameNetMessage(... COMMAND_LOAD_UNIT,
                    pUnit->getOwner(), pUnit->getID());
                break;
            }
        }
    }

WIP05 had already established that this is weaker than a stable-ID design and that
a same-team remote transport can change the candidate set while the local modal
popup remains open. What had not yet been proved was a deterministic RETARGET,
rather than merely a stale ordinal becoming a no-op.

CURRENT PRODUCER / ORDERING PROOF
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The missing ordering fact is in CvPlot::addUnit. Plot units are not append-only.
A newly arriving unit is inserted at the first point where isBeforeUnitCycle says
it belongs:

    CvUnit const& kLoopUnit = *::getUnit(pUnitNode->m_data);
    if (!kLoopUnit.isBeforeUnitCycle(kUnit))
        break;
    ...
    m_units.insertBefore(kUnit.getIDInfo(), pUnitNode);

CvUnit::isBeforeUnitCycle's FIRST ordering key is owner ID:

    if (getOwner() != kOther.getOwner())
        return (getOwner() < kOther.getOwner());

CvUnit::canLoadOnto explicitly allows a transport owned by another player on the
same team:

    if (kUnit.getTeam() != getTeam())
        return false;

There is no same-player ownership requirement.

Therefore a fully current supported simultaneous-MP sequence is:

1. Human P selects cargo units on plot X. Two valid transports A and B are on X,
   so CvGame::handleAction creates BUTTONPOPUP_LOADUNIT and the launcher displays,
   for example, A as button ordinal 1 and B as ordinal 2.
2. The popup is immediate/modal only on P's client. With simultaneous turns, a
   teammate Q can continue issuing moves on another client while P is deciding.
3. Let Q have a lower PlayerTypes ID than P. Q moves valid same-team transport C
   onto X.
4. CvPlot::addUnit inserts C ahead of P's A/B because owner ID is the first cycle
   sort key. The current eligible order is now C, A, B.
5. P clicks the still-visible button labelled/described as A (old ordinal 1).
6. OnOkClicked re-enumerates current plot units. Ordinal 1 now resolves C.
7. canDoCommand(COMMAND_LOAD_UNIT, Q, C-ID) succeeds because C is same-team and
   otherwise load-compatible; the sent command therefore loads P's selected units
   onto C, not onto the transport P chose from the popup.

This does not require stale object memory, ID aliasing, or malformed packets. Both
old and new transports are legitimate current objects; the defect is that UI
identity is represented by a mutable ordinal.

CONSEQUENCE
~~~~~~~~~~~
The player's explicit popup choice can execute against the wrong transport. In
cooperative simultaneous multiplayer this can transfer/load the selected cargo
onto a teammate-owned transport that arrived only after the choice list was
rendered. Downstream command validation cannot repair the semantic mismatch because
the newly resolved transport is itself a legal target.

Severity is lower than F308's cross-player Great-General alias/crash-risk family,
but this is still a concrete wrong-action UI/network race with a supported producer,
not merely defensive hardening.

ANCESTRY
~~~~~~~~
The ordinal-at-launch / re-enumerate-at-reply design is inherited. Supplied K-Mod,
Base AdvCiv and Civ4CE all use the same conceptual scheme for BUTTONPOPUP_LOADUNIT.
Current AdvCiv/SAS added useful null checks and a single-transport auto-load path,
but retained ordinal identity for the multi-choice popup.

FIX DIRECTION
~~~~~~~~~~~~~
Do not identify transports by list position. Each displayed button needs a stable
transport identity containing BOTH owner and player-local unit ID, because unit ID
alone is not globally unique.

Possible approaches:

* encode owner + unit ID in popup button payload/data if the popup interface has two
  stable integer fields available per choice;
* maintain a popup-owned mapping from button ID to IDInfo and re-resolve that exact
  unit on reply;
* or introduce a network/UI helper whose choice token is an IDInfo-equivalent.

On reply, re-resolve the stored transport and repeat canDoCommand before sending the
command. If the exact chosen transport disappeared or became illegal, do nothing or
refresh the popup; never silently substitute another current ordinal.

NUMBERING
~~~~~~~~~
F312 is CONFIRMED and consumes provisional KI#635.
F313 / provisional KI#636 is now the next unused root slot.

RECONCILIATION WITH WIP05
-------------------------
WIP05 said the generic LOADUNIT ordinal observation should remain hardening unless
runtime/source evidence made the retarget important. The new CvPlot::addUnit /
isBeforeUnitCycle proof supplies exactly that missing evidence and deterministically
constructs a wrong-target sequence. The earlier note is therefore promoted, not
counted as a separate issue.

CONTINUATION FROM C030-WIP06
----------------------------
Queue 019 remains OPEN. Continue with F313 UNUSED through:
  * Python popup callbacks / ownership and callback index lifetime;
  * DETAILS / ADMIN / ADMIN_PASSWORD / PBEM-PitBoss/session popups;
  * Rise & Fall custom popup contracts;
  * CHOOSETECH / production stale-choice receiver revalidation;
  * VASSAL_DEMAND_TRIBUTE master-side delayed-state control;
  * remaining OnFocus cancellation asymmetries and stable-ID pairs;
  * final all-43-function + history/ancestry saturation.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP06


======================================================================
C030-WIP07 -- QUEUE 019 FREE-TECH STALE-CHOICE ROOT + SESSION/RF CONTROLS
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F313 are CONFIRMED in queue 019.
F314 / provisional KI#637 is UNUSED.

SESSION / PYTHON / RISE-&-FALL CONTROL PASS
-------------------------------------------
The remaining generic callback/session popup families were paired with their reply
identity rather than judged from launcher code alone.

* BUTTONPOPUP_PYTHON: each button ID is simply the index into the same CvPopupInfo
  Python-button array whose callback name/data are retained by the popup info. The
  generic layer intentionally delegates semantic validation to the Python callback;
  no mutable game-object ordinal is reconstructed in C++.

* BUTTONPOPUP_DETAILS: launch and reply both refer to the local runtime active
  player. PBEM sends the turn only AFTER applying the edited player details. A
  normal Hotseat/PBEM active-player handoff cannot occur behind a still-unanswered
  local modal details popup. No independent remote producer was found that changes
  the client's active-player slot while this popup is live.

* ADMIN / ADMIN_PASSWORD carry game-global settings and the current runtime
  ControlTypes value. The password reply only dispatches the two controls for which
  this popup is created (World Builder/Admin Details); no stale object pointer or
  player-local ID is retained.

* ADDBUDDY / forced-disconnect / PitBoss-disconnect / kicked are session-service
  prompts with copied text/IDs or an unconditional exit-to-menu action. Their state
  is not reconstructed by mutable gameplay list position.

* Rise & Fall civ selection is stronger than the ordinary LOADUNIT/LEADUNIT popup
  design: each displayed civilization uses WIDGET_RF_CIV_CHOICE with the actual
  PlayerTypes as widget data, and CvDLLWidgetData forwards that stable player ID to
  RiseFall::handleCivSelection. Defeat/retire use chapter/button state owned by the
  local Rise & Fall controller. Rise & Fall is a local-control game mode; no
  simultaneous remote-client producer was established that can rewrite the eligible
  civ set behind this modal choice.

These families remain controls, not F314 candidates.

F311 BROADENING -- MASTER-SIDE VASSAL-DEMAND POPUP HAS THE SAME REPLY-TIME RELATIONSHIP GAP
-------------------------------------------------------------------------------------------
F311 was initially proved through BUTTONPOPUP_VASSAL_GRANT_TRIBUTE on the human
vassal's client. The inverse BUTTONPOPUP_VASSAL_DEMAND_TRIBUTE path belongs to the
same root and should be repaired at the same boundary.

The master-side launcher checks at display time:

    if (!GET_TEAM(kVassal.getTeam()).isVassal(getActiveTeam()))
        return false;

but OnOkClicked immediately trusts info.data1 as the old vassal and contains no
current isVassal check. For an AI vassal, a rejected `AI_considerOffer` flows
straight to sendChangeWar(TEAMID(eVassal), true), i.e. the same un-revalidated war
message family as the already proved human-vassal Refuse path.

A supported remote transition exists in cooperative simultaneous team play: human
master M can have another human teammate T on the same team. While M's locally
modal demand popup against AI vassal V remains open, T can negotiate team-wide
voluntary vassalage to a third HUMAN player/team C. TRADE_VASSAL is team-wide and
CvDeal::startTrade -> CvTeam::setVassal makes M/T's team a vassal; setVassal then
frees that team's existing vassals, including V. M's already rendered demand popup
has no OnFocus relation check or invalidation hook. A stale resource click can
therefore continue into AI consideration and, on rejection, the direct declaration
path against a civilization that is no longer the team's vassal.

This is NOT a new number. It strengthens F311's root from "grant tribute reply does
not revalidate the relationship" to the broader contract:

    both vassal-tribute reply handlers trust launch-time vassalage after a modal
    time gap and can reach a direct war path after that relationship has ended.

The narrow repair should re-resolve and verify the current master/vassal
relationship in BOTH BUTTONPOPUP_VASSAL_DEMAND_TRIBUTE and
BUTTONPOPUP_VASSAL_GRANT_TRIBUTE before any response action.

F313 / KI#636 -- STALE FREE-TECH POPUP CAN CONSUME THE FREE CHOICE ON A TECHNOLOGY A TEAMMATE ACQUIRED WHILE IT WAS OPEN
------------------------------------------------------------------------------------------------------------------------
ROOT
~~~~
BUTTONPOPUP_CHOOSETECH uses current research eligibility when constructing its
buttons. For a free technology (info.data1 / iDiscover > 0), each displayed tech is
added only after:

    if (!kActivePlayer.canResearch(eTech))
        continue;

and the button carries the stable technology ID through WIDGET_RESEARCH together
with iDiscover. Stable ID identity is good, but eligibility is not revalidated at
the point that consumes the free entitlement.

The K-Mod OnFocus code itself documents the stale-list concern:

    // K-Mod todo: Perhaps we should cancel this popup and create a new one
    // on-the-fly so that we can be sure the tech-list is up-to-date.

For ordinary research (iDiscover == 0), OnFocus cancels after current research is
set or game over. For the free-tech branch it only does:

    else if (!GET_PLAYER(getActivePlayer()).isChoosingFreeTech())
        popupSetAsCancelled(...);

It does NOT rebuild/cancel merely because one displayed technology has become known
or otherwise unresearchable.

The click path then consumes the local free-tech-choice count BEFORE sending the
research message:

    if (widgetDataStruct.m_iData2 > 0)
    {
        if (!kActivePlayer.isChoosingFreeTech()) ...
        else kActivePlayer.changeChoosingFreeTechCount(-1);
    }
    sendResearch(eNewActiveResearch, widgetDataStruct.m_iData2, bShift);

On synchronized execution, CvNetResearch treats m_iDiscover > 0 as an unconditional
free-tech grant:

    GET_TEAM(kPlayer.getTeam()).setHasTech(m_eTech, true, ...);

There is no current `canResearch` / `!isHasTech` guard on that free-tech branch.
CvTeam::setHasTech immediately returns when the team already has the technology:

    if (isHasTech(eTech) == bNewValue)
        return;

Thus a stale but valid TechTypes ID can consume the free choice without granting
anything.

SUPPORTED SIMULTANEOUS-TEAM PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
1. Human P receives one free-tech choice and BUTTONPOPUP_CHOOSETECH displays tech T,
   which is currently researchable and not yet known by P's team.
2. P has human teammate Q in a simultaneous multiplayer game. The popup is modal on
   P's client only; Q continues acting on another client.
3. While P is deciding, Q causes the shared team to acquire T -- e.g. through an
   accepted technology trade with another team. Technology ownership is team-wide.
4. P's already-rendered free-tech popup is not rebuilt merely because T became
   known. Its T button still exists.
5. P clicks T. CvDLLWidgetData::doResearch sees a free-tech click and decrements
   P's choosing-free-tech count.
6. CvNetResearch::Execute calls setHasTech(T, true). Since the team already owns T,
   setHasTech returns immediately.
7. For a one-tech entitlement (iDiscover == 1), no replacement chooseTech call is
   made. The entitlement is gone and P receives no technology from it.

The producer does not depend on object deletion, enum aliasing, malformed packets,
or an EXE timing guess. Simultaneous remote teammate actions are precisely the kind
of state change a local modal popup cannot freeze.

CONSEQUENCE
~~~~~~~~~~~
A legitimate free technology can be silently lost. The user clicked the technology
that the still-visible popup offered, but by execution time it is already team-owned;
the grant becomes a no-op after the free-choice state has been consumed.

For iDiscover > 1, the next free-tech popup is still created with iDiscover-1, so
one of the multiple free choices is likewise lost.

ANCESTRY
~~~~~~~~
The relevant architecture is inherited from K-Mod / Base AdvCiv. Supplied K-Mod
contains both:

* the OnFocus TODO acknowledging that the tech list may be stale;
* the free-tech widget-side decrement followed by CvNetResearch's unconditional
  setHasTech branch.

Current SAS retains those contracts. This is not introduced by the recent SAS popup
changes.

FIX DIRECTION
~~~~~~~~~~~~~
The free entitlement must be consumed only after the chosen technology is still a
valid current free-tech target.

At minimum:

1. Before decrementing the free-tech-choice state in CvDLLWidgetData::doResearch,
   recheck that the exact selected TechTypes is still researchable/not already
   known. If stale, do not consume the entitlement; refresh/reopen the choice.
2. Defense-in-depth at CvNetResearch::Execute: for m_iDiscover > 0, reject an
   already-known or otherwise invalid free-tech target rather than silently treating
   setHasTech's no-op as a successful consumption. The target player's local client
   should be given/reopened the same remaining free-tech choice.
3. Rebuilding/canceling the free-tech popup when relevant team tech state changes
   would improve UX, but receiver-side validation is still required for network
   latency between click and execution.

Care is needed because m_iChoosingFreeTechCount is UI/control state with historical
K-Mod multiplayer handling; the repair should preserve the entitlement exactly once
rather than merely adding another unconditional chooseTech increment on every
client.

DISTINCTNESS
~~~~~~~~~~~~
This is not F312: F312 resolves a mutable ordinal to the WRONG legal transport.
F313 carries the correct stable TechTypes but fails to revalidate its changed
eligibility before consuming a one-shot entitlement.

It is also unrelated to F020/KI#341, which concerns initial-score normalization of
civilization free starting technologies.

NUMBERING
~~~~~~~~~
F313 is CONFIRMED and consumes provisional KI#636.
F314 / provisional KI#637 is now the next unused root slot.

CONTINUATION FROM C030-WIP07
----------------------------
Queue 019 remains OPEN. Continue with F314 UNUSED through:
  * production popup stale-choice semantics (stable class/project IDs + pushOrder
    receiver canTrain/canConstruct/canCreate/canMaintain validation already look
    substantially safer than free-tech consumption);
  * CHANGERELIGION / CHANGECIVIC delayed-state execution;
  * event-choice reply-time canDoEvent validation beyond F307's launch mutation;
  * deal-cancel / confirm-command / main-menu delayed controls;
  * final OnAltExecute and OnFocus cancellation semantics;
  * all-43-function reconciliation and history/ancestry saturation.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP07


======================================================================
C030-WIP08 -- QUEUE 019 STALE SECRETARY-GENERAL SELECTION / MIN-VOTER ROOT
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F314 are CONFIRMED in queue 019.
F315 / provisional KI#638 is UNUSED.

F314 / KI#637 -- STALE SECRETARY-GENERAL RESOLUTION POPUP CAN BYPASS THE CURRENT MINIMUM-VOTER RULE
-------------------------------------------------------------------------------------------------
ROOT
~~~~
Human Secretary-General resolution selection is a two-stage delayed contract:

1. CvGame::addVoteSelection(eVoteSource) snapshots the currently valid resolution
   options into VoteSelectionData.
2. CvDLLButtonPopup displays those copied options in BUTTONPOPUP_CHOOSEELECTION.
3. A later human click sends only the stored selection ID + option ordinal.
4. CvGame::setVoteChosen turns that old snapshot into a live VoteTriggeredData.

K-Mod deliberately strengthened stage 1 by replacing the generic resolution test:

    //if (canDoResolution(eVoteSource, kData))
    if (isValidVoteSelection(eVoteSource, kData)) // K-Mod (zomg!)

isValidVoteSelection contains validity that is NOT part of canDoResolution. Most
importantly, it counts the CURRENT eligible voting teams and rejects the proposal
when they fall below CvVoteInfo::getMinVoters():

    int iVoters = 0;
    ...
    if (iVoters < GC.getInfo(kData.eVote).getMinVoters())
        return false;

Current shipped CIV4VoteInfo.xml sets iMinVoters=3 for the vote definitions checked
in this pass, including ordinary choose-election resolutions.

The delayed human reply does not re-run that strengthened validity contract.
CvDLLButtonPopup::OnOkClicked merely verifies that the old VoteSelectionData still
exists and that the old ordinal is in range:

    VoteSelectionData* pData = kGame.getVoteSelection(info.getData1());
    if (pData != NULL &&
        pPopupReturn->getButtonClicked() < (int)pData->aVoteOptions.size())
    {
        sendChooseElection((VoteTypes)pPopupReturn->getButtonClicked(),
                info.getData1());
    }

(The sendChooseElection parameter type/name is historical; this value is the option
ordinal, not the option's VoteTypes.)

The synchronized receiver then trusts the snapshot:

    void CvGame::setVoteChosen(int iSelection, int iVoteId)
    {
        VoteSelectionData* pVoteSelectionData = getVoteSelection(iVoteId);
        if (pVoteSelectionData != NULL)
            addVoteTriggered(*pVoteSelectionData, iSelection);
        deleteVoteSelection(iVoteId);
    }

addVoteTriggered(VoteSelectionData,int) only checks ordinal bounds and copies the old
VoteSelectionSubData. It does not call isValidVoteSelection.

The later result-time cancellation is also insufficient. CvGame::doVoteResults calls
canDoResolution(eVoteSource, subdata), not isValidVoteSelection. canDoResolution
checks whether the resolution remains semantically executable by current members,
but contains no getMinVoters check. Thus the exact validity K-Mod added at proposal
construction can disappear while the human popup is pending and never be checked
again.

SUPPORTED CURRENT PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~
This does not rely on an imagined EXE race. Current turn ordering exposes a normal
simultaneous-multiplayer time gap:

    incrementGameTurn();
    ...
    doDiploVote();
    if (isMPOption(MPOPTION_SIMULTANEOUS_TURNS))
    {
        ...
        kLoopPlayer.setTurnActive(true);
    }

CvGame::doDiploVote -> doVoteSelection therefore creates the human Secretary-
General's VoteSelectionData and queues BUTTONPOPUP_CHOOSEELECTION before the new
turn's simultaneous players are activated. The popup is immediate/modal only on the
Secretary-General player's local client; other human clients can act independently
once their turns are activated.

A concrete current sequence is:

1. Vote source V has exactly three eligible voting teams. Human Secretary General S
   receives a resolution-selection popup. An ordinary non-targeted proposal such as
   Free Trade is included because isValidVoteSelection sees iVoters == 3 and current
   semantic requirements are satisfied.
2. While S leaves that popup unanswered, another simultaneous human client acts.
   One of the three voting civilizations/teams is eliminated, or otherwise loses
   all vote-source voting membership, leaving only two current voting teams.
3. A fresh isValidVoteSelection for the displayed resolution now returns false:
   current iVoters == 2 < shipped iMinVoters == 3.
4. S clicks the still-visible old resolution button. CHOOSEELECTION has no OnFocus
   refresh beyond cancelling when GAMESTATE_OVER, so the stale option remains.
5. setVoteChosen copies the old option into a live VoteTriggeredData without the
   current isValidVoteSelection check.
6. doVoteResults later calls canDoResolution, which can still return true for an
   ordinary resolution whose remaining two members can comply.
7. Vote threshold is recomputed from current vote population by getVoteRequired;
   there is no separate minimum-team floor there. The remaining voters can therefore
   meet the percentage threshold and pass the ballot.
8. setVoteOutcome/processVote applies a real resolution effect -- e.g. Free Trade
   changes the global free-trade count; other generic resolutions can similarly
   alter no-nukes/forced-civic/trade-route state -- despite the current electorate
   being below the minimum required to propose that resolution.

The producer requires neither stale object memory nor malformed network input. It is
ordinary asynchronous human decision time in supported simultaneous multiplayer,
combined with a membership-changing game event on another client.

CONSEQUENCE
~~~~~~~~~~~
A resolution can be proposed and enacted with fewer current voting teams than XML
explicitly requires. This is a semantic rules bypass, not merely stale popup text.
The current game state itself says the option is invalid under isValidVoteSelection,
but the delayed human-selection path converts the historical snapshot into a live
ballot anyway.

ANCESTRY / ROOT CAUSE
~~~~~~~~~~~~~~~~~~~~~
The important split is inherited from K-Mod:

* Civ4CE/BtS's generic non-targeted branch used canDoResolution when building the
  Secretary-General choice list.
* K-Mod explicitly changed that branch to isValidVoteSelection, thereby making
  minimum voter count and the rest of that stronger predicate part of proposal
  validity.
* K-Mod left setVoteChosen/addVoteTriggered unchanged and left result-time
  cancellation on canDoResolution.
* Base AdvCiv and current SAS inherit the same split.

So this is best understood as an incomplete K-Mod strengthening: proposal creation
learned a stronger validity predicate, but delayed human acceptance and result
validation were not brought onto the same contract.

DISTINCTNESS
~~~~~~~~~~~~
This is distinct from the other queue-019 vote roots:

* F309 is ownership/lifetime: choosing None/Early Election fails to run the normal
  setVoteChosen cleanup and leaks VoteSelectionData.
* F310 is candidate validity during an already-created TEAM VOTE: stale candidate
  team IDs are accepted/counted after eligibility changes.
* F314 is resolution-option validity BEFORE a ballot exists: a stale Secretary-
  General proposal snapshot bypasses current isValidVoteSelection, specifically
  including K-Mod's minimum-voter rule, and creates a ballot that should no longer
  be creatable.

No prior album finding mentioning iMinVoters or this setVoteChosen validity split was
found during duplicate reconciliation.

FIX DIRECTION
~~~~~~~~~~~~~
The synchronized transition from VoteSelectionData to VoteTriggeredData needs to
revalidate the exact stored VoteSelectionSubData against CURRENT game state before
creating the ballot.

A narrow repair is conceptually:

    VoteSelectionData* pData = getVoteSelection(iVoteId);
    if (pData != NULL && iSelection >= 0 &&
        iSelection < (int)pData->aVoteOptions.size() &&
        isValidVoteSelection(pData->eVoteSource,
            pData->aVoteOptions[iSelection]))
    {
        addVoteTriggered(*pData, iSelection);
    }
    deleteVoteSelection(iVoteId);

The normal cleanup should still happen even when the old choice is now invalid.
For UX, the Secretary General could be offered a fresh current selection rather than
silently losing the proposal opportunity, but synchronized validity belongs at this
transition regardless of UI refresh.

Defense-in-depth could also make doVoteResults cancel any live vote whose current
selection-level validity no longer holds. That requires care: some targeted
resolution conditions are intentionally re-evaluated differently after voting has
begun, so blindly replacing canDoResolution with isValidVoteSelection at result time
could alter legitimate semantics. The safest primary boundary is setVoteChosen,
where a ballot has not yet been created and proposal validity is exactly the relevant
contract.

NUMBERING
~~~~~~~~~
F314 is CONFIRMED and consumes provisional KI#637.
F315 / provisional KI#638 is now the next unused root slot.

CONTINUATION FROM C030-WIP08
----------------------------
Queue 019 remains OPEN. Continue with F315 UNUSED through:
  * remaining confirm-command / main-menu / deal/session delayed actions;
  * espionage popup target identity and receiver revalidation, including keeping
    dormant shipped-XML branches separate from live paths;
  * OnAltExecute semantics and any popup relaunch/keyboard alternate path;
  * final OnFocus cancellation coverage;
  * explicit reconciliation of all 43 CvDLLButtonPopup function definitions against
    the queue-019 matrix and K-Mod/Base/CE ancestry/history.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP08


======================================================================
C030-WIP09 -- QUEUE 019 CONFIRM-DELETE SELECTION-REBIND ROOT
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F315 are CONFIRMED in queue 019.
F316 / provisional KI#639 is UNUSED.

F315 / KI#638 -- DELETE CONFIRMATION CAN REBIND TO AN UNRELATED UNIT GROUP AFTER RAPID CYCLING DURING THE MODAL POPUP
-------------------------------------------------------------------------------------------------------------------
ROOT
~~~~
BUTTONPOPUP_CONFIRMCOMMAND preserves which ActionInfo the player asked to confirm,
but it does NOT preserve the selected units/group that the confirmation described.
The producer in CvGame::handleAction stores only:

    pInfo->setData1(iAction);
    pInfo->setOption1(bAlt);

The popup text is therefore effectively "Are you sure you want to perform this
ACTION?" against the selection that existed at construction time. On Yes,
CvDLLButtonPopup::OnOkClicked sends that action to whatever selection happens to be
CURRENT at reply time:

    int iAction = info.getData1();
    kGame.selectionListGameNetMessage(GAMEMESSAGE_DO_COMMAND,
            GC.getActionInfo(iAction).getCommandType(),
            GC.getActionInfo(iAction).getCommandData(), -1, 0,
            info.getOption1());

selectionListGameNetMessage then enumerates the current UI selection and sends one
CvNetDoCommand per currently selected unit. There is no launch-time unit/group
identity in the popup to compare against.

CURRENT SHIPPED SCOPE
~~~~~~~~~~~~~~~~~~~~~
The broad shape is narrowed by XML: current shipped CIV4CommandInfos.xml has
bConfirmCommand=1 only for COMMAND_DELETE. Thus the live consequence is not an
arbitrary command being retargeted; it is specifically a confirmed unit deletion /
scrap action being applied to a replacement selection.

SUPPORTED PRODUCER -- REMOTE DEATH + RAPID UNIT CYCLING
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A deterministic supported path exists in simultaneous multiplayer when the shipped
BUG option "Rapid Unit Cycling" is enabled.

CvUnit::kill contains special handling for the last selected unit:

    if (IsSelected())
    {
        if (gDLL->UI().getLengthSelectionList() == 1)
        {
            if (!gDLL->UI().isFocused() &&
                !gDLL->UI().isCitySelection() &&
                !gDLL->UI().isDiploOrPopupWaiting())
            {
                GC.getGame().updateSelectionList();
            }
            if (IsSelected())
                GC.getGame().cycleSelectionGroups_delayed(1, false);
            ...
        }
    }
    gDLL->UI().removeFromSelectionList(this);

The popup-waiting test protects only the immediate updateSelectionList call. It does
NOT protect the following cycleSelectionGroups_delayed call.

K-Mod's cycleSelectionGroups_delayed supports the user-visible BUG option:

    BUGOption::isEnabled("MainInterface__RapidUnitCycling", false)

When enabled and the camera is not locked, the helper directly executes:

    cycleSelectionGroups(true);

rather than merely arming the delayed engine counter. cycleSelectionGroups can call:

    gDLL->UI().selectUnit(pNextSelectionGroup->getHeadUnit(), bClear);

and therefore replace the old selected unit with another ready selection immediately.
There is no isDiploOrPopupWaiting guard in this rapid path.

A concrete sequence is:

1. Human P selects scrap-eligible unit/group A and invokes COMMAND_DELETE.
2. Because COMMAND_DELETE has bConfirmCommand=1, the game opens
   BUTTONPOPUP_CONFIRMCOMMAND. The popup stores only the Delete ActionInfo; A's
   ID/group identity is not stored.
3. P leaves the modal popup unanswered. In simultaneous multiplayer, enemy human Q
   continues acting on another client and attacks A.
4. Q destroys P's LAST currently selected unit while the confirmation popup is still
   waiting on P's client. Network/game-state processing continues despite the local
   modal decision.
5. CvUnit::kill sees isDiploOrPopupWaiting and therefore skips updateSelectionList,
   but A is still selected at that point, so it calls cycleSelectionGroups_delayed.
6. With Rapid Unit Cycling enabled, that helper immediately runs
   cycleSelectionGroups(true) and selects another ready P group B before the old
   unit is removed from the selection list.
7. P's old "Are you sure?" popup is still visible. P clicks Yes believing it refers
   to A.
8. OnOkClicked resolves no old unit identity; it sends COMMAND_DELETE to the CURRENT
   selection B.
9. CvNetDoCommand/CvUnit::doCommand revalidate COMMAND_DELETE through canScrap, but
   if B is itself scrap-eligible that validation succeeds. B is deleted.

Thus ordinary receiver legality checks cannot repair the semantic mismatch: B is a
perfectly legal unit to scrap; it is simply not the unit/group whose deletion the
player confirmed.

CONSEQUENCE
~~~~~~~~~~~
A local confirmation dialog can delete an unrelated replacement unit/group selected
automatically after the original selection dies. This is direct unintended loss of
player units caused by stale popup target identity.

The scenario is configuration-dependent because Rapid Unit Cycling defaults to
False, but it is a shipped, exposed BUG option with normal gameplay support. The
audit does not treat an explicitly supported user option as an unsupported/pathological
configuration.

ANCESTRY / ROOT CAUSE
~~~~~~~~~~~~~~~~~~~~~
The popup's "remember action, use current selection later" design is inherited from
BtS/Civ4CE.

The concrete immediate-rebind producer is introduced by K-Mod's Rapid Unit Cycling
work. K-Mod commit 280 changed CvUnit::kill from:

    setCycleSelectionCounter(1);

to:

    cycleSelectionGroups_delayed(1, false);

and introduced the new helper/option whose rapid branch directly calls
cycleSelectionGroups(true). The pre-existing kill code's isDiploOrPopupWaiting guard
remained around updateSelectionList only. K-Mod therefore turned what had been an
engine-delayed cycle request into a possible immediate selection replacement inside
the popup-waiting death path without changing CONFIRMCOMMAND to retain its original
target.

Base AdvCiv and current SAS inherit both halves unchanged in substance.

DISTINCTNESS
~~~~~~~~~~~~
This is distinct from F312:

* F312 stores a mutable ORDINAL for a transport choice and later resolves that
  ordinal to the wrong legal transport.
* F315 stores the correct stable ACTION (Delete), but no target identity at all;
  K-Mod rapid cycling changes the current selection while the confirmation is still
  open, so the action is executed on the wrong legal selected group.

No prior album root for BUTTONPOPUP_CONFIRMCOMMAND / COMMAND_DELETE target rebinding
was found in duplicate search.

FIX DIRECTION
~~~~~~~~~~~~~
The strongest fix is to bind the confirmation to the selection it describes rather
than whatever happens to be selected later.

Possible approaches:

1. Store stable IDInfo identities for the units being confirmed and, on Yes,
   re-resolve exactly those surviving units and issue COMMAND_DELETE only to them.
   If all original units disappeared, the confirmation becomes a no-op.
2. For the currently shipped scope (only COMMAND_DELETE), a narrower popup-specific
   snapshot of the selected player-local unit IDs can be sufficient if ownership is
   implicit and unchanged; IDInfo is safer/general if the mechanism is later reused.
3. Defense-in-depth: cancel BUTTONPOPUP_CONFIRMCOMMAND when its original selection
   disappears/changes, or suppress rapid selection cycling while such a popup is
   outstanding. This improves UX but target identity at reply remains the robust
   contract.

Do NOT simply add canDoCommand at OnOk: the wrong replacement unit B can legitimately
pass canScrap, which is why receiver validation cannot detect the stale confirmation.

NUMBERING
~~~~~~~~~
F315 is CONFIRMED and consumes provisional KI#638.
F316 / provisional KI#639 is now the next unused root slot.

FUNCTION-COUNT BOOKKEEPING
--------------------------
The queue-019 implementation contains 43 CvDLLButtonPopup member definitions when
CvDLLButtonPopup::getInstance (reference return type) is included. A simple grep
pattern that only matched void/bool/constructor/destructor lines counted 42 and
missed getInstance. The intended final saturation target therefore remains 43, not
42; this is audit bookkeeping only, not a source defect.

CONTINUATION FROM C030-WIP09
----------------------------
Queue 019 remains OPEN. Continue with F316 UNUSED through:
  * remaining espionage popup / target identity and synchronized receiver checks;
  * main-menu/confirm-menu/extended-game delayed-control state and OnAltExecute;
  * final OnFocus and relaunch contracts;
  * explicit 43/43 member-definition coverage matrix;
  * final SAS-vs-Base semantic-delta and K-Mod/Base/Civ4CE history saturation.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP09


======================================================================
C030-WIP10 -- QUEUE 019 F315 BROADENING / SELECTION-BOUND MODAL TARGET IDENTITY
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F315 are CONFIRMED in queue 019.
F316 / provisional KI#639 remains UNUSED.

This checkpoint BROADENS the already-confirmed F315 from C030-WIP09. It does not
allocate another root number. The narrower WIP09 title focused on CONFIRMCOMMAND /
COMMAND_DELETE because that was the first fully proven manifestation. The same
underlying missing target-identity contract also affects other selection-bound modal
popups once K-Mod Rapid Unit Cycling replaces the launch-time selection while the
modal is still open.

F315 / KI#638 -- SELECTION-BOUND MODAL POPUPS CAN REBIND TO AN UNRELATED UNIT GROUP AFTER K-MOD RAPID CYCLING
---------------------------------------------------------------------------------------------------------------
GENERAL ROOT
~~~~~~~~~~~~
Several popup families are constructed from the CURRENT selected unit/group, but the
popup stores only the requested action/mission/auxiliary choice. It does not retain
stable identity for the unit/group/plot whose state the popup described. At reply,
CvDLLButtonPopup::OnOkClicked sends the stored action to whatever selection is
CURRENT then.

Normally that historical design relies on a practical UI invariant: the selected
unit/group does not silently change beneath an unanswered selection-bound modal.
K-Mod's Rapid Unit Cycling breaks that invariant in one supported path. When the
last selected unit dies while a popup is waiting, CvUnit::kill deliberately skips
updateSelectionList because isDiploOrPopupWaiting() is true, but still calls:

    GC.getGame().cycleSelectionGroups_delayed(1, false);

if the dying unit remains selected. With the user-visible BUG option
MainInterface__RapidUnitCycling enabled, cycleSelectionGroups_delayed can immediately
call cycleSelectionGroups(true) when the camera is not locked. There is no
isDiploOrPopupWaiting guard around that rapid path. A different ready group can
therefore become current before the old modal is answered.

The receiver-side legality checks then validate the REPLACEMENT selection. They
cannot determine that the popup text/options were generated for another unit/plot.

MANIFESTATION A -- CONFIRMCOMMAND / COMMAND_DELETE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This is the original C030-WIP09 proof and remains the simplest destructive example.
Current shipped XML sets bConfirmCommand=1 only for COMMAND_DELETE. The popup stores
the ActionInfo but no launch-time unit/group identity. If original selected unit A
dies while the modal waits and Rapid Unit Cycling selects scrap-eligible unit/group
B, clicking the old Yes sends Delete to current selection B. B passes normal
canScrap validation and is deleted even though the confirmation described A.

MANIFESTATION B -- ONE-PHASE ESPIONAGE POPUP CAN EXECUTE AT A DIFFERENT ENEMY CITY
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
launchDoEspionagePopup resolves an eligible selected Spy and builds mission buttons
from THAT spy's current plot and target player:

    CvSelectionGroup const* pSelectionGroup = m_kUI.getSelectionList();
    CvUnit const* pUnit = (pSelectionGroup == NULL ? NULL :
            pSelectionGroup->getEspionageUnit());
    CvPlot const* pPlot = pUnit->plot();
    ... canDoEspionageMission(eLoopMission, pPlot->getOwner(), pPlot, -1, pUnit) ...

The popup does not retain pUnit or pPlot. On reply, BUTTONPOPUP_DOESPIONAGE simply
sends:

    selectionListGameNetMessage(GAMEMESSAGE_PUSH_MISSION,
            MISSION_ESPIONAGE, clickedMission);

against the CURRENT selection.

Current shipped espionage XML supplies especially clean direct examples:

* ESPIONAGEMISSION_CITY_POISON_WATER has bIsTwoPhases=0;
* ESPIONAGEMISSION_CITY_UNHAPPINESS has bIsTwoPhases=0;
* ESPIONAGEMISSION_CITY_REVOLT has bIsTwoPhases=0.

Therefore no second target popup refresh intervenes. A supported sequence is:

1. Human P has selected Spy A on enemy city X and opens the espionage mission popup.
   The popup lists Poison Water because that mission is legal for A at X.
2. P leaves the modal unanswered. In simultaneous multiplayer, remote player Q
   kills A.
3. CvUnit::kill runs while the modal is waiting. With Rapid Unit Cycling enabled,
   cycleSelectionGroups_delayed immediately selects ready Spy B elsewhere.
4. Assume B is on enemy city Y where Poison Water is also legal -- an ordinary state
   for two active spies.
5. P clicks the still-visible Poison Water button generated from A/X.
6. OnOkClicked sends MISSION_ESPIONAGE to current selection B. CvUnit::espionage
   derives the target player and mission plot from B's CURRENT plot, and
   CvPlayer::doEspionageMission revalidates legality for B/Y.
7. Because Poison Water is legal there, it succeeds at Y.

The synchronized legality check is therefore not a repair: Y is a legal target, but
it is not the city whose popup the human answered.

MANIFESTATION C -- TWO-PHASE ESPIONAGE TARGET POPUP HAS THE SAME IDENTITY GAP
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
launchDoEspionageTargetPopup likewise derives its button list from the currently
selected eligible Spy and that spy's current plot/player. The popup retains only the
EspionageMissionTypes in info.data1 and the clicked extra-data value (building,
project, technology, civic, religion, etc.). It retains no original Spy IDInfo,
plot coordinates or target player.

On reply:

    selectionListGameNetMessage(GAMEMESSAGE_PUSH_MISSION, MISSION_ESPIONAGE,
            (EspionageMissionTypes)info.getData1(), clickedExtraData);

again targets the CURRENT selection. If original Spy A dies and Rapid Unit Cycling
selects Spy B at a different enemy city/player, the stale target choice can execute
there whenever the same stable extra-data is legal for B's current target. Examples
include the same technology being stealable from a different rival or the same
building type existing in a different city. If the old choice is not legal there,
receiver validation safely no-ops; when it IS legal, it cannot know that the popup
was built for A's target.

Current shipped XML has active two-phase Destroy Building, Destroy Project, Buy Tech,
Switch Civic and Switch Religion missions, so this is not dependent on dormant XML.
The Destroy Unit branch remains a separate dormant hardening note because current
shipped XML has no active iDestroyUnitCostFactor; do not allocate a root for that
branch merely from source shape.

MANIFESTATION D -- DECLAREWARMOVE CAN REPLAY THE OLD DESTINATION WITH A REPLACEMENT SELECTION
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
BUTTONPOPUP_DECLAREWARMOVE stores rival team and the intended destination/flags, but
not the moving selection that triggered the confirmation. After Yes, it gets the
CURRENT head-selected unit for its local tile-flip check and then sends the stored
MISSION_MOVE_TO coordinates to the CURRENT selection.

Thus the same original-selection-death + Rapid Unit Cycling producer can cause a
replacement group B to receive A's old move destination after the human answers A's
war/move popup. Current move legality/path handling may reject some replacements;
when B can legally perform the replayed move, the popup has silently rebound its
movement consequence to the wrong group.

COMPOUNDING WITH F312 AND F308 -- DO NOT MERGE ROOTS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
LOADUNIT and LEADUNIT are also selection-bound at reply time and can therefore be
compounded by F315 when the original selected group disappears and rapid cycling
selects another group.

Keep their existing independent roots distinct:

* F312 is the LOADUNIT mutable-ORDINAL bug. It can retarget the chosen transport even
  when the original cargo selection stays stable.
* F308 is the LEADUNIT cross-player unit-ID alias. It can resolve a displayed
  teammate unit to a different own unit even when the Great General selection stays
  stable.
* F315 is the missing launch-time SELECTION identity. It changes which group receives
  the popup action/mission after automatic rapid cycling.

ANCESTRY / ROOT CAUSE
~~~~~~~~~~~~~~~~~~~~~
The popup pattern of storing action/mission data while relying on the current UI
selection at reply is inherited from BtS/Civ4CE. Stock's selected-unit death path
requested an engine-delayed cycle with setCycleSelectionCounter(1).

K-Mod commit 280 introduced Rapid Unit Cycling and replaced that path with
cycleSelectionGroups_delayed(1, false). Its rapid branch can call
cycleSelectionGroups(true) immediately. The older isDiploOrPopupWaiting guard was
left around updateSelectionList only, so K-Mod introduced a supported way for the
current selection to change inside the very popup-waiting state that the old code
had treated specially.

Base AdvCiv and current SAS inherit this interaction. Current SAS's KI#398 espionage
launcher repair correctly chooses an eligible selected Spy instead of an arbitrary
selection head when CONSTRUCTING the espionage popup; it does not and cannot repair
the later missing launch-time identity at popup reply.

CONSEQUENCE
~~~~~~~~~~~
Depending on popup family, the player can:

* delete an unrelated replacement unit/group;
* execute a one-phase espionage mission at a different enemy city/player;
* apply a stale two-phase espionage target choice to another legal target;
* replay a declaration-of-war move destination using another selected group;
* compound LOADUNIT / LEADUNIT's separate target-resolution bugs.

The common semantic defect is that a modal decision created for selection A can
silently become a command for automatically selected B without any user action to
change the decision target.

FIX DIRECTION
~~~~~~~~~~~~~
Robust repair should bind selection-dependent popup decisions to their launch-time
target identity.

* Store stable IDInfo/unit identities (or an exact group/member snapshot appropriate
  to the popup) and re-resolve them on reply.
* For espionage, retain the launch-time Spy IDInfo and verify the same Spy/plot/target
  context before executing; cancel or rebuild if it changed.
* For DECLAREWARMOVE, retain the initiating unit/group identity before replaying the
  stored destination.
* Defense-in-depth: do not perform Rapid Unit Cycling while a selection-bound modal
  popup is outstanding, or cancel/rebuild those popups when the original selection
  disappears. This is useful UX protection, but reply-time identity validation is
  the stronger contract.

Do not rely only on current canDoCommand/canDoEspionageMission/path legality: the
wrong replacement selection may be perfectly legal, which is exactly why the
semantic retarget survives those checks.

NUMBERING
~~~~~~~~~
F315 / KI#638 remains one CONFIRMED generalized root.
F316 / provisional KI#639 remains UNUSED.

CONTINUATION FROM C030-WIP10
----------------------------
Queue 019 remains OPEN. Continue with F316 UNUSED through:
  * raze/disband, extended-game, session/disconnect and remaining launcher/reply
    contracts not yet explicitly frozen;
  * OnAltExecute / relaunch behavior and final OnFocus matrix;
  * explicit 43/43 CvDLLButtonPopup member-definition coverage;
  * final six-family SAS-vs-Base semantic-delta reconciliation;
  * final K-Mod/Base/Civ4CE history saturation and duplicate scan.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP10


======================================================================
C030-WIP11 -- QUEUE 019 PLAYER-0 CONQUEST-LIBERATION BOOLEAN COLLISION
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F316 are CONFIRMED in queue 019.
F317 / provisional KI#640 is UNUSED.

F316 / KI#639 -- RETURN-CAPTURED-CITY MISROUTES PLAYER 0 THROUGH A PLAYER-ID-AS-BOOLEAN COLLISION
-------------------------------------------------------------------------------------------------
ROOT
~~~~
AdvCiv's conquest-city popup deliberately distinguishes the special
bConquest=true liberation target from the normal liberation target. CvPlayer::acquireCity
computes:

    PlayerTypes eLiberationPlayer = kNewCity.getLiberationPlayer(true);
    ...
    pInfo->setData2(bGift ? eLiberationPlayer : NO_PLAYER);

launchRazeCityPopup uses info.data2 as that PlayerTypes and labels the button for the
stored liberation civilization.

On click, however, CvDLLButtonPopup::OnOkClicked sends that PLAYER ID as TASK_LIBERATE's
first integer payload:

    sendDoTask(info.getData1(), TASK_LIBERATE,
            info.getData2(), -1, false, false, false, false);

CvCity::doTask does not interpret the payload as a PlayerTypes. It converts it to a
BOOLEAN:

    case TASK_LIBERATE:
        liberate(iData1 != 0);
        break;

CvCity::liberate(bool bConquest) then recomputes the recipient using:

    PlayerTypes ePlayer = getLiberationPlayer(bConquest);

Thus every nonzero player ID happens to mean bConquest=true, while the perfectly
valid player ID 0 means bConquest=false.

PLAYER-0 COLLISION
~~~~~~~~~~~~~~~~~~
PlayerTypes uses NO_PLAYER=-1 and valid major-player slots are integer IDs beginning
at 0. Therefore a conquest liberation whose intended stored target is player slot 0
passes exactly 0 into TASK_LIBERATE and becomes:

    liberate(false)

instead of the intended:

    liberate(true)

This is not merely a type-cleanliness concern. The two modes can select different
recipients by design.

WHY TRUE AND FALSE CAN DIFFER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
CvCity::getLiberationPlayer has an explicit conquest-only bonus:

    if (bConquest && kLoopPlayer.getID() == getOriginalOwner())
    {
        iCultureScore *= 3;
        iCultureScore /= 2;
    }

The original owner receives a 50% score bonus only for conquest liberation. All
other candidate scoring continues normally. Therefore for a city that has been owned
by/cultured by multiple eligible civilizations, it is entirely possible for:

    getLiberationPlayer(true)  == PLAYER_0
    getLiberationPlayer(false) == PLAYER_X

For example, holding other multipliers equal, an original-owner score of 100 and
another eligible former owner's score of 140 gives PLAYER_0 a conquest score of 150
but a normal score of only 100. The conquest popup correctly chooses PLAYER_0; the
reply's false conversion then recomputes PLAYER_X instead.

The alternative normal result may also be NO_PLAYER depending on current owner and
candidate scores. In that case CvCity::liberate(false) simply returns without
transferring the city.

SUPPORTED SEQUENCE / CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
1. A human-controlled conqueror in any supported setup where its player slot is not
   0 captures a city whose original owner is alive PLAYER_0.
2. The city has enough competing eligible former-owner/culture state that the
   conquest-only 50% original-owner bonus makes PLAYER_0 the best liberation target,
   while normal liberation would choose another player (or no player).
3. CvPlayer::acquireCity computes getLiberationPlayer(true)==PLAYER_0, verifies that
   the city can legally be returned, and creates BUTTONPOPUP_RAZECITY with data2=0.
4. launchRazeCityPopup displays the Return/Liberate button naming PLAYER_0.
5. Human clicks that button.
6. OnOkClicked sends TASK_LIBERATE with iData1=0.
7. CvCity::doTask converts 0 to false and calls liberate(false).
8. The city is transferred to the normal liberation target rather than the
   civilization named by the conquest popup, or is not transferred at all.

This can therefore make an explicit human "Return city to civilization A" decision
give the city to civilization B. No simultaneous-turn race is required.

REACHABILITY / PLAYER 0
~~~~~~~~~~~~~~~~~~~~~~~
The active human need only occupy a nonzero player slot while PLAYER_0 is the intended
return target. This is a normal supported state in multiplayer/hotseat/scenarios and
other control arrangements; player slot 0 is not a sentinel. The defect is therefore
configuration-dependent only in the ordinary sense that the target has to be the
first player slot.

ANCESTRY / INTENT EVIDENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~
This is an AdvCiv regression introduced in pre-SAS AdvCiv practical commit 1719
(7bc68e5a42b54474b646a84cb64d5077bab8f9b6), titled:

    Tweak/ refactor conditions for city trade and liberation

That patch changed the conquest popup to use eLiberationPlayer and replaced K-Mod's
TASK_GIFT path with TASK_LIBERATE. The added source comment explicitly explains the
reason:

    TASK_GIFT doesn't call CvCity::liberate if the
    bConquest=true liberation player differs from the normal
    liberation player

But the same patch passed `info.getData2()` -- the PlayerTypes value -- into the
existing TASK_LIBERATE payload whose receiver interprets nonzero as the bConquest
boolean. The intended distinction was therefore implemented correctly for player
slots 1+ but accidentally collapses for player slot 0.

Base AdvCiv 1.14 and current SAS retain this code in substance. K-Mod used TASK_GIFT
here and therefore does not contain this specific PlayerTypes-as-bool regression.

DISTINCTNESS
~~~~~~~~~~~~
No prior album root for TASK_LIBERATE / player-slot-0 conquest-return misrouting was
found. This is independent of queue-019 stale-popup findings: the wrong behavior can
occur immediately after popup construction with no state change while the modal is
open.

It also explains why merely proving generational city IDs safe was insufficient for
this branch: the city object can be exactly the correct live city while the task
payload changes the meaning of the requested liberation mode.

FIX DIRECTION
~~~~~~~~~~~~~
For this conquest-popup button, send the boolean mode that TASK_LIBERATE actually
expects, not the recipient PlayerTypes:

    sendDoTask(info.getData1(), TASK_LIBERATE,
            1, -1, false, false, false, false);

CvCity::liberate(true) deliberately recomputes the current conquest liberation
recipient. If the intended contract instead requires binding the exact player named
by the popup, then TASK_LIBERATE would need a separate explicit recipient field or a
new task contract; simply passing the PlayerTypes in the boolean slot is invalid.

As defense-in-depth for delayed replies, the popup may also want to verify that the
current `getLiberationPlayer(true)` still matches the named/stored target before
executing. That is a separate stale-state policy question; it is not needed to fix
the deterministic PLAYER_0 collision.

NUMBERING
~~~~~~~~~
F316 is CONFIRMED and consumes KI#639.
F317 / provisional KI#640 is now the next unused root slot.

RELATED RAZE/DISBAND CONTROL
----------------------------
The ordinary Raze button itself does NOT have the same missing receiver-validation
problem: TASK_RAZE reaches CvPlayer::raze, which begins with `if (!canRaze(kCity))
return;`. A stale Raze button therefore rechecks current raze legality.

The city ID is also generational/player-local, so destruction/capture of the old city
cannot silently retarget the task to a newer city reusing the raw slot. The newly
confirmed F316 is specifically the liberation-mode payload collision, not a generic
city-ID problem.

CONTINUATION FROM C030-WIP11
----------------------------
Queue 019 remains OPEN. Continue with F317 UNUSED through:
  * finish DISBANDCITY semantics and extended-game/session/disconnect launch/reply
    contracts;
  * OnAltExecute / relaunch and final OnFocus matrix;
  * explicit 43/43 member-definition coverage;
  * final six-family SAS-vs-Base semantic-delta reconciliation;
  * final K-Mod/Base/Civ4CE history saturation and duplicate scan.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP11


======================================================================
C030-WIP12 -- QUEUE 019 STALE LIBERATION-RECIPIENT REBIND ROOT
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F317 are CONFIRMED in queue 019.
F318 / provisional KI#641 is UNUSED.

F317 / KI#640 -- LIBERATION POPUPS CAN GIVE A CITY TO A DIFFERENT CIVILIZATION THAN THE BUTTON NAMES
----------------------------------------------------------------------------------------------------
ROOT
~~~~
Liberation UI text is built from a concrete recipient at popup-construction time,
but the reply does not bind that recipient. Instead it sends TASK_LIBERATE with only
the city ID and a conquest/non-conquest boolean. CvCity::liberate then recomputes the
best liberation player from CURRENT game state.

Thus the city identity can remain perfectly stable while the semantic target named
by the button changes underneath an unanswered modal popup.

MANIFESTATION A -- FREE COLONY / LIBERATE CITY
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
launchFreeColonyPopup iterates the active player's cities and computes:

    PlayerTypes eLibPlayer = pLoopCity->getLiberationPlayer();

It uses that exact player to build text such as "Liberate <city> to <civilization>",
but the button ID stores only:

    -pLoopCity->getID()

No recipient identity is preserved.

On reply, BUTTONPOPUP_FREE_COLONY sends:

    sendDoTask(-clickedButton, TASK_LIBERATE,
            0, -1, false, false, false, false);

CvCity::doTask turns this into:

    liberate(false);

and CvCity::liberate starts by recomputing:

    PlayerTypes ePlayer = getLiberationPlayer(false);

The civilization named by the button is therefore not part of the synchronized
request at all.

SUPPORTED SIMULTANEOUS PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
getLiberationPlayer(false) depends on live state: only alive/city-trade-eligible
players are considered; current capitals and capital distance matter; culture,
plot-team culture and current team/vassal relationships affect the score.

A particularly clean supported transition is elimination of the displayed recipient:

1. Human P owns city C, which has multiple eligible former owners/culture candidates.
2. P opens the Free Colony popup. At construction, X is alive and is the best
   liberation player, so the popup displays "Liberate C to X".
3. P leaves the modal unanswered during a simultaneous multiplayer turn.
4. Remote human Q captures/destroys X's last city. X is eliminated while P's local
   popup remains visible.
5. C itself is unchanged and still belongs to P. Another eligible former owner Y
   remains alive.
6. A fresh getLiberationPlayer(false) for C now cannot select X because
   PlayerIter<CIV_ALIVE> excludes it; suppose Y is now the best remaining candidate.
7. P clicks the still-visible "Liberate C to X" button.
8. The reply sends only C's city ID + false. CvCity::liberate(false) recomputes Y and
   transfers C to Y.

Receiver legality/state freshness therefore does not fix the problem. It is exactly
what causes the semantic retarget: the receiver intentionally uses the fresh best
recipient, but the human was asked to approve a different named recipient.

If no current liberation player remains, the stale click can instead become a no-op.
The wrong-recipient Y case is the stronger consequence.

MANIFESTATION B -- CAPTURED-CITY RETURN/LIBERATE BUTTON
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
AdvCiv's BUTTONPOPUP_RAZECITY construction stores the conquest liberation player in
info.data2 and labels the Return/Liberate button for that player. But its reply sends
TASK_LIBERATE rather than an explicit target, and CvCity::liberate(true) recomputes
the current conquest liberation player.

Therefore even after F316's PLAYER_0 boolean collision is repaired, a delayed
captured-city popup can still say "Return to X" and transfer to current Y if the
liberation ranking/eligibility changes before reply.

F316 and F317 are distinct:

* F316 is deterministic PlayerTypes-as-bool corruption for target PLAYER_0. It can
  misroute immediately with no game-state change.
* F317 is missing RECIPIENT identity across popup lifetime. It affects any player ID
  when the valid/best liberation target changes while the modal remains open.

ANCESTRY
~~~~~~~~
The Free Colony manifestation is inherited from stock BtS/Civ4CE. BtS builds the
button from getLiberationPlayer(false), stores only negative city ID, and replies
with TASK_LIBERATE,0. The Unofficial Patch/K-Mod adds a war check at construction but
retains the same target-less reply. Base AdvCiv and current SAS inherit it.

K-Mod's captured-city popup used TASK_GIFT with the explicit target player ID, so it
did not have this exact recompute-on-reply behavior there. AdvCiv practical commit
1719 switched that branch to TASK_LIBERATE in order to honor conquest-specific
liberation semantics, thereby extending the general target-rebinding issue to the
captured-city Return button as well.

ONFOCUS / REFRESH CHECK
~~~~~~~~~~~~~~~~~~~~~~~~
There is no refresh that repairs this before click:

* BUTTONPOPUP_FREE_COLONY has no dedicated OnFocus revalidation branch.
* RAZECITY OnFocus checks only that the city still exists and looks at it; it does
  not verify that the stored/named liberation player is still the current
  getLiberationPlayer(true).
* launchRazeCityPopup itself treats info.data2 as the named immediate liberation
  target; its separate eFutureLiberationPlayer calculation is only explanatory text
  about what would happen if the player KEEPS the city and liberates later.

CONSEQUENCE
~~~~~~~~~~~
A human can explicitly click a button naming civilization X and have the city ceded
to civilization Y. This is a direct wrong-recipient gameplay action, not merely stale
text or a harmless failed command.

FIX DIRECTION
~~~~~~~~~~~~~
Bind the recipient shown by the popup and verify that exact recipient at reply.
Possible designs:

1. Store a stable PlayerTypes recipient alongside each liberation choice and send an
   explicit liberation/gift request for that player after current legality checks.
2. If the design intentionally wants `CvCity::liberate` to choose the current best
   recipient, then on reply compare current getLiberationPlayer(bConquest) against
   the launch-time named recipient. If they differ, cancel/rebuild the popup and ask
   the human again rather than silently redirecting.
3. RAZECITY already has its launch-time target in info.data2, so after fixing F316 it
   can directly compare current getLiberationPlayer(true) to that stored target.
4. FREE_COLONY needs to preserve the per-button target somehow; negative city ID alone
   is insufficient. A popup-side mapping or explicit message payload can carry both
   city and recipient.

Do not regard generational city IDs as sufficient: they solve stale city-object
identity, not stale recipient identity.

NUMBERING
~~~~~~~~~
F317 is CONFIRMED and consumes KI#640.
F318 / provisional KI#641 is now the next unused root slot.

CONTINUATION FROM C030-WIP12
----------------------------
Queue 019 remains OPEN. Continue with F318 UNUSED through:
  * finish DISBANDCITY semantics and extended-game/session/disconnect launch/reply
    contracts;
  * OnAltExecute / relaunch and the remaining OnFocus cases;
  * explicit 43/43 member-definition coverage matrix;
  * final six-family SAS-vs-Base semantic-delta reconciliation;
  * targeted history reconciliation for popup bugfix commits and final duplicate scan.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP12


======================================================================
C030-WIP13 -- QUEUE 019 FORMER-VOTER STALE DEFY ROOT + RESEARCH NOTE
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp remains OPEN.
Files complete remain 29 / 169.
F307-F318 are CONFIRMED in queue 019.
F319 / provisional KI#642 is UNUSED.

F318 / KI#641 -- FORMER VOTING MEMBER CAN USE A STALE DEFY BALLOT TO VETO A RESOLUTION
--------------------------------------------------------------------------------------
ROOT
~~~~
BUTTONPOPUP_DIPLOVOTE is constructed only for players who are voting members at the
moment CvGame::addVoteTriggered creates the ballot. For an ordinary non-team
resolution, launchDiploVotePopup offers the Defy Resolution button whenever:

    GET_PLAYER(getActivePlayer()).canDefyResolution(eVoteSource,
            pVoteTriggered->kVoteOption)

returns true.

The popup can remain unanswered across a simultaneous multiplayer turn, but neither
the popup reply nor CvGame::castVote requires the voter still to be a CURRENT voting
member when the delayed choice arrives:

    case BUTTONPOPUP_DIPLOVOTE:
        sendDiploVote(info.getData1(), clickedVote);

    CvNetDiploVote::Execute()
    {
        GC.getGame().castVote(m_ePlayer, m_iVoteId, m_eChoice);
    }

    CvGame::castVote(...)
    {
        ...
        setPlayerVote(eVoter, iVote, ePlayerVote);
    }

There is no `isVotingMember` rejection in that path.

Ordinary vote WEIGHT mostly self-corrects because countVote asks getVotes from
current state. The dangerous exception is `PLAYER_VOTE_NEVER` (Defy Resolution).
When the resolution would otherwise pass, doVoteResults scans ALL major players,
not merely current voting members:

    if (getPlayerVote(itPlayer->getID(), voteID) == PLAYER_VOTE_NEVER &&
        itPlayer->canDefyResolution(eVoteSource, subdata))
    {
        bPassed = false;
        itPlayer->setDefiedResolution(eVoteSource, subdata);
    }

So a stale former-member Defy can still veto the entire resolution whenever
`canDefyResolution` remains true after membership was lost.

CURRENT-SAS XML / RULE PROOF
~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This is reachable with shipped Apostolic Palace data, not merely a hypothetical
VoteInfo configuration.

* DIPLOVOTE_POPE is religion-based.
* `CvPlayer::isVotingMember(eVoteSource)` is exactly
  `getVotes(NO_VOTE,eVoteSource) > 0`.
* For a religious vote source, `getVotes(NO_VOTE,...)` is the player's current
  population in cities containing the vote-source religion. Thus an alive player
  immediately ceases to be an AP voting member after losing the final owned city
  containing the AP religion.
* Current CIV4VoteInfo.xml includes `VOTE_FORCE_NO_TRADE` for DIPLOVOTE_POPE with
  iMinVoters=3.
* `CvPlayer::canDefyResolution` handles Force No Trade as an unconditional
  `return true` after only the general Secretary-General / vassal exclusions. It
  does NOT require current `isVotingMember` or `isFullMember` there.

Therefore there is a shipped AP resolution whose stale Defy remains result-time
legal according to the existing canDefyResolution predicate even after the player
has left the voting body.

SUPPORTED SIMULTANEOUS PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A clean sequence uses at least four AP voting civilizations initially so that the
resolution can remain valid after one voter leaves:

1. Human P owns at least one city with the AP religion and is a current AP voting
   member. A non-team AP `VOTE_FORCE_NO_TRADE` ballot is created for P, and the popup
   includes Defy Resolution.
2. P leaves the modal unanswered.
3. CvGame::doTurn creates/processes diplomatic-vote state BEFORE simultaneous
   players are activated for the new game turn. Remote clients therefore act after
   the already-created ballot exists.
4. During that simultaneous turn, remote player Q captures P's final city containing
   the AP religion. P remains alive through other cities, but P's AP religion
   population becomes 0 and `P.isVotingMember(DIPLOVOTE_POPE)` becomes false.
5. At least three other AP voters remain, so the vote source still satisfies the
   shipped iMinVoters=3 rule and the Force No Trade resolution can remain otherwise
   valid.
6. P's already-rendered BUTTONPOPUP_DIPLOVOTE is not cancelled or rebuilt on loss of
   voting membership. Its OnFocus case checks only GAMESTATE_OVER.
7. P clicks the still-visible Defy Resolution button.
8. CvNetDiploVote -> castVote stores PLAYER_VOTE_NEVER without checking current
   membership.
9. At doVoteResults, P is correctly ignored for ordinary voting weight and for the
   normal result text because P is no longer a voting member. But the Defy scan does
   not have that membership guard. `canDefyResolution` still returns true for Force
   No Trade, so P sets `bPassed=false` and vetoes the resolution.

The built-in missing-vote/grace handling is not needed to invent timing here; the
normal ballot lives until results processing, and simultaneous remote actions can
change the membership state during that interval. If P submits the stale Defy before
results are processed, it is retained in the VoteTriggeredData/player vote state.

CONSEQUENCE
~~~~~~~~~~~
A civilization with zero current AP voting membership can veto an Apostolic Palace
resolution. Its numerical votes are zero, yet its stale Defy choice has stronger
than numerical effect: it unconditionally turns an otherwise-passing resolution
into failure.

`setDefiedResolution` also marks the former voter non-loyal and applies defiance
anger to any qualifying current cities. In the clean producer above P has no current
AP-religion city, so the main demonstrated consequence is the unauthorized veto;
the membership/loyalty side effect is secondary.

The results display then hides P from the listed vote breakdown because that display
DOES check current `isVotingMember`, making the veto particularly opaque: the
resolution can fail due to a former member whose Defy is not shown among current
voters.

ANCESTRY
~~~~~~~~
The underlying delayed-voter contract is inherited from stock BtS/Civ4CE:

* stock launch code offers Defy based on launch-time canDefyResolution;
* stock castVote stores the delayed choice without rechecking voting membership;
* stock doVoteResults scans every player with PLAYER_VOTE_NEVER and immediately
  vetoes, with no current membership check at all.

K-Mod retains that architecture.

AdvCiv improves the result-time Defy loop by re-running `canDefyResolution` (comment:
"May e.g. have become a vassal"), which prevents some stale Defy states. However,
`canDefyResolution` is not itself a membership predicate and intentionally contains
branches such as Force No Trade that remain true for a former voter. Base AdvCiv and
current SAS therefore inherit a partial stale-state repair that does not cover loss
of voting membership.

DISTINCTNESS
~~~~~~~~~~~~
F318 is separate from the other queue-019 diplomatic-vote roots:

* F309 concerns selection/proposal ownership/visibility state.
* F310 concerns a TEAM-VOTE CANDIDATE becoming ineligible while the ballot is live.
* F314 concerns a Secretary-General RESOLUTION OPTION becoming invalid before the
  human chooses it (notably iMinVoters).
* F318 concerns the VOTER who already received a live ballot ceasing to be a voting
  member, while a stale PLAYER_VOTE_NEVER retains veto power.

The same broad delayed-popup theme is present, but the violated identity/eligibility
invariant and receiver path are different.

FIX DIRECTION
~~~~~~~~~~~~~
The synchronized vote receiver/result processing should enforce current voter
eligibility, rather than relying only on launch-time popup eligibility.

A narrow robust direction is:

1. In `CvGame::castVote`, after resolving VoteTriggeredData, reject a voter who is no
   longer `isVotingMember(pTriggeredData->eVoteSource)`. This prevents stale former-
   member choices from entering vote state at all.
2. Defense-in-depth in doVoteResults: require current `isVotingMember(eVoteSource)`
   before honoring PLAYER_VOTE_NEVER, in addition to current canDefyResolution.
3. OnFocus for BUTTONPOPUP_DIPLOVOTE can cancel a ballot when the active player is no
   longer a voting member, improving UI clarity; this is not sufficient by itself
   because network state can change between focus/click and synchronized execution.
4. Preserve the existing `canDefyResolution` recheck as well, because membership can
   remain valid while other Defy-specific legality changes (e.g. vassalization).

Do not "fix" this by changing countVote alone. Defy is processed outside the numeric
vote-count path.

NUMBERING
~~~~~~~~~
F318 is CONFIRMED and consumes KI#641.
F319 / provisional KI#642 is now the next unused root slot.

F313 EXTENSION -- ORDINARY CHOOSE-RESEARCH POPUP SHARES THE STALE TECH-LIST GAP
-------------------------------------------------------------------------------
The stale technology-list architecture documented by F313 is not limited to free
technology awards. For the ordinary choose-research popup (`iDiscover == 0`), a
teammate can likewise acquire displayed technology T after the popup is rendered.
The ordinary OnFocus path does not rebuild the list merely because T became known;
it cancels when the active player has a current research target or the game ends.

If P still has NO_TECH selected and clicks stale T after teammate Q acquired it,
CvNetResearch::Execute sees T already team-known. With Shift not held it clears the
research queue and calls `pushResearch(T, true)`. pushResearch immediately returns
because the team already owns T, so T is not selected.

This is a LOWER-SEVERITY manifestation than F313's free-tech loss:

* the ordinary branch does not consume a one-shot free-tech entitlement;
* if the player reaches research processing without a valid target, current SAS
  stores the turn's research as overflow rather than dropping the beakers;
* the player can be prompted/choose again.

So do NOT allocate another root number from this ordinary-research case. It supports
F313's broader diagnosis that CHOOSETECH is a delayed snapshot that can become stale,
while the confirmed high-severity F313 consequence remains the free-tech entitlement
being consumed before the stale target's no-op is discovered.

CONTINUATION FROM C030-WIP13
----------------------------
Queue 019 remains OPEN. Continue with F319 UNUSED through:
  * finish the explicit 43/43 CvDLLButtonPopup member-definition matrix, including
    getInstance + destructor so the earlier 42-line grep omission is not repeated;
  * finish DISBANDCITY and extended-game/session/disconnect controls;
  * final OnAltExecute / OnFocus / relaunch bookkeeping saturation;
  * final six-family SAS-vs-Base semantic-delta reconciliation;
  * targeted K-Mod/Base/Civ4CE popup-history reconciliation and duplicate scan.

Once queue 019 genuinely saturates, close it durably and jump to queue 001 for the
planned deep-method re-audit of queues 001-009. Do NOT resume queue 020 first.

END C030-WIP13


C030-WIP14 APPEND -- QUEUE 019 CHOOSEELECTION SENTINEL COLLISION / F309 BROADENING
==================================================================================
Physical continuation from C030-WIP13. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307-F318 remain confirmed.
F319 / provisional KI#642 remains UNUSED.

F309 BROADENING -- "NONE / EARLY ELECTION" PSEUDO-SENTINEL CAN COLLIDE WITH A REAL RESOLUTION ORDINAL
------------------------------------------------------------------------------------------------------
The final 43-function reconciliation challenged a premise in the original F309 write-up.
F309 correctly identified that BUTTONPOPUP_CHOOSEELECTION does not have an explicit
synchronized branch for the final "None" / "Early Election" button.  However, the
original note described its button ID `GC.getNumVoteInfos()` as deliberately outside
`0..aVoteOptions.size()-1`.  That is NOT a valid invariant.

CURRENT CODE CONTRACT
~~~~~~~~~~~~~~~~~~~~~
launchChooseElectionPopup assigns every real proposal its VECTOR ORDINAL:

    for (int iI = 0; iI < (int)pVoteSelectionData->aVoteOptions.size(); iI++)
        popupAddGenericButton(..., iI, WIDGET_GENERAL);

and assigns the special no-proposal button:

    popupAddGenericButton(...,
        bEarlyElection ? TXT_KEY_EARLY_ELECTION : TXT_KEY_NONE,
        ..., GC.getNumVoteInfos(), WIDGET_GENERAL);

OnOk does not identify that special button semantically.  It merely tests:

    if (pData != NULL &&
        clicked < (int)pData->aVoteOptions.size())
    {
        sendChooseElection(clicked, selectionID);
    }

The synchronized receiver also treats `clicked` purely as an OPTION ORDINAL:

    CvNetChooseElection::Execute -> CvGame::setVoteChosen(iSelection, iVoteId)
    -> addVoteTriggered(kSelection, iSelection)
    -> kSelection.aVoteOptions[iSelection]

There is no downstream recognition of `GC.getNumVoteInfos()` as a special sentinel.

WHY THE VECTOR IS NOT BOUNDED BY getNumVoteInfos()
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Current SAS has 22 `<VoteInfo>` definitions, so the special button ID is 22.
But `CvGame::addVoteSelection` can append MORE THAN ONE `VoteSelectionSubData` for a
single VoteInfo:

* Force Peace appends one option per valid target major team.
* Force No Trade appends one option per valid target major team.
* Force War appends one option per valid target major team.
* Assign City can append one option per valid city/recipient combination.

The vector therefore counts concrete proposal INSTANCES, not VoteInfo definitions.
`GC.getNumVoteInfos()` cannot be a safe upper-bound sentinel for it.

CURRENT-SAS REACHABILITY -- ONE SHIPPED RESOLUTION IS ENOUGH
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This is especially easy to reach in current AdvCiv-SAS because CvEnums.h compiles:

    MAX_CIV_PLAYERS = 48
    MAX_CIV_TEAMS   = 48

Current shipped CIV4VoteInfo.xml has 22 VoteInfo blocks and includes Apostolic Palace
`VOTE_FORCE_NO_TRADE` (`bForceNoTrade=1`, DIPLOVOTE_POPE, iMinVoters=3).

For that one VoteInfo, addVoteSelection loops every alive major team.  A target is
valid when it is not a full AP member and at least one other AP full-member player can
stop trading with that target.  A perfectly supported large SAS game can therefore
have, for example:

* at least 3 full Apostolic Palace member teams, satisfying iMinVoters=3; and
* at least 23 other alive major teams that are not full members and have a qualifying
  trade relation with one of those full members.

Those 23 targets alone can append at least 23 Force No Trade proposal instances.
Thus `aVoteOptions.size() > 22` is reachable without relying on Assign City or on a
sum of several resolution families.

CONSEQUENCE
~~~~~~~~~~~
When the vector has more than 22 entries, the final visible "None" / "Early Election"
button has ID 22, and 22 now satisfies:

    22 < aVoteOptions.size()

Clicking the button therefore sends `CvNetChooseElection(selection=22, voteId)`.
`setVoteChosen` creates a live ballot from `aVoteOptions[22]` and deletes the selection.
The human explicitly chose NO proposal (or Early Election), yet an unrelated real
resolution at vector ordinal 22 is proposed instead.

This is stronger than the original F309 leak consequence and is deterministic once
such an option vector exists; no delayed-state race is required.

RELATION TO THE ORIGINAL F309 CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Do NOT allocate F319.  Both behaviors come from the SAME line-level encoding defect:
the special no-proposal action is represented by a pseudo-sentinel derived from the
number of VoteInfo definitions, while OnOk has no explicit semantic branch for it.

The two size regimes merely expose different consequences:

* `aVoteOptions.size() <= GC.getNumVoteInfos()`:
  the special ID fails the `< size` test, no synchronized message is sent, and the
  VoteSelectionData leaks indefinitely (original F309 consequence).
* `aVoteOptions.size() > GC.getNumVoteInfos()`:
  the special ID PASSES the `< size` test and aliases a real option ordinal, so the
  wrong resolution is proposed (this WIP14 consequence).

At exactly size == 22 the button is still out of range (`22 < 22` is false), so the
original leak applies.  The wrong-resolution branch starts at size 23.

ANCESTRY
~~~~~~~~
The same pseudo-sentinel plus range-only OnOk structure is present in supplied
Civ4CE/BtS and K-Mod and survives Base AdvCiv/current SAS. AdvCiv advc.178 only changes
the special button's label to "Early Election" in the relevant state; it retains the
same ID and dispatch contract.

Current SAS's 48-civ capacity makes a collision particularly easy to prove with one
shipped target-expanded resolution, but the architectural mistake is inherited.

REPAIR BOUNDARY -- SAME REPAIR AS F309
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The original F309 repair direction remains correct and now fixes BOTH consequences:

1. Give the special UI action an explicit semantic branch; do not infer it from a
   numeric value presumed to lie beyond the dynamic vector.
2. When the human clicks None/Early Election, send the synchronized choose-election
   message with the receiver-supported no-choice value `-1`.
3. `CvGame::setVoteChosen` then calls `addVoteTriggered(...,-1)` (which returns NULL)
   and still deletes the VoteSelectionData on every peer.
4. Validate ordinary proposal indices explicitly as `0 <= clicked < size`.

Using `GC.getNumVoteInfos()` as a UI button ID can be retained only if OnOk tests that
ID BEFORE interpreting ordinary ordinals, but a dedicated negative/special ID would
make the contract clearer and avoid future collisions entirely.

BOOKKEEPING CORRECTION
~~~~~~~~~~~~~~~~~~~~~~
The original F309 text saying the special ID is "deliberately outside the
0..aVoteOptions.size()-1 choice range" should be corrected to say that the CODE
INTENDS it as an out-of-band pseudo-sentinel, but the intended invariant is false
because aVoteOptions is a target-expanded vector.

F309 / KI#632 remains one confirmed root, now with TWO demonstrated consequences:
selection-state leak and wrong-resolution aliasing.
F319 / provisional KI#642 remains UNUSED.

43/43 MATRIX PROGRESS
---------------------
The explicit member-definition pass is still in progress.  This WIP freezes the
sentinel-collision correction before continuing so that the new proof is not left
only in chat state.

Already reconciled/closed in this final pass include singleton lifetime helpers,
production, civic/religion, disband-city identity, diplomacy/contact, spaceship
launch, found-religion, event, free-colony/liberation, vassal tribute, LOADUNIT,
LEADUNIT, espionage, confirm-command, declaration-war-move, details/admin/Python,
Rise & Fall, and most main-menu/session branches, subject to their already-numbered
roots where applicable.

Continue with F319 UNUSED through the remaining OnAltExecute/OnFocus/relaunch/session
bookkeeping, final six-family SAS semantic-delta reconciliation, and final
Base/K-Mod/Civ4CE history + duplicate saturation before closing queue 019.

END C030-WIP14


C030-WIP15 APPEND -- QUEUE 019 EXTENDED-GAME DEAD-RECIPIENT UI CONTRACT
========================================================================
Physical continuation from C030-WIP14. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307-F319 are now confirmed.
F320 / provisional KI#643 is UNUSED.

F319 / KI#642 -- DEAD MULTIPLAYER HUMAN CAN RECEIVE AN INERT "ONE MORE TURN" BUTTON
-----------------------------------------------------------------------------------
ROOT
~~~~
When the game enters GAMESTATE_OVER, CvGame::setGameState queues
BUTTONPOPUP_EXTENDED_GAME to EVERY ever-alive human, including eliminated humans.
Current AdvCiv/SAS even documents the intended special case immediately above that
loop:

    // advc (note): For dead humans, the popup will only lead to the opening menu.

CvPlayer::addPopup does not suppress the popup for dead humans; it rejects only
non-human recipients. Thus an eliminated human really can retain/receive this popup.

However, launchExtendedGamePopup decides whether to add the affirmative button from
GLOBAL state:

    if (GC.getGame().countHumanPlayersAlive() > 0)
        popupAddGenericButton(... TXT_KEY_EXTENDED_GAME_YES ..., 0, ...);

This asks only whether ANY human civilization remains alive, not whether the ACTIVE
RECIPIENT of this popup is alive.

SUPPORTED CURRENT PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~
A normal multiplayer game is sufficient:

1. Human D has been eliminated but remains a human/ever-alive player/client.
2. At least one other human A remains alive when a victory ends the game.
3. CvGame::setGameState(GAMESTATE_OVER) queues BUTTONPOPUP_EXTENDED_GAME for D as
   well as A. The source explicitly anticipates dead-human recipients.
4. `countHumanPlayersAlive() > 0` is true because A is alive, so D's popup contains
   the same "One More Turn" / Extended Game Yes button as a living player.
5. D clicks Yes.

RECEIVER / CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~
In network multiplayer, OnOk sends CvNetExtendedGame for the active player. Its
receiver simply calls:

    GET_PLAYER(m_ePlayer).makeExtendedGame();

which sets D's `m_bExtendedGame = true` even though D is dead.

But CvGame::testExtendedGame intentionally counts only LIVING human players. In
stock/K-Mod this is spelled out as `isAlive() && isHuman()`; current AdvCiv/SAS uses
`PlayerIter<HUMAN>`, whose HUMAN status predicate explicitly requires both
`kAgent.isHuman()` and `isAlive(kAgent)`.

Therefore D's affirmative choice cannot cause GAMESTATE_OVER -> GAMESTATE_EXTENDED.
If no living human chooses Yes, D clicks a button labelled as continuing the game,
the popup closes, and the game remains over. D's choice is semantically inert.

This directly contradicts the current AdvCiv comment that a dead human's popup
"will only lead to the opening menu": the launcher's global test can expose a second,
nonfunctional path whenever another human survives.

SEVERITY
~~~~~~~~
LOW. This does not corrupt synchronized gameplay state and does not let an eliminated
player force continuation; `testExtendedGame` correctly ignores dead players. The
bug is a concrete multiplayer UI/action-contract mismatch: a supported button is
shown to a recipient for whom its action cannot have the promised effect.

ANCESTRY
~~~~~~~~
The mismatch is inherited from Firaxis BtS/Civ4CE:

* stock setGameState queues the Extended Game popup to every human player;
* stock launchExtendedGamePopup uses global `countHumanPlayersAlive() > 0`;
* stock testExtendedGame accepts the flag only from `isAlive() && isHuman()` players.

K-Mod retains the same three-part contract. AdvCiv refactors the loops to AgentIter
and adds the explicit dead-human/opening-menu comment, but keeps the launcher's global
alive-human gate unchanged. Base AdvCiv and current SAS therefore retain the defect.

FIX DIRECTION
~~~~~~~~~~~~~
At launch time, the affirmative button should be based on the recipient:

    if (getActivePlayer() != NO_PLAYER &&
        GET_PLAYER(getActivePlayer()).isAlive())
    {
        // add One More Turn / Yes
    }

The global count is unnecessary for a displayed active-player popup once recipient
liveness is known.

Defense-in-depth in CvNetExtendedGame::Execute (or CvPlayer::makeExtendedGame) could
also reject a dead player, matching the invariant already enforced by
testExtendedGame. The important user-facing repair is to avoid advertising the
inapplicable action in the first place.

NUMBERING
~~~~~~~~~
F319 is CONFIRMED and consumes KI#642.
F320 / provisional KI#643 becomes the next unused slot.

REMAINING QUEUE-019 SATURATION
------------------------------
The final member/function reconciliation continues. The remaining focus is:

* OnAltExecute relaunch semantics and whether it creates any independent ownership
  defect beyond already-numbered delayed-popup roots;
* OnFocus coverage/cancellation and no-active-player boundaries;
* launchButtonPopup/relaunch-sound bookkeeping, including the SAS AI-Auto-Play
  informational-popup early return already held below the root bar;
* final six executable SAS-vs-Base delta families;
* final main-menu/admin/disconnect/session cleanup checks;
* targeted stock/K-Mod/AdvCiv history and duplicate sweep;
* final explicit 43/43 matrix and durable queue-019 closure if saturated.

Do not move to queue 020 after closure. The protected route is queue 019 -> queue 001
and then the deep-method 001-009 re-audit.

END C030-WIP15


C030-WIP16 APPEND -- QUEUE 019 PICK-RELIGION RESERVED-SLOT LOSS ON FOUNDER ELIMINATION
=====================================================================================
Physical continuation from C030-WIP15. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307-F320 are now confirmed.
F321 / provisional KI#644 is UNUSED.

F320 / KI#643 -- PICK-RELIGION SLOT CAN BE PERMANENTLY RESERVED IF THE HUMAN FOUNDER DIES BEFORE CHOOSING
----------------------------------------------------------------------------------------------------------
ROOT
~~~~
The first-discovery religion path reserves the RELIGION SLOT before a human has
actually selected/founded a religion.

In CvTeam::setHasTech, after choosing eBestPlayer for the first-to-discover reward:

    kGame.setReligionSlotTaken(eLoopReligion, true);
    if (kGame.isOption(GAMEOPTION_PICK_RELIGION))
    {
        if (GET_PLAYER(eBestPlayer).isHuman())
        {
            CvPopupInfo* pInfo =
                new CvPopupInfo(BUTTONPOPUP_FOUND_RELIGION, eLoopReligion);
            ... addPopup(pInfo, eBestPlayer);
        }
        ...
    }

Thus the synchronized global slot reservation exists while the actual entitlement is
represented only by a human popup.

NORMAL SUCCESS PATH
~~~~~~~~~~~~~~~~~~~
BUTTONPOPUP_FOUND_RELIGION sends the chosen religion and reserved slot through
CvNetFoundReligion. CvPlayer::foundReligion then creates the Holy City (or reprompts
if the selected religion was taken first). The slot remains taken, now correctly
representing a consumed founding opportunity.

ELIMINATION PATH LOSES THE ONLY OWNER OF THE PENDING CHOICE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Current player elimination removes the popup but does not release/reassign the slot:

* CvPlayer::setAlive(false) calls `clearPopups()`, deleting queued CvPopupInfo objects.
* It then calls `setTurnActive(false)`; for the local active player that calls the UI's
  `clearQueuedPopups()`, covering the displayed/EXE-owned popup side as well.
* No religion-slot cleanup accompanies either operation.

Repository-wide current-source search finds no ordinary
`setReligionSlotTaken(eReligion, false)` call at all. The only other state transition
is reset/deserialize. Once a slot is true during a running game, it is never released.

CvGame::doHolyCity cannot recover the reward either:

    if (isReligionSlotTaken(eReligion))
        continue;

so the global fallback deliberately skips the orphaned reservation forever.

SUPPORTED SIMULTANEOUS PRODUCER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
No malformed popup or debug action is required:

1. GAMEOPTION_PICK_RELIGION is enabled.
2. Human P is selected as the best player when P's team is first to discover a
   religion-founding technology.
3. CvTeam::setHasTech immediately reserves that XML religion slot and queues
   BUTTONPOPUP_FOUND_RELIGION for P.
4. P leaves the modal unanswered.
5. In simultaneous multiplayer, remote player Q can continue acting while P's modal
   is open and eliminates P before P chooses a religion.
6. P's synchronized death clears P's queued/displayed popup state.
7. The reserved slot remains globally `true`; no CvNetFoundReligion response will
   ever arrive for it.
8. doHolyCity and later founding checks skip that slot permanently.

The producer is especially natural in simultaneous turns because technology can be
acquired while multiple players are active (trade/other synchronized acquisition),
and ordinary turn-start team research processing has completed before players are
left free to act. The modal blocks P's local decisions, not Q's remote military
execution.

CONSEQUENCE
~~~~~~~~~~~
One religion-founding opportunity is permanently lost from the running game. The
reserved slot remains serialized and blocks the fallback founder indefinitely even
though no religion was actually founded from that entitlement.

With Pick Religion, slot identity and actual founded-religion identity are decoupled:
a Buddhism XML slot can be used to choose/found another unfounded religion. Therefore
recovery cannot safely infer an orphan merely from
`slotTaken(slot) && !isReligionFounded(slot)`. That condition can be legitimate after
a successfully resolved Pick Religion choice.

A later founding slot may choose the religion that would otherwise have been chosen,
but the TOTAL number of founding opportunities has still dropped by one. Eventually
one religion can remain unfounded with all available slots already consumed/reserved.

ANCESTRY
~~~~~~~~
The core reservation-before-human-choice architecture is inherited unchanged from
Firaxis BtS/Civ4CE through K-Mod and Base AdvCiv:

* setReligionSlotTaken(slot,true) is performed before queuing FOUND_RELIGION;
* the human popup is the deferred owner of the choice;
* no normal false/reset path exists for an abandoned pending entitlement.

AdvCiv's popup/lifecycle refactors do not add synchronized pending-religion ownership,
so current SAS retains the architecture.

DISTINCTNESS
~~~~~~~~~~~~
F320 is not F313 despite the shared one-shot-choice theme.

* F313: a stale CHOOSETECH target is clicked and the free-tech counter is consumed
  before the already-known target no-ops.
* F320: no click occurs at all. The entitlement is reserved globally BEFORE popup
  response, then its only pending human owner disappears during elimination cleanup.

It is also separate from F307 event-popup construction and from the vote-selection
ownership roots.

FIX BOUNDARY
~~~~~~~~~~~~
A robust repair should not derive synchronized religion-slot state from local popup
inventory; network clients do not have identical displayed-popup ownership.

The architectural fix is to represent a pending Pick Religion entitlement in
synchronized game/player state, including at least:

* reserved slot;
* pending owning player/team (or another deterministic claimant);
* resolved/unresolved state.

Then player elimination can deterministically transfer the pending choice to another
eligible living team member, resolve it automatically, or release the reservation
according to an explicit policy.

A smaller but still synchronized design could reserve the slot with a tracked owner
field and, during `setAlive(false)`, resolve/reassign every pending slot owned by that
player BEFORE popup cleanup. Do not inspect only local displayed popups to decide
whether to mutate `m_abReligionSlotTaken`; that would risk OOS.

Simply rejecting CvNetFoundReligion for dead players is not a fix: the important
failure occurs because no response arrives after the popup is destroyed.

NUMBERING
~~~~~~~~~
F320 is CONFIRMED and consumes KI#643.
F321 / provisional KI#644 becomes the next unused slot.

43/43 MATRIX NOTE
-----------------
The initial alternative suspicion -- clicking FOUND_RELIGION after the player has no
city could mark the slot and then return with no Holy City -- is NOT the primary root:
normal first-discovery code has already marked the slot before the popup. Moreover,
ordinary elimination clears the popup. The durable defect is the orphaned pre-reserved
slot itself, documented above.

Continue final queue-019 saturation with F321 UNUSED.

END C030-WIP16


C030-WIP17 APPEND -- QUEUE 019 EXPLICIT 43/43 SATURATION MATRIX AND LATE CONTROL CLOSURES
========================================================================================
Physical continuation from C030-WIP16. No repository source file was modified.
Queue 019 remains OPEN; files complete remain 29 / 169.

STATUS
------
F307-F320 remain confirmed.
F321 / provisional KI#644 remains UNUSED.

LATE CONTROL: EVENT TRIGGER RECORDS SURVIVING PLAYER DEATH ARE NOT A CURRENT GAMEPLAY ROOT
-------------------------------------------------------------------------------------------
Player elimination clears queued/displayed popups but does not explicitly call
`m_eventsTriggered.removeAll()`. A pending BUTTONPOPUP_EVENT can therefore leave its
EventTriggeredData object in the eliminated player's private free-list.

That shape is not sufficient for a numbered finding:

* the destroyed popup no longer owns an action capable of applying the pending event;
* `CvPlayer::doEvents` creates fresh trigger candidates and does not redisplay arbitrary
  old pending EventTriggeredData objects;
* the synchronized `applyEvent` path re-resolves the trigger id and re-runs
  `canDoEvent` before mutating state;
* revival by itself does not requeue the abandoned pending popup.

No gameplay consumer or state mutation has been established. At most this is stale
private/serialized bookkeeping for a dead player. Under the album standard it remains
cleanup debt, not F321.

LATE CONTROL / F311 EXTENSION: STALE TRIBUTE RESOURCE AVAILABILITY IS RECEIVER-VALIDATED
-----------------------------------------------------------------------------------------
The human vassal's Grant reply constructs a TRADE_RESOURCES bundle without rechecking
that the demanded resource is still tradeable. This is another delayed-state weakness,
but the actual deal implementation is safer than the popup code suggests:

* `CvGame::implementDeal(..., bForce=false)` calls `implementAndReturnDeal`;
* this passes `bCheckAllowed=true` to `CvDeal::addTradeItems`;
* `addTradeItems` re-runs `CvPlayer::canTradeItem` for every item before calling
  `startTrade`.

Therefore a resource that was lost/pillaged/traded away while the popup remained open
cannot create an impossible export through this path. The popup handler can still emit
its immediate `TXT_KEY_VASSAL_GRANT_TRIBUTE_ACCEPTED` message after sending a request
that the synchronized deal layer rejects, so stale availability can produce misleading
feedback. This is a lower-severity manifestation of the same delayed tribute contract
already represented by F311, not an independent F321.

EXACT CURRENT SAS-VS-BASE ADVCIV EXECUTABLE DELTA INVENTORY
-----------------------------------------------------------
A direct current-file diff against Base AdvCiv 1.14 yields only six executable change
families (plus the SASGameRecordLog include):

1. BUTTONPOPUP_CHANGECIVIC opens `PolicyAdvisorScreen` instead of `CivicsScreen`.
   Current Python registers/imports that entrypoint. Control.
2. `launchButtonPopup` optionally suppresses informational BUTTONPOPUP_TEXT during AI
   Auto Play and logs the omission. Gameplay decision popups are not auto-accepted.
3. `PLAYER_ALWAYS_RAZES_CITIES` is hoisted into a function-static bool.
4. `launchDoEspionagePopup` resolves the first eligible selected Spy instead of the
   arbitrary selection head (KI#398 repair).
5. `launchDoEspionageTargetPopup` applies the same KI#398 selected-Spy repair.
6. Event show-plot warning color lookup is hoisted into a function-static value.

The two espionage repairs do not solve the broader selection-bound modal rebinding
family; that is already F315. The other semantic deltas have no independent current
failure established.

43 / 43 CURRENT MEMBER-DEFINITION MATRIX
-----------------------------------------
The definition inventory was regenerated directly from current source. It contains
exactly 43 CvDLLButtonPopup member definitions; earlier 41/42 counts were regex misses
(getInstance's reference return and/or ctor/dtor), not missing code.

01 getInstance -- ordinary singleton accessor; CONTROL.
02 freeInstance -- SAFE_DELETE singleton teardown; CONTROL.
03 CvDLLButtonPopup ctor -- stores DLL UI interface reference; CONTROL.
04 ~CvDLLButtonPopup -- empty destructor; CONTROL.
05 OnAltExecute -- CvPopupInfo operator= deep-copies scalar/text/callback/button state;
   no shallow ownership/UAF root. Relaunch can re-expose already-numbered stale-state
   families but creates no independent contract failure. CONTROL.
06 OnOkClicked -- fully dispatcher-audited. Numbered roots carried by its reply paths:
   F307-F318 as applicable; F319/F320 involve launch/lifecycle ownership. No currently
   unclassified dispatcher branch remains.
07 OnFocus -- CHOOSETECH staleness = F313; CHOOSEELECTION/DIPLOVOTE insufficient refresh
   belongs to F310/F314/F318; production/civic/religion receiver legality closes their
   delayed state; Python delegates; no separate F321. CONTROL / EXISTING ROOTS.
08 launchButtonPopup -- dispatcher and six SAS deltas classified; Rise & Fall and
   Auto-Play early-return sound/relaunch bookkeeping remains below root bar. CONTROL.
09 launchTextPopup -- informational body only; no action in OnOk. CONTROL.
10 launchProductionPopup -- stable XML object ids and receiver `pushOrder` legality;
   Examine-City sentinel is max(info counts), outside all real id ranges. CONTROL.
11 launchChangeReligionPopup -- launch `canConvert`; synchronized convert rechecks
   current legality. CONTROL.
12 launchChooseElectionPopup -- F309 sentinel/ownership collision + F314 stale proposal
   validity. EXISTING ROOTS.
13 launchDiploVotePopup -- F310 stale candidate eligibility + F318 stale former-member
   Defy. EXISTING ROOTS.
14 launchRazeCityPopup -- ordinary Raze re-runs `canRaze`; Return/Liberate problems are
   F316 and F317. EXISTING ROOTS / otherwise CONTROL.
15 launchDisbandCityPopup -- generational city id; intentional culture-flip Keep/Disband
   flow; no independent changing legality/recipient invariant proved. CONTROL.
16 launchChooseTechPopup -- free-tech stale consumption and ordinary stale research
   queue effect are F313. EXISTING ROOT.
17 launchChangeCivicsPopup -- synchronized revolution path re-runs legality. CONTROL.
18 launchAlarmPopup -- display/sound only. CONTROL.
19 launchDeclareWarMovePopup -- selection-target rebinding is F315; stored destination
   itself is map-coordinate stable and war/move paths have their established checks.
   EXISTING ROOT / otherwise CONTROL.
20 launchConfirmCommandPopup -- shipped confirmation is COMMAND_DELETE; Rapid Unit
   Cycling replacement-selection target is F315. EXISTING ROOT.
21 launchLoadUnitPopup -- mutable valid-transport ordinal = F312; selection replacement
   can compound through F315. EXISTING ROOTS.
22 launchLeadUnitPopup -- cross-player/unit-id alias = F308; replacement selection can
   compound through F315. EXISTING ROOTS.
23 launchDoEspionagePopup -- KI#398 launcher repair; modal reply selection identity
   remains F315. EXISTING ROOT / SAS REPAIR.
24 launchDoEspionageTargetPopup -- same; dormant Destroy Unit parameter mismatch remains
   unreachable with shipped XML destroy/buy-unit factor 0 and is not numbered.
25 launchMainMenuPopup -- displayed controls use current `canDoControl`; execution goes
   back through doControl where relevant. Regenerate is single-player turn-0/no-diplo
   and has no remote stale producer. CONTROL.
26 launchConfirmMenu -- local confirmation of session/control actions; no independent
   supported stale-state consequence. CONTROL.
27 launchCancelDeal -- generational FFreeList deal id prevents silent retarget; stale
   removed deal fails lookup. CONTROL.
28 launchPythonPopup -- button vector and Python callback stay in the same CvPopupInfo;
   OnAlt copy is deep. Core C++ ownership CONTROL (Python callback semantics external).
29 launchDetailsPopup -- edits current local InitCore active-player details; PBEM send
   occurs after applying edits; no supported active-player swap while modal is pending.
   CONTROL.
30 launchAdminPopup -- stable game-detail/admin fields; CONTROL.
31 launchAdminPasswordPopup -- stable ControlTypes values and explicit switch for WB /
   admin details; CONTROL.
32 launchExtendedGamePopup -- dead-human inert affirmative button = F319.
33 launchDiplomacyPopup -- player ids 0..MAX_CIV_PLAYERS-1; cancel sentinel is exactly
   MAX_CIV_PLAYERS; contact rechecks current contact legality. CONTROL.
34 launchAddBuddyPopup -- external/session buddy response with popup-stored account text
   and data; no game-state target reconstruction. CONTROL.
35 launchForcedDisconnectPopup -- notification -> exitToMenu. CONTROL.
36 launchPitbossDisconnectPopup -- notification -> exitToMenu. CONTROL.
37 launchKickedPopup -- notification -> exitToMenu. CONTROL.
38 launchVassalDemandTributePopup -- delayed relationship contract = F311; stale resource
   availability itself is deal-layer revalidated as documented above.
39 launchVassalGrantTributePopup -- same F311 family; resource implementation current-
   legality recheck prevents impossible stale export.
40 launchEventPopup -- trigger reinitialization/ghost-state root = F307; abandoned
   event record after player death has no independent gameplay consumer proven.
41 launchFreeColonyPopup -- positive area / negative generational city ids / zero cancel
   do not collide; split-area receiver revalidates. Liberation recipient recomputation
   is F317. EXISTING ROOT / otherwise CONTROL.
42 launchLaunchPopup -- current `canLaunch` checked at launch and synchronized launch path
   rechecks; victory/game-state guards present. CONTROL.
43 launchFoundReligionPopup -- selected religion can be reprompted if taken; reserved
   Pick Religion entitlement orphan on founder elimination = F320. EXISTING ROOT.

MATRIX RESULT
-------------
Every current member definition is now assigned to one of:

* a numbered root F307-F320;
* a verified receiver/lifecycle/control contract;
* a deliberately unnumbered dormant/moddability/bookkeeping edge with no current
  producer+consequence chain.

No 44th/omitted member exists in current source, and F321 remains unused after this
explicit pass.

REMAINING BEFORE DURABLE QUEUE-019 CLOSURE
-------------------------------------------
Do not close merely from the matrix. Still perform:

* final launch/relaunch counter and popup-sound history reconciliation;
* targeted stock/Civ4CE -> K-Mod -> Base AdvCiv ancestry sweep for every numbered root,
  looking for duplicate roots or a later upstream repair current SAS failed to inherit;
* final finding-number / KI-number / cross-reference consistency pass;
* final queue-routing check (019 -> 001, not 020).

If those close without an independent producer, queue 019 can move to the next durable
checkpoint with F321 unused.

END C030-WIP17


C031 DURABLE -- QUEUE 019 CvDLLButtonPopup.cpp CLOSED / FINAL RELAUNCH + ANCESTRY SATURATION
================================================================================================
Durable closure synthesized from C030-WIP01 through C030-WIP17 against the current
AdvCiv-SAS light-source snapshot. No repository source file was modified.

STATUS
------
Queue 019 CvGameCoreDLL/CvDLLButtonPopup.cpp is CLOSED.
Files complete advance from 29 / 169 to 30 / 169.
F307-F320 remain CONFIRMED.
F321 / provisional KI#644 remains UNUSED.

FINAL RELAUNCH / POPUP-SOUND RECONCILIATION
--------------------------------------------
The remaining C030-WIP17 question was whether an early return from
CvDLLButtonPopup::launchButtonPopup can strand CvPlayer::m_iButtonPopupsRelaunching,
causing later popup sounds to be suppressed.

The shape is real: normal launchButtonPopup attempts reach
CvPlayer::reportButtonPopupLaunched() at the common footer, whereas these branches
return earlier:

* SAS optional BUTTONPOPUP_TEXT suppression during AI Auto Play;
* Rise & Fall RF_CHOOSECIV / RF_DEFEAT / RF_RETIRE dispatch;
* Rise & Fall CvPlot::isAllFog() suppression;
* Rise & Fall chapter-start CHOOSEPRODUCTION / CHOOSETECH suppression through
  RiseFall::isBlockPopups().

However, no supported current producer was established that combines one of those
returns with a live relaunch counter:

1. AI Auto Play cannot normally arm CvPlayer::killAll's counter.
   Starting Auto Play calls CvGame::setAIAutoPlay(..., bChangePlayerStatus=true),
   which calls CvPlayer::setHumanDisabled(true). updateHuman then makes isHuman()
   false. CvPlayer::killAll immediately returns unless the active player is human.
   Countdown calls use bChangePlayerStatus=false only after that status transition.
   Therefore SAS's Auto-Play BUTTONPOPUP_TEXT early return does not intersect a
   newly armed killAll relaunch batch under the supported path.

2. Rise & Fall all-fog/interlude state likewise cannot normally arm the counter.
   RiseFall::setPlayerControl(..., false) clears the EXE popup queue and changes the
   former human civilization back to AI control before/while the map is hidden.
   Once isHuman()==false, killAll returns. On resuming human control, the popup queue
   is cleared before the AI civilization is handed to the human, and all-fog is then
   removed.

3. RiseFall::isBlockPopups is true only on the first turn of a later chapter
   (getCurrentChapter() >= 1). The selected civilization is still AI-controlled when
   setPlayerControl(..., true) begins, so the EXE popup queue is cleared before the
   human takeover. Any Choose Production / Choose Tech popup newly attempted during
   that blocked turn returns false before it can become a displayed popup. It cannot
   therefore later be one of killAll's relaunchDisplayed entries. The first chapter
   has isBlockPopups()==false.

4. RF_CHOOSECIV, RF_DEFEAT and RF_RETIRE are modal control-transition popups. No
   source-visible path was found that arms killAll and then relaunches one of these
   special popups as a preserved displayed entry before the counter drains.

A theoretical asynchronous edge remains possible in principle if a relaunch counter
survived an abrupt control/lifecycle transition before the EXE had consumed the
relaunch queue. The local source does not establish such a producer, and the only
established consequence is transient missing popup audio. Under the album standard
this remains defensive bookkeeping debt, not F321.

FINAL ANCESTRY / DUPLICATE SWEEP
---------------------------------
The queue-019 findings were rechecked against stock/Civ4CE, K-Mod, Base AdvCiv and
current SAS source/history where locally available. No later upstream repair was found
that current SAS failed to inherit, and no pre-queue-019 album root duplicates the
same root contracts.

The resulting ancestry picture remains coherent:

* F307 is AdvCiv-local: the launchEventPopup "double-check" calls initTriggeredData
  again, creating/randomizing a second EventTriggeredData instead of validating the
  original record.
* F308 is an inherited Great-General popup identity bug: the launcher can expose
  teammate units while reply resolution uses the active player's player-local unit id.
* F309 is inherited election-option encoding: a dynamic option ordinal vector shares
  its integer namespace with the GC.getNumVoteInfos() pseudo-sentinel. The root now
  covers both the leaked VoteSelectionData path when that value is out of range and
  the current 48-civ collision path when ordinal 22 is real.
* F310 is inherited delayed team-ballot eligibility, made especially visible by
  AdvCiv's stronger capitulated-team candidate rule.
* F311 is inherited delayed human vassal-tribute relationship state; synchronized
  resource-trade legality revalidation narrows Grant-resource staleness, but the stale
  relationship / Refuse-war consequence remains.
* F312 is inherited LOADUNIT ordinal identity; current SAS's larger multiplayer/team
  configurations do not change the root contract.
* F313 is inherited stale CHOOSETECH state. The free-tech manifestation consumes the
  entitlement before the synchronized grant discovers that the team already owns the
  tech; ordinary research shares the stale list but has the lower-severity queue
  consequence recorded in WIP13.
* F314 originates in K-Mod's strengthened isValidVoteSelection proposal rule without
  equivalent delayed human-choice/result revalidation.
* F315 originates in the interaction between selection-bound stock popup contracts and
  K-Mod's immediate Rapid Unit Cycling path; current SAS inherits both.
* F316 is AdvCiv-local: practical city-liberation refactoring passes a PlayerTypes
  value into TASK_LIBERATE's boolean argument, making PLAYER_0 alias false.
* F317 combines the older Free Colony "display recipient, recompute later" contract
  with AdvCiv's conquest-city TASK_LIBERATE extension. Stable city identity does not
  bind the displayed recipient.
* F318 is inherited delayed diplomatic-vote state combined with AdvCiv's Defy/result
  implementation: a former AP voting member can retain PLAYER_VOTE_NEVER and veto a
  shipped Force No Trade resolution despite current vote weight zero.
* F319 is inherited Extended Game UI logic: the button is gated by a global count of
  alive humans while testExtendedGame ignores eliminated humans; AdvCiv's adjacent
  comment explicitly states dead humans should only be offered the menu-exit path.
* F320 is inherited Pick Religion reservation ownership: the religion slot is marked
  taken before the human chooses, elimination destroys the popup, and no runtime path
  releases the orphaned slot.

NUMBER / CROSS-REFERENCE CONSISTENCY
-------------------------------------
The final queue-019 map is contiguous and has no accidental duplicate allocation:

F307 -> provisional KI#630
F308 -> provisional KI#631
F309 -> provisional KI#632
F310 -> provisional KI#633
F311 -> provisional KI#634
F312 -> provisional KI#635
F313 -> provisional KI#636
F314 -> provisional KI#637
F315 -> provisional KI#638
F316 -> provisional KI#639
F317 -> provisional KI#640
F318 -> provisional KI#641
F319 -> provisional KI#642
F320 -> provisional KI#643
F321 -> provisional KI#644 remains UNUSED

The historical WIP append sections intentionally preserve the status that was true at
those checkpoints; this C031 durable status and the top-of-file CURRENT PROGRESS
SUMMARY are authoritative going forward.

QUEUE-019 CLOSURE DECISION
---------------------------
The file has now received:

* current-source linear review;
* six-family SAS-vs-Base executable-delta classification;
* delayed-state popup construction -> reply -> synchronized receiver tracing;
* explicit 43 / 43 member-definition coverage;
* stock/Civ4CE / K-Mod / Base AdvCiv ancestry checks for the numbered roots;
* final relaunch/audio lifecycle challenge;
* duplicate and finding/KI-number consistency passes.

No independent F321 survived. Queue 019 is therefore saturated enough to close under
our current methodology.

PROTECTED NEXT ROUTE
--------------------
Do NOT resume at queue 020 yet.

The next audit target is the planned deeper-method re-loop:

001 CvGameCoreDLL/CvPlayerAI.cpp
002 CvGameCoreDLL/CvUnitAI.cpp
003 CvGameCoreDLL/CvGameTextMgr.cpp
004 CvGameCoreDLL/CvPlayer.cpp
005 CvGameCoreDLL/CvCityAI.cpp
006 CvGameCoreDLL/CvCity.cpp
007 CvGameCoreDLL/CvUnit.cpp
008 CvGameCoreDLL/CvGame.cpp
009 CvGameCoreDLL/CvPlot.cpp

Only after queue 009's re-audit should the normal pending cursor resume at queue 020.

END C031 DURABLE


================================================================================
C031-WIP01 - QUEUE 001 DEEP-METHOD RE-AUDIT OPENED
================================================================================

SOURCE / ROUTING
----------------
Current authority remains the 2026-08-29 light-source snapshot used by C031 durable.
The protected post-019 route is now active: queue 001 CvPlayerAI.cpp is reopened for
its stronger iterative-WIP re-audit. This is a methodological re-loop of a file that
was already counted complete by the original pass, so the unique completed-file count
remains 30/169 while one re-audit file is open.

Current source examined locally:
  CvGameCoreDLL/CvPlayerAI.cpp (~32.2k current physical lines)
Comparators available locally:
  Base AdvCiv 1.14 CvPlayerAI.cpp
  K-Mod 1.46 CvPlayerAI.cpp
  Civ4CE material where relevant
No repository source file was modified.

FINDING CURSOR
--------------
F321 / provisional KI#644 remains UNUSED.
Do not renumber old queue-001 roots merely because the deeper pass rediscovers them.

OLD QUEUE-001 ROOT RECONCILIATION
----------------------------------
F001 / KI#330 - REPAIRED IN CURRENT SOURCE.
The master/vassal-locus relation is now computed independently of
SAS_AI_DO_DIPLO_TECH_TRADE_MASTER_FROM_TO_VASSAL_CONTACT_PERCENT_OPTIMIZE and is
reused by the separate power-bias path. The old supported-toggle dependency no longer
survives.

F002 / KI#331 - REPAIRED AT THE CURRENT CONTRACT/CALLER LEVEL CHECKED SO FAR.
* CvUnit::scrap() now returns bool and reports canScrap vetoes rather than silently
  presenting success to callers.
* AI_doTurnUnitsPost marks bKilled and skips the later upgrade path only after
  scrap() succeeds.
* AI_doMilitary prefilters canScrap(), advances candidate traversal independently of
  the successful-disband quota, and increments that quota only after scrap() succeeds.
* CvTeam::setVassal capitulation nuclear disarm now uses scrapForced(true), correctly
  separating the mandatory AdvCiv rule from optional SAS AI preservation policy.
The later queue-002 re-loop will still recheck the CvUnitAI caller expansion before
KI#331 is considered fully saturated cross-file.

F003 / existing KI#312 - STILL SURVIVES AS AN INCOMPLETE REPAIR / EXISTING ROOT.
The current KI#312 work materially improves the tech-locus predicate:
* AI_techValue discounts self-research only when AI_canTradeTechFrom says an internal
  master/vassal/sibling source could legally transfer the tech.
* AI_doDiplo suppresses the randomly chosen outsider eBestReceiveTech only when
  AI_hasLocusTechTradeSource finds an immediate legal/non-denied internal source and
  the current partner is outside AI_isLocusMember.
However, AI_doDiplo still computes the independent AdvCiv eBestProgressTech from the
same outsider. After the cluster-first block potentially sets eBestReceiveTech to
NO_TECH, the later advc.550f branch still tests only:
    eBestProgressTech != NO_TECH && eBestProgressTech != eBestReceiveTech
and can buy that outsider tech directly for gold. eBestProgressTech is never passed
through the cluster-first/internal-source predicate. In fact, clearing
 eBestReceiveTech can make the fallback inequality trivially true.
Therefore the old F003 broadening remains current under KI#312; do not allocate F321.
The narrow repair direction remains to apply the shared internal-supply/acquisition
policy consistently to every outsider acquisition route that the cluster-first option
intends to suppress, including the progress-for-gold fallback.

CURRENT FUNCTION / DELTA TRIAGE
-------------------------------
A current-vs-Base function inventory was regenerated before new hunting. The rough
parser sees ~383 current CvPlayerAI definitions vs ~374 in Base; current-only/helper
surface includes the newer SAS military-unit-count helpers, Worker Sea diagnostics,
city-benefit/minimum-worker helpers and related support code.

Largest normalized semantic-delta regions are being prioritized first while retaining
an eventual whole-file inherited-code pass. The leading current regions are:
  AI_updateCommerceWeights
  AI_bonusTradeVal
  AI_updateGreatPersonWeights
  AI_doTurnUnitsPost
  AI_doCivics / AI_civicValue
  AI_doDiplo
  AI_barbarianTargetCityScore / AI_religionValue
  AI_doMilitary / AI_doCommerce
  serialization/read and worker-demand helpers
  AI_disbandValue / AI_isVictoryValid / AI_chooseResearch / AI_diploVote / etc.
Existing KI-tagged repairs and later cross-file findings involving CvPlayerAI will be
reconciled before any new F321 allocation.

AI_updateCommerceWeights - FIRST DEEP PASS
-------------------------------------------
This is the largest current normalized SAS-vs-Base function delta (~369 current lines
vs ~169 Base), but much of the added surface is explicit culture-victory specialization
and level-gated diagnostics rather than hidden control flow.

Controls established so far:
* AI culture-victory targeting is now explicitly separated from human culture-slider
  inference. bAICultureVictory requires !isHuman(), Culture2 stage and a valid culture
  victory; human slider thresholds remain available for automation.
* city_countdown_list is populated once per current city and sorted before rank use;
  the required-city bottleneck index is read only when iVictoryCities > 0 and the
  empire has at least that many cities.
* The per-required-city weight division by iVictoryCities sits behind rank <=
  iVictoryCities; iVictoryCities==0 therefore cannot enter that divide.
* Legendary AI culture-victory cities intentionally receive zero victory investment
  while separately calculated local culture pressure remains available; this matches
  the stated goal of stopping further victory production in an already-finished city.
* Newly introduced AI_setCultureVictoryRank / AI_setCultureVictoryInvestmentPercent
  state is assigned for every city during the normal pass and explicitly cleared for
  Barbarians. No obvious stale per-city state leak has survived this first pass.
* The large CULTURE_* logging additions are gated by gCultureLogLevel and have not yet
  produced a gameplay-state mutation or independent defect.

No F321 has emerged from this function yet. It remains open to a second caller/data
pass when the consuming city-production/specialist code is reconciled later in the
queue-001/queue-005 re-loop.

NEXT EXACT WORK
---------------
1. Deep-audit AI_bonusTradeVal, currently one of the largest SAS semantic additions:
   building bonus synergy, buyer-civilization building substitution, religious
   coverage heuristics, strategic-resource relative multipliers, master/vassal-locus
   exemption and market-value interaction.
2. Reconcile any suspicious shape against current defines/data and later KI#311/
   KI#387 tech/team-overload history before numbering it.
3. Continue to AI_updateGreatPersonWeights and the remaining largest semantic deltas.
4. Build an explicit current KI/known-root map for CvPlayerAI (including later
   cross-file F050/F051-era roots) so the deep re-loop cannot duplicate them.
5. Eventually perform the inherited high-risk whole-file pass; this WIP is not a
   closure claim.

END C031-WIP01


================================================================================
C031-WIP02 - F321 / KI#644: AI_bonusTradeVal uses pre-change strategic holdings
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 is now CONFIRMED.
F322 / provisional KI#645 is UNUSED.
No repository source file was modified.

F321 / provisional KI#644 - CONFIRMED CURRENT SAS MARGINAL-VALUATION DEFECT
----------------------------------------------------------------------------
Title:
AI_bonusTradeVal strategic substitute multipliers ignore iChange and can undervalue
cancellation of the buyer's last imported key resource.

Birth / scope:
SAS-local. The relative strategic-resource valuation block was introduced by the
2025-09-30 AdvCiv-SAS work that added era/substitute-aware Iron/Copper/Horse/Camel/
Elephant pricing. Base AdvCiv does not contain this block.

Current supported configuration:
SAS_AI_BONUS_TRADE_VAL_VALUE_MORE_IRON_COPPER_HORSE_CAMEL_RELATIVELY = 1 in the
current GlobalDefines_advciv_sas.xml. The individual percentages are active current
balance values, e.g. Renaissance Iron with mounts and no Iron = 200%.

Root contract:
AI_bonusTradeVal(eBonus, eFromPlayer, iChange, ...) is not acquisition-only. AdvCiv's
advc.036 contract uses iChange prospectively; AI_considerOffer passes iChange=-1 while
checking whether an existing deal should be canceled/renegotiated. The inherited
AI_bonusVal call therefore evaluates the marginal state after loss.

The SAS strategic block instead builds its substitute state solely from the current
live inventory:
  bHaveCopper    = getNumAvailableBonuses(Copper) > 0
  bHaveIron      = getNumAvailableBonuses(Iron) > 0
  bHaveHorse     = getNumAvailableBonuses(Horse) > 0
  bHaveCamel     = getNumAvailableBonuses(Camel) > 0
  bHaveElephants = getNumAvailableBonuses(Elephants) > 0
It never adjusts the bonus being evaluated by iChange.

Supported reproduction:
1. Renaissance AI P has Horse/Camel but no Iron.
2. P considers buying its first/only Iron from outsider Q.
3. Before the deal, bHaveIron=false, so current configured logic applies
   SAS_AI_BONUS_TRADE_VAL_RELATIVE_BONUSES_REN_NO_IRON_WITH_MOUNT_IRON_PCT = 200.
   That high marginal value can help justify the purchase.
4. The deal starts; the imported Iron is now included in P's current
   getNumAvailableBonuses(Iron), so bHaveIron=true.
5. Once the deal is cancelable, AI_checkCancel calls
   AI_considerOffer(..., iChange=-1). The resource received from Q reaches
   P.AI_bonusTradeVal(Iron, Q, -1, ...).
6. Base/marginal valuation correctly treats this as losing a copy, but the SAS
   substitute block sees the pre-change live import and therefore concludes P already
   has Iron. The intended "no Iron, but mounts" 200% branch is skipped and the relative
   multiplier remains 100%.
7. AI_considerOffer can therefore value the same still-critical import at roughly
   half of the relative strategic value that caused it to be bought and can return
   false, making AI_checkCancel return RENEGOTIATE. AI-AI handling then kills the deal
   and may repropose; AI-human handling can cancel/renegotiate through its existing
   path. Once the import is gone, a fresh acquisition valuation again sees no Iron and
   restores the high multiplier, permitting oscillatory "valuable to buy, cheap to
   lose" behavior.

The same root affects other substitute-state rules whenever the evaluated -1 change
removes the last currently available configured resource: Horse/Camel/Elephants in
relevant eras and pre-Medieval metal/mount group tests. Losing one of multiple copies
correctly leaves the boolean true; the defect is specifically that the prospective
availability is not computed at all.

Why this is not KI#239 / KI#240:
* KI#239 concerned a hard AI-buy DENIAL leaking onto human recipients.
* KI#240 concerned the hard dominated-Copper denial overriding live strategic and
  corporation usefulness.
* F321 is in the price/marginal-value path after legality. It exists even with those
  denial fixes and is driven by current-vs-post-change inventory state.

Why this is not merely tuning:
The XML percentages themselves are balance choices. The defect is applying those
choices to a state different from the state that the function's iChange argument is
explicitly evaluating. Acquisition and cancellation of the same last imported copy
therefore use inconsistent substitute predicates.

Narrow repair direction:
Compute prospective availability for each configured resource, applying iChange only
to the resource currently being valued, e.g. conceptually:
  availableAfter(B) = getNumAvailableBonuses(B) + (eBonus == B ? iChange : 0) > 0
Then derive bHaveIron/bHaveHorse/etc. and grouped predicates from that prospective
state. Preserve all current era percentages, master/vassal-locus exemption and
ordinary AI_bonusVal behavior.

AI_bonusTradeVal - OTHER SURFACE CHECKED SO FAR
------------------------------------------------
The multi-effect building-synergy pass is intentionally heuristic. Current XML was
pulled directly from the supplied light-source ZIP to distinguish data reality from
source speculation:
* the feature is enabled by default;
* current SPECIALBUILDING_TEMPLE and SPECIALBUILDING_MONASTERY have their own
  SpecialBuildingInfo technology gates, but no current Temple/Monastery building in
  CIV4BuildingInfos carries a nonzero BonusHealthChange/BonusHappinessChange, so the
  function's omission of SpecialBuildingInfo tech-gate checks does not currently
  create a bonus-price error through those two budget branches;
* ordinary nonreligious bonus-effect buildings remain deliberate approximate
  coverage valuation rather than a canConstruct simulation. Missing city-specific
  prerequisites/location coverage alone is therefore not promoted without a current
  contradiction/consequence.

The overloaded GET_TEAM(eFromPlayer) shape was also rechecked against rejected KI#311
/ KI#387: AdvCiv deliberately overloads GET_TEAM(PlayerTypes) through TEAMID, so there
is no player-ID-as-team-ID defect here.

NEXT
----
Continue AI_bonusTradeVal through:
* human-information/fairness substitution (bUseOurBonusVal) vs new buyer-specific
  multipliers;
* master/vassal-locus exemption vs generic AIObjective multiplier;
* market-value cap and vassal-price tail;
* current XML building/resource data for any rule that appears source-suspicious.
Then move to AI_updateGreatPersonWeights and the next largest current semantic deltas.
F322 remains unused until an independent root survives producer/contract/consequence
and duplicate checks.

END C031-WIP02


================================================================================
C031-WIP03 - F322 / KI#645: KI#66 building-synergy gate ignores additional AND techs
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 is now CONFIRMED.
F323 / provisional KI#646 is UNUSED.
No repository source file was modified.

F322 / provisional KI#645 - CONFIRMED CURRENT SAS TECH-GATE DEFECT
-----------------------------------------------------------------
Title:
AI_bonusTradeVal's KI#66 multi-effect building-synergy pre-check tests only the
building's primary prerequisite tech, so bonuses can receive building-effect price
markup before all of that building's required AND technologies are actually known.

Birth / lineage:
SAS-local. The KI#66 multi-effect bonus valuation block was introduced at practical
commit 4997, fce68b70e9161e7428339b593c29c3817ec79fe7, titled:
"(Fixed and greatly enhanced) AI valuing for example pig (+1 health) at the same value
than maize ..."
Base AdvCiv does not contain this extra rBoost pass. The original implementation and
current source both use only kB.getPrereqAndTech() for the building-tech gate.

Current supported configuration:
SAS_AI_BONUS_TRADE_VAL_VALUE_MORE_MULTI_EFFECT_BONUSES = 1 in current
GlobalDefines_advciv_sas.xml. KI#66 and the source comments explicitly describe the
rule as applying the extra bonus valuation once the buyer has the tech required to
build the relevant building.

Engine construction contract:
CvPlayer::canConstruct does not treat CvBuildingInfo::getPrereqAndTech() as the whole
technology requirement. Unless bIgnoreTech is requested, it requires:
  1. kBuilding.getPrereqAndTech(), AND
  2. every entry in kBuilding.getPrereqAndTechs(i).
CvBuildingInfo::isTechRequired likewise recognizes both the primary prerequisite and
every additional getPrereqAndTechs entry (and also a SpecialBuilding tech gate).
These are genuine AND requirements, not merely Pedia annotations.

Current KI#66 pricing code instead does only:
  TechTypes const eReq = (TechTypes)kB.getPrereqAndTech();
  if (eReq != NO_TECH && !kOurTeam.isHasTech(eReq))
      continue;
It never tests kB.getNumPrereqAndTechs()/getPrereqAndTechs(i) before adding that
building's BonusHealthChanges/BonusHappinessChanges to rBoost.

Concrete current-data reproduction - ordinary Grocer:
Current CIV4BuildingInfos.xml has:
  BUILDING_GROCER
    primary PrereqTech = TECH_MATHEMATICS
    additional AND tech = TECH_CURRENCY
    bonus effects include +1 for Spices/Banana/Grapes/Sugar.
Current CIV4TechInfos.xml does not make Currency a prerequisite of Mathematics:
  Mathematics requires Writing plus one of its own OR prerequisites;
  Currency instead requires Code of Laws plus its own OR prerequisite.
Therefore a perfectly normal current game state can have Mathematics while Currency
is still unknown.

In that state:
1. Buyer P has Mathematics but not Currency.
2. P cannot construct a Grocer; CvPlayer::canConstruct rejects it on the missing
   additional TECH_CURRENCY prerequisite.
3. AI_bonusTradeVal nevertheless passes its sole primary-tech test because P has
   Mathematics.
4. For Banana/Spices/Grapes/Sugar, the Grocer's positive bonus effect is added to
   rBoost as though the building were already technology-enabled.
5. For an ordinary non-water +1 effect, rBoost changes from 1 to 2 before the block's
   final 0.75 damping, i.e. this building alone materially raises the KI#66 multiplier
   while the building remains unavailable for a missing required technology.
6. The AI can consequently offer/pay a higher bonus price during a real Classical-era
   interval than the KI#66 rule says it should.

Additional current examples:
* BUILDING_PERSIAN_APOTHECARY, BUILDING_CARTHAGE_PUNIC_AGORA and
  BUILDING_PORTUGAL_PEIXARIA have the same Mathematics + Currency requirement and
  positive bonus effects, so their buyer-civilization substitutions inherit the same
  premature valuation.
* BUILDING_OTTOMAN_HAMMAM and BUILDING_IRELAND_MUILEANN_UISCE require Mathematics plus
  the additional AND tech Masonry. Their Marble/Wheat effects are likewise counted
  after Mathematics alone even when Masonry is still unknown.

Why this is a contract defect rather than a tuning disagreement:
KI#66's simplifying heuristic is intentionally generous: once the building is
technology-enabled, treat it approximately as broadly available without simulating
city-by-city construction. That approximation is not the defect. The defect occurs
one stage earlier: the code says a building is technology-enabled after checking only
one member of a multi-tech AND requirement. Current construction code and XML both
make the omitted prerequisites mandatory, and KI#66's own documentation says "if we
have their required tech" / "as soon as the buyer has the tech required to build these
buildings." The pricing code therefore violates its stated gate in current data.

Why this is independent from F321:
F321 concerns prospective iChange state in the later strategic-substitute multiplier,
especially cancellation of the last imported strategic resource. F322 concerns the
earlier building-synergy rBoost and exists on ordinary acquisition pricing even with
no cancellation and no Iron/Horse/Camel substitute logic.

Why existing AdvCiv AI_baseBonusBuildingVal does not rescue it:
AI_baseBonusBuildingVal elsewhere in this same file explicitly constructs
bHasTechForBuilding by checking both getPrereqAndTech() and every
getPrereqAndTechs(i). That demonstrates that the codebase already knows the complete
building-tech contract, but the newer KI#66 pass independently reimplemented only the
primary check.

Current-data SpecialBuilding note:
CvPlayer::canConstruct also checks a SpecialBuildingInfo technology prerequisite.
The KI#66 pass does not. Current bonus-effect cathedrals are deliberately skipped, and
current Temple/Monastery buildings have no nonzero bonus health/happiness effects, so
that omission does not supply an additional current F322 reproduction. It is sensible
defense-in-depth to use the complete technology gate, but this finding does not rely
on speculative future XML.

Narrow repair direction:
Before counting kB's bonus effect, require every building technology prerequisite,
not only getPrereqAndTech(). The smallest behavior-preserving repair is conceptually:
  if (eReq != NO_TECH && !kOurTeam.isHasTech(eReq))
      continue;
  bool bMissingAndTech = false;
  for (int i = 0; i < kB.getNumPrereqAndTechs(); i++)
      if (!kOurTeam.isHasTech(kB.getPrereqAndTechs(i))) ...
  if (bMissingAndTech)
      continue;
Optionally include the SpecialBuilding tech prerequisite for future XML robustness.
Do not replace the KI#66 heuristic with full canConstruct: city/religion/resource/
location requirements were intentionally abstracted away and doing so would change
its design rather than fix this specific gate.

DUPLICATE / HISTORY CHECK
-------------------------
The current reconciled album has no existing root for missing secondary
CvBuildingInfo technology prerequisites in AI_bonusTradeVal/KI#66. Existing KI#66 is
the enhancement being audited, not a prior report of this defect. F321 is a separate
prospective-resource-state root. The initial commit diff confirms the incomplete
single-tech check was present when the KI#66 feature was introduced rather than being
a later accidental formatting/refactor regression.

AI_bonusTradeVal - CURRENT CONTINUATION
---------------------------------------
F321 and F322 show two independent defects in the large SAS-local valuation extension,
but the function is not saturated yet. Continue through:
* human-information/fairness substitution (bUseOurBonusVal) versus buyer-specific
  building and strategic multipliers;
* whether the generic AIObjective multiplier is intentionally outside the
  master/vassal-cluster exemption or contradicts current docs/defines;
* seller/vassal and market-value tail behavior;
* remaining current XML/data assumptions in the building-synergy block, separating
  deliberate approximation from actual prerequisite/data-contract errors.
Then continue to AI_updateGreatPersonWeights and the remaining largest semantic
CvPlayerAI deltas. F323 remains unused until another independent root survives the
same producer/contract/consequence and duplicate standard.

END C031-WIP03


================================================================================
C031-WIP04 - F323 / KI#646: AI_doCivics last-pass flag can bypass paid-anarchy safeguards
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 is now CONFIRMED.
F324 / provisional KI#647 is UNUSED.
Existing KI#312 remains an incompletely repaired older root; it was not renumbered.
No repository source file was modified.

F323 / provisional KI#646 - CONFIRMED CURRENT SAS CONTROL-STATE DEFECT
---------------------------------------------------------------------
Title:
AI_doCivics uses the final recheck pass's bWillSwitch flag as though it described the
accumulated revolution bundle, so the SAS anti-flip-flop recheck can bypass the
near-future-civic and low-treasury safeguards for an already accepted paid-anarchy
switch.

Birth / lineage:
The multi-pass bWillSwitch/bWantSwitch loop is inherited from K-Mod and Base AdvCiv,
but its original thresholds preserve an important coupling: a rejected candidate sets
bWantSwitch only when its value is roughly >20% better, while the recheck lowers the
extra-anarchy threshold to 20%. Thus a candidate strong enough to request the recheck
is normally strong enough to be accepted on that recheck, keeping the final
bWillSwitch state aligned with the nonempty proposed bundle.

AdvCiv-SAS's anti-flip-flop work changed that contract. Current source says:
  bWouldLikeSwitch = (iBestValue > iCurrent);
  if (bWouldLikeSwitch)
      bWantSwitch = true;
but acceptance additionally requires BOTH the inherited percent test and the SAS
absolute-slack test. Current XML enables a 20% absolute slack of |current value|
(SAS_AI_DO_CIVICS_SWITCH_SLACK_PERCENT=20, minimum 5), with still larger slack for a
candidate that adds paid anarchy. Therefore a candidate can now request another pass
while being intentionally unable to pass that next pass.

Root state mismatch:
* aeBestCivic and iAnarchyLength accumulate accepted switches across passes.
* bWillSwitch and bWantSwitch are reset to false at the start of EVERY pass.
* The loop continues while the PREVIOUS pass ended with both true.
* After the loop, the near-future-civic and treasury safeguards are guarded by:
    if (iAnarchyLength > 0 && bWillSwitch)
  Here bWillSwitch describes only whether the FINAL pass accepted something, not
  whether aeBestCivic still contains a switch accepted in an earlier pass.
* canRevolution(aeBestCivic) later tests the actual accumulated bundle and can return
  true even when final-pass bWillSwitch is false. CvPlayer::revolution likewise only
  re-runs canRevolution; neither function repeats the AI wait/treasury safeguards.

Concrete current-configuration control-flow reproduction:
Assume two civic options A and B are encountered in that order, with the empire not
currently in anarchy and eligible to revolution.

1. Pass 1 accepts A. Let A require paid anarchy; iAnarchyLength becomes 2,
   aeBestCivic[A] becomes A-new, and bWillSwitch=true.
2. Later in pass 1, B's current AI value is 100 and its best alternative is 110. If B
   adds no more anarchy to A's existing bundle, the percent threshold is only 5%, so
   110 passes the inherited percent test.
3. Current SAS absolute slack is max(5, 20% of 100)=20. The +10 improvement fails
   bPassSlack because 10 is not >20. B is rejected, but because 110>100 the SAS branch
   still sets bWantSwitch=true.
4. Pass 1 therefore ends with bWillSwitch=true && bWantSwitch=true, entering pass 2.
5. At the start of pass 2 both booleans are reset. Option A is skipped because the
   accumulated aeBestCivic already contains the same best civic A-new.
6. B is evaluated again against the live civic state: AI_bestCivic calls
   AI_civicValue directly and does not install aeBestCivic's local A change into the
   player. B still has 110 vs 100 and still fails the same 20-point absolute slack.
   It sets bWantSwitch=true but never sets bWillSwitch=true.
7. The loop exits because final-pass bWillSwitch=false. aeBestCivic still contains A
   and iAnarchyLength is still 2.
8. The block guarded by `iAnarchyLength > 0 && bWillSwitch` is skipped entirely.
9. `canRevolution(aeBestCivic)` sees that A differs from the live civic and returns
   true; the revolution executes and pays the 2 turns of anarchy.

Concrete skipped consequences:
A) AdvCiv low-treasury protection (advc.131):
The skipped block contains the explicit test intended to avoid paid anarchy when the
AI lacks enough treasury for its negative gold rate. For example, with 2-turn anarchy,
0 strike turns, gold=20 and gold-per-turn=-30, the normal test can return before the
revolution. Under the state mismatch above it is never reached, so the AI can enter
anarchy despite failing AdvCiv's own affordability rule.

B) Near-future civic wait:
The same skipped block checks whether current research will shortly unlock another
valuable civic that can be bundled without increasing the current anarchy length.
When the state mismatch occurs, this intended delay is bypassed too; the AI can pay
anarchy now and then reconsider after the newly researched civic arrives.

Why this is distinct from existing KI#159:
KI#159 diagnosed/tuned excessive paid-anarchy civic churn and direct reversals. Its
implemented fixes deliberately add larger acceptance slack for anarchy-increasing
candidates and a longer post-anarchy civic timer. F323 is a control-flow defect in that
new hysteresis interaction: the broadened `bWantSwitch = any positive improvement`
can make a recheck pass accept nothing, which causes an older last-pass flag to hide an
already accepted bundle from the safeguards below. Fixing F323 does not change the
KI#159 tuning values; it makes those values coexist correctly with the existing
AdvCiv safeguards.

Why this is not simply an inherited K-Mod bug:
The potentially fragile use of final-pass bWillSwitch is inherited, but K-Mod's own
recheck trigger is tied to a >20% rejected improvement and its recheck acceptance
threshold is 20%. SAS explicitly replaced that trigger with any `iBestValue > iCurrent`
while adding an independent absolute-slack requirement that remains active on the
recheck. This breaks the threshold coupling on which the inherited control structure
relied and provides the current supported reproduction above. Base AdvCiv 1.14 retains
the K-Mod-style 20% bWantSwitch trigger and does not contain the SAS absolute slack.

Narrow repair direction:
Do not use final-pass bWillSwitch as a proxy for "the accumulated bundle contains a
paid-anarchy switch." The smallest repair for the two safeguards is to key them on the
accumulated state, e.g. `if (iAnarchyLength > 0)` (or an explicit persistent
`bAnySwitch`/bundle-diff flag), rather than on bWillSwitch from the last pass.
Separately, bWantSwitch could be restored to a predicate that guarantees the recheck
has some chance to pass, but that is a tuning/efficiency choice; the correctness fix is
to make the post-loop safeguards describe the accumulated bundle.

DUPLICATE / HISTORY CHECK
-------------------------
The reconciled album has no earlier numbered root for AI_doCivics's final-pass
bWillSwitch masking an accumulated civic bundle. KI#159 is a tuning/improvement entry
for churn and paid-anarchy damping, not this safeguard-bypass diagnosis. Current
CvPlayer::canRevolution / revolution confirm there is no downstream affordability or
near-future-civic revalidation that would neutralize the bug.

QUEUE-001 CONTINUATION
----------------------
AI_doCivics remains open beyond F323. Continue through:
* the remainder of the current SAS logging/timer changes and any interaction between
  iFinalAnarchyLength, getMaxAnarchyTurns and no-anarchy civic switches;
* AI_civicValue's large SAS specialist/valuation changes, reconciling current XML and
  civic data rather than treating heuristic disagreements as bugs;
* then continue the current semantic-delta priority list (AI_doDiplo,
  AI_barbarianTargetCityScore / AI_religionValue, AI_doMilitary, AI_doCommerce, etc.)
  before the inherited whole-file saturation pass.
F324 remains unused until an independent producer/contract/consequence chain survives
current-data and duplicate checks.

END C031-WIP04


================================================================================
C031-WIP05 - F324 / KI#647: unlimited-specialist civic value double-counts shared citizens
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 is now CONFIRMED.
F325 / provisional KI#648 is UNUSED.
No repository source file was modified.

F324 / provisional KI#647 - CONFIRMED CURRENT SAS CAPACITY DOUBLE COUNT
-----------------------------------------------------------------------
Title:
AI_civicValue's generalized unlimited-specialist estimator reuses the same finite
citywide specialist-capable population independently for every specialist type a civic
unlocks, so Caste System can value one citizen as several simultaneous specialists.

Birth / lineage:
SAS-local, in the generalized unlimited-specialist civic valuation introduced for
existing KI#76. Base AdvCiv/K-Mod do not contain this per-specialist capacity/output
estimator. Later KI#315 and KI#316 repaired the sign and baseline-GPP treatment of the
same block but intentionally left its capacity estimate unchanged.

Current code contract:
For every specialist type where `kCivic.isSpecialistValid(eLoopSpecialist)`, current
AI_civicValue loops every city and computes:

  iSpecialistNow = max(0,
      population - workingPopulation - angryPopulation)
  iFoodSlackSpecs = (foodDifference() >= 2 ? 1 : 0)
  iSurplus = iSpecialistNow + iFoodSlackSpecs
  iOpen = max(0, maxSlotsForThisType - assignedCountForThisType)
  iNeed = max(0, iSurplus - iOpen)
  iPotential += min(iNeed, softCap)

It then multiplies that iPotential by the output value of THIS specialist and adds the
result to iTotal before repeating the entire city-capacity calculation for the next
unlimited specialist type.

The population term is explicitly a shared finite pool, not a per-specialist-type
resource. CvCity::visiblePopulation() is defined by the same expression:
  getPopulation() - angryPopulation() - getWorkingPopulation().
Assigned specialist counts change the city's specialist population; one population
point cannot be an Artist and a Scientist and a Merchant at the same time.

Concrete current-data reproduction:
Current CIV4CivicInfos.xml has exactly one multi-type unlimited-specialist civic:
  CIVIC_CASTE_SYSTEM -> SPECIALIST_ARTIST,
                        SPECIALIST_SCIENTIST,
                        SPECIALIST_MERCHANT.
The other current unlimited-specialist civics unlock only one type each (Police State
Spy, Wage Labor Engineer, Trade Bloc Merchant, Organized Religion Priest), so the
shared-capacity duplication specifically biases Caste in current data.

Minimal one-city case:
1. P is evaluating Caste System while not currently running it.
2. City C has zero currently assigned specialists, no open Artist/Scientist/Merchant
   slots, no angry population, and `foodDifference() >= 2`.
3. For Artist, iSpecialistNow=0 and iFoodSlackSpecs=1, so iNeed/iPotential gain 1.
4. The entire same city calculation is repeated for Scientist: the SAME one-person
   food-slack capacity is again counted as iPotential=1.
5. It repeats again for Merchant.
6. iTotal therefore prices one plausible extra citizen as one Artist PLUS one Scientist
   PLUS one Merchant. All three current specialist types have positive commerce and/or
   GPP output, so all three impossible simultaneous assignments add positive civic
   value.

Existing-specialist case is stronger:
If a city has, for example, three non-angry specialist citizens and no open slots for
Caste's three types, the shared iSpecialistNow pool can contribute up to three Artists,
three Scientists and three Merchants before the soft caps - nine valued specialist
assignments from three people. The per-type current-slot subtraction does not make the
pools independent; it only changes how much of the SAME population each type appears
to need.

Consequence / why this is a defect rather than ordinary heuristic tuning:
The block's own comment says it estimates "How many extra of this specialist could we
plausibly run empire-wide?" and "the marginal need this civic actually unlocks." Its
soft cap and food-slack approximation are tuning choices. Reusing the same people as
productive specialists several times is a capacity-accounting contradiction: the
outputs summed into iTotal cannot coexist. Because Caste is the only current civic
unlocking three types, this systematically inflates Caste relative to the one-type
unlimited-specialist civics the KI#76 rewrite was intended to compare more fairly.
It can also make active Caste unusually sticky: existing specialist population is
reused for each of its three valid specialist types when its absolute civic score is
recomputed.

Why this is distinct from KI#76 / KI#315 / KI#316:
* KI#76 broadly rewrote and tuned unlimited-specialist valuation because inherited
  logic overfavored Caste and failed to value Engineer-like alternatives well.
* KI#315 fixed an active-civic sign error in this block.
* KI#316 restored baseline GPP value.
* F324 is independent shared-capacity arithmetic: even with those repairs, one finite
  population pool is still multiplied across every unlocked type.

Narrow repair direction:
Compute each city's marginal unrestricted-specialist capacity once, then allocate that
finite capacity across the specialist types the civic unlocks. A conservative heuristic
could value each marginal citizen at the best currently useful unlocked specialist (or
allocate by the same specialist-value ranking used by citizen assignment) rather than
summing a full copy of the capacity for every type. If specialist-type flexibility is
intended to carry extra value, add a bounded diversity/flexibility premium separately;
do not represent flexibility as simultaneous duplicate citizens.

DUPLICATE CHECK
---------------
No existing reconciled album root or current Known Issues entry diagnoses reuse of the
same citywide specialist-capacity pool across multiple `SpecialistValid` types. KI#76
is the feature/tuning history and KI#315/#316 are different arithmetic defects within
the block. Current XML provides a direct three-type Caste reproduction, so this is not
future moddability-only debt.

AI_doCivics FINAL TAIL CONTROL
------------------------------
After F323, the rest of AI_doCivics was rechecked before moving into AI_civicValue:
* iFinalAnarchyLength is recomputed from the actual accumulated aeBestCivic bundle,
  so the KI#159 extra civic timer uses the final bundle rather than stale iAnarchyLength.
* no-anarchy switches receive no per-anarchy extra timer; the inherited base timer
  choice still distinguishes players whose maximum anarchy is zero.
* the structured logging caches are diagnostic-only and do not alter civic choice.
No independent F324 arose from the timer/log tail.

QUEUE-001 CONTINUATION
----------------------
Continue AI_civicValue through the other SAS semantic additions:
* current civic-percent-anger replacement (reconcile against repaired KI#317);
* specialist-commerce weighting / toned multiplier and its define contracts;
* unlimited-specialist per-output/anarchy scaling beyond the shared-capacity root;
* current civic XML cases and any state-dependent iS behavior.
Then continue to AI_doDiplo and the next high-delta functions before inherited whole-
file saturation. F325 remains unused until an independent current root survives.

END C031-WIP05


================================================================================
C031-WIP06 - F325 / KI#648: active civic-pressure benefit is sign-inverted
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 remains CONFIRMED.
F325 / provisional KI#648 is now CONFIRMED.
F326 / provisional KI#649 is UNUSED.
No repository source file was modified.

F325 / provisional KI#648 - CONFIRMED CURRENT SAS DIRECTION/SIGN DEFECT
-----------------------------------------------------------------------
Title:
AI_civicValue's city-level CivicPercentAnger replacement adds the hypothetical welfare
change directly to the civic's absolute score; when the pressure civic is already
active, the modeled harm of dropping it therefore becomes negative value for KEEPING
the civic.

Birth / lineage:
SAS-local in the KI#75 replacement of K-Mod's broad civic-pressure estimate with a
city-level actual-angry-citizen model. Current KI#317 later fixed the city-selection
logic so happy cities that would become unhappy are no longer skipped, but retained
the direction/sign calculation examined here.

Relevant AI_civicValue contract:
`iS` is the inherited K-Mod sign identifying the direction of the hypothetical change:
  iS = +1 if eCivic is not currently active (evaluate gaining/adopting its effects)
  iS = -1 if eCivic is currently active (evaluate losing/dropping its effects).
Surrounding state-dependent civic components use that sign so the marginal welfare
change is converted into positive absolute value for a beneficial civic in either
state.

Current pressure block instead computes:
  iPermilDelta = -iS * iPermilIfWeDont
  iAngryCitizenChange = changedAngry - currentAngry
  iCityVal = -12 * iAngryCitizenChange
  iValue += scaled iCityVal
The first two lines correctly model the hypothetical direction. The final value is
then the welfare change of MAKING that hypothetical switch, not yet the absolute value
of eCivic. For iS=-1 the code fails to reverse the drop result back to the value of
keeping the active civic.

Current-data reachability:
Current CIV4CivicInfos.xml gives CIVIC_THEOCRACY `iCivicPercentAnger=200`; it is the
current nonzero CivicPercentAnger civic. Current XML also enables both city-level
pressure scales:
  SAS_AI_CIVIC_VALUE_ANGER_PRESSURE_SCALE_PERCENT_IF_A_CITY_IS_UNHAPPY = 150
  SAS_AI_CIVIC_VALUE_ANGER_PRESSURE_SCALE_PERCENT_IF_A_CITY_IS_HAPPY = 100.
Therefore this is live current gameplay logic whenever known rivals running Theocracy
create enough pressure to change actual angry-citizen counts.

Concrete direction reproduction:
Assume P is not running Theocracy, rivals create enough Theocracy pressure that city C
has two angry citizens attributable to that pressure, and adopting Theocracy removes
those two angry citizens.

Before adoption:
1. iS=+1.
2. iPermilDelta is negative, modeling removal of pressure.
3. changedAngry-currentAngry = -2.
4. iCityVal = -12 * -2 = +24 before the configured scale.
5. The pressure relief correctly makes Theocracy more attractive to adopt.

After adoption, with the same rivals still running Theocracy:
1. AI_civicValue evaluates active Theocracy with iS=-1.
2. `getCivicPercentAnger(eCivic,true)` still reports the rival pressure that would
   return if P stopped running the civic.
3. iPermilDelta is now positive, correctly modeling the hypothetical DROP.
4. If dropping would restore two angry citizens, iAngryCitizenChange=+2.
5. iCityVal becomes -24 and is added directly to THE ACTIVE THEOCRACY'S score.
Thus the very anger that Theocracy is currently preventing LOWERS its active civic
value instead of raising it.

The same logic can produce a pick-then-drop discontinuity: the component is positive
while considering adoption and negative after adoption, even though the underlying
rival pressure has not changed. Safely happy cities that would remain happy contribute
zero as intended; the defect concerns cities where the civic actually changes angry
population.

Why this is a sign/contract bug rather than civic tuning:
The KI#75 design explicitly says to value the pressure civic when it actually benefits
our cities. If keeping an active civic prevents angry citizens, that is the same
benefit as adopting it to remove those citizens. Current code instead treats the harm
of a hypothetical drop as evidence against the active civic. This is a direction
conversion error independent of the chosen 12-point base or 100/150% scales.

Why this is distinct from KI#315 and KI#317:
* KI#315 fixed a sign bug in the separate unlimited-specialist intrinsic-value block,
  where active civics subtracted their own specialist benefit.
* KI#317 fixed the pressure block's pre/post-happiness filtering so cities crossing the
  happiness cap are counted.
* F325 is the remaining directional normalization of the pressure welfare delta. The
  KI#317 city set can now be exactly correct while the resulting active-civic value is
  still sign-inverted.

Narrow repair direction:
Convert the hypothetical welfare change back into the absolute value of eCivic using
`iS`, conceptually e.g.:
  iCityVal = -12 * iAngryCitizenChange * iS;
This leaves the current pressure calculation, current/post-change angry counts and XML
scales intact:
* inactive/adopt: -angry change * +1 stays positive for relief;
* active/drop: harm is negative marginal welfare, then * -1 becomes positive value for
  retaining the civic.
Recheck the intended post-change pressure-scale asymmetry separately as tuning; it is
not necessary to change it to fix this sign defect.

DUPLICATE / CURRENT-HISTORY CHECK
---------------------------------
No reconciled album entry diagnoses this CivicPercentAnger active-state sign. KI#75 is
the feature/tuning history; KI#317 owns the city-crossing/filter repair; KI#315 is a
different sign regression in the specialist component. Current Theocracy and current
pressure defines provide a shipped reproduction rather than future-mod data only.

AI_civicValue CONTINUATION
--------------------------
Continue through:
* specialist-commerce weighting and toned denominator (including define-boundary and
  which current civics receive the generalized commerce treatment);
* remaining unlimited-specialist anarchy/output scaling after F324;
* any other SAS-only state-dependent additions in the function;
then move to AI_doDiplo and the next high-delta functions before inherited whole-file
saturation. F326 remains unused until another independent current root survives.

END C031-WIP06


================================================================================
C031-WIP07 - F326 / KI#649: specialist-commerce tuning replaced structural modifier normalization
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 remains CONFIRMED.
F325 / provisional KI#648 remains CONFIRMED.
F326 / provisional KI#649 is now CONFIRMED.
F327 / provisional KI#650 is UNUSED.
No repository source file was modified.

F326 / provisional KI#649 - CONFIRMED CURRENT SAS FORMULA/CONTRACT REGRESSION
-----------------------------------------------------------------------------
Title:
AI_civicValue's specialist-extra-commerce tuning also replaced K-Mod's structural
AI_averageCommerceMultiplier divisor for ordinary civic percentage-commerce modifiers;
the custom preference weight then cancels against its own toned divisor and existing
empire commerce multipliers are no longer backed out.

Birth / lineage:
SAS-local. Commit 59d8c92c41f71d9698bfd1cf8b909eb1706f203b is titled:
  "Fix the issue of civics that give specialist extra commerce ... by valuing commerce
   per specialist more"
and names CIVIC_REPRESENTATION's specialist beaker/gold/culture output as the target.
The patch generalized custom SAS commerce weights across the shared commerce loop,
including the ordinary `kCivic.getCommerceModifier` branch which is a structurally
different effect and was not the stated specialist-extra-commerce problem.

Inherited K-Mod / Base AdvCiv contract:
For ordinary civic percentage-commerce modifiers Base AdvCiv retains K-Mod's:
  iTempValue += (kCivic.getCommerceModifier(eCommerce) *
      100 * getCommerceRate(eCommerce)) /
      AI_averageCommerceMultiplier(eCommerce);
...
  iTempValue /= 100;
  iTempValue *= AI_commerceWeight(eCommerce);
  iTempValue /= 100;

`CvPlayer::getCommerceRate` is the cached sum of actual city commerce rates.
`CvCity::updateCommerce` computes those actual rates by multiplying each city's
`getBaseCommerceRateTimes100` by `getTotalCommerceRateModifier(eCommerce)`.
`AI_averageCommerceMultiplier` is independently cached as the population-weighted
average of those same city total-commerce-rate modifiers.

Therefore the inherited division is not merely a taste/priority coefficient. It backs
out the empire's already-applied current commerce multiplier before estimating the
incremental output of an additional civic percentage modifier. Only after that
structural normalization does K-Mod apply `AI_commerceWeight` as the AI preference for
the resulting commerce type.

Current SAS formula:
Current SAS replaces the divisor with:
  iSASCommerceWeight = SAS_SpecCommerceBaseWeight(...)
  iSASCommerceTonedDownMult =
      iSASCommerceWeight * SAS_AI_CIVIC_VALUE_SPEC_COMMERCE_WEIGHT_TO_MULT_TONING_DOWN_PERCENT / 100
and then still multiplies the final iTempValue by iSASCommerceWeight.
The shipped toning percentage is 45.

Ignoring only integer truncation, the ordinary modifier term becomes:
  M * 100 * Rate / (W * 45/100)
  then /100
  then *W/100
  = M * Rate / 45.
Thus the very custom commerce weight W that is supposed to encode the hierarchy
cancels from this ordinary-modifier term. With current base weights:
  research 300 -> toned divisor 135 -> effective post-weight divisor 45
  gold     300 -> toned divisor 135 -> effective post-weight divisor 45
  culture  100 -> toned divisor  45 -> effective post-weight divisor 45
  espionage200 -> toned divisor  90 -> effective post-weight divisor 45.
At the same time the actual `AI_averageCommerceMultiplier` no longer participates, so
an empire with 100%, 150% or 250% existing city commerce multipliers is normalized as
though those were the same.

Current-data reachability:
This branch is live in shipped CIV4CivicInfos.xml independently of Representation:
* CIVIC_FREE_SPEECH has CommerceModifiers [0, 0, 50] (+50% culture).
* CIVIC_FREE_RELIGION has CommerceModifiers [0, 10, 0] (+10% research).
Current specialist-extra-commerce civics are different effects:
* CIVIC_REPRESENTATION has SpecialistExtraCommerces [1,1,1,0].
* CIVIC_FREE_MARKET has SpecialistExtraCommerces [1,0,0,0].
So the specialist-targeted formula change currently changes the valuation of two
ordinary percentage-modifier civics as well.

Intent / documentation evidence:
Long Comment #168 describes the new value explicitly as a toned-down commerce
"multiplier" derived from the SAS specialist commerce weights, with the example
300 * 50% = 150, and explains that the purpose is to stop Representation from moving
from underpicked to overpicked after strengthening its specialist commerce.
The introducing commit likewise says the issue is civics "that give specialist extra
commerce" and that the formula is being strengthened so specialist extra commerce is
valued more. Neither describes replacing the inherited normalization for already-
modified ordinary player commerce.

Why this is a formula bug rather than merely different AI tuning:
A tuning change may intentionally make research or culture more/less important. Here:
1. the configured commerce preference weight algebraically cancels for the ordinary
   `% commerce` effect despite that weight being deliberately different by commerce;
2. an actual state variable needed to avoid double-applying existing city multipliers
   (`AI_averageCommerceMultiplier`) is replaced with that preference value; and
3. the change was introduced for the separate specialist-extra-commerce component.
The resulting formula therefore violates both the inherited marginal-output structure
and the stated scope/meaning of the new knob.

Narrow repair direction:
Keep K-Mod's structural divisor for `kCivic.getCommerceModifier`:
  ... / AI_averageCommerceMultiplier(eCommerce)
Then decide independently which final preference weight SAS wants for that ordinary
commerce output (`AI_commerceWeight` or the deliberate SAS replacement).
Keep the toned SAS multiplier on `kCivic.getSpecialistExtraCommerce`, where the
introducing commit and Long Comment #168 actually intend stronger specialist-commerce
valuation. Do not reuse a preference weight as a substitute for the empire's current
commerce-rate multiplier.

DUPLICATE CHECK
---------------
No reconciled album or current Known Issues entry diagnoses this ordinary-commerce
modifier denominator/weight cancellation. KI#76 concerns unlimited specialist slots;
KI#315/#316 are later specialist sign/GPP repairs. F326 concerns the older shared
commerce loop and is independently reachable through Free Speech and Free Religion.

AI_civicValue CONTINUATION
--------------------------
Continue with F327 unused through:
* define-boundary / integer-zero behavior around the toned specialist multiplier,
  distinguishing current shipped configuration from optional hardening;
* the remaining unlimited-specialist anarchy/output scaling after F324;
* inherited high-risk civic valuation components beyond the SAS delta slice.
Then continue queue-001 high-delta functions and ultimately whole-file saturation.

END C031-WIP07


================================================================================
C031-WIP08 - F327 / KI#650: civic war-weariness happiness uses current modifier, not candidate
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 remains CONFIRMED.
F325 / provisional KI#648 remains CONFIRMED.
F326 / provisional KI#649 remains CONFIRMED.
F327 / provisional KI#650 is now CONFIRMED.
F328 / provisional KI#651 is UNUSED.
No repository source file was modified.

F327 / provisional KI#650 - CONFIRMED INHERITED K-MOD WRONG-VARIABLE REGRESSION
-------------------------------------------------------------------------------
Title:
AI_civicValue's K-Mod war-weariness happiness component guards on the candidate civic's
WarWearinessModifier but computes the happiness change from the PLAYER'S CURRENT
aggregate war-weariness modifier; inactive candidates can therefore receive the
magnitude or even the sign of the civic currently being run instead of their own
prospective effect.

Current SAS code:
  if (kCivic.getWarWearinessModifier() != 0) // K-Mod. (original code deleted)
  {
      iValue += (12 * iCities * iS * AI_getHappinessWeight(
          iS * intdiv::round(
          getWarWearinessPercentAnger() * -getWarWearinessModifier(),
          GC.getPERCENT_ANGER_DIVISOR()), 1, true)) / 100;
  }

The outer guard is explicitly about `kCivic`, but the magnitude inside the hypothetical
happiness delta is `getWarWearinessModifier()` on CvPlayerAI. That getter is the current
aggregate player state, not the candidate civic being evaluated.

Mechanics / contract proof:
`CvPlayer::updateWarWearinessPercentAnger` computes and caches the player's BASE war-
weariness anger from team war-weariness plus game/world/handicap modifiers. It does not
apply the player's civic WarWearinessModifier there.

The civic/player modifier is applied later at the city level. `CvCity` takes the
player's base `getWarWearinessPercentAnger()` and multiplies it by roughly:
  100 + cityWarWearinessModifier + playerWarWearinessModifier.
`CvPlayer::processCivics` changes the player aggregate by exactly
`kCivic.getWarWearinessModifier() * iChange`.

Therefore the prospective happiness effect of THIS civic is structurally based on
`kCivic.getWarWearinessModifier()` (with `iS` converting adopt/drop direction), not on
whatever modifier the player happens to have before the hypothetical change.

Stock / K-Mod / AdvCiv lineage:
Civ4CE / stock BtS uses the candidate civic correctly. Its corresponding block is:
  if (kCivic.getWarWearinessModifier() != 0)
  {
      int iAngerPercent = getWarWearinessPercentAnger();
      int iPopulation = 3 + getTotalPopulation()/max(1,getNumCities());
      int iTempValue = (-kCivic.getWarWearinessModifier() *
          iAngerPercent * iPopulation) /
          (GC.getPERCENT_ANGER_DIVISOR() * 100);
      if (iTempValue != 0)
          iValue += ... AI_getHappinessWeight(
              isCivic(eCivic) ? -iTempValue : iTempValue, 1) ...;
  }

K-Mod replaced this with its newer city-happiness estimator and introduced the wrong
getter:
  getWarWearinessPercentAnger() * -getWarWearinessModifier()
while still guarding on `kCivic.getWarWearinessModifier() != 0`.
Base AdvCiv and current SAS retain the exact K-Mod variable choice unchanged.
Thus this is an inherited K-Mod regression, not a SAS-local tuning change.

Current-data reachability:
Current SAS has several live nonzero civic modifiers, including:
* Hereditary Rule: -25
* Universal Suffrage: +25
* Police State: -50
* Theocracy: -25
* Free Religion: +25.
Government therefore supplies especially clean same-option reproductions.

Concrete wrong-direction reproduction:
Assume P currently runs Hereditary Rule (-25), has nonzero war-weariness anger, and is
evaluating inactive Universal Suffrage (+25).

Actual prospective effect:
* Universal Suffrage increases the player's WW multiplier by +25 compared with its own
  absolute civic effect; this civic's WW happiness component should be harmful / have
  a negative sign.

Current K-Mod/SAS happiness block:
1. Guard passes because candidate Universal Suffrage has +25.
2. iS=+1 because Universal Suffrage is inactive.
3. Inner expression ignores +25 and reads P's CURRENT aggregate -25.
4. Negating current -25 produces +25.
5. `AI_getHappinessWeight(...,+change, bPercent=true)` is therefore asked to value a
   happiness IMPROVEMENT.
The candidate's +25 war-weariness penalty can consequently receive a positive
happiness contribution solely because the currently active civic has -25.

Magnitude loss / zero-current reproduction:
Assume instead P currently runs Representation (0) with no other global player WW
modifier. Evaluating inactive Police State (-50) and Universal Suffrage (+25):
* both candidate guards pass;
* current `getWarWearinessModifier()` is 0 for BOTH;
* the candidate -50 versus +25 magnitude and sign disappear entirely from this block.
Moreover `AI_getHappinessWeight` explicitly coerces an input of 0 to +1, so this caller
can give both candidates the same small positive phantom happiness contribution rather
than zero. This is especially perverse for Universal Suffrage, whose +25 actually
worsens war weariness.

The earlier cdtw.4 urgency block DOES use `kCivic.getWarWearinessModifier()` and can
partly offset these errors, but that block explicitly says the city-happiness effect is
also valued below and intentionally double-counts high urgency. A correct first block
does not make a wrong-variable second block harmless; the latter is intended to value
the actual city happiness consequences and currently does not.

Active-civic nuance:
When the current aggregate happens to equal the active candidate civic's modifier and
there are no other player-level sources, the erroneous getter can accidentally produce
a plausible drop-direction delta for the active civic. This does not rescue inactive
candidate comparison: every alternative is still evaluated through the active/current
modifier rather than its own. Additional aggregate WW sources also break that accidental
identity.

Zero-delta caller issue:
Even after replacing the wrong getter, the rounded candidate happiness delta can be 0
at sufficiently low war weariness. `AI_getHappinessWeight` intentionally maps iHappy=0
to 1. Stock's old block explicitly skipped the helper when `iTempValue == 0`; the K-Mod
replacement lost that guard. This is best fixed in the same block while repairing the
wrong-variable contract: calculate the prospective `iHappy` first and call
AI_getHappinessWeight only when it is nonzero. It is a closely coupled consequence of
the same replacement rather than a separate F328 at this checkpoint.

Narrow repair direction:
Compute the hypothetical percentage-happiness delta from the candidate civic, e.g.
conceptually:
  int iHappy = iS * intdiv::round(
      getWarWearinessPercentAnger() * -kCivic.getWarWearinessModifier(),
      GC.getPERCENT_ANGER_DIVISOR());
  if (iHappy != 0)
      iValue += 12 * iCities * iS *
          AI_getHappinessWeight(iHappy, 1, true) / 100;
Preserve the surrounding K-Mod scaling unless empirical tuning says otherwise. The
important contract repair is candidate modifier + adopt/drop direction, not current
aggregate state; the nonzero guard preserves the stock behavior that a rounded zero
change has zero value.

DUPLICATE CHECK
---------------
No reconciled album or current Known Issues entry owns this civic-value wrong-variable
root. Existing war-weariness findings are in different systems:
* F036 / KI#357: AI_warCommitmentCost passed accumulated AI cost instead of raw war
  weariness to a team-war valuation helper.
* KI#415: dead-team war-weariness decay became unreachable after an AdvCiv iterator
  refactor.
* later UWAI war-anger findings concern enemy/team attribution and caching.
F327 is specifically `AI_civicValue` using current player WarWearinessModifier where
the candidate civic's modifier is required.

AI_civicValue CONTINUATION
--------------------------
Continue with F328 unused through the inherited happiness/religion/yield/trade/hurry
and state-dependent blocks, challenging other candidate-vs-current variable uses rather
than assuming inherited K-Mod logic is safe. Then continue queue-001 high-delta
functions and ultimately the full CvPlayerAI.cpp saturation pass.

END C031-WIP08


================================================================================
C031-WIP09 - F328 / KI#651: -100% maintenance civics erase their own active valuation base
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 remains CONFIRMED.
F325 / provisional KI#648 remains CONFIRMED.
F326 / provisional KI#649 remains CONFIRMED.
F327 / provisional KI#650 remains CONFIRMED.
F328 / provisional KI#651 is now CONFIRMED.
F329 / provisional KI#652 is UNUSED.
No repository source file was modified.

F328 / provisional KI#651 - CONFIRMED INHERITED SATURATION / NON-INVERTIBLE-STATE DEFECT
----------------------------------------------------------------------------------------
Title:
AI_civicValue reconstructs pre-civic maintenance by dividing CURRENT maintenance by
`100 + current player modifier`; at the legitimate -100% boundary current maintenance
has already saturated to zero, so the lost base cannot be reconstructed and a civic
that abolishes that maintenance category loses its own active valuation.

Relevant AI_civicValue pattern (distance; colony is analogous):
  if (kCivic.getDistanceMaintenanceModifier() != 0)
  {
      int iTemp = 0;
      FOR_EACH_CITY(pLoopCity, *this)
      {
          iTemp += pLoopCity->calculateDistanceMaintenanceTimes100() *
              (pLoopCity->getMaintenanceModifier() + 100) / 100;
      }
      iTemp *= 100;
      iTemp /= std::max(1, getDistanceMaintenanceModifier() + 100);
      ...
      iValue -= iTemp * kCivic.getDistanceMaintenanceModifier() / 10000;
  }

This is intended to back the player's CURRENT modifier out of measured current city
maintenance and then apply the candidate civic's modifier to that recovered base.
That inversion works only while the current multiplier is nonzero.

Actual distance-maintenance mechanics:
`CvCity::calculateDistanceMaintenanceTimes100(..., bNoPlayerModifiers=false)` computes
its base and then applies:
  iTempMaintenance *= std::max(0,
      GET_PLAYER(eOwner).getDistanceMaintenanceModifier() + 100);
  iTempMaintenance /= 100;
Thus a current player distance modifier of -100 makes the returned distance-maintenance
component exactly zero before AI_civicValue sees it.

Current-data State Property reproduction:
Current CIV4CivicInfos.xml has:
  CIVIC_STATE_PROPERTY
  iDistanceMaintenanceModifier = -100.
`CvPlayer::processCivics` applies that value directly through
`changeDistanceMaintenanceModifier(... * iChange)`, and this modifier has no other
current source in CvPlayer.cpp; State Property therefore sets the relevant aggregate to
exactly -100 in the normal case.

Before adoption:
1. current distance modifier is normally 0;
2. city distance maintenance is positive where distance costs exist;
3. AI_civicValue divides by 100 and applies candidate -100;
4. State Property correctly receives a positive value for abolishing distance cost.

After adoption:
1. current distance modifier is -100;
2. each `calculateDistanceMaintenanceTimes100()` already returns 0 because of
   `max(0,100-100)`;
3. the evaluator multiplies that 0 by the city maintenance modifier and sums 0;
4. it attempts to divide the state back out by
   `max(1, getDistanceMaintenanceModifier()+100) = max(1,0) = 1`;
5. zero remains zero;
6. applying State Property's candidate -100 to that recovered value still gives 0.
The exact economic benefit that made State Property attractive before adoption therefore
disappears from its own active civic score merely because the civic successfully reduced
the measured cost to zero.

Current-data colony manifestation:
Current SAS Vassalage has:
  iNumCitiesMaintenanceModifier = -25
  iColonyMaintenanceModifier = -100.
AdvCiv's `calculateColonyMaintenanceTimes100` applies the player colony modifier to the
maintenance cap:
  iMaintenanceCap *= max(0, getColonyMaintenanceModifier()+100) / 100;
so active Vassalage's -100 makes the colony-maintenance cap and resulting colony
maintenance zero.

AdvCiv's later `AI_civicValue` colony block copied the same inverse-current-state pattern:
  current colony maintenance -> divide by max(1,current modifier+100) -> apply candidate.
At current -100 it has the same information-loss failure: Vassalage's abolition of
colony maintenance is valued before adoption but not recoverable once active. Its -25%
num-cities-maintenance effect remains separately valued; F328 concerns the saturated
-100 component.

Why `std::max(1, ...)` is not a valid repair:
The guard avoids division by zero, but zero current output contains no information about
the suppressed pre-modifier base. Replacing denominator 0 with 1 merely converts an
undefined inverse into a defined but wrong zero estimate. This is a mathematical
non-invertibility at a valid gameplay boundary, not optional XML misconfiguration.

Stock / K-Mod / AdvCiv lineage:
* Civ4CE / stock BtS used a rough direct candidate formula for distance and num-cities
  maintenance, e.g. `-kCivic.getDistanceMaintenanceModifier() * cityCount / ...`; it
  did not derive candidate value by inverting current maintenance and therefore did not
  lose the benefit simply because the candidate was active.
* K-Mod introduced the more accurate current-maintenance reconstruction for distance
  and num-cities maintenance. Stock State Property already has the -100 distance effect,
  so the saturation boundary exists in K-Mod's improved formula.
* Base AdvCiv inherits that distance pattern and `advc.912g` extends the same pattern to
  ColonyMaintenanceModifier.
* Current SAS retains both and additionally has a live -100 colony modifier on
  Vassalage.
Thus the distance root is inherited from K-Mod; the colony manifestation is the same
formula assumption extended by AdvCiv and reached by current SAS data.

Gameplay consequence:
`AI_civicValue` compares absolute civic scores. An intrinsic economic saving should not
vanish from the score merely because the evaluated civic is currently active. The
current discontinuity can therefore cause pick-then-drop pressure: State Property can
score highly while inactive because it abolishes substantial distance maintenance, then
lose that entire component immediately after adoption while competing Economy civics
retain their intrinsic benefits. Vassalage can similarly lose its colony-maintenance
component after adoption.

Narrow repair direction:
Do not infer the pre-modifier maintenance base by dividing an already-saturated current
value at -100. Available robust directions include:
* use the maintenance calculation's existing ability to skip player modifiers / expose
  a base component where available (distance already has `bNoPlayerModifiers=true` in
  its static helper), then apply the candidate modifier explicitly;
* add/use an analogous base calculation for colony maintenance; or
* simulate the candidate state from an unsaturated base rather than algebraically
  inverting current state.
Preserve city/handicap/world/local maintenance modifiers as appropriate. The key is that
-100 must be valued from a pre-player-modifier base, not from current zero output.

DUPLICATE CHECK
---------------
No reconciled album or current Known Issues entry diagnoses this active-civic -100%
maintenance inversion. Existing maintenance findings concern different domains. F328
is specifically the `AI_civicValue` current-state reconstruction becoming non-invertible
at a valid -100% civic modifier.

AI_civicValue CONTINUATION
--------------------------
Continue with F329 unused through the remaining top-half unit upkeep/conscript and
inherited civic-value components. The unit-cost algebra and direct intrinsic-effect
blocks should be closed explicitly, then AI_civicValue can be provisionally saturated
before moving to AI_doDiplo and later whole-file rechecks.

END C031-WIP09


================================================================================
C031-WIP10 - queue 001 continuation: AI_civicValue saturation + diplo/religion/military/commerce controls
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 remains CONFIRMED.
F322 / provisional KI#645 remains CONFIRMED.
F323 / provisional KI#646 remains CONFIRMED.
F324 / provisional KI#647 remains CONFIRMED.
F325 / provisional KI#648 remains CONFIRMED.
F326 / provisional KI#649 remains CONFIRMED.
F327 / provisional KI#650 remains CONFIRMED.
F328 / provisional KI#651 remains CONFIRMED.
F329 / provisional KI#652 remains UNUSED.
No repository source file was modified.

AI_civicValue - PROVISIONAL LOCAL SATURATION
--------------------------------------------
The deep SAS-delta + inherited high-risk pass over AI_civicValue is provisionally
saturated with F324-F328. This is not yet a whole-file/caller final closure; a later
CvPlayerAI.cpp reconciliation can reopen a component if another caller exposes a new
contract. Locally, however, no remaining independent candidate is open.

Controls closed after F328:
* Unit-upkeep value uses current unit-cost state deliberately to price candidate free-
  unit and per-military-unit cost deltas. Current SAS civics keep the especially tricky
  fields separated enough that no candidate/current-state alias comparable to F327
  survived.
* Conscript value uses the candidate civic's own getMaxConscript.
* Direct intrinsic effects (feature happiness, hurry access, improvement yields etc.)
  legitimately do not all multiply by iS: AI_civicValue compares absolute civic scores.
  iS is required where a block begins from CURRENT state and estimates a hypothetical
  marginal transition; F325/F327 were defects precisely because that direction contract
  was violated.
* The inherited corporation block's apparently odd use of HeadquarterCommerce in a
  quantity named maintenance is mechanically correct: CvCity's actual corporation
  maintenance base includes those HeadquarterCommerce values too.
* Unlimited-specialist anarchy scaling remains a rough heuristic but not a new current
  contract failure. All current civics that unlock unlimited specialists have base
  anarchy length 1; after KI#315 the block no longer has the earlier active/inactive sign
  discontinuity. AI_doCivics separately handles actual bundle anarchy.
* F326 also owns the optional zero-divisor consequence: a 0 toned specialist-commerce
  tuning value becomes dangerous only because that specialist tuning value was wrongly
  substituted as the ordinary commerce-rate divisor. Restoring
  AI_averageCommerceMultiplier for ordinary percentage-commerce effects removes the
  divide-by-zero shape too, so no separate finding is warranted.

AI_doDiplo - SAS-LOCAL DELTA CONTROL + EXISTING KI#312 STILL PARTIAL
--------------------------------------------------------------------
The current SAS semantic delta in AI_doDiplo is concentrated in the tech-contact block.
The F001/KI#330 repair is sound: master/vassal/sibling-locus classification is computed
independently of the preferential-contact option, so the power-bias exemption no longer
inherits the unrelated toggle.

The new contact multiplier is passed through AI_contactRoll coherently and acts only as
an intended contact-probability heuristic. The master/vassal/sibling classification
matches its documented scope. No new F329 emerged from those changes.

The old F003 / existing KI#312 repair remains INCOMPLETE, as already recorded in WIP01:
cluster-first eligibility is applied when choosing/suppressing eBestReceiveTech, but the
independent outsider high-progress fallback eBestProgressTech can still be purchased for
gold without passing through the cluster-first sourcing check. This remains the existing
KI#312 root and must not receive a new F number during the re-loop.

AI_religionValue - INTENTIONAL LARGE POWER-BLOC AMPLIFICATION
--------------------------------------------------------------
The repaired KI#309 player-by-player power accumulation is coherent: it now weights the
actual individual player's power whose state religion is being examined, so mixed-
religion teammates/vassals no longer inherit or duplicate whole-bloc power.

The resulting diplomatic religion multiplier can be extremely large (including values
above 10,000% for a strong known power bloc and low ReligionFlavor), but this is NOT a
missing /100 bug. Long Comment #107 explicitly documents the percentage-scale formula:
  Modifier = [MULT * Base * PowerBlocPercent + ADD] /
             [DIVIDER_MULT * ReligionFlavor + ADD]
and works through an example Base 75, power bloc 70%, ReligionFlavor 5 -> 10,500%,
explicitly describing the resulting boost as huge and intentionally left for autoplay /
tuning. Therefore this is deliberate design, not F329.

AI_doMilitary - KI#331 REPAIR VERIFIED
--------------------------------------
The current SAS repair to routine financial scrapping survives the deeper pass.

Candidate construction:
* AI_disbandValue still supplies the ordered desirability metric.
* candidates now enter the quota-indexed vector only if canScrap() currently permits
  routine removal;
* an independent iDisbandCandidateIndex traverses candidates;
* iDisbandCount increments only after scrap() returns success.
This repairs the old KI#331 contract where a restrictive SAS canScrap veto could consume
quota / be reported as though removal succeeded.

Lifetime/index check:
* unit_values stores stable player-unit IDs, not dense positional indices;
* removing one unit does not renumber later unit IDs;
* scrap() calls scrapInternal -> kill(true), starting delayed death for that same unit;
* the caller immediately calls doDelayedDeath() after a successful scrap and never uses
  that unit pointer afterward;
* killing that candidate does not normally delete the unrelated units whose stable IDs
  are still present later in unit_values.
Thus the separated candidate index does not introduce an index-shift/use-after-free loop.

Budget/quota check:
* iCost is recalculated after each successful scrap;
* the loop stops as soon as the actual budget condition clears;
* the inherited per-turn disband quota still applies outside strike;
* strike deliberately retains the inherited ability to continue beyond that quota while
  financial pressure persists.
The SAS extra-unit tolerance derives a rough average per-unit cost from
max(1, iCost / getNumUnits()) and raises iMaxCost by an era-scaled number of units. This
is approximate tuning, not a legality/accounting contract: the real cost/gold condition
is still recomputed after every removal. No F329 here.

AI_doCommerce - SAS DELTA CONTROL
---------------------------------
The SAS-vs-Base executable delta is logging/performance-only in current behavior:
* culture-slider intermediate values are captured for CULTURE_SLIDER_DECISION logging;
* AI_averageCulturePressure() is called once, stored, and reused for the two inherited
  calculations instead of being called twice;
* ESPIONAGE_EACH_TURN_UNIT_COST_DECREASE is restart-cached in a static const;
* final slider state is logged after verifyGoldCommercePercent().

The potentially semantic-looking culture-pressure cache is exactly equivalent:
AI_averageCulturePressure() is a const accessor that simply returns the turn-cached
m_iAverageCulturePressure member; it performs no calculation, random draw, cache refresh
or mutation. Reusing one local value therefore cannot change the inherited decision.
The new diagnostic locals are written/read only for logging and do not feed any slider
calculation. No F329 here.

QUEUE-001 CONTINUATION
----------------------
F329 / provisional KI#652 remains UNUSED.
Continue the deep re-loop through the next high-value/delta functions rather than
reopening the now-provisionally-saturated local AI_civicValue block without a caller-
level reason. Priority next surfaces include serialization/read state, worker-demand and
disband helpers, AI_isVictoryValid / AI_chooseResearch / AI_diploVote, followed by the
full inherited high-risk and whole-file reconciliation. Existing historical roots must
remain under their old numbers when rediscovered.

END C031-WIP10


================================================================================
C031-WIP11 - queue 001 continuation: disband/Worker/serialization + small delta controls
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 through F328 / provisional KI#651 remain CONFIRMED.
F329 / provisional KI#652 remains UNUSED.
No repository source file was modified.

AI_disbandValue - SAS REWRITE PROVISIONALLY CLEAN
-------------------------------------------------
The SAS rewrite deliberately makes XML production cost dominant, removes SyncRand jitter,
reduces XP/level influence, adds a light combat-strength factor, devalues units whose
ObsoleteTech is already known, and adds land-heavy/naval-heavy map preferences. These are
large heuristic changes, but no independent correctness root survived the current-data and
caller-contract pass.

Important controls:
* Routine AI disband callers fixed under KI#331 now prefilter canScrap / count only actual
  successful scraps, so AI_disbandValue no longer causes an ineligible-but-cheap obsolete
  unit to consume quota merely because its ObsoleteTech devalues it before the configured
  obsolete-era delay permits removal.
* Current maximum XML unit cost is 500 (ICBM); ordinary shipped values do not by themselves
  overflow the rewritten integer score. Extremely high accumulated XP could eventually
  make intermediate int multiplication unsafe, but no ordinary/current producer near that
  range was established; record as arithmetic hardening debt rather than F329.
* UNIT_SETTLER currently has iCost=0, but recommended obsolete-only AI scrapping rejects it
  (no qualifying ObsoleteTech), so cost dominance does not make routine AI scrapping delete
  Settlers under the shipped policy.
* Map-type and obsolete multipliers are monotonic ranking preferences and are documented as
  tuning. No legality state is derived from them.

AI_neededWorkers / SAS_minimumAreaWorkerFloor - CURRENT POLICY COHERENT
-----------------------------------------------------------------------
The KI#192/KI#198 repair chain was rechecked through the shared helper and main callers.

Controls:
* Secondary civilized areas may now reach zero standing demand only when their raw task
  demand is zero; the pre-rounding bKeepAreaWorkerReserve preserves the inherited one-Worker
  reserve whenever that secondary area still has actual raw work. Barbarians and the
  primary area retain the reserve explicitly.
* The primary-area standing floor and AI_neededWorkers are shared by production/usefulness
  and Worker-scrap guards, preventing one subsystem from intentionally retaining fewer
  Workers than another insists on rebuilding.
* The one-city <=1 Worker cap is intentionally applied after the stronger primary floor and
  therefore remains the final opening-growth protection. Current XML describes it as the
  minimum highest-ever capital population before a "one-city empire" may need >1 Worker;
  it does not promise original-capital/turn-0-only scope. A late empire reduced to a genuinely
  young low-pop sole capital can therefore re-enter this protection without contradicting
  the shipped setting. This is policy/tuning, not F329.
* CvPlayer::acquireCity preserves CvCity::getHighestPopulation when ownership changes, so
  ordinary conquest/trade does not silently reset a mature city's history and re-arm the
  cap merely because the city object changed owner.

SERIALIZATION read/write - KI#46 POLICY + CURRENT FORMAT PAIRED
--------------------------------------------------------------
CvPlayerAI::read has a visually large delta because SAS deleted historical uiFlag migration
branches. This is deliberate KI#46 policy: AdvCiv-SAS supports same-version saves rather than
cross-version migration.

Current same-version format remains paired:
* write emits uiFlag=22;
* read consumes exactly the modern fields that Base AdvCiv consumes on its uiFlag>=22 path;
* scalar caches, UnitAI arrays, diplomacy counters, expansionist-hate map, brag/warn arrays,
  attitude/first-contact/contact/memory arrays, city-site vector, bonus/unit weights,
  victory weights, Great Person weights, best-tech vector, close-border cache,
  needed-explorer map and alive-major UWAI cache occur in matching order;
* the read/write UWAI gate is the same alive+major-civ predicate.
Unsupported pre-22 historical layouts are therefore not a current defect.

Inherited note only: write uses `&m_aeBestTechs[0]` even when size can be zero. In old STL this
is formally unsafe/implementation-dependent, but the count passed to Write is zero and this
shape is inherited from Base AdvCiv. No present runtime failure was established; retain as
hardening/portability debt for the later inherited sweep, not F329.

AI_isVictoryValid / AI_chooseResearch / AI_diploVote / AI_doAdvancedStart CONTROLS
-----------------------------------------------------------------------------------
* AI_isVictoryValid differs only by restart-caching BBAI victory-strategy defines. The Space
  branch declares a cached value but accidentally still performs the original direct define
  lookup; that is merely a missed micro-optimization, not changed behavior. No shipped
  runtime setDefine writer for these defines was found.
* AI_chooseResearch adds RESEARCH_TARGET_CHOSEN diagnostics only; no decision state is fed
  back from the logging locals.
* AI_diploVote's substantive changes are the already-numbered/currently-fixed KI#371 repair:
  Force-Peace war-success and unit normalization now aggregate the same master+vassal
  coalition population as the power comparison, and coalition identity uses master team.
  Do not allocate a new finding during the queue-001 re-loop.
* AI_doAdvancedStart only hoists ADVANCED_START visibility/sight GlobalDefine reads into
  static locals. No current runtime mutation of those defines was found.

SAS UNIT-COMBAT / COMPOSITION HELPERS - CURRENT CALL-SITE DATA CONTROL
-----------------------------------------------------------------------
AI_countUnitsByCombat and the Trebuchet-like helpers classify each owned UnitClass through
this civilization's active unit type rather than iterating every actual CvUnit. This could
misclassify a gifted/captured foreign UU if that UU changes combat class or city-attack
identity relative to the recipient's class mapping.

Current call-site/data reconciliation prevents a present root:
* AI_countUnitsByCombat is currently used only for UNITCOMBAT_SIEGE in the no-excess-siege
  production gate.
* Current civ-specific replacements that change UnitCombat exist (Arabia, Babylon,
  Carthage, England, Khmer, Mali, Persia), but none changes a class to/from UNITCOMBAT_SIEGE.
* Current siege-class replacements (Holy Roman Houfnice and Korean Hwacha, Cannon class)
  remain UNITCOMBAT_SIEGE and have the same current city-attack threshold side for the
  Trebuchet-like split.
Thus actual-type iteration would be more robust for future modding/gifted foreign units, but
no current siege-cap miscount is established. Keep as future-proofing debt, not F329.

QUEUE-001 CONTINUATION
----------------------
F329 / provisional KI#652 remains UNUSED.
Continue with the remaining meaningful SAS delta surfaces: AI_isSASCityLikelyToBenefitUsLongTerm
and its conquest/trade/overseas callers, the military composition helpers' callers, then the
smaller semantic deltas (AI_doAdvancedStart already controlled, AI_doCounter,
AI_enemyTargetMissions, AI_stopTradingTrade, AI_targetCityValue, AI_cityTrade,
AI_negotiatePeace, AI_updateStrategyHash, AI_doTurnPost, AI_doReligion, etc.). After the
SAS-delta inventory is exhausted, resume the independent inherited high-risk whole-file pass.

END C031-WIP11


================================================================================
C031-WIP12 ADDENDUM - QUEUE 001 DEEP RE-AUDIT CONTINUES
================================================================================

F329 / provisional KI#652 - CONFIRMED
-------------------------------------
AI_isSASCityLikelyToBenefitUsLongTerm checks active World Wonders using the city's CURRENT
owner/team even when its callers are explicitly evaluating the city for a DIFFERENT
prospective recipient.

Current helper:
    bool const bIntrinsicSiteProtectedByActiveWorldWonder =
        (bDistanceOverrideActiveWorldWonder && kCity.hasActiveWorldWonder());

CvCity::hasActiveWorldWonder() calls getNumActiveWorldWonders(1) with the default
`eOwner=NO_PLAYER`, so obsolescence is tested against kCity.getTeam(). Yet AdvCiv already
provides exactly the needed prospective-owner API:
    int CvCity::getNumActiveWorldWonders(..., PlayerTypes eOwner) const;
and labels eOwner "Hypothetical owner". The implementation selects the obsolescence team as
TEAMID(eOwner) when one is supplied.

This mismatch is harmless in AI_conquerCity after acquisition, because the captured city is
already owned by `this`. It is live in the other behavioral callers:
* AI_cityTrade asks the RECIPIENT player whether the still-seller-owned city is likely to
  benefit that recipient long-term. A false result is an absolute DENIAL_UNKNOWN before
  AI_cityTradeVal can value the developed city.
* SAS_findNearbyOverseasConquestTarget evaluates still-foreign cities and skips a target
  entirely when this helper returns false, before AI_targetCityValue.
* SAS_isDistantDisposableBarbarianTarget likewise evaluates a still-Barbarian city for its
  prospective conqueror and can reject a distant expedition on the result.
The overseas logging caller inherits the same displayed misclassification, though its log is
not itself a separate behavioral root.

Current XML proves owner-team obsolescence can differ for real World Wonders. Seven shipped
World Wonders have an ObsoleteTech:
* Great Lighthouse -> Astronomy
* Colossus -> Democracy
* Oracle -> Broader Education
* Temple of Kukulcan -> Chemistry
* Temple of Artemis -> Broader Education
* Great Wall -> Gunpowder
* Apostolic Palace -> Mass Media

Concrete current reproduction direction:
1. Seller/foreign owner S owns a poor-intrinsic-site city containing the Great Lighthouse.
2. S's team has Astronomy, so kCity.hasActiveWorldWonder() returns false.
3. Prospective AI recipient/attacker B does NOT have Astronomy. On transfer/capture the World
   Wonder building is preserved, and it is active for B.
4. KI#186's helper nevertheless evaluates S's obsolescence state and can reject the city as
   unlikely to benefit B long-term.
5. The later AdvCiv target valuation would have handled this correctly: AI_targetCityValue
   already uses `c.getNumActiveWorldWonders(MAX_INT, getID())`, explicitly evaluating active
   Wonder count for the prospective attacker. The KI#186 filter can prevent reaching that
   correct broader valuation.

The reverse asymmetry is also wrong: if S lacks the obsoleting tech but B already has it, the
helper can protect an intrinsically poor site because the Wonder is active for S even though
it becomes obsolete for B immediately upon acquisition.

This contradicts KI#186's documented question - whether ACQUIRING the city is likely to
benefit the RECIPIENT long-term - and KI#186.2's rule that an ACTIVE World Wonder protects
intrinsic site quality. The define is enabled by default. The error was introduced by SAS's
KI#186/KI#186.2 helper; Base AdvCiv's hypothetical-owner World-Wonder API already exists.

Narrow repair direction:
* Inside AI_isSASCityLikelyToBenefitUsLongTerm, evaluate the protection as e.g.
  `kCity.getNumActiveWorldWonders(1, getID()) > 0` rather than `kCity.hasActiveWorldWonder()`.
  This automatically remains equivalent for the post-conquest self-owned caller while
  becoming correct for trade and pre-conquest target callers.
* Keep AI_conquerCity's separate outer `bActiveWorldWonder` check as current-owner state; at
  that point acquisition has already happened and current owner == evaluator.
* Add an autoplay/unit-style diagnostic case with different obsolescence tech state between
  city owner and prospective recipient (Great Lighthouse/Astronomy is a simple fixture).

Severity: MEDIUM AI acquisition/target-selection correctness. Can veto a city whose Wonder
would be active for the recipient, or admit/protect a site based on a Wonder that will be
obsolete for the recipient. Current city-trade and overseas-conquest filters make the
consequence behavioral, not cosmetic.

Duplicate check: no earlier album/Known-Issues root was found for hypothetical-owner World
Wonder obsolescence in this helper. KI#186/KI#186.2 are the introducing/fixed-feature family,
not an already-diagnosed instance of this recipient-state mismatch.

STATUS AFTER F329
-----------------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 through F329 / provisional KI#652 are CONFIRMED.
F330 / provisional KI#653 is UNUSED.
No repository source file was modified.

CONTINUATION
------------
Continue AI_isSASCityLikelyToBenefitUsLongTerm across its remaining prospective-state inputs
(bonus visibility/obsolescence, water-improvement assumptions, overlap/culture and distance),
then proceed through AI_cityTrade / AI_targetCityValue and the remaining meaningful SAS delta
surfaces. Keep inherited high-risk whole-file saturation for after the SAS-delta inventory.

END C031-WIP12


================================================================================
C031-WIP13 ADDENDUM - QUEUE 001 DELTA/REPAIR RECONCILIATION
================================================================================

STATUS
------
Queue 001 CvPlayerAI.cpp remains OPEN under the deep-method re-audit.
Files complete remains 30/169; one re-audit file is open.
F321 / provisional KI#644 through F329 / provisional KI#652 remain CONFIRMED.
F330 / provisional KI#653 remains UNUSED.
No repository source file was modified.

AI_isSASCityLikelyToBenefitUsLongTerm - REMAINING INPUT FRAME CONTROL
---------------------------------------------------------------------
F329 is localized to active-World-Wonder ownership state rather than evidence that the whole
helper uses the wrong owner frame.

The other behavioral inputs were traced:
* BFC bonus visibility/obsolescence uses `kPlot.getBonusType(getTeam())`, i.e. the evaluator's
  prospective team.
* Improvement feasibility and improvement yields use the evaluator's PlayerTypes/TeamTypes.
* Harbor-class sea-food assumption resolves the evaluator's civilization building.
* Water-resource improvement access uses the candidate city's physical adjacency to the
  water area and other ports owned by the evaluator. The city does not need to be currently
  owned for its coastline to become usable after acquisition.
* `iOverlapPlots`, third-party culture counters and feature-health totals still inspect some
  current-ownership state but are BBAI diagnostics only; they do not enter the return value.
* Same-area distance searches the evaluator's current own cities and is therefore correctly
  prospective.
No second owner-frame root survived after F329.

AI_cityTrade / AI_targetCityValue - SAS DELTA CONTROL
-----------------------------------------------------
AI_cityTrade differs behaviorally from Base AdvCiv only by replacing AI_isAwfulSite with the
KI#186 helper. F329 therefore owns the current SAS regression in that delta; no independent
city-trade change survived.

AI_targetCityValue differs behaviorally only by making Base AdvCiv's Space4 capital bonus
XML-tunable. The current default remains 10, preserving Base behavior. Its inherited Wonder
valuation already uses `c.getNumActiveWorldWonders(MAX_INT, getID())`, correctly applying
prospective-attacker obsolescence. This is a useful positive control for F329.

EXISTING REPAIR CLUSTER - DEEP RECHECK CLEAN
--------------------------------------------
KI#362 / AI_doCounter:
* Bonus Trade now computes the accumulated ceiling, subtracts the existing counter and caps
  the random increment by the nonnegative remaining room.
* No second Bonus-Trade increment path inside AI_doCounter bypasses this repair.
* Unsupported pre-fix saves could contain an already-over-cap value, but current code adds no
  further excess and normal decay can reduce it; do not reopen KI#362.

KI#534 + KI#544 / AI_enemyTargetMissions:
* Transient zero-unit groups are skipped before MissionAI/declare-war inspection, eliminating
  the null-head assertion without omitting any physical unit/cargo contribution.
* Valid loaded units are counted once: transport groups contribute `getCargo`, while units
  encountered in their own boarded groups contribute only when `!isCargo()`.
* No partial repair or second count path survived.

KI#608 / AI_stopTradingTrade:
* `CvDeal::isPeaceDeal()` supplies whole-deal peace classification rather than requiring the
  peace marker and annual payment on the same gives-list.
* The annual-payment scan deliberately remains directional: only resources/GPT that THIS AI
  owes prevent it from honoring an embargo request. This matches the documented reparations
  safeguard.

KI#372 / AI_proposeWarTrade:
* UWAI target selection stores `iBestTeamPrice` when accepting its target.
* The supported legacy target-selection branch now does the same.
* No branch reaches common compensation balancing with a valid eBestTarget and the old -1
  sentinel price.
No new F330 belongs to this repair cluster.

AI_doTurnPost / AI_logWorkerSeaAudit - DIAGNOSTIC CONTROL
---------------------------------------------------------
The level-3 Worker-Sea audit added to AI_doTurnPost is diagnostic-only in the current path.
It maintains diagnostic first-seen maps, queries city/unit/path state and writes logs. It
consumes no gameplay RNG and issues no missions/orders.

It ForceResets GC.getBorderFinder before its reachability scan; the underlying
AI_isUnimprovedBonus contract already requires every caller using this shared finder to reset
it before its own query, so no persistent gameplay path/cache is invalidated by the log pass.
New-game/load turn rollback clears the diagnostic first-seen maps. No behavioral root found.

AI_updateCommerceWeights - NEW CULTURE STATE RECHECK CLEAN
-----------------------------------------------------------
The previously reviewed SAS culture-victory changes were revisited through their consumers.
AI_setCultureVictoryRank / AI_setCultureVictoryInvestmentPercent are reset to zero when the
player is not an AI Culture2 candidate (and explicitly for Barbarians). Behavioral CvCityAI
consumers additionally require current culture-victory stage and positive stored rank /
investment. No stale culture-victory state survived a strategy exit.

The bottleneck-countdown indexing remains guarded by positive required-victory-city count and
enough cities; Legendary cities have their AI victory investment zeroed while independent
local culture pressure is added afterward. No new current defect found in this delta pass.

QUEUE-001 CONTINUATION
----------------------
F330 / provisional KI#653 remains UNUSED.
Resume the larger AI_techValue SAS delta, with special attention to the current KI#77/KI#312
master-vassal supply modifiers and the military-danger percentage composition; then continue
the remaining semantic delta inventory before the independent inherited whole-file pass.

END C031-WIP13


===============================================================================
C031-WIP14 -- QUEUE 001 DEEP RE-LOOP: INHERITED TOP-OF-FILE / TURN-LIFECYCLE PASS
CvGameCoreDLL/CvPlayerAI.cpp
Continuation after C031-WIP13
===============================================================================

METHOD / SCOPE
--------------
The current SAS-vs-Base semantic delta inventory is provisionally saturated through
C031-WIP13. This checkpoint begins the independent inherited-code pass that the original
queue-001 review did much less deeply. The standard remains producer -> violated
contract -> current consequence, and an inherited line is not treated as safe merely
because Base AdvCiv or K-Mod contains it.

No new F330 / KI#653 root is established in this checkpoint.

INITIALIZATION / CACHE / TURN-PRE CONTROLS
------------------------------------------
- Constructor/destructor/reset ownership for the dynamic AI arrays is paired coherently:
  constructor allocates the fixed-size player/contact/memory/commerce arrays, AI_uninit
  owns the XML-sized value/weight caches, and AI_reset recreates those XML-sized caches
  only outside constructor initialization.
- AI_reset's reciprocal player-state clearing is intentional CHANGE_PLAYER/reset state;
  the getID()!=NO_PLAYER guard prevents indexing another player's reciprocal arrays with
  the constructor sentinel.
- AI_doTurnPre deliberately runs player counters/cache updates before the human return.
  Humans still need attitude/bonus/commerce/GP/enemy-unit cache state for diplomacy/UI
  and automated subsystems. AI-only research/commerce/military/civic/religion decisions
  correctly occur after the human return.
- The K-Mod comment about attitude timing remains coherent: player counters update here,
  while team-side attitude-affecting counters are still updated later by the team turn
  machinery before the shared attitude cache refresh.

PEACE-NEGOTIATION PASS
----------------------
- AI_doPeace / AI_willOfferPeace preserve the intended contact/team-leader restrictions
  and contact timer gating. A human target team is contacted only through the proper
  team leader path; abContacted prevents multiple human contacts for one target team.
- AI_negotiatePeace builds reparations only from currently tradeable tech/gold/city
  items. AI-AI execution goes through CvGame::implementDeal/CvDeal validation; human
  proposals go through the diplomacy/trade UI and synchronized deal validation.
- The recent SAS victory-threat pre-check at AI_negotiatePeace entry is the already
  repaired false-success peace issue documented in Known Issues; it is not a new root.
- The overloaded compensation helper starts with caller-initialized NO_TECH / NULL / 0
  outputs and does not retain object pointers beyond the immediate negotiation build.
  No delayed stale-city pointer path exists for the AI-AI case; the human UI stores
  trade item identity and the final deal layer revalidates execution.

FOUND-VALUE / AREA-TARGET CONTROLS
----------------------------------
- AI_updateFoundValues explicitly zeros unrevealed plot found values before invoking the
  CitySiteEvaluator. This checkpoint does not duplicate the separately pending hidden
  topology / CitySiteEvaluator information-leak root (e.g. KI#502 lineage) merely because
  deeper evaluator code can infer map structure.
- AI_updateAreaTargets' K-Mod timer logic is coherent: an existing/new target guarantees
  the short timer refresh; an area deliberately/randomly cleared to NULL does not itself
  force another reset unless another area or the old timer already requires it.
- SyncRandOneChanceIn is synchronized gameplay RNG, not async/UI RNG, so area-target
  random clearing remains deterministic in multiplayer.

MOVEMENT / UNIT-SCHEDULER PASS
------------------------------
- AI_movementPriority's switch fall-through among ATTACK_CITY -> ATTACK -> COUNTER ->
  PILLAGE/RESERVE is intentional cumulative priority shaping, not a missing-break bug.
- Empty groups return MAX_INT after the explicit head-unit NULL check. Ordinary land,
  sea, air, cargo, worker, explorer, bombard/collateral and stranded priority classes
  remain internally ordered and bounded under current combat strengths.
- AI_unitUpdate's force-separation handling encounters an ALREADY-NUMBERED cross-file
  root: F212 / provisional KI#535 from the later CvSelectionGroupAI audit. That root
  explicitly includes this pre-pass, where stale turn-start forceUpdate can override a
  newer AI_isGroupAttack continuation. Do not allocate F330 from the same state contract.
- Apart from F212, the scheduler rebuilds group IDs before each priority pass and
  re-resolves each ID before AI_update; the post-sort loop tolerates groups destroyed by
  earlier updates. The first force-separation pass relies on the engine-maintained
  group-cycle invariant asserted at function entry; no current producer of a stale
  group-cycle node independent of F212 has been established.
- The repeat condition is deliberately restricted to selection-group count increases;
  destroyed groups cannot create an infinite repeat, and newly created groups are
  handled without waiting for another turn slice.

WORK-ASSIGNMENT / INACTIVE CENTRALIZED PRODUCTION
--------------------------------------------------
- AI_makeAssignWorkDirty / AI_updateAssignWork are thin all-city dispatchers.
- AI_assignWorkingPlots' anarchy early return is inherited advc.rom2 policy; there is no
  pointer/cache ownership issue in this wrapper.
- AI_doCentralizedProduction is under #if 0. Its unfinished BBAI/K-Mod body is excluded
  from current executable audit findings.

UNIT-ACCEPT / CAPTURE / BASIC WEIGHT CONTROLS
---------------------------------------------
- AI_acceptUnit's class-cap checks are performed before financial-trouble exceptions;
  essential Worker/Worker-Sea/Missionary/Great-Person exceptions therefore cannot bypass
  hard unit-class maxima.
- AI_captureUnit makes a location/force-safety decision; the eUnit parameter is unused
  by design in inherited advc.010 logic. No current type-specific capture legality is
  promised by this helper, so the unused parameter alone is not a defect.
- AI_yieldWeight and AI_commerceWeight are heuristic weights rather than legality/
  accounting functions. The current food/production/gold/culture/espionage adjustments
  remain bounded under shipped values; no independent producer/contract failure was
  established in this first inherited pass.

CURRENT STATE
-------------
Queue 001 CvPlayerAI.cpp remains OPEN (deep-method re-loop).
Unique completed files: 30 / 169.
Confirmed new re-loop roots: F321-F329 / KI#644-KI#652.
Existing KI#312 remains incompletely repaired and is not renumbered.
Existing cross-file F212/KI#535 was encountered in AI_unitUpdate and is not duplicated.
NEXT ROOT ID: F330 / provisional KI#653 -- UNUSED.

Exact continuation cursor:
- continue independent inherited pass through financial state / gold targeting and
  research-tech selection helpers, then attitude/diplomacy valuation;
- prioritize state-inversion, prospective-vs-current, saturation/clamping, denominator,
  and delayed-legality contracts rather than subjective heuristic weights;
- revisit already saturated SAS-delta functions only if an inherited caller exposes a
  new contract.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP15 -- QUEUE 001 DEEP RE-LOOP: AI_bestTech BARBARIAN canResearch BYPASS
CvGameCoreDLL/CvPlayerAI.cpp
Continuation after C031-WIP14
===============================================================================

F330 / provisional KI#653 -- CONFIRMED
---------------------------------------
AI_bestTech can select a technology that AdvCiv explicitly forbids Barbarians from
researching, allowing ordinary Barbarian commerce research to innovate a technology before
any civilization knows it and to enter normal first-to-tech processing.

ROOT / CONTRACT
---------------
AdvCiv's CvPlayer::canResearch contains an explicit advc.307 rule:

    // <advc.307> Don't allow Barbarians to innovate
    if (isBarbarian() && kGame.countKnownTechNumTeams(eTech) <= 0)
        return false;

The dedicated CvTeam::doBarbarianResearch catch-up path enforces the same intended model by
making no progress on a technology when no living civilization team knows it. Barbarian
technology acquisition is therefore intended to catch up to civilization knowledge, not to
be the first research source for an otherwise unknown technology.

AI_bestTech does not enforce that player-level legality contract during ordinary AI tech
selection. Its candidate loop filters with canEverResearch rather than canResearch. Its
final advc.126 canResearch recheck is conditional on:

    isResearch() && getAdvancedStartPoints() >= 0

and was introduced for scenario / Advanced-Start prerequisite anomalies. In an ordinary game,
getAdvancedStartPoints() is negative, so the selected depth-0 technology is returned even when
CvPlayer::canResearch would reject it solely because the Barbarian team would be innovating.

REACHABLE PRODUCER
------------------
This is not a dead helper path for Barbarians:
* AI_doTurnPre runs AI_doResearch for every non-human player, including Barbarians.
* AI_doResearch invokes AI_chooseResearch whenever the player has no current research target
  and isResearch() is true.
* A Barbarian player becomes research-capable after founding its first city; isResearch()
  is based on founded-first-city and anarchy state and does not exclude Barbarians.
* AI_chooseResearch explicitly gives Barbarians iResearchDepth=1 and calls AI_bestTech.
* Current commerce XML initializes COMMERCE_RESEARCH to 100%. AI_doCommerce returns early for
  Barbarians, so it does not deliberately zero that research allocation.
* Even if the calculated research rate were zero, CvPlayer::doResearch contributes at least
  one beaker per turn to an already-selected technology through max(1, calculateResearchRate()).

Once AI_bestTech returns an otherwise prereq-legal but globally unknown technology,
pushResearch again checks only canEverResearch and recursively queues ordinary prerequisites;
it does not run canResearch. Research completion reaches CvTeam::setResearchProgress and then
setHasTech(..., bFirst=true) without a final canResearch revalidation.

CURRENT CONSEQUENCE
-------------------
A Barbarian city can therefore research and become the first team to discover a technology
that advc.307 explicitly says Barbarians must not innovate. This can alter technology pacing
and the set of technologies available to the normal contact-weighted Barbarian catch-up system.

The consequence can also enter normal first-discovery rewards. CvTeam::setHasTech computes
bFirstToDiscover from the current known-team count and, when true, executes ordinary religion
founding and first-to-tech reward logic. Current SAS still has multiple religion-founding
technologies, and TECH_THEORY_OF_EVOLUTION currently awards one first free technology.
CvPlayer::foundReligion contains no Barbarian exclusion. Thus a Barbarian innovation can in
principle found a religion and, for an appropriate first-reward technology, trigger an
additional free-tech choice that itself uses AI_bestTech.

ANCESTRY / DUPLICATE CHECK
--------------------------
K-Mod already used canEverResearch in AI_bestTech and ordinary AI research. AdvCiv added the
advc.307 anti-innovation rule to CvPlayer::canResearch and to its dedicated Barbarian-research
logic, but did not propagate that new legality rule into AI_bestTech/pushResearch for the
ordinary Barbarian AI path. The later advc.126 final canResearch loop was scoped to the
Advanced-Start/scenario prerequisite problem and therefore does not close this ordinary-game
Barbarian hole.

Known Issues contains KI#408, another inherited AdvCiv Barbarian-research defect, but KI#408 is
about geographic-contact weighting becoming global in the Ancient era. It does not cover the
ability of AI_bestTech / pushResearch to bypass the anti-innovation canResearch rule. No prior
numbered album root found for this contract.

REPAIR DIRECTION
----------------
The narrowest repair is to make the Barbarian ordinary research-selection path obey the same
canResearch rule as advc.307, e.g. reject AI_bestTech depth-0 candidates for which canResearch
is false (at minimum for Barbarians), and defensively avoid queueing an illegal returned target.
A broader AI_bestTech final legality recheck may be desirable, but must preserve the scenario
handling and any intentional callers that evaluate techs hypothetically/free-of-current-state.
Do not simply remove advc.307: CvTeam::doBarbarianResearch and its comments demonstrate that
catch-up-with-known-civs is deliberate current behavior.

AI_bestTech SCENARIO QUESTION -- RECLASSIFIED
---------------------------------------------
The original C031-WIP14 question about advc.126's Earth1000AD / Advanced-Start condition did
not itself need a separate finding in this checkpoint. The deeper trace instead established
the live Barbarian canResearch bypass above. The unusual scenario condition should remain a
separate hardening/history question unless a current scenario producer is proven; do not
inflate F330 to claim all AI_bestTech legality mismatches.

CURRENT STATE
-------------
Queue 001 CvPlayerAI.cpp remains OPEN (deep-method re-loop).
Unique completed files: 30 / 169.
Confirmed new re-loop roots: F321-F330 / KI#644-KI#653.
Existing KI#312 remains incompletely repaired and is not renumbered.
NEXT ROOT ID: F331 / provisional KI#654 -- UNUSED.

Exact continuation cursor:
- finish inherited financial/research-selection helpers around AI_bestTech and the nearby
  research ranking/cache consumers without duplicating F330;
- then continue into inherited attitude / diplomacy / trade valuation;
- keep the producer -> violated contract -> current consequence threshold and do not promote
  heuristic disagreement alone.

No repository source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP16 -- QUEUE 001 DEEP RE-LOOP: INHERITED DIPLO/TRADE + KI#83 MARKET POPULATION
Date: 2026-08-30
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayerAI.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 001 CvPlayerAI.cpp remains OPEN under the deeper WIP-era re-audit.
- Unique file progress remains 30/169 because queue 001 is a re-loop of an already-completed early queue.
- F330 / KI#653 remains confirmed from WIP15.
- New root in this checkpoint: F331 / provisional KI#654.
- Next root number: F332 / provisional KI#655 remains UNUSED.
- No repository source files were modified.

INHERITED ATTITUDE / IDEOLOGY INCREMENTAL-CACHE CONTROL
-------------------------------------------------------
AI_updateIdeologyAttitude has an odd post-advc.130n shape:

    if (!isMajorCiv() || GET_PLAYER(kCity.getOwner()).isMajorCiv())
        return;

Current ideology popularity, however, is based on revealed cities of known MAJOR civilizations. This means the incremental helper now returns for the major-owned cities that actually contribute to that popularity population, while proceeding for non-major-owned cities that do not contribute to it. The helper therefore looks largely obsolete/inverted relative to the newer popularity formula.

This was NOT promoted to a finding in this pass because the obvious gameplay consequence is repaired by full cache rebuilds at the important decision boundaries:
- CvTeamAI::AI_doTurnPost refreshes attitude state;
- CvPlayerAI::AI_doTurnPost calls AI_updateExpansionistHate(), and that helper finishes with AI_updateAttitude(), immediately before AI_doDiplo;
- a number of ownership/team/revival transitions also explicitly rebuild affected attitudes.

A narrower mid-turn UI/contact/map-reveal stale-cache manifestation has not been proven. Keep the helper as cleanup/obsolete-code debt unless a concrete consumer is later found. Do not spend an F-number merely because the guard no longer mirrors the formula.

KI#108 PURE-VASSAL EARLY ACCEPT -- SHIPPED REVERSE DIRECTION CONTROL
--------------------------------------------------------------------
Current SAS adds a human-facing early return in AI_considerOffer for a pure one-item Vassal/Surrender offer on either side. Raw source therefore looks dangerous in the reverse direction: a human offering itself as vassal to an AI could theoretically bypass the later master-side AI war/acceptance checks.

That reverse shape is NOT supported in the shipped configuration:
- Assets/XML/BBAI_Game_Options_GlobalDefines.xml has BBAI_HUMAN_AS_VASSAL_OPTION = 0;
- the XML comment explicitly says the BBAI default was 0, K-Mod used 1, and the option is "Not supported in AdvCiv";
- CvPlayer::canPossiblyTradeItem rejects a human offering Vassal/Surrender to an AI when that option is disabled.

The supported KI#108 direction is the AI offering its own Vassal/Surrender item to the human, which is the inconsistency the repair was designed to remove. Therefore the symmetric source predicate is an unsupported-configuration caveat, not F331.

KI#83 MINIMUM-FLOOR UNIT LABEL -- DOCUMENTATION, NOT GAMEPLAY ROOT
-----------------------------------------------------------------
The widely-known-tech feature documents its floor as a "Minimum Gold Floor":

    iMinGold = (GameTurn * MULT + ADD) / DIV

and stores it directly in iTechVal, whose units are diplomacy trade value. AI_goldTradeValuePercent() currently returns 200, so 1 gold equals 2 trade-value units. Read literally, an XML/comment example of a 50 "gold" floor would therefore correspond to only 25 actual gold.

However, KI#83's canonical design text says a technology worth roughly 200 gold should opportunistically be sold for roughly 20-30 gold when rivals can beat the AI to the sale. At turn 100, the current floor is 50 trade-value units = 25 actual gold under the fixed 200% gold conversion, matching that stated gameplay target closely. The percentage discount itself is dimensionally safe because the conversion is linear.

Conclusion: the comments/XML call trade-value units "gold" too loosely. This is documentation/unit-label debt, not a defensible current gameplay defect.

F331 / provisional KI#654 -- SAS KI#83 widely-known-tech fire-sale trigger counts DEAD / historical teams as live market competitors
------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
KI#83's explicit design goal is to make the AI sell a technology cheaply once one or more RIVALS also know it, because a rival can beat the AI to the sale and leave it with zero gold.

Current AI_dealVal triggers the aggressive discount with:

    if (bSAS_AI_DEAL_VAL_TECH_TRADE_CHEAPLY_OPTIMIZE &&
        kGame.countKnownTechNumTeams(eTech) >= 2)

But CvGame::countKnownTechNumTeams is deliberately a historical/global helper:

    for (TeamIter<EVER_ALIVE> itTeam; itTeam.hasNext(); ++itTeam)
        if (itTeam->isHasTech(eTech))
            iCount++;

It therefore counts eliminated teams, not only teams that remain able to trade the technology.

CURRENT PRODUCER
----------------
1. Living seller team A knows technology T.
2. Rival team B also learns T. The global count becomes at least 2, so KI#83's fire-sale state is initially justified.
3. B is later eliminated.
4. CvPlayer::setAlive(false) merely decrements the team's alive count. CvTeam::changeAliveCount tears down wars/vassal state, but it does NOT clear m_abHasTech. B remains an EVER_ALIVE team and continues to report isHasTech(T).
5. A later values selling T to living buyer C.
6. countKnownTechNumTeams(T) still returns at least 2 from A + dead B, so the SAS 25% fire-sale discount remains active even though B can no longer sell anything to C.

CONSEQUENCE
-----------
The discount can persist for the rest of the game after the only competing technology holder is eliminated. A technology that has returned to an effective living monopoly can therefore still be priced at the configured widely-known fire-sale percentage/floor.

This directly contradicts KI#83's stated economic rationale: the discount is meant to react to a competitor who can beat us to the sale. An eliminated team is not a market competitor.

The mismatch can be large. If normal AI_techTradeVal corresponds to about 200 gold, the KI#83 25% layer can reduce the effective asking value toward about 50 gold (subject to the turn floor) merely because a dead historical team once knew the technology.

STRONG NEGATIVE CONTROL IN THE SAME CODEBASE
--------------------------------------------
The ordinary K-Mod/AdvCiv known-tech valuation does NOT use this historical population. CvTeamAI::AI_knownTechValModifier iterates:

    TeamIter<CIV_ALIVE, OTHER_KNOWN_TO>

and therefore naturally stops treating eliminated teams as current known-tech competitors. Current SAS has even repaired that helper further (KI#360) to keep its numerator/denominator populations consistent for capitulated teams.

So this is not an engine convention that technology-market valuation should use EVER_ALIVE teams. The SAS KI#83 shortcut chose a helper whose historical semantics are appropriate for things such as first-discovery history, but not for current competitive-sale pressure.

ANCESTRY
--------
- Base AdvCiv and K-Mod do not contain the SAS KI#83 fire-sale block.
- The feature was introduced in AdvCiv-SAS commit 632163dc4b360293e2cf7eaf389bacc843bc44e1 (2025-12-08), documented as "Trade more aggressively and much cheaper any tech once 1+ rivals also discover it."
- KI#83 repeats the intent: sell cheaply when rivals discover the tech because "other rivals can profit" and may beat us to the sale.
- CvGame::countKnownTechNumTeams itself is older/general infrastructure and intentionally uses EVER_ALIVE; the defect is the SAS feature using that historical count as a live-market predicate.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier album root found in the current duplicate sweep owns "dead historical technology holders keep KI#83 competitive fire-sale mode active."
- KI#83 is the feature being audited, not an already-diagnosed dead-team defect.
- KI#360 concerns a different numerator/denominator population mismatch inside AI_knownTechValModifier and has already been repaired.
- KI#416 / KI#418 concern dead teammate persistent technology effects / Tech Share thresholds, not technology-sale pricing.
- F330 / KI#653 concerns Barbarian canResearch anti-innovation bypass, not trade-market population.
- KI#312 concerns legal internal supply / sourcing locus, not whether a dead external historical owner creates market competition.

REPAIR DIRECTION
----------------
Do not use the historical count directly as the competitive-sale trigger.

Narrow minimum repair:
- count only currently alive civilization teams that know eTech, plus the seller/current holder as appropriate.

Stronger contract-faithful repair:
- ask whether at least one OTHER living team is a plausible competing source for the intended recipient rather than merely whether some global team knows the technology;
- at minimum exclude dead teams; optional later refinement can consider contact/trading/no-brokering legality if KI#83 is intended to model actual undercut risk rather than broad world diffusion.

The existing AI_knownTechValModifier is useful evidence for the alive-team population but should not be copied blindly because its purpose/known-to perspective differs from KI#83's explicit sale-competition trigger.

CURRENT QUEUE-001 STATE AFTER WIP16
-----------------------------------
Confirmed new deep-reloop roots:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654

Existing cross-file / historical state retained:
- old F001/KI#330 repaired;
- old F002/KI#331 repaired;
- KI#312 remains incompletely repaired through the independent eBestProgressTech outsider gold-purchase fallback;
- F212/KI#535 encountered in AI_unitUpdate and deliberately not duplicated.

F332 / provisional KI#655 remains UNUSED.

EXACT CONTINUATION CURSOR
-------------------------
1. Continue AI_considerOffer / AI_dealVal cancellation and side-identity audit after the KI#108 and KI#83 controls/root above.
2. Challenge annual/dual/vassal cancellation paths for prospective-state or side-direction mistakes; fold resource prospective-state consequences into F321 when they share that root.
3. Continue inherited attitude/diplomacy/trade-value functions; keep AI_updateIdeologyAttitude as an unnumbered stale-cache/obsolete-hook control unless a concrete mid-turn consumer is established.
4. Reconcile original queue-001 coverage as each inherited function is crossed so later-deep findings are not duplicated.
5. Freeze another WIP before widening into the next large function tranche.



QUEUE 001 DEEP RE-AUDIT — WIP17 ADDENDUM
=========================================

AI_FOLDDEALS MATURE-DEAL REPLACEMENT — F332 / provisional KI#655 CONFIRMED
--------------------------------------------------------------------------

F332 / provisional KI#655 -- inherited AdvCiv AI_foldDeals readability fold restarts the mandatory no-cancel term of already-mature AI-AI resource deals
-----------------------------------------------------------------------------------------------------------------------------------------

ROOT / INTENDED CONTRACT
------------------------
AdvCiv change advc.036 introduced AI_foldDeals as a usability/readability transformation. Its introducing commit says:

    "When an AI civ both buys resources from and sells resources to another AI civ,
     the AI tries to cancel those deals and replace them with resource-for-resource
     trades. This should make the AI trades easier to read on the Foreign Advisor."

The function only selects deals that are already cancelable:

    if(!d->isCancelable())
        continue;

and only the narrow mature AI-AI GPT-for-one-resource shape. It pairs opposite-direction deals, kills both silently, and reconstructs an economically equivalent resource-for-resource deal plus the net GPT difference.

The transformation is therefore initiated after the original contractual minimum term has already expired and is described as representational/readability cleanup rather than a newly negotiated commitment.

CURRENT PRODUCER / AGE RESET
----------------------------
AI_foldDeals(CvDeal& d1, CvDeal& d2) stores the players and economic terms, then:

    d1.killSilent(false, false);
    d2.killSilent(false, false);
    ...
    GC.getGame().implementAndReturnDeal(eFirstPlayer, eSecondPlayer,
            give1, give2, true);

CvDeal::init unconditionally stamps every newly implemented deal with the current turn:

    setInitialGameTurn(GC.getGame().getGameTurn());

CvDeal::turnsToCancel then computes:

    getInitialGameTurn() + PEACE_TREATY_LENGTH - currentTurn

and isCancelable is false while that value is positive.

Thus every successful fold turns two already-mature, already-cancelable agreements into a fresh agreement that cannot be canceled for another full PEACE_TREATY_LENGTH (10 turns under the normal/current project setting).

GAMEPLAY CONSEQUENCE
--------------------
This is not only UI metadata. CvPlayerAI::AI_doDeals starts its cancellation/renegotiation pass with:

    if (!pLoopDeal->isBetween(getID(), eOther) ||
        !pLoopDeal->isCancelable(getID()))
        continue;

So the replacement deal is completely excluded from normal AI cancellation review throughout the renewed lock. During those turns the AI cannot respond through AI_checkCancel to changed resource need, changed denial/attitude state, changed valuation, worst-enemy state, or other ordinary reasons that would have allowed the mature constituent deals to be canceled immediately before the fold.

The fold can therefore extend an unwanted resource export/import or net GPT payment for another treaty-length solely because AdvCiv rewrote the representation for readability.

SOURCE SELF-CONFIRMATION
------------------------
The original implementation itself leaves the exact missing semantic restoration directly below implementAndReturnDeal:

    // Allow new deal to be canceled right away?
    /*if(pNewDeal != NULL)
        pNewDeal->setInitialGameTurn(
            GC.getGame().getGameTurn() - GC.getPEACE_TREATY_LENGTH());*/

This is unusually strong evidence that the age reset was noticed as a consequence of the representation fold rather than deliberately specified as a renewed bargain. The code currently discards the returned CvDeal pointer, so the proposed correction remains disabled.

ANCESTRY
--------
- Stock BtS / Civ4CE / K-Mod do not have AI_foldDeals in this form.
- AdvCiv introduced it in commit 2989cda6e0a97c60bd15d38a45913209af8c701e (2018-02-22) as advc.036, explicitly under "Usability improvements for AI resource trades" and with the Foreign Advisor readability rationale above.
- Base AdvCiv 1.14 still contains the same current-turn replacement and the same commented "Allow new deal to be canceled right away?" repair.
- AdvCiv-SAS inherits the behavior unchanged.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier album root found in the current duplicate sweep owns "AI_foldDeals restarts the mandatory cancellation age of mature deals."

Relevant but distinct roots/repairs:
- F321/KI#644 concerns prospective iChange resource valuation when a cancellation is actually evaluated; F332 prevents that evaluation from occurring at all for the renewed lock period.
- F296/KI#619 concerns annual-deal attitude-cache update orientation/timing, not deal maturity.
- KI#241 concerns UI representation of mixed resource/GPT deals and confirms such mixed deals are supported, but does not cover age reset.
- Existing deal-age/prolongation findings in CvDeal concern peace/dual agreement lifetime bookkeeping, not this AI-only representational folding step.

REPAIR DIRECTION
----------------
Minimum repair: retain the returned CvDeal* and make the folded replacement immediately cancelable instead of stamping a fresh mandatory term.

A more semantically faithful repair should also preserve meaningful age rather than merely backdating by exactly one treaty length, because AI cancellation willingness itself can depend on deal age. Before killing d1/d2, save their initial turns and assign the replacement an inherited age. A conservative single-age representation would use the younger of the two constituent agreements (max(initialTurn1, initialTurn2)); both inputs are already cancelable, so the replacement remains immediately cancelable while not pretending that the combined arrangement is older than either constituent.

This stronger form preserves the readability fold without changing contractual freedom merely because two deal objects were normalized into one.

CURRENT QUEUE-001 STATE AFTER WIP17
-----------------------------------
Confirmed new deep-reloop roots:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655

Existing cross-file / historical state retained:
- old F001/KI#330 repaired;
- old F002/KI#331 repaired;
- KI#312 remains incompletely repaired through the independent eBestProgressTech outsider gold-purchase fallback;
- F212/KI#535 encountered in AI_unitUpdate and deliberately not duplicated.

F333 / provisional KI#656 remains UNUSED.

EXACT CONTINUATION CURSOR
-------------------------
1. Continue AI_checkCancel / AI_considerOffer mixed annual and dual-deal audit, especially whether a dual item at the head can suppress resource-specific cancellation checks in a supported mixed deal.
2. Keep F321 separate: prospective resource valuation errors belong there; F332 is the independent deal-age lock introduced by folding.
3. Continue inherited trade denial/value helpers and attitude/diplomacy functions after cancellation saturation.
4. Reconcile each crossed function against original queue-001 coverage and existing later roots before assigning F333.
5. Freeze another WIP before widening into the next large inherited tranche.


===============================================================================
QUEUE 001 DEEP RE-AUDIT — WIP18 ADDENDUM
=========================================

F333 / provisional KI#656 -- AI_checkCancel treats a dual item at the head of a mixed annual bundle as the whole deal and skips later resource/dual cancellation denials
-----------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
AdvCiv's advc.133 cancellation refactor explicitly separates two kinds of checks:

1. AI_considerOffer(..., iChange=-1) decides whether the *aggregate economics* of the ongoing deal are still acceptable.
2. Because aggregate value is insufficient for resource cancellation policy, AI_checkCancel then explicitly re-runs cancellation-specific resource denial through AI_bonusTrade(..., iChange=0).

The source comment says this directly:

    Resource trades do need special treatment.
    For duals (Open Borders, Def. Pact, Perm. Alliance), getTradeDenial
    is all right.

But the implementation does not classify every item. It inspects only the first outgoing item:

    pNode = kDeal.getGivesList(getID()).head();
    if (pNode != NULL && CvDeal::isDual(pNode->m_data.m_eItemType, true))
    {
        DenialTypes eDenial = getTradeDenial(ePlayer, pNode->m_data);
        ...
        else return NO_CANCEL;
    }

That unconditional return means a dual item at the head converts the *entire outgoing list* into the dual-only path. The later loops that inspect every outgoing and incoming resource are never reached.

SUPPORTED MIXED-DEAL PRODUCER
-----------------------------
Mixed dual + resource/GPT annual bundles are supported, not a malformed-save construction.

- CvDeal::isAnnual classifies Resources, GPT, Open Borders, Defensive Pact and Permanent Alliance as annual.
- CvPlayer::updateTradeList deliberately allows AI-human peacetime offers to mix items so long as the first offered item and later items share annual/nonannual status. Therefore selecting a dual annual item does not hide Resources/GPT.
- CvDeal::doTurn explicitly documents the engine reality:

      "In multiplayer, dual deals can be mixed with e.g. gold per turn"

  and handles mixed dual deals separately from the one-item pure-dual fast path.
- CvDeal::addTradeItems persists the supplied list order by iterating the offer and insertAtEnd'ing each saved item. There is no deal-layer normalization that extracts dual items into a separate deal.

Therefore a persisted outgoing list whose head is Open Borders/Defensive Pact and which also contains a resource is a supported annual-deal representation. The bug does not require corrupt data or an unsupported item-class mixture.

CLEAN CURRENT REPRODUCTION
--------------------------
Alexander gives a particularly clean current-XML example:

    StrategicBonusRefuseAttitudeThreshold = ATTITUDE_CAUTIOUS
    OpenBordersRefuseAttitudeThreshold    = ATTITUDE_ANNOYED

1. Human H and Alexander A have accessible territory and relations high enough (e.g. Pleased) to establish a mature annual bundle:

       A gives: Open Borders + Iron
       H gives: Open Borders + enough GPT / other annual compensation

   The exact compensation is not important; it only needs to keep the whole deal economically acceptable when reviewed later.

2. After the deal becomes cancelable, A's attitude toward H falls to Cautious, while H is not A's worst enemy and the existing Open Borders still gives territorial access.

3. `AI_considerOffer(...,-1)` can still accept the *aggregate* bundle because H's compensation makes the package sufficiently valuable. Cancellation mode deliberately does not run ordinary per-item `getTradeDenial` for A's outgoing items.

4. Current Open Borders denial remains NO_DENIAL:
   - Cautious is above Alexander's Annoyed OB refusal threshold;
   - territory is accessible;
   - no recent-cancel / worst-enemy condition is required for the reproduction.

5. Current Iron resource denial is different. `AI_bonusTrade(IRON, H, 0)` identifies Iron as strategic from current unit prerequisites and rejects at Cautious because Alexander's strategic-resource refusal threshold is Cautious:

       eAttitude <= iRefusalThresh  -> DENIAL_ATTITUDE

6. If Open Borders is the saved outgoing head, AI_checkCancel enters the dual block. Because OB itself has NO_DENIAL, the function executes:

       else return NO_CANCEL;

   and never reaches the subsequent per-resource loop.

7. The AI therefore continues exporting Iron despite its current resource-trade policy explicitly saying that it should refuse/cancel that export. The identical Iron deal without a dual item at the head reaches AI_bonusTrade(...,0) and is canceled.

WHY AI_considerOffer DOES NOT REPAIR THIS
-----------------------------------------
This is not duplicate economics checking.

The whole reason advc.133 added the later resource loops is that `getTradeDenial` / resource policy and aggregate deal value are different contracts during cancellation. A human can compensate the AI generously enough that `AI_considerOffer` still considers the package fair while the AI nevertheless has a policy reason not to export a strategic/happiness/health resource anymore.

The head-dual early return bypasses that deliberate second contract.

BROADER SAME-ROOT CONSEQUENCE
-----------------------------
The same classifier mistake can hide later dual items too. Human-facing annual bundles may contain more than one dual item; checking only the head dual and returning NO_CANCEL means a later Defensive Pact/Open Borders/Permanent Alliance denial is not consulted either.

The resource case is the strongest reproduction because AdvCiv explicitly added full-list resource cancellation loops and the current source comment says those checks are necessary. Treat later-dual suppression as a same-root extension, not a second finding.

ANCESTRY
--------
- K-Mod cancellation primarily relied on aggregate `AI_considerOffer(...,-1)` and did not contain the current AdvCiv full resource-denial follow-up pipeline.
- AdvCiv advc.133 factored cancellation into AI_checkCancel and added the special per-resource cancellation checks, but placed them behind a head-only dual fast path with an unconditional NO_CANCEL return.
- Base AdvCiv 1.14 and current AdvCiv-SAS contain the same structure; SAS has not introduced this defect locally.

So F333 is an inherited AdvCiv cancellation-classification bug exposed by a supported mixed annual deal shape.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier album root found in the current sweep owns "head dual suppresses later resource/dual cancellation denials."

Distinct nearby roots:
- F321/KI#644: wrong *prospective resource valuation* when a cancellation is actually evaluated. F333 can prevent the resource-specific cancellation evaluation from happening at all.
- F332/KI#655: AI_foldDeals restarts deal maturity and temporarily prevents AI_checkCancel from running at all. F333 applies once a deal is mature and AI_checkCancel does run.
- F281-F287 / queue 042: CvDeal list-orientation / peace / transaction-lifetime problems; none owns AI_checkCancel's first-item whole-list classification.
- KI#241 confirms mixed resource/GPT deal representation but does not cover cancellation policy.

REPAIR DIRECTION
----------------
Do not use the first outgoing item as a whole-deal classifier.

A narrow robust structure is:
1. let AI_considerOffer perform aggregate economics as now;
2. iterate *all* outgoing dual items and evaluate each current getTradeDenial independently (preserving the DP immediate-cancel special case / probabilistic behavior as intended);
3. independently iterate *all* outgoing resources with AI_bonusTrade(...,0);
4. independently iterate all incoming resources for the recipient-side DENIAL_JOKING check;
5. return NO_CANCEL only after every relevant item class has been examined.

If the intended design is that a denial on any dual should govern the whole mixed bundle, that can still be expressed explicitly without preventing resource checks merely because another dual happens to be first.

CURRENT QUEUE-001 STATE AFTER WIP18
-----------------------------------
Confirmed new deep-reloop roots:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655
- F333 / provisional KI#656

Existing cross-file / historical state retained:
- old F001/KI#330 repaired;
- old F002/KI#331 repaired;
- KI#312 remains incompletely repaired through the independent eBestProgressTech outsider gold-purchase fallback;
- F212/KI#535 encountered in AI_unitUpdate and deliberately not duplicated.

F334 / provisional KI#657 remains UNUSED.

EXACT CONTINUATION CURSOR
-------------------------
1. Continue inherited trade-denial/value helpers below AI_checkCancel; inspect current-state vs prospective-state and side-orientation assumptions.
2. Revisit AI_updateIdeologyAttitude only if a concrete mid-turn consumer appears; otherwise retain as obsolete/inverted incremental-cache debt.
3. Continue through remaining attitude/diplomacy helpers, cross-checking original queue-001 coverage and later cross-file roots before assigning F334.
4. Freeze another WIP before widening into the next large inherited tranche.


===============================================================================
QUEUE 001 DEEP RE-AUDIT — WIP19 CORRECTION / SUPERSEDING WIP18 F333
===================================================================

AUDIT CORRECTION — WIP18 F333 IS RETRACTED; F333 RETURNS TO UNUSED
------------------------------------------------------------------
WIP18 promoted a plausible `AI_checkCancel` mixed-dual/resource defect before the persistent-deal creation path had been fully reconciled. The next lifecycle pass invalidates the required current producer.

The raw source shape remains real:

    pNode = kDeal.getGivesList(getID()).head();
    if (pNode != NULL && CvDeal::isDual(pNode->m_data.m_eItemType, true))
    {
        ...
        else return NO_CANCEL;
    }

and would indeed skip later resource checks *if* a current persistent AI deal could contain a non-peace dual item followed by resources/GPT.

CURRENT PERSISTENT REPRESENTATION PREVENTS THAT NORMAL PRODUCER
--------------------------------------------------------------
The missing piece is `CvDeal::startTrade`.

Normal diplomacy items are created through `setTradeItem`, whose default `iData` is 0. For the non-peace dual/team items used by `AI_checkCancel`:

- TRADE_OPEN_BORDERS:
      if (trade.m_iData <= 0)
          startTeamTrade(..., true);
      else bSave = true;

- TRADE_DEFENSIVE_PACT:
      same contract;

- TRADE_DISENGAGE:
      same positive-proxy contract;

- TRADE_PERMANENT_ALLIANCE:
      never saved as a persistent trade item in `startTrade` at all.

`startTeamTrade` constructs its own fresh lists:

    TradeData item(eItem, 1);
    ourList.insertAtEnd(item);
    if (bDual)
        theirList.insertAtEnd(item);
    implementDeal(..., ourList, theirList);

Those generated proxy deals contain only the dual/team item. The outer negotiated deal continues separately and saves resources/GPT, but not the original zero-data Open Borders/DP item.

A repository-wide current-source search found no other DLL producer of positive-data Open Borders/Defensive Pact trade items. `resetDualDeal` only refreshes the lifetime of already-existing proxy deals; it does not merge resource/GPT terms into them. `AI_foldDeals` handles only resource/GPT deals and cannot create a mixed dual bundle.

WHY THE `CvDeal::doTurn` COMMENT IS NOT ENOUGH
----------------------------------------------
`CvDeal::doTurn` still contains the historical comment:

    "In multiplayer, dual deals can be mixed with e.g. gold per turn"

That is useful caution but not sufficient producer proof under the current representation. The current DLL construction path above actively splits normal team-agreement items into dedicated positive-data proxy deals. Without an identified current EXE/network path that supplies an already-positive dual item together with resource/GPT terms to an AI-party persistent deal, the old comment cannot justify numbering the hypothetical mixed-list behavior.

`TRADE_PEACE_TREATY` can genuinely coexist with annual reparations in one persistent deal, but `AI_checkCancel` deliberately calls:

    CvDeal::isDual(item, true)

which excludes peace treaties from this head-dual fast path. Therefore the known mixed peace/reparations representation does not revive WIP18 F333.

RESULT
------
- WIP18's F333 / provisional KI#656 is RETRACTED.
- F333 / provisional KI#656 is UNUSED again.
- The head-dual `AI_checkCancel` structure may deserve defensive hardening against malformed/legacy/custom-created mixed lists, but no supported current producer has been established.
- This correction is exactly the conservative audit standard: an attractive source shape is not a finding without a live representation path.

RELATED CONTROL — AI_stopTradingTradeVal FIRST-LIST OPEN-BORDERS SKIP
---------------------------------------------------------------------
The same representation reconciliation also prevents a tempting duplicate/new root in `AI_stopTradingTradeVal`:

    if (pLoopDeal->isPeaceDeal() ||
        pLoopDeal->getLengthFirst() > 0 &&
        pLoopDeal->getFirstList().head()->m_data.m_eItemType == TRADE_OPEN_BORDERS)
        continue;

For current normally-created Open Borders state, the persistent OB proxy is a dedicated pure dual deal, so skipping that proxy after pricing the team-level OB relationship is coherent. No current mixed OB+resource persistent producer has been proved here either.

Existing queue-042 F294/F295 remain the real independent `AI_stopTradingTradeVal` defects:
- F294: team-scoped ordinary-deal inventory where the embargo is player-scoped;
- F295: repeated team-level OB/DP loss across multi-member war-bribe member sums.
Do not create another root from the head-OB skip without a current mixed persistent producer.

CURRENT QUEUE-001 STATE AFTER WIP19 CORRECTION
----------------------------------------------
Confirmed new deep-reloop roots remain:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655

F333 / provisional KI#656 — UNUSED.

EXACT CONTINUATION CURSOR
-------------------------
1. Continue inherited `AI_considerOffer` / `AI_dealVal` / trade-denial-value audit below the now-closed dual representation question.
2. Cross-reference `AI_stopTradingTradeVal` / `AI_stopTradingTrade` against already-confirmed queue-042 F294/F295/F285 rather than duplicating those roots.
3. Inspect `AI_civicTradeVal` / `AI_religionTradeVal` and corresponding denial functions for current-state vs requested-state sign/side mismatches.
4. Continue down the remaining inherited trade/attitude helpers; assign F333 only if a new independent supported root survives.
5. Freeze another WIP before widening into the next large inherited tranche.


================================================================================
QUEUE 001 DEEP RE-AUDIT — WIP20
CvPlayerAI.cpp — inherited trade-value / resource-denial continuation
================================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 001 CvPlayerAI.cpp remains OPEN.
Unique file progress remains 30 / 169.

New deep-reloop roots currently confirmed:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655
- F333 / provisional KI#656  <-- CONFIRMED IN THIS WIP, replacing the retracted WIP18 candidate

F334 / provisional KI#657 — UNUSED.

No repository source files were modified.


CONTROL — AI_civicTradeVal / AI_religionTradeVal payer-side multiplier is intentional
-------------------------------------------------------------------------------------
The final AdvCiv multipliers initially looked like a PlayerTypes orientation mistake:

    // This CvPlayer asks ePlayer to switch to eCivic.
    ...
    if (!isCivic(eCivic))
        iValue *= 2;

and:

    if (getStateReligion() != eReligion)
        iValue *= 2;

The target whose switching cost is otherwise evaluated is ePlayer / kPlayer, whereas
these final conditions deliberately inspect `this` (the requester/payor).

This is NOT a side-identity defect. AdvCiv history explicitly confirms the intended
contract. The 2024-10-27 advc.132 UI commit says the espionage cost breakdown should
warn that the cost is increased when the spy owner does not practise the chosen civic
or religion, and emphasizes that allowing a player to choose a civic/religion other
than its own is deliberate AdvCiv behavior rather than a bug.

That maps exactly to these payer-side conditions. `AI_civicTradeVal` and
`AI_religionTradeVal` are also reused by the force-civic / force-religion espionage
valuation path, where `this` is the spy owner and ePlayer is the target. Therefore no
F333 arises from the apparent requester/target asymmetry.


F333 / provisional KI#656 — AI_bonusTrade's "disregard obsolete units" filter runs after strategic classification and therefore has no effect
------------------------------------------------------------------------------------------------------------------------------------------

CLASSIFICATION
--------------
Independent inherited AdvCiv / Better-AI logic defect in CvPlayerAI::AI_bonusTrade.
Current SAS data provides a direct gameplay consequence. Not a tuning disagreement.

ROOT CAUSE
----------
`AI_bonusTrade` tries to classify a resource as strategically important only while it
still enables relevant military units. The imported Better-AI fragment says:

    // <advc.001> Fuyu, Better AI: Strategic For Current Era, 22.07.2010
    // disregard obsolete units
    if (pCapital != NULL && pCapital->allUpgradesAvailable(eUnit) != NO_UNIT)
        continue;

But the function has already inspected the same unit's resource requirements and set
`bStrategic` / `bCrucialStrategic` BEFORE this test:

    if (GC.getInfo(eUnit).getPrereqAndBonus() == eBonus)
    {
        bStrategic = true;
        bCrucialStrategic = true;
    }
    for (... prereq-or bonuses ...)
    {
        if (... == eBonus)
        {
            bStrategic = true;
            bCrucialStrategic = true;
        }
    }

    // disregard obsolete units
    if (pCapital != NULL && pCapital->allUpgradesAvailable(eUnit) != NO_UNIT)
        continue;

There is no executable code after that `continue` inside the unit loop. Consequently,
for strategic classification the supposed obsolete-unit filter is a complete no-op.
Once any historical unit has ever required eBonus, that unit can set `bStrategic=true`
even when all of its upgrades are available and the unit is obsolete for this civ.

The placement is especially conspicuous because `AI_baseBonusUnitVal`, elsewhere in
this same file, performs the equivalent `allUpgradesAvailable` test as an actual value
filter and sets an obsolete unit's bonus value to zero.

CURRENT-DATA REPRODUCTION — COPPER
---------------------------------
Copper is a clean current SAS example because:

1. Current CIV4UnitInfos.xml has Copper as an AND/OR requirement for historical units
   including Axemen/Spearmen/Macemen and their UUs, Houfnice and Privateer.
2. Every current Copper-consuming unit has a later upgrade path. Examples:
   - Axeman / early UUs -> Maceman
   - Maceman / Samurai / Viking / Zhuge Nu / Disciplined Crossbowman -> Musketman
   - Spearman / Hoplite -> Pikeman
   - Houfnice -> Field Gun
   - Privateer -> Attack Submarine
3. At sufficiently advanced technology, `pCapital->allUpgradesAvailable(eUnit)` can
   therefore be non-NO_UNIT for every Copper-consuming unit.
4. Current CIV4BuildingInfos.xml has no building whose PrereqAndBonus or
   PrereqOrBonuses requires Copper. (Iron Works requires Coal + Iron, not Copper.)
5. Copper has 0 happiness and 0 health, so if it is no longer military/building
   strategic, no happiness/health attitude denial substitutes for the erroneous
   strategic one.
6. `AI_corporationBonusVal(Copper)` can be zero for an AI that currently has none of
   the Copper-using corporations in its cities.

A concrete late-game trade therefore reaches the bad behavior:

- AI Alexander has at least two available Copper copies, no Copper-using corporation,
  and has reached tech/unit availability where all current Copper military uses have
  available upgrades.
- Human H has no Copper.
- Alexander is Cautious toward H.
- Current XML gives Alexander
  `StrategicBonusRefuseAttitudeThreshold = ATTITUDE_CAUTIOUS`.
- `getTradeDenial(H, TRADE_RESOURCES:Copper)` calls
  `AI_bonusTrade(Copper, H, iChange=1)`.

Intended/current-era classification:
- every Copper-consuming unit is filtered as obsolete/upgraded;
- no building reclassifies Copper as strategic;
- Copper supplies neither happiness nor health;
- an extra copy with no corporation value can therefore proceed to the ordinary
  value checks without a strategic-attitude veto.

Actual classification:
- the first historical Copper unit sets `bStrategic=true` before the obsolete test;
- every later `continue` is too late to undo that state;
- `eAttitude <= StrategicBonusRefuseAttitudeThreshold` is true at Cautious;
- the AI returns `DENIAL_ATTITUDE` and refuses the export solely because obsolete
  military units are still counted as current strategic uses.

ANCESTRY
--------
Stock/Civ4CE and K-Mod classify strategic resources from historical unit/building
requirements without this current-era filter. AdvCiv imported the Better-AI/Fuyu
"Strategic For Current Era" repair (`advc.001`) and added `allUpgradesAvailable`, but
placed it after the state mutation it is supposed to guard. Base AdvCiv 1.14 and
current SAS retain the same ordering unchanged.

This is therefore best described as an INCOMPLETE / NONFUNCTIONAL AdvCiv repair: the
source explicitly says obsolete units should be disregarded, but the inserted control
cannot affect the strategic-classification result.

CONSEQUENCE
-----------
Resources whose military role has ended can remain subject to strategic-resource
attitude refusal indefinitely. That can hide/refuse otherwise reasonable surplus
exports and reduces late-game resource trading. Copper gives a current shipped-data
reproduction; the flaw is generic to any unit-resource whose current strategic uses
all become obsolete while no still-relevant building reclassifies it.

REPAIR DIRECTION
----------------
Filter the unit before mutating `bStrategic` / `bCrucialStrategic`, e.g. conceptually:

    FOR_EACH_ENUM2(Unit, eUnit)
    {
        if (pCapital != NULL &&
            pCapital->allUpgradesAvailable(eUnit) != NO_UNIT)
        {
            continue;
        }
        // only now inspect AND/OR bonus requirements
        ...
    }

Then retain the existing building pass so a resource with no current unit role can
still be classified strategic when a building genuinely requires it.

A narrower alternative is to compute per-unit `bUsesBonus`, perform the obsolete test,
and only then OR that surviving unit into the aggregate flags. Do not merely clear
`bStrategic` on an obsolete unit, because an earlier/later nonobsolete unit may have
already established a legitimate strategic use.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier numbered album root covers this obsolete-unit ordering failure.

- F321/KI#644 concerns prospective `iChange` in the *value* multiplier for strategic
  resources during cancellation.
- KI#239/KI#240 concern SAS hard dominated-resource buy denials and substitutes.
- F333 concerns the inherited *classification* used by `AI_bonusTrade` refusal policy:
  the intended current-era obsolete-unit filter is placed after its side effect.

The earlier WIP18 F333 (mixed dual/resource `AI_checkCancel`) remains retracted. The
number F333 / provisional KI#656 is now reused for this independently proved root.


OTHER TRADE-HELPER CONTROLS IN THIS SLICE
-----------------------------------------
- `AI_checkResourceLimits`'s `getNumAvailableBonuses - iChange` orientation is
  coherent for both acquisition (`+1`) and cancellation (`-1`): cancellation adds
  the exported copy back when asking whether it was non-surplus.
- `AI_civicTradeVal` / `AI_religionTradeVal` requester-side x2 is deliberate advc.132
  policy as documented above.
- `AI_stopTradingTradeVal` / `AI_stopTradingTrade` remain cross-referenced to existing
  queue-042 F285/F294/F295; no duplicate root is allocated here.


CURRENT CONTINUATION CURSOR
---------------------------
1. Continue inherited `AI_bonusTrade` after F333 and challenge the remaining
   value/attitude/current-state branches for independent defects.
2. Continue `AI_considerOffer` / `AI_counterPropose` / trade balancing, including the
   old Feb-2020 `FAssert(bPlayerHuman)` demand path, but require a supported AI-AI
   producer and consequence before allocating F334.
3. Continue downward through unit/trade-adjacent inherited helpers and then the next
   major inherited tranche.
4. F334 / provisional KI#657 remains UNUSED.
5. Freeze another WIP before moving far beyond this trade section.


================================================================================
QUEUE 001 DEEP RE-AUDIT — WIP21
CvPlayerAI.cpp — resource-classification closure / same-team cancellation continuation
================================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 001 CvPlayerAI.cpp remains OPEN.
Unique file progress remains 30 / 169.

New deep-reloop roots currently confirmed:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655
- F333 / provisional KI#656
- F334 / provisional KI#657  <-- CONFIRMED IN THIS WIP

F335 / provisional KI#658 — UNUSED.

No repository source files were modified.


CONTROL — AI_bonusTrade building scan does not currently reproduce an F334 analogue of F333
------------------------------------------------------------------------------------------------
WIP20 left one deliberately unresolved question after the obsolete-unit ordering bug:
`AI_bonusTrade` also scans every building prerequisite when deciding whether a resource is
strategic, and that building scan has no current-constructibility / obsolescence filter.

The current SAS XML closes the obvious gameplay reproduction. CIV4BuildingInfos.xml contains
only one building with a hard bonus prerequisite:

    BUILDING_IRON_WORKS
        PrereqAndBonus = BONUS_COAL
        PrereqOrBonuses = BONUS_IRON
        ObsoleteTech = NONE

Iron Works therefore never becomes an obsolete historical building prerequisite, and both Iron
and Coal retain independent strategic uses. Copper — the clean F333 resource — is not required by
any building at all.

Unlike the unit loop, this building loop also carries no source comment or imported repair contract
saying obsolete buildings are meant to be disregarded. A broader design improvement could still
make the classification more current-state-aware, but current shipped data does not support a
separate bug root. F334 is not spent here.


CONTROL — AI_bonusTradeVal's human/AI wording is reversed in the inline comment, not in code
-----------------------------------------------------------------------------------------------
Another apparent orientation mismatch dies against AdvCiv history.

Current code applies the second-health/second-happiness 0.75 adjustment under:

    if (!isHuman() && kFromPlayer.isHuman())

The immediately following comment says this is for "a human civ receiving two health resources or
two happiness resources from an AI civ", which is the opposite orientation.

However, the AdvCiv changelog that introduced this advc.036 behavior explicitly says:

    "Improved the AI evaluation of deals where the AI receives two health resources or two
     happiness resources from a human. (The AI still refuses to trade more than two.)"

That matches the executable condition exactly: `this` is the AI recipient/buyer and
`kFromPlayer` is the human giver/seller. The inline comment is stale/reversed documentation; the
implementation is not a side-identity defect.


F334 / provisional KI#657 — AdvCiv's same-team cancellation exemption reads an uninitialized-to-policy sentinel and is permanently dead for bilateral deals
---------------------------------------------------------------------------------------------------------------------------------------------------

CLASSIFICATION
--------------
Independent inherited AdvCiv logic defect in `CvPlayerAI::AI_considerOffer`, introduced by the
advc.155 human-AI team trade policy. The source contains an explicit same-team cancellation
exemption, but its threshold is never computed on the bilateral-deal path that reaches it.

ROOT CAUSE
----------
After computing both sides' deal values, AdvCiv declares:

    int iThreshold = -1; // advc.155: Declaration moved up

The only assignment that turns `iThreshold` into the help/gift allowance occurs inside the
one-sided-gift branch:

    if (iTheyReceive > 0 && kTheyGive.getLength() == 0 && iWeReceive == 0)
    {
        ...
        iThreshold = kOurTeam.AI_getHasMetCounter(kPlayer.getTeam()) + 50;
        iThreshold *= 2;
        if (bSameTeam)
            iThreshold *= 5;
        ...
        iThreshold -= kPlayer.AI_getPeacetimeGrantValue(getID());
        ...
        return bAccept;
    }

That branch always returns. Therefore every bilateral deal proceeds with `iThreshold == -1`.

Later, during cancellation (`iChange < 0`), advc.155 adds a same-team special case:

    if (bSameTeam)
    {
        if (iTheyReceive < iThreshold)
            return true;
        iTolerance = 150;
    }

For ordinary nonnegative deal values, `iTheyReceive < -1` is impossible. The explicit threshold
exemption is therefore dead code on exactly the bilateral cancellation path where it appears.

The comment "Declaration moved up" is strong archaeology: advc.155 moved the variable's scope so
the later cancellation block could read it, but did not move or duplicate the threshold
calculation needed to make that read meaningful.

SUPPORTED PRODUCER / CONCRETE CONSEQUENCE
-----------------------------------------
Human-AI teammates can create ordinary persistent annual trades. AdvCiv deliberately removed the
older K-Mod shortcut that automatically returned NO_DENIAL for teammate resources; advc.155's
published purpose was instead to make human-AI team trading bounded by normal utility while still
allowing bounded help.

A concrete supported resource-for-resource lifecycle is enough:

1. Human H and AI A are on the same team (ordinary team game or Permanent Alliance).
2. A has multiple spare Wheat copies. H has spare Copper.
3. While Copper still has military utility for A, H offers Copper and A gives Wheat. The bilateral
   deal can pass the normal proposal valuation because A values the first useful Copper highly.
4. The deal matures past the mandatory no-cancel window.
5. Later A reaches the state already established for F333: all Copper-consuming military units
   have available upgrades, A has no Copper-using corporation, and `AI_bonusVal(Copper, ..., true)`
   can fall to zero. Wheat remains useful to H.
6. `AI_checkCancel` calls `AI_considerOffer(..., iChange=-1, ...)`.

At this point:
- `iWeReceive` (A's value for continuing to receive Copper) can be 0;
- `iTheyReceive` (the proxy value of H continuing to receive Wheat) remains positive;
- the deal is bilateral, so the one-sided gift branch — the only place that computes
  `iThreshold` — is skipped;
- `bSameTeam` is true, but `iTheyReceive < -1` is false;
- the forced same-team fairness tolerance then tests roughly `150 * 0 >= positive * 100`, which is
  false;
- `AI_considerOffer` returns false and `AI_checkCancel` returns RENEGOTIATE.

For a human counterpart, `AI_doDeals` subsequently queues the deal for cancellation, kills it and
opens the combined CANCEL_DEAL diplomacy popup.

The resource-specific checks do not rescue this example:
- A can continue to have no denial for exporting a genuinely spare Wheat copy to its teammate;
- when H's outgoing Copper is checked from H's proxy `AI_bonusTrade(..., iChange=0)` perspective,
  A's zero recipient value produces `DENIAL_NO_GAIN`, whereas `AI_checkCancel` intentionally cancels
  an incoming resource at that stage only for `DENIAL_JOKING`.

Thus the dead threshold can change the actual lifetime of a normal, previously accepted teammate
trade rather than merely leaving unreachable bookkeeping behind.

INTENDED POLICY SIGNAL
----------------------
The advc.155 release note states:

    "In human-AI teams (team game or Permanent Alliance), the AI no longer accepts all trades
     proposed by the human; only accepts if the trade is at least somewhat even. Can reject gift
     requests."

Within `AI_considerOffer`, one-sided teammate help gets a deliberately enlarged threshold
(`* 5`). The later cancellation branch then explicitly tries to preserve a same-team deal when
`iTheyReceive < iThreshold` before applying the stricter 150% fairness test. That policy is not
being executed because the threshold remains the `-1` sentinel.

For example, before grant-memory subtraction and the small deterministic random perturbation, a
same-team threshold after only 20 met turns is already:

    (20 + 50) * 2 * 5 = 700 trade-value units

so ordinary small teammate resource support can naturally fall below the intended bound. The
current code never observes that bound on bilateral cancellation.

ANCESTRY
--------
K-Mod 1.46 has no advc.155 same-team cancellation exemption. Its cancellation path simply applies
the generic 125% fairness test after the one-sided gift logic.

Base AdvCiv 1.14 contains the same `iThreshold = -1`, the same assignment confined to the returning
one-sided branch, and the same later same-team comparison. Current SAS inherits the structure
unchanged. This is therefore an AdvCiv-introduced regression/incomplete implementation, not an SAS
addition and not a stock/K-Mod defect.

REPAIR DIRECTION
----------------
Ensure the same-team cancellation path receives the same bounded-help threshold that advc.155
intended, without altering non-team gift/demand behavior.

A conservative repair is to factor or precompute the same-team threshold before the one-sided gift
branch when `bSameTeam` is true, including the existing deterministic random adjustment and
peacetime-grant subtraction, then let the one-sided branch reuse it. Non-team/vassal threshold
logic can remain where it is.

Do not merely change the sentinel from `-1` to a large constant: the threshold is intentionally
age/contact/grant-sensitive, and the same-team `*5` policy should remain the controlling contract.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier numbered album root covers this dead advc.155 threshold.

- F332 concerns `AI_foldDeals` resetting the cancellation age of an equivalent AI-AI deal.
- F333 concerns obsolete-unit resource classification inside `AI_bonusTrade`.
- Earlier queue-042 deal roots concern deal representation, Permanent Alliance teardown and
  embargo valuation, not `AI_considerOffer`'s human-AI teammate fairness policy.

F334 is therefore independent.


CURRENT CONTINUATION CURSOR
---------------------------
1. Continue through the remainder of `AI_considerOffer`, especially proposal-only list-shape
   assumptions and the small-value/dual exemption, but do not revive WIP18's persistent mixed-dual
   cancellation root without a producer.
2. Continue `AI_counterPropose` / `AI_balanceDeal`, checking giver/recipient orientation, bundle
   ordering and annual-item handling against supported human proposal shapes.
3. Continue downward through inherited trade/value helpers and then `AI_unitValue`.
4. F335 / provisional KI#658 remains UNUSED.
5. WIP21 is the latest physical checkpoint; WIP18 remains superseded/retracted.


========================================================================================================================
C031-WIP22 — QUEUE 001 DEEP RE-LOOP CONTINUATION — F335 CONFIRMED
========================================================================================================================

STATUS
------
Queue 001 — `CvPlayerAI.cpp` — OPEN.
Unique progress remains 30/169 files complete.

Deep re-loop findings currently confirmed:
- F321 / KI#644
- F322 / KI#645
- F323 / KI#646
- F324 / KI#647
- F325 / KI#648
- F326 / KI#649
- F327 / KI#650
- F328 / KI#651
- F329 / KI#652
- F330 / KI#653
- F331 / provisional KI#654
- F332 / provisional KI#655
- F333 / provisional KI#656
- F334 / provisional KI#657
- F335 / provisional KI#658  <-- CONFIRMED IN THIS WIP

F336 / provisional KI#659 — UNUSED.

No repository source files were modified.


CONTROL — proposal-only dual/head small-value exemption remains unpromoted
-------------------------------------------------------------------------
The proposal-phase `AI_considerOffer` small-value guard still has an order-sensitive shape:

    if (iWeReceive < 2 * DIPLOMACY_VALUE_REMAINDER &&
        ... &&
        (kTheyGive.getLength() <= 0 ||
         !CvDeal::isDual(kTheyGive.head()->m_data.m_eItemType)))
        return false;

Unlike the retracted WIP18 persistent-deal theory, negotiation-time annual offers genuinely can
contain dual and resource/GPT items together: `CvPlayer::updateTradeList` filters by annual versus
nonannual class, and Open Borders / Defensive Pact / resources / GPT are all annual.

However, the dangerous shape depends on which item the EXE puts at `head()` in the human's active
offer list. The DLL constructs the inventory in a canonical order, but the source available here
does not prove whether selected offer items are preserved in click order, inventory order or some
other EXE-controlled order before `AI_considerOffer` receives them. Without that producer-order
proof, this remains a defensive/list-shape concern rather than a numbered root. WIP18 is not
revived.


F335 / provisional KI#658 — AdvCiv's non-surplus counterproposal fallback can select an item that its own no-overshoot filter rejected
-----------------------------------------------------------------------------------------------------------------------------------

CLASSIFICATION
--------------
Independent inherited AdvCiv logic defect in `CvPlayerAI::AI_balanceDeal`, introduced with the
advc.036 resource-trade overhaul. The bug can make an AI-generated offer to a human demand a
non-surplus resource even though the same function explicitly rejected that resource for
overshooting the remaining balance gap.

ROOT CAUSE
----------
`AI_counterPropose` states the policy directly:

    So if their value is greater, we don't mind suggesting
    items which overshoot the balance. But if our value is greater, we will
    never suggest adding items which overshoot the balance.

The second case calls the side that needs to receive more through:

    AI_balanceDeal(..., bTheyGenerous = false, ...)

Inside `AI_balanceDeal`, AdvCiv separates non-surplus resources from ordinary items so that, when a
human is involved, GPT and ordinary surplus items can be preferred before asking for a player's
last resource copy.

Candidate admission initially respects the non-generous no-overshoot rule:

    if (iItemValue > 0 &&
        (bTheyGenerous || iTheyReceive >= iWeReceive + iItemValue || ...))
    {
        ...
        if (bNonsurplus)
            aNonSurplusItems.push_back(itemValue);
        else
            aItemValues.push_back(itemValue);
    }

Ordinary items are then applied first. In the non-generous branch each one is rechecked against the
CURRENT remaining gap before being appended:

    if (iTheyReceive >= iWeReceive + iItemValue)
    {
        kWeWant.insertAtEnd(it->first);
        iWeReceive += ...;
    }

Consequently, after one or more ordinary items have been added, a non-surplus resource that was
legal against the ORIGINAL gap can now be too large for the NEW gap.

The later non-surplus fallback appears to account for exactly that:

    for (best_it = it = aNonSurplusItems.begin();
         it != aNonSurplusItems.end(); ++it)
    {
        if (!bTheyGenerous && iWeReceive + it->second > iTheyReceive)
            continue;
        ...
    }

But `best_it` is initialized to `begin()` BEFORE any candidate passes the filter. If every remaining
non-surplus item overshoots, every iteration executes `continue`, yet `best_it` still points to the
first rejected item.

The post-loop code then treats that rejected element as selected:

    if (best_it->second <= 2 * (iTheyReceive - iWeReceive) || bTheyGenerous)
    {
        kWeWant.insertAtEnd(best_it->first);
        iWeReceive += best_it->second;
    }

Thus, when `bTheyGenerous == false`, a rejected resource whose value is greater than the remaining
gap but no greater than twice that gap is appended anyway. The explicit no-overshoot predicate and
the caller's stated contract are both defeated.

The defect is broader than the all-candidates-rejected case. If `best_it` starts on a rejected
overshooting first element and a later valid element does not happen to beat it under the distance
comparison, the rejected iterator can still win. The simplest proof, however, needs only one
remaining non-surplus candidate.

CONCRETE ARITHMETIC REPRODUCTION INSIDE THE FUNCTION
----------------------------------------------------
A simple trade-value state demonstrates the error without relying on rounding:

    iTheyReceive = 200
    iWeReceive   = 100
    original gap = 100

Available additional items from the side that needs to give more:

    ordinary tradable tech U = 70
    non-surplus resource R   = 40

Both are initially legal because 70 <= 100 and 40 <= 100, so U enters `aItemValues` and R enters
`aNonSurplusItems`.

The non-generous ordinary-item pass adds U first:

    iWeReceive = 170
    remaining gap = 30

R is now too large:

    170 + 40 > 200

so the non-surplus loop's explicit no-overshoot check rejects it. Because R is the only candidate,
`best_it` nevertheless remains R. The subsequent closeness check passes:

    40 <= 2 * 30

and R is appended:

    iWeReceive = 210

The allegedly non-generous / no-overshoot side has therefore been pushed 10 trade-value units past
balance by the very item its filter rejected.

SUPPORTED PRODUCER — AI TECH OFFER TO A HUMAN
---------------------------------------------
This state can reach a real diplomacy proposal; it is not merely an artificial direct call to the
helper.

`AI_doDiplo`'s AI-initiated tech-trade path first constructs a tech-for-tech offer. If the initial
pair is not mutually acceptable, it explicitly lets the other player counter-propose, including a
human:

    if (kPlayer.AI_counterPropose(getID(), weGive, theyGive, true, true))
        bDeal = true;

The source comment says:

    let the other team counter-propose (even if the other team is a human player)

For a human H receiving an AI P's offer, H's proxy `AI_counterPropose` can find that H receives more
value than P. It then calls **P's** `AI_balanceDeal` with H's inventory and
`bTheyGenerous = false`; i.e. P is trying to ask H for additional items without overshooting the
balance.

A supported offer shape is therefore:

1. P offers tech T1 to H and H initially offers tech T2 to P.
2. From the two proxy valuations, H's side is ahead enough that P needs additional value.
3. H owns another tradable tech U and exactly one copy of a tradable resource R that P values.
4. Both U and R individually fit the original value gap, so the initial candidate filter admits
   both. Because H owns only one tradable copy, R is deliberately placed in
   `aNonSurplusItems` by advc.036.
5. U is processed first as an ordinary item and shrinks the remaining gap until R is now too large,
   but still no more than twice the new gap.
6. The broken `best_it` fallback adds R anyway.

The human-last-copy resource is genuinely tradeable in current AdvCiv/SAS. `CvPlayer::canTradeItem`
uses `bCanTradeAll = true` for resources, allowing one tradeable copy, and a human giver's
`AI_bonusTrade` returns `NO_DENIAL` when the AI recipient values the resource at least the ordinary
trade threshold. This is exactly why advc.036 needed a separate `aNonSurplusItems` policy rather
than simply filtering last-copy resources out as K-Mod did.

No-gold states are also straightforward in current SAS: Writing enables technology trading while
Currency separately enables gold trading, so an AI tech proposal can occur in a period where gold
cannot simply fill the residual gap. Even with gold trading available, zero/insufficient available
gold can leave the same fallback active.

VISIBLE CONSEQUENCE
-------------------
After `AI_balanceDeal` mutates the counter list and the referenced value, control returns to the
human-proxy `AI_counterPropose` branch. That branch intentionally does not require the human to get
at least as much value; it only checks whether the AI's resulting side is acceptable before showing
the proposal to the actual human for confirmation.

Therefore the wrongly appended resource can survive into:

    gDLL->beginDiplomacy(... OFFER_DEAL ...)

The user can receive an AI-generated tech offer that asks for T2 + U + R even though the intended
non-generous balancing algorithm should have stopped at T2 + U rather than request R after R ceased
to fit the remaining gap.

This is not necessarily a huge exploit or catastrophic overcharge — the final `<= 2 * gap` guard
bounds this particular mistake — but it directly makes AI-generated offers less faithful to their
own fairness policy and can demand a player's last strategic/luxury/health resource unnecessarily.

ANCESTRY / ARCHAEOLOGY
----------------------
K-Mod 1.46 has the same high-level contract:

    if our value is greater, we will never suggest adding items which overshoot the balance.

Its corresponding non-generous item loop rechecks every candidate against the CURRENT gap before
adding it. K-Mod also excludes last-copy/corporation resources from the candidate list because it
does not yet evaluate the giver's loss there. It therefore has no `aNonSurplusItems` fallback and
not this bug.

AdvCiv practical commit 1287 / SHA `ad5faf8aa2740b991c49d8f35e14769d190a4c71`
(2018-02-15, "Test version incl. DLL and manual; needs some more work.") introduced the
`nonsurplusItems` vector and this exact fallback as part of the advc.036 AI-resource-trade overhaul.
The commit message explicitly describes an "Overhaul of AI resource trades". The original inserted
code already initialized `best_it` to `begin()`, skipped overshooting candidates with `continue`,
and then dereferenced `best_it` afterward without recording whether any candidate survived.

Base AdvCiv 1.14 still contains the same logic at its current `aNonSurplusItems` fallback. Current
SAS inherits it unchanged apart from surrounding refactors. This is therefore an inherited AdvCiv
regression introduced when extending K-Mod to reason about non-surplus resources.

REPAIR DIRECTION
----------------
Do not allow `best_it` to represent a candidate until that candidate has passed the current-gap
filter.

A conservative structure is:

    best_it = aNonSurplusItems.end();
    for (it = aNonSurplusItems.begin(); it != aNonSurplusItems.end(); ++it)
    {
        if (!bTheyGenerous && iWeReceive + it->second > iTheyReceive)
            continue;
        if (best_it == aNonSurplusItems.end() || better_than(*it, *best_it, ...))
            best_it = it;
    }
    if (best_it == aNonSurplusItems.end())
        break;

Then apply the existing `<= 2 * remaining gap` closeness rule only to a candidate that actually
passed the no-overshoot predicate.

For `bTheyGenerous == false`, breaking when no valid candidate exists is the natural contract: all
remaining non-surplus resources are now too large, and adding any of them would violate the exact
policy stated by `AI_counterPropose`.

DUPLICATE / ROOT SEPARATION
---------------------------
No earlier numbered album finding covers this selection failure. The old shallow Queue-001 control
merely listed `AI_balanceDeal` among functions where no semantic defect had yet been found; this
deeper producer/lifecycle pass supersedes that provisional clean note.

- F321 concerns prospective resource state in cancellation valuation.
- F332 concerns `AI_foldDeals` resetting deal age.
- F333 concerns obsolete-unit strategic classification.
- F334 concerns the dead advc.155 same-team cancellation threshold.
- The retracted WIP18 dual-head theory concerned persistent deal representation, not counterproposal
  item selection.

F335 is independent.


NEXT AUDIT CURSOR
-----------------
1. Continue the remainder of `AI_balanceDeal`, including the advc.705 `rLeniency` accounting and
   final-item paths; F336 / provisional KI#659 is UNUSED.
2. Continue the remaining `AI_counterPropose` callers and trade/value helpers, retaining the same
   producer -> contract -> consequence standard.
3. Then continue downward into `AI_unitValue` and its surrounding inherited unit-role valuation
   helpers.
4. Keep the proposal-only dual/head concern as an unnumbered control unless EXE/list ordering can
   be proved from a supported producer.
5. WIP22 is the latest physical checkpoint; WIP18 remains superseded/retracted.


===============================================================================
C031-WIP23 CONTINUATION — QUEUE 001 `CvPlayerAI.cpp`
===============================================================================

F336 / provisional KI#659 — CONFIRMED
-------------------------------------

TITLE
-----
`AI_balanceDeal` forgets the advc.705 `rLeniency` multiplier when accumulating
non-surplus resources, so the special Rise & Fall pre-Currency retry can count
AI resources at 100% instead of the documented 90% and stop balancing too early.

STATUS
------
CONFIRMED inherited AdvCiv regression. Current SAS inherits the defect from Base
AdvCiv 1.14. K-Mod predates Rise & Fall/advc.705 and does not have this contract.
The defect was introduced by the later advc.036 non-surplus-resource balancing
code without carrying forward the already-existing leniency accumulator rule.

CONTRACT
--------
Rise & Fall change advc.705 deliberately gives the AI a second pre-Currency
attempt at a technology trade. If the first proposal/counterproposal fails and
neither team can trade gold, `AI_counterPropose` retries with:

    scaled const rSecondAttempt = fixp(0.9);
    ...
    return AI_counterPropose(..., rSecondAttempt);

The current AdvCiv manual states the user-facing rule directly:

    "The AI is a bit more lenient in pre-Currency tech trades – accepts if it
     receives only 90% of what the player receives."

The introduction history is equally explicit. Practical commit 1268 /
`e11525d303...` introduced the Rise & Fall option and refactored the
counterproposal accumulator with the comment:

    advc.705: Apply this to all assignments to iTheirValue

In the modern names this is `rLeniency`, and the current lower-level signature
still says:

    // advc.705: Applied to everything that's added to iValueForThem

The initial value of the human side is discounted:

    int iTheyReceive = (rLeniency *
            kPlayer.AI_dealVal(...)).round();

When the side being balanced adds ordinary items, gold, or GPT, current
`AI_balanceDeal` consistently discounts the accumulator too:

    iWeReceive += (rLeniency * iItemValue).round();

    iWeReceive += (rLeniency *
            iGoldData * per100(iGoldValuePercent)).round();

    iWeReceive += (rLeniency *
            AI_goldPerTurnTradeVal(iGoldData)).round();

Therefore `rLeniency` is not merely a final acceptance threshold. It defines the
value units of the accumulator throughout the 90% retry.

ROOT
----
The later advc.036 non-surplus-resource fallback is the sole item-addition path
in `AI_balanceDeal` that omits the multiplier:

    if (best_it->second <= 2 * (iTheyReceive - iWeReceive) || bTheyGenerous)
    {
        kWeWant.insertAtEnd(best_it->first);
        iWeReceive += best_it->second;
    }

At `rLeniency == 0.9`, a resource worth V to the human side is therefore added
as V rather than round(0.9 * V). The accumulator immediately ceases to represent
the discounted value used by the surrounding function.

The problem is not shared by the ordinary-item, gold or GPT paths; all three
carry the `rLeniency` multiplication explicitly. Nor is this merely a display
or logging mismatch: the inflated accumulator controls the `while
(iTheyReceive > iWeReceive)` continuation condition and is passed back to
`AI_counterPropose` for acceptance.

SUPPORTED PRODUCER
------------------
The special retry is reachable through the ordinary human trade-table path.
`CvPlayer::AI_counterProposeExternal` is the virtual wrapper explicitly called
by the EXE for "what would make this deal work?" and forwards the human's deal
and trade inventories into the AI's lower-level `AI_counterPropose`.

When Rise & Fall is enabled, the other party is human, the first attempt fails,
and neither team has gold trading, that exact AI instance recursively retries
with `rLeniency = 0.9`.

This is importantly different from the AI-initiated tech-offer helper path:
that path can call a human proxy. F336 does not need that ambiguous orientation;
the direct EXE `AI_counterProposeExternal` producer gives the required
AI-callee / human-counterparty shape explicitly.

INDEPENDENT ARITHMETIC REPRODUCTION
-----------------------------------
The effect can be demonstrated without relying on F335's rejected-iterator bug.
Use trade-value units that are legal for the current evaluators (tech values are
rounded to the configured diplomacy remainder; resource trade values are
ordinary integer trade values and can span from very small values to hundreds
as city count and resource utility rise).

Suppose a human asks "what would make this deal work?" in Rise & Fall before
Currency, with initial raw values:

    AI receives from human:         1380
    human receives from AI:         1400

On the first (`rLeniency = 1`) attempt, the human side leads by 20. If the human
has no additional permitted item that the AI can ask for, that attempt fails.

The advc.705 retry discounts the human side:

    1400 * 0.9 = 1260

so the direction flips:

    AI side:                         1380
    discounted human side:          1260
    gap to sweeten for human:         120

Assume the AI has two otherwise-legal last-copy/non-surplus resources valued by
the human proxy at 120 and 10 trade-value units. Both pass the *initial* current
gap admission test (120 <= 120 and 10 <= 120), and the 120-value resource is the
best gap filler.

Current buggy path:

    1260 + 120 = 1380

The loop considers the gap closed and stops. The resulting counterproposal adds
only the 120-value resource.

Correct advc.705 accounting:

    1260 + round(0.9 * 120) = 1260 + 108 = 1368
    remaining discounted gap = 12

The 10-value resource still passes the *current* no-overshoot filter:

    1368 + 10 <= 1380

so F335 is not involved. It should then also be appended, counted at
round(0.9 * 10) = 9, leaving the discounted human side at 1377. The documented
90% rule permits the trade and the corrected counterproposal gives the human
both resources instead of prematurely stopping after the first one.

The exact numbers are illustrative rather than a claim about one fixed leader
or savegame; the important constraints are all supported by the live value
functions. A large first resource whose value is the retry gap and a second
resource no larger than 10% of it produces the same divergence. Resource trade
value scales with empire/resource utility and is not restricted to a single
fixed XML price.

VISIBLE CONSEQUENCE
-------------------
The 90% retry can produce a counterproposal that is less generous to the human
than advc.705 intended. A non-surplus AI resource is counted at full value,
closing the discounted gap earlier than the same function's ordinary-item/gold/
GPT accounting would allow. Consequently later legal sweeteners can be omitted
from the "what would make this deal work?" response.

This is a behavioral change, not merely an internal valuation discrepancy:
`kWeWant` is the counterproposal list returned through the EXE trade-table
wrapper. The missing later resource therefore changes what the player is asked
to accept.

ANCESTRY / ARCHAEOLOGY
----------------------
The ordering of the two AdvCiv changes identifies the regression cleanly.

1. Practical commit 1268 / `e11525d303...` introduced Rise & Fall and advc.705.
   Its counterproposal refactor explicitly said to apply the leniency factor to
   *all* assignments to the discounted side's accumulator, and did so for the
   existing item/gold/GPT additions.

2. Practical commit 1287 / `ad5faf8aa2...` later introduced the advc.036
   non-surplus-resource vector/fallback as part of the AI-resource-trade
   overhaul. Its newly inserted fallback used:

       iSmallerVal += best_it->second;

   rather than the already-required leniency-scaled accumulation.

3. Base AdvCiv 1.14 still contains exactly the unscaled modern line:

       iWeReceive += best_it->second;

4. Current SAS inherits the same omission.

Thus this is not a SAS tuning disagreement and not a K-Mod defect. It is a
cross-feature AdvCiv regression: advc.036 added a new accumulator assignment
after advc.705 had established the 90% accounting contract, but failed to apply
that contract to the new path.

REPAIR DIRECTION
----------------
Make the non-surplus fallback use the same accumulator units as every other item
addition in `AI_balanceDeal`:

    iWeReceive += (rLeniency * best_it->second).round();

The surrounding selection comparisons deserve a separate review because they
currently compare raw `it->second` against a discounted remaining gap when
`rLeniency != 1`. F336 does not need that broader claim: the confirmed root is
the direct accumulator omission, and the reproduction above deliberately keeps
the second resource inside the raw current-gap filter so no selection-filter
repair is required for the demonstrated consequence.

DUPLICATE / ROOT SEPARATION
---------------------------
F336 is independent of F335.

F335 concerns `best_it` being initialized to `begin()` before a candidate passes
the no-overshoot filter, allowing a rejected resource to be appended. In the
F336 reproduction every selected resource passes that filter. The wrong result
exists solely because the first non-surplus resource advances the lenient
accumulator by 100% instead of 90%.

It is also independent of F321/F332/F333/F334; those concern cancellation state,
deal age, strategic classification and teammate cancellation respectively.


CURRENT AUDIT STATE AFTER F336
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F336
F337 / provisional KI#660 — UNUSED
WIP18 mixed-dual F333 — RETRACTED / superseded
Latest physical checkpoint — C031-WIP23
No repository source files modified.

NEXT CURSOR
-----------
1. Continue the remaining `AI_balanceDeal` / `rLeniency` selection comparisons,
   but do not merge a raw-vs-discounted comparison issue into F336 unless it has
   an independent supported consequence.
2. Continue the public `AI_counterPropose` callers and nearby trade/value helpers.
3. Then proceed downward into `AI_tradeValToGold`, gold-cap helpers and toward
   `AI_unitValue`, preserving producer -> contract -> consequence proof.


===============================================================================
C031-WIP24 CORRECTION / CONTINUATION — QUEUE 001 `CvPlayerAI.cpp`
===============================================================================

STATUS
------
F336 / provisional KI#659 remains CONFIRMED.
F337 / provisional KI#660 remains UNUSED.
This checkpoint supersedes two overbroad current-source statements in WIP23; it does
not retract F336's root, producer or independent two-resource consequence.

CORRECTION TO F336 CURRENT-SOURCE DESCRIPTION
---------------------------------------------
WIP23 said that ordinary items, gold and GPT in current `AI_balanceDeal` consistently
apply `rLeniency`, and described the non-surplus fallback as the sole item-addition
path omitting the multiplier. That wording is too broad.

Current SAS (and Base AdvCiv 1.14) has TWO earlier monetary paths that are also
unscaled today:

1. Initial lump-sum gold, before item enumeration:

       iWeReceive += (iGoldData * iGoldValuePercent) / 100;

2. The advc.036 negative-income early-GPT path:

       int iGPTTradeVal = AI_goldPerTurnTradeVal(iGoldData);
       iWeReceive += iGPTTradeVal;

The later/final gold and GPT paths do still use:

       (rLeniency * ...).round()

and the ordinary non-generous item loop still uses:

       (rLeniency * iItemValue).round()

ARCHAEOLOGY OF THE CORRECTION
-----------------------------
The original advc.036 extraction in practical commit 1287 (`ad5faf8aa2...`) DID
preserve advc.705's multiplier for both the new initial-gold path and the new
early-GPT path. At that point the code contained the scaled assignments.

Practical commit 1420 / `c24d361ed8...` (2019-02-15, "UI conveniences for resource
trade") later removed the multiplier from exactly those two early assignments while
refactoring trade-value-to-GPT conversion for the new resource-trade UI/tooltips.
The same commit left the later/final GPT path scaled. No rationale for changing the
leniency contract is stated in the commit message; the user-facing changes concern
resource-trade display/help and factoring `AI_tradeValToGold`.

WHY F336 STILL STANDS
---------------------
F336's confirmed reproduction is the Rise & Fall pre-Currency `rLeniency = 0.9`
second attempt. That retry is explicitly gated on BOTH teams lacking gold trading, so
neither early gold nor early GPT is available there. The demonstrated divergence is
therefore still caused solely by the non-surplus-resource accumulator omission.

The ancestry of that omission also remains unchanged: advc.705 established the
"apply to all assignments" accumulator contract in practical commit 1268, and
advc.036 practical commit 1287 later added the non-surplus fallback as an unscaled
assignment. F336 does not depend on claiming that later code never introduced other
violations of the same contract.

F337 CONTROL / OPEN QUESTION
----------------------------
The 2019 removal of leniency from initial gold / early GPT is now a separate F337
candidate, not silently folded into F336.

It cannot be promoted merely from source inconsistency. The ordinary EXE Rise & Fall
0.9 retry is pre-Currency and therefore cannot exercise those monetary branches.
Non-default leniency also appears in AI-initiated city-trade and capitulation helpers,
but their public `AI_counterPropose` wrapper can invert the multiplier depending on
which side is allowed to add items. A real producer must therefore establish:

- a supported non-1 `rLeniency` reaching `AI_balanceDeal` on the side adding gold/GPT;
- gold/GPT being legally available there;
- and a visible proposal/acceptance difference caused by the unscaled early path.

Until that full orientation and consequence survives, F337 remains UNUSED.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F336
F337 / provisional KI#660 — UNUSED
Latest physical checkpoint — C031-WIP24
No repository source files modified.

NEXT CURSOR
-----------
1. Finish F337's non-default-leniency caller/orientation proof or kill it.
2. Continue `AI_tradeValToGold`, gold-cap helpers and remaining trade-value helpers.
3. Continue downward toward `AI_unitValue` under the same producer -> contract ->
   consequence standard.


===============================================================================
C031-WIP25 CONTINUATION — QUEUE 001 `CvPlayerAI.cpp`
===============================================================================

F337 / provisional KI#660 — CONFIRMED
-------------------------------------

TITLE
-----
The 2019 AdvCiv resource-trade/UI refactor dropped advc.705 `rLeniency` from the
initial gold accumulator in `AI_balanceDeal`, so discounted AI-initiated city trades
can count an AI gold sweetener at 100% instead of 90% and omit another item that the
city-trade policy intended to give the human.

STATUS
------
CONFIRMED inherited AdvCiv regression. Base AdvCiv 1.14 and current SAS both contain
the unscaled initial-gold assignment. K-Mod has neither AdvCiv's advc.705 leniency
contract nor the later `AI_proposeCityTrade` producer.

A sibling omission exists in the same 2019 commit for the negative-income early-GPT
path. F337's confirmed reproduction uses lump-sum gold only; the GPT line is recorded
as the same historical contract break but does not need to be relied on for the
finding.

CONTRACT
--------
The lower-level counterproposal function still documents advc.705 as:

    // advc.705: Applied to everything that's added to iValueForThem

and its public wrapper describes `rLeniency` as the multiplier for the value counted
for items of the side that does not give more.

AdvCiv's AI-initiated city-trade helper makes deliberate non-default use of that
mechanism. When `bWeGiveMore` and the recipient is human it calls:

    AI_counterPropose(..., true, false,
            // The usual discount to make the offer worth considering
            fixp(1/0.9));

Because `bTheyMayGiveMore` is true, the public wrapper calls
`rLeniency.flipFraction()`, so the lower-level call receives 0.9. This is not a
Rise & Fall-only value: city-trade negotiation uses the multiplier explicitly even
when Currency/gold trading is available.

The city-pair sign is also source-defined rather than guessed. `AI_proposeCityTrade`
gets `iWeGiveVal` from the recipient's `AI_cityTradeVal` for our city and
`iTheyGiveVal` from our `AI_cityTradeVal` for their city, then sets:

    bWeGiveMore = (iWeGiveVal > iTheyGiveVal)

Thus a city pair where the human values our city slightly more than we value theirs
is exactly the `bWeGiveMore` path using the 0.9 human-side discount.

ROOT
----
Current `AI_balanceDeal`, before enumerating maps/techs/resources/cities, tries to
fill the gap with lump-sum gold. For a non-generous side it computes and rounds a
gold amount from the CURRENT discounted gap, then updates the accumulator with:

    iWeReceive += (iGoldData * iGoldValuePercent) / 100;

The `rLeniency` multiplier is missing.

Later ordinary items in this same non-generous balance path correctly use:

    iWeReceive += (rLeniency * iItemValue).round();

and the final-gold/final-GPT paths also still use scaled accumulation. Therefore the
initial gold update changes units in the middle of a single balancing operation.

SUPPORTED PRODUCER / LIST SHAPE
-------------------------------
`AI_proposeCityTrade` is a current shipped AI producer. A non-liberation city trade is
a nonannual deal. `CvPlayer::updateTradeList` permits other nonannual items alongside
it; lump-sum Gold and World Map are both nonannual (`CvDeal::isAnnual` returns false
for both), so an AI inventory can legally supply both as city-trade sweeteners.

Assume ordinary supported conditions:
- AI A and human H can trade cities, gold and maps;
- A has at least 25 tradeable gold;
- A's map has trade value 10 to H and has no denial;
- both candidate cities pass `AI_intendsToCede` / city-trade denial;
- H values A's city X at 1050 trade-value units;
- A values H's city Y at 1000.

These values need not correspond to one named save; `AI_cityTradeVal` is an integer
heuristic with many continuous empire/city inputs, and `AI_mapTradeVal` rounds to the
diplomacy remainder, so a 10-value map is an ordinary supported result.

EXACT CURRENT ARITHMETIC
------------------------
Because 1050 > 1000, `bWeGiveMore` is true. The public wrapper flips 1/0.9 to 0.9.
The lower-level values become:

    AI receives H's city:                  1000
    H receives A's city, discounted:       1050 * 0.9 = 945

The discount intentionally creates a 55-value sweetener gap for H.

The direction therefore calls the HUMAN proxy's `AI_balanceDeal` with A's inventory,
`rLeniency = 0.9`, and `bTheyGenerous = false`: A is the side that must add more.
This is the required non-1-leniency monetary producer.

Current AI gold is worth 200 trade-value units per 100 gold. The initial gold path
computes:

    55 * 100 / 200 = 27 gold

Because a human is involved, `AI_roundTradeValBounds` rounds the non-generous amount
down to the diplomacy multiple; with the standard remainder 5 this becomes 25 gold.
Its raw trade value is 50.

CURRENT BUGGY ACCUMULATOR:

    945 + 50 = 995
    remaining gap = 5

The subsequent 10-value map is rejected by the ordinary non-generous admission test:

    1000 >= 995 + 10    // false

So the generated city offer can stop with X + 25 gold for Y (apart from the automatic
peace-treaty representation used for city trades).

CORRECT / PRE-2019 ADVC.705 ACCUMULATOR:

    945 + round(0.9 * 50) = 990
    remaining gap = 10

Now the same map passes the CURRENT raw no-overshoot check exactly:

    1000 >= 990 + 10    // true

and the ordinary item path adds it as:

    990 + round(0.9 * 10) = 999

The resulting proposal contains X + 25 gold + World Map for Y. This example therefore
does not depend on F335's invalid-iterator fallback or F336's non-surplus fallback;
it stays entirely in the ordinary item path after the erroneous initial-gold update.

VISIBLE CONSEQUENCE
-------------------
The bug makes a current AI-initiated city trade less generous to the human than the
explicit 0.9 city-trade discount intends. Counting the initial gold at 100% closes
too much of the discounted gap and can suppress a legal map/tech/resource/city
sweetener that would have been added under consistent advc.705 accounting.

The proposal is not merely internal calculation: `AI_proposeCityTrade` places the
mutated `weGive` / `theyGive` lists into an `OFFER_DEAL` diplomacy popup for the human.
The omitted item therefore changes the offer the player actually sees.

ANCESTRY / ARCHAEOLOGY
----------------------
The regression can be dated precisely.

1. Practical commit 1268 / `e11525d303...` introduced advc.705 and explicitly changed
   counterproposal accumulator assignments to multiply by leniency.

2. Practical commit 1287 / `ad5faf8aa2...` extracted the balancing code for the
   advc.036 resource-trade overhaul. Its NEW initial-gold and negative-income GPT
   paths still preserved the multiplier:

       iSmallerVal += round(leniency * goldTradeValue);
       iSmallerVal += round(leniency * gptTradeVal);

3. Practical commit 1420 / `c24d361ed8...` (2019-02-15, "UI conveniences for resource
   trade") factored out `AI_tradeValToGold` for resource-trade display/tooltips and
   simultaneously changed those two early assignments to unscaled additions. The
   commit message documents resource-trade UI/help changes and the helper extraction;
   it gives no policy rationale for dropping advc.705. Importantly, the same commit
   left the later final-GPT assignment scaled.

4. Practical commit 2085 / `f54cc30550...` (2020-04-01, "AI code for initiating city
   trades") later added the current city-trade producer and its explicit "usual
   discount" non-1-leniency calls. By then the early-gold contract break was already
   present.

5. Base AdvCiv 1.14 still has the unscaled line. Current SAS inherits it.

K-Mod 1.46 has the older counterproposal machinery without advc.705 and without the
AdvCiv city-trade helper, so this is not a K-Mod root.

SIBLING EARLY-GPT OMISSION
--------------------------
The same practical commit 1420 also changed the negative-income early-GPT path from:

    iSmallerVal += round(leniency * gptTradeVal);

to the current equivalent of:

    iWeReceive += iGPTTradeVal;

This is the same dimensional/contract omission. F337 is already independently proved
through lump-sum gold, so no GPT-specific scenario is needed to establish the root.
For a repair, restoring the multiplier on BOTH early monetary assignments matches the
pre-1420 implementation and the still-current advc.705 signature contract. If review
prefers ultra-narrow changes, the initial-gold line alone is sufficient for the
confirmed city-trade reproduction.

REPAIR DIRECTION
----------------
Restore the accumulator to the same units as the surrounding path, conceptually:

    iWeReceive += (rLeniency *
            iGoldData * per100(iGoldValuePercent)).round();

and, for the sibling path:

    iWeReceive += (rLeniency * iGPTTradeVal).round();

Do not change the gold amount computation merely to force the accumulator to equality.
The pre-1420 design deliberately allowed the leniency-scaled addition to leave a
residual gap so another item could be considered; the city-trade reproduction above
shows that this affects the intended sweetener list.

DUPLICATE / ROOT SEPARATION
---------------------------
F337 is separate from F336 and F335.

- F335: rejected `best_it` can be dereferenced in the non-surplus fallback.
- F336: the later advc.036 non-surplus fallback itself omits `rLeniency`; its confirmed
  producer is Rise & Fall's pre-Currency retry where gold is unavailable.
- F337: the EARLY lump-sum gold assignment lost a multiplier in a later 2019 refactor;
  its confirmed producer is AI-initiated city trading where gold is available.

The two findings share the broader advc.705 accounting contract but have different
birth commits, branches and visible producer paths, so keeping them separate is useful
for review and archaeology.

CURRENT AUDIT STATE AFTER F337
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F337
F338 / provisional KI#661 — UNUSED
Latest physical checkpoint — C031-WIP25
No repository source files modified.

NEXT CURSOR
-----------
1. Continue `AI_tradeValToGold` / `AI_maxGoldTrade` / `AI_maxGoldPerTurnTrade` and
   adjacent current-deal enforcement paths.
2. Keep the raw-vs-discounted candidate comparisons as controls unless a distinct
   policy/consequence survives independently of F335-F337.
3. Continue through remaining inherited trade/value helpers toward `AI_unitValue`.


===============================================================================
C031-WIP26 ADDENDUM — 2026-08-30
F338 / provisional KI#661 CONFIRMED
===============================================================================

F338 — AI-HUMAN CANCELLATION QUEUE IS STILL LIVE WHEN ADVC.133 ENFORCES THE
PER-RIVAL GPT CAP, SO GPT FROM A DEAL ALREADY CHOSEN FOR CANCELLATION CAN FORCE
AN ADDITIONAL HEALTHY GPT DEAL TO BE CANCELED

STATUS
------
CONFIRMED current inherited Base AdvCiv bug. This is an ordering/state-projection
error introduced together with advc.133 in practical commit 2967. Current SAS and
Base AdvCiv 1.14 retain the same lifecycle. K-Mod predates this GPT-cap enforcement.

ROOT
----
`CvPlayerAI::AI_doDeals(eOther)` first runs `AI_checkCancel` over every mature deal
between the AI and `eOther`.

For an AI-human non-vassal deal that should be canceled, AdvCiv does NOT kill the
`CvDeal` immediately. It stores the live pointer in:

    apHumanDealsToCancel.push_back(pLoopDeal);

The source/history explicitly describes this as:

    // advc.133: Don't contact human (nor kill deal) yet

Deals for which `AI_checkCancel` returned `NO_CANCEL`, by contrast, are stored in
`aapDealsPerPlayer[eOther]` as candidates for the subsequent GPT-cap pass.

The bug is the order of the next two phases. Before `apHumanDealsToCancel` is
processed and killed, `AI_doDeals` executes:

    int iOverdraft = -AI_maxGoldPerTurnTrade(eOther, true);

`AI_maxGoldPerTurnTrade(..., true)` reads LIVE player state. In particular its
per-rival limit contains:

    rMaxGoldPerTurn += std::min(0, getGoldPerTurnByPlayer(ePlayer));

and its availability calculation also reads the current GPT state. A queued human
deal therefore still contributes to the exact state used to decide whether surviving
deals exceed the cap, even though `AI_checkCancel` has already decided that the deal
will disappear later in this same call.

The GPT enforcement loop only considers `NO_CANCEL` deals as possible victims. It
therefore cannot cure the stale contribution by selecting the already-doomed deal;
it can only cancel an additional surviving GPT deal. The queued deals are finally
killed afterward, while the combined human cancellation popup is assembled.

SUPPORTED PRODUCER
------------------
No exotic deal shape is needed. Consider an AI A and human H with two mature,
cancelable annual resource-for-GPT deals. Such resource/GPT deals are ordinary
supported objects throughout AdvCiv's resource-trading code.

- D_bad: A pays H 15 GPT for a resource. Since the deal was signed, A's resource
  valuation/state has changed enough that `AI_checkCancel(D_bad,H)` now returns a
  cancellation code. Because H is human, A can contact H and this is not a vassal
  deal, D_bad is appended to `apHumanDealsToCancel` but remains LIVE.
- D_good: A independently pays H 15 GPT for another resource that remains worthwhile.
  `AI_checkCancel(D_good,H)` returns `NO_CANCEL`, so D_good is placed in
  `aapDealsPerPlayer[H]`.

`CvDeal::startTrade` makes A's outgoing GPT negative in the bilateral accumulator:

    A.changeGoldPerTurnByPlayer(H, -trade.m_iData);

and `CvDeal::endTrade` reverses it only when the deal is actually killed. Thus both
15-GPT payments are still present when the cap check runs.

EXACT CLEAN ARITHMETIC
----------------------
Choose an ordinary state in which A's post-personality/post-attitude per-rival GPT
cap before the current bilateral-flow adjustment is 20 GPT, and make A's overall
available income comfortably nonbinding. At Cautious attitude the diplomacy
multiplier is 1, so no extra scaling is needed in the example.

Immediately before the cap check, because D_bad has only been QUEUED rather than
killed:

    current A -> H GPT = 15 + 15 = 30
    getGoldPerTurnByPlayer(H) = -30
    base per-rival cap = 20
    current additional GPT capacity = 20 + (-30) = -10

With `bCheckOverdraft=true`, `AI_maxGoldPerTurnTrade` deliberately preserves this
negative result. `AI_doDeals` negates it:

    iOverdraft = 10

which is above the function's 3-GPT tolerance.

The enforcement candidate list contains D_good but NOT D_bad. D_good pays 15 GPT,
so the loop cancels D_good to cover the apparent 10-GPT overdraft. Only afterward
does the combined-human-cancellation phase finally kill D_bad.

End result: BOTH deals are canceled.

POST-CANCELLATION STATE SHOWS THE ERROR
---------------------------------------
Had D_bad been removed from live player state before checking the cap, only D_good's
15 GPT would remain:

    projected A -> H GPT = 15
    projected additional GPT capacity = 20 - 15 = +5

There is no overdraft at all. D_good is already within the same negotiation limit
that advc.133 says it is enforcing and should survive.

The defect is therefore not a disagreement about the cap size or the 3-GPT tolerance.
The exact same cap, personality, attitude and financial state produce opposite
results solely because an already-doomed human deal remains live until after the
cap is evaluated.

The example deliberately assumes ample overall `iAvailable`, making the per-rival
`iMax` branch binding. Changes to total available-income state when D_bad ends can
therefore only be additional state-projection concerns; they are not needed for the
confirmed reproduction.

VISIBLE / GAMEPLAY CONSEQUENCE
------------------------------
A single ordinary AI-human cancellation can cascade into cancellation of a second,
otherwise healthy GPT/resource agreement. Both then appear in AdvCiv's combined
cancellation handling even though only one failed `AI_checkCancel` and the survivor
would satisfy the GPT cap after the first deal disappears.

This is especially contrary to the purpose of advc.133: the feature exists to stop
players from circumventing the AI's negotiated per-rival GPT limit through old
subsidy/payment state. Here it instead enforces that limit against payment state
that the same function has already decided is obsolete.

ANCESTRY / ARCHAEOLOGY
----------------------
The root entered in practical commit 2967 / `b1cf90d56b...` (2021-04-01),
"AI: cancel GPT deals when per-player limit exceeded" [advc.133]. The commit message
explicitly says the goal is to close the gold-subsidy loophole because per-rival GPT
limits "still exist and shouldn't be circumvented."

Crucially, that SAME patch made both relevant changes:

1. Human cancellations that had previously been processed immediately were changed
   to `apHumanDealsToCancel.push_back(pLoopDeal)`, with the source comment
   "Don't contact human (nor kill deal) yet."
2. The new `// Enforce GPT limit` phase was inserted BEFORE the later loop that walks
   `apHumanDealsToCancel` and calls `kDeal.kill()`.

The patch also added the `bCheckOverdraft` parameter to `AI_maxGoldPerTurnTrade`
specifically so the new enforcement phase could observe negative remaining capacity.
Thus this is an original advc.133 phase-ordering defect rather than a later SAS or
AdvCiv regression.

Base AdvCiv 1.14 retains the same ordering and helper logic. Current SAS inherits it.
K-Mod 1.46 has `AI_maxGoldPerTurnTrade` but not AdvCiv's advc.133 surviving-deal GPT
cap pass, so this is not a K-Mod root.

The AdvCiv manual also states the intended behavior directly: the AI should cancel
GPT deals when its total payments to a player exceed the limits that apply during
deal negotiation. The surviving D_good in the reproduction does not exceed that
limit after the independently canceled D_bad is removed.

DUPLICATE / ROOT SEPARATION
---------------------------
No existing album or current Known-Issues entry was found for this queued-cancellation
vs GPT-cap ordering root. Earlier GPT mentions in the album concern logging or trade
value/leniency, not advc.133's live-deal phase ordering.

F338 is independent of:
- F321/F333 resource valuation/classification bugs: D_bad merely needs any supported
  reason to fail `AI_checkCancel`; the stale-state root remains even when that reason
  is perfectly correct.
- F335-F337 counterproposal accounting: F338 runs on already-existing mature deals,
  not deal construction/balancing.
- F332 deal-fold age reset: neither deal needs to have been folded.

REPAIR DIRECTION
----------------
The GPT-cap phase should evaluate the state that will exist after deals already chosen
for cancellation in this `AI_doDeals` call are gone.

The safest structural repair is to separate "remember data for one combined human
popup" from "keep the CvDeal economically active": capture the human-facing trade
items/cancellation metadata, kill already-doomed non-vassal human deals BEFORE the
GPT-overdraft computation, then enforce the GPT cap on the truly surviving deal set.
Any additional human deal selected by the cap pass can be captured/killed through the
same aggregation path before the single popup is opened.

A narrower manual projection would have to reproduce not only the bilateral
`getGoldPerTurnByPlayer(eOther)` delta but also every relevant total-GPT/availability
state change from all queued deals, including GPT flowing in the opposite direction.
That is easier to get subtly wrong than updating the actual deal state first.

Do not simply remove D_bad from `aapDealsPerPlayer`: it is already absent there. The
bug is that it remains present in PLAYER ECONOMIC STATE read by
`AI_maxGoldPerTurnTrade(..., true)`.

CURRENT AUDIT STATE AFTER F338
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F338
F339 / provisional KI#662 — UNUSED
Latest physical checkpoint — C031-WIP26
No repository source files modified.

NEXT CURSOR
-----------
1. Finish the adjacent `AI_maxGoldPerTurnTrade` / GPT-overdraft caller pass now that
   the helper arithmetic itself is separated from F338's lifecycle error.
2. Continue through `AI_goldPerTurnTradeVal`, remaining trade-value helpers and the
   descent toward `AI_unitValue`.
3. Keep F339 unused unless another complete producer -> contract -> consequence chain
   survives independently of F321-F338.


===============================================================================
C031-WIP27 ADDENDUM — 2026-08-30
F339 / provisional KI#662 CONFIRMED
===============================================================================

F339 — `AI_unitValue(UNITAI_ICBM)` CAN COUNT THE OWNER'S OWN NUCLEAR
INTERCEPTION AS A CHANCE OF ITS OUTGOING NUKES BEING SHOT DOWN

STATUS
------
CONFIRMED current inherited K-Mod/AdvCiv AI valuation bug. Current SAS inherits the
same K-Mod nuclear-unit valuation logic. Civ4CE/BtS does not contain this K-Mod
interception-estimation block.

ROOT
----
The `UNITAI_ICBM` branch of `CvPlayerAI::AI_unitValue` estimates expected nuclear
losses from interception. In a neutral/non-war area it power-weights the
`getNukeInterception()` values of every live team `KNOWN_TO` the owner:

    bool const bWar = (pArea != NULL &&
            pArea->getAreaAIType(getTeam()) != AREAAI_NEUTRAL);
    int iInterceptTally = 0;
    int iPowerTally = 0;
    for (TeamIter<CIV_ALIVE,KNOWN_TO> itTeam(getTeam());
        itTeam.hasNext(); ++itTeam)
    {
        if (!bWar ||
            GET_TEAM(getTeam()).AI_getWarPlan(itTeam->getID()) != NO_WARPLAN)
        {
            int iPower = itTeam->getPower(false);
            iPowerTally += iPower;
            iInterceptTally += itTeam->getNukeInterception() * iPower;
        }
    }

AdvCiv's iterator contract explicitly documents `KNOWN_TO` as including the first
agent's own team ("agents know themselves"). Thus, whenever `bWar == false`, the
owner contributes its own power and its own nuclear-interception percentage to the
estimated chance that its outgoing nuclear unit will be intercepted.

That state is semantically impossible. Actual nuclear interception is computed by
`CvUnit::nukeInterceptionChance` from teams AFFECTED by the target detonation (plus
master-team protection). The launching team's own SDI does not intercept its own
nuclear strike merely because the launcher owns SDI.

The `bWar` subcase partly masks the defect: when the area is non-neutral, only teams
for which the owner has a war plan are accumulated, and the owner has no war plan
against itself. The confirmed root therefore concerns the neutral/peacetime branch
(and any NULL-area consumer), not every call to `AI_unitValue`.

SUPPORTED PRODUCER
------------------
This is reachable through ordinary AI production, not just a detached valuation
helper.

`CvCityAI::AI_chooseProduction` has both a high-priority AdvCiv nuclear-production
rule and the later K-Mod nuclear-production rule. Neither requires an active war;
`AI_nukeWeight()` only requires that nukes are valid and war is possible in the game.
Both rules can therefore call:

    AI_chooseUnit(UNITAI_ICBM)

while the city's land area is `AREAAI_NEUTRAL`.

`AI_chooseUnit(UNITAI_ICBM)` calls `AI_bestUnitAI(UNITAI_ICBM)`, which evaluates every
trainable nuclear candidate through:

    kOwner.AI_unitValue(eUnit, UNITAI_ICBM, area())

so the contaminated interception estimate directly participates in selecting which
nuclear unit the city trains.

CURRENT SAS DATA MAKES THE ERROR LARGE
--------------------------------------
Current SAS retains the two ordinary nuclear candidates:

- `UNIT_ICBM`: cost 500, nuke range 1, unlimited (`iAirRange = 0`), evasion 0.
- `UNIT_TACTICAL_NUKE`: cost 250, nuke range 1, air range 4, evasion 50.

Both use `UNITAI_ICBM`, require Fission + Rocketry + Uranium, and are suicide units.
Current `PROJECT_SDI` gives 60 nuclear interception.

Before the interception adjustment, the K-Mod `UNITAI_ICBM` role component is:

    ICBM:          100 + 1*40 = 140
    Tactical Nuke: (100 + 1*40) * 4/10 = 56

and `AI_unitValue` starts from 1, so with no interceptor present their relevant base
values are 141 and 57.

Consider a fully supported isolated/unknown-rivals state in which the AI has SDI.
The game can still have living rivals and therefore `AI_isWarPossible()==true`; the
AI simply has not met a foreign team. Its own team is nevertheless `KNOWN_TO`.
The buggy power-weighted mean interception is therefore exactly its own 60% SDI.

The code then values the same units approximately as:

    ICBM role component:
        140 - 140 * 60 * (100 - 0) / 10000 = 56
        AI_unitValue = 57

    Tactical Nuke role component:
        56 - 56 * 60 * (100 - 50) / 10000 = 40  [integer arithmetic]
        AI_unitValue = 41

The owner has therefore cut the estimated value of its own ICBM from 141 to 57 solely
because IT owns SDI, and also reduced Tactical-Nuke value from 57 to 41.

DIRECT CANDIDATE-SET CONSEQUENCE
--------------------------------
`CvCityAI::AI_bestUnitAI` first finds the largest `AI_unitValue`, then rejects ordinary
candidates below two thirds of that best base value:

    if (iValue < iBestBaseValue*2/3 ...)
        continue;

Without the erroneous self-SDI interception:

    best = ICBM 141
    Tactical Nuke = 57 < 141*2/3 (=94)

so Tactical Nuke is eliminated before production cost, randomization and unit-class
saturation are even considered. `AI_bestUnitAI(UNITAI_ICBM)` cannot choose it while
the ICBM is trainable.

With the erroneous 60% self-SDI interception:

    best = ICBM 57
    Tactical Nuke = 41 >= 57*2/3 (=38)

so Tactical Nuke suddenly enters the production candidate set. Owning a defensive
project has therefore changed which offensive nuclear units the AI is even allowed
to compare.

A natural lifecycle makes the visible choice change stronger: before SDI, the AI can
build an ICBM while Tactical Nuke is filtered out; after SDI, suppose it has one ICBM
and zero Tactical Nukes. `AI_bestUnitAI` then also applies K-Mod's unit-class
saturation factor and divides by remaining production cost. The cheaper unsaturated
Tactical Nuke receives both advantages. For ordinary city production values it can
therefore become the selected `UNITAI_ICBM` unit after SDI, whereas without counting
self-SDI it never reached that comparison at all. This does not require a different
tech/resource state: the changed input is the owner's own interception project.

The exact production winner can also vary through the function's intended random
100-149% factor and city-specific production modifiers; the confirmed defect does
not depend on one particular random draw. The deterministic candidate-set change is
already upstream of those legitimate tie-breaking/production considerations.

ACTUAL INTERCEPTION CONTRACT
----------------------------
`CvUnit::nukeInterceptionChance` constructs the teams affected by the chosen target
and selects the strongest interception coverage among those teams, with AdvCiv also
considering an affected vassal's master. The launcher's own team is not an
interceptor unless it is somehow an affected victim of the target itself.

Thus the production heuristic is not merely using a deliberately pessimistic
approximation of enemy SDI. It is incorporating a defensive capability that has no
corresponding interception path for a normal outgoing strike.

ANCESTRY / ARCHAEOLOGY
----------------------
Civ4CE/BtS has the older nuclear-unit value formula and does not attempt this
power-weighted interception estimate at all.

K-Mod 1.46 introduced the current block. It iterates every alive team for which:

    kTeam.isHasMet(i)

and, in the neutral-area branch, has no `i != getTeam()` exclusion. K-Mod's normal
visibility/contact code calls `GET_TEAM(getTeam()).meet(eTeam, true)` when a plot
becomes visible; on the owner's own visible plots those two team IDs are the same.
So the K-Mod representation can mark the owner as having met itself and feed it to
the new interception loop.

AdvCiv preserves the K-Mod behavior through its agent-iterator rewrite. Its
`AgentPredicates.h` contract explicitly says `KNOWN_TO` includes the first agent's
team, while `OTHER_KNOWN_TO` is the corresponding predicate that excludes it.
Base AdvCiv 1.14 and current SAS both use:

    TeamIter<CIV_ALIVE,KNOWN_TO>

in this exact K-Mod block.

This is therefore best classified as an inherited K-Mod root retained/clarified by
AdvCiv, not a SAS-local change and not a vanilla BtS defect.

DUPLICATE / ROOT SEPARATION
---------------------------
No existing album or current Known-Issues root was found for self-SDI contaminating
`AI_unitValue(UNITAI_ICBM)`.

This is distinct from other nuclear AI work in AdvCiv (`advc.650` target valuation,
nuke deterrence and SDI project value): those systems explicitly examine rival or
affected-team interception. F339 is specifically the unit-production valuation
loop counting the OWNER as a possible interceptor.

REPAIR DIRECTION
----------------
The narrow semantics-preserving repair is to exclude the owner's team from this
expected-enemy-interception tally, e.g. use:

    TeamIter<CIV_ALIVE,OTHER_KNOWN_TO>

instead of `KNOWN_TO`, or add an explicit self-team skip.

Do not remove the whole interception adjustment: K-Mod intentionally wanted rival
SDI/evasion to affect the relative desirability of nuclear units. The confirmed
error is only that the launcher's own interception is allowed into that rival-loss
estimate.

A broader redesign could weight only plausible nuclear target teams and model
vassal/master interception like `nukeInterceptionChance`; that would be a tuning/
accuracy project beyond this finding. F339 requires only the indisputable self-team
exclusion.

CURRENT AUDIT STATE AFTER F339
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F339
F340 / provisional KI#663 — UNUSED
Latest physical checkpoint — C031-WIP27
No repository source files modified.

NEXT CURSOR
-----------
1. Continue downward through the remaining `AI_unitValue` role cases with F340 unused.
2. Challenge role-specific formulas against current XML and actual city/unit-value
   consumers; strange weights alone are not findings.
3. Compare suspicious branches against K-Mod and Civ4CE/BtS to separate inherited
   design from later AdvCiv/SAS regressions.


===============================================================================
C031-WIP28 ADDENDUM — 2026-08-30
F340 / provisional KI#663 CONFIRMED
===============================================================================

F340 — ADVciv's 2020 "SPECIAL CARGO" FIX ACCEPTS A SPECIAL-CARGO SHIP FOR
EVERY SPECIAL-CARRIER UnitAI ROLE, EVEN WHEN IT CANNOT CARRY THAT ROLE'S CARGO

STATUS
------
CONFIRMED current inherited AdvCiv regression. The defect was introduced by AdvCiv
commit 8c833b49222cf8a8dc16bd0f25a1fb7399e04832 (2020-01-01), titled:

    Fix BtS bug in AI evaluation of special cargo units

Civ4CE/BtS and K-Mod use the requested UnitAI role in the SpecialUnit carrier-role
check. AdvCiv changed that check to the loop variable and thereby broadened every
special-cargo carrier into every special-carrier sea role. Current SAS inherits that
AdvCiv change unchanged.

ROOT
----
`CvPlayerAI::AI_unitValue` starts by asking whether the unit is valid for the requested
`eUnitAI`. XML can establish validity directly through `u.getUnitAIType(eUnitAI)`.
When XML does not, the function has role-specific fallback tests.

For the four special-cargo sea roles, current Base AdvCiv and SAS execute:

    case UNITAI_MISSIONARY_SEA:
    case UNITAI_SPY_SEA:
    case UNITAI_CARRIER_SEA:
    case UNITAI_MISSILE_CARRIER_SEA:
        if (u.getCargoSpace() > 0 && u.getSpecialCargo() != NO_SPECIALUNIT)
        {
            FOR_EACH_ENUM(UnitAI)
            {
                // advc.001: was ...isCarrierUnitAIType(eUnitAI)
                if (GC.getInfo(u.getSpecialCargo()).
                    isCarrierUnitAIType(eLoopUnitAI))
                {
                    bValid = true;
                    break;
                }
            }
        }
        break;

The loop never asks whether the unit's SpecialCargo supports THE REQUESTED `eUnitAI`.
It merely asks whether the SpecialUnit supports ANY carrier UnitAI anywhere in its
`CarrierUnitAIs` table.

All three shipped special cargo classes support at least one carrier role, so any ship
with any of those SpecialCargo values and positive cargo space passes all four cases.
For example, a Missile Cruiser carrying `SPECIALUNIT_MISSILE` becomes "valid" for
`UNITAI_MISSIONARY_SEA` merely because `SPECIALUNIT_MISSILE` supports
`UNITAI_MISSILE_CARRIER_SEA`.

That is not merely a role-label mismatch. The physical cargo contract remains strict.
`CvUnit::cargoSpaceAvailable` returns zero whenever the transport's `specialCargo()`
differs from the cargo unit's `SpecialUnitType`, and also checks DomainCargo. Therefore
a Missile Cruiser or Carrier selected as `UNITAI_MISSIONARY_SEA` cannot actually load
a missionary/spy/other `SPECIALUNIT_PEOPLE` land unit.

ANCESTRY / THE 2020 "FIX"
--------------------------
Civ4CE/BtS contains the same old redundant loop as K-Mod, but its condition is:

    isCarrierUnitAIType(eUnitAI)

K-Mod 1.46 preserves that condition. The loop variable is unused, which is ugly and
makes the loop itself unnecessary, but the SEMANTIC QUESTION is correct: does this
unit's SpecialCargo permit the role being evaluated?

AdvCiv practical commit 1897 / SHA 8c833b4922 changed exactly that parameter:

    - isCarrierUnitAIType(eUnitAI)
    + isCarrierUnitAIType(eLoopUnitAI)

and added:

    // advc.001: was ...isCarrierUnitAIType(eUnitAI)

The commit title calls this a BtS bugfix. Its full message says:

    "I think the error only meant that the AI had to rely on the Unit AI types set in
    Civ4UnitInfos.xml for special cargo units. So only a potential problem for
    mod-mods."

AdvCiv's manual repeats that diagnosis as a "Wrong parameter" bug.

The source semantics show the opposite for this parameter. If XML does not explicitly
list the requested role, the old fallback already recognizes a special-cargo unit when
its SpecialUnitInfo lists THAT REQUESTED carrier role. The pointless loop was a code
smell, but `eUnitAI` was the meaningful operand. Replacing it with the loop variable
turns a requested-role predicate into an existential "supports any carrier role"
predicate and introduces the cross-role acceptance.

CURRENT SAS DATA — THREE DISJOINT SPECIAL-CARGO SHIPS
------------------------------------------------------
Current SAS `CIV4SpecialUnitInfos.xml` keeps the carrier-role categories distinct:

    SPECIALUNIT_FIGHTER:
        UNITAI_CARRIER_SEA

    SPECIALUNIT_PEOPLE:
        UNITAI_SPY_SEA
        UNITAI_MISSIONARY_SEA
        UNITAI_ASSAULT_SEA

    SPECIALUNIT_MISSILE:
        UNITAI_MISSILE_CARRIER_SEA

Current SAS has three ordinary sea units with non-NONE SpecialCargo:

    Missile Cruiser
        SpecialCargo = SPECIALUNIT_MISSILE
        DomainCargo  = DOMAIN_AIR
        cargo = 4, moves = 7, combat = 26, cost = 250
        XML UnitAIs: ATTACK_SEA, RESERVE_SEA, MISSILE_CARRIER_SEA

    Attack Submarine
        SpecialCargo = SPECIALUNIT_PEOPLE
        DomainCargo  = DOMAIN_LAND
        cargo = 1, moves = 6, combat = 24, cost = 200
        XML UnitAIs: RESERVE_SEA, EXPLORE_SEA,
                     MISSIONARY_SEA, SPY_SEA

    Carrier
        SpecialCargo = SPECIALUNIT_FIGHTER
        DomainCargo  = DOMAIN_AIR
        cargo = 3, moves = 5, combat = 16, cost = 180
        XML UnitAIs: CARRIER_SEA

Thus the intended late people-carrier is the Attack Submarine. Carrier cannot carry
people because it accepts Fighters/Air; Missile Cruiser cannot carry people because it
accepts Missiles/Air.

SUPPORTED PRODUCTION CONSEQUENCE
--------------------------------
This reaches ordinary city production directly.

`CvCityAI::AI_chooseProduction` contains a shipped path that, when the AI is not in a
land war, is in financial trouble, has a land missionary, and lacks a missionary sea
unit in the water area, calls:

    AI_chooseUnit(UNITAI_MISSIONARY_SEA)

The default `iOdds` for this requested-role overload is -1, so once a best unit exists
there is no additional random "should we build it?" gate.

`AI_chooseUnit` calls:

    AI_bestUnitAI(UNITAI_MISSIONARY_SEA)

and `AI_bestUnitAI` evaluates every trainable civilization unit through the exact
`AI_unitValue(eUnit, UNITAI_MISSIONARY_SEA, area())` validation above.

Take an ordinary late-game civ/city that can train all three generic ships. Current
`UNITAI_MISSIONARY_SEA` role value is:

    1 + iAIWeight + iCombatValue/2 + moves*200 + cargo*500

(no current one of these ships has a relevant positive iAIWeight or impassable penalty).
Ignoring the small combat term for clarity gives approximately:

    Missile Cruiser:  1 + 7*200 + 4*500 = 3401 + combat/2
    Carrier:          1 + 5*200 + 3*500 = 2501 + combat/2
    Attack Submarine: 1 + 6*200 + 1*500 = 1701 + combat/2

The wrong-role Missile Cruiser is therefore the highest BASE role value. More
importantly, `AI_bestUnitAI` rejects any ordinary candidate below two thirds of that
best base value BEFORE cost/random/promotion comparison:

    if (iValue < iBestBaseValue * 2 / 3)
        continue;

The valid Attack Submarine is deterministically below two thirds of the Missile
Cruiser. This remains true for any positive `getBestLandUnitCombat()` denominator in
`AI_combatValue`; the tiny combat normalization cannot bridge the ~1700-point cargo/
movement gap.

The Carrier is above two thirds and therefore survives too. The result is especially
bad: once all three are trainable, the only physically valid `SPECIALUNIT_PEOPLE`
carrier is filtered out, while the surviving candidate set consists of ships that
cannot carry the missionary at all. Production-cost/random/saturation logic can choose
between Carrier and Missile Cruiser, but either result defeats the requested role.

So this is not a hypothetical mod-mod XML concern. Current SAS can ask for a missionary
transport and spend hammers on a Carrier or Missile Cruiser that is mechanically unable
to load the missionary, while excluding its actual Attack Submarine people carrier.
The same root can cross-contaminate SPY_SEA, CARRIER_SEA and MISSILE_CARRIER_SEA in the
opposite directions as well.

ADDITIONAL LIFECYCLE SUPPORT
----------------------------
The wrong broad `AI_unitValue` predicate is also reachable outside fresh production.
Some inherited sea-unit reclassification paths call `AI_unitValue(..., SPY_SEA)` or
`AI_unitValue(..., MISSIONARY_SEA)` directly when considering a new UnitAI role. Those
paths therefore can also regard a special carrier as suitable for cargo it cannot
transport. Not all transform paths are affected because some first require the unit's
XML UnitAI flag; this finding does not rely on those secondary paths.

DUPLICATE / ROOT SEPARATION
---------------------------
No existing album or current `README_Known_Issues.md` root was found for AdvCiv's
`isCarrierUnitAIType(eLoopUnitAI)` cross-role validation.

The earlier album control around `PUF_isEnemyCityAttacker` merely catalogued the same
three non-NONE-SpecialCargo ships to prove that a generic invasion-transport shortcut
does NOT classify them as generic land transports. That is a different function and
a different contract.

F340 is also separate from XML balancing choices such as giving Attack Submarine
MISSIONARY_SEA/SPY_SEA. Those XML flags are internally coherent with
`SPECIALUNIT_PEOPLE`; the bug is that C++ admits incompatible `SPECIALUNIT_FIGHTER` and
`SPECIALUNIT_MISSILE` carriers into those roles anyway.

REPAIR DIRECTION
----------------
Restore the requested-role predicate and delete the redundant loop entirely:

    if (u.getCargoSpace() > 0 && u.getSpecialCargo() != NO_SPECIALUNIT &&
        GC.getInfo(u.getSpecialCargo()).isCarrierUnitAIType(eUnitAI))
    {
        bValid = true;
    }

This keeps the useful fallback that the 2020 commit was trying to preserve: a mod-mod
need not explicitly add the UnitAI flag to every special carrier, provided its
SpecialUnitInfo says that the requested carrier role can carry that SpecialUnit class.
It simply prevents a Fighter carrier from satisfying a Missile/People role and vice
versa.

No XML change is required to fix the root.

CURRENT AUDIT STATE AFTER F340
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F340
F341 / provisional KI#664 — UNUSED
Latest physical checkpoint — C031-WIP28
No repository source files modified.

NEXT CURSOR
-----------
1. Continue downward through `AI_unitValue` with F341 unused.
2. Recheck the remaining air/naval role-validity fallbacks against current XML and
   physical unit contracts before spending a finding number.
3. Then continue through the post-switch promotion/value tail and the next inherited
   unit/economic valuation helpers.


===============================================================================
C031-WIP29 ADDENDUM — F341 CONFIRMED
===============================================================================

F341 / provisional KI#664 — `AI_isExcessSeaExplorers` subtracts a PLAYER-GLOBAL
sea-explorer training count from a WATER-AREA-LOCAL count, allowing explorer builds
for unrelated oceans to suppress excess-explorer retirement in this ocean.

STATUS
------
CONFIRMED — inherited AdvCiv `advc.017b` scope mismatch; current SAS retains it.

ROOT
----
Current `CvPlayerAI::AI_isExcessSeaExplorers` does:

    int iHave = AI_totalWaterAreaUnitAIs(kWaterArea, UNITAI_EXPLORE_SEA) -
            AI_getNumTrainAIUnits(UNITAI_EXPLORE_SEA);
    return (AI_neededExplorers(kWaterArea) < iHave + iChange);

The first term is deliberately scoped to `kWaterArea`. The subtraction is not:
`AI_getNumTrainAIUnits` is the player's global count for that UnitAI across all cities.

BOOKKEEPING / NO HIDDEN DOUBLE-COUNT
------------------------------------
This is not compensating for a hidden global duplicate inside
`AI_totalWaterAreaUnitAIs`.

When a city queues a unit, `CvCity::pushOrder` increments:

    getArea().changeNumTrainAIUnits(getOwner(), eUnitAI, 1);
    GET_PLAYER(getOwner()).AI_changeNumTrainAIUnits(eUnitAI, 1);

For a coastal city, `getArea()` is the CITY'S LAND AREA. Thus a queued naval explorer
is not entered into the target water area's `CvArea::m_aaiNumTrainAIUnits` simply by
being naval.

`AI_totalWaterAreaUnitAIs(kWaterArea, ...)` compensates for exactly that by scanning
cities whose `waterArea() == &kWaterArea` and adding that city's local
`getNumTrainUnitAI`. Therefore only explorer orders in coastal cities attached to the
requested sea are added to the water-area count.

Subtracting the global player training counter correctly removes those LOCAL queued
explorers when the helper wants to judge already-existing explorers, but it also
subtracts explorer orders from every OTHER disconnected sea. There is no matching
foreign-area addition for those remote orders.

CONCRETE CURRENT CONSEQUENCE
----------------------------
Take two disconnected oceans A and B.

Ocean A:
    `AI_neededExplorers(A) == 0`
    one existing `UNITAI_EXPLORE_SEA`
    no explorer being trained for A

Ocean B:
    one coastal city is training one `UNITAI_EXPLORE_SEA`

Then for A:

    AI_totalWaterAreaUnitAIs(A, EXPLORE_SEA) == 1
    AI_getNumTrainAIUnits(EXPLORE_SEA)       == 1   // the unrelated B order
    iHave                                    == 0

and:

    AI_neededExplorers(A) < iHave
    0 < 0  -> false

So the explorer in fully explored ocean A is declared NOT excess solely because a
replacement/explorer is being built for disconnected ocean B.

Without the unrelated B order, `iHave == 1` and `0 < 1` is true, so exactly the same
unit is correctly recognized as excess.

LIVE UNIT-AI EFFECT
-------------------
`CvUnitAI::AI_exploreSeaMove` periodically (13% synchronized chance) asks this helper
whether its current sea explorer is excess. When true, AdvCiv deliberately tries to
re-purpose the ship into another useful naval role; if no suitable role survives, the
later excess path can scrap it when unit maintenance is positive.

The cross-ocean subtraction therefore suppresses a real retirement/reclassification
path. The effect is bounded by remote training state in a simple one-order example,
but it is systematic and can recur or persist when an empire has multiple disconnected
coasts successively training explorers. Each unrelated queued explorer can mask one
existing explorer from every other sea's local excess test.

This is exactly the kind of area isolation this helper otherwise tries to preserve:
`AI_neededExplorers` and `AI_totalWaterAreaUnitAIs` are both evaluated per water area.
The global subtraction breaks only the final reconciliation.

ANCESTRY
--------
K-Mod has `AI_totalWaterAreaUnitAIs` and the global/player training counter but does not
have `AI_isExcessSeaExplorers` in this AdvCiv form. Base AdvCiv 1.14 contains the same
`advc.017b` helper and the same global subtraction as current SAS. This root is therefore
inherited AdvCiv rather than created by SAS.

The older K-Mod changelog's sea-explorer fixes do not repair this later helper scope;
this specific `AI_isExcessSeaExplorers` reconciliation is AdvCiv code.

DUPLICATE / ROOT SEPARATION
---------------------------
No existing album/Known-Issues root was found for this exact explorer helper.

SAS already has a closely related design precedent elsewhere: global queued naval-role
guards have been rejected when they suppress valid behavior on unrelated seas, because
water-area helper counts already provide the correct local scope. F341 is the inherited
mirror image: a global queued explorer count suppresses local retirement rather than
local construction.

REPAIR DIRECTION
----------------
Preserve the apparent purpose — exclude explorers currently being trained for THIS sea
when deciding whether an existing explorer is excess — but subtract a water-area-local
training count rather than `AI_getNumTrainAIUnits`.

A simple robust direction is to expose/reuse the same coastal-city training component
that `AI_totalWaterAreaUnitAIs(kWaterArea, EXPLORE_SEA)` adds, e.g. sum
`getNumTrainUnitAI(UNITAI_EXPLORE_SEA)` only for this player's cities whose
`waterArea() == &kWaterArea`, then subtract that local sum.

Do not merely remove the subtraction without checking the original `advc.017b` intent:
local queued explorers would then make existing ships look excess while their
replacement is still under construction, reintroducing the oscillation/inconsistency
that the helper was designed to avoid.

CURRENT AUDIT STATE AFTER F341
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F341
F342 / provisional KI#665 — UNUSED
Latest physical checkpoint — C031-WIP29
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the SAS-local combat-count helpers immediately following
   `AI_unitValue`, especially whether class-based proxy classification can diverge from
   actual owned unit types.
2. Then continue into `AI_countUnimprovedBonuses` / `AI_isUnimprovedBonus` and the
   inherited worker-demand helpers, preserving producer->contract->consequence proof.


===============================================================================
C031-WIP30 ADDENDUM — F342 CONFIRMED
===============================================================================

F342 / provisional KI#665 — `AI_exploreSeaMove` retains AdvCiv's positive
`AI_countUnimprovedBonuses(..., 5)` look-ahead when converting an excess/outdated
exploring Work Boat into `UNITAI_WORKER_SEA`. Positive look-ahead deliberately calls
`canBuild(..., bTestVisible=true)` and can therefore admit an imminent-border resource
whose improvement tech is not actually known. The unit changes role immediately, but
`AI_workerSeaMove` then uses real executable `canBuild` checks and can have no work at
all. Current SAS already fixed the same future-tech false demand on the city-production
side by changing `AI_neededSeaWorkers` from look-ahead 5 to 0; the inherited transform
path was left unreconciled.

STATUS
------
CONFIRMED — inherited AdvCiv transform bug still present in SAS; current SAS's explicit
Work Boat demand repair supplies especially strong contract evidence that this remaining
positive-look-ahead use is wrong for an immediate role change.

ROOT
----
Inside current `CvUnitAI::AI_exploreSeaMove`, when a non-human sea explorer is judged
excess or outdated, AdvCiv tries several alternative UnitAI roles. `UNITAI_WORKER_SEA`
is first. Immediately before changing the role it says:

    // Before transforming a Work Boat, check if there is sth. to improve.
    if(transformTypes[i] == UNITAI_WORKER_SEA &&
        kOwner.AI_countUnimprovedBonuses(*pWaterArea, plot(), 5) <= 0)
    {
        continue;
    }
    AI_setUnitAIType(transformTypes[i]);
    AI_update();
    return;

The comment calls this the "last check before changing the AI type", but the helper's
positive `iLookAhead` does not mean "work this unit can perform now".

`CvPlayerAI::AI_countUnimprovedBonuses` uses the positive look-ahead only for unowned
plots expected to enter the first cultural expansion soon. For those plots it calls:

    AI_isUnimprovedBonus(p, pFromPlot, false)

and `AI_isUnimprovedBonus(..., bCheckPath=false)` in turn tests connecting builds with:

    canBuild(p, eLoopBuild, false, !bCheckPath)

so this becomes `canBuild(..., false, true)` — `bTestVisible=true`.

`CvPlayer::canBuild` explicitly permits a missing build prerequisite tech in visible
mode unless that tech belongs more than one era beyond the player's current era. It
also skips the normal feature-tech and gold-cost checks in visible mode. Thus this
look-ahead is a UI/future-possibility style test, not an executable Work Boat mission
contract.

CURRENT SAS CONTRACT EVIDENCE
-----------------------------
Current `CvCityAI::AI_neededSeaWorkers` contains a SAS repair documenting this exact
problem and deliberately changed both inherited calls from look-ahead 5 to 0:

    const int iLookAhead = 0;

The accompanying source comment explains that positive look-ahead sets
`bCheckPath=false`, which makes `canBuild` use `bTestVisible=true`, and that this was
causing Work Boats for resources such as ocean Whales that could not actually be
improved yet. The stated intended production behavior is to count bonuses that are
owned, reachable and buildable NOW.

That repair did not change the separate `AI_exploreSeaMove` last-check, which still
passes literal 5 in current SAS.

CONCRETE CURRENT XML REPRODUCTION
---------------------------------
Current SAS supplies a direct early-game example without hypothetical mod-mod data:

* `UNIT_WORKBOAT`
  - prerequisite tech: `TECH_FISHING`
  - default/explicit role includes `UNITAI_WORKER_SEA`
  - can build `BUILD_WHALING_BOATS`
  - has two moves, no cargo, and `bNoRevealMap=0`
* `AI_unitValue(UNIT_WORKBOAT, UNITAI_EXPLORE_SEA, ...)` accepts it through the
  inherited sea-explorer fallback (`cargoSpace <= 1 && !isNoRevealMap()`), so ordinary
  city `AI_chooseUnit(UNITAI_EXPLORE_SEA)` can legitimately train a Work Boat as a sea
  explorer even though its XML default role is worker-sea.
* `BONUS_WHALE` has `TechReveal=NONE`, so the Whale is visible before Seafaring.
* `BUILD_WHALING_BOATS` requires `TECH_SEAFARING`.
* `TECH_SEAFARING` is Ancient Era.

Take an Ancient player that knows Fishing but not Seafaring. It has an exploring Work
Boat and no currently owned/buildable sea bonus. A Whale lies on an unowned city-radius
plot in the same water area, and the city's first border expansion is within the
look-ahead window.

`AI_countUnimprovedBonuses(..., 5)` examines that unowned Whale. Because Whale is
already revealed and Seafaring is in the current era, `canBuild(...,
bTestVisible=true)` does not reject the missing Seafaring prerequisite. The helper
therefore returns positive.

If the exploring Work Boat reaches the normal `bTransform` branch — e.g. the 13%
excess-explorer reconsideration succeeds — `UNITAI_WORKER_SEA` is its first transform
candidate. The Work Boat has that role flag and positive worker-sea `AI_unitValue`, so
the false-positive Whale passes the last check and the unit immediately changes from
`UNITAI_EXPLORE_SEA` to `UNITAI_WORKER_SEA`.

LIVE CONSEQUENCE AFTER TRANSFORMATION
-------------------------------------
`CvUnitAI::AI_workerSeaMove` does not use the future-visible test. Its first productive
choice is `AI_improveBonus()`, and that helper scans OWNED plots and ultimately requires
ordinary executable `canBuild(kPlot, eBestBuild)`.

In the reproduction there is no owned actionable sea bonus and the only anticipated
Whale cannot be improved without Seafaring anyway. `AI_improveBonus()` therefore
returns false. The worker-sea routine then only tries retreat/safety and finally:

    getGroup()->pushMission(MISSION_SKIP);

There is no `UNITAI_WORKER_SEA` -> `UNITAI_EXPLORE_SEA` recovery path in
`AI_workerSeaMove`. Repository-wide checks of this role path show the automatic
explorer conversions occur in other naval-role routines, not in worker-sea movement.
Thus the false-positive last check can take a useful explorer out of exploration and
leave it repeatedly idling as a worker until a genuinely executable sea-bonus job
appears. If the missing tech is the blocker, that can last far beyond the nominal
five-turn cultural look-ahead.

Even when the improvement tech is already known, the positive look-ahead can still
convert the explorer before the tile is actually owned, producing several turns of
avoidable worker idling. The missing-tech Whale case demonstrates that the mismatch is
not merely deliberate anticipation of an imminent border pop.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same `AI_exploreSeaMove` transform check with literal
look-ahead 5. This is part of the AdvCiv sea-explorer transform/reclassification path
(`advc.017b` neighborhood), not a new SAS transform implementation.

Base AdvCiv also used positive look-ahead in `AI_neededSeaWorkers`; SAS later repaired
that production-side use by changing it to 0 after observing future-tech/unreachable
Work Boat demand. The transform-side use remained unchanged. Therefore the underlying
false-positive transform is inherited AdvCiv, while SAS currently contains an
incomplete reconciliation: its production contract has been corrected but its
explorer-role conversion still applies the old future-visible semantics.

DUPLICATE / ROOT SEPARATION
---------------------------
KI#157 covers later Work Boat production/availability and movement-target mismatches:
matching need to the correct water areas, avoiding unrelated global queued-boat
suppression, allowing owned off-BFC seafood to be resolved, and skipping already
connected targets. It does not document or repair an exploring Work Boat changing into
worker-sea because an unowned/future-tech look-ahead target passes visible-mode
`canBuild`.

The earlier SAS `AI_neededSeaWorkers` look-ahead=0 source comment is relevant evidence
for F342 but is not itself this finding; F342 is the still-live role-transition caller.

This is also separate from F341. F341 can incorrectly delay whether an explorer is
considered excess because of unrelated-ocean training. F342 starts only once
`bTransform` is already true and can choose the wrong replacement role from a
non-executable future target.

REPAIR DIRECTION
----------------
For the narrow SAS-consistency repair, make the final `UNITAI_WORKER_SEA` transform
check use the same current-actionability semantics as current `AI_neededSeaWorkers`,
i.e. call `AI_countUnimprovedBonuses(..., 0)` rather than 5.

That is deliberately conservative for a role transition: do not convert an explorer
until there is an owned, pathable, currently buildable sea-bonus target. A city may
still produce/retain exploration normally until the work actually exists.

If retaining border-expansion anticipation is desired, do not reuse the current
positive-look-ahead helper unchanged. Split "future ownership / skip border path" from
"visible-mode build legality" so the anticipated plot can be tested without allowing
missing prerequisite techs. The core requirement is that the LAST CHECK before an
immediate UnitAI change must agree with the executable build contract used by
`AI_workerSeaMove`.

CURRENT AUDIT STATE AFTER F342
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F342
F343 / provisional KI#666 — UNUSED
Latest physical checkpoint — C031-WIP30
No repository source files modified.

NEXT CURSOR
-----------
1. Continue downward through `AI_countCityFeatures`, `AI_neededWorkers`, and adjacent
   economic/worker demand helpers with F343 unused.
2. Preserve the current methodology: prove an actual current producer and consequence;
   dormant mod-mod classification hazards remain control notes only.
3. Reconcile any new worker/bonus finding against KI#157, KI#195, KI#216 and the recent
   SAS worker/Work Boat changes before spending another finding number.


===============================================================================
F343 / provisional KI#666 — AI_countOwnedBonuses double-counts one neutral
shared-radius resource once per city, inflating physical resource multiplicity
===============================================================================

STATUS
------
CONFIRMED — inherited BtS/K-Mod physical-count bug still present in AdvCiv/SAS.
Owned shared-radius resources are explicitly deduplicated through getWorkingCity(),
but the parallel neutral/easily-claimed branch has no unique-plot ownership and can
count the same physical plot from every overlapping city radius.

ROOT
----
Current `CvPlayerAI::AI_countOwnedBonuses` says it counts bonuses "inside city radius
or easily claimed" by summing the city helper independently for every city:

    FOR_EACH_CITYAI(pCity, *this)
    {
        iCount += pCity->AI_countNumBonuses(eBonus, true,
                pCity->getCommerceRate(COMMERCE_CULTURE) > 0, -1);
        if (iCount >= iMaxCount)
            return iMaxCount;
    }

`CvCityAI::AI_countNumBonuses` correctly makes an OWNED shared-radius plot belong to
only one city for counting purposes:

    if (bIncludeOurs && ePlotOwner == eOwner &&
        kPlot.getWorkingCity() == this)
    {
        iCount++;
    }

but its immediately following neutral branch is simply:

    else if (bIncludeNeutral && !kPlot.isOwned())
        iCount++;

There is no `getWorkingCity()` equivalent for an unowned plot and no visited-plot set
in the player-level sum. `CityPlotIter` visits the full workable radius of each city.
Therefore one unowned bonus lying in the overlapping radii of two culturally active
cities is added twice; three overlapping cities can add it three times, etc.

This is not compensating for some other omission. Bonuses already owned inside city
radii are deliberately assigned to one working city, while owned bonuses outside all
city radii are counted in a separate unique map-wide pass. Only the speculative
neutral/easily-claimed part lacks physical-plot uniqueness.

CURRENT CONTRACT EVIDENCE
-------------------------
The callers do not treat this return value as "number of city opportunities". They
explicitly use it as the number of physical resource copies.

In the improvement/resource part of `AI_techValue`, current SAS even labels the result:

    int iBonuses = (bRevealed ? AI_countOwnedBonuses(eLoopBonus) : // actual count
        ...);

and later adds value for extra copies through `(iBonuses - 1)`.

More decisively, the K-Mod resource-enabling branch caps the helper at 2 and uses the
result as a binary "one copy versus multiple copies" distinction:

    int iOwned = AI_countOwnedBonuses(eLoopBonus, 2);
    if (iOwned > 0)
    {
        int iEnableValue = 4;
        iEnableValue += AI_bonusVal(eLoopBonus, 1, true) * iCityCount;
        iEnableValue *= (iOwned > 1) ? 120 : 100;
        iEnableValue /= 100;
        iValue += iEnableValue;
    }

Thus a duplicate city-radius incidence is directly misinterpreted as a second physical
copy and earns the 20% multiple-copy modifier.

CURRENT SAS REPRODUCTION
------------------------
Current SAS supplies a clean ordinary example with `BONUS_WHALE`:

* `BONUS_WHALE` has `TechReveal=NONE`, so it is already visible.
* Its `TechCityTrade` is `TECH_SEAFARING`.
* Put one Whale on an unowned water plot that lies in the outer workable ring of two
  coastal cities belonging to the same AI.
* Both cities have positive culture commerce. They can already have their first border
  expansion while the shared outer-ring Whale remains neutral until the later border
  expansion; this need not be a one-turn founding edge.

When the AI evaluates `TECH_SEAFARING`, the resource-enabling branch calls:

    AI_countOwnedBonuses(BONUS_WHALE, 2)

Each city calls `AI_countNumBonuses(..., bIncludeNeutral=true, ...)`. The shared Whale
is unowned and therefore passes the neutral branch in BOTH city iterations. The
player-level helper reaches its cap and returns 2 although there is only one Whale on
the map in those two radii.

`AI_techValue` consequently applies the 120% "multiple copies" multiplier instead of
100%. The earlier build/improvement valuation can likewise see `iBonuses == 2` and add
an extra-copy term. This directly biases research ranking toward Seafaring based on a
nonexistent second resource copy.

The same helper is also used in corporation and civic valuation as literal resource
multiplicity. For `AI_corporationValue(pCity == NULL)`, prerequisite-resource counts are
summed through `AI_countOwnedBonuses` and then multiply the corporation's yield and
commerce effects (and maintenance). A neutral shared-radius corporation resource can
therefore also inflate global corporation estimates while it remains unowned. The
tech example is sufficient on its own and does not rely on that later-game state.

ANCESTRY
--------
This architecture is inherited all the way from BtS.

Civ4CE/BtS `CvPlayer::countOwnedBonuses` first counts owned bonuses outside city radii,
then loops every city and adds:

    AI_countNumBonuses(eBonus, true,
        pLoopCity->getCommerceRate(COMMERCE_CULTURE) > 0, -1)

K-Mod retains the same structure (plus its bonus-revealed shortcut). AdvCiv's
`advc.042` explicitly says `AI_countOwnedBonuses` was cut from `CvPlayer.cpp` "w/o
functional changes"; later AdvCiv optimizations added `iMaxCount` and cleaned up
Advanced Start handling but retained the per-city neutral sum.

So this is an inherited BtS/K-Mod root preserved by AdvCiv and current SAS, not a
SAS-local regression.

DUPLICATE / ROOT SEPARATION
---------------------------
This is distinct from the nearby `AI_countCityFeatures` overlap question. That helper
counts city-radius FEATURE incidences, and at least one important consumer (per-city
feature happiness) genuinely needs one incidence per affected city. Its semantics are
therefore mixed.

`AI_countOwnedBonuses`, by contrast, has direct callers that call the value an "actual
count", cap it at two to ask whether more than one copy exists, and multiply
corporation input effects by that count. One physical resource must therefore be unique
regardless of how many city radii overlap it.

This is also separate from F342. F342 concerns future-visible build legality when
turning an explorer into a worker-sea role. F343 concerns physical resource
multiplicity and requires no worker or pathfinding behavior.

REPAIR DIRECTION
----------------
Preserve the intended "easily claimed" anticipation, but count each neutral physical
plot at most once for the player.

A robust repair is to make the neutral part plot-unique at player scope rather than
blindly summing every city's neutral incidences. For example:

1. Keep owned in-radius bonuses deduplicated through the existing working-city rule.
2. Count neutral candidate plots in a separate unique map/plot pass, adding a plot only
   if at least one culturally active city would qualify it under the existing
   land/water city-radius rules.

Alternatively maintain an explicit visited-plot set while aggregating neutral city
radius candidates. In Civ4's C++03 environment a compact plot-index marker/vector is
safer than trying to assign an unowned plot to `getWorkingCity()`, because an unowned
plot normally has no working-city owner assignment.

Do NOT merely turn `bIncludeNeutral` off: the original helper intentionally anticipates
resources that are easy to claim, and tech valuation depends on that behavior. The
bug is duplicate physical multiplicity, not the anticipation itself.

CURRENT AUDIT STATE AFTER F343
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F343
F344 / provisional KI#667 — UNUSED
Latest physical checkpoint — C031-WIP31
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_neededWorkers` and adjacent worker/economic helpers with F344
   unused, preserving KI#157/KI#195/KI#216 as duplicate/control boundaries.
2. Continue downward through corporation/resource helpers after the immediate worker
   tranche; F343 already owns neutral-overlap physical-count duplication.
3. Keep distinguishing physical-count helpers from deliberately per-city incidence
   helpers rather than applying a blanket "overlap is bad" rule.


===============================================================================
F344 / provisional KI#667 — AI_isFocusWar never records the first pushover war,
so its explicit "multiple weak wars" focus exception can never trigger
===============================================================================

STATUS
------
CONFIRMED — inherited AdvCiv one-line state bug introduced by the 2021 advc.105
pushover-war refinement; still present unchanged in Base AdvCiv 1.14 and current SAS.

ROOT
----
Current `CvPlayerAI::AI_isFocusWar` intentionally avoids treating a war against a far
weaker civilization as something that should dominate the AI's economy. The source
explicitly preserves an exception when there are multiple such wars:

    // Much weaker targets aren't worth focusing on (except maybe when there are multiple)
    bool bFirst = true;
    for (TeamIter<CIV_ALIVE,KNOWN_POTENTIAL_ENEMY_OF> itTeam(getTeam());
        itTeam.hasNext(); ++itTeam)
    {
        if (kOurTeam.AI_getWarPlan(itTeam->getID()) != NO_WARPLAN)
        {
            if (!kOurTeam.AI_isPushover(itTeam->getID()))
                return true;
            ... find a member capital ...
            if (pEnemyCapital != NULL && pEnemyCapital->isArea(*pArea))
            {
                if (!bFirst) // Multiple wars
                    return true;
                bFirst = true;
            }
        }
    }
    return false;

`bFirst` starts true and is never assigned false. Consequently `if (!bFirst)` is
unreachable. The first matching pushover war leaves the flag exactly as it began, the
second matching pushover war sees true again, and any further matching wars do the
same.

The only coherent implementation of the written state machine is to mark the first
match as seen after it fails the `!bFirst` test — i.e. `bFirst = false` (or, more
readably, replace the variable with a `bFoundPushoverWarInArea` flag).

INTRODUCTION / ANCESTRY
-----------------------
The defect is directly visible at its introduction.

AdvCiv commit `29ec9f8034a42209c84136c2baca2491453f0e2b`, dated 2021-02-15, is titled:

    AI: Be less distracted by wars against far weaker civs
    [change id advc.105]

Before that commit `AI_isFocusWar` broadly returned true for chosen wars (plus the
existing defensive/alert cases). The commit introduced `CvTeamAI::AI_isPushover` and
replaced that broad behavior with the current per-war loop. The added diff contains
exactly:

    // Much weaker targets aren't worth focusing on (except maybe when there are multiple)
    bool bFirst = true;
    ...
    if (pEnemyCapital != NULL && pEnemyCapital->isArea(*pArea))
    {
        if (!bFirst) // Multiple wars
            return true;
        bFirst = true;
    }

Thus this is not an ambiguous later refactor or SAS edit. The typo entered in the
feature commit that created the exception. Base AdvCiv 1.14 and SAS retain it.
K-Mod/BtS do not contain this later AdvCiv pushover-focus loop.

SUPPORTED CURRENT REPRODUCTION
------------------------------
Take an AI that:

* has a capital and is not currently `AI_STRATEGY_ALERT2`;
* has chosen war plans against two enemy teams;
* both enemy teams satisfy current `CvTeamAI::AI_isPushover` (e.g. two almost-defeated
  one-city rivals against a much larger/more powerful AI);
* both enemy capitals are in the AI capital's land area; and
* the area is not independently `AREAAI_DEFENSIVE` in a state that makes the earlier
  checks sufficient by themselves.

`AI_isAnyChosenWar()` permits the function to enter the pushover loop. Both war-plan
teams are deliberately rejected by the `!AI_isPushover` fast return. The first capital
passes the area test, but `bFirst` remains true. The second capital passes too, yet
`!bFirst` is still false. The function returns false.

The source comment and branch structure say that the second such war is the intended
point at which the AI should return true.

LIVE CONSEQUENCES
-----------------
`AI_isFocusWar` is a central AdvCiv replacement for many older raw "has a war plan"
checks, so this false negative propagates beyond the helper itself.

A particularly direct consequence occurs in `AI_doTurnUnitsPost`:

    bool const bUpgradeFocusWar = AI_isFocusWar();
    if (bUpgradeFocusWar)
        iUpgradeBudget = std::min(getGold(), AI_goldTarget(true));
    else
        ... peacetime-style proportional budget ...

and `AI_goldTarget` itself divides the upgrade component by 4 (or by 10 in some states)
when `AI_isFocusWar()` is false. Thus an AI fighting the exact multiple-pushover case
that the source intended to count as war focus can reserve/spend only a fraction of its
intended emergency upgrade budget.

`AI_maxUnitCostPerMil(NULL)` is another explicit quantitative consumer. When focus-war
is true it adds:

    iMaxUnitSpending += 55;

whereas the false result omits that allowance (apart from unrelated total-war/dagger
adjustments). This can suppress military production/spending during two simultaneous
weak wars.

Other current consumers use `AI_isFocusWar` to:

* run CRUSH UnitAI conversions (`AI_doTurnUnitsPre`);
* tolerate a lower financial safety margin during war;
* double selected military/mobility tech value and mark tech-unit valuation as wartime;
* reduce infrastructure emphasis while fighting;
* alter civic/hurry valuation and a large number of production/mission heuristics.

The exact downstream effect depends on the surrounding strategy state, but the false
negative is therefore behaviorally live and broad.

DUPLICATE / ROOT SEPARATION
---------------------------
Earlier album controls around `AI_isPushover` concerned the heuristic definition and
possible asymmetric/tuning questions. F344 is not about whether the pushover threshold
is well tuned. It assumes `AI_isPushover` is correct and shows that the explicitly
written "multiple pushover wars" exception cannot execute because its local state flag
never changes.

This is separate from war-plan/area-AI approximations elsewhere. No alternative helper
can make `!bFirst` true; it is a local deterministic state bug.

REPAIR DIRECTION
----------------
The surgical correction is the obvious one-line state transition:

    if (!bFirst)
        return true;
    bFirst = false;

A clearer formulation would avoid inverted naming altogether:

    bool bFoundPushoverWarInArea = false;
    ...
    if (pEnemyCapital != NULL && pEnemyCapital->isArea(*pArea))
    {
        if (bFoundPushoverWarInArea)
            return true;
        bFoundPushoverWarInArea = true;
    }

No broader tuning change is needed. This restores exactly the behavior described by
the introducing commit's own comment: one far weaker war can be ignored as a major
focus, but multiple relevant weak wars should collectively count.

CURRENT AUDIT STATE AFTER F344
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F344
F345 / provisional KI#668 — UNUSED
Latest physical checkpoint — C031-WIP32
No repository source files modified.

NEXT CURSOR
-----------
1. Continue downward from the nuke/focus-war neighborhood through missionary and
   corporation valuation with F345 unused.
2. Treat the killed nuke-third-party concern as a control: `canNukeAt` validates away
   non-enemy major-civ victims before `AI_nukePlotValue` is summed.
3. Continue reconciling any corporation/resource issue against F343 so one physical
   neutral-resource duplication root is not renumbered through each consumer.


===============================================================================
F345 / provisional KI#668 — area-specific AI_missionaryValue drops same-team
cities, so production can value a real teammate spread mission at zero
===============================================================================

STATUS
------
CONFIRMED — inherited BtS/K-Mod team-internal missionary valuation inconsistency,
explicitly noticed but not repaired by AdvCiv; still live in current SAS.

ROOT
----
`CvPlayerAI::AI_missionaryValue` uses two different definitions of "our" cities
depending on whether the caller supplies an area.

For a global call (`pArea == NULL`) it is team-wide:

    iOurCitiesHave = kTeam.getHasReligionCount(eReligion);
    iOurCitiesCount = kTeam.getNumCities();

but for the area-specific call used by city production it becomes player-only:

    iOurCitiesHave = pArea->countHasReligion(eReligion, getID()) +
            countReligionSpreadUnits(pArea, eReligion, true);
    iOurCitiesCount = pArea->getCitiesPerPlayer(getID());

If all of this player's cities in the area already have the religion, the internal
spread value is therefore set to zero even when a teammate has reachable same-area
cities that lack it.

Those teammate cities are not recovered by the external-spread pass. Current AdvCiv
uses:

    for (PlayerIter<MAJOR_CIV,OTHER_KNOWN_TO> itOther(getTeam()); ...)

and `AgentIterator` defines `OTHER_KNOWN_TO` as a met player whose team is explicitly
NOT `m_eTeam`. `AI_isTargetForMissionaries`, called inside that loop, likewise asserts
that the target is on another team.

Thus with `pArea != NULL`, same-team cities belong to neither side of the valuation:
not player-internal and deliberately not external.

CURRENT SOURCE ACKNOWLEDGEMENT
------------------------------
AdvCiv itself leaves the inconsistency documented immediately above
`AI_isTargetForMissionaries`:

    Tbd.: AI_missionaryValue and AI_spreadReligion handle (team-)internal spread
    differently (they probably shouldn't)

The deeper producer/consumer trace confirms that this is behaviorally meaningful, not
just stylistic duplication.

SUPPORTED REPRODUCTION
----------------------
Take two AI players A and B on the same team (or otherwise in the ordinary Civ4
same-team configuration). Let:

* A own the holy city for religion R;
* all of A's cities in the relevant land area already contain R;
* B have a reachable city in that same area that does not contain R;
* there be no more valuable eligible foreign-team spread target in the area (for the
  cleanest example, no peaceful foreign target at all).

A city's `AI_bestSpreadUnit` calls:

    kPlayer.AI_missionaryValue(R, area())

The area branch counts only A's cities. `iOurCitiesHave == iOurCitiesCount`, so
`iSpreadInternalValue` becomes 0. The external loop excludes B because B shares A's
team. With no foreign target, the function returns 0.

`CvCityAI::AI_bestSpreadUnit` rejects the religion immediately when
`iReligionValue <= 0`; the normal high-value spread-unit production path therefore
cannot select an R missionary for B's city.

MOVEMENT AI PROVES THE MISSION IS VALID
---------------------------------------
An already-existing A missionary does not share that blind spot.
`CvUnitAI::AI_spreadReligion` loops all alive players and contains an explicit same-team
branch:

    else if (bHasHolyCity && kLoopPlayer.getTeam() == getTeam())
        iPlayerMultiplierPercent =
            (kLoopPlayer.getStateReligion() == eReligion ? 600 : 300);

It then iterates that teammate's cities, checks land access, `canSpread`, pathing and
mission reservations, and can assign the city a positive spread value and move there.

So in the reproduction the production evaluator says the religion has zero spread
value while the movement evaluator for the very unit being valued identifies B's city
as a valid, positively weighted target. This is a direct producer/consumer contract
mismatch.

`AI_neededMissionaries` does not repair the valuation. Its holy/state-religion external
need estimate can remain positive for the area, and `AI_unitValue(UNITAI_MISSIONARY)`
can therefore regard a missionary type as role-valid, but the dedicated
`AI_bestSpreadUnit` production path still skips the religion when
`AI_missionaryValue(..., area())` returns zero.

ANCESTRY
--------
The split is inherited from BtS.

Civ4CE/BtS `AI_missionaryValue` already uses team-wide `kTeam.getHasReligionCount` /
`kTeam.getNumCities` for the NULL-area branch, player-only area counts for non-NULL,
and explicitly excludes same-team players from external spread.

K-Mod rewrote and rebalanced much of missionary valuation but retained this exact
team/global versus player/area split. AdvCiv retained it and later factored target
checks into `AI_isTargetForMissionaries`, adding the explicit TODO quoted above rather
than changing the semantics. Current SAS remains the same in this respect.

This is therefore an inherited BtS/K-Mod bug, not a SAS regression.

DUPLICATE / ROOT SEPARATION
---------------------------
F345 is not a general claim that missionaries should always help teammates, nor a
balance complaint about the 600/300 same-team movement weights. The movement code
already establishes that AdvCiv intends those targets to be valid when the team has the
holy city. The bug is that area-specific production valuation omits the entire target
class that its own movement AI supports.

It is separate from F340's missionary-SEA special-cargo validation bug. F340 can select
a physically incompatible naval carrier for missionaries; F345 can fail to value the
land missionary itself for a same-team city even when no transport is involved.

It is also independent of F343 resource multiplicity and F344 war-focus state.

REPAIR DIRECTION
----------------
Make the area-specific internal-spread accounting consistent with the global branch's
team semantics, or explicitly add same-team target valuation equivalent to the movement
AI.

A team-wide area formulation would sum, over living members of `getTeam()`:

* the member's cities in `pArea`;
* those cities already containing `eReligion`; and
* preferably the team's relevant missionaries already in/spreading through the area so
  multiple teammates do not all train for the same missing city.

Alternatively retain player-local internal accounting but add a distinct teammate
spread component before the foreign-team external loop, using the same eligibility
principles as `AI_spreadReligion`.

The important contract is not a particular weight: a reachable same-team city that the
movement AI considers a valid positive mission must not disappear entirely from the
area-specific production value.

CURRENT AUDIT STATE AFTER F345
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F345
F346 / provisional KI#669 — UNUSED
Latest physical checkpoint — C031-WIP33
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_executiveValue` / `AI_corporationValue` and the mission-count
   helpers with F346 unused.
2. Keep the executive global-follow-on-spread candidate CLOSED: its source explicitly
   estimates propagation by the recipient player after the first spread, so global
   city count is defensible even for an area-local current executive.
3. Keep corporation neutral-resource multiplicity under F343 unless a new independent
   state or arithmetic root is demonstrated.


===============================================================================
F346 / provisional KI#669 — AI_neededCityAttackers mistakes legitimate Ancient-era
war targets for "no targets" and contaminates their mean era with Barbarian era
===============================================================================

STATUS
------
CONFIRMED — inherited AdvCiv `advc.104p` value/sentinel collision introduced by the
feature that changed city-attack stack sizing from own/game era to target era; still
present unchanged in Base AdvCiv 1.14 and current SAS.

ROOT
----
`CvPlayerAI::AI_neededCityAttackers` intentionally estimates stack size from the mean
current era of civilizations against which this team has a war plan:

    scaled rMeanTargetEra;
    int iEnemies = 0;
    ...
    if (kOurTeam.AI_getWarPlan(itEnemy->getTeam()) != NO_WARPLAN)
    {
        rMeanTargetEra += itEnemy->getCurrentEra();
        iEnemies++;
    }

If no such target exists, the function wants a fallback era so that the calculation is
still defined. But it tests the accumulated *value* instead of whether any enemy was
found:

    if (rMeanTargetEra == 0)
    {
        rMeanTargetEra += GET_PLAYER(BARBARIAN_PLAYER).getCurrentEra();
        iEnemies++;
    }
    rMeanTargetEra /= iEnemies;

Era 0 is a legitimate civilization era. Therefore one or more real war-plan targets
can be present (`iEnemies > 0`) while `rMeanTargetEra == 0` simply because every target
is Ancient. Those targets are then treated as though none had been found: the Barbarian
player's era is injected and the divisor is incremented.

The correct sentinel already exists independently as `iEnemies`.

SUPPORTED REPRODUCTION
----------------------
Let an AI have exactly one known war-plan target T and let T still be in
`ERA_ANCIENT` (current-era index 0). Let the Barbarian player meanwhile have reached,
for example, era 4 through the ordinary AdvCiv Barbarian catch-up system.

The intended mean target era is:

    0 / 1 = 0

Current code executes the fallback because the sum is numerically zero:

    (0 + 4) / (1 + 1) = 2

So a real Ancient target is evaluated as though its era were 2 solely because unrelated
Barbarian technological progress exists elsewhere in the game.

With several Ancient targets the same collision remains: their summed era is still
zero, so the Barbarian era is added once and divided across `iEnemies + 1`.

CURRENT FORMULA CONSEQUENCE
---------------------------
The contaminated mean is not informational only. It feeds both deterministic and
random stack-size terms:

    scaled rEraRatio = (rMeanTargetEra + 1) / GC.getNumEraInfos();
    iModulus = round(strategyBase + rEraRatio * iFlavourExtra +
            rMeanTargetEra / 2);
    iR = floor(rMeanTargetEra / 2) + 4 + (hash % iModulus);

Current SAS has seven eras. For an ordinary military-flavoured AI, raising the mean
from 0 to 2 increases both the fixed term and the randomization range. For some
birthmarks the requested stack can jump by several units even before training-speed,
assault-area and colonial multipliers are applied.

The current SAS `SAS_MIN_ATTACK_CITY_STACK_UNITS = 9` can mask low-end differences in
one readiness comparison, but it does not neutralize the root:

* sufficiently high Barbarian-era contamination can make `AI_neededCityAttackers`
  exceed that floor;
* `AI_stackOfDoomExtra()` is also used directly as the target size for regrouping;
* assault transports use it directly to derive reinforcement and invasion cargo
  thresholds, without the SAS 9-unit city-attack readiness floor.

LIVE CONSUMERS
--------------
The main current `UNITAI_ATTACK_CITY` movement path obtains:

    iWarStackNeeded = AI_stackOfDoomExtra();

and requires a non-Barbarian real-war stack to meet `iWarStackNeeded` (plus the SAS
minimum) before setting `bReadyToAttack`. The same helper is used while under-sized
city-assault groups wait for or join other groups, including an `AI_omniGroup` target
of roughly twice `AI_stackOfDoomExtra()`.

`UNITAI_ASSAULT_SEA` also derives:

    rTargetReinforcementSize = AI_stackOfDoomExtra();
    rTargetInvasionSize = 2 * rTargetReinforcementSize;

so the false target-era increase can make naval invasion transports wait for more land
cargo even though the actual civilization target remains Ancient.

The result therefore affects real army assembly/readiness and amphibious launch
thresholds rather than merely a debug or advisory value.

ANCESTRY
--------
K-Mod's older stack-size formula scaled from the current/game era and therefore did not
have this particular value/sentinel collision.

AdvCiv practical commit 2710 (`168c4a2c799cef88f0461ed850ff1eaa34faa6a6`), titled:

    Tweak to AI the target size of AI city attack stacks

introduced the target-era calculation. The introducing diff explicitly says:

    We don't need more units as _we_ become more advanced.
    Instead check what era our potential targets are in.

and in that same new block adds:

    if (rMeanTargetEra == 0)
    {
        rMeanTargetEra += GET_PLAYER(BARBARIAN_PLAYER).getCurrentEra();
        iEnemies++;
    }

Thus the bug is present at the feature's introduction: zero was simultaneously made a
valid target-era result and used as the "no target" sentinel.

AdvCiv later moved this body from `CvUnitAI::AI_stackOfDoomExtra` into
`CvPlayerAI::AI_neededCityAttackers` (practical commit 3959, refactor only). Base
AdvCiv 1.14 retains the same `rMeanTargetEra == 0` condition verbatim, and current SAS
retains it too.

This is therefore inherited AdvCiv, not SAS-local.

DUPLICATE / ROOT SEPARATION
---------------------------
F346 is not about Barbarian-city expedition sizing; current SAS has a separate
`AI_neededCityAttackersVsBarbarians()` path for deliberate Barbarian-city attacks.
Here the actual intended target is a normal civilization, and the Barbarian player's
technological era leaks into that civilization-war stack estimate only because Ancient
has index 0.

It is also separate from earlier Barbarian-research findings such as KI#408/F086. Those
can affect how quickly Barbarians advance, but F346 remains a deterministic
misclassification whenever the Barbarian era is nonzero and all real war-plan targets
sum to era zero.

REPAIR DIRECTION
----------------
Use the actual presence/count sentinel:

    if (iEnemies == 0)
    {
        rMeanTargetEra = GET_PLAYER(BARBARIAN_PLAYER).getCurrentEra();
        iEnemies = 1;
    }

or equivalently retain the addition form while testing `iEnemies <= 0`.

This preserves the intended fallback when no civilization target exists while allowing
one or many legitimate Ancient targets to produce a mean era of exactly zero.

No retuning of stack-size coefficients is required; the repair only restores the
feature's stated target-era contract.

CURRENT AUDIT STATE AFTER F346
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F346
F347 / provisional KI#670 — UNUSED
Latest physical checkpoint — C031-WIP34
No repository source files modified.

NEXT CURSOR
-----------
1. Continue downward through the attacker/target-strength and neighboring military
   helpers with F347 unused.
2. Keep the `AI_neededCityAttackers` root isolated from later consumers; downstream
   readiness/regrouping/assault effects belong to F346 unless another independent
   state or arithmetic defect is found.
3. Continue applying the current producer -> contract -> consequence standard and
   leave tuning-only differences unnumbered.


===============================================================================
F347 / provisional KI#670 — failed bounded path in AI_unitTargetMissionAIs can
MAX_INT++ into a negative "nearby" distance and count far joiners as imminent
===============================================================================

STATUS
------
CONFIRMED — inherited K-Mod bounded-mission-census failure-path bug; retained by
AdvCiv and current SAS. Vanilla BtS/Civ4CE does not contain the bounded path-turn
extension that creates this root.

ROOT
----
`CvPlayerAI::AI_unitTargetMissionAIs` optionally counts only groups that can reach the
specified target unit within `iMaxPathTurns`:

    int iPathTurns = MAX_INT;
    if (iMaxPathTurns >= 0 && kUnit.plot() != NULL &&
        pLoopSelectionGroup->plot() != NULL)
    {
        pLoopSelectionGroup->generatePath(..., &iPathTurns,
                iMaxPathTurns);
        if (!pLoopSelectionGroup->canAllMove())
            iPathTurns++;
    }
    ...
    if (iMaxPathTurns < 0 || iPathTurns <= iMaxPathTurns)
        iCount += pLoopSelectionGroup->getNumUnits();

The return value of `generatePath` is ignored.

`CvSelectionGroup::generatePath` explicitly defines its output contract as:

    if (piPathTurns != NULL)
    {
        *piPathTurns = MAX_INT;
        if (bSuccess)
            *piPathTurns = kPathFinder.getPathTurns();
    }
    return bSuccess;

Therefore a path that does not exist *within the requested maximum* leaves
`iPathTurns == MAX_INT` by design.

The caller then increments that failure sentinel whenever the joining group cannot all
move. On Civ4's supported 32-bit MSVC/x86 runtime, incrementing `INT_MAX` wraps to the
negative signed endpoint. That negative value trivially satisfies every ordinary
nonnegative path window such as `<= 2` or `<= 3`.

So the exact branch intended to exclude a distant/unreachable group can instead count
it as nearby whenever that group has already spent movement this turn.

SUPPORTED REPRODUCTION
----------------------
No permanent physical disconnection is needed.

1. City-assault group A is the target group.
2. A second group B has an ordinary `MISSIONAI_GROUP` mission targeting A's head unit.
   Current `AI_omniGroup` producers can create such grouping missions with a maximum
   path of 10 turns, so B may legitimately be, for example, 5 turns away.
3. B acts earlier in the player turn and moves toward A. It therefore still has the
   same grouping target but `canAllMove()` is now false.
4. A later runs its city-assault movement and asks only for imminent joiners, e.g.:

       AI_unitTargetMissionAIs(*this, &eMissionAIType, 1,
               getGroup(), 2)

5. The helper asks B's group for a path capped at 2 turns. Because B is still about
   4 turns away, `generatePath` returns false and writes `MAX_INT`.
6. Because B has already moved, the helper executes `iPathTurns++` on that sentinel.
7. The wrapped negative value passes `iPathTurns <= 2`, and every unit in B is counted
   as a nearby joiner.

The same shape can arise from a truly unreachable target, but the ordinary
"longer than this caller's local wait window" case is sufficient and considerably
more common.

LIVE CONSEQUENCE
----------------
The main `UNITAI_ATTACK_CITY` path uses bounded mission counts specifically to decide
whether a stack should wait for backup.

For a ready attack stack, K-Mod/AdvCiv calculates a short `iMaxWaitTurns` (often 1-3)
and obtains:

    int iJoiners = AI_unitTargetMissionAIs(..., iMaxWaitTurns);

It can then `MISSION_SKIP` when the supposedly imminent joiners are large enough.

A concrete current shape is a 6-unit ready stack A and a 2-unit B group still more than
2 turns away. Correct bounded counting returns 0. With the failure-sentinel overflow,
B contributes 2 and the readiness check can become:

    2 * 5 > 6

causing A to wait despite the backup being explicitly outside the 2-turn horizon.

The not-yet-ready and nearby-joiner branches likewise use 2-turn bounded calls, so the
same false-positive census can make attack stacks park or avoid regrouping decisions
for distant groups.

This is a gameplay control-flow consequence, not merely an inaccurate diagnostic
count.

PRODUCER CONTRACT
-----------------
`MISSIONAI_GROUP` groups farther than the bounded query window are normal supported
state. `AI_omniGroup` selects a candidate after a successful path search using its own
caller-supplied maximum, and then pushes:

    MISSION_MOVE_TO_UNIT ... MISSIONAI_GROUP ... pBestUnit

Several attack-city grouping calls permit paths up to roughly 10 turns. The later
`AI_unitTargetMissionAIs(..., 2)` query is deliberately a *narrower* question:
"which of those joiners are close enough that we should wait now?"

Thus a 5-10-turn grouping mission presented to a 2-turn census is not stale or invalid
state; it is exactly why the helper has an `iMaxPathTurns` parameter.

Likewise `canAllMove()==false` is ordinary after B has already taken its movement for
the current turn. The function even contains the increment specifically to account for
that state on successful paths.

ANCESTRY
--------
Civ4CE/BtS `AI_unitTargetMissionAIs` has no path-turn filtering at all; it simply counts
groups whose MissionAI unit/type match. Therefore this particular failure sentinel does
not exist there.

K-Mod 1.46 adds the `iMaxPathTurns` overload. Its implementation already contains the
same sequence:

    int iPathTurns = MAX_INT;
    generatePath(..., &iPathTurns);
    if (!pLoopSelectionGroup->canAllMove())
        iPathTurns++;
    if (iMaxPathTurns == -1 || iPathTurns <= iMaxPathTurns)
        count ...

AdvCiv retained that logic while optimizing mission-type checks, passing the max path
directly to the newer pathfinder, adding the cargo-load kludge and adding `iMaxCount`.
Those changes do not repair the ignored failure return plus sentinel increment.

Base AdvCiv 1.14 and current SAS retain the root. This is therefore inherited from
K-Mod/BBAI-era mission-count logic, not SAS-local.

DUPLICATE / ROOT SEPARATION
---------------------------
Earlier mission-census findings KI#534/F206 and KI#544/F221 concern empty-group
lifecycle and double-counted loaded cargo in `AI_enemyTargetMissions`. F347 is a
separate helper and a separate invariant: a failed bounded path is converted into a
small/negative path-turn result before the distance filter.

The previously closed multi-MissionAI-array concern is also unrelated; F347 reproduces
with the ordinary single `MISSIONAI_GROUP` request used by live attack-city callers.

REPAIR DIRECTION
----------------
Honor path failure before adjusting turns:

    if (iMaxPathTurns >= 0 && ...)
    {
        if (!pLoopSelectionGroup->generatePath(..., &iPathTurns,
                iMaxPathTurns))
            continue;
        if (!pLoopSelectionGroup->canAllMove())
            iPathTurns++;
    }

Alternatively guard the increment/result explicitly against `MAX_INT`, but using the
boolean return is clearer and preserves the pathfinder's contract.

If the "already moved" adjustment can push a successful path from the maximum to one
turn beyond it, the existing final `iPathTurns <= iMaxPathTurns` check correctly rejects
that group; no additional tuning change is required.

CURRENT AUDIT STATE AFTER F347
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F347
F348 / provisional KI#671 — UNUSED
Latest physical checkpoint — C031-WIP35
No repository source files modified.

NEXT CURSOR
-----------
1. Continue from the remaining mission-target helpers and then the next unsaturated
   inherited military/strength/value block with F348 unused.
2. Keep KI#534/KI#544 fixes as controls rather than renumbering their already-known
   empty-group/cargo roots.
3. Skip the already provisionally saturated `AI_civicValue` block unless a new external
   caller contract specifically reopens it.


===============================================================================
C031-WIP36 — F348 / provisional KI#671 CONFIRMED
AI_espionageVal Counterespionage era-0 normalization can divide by zero for a
supported WorldBuilder/scenario Ancient Spy
===============================================================================

STATUS
------
CONFIRMED, but deliberately bounded to supported scenario / WorldBuilder state.
This is not an ordinary current-SAS random-game crash because the only normal
`UNITAI_SPY` unit requires Classical-era Monarchy.

ROOT
----
`CvPlayerAI::AI_espionageVal` values Counterespionage with:

    int const iEra = getCurrentEra();
    ...
    iCounterValue /= std::max(1, 50 * iEra * (iEra + 1) +
            GET_TEAM(getTeam()).getEspionagePointsAgainstTeam(eTargetTeam)) / 2;

The intended normalization floor is defeated by integer arithmetic. The divisor is
not:

    std::max(1, (...)/2)

but rather:

    std::max(1, X) / 2

because `/` is evaluated on the right-hand side before the compound assignment.
When `X` is 0 or 1, `std::max(1, X)` is 1 and integer `1 / 2` is 0. The subsequent
integer division is therefore a divide-by-zero fault.

At era 0:

    X = own espionage points against the target team

so an Ancient player with 0 or 1 EP against the target reaches the zero divisor.

WHY ORDINARY CURRENT-SAS TRAINING DOES NOT REPRODUCE
-----------------------------------------------------
Current SAS has exactly one unit that uses `UNITAI_SPY`: `UNIT_SPY`.
Its current XML has:

    <DefaultUnitAI>UNITAI_SPY</DefaultUnitAI>
    <bSpy>1</bSpy>
    <PrereqTech>TECH_MONARCHY</PrereqTech>

and current Monarchy belongs to the Classical era. Therefore a normally trained
current-SAS Spy has `iEra >= 1`; the era term alone makes the normalization quantity
at least 100 and the zero-divisor case disappears.

`UNIT_GREAT_SPY` does not supply a hidden normal-game producer. Current XML gives it
`bSpy=0` and `UNITAI_GREAT_SPY`; the UnitAI dispatcher sends it through
`AI_greatPersonMove`, not `AI_spyMove` / `AI_espionageSpy`.

SUPPORTED SCENARIO / WORLDBUILDER PRODUCER
------------------------------------------
The WorldBuilder save loader does not reconstruct units through `canTrain`. In
`Assets/Python/pyWB/CvWBDesc.py`, `CvUnitDesc.apply` resolves the serialized
`UnitType` and directly calls:

    player.initUnit(unitTypeNum, self.plotX, self.plotY,
            UnitAITypes(eUnitAI), self.facingDirection)

If the save leaves `UnitAIType` unspecified, it passes `NO_UNITAI`; `CvPlayer::initUnit`
then deliberately substitutes the unit XML's default UnitAI. Thus a WB scenario can
legitimately define an Ancient AI-owned `UNIT_SPY` and obtain `UNITAI_SPY` without the
player knowing Monarchy.

The same WB format independently serializes/sets the player's starting era, so
`ERA_ANCIENT + UNIT_SPY` is an ordinary supported scenario combination rather than
memory corruption or an invalid direct DLL call.

LIVE CRASH CHAIN
----------------
A minimal supported reproduction is:

1. WorldBuilder/scenario player A is AI-controlled and has current era Ancient.
2. A owns a normal `UNIT_SPY` / `UNITAI_SPY`, e.g. serialized directly in or moved to
   a city owned by major-civ player B.
3. A has 0 (or 1) espionage point against B.
4. On the AI turn, the Spy reaches the ordinary `AI_spyMove` city handling and invokes
   `AI_espionageSpy` once its normal mission-selection conditions are met (placing it
   already fortified in the target city makes this easy to reproduce).
5. `AI_espionageSpy` calls `AI_bestPlotEspionage`.
6. Current Counterespionage XML has no technology prerequisite, costs 100 EP and has
   `iCounterespionageNumTurns=10`.
7. `AI_bestPlotEspionage` deliberately evaluates missions that are unaffordable due
   only to insufficient EP so the AI can decide whether they are worth saving points
   for. Its guard is:

       if (iCost < 0 ||
           (iCost <= iEspPoints && !canDoEspionageMission(...)))
           continue;

   With cost 100 and current EP 0, the second condition is false; valuation proceeds
   even though the mission cannot yet be executed.
8. `AI_espionageVal` enters the Counterespionage branch because its duration is 10.
   At era 0 and own EP 0:

       std::max(1, 0) / 2 == 1 / 2 == 0

9. `iCounterValue /= 0` executes. On Civ4's supported 32-bit MSVC/x86 runtime this is
   an integer divide-by-zero exception / crash.

The mission does not need to be affordable; that is precisely why the valuation path
can encounter the low-EP state.

ANCESTRY
--------
Vanilla BtS / Civ4CE contains the Counterespionage branch but leaves it effectively
empty apart from the old commented attitude line. It therefore does not contain this
arithmetic crash.

K-Mod commit 161 (`f9e8a3c4ceeab74d8d33f1fe8a0323ac5d1bc5be`) introduced an actual
Counterespionage value. Its first normalization form used:

    iCounterValue /= std::max(1, 50*iEra*iEra + ownEP);

which retained a valid denominator floor.

K-Mod commit 162 changed the era scaling to `50*iEra*(iEra+1)` but still kept the
whole denominator inside `std::max`, so it remained safe.

K-Mod practical commit 228 (`285f6c4323e98fe3527a9f6659fcd9a05af9b83c`) changed the formula to the
current half-scale form:

    iCounterValue *= (...)/2 + targetEP;
    iCounterValue /= std::max(1, ... + ownEP)/2;

Moving `/2` *outside* the `std::max` introduced the zero-divisor state for a floored
value of 1. AdvCiv inherited that exact arithmetic; later AdvCiv changes to victory
strategy and memory weighting did not repair the divisor. Base AdvCiv 1.14 and current
SAS retain it.

This is therefore an inherited K-Mod regression, absent from vanilla BtS.

DUPLICATE / ROOT SEPARATION
---------------------------
The earlier team-lifecycle Counterespionage findings in the album concern migration or
persistence of Counterespionage turns/modifiers across team changes. F348 is unrelated:
it is a local arithmetic failure during AI mission valuation before any mission is
performed.

The `GET_PLAYER(eTargetPlayer)`-before-`NO_PLAYER` ordering at the top of
`AI_espionageVal` is also separate and is not being numbered here; the sole current
gameplay caller rejects unowned plots before calling this helper.

REPAIR DIRECTION
----------------
Keep the intended half-scale normalization but floor the *actual divisor*, e.g.:

    int const iCounterDivisor = std::max(1,
            (50 * iEra * (iEra + 1) +
             GET_TEAM(getTeam()).getEspionagePointsAgainstTeam(eTargetTeam)) / 2);
    iCounterValue /= iCounterDivisor;

Equivalently, if the historical intent was to halve after flooring but never below 1:

    iCounterValue /= std::max(1,
            std::max(1, 50 * iEra * (iEra + 1) + ownEP) / 2);

The first form is simpler. This repair only restores denominator safety; it does not
retune normal Classical+ Counterespionage valuation.

CURRENT AUDIT STATE AFTER F348
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F348
F349 / provisional KI#672 — UNUSED
Latest physical checkpoint — C031-WIP36
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remaining `AI_espionageVal` mission branches with F349 unused,
   challenging arithmetic/identity/target legality rather than tuning.
2. Treat the era-0 Counterespionage crash as F348 downstream wherever encountered;
   do not renumber its mission-selection consequence.
3. Keep the unowned-plot `eTargetPlayer` guard ordering as API hardening unless a
   separate shipped caller appears.


===============================================================================
C031-WIP37 — CORRECTION: WIP36 F348 RETRACTED; F348 UNUSED AGAIN
===============================================================================

STATUS
------
WIP36's F348 promotion is RETRACTED after tracing the inner
`AI_espionageVal -> canDoEspionageMission` legality gate and the minimum reachable
Counterespionage mission cost. Do not treat the WIP36 scenario crash as a confirmed
finding.

The arithmetic expression remains unsafe in isolation, but the current supported
producer chain does not reach it with the only EP values that make the divisor zero.
This is therefore latent hardening debt, not a numbered current root under the album's
producer -> contract -> consequence standard.

WHAT WIP36 MISSED
-----------------
`CvUnitAI::AI_bestPlotEspionage` deliberately considers an espionage mission even when
it is currently unaffordable, so that the AI can decide whether saving EP is worthwhile.
That outer behavior initially appeared to make an Ancient WB-created Spy with 0-1 EP a
producer for the Counterespionage divisor.

However, `AI_espionageVal` itself begins with:

    TeamTypes eTargetTeam = GET_PLAYER(eTargetPlayer).getTeam();
    if (eTargetPlayer == NO_PLAYER ||
        !canDoEspionageMission(eMission, eTargetPlayer, &kPlot, iData, NULL))
    {
        return 0;
    }

`canDoEspionageMission` defaults `bCheckPoints=true` and ends with:

    return (iEspionagePoints >= iCost && iEspionagePoints > 0);

Therefore an unaffordable mission never reaches the valuation body. The outer
save-for-later path does not bypass this inner gate.

WHY EP=0 CANNOT REACH THE DIVISOR
---------------------------------
The inner gate explicitly requires:

    iEspionagePoints > 0

So the era-0 `ownEP == 0` zero-divisor case cannot enter `AI_espionageVal`'s mission
body through its current gameplay caller.

WHY EP=1 ALSO FAILS UNDER CURRENT SUPPORTED DATA
-------------------------------------------------
At era 0 the dangerous denominator is:

    std::max(1, ownEP) / 2

which is zero for ownEP 0 or 1. Since 0 is excluded above, a live producer would need
a Counterespionage mission cost of at most 1 EP.

The inner legality check passes `pUnit=NULL`. This matters: unlike the outer
unit-specific mission cost, the inner cost does NOT receive the Spy's fortification /
stationary discount. Consequently the strongest normal current-data cost reductions
for a city-target Counterespionage mission are bounded approximately by:

    trade route                 80%
    our state religion + holy   60%
    plot-culture modifier       50%
    distance                    >=100%   (cannot reduce below the base)
    espionage-points modifier   >=50%
    city espionage defense      >=100% in current XML
    target counterespionage     >=100% in ordinary current state

Population is neutral under the current default define. Multiplying the reducing
terms gives a floor of about 12% before team-size adjustment. The omitted stationary
50% discount is exactly what would otherwise have made the first WIP36 lower-bound
argument too optimistic.

`adjustMissionCostToTeamSize` can reduce the Counterespionage base cost before the
modifier when the target team is much larger than the acting team. Current
`TECH_COST_EXTRA_TEAM_MEMBER_MODIFIER` is 50. Even an extreme supported 48-civ setup
with one acting civ and 47 members on the target team gives approximately:

    adjusted base = floor(100 * sqrt(1 / (1 + 0.5 * 46)))
                  = about 20

Applying the approximately 12% minimum inner modifier leaves a mission cost of at
least 2 EP after integer arithmetic. Smaller target teams cost more.

Thus current supported WB/team setups cannot satisfy both required conditions:

    ownEP <= 1        (needed for zero divisor)
    ownEP >= mission cost > 0   (needed to pass the inner legality gate)

The dangerous arithmetic is unreachable under the current rules/data examined.

WHY ARBITRARY PYTHON/API STATE DOES NOT RESCUE THE FINDING
----------------------------------------------------------
Python exposes setters capable of constructing nonstandard state (for example a
negative Counterespionage modifier could artificially drive mission cost to zero),
but such arbitrary scripted mutation is not a current gameplay/scenario producer and
would reduce the standard to API misuse. The normal WB serializer does not establish
that state.

The supported WB ability to create an Ancient `UNIT_SPY` remains real, but it is not
enough by itself because the inner cost/EP gate blocks the divisor.

ANCESTRY NOTE RETAINED AS HARDENING EVIDENCE
---------------------------------------------
The arithmetic hazard itself still entered in K-Mod practical commit 228
(`285f6c4323e98fe3527a9f6659fcd9a05af9b83c`) when `/2` was moved outside the
`std::max(1, ...)` denominator floor. Vanilla BtS lacks the valuation arithmetic and
earlier K-Mod forms kept the whole denominator inside the floor.

That remains a worthwhile defensive cleanup:

    iCounterValue /= std::max(1, (...)/2);

would express the intended nonzero-divisor invariant directly. But without a current
supported producer, it does not earn F348.

CURRENT AUDIT STATE AFTER CORRECTION
------------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F347
F348 / provisional KI#671 — UNUSED
Latest physical checkpoint — C031-WIP37
WIP36 F348 — RETRACTED / superseded by this correction
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remainder of `AI_espionageVal` with F348 unused.
2. Keep the Counterespionage `/2` expression as a latent hardening note unless a
   genuinely supported producer below the current 2-EP legality floor appears.
3. Continue to prefer producer/lifecycle proof over attractive local arithmetic.


===============================================================================
C031-WIP38 — F348 CONFIRMED (reused after WIP36 retraction)
===============================================================================

F348 / provisional KI#671 — AI_espionageVal's unitless current-EP gate defeats
K-Mod's save-for-later mission valuation and can suppress missions affordable only
through the acting Spy's stationary discount

STATUS
------
CONFIRMED current inherited root.

This is NOT WIP36's retracted Counterespionage divide-by-zero claim. WIP37 remains the
correction for that false producer. F348 is now reused for an independent, fully
supported mission-selection contract bug found while continuing through the same
`AI_espionageVal` entry gate.

ROOT
----
Current `CvUnitAI::AI_bestPlotEspionage` is deliberately unit-aware. For every candidate
mission it obtains the cost with the ACTUAL Spy:

    int iCost = kOwner.getEspionageMissionCost(
            eMission, eTargetPlayer, &kSpyPlot, iTestData, this);

and, when that cost is currently affordable, validates the mission with the same Spy:

    if (iCost < 0 || (iCost <= iEspPoints &&
        !kOwner.canDoEspionageMission(
            eMission, eTargetPlayer, &kSpyPlot, iTestData, this)))
    {
        continue;
    }

The intentional K-Mod contract is broader than "only value what is affordable now".
Immediately after valuation the caller explicitly handles an unaffordable mission:

    /* If we can't do the mission yet, don't completely give up.
       It might be worth saving points for. */
    if (!kOwner.canDoEspionageMission(
        eMission, eTargetPlayer, &kSpyPlot, iTestData, this))
    {
        ...
        iValue *= 3;
        iValue /= iTurns + 3;
    }

However the only value helper call in between is:

    int iValue = kOwner.AI_espionageVal(
            eTargetPlayer, eMission, kSpyPlot, iTestData);

and `AI_espionageVal` still begins with the old player-level gate:

    if (eTargetPlayer == NO_PLAYER ||
        !canDoEspionageMission(
            eMission, eTargetPlayer, &kPlot, iData, NULL))
    {
        return 0;
    }

`canDoEspionageMission` defaults `bCheckPoints=true`. It computes the mission cost and
then requires:

    iEspionagePoints >= iCost && iEspionagePoints > 0

Thus the value helper is enforcing CURRENT affordability even though its caller
explicitly needs a value before current affordability has been reached.

CONSEQUENCE A — THE SAVE-FOR-LATER BRANCH IS EFFECTIVELY DEAD
--------------------------------------------------------------
Take any otherwise legal mission whose actual Spy-specific cost is 200 EP while the
team currently has 150 EP.

`AI_bestPlotEspionage` sees:

    iCost = 200
    iEspPoints = 150

The early `continue` deliberately does NOT fire because `iCost > iEspPoints`; K-Mod
wants to value the mission and decide whether it is worth saving for.

But `AI_espionageVal` immediately calls `canDoEspionageMission(..., NULL)` with points
checking enabled. Its no-unit cost cannot be lower through the stationary-Spy mechanic,
and in the ordinary unfortified case it is the same 200 EP. Since 150 < 200, the helper
returns 0 before evaluating any mission benefit.

Back in `AI_bestPlotEspionage`, the intended save-for-later branch runs and scales that
zero value according to the estimated wait:

    iValue *= 3;
    iValue /= iTurns + 3;

Zero remains zero. Risk/escape overhead can then make the candidate negative. Since
`iBestValue` starts at zero, the mission cannot become the selected future target.

This does not require a special mission, scenario state, stationary Spy, or unusual XML.
For every currently unaffordable candidate admitted specifically for K-Mod's
save-for-later logic, the inner current-EP gate prevents the mission's positive value
from ever reaching that logic.

CONSEQUENCE B — A FULLY FORTIFIED SPY CAN AFFORD A MISSION THAT VALUATION REJECTS
---------------------------------------------------------------------------------
The same root also creates a direct current-action mismatch.

`CvPlayer::getEspionageMissionCostModifier` applies the stationary discount only when an
actual Spy pointer is supplied:

    if (pSpyUnit != NULL)
    {
        iModifier *= 100 - pSpyUnit->getFortifyTurns() *
                ESPIONAGE_EACH_TURN_UNIT_COST_DECREASE;
        iModifier /= 100;
    }

Current BtS/SAS defaults are:

    MAX_FORTIFY_TURNS = 5
    ESPIONAGE_EACH_TURN_UNIT_COST_DECREASE = 10

so a Spy that has waited the normal five turns receives a 50% cost modifier at this
stage.

`AI_spyMove` deliberately calls `AI_espionageSpy()` once the Spy reaches that exact
maximum fortification state, making this ordinary AI gameplay rather than a constructed
API state.

Let a candidate have:

    G = its mission cost when no Spy is supplied
    A = its cost for the fully stationary acting Spy (approximately half G)

Choose an ordinary accumulated-EP state with:

    A <= current EP < G

The outer selector computes A with `this`, and `canDoEspionageMission(..., this)` says
the mission is executable NOW. `AI_espionageVal`, however, recomputes legality with
`pUnit=NULL`, loses the stationary discount, sees cost G > current EP, and returns 0.
The later current-action check again uses the actual Spy and would pass, but the positive
mission value has already been erased.

NORMAL CURRENT-DATA REPRODUCTION — SPACESHIP COMPONENT SABOTAGE
---------------------------------------------------------------
Current SAS keeps `ESPIONAGEMISSION_DESTROY_PROJECT` enabled with:

    iCost = 0
    iDestroyProjectCostFactor = 500
    TechPrereq = NONE

and current spaceship components include e.g. `PROJECT_SS_COCKPIT` with XML cost 1000,
along with Life Support, Stasis Chamber, Docking Bay, Engine, Casing and Thrusters.

`getEspionageMissionBaseCost` prices destroy-project at:

    base mission cost + (100 + 500)% * getProductionNeeded(project)

so component sabotage has a large positive generic cost and therefore a large interval
between the fully stationary actual-Spy cost and the NULL-unit cost.

Before launch, `canSpyDestroyProject` accepts completed non-world spaceship components.
For a malicious target, `AI_espionageVal` would value the destruction at:

    getProductionNeeded(project) *
        (maxTeamInstances == 1 ? 8 : 6)

which is thousands of value points for current components. Thus this is not a marginal
mission whose zeroing is likely irrelevant: a high-value, immediately executable
spaceship sabotage can be omitted from selection solely because the value helper
rechecks affordability without the acting Spy.

A war-plan target is sufficient to satisfy current `AI_isMaliciousEspionageTarget`.
Final `AI_espionageSpy` execution again calls `canDoEspionageMission(..., this)`, so both
ends of the live chain agree that the fortified Spy can perform the mission. The middle
value helper is the inconsistent layer.

CALLER / CONTRACT RECONCILIATION
--------------------------------
Current SAS has exactly one gameplay caller of `AI_espionageVal`:

    CvUnitAI::AI_bestPlotEspionage

So there is no competing current caller that requires `AI_espionageVal` itself to reject
missions solely because the team does not yet have enough EP.

The caller already owns:
- actual-Spy mission cost,
- actual-Spy current legality,
- the explicit save-for-later policy,
- wait-time discounting,
- risk/escape overhead,
- final actual-Spy execution validation.

The player-level value helper's second current-affordability gate is therefore both
redundant for immediately executable missions and directly incompatible with the
future-value contract.

ANCESTRY
--------
Vanilla BtS/Civ4CE already has the old player-level `AI_espionageVal` affordability
check using `pUnit=NULL`. Its old `CvPlayerAI::AI_bestPlotEspionage` is also player-level
and simply asks the value helper for missions; consequently vanilla selection already
does not make proper use of an acting Spy's stationary cost discount.

K-Mod then moved/reworked `AI_bestPlotEspionage` into `CvUnitAI` (the practical commit
around `f17786fc36db621009675c2a005fdd2c88f5559b`, which reworked Spy mission selection).
The new selector became explicitly unit-aware:
- mission cost is computed with `this`,
- actual legality is checked with `this`,
- final execution is checked with `this`, and
- currently unaffordable missions are intentionally retained so the AI can decide to
  save EP for them.

But K-Mod retained the old BtS `AI_espionageVal(... canDoEspionageMission(..., NULL))`
affordability gate. Current K-Mod source therefore already contains the contradiction,
and AdvCiv/SAS inherit it.

The most useful attribution is therefore:
- old stationary-discount blindness is inherited from BtS;
- K-Mod's unit-aware/save-for-later rework failed to remove the old unitless current-EP
  gate, making the intended future-value branch internally self-defeating;
- AdvCiv and current SAS inherit that integration defect.

DUPLICATE / ROOT SEPARATION
---------------------------
This is separate from F055 / KI#376. F055 concerns a diagnostic/preparation metric that
counts fortified states which do not represent a usable stationary espionage discount.
F348 concerns ACTUAL mission selection: even a real ordinary Spy fortified in the
correct foreign target city can have a legal discounted mission valued at zero.

It is also separate from WIP36's retracted Counterespionage divisor claim. WIP37 remains
the authoritative correction for that failed producer. The F348 number is reused here
only after that retraction, exactly as earlier retracted numbers in the album have been
reused for independent confirmed roots.

REPAIR DIRECTION
----------------
Do not fix only the stationary example by passing a Spy pointer while leaving current
EP checking enabled; that would still keep K-Mod's save-for-later branch dead.

The value helper needs STRUCTURAL mission legality without requiring current EP.
Since its only current caller has the acting Spy, a clean direction is to make that
context explicit, for example:

    int AI_espionageVal(..., CvUnit const* pSpyUnit) const;

and gate with:

    if (eTargetPlayer == NO_PLAYER ||
        !canDoEspionageMission(
            eMission, eTargetPlayer, &kPlot, iData, pSpyUnit, false))
    {
        return 0;
    }

where `false` is `bCheckPoints`.

That preserves structural/unit legality and the actual Spy context while leaving EP
affordability to `AI_bestPlotEspionage`, which already has the dedicated current-vs-
future logic.

A smaller repair could remove/relax the current-points gate in `AI_espionageVal`, but
passing the acting Spy is preferable because it keeps unit-sensitive legality coherent
with the sole caller and future-proofs any additional unit-dependent mission rules.

CURRENT AUDIT STATE AFTER F348
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F348
F349 / provisional KI#672 — UNUSED
Latest physical checkpoint — C031-WIP38
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — only this mission-valuation affordability/context mismatch
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remaining `AI_espionageVal` branches with F349 unused.
2. Treat dead save-for-later behavior and stationary-discount suppression as two
   consequences of F348's same current-EP/unit-context gate; do not renumber them.
3. Continue downward into the espionage weight/target helpers and then the next
   unsaturated `CvPlayerAI.cpp` section, maintaining producer -> contract -> consequence.


===============================================================================
C031-WIP39 — F349 CONFIRMED
===============================================================================

F349 / provisional KI#672 — AI_doCommerce reuses one tech-theft accumulator across
rivals, so the "at least two stealable techs" gate both misses real targets and can
combine unrelated one-tech targets into a false cheap-tech-steal strategy

STATUS
------
CONFIRMED — inherited K-Mod espionage-target state/accumulator bug retained by AdvCiv
and current SAS. Civ4CE/BtS does not contain this K-Mod cheap-tech-steal target block.

ROOT
----
`CvPlayerAI::AI_doCommerce` declares these once before iterating rival teams and their
members:

    int iMinModifier = MAX_INT;
    int iApproxTechCost = 0;
    TeamTypes eMinModTeam = NO_TEAM;

For each rival member, the code then tries to establish that the target has at least
TWO technologies worth stealing:

    bool bValid = false;
    FOR_EACH_ENUM(Tech)
    {
        if (canStealTech(kRivalMember.getID(), eLoopTech))
        {
            // don't set it true unless there are at least 2 stealable techs.
            bValid = iApproxTechCost > 0;
            iApproxTechCost = (kOurTeam.getResearchCost(eLoopTech) +
                    iApproxTechCost) / (iApproxTechCost != 0 ? 2 : 1);
            break;
        }
    }

There are two incompatible contracts packed into `iApproxTechCost`:

1. `bValid = iApproxTechCost > 0` treats it as a PER-TARGET "already found one tech"
   sentinel.
2. The later `bCheapTechSteal` logic treats it as the approximate cost of the selected
   tech-theft opportunity.

But the variable is never reset for a new rival/member, and the loop `break`s after the
first stealable tech. Therefore the intended per-target "two techs" test cannot work.

FAILURE A — REAL MULTI-TECH TARGET MISSED
-----------------------------------------
A normal singleton rival can have two, five or ten stealable technologies. On that
player's scan:

- the first stealable tech enters the branch;
- `iApproxTechCost` was 0, so `bValid` is false;
- the cost is stored;
- `break` exits the entire technology loop immediately.

No second technology belonging to that same target is ever inspected. If there is no
previous rival/member to have seeded `iApproxTechCost`, `bValid` therefore remains
false and the target cannot become `eMinModTeam` at all.

This directly contradicts the source comment "don't set it true unless there are at
least 2 stealable techs." In the common one-rival/single-member shape, having multiple
stealable technologies is insufficient to satisfy the gate.

FAILURE B — UNRELATED ONE-TECH TARGETS COMBINE
----------------------------------------------
Now take two singleton rival teams A and B, each with exactly one stealable tech.

A is scanned first:

    iApproxTechCost = cost(A.tech)
    bValid = false

B is scanned next. Before B's first (and only) stealable tech is processed,
`iApproxTechCost` is still nonzero from A. Thus:

    bValid = true

and B is admitted as though B itself had at least two stealable technologies.
`iApproxTechCost` simultaneously becomes an average mixing A's tech cost with B's tech
cost.

So the same leak causes the opposite false positive: one tech on A plus one unrelated
tech on B satisfies a gate documented as requiring at least two stealable techs from
the candidate target.

LIVE CONSEQUENCE
----------------
When `bValid` is true, the candidate can set `eMinModTeam` based on its low espionage
cost modifier and is added to `potentialTechTargets`. After all rivals are scanned,
`eMinModTeam` drives `bCheapTechSteal`.

A cheap-tech-steal result is not cosmetic. Current K-Mod/AdvCiv/SAS logic then:

- adds `iApproxTechCost / 10` to that team's espionage target and to the global target
  rate;
- gives `eMinModTeam` a 50% spending-weight boost;
- halves other teams' weights (and can halve them again in the espionage-economy
  strategy when they are not in `potentialTechTargets`);
- skips the normal gold-based reduction of the target espionage rate;
- raises the espionage commerce cap by 50 percentage points; and
- disables the normal research-vs-espionage ratio brake while increasing the slider.

Thus Failure A can suppress an intended espionage-economy opportunity even when the
only rival has many technologies available to steal. Failure B can activate and focus
the same strategy on a rival with only one stealable technology, while using a cost
estimate partly inherited from a different rival.

The false-positive and false-negative are one root: per-target scan state was made
player-global within the function.

ANCESTRY
--------
K-Mod 1.46 contains the same structure: `iApproxTechCost` is declared once before the
rival loop, the first stealable tech assigns `bValid = iApproxTechCost > 0`, updates the
same accumulator and immediately breaks. AdvCiv 1.14 retains it while adapting team
iteration and later targeting details; current SAS retains the root.

Civ4CE/BtS does not contain this K-Mod cheap-tech-steal targeting implementation, so the
root is inherited from K-Mod rather than vanilla BtS or SAS-local changes.

REPAIR DIRECTION
----------------
Separate PER-TARGET discovery state from any aggregate cost estimate. For each rival
member/team, scan enough technologies to count at least two genuinely stealable techs
and compute an estimate from that target only. For example:

    int iTargetStealable = 0;
    int iTargetApproxCost = 0;
    FOR_EACH_ENUM(Tech)
    {
        if (!canStealTech(kRivalMember.getID(), eLoopTech))
            continue;
        ... accumulate this target's cost ...
        if (++iTargetStealable >= 2)
            ... // target is valid; may stop once the intended sample is obtained
    }

If a team can have multiple members, avoid treating the same team technology as a new
independent stealable technology merely because another member is iterated; the gate is
about stealable technologies, not player entries.

Whichever target wins `iMinModifier` should carry its OWN approximate technology cost
alongside it (e.g. `iMinModApproxTechCost`) so the later `bCheapTechSteal` target-rate
boost cannot inherit costs from another rival.

CURRENT AUDIT STATE AFTER F349
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F349
F350 / provisional KI#673 — UNUSED
Latest physical checkpoint — C031-WIP39
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remainder of `AI_doCommerce` espionage target weighting with
   F350 unused, checking target/team state isolation and denominator/cost legality.
2. Keep all wrong `iApproxTechCost` target-rate and focus consequences under F349.
3. Then continue into the next unsaturated `CvPlayerAI.cpp` block rather than
   re-auditing already saturated civic tuning without a new caller contract.


===============================================================================
C031-WIP40 — F350 CONFIRMED
===============================================================================

F350 / provisional KI#673 — AI_doCounter decays cash-trade memory only while a
qualifying resource import is actively increasing Bonus-Trade attitude, so ordinary
gold-trade memory can remain permanent despite AdvCiv's explicit decay contract

STATUS
------
CONFIRMED — inherited AdvCiv `advc.036` control-flow bug retained by AdvCiv 1.14 and
current SAS. K-Mod/BtS keep the older non-decaying gold-traded state and therefore do
not contain AdvCiv's attempted decay implementation.

ROOT
----
`CvPlayerAI::AI_doCounter` first updates the resource-trade attitude counter for each
known major rival. Current AdvCiv/SAS structure is:

    scaled rBonusVal = AI_bonusImportValue(ePlayer);
    int iBonusTrade = AI_getBonusTradeCounter(ePlayer);
    if (rBonusVal <= iBonusTrade / (fixp(1.2) * iAttitudeDiv))
    {
        AI_setBonusTradeCounter(... decay ...);
    }
    else
    {
        ... increase BonusTradeCounter ...

        // <advc.036>
        {
            int iOldGoldTraded = AI_getGoldTradedTo(ePlayer);
            int iNewGoldTraded = (rDecayFactor * iOldGoldTraded).floor();
            AI_changeGoldTradedTo(ePlayer, iNewGoldTraded - iOldGoldTraded);
        }
        // </advc.036>
    }

The cash-memory decay is therefore not a general per-turn decay. It runs only when the
resource-import value is high enough to enter the *increase* branch of the unrelated
Bonus-Trade attitude counter.

The simplest ordinary case demonstrates the defect immediately:

- AI A has previously paid player B a lump sum, so `AI_getGoldTradedTo(B) > 0`.
- B is not currently exporting any resource to A.
- `AI_bonusImportValue(B)` is therefore 0.
- With the normal nonnegative BonusTradeCounter, the `rBonusVal <= ...` condition is
  true.
- A's BonusTradeCounter may decay, but `AI_getGoldTradedTo(B)` is untouched.

The same happens whenever imported-resource utility is too small to outrun the current
BonusTrade counter. Cash memory therefore ages only as a side effect of sufficiently
valuable resource imports from that same player.

EXPLICIT DESIGN CONTRACT
------------------------
The AdvCiv manual's change-036 section states, independently of resource-attitude
memory:

    "AI memory about cash traded to other civs decays."

It then explains the exact reason:

    "The AI keeps track of how much gold it has traded to every other civ. That
    amount is subtracted from the cash that the AI is willing to trade to another
    civ. ... The amount of gold that a civ is willing to trade (prior to subtracting
    gold-traded memory) can stagnate, and then the BtS AI can stay unwilling to trade
    any gold indefinitely. Also, pretty much all other AI memory decays in AdvCiv."

So the present resource-dependent decay is not merely an alternative tuning policy; it
recreates exactly the indefinite-memory failure that `advc.036` says it is fixing.

LIVE CONSEQUENCE
----------------
`CvDeal::startTrade` increments the giver's `AI_changeGoldTradedTo(eToPlayer, gold)`
when lump-sum gold changes hands. `AI_maxGoldTrade` later computes the AI's current
cash budget and subtracts:

    iMaxGold -= AI_getGoldTradedTo(ePlayer);

before clamping the amount available for trade.

Therefore, for example, if A has previously traded 100 gold to B and A's pre-memory
cash budget later stabilizes around 100 gold or less, A can remain at zero available
cash toward B indefinitely so long as B does not provide a sufficiently valuable
resource import. No additional gold trade is needed to maintain the lockout; the
supposedly decaying memory itself is simply never visited.

An unrelated resource deal with B can suddenly start shrinking the old cash memory,
which also demonstrates the wrong dependency: B exporting a useful resource to A
changes how quickly A forgets an old lump-sum cash payment, although the manual defines
cash-trade memory and resource-trade appreciation as separate mechanisms.

ANCESTRY
--------
K-Mod retains the BtS-style `m_aiGoldTradedTo` cap but contains no per-turn decay in
`AI_doCounter`. AdvCiv `advc.036` explicitly adds both the changed cash-trade budget and
the decay block. AdvCiv 1.14 already has the decay nested inside the Bonus-Trade
increase branch exactly as current SAS does.

Civ4CE/BtS contains neither AdvCiv's decay contract nor this attempted implementation.
Thus this is an AdvCiv-introduced control-flow bug, not a SAS-local regression.

REPAIR DIRECTION
----------------
Make cash-memory decay an unconditional per-rival `AI_doCounter` operation, independent
of resource-trade attitude state. For example, perform:

    int const iOldGoldTraded = AI_getGoldTradedTo(ePlayer);
    int const iNewGoldTraded = (rDecayFactor * iOldGoldTraded).floor();
    AI_changeGoldTradedTo(ePlayer, iNewGoldTraded - iOldGoldTraded);

once per eligible rival outside the `rBonusVal` if/else.

Prefer placing it before the BonusTradeAttitudeDivisor early `continue` as well. A
leader/personality with no Bonus-Trade attitude mechanism should not thereby disable
cash-memory decay; the manual describes the latter as a general cash-trade rule.

Do not tie the fix to whether there is a current resource deal, and do not reset the
memory abruptly: AdvCiv deliberately chose exponential diplomatic decay through
`rDecayFactor`.

CURRENT AUDIT STATE AFTER F350
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F350
F351 / provisional KI#674 — UNUSED
Latest physical checkpoint — C031-WIP40
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remainder of AI_doCounter memory decay and `AI_bonusImportValue`
   with F351 unused; keep the cash-memory control-flow root entirely under F350.
2. Then move into the next unsaturated post-commerce block, using current callers and
   AdvCiv/K-Mod/Civ4CE ancestry to distinguish tuning from state/identity bugs.
3. Maintain producer -> contract -> consequence and do not promote dormant XML/API
   shapes without a current producer.


===============================================================================
C031-WIP41 — F351 CONFIRMED
===============================================================================

F351 / provisional KI#674 — AI_proposeWarTrade city-for-war fallback ignores a
failed AI_counterPropose result and unconditionally implements the original deal,
allowing a separately legal but AI-rejected/unbalanced city-for-war trade to execute

STATUS
------
CONFIRMED — AdvCiv-introduced `advc.ctr` control-flow bug retained by AdvCiv 1.14 and
current SAS. The city-for-war fallback does not exist in K-Mod 1.46 or Civ4CE/BtS.

ROOT
----
`CvPlayerAI::AI_proposeWarTrade` has an AdvCiv `advc.ctr` fallback for a sponsor that
has no suitable tech payment (or would otherwise need two techs). If the war is going
badly enough, it searches for a city to offer to an AI hireling in exchange for a
`TRADE_WAR` declaration.

After selecting the city, current AdvCiv/SAS does:

    CLinkList<TradeData> weGive;
    weGive.insertAtEnd(TradeData(TRADE_CITIES, pBestCity->getID()));
    CLinkList<TradeData> hirelingGives;
    hirelingGives.insertAtEnd(TradeData(TRADE_WAR, eBestTarget));
    AI_counterPropose(eHireling, hirelingGives, weGive, true, true);
    kGame.implementDeal(getID(), eHireling, weGive, hirelingGives);
    return;

The return value of `AI_counterPropose` is discarded.

That boolean is not advisory. The AI-initiated wrapper builds separate `ourCounter`
and `theirCounter` lists and concatenates them into the caller's original lists only
when the underlying balancing function returns true:

    if (AI_counterPropose(..., theirCounter, ourCounter, rLeniency))
    {
        kWeGive.concatenate(ourCounter);
        kTheyGive.concatenate(theirCounter);
        return true;
    }
    return false;

Thus, if balancing fails, `weGive` still contains only the selected city and
`hirelingGives` still contains only the declaration-of-war item. The caller then
implements precisely that rejected original package.

WHY AI_counterPropose CAN LEGITIMATELY FAIL HERE
------------------------------------------------
The selected city is not chosen by comparing its deal value with the hireling's war
price. The city search deliberately uses city keep/acquisition fitness and the
sponsor's war-success rating. This is useful for finding a strategically sensible city
to cede, but it does not establish trade-value balance.

The surrounding function separately computed the target war price for the ordinary
tech/gold path (`iBestTeamPrice`), but the city fallback does not require the chosen
city's value to approximate that price.

`AI_counterPropose` therefore has real work to do. It evaluates both sides, tries to
add balancing items and can return false when no acceptable package can be constructed.
For an AI-AI deal its final fairness checks require each side's received value to meet
that side's `AI_tradeAcceptabilityThreshold` fraction of what it gives. A large enough
mismatch — e.g. a roughly 5:1 value gap with no usable sweeteners — cleanly fails those
checks.

The wrapper does not partially mutate the original lists on such a failure: its extra
items are kept in separate local counter lists and only concatenated after success.

LIVE PRODUCER / CONSEQUENCE
---------------------------
This path has ordinary shipped producers:

- `AI_doDiplo` calls `AI_proposeWarTrade` for an AI sponsor and an eligible AI
  hireling while the sponsor is already at war.
- The city fallback is reached when there is no suitable tech to give, or when two
  techs would otherwise be needed, and the sponsor's war-success rating is poor enough.
- The selected city and requested `TRADE_WAR` item have already passed individual
  `canTradeItem(..., true)` / denial legality checks.

A clean supported example is therefore:

1. AI A is losing a war against T and asks AI H to join.
2. A has no suitable tradable techs and owns a city that the `advc.ctr` city-fitness
   test allows it to cede.
3. H's price for declaring war on T is far from the trade value of that city.
4. Neither side has legal/appropriate extra items sufficient to close the value gap.
5. `AI_counterPropose` returns false because it cannot construct a mutually acceptable
   AI-AI package.
6. `AI_proposeWarTrade` ignores false and calls `implementDeal` on the untouched
   city-for-war lists anyway.

Depending on the direction of the mismatch, H can be forced into a war for compensation
it would not accept, or A can give away a city for military help whose value does not
justify it. Both are manifestations of the same ignored balancing result.

NO DOWNSTREAM FAIRNESS SAFETY NET
---------------------------------
`CvGame::implementDeal` forwards directly to `implementAndReturnDeal`. The latter
creates a `CvDeal` and calls `CvDeal::addTradeItems(..., bCheckAllowed=true)`.

That final check verifies each item's `canTradeItem` legality. It does not rerun
`AI_counterPropose`, `AI_considerOffer` or the bilateral AI trade-value thresholds.
The city and war items were already selected as individually legal, so a failed
*package-balance* decision is not recovered at implementation time.

EXPLICIT ADVCIV DESIGN CONTRACT
-------------------------------
The AdvCiv manual's sponsored-war section explicitly describes the feature:

- when two AI civs negotiate a joint war, the civ already at war may offer a city in
  exchange for the declaration of war;
- the rationale is the possibility of a seemingly doomed AI bringing in a powerful
  neighbor by ceding a city;
- the manual's Tbd. note says such trades would work through the changes to
  `CvPlayerAI::AI_counterPropose` and suggests that function should be used for war
  trades.

The source comment around the `advc.ctr` fallback likewise says it considers giving a
city "instead (through AI_counterPropose)" and describes the deal as "city ceded in
exchange for help in a war."

So `AI_counterPropose` is the intended package-balancing contract here, not a best-effort
side calculation whose failure may be ignored.

ANCESTRY
--------
K-Mod 1.46 and Civ4CE/BtS do not contain this `advc.ctr` city-for-war fallback or its
call to `AI_counterPropose`. AdvCiv 1.14 contains the same ignored-return sequence as
current SAS. This is therefore an AdvCiv-introduced control-flow defect, not a SAS-local
regression.

REPAIR DIRECTION
----------------
Honor the balancing result:

    if (AI_counterPropose(eHireling, hirelingGives, weGive, true, true))
    {
        kGame.implementDeal(getID(), eHireling, weGive, hirelingGives);
        return;
    }

If balancing fails, do not implement the original city-for-war package. The function
can fall through to its existing ordinary tech/gold logic (where applicable) or simply
abandon this proposal attempt. Do not try to duplicate the counterproposal fairness
logic locally; the whole purpose of `advc.ctr` is to delegate package construction to
`AI_counterPropose`.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of the earlier `AI_proposeWarTrade` KI#372/F051 legacy
`iBestTeamPrice == -1` defect. KI#372 concerns the ordinary legacy target-selection
branch feeding a bad price into later tech/gold balancing. F351 occurs inside the
separate AdvCiv city fallback and remains possible with a fully correct positive war
price.

It is also independent of the previously rejected city-value orientation suspicion:
`kHireling.AI_cityTradeVal(*pCity, kHireling.getID())` is the correct acquiring-player
contract. F351 is about ignoring the package balancer's failure after a city has been
legitimately selected.

CURRENT AUDIT STATE AFTER F351
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F351
F352 / provisional KI#675 — UNUSED
Latest physical checkpoint — C031-WIP41
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_proposeEmbargo` and `AI_proposeResourceTrade` with F352 unused,
   checking proposal-list state, caller/recipient orientation and whether failed
   counterproposal attempts leave stale mandatory items behind.
2. Keep all city-for-war ignored-return consequences under F351; do not duplicate them
   as generic `implementDeal` concerns.
3. Then continue into religion/civic/help/tribute/city proposal helpers before returning
   to lower valuation/strength sections.


===============================================================================
C031-WIP42 — F352 CONFIRMED
===============================================================================

F352 / provisional KI#675 — AI_bestRequestCity says liberation takes precedence but
its numeric-best comparison can let an earlier non-liberation city block every later
liberation city, making help/tribute city requests depend on city iteration order

STATUS
------
CONFIRMED — AdvCiv-introduced `advc.ctr` selection-order bug retained by AdvCiv 1.14
and current SAS. The helper and city-request feature do not exist in K-Mod/BtS.

ROOT
----
`CvPlayerAI::AI_bestRequestCity` explicitly establishes a two-level preference:

    bool bBestLiberate = false;
    ...
    bool bLiberate = (pCity->getLiberationPlayer() == getID());
    if (bBestLiberate && !bLiberate)
        continue; // Liberation takes precedence

After all eligibility/value checks, however, the actual replacement condition is only:

    int iValue = 3 * iOwnerValue - iOurValue;
    if (iValue < iBestValue &&
        kOwner.AI_cityTrade(*pCity, getID()) == NO_DENIAL)
    {
        iBestValue = iValue;
        pBestCity = pCity;
        bBestLiberate = bLiberate;
    }

That makes the supposed precedence one-sided:

- Once a liberation city has become `pBestCity`, later non-liberation cities are
  correctly skipped.
- But before that happens, an eligible non-liberation city may establish a low
  `iBestValue`.
- A later liberation city is then accepted only if its ordinary numeric score is even
  lower than that non-liberation score.

Therefore liberation does not take precedence; it takes precedence only if encountered
early enough or if it independently wins the secondary score.

ORDER-DEPENDENT REPRODUCTION
----------------------------
Nothing sorts `FOR_EACH_CITYAI(pCity, kOwner)` by liberation status. Ordinary city-list
order can therefore expose the defect.

For a help request (`rMinRatio = 5/3`), consider two separately legal human cities:

- N: non-liberation city, `iOurValue = 200`, `iOwnerValue = 100`.
  Ratio = 2, so it passes; score = `3*100 - 200 = 100`.
- L: liberation city, `iOurValue = 250`, `iOwnerValue = 150`.
  Ratio = 5/3, so it also passes; score = `3*150 - 250 = 200`.

If N is encountered first, it becomes best with score 100. When L is encountered,
`bBestLiberate` is still false, so the early precedence guard does not skip anything;
but `200 < 100` is false. L therefore cannot become best despite being a liberation
city.

If the same two cities are encountered in the opposite order, L becomes best first and
then N is skipped by `if (bBestLiberate && !bLiberate)`. The diplomatic request thus
changes merely with city iteration order.

The exact numbers are illustrative; the root only requires both cities to pass the
caller's value/ratio constraints while the liberation city's secondary score is not
lower than an earlier non-liberation candidate.

LIVE PRODUCERS / CONSEQUENCE
----------------------------
The helper has two current shipped diplomacy consumers:

1. `AI_askHelp(eHuman)` calls

       AI_bestRequestCity(eHuman, 1, 5/3)

   and may ask for the returned city instead of a technology.

2. `AI_demandTribute(eHuman, DEMAND_CITY)` calls

       AI_bestRequestCity(eHuman, rMinVal, 0.5)

   and places the returned city in the tribute demand.

So the bug is visible policy, not merely an internal ranking detail. The AI can ask a
human to hand over an unrelated non-liberation city even though an eligible city that
would be liberated to that AI is available and the source says liberation has
precedence. For tribute, this can make the demand unnecessarily intrusive; for help,
it directly undermines the intended liberation-oriented request behavior.

EXPLICIT DESIGN CONTRACT / ARCHAEOLOGY
--------------------------------------
The bug is present in the exact feature-introducing diff:

Practical commit 2075 / `3d9219cb2d...` (2020-03-29),
"AI asks for city as tribute/help; tweak city trade eval." [advc.ctr]

adds `AI_bestRequestCity` with the unchanged lines:

    if (bBestLiberate && !bLiberate)
        continue; // Liberation takes precedence

followed later by the plain `iValue < iBestValue` replacement condition. This is not a
later SAS refactor or a lost comment copied from unrelated code; the intended precedence
and the faulty comparator were introduced together.

The AdvCiv manual says that help requests may ask for cities that are significantly
more useful to the AI than to the human, and gives as part of the rationale:

    "To incentivize liberating AI cities"

It separately allows cities as tribute for variety and to give technologically lagging
players another way to avert war. Choosing a non-liberation city because it happened
to enter the list first is contrary to both the source's explicit precedence comment
and the liberation rationale.

AdvCiv 1.14 and current SAS retain the introduction logic. K-Mod 1.46 and Civ4CE/BtS
do not have `AI_bestRequestCity` or these AdvCiv city help/tribute requests.

REPAIR DIRECTION
----------------
Make the ranking lexicographic: liberation status first, then the existing `iValue`
score among candidates of equal liberation status.

For example, after the existing eligibility/denial checks, replacement can use the
conceptual condition:

    if ((!bBestLiberate && bLiberate) || iValue < iBestValue)

while retaining the earlier guard that skips non-liberation candidates once a
liberation best has been selected.

An even clearer formulation is to compare `(bLiberate, -iValue)` lexicographically or
write the two priority levels explicitly. The important part is that the *first*
eligible liberation candidate must be able to replace a non-liberation best regardless
of its secondary numeric score; subsequent liberation candidates can again use
`iValue` to choose the least painful/best request.

Do not simply sort the city list: iteration order is not the policy, and future callers
or city-list changes should not be able to alter liberation precedence.

CONTROL / ROOT SEPARATION
-------------------------
The earlier `AI_proposeResourceTrade` list-reuse/comment discrepancy remains a control,
not F352. Its failed first anchor can often be transformed by `AI_counterPropose` into a
valid resource-for-resource/GPT package, so no independent harmful consequence has yet
been established there.

F352 is also independent of F337's `AI_proposeCityTrade` leniency bug and F351's
city-for-war ignored-return bug. This root is confined to selecting which HUMAN-owned
city the AI asks for as help or tribute.

CURRENT AUDIT STATE AFTER F352
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F352
F353 / provisional KI#676 — UNUSED
Latest physical checkpoint — C031-WIP42
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_askHelp`, `AI_demandTribute`, `AI_proposeCityTrade` and
   `AI_intendsToCede` with F353 unused; keep liberation-precedence effects under F352.
2. Revisit `AI_proposeResourceTrade` only if a concrete alternate-anchor deal is proved
   to be blocked by the retained first mandatory resource.
3. Then continue into `AI_totalYieldVal`, deal-contact helpers and lower post-diplomacy
   sections under producer -> contract -> consequence.


===============================================================================
C031-WIP43 — F353 CONFIRMED
===============================================================================

F353 / provisional KI#676 — AI_eventValue values BonusRevealed through the unrelated
BonusType field, so the shipped "A Man Named Jed" Oil-reveal option receives zero
resource-reveal value and AI event choice can ignore the benefit it actually applies

STATUS
------
CONFIRMED — inherited BtS event-valuation field mix-up retained by Civ4CE/BtS,
K-Mod 1.46, AdvCiv 1.14 and current SAS. Current shipped Event XML contains the exact
producer required for the defect.

ROOT
----
`CvPlayerAI::AI_eventValue` computes one local bonus value only from the event's
ordinary plot-bonus field:

    int iBonusValue = 0;
    if (NO_BONUS != kEvent.getBonus())
        iBonusValue = AI_bonusVal((BonusTypes)kEvent.getBonus(), 0, true);

That value is then correctly reused for `getBonusChange`, because BonusChange changes
that same `BonusType` on the event plot.

Later, however, the separate reveal effect is evaluated with the same variable:

    if (kEvent.getBonusRevealed() != NO_BONUS)
        iValue += (iBonusValue * 10 * iGameSpeedPercent) / 100;

`BonusRevealed` is an independent EventInfo field. Nothing requires it to equal
`BonusType`, and the shipped data demonstrates that they are independent.

CURRENT-DATA PRODUCER — A MAN NAMED JED
---------------------------------------
Current SAS `CIV4EventTriggerInfos.xml` contains the active global trigger
`EVENTTRIGGER_MAN_NAMED_JED`:

- `iPercentGamesActive = 75`
- `iWeight = 5000`
- requires a Scout or Explorer on a plot with `BONUS_OIL`
- presents two choices, `EVENT_MAN_NAMED_JED_1` and `EVENT_MAN_NAMED_JED_2`

The second event is exactly the mismatched-field shape:

    <Type>EVENT_MAN_NAMED_JED_2</Type>
    <iGold>-10</iGold>
    ...
    <BonusType>NONE</BonusType>
    <iBonusChange>0</iBonusChange>
    ...
    <BonusRevealed>BONUS_OIL</BonusRevealed>
    ...
    <iAIValue>1000</iAIValue>

Thus `kEvent.getBonus() == NO_BONUS`, so `iBonusValue` remains 0. The later
`BonusRevealed` branch adds exactly zero for revealing Oil.

The first choice has no Oil reveal, no gold cost and the same `iAIValue = 1000`.
Consequently the intended differentiating benefit of option 2 is absent from AI
valuation; option 2 is instead slightly penalized by its 10-gold cost while both
choices retain their independent random AIValue component.

LIVE EFFECT / CONTRACT
----------------------
This is not dead XML and the reveal is not cosmetic.

`CvPlayer::canDoEvent` allows a BonusRevealed event only while the team lacks the
bonus's normal reveal technology and the bonus has not already been force-revealed.
For Oil in current SAS, `TechReveal = TECH_COMBUSTION`.

When the event is applied, `CvPlayer::applyEvent` executes:

    GET_TEAM(getTeam()).setForceRevealedBonus(
            (BonusTypes)kEvent.getBonusRevealed(), true);

So the selected option genuinely reveals Oil before Combustion.

Oil also has substantial current strategic use: current SAS has numerous Oil-requiring
units (Gunship, Tank, Modern Tank, Mobile SAM, Self-Propelled Artillery, Transport,
Destroyer, Battleship, Missile Cruiser, Attack Submarine, Carrier, Fighter, Jet
Fighter, Bomber and Stealth Bomber) and Oil-dependent building effects. The function's
own intended contract makes this even more direct: `AI_bonusVal(..., 0, true)` is the
resource-value routine that the surrounding event code uses for bonus creation/removal.
The omitted term is therefore not an abstract UI preference; it is the exact resource
value the source intended to add for early knowledge of Oil.

`AI_chooseEvent` loops every legal choice and chooses the largest `AI_eventValue`, so
the missing reveal value directly affects which event option an AI selects.

ANCESTRY
--------
The same field coupling is present throughout all supplied lineages:

- Civ4CE/BtS initializes `iBonusValue` from `kEvent.getBonus()` and later uses it for
  `getBonusRevealed()`.
- K-Mod retains the structure (changing the bonus-value call to its newer signature).
- AdvCiv 1.14 retains the K-Mod structure.
- Current SAS retains the same coupling.

The shipped `EVENT_MAN_NAMED_JED_2` producer is likewise present in Civ4CE/BtS,
K-Mod, AdvCiv and current SAS with `BonusType=NONE` / `BonusRevealed=BONUS_OIL`.
This is therefore a stock BtS inherited root, not a SAS regression.

CONSEQUENCE
-----------
For "A Man Named Jed," the AI can fail to prefer paying 10 gold to reveal Oil early
because the Oil knowledge itself contributes zero to the choice score. More generally,
any EventInfo whose `BonusRevealed` differs from `BonusType` is evaluated using the
wrong resource; if `BonusType` is NONE the reveal is worth zero, and if the two fields
name different bonuses then the reveal is valued as the wrong bonus entirely.

REPAIR DIRECTION
----------------
Value the reveal field independently, e.g. conceptually:

    if (kEvent.getBonusRevealed() != NO_BONUS)
    {
        int iRevealedBonusValue = AI_bonusVal(
                (BonusTypes)kEvent.getBonusRevealed(), 0, true);
        iValue += (iRevealedBonusValue * 10 * iGameSpeedPercent) / 100;
    }

Keep the existing `iBonusValue` based on `kEvent.getBonus()` for `BonusChange`; those
are separate EventInfo effects and should not share identity state.

DUPLICATE / ROOT SEPARATION
---------------------------
The earlier album control on `AI_eventValue` recursion is unrelated: the current event
graph has no non-self recursion cycle. F353 is a direct field-identity bug on a shipped
choice.

KI#46 (deliberately unsupported old-save compatibility) is also unrelated. That was
checked immediately before entering the event block and remains an intentional project
policy, not a numbered root.

CURRENT AUDIT STATE AFTER F353
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F353
F354 / provisional KI#677 — UNUSED
Latest physical checkpoint — C031-WIP43
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remainder of `AI_eventValue` and `AI_chooseEvent` with F354
   unused, checking field identity, target-player orientation, city/global scope and
   current Event XML producers.
2. Keep recursive-event concerns as controls unless a real EventInfo cycle is added;
   current data was already mechanically checked clean.
3. Then continue into `AI_doSplit` and the post-event empire/victory helpers.


===============================================================================
C031-WIP44 — F354 CONFIRMED
===============================================================================

F354 / provisional KI#677 — non-city EventInfo building yield/commerce changes are
collected by AI_eventValue but never consumed, so shipped multi-choice events can make
permanent building bonuses while the AI chooses as though those bonuses did not exist

STATUS
------
CONFIRMED — inherited BtS event-valuation omission retained by Civ4CE/BtS, K-Mod,
AdvCiv 1.14 and current SAS. Multiple current multi-choice EventInfos exercise the
omitted path; `EVENTTRIGGER_TOO_CLOSE_TO_CALL` gives a particularly clean reproduction.

ROOT
----
`CvPlayerAI::AI_eventValue` first aggregates every EventInfo building yield and commerce
change:

    FOR_EACH_NON_DEFAULT_PAIR(kEvent.getBuildingYieldChange(), ...)
        aiYields.add(...);

    FOR_EACH_NON_DEFAULT_PAIR(kEvent.getBuildingCommerceChange(), ...)
        aiCommerces.add(...);

For `kEvent.isCityEffect()`, those accumulators are then valued through
`iCityTurnValue`.

But the non-city branch only consumes happiness/health and the generic event fields:

    else if (!kEvent.isOtherPlayerCityEffect())
    {
        int iPerTurnValue = 0;
        iPerTurnValue += iNumCities * ((iHappy * 4) + ...);
        iPerTurnValue += iNumCities * ((iHealth * 3) + ...);
        ...
    }

There is no use of `aiYields` or `aiCommerces` in this branch. Therefore every
non-city building-specific yield or commerce effect contributes exactly zero to the AI
score despite the code having just parsed it.

CURRENT-DATA REPRODUCTION — TOO CLOSE TO CALL
---------------------------------------------
`EVENTTRIGGER_TOO_CLOSE_TO_CALL` is an active two-choice random event. Both choices are
non-city effects (`bPickCity=0`, `bPickOtherPlayerCity=0`) and both have
`iAIValue=1000` with no gold, tech, population, diplomacy or other differentiating
field.

Choice 1 contains only this material effect:

    BUILDINGCLASS_COURTHOUSE
    COMMERCE_GOLD +1

Choice 2 contains only this material effect:

    BUILDINGCLASS_COURTHOUSE
    COMMERCE_CULTURE +3

Because neither event is a city effect, `AI_eventValue` stores those values in
`aiCommerces` and then never reads them. The two options therefore reach the final
random multiplier with the same base valuation. The AI chooses between a permanent
+1-gold-per-Courthouse effect and a permanent +3-culture-per-Courthouse effect solely
through the generic random `iAIValue` noise rather than its economic/cultural needs or
the number of affected Courthouses.

LIVE EFFECT / PRODUCER
----------------------
`CvPlayer::applyEvent` confirms that these XML fields are real gameplay state. For
non-city events it iterates every current city and executes:

    pLoopCity->changeBuildingCommerceChange(
            perBuildingClassVal.first,
            perCommerceVal.first,
            perCommerceVal.second);

The analogous building-yield path calls `changeBuildingYieldChange` for every current
city. Building-specific happiness and health are also applied through dedicated
player modifiers.

Thus the valuation omission is not compensated at application time. The event really
changes Courthouse output while the AI selection logic assigns that output zero value.

The defect is broader than this one trigger. Current SAS has numerous multi-choice
non-city events with `BuildingExtraYields` or `BuildingExtraCommerces`, including
Horse Whispering completion, Harbormaster completion, Best Defense completion,
Sports League completion, Crisis in the Senate, Warships completion, Overwhelm,
Civ Game, Better Coal, Rogue Station and others. F354 is the shared valuation root;
those are downstream producers rather than separate findings.

ANCESTRY
--------
Civ4CE/BtS already has the same structure: building yield/commerce arrays are summed
before the city/non-city split and used only by the city-effect calculation. K-Mod
retains it (with revised specialist/commerce weights), AdvCiv retains it while
refactoring the containers to EnumMaps, and current SAS retains that logic.

This is therefore an inherited stock BtS omission, not a SAS regression.

CONTROL — SPACE PRODUCTION MODIFIER
-----------------------------------
The adjacent block contains another clear source-level field mistake:

    if (kEvent.getSpaceProductionModifier() != 0)
        iValue += ((20 + iNumCities) * getSpaceProductionModifier() * ...) / 100;

It tests the EventInfo modifier but values the player's *current* modifier instead of
`kEvent.getSpaceProductionModifier()`. The only current producer is
`EVENT_COMET_FRAGMENT_1` (`iSpaceProductionMod=5`). However that trigger has only one
choice and no other EventInfo references Comet Fragment through Additional/Clear event
valuation. Therefore the wrong number cannot alter current AI choice behavior and is
kept as dormant valuation debt rather than receiving its own finding number.

CONSEQUENCE
-----------
The AI can choose materially inferior random-event rewards because permanent
building-class yield/commerce bonuses are invisible to `AI_chooseEvent` whenever the
EventInfo is non-city. In the clean Too Close to Call pair, the source supplies no
other deterministic distinction, so the omitted field is exactly what the AI should
be comparing.

REPAIR DIRECTION
----------------
Value non-city building yield/commerce effects in the non-city branch instead of
throwing away the already-collected accumulators.

A robust repair should preserve building-class identity rather than only summing by
Yield/Commerce, because the number of affected/owned buildings matters. At minimum,
for each BuildingClass change, map the class to this civilization's BuildingType and
weight the permanent yield/commerce effect by the cities/buildings to which it can
actually apply. Current buildings are the safest baseline; a modest future-building
allowance can be considered separately if desired because `applyEvent` stores the
change on every current city.

Do not simply multiply the aggregated `aiCommerces` by `iNumCities`: that would value a
Courthouse bonus as though every city necessarily had a Courthouse and would preserve
the existing loss of BuildingClass identity.

DUPLICATE / ROOT SEPARATION
---------------------------
F353 concerns `BonusRevealed` reading the wrong resource field. F354 is independent:
it concerns building yield/commerce data that is parsed correctly but dropped by the
non-city valuation branch.

KI#206 (already repaired in current SAS) concerns the missing `/100` after
ConstructPercent when valuing direct `BuildingChange`; it does not cover
`BuildingExtraYield` / `BuildingExtraCommerce`.

CURRENT AUDIT STATE AFTER F354
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F354
F355 / provisional KI#678 — UNUSED
Latest physical checkpoint — C031-WIP44
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue the rest of `AI_eventValue` / `AI_chooseEvent` with F355 unused, checking
   each field against current multi-choice Event XML and the actual apply-event path.
2. Keep Comet Fragment's current-space-modifier error as a control unless a real
   multi-choice/recursive consumer is introduced.
3. Then continue into `AI_doSplit` and post-event empire/victory helpers.


===============================================================================
C031-WIP45 — F355 CONFIRMED
===============================================================================

F355 / provisional KI#678 — AI_eventValue discards BuildingClass identity for
building-specific happiness/health and multiplies the modifier by every city, so
non-city events can value one-building effects as empire-wide happiness/health

STATUS
------
CONFIRMED — inherited BtS event-valuation scope/multiplicity bug retained by K-Mod,
AdvCiv 1.14 and current SAS. Current Hindenburg and Antimonarchists event choices
exercise it; Antimonarchists gives an especially exact one-Palace multiplicity proof.

ROOT
----
`CvPlayerAI::AI_eventValue` parses building-specific happiness and health by summing
away the BuildingClass:

    FOR_EACH_NON_DEFAULT_PAIR(kEvent.getBuildingHappyChange(), BuildingClass, int)
        iHappy += perBuildingClassVal.second;

    FOR_EACH_NON_DEFAULT_PAIR(kEvent.getBuildingHealthChange(), BuildingClass, int)
        iHealth += perBuildingClassVal.second;

For a non-city EventInfo it then assumes that aggregate applies once per city:

    iPerTurnValue += iNumCities * ((iHappy * 4) + ...);
    iPerTurnValue += iNumCities * ((iHealth * 3) + ...);

But that is not the gameplay effect. `CvPlayer::applyEvent` maps each BuildingClass to
this civilization's BuildingType and stores a modifier for that specific building:

    changeExtraBuildingHappiness(eBuilding, perBuildingClassVal.second);
    changeExtraBuildingHealth(eBuilding, perBuildingClassVal.second);

`CvCity::updateExtraBuildingHappiness` then computes the actual city effect as:

    getNumActiveBuilding(eBuilding) *
            GET_PLAYER(getOwner()).getExtraBuildingHappiness(eBuilding)

(and health is analogous). A city without the affected building receives no current
happiness/health from the event. `iNumCities` is therefore not the affected-instance
count.

CURRENT-DATA REPRODUCTION 1 — ANTIMONARCHISTS / PALACE
-----------------------------------------------------
`EVENT_ANTIMONARCHISTS_1` is a non-city choice and grants:

    BUILDINGCLASS_PALACE: +3 happiness

The trigger is active current data for France under Hereditary Rule and does not pick a
city. A player can have many cities but only the active Palace instance supplies the
building-specific happiness at any one time. Yet at Normal speed the current valuation
contributes approximately:

    iNumCities * (+3 * 4) * 20
    = 240 * iNumCities

before the final random multiplier.

Thus a 10-city empire values the Palace-specific +3 happiness as roughly 2400 value
units, i.e. ten concurrent building instances, even though the runtime effect is tied
to the one active Palace. This multiplicity error remains conceptually wrong even if
the Palace later moves: the modifier follows the building type, not all cities at once.

CURRENT-DATA REPRODUCTION 2 — HINDENBURG / AIRPORT
-------------------------------------------------
`EVENTTRIGGER_HINDENBERG` is an active two-choice event (85% of games, weight 500)
requiring an Airship and Radio. It does NOT require any Airport.

`EVENT_HINDENBERG_1` has no material effect and `iAIValue=1000`.
`EVENT_HINDENBERG_2` costs 45 gold, also has `iAIValue=1000`, and grants:

    BUILDINGCLASS_AIRPORT: +1 happiness

The second choice can therefore be offered to an empire with zero or only a few
Airports. Nevertheless `AI_eventValue` gives the Airport modifier `80 * iNumCities`
Normal-speed value, as though every city currently receives it. With one Airport in a
10-city empire, the immediate active-building multiplicity is one while the evaluator
uses ten. With zero Airports, the immediate effect is zero while the evaluator still
awards the full empire-wide per-turn value.

The player-level modifier does persist and can benefit Airports built later, so a good
AI valuation may reasonably include some future-building allowance. But multiplying by
every *current city* without retaining the affected BuildingClass is not such a model;
it cannot know whether the building exists, can be built in every city, or is unique.
The Palace example proves the same implementation is objectively wrong even under a
future-building interpretation.

CHOICE CONSEQUENCE
------------------
`AI_chooseEvent` directly compares `AI_eventValue` across legal event choices. The
incorrect empire-wide multiplier can therefore strongly bias selection toward
building-specific happiness/health rewards. Hindenburg is a direct clean producer:
the paid Airport-happiness option can receive hundreds or thousands of value units
from cities that do not have an Airport, while its alternative has no material effect.

Antimonarchists also exercises F355, although its second choice contains
building-commerce effects that are independently affected by F354. That interaction
does not merge the roots: F354 drops yield/commerce entirely, whereas F355 *does value*
happiness/health but applies it at the wrong building multiplicity.

ANCESTRY
--------
Civ4CE/BtS already sums `getBuildingHappyChange` / `getBuildingHealthChange` into
scalar `iHappy` / `iHealth` and, in the non-city branch, multiplies those scalars by
`iNumCities`. K-Mod retains the structure, AdvCiv refactors the XML access to sparse
maps but retains the same loss of BuildingClass identity, and current SAS retains it.

This is therefore an inherited stock BtS event-valuation bug, not a SAS regression.

REPAIR DIRECTION
----------------
Preserve BuildingClass identity when valuing building-specific happiness and health.
For each affected class:

1. map it to this civilization's BuildingType;
2. count/inspect current active instances (or current cities containing the building);
3. value the happiness/health for those actual instances;
4. optionally add a separately bounded future-building allowance if desired.

Do not multiply a class-agnostic aggregate by `getNumCities()`. Unique/limited buildings
such as the Palace are the clearest reason that approximation cannot be generally
correct.

DUPLICATE / ROOT SEPARATION
---------------------------
F354 concerns non-city `BuildingExtraYield` / `BuildingExtraCommerce` values that are
parsed and then never consumed. F355 concerns `BuildingExtraHappy` /
`BuildingExtraHealth`, which are consumed but after their BuildingClass identity has
been discarded and are therefore valued at the wrong multiplicity.

KI#206 concerns direct `BuildingChange` ConstructPercent normalization and is already
repaired in current SAS; it is unrelated.

CURRENT AUDIT STATE AFTER F355
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F355
F356 / provisional KI#679 — UNUSED
Latest physical checkpoint — C031-WIP45
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the diplomacy / other-player portion of `AI_eventValue` with F356
   unused. Highest-priority candidate is the apparent double application of
   `iGameSpeedPercent` to attitude / worst-enemy diplomacy terms; prove or kill it
   against shipped multi-choice Event XML and effect duration semantics.
2. Check `isOtherPlayerCityEffect` valuation against current Famine/Looters producers.
3. Keep conversion-count and other field mismatches unnumbered until a current choice
   consequence survives.
4. Then continue through `AI_chooseEvent`, `AI_doSplit`, and post-event empire/victory
   helpers.


===============================================================================
C031-WIP46 — F356 CONFIRMED
===============================================================================

F356 / provisional KI#679 — AI_eventValue applies game-speed scaling twice to
attitude / worst-enemy diplomacy value, making relationship effects scale roughly with
the square of game speed and distorting shipped multi-choice event decisions

STATUS
------
CONFIRMED — inherited BtS event-valuation arithmetic bug retained by K-Mod, AdvCiv
1.14 and current SAS. Current Joyous Wedding, Wedding Feud, Airliner Crash, Fugitive,
Spy Discovered and Famine choices provide live multi-choice producers.

ROOT
----
Inside the other-player branch, `AI_eventValue` first constructs `iDiploValue` from
`getAttitudeModifier()` and `getTheirEnemyAttitudeModifier()` with no speed factor.
It then immediately applies game-speed scaling:

    iDiploValue *= iGameSpeedPercent;
    iDiploValue /= 100;

Later, after optional BonusGift and Aggressive-AI adjustments, the entire accumulator
is added through another game-speed multiplication:

    iValue += (iDiploValue * iGameSpeedPercent) / 100;

Therefore the attitude / worst-enemy terms that entered before the first scaling are
multiplied by:

    (iGameSpeedPercent / 100)^2

while BonusGift, which is inserted after the first scaling, is multiplied only once.
There is no corresponding second duration or quantity dimension in the attitude effect.

EFFECT-DURATION CONTRACT
------------------------
The event's `AttitudeModifier` is applied as diplomacy memory. Current AdvCiv/SAS
`CvPlayer::applyEvent` calls `AI_rememberEvent` once per magnitude point, producing
`MEMORY_EVENT_GOOD_TO_US` / `MEMORY_EVENT_BAD_TO_US` state.

Memory decay is itself adjusted for game speed in `AI_doCounter`:

    rProb /= per100(GameSpeedInfo.getGoldenAgePercent());

so slower games make these memories last longer approximately linearly. That supplies
the normal reason for scaling a relationship benefit by game speed once.

Current SAS makes the double count particularly obvious:

    Normal:    ResearchPercent = 100, GoldenAgePercent = 100
    Very Slow: ResearchPercent = 400, GoldenAgePercent = 400

`CvGame::getSpeedPercent()` returns ResearchPercent, so on Very Slow:

    expected duration/value scale from game length: roughly 4x
    current attitude valuation scale:             4x * 4x = 16x

The actual diplomacy memory does not last sixteen times longer; its decay adjustment is
about four times Normal. Conversely fast speeds square the reduction and can
under-value relationship effects.

CURRENT-DATA REPRODUCTION — JOYOUS WEDDING
------------------------------------------
`EVENTTRIGGER_JOYOUS_WEDDING` is an active three-choice other-player event
(90% of games, weight 200, neighboring player selected):

    EVENT_JOYOUS_WEDDING_1: no attitude change, no gold cost
    EVENT_JOYOUS_WEDDING_2: +1 AttitudeModifier / +1 OurAttitudeModifier, -15 gold
    EVENT_JOYOUS_WEDDING_3: +3 AttitudeModifier / +3 OurAttitudeModifier, -60 gold

All three have `iAIValue=1000`, so the relationship value and gold cost materially
shift their relative choice scores.

For a roughly Cautious, equal-power selected rival, the raw incoming-attitude term is
on the order of 50 value units per +1 attitude point (with the existing small random
attitude-weight perturbation). On Normal, +1 therefore remains about 50 before other
terms. On Very Slow, the current two multiplications turn the same raw relationship
term into roughly 800 rather than the roughly 200 implied by a single 4x duration
scale. The +3 paid choice is inflated analogously.

`CvPlayer::getEventCost` does not multiply the XML one-time gold amount by
`getSpeedPercent`; it applies inflation and optional tech cost. Thus the double speed
factor materially changes the diplomacy-versus-gold tradeoff rather than cancelling
against the event's cost.

`EVENTTRIGGER_WEDDING_FEUD` supplies the negative mirror: its -1/-2/-3 relationship
choices are also squared by speed. Other shipped multi-choice triggers with
`AttitudeModifier` include Famine, Airliner Crash, Fugitive and Spy Discovered.

ANCESTRY
--------
Civ4CE/BtS already contains both speed multiplications around the same
`iDiploValue` accumulator. K-Mod retains them while changing other event-value terms;
AdvCiv retains them while rewriting the attitude-power calculation (`advc.104z`), and
current SAS still has both multiplications.

This is therefore inherited stock BtS arithmetic rather than a SAS regression.

WHY THIS IS NOT AN INTENTIONAL TWO-STAGE SCALE
----------------------------------------------
The two-stage accumulator itself demonstrates the inconsistency. BonusGift value is
added *after* the first multiplication and consequently receives only the final speed
factor, while attitude value receives both. Other long-lived/per-turn event effects in
this same function (city yield/happiness, religion conversion, etc.) are scaled once
for game duration, not squared.

Moreover, the actual attitude-memory lifetime already scales approximately once with
game speed. No second independent gameplay duration is created by the event.

CONSEQUENCE
-----------
On slow/very-slow settings the AI can greatly overpay or choose otherwise inferior
random-event options for relationship changes, while on fast settings it can
under-value those same effects. The error grows nonlinearly with speed: 400% becomes
1600% for the affected diplomacy terms.

The result is not merely a hidden score mismatch because `AI_chooseEvent` directly
compares these values among current multi-choice EventInfos.

REPAIR DIRECTION
----------------
Apply game-speed scaling to each diplomacy term exactly once.

The smallest likely repair is to remove the early:

    iDiploValue *= iGameSpeedPercent;
    iDiploValue /= 100;

and retain the final common:

    iValue += (iDiploValue * iGameSpeedPercent) / 100;

That also keeps the current BonusGift term on the same one-speed-factor basis.
Alternatively, if term-specific scaling is desired, remove the final common scaling
and explicitly scale each component once; do not leave attitude terms squared.

DUPLICATE / ROOT SEPARATION
---------------------------
F353 is a BonusRevealed field-identity bug; F354 drops non-city building
Yield/Commerce; F355 mis-multiplies building-specific Happiness/Health. F356 is an
independent game-speed arithmetic bug in the diplomacy portion of the same evaluator.

The adjacent event-space-production source debt remains unnumbered because its only
current producer has no choice consequence.

CURRENT AUDIT STATE AFTER F356
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F356
F357 / provisional KI#680 — UNUSED
Latest physical checkpoint — C031-WIP46
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue `AI_eventValue` with F357 unused, especially
   `isOtherPlayerCityEffect()` because current Famine choices modify another player's
   city food while the city/global valuation branches skip that category.
2. Check requested religion-conversion counts against the actual capped eligible-city
   application path.
3. Then finish `AI_chooseEvent` and continue into `AI_doSplit` / post-event helpers.


===============================================================================
C031-WIP47 — F357 CONFIRMED
===============================================================================

F357 / provisional KI#680 — AI_eventValue values requested religion-conversion counts
instead of the number of eligible cities that applyEvent can actually convert, so
Inspired Mission can value 4 foreign conversions when only 1 will occur

STATUS
------
CONFIRMED — inherited BtS event-valuation executable-count bug retained by K-Mod,
AdvCiv 1.14 and current SAS. `EVENTTRIGGER_INSPIRED_MISSION` gives a clean current
multi-choice reproduction where two differently priced options can perform the same
actual foreign conversion but receive different conversion value.

ROOT
----
When a triggered religion exists, `AI_eventValue` directly multiplies the XML-requested
counts:

    iValue += (kEvent.getConvertOwnCities() * iReligionValue *
            iGameSpeedPercent) / 100;

    iValue += (kEvent.getConvertOtherCities() *
            (iOtherPlayerAttitudeWeight + 50) *
            iReligionValue * iGameSpeedPercent) / 15000;

It never counts how many cities are currently eligible for conversion.

But the legality and application layers explicitly do not guarantee the requested
number:

- `CvPlayer::canDoEvent` only searches until it finds *one* eligible own/foreign city;
- `CvPlayer::applyEvent` builds the vector of eligible cities;
- it randomly removes entries only while vector size exceeds the requested count;
- therefore the number actually converted is:

      min(kEvent.getConvert*Cities(), eligible_city_count)

The evaluator instead assumes the requested count is always executable.

CURRENT-DATA REPRODUCTION — INSPIRED MISSION
--------------------------------------------
`EVENTTRIGGER_INSPIRED_MISSION` is an active current random event (75% of games,
weight 200) that picks the state religion and another player. Its three choices are:

    EVENT_INSPIRED_MISSION_1
        ConvertOwnCities   = 4
        ConvertOtherCities = 0
        gold               = 0

    EVENT_INSPIRED_MISSION_2
        ConvertOwnCities   = 4
        ConvertOtherCities = 1
        gold               = -10

    EVENT_INSPIRED_MISSION_3
        ConvertOwnCities   = 4
        ConvertOtherCities = 4
        gold               = -25

All three have `iAIValue=1000` and `iMaxNumReligions=1`.

Suppose the selected rival has exactly one city eligible for the chosen religion.
Options 2 and 3 are both legal because `canDoEvent` requires only one eligible city.
At application time:

    option 2 converts min(1, 1) = 1 foreign city
    option 3 converts min(4, 1) = 1 foreign city

Their actual foreign-religion effect is therefore identical. But `AI_eventValue`
credits option 3 with four foreign conversions and option 2 with one, while option 3
also pays the larger 25-gold cost. The AI is comparing a three-conversion benefit that
will never happen.

The common `ConvertOwnCities=4` term does not muddy this clean comparison: it is the
same requested own-city count in all three choices. The independently varying foreign
count is enough to alter the option ranking.

The same mismatch can occur on the own-city side for events such as Miracle, Heresy,
Rabbi and quest completion rewards whenever fewer eligible cities exist than the XML
request. Inspired Mission is simply the strongest current decision-level producer.

ANCESTRY
--------
Civ4CE/BtS already values `getConvertOwnCities()` and `getConvertOtherCities()`
directly in `AI_eventValue`. K-Mod, AdvCiv and current SAS retain that requested-count
valuation. The application path in the same lineages caps the city vectors to the
requested maximum but naturally cannot create eligible cities that do not exist.

This is therefore an inherited stock BtS evaluator/application contract mismatch.

CONTROL — OTHER-PLAYER-CITY BRANCH
----------------------------------
The immediately preceding `isOtherPlayerCityEffect()` suspicion was checked and not
promoted. Famine 2/3 set both city flags; the runtime gives `isCityEffect()` precedence,
so their food effect goes through the selected ordinary city branch. The current pure
other-player-city Looters choices carry their material pillage effect through the
separately valued pillage fields. No current producer was found for the initially
suspected skipped generic other-city food/population valuation shape.

CONSEQUENCE
-----------
The AI can overpay for or prefer event options whose nominal religion-spread count
exceeds the currently eligible target set. In Inspired Mission, the expensive
four-foreign-city option can receive up to four times the foreign conversion value even
when it performs exactly the same one-city spread as the cheaper option.

REPAIR DIRECTION
----------------
Before valuing conversion, compute the number of eligible cities using the same
conditions as `canDoEvent` / `applyEvent`:

- chosen religion absent from city;
- `MaxNumReligions` condition satisfied;
- correct owner (self or triggered other player).

Then use:

    min(requested_count, eligible_count)

for each own/other conversion term. Ideally factor the eligibility/counting contract so
legality, valuation and application cannot drift independently.

DUPLICATE / ROOT SEPARATION
---------------------------
F357 is independent of F353's wrong BonusRevealed field, F354's dropped building
Yield/Commerce, F355's building Happiness/Health multiplicity and F356's double
speed-scaling of diplomacy. Here the field identity and units are correct; the error is
assuming all requested city conversions are executable.

CURRENT AUDIT STATE AFTER F357
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F357
F358 / provisional KI#681 — UNUSED
Latest physical checkpoint — C031-WIP47
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish the remaining `AI_eventValue` terms with F358 unused, including event
   FreeUnitSupport / promotion scope / plot-extra-yield and other player-target state.
2. Finish `AI_chooseEvent` itself and current recursive Additional/Clear producers.
3. Then continue into `AI_doSplit` and post-event empire/victory helpers.


===============================================================================
C031-WIP48 — F358 CONFIRMED
===============================================================================

F358 / provisional KI#681 — AI_eventValue adds PlotExtraYield to aiYields only after
all aiYields valuation has already run, so a permanent plot-yield event is valued at
zero whenever the selected plot has a working city

STATUS
------
CONFIRMED — inherited stock BtS event-valuation ordering bug retained by K-Mod,
AdvCiv 1.14 and current SAS. Current Great Beast supplies a live multi-choice producer.

ROOT
----
`AI_eventValue` first builds `aiYields` from BuildingExtraYields and then consumes that
accumulator in the city/global event branches:

    if (kEvent.isCityEffect())
    {
        ...
        iCityTurnValue += aiYields.get(YIELD_FOOD) * 5;
        iCityTurnValue += aiYields.get(YIELD_PRODUCTION) * 5;
        iCityTurnValue += aiYields.get(YIELD_COMMERCE) * 3;
        ...
    }
    else if (!kEvent.isOtherPlayerCityEffect())
    {
        ...
    }

Only *after those branches have finished*, the plot block processes permanent
`PlotExtraYield`:

    if (kEvent.getPlotExtraYield(eLoopYield) != 0)
    {
        if (pPlot->getWorkingCity() != NULL)
            aiYields.add(eLoopYield, kEvent.getPlotExtraYield(eLoopYield));
        else
            iValue += ...;
    }

There is no later read of `aiYields`. Therefore the add performed for a worked plot is
dead for this invocation. The exact permanent yield receives zero value.

By contrast, when the same selected plot currently has no working city, the `else`
branch immediately adds an explicit value. The evaluator therefore has the backwards
state dependency that a permanent tile improvement can be worth something when the
plot is unworked, yet worth nothing once a city actually works it.

RUNTIME EFFECT CONTRACT
-----------------------
`CvPlot::applyEvent` does not share this valuation omission. It loops every yield and
unconditionally applies the XML delta through:

    GC.getMap().setPlotExtraYield(*this, eLoopYield, iChange);

Thus the yield really is a persistent map effect regardless of whether the plot has a
working city at event-choice time.

CURRENT-DATA REPRODUCTION — GREAT BEAST
---------------------------------------
`EVENTTRIGGER_GREAT_BEAST` is active current XML (75% games, weight 100), requires a
Camp plot and offers three current choices. `EVENT_GREAT_BEAST_1` has:

    PlotExtraYields: +1 food
    iGold: 0
    iAIValue: 1000

The competing `EVENT_GREAT_BEAST_2` costs 15 gold and grants +1 population to the
selected city; `EVENT_GREAT_BEAST_3` runs its separate temporary-happiness Python
effect. All three participate in `AI_chooseEvent` through `AI_eventValue`.

If the selected Camp is already worked, option 1's +1 permanent food is added to
`aiYields` too late and contributes zero. The AI therefore compares Great Beast 1
without its main material benefit. If the same Camp is not worked, the fallback branch
credits the +1 food immediately (160 value at 100% speed under the stock formula).

Horticulture, Parrots, Jade, Black Pearls, Saltpeter, Truffles, Sea Turtles, Tin,
Prairie Dogs, Appleseed, Forty Thieves and Waters of Life provide additional current
`PlotExtraYield` producers. Great Beast is the cleanest current multi-choice proof.

ANCESTRY
--------
Civ4CE/BtS already contains the same ordering: the city/global `aiYields` use precedes
the plot-extra-yield block, and worked plots append to the already-consumed array.
K-Mod preserved it; AdvCiv converted the container syntax but retained the ordering;
current SAS still has the same control flow.

This is therefore inherited BtS rather than SAS-local.

CONSEQUENCE
-----------
The AI can under-value or reject permanent plot-yield event options precisely when the
affected tile is already economically relevant enough to be worked. In current Great
Beast, the +1-food option can be selected as though it had no +1-food effect at all,
changing its ranking against the population and happiness alternatives.

REPAIR DIRECTION
----------------
Do not append plot yield to a shared accumulator after that accumulator's consumers.
Either:

1. collect `PlotExtraYield` before the city/global yield valuation and route it through
   an appropriate plot-specific valuation; or
2. value the permanent plot yield directly inside the plot block for both worked and
   unworked cases.

If preserving the existing worked-vs-unworked distinction, a worked plot should use
its actual working-city/tile value rather than mutating `aiYields` after the fact.

DUPLICATE / ROOT SEPARATION
---------------------------
F354 concerns non-city BuildingExtraYield/Commerce fields disappearing from the
non-city branch. F358 is independent: the correct PlotExtraYield field is read and the
runtime effect is applied, but its worked-plot value is written into an accumulator
after its final read.

CURRENT AUDIT STATE AFTER F358
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F358
F359 / provisional KI#682 — UNUSED
Latest physical checkpoint — C031-WIP48
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish remaining `AI_eventValue` promotion / FreeUnitSupport / support-scope terms.
2. Audit `AI_chooseEvent`, especially recursive AdditionalEvents/ClearEvents valuation
   versus actual application probabilities and legality.
3. Then continue into `AI_doSplit` and post-event empire/victory helpers.


===============================================================================
C031-WIP49 — F359 CONFIRMED
===============================================================================

F359 / provisional KI#682 — AI_eventValue never values UnitClassPromotions even though
applyEvent grants them to all matching existing units and as permanent free promotions
for future units, so current multi-choice quest rewards can be ranked with an entire
reward branch missing

STATUS
------
CONFIRMED — inherited stock BtS event-valuation field omission retained by K-Mod,
AdvCiv 1.14 and current SAS. Several current quest-completion reward sets expose it.

ROOT
----
`AI_eventValue` has a dedicated promotion block, but it loops only UnitCombat:

    FOR_EACH_ENUM(UnitCombat)
    {
        if (NO_PROMOTION != kEvent.getUnitCombatPromotion(eLoopUnitCombat))
        {
            ... value existing matching units ...
            iPromotionValue += iNumCities * 50;
        }
    }

There is no corresponding `getUnitClassPromotion` read anywhere in the function.

The application layer explicitly supports both families. After applying UnitCombat
promotions, `CvPlayer::applyEvent` separately loops UnitClass and:

    - grants the event promotion to every existing unit of that UnitClass;
    - calls `setFreePromotion(eLoopUnitClass, eEventPromo, true)`, making the
      promotion available to future units of the class too.

Thus the runtime reward can be broad and permanent while AI valuation is exactly zero
for that field.

CURRENT-DATA REPRODUCTIONS
--------------------------
Current SAS contains many UnitClassPromotion events, including multi-choice quest
rewards where the omission can directly alter the selected reward.

A particularly clean pair is `EVENT_ELITE_SWORDS_DONE`:

    EVENT_ELITE_SWORDS_DONE_1
        UnitClassPromotion:
            UNITCLASS_SWORDSMAN -> PROMOTION_CITY_RAIDER1
        iAIValue = 1000

    EVENT_ELITE_SWORDS_DONE_2
        UnitCombatPromotions:
            UNITCOMBAT_MELEE_POLEARM -> PROMOTION_FIRST_STRIKE1
            UNITCOMBAT_MELEE_SHOCK   -> PROMOTION_FIRST_STRIKE1
        iAIValue = 1000

The second reward enters the existing promotion valuation block; the first reward's
promotion enters no valuation block at all. Yet the first reward is actually applied to
all current Swordsman-class units (including the civilization replacement for that
class) and becomes a permanent free class promotion for future Swordsman-class units.

Other current multi-choice examples include:

- `MASTER_BLACKSMITH_DONE_2`: Swordsman-class Counter Melee;
- `WAR_CHARIOTS_DONE_1`: Chariot-class Combat I;
- `WARSHIPS_DONE_1`: Trireme-class Combat I;
- `GUNS_BUTTER_DONE_1`: Musketman-class Counter Gun;
- `NOBLE_KNIGHTS_DONE_1`: Horse/Camel Knight class Retreat;
- `OVERWHELM_DONE_1`: Combat I for Destroyer, Battleship, Carrier, Fighter and Jet
  Fighter classes.

`OVERWHELM_DONE` is especially broad: reward 1 grants five permanent class promotions,
yet none of those promotion effects contributes to `AI_eventValue`.

ANCESTRY
--------
Civ4CE/BtS already contains only a `getUnitCombatPromotion` valuation loop and no
`getUnitClassPromotion` valuation. K-Mod retains the omission; AdvCiv retains it;
current SAS retains it while continuing to use UnitClassPromotion XML extensively.

This is therefore inherited BtS rather than a SAS-local regression.

CONSEQUENCE
-----------
The AI can systematically under-value UnitClassPromotion reward options and prefer
competing rewards whose fields happen to be represented in `AI_eventValue`. The bias
is not a minor coefficient disagreement: the entire current-unit benefit and the
permanent future free-promotion benefit are absent.

REPAIR DIRECTION
----------------
Add a UnitClass promotion valuation parallel to the existing UnitCombat block:

- value current owned units of the affected UnitClass that lack the promotion;
- include a reasonable future-production/free-promotion term;
- avoid double counting if an event grants the same promotion through both UnitCombat
  and UnitClass paths to the same units.

Ideally factor the common promotion-value logic so application and valuation continue
to support the same two field families.

DUPLICATE / ROOT SEPARATION
---------------------------
F359 is independent of F358's late PlotExtraYield ordering. Here the event field is
never read by the evaluator at all. It also differs from F354's non-city building
Yield/Commerce omission because UnitClassPromotion has its own distinct runtime and
multi-choice producers.

CURRENT AUDIT STATE AFTER F359
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F359
F360 / provisional KI#683 — UNUSED
Latest physical checkpoint — C031-WIP49
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish `AI_eventValue` support/promotion-tail controls and recursive
   AdditionalEvents/ClearEvents valuation.
2. Finish `AI_chooseEvent`.
3. Continue into `AI_doSplit`, `AI_launch`, and post-event empire/victory helpers.


===============================================================================
C031-WIP50 — F360 CONFIRMED
===============================================================================

F360 / provisional KI#683 — AI_eventValue values TechPercent as the full requested
percentage of total tech cost instead of capping it to remaining research like the
runtime, so near-complete tech rewards are overvalued

STATUS
------
CONFIRMED — inherited stock BtS requested-progress-versus-executable-progress bug
retained by K-Mod, AdvCiv 1.14 and current SAS. Current Airliner Crash supplies a clean
multi-choice reproduction with no minimum-turn guard.

ROOT
----
For a fixed event technology, `AI_eventValue` immediately adds:

    iValue += (kTeam.getResearchCost(kEvent.getTech()) *
            kEvent.getTechPercent()) / 100;

For flavor/current-research event technologies, the later `eBestTech` path uses the
same full-cost percentage formula.

The application layer does not award that amount blindly. `CvPlayer::applyEvent`
ultimately calls:

    CvTeam::changeResearchProgressPercent(...)

and that function explicitly caps a positive award:

    iBeakers = std::min(getResearchLeft(eIndex),
            getResearchCost(eIndex) * iPercent / 100);

Thus the executable value is:

    min(requested_percent_of_total_cost, research_left)

whereas AI valuation assumes the requested percentage is always fully usable.

CURRENT-DATA REPRODUCTION — AIRLINER CRASH
------------------------------------------
`EVENT_AIRLINER_CRASH_3` is one of three current choices and has:

    Tech              = TECH_FLIGHT
    iTechPercent      = 15
    iTechMinTurnsLeft = 0
    iGold              = 0
    iAIValue           = 1000

Its trigger selects another player who already knows Flight, so this option is a live
catch-up research reward while the AI itself can still research Flight.

Suppose the AI has already completed 90% of Flight. The option is legal: there is no
minimum-turn requirement, and Flight is still researchable before the event.

Runtime:

    requested award = 15% of total Flight cost
    research left   = 10%
    actual award    = 10%

Evaluator:

    credits 15%

The AI therefore overstates the immediate research benefit by 50% in this simple
state. At 99% progress it can credit fifteen times the research that will actually be
added.

Other current TechPercent events (City Ruins, Patron of Knowledge, Ancient Texts,
Secret Knowledge, Heresy and others) inherit the same cap mismatch whenever the
selected event tech has less remaining research than the XML percentage.

ANCESTRY
--------
Civ4CE/BtS already values `researchCost * TechPercent / 100` directly in
`AI_eventValue`; its application path caps positive progress to research remaining.
K-Mod and AdvCiv preserve that contract mismatch; current SAS does too.

This is therefore inherited BtS rather than SAS-local.

CONSEQUENCE
-----------
The AI can prefer or overpay for tech-progress event options near the end of a
technology because it prices beakers that cannot be applied. This is decision-level in
current multi-choice events such as Airliner Crash and Heresy.

REPAIR DIRECTION
----------------
Resolve the same event technology used by the application path, then value positive
TechPercent by the executable beaker amount:

    min(researchLeft(eTech), researchCost(eTech) * TechPercent / 100)

Negative TechPercent should likewise mirror `changeResearchProgressPercent`'s lower
bound rather than assuming the raw requested percentage if that branch is retained.

Prefer factoring/shared helper logic so legality, event cost, application and AI value
agree on the selected event tech and executable progress.

DUPLICATE / ROOT SEPARATION
---------------------------
F357 is the analogous city-conversion requested-count cap, but the state and runtime
contract here are independent: F360 concerns research progress and `researchLeft`.
F360 also exists before considering any AdditionalEvent follow-up; that post-state
problem remains a separate F361 candidate.

CURRENT AUDIT STATE AFTER F360
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F360
F361 / provisional KI#684 — UNUSED
Latest physical checkpoint — C031-WIP50
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish AdditionalEvents valuation against the post-main-event legality state,
   especially Airliner Crash 3 -> 4.
2. Finish ClearEvents controls and `AI_chooseEvent`.
3. Continue into `AI_doSplit`, `AI_launch` and post-event helpers.


===============================================================================
C031-WIP51 — F361 CONFIRMED
===============================================================================

F361 / provisional KI#684 — AI_eventValue recursively values AdditionalEvents against
the pre-parent-event state, while runtime rechecks their legality after the parent
has already changed state, so it can credit follow-up effects that cannot fire

STATUS
------
CONFIRMED — inherited stock BtS recursive event-valuation state-transition bug retained
by K-Mod, AdvCiv 1.14 and current SAS. Airliner Crash 3 -> 4 supplies a current,
independent reproduction.

ROOT
----
At the end of `AI_eventValue`, every non-self AdditionalEvent is valued directly from
the original triggered data and the player's *current pre-choice state*:

    iValue += chance * AI_eventValue(eAdditional, kTriggeredData) / 100;

No parent-event effects are simulated before that recursive call, and no
`canDoEvent(eAdditional, ...)` check is performed for the post-parent state.

Runtime ordering is different and explicit:

1. `CvPlayer::applyEvent(parent)` applies the parent's gold/research/city/etc. effects;
2. only near the end does it process AdditionalEvents;
3. for an immediate follow-up it requires:

       canDoEvent(eAdditional, *pTriggeredData)

   *after* the parent has already changed game state;
4. only then can it call `applyEvent(eAdditional, ..., false)`.

Therefore parent effects can invalidate or reduce a child event while the evaluator
still prices the child from the old state.

CURRENT-DATA REPRODUCTION — AIRLINER CRASH 3 -> 4
-------------------------------------------------
`EVENT_AIRLINER_CRASH_3` grants +15% Flight research and has:

    AdditionalEvent EVENT_AIRLINER_CRASH_4, chance 50%

`EVENT_AIRLINER_CRASH_4` grants +20% Flight research. Both require Flight to remain a
valid event tech through the ordinary `canDoEvent` -> `getBestEventTech` ->
`isValidEventTech` path; once the team has Flight, `canResearch(Flight)` is false and
the follow-up cannot execute.

Use a state with exactly 15% of Flight research remaining.

Parent runtime:

    Airliner Crash 3 applies +15%
    Flight completes

Follow-up runtime:

    50% chance branch is reached
    canDoEvent(EVENT_AIRLINER_CRASH_4) == false
    event 4 does not apply
    actual follow-up research value = 0

AI valuation:

    recursively calls AI_eventValue(EVENT_AIRLINER_CRASH_4)
    while Flight is still incomplete in the evaluator's real game state
    credits 50% of the follow-up's research value

This reproduction is deliberately independent of F360. At exactly 15% remaining, the
parent's requested +15% is itself fully executable, so fixing F360's research-left cap
does not remove F361. Even with F360 fixed, the child would still be valued from the
pre-parent state despite being illegal after the parent completes Flight.

The mismatch also occurs without full invalidation. If a parent reduces the amount of
research left, the child's executable reward can shrink before it fires; recursively
valuing the child from the original state still overstates it.

ANCESTRY
--------
Civ4CE/BtS already recursively calls `AI_eventValue` for AdditionalEvents without
simulating the parent state. Its runtime applies the parent before checking/performing
immediate additional events. K-Mod and AdvCiv retain that split; current SAS does too.

This is therefore inherited BtS.

CONSEQUENCE
-----------
The AI can choose an event partly for an expected follow-up that the parent event itself
makes impossible. Airliner Crash 3 can receive phantom expected Flight research when
its own research grant completes Flight first, biasing its ranking against the other
current Airliner Crash choices.

REPAIR DIRECTION
----------------
AdditionalEvent expected value must be based on the state in which the child would
actually execute. Robust options include:

- simulate/project the relevant parent state before child valuation;
- create an event-value helper that accepts projected changes (research, bonus reveal,
  religion/city state, etc.); or
- for simple child contracts, explicitly compute post-parent legality/executable value.

Merely calling `canDoEvent` before the recursive call is insufficient because that
would still test the pre-parent state. The key requirement is post-parent state.

DUPLICATE / ROOT SEPARATION
---------------------------
F360 caps the parent's/child's requested TechPercent to research remaining in whatever
state is being evaluated. F361 concerns *which state* is evaluated. Fixing F360 still
leaves the exactly-15%-remaining Airliner reproduction above.

CURRENT AUDIT STATE AFTER F361
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F361
F362 / provisional KI#685 — UNUSED
Latest physical checkpoint — C031-WIP51
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish ClearEvents valuation controls and `AI_chooseEvent`.
2. Continue into `AI_doSplit`, `AI_launch`, `AI_doCheckFinancialTrouble` and adjacent
   post-event empire/victory helpers.


===============================================================================
C031-WIP52 — F362 CONFIRMED
===============================================================================

F362 / provisional KI#685 — AI_roundTradeValBounds can leave a rounded value outside
its requested bounds when the preferred multiple is unavailable, so a generous
counterproposal whose lower bound is the original gold amount can be rounded *down*
below that lower bound

STATUS
------
CONFIRMED — inherited AdvCiv helper-contract bug introduced with `AI_roundTradeValBounds`
and retained by current SAS. The two live `AI_balanceDeal` gold callers use exactly the
bound shape that exposes it.

ROOT
----
The helper's explicit contract says:

    Set iTradeVal to the nearest multiple of DIPLOMACY_VALUE_REMAINDER
    in [iLower,iUpper]. No change if no such multiple exists.

For `bPreferRoundingUp`, however, implementation does:

    int iTmpVal = iTradeVal;
    AI_roundTradeVal(iTradeVal);      // mutates iTradeVal DOWN immediately
    if (iTradeVal != iTmpVal)
        iTmpVal = iTradeVal + remainder; // preferred UP candidate
    if (iTmpVal within bounds)
        iTradeVal = iTmpVal;

If the upward candidate is outside the interval, nothing restores the original value
or validates the already-mutated downward value. The function can therefore return a
value outside `[iLower,iUpper]`, contradicting both its comment and its parameter
contract.

CURRENT CALLER REPRODUCTION
---------------------------
Current `AI_balanceDeal` has two equivalent gold-balancing sites. When
`bTheyGenerous` and either party is human, they call:

    AI_roundTradeValBounds(iGoldData, true, iGoldData, iMaxGold);

The original required gold amount is deliberately passed as `iLower`; i.e. generous
rounding may round UP but must not lower the balancing amount.

Current SAS defines:

    DIPLOMACY_VALUE_REMAINDER = 5

Take a fully ordinary state:

    computed iGoldData = 12
    iMaxGold           = 14
    bounds             = [12,14]

There is no multiple of 5 in [12,14]. The documented result is therefore “no change”:
12.

Current implementation:

    iTradeVal = 12
    AI_roundTradeVal -> 10
    preferred upward multiple -> 15
    15 is outside [12,14]
    assignment is skipped
    returned iTradeVal -> 10

The result is not merely the nonpreferred multiple; 10 is explicitly below the lower
bound 12.

CALLER CONSEQUENCE
------------------
`AI_balanceDeal` then sees `iMaxGold >= iGoldData` and can insert the accidental 10
gold into the counterproposal. In the generous branch, the caller's design is that the
side being balanced may overshoot / offer a larger treasury portion, not that the
required balancing amount may be rounded down. The helper reverses that policy in the
exact max-gold gap where the next display multiple is unaffordable.

The same defect exists at both the initial-gold and final-gold balancing sites.

INTRODUCTION HISTORY
--------------------
The AdvCiv commit that introduced/refactored the helper adds the exact comment quoted
above and the same implementation. The same change updates the generous balance caller
to pass:

    lower = original iGoldData
    upper = iMaxGold

So intent and failure were introduced together: the lower bound exists specifically to
protect the unrounded balancing requirement, but the helper can return below it.
Base AdvCiv 1.14 retains the same body; current SAS only modernizes surrounding style.

This root is AdvCiv-local rather than BtS/K-Mod: BtS/K-Mod did not have this bounded
rounding helper in this form.

REPAIR DIRECTION
----------------
Never mutate the caller's value until a valid candidate has been selected. For example:

    original = iTradeVal
    down = rounded-down multiple
    up = (down == original ? original : down + remainder)

Try candidates in requested preference order, assigning `iTradeVal` only if that
candidate lies within `[iLower,iUpper]`. If neither candidate lies in bounds, leave the
original unchanged exactly as documented.

A minimal patch can also store/restore the original on failure, but computing both
candidates independently is clearer and avoids the recursive false-path state mutation.

DUPLICATE / ROOT SEPARATION
---------------------------
F335 concerns invalid fallback selection of a non-surplus resource in `AI_balanceDeal`;
F336/F337 concern leniency accounting. F362 is independent: the correct gold amount is
computed and the correct bounds are supplied, but the rounding helper violates those
bounds before the caller sees the result.

CURRENT AUDIT STATE AFTER F362
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F362
F363 / provisional KI#686 — UNUSED
Latest physical checkpoint — C031-WIP52
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through victory-stage functions beginning with Culture/Space.
2. Keep `AI_doSplit` / `AI_launch` controls open if caller or state archaeology exposes
   a stronger invariant violation.


===============================================================================
C031-WIP53 — F363 CONFIRMED
===============================================================================

F363 / provisional KI#686 — AI_calculateConquestVictoryStage mis-parenthesizes the
"don't spare friends when only military victory is possible" exception, so a
nonpositive other-victory weight bypasses every level-3 Conquest gate instead of only
the friendly-rival gate

STATUS
------
CONFIRMED — inherited AdvCiv precedence/regression bug introduced by advc.104c in the
2025 commit "AI doesn't try sparing friends when only military victory possible" and
retained by Base AdvCiv 1.14 and current SAS.

ROOT
----
Current level-3 test ends with:

    if (iKnownCivs >= iStartCivs && bTopRank &&
        !bManyOffshoreRivals &&
        (bVeryStrong ||
         (bWarmonger && bHateful && 2 * iConqueredCivs >= iKnownCivs)) &&
        (iFriends == 0 || (iFriends == iRemaining && iFriends == 1)) ||
        iOtherVictoryWeight <= 0)

C++ `&&` binds more tightly than `||`, so this is parsed as:

    (all ordinary level-3 gates && friend gate) ||
    (iOtherVictoryWeight <= 0)

The introducing commit's stated purpose is much narrower. Its full commit message says:

    "I.e. it won't shy away from stages 3 and 4 of a military victory on account of
    friendly relations when all other victory conditions have been disabled."

The diff itself changes only the tail of the old friend condition, making the intended
logical grouping clear. The exception belongs *inside* the final friendship gate:

    ... &&
    (friend gate || iOtherVictoryWeight <= 0)

It was instead appended outside the whole conjunction.

REACHABILITY / CURRENT CONTRACT
-------------------------------
`AI_updateVictoryWeights` stores -1 for victory conditions that are invalid/disabled
and ordinary nonnegative personality weight for valid ones. `AI_calculateConquest...`
sums every non-Conquest victory into `iOtherVictoryWeight`.

Therefore a supported custom-game setup with Conquest enabled and all alternative
victory conditions disabled naturally makes `iOtherVictoryWeight <= 0`. This is exactly
the unusual-victory-settings case named by the introducing commit, not a hypothetical
personality state.

The outer level-2 conditions still have to succeed; this does *not* create Conquest 3
from an otherwise uninterested AI. The defect is the transition from stage 2 to stage 3.

CLEAN REPRODUCTION
------------------
Use a Conquest-only victory setup and a warmongering AI whose current state satisfies
the level-1/2 conditions, for example:

    iStartCivs      = 4
    iKnownCivs      = 3
    iConqueredCivs  = 1
    bWarmonger      = true

Then the level-2 progress tests can pass:

    4 * iKnownCivs >= 3 * iStartCivs
    4 * 3 >= 3 * 4                       -> true

    3 * iConqueredCivs >= iKnownCivs
    3 * 1 >= 3                           -> true

But the intended level-3 test should still fail immediately because:

    iKnownCivs >= iStartCivs
    3 >= 4                               -> false

It may independently also fail `bTopRank`, `!bManyOffshoreRivals`, or the stronger
power/warmonger+hateful threshold.

In a Conquest-only game, however, `iOtherVictoryWeight <= 0` is true. Due to operator
precedence the entire ordinary level-3 conjunction is bypassed, and the function enters
the level-3 block and ultimately returns at least 3.

This precisely exceeds the commit's intended exception: disabling other victories was
supposed to permit fighting Friendly rivals, not to erase knowledge, rank, geography,
power and conquest-progress requirements.

CONSEQUENCE
-----------
Conquest stage 3 is a broadly consumed strategy flag, not merely UI state. Current
consumers include, among others:

- `AI_maxUnitCostPerMil`: military stage 3 adds +20 to recommended unit spending;
- `AI_nukeBaseDestructionWeight`: Conquest 3 increases willingness to value civilian
  nuclear destruction;
- CvTeamAI war valuation: Conquest 3 reduces diplomacy cost in war calculations;
- additional team/UWAI/city-production and victory-strategy consumers throughout the
  DLL.

Thus unusual Conquest-only settings can push a stage-2 AI into materially more extreme
military behavior before the ordinary stage-3 readiness conditions are met.

INTRODUCTION / ANCESTRY
-----------------------
Commit 4baba59a4380e956452d6baf05c8ca36aa3d2b6d (2025-01-10), title:

    AI doesn't try sparing friends when only military victory possible

changed:

    ... && friend_condition)

to:

    ... && friend_condition ||
    iOtherVictoryWeight <= 0)

without adding parentheses around the intended final subcondition. The same precedence
shape is present in Base AdvCiv 1.14 and current SAS.

K-Mod predates this advc.104c exception; this root is therefore an AdvCiv regression,
not a BtS/K-Mod inheritance.

REPAIR DIRECTION
----------------
Group the exception with the friendship test only:

    ... &&
    (bVeryStrong || ...) &&
    ((iFriends == 0 || (iFriends == iRemaining && iFriends == 1)) ||
     iOtherVictoryWeight <= 0)

Or factor the final friendship permission into a named boolean before the conjunction,
which would make precedence mistakes much harder to reintroduce.

The stage-4 inner checks should then remain unchanged; the purpose of advc.104c is still
preserved because a Conquest-only AI no longer has to satisfy the friendship blocker.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of prior victory-weight tuning and of F344 (`AI_isFocusWar`). F363
is specifically an operator-precedence error in the stage-2 -> stage-3 Conquest
transition introduced by the "don't spare friends" exception.

CURRENT AUDIT STATE AFTER F363
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F363
F364 / provisional KI#687 — UNUSED
Latest physical checkpoint — C031-WIP53
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through Domination and Diplomacy victory-stage calculations, using F363 as
   a precedence/control boundary.
2. Then inspect `AI_updateVictoryWeights` / stage-hash consumers for independent
   validity, redistribution or state-transition bugs rather than retuning coefficients.


===============================================================================
C031-WIP54 — F364 CONFIRMED
===============================================================================

F364 / provisional KI#687 — AI_doCheckFinancialTrouble consumes the
false->true financial-trouble transition even when the shared production-dirty cooldown
suppresses the requested refresh, so the deferred refresh is never retried for the
rest of that trouble episode

STATUS
------
CONFIRMED — inherited BtS transition/cooldown bug present in Civ4CE/BtS, K-Mod,
Base AdvCiv 1.14 and current SAS.

ROOT
----
`AI_doCheckFinancialTrouble` is explicitly edge-triggered:

    bool bFinancialTrouble = AI_isFinancialTrouble();
    if (bFinancialTrouble != m_bWasFinancialTrouble)
    {
        if (bFinancialTrouble)
        {
            int iGameTurn = GC.getGame().getGameTurn();
            // only reset at most every 10 turns
            if (iGameTurn > m_iTurnLastProductionDirty + 10)
            {
                AI_makeProductionDirty();
                m_iTurnLastProductionDirty = iGameTurn;
            }
        }
        m_bWasFinancialTrouble = bFinancialTrouble;
    }

The cooldown timestamp is shared with other production-refresh producers, notably
`AI_nowHasTech`. Therefore entering financial trouble can legitimately happen while
`m_iTurnLastProductionDirty` is recent.

The bug is that the transition state is committed regardless of whether the intended
financial-trouble refresh was actually allowed. Once `m_bWasFinancialTrouble` becomes
true, persistent financial trouble no longer satisfies the outer transition test, so
expiration of the 10-turn cooldown cannot trigger the missed refresh later.

CLEAN REPRODUCTION
------------------
For example:

    turn 100: AI_nowHasTech dirties production
              m_iTurnLastProductionDirty = 100
              m_bWasFinancialTrouble = false

    turn 105: empire first enters financial trouble
              bFinancialTrouble = true
              105 > 100 + 10 -> false
              AI_makeProductionDirty is skipped
              m_bWasFinancialTrouble = true anyway

    turn 111+: cooldown is now old enough, but
               bFinancialTrouble == m_bWasFinancialTrouble == true
               outer transition branch never runs again

The refresh can therefore be skipped for the entire continuous financial-trouble
episode, not merely delayed to turn 111.

LIVE CONSEQUENCE / CITY CONTRACT
--------------------------------
`AI_makeProductionDirty` sets `chooseProductionDirty` on every AI city. `CvCity::doProduction`
checks that flag even when the city already has active production and calls
`CvCityAI::AI_chooseProduction`; that function can clear/reconsider the current queue.

Financial trouble materially changes that chooser. Current code, among other effects:

- makes a near-trouble empire seek `BUILDINGFOCUS_GOLD`;
- reduces ordinary unit-production pressure through `AI_buildUnitProb`;
- changes Settler continuation/production behavior;
- gates or suppresses multiple worker, naval and military production paths.

Thus the dirty refresh is not redundant bookkeeping: it is the mechanism intended to
make already-busy cities react promptly when the empire crosses into the financial-
trouble state. Normal production completion can eventually cause individual cities to
choose again, but that does not repair the missed empire-wide transition refresh and
long builds can remain selected under stale healthy-economy assumptions.

The source comment itself states the intended event:

    // if we just ran into financial trouble this turn

and, when the cooldown permits:

    // redeterimine the best things to build in each city

The implementation loses that event when the cooldown blocks the immediate action.

ANCESTRY
--------
The same state machine appears in the Civ4CE/BtS source, in K-Mod 1.46, in Base AdvCiv
1.14 and in current SAS. K-Mod inherited/retained the original structure and later
changed many financial-trouble heuristics without repairing this edge/cooldown
interaction.

This is therefore an inherited BtS root rather than a SAS or AdvCiv regression.

REPAIR DIRECTION
----------------
Preserve the cooldown, but do not consume the entering-trouble transition until its
requested refresh has occurred, or represent the missed action explicitly as pending.

A minimal state-oriented repair could keep `m_bWasFinancialTrouble == false` while
`bFinancialTrouble` is true and the cooldown blocks the refresh; then a later turn will
retry and finally set the remembered state once `AI_makeProductionDirty` succeeds.
That needs care on a true->false exit so the remembered state remains semantically
sound.

A clearer repair is to track a separate pending financial-trouble production refresh:
set it on the false->true transition, execute it once the shared cooldown permits, then
clear the pending flag. This keeps `m_bWasFinancialTrouble` as a pure observed-state
cache and makes deferred-work semantics explicit.

Simply removing the 10-turn rate limit is not necessary to fix the root.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of SAS financial-trouble production tuning and of earlier unit-
spending findings. Those determine *what* cities choose once reevaluated. F364 concerns
the state-transition scheduler failing to deliver the intended reevaluation at all
when another recent producer has occupied the shared cooldown.

CURRENT AUDIT STATE AFTER F364
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F364
F365 / provisional KI#688 — UNUSED
Latest physical checkpoint — C031-WIP54
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue below the victory/financial-trouble tranche into the remaining strategy,
   launch, empire-management and utility helpers.
2. Keep the shared production-dirty cooldown as a control boundary: do not create a
   second root unless another producer has an independent state/consequence defect.


===============================================================================
C031-WIP55 — F365 CONFIRMED
===============================================================================

F365 / provisional KI#688 — AI_updateGoldToUpgradeAllUnits caches a per-unit
upgradePrice by UnitType, so Great-General units with PROMOTION_LEADER's 100% upgrade
discount can be charged the ordinary same-type upgrade cost in the aggregate AI gold
reserve

STATUS
------
CONFIRMED — inherited BtS bug present in Civ4CE/BtS, K-Mod 1.46, Base AdvCiv 1.14
and current SAS.

ROOT
----
`AI_updateGoldToUpgradeAllUnits` deliberately caches the estimated upgrade cost of each
unit type:

    // cache the value for each unit type
    std::vector<int> aiUnitUpgradePrice(GC.getNumUnitInfos(), 0);

For each owned unit it first checks:

    UnitTypes eUnitType = pLoopUnit->getUnitType();
    int iCachedUnitGold = aiUnitUpgradePrice[eUnitType];
    if (iCachedUnitGold != 0)
    {
        if (iCachedUnitGold > 0)
            iTotalGold += iCachedUnitGold;
        continue;
    }

When there is no cached value, the function discovers desirable/available upgrade
classes and accumulates the price by calling the *individual unit*:

    iUnitGold += pLoopUnit->upgradePrice(eUpgradeUnitType);

then stores the resulting average under only `eUnitType`:

    aiUnitUpgradePrice[eUnitType] = iUnitGold;

That cache key is invalid because `CvUnit::upgradePrice` is not type-invariant. It ends
with a per-unit promotion-derived adjustment:

    iPrice -= (iPrice * getUpgradeDiscount()) / 100;

Current SAS retains the standard Great-General promotion:

    PROMOTION_LEADER
        iUpgradeDiscount = 100

and attaching the Great General calls `promote(PROMOTION_LEADER, ...)`; promotion
processing feeds that XML value into the unit's `m_iUpgradeDiscount` through
`changeUpgradeDiscount`.

Thus an ordinary unit and a Great-General-attached unit of the exact same UnitType can
have different — in the leader case, radically different — `upgradePrice` values.
Caching that value by UnitType alone loses real per-unit state.

CLEAN CURRENT-DATA REPRODUCTION
-------------------------------
A Rifleman is a straightforward current example:

- `UNIT_RIFLEMAN` is `UNITCOMBAT_GUN` and upgrades to `UNITCLASS_INFANTRY`;
- `PROMOTION_LEADER` is valid for `UNITCOMBAT_GUN`;
- the promotion has `iUpgradeDiscount = 100`;
- therefore an attached Great-General Rifleman's real `upgradePrice(Infantry)` is 0,
  while an otherwise ordinary Rifleman's price is positive whenever the upgrade has a
  positive base cost.

Suppose the player's unit iteration encounters an ordinary Rifleman first. The normal
positive Infantry upgrade estimate is stored in:

    aiUnitUpgradePrice[UNIT_RIFLEMAN]

When a later Great-General Rifleman is reached, the cache branch adds that same positive
amount directly to `iTotalGold` and never calls the leader unit's own
`upgradePrice(Infantry) == 0`.

The iteration order does not make the root disappear. If the free-upgrade leader unit
happens to be encountered before any ordinary copy, its computed zero is also the
vector's "not cached" sentinel, so a later ordinary copy is recomputed and establishes
the positive type cache; any subsequent leader copy is again overcounted. The defect is
therefore fundamentally the cache key, not one particular order.

A second independent reason the UnitType key is too coarse is already present in the
same function: the set of upgrades regarded as useful is tested with the individual
unit's `UnitAI` and area:

    AI_unitValue(eUpgradeUnitType, eUnitAIType, pUnitArea)

so same-type units can also disagree about which upgrade candidates should contribute.
The Great-General discount is the cleaner shipped-data proof because it requires no
argument about AI-role valuation coefficients.

LIVE CONSEQUENCE
----------------
The result is saved in `m_iUpgradeUnitsCachedGold` by `AI_updateCacheData` and is used as
an important empire-level budget input rather than debug-only information.

`AI_goldTarget` begins with:

    int iUpgradeBudget = AI_getGoldToUpgradeAllUnits();

and folds a war/financial-trouble-adjusted fraction of that estimate into the AI's gold
reserve. `AI_doTurnUnitsPost` also derives its actual wartime/peacetime upgrade spending
budget from `AI_goldTarget(true)`; other consumers include hurry/armament logic and the
current SAS upgrade diagnostics.

Therefore a Great-General unit whose next upgrade is actually free can nevertheless
make the AI believe that another ordinary upgrade price must be reserved. Depending on
war/economic state, that can make it hoard too much cash, alter commerce allocation and
change other spending decisions. Actual per-unit upgrading still calls the real
`upgradePrice`, so the execution layer can know the leader upgrade is free while the
strategic reserve that enabled it remains inflated — exactly the estimator/executor
state mismatch at the root.

ANCESTRY
--------
The same UnitType cache and per-unit `upgradePrice` call are present in Civ4CE/BtS,
K-Mod 1.46, Base AdvCiv 1.14 and current SAS.

The XML producer is equally old: Civ4CE/BtS and Base AdvCiv both define
`PROMOTION_LEADER` with `iUpgradeDiscount = 100`, including `UNITCOMBAT_GUN` among the
eligible combat classes. Great-General attachment applies the leader promotion to the
selected unit in the normal shipped gameplay path.

This is therefore an inherited BtS cache-key defect, not a SAS/K-Mod tuning regression.

REPAIR DIRECTION
----------------
Do not cache the final `upgradePrice` solely by UnitType.

Safe options include:

1. cache only type-invariant discovery data (e.g. reachable upgrade target classes),
   then call each physical unit's `upgradePrice` when adding its contribution; or
2. expand the cache key to include every state that affects the result — at minimum
   upgrade discount, UnitAI and relevant area/can-train context — though that is more
   complicated and less robust than option 1.

Given that `upgradePrice` is explicitly a `CvUnit` method and promotion state can alter
it, retaining the per-unit price call while caching only reusable candidate discovery
looks like the clearest repair.

Also avoid using literal 0 simultaneously as both "not cached" and a legitimate cached
free-upgrade value if any redesigned cache continues to store prices.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of F151/KI#474, which concerned the engine/UI upgrade action cache.
F365 concerns the AI's aggregate strategic gold estimator in `CvPlayerAI` and its invalid
assumption that an individual `CvUnit::upgradePrice` can be memoized by UnitType.

It is also separate from SAS KI#160's actual upgrade-spending cap/diagnostics: those
operate when choosing and paying for concrete upgrades, whereas F365 corrupts the
cached amount the empire believes it should reserve before those per-unit decisions.

CURRENT AUDIT STATE AFTER F365
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F365
F366 / provisional KI#689 — UNUSED
Latest physical checkpoint — C031-WIP55
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_updateAvailableIncome`, `AI_calculateAverages` and the nearby
   strategy/empire caches, treating the repaired break-even denominator as a control.
2. Then continue into player closeness/paranoia/floating-defender and Advanced Start
   helpers, looking for current scope/state/cache errors rather than retuning heuristic
   coefficients.


===============================================================================
C031-WIP56 — F366 CONFIRMED
===============================================================================

F366 / provisional KI#689 — AI_advancedStartPlaceExploreUnits selects a sea
explorer as UNITAI_EXPLORE_SEA but hard-codes UNITAI_EXPLORE when purchasing it, so
Advanced Start naval explorers are initialized with the land-explorer AI role

STATUS
------
CONFIRMED — AdvCiv regression introduced by advc.250c and present in Base AdvCiv 1.14
and current SAS. BtS/Civ4CE and K-Mod predate the explicit UnitAI argument and do not
contain this hard-coded-role regression.

ROOT
----
`AI_advancedStartPlaceExploreUnits(bool bLand)` correctly derives the requested role:

    UnitAITypes eUnitAI = NO_UNITAI;
    if (bLand)
        eUnitAI = UNITAI_EXPLORE;
    else
        eUnitAI = UNITAI_EXPLORE_SEA;

That variable is used throughout selection:

- nearby explorer counting asks for `eUnitAI`;
- `AI_bestAdvancedStartUnitAI(kCityPlot, eUnitAI)` selects a unit that is valuable for
  precisely that requested role.

But the purchase call discards the variable and always supplies the land role:

    doAdvancedStartAction(ADVANCEDSTARTACTION_UNIT,
            pBestExplorePlot->getX(), pBestExplorePlot->getY(), eBestUnitType, true,
            UNITAI_EXPLORE); // advc.250c

Thus `bLand == false` performs selection under `UNITAI_EXPLORE_SEA` and execution under
`UNITAI_EXPLORE`.

EXECUTION CONTRACT
------------------
The last `doAdvancedStartAction` argument is not decorative metadata. In current
`CvPlayer::doAdvancedStartAction`, the unit case does:

    UnitAITypes eUnitAI = (UnitAITypes)iData2;
    ...
    CvUnit* pUnit = initUnit(eUnit, iX, iY, eUnitAI);

`CvPlayer::initUnit` then uses exactly that supplied role unless it is `NO_UNITAI`:

    pUnit->init(...,
        (eUnitAI == NO_UNITAI ? GC.getInfo(eUnit).getDefaultUnitAIType() : eUnitAI),
        ...);

There is no validation that a supplied `UNITAI_EXPLORE` is appropriate for a naval
unit. Therefore the mismatch survives unit creation exactly as written.

CURRENT-DATA PRODUCER
---------------------
The sea branch is live whenever Advanced Start reaches the Age of Exploration:

    AI_advancedStartPlaceExploreUnits(false);

Current SAS contains multiple units accepted for `UNITAI_EXPLORE_SEA`, including
Galley, Galleon, Trireme, Frigate, Destroyer and Attack Submarine. Their normal/default
naval roles are sea UnitAIs; none requires the land `UNITAI_EXPLORE` role selected by
the buggy purchase call.

A straightforward reproduction is therefore:

1. start an AI with enough Advanced Start points in an era where sea exploration is
   enabled and with a coastal city / useful unrevealed water area;
2. `AI_bestAdvancedStartUnitAI(..., UNITAI_EXPLORE_SEA)` selects a legal naval explorer;
3. the purchase call passes `UNITAI_EXPLORE`;
4. `initUnit` stores that land-explorer role on the newly created naval unit.

LIVE CONSEQUENCE
----------------
`CvUnitAI::AI_update` dispatches directly on the stored UnitAI:

    case UNITAI_EXPLORE:
        AI_exploreMove();
        break;
    ...
    case UNITAI_EXPLORE_SEA:
        AI_exploreSeaMove();
        break;

The wrongly initialized ship therefore executes the land-explorer routine instead of
the dedicated naval routine.

This affects more than naming:

- `AI_exploreSeaMove` has dedicated naval threat/retreat handling;
- it checks `AI_isExcessSeaExplorers`, sea-role obsolescence and the sea-specific
  transformation chain (Worker/Pirate/Assault/Settler/Missionary/Reserve Sea);
- sea explorer demand/counting uses `UNITAI_EXPLORE_SEA`, so the purchased ship is not
  counted as fulfilling the role it was bought to fill;
- the land `AI_exploreMove` instead uses `UNITAI_EXPLORE` counts and land-explorer
  retirement/attack logic even though the physical unit is naval.

The AI can consequently spend Advanced Start points specifically to fill sea-exploration
demand and immediately create a unit that both follows the wrong behavior tree and
remains invisible to that sea-role count, encouraging further incorrect demand.

INTRODUCTION / ANCESTRY
-----------------------
BtS/Civ4CE and K-Mod call:

    doAdvancedStartAction(..., eBestUnitType, true);

with no explicit UnitAI override. The normal `initUnit` default-role mechanism therefore
remains in control there.

AdvCiv commit dde66d276082911b61d2b966fc0b6cb347270f82, practical commit 1472,
title:

    AI explore units in Adv. Start, bugfix unit layer colors

introduced advc.250c's extra `iData2` argument and changed the unit action to initialize
with that explicit UnitAI. In the very same diff, the explorer purchase call was changed
to:

    ..., true, UNITAI_EXPLORE);

without using the already-computed `eUnitAI`. That introduced the sea-branch regression.
Base AdvCiv 1.14 and current SAS retain the hard-coded value.

REPAIR DIRECTION
----------------
The surgical repair is simply to preserve the role already used for selection:

    doAdvancedStartAction(ADVANCEDSTARTACTION_UNIT,
            pBestExplorePlot->getX(), pBestExplorePlot->getY(), eBestUnitType, true,
            eUnitAI);

This keeps the intended advc.250c explicit-role feature for land explorers while making
the sea branch initialize as `UNITAI_EXPLORE_SEA`.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of F341/F342. Those concern normal-game sea-explorer retirement and
explorer->worker transformation bookkeeping after a correctly typed
`UNITAI_EXPLORE_SEA` exists. F366 occurs earlier during Advanced Start creation and
assigns the wrong UnitAI in the first place.

It is also independent of the retracted GET_TEAM(PlayerTypes) hypothesis immediately
preceding this finding; the `CoreAI` overload control remains valid.

CURRENT AUDIT STATE AFTER F366
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F366
F367 / provisional KI#690 — UNUSED
Latest physical checkpoint — C031-WIP56
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remainder of Advanced Start construction/routing/unit placement,
   using the explicit-UnitAI parameter as a newly established contract boundary.
2. Then continue into found-value/city-site and area/city UnitAI helpers; prefer hard
   legality/state/scope mismatches over subjective Advanced Start allocation tuning.


===============================================================================
C031-WIP57 — F367 CONFIRMED
===============================================================================

F367 / provisional KI#690 — advc.036 mutates the shared `iExtraPop` argument while
iterating cities in AI_getHappinessWeight / AI_getHealthWeight, so an espionage effect
in one city changes how all later cities are valued

STATUS
------
CONFIRMED — AdvCiv regression introduced by advc.036 and present in Base AdvCiv 1.14
and current SAS. BtS and K-Mod do not contain the espionage-counter adjustment that
creates the cross-city state leak.

ROOT
----
Both helpers take one empire-level input parameter:

    int iExtraPop

and then iterate every city. advc.036 added a city-specific adjustment for temporary
espionage anger / sickness, but writes the result back into that shared parameter:

    if (iExtraPop > 0)
        iExtraPop = std::max(0, iExtraPop -
                pLoopCity->getEspionageHappinessCounter() / 2);

and analogously for `getEspionageHealthCounter()`.

Once an earlier city reduces `iExtraPop`, every later city inherits the reduced value,
regardless of its own espionage counter. The helper therefore ceases to be a sum of
independent per-city valuations and becomes dependent on city iteration order.

INTRODUCTION / INTENDED CONTRACT
--------------------------------
AdvCiv practical commit 1287, commit ad5faf8aa2740b991c49d8f35e14769d190a4c71,
introduced this advc.036 adjustment. The source comment explains the intended local
reasoning:

    "By the time iExtraPop matters, the espionage effect will be gone."

The same commit adds the adjustment inside the per-city loop for both happiness and
health. Nothing in the change says that an espionage effect in city A should alter the
future-population assumption for city B. The appropriate state is therefore a local
per-city derivative, e.g.:

    int iCityExtraPop = iExtraPop;
    if (iCityExtraPop > 0)
        iCityExtraPop = std::max(0, iCityExtraPop -
                pLoopCity->getEspionageHappinessCounter() / 2);

and then use `iCityExtraPop` for that city only.

CURRENT PRODUCERS
-----------------
Nonzero `iExtraPop` is common in current strategic valuation. Examples include:

- tech health valuation:

      AI_getHealthWeight(kTech.getHealth(), 1)

- tech happiness valuation:

      AI_getHappinessWeight(kTech.getHappiness(), 1)

- early-religion valuation:

      AI_getHappinessWeight(1, 2)

- multiple civic / war-weariness / trade valuation paths also pass 1 or larger values.

A minimal current reproduction therefore needs only two ordinary cities and one
city-specific espionage counter:

1. evaluator calls `AI_getHappinessWeight(..., 1)`;
2. city A is visited first and has `getEspionageHappinessCounter() >= 2`;
3. A changes the shared `iExtraPop` from 1 to 0;
4. city B has no espionage happiness counter at all;
5. B is nevertheless evaluated with `iExtraPop == 0` rather than the caller's 1.

If B is visited before A, B instead sees 1. Identical empire state can therefore produce
a different result solely from city iteration order / city-list ordering.

LIVE CONSEQUENCE
----------------
`iExtraPop` is used in several material terms of each city's score:

- `unhappyLevel(iExtraPop)` / `badHealth(false, iExtraPop)`;
- percent-happiness/health population scaling;
- the advc.036 severe-unhappiness / severe-unhealth correction.

So the leak is not merely an unused temporary. It changes the returned empire-average
happiness/health weight and therefore downstream tech, civic, religion, war-weariness
and other AI valuations.

The direction can also vary with city order: a temporary espionage effect in one city
can suppress the extra-pop assumption for all later unaffected cities, undervaluing or
otherwise distorting happiness/health effects across the empire.

ANCESTRY
--------
BtS/Civ4CE and K-Mod contain the underlying happiness/health helpers but do not perform
this advc.036 espionage-counter rewrite of `iExtraPop`. AdvCiv introduced the mutation
and Base AdvCiv 1.14/current SAS retain it unchanged. This is therefore an inherited
AdvCiv root, not SAS-local.

REPAIR DIRECTION
----------------
Keep the caller's `iExtraPop` immutable throughout the loop. Derive a local value for
each city:

    int iCityExtraPop = iExtraPop;
    if (iCityExtraPop > 0)
        iCityExtraPop = std::max(0, iCityExtraPop -
                pLoopCity->getEspionageHappinessCounter() / 2);

and use `iCityExtraPop` consistently for all formulas concerning that city. Apply the
same pattern to the health helper.

This preserves advc.036's intended temporary-espionage adjustment without leaking one
city's state into the next.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of F355's event building-happiness multiplicity bug. F355 concerns
an event evaluator losing BuildingClass multiplicity; F367 is a general cross-city
mutable-state bug in the reusable happiness/health helper itself.

It is also independent of the preceding `AI_doEnemyUnitData` own-unit question, which
remains unpromoted because the baseline friendly-unit contribution may be intentional
smoothing/prior behavior and lacks a sufficiently explicit exclusion contract.

CURRENT AUDIT STATE AFTER F367
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F367
F368 / provisional KI#691 — UNUSED
Latest physical checkpoint — C031-WIP57
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through `AI_calculateUnitAIViability` / `AI_disbandValue` and nearby
   post-weight utility helpers, using current SAS XML/callers to distinguish dormant
   generic-code hazards from live defects.
2. Then continue through plot airbase/canal, attitude/safety, and remaining helper
   tranche toward the end of Queue 001, preserving F368 until a full current producer
   and gameplay consequence survive.


===============================================================================
C031-WIP58 — F368 CONFIRMED
===============================================================================

F368 / provisional KI#691 — AI_getPlotAirbaseValue increments `iOtherCityCount` only
when a newly visited foreign city/airbase is closer than every previous one, so the
score counts traversal-order record minima rather than the number of nearby targets

STATUS
------
CONFIRMED — inherited BtS root, present in Civ4CE/BtS, K-Mod, Base AdvCiv 1.14 and
current SAS. AdvCiv later changed the traversal/API representation but retained the
same misplaced count increment.

ROOT
----
The helper tracks both nearest-distance state and a count:

    int iMinOtherCityDistance = MAX_INT;
    int iOtherCityCount = 0;

For each revealed foreign/other airbase, current AdvCiv does:

    int iDist = it.currPlotDist();
    if (iDist < iMinOtherCityDistance)
    {
        iMinOtherCityDistance = iDist;
        iOtherCityCount++;
    }

The final score then explicitly scales by that supposed count:

    int iValue = iOtherCityCount * 50;

Therefore `iOtherCityCount` is not actually a count of nearby foreign cities/airbases.
It is the number of times traversal happens to encounter a new record-low distance.

CURRENT ITERATION MAKES THE UNDERCOUNT ESPECIALLY CLEAR
--------------------------------------------------------
Current AdvCiv/SAS uses `SquareIter(kPlot, 4, false)`, whose documented traversal is a
clockwise spiral away from the center. Once a nearby foreign city/base has been seen,
subsequent targets at equal or greater distance cannot increment the count. Even within
the first occupied spiral ring, only a later target that happens to have a smaller
`plotDistance` than the first one can increment it.

Thus two, three or many ordinary nearby foreign cities can all produce
`iOtherCityCount == 1`. The result depends on traversal record minima, not physical
multiplicity.

BtS/Civ4CE and K-Mod use nested coordinate scanning rather than `SquareIter`, but contain
the same root:

    if (iDistance < iMinOtherCityDistance)
    {
        iMinOtherCityDistance = iDistance;
        iMinOtherCityPlot = pLoopPlot;
        iOtherCityCount++;
    }

so their result is likewise scan-order-dependent rather than a true count.

LIVE CONSEQUENCES
-----------------
Current C++ has three gameplay consumers:

1. `CvUnitAI::AI_getPlotDefendersNeeded`

       iNeeded += AI_getPlotAirbaseValue(kPlot) / 50;

   Each additional 50 points is treated as another defender requirement. Multiple
   nearby cities/bases that collapse to one record minimum therefore fail to increase
   Fort/airbase guard demand as intended by the score scale.

2. `CvUnitAI::AI_fortTerritory`

       iValue += (bAirbase ? kOwner.AI_getPlotAirbaseValue(kPlot) : 0);

   This directly ranks worker Fort/airbase sites. SAS currently defaults
   `SAS_AI_WORKER_FORT_AIRBASE_ENABLE` to 0, so this particular producer is disabled by
   default, but the helper remains a supported optional path and is not the only
   consumer.

3. `CvUnitAI::AI_airOffenseBaseValue`

   For an existing non-city airbase in peacetime:

       iValue = GET_PLAYER(getOwner()).AI_getPlotAirbaseValue(kPlot);
       iValue /= 6;

   Existing/captured/preplaced Forts therefore receive too little offensive-air base
   value when several foreign cities/bases are nearby.

Current Forts remain normal game objects (`IMPROVEMENT_FORT`, `bActsAsCity=1`), can be
built by humans, captured, pre-existing, or used when the SAS worker-Fort option is
restored. The default-disabled worker-construction path therefore reduces frequency but
does not make F368 dead.

CONCRETE SHAPE
--------------
Take an owned Fort with no adjacent friendly city and two revealed foreign cities within
radius 4. Suppose the outward traversal encounters a distance-2 city first and another
foreign city at distance 2 or 3 later.

Intended physical count:

    iOtherCityCount = 2
    base airbase value = 100

Current code:

    first city: 2 < MAX_INT  -> count = 1, min = 2
    second:     2/3 < 2      -> false
    final count = 1
    base airbase value = 50

The second city can consequently add no guard demand and no additional airbase-site/base
value at all.

CONTRACT / INTENT EVIDENCE
--------------------------
The naming and downstream arithmetic are unusually explicit:

- variable: `iOtherCityCount`;
- zero gate: `if (iOtherCityCount == 0) return 0;`;
- magnitude: `iOtherCityCount * 50`.

Nearest-distance state is tracked separately in `iMinOtherCityDistance` (and BtS also
tracked `iMinOtherCityPlot`). There is therefore no coherent reason for the count itself
to be updated only when the nearest-distance record changes.

REPAIR DIRECTION
----------------
Separate the two operations:

    iOtherCityCount++;
    if (iDist < iMinOtherCityDistance)
        iMinOtherCityDistance = iDist;

If the nearest-distance variables are genuinely no longer consumed (as in current
AdvCiv, where their old downstream code remains commented), the cleaner current repair
may simply count every qualifying foreign/other revealed airbase and remove the dead
minimum-distance state entirely.

The exact qualifying-object semantics (`isRevealedAirBase`, including Fort-like bases)
should be preserved; F368 concerns only the misplaced multiplicity increment.

DUPLICATE / ROOT SEPARATION
---------------------------
This is independent of SAS's later policy of disabling routine AI Fort construction by
default. That define controls one producer (`AI_fortTerritory`), whereas F368 is an
inherited valuation/counting defect with additional existing-Fort consumers.

It is also independent of F367's cross-city happiness/health state mutation.

CURRENT AUDIT STATE AFTER F368
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F368
F369 / provisional KI#692 — UNUSED
Latest physical checkpoint — C031-WIP58
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Continue through the remaining tail helpers (`AI_isPlotThreatened`, first-tech /
   resource-train / piracy / partner-war / submarine / safety/contact helpers), using
   current callers and data rather than local oddity alone.
2. Then reconcile whether Queue 001 has any unvisited deep-method tranche left before
   considering the file complete and returning to the protected queue order.


===============================================================================
C031-WIP59 — F369 CONFIRMED
===============================================================================

F369 / provisional KI#692 — advc.079 stores one randomized "best attack unit" as the
last unit bragged about, but the diplomacy text independently reruns the randomized
selection when expanding [OUR_BEST_UNIT], so the remembered unit and the unit actually
shown to the player can differ

STATUS
------
CONFIRMED — AdvCiv regression introduced by practical commit 1439
`65a1472094e89d913ccf565c76687027a530a30d` ("Changes to AI greetings"). The underlying
`getBestAttackUnitKey` / asynchronous unit-selection randomness is inherited from BtS /
K-Mod; advc.079 newly made the selected UnitType persistent state without carrying that
same selection into text rendering.

ROOT
----
The advc.079 greeting code now asks for a candidate and stores it:

    UnitTypes eBragUnit = AI_getBestAttackUnit();
    ...
    if (eBragUnit != m_aeLastBrag[ePlayer])
    {
        m_aeLastBrag[ePlayer] = eBragUnit;
        return GC.getAIDiploCommentType("UNIT_BRAG");
    }

But none of the returned diplomacy state carries `eBragUnit`. The selected XML text
contains `[OUR_BEST_UNIT]`, e.g.:

    Our numberless [OUR_BEST_UNIT] shall crush ...

and `CvDllTranslator::replaceOur` resolves that token later by calling:

    kPlayer.getBestAttackUnitName(iForm)
      -> getBestAttackUnitKey()
      -> AI().AI_getBestAttackUnit()

This is a fresh, independent second evaluation.

The evaluation is intentionally asynchronous/randomized. `AI_getBestAttackUnit` calls
`CvCityAI::AI_bestUnitAI(UNITAI_ATTACK, true)`, and `AI_bestUnitAI` multiplies every
surviving candidate by an independent asynchronous random factor:

    if (bAsync)
    {
        iValue *= (GC.getASyncRand().get(50, "AI Best Unit ASYNC") + 100);
        iValue /= 100;
    }

Thus there is no invariant that the second text-rendering call returns the UnitType
stored by the first greeting-selection call.

CURRENT-DATA REACHABILITY
-------------------------
Current SAS has many simultaneously trainable `UNITAI_ATTACK` alternatives. A simple
early example is Axeman (strength 5) and Swordsman (strength 6) when the civ has both
required resources/technologies. Before the async multiplier, candidates below 2/3 of
the best are discarded, so these close alternatives both survive. A 100..149% random
multiplier is then easily large enough to reverse their ordering between two calls.

The same structural possibility occurs throughout the unit tree; this does not depend
on a scenario-only unit.

LIVE CONSEQUENCES
-----------------
1. The player can literally be shown a different unit from the one that advc.079 records
   as having just been bragged about.

   Example shape:

       greeting-selection call -> Axeman
       m_aeLastBrag[human] = Axeman
       text-rendering call      -> Swordsman
       visible line says        -> Swordsman

2. On the next contact, another randomized selection can return Swordsman. The new
   advc.079 test then sees:

       Swordsman != m_aeLastBrag[human] (Axeman)

   and treats this as a "new best unit", immediately producing another brag despite no
   technology, resource or trainability change at all.

3. Conversely, a real change in the best-unit set can be hidden or delayed if the
   randomized identity happens to equal the stored previous candidate.

This directly defeats the introducing commit's stated purpose: "When a braggart AI has
a new best unit, it brags about that unit at the first opportunity" and the memory is
supposed to reduce repeated greetings rather than create them from renderer randomness.

ANCESTRY / ROOT SEPARATION
--------------------------
BtS/Civ4CE and K-Mod already use the capital-first `AI_bestUnitAI(UNITAI_ATTACK, true)`
logic for the `[OUR_BEST_UNIT]` text. That can make the phrase "best" somewhat noisy,
but there is no persistent `last brag` identity to contradict.

advc.079 refactored that old computation into `AI_getBestAttackUnit`, added
`m_aeLastBrag`, and calls the helper while deciding whether a unit is new. The existing
text token remained dynamic and calls the same helper again. F369 is therefore the new
state/display mismatch created when a randomized display helper was reused as stable
memory identity.

This finding does NOT currently promote the separate inherited question that
`AI_getBestAttackUnit` returns the first capital-trainable attacker rather than searching
for a global maximum. That may make the textual notion of "best" inaccurate in some
city-local prerequisite states, but it is a separate contract and remains unnumbered.

REPAIR DIRECTION
----------------
Do not rerun a randomized selector when expanding the brag that has just been chosen.
Preserve the exact `eBragUnit` for the active greeting and feed that UnitType to the
rendered line, or otherwise make the identity used by `m_aeLastBrag` and
`[OUR_BEST_UNIT]` come from one evaluation.

A second possible cleanup would make the selector deterministic when used as persistent
identity, but that is broader: BtS deliberately used asynchronous selection for this
UI-facing helper. The minimal advc.079 repair is simply to keep selection and display
atomic.

CONTROLS CLOSED BEFORE F369
---------------------------
- Sibling-vassal Privateer targeting is deliberate. AdvCiv's manual explicitly says
  that the vassal/master Privateer restrictions do not apply to vassals of the same
  master.
- `AI_isUnitNeedingOpenBorders` may inspect the group's head unit for domain, but
  `CvUnit::canJoinGroup` enforces a single domain for all units in a selection group;
  there is no mixed-domain producer.

CURRENT AUDIT STATE AFTER F369
------------------------------
Queue 001 `CvPlayerAI.cpp` — OPEN
Unique files complete — 30/169
Confirmed deep-reloop roots — F321 through F369
F370 / provisional KI#693 — UNUSED
Latest physical checkpoint — C031-WIP59
WIP36 F348 — RETRACTED / superseded by WIP37
Current F348 — mission-valuation affordability/context mismatch from WIP38
No repository source files modified.

NEXT CURSOR
-----------
1. Finish the remaining tail (`AI_atWarWithPartner`, `AI_disapprovesOfDoW`, submarine
   danger, safety/contact, first-tech/resource-train and related public/UI helper
   contracts), keeping the separate capital-first "best attacker" question unnumbered
   unless its global-best contract has a concrete consequential consumer.
2. Reconcile the full Queue 001 coverage against the album / function ranges. If no
   unsaturated deep tranche remains, close the CvPlayerAI.cpp reloop and resume the
   protected queue order from the next file.

===============================================================================
C031-WIP60 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — F370 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvPlayerAI.cpp` remains OPEN during final closure reconciliation.
F370 / provisional KI#693 is CONFIRMED.
F371 / provisional KI#694 is UNUSED.
Unique files complete remains 30/169 until Queue 001 itself is closed.
No repository source files were modified.

-------------------------------------------------------------------------------
F370 / provisional KI#693 — Advanced Start city setup counts a failed
improvement purchase as successful, then can buy population against phantom
improved plots
-------------------------------------------------------------------------------

ROOT
----
`CvPlayerAI::AI_advancedStartPlaceCity` chooses the plot with the highest
`AI_getBestBuildValue`, calls the void Advanced Start action, and then increments
`iPlotsImproved` unconditionally:

    doAdvancedStartAction(ADVANCEDSTARTACTION_IMPROVEMENT,
            pBestPlot->getX(), pBestPlot->getY(), eBestImprovement, true);
    iPlotsImproved++;

There is no Advanced Start affordability check in the candidate loop and no
post-action verification that `pBestPlot` actually received `eBestImprovement`.

`CvPlayer::doAdvancedStartAction`, however, explicitly permits the action to fail
silently. For `ADVANCEDSTARTACTION_IMPROVEMENT` it computes
`getAdvancedStartImprovementCost`; if the action is legal but the player lacks
that many points, the function simply leaves the plot unchanged and returns to
the caller. Its return type is `void`, so success is not communicated.

The local counter therefore changes even when the game state does not.

SUPPORTED PRODUCER
------------------
This is reachable through ordinary AI Advanced Start. `AI_doAdvancedStart`
checks whether it can afford the city itself before calling
`AI_advancedStartPlaceCity`, but it does not reserve enough points for that
helper's preferred culture/improvements/population package. Later cities in the
city-budget loop can therefore enter the helper with only a small remainder.

Current SAS data makes an affordability split easy to construct. For example:

* `IMPROVEMENT_FARM` has `iAdvancedStartCost = 30`;
* SAS `ADVANCED_START_WORKER_BUILD_MODIFIER = 67`, so the base Farm portion is
  about 20 points at Normal build speed;
* removing `FEATURE_JUNGLE` adds its current Advanced Start removal cost of 40,
  making that Farm roughly 60 points before other speed adjustment;
* population is bought through the independent Advanced Start population-cost
  formula (BtS default `ADVANCED_START_POPULATION_COST = 150`). On ordinary
  Normal-speed/100%-growth settings, population 1 has a growth threshold of
  `20 + 2 = 22`, hence the next population point costs about 33.

A late Advanced Start city with, for example, a remainder between those costs
can therefore afford population but not the AI's highest-valued Jungle Farm.
The exact numeric remainder is not an exotic API state: `AI_doAdvancedStart`
spends the common point pool incrementally on cities, techs, visibility and the
city helper's own actions.

CONSEQUENCE
-----------
Suppose the city has no improved plots and the Jungle Farm is its current
highest `AI_getBestBuildValue` target, but the remaining points are below that
Farm's Advanced Start cost.

1. The improvement action is attempted and silently fails.
2. `iPlotsImproved` nevertheless rises from 0 to 1.
3. On the next pass the same still-unimproved high-value plot remains the best
   target. The action can fail again and `iPlotsImproved` rises again.
4. Once the phantom count exceeds city population, the helper's final loop uses
   the real city plot and can spend any still-affordable points on population:

       while (iPlotsImproved > pCity->getPopulation())
       {
           int iPopCost = getAdvancedStartPopCost(true, pCity);
           ...
           doAdvancedStartAction(ADVANCEDSTARTACTION_POP,
                   pPlot->getX(), pPlot->getY(), -1, true);
       }

The AI can therefore grow a city because of improvements that do not exist.
Because candidate selection is based on build value rather than purchasability,
it can also keep retrying the same unaffordable high-value improvement while a
cheaper lower-valued improvement could actually have been bought with the
remaining points.

This is a genuine execution-state mismatch, not merely a subjective Advanced
Start allocation heuristic: the loop variable is explicitly named/counting
`iPlotsImproved`, and the runtime plot remains unimproved.

ANCESTRY
--------
The same unconditional post-action `iPlotsImproved++` structure is present in
BtS/Civ4CE, K-Mod, Base AdvCiv 1.14 and current SAS. AdvCiv modernized iterators
and Advanced Start costs but preserved this state assumption. Classification:
inherited BtS Advanced Start execution-vs-assumed-success defect.

No existing current KI/F321-F369 root found for this behavior. Earlier Advanced
Start album items concern tech-refund reachability/cache effects, first-city
callbacks, or F366's forced land `UNITAI_EXPLORE` assignment; none owns failed
improvement purchases being counted as successful.

REPAIR DIRECTION
----------------
The stronger repair is to make the candidate/action state coherent rather than
merely patch the later population loop:

* before selecting/attempting an improvement, require
  `getAdvancedStartImprovementCost(eImprovement, true, pLoopPlot)` to be
  nonnegative and no greater than the current Advanced Start points; and/or
* after the action, increment `iPlotsImproved` only if the plot now actually has
  `eBestImprovement`.

Skipping unaffordable candidates is preferable because it lets the AI consider
a cheaper second-best improvement instead of repeatedly retrying an action that
cannot execute. A post-action verification remains useful defensive hardening
because `doAdvancedStartAction` is void and can reject actions for other
legality/state reasons.

CONTROL NOTE
------------
The immediate population action inside the improvement loop uses the improved
plot coordinates rather than the city plot, so it is normally a no-op when that
plot is not itself a city. The later `while (iPlotsImproved > population)` loop
uses the city plot correctly. That oddity does not rescue F370; it makes the
phantom count survive until the later real population-purchase loop.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN (final closure reconciliation)
Unique files complete — 30/169
Confirmed deep-reloop findings — F321 through F370
F371 / provisional KI#694 — UNUSED
Latest physical checkpoint — C031-WIP60
No repository source files modified.

NEXT
----
1. Continue the final Queue 001 function-range / bug-pattern reconciliation with
   F371 unused.
2. Recheck the remaining unmentioned large helpers and SAS-vs-Base deltas, now
   including Advanced Start purchase-success assumptions as a control pattern.
3. If no independent root survives, freeze a closure checkpoint for
   `CvPlayerAI.cpp`, increment the unique-file completion count, and move to the
   protected Queue 002 `CvUnitAI.cpp` deep reloop.
===============================================================================
C031-WIP61 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — F371 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvPlayerAI.cpp` remains OPEN during the final closure reconciliation.
F371 / provisional KI#694 is CONFIRMED.
F372 / provisional KI#695 is UNUSED.
Unique files complete remains 30/169 until Queue 001 itself is closed.
No repository source files were modified.

-------------------------------------------------------------------------------
F371 / provisional KI#694 — Advanced Start building phase treats one
unaffordable selected building as a global "done" state, so it can terminate
before later passes expose cheaper affordable buildings
-------------------------------------------------------------------------------

ROOT
----
`CvPlayerAI::AI_doAdvancedStart` uses `bDoneBuildings` both as the outer-loop
termination condition and as a per-city affordability result:

    bool bDoneBuildings =
            (iLastPointsTotal - getAdvancedStartPoints() > iCityPoints);

    for (int iPass = 0; iPass < 10 && !bDoneBuildings; ++iPass)
    {
        FOR_EACH_CITYAI(pLoopCity, *this)
        {
            BuildingTypes const eBuilding =
                    pLoopCity->AI_bestAdvancedStartBuilding(iPass);
            if (eBuilding != NO_BUILDING)
            {
                bDoneBuildings = (iCityPoints < iLastPointsTotal -
                        (getAdvancedStartPoints() -
                        getAdvancedStartBuildingCost(
                                eBuilding, true, pLoopCity)));

                if (!bDoneBuildings)
                {
                    doAdvancedStartAction(
                            ADVANCEDSTARTACTION_BUILDING,
                            pLoopCity->getX(), pLoopCity->getY(),
                            eBuilding, true);
                }
                else
                {
                    //continue. there might be cheaper buildings
                    //in other cities we can afford
                }
            }
        }
    }

The condition itself is a valid projected-budget test for that particular
building. The defect is storing the result in the global phase-termination
flag.

`CvCityAI::AI_bestAdvancedStartBuilding(iPass)` deliberately changes what it is
willing to select across passes:

    pass 0: FOOD
    pass 1: + PRODUCTION
    pass 2: + EXPERIENCE
    pass 3: + HAPPY | HEALTHY
    pass 4+: + GOLD | RESEARCH | MAINTENANCE | (normally) ESPIONAGE

The threshold also falls from 20 to 0 across passes.

Therefore, "the selected building for this city on this pass exceeds the
remaining city budget" does not imply "no later-pass building is affordable."
Yet if that unaffordable candidate leaves `bDoneBuildings == true` at the end
of the city traversal, the outer loop terminates and those later selector
passes are never run.

The source comment already acknowledges the missing implication -- "there
might be cheaper buildings ... we can afford" -- but only continues the
current city traversal. It does not protect the next `iPass`.

SUPPORTED CURRENT-DATA SHAPE
----------------------------
Current SAS has an ordinary generic cost/focus split that demonstrates the
problem without relying on a mod-mod edge case:

* `BUILDING_COLOSSEUM`
  - production cost 90
  - Advanced Start cost percent 100
  - +1 happiness
  - prerequisite `TECH_CONSTRUCTION`

* `BUILDING_LIBRARY`
  - production cost 80
  - Advanced Start cost percent 100
  - +25% research
  - prerequisite `TECH_WRITING`
  - not a free start-era building until the Renaissance

`CvPlayer::getAdvancedStartBuildingCost` starts from production cost times the
building's Advanced Start percent and then applies AdvCiv's 1.5 multiplier.
On Normal speed, absent a city-specific production modifier, these are about:

    Colosseum: 90 * 1.5 = 135 points
    Library:   80 * 1.5 = 120 points

A pre-Renaissance Advanced Start city that knows both prerequisite techs and
currently values happiness can therefore reach the building phase with a
remaining city-budget allowance between 120 and 134 points.

On pass 3, the happiness/health focus can select the Colosseum. Its projected
135-point purchase exceeds the remaining city budget, so the code assigns
`bDoneBuildings = true` and does not buy it.

Pass 4 is exactly where RESEARCH is added to the selector. A 120-point Library
could be legal and affordable there, but pass 4 never executes because the
outer loop sees the `true` value left by the unaffordable pass-3 candidate.

The exact remainder does not require a special API state. `AI_doAdvancedStart`
uses one shared point pool and reaches the building phase only after incremental
spending on cities, improvements/population, visibility, technology, explorers
and routes. The city-budget remainder is not constrained to building-cost
multiples.

MULTI-CITY ORDER DEPENDENCE
---------------------------
The same variable is overwritten for every city that returns a building
candidate:

    bDoneBuildings = projectedPurchaseExceedsBudget;

Consequently, in a multi-city empire the decision to run the next building
pass can depend on which candidate-bearing city happens to be visited last.

For example, on the same pass:

* City A's selected building is affordable -> `bDoneBuildings = false`;
* City B's selected building is unaffordable -> `bDoneBuildings = true`.

If B is the last relevant city, the whole building phase stops after the pass.
If a later city instead has an affordable candidate, that later assignment can
reset the flag to false and the next pass runs.

Thus the flag is neither a monotonic "budget exhausted" state nor an
all-cities affordability result. It is effectively the affordability result of
the last candidate that happened to overwrite it.

CONSEQUENCE
-----------
The AI can leave Advanced Start points unspent on useful buildings even though
a later focus pass would identify an affordable legal building.

This is not merely a disagreement with building valuation. The ten-pass design
explicitly broadens the candidate set over time; F371 can prevent those
designed passes from executing at all based on the cost of one earlier selected
candidate.

The effect is strongest near the end of the city/economic budget, where an
expensive happiness/health building can block a cheaper research, maintenance
or espionage building that only becomes eligible on the next pass.

ANCESTRY
--------
The same `bDoneBuildings` structure and the same explanatory comment are present
in BtS/Civ4CE, K-Mod, Base AdvCiv 1.14 and current SAS. AdvCiv reformatted the
condition but preserved the control flow.

Classification: inherited BtS Advanced Start budget/control-flow defect.

This is independent of F370. F370 is an execution-state mismatch inside
`AI_advancedStartPlaceCity` where a failed improvement purchase increments
`iPlotsImproved`. F371 is a separate building-phase termination error even when
every attempted building action succeeds exactly as requested.

REPAIR DIRECTION
----------------
Do not use one candidate's affordability as the global outer-loop done flag.

A conservative repair is:

1. Keep the initial "city budget already exhausted" test if desired.
2. For each selected building, compute the projected city-budget spending.
3. If that particular building would exceed the budget, `continue` to the next
   city/candidate instead of setting a persistent phase-done state.
4. Allow the bounded later passes to run so that their broader focus flags can
   expose cheaper alternatives.

For example, conceptually:

    if (projectedCitySpending > iCityPoints)
        continue;

    doAdvancedStartAction(...);

Because the loop already has a hard ten-pass bound, no global "one selected
building was too expensive" flag is needed to prevent an infinite loop.

A stronger cleanup could track whether any building was purchased in a pass,
but "nothing was bought on pass N" still should not automatically terminate
before pass N+1 while the selector is intentionally gaining new focus flags.
Any early-exit optimization therefore needs a real proof that no later pass can
introduce a new affordable candidate.

CONTROLS CLOSED DURING THIS SLICE
---------------------------------
* The other void Advanced Start actions do not all share F370's false-state
  consequence. In particular, the final tech loop and the building action's
  projected-budget inequality imply affordability before execution; the
  earlier/interleaved tech attempts can fail when the common point pool is
  depleted, but the local variables changed afterward are allocation
  heuristics rather than a false representation of purchased game state. No
  independent F372 was assigned there in this slice.

* `AI_bestAreaUnitAIValue` does select only one representative city for a
  non-null area and can therefore miss a unit with a city-local prerequisite
  that is trainable elsewhere in the same area. Current SAS has Factory-gated
  Tanks that make the general hazard real, but the area-scoped sea/air cleanup
  consumers do not currently intersect such a local prerequisite. The only
  current-data intersection established here is K-Mod's gifted combat-unit
  valuation. That remains an inherited approximation / mod-mod hazard, not a
  numbered root at this checkpoint.

* `AI_getRankDifferenceAttitude`'s `iMajorCivsAlive - 1` denominator is protected
  on its live cached-attitude path: `AI_updateAttitude` asserts both participants
  are major civs and requires both to be alive before the rank helper is
  reached. No zero-divisor producer was established.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN (final closure reconciliation)
Unique files complete — 30/169
Confirmed deep-reloop findings — F321 through F371
F372 / provisional KI#695 — UNUSED
Latest physical checkpoint — C031-WIP61
No repository source files modified.

NEXT
----
1. Continue the final Queue 001 bug-pattern / function-range reconciliation from
   F372 unused; do not close the file merely because F371 was found late.
2. Finish the remaining unmentioned state/cache/value helpers and recheck the
   rest of Advanced Start for independent "candidate result controls global
   phase" or "attempted action assumed successful" patterns.
3. If no independent F372 survives, freeze the Queue 001 closure checkpoint,
   increment unique files complete to 31/169, and open protected Queue 002
   `CvUnitAI.cpp` for its deeper reloop.


===============================================================================
C031-WIP62 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — F372 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvPlayerAI.cpp` remains OPEN during the final closure reconciliation.
F372 / provisional KI#695 is CONFIRMED.
F373 / provisional KI#696 is UNUSED.
Unique files complete remains 30/169 until Queue 001 itself is closed.
No repository source files were modified.

-------------------------------------------------------------------------------
F372 / provisional KI#695 — AdvCiv made BtS's unused Advanced Start `bDone`
flag terminate the entire unit-buying phase when one selected unit is
unaffordable, even though the next role/pass can contain a cheaper affordable
unit
-------------------------------------------------------------------------------

ROOT
----
The Advanced Start unit phase cycles through four roles:

    CITY_DEFENSE
    WORKER
    RESERVE
    COUNTER

and, for each pass/city, asks `AI_bestAdvancedStartUnitAI` for the preferred
unit of that role.

BtS/Civ4CE and K-Mod contain this affordability branch:

    bool bDone = false;
    for (int iPass = 0; iPass < 10; ++iPass)
    {
        ...
        if (getAdvancedStartUnitCost(eBestUnit, true, pUnitPlot) >
            getAdvancedStartPoints())
        {
            bDone = true;
            break;
        }
        ...
    }

`bDone` is written but never read. Therefore an unaffordable selected unit
breaks the current city traversal, but the outer loop continues to the next
`iPass` and thus to the next UnitAI role.

AdvCiv changed the outer loop to:

    for (int iPass = 0; iPass < 10 &&
        !bDone; // advc.001: bDone was never read. This is probably its intended use.
        iPass++)

Current SAS inherits that change unchanged.

This converts a dead/unused flag into a global phase-termination condition.
The implicit assertion is now:

    selected unit for current role is unaffordable
        =>
    no useful unit in any later role/pass is affordable

That implication is false.

CURRENT-SAS COUNTEREXAMPLE
--------------------------
The current unit data gives a simple one-city, generic-civilization example.

`UNIT_WORKER`:
* production cost 60
* Advanced Start cost percent 100
* no tech prerequisite
* no obsolete tech
* supports `UNITAI_WORKER`

The generic city-defense chain includes:

`UNIT_ARCHER`
* production cost 25
* obsolete at Construction

`UNIT_LONGBOWMAN`
* production cost 40
* obsolete at Firearms

`UNIT_MUSKETMAN`
* production cost 100
* prerequisite Firearms
* obsolete at Military Tradition
* explicitly supports `UNITAI_CITY_DEFENSE`

`UNIT_RIFLEMAN`
* production cost 120
* prerequisite Military Tradition (+ Firearms)
* obsolete at Fascism
* explicitly supports `UNITAI_CITY_DEFENSE`

`getAdvancedStartUnitCost` begins from `getProductionNeeded(eUnit)` multiplied
by 3 * AdvancedStartCost / 200, i.e. a 1.5 multiplier for the ordinary
100-percent units, then applies the same game-speed point normalization (plus
any applicable production modifier).

Consequently, in an otherwise ordinary Firearms / pre-Military-Tradition
state without a special unit-production modifier:

    Musketman nominal AS cost ~= 150 points
    Worker nominal AS cost     ~=  90 points

The exact absolute values scale with game/start speed, but the generic
Musketman remains substantially more expensive than the Worker.

The point pool reaches the unit phase only after many incremental Advanced
Start purchases (cities, improvements/population, visibility, technology,
explorers, routes, buildings), so a remainder in the interval where the Worker
is affordable but the Musketman is not is entirely ordinary.

At Firearms, the earlier Archer/Longbow city defenders are obsolete. Therefore
for a generic civilization `AI_bestAdvancedStartUnitAI(...,
UNITAI_CITY_DEFENSE)` can select the Musketman on pass 0.

With, for example, enough points for a Worker but not for that Musketman:

1. pass 0 selects the Musketman;
2. the cost test sees `Musketman cost > remaining points`;
3. `bDone = true`;
4. the city loop breaks;
5. AdvCiv's added `!bDone` prevents pass 1 from running;
6. `UNITAI_WORKER` is never considered despite an affordable Worker.

The same class of failure exists in later eras (e.g. Rifleman vs Worker), and
city-local production modifiers can add further city-order cases because
`getAdvancedStartUnitCost(..., &kCityPlot)` explicitly applies the city's unit
production modifier.

CONSEQUENCE
-----------
AdvCiv/SAS can prematurely stop the Advanced Start military/worker purchase
phase and leave points that could still buy a useful unit.

This is stronger than merely choosing the wrong military unit: the code's
four-role round-robin is explicitly designed to move from CITY_DEFENSE to
WORKER/RESERVE/COUNTER, but the AdvCiv outer-loop change can prevent those roles
from being reached at all.

ANCESTRY
--------
* BtS/Civ4CE: `bDone` is assigned but never used by the outer loop.
* K-Mod 1.46: same as BtS.
* Base AdvCiv 1.14: adds `&& !bDone` and the comment
  `"bDone was never read. This is probably its intended use."`
* Current AdvCiv-SAS: inherits the AdvCiv behavior.

Classification: AdvCiv regression / speculative cleanup of dead BtS state that
made behavior strictly less robust.

This is related in pattern to F371 but is an independent code root and ancestry:
F371 is the inherited building-pass flag being overwritten by one building's
affordability. F372 is AdvCiv newly making BtS's formerly-unused unit flag
terminate all later role passes.

REPAIR DIRECTION
----------------
The safest minimal repair is to remove `!bDone` from the outer loop, restoring
the BtS/K-Mod behavior, and preferably remove/rename the dead flag as part of
the cleanup:

    for (int iPass = 0; iPass < 10; iPass++)
    {
        ...
        if (selectedCost > getAdvancedStartPoints())
            break; // only stop this city's/current pass traversal
    }

An even better policy would skip only the unaffordable candidate/city and keep
examining other cities on the same pass, because city-local production
modifiers can make another city's candidate cheaper.

If global early termination is desired for performance, it needs a real
"nothing remaining can be afforded" proof, e.g. the minimum legal cost across
the remaining roles/cities, not the cost of one selected candidate.

-------------------------------------------------------------------------------
CLOSURE-RECONCILIATION CONTROLS CHECKED IN THIS SLICE
-------------------------------------------------------------------------------

The fresh current-SAS vs Base-AdvCiv function-body reconciliation was repeated.
The remaining changed functions that had not been explicitly named in the
physical C031 WIP chain were reopened rather than silently treated as covered.

Clean/simple deltas established so far:

* `AI_unitAIDomainType`
  - SAS difference is the intentional Great-Person UnitAI enum rename only.

* `AI_foundValue`
  - SAS difference is `short` -> `int`, consistent with the known found-value
    integer-width hardening.

* `AI_techUnitValue`
  - SAS deltas are cached `CvGame` access plus the Great-Person UnitAI enum
    rename; no new behavior root established in this slice.

* `AI_techProjectValue`
  - cached `CvGame` access / signature formatting only.

* `AI_techReligionValue`
  - cached `CvGame` access / signature formatting only.

* `AI_chooseFreeTech`
  - the SAS delta is the explicit `TECH_ACQUISITION_FREE_CHOICE` metadata
    argument. The earlier free-tech legality concern remains downstream of the
    already-confirmed F330 Barbarian anti-innovation root rather than a new
    F373.

* `AI_getGreeting`, `AI_isWillingToTalk`, `AI_demandRebukedSneak`,
  `AI_getAttitudeVal`
  - the SAS-local differences are cached `GET_PLAYER` references. The separate
    randomized brag state/display defect is already F369.

* `AI_recalculateFoundValues`
  - SAS difference is `short` -> `int`, matching `AI_foundValue` hardening.

* `logFoundValue`
  - disabled-log early return plus cached define lookup; no gameplay mutation.

`AI_localAttackStrength` and the large SAS-local
`AI_calculateCultureVictoryStage` changes remain deliberately open for the next
closure slice rather than being waved through by this checkpoint.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN (final closure reconciliation)
Unique files complete — 30/169
Confirmed deep-reloop findings — F321 through F372
F373 / provisional KI#696 — UNUSED
Latest physical checkpoint — C031-WIP62
No repository source files modified.

NEXT
----
1. Audit the current SAS-local `AI_localAttackStrength` collateral-role repair
   against all callers and `estimateCollateralWeight` semantics.
2. Deliberately re-review the large current-SAS
   `AI_calculateCultureVictoryStage` additions for logging-dependent behavior,
   sentinel/rank handling, countdown arithmetic and cross-victory recursion.
3. Finish the remaining function-range/pattern reconciliation.
4. If no independent F373 survives, close Queue 001 at the next durable WIP,
   increment unique files complete to 31/169, and open protected Queue 002
   `CvUnitAI.cpp`.
===============================================================================
C031-WIP63 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — F373 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvPlayerAI.cpp` remains OPEN during the final closure reconciliation.
F373 / provisional KI#696 is CONFIRMED.
F374 / provisional KI#697 is UNUSED.
Unique files complete remains 30/169 until Queue 001 itself is closed.
No repository source files were modified.

-------------------------------------------------------------------------------
F373 / provisional KI#696 — SAS Culture competing-victory gate can never see
the CURRENT Diplomacy victory stage during the normal victory-hash update
-------------------------------------------------------------------------------

ROOT
----
Current SAS added a Culture-stage behavior gate intended to postpone expensive
Culture 2 investment when another victory route is already advanced:

    static int const iMinCompetingVictoryStage =
            range(GC.getDefineINT(
                "SAS_AI_CULTURE_VICTORY_COMPETING_VICTORY_MIN_STAGE"),
                0, 4);

    if (!isHuman() && iMinCompetingVictoryStage > 0 &&
        iHighCultureCount < iVictoryCities)
    {
        AIVictoryStage const eVictoryStageHash = AI_getVictoryStageHash();
        int const iSpaceStage = ...
        int const iConquestStage = ...
        int const iDominationStage = ...
        int const iDiplomacyStage =
                getSASDiplomacyVictoryStageLevel(eVictoryStageHash);

        int const iMostAdvancedCompetingStage =
                max(space, conquest, domination, diplomacy);

        if (iMostAdvancedCompetingStage >= iMinCompetingVictoryStage)
            return 1;
    }

The XML contract explicitly describes the default stage-3 gate as preventing an
unready Culture 2 hedge from diverting resources from an advanced:

    "Space, Conquest, Domination, or Diplomatic pursuit"

However, normal cached victory-stage construction is ordered as follows in
`CvPlayerAI::AI_updateVictoryStageHash`:

    m_eVictoryStageHash = AI_DEFAULT_VICTORY_STAGE;

    AI_calculateSpaceVictoryStage();
        -> add SPACE bits

    AI_calculateConquestVictoryStage();
        -> add CONQUEST bits

    AI_calculateDominationVictoryStage();
        -> add DOMINATION bits

    AI_calculateCultureVictoryStage();
        -> Culture reads AI_getVictoryStageHash()

    AI_calculateDiplomacyVictoryStage();
        -> only now add DIPLOMACY bits

The source even retains K-Mod's order comment immediately before Culture:

    "AI_calculateCultureVictoryStage now checks
     some of the other victory strategies,
     so it is important that they are set first."

SAS expanded the set of strategies Culture checks to include Diplomacy without
moving Diplomacy before Culture.

WHY NO CACHE/LATE-EVALUATION ESCAPE EXISTS
------------------------------------------
`CvPlayerAI.h` defines:

    AIVictoryStage AI_getVictoryStageHash() const
    {
        return m_eVictoryStageHash;
    }

It is not a lazy getter and performs no recomputation.

`AI_updateVictoryStageHash` resets `m_eVictoryStageHash` to
`AI_DEFAULT_VICTORY_STAGE` before rebuilding the individual strategy bits.
Therefore the Culture call during this normal update cannot even see a previous
turn's Diplomacy stage. The current Diplomacy bits simply do not exist yet.

The call path is live and routine:

    CvPlayerAI::AI_updateCacheData()
        -> AI_updateVictoryStageHash()
            -> reset hash
            -> Space
            -> Conquest
            -> Domination
            -> Culture
            -> Diplomacy

Thus, in the ordinary cached-strategy update, `iDiplomacyStage` inside the new
Culture competing-victory gate is necessarily 0 regardless of what
`AI_calculateDiplomacyVictoryStage()` will return a few statements later.

CONCRETE CONSEQUENCE
--------------------
With the default XML value:

    SAS_AI_CULTURE_VICTORY_COMPETING_VICTORY_MIN_STAGE = 3

an AI that is genuinely at Diplomacy stage 3 or 4, while Space/Conquest/
Domination are below stage 3, does NOT receive the intended competing-victory
Culture downgrade.

If the remaining Culture conditions permit Culture 2, the AI can therefore
continue diverting production, specialists and commerce toward an unready
cultural hedge even though the exact SAS policy says an advanced Diplomatic
pursuit should suppress it.

On the same turn, `AI_updateVictoryStageHash` subsequently calls
`AI_calculateDiplomacyVictoryStage` and stores DIPLOMACY3/4 correctly. Other
consumers later in the turn can see those bits; the Culture calculation that
needed them has already finished.

This is a deterministic state-order omission, not random disagreement between
two strategy estimates.

SECONDARY DIAGNOSTIC EFFECT
---------------------------
The level-2 Culture-4 diagnostic probe later in
`AI_calculateCultureVictoryStage` also reads `AI_getVictoryStageHash()` and
logs `diplomacyStage` / `competingVictoryPressure`. During normal victory-hash
construction it has the same partial-hash limitation, so diagnostics can report
Diplomacy stage 0 even though the immediately following Diplomacy calculation
will establish stage 3/4.

The numbered gameplay root is the competing-victory behavior gate; the
diagnostic mismatch is a downstream symptom of the same ordering.

ANCESTRY
--------
Base AdvCiv 1.14 already uses the inherited K-Mod ordering:

    Space -> Conquest -> Domination -> Culture -> Diplomacy

That ordering was coherent with the pre-SAS Culture function because the
existing Culture logic checks strategy bits that have already been established
before it.

SAS later added `SAS_AI_CULTURE_VICTORY_COMPETING_VICTORY_MIN_STAGE` and
explicitly included Diplomacy in the competing-stage maximum, but retained the
old update order.

Classification: SAS-local state-order regression introduced by the new Culture
competing-victory gate.

REPAIR DIRECTION
----------------
Make Diplomacy's current stage available before Culture consumes competing
victory stages.

The cleanest structural repair is likely to compute/store Diplomacy before
Culture in `AI_updateVictoryStageHash`, provided the diplomacy-stage calculator
is kept independent of Culture-stage bits. The current
`AI_calculateDiplomacyVictoryStage` does not call `AI_atVictoryStage`, so the
direct dependency is one-way and this reorder is naturally compatible with the
new SAS gate:

    Space
    Conquest
    Domination
    Diplomacy
    Culture

Then the existing Culture code can read one coherent current-turn hash.

An alternative is to compute the Diplomacy stage explicitly before the Culture
gate and pass/use that value, but that duplicates a nontrivial calculation and
the later hash update unless the update function is refactored to preserve the
result.

Do not "fix" this by retaining a previous-turn Diplomacy hash during rebuild;
that would merely replace a deterministic missing dependency with stale-state
behavior.

-------------------------------------------------------------------------------
CLOSURE-RECONCILIATION CONTROL — AI_localAttackStrength / KI#593 REPAIR
-------------------------------------------------------------------------------

The current SAS-local KI#593 repair in `AI_localAttackStrength` was deliberately
reopened against the original F270 finding and the current
`estimateCollateralWeight` contract.

No independent F374 has been established there.

The repair now derives collateral roles as follows:

* explicit/default hostile-strength queries:
  - attacker = requested enemy team, or `NO_TEAM` for the intentionally generic
    multiple-hostile mode;
  - defender = evaluating player's team;

* explicit own-offense queries:
  - attacker = evaluating player's own team;
  - defender = opposing target-plot team when known;
  - if target plot is our own team, defender remains generic rather than
    falsely treating our team as both sides.

This matches F270's repair boundary: the original bug was hard-wiring
`getTeam()` as the collateral ATTACKER even when `AI_localAttackStrength`
was counting enemy units. The current code no longer does that, while preserving
`NO_TEAM` when the query aggregates multiple hostile teams and therefore has no
single truthful attacker identity.

Representative caller classes were reopened:
* city safety / enemy offense (`NO_TEAM`);
* explicit enemy-team recapture/raze risk;
* own local offense against a target plot;
* spy/city-attack and stack-vs-stack tactical comparisons;
* city-trade third-party danger.

No supported caller was found in this slice that inverts the new role mapping.
The remaining approximation for genuinely mixed hostile teams is inherent in a
single aggregate collateral scalar and was explicitly anticipated by F270; it
is not a new root.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN (final closure reconciliation)
Unique files complete — 30/169
Confirmed deep-reloop findings — F321 through F373
F374 / provisional KI#697 — UNUSED
Latest physical checkpoint — C031-WIP63
No repository source files modified.

NEXT
----
1. Finish the remaining large `AI_calculateCultureVictoryStage` audit rather
   than stopping at F373:
   - rival visibility/eligibility semantics;
   - foundation and projected-countdown rank sentinels;
   - deadline arithmetic / no-time-limit behavior;
   - logging-dependent paths;
   - active victory-countdown comparison.
2. Finish the residual current-SAS-vs-Base changed-function checklist and the
   final state/sentinel/candidate-vs-execution pattern sweep.
3. If no independent F374 survives, freeze the Queue 001 closure checkpoint,
   increment unique files complete to 31/169, and open protected Queue 002
   `CvUnitAI.cpp`.
===============================================================================
C031-WIP64 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — F374 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvPlayerAI.cpp` remains OPEN during the final closure reconciliation.
F374 / provisional KI#697 is CONFIRMED.
F375 / provisional KI#698 is UNUSED.
Unique files complete remains 30/169 until Queue 001 itself is closed.
No repository source files were modified.

-------------------------------------------------------------------------------
F374 / provisional KI#697 — Culture victory strategy is player-local even
though the actual Cultural victory city requirement is TEAM-wide; SAS's new
deadline gate can now force cooperating teammates back to Culture 1
-------------------------------------------------------------------------------

ROOT
----
The actual game victory test counts Legendary/high-enough culture cities across
ALL members of the winning team.

Current `CvGame::testVictory`:

    int iCount = 0;
    for (MemberIter itMember(eTeam); itMember.hasNext(); ++itMember)
    {
        FOR_EACH_CITY(pCity, *itMember)
        {
            if (pCity->getCultureLevel() >=
                culturalVictoryCultureLevel())
            {
                iCount++;
            }
        }
    }

    if (iCount < kVictory.getNumCultureCities())
        return false;

K-Mod and BtS/Civ4CE use the same team-wide victory semantics (with older
explicit player loops).

By contrast, `CvPlayerAI::AI_calculateCultureVictoryStage` evaluates Cultural
victory entirely from the current player's own cities:

    FOR_EACH_CITY(pLoopCity, *this)
    {
        ...
        countdownList.push_back(iCountdown);
        if (iCountdown < iHighCultureMark)
            iHighCultureCount++;
        ...
    }

The required count remains the GAME/TEAM victory requirement:

    int iVictoryCities = kGame.culturalVictoryNumCultureCities();

but all of:

    iHighCultureCount
    iCloseToLegendaryCount
    iLegendaryCount
    countdownList
    iWinningCountdown

describe only `*this` player's cities.

The decisive Stage-3 condition is therefore:

    if (iHighCultureCount >= iVictoryCities)
        bAt3 = true;

rather than asking whether this player's TEAM has the required number of
credible cultural candidates.

SUPPORTED TEAM COUNTEREXAMPLE
-----------------------------
Assume the ordinary Cultural-victory requirement is three cities and a
two-player permanent team has:

    Player A: 2 strong near-Legendary/high-countdown culture cities
    Player B: 1 strong near-Legendary/high-countdown culture city

The actual victory engine sees three team cities. This is a complete collective
set of candidates; once those three reach Legendary, the team wins.

The AI strategy calculators see:

    Player A:
        iHighCultureCount <= 2
        countdownList.size() <= 2
        iWinningCountdown = MAX_INT

    Player B:
        iHighCultureCount <= 1
        countdownList.size() <= 1
        iWinningCountdown = MAX_INT

Neither player can satisfy:

    iHighCultureCount >= iVictoryCities /* 3 */

so neither can enter the normal Stage-3/Stage-4 cultural sprint on the strength
of the team's actual three-city path.

This is not merely a display mismatch. Culture victory stages affect
player-level commerce, specialists, buildings, production and other strategic
choices, so failing to recognize teammates' candidate cities can stop the
players from making the final coordinated push that their team victory
condition requires.

SAS DEADLINE GATE MAKES THE INHERITED GAP STRICTER
--------------------------------------------------
Current SAS adds:

    bool const bLateCultureRace = ...;

    if (!isHuman() && kGame.culturalVictoryValid() &&
        (bLateCultureRace || iWinningCountdown < MAX_INT))
    {
        ...
        bool const bWinningCountdownAvailable =
                (iWinningCountdown < MAX_INT);

        if (!bWinningCountdownAvailable || deadlineMiss)
            return 1;
    }

In the 2+1 team example, neither player has three qualifying OWN cities, so
neither can produce an Nth-city `iWinningCountdown`.

Once the late-race condition becomes true, SAS therefore does not merely fail
to promote the team path to Culture 3. It explicitly returns Culture 1 for both
players because each individual's complete three-city projection is missing.

Thus a team can collectively have exactly the required three credible culture
cities while the current SAS strategy logic treats BOTH members as lacking a
complete Cultural-victory projection.

The same mismatch also affects the new foundation/race helpers:
`getSASCultureVictoryNthCityCulture(*this, ...)` and rival countdowns are
computed per player, whereas the victory being ranked is achieved by teams.

ANCESTRY
--------
The actual Cultural-victory city test is team-wide in Civ4CE/BtS, K-Mod,
Base AdvCiv and current SAS.

K-Mod's `AI_calculateCultureVictoryStage` already loops only over the current
player's cities and compares those counts against the full victory-city
requirement. Base AdvCiv inherits that structure.

Current SAS inherits the player-local foundation and adds additional
player-local foundation/race/deadline gates; the new deadline gate makes the
late-team failure deterministic whenever the team distributes the required
candidates across members and no individual member owns a full required set.

Classification:
* root: inherited K-Mod/BBAI-style player-vs-team Cultural strategy mismatch;
* aggravating current behavior: SAS-local deadline/foundation/race logic
  preserves the same scope mismatch and can force Stage 1.

This is independent of F373. F373 is a current-turn ordering defect that hides
Diplomacy stage from the competing-victory gate. F374 is the object-scope
mismatch between a player strategy calculator and a team-wide victory
condition.

REPAIR DIRECTION
----------------
A correct team-game implementation needs to separate two questions:

1. What is the TEAM's Cultural-victory position?
2. What should THIS PLAYER contribute to that team plan?

The first must aggregate candidate culture/countdowns across `MemberIter`
rather than requiring every player independently to own `iVictoryCities`
candidates.

For team-level feasibility/race/deadline checks, build one combined list from
all team members' cities and take the team's Nth-best required candidate.

The second can remain player-local for deciding which player's slider,
specialists and production should contribute, but it should use the team's
collective race state and then recognize whether this player owns one or more
of the team's selected Cultural candidates.

Do not simply make every teammate enter Culture 4 whenever the team has three
candidates; that could waste the slider of a teammate who owns none of them.
The minimal correctness boundary is:
* team aggregation for victory feasibility/stage/race;
* player ownership for contribution intensity.

-------------------------------------------------------------------------------
ADDITIONAL CULTURE-STAGE CONTROLS CLOSED IN THIS SLICE
-------------------------------------------------------------------------------

* `SAS_AI_CULTURE_VICTORY_MAX_DEADLINE_PERCENT`
  - the default is 125 and its XML contract describes an estimated-game-length
    strategic deadline, not a 0-to-disable switch.
  - `CvGame::gameTurnProgress` explicitly documents the inherited design
    assumption that even with Time victory disabled the AI does not expect the
    game to last beyond the estimated end. Therefore using
    `getEstimateEndTurn()` as a strategic horizon is aggressive policy, not by
    itself a new F375.

* Culture logging levels:
  - the diagnostic-only foundation/race calculations are guarded so disabled
    behavior limits are checked explicitly before affecting strategy.
  - the Culture-4 candidate probe is enclosed in logging conditions and only
    computes/logs diagnostics.
  - no independent "turn logging on, strategy changes" F375 was established.

* `iOwnVictoryCountdown` comparison:
  - a real active team victory countdown is intentionally preferred over the
    projected Cultural finish when shorter. No independent sentinel inversion
    was established.

CURRENT AUDIT STATE
-------------------
Queue 001 `CvPlayerAI.cpp` — OPEN (final closure reconciliation)
Unique files complete — 30/169
Confirmed deep-reloop findings — F321 through F374
F375 / provisional KI#698 — UNUSED
Latest physical checkpoint — C031-WIP64
No repository source files modified.

NEXT
----
1. Finish the remaining Culture-stage visibility/competitor-ranking policy
   review, especially whether all-major-player ranking creates an independent
   information/eligibility defect once team scope is repaired.
2. Finish the residual changed-function checklist and final generic
   state/sentinel/loop/candidate-execution sweep.
3. If no independent F375 survives, freeze a Queue-001 closure checkpoint,
   increment unique files complete to 31/169, and open protected Queue 002
   `CvUnitAI.cpp`.
===============================================================================
C031-WIP65 — QUEUE 001 CvPlayerAI.cpp DEEP RELOOP — CLOSED
===============================================================================

STATUS
------
Queue 001 `CvGameCoreDLL/CvPlayerAI.cpp` is CLOSED under the deeper WIP-era
re-audit methodology.

Unique files complete: 31/169.
Deep-reloop findings established in Queue 001: F321 through F374.
F375 / provisional KI#698 remains UNUSED.
No repository source files were modified.

The next protected target is Queue 002:
    `CvGameCoreDLL/CvUnitAI.cpp`

-------------------------------------------------------------------------------
QUEUE 001 CLOSURE BASIS
-------------------------------------------------------------------------------

This closure is not based merely on reaching the physical end of the file.

The reloop included:

1. Current-SAS semantic-delta review against Base AdvCiv 1.14, with K-Mod
   1.46 and Civ4CE/BtS ancestry where a root depended on inherited behavior.

2. Reopening of large/high-fanout current changes rather than relying on the
   original first pass:
   * diplomacy and trade valuation;
   * technology valuation / free-tech / research;
   * civic, religion and commerce strategy;
   * worker demand and composition helpers;
   * local attack/defense strength;
   * military/disband paths;
   * Great-Person weights;
   * victory-stage caches, especially the large SAS Culture additions;
   * Advanced Start;
   * attitude/rank, danger, city-site, resource, airbase and tail helpers.

3. A whole-file function-range reconciliation after the linear pass reached the
   physical tail. This reopened functions that had not appeared explicitly in
   later WIP prose instead of assuming they were covered.

4. A current-SAS-vs-Base function-body reconciliation to catch small SAS-local
   mutations hidden among the large inherited file.

5. Repeated targeted bug-pattern sweeps for:
   * loop-carried mutation of function inputs/state;
   * sentinel collisions / disabled-value semantics;
   * candidate-selection result differing from executed candidate;
   * attempted void action treated as successful;
   * one candidate's result controlling a global phase;
   * premature returns/breaks before later alternatives;
   * state-cache construction order;
   * player-vs-team scope mismatches;
   * logging-dependent behavior;
   * division/empty-set boundaries;
   * information-scope and iterator assumptions.

6. Current XML/unit/building/tech data was used to require supported
   reproductions where source shape alone did not establish consequence.

7. Suspicions were explicitly allowed to close without numbering when the
   caller/data/contract proof did not survive.

-------------------------------------------------------------------------------
LATE CLOSURE FINDINGS
-------------------------------------------------------------------------------

The final reconciliation materially improved Queue 001 rather than merely
rubber-stamping it. Its last confirmed roots were:

F367
    `AI_getHappinessWeight` / `AI_getHealthWeight` mutate the shared
    `iExtraPop` argument inside the city loop, making later-city valuation
    depend on city iteration order.

F368
    `AI_getPlotAirbaseValue` increments `iOtherCityCount` only when a newly
    visited base/city sets a new minimum distance, so it counts traversal
    record-minima rather than nearby bases.

F369
    AdvCiv's persistent unit-brag state stores one randomized
    `AI_getBestAttackUnit()` result while diplomacy text independently rolls
    the randomized selector again, allowing remembered unit A and displayed
    unit B.

F370
    `AI_advancedStartPlaceCity` increments `iPlotsImproved` after a void
    improvement purchase that can silently fail, producing phantom
    improvements that can drive real population spending.

F371
    Advanced Start building selection stores one candidate building's
    affordability result in the global `bDoneBuildings` outer-loop flag,
    preventing later focus passes from exposing cheaper affordable buildings.

F372
    AdvCiv changed BtS/K-Mod's formerly unread Advanced Start unit `bDone`
    flag into an outer-loop terminator. One unaffordable CITY_DEFENSE unit can
    therefore prevent the next WORKER/RESERVE/COUNTER role pass even when a
    cheaper unit is affordable.

F373
    SAS's new Culture competing-victory gate checks Diplomacy stage from
    `m_eVictoryStageHash` while the normal hash rebuild computes Diplomacy only
    AFTER Culture. The current Diplomacy arm of the gate is therefore absent
    during the ordinary strategy update.

F374
    `AI_calculateCultureVictoryStage` evaluates only the current player's
    culture cities even though the real Cultural-victory city test aggregates
    every member of the team. Current SAS's late deadline gate further forces
    Stage 1 when no individual teammate owns a full N-city projection, even
    when the team collectively has the required candidate set.

These late roots justify the function-range and pattern reconciliation: several
would not have been found by merely stopping when the earlier linear walk
reached the bottom of the file.

-------------------------------------------------------------------------------
FINAL F375 CONTROLS — NO NUMBER ALLOCATED
-------------------------------------------------------------------------------

No independent F375 survived the final closure slice.

`AI_localAttackStrength` / KI#593 repair
---------------------------------------
The current SAS repair correctly stopped hard-wiring the evaluating team as the
collateral attacker. Representative enemy/default, explicit-enemy, own-offense,
city-safety, trade-danger and tactical callers were rechecked against
`estimateCollateralWeight`. Remaining mixed-hostile approximation is inherent
in a single aggregate scalar and was already part of F270's repair boundary.

Culture rival visibility
------------------------
The new foundation/projected race scans use all alive major players and can
therefore consult exact data for unmet rivals. That is genuine omniscient
information use, but the current feature is explicitly a global race-ranking
heuristic and the source/XML contract does not promise a known-rivals-only
scope. Civ4 AI contains other deliberate information advantages. Without a
stronger fairness/knowledge contract, this remains design debt / a possible
policy improvement rather than F375. Same-team competition is NOT discarded:
the materially incorrect player-vs-team victory scope is already captured by
F374.

Culture deadline horizon
------------------------
Using `getEstimateEndTurn()` even when Time victory is disabled can aggressively
reject very late cultural plans, but AdvCiv's own `gameTurnProgress` contract
explicitly states that the AI does not expect the game to last beyond the
estimated end even with Time victory disabled. This is strategic policy, not a
new arithmetic bug.

Culture logging
---------------
The logging-only foundation/race/C4 probes were checked for feedback into
strategy. Behavior gates explicitly test their XML enable values, and diagnostic
vectors/counters do not alter decisions. No log-level-dependent strategy root
was established.

Great-Person weights
--------------------
The SAS Culture/corporation damping rewrite was reopened:
* `AI_permanentSpecialistValue` initializes all optional diagnostic outputs;
* logging-only maps/vectors do not feed the chosen weights back;
* the Culture-stage dependency reads a refreshed per-turn victory hash before
  the three-turn GP-weight update in the ordinary turn lifecycle;
* corporation-founder and Join-City damping are intentional policy according
  to the current XML/source comments.
The inherited three-turn refresh cadence can make any GP input stale briefly,
but it predates these changes and is an explicit performance tradeoff.

`AI_updateStrategyHash`
-----------------------
The current-vs-Base behavioral delta in the reopened strategy hash is only the
restart-cached `BBAI_TURTLE_ENEMY_POWER_RATIO` read. No runtime writer was
established and behavior is unchanged.

Advanced Start tech attempts
----------------------------
The early/interleaved void tech actions can fail after selection while local
tech-allocation heuristics still advance. This is a weaker sibling of F370.
The false state is confined to the current Advanced Start allocation heuristic
rather than representing a purchased object or consuming a hard success quota,
and the stronger independent building/unit termination defects are already
F371/F372. It remains documented control/debt rather than manufacturing F375.

Capital-first `AI_getBestAttackUnit`
------------------------------------
A Factory-less capital can miss a stronger Factory-gated attacker trainable in
another city. The demonstrated impact remains mostly presentation/brag
semantics and overlaps F369; no independent gameplay consequence strong enough
for F375 was established.

`AI_calculateStolenCityRadiusPlots`
-----------------------------------
AdvCiv's per-city-radius rewrite can count one physical foreign-owned plot more
than once when it affects multiple cities, but the rewrite is explicitly a
per-city capped border-grievance model and one plot can deny workable space to
multiple cities. No unique-physical-plot contract was established.

`AI_calculateUnitAIViability`
-----------------------------
Default UnitClass units can differ from civilization-specific UUs in UnitAI
availability. Current gameplay callers requiring the relevant discrepancy are
sea-role callers, and current SAS has no naval UU producing it. This remains a
mod-mod hazard, not a current root.

-------------------------------------------------------------------------------
QUEUE 001 FINAL STONE
-------------------------------------------------------------------------------

Queue 001:
    CvGameCoreDLL/CvPlayerAI.cpp
    DEEP RELOOP COMPLETE / CLOSED

Unique files complete:
    31 / 169

Confirmed deep-reloop finding range:
    F321 - F374

Next finding:
    F375 UNUSED
    provisional KI#698

Latest physical checkpoint:
    C031-WIP65

Repository source modifications:
    NONE

PROTECTED ROUTE
---------------
Open Queue 002 `CvGameCoreDLL/CvUnitAI.cpp`.

The original Queue-002 pass already strengthened old F002/KI#331 around
`scrap()` caller assumptions and retained KI#213 as a known cross-file
`AI_bestCityBuild`/empty-city-cache architecture issue. The deeper reloop must
first reconcile those old roots against CURRENT source (several KI#331 caller
repairs may now exist), then perform the same stronger methodology used here:
current-SAS deltas first, high-fanout methods, inherited ancestry, current-data
proofs, function-range reconciliation, and final bug-pattern sweep.

Do not resume the normal pending queue 020 until protected Queues 002 through
009 have all received this deeper reloop.

END C031-WIP65
===============================================================================
C031-WIP66 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — OPEN
===============================================================================

STATUS
------
Queue 001 `CvGameCoreDLL/CvPlayerAI.cpp` remains CLOSED.
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` is now OPEN under the deeper WIP-era
methodology.

Unique files complete: 31/169.
Next new finding: F375 / provisional KI#698 — UNUSED.
No repository source files were modified.

-------------------------------------------------------------------------------
OPENING RECONCILIATION — OLD F002 / KI#331
-------------------------------------------------------------------------------

The old cross-file `scrap()` contract root is materially repaired in CURRENT
source and should not be rediscovered as F375.

Current `CvUnit::scrap()` now returns explicit success:

    bool CvUnit::scrap()
    {
        if (!canScrap())
            return false;
        scrapInternal();
        return true;
    }

The previously broadened Queue-002 callers now respect that contract:

* Worker discretionary cleanup:
      if (SyncRandSuccess(rScrapProb) && scrap())
          return;
  A restrictive SAS veto now falls through to ordinary Worker fallback.

* Excess land Explorer:
      if (scrap()) return;
  Failed cleanup continues into stranded/patrol/safety handling.

* Trapped Barbarian ship:
      if (bScrap && scrap()) return;

* Escort Sea cleanup:
      if (scrap()) return;

* obsolete/excess Explore Sea cleanup:
      if (scrap()) return;
  Both relevant inherited branches now continue if cargo-capacity protection
  vetoes the removal.

The two contexts that genuinely require removal after caller-specific safety
checks use the explicit forced path:

* retired inferior Settler transport after unloading:
      if (scrapForced()) return;

* doomed-city scorched-earth evacuation:
      if (scrapForced()) return true;

No remaining active `scrap(); return;` pattern from the original Queue-002
KI#331 expansion was found in the opening current-source grep. The idle
Barbarian Worker branch checks `canScrap()` before calling `scrap()`, and
`AI_scrapSettlerWithNoValidSite` deliberately uses its own guarded
`scrapInternal()` mandatory-cleanup operation.

Conclusion:
old F002 / KI#331 remains historical evidence and a cross-file contract lesson,
but its Queue-002 caller set is currently repaired/saturated. Do not allocate a
new finding merely for those old sites.

-------------------------------------------------------------------------------
OPENING RECONCILIATION — EXISTING KI#213 STILL SURVIVES
-------------------------------------------------------------------------------

The known city-cache / Worker-policy architecture split is still present and
must be retained as an EXISTING cross-file root, not duplicated as F375.

Current `CvCityAI::AI_getImprovementValue` explicitly disables the legacy
evaluator for every land plot:

    if (!kPlot.isWater())
    {
        if (peBestBuild)
            *peBestBuild = NO_BUILD;
        return 0;
    }

Current land Workers instead use the large custom
`CvUnitAI::AI_bestCityBuild`, which explicitly understands ordinary no-bonus
land terrain/features, including dedicated Flood Plains branches and the
current XML-driven build picker.

Several city/player consumers still ask the intentionally empty legacy cache.

A) Advanced Start remains structurally disconnected
----------------------------------------------------
`CvPlayerAI::AI_advancedStartPlaceCity` calls:

    pCity->AI_updateBestBuild();

and then searches ordinary city plots solely through:

    pCity->AI_getBestBuildValue(ePlot)
    pCity->AI_getBestBuild(ePlot)

before purchasing `ADVANCEDSTARTACTION_IMPROVEMENT`.

Because the city evaluator returns 0 / NO_BUILD for ordinary land, the custom
Worker policy that knows how to Farm/Mine/Cottage/etc. those plots is invisible
to this land-improvement purchase loop.

This is separate from F370's phantom-success bookkeeping: even after F370 is
repaired, Advanced Start still lacks the land candidate source that ordinary
Workers use.

B) Worker-demand workaround still has a feature-land hole
----------------------------------------------------------
`CvCityAI::AI_updateWorkersHaveAndNeeded` contains a current SAS workaround:

    bool const bSASWorkedLandDemand =
        (!kPlot.isWater() && !kPlot.isFeature() &&
         kPlot.getOwner() == getOwner());

For a worked unimproved plot it counts Worker demand when either the legacy
cache has a build or this featureless-land fallback is true.

That deliberately excludes FEATURE land. Yet `AI_bestCityBuild` explicitly has
valid custom no-bonus feature branches, including `FEATURE_FLOOD_PLAINS`, and
ordinary Forest/Jungle removal/build-followup policy.

Thus a currently worked improvable land plot can still be classified as
`iWorkedUnimprovableCount` solely because it has a feature and the city-side
cache is intentionally empty.

For unworked land, the mismatch is broader: the future-work count still
requires `AI_getBestBuild(ePlot) != NO_BUILD`, so ordinary custom land jobs are
invisible to that cache-derived path.

Existing classification:
KI#213 — pending cross-file architecture root.
Do not assign F375 for these rediscovered consequences.

Repair boundary remains architectural:
share the pure city/plot/build policy between city-side consumers and
`AI_bestCityBuild`; keep unit-specific pathing, danger, reservations and
mission execution in CvUnitAI.

-------------------------------------------------------------------------------
QUEUE-002 CURRENT DELTA INVENTORY
-------------------------------------------------------------------------------

The current file contains roughly 230 unique `CvUnitAI::` member definitions.
A brace-aware current-SAS vs Base-AdvCiv comparison identifies the main
behavioral/custom delta surface, headed by:

* `AI_bestCityBuild` — ~1,076 current lines vs ~135 Base lines;
* `AI_promotionValue` — large SAS hard-policy layer;
* `AI_foundFirstCity` — extensive first-capital scouting/site rewrite;
* `AI_attackCityMove`;
* `AI_workerMove`;
* `AI_nextCityToImprove`;
* `AI_betterPlotBuild` / functionally disabled `AI_irrigateTerritory`;
* Settler/founding helpers;
* Great-Person / Great-General action helpers;
* city guard / target-city;
* naval assault/transport/exploration;
* Worker improvement/transport/stranded helpers.

The original Queue-002 pass covered these broadly. The deeper reloop will not
simply repeat that prose. It will prioritize:
1. current SAS-only state transitions and caller contracts;
2. the large custom Worker/founding functions;
3. current-data proofs for any suspicious candidate;
4. inherited ancestry only where the root survives current SAS;
5. a final function-range and bug-pattern reconciliation before closure.

-------------------------------------------------------------------------------
FIRST DEEP TRANCHE — AI_bestCityBuild (OPEN)
-------------------------------------------------------------------------------

The first ~1,076-line custom Worker selector is being reread as a pipeline:

1. irrigation-chain candidate generation;
2. per-BFC bonus-specific / no-bonus build selection;
3. feature-removal overlay and optional follow-up;
4. anti-oscillation / food / irrigation replacement guards;
5. candidate sorting;
6. pathability, enemy and one-worker reservation checks;
7. output atomization.

Controls already surviving:
* output parameters are initialized and only committed after `bFound`;
* `NO_BUILD` is rejected before BuildInfo indexing on the final candidate path;
* the BuildTypes-indexed adjustment vector is sized to `GC.getNumBuildInfos()`;
* the generic XML branch picker filters every candidate through `canBuild`;
* the irrigation search uses the already-fixed KI#214 `(plot,steps)` state,
  not the older one-cost-per-plot collapse;
* F370-style "attempted action == successful state" does not occur inside the
  selector itself; mission execution is in callers.

No independent F375 has crossed the proof threshold in this opening slice.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F321-F374 confirmed from protected Queue 001.
F375 / provisional KI#698 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set currently repaired.
Latest physical checkpoint: C031-WIP66.
No repository source files modified.

NEXT
----
Continue the deep `AI_bestCityBuild` pipeline and its three live land-Worker
callers (`AI_workerMove`, `AI_improveCity`, `AI_nextCityToImprove`), then widen
to first-city founding and combat/transport deltas only after the Worker
candidate/execution contracts are saturated.
===============================================================================
C031-WIP67 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F375 CONFIRMED
===============================================================================

STATUS
------
Queue 001 `CvGameCoreDLL/CvPlayerAI.cpp` remains CLOSED.
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN under the deeper WIP-era
methodology.

Unique files complete: 31/169.
F375 / provisional KI#698 is CONFIRMED.
F376 / provisional KI#699 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F375 / provisional KI#698 — SAS `countImprovedTiles` counts improvements this
city cannot work, so foreign/shared BFC improvements can make an
underdeveloped city look worker-ready
-------------------------------------------------------------------------------

ROOT
----
Current SAS added the file-local helper:

    static int countImprovedTiles(CvCity const* pCity)
    {
        int iCount = 0;
        for (int i = 0; i < NUM_CITY_PLOTS; ++i)
        {
            CvPlot* pPlot = plotCity(...);
            if (pPlot == NULL)
                continue;

            const ImprovementTypes eImp = pPlot->getImprovementType();
            if (eImp != NO_IMPROVEMENT)
                ++iCount;
        }
        return iCount;
    }

The accompanying history comment is particularly useful: an earlier version
filtered by

    getWorkingCity() == pCity

but SAS deliberately removed that filter in order to make the tile count
"tolerant of overlap."

That broadening crossed the actual worker/city contract.

`WorkablePlotIter(kCity)`, used by current `AI_bestCityBuild`, explicitly skips
a city-radius plot when:

    m_pNext->getWorkingCity() != m_pCity

and `CvCity::canWork` begins with the same condition:

    if (kPlot.getWorkingCity() != this)
        return false;

`CvPlot::updateWorkingCity` also establishes why ownership matters. If a plot
is not owned, it receives no default working city. For owned overlap, one
specific same-owner city is selected as the working city (or an override is
used).

Therefore "has an improvement somewhere inside the geometric 21-tile BFC" is
not equivalent to "is an improved plot available to this city."

CURRENT BEHAVIORAL CONSUMERS
----------------------------
The incorrect count feeds two live SAS worker decisions.

A) Small-city "improved enough" early departure
------------------------------------------------
`AI_workerMove` currently performs:

    if (maxPopDefine > 0 &&
        cityPopulation <= maxPopDefine &&
        countImprovedTiles(pCity) >=
            cityPopulation + buffer + smallCityExtra)
    {
        ...
        if (AI_nextCityToImprove(pCity))
            return;
    }

The current XML default is:

    SAS_AI_WORKER_OVERIMPROVE_SKIP_MAX_CITY_POPULATION = 5

and the C++ buffers are:

    iBufferForAllCities = 2
    iBufferExtraForSmallCities = 3 when population <= 4

Thus a size-5 city is declared geometrically "improved enough" at 7 counted
improvements.

Those seven improvements need not be seven usable improvements for that city.
They can include:
* improvements on tiles now owned by a rival;
* pre-existing improvements on currently unowned plots;
* same-owner overlap plots whose `getWorkingCity()` is another city.

The keep-current-plot safeguard does not repair the count. It only prevents
departure when the Worker happens already to stand on this city's own
currently-workable best local plot. In every other case the inflated count can
send it to another city.

B) Cross-city Worker readiness ranking
---------------------------------------
`AI_nextCityToImprove` computes:

    iImprovedPlots = countImprovedTiles(pLoopCity);
    iEffectiveImprovedPlots = iImprovedPlots + iAssignedWorkers;
    iReadinessPercent =
        max(100,
            100 * (population + 1) /
            (iEffectiveImprovedPlots + 1));

    score = iBuildValue * iReadinessPercent / 100;

This consumer is not limited to population <= 5.

A city with few genuinely usable completed improvements is supposed to receive
a multiplier above 100%. Every foreign/unassigned improvement counted by the
helper enlarges the denominator and suppresses that boost.

A simple shape demonstrates the magnitude. For a size-4 city with no completed
usable improvements and no assigned Worker, the intended readiness term would
be:

    100 * (4 + 1) / (0 + 1) = 500%

If four physically improved BFC plots belong to a rival or are assigned to
another city, the current helper reports four and the same city receives:

    100 * 5 / 5 = 100%

Its genuine `AI_bestCityBuild` candidate is still selected only from
`WorkablePlotIter(kCity)`, so those four counted plots are not part of the
candidate set whose shortage the readiness multiplier is meant to describe.

This can cause a genuinely underdeveloped border/overlap city to lose Worker
priority to a better-looking city elsewhere.

WHY THE OVERLAP CHANGE DOES NOT ESTABLISH SAFETY
------------------------------------------------
The historical rationale says to count overlapping improvements even when
`getWorkingCity() != pCity`.

That can be a reasonable *separate* measure of potential same-owner tile
flexibility, but the current consumers treat the number as completed
improvement capacity of this specific city.

Most importantly, the helper does not distinguish same-owner overlap from
foreign/unowned plots at all. A rival Mine inside the geometric BFC and a
currently usable city Mine both add exactly 1.

Even for friendly overlap, current `AI_bestCityBuild` itself cannot use the
shared plot while its `getWorkingCity()` points at the other city. Counting it
as completed capacity while excluding it from the job/candidate set makes the
readiness numerator and denominator describe different plot populations.

CONSEQUENCE
-----------
Current SAS can:
* move a Worker out of a small city earlier than the city's own workable
  improvements justify;
* underweight that city in `AI_nextCityToImprove`;
* preserve the exact type of edge-city underdevelopment the custom cross-city
  Worker rewrite was intended to solve, especially around contested borders
  and overlapping BFCs.

This is independent of KI#213. KI#213 is the architectural split where city
consumers read the intentionally-empty legacy land `AI_getBestBuild` cache
instead of the custom `AI_bestCityBuild` policy.

F375 is entirely inside the new SAS Worker policy: its completed-improvement
counter and its actual custom candidate iterator disagree about which plots
belong to the city.

ANCESTRY
--------
Base AdvCiv 1.14, K-Mod and BtS/Civ4CE do not contain this SAS
`countImprovedTiles` helper or the current cross-city readiness formula.

Classification: SAS-local worker-allocation scope regression.

REPAIR DIRECTION
----------------
At minimum, never count foreign or unowned plots as completed capacity for the
city.

The most contract-consistent conservative repair is to count the same plot
population that the actual city Worker selector can currently act for:

    if (pPlot->getWorkingCity() != pCity)
        continue;

before counting the improvement.

If SAS deliberately wants friendly overlap to count as *potentially
reassignable* capacity, that should be modeled separately and must at least
require same ownership. It should not be mixed one-for-one into the
`iImprovedPlots` term unless the corresponding candidate/job logic can also
treat those overlap plots as available to this city.

A useful split would be:
* `iUsableImprovedPlots`: `getWorkingCity() == pCity`;
* optional `iFriendlyOverlapImprovedPlots`: same owner, another working city,
  with a reduced/tunable contribution if desired.

Foreign/unowned improvements should contribute zero.

-------------------------------------------------------------------------------
QUEUE-002 DEEP CONTROLS CHECKED IN THIS SLICE
-------------------------------------------------------------------------------

Worker candidate/execution pairing
----------------------------------
The large `AI_bestCityBuild` pipeline was followed through its live custom
callers. It initializes outputs, commits them only after the final candidate
survives pathability/enemy/reservation checks, and the custom
`AI_improveCity` / `AI_nextCityToImprove` callers preserve the selected build
and follow-up rather than silently substituting another build.

No F370-style "selected candidate != executed candidate" root was established
there.

`AI_improveLocalPlot` still reading the city-side land cache reduces to the
already-existing KI#213 architecture mismatch rather than a new F376.

Great General / Great Person tranche
------------------------------------
The current KI#204 safety repair in `AI_construct` is present. `AI_join`,
`AI_construct`, `AI_lead` and the large `AI_greatPersonMove` changes were
reopened.

* The default Great-General instructor cap is 999, so the fact that `AI_join`
  counts already-joined specialists only after city safety/path eligibility
  has no demonstrated default-game cap consequence. It remains a low-define
  customization hazard, not F376.
* Current GP slow-action state preserves the actual chosen mission/city/path
  variables; the known KI#168/KI#169/KI#170 repairs survive.
* Added Great-Artist / GP diagnostics do not feed values back into choice.

Promotion tranche
-----------------
`AI_promotionValue` indexes `GC.getInfo(ePromotion)` before its local
NO_PROMOTION check, but its live `AI_promote` caller iterates valid Promotion
enums and calls only legal promotions. No invalid enum producer was
established.

The large SAS forbidden-promotion policy can leave a unit unpromoted only if
all legal alternatives score nonpositive/forbidden; the deeper current-data
pass has still not established such a current all-options-blocked state.
Policy remains policy rather than F376.

First-city / Settler tranche
----------------------------
The large `AI_foundFirstCity`, `AI_found` and `AI_settleMove` rewrites were
reopened. The bounded first-city scout, post-scout return target and current
founding guards preserve distinct target/end-turn state.

`SAS_chooseFirstCityReturnPlot` uses the first-city turn window as a pathfinder
step bound, but the scout itself advances only one adjacent scouting step per
turn and the remembered origin therefore remains within the same geometric
bound. No concrete lost-origin failure was established.

The K-Mod settler/escort rendezvous call still initializes `iPathTurns` to
MAX_INT before its unchecked `generatePath`; a failed path therefore remains
above the two-turn acceptance threshold on the live implementation. No new
root was assigned.

Combat / target selection opening
---------------------------------
An exact brace-aware comparison corrected an earlier parser artifact:
`AI_attackOdds` is 85 lines in current SAS and 85 lines in Base AdvCiv 1.14,
with no body diff. It is not a hidden large SAS hotspot.

The genuinely large current deltas `AI_attackCityMove`,
`AI_pickTargetCity`, `AI_guardCity`, `AI_attackMove` and
`AI_assaultSeaMove` have been reopened.

So far:
* launched-Space-capital target identity remains paired through the separate
  emergency target ranking and execution path;
* `bTargetTooStrong` is computed only after the final normal/barbarian target
  re-pick and no later target replacement using that stale strength state was
  established;
* emergency multi-defender `AI_guardCity` preserves at least one unit in a
  multi-unit donor group and returns true exactly when the currently updating
  unit/group has itself been committed;
* the opportunistic assault-transport addition calls the existing
  target-specific `AI_assaultSeaTransport` after ordinary safety/loading waits;
  no independent threshold bypass defect was established in this slice.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F321-F374 confirmed from protected Queue 001.
F375 / provisional KI#698 CONFIRMED.
F376 / provisional KI#699 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set currently repaired.
Latest physical checkpoint: C031-WIP67.
No repository source files modified.

NEXT
----
Continue from F376 unused.

Priority:
1. finish `AI_attackCityMove` / `AI_guardCity` / `AI_attackMove` target and
   group-mutation contracts;
2. deepen naval transport (`AI_assaultSeaTransport`,
   settler/assault loading, pickup/stranded and cargo-state transitions);
3. resume the residual current-SAS-vs-Base changed-function reconciliation;
4. run the same final loop/state/sentinel/candidate-vs-execution sweep over
   Queue 002 before any closure decision.
===============================================================================
C031-WIP68 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F376 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375 / provisional KI#698 remains CONFIRMED.
F376 / provisional KI#699 is CONFIRMED.
F377 / provisional KI#700 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F376 / provisional KI#699 — AdvCiv `AI_plotValid` optimization inverted the
`AI_connectBonus` area test into a tautology, making the helper always return
false
-------------------------------------------------------------------------------

ROOT
----
Current SAS and Base AdvCiv 1.14 contain:

    bool CvUnitAI::AI_connectBonus(bool bTestTrade)
    {
        ...
        for (int iI = 0; iI < GC.getMap().numPlots(); iI++)
        {
            CvPlot const& kPlot = GC.getMap().getPlotByIndex(iI);
            if (kPlot.getOwner() != getOwner() || // XXX team???
                /*!AI_plotValid(kPlot)*/kPlot.isArea(kPlot.getArea())) // advc.opt
            {
                continue;
            }

            ...
        }
        return false;
    }

The second rejection term is always true for a valid plot.

`CvPlot.h` defines:

    bool isArea(CvArea const& kArea) const
    {
        return (area() == &kArea);
    }

and `kPlot.getArea()` is, by definition, that same plot's area. Therefore:

    kPlot.isArea(kPlot.getArea()) == true

for every ordinary map plot.

The `if` therefore reduces to:

    if (anything || true)
        continue;

Every loop iteration is skipped. No bonus can reach the subsequent
non-obsolete-bonus, capital-connection or `AI_connectPlot` logic, and the
function deterministically falls through to:

    return false;

ANCESTRY / INTRODUCING CHANGE
-----------------------------
Civ4CE/BtS and K-Mod use the meaningful original gate:

    if (AI_plotValid(pLoopPlot))
    {
        if (pLoopPlot->getOwner() == getOwner())
        {
            ...
        }
    }

The reachable AdvCiv history pinpoints the regression to pre-SAS AdvCiv commit:

    practical commit 1925
    e4ff624d23ec90298119911fa182fa0e4c870d5a
    "Remove or replace unnecessary AI_plotValid calls"

Immediately before that commit, AdvCiv had:

    if (!AI_plotValid(kPlot) || kPlot.getOwner() != getOwner())
        continue;

The commit changed it to:

    if (kPlot.getOwner() != getOwner() ||
        /*!AI_plotValid(kPlot)*/kPlot.isArea(kPlot.getArea()))
    {
        continue;
    }

The surrounding changes show the intended same-area replacement pattern
elsewhere in the same commit. For example, `AI_improveBonus` changes:

    !AI_plotValid(kPlot)

to:

    !isArea(kPlot.getArea())

and nearby `AI_routeTerritory` likewise uses:

    /*!AI_plotValid(kPlot)*/ !isArea(kPlot.getArea())

The `AI_connectBonus` replacement alone lost the negation and, additionally,
asks the plot whether it belongs to its own area instead of asking the unit
whether the plot belongs to the unit's area.

Classification: inherited AdvCiv optimization regression, introduced while
replacing `AI_plotValid`.

LIVE CONSEQUENCE
----------------
This is not dead source. Current SAS has three live calls.

A) Normal land Worker fallback in `AI_workerMove`
--------------------------------------------------
After city/local improvement, next-city reassignment and
`AI_routeTerritory(true)`, a route-capable Worker executes:

    if (AI_connectBonus(false))
        return;

That pass is intended to connect an owned bonus even without requiring its
current improvement to be the connecting improvement.

Because F376 makes the helper always false, this dedicated fallback never
pushes such a mission.

B) `AI_networkAutomated` first bonus-connection pass
----------------------------------------------------
After `AI_improveBonus()`:

    if (AI_connectBonus())
        return;

This is the stricter trade-ready form. It is dead at runtime because of F376.

C) `AI_networkAutomated` second bonus-connection pass
-----------------------------------------------------
After `AI_connectCity()` and `AI_routeTerritory(true)`:

    if (AI_connectBonus(false))
        return;

This broader connection pass is also dead.

Other Worker/route functions can sometimes connect the same resources by
different routes, so the consequence is NOT "the AI can never connect a
bonus." The precise defect is stronger and cleaner: every caller of this
dedicated helper is guaranteed to receive false, and all strategy encoded
inside the helper is unreachable.

This especially matters for fallback cases for which the ordinary local-city
improvement pipeline has no currently selected route job, including resources
outside ordinary city work assignments and network-automation behavior.

WHY THIS IS NOT KI#213 OR F375
------------------------------
KI#213 concerns the intentionally empty city-side land best-build cache versus
the custom SAS Worker selector.

F375 concerns SAS `countImprovedTiles` using a broader physical BFC population
than the actual workable candidate iterator.

F376 is independent and much older: the entire inherited bonus-connection
helper is disabled by a boolean typo before any cache, candidate valuation or
SAS Worker policy is reached.

REPAIR DIRECTION
----------------
At minimum restore the intended negative same-area test for the land-Worker
optimization, consistent with the neighboring AdvCiv replacements:

    if (kPlot.getOwner() != getOwner() ||
        /*!AI_plotValid(kPlot)*/ !isArea(kPlot.getArea()))
    {
        continue;
    }

If preserving the full modern `AI_plotValid` semantics is desired for unusual
can-move-impassable / all-terrain units, simply restore:

    if (kPlot.getOwner() != getOwner() || !AI_plotValid(kPlot))
        continue;

The second form most directly restores the pre-regression contract; the first
matches the performance-oriented intent of the introducing AdvCiv commit for
ordinary land Workers.

-------------------------------------------------------------------------------
NAVAL / TRANSPORT CONTROL WORK COMPLETED BEFORE F376
-------------------------------------------------------------------------------

The new destination-aware Settler cargo helper was traced end to end.

`AI_unloadSettlerCargoForDestination`
-------------------------------------
The helper's main accounting is internally coherent:

* before the founding voyage, it computes the destination area's Worker
  deficit and keeps that many cargo Workers aboard while unloading true excess
  only when still in an owned origin city and before a `MISSIONAI_FOUND`
  assignment exists;

* at the destination, cargo Workers can already be included in
  `AI_totalAreaUnitAIs(destination, UNITAI_WORKER)` because the transport is
  physically on the destination land area; the helper explicitly subtracts
  those cargo Workers before computing the shortage, preventing self-counting;

* it unloads Settlers/defenders at the destination while allowing excess
  Workers to remain aboard for reuse.

No selected-vs-executed or false-success state root was established in the
helper itself.

`AI_findTransport` Worker reload gate
-------------------------------------
The new gate rejects a Worker from rejoining a Settler transport already on
`MISSIONAI_FOUND` when the destination area's existing Worker count already
meets demand.

One subtle limitation was traced: while a transport is still outside the
destination area, Workers already aboard are not represented in
`AI_totalAreaUnitAIs(destination, UNITAI_WORKER)`. A capacity-four Settler
transport can therefore temporarily accept a second Worker for a destination
whose deficit is only one.

However:
* the existing Settler-transport cargo-space guard prevents the analogous
  duplicate on the common tighter capacities once only two non-Worker slots
  remain;
* before the initial voyage, the destination-aware unload helper removes
  excess Workers;
* after departure, any excess that does remain is retained aboard at the
  destination rather than dumped into a zero-demand colony.

This can waste a cargo slot in a narrow timing/capacity case, but no sufficiently
independent strategic consequence crossed the F377 threshold in this slice.

`AI_pickupStranded`
-------------------
The new completed-area Worker coordination gate merely prevents two empty
transports being dispatched to the same zero-demand Worker group. Ordinary
stranded groups retain Base AdvCiv's capacity-based coordination. No root was
established.

`AI_assaultSeaMove`
-------------------
The new below-threshold opportunistic launch still calls the existing
target-specific `AI_assaultSeaTransport` after the earlier safety, escort and
imminent-loading waits. No independent target-strength bypass was established.

-------------------------------------------------------------------------------
CURRENT STONE
-------------------------------------------------------------------------------

Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F376 confirmed in protected Queue 002.
F377 / provisional KI#700 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set remains repaired.
Latest physical checkpoint: C031-WIP68.
No repository source files modified.

NEXT
----
Continue from F377 unused.

Priority:
1. use the same whole-file boolean/predicate sweep that exposed F376 to inspect
   the remaining inherited `AI_plotValid` replacements and always-true/always-
   false area/domain predicates;
2. finish `AI_attackCityMove` / `AI_guardCity` group-splitting and target-state
   review;
3. finish naval cargo/loading transitions;
4. reconcile the moderate current-SAS deltas (`AI_update`, `AI_evacuateCity`,
   `AI_improvePlot`, `AI_connectBonusCost`, Worker Sea, upgrade travel, etc.)
   before any Queue-002 closure decision.
===============================================================================
C031-WIP69 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F377 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F376 remain CONFIRMED.
F377 / provisional KI#700 is CONFIRMED.
F378 / provisional KI#701 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F377 / provisional KI#700 — SAS `AI_betterPlotBuild` reversed its feature-
removal predicate, so a legal improvement that preserves Forest/Jungle can be
replaced with a pure chop
-------------------------------------------------------------------------------

ROOT
----
Current SAS `CvUnitAI::AI_betterPlotBuild` says:

    // High-Priority Override 1: Clear Feature
    // Check if the originally planned improvement requires a feature
    // to be removed first

but executes:

    if (!kOriginalBuildInfo.isFeatureRemove(eFeature))
    {
        if (eFeature == eFeatureForest)
        {
            if (canBuild(kPlot, eBuildRemoveForest))
                return eBuildRemoveForest;
        }
        else if (eFeature == eFeatureJungle)
        {
            if (canBuild(kPlot, eBuildRemoveJungle))
                return eBuildRemoveJungle;
        }
        ...
    }

The condition has the opposite meaning from the comment and from the safe
contract.

`isFeatureRemove(eFeature) == false` means the original build does NOT remove
that feature. The helper therefore substitutes a dedicated removal build
precisely when the selected build preserves the feature.

CURRENT-SAS CONCRETE CASE: FOREST CAMP
--------------------------------------
Current XML supplies an ordinary supported example.

`BUILD_CAMP`:
* prerequisite Hunting;
* builds `IMPROVEMENT_CAMP`;
* its `FeatureStructs` contain Fallout removal only;
* it has NO `FEATURE_FOREST` removal entry.

Therefore:

    GC.getInfo(BUILD_CAMP).isFeatureRemove(FEATURE_FOREST) == false.

`BONUS_DEER`:
* can occur on `FEATURE_FOREST`;
* is valid on Tundra / Forest configurations.

`BONUS_FUR`:
* can occur on `FEATURE_FOREST`;
* is valid on Tundra/Snow Forest configurations.

`IMPROVEMENT_CAMP`:
* explicitly connects Deer and Fur;
* those bonus entries make Camp valid on the resource.

`BUILD_REMOVE_FOREST`:
* becomes legal through its Forest `FeatureStruct` at Masonry;
* removes `FEATURE_FOREST`.

Thus after Masonry a Worker can legally be assigned `BUILD_CAMP` on a Forest
Deer/Fur tile while `BUILD_REMOVE_FOREST` is also legal.

LIVE CALLER PROOF
-----------------
The important live path is `CvUnitAI::AI_improveBonus`.

For a land bonus in a working city, current SAS first obtains its
bonus-specific build:

    eBuild = kPlot.SAS_getBonusSpecificBuild(eNonObsoleteBonus);

It then requires all of:

    eBuild != NO_BUILD

    kOwner.doesImprovementConnectBonus(
        GC.getInfo(eBuild).getImprovement(),
        eNonObsoleteBonus)

    canBuild(kPlot, eBuild)

before accepting it as `eBestTempBuild`.

For Forest Deer/Fur this can establish `BUILD_CAMP` as the selected build.
The caller later queues movement toward the exact target and only then runs:

    eBestBuild = AI_betterPlotBuild(*pBestPlot, eBestBuild);

At this point the caller has already proved:

    canBuild(Forest Deer/Fur, BUILD_CAMP) == true.

That fact is decisive. The Forest does not need to be cleared in order to
execute the planned Camp.

Nevertheless the current postprocessor evaluates:

    !BUILD_CAMP.isFeatureRemove(FEATURE_FOREST) == true

and, when Forest removal is legal, returns:

    BUILD_REMOVE_FOREST

instead of `BUILD_CAMP`.

The Worker therefore performs a pure chop rather than the already-selected
resource improvement.

The custom `AI_bestCityBuild` execution paths now intentionally bypass
`AI_betterPlotBuild`, so they do not reproduce this mismatch. That does not
make F377 dormant because `AI_improveBonus` remains a live land-Worker fallback
and executes the postprocessor exactly as above.

CONSEQUENCE
-----------
The AI can:
* spend an extra Worker action chopping a Forest before constructing a Camp
  that was already legal without the chop;
* destroy a feature that the resource improvement can coexist with;
* delay connecting the Deer/Fur resource that `AI_improveBonus` explicitly
  selected;
* replace a caller-validated exact improvement choice with a different action
  whose prerequisite rationale is false.

The immediate chop production may sometimes be strategically useful, but that
does not repair the contract. The helper is not evaluating chop value here;
it claims to clear a feature only when required by the planned improvement,
and the caller has already proved the improvement is legal on the feature.

The same inverted class can affect any current/future Forest/Jungle-preserving
build for which the dedicated removal build is legal.

ANCESTRY / INTRODUCING CHANGE
-----------------------------
Base AdvCiv 1.14 and K-Mod retain the older `bClearFeature` logic. They only
consider a feature-clearing override when the ORIGINAL build itself removes
the feature:

    if (kOriginalBuildInfo.isFeatureRemove(eFeature))
        ...

SAS practical commit 4921
`cc59407b55267151ae1cad01472a3625b83fa9bb`
rewrote/simplified `AI_betterPlotBuild` and still preserved the correct sense:

    if (eFeature != NO_FEATURE &&
        kOriginalBuildInfo.isFeatureRemove(eFeature))
    {
        // find a build that removes the feature
    }

The very next reachable practical commit 4922
`7e7a475897...`
changed that condition to:

    if (!kOriginalBuildInfo.isFeatureRemove(eFeature))

while adding hardcoded Forest/Jungle removal builds.

The surrounding prose still says this is a safeguard for cases where a
"specific build to remove feature" may have been missed, but the inversion
means it now fires for builds that explicitly do NOT remove the feature.

Classification: SAS-local regression introduced in practical commit 4922.

REPAIR DIRECTION
----------------
The minimum correctness repair is to restore the positive predicate:

    if (eFeature != NO_FEATURE &&
        kOriginalBuildInfo.isFeatureRemove(eFeature))
    {
        ...
    }

However, given current SAS's stronger exact Worker/build selection, an even
safer conservative policy is to avoid replacing a caller-validated improvement
at all unless there is a demonstrated sequencing reason.

In particular, `AI_improveBonus` has already checked `canBuild(kPlot, eBuild)`.
If that succeeds, no prerequisite feature-clearing action is required.

If SAS wants intentional chop-before-improvement behavior, that should be a
separate value decision that explicitly weighs the feature/chop yield and
preservation value; it should not masquerade as a prerequisite repair.

-------------------------------------------------------------------------------
CONTROLS CLOSED WHILE PROVING F377
-------------------------------------------------------------------------------

* The main current custom `AI_bestCityBuild` / `AI_improveCity` paths no longer
  pass their exact chosen build through `AI_betterPlotBuild`, specifically to
  avoid postprocessing replacing a scored/reserved Worker job. That prior
  repair is effective.

* `AI_improvePlot` still calls `AI_betterPlotBuild`, but the source itself
  labels `AI_improvePlot` as not currently used after K-Mod changes. It was not
  needed for the F377 live proof.

* The Forest/Camp proof does not depend on the legacy city best-build cache and
  is therefore independent of existing KI#213.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F377 confirmed in protected Queue 002.
F378 / provisional KI#701 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set remains repaired.
Latest physical checkpoint: C031-WIP69.
No repository source files modified.

NEXT
----
Continue from F378 unused.

Priority:
1. continue the whole-file predicate / boolean-inversion sweep, especially
   feature/area/domain replacements and post-selection overrides;
2. finish `AI_attackCityMove` / `AI_guardCity` / `AI_attackMove` group mutation
   and target-state contracts;
3. finish naval cargo/loading and upgrade/retreat transitions;
4. resume the remaining current-SAS-vs-Base changed-function reconciliation
   before any Queue-002 closure decision.
===============================================================================
C031-WIP70 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F378 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F377 remain CONFIRMED.
F378 / provisional KI#701 is CONFIRMED.
F379 / provisional KI#702 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F378 / provisional KI#701 — `AI_guardCity` selects the unit to detach using
the SOURCE plot's defensive value instead of the DESTINATION city it is being
sent to defend
-------------------------------------------------------------------------------

ROOT
----
After `AI_guardCity` searches for a city that needs reinforcement, it stores:

    pBestGuardPlot = pLoopCity->plot();

and the path/end-turn plot for movement toward that city.

Current SAS then chooses which member of the donor group to detach with:

    CvPlot* pSourcePlot = plot();
    ...
    CvUnit* pEjectedUnit =
            pOldGroup->AI_ejectBestDefender(pSourcePlot);

The detached unit is immediately assigned to `pBestGuardPlot`:

    pEjectedUnit->pushGroupMoveTo(
            *pEndTurnPlot, eFlags, false, false,
            eMissionAI, pBestGuardPlot);

Thus candidate selection asks "which unit defends the source plot best?" while
execution asks that selected unit to defend a different target city.

The selection helper is explicitly plot-sensitive.

`CvSelectionGroupAI::AI_ejectBestDefender(CvPlot* pDefendPlot)` begins with:

    int iValue = pUnit->AI_currEffectiveStr(pDefendPlot) * 20;

and, if the supplied plot is a city-defense plot:

    iValue *= 100 + pUnit->cityDefenseModifier();
    iValue /= 100;

`AI_currEffectiveStr(pDefendPlot)` itself reaches `CvUnit::maxCombatStr` and
therefore applies the supplied plot's:
* plot/city defense;
* city-defense modifier;
* hill defense;
* feature/terrain defense;
* other relevant defensive combat modifiers.

The public/group interface names the same argument `pTargetPlot`.

Therefore the plot argument is not an incidental non-null context; it controls
the exact ranking of which group member is the "best defender."

CURRENT-SAS RANK-REVERSAL PROOF
-------------------------------
Current XML provides a simple same-unit/same-level pair.

`UNIT_ARCHER`:
* `UNITCOMBAT_ARCHER_BOW_SHORT`;
* base combat 2;
* +50% city defense;
* 1 first strike + 1 chance first strike;
* accepts both Combat and City Garrison promotions.

`PROMOTION_COMBAT1`:
* +10% combat;
* valid for short-bow Archers.

`PROMOTION_CITY_GARRISON1`:
* +25% city defense;
* valid for short-bow Archers.

Take two otherwise-identical, full-health, same-level Archers in one donor
group:

    Archer A: Combat I
    Archer B: City Garrison I

On an ordinary flat non-city source plot, City Garrison contributes nothing to
`currEffectiveStr`, while Combat I contributes +10% general combat.

The remaining `AI_ejectBestDefender` factors are identical for these two
otherwise-identical units. Therefore the SOURCE-plot call prefers:

    Archer A (Combat I).

Now evaluate the same pair for the destination city.

Ignoring common city/terrain defense terms for clarity:

    Combat-I Archer:
        +50% built-in city defense
        +10% Combat

    CG-I Archer:
        +50% built-in city defense
        +25% City Garrison

So `AI_currEffectiveStr(destinationCity)` already favors City Garrison I.

The helper then applies ANOTHER explicit city-defense weighting:

    * (100 + cityDefenseModifier) / 100

which is:

    Combat-I Archer: * 150 / 100
    CG-I Archer:     * 175 / 100

The target-city ranking therefore favors:

    Archer B (City Garrison I).

This is a true rank reversal:
* source open terrain -> Combat I selected;
* target city -> City Garrison I selected.

The current call detaches the former and sends it to the city while leaving the
more city-specialized defender in the donor group.

The proof does not depend on unusual unique units, damage, or different unit
levels. Two copies of one ordinary current unit with one ordinary promotion
each suffice.

LIVE OFF-CITY PATH
------------------
`AI_guardCity` is not restricted to a group already standing in the target
city.

Its search explicitly requires a path to another city and stores a remote
`pBestGuardPlot`. Current SAS calls the search form from many mobile UnitAI
moves. In particular:

* `AI_attackMove` calls `AI_guardCity(false, true, 1)` for nearby colony/city
  defense;
* the SAS threatened-city response calls
  `AI_guardCity(false, true, 3, ..., bDangerOnly=true, bFillShortfall=true)`;
* the central continuation of a targeted `MISSIONAI_GUARD_CITY` calls the same
  search while the reinforcing group is between source and destination;
* the early city-to-city garrison rebalance can detach a defender from a donor
  group and send it several path turns away.

Therefore source != destination is an ordinary supported state, not a
hypothetical API misuse.

The current SAS `bFillShortfall` extension can repeat
`AI_ejectBestDefender(pSourcePlot)` several times, so a threatened-city rescue
can detach multiple units according to the wrong terrain/city context.

ANCESTRY
--------
The root predates SAS.

Civ4CE/BtS search-mode `AI_guardCity`:
* finds `pBestGuardPlot`;
* calls `getGroup()->AI_ejectBestDefender(pPlot)`, where `pPlot` is the current
  source plot;
* sends the returned unit toward the remote guard city.

K-Mod preserves the same source-plot call.

Base AdvCiv 1.14 preserves it as:

    AI_getGroup()->AI_ejectBestDefender(plot());

Current SAS names the source explicitly but preserves the inherited semantics:

    pOldGroup->AI_ejectBestDefender(pSourcePlot);

The selection helper itself has always treated its argument as the plot being
defended, and the interface calls it `pTargetPlot`.

Classification: inherited BtS/BBAI guard-city target-context defect.

WHY THIS IS NOT MERELY A HEURISTIC PREFERENCE
---------------------------------------------
The caller first makes a separate strategic decision about WHICH CITY needs a
defender. It then invokes a helper specifically designed to identify the
group's best defender for a supplied plot.

Passing the source plot causes that helper to answer a different question from
the mission that immediately follows.

This can systematically send:
* generic Combat-promoted units instead of City Garrison specialists from open
  terrain;
* hill/feature specialists favored by the source terrain instead of units best
  suited to the city;
* analogous source-specific defensive specialists whose bonuses disappear at
  the destination.

The distinction is particularly relevant to the SAS emergency shortfall mode,
whose purpose is explicitly to detach "suitable defenders" for a threatened
city.

REPAIR DIRECTION
----------------
Use the selected guard destination when ranking the detached defender:

    CvUnit* pEjectedUnit =
            pOldGroup->AI_ejectBestDefender(
                    const_cast<CvPlot*>(pBestGuardPlot));

or, preferably, make the helper parameter const-correct and pass
`pBestGuardPlot` directly.

When the group is already standing in the city being defended,
`pBestGuardPlot == source`, so this produces no behavioral change.

When reinforcing a remote city, the selected unit is then ranked for the plot
it is actually being sent to defend.

If SAS intentionally wants to consider the cost of weakening the donor/source
stack as well, that is a legitimate additional optimization, but it should be
modeled explicitly as a source-retention term rather than substituting the
source plot for the destination in a helper whose result is the unit sent away.

-------------------------------------------------------------------------------
CONTROL CLOSED — `AI_betterPlotBuild` "ROAD IMPROVED BONUSES"
-------------------------------------------------------------------------------

The separate post-F377 suspicion around this block was not promoted:

    if (doesImprovementConnectBonus(...))
        bNeedsRoad = false;

The comment's interpretation is misleading. K-Mod's
`doesImprovementConnectBonus` answers whether the improvement is a valid
bonus-trading improvement / acts as a city under the relevant technology; it
does not prove that the land plot has a route to the capital.

However, the current live disconnected-bonus path in `AI_improveBonus`
handles an already-correct resource improvement through its explicit route
branch and `AI_connectPlot`, rather than relying on this postprocessor block.

The other relevant land-cache path remains subsumed by existing KI#213, and
`AI_improvePlot` is documented as not currently used.

So the semantic/comment mismatch is retained as robustness/design debt, not
manufactured into an independent F379.

-------------------------------------------------------------------------------
CURRENT STONE
-------------------------------------------------------------------------------

Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F378 confirmed in protected Queue 002.
F379 / provisional KI#702 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set remains repaired.
Latest physical checkpoint: C031-WIP70.
No repository source files modified.

NEXT
----
Continue from F379 unused.

Priority:
1. continue `AI_guardCity` / `AI_attackCityMove` / `AI_attackMove` group-split
   and source-vs-target context checks;
2. inspect other helper calls that pass current/source plots into
   destination-scoring helpers;
3. finish naval cargo/loading and retreat/upgrade state transitions;
4. resume the residual current-SAS-vs-Base function reconciliation and final
   boolean/state/sentinel sweep before any Queue-002 closure decision.
===============================================================================
C031-WIP71 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F379 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F378 remain CONFIRMED.
F379 / provisional KI#702 is CONFIRMED.
F380 / provisional KI#703 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F379 / provisional KI#702 — SAS central guard-mission continuation treats every
targeted `MISSIONAI_GUARD_CITY` as an emergency/danger-only assignment, so the
older six-turn SAFE-city garrison rebalance can lose its destination after one
movement turn
-------------------------------------------------------------------------------

ROOT
----
Current SAS contains two independent targeted guard-city systems.

A) Early ordinary city-to-city garrison rebalance
-------------------------------------------------
Near the top of `CvUnitAI::AI_update`, during the early-game window, many land
combat UnitAI types in an overstocked owned city may call:

    AI_guardCity(
        false, true,
        /*iMaxPath*/ 6,
        MOVE_SAFE_TERRITORY | MOVE_AVOID_ENEMY_WEIGHT_3,
        /*iExtraDefenders*/ 2)

This call has:

    bDangerOnly = false
    bFillShortfall = false

by default.

Its explicit purpose is to push surplus defenders toward weaker cities even
when those cities are not currently endangered.

When a remote city is selected, `AI_guardCity` queues movement with:

    MISSIONAI_GUARD_CITY
    missionAIPlot = pBestGuardPlot

B) Later emergency/endangered-city continuation
-----------------------------------------------
Current SAS also added, before UnitAI-specific movement:

    if (getDomainType() == DOMAIN_LAND &&
        !isBarbarian() &&
        AI_getGroup()->AI_getMissionAIType() == MISSIONAI_GUARD_CITY &&
        AI_getGroup()->AI_getMissionAIPlot() != NULL &&
        AI_guardCity(
            false, true, 3,
            MOVE_AVOID_ENEMY_WEIGHT_2,
            0,
            /*bDangerOnly*/ true,
            /*bFillShortfall*/ true))
    {
        return false;
    }

The comment correctly describes its original purpose:

    "Targeted emergency city reinforcements ..."

but the outer condition does not identify an emergency assignment.

It catches EVERY targeted `MISSIONAI_GUARD_CITY`, including the older safe-city
rebalance mission.

The continuation call then hardcodes:

    bDangerOnly = true

and `AI_guardCity` immediately excludes each safe city with:

    if (bDangerOnly && !pLoopCity->AI_isDanger())
        continue;

Therefore a still-valid SAFE rebalance target cannot be continued by this
central mission-preservation hook.

CONCRETE CURRENT-SAS FAILURE
----------------------------
A straightforward current path uses `UNITAI_ATTACK`.

Assume:
* early-game rebalance window is active;
* City A has more defend-capable units than its own minimum;
* City B is safe but underdefended enough to win the rebalance score;
* an ordinary one-move Archer/other `UNITAI_ATTACK` unit can reach B in six
  path turns over safe unroaded territory.

Turn N:
1. Unit is in City A.
2. early rebalance runs before `AI_attackMove`;
3. `AI_guardCity(... iMaxPath=6, iExtraDefenders=2)` selects City B;
4. the unit/group receives targeted `MISSIONAI_GUARD_CITY` for City B;
5. movement goes only to that turn's path end.

Turn N+1:
1. the group still carries a targeted `MISSIONAI_GUARD_CITY`;
2. City B is still safe and still underdefended;
3. central continuation recognizes the mission type/target;
4. it calls `AI_guardCity(... bDangerOnly=true ...)`;
5. City B is rejected solely because `AI_isDanger()==false`;
6. with no endangered replacement city, the continuation returns false;
7. normal `AI_attackMove` now runs.

The ordinary `UNITAI_ATTACK` guard searches do not preserve the original
six-turn reach:
* nearby guard search: 1 turn;
* colony/search paths: 3 turns in relevant branches;
* later general guard search: 3 turns.

After one ordinary movement turn from an initial six-turn target, an unroaded
one-move unit can still be five turns away.

Therefore the exact still-valid City-B assignment cannot be rediscovered by
those searches. The unit proceeds into offensive/grouping/other role logic and
can overwrite/abandon the previous guard mission.

The nominal six-turn rebalance radius consequently does not imply a six-turn
commitment for `UNITAI_ATTACK` and several other donor roles.

This is not limited to Attack units. The early exporter includes:

    UNITAI_ATTACK
    UNITAI_ATTACK_CITY
    UNITAI_COLLATERAL
    UNITAI_PILLAGE
    UNITAI_RESERVE
    UNITAI_COUNTER
    UNITAI_CITY_DEFENSE
    UNITAI_CITY_COUNTER
    UNITAI_CITY_SPECIAL

Their ordinary guard-search ranges differ. Some, such as City Defense, can
eventually perform broad searches and may rediscover the target; others use
shorter ranges and can lose a long-distance safe-city assignment.

The defect is therefore role-dependent, which also makes the apparent
six-turn rebalance behavior inconsistent across otherwise eligible donors.

WHY MISSIONAI STATE DOES NOT SAVE IT
------------------------------------
The queued movement is to the current turn's `pEndTurnPlot`, not an indefinitely
executing full multi-turn path. On the next AI update the role logic can choose
again.

The new practical-6049 central continuation exists precisely because targeted
emergency defenders were observed to "immediately rejoin offensive groups and
receive another assignment before reaching their city." That is direct history
evidence that carrying `MISSIONAI_GUARD_CITY` alone does not force completion.

The same lifecycle applies to the older targeted safe-city rebalance.

ANCESTRY / INTERACTION HISTORY
------------------------------
This is SAS-local.

Practical commit 4953
    `0704fd52d2...`

introduced the early city-to-city rebalance in `AI_update`, including the
six-turn search and ordinary `AI_guardCity` semantics.

Practical commit 6049
    `c89861b618e8512ea2731d1cfd832b33ec060970`

later fixed KI#188.2 emergency reinforcement persistence by adding the central
targeted-guard continuation. Its commit title is:

    "(Fixed/Improved) Base AdvCiv/K-Mod issue: offensive armies could ignore
     nearby endangered cities instead of going back to try to help defending
     them"

and its new comment explicitly scopes the hook to "Targeted emergency city
reinforcements."

However, the code distinguishes targeted vs targetless guard missions, not
emergency vs ordinary targeted guard missions. The older 4953 rebalance also
uses a target plot, so it is intercepted by 6049's emergency-only continuation
and then rejected when its city is safe.

Classification: SAS-local interaction/state-lifetime regression between the
ordinary garrison-rebalance feature and the later emergency continuation.

CONSEQUENCE
-----------
The AI can:
* export a surplus defender toward a safe weak city;
* spend one turn moving it there;
* discard the still-valid assignment on the following turn;
* resume offensive/grouping work before reaching the city.

This weakens the exact purpose of the early rebalance: spreading real existing
defensive capacity from overstocked core cities toward weak secondary cities.

It also wastes movement turns and can create role-dependent "starts to
reinforce, then changes its mind" behavior.

REPAIR DIRECTION
----------------
Preserve the semantic type of the targeted guard assignment.

Several conservative designs are possible.

1. Distinguish emergency targeted guard missions from ordinary guard-city
   rebalance missions with an explicit mission/state marker, then:
   * emergency -> continue with `bDangerOnly=true`, `bFillShortfall=true`,
     3-turn emergency policy;
   * ordinary rebalance -> continue toward its existing safe underdefended
     target under the original rebalance policy/range.

2. If adding a state bit is undesirable, inspect the existing mission target
   first:
   * if it is endangered, use the emergency continuation;
   * otherwise validate that the same target still needs the ordinary garrison
     reinforcement and continue toward that exact target rather than globally
     rerunning danger-only search.

3. Alternatively make all targeted guard-city movement directly persistent
   until its target becomes invalid/satisfied, with the target's current
   shortage/danger deciding when to release it.

Do not simply change the central call to `bDangerOnly=false` while retaining
`bFillShortfall=true`; that would merge two policies and could cause ordinary
safe-city shortages to detach emergency-sized fractions of offensive groups.

-------------------------------------------------------------------------------
CONTROLS / RELATED BEHAVIOR
-------------------------------------------------------------------------------

* The existing central continuation correctly improves the practical-6049
  emergency case. F379 does not argue that it should be removed.

* City-Defense donors often have broader ordinary guard fallbacks and can
  rediscover a safe destination; this does not repair the cross-role contract
  because the early exporter intentionally includes many UnitAI types whose
  normal ranges are much shorter than six turns.

* A safe target becoming genuinely endangered naturally transitions into the
  emergency continuation and is then preserved. The defect concerns the
  ordinary, still-needed, still-safe garrison redistribution that practical
  4953 explicitly introduced.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F379 confirmed in protected Queue 002.
F380 / provisional KI#703 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set remains repaired.
Latest physical checkpoint: C031-WIP71.
No repository source files modified.

NEXT
----
Continue from F380 unused.

Priority:
1. finish the rest of the current `AI_update` mission-preservation/state
   interaction review;
2. continue `AI_attackCityMove` / `AI_attackMove` target/group state;
3. finish naval cargo/loading, retreat and upgrade transitions;
4. run the residual current-SAS-vs-Base changed-function reconciliation and
   predicate/sentinel sweep before considering Queue-002 closure.
===============================================================================
C031-WIP72 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F380 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F379 remain CONFIRMED.
F380 / provisional KI#703 is CONFIRMED.
F381 / provisional KI#704 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F380 / provisional KI#703 — SAS early garrison rebalance says it counts every
defend-capable unit but actually counts only defend-capable GROUP HEADS, so a
large stacked military concentration can fail the source-surplus gate
-------------------------------------------------------------------------------

ROOT
----
Current SAS practical-4953 city-to-city garrison redistribution contains:

    // Count any unit that can defend, not just GUARD_CITY mission
    const int iHave =
        pHere->getPlot().plotCount(
            PUF_canDefendGroupHead,
            -1, -1, getOwner());

    const int iMinHere = pHere->AI_minDefenders();

    if (iHave > iMinHere)
    {
        if (AI_guardCity(...))
            ...
    }

The implementation does not match the stated population.

`CvGameCoreUtils.cpp` defines:

    bool PUF_canDefendGroupHead(CvUnit const* pUnit, ...)
    {
        return (PUF_canDefend(pUnit, ...) &&
                PUF_isGroupHead(pUnit, ...));
    }

Therefore the source count is:

    number of defend-capable SELECTION GROUP HEADS

not:

    number of defend-capable UNITS.

A multi-unit selection group contributes exactly one to `iHave`.

WHY GROUP-HEAD COUNTING IS WRONG FOR THIS SAS ACTION
----------------------------------------------------
The rebalance is not about moving an indivisible group.

When it decides the city has surplus, it calls `AI_guardCity`. That function
uses:

    AI_ejectBestDefender(...)

which can detach one selected unit from a multi-unit donor group and send only
that unit toward the weak city.

Current SAS's threat-shortfall extension can detach multiple selected
defenders, but the ordinary early rebalance still deliberately supports
splitting one defender away while leaving the rest of the donor group intact.

Thus the source-surplus test treats a ten-unit stack as one indivisible
defensive object, while the action it gates is explicitly capable of treating
that stack as ten individual defender candidates and exporting only one.

CONCRETE FAILURE SHAPE
----------------------
Suppose City A has:

    one 8-unit defend-capable ATTACK stack
    one separate CITY_DEFENSE garrison group

and:

    AI_minDefenders(City A) == 2.

Actual defend-capable land units physically available in the city:

    9

Current practical-4953 `iHave`:

    2 group heads

so:

    iHave > iMinHere
    2 > 2
    false

The early redistribution does not even call `AI_guardCity`.

Yet the intended action could safely detach one suitable defender from the
8-unit stack and still leave seven members of that stack plus the dedicated
garrison in City A.

The mismatch becomes even sharper for one large stack plus one minimum
garrison: adding more and more members to the same stack never increases
`iHave` at all.

This directly undercuts the feature's documented motivation: recognizing
overstocked cities and exporting their excess military to weaker cities.
Large ATTACK/ATTACK_CITY/RESERVE/COUNTER stacks are exactly the type of
concentration most likely to be grouped.

INTRODUCING HISTORY
-------------------
Practical commit 4953:

    0704fd52d2...

introduced both the comment and the predicate together:

    // Count any unit that can defend, not just GUARD_CITY mission
    plotCount(PUF_canDefendGroupHead, ...)

So this is not an inherited BtS/K-Mod convention accidentally exposed later.
The new SAS feature stated an individual-unit contract while selecting the
older group-head predicate.

Classification: SAS-local garrison-rebalance counting regression.

RELATION TO F379
----------------
F379 is the later state-lifetime defect: a long-range safe-city rebalance
mission can be abandoned after one movement turn because the practical-6049
central continuation only preserves endangered-city assignments.

F380 happens BEFORE an assignment is created. It can prevent the source city
from being considered surplus at all when its excess military is stacked.

The two therefore independently reduce the same feature:
* F380: some valid donors never dispatch;
* F379: some dispatched safe reinforcements do not persist.

REPAIR DIRECTION
----------------
Count individual defenders that are actually available to detach.

A naive:

    plotCount(PUF_canDefend, ...)

matches the unit-vs-group dimension, but the final repair should explicitly
consider cargo and any other units physically represented on the plot but not
available as city defenders/donors.

The safest implementation is likely a small explicit unit loop with conditions
such as:

    owner == getOwner()
    canDefend()
    !isCargo()

plus any intended immobility/special-unit exclusions.

Alternatively add/use a precise predicate for "defend-capable, non-cargo
individual unit."

Do NOT change the general semantics of `PUF_canDefendGroupHead`; many inherited
callers deliberately use group-head counting for mission/group accounting.
The defect is the practical-4953 choice of that predicate for this new
individual-unit surplus feature.

-------------------------------------------------------------------------------
ADDITIONAL CONTROL
-------------------------------------------------------------------------------

The broader inherited `AI_guardCity` logic also uses
`PUF_canDefendGroupHead` in several places. That is older mission/group
accounting and was not automatically folded into F380.

F380 is deliberately scoped to the new SAS source-surplus gate because:
* its own comment explicitly says "any unit";
* its purpose is to detect military concentration;
* its subsequent action can split one unit out of a group.

No claim is made here that every historical group-head defender count should be
converted to an individual-unit count.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F380 confirmed in protected Queue 002.
F381 / provisional KI#704 UNUSED.
Existing KI#213 retained/reconfirmed.
Old F002/KI#331 Queue-002 caller set remains repaired.
Latest physical checkpoint: C031-WIP72.
No repository source files modified.

NEXT
----
Continue from F381 unused.

Priority:
1. continue `AI_update` and garrison/mission lifecycle reconciliation after
   practical 4953/6049;
2. return to `AI_attackCityMove` and group-target state after the guard-city
   family is saturated;
3. finish naval cargo/loading, retreat and upgrade-state transitions;
4. continue residual current-vs-Base function coverage and final predicate /
   sentinel / source-vs-target sweeps.
===============================================================================
C031-WIP73 — PROTECTED QUEUE 002 CvUnitAI.cpp — F380 RETRACTED AFTER
DOWNSTREAM-CONSUMER PROOF
===============================================================================

STATUS CORRECTION
-----------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

F375-F379 remain CONFIRMED.
The provisional F380 / KI#703 claim in C031-WIP72 is RETRACTED.
F380 / provisional KI#703 is UNUSED again.
Unique files complete remains 31/169.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
WHY WIP72'S OUTER-GATE F380 DOES NOT SURVIVE AS AN INDEPENDENT ROOT
-------------------------------------------------------------------------------

WIP72 correctly established this local mismatch in practical-4953's early
garrison rebalance:

    // Count any unit that can defend, not just GUARD_CITY mission
    const int iHave =
        pHere->getPlot().plotCount(PUF_canDefendGroupHead, ...);

and correctly established that `PUF_canDefendGroupHead` counts only
defend-capable GROUP HEADS.

However, the deeper downstream proof found that changing this OUTER gate alone
does not make the clean stacked-donor example dispatch a unit.

Immediately after the gate, the feature calls:

    AI_guardCity(false, true, 6, ..., iExtraDefenders)

and `AI_guardCity` performs its own source-city retention check:

    int const iHave =
        kPlot.plotCount(
            PUF_canDefendGroupHead,
            ...,
            AI_isCityAIType() ? PUF_isCityAIType : NULL)
        - 1;

    int iNeed = pCity->AI_neededDefenders() + iExtra;

    if (iHave < iNeed)
    {
        bSearch = false;
        pBestGuardPlot = &kPlot;
        ...
    }

For the WIP72 example:

    one large ATTACK stack
    + one dedicated garrison group
    AI_minDefenders / needed defenders ~= 2

the outer practical-4953 gate counts two group heads and rejects the call.

If that outer gate were changed to count all nine individual defenders, the
call would proceed -- but the inner inherited `AI_guardCity` check would count
the two group heads, subtract the current group head, see only one remaining
defender-group against a need of about two, and force the current group to stay
in the source city.

Therefore the WIP72 outer predicate is genuinely inconsistent with its comment,
but the clean demonstrated behavior is already blocked by the downstream
`AI_guardCity` contract.

Under the audit's root standard, that means the outer-only issue is not an
independent F380.

-------------------------------------------------------------------------------
BROADER INHERITED QUESTION — DELIBERATELY REOPENED, NOT YET NUMBERED
-------------------------------------------------------------------------------

The downstream result exposes a potentially more fundamental inherited
question:

`AI_guardCity` compares group-head counts against `AI_neededDefenders` and, for
the current source group, effectively removes that entire group from the
available-defender count even though successful remote guard assignment later
calls:

    AI_ejectBestDefender(...)

and sends only ONE selected unit from the group.

For a multi-unit donor group, this can be very conservative: the source check
can behave as though all members of the current group leave even though
N-1 members remain after one-unit ejection.

K-Mod's rewritten `AI_guardCity` already contains the group-head accounting,
and Base AdvCiv/current SAS inherit it. K-Mod's city-level
`AI_isDefended`/AdvCiv `AI_countExcessDefenders` also use group-head counting,
so this may reflect a broader deliberate "defensive groups, not units" model
rather than a simple typo.

That broader question therefore needs:
* source-city multi-unit group proof;
* comparison with intended city-defense/group semantics;
* caller behavior when the ejected unit is or is not the group head;
* confirmation that individual remaining group members really remain usable
  city defenders after the split;
* ancestry/design comments.

Until that survives, do NOT relabel it F380 merely to preserve the number.

-------------------------------------------------------------------------------
AUDIT METHODOLOGY NOTE
-------------------------------------------------------------------------------

This retraction is intentional evidence that the deeper audit is not treating
every local mismatch as a finding.

The provisional claim passed:
* local source inspection;
* explicit predicate-definition proof;
* feature-comment comparison;

but failed the stronger:
* "would repairing this root change supported runtime behavior?"
downstream-consumer test.

That is sufficient reason to remove it from the confirmed set.

C031-WIP72 remains historical investigation context only; its F380 section is
superseded by this correction.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F379 CONFIRMED in protected Queue 002.
F380 / provisional KI#703 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP73.
No repository source files modified.

NEXT
----
1. Decide the broader inherited `AI_guardCity` group-head-vs-one-unit-ejection
   question on its own merits; promote only if its group/city contract is hard.
2. Continue `AI_attackCityMove` / target/group state.
3. Continue naval cargo/loading, retreat/upgrade transitions.
4. Resume current-vs-Base function reconciliation and generic predicate /
   sentinel / source-target sweeps.
===============================================================================
C031-WIP74 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F380 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F379 remain CONFIRMED.
F380 / provisional KI#703 is CONFIRMED.
F381 / provisional KI#704 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

NOTE ON NUMBERING
-----------------
C031-WIP72 contained a provisional F380 about the practical-4953 garrison
surplus predicate. C031-WIP73 explicitly RETRACTED that claim after the
downstream `AI_guardCity` consumer proof. The F380 number was therefore unused
again.

This WIP allocates F380 to the independent air-base defect below. WIP73 remains
the authoritative retraction of the older provisional claim.

-------------------------------------------------------------------------------
F380 / provisional KI#703 — AdvCiv SquareIterator rewrite dropped `!` from the
enemy/vassal test in `AI_airOffenseBaseValue`, so enemy border danger is
ignored while friendly vassal territory is treated as dangerous
-------------------------------------------------------------------------------

ROOT
----
Current SAS / Base AdvCiv 1.14:

    // Consider threat from nearby enemy territory
    int iBorderDanger = 0;
    for (SquareIter it(kPlot, 1); it.hasNext(); ++it)
    {
        CvPlot const& p = *it;
        if (p.sameArea(kPlot) && p.isOwned())
        {
            if (!p.isRevealed(getTeam()))
                continue;

            if (p.getTeam() != getTeam() &&
                GET_TEAM(p.getTeam()).isVassal(getTeam()))
            {
                int const iDistance = it.currStepDist();

                if (iDistance == 1)
                    iBorderDanger++;

                if (::atWar(p.getTeam(), getTeam()))
                {
                    if (iDistance == 1)
                        iBorderDanger += 2;
                    ...
                }
            }
        }
    }

    iDefenders -= std::min(2, (iBorderDanger + 1) / 3);

The comment says this is "threat from nearby enemy territory."

But the condition enters only when the neighboring team IS a vassal of our
team:

    GET_TEAM(p.getTeam()).isVassal(getTeam())

That is the opposite of the inherited test.

K-Mod:

    if (pLoopPlot->getTeam() != getTeam() &&
        !(GET_TEAM(pLoopPlot->getTeam()).isVassal(getTeam())))

Pre-regression AdvCiv likewise had:

    if (pLoopPlot->getTeam() != getTeam() &&
        !GET_TEAM(pLoopPlot->getTeam()).isVassal(getTeam()))

Thus the intended population is:
    other-team territory that is NOT our vassal

whereas current AdvCiv/SAS uses:
    other-team territory that IS our vassal.

Two semantic inversions follow immediately.

A) Ordinary hostile/non-vassal borders contribute zero
-------------------------------------------------------
A normal enemy team at war with us is not our vassal.

Therefore current code fails the vassal predicate before it can execute either:

    iBorderDanger++;

or the stronger wartime:

    iBorderDanger += 2;

So the dedicated nearby-enemy-territory risk term ignores the normal enemy
border case it was written to measure.

B) Friendly vassal borders contribute danger
---------------------------------------------
A vassal of our team satisfies the current predicate and therefore contributes
the basic adjacency danger:

    if (iDistance == 1)
        iBorderDanger++;

The nested:

    atWar(vassalTeam, ourTeam)

branch is normally contradictory to the vassal relationship itself and is a
useful diagnostic sign of the lost negation. The meaningful current effect is
the basic false-positive border penalty on friendly-vassal territory.

LIVE CONSEQUENCE
----------------
`AI_airOffenseBaseValue` is a live magnitude selector for offensive air-unit
basing.

`AI_airOffensiveCity` scans revealed air bases, calls:

    int iValue = AI_airOffenseBaseValue(kPlot);

and moves to the highest-valued base.

Inside `AI_airOffenseBaseValue`, `iBorderDanger` directly reduces the effective
land-defender count:

    iDefenders -= std::min(2, (iBorderDanger + 1) / 3);

The function then rejects a base entirely when:

    if (iAttackAirCount >= iDefenders || iDefenders <= 0)
        return 0;

and later scales its offensive value by local defense versus enemy offense.

So the lost negation can affect both:
* whether a candidate air base is considered usable at all;
* its value relative to safer alternative bases.

A wartime adjacent-enemy example is particularly direct.

Under K-Mod / correct logic, one adjacent at-war enemy-owned plot contributes:

    +1 basic foreign-border danger
    +2 wartime adjacency danger
    = 3

and therefore:

    (3 + 1) / 3 = 1

effective defender is removed.

Current AdvCiv/SAS sees the enemy as a non-vassal, fails the predicate, and
removes zero defenders from this term.

Conversely, enough adjacent friendly-vassal plots can lower the effective
defender count even though they are not enemy territory.

This is not merely a stale comment: the predicate reversal changes actual air
relocation/base ranking.

ANCESTRY / INTRODUCING CHANGE
-----------------------------
Civ4/K-Mod has the negated vassal test.

Reachable AdvCiv history shows the negation still present immediately before
practical commit:

    practical commit 1916
    085ab2c9b697b5690ba398ef31899cb27e2a8a77
    Title: `"SquareIterator" for loops over tiles within movement range`

That commit mechanically rewrote the old nested coordinate loops into
`SquareIter`.

The exact diff changes:

    if (pLoopPlot->getTeam() != getTeam() &&
        !GET_TEAM(pLoopPlot->getTeam()).isVassal(getTeam()))

to:

    if (p.getTeam() != getTeam() &&
        GET_TEAM(p.getTeam()).isVassal(getTeam()))

The only semantic change needed to create the defect is the missing `!`; it is
not part of the iterator abstraction itself.

Base AdvCiv 1.14 retains that regression, and current SAS inherits it
unchanged.

Classification:
    inherited AdvCiv iterator-refactor regression.

REPAIR DIRECTION
----------------
Restore the negation:

    if (p.getTeam() != getTeam() &&
        !GET_TEAM(p.getTeam()).isVassal(getTeam()))

This matches:
* the function's "enemy territory" comment;
* K-Mod;
* pre-1916 AdvCiv;
* the nested wartime branch.

A later cleanup could separately review whether current diplomacy/team
relations warrant treating other friendly relationships similarly, but that is
policy beyond the regression repair.

-------------------------------------------------------------------------------
CONTROL CLOSED — RETRACTED WIP72 GARRISON SURPLUS CLAIM
-------------------------------------------------------------------------------

The broader inherited `AI_guardCity` group-head accounting question was also
finished after WIP73.

Although `AI_guardCity` may eject only one defender toward a remote city, the
N-1 members remaining in the donor group are not thereby committed to defend
the source city. The remainder selection group continues its own UnitAI logic
and can leave on the same/later update.

BtS already uses the same lifecycle principle: when a city actually needs one
unit to stay, `AI_guardCity` ejects a defender into a separate staying group
while leaving the remainder group free to continue.

Therefore source retention expressed in group-head / current-group terms is a
conservative group-lifecycle model, not a demonstrated individual-unit-count
bug.

F380 remains correctly retracted for the WIP72 claim and is allocated only to
the independent air-base regression above.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F380 CONFIRMED in protected Queue 002.
F381 / provisional KI#704 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP74.
No repository source files modified.

NEXT
----
Continue from F381 unused.

Priority:
1. finish `AI_attackCityMove` / `AI_pickTargetCity` current target/deadline
   contracts, including launched-Space-capital timing without assuming turn
   order;
2. finish air/naval/transport helper predicate reconciliation now that the
   inherited air-base typo has surfaced;
3. finish upgrade/retreat/cargo state transitions;
4. continue residual current-vs-Base function coverage and final
   source-vs-target / boolean / sentinel sweeps before Queue-002 closure.
===============================================================================
C031-WIP75 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F381 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F380 remain CONFIRMED.
F381 / provisional KI#704 is CONFIRMED.
F382 / provisional KI#705 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F381 / provisional KI#704 — K-Mod `AI_load` scans an enemy coastal target but
tests transport pathing to water beside the PASSENGER instead of beside that
enemy target, breaking the long-distance boat-shortcut test
-------------------------------------------------------------------------------

ROOT
----
`CvUnitAI::AI_load` has a special `iMaxTransportPath` check whose own comment
states the contract:

    // Can transport reach enemy in requested time

The current code first selects a transport `pBestUnit`, initializes a transport
pathfinder from that transport group, and scans plots around the TRANSPORT:

    for (SquareIter it(*pBestUnit,
        iMaxTransportPath * pBestUnit->baseMoves());
        !bFoundEnemyPlotInRange && it.hasNext(); ++it)
    {
        CvPlot const& p = *it;

        if (p.isCoastalLand() && p.isOwned() &&
            kOurTeam.AI_mayAttack(p.getTeam()) &&
            p.getArea().getCitiesPerPlayer(p.getOwner()) > 0)
        {
            ...
        }
    }

So `p` is the candidate enemy coastal land plot whose reachability should be
tested.

But inside that exact branch current SAS does:

    FOR_EACH_ADJ_PLOT(getPlot())
    {
        if (!pAdj->isWater())
            continue;

        if (tempFinder.generatePath(*pAdj))
        {
            ...
            if (iPathTurns <= iMaxTransportPath)
                bFoundEnemyPlotInRange = true;
        }
    }

`getPlot()` here is the CURRENT PASSENGER UNIT'S plot (`this`), not the scanned
enemy plot `p`.

Therefore the algorithm asks:

    "Does the selected transport have a short path to water beside the
     passenger's current location?"

while claiming to answer:

    "Can the selected transport reach the scanned enemy coast in time?"

The candidate enemy plot is used only as a boolean existence trigger. Its
actual adjacent sea tiles are never path-tested.

FALSE-NEGATIVE CLASS — INLAND ATTACK STACK
------------------------------------------
This produces a deterministic failure for the most useful boat-shortcut shape.

If the attack stack currently stands on inland land with no adjacent water:

    FOR_EACH_ADJ_PLOT(getPlot())

contains no water tile.

Therefore `bFoundEnemyPlotInRange` stays false regardless of:
* which assault transport `AI_findTransport` selected;
* whether the passenger can walk to that transport within `iMaxPath`;
* whether that transport can reach the target enemy coast within
  `iMaxTransportPath`.

The function then executes:

    if (!bFoundEnemyPlotInRange)
        return false;

and refuses the loading plan.

This is not rescued by `AI_findTransport`.

`AI_findTransport` independently verifies that the passenger can reach the
transport within its allowed loading path:

    generatePath(pTransport->getPlot(), eFlags, true,
        &iPathTurns, iMaxPath)

A land stack can therefore have a valid short walk to a coastal transport even
when its CURRENT tile itself has no adjacent water. The later
`iMaxTransportPath` branch throws that valid candidate away solely because it
looks for water around the passenger's old location.

LIVE CURRENT CALLER
-------------------
Current SAS has one genuine external call that supplies finite
`iMaxTransportPath`: `AI_attackCityMove`.

After selecting `pTargetCity`, it first estimates the ordinary land route:

    generatePath(pTargetCity->getPlot(), eMoveFlags, true, &iPathTurns)

When that route is longer than five turns, it tries to see whether a boat is
faster:

    int iLoadTurns =
        std::max(3, iPathTurns / 3 - 1);

    int iMaxTransportTurns =
        iPathTurns - iLoadTurns - 2;

    if (AI_load(
        UNITAI_ASSAULT_SEA,
        MISSIONAI_LOAD_ASSAULT,
        NO_UNITAI,
        -1, -1, -1, -1,
        eMoveFlags,
        iLoadTurns,
        iMaxTransportTurns))
    {
        return;
    }

A simple supported geometry is:

    land path to target city = 12 turns
    allowed walk/load time   = 3 turns
    allowed transport time   = 7 turns

Suppose:
* the attacking stack is currently one or two inland tiles from the coast;
* a valid assault transport is reachable by the stack within those three
  loading turns;
* after pickup, that transport can reach water adjacent to the enemy target
  coast within seven turns.

That is precisely the type of route the caller is trying to discover.

`AI_findTransport` can select the transport because the land group can reach it
within `iLoadTurns`.

F381 then rejects it deterministically if the attacker's CURRENT plot itself
has no adjacent water. The actual enemy coast is never passed to the transport
pathfinder.

FALSE-POSITIVE CLASS — WRONG COAST / SEA ACCESS
-----------------------------------------------
The same source-vs-target error can also accept a route it did not prove.

For a passenger already on coastal land:
* the scan can encounter an enemy coastal plot `p` within the transport's
  geometric SquareIterator radius;
* instead of testing water adjacent to `p`, the code tests water adjacent to
  the passenger;
* the selected transport is usually already near/reachable from that passenger
  coast, so this local path can succeed quickly.

`bFoundEnemyPlotInRange` can therefore become true without any path to the
scanned enemy coast having been established.

This matters around:
* separate seas;
* peninsulas/isthmuses;
* blocked water routes;
* other geometries where geometric proximity does not imply transport-path
  reachability.

The temp pathfinder was added specifically to distinguish actual transport
reachability from geometric range, but the final destination supplied to it is
the wrong end of the trip.

ANCESTRY
--------
BtS/Civ4CE does not have this `iMaxTransportPath` mechanism in `AI_load`.

K-Mod adds the extra transport-range parameter and the long-distance
`AI_attackCityMove` loading calculation.

K-Mod correctly fixed one nearby BtS error by centering the enemy search on the
selected transport:

    // BtS had used plotXY(getX(), getY(), ...)
    pLoopPlot = plotXY(
        pBestUnit->getX(), pBestUnit->getY(), ...);

However, once a candidate enemy coastal `pLoopPlot` is found, K-Mod still uses:

    pAdjacentPlot =
        plotDirection(getX(), getY(), ...);

rather than coordinates from `pLoopPlot`.

Base AdvCiv preserves that source/target mix-up while converting the outer scan
to `SquareIter`.

Current SAS inherits it unchanged.

Classification:
    inherited K-Mod/BBAI transport-target context bug.

WHY THIS IS NOT A MERE HEURISTIC APPROXIMATION
----------------------------------------------
The code already:
* identifies a specific candidate enemy coastal plot;
* creates a real group pathfinder for the selected transport;
* gives that pathfinder the exact requested maximum transport turns.

There is no approximation requirement forcing it to use the passenger's
location. Passing the passenger-adjacent water simply asks the pathfinder the
wrong spatial question.

The function's comment, caller arithmetic and temporary pathfinder all agree
that the destination should represent the enemy coast.

CONSEQUENCE
-----------
Current SAS can:
* miss faster naval shortcuts for inland attack stacks;
* force a long overland march despite a nearby assault transport and reachable
  enemy coast;
* accept geometrically-near enemy coast candidates whose actual water approach
  was never path-checked;
* make the K-Mod "boat faster than walking" branch strongly dependent on
  whether the stack happens to be standing directly on the coast at the moment
  of evaluation.

This can also help explain route-selection inconsistency where otherwise
similar attack stacks choose sea transport from coastal staging tiles but fail
to do so from nearby inland tiles.

REPAIR DIRECTION
----------------
Path-test water adjacent to the candidate enemy coastal plot `p`, not the
passenger:

    FOR_EACH_ADJ_PLOT(p)
    {
        if (!pAdj->isWater())
            continue;

        if (tempFinder.generatePath(*pAdj))
        {
            int iPathTurns = tempFinder.getPathTurns();
            if (tempFinder.getFinalMoves() == 0)
                iPathTurns++;

            if (iPathTurns <= iMaxTransportPath)
            {
                bFoundEnemyPlotInRange = true;
                break;
            }
        }
    }

The exact macro syntax may need the local const/non-const form supported by the
codebase, but the semantic target must be `p`.

That single change fixes both directions:
* inland passengers no longer require water beside themselves;
* coastal passengers no longer prove enemy reachability by pathing only back
  toward their own coast.

A stronger future cleanup could pass `pTargetCity` / an explicit intended
transport destination from `AI_attackCityMove` rather than scanning for any
attackable coastal city in range. That is a separate precision improvement;
the minimum F381 repair is simply to path toward the candidate enemy coast the
current helper already selected.

-------------------------------------------------------------------------------
CONTROL CLOSED — AI_travelToUpgradeCity ALTERNATIVE-UPGRADE CONCERN
-------------------------------------------------------------------------------

The current SAS KI#188.3.3 repair asks `getUpgradeCity(true, &eUpgradeUnit)` for
one selected upgrade and then rejects travel if that exact upgrade fails the
real economic `canUpgrade` checks.

Because `getUpgradeCity` can return an early close candidate, it was reasonable
to test whether:
    first upgrade fails ROI
    later upgrade passes ROI
could make the new gate return false without trying the valid alternative.

A tempting current-data branch is:

    Grenadier
        -> Bazooka
        -> Machine Gun

At 0 XP under current SAS prices/ROI gates, Bazooka can be expensive enough to
fail while Machine Gun can be cheap/strong enough to pass.

However the LIVE caller/UnitAI contract kills that example.

Land movement roles that call `AI_travelToUpgradeCity` include:
    UNITAI_ATTACK
    UNITAI_PILLAGE
    UNITAI_EXPLORE

`UNIT_MACHINE_GUN` has:
    bOnlyDefensive = 1

and therefore is not a valid positive-value ATTACK/PILLAGE alternative in
`AI_unitValue` for the relevant travel path.

The defensive Grenadier roles for which Machine Gun is meaningful
(CITY_DEFENSE/CITY_COUNTER etc.) do not use the same travel helper.

Other current branching upgrade chains inspected so far either:
* have equal/similar alternatives;
* present the cheaper/better candidate first;
* or fail to provide two live alternatives for one role that actually calls
  `AI_travelToUpgradeCity`.

So the structural completeness concern remains worth remembering if future XML
adds another branching upgrade graph, but no supported current F382 was
established.

-------------------------------------------------------------------------------
CONTROL CLOSED — LAUNCHED SPACE CAPITAL TARGET
-------------------------------------------------------------------------------

The unfinished C031-WIP74 launched-Space-capital concern was also closed.

`CvCity::kill` records whether the destroyed city was the owner's current
capital. After city removal / capital reassignment, if it was the capital it
calls:

    GET_TEAM(eOwner).resetVictoryProgress();

`CvTeam::resetVictoryProgress` clears active victory countdowns and removes the
team's victory projects.

Thus capturing/destroying ANY team member's current capital resets that team's
Space-victory progress at the game-engine boundary.

`AI_pickTargetCity` scans enemy players' current capitals while consulting the
target TEAM's active Space victory countdown, matching that team-wide reset
contract.

The `AI_attackCityMove` emergency check:

    iPathTurns <= iSpaceVictoryCountdown

and its "must attack now" boundary:

    iPathTurns >= iSpaceVictoryCountdown

were traced against the ordinary countdown decrement / victory resolution
lifecycle. No supported off-by-one was established.

No F382 allocated there.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F381 CONFIRMED in protected Queue 002.
F382 / provisional KI#705 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP75.
No repository source files modified.

NEXT
----
Continue from F382 unused.

Priority:
1. continue naval/transport target-context review around `AI_load`,
   `AI_assaultSeaTransport`, `AI_transportGoTo`, pickup and unloading;
2. inspect the remaining upgrade/retreat/cargo transitions after closing the
   alternative-upgrade candidate;
3. resume the current-SAS-vs-Base changed-function reconciliation;
4. run another source-vs-target, boolean-negation and
   candidate-vs-execution sweep before Queue-002 closure.
===============================================================================
C031-WIP76 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F382 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F381 remain CONFIRMED.
F382 / provisional KI#705 is CONFIRMED.
F383 / provisional KI#706 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F382 / provisional KI#705 — AdvCiv `AI_pickup` restructuring inverted K-Mod's
city-danger test, so transports reject SAFE remote pickup cities and travel
only toward DANGEROUS ones
-------------------------------------------------------------------------------

ROOT
----
`CvUnitAI::AI_pickup` has two major cases:

1. if the transport is already in a suitable pickup city, it can wait there
   with `MISSIONAI_PICKUP`;
2. otherwise, it scans the owner's cities and chooses a remote city to travel
   toward for pickup.

The current remote-city branch contains:

    if (GET_PLAYER(getOwner()).AI_plotTargetMissionAIs(
        pLoopCity->getPlot(), MISSIONAI_PICKUP, getGroup()) <
        (iCount + cargoSpace() - 1) / cargoSpace())
    {
        if (!pLoopCity->AI_isDanger())
            continue;

        int iPathTurns;
        if (atPlot(pLoopCity->plot()) ||
            !generatePath(pLoopCity->getPlot(),
                MOVE_AVOID_ENEMY_WEIGHT_3,
                true, &iPathTurns))
        {
            continue;
        }

        ...
        pBestPickupPlot = pLoopCity->plot();
    }

This explicitly discards a remote city when it is NOT in danger.

Therefore the only remote cities allowed through this gate are cities for
which:

    pLoopCity->AI_isDanger() == true.

That is the opposite of the inherited K-Mod behavior and the opposite of the
obvious naval-pickup safety policy.

K-MOD CONTROL
-------------
K-Mod's same branch is:

    if (AI_plotTargetMissionAIs(...) < ...)
    {
        if (!(pLoopCity->AI_isDanger()))
        {
            if (!atPlot(pLoopCity->plot()) &&
                generatePath(pLoopCity->plot(),
                    MOVE_AVOID_ENEMY_WEIGHT_3,
                    true, &iPathTurns))
            {
                ...
                pBestPickupPlot = pLoopCity->plot();
            }
        }
    }

So K-Mod travels toward the pickup city only when:

    !pLoopCity->AI_isDanger()

Current AdvCiv/SAS does the precise complement.

This is not a change in the meaning of `AI_isDanger`; the two code versions
surround the same pathing/value logic and use the same city object.

ASYMMETRY WITH THE CURRENT-CITY BRANCH
--------------------------------------
The error produces a particularly clean behavioral contradiction inside one
function.

At the beginning of `AI_pickup`, if the transport is ALREADY in an owned,
suitable city, the code can execute:

    getGroup()->pushMission(
        MISSION_SKIP,
        ...,
        MISSIONAI_PICKUP,
        pCity->plot());

and wait for units there.

That current-city branch does NOT require the city to be dangerous.

Therefore:

    safe City A + transport already at City A
        -> transport may wait and pick units up there

while:

    same safe City A + transport one or more turns away
        -> remote scan executes
           if (!CityA.AI_isDanger()) continue;
        -> transport refuses to travel there.

The city is considered a valid pickup location only after the transport
somehow reaches it by another mechanism.

LIVE CALLERS
------------
Current SAS uses `AI_pickup` broadly.

Assault transports:
* `AI_pickup(UNITAI_ATTACK_CITY, true, 5)`
* `AI_pickup(UNITAI_ATTACK, true, 5)`
* later ATTACK_CITY / ATTACK / COUNTER pickup searches with ranges commonly
  3 or 7 turns

Settler transports:
* `AI_pickup(UNITAI_CITY_DEFENSE)`
* `AI_pickup(UNITAI_SETTLE, true)`
* `AI_pickup(UNITAI_WORKER)`

Special transports:
* Missionary pickup
* Spy pickup
* Missile-air pickup

Thus this is not a dormant helper or a single unusual role.

A routine assault example is enough:

1. a safe rear/coastal city has several available `UNITAI_ATTACK_CITY` units;
2. an empty Assault transport is a few turns away;
3. no other pickup group has already claimed enough capacity;
4. `AI_considerPickup` accepts the city;
5. `iCount > 0`;
6. the city is safe, so current AdvCiv/SAS executes:

       if (!pLoopCity->AI_isDanger())
           continue;

7. the transport never even pathfinds toward that staging city.

If the same transport later happens to enter that city, the current-city branch
can immediately wait for those same units.

DANGEROUS-CITY FALSE POSITIVE
-----------------------------
The inversion is two-sided.

After the earlier acquisition/enemy-power filter, a sufficiently established
city that is currently dangerous can reach the pathfinding branch.

For Assault transports, `AI_considerPickup` can still accept a dangerous city
when it considers the city adequately defended.

Current code then permits the transport to path toward that dangerous city,
using `MOVE_AVOID_ENEMY_WEIGHT_3` but not rejecting the city itself.

K-Mod did the opposite: danger prevented the remote pickup trip.

So the regression both:
* suppresses ordinary safe logistical pickup;
* enables precisely the endangered destination K-Mod tried to avoid.

The outer condition:

    city age > 15 OR enemy power in area == 0

does not repair the inversion. Any older city passes the first arm regardless
of current area enemy power.

ANCESTRY / INTRODUCING CHANGE
-----------------------------
BtS/BBAI/K-Mod history already contains the safe-city policy.

The reachable AdvCiv history identifies the regression precisely:

    practical commit 1583
    715fa87579778a04e8da1adb5a6f1170f141aed5
    "Several improvements to AI worker production and assignment"

That commit substantially flattened/reformatted `AI_pickup`.

Before the rewrite, the inherited logic was:

    if (!(pLoopCity->AI_isDanger()))
    {
        if (... generatePath ...)
        {
            ...
        }
    }

The rewrite transformed that into early-continue style:

    if (!pLoopCity->AI_isDanger())
        continue;

    if (... !generatePath ...)
        continue;

The early-continue conversion failed to negate the old enclosing condition.
To preserve:

    if (SAFE) { body }

the equivalent guard is:

    if (DANGER) continue;

but the commit emitted:

    if (SAFE) continue.

This behavioral inversion is unrelated to the commit's advertised Worker
production/assignment changes.

Base AdvCiv 1.14 retains the regression.
Current AdvCiv-SAS inherits it unchanged.

Classification:
    inherited AdvCiv control-flow-flattening regression.

CONSEQUENCE
-----------
The AI can:
* leave Assault transports idle/elsewhere instead of collecting invasion
  troops from safe coastal staging cities;
* fail to collect Settlers, Workers or escorts from ordinary safe ports;
* fail to route special transports to safe Missionary/Spy/Missile staging
  cities;
* create inconsistent logistics where a port is valid for pickup if the ship
  is already there but invalid if the ship must travel there;
* preferentially allow remote pickup travel toward dangerous cities rather
  than safe ones.

This can directly reduce naval invasion assembly and island logistics.

The effect is particularly broad because `AI_pickup` is a shared transport
primitive rather than one role-specific heuristic.

REPAIR DIRECTION
----------------
Restore K-Mod's safe-city contract:

    if (pLoopCity->AI_isDanger())
        continue;

then retain the current early-continue structure for the path test.

Conceptually:

    if (targetPickupGroups < requiredPickupGroups)
    {
        if (pLoopCity->AI_isDanger())
            continue;

        int iPathTurns;
        if (atPlot(pLoopCity->plot()) ||
            !generatePath(...))
        {
            continue;
        }

        ...
    }

This is the exact logical translation of K-Mod's older:

    if (!AI_isDanger())
    {
        if (generatePath(...))
        {
            ...
        }
    }

No broader redesign is required.

-------------------------------------------------------------------------------
CONTROLS / COVERAGE COMPLETED IN THIS SLICE
-------------------------------------------------------------------------------

Current-SAS residual delta reconciliation
-----------------------------------------
A function-name reconciliation found a small set of current-vs-Base changed
`CvUnitAI` methods that had not yet been named explicitly in the later WIP
chain. The meaningful remaining deltas checked in this slice were mostly
formatting, logging or already-known policy:

* `AI_escortSeaMove`
  - current change is the repaired KI#331 `if (scrap()) return` contract.

* `AI_shadow`
  - signature formatting only.

* `AI_guardCityMinDefender`
  - current behavior change deliberately uses `AI_neededDefenders(true)` and
    gives empty/dangerous cities a ranking boost; no independent F383
    established.

* `AI_guardCityAirlift`
  - logging only.

* `AI_guardCoast`
  - formatting only.

* `AI_getGuardedPlots`
  - formatting only.

* `AI_safety`
  - logging only.

* `AI_pillageAroundCity`
  - formatting only.

* `AI_cityAttack`
  - signature/comment collapse only.

* `AI_defendTerritory`
  - formatting only.

* `AI_shouldRouteWhileImproving`
  - signature formatting only.

* `AI_canRouteThroughSafeTerritory`
  - signature formatting only.

* `AI_nukeValue`
  - signature formatting only.

* `AI_countPiracyTargets`
  - signature formatting only.

* `AI_moveIntoCity`
  - evacuation diagnostics only.

`AI_ferryWorkers`
-----------------
The current ferry's old fallback for a workable land tile still asks:

    pWorkingCity->AI_getBestBuild(...)

Current SAS intentionally returns `NO_BUILD` for ordinary land through the
city-side legacy cache. That is another consequence of existing KI#213, not a
new F383.

The current destination-aware Settler cargo helper was rechecked through both
origin and destination states; its cargo-Worker subtraction and exact deficit
accounting remain internally coherent. No false-success state transition was
established there.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F382 CONFIRMED in protected Queue 002.
F383 / provisional KI#706 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP76.
No repository source files modified.

NEXT
----
Continue from F383 unused.

Priority:
1. keep the naval logistics reloop open around `AI_pickup`,
   `AI_pickupStranded`, ferrying and special transports now that a second
   inherited predicate bug has surfaced;
2. finish `AI_attackCityMove` / combat-group residual state contracts;
3. inspect residual inherited flattening/early-continue conversions for the
   same lost-negation class as F382;
4. continue function-range reconciliation and final predicate/sentinel/
   source-vs-target sweeps before Queue-002 closure.
===============================================================================
C031-WIP77 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F383 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F382 remain CONFIRMED.
F383 / provisional KI#706 is CONFIRMED.
F384 / provisional KI#707 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F383 / provisional KI#706 — SAS `AI_ferryWorkers` hard area-capacity gate
counts its own cargo Worker as already delivered at the destination, so the
transport can reject the island immediately before the branch that unloads it
-------------------------------------------------------------------------------

ROOT
----
Current SAS practical-6060 added a hard landmass-capacity guard to
`CvUnitAI::AI_ferryWorkers`:

    int iAreaHave =
        kOwner.AI_totalAreaUnitAIs(
            pLoopCity->getArea(), UNITAI_WORKER);

    // advc.113: Don't count the workers in cargo as available
    if (!getPlot().isWater() &&
        pLoopCity->isArea(getArea()))
    {
        iAreaHave -= (2 * iWorkers) / 3;
        FAssert(iAreaHave >= 0);
    }

    int const iAreaNeeded =
        kOwner.AI_neededWorkers(pLoopCity->getArea());

    if (iAreaHave >= iAreaNeeded)
        continue;

The newly added `continue` happens BEFORE pathfinding, scoring and the eventual
arrival branch:

    if (atPlot(pBestPlot))
    {
        unloadAll();
        ...
        return true;
    }

The inherited `(2 * iWorkers) / 3` adjustment is not an exact exclusion of
cargo Workers.

For one cargo Worker:

    (2 * 1) / 3 == 0

so the transport subtracts nothing.

CARGO WORKERS REALLY ARE IN THE AREA COUNTER
--------------------------------------------
`CvPlayerAI::AI_totalAreaUnitAIs` is:

    return kArea.getNumTrainAIUnits(...) +
           kArea.getNumAIUnits(...);

`CvUnit::setXY` moves cargo units together with their transport:

    if (pLoopUnit->getTransportUnit() == this)
        pLoopUnit->setXY(iX, iY, ...);

and every moved UnitAI updates the new plot area's counter:

    pNewPlot->getArea().changeNumAIUnits(
        getOwner(), AI_getUnitAIType(), 1);

Therefore, when a Settler transport enters a destination CITY on an island, a
Worker still aboard that ship is counted by:

    AI_totalAreaUnitAIs(destinationArea, UNITAI_WORKER)

even though it has not unloaded and cannot work the island while remaining
cargo.

CURRENT ONE-WORKER FAILURE
--------------------------
Consider a destination island for which:

    AI_neededWorkers(destinationArea) == 1

and, before the ferry arrives:

    actual land Workers in destinationArea == 0.

A Settler transport carries one Worker to that city's port.

Before arrival, while the ship is still on water / another area:
* that cargo Worker is not counted as a Worker in the destination land area;
* `iAreaHave == 0`;
* `iAreaNeeded == 1`;
* the destination passes the new hard-capacity gate;
* `AI_ferryWorkers` can choose it and sail there.

After the ship reaches the destination city, the cargo Worker has moved with
the ship to that land plot and now contributes to the destination area's
UnitAI count.

On the next `AI_settlerSeaMove` update:

    iWorkerCount > 0
    iSettlerCount == 0

is already sufficient to call `AI_ferryWorkers`.

For the destination city:

    iAreaHave = 1   // includes the still-loaded Worker

The inherited correction does:

    iAreaHave -= (2 * 1) / 3
               = 0

so:

    iAreaHave == 1
    iAreaNeeded == 1

and the new practical-6060 test executes:

    if (iAreaHave >= iAreaNeeded)
        continue;

The destination is discarded before:

    if (atPlot(pLoopCity->plot()))
        ...

and therefore before the later:

    if (atPlot(pBestPlot))
        unloadAll();

which is the helper's ordinary delivery mechanism.

FALLBACK DOES NOT RE-DISCOVER LOCAL ISLAND WORK
-----------------------------------------------
If the city loop produces no other destination, the old advc.040 fallback may
scan individual plots.

But when the transport is in a city on the same destination landmass, the
fallback deliberately rejects those local-area plots through:

    if (kPlot.getArea().getCitiesPerPlayer(owner) > 0 ||
        AI_neededWorkers(kPlot.getArea()) <= 0 ||
        AI_totalAreaUnitAIs(kPlot.getArea(), UNITAI_WORKER) > 0)
    {
        bool bValid =
            (pCurrentCity != NULL &&
             !kPlot.isArea(getArea()));

        ...
        if (!bValid)
            continue;
    }

For the current island:

    kPlot.isArea(getArea()) == true

so `bValid` is false and the scan skips its plots.

Thus the second half of `AI_ferryWorkers` cannot compensate by noticing that
the still-loaded Worker has work on the island.

CALLER LIFECYCLE
----------------
`AI_settlerSeaMove` calls `AI_ferryWorkers` whenever:

    iWorkerCount > 0 &&
    no incoming LOAD_SETTLER mission &&
    (isFull() || iSettlerCount == 0)

A Worker-only Settler transport therefore re-enters this logic after reaching
the delivery city.

There is no generic automatic unload on arrival.

If `AI_ferryWorkers` returns false:
* the normal later pickup/role/retreat/safety logic runs;
* none of those branches guarantees unloading the Worker merely because its
  current landmass needs it;
* with no alternative target, the transport can skip with the needed Worker
  still aboard and reconsider again next turn.

The new hard capacity can therefore convert:
    "the Worker we just delivered fills the deficit"
into:
    "the area already has enough Workers, so do not deliver this Worker."

HISTORY PROVIDES AN INTERNAL CONFIRMING CONTROL
-----------------------------------------------
The ancestry is unusually strong because SAS itself discovered the same
counting problem in a neighboring new helper one practical commit later.

Practical commit 6060:
    71ff2a0ba7...
introduced:
* the hard `iAreaHave >= iAreaNeeded` rejection in `AI_ferryWorkers`;
* `AI_unloadSettlerCargoForDestination`.

The first version of the new destination helper likewise used:

    iWorkersExisting =
        AI_totalAreaUnitAIs(destinationArea, UNITAI_WORKER);

without excluding cargo Workers already physically at the destination.

Practical commit 6061:
    08cb9d272e...

explicitly fixed that neighboring bug:

    // Cargo Workers may already be included in the
    // destination-area count when the transport reaches its city.
    // Count them separately so the exact shortage below can exclude them.

It then counts ALL cargo Workers and, when at the destination, executes:

    iWorkersExisting =
        std::max(0,
            iWorkersExisting - iCargoWorkers);

That is exactly the missing semantic correction in `AI_ferryWorkers`.

However practical 6061 left the inherited:

    iAreaHave -= (2 * iWorkers) / 3;

unchanged in the ferry hard-capacity path.

So current source contains:
* one destination Worker-demand helper that correctly removes all onboard
  Workers from the "already existing" count;
* one adjacent destination Worker-demand helper that can remove zero of one
  onboard Worker before a hard rejection.

Classification:
    SAS-local practical-6060 worker-ferry regression, with a partial sibling
    repair in practical 6061 that did not reach `AI_ferryWorkers`.

CONSEQUENCE
-----------
Current SAS can:
* transport the exact Worker an island needs;
* reach the correct destination;
* count that still-loaded Worker as evidence that the island no longer needs
  the Worker;
* refuse to execute its own arrival `unloadAll()` branch;
* leave the needed Worker aboard or redirect the ship elsewhere.

The one-Worker case is particularly important because secondary islands often
have an exact demand of one, and integer `(2 * iWorkers) / 3` provides no
correction at all there.

The same under-subtraction can affect larger cargo:
    2 cargo Workers -> subtract 1
    3 cargo Workers -> subtract 2

so one onboard Worker is systematically left in `iAreaHave` for every positive
cargo count under this formula.

REPAIR DIRECTION
----------------
For the new HARD area-capacity test, cargo Workers on this transport must not be
counted as already delivered.

Use the same exact semantic correction already implemented in
`AI_unloadSettlerCargoForDestination`.

When the transport is physically in the candidate destination land area:

    iAreaHave = std::max(0, iAreaHave - iWorkers);

rather than:

    iAreaHave -= (2 * iWorkers) / 3;

At minimum this exact exclusion should govern the hard:

    iAreaHave >= iAreaNeeded

decision.

If the inherited two-thirds adjustment is still desired for the older SOFT
scoring heuristic, keep a separately named weighted count for that purpose.
Do not reuse a deliberately fractional availability estimate as an exact
capacity count.

A cleaner design would centralize "existing Workers excluding cargo on this
transport" so both:
* `AI_ferryWorkers`, and
* `AI_unloadSettlerCargoForDestination`
share the same definition.

-------------------------------------------------------------------------------
CONTROLS CLOSED IN THIS SLICE
-------------------------------------------------------------------------------

AdvCiv nested-if / early-continue flattening sweep
-------------------------------------------------
After F382, reachable pre-SAS `CvUnitAI.cpp` commit diffs were scanned for the
same structural family:
    old enclosing condition
    -> copied condition used as early `continue`.

The other candidates inspected so far preserve the intended boolean direction
or are ordinary exclusion guards:
* path-generation flattening;
* `AI_plotValid` flattening;
* cargo checks;
* build legality;
* same-area / domain exclusions;
* various candidate-value pruning.

No second surviving F382-style lost-negation sibling was established.

`AI_pickupStranded`
-------------------
The current destination handling uses adjacency of the actual stranded group's
pickup plot, not the transport/passenger source-context mistake from F381.
Its best adjacent pickup water tile is selected by real path turns, and its
mission target preserves the stranded unit identity.

The new completed-area-Worker duplicate-dispatch gate retains Base AdvCiv's
capacity-based coordination for ordinary groups. No F384 established there.

Residual current-vs-Base changed functions
------------------------------------------
Several moderate current deltas were reopened.

* `AI_bestPlotEspionage`
  - current changes are cached immutable define reads / formatting.

* `AI_groupFirstVal`
  - intentional Great-Person UnitAI enum renames.

* `AI_fortTerritory`
  - current top-level define gate intentionally disables canal/airbase Fort
    worker paths when configured off.

* `AI_workerSeaMove`
  - primary functional change is the intentional removal of inherited
    Work-Boat scrapping, plus diagnostics.

* `AI_handleStranded`
  - current completed-area Worker extension deliberately allows zero-demand
    Workers to advertise for naval rescue; no contradictory state transition
    established.

* `AI_exploreSeaMove` / `AI_barbAttackSeaMove`
  - current behavior changes are repaired KI#331 success-sensitive `scrap()`
    callers plus the existing executable-target Work Boat lookahead repair.

No independent F384 was assigned from these in this slice.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F383 CONFIRMED in protected Queue 002.
F384 / provisional KI#707 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP77.
No repository source files modified.

NEXT
----
Continue from F384 unused.

Priority:
1. follow F383 through the remaining `AI_ferryWorkers` / Settler-transport
   arrival, unload and reassignment states for any independent sibling rather
   than assuming the whole family is covered;
2. finish remaining current-SAS behavioral deltas (`AI_omniGroup`,
   `AI_connectBonusCost`, `AI_generalMove`, `AI_lead`, etc.);
3. return to combat/transport residual source-vs-target and candidate-vs-
   execution checks;
4. perform a function-range reconciliation and final pattern sweep before any
   Queue-002 closure decision.
===============================================================================
C031-WIP78 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F384 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F383 remain CONFIRMED.
F384 / provisional KI#707 is CONFIRMED.
F385 / provisional KI#708 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F384 / provisional KI#707 — AdvCiv ProbabilityTypes refactor inverted the
Carrier's land-threat retreat condition: `!= PROBABILITY_REAL` instead of
`>= PROBABILITY_REAL`
-------------------------------------------------------------------------------

ROOT
----
Current SAS / Base AdvCiv 1.14:

    void CvUnitAI::AI_carrierSeaMove()
    {
        ...
        if (AI_isThreatenedFromLand() != PROBABILITY_REAL)
        {
            if (AI_retreatToCity(true))
                return;
            if (AI_retreatToCity())
                return;
            if (AI_safety())
                return;
        }
        ...
    }

This is the only analogous naval role using `!= PROBABILITY_REAL`.

Current nearby roles use the expected threshold:

    AI_attackSeaMove
    AI_reserveSeaMove
    AI_escortSeaMove
    AI_exploreSeaMove
    AI_assaultSeaMove
    AI_settlerSeaMove
    AI_missionarySeaMove
    AI_spySeaMove
    AI_missileCarrierSeaMove
        -> AI_isThreatenedFromLand() >= PROBABILITY_REAL

The helper's enum is ordered:

    NO_PROBABILITY      = -1
    PROBABILITY_LOW     = 0
    PROBABILITY_REAL    = 1
    PROBABILITY_HIGH    = 2

Therefore the Carrier test means:

    NO   -> run emergency retreat block
    LOW  -> run emergency retreat block
    REAL -> SKIP emergency retreat block
    HIGH -> run emergency retreat block

That is not a sensible threat threshold and is the exact complement of the
important ordinary-threat state.

LIVE HELPER CONTRACT
--------------------
`AI_isThreatenedFromLand()` returns:

    if water:
        NO_PROBABILITY

    if in city:
        if city evacuating:
            HIGH
        if city safe:
            NO
        otherwise:
            damaged ? LOW : REAL

    outside city:
        if enemyOffense * 2 > ourDefense:
            REAL
        else if enemyOffense * 4 > ourDefense:
            LOW
        else:
            NO

So an ordinary, full-health naval unit sitting in an owned city that
`AI_isSafe()` does NOT consider safe receives:

    PROBABILITY_REAL.

That is exactly the state current `AI_carrierSeaMove` excludes from its urgent
retreat/safety block.

The Carrier then proceeds into:
* healing;
* grouping;
* carrier transport movement;
* blockade;
* assault-transport shadowing;
* upgrade travel;

before the generic late retreat/safety fallbacks.

Thus the dedicated "threat from land" priority is defeated at the threat level
it was designed to catch.

ANCESTRY / INTRODUCING REFACTOR
--------------------------------
K-Mod's Carrier code did not use ProbabilityTypes. It directly computed local
land defense and enemy offense and entered the emergency retreat block when:

    iEnemyOffense > iOurDefense / 2

(with damaged units treated more cautiously by scaling defense).

AdvCiv later extracted duplicate naval threat code into
`AI_isThreatenedFromLand`.

Immediately before the ProbabilityTypes conversion, the Carrier had the same
boolean helper contract as the other naval roles:

    if (getPlot().isCity(true))
    {
        if (AI_isThreatenedFromLand())
        {
            retreat/safety...
        }
    }

Reachable pre-SAS AdvCiv practical commit 2612,
SHA:

    fc7640933b544f0f943f527e2edc08999da1dc00

changed the helper return type from:

    bool CvUnitAI::AI_isThreatenedFromLand() const

to:

    ProbabilityTypes CvUnitAI::AI_isThreatenedFromLand() const

and converted the call sites.

For Spy and the ordinary naval roles, the old boolean condition became:

    AI_isThreatenedFromLand() >= PROBABILITY_REAL

which preserves the helper's old strong-threat meaning while allowing
LOW to represent a deliberately weaker state.

The same patch converted the Carrier to:

    AI_isThreatenedFromLand() != PROBABILITY_REAL

instead.

The diff therefore gives an unusually strong introducing-control:
* same helper conversion;
* same retreat body;
* sibling callers converted to `>=`;
* Carrier alone converted to `!=`.

Base AdvCiv 1.14 retains the Carrier condition.
Current AdvCiv-SAS inherits it unchanged.

Classification:
    inherited AdvCiv ProbabilityTypes/refactor regression.

CURRENT-SAS CONSEQUENCE
-----------------------
Take an undamaged Carrier in an owned city that is currently unsafe because
nearby enemy land offense is sufficiently threatening.

The helper returns:

    PROBABILITY_REAL.

Expected/K-Mod behavior:
    immediately prioritize retreat to a safer/primary city or safety plot.

Current AdvCiv/SAS behavior:
    `REAL != REAL` is false;
    skip the emergency retreat block;
    continue into ordinary Carrier logic.

A loaded/escorted Carrier can therefore proceed toward:
* a carrier-sea transport target;
* blockade behavior;
* shadowing an assault fleet;

instead of first responding to the land threat that the inherited naval safety
block was written to handle.

The inverse states are also diagnostic:
* `NO_PROBABILITY != REAL` is true, so a completely safe/no-threat state enters
  the emergency retreat code unnecessarily;
* `LOW != REAL` is true, so the weaker state that the ProbabilityTypes refactor
  created specifically to distinguish from a real threat also enters it.

Some of those false-positive calls can harmlessly fail and fall through, but
they confirm that the predicate is not expressing a threshold. The
false-negative `REAL` state is the direct gameplay defect.

`PROBABILITY_HIGH`, used for an evacuating city, correctly enters the block
under both `!= REAL` and the intended `>= REAL`; this does not rescue the
ordinary REAL case.

REPAIR DIRECTION
----------------
Make Carrier consistent with the extracted threat contract and the sibling
naval roles:

    if (AI_isThreatenedFromLand() >= PROBABILITY_REAL)

This:
* catches REAL ordinary threats;
* catches HIGH evacuating-city threats;
* ignores LOW/NO states;
* matches the semantic conversion used by the other naval callers.

No broader redesign is required.

-------------------------------------------------------------------------------
F380 COMPANION REPAIR NOTE — LATENT REVEALED-ROUTE ENUM BOOLEAN
-------------------------------------------------------------------------------

The final F384 sweep found another genuine historical defect adjacent to F380,
but it is CURRENTLY MASKED BY F380 and therefore is deliberately NOT assigned
a separate finding number.

Inside the same `AI_airOffenseBaseValue` enemy-border branch:

    else if (iDistance == 2 && p.
        getRevealedRouteType(getTeam()))
    {
        iBorderDanger += 2;
    }

K-Mod used:

    pLoopPlot->isRoute()

AdvCiv practical commit 1396,
SHA beginning:

    662e020a...

("Tentative changes to make naval invasions easier to intercept")

replaced that with a revealed-route query but used the returned `RouteTypes`
enum directly as a C++ boolean.

Civ4 dynamic route enums use:

    NO_ROUTE = -1

and current route XML orders real routes as:

    ROAD     = 0
    RAILROAD = 1
    ...

So as a boolean:

    NO_ROUTE (-1) -> true
    ROAD      (0) -> false
    RAILROAD  (1) -> true

The intended test must be explicit, e.g.:

    p.getRevealedRouteType(getTeam()) != NO_ROUTE

or equivalent revealed-route semantics.

WHY NO SEPARATE F385
--------------------
Current F380's lost vassal negation prevents ordinary enemy territory from
entering the enclosing branch at all:

    current:
        other team && IS our vassal

rather than:

        other team && IS NOT our vassal.

The nested route test then additionally requires:

    atWar(p.getTeam(), getTeam())

A normal vassal/master pair cannot supply the ordinary hostile state the
border-danger code is meant to measure.

Thus F380 currently masks the route-enum inversion for the meaningful enemy
case. It has no independent current runtime consequence proven beyond F380.

However, repairing F380 by restoring the missing `!isVassal(...)` would
immediately expose this older route predicate:
* an enemy tile with NO route would be treated as routed;
* an enemy tile with a ROAD (route enum 0) would be treated as unrouted.

Therefore the F380 implementation review should fix BOTH predicates together.

This remains a mandatory companion repair / latent historical defect, not a
manufactured F385.

-------------------------------------------------------------------------------
CONTROLS CLOSED IN THIS SLICE
-------------------------------------------------------------------------------

`AI_connectBonusCost`
---------------------
Current SAS computes an `iMultiplier` based on missing Workers in the area but
no longer applies it after the Fort/defensive weighting rewrite.

That is dead/confusing logic, but not a demonstrated ranking defect in the
current function: once the removed Fort-specific terms are excluded,
`iMissingWorkersInArea` is constant for all candidate builds being compared for
the same plot. Multiplying every build cost by the same factor would not change
which build wins.

Cleanup is warranted; no F385.

`AI_lead`
---------
The current Great-General leader threshold:

    bBelowMinStrength =
        minStrength > 0 && strengthScore < minStrength;

    bBelowMinHealing =
        minHealing <= 0 || healing < minHealing;

    reject if bBelowMinStrength && bBelowMinHealing

matches the current XML semantics:
* strength threshold 0 disables the strength gate;
* healing threshold 0 disables the healing bypass;
* with both positive, meeting either configured objective can preserve a valid
  lead target.

No predicate inversion established.

`AI_omniGroup`
--------------
The SAS peacetime ATTACK_CITY capacity gates and
`SAS_countPlotAttackCityGroupUnits` / `bOurGroupAlreadyCounted` adjustment were
reopened. No double-count/under-count root survived the live group-state proof
in this slice.

`AI_guardCity` source candidate arithmetic
------------------------------------------
A potentially suspicious inherited line subtracts:

    getGroup()->getNumUnits()

from a plot count based on `PUF_canDefendGroupHead` when evaluating the current
city.

That mixes unit count and group-head count locally.

However the current/source city is then rejected by the movement/path
candidate condition (`at(city)`), so the distorted candidate value does not
become a remote guard destination. No independent runtime consequence was
established.

This is another example deliberately closed rather than numbered.

Ferry city-demand adjustment
-----------------------------
`AI_ferryWorkers` can subtract its cargo Worker count from a city's
`AI_getWorkersHave()` when its old mission plot is that city even if the ferry
is farther away than the city cache's normal incoming-Worker radius.

That can alter the soft city score, but current SAS's newer hard area-demand
capacity gate prevents a clean independent over-delivery proof in the examined
state.

The exact hard cargo-as-delivered defect remains F383; this softer cache-range
inconsistency is retained as a note, not F385.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F384 CONFIRMED in protected Queue 002.
F385 / provisional KI#708 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP78.
No repository source files modified.

NEXT
----
Continue from F385 unused.

Priority:
1. keep the naval-role safety/predicate family open long enough to verify no
   other `ProbabilityTypes` conversion sites have a surviving threshold error;
2. finish the remaining Settler/transport group-cargo and arrival state review;
3. return to the large `AI_settleMove` / `AI_foundFirstCity` and combat target
   deltas for state/sentinel/source-target errors;
4. perform the Queue-002 function-range reconciliation and final whole-file
   pattern sweep before any closure decision.

===============================================================================
C031-WIP79 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F385 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F384 remain CONFIRMED.
F385 / provisional KI#708 is CONFIRMED.
F386 / provisional KI#709 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F385 / provisional KI#708 — AdvCiv `AI_guardCitySite` replaced K-Mod's exact
mission-target check with an adjacent-only check, so a defender targeting the
city-site tile itself does not prevent duplicate guards
-------------------------------------------------------------------------------

K-Mod rejects a planned city site when another group already has
`MISSIONAI_GUARD_CITY` targeted at that exact site:

    AI_plotTargetMissionAIs(pLoopPlot, MISSIONAI_GUARD_CITY, getGroup()) == 0

AdvCiv advc.300 comments out that exact check and says:

    // Need to check the adjacent tiles too

but implements only an adjacent-plot loop. `FOR_EACH_ADJ_PLOT(kLoopPlot)`
enumerates the eight neighboring tiles, not `kLoopPlot` itself, and the mission
query uses range zero by default. An existing mission targeted at the exact
city-site tile is therefore invisible to the new duplicate check.

The same rewritten function can still create precisely that omitted state.
`pBetterGuardPlot` starts as the city-site tile and changes only if an adjacent
plot receives a strictly positive guard value. On an ordinary revealed flat
neighborhood with no defensive/visibility bonus, every adjacent candidate can
remain at zero and the queued `MISSIONAI_GUARD_CITY` target stays the exact city
site.

A second eligible group then scans only the eight adjacent tiles, fails to see
the first exact-site mission, and can be sent redundantly to the same planned
city site. Current live callers include ordinary combat-unit movement such as
`AI_collateralMove` and `AI_counterMove`, so multiple groups can reach the
state.

A second internal control reinforces the omission. Inside the adjacency-only
loop the rewrite contains:

    if (pAdj == pBestGuardPlot)
        iGuardValue += 1; // tie-breaker

At that point `pBestGuardPlot` is the center city-site tile and `pAdj` is
necessarily adjacent, so the comparison is unreachable. The rewrite's logic
thus treats the center as though it were in the candidate neighborhood while
the iterator excludes it.

ANCESTRY
--------
* K-Mod: exact planned city-site mission target checked.
* Base AdvCiv 1.14: advc.300 replaces it with adjacent-only coordination.
* Current AdvCiv-SAS: inherits the AdvCiv structure unchanged.

Classification: inherited AdvCiv advc.300 mission-target scope regression.

CONSEQUENCE
-----------
The AI can send multiple defenders to the same future city site instead of
spreading them across separate sites, wasting fogbusting/anti-Barbarian
coverage and diverting extra defenders. Duplicate suppression depends on
whether the first guard happened to choose the exact site or an adjacent tile.

REPAIR DIRECTION
----------------
Check the center and adjacent plots. The conservative repair is to restore the
old exact-site mission check and then scan adjacent tiles. Alternatively, use
the existing mission-target helper with range 1 around the planned site if that
matches the intended area semantics. The unreachable center tie-breaker should
also be removed or the center explicitly included in position scoring.

-------------------------------------------------------------------------------
CONTROLS / RECONCILIATION PROGRESS
-------------------------------------------------------------------------------

`AI_foundFirstCity` / `AI_settleMove` remained clean under the current deep
state/path pass: candidate city site, temporary scout endpoint, return target
and queued founding state remain distinct; no stale path endpoint or false
success transition was established.

The current SAS-vs-Base changed-member inventory is effectively saturated. The
apparent `AI_getCityToImprove` holdout was a parser artifact caused by the
following file-local `countImprovedTiles` helper.

`AI_missileLoad`'s apparent remote-transport attachment hazard closes on
mission execution: the DOMAIN_AIR move/rebase mission executes before
`setTransportUnit`, so no supported non-colocated load state was established.

The remaining closure blind spot is inherited K-Mod -> AdvCiv changed functions
not explicitly named in the later WIP chain, with compact/high-risk helpers
prioritized before the final function-range sweep.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F385 CONFIRMED.
F386 / provisional KI#709 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP79.
No repository source files modified.

NEXT
----
Continue the inherited K-Mod -> AdvCiv reconciliation from F386 unused,
starting with `AI_leaveAttack`, then airlift/protect/patrol/hide, sea bombard /
carrier / route-city, spy, choke/poach and blockage helpers. Preserve clean
controls and run the final Queue-002 function-range/pattern reconciliation only
after those inherited deltas are saturated.

===============================================================================
C031-WIP80 — PROTECTED QUEUE 002 CvUnitAI.cpp — INHERITED RECONCILIATION
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F385 remain CONFIRMED.
F386 / provisional KI#709 remains UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

This checkpoint deliberately records a substantial CLEAN/CONTROL tranche rather
than allocating a finding merely because the file is still under review.

-------------------------------------------------------------------------------
RECONCILIATION EXPANSION
-------------------------------------------------------------------------------

The current-SAS-vs-Base-AdvCiv changed-member inventory is effectively
saturated. The remaining closure blind spot was widened in two directions:

1. K-Mod -> Base AdvCiv member functions that differ but had never been named
   explicitly in the later C031 WIP chain;
2. functions added by AdvCiv itself, which by definition have no K-Mod body and
   therefore disappear from a common-function diff.

The latter was an important methodology correction. AdvCiv-only helpers now
explicitly inventoried include:

    AI_airStrikeValue
    AI_barbAmphibiousCapture
    AI_canConnectBonus
    AI_countEnemyDefenders
    AI_exploreAirCities
    AI_exploreAirRange
    AI_guardYield
    AI_isAnyEnemyDefender
    AI_isAnyPiracyTarget
    AI_isPotentialEnemyOf
    AI_moveSettlerToCoast
    AI_plotDefense
    AI_singleUnitHeal
    AI_upgradePriority
    fromIDInfo

The first batch of these has been deliberately read rather than silently
assumed safe. No F386 crossed the caller/data threshold from them in this slice.

-------------------------------------------------------------------------------
CLEAN CONTROL — AI_leaveAttack GROUP-HEAD / UNIT-COUNT MIX
-------------------------------------------------------------------------------

`AI_leaveAttack` compares a defend-capable group-head count on the source plot
against `getGroup()->getNumUnits()`. This initially looked like a possible
repeat of the group-vs-unit accounting class explored around WIP72/WIP73.

The deeper ancestry/caller pass does not support promoting it:
* the shape already exists in BtS rather than being an AdvCiv/K-Mod rewrite;
* it is gated behind explicit local attack/defense-strength checks;
* the whole current selection group really is the object that would depart;
* no explicit source contract establishes that one remaining defender group
  must be treated as N individually available defenders for this decision.

The branch is conservative, but no independent correctness defect was proved.
F386 remains unused.

-------------------------------------------------------------------------------
CLEAN CONTROL — AI_assaultSeaReinforce MASTER-TEAM OPTIMIZATION
-------------------------------------------------------------------------------

K-Mod enters its final "assist master in attacking" block only when:

    GET_TEAM(getTeam()).isAVassal()

and finds the master through a loop over `isVassal`.

AdvCiv replaces the loop with:

    TeamTypes eMasterTeam = GET_TEAM(getTeam()).getMasterTeam();
    if (eMasterTeam != NO_TEAM &&
        GET_TEAM(getTeam()).isOpenBorders(eMasterTeam))
    {
        ...
    }

`getMasterTeam()` deliberately returns the team's own ID when it is free, so
this initially looked as though the vassal-only policy had been broadened to
all civs.

The state contract closes the concern. `CvTeam::reset` clears every
`m_abOpenBorders` entry, including self. `isOpenBorders(eTeam)` is a direct
array lookup and no self-open-borders initialization was found. Therefore a
free team gets:

    eMasterTeam == getTeam()
    isOpenBorders(getTeam()) == false

and does not enter the assistance block. A real vassal returns its external
master and can pass the ordinary vassal/master open-borders state.

No F386.

-------------------------------------------------------------------------------
CLEAN CONTROL — F385 / SETTLER EXACT-SITE SAFETY INTERACTION
-------------------------------------------------------------------------------

F385 established that advc.300 city-site guards can stand on an adjacent
fogbusting tile while the duplicate-guard check accidentally omits the exact
center site.

A neighboring Settler check looked inconsistent because an undefended Settler
in `AI_found` recognizes `MISSIONAI_GUARD_CITY` only when the mission target is
the exact founding tile, not merely adjacent.

That stricter test is justified: an adjacent fogbuster does not physically
defend the Settler after it steps onto the founding tile. Treating an adjacent
mission as a valid on-tile escort would weaken Settler safety. This is therefore
not an F385 sibling.

-------------------------------------------------------------------------------
CLEAN CONTROL — ROUTE / MISSILE / GUARD HELPERS
-------------------------------------------------------------------------------

`AI_routeCity`:
The advc.121 midpoint/incomplete-route rewrite was traced through the queued
`MISSION_ROUTE_TO` sequence. The current `bRouteTo` split is consistent with
continuing an incomplete route rather than necessarily walking back to the
first endpoint. No stale path-endpoint or queue-order defect was established.

`AI_missileLoad`:
The apparent sequence "queue movement toward remote carrier, then immediately
`setTransportUnit`" does not prove a non-colocated load. DOMAIN_AIR move/rebase
mission execution occurs when the mission is started before the following
`setTransportUnit` call on the supported path. No F386.

`AI_guardBonus`:
A possible foreign/unowned guarded-bonus scope problem closes through
`CvUnit::canReachBySeaPatrol`. The naval patrol destination must be water,
revealed-improved and owned by our team; land guard range is zero at the
candidate itself. No foreign-bonus leak established.

`AI_guardYield`:
The AdvCiv-only helper can derive a working city from one plot while centering
its local protected-yield scan on the current guard-bonus plot. This is unusual,
but its documented purpose is nearby high-yield/barbarian protection rather
than an exact city-BFC accounting function. No harder target-scope contract was
found.

-------------------------------------------------------------------------------
ADDITIONAL INHERITED FUNCTIONS DELIBERATELY OPENED
-------------------------------------------------------------------------------

The compact/high-risk K-Mod -> AdvCiv tranche opened in this slice includes:
* `AI_protect`
* `AI_goody`
* `AI_poach`
* `AI_solveBlockageProblem`
* `AI_routeCity`
* `AI_guardBonus`
* `AI_guardFort`
* `AI_guardSpy`
* `AI_hide`
* `AI_patrol`
* airlift/paradrop/protect helpers
* spy target/action helpers
* sea bombard / pirate blockade / carrier transport
* `AI_assaultSeaReinforce`

Controls established so far:
* current `AI_goody` already contains the historical iterator-condition repair;
* `AI_poach` intentionally fixes the old diagonal-only offset condition;
* `AI_solveBlockageProblem` preserves the old reverse path-node traversal while
  modernizing war-plan checks;
* `AI_protect` is essentially signature/format modernization;
* `AI_hide`'s known-potential-enemy detector-team scope has no demonstrated
  current concealment-contract violation;
* `AI_patrol` is a deliberate advc.102/advc.309 policy rewrite; no state or
  boolean inversion established;
* `AI_heal`'s extracted single-unit helper and city-heal generalization preserve
  the multi-unit healing state flow.

The large naval functions remain part of the open reconciliation; this WIP does
NOT claim they are all saturated.

-------------------------------------------------------------------------------
F380 COMPANION-REPAIR EXPANSION (NO NEW FINDING NUMBER)
-------------------------------------------------------------------------------

The deeper inheritance sweep found a second latent problem inside the same
`AI_airOffenseBaseValue` branch already broken by F380.

K-Mod/current code contains a distance-2 wartime route term, but the surrounding
search radius is only 1. In current AdvCiv this is:

    for (SquareIter it(kPlot, 1); ...)
        ...
        else if (iDistance == 2 && ...)

so the distance-2 arm is unreachable.

This is older than the SquareIterator conversion: K-Mod's coordinate loop also
used `iRange = 1` while retaining the distance-2 arm.

Combined with the already-recorded bare revealed-route enum bug:

    p.getRevealedRouteType(getTeam())

(where `NO_ROUTE=-1` is truthy and ROAD=0 is false), a complete F380 repair
should review all three conditions together:
1. restore `!isVassal(...)` for normal hostile territory;
2. compare revealed route explicitly with `NO_ROUTE`;
3. if the inherited distance-2 route-danger policy is intended, use a search
   radius that can actually reach distance 2.

No separate F386 is allocated because current F380 prevents the ordinary enemy
state from reaching the nested wartime branch in the first place.

-------------------------------------------------------------------------------
CURRENT STONE
-------------------------------------------------------------------------------

Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F385 CONFIRMED.
F386 / provisional KI#709 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP80.
No repository source files modified.

NEXT
----
Continue the inherited K-Mod -> AdvCiv and AdvCiv-only reconciliation from F386
unused. Prioritize the remaining large naval/air/spy helpers, then inventory and
review file-local non-member helpers before the final Queue-002 function-range,
source-vs-target, boolean, sentinel and candidate/execution sweeps.

===============================================================================
C031-WIP81 — PROTECTED QUEUE 002 CvUnitAI.cpp — RECONCILIATION CHECKPOINT
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F385 remain CONFIRMED.
F386 / provisional KI#709 remains UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

POST-WIP80 RECONCILIATION CONTROLS
----------------------------------

`AI_leaveAttack`
----------------
The inherited comparison between defend-capable group heads and the departing
selection group's unit count is conservative and visually odd, but it already
exists in BtS and sits behind real local offense/defense-strength gates. No
explicit contract was established that the source city must retain a count of
individual defenders rather than independent defender groups. No F386.

`AI_missileLoad`
----------------
The apparent state hazard where the helper queues movement toward a remote
carrier and then calls `setTransportUnit` closes on mission execution: the air
`MISSION_MOVE_TO` is activated/executed before `setTransportUnit` in the normal
supported AI path, so the missile reaches the carrier plot before attachment.
No supported non-colocated load state was established.

`AI_nextCityToImproveAirlift`
-----------------------------
This helper ranks candidate cities partly through `AI_totalBestBuildValue`,
which in turn consumes the city-side `AI_getBestBuildValue` cache. Current SAS
intentionally returns 0/NO_BUILD for ordinary land through that legacy city
cache while land Workers use the custom `CvUnitAI::AI_bestCityBuild` policy.
This is another live consequence of EXISTING KI#213, not a new F386.

Great-Person execution helpers
------------------------------
AdvCiv removed some runtime legality checks from Great Work / Trade execution
while retaining assertions. On the live `AI_greatPersonMove` path, however,
Great Work and Trade destinations are selected from same-pass value functions
that require `canGreatWork` / `canTrade`. No supported intervening state change
or illegal-action execution was established.

`AI_spreadCorporationAirlift`
-----------------------------
The airlift mission uses generic `MISSIONAI_SPREAD` while ordinary corporation
movement coordinates through `MISSIONAI_SPREAD_CORPORATION`. That tag mismatch
is real, but the airlift is immediate and the inherited Unofficial-Patch guard
rejects a destination once the same executive unit type is physically there.
No supported duplicate-airlift root was established.

`AI_assaultSeaReinforce` master-team optimization
-------------------------------------------------
AdvCiv replaced K-Mod's explicit "team that we are a vassal of" search with
`getMasterTeam()`. For a free team this returns self, but team initialization /
reset leaves self open-borders false, so the subsequent
`isOpenBorders(eMasterTeam)` test prevents ordinary non-vassals from entering
the master-assistance branch. No F386.

F380 companion repair expansion
-------------------------------
The deeper inherited reconciliation found two additional defects inside the
nested wartime-border portion of `AI_airOffenseBaseValue`, both currently
masked by F380's enclosing lost-vassal-negation defect and therefore not given
independent finding numbers:

1. `getRevealedRouteType(getTeam())` is used as a raw boolean. Dynamic route
   enums use `NO_ROUTE=-1`, while ROAD is the first real route (0), so the
   intended route-presence test must explicitly compare against `NO_ROUTE`.

2. Current code iterates `SquareIter(kPlot, 1)` but still contains an
   `iDistance == 2` route-danger branch inherited from the older radius-2
   search. That branch is unreachable under the current iterator radius.

Fixing F380 should therefore review all three pieces together:
* restore `!isVassal(...)`;
* use explicit revealed-route presence semantics;
* restore/reconcile the distance-2 search radius if that term remains desired.

F385/Settler adjacency cross-check
----------------------------------
The apparent sibling where a future-city guard may stand on an adjacent
fogbusting tile while an undefended Settler's founding safety test accepts only
an exact-site `MISSIONAI_GUARD_CITY` target is not a supported bug. An adjacent
fogbuster does not physically defend the Settler once it steps onto the city
site, so the stricter exact-site escort exception has a defensible safety
contract. No F386.

SAS file-local helper layer — opening controls
----------------------------------------------
The closure inventory was expanded beyond `CvUnitAI::` members because current
SAS contains behaviorally important file-local helpers that do not appear in a
member-function reconciliation. This layer already produced F375 and therefore
must be saturated before Queue 002 closes.

Controls established so far:

* `SAS_releaseTargetlessPeacetimeAttackCityExcess`:
  converting ATTACK_CITY -> ATTACK through `AI_setUnitAIType` is not merely a
  label change. `AI_setUnitAIType` calls `joinGroup(NULL)`, so the converted
  unit detaches from the original group. Explicit non-ATTACK_CITY releases
  also detach. No trapped-role-conversion root.

* `SAS_createMissingBarbarianCityExpeditionLeader`:
  the ATTACK -> ATTACK_CITY conversion inherits the same automatic detachment
  contract and does not leave the new leader role hidden inside its old group.

* `SAS_pickPathableBarbCityForAttackStack`:
  overall candidate identity and pathable candidate identity remain separate;
  no stale overall target was shown to execute after a failed path test.

* First-city food/fog helpers:
  `SAS_canAssumeWaterBonusImprovement` intentionally returns true for land, so
  the food-environment scorer is not accidentally restricting bonus credit to
  water. Current default minimum-revealed-BFC gates also prevent the diagnostic
  revealed-count divisor from reaching zero.

* `SAS_findBestReachableSettlerSite`:
  current use found in this pass is diagnostic/logging; no gameplay root was
  assigned from its tie/path details.

* `SAS_collectSettlerCityEscortPool`:
  the initially suspicious cross-domain escort case closes because
  `canDefend(cityPlot)` applies domain validity; ordinary naval units sitting
  in a coastal city are not eligible land defenders for this pool.

* Worker irrigation helpers:
  the current `SAS_findWorkerIrrigationChainStep` retains the already-fixed
  KI#214 `(plot, steps)` state rather than collapsing multiple path costs into
  one visited-plot state. No new root established yet.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F385 CONFIRMED.
F386 / provisional KI#709 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP81.
No repository source files modified.

NEXT
----
Continue from F386 unused through the remaining file-local attack-stack/Worker
helpers and the inherited AdvCiv-only / K-Mod->AdvCiv spy, naval and air helper
surface. Then run the final member + static-helper function-range reconciliation
and whole-file boolean/sentinel/source-target/candidate-execution sweep before
any Queue-002 closure decision.

===============================================================================
C031-WIP82 — PROTECTED QUEUE 002 CvUnitAI.cpp — LATE RECONCILIATION CHECKPOINT
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F385 remain CONFIRMED.
F386 / provisional KI#709 remains UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

POST-WIP81 CONTROLS PRESERVED
-----------------------------

`AI_guardYield`
---------------
The AdvCiv-only helper has a real geometry/lifecycle oddity: after a unit is
already guarding an outer-ring improvement, the function obtains that plot's
`getWorkingCity()` but iterates `CityPlotIter(getPlot())`, i.e. a 21-tile
footprint centered on the guard unit's current improvement rather than on the
identified city. AI sleeping/fortified groups are force-updated on later turns,
so this can genuinely retarget the guard rather than being dormant code.

However the source can still be read as intentionally allowing an established
yield guard to protect another nearby valuable improvement. The comments and
unused `pCity` are suspicious but do not establish a hard BFC-only contract.
Retained as design debt / review note, not F386.

Late inherited air/naval/action batch
-------------------------------------
The following helpers were deliberately reopened against K-Mod / Base AdvCiv
and their live current-SAS callers. No independent F386 survived:

* `AI_offensiveAirlift`: same-area exclusion is the correct flattened form of
  K-Mod's cross-area candidate condition.
* `AI_paradrop`: iterator rewrite preserves enemy/range candidate semantics.
* `AI_defensiveCollateral`: AdvCiv's city/team interpretation change is
  intentional and the SquareIterator candidate/path state remains paired.
* `AI_blockade`: restructured candidate target and path endpoint remain paired.
* `AI_carrierSeaTransport`: repeated cargo-group processing preserves the K-Mod
  lifecycle; no new source/target mismatch established.
* `AI_airDefensiveCity`, `AI_airCarrier`, `AI_defendBaseAirStrike`,
  `AI_airBombPlots`: no new polarity/target/interception root established.
* Great Work / Trade execution remains protected by same-pass value functions
  requiring `canGreatWork` / `canTrade`; no supported illegal action state.
* `AI_pillageValue` explicitly compares route type with `NO_ROUTE`, providing
  another useful control for the raw-enum route bug retained as an F380
  companion repair.

Worker no-bonus branch scorer
-----------------------------
`SAS_pickWorkerNoBonusBranchBuild` can return the highest buildable candidate
without a generic `score > 0` requirement, but under current default XML the
irrigation-blocking penalty does not produce the feared all-negative executable
state. The penalty activates only where a real irrigation-carrying option is
available; low-yield branches where Farm is impossible do not receive that
penalty. No F386.

Reconciliation scope
--------------------
Current SAS-vs-Base changed-member coverage is effectively saturated. The
remaining blind spot is inherited AdvCiv-vs-K-Mod behavior plus file-local SAS
helpers. Closure still requires an explicit function-range reconciliation over
both member and static/helper layers and a final boolean/sentinel/source-target/
candidate-execution pattern sweep.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F385 CONFIRMED.
F386 / provisional KI#709 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP82.
No repository source files modified.

NEXT
----
Continue from F386 unused through the remaining inherited espionage/nuke/
reserve/pirate/action helpers and the rest of the SAS file-local helper layer,
then begin the final Queue-002 function-range and whole-file pattern
reconciliation.

===============================================================================
C031-WIP83 — PROTECTED QUEUE 002 CvUnitAI.cpp — DEEP RECONCILIATION CONTINUES
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F385 remain CONFIRMED.
F386 / provisional KI#709 remains UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

This checkpoint deliberately records additional negative/coverage evidence rather
than allocating a finding merely because the protected reloop is deep.

-------------------------------------------------------------------------------
POST-WIP82 RECONCILIATION — ROUTE / MOVEMENT / LATE ACTION CONTROLS
-------------------------------------------------------------------------------

`AI_routeCity` / advc.121 midpoint behavior
-------------------------------------------
A potentially serious-looking queue shape was traced through the actual mission
engine before being judged.

When advc.121 decides a Worker may already be in the middle of an incomplete
city-to-city route, its comment says:

    Don't move to kLoopCity first then.

The function can nevertheless queue:

    MISSION_ROUTE_TO -> kLoopCity
    MISSION_ROUTE_TO -> pRouteToCity

This initially looked like an accidental backtrack to the first endpoint.
`CvSelectionGroup::MISSION_ROUTE_TO`, however, executes `groupRoadTo`, and
`groupRoadTo` first attempts the best route build on the Worker's CURRENT plot
before pathing toward the mission endpoint.

The advc.121 distinction is therefore between:
* a plain MOVE_TO back to kLoopCity, which would waste travel without completing
  the route; and
* a ROUTE_TO back toward kLoopCity, which builds/completes the unfinished half
  while moving.

The queued shape is consistent with finishing an incomplete connection from a
midpoint. No F386.

Movement-family inherited controls
----------------------------------
The previously unnamed movement members were deliberately reopened against
K-Mod/Base rather than assumed safe:

* `AI_animalMove`
  - advc.309's `bAttackAttempted` prevents a second random attack attempt while
    still allowing a cornered animal to attack after patrol failure.
  - the damage-scaled heal chance is explicit animal policy; no polarity bug.

* `AI_barbAttackMove`
  - advc.300 replaces coarse civilization-city thresholds with a deterministic
    birthmark/aggression curve and area-city context. The major branch changes
    are policy, not an execution-state or sentinel regression found here.

* `AI_paratrooperMove`
  - the hostile-state change from potential enemy to actual `atWar` matches the
    accompanying contract that the unit should not already occupy undeclared
    enemy territory. No live branch inversion established.

* `AI_pillageMove`
  - current plot-aware `isEnemy(getPlot())` preserves hidden-nationality / plot
    combat-owner semantics.
  - advc.017b role conversion detaches through `AI_setUnitAIType`; no group-state
    trapping root.

* `AI_reserveMove`, `AI_cityDefenseMove`, `AI_cityDefenseExtraMove`
  - reopened danger, transport-loading, defensive and grouping deltas; no new
    candidate survived caller/state review.

`AI_afterAttack`
----------------
AdvCiv's Blitz handling was reopened. The K-Mod early return for Blitz units is
replaced by the more precise `!isMadeAllAttacks()` gate so a Blitz unit can keep
using its remaining attacks, while an exhausted Blitz unit enters the ordinary
post-attack regroup/retreat behavior. Intentional exhaustion handling; no F386.

`AI_leaveAttack`
----------------
The group-head/unit-count arithmetic is genuinely conservative but already
exists in BtS lineage and sits behind live local attack/defense strength checks.
No hard contract was established that the function is supposed to retain a
specific number of INDIVIDUAL defenders rather than sufficient independent
selection groups. Kept as inherited heuristic debt, not F386.

-------------------------------------------------------------------------------
NUKE / AIR / GREAT-PERSON / ESPIONAGE CONTROLS
-------------------------------------------------------------------------------

`AI_ICBMMove` / `AI_nuke`
-------------------------
The advc.650 merge of K-Mod's separate limited-range `AI_nukeRange` into
`AI_nuke` was traced through candidate creation and execution.

Current city and stack targets are still filtered by real:

    canNukeAt(getPlot(), targetX, targetY, getTeam())

before valuation, and the final selected target is asserted legal again before
`MISSION_NUKE`. No loss of limited-range missile legality was established.

`AI_exploreAirPlotValue`, `AI_exploreAirMove`, air retreat/damaged helpers
-------------------------------------------------------------------------
The current active exploration-air logic was reopened. Global `atWar` safely
handles `NO_TEAM`. The older K-Mod `AI_airRetreatFromCityDanger` and
`AI_airAttackDamagedSkip` bodies are retained only as deleted/commented legacy
code in AdvCiv and are not active hidden paths.

`AI_followBombard`
------------------
AdvCiv's active change from unit-level `canBombard(plot())` to the group-level
`getGroup()->canBombard(getPlot())` matches the mission that is immediately
pushed by the group. The large neighboring “wait for another bombard group”
body remains disabled inherited code. No F386.

Great Work / Trade mission execution
------------------------------------
The Great Work and Trade selection/execution paths were reopened because some
runtime legality checks were reduced to assertions.

On the live `AI_greatPersonMove` decision path, the chosen Great Work/Trade
candidate is produced in the same decision pass by valuation helpers that
require the corresponding legal action (`canGreatWork` / `canTrade`). No
supported intervening state mutation was established between candidate choice
and execution.

`AI_tradeMissionValue` does contain a theoretical initial arithmetic expression
using `MAX_INT` path-turn state (`4 + iBestPathTurns`). This is undesirable
signed-overflow territory in strict C++ terms. The first comparison nevertheless
has an initial best value of zero and no supported current toolchain/gameplay
mis-selection was established from it. Retained as numeric cleanup debt rather
than manufacturing F386.

Espionage late helpers
----------------------
`AI_getEspionageTargetValue`, `AI_cityOffenseSpy`, `AI_bonusOffenseSpy`,
`AI_reconSpy` and `AI_infiltrate` were deliberately checked.

* `AI_cityOffenseSpy`'s chained assignment to average/unspent points is odd but
  not behaviorally contradictory in the live computation.
* `AI_reconSpy` keeps the remote recon target separate from the end-turn
  movement plot and preserves both when pushing the mission.
* the old destroy/sabotage/pickup-target Spy routines that appear in source
  history are disabled/deleted legacy bodies rather than active uncovered AI.

No F386 in this tranche.

-------------------------------------------------------------------------------
OTHER INHERITED MEMBER CONTROLS
-------------------------------------------------------------------------------

`AI_guardCityBestDefender` / `AI_guardCityOnlyDefender`
-------------------------------------------------------
The AdvCiv changes are const/API modernization and mission flag typing around
the inherited exact city-best/only-defender checks. No source-vs-target or
counting regression established.

`AI_chokeDefend`
----------------
The flattened version preserves the K-Mod nested conditions:
owned city, multiple needed defenders, sufficient non-city-AI defenders,
limited danger, then a local attack. The optimized danger counter stops after
its threshold but does not alter the branch result around the threshold. No
F386.

`AI_defend` / `AI_defendPlot`
-----------------------------
The SquareIterator rewrite preserves the one-turn reachable defender search.
AdvCiv intentionally makes non-city protection domain-aware and value-sensitive
for vulnerable nondefending units. No iterator-center, source-target or empty
candidate error survived.

`AI_calculatePlotWorkersNeeded`
-------------------------------
The change to owner-aware build time (`getBuildTime(eBuild, getOwner())`) is a
straight correctness improvement; ceiling and bonus doubling remain protected
by positive Worker work rate. No divide/sentinel issue.

`AI_setBirthmark`, `AI_unitBirthmarkHash`, `AI_unitPlotHash`
-----------------------------------------------------------
These are unchanged in semantic substance from K-Mod/Base aside from API
modernization. Circumnavigation direction bits and hash arithmetic are
intentional unsigned behavior.

-------------------------------------------------------------------------------
SAS FILE-LOCAL HELPER RECONCILIATION
-------------------------------------------------------------------------------

The Queue-002 closure inventory was expanded beyond `CvUnitAI::` members to the
file-local SAS helper layer, which member-function inventories cannot see.
This is required because F375 itself originated in such a helper.

Settler helpers checked further
-------------------------------
* first-city food/fog/reveal helpers retain their documented land/water and
  minimum-reveal assumptions;
* `SAS_findBestReachableSettlerSite` stores the end-turn plot at the moment a
  new best candidate is found, so its diagnostic does not use the pathfinder's
  later candidate state;
* `SAS_shouldDelayFoundInPlaceForBetterReachableSite` stores the winning target,
  raw value and path-turn count. Its caller's `getPathEndTurnPlot` usage in the
  delay branch is diagnostic only, not mission execution;
* `SAS_tryAttachCityEscortToSettler` intentionally uses the configurable
  minimum healthy defenders left rather than hard-gating on
  `AI_neededDefenders`; the XML/source rationale explicitly says the latter is
  too conservative for this expansion choice;
* `SAS_shouldBlockDangerousUnescortedFounding` was challenged for its own-
  territory exclusion. The preceding Settler danger path already attempts to
  retreat an undefended threatened Settler; if retreat is impossible, founding
  can itself be safer than waiting exposed. No sufficiently hard contract was
  established to classify the owner condition as F386.

Attack-stack helpers checked further
------------------------------------
* `SAS_releaseTargetlessPeacetimeAttackCityExcess` and
  `SAS_createMissingBarbarianCityExpeditionLeader` genuinely detach converted
  units because `AI_setUnitAIType` calls `joinGroup(NULL)`;
* pathable-vs-overall Barbarian city candidates remain separate through the
  selection helpers; no stale “overall” city is executed after a different
  pathable candidate wins;
* `SAS_getBestUpgradeForLog` is diagnostic-only and now reads each iterated
  unit's own UnitAI rather than the stack head's role.

Worker helpers checked further
------------------------------
* improvement-list caches compare literal pointer first but fall back to string
  equality, so equivalent define-name strings do not create semantic cache
  aliases;
* upgrade-chain traversal is bounded by `getNumImprovementInfos`, preventing an
  XML cycle from running forever;
* food-cost recovery only rewards a configured negative-base-food improvement
  once current plot/player rules have made it food-neutral or positive;
* the no-bonus branch picker can technically return the highest buildable
  candidate even if scores are negative, but current default XML does not
  produce the feared all-negative irrigation case: the irrigation-blocking
  penalty is active only when a real irrigation carrier is available and
  competes in the same branch. No current-data F386.
* KI#214's `(plot, steps)` irrigation-search state remains present.

-------------------------------------------------------------------------------
FINAL-PATTERN SWEEP PROGRESS
-------------------------------------------------------------------------------

Another current-source sweep was run specifically for the root families that
have produced high-value Queue-002 findings:

* raw enum used as boolean;
* same-object `isArea(object.getArea())` tautologies;
* source plot substituted for target plot;
* iterator center/radius mismatch;
* multiple `generatePath` calls followed by stale path-end consumption;
* candidate identity vs executed identity;
* mission target coordination;
* attempted action vs assumed success;
* MAX/MIN sentinel arithmetic.

The known F376 tautology remains the unique surviving same-object area instance
found by the direct pattern search. The known F380 route enum remains the
important raw-enum companion repair. The other current raw `*Type` conditions
found are boolean metadata functions/arrays (`getUnitAIType` etc.), not enum
sentinels.

No independent F386 has crossed the current-source/caller/data threshold in
this checkpoint.

-------------------------------------------------------------------------------
CURRENT STONE
-------------------------------------------------------------------------------

Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F385 CONFIRMED in protected Queue 002.
F386 / provisional KI#709 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP83.
No repository source files modified.

NEXT
----
1. Finish the still-active larger holdouts explicitly: pirate/naval movement,
   corporation spreading, hurry/defend/pillage, remaining city-defense and
   reserve movement, and any active espionage names not yet reconciled.
2. Reduce the exact-name “unmentioned” inventory by separating compiled-out /
   deleted historical bodies from active functions.
3. Finish the file-local static helper inventory, distinguishing diagnostics
   from behavior.
4. Repeat the final function-range + predicate/sentinel/source-target sweep.
5. Only then either confirm a real F386 or freeze a Queue-002 closure checkpoint
   with F386 unused and advance to protected Queue 003.

===============================================================================
C031-WIP84 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F386 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
Unique files complete: 31/169.
F375-F385 remain CONFIRMED.
F386 / provisional KI#709 is CONFIRMED.
F387 / provisional KI#710 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F386 / provisional KI#709 — advc.139 replaced Pirate city/fort-only healing
with a land-threat helper that returns NO threat on water, so lightly damaged
Privateers can stop and heal in open sea
-------------------------------------------------------------------------------

ROOT
----
K-Mod `AI_pirateSeaMove` only offers the early heal action when the Pirate is in
an actual city/fort-like naval base with friendly defense and no nearby danger:

    if (plot()->isCity(true) &&
        AI_localDefenceStrength(...) > 0 &&
        !AI_getAnyPlotDanger(...))
    {
        if (AI_heal())
            return;
    }

Base AdvCiv/current SAS retain the original explanatory comment:

    // heal in defended, unthreatened forts and cities

and show the immediately preceding city/base expression as commented code:

    /*if ((kPlot.isCity() && kPlot.AI_getPlotCity()->AI_isSafe()) ||
        (kPlot.isCity(true) && ...localDefence... && ...no danger...))*/
    // advc.139: Probably better than the above
    if (AI_isThreatenedFromLand() <= PROBABILITY_LOW)
    {
        if (AI_heal())
            return;
    }

The extracted helper's first live condition is:

    if (getPlot().isWater())
        return NO_PROBABILITY;

and the ProbabilityTypes ordering used by this file is:

    NO_PROBABILITY   = -1
    PROBABILITY_LOW  = 0
    PROBABILITY_REAL = 1
    PROBABILITY_HIGH = 2

Therefore every ordinary water tile satisfies:

    NO_PROBABILITY <= PROBABILITY_LOW

and enters the early Pirate healing branch.

This is a scope change, not merely a changed danger threshold. The old condition
required a defended city/fort; the new condition explicitly includes open sea.

RUNTIME PROOF — SINGLE PRIVATEER
--------------------------------
`AI_heal()` immediately delegates a one-unit group to:

    AI_singleUnitHeal()

whose default parameters are:

    iMaxTurnsExposed = 1
    iMaxTurnsOutsideCity = 3

For a non-Barbarian single unit outside city-heal territory,
`AI_singleUnitHeal` heals whenever:

    iHealTurns <= iMaxTurnsExposed

i.e. one exposed healing turn is sufficient.

`CvUnit::healRate` gives a non-friendly, non-enemy tile the standard neutral
heal rate:

    NEUTRAL_HEAL_RATE + getExtraNeutralHeal()

and the inherited BtS/Civ4CE GlobalDefines value is:

    NEUTRAL_HEAL_RATE = 10

Current SAS does not override that define in its supplied XML set.

So a routine single Privateer on neutral/open water with, for example, 10 HP of
damage has:

    healTurns() == 1

and the current call chain becomes:

    AI_pirateSeaMove
      -> AI_isThreatenedFromLand() == NO_PROBABILITY (because water)
      -> NO <= LOW, enter early heal branch
      -> AI_heal()
      -> AI_singleUnitHeal()
      -> push MISSION_HEAL
      -> return from AI_pirateSeaMove

K-Mod's old caller never reached `AI_heal()` on that open-water plot because
`plot()->isCity(true)` was false.

This exact one-turn exposed-heal behavior is not new to current SAS and does not
save the refactor: K-Mod's own single-unit `AI_heal` logic also allowed a
one-turn heal when called outside a city. The important protective contract was
the Pirate CALLER's city/fort gate, and advc.139 removed that gate.

INTERACTION WITH PIRATE-SPECIFIC DAMAGE LOGIC
---------------------------------------------
The broadening changes real action priority, not only which helper is queried.
Later in `AI_pirateSeaMove`, current SAS calls `AI_pirateBlockade`, whose damaged
open-sea logic says roughly:

    if not in immediate stronger-enemy danger and damaged and
       plot not owned and not adjacent owned:
        try AI_retreatToCity(...)
        otherwise skip

That logic is reached only after the new early `AI_heal` branch.

A lightly damaged single Pirate on remote neutral water can therefore now heal
in place and return BEFORE the Pirate-specific retreat/blockade logic has a
chance to route it toward a base or make the normal blockade decision.

For damage requiring more than one neutral turn, `AI_singleUnitHeal()` normally
returns false under its default exposed threshold and the later Pirate logic
still runs. The regression therefore has a clean ordinary boundary rather than
requiring an exotic state:

    <= 10 damage at standard neutral heal rate -> can heal in open sea early
    larger damage -> usually falls through to normal Pirate damage handling

Extra neutral-heal bonuses can widen the affected damage range.

ANCESTRY
--------
K-Mod:
* Pirate early healing explicitly requires `plot()->isCity(true)` plus defense
  and no nearby danger.

AdvCiv advc.139:
* extracts duplicated naval land-threat tests into `AI_isThreatenedFromLand`;
* current source retains the old city/base expression immediately above the new
  helper call and the comment "heal in defended, unthreatened forts and cities";
* the helper deliberately returns `NO_PROBABILITY` for all water plots;
* Pirate uses the unique low-threat predicate `<= PROBABILITY_LOW`.

Base AdvCiv 1.14 and current SAS inherit that extracted condition.

Classification:
    inherited AdvCiv helper-extraction scope regression.

RELATION TO F384
----------------
F384 is a different advc.139/ProbabilityTypes caller defect:
`AI_carrierSeaMove` uses `!= PROBABILITY_REAL` instead of the sibling naval
threshold `>= PROBABILITY_REAL`.

F386 is not that threshold typo. The Pirate's `<= LOW` sense is reasonable for
"heal only when land threat is low" once the location is appropriate. The bug
is that extraction also removed the old city/fort location requirement, while
the helper's water sentinel automatically satisfies the low-threat comparison.

REPAIR DIRECTION
----------------
Restore the location scope while retaining the extracted danger policy.

The closest translation of the retained comment/K-Mod contract is conceptually:

    if (getPlot().isCity(true) &&
        AI_isThreatenedFromLand() <= PROBABILITY_LOW)
    {
        if (AI_heal())
            return;
    }

If current AdvCiv's `isCity(true)` API has been replaced/deprecated, use the
modern team/base predicate that represents an actual naval city/fort heal base
(e.g. the same `isCityHeal`/base semantics used by current healing code), but do
not let ordinary water satisfy the early branch solely because
`AI_isThreatenedFromLand` returns `NO_PROBABILITY` there.

If intentionally allowing minor open-sea healing is desired as NEW Pirate
policy, it should be stated and valued explicitly; it should not arise as an
accidental consequence of a helper whose purpose is land-threat extraction and
whose retained caller comment still says city/fort only.

-------------------------------------------------------------------------------
EXACT-NAME CLOSURE INVENTORY — ADDITIONAL CLASSIFICATION
-------------------------------------------------------------------------------

After WIP83 the exact-name gap had fallen to 22 source names. Several are not
active functions and must not inflate the closure count:

* `AI_potentialEnemy`
  - old name appears only in comments; active AdvCiv replacement is
    `AI_mayAttack`, which has already been audited through many live callers.

* `AI_destroySpy`, `AI_sabotageSpy`, `AI_pickupTargetSpy`
  - explicitly deleted/commented legacy BtS/BBAI bodies.

* `AI_switchHurry`
  - inside `#if 0`, obsolete by K-Mod Great-Person logic.

* old `AI_greatWork`
  - disabled/deleted legacy body; active K-Mod split is
    `AI_greatWorkValue` + `AI_doGreatWork`.

* old `AI_airBombDefenses`
  - only commented/deleted BtS calls/body remain; K-Mod replacement paths are
    active and were audited separately.

Additional active controls:

* `AI_discover` is semantically unchanged from K-Mod apart from refactoring;
  `canDiscover` precedes discovery-tech/value arithmetic and protects the live
  denominator/tech contract.
* `AI_goldenAge` is unchanged from K-Mod.
* `AI_revoltCitySpy` preserves K-Mod's war/disorder/remaining-defense and
  `canDoEspionageMission` checks.
* `AI_doTradeMission` performs a live `canTrade` recheck when actually at the
  trade plot.
* `AI_greatWorkValue` selects only `canGreatWork` cities; the active
  Great-Person pass recomputes candidates each turn before `AI_doGreatWork`.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F386 CONFIRMED in protected Queue 002.
F387 / provisional KI#710 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP84.
No repository source files modified.

NEXT
----
1. Finish the now-small active exact-name set: Pirate Sea/Blockade, corporation
   spreading, religion airlift, sea bombard, pillage and remaining active
   wrappers.
2. Finish file-local static behavior helpers vs diagnostics.
3. Run the final function-range and root-pattern reconciliation one more time.
4. Only then decide whether Queue 002 closes with F387 unused or yields another
   supported root.

===============================================================================
C031-WIP85 — PROTECTED QUEUE 002 CvUnitAI.cpp — POST-F384 RECONCILIATION
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.
31/169 unique files complete.
F375-F386 CONFIRMED.
F387 / provisional KI#710 UNUSED.
Existing KI#213 retained/reconfirmed.
No repository source files modified.

POST-WIP84 CONTROLS PRESERVED
-----------------------------
The exact member-function reconciliation is now essentially saturated.
Previously unnamed active members such as `AI_cityAutomated`,
`AI_groupMergeRange`, `AI_spreadCorporation`, `AI_spreadReligionAirlift`,
`AI_seaBombardRange` and `AI_pillage` were directly compared against their
K-Mod/Base/current contracts and no independent F387 survived.

The remaining apparent member-name holdouts such as `AI_canPillage`,
`AI_foundRange`, `AI_seaRetreatFromCityDanger`, `AI_finalOddsThreshold` and
`AI_hurry` are compiled-out/deleted legacy code in current AdvCiv/SAS rather
than unreviewed live behavior.

The SAS file-local helper layer was deliberately opened because it does not
appear in a `CvUnitAI::` member inventory. Additional checks covered:
* first-city food/fog scoring and raw-vs-path-adjusted candidate comparisons;
* remote captured-city attack-stack trap helpers;
* Barbarian expedition sizing and city-attack capability counts;
* peaceful ATTACK_CITY excess-release state;
* military-concentration diagnostics;
* Worker no-bonus branch parsing and selection;
* improvement-upgrade-chain / growth-value helpers;
* irrigation-carrier and irrigation-chain bookkeeping.

No independent F387 was established from those helpers in this slice.

The earlier Worker concern where `SAS_pickWorkerNoBonusBranchBuild` could return
its highest buildable candidate even after negative adjustments was tested
against current default XML. The irrigation-blocking penalty activates only
when an irrigation-carrying candidate is actually available; the feared
Farm-impossible low-yield all-negative state was not established.

`AI_guardYield` remains an explicitly UNPROMOTED design-debt candidate.
When a unit is already guarding an outer-ring improvement, the function obtains
`pCity = getPlot().getWorkingCity()` but scans `CityPlotIter(getPlot())`, i.e.
centered on the guard unit rather than the recovered city. AI sleeping groups
are force-updated and can reevaluate, so the shifted footprint is real and can
retarget onto an improved owned tile outside the original city's BFC. However,
the current source/history still does not establish a hard contract that the
post-arrival guard search must remain confined to that city's BFC; a one-hop
nearby-improvement policy remains plausible. Do not allocate F387 without a
stronger ancestry/semantic control.

Late inherited controls also closed cleanly:
* AdvCiv's nuke-range merge retains real `canNukeAt` legality for finite-range
  missile targets.
* `AI_afterAttack`'s Blitz change intentionally delays post-attack regroup /
  retreat until the Blitz unit has exhausted its attacks.
* Great Work / Trade candidates remain selected from same-pass legality-tested
  destinations; no supported illegal execution state was established.
* `AI_routeCity` advc.121 does not perform the pointless backtrack initially
  suspected: `MISSION_ROUTE_TO` first builds on the current tile and then
  routes toward its endpoint, so the queued endpoint route is completing the
  unfinished connection rather than merely walking back to the first city.
* `AI_leaveAttack`'s group-head-vs-group-size comparison is conservative and
  inherited from BtS; no hard individual-unit-count contract was established.

F380 companion repair notes remain unchanged:
* restore the missing `!isVassal(...)`;
* test `getRevealedRouteType(...) != NO_ROUTE` explicitly;
* reconcile the radius-1 `SquareIter` with the otherwise unreachable
  `iDistance == 2` route-danger branch.
The latter two remain masked/companion defects rather than separately numbered
findings under the current F380 execution path.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F386 CONFIRMED.
F387 / provisional KI#710 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP85.
No repository source files modified.

NEXT
----
Finish the remaining file-local helper classification and the final whole-file
predicate/sentinel/source-target/path-state/candidate-execution reconciliation.
Only then decide whether Queue 002 closes with F387 unused or yields another
supported root.

===============================================================================
C031-WIP86 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — F387 CONFIRMED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains OPEN.

Unique files complete: 31/169.
F375-F386 remain CONFIRMED.
F387 / provisional KI#710 is CONFIRMED.
F388 / provisional KI#711 is UNUSED.
Existing KI#213 remains retained/reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F387 / provisional KI#710 — AdvCiv `AI_guardYield` recovers the guarded tile's
working city but centers its next "city plot" search on the GUARD UNIT instead,
allowing a high-yield guard to drift outside that city's radius and breaking
the function's own one-hop-to-city safety rationale
-------------------------------------------------------------------------------

ROOT
----
AdvCiv added `AI_guardYield` under advc.300 to:

    // Protect high-yield tiles from Barbarians

The helper starts by identifying the relevant owned city:

    CvCity const* pCity = getPlot().getPlotCity();

    // For now, only consider nearby city if already guarding bonus.
    if (pCity == NULL &&
        AI_getGroup()->AI_getMissionAIType() == MISSIONAI_GUARD_BONUS)
    {
        pCity = getPlot().getWorkingCity();
    }

    if (pCity == NULL || pCity->getOwner() != getOwner())
        return false;

So after a guard has reached an improved tile in a city radius, the function
explicitly recovers that tile's working city.

However, `pCity` is then not used to define the candidate population.

Current Base AdvCiv 1.14 and SAS execute:

    for (CityPlotIter it(getPlot()); it.hasNext(); ++it)
    {
        CvPlot& kLoopPlot = *it;
        ...
    }

This is not an iterator over `pCity`.

`CityPlotIterator.h` explicitly defines the `CvPlot` constructor as:

    "Iterator over the CvPlot instances in the city radius
     around a plot in the center."

and stores:

    m_iCenterX(kCenter.getX()),
    m_iCenterY(kCenter.getY())

Therefore:

    CityPlotIter(getPlot())

means the 21-tile city-radius SHAPE centered on the unit's current guard plot.

The function had already recovered `pCity`, but searches a different footprint.

WHY THE FIRST CALL LOOKS CORRECT
--------------------------------
The defect is easy to miss because the normal first invocation occurs while a
Reserve unit is in an owned city.

At that moment:

    getPlot() == pCity->plot()

so:

    CityPlotIter(getPlot())

and:

    CityPlotIter(*pCity)

would describe the same center.

The function can select a high-value improved tile and queues:

    MISSIONAI_GUARD_BONUS

for that tile.

Only after the unit arrives does `getPlot()` cease to be the city plot.

THE GUARD REALLY RE-EVALUATES AFTER ARRIVAL
-------------------------------------------
This is not a dormant second-call path.

When the guard is on its yield tile it normally fortifies/skips with:

    MISSIONAI_GUARD_BONUS

AI-controlled groups that are not executing `ACTIVITY_MISSION` are force-
updated at the turn boundary. `CvSelectionGroup::doTurn` sets:

    setForceUpdate(true);
    clearMissionQueue();

for such AI groups.

`clearMissionQueue()` removes mission queue entries but does NOT clear the
selection group's `m_eMissionAIType` / mission-AI plot.

`CvSelectionGroup::readyToMove()` explicitly treats:

    isForceUpdate()

as sufficient to re-enter AI updating even when the group had been sleeping.

Consequently the fortified yield guard re-enters `AI_reserveMove` and reaches
`AI_guardYield` on later turns while its `MISSIONAI_GUARD_BONUS` identity is
still available.

The branch:

    pCity = getPlot().getWorkingCity();

is therefore live exactly as its comment implies.

LIVE GEOMETRY — SEARCH FOOTPRINT DRIFTS OUTWARD
------------------------------------------------
Take city C and an improved outer-ring plot A that C can work.

The standard Civ4 city geometry includes radius-two positions such as:

    C = (0,0)
    A = (2,0)

Once the unit is guarding A:

    pCity == C

but the current loop is equivalent to a new city radius centered at:

    A = (2,0)

That shifted footprint contains positions such as:

    B = (3,0)

which are outside C's real city radius.

If B is:
* owned by the same player;
* improved;
* land;
* not a city;
* currently empty;
* high-yield enough;
* sufficiently near invisible terrain;
* reachable from A in one path turn;

then B can outrank A and become `pBestPlot`.

Nothing in `AI_guardYield` requires:

    B.getWorkingCity() == pCity

or even:

    B.getWorkingCity() != NULL.

The unit can therefore move from a genuine worked/working-city yield tile to an
improved plot that the recovered city cannot work at all.

On the following turn, if B lies outside every city assignment:

    getPlot().getPlotCity() == NULL
    getPlot().getWorkingCity() == NULL

so the same `AI_guardYield` function immediately returns false and the
high-yield guard assignment has effectively dissolved.

BROKEN "HURRY BACK TO THE CITY" CONTRACT
-----------------------------------------
The function explains its one-turn path restriction explicitly:

    /* Must be reachable in one hop, so that we can hurry back to the city
       when it's in danger. */

It checks:

    generatePath(kLoopPlot, ..., &iPathTurns)
    iPathTurns <= 1

But `generatePath` starts from THIS UNIT'S CURRENT position.

On the first call, while the unit is in C, that does mean:
    candidate is one path turn from the city.

On a later call from outer-ring A it only means:
    new candidate B is one path turn from A.

It provides no guarantee that B is one turn from C.

A guard can therefore chain one legal one-turn step outward and end at a plot
that takes more than one turn to return from, contradicting the exact safety
rationale used to justify the path bound.

This also interacts with `AI_reserveMove` ordering. The earlier general
`AI_guardCity(false, true, 2)` search has a two-turn limit. Once a guard drifts
far enough from its original city, the ordinary high-priority city-guard branch
is no longer guaranteed to rediscover that city when it becomes endangered.

UNUSED `pCity` IS AN INTERNAL CONTROL
-------------------------------------
After ownership validation, `pCity` is not used anywhere else in
`AI_guardYield`.

That is particularly revealing because the special post-arrival branch exists
solely to recover a city from the current guarded tile:

    pCity = getPlot().getWorkingCity();

If the intended candidate population were simply "a 21-tile neighborhood
around wherever the unit currently stands," there would be little reason to
recover the exact working city rather than only test that the current tile is
near/owned by a city.

The recovered object is exactly what `CityPlotIter` can accept directly:

    CityPlotIter(*pCity)

but the implementation passes:

    CityPlotIter(getPlot())

instead.

ANCESTRY
--------
K-Mod 1.46 does not contain `AI_guardYield`.

Base AdvCiv adds it under advc.300, with:
* the "Protect high-yield tiles from Barbarians" caller comment;
* the explicit `pCity` / `getWorkingCity()` recovery;
* the "hurry back to the city" one-hop rationale;
* the current `CityPlotIter(getPlot())` center.

Current AdvCiv-SAS inherits that structural behavior unchanged.

Classification:
    inherited AdvCiv advc.300 state/scope regression.

WHY THIS IS STRONGER THAN THE WIP85 UNPROMOTED NOTE
----------------------------------------------------
C031-WIP85 deliberately left this unnumbered because a generic "protect nearby
improvements one hop at a time" policy was still plausible.

The final closure pass supplied the missing contracts:

1. `CityPlotIterator.h` proves that the CvPlot overload is literally centered
   on the current guard plot; it does not silently resolve the working city.

2. the AI group lifecycle proves the `MISSIONAI_GUARD_BONUS` reevaluation path
   executes after fortifying; it is not merely theoretical state.

3. the function's own one-hop comment states that the path restriction is
   supposed to preserve rapid return TO THE CITY. After the first move, current
   code measures the hop from the guard tile instead.

4. the shifted iterator admits candidates outside `pCity`'s radius, including
   candidates with no working city, after which the helper can stop applying
   entirely.

Together those establish a concrete state transition that contradicts the
function's stated safety invariant.

REPAIR DIRECTION
----------------
At minimum, keep the candidate population tied to the recovered city:

    for (CityPlotIter it(*pCity); it.hasNext(); ++it)

rather than:

    for (CityPlotIter it(getPlot()); it.hasNext(); ++it)

If "high-yield tile" means an actually usable tile for that city, a stronger
version can use / require the corresponding working-city contract as
appropriate rather than counting arbitrary owned overlap plots.

The one-hop safety rule should also be made relative to `pCity`, not merely
relative to the guard's current location on subsequent reevaluations.

A conservative design would:
* validate that the current guarded tile is still a valid high-yield candidate
  for `pCity`;
* stay if so;
* otherwise choose another candidate from `pCity`'s real radius;
* ensure the chosen guard position remains within the intended rapid-return
  distance to `pCity`.

Do not allow repeated "one hop from where I am now" retargeting to substitute
for "one hop from the city."

-------------------------------------------------------------------------------
FINAL-SWEEP CONTROLS CHECKED BEFORE F387
-------------------------------------------------------------------------------

SAS file-local Worker / attack helpers
--------------------------------------
The remaining behavior helpers were deliberately reopened.

* `SAS_getImprovementUpgradeChainLevel`
  - bounded by `GC.getNumImprovementInfos()`, so malformed/cyclic XML upgrade
    chains cannot loop forever.

* `SAS_getWorkerGrowthImprovementLevel`
  - takes the maximum valid configured chain level; no cross-chain stale state.

* `SAS_getWorkerRecoveredFoodCostImprovementValue`
  - only rewards configured improvements once current plot/player modifiers
    make their food effect nonnegative; no reversed "partial recovery" bonus.

* `SAS_isWorkerBuildActivelyCommitted`
  - empty mission queues are safe because `getMissionType(0)` returns the
    mission sentinel when no node exists; stale build progress alone is not
    treated as active commitment.

* `SAS_findWorkerIrrigationChainStep`
  - KI#214's `(plot,steps)` state remains intact;
  - generation-stamped cost storage is reset on size change / unsigned
    generation wrap;
  - route outputs are initialized before search;
  - returned source-side build candidates remain owned, same-area, flat,
    potential-irrigation-valid and buildable.

* targetless peaceful ATTACK_CITY release
  - aggregate plot counts are recomputed after role conversion;
  - `AI_setUnitAIType()` detaches converted units through `joinGroup(NULL)`,
    so the release is not merely a label change inside the old stack.

No independent F388 emerged from those helpers in this slice.

`AI_canRouteThroughSafeTerritory`
---------------------------------
The helper ignores the bool return from its first ordinary `generatePath` and
stores `iOriginalPathTurns` through an output parameter.

That looks like a possible uninitialized read in isolation.

Its live callers, however, reach it only for candidate destinations already
path-tested by the corresponding Worker selection path (`AI_bestCityBuild`,
`AI_improveBonus`, or the legacy local candidate path).

Moreover, the second path is strictly more restrictive (`MOVE_SAFE_TERRITORY`);
a successful safe path with no ordinary path is not a supported state under
the current movement contract.

No live uninitialized-path consequence was established; no F388.

`AI_tradeMissionValue`
----------------------
The initial comparison contains:

    iBestPathTurns = MAX_INT
    4 + iBestPathTurns

which overflows signed 32-bit arithmetic on the legacy compiler model.

At that initial state the paired `iBestValue` numerator is zero, and the
comparison still supplies the intended zero baseline on the actual Civ4
toolchain. No current city ranking mis-selection was established.

This remains worthwhile numeric cleanup / hardening, not F388.

Raw enum / area predicate sweep
-------------------------------
The whole-file scan reconfirmed the known strong cases rather than finding a
new sibling:

* F376:
    `kPlot.isArea(kPlot.getArea())` tautology in `AI_connectBonus`.

* F380 companion:
    raw `getRevealedRouteType(getTeam())` boolean in
    `AI_airOffenseBaseValue`.

Other current `getRevealedRouteType` uses in CvUnitAI explicitly compare with
`NO_ROUTE`.

No additional live raw-enum-as-bool or self-area predicate crossed the F388
threshold in this slice.

CURRENT STONE
-------------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` — OPEN.
31/169 unique files complete.
F375-F387 CONFIRMED in protected Queue 002.
F388 / provisional KI#711 UNUSED.
Existing KI#213 retained/reconfirmed.
Latest physical checkpoint: C031-WIP86.
No repository source files modified.

NEXT
----
Continue from F388 unused.

Priority:
1. finish the remaining whole-file source-vs-target / mission-target /
   stale-path-state sweeps now that the static-helper inventory is saturated;
2. run one final exact active member + file-local helper coverage reconciliation;
3. recheck the late F380/F385/F387 neighborhoods for masked sibling defects;
4. if no independent F388 survives, freeze a Queue-002 closure checkpoint,
   increment unique files complete to 32/169, and open protected Queue 003
   according to the album's protected route.

===============================================================================
C031-WIP87 — PROTECTED QUEUE 002 CvUnitAI.cpp DEEP RELOOP — CLOSED
===============================================================================

STATUS
------
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` is CLOSED under the deeper WIP-era
re-audit methodology.

Unique files complete: 32/169.
Confirmed Queue-002 deep-reloop findings: F375 through F387.
F388 / provisional KI#711 remains UNUSED.
Existing cross-file KI#213 remains retained/reconfirmed.
Old F002/KI#331 Queue-002 `scrap()` caller family remains repaired/saturated in
current source.
No repository source files were modified.

Next protected target:
    Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp`

-------------------------------------------------------------------------------
QUEUE 002 CLOSURE BASIS
-------------------------------------------------------------------------------

This closure is not based on reaching the physical end of `CvUnitAI.cpp`.

The protected deeper reloop ultimately included all of the following:

1. CURRENT SAS vs Base AdvCiv behavioral-delta reconciliation
-------------------------------------------------------------
The full current-SAS member-function surface was compared against Base AdvCiv.
Large/high-risk current rewrites were deliberately reopened rather than treated
as already reviewed merely because they had appeared in the original pass.

Major tranches included:
* `AI_bestCityBuild` and its Worker execution callers;
* first-capital / Settler scouting and founding;
* attack-city / target-city / garrison state;
* Great Person / Great General actions;
* promotion policy;
* naval assault, loading, pickup, ferrying and stranded rescue;
* upgrade travel / retreat;
* air/naval safety;
* espionage;
* city-site / bonus / fort guarding.

Every genuinely current-SAS-changed `CvUnitAI::` member was accounted for in
the physical WIP chain.

2. Exact active-member reconciliation
--------------------------------------
A final definition inventory found 226 active `CvUnitAI::` member definitions
in current source.

All 226 active members now have explicit coverage in the physical audit chain.

Apparent name holdouts after the semantic passes were checked individually.
The remaining names were compiled-out/deleted legacy bodies rather than active
runtime behavior.

3. SAS file-local helper reconciliation
----------------------------------------
Member-function inventories do not see file-local/static helpers, so this layer
was audited separately before closure.

Behavioral helper families checked included:
* first-city found-value / food-environment / fog-scouting helpers;
* Settler escort and remote-captured-city trap helpers;
* Barbarian city-expedition / attack-stack capability and cap helpers;
* peaceful targetless ATTACK_CITY release machinery;
* military-concentration helpers;
* Worker no-bonus branch parsing/selection;
* improvement-list / improvement-upgrade-chain helpers;
* recovered-food and irrigation-carrier value helpers;
* KI#214 irrigation-chain search/state;
* active Worker-build commitment detection.

Diagnostic/name/logging helpers were classified separately and checked not to
feed diagnostic state back into gameplay decisions.

This static-helper pass is important because F375 itself originated in the
file-local `countImprovedTiles` layer.

4. K-Mod -> Base AdvCiv inherited-change reconciliation
-------------------------------------------------------
Queue 002 was not closed after only comparing SAS with its immediate base.

K-Mod -> Base AdvCiv changed functions were reopened, with Civ4CE/BtS ancestry
where needed. AdvCiv-only helpers were separately enumerated because they have
no K-Mod counterpart and therefore disappear from an ordinary common-function
diff.

This pass materially produced/reinforced inherited roots such as:
* F376 (`AI_connectBonus`);
* F380 (`AI_airOffenseBaseValue`);
* F382 (`AI_pickup`);
* F384 (`AI_carrierSeaMove`);
* F385 (`AI_guardCitySite`);
* F386 (`AI_pirateSeaMove`);
* F387 (`AI_guardYield`).

5. Mechanical predicate-refactor mining
---------------------------------------
The K-Mod -> AdvCiv diff was mined independently for condition transformations,
especially:
* lost/added negations;
* nested-positive-block -> early-continue flattening;
* enum-returning getters used as booleans;
* candidate/source operand substitutions;
* area/domain optimizations.

The suspicious pairs were then checked in their full function/caller context.

Most apparent polarity changes are valid logical flattenings.

The strongest surviving semantic mistakes are already represented by the
confirmed finding set, notably:
* F382's safe/dangerous pickup inversion;
* F384's Carrier `ProbabilityTypes` threshold inversion.

No independent F388 survived this independent reverse-direction check.

6. Pathfinder-state reconciliation
-----------------------------------
All uses of:
* `getPathEndTurnPlot()`;
* `getPathFinder().getPathTurns()`;
* `getPathFinder().getFinalMoves()`;
* `getPathLastNode()`

were mechanically inventoried and the potentially nonlocal cases were
inspected for stale shared-pathfinder state.

The final pass specifically challenged cases where:
* a candidate path is generated;
* other AI/value/helper calls occur;
* path metrics or end-turn state are consumed later.

No independent F388 survived.

Important controls:
* stored `CvPlot*` / references returned from `getPathEndTurnPlot()` point to
  stable map plots; later path searches do not invalidate those plot objects;
* candidate scoring calls such as city value, local attack/defense value,
  mission-count queries and weighted combat odds do not regenerate this
  unit group's movement path before the saved path metrics are consumed;
* `AI_solveBlockageProblem` looks vulnerable because `AI_goToTargetCity` can
  regenerate paths, but that branch is returned immediately, so execution
  cannot fall through and consume the old blockage path afterward;
* the unchecked first `generatePath` in `AI_canRouteThroughSafeTerritory`
  remains a robustness smell, but its live callers have already established
  ordinary pathability and the second safe-territory path is more restrictive;
  no supported successful-safe / failed-ordinary state was established.

7. Source-vs-target spatial-context sweep
-----------------------------------------
The whole file was rechecked for the class that produced F378, F381 and F387:
a loop/caller selects one destination but a helper accidentally evaluates the
current/source plot.

The final mechanical scan covered:
* candidate-centered `SquareIter` / adjacency loops containing `getPlot()`;
* current-plot operands inside destination scoring;
* transport source vs enemy-coast contexts;
* city/working-city center selection;
* guard destination vs donor/source plot selection.

The surviving roots are already numbered:
* F378 — source plot used to choose defender sent to another city;
* F381 — passenger coast used instead of enemy coast;
* F387 — current guard tile used as the next city-radius center.

Other current-plot uses were confirmed to describe true source state, e.g.
launch position for nukes, current border ownership for pillage-DoW
discounting, or current Worker assignment for worker-count adjustment.

8. Mission-target / lifetime reconciliation
-------------------------------------------
Mission coordination was checked across:
* FOUND;
* GUARD_CITY;
* GUARD_BONUS;
* BUILD;
* PICKUP;
* ASSAULT;
* BLOCKADE;
* SPREAD;
* espionage mission families.

This produced/confirmed:
* F379 — ordinary targeted safe-city guard mission intercepted by an
  emergency-only continuation policy;
* F385 — exact city-site guard target lost when AdvCiv changed to an
  adjacent-only duplicate scan.

A proposed F385 sibling in Settler safety was explicitly rejected: an adjacent
fogbuster is not physically an escort on the founding tile, so the Settler's
stricter exact-site guard requirement is coherent.

9. Sentinel / raw-enum / arithmetic sweep
------------------------------------------
The final pass reconfirmed:
* F376's tautological self-area predicate;
* the F380 companion raw `RouteTypes` boolean problem.

Other current `getRevealedRouteType` consumers explicitly compare to
`NO_ROUTE`.

The `AI_tradeMissionValue` initial `MAX_INT + 4` expression is numeric cleanup
debt on the legacy compiler model, but its initial paired numerator is zero and
no supported ranking failure was established.

No independent F388 was allocated.

-------------------------------------------------------------------------------
QUEUE-002 CONFIRMED FINDINGS — FINAL RANGE
-------------------------------------------------------------------------------

F375 / KI#698
    SAS `countImprovedTiles` counts physical improvements outside the city's
    actual workable population, letting foreign/unowned/other-city improvements
    inflate Worker readiness.

F376 / KI#699
    AdvCiv `AI_connectBonus` uses
        kPlot.isArea(kPlot.getArea())
    in the reject condition, a tautology that makes the helper always skip and
    return false.

F377 / KI#700
    SAS `AI_betterPlotBuild` reversed `isFeatureRemove`, allowing a legal
    Forest-preserving Camp to be replaced by a pure Forest chop.

F378 / KI#701
    `AI_guardCity` ranks the unit to detach against the SOURCE plot, then sends
    it to a different destination city; current promotion data gives a direct
    Combat-I vs City-Garrison-I rank reversal.

F379 / KI#702
    SAS's central emergency GUARD_CITY continuation intercepts older targeted
    safe-city rebalance missions and reruns them with `bDangerOnly=true`, so a
    still-valid safe target can be abandoned after one movement turn.

F380 / KI#703
    AdvCiv SquareIterator rewrite dropped `!` from the vassal predicate in
    `AI_airOffenseBaseValue`; normal hostile borders are ignored while friendly
    vassal borders contribute danger.

    Mandatory companion repairs, intentionally not double-numbered while
    masked by F380:
    * compare `getRevealedRouteType(...) != NO_ROUTE` explicitly;
    * reconcile the current radius-1 SquareIterator with the otherwise
      unreachable inherited `iDistance == 2` route-danger branch.

F381 / KI#704
    K-Mod/AdvCiv `AI_load(... iMaxTransportPath)` scans an enemy coastal target
    but path-tests water adjacent to the passenger's CURRENT plot rather than
    adjacent to that enemy coast.

F382 / KI#705
    AdvCiv `AI_pickup` early-continue flattening inverted K-Mod's city-danger
    gate; remote pickup travel rejects safe cities and admits dangerous ones.

F383 / KI#706
    SAS `AI_ferryWorkers` hard area-capacity gate counts still-loaded cargo
    Workers as already delivered; the one-Worker case subtracts zero and can
    reject the destination before `unloadAll()`.

F384 / KI#707
    AdvCiv `ProbabilityTypes` conversion changed Carrier land-threat handling
    to:
        AI_isThreatenedFromLand() != PROBABILITY_REAL
    rather than the sibling/naval threshold `>= PROBABILITY_REAL`.

F385 / KI#708
    AdvCiv `AI_guardCitySite` replaced K-Mod's exact-site duplicate mission
    check with an adjacent-only scan despite saying adjacent tiles should be
    checked "too"; exact-site guard missions can be duplicated.

F386 / KI#709
    AdvCiv's land-threat helper extraction broadened Pirate early healing from
    defended/unthreatened city/fort state to open water because
    `AI_isThreatenedFromLand()` returns NO_PROBABILITY on water and the caller
    accepts `<= PROBABILITY_LOW`.

F387 / KI#710
    AdvCiv `AI_guardYield` recovers the guarded tile's working city but centers
    `CityPlotIter` on the current guard tile. A guard can chain outward beyond
    that city's radius, contradicting the function's stated one-hop-to-city
    safety rationale and potentially losing its working-city association.

-------------------------------------------------------------------------------
FINAL F388 CONTROLS — NO NUMBER ALLOCATED
-------------------------------------------------------------------------------

No independent F388 survived the final closure slice.

`AI_guardYield` was the last late candidate to cross the finding threshold and
became F387. The subsequent path-state, mission-target, source-target,
predicate-refactor, raw-enum and exact-coverage sweeps did not establish
another current root.

Notable unpromoted controls retained for future review:
* `AI_leaveAttack` group-head/unit-count arithmetic is conservative but already
  inherited from BtS and lacks a hard contract proving individual-unit rather
  than defender-group semantics.
* `AI_connectBonusCost` contains dead Worker-busy multiplier logic after the
  Fort weighting rewrite, but that multiplier would be constant across the
  builds compared for one plot and no current ranking consequence was proven.
* `AI_travelToUpgradeCity` can structurally stop on one economically rejected
  upgrade candidate, but current branching upgrade graphs/UnitAI legality did
  not provide a live first-fails/second-passes caller state.
* `AI_ferryWorkers` has a softer city-cache/cargo-range inconsistency beyond
  F383, but the hard area-capacity gate prevented an independent over-delivery
  proof.
* `AI_calculateUnitAIViability`/UU-style concerns belong to other files/contracts
  and are not a new Queue-002 root.
* `AI_tradeMissionValue` signed-overflow-looking initial arithmetic is hardening
  debt, not a demonstrated gameplay defect on the actual legacy runtime.

-------------------------------------------------------------------------------
QUEUE 002 FINAL STONE
-------------------------------------------------------------------------------

Queue 002:
    `CvGameCoreDLL/CvUnitAI.cpp`
    DEEP RELOOP COMPLETE / CLOSED

Unique files complete:
    32 / 169

Queue-002 confirmed finding range:
    F375 - F387

Next finding:
    F388 UNUSED
    provisional KI#711

Existing root retained:
    KI#213

Latest physical checkpoint:
    C031-WIP87

Repository source modifications:
    NONE

PROTECTED ROUTE
---------------
Open Queue 003:
    `CvGameCoreDLL/CvGameTextMgr.cpp`
    22,412 queued lines

The original Queue-003 pass was marked:
    REVIEWED / NEW FINDING + EXISTING ROOT REOPENED

The deeper reloop should first reconcile that original finding and reopened
existing root against CURRENT source, then repeat the stronger methodology now
used for Queues 001-002:
* current SAS vs Base AdvCiv semantic deltas;
* K-Mod/Civ4CE ancestry where relevant;
* AdvCiv-only helper/member coverage;
* file-local helper coverage;
* formatting/index/sentinel/visibility/state contracts;
* exact function-range reconciliation;
* final mechanical pattern sweeps.

Do not resume ordinary Queue 020 until protected Queues 003 through 009 have
all received the deeper reloop.

END C031-WIP87

===============================================================================
C031-WIP88 — PROTECTED QUEUE 003 CvGameTextMgr.cpp DEEP RELOOP — OPEN
===============================================================================

STATUS
------
Queue 001 `CvGameCoreDLL/CvPlayerAI.cpp` remains CLOSED.
Queue 002 `CvGameCoreDLL/CvUnitAI.cpp` remains CLOSED.
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` is OPEN under the deeper WIP-era
methodology.

Unique files complete: 32/169.
F388 / provisional KI#711 is UNUSED.
No repository source files were modified.

-------------------------------------------------------------------------------
OPENING RECONCILIATION — OLD F004 / provisional KI#332 IS FIXED
-------------------------------------------------------------------------------

The original Queue-003 pass established a wrong-civilization leader-hover root:
multiple Python `WIDGET_PEDIA_JUMP_TO_LEADER` callers passed literal
`data2 = 1`, while native `CvDLLWidgetData::parseLeaderHelp` interprets data2
as a `CivilizationTypes` value and forwards it to
`CvGameTextMgr::parseLeaderTraits`.

That could resolve trait-dependent UnitClass / BuildingClass entries through
Arabia (civilization enum 1) regardless of the displayed leader; Shaka's
Aggressive Barracks-class line could therefore name ordinary Barracks instead
of the Zulu Ikhanda.

CURRENT SAS no longer has that active caller convention in the reconciled UI
surface checked for this pass.

The active Scoreboard, Hall of Fame, Info Screen, Military Advisor and Sevopedia
leader-hover callers now supply the actual civilization or use the canonical
leader-to-civilization resolution used by the current leader page.

Conclusion:
old F004 / provisional KI#332 remains historical evidence but is FIXED in
current SAS. Do not allocate F388 for it.

-------------------------------------------------------------------------------
OPENING RECONCILIATION — EXISTING KI#21 DYNAMIC-STATE SUBCASE STILL SURVIVES
-------------------------------------------------------------------------------

The old Queue-003 reopening of KI#21 remains valid.

The live-unit `setUnitHelp` map-view "Cannot Enter" path still tests the actual
unit's raw XML restrictions:

    pUnit->getTerrainImpassable(eTerrain)
    pUnit->getFeatureImpassable(eFeature)

and prints those terrain/feature names without applying the unit team's
current passable-tech state.

Elsewhere, the static/detailed unit help correctly understands:

    getTerrainPassableTech(...)
    getFeaturePassableTech(...)

and the movement engine also allows raw impassable terrain/feature once the
team has the configured passable technology.

Current-data control:
`UNIT_WORKBOAT` has Ocean marked impassable and Astronomy as its terrain
passable tech. After the team researches Astronomy, movement can be legal while
live map help can still print Ocean in "Cannot Enter."

Classification:
existing KI#21 dynamic-state display consequence, not a new F388.

Repair boundary:
live-unit restriction prose must describe effective current movement legality,
or at minimum suppress a raw terrain/feature restriction when the current team
has the configured passable tech, while preserving any cultural-border nuance.

-------------------------------------------------------------------------------
OPENING CONTROL — SELF-REFERENTIAL DEFAULT-SPECIALIST CACHE STILL DATA-MASKED
-------------------------------------------------------------------------------

Current source still contains:

    static const SpecialistTypes eDefaultSpecialist =
        (SpecialistTypes)eDefaultSpecialist;

rather than obtaining the global default specialist.

Static zero-initialization makes the self-read resolve to enum 0. Current data
has enum 0 = `SPECIALIST_CITIZEN`, which is also the configured default
specialist.

The source typo is therefore real but its current runtime value is exactly the
intended value.

Retain as known masked archaeology / cleanup debt. Do not manufacture F388
without a current data/configuration consequence.

-------------------------------------------------------------------------------
CURRENT QUEUE-003 DELTA SURFACE
-------------------------------------------------------------------------------

Current `CvGameTextMgr.cpp`:
    ~22,407 physical lines
    ~230 member definitions

Opening current-SAS vs Base AdvCiv reconciliation identified a finite changed
surface of roughly:
    29 changed member bodies
    + 2 current added helpers (`buildBTradeString`, `buildBRepeatString`)

High-value current deltas include:
* `setUnitHelp`;
* `setBasicUnitHelp`;
* trait parsing;
* civic parsing;
* building / project requirement prose;
* Great-People help;
* event help;
* trade-route / espionage-cost help;
* plot / air-strike / air-bomb / paradrop help.

The deeper reloop will classify these first, then expand to inherited
K-Mod/Civ4CE behavior, AdvCiv-only functions, file-local helpers if any, and an
exact member-range / sentinel / index / visibility / formatting-state sweep.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388 / provisional KI#711 UNUSED.
Old F004/KI#332 wrong-civilization hover root: fixed in current SAS.
Existing KI#21 dynamic-state live-unit "Cannot Enter" subcase: reconfirmed.
Default-specialist self-cache typo: still exactly data-masked.
Latest physical checkpoint: C031-WIP88.
No repository source files modified.

NEXT
----
Continue from F388 unused.

Priority:
1. classify the current-SAS vs Base AdvCiv changed member bodies, beginning
   with live/static unit help and trait/civic/building/project requirement
   contracts;
2. explicitly check all ID/enumeration/sentinel conversions used for text
   lookups and widget-facing contexts;
3. reconcile K-Mod/Civ4CE ancestry for any candidate root;
4. only after current deltas are saturated widen to exact member coverage and
   final formatting/index/visibility/state pattern sweeps.

===============================================================================
C031-WIP89 — PROTECTED QUEUE 003 CvGameTextMgr.cpp DEEP RELOOP — F388 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388 / provisional KI#711 is CONFIRMED.
F389 / provisional KI#712 is UNUSED.
Old F004/KI#332 remains fixed in current SAS.
Existing KI#21 live-unit dynamic-state "Cannot Enter" subcase remains
reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F388 / provisional KI#711 — SAS tech-tree obsolete-unit consolidation ignores
civilization context and globally lists every civilization's UU
-------------------------------------------------------------------------------

ROOT
----
Current SAS added an obsolete-unit consolidation block to
`CvGameTextMgr::setTechTradeHelp`:

    if (!bCivilopediaText)
    {
        ...
        if (!bTreeInfo)
        {
            CvWString szObsoleteUnits;
            bool bAnyObsoleteUnits = false;

            FOR_EACH_ENUM(Unit)
            {
                CvUnitInfo const& kLoopUnitInfo =
                        GC.getInfo(eLoopUnit);

                if (kLoopUnitInfo.getObsoleteTech() == eTech)
                {
                    ...
                    szObsoleteUnits +=
                        kLoopUnitInfo.getDescription();
                }
            }

            if (bAnyObsoleteUnits)
                append TXT_KEY_TECH_OBSOLETES...
        }
    }

The code iterates every `UnitInfo` in the game.

It never asks:
* which UnitClass the active civilization can train;
* which UnitType that civilization maps the UnitClass to;
* whether the listed UnitType belongs to the active civilization at all.

This differs from the immediately preceding obsolete-BUILDING logic in the
same function.

That existing code iterates BuildingClasses and, in active-player context,
resolves each class through:

    kGame.getActiveCivilization()->getBuilding(eLoopBuildingClass)

before asking whether the resolved building is obsoleted by `eTech`.

The new Unit block therefore discarded the same civilization-resolution
contract for units.

LIVE CALLER — TECH TREE
-----------------------
This is not restricted to generic/no-player help.

`CvDLLWidgetData::parseTechTreeHelp` calls:

    GAMETEXT.setTechHelp(
        szBuffer,
        eTech,
        false,  // bCivilopediaText
        !GET_TEAM(getActiveTeam()).isHasTech(eTech),
        false,
        false); // bTreeInfo

`setTechHelp` forwards those values to `setTechTradeHelp`.

For an unresearched tech in the normal Tech Tree:
    bCivilopediaText = false
    bPlayerContext   = true
    bTreeInfo        = false

Therefore the new obsolete-unit block executes in an explicitly
active-player-context hover.

Despite that, it still lists all global UnitTypes.

CURRENT-DATA PROOF — CIVIL SERVICE
----------------------------------
Current SAS Unit XML has nine UnitInfos with:

    ObsoleteTech = TECH_CIVIL_SERVICE

They are:

Generic:
    UNIT_AXEMAN
    UNIT_SWORDSMAN

Civilization-specific:
    UNIT_SUMERIAN_VULTURE
    UNIT_NATIVE_AMERICA_DOG_SOLDIER
    UNIT_AZTEC_JAGUAR
    UNIT_CELTIC_GALLIC_WARRIOR
    UNIT_ROMAN_LEGIONARY
    UNIT_PERSIA_IMMORTAL
    UNIT_INCAN_QUECHUA

Take an Arabian active player.

Current `CIVILIZATION_ARABIA` has no Axeman- or Swordsman-class replacement;
its current unique-unit mapping in this portion of the tree is unrelated
(`UNITCLASS_CAMEL_KNIGHT -> UNIT_ARABIA_HAJJAN`).

When that player hovers unresearched Civil Service in the Tech Tree, the new
global Unit loop can therefore produce one comma-separated "Obsoletes" line
containing:
* Axeman;
* Swordsman;
* Vulture;
* Dog Soldier;
* Jaguar;
* Gallic Warrior;
* Legionary;
* Immortal;
* Quechua.

Seven of those are UUs belonging to other civilizations and can never be the
Arabian player's corresponding unit.

The same class repeats at other current techs.

For example current `TECH_FIREARMS` is the ObsoleteTech of sixteen UnitInfos:
generic Mace/Pike/Longbow/Crossbow/Knight/Camel-Knight units plus Samurai,
Viking, Skirmisher, Bowman, Cho-Ko-Nu, Benin Disciplined Crossbowman, Yeoman
Archer, Cataphract, Khishigten and Arabia's Hajjan.

A player-context hover therefore mixes:
* the player's real UnitClass mapping;
* generic defaults that may have been replaced;
* other civilizations' replacements.

WHY `bPlayerContext` DOES NOT SAVE IT
-------------------------------------
The outer function knows whether player context is active.

The obsolete-building branch uses that context to resolve civilization-specific
types.

The new obsolete-unit block never references `bPlayerContext`,
`eActivePlayer`, `getActiveCivilization()`, or UnitClass mappings.

So this is not merely a generic encyclopedia-style listing accidentally viewed
during play; the caller explicitly requested player-context help and the new
block ignores that contract.

WHY THE CIVILOPEDIA PANEL DOES NOT SAVE IT
-------------------------------------------
The new block is deliberately disabled when:

    bCivilopediaText == true

because current Sevopedia Tech has a dedicated Obsoletes panel.

F388 occurs in the non-Civilopedia Tech Tree hover, where that dedicated panel
does not replace the text being generated.

The suppression therefore makes the player-context hover itself the primary
surface for this new consolidated Unit line.

ANCESTRY
--------
Base AdvCiv 1.14 does not contain this global obsolete-Unit consolidation
block.

Its obsolete-building handling already performs civilization-class resolution,
while bonus/special-building obsolescence is naturally global.

Current SAS added the Unit block directly while consolidating obsolete prose.

Classification:
    SAS-local civilization-context UI regression.

This is conceptually related to the old F004/KI#332 leader-hover context bug,
but it is an independent current root:
* old F004: caller supplied the wrong civilization ID to trait help;
* F388: the current caller supplies active-player context correctly, but the
  newly added Unit loop ignores civilization context internally.

REPAIR DIRECTION
----------------
Mirror the existing BuildingClass pattern.

For player-context help, iterate UnitClasses and resolve each class through the
active civilization:

    UnitTypes eLoopUnit;

    if (eActivePlayer != NO_PLAYER)
        eLoopUnit =
            kGame.getActiveCivilization()->getUnit(eLoopUnitClass);
    else
        eLoopUnit =
            GC.getInfo(eLoopUnitClass).getDefaultUnit();

then test the resolved UnitType's `getObsoleteTech()`.

A generic/no-player help mode can use default units, consistent with the
building branch.

Do not globally enumerate every UnitInfo for a player-context Tech Tree hover.

If the intended UI instead wants an "all civilizations" encyclopedia list, it
needs to be explicitly limited to a generic context and clearly labeled as
such; it must not silently replace active-civilization semantics.

-------------------------------------------------------------------------------
CONTROLS COMPLETED BEFORE F388
-------------------------------------------------------------------------------

* Current leader-hover callers were rechecked first:
  old F004/KI#332 is fixed; current callers no longer use the literal
  civilization-1 convention established by the original Queue-003 audit.

* Existing KI#21 still survives independently:
  live-unit map help prints raw Terrain/Feature impassability without applying
  current passable-tech state.

* The self-referential static default-specialist cache remains source-wrong but
  exactly data-masked because enum 0 and current DEFAULT_SPECIALIST are both
  `SPECIALIST_CITIZEN`.

* `parseCivicInfo`'s new CivicPercentAnger number is paired with the current
  customized `TXT_KEY_CIVIC_FREEDOM_ANGER` format key; no stale parameter-type
  mismatch was established.

* `parseGreatPeopleHelp`'s obsolete-building GPP split correctly separates
  obsolete and active building GPP for transparency and does not alter the
  underlying city total.

* null-selection guards in combat/air/paradrop/revolt help are defensive
  hardening; the target-eligible aircraft/paratrooper selection changes repair
  already-known UI mismatch classes rather than introduce a new root.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388 / provisional KI#711 CONFIRMED.
F389 / provisional KI#712 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP89.
No repository source files modified.

NEXT
----
Continue from F389 unused.

Priority:
1. finish the remaining current-SAS vs Base changed functions, especially
   `setBasicUnitHelp`, `setBuildingHelpActual`, trait resolution,
   building/project requirements, espionage/trade/event and plot/air help;
2. inspect all current added helpers and their call contexts;
3. reconcile any candidate against K-Mod/Civ4CE and current XML/text-key
   contracts;
4. then widen to inherited/AdvCiv-only member coverage and final
   enum/index/visibility/state formatting sweeps.

===============================================================================
C031-WIP90 — PROTECTED QUEUE 003 CvGameTextMgr.cpp DEEP RELOOP — F389 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388 remains CONFIRMED.
F389 / provisional KI#712 is CONFIRMED.
F390 / provisional KI#713 is UNUSED.
Old F004/KI#332 remains fixed in current SAS.
Existing KI#21 live-unit dynamic-state "Cannot Enter" subcase remains
reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F389 / provisional KI#712 — AdvCiv founding-health hover prints feature
unhealth instead of negative PLAYER extra health
-------------------------------------------------------------------------------

ROOT
----
AdvCiv's advc.004b `CvGameTextMgr::setFoundHealthHelp` computes surrounding
feature health first:

    int iGoodHealth = iGoodHealthPercent / 100;
    int iBadHealth = iBadHealthPercent / 100;

and later obtains the active player's global extra-health modifier:

    int iExtraHealth =
        GET_PLAYER(getActivePlayer()).getExtraHealth();

For positive extra health it correctly prints `iExtraHealth`:

    if (iExtraHealth > 0)
    {
        iIcon = gDLL->getSymbolID(HEALTHY_CHAR);
        szBuffer.append(
            CvWString::format(L"%d%c", iExtraHealth, iIcon));
    }

But the negative branch prints a different local variable:

    else
    {
        iIcon = gDLL->getSymbolID(UNHEALTHY_CHAR);
        szBuffer.append(
            CvWString::format(L"%d%c", iBadHealth, iIcon));
    }

`iBadHealth` is the number of rounded unhealthy points contributed by FEATURES
around the proposed city site.

It has no relation to the player's negative `iExtraHealth`.

The function therefore displays the local feature-unhealth value in the slot
that is explicitly meant to describe global player extra health.

LIVE CALLER
-----------
This is ordinary founding-mission UI, not dead Civilopedia code.

`CvDLLWidgetData` handles `MISSION_FOUND`.

When the selected Settler can found on the mission plot it calls:

    GAMETEXT.setFoundCostHelp(...)
    GAMETEXT.setHomePlotYieldHelp(...)
    GAMETEXT.setFoundHealthHelp(...)

before displaying the founding action help.

Current `UNIT_SETTLER`:
* has `bFound = 1`;
* has no prerequisite technology;
* has `ObsoleteTech = NONE`.

So this hover remains live throughout the game, including the Future Era.

NEGATIVE PLAYER EXTRA HEALTH IS LIVE IN CURRENT SAS
---------------------------------------------------
Current `CvPlayer::getExtraHealth()` is not constrained nonnegative.

Player-level health is modified by, among other sources:
* traits through `CvPlayer::processTraits`;
* civics through `CvPlayer::processCivics`;
* technologies through `CvTeam::processTech`;
* events.

In particular current SAS has:

    TECH_DEPOPULATION
        iHealth = -9

and `CvTeam::processTech` applies every technology's health directly to each
team member:

    kMember.changeExtraHealth(
        kTech.getHealth() * iChange);

The prerequisite graph gives an especially clean current-data state.

For `TECH_DEPOPULATION`:
* OR prerequisite: `TECH_COMPOSITES`;
* AND prerequisites:
  - `TECH_SATELLITES`;
  - `TECH_COMPUTERS`;
  - `TECH_ENVIRONMENTALISM`.

Among every technology reachable in the full prerequisite ancestry of
Depopulation, the only other nonzero health modifier is:

    TECH_ENVIRONMENTALISM
        iHealth = +1

The other current positive-health technologies
(Bioengineering, Alien Life, Genetics, AI, Transhumanism) are not required
ancestors of Depopulation; several are descendants/alternatives after it.

Therefore a player with:
* no health-changing trait;
* no health-changing civic;
* no health-changing event;
* only the technologies necessary to reach Depopulation,

has a technology-derived player extra-health total of:

    Environmentalism +1
    Depopulation     -9
    ------------------
    total            -8

So the `iExtraHealth < 0` branch is directly reachable in current SAS.

CONCRETE DISPLAY FAILURE
------------------------
Take that player after researching Depopulation and select a Settler over a
valid founding tile whose revealed city radius has no unhealthy feature
contribution.

Then:

    iExtraHealth = -8
    iBadHealth   = 0

`setFoundHealthHelp` executes:

    szBuffer.append(L"+");

then enters the negative extra-health branch and prints:

    iBadHealth == 0

with the unhealthy icon.

The result is effectively:

    +0 [unhealthy] "from trait"

despite the player actually carrying 8 points of global extra unhealth.

If the proposed site happens to have, for example, 2 unhealthy feature points,
the same player-global line instead prints 2 — still unrelated to the actual
-8 player modifier.

Thus the displayed number changes with the proposed site's feature health even
though the underlying `getExtraHealth()` value is global and unchanged.

INTERNAL FORMAT CONTROL
-----------------------
The ordinary city-health breakdown elsewhere in the same file already shows
the correct negative-player-health convention.

For an existing city:

    int iHealth =
        GET_PLAYER(kCity.getOwner()).getExtraHealth();

    if (iHealth < 0)
    {
        szBuffer.append(
            CvWString::format(L"%d%c ",
                -iHealth,
                gDLL->getSymbolID(UNHEALTHY_CHAR)));
        szBuffer.append(
            gDLL->getText("TXT_KEY_FROM_TRAIT"));
    }

So the intended negative branch is explicit:
* use `-iExtraHealth` as the positive magnitude;
* pair it with the unhealthy icon.

The founding helper's use of `iBadHealth` is not an alternative formatting
policy; it is a wrong-variable typo.

ANCESTRY
--------
K-Mod 1.46 and Civ4CE/BtS do not contain `setFoundHealthHelp`.

Base AdvCiv 1.14 contains the same advc.004b helper and the same wrong-variable
negative branch:

    CvWString::format(L"%d%c", iBadHealth, iIcon)

Current SAS inherits that line unchanged.

Classification:
    inherited AdvCiv advc.004b UI wrong-variable defect.

REPAIR DIRECTION
----------------
Mirror the existing-city health formatting:

    else
    {
        iIcon = gDLL->getSymbolID(UNHEALTHY_CHAR);
        szBuffer.append(
            CvWString::format(
                L"%d%c",
                -iExtraHealth,
                iIcon));
    }

The surrounding function already prepends `"+"` because healthy/unhealthy
icons encode the sign semantically in this compact founding preview.

A separate wording cleanup may eventually be appropriate because
`TXT_KEY_FROM_TRAIT` labels the aggregate player `getExtraHealth()` value even
though that value can also contain civic, technology and event effects.
That inherited wording imprecision is broader than F389 and is not required
for the numeric correctness repair.

-------------------------------------------------------------------------------
CONTROLS COMPLETED IN THIS SLICE
-------------------------------------------------------------------------------

* `setFoundHealthHelp` is a live `MISSION_FOUND` hover path; its Future-Era
  reachability does not depend on keeping an obsolete Settler because current
  Settlers have `ObsoleteTech = NONE`.

* Current health-tech ancestry was checked rather than assuming Depopulation's
  -9 could survive prerequisites. The mandatory prerequisite health total is
  only Environmentalism's +1, yielding a direct -8 current-data state without
  optional traits/civics/events.

* Existing city-health help already uses `-iHealth` for negative player extra
  health, providing an in-file formatting contract for the repair.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F389 CONFIRMED in protected Queue 003.
F390 / provisional KI#713 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP90.
No repository source files modified.

NEXT
----
Continue from F390 unused.

Priority:
1. finish the remaining current-SAS vs Base changed member reconciliation:
   building/project requirements, trait resolution, espionage/trade/event and
   plot/air help;
2. audit AdvCiv-only text helpers for wrong-variable / stale-context siblings;
3. reconcile text-key parameter counts/types and enum/class resolution;
4. then widen to exact active-member coverage and final
   enum/index/visibility/state formatting sweeps.

===============================================================================
C031-WIP91 — PROTECTED QUEUE 003 CvGameTextMgr.cpp DEEP RELOOP — F390 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F389 remain CONFIRMED.
F390 / provisional KI#713 is CONFIRMED.
F391 / provisional KI#714 is UNUSED.
Old F004/KI#332 remains fixed in current SAS.
Existing KI#21 live-unit dynamic-state "Cannot Enter" subcase remains
reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F390 / provisional KI#713 — AdvCiv relabeled aggregate PLAYER extra health as
"from Traits" even though the accumulator also contains civics, technologies
and events
-------------------------------------------------------------------------------

ROOT
----
Current city-health help uses the PLAYER-level aggregate:

    GET_PLAYER(kCity.getOwner()).getExtraHealth()

for both bad and good health.

AdvCiv advc.004g replaced K-Mod's neutral civilization-source text with:

    TXT_KEY_FROM_TRAIT

Current XML defines that key as:

    English: " from Traits"
    German:  "durch Merkmale des Herrschers"
             ("through leader traits")

So the UI explicitly attributes the complete `getExtraHealth()` value to
leader Traits.

That attribution does not match the data represented by `getExtraHealth()`.

CURRENT ACCUMULATOR SOURCES
---------------------------
`CvPlayer::m_iExtraHealth` is modified by several independent systems.

Traits:
    CvPlayer::processTraits
        changeExtraHealth(iChange * kTrait.getHealth());

Civics:
    CvPlayer::processCivics
        changeExtraHealth(
            GC.getInfo(eCivic).getExtraHealth() * iChange);

Technologies:
    CvTeam::processTech
        kMember.changeExtraHealth(
            kTech.getHealth() * iChange);

Events:
    CvPlayer::applyEvent
        changeExtraHealth(kEvent.getHealth());

Therefore:

    CvPlayer::getExtraHealth()

is a global player modifier accumulator, not a Trait accumulator.

CURRENT-DATA POSITIVE PROOF — ORGANIZED RELIGION
------------------------------------------------
Current SAS has:

    CIVIC_ORGANIZED_RELIGION
        iExtraHealth = +1

and:

    TRAIT_PROTECTIVE
        iHealth = +1

as the ordinary current health Trait.

Take a leader without Protective and adopt Organized Religion, with no other
player-level health modifier active.

Then:

    getExtraHealth() == +1

entirely because of the CIVIC.

Current city good-health help nevertheless executes:

    int iHealth =
        GET_PLAYER(kCity.getOwner()).getExtraHealth();

    if (iHealth > 0)
    {
        append "+1 [healthy]"
        append TXT_KEY_FROM_TRAIT;
    }

and displays the civic-derived health as:

    +1 [healthy] from Traits

despite no health Trait contributing anything.

CURRENT-DATA NEGATIVE PROOF — DEPOPULATION
------------------------------------------
F389 already established a particularly clean current technology state.

Current:

    TECH_ENVIRONMENTALISM  iHealth = +1
    TECH_DEPOPULATION      iHealth = -9

and Environmentalism is the only nonzero-health mandatory prerequisite in the
Depopulation ancestry.

A non-Protective player with no health civic/event can therefore have:

    getExtraHealth() == -8

from technology alone.

Existing-city bad-health help correctly prints the magnitude:

    8 [unhealthy]

but then appends:

    TXT_KEY_FROM_TRAIT

so the tech-derived penalty is presented as:

    8 [unhealthy] from Traits

This is independent of F389.

F389 is the wrong-variable numeric bug in `setFoundHealthHelp`, where the
negative founding preview prints local feature bad health instead of the
negative player extra-health magnitude.

F390 affects the SOURCE ATTRIBUTION of the aggregate value in:
* existing-city good-health breakdown;
* existing-city bad-health breakdown;
* founding-health preview once the numeric branch is repaired.

Even a correct F389 numeric repair would still display:
    8 unhealthy from Traits
for a technology-derived -8 state.

EVENTS PROVIDE ANOTHER LIVE SOURCE
----------------------------------
Current event XML also contains player events with positive `iHealth`
(e.g. +1, +2 and +3 values).

`CvPlayer::applyEvent` applies those directly through `changeExtraHealth`.

So the wrong attribution is not limited to one custom Civic or one Future Era
technology; the accumulator has always had a broader source contract.

ANCESTRY
--------
K-Mod's city health breakdown uses:

    TXT_KEY_MISC_HEALTH_FROM_CIV
    TXT_KEY_MISC_GOOD_HEALTH_FROM_CIV

for negative/positive:

    GET_PLAYER(city.getOwner()).getExtraHealth()

That wording is broad enough for an aggregate civilization/player modifier.

Base AdvCiv 1.14 comments those neutral keys out under advc.004g and instead
formats the number/icon manually followed by:

    TXT_KEY_FROM_TRAIT

without changing `getExtraHealth()` or separating its contributing sources.

Current SAS inherits the same advc.004g source attribution.

Classification:
    inherited AdvCiv advc.004g UI source-attribution regression.

WHY THE SAME KEY IS NOT ALWAYS WRONG
------------------------------------
`TXT_KEY_FROM_TRAIT` also appears in current commerce/trade help.

Those uses were checked separately.

`CvPlayer::getFreeCityCommerce(eCommerce)` is changed in current C++ only by:

    CvPlayer::processTraits
        changeFreeCityCommerce(
            eCommerce,
            iChange * kTrait.getCommerceChange(eCommerce));

So "from Traits" is correct for that use.

The trade-yield modifier line explicitly iterates the player's actual Traits
and sums `getTradeYieldModifier`, also making the label correct there.

F390 is therefore not a complaint about the localization key itself. The bug is
using a Trait-specific key for the multi-source `getExtraHealth()` accumulator.

REPAIR DIRECTION
----------------
The minimal robust repair is to restore neutral aggregate wording for
`getExtraHealth()`.

For example, re-use/adapt the prior civilization/global-modifier text rather
than `TXT_KEY_FROM_TRAIT`.

A higher-fidelity UI could decompose the aggregate into known sources:
* Traits;
* current Civics;
* researched Technologies;
* persistent Events / other residual modifiers.

However events and other persistent mutations mean reconstructing every source
perfectly from current static configuration may require additional bookkeeping.

Therefore a neutral label such as:
    "from player modifiers"
or the former:
    "from civilization"
is safer than falsely attributing the aggregate to Traits.

The founding preview should use the same neutral aggregate label after F389's
numeric repair.

-------------------------------------------------------------------------------
ADVCIV-ONLY HELPER CONTROL — SPEED BONUS RESOURCE MISMATCH IS DATA-DORMANT
-------------------------------------------------------------------------------

The AdvCiv-only `setBonusExtraHelp(eBonus, ...)` contains a genuine dormant
source defect in its unit speed-bonus handling.

It loops a unit's speed-bonus entries:

    for (int j = 0; j < kUnit.getNumSpeedBonuses(); j++)
        iSpeed = kUnit.getExtraMoves(j);

but never checks:

    kUnit.getSpeedBonuses(j) == eBonus

before using `iSpeed` while generating help for the current bonus `eBonus`.

Semantically, a unit with a nonzero speed bonus for resource A could therefore
be listed as receiving that movement bonus in the help for unrelated resource
B.

`CvUnitInfo` stores these as explicit `(BonusType, ExtraMoves)` pairs, and the
actual movement logic checks ownership of the corresponding bonus, so the help
code's missing resource comparison is real.

Current SAS Unit XML, however, contains ZERO `<SpeedBonuses>` entries.

Therefore no current UnitType can trigger the erroneous help branch.

Classification for this checkpoint:
    dormant inherited AdvCiv helper defect / future XML hazard;
    NOT F391 under the current-data proof threshold.

-------------------------------------------------------------------------------
OTHER CONTROLS COMPLETED IN THIS SLICE
-------------------------------------------------------------------------------

`TXT_KEY_FROM_TRAIT` commerce uses
----------------------------------
Checked all live current uses.

* free-city Commerce:
  only `processTraits` calls `changeFreeCityCommerce`;
  Trait attribution is correct.

* trade-yield modifier:
  help explicitly sums actual Trait `getTradeYieldModifier`;
  Trait attribution is correct.

Thus the health source is the exceptional wrong use rather than a globally bad
key.

`setBonusExtraHelp` team overload
---------------------------------
A superficially suspicious `GET_TEAM(eActivePlayer)`-style call is safe in the
current CoreAI interface because `getTeam` is overloaded for `PlayerTypes` and
converts through the player's team ID.

No enum-domain F391.

`setFoodHelp`
-------------
`healthRate()` is nonpositive by contract for unhealth-food loss, so:

    iSpoiledFood = -healthRate();

has the expected nonnegative magnitude.

No sign inversion.

`setFoundCostHelp`
------------------
The founding mission target is the selected Settler's current plot; using that
plot's ownership for unit-supply cost is consistent with the MISSION_FOUND
execution surface.

No stale target context established.

`setDiscoverPathHelp`
---------------------
The temporary-tech exploration uses the purpose-built
`setHasTechTemporarily` mechanism and restores state. No persistent tech-side
effect was established.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F390 CONFIRMED in protected Queue 003.
F391 / provisional KI#714 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP91.
No repository source files modified.

NEXT
----
Continue from F391 unused.

Priority:
1. finish AdvCiv-only bonus/trade/research/production/founding/finance helpers;
2. return to current changed building/project/trait requirement contexts;
3. audit text-key argument arity/type and civilization/class resolution
   mechanically;
4. then build exact active-member/helper coverage and begin the final
   index/sentinel/visibility/state formatting sweeps.


===============================================================================
C031-WIP92 — PROTECTED QUEUE 003 CvGameTextMgr.cpp DEEP RELOOP — F391 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F391 are CONFIRMED in protected Queue 003.
F392 / provisional KI#715 is UNUSED.
Old F004/KI#332 remains fixed in current SAS.
Existing KI#21 live-unit dynamic-state "Cannot Enter" subcase remains
reconfirmed.
No repository source files were modified.

-------------------------------------------------------------------------------
F391 / provisional KI#714 — production-decay hover reports the wrong amount on
non-Normal game speeds because the BULL getter ignores game-speed scaling
-------------------------------------------------------------------------------

ROOT
----
The current unit/building hover path calls:

    setProductionDecayHelp(...,
        pCity->getUnitProductionDecay(eUnit), ...)

or:

    setProductionDecayHelp(...,
        pCity->getBuildingProductionDecay(eBuilding), ...)

and the text explicitly says, for example:

    "Will lose %d1[ICON_PRODUCTION] this turn"

or:

    "Will lose %d1[ICON_PRODUCTION] in %d2 turns"

The shared BULL getter generated in `CvCity.cpp` is:

    int CvCity::get##Order##ProductionDecay(...) const
    {
        int const iProduction = get##Order##Production(...);
        return iProduction -
            (iProduction *
            GC.getDefineINT(CvGlobals::ORDER##_PRODUCTION_DECAY_PERCENT)) / 100;
    }

That computes the decay amount as though the game-speed production percentage
were always 100.

The actual runtime decay in `CvCity::doDecay`, however, is:

    int const iGameSpeedPercent = ...getConstructPercent();
    ...
    setBuildingProduction(eBuilding, iProduction -
        (iProduction * (100 - iDecayPercent) +
         iGameSpeedPercent - 1) /
        iGameSpeedPercent);

and analogously uses `getTrainPercent()` for units.

Therefore the actual amount LOST is:

    ceil(iProduction * (100 - iDecayPercent) / iGameSpeedPercent)

whereas the UI getter effectively reports:

    ceil(iProduction * (100 - iDecayPercent) / 100)

The two agree only when the relevant game-speed percentage is 100 (or by
accidental integer rounding for a particular production amount).

CURRENT CONFIG / LIVE SURFACE
-----------------------------
The feature is not hidden behind a dormant option.

Current:

    Assets/Config/BUG City Screen.xml

sets:

    ProductionDecayQueue default="True"

and `setUnitHelp` / `setBuildingHelp` use that same option for the decay hover.

Current SAS has multiple non-100% game speeds:

    Nitro       Train/Construct = 33%
    Turbo       Train/Construct = 50%
    Quick       Train/Construct = 67%
    Normal      Train/Construct = 100%
    Epic        Train/Construct = 150%
    Marathon    Train/Construct = 200%
    Slow        Train/Construct = 300%
    Very Slow   Train/Construct = 400%

The inherited BtS production-decay defines are:

    BUILDING_PRODUCTION_DECAY_PERCENT = 99
    UNIT_PRODUCTION_DECAY_PERCENT     = 98

and current SAS has no override of those standard values in its modified XML
surface.

CONCRETE CURRENT EXAMPLES
-------------------------
Take 300 stored production in a building on Slow (ConstructPercent = 300).

UI getter:

    300 - floor(300 * 99 / 100)
    = 300 - 297
    = 3 production

Actual `doDecay` loss:

    ceil(300 * (100 - 99) / 300)
    = ceil(300 / 300)
    = 1 production

The hover can therefore say:

    Will lose 3 [production]

when the runtime will remove only 1.

For a unit with 300 stored production on Slow:

UI getter:

    300 - floor(300 * 98 / 100)
    = 6

Actual runtime loss:

    ceil(300 * 2 / 300)
    = 2

so the tooltip can report 6 when only 2 are lost.

The direction reverses on fast speeds. With 300 building production on Quick
(ConstructPercent = 67):

UI getter:

    3

Actual runtime loss:

    ceil(300 / 67)
    = 5

Thus this is not merely a conservative warning; it can either overstate or
understate the actual loss depending on game speed.

NORMAL-SPEED CONTROL
--------------------
At 100% speed the formulas coincide:

    ceil(P * (100 - decayPercent) / 100)

so a Normal-speed-only test would mask the defect. This likely explains why the
BULL-derived display can appear correct in ordinary testing.

ANCESTRY
--------
BtS/Civ4CE and K-Mod already have the game-speed-scaled runtime `doDecay`
formula.

The production-decay warning getters and `CvGameTextMgr::setProductionDecayHelp`
were imported into AdvCiv by pre-SAS AdvCiv commit:

    0c4bd84a716240c6c6889acaa15b51a1d9ce5105
    "Production decay warnings from BULL"

The introducing diff added the getter with `/ 100` while leaving the existing
runtime decay calculation scaled by `iGameSpeedPercent`.

Classification:
    inherited AdvCiv/BULL UI calculation regression against the inherited BtS
    runtime decay formula.

REPAIR DIRECTION
----------------
Make `getBuildingProductionDecay` / `getUnitProductionDecay` mirror the actual
runtime formula, including the corresponding Construct/Train game-speed
percentage.

Conceptually:

    int const iGameSpeedPercent = ...get##Verb##Percent();
    int const iDecayPercent =
        GC.getDefineINT(CvGlobals::ORDER##_PRODUCTION_DECAY_PERCENT);
    return (iProduction * (100 - iDecayPercent) +
            iGameSpeedPercent - 1) /
           iGameSpeedPercent;

Ideally the runtime and preview should share one helper so the two formulas
cannot drift again.

-------------------------------------------------------------------------------
F391 CONTROL — RESEARCH MODIFIER BREAKDOWN CAN HIDE A TERM, BUT CURRENT
GROUNDBREAKING ENABLES ARE OFF
-------------------------------------------------------------------------------

The previously open research-tooltip candidate is a real configurable display
hazard but does NOT qualify as a separate current-data finding.

`CvGameTextMgr::setResearchModifierHelp` obtains ordinary known-team / tech-
diffusion contribution in `iFromOtherKnown` and independently computes:

    iFromGroundbreaking = -groundbreakingPenalty(eTech);

It then intentionally groups those into one display slot and chooses which
label/value to show based on the sign of their SUM:

    bool const bShowOtherKnown =
        (iFromOtherKnown + iFromGroundbreaking > 0);

If both are nonzero, one real component is omitted from the breakdown even
though `calculateResearchModifier` applies both to the actual total.

However current SAS configuration has:

    TECH_DIFFUSION_ENABLE                    = 0
    AI_GROUNDBREAKING_PENALTY_ENABLE         = 0
    HUMAN_GROUNDBREAKING_PENALTY_ENABLE      = 0

The ordinary BtS known-team research bonus still exists while
`TECH_DIFFUSION_ENABLE = 0`, but `groundbreakingPenalty` returns 0 for both
human and AI players because both groundbreaking enable defines are 0.

Therefore the simultaneous positive-diffusion + negative-groundbreaking state
cannot occur under current configuration.

Current era XML does contain nonzero maximum groundbreaking penalties in
several eras, so enabling either groundbreaking define would immediately make
the hidden-component behavior reachable. The XML comment itself documents the
intentional "only the sum" UI policy.

Classification:
    configurable / dormant AdvCiv UI decomposition limitation;
    NOT an additional numbered finding under the current-data proof threshold.

-------------------------------------------------------------------------------
OTHER CONTROLS CONTINUED IN THIS SLICE
-------------------------------------------------------------------------------

`setProductionSpeedHelp`
------------------------
Its color-selection expression would be questionable for negative resource
production modifiers, because absence of a harmful bonus is colored positive
rather than neutral. Current SAS XML contains no negative bonus- or Trait-
production modifiers, so no live consequence was established.

`buildFinanceSpecialistGoldString`
----------------------------------
The helper excludes disorder cities; this matches the broader city-commerce /
trade suppression behavior during disorder. No supported F392 from that branch.

`setProductionDecayHelp` turn threshold
---------------------------------------
The `iTurnsLeft` / threshold presentation is consistent with the corresponding
production-time getter. F391 concerns the AMOUNT passed into the text, not the
turn countdown.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F391 CONFIRMED in protected Queue 003.
F392 / provisional KI#715 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP92.
No repository source files modified.

NEXT
----
Continue from F392 unused.

Priority:
1. finish remaining AdvCiv-only trade / plot-health / finance / revolt helpers;
2. return to current changed building/project/trait requirement contexts;
3. mechanically audit text-key argument arity/type and civilization/class
   resolution;
4. build exact active-member/helper coverage and run final index/sentinel/
   visibility/state-formatting sweeps before deciding whether Queue 003 closes.

END C031-WIP92


===============================================================================
C031-WIP93 — QUEUE 003 CONTINUED — F392 / KI#715 CONFIRMED
===============================================================================

Continuation stone:
    Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN
    32/169 unique files complete
    F388-F391 confirmed entering this slice
    F392 / provisional KI#715 unused entering this slice
    no repository source files modified

-------------------------------------------------------------------------------
F392 / KI#715 — SETTLER FOUNDING-COST HOVER CHARGES NORMAL MAINTENANCE FOR
WLTKD CITIES THAT ACTUALLY CONTINUE PAYING ZERO MAINTENANCE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current-data UI/state mismatch.

Current SAS `CvGameTextMgr::setFoundCostHelp` projects the expenses after
founding a new city. For each existing city it does:

    FOR_EACH_CITY (c, kPlayer)
    {
        if (c->isDisorder()) // Can't account for these
            continue;
        int iProjected =
                c->calculateDistanceMaintenanceTimes100() +
                c->calculateCorporationMaintenanceTimes100() +
                CvCity::calculateNumCitiesMaintenanceTimes100(..., 1) +
                CvCity::calculateColonyMaintenanceTimes100(..., 1);
        iProjPreInfl += (iProjected *
                std::max(0, c->getMaintenanceModifier() + 100)) / 100;
    }

It therefore treats every city that is not in `isDisorder()` as if its normal
post-founding maintenance will actually be charged.

That is not the runtime maintenance contract.

`CvCity::isDisorder()` is only:

    return (isOccupation() || GET_PLAYER(getOwner()).isAnarchy());

whereas actual maintenance suppression is controlled by:

    bool CvCity::isNoMaintenance() const
    {
        return (isDisorder() || isWeLoveTheKingDay() || getPopulation() <= 0);
    }

and `CvCity::updateMaintenance()` does:

    int iNewMaintenance = 0;
    if (!isNoMaintenance())
    {
        iNewMaintenance = calculateBaseMaintenanceTimes100() *
                std::max(0, getMaintenanceModifier() + 100) / 100;
    }

So a city celebrating We Love the King Day (WLTKD) is NOT in disorder, but it
pays exactly zero city maintenance.

WHY FOUNDING DOES NOT MAKE THE PROJECTION TRUE
----------------------------------------------
The mismatch survives the actual founding transition rather than merely
comparing the hover with a stale pre-action state.

During `CvCity::init`, after the new city increments the team/game city counts,
the owner immediately runs:

    kOwner.updateMaintenance();

`CvPlayer::updateMaintenance()` updates every city, and each old WLTKD city
again executes `CvCity::updateMaintenance()`. Its WLTKD flag is still set, so
`isNoMaintenance()` remains true and its actual maintenance remains 0.

Founding another city does not clear WLTKD. The flag is reconsidered in the
city's ordinary `doTurn` WLTKD logic, not as a side effect of founding a city
elsewhere.

Thus the preview is not validly anticipating that the celebration will end.
It includes maintenance that the runtime explicitly continues suppressing
immediately after the city is founded.

WHY THE DISPLAY OVERSTATES THE INCREMENT
----------------------------------------
After constructing the projected pre-inflation total, `setFoundCostHelp`
subtracts:

    kPlayer.calculateInflatedCosts();

Current actual inflated costs are based on:

    calculateUnitCost() + calculateUnitSupply() + getTotalMaintenance() +
    getCivicUpkeep() + extraExpenses

and `getTotalMaintenance()` already contains zero maintenance for each WLTKD
city.

For an existing WLTKD city, therefore:

    projected side: full normal projected city maintenance is added
    current side:   0 city maintenance is subtracted

instead of comparing 0 before with 0 after.

This can overstate the displayed incremental founding cost by approximately
the entire projected maintenance of every WLTKD city, after the same city and
inflation modifiers, rather than merely by a rounding unit.

LIVE CURRENT SAS SURFACE
------------------------
This is not merely an inherited dormant AdvCiv edge case.

Base AdvCiv 1.14 effectively disables celebrations through:

    WE_LOVE_THE_KING_POPULATION_MIN_POPULATION = 100

and explicitly documents values above 50 as effectively disabling WLTKD.

AdvCiv-SAS deliberately re-enabled the mechanic and currently sets:

    WE_LOVE_THE_KING_POPULATION_MIN_POPULATION = 5

The inherited BtS define remains:

    WE_LOVE_THE_KING_RAND = 1000

so an eligible healthy/non-angry/non-occupied city of population P >= 5 has
the standard P/1000 per-turn chance to enter WLTKD.

Current SAS maintenance factors are also positive across ordinary world sizes
and handicap levels, so an established WLTKD city can readily have positive
normal distance / number-of-cities / corporation / colony maintenance that is
suppressed to 0 by WLTKD. The error therefore has an ordinary live state under
current data.

USER-FACING CALLER
------------------
This is directly exposed in the Settler found-action hover.

`CvDLLWidgetData::parseActionHelp_Mission`, for `MISSION_FOUND`, calls:

    if (kUnitOwner.getNumCities() > 0)
        GAMETEXT.setFoundCostHelp(szBuffer, kMissionPlot);

and the localization says:

    TXT_KEY_PROJECTED_COST
    "Increases city cost by %s1_MaintNum [ICON_GOLD]"

So the wrong value is presented specifically as the incremental expense caused
by founding the proposed city.

ANCESTRY
--------
The founding-cost preview is an AdvCiv addition (`advc.004b`), not a BtS/K-Mod
UI feature.

The initial AdvCiv implementation already projected maintenance for existing
cities without excluding WLTKD. That older form primarily computed the change
in number-of-cities / colony maintenance, so the same conceptual omission was
present but its impact was narrower.

Pre-SAS AdvCiv commit:

    ef40cbb8b47a2b11e4b213076d0f1cad62ad13b1
    "Misc. tweaks (mostly UI)"

rewrote the projection into essentially the current form: it adds each
non-disorder city's FULL projected maintenance, then subtracts the player's
current aggregate inflated expenses. That rewrite introduced the especially
large current WLTKD error because the projected side includes full normal
maintenance while the current aggregate contains 0 for a celebrating city.

Later commits added corporation maintenance and moved/refactored the helper into
`CvGameTextMgr`, while preserving the `c->isDisorder()` guard.

Classification:
    inherited AdvCiv UI projection defect, mostly dormant in Base AdvCiv 1.14
    because AdvCiv disables WLTKD through XML; made live again by SAS's
    intentional re-enabling of WLTKD.

REPAIR DIRECTION
----------------
The smallest contract-aligned repair is for the existing-city projection to
skip cities whose runtime maintenance remains suppressed:

    if (c->isNoMaintenance())
        continue;

rather than only:

    if (c->isDisorder())
        continue;

That mirrors `CvCity::updateMaintenance()` for occupation/anarchy, WLTKD and
the zero-population guard in one place.

A still stronger long-term design would project the actual before/after city
maintenance through a shared helper rather than manually reproducing pieces of
`CvCity::updateMaintenance`, but replacing the guard with `isNoMaintenance()`
is the surgical fix for the demonstrated current bug.

-------------------------------------------------------------------------------
CONTROL — APPARENT NO_IMPROVEMENT DEREFERENCE IN PLOT-HEALTH TRANCHE IS SAFE
-------------------------------------------------------------------------------
During the surrounding plot-health pass, a narrow source excerpt initially
made an improvement-upgrade lookup appear reachable when:

    ePlotImprovement == NO_IMPROVEMENT

A wider brace/context check disproved that suspicion. The upgrade lookup is
inside the pre-existing:

    if (ePlotImprovement != NO_IMPROVEMENT)

block. No invalid enum/info access exists there. This is explicitly retained as
a control rather than promoted into a finding.

-------------------------------------------------------------------------------
OTHER CONTROLS IN THE SAME TRANCHE
-------------------------------------------------------------------------------

`setRevoltHelp`
---------------
The tooltip combines the probability of occupation-timer decrement with the
subsequent revolt probability in the same order as `CvCity::doRevolt`. No
supported mismatch was found.

`setPlotHealthHappyHelp` / `setHealthHappyBuildActionHelp`
----------------------------------------------------------
The access rule through `CvTeam::canAccessHappyHealth` agrees with runtime city
surrounding-health/happiness calculations. No F393 from that contract so far.

Finance unit extra cost
-----------------------
`buildFinanceUnitCostString` has a theoretical scaling asymmetry for
`UnitInfo::iExtraCost`, but every current SAS unit has `iExtraCost = 0`.
Dormant XML/mod hazard only; not numbered.

Trade-route breakdown
---------------------
The trade profit/modifier/yield decomposition checked so far agrees with the
runtime route calculation. The domestic/foreign count strings suppress an
exactly-one-route case, but their localization is explicitly plural and no
stronger contract was found; retained as presentation policy, not a finding.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F392 CONFIRMED in protected Queue 003.
F393 / provisional KI#716 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP93.
No repository source files modified.

NEXT
----
Continue from F393 unused.

Priority remains:
1. finish the remaining AdvCiv-only finance / trade / plot-state helpers;
2. return to changed building/project/trait requirement contexts;
3. mechanically audit text-key argument arity/type and civilization/class
   resolution;
4. reconcile exact active-member/helper coverage and run final sentinel/index/
   visibility/state-formatting sweeps before Queue 003 can close.

===============================================================================
C031-WIP94 — QUEUE 003 CONTINUATION FROM F393 UNUSED
===============================================================================

F393 / KI#716 — SAS SEVOPEDIA-ONLY UNIT-STAT SUPPRESSION LEAKS INTO PLE UPGRADE HOVERS BECAUSE `bCivilopediaText` IS NOT A SEVOPEDIA-ONLY CONTEXT FLAG
------------------------------------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — SAS-local UI regression / incomplete repair of an earlier SAS Sevopedia cleanup.

ROOT
----
Current SAS `CvGameTextMgr::setBasicUnitHelp` suppresses several static unit
ability families whenever:

    bCivilopediaText == true

because Sevopedia Unit now renders those values through dedicated button
panels instead of repeating them in `placeSpecial` text.

Examples include:

    city attack / city defense modifiers
    hill attack / hill defense modifiers
    terrain attack / terrain defense modifiers
    feature attack / feature defense modifiers
    UnitClass attack / defense modifiers
    UnitCombat modifiers
    free starting promotions (`Starts with ...`)

The relevant current code repeatedly uses:

    if (!bCivilopediaText)
    {
        ... ordinary static ability text ...
    }

and the SAS comments explicitly describe the reason as hiding duplicate rows
"in sevopedia" because Sevopedia has replacement button panels.

But `bCivilopediaText` is a formatting/mode parameter shared by other clients;
it is NOT an identity test for the Sevopedia Unit screen.

LIVE NON-SEVOPEDIA CONSUMER
---------------------------
The Plot List Enhancements (PLE) live map UI has an Upgrade grouping. It shows
buttons for actual upgrade targets and displays a custom information pane when
the mouse enters one of those target buttons.

`Assets/Python/Screens/PLE.py::showUpgradeInfoPane` still does:

    szSpecialText = mt.removeLinks(
        CyGameTextMgr().getUnitHelp(iUnitType, True, False, False, None)[1:]
    ) + "\n"

That `True` is `bCivilopediaText`.

PLE does NOT have the Sevopedia terrain / feature / hill / city / matchup
button panels that motivated the suppression. Therefore every static ability
removed solely by `bCivilopediaText` disappears from this live upgrade-target
hover.

This is a real supported UI route:

1. `Assets/Config/Plot List Enhancements.xml` exposes `PLE_Style` as a normal
   user option.
2. `CvMainInterface.py` selects the PLE draw method when
   `PleOpt.isPLE_Style()` is true.
3. PLE exposes `PLE_GRP_UPGRADE` as an interactive grouping mode.
4. In that mode, `displayUnitUpgrades` obtains candidate targets from:

       mt.getPossibleUpgrades(pUnit)

5. `MonkeyTools.getPossibleUpgrades` iterates UnitInfos and retains targets
   for which:

       pUnit.canUpgrade(i, true)

6. Each target is shown as an image button whose hover dispatch calls
   `showUpgradeInfoPane`.

The mode is optional and not the default (`PLE_Style` defaults false), but it
is a shipped, user-selectable interface path rather than dead/debug code.

SIMPLE CURRENT-DATA EXAMPLE
---------------------------
A generic Archer directly upgrades to the Longbowman class in current SAS:

    UNIT_ARCHER
        UnitClassUpgrade -> UNITCLASS_LONGBOWMAN

The generic current Longbowman has:

    iCityDefense = 25
    iHillsDefense = 20

In ordinary in-game unit help (`bCivilopediaText == false`), those are rendered
as the corresponding +25% city-defense and +20% hill-defense ability lines.

In Sevopedia (`bCivilopediaText == true`), hiding those rows is intentional
because the dedicated Sevopedia panels carry them.

In PLE's Archer -> Longbowman upgrade-target hover, however, PLE also passes
`true`, but supplies no replacement panels. The target preview therefore
omits both inherent Longbowman defensive modifiers even though they are
important properties of the unit the player is considering upgrading into.

This is broad rather than a one-off XML accident. A direct current UnitInfo
edge scan found 55 upgrade-target UnitTypes with at least one currently
suppressed city/hill/terrain/feature/UnitClass/UnitCombat modifier family.
Examples include Explorer, Axeman, Spearman, Pikeman, Longbowman, Crossbowman,
several UUs, Grenadier, many mounted lines, Tank and later units.

FREE-PROMOTION SUBCASE
----------------------
The same context leak also applies to the SAS block that hides:

    TXT_KEY_UNIT_STARTS_WITH

when `bCivilopediaText` is true. Sevopedia has its own Free Promotions panel;
PLE's upgrade hover does not. Upgrade targets with XML free promotions can
therefore lose that information through the same root.

ANCESTRY / WHY THIS IS A PARTIAL REPAIR
---------------------------------------
Base AdvCiv's `setBasicUnitHelp` emitted the city/hill/terrain/feature/class/
combat modifier text regardless of `bCivilopediaText`.

SAS commit:

    e82115dc5a...  (practical commit 4764)

commented those blocks out globally while building dedicated Sevopedia Unit
panels. Its own comments said the goal was to hide the cumbersome duplicate
data from Sevopedia, while still wanting it in-game.

That global suppression caused player-facing non-Sevopedia tooltips to lose
combat information.

SAS commit:

    9c01aa6e3f18c6826441cd77b84c7c160bb970b4
    2025-12-16
    "Fix some combat or other tooltips also being disabled outside sevopedia
     now instead of being disabled everywhere, they are only disabled in
     sevopedia."

re-enabled the blocks under:

    if (!bCivilopediaText)

The full commit message explicitly says the intended scope is "only in
sevopedia" and that the change fixes player feedback.

However the repair changed only `CvGameTextMgr.cpp`; PLE's pre-existing
upgrade-pane call still asks for `getUnitHelp(..., True, ...)`. Thus the repair
used a broader proxy (`bCivilopediaText`) than the actual intended scope
(Sevopedia Unit `placeSpecial`) and left this live side consumer suppressed.

A later comment/layout commit (`fb9054847f...`) preserved the same guard and
Sevopedia-only rationale.

CLASSIFICATION
--------------
SAS-local UI context-leak / partial-fix regression.

This is not a disagreement over whether Civ4 "Civilopedia text" should be
shorter. The source history explicitly states the desired policy: the rows
should be disabled ONLY in Sevopedia. PLE is a live main-interface upgrade
hover with no replacement presentation, and current data provides many
ordinary victims.

REPAIR DIRECTION
----------------
Do not use the generic `bCivilopediaText` parameter as a proxy for
"Sevopedia Unit has replacement buttons."

Best structural fix:

    introduce a dedicated Sevopedia-only suppression/context flag (or a
    dedicated help entry point) that SevoPediaUnit passes when it wants the
    replacement-panel version, while other `bCivilopediaText` consumers keep
    complete static ability help.

A smaller PLE-local repair may be possible by having `showUpgradeInfoPane`
request non-Civilopedia unit help, but that parameter controls more than these
specific rows and could alter unrelated formatting/content. It is therefore
less robust than separating the actual Sevopedia-only policy from the generic
Civilopedia formatting mode.

-------------------------------------------------------------------------------
CONTROL — SUPPORTED BUT OPTIONAL PLE SURFACE DOES NOT MAKE THE ROOT DORMANT
-------------------------------------------------------------------------------
`PLE_Style` defaults to False and the Upgrade grouping is user-selected, so
this is not a default-screen-every-turn defect. It is nevertheless a normal
shipped configuration path with active handlers, real upgrade candidates and
user-facing hover rendering. Unlike dormant XML/schema hazards retained
elsewhere in this audit, no hypothetical data edit or unsupported mod-mod
combination is needed to trigger F393.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F393 CONFIRMED in protected Queue 003.
F394 / provisional KI#717 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP94.
No repository source files modified.

NEXT
----
Continue from F394 unused.

Priority:
1. finish the remaining current-SAS-vs-Base `CvGameTextMgr` behavioral deltas,
   especially other `bCivilopediaText` / replacement-panel context guards;
2. complete text-key arity/type and civilization/class-resolution sweeps;
3. revisit event / espionage / project / building requirement state-vs-display
   contracts;
4. reconcile exact member/helper coverage and run final sentinel/index/
   visibility/state-formatting patterns before Queue 003 closure.

===============================================================================
C031-WIP95 — QUEUE 003 CONTINUATION FROM F394 UNUSED
===============================================================================

F394 / KI#717 — VASSAL-ENABLING TECH HELP TESTS `NO_VASSAL_STATES` WITH THE WRONG POLARITY, HIDING THE EFFECT WHEN IT WORKS AND SHOWING IT WHEN THE OPTION DISABLES IT
---------------------------------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited BtS UI/help logic bug, retained by K-Mod, Base AdvCiv and current SAS.

ROOT
----
Current `CvGameTextMgr::buildVassalStateString` contains:

    if (GC.getInfo(eTech).isVassalStateTrading() &&
        (!bPlayerContext ||
        (!GET_TEAM(getActiveTeam()).isVassalStateTrading() &&
        GC.getGame().isOption(GAMEOPTION_NO_VASSAL_STATES))))
    {
        if (bList)
            szBuffer.append(NEWLINE);
        szBuffer.append(gDLL->getText("TXT_KEY_MISC_ENABLES_VASSAL_STATES"));
    }

For player-context help, the final condition requires:

    GAMEOPTION_NO_VASSAL_STATES == true

before the tooltip says that the technology enables vassal states.

That is the opposite of the runtime contract.

RUNTIME CONTRACT
----------------
When a team gains a technology, `CvTeam::processTech` does:

    if (kTech.isVassalStateTrading())
        changeVassalTradingCount(iChange);

But the public capability queried by diplomacy is:

    bool CvTeam::isVassalStateTrading() const
    {
        return (!GC.getGame().isOption(GAMEOPTION_NO_VASSAL_STATES) &&
                getVassalTradingCount() > 0);
    }

and actual vassal/capitulation trading checks require the prospective master's
team to satisfy that capability. For example `CvPlayer::canTradeItem` rejects
`TRADE_VASSAL` / `TRADE_SURRENDER` when:

    !kToTeam.isVassalStateTrading()

Thus:

    No Vassal States OFF + enabling tech known -> capability can become TRUE.
    No Vassal States ON  + enabling tech known -> capability remains FALSE.

The help test is therefore exactly reversed with respect to whether the effect
can function.

CURRENT SAS DATA / ORDINARY EXAMPLE
-----------------------------------
Current SAS has:

    TECH_PHILOSOPHY
        <bVassalTrading>1</bVassalTrading>

and the visible game option:

    GAMEOPTION_NO_VASSAL_STATES
        <bDefault>0</bDefault>

So the default game is the simplest live example.

Before Philosophy is researched in a normal/default game:

    kTech.isVassalStateTrading()                         = true
    activeTeam.isVassalStateTrading()                    = false
    GAMEOPTION_NO_VASSAL_STATES                          = false

The player-context helper evaluates:

    true && (false || (true && false))
    -> false

and omits "Enables Vassal States" precisely while the player is evaluating a
technology whose acquisition really will enable the capability.

If the player instead starts a custom game WITH No Vassal States enabled:

    activeTeam.isVassalStateTrading()                    = false
    GAMEOPTION_NO_VASSAL_STATES                          = true

then the same pre-research helper evaluates true and advertises "Enables
Vassal States" even though runtime `CvTeam::isVassalStateTrading()` is forced
false by that option after the tech is acquired.

USER-FACING CALLERS
-------------------
The affected function is part of the ordinary technology-help effect list.
`setTechTradeHelp` calls:

    buildVassalStateString(szBuffer, eTech, true, bPlayerContext);

Player-context tech help is used by live research/technology UI, including
`CvDLLWidgetData::parseResearchHelp` and the unresearched-tech branch of
`parseTechTreeHelp`.

`parseTechTreeHelp` deliberately passes player context while the technology is
not yet known:

    GAMETEXT.setTechHelp(...,
        !GET_TEAM(getActiveTeam()).isHasTech(eTech), ...);

That is exactly the decision-time state in which F394 suppresses Philosophy's
real vassal-enabling effect in the default game.

After the tech is already known, that caller passes `bPlayerContext=false`, so
the static Civilopedia-style branch can display the line. This makes the
mismatch especially perverse: the effect is hidden while deciding whether to
research the tech and can reappear after the player already owns it.

ANCESTRY
--------
The same conditional, including the positive test of
`GAMEOPTION_NO_VASSAL_STATES`, is present in:

    Civ4CE/BtS
    K-Mod
    Base AdvCiv 1.14
    current AdvCiv-SAS

So this is an inherited BtS root rather than a SAS regression.

The unchanged ancestry is also a useful control against interpreting the SAS
technology-tree rework as the cause. SAS's current placement of the flag on
Philosophy merely supplies a live current example; the logic defect predates
it.

REPAIR DIRECTION
----------------
For player-context help, the option predicate should match the runtime's
availability direction. The narrow correction is:

    if (GC.getInfo(eTech).isVassalStateTrading() &&
        (!bPlayerContext ||
        (!GET_TEAM(getActiveTeam()).isVassalStateTrading() &&
         !GC.getGame().isOption(GAMEOPTION_NO_VASSAL_STATES))))

That says to advertise the not-yet-active effect only when the game option
allows it to become active.

A shared/query-based future-proof form would be even safer if technology help
can project the post-tech capability without duplicating option gates, but the
single missing `!` is the demonstrated root.

-------------------------------------------------------------------------------
CONTROL — NEW `buildBTradeString` / `buildBRepeatString` PLAYER-CONTEXT GATES
-------------------------------------------------------------------------------
The two SAS-added technology helpers initially looked similar because they
also combine XML flags with `bPlayerContext` and current-team tech state.
Their current callers explain the shape:

* while a technology is unresearched, research/tree help passes player context;
* after it is known, the tech-tree caller passes non-player-context static
  help.

No analogous option-polarity contradiction was established there. Current SAS
also has no repeatable technology, so the repeat helper remains dormant under
current XML. These do not consume F395.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F394 CONFIRMED in protected Queue 003.
F395 / provisional KI#718 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP95.
No repository source files modified.

NEXT
----
Continue from F395 unused through the remaining technology-effect helpers and
then resume the exact current-SAS delta / text-key / sentinel / state-formatting
closure sweeps for Queue 003.

===============================================================================
C031-WIP96 — QUEUE 003 CONTINUATION FROM F395 UNUSED
===============================================================================

F395 / KI#718 — `NO_TECH_TRADING` GAME OPTION DOES NOT SUPPRESS PLAYER-CONTEXT "ENABLES TECHNOLOGY TRADING" TECH HELP
---------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited BtS player-context help bug, retained by K-Mod, Base AdvCiv and current SAS.

ROOT
----
Current `CvGameTextMgr::buildTechTradeString` says:

    if (GC.getInfo(eTech).isTechTrading() &&
        (!bPlayerContext || !GET_TEAM(getActiveTeam()).isTechTrading()))
    {
        if (bList)
            szBuffer.append(NEWLINE);
        szBuffer.append(gDLL->getText("TXT_KEY_MISC_ENABLES_TECH_TRADING"));
    }

Unlike the neighboring Permanent Alliance helper, and unlike the intended
(option-corrected) Vassal helper from F394, it never checks the game option
that globally disables the advertised action:

    GAMEOPTION_NO_TECH_TRADING

RUNTIME CONTRACT
----------------
The team-level technology flag is only a count:

    bool CvTeam::isTechTrading() const
    {
        return getTechTradingCount() > 0;
    }

so researching the enabling technology still raises that count even in a
No-Tech-Trading game.

But actual player trade legality explicitly gates the trade item:

    case TRADE_TECHNOLOGIES:
        return (!kGame.isOption(GAMEOPTION_NO_TECH_TRADING) &&
                (kOurTeam.isTechTrading() || kToTeam.isTechTrading()));

Thus when the option is enabled, no team can actually trade technologies even
if its technology-count flag says that the enabling technology is known.

CURRENT SAS DATA / LIVE EXAMPLE
-------------------------------
Current SAS has:

    TECH_WRITING
        <bTechTrading>1</bTechTrading>

and exposes the standard visible option:

    GAMEOPTION_NO_TECH_TRADING
        <bDefault>0</bDefault>
        <bVisible>1</bVisible>

In a supported Custom Game with No Tech Trading enabled, before Writing is
known:

    kTech.isTechTrading()             = true
    activeTeam.isTechTrading()        = false
    bPlayerContext                    = true

so `buildTechTradeString` displays:

    "Enables Technology Trading"

while acquiring Writing cannot make any `TRADE_TECHNOLOGIES` item legal
because the runtime option check always rejects it.

USER-FACING SURFACE
-------------------
As with F394, `setTechTradeHelp` invokes this helper in the ordinary technology
effect list. Live research and unresearched Tech Tree hovers use player
context, so this is specifically misleading decision-time help in the game
configuration where the advertised effect has been disabled.

This is not merely the static Civilopedia continuing to describe the XML
technology in isolation. The code has a distinct `bPlayerContext` mode and
neighboring helpers already use game-option state to contextualize effects.

ANCESTRY
--------
The same `buildTechTradeString` condition, without a
`GAMEOPTION_NO_TECH_TRADING` guard, is present in:

    Civ4CE/BtS
    K-Mod
    Base AdvCiv 1.14
    current AdvCiv-SAS

Classification: inherited BtS UI/help omission.

RELATION TO F394
----------------
F394 and F395 share a high-level theme but have different roots:

* F394: `buildVassalStateString` DOES test its disabling option but tests it
  with the wrong polarity, reversing the help state even in the default game.
* F395: `buildTechTradeString` never checks its disabling option at all, so the
  false advertisement occurs in the supported No Tech Trading configuration.

They require different local repairs and are retained as separate findings.

REPAIR DIRECTION
----------------
Preserve static/non-player-context description, but suppress the advertised
not-yet-active effect in player context when the option makes it unusable, e.g.
conceptually:

    if (kTech.isTechTrading() &&
        (!bPlayerContext ||
         (!kTeam.isTechTrading() &&
          !kGame.isOption(GAMEOPTION_NO_TECH_TRADING))))

This parallels the correct positive-option treatment already used for
Permanent Alliances.

-------------------------------------------------------------------------------
CONTROL — PERMANENT ALLIANCE HELPER HAS THE EXPECTED OPTION POLARITY
-------------------------------------------------------------------------------
`buildPermanentAllianceString` requires:

    GAMEOPTION_PERMANENT_ALLIANCES == true

for player-context "Enables Permanent Alliances" help, matching
`CvTeam::isPermanentAllianceTrading()`.

That neighboring implementation is a useful internal control: player-context
tech help is intended to take global option availability into account, and the
Vassal/Tech-Trading cases are not simply a policy of always describing raw XML
flags.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F395 CONFIRMED in protected Queue 003.
F396 / provisional KI#719 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP96.
No repository source files modified.

NEXT
----
Continue from F396 unused. Finish the remaining technology helper family with
option/runtime controls, then return to current-SAS behavioral deltas and the
whole-file mechanical closure sweeps.

END C031-WIP96

===============================================================================
C031-WIP97 — QUEUE 003 CONTINUATION FROM F396 UNUSED
===============================================================================

F396 / KI#719 — NO-SLAVERY PLAYER-CONTEXT TECH HELP STILL SAYS THE PREREQUISITE TECH "ENABLES" SLAVERY
------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited AdvCiv option/help omission (advc.912d), live in current SAS.

ROOT
----
Current `CvGameTextMgr::buildCivicRevealString` ignores both `bPlayerContext`
and the No Slavery game option when it lists civics unlocked by a technology:

    if (GC.getInfo(eCivic).getTechPrereq() == eTech)
    {
        ...
        setListHelp(szBuffer, gDLL->getText("TXT_KEY_MISC_ENABLES").c_str(),
                szTempBuffer, L", ", bFirst);
    }

The `bPlayerContext` parameter is accepted but unused. Consequently the live
research/Tech Tree help describes the raw XML prerequisite even when current
rules make adoption impossible for the active human.

RUNTIME CONTRACT
----------------
AdvCiv's `advc.912d` No Slavery option explicitly rejects any human civic that
allows population rushing:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) && isHuman())
    {
        FOR_EACH_ENUM(Hurry)
        {
            if (GC.getInfo(eCivic).isHurry(eLoopHurry) &&
                GC.getInfo(eLoopHurry).getProductionPerPopulation() > 0)
            {
                return false;
            }
        }
    }

This is in `CvPlayer::canDoCivics`, i.e. the actual adoption legality path.
The same AdvCiv feature also taught `CvGameTextMgr::parseCivicInfo` to detect
this state and say explicitly that the civic cannot be adopted because of the
No Slavery option. It even suppresses the ordinary technology-requirement
message in that disabled state.

Thus the technology-side "Enables" line and the civic-side player-context help
contradict one another under the same supported game configuration.

CURRENT SAS DATA / LIVE EXAMPLE
-------------------------------
Current SAS exposes:

    GAMEOPTION_NO_SLAVERY
        bDefault = 0
        bVisible = 1

Current Slavery data is:

    CIVIC_SLAVERY
        TechPrereq = TECH_MASONRY
        HURRY_POPULATION = true

and HURRY_POPULATION has positive production-per-population, so the runtime
No-Slavery rejection applies.

Therefore, in a human Custom Game with No Slavery enabled, before Masonry is
known:

    * Masonry's player-context research/Tech Tree hover can say
      "Enables Slavery";
    * acquiring Masonry does not make Slavery adoptable;
    * hovering Slavery itself correctly reports that it cannot be adopted
      because of the No Slavery option.

USER-FACING CALLERS
-------------------
`setTechTradeHelp` calls `buildCivicRevealString` for every civic. The ordinary
research hover passes player context through `CvDLLWidgetData::parseResearchHelp`,
and an unresearched Tech Tree hover does the same through
`CvDLLWidgetData::parseTechTreeHelp`.

This is therefore decision-time in-game help, not merely static Civilopedia
text describing XML in isolation.

ANCESTRY
--------
The No Slavery option was introduced in AdvCiv commit:

    df5a03e743a64c58380ae91f2bdaf586c817f34f
    v0.92f: Changes to replays; "No Slavery" option

The introducing diff modified `CvPlayer` legality and `parseCivicInfo` so that
the civic's own player-context help understands the new option, but it did not
modify the pre-existing `buildCivicRevealString` technology-side "Enables"
list.

BtS/K-Mod cannot contain this exact root because they do not have AdvCiv's
No Slavery option. Base AdvCiv 1.14 and current SAS retain the omission.
Classification: inherited AdvCiv UI/help integration bug.

RELATION TO F394/F395
---------------------
This belongs to the same closure family of option-sensitive technology help,
but has an independent origin and repair point:

* F394: BtS vassal helper tests its disabling option with reversed polarity.
* F395: BtS tech-trading helper omits its disabling option.
* F396: AdvCiv adds a new civic-disabling option and updates civic help/runtime,
  but leaves the technology-to-civic "Enables" helper context-blind.

REPAIR DIRECTION
----------------
Preserve static/non-player-context Civilopedia behavior, but in player context
omit a civic from the technology's "Enables" list when the active player is
not allowed to adopt it for a game-option reason. The narrow current repair can
mirror the `advc.912d` No-Slavery test; a more future-proof repair would share
an option-legality helper with `parseCivicInfo` rather than duplicate the rule.

Do not blindly use `canDoCivics(eCivic)` as the only predicate: player-context
technology help normally needs to list civics whose only missing condition is
the technology being hovered. The projection must ignore the technology
prerequisite while retaining independent blockers such as No Slavery.

-------------------------------------------------------------------------------
CONTROL — `GET_TEAM(eActivePlayer)` IN `CvGameTextMgr.cpp` IS NOT A PLAYER/TEAM-ID MIXUP
-------------------------------------------------------------------------------
The whole-file active-context sweep initially made several calls such as:

    GET_TEAM(eActivePlayer).isHasTech(...)

look suspicious because `eActivePlayer` is `PlayerTypes`. In this translation
unit, however, `CoreAI.h` supplies an overload:

    getTeam(PlayerTypes ePlayer) { return CvTeamAI::AI_getTeam(TEAMID(ePlayer)); }

so the macro intentionally converts a player to that player's actual team.
This remains safe in team games where player and team numeric IDs differ and
does not consume F397.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F396 CONFIRMED in protected Queue 003.
F397 / provisional KI#720 UNUSED.
Old F004/KI#332: fixed in current SAS.
Existing KI#21 dynamic-state live-unit restriction subcase: retained.
Latest physical checkpoint: C031-WIP97.
No repository source files modified.

NEXT
----
Continue from F397 unused. Finish the remaining option-sensitive technology
and finance/runtime-decomposition controls, then resume the whole-file
active-context, sentinel/index, and changed-body closure sweeps.

END C031-WIP97

===============================================================================
C031-WIP98 — PROTECTED QUEUE 003 CvGameTextMgr.cpp — F397 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F397 CONFIRMED in protected Queue 003.
F398 / provisional KI#721 UNUSED.
Old F004/KI#332 remains fixed in current SAS.
Existing KI#21 live-unit dynamic-state "Cannot Enter" subcase remains retained.
No repository source files were modified.

-------------------------------------------------------------------------------
F397 / provisional KI#720 — AdvCiv LuxuryModifier is omitted from BUG's
additional-building-happiness projection
-------------------------------------------------------------------------------

ROOT
----
The optional BUG/BULL city happiness hover uses:

    CvGameTextMgr::setBuildingAdditionalHappinessHelp
      -> CvCity::getAdditionalHappinessByBuilding

for every currently constructible building.

The inherited additional-happiness helper handles a building's resource-
conditional happiness by adding the raw XML values:

    FOR_EACH_NON_DEFAULT_PAIR(kBuilding.getBonusHappinessChanges(), Bonus, int)
    {
        if ((hasBonus(eLoopBonus) || kBuilding.getFreeBonus() == eLoopBonus) &&
            kBuilding.getNoBonus() != eLoopBonus)
        {
            addGoodOrBad(perBonusVal.second, iGood, iBad);
        }
    }

AdvCiv's advc.912c Hereditary-Rule rework, however, changed the runtime meaning
of positive bonus happiness. `CvCity::getBonusGoodHappiness()` now returns:

    r = m_iBonusGoodHappiness;
    r = r * (100 + GET_PLAYER(getOwner()).getLuxuryModifier()) / 100;

and `CvCity::happyLevel()` consumes that scaled getter.

Building processing adds positive `BonusHappinessChanges` into
`m_iBonusGoodHappiness`, so the effective happiness gained from a building is
not generally the raw sum that `getAdditionalHappinessByBuilding` reports.
The omission is especially visible because integer rounding is applied to the
whole bonus-happiness pool, not independently to each building entry.

CURRENT SAS DATA / CONCRETE EXAMPLE
-----------------------------------
Current SAS has:

    CIVIC_HEREDITARY_RULE
        iLuxuryModifier = 25

and many live buildings with positive resource-conditioned happiness. The
ordinary Forge alone has:

    Gemstones +1 happiness
    Gold      +1 happiness
    Silver    +1 happiness

Current Gemstones, Gold and Silver each also have base `iHappiness = 1`.

Take a city under Hereditary Rule connected to exactly those three luxury
resources and able to construct a Forge.

Before the Forge:

    raw m_iBonusGoodHappiness = 3
    runtime bonus happiness   = floor(3 * 125 / 100) = 3

After the Forge:

    raw m_iBonusGoodHappiness = 6
    runtime bonus happiness   = floor(6 * 125 / 100) = 7

Actual happiness change from constructing the Forge:

    +4 happiness

The additional-building-happiness helper instead adds the three raw Forge
entries and reports:

    +3 happiness

Its derived `getAdditionalAngryPopuplation(iGood, iBad)` value can therefore
also be wrong in a city near the happy/unhappy threshold.

LIVE USER-FACING CALLER
-----------------------
`CvDLLWidgetData::parseHappinessHelp` invokes this helper for the active-owned
selected city when either:

    MiscHover__BuildingAdditionalHappiness == true

or the player holds Alt while hovering the happiness display.

The BUG option defaults to false, but the Alt-key path makes the calculation
available in every ordinary current game without configuration editing.
This is therefore a live optional/modified-hover information defect, not a
dormant helper with no UI caller.

BREADTH
-------
Current SAS contains 32 BuildingInfos with at least one nonzero
`BonusHappinessChange`, including Forge, Market, Grocer and numerous UBs and
religious/cultural buildings. The defect is therefore not limited to one
custom building.

ANCESTRY
--------
K-Mod already contains the BUG `getAdditionalHappinessByBuilding` raw
BonusHappinessChanges calculation, but it has no AdvCiv LuxuryModifier system.

AdvCiv commit/diff `AdvCivPreSAS_1287_ad5faf8aa2` introduced advc.912c:
* `iLuxuryModifier` in CivicInfo;
* Hereditary Rule `iLuxuryModifier = 25`;
* player LuxuryModifier state;
* the scaling in `CvCity::getBonusGoodHappiness`.

That introducing diff does not update `getAdditionalHappinessByBuilding`.
Base AdvCiv 1.14 retains both the LuxuryModifier scaling and the old raw BUG
projection, and its own current building data also has many positive
BonusHappinessChanges. Current SAS inherits the omission and expands the number
of affected buildings.

Classification:
    inherited AdvCiv integration defect between advc.912c and BUG's
    additional-building-happiness preview.

REPAIR DIRECTION
----------------
Do not simply multiply each building bonus-happiness entry by LuxuryModifier
independently; runtime rounds the whole positive bonus-happiness pool.

The projection should compare the effective before/after bonus-happiness
getter, e.g. conceptually:

    oldRaw = getBonusGoodHappiness(true);
    newRaw = oldRaw + raw positive bonus-happiness added by the building;
    oldEffective = oldRaw * (100 + luxuryMod) / 100;
    newEffective = newRaw * (100 + luxuryMod) / 100;
    iGood += newEffective - oldEffective;

while preserving separate handling of negative bonus happiness, which
`getBonusBadHappiness()` does not pass through LuxuryModifier.

A still stronger repair would centralize the projected building effect around
the same runtime transformation rather than duplicating the rounding rule.

-------------------------------------------------------------------------------
CONTROL — `setHealthHappyBuildActionHelp` SIGN / ROUNDING RECONCILIATION
-------------------------------------------------------------------------------
The unfinished WIP97 comparison was completed before promoting F397.

The apparent sign oddities are intentional:
* surrounding bad happiness/health storage uses non-positive values;
* the UI flips bad-health/unhappiness deltas so "more bad" is shown as a
  positive amount beside the bad icon;
* percentage offsets are fed into `calculateSurroundingHealth` while the
  current feature/improvement is still physically present, correctly
  projecting feature removal and improvement replacement;
* good and bad health are rounded separately, matching the runtime storage
  contract.

Opposite-sign replacement and fractional Forest/Jungle cases were challenged;
no independent F397 root exists in this helper chain.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F397 CONFIRMED in protected Queue 003.
F398 / provisional KI#721 UNUSED.
Latest physical checkpoint: C031-WIP98.
No repository source files modified.

NEXT
----
Continue from F398 unused. Resume the remaining finance/runtime decomposition
and changed building/project requirement helpers, then the whole-file text-key
arity/type, active-context, enum/sentinel and exact-member closure sweeps.

END C031-WIP98

===============================================================================
C031-WIP99 — PROTECTED QUEUE 003 CvGameTextMgr.cpp — F398 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F398 CONFIRMED in protected Queue 003.
F399 / provisional KI#722 UNUSED.
No repository source files were modified.

-------------------------------------------------------------------------------
F398 / provisional KI#721 — BUG additional-production hover treats conditional
military/domain/space production modifiers as ordinary production yield
-------------------------------------------------------------------------------

ROOT
----
The optional production hover calls:

    CvGameTextMgr::setBuildingAdditionalYieldHelp(..., YIELD_PRODUCTION, ...)
      -> CvCity::getAdditionalYieldByBuilding
      -> CvCity::getAdditionalYieldRateModifierByBuilding

The helper contract says it returns the additional *yield* supplied by adding a
building. For `YIELD_PRODUCTION`, however, the inherited BUG helper adds:

    kBuilding.getMilitaryProductionModifier();
    kBuilding.getSpaceProductionModifier();
    kBuilding.getGlobalSpaceProductionModifier();
    max(kBuilding.getDomainProductionModifier(...));

into the city's projected ordinary base-yield modifier.

Those fields do not modify `CvCity::getYieldRate(YIELD_PRODUCTION)`.
Runtime applies them only to matching production orders:

* `CvCity::getProductionModifier(UnitTypes)` adds the city's domain modifier
  for the unit's actual domain and adds military production only when the unit
  is military production;
* `CvCity::getProductionModifier(ProjectTypes)` adds space production only for
  spaceship projects;
* building production uses neither military nor domain nor spaceship-only
  modifiers.

The preview therefore converts conditional order modifiers into generic hammer
output, and also loses the conditions by adding *all* families at once (using
only the largest DomainProductionModifier).

CURRENT SAS DATA / SIMPLE LIVE EXAMPLE
--------------------------------------
Current SAS `BUILDING_HEROIC_EPIC` has:

    iMilitaryProductionModifier = 50
    ordinary YieldModifier(PRODUCTION) = 0

Take an active-owned city with:

    base production yield = 10 hammers
    base yield modifier   = 100%
    Heroic Epic currently constructible

The additional-building-production calculation projects:

    10 * (100 + 50) / 100 - 10 * 100 / 100 = +5 hammers

and can therefore list approximately:

    Heroic Epic: +5 [production]

Yet once Heroic Epic exists, the city's ordinary production yield remains 10.
A building order still receives no Heroic-Epic modifier at all. Only qualifying
military-unit production receives the +50% order-specific bonus.

Current SAS provides another direct family example:

    BUILDING_DRYDOCK
        DomainProductionModifier(DOMAIN_SEA) = 50

The preview treats that as generic city production although runtime applies it
only when the actual unit being trained has DOMAIN_SEA.

LIVE USER-FACING CALLER
-----------------------
`CvGameTextMgr::setProductionHelp` enables the additional-building-yield block
when either:

    MiscHover__BuildingAdditionalProduction == true

or the player holds Alt. It then calls the helper for every visible/
constructible building in the active-owned selected city.

Thus the defect is reachable in an ordinary current game simply by holding Alt
on the production hover; the BUG option need not be permanently enabled.

ANCESTRY
--------
K-Mod 1.46 already contains the same BUG `getAdditionalYieldRateModifierByBuilding`
logic, including unconditional addition of military/space/global-space and the
maximum domain production modifier for `YIELD_PRODUCTION`.

The Civ4CE/BtS core does not provide this BUG helper layer. Base AdvCiv 1.14
inherits the K-Mod/BUG code and current SAS retains it.

Classification:
    inherited BUG/K-Mod optional-hover calculation defect.

REPAIR DIRECTION
----------------
If this helper is meant to honor its stated *yield* contract, remove
order-specific military/domain/space modifiers from
`getAdditionalYieldRateModifierByBuilding`; only modifiers that actually alter
`getBaseYieldRateModifier(YIELD_PRODUCTION)` belong there.

If the UI instead intends to answer "how much would this building change the
currently selected production order?", then it needs a different projection:
* inspect the current order type;
* for a unit, use that unit's actual Domain and military-production flag;
* for a project, apply space production only when it is a spaceship;
* for a building, omit those order-specific fields;
* compare the projected before/after `getCurrentProductionDifference`-style
  result rather than relabeling those modifiers as ordinary yield.

The current hybrid (all conditional modifiers -> generic production yield) is
not contract-correct under either interpretation.

-------------------------------------------------------------------------------
CONTROL — BUILDING UnhealthyPopulationModifier ROUNDING
-------------------------------------------------------------------------------
The adjacent health-preview rounding suspicion was challenged and not promoted.
Current SAS has only two buildings with nonzero population-unhealth modifiers:
Public Transportation (-20) and National Park (-100), and no current civic adds
another population-unhealth modifier. The helper's signed rounded delta agrees
with runtime before/after `unhealthyPopulation` for those reachable modifier
states, including the cap at -100%.

No F399 from that rounding path.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F398 CONFIRMED in protected Queue 003.
F399 / provisional KI#722 UNUSED.
Latest physical checkpoint: C031-WIP99.
No repository source files modified.

NEXT
----
Continue from F399 unused through the remaining BUG additional-building
commerce/maintenance/GP projections and finance helpers, then return to changed
building/project requirement contexts and whole-file text-key/context/sentinel
closure sweeps.

END C031-WIP99

===============================================================================
C031-WIP100 — PROTECTED QUEUE 003 CvGameTextMgr.cpp — F399 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F399 CONFIRMED in protected Queue 003.
F400 / provisional KI#723 UNUSED.
No repository source files were modified.

-------------------------------------------------------------------------------
F399 / provisional KI#722 — BUG saved-maintenance hover omits government-center
maintenance savings entirely
-------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
The optional city maintenance hover calls:

    CvGameTextMgr::setBuildingSavedMaintenanceHelp
      -> CvCity::getSavedMaintenanceTimes100ByBuilding

The helper was introduced by K-Mod's BUG "actual building effect" integration
with the explicit contract:

    "Returns the total additional gold from saved maintenance times 100 that
     adding one of the given buildings will provide."

Yet the implementation considers only:

    int iModifier = kBuilding.getMaintenanceModifier();

and returns zero whenever that scalar percentage modifier is zero.

Government-center buildings reduce maintenance by an entirely different live
mechanism. `CvCity::processBuilding` calls:

    changeGovernmentCenterCount(kBuilding.isGovernmentCenter() ? iChange : 0);

and `changeGovernmentCenterCount` immediately calls:

    GET_PLAYER(getOwner()).updateMaintenance();

The player's cities then recompute distance maintenance. K-Mod's
`calculateMaintenanceDistance` explicitly uses the shortest distance to any
owned city for which `isGovernmentCenter()` is true.

CURRENT SAS DATA / SIMPLE LIVE PROOF
------------------------------------
Current SAS `BUILDING_FORBIDDEN_PALACE` has:

    bGovernmentCenter     = 1
    iMaintenanceModifier  = 0
    PrereqTech            = TECH_CONSTITUTION
    iCitiesPrereq         = 6

Therefore, whenever Forbidden Palace is currently constructible:

    getSavedMaintenanceTimes100ByBuilding(BUILDING_FORBIDDEN_PALACE)
        -> iModifier == 0
        -> returns 0

and `setBuildingSavedMaintenanceHelp` omits the building completely.

But take any eligible city that currently pays positive distance maintenance.
After Forbidden Palace completes there, that same city is itself a government
center. In `calculateMaintenanceDistance` the loop encounters the city at
plotDistance 0, so its shortest-government-center distance becomes 0 and its
positive distance-maintenance component disappears. The resulting maintenance
saving is real even before considering any other city.

The runtime effect can be larger still: because the government-center count
change triggers an owner-wide `updateMaintenance()`, other nearby cities can
also replace a farther Palace/government center with the new Forbidden Palace
as their shortest government center and reduce their distance maintenance.

Thus the helper can report zero saved gold for a building whose completion
immediately saves positive maintenance in the very city being hovered, and can
also save maintenance elsewhere in the empire.

LIVE USER-FACING CALLER
-----------------------
`CvDLLWidgetData::parseMaintenanceHelp` invokes the helper for an active-owned
selected city when either:

    MiscHover__BuildingSavedMaintenance == true

or the player holds Alt (`GC.altKey()`, AdvCiv advc.063).

The BUG option defaults to false, but the Alt path makes the defect ordinarily
reachable without changing configuration.

Static building help separately prints `TXT_KEY_BUILDING_REDUCES_MAINTENANCE`
for `isGovernmentCenter()`, so the government-center reduction is also
explicitly advertised as a maintenance effect; it is not an unrelated ability
outside the saved-maintenance concept.

ANCESTRY
--------
K-Mod commit 8478443165fcaeca861fc9d2dbb725de7e94ca59,
"Implemented \"actual building effect\" mouse-over hover options from BUG",
introduced `getSavedMaintenanceTimes100ByBuilding` with the same percentage-
modifier-only algorithm and the quoted total-saved-gold contract.

Civ4CE/BtS does not contain this BUG helper layer. Base AdvCiv 1.14 inherits the
same omission, and current SAS retains it while current data provides the live
Forbidden Palace example above.

Classification:
    inherited BUG/K-Mod optional-hover calculation omission.

REPAIR DIRECTION
----------------
A contract-correct projection cannot be based only on `iMaintenanceModifier`.
For an ordinary maintenance-modifier building, the current cheap calculation is
fine. For a hypothetical new government center, the projection must also
recalculate distance maintenance with the candidate city treated as a center.

If "total additional gold" is interpreted literally, the strongest repair is
to compare owner-wide maintenance before/after the hypothetical center, because
the new center can reduce maintenance in several cities. A narrower city-only
repair would at least project the hovered city's distance maintenance with a
zero-distance center and would fix the simplest Forbidden Palace false zero,
but would still understate the building's empire-wide saved gold.

-------------------------------------------------------------------------------
CONTROL — GOVERNMENT-CENTER EFFECT IS NOT MERELY HIDDEN ELSEWHERE
-------------------------------------------------------------------------------
Generic building help does tell the player that a government center "reduces
maintenance", but it does not calculate the current-state gold amount. The BUG
"Building Saved Maintenance" feature exists specifically to provide those
actual-state numerical projections. Therefore the static line is not a numeric
replacement that would make the false-zero projection intentional.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F399 CONFIRMED in protected Queue 003.
F400 / provisional KI#723 UNUSED.
Latest physical checkpoint: C031-WIP100.
No repository source files modified.

NEXT
----
Continue from F400 unused through the remaining BUG additional-building
commerce/Great-People projections and finance helpers, then return to changed
building/project requirement contexts and whole-file text-key/context/sentinel
closure sweeps.

END C031-WIP100

===============================================================================
C031-WIP101 — PROTECTED QUEUE 003 CvGameTextMgr.cpp — F400 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.

Unique files complete: 32/169.
F388-F400 CONFIRMED in protected Queue 003.
F401 / provisional KI#724 UNUSED.
No repository source files were modified.

-------------------------------------------------------------------------------
F400 / provisional KI#723 — Finance city-maintenance breakdown rounds its
components to a different total than the total it actually displays
-------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::buildFinanceCityMaintString` computes the displayed distance,
colony and corporation components at scaled precision and rounds each one.
It then derives the residual number-of-cities component from:

    (kPlayer.getTotalMaintenance() * rInflationFactor).round()

so the four displayed components are forced to add up to a *rounded* inflated
maintenance total.

But the fifth argument sent to `TXT_KEY_FINANCE_ADVISOR_CITY_MAINT_COST`—the
line's displayed total—is instead:

    (kPlayer.getTotalMaintenance() * iInflationFactorTimes100) / 100

which is integer division and therefore truncates.

The engine's normal inflated-cost calculation likewise multiplies the integer
pre-inflated cost and then divides by 100. Thus the helper uses two different
rounding contracts inside one breakdown: round-to-nearest to derive the
components, truncate for the displayed total.

EXACT CURRENT-SAS CONSTRUCTION
------------------------------
A fully current-data state can make the contradiction explicit.

Use:

    game speed       = Normal
    world size       = Standard
    handicap         = Noble
    civ players ever = 8 (the Standard recommended count)
    owned cities     = 6
    each population  = 8
    civic            = State Property
    all cities on capital's landmass
    no corporations
    no maintenance-modifier buildings

State Property has `iDistanceMaintenanceModifier = -100`, so distance
maintenance is zero. Same-area cities have zero colony maintenance, and no
corporations means zero corporation maintenance.

For Standard/Noble with the recommended-player crowdedness factor 1, current
number-of-cities maintenance per population-8 city is:

    maintenance cities       = 6
    population factor        = (8 + 17) / 18 = 25/18
    Standard city factor     = 30%
    Noble city factor        = 75%

    6 * 25/18 * 30% * 75% = 1.875 gold

`scaled::getPercent()` rounds this to 188 hundredths per city. Across six
cities, `m_iTotalMaintenance` is therefore 1128 hundredths and:

    getTotalMaintenance() = 1128 / 100 = 11

(the rules intentionally truncate only after city maintenance has been
aggregated).

On an ordinary Normal-speed game where game turn and elapsed turns are both
230, current inflation is exactly 50%:

    adjusted turns = 230 + (-90 Normal offset) = 140
    first term      = floor(140 * 30 / 100) = 42
    second term     = floor(140 * 139 * 30 * 30 / 2,000,000) = 8
    inflation       = 50%

With distance/colony/corporation components all zero, the helper computes:

    displayed number-of-cities component
      = round(11 * 1.50)
      = 17 gold

but passes as the displayed total:

    floor(11 * 150 / 100)
      = 16 gold

So the same Finance Advisor tooltip can effectively say:

    Number of Cities: 17 gold
    Total:            16 gold

with every other city-maintenance component equal to zero.

This is a direct internal contradiction, not merely a one-gold disagreement
with an external screen.

LIVE CALLER
-----------
`CvDLLWidgetData::parseFinanceCityMaint` calls
`buildFinanceCityMaintString` for the Finance Advisor city-maintenance widget
when its data flag is positive. The path is part of the ordinary Finance
Advisor hover UI.

ANCESTRY
--------
K-Mod already derived `iNumCityMaint` with `ROUND_DIVIDE` but displayed the
maintenance total using truncating integer division, so the rounding split is
inherited from K-Mod.

AdvCiv commit 305e9a675013ef3e800526eee894c92864f05210,
"Refactor code for city maintenance breakdown", explicitly changed the three
named components to `scaled` precision and retained the split:

    iNumCityMaint = (... * rInflationFactor).round() - components

versus

    total = (... * iInflationFactorTimes100) / 100

The commit comment says the goal is to improve precision and notes that rules
round costs down only after adding components, but the residual component still
balances against a rounded rather than truncated total. Base AdvCiv 1.14 and
current SAS retain that result.

Classification:
    inherited K-Mod finance-breakdown rounding defect, retained through
    AdvCiv's later precision refactor.

REPAIR DIRECTION
----------------
Use one target total consistently. Since the displayed/rules total uses integer
truncation, derive the residual component from that same integer:

    int iTotalMaint = (kPlayer.getTotalMaintenance() *
            iInflationFactorTimes100) / 100;

    int iNumCityMaint = iTotalMaint - iDistanceMaint -
            iColonyMaint - iCorpMaint;

and pass `iTotalMaint` as the final localization argument.

That guarantees the displayed breakdown sums to its displayed total. If the UI
instead intentionally wants a rounded presentation, then the total line must be
changed to that same rounded value; mixing both contracts is the root.

-------------------------------------------------------------------------------
CONTROL — REMAINING BUG COMMERCE / GREAT-PEOPLE BUILDING PROJECTIONS
-------------------------------------------------------------------------------
The adjacent BUG projections were challenged before moving into finance.

* Additional commerce uses the city's runtime base-commerce and total commerce
  modifier structure, including the No Espionage culture/espionage transfer.
* Additional Great People combines current base GPP with local and global GPP
  modifiers consistently with `getTotalGreatPeopleRateModifier`.
* AdvCiv advc.051 introduced a valid-civilization-GP-unit mapping guard in
  runtime base-GPP processing that the old BUG projection does not reproduce.
  Current playable civilizations, however, have the needed mappings; the
  obvious Barbarian mismatch is not reachable through the active-human hover
  caller. It remains a dormant mod-data hazard, not a supported F401.
* Finance unit-cost breakdown also leaves `iExtraCost` unscaled by inflation,
  but current SAS has zero UnitInfos with nonzero `iExtraCost`, so that inherited
  issue is data-dormant and not numbered.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F400 CONFIRMED in protected Queue 003.
F401 / provisional KI#724 UNUSED.
Latest physical checkpoint: C031-WIP101.
No repository source files modified.

NEXT
----
Continue from F401 unused through the remaining finance civic/supply/income
breakdowns, then return to changed building/project requirement contexts and
whole-file text-key/context/sentinel closure sweeps.

END C031-WIP101


===============================================================================
C031-WIP102 — QUEUE 003 CONTINUATION FROM WIP101
===============================================================================

-------------------------------------------------------------------------------
F401 / provisional KI#724 — Finance Advisor foreign-trade row can have a blank
hover when exactly one profitable foreign trade route exists
-------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::buildForeignTradeString` calculates the player's current
foreign trade yield and route count, but only assigns any hover text when:

    if(iForeignRoutes > 1)

The corresponding Finance Advisor row has a different visibility contract.
Current `CvDomesticAdvisor.py` obtains the trade yield through
`TradeUtil.calculateTradeRoutes(player)` and creates the Foreign Trade row
whenever:

    if iForeignTrade > 0:

Both the row label and its displayed amount use
`WIDGET_HELP_FINANCE_FOREIGN_TRADE`, so a positive one-route state produces a
visible interactive row whose C++ help builder returns an empty buffer.

CURRENT-DATA REACHABILITY
-------------------------
This does not require an exotic route-count distribution.

BtS `INITIAL_TRADE_ROUTES` is 1, and current SAS does not override that
inherited global define. `CvPlayer::initOtherData` applies that value through
`changeTradeRoutes`, and `CvCity::getTradeRoutes` starts from the player's
trade-route count.

A one-city player connected to a foreign civilization can therefore have one
available route slot filled by one foreign city and no second owned city whose
routes would inflate the empire-wide count. `CvCity::calculateTradeTotals`
counts every non-null foreign trade city independently, so this state gives:

    iForeignRoutes = 1

The route can ordinarily have positive commerce: `getBaseTradeProfit` clamps
base trade profit to at least 100 (hundredths), and the normal commerce trade
yield modifier is positive. Thus the Python Finance Advisor can simultaneously
have:

    iForeignTrade > 0     -> Foreign Trade row is shown
    iForeignRoutes == 1   -> C++ hover text is not assigned

The result is a visible, profitable Foreign Trade row with a blank tooltip.

DOMESTIC COMPANION
------------------
`buildDomesticTradeString` has the same `iDomesticRoutes > 1` predicate, while
the current Finance Advisor deliberately shows the domestic/trade row always
(`advc.086: "Always show this to avoid confusion"). Exactly-one-domestic-route
states therefore deserve the same treatment if reached. The foreign-route
construction above is already sufficient to establish the root without
needing to rely on domestic route symmetry or disruption edge cases.

TEXT CONTRACT
-------------
The localization keys currently read:

    "%d1[ICON_COMMERCE] from %d2 Domestic Trade Routes"
    "%d1[ICON_COMMERCE] from %d2 Foreign Trade Routes"

so the original `> 1` may have been intended to avoid a singular/plural grammar
issue. That does not make an empty hover on a visible Finance row correct. A
singular key, plural-aware localization, or simply displaying the count-1
summary would all preserve information better than suppressing the hover.

ANCESTRY
--------
K-Mod's current `CvGameTextMgr.cpp` does not contain these two finance-summary
builders. They were added to AdvCiv in commit
4a65bf5c0fb187fd25d4908346abb462354a9172,
"Option for expanding scoreboard; BULL hover text".

The introducing diff explicitly shows the AdvCiv `advc.086` replacement:

    //buildTradeString(... false, true, false);
    // <advc.086> Replacing the above
    ...
    if(iForeignRoutes > 1) {
        szBuffer.assign(...);
    }

The same commit adds the Finance Advisor widget parsers and imports the BULL
hover subsystem. Base AdvCiv 1.14 and current SAS retain the `> 1` gate.

Classification:
    AdvCiv/advc.086 Finance Advisor hover integration defect, retained by SAS.

REPAIR DIRECTION
----------------
Match the builder's visibility threshold to the data that makes the Finance
row meaningful:

    if(iForeignRoutes > 0)

and likewise for domestic trade if retaining these summary builders.

If English singular/plural grammar is important, use a dedicated count-1 text
key or a plural-aware text form rather than suppressing the only explanatory
hover.

-------------------------------------------------------------------------------
CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F401 CONFIRMED in protected Queue 003.
F402 / provisional KI#725 UNUSED.
Latest physical checkpoint: C031-WIP102.
No repository source files modified.

NEXT
----
Continue from F402 unused through the remaining Finance Advisor
civic/supply/income helpers, keeping F400-family rounding consequences under
one root unless a distinct state/variable contract appears. Then return to the
changed building/project requirement contexts and whole-file text-key,
active-context, sentinel and index closure sweeps.

END C031-WIP102


===============================================================================
C031-WIP103 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
F402 / provisional KI#725 CONFIRMED
===============================================================================

Continuation basis
------------------
Resumed from C031-WIP102 with Queue 003 still open and F402 unused. This pass
continued the AdvCiv-only TextMgr-member reconciliation rather than revisiting
already saturated linear regions. The first targets were the city/bonus trade
perspective helpers, relation helpers, civic-reveal button path, combat-action
helpers and other previously low-mention AdvCiv additions.

Controls before F402
--------------------

1. setCityTradeHelp perspective/liberation logic survives.

The Foreign Advisor folds city owner and recipient into WIDGET_CITY_TRADE.
CvDLLWidgetData::parseCityTradeHelp reconstructs those exact two players.
setCityTradeHelp then chooses eOtherPlayer from the active-player perspective
and calls AI_cityTrade on the actual city owner. The initially odd:

    bool bLiberate = (eWhoTo == eOtherPlayer &&
                      kCity.getLiberationPlayer() == eWhoTo);

is correct for the relevant direction: it describes a city owned by the active
player that the rival recipient wants liberated. When the rival owns the city
and the active player is recipient, the owner is instead described as willing
to cede it. The Foreign Advisor's own column population uses canTradeItem and
getTradeDenial/AI_cityTrade consistently with this helper. No F402 here.

2. setBonusTradeHelp valuation direction survives.

For a rival exporting a resource to the active human, the helper's
pActivePlayer->AI_goldForBonus(eBonus, eTradePlayer) asks how much the human is
willing to pay for the rival's bonus in the rival's estimate, matching
AI_goldForBonus/AI_bonusTradeVal's documented eBonusOwner/eFromPlayer contract.
For the opposite (rival importing the active player's resource), the rival's
AI_goldForBonus(eBonus, eActivePlayer) correctly gives how much that rival will
pay. The Foreign Advisor's +1000 import-column encoding is decoded consistently.
No supported perspective inversion found.

3. parseSingleCivicRevealHelp has a live No-Slavery omission, but it is a
   companion consequence of F396 rather than a new root.

The Tech Tree's WIDGET_HELP_CIVIC_REVEAL calls parseSingleCivicRevealHelp.
That helper intentionally passes:

    parseCivicInfo(..., bCivilopediaText=true,
                   bPlayerContext=true, bSkipName=true)

in order to hide the civic's technology prerequisite. AdvCiv's No-Slavery
warning in parseCivicInfo is itself gated by !bCivilopediaText, so a Slavery
button revealed by Masonry also omits the "cannot adopt" warning under No
Slavery. This is the same AdvCiv option/help integration omission already
numbered F396 (tech help says Masonry enables an unavailable Slavery civic),
not an independent root.

-------------------------------------------------------------------------------
F402 / provisional KI#725 — PAIR-SPECIFIC LEADER-RELATIONS HOVERS CAN LIST
UNRELATED THIRD-PARTY WARS / WORST-ENEMY RELATIONS
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary functions:
    CvGameTextMgr::parseLeaderHeadRelationsHelp
    CvGameTextMgr::getOtherRelationsString(PlayerTypes, PlayerTypes)
    CvGameTextMgr::getOtherRelationsString(TeamTypes, TeamTypes, TeamTypes)
Live callers:
    CvDLLWidgetData::parseLeaderheadRelationsHelp
    WIDGET_LEADERHEAD_RELATIONS
    WIDGET_LH_GLANCE
    CvForeignAdvisor.py Glance matrix
    CvBUGMilitaryAdvisor.py relation icons
Classification:
    AdvCiv BULL/K-Mod leader-relations integration defect; retained by SAS.

ROOT
----
AdvCiv contains two incompatible contracts for the same relation helper chain.

The top-level pair hover explicitly says:

    // Displays the relations between two leaders only.
    // This is used by the F4:GLANCE and F5:SIT-REP tabs.

and, for a non-active-player pair, does:

    else getOtherRelationsString(szBuffer, eThisPlayer, eOtherPlayer);

The PlayerTypes overload converts that pair directly to:

    getOtherRelationsString(szString,
        TEAMID(eThisPlayer), TEAMID(eOtherPlayer), NO_TEAM);

The TeamTypes overload's own contract then says:

    If eOtherTeam is not NO_TEAM, only relations between it and
    eThisTeam are shown.

But current code explicitly marks eOtherTeam unused and comments out the only
predicate that would implement that contract:

    void CvGameTextMgr::getOtherRelationsString(...,
        TeamTypes eThisTeam,
        /* (advc: unused) */ TeamTypes eOtherTeam,
        TeamTypes eSkipTeam)
    {
        ...
        if (itThird->getID() == eSkipTeam)
            // || (eOtherTeam != NO_TEAM &&
            //     itThird->getID() != eOtherTeam)
            continue;
        ...
    }

Consequently, supplying eOtherTeam does not restrict anything. The iterator
walks every major civilization known to eThisTeam and visible to the active
team, and appends every matching war / worst-enemy relation it finds.

LIVE UI PATH
------------
Current CvForeignAdvisor.py Glance cells use either
WIDGET_LEADERHEAD_RELATIONS or WIDGET_LH_GLANCE, with the matrix's two player
IDs stored in data1/data2. Both widget parsers reach
parseLeaderHeadRelationsHelp(data1, data2).

Current CvBUGMilitaryAdvisor.py also uses WIDGET_LEADERHEAD_RELATIONS for
specific pair icons such as:

- current-war opponents,
- vassals,
- defensive pacts,
- possible war targets,
- possible embargo targets,
- worst enemy.

Thus this is not a dead helper or a generic leader tooltip only; it is directly
used on UI elements whose visible cell/icon identifies a particular pair.

CONCRETE STATE
--------------
Let the active human know all three AI civilizations A, B and C.

Suppose:

    A is at peace with B,
    A is at war with C.

Hover a Glance matrix cell representing the A/B pair (or another pair-specific
WIDGET_LEADERHEAD_RELATIONS icon involving A and B).

parseLeaderHeadRelationsHelp first shows A/B-specific material such as
attitude and active deals. Because B is not the active player, it then calls
getOtherRelationsString(A, B).

The intended eOtherTeam=B filter is disabled. The iterator reaches C, sees
that C is at war with A, and appends:

    "At war with C"

Even though C is not one of the two leaders represented by the hovered cell.
If another known AI has A as worst enemy, that unrelated civilization can be
appended as well.

The amount of unrelated output grows with the number of known third parties
and A's diplomatic state; this is not a one-off formatting discrepancy.

WHY THIS IS NOT THE GENERAL K-MOD LEADER-HOVER CONTRACT
-------------------------------------------------------
There is a legitimate separate caller with broader semantics:
parseLeaderHeadHelp (the ordinary leader tooltip) intentionally retains
K-Mod's behavior of showing a leader's relations with other civilizations.
That explains why simply deleting all third-party relation display would be
wrong.

The defect is the collision between that broad K-Mod behavior and the BULL
pair-specific helper. parseLeaderHeadRelationsHelp explicitly promises
"relations between two leaders only," while the shared PlayerTypes overload
currently routes it through the broad behavior.

ANCESTRY
--------
Civ4CE/BtS and K-Mod have only the older PlayerTypes
getOtherRelationsString. K-Mod deliberately broadened that old helper by
allowing the selected other player's team to appear among the leader's wider
relations (its comment: "show 'at war' even for the civ selected").

AdvCiv commit:

    4a65bf5c0fb187fd25d4908346abb462354a9172
    "Option for expanding scoreboard; BULL hover text"

introduced the BULL relation-hover subsystem, including:

- parseLeaderHeadRelationsHelp with the explicit two-leader-only contract,
- getAllRelationsString,
- getActiveTeamRelationsString,
- the TeamTypes getOtherRelationsString with eOtherTeam/eSkipTeam controls.

The introducing diff already contains the contradictory merge:

    /* advc: This BULL function replaces ... I've merged just one K-Mod
       change from the latter. */
    ...
    i == eSkipTeam /*|| (eOtherTeam != NO_TEAM && i != eOtherTeam)*/

and marks eOtherTeam unused. In other words, while importing BULL's
pair-filterable helper, AdvCiv applied K-Mod's broader old-helper behavior by
commenting out the new helper's eOtherTeam filter. Base AdvCiv 1.14 and current
SAS retain that state.

This is therefore best classified as an AdvCiv BULL/K-Mod integration defect,
not a SAS-local regression and not a raw BtS bug.

REPAIR DIRECTION
----------------
The two contracts need to remain distinct.

For pair-specific relation help, honor eOtherTeam:

    if (itThird->getID() == eSkipTeam ||
        (eOtherTeam != NO_TEAM && itThird->getID() != eOtherTeam))
    {
        continue;
    }

But do not blindly narrow the ordinary K-Mod-style leader hover, which
intentionally wants broader third-party relations. One conservative structure
is:

- let pair-specific parseLeaderHeadRelationsHelp call the TeamTypes overload
  with eOtherTeam set, and honor the filter there;
- let broad parseLeaderHeadHelp call the TeamTypes overload with
  eOtherTeam=NO_TEAM (and an appropriate skip team if desired), preserving its
  broader relation summary.

Equivalently, split pair and broad modes explicitly so a PlayerTypes overload
cannot silently serve both incompatible purposes.

RELATED CONTROL
---------------
The TeamTypes helper also accumulates szPeace and szPact but currently never
prints them. That is suspicious against its "peace/war/enemy/pact" comment,
but direct pair peace/pact information is already covered by active-deal help,
and the historical merge needs a separate consumer/intent proof before this
should be numbered independently. It is therefore retained as closure debt,
not promoted as F403 here.

-------------------------------------------------------------------------------
CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F402 CONFIRMED in protected Queue 003.
F403 / provisional KI#726 UNUSED.
Latest physical checkpoint: C031-WIP103.
No repository source files modified.

NEXT
----
Continue the AdvCiv-only member reconciliation from F403 unused. Priority:
finish the relation helper family (including the unprinted peace/pact control),
then inspect setCannotAttackHelp / setInterceptPlotHelp execution-vs-help
contracts, setPillageHelp/buildDisengageString, and the remaining low-mention
value/unit aggregation helpers. Afterwards resume whole-file sentinel,
active-context and calculation/runtime reconciliation before deciding whether
Queue 003 can close.

END C031-WIP103


===============================================================================
C031-WIP104 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
F403 / provisional KI#726 CONFIRMED
===============================================================================

Continuation basis
------------------
Resumed from C031-WIP103 with Queue 003 still open and F403 unused. The first
unfinished control was the relation helper's szPeace / szPact accumulation.
This pass finished the live-caller proof rather than promoting the unused local
variables merely because they looked suspicious.

-------------------------------------------------------------------------------
F403 / provisional KI#726 — THIRD-PARTY FORCE-PEACE AND DEFENSIVE-PACT STATES
ARE COMPUTED BUT NEVER RENDERED IN LEADER-RELATIONS HELP
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary functions:
    CvGameTextMgr::getAllRelationsString
    CvGameTextMgr::getActiveTeamRelationsString
    CvGameTextMgr::getOtherRelationsString(TeamTypes, TeamTypes, TeamTypes)
Live broad consumer:
    CvGameTextMgr::parseLeaderHeadHelp
    ordinary leader / scoreboard-style help through CvDLLWidgetData
Classification:
    AdvCiv BULL leader-relations integration omission; retained by SAS.

ROOT
----
The TeamTypes relation helper explicitly advertises four relation families:

    "peace/war/enemy/pact status"

and allocates / fills four corresponding strings:

    CvWString szWar, szPeace, szEnemy, szPact;

    if (itThird->isAtWar(eThisTeam))
        setListHelp(szWar, ...);
    else if (itThird->isForcePeace(eThisTeam))
        setListHelp(szPeace, ...);
    ...
    if (itThird->isDefensivePact(eThisTeam))
        setListHelp(szPact, ...);

But the function's output section renders only szWar and szEnemy. szPeace and
szPact are never appended to szString at all.

This is not harmless dead computation in the broad leader-hover path.
parseLeaderHeadHelp(eThisPlayer, eOtherPlayer) does:

    getActiveDealsString(szBuffer, eThisPlayer, eOtherPlayer);
    ...
    getOtherRelationsString(szBuffer, eThisPlayer, eOtherPlayer);

The active-deal call is strictly pair-specific: getActiveDealsString iterates
only deals for which pDeal->isBetween(eThisPlayer, eOtherPlayer). In the usual
leader hover eOtherPlayer is the active human. It therefore cannot render a
peace treaty or defensive pact between the hovered AI and some third AI.

CONCRETE LIVE STATE
-------------------
Let the active human H have met AI teams A and B, and let A and B have a
Defensive Pact.

Hover A through a normal leader-help consumer with H as the comparison player.
The pair-specific active-deal scan can display A<->H deals only. The subsequent
relation scan reaches B, verifies that the active team knows B, sees:

    B.isDefensivePact(A) == true

and appends B's name to szPact. But there is no later output block for szPact,
so the broader leader-relations help omits that known A<->B pact entirely.

The same omission applies to a known force-peace relation between A and B:
szPeace is populated but never rendered.

WHY THIS IS DISTINCT FROM F402
------------------------------
F402 is a filtering defect: pair-specific A/B hovers accidentally include
unrelated C/D war and worst-enemy information because eOtherTeam is ignored.

F403 is an output defect: even in the broad relation mode where third-party
relations are wanted, two relation families that the helper deliberately
collects are silently discarded.

Restoring the eOtherTeam filter for F402 does not repair F403, and rendering
szPeace/szPact does not repair F402. They therefore have independent roots and
repairs.

ACTIVE-PLAYER PAIR CONTROL
--------------------------
getActiveTeamRelationsString intentionally comments out force-peace and
Defensive Pact lines for relations directly involving the active player:

    // advc: Covered by active deals

That is a valid duplicate-suppression argument because getActiveDealsString can
show the direct A<->H treaty/pact. It does not apply to A<->B third-party
relations in parseLeaderHeadHelp, because that active-deal scan never examines
A<->B.

ANCESTRY
--------
Civ4CE/BtS and K-Mod contain only the older PlayerTypes relation helper and do
not contain this four-list BULL TeamTypes implementation.

AdvCiv commit:

    4a65bf5c0fb187fd25d4908346abb462354a9172
    "Option for expanding scoreboard; BULL hover text"

introduced the BULL relation subsystem. The introducing diff already creates,
populates, and then drops szPeace and szPact while rendering only szWar and
szEnemy. The same introducing code comments out the active-player peace/pact
lines specifically because active deals cover them, which strengthens the
contrast: no equivalent third-party deal consumer exists for the discarded
lists.

Base AdvCiv 1.14 and current SAS retain the omission unchanged.

REPAIR DIRECTION
----------------
After the existing szWar output, render non-empty szPeace with the existing
TXT_KEY_PEACE_TREATY_WITH key; after the relevant relation output, render
non-empty szPact with TXT_KEY_DEFENSIVE_PACT_WITH. Preserve the direct
active-player duplicate suppression in getActiveTeamRelationsString.

When combined with F402's repair, pair-specific mode should honor eOtherTeam,
while broad mode should continue scanning all known third parties and actually
render all relation families it collects.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F403
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F403 CONFIRMED in protected Queue 003.
F404 / provisional KI#727 UNUSED.
No repository source files modified.

NEXT
----
Continue immediately with setCannotAttackHelp / combat-action execution-vs-help
contracts, then setInterceptPlotHelp, setPillageHelp/buildDisengageString and
the remaining low-mention AdvCiv additions before returning to whole-file
sentinel/context/calculation closure sweeps.

END C031-WIP104


===============================================================================
C031-WIP105 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
F404 / provisional KI#727 CONFIRMED
===============================================================================

Continuation basis
------------------
Resumed from C031-WIP104 with F404 unused. The first action-help family
(setCannotAttackHelp, setInterceptPlotHelp, setPillageHelp/buildDisengageString)
did not justify a finding: setCannotAttackHelp explicitly documents that it
only explains the damage-limit condition so far; the interception helper's
visible-interceptor restriction is an information-leak safeguard; pillage and
disengage text matched their execution contracts. The low-mention AdvCiv
helper reconciliation then reopened the founding-yield projection.

-------------------------------------------------------------------------------
F404 / provisional KI#727 — ADVCIV FOUNDING HOVER OMITS GOLDEN-AGE YIELD FROM
THE PROJECTED CITY HOME TILE
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary function: CvGameTextMgr::setHomePlotYieldHelp
Runtime comparator: CvPlot::calculateYield / CvPlot::calculateCityPlotYieldChange
Live consumer: CvDLLWidgetData MISSION_FOUND action help
Classification: inherited AdvCiv founding-help projection omission; retained by
Base AdvCiv 1.14 and current SAS.

ROOT
----
The Found mission hover labels a line as the projected home-tile yield, but
setHomePlotYieldHelp reconstructs only nature yield and the minimum city yield:

    int iYieldRate = kCityPlot.calculateNatureYield(
            eLoopYield, getActiveTeam(), true);
    CvYieldInfo const& kLoopYield = GC.getInfo(eLoopYield);
    iYieldRate = std::max(iYieldRate, kLoopYield.getMinCity());

Actual city-center yield does not stop there. CvPlot::calculateYield performs
its ordinary plot calculation and, once the plot has a city, applies
calculateCityPlotYieldChange. It then still applies player-level yield
thresholds and Golden Age yield:

    if (GET_PLAYER(ePlayer).isGoldenAge())
    {
        if (iYield >= GC.getInfo(eYield).getGoldenAgeYieldThreshold())
            iYield += GC.getInfo(eYield).getGoldenAgeYield();
    }

The founding helper never applies that Golden Age stage.

CONCRETE CURRENT-SAS STATE
--------------------------
Current Yield XML has:

    YIELD_FOOD:
        iMinCity = 2
        iGoldenAgeYield = 0

    YIELD_PRODUCTION:
        iMinCity = 1
        iGoldenAgeYieldThreshold = 1
        iGoldenAgeYield = 1

    YIELD_COMMERCE:
        iMinCity = 1
        iGoldenAgeYieldThreshold = 1
        iGoldenAgeYield = 1

Take an ordinary legal city site whose post-city minimum home-tile values are
2 food, 1 hammer and 1 commerce, and let the active player be in a Golden Age.
The founding hover reconstructs and displays:

    Home Tile Yield: 2 food, 1 hammer, 1 commerce

After founding, CvCity::init assigns the owner and calls kPlot.setPlotCity(this).
CvPlot::setPlotCity calls updateYield, so the new city plot immediately runs
CvPlot::calculateYield as a city tile while its owner is still in the same
Golden Age. The 1 hammer and 1 commerce both meet their threshold, producing:

    actual home tile: 2 food, 2 hammers, 2 commerce

Nothing about founding a city ends the player's Golden Age, and Settlers remain
ordinary live founding units in current SAS. This is therefore reachable
normal gameplay, not a hypothetical XML configuration.

BROADER CONTRACT
----------------
The root is slightly broader than Golden Ages: setHomePlotYieldHelp is a manual
partial reconstruction of CvPlot::calculateYield. Player-level extra-yield
thresholds are also evaluated after the city-plot stage at runtime and are not
reproduced by this helper. Golden Age yield is the clean current-data proof and
sufficient live consequence; no speculative extra case is needed for F404.

WHY THE PREVIEW REALLY MEANS POST-FOUNDING YIELD
------------------------------------------------
The helper is called only in the successful MISSION_FOUND action-help path,
alongside setFoundCostHelp and setFoundHealthHelp, and its localization label
is TXT_KEY_HOME_TILE_YIELD. It is explicitly a projection of the city that will
exist after the action, not merely a description of the terrain before the
city exists.

ANCESTRY
--------
This helper is not a BtS/K-Mod member. The founding preview was introduced by
AdvCiv's advc.004b work. Reachable-history commit:

    f6ff6a182f5187541fff6e5bbbe12f9573b3778c
    "Initial commit (cont.)"

already contains the original CvDLLWidgetData::getHomePlotYieldText with the
same nature-yield + MinCity reconstruction. AdvCiv commit:

    6f8d41a1ac548879d04f56ff9210e9a5e10aa975
    "Help text for feature health made more flexible, refactored."

later moved that logic into CvGameTextMgr::setHomePlotYieldHelp without changing
the calculation. Base AdvCiv 1.14 and current SAS retain it.

REPAIR DIRECTION
----------------
Avoid stopping at NatureYield + MinCity. The projection should mirror the
post-founding CvPlot::calculateYield stages that can be evaluated without
actually creating a city:

1. calculate nature yield for the active team;
2. apply the city-plot yield change/minimum for CvCity::initialPopulation();
3. apply the active player's normal and natural extra-yield thresholds;
4. apply Golden Age threshold/yield;
5. clamp as runtime does.

Prefer sharing/factoring the runtime city-plot/player-yield logic where
practical rather than maintaining a second partial formula that can drift
again. Current RouteInfos have no direct route yields, so the newly created
city route does not add another current-data discrepancy here.

CONTROL — TECH "CAN TRAIN/CREATE" UNDER GLOBAL BLOCKERS
--------------------------------------------------------
A neighboring candidate was deliberately rejected. Player-context tech help
can still list a unit/project whose tech prerequisite is eTech even when an
external condition such as No Nukes currently blocks construction. That
section already represents prerequisite relationships rather than immediate
eligibility: it likewise lists targets whose other AND-techs, resources,
projects, etc. are still missing. Therefore treating every external blocker as
a false "Can Train/Create" promise would overstate that UI contract. No F404
was spent on that path.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F404
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F404 CONFIRMED in protected Queue 003.
F405 / provisional KI#728 UNUSED.
Latest physical checkpoint: C031-WIP105.
No repository source files modified.

NEXT
----
Continue the remaining low-mention AdvCiv member reconciliation from F405
unused: building-vote-source religion yield, unit aggregation/combat modifier
helpers, specialist/corporation links and debug/action helpers. Then resume the
whole-file sentinel, player/team context, class/civilization resolution and
runtime-calculation closure sweeps before deciding whether Queue 003 can close.

END C031-WIP105


===============================================================================
C031-WIP106 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
F405 / provisional KI#728 CONFIRMED
===============================================================================

Continuation basis
------------------
Continued from C031-WIP105 / F405 unused into the low-mention AdvCiv debug-help
helpers. The Ctrl+Alt path-reachability display contains a persistent cache
whose key does not include the source plot even though every cached query does.

-------------------------------------------------------------------------------
F405 / provisional KI#728 — CTRL+ALT PLOT DEBUG HELP REUSES ENEMY-CITY PATH
RESULTS FROM A DIFFERENT PLOT OWNED BY THE SAME TEAM
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary function: CvGameTextMgr::setPlotHelpDebug_Ctrl
Runtime comparator: CvTeamAI::AI_isHasPathToEnemyCity overloads
Classification: inherited AdvCiv debug-UI stale-cache defect; retained by Base
AdvCiv 1.14 and current SAS.

ROOT
----
For an owned non-city land plot under Ctrl+Alt debug help, the function caches
whether the plot owner's team has a path to enemy cities:

    static TeamTypes eCacheTeam = NO_TEAM;
    static bool bHasPath = false;
    static ArrayEnumMap<TeamTypes,bool> abHasPath;
    bool const bUpdCache = (kPlotTeam.getID() != eCacheTeam);
    eCacheTeam = kPlotTeam.getID();

    if (bUpdCache)
        bHasPath = kPlotTeam.AI_isHasPathToEnemyCity(kPlot);

and, for every target team:

    if (bUpdCache)
        abHasPath.set(itTarget->getID(),
            &kPlotTeam == &*itTarget ||
            kPlotTeam.AI_isHasPathToEnemyCity(kPlot, *itTarget));

The cache invalidation condition contains only TeamTypes. The queried source
`kPlot` is absent from the cache key.

The first hovered plot for a team therefore determines the displayed results
for every subsequently hovered eligible plot owned by that same team until a
plot owned by a different team happens to be inspected.

SOURCE PLOT IS SEMANTIC INPUT, NOT COSMETIC
-------------------------------------------
`CvTeamAI::AI_isHasPathToEnemyCity(CvPlot const& kFrom, CvTeam const& kEnemy)`
constructs a TeamPathFinder and, for each enemy city in the same area, calls:

    pathFinder.generatePath(kFrom, pCity->getPlot())

The aggregate overload likewise passes the supplied `kFrom` into the
per-enemy-team overload.

Thus two plots of the same owner can legitimately have different answers due
to geography, impassable boundaries, hostile borders and reachable city
locations. Team identity cannot substitute for the source plot.

CONCRETE REPRODUCTION SHAPE
---------------------------
Run with debug mode enabled and inspect owned, non-city, non-water tiles using
Ctrl+Alt.

Let A and B be two land plots owned by the same team where:

    AI_isHasPathToEnemyCity(A, enemy) == true
    AI_isHasPathToEnemyCity(B, enemy) == false

Hover A first. `eCacheTeam` changes to the owner's team and the cache stores
A's result. Hover B next. The team ID is unchanged, so `bUpdCache == false` and
neither `bHasPath` nor any entry in `abHasPath` is recomputed.

B's debug tooltip therefore reports A's reachability, including both:

    "Can reach an enemy city" / "No reachable enemy cities"

and each per-target-team "Can reach ... city" line.

The reverse hover order produces the reverse stale result, proving display
output depends on the previously inspected plot rather than only current game
state.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same team-only cache.

Reachable-history commit:

    a936404e1b97a691e39e57a3dcb41f652796b0a0
    "Fix BBAI bug in PathToEnemyCity check"

introduced the static TeamTypes/bool/per-team cache while replacing the older
BBAI player-city path display. Its intent comment says not to recompute this
"over and over as the user inspects" the text.

The initial cache patch did not update `eCacheTeam`, which accidentally caused
the expensive path result to be recomputed every time and therefore avoided
this particular stale-plot behavior.

A later amendment:

    e7c8b9592e0dd30273950bde6c3746d85a97f0ed
    "Amend a minor bug in CvGameTextMgr"

added:

    eCacheTeam = kPlotTeam.getID();

making the cache persistent as intended, but still without adding the source
plot to the key. Current Base AdvCiv and SAS retain that state.

This is therefore an AdvCiv optimization/cache regression in debug help rather
than a raw BtS/K-Mod root.

REPAIR DIRECTION
----------------
At minimum, key the cache by both team and source plot, e.g. team plus plot
index/coordinates (or the plot pointer while map storage is stable):

    bUpdCache = (eTeam != eCacheTeam || iPlot != iCachePlot);

A more robust debug cache should also consider invalidation when relevant game
state changes; a simple per-call computation is acceptable if this diagnostic
path is infrequent enough. The essential correctness requirement is that a
result computed from `kPlot A` must never be reused for `kPlot B` solely
because both share an owner team.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F405
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F405 CONFIRMED in protected Queue 003.
F406 / provisional KI#729 UNUSED.
Latest physical checkpoint: C031-WIP106.
No repository source files modified.

NEXT
----
Continue the remaining low-mention AdvCiv helpers from F406 unused, especially
building-vote-source religion yield, unit aggregation/combat modifier helpers,
link/value helpers and the rest of debug/action help. Then run the planned
whole-file sentinel, active-context, civilization/class-resolution and
calculation-vs-runtime closure sweeps before deciding whether Queue 003 can
close.

END C031-WIP106


===============================================================================
C031-WIP107 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
F406 / provisional KI#729 CONFIRMED
===============================================================================

Continuation basis
------------------
Continued from C031-WIP106 / F406 unused by extending F405's static-cache
pattern sweep through the neighboring TeamPath diagnostic cache.

-------------------------------------------------------------------------------
F406 / provisional KI#729 — CTRL+SHIFT TEAM-PATH DEBUG HELP CAN REUSE PATH
COST/LENGTH AFTER THE SAME SELECTED UNIT'S SOURCE PLOT HAS CHANGED
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary function: CvGameTextMgr::setPlotHelpDebug_Ctrl
Classification: inherited AdvCiv debug-UI stale-cache defect; retained by Base
AdvCiv 1.14 and current SAS; absent from K-Mod/Civ4CE because this TeamPath
 diagnostic was added by AdvCiv.

ROOT
----
The Ctrl+Shift TeamPath diagnostic avoids recomputation with:

    static CvPlot const* pOldPlot = NULL;
    static CvUnit const* pOldUnit = NULL;
    static int iCost = MAX_INT;
    static int iLength = MAX_INT;

    if (pPlot != pOldPlot || pUnit != pOldUnit)
    {
        pOldPlot = pPlot;
        pOldUnit = pUnit;
        ...
        CvPlot const& kStart = pUnit->getPlot();
        CvPlot const& kDest = *pPlot;
        ... generatePath(kStart, kDest) ...
    }

The cache key therefore contains the destination-plot pointer and selected-unit
pointer, but not the selected unit's current source plot. The cached value is a
function of that omitted source plot.

LIVE STALE-STATE SHAPE
----------------------
Keep one unit selected and inspect a destination plot D under the diagnostic.
The cache stores the TeamPath cost/length from the unit's current source A to D.
If that same unit subsequently moves to B while remaining the same CvUnit
object, then the semantic query becomes B -> D. If D remains/rebecomes the same
mouse-over CvPlot object, both cache keys still compare equal:

    pPlot == pOldPlot
    pUnit == pOldUnit

so no pathfinder is run and the text still prints A -> D's old cost/length.

This is not a mere formatting cache: the omitted input is read explicitly as
`pUnit->getPlot()` immediately before `generatePath`. Different source plots
can have different path lengths, route costs or even reachability.

The same underspecified key can also retain stale results when path-relevant
map/diplomatic state changes while both object pointers remain unchanged (for
example routes or war-access state). The moving-source case alone is sufficient
for the root.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same cache. K-Mod and Civ4CE/BtS do not contain
this TeamPath debug display.

Reachable-history commit:

    147c51de866e5decea37cebabce251b5061bf574
    "TeamPathFinder with test code (CvGameTextMgr)"

introduced the diagnostic and initially cached only `pOldPlot`.

The follow-up:

    9294e00681e1f5307a4a3f81a35e53959a3c9aff
    "Test code ensuring symmetrical team path distances"

noticed another omitted input and added `pOldUnit`, changing the invalidation
condition to the current:

    pPlot != pOldPlot || pUnit != pOldUnit

but still did not include the unit's current source plot or other mutable path
state. Current Base AdvCiv and SAS retain that partial key.

DISTINCT FROM F405
------------------
F405 caches enemy-city reachability by owner TeamTypes while omitting the
queried source plot `kPlot`. F406 is a separate static cache in the TeamPath
cost/length diagnostic: it does include the destination plot and unit identity,
but omits mutable state of that same unit, most directly `pUnit->getPlot()`.
The caches have different producers, keys, values and visible stale-state
transitions.

REPAIR DIRECTION
----------------
At minimum, include the source plot in cache invalidation, e.g. remember
`pUnit->plot()` / plot index alongside `pOldPlot` and `pOldUnit`.

For a diagnostic path whose correctness depends on routes, diplomacy and other
mutable traversal state, a turn/state stamp or simply recomputing on tooltip
refresh may be safer. The essential fix is not to treat object identity as a
complete representation of the path query's state.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F406
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F406 CONFIRMED in protected Queue 003.
F407 / provisional KI#730 UNUSED.
Latest physical checkpoint: C031-WIP107.
No repository source files modified.

NEXT
----
Continue the remaining low-mention non-debug helpers from F407 unused,
especially building-vote-source religion yield, unit aggregation/combat helper
contracts, specialist/corporation links and value-format helpers. Then resume
the whole-file sentinel, active-context, civilization/class-resolution and
runtime-calculation closure sweeps before deciding whether Queue 003 can close.

END C031-WIP107


===============================================================================
C031-WIP108 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION
POST-WIP107 CLOSURE CONTROLS + F407 / provisional KI#730 CONFIRMED
===============================================================================

Continuation basis
------------------
Continued from C031-WIP107 / F407 unused by finishing the outstanding
UnitClass/BuildingClass -> civilization-resolution sweep, reopening the small
AdvCiv-added helper inventory, and continuing sentinel/active-context/runtime
calculation checks.

-------------------------------------------------------------------------------
POST-WIP107 CONTROLS — NO NEW NUMBER
-------------------------------------------------------------------------------

1. UnitClass / BuildingClass civilization resolution
----------------------------------------------------
The current player-context help paths inspected generally resolve an actual
player/civilization's unit or building before falling back to the class default.
The remaining default-class uses do not produce a current-SAS UU/UB mismatch:

* The Great Artist/Scientist/Merchant/Engineer/Spy UnitClasses used by the
  building Great-Person/free-unit helpers only differ for the Barbarian
  civilization, where the mapping is NONE. The normal player-facing building
  callers do not reach that combination.
* Religion free-unit classes in current XML are missionary-specific and have
  no civilization replacement that can make the default disagree with a
  player's actual unit.
* No current live FreeBuildingClass case established an analogous UB mismatch.

So the class-resolution family is provisionally closed without F407.

2. appendCombatModifiers Barbarian-city-attack omission
-------------------------------------------------------
Runtime combat strength contains the attack-side Handicap
BarbarianCityAttackBonus when a Barbarian attacks a city, while
appendCombatModifiers does not print that term. This is a real helper-
completeness asymmetry, but the ordinary combat-odds callers obtain the
attacker from the active player's selected group. They therefore do not supply
a Barbarian attacker in normal player UI. Kept as an unreachable-orientation
control rather than F407.

3. setSpecialistLink singular/plural polarity
---------------------------------------------
One building-slot caller computes `bSingular` and passes it directly to the
helper parameter named `bPlural`, which is source-level opposite polarity.
The parameter mainly changes grammatical link-target syntax for Italian/
Spanish while the visible description is passed separately. Without a proven
broken visible link/text state in the current EXE/localization resolver, this
remains an unresolved low-level grammar/link hazard rather than a numbered
finding.

4. Sentinel / dead-path controls retained from the closure sweep
----------------------------------------------------------------
The sweep reconfirmed several genuine defensive/source oddities without a live
current-SAS consequence strong enough for a finding:

* a plot-resource branch compares ImprovementTypes against NULL rather than
  NO_IMPROVEMENT; current path/data short-circuits mask the sentinel collision;
* old setGoldStr negative-color formatting uses SETCOLR where ENDCOLR is
  required, but current BUG Main Interface no longer calls that inherited path;
* setResearchStr can reuse the repeat-tech ordinal buffer if research turns are
  negative, but current SAS has no repeatable technologies;
* parseSpecialistHelp retrieves GC.getInfo(eSpecialist) before its own
  NO_SPECIALIST guard, but all current UI callers traced supply a valid
  specialist or reject the sentinel first;
* eventGoldHelp has asymmetric sign handling for a randomized negative-gold
  range, but current event XML has no reachable negative randomized range.

These are worth preserving as future mod-mod/cleanup controls, but none consumes
F407 under the current audit threshold.

-------------------------------------------------------------------------------
F407 / provisional KI#730 — CITY-BAR TEMPORARY-ANGER SUMMARY TAKES THE MAXIMUM
SOURCE INSTEAD OF ADDING SIMULTANEOUS HURRY, DRAFT AND DEFY ANGER
-------------------------------------------------------------------------------
Status: CONFIRMED
File: CvGameCoreDLL/CvGameTextMgr.cpp
Primary function: CvGameTextMgr::setCityBarHelp
Runtime comparison: CvCity::unhappyLevel, getHurryPercentAnger,
getConscriptPercentAnger, getDefyResolutionPercentAnger
Classification: AdvCiv BULL-integration aggregation/display defect; introduced
by AdvCiv city-bar hover integration and retained by Base AdvCiv 1.14 and
current SAS.

ROOT
----
The city-bar temporary-anger helper first takes the longest timer:

    int iAngerTimer = std::max(kCity.getHurryAngerTimer(),
            kCity.getConscriptAngerTimer());
    iAngerTimer = std::max(iAngerTimer,
            kCity.getDefyResolutionAngerTimer());

It then also takes only the greatest anger percentage:

    int iPercentAnger = std::max(kCity.getHurryPercentAnger(),
            kCity.getConscriptPercentAnger());
    iPercentAnger = std::max(iPercentAnger,
            kCity.getDefyResolutionPercentAnger());
    int iAngryPop = (iPercentAnger * kCity.getPopulation()) /
            GC.getPERCENT_ANGER_DIVISOR();

and formats:

    TXT_KEY_CITY_BAR_ANGER_TIMER
    "-%d1[ICON_UNHAPPY] in %d2 Turns"

Runtime city unhappiness does not choose one temporary-anger source. In
CvCity::unhappyLevel it ADDS all three before the one final population/divisor
conversion:

    iAngerPercent += getHurryPercentAnger(iExtra);
    iAngerPercent += getConscriptPercentAnger(iExtra);
    iAngerPercent += getDefyResolutionPercentAnger(iExtra);
    ...
    iUnhappiness = (iAngerPercent * (getPopulation() + iExtra)) /
            GC.getPERCENT_ANGER_DIVISOR();

Therefore simultaneous temporary anger is cumulative at runtime but mutually
exclusive in this city-bar display.

CURRENT-DATA REACHABILITY
-------------------------
This is ordinary current SAS gameplay, not an XML combination requiring a
custom scenario:

* CIVIC_SLAVERY is a Labor civic and enables HURRY_POPULATION.
* CIVIC_NATIONHOOD is a Legal civic and allows up to 3 conscriptions.
  The two civics can therefore be active simultaneously.
* Normal speed has iHurryConscriptAngerPercent = 100.
* BtS inherited defines used by current SAS are:

      HURRY_ANGER_DIVISOR = 10
      CONSCRIPT_ANGER_DIVISOR = 10
      HURRY_POP_ANGER = 1
      CONSCRIPT_POP_ANGER = 3
      PERCENT_ANGER_DIVISOR = 1000
      CONSCRIPT_POPULATION_PER_COST = 60

* Current UNIT_RIFLEMAN costs 120 production, so when it is the conscription
  unit getConscriptPopulation() returns max(1, 120 / 60) = 2.
* Current HURRY_POPULATION gives 27 production per population, so an ordinary
  already-started build with sufficiently little adjusted production remaining
  can be a one-pop whip.
* The CityScreen Anger Counter option controlling this city-bar line is enabled
  by default.

CONCRETE NUMERIC STATE
----------------------
On Normal speed, take an ordinary population-11 city running Slavery and
Nationhood in a state where Rifleman is its conscription unit.

1. Hurry an eligible build for 1 population.
   Population becomes 10 and hurry anger timer becomes 10.
2. Conscript the 120-production Rifleman for 2 population.
   Population becomes 8 and conscription anger timer becomes 10.

At final population 8 with one layer of each anger:

    hurry percent = 1 + (1 * 1 * 1000 / 8) = 126
    draft percent = 1 + (1 * 3 * 1000 / 8) = 376

Runtime temporary-anger contribution:

    (126 + 376) * 8 / 1000
    = 4016 / 1000
    = 4 unhappy citizens

City-bar helper:

    max(126, 376) * 8 / 1000
    = 3008 / 1000
    = 3 unhappy citizens

Thus the city-bar tooltip can print:

    -3 unhappy in 10 turns

while the same city's actual current unhappiness calculation contains 4 unhappy
citizens from those two temporary sources alone.

The issue is not limited to equal timers. With different source timers or
multiple stacked anger layers, the helper independently takes the maximum
percentage and maximum duration. Those maxima can come from different anger
sources, producing a displayed `(angry citizens, turns)` pair that corresponds
to no single underlying temporary-anger state.

ANCESTRY
--------
Base AdvCiv 1.14 contains the same max-based block. K-Mod/Civ4CE do not contain
AdvCiv's integrated C++ city-bar helper in this form.

Reachable-history commit:

    92202f546295e43857d6172e1b9c6665446e68a5
    "Overhaul city bar hover text (based on BULL)"

introduced this exact block into CvGameTextMgr::setCityBarHelp as advc.186.
The same patch notes that BULL had separate CityBar__HurryAnger and
CityBar__DraftAnger options, while AdvCiv ties the consolidated city-bar line to
CityScreen__Anger_Counter. The introduced consolidation uses max for both the
timer and anger amount. Later/current source also includes defy-resolution
anger in the same max aggregation.

This is therefore best classified as an AdvCiv BULL-integration aggregation
regression, retained by SAS, rather than a raw BtS/K-Mod runtime bug.

REPAIR DIRECTION
----------------
The minimum correction for the displayed CURRENT temporary-unhappiness amount
is to mirror CvCity::unhappyLevel's aggregation and convert only after summing:

    int iPercentAnger =
            kCity.getHurryPercentAnger() +
            kCity.getConscriptPercentAnger() +
            kCity.getDefyResolutionPercentAnger();

    int iAngryPop = (iPercentAnger * kCity.getPopulation()) /
            GC.getPERCENT_ANGER_DIVISOR();

Do not convert each source to citizens separately and add afterward; runtime
sums the percentages first, so one final conversion is required to preserve
its integer-rounding behavior.

Keeping `max(timer)` can still serve as an upper-bound "temporary anger fully
cleared by" duration, but it should not imply that the entire current summed
amount persists until that exact turn when sources have different timers. A
more precise UI would either label the amount as current temporary
unhappiness plus a maximum remaining duration, or report hurry/draft/defy
sources separately as BULL's original option structure suggests.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F407
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F388-F407 CONFIRMED in protected Queue 003.
F408 / provisional KI#731 UNUSED.
Latest physical checkpoint: C031-WIP108.
No repository source files modified.

NEXT
----
Continue from F408 unused through the rest of setCityBarHelp and the remaining
small AdvCiv-added helpers, then resume the whole-file sentinel/active-context,
class-resolution, localization/index and runtime-vs-display closure sweeps.
Keep the same live-caller/current-data threshold before numbering anything.

END C031-WIP108


===============================================================================
POST-WIP108 CONTINUATION — QUEUE 003 CvGameTextMgr.cpp
===============================================================================

Starting stone:
- Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp OPEN.
- 32/169 unique files complete.
- F388-F407 confirmed.
- F408 / provisional KI#731 UNUSED.
- C031-WIP108 latest physical checkpoint.
- No repository source files modified.

This continuation first finished the remainder of the setCityBarHelp tranche after
F407, then reconciled setRevoltHelp, the ordinary health/happiness breakdowns and
the food-rate hover against their exact CvCity runtime calculations.

POST-WIP108 CONTROLS / CLEAN RESULTS
------------------------------------

1. Remainder of setCityBarHelp after F407 is provisionally clean.

- Production/process conversion uses the same yield/modifier units as runtime.
- CvCity::calculateTradeTotals explicitly returns in disorder, so the city-bar
  trade detail does not resurrect trade-route yield during disorder.
- CvCity::getGreatPeopleRate() explicitly returns 0 in disorder, matching the
  city-bar GP line.
- On city acquisition, non-Wonder foreign UBs are converted to the new owner's
  civilization-equivalent BuildingClass entry before the city-bar building list
  sees them; iterating the current civilization's buildings therefore does not
  silently lose retained captured UBs.
- Free buildings are intentionally omitted, and the controlling BUG option help
  explicitly documents that free buildings (e.g. Stonehenge Monuments) are never
  listed. This is not an accidental omission.

2. setRevoltHelp state ordering matches runtime.

Runtime CvCity::doRevolt first rolls probabilityOccupationDecrement(); on success
it decrements occupation and returns. Only if that fails does it roll
revoltProbability(). setRevoltHelp displays the effective revolt probability as:

    revoltProbability() * (1 - probabilityOccupationDecrement())

which matches that ordered runtime transition. CvDLLWidgetData's nationality help
uses the same effective probability. No F408 here.

3. Ordinary happiness breakdown remains internally reconciled after F407.

setHappyHelp accumulates the same positive source getters used by
CvCity::happyLevel(), and retains its final assertion against happyLevel(). The
F407 temporary-anger bug is in setCityBarHelp's separate consolidated anger
summary, not in setHappyHelp.

4. Ordinary health breakdown has a semantic split mismatch, but current SAS data
masks it.

CvCity::goodHealth()/badHealth() combine:

    GET_PLAYER(getOwner()).getExtraHealth() + getExtraHealth()

before clamping that sum to its positive or negative part. setGoodHealthHelp and
setBadHealthHelp instead print player-wide ExtraHealth and city-local ExtraHealth
as independent contributions. Opposite-signed sources could therefore make the
listed source lines disagree with runtime's combined clamp.

Current SAS does not provide the needed opposing signs in normal data paths:
player-wide ExtraHealth sources found are nonnegative (e.g. Protective and the
custom civic), while current event-driven CvCity ExtraHealth values are likewise
positive. No current-data F408 is promoted; retain as a mod-mod/defensive display
hazard.

5. Additional-building projection defects remain the already-numbered F397-F399
family; the ordinary health/happy breakdown pass did not establish a distinct new
root.


F408 / provisional KI#731 — CONFIRMED
--------------------------------------
TITLE:
FOOD-RATE HOVER IGNORES CITY DISORDER AND CAN DISPLAY GROWTH/STARVATION THAT
RUNTIME EXPLICITLY FREEZES AT ZERO

CLASSIFICATION:
Inherited BULL food-hover integration defect retained by AdvCiv and current SAS.
The runtime city-food rule itself is correct; the defect is in the displayed
breakdown.

CURRENT SAS DISPLAY PATH:
CvDLLWidgetData::parseFoodModHelp obtains the currently selected city and calls:

    GAMETEXT.setFoodHelp(szBuffer, *pCity);

CvMainInterface.py attaches WIDGET_FOOD_MOD_HELP to PopulationInputText even in
the disorder branch. In that branch the visible city-screen number itself uses
pHeadSelectedCity.getFoodDifference(), so it correctly displays 0 during
occupation/anarchy disorder, but hovering that same label still enters
setFoodHelp.

CURRENT SAS TOOLTIP CALCULATION:
CvGameTextMgr::setFoodHelp has no isDisorder() guard. It reconstructs production
from currently worked plots (the city center is always worked), trade,
specialists, corporations and buildings; subtracts ordinary population food
consumption plus spoiled food; then reports growing/shrinking/stagnant based on
that reconstructed net result.

RUNTIME CONTRACT:
CvCity::foodDifference begins with:

    if (isDisorder())
        return 0;

Thus stored city food does not grow or shrink while the city is in disorder.
CvCity::getYieldRate itself does not contain that disorder suppression; the
suppression belongs specifically to foodDifference(). Therefore rebuilding the
normal yield-minus-consumption arithmetic in the tooltip bypasses the rule.

LIVE CURRENT-SAS EXAMPLE:
Take an occupied captured city at population 3. WorkingPlotIter includes the city
center because the home plot is always worked. On an ordinary 2-food city-center
state, even if no other workable plots contribute and health is neutral,
setFoodHelp can reconstruct approximately:

    2 food produced
  - 6 food eaten (3 population * 2)
  = -4 food / shrinking

while CvCity::foodDifference() returns exactly 0 for the same city and stored food
is frozen. The city screen's primary food label therefore correctly shows 0, but
its hover can claim a negative food rate / shrinking state that cannot occur.
Other occupied-city yields merely change the wrong nonzero number; they do not
remove the root mismatch.

REACHABILITY:
This is not hidden behind an optional Alt-only projection. CvMainInterface.py
still attaches WIDGET_FOOD_MOD_HELP when the city is in disorder; its explicit
`else` branch for disorder/food-production sets PopulationInputText and gives it
that widget. Captured occupied cities are ordinary gameplay states.

ANCESTRY:
Reachable-history diff:

    LLM_Helpers/context/commit_diffs/AdvCivPreSAS_1481_4a65bf5c0f.diff

Commit:
    4a65bf5c0fb187fd25d4908346abb462354a9172
Title:
    "Option for expanding scoreboard; BULL hover text"

That AdvCiv integration imports the BULL Food Rate Hover path, including
parseFoodModHelp and setFoodHelp. Base AdvCiv 1.14 and current SAS retain the
no-disorder tooltip arithmetic. This is therefore best classified as an inherited
BULL/AdvCiv UI integration defect, not a SAS runtime-rule regression.

CONSERVATIVE REPAIR:
Mirror the runtime state contract at the top of setFoodHelp. For example, if the
city is in disorder, either return after showing an explicit "0 / no food change
in disorder" explanation, or build a dedicated disorder line. Do not run the
ordinary yield-minus-consumption breakdown and then label it as net growth/shrink.
At minimum:

    if (kCity.isDisorder())
    {
        // append a suitable zero/disorder explanation if desired
        return;
    }

A dedicated explanation is preferable to a blank tooltip because the widget is
visible and the main label is already showing the frozen 0-food state.

WHY THIS IS DISTINCT FROM F407:
F407 is a max-vs-sum aggregation error in temporary unhappiness. F408 is a
state-rule omission in the food hover: it applies normal food arithmetic in a
state where runtime bypasses that arithmetic entirely.


CURRENT STONE AFTER F408
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F408 CONFIRMED.
F409 / provisional KI#732 UNUSED.
Latest physical checkpoint: C031-WIP109.
No repository source files modified.

Next continuation: continue the ordinary city-resource/help reconciliation from
production and commerce into the remaining active-context/sentinel/default-class
closure patterns; specifically challenge disorder/process/overflow special states
in setProductionHelp and its neighboring helpers before widening again.


===============================================================================
POST-WIP109 CONTINUATION — QUEUE 003 CvGameTextMgr.cpp
===============================================================================

Starting stone:
- Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp OPEN.
- 32/169 unique files complete.
- F388-F408 confirmed.
- F409 / provisional KI#732 UNUSED.
- C031-WIP109 latest physical checkpoint.
- No repository source files modified.

This continuation resumed exactly at the unfinished disorder-production question in
setProductionHelp and reconciled the Alt/"Actual Building Effects" bypass against
the runtime CvCity::getProductionDifference contract, the city-screen widget and
the K-Mod -> AdvCiv ancestry.


F409 / provisional KI#732 — CONFIRMED
--------------------------------------
TITLE:
ALT / "BUILDING ADDITIONAL PRODUCTION" BYPASSES THE DISORDER ZERO-PRODUCTION
GUARD AND RECONSTRUCTS NONZERO PRODUCTION / OVERFLOW THAT RUNTIME CANNOT EARN

CLASSIFICATION:
Inherited K-Mod BUG "Actual Building Effects" integration defect retained by
AdvCiv and current SAS; AdvCiv additionally made the broken path directly
reachable through the Alt key even with the option disabled.

RUNTIME CONTRACT:
CvCity::getProductionDifference begins with:

    if (isDisorder() && !bForceFeatureProd)
        return 0;

and CvCity::isDisorder() is true for either occupation or owner anarchy:

    return (isOccupation() || GET_PLAYER(getOwner()).isAnarchy());

Thus ordinary current production is exactly zero throughout disorder. The city
screen follows that contract: CvMainInterface.py gets
getCurrentProductionDifference(True, True) and displays that value in
ProductionInputText, which is still attached to WIDGET_PRODUCTION_MOD_HELP.

BROKEN TOOLTIP PATH:
CvGameTextMgr::setProductionHelp normally uses the same zero result to return:

    bool bBuildingAdditionalYield =
        (BUGOption::isEnabled("MiscHover__BuildingAdditionalProduction", false) ||
         GC.altKey());

    if (kCity.getCurrentProductionDifference(false, true) == 0 &&
        !bBuildingAdditionalYield)
        return;

Holding Alt, or enabling the BUG option, therefore bypasses the return even when
the only reason production is zero is disorder.

After bypassing it, setProductionHelp does not switch to a dedicated hypothetical
"after disorder" mode. It runs the ordinary live-production reconstruction:

- setYieldHelp prints the city's normal base production and yield modifiers;
- iBaseProduction is rebuilt from getBaseYieldRate(YIELD_PRODUCTION);
- unit/building/project production modifiers are added;
- iModProduction is computed from those normal values;
- the tooltip appends TXT_KEY_MISC_HELP_PROD_FINAL_YIELD with iModProduction;
- if an order is queued, it can also compute/display iNewOverflow from that
  reconstructed production.

But runtime remains at exactly zero for the turn.

INTERNAL SELF-CHECK CONFIRMS THE MISMATCH:
The helper contains:

    FAssertMsg(iModProduction ==
        kCity.getCurrentProductionDifference(false, !bProcess) + ...,
        "Modified Production does not match actual value");

In an anarchy/occupation city with ordinary positive base production and a queued
order, the left side is positive while getCurrentProductionDifference is 0.
Therefore the Alt/option path can trip the helper's own assertion in a debug DLL;
in a release DLL it proceeds to show the wrong nonzero final production instead.

LIVE CURRENT-SAS EXAMPLE:
Use an ordinary city with a queued unit/building and 5 base hammers, then enter
civic or religious anarchy. The queue remains present but isDisorder() makes the
actual production difference 0, which the main city-screen production input
correctly displays.

Hover that same ProductionInputText while holding Alt:

    runtime/current-production display: 0 hammers
    setProductionHelp reconstruction:   5 hammers (before any order modifiers)

and the hover can label that reconstructed value as final production and even
project overflow that cannot be generated during the disorder turn. Any normal
positive production city supplies the mismatch; 5 hammers is only a simple
illustration.

REACHABILITY:
This is live even though the BUG menu option defaults to False in current SAS.
AdvCiv's advc.063 integration explicitly ORs the option with GC.altKey(), and Alt
is the documented shortcut for showing all "Actual Effects" information. The
production input label remains attached to WIDGET_PRODUCTION_MOD_HELP in disorder.

ANCESTRY:
K-Mod commit:
    8478443165fcaeca861fc9d2dbb725de7e94ca59
Title:
    "Implemented \"actual building effect\" mouse-over hover options from BUG."

Its diff changes the original unconditional zero-production return into:

    if (city.getCurrentProductionDifference(false, true) == 0 &&
        !bBuildingAdditionalYield)
        return;

so the defective bypass originates with the BUG/K-Mod integration.

AdvCiv commit:
    aa7146bdeef00ac4ce12198f795dc3ac0cb46770
Title:
    "Improved the integration of some UI conveniences; smarter Sentry mission"

advc.063 changes bBuildingAdditionalYield to include:

    || GC.altKey()

and its log explicitly says Alt now shows all Actual Effects info. Thus AdvCiv
makes the already-broken zero-production bypass directly reachable on demand,
even with the menu option disabled. Base AdvCiv 1.14 and current SAS retain it.

CONSERVATIVE REPAIR:
Disorder must win over the Actual Effects toggle for the live-production portion.
For example, return (or show an explicit zero/disorder line) before the optional
building projections:

    if (kCity.isDisorder())
    {
        // optionally show "0 production during disorder"
        // optional building-effect projections may be shown separately,
        // but must not be labelled as current/final production.
        return;
    }

If keeping Alt building projections during disorder is desirable, separate them
from the current-production reconstruction rather than using the option to bypass
the runtime-zero guard.

WHY THIS IS DISTINCT FROM F408:
F408 is an unconditional state omission in setFoodHelp: ordinary food arithmetic
is used throughout disorder. F409 is a separate control-flow regression in
setProductionHelp: the helper does have a zero-production guard, but the BUG
Actual Effects option / Alt shortcut deliberately bypasses it and then continues
through live-production and overflow arithmetic. They have different introducing
changes and different repairs.


CURRENT STONE AFTER F409
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F409 CONFIRMED.
F410 / provisional KI#733 UNUSED.
Latest physical checkpoint: C031-WIP110.
No repository source files modified.

Next continuation: stay in the adjacent setProductionHelp/setCommerceHelp state
reconciliation long enough to challenge process conversion, overflow and
zero-commerce Alt paths; then resume the whole-file active-context/sentinel/
default-class/runtime-vs-display closure sweeps. Keep the same live-caller and
current-data threshold before numbering F410.

END C031-WIP110


===============================================================================
C031-WIP111 CONTINUATION — QUEUE 003 CvGameTextMgr.cpp
F410 / provisional KI#733 CONFIRMED
===============================================================================

POST-WIP110 RECONCILIATION CONTROLS
-----------------------------------
The current-SAS-vs-Base-AdvCiv delta pass continued before promoting F410.
Several high-risk-looking current deltas stayed clean:

* setTradeRouteHelp still reconstructs the same base profit, partner modifiers,
  total trade modifier and final trade profit as runtime, with exact assertions
  against CvCity::totalTradeModifier and calculateTradeProfit. Current SAS's
  changes there are static-define/cache/display-name cleanup, not a new root.

* setEspionageCostHelp still carries exact assertions against
  CvPlayer::getEspionageMissionCostModifier and getEspionageMissionCost. Current
  SAS's spy-selection path also already uses the mission-capable selection-group
  spy (the earlier KI#398 repair), so no new stale-head-unit mismatch survived.

* setVassalRevoltHelp's displayed population/land/loss ratios line up with the
  corresponding CvTeam revolt predicates/defines.

* setScoreHelp's component reconstruction is protected by its equality check
  against calculateScore in the ordinary current configuration; no current
  Python-score callback mismatch was established.

* The SAS air-bomb/air-strike/paradrop deltas inspected here are repairs rather
  than regressions: they null-check the selection list and select a unit that can
  actually execute the mission at the target instead of trusting the arbitrary
  selection head / highest-evasion unit.

* The SAS parseGreatPeopleHelp delta that separates obsolete-building GPP is
  consistent with runtime: obsolete wonders can keep their Great-Person-point
  rate, so exposing that contribution fixes rather than creates a decomposition
  mismatch.

KNOWN-TECH ROUTE-MOVEMENT DOUBLE-APPLICATION CANDIDATE — NOT PROMOTED:
`buildMoveString` uses the active team's current route change as a baseline when
bPlayerContext is true, then adds the hovered technology's movement change to
show its marginal effect. If called with bPlayerContext=true for an already-known
Engineering, the current route change would already contain Engineering and the
helper would indeed apply it a second time.

The live widget contract prevents that state:

* CvDLLWidgetData::parseTechTreeHelp passes
      bPlayerContext = !GET_TEAM(getActiveTeam()).isHasTech(eTech)
  so known technologies deliberately switch to context-free help.
* WIDGET_RESEARCH supplies the current/research-choice technology, i.e. an
  unacquired technology; the current-research widget cannot represent a tech the
  team already owns.

Current XML does make the arithmetic relevant (Road has Engineering
TechMovementChange = -10), but the bad context is not supplied by the current UI.
This remains a latent helper contract, not F410.

LARGE BUILDING-HELP SAS DELTA CONTROL:
The custom clean/dirty/area-clean power wording was compared with
CvCity::updatePowerHealth / isDirtyPower. Its arithmetic matches runtime:
POWER_HEALTH_CHANGE applies whenever a city has power; DIRTY_POWER_HEALTH_CHANGE
is added only while `isDirtyPower()` is true; area clean power makes
isDirtyPower() false. The old K-Mod <= -100% population-unhealth wording caveat
also stays current-data-masked because SAS has no positive civic population-
unhealth modifier that can offset the -100% building case.


F410 / provisional KI#733 — CONFIRMED
-------------------------------------
TITLE:
Alt / "Building Additional Great People" bypasses disorder's zero-GPP contract,
so the Great Person hover can display a nonzero final rate (and trip its own
assertion) while occupation/anarchy actually generates 0 GPP.

CLASSIFICATION:
Inherited BUG/K-Mod UI-calculation defect; AdvCiv makes it directly reachable
through the Alt "Actual Effects" shortcut. Current SAS retains it.

RUNTIME CONTRACT:
CvCity::getGreatPeopleRate() explicitly returns zero in disorder:

    int CvCity::getGreatPeopleRate() const
    {
        if (isDisorder())
            return 0;
        return (getBaseGreatPeopleRate() * getTotalGreatPeopleRateModifier()) / 100;
    }

CvCity::doGreatPeople() independently enforces the same state transition:

    if (isDisorder())
        return;

    changeGreatPeopleProgress(getGreatPeopleRate());

So occupation or owner anarchy produces exactly 0 Great Person points for that
turn. This is not merely a display convention.

BROKEN TOOLTIP CONTROL FLOW:
CvGameTextMgr::parseGreatPeopleHelp first obtains the correct live value:

    int const iGPRate = kCity.getGreatPeopleRate();

but the BUG "Building Additional Great People" integration changes the normal
zero-rate return into:

    bool bBuildingAdditionalGreatPeople =
        (BUGOption::isEnabled("MiscHover__BuildingAdditionalGreatPeople", false) ||
         GC.altKey());

    if (iGPRate == 0 && !bBuildingAdditionalGreatPeople)
        return;

Thus Alt (or the option) bypasses the return even when iGPRate is zero solely
because the city is in disorder.

After bypassing it, the helper does not keep the live zero rate. It reconstructs
normal, non-disorder GPP from:

* specialists and free specialists;
* building GPP (including the SAS explicit obsolete-building line);
* base Great People rate;
* building/global GPP modifiers;
* civic/state-religion modifiers;
* trait modifiers;
* Golden Age modifier.

It then computes:

    int iModGreatPeople =
        (iModifier * kCity.getBaseGreatPeopleRate()) / 100;

and appends TXT_KEY_MISC_HELP_GREATPEOPLE_FINAL with that value.

INTERNAL SELF-CHECK PROVES THE STATE MISMATCH:
Immediately before displaying the final value, the helper has:

    FAssertMsg(iModGreatPeople == kCity.getGreatPeopleRate(),
        "Great person rate does not match actual value");

During disorder, getGreatPeopleRate() is 0. If the city has any normal positive
base GPP, Alt causes iModGreatPeople > 0, so a debug DLL can fire this assertion.
A release DLL proceeds and displays the wrong nonzero final rate.

LIVE CURRENT-SAS REACHABILITY:
The city-screen GreatPeopleBar is created with WIDGET_HELP_GREAT_PEOPLE and
CvDLLWidgetData::parseGreatPeopleHelp forwards that hover to this function.

CvMainInterface.py keeps the Great People bar visible when either:

    pHeadSelectedCity.getGreatPeopleProgress() > 0
    or pHeadSelectedCity.getGreatPeopleRate() > 0

Therefore disorder does NOT necessarily hide the widget: an established GP city
with any stored progress remains hoverable even though its current rate has
fallen to 0.

A simple supported state:

1. Have an ordinary city with stored Great Person progress and 3 base GPP/turn
   (e.g. from a specialist/building).
2. Enter civic or religious anarchy, or put the city in occupation.
3. Runtime `getGreatPeopleRate()` becomes 0 and doGreatPeople adds nothing.
4. The stored progress keeps GreatPeopleBar visible.
5. Hover normally: the zero-rate guard stops the detailed rate breakdown.
6. Hold Alt: the guard is bypassed; the helper reconstructs and labels the
   ordinary positive rate (e.g. 3 GPP/turn, before any modifiers) as Final.

So the same current city state is simultaneously:

    runtime/current GPP rate: 0
    Alt detailed-hover final: >0

and the debug assertion can fail for exactly that reason.

ANCESTRY:
K-Mod commit:
    8478443165fcaeca861fc9d2dbb725de7e94ca59
Title:
    "Implemented \"actual building effect\" mouse-over hover options from BUG."

That integration introduced BuildingAdditionalGreatPeople and replaced the old
zero-GPP return with the option-gated return, creating the state bypass.

AdvCiv commit:
    aa7146bdeef00ac4ce12198f795dc3ac0cb46770
Title:
    "Improved the integration of some UI conveniences; smarter Sentry mission"

advc.063 changes the option test to:

    BUG option || GC.altKey()

making the bad path directly reachable through the documented Alt "Actual
Effects" shortcut even though the menu option defaults to false. Base AdvCiv
1.14 and current SAS retain it.

CONSERVATIVE REPAIR:
Disorder must remain authoritative for the live/final GPP-rate portion. E.g.:

    if (kCity.isDisorder())
    {
        // Optionally allow a separately-labelled hypothetical building-effect
        // section, but do not reconstruct or label a nonzero current/final rate.
        return;
    }

or preserve the building projections in a separate path while leaving
`iGPRate == 0` as an unconditional stop for the ordinary live-rate breakdown.

WHY F410 IS DISTINCT FROM F408/F409:
F408 is setFoodHelp's unconditional failure to mirror disorder.
F409 is setProductionHelp's Building Additional Production/Alt bypass of its
otherwise-correct zero-production guard.
F410 is a separate BUG feature/helper (`parseGreatPeopleHelp` and
BuildingAdditionalGreatPeople), separate runtime quantity and separate display
widget. It happens to repeat the same integration pattern as F409, which is
useful evidence that the BUG "Actual Effects" zero-rate bypass family should be
checked systematically.


CURRENT STONE AFTER F410
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F410 CONFIRMED.
F411 / provisional KI#734 UNUSED.
Latest physical checkpoint: C031-WIP111.
No repository source files modified.

Next continuation: use F409/F410 as a targeted pattern sweep over the remaining
BUG "Actual Effects" helpers (specialist, commerce, health/happiness) for zero-
runtime-state guards bypassed by option/Alt; then return to the current-SAS delta
inventory and whole-file active-context/sentinel/runtime-vs-display closure.

END C031-WIP111


POST-WIP111 CLOSURE CONTROLS — PHYSICALLY PRESERVED IN WIP112
--------------------------------------------------------------
F411 / provisional KI#734 remains UNUSED at this checkpoint.

1. setSpecialistLink singular/plural polarity — latent, not promoted.
A building specialist-slot caller passes a local `bSingular` value directly to
setSpecialistLink's third parameter, whose contract/name is `bPlural`. This is
source-level opposite polarity. However, the flag changes only the grammatical
form embedded in the generated specialist link for Italian/Spanish; the visible
specialist description is unchanged. The EXE-side grammatical link resolver is
not locally available, so a broken/misrouted live hyperlink cannot be proved.

2. setEventHelp global negative-health localization branch — genuine dormant
wrong-key/argument contract, not promoted under current SAS data.
The non-city negative-health branch calls the city-specific
TXT_KEY_EVENT_UNHEALTH_CITY while supplying only the health amount and no city
name. The corresponding positive branch uses the global TXT_KEY_EVENT_HEALTH,
and a global negative key exists. Current EventInfos contain no negative iHealth
values, so the malformed branch is unreachable with current SAS XML.

3. SAS cached default-specialist initializer — real source typo, data-masked.
The self-referential initializer resolves through static zero initialization to
enum 0; current DEFAULT_SPECIALIST is also SPECIALIST_CITIZEN / enum 0, so the
current result is accidentally correct. No F411.

4. City-bar commerce during disorder — candidate killed by runtime cache update.
Occupation/anarchy transitions trigger updateCommerce(); CvCity::updateCommerce
sets the cached commerce rates to zero while isDisorder() is true. Although the
city-bar loop itself has no separate guard on every commerce line, it therefore
reads zeros rather than stale pre-disorder rates.

5. War-weariness visibility self-query — control.
The active-player canSeeDemographics(self) concern closes because canSeeIntel's
same-team case guarantees visibility to self.

6. Billboard/disorder controls.
Food/production/GP/top-city billboard calculations inspected after WIP111 use
runtime-updated values or collapse their estimates when the corresponding rate
is zero; no independent F411 established.

7. Global-option integration sweep — IN PROGRESS at the WIP112 freeze point.
F394-F396 showed that player-facing "enables/allows" text can drift from global
option runtime state. The next exact continuation is the organization /
corporation first-discovery/founding help versus CvTeam::setHasTech and the No
Corporations option, followed by the remaining option-gated unlock helpers.

CURRENT STONE AT WIP112 FREEZE
------------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp OPEN.
32/169 unique files complete.
F388-F410 CONFIRMED.
F411 / provisional KI#734 UNUSED.
No repository source files modified.
Latest physical checkpoint: C031-WIP112.

END C031-WIP112 FREEZE


POST-WIP112 OPTION-SWEEP CONTROL
--------------------------------
The unfinished corporation/organization first-discovery question closes without
F411. Current SAS has no GAMEOPTION_NO_CORPORATIONS; No Corporations is a civic
state. That civic does not prevent a first-discovery corporation from being
founded: CvTeam::setHasTech still calls CvPlayer::foundCorporation, while the
civic subsequently makes corporations inactive / blocks applicable construction
and spread. Thus "First to discover incorporates ..." remains literally true.
Moreover current corporation XML uses TechPrereq=NONE, so the technology-first-
discovery corporation branch is not a current-data producer anyway.


F411 / provisional KI#734 — CONFIRMED
-------------------------------------
TITLE:
City corporation hover ignores disorder: it can display normal corporation
output and maintenance while occupation/anarchy suppresses those live effects.

CLASSIFICATION:
Inherited BtS city-hover/runtime-state defect; retained unchanged in K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS.

BROKEN DISPLAY CONTRACT:
CvGameTextMgr::setCorporationHelpCity determines:

    bool const bActive = (pCity->isActiveCorporation(eCorporation) ||
        (bForceCorporation && iNumResources > 0));

and, whenever bActive is true, reconstructs corporation yield/commerce from the
resource count and CorporationInfo values. It also reconstructs corporation
maintenance and inflation. There is no `pCity->isDisorder()` /
`pCity->isNoMaintenance()` check in this display path.

RUNTIME CONTRACT — CORPORATION OUTPUT:
CvCity::getCorporationYieldByCorporation and
CvCity::getCorporationCommerceByCorporation both require:

    if (isActiveCorporation(eCorporation) && !isDisorder())

before adding any output. In disorder they return 0.

`CvCity::isActiveCorporation` itself deliberately does NOT include disorder; it
checks possession, owner corporation policy and prerequisite resources. Thus the
TextMgr's bActive can remain true while the runtime-output functions explicitly
return zero.

RUNTIME CONTRACT — MAINTENANCE:
CvCity::isNoMaintenance() is:

    return (isDisorder() || isWeLoveTheKingDay() || getPopulation() <= 0);

and updateMaintenance sets current city maintenance to zero whenever
isNoMaintenance() is true. The corporation hover nevertheless computes its own
corporation-maintenance number when bActive is true, without that state guard.

LIVE CURRENT-SAS UI REACHABILITY:
CvMainInterface.py keeps corporation icons for corporations that the selected
city has, independent of disorder, and attaches
WIDGET_HELP_CORPORATION_CITY. CvDLLWidgetData::parseCorporationHelpCity passes the
selected city to setCorporationHelpCity(..., bCityScreen=true).

Therefore both player anarchy and captured-city occupation leave a normal live
hover path to the broken calculation.

CONCRETE CURRENT-DATA EXAMPLE:
Cereal Mills (CORPORATION_1) produces 100 food-hundredths per qualifying resource
and Standard world size has CorporationMaintenancePercent=100. A city with
Cereal Mills and one Wheat therefore ordinarily receives +1 food from that
corporation.

If the owner enters anarchy (or the city is occupied):

    runtime getCorporationYieldByCorporation(FOOD, Cereal Mills) = 0

but the corporation icon hover still reconstructs and prints +1 food because
`bActive` remains true. Any displayed corporation-maintenance amount is likewise
not the currently charged city maintenance, which is zero in disorder.

ANCESTRY:
The same missing disorder/no-maintenance guard is present in Civ4CE/BtS's
setCorporationHelpCity, K-Mod, Base AdvCiv 1.14 and current SAS. K-Mod adds
inflation to the displayed maintenance but does not repair the state mismatch.

CONSERVATIVE REPAIR:
For live city help, derive displayed corporation yield/commerce through the
runtime getters (getCorporationYieldByCorporation /
getCorporationCommerceByCorporation) or at minimum require `!pCity->isDisorder()`
for those values. Do not show a currently-paid corporation maintenance value when
`pCity->isNoMaintenance()` is true. Keep bForceCorporation hypothetical-preview
semantics separate if those callers intentionally want counterfactual output.

WHY DISTINCT FROM F408-F410:
F408 is the food-rate hover's missing disorder guard; F409 is production Actual
Effects/Alt bypass; F410 is Great-People Actual Effects/Alt bypass. F411 is a
separate corporation-icon helper with its own corporation output and maintenance
runtime contracts, and it is wrong without any modifier key or BUG option.


CURRENT STONE AFTER F411
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F411 CONFIRMED.
F412 / provisional KI#735 UNUSED.
Latest physical checkpoint: C031-WIP113.
No repository source files modified.

Next continuation: check the neighboring religion-city hover for the same
live-state family; then finish global-option integration and proceed through the
remaining sentinel/localization/source-vs-target/runtime-decomposition closure
sweeps.

END C031-WIP113


POST-WIP113 RELIGION/CORPORATION CONTROL
----------------------------------------
The neighboring religion-city hover does not reproduce F411 merely because it
can show religion commerce during disorder. CvCity::getReligionCommerceByReligion
is itself an underlying religion-contribution getter and does not have a disorder
state gate; the old Python religion-icon code also used that getter directly.
By contrast, the corporation runtime getters explicitly require !isDisorder(),
and the old commented Python corporation display used those disorder-sensitive
getCorporationYieldByCorporation / getCorporationCommerceByCorporation getters.
This strengthens F411 as a corporation-specific live-value regression rather
than supporting a generic icon-hover-in-disorder finding.


F412 / provisional KI#735 — CONFIRMED
-------------------------------------
TITLE:
Finance Advisor unit-cost / unit-supply hovers ignore player anarchy and can
itemize positive expenses while the visible/runtime expense rows are exactly 0.

CLASSIFICATION:
Inherited BUG/K-Mod Finance Advisor state mismatch; present in K-Mod and retained
by Base AdvCiv 1.14/current AdvCiv-SAS. Stock BtS/Civ4CE does not contain these
finance-breakdown TextMgr helpers.

RUNTIME OVERLOAD CONTRACT:
CvPlayer exposes ordinary unit-cost / supply functions and detailed breakdown
overloads.

The ordinary unit-cost path explicitly does:

    int CvPlayer::calculateUnitCost(int iExtraPop, int iExtraUnits) const
    {
        if (isAnarchy())
            return 0;
        ... detailed overload ...
    }

and ordinary unit supply similarly does:

    int CvPlayer::calculateUnitSupply(int iExtraOutsideUnits) const
    {
        if (isAnarchy())
            return 0;
        ... detailed overload ...
    }

The detailed overloads
`calculateUnitCost(iFreeUnits, ..., iExtraCost, ...)` and
`calculateUnitSupply(iPaidUnits, iBaseSupplyCost, ...)` do not perform the
anarchy test; they calculate the normal pre-anarchy support burden.

BROKEN TEXTMGR CONTRACT:
CvGameTextMgr::buildFinanceUnitCostString calls the detailed unit-cost overload
directly and then inflates / prints its components and total.
CvGameTextMgr::buildFinanceAwaySupplyString likewise calls the detailed supply
overload directly. Neither helper checks `kPlayer.isAnarchy()`.

LIVE CURRENT-SAS UI PROOF:
The Economics/Finance tab in CvDomesticAdvisor.py computes its displayed rows
through the ordinary Python-exposed runtime calls:

    totalUnitCost = player.calculateUnitCost() * inflation...
    totalUnitSupply = player.calculateUnitSupply() * inflation...

Thus both visible values are exactly 0 during anarchy.

The Unit Cost and Unit Supply labels are nevertheless always created and always
carry WIDGET_HELP_FINANCE_UNIT_COST / WIDGET_HELP_FINANCE_AWAY_SUPPLY with
m_iData2=1. CvDLLWidgetData consequently invokes buildFinanceUnitCostString and
buildFinanceAwaySupplyString for those zero-valued rows.

SUPPORTED STATE:
Take any ordinary empire with enough units to owe positive regular unit cost and
at least one paid outside unit for supply. Before a civic/religion switch its
Finance rows and hovers show positive costs. During the resulting player anarchy:

    visible Unit Cost row = 0
    visible Unit Supply row = 0
    runtime calculateUnitCost() = 0
    runtime calculateUnitSupply() = 0

but the hover helpers bypass those anarchy-aware wrappers and reconstruct the
normal positive support costs. A zero row can therefore have a tooltip whose own
itemized total says that the player is paying positive gold per turn.

ANCESTRY:
The detailed finance helpers and the same direct detailed-overload calls are
present in K-Mod and Base AdvCiv. They are absent from stock BtS/Civ4CE's
CvGameTextMgr, making this part of the inherited BUG/K-Mod Finance Advisor
integration rather than a stock-BtS root. Current SAS retains the behavior.

REPAIR DIRECTION:
Mirror the runtime state before constructing the breakdown. At minimum, when
`kPlayer.isAnarchy()` the unit-cost and away-supply helpers should report zero /
anarchy-suppressed cost rather than invoke the detailed overload as though the
state were normal. An alternative is to make the detailed breakdown API accept
or expose the same runtime suppression explicitly so the row and tooltip share
one source of truth.

NEIGHBORING EXTRA-COST CONTROL:
The unit-cost helper inflates iCost, iUnitCost and iMilitaryCost but does not
inflate `iExtraCost` before printing that component. That is structurally
suspicious, but current SAS UnitInfo XML has no nonzero iExtraCost values, so it
is data-dormant and is not promoted as F413 here.


CURRENT STONE AFTER F412
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F412 CONFIRMED.
F413 / provisional KI#736 UNUSED.
Latest physical checkpoint: C031-WIP114.
No repository source files modified.

Next continuation: continue the Finance Advisor breakdown family briefly for
state/rounding controls, then return to the independent whole-file closure
passes: global-option integration, NO_* / info-access sentinels, localization
key/type contracts, source-vs-target context, and runtime-vs-displayed state.

END C031-WIP114


POST-WIP114 CLOSURE CONTROLS
----------------------------
The remaining project-count display was reconciled against CvPlayer::canCreate.
For world/team projects, both runtime legality and setProjectHelp reserve copies
that the team is already making. The current city's in-progress copy is removed
from the runtime "continue this order" legality test through bContinue=-1, but
`N left` in the hover is naturally the number of *additional* slots still free.
Thus a city can correctly be allowed to finish its current copy while the hover
says 0 left. No F413 is spent on that wording.

The random-event pass also retained earlier dormant controls: the global
negative-health branch uses the wrong city-specific localization key but current
EventInfo data has no negative iHealth; eventGoldHelp has a negative random-range
sign asymmetry but current EventInfo data does not produce that state; and its
use of getActivePlayer() when filtering additional outcomes is a perspective/API
hazard whose current normal callers all describe the active event owner.


F413 / provisional KI#736 — CONFIRMED
-------------------------------------
TITLE:
Random-event pillage ranges advertise an inclusive XML maximum that runtime can
never roll when MinPillage and MaxPillage differ.

CLASSIFICATION:
Inherited stock-BtS random-event off-by-one; present in Civ4CE/BtS, K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS. The defect is in the event execution
range calculation; CvGameTextMgr faithfully exposes the XML's stated min/max.

TEXT / DATA CONTRACT:
CvGameTextMgr::setEventHelp displays the two XML endpoints directly:

    TXT_KEY_EVENT_PILLAGE_RANGE_CITY(
        kEvent.getMinPillage(), kEvent.getMaxPillage(), city)

(or the non-city range key for empire-wide events).

The fields are named iMinPillage and iMaxPillage, and current SAS's Sevopedia
random-event page independently describes them as the "possible count range"
and formats unequal values as `min-max`. The stock BtS localization likewise
presents the two values as a range. There is no indication that iMaxPillage is
intended as an exclusive bound.

BROKEN RUNTIME CONTRACT:
Both CvCity::applyEvent (city / other-player-city events) and
CvPlayer::applyEvent (empire-wide events) choose the count with:

    iMinPillage + SyncRandNum(iMaxPillage - iMinPillage)

CvRandom::getInt(N) returns an integer in [0, N-1]. Therefore, when max > min,
the actual event range is:

    MinPillage ... MaxPillage-1

and the XML / tooltip maximum can never happen. When min==max, the current call
uses SyncRandNum(0), which happens to return 0 and therefore masks the same
missing-inclusive-endpoint issue.

LIVE CURRENT-SAS EXAMPLES:
Current CIV4EventInfos.xml contains ordinary reachable Looters outcomes:

    EVENT_LOOTERS_2: iMinPillage=2, iMaxPillage=4,
                     bPickOtherPlayerCity=1
    EVENT_LOOTERS_3: iMinPillage=3, iMaxPillage=5,
                     bPickOtherPlayerCity=1

CvEventInfo deliberately loads bPickOtherPlayerCity into
m_bOtherPlayerCityEffect, so these outcomes run through CvCity::applyEvent on
the selected other-player city and through the city-specific help text.

For Looters 2 the hover says 2-4 improvements may be destroyed, while runtime
computes `2 + Rand(2)` and can destroy only 2 or 3.
For Looters 3 the hover says 3-5, while runtime computes `3 + Rand(2)` and can
choose only 3 or 4.

This is therefore reachable with unmodified current SAS EventInfo data and does
not depend on a hypothetical mod-mod field.

ANCESTRY:
The same range display and the same `Rand(max-min)` execution formula are
present in stock BtS/Civ4CE. K-Mod and Base AdvCiv retain both, and current SAS
retains them as well. This is a stock-BtS inherited root rather than a SAS-local
regression.

REPAIR DIRECTION:
Make the execution range inclusive to match the XML field names and all player-
facing descriptions:

    MinPillage + SyncRandNum(MaxPillage - MinPillage + 1)

in both CvCity::applyEvent and CvPlayer::applyEvent. That also replaces the
min==max zero-outcome RNG call with the natural Rand(1) form.


CURRENT STONE AFTER F413
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F413 CONFIRMED.
F414 / provisional KI#737 UNUSED.
Latest physical checkpoint: C031-WIP115.
No repository source files modified.

Next continuation: continue the event/runtime field reconciliation for other
random-range effects, then resume the whole-file closure passes over source-vs-
target state, localization/key contracts, enum sentinels, option integration and
still-unvisited member definitions. Only promote F414 if current callers/data
support a distinct live consequence.

END C031-WIP115


POST-WIP115 EVENT-RUNTIME RECONCILIATION
----------------------------------------
The conversion-count branches were reconciled before spending F414. For
ConvertOwnCities / ConvertOtherCities, runtime first builds the eligible-city
set and then randomly erases entries until its size is at most the requested
count. Thus the event converts exactly min(requested, eligible) cities; there is
no F413-style exclusive-upper-bound error in those count fields.

The event experience branch exposed a separate live stock-BtS help/runtime
mismatch. This one is not a hypothetical unit-state edge case: current event
trigger data deliberately requires the affected unit to be damaged.


F414 / provisional KI#737 — CONFIRMED
-------------------------------------
TITLE:
Unit-experience random events fully heal their necessarily damaged target, but
the event-choice help advertises only the XP gain.

CLASSIFICATION:
Inherited stock-BtS random-event help omission; present in Civ4CE/BtS, K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS.

BROKEN TEXT / RUNTIME CONTRACT:
CvGameTextMgr::setEventHelp handles iUnitExperience with only:

    TXT_KEY_EVENT_UNIT_EXPERIENCE(
        kEvent.getUnitExperience(), szUnit)

whose stock English text is:

    "The [unit] receives N XP"

No healing effect is shown.

CvUnit::applyEvent, however, executes:

    if (kEvent.getUnitExperience() != 0)
    {
        setDamage(0);
        changeExperience(kEvent.getUnitExperience());
    }

Thus every XP event also restores the target to full health before granting the
advertised experience.

LIVE CURRENT-SAS DATA / REACHABILITY:
Current CIV4EventInfos.xml contains two nonzero iUnitExperience outcomes:

    EVENT_AT_THE_SWORD_1       iUnitExperience=3
    EVENT_FRIENDLY_LOCALS_1    iUnitExperience=1

Their corresponding trigger data both use:

    iUnitDamagedWeight=1

CvUnit::getTriggerValue has the explicit contract:

    if (getDamage() == 0 && kTrigger.getUnitDamagedWeight() > 0)
        return MIN_INT;

and then weights candidates by `getDamage() * iUnitDamagedWeight`.
Consequently, an undamaged unit is not merely unlikely for these triggers; it
is ineligible. Every reachable At the Sword / Friendly Locals XP event targets
a damaged unit, so the undisclosed `setDamage(0)` has a real effect on every
legal application.

SUPPORTED EXAMPLE:
A damaged Swordsman selected by At the Sword can be at any positive damage
amount permitted by the trigger. The event-choice help states only that the
unit receives +3 XP. Selecting the outcome also sets damage to 0 immediately,
fully healing the Swordsman. Friendly Locals behaves the same way for +1 XP.

ANCESTRY:
Stock BtS/Civ4CE CvUnit::applyEvent already performs `setDamage(0)` for any
nonzero UnitExperience event. Stock CvGameTextMgr::setEventHelp already prints
only TXT_KEY_EVENT_UNIT_EXPERIENCE. K-Mod, Base AdvCiv and current SAS retain
both sides unchanged in substance. This is therefore an inherited stock-BtS
help omission rather than a SAS-local regression.

REPAIR DIRECTION:
The conservative UI-side repair is to expose the healing that runtime already
performs, e.g. append a "fully heals [unit]" line when a UnitExperience event
has a damaged concrete target. A gameplay-side alternative would be to remove
the unconditional `setDamage(0)` if that hidden healing was never intended,
but that would deliberately alter longstanding event mechanics and therefore
requires a design decision rather than being the surgical compatibility fix.


CURRENT STONE AFTER F414
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F414 CONFIRMED.
F415 / provisional KI#738 UNUSED.
Latest physical checkpoint: C031-WIP116.
No repository source files modified.

Next continuation: finish the remaining event execution/help field comparison
(unit/building/plot/additional-event and global per-city clamps), then resume
whole-file closure sweeps over localization/sentinel, source-vs-target context,
global-option integration and still-unvisited member definitions. Only promote
F415 if current callers/data support a distinct live consequence.

END C031-WIP116


POST-WIP116 EVENT-RUNTIME RECONCILIATION
----------------------------------------
The empire-wide population branch was compared directly against its recursive
AdditionalEvent help path and current EventInfo/TriggerInfo data.


F415 / provisional KI#738 — CONFIRMED
-------------------------------------
TITLE:
Empire-wide negative-population event help says every city loses population,
but runtime silently spares cities that would fall to population 0.

CLASSIFICATION:
Inherited stock-BtS random-event help/runtime mismatch; present in Civ4CE/BtS,
K-Mod, Base AdvCiv 1.14 and current AdvCiv-SAS.

BROKEN TEXT CONTRACT:
For a non-city event with nonzero PopulationChange, setEventHelp uses:

    TXT_KEY_EVENT_POPULATION_CHANGE(kEvent.getPopulationChange())

whose stock English text is:

    "N population in all cities"

There is no minimum-population qualification.

BROKEN RUNTIME MATCH:
CvPlayer::applyEvent handles empire-wide PopulationChange by iterating every
city but applies the change only when:

    pLoopCity->getPopulation() + kEvent.getPopulationChange() > 0

Cities that would be reduced to population 0 or below are skipped entirely.
Thus the runtime effect is conditional per city while the help states that it
applies to all cities.

LIVE CURRENT-SAS DATA / REACHABILITY:
Current EVENT_HEALING_PLANT_4 is a non-city event with:

    iPopulationChange=-1

It is an immediate additional outcome of both EVENT_HEALING_PLANT_2 and
EVENT_HEALING_PLANT_3, with 20% / 50% AdditionalEventChance respectively.
CvGameTextMgr::setEventHelp recursively expands immediate additional events and
therefore shows Healing Plant 4's `-1 population in all cities` consequence in
the parent choice help.

EVENTTRIGGER_HEALING_PLANT has no minimum-city-population restriction and no
picked city. An ordinary empire can therefore have, for example, one population-
1 city and one larger city when the event fires. If Healing Plant 4 occurs:

    larger city: population decreases by 1
    population-1 city: unchanged

although the displayed nested effect said the loss applied in all cities.

ANCESTRY:
Stock BtS/Civ4CE already uses the unconditional "population in all cities" help
key and the per-city `population + change > 0` runtime guard. K-Mod, Base AdvCiv
and current SAS retain both. This is an inherited stock-BtS display/runtime
mismatch rather than a SAS-local regression.

REPAIR DIRECTION:
Preserve the no-city-death runtime rule and make the player-facing text match it,
e.g. explain that the population loss applies to every city where the reduction
would leave at least population 1. A dynamic concrete count could be even more
precise when a triggered player is available, but the minimum requirement is to
stop promising a loss in cities that runtime explicitly exempts.


CURRENT STONE AFTER F415
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F415 CONFIRMED.
F416 / provisional KI#739 UNUSED.
Latest physical checkpoint: C031-WIP117.
No repository source files modified.

Next continuation: finish the remaining event field / additional-event / plot
execution comparison, then return to the independent whole-file closure sweeps.
Only promote F416 if current callers/data support a distinct live consequence.

END C031-WIP117


POST-WIP117 CLOSURE / TRADE-HOVER RECONCILIATION
-------------------------------------------------
The remaining random-event execution fields were provisionally closed after
F414/F415: conversion help uses "up to" wording and matches the runtime clamp,
unit/building/plot changes and delayed/additional events matched their execution
contracts, and no further current-data event root crossed the finding threshold.

The global-option pass was also resumed. The corporation first-discovery wording
remains correct under no-corporation civics because those civics suppress active
corporation effects/spread rather than preventing CvTeam::setHasTech from
founding the corporation. Other option-dependent tech entries were kept separate
from generic prerequisite descriptions rather than overclaiming immediate
trainability/constructability.

A mechanical low-mention member pass reopened process/project/mission,
Advanced-Start/WorldBuilder and BUG trade-hover helpers. The most important new
root came from comparing the BUG trade explanation against AdvCiv's altered
runtime trade-route eligibility.


F416 / provisional KI#739 — CONFIRMED
-------------------------------------
TITLE:
Trade-route hover says there is no trade with a civilization in anarchy but
omits anarchy from its explanatory reasons.

CLASSIFICATION:
Inherited AdvCiv integration omission: AdvCiv advc.124 added the foreign-player
anarchy prohibition to trade-route eligibility after the BUG trade hover had
been integrated, but the hover's reason decomposition was not updated to cover
the new runtime blocker. Present in Base AdvCiv 1.14 and current AdvCiv-SAS;
the underlying anarchy blocker is absent from K-Mod/BtS.

RUNTIME CONTRACT:
CvPlayer::canHaveTradeRoutesWith(PlayerTypes ePlayer) now contains:

    if (getID() != ePlayer && kOtherPlayer.isAnarchy())
        return false;

before its normal same-team / free-trade / civic checks. Thus, from player A's
perspective, ordinary foreign trade with B is unavailable while B is in anarchy.

BROKEN HELP DECOMPOSITION:
CvGameTextMgr::buildTradeString first uses that exact runtime predicate to choose
its heading and to enter the "cannot trade" explanation branch:

    if (bForeign && !kPlayer.canHaveTradeRoutesWith(eWithPlayer))

It then diagnoses only:

    - defeated player
    - no trade-network connection
    - no free-trade / Open Borders relationship
    - our no-foreign-trade civic state
    - their no-foreign-trade civic state
    - none of their cities known to us

There is no test for `kWithPlayer.isAnarchy()` and no anarchy explanation key.
If anarchy is the sole blocker, `bCanTrade` remains true, no explanatory bullet
is appended, and the function continues to a zero-route summary after already
printing the "No Trade with <player>" heading.

LIVE UI REACHABILITY:
This is not a dormant helper. CvDLLWidgetData::parseTradeRoutes routes
WIDGET_TRADE_ROUTES directly to buildTradeString(activePlayer, otherPlayer).
Current CvForeignAdvisor.py attaches that widget to both trade-route count and
trade-yield text for every shown civilization pair. Current SAS CvInfoScreen.py
also attaches WIDGET_TRADE_ROUTES to its trade column using the active/other
player IDs.

The Foreign Advisor's own pre-display test is additionally asymmetric here:
for a row about B it asks `B.canHaveTradeRoutesWith(activePlayer)`. If B is in
anarchy and the active player is not, that direction does not trip AdvCiv's
"other player is in anarchy" rule, so the row/widget remains naturally present
while the C++ hover, evaluated from the active player's direction, correctly
enters the no-trade branch and then fails to explain why.

SUPPORTED CURRENT STATE:
Take two living civilizations A (active) and B that:

    - have met and know at least one of B's cities,
    - have a valid trade-network connection,
    - satisfy free-trade/Open-Borders requirements,
    - neither has a no-foreign-trade civic restriction,
    - B is temporarily in civic/religious anarchy.

A.canHaveTradeRoutesWith(B) is false solely because B is in anarchy. Hovering
B's Trade field can therefore show:

    No Trade with B
    0 commerce from 0 Trade Routes

without any bullet that identifies the actual blocker.

ANCESTRY:
K-Mod's canHaveTradeRoutesWith has no foreign-player-anarchy rejection. Base
AdvCiv adds that check under advc.124. Base AdvCiv's integrated buildTradeString
already contains an advc.124 diagnostic addition for unknown foreign cities,
but it has no corresponding anarchy diagnostic. Current SAS retains both the
runtime prohibition and the incomplete decomposition. This is therefore best
classified as an AdvCiv integration omission rather than a stock-BtS root.

REPAIR DIRECTION:
Mirror the runtime predicate inside the explanation block, before the remaining
reasons, e.g. append a dedicated localized bullet when
`kWithPlayer.isAnarchy()`. Keep the direction explicit: the runtime restriction
is specifically on the *other* player being in anarchy from kPlayer's
`canHaveTradeRoutesWith(eWithPlayer)` perspective.


CONTROLS CLOSED AROUND F416
---------------------------
- setProcessHelp matches current Wealth/Research/Culture process conversion
  modifiers; current SAS has no Espionage process to create a No-Espionage
  mismatch there.
- No-Nukes/No-Espionage external blockers were not promoted from generic tech
  "Can Train/Construct" prerequisite listings, which already intentionally list
  entries that can have additional resource/project/AND-tech blockers.
- Advanced Start / WorldBuilder CvWBData IDs inspected so far follow their
  consuming list/enum contracts; no supported index-vs-enum action error was
  established.
- parseWarTradesHelp still performs TEAMID on its player arguments before its
  NO_PLAYER tests, but every current WIDGET_LH_GLANCE caller supplies valid
  player IDs; retained as a defensive/API hazard, not a live finding.
- The earlier event negative-health wrong-key branch, specialist-link polarity,
  specialist sentinel-order access, SpeedBonus resource-loop issue, and old
  setGoldStr formatter remain latent/data-masked/dead controls rather than
  numbered current-SAS roots.


CURRENT STONE AFTER F416
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F416 CONFIRMED.
F417 / provisional KI#740 UNUSED.
Latest physical checkpoint: C031-WIP118.
No repository source files modified.

Next continuation: continue the still-low-mention helper/member reconciliation,
with special attention to trade/deal perspective, WorldBuilder/Advanced Start,
plot mission/action help, localization/sentinel contracts and duplicated runtime
arithmetic. Only promote F417 when a current caller/data path proves a distinct
live consequence.

END C031-WIP118


POST-WIP118 ADVANCED-START RECONCILIATION
-----------------------------------------
The Advanced Start exported data builders were compared against the actual
CvPlayer purchase-cost functions and doAdvancedStartAction acceptance rules.
This exposed a live zero-cost visibility mismatch created by AdvCiv's later
speed normalization while retaining BtS's older positive-only UI filter.


F417 / provisional KI#740 — CONFIRMED
-------------------------------------
TITLE:
Advanced Start hides the Visibility purchase control on slower game speeds when
the legal visibility cost rounds down to zero.

CLASSIFICATION:
AdvCiv regression/integration defect created by advc.250c cost normalization
interacting with an inherited BtS UI filter. Present in Base AdvCiv 1.14 and
current AdvCiv-SAS; stock BtS's unscaled/higher visibility cost normally masked
the old `> 0` assumption.

BROKEN UI CONTRACT:
CvGameTextMgr::getVisibilityDataForAS obtains:

    int iCost = GET_PLAYER(getActivePlayer()).
            getAdvancedStartVisibilityCost(true);

Its own comment states that cost -1 means the action may not be purchased, but
it only exposes the Visibility entry when:

    if (iCost > 0)

Thus a valid zero-cost action is treated exactly like an unavailable action and
removed from the Advanced Start palette.

RUNTIME CONTRACT:
CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_VISIBILITY) obtains the
plot-specific cost and rejects only:

    if (iCost < 0)
        return;

For an add action it then performs the reveal whenever:

    getAdvancedStartPoints() >= iCost

A cost of 0 is therefore explicitly legal: the plot is revealed and zero start
points are deducted.

WHY CURRENT DATA REACHES ZERO:
Current AdvCiv/SAS GlobalDefines_advc.xml sets:

    ADVANCED_START_VISIBILITY_COST = 1

CvPlayer::getAdvancedStartVisibilityCost eventually returns:

    adjustAdvStartPtsToSpeed(iCost)

and advc.250c's helper performs:

    iPoints *= 100;
    return max(0, iPoints / GC.getGame().getSpeedPercent());

CvGame::getSpeedPercent() is the game speed's ResearchPercent. Current SAS has:

    Normal       100% -> 1 * 100 / 100 = 1
    Epic         150% -> 1 * 100 / 150 = 0
    Marathon     200% -> 0
    Slow         300% -> 0
    Very Slow    400% -> 0

Immediately after an Advanced Start city supplies the ordinary initial revealed
city area, the base visibility cost can therefore be exactly 0 on these slower
speeds. The runtime permits the free reveal, but the exported UI list contains
no Visibility tool with which the human can request it.

REACHABILITY / UI ROLE:
getVisibilityDataForAS is a DllExport whose sole purpose is to populate the
Advanced Start Visibility data list (CvWBData entry using the
INTERFACE_TECH_LOS button). Advanced Start actions dirty/rebuild the Advanced
Start interface when purchases change state. Once the player has a city, the
cost function is no longer blocked by its `getNumCities() <= 0` guard; the
positive-only list filter is then the remaining reason a legal zero-cost entry
is omitted.

ANCESTRY:
Stock BtS/Civ4CE already uses `if (iCost > 0)` in getVisibilityDataForAS, but
stock getAdvancedStartVisibilityCost simply returns its positive define-derived
cost without AdvCiv's game-speed division. AdvCiv advc.250c introduces
`adjustAdvStartPtsToSpeed` for this cost and also recommends/redefines
ADVANCED_START_VISIBILITY_COST to 1. Base AdvCiv retains the old `> 0` UI test,
and current SAS retains the same interaction while also adding very slow speed
settings that make the zero truncation common. Hence the live defect is best
classified as an AdvCiv integration regression, not an originally reachable
stock-BtS bug.

REPAIR DIRECTION:
Honor the cost API's documented sentinel and expose the control whenever
`iCost >= 0`, not only when positive. A zero-cost item should remain selectable;
its text can simply show the normal visibility action without a positive cost.
The same `> 0` vs `>= 0` assumption should be reviewed in the neighboring
Advanced Start list builders, but no additional current zero-cost category is
numbered without its own data/formula proof.


CURRENT STONE AFTER F417
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F417 CONFIRMED.
F418 / provisional KI#741 UNUSED.
Latest physical checkpoint: C031-WIP119.
No repository source files modified.

Next continuation: finish the neighboring Advanced Start zero-cost/list-builder
checks, then continue low-mention WorldBuilder, mission/action, trade/deal and
whole-file runtime/display/sentinel/context reconciliation. Only promote F418
if current callers/data prove a distinct live consequence.

END C031-WIP119


POST-WIP119 CONVERSION-HELP RECONCILIATION
------------------------------------------
The unfinished religion-conversion diagnostic was completed against current SAS
runtime, current Policy Advisor callers, and the full BtS -> K-Mod -> AdvCiv ->
SAS ancestry. The omission is live in ordinary current games and is inherited
all the way from stock BtS.


F418 / provisional KI#741 — CONFIRMED
-------------------------------------
TITLE:
Religion conversion help can say only "Cannot convert to <religion>" while
omitting the actual blocker: no-state-religion civics or absence of the target
religion from the player's cities.

CLASSIFICATION:
Inherited stock-BtS diagnostic/help omission. Present unchanged in BtS/Civ4CE,
K-Mod, Base AdvCiv 1.14 and current AdvCiv-SAS.

RUNTIME CONTRACT:
Current CvPlayer::canConvert(ReligionTypes eReligion) rejects conversion when:

    if (isBarbarian())
        return false;
    if (isAnarchy())
        return false;
    if (getConversionTimer() > 0)
        return false;
    if (!isStateReligion())
        return false;
    if (getLastStateReligion() == eReligion)
        return false;
    if (eReligion != NO_RELIGION && !canDoReligion(eReligion))
        return false;

and current canDoReligion is simply:

    getHasReligionCount(eReligion) > 0

Thus two ordinary non-Barbarian blockers are part of the executable contract:

    1. the current civic setup does not permit a state religion;
    2. the requested religion is absent from all of the player's cities.

BROKEN HELP DECOMPOSITION:
CvGameTextMgr::setConvertHelp always begins with:

    "Cannot convert to <religion>"

but only explains:

    - player is in anarchy;
    - requested religion is already the state religion;
    - conversion timer / recent revolution is still active.

It contains no branch corresponding to `!isStateReligion()` and no branch for
`!canDoReligion(eReligion)`. When either omitted condition is the sole blocker,
the help ends after the generic heading and gives the user no reason.

LIVE CURRENT-SAS CALLER:
Current Assets/Python/Screens/CvPolicyAdvisorScreen.py uses the DLL predicate
and the C++ help together. For the selected religion it tests its canConvert
wrapper, which calls:

    gc.getPlayer(self.iActivePlayer).canConvert(iConvertReligion)

and whenever conversion is unavailable it displays:

    CyGameTextMgr().setConvertHelp(self.iActivePlayer, iLinkReligion)

on the Policy Advisor Religion tab. Therefore the missing diagnostic is directly
player-facing and does not depend on a debug or unused API path.

SUPPORTED CURRENT STATE A — FREE RELIGION:
Current SAS CIV4CivicInfos.xml gives CIVIC_FREE_RELIGION:

    <CivicOptionType>CIVICOPTION_RELIGION</CivicOptionType>
    ...
    <bStateReligion>0</bStateReligion>

CvPlayer::isStateReligion() is `getStateReligionCount() > 0`, so under Free
Religion the runtime rejects conversion at `!isStateReligion()`. The player can
still have founded religions in their cities and select/hover them on the
Religion tab. Assuming no anarchy and no conversion timer, setConvertHelp then
has no matching branch and displays only the generic inability to convert.

SUPPORTED CURRENT STATE B — RELIGION ABSENT FROM OUR CITIES:
While using an ordinary state-religion civic, select a founded religion that
exists in the world but is present in none of our cities. canDoReligion returns
false because getHasReligionCount(eReligion) is zero, so canConvert rejects it.
setConvertHelp again has no branch for that reason and stops at the generic
"Cannot convert to <religion>" heading.

ANCESTRY:
The BtS/Civ4CE implementation of setConvertHelp is materially the same: it
handles anarchy, already-current religion and conversion timer only. BtS's own
canConvert already includes both `!isStateReligion()` and `!canDoReligion`.
K-Mod retains the same mismatch, and Base AdvCiv/current SAS retain it as well.
This is therefore a stock-BtS help omission, not an AdvCiv/SAS integration
regression.

REPAIR DIRECTION:
Mirror the remaining executable reasons in setConvertHelp. After the existing
anarchy/current-religion/timer handling (or in runtime-order if preferred), add
localized diagnostics for:

    - state religions are not permitted by the player's current civics;
    - the target religion is not present in any of the player's cities.

A dedicated localized key is preferable to reusing unrelated "requires state
religion" / spread text because this is a conversion-action explanation. Also
consider the NO_RELIGION case explicitly: when the current civic setup already
prohibits state religions, `canConvert(NO_RELIGION)` is false through the same
`!isStateReligion()` gate and should not fall back to an unexplained generic
heading.


CONTROLS CLOSED AROUND F418
---------------------------
- The earlier Monarchy / Espionage-slider contradiction under
  GAMEOPTION_NO_ESPIONAGE is real compatibility debt, but current SAS hides the
  option, defaults it off and ships no scenario/map enabling it; it was not
  promoted under the ordinary-current-game threshold.
- The corporation first-discovery help remains literally correct under
  no-corporation civics: the technology still founds the corporation; such
  civics suppress its active effects/spread rather than the founding action.
- No existing current SAS localization key was found that cleanly expresses
  either missing conversion reason; a new dedicated key is the conservative
  repair rather than overloading unrelated religion text.


CURRENT STONE AFTER F418
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F418 CONFIRMED.
F419 / provisional KI#742 UNUSED.
Latest physical checkpoint: C031-WIP120.
No repository source files modified.

Next continuation: inspect setRevolutionHelp against canDoAnyRevolution / forced
civic states, then continue event-tech, mission/action and whole-file
runtime/display/sentinel/context closure sweeps. Only promote F419 with a
current ordinary caller/data consequence distinct from F418.

END C031-WIP120


POST-WIP120 CONTINUATION — QUEUE 003 CvGameTextMgr.cpp
=====================================================

CONTROLS / COVERAGE CLOSED BEFORE F419
--------------------------------------
- Rechecked the low-mention mission/action helpers. Current SAS's air/paradrop
  help consistently chooses a target-eligible mission unit; no new root was
  established. getInterfaceCenterText has no local NO_PLAYER guard around the
  active player, but it is an EXE-facing in-game interface callback and no
  supported pre-active-player invocation was established.
- Reconciled the WorldBuilder / Advanced Start data builders around F417. IDs
  match their palette contracts, the Barbarian-inclusive territory loop stays
  inside the valid player range, and no current non-visibility category was
  found with a legitimately purchasable zero-cost generic palette entry.
- Rechecked obsolete-resource help against corporation inputs. Current bonuses
  that obsolete (Fur, Elephants, Whale, Drama, Music) are not corporation
  prerequisite bonuses, so the active-player obsolete-bonus suppression does
  not currently hide a live corporation use.
- Rechecked the Finance unit-cost iExtraCost inflation caveat. Every current SAS
  unit has iExtraCost=0, so the omission remains dormant under current data.
- Reconciled several small trade/espionage/tech helpers against their runtime
  formulas. No independent F419 emerged from those paths.


F419 / provisional KI#742 — CONFIRMED
-------------------------------------
TITLE:
Found-city health preview prints nearby-feature unhealth instead of the
player-wide negative extra-health value.

CLASSIFICATION:
Inherited AdvCiv advc.004b TextMgr wrong-variable bug, present unchanged in
Base AdvCiv 1.14 and current AdvCiv-SAS. The negative branch was latent under
older mostly/nonnegative data but is ordinarily live in current SAS because
TECH_DEPOPULATION applies negative player-wide health.

BROKEN SOURCE:
Current CvGameTextMgr::setFoundHealthHelp computes feature health first:

    int iGoodHealth = iGoodHealthPercent / 100;
    int iBadHealth = iBadHealthPercent / 100;

and later obtains the independent player-wide value:

    int iExtraHealth = GET_PLAYER(getActivePlayer()).getExtraHealth();

The positive branch prints the correct variable:

    if (iExtraHealth > 0)
        ... format("%d", iExtraHealth) ...

but the negative branch instead prints the unrelated earlier accumulator:

    else
    {
        iIcon = UNHEALTHY_CHAR;
        ... format("%d", iBadHealth) ...
    }

Thus the displayed magnitude is determined by nearby feature unhealth, not by
negative player-wide extra health.

LIVE CURRENT-SAS DATA:
Current TECH_DEPOPULATION is enabled Future-era content and has:

    <iHealth>-9</iHealth>
    <iHappiness>-9</iHappiness>
    <bDisable>0</bDisable>

Its prerequisites are current enabled technologies (OR Composites; AND
Satellites, Computers and Environmentalism), so the technology is an ordinary
reachable part of the current tree.

Current CvTeam::setHasTech applies technology health directly to every team
member's persistent player value:

    kMember.changeExtraHealth(kTech.getHealth() * iChange);

Therefore acquiring Depopulation can make CvPlayer::getExtraHealth() negative
(e.g. -9 for a player without offsetting positive extra-health sources).

RUNTIME CITY CONTRACT:
CvCity::goodHealth and CvCity::badHealth both combine the player-wide and
city-local values before taking positive/negative parts:

    GET_PLAYER(getOwner()).getExtraHealth() + getExtraHealth()

so the negative player value is a real health contribution inherited by a
newly founded city. It is not a cosmetic or unused technology statistic.

LIVE CALLER:
CvDLLWidgetData::parseActionHelp_Mission handles MISSION_FOUND. Whenever the
selected unit may legally found on the mission plot, it calls in sequence:

    GAMETEXT.setFoundCostHelp(...)
    GAMETEXT.setHomePlotYieldHelp(...)
    GAMETEXT.setFoundHealthHelp(...)

Thus an existing Settler after Depopulation can reach the defective preview on
any legal founding plot; no debug or dormant API path is required.

CONCRETE CURRENT STATE:
Take a player whose net player-wide extra health is -9 after Depopulation and a
legal feature-neutral founding plot with iBadHealth=0.

Runtime consequence for the future city:
    player-wide extra health contribution = -9

Founding hover consequence:
    negative branch enters because iExtraHealth != 0 and iExtraHealth < 0;
    it prints iBadHealth = 0 with the unhealthy icon.

So a real -9 unhealthy contribution can literally be previewed as +0 unhealthy.
On a site with e.g. two whole points of nearby feature unhealth, the same -9
player effect is instead mislabeled as 2, proving this is wrong-source variable
contamination rather than a mere sign/rounding issue.

ANCESTRY:
AdvCiv commit:

    6f8d41a1ac548879d04f56ff9210e9a5e10aa975
    "Help text for feature health made more flexible, refactored."

introduced setFoundHealthHelp as part of advc.004b and the introducing diff
already contains the erroneous `iBadHealth` reference in the negative
`iExtraHealth` branch. Base AdvCiv 1.14 retains the line unchanged, as does
current SAS. Stock BtS/K-Mod do not have this AdvCiv helper in this form.

SAS ACTIVATION HISTORY:
SAS introduced negative health on Depopulation early in the Future-era work.
Commit 2a7f9e05e8 (practical 4895, "add initial future era techs ...") changed
Depopulation from iHealth=0 to iHealth=-5; later balance changes increased that
to -6, -8 and the current -9. Thus SAS content made an old AdvCiv negative-
branch typo an ordinary reachable player-facing defect.

REPAIR DIRECTION:
Use the actual negative player-wide value in the negative branch, e.g.:

    szBuffer.append(CvWString::format(L"%d%c", -iExtraHealth, iIcon));

(or an equivalent absolute-magnitude expression consistent with the helper's
existing `+<magnitude><healthy/unhealthy icon>` display convention).

The trailing TXT_KEY_FROM_TRAIT wording is also semantically incomplete because
CvPlayer::getExtraHealth can come from technologies, civics and other sources;
that is a secondary labeling concern, not the F419 root. The correctness repair
is first to stop substituting `iBadHealth` for `iExtraHealth`.


CURRENT STONE AFTER F419
------------------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp remains OPEN.
32/169 unique files complete.
F388-F419 CONFIRMED.
F420 / provisional KI#743 UNUSED.
Latest physical checkpoint: C031-WIP121.
No repository source files modified.

Next continuation: continue the remaining low-mention AdvCiv/K-Mod helpers and
search for analogous wrong-variable branches near founding/health/happiness
help, then resume whole-file source-vs-target, sentinel/localization and
runtime/display closure reconciliation. Only promote F420 with a supported
current caller/data consequence distinct from F419.

END C031-WIP121

===============================================================================
C031-WIP122R — RECONCILIATION CHECKPOINT BEFORE NEW ROOT NUMBERING
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
Distinct confirmed roots from this Queue remain F388-F418.
F419 / provisional KI#742 is RECONCILED AS A DUPLICATE OF F389 / KI#712.
F420 / provisional KI#743 remains UNUSED.
No repository source files modified.

WHY THIS RECONCILIATION WAS NECESSARY
-------------------------------------
The continuation handoff after C031-WIP121 proposed a new F420 candidate for the
SAS tech-tree obsolete-unit block. Re-reading the authoritative reconciled album
before numbering it showed that the exact same root was already confirmed much
earlier as F388 / KI#711.

That prompted a deliberate duplicate check of the immediately preceding late
finding too. It showed that F419 / KI#742 is likewise not independent: it is the
same setFoundHealthHelp wrong-variable root already confirmed as F389 / KI#712.

This checkpoint therefore corrects the continuation ledger before any further
new number is assigned. The earlier generated C031-WIP122 that temporarily
called the obsolete-unit root F420 is superseded by this WIP122R and must not be
used as an authoritative ledger state.

-------------------------------------------------------------------------------
DUPLICATE 1 — PROPOSED F420 == EXISTING F388 / KI#711
-------------------------------------------------------------------------------
Existing F388 already proves all of the material facts re-derived at the new
thread boundary:

- current SAS `setTechTradeHelp` globally scans every UnitInfo whose
  `getObsoleteTech() == eTech`;
- it ignores active-civilization UnitClass -> UnitType resolution;
- normal Tech Advisor `WIDGET_TECH_TREE` -> `parseTechTreeHelp` calls
  `setTechHelp` with `bPlayerContext=true` and `bTreeInfo=false` for an
  unresearched technology;
- current `TECH_CIVIL_SERVICE` therefore lists generic units plus foreign UUs;
- adjacent obsolete-building handling and the ordinary `Can Train` block both
  use civilization-aware class resolution;
- repair is to resolve UnitClass through the active civilization instead of
  globally enumerating UnitInfo.

The new-thread archaeology adds one useful refinement to F388 but does not form
another root:

    a134450486e0104df9473d46017ebc6e4d7bc553 (practical 5317)

is the semantic introduction of unit ObsoleteTech help and already contained
the global UnitInfo loop. Later practical 5332 / `8340dcb4...` only consolidated
those pre-existing per-unit bullets into a comma-separated line. Therefore any
future repair note for F388 should attribute the semantic context omission to
practical 5317, not practical 5332.

F420 is consequently still UNUSED.

-------------------------------------------------------------------------------
DUPLICATE 2 — F419 / KI#742 == EXISTING F389 / KI#712
-------------------------------------------------------------------------------
F419 states that `CvGameTextMgr::setFoundHealthHelp` prints nearby-feature
`iBadHealth` in the negative player-wide `iExtraHealth` branch, with current
TECH_DEPOPULATION making the negative branch ordinarily live.

That is already exactly F389 / KI#712. F389 contains the same wrong-variable
branch, the same player-wide health runtime contract, and the same repair
boundary. F419 therefore adds no independent producer or violated contract.

The secondary wording concern recorded beside F419 (the aggregate being labeled
"from trait") is also not new: it was already separately confirmed as F390 /
KI#713. Thus F419 cannot be salvaged as a different wording root either.

Disposition:

    F419 / KI#742 = DUPLICATE OF F389 / KI#712
    secondary wording = already F390 / KI#713

Do not implement or count KI#742 independently. Repository docs can be reconciled
later by the user/Codex; this investigation does not modify them.

-------------------------------------------------------------------------------
CURRENT AUTHORITATIVE STONE
-------------------------------------------------------------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F388-F418 are the distinct confirmed roots in this protected Queue-003 run.
F419/KI#742 is duplicate-reconciled into F389/KI#712.
F420/KI#743 is UNUSED and available for the next genuinely independent root.
Latest physical checkpoint: C031-WIP122R.
No repository source files modified.

Next continuation must use this reconciled state, not the superseded temporary
WIP122. Continue the remaining low-mention/current-delta/mechanical closure
sweeps and only promote F420 with a root not already present anywhere in the
album.

END C031-WIP122R


===============================================================================
C031-WIP123 — F420 CONFIRMED: HEALTH BREAKDOWN SPLITS A RUNTIME-NETTED VALUE
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F388-F418 remain the distinct earlier Queue-003 roots.
F419 / provisional KI#742 remains duplicate-reconciled into F389 / KI#712.
F420 / KI#743 is now CONFIRMED.
F421 / provisional KI#744 is UNUSED.
No repository source files modified.

-------------------------------------------------------------------------------
F420 / KI#743 — CONFIRMED
-------------------------------------------------------------------------------
TITLE:
CITY HEALTH HOVER SPLITS PLAYER-WIDE AND CITY-LOCAL EXTRA HEALTH EVEN THOUGH
RUNTIME NETS THEM FIRST, SO OPPOSITE-SIGNED CURRENT SOURCES MISSTATE THE
COMPONENT TOTALS

CLASSIFICATION:
K-Mod-origin health-help integration mismatch retained by Base AdvCiv and current
SAS; latent under K-Mod/Base-AdvCiv normal data, made ordinarily live by SAS's
negative-health Depopulation technology together with an inherited positive
city-health random event.

WHY THIS IS DISTINCT FROM F389/F390:
F389/KI#712 concerns `setFoundHealthHelp` and a direct wrong-variable typo in a
future-city founding preview. F390/KI#713 concerns the aggregate player-health
source being mislabeled "from trait". F420 is instead in the ordinary selected-
city health breakdown (`setBadHealthHelp` / `setGoodHealthHelp`) and is caused by
splitting two operands that runtime deliberately combines before sign clamping.
It therefore has a separate caller, producer shape and repair contract.

PREVIOUS DORMANT CONTROL REACTIVATED:
An earlier whole-file sweep had already noticed the semantic seam and correctly
kept it unnumbered at that time:

    CvCity::goodHealth()/badHealth() combine
        GET_PLAYER(getOwner()).getExtraHealth() + getExtraHealth()
    before clamping, while TextMgr prints the two values independently.

That control was dismissed because the then-audited current data was believed to
provide no ordinary opposite-signed pair. The later F389 investigation proved
that assumption stale: current enabled `TECH_DEPOPULATION` has `iHealth = -9`
and feeds that value into `CvPlayer::changeExtraHealth` on acquisition.

RUNTIME CONTRACT:
Current `CvCity::goodHealth()` uses:

    iHealth += std::max(0,
            GET_PLAYER(getOwner()).getExtraHealth() + getExtraHealth());

Current `CvCity::badHealth()` analogously uses:

    iHealth += std::min(0,
            GET_PLAYER(getOwner()).getExtraHealth() + getExtraHealth());

Thus these are not two independent health buckets. They are one net ExtraHealth
contribution whose sign is determined only after player-wide and city-local
values have been added.

CURRENT TEXTMGR CONTRACT VIOLATION:
`setBadHealthHelp` independently checks and prints:

    GET_PLAYER(kCity.getOwner()).getExtraHealth()

when it is negative, then independently checks `kCity.getExtraHealth()` only
when that second value is negative.

`setGoodHealthHelp` does the mirror image, printing the player value if positive
and the city value if positive.

Therefore opposite signs are never allowed to cancel in the source lines even
though runtime cancels them before assigning anything to healthy/unhealthy.
Both helpers then print footers from the correct runtime totals (`goodHealth()` /
`badHealth()`), so the displayed components can visibly fail to add up to their
own footer.

LIVE CURRENT-SAS PLAYER-WIDE NEGATIVE SOURCE:
Current enabled Future-era `TECH_DEPOPULATION` has:

    <iHealth>-9</iHealth>
    <bDisable>0</bDisable>

and ordinary team-tech acquisition executes:

    kMember.changeExtraHealth(kTech.getHealth() * iChange);

Its current prerequisites are enabled (OR Composites; AND Satellites, Computers
and Environmentalism), so this is normal reachable tree content, not a hidden
option or debug-only state.

LIVE CURRENT-SAS CITY-LOCAL POSITIVE SOURCE:
Current inherited `EVENT_HORTICULTURE_3` has:

    <bPickCity>1</bPickCity>
    <iHealth>3</iHealth>

`CvEventInfo::isCityEffect()` is loaded directly from the XML `bPickCity` field,
and `CvCity::applyEvent` applies such event health through:

    changeExtraHealth(kEvent.getHealth());

The Horticulture trigger is ordinary current content:
- 80% games active;
- weight 200;
- Calendar prerequisite;
- normal owned Forest/Jungle plot requirement;
- `bPickCity=1`;
- no obsolete tech.

Its selectable `EVENT_HORTICULTURE_2` outcome has a 67% AdditionalEventChance for
`EVENT_HORTICULTURE_3`. The +3 city value is persistent state: `m_iExtraHealth`
is incremented by `changeExtraHealth`, serialized in city saves, and Horticulture
3 has no EventTimes/ClearEvents entry that later removes it.

Thus an ordinary Calendar-era city can permanently acquire +3 city-local health
and later survive into the Future era when its owner researches Depopulation.
No WorldBuilder, Python injection, custom scenario or mod-mod XML is needed.

CONCRETE CURRENT-SAS FAILURE:
Take such a Horticulture city after Depopulation, with no other player-wide
ExtraHealth offset for the simple example:

    CvPlayer::getExtraHealth() = -9
    CvCity::getExtraHealth()   = +3

Runtime nets them first:

    -9 + 3 = -6

so this pair contributes exactly 6 unhealth and 0 health.

But `setBadHealthHelp` prints the full player-wide 9 unhealth source because it
checks -9 independently. It does not print the +3 city value in the bad section
because +3 is positive. The helper's final `TOTAL_UNHEALTHY` footer, however,
uses `kCity.badHealth()` and therefore incorporates only the correct net -6.
Its listed components consequently overstate unhealth by 3.

The good-health side has the mirror semantic problem: when the city has any
other positive source causing `goodHealth()>0`, `setGoodHealthHelp` can list the
+3 city ExtraHealth line even though this +3 contributes zero to runtime good
health because it is consumed by the -9 player-wide value before clamping.

LIVE UI CALLER:
Current `CvMainInterface.py` attaches `WIDGET_HELP_HEALTH` to the ordinary city
screen `HealthText`. `CvDLLWidgetData::parseHealthHelp` then calls:

    GAMETEXT.setBadHealthHelp(szBuffer, *pHeadSelectedCity);
    ...
    GAMETEXT.setGoodHealthHelp(szBuffer, *pHeadSelectedCity);

This is the standard selected-city health hover, so the mismatch is directly
player-facing.

ANCESTRY:
The supplied stock-BtS/Civ4CE source has only player-wide ExtraHealth in both
runtime good/bad health and the matching TextMgr breakdown; it does not have the
city-local operand in this contract.

The supplied K-Mod source is the first point in the local lineage where runtime
uses:

    GET_PLAYER(...).getExtraHealth() + getExtraHealth()

while its TextMgr simultaneously displays player ExtraHealth and
`city.getExtraHealth()` as separate sign-filtered lines. Base AdvCiv retains the
same semantic split, as does current SAS (with AdvCiv wording/refactoring around
the individual lines).

K-Mod already has positive city-local event health such as Horticulture 3, but
its supplied stock data has no negative direct tech/event health source that
creates the opposing-sign player-wide side. Base AdvCiv likewise has no ordinary
negative player-wide direct health source in the checked event/tech/civic/trait
XML. Hence the mismatch remained data-masked there.

SAS's Future-era Depopulation health penalty removes that masking condition.
This makes F420 best classified as a latent K-Mod integration mismatch retained
through AdvCiv and activated by current SAS data.

REPAIR DIRECTION:
Make the ordinary health breakdown honor the same net ExtraHealth bucket as
runtime. The player-wide and city-local values should be combined before deciding
whether this source contributes to the good or bad section, e.g. conceptually:

    iExtraHealth = GET_PLAYER(kCity.getOwner()).getExtraHealth()
                 + kCity.getExtraHealth();

and then print only the positive net in `setGoodHealthHelp` and only the negative
net magnitude in `setBadHealthHelp`.

If source attribution is preserved, it must still reconcile to that net rather
than presenting the two operands as independent health buckets. F390 separately
covers the existing inaccurate "from trait" attribution of the aggregate player
value and should not be conflated with this arithmetic repair.

-------------------------------------------------------------------------------
POST-F420 CONTROLS / CONTINUATION STONE
-------------------------------------------------------------------------------
The small technology-effect builder tranche checked immediately before F420 did
not yield another root: additive worker/feature-production/trade-route/health/
happiness/domain-move fields track runtime acquisition; free-unit help delegates
to `CvPlayer::getTechFreeUnit`; boolean capability helpers checked so far honor
the relevant team state. Existing option mismatches remain the already-numbered
F394-F396 family.

The specialist-link singular/plural polarity concern remains the already-recorded
latent grammar/link hazard: visible source text is unchanged and the only
potential effect lies in the EXE-side localized link resolver unavailable to
local proof. Do not promote it without new evidence.

CURRENT AUTHORITATIVE STONE:
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F388-F418 distinct earlier roots.
F419/KI#742 duplicate of F389/KI#712.
F420/KI#743 CONFIRMED.
F421/KI#744 UNUSED.
Latest physical checkpoint: C031-WIP123.
No repository source files modified.

Next continuation: use the reconciled-album-before-promotion discipline and move
into the remaining SAS/K-Mod-added low-mention helpers plus whole-file source-
vs-target, sentinel, caller-context and arithmetic closure sweeps. Give priority
to current-data activations of previously documented dormant hazards, because
F420 showed that later SAS content can invalidate an older "data-masked" control.

END C031-WIP123


===============================================================================
C031-WIP124 — PROTECTED QUEUE 003 CvGameTextMgr.cpp — F421 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F388-F418 distinct earlier roots.
F419/KI#742 remains RETRACTED as a duplicate of F389/KI#712.
F420/KI#743 CONFIRMED.
F421/KI#744 CONFIRMED.
F422 / provisional KI#745 UNUSED.
No repository source files modified.

-------------------------------------------------------------------------------
F421 / KI#744 — CONFIRMED
-------------------------------------------------------------------------------
TITLE:
CORPORATION MAINTENANCE CACHE EMBEDS THE INVERSE OF THE INFLATION RATE BUT IS
NOT REFRESHED WHEN INFLATION CHANGES, SO STABLE CORPORATION CITIES CAN BE
SYSTEMATICALLY OVERCHARGED AS THE GAME ADVANCES

CLASSIFICATION:
Inherited stock-BtS runtime cache defect. Present in stock BtS/Civ4CE, later
explicitly recognized in K-Mod source as "obviously a bug", and retained by
Base AdvCiv 1.14 and current AdvCiv-SAS.

HOW THIS SURFACED FROM THE TEXTMGR AUDIT:
The post-F420 pass reopened `setCorporationHelpCity` because that helper
recalculates corporation maintenance and then restores inflation for its displayed
corporation cost. A first lead concerned per-component versus aggregate rounding.
That weaker lead does not justify a new finding by itself: with a freshly updated
maintenance cache, the corporation icon has no independent aggregate total against
which its per-corporation allocation must sum exactly.

Tracing the helper back to the actual city-maintenance producer exposed a much
harder defect: the corporation-maintenance term stored in `m_iMaintenance` depends
on the current inflation rate, but inflation changing with game time does not
invalidate that cache.

RUNTIME CONTRACT:
Current `CvCity::calculateCorporationMaintenanceTimes100(eCorporation)` computes
normal corporation maintenance and then deliberately divides by inflation:

    int iInflation = GET_PLAYER(getOwner()).calculateInflationRate() + 100;
    if (iInflation > 0)
    {
        iMaintenance *= 100;
        iMaintenance /= iInflation;
    }

The reason for that inverse factor is that ordinary expenses later pass through
`CvPlayer::calculateInflatedCosts`:

    int iCosts = calculatePreInflatedCosts();
    iCosts *= std::max(0, calculateInflationRate() + 100);
    iCosts /= 100;

Thus, when recomputed at the same inflation rate, corporation maintenance is
intended to be approximately inflation-neutral: the inverse factor inside the
corporation component cancels the later general expense inflation.

CACHE CONTRACT:
`CvCity::updateMaintenance()` calculates:

    iNewMaintenance = calculateBaseMaintenanceTimes100() *
            max(0, getMaintenanceModifier() + 100) / 100;

and stores it in `m_iMaintenance`. `calculateBaseMaintenanceTimes100` includes
`calculateCorporationMaintenanceTimes100()`.

Therefore the inverse-inflation divisor used at the moment of a maintenance
refresh becomes embedded in the persistent city-maintenance cache.

But current `calculateInflationRate()` is a live calculation from game turn /
elapsed turns, game speed, handicap and modifiers. It increases as the game
advances without modifying any of the inputs that normally call
`updateMaintenance()`.

INVALIDATION FAILURE:
A complete source grep of current SAS maintenance refreshes finds updates for
specific state mutations such as:
- city creation/removal;
- population changes;
- government-center / maintenance-modifier changes;
- corporation/resource updates;
- relevant player modifiers, anarchy-state changes, religion/capital changes;
- a few Rise & Fall / vassal-state transitions.

There is no ordinary per-turn `CvPlayer::updateMaintenance()` and no hook that
refreshes city maintenance merely because `calculateInflationRate()` has advanced.
`CvCity::doTurn()` likewise does not refresh maintenance unconditionally.

Consequently a stagnant corporation city whose population, corporation/resource
network, maintenance modifiers and other invalidating state remain unchanged can
retain an inverse-inflation divisor from many turns earlier.

DIRECTION OF THE ERROR:
Suppose the cache was last refreshed at inflation I_old and the current inflation
has risen to I_new. Ignoring integer truncation for clarity, the corporation
portion stored in city maintenance is proportional to:

    nominal corporation maintenance * 100 / (100 + I_old)

but current general expenses multiply it by:

    (100 + I_new) / 100

so the final corporation burden becomes proportional to:

    nominal * (100 + I_new) / (100 + I_old)

rather than nominal.

Since ordinary inflation rises over time, a stale cache normally OVERCHARGES the
corporation. The longer the cache survives without an unrelated maintenance
refresh, the larger the drift can become.

CONCRETE CURRENT-SAS NUMERIC CONSTRUCTION:
Use current Normal speed, Standard world size and Noble handicap. Relevant XML:

    Normal iInflationPercent              = 30
    Normal iInflationOffset               = -90
    Standard CorporationMaintenancePercent = 100
    Noble CorporationMaintenancePercent    = 100

Use current Cereal Mills (`CORPORATION_1`), which has:

    iMaintenance = 100
    total HeadquarterCommerce = 4

Take a population-20 corporation city with ten qualifying connected grain
resources, no city maintenance modifier and no player corporation-maintenance
modifier.

Before the inverse-inflation step its corporation maintenance is:

    HQ term                 = 4 * 100 = 400 hundredths
    resource term           = 10 * 100 = 1000 hundredths
    subtotal                = 1400
    population factor       = (20 + 17) / 18
    floor(1400 * 37 / 18)   = 2877 hundredths

On an ordinary Normal-speed timeline with game turn == elapsed turns, current
SAS inflation is 48% at turn 224 and 59% at turn 250.

If the city's maintenance is refreshed at turn 224, the cached corporation term
becomes:

    floor(2877 * 100 / 148) = 1943 hundredths

A normal whole-player refresh can happen, for example, when anarchy ends;
`changeAnarchyTurns` calls `updateMaintenance()` when the anarchy state changes.

Now let the city remain stable through turn 250: no population change, resource
network change, corporation change, maintenance-modifier change or other refresh
producer. `CvCity::doTurn` does not itself refresh maintenance each turn.

At turn 250 the CORRECT freshly calculated inverse-inflation term would be:

    floor(2877 * 100 / 159) = 1809 hundredths

but the stored city cache still contains 1943. It is therefore already 134
hundredths (1.34 pre-inflated gold) too high for this one corporation city.

Applying the current 59% general inflation illustrates the intended cancellation
versus the stale result at component precision:

    fresh: floor(1809 * 159 / 100) = 2876 hundredths ~= 28.76 gold
    stale: floor(1943 * 159 / 100) = 3089 hundredths ~= 30.89 gold

The stale component is about 2.13 gold/turn above the inflation-neutral target
before the engine's later whole-expense aggregation/truncation.

This is not restricted to sub-gold noise. If ten otherwise stable corporation
cities have the same 134-hundredth stale excess, `m_iTotalMaintenance` is 1340
hundredths too high. Regardless of the unrelated maintenance fractional residue,
`getTotalMaintenance()` is then 13 or 14 whole pre-inflation gold too high; at
59% inflation that necessarily changes the final charged expenses by roughly
20+ gold/turn.

LIVE TEXTMGR CONSEQUENCE:
Current `setCorporationHelpCity` does NOT read the cached corporation portion.
It calls `calculateCorporationMaintenanceTimes100(eCorporation)` afresh using the
CURRENT inflation rate and then multiplies by the current inflation factor for
display (a K-Mod UI change).

Thus after the cache has gone stale the corporation icon can continue to show the
roughly inflation-neutral/current corporation maintenance while the player's
actual cached city maintenance is based on an older, larger inverse-inflation
term. The UI can therefore understate the maintenance actually feeding the
player's expenses. This is a symptom of the runtime cache root, not a separate
TextMgr finding.

ANCESTRY:
Stock BtS/Civ4CE already has all essential pieces:
- corporation maintenance divides by `calculateInflationRate() + 100`;
- city maintenance is cached through `m_iMaintenance` / `updateMaintenance()`;
- inflation is time-dependent;
- there is no per-turn maintenance refresh tied to inflation.

So the runtime root is stock BtS, not a K-Mod invention.

K-Mod later made inflation itself cached/updated per turn. In practical commit
1195, SHA `8c6e324ab4e02e1276b0f565d8746696d4f2ee51`, source commentary explicitly
states that because city maintenance is cached, the inverse inflation correction
can remain unchanged while inflation grows, making corporation maintenance too
high, and concludes:

    "This is obviously a bug; but I'm not going to worry about it right now."

That K-Mod commit documents rather than introduces the root.

K-Mod practical 1197 (`efe8a852fa0d6c56a7c3dd57af2eeced1a99b72c`) then changed
`setCorporationHelpCity` to include inflation when displaying costs, but did not
repair maintenance-cache invalidation.

AdvCiv's early import/refactor (`f6ff6a182f5187541fff6e5bbbe12f9573b3778c`)
removed K-Mod's stored `m_iInflationRate` / `updateInflationRate()` and returned
inflation to an on-demand `calculateInflationRate()` result. It retained the
inverse-inflation corporation-maintenance formula and still did not add a
maintenance refresh when time changes. Base AdvCiv 1.14 and current SAS therefore
retain the stock defect.

REPAIR DIRECTION:
The conservative repair is to keep the intended inflation-neutral corporation
rule but ensure that any cached maintenance containing the inverse inflation term
is rebuilt whenever the effective inflation rate changes, before that maintenance
is used for economic charging / UI totals.

Possible implementations include:
- track the inflation rate used for the maintenance cache and call
  `CvPlayer::updateMaintenance()` when the effective rate changes;
- refresh maintenance at a turn transition point that is guaranteed to precede
  all expense calculations for that inflation rate (ideally only when the rate
  actually changes, and potentially only for players with active corporations);
- architecturally remove the time-varying inverse-inflation term from persistent
  cached city maintenance and handle corporation inflation cancellation outside
  that cache.

Simply deleting the inverse-inflation division would make corporation maintenance
inflate normally and would therefore be a gameplay-rule change rather than the
minimal bug fix.

ROUNDING / DECOMPOSITION CONTROL THAT LED HERE:
With a freshly updated cache, `setCorporationHelpCity` applies city maintenance
modifier and inflation to one corporation separately, whereas actual city/player
expenses aggregate components and perform integer truncation at later stages.
This can create one-gold allocation differences. However the corporation hover
has no independently displayed aggregate total that its one-corporation number
must sum to, unlike F400's internally contradictory Finance Advisor breakdown.
Without a stronger semantic contract, keep that as presentation/decomposition
debt rather than creating a second finding. F421 is the independently provable
runtime cache defect.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F421
-------------------------------------------------------------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F388-F418 distinct earlier roots.
F419/KI#742 duplicate/retracted.
F420/KI#743 CONFIRMED.
F421/KI#744 CONFIRMED.
F422 / provisional KI#745 UNUSED.
Latest physical checkpoint: C031-WIP124.
No repository source files modified.

Next continuation: preserve the reconciled-album-before-promotion discipline.
Resume the data-masked-control activation sweep and the remaining low-mention
SAS/K-Mod TextMgr deltas. In particular, inspect other helpers that recalculate
values from current dynamic state while the actual runtime consumes a cached
quantity, because F421 shows that such fresh-help/cached-runtime splits can hide
old invalidation defects. Then continue whole-file source-vs-target, sentinel,
localization and arithmetic closure sweeps.

END C031-WIP124


===============================================================================
C031-WIP125 — FULL LITERAL LOCALIZATION-CONTRACT SWEEP + F422-F425 CONFIRMED
===============================================================================

Continuation state on entry:
- Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
- F421/KI#744 confirmed in C031-WIP124.
- F422/provisional KI#745 unused.
- No repository source files modified.

This tranche completed the mechanical cross-language contract pass that had been
started after WIP124. An initial parser was deliberately discarded before any
finding was frozen because it did not robustly handle comments/nested C++ calls.
The corrected pass strips C/C++ comments, balances calls syntactically and parses
ordinary percent placeholders plus indexed grammar references before comparing
those references with the arguments actually supplied by each literal
`gDLL->getText("TXT_KEY_...")` call.

AUTHORITATIVE SCAN SIZE
-----------------------
Current `CvGameTextMgr.cpp` contains, after comment removal:

    1252 active literal getText calls
     980 distinct literal localization keys

The corrected scan found:

* two effective keys absent from the loaded Civ4/Warlords/BtS/SAS text set;
* ten out-of-range language/call combinations, representing only three source
  roots (four languages for one root, five languages for the already-known
  dormant negative-event-health root, and one Spanish root);
* eight percent-placeholder signature differences across languages, collapsing
  to five semantic candidates after identical multi-language roots are grouped;
* two `%d` versus `%D` language differences that do not by themselves prove a
  behavioral defect;
* one literal placeholder-looking token without `%`, the French Pacifism root
  promoted below as F423.

The scan was then reconciled against the album and current XML/caller reachability
before assigning any number. In particular, it mechanically rediscovered the
already-recorded dormant negative-event-health city/global key mismatch and did
not duplicate it.

-------------------------------------------------------------------------------
F422 / provisional KI#745 — FRENCH CITY-BAR ANGER LOCALIZATION PRINTS THE ANGRY
POPULATION ARGUMENT TWICE, INSERTING AN EXTRA NUMBER BEFORE THE TURN COUNT NOUN
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + Assets/XML/Text/CIV4GameText_advc.xml
Primary function: CvGameTextMgr::setCityBarHelp
Classification: AdvCiv French localization typo introduced with advc.186 city-bar
hover integration; retained by Base AdvCiv 1.14 and current SAS.

ROOT
----
The C++ contract is unambiguous:

    szString.append(gDLL->getText("TXT_KEY_CITY_BAR_ANGER_TIMER",
            iAngryPop, iAngerTimer));

There are exactly two arguments:

    %d1 = displayed angry population
    %d2 = displayed anger timer

The current English/German/Italian/Spanish strings use those two arguments once.
French instead contains:

    -%d1[ICON_UNHAPPY] pour %d2 %d1 [NUM2:tour:tours]

The second `%d1` has no grammatical role. It repeats the angry-population number
between the timer and the noun selected from argument 2.

For example, if the helper supplies `(3, 10)`, the French text structurally
contains:

    -3 unhappy pour 10 3 tours

rather than the intended equivalent of `-3 unhappy for 10 turns`.

CURRENT REACHABILITY
--------------------
This is an ordinary current-SAS path:

* `setCityBarHelp` executes this block for the active player's city when the BUG
  option `CityScreen__Anger_Counter` is enabled.
* Current `Assets/Config/BUG City Screen.xml` sets that option to `default="True"`.
* Any positive hurry, conscription or defy-resolution anger timer reaches the
  formatter.
* F407 already provides a fully current-data construction with simultaneous
  hurry/draft anger; no special scenario or custom XML is needed merely to
  reach the localization key.

DISTINCT FROM F407
------------------
F407 is the C++ arithmetic root in the same display: AdvCiv takes the maximum of
multiple temporary-anger sources where runtime adds them. F422 remains wrong even
if F407 is repaired perfectly, because it is an independent French XML format
string typo. Conversely, changing this French string does not repair F407.

ANCESTRY
--------
Reachable-history practical commit 3010:

    92202f546295e43857d6172e1b9c6665446e68a5
    "Overhaul city bar hover text (based on BULL)"

introduced both the C++ call and `TXT_KEY_CITY_BAR_ANGER_TIMER`. Its introducing
diff already contains the duplicate French `%d1`. Base AdvCiv 1.14 and current
SAS retain it. This therefore belongs to AdvCiv's advc.186 integration rather
than stock BtS/K-Mod.

REPAIR DIRECTION
----------------
Remove the second `%d1` from the French localization, preserving argument 2 for
the `[NUM2:tour:tours]` grammar token, e.g. structurally:

    -%d1[ICON_UNHAPPY] pour %d2 [NUM2:tour:tours]

This is a localization-only repair; it should be made independently of F407's
C++ anger aggregation repair.

-------------------------------------------------------------------------------
F423 / provisional KI#746 — STOCK CIV4 FRENCH STATE-RELIGION GREAT-PEOPLE CIVIC
HELP OMITS THE `%` BEFORE ARGUMENT 2 AND DISPLAYS `F2_Religion` AS LITERAL TEXT
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + inherited Civ4 text XML
Primary function: CvGameTextMgr::parseCivicInfo
Classification: stock Civ4 French localization typo retained through BtS/K-Mod,
AdvCiv and current SAS.

ROOT
----
For a civic with a nonzero state-religion Great Person modifier, the generic /
non-player-context branch supplies two arguments:

    gDLL->getText("TXT_KEY_CIVIC_GREAT_PEOPLE_MOD_STATE_RELIGION",
            kCivic.getStateReligionGreatPeopleRateModifier(),
            gDLL->getSymbolID(RELIGION_CHAR));

The inherited key is:

English:
    ... State %F2_Religion
German/Italian/Spanish:
    ... %F2_Religion ...
French:
    ... religion d'etat : F2_Religion

French alone lacks the leading `%`. Therefore its second argument is never
interpreted as the religion glyph/formatter token; the user instead receives the
literal text `F2_Religion` at the end of the help line.

CURRENT REACHABILITY
--------------------
Current `CIVIC_PACIFISM` has:

    iStateReligionGreatPeopleRateModifier = 100

and is normal current content (currently unlocked at Medicine).

There are two ordinary current callers:

1. Policy Advisor:

       CyGameTextMgr().parseCivicInfo(iCivic, False, True, True)

   If the active player currently has no state religion, `parseCivicInfo` takes
   the `_STATE_RELIGION` branch.

2. Current SAS Sevopedia civic page:

       CyGameTextMgr().parseCivicInfo(self.iCivic, True, False, True)

   Here `bPlayerContext == false`, so Pacifism's pedia effects always use the
   `_STATE_RELIGION` key regardless of the active player's religion.

Thus merely opening Pacifism in the current Sevopedia while the game language is
French is sufficient to expose the malformed line.

ANCESTRY
--------
`TXT_KEY_CIVIC_GREAT_PEOPLE_MOD_STATE_RELIGION` is present in the stock Civ4 text
set represented by Civ4CE. Its English/German/Italian/Spanish entries contain
`%F2_Religion`; its French entry already contains bare `F2_Religion`.
No Warlords/BtS/SAS override repairs it. This is therefore a stock-Civ4 French
text defect inherited intact through the lineage rather than an AdvCiv/SAS
regression.

REPAIR DIRECTION
----------------
Change French `F2_Religion` to `%F2_Religion`. No C++ change is required.

-------------------------------------------------------------------------------
F424 / provisional KI#747 — ADVCIV ESPIONAGE CIVIC/RELIGION COST WARNING PASSES
ONE ARGUMENT BUT FOUR NON-ENGLISH LOCALIZATIONS REQUEST `%s2`
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + Assets/XML/Text/CIV4GameText_advc.xml
Primary function: CvGameTextMgr::setEspionageCostHelp
Classification: AdvCiv advc.132 localization argument-index defect; introduced
with the warning feature and retained by Base AdvCiv 1.14 and current SAS.
Affected current languages: French, German, Italian, Spanish.

ROOT
----
AdvCiv deliberately added a warning when a force-civic/force-religion espionage
mission remains legal even though the spying player does not itself practice the
chosen civic/religion. The formatter is called with exactly ONE argument:

    gDLL->getText("TXT_KEY_ESPIONAGE_CIVIC_RELIGION_MOD",
            kMission.getSwitchCivicCostFactor() > 0 ?
            GC.getInfo((CivicTypes)iExtraData).getTextKeyWide() :
            GC.getInfo((ReligionTypes)iExtraData).getTextKeyWide())

English correctly references:

    %s1_ReligionOrCivic

but French, German, Italian and Spanish all reference:

    %s2_ReligionOrCivic

There is no argument 2. This is a hard localization/C++ contract mismatch, not a
translation-style difference. Those four languages request an out-of-range
placeholder whenever this warning branch is used.

CURRENT REACHABILITY
--------------------
Current espionage XML retains both ordinary missions:

    ESPIONAGEMISSION_SWITCH_CIVIC
        iSwitchCivicCostFactor = 1000

    ESPIONAGEMISSION_SWITCH_RELIGION
        iSwitchReligionCostFactor = 1000

Both are active city-targeting missions with no tech prerequisite in current XML.

Current `CvPlayer::getEspionageMissionBaseCost` intentionally supports the exact
mismatch state that the warning describes. For force-civic cost it adds 100 to
the speed/anarchy modifier when the spying player is not using the candidate
civic; for force-religion it analogously adds 100 when the spying player's state
religion differs. In other words, mismatch does not invalidate the mission; it
increases its cost.

`CvDLLWidgetData::parseEspionageCostHelp` is the ordinary
`WIDGET_HELP_ESPIONAGE_COST` path. It obtains the selected eligible Spy and calls
`setEspionageCostHelp` with the mission, target plot/player and mission extra data.
Thus an ordinary spy mission button hover can enter this warning path.

ANCESTRY
--------
Reachable-history practical commit 4221:

    cd4282f9fb8bf792a353e416fdfc5abf4fae0f3d
    "Espionage cost breakdown warns when civic/religion not practised"

introduced the C++ warning and its new localization key together. The introducing
diff already has English `%s1` and FR/DE/IT/ES `%s2`. Base AdvCiv 1.14 and current
SAS retain the mismatch.

REPAIR DIRECTION
----------------
Change `%s2_ReligionOrCivic` to `%s1_ReligionOrCivic` in French, German, Italian
and Spanish. The current C++ one-argument call is the intended contract and the
English entry already demonstrates it.

-------------------------------------------------------------------------------
F425 / provisional KI#748 — SPANISH ZERO-ATTITUDE SUMMARY REQUESTS NONEXISTENT
ARGUMENT 3 EVEN THOUGH THE ZERO CASE PASSES ONLY ATTITUDE AND LEADER NAME
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + Assets/XML/Text/CIV4GameText_advc.xml
Primary function: CvGameTextMgr::getAttitudeString
Classification: early AdvCiv/advc.004q Spanish localization argument-index typo,
present in the earliest tracked AdvCiv import and retained by current SAS.

ROOT
----
The exact-zero Cautious branch calls:

    gDLL->getText("TXT_KEY_ZERO_ATTITUDE_TOWARDS",
            szAttitude, szTargetName)

so the contract is:

    %s1 = attitude description
    %s2 = target leader name

English/French/German/Italian all obey that contract. Spanish instead says:

    Actitud hacia %s3_leader: %s1_attitude (+/- 0)

There is no third argument in the zero branch.

The copy/paste origin is especially clear in the neighboring positive/negative
neutral-attitude keys. Those nonzero variants genuinely pass THREE arguments:
attitude, numeric total, leader name, and therefore correctly use `%s3_leader`.
The Spanish zero variant retained that third-argument index even though the zero
formatter omits the numeric middle argument.

CURRENT REACHABILITY
--------------------
This is an ordinary diplomacy/scoreboard state, not a rare XML branch:

* current `AI_getAttitudeFromValue` returns Cautious for totals not reaching the
  configured Friendly/Pleased/Annoyed/Furious thresholds;
* current thresholds are Friendly >= 9, Pleased >= 4, Annoyed <= -2,
  Furious <= -9, so an exact total of 0 is unambiguously Cautious;
* `getAttitudeString` therefore selects `TXT_KEY_ZERO_ATTITUDE_TOWARDS` whenever
  the visible summed attitude is exactly 0;
* current `CvDLLWidgetData` invokes `getAttitudeString` in normal player /
  scoreboard hover paths, and BUG `AttitudeUtil.py` also consumes the same DLL
  string.

Thus a Spanish game merely needs an AI relationship whose current visible total
is exactly zero, a normal early/intermediate diplomacy state.

ANCESTRY
--------
The key appears in reachable-history practical commit 1253:

    f6ff6a182f5187541fff6e5bbbe12f9573b3778c
    "Initial commit (cont.)"

inside the advc.004q attitude-summary text family. That earliest tracked entry
already has `%s3_leader` in Spanish while the other four languages use `%s2`.
A later attitude refactor retained the two-argument C++ call and did not repair
the Spanish key. K-Mod/Civ4CE do not contain this AdvCiv key family.

Because practical 1253 is an early history/import boundary, classify this as an
early-AdvCiv/advc.004q defect present in the earliest tracked AdvCiv form rather
than claiming that the import commit necessarily originated the typo historically.

REPAIR DIRECTION
----------------
Change the Spanish zero-case leader placeholder from `%s3_leader` to
`%s2_leader`. Leave the nonzero three-argument Spanish attitude keys at `%s3`.

-------------------------------------------------------------------------------
FULL-SWEEP CONTROLS / NON-FINDINGS PRESERVED AFTER F422-F425
-------------------------------------------------------------------------------

1. Already-known dormant negative-event-health key mismatch
------------------------------------------------------------
The corrected scanner flags `TXT_KEY_EVENT_UNHEALTH_CITY` because one current C++
negative-health branch supplies only the numeric health argument although the key
also expects `%s2_city`. This is the same already-recorded dormant event-health
city/global-key mismatch, not a new root. Current Event XML still supplies no
negative `iHealth`, so it remains data-dormant and consumes no number here.

2. `TXT_KEY_BUILDING_RELIGION_YIELD` German omits `%s1`
--------------------------------------------------------
Other languages name the building/religion argument. German instead uses a
self-contained description equivalent to "all religious buildings whose religion
matches this building." It does not require the argument to preserve the effect's
meaning. Treat as translation phrasing, not a contract bug.

3. `%d` vs `%D` language differences
------------------------------------
The scanner finds Spanish `TXT_KEY_EVENT_HURRY_ANGER` and French
`TXT_KEY_MISC_TOTAL_HEALTHY` differing in signed/unsigned-style placeholder code
from English. No current source/runtime contradiction was proven from that alone.
Do not promote without a concrete formatting failure.

4. Missing `TXT_KEY_CIVIC_COLONY_MAINT_MOD`
-------------------------------------------
Current C++ asks for `TXT_KEY_CIVIC_COLONY_MAINT_MOD`, while AdvCiv text defines
`TXT_KEY_CIVIC_COLONYE_MAINT_MOD` (extra E). This is a genuine AdvCiv key-name
typo, but current SAS's only nonzero `iColonyMaintenanceModifier` is Vassalage at
-100, which takes the separate "No Colonial Expenses" branch. The broken general
modifier branch remains current-data dormant.

5. Missing `TXT_KEY_CIV_UNKNOWN`
--------------------------------
`parseCivInfos(NO_CIVILIZATION, ...)` asks for `TXT_KEY_CIV_UNKNOWN`, and the
combined stock+SAS text search finds no effective key. The branch is inherited
from stock code. Known current Python callers pass concrete civilizations; an
EXE-side SimpleCivPicker/random-civilization path may call the sentinel branch,
but local source does not prove that. Preserve as an EXE-facing suspicion, not a
finding.

6. WorldBuilder Holy City / Headquarters extra arguments
---------------------------------------------------------
The stock WorldBuilder data helpers pass religion/corporation names into generic
keys whose shipped strings contain no placeholders. This may discard identifying
text, but the functions are EXE-facing `DllExport` palette records and local
source does not establish how the EXE displays those descriptions beside IDs /
icons. Keep below threshold.

7. Earlier partial scanner is superseded
-----------------------------------------
Do not reuse the earlier 529-key / 717-ish-call intermediate result. The
comment-aware corrected scan above (1252 active literal calls / 980 unique keys)
is authoritative for this tranche.

-------------------------------------------------------------------------------
CURRENT STONE AFTER F425
-------------------------------------------------------------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F388-F418 distinct earlier roots.
F419/KI#742 duplicate/retracted.
F420/KI#743 CONFIRMED.
F421/KI#744 CONFIRMED.
F422/KI#745 CONFIRMED.
F423/KI#746 CONFIRMED.
F424/KI#747 CONFIRMED.
F425/KI#748 CONFIRMED.
F426 / provisional KI#749 UNUSED.
Latest physical checkpoint: C031-WIP125.
No repository source files modified.

NEXT
----
Continue from F426 unused rather than stopping at the localization finds.
First finish the localization closure beyond strict argument indices: inspect
literal calls whose C++ supplies arguments that every effective localization
ignores, missing-language/fallback behavior where locally provable, and suspicious
same-language sibling/copy-paste key contracts. Keep `TXT_KEY_CIV_UNKNOWN` below
threshold unless an ordinary current caller is proven. Then return to the
remaining source-vs-target, cache-vs-live, sentinel/index and SAS/AdvCiv delta
closure sweeps for `CvGameTextMgr.cpp`.

END C031-WIP125

===============================================================================
C031-WIP126 — POST-LOCALIZATION CLOSURE / GET_TEAM OVERLOAD RECONCILIATION
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F388-F418 distinct earlier roots.
F419/KI#742 duplicate/retracted.
F420/KI#743 through F425/KI#748 confirmed.
F426 / provisional KI#749 remains UNUSED.
No repository source files modified.

WORK SINCE WIP125
-----------------
The post-localization sweep first rechecked several old dormant controls against
current 6351-era SAS data. The Groundbreaking/research-modifier overlap remains
masked: TECH_DIFFUSION_ENABLE, AI_GROUNDBREAKING_PENALTY_ENABLE and
HUMAN_GROUNDBREAKING_PENALTY_ENABLE are all still 0. The source contradiction in
setResearchModifierHelp therefore remains future/mod-mod debt rather than a
current finding.

A function-level SAS-vs-Base-AdvCiv delta map was also rebuilt. The executable /
textual current deltas had already received prior finding-or-control
classification. In particular, the self-referential debug cache

    static const SpecialistTypes eDefaultSpecialist =
            (SpecialistTypes)eDefaultSpecialist;

in setPlotHelpDebug_ShiftAltOnly is still a genuine source typo but remains data
masked because current enum 0 and DEFAULT_SPECIALIST are both SPECIALIST_CITIZEN.
It does not consume F426.

FALSE-POSITIVE FAMILY CLOSED — GET_TEAM(PlayerTypes) IS TYPE-AWARE
------------------------------------------------------------------
A mechanical player/team-domain scan made three current expressions look highly
suspicious:

    setResearchStr:
        GET_TEAM(ePlayer).getTechCount(...)

    getVassalInfoString:
        CvTeam const& kTeam = GET_TEAM(ePlayer);

    parseWarTradesHelp:
        GET_TEAM(eThisPlayer).isAtWar(...)
        GET_TEAM(eOtherPlayer).isHuman()
        GET_TEAM(eThisPlayer).AI_declareWarTrade(...)

The history makes the syntax especially deceptive: old AdvCiv often used the
TEAMREF(player) macro and practical 1679 mechanically changed many such calls to
GET_TEAM(player); K-Mod's getVassalInfoString explicitly used
GET_TEAM(GET_PLAYER(ePlayer).getTeam()), while AdvCiv practical 3062 later
simplified it to GET_TEAM(ePlayer).

These are nevertheless NOT player-ID-as-team-ID bugs. Current AdvCiv/SAS defines
GET_TEAM through an overloaded helper. In the gameplay layer:

    #define GET_TEAM(x) CvGamePlay::getTeam(x)

    __forceinline CvTeam& getTeam(TeamTypes eTeam)
    {
        return CvTeam::getTeam(eTeam);
    }

    __forceinline CvTeam& getTeam(PlayerTypes ePlayer)
    {
        return CvTeam::getTeam(TEAMID(ePlayer));
    }

and the CoreAI layer analogously defines:

    getTeam(PlayerTypes ePlayer)
    {
        return CvTeamAI::AI_getTeam(TEAMID(ePlayer));
    }

Therefore overload resolution preserves the PlayerTypes static type and converts
the player through that player's actual team. This is safe when player and team
numeric IDs differ, including team games / team reassignment.

This exact semantic trap was already preserved in Queue-003 WIP97 and elsewhere
in the album. The current recheck broadens that positive control explicitly to:

* setResearchStr;
* getVassalInfoString;
* parseWarTradesHelp.

Do not promote any of these merely because the source argument is named ePlayer.
The old F113/KI#436 player/team-ID hypothesis was likewise retracted after this
overload was traced. parseLeaderLineHelp has similar-looking calls but is already
explicit dead code; it remains a dead-path control.

The history remains useful for archaeology but does not change the runtime
contract:

* practical 1679 (`1c56cc6ac1...`, "GET_PLAYER, GET_TEAM macros outside of AI
  headers") converts TEAMREF(player) spellings to GET_TEAM(player) precisely as
  part of introducing the typed overload infrastructure;
* practical 3062 (`457122d485...`, "Misc. refactoring") simplifies K-Mod's
  explicit player->team chain in getVassalInfoString to that overloaded form.

No F426 is consumed.

OTHER CONTROLS CLOSED IN THIS TRANCHE
-------------------------------------
* The Finance foreign-income helper enumerates the same per-player gold-per-turn
  state maintained by changeGoldPerTurnByPlayer; no independent hidden component
  was established.
* Civic upkeep help follows the same anarchy-aware upkeep path as the migrated
  Domestic Advisor/runtime; no sibling to the already-numbered unit/supply
  anarchy mismatch appeared.
* The plot-list `appendUnitOwnerHeading` comma suspicion is false: when Other is
  the only category, iOther == iTotal triggers the generic `(N Units)` fast path.
  The comma branch is reachable only after Army or Navy has already set bFirst
  false.
* vassal-revolt ratios and trade-route calculation assertions were already
  explicitly reconciled in WIP111 and were not re-promoted from low literal
  mention counts.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F425 are the latest confirmed distinct Queue-003 roots.
F426 / provisional KI#749 UNUSED.
Latest physical checkpoint: C031-WIP126.
No repository source files modified.

NEXT
----
Continue from F426 unused. With current SAS-vs-AdvCiv executable deltas already
classified and the localization argument-contract family provisionally saturated,
focus on inherited paired-branch/wrong-sibling-variable patterns, cache-vs-live
state, aggregate-vs-component arithmetic, sentinel/index asymmetries, and old
data-masked controls whose masking assumptions may have changed in current SAS.
For any new candidate, search the reconciled album before promotion.

END C031-WIP126


===============================================================================
C031-WIP127 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION — F426 CONFIRMED
===============================================================================

STATUS
------
F420-F425 remain CONFIRMED.
F426 / provisional KI#749 is CONFIRMED.
F427 / provisional KI#750 is UNUSED.

-------------------------------------------------------------------------------
F426 / provisional KI#749 — FORCE-RELIGION ESPIONAGE HELP REUSES THE FORCE-CIVIC
TEXT KEY, SO GERMAN ORDINARY MISSION HELP CALLS THE CHOSEN RELIGION A CIVIC /
GOVERNMENT FORM
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + stock BtS localization
Primary function: CvGameTextMgr::setEspionageCostHelp
Classification: stock-BtS localization/help integration defect retained by K-Mod,
Base AdvCiv 1.14 and current SAS; ordinary current German UI exposure.

ROOT
----
The force-civic and force-religion branches are distinct runtime effects, but
stock BtS uses the exact same help key for both:

    if (kMission.getSwitchCivicCostFactor() > 0)
    {
        ...
        gDLL->getText("TXT_KEY_ESPIONAGE_HELP_SWITCH_CIVIC",
                targetName, civicName)
    }

    if (kMission.getSwitchReligionCostFactor() > 0)
    {
        ...
        gDLL->getText("TXT_KEY_ESPIONAGE_HELP_SWITCH_CIVIC",
                targetName, religionName)
    }

There is no TXT_KEY_ESPIONAGE_HELP_SWITCH_RELIGION anywhere in the locally
available lineage.

The inherited BtS localization for the reused key is not language-neutral in
all supported languages. In particular German is:

    %s1_player zum Wechsel zur Staatsform %s2_civic verleiten

"Staatsform" means the civic/government form. Supplying a ReligionInfo text key
as argument 2 therefore produces a semantically false ordinary help sentence --
for example, a Buddhism choice is described as inducing the target to switch to
the civic/government form Buddhism.

English happens to mask the design error because its wording is generic:

    Induce %s1_player to switch to %s2_civic

Italian is similarly generic, and French "doctrine" is less categorical. The
German localization demonstrates a hard visible consequence of the shared-key
contract rather than a mere naming-style concern.

CURRENT SAS REACHABILITY
------------------------
Current CIV4EspionageMissionInfo.xml retains the normal two-phase mission:

    ESPIONAGEMISSION_SWITCH_RELIGION
        bIsTwoPhases = 1
        bTargetsCity = 1
        TechPrereq = NONE
        iSwitchReligionCostFactor = 1000

CvDLLButtonPopup::launchEspionageTargetPopup handles this mission by iterating
all religions. For every religion for which canDoEspionageMission succeeds, it
adds a normal selectable button with:

    WIDGET_HELP_ESPIONAGE_COST,
    eMission,
    eLoopReligion

Thus widget data2 is the actual chosen ReligionTypes value. The ordinary widget
parser finds the eligible selected Spy and calls:

    GAMETEXT.setEspionageCostHelp(...,
            widgetDataStruct.m_iData2, pUnit)

so hovering one of those legal religion choices enters the force-religion help
branch with exactly the religion selected by the popup. No debug mode, hidden
option, unsupported scenario or dormant XML value is required.

Runtime execution separately confirms the intended semantics: CvPlayer's
espionage execution branch for getSwitchReligionCostFactor sets the target's
last state religion to the chosen ReligionTypes and uses the religion-specific
TXT_KEY_ESPIONAGE_TARGET_SWITCH_RELIGION result key. The help line is therefore
the mismatched component, not ambiguous gameplay behavior.

ANCESTRY
--------
The Civ4CE BtS reference already contains both pieces of the defect:

* CvGameTextMgr::setEspionageCostHelp uses
  TXT_KEY_ESPIONAGE_HELP_SWITCH_CIVIC in the force-religion branch;
* CIV4GameText_BTS_FourthRoundTranslation.xml defines that key with the German
  word "Staatsform" and has no religion-specific sibling help key.

K-Mod retains the same reuse. Base AdvCiv 1.14 and current SAS retain it too.
Current SAS does not override the inherited help key. This is therefore a
stock-BtS help/localization integration defect rather than an AdvCiv/SAS
regression.

RELATION TO F424
----------------
F424 / KI#747 is independent. F424 concerns AdvCiv's later
TXT_KEY_ESPIONAGE_CIVIC_RELIGION_MOD warning and an out-of-range %s2 argument in
four non-English translations. F426 concerns the older primary mission-effect
line and reuse of a civic-specific text key for a ReligionTypes effect. Fixing
one does not repair the other.

REPAIR DIRECTION
----------------
Prefer a dedicated force-religion help key, e.g.
TXT_KEY_ESPIONAGE_HELP_SWITCH_RELIGION, and call it from the
getSwitchReligionCostFactor branch. Its translations should explicitly describe
a religion conversion rather than a civic/government-form switch.

A smaller alternative is to make the existing shared key semantically neutral
in every language and rename the placeholder concept, but the two runtime
branches already have separate mission/result localization families, so a
separate help key is clearer and less translation-fragile.

POST-F426 CONTROLS / CONTINUATION
--------------------------------
Before F426, the persistent-cache inventory was reconciled again:

* F405/F406 remain the two live underspecified debug caches already numbered.
* setPlotListHelp's static vectors are scratch storage and are explicitly
  cleared/rebuilt for the relevant call shapes.
* static define/info caches elsewhere in CvGameTextMgr are immutable settings or
  immutable XML identities for the lifetime of a loaded ruleset.
* getTechDataForAS's static localized help string is fixed-content UI text and no
  supported in-game language/ruleset mutation path was established.

The obvious happiness analogue of F420 was also explicitly disproved. Runtime
CvCity::happyLevel/unhappyLevel and TextMgr setHappyHelp/setAngerHelp both combine

    city.getExtraHappiness() + player.getExtraHappiness()

before sign splitting. The health mismatch does not have a parallel happiness
root.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F426 are the latest confirmed distinct Queue-003 roots.
F427 / provisional KI#750 UNUSED.
Latest physical checkpoint: C031-WIP127.
No repository source files modified.

NEXT
----
Continue from F427 unused. Stay in setEspionageCostHelp long enough to reconcile
its remaining paired mission-effect branches and its displayed modifier product
against CvPlayer::getEspionageMissionCost, then resume inherited wrong-sibling /
paired-branch, aggregate-vs-component, cached-vs-live and sentinel/index closure
sweeps. Search the reconciled album before promoting any candidate.

END C031-WIP127


===============================================================================
C031-WIP128 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION — F427 CONFIRMED
===============================================================================

STATUS
------
F420-F426 remain CONFIRMED.
F427 / provisional KI#750 is CONFIRMED.
F428 / provisional KI#751 is UNUSED.

-------------------------------------------------------------------------------
F427 / provisional KI#750 — SABOTAGE-IMPROVEMENT ESPIONAGE HELP OMITS THE ROUTE
TARGET THAT THE SAME MISSION ACCEPTS AND DESTROYS ON A ROUTE-ONLY PLOT
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + CvPlayer.cpp
Primary function: CvGameTextMgr::setEspionageCostHelp
Classification: stock-BtS espionage help/runtime branch omission retained by
K-Mod, Base AdvCiv 1.14 and current SAS; ordinary current gameplay.

ROOT
----
The runtime contract of a mission with isDestroyImprovement() is explicitly
"improvement OR route".

Current CvPlayer::getEspionageMissionBaseCost permits the mission on any non-city
plot where either component exists:

    if (pPlot->isImproved() || pPlot->isRoute())
        iMissionCost = ...;

Current CvPlayer::doEspionageMission mirrors that exactly:

    if (pPlot->isImproved())
    {
        ... destroy/pillage improvement ...
    }
    else if (pPlot->isRoute())
    {
        ... report the route description ...
        pPlot->setRouteType(NO_ROUTE);
    }

But CvGameTextMgr::setEspionageCostHelp only implements the first half:

    if (kMission.isDestroyImprovement() && pPlot != NULL && pPlot->isImproved())
    {
        append TXT_KEY_ESPIONAGE_HELP_DESTROY_IMPROVEMENT(improvementName);
    }

There is no else-if route branch. Consequently the exact state in which runtime
will destroy a route because no improvement exists receives no target-effect
line in the detailed cost help.

CURRENT SAS REACHABILITY
------------------------
Current XML retains:

    ESPIONAGEMISSION_DESTROY_IMPROVEMENT
        iCost = 75
        bIsTwoPhases = 0
        bTargetsCity = 0
        bSelectPlot = 1
        TechPrereq = NONE
        bDestroyImprovement = 1

Current route XML retains ordinary ROUTE_ROAD and ROUTE_RAILROAD.

For a Spy on a legal foreign non-city tile that has a road/railroad but no
improvement, getEspionageMissionBaseCost returns a valid positive cost because
isRoute() is true. The first espionage popup therefore includes the ordinary
one-phase mission button and assigns WIDGET_HELP_ESPIONAGE_COST to it. Hovering
that button calls setEspionageCostHelp with the Spy's current plot.

Because pPlot->isImproved() is false, the specific sabotage line is skipped.
The rest of the help (base cost and modifiers) can still appear, but it never
names the Road/Railroad that execution is about to remove. Selecting the mission
then enters doEspionageMission's route branch and deletes that route.

This is therefore not an unsupported XML combination or a theoretical
CvRouteInfo capability. A route-only target is exactly one of the two plot states
accepted by the mission's normal runtime cost gate.

ANCESTRY
--------
The Civ4CE BtS reference already has the mismatch:

* getEspionageMissionBaseCost accepts
  `getImprovementType() != NO_IMPROVEMENT || getRouteType() != NO_ROUTE`;
* doEspionageMission destroys the improvement first and otherwise the route;
* setEspionageCostHelp only emits the specific line when
  `getImprovementType() != NO_IMPROVEMENT`.

K-Mod retains all three parts. Base AdvCiv modernizes the predicates to
isImproved()/isRoute() but retains the missing help sibling. Current SAS retains
that implementation. F427 is therefore inherited from stock BtS.

REPAIR DIRECTION
----------------
Mirror the runtime target selection in help. For example:

    if (kMission.isDestroyImprovement() && pPlot != NULL)
    {
        if (pPlot->isImproved())
            append sabotage-help with ImprovementInfo text key;
        else if (pPlot->isRoute())
            append sabotage-help with RouteInfo text key;
    }

The existing TXT_KEY_ESPIONAGE_HELP_DESTROY_IMPROVEMENT wording is "Sabotage
%s1..." in English and can accept a route description; a dedicated route key
would also be reasonable if any translation needs a different noun contract.

RELATION TO F426/F424
---------------------
F427 is a target-branch omission for the Sabotage mission. It is unrelated to
F426's force-religion/civic text-key reuse and F424's AdvCiv warning-placeholder
index bug. Each requires a different repair.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F427 are the latest confirmed distinct Queue-003 roots.
F428 / provisional KI#751 UNUSED.
Latest physical checkpoint: C031-WIP128.
No repository source files modified.

NEXT
----
Continue from F428 unused through the remaining setEspionageCostHelp mission
branches. Reconcile each effect line against getEspionageMissionBaseCost and
doEspionageMission, including speed scaling, target selection and one-/two-phase
popup data. Then resume whole-file inherited paired-branch, aggregate/component,
cache/live and sentinel/index closure passes.

END C031-WIP128


================================================================================
C031-WIP129 — POST-ESPIONAGE WHOLE-FILE CLOSURE / F428 STILL UNUSED
================================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F420-F427 remain the latest confirmed distinct Queue-003 roots.
F428 / provisional KI#751 remains UNUSED.
No repository source files modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Continue conservatively from C031-WIP128 after F427, finish the remaining
`setEspionageCostHelp` effect/runtime reconciliation, and record the subsequent
stateful whole-file controls before deeper inherited paired-branch / cache-live /
sentinel-index passes. This checkpoint deliberately preserves several plausible
but rejected candidates so they are not repeatedly rediscovered or promoted
without current-data/caller proof.

POST-F427 ESPIONAGE CLOSURE
---------------------------
The remaining active espionage cost-modifier arithmetic is guarded by exact
internal assertions against `getEspionageMissionCostModifier` and the final
mission cost. The remaining review therefore concentrated on effect-description
branches, where F426/F427 had both lived.

Destroy Production at zero accumulated production:
* `getEspionageMissionBaseCost` can technically return a zero base/final cost
  when the target city has zero current production, and `canDoEspionageMission`
  is permissive enough that the mission predicate itself can still succeed.
* The human first-phase espionage popup only creates one-phase mission buttons
  when `iCost > 0`, so the zero-cost/no-effect case is not exposed as a normal
  human action.
* `CvPlayerAI::AI_espionageVal` adds value for this mission only when the target
  city's production is greater than zero, so the normal AI selection path does
  not choose the zero-production no-op either.
Classification: harmless/current-caller structural looseness, not F428.

Insert Culture fractional precision:
* Runtime computes culture insertion in hundredths and can therefore apply a
  fractional remainder below one culture.
* Both TextMgr's displayed whole-culture amount and the mission base-cost
  calculation apply the same `/100` truncation, so the displayed integer and
  charged cost remain mutually consistent.
* Execution can still place a sub-1-culture remainder on plots, but that is UI
  precision rather than a contradictory gameplay contract.
Classification: low-value precision control, not F428.

Investigate City / counterespionage / return-to-capital:
* Current speed scaling leaves the active counterespionage duration positive.
* The help's return/no-return wording mirrors the actual post-mission relocation
  logic, including the existence of a capital.
* Passive missions do not use the active espionage-cost popup widget.
No further live root found.

Conclusion: the active `setEspionageCostHelp` subsystem is provisionally
saturated after F426 and F427. F428 remains unused.

WHOLE-FILE STATEFUL CONTROLS
----------------------------
`getWarWearinessString`:
* Reconciled target-specific and total values against the player/team war-
  weariness calculations.
* The helper intentionally reports natural/pre-city-modifier war-weariness
  units. Ordinary per-opponent integer decomposition can round independently,
  but no hard total/effect contradiction was established.
Classification: closed control.

`buildProcessInfoString`:
* `CvPlayer::canMaintain` is the same simple team-tech prerequisite described by
  the help path.
Classification: closed control.

`buildFoundCorporationString` / old tech-founded corporations:
* The apparently surprising "first to discover incorporates" behavior is real
  runtime behavior for corporations with a TechPrereq: `CvTeam::setHasTech`
  contains the first-discovery auto-founding path.
* Current SAS sets every corporation `TechPrereq=NONE`, so that old path/help is
  dormant rather than false.
Classification: dormant historical capability, not F428.

Population-unhealth modifier <= -100 wording:
* K-Mod's own caveat remains structurally valid if a -100 source could be offset
  by a positive population-unhealth modifier while help claims absolute
  immunity.
* Current SAS has National Park at -100 and Public Transportation at -20, with
  no positive civic/building population-unhealth modifier able to offset the
  -100 case.
Classification: current-data masked inherited hazard, not F428.

`setTerrainHelp` city-founding exception separator:
The code appends `TXT_KEY_OR` immediately after a coastal founding exception,
before testing whether a fresh-water exception actually exists. A terrain with:

    bFound = 0
    bFoundCoast = 1
    bFoundFreshWater = 0

would therefore end its founding help with a dangling "or".

Current SAS terrain XML activates no such exception: terrains with bFound=0
have both bFoundCoast=0 and bFoundFreshWater=0. Thus this is a genuine latent
branch-grammar defect but current-data dormant.
Classification: control, not F428.

Improvement/bonus connector multiplicity:
* The plot-help logic has comments/structure that are easiest to reason about
  when a bonus has a single connecting improvement.
* Current Oil is the notable multi-connector bonus (Well + Offshore Platform),
  but their land/water validity makes the alternatives mutually exclusive on a
  particular plot.
Classification: current-data safe.

BUILDING REQUIREMENT RECONCILIATION
-----------------------------------
`buildBuildingRequiresString` does not explicitly diagnose every predicate in
`CvCity::canConstruct`, notably `PrereqReligion`, `PrereqCorporation`,
`bPrereqReligion`, competing-corporation headquarters and government-center
exclusivity.

Current-data/caller proof prevents promotion:
* Current SAS has 28 live `PrereqReligion` buildings (the seven religion sets of
  temple/cathedral/monastery/shrine, including Paganism).
* However, the specific-religion rejection in `CvCity::canConstruct` occurs
  before its `bTestVisible` early return. `CvMainInterface.py` adds a production
  button only when `city.canConstruct(..., bTestVisible=true)` succeeds.
  Therefore a religious building whose religion is absent is hidden, not shown
  as a disabled production button that relies on this helper to explain why.
* Current `PrereqCorporation` and `bPrereqReligion` building fields are unused.
* Palace/Forbidden Palace use the live GovernmentCenter flag, but the
  already-government-center city rejection is likewise before bTestVisible and
  therefore filtered from the normal build palette.
* Current NumCitiesPrereq, UnitLevelPrereq, NumTeamsPrereq and VictoryPrereq are
  unused in building XML; the helper/runtime arithmetic for the generic fields
  was nevertheless checked.

The omission may still matter for generic Civilopedia/API completeness or
unusual mission/scenario data, but ordinary current production-button
reachability is not established. Do not promote as F428 without a stronger
caller/data state.

WORLD-SIZE PREREQUISITE RANGE CONTROL
-------------------------------------
The Civilopedia building-class prerequisite range uses the raw prerequisite as
its low end and the largest current world-size modifier for its high end.
Current SAS world-size BuildingClassPrereqModifier values are monotonic across
its extended sizes, so the resulting displayed range is valid. The analogous
conscription world-size modifier sequence is also monotonic.
Classification: closed control.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F427 are the latest confirmed distinct Queue-003 roots.
F428 / provisional KI#751 UNUSED.
Latest physical checkpoint: C031-WIP129.
No repository source files modified.

NEXT
----
Continue from F428 unused. Prioritize inherited live gameplay/help contracts:
paired good/bad or positive/negative branches, aggregate-vs-component
calculations, live runtime state vs cached/display state, and sentinel/index
asymmetries. Recheck the album before any promotion. Low-value formatting or
currently masked XML capabilities remain controls unless ordinary current-SAS
reachability is proven.

END C031-WIP129


================================================================================
C031-WIP130 — AGGREGATE FINANCE RECONCILIATION + F428 CONFIRMED
================================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F420-F428 are the latest confirmed distinct Queue-003 roots.
F429 / provisional KI#752 is UNUSED.
No repository source files were modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Continue from C031-WIP129 / F428 unused through the aggregate-vs-component
recomputation pass. This tranche broadens the already-confirmed F400 Finance
rounding root with two sibling live manifestations instead of manufacturing new
numbers from the same arithmetic contract, then establishes one genuinely
independent inherited free-specialist help omission as F428.

-------------------------------------------------------------------------------
F400 / provisional KI#723 BROADENED — THE FINANCE INFLATION BREAKDOWN FAMILY
CAN ROUND COMPONENTS INDEPENDENTLY FROM ITS AGGREGATE TOTAL
-------------------------------------------------------------------------------

ORIGINAL ROOT RETAINED
----------------------
F400 was originally established in `buildFinanceCityMaintString`: named
maintenance components plus the residual number-of-cities component are made to
sum to a round-to-nearest inflated aggregate, while the line's displayed total
uses truncating integer division just like the engine. At 50% inflation an exact
current construction yields Number of Cities = 17 gold and Total = 16 gold.

The new whole-Finance aggregate pass shows that the same K-Mod inflation-help
integration pattern has two additional ordinary live manifestations. Keep these
under F400 rather than assigning new roots: in each case inflation is applied
and rounded separately to displayed components while the total is obtained by
applying inflation once to the already-summed pre-inflation runtime value.
Because rounding does not distribute over addition, the listed parts need not
sum to the listed total.

A. UNIT COST
------------
`buildFinanceUnitCostString` obtains the pre-inflation runtime decomposition:

    iCost = iUnitCost + iMilitaryCost + iExtraCost (+ residual handicap, if any)

and then independently does:

    iCost         = round(iCost * inflationFactor / 100)
    iUnitCost     = round(iUnitCost * inflationFactor / 100)
    iMilitaryCost = round(iMilitaryCost * inflationFactor / 100)

For an ordinary human current-SAS state the residual handicap component is zero,
and current Unit XML has `iExtraCost=0`, so ordinary unit and military support
alone are sufficient to expose non-additivity.

Exact current-data construction:

    game speed   = Normal
    world size   = Standard
    handicap     = Noble
    total pop    = 10
    civic        = Pacifism
    total units  = 13
    military     = 4
    inflation    = 50%

Current inherited/base support plus Noble gives:

    total-unit free allowance
      = Noble FreeUnits 6
      + INITIAL_BASE_FREE_UNITS 4
      + floor(10 * 24%)
      = 12

    military free allowance
      = INITIAL_BASE_FREE_MILITARY_UNITS 2
      + floor(10 * 12%)
      = 3

Normal/Standard/Noble gives a 100% ordinary unit-cost multiplier. Thus:

    paid ordinary units = 13 - 12 = 1
    iUnitCost            = 1 gold

Current Pacifism has `iGoldPerMilitaryUnit=100`, i.e. +1 gold per paid military
unit under the K-Mod percentage representation:

    paid military units = 4 - 3 = 1
    iMilitaryCost        = 1 gold

At 50% inflation the helper therefore displays:

    ordinary unit component = round(1 * 1.5) = 2
    military component      = round(1 * 1.5) = 2
    total                   = round(2 * 1.5) = 3

So one ordinary Finance hover can list 4 gold of components and then state a
3-gold total.

B. CIVIC UPKEEP
---------------
`buildFinanceCivicUpkeepString` applies the same pattern:

    each row = round(getSingleCivicUpkeep(civic) * inflationFactor / 100)
    total    = round(getCivicUpkeep() * inflationFactor / 100)

`getCivicUpkeep()` is exactly the sum of the five `getSingleCivicUpkeep` values
before inflation. Therefore the separately rounded rows are not guaranteed to
sum to the rounded total.

A small fully current construction on Noble at 50% inflation is one city with
population 15 using:

    Government: Police State       (HIGH upkeep)
    Legal:      Free Speech        (LOW upkeep)
    Labor:      Slavery            (LOW upkeep)
    Economy:    State Property     (LOW upkeep)
    Religion:   Organized Religion (HIGH upkeep)

With the inherited current upkeep offsets (-8 population, -1 city), the five
pre-inflation single-civic values are exactly:

    [1, 0, 0, 0, 1] gold

The helper displays the two nonzero rows as 2 gold each at 50% inflation, while
its rounded aggregate is only 3 gold:

    sum of displayed rows = 4
    displayed total       = 3

ANCESTRY / ROOT NORMALIZATION
-----------------------------
K-Mod already contains all three Finance inflation patterns: it introduced
ROUND_DIVIDE inflation treatment separately in Unit Cost, City Maintenance and
Civic Upkeep. Base AdvCiv retains them, replacing ROUND_DIVIDE with its corrected
`intdiv::round` and improving city-maintenance intermediate precision without
changing the aggregate/component contract. Current SAS retains the behavior.

These are not three independent gameplay rules. They are sibling manifestations
of the same Finance-breakdown presentation architecture: separately rounding
inflated parts while presenting an independently rounded/truncated aggregate.
Keep one F400 / KI#723 root and broaden its repair surface.

REPAIR DIRECTION (BROADENED)
----------------------------
Choose one additive display contract for every Finance breakdown.

For exact internal consistency, compute the authoritative displayed total using
the same rule as runtime, then either:

* derive one residual displayed component after the other components have been
  rounded; or
* preserve enough fractional precision for all components and perform a
  deterministic largest-remainder/allocation step so displayed integer parts
  sum exactly to the runtime total.

Do not independently round every component and also independently round the
aggregate unless the UI explicitly labels the components as approximate.

-------------------------------------------------------------------------------
F428 / provisional KI#751 — COLLAPSED FREE-SPECIALIST HELP TOTALS YIELD,
COMMERCE AND XP BY COUNT BUT OMITS THE SPECIALISTS' GREAT-PERSON POINTS
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + CvDLLWidgetData.cpp +
Assets/Python/Screens/CvMainInterface.py + current specialist/building XML
Primary function: CvGameTextMgr::parseFreeSpecialistHelp
Classification: stock-BtS city-help omission retained by K-Mod, Base AdvCiv 1.14
and current SAS; ordinary current SAS can reach the collapsed UI from buildings
alone.

ROOT
----
`parseFreeSpecialistHelp` is the aggregate helper used when the city UI needs to
summarize its specific free specialists. For every SpecialistTypes entry with a
positive `getFreeSpecialistCount`, it prints a row headed by the specialist name
and count, then multiplies that count into:

* specialist yields;
* specialist commerce;
* specialist free unit experience.

It never displays or multiplies:

    CvSpecialistInfo::getGreatPeopleRateChange()

Yet ordinary `CvCity::processSpecialist` applies that exact field to both the
specialist-specific Great Person unit rate and the city's base Great Person rate
for every added free specialist. The omitted effect is gameplay state, not
pedia flavor.

The contrast with `parseSpecialistHelp` is direct. The ordinary one-specialist
helper explicitly prints `TXT_KEY_SPECIALIST_BIRTH_RATE` whenever
`getGreatPeopleRateChange()!=0`; the aggregate free-specialist helper simply
stops after yield/commerce/experience.

WHY THE CURRENT UI MAKES THIS A REAL TOTALING ERROR
---------------------------------------------------
Current `CvMainInterface.py` uses `MAX_CITIZEN_BUTTONS = 8`.

At eight or fewer specific free specialists, it can draw one icon per free
specialist and passes `WIDGET_FREE_CITIZEN(..., data2=-1)`. Hovering that icon
uses the ordinary one-specialist help, which does contain the specialist's GPP.

Above eight, `updateFreeSpecialistButtons` switches representation: it draws one
button per specialist TYPE with `data2=1`, regardless of how many free
specialists of that type are present. `CvDLLWidgetData::parseFreeCitizenHelp`
then appends `parseFreeSpecialistHelp` after the ordinary specialist header.
The aggregate rows are therefore the UI's mechanism for explaining the hidden
multiplicity. Yields, commerce and XP are multiplied by the displayed count;
GPP are not.

CURRENT-SAS BUILDING-ONLY REACHABILITY
--------------------------------------
Current XML can exceed the collapse threshold in one city without events,
debugging or unsupported data. The following currently non-obsolete buildings
can coexist and together supply ten *specific* free specialists:

    Custom House        +1 Merchant
    Industrial Park     +2 Engineers
    Laboratory          +1 Scientist
    Natya Shastra       +1 Artist
    Oxford University   +2 Scientists
    Statue of Liberty   +1 Merchant
    Cristo Redentor     +1 Priest
    Versailles          +1 Spy
                        ----------------
                        10 free specialists

All six ordinary specialist types involved currently have
`iGreatPeopleRateChange=3`:
Priest, Artist, Scientist, Merchant, Engineer and Spy.

Those ten free specialists therefore contribute exactly:

    10 * 3 = 30 base GPP/turn

through `CvCity::processSpecialist`.

For example, once collapsed, an Engineer button represents two free Engineers.
The ordinary header describes a single Engineer and can show +3 GPP. The
aggregate row says `Engineer (2)` and correctly doubles its yields, but omits the
corresponding +6 base GPP entirely. Across the ten-specialist construction the
aggregate section omits all 30 base GPP while totaling the other effects.

ANCESTRY
--------
Civ4CE stock BtS already contains `parseFreeSpecialistHelp` with the same
omission: it totals yield, commerce and specialist experience but never calls
`getGreatPeopleRateChange`.

K-Mod retains that structure (while fixing a neighboring separator typo).
Base AdvCiv 1.14 retains it, and current SAS retains it. Current SAS's richer
specific-free-specialist building data makes the >8 collapsed state especially
straightforward to reach, but the root is inherited stock BtS.

REPAIR DIRECTION
----------------
Include the count-scaled GPP contribution in each aggregate specialist row,
parallel to yield/commerce/XP, e.g. using the existing GREAT_PEOPLE_CHAR or an
appropriate short birth-rate text:

    iNumSpecialists * GC.getInfo(eSpecialist).getGreatPeopleRateChange()

This should describe the specialist contribution at the same base-effect level
as the ordinary `parseSpecialistHelp`; city-wide Great Person modifiers belong
in the separate Great People rate breakdown and need not be duplicated here.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F428 are the latest confirmed distinct Queue-003 roots.
F429 / provisional KI#752 UNUSED.
Latest physical checkpoint: C031-WIP130.
No repository source files modified.

NEXT
----
Continue from F429 unused. Resume the low-mention inherited helper reconciliation
with emphasis on helpers whose compact/grouped representation replaces multiple
ordinary UI items, then return to aggregate-vs-component, current-state vs
recomputed-state, paired-branch and sentinel/index asymmetry closure. Search the
reconciled album before promotion.

END C031-WIP130


===============================================================================
C031-WIP131 APPEND - F429 CONFIRMED / CORPORATION FORCE-PREVIEW RECONCILIATION
===============================================================================

-------------------------------------------------------------------------------
F429 / provisional KI#752 - FORCE-CORPORATION HELP COUNTS A COMPETING
CORPORATION'S PRODUCED RESOURCE EVEN THOUGH A SUCCESSFUL SPREAD REMOVES THAT
CORPORATION BEFORE THE NEW CORPORATION'S EFFECTS ARE CALCULATED
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvGameTextMgr.cpp + CvCity.cpp + CvUnit.cpp +
current Corporation XML
Primary function: CvGameTextMgr::setCorporationHelpCity
Classification: inherited stock-BtS hypothetical-preview algorithm made
normally wrong by K-Mod's corporation-resource rework; retained by Base AdvCiv
1.14 and current AdvCiv-SAS.

ROOT
----
The corporation spread mission asks:

    setCorporationHelpCity(..., bCityScreen=false, bForceCorporation=true)

for the prospective effects of successfully adding the corporation to the
mission city.

The helper first counts every CURRENT prerequisite resource through:

    iNumResources += pCity->getNumBonuses(kCorporation.getPrereqBonus(i));

and then uses that fixed current-state count for the incoming corporation's
prospective yields, commerce and maintenance.

However a successful corporation spread does not simply add the new corporation
on top of the current city state. `CvCity::setHasCorporation(..., true, ...)`
first removes every CURRENT corporation that competes with the incoming one.
Those recursive removals immediately call `updateCorporation()`, whose
`updateCorporationBonus()` rebuilds corporation-produced resources. Thus the
resource set on which the new corporation actually operates can differ from the
resource set that `setCorporationHelpCity` counted before the replacement.

CURRENT-SAS CONCRETE FAILURE - ALUMINUM CO -> MINING INC
--------------------------------------------------------
Current corporation data gives a direct ordinary case.

Aluminum Co (`CORPORATION_6`):

    prerequisite resources: Coal
    produced resource:       Aluminum

Mining Inc (`CORPORATION_5`):

    prerequisite resources: Coal, Iron, Copper, Uranium, Aluminum
    production output:       100 hundredths per prerequisite resource

The corporations compete because both consume Coal.

Take a normal non-HQ city with:

    one connected Coal;
    no natural/imported Aluminum;
    Aluminum Co active;
    the tech state needed for corporation-produced Aluminum to be usable;
    no civic/policy disabling either corporation.

`updateCorporationBonus` gives the active Aluminum Co one corporation-produced
Aluminum from that Coal. Therefore immediately before the Mining Inc spread:

    getNumBonuses(Coal)      = 1
    getNumBonuses(Aluminum)  = 1   // supplied by Aluminum Co

`setCorporationHelpCity(... Mining Inc ..., bForceCorporation=true)` therefore
counts two Mining Inc resources.

On current Standard world size, CorporationMaintenancePercent=100 and Mining
Inc produces 100 production-hundredths per qualifying resource, so the
prospective mission help reports:

    +2 production

and calculates Mining Inc maintenance from two qualifying resources.

But on a SUCCESSFUL spread, `CvCity::setHasCorporation(Mining Inc, true, ...)`
first sees Aluminum Co as competing and removes it. That removal immediately
runs `updateCorporationBonus`; Aluminum Co no longer exists in the city, so its
corporation-produced Aluminum disappears. Mining Inc is then installed and its
own corporation update sees only:

    Coal = 1
    Aluminum = 0

The actual incoming Mining Inc contribution is therefore only:

    +1 production

and its actual corporation maintenance is correspondingly based on one resource,
not the two counted by the pre-spread help.

This is not merely failure to itemize the LOSING corporation's effects. The
numeric effects attributed to the INCOMING corporation itself are calculated
from a resource that the incoming corporation's own successful replacement
removes before runtime evaluates those effects.

NORMAL CALLER / LEGALITY
------------------------
`CvDLLWidgetData`'s spread-corporation mission help calls
`setCorporationHelpCity(..., false, true)` for the mission city.

`CvUnit::canSpreadCorporation` permits the state above:
- the incoming corporation is active for both players;
- the city does not already have it;
- the city is not disordered;
- the corporation has at least one prerequisite bonus (Coal suffices);
- the target is not the headquarters of a competing corporation;
- ordinary territory/gold conditions hold.

The mission can fail probabilistically, but the force-help is explicitly the
prospective successful-spread effect. Whenever the supported state succeeds, the
reported two-resource Mining Inc result and the actual one-resource result
diverge deterministically.

ANCESTRY / ACTIVATION
---------------------
Stock BtS/Civ4CE already implements the force-corporation helper by counting the
city's CURRENT prerequisite bonuses before any replacement simulation. It also
already removes competing corporations during successful spread.

Stock corporation data, however, masks this particular resource-dependency
failure: stock Mining Inc consumes Coal/Iron/Copper/Gold/Silver and therefore
does not consume the Aluminum produced by Aluminum Co.

K-Mod changes Mining Inc's Gold/Silver prerequisites to Uranium/Aluminum (the
XML comment in Base AdvCiv explicitly records: `K-Mod: Gold, Silver replaced
with Uranium, Aluminum`) while Aluminum Co continues to consume Coal and produce
Aluminum. K-Mod retains the inherited force-preview algorithm unchanged, making
the previously latent replacement-state mismatch normally reachable.

Base AdvCiv 1.14 retains both K-Mod's corporation inputs and the inherited
preview algorithm. Current SAS retains them too.

RELATION TO EXISTING CORPORATION FINDINGS
-----------------------------------------
This is independent of:

* F411/KI#734: ordinary corporation city hover ignores disorder/no-maintenance
  state. F429 occurs in a normal non-disordered prospective spread and concerns
  replacement-state simulation.
* F421/KI#744: cached corporation maintenance embeds stale inverse inflation.
  F429 is wrong immediately even with a freshly updated cache because the
  preview uses a resource that successful execution removes.

REPAIR DIRECTION
----------------
For `bForceCorporation`, derive the incoming corporation's effects from the
POST-SUCCESS hypothetical corporation/resource state rather than the current
city resource count.

A robust repair should account for the full corporation-produced-resource
fixpoint after every competing corporation that `setHasCorporation` would remove
has been removed. Merely subtracting one hardcoded Aluminum source is too narrow;
corporation-produced resources can form dependency chains and future data can
create additional replacement interactions.

Possible conservative approaches:
- factor a side-effect-free helper that computes corporation-produced resources
  for a hypothetical corporation membership set and use it for preview;
- or suppress exact force-preview yield/commerce/maintenance numbers when a
  competing-corporation removal changes any prerequisite resource, while still
  showing the existing `will eliminate corporation` warning.

Do not mutate the real city merely to obtain help text.

-------------------------------------------------------------------------------
POST-F429 CONTROL - FORCED RELIGION COMMERCE MAXIMUM
-------------------------------------------------------------------------------
The neighboring `setReligionHelpCity(..., bForceReligion=true)` path initially
looked similar because its maximum-commerce search evaluates currently absent
religions without the force flag. Current SAS data closes this candidate:
all seven religions currently provide the same ordinary +1 culture religion
commerce. If the city already has a religion, forcing another equal-value
religion does not increase the max-based runtime religion-commerce total; if the
city has no religion, the helper's `eMaxReligion==NO_RELIGION && bForceReligion`
path displays the prospective +1 correctly. Keep this as a checked control, not
F430.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
32/169 unique files complete.
F420-F429 are the latest confirmed distinct Queue-003 roots.
F430 / provisional KI#753 UNUSED.
Latest physical checkpoint: C031-WIP131.
No repository source files modified.

NEXT
----
Continue from F430 unused. Finish the compact/grouped replacement-helper pass,
then return to aggregate-vs-component, current-state-vs-hypothetical-state,
paired-branch and sentinel/index asymmetry closure. Reconcile against F411/F421
and the rest of the album before promoting any neighboring corporation-help
symptom.

END C031-WIP131


===============================================================================
C031-WIP132 APPEND - F430 CONFIRMED / QUEUE-012 CLOSURE CORRECTION DISCOVERED
DURING QUEUE-003 IMPROVEMENT-HELP CONTRACT TRACE
===============================================================================

-------------------------------------------------------------------------------
POST-WIP131 DUPLICATE CONTROL - SETFOUND COST / WLTKD MAINTENANCE
-------------------------------------------------------------------------------
The prospective founding-cost pass rediscovered that `setFoundCostHelp` excludes
cities in disorder from its current-maintenance comparison but not cities under
We Love the King Day, despite both states being maintenance-free through
`CvCity::isNoMaintenance()`.

This is NOT a new root. The reconciled album already owns the exact defect as
F392 / provisional KI#715, including current WLTKD reachability, founding-time
maintenance refresh, persistence of the celebration through founding, ancestry
and repair direction. Keep this rediscovery as a positive duplicate-control and
do not assign F430 to it.

-------------------------------------------------------------------------------
F430 / provisional KI#753 - ROUTE-ONLY BUILD ACTION HELP OMITS THE YIELD CHANGE
THAT THE NEW ROUTE CAUSES THROUGH AN EXISTING IMPROVEMENT
-------------------------------------------------------------------------------
Status: CONFIRMED
Root file: CvGameCoreDLL/CvDLLWidgetData.cpp
Cross-files: CvPlot.cpp + current Build/Improvement XML
Audit accounting: Queue-012 closure correction discovered while Queue 003 traced
CvGameTextMgr improvement/build-help contracts. Queue 012 does not require a
full re-audit; amend its closure with this missed inherited root.
Classification: inherited stock-BtS action-help defect retained by K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
----
`CvDLLWidgetData::parseActionHelp_Mission`, MISSION_BUILD, previews build yield
changes by looping yields and calculating a prospective improvement delta only
inside:

    if (eImprovement != NO_IMPROVEMENT)
    {
        iYield += kMissionPlot.calculateImprovementYieldChange(eImprovement, ...);
        if (kMissionPlot.isImproved())
            iYield -= kMissionPlot.calculateImprovementYieldChange(
                    kMissionPlot.getImprovementType(), ...);
    }

The build's route is read separately into `eRoute`, but it is used only by the
later route-prerequisite diagnostics. It never participates in the yield-delta
preview.

That is incomplete because an existing improvement can itself have
`RouteYieldChanges`. A route-only build can therefore change plot yield even
when it does not replace the improvement.

CURRENT-SAS CONCRETE FAILURE - RAILROAD ON MINE / LUMBERMILL
-------------------------------------------------------------
Current `BUILD_RAILROAD` is an ordinary route-only Worker build:

    PrereqTech       = TECH_STEAM_POWER
    ImprovementType  = NONE
    RouteType        = ROUTE_RAILROAD

Current Mine and Lumbermill each contain:

    RouteType ROUTE_RAILROAD
    production yield change +1

`CvPlot::calculateImprovementYieldChange` explicitly obtains the plot's
current/revealed route and adds:

    kImpr.getRouteYieldChanges(eRoute, eYield)

Thus, once a Railroad exists on a Mine or Lumbermill, that improvement's actual
production contribution is one hammer larger than before.

Normal execution confirms the state transition. `CvUnit::build` completes
through `CvPlot::changeBuildProgress`; when a build has a route, completion calls:

    setRouteType(kBuild.getRoute())

so a successful `BUILD_RAILROAD` immediately changes the plot to Railroad and
therefore activates the Mine/Lumbermill +1 production RouteYieldChange.

But for the same Railroad action hover:

    eImprovement == NO_IMPROVEMENT

so the action-help yield loop never calls `calculateImprovementYieldChange` at
all. With no feature-removal yield in the example, `iYield` remains zero and no
`, +1 production` effect is appended.

A Worker can therefore hover a fully legal Railroad build over an existing Mine
or Lumbermill, receive no yield-change preview, complete the build and gain +1
production on the tile.

This is not merely a missing direct RouteInfo yield. The live current effect is
stored on the EXISTING IMPROVEMENT's `RouteYieldChanges`, and the normal plot
yield path applies it through `calculateImprovementYieldChange`.

ANCESTRY
--------
Stock BtS/Civ4CE already contains the same MISSION_BUILD preview structure:
prospective yield calculation is conditional on the build having an
ImprovementType, while the route is not incorporated into the yield delta.

Stock BtS XML also already gives Mine and Lumbermill +1 production with
Railroad. Thus the defect is live in stock BtS data, not merely activated by a
later K-Mod/AdvCiv/SAS data change.

K-Mod retains both the action-help structure and the Railroad
RouteYieldChanges. Base AdvCiv 1.14 retains both. Current SAS retains both while
moving Railroad construction to Steam Power; that tech-tree change affects when
the ordinary state is reached, not the root itself.

RELATION TO PRIOR QUEUE-012 COVERAGE
------------------------------------
Queue 012 previously checked several MISSION_BUILD contracts, including:
- feature-tech sentinel handling;
- MAX_INT/zero-work ETA reachability;
- worker-count ETA double-counting;
- build-button data domains.

The reconciled album contains no prior MISSION_BUILD route-dependent yield
preview root. Searches for Railroad/RouteYieldChanges/action-hover equivalents
also found no existing Known Issue owning this defect. F430 therefore amends the
Queue-012 closure rather than duplicating a prior root.

REPAIR DIRECTION
----------------
For MISSION_BUILD, calculate the yield delta from the full hypothetical
post-build plot state, not only from a prospective replacement improvement.

At minimum, when `eRoute != NO_ROUTE` and the plot already has an improvement,
include the change in that improvement's `RouteYieldChanges` between the current
route and `eRoute`. A general repair should also include direct RouteInfo yield
changes so future/current data do not require another special case.

Prefer one side-effect-free prospective-yield helper or a small calculation that
mirrors `CvPlot::calculateYield` under hypothetical improvement/route/feature
state, rather than independently reconstructing only whichever XML fields happen
to be nonzero today.

Do not mutate the real plot for help text.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 `CvGameCoreDLL/CvDLLWidgetData.cpp` remains broadly complete but its
closure is amended by cross-file F430 discovered during Queue 003.
32/169 unique files complete.
F420-F429 remain the latest Queue-003 roots; F430 is a distinct Queue-012 root
found during the Queue-003 contract trace.
F431 / provisional KI#754 UNUSED.
Latest physical checkpoint: C031-WIP132.
No repository source files modified.

NEXT
----
Resume Queue 003 from F431 unused. Continue the low-mention stateful-helper pass,
especially hypothetical post-action state, compact/grouped replacement helpers,
aggregate-vs-component arithmetic, paired positive/negative branches, and
live-vs-recomputed state. Search the reconciled album before every promotion.

END C031-WIP132


===============================================================================
C031-WIP133 APPEND - F431 CONFIRMED / ADVANCED-START PEACE COUNTDOWN
===============================================================================

-------------------------------------------------------------------------------
F431 / provisional KI#754 - ADVANCED-START TURN-TIMER STILL ANNOUNCES ZERO
TURNS OF FORCED PEACE AFTER ADVCIV HAS ALREADY EXPIRED THE SETUP PEACE DEALS
-------------------------------------------------------------------------------
Status: CONFIRMED
Root file: CvGameCoreDLL/CvGameTextMgr.cpp
Cross-files: CvGame.cpp + CvDeal.cpp
Classification: stock-BtS countdown condition made wrong by AdvCiv advc.004's
faster peace-deal expiry; retained by Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT
----
`CvGameTextMgr::getTurnTimerText` displays the Advanced Start universal-peace
countdown while:

    kGame.getElapsedGameTurns() <= PEACE_TREATY_LENGTH

and formats:

    PEACE_TREATY_LENGTH - getElapsedGameTurns()

Ordinary Advanced Start creates reciprocal `TRADE_PEACE_TREATY` deals between
eligible major players during game initialization. A peace treaty becomes
cancelable when:

    initialGameTurn + PEACE_TREATY_LENGTH - currentGameTurn <= 0

Current AdvCiv/SAS then deliberately calls `verifyDeals()` immediately AFTER:

    incrementGameTurn();
    incrementElapsedGameTurns();

with the explicit comment:

    Already done in doDeals, but that's before incrementing the turn counter.
    Want to kill peace treaties asap.

`CvDeal::verify` kills a peace deal once it is cancelable. Therefore, when the
new elapsed-turn count first equals `PEACE_TREATY_LENGTH`, those setup peace
treaties have already reached zero remaining turns and have already been killed
before the new turn begins.

CURRENT-SAS CONCRETE TIMELINE
-----------------------------
With the normal 10-turn peace-treaty length and an initial setup deal created at
game turn 0:

    elapsed/game turn 9:  turnsToCancel = 1; treaty still enforces peace.

At the round boundary:

    incrementGameTurn()        -> 10
    incrementElapsedGameTurns()-> 10
    verifyDeals()              -> treaty is cancelable (10 + 0 - 10 == 0)
                                  and is killed immediately.

The new turn therefore begins with the Advanced-Start peace treaty gone.

But `getTurnTimerText` still evaluates:

    10 <= 10   // true

and prints `TXT_KEY_MISC_ADVANCED_START_PEACE_REMAINING` with value 0.

Thus the first turn on which the setup treaty no longer protects the players is
still labelled as having `0 turns` of Advanced Start peace remaining. Because
this branch precedes the normal victory/max-turn countdown branches, it can also
suppress a real competing countdown for that turn.

REACHABILITY
------------
This is the ordinary non-SPAH Advanced Start setup path, not a scenario-only or
hidden-option state:
- `CvGame::initDiplomacy` creates the reciprocal peace-treaty deals whenever
  GAMEOPTION_ADVANCED_START is active and GAMEOPTION_SPAH is not;
- `getTurnTimerText` is the normal interface turn/countdown text path;
- current SAS retains PEACE_TREATY_LENGTH and the inherited countdown display.

The `GAMEOPTION_ALWAYS_WAR` guard is unrelated to the demonstrated ordinary
Advanced Start state.

ANCESTRY
--------
Stock BtS/Civ4CE and K-Mod already use the `<= PEACE_TREATY_LENGTH` countdown.
They also create the initial Advanced Start peace treaties.

However stock BtS/K-Mod do NOT perform the second post-increment `verifyDeals`
call. Their deal verification occurs before the turn counter increments, so a
peace treaty reaching zero can remain present through the newly incremented turn
until the next verification boundary. The inherited zero-count display therefore
matched that older delayed-expiry lifecycle much more closely.

AdvCiv practical 1363 (`advc.004`) added:

    incrementGameTurn();
    incrementElapsedGameTurns();
    verifyDeals();

specifically to "kill peace treaties asap." The inherited TextMgr `<=` condition
was not changed. Base AdvCiv 1.14 retains both the faster expiry and the old
countdown predicate; current SAS retains both.

This is therefore best classified as an AdvCiv integration regression between a
runtime lifecycle fix and unchanged stock UI timing, not as a pure stock-BtS
TextMgr typo.

DUPLICATE CHECK
---------------
The reconciled album and current Known-Issue documentation contain no existing
Advanced Start peace-countdown/off-by-one root. Existing Advanced Start findings
in Queue 003 concern zero-cost visibility (F417) and unrelated setup/UI
contracts. F431 is independent.

REPAIR DIRECTION
----------------
For the current lifecycle, the minimal repair is to display the elapsed-based
setup countdown only while:

    getElapsedGameTurns() < PEACE_TREATY_LENGTH

so the branch disappears as soon as post-increment `verifyDeals` expires the
setup treaties.

A stronger future-proof implementation would derive the message from the actual
surviving Advanced-Start peace-deal / force-peace state and its real remaining
turns rather than duplicating lifetime arithmetic from elapsed game turns. That
would remain correct if start-turn/deal initialization semantics change again.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F431 / provisional KI#754 CONFIRMED.
F432 / provisional KI#755 UNUSED.
Latest physical checkpoint: C031-WIP133.
No repository source files modified.

NEXT
----
Continue Queue 003 from F432 unused. Resume low-mention stateful helpers and
current-state-vs-runtime lifecycle checks; include timer/interface lifecycle,
compact/grouped summaries and action/state previews, with album duplicate checks
before promotion.

END C031-WIP133


===============================================================================
C031-WIP134 APPEND - GREAT-PEOPLE CONTROL CLOSED / F432 CONFIRMED
===============================================================================

-------------------------------------------------------------------------------
POST-WIP133 CONTROL - GREAT-PERSON BASE-RATE SOURCE RECONCILIATION
-------------------------------------------------------------------------------
The unfinished Great Person breakdown check from WIP133 closes cleanly.

Current `CvCity::m_iBaseGreatPeopleRate` gameplay writers were traced through the
current C++/Python surface. Ordinary gameplay changes come through building and
specialist processing:

- `CvCity::processBuilding` adds/removes both the building's mapped
  GreatPeopleUnitRate and the same amount from BaseGreatPeopleRate;
- `CvCity::processSpecialist` likewise updates the mapped GreatPeopleUnitRate
  and BaseGreatPeopleRate together;
- event free-specialist changes flow through the specialist-count path rather
  than directly mutating BaseGreatPeopleRate;
- the Python wrapper exposes `CyCity.changeBaseGreatPeopleRate`, but no current
  SAS Python caller was found using that escape hatch.

`CvCity::doGreatPeople` asserts that the per-Great-Person unit-rate sum equals
BaseGreatPeopleRate, while `parseGreatPeopleHelp` separately asserts its rebuilt
modifier against `getTotalGreatPeopleRateModifier()` and its final result against
`getGreatPeopleRate()`. No ordinary current third base-rate source was proved
that the TextMgr specialist/building decomposition omits.

Keep this as a strong control; do not spend F432 on the Great Person path.

-------------------------------------------------------------------------------
F432 / provisional KI#755 - RELIGION-SPECIFIC CITY HELP OMITS THE CURRENT
NON-STATE-RELIGION HAPPINESS EFFECT
-------------------------------------------------------------------------------
Status: CONFIRMED
Root file: CvGameCoreDLL/CvGameTextMgr.cpp
Cross-files/data: CvCity.cpp, CvPlayer.cpp, CvDLLWidgetData.cpp,
Assets/Python/Screens/CvMainInterface.py, CvPolicyAdvisorScreen.py,
CIV4CivicInfos.xml.
Classification: inherited stock-BtS religion-help omission, normally live in
stock BtS and current AdvCiv-SAS through Free Religion.

RUNTIME CONTRACT
----------------
`CvCity::getReligionHappiness(ReligionTypes eReligion)` applies a happiness
contribution for every religion actually present in the city:

    if (eReligion == owner.getStateReligion())
        iHappiness += owner.getStateReligionHappiness();
    else
        iHappiness += owner.getNonStateReligionHappiness();

`CvCity::updateReligionHappiness` loops every religion and accumulates those
values into the city's religion-good / religion-bad happiness state.

The player-side civic processing is live state, not merely descriptive XML:
`CvPlayer::processCivics` feeds `iNonStateReligionHappiness` through
`changeNonStateReligionHappiness`, which immediately calls
`updateReligionHappiness()` across the player's cities.

CURRENT DATA / ORDINARY REACHABILITY
------------------------------------
Current SAS `CIVIC_FREE_RELIGION` has:

    <bStateReligion>0</bStateReligion>
    <iStateReligionHappiness>0</iStateReligionHappiness>
    <iNonStateReligionHappiness>1</iNonStateReligionHappiness>

Therefore, while using Free Religion, every religion present in a city is a
non-state religion and contributes +1 happiness through the executable runtime
contract above.

Concrete ordinary state:

    city has Hinduism + Buddhism
    owner runs Free Religion

Runtime religion happiness is therefore:

    Hinduism  +1
    Buddhism  +1
    total     +2 happiness

The ordinary city happiness breakdown does correctly expose the aggregate
`getReligionGoodHappiness()` total through `TXT_KEY_HAPPY_RELIGIOUS_FREEDOM`, so
this finding should NOT claim that the two happiness points vanish from every
UI surface. The defect is more precise: the religion-specific city/help path
fails to attribute or preview the per-religion effect it is meant to describe.

BROKEN HELP CONTRACT
--------------------
Current `CvGameTextMgr::setReligionHelpCity` first derives the effective state
religion, then handles religion commerce. Its happiness/effect block is entered
only when:

    if (eStateReligion == eReligion)

Inside that block it prints:

    pCity->getStateReligionHappiness(eReligion)
        + owner.getStateReligionHappiness()

along with the state-religion production / XP / Great-Person modifiers.

There is no sibling branch for:

    owner.getNonStateReligionHappiness()

when the requested religion is present but is not the effective state religion.
Under Free Religion `eStateReligion == NO_RELIGION`, so the happiness block is
skipped for every religion icon even though every one of those religions is
actively contributing +1 happiness to the city.

LIVE CALLERS
------------
This is not a dormant TextMgr API.

1. Main city screen:
   Current `CvMainInterface.py` attaches `WIDGET_HELP_RELIGION_CITY` to every
   displayed city religion icon (and its Holy City overlay). The DLL widget
   parser calls:

       GAMETEXT.setReligionHelpCity(...,
           gDLL->UI().getHeadSelectedCity(), true)

   Thus hovering Hinduism or Buddhism in the concrete Free Religion city above
   reaches the incomplete helper directly and omits that religion's +1 happy.

2. Current Policy Advisor Religion tab:
   `CvPolicyAdvisorScreen.py` calls
   `CyGameTextMgr().getReligionHelpCity(...)` for the per-city religion help.
   The same missing non-state branch therefore affects that integrated current
   SAS screen as well.

The current Main Interface still contains the older commented Python-side
religion tooltip reconstruction using `pHeadSelectedCity.getReligionHappiness`
for each religion. That is supporting evidence for the per-religion semantic
contract, though the live DLL/runtime comparison above is sufficient by itself.

ANCESTRY
--------
Stock BtS/Civ4CE already has the mismatch:

- stock `CvCity::getReligionHappiness` applies
  `getNonStateReligionHappiness()` to present non-state religions;
- stock `setReligionHelpCity` only prints happiness inside its state-religion /
  forced-state block and has no non-state happiness counterpart;
- stock `CIVIC_FREE_RELIGION` already uses
  `<iNonStateReligionHappiness>1</iNonStateReligionHappiness>`.

So the omission was normally reachable in stock BtS itself. K-Mod retains the
same missing branch, Base AdvCiv 1.14 retains it through the advc.172 religion-
help refactor, and current SAS retains it.

This is therefore an inherited stock-BtS help omission rather than a SAS data
activation or AdvCiv integration regression.

DUPLICATE CHECK
---------------
The reconciled album contains no existing non-state-religion-happiness city-help
root.

F418/KI#741 also uses Free Religion as a current proof state, but it is a
different contract: `setConvertHelp` fails to explain why conversion is blocked
when current civics permit no state religion (and when the target religion is
absent from the player's cities). F432 concerns an actually applied city
happiness effect that `setReligionHelpCity` omits from the religion-specific
help. Keep the roots separate.

F429's neighboring forced-religion commerce-max control is likewise independent:
that control concerns max religion-commerce semantics during prospective spread;
F432 is the ordinary present-city happiness branch and needs no forced preview.

REPAIR DIRECTION
----------------
After the existing effective `eStateReligion` calculation, mirror the runtime
happiness split:

- when `eReligion == eStateReligion`, retain the current state-religion
  happiness path, including city-specific state-religion building happiness;
- when the religion is present (or is being force-previewed as present) and is
  not the effective state religion, append
  `GET_PLAYER(owner).getNonStateReligionHappiness()` with the same correct
  happy/unhappy sign formatting.

Use the already-adjusted effective `eStateReligion` so advc.172's `bForceState`
preview semantics continue to work in both directions. Do not apply
`pCity->getStateReligionHappiness(eReligion)` to the non-state branch; runtime's
non-state religion happiness comes from the player-wide non-state value.

For `bForceReligion`, the same branch can also show the prospective non-state
happiness of spreading a religion while a no-state-religion civic is active.
That is a natural consequence of mirroring the runtime predicate rather than a
separate finding.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F432 / provisional KI#755 CONFIRMED.
F433 / provisional KI#756 UNUSED.
Latest physical checkpoint: C031-WIP134.
No repository source files modified.

NEXT
----
Continue Queue 003 from F433 unused. Resume the low-mention stateful-helper
inventory, with emphasis on present-state vs hypothetical-action help,
aggregate/component decompositions, paired state/non-state or positive/negative
branches, and inherited lifecycle arithmetic. Reconcile the album before every
promotion.

END C031-WIP134


===============================================================================
C031-WIP135 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION — F433 STILL UNUSED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Unique files complete: 32/169.
F432 / provisional KI#755 remains the latest confirmed distinct root.
F433 / provisional KI#756 remains UNUSED.
No repository source files were modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Preserve the post-WIP134 closure tranche before continuing deeper. This slice
finished the neighboring religion-happiness check, revalidated the principal
old data-masked Queue-003 controls against the current 2026-08-31 SAS snapshot,
closed the Great-Person / air-target / civic / trait field-coverage questions,
and investigated improvement-placement help without promoting a documentation-
completeness concern into a gameplay defect.

-------------------------------------------------------------------------------
CONTROL — BUILDING-SIDE STATE-RELIGION HAPPINESS DOES NOT ACTIVATE A SECOND
F432-LIKE CURRENT OMISSION
-------------------------------------------------------------------------------
Runtime has a separate building-side `iStateReligionHappiness` mechanism in
addition to the player/civic religion happiness covered by F432. Current SAS
Building XML was enumerated mechanically: every current BuildingInfo has
`iStateReligionHappiness = 0`.

Therefore there is no live current building-side state-religion-happiness
component to add to F432 or to promote separately. F432 remains specifically
about `CvCity::getReligionHappiness` applying civic non-state-religion happiness
while `setReligionHelpCity` omits that individual per-religion contribution.

-------------------------------------------------------------------------------
CURRENT-DATA MASK RECHECK — OLD QUEUE-003 DORMANT CONTROLS REMAIN DORMANT
-------------------------------------------------------------------------------
The masking assumptions behind several earlier controls were mechanically
re-tested against the current snapshot rather than assumed to remain true.

Still absent/currently masked:
* UnitInfo SpeedBonuses: zero current entries. The known `setBonusExtraHelp`
  missing BonusType comparison therefore remains dormant.
* UnitInfo `iExtraCost`: zero for every current unit.
* repeatable technologies: zero current `<bRepeat>1</bRepeat>` TechInfos.
* EventInfo negative `iHealth`: zero current entries.
* EventInfo negative randomized-gold ranges: zero current entries.
* positive civic/building population-unhealth modifiers capable of offsetting
  a <= -100 "No population unhealthiness" source: none current.
* colony-maintenance modifiers: no current CivicInfo now enters the already-
  known misspelled modifier-key branch; the old localization typo is even more
  strongly data-dormant in this snapshot.
* the terrain coast-only founding-exception combination needed for the known
  dangling `TXT_KEY_OR` branch: no current TerrainInfo uses it.

No F433 is consumed by any of these old hazards.

-------------------------------------------------------------------------------
CONTROL — IMPROVEMENT CIVILOPEDIA PLACEMENT HELP IS SELECTIVE, BUT THE FARM
IRRIGATION OMISSION IS NOT PROMOTED AS F433
-------------------------------------------------------------------------------
`CvPlot::canHaveImprovement` enforces more placement semantics than
`CvGameTextMgr::setImprovementHelp(..., bCivilopediaText=true)` explicitly
lists. Current Farm is a concrete example:

    bRequiresIrrigation = 1
    bFreshWaterMakesValid = 1
    bRequiresFlatlands = 1
    PrereqNatureYields food = 1

The DLL Pedia helper lists the flatland restriction and "carries irrigation"
but has no direct `isRequiresIrrigation()` requirement line. Current custom
SevoPedia's Requires panel lists the Build's technology prerequisite and its
Most Yields panel links the irrigated-yield effect to the Irrigation concept;
it does not add a separate irrigation-placement requirement row.

However the actual rule is deliberately layered rather than an unconditional
"Farm requires irrigation" statement:
* fresh water itself satisfies `isIrrigationAvailable()`;
* a bonus for which Farm has BonusMakesValid returns true before the later
  irrigation gate;
* current `TECH_WATER_WHEEL` sets both `bIrrigation=1` and
  `bIgnoreIrrigation=1`, after which the team ignores that placement gate;
* the current custom Irrigation/Fresh Water concept text separately explains
  the Farm/irrigation relationship.

The action-help path on an actually blocked build also explicitly checks
`isRequiresIrrigation() && !isIrrigationAvailable()` and points to the
irrigation-enabling technology.

Classification: selective/static Pedia completeness and future UI-improvement
opportunity, not a crisp current runtime/help contradiction strong enough for
F433. A repair would need to express the exceptions/bypass coherently rather
than append an unconditional requirement sentence.

The same family contains other selectively represented placement fields such
as `RequiresFeature`, PrereqNatureYields and the various *MakesValid flags.
Their existence reinforces that `setImprovementHelp` is not currently a full
serialization of `canHaveImprovement`; no individual live contradiction was
promoted from that static-reference scope in this tranche.

-------------------------------------------------------------------------------
CONTROL — AIR-BOMB / PARADROP TARGET HELP RECONCILES WITH CURRENT EXECUTION
-------------------------------------------------------------------------------
`getAirBombPlotHelp` was compared to `CvUnit::canAirBombAt`,
`getDestructibleStructureAt` and `CvUnit::airBomb` after F430's separate
Railroad build-action discovery.

* the helper asks the same mission executor for a target-eligible aircraft;
* city-defense damage uses the same `airBombDefenseDamage` calculation;
* route-vs-improvement priority uses the same `getDestructibleStructureAt`
  contract as execution;
* AirBombDefense < 0 structures are filtered by the same target resolver;
* the displayed success probability is mathematically the exact probability
  of runtime's two synchronized uniform dice rolls
  `SyncRandNum(attack) >= SyncRandNum(defense)`;
* paradrop interception help likewise delegates through the target-eligible
  mission-unit path.

No independent F433 survived this action-target reconciliation.

-------------------------------------------------------------------------------
MECHANICAL COVERAGE — CIVIC AND TRAIT RUNTIME FIELDS ARE REPRESENTED IN THEIR
STATIC HELPERS
-------------------------------------------------------------------------------
A field-level comparison was made between `CvPlayer::processCivics` and
`CvGameTextMgr::parseCivicInfo`.

After accounting for array accessors (`getYieldModifierArray`,
`getCommerceModifierArray`, etc.) and explicit loops, every current CivicInfo
field that processCivics applies has a corresponding parseCivicInfo help branch:
health/happiness, maintenance, unit support, military support, state/non-state
religion effects, production/experience, yield/commerce arrays, building and
feature happiness/health, hurry/specialist/special-building permissions,
improvement yields and the remaining scalar effects.

The analogous `CvPlayer::processTraits` -> `CvGameTextMgr::parseTraits` field
comparison also has no runtime TraitInfo getter absent from the help helper.

This does not prove every wording/arithmetic branch correct (several numbered
findings already show integration mistakes despite field presence), but it
closes the broad "an entire current civic/trait runtime field is simply missing
from static help" hypothesis after F432.

-------------------------------------------------------------------------------
CURRENT STONE
-------------------------------------------------------------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F432 / provisional KI#755 is latest confirmed root.
F433 / provisional KI#756 UNUSED.
Latest physical checkpoint: C031-WIP135.
No repository source files modified.

NEXT
----
Continue from F433 unused. Prioritize remaining stateful city/action helpers,
component-vs-authoritative aggregate reconstructions, hypothetical post-action
previews and paired/sentinel branches. Keep the album duplicate check before
promotion and classify cross-file findings at their actual root file rather
than forcing them into Queue 003.

END C031-WIP135



===============================================================================
C031-WIP136 — QUEUE 003 CONTINUATION — F433 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Unique files complete: 32/169.
F433 / provisional KI#756 is CONFIRMED.
F434 / provisional KI#757 is UNUSED.
No repository source files were modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Preserve the post-WIP135 resource-layer cache investigation before continuing.
The candidate is a cross-file TextMgr/cache contract: the stale string is built
by `CvGameTextMgr::setBonusHelp` / `setBonusExtraHelp`, cached by
`CvPlayer::getResourceLayerColors`, and incorrectly invalidated from
`CvCity::processBuilding` when another civilization completes a World Wonder.

-------------------------------------------------------------------------------
F433 / provisional KI#756 — WORLD-WONDER COMPLETION DIRTIES THE FOREIGN
BUILDER'S BONUS-HELP CACHE INSTEAD OF THE ACTIVE PLAYER'S CACHE
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV UI CACHE-INVALIDATION DEFECT ROOT;
ordinary current-data proof strong.

ROOT CONTRACT
-------------
AdvCiv caches the complete resource-indicator help string for each BonusType in
`CvPlayer::m_aszBonusHelp`. `CvPlayer::getResourceLayerColors` behaves as:

    if (m_aszBonusHelp[eLoopBonus] != NULL)
        szBuffer.append(*m_aszBonusHelp[eLoopBonus]);
    else
    {
        GAMETEXT.setBonusHelp(szTempBuffer, eLoopBonus, false);
        m_aszBonusHelp[eLoopBonus] = new CvWString(...);
    }

The cached string is stateful. `setBonusExtraHelp` decides whether to list
resource-related buildings and Wonders from current game/player state,
including global/player/team max-instance status through `CvGame::canConstruct`
and the class-max predicates.

For that reason `CvCity::processBuilding` contains an explicit resource-layer
invalidation block. Its comments state that it is updating the text generated
by `CvGameTextMgr::setBonusExtraHelp`.

The block correctly computes which player's cached help must be invalidated:

    PlayerTypes eDirtyPlayer = NO_PLAYER;
    if (kBuilding.isNationalWonder() && isActiveOwned())
        eDirtyPlayer = getOwner();
    else if (kBuilding.isWorldWonder())
        eDirtyPlayer = kGame.getActivePlayer();

For a World Wonder completed by another civilization, the active human's
resource help must be rebuilt because that Wonder can have disappeared from the
active human's future construction/resource-production options.

But after calculating `eDirtyPlayer`, current code executes:

    GET_PLAYER(getOwner()).setBonusHelpDirty();

instead of:

    GET_PLAYER(eDirtyPlayer).setBonusHelpDirty();

Thus the explicit target calculation is discarded. If a foreign player built
the Wonder, the code clears the foreign builder's cache and leaves the active
human's cache unchanged.

CURRENT-SAS CONCRETE PROOF — STONE / PYRAMIDS
---------------------------------------------
Current `BUILDING_PYRAMID` is a normal one-instance World Wonder and has:

    BuildingClass = BUILDINGCLASS_PYRAMID
    PrereqTech = TECH_MINING
    BonusProductionModifier:
        BONUS_STONE -> +25%

Take an ordinary Ancient-era active human who can see/use Stone and for whom
Pyramids has not yet been globally constructed. While the Globe resource layer
is open, hovering/creating the Stone resource indicator causes
`getResourceLayerColors` to call `setBonusHelp` and cache the resulting Stone
help in the human's `m_aszBonusHelp[BONUS_STONE]`.

`setBonusExtraHelp` sees Pyramids as still constructible. Pyramids is current-
era rather than a remote future Wonder, so the cached Stone string legitimately
contains its construction effect:

    +25% production for Pyramids

Now another civilization completes Pyramids while the human remains on the
resource layer.

`CvCity::processBuilding` reaches the World-Wonder invalidation branch:

    eDirtyPlayer = kGame.getActivePlayer();

and dirties `GlobeLayer_DIRTY_BIT`, so the layer is redrawn. However it then
calls `setBonusHelpDirty()` on `getOwner()`, i.e. the foreign Wonder builder.
The active human's cached Stone text survives.

On redraw `getResourceLayerColors` sees the human's non-NULL cached Stone string
and reuses it without calling `setBonusHelp` again. The resource indicator can
therefore continue advertising the now-impossible Pyramids +25% construction
bonus.

A freshly generated string differs deterministically. After Pyramids is
completed, `CvGame::canConstruct(BUILDING_PYRAMID, true, true)` fails because
`isBuildingClassMaxedOut(BUILDINGCLASS_PYRAMID)` is true. In
`setBonusExtraHelp`, `bCanEverConstruct` becomes false; because the active human
owns zero Pyramids, the building is skipped before its production-bonus line.
The stale cached and fresh authoritative texts therefore disagree.

INVALIDATION-LIFECYCLE CHECK
----------------------------
All current `setBonusHelpDirty()` call sites were enumerated. No independent
World-Wonder-completion path repairs the active human cache:

* `CvPlayer::updateActiveVisibility` / active-player changes invalidate the new
  active player's cache, but no active-player switch occurs in the concrete
  case above.
* active-owned religion changes can invalidate the active owner's cache because
  religion prerequisites affect `setBonusExtraHelp`; unrelated to Wonder
  completion.
* `CvCity::processBuilding` is the explicit building/Wonder invalidation path
  and contains the wrong-target call described here.
* ordinary `CvPlayer::changeBuildingClassCount` only changes the class counter;
  it does not invalidate `m_aszBonusHelp`.

Therefore the dirty Globe redraw does not rescue the string: it can redraw
using exactly the stale cache entry that should have been deleted.

SCOPE / OTHER BUILDINGS
-----------------------
Pyramids is only a compact current example. Current SAS has many World Wonders
with resource production modifiers (Stone/Marble/Copper/etc.). Completion of
any such foreign Wonder can make a previously listed active-player resource
construction perk disappear while this wrong-owner invalidation leaves the
corresponding resource help cached.

The National-Wonder branch is not the same failure when the active player owns
the city: `isActiveOwned()` implies `getOwner() == eDirtyPlayer`, so the wrong
expression happens to select the intended player there. The defect is exposed
specifically by the World-Wonder branch's deliberate cross-player target.

ANCESTRY
--------
Civ4CE/stock BtS and K-Mod do not contain AdvCiv's `m_aszBonusHelp` resource-
layer help cache / `setBonusHelpDirty` invalidation architecture.

Base AdvCiv 1.14 contains the same cache and the exact same code sequence:

    else if (kBuilding.isWorldWonder())
        eDirtyPlayer = kGame.getActivePlayer();
    ...
    GET_PLAYER(getOwner()).setBonusHelpDirty();

The code is annotated by AdvCiv's `advc.004w` resource-help work, with the cache
itself marked `advc.003p`. Current SAS retains the wrong-target call unchanged.

Classification: inherited AdvCiv resource-layer cache-invalidation defect,
retained by SAS; not a stock-BtS/K-Mod defect.

DUPLICATE CHECK
---------------
The reconciled album contains no prior root for `m_aszBonusHelp`,
`setBonusHelpDirty`, the `eDirtyPlayer` variable, or stale World-Wonder resource
help. Existing resource-layer/bonus findings concern different contracts.

This is also not F429: F429 is a corporation-spread prospective calculation
that uses pre-removal corporation-produced resources. F433 is a persistent UI
cache invalidation bug after a real World Wonder state change.

REPAIR DIRECTION
----------------
Use the target that the block already computed:

    GET_PLAYER(eDirtyPlayer).setBonusHelpDirty();

inside the `eDirtyPlayer != NO_PLAYER` branch.

That is the minimal local repair and matches the surrounding intent exactly.
The existing `GlobeLayer_DIRTY_BIT` redraw can remain; once the correct cache is
cleared, redraw will regenerate the active player's affected resource strings.

A broader robustness option would centralize resource-help-cache invalidation
inside state mutations that change `setBonusExtraHelp` eligibility rather than
requiring individual callers to remember both redraw and cache deletion, but
that is not necessary to fix this root.

-------------------------------------------------------------------------------
POST-F433 CONTROL — STATE-RELIGION BUILDING HAPPINESS / CURRENT DATA
-------------------------------------------------------------------------------
The unfinished neighboring state-religion building-happiness check from WIP134
was completed before F433. Current BuildingInfo XML has no nonzero
`iStateReligionHappiness`, so it does not activate a second F432-like omission.
No additional root is assigned.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F433 / provisional KI#756 CONFIRMED.
F434 / provisional KI#757 UNUSED.
Latest physical checkpoint: C031-WIP136.
No repository source files modified.

NEXT
----
Continue Queue 003 from F434 unused. Stay briefly on the cached resource-help
family to audit all state dependencies of `setBonusHelp` against the complete
`setBonusHelpDirty` invalidation set, then return to low-mention stateful
helpers, hypothetical post-action previews and aggregate/component
reconstructions. Reconcile the album before every promotion.

END C031-WIP136



===============================================================================
C031-WIP137 — QUEUE 003 CONTINUATION — F434 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Unique files complete: 32/169.
F434 / provisional KI#757 is CONFIRMED.
F435 / provisional KI#758 is UNUSED.
No repository source files were modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Continue the resource-layer cache audit begun by F433. F433 is a wrong-target
bug inside an existing World-Wonder invalidation hook. F434 is a separate cache
contract: the complete resource help string contains several ordinary volatile
player/team values for which AdvCiv added no invalidation hook at all.

-------------------------------------------------------------------------------
F434 / provisional KI#757 — RESOURCE-LAYER BONUS-HELP CACHE IS NOT INVALIDATED
WHEN ORDINARY RESOURCE / CORPORATION / TECHNOLOGY STATE CHANGES
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV UI CACHE-LIFETIME DEFECT ROOT; multiple
ordinary current gameplay proofs.

CACHE CONTRACT
--------------
`CvPlayer::getResourceLayerColors` caches the complete non-Civilopedia
`CvGameTextMgr::setBonusHelp` result per BonusType in `m_aszBonusHelp`.

That string is not static. In current code it directly contains or conditionally
selects text from live state including:

1. `CvPlayer::getNumAvailableBonuses(eBonus)` — current capital-network
   availability count;
2. active-player corporation ownership — corporation glyphs are appended when
   a corporation currently present in the player's empire consumes the bonus;
3. active-team bonus obsolescence — obsolete resources print `(Obsolete)` and
   suppress the ordinary perk block;
4. current era / team tech / constructibility state used by
   `setBonusExtraHelp` to decide which buildings, projects and units to list.

Yet all current `setBonusHelpDirty()` callers are limited to:

* active-player/civilization visibility changes;
* the special building/Wonder resource-layer hook (whose World-Wonder branch is
  independently wrong-targeted by F433);
* a first active-owned religion acquisition while the resource layer is open.

There is no cache invalidation in the ordinary mutation paths below.

CURRENT PROOF A — AVAILABLE RESOURCE COUNT CAN REMAIN STALE
-----------------------------------------------------------
`setBonusTradeHelp` always appends the active player's available count in the
resource-layer context (`pCity == NULL`), using:

    pActivePlayer->getNumAvailableBonuses(eBonus)

`getNumAvailableBonuses` reads the capital plot group's live bonus count.
Ordinary connection/trade changes update that count through
`CvPlotGroup::changeNumBonuses` / `CvPlot::updatePlotGroupBonus` and
`CvPlayer::changeBonusImport` / `changeBonusExport`.

None of those paths calls `setBonusHelpDirty()`.

Concrete current state:

1. Active human has one Iron in the capital network and opens the resource
   Globe layer. Iron help is generated and cached with `1 available`.
2. The human connects a second Iron or begins importing another Iron.
3. The capital plot group now reports 2 Iron, so a fresh `setBonusHelp` call
   would print `2 available`.
4. `m_aszBonusHelp[BONUS_IRON]` is still non-NULL. Reusing or reopening the
   resource layer therefore continues to use the cached `1 available` string
   until some unrelated cache-clear event happens.

This does not depend on a one-frame redraw race; the stale string is persistent
heap state and can survive later layer rebuilds because the cache entry itself
was never deleted.

CURRENT PROOF B — CORPORATION-USE GLYPHS CAN REMAIN STALE
---------------------------------------------------------
The cached string loops all corporations and appends a corporation's glyph when
both:

    pActivePlayer->getHasCorporationCount(eCorporation) > 0

and the hovered bonus is one of that corporation's prerequisite resources.

`CvCity::setHasCorporation` changes the owning player's corporation count and
updates corporation effects, but it does not call `setBonusHelpDirty()`.

Therefore a player can cache a resource string before acquiring their first
copy of a corporation, then have that corporation spread normally and continue
to see the old string without the newly relevant corporation glyph. Removal of
the last copy has the symmetric stale-glyph possibility.

CURRENT PROOF C — TECHNOLOGY / BONUS OBSOLESCENCE CAN REMAIN STALE
-----------------------------------------------------------------
`CvTeam::setHasTech` explicitly dirties `GlobeLayer_DIRTY_BIT` for the active
team, but does not clear the active player's `m_aszBonusHelp` cache.

Current SAS provides a very direct state transition:

    BONUS_FUR       iHappiness = +1
                    TechObsolete = TECH_ENVIRONMENTALISM
    BONUS_ELEPHANTS iHappiness = +1
                    TechObsolete = TECH_ENVIRONMENTALISM
    BONUS_WHALE     iHappiness = +1
                    TechObsolete = TECH_ENVIRONMENTALISM

Take an active human with cached Fur help immediately before completing
Environmentalism. The pre-tech string can show Fur's ordinary +1 happiness,
available count and eligible perk lines.

After the tech is acquired, runtime `isBonusObsolete(BONUS_FUR)` becomes true.
A fresh `setBonusHelp` would append `TXT_KEY_BONUS_OBSOLETE` and skip the normal
bonus-perk block. The tech-acquisition path redraws the Globe layer but leaves
the cached pre-tech string intact, so the redraw can display the stale
non-obsolete help.

The same missing tech invalidation can stale the era/tech-sensitive
building/project/unit shortlist inside `setBonusExtraHelp` even when no bonus
itself becomes obsolete.

WHY THIS IS NOT F433
--------------------
Both roots involve `m_aszBonusHelp`, but the implementation defects and repairs
are independent:

* F433: `CvCity::processBuilding` already knows a World Wonder changed the
  active player's cached help and computes `eDirtyPlayer = activePlayer`, but
  accidentally clears `getOwner()` instead. This is a wrong-target variable in
  an existing hook.
* F434: routine bonus-network, corporation and technology mutations have no
  cache-clear hook at all. Fixing F433's single expression does not repair any
  F434 state.

Keep separate roots unless a future repair deliberately replaces the whole
cache-invalidation architecture with one centralized dependency revision.

ANCESTRY
--------
Stock BtS/Civ4CE and K-Mod recompute resource-layer bonus help and do not have
AdvCiv's `m_aszBonusHelp` / `setBonusHelpDirty` cache architecture.

Base AdvCiv 1.14 already contains:

* the `advc.003p` per-Bonus help cache;
* dynamic available-count / corporation / obsolescence content in the cached
  `setBonusHelp` string;
* the active-team tech path dirtying `GlobeLayer_DIRTY_BIT` without clearing
  the cache;
* no cache invalidation in the ordinary plot-group bonus-count,
  import/export, or corporation-count mutation paths.

Current SAS retains this cache lifetime unchanged. Classification: inherited
AdvCiv cache-dependency/invalidation defect, not stock BtS/K-Mod.

DUPLICATE CHECK
---------------
The reconciled album has no pre-F433/F434 root for `m_aszBonusHelp` lifetime or
stale resource-layer availability/corporation/tech text. F433 is deliberately
kept separate for the wrong-target World-Wonder hook as explained above.

REPAIR DIRECTION
----------------
The narrow repair is to invalidate the active player's bonus-help cache whenever
one of its semantic inputs changes, including at least:

* capital-network bonus counts / imports / exports;
* player corporation-presence transitions relevant to prerequisite resources;
* active-team technology changes that can obsolete bonuses or alter the
  building/project/unit shortlist;
* the already-existing building/Wonder transitions (with F433's target fix).

A safer architecture is preferable to proliferating hooks: cache either a
static-only portion of bonus help, or maintain a revision/generation counter for
resource-help semantic state and regenerate cached entries whenever the
revision changes. The current full-string cache is too dynamic for a handful of
ad hoc invalidation sites to be reliable.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F434 / provisional KI#757 CONFIRMED.
F435 / provisional KI#758 UNUSED.
Latest physical checkpoint: C031-WIP137.
No repository source files modified.

NEXT
----
Continue Queue 003 from F435 unused. Finish the remaining resource-help cache
dependencies (projects/team Wonders/capital-sensitive unit filtering) only far
enough to decide whether they broaden F434 or reveal an independent root; then
return to low-mention stateful TextMgr helpers and hypothetical post-action /
aggregate-component contracts.

END C031-WIP137


===============================================================================
C031-WIP138 — QUEUE 003 CONTINUATION — F435 CONFIRMED
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Unique files complete: 32/169.
F435 / provisional KI#758 is CONFIRMED.
F436 / provisional KI#759 is UNUSED.
No repository source files were modified.

PURPOSE OF THIS CHECKPOINT
--------------------------
Continue the low-mention stateful TextMgr pass after the F433/F434 resource-help
cache findings. This checkpoint establishes a separate inherited BtS defect in
the ordinary plot-resource hover: the prospective yield of the improvement that
would connect a resource is rebuilt from only two XML fields instead of using
the engine's authoritative improvement-yield calculation.

-------------------------------------------------------------------------------
F435 / provisional KI#758 — RESOURCE HOVER'S PROSPECTIVE CONNECTING-IMPROVEMENT
YIELD OMITS LIVE TECH / CIVIC / IRRIGATION / ROUTE / TERRAIN-SIDE MODIFIERS
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED BTS UI/RUNTIME-YIELD CONTRACT DEFECT ROOT; ordinary
current SAS reproduction and full lineage proof.

ROOT
----
When `CvGameTextMgr::setPlotHelp` sees a visible, nonobsolete bonus that is not
currently connected by its required improvement, it finds the first legal
`isImprovementBonusTrade(eBonus)` improvement and previews the yield difference.

For the prospective NEW improvement, current SAS computes only:

    kImprovementInfo.getImprovementBonusYield(eBonus, eYield)
    + kImprovementInfo.getYieldChange(eYield)

If another improvement is already on the plot, AdvCiv subtracts that old
improvement through the authoritative:

    kPlot.calculateImprovementYieldChange(oldImprovement, ...)

The asymmetry is therefore explicit: the old side is evaluated with full live
plot/player/team state, while the proposed new side is only base improvement
XML + bonus-specific XML.

AUTHORITATIVE RUNTIME CONTRACT
------------------------------
`CvPlot::calculateImprovementYieldChange` is the engine path for the actual
improvement yield. In current SAS it includes, in addition to the two fields
used by the hover:

* RiverSideYieldChange;
* HillsYieldChange;
* IrrigatedYieldChange when irrigation is currently available;
* RouteYieldChanges for the plot's current/revealed route;
* player ImprovementYieldChange (including civic effects);
* team ImprovementYieldChange (including technology effects);
* the bonus-specific yield.

`CvTeam::processTech` feeds current technology improvement-yield changes into
that team accumulator through `changeImprovementYieldChange`.

CURRENT SAS DETERMINISTIC REPRODUCTION — MAIZE + BIOLOGY
--------------------------------------------------------
Current `IMPROVEMENT_FARM` data has:

    base YieldChanges:              +2 food
    BONUS_MAIZE YieldChanges:       +2 food
    TECH_BIOLOGY TechYieldChanges:  +1 food

Take an ordinary active-player Maize plot after Biology with no connecting
improvement (for example an unimproved or pillaged Farm resource tile) on which
a Farm is legal.

The resource hover's prospective Farm line computes:

    +2 base Farm food
    +2 Maize-specific Farm food
    = +4 food with Farm

But actually building the Farm causes plot yield to be evaluated through
`calculateImprovementYieldChange`. The active team already owns Biology, so the
Farm receives the additional +1 technology yield and its actual improvement
contribution is:

    +2 +2 +1 = +5 food

Thus the ordinary revealed-plot hover understates the result of the action it is
recommending by one food.

OTHER LIVE SHAPES OF THE SAME ROOT
----------------------------------
The missing set is broader than Biology alone. The same manual prospective
formula can omit currently active:

* Farm IrrigatedYieldChange;
* Mine/Lumbermill Railroad RouteYieldChanges;
* river-side or hill-specific improvement yields;
* civic/player improvement-yield modifiers;
* other technology/team improvement-yield modifiers.

These are manifestations of the same partial-reconstruction root and should not
receive separate finding numbers.

ORDINARY CALLER / VISIBILITY CONTRACT
-------------------------------------
This is not a dead Civilopedia helper. `CvGameTextMgr::getPlotHelp` calls
`setPlotHelp` for ordinary revealed mouse-over plots in both map and city-screen
contexts. The resource-improvement branch is therefore normal map help whenever
a visible nonobsolete bonus lacks its connecting improvement.

The reproduction does not require hidden information, unsupported XML, debug
mode, Python direct calls or a future/mod-mod configuration.

ANCESTRY
--------
Firaxis BtS/Civ4CE already computes the proposed improvement yield as exactly:

    getImprovementBonusYield(...) + getYieldChange(...)

K-Mod retains the same partial formula. Base AdvCiv 1.14 retains it as well;
`advc.047` improves the OLD-improvement subtraction by calling
`calculateImprovementYieldChange`, but leaves the proposed improvement side
partial. Current SAS inherits that asymmetry unchanged.

Classification: inherited stock-BtS help/runtime mismatch, not a SAS-local
regression.

DUPLICATE / RELATED-ROOT RECONCILIATION
---------------------------------------
F180 / KI#503 is related in coding smell but is NOT this root. F180 concerns the
SAS practical-5976 `SAS_getBonusImprovementFoodChange` helper used by the
first-city low-food evaluation; it independently omitted already-active
IrrigatedYieldChange from a gameplay AI score.

F435 is an older, separate `CvGameTextMgr::setPlotHelp` formula used by ordinary
player-facing resource hover and omits a broader set of live modifiers. Fixing
F180 does not affect F435 and vice versa.

F430 is also separate. F430 is `CvDLLWidgetData::parseActionHelp` failing to
preview an existing Mine/Lumbermill's Railroad `RouteYieldChanges` for the
route-only BUILD_RAILROAD action. F435 instead previews placing a resource-
connecting improvement and partially reconstructs that prospective
improvement's own yield. The code sites and repair surfaces are independent.

REPAIR DIRECTION
----------------
Do not manually rebuild the proposed improvement yield from base + bonus fields.
Use the same authoritative plot calculation already used for the old side, e.g.
conceptually:

    newYield = kPlot.calculateImprovementYieldChange(
        eLoopImprov, eLoopYield, eActivePlayer);
    oldYield = (no old improvement ? 0 :
        kPlot.calculateImprovementYieldChange(
            ePlotImprovement, eLoopYield, eActivePlayer));
    iYieldChange = newYield - oldYield;

That automatically follows current river/hill/irrigation/route/player/team/bonus
semantics and prevents later XML or civic/tech changes from creating new drift.
Preserve the surrounding revealed-owner/observer semantics already implemented
inside `calculateImprovementYieldChange` rather than duplicating them here.

POST-WIP137 CONTROL
-------------------
The earlier F434 cache audit remains separate. Project-max / capital-sensitive /
era-sensitive resource-help cache dependencies, where current, should broaden
F434's missing-invalidation family rather than be renumbered unless an
independent invalidation defect is proved.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F435 / provisional KI#758 CONFIRMED.
F436 / provisional KI#759 UNUSED.
Latest physical checkpoint: C031-WIP138.
No repository source files modified.

NEXT
----
Continue Queue 003 from F436 unused. Finish the resource-help cache dependency
extensions only far enough to classify them under F434, then return to the
low-mention stateful TextMgr / hypothetical-action / aggregate-component sweep.
Use the reconciled album before every promotion and do not manufacture a root
from static-Pedia completeness or hidden precision differences alone.

END C031-WIP138


===============================================================================
C031-WIP139 — QUEUE 003 CvGameTextMgr.cpp CONTINUATION — F436 CONFIRMED
===============================================================================

Continuation basis
------------------
Resumed from C031-WIP138 / F436 unused on current light-source snapshot practical
6351 / HEAD 9068ad28635d8be44965efadf9bce1738e1b412d. The first continuation pass
finished the subject-vs-active-player / observer-vs-subject scan and then ranked
low-mention stateful helpers. The observer/subject family did not establish a
new root: the active-player references in espionage, war-trade, relations and
trade diagnostics represent the actual observer/actor role, while the known
event-help active-player mismatch remains a non-current API hazard already
preserved in earlier controls.

The low-mention billboard pass then found a sign-sensitive regression in
`buildCityBillboardCityNameString`.

-------------------------------------------------------------------------------
F436 / provisional KI#759 — AVOID GROWTH HIDES THE CITY-BILLBOARD'S IMMINENT
STARVATION COUNTDOWN BECAUSE THE ONE-TURN SUPPRESSION USES abs(iTurns)
-------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV UI/LIFECYCLE CONTRACT DEFECT ROOT; ordinary
current-SAS reproduction and exact introducing-history proof.

ROOT
----
`CvCity::getFoodTurnsLeft()` has a signed contract in current AdvCiv/SAS:

    positive value  = turns to population growth
    negative value  = turns to starvation / population loss
    MAX_INT         = stagnant

For negative food difference it explicitly returns:

    return std::min(-1, -intdiv::uceil(getFood(), -iFoodDiff));

The city billboard deliberately displays the absolute number because the small
billboard suffix has no color/sign channel:

    int iTurns = pCity->getFoodTurnsLeft();
    if (abs(iTurns) > 1 || !pCity->AI().AI_isEmphasizeAvoidGrowth())
    {
        if (iTurns < MAX_INT)
            ... append abs(iTurns) ...
    }

The inherited Avoid Growth suppression therefore treats `+1` growth and `-1`
starvation as if Avoid Growth affected both. It does not.

AUTHORITATIVE RUNTIME CONTRACT
------------------------------
`CvCity::doGrowth()` applies Avoid Growth only after positive food has reached
the growth threshold:

    if (getFood() >= growthThreshold())
    {
        if (AI().AI_isEmphasizeAvoidGrowth())
            setFood(growthThreshold());
        else
            changePopulation(1);
    }
    else if (getFood() < 0)
    {
        ...
        if (getPopulation() > 1)
            changePopulation(-1);
    }

The starvation branch contains no Avoid Growth exemption. Thus Avoid Growth can
make a displayed `+1` growth estimate non-actionable, but it cannot prevent a
`-1` starvation estimate from becoming a real population loss.

CONCRETE CURRENT-SAS REPRODUCTION
---------------------------------
Take an ordinary selectable active-player city with population > 1, Avoid Growth
enabled, stored food = 1 and `foodDifference() = -2`.

`getFoodTurnsLeft()` computes:

    ceil(1 / 2) = 1
    => -1 turn to starvation

On the city turn, `doGrowth()` subtracts two food, reaches negative stored food,
and executes `changePopulation(-1)` despite Avoid Growth.

But the billboard test sees:

    abs(-1) > 1                         => false
    !AI_isEmphasizeAvoidGrowth()        => false

and therefore suppresses the `(1)` suffix entirely. The same city without Avoid
Growth shows the one-turn starvation countdown. Toggling an option that only
prevents positive growth thus hides an imminent negative-population event that
it does not prevent.

WHY THE POSITIVE +1 CASE IS DIFFERENT
-------------------------------------
The old one-turn suppression has a legitimate purpose for growth. When
`iTurns == +1` and Avoid Growth is active, the city can hit its growth threshold
this turn and then be clamped instead of gaining population. Showing `(1)` as a
growth countdown would therefore be misleading.

The defect is not the suppression itself; it is broadening that suppression to
`-1` through `abs(iTurns)` after starvation support was added.

ANCESTRY / BIRTH
----------------
Stock BtS/Civ4CE and the older AdvCiv code used the billboard suffix only for
positive food difference, so the Avoid Growth `iTurns > 1` suppression could not
hide starvation there.

AdvCiv practical 3000 / commit

    63477cec3fd52c882ef69b1e61e7c9213fa05df2
    "Show turns to starvation on city bar"

changed `CvCity::getFoodTurnsLeft()` to return negative starvation turns and
broadened `buildCityBillboardCityNameString` from growth-only to both signs. The
introducing diff changes:

    if (pCity->foodDifference() > 0)
        ... if (iTurns > 1 || !AvoidGrowth) ...

to:

    int iTurns = pCity->getFoodTurnsLeft();
    if (abs(iTurns) > 1 || !AvoidGrowth)
        ... append abs(iTurns) ...

That commit therefore introduces the regression directly: starvation became a
supported billboard state, but the growth-only Avoid Growth exception was made
sign-insensitive. Base AdvCiv 1.14 and current SAS retain the same code.

DUPLICATE / CONTROL RECONCILIATION
----------------------------------
The reconciled Queue-003 album already contains F408's disorder-food hover bug
and later general billboard controls, but no prior root or control covers this
signed one-turn Avoid Growth suppression. F408 concerns reconstructing normal
food arithmetic while runtime freezes food in disorder; F436 concerns a normal
non-disorder city's imminent starvation countdown being hidden solely because
Avoid Growth is enabled. The code paths, states and repairs are independent.

The earlier broad billboard closure note after WIP111 is therefore refined, not
contradicted wholesale: production/GP/top-city billboard calculations remain
closed; this specific city-name food-turn sign branch had not been isolated.

REPAIR DIRECTION
----------------
Make the Avoid Growth exception sign-sensitive. Conceptually:

    if (iTurns < 0 || iTurns > 1 || !pCity->AI().AI_isEmphasizeAvoidGrowth())
        append abs(iTurns);

or equivalently suppress only the exact positive-growth case:

    if (!(iTurns == 1 && pCity->AI().AI_isEmphasizeAvoidGrowth()))
        ...

Preserve `abs(iTurns)` in the displayed suffix if the billboard cannot encode
negative/starvation color; the important fix is that `-1` must not be discarded
by a growth-only policy.

POST-F435 / F436 CONTROLS
-------------------------
1. Subject-vs-active-player scan: no new root.
   Explicit active-player use in `setEspionageCostHelp`, `parseWarTradesHelp`,
   relation helpers and trade diagnostics corresponds to observer/actor state;
   no ordinary subject substitution mismatch survived caller review.

2. `setEventHelp` nested additional-event legality can still consult the actual
   global active player in one API shape, but current ordinary event widgets
   supply the active player and current quest/global/team data do not activate a
   distinct supported mismatch. Retain the earlier compatibility/API control.

3. `getTradeString` / disengagement and nuke-target helpers remain under the
   previously completed execution-contract controls; no new current root was
   established in this pass.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
Queue 012 closure remains amended by F430.
32/169 unique files complete.
F436 / provisional KI#759 CONFIRMED.
F437 / provisional KI#760 UNUSED.
Latest physical checkpoint: C031-WIP139.
No repository source files modified.

NEXT
----
Continue Queue 003 from F437 unused. Resume the low-mention stateful-member pass,
prioritizing helpers that reconstruct a signed/threshold quantity or hypothetical
post-action state without an equality assertion. Then continue enum/sentinel and
paired-branch asymmetry sweeps. Reconcile every candidate against the protected
album before promotion; a clean tranche remains a valid result.

END C031-WIP139


===============================================================================
C031-WIP140 — QUEUE 003 CONTINUATION FROM WIP139
===============================================================================

CURRENT SOURCE IDENTITY
-----------------------
Current SAS branch: main
Current SAS HEAD: 9068ad28635d8be44965efadf9bce1738e1b412d
Current practical/commit count: 6351
Primary current file: CvGameCoreDLL/CvGameTextMgr.cpp
Comparison trees: local AdvCiv 1.14, K-Mod 1.46, Civ4CE/BtS reconstruction
No repository source files modified by this audit.

CURRENT STONE
-------------
Queue 003 CvGameCoreDLL/CvGameTextMgr.cpp — OPEN.
32/169 unique files complete.
F436 / provisional KI#759 remains the latest confirmed distinct root.
F437 / provisional KI#760 remains UNUSED.

POST-F436 SENTINEL / LOW-MENTION CLOSURE — NO NEW NUMBER
--------------------------------------------------------

1. ImprovementTypes compared against NULL remains the prior dormant control.

The resource section of setPlotHelp contains:

    (ePlotImprovement != NULL &&
     GC.getInfo(ePlotImprovement).isActsAsCity())

instead of comparing the enum against NO_IMPROVEMENT. This is genuinely bad
enum/sentinel style, but current data still masks it exactly. Improvement enum
0 is IMPROVEMENT_LAND_WORKED, a graphical-only helper with bActsAsCity=0. The
current live acts-as-city improvement is Fort at a nonzero enum. The wrong NULL
comparison therefore cannot currently change this branch's result. This was
already preserved in the WIP107 closure controls and is not promoted as F437.

A broader current-file scan found no second enum-as-NULL, enum-as-bool or
numeric-zero sentinel contract strong enough for a new root.

2. Advanced Start generic categories remain F417-visibility-specific.

getCityDataForAS and the neighboring generic city/population/culture/building
palette builders retain the old `iCost > 0` convention even though -1 is the
actual unavailable sentinel. Their current generic costs were rechecked against
adjustAdvStartPtsToSpeed and today's cost inputs. Unlike Visibility, whose base
cost 1 is known to round to a legal zero on slower game speeds (F417), these
categories start high enough that the current supported arithmetic does not
collapse the generic purchase into a legal zero-cost palette entry. No sibling
F437 established.

3. Minimized-popup payload contracts are safe enough under current callers.

setMinimizePopupHelp decodes production, religion and civic popup payloads in
the same fields used when those concrete popups are created. Advanced Start
exit also creates a generic BUTTONPOPUP_CHANGECIVIC without a specific civic.
CvPopupInfo's constructor defaults Data2 to -1, i.e. NO_CIVIC, so this path does
not accidentally resolve civic enum 0. The helper simply emits no civic-specific
minimized label for that generic popup. Kept as minor presentation/API
completeness debt rather than F437.

4. Compact tech-tree research-speed assumptions still match current config.

buildSingleLineTechTreeString explicitly asserts that
TECH_COST_FIRST_KNOWN_PREREQ_MODIFIER is 0 because its classification does not
model that older first-known-OR modifier. Current TechDiffusion_GlobalDefines.xml
still overrides that define to 0. The ordinary known-prerequisite modifier
remains the expected 20-percent mechanism. No current config drift was found
that invalidates this helper's speed-up classification.

5. Building Actual Effects aggregation does not introduce a new independent
root in this tranche.

setBuildingNetEffectsHelp delegates defense, happiness, health, yield, commerce,
saved maintenance and Great-Person deltas to the existing CvCity authoritative
projection helpers. The significant disorder/BUG-Actual-Effects integration
failures are already owned by F409/F410 and neighboring Queue-003 roots. No new
independent double-count/omitted-current-source contract was established here.

6. Project help remains consistent with the earlier WIP114 project-count
control.

setProjectHelp's world/team copy counts reserve copies already being made, while
runtime continuation legality discounts the current city's own in-progress copy.
Thus a current city can legally finish its project while the hover says zero
additional copies remain; that wording is coherent. The remaining project
effects/prerequisites checked here follow their ProjectInfo/runtime fields. No
new F437 established.

NEXT
----
Continue Queue 003 from F437 unused. Prioritize the remaining inherited stateful
helpers with low explicit album coverage, especially helpers that reconstruct
live state without a final equality assertion. Keep duplicate/current-data/
caller/ancestry proof before promotion. The file is increasingly saturated, so
clean closure is preferred over manufacturing a late finding.

END C031-WIP140


===============================================================================
C031-WIP141 — QUEUE 003 CONTINUATION FROM WIP140
===============================================================================

CURRENT SOURCE IDENTITY
-----------------------
Current SAS branch: main
Current SAS HEAD: 9068ad28635d8be44965efadf9bce1738e1b412d
Current practical/commit count: 6351
Primary current file: CvGameCoreDLL/CvGameTextMgr.cpp
Comparison trees: local AdvCiv 1.14, K-Mod 1.46, Civ4CE/BtS reconstruction
No repository source files modified by this audit.

F437 / provisional KI#760 — FEATURE HELP DROPS ADVC.012'S CONDITIONAL RIVAL-DEFENSE FIELD, SO FOREST/JUNGLE PEDIA STATS DO NOT DESCRIBE THEIR ACTUAL DEFENSE RULE
--------------------------------------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited AdvCiv UI/help integration omission; current SAS data and current Sevopedia caller make it ordinary-live.

ROOT
----
AdvCiv change advc.012 replaced BtS/K-Mod's single unconditional Forest/Jungle
feature-defense value with a two-part rule:

    ordinary feature defense       = CvFeatureInfo::getDefenseModifier()
    conditional rival defense      = CvFeatureInfo::getRivalDefenseModifier()

`CvPlot::defenseModifier` applies the second field when:

    eAttacker == NO_TEAM || getTeam() != eAttacker

For actual combat, the attacker team is supplied. Thus the intended player-facing
rule is the one stated by the AdvCiv manual: Forest and Jungle provide feature
defense except when the attacker owns the tile. The live plot-help implementation
also has explicit advc.012 code that computes and labels this conditional component.

Current SAS feature data makes the split concrete:

    FEATURE_FOREST:
        iDefense       =   0
        iRivalDefense  = +25

    FEATURE_JUNGLE:
        iDefense       = -25
        iRivalDefense  = +25

The SAS Jungle rework deliberately retains the inherited +25 rival-defense field
while changing the ordinary component to -25.

STATIC FEATURE-HELP OMISSION
----------------------------
`CvGameTextMgr::setFeatureHelp` still contains only the old single-field display:

    if (kFeature.getDefenseModifier() != 0)
        ... TXT_KEY_TERRAIN_DEFENSE_MODIFIER ...

There is no call to `getRivalDefenseModifier()` anywhere in setFeatureHelp and no
second static line describing the ownership-dependent feature defense.

This matters to current SAS's actual feature page. `SevoPediaFeature::placeSpecial`
constructs the Special Abilities text with:

    info.getHelp() + CyGameTextMgr().getFeatureHelp(self.iFeature, True)

so the omission is directly inherited by the custom Sevopedia Feature screen.
`WIDGET_PEDIA_JUMP_TO_FEATURE` hover help also routes through
`CvDLLWidgetData::parseFeatureHelp -> CvGameTextMgr::setFeatureHelp`.

CONCRETE CURRENT CONSEQUENCES
-----------------------------
1. Forest

   The Feature page sees `iDefense == 0`, so setFeatureHelp emits no defense line.
   A reader inspecting Forest itself therefore sees no feature-defense stat even
   though normal combat on a Forest tile receives +25% from iRivalDefense whenever
   the attacker does not own that tile.

2. Jungle

   Current SAS sets ordinary iDefense to -25 while retaining +25 iRivalDefense.
   setFeatureHelp therefore prints the -25% defense modifier but omits the +25%
   conditional component. In the common case where the attacker does not own the
   Jungle tile, the two feature components net to 0; the static feature page shows
   only the -25 half of that rule.

The live map plot help is better: advc.012 explicitly compares defense with and
without the ownership-conditioned component and emits TXT_KEY_FEATURE_RIVALDEF.
The omission is therefore specifically the static feature/widget contract, not a
claim that every combat hover is wrong.

DOCUMENTATION CONTROL
---------------------
AdvCiv's manual states the rule directly:

    Forest and Jungle provide only 25% defense, and none if the tile is owned by
    the attacker. 012

Current SAS also preserves this information in its Mods Info / K-Mod-to-AdvCiv
summary, and AdvCiv modified its combat concept/hints for the same rule. Those
separate documents reduce discoverability impact but do not make the feature's own
stat/helper correct. The Sevopedia Feature page is the canonical per-feature stat
surface and explicitly delegates Special Abilities to setFeatureHelp.

ANCESTRY
--------
Stock BtS/Civ4CE uses one unconditional `iDefense = 50` for both Forest and Jungle.
Its setFeatureHelp displays that one field, so there is no missing second defense
component in the stock data model.

K-Mod retains the same single-field 50% feature-defense data and corresponding
single-field help model.

AdvCiv change advc.012 changes Forest/Jungle ordinary iDefense from 50 to 0, adds
`iRivalDefense = 25`, extends CvFeatureInfo/XML, changes CvPlot::defenseModifier,
AI plot-defense consumers, live plot help, hints/manual/concept wording, etc. But
`setFeatureHelp` remains structurally the old single-field implementation. Base
AdvCiv 1.14 therefore already contains the omission; current SAS inherits it and
makes the Jungle presentation especially misleading through its -25 ordinary
component.

DUPLICATE RECONCILIATION
------------------------
No prior F/KI root or preserved Queue-003 control in the reconciled album covers
`getRivalDefenseModifier` being absent from setFeatureHelp. Existing promotion
feature-defense help concerns unit promotion modifiers, not the plot feature's
advc.012 ownership-conditioned base defense. Existing KI#21 concerns live-unit
Cannot Enter state, not feature-defense documentation.

REPAIR DIRECTION
----------------
Extend `setFeatureHelp` to represent `getRivalDefenseModifier()` explicitly rather
than folding it blindly into ordinary defense. The condition matters; a safe static
line should communicate the advc.012 rule (e.g. +25% defense except when attacked by
the tile owner / no feature defense against the tile owner), with localized wording.

Do not simply add iDefense + iRivalDefense into one unconditional number: that would
make Forest/Jungle look like permanent +25/0 and lose the ownership-dependent rule.
Reuse or adapt the existing advc.012 localization where semantically appropriate,
but note that `TXT_KEY_FEATURE_RIVALDEF` was designed for contextual plot help
("for tile owner") rather than a context-free Feature page.

POST-WIP140 CONTROLS BEFORE F437
--------------------------------
- parsePromotionHelp effect-field and acquisition-prerequisite reconciliation was
  completed. Every current runtime promotion effect has corresponding help; no
  promotion-help F437 sibling was established.
- Great-General progress help uses the same stored combat XP and
  `greatPeopleThreshold(true)` runtime threshold; no drift found.
- The low-mention live-helper classification found no new deal/trade or sentinel
  root before the feature-field comparison reached setFeatureHelp.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F437 / provisional KI#760 CONFIRMED.
F438 / provisional KI#761 UNUSED.
Latest physical checkpoint: C031-WIP141.
No repository source files modified.

NEXT
----
Continue Queue 003 from F438 unused. Finish the remaining Terrain/Feature/Improvement
runtime-field-vs-help reconciliation, then resume low-mention live-state helpers and
mechanical member-coverage closure. Preserve the distinction between static Pedia
completeness and a hard contradictory stat; do not promote a merely exhaustive-doc
wish unless current data makes the omitted field materially misleading.

END C031-WIP141


===============================================================================
C031-WIP142 APPEND - Queue 003 post-F437 field/state closure; F438 unused
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
F437 / provisional KI#760 remains the latest confirmed distinct root.
F438 / provisional KI#761 remains UNUSED.
No repository source files modified.

TERRAIN / FEATURE / IMPROVEMENT FIELD RECONCILIATION AFTER F437
---------------------------------------------------------------
The remainder of this family was rechecked with a stricter distinction between a
static-reference completeness wish and a contradictory live rule.

- Terrain help covers the current live terrain build modifier, defense modifier,
  movement, impassability and founding restrictions. The previously preserved
  coast-only founding dangling-OR branch remains data-dormant: current Terrain XML
  has no coast-only exception state that reaches it.
- Feature iRivalDefense remains the one hard split-field integration omission and is
  F437. Feature iWarmingDefense is not printed by setFeatureHelp, but AdvCiv/SAS's
  Environmental Advisor explicitly surfaces per-feature global-warming offsets;
  this was not promoted as a second F437-like stat contradiction.
- Improvement AirBombDefense is a real gameplay field absent from static
  setImprovementHelp, but the actual Air Bomb target hover uses getAirBombDefense
  and computes the live success probability. Current omission therefore remains a
  static-reference gap rather than a false action result.
- RequiresIrrigation / RequiresFeature / OutsideBorders and related placement fields
  were checked against CvPlot::canHaveImprovement and current Sevopedia panels.
  These rules have context-sensitive bypasses and interactions (BonusMakesValid,
  fresh-water/irrigation availability, Water Wheel's ignore-irrigation state,
  terrain/feature validity). No single omitted static line was shown to contradict a
  legal current action; keep as Pedia completeness/robustness debt.
- Static improvement evolution time uses the game-speed/start-era base upgrade time
  and does not include the owner's ImprovementUpgradeRateModifier (e.g. Wage Labor).
  Actual plot help uses CvPlot::getUpgradeTimeLeft and therefore reports the
  player-modified live time correctly. Static baseline-vs-player-context limitation,
  not F438.

PLOT-LIST PER-OWNER / AGGREGATION CLOSURE
-----------------------------------------
`setPlotListHelpPerOwner`, `appendUnitTypeAggregated`, the two sort helpers and their
owner/strength aggregation were given an explicit live-state pass.

- Invisible units are filtered against the active team before owner grouping.
- The center unit's no-argument getVisualOwner call is not an observer mismatch:
  CvUnit::getVisualOwner defaults NO_TEAM to the active team internally, matching
  the explicit active-team calls used by the surrounding loop.
- Center-unit and Warlord exclusions are paired with separate rendering; they do not
  disappear from the final list.
- Aggregate promotion counts and average strengths are computed only across the
  units represented by the aggregate row.
- `m_pHelpPlot` is only consumed by this same plot-list sorting family; no unrelated
  caller was found that can reuse its last pointer as stale sort context.
- The previously investigated apparent leading-comma bug in the "Other" owner
  heading remains disproved by the earlier all-one-category fast path.

No independent F438 was established in this subsystem.

CITY BILLBOARD ICON CLOSURE
---------------------------
`buildCityBillboardIconString` was rechecked after F436's signed starvation bug.

- Airport currently has ObsoleteTech=NONE, so the icon does not survive an ordinary
  Airport obsolescence state.
- The NoBadHealthIcon <= -100 caveat remains the older data-masked control; current
  data still lacks the positive population-unhealth modifier needed to contradict
  the absolute-immunity icon.
- Top-XP and Next-GP tie handling is intentionally asymmetric in source comments;
  no accidental comparator/state regression was established.
- Power, occupation, defense and corporation/HQ icon conditions map to their live
  city state; HQ identity versus active-corporation output is intentional.

No F437-style sibling survived here; F436 remains the billboard root from this
late Queue-003 pass.

SCORE BREAKDOWN CLOSURE
-----------------------
`setScoreHelp` explicitly warns that it must remain consistent with
`CvPlayer::calculateScore`, so its duplicated component arithmetic was reconciled.

Current SCORE_FREE_PERCENT is 0. For the ordinary non-final/non-victory score shown
by the scoreboard, CvGame::getScoreComponent therefore reduces to the same rounded
`factor * raw / max` form reconstructed by setScoreHelp. In addition, setScoreHelp
prints the breakdown only if its reconstructed total equals `calculateScore()`.
No silent current score-breakdown mismatch was established.

STATIC CORPORATION HELP CLOSURE
-------------------------------
`setCorporationHelp` was checked separately from F429's city-specific force-spread
preview.

- Per-resource yield/commerce coefficients are scaled by the current world-size
  corporation percent when an active game/player context exists.
- prerequisite bonuses, produced bonus and free-unit-class resolution match the
  corporation data contract.
- the competitor list mirrors the engine's shared-prerequisite-resource competition
  rule.
- No separate stale/pre-action state defect was found here. F429 remains the
  corporation preview root where competing-corporation removal changes the resource
  set before execution output is calculated.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F437 / provisional KI#760 CONFIRMED.
F438 / provisional KI#761 UNUSED.
Latest physical checkpoint: C031-WIP142.
No repository source files modified.

NEXT
----
Continue Queue 003 from F438 unused through the remaining live-unit/deal/UI-state
residue. Prefer stateful helpers that reconstruct runtime quantities without a final
equality assertion. After that, perform another mechanical member-definition vs
explicit-control/finding reconciliation to judge whether Queue 003 is approaching a
defensible closure rather than manufacturing late findings from static documentation
completeness.

END C031-WIP142


===============================================================================
C031-WIP143 APPEND - F438 / KI#761 confirmed; Finance Specialist-extra-Gold
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F438 / provisional KI#761 CONFIRMED.
F439 / provisional KI#762 UNUSED.
No repository source files modified.

F438 / provisional KI#761 — FINANCE ADVISOR SPECIALIST-GOLD ROW USES BASE-ONLY
CITY SPECIALIST COMMERCE WHILE ITS OWN BULL HOVER AND RUNTIME INCLUDE PLAYER-
WIDE SPECIALIST-EXTRA GOLD
-------------------------------------------------------------------------------

ROOT
----
The current SAS Finance screen (`Assets/Python/Screens/CvDomesticAdvisor.py`)
computes the visible Specialists income row by summing, for every non-disorder
city:

    fCitySpecialists = city.getSpecialistCommerce(COMMERCE_GOLD)
    fSpecialists += fCitySpecialists

`CvCity::getSpecialistCommerce` is the city's stored specialist-info commerce.
`CvCity::processSpecialist` updates that store only from:

    CvSpecialistInfo::getCommerceChange

It does not contain the player-wide `getSpecialistExtraCommerce` contribution.

The exact widget attached to that same row is `WIDGET_HELP_FINANCE_SPECIALISTS`.
Its C++ path reaches `CvGameTextMgr::buildFinanceSpecialistGoldString`, which
counts assigned + free specialists in every non-disorder city and computes each
specialist's Gold through:

    kPlayer.specialistCommerce(eSpecialist, COMMERCE_GOLD)

`CvPlayer::specialistCommerce` is explicitly:

    SpecialistInfo base commerce + getSpecialistExtraCommerce(eCommerce)

Therefore the displayed row and its own tooltip use different definitions of
"specialist Gold".

RUNTIME CONTROL
---------------
The actual city commerce formula agrees with the tooltip, not with the Python
row. `CvCity::getBaseCommerceRateTimes100` adds:

    (getSpecialistPopulation() + getNumGreatPeople()) *
        owner.getSpecialistExtraCommerce(eCommerce)

separately from `getSpecialistCommerce(eCommerce)`.

The ordinary city commerce hover (`CvGameTextMgr::setCommerceHelp`) already uses
this same complete formula for its Specialists line:

    city.getSpecialistCommerce(eCommerce) +
    (city.getSpecialistPopulation() + city.getNumGreatPeople()) *
        owner.getSpecialistExtraCommerce(eCommerce)

This is an especially strong internal control: the authoritative runtime and an
existing TextMgr breakdown both include the component which the Finance row
omits.

CURRENT-DATA REACHABILITY
-------------------------
This latent split is live in current SAS because current data supplies
specialist-extra GOLD.

Current `CIV4CivicInfos.xml`:

    CIVIC_REPRESENTATION  SpecialistExtraCommerces = [1, 1, 1, 0]
    CIVIC_FREE_MARKET     SpecialistExtraCommerces = [1, 0, 0, 0]

Both feed `CvPlayer::changeSpecialistExtraCommerce` through `processCivics`.
The two civics belong to different civic-option categories and can therefore
stack, but either one is independently sufficient.

Current Specialist XML gives the simple controls:

    Scientist intrinsic Gold = 0
    Merchant  intrinsic Gold = 3

Ordinary one-Merchant reproduction under Representation:

    Finance visible Specialists row = 3
        because city.getSpecialistCommerce(GOLD) sees only intrinsic 3

    Hover on that exact row          = 4
        because player.specialistCommerce(Merchant, GOLD) sees 3 + 1

    Runtime base specialist Gold     = 4
        because CvCity adds the player-wide +1 per specialist

A more severe one-Scientist shape is possible. The Scientist has zero intrinsic
Gold, so the Finance loop leaves `fSpecialists == 0` and suppresses the
Specialists row entirely. Under Representation the same Scientist nevertheless
contributes +1 Gold to real city commerce. With Representation + Free Market,
it contributes +2 while the row still remains absent if no intrinsic-Gold
specialist is present.

FINANCE BREAKDOWN CONSEQUENCE
-----------------------------
The omission is broader than only the row label/hover number.

The Finance screen also computes each Gold-multiplier building's contribution
from:

    fCityTotel = fCityTaxes + fCityBuildings + fCityHeadquarters +
        fCityCorporations + fCitySpecialists

Because `fCitySpecialists` is base-only, any Gold multiplier contribution caused
by specialist-extra Gold is omitted from those multiplier rows too. The final
Income line is still based on the authoritative `player.getCommerceRate(GOLD)`,
so the total contains the missing specialist-extra output while the detailed
component rows fail to reconstruct it.

Replacing the base-only specialist quantity with the complete runtime quantity
would repair both the Specialist row and the multiplier base in the same place.

ANCESTRY
--------
K-Mod's `Assets/Python/Screens/EconomicsAdvisor.py` already contains the
base-only row calculation:

    fCitySpecialists = city.getSpecialistCommerce(COMMERCE_GOLD)

and attaches a BUG/BULL specialist-help widget through `BugDll.widget`.
However the supplied K-Mod DLL source does not itself contain
`buildFinanceSpecialistGoldString` or the corresponding BULL widget enum/parser.

AdvCiv practical commit 1481,
4a65bf5c0fb187fd25d4908346abb462354a9172,
"Option for expanding scoreboard; BULL hover text", imports/enables the BULL
Finance hover subsystem in the DLL. The introducing diff adds
`CvGameTextMgr::buildFinanceSpecialistGoldString` using
`kPlayer.specialistCommerce(...)` and converts the existing Finance Specialist
row from optional `BugDll.widget(...)` plumbing to the concrete
`WIDGET_HELP_FINANCE_SPECIALISTS` enum. It does not change the inherited
base-only Python row calculation.

Base AdvCiv 1.14 retains that split, but its shipped data masks the GOLD case:
its nonzero specialist-extra-commerce sources are Research/Culture only
(Representation [0,3,0], Scientific Method [0,1,0], Sistine Chapel [0,0,2]).
The supplied K-Mod and stock BtS/Civ4CE data likewise have no nonzero first
(Gold) SpecialistExtraCommerce entry.

Current SAS data removes that masking condition. Representation is already
shown with +Gold specialist commerce in SAS history by practical commit 5109
(the commit removes only its former fourth/espionage +1, leaving [1,1,1,0]),
and current Free Market supplies another +1 Gold per specialist.

Classification:
    latent inherited K-Mod/BULL/AdvCiv Finance-definition split, data-masked in
    base AdvCiv's Gold channel but made directly live by current SAS civic data.

DUPLICATE RECONCILIATION
------------------------
The earlier Queue-003 control for `buildFinanceSpecialistGoldString` checked only
its exclusion of disorder cities and found that state handling correct. It did
not compare the helper's `player.specialistCommerce` quantity against the
Python row's `city.getSpecialistCommerce` quantity.

Existing Finance roots are distinct:

- F400/KI#723: separately rounded/truncated Finance cost decomposition.
- F401/KI#724: exactly-one profitable trade route can have a blank Finance hover.
- later Finance anarchy controls concern unit-cost/supply state.
- F326/KI#649 mentions current Representation/Free Market
  SpecialistExtraCommerces only in AI civic valuation; it does not cover the
  Finance Advisor display/hover contract.

No existing root in the reconciled album owns this row-vs-tooltip/runtime split.

REPAIR DIRECTION
----------------
Use the same complete specialist-commerce quantity that runtime and
`setCommerceHelp` already use when computing `fCitySpecialists`.

A narrow Python repair is conceptually:

    fCitySpecialists = city.getSpecialistCommerce(COMMERCE_GOLD)
    fCitySpecialists += (city.getSpecialistPopulation() +
            city.getNumGreatPeople()) * player.getSpecialistExtraCommerce(
                    COMMERCE_GOLD)

All three Python APIs are exposed in current SAS. This preserves the existing
assigned/free-specialist count contract, excludes disorder exactly where the
Finance screen already does, makes the row agree with its BULL hover/runtime,
and automatically lets the later multiplier computation include the extra
specialist Gold as part of its base.

An alternative per-specialist loop using `player.specialistCommerce` would also
match the BULL helper, but the compact formula above is already the formula used
by `CvCity::getBaseCommerceRateTimes100` and `CvGameTextMgr::setCommerceHelp`.

Do not "fix" the C++ hover by switching it down to `city.getSpecialistCommerce`:
that would make the tooltip agree with the buggy row by hiding real current
Gold output that the runtime actually grants.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F438 / provisional KI#761 CONFIRMED.
F439 / provisional KI#762 UNUSED.
Latest physical checkpoint: C031-WIP143.
No repository source files modified.

NEXT
----
Continue Queue 003 from F439 unused through the remaining live-unit/deal/UI-state
residue, then perform another mechanical member-definition/coverage
reconciliation. Prefer exact duplicated-runtime contracts and current-data
contradictions over static completeness wishes; Queue 003 is late and saturated,
so a clean closure tranche is a valid result.

END C031-WIP143


===============================================================================
C031-WIP144 APPEND - F439 / KI#762 confirmed; Nuke target help uses group head
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F439 / provisional KI#762 CONFIRMED.
F440 / provisional KI#763 UNUSED.
No repository source files modified.

F439 / provisional KI#762 — NUKE TARGETING IS GROUP-WIDE, BUT NUKE PLOT HELP
AND THE AOE RADIUS RENDERER USE ONLY THE HEAD-SELECTED UNIT; A FIGHTER-HEADED
TACTICAL-NUKE GROUP CAN LEGALLY LAUNCH WHILE ITS TARGET HELP/RADIUS USE THE
NON-NUKE FIGHTER
-------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::getPlotHelp` dispatches `INTERFACEMODE_NUKE` through:

    CvUnit* pHeadSelectedUnit = gDLL->UI().getHeadSelectedUnit();
    ...
    getNukePlotHelp(*pMouseOverPlot, *pHeadSelectedUnit, szTempBuffer);

`getNukePlotHelp` then treats that unit as `kNuke` for every nuke-specific
quantity:

- `canNukeAt`;
- `nukeInterceptionChance`;
- `nukeRange` for explored-tile checks;
- `isNukeVictim` and all detailed invalid-target explanations.

That actor choice does not match the interface-mode or mission contract.

`CvSelectionGroup::canDoInterfaceMode(INTERFACEMODE_NUKE)` iterates every group
member and returns true as soon as ANY unit can `canNuke`.

`CvSelectionGroup::canDoInterfaceModeAt(INTERFACEMODE_NUKE, pPlot)` likewise
iterates every member and returns true as soon as ANY unit can `canNukeAt` the
hovered plot.

`CvSelectionGroup::canDoMission(MISSION_NUKE, ...)` is also any-member logic,
and `CvSelectionGroup::startMission` iterates the group's units; each member is
asked to `nuke(iData1, iData2)`, so an ineligible earlier member simply returns
false and a later eligible nuke can execute the order.

The visible hover therefore reconstructs a group-wide action from the wrong,
head-only actor.

CURRENT DETERMINISTIC PRODUCER
------------------------------
Current SAS supplies an ordinary mixed group that makes the mismatch live:

    UNIT_TACTICAL_NUKE  Domain = DOMAIN_AIR
                         DefaultUnitAI = UNITAI_ICBM
                         iNukeRange = 1
                         iAirRange = 4

    UNIT_FIGHTER / UNIT_JET_FIGHTER
                         Domain = DOMAIN_AIR
                         non-nuclear, positive air combat

`CvUnit::canJoinGroup` permits the grouping: once the group is nonempty it
requires the same owner/plot and the same domain; it does not require the same
unit type or mission abilities. Thus a Tactical Nuke and Fighter on one plot can
be one human selection group.

The group ordering makes the non-nuke-head case stronger than mere user-order
speculation. `CvSelectionGroup::addUnit` orders members by
`AI_groupFirstVal`, then `AI_groupSecondVal`. Current `UNITAI_ICBM` and the air
combat roles all have first value 0, while `AI_groupSecondVal` for air units is
`airBaseCombatStr()`. A Fighter therefore sorts ahead of the zero-combat
Tactical Nuke and becomes group head.

The Nuke interface XML does not repair this by narrowing the selection:

    INTERFACEMODE_NUKE
        bSelectType = 0
        bSelectAll  = 0

So entering Nuke mode leaves the mixed group intact.

Concrete current reproduction:

1. Put a Fighter and Tactical Nuke of the same player on the same air-base plot.
2. Group them. The Fighter is the group head because of the air-combat group
   ordering above.
3. Enter Nuke mode. The action is available because the group-wide
   `canDoInterfaceMode` finds the Tactical Nuke.
4. Hover a target legal for the Tactical Nuke. The normal target-validity test
   is positive because `canDoInterfaceModeAt` finds that Tactical Nuke.
5. `getNukePlotHelp` nevertheless receives the Fighter and calls the Fighter's
   `canNukeAt`, `nukeRange`, victim checks and interception logic.
6. Clicking the legal target sends `MISSION_NUKE` to the selected group. The
   Fighter cannot nuke and does nothing; the Tactical Nuke reaches `CvUnit::nuke`
   and executes the strike.

Thus the UI can present a target which is genuinely legal and executable while
its Nuke-specific hover is based on a unit that cannot perform the action.

VISIBLE CONSEQUENCES
--------------------
On an otherwise legal target, the head Fighter fails `canNukeAt`, so the helper
never enters the valid-nuke branch that displays the real
`nukeInterceptionChance`. Its subsequent explanation scan also uses the
Fighter's `iNukeRange=-1` rather than the Tactical Nuke's radius 1; it therefore
cannot be relied upon to explain the same affected area or legality that the
actual mission uses.

There is a deterministic sibling manifestation in `CvGame::updateNukeAreaOfEffect`.
That renderer also starts with:

    CvUnit const* pNuke = gDLL->UI().getHeadSelectedUnit();
    if (pNuke == NULL || !pNuke->isNuke())
        return false;

For the Fighter-headed Tactical-Nuke group, `canDoInterfaceModeAt` can mark the
hovered target legal, but the yellow nuclear AOE radius is never painted because
the renderer rejects the Fighter head before resolving the group's actual nuke.

This is separate from the earlier Queue-018 UNNUMBERED control about possible
STALE AOE-layer cleanup ordering after selection changes. F439 needs no unknown
EXE transition ordering: the mixed Fighter/Tactical-Nuke group is a fully
source-proven current producer, and its no-radius/help mismatch occurs while the
selection is stable.

ANCESTRY
--------
The head-selected-unit assumption is inherited.

Stock BtS/Civ4CE `CvGameTextMgr::getNukePlotHelp` fetches
`getHeadSelectedUnit()` internally and calls `isNukeVictim` on that unit. K-Mod
retains the same structure. Their selection-group nuke target test is already
an any-member loop, so the basic actor mismatch predates AdvCiv.

AdvCiv practical 2784,
160fa499a896cf595f74bcbde68b7ce41bedc466,
"Show best visible enemy interceptor in tile hover", refactors plot-mode help and
passes the previously fetched `pHeadSelectedUnit` explicitly into
`getNukePlotHelp`; it preserves rather than fixes the inherited assumption.

Later AdvCiv changes make the wrong actor more consequential:

- practical 3096, f28226d0dd6a779876d9737699f8d2334fe4ddbc,
  "AI considers nuke interception chance, UI displays it", adds the valid-target
  interception percentage using the passed `kNuke`;
- practical 3770, b6fce2cedca0f66e226c10bad755f8ff248e88e5,
  "Can't nuke cities with >=10% friendly nationality", expands the detailed
  invalid-target explanations;
- practical 3776, eed5e3b09739596c7aec19f0965443dbc804aaab,
  "Can't nuke cities culturally owned by a neutral party", further aligns
  runtime target legality and its explanations with observer/fog-of-war state.

All of those still receive the arbitrary selection head rather than the
selected unit which can execute Nuke at the target.

DUPLICATE RECONCILIATION
------------------------
The album contains nuclear AI/valuation/report findings, but no existing
`getNukePlotHelp` or mixed-air-selection root.

The closest prior control is Queue 018's unnumbered Nuke AOE-layer cleanup
ordering question. That control asked whether a radius already painted by a
selected nuke can become STALE after the EXE changes selection before clearing
the border layer; it remained unnumbered because callback ordering could not be
proved locally. F439 is different: a stable mixed group has a non-nuke head from
the outset, while group-wide mode legality comes from its Tactical Nuke. The
same renderer consequently paints NO radius for an actually executable target.
No EXE teardown/callback ordering is needed.

No reconciled KI/F root owns the TextMgr head-vs-executor mismatch or this
stable mixed-group AOE symptom.

REPAIR DIRECTION
----------------
Do not make Nuke targeting head-only. The game deliberately defines the
interface mode and mission for any eligible selected/group member.

Resolve a target-eligible selected nuke first, then use that SAME unit for all
Nuke hover calculations. Conceptually, before calling `getNukePlotHelp`, scan
the current selection/group for a unit satisfying the execution gates, most
importantly:

    pUnit->canMove() &&
    pUnit->canNukeAt(pUnit->getPlot(), kPlot.getX(), kPlot.getY(),
            pUnit->getTeam())

and pass that unit into `getNukePlotHelp`.

For an invalid target, where no selected nuke passes `canNukeAt`, explanation
text still needs a real selected nuclear unit rather than an arbitrary Fighter;
resolve a selected `isNuke()` unit (preferably using the same selection/group
ordering that mission execution uses) and let that nuke explain why the target
is invalid.

The same resolver principle should be applied to
`CvGame::updateNukeAreaOfEffect`: obtain the selected target-eligible nuke (or a
real selected nuke as appropriate) rather than requiring the head-selected unit
to be nuclear.

The neighboring current Air Bomb / Paradrop help already demonstrates the
right architectural direction: it asks the current selection for a
mission-eligible unit instead of assuming that the selection head is the actor.
A small shared Nuke-selection helper would avoid letting TextMgr and the AOE
renderer drift independently.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F439 / provisional KI#762 CONFIRMED.
F440 / provisional KI#763 UNUSED.
Latest physical checkpoint: C031-WIP144.
No repository source files modified.

NEXT
----
Continue Queue 003 from F440 unused. First check the sibling Rebase head-unit
assumption under mixed air groups, but keep it unnumbered unless the head affects
a target-specific quantity that disagrees with group execution. Then finish the
remaining live-unit/deal/UI-state residue and rerun the member-definition vs
explicit-control/finding reconciliation. Queue 003 remains late/saturated, so
clean closure is preferable to forcing a final finding.

===============================================================================
C031-WIP145 APPEND - F440 / KI#763 confirmed; Advanced Start advertised
per-city military-unit cap is actually enforced only as an empire-wide average
===============================================================================

STATUS
------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F440 / provisional KI#763 CONFIRMED.
F441 / provisional KI#764 UNUSED.
No repository source files modified.

F440 / provisional KI#763 — ADVANCED START UNIT HELP SAYS "MAXIMUM N UNITS PER
CITY", BUT THE PURCHASE GATE ONLY CAPS TOTAL MILITARY UNITS AT N * CITY COUNT;
MULTIPLE CITIES LET THE PLAYER CONCENTRATE MORE THAN N UNITS IN ONE CITY
-------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::getUnitDataForAS` appends the inherited Advanced Start warning
for every purchasable military-support unit:

    int iMaxUnitsPerCity = GC.getDefineINT("ADVANCED_START_MAX_UNITS_PER_CITY");
    if (iMaxUnitsPerCity >= 0 && GC.getInfo(eUnit).isMilitarySupport())
    {
        ...
        gDLL->getText("TXT_KEY_WB_AS_MAX_UNITS_PER_CITY", iMaxUnitsPerCity)
    }

The stock BtS text behind that key is explicit:

    "Maximum %d units per city"

The runtime gate in `CvPlayer::getAdvancedStartUnitCost`, however, does NOT
count units on the target city's plot. For an added military-support unit it
checks only:

    if (getNumMilitaryUnits() >= iMaxUnitsPerCity * getNumCities())
        return -1;

`getNumMilitaryUnits()` is player-wide. `getNumCities()` is player-wide. The
candidate `pPlot` / `pCity` is not used by this cap at all.

After this aggregate check, the city branch verifies only `pCity->canTrain(eUnit)`;
it does not impose any local unit-count limit. `CvPlayer::doAdvancedStartAction`
then calls this same cost function and, when the cost is nonnegative and points
are sufficient, directly creates the unit with `initUnit(eUnit, iX, iY, ...)`.
There is no later per-city cap.

CURRENT DETERMINISTIC PRODUCER
------------------------------
The base `ADVANCED_START_MAX_UNITS_PER_CITY` define is 2. Current SAS does not
override that define in its supplied XML, so the inherited value remains the
current configuration.

With two cities and zero military units, choose any military-support unit that
the target city can train and has enough Advanced Start points to buy.

The advertised rule says that the target city may contain at most 2 such units.
The actual checks for four consecutive purchases on the SAME city are:

    before purchase 1: 0 >= 2 * 2  -> false, allowed
    before purchase 2: 1 >= 2 * 2  -> false, allowed
    before purchase 3: 2 >= 2 * 2  -> false, allowed
    before purchase 4: 3 >= 2 * 2  -> false, allowed
    before purchase 5: 4 >= 2 * 2  -> true, denied

Thus one city can hold four purchased military units while every one of those
unit entries advertises "Maximum 2 units per city". With three cities, all six
may likewise be concentrated in a single city, and so on.

The runtime rule is therefore an empire-wide allowance equal to N times city
count — effectively N military units PER CITY ON AVERAGE — not a local maximum
of N units in each city.

CURRENT CONSEQUENCE
-------------------
This is player-visible and mechanically exploitable during Advanced Start.
The help claims a local placement restriction that the action does not enforce.
A player can therefore legally do exactly what the warning says is forbidden,
concentrating the whole empire allowance in one city.

The discrepancy is not a transient UI/cache issue. `doAdvancedStartAction`
revalidates through `getAdvancedStartUnitCost(..., pPlot)` immediately before
creating the unit, and that authoritative action-time check uses the same
aggregate formula.

ANCESTRY
--------
This mismatch is inherited from stock BtS rather than introduced by AdvCiv-SAS.

Civ4CE's reconstructed BtS source contains both halves unchanged:

* `CvPlayer::getAdvancedStartUnitCost` tests
  `getNumMilitaryUnits() >= iMaxUnitsPerCity * getNumCities()`;
* `CvGameTextMgr::getUnitDataForAS` displays
  `TXT_KEY_WB_AS_MAX_UNITS_PER_CITY`;
* stock BtS `CIV4GameText_BTS.xml` defines the English text as
  `Maximum %d units per city`;
* stock `GlobalDefines.xml` sets `ADVANCED_START_MAX_UNITS_PER_CITY` to 2.

K-Mod 1.46 retains the same aggregate runtime gate and the same TextMgr warning.
Base AdvCiv 1.14 also retains both pieces unchanged. Current SAS inherits the
same semantics; its recent change in this helper only caches/hoists the define
lookup and does not alter behavior.

Classification: inherited BtS Advanced Start rule/help mismatch, still live in
K-Mod, AdvCiv and current AdvCiv-SAS.

DUPLICATE RECONCILIATION
------------------------
The album contains several Advanced Start findings, especially the deep
`CvPlayerAI` automation roots F370-F372 and the later generic-cost/visibility
checks, but none owns this player-facing local-vs-global military-unit cap.

F372 concerns AI automation terminating later unit-role passes after one
unaffordable candidate. F440 concerns the authoritative human/AI purchase-cost
gate itself disagreeing with the static Advanced Start unit warning about the
spatial scope of the cap. They are independent code roots and consequences.

No existing reconciled KI/F entry describes `ADVANCED_START_MAX_UNITS_PER_CITY`,
`TXT_KEY_WB_AS_MAX_UNITS_PER_CITY`, or a per-city-vs-empire-wide military-unit
cap.

REPAIR DIRECTION
----------------
The correct repair depends on intended design; the current name and stock text
both strongly suggest that a true local per-city limit was intended, whereas
the implementation behaves like an empire-wide average allowance.

If the intended rule really is LOCAL "maximum N per city", enforce it on the
candidate plot/city in `getAdvancedStartUnitCost` by counting relevant existing
military units there and rejecting once that local count reaches N. The action
already has `pPlot`/`pCity`, so this can be authoritative at purchase time.

If the aggregate allowance is deliberate and should be preserved, then the
warning and define naming need to stop claiming a local cap. A conservative UI
repair would describe the actual rule as an empire maximum derived from city
count (e.g. "Maximum N military units per city on average" or, better, show the
current total allowance explicitly).

Do not merely change the TextMgr number from N to N * city count while retaining
"per city"; that would still misstate the spatial rule.

POST-F440 CONTROLS ALREADY CLOSED BEFORE THIS CHECKPOINT
--------------------------------------------------------
The post-WIP144 residue also established the following clean controls without
allocating another root:

* `getDealString`: city trades are one-shot and are not stored in active deals,
  so `TRADE_CITIES` cannot later dereference a traded-away/razed city through
  this active-deal renderer. Its cancellation countdown is deal-wide by design.
* `buildFinanceForeignIncomeString`: the visible Finance row and its hover use
  the same net `getGoldPerTurn()` quantity; per-player positive/negative entries
  are a breakdown, not an F438-style source mismatch.
* `setCycleUnitHelp`: the unit comes from `getCycleButtonUnit`, which resolves
  the exact prospective cycle group; worker cycling requires a group with a
  worker with moves and the button resolver preferentially returns a worker.
* `getRebasePlotHelp`: the inherited head-unit seam no longer controls a
  meaningful target-specific quantity because the old capacity warning was
  moved into generic plot help.
* `eventTechHelp`: its beaker arithmetic and caps match the authoritative event
  research-progress mutation.
* `setHealthChangeBuildActionHelp`: integer and fractional good/bad-health
  deltas come from monotone same-sign buckets; the explicit bad-health sign flip
  cannot create an opposing-sign display.
* `appendFirstStrikes` and `setHurtUnitStrength`: their execution/display guards
  and endpoint formatting were reconciled without a current contradiction.
* trade-screen civic icons: the apparent `NO_CIVIC` indexing concern is masked
  by the living-player initialization invariant that every civic option has an
  initial civic before this surface is reachable.
* the current-SAS-vs-base-AdvCiv TextMgr delta inventory contains no unreviewed
  zero-mention semantic SAS rewrite; remaining unmatched deltas are lookup/
  define caching, signature/layout refactors or debug-only helpers.

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F440 / provisional KI#763 CONFIRMED.
F441 / provisional KI#764 UNUSED.
Latest physical checkpoint: C031-WIP145.
No repository source files modified.

NEXT
----
Continue Queue 003 from F441 unused. Finish the remaining inherited live-state
residue (especially the small Advanced Start/WB helpers now that the unit helper
was reopened, deal/trade/UI-state helpers and any uncompared sorting contracts),
then rerun the member-definition versus explicit-control/finding reconciliation.
Queue 003 is late and saturated; if no independent F441 survives, freeze a
clean closure checkpoint rather than forcing another root.

END C031-WIP145


===============================================================================
C031-WIP146 APPEND - F441 / KI#764 confirmed; Advanced Start unit palette keeps
military entries after every placement target has become illegal at the cap
===============================================================================

CURRENT STONE
-------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` remains OPEN.
32/169 unique files complete.
F441 / provisional KI#764 CONFIRMED.
F442 / provisional KI#765 UNUSED.
No repository source files modified.

-------------------------------------------------------------------------------
F441 / provisional KI#764 — ADVANCED START UNIT PALETTE USES A GENERIC COST
QUERY THAT SKIPS THE MILITARY CAP, SO THE REFRESHED PALETTE CAN ADVERTISE AN
ITEM THAT IS ILLEGAL ON EVERY ACTUAL TARGET
-------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::getUnitDataForAS`, the exported builder for the Advanced Start
unit palette, decides whether to include a civilization unit by asking:

    int iCost = kActivePlayer.getAdvancedStartUnitCost(eUnit, true);
    if (iCost > 0)
        mapUnitList.push_back(...);

The omitted plot argument means `pPlot == NULL`.

`CvPlayer::getAdvancedStartUnitCost` deliberately has separate generic and
plot-specific branches. In the generic (`pPlot == NULL`) add branch it only asks
whether at least one city can train the unit:

    bool bValid = false;
    FOR_EACH_CITY(pLoopCity, *this)
    {
        if (pLoopCity->canTrain(eUnit))
        {
            bValid = true;
            break;
        }
    }
    if (!bValid)
        return -1;

The Advanced Start military-unit cap is NOT checked there. It exists only in
the `pPlot != NULL` branch:

    if (bAdd)
    {
        int iMaxUnitsPerCity = ADVANCED_START_MAX_UNITS_PER_CITY;
        if (iMaxUnitsPerCity >= 0 &&
            GC.getInfo(eUnit).isMilitarySupport() &&
            getNumMilitaryUnits() >= iMaxUnitsPerCity * getNumCities())
        {
            return -1;
        }
        ...
    }

Thus the same cost API has the following reachable state:

    getAdvancedStartUnitCost(militaryUnit, true, NULL)  > 0
    getAdvancedStartUnitCost(militaryUnit, true, cityA) == -1
    getAdvancedStartUnitCost(militaryUnit, true, cityB) == -1
    ... for every legal city/plot target

CURRENT-SAS REPRODUCTION
------------------------
Current SAS inherits `ADVANCED_START_MAX_UNITS_PER_CITY = 2` from the stock
BtS global define (the SAS tree contains no XML override of that define).

Use an ordinary one-city Advanced Start position with a trainable military unit
and enough points. Buy two military units. At that point:

    getNumMilitaryUnits() == 2
    getNumCities() == 1
    cap threshold == 2 * 1 == 2

For any actual placement plot, the plot-specific cost reaches the cap test and
returns -1.

But the no-plot generic cost used by `getUnitDataForAS` does not execute that
check. The city can still `canTrain(eUnit)`, so the generic call returns its
normal positive cost and TextMgr re-adds the unit to `mapUnitList`.

With two cities the same shape occurs at four total military units, etc. This
finding does not depend on F440's separate question of whether that aggregate
formula should really be local per-city: under the CURRENT aggregate runtime
rule itself, reaching the threshold makes all new military placements illegal,
yet the generic palette query still reports them available.

UI / REFRESH PROOF FROM THE SUPPLIED BtS/Civ4CE MATERIAL
---------------------------------------------------------
The supplied Civ4CE/BtS `CvWorldBuilderScreen.py` confirms the two roles of the
cost API rather than leaving them as an EXE guess:

* `refreshASItemCost` asks `getAdvancedStartUnitCost(activeUnit, true,
  currentPlot)` for the selected map target;
* left-click handling again requires that same plot-specific call to be != -1
  before sending `ADVANCEDSTARTACTION_UNIT`;
* after a successful unit action, `CvPlayer::doAdvancedStartAction` sets
  `Advanced_Start_DIRTY_BIT` for the active player;
* `CvWorldBuilderScreen.updateScreen` responds to that dirty bit by calling
  `refreshAdvancedStartTabCtrl(true)`;
* `refreshAdvancedStartTabCtrl` reinitializes the Advanced Start control, whose
  exported unit data are supplied by `CvGameTextMgr::getUnitDataForAS`.

Therefore the cap is not merely reached while an old stale list remains on
screen. The purchase explicitly dirties and rebuilds the interface; the
rebuilt unit list still includes the capped military unit because its generic
cost remains positive. Moving/hovering over a real city then gives no purchase
cost, and clicking is rejected because the plot-specific cost is -1.

This is a concrete dead-palette state: an entry advertised by the refreshed
Advanced Start palette has no legal target anywhere.

RUNTIME ACTION CONTROL
----------------------
`CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_UNIT)` rechecks:

    int iCost = getAdvancedStartUnitCost(eUnit, bAdd, pPlot);
    if (bAdd && iCost < 0)
        return;

so the stale availability cannot accidentally bypass the cap. The defect is
UI/API availability disagreement, not an extra-unit purchase exploit.

ANCESTRY
--------
The split is inherited:

* BtS/Civ4CE: generic null-plot unit cost checks `canTrain`, while the max-unit
  check is inside the non-null plot branch; `getUnitDataForAS` uses the generic
  call.
* K-Mod 1.46: same structure.
* Base AdvCiv 1.14: same structure (with AdvCiv cost normalization changes,
  but not a repair of this availability split).
* Current AdvCiv-SAS: same semantic structure; recent SAS edits here only cache
  relevant defines / clean implementation details.

Classification: inherited BtS Advanced Start generic-vs-target availability
mismatch, retained by K-Mod, AdvCiv and SAS.

DUPLICATE RECONCILIATION
------------------------
No existing album root owns this contract.

* F417 is the opposite UI-filter shape: a legally zero-cost Visibility action is
  omitted because TextMgr tests `iCost > 0` instead of the API's -1 sentinel.
* F440 concerns the text "Maximum N units per city" contradicting the current
  aggregate runtime formula.
* Queue-001 F372 concerns AI Advanced Start stopping later unit-role passes after
  one selected unit is unaffordable.

F441 instead concerns `getUnitDataForAS` accepting the generic no-plot unit cost
as proof that SOME placement remains legal when the military cap is only tested
by plot-specific calls. Its reproduction persists even if the F440 wording is
changed to describe the aggregate cap accurately.

REPAIR DIRECTION
----------------
Under the current aggregate-cap policy, the minimal semantic fix is to make the
cap independent of `pPlot`: for an add of a military-support unit, reject the
cost once

    getNumMilitaryUnits() >= maxUnits * getNumCities()

before the generic/non-generic branch split (or perform the same check in the
null branch). Then `getUnitDataForAS` naturally stops exporting military entries
when no purchase can succeed anywhere.

If F440 is instead repaired by implementing a true local per-city cap, the
generic query cannot simply use the empire total. It should answer "available"
only when at least one eligible city both can train the unit AND has remaining
local military capacity. The important invariant is:

    generic add cost >= 0
        =>
    at least one current target can return a legal plot-specific add cost

for constraints that make the action globally impossible.

-------------------------------------------------------------------------------
POST-F440 / PRE-F441 CONTROLS CLOSED IN THIS TRANCHE
-------------------------------------------------------------------------------

* `getDealString`: city trades are one-shot in `CvDeal::startTrade` and are not
  retained as saved deal items, so the active-deal renderer cannot later hold a
  stale traded-city pointer. Its cancellation countdown is deal-wide by design.

* `buildFinanceForeignIncomeString`: the visible Finance foreign-GPT row and its
  hover both use the player's net `getGoldPerTurn()` quantity; per-player lines
  are a decomposition, not a second definition.

* `setCycleUnitHelp`: receives the exact prospective cycle unit/group returned
  by the cycle-selection machinery. The worker shortcut is only entered for a
  group proven to contain a movable worker, and the group button resolver
  deliberately prefers a worker there.

* Plot-list comparator hidden-nationality caveat retained as robustness debt:
  visual-owner grouping can combine same-team hidden-nationality units from
  different actual players, so the comment that unit IDs are unique because
  they have the same owner is stronger than the actual visual grouping
  invariant. Equal same-type/player-local IDs merely become equivalent sort
  keys in the current reachable data; no release-visible ordering failure was
  proven and no root was assigned.

CURRENT STONE AFTER F441
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F441 / provisional KI#764 latest confirmed.
F442 / provisional KI#765 UNUSED.
Latest physical checkpoint: C031-WIP146.
No repository source files modified.

NEXT
----
Continue Queue 003 from F442 unused. Finish the remaining inherited live-state
residue, then repeat the member-definition / subsystem-control reconciliation.
Do not manufacture a late finding: if the residue closes cleanly, freeze the
next WIP with F442 unused and use that saturation evidence toward a defensible
Queue-003 closure decision.

END C031-WIP146


===============================================================================
C031-WIP147 — LATE WHOLE-FILE COVERAGE TRANCHE; F442 REMAINS UNUSED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP146 with F441/KI#764 confirmed and F442/KI#765 unused.
This checkpoint deliberately preserves a clean saturation tranche rather than
manufacturing a late root.

TECHINFO RUNTIME-EFFECT / HELP-SURFACE RECONCILIATION
-----------------------------------------------------
A field-level audit compared current `CvTechInfo` gameplay effects actually
processed by `CvTeam::processTech` / `CvTeam::setHasTech` with the current
`CvGameTextMgr::setTechHelp` builders.

The following live effects all have corresponding tech-help coverage:

* route movement changes -> `buildMoveString`;
* domain extra moves -> `buildDomainExtraMovesString`;
* feature-production modifier -> `buildFeatureProductionString`;
* worker-speed modifier -> `buildWorkerRateString`;
* per-city trade routes -> `buildTradeRouteString`;
* health / happiness -> `buildHealthRateString` / `buildHappinessRateString`;
* first free unit / first free techs -> `buildFreeUnitString` /
  `buildFreeTechString`;
* extra water line of sight -> `buildLOSString`;
* map centering / map reveal -> `buildMapCenterString` / `buildMapRevealString`;
* map, tech, gold, open-borders, defensive-pact, permanent-alliance and vassal
  trading unlocks -> the corresponding trading builders;
* bridge building, irrigation spread, ignore-irrigation and water work -> the
  corresponding builders;
* commerce-rate modifiers and specialist-extra commerce -> the K-Mod
  `setCommerceChangeHelp` calls;
* commerce flexibility -> `buildAdjustString`;
* terrain / river trade -> `buildTerrainTradeString` /
  `buildRiverTradeString`;
* improvement tech-yield changes -> `buildYieldChangeString`;
* special-building availability -> `buildSpecialBuildingString`;
* bonus reveal, civic/process/build/unit/building unlock surfaces -> the
  corresponding reveal/unlock builders;
* religion/corporation first-discovery effects -> the found-organization
  builders;
* `CvTechInfo::NoFearForSafety` -> `buildNoFearForSafetyString`;
* obsolete buildings, bonuses, special buildings and units -> the consolidated
  obsolete blocks near the start of `setTechTradeHelp`.

`CvTeam::setHasTech` also performs consequences that are not independent
missing stat bullets: advancing member era to the tech's Era is the normal era
classification itself; reveal/update/rebuild work applies the already-described
map/bonus/build effects; research-queue/AI notifications and replay/event hooks
are state propagation; and first-discovery religion/free-unit/free-tech effects
are covered above.

The remaining `CvTechInfo` fields are metadata / AI / economy inputs rather
than hidden tech-granted player rules (`Advisor`, AIWeight, AITradeModifier,
research/Advanced-Start cost, AssetValue, PowerValue, flavors, grid position,
sounds/quote, GoodyTech/Disable). `bTrade` and `bRepeat` are explicitly surfaced
by current SAS's custom builders. Tech prerequisites are rendered by the tech
tree/prerequisite family already audited.

No F442 emerged from the TechInfo field audit.

ROUTE TECH-MOVEMENT CONTROL
---------------------------
`buildMoveString` was rechecked against the authoritative route movement
arithmetic rather than accepted from field-name symmetry alone. Current Road is
30/30 movement/flat movement and Engineering contributes TechMovementChange
-10. Under the game's route-cost formula this changes the common 2-plots-per-
move Road rate to 3, matching the helper's displayed +1 movement result.
Current Railroad has no separate tech-movement change. No current flat-cost
case was found that makes the helper advertise a false whole-move increment.

FIRST-TO-TECH FREE-UNIT CONTROL
-------------------------------
`buildFreeUnitString` resolves the active player's civilization-specific unit
through the same `CvPlayer::getTechFreeUnit` path used by `CvTeam::setHasTech`.
That runtime helper also handles the No-Espionage suppression. Current first-
free-unit classes are Great-Person classes; no current civilization override of
those classes was found that creates a help/runtime disagreement. This closes
the late zero-literal-mention helper rather than creating F442.

VASSAL-REVOLT / WAR-WEARINESS CONTROLS
--------------------------------------
The earlier war-weariness decomposition remains consistent with the live
formula. `setVassalRevoltHelp` was also reconciled directly with
`CvTeam::canVassalRevolt` / `isLossesAllowRevolt` for current active branches.
One apparent master-land denominator difference is structurally present
(runtime master total-land defaults to including vassals while the help uses
`getTotalLand(false)`), but the controlling current stock/AdvCiv
`VASSAL_REVOLT_MASTER_LOSSES_FACTOR` is 0, so that branch is data-dormant and
cannot support a current F442.

ADVANCED-START SIBLING CONTROL AFTER F441
-----------------------------------------
The generic-vs-target sweep was extended to city / population / culture /
building / route / improvement palette builders.

Routes, improvements and city tools intentionally use generic "unlocked tool"
palette semantics and defer geometry/ownership to the target-specific query.
This differs from unit/building generic queries, which explicitly try to prove
an existentially valid train/construct location. F441 is therefore not being
mechanically generalized into every generic Advanced-Start tool.

Current Build XML also maps each purchasable route/improvement through a single
build definition, so the shared tech-prerequisite loops do not create a live
multi-build/all-tech ambiguity in current SAS.

LATE ZERO-MENTION MEMBER CLASSIFICATION
---------------------------------------
A fresh definition-to-album literal inventory found 226 unique TextMgr member
names. Literal zero mention is not treated as proof of no review: the album
usually records a subsystem/root rather than every formatting helper.

The largest zero-mention definitions are now classified as follows:

* `setPlotListHelpDebug` and the large `setPlotHelpDebug_*` family are explicit
  debug-mode / modifier-key diagnostics, not normal gameplay-rule help;
* `buildTechTreeString` belongs to the compact tech-tree family already audited
  against current prerequisite/research-modifier assumptions;
* `getGlobeLayerName`, font-symbol assignment helpers and WB data enumerators
  are enum/resource plumbing rather than reconstructed live gameplay values;
* `setCommerce*ChangeHelp`, `setYield*ChangeHelp`, `setValue*ChangeHelp` are
  shared formatting primitives whose authoritative callers were covered in the
  Building Actual Effects / civic / tech / specialist audits;
* `setBuildingAdditionalHealth/Yield/Commerce/GreatPeopleHelp` delegate to the
  city's authoritative projected-building helpers. Their parent Actual Effects
  framework and its known disorder defects are already owned by F409/F410;
* `appendUniqueDesc`, `parsePlayerTraits`, short-trait formatters and similar
  helpers are direct description/list formatters;
* `buildFoundReligionString`, `buildFreeUnitString`, `buildFreeTechString`,
  bonus/yield/promotion tech builders are covered by the completed TechInfo
  audit;
* city billboard production/color helpers read direct production state or
  return fixed UI colors; the only proved signed billboard defect remains F436;
* the Advanced-Start improvement builder is covered by the sibling control
  above; visibility and units retain F417/F441 respectively.

This substantially reduces the remaining unclassified live-state residue.

CURRENT STONE AFTER C031-WIP147
-------------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F441 / provisional KI#764 latest confirmed.
F442 / provisional KI#765 UNUSED.
Latest physical checkpoint: C031-WIP147.
No repository source files modified.

NEXT
----
Continue the final definition/member-to-control reconciliation. Concentrate on
any still-unclassified normal-game live-state helpers rather than debug/WB or
pure formatting utilities. If that reconciliation remains clean, decide whether
Queue 003 has reached defensible closure; do not allocate F442 merely to avoid
closing with an unused root.

END C031-WIP147


===============================================================================
C031-WIP148 — FINAL RESIDUE CONTINUATION; F442 / KI#765 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP147 with F441/KI#764 confirmed and F442/KI#765 unused.
The intended next pass was the final assertion/reconstructed-quantity and
unclassified normal-game live-state reconciliation before deciding whether
Queue 003 had reached defensible closure.

ASSERTION / RECONSTRUCTED-QUANTITY PASS — CONTROLS
--------------------------------------------------
The explicit `FAssert` inventory in current `CvGameTextMgr.cpp` was mapped back
to its containing helpers. The large duplicated-runtime decompositions are
already either exact controls or owned findings:

* production / commerce / yield / Great-Person decompositions carry direct
  equality assertions against the authoritative city results; their known
  disorder/Actual-Effects exceptions are already F409/F410 and neighbours;
* trade-route profit/modifier reconstruction asserts against
  `totalTradeModifier` and `calculateTradeProfit` and was independently closed;
* espionage modifier/final-cost reconstruction asserts against
  `getEspionageMissionCostModifier` / `getEspionageMissionCost`; the proved
  remaining espionage defects are effect-description branches rather than the
  arithmetic product;
* anger/happiness totals assert against the city totals and the earlier health
  analogue did not generalize to happiness;
* compact tech-tree research assumptions and Great-General/score controls were
  already separately reconciled.

One adjacent inherited typo was also isolated but remains current-data dormant:
`eventGoldHelp`'s ordinary-player negative random-Gold range passes
`-iGold1, iGold2` to `TXT_KEY_EVENT_GOLD_RANGE_LOST`, unlike the player-to-player
branch which correctly passes both endpoints negated. BtS, K-Mod, AdvCiv and SAS
all retain this. However every current SAS event with `iRandomGold != 0` has
positive base `iGold` and `iTechCostPercent = 0`, so `CvPlayer::getEventCost`
cannot reach that bad negative-range branch in the shipped ruleset. Retain as
latent robustness debt, not F442.

-------------------------------------------------------------------------------
F442 / KI#765 — QUEUED BUILDING HOVER APPLIES THE "START ANOTHER" BUILDING-
CLASS PREREQUISITE FORMULA TO THE BUILDING THAT IS ALREADY CONTINUING
-------------------------------------------------------------------------------
Status: CONFIRMED
Primary file family: `CvGameCoreDLL/CvGameTextMgr.cpp` +
`CvGameCoreDLL/CvDLLWidgetData.cpp` + `CvGameCoreDLL/CvPlayer.cpp` + city-screen
queue caller
Classification: K-Mod v1.39 UI integration regression retained by Base AdvCiv
1.14 and current SAS; ordinary current gameplay.

ROOT
----
K-Mod extended `CvPlayer::getBuildingClassPrereqBuilding` with an `iExtra`
argument so the requirement for a repeatable building class (most visibly a
Cathedral) can account for other buildings of that target class already under
construction.

Runtime deliberately distinguishes STARTING a new building from CONTINUING an
already accepted queue order. In `CvPlayer::canConstruct`:

    int iMaking = getBuildingClassMaking(eBuildingClass);
    ...
    getBuildingClassPrereqBuilding(
        eBuilding, ePrereqClass, bContinue ? 0 : iMaking)

Thus:

* starting a new Cathedral counts all Cathedrals currently making, because the
  new order must satisfy the requirement for one additional Cathedral;
* continuing a Cathedral already in the queue passes `iExtra = 0`, because that
  order has already passed its start gate and must not be reclassified as yet
  another Cathedral being started.

`buildBuildingRequiresString` has no corresponding continuation context. For
ordinary city help it always does:

    getBuildingClassPrereqBuilding(
        eBuilding, ePrereqClass,
        kPlayer.getBuildingClassMaking(targetBuildingClass))

(the Civilopedia path is a separate special case).

That is correct for a production-choice button representing a NEW order, but
wrong for an existing queue item.

CALLER PROOF
------------
The current city production queue is a direct ordinary caller:

* `CvMainInterface.py` renders every queued order row with
  `WIDGET_HELP_SELECTED` and passes its queue index;
* `CvDLLWidgetData::parseSelectedHelp` retrieves that exact order via
  `getOrderFromQueue`;
* for `ORDER_CONSTRUCT`, it calls
  `setBuildingHelpActual(..., pHeadSelectedCity)`;
* `setBuildingHelpActual` calls `buildBuildingRequiresString` without any flag
  saying that the hovered item is an already-accepted/continuing order.

Therefore the bad formula is not limited to Civilopedia/API use and does not
require an EXE-ordering assumption.

CURRENT SAS REPRODUCTION
------------------------
Current SAS Cathedrals are non-limited building classes with a raw requirement
of 2 Temples. On `WORLDSIZE_STANDARD`,
`iBuildingClassPrereqModifier = 38`, so the authoritative scaled prerequisite is:

    ceil(2 * 138 / 100) = 3 Temples per Cathedral step.

With zero completed Cathedrals and exactly 3 Temples:

1. Starting the first Cathedral is legal:

       getBuildingClassMaking(Cathedral) = 0
       required = 3 * (0 built + 0 making + 1) = 3

2. Once the order is pushed, `changeBuildingClassMaking(..., +1)` makes:

       getBuildingClassMaking(Cathedral) = 1

3. Runtime continuation still checks with `bContinue = true`, hence
   `iExtra = 0`, and continues to require only 3 Temples.

4. Hovering that queued Cathedral instead calls the TextMgr formula with
   `iExtra = getBuildingClassMaking(...) = 1`, producing:

       required = 3 * (0 built + 1 making + 1) = 6

The Unofficial Patch localization used by the helper is warning-coloured and
explicitly says:

    Requires Temple (3/6 Total)

although the same queued Cathedral is legal to continue with the 3 Temples the
player already has.

The discrepancy grows with simultaneous accepted orders. Runtime continuation
passes zero `iExtra` for each existing order, while the shared hover counts all
currently-making target buildings as though the hovered order were a fresh
additional start.

ANCESTRY / INTENT PROOF
-----------------------
Stock BtS did not have this exact regression: its TextMgr used the older
prerequisite calculation without K-Mod's under-construction `iExtra` extension.

K-Mod v1.39 explicitly introduced the relevant UI behaviour. Its changelog says:

    "The mouseover text for cathedrals will now include the number of cathedrals
     currently under construction when calculating the required number of
     temples. (ie. it will report how many temples are required to start the
     construction of another cathedral.)"

That stated intent is correct for a NEW production choice. The implementation
placed the calculation in the shared building-help path, which is also used for
an EXISTING queue entry, and did not carry the runtime `bContinue` distinction
through the UI call chain.

Base AdvCiv 1.14 retains the K-Mod formula; current SAS retains it as well.

DUPLICATE RECONCILIATION
------------------------
The earlier Queue-003 Building Requirement reconciliation checked missing
`canConstruct` predicates, current production-button visibility, and the
Civilopedia world-size prerequisite range. It did not cover the separate
NEW-ORDER-vs-CONTINUING-ORDER arithmetic distinction.

No existing album root was found for Cathedral/building-class `iMaking` leaking
into an already-queued building's hover. This is therefore independent of the
previous building-help controls and the Actual Effects findings.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Carry an explicit continuation/queue-order context to the prerequisite helper.
The important arithmetic contract is:

* NEW production choice: pass current `getBuildingClassMaking(targetClass)`,
  matching `canConstruct(..., bContinue=false)`;
* EXISTING queue order: pass `0`, matching
  `canConstruct(..., bContinue=true)`;
* Civilopedia: preserve its existing context-free/base-requirement behaviour.

Do not merely subtract one from `getBuildingClassMaking`. Runtime continuation
ignores all currently-making siblings for the already accepted order; its exact
contract is `iExtra = 0`.

A narrow implementation would let `parseSelectedHelp` mark the
`ORDER_CONSTRUCT` help as continuing (it knows the row is an existing queue
order), thread that boolean through `setBuildingHelpActual` /
`buildBuildingRequiresString`, and select the same `iExtra` as runtime.

CURRENT STONE AFTER F442
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F442 / KI#765 latest confirmed.
F443 / provisional KI#766 UNUSED.
Latest physical checkpoint: C031-WIP148.
No repository source files modified.

NEXT
----
Continue Queue 003 from F443 unused. Finish the remaining assertion/member
reconciliation and specifically challenge the neighbouring production-queue
unit/project help for any analogous START-vs-CONTINUE state leak; do not assume
one exists. If those siblings and the residual normal-game member inventory
close cleanly, perform the final duplicate/coverage reconciliation and decide
whether Queue 003 can be closed defensibly before advancing to Queue 004.

END C031-WIP148


===============================================================================
C031-WIP149 — QUEUED PRODUCTION-TURNS HOVER INDEX LOSS; F443 / KI#766 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP148 with F442/KI#765 confirmed and F443/KI#766 unused.
The first sibling check showed that Unit/Project requirement text does not
mechanically inherit F442's building-class START-vs-CONTINUE prerequisite bug.
The adjacent production-turns display, however, exposed a separate queue-index
contract mismatch shared by Units, Buildings and Projects.

-------------------------------------------------------------------------------
F443 / KI#766 — PRODUCTION-QUEUE ROW HOVER DISCARDS THE ROW'S ACTUAL QUEUE
INDEX AND RECOMPUTES TURNS AS A HYPOTHETICAL FRONT/END INSERTION
-------------------------------------------------------------------------------
Status: CONFIRMED
Primary file family: `CvGameCoreDLL/CvDLLWidgetData.cpp` +
`CvGameCoreDLL/CvGameTextMgr.cpp` + `CvGameCoreDLL/CvCity.cpp` + city-screen
queue caller
Classification: inherited BtS UI integration defect retained by K-Mod, Base
AdvCiv 1.14 and current SAS; ordinary current gameplay. AdvCiv's advc.064b
queued-overflow predictor increases the importance of the lost queue index but
did not introduce the root.

ROOT
----
The city production queue already knows the exact queue index of every row.
Current `CvMainInterface.py` uses that exact `i` when rendering its visible
turn count:

    getUnitProductionTurnsLeft(eUnit, i)
    getBuildingProductionTurnsLeft(eBuilding, i)
    getProjectProductionTurnsLeft(eProject, i)

The same row is assigned `WIDGET_HELP_SELECTED` with `iData1 = i`.
`CvDLLWidgetData::parseSelectedHelp` therefore receives the exact row index and
uses it to retrieve the exact `OrderData` through `getOrderFromQueue(i)`.

But after identifying the order, `parseSelectedHelp` discards the index and
calls the ordinary generic help functions:

    setUnitHelp(..., pHeadSelectedCity)
    setBuildingHelpActual(..., pHeadSelectedCity)
    setProjectHelp(..., pHeadSelectedCity)

Those generic functions were designed primarily for prospective production
choices. Their turns-left call chooses a simulated insertion position from the
keyboard state:

    pCity->getProductionTurnsLeft(eThing,
        (GC.ctrlKey() || !GC.shiftKey()) ? 0 :
        pCity->getOrderQueueLength())

Thus an ordinary no-modifier hover over an EXISTING queue row recomputes the
turns as though the item were a hypothetical order at queue position 0. Holding
Shift instead simulates an append at `getOrderQueueLength()`. Neither path uses
the actual existing row index which `WIDGET_HELP_SELECTED` already supplied.

WHY THE INDEX IS SEMANTIC
-------------------------
`CvCity::getProductionTurnsLeft(Unit/Building/Project, iNum)` does not treat
`iNum` as decorative UI data.

For each production kind it finds the first queued order of the same type and
only credits the city's stored production when either no such queued order
exists or that first occurrence is exactly `iNum`:

    if (iFirstBuildingOrder == -1 || iFirstBuildingOrder == iNum)
        iProduction += getBuildingProduction(eBuilding);

(and equivalently for Units and Projects).

This is correct: stored production belongs to the already-existing first queued
occurrence, not to a hypothetical duplicate inserted before it.

AdvCiv's advc.064b additionally makes queue position affect first-turn overflow
prediction:

* `iNum == 0` uses current-production overflow/feature-production semantics;
* `iNum == 1` can predict overflow from the head order when it is about to
  complete;
* later queued positions deliberately do not attempt that overflow prediction.

So the exact queue index is authoritative input to the displayed estimate.

ORDINARY CURRENT REPRODUCTION
-----------------------------
A particularly simple reproduction needs no unusual modifiers and no EXE
assumption:

1. Start a production item B and accumulate some production into it.
2. Switch the city to another item A. Civ4 retains B's invested production.
3. Queue B behind A.
4. The visible B queue row calls `get*ProductionTurnsLeft(B, 1)` and therefore
   recognises that B's first queued occurrence is index 1 and credits B's stored
   production.
5. Hover that same B row with no modifier keys. `parseSelectedHelp` identifies
   row 1, but the generic TextMgr helper calls
   `getProductionTurnsLeft(B, 0)`. Because B's first queued occurrence is 1,
   the index-0 hypothetical order receives none of B's stored production.

Illustrative current arithmetic with a 100-production item, 50 production
already stored and a steady 10 production/turn:

    queue row, real index 1:  5 turns
    row hover, simulated 0:  10 turns

Current SAS has ordinary 100-production buildings (e.g. Forge), so this shape
does not depend on a hypothetical XML cost. The exact numeric difference in a
real city follows that city's production modifiers/rate, but the source
mismatch is deterministic whenever enough stored production changes the
rounded estimate.

A second AdvCiv-specific manifestation can occur for the row immediately after
a head order that is about to finish: the exact `iNum == 1` row prediction can
include predicted head-order overflow, while the queue-row hover's generic
`iNum == 0` path instead treats the hovered item as current production.

CALLER / UI CONTRADICTION
-------------------------
This is unusually direct because the same visual queue row can expose both
values simultaneously:

* its right-side `(N)` is computed in Python using the exact queue index;
* hovering that row calls TextMgr through `WIDGET_HELP_SELECTED` and can show a
  different `N Turns` computed from index 0 / queue end.

The hover is therefore not merely less precise than runtime. It can contradict
the number printed next to the exact item being hovered.

ANCESTRY
--------
Stock BtS already has both halves of the root:

* `CvMainInterface.py` renders queue Unit/Building/Project turns using the exact
  row index `i`;
* `parseSelectedHelp` retrieves that row but calls the generic help functions;
* those generic TextMgr functions choose 0 or queue length from Ctrl/Shift
  state instead of the selected row index.

Stock BtS `CvCity::getProductionTurnsLeft(eThing, iNum)` already uses `iNum` to
decide which occurrence receives stored production, so the bug is live in the
original contract.

K-Mod retains that flow. Base AdvCiv 1.14 and current SAS retain it as well.
AdvCiv advc.064b improves queued-order prediction and adds special `iNum == 1`
overflow handling, but the `parseSelectedHelp` caller was never updated to pass
the row index into the generic help functions.

DUPLICATE RECONCILIATION
------------------------
The album contains prior production-turn and production-decay work, including
sentinel handling and the separate game-speed production-decay amount defect.
None covers an existing production queue row discarding its own queue index.

F442 is adjacent but independent:

* F442 concerns Building-class prerequisite arithmetic for an already accepted
  Building order (START another vs CONTINUE this one);
* F443 concerns the turns-left projection for Unit, Building and Project queue
  rows and the lost `iNum` argument.

No existing root was found for `parseSelectedHelp` / `WIDGET_HELP_SELECTED`
recomputing an existing queued item's turns at position 0 or queue end.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve the generic production-button semantics, but let existing queue-row
help carry its actual queue index.

A narrow design is to add an optional queue-position argument (defaulting to a
sentinel meaning "prospective production choice") to the Unit / Building /
Project city-help path:

* production-choice buttons with no queue position continue using the existing
  Ctrl/Shift 0-vs-end insertion simulation;
* `parseSelectedHelp`, which already owns `widgetDataStruct.m_iData1`, passes
  that exact index for `ORDER_TRAIN`, `ORDER_CONSTRUCT` and `ORDER_CREATE`;
* the turns-left line then calls `getProductionTurnsLeft(eThing, iQueuePos)`
  for an existing order.

This should be coordinated with F442's queue/continuation context so that one
explicit "existing queue order" path can carry both the correct prerequisite
semantics and the correct turns position rather than adding unrelated ad-hoc
special cases.

CURRENT STONE AFTER F443
------------------------
Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` — OPEN.
32/169 unique files complete.
F443 / KI#766 latest confirmed.
F444 / provisional KI#767 UNUSED.
Latest physical checkpoint: C031-WIP149.
No repository source files modified.

NEXT
----
Continue Queue 003 from F444 unused. Finish the residual assertion/member
reconciliation after the production-queue family, then run the final duplicate
and definition/control coverage pass. If the remaining normal-game TextMgr
surface closes cleanly, close Queue 003 defensibly rather than manufacturing a
late issue and advance to the next protected queue target.

END C031-WIP149


===============================================================================
C031-WIP150 — QUEUE 003 FINAL COVERAGE RECONCILIATION / DEFENSIBLE CLOSURE
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP149 with F443/KI#766 confirmed and F444/KI#767 unused.
The remaining work was deliberately closure-oriented: finish the assertion /
explicit-caveat sweep, classify the zero/one-literal-mention member residue,
reconcile duplicates and confirm that no normal-game live-state TextMgr helper
remained materially unreviewed.

FINAL ASSERTION / CAVEAT PASS
-----------------------------
The remaining explicit `FAssert`, `FError`, `advc.test`, TODO/caveat and
"Can this happen?" sites were revisited after the production-queue findings.
No additional current release defect survived.

Useful closure notes:

* `setTradeRouteHelp` contains a debug-assertion typo in the `pOtherCity == NULL`
  branch: it asserts `pCity != NULL` a second time instead of asserting
  `pOtherCity != NULL`. The function immediately returns safely, so release
  behaviour is unaffected. Keep as debug-diagnostic cleanup only, not F444.
* `eventGoldHelp` retains the inherited negative-random-Gold endpoint typo
  recorded in WIP148 (`-iGold1, iGold2`), but current SAS event data cannot
  reach that negative random-loss branch. Keep as dormant robustness debt.
* compact tech-tree `advc.test` / model assumptions remain satisfied by current
  prerequisite defines/data, as established in the earlier full TechInfo pass.
* Improvement NO_IMPROVEMENT assertions are defensive entry contracts rather
  than evidence of a current bad caller.
* Score-help's explicit "must match calculateScore" caveat remains protected by
  its final equality guard and current SCORE_FREE_PERCENT configuration.

ZERO / ONE-LITERAL-MENTION RESIDUE
----------------------------------
A fresh member-definition inventory found no remaining material normal-game
stateful hole. The low-literal-mention tail is now explicitly classified:

* `setPlotHelpDebug_*`, `setPlotListHelpDebug`, `setCityPlotYieldValueString`
  are debug/AI diagnostics;
* WB data enumerators, GameFont assignment, language/time/net-stat helpers and
  interface theme helpers are resource/UI plumbing;
* billboard production/name/bar-colour helpers read authoritative city state
  directly; billboard icon and signed growth/starvation logic were separately
  audited, with F436 owning the real defect;
* `getTradeScreenTitleIcon`, `getTradeScreenIcons`, `getTradeScreenHeader` are
  direct current religion/civic/name/attitude renderers; the civic enum
  invariant was independently checked through player initialization;
* `buildBridgeString`, irrigation/water-work/trade-unlock builders and the other
  small Tech builders are covered by the complete TechInfo runtime-vs-help
  reconciliation;
* Promotion/UnitCombat wrappers are covered by the full PromotionInfo effect and
  eligibility reconciliation;
* Additional Yield/Commerce/Health/GreatPeople formatters delegate into the
  already-audited Building Actual Effects projection family;
* `buildBuildingReligionYieldString` directly renders the VoteSource religion
  yield field; the city runtime consumes the same VoteSource field through
  `processVoteSource`, while contextual membership/religion activation is a
  runtime-state condition rather than a contradictory static Building rule;
* Advanced Start data builders are covered by F417/F440/F441 plus the completed
  sibling generic-vs-target sweep;
* `getEspionageString` is compiled out / dead in the current build family;
* `setUnselectUnitHelp`, trait short-formatters, hints and simple list/value
  helpers contain no reconstructed gameplay calculation.

FINAL CROSS-FAMILY RECONCILIATION
---------------------------------
The large normal-game families now all have explicit coverage in this deep
re-loop:

* live unit / promotion / plot action help;
* combat, intercept, air, paradrop, rebase and Nuke action help;
* city bar, billboard, health/happiness/revolt and production state;
* Unit / Building / Project production, requirements and queue help;
* civics, traits, religions, corporations, projects and improvements;
* TechInfo effects and compact tech-tree reconstruction;
* trade/deal/diplomacy/vassal/war-weariness help;
* Finance Advisor component hovers;
* events and espionage costs/effects;
* Advanced Start generic and target-specific data/help;
* score, Great People / Great General and commerce/yield/production
  decompositions;
* debug/WB/resource-only tail separated from release gameplay coverage.

The current-SAS-vs-Base-AdvCiv semantic-delta inventory was also previously
reconciled: the remaining zero-literal-mention current SAS body differences are
lookup/define caching, signature/comment hoists or diagnostics rather than an
unreviewed SAS gameplay rewrite.

DUPLICATE / NUMBERING RECONCILIATION
------------------------------------
No final candidate was promoted merely because F444 was available.
F419 remains duplicate-reconciled into F389. The late Queue-003 roots F436-F443
remain distinct after sibling reconciliation, including:

* F442: continuing queued Building prerequisite uses NEW-order iMaking formula;
* F443: Unit/Building/Project queued row hover discards exact queue index for
  turns-left prediction.

The latter two share a natural implementation context but have independent
runtime contracts and symptoms.

QUEUE 003 CLOSURE
-----------------
`CvGameCoreDLL/CvGameTextMgr.cpp` is now defensibly CLOSED under the protected
deep-method re-audit method.

This does not claim mathematical impossibility of another defect in a 22k-line
file. It means the planned systematic surfaces, duplicated-runtime contracts,
state/actor/sentinel/sign pairs, current XML reachability, inherited ancestry,
low-mention members and final coverage ledger have all been worked through
without leaving a known unclassified normal-game residue.

CURRENT STONE AFTER C031-WIP150
-------------------------------
Protected deep-method re-audit Queue 003 `CvGameCoreDLL/CvGameTextMgr.cpp` —
CLOSED.
32/169 unique files complete in the overall album accounting; Queue 003 was a
protected re-loop of a previously completed file and therefore does not change
that unique-file count.
F443 / KI#766 latest confirmed root.
F444 / provisional KI#767 remains UNUSED for the next independent root.
Latest physical checkpoint: C031-WIP150.
No repository source files modified.

NEXT
----
Advance to protected deep-method re-audit Queue 004:
`CvGameCoreDLL/CvPlayer.cpp` (original pass recorded REVIEWED/CLEAN CURRENT).
Reopen it with the stronger WIP method rather than assuming the clean original
pass was exhaustive. Start from F444 unused and prioritize state-mutating
player-level contracts, arithmetic/cache invalidation, caller/target identity,
serialization/current-format invariants and current-data reachability.

END C031-WIP150


================================================================================
C031-WIP151 - Queue 004 CvPlayer.cpp - F444 / KI#767 commerce rounding correction reselects the same remainder
================================================================================

STATUS
------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` is OPEN.
32/169 unique files complete in overall album accounting; this protected re-loop does
not change that unique-file count.
F443 / KI#766 remains the latest Queue-003 confirmed root.
F444 / provisional KI#767 is now CONFIRMED.
F445 / provisional KI#768 is UNUSED.
No repository source file was modified.

QUEUE-004 METHOD / EARLY CONTROLS
---------------------------------
This is the protected second pass over CvPlayer.cpp. The original pass recorded the
file clean, so this re-loop deliberately starts from inherited state-mutating and
arithmetic/cache contracts rather than merely redoing the SAS-vs-Base-AdvCiv diff.

Early controls before F444:

* `canReceiveGoody` / `receiveGoody` technology selection is safe because
  `isGoodyTech()` itself ends in `canResearch(...)`; execution cannot randomly select
  an unresearchable technology merely because the outer reward loop does not repeat
  the check.
* A bad-Goody divide-by-zero shape exists if XML combines a BarbarianUnitClass with
  `iMinBarbarians == 0`, but both current hostile SAS goodies have positive minimums.
  Keep as future-data robustness debt, not F444.
* `updateExtraYieldThresholds` contains a real inherited typo in the parallel natural-
  threshold minimum selection: the replacement condition compares the current best
  natural threshold against the unrelated ordinary `iBestThresh` rather than comparing
  the new natural threshold against `iBestNaturalThresh`. Current SAS data masks it:
  only Financial supplies a nonzero `ExtraYieldNaturalThreshold` (Commerce 3), so
  there are no competing active natural-threshold trait values whose iteration order
  could currently change the result. Keep as strong latent cleanup, not F444.

F444 / provisional KI#767 - CONFIRMED CURRENT BASE ADVCIV ARITHMETIC/CACHE DEFECT
---------------------------------------------------------------------------------
Title:
`CvPlayer::updateCommerceRates` repeatedly awards every whole rounding correction to
the same commerce type because its advertised implicit selection sort never removes
or updates the selected fractional remainder.

Birth / lineage:
Base AdvCiv, change id `advc.157`, introduced by exact locally preserved commit:

    4e6fe9df58dfbce36a575aa24224e44030bd76fa
    2021-09-08
    "Redistribute rounding errors from non-binary research"

The introducing diff adds `updateCommerceRates` and already contains the defective loop
in exactly the relevant form. K-Mod / BtS do not have this redistribution/cache
algorithm; current Base AdvCiv 1.14 and current AdvCiv-SAS retain it unchanged in the
relevant logic.

Intended/documented contract:
The source immediately calls the loop:

    // Implicit selection sort (b/c the error is going to be small)

The AdvCiv manual for change 157 says that when accumulated rounding errors reach at
least 1 commerce, the integer correction is added to the commerce rate(s) that
contribute(s) the most to the total error. The purpose is to remove the systematic
penalty from non-binary commerce-slider positions.

Implemented contract:
For each CommerceTypes value, `updateCommerceRates` stores the signed fractional
remainder:

    iError = sign(iRate) * (abs(iRate) % 100)

and accumulates all remainders in `iTotalError`. It truncates every Times100 rate to an
integer, then performs `abs(iTotalError) / 100` correction iterations. Each iteration
searches `aiError` for the largest signed remainder and adds +/-1 to that commerce
rate.

However, after selecting `eArgMax`, the implementation only does:

    m_aiCommerceRate.add(eArgMax, iSign * 1);

It never reduces, clears, or otherwise updates `aiError[eArgMax]`.

Therefore, when at least two whole correction points are required, the next iteration
sees exactly the same maximum and selects exactly the same commerce type again. All
corrections go to the first maximum. The advertised selection sort never advances.

This is mathematically inconsistent with the residual after the first correction: if
an exact rate has remainder +90 and receives +1, its new residual is -10, not still
+90. It therefore cannot legitimately remain ahead of an uncorrected +80 remainder.

CURRENT SAS REACHABILITY - NO FUTURE/MODDED DATA REQUIRED
---------------------------------------------------------
The multi-correction branch is ordinary current-data gameplay, not a theoretical
edge case.

Current SAS Palace (`BUILDING_PALACE`) has:

    YieldChanges:          [0, 0, 8]  => +8 raw YIELD_COMMERCE
    CommerceChanges:       [0, 0, 0, 4] => +4 flat Espionage
    CommerceModifiers:     empty

Current `YIELD_COMMERCE` has `iMinCity = 1`; `CvPlot::calculateCityPlotYieldChange`
raises the city plot to that minimum. Thus a capital can have exactly 9 raw commerce
from the Palace's +8 plus the city plot's minimum +1 while avoiding extra worked-tile
commerce (e.g. use a specialist / zero-commerce worked configuration).

Current Tech XML makes the relevant slider state legal:

* `TECH_MONARCHY` has `CommerceFlexible [0,0,0,1]` -> Espionage slider flexible.
* `TECH_DRAMA` has `CommerceFlexible [0,0,1,0]` -> Culture slider flexible.

Gold/Research are the ordinary slider pair. Once Monarchy + Drama are known, a
30% Gold / 10% Research / 20% Culture / 40% Espionage split is legal.

For 9 raw commerce, before optional percentage-commerce modifiers:

    Gold:       9 * 30% = 2.70 -> 270
    Research:   9 * 10% = 0.90 ->  90
    Culture:    9 * 20% = 1.80 -> 180
    Espionage:  9 * 40% = 3.60 -> 360, plus Palace +4 = 760

The fractional remainders are therefore:

    Gold       70
    Research   90
    Culture    80
    Espionage  60
    ----------------
    total     300

`abs(iTotalError) / 100 == 3`, so the correction loop runs THREE times.

After initial truncation, special-commerce integer rates are:

    Gold 2, Research 0, Culture 1, Espionage 7

BROKEN RESULT:
The first scan selects Research (90). Because its stored 90 remainder never changes,
the second and third scans select Research again:

    Gold 2, Research 3, Culture 1, Espionage 7

(before AdvCiv's separate BASE_RESEARCH_RATE addition).

INTENDED SELECTION / LARGEST-REMAINDER RESULT:
After Research receives +1, its effective remainder is -10. The next largest
uncorrected remainder is Culture 80, then Gold 70:

    Gold 3, Research 1, Culture 2, Espionage 7

(before BASE_RESEARCH_RATE).

The bug therefore does not merely choose a different cosmetic tie-break. It can move
multiple whole commerce points between different commerce categories.

AUTHORITATIVE GAMEPLAY CONSUMERS
--------------------------------
The corrected `m_aiCommerceRate` cache is authoritative for normal player-level rates:

* `calculateGoldRate()` starts from `getCommerceRate(COMMERCE_GOLD)` and applies GPT /
  expenses; this feeds actual treasury changes.
* `calculateResearchRate()` starts from `getCommerceRate(COMMERCE_RESEARCH)` and applies
  the research modifier; `doResearch()` sends that rate into team research progress.
* `doEspionagePoints()` uses `getCommerceRate(COMMERCE_ESPIONAGE)` for actual EP-ever
  gain and `getEspionageSpending()` distributes the same integer budget against teams.

Thus an erroneous redistribution among these categories changes real Gold, Research,
and/or Espionage output. It is not confined to UI display.

Culture is a useful additional warning: player-wide Culture rate is much less
important because city culture is applied per city (the AdvCiv manual itself says
"total culture isn't really relevant"). A malformed correction can therefore also be
dumped into or withheld from a civ-wide Culture aggregate that does not symmetrically
substitute for lost Gold/Research/Espionage gameplay output.

WHY THIS IS A DEFECT, NOT AN ALTERNATIVE ROUNDING POLICY
--------------------------------------------------------
Several independent pieces agree:

1. the code names the algorithm an "Implicit selection sort";
2. the manual says correction goes to the rate(s) contributing most to the total
   error, not repeatedly to the first rate that happened to have the initial maximum;
3. after +1 is awarded to a +90 remainder, that rate's actual residual becomes -10,
   so treating it as +90 on the next pass is arithmetically stale;
4. the introducing commit's stated purpose is to redistribute accumulated rounding
   error from non-binary research, and the malformed repeated maximum can transfer
   multiple whole units to the wrong commerce category instead.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains no prior `updateCommerceRates`, advc.157, non-binary-
research redistribution, or player-commerce-rounding root. F326 references
`getCommerceRate` only as an input to AI civic valuation and concerns removal of
`AI_averageCommerceMultiplier` normalization; it is unrelated. Queue-003 Finance /
commerce findings concern display decomposition and rounding of other components, not
this authoritative player cache.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
After awarding the +/-1 correction, update the selected stored residual before the
next scan. The mathematically direct form is conceptually:

    m_aiCommerceRate.add(eArgMax, iSign);
    aiError.add(eArgMax, -iSign * 100);

because giving +1 to a +90 hundredths residual changes the residual to -10, and giving
-1 to a -90 residual changes it to +10.

For currently nonnegative special-commerce rates, simply excluding each selected
candidate from subsequent passes would also produce the intended largest-remainder
selection because each original remainder has magnitude <100 and at most three whole
corrections can exist across four commerce types. Updating by +/-100 is preferable as
it directly preserves the residual arithmetic and the function's stated negative-rate
future-proofing intent.

A regression test should cover at least one `abs(iTotalError) / 100 >= 2` state, not
only the typical one-correction case; a 9-commerce four-slider state above is a compact
current-SAS fixture.

CURRENT STONE AFTER C031-WIP151
-------------------------------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete in overall album accounting.
F444 / provisional KI#767 latest confirmed root.
F445 / provisional KI#768 UNUSED.
Latest physical checkpoint: C031-WIP151.
No repository source files modified.

NEXT
----
Continue Queue 004 from F445 unused. Resume the inherited cache/state-mutator pass
around `updateExtraYieldThresholds`, commerce/flexible-count caches, financial turn
processing and disband/ownership contracts; then expand into serialization and
player/team/city cache invalidation. Do not promote the data-masked natural-threshold
typo unless current reachability changes or a separate current consequence is proven.

END C031-WIP151


======================================================================
C031-WIP152 — QUEUE 004 CVPLAYER DEEP RE-AUDIT: F445 STILL UNUSED
======================================================================

STATUS
------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete in overall album accounting.
F444 / provisional KI#767 remains latest confirmed root.
F445 / provisional KI#768 remains UNUSED.
Latest physical checkpoint: C031-WIP152.
No repository source files modified.

POST-F444 CONTROL TRANCHE
-------------------------
Several inherited state/cache candidates were chased through full transitions rather
than promoted on suspicious syntax alone.

1. CHANGELEADER / TRUE STARTS FREE-PROMOTION CONTROL — CLEAN FOR ORDINARY TRUE STARTS

`CvPlayer::changeLeader` removes old-trait processing and applies the new leader's
traits, while trait-granted unit free promotions themselves are normally installed by
`CvUnit::init`. That initially suggested that a True Starts leader swap could leave
already-created starting units with the old leader's promotions. The actual game-init
order closes the ordinary case: `TrueStarts::changeCivs()` runs before
`CvGame::initFreeUnits()` in `CvGame::setInitialItems`, so normal starting units are
created only after the final leader is installed. Scenario-authored pre-existing units
remain a scenario/robustness caveat rather than a proven generic current root.

2. PERMANENT-ALLIANCE TECHNOLOGY PROCESSING CONTROL — CLEAN

`CvTeam::addTeam` calls `shareItems(eTeam)` before moving absorbed players to the
surviving team. A partial trace suggested that survivor-only technologies might be
processed before absorbed players became members and therefore fail to install their
player-level effects. Full `shareItems` tracing rejects that: K-Mod/AdvCiv makes the
technology sharing bidirectional before reassignment. Each team receives the other's
missing technologies through `setHasTech` while its own players still belong to it;
therefore persistent player-level technology effects are processed for both sets of
players before `CvPlayer::setTeam` performs the merge.

3. `updateExtraYieldThresholds` NATURAL-THRESHOLD TYPO — REAL BUT CURRENTLY DATA-MASKED

The natural-yield branch contains an apparent inherited typo:

    if (iBestNaturalThresh == 0 || iBestNaturalThresh < iBestThresh)
        iBestNaturalThresh = iNaturalThresh;

The ordinary threshold branch correctly compares the candidate against the current
best ordinary threshold. The natural branch instead compares the existing natural
threshold against the unrelated ordinary `iBestThresh`, making multiple natural
threshold sources potentially trait-order dependent. Current SAS data masks the
fault: only Financial currently supplies a nonzero `ExtraYieldNaturalThreshold`
(Commerce 3), so no competing current natural thresholds exist. Preserve as
robustness/future-data debt; do not number as F445 without a current consequence.

4. `canStealTech` / `canSpyDestroyProject` APPARENT PLAYER->TEAM ENUM MISMATCH — REJECTED

Current source contains lines such as:

    GET_TEAM(eTarget).isHasTech(eTech)
    GET_TEAM(eTarget).getProjectCount(eProject)

where `eTarget` is declared `PlayerTypes`. Read without include context, these appear
to index the team table by player ID and would imply severe team-game espionage bugs.
That diagnosis is false.

`CvPlayer.cpp` includes `CoreAI.h`. `CoreAI.h` deliberately replaces the ordinary
`GET_TEAM` macro with an overloaded accessor:

    #define GET_TEAM(x) CoreAI::getTeam(x)

    getTeam(TeamTypes eTeam)     -> AI_getTeam(eTeam)
    getTeam(PlayerTypes ePlayer) -> AI_getTeam(TEAMID(ePlayer))

Therefore `GET_TEAM(eTarget)` resolves the target player's ACTUAL team even when
PlayerTypes and TeamTypes numeric IDs differ.

Lineage confirms intent. BtS and K-Mod spell the conversion explicitly as
`GET_TEAM(GET_PLAYER(eTarget).getTeam())`. An AdvCiv style/refactor stage first used
the player-aware `TEAMREF(eTarget)` helper; commit
`1c56cc6ac149d97c3a28eae56440f4f0ab0183bd`, titled
"GET_PLAYER, GET_TEAM macros outside of AI headers", then deliberately changed that
spelling to overloaded `GET_TEAM(eTarget)`. The active CoreAI overload preserves the
same semantics.

This independently revalidates the album's earlier KI#311 rejected-false-positive
methodology: apparent PlayerTypes/TeamTypes mismatches through `GET_TEAM` must be
resolved through the active include-context macro/overloads before diagnosis.

`canStealTech` does appear in the album under F349, but only as an input to the
separate `CvPlayerAI::AI_doCommerce` accumulator defect. There is no missing
`canStealTech` root here; F445 remains unused.

5. OTHER EARLY QUEUE-004 CONTROLS

* `canReceiveGoody` / `receiveGoody`: technology rewards are still protected by
  `isGoodyTech()->canResearch`; no current mismatch found. A bad goody with a
  BarbarianUnitClass plus `iMinBarbarians==0` would expose a latent division-by-zero
  shape, but current hostile goodies have positive minima.
* `disbandUnit`: preliminary forced-financial-disband scoring/eligibility review has
  not established a defect. Its behavior is intentionally distinct from voluntary
  scrapping and should not inherit the earlier SAS scrapping findings automatically.
* `changeCommerceFlexibleCount`, trade-route/anarchy transitions and the current
  stacked availability counters checked so far preserve their zero/nonzero transition
  invariants under current callers/data.

CURRENT STONE AFTER C031-WIP152
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F444 / provisional KI#767 latest confirmed.
F445 / provisional KI#768 UNUSED.
Latest physical checkpoint: C031-WIP152.
No repository source files modified.

NEXT
----
Continue the inherited state-mutator/cache pass from F445 unused. Prioritize player
cached totals and old/new-state transitions, forced-disband/strike/economic turn
processing, religion/corporation/bonus processing, Advanced Start player costs,
serialization and ownership/team-change invalidation. Continue the mechanical
zero/low-album-mention inventory rather than forcing a new root.

END C031-WIP152


======================================================================
C031-WIP153 — QUEUE 004 CVPLAYER DEEP RE-AUDIT: F445 / KI#768 CAPITAL MOVE LEAVES YIELD-RANK CACHE STALE
======================================================================

STATUS
------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete in overall album accounting.
F445 / provisional KI#768 CONFIRMED.
F446 / provisional KI#769 UNUSED.
Latest physical checkpoint: C031-WIP153.
No repository source files modified.

F445 / KI#768 — `CvPlayer::setCapital` changes capital-only yield modifiers without invalidating cached city yield ranks
---------------------------------------------------------------------------------------------------------------

ROOT
----
`CvCity::getBaseYieldRateModifier(YieldTypes)` dynamically includes the owning
player's `getCapitalYieldRateModifier(eYield)` whenever `isCapital()` is true.
`CvCity::isCapital()` is itself dynamic and simply compares the city to
`GET_PLAYER(getOwner()).getCapital()`.

K-Mod's `CvCity::findYieldRateRank` caches yield-rate rankings. When one city asks for
a rank, it calculates the ordering for ALL of the player's cities and marks every
city's rank valid for that yield.

The source next to `getBaseYieldRateModifier` explicitly states the cache contract:

    // note: player->invalidateYieldRankCache() must be called for anything that is checked here

Capital status is one of the conditions checked there.

However, `CvPlayer::setCapital(CvCity* pNewCapital)` changes `m_iCapitalCityID` and
updates maintenance, trade routes and commerce for the old/new capitals, but never
calls `invalidateYieldRankCache`.

Therefore changing capital identity can change an old/new capital's actual Food /
Production / Commerce yield-rate modifier while a previously computed city-yield
ranking remains valid according to the cache flags.

CURRENT LIVE DATA / REPRODUCTION
--------------------------------
This is live in ordinary current SAS data. `CIVIC_BUREAUCRACY` has:

    CapitalYieldModifiers = [0, +50, +50]

so the capital receives +50% Production and +50% Commerce.

A simple deterministic shape is:

1. Player runs Bureaucracy and owns at least two cities A and B.
2. Before the capital move, actual production rates are such that A (the capital)
   ranks above B only because of Bureaucracy's +50% capital Production modifier; e.g.
   A base 10 -> 15, B base 11 -> 11.
3. Something calls `findYieldRateRank(YIELD_PRODUCTION)`. K-Mod computes and caches the
   complete ordering, e.g. A rank 1, B rank 2.
4. Complete a Palace in B, causing `CvPlayer::setCapital(B)`.
5. Actual rates immediately change dynamically: A loses the +50% and becomes 10;
   B gains it and becomes 16 (integer arithmetic), so the correct order is B rank 1,
   A rank 2.
6. `setCapital` does not invalidate `YIELD_PRODUCTION`, therefore the cached ordering
   can remain A rank 1 / B rank 2 until some unrelated Production-yield change later
   invalidates it.

PALACE SIDE-EFFECT CONTROL
--------------------------
The capital relocation path does not accidentally repair the missing Production-rank
invalidation. Current `BUILDING_PALACE` has base YieldChanges `[0, 0, +8]` — only
Commerce yield. `CvCity::processBuilding` therefore invalidates the Commerce-yield
rank through the Palace's base-commerce-yield change, but it performs no
Production-yield mutation that would invalidate `YIELD_PRODUCTION`.

The relevant capital-only Production modifier comes from the player's civic and changes
solely because `isCapital()` now points at a different city.

RUNTIME CONSEQUENCE
-------------------
The underlying Production yield itself is NOT stale; `getYieldRate()` consults
`isCapital()` dynamically, so actual production changes immediately. The stale object
is specifically the cached rank returned by `findYieldRateRank(YIELD_PRODUCTION)`.

That rank is gameplay-significant. Current SAS has multiple AI consumers in
`CvCityAI.cpp` / `CvPlayerAI.cpp` (plus additional logging/UI consumers) that use
Production rank when evaluating city roles, production choices and related decisions.
A capital move can therefore temporarily feed those AI decisions an ordering that no
longer corresponds to the actual Production yields.

ANCESTRY
--------
This omission is inherited. BtS `CvPlayer::setCapitalCity`, K-Mod
`CvPlayer::setCapitalCity`, Base AdvCiv 1.14 `CvPlayer::setCapital`, and current SAS all
perform the same capital-ID transition plus maintenance/trade/commerce refresh without
yield-rank invalidation.

BtS data already makes the state live: Bureaucracy carries the same +50% Production /
+50% Commerce `CapitalYieldModifiers`. K-Mod's later all-city rank-cache implementation
makes the stale state especially broad because one rank lookup marks all city ranks for
that yield valid together.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains no prior `setCapital`, Bureaucracy capital-yield,
`findYieldRateRank`, capital-modifier or yield-rank-cache root. Existing capital,
Palace, commerce and rank mentions belong to unrelated systems.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
`CvPlayer::setCapital` should invalidate player city yield-rate ranks after changing
capital identity, at least for every yield whose `getCapitalYieldRateModifier` can be
nonzero. The simplest and safest repair is conceptually:

    invalidateYieldRankCache();

once the capital ID has changed (before or alongside the old/new UI/commerce refreshes).
This directly satisfies the maintenance contract documented beside
`CvCity::getBaseYieldRateModifier` and remains correct for XML/mod data that adds
capital-only Food or other yield modifiers later.

A regression test should first populate Production ranks under Bureaucracy, move the
Palace/capital to another city without otherwise changing Production yields, and verify
that a subsequent `findYieldRateRank(YIELD_PRODUCTION)` recomputes the new ordering.

POST-F445 CLEAN CONTROLS
------------------------
* `CvPlayer::setCommercePercent`: the apparently negative adjustment when total
  commerce is below 100 is intentional; subtracting a negative increases another
  flexible category. Player-level flexibility loss resets the newly inflexible
  category to its XML initial percent. No current broken 100%-sum transition proven.
* `changeHurryCount`: derived pop-/gold-rush counts add `iChange` on a zero/nonzero
  transition, but its gameplay caller `processCivics` supplies only -1/0/+1 per civic,
  so current data cannot create a multi-point transition mismatch.
* `changeFreeBuildingCount`: correctly applies/removes free buildings only on the
  zero-to-positive / positive-to-zero source-count boundaries.

CURRENT STONE AFTER C031-WIP153
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / provisional KI#768 latest confirmed.
F446 / provisional KI#769 UNUSED.
Latest physical checkpoint: C031-WIP153.
No repository source files modified.

NEXT
----
Continue Queue 004 from F446 unused. Resume the mechanical high-value low-coverage
state-mutator inventory: `setLastStateReligion`, `setCivics`, civilization/player
change/reset paths, free-building/availability counters, Advanced Start player costs,
serialization/ownership/team-change invalidation and other cached totals. Preserve the
same caller + current-data + ancestry + duplicate threshold and do not manufacture a
sibling merely because F445 is a cache invalidation bug.

END C031-WIP153


======================================================================
C031-WIP154 — QUEUE 004 CVPLAYER DEEP RE-AUDIT: F446 / KI#769 CIVILIAN UNIT PROJECTION ALSO CHANGES MILITARY UPKEEP
======================================================================

STATUS
------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete in overall album accounting.
F446 / provisional KI#769 CONFIRMED.
F447 / provisional KI#770 UNUSED.
Latest physical checkpoint: C031-WIP154.
No repository source files modified.

F446 / KI#769 — `calculateUnitCost(..., iExtraUnits)` applies generic unit delta to military-unit count too
----------------------------------------------------------------------------------------------------------

ROOT
----
AdvCiv extends `CvPlayer::calculateUnitCost` with prospective-state parameters:

    int iExtraPop, int iExtraUnits

The implementation applies `iExtraUnits` to BOTH support populations:

    iPaidUnits = max(0, getNumUnits() - iFreeUnits + iExtraUnits);
    iPaidMilitaryUnits = max(0,
            getNumMilitaryUnits() - iFreeMilitaryUnits + iExtraUnits);

That silently assumes every prospective extra/removed unit is also a military-support
unit. The parameter itself is generic and live callers use it for explicitly civilian
units.

CURRENT LIVE CALLER 1 — CITY-FOUND COST HELP
---------------------------------------------
`CvGameTextMgr::setFoundCostHelp` projects expenses after founding a city. It correctly
accounts for the Settler disappearing by calling:

    kPlayer.calculateUnitCost(CvCity::initialPopulation(), -1)

Current `UNIT_SETTLER` has:

    bMilitarySupport = 0

and therefore does NOT contribute to `CvPlayer::getNumMilitaryUnits()` at runtime.
Founding a city removes one total unit but zero military units.

The projection nevertheless subtracts one from both populations through the shared
`iExtraUnits=-1` argument.

CURRENT DATA MAKES THE MILITARY-COST ERROR LIVE
-----------------------------------------------
Current `CIVIC_PACIFISM` has:

    iGoldPerMilitaryUnit = 100

(1 gold per paid military-support unit), and current civic data has no nonzero
`iBaseFreeMilitaryUnits` or `iFreeMilitaryUnitsPopulationPercent` source.

A compact ordinary reproduction is:

1. Human player runs Pacifism.
2. Player owns at least one military-support unit and a Settler.
3. Hover a legal city-found action.
4. Actual post-found state consumes the Settler but keeps the military-support unit;
   military upkeep therefore remains charged for that military unit.
5. `setFoundCostHelp` passes `iExtraUnits=-1`; the projection also decrements
   `iPaidMilitaryUnits`, reducing projected military cost by 1 gold/turn before
   inflation whenever the paid military count is positive.

The projected founding-cost delta is therefore understated. Inflation can scale the
visible error further.

CURRENT LIVE CALLER 2 — DELETE / SCRAP COMMAND HELP
----------------------------------------------------
`CvDLLWidgetData`'s `COMMAND_DELETE` help builds `iUnits` by subtracting every selected
unit and every carried cargo unit, without distinguishing military-support units. It
then calls:

    calculateUnitCost(0, iUnits)

for the projected expenses and again with further generic negative unit deltas when
estimating how many additional units would need deletion before expenses decrease.

Selecting/deleting a Worker, Settler or other `bMilitarySupport=0` unit therefore also
pretends to reduce the military-support population. Under Pacifism this can overstate
the gold savings from deleting civilian units and can distort the helper's "one more /
N more units" guidance.

RUNTIME CONTROL
---------------
Actual military-unit accounting is type-aware. `CvUnit::init` increments
`changeNumMilitaryUnits(1)` only when `m_pUnitInfo->isMilitarySupport()`; `CvUnit::kill`
symmetrically decrements only those units. Settlers are explicitly non-military-support.
The projection is therefore not mirroring runtime unit accounting.

ANCESTRY / INTRODUCING COMMIT
-----------------------------
This is an AdvCiv regression, not stock BtS/K-Mod unit-cost arithmetic.

K-Mod's ordinary `calculateUnitCost` has no prospective `iExtraUnits` parameter.
Before the relevant AdvCiv change, the founding-cost projection handled the Settler
specifically:

    iPaidUnits = max(0, getNumUnits() - iFreeUnits
            - (iExtraPop > 0 ? 1 : 0)); // The Settler is gone after founding
    iPaidMilitaryUnits = max(0, getNumMilitaryUnits() - iFreeMilitaryUnits);

That correctly removed the Settler from total-unit upkeep while leaving military-unit
upkeep untouched.

AdvCiv practical commit 1412, SHA `ef40cbb8b47a2b11e4b213076d0f1cad62ad13b1`,
titled "Misc. tweaks (mostly UI)", generalized the projection API to add
`iExtraUnits`. In the same introducing diff it replaced the Settler-specific total-unit
adjustment and added `+ iExtraUnits` to BOTH `iPaidUnits` and `iPaidMilitaryUnits`.
The same commit also introduced the Delete-command unit-cost projection that counts
selected units generically. Thus the wrong military delta is present in the
introducing change itself.

Base AdvCiv 1.14 and current SAS retain the generalized implementation.

DUPLICATE / ALBUM CHECK
-----------------------
The album already audits `setFoundCostHelp`, but its confirmed F392/KI#715 root is the
separate WLTKD maintenance projection mismatch. F412/KI#735 concerns Finance Advisor
unit-cost/supply hovers bypassing anarchy-aware wrappers. Neither covers prospective
civilian-unit deltas being applied to military-unit upkeep.

The album contains no prior root for `iExtraUnits`, prospective paid-military count, or
civilian Delete-command military-upkeep savings. F446 is independent.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The prospective API needs distinct total-unit and military-unit deltas, e.g.
conceptually:

    calculateUnitCost(iExtraPop, iExtraUnits, iExtraMilitaryUnits)

with the detailed overload using:

    iPaidUnits         ... + iExtraUnits
    iPaidMilitaryUnits ... + iExtraMilitaryUnits

`setFoundCostHelp` should pass `-1` total units and `0` military units for the Settler.

The Delete-command helper should count both selected/cargo total units and selected /
cargo `isMilitarySupport()` units separately, then pass both deltas. Its iterative
"how many more deletions" prediction cannot know the military status of hypothetical
future deletions; that fallback should either state its assumption explicitly, use a
conservative model, or be refactored around actual candidate unit types rather than a
single generic unit count.

Regression tests should include Pacifism with one paid military unit plus a Settler /
Worker and verify that removing the civilian changes total-unit support only, not
military support.

CURRENT STONE AFTER C031-WIP154
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F446 / provisional KI#769 latest confirmed.
F447 / provisional KI#770 UNUSED.
Latest physical checkpoint: C031-WIP154.
No repository source files modified.

NEXT
----
Continue Queue 004 from F447 unused. Resume the mechanical high-value arithmetic /
state-mutator inventory after the unit-cost projection family: Advanced Start player
costs, `setLastStateReligion` / `setCivics` closure, civ/player reset/change paths,
serialization, ownership/team-change invalidation and remaining cached counters. Keep
prospective APIs especially high priority because F446 shows that added hypothetical
state parameters can collapse distinct runtime populations.

END C031-WIP154


======================================================================
C031-WIP155 -- QUEUE 004 F447 RECYCLED COLONY PLAYER / LIVE-TEAM STATE SPLIT
======================================================================

STATUS AT THIS CHECKPOINT
-------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F447 / provisional KI#770 is CONFIRMED below.
F448 / provisional KI#771 is now UNUSED.
Latest physical checkpoint: C031-WIP155.
No repository source files modified.


F447 / KI#770 -- RECYCLING A DEFEATED PLAYER WHOSE OLD TEAM IS STILL ALIVE SKIPS COLONY INITIALIZATION, THEN REVIVES THE OLD PLAYER FROM THE TRANSFERRED CITIES
------------------------------------------------------------------------------------------------------------------------------------------

ROOT
----
`CvPlayer::getSplitEmpirePlayer` chooses a recycled slot according to PLAYER state:

    if (!itPlayer->isAlive())
        eNewPlayer = itPlayer->getID();

when no never-used player slot remains (AdvCiv restricts this fallback to human
splitters through `<kekm.24>`).

But `CvPlayer::splitEmpire` immediately decides whether that selected slot already
"exists" according to TEAM state instead:

    bool bPlayerExists = GET_TEAM(eNewPlayer).isAlive();
    FAssert(!bPlayerExists);

and performs all new-colony initialization only when that TEAM is dead:

    if (!bPlayerExists)
    {
        ... choose eBestLeader / eBestCiv ...
        kGame.addPlayer(eNewPlayer, eBestLeader, eBestCiv);
        GET_PLAYER(eNewPlayer).setParent(getID());
        ... inherit technologies ...
        GET_TEAM(getTeam()).assignVassal(TEAMID(eNewPlayer), false);
        ...
    }

A player can be dead while their team remains alive whenever another teammate
survives. In that supported state, `getSplitEmpirePlayer` selects the slot precisely
because the PLAYER is dead, but `splitEmpire` sees `bPlayerExists == true` precisely
because the TEAM is alive. The debug assertion fails and, in release builds, the
entire colony-initialization block is skipped.

The function does NOT abort after that mismatch. It proceeds to transfer cities,
culture and free defenders to the selected dead player.

DETERMINISTIC STATE / REPRODUCTION SHAPE
----------------------------------------
Use a team game or scenario with the following state:

1. All player slots have been ever-alive, so `getSplitEmpirePlayer` cannot find a
   `!isEverAlive()` slot.
2. Human player P can create a colony from an overseas area containing at least two
   P cities (the ordinary `canSplitArea` requirement for a dead recipient).
3. Player X was eliminated, but X had teammate Y and Y is still alive. Therefore:

       X.isAlive() == false
       GET_TEAM(X).isAlive() == true

4. X is the first dead ever-alive player found by the player iterator.
5. At least one valid civilization/leader combination has never been used, so
   `getSplitEmpireLeaders` allows colony creation.
6. P chooses "Create Colony" for the eligible area.

`getSplitEmpirePlayer` returns X. `splitEmpire` sets `bPlayerExists=true` from X's
still-alive team and skips `CvGame::addPlayer` and every new-colony setup step.
Nevertheless the later city loop executes:

    GET_PLAYER(eNewPlayer).acquireCity(...);

and the free-defender loop creates units in those transferred cities for X.

REVIVAL TIMING IS SOURCE-PROVEN
-------------------------------
Neither `CvPlayer::acquireCity` / `initCity` nor the free-defender creation in this
path makes the recipient player alive immediately. The split therefore finishes with
X still dead but now owning cities (and normally units).

The ordinary game-update path then executes:

    CvGame::updateUnprofiled()
        -> testAlive()
            -> GET_PLAYER(eLoopPlayer).verifyAlive()

`CvPlayer::verifyAlive` explicitly revives any dead player who now owns a city or unit:

    if (!isAlive())
    {
        if (getNumCities() > 0 || getNumUnits() > 0)
            setAlive(true);
        return;
    }

Thus the recycled old player is deterministically resurrected on the next normal
update from the possessions that `splitEmpire` just gave them.

WHAT IS SKIPPED / MALFORMED
---------------------------
Because `bPlayerExists` was true, the revived player never went through the intended
new-colony initialization:

* no `CvGame::addPlayer(eNewPlayer, eBestLeader, eBestCiv)`;
* no `CvPlayer::initInGame` reset/reinitialization performed by `addPlayer`;
* no new colony civilization/leader selection;
* no `setParent(getID())` for the splitting civilization;
* no inheritance of the parent's technologies / no-trade-tech flags;
* no `assignVassal(..., false)` establishing the intended colonial-vassal team;
* no new-player replay/message setup guarded by `!bPlayerExists`.

Instead X retains the old defeated player's persistent identity/state and remains a
member of X's old still-alive team. Once `verifyAlive` resurrects X, the supposed
"colony" is therefore an old civilization/player revived on its former team rather
than a freshly initialized colonial vassal of P.

Any old `parent` value also remains whatever the defeated player had; the intended
new parent is not installed.

CURRENT SAS REACHABILITY
------------------------
Current SAS has:

    MAX_CIV_PLAYERS = 48
    36 CivilizationInfos with bPlayable=1 and bAIPlayable=1
    55 unique leaders mapped to those playable civilizations

With an ordinary fully-random 48-player setup, AdvCiv's random assignment tends to
spread players across all available civilizations before duplicating them, which can
consume every civilization and leave no valid unused colony civ. That masks this
specific fallback in that common setup.

It does NOT establish an engine invariant. Scenario/custom initialization can reuse
civilizations; `CvInitCore::setCiv` simply installs the requested CivilizationTypes
and has no uniqueness check. Current WorldBuilder/scenario player data can therefore
use all 48 player slots while repeating some civilizations and leaving another
playable civilization plus at least one of its leaders unused.

Because SAS has 55 mapped playable leaders, it is straightforward to reserve a valid
leader for the unused civilization while populating the other 48 ever-alive slots.
After one member of a multi-player team is eliminated, the exact dead-player /
live-team state above is reachable and the colony command remains source-legal.

So this is not merely a hypothetical future-data condition; it is a supported
scenario/team-game lifecycle defect. It is rarer in ordinary random games under the
48-slot SAS configuration than it was under stock BtS's smaller player cap.

ANCESTRY
--------
The root predates AdvCiv.

Stock BtS/Civ4CE already does both halves:

* `getSplitEmpirePlayer` first searches never-alive players and then recycles any
  `!GET_PLAYER(i).isAlive()` slot;
* `splitEmpire` then computes:

      bool bPlayerExists =
          GET_TEAM(GET_PLAYER(eNewPlayer).getTeam()).isAlive();
      FAssert(!bPlayerExists);

  and only calls `addPlayer` / parent-tech-vassal setup when that TEAM is dead.

K-Mod 1.46 retains the same mismatch.

Base AdvCiv 1.14 retains the same split logic. Its `<kekm.24>` change does NOT
introduce the player/team mismatch; it merely recognizes that "Reusing a defeated
player might not work correctly" and restricts the fallback to human players so AI
players cannot trigger the risky recycling path automatically.

Current SAS retains that AdvCiv behavior.

DUPLICATE / ALBUM CHECK
-----------------------
The album's existing Free Colony finding F317/KI#640 is a different root: its popup
names a liberation recipient at launch but sends only a city/area action and later
recomputes the recipient, allowing stale UI approval to retarget to another civ.
That finding explicitly established that the `splitEmpire` receiver rechecks the area
and general split legality; it does not audit the lifecycle semantics of the recycled
player slot selected by `getSplitEmpirePlayer`.

No existing album root covers:

* dead PlayerTypes whose TeamTypes remains alive;
* `getSplitEmpirePlayer` player-state selection versus `splitEmpire` team-state
  `bPlayerExists`;
* skipped `addPlayer/initInGame` for a recycled colony slot;
* revival by `verifyAlive` after transferred cities are assigned to the dead slot.

F447 is therefore independent.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The "new colony needs initialization" decision must be based on the PLAYER SLOT that
was selected for recycling, not merely on whether that slot's old team still happens
to have another living member.

The safest repair is probably to treat every selected `!isAlive()` recycled player as
requiring full recycled-player initialization, while also handling its old live-team
membership explicitly before `addPlayer/initInGame` rather than assuming a recycled
slot's team is dead.

At minimum, do NOT allow this sequence:

    dead player selected -> old team alive -> skip initialization -> transfer cities

Possible conservative policies include:

1. Reject dead-player recycling when `GET_TEAM(eNewPlayer).isAlive()` and return
   `NO_PLAYER` / disable the colony choice. This is narrow and avoids having to detach
   a recycled player from a still-active team.
2. If supporting such recycling is desirable, explicitly move/reset the slot onto a
   proper new colony team and run the same `addPlayer/initInGame`, parent, technology
   and vassal initialization as other recycled slots before any city transfer.

The existing `FAssert(!bPlayerExists)` is useful evidence of the intended invariant
but is not a release guard. Turn it into enforced runtime handling rather than
continuing with malformed state when the assertion condition fails.

Regression coverage should include a dead member of a still-alive team selected as
the first recyclable slot, followed by a human colony split. The result should either
be rejected cleanly or produce a fully initialized new colonial vassal; it must never
revive the old player/team identity from the transferred cities.

NUMBERING
---------
F447 is CONFIRMED and consumes KI#770.
F448 / provisional KI#771 is the next unused root slot.


CURRENT STONE AFTER C031-WIP155
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F447 / KI#770 latest confirmed.
F448 / provisional KI#771 UNUSED.
Latest physical checkpoint: C031-WIP155.
No repository source files modified.

NEXT
----
Continue Queue 004 from F448 unused. Return to the mechanical low-coverage state
mutator/arithmetic inventory after the split-empire lifecycle family. Prioritize
player/team/city ownership transitions, cached derived-state invalidation, and
prospective-state helpers; preserve the same full producer + current-data + ancestry
+ duplicate threshold before numbering anything.

END C031-WIP155


===============================================================================
C031-WIP156 — QUEUE 004 CVPLAYER.CPP CONTINUATION — F448 / KI#771 CONFIRMED
===============================================================================

POST-WIP155 CONTROL: `CvPlayer::setStartingPlot`
-------------------------------------------------
The second-pass low-coverage inventory first exposed a genuine mutation-order flaw in
`setStartingPlot` when `bUpdateStartDist` is true:

* while removing an old start, the function decrements the old area's start count;
* it then calls `GC.getMap().updateMinOriginalStartDist(oldArea)` BEFORE assigning
  `m_pStartingPlot = pNewValue`;
* the recalculation iterates current player `getStartingPlot()` values and therefore
  still sees/re-adds this player's old starting plot;
* after `m_pStartingPlot` changes, only the NEW area is recalculated.

Thus a cross-area start move can leave the old area's `MinOriginalStartDist` cache
stale.

This was NOT numbered. Current normal-game consumers are the Boreal, Highlands and
Rainforest map scripts, and their start-assignment loops assign each player's first
start from NULL. Current C++ start-moving callers use the default
`bUpdateStartDist=false`. No ordinary supported current gameplay consumer was found
that performs the problematic old-start -> different-area transition while asking for
this cache update. Keep as API/WorldBuilder robustness debt, not F448.


F448 / KI#771 — RISE & FALL HUMAN/AI CONTROL SWITCH LEAVES CACHED SETTLER
"NEW CITY" PRODUCTION COST PRICED FOR THE PREVIOUS CONTROLLER
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Current normal Rise & Fall chapter transitions reach the stale cache with
current SAS data. This is an inherited AdvCiv integration regression introduced when
advc.251 made the cached Settler surcharge depend on human/AI handicap modifiers.

ROOT
----
Founding units do not obtain their city-equivalent production surcharge dynamically.
`CvPlayer::resetCivTypeEffects` and `CvPlayer::processTraits` precompute it and store it
per unit class:

    if (GC.getInfo(eUnit).isFound())
        setUnitExtraCost(kCiv.unitClass(eUnit), getNewCityProductionValue());

`CvPlayer::getProductionNeeded(UnitTypes)` later adds that cached value directly:

    iProductionNeeded += getUnitExtraCost(eUnitClass);

Current SAS `UNIT_SETTLER` has XML `iCost = 0`, so in an Ancient normal start the
Settler's production cost is effectively this cached "new city" surcharge (subject to
any later Python modifier), rather than a nonzero XML base cost.

`getNewCityProductionValue()` is controller/handicap-sensitive. It includes:

* free-start-era building production costs through `getProductionNeeded(Building)`;
* Advanced Start city cost;
* population costs through `getGrowthThreshold`, whose AI branch applies the game
  handicap's `iAIGrowthPercent` when `!isHuman()`;
* finally half of `trainingModifierFromHandicap(false)`, which also applies game
  handicap AI production modifiers when `!isHuman()`.

Therefore `isHuman()` and the player's handicap are inputs of the cached value.

RISE & FALL CHANGES BOTH INPUTS WITHOUT REFRESHING THE CACHE
-------------------------------------------------------------
`RiseFall::setPlayerControl(civId, b)` does, in order:

    setPlayerHandicap(civId, b, b);
    if (b)
        g.changeHumanPlayer(civId);
    else
        civ.setIsHuman(false, true);

`setPlayerHandicap` writes the player's handicap and updates maintenance/AI work
assignment only. `CvPlayer::setIsHuman` changes slot/human/AI state and alert/leader
state only. Neither recomputes founding-unit extra cost.

A complete current-tree search finds the actual recomputation only in:

1. `CvPlayer::processTraits`, and
2. `CvPlayer::resetCivTypeEffects`.

Normal Rise & Fall chapter transitions call `setPlayerControl` directly when the old
chapter ends and when the next civilization becomes active. `RFChapter::start`, the
welcome path and the surrounding chapter-control code do not call either refresh
function. Hence the stale value persists into ordinary production calculations.

CURRENT SAS REPRODUCTION — ANCIENT / NORMAL / MONARCH
------------------------------------------------------
Current SAS makes the difference deterministic without traits or later-era free
buildings.

Relevant current/inherited values:

    UNIT_SETTLER iCost                         = 0
    Ancient FreePopulation                    = 0
    Normal research/speed percent             = 100
    BASE_CITY_GROWTH_THRESHOLD                = 20
    CITY_GROWTH_MULTIPLIER                    = 2
    ADVANCED_START_CITY_COST                  = 67
    ADVANCED_START_POPULATION_COST            = 150
    INITIAL_CITY_POPULATION                   = 1
    current RF_PLAYER_HANDICAP_ADJUSTMENT     = 0
    Monarch iAIGrowthPercent                  = 90
    Monarch iAITrainPercent                   = 90
    Monarch iAIConstructPercent               = 90
    human BaseGrowth/Train/Construct percents = 100

There are no Ancient `FreeStartEra` buildings in current SAS, so the simple Ancient
case begins with the city + population terms only.

Fresh HUMAN calculation on Monarch:

    growth threshold at pop 1 = 20 + 1*2 = 22
    population value          = 22 * 150% = 33
    city value                = 67
    subtotal                  = 100
    half-training adjustment  = 100%
    getNewCityProductionValue = 100

Fresh AI calculation on Monarch (AI player handicap Noble, game-handicap AI factors
90%):

    growth threshold at pop 1 = round(22 * 90%) = 20
    population value          = 20 * 150% = 30
    city value                = 67
    subtotal                  = 97
    training modifier         = 90%
    half-training factor      = 95%
    round(97 * 95%)           = 92
    getNewCityProductionValue = 92

Thus a normal Rise & Fall handoff can produce both directions:

AI -> HUMAN chapter start
~~~~~~~~~~~~~~~~~~~~~~~~~
The civilization had its founding-unit surcharge cached as 92 while AI-controlled.
`setPlayerControl(..., true)` changes handicap/human status, but no refresh follows.
The now-human player can therefore continue to have:

    getProductionNeeded(UNIT_SETTLER) = 92

when the same current player state freshly evaluated should price the Settler at 100.

HUMAN -> AI chapter end
~~~~~~~~~~~~~~~~~~~~~~~
The old chapter civilization can retain the human-priced 100 surcharge after becoming
AI-controlled, even though a fresh AI calculation is 92. Its AI Settlers can therefore
remain 8 production too expensive in this clean current example.

The exact numeric gap varies with handicap, game speed/start era and free-start-era
buildings, but the cache invariant is broken whenever the controller-sensitive inputs
change enough to alter `getNewCityProductionValue`.

The current SAS removal of the old human-only "round to multiples of 5" production
rounding does not mask the root; the current `.round()` path still yields the 100 vs
92 example above.

CALLER / GAMEPLAY SIGNIFICANCE
------------------------------
This is not a stale tooltip-only number. `CvPlayer::getProductionNeeded(UnitTypes)` is
the authoritative unit production-cost calculation used by cities/AI and directly
adds `getUnitExtraCost(eUnitClass)`.

Because the current Settler XML base production cost is zero, this cache is especially
important: the cached city-equivalent surcharge supplies the Settler's actual base
production requirement.

Normal Rise & Fall chapter progression repeatedly performs the relevant transitions:

* at a chapter end, `setPlayerControl(currentChapterCiv, false)`;
* when the chosen next civ becomes active, `setPlayerControl(nextCiv, true)`.

No debug/Civ-Changer path is needed for reproduction.

ANCESTRY / INTRODUCING CHANGE
-----------------------------
K-Mod 1.46 already caches `getNewCityProductionValue()` in `m_aUnitExtraCosts`, but
its `getNewCityProductionValue()` does NOT apply the later advc.251 human/AI training
adjustment. The cached value therefore did not acquire this controller-switch
invariant from K-Mod.

AdvCiv practical 1268 / commit:

    e11525d3039c7b1baf017b3f5a25a963a8abef50
    "v0.89rc ... New game option 'Rise & Fall' added ..."

introduced Rise & Fall and its civilization-control switching. At that point the
K-Mod-style new-city value was still not controller-sensitive in the later advc.251
way, so this specific root was not yet present.

AdvCiv practical 1407 / commit:

    bf48af8b3529f23c93ddbf21fde05e6b4ad8539a
    2019-01-21 — "Many misc. tweaks"

introduced the regression. Its commit message explicitly says:

    "Production modifiers from difficulty only apply half to Settler units."

and the introducing diff changes `getNewCityProductionValue()` by adding:

    iValue = roundToMultiple(iValue *
        (1 + 0.5 * (trainingModifierFromHandicap(false) - 1)),
        isHuman() ? 5 : 1);

while also moving the cached unit extra cost after the ordinary unit handicap
multiplier so this city-equivalent portion gets the special half modifier.

That commit touched `RiseFall.cpp` only for unrelated capital-coordinate visibility
text and added no cache refresh to `setPlayerControl`. Thus practical 1407 made an
existing cached value depend on exactly the human/AI state that Rise & Fall changes,
without adding the required invalidation/recompute step.

Base AdvCiv 1.14 retains the controller-sensitive calculation and the missing Rise &
Fall refresh. Current SAS retains the same semantic structure (with later arithmetic
refactors/current handicap XML).

DUPLICATE / ALBUM CHECK
-----------------------
The album contains no prior Rise & Fall / Settler unit-extra-cost cache root.

The earlier literal "Finance unit extra cost" control in Queue 003 concerns
`UnitInfo::iExtraCost` XML scaling in Finance help and is explicitly data-dormant; it
is unrelated to the per-unit-class `m_aUnitExtraCosts` city-equivalent founding-unit
surcharge audited here.

No existing R&F finding covers production-cost cache refresh after human/AI chapter
control changes.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Centralize the founding-unit surcharge refresh rather than duplicating the current
loops. For example, a small `updateNewCityProductionValue()`/`updateFoundingUnitExtraCosts()`
helper can iterate the civilization's founding units and execute:

    setUnitExtraCost(unitClass, getNewCityProductionValue());

Then call it whenever an input of `getNewCityProductionValue()` changes outside the
existing trait/civilization reset paths.

For this root, the most important transition is AFTER the final human/AI state is
installed in `CvPlayer::setIsHuman`, because Rise & Fall deliberately changes the
handicap first and human status second. Recomputing after `setIsHuman` therefore sees
both final inputs.

For robustness, player-handicap changes that do not also change human status can alter
the same cached value. `RiseFall::setPlayerHandicap` currently even comments
"Further updates might be warranted." A dedicated refresh callable from both
handicap-change and human-state-change paths would make that dependency explicit and
also support nonzero `RF_PLAYER_HANDICAP_ADJUSTMENT` cleanly. Avoid unnecessary double
refresh in the paired Rise & Fall transition if desired by choosing one final
controller-switch hook or by structuring the helper/caller sequence accordingly.

Regression tests should cover both chapter directions at a difficulty where AI
production/growth modifiers differ from 100:

1. record a civ's `getUnitExtraCost(UNITCLASS_SETTLER)` while AI-controlled;
2. hand control to the human through normal R&F `setPlayerControl`;
3. assert cached extra cost equals a fresh `getNewCityProductionValue()`;
4. relinquish control and assert equality again under AI state.

On current Ancient/Normal/Monarch data, the clean expected fresh values are 92 for AI
and 100 for human.

NUMBERING
---------
F448 is CONFIRMED and consumes KI#771.
F449 / provisional KI#772 is the next unused root slot.


CURRENT STONE AFTER C031-WIP156
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F448 / KI#771 latest confirmed.
F449 / provisional KI#772 UNUSED.
Latest physical checkpoint: C031-WIP156.
No repository source files modified.

NEXT
----
Continue Queue 004 from F449 unused using the mechanical low-coverage
state-mutator/arithmetic inventory. Prioritize `getNewCityProductionValue` siblings /
other cached prospective costs only if they have an independent current invariant;
then move through Great Person/General counters, diplomacy/state validation and
player/team/city ownership transitions. Preserve the full current-data + caller +
ancestry + duplicate threshold before numbering.

END C031-WIP156


===============================================================================
POST-C031-WIP156 CONTINUATION — QUEUE 004 CvPlayer.cpp
===============================================================================

Continuation anchor:

    Queue 004 CvGameCoreDLL/CvPlayer.cpp OPEN
    32/169 unique files complete
    F448 / KI#771 latest confirmed
    F449 / provisional KI#772 UNUSED
    C031-WIP156 latest prior physical checkpoint
    no repository source files modified

The protected re-audit continued with the mechanical low-coverage inventory rather
than forcing siblings of F448.

POST-WIP156 CLEAN CONTROLS
--------------------------

1. Great General / Great Person threshold counters
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::setCombatExperience` safely handles threshold overshoot: after spawning a
Great General it recursively calls itself with the remaining combat XP, so an input
large enough to cross more than one threshold can continue through successive
thresholds. Current combat awards are normally much smaller in any case.

The player+team Great Person / Great General threshold-modifier increments in
`createGreatPeople` also match the inherited BtS/K-Mod scheme. No independent
counter/double-count root was established there.

2. `CvPlayer::validateDiplomacy`
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The Defensive Pact stale-offer check initially looked suspicious because it checks
the first offered trade item. The actual AI Defensive Pact contact producer constructs
a one-item Defensive Pact offer on each side, so there is no supported producer with
a later-list Defensive Pact silently surviving the cleanup. No F449 there.


F449 / KI#772 — GP BIRTH MESSAGE IS MUTATED BY ONE HIDDEN OBSERVER AND LEAKS THAT
FALLBACK INTO LATER VISIBLE OBSERVERS
===============================================================================

STATUS
------
CONFIRMED current inherited AdvCiv player-facing message-state defect.

Current SAS and Base AdvCiv 1.14 retain it. BtS/K-Mod do not have the defect in this
form. The regression is introduced by AdvCiv's advc.106 Great-Person birth-message
privacy integration and is already present in the reachable practical-1253 initial
AdvCiv history snapshot.

ROOT
----
`CvPlayer::createGreatPeople` constructs one detailed non-replay GP birth message
before iterating observers:

    CvWString szMessage;
    ...
    if (pCity != NULL)
        szMessage = TXT_KEY_MISC_GP_BORN(... city ...);
    else
        szMessage = TXT_KEY_MISC_GP_BORN_FIELD(...);

It then reuses that SAME mutable string for every observer:

    for (PlayerIter<MAJOR_CIV,KNOWN_TO> it(kGPOwner.getTeam()); it.hasNext(); ++it)
    {
        CvPlayer const& kObs = *it;
        bool const bRev = (... observer can see birth location ...);
        if (!bRev)
        {
            szMessage = gDLL->getText("TXT_KEY_MISC_GP_BORN_CIV", ...);
        }
        ...
        gDLL->UI().addMessage(kObs.getID(), ..., szMessage, ...,
                bRev ? kAt.getX() : -1, bRev ? kAt.getY() : -1, bRev, bRev);
    }

The hidden-observer branch permanently overwrites `szMessage` with the less-specific
"GP born in [civilization]" text. There is no corresponding branch that restores the
detailed city/field message when a LATER observer can reveal the location.

Consequently observer A's visibility state changes observer B's message text.

CURRENT CLEAN REPRODUCTION
--------------------------
No unusual XML, scenario scripting or EXE callback ordering is required.

Example:

* Player 2 owns the city in which the Great Person is born.
* Player 0 has met Player 2's team, so Player 0 belongs to the `KNOWN_TO` iterator,
  but Player 0 has not revealed the GP city / plot.
* Player 2 of course can reveal its own city and birth plot.

The cached major-alive player sequences are kept in ascending PlayerTypes order.
`PlayerIter<MAJOR_CIV,KNOWN_TO>` therefore visits the lower-ID Player 0 before the
higher-ID owner Player 2.

For Player 0:

    bRev == false

so the shared message is overwritten with `TXT_KEY_MISC_GP_BORN_CIV`.

Later, for Player 2:

    bRev == true

but the code does not reconstruct the detailed message. Player 2 therefore receives
the civ-only fallback text that was created for Player 0.

The inconsistency is especially visible because the SAME `addMessage` call still
passes the real birth coordinates and reveal/link booleans for Player 2:

    bRev ? kAt.getX() : -1
    bRev ? kAt.getY() : -1
    bRev
    bRev

Thus the owner can receive a location-enabled announcement whose text nevertheless
claims only the civilization-level location because an earlier observer could not see
the city.

The owner need not be the affected visible observer. Any later observer with location
visibility can inherit the fallback after an earlier hidden observer. Using the owner
simply gives the strongest deterministic visibility invariant.

ITERATOR / OWN-VISIBILITY PROOF
-------------------------------
`AgentPredicates.h` explicitly documents `KNOWN_TO` as:

    "Incl. the first agent's team (agents know themselves)"

so the GP owner's own team is part of this observer sequence.

`CvAgents` stores these player sequences ordered by player ID. `insertIntoVector`
walks until it encounters a greater ID and inserts before it, preserving ascending
`getID()` order. Normal `MAJOR_ALIVE` construction follows player-ID order as well.

A city also establishes sight for its owning team as part of ordinary city/plot
visibility processing. The detailed observer condition requires both the plot and,
when present, the city to be revealed; the owner satisfies that condition for its own
ordinary GP city. No speculative UI ordering is therefore involved in the repro.

ANCESTRY / INTRODUCING CHANGE
-----------------------------
BtS/K-Mod keep the detailed message immutable across observers. K-Mod's inherited
shape is:

    if (pPlot->isRevealed(observerTeam, false))
        addHumanMessage(... szReplayMessage ...);
    else
    {
        CvWString szMessage = TXT_KEY_MISC_GP_BORN_SOMEWHERE(...);
        addHumanMessage(... szMessage ...);
    }

The hidden fallback is a LOCAL string in the hidden branch. It cannot contaminate a
later visible observer.

AdvCiv practical 1253 / reachable initial-history commit:

    f6ff6a182f...

contains the advc.106 integration that introduces `TXT_KEY_MISC_GP_BORN_CIV` and,
critically, changes the observer loop to write that fallback directly into the shared
`szReplayMessage`:

    if (!isRev)
    {
        if (isMet)
            szReplayMessage = gDLL->getText("TXT_KEY_MISC_GP_BORN_CIV", ...);
        else continue;
    }
    ... addHumanMessage(... szReplayMessage ...);

That is the regression point visible in the reachable AdvCiv history: privacy was
improved from "somewhere" to civilization-level information for met observers, but
the fallback was no longer kept observer-local.

AdvCiv practical 1268 later renames/reuses the shared variable as `szMessage`; the
same mutation remains.

Most notably, practical 2188 / commit:

    66a705a7830c6df13301724d874f69ba792d054e
    "Fix minor info leak in GP birth messages"

changes the observer loop to `PlayerIter<MAJOR_CIV,KNOWN_TO>` and strengthens `bRev`
so a city birth requires the city itself to be revealed too. That privacy fix is
correct in isolation, but it retains:

    if (!bRev)
        szMessage = TXT_KEY_MISC_GP_BORN_CIV(...);

followed by unconditional reuse of `szMessage`. It therefore does not fix the
cross-observer state contamination; by creating more legitimate `bRev == false`
cases for partially revealed cities, it makes the importance of keeping the fallback
observer-local even clearer.

Base AdvCiv 1.14 and current SAS retain this exact shared-mutation structure.

DUPLICATE / ALBUM CHECK
-----------------------
No prior album finding covers Great-Person birth-message cross-observer state.

The nearest literal observer-message material is an unrelated nuke hidden-nationality
information leak. That concerns identifying real unit civilizations in nuke messages,
not mutation/reuse of a GP observer-specific message string.

No prior `createGreatPeople`, GP-birth, or `TXT_KEY_MISC_GP_BORN*` root owns this
behavior.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Keep the detailed message immutable and choose an observer-specific message without
mutating shared state.

For example:

    CvWString const szDetailedMessage = ... city/field message ...;
    CvWString const szCivMessage = gDLL->getText(
            "TXT_KEY_MISC_GP_BORN_CIV", ...);

    for (... observers ...)
    {
        bool const bRev = ...;
        CvWString const& szObsMessage = (bRev ? szDetailedMessage : szCivMessage);
        gDLL->UI().addMessage(..., szObsMessage, ...);
    }

The exact implementation can avoid constructing the fallback until first needed if
that matters, but the important invariant is that one observer must never mutate the
message subsequently used for another observer.

Regression test:

1. lower-ID observer has met the GP owner but cannot reveal the GP birth city;
2. higher-ID GP owner can reveal its own city;
3. generate a city-born GP;
4. assert lower observer receives the civilization-only fallback and no coordinates;
5. assert owner receives the detailed city message and real coordinates.

Also test the inverse player-ID ordering to ensure each observer's text depends only
on that observer's own `bRev`, never on whoever happened to be visited earlier.

NUMBERING
---------
F449 is CONFIRMED and consumes KI#772.
F450 / provisional KI#773 is the next unused root slot.


CURRENT STONE AFTER C031-WIP157
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F449 / KI#772 latest confirmed.
F450 / provisional KI#773 UNUSED.
Latest physical checkpoint: C031-WIP157.
No repository source files modified.

NEXT
----
Continue Queue 004 from F450 unused using the mechanical low-coverage inherited
state/arithmetic inventory. Resume after `createGreatPeople` / `validateDiplomacy`,
prioritizing large zero/one-mention functions that mutate persistent player state or
reconstruct runtime quantities. Preserve the current-data + caller/runtime + ancestry
+ duplicate threshold before numbering anything; a clean saturation tranche is
preferable to manufacturing F450.

END C031-WIP157

===============================================================================
C031-WIP158 — QUEUE 004 CVPLAYER DEEP RE-AUDIT: F450 STILL UNUSED / EVENT + MESSAGE + GA CONTROLS
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP157 with Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN,
F449/KI#772 latest confirmed and F450/provisional KI#773 unused.
No repository source files were modified.

POST-WIP157 MECHANICAL LOW-COVERAGE PASS
----------------------------------------
The next inherited-state pass ranked large zero/one-album-mention CvPlayer member
functions by body size/statefulness instead of hunting by intuition. Three suspicious
contracts were fully or substantially classified without manufacturing F450.

CONTROL — GOLDEN-AGE READINESS UNITCLASS VS CONSUMPTION UNITTYPE IS CURRENT-DATA MASKED
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
K-Mod intentionally changed `unitsGoldenAgeReady()` to require distinct Great Person
`UnitClassTypes`, but `killGoldenAgeUnits()` still records already-consumed kinds by
`UnitTypes`. In a mod with multiple Golden-Age-capable unit types mapped to the same
Great Person class, readiness and consumption could therefore disagree.

Current SAS data masks the mismatch: the Golden-Age-capable Great People are exactly
one unit type per Great Person class (Prophet, Artist, Scientist, Merchant, Engineer,
Spy), and current CivilizationInfos provides no alternate same-class Golden-Age unit
mapping. No current execution state was established in which the two definitions
diverge. Keep as latent robustness/data-extension debt; not F450.

CONTROL — TEAM EVENT PROPAGATION BRANCH IS A REAL ADVCIV REFACTOR REGRESSION, BUT DORMANT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::setEventOccured` currently contains:

    else if (GC.getInfo(eEvent).isTeam())
    {
        for (MemberIter itOtherMember(getTeam()); itOtherMember.hasNext(); ++itOtherMember)
        {
            if (itOtherMember->getID() != getID())
                continue;
            itOtherMember->setEventOccured(..., false);
        }
    }

The predicate is inverted for propagation: it discards every teammate except self.
K-Mod's corresponding loop is correct (`i != getID()` -> propagate to other team
members). Reachable-history commit practical 2416, SHA
`b9b15b001b7f47a1bef8f264332988026f87dedb`, title "Use EnumMap in CvPlayer; and
other refactoring changes", introduced the MemberIter rewrite and the inverted
predicate directly.

However, current SAS EventInfo and EventTriggerInfo XML contains no `<bTeam>1</bTeam>`
entry at all. The broken EventInfo::isTeam branch therefore has no current data
producer. Preserve as a genuine dormant AdvCiv regression, but do not spend F450.

CONTROL — AI PLAYER-LEVEL WORK-RATE PREDICTOR CONTAINS DUPLICATE +100, NORMAL CALLERS MASK IT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Current/Base AdvCiv `CvPlayer::getWorkRate(BuildTypes)` has an AI branch equivalent to

    iRate *= max(0, 100 + AIWorkRateModifier + 100);

whereas actual `CvUnit::workRate` and K-Mod use the normal single
`100 + AIWorkRateModifier`. Current handicap AIWorkRateModifier values are 0, so the
player-level helper would predict double ordinary AI worker rate if its AI branch were
used. Normal runtime worker progress uses unit workRate; the player helper's internal
uses are human-facing build prediction / a human selection-group finish check. No
ordinary shipped gameplay mismatch was established. AI-Auto-Play active-player UI is
a possible robustness edge still worth remembering, but not presently a numbered root.

LIVE LOW-SEVERITY CANDIDATE — POSTPROCESSMESSAGES DISCOUNTS ONE REAL MESSAGE WHEN NO TECH IS PERSISTENT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::postProcessMessages` contains the advc.106b heuristic:

    // Finishing a tech should generate a message ... don't count it ...
    if (getCurrentResearch() == NO_TECH)
        m_iNewMessages--;

This was introduced in the initial advc.106b implementation (reachable history already
contains the line in practical 1253 / `f6ff6a182f...`). The condition is only a proxy
for "a technology just finished". A human can persistently have `NO_TECH` without a
new completion message — the cleanest current case is after all researchable
technologies are already known. `doResearch` then fails to choose a target and retains
NO_TECH, while unrelated INFO/MINOR/MAJOR interturn messages still increment
m_iNewMessages normally.

At the next active turn, postProcessMessages subtracts one anyway. Therefore exactly
`MessageLimit + 1` genuine qualifying messages are treated as only `MessageLimit` and
can fail the `m_iNewMessages > iLimit` auto-open test. The underlying Turn Log entries
are not lost; only the auto-open heuristic is wrong. This is current and deterministic
but low-severity/UI-only, so F450 is intentionally still unused while higher-value
state/arithmetic functions are audited.

EVENT/STATE FOLLOW-UP
---------------------
`CvPlayer::canTrigger` was reconciled with its caller in `initTriggeredData`: it is
invoked on each candidate OTHER player, with the originating player passed as ePlayer.
Thus its `OtherPlayerHasTech` test against `this->getTeam()` correctly refers to the
candidate other player's team; no actor/target inversion was established.

NUMBERING
---------
F449/KI#772 remains the latest confirmed root.
F450/provisional KI#773 remains UNUSED.

CURRENT STONE AFTER C031-WIP158
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F449 / KI#772 latest confirmed.
F450 / provisional KI#773 UNUSED.
Latest physical checkpoint: C031-WIP158.
No repository source files modified.

NEXT
----
Continue Queue 004 from F450 unused using the mechanical low-coverage inherited
state/arithmetic inventory. Prefer gameplay-significant persistent-state, projection,
event, empire-transition and cache-contract functions before promoting the live but
low-severity postProcessMessages auto-open edge. Freeze immediately if a current root
survives caller/runtime + current-data + ancestry + duplicate proof; otherwise keep
closing controls and save another saturation checkpoint rather than manufacturing F450.

===============================================================================
C031-WIP159 — AUTHORITATIVE CORRECTION: F445/KI#768 RETRACTED
===============================================================================

STATUS CORRECTION
-----------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.

F445 / KI#768 is RETRACTED.
F446 / KI#769, F447 / KI#770, F448 / KI#771 and F449 / KI#772 remain CONFIRMED.
F450 / provisional KI#773 remains UNUSED.

C031-WIP153's F445 section is historical investigation context only and is
superseded by this correction. Later WIPs that inherited "F445 confirmed" in their
status lines are likewise superseded on that one ledger point; their independent
F446-F449 proofs are not affected.

No repository source files were modified.

-------------------------------------------------------------------------------
WHY F445 DOES NOT SURVIVE THE CROSS-FILE INVALIDATION PROOF
-------------------------------------------------------------------------------
WIP153 correctly observed that capital status participates directly in
`CvCity::getBaseYieldRateModifier`:

    if (isCapital())
        iModifier += GET_PLAYER(getOwner()).getCapitalYieldRateModifier(eYield);

and correctly noted that Bureaucracy makes a capital relocation change current
Production yield in ordinary current data.

The earlier proof then incorrectly concluded that `CvPlayer::setCapital` refreshed
only commerce and therefore failed to invalidate cached Production-yield ranks.

The missing downstream step is the exact implementation of the refresh that
`setCapital` calls.

Current `CvPlayer::setCapital` does:

    if (pOldCapital != NULL)
        pOldCapital->updateCommerce();
    if (pNewCapital != NULL)
        pNewCapital->updateCommerce();

The no-argument `CvCity::updateCommerce()` begins with:

    GET_PLAYER(getOwner()).invalidateYieldRankCache();

before looping over commerce types.

That player-level invalidation reaches every city and invalidates both base-yield and
yield-rate rank caches for every YieldTypes value. Thus the capital transition itself
already satisfies the cache-maintenance contract that WIP153 thought was missing.

A Bureaucracy Palace relocation therefore behaves as follows:

1. capital identity changes;
2. old/new actual Production modifiers change dynamically through `isCapital()`;
3. `setCapital` calls `updateCommerce()` on the old capital;
4. that call invalidates the player's complete yield-rank cache;
5. the next `findYieldRateRank(YIELD_PRODUCTION)` recomputes the city ordering from
   the post-relocation rates.

There is no stale Production-rank window under this source path.

-------------------------------------------------------------------------------
HOW THE CORRECTION SURFACED
-------------------------------------------------------------------------------
The F450 low-coverage pass reopened Golden-Age/base-yield updates and followed
`CvPlot::updateYield -> CvCity::changeBaseYieldRate -> setBaseYieldRate ->
updateCommerce()`. That exposed the otherwise easy-to-miss fact that the no-argument
commerce refresh is deliberately also a global yield-rank invalidation point.
Rechecking `setCapital` against that exact callee invalidated WIP153's root.

This is a useful cross-file methodology control: a setter's call-site name is not
sufficient evidence about what caches it refreshes; the complete callee body must be
traced before declaring a missing invalidation.

-------------------------------------------------------------------------------
NUMBERING / LEDGER RULE
-------------------------------------------------------------------------------
Because later independent roots F446-F449 have already been assigned and physically
preserved, do NOT renumber them. Follow the album's established duplicate/retraction
convention (e.g. the reconciled F419 gap):

    F445 / KI#768 — RETRACTED / not an implementable root
    F446 / KI#769 — confirmed
    F447 / KI#770 — confirmed
    F448 / KI#771 — confirmed
    F449 / KI#772 — confirmed
    F450 / provisional KI#773 — next UNUSED root

F445/KI#768 should not be implemented or counted as a distinct bug.

-------------------------------------------------------------------------------
POST-WIP158 F450 CONTROL
-------------------------------------------------------------------------------
A possible broader F448 sibling was also closed during the same cross-file pass.
`getNewCityProductionValue()` prices free start-era buildings through the PLAYER-level
`getProductionModifier(BuildingTypes)`, not `CvCity::getProductionModifier`.
The player-level function consists of trait production modifiers plus trait-derived
world/team/national-wonder production modifiers. It does not include ordinary
civic/state-religion city production bonuses. `processTraits()` explicitly rebuilds
founding-unit extra costs after trait/leader changes. Thus no ordinary civic-change
cache invalidation root was established; F448 remains specifically the later
human/handicap dependency that Rise & Fall fails to refresh.

CURRENT STONE AFTER C031-WIP159
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F449 / KI#772 CONFIRMED.
F450 / provisional KI#773 UNUSED.
Latest physical checkpoint: C031-WIP159.
No repository source files modified.

NEXT
----
Continue Queue 004 from F450 unused. Preserve the WIP158 live-but-low-severity
`postProcessMessages` edge and dormant event/work-rate controls, but prioritize a more
gameplay-significant state/arithmetic root if one survives. Keep the strengthened
cross-file invalidation discipline that retracted F445: trace every apparent missing
refresh through the full callee chain before promotion.

===============================================================================
C031-WIP160 — QUEUE 004 CVPLAYER DEEP RE-AUDIT CONTINUATION
F450 / KI#773 CONFIRMED — ADV. CIV PRACTICAL 2416 GLOBE MILITARY-STRENGTH
ARITHMETIC REGRESSION INVERTS LAND-UNIT WEIGHT AND DIVIDES BY ZERO FOR AIR UNITS
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F445/KI#768 remains RETRACTED.
F446/KI#769 through F450/KI#773 are CONFIRMED.
F451 / provisional KI#774 is the next UNUSED root.
No repository source files modified.

-------------------------------------------------------------------------------
F450 / KI#773 — `CvPlayer::getUnitLayerColors` computes military Globe-layer
strength as HP / (maxHP * baseCombatStr) instead of (HP / maxHP) * baseCombatStr
-------------------------------------------------------------------------------

CURRENT SAS CODE
----------------
In all three Globe unit-layer modes that accumulate a numeric military strength —
`SHOW_ALL_MILITARY`, `SHOW_TEAM_MILITARY`, and `SHOW_ENEMIES` — current SAS does:

    fPlotStrength += pUnit->currHitPoints() /
            (float)(pUnit->maxHitPoints() *
            pUnit->baseCombatStr());

The same code is present in Base AdvCiv 1.14.

K-Mod instead uses the expected expression:

    fPlotStrength += ((float)pUnit->currHitPoints() /
            (float)pUnit->maxHitPoints() *
            (float)pUnit->baseCombatStr());

The important difference is not cosmetic parenthesization. Current AdvCiv/SAS divide
by combat strength where the inherited calculation multiplies by it.

-------------------------------------------------------------------------------
DETERMINISTIC CURRENT LAND-UNIT REPRODUCTION
-------------------------------------------------------------------------------
Current SAS XML gives ordinary live examples:

    UNIT_WARRIOR   iCombat = 3
    UNIT_TANK      iCombat = 30

At full health, the intended/K-Mod contribution is therefore:

    Warrior: (100 / 100) * 3  = 3
    Tank:    (100 / 100) * 30 = 30

Current AdvCiv/SAS instead computes:

    Warrior: 100 / (100 * 3)  = 1/3
    Tank:    100 / (100 * 30) = 1/30

So, for equal-health single-unit plots, the Globe layer considers the Warrior's
numeric plot strength ten times the Tank's. The ordering is exactly backwards.

The function then normalizes each visible plot against the global maximum:

    fAlpha = (fPlotStrength / fMaxPlotStrength * 0.75f + 0.25f) * 0.8f;

With one visible full-health Warrior plot and one full-health Tank plot:

    current bad formula:
        Warrior alpha = 0.80
        Tank alpha    = (0.1 * 0.75 + 0.25) * 0.8 = 0.26

    intended/K-Mod formula:
        Tank alpha    = 0.80
        Warrior alpha = (0.1 * 0.75 + 0.25) * 0.8 = 0.26

Thus the map visualization does not merely have an imprecise scale. It visually ranks
weaker land units/plots as stronger than stronger ones. Stacks likewise sum reciprocal
base strengths rather than actual damaged base strength.

-------------------------------------------------------------------------------
AIR-UNIT CONSEQUENCE / SECONDARY STRENGTHENING
-------------------------------------------------------------------------------
AdvCiv's surrounding code deliberately classifies military units through:

    pUnit->canCombat();

and `CvUnit::canCombat()` is:

    return (baseCombatStr() > 0 || airBaseCombatStr() > 0 || isNuke());

Current SAS aircraft use the separate air-strength field and have zero ordinary
`baseCombatStr`. Verified current examples include:

    Fighter         iCombat = 0, iAirCombat = 12
    Jet Fighter     iCombat = 0, iAirCombat = 24
    Bomber          iCombat = 0, iAirCombat = 16
    Stealth Bomber  iCombat = 0, iAirCombat = 20

Those units therefore pass the `bMilitary` test but reach a denominator containing
`baseCombatStr() == 0` in the bad formula.

The land-unit inversion above is already sufficient to confirm F450 and does not rely
on platform floating-point divide-by-zero behavior. On the normal masked x86 floating
point environment, a positive float divided by zero may become infinity, which can
also poison `fMaxPlotStrength`/normalization; however that stronger platform-specific
manifestation is not needed for the root.

There is also an older independent design limitation in the pre-regression expression:
using only `baseCombatStr()` means aircraft contribute zero rather than their actual
air strength. The practical-2416 regression makes that old limitation worse by moving
zero into the denominator. Do not conflate fixing the introduced land arithmetic
regression with deciding whether the Globe layer should additionally use
`airBaseCombatStr()` for aircraft.

-------------------------------------------------------------------------------
CALLER / PLAYER-VISIBLE REACHABILITY
-------------------------------------------------------------------------------
This is the normal active-player Globe View path, not debug-only code:

    CvPlayer::getGlobeLayerColors(...)
        case GLOBE_LAYER_UNIT:
            getUnitLayerColors((GlobeLayerUnitOptionTypes)iOption, ...);

`getUnitLayerColors` fills the Globe-layer per-plot colors and unit indicators. The
wrong `fPlotStrength` directly determines the color alpha in the ordinary Unit Globe
layer for the affected modes.

No special scenario, AI-only path, future XML, assertion build, or uncommon team setup
is required. A visible Warrior and Tank are enough to produce the reversed intensity.

-------------------------------------------------------------------------------
INTRODUCTION / ANCESTRY
-------------------------------------------------------------------------------
The exact regression is visible in the supplied AdvCiv practical-2416 diff:

    practical 2416
    SHA b9b15b001b7f47a1bef8f264332988026f87dedb
    "Use EnumMap in CvPlayer; and other refactoring changes."

Before that refactor, the expression was:

    fPlotStrength += ((float)pUnit->currHitPoints() /
            (float)pUnit->maxHitPoints() *
            (float)pUnit->baseCombatStr());

Practical 2416 rewrote it to:

    fPlotStrength += pUnit->currHitPoints() /
            (float)(pUnit->maxHitPoints() *
            pUnit->baseCombatStr());

The same edit was made in the `SHOW_ALL_MILITARY`, `SHOW_TEAM_MILITARY`, and
`SHOW_ENEMIES` branches. There is no corresponding gameplay-rule change that could
justify taking the reciprocal of combat strength; this sits inside a broad refactor
commit and is a straightforward parenthesization/arithmetic regression.

K-Mod retains the correct multiply-by-strength expression. Base AdvCiv 1.14 and
current SAS retain the practical-2416 bad denominator unchanged.

The same practical-2416 audit also found the separate inverted `setEventOccured`
Team-event member predicate preserved in WIP158, but current SAS has no Team EventInfo
or Team EventTriggerInfo producer. F450 is different: the Globe-layer arithmetic is
live under current ordinary data and UI.

-------------------------------------------------------------------------------
DUPLICATE / ALBUM RECONCILIATION
-------------------------------------------------------------------------------
A literal and semantic album search found no existing root for:

    getUnitLayerColors
    fPlotStrength
    Globe military/unit-layer strength normalization
    SHOW_ALL_MILITARY / SHOW_TEAM_MILITARY / SHOW_ENEMIES

Earlier album material involving aircraft/baseCombatStr belongs to different roots or
controls (AI effective-strength/debug/attack-order contracts and the mixed-group Nuke
help finding). None covers this Globe View reciprocal-strength arithmetic.

Therefore F450/KI#773 is independent.

-------------------------------------------------------------------------------
REPAIR DIRECTION
-------------------------------------------------------------------------------
The conservative regression repair is to restore the inherited/K-Mod multiplication:

    fPlotStrength += (float)pUnit->currHitPoints() /
            (float)pUnit->maxHitPoints() *
            (float)pUnit->baseCombatStr();

in all three strength-accumulating branches.

That repairs the confirmed land-unit inversion and removes the newly introduced
zero-denominator problem while preserving the pre-practical-2416 behavior exactly.

A separate optional follow-up can make the AdvCiv air-inclusive `canCombat()` change
internally consistent by selecting an appropriate air strength when
`baseCombatStr()==0` (for example using `airBaseCombatStr()` for true aircraft). That
would be a broader correctness improvement, not required to prove or minimally fix the
practical-2416 regression itself.

CURRENT STONE AFTER C031-WIP160
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F450 / KI#773 CONFIRMED.
F451 / provisional KI#774 UNUSED.
Latest physical checkpoint: C031-WIP160.
No repository source files modified.

NEXT
----
Continue Queue 004 from F451 unused. First finish the practical-2416 self/other/member
predicate refactor sweep that produced both the dormant Team-event inversion and F450,
then resume the mechanically ranked low-coverage `CvPlayer` state/arithmetic helpers.
Keep full-callee-chain cache/invalidation proof before promoting any apparent missing
refresh, per the WIP159/F445 retraction control.

===============================================================================
C031-WIP161 — QUEUE 004 CvPlayer.cpp — POST-F450 PRACTICAL-2416 / EVENT CLEAN TRANCHE
===============================================================================
Date: 2026-08-31
Status: Queue 004 OPEN. F451 / provisional KI#774 remains UNUSED.
No repository source files modified.

This checkpoint preserves the first post-F450 closure tranche. Its purpose is not to
manufacture a finding after the strong practical-2416 Globe regression; it records
which nearby refactor-looking candidates were traced through full current semantics
and rejected, so they do not get rediscovered later.

-------------------------------------------------------------------------------
PRACTICAL-2416 ACCESSOR RELOCATION CONTROL — BARBARIAN UNIT UPKEEP
-------------------------------------------------------------------------------
The practical-2416 refactor appears at first glance to have removed meaningful logic
when `CvPlayer::getGoldPerUnit()` and `getGoldPerMilitaryUnit()` were moved to raw
one-line member getters. Older code returned 0 directly for Barbarians.

The whole introducing hunk shows that this was intentional relocation, not semantic
loss. `CvPlayer::initOtherData()` now initializes the ordinary per-unit rate with:

    changeGoldPerUnit(isBarbarian() ? 0 : iINITIAL_GOLD_PER_UNIT);

The current source retains an explicit AdvCiv comment explaining that the former
Barbarian special case had been implemented in the getters and was moved here; it
also records that the analogous military getter guard was considered unnecessary.
Current Barbarian initialization leaves the stored rates at the intended zero values.

Therefore the raw current getters do not establish F451.

-------------------------------------------------------------------------------
PRACTICAL-2416 HISTORICAL GLOBAL-BUILDING ITERATOR REGRESSION — ALREADY REPAIRED
-------------------------------------------------------------------------------
The same large refactor historically contained a genuine iterator conversion defect
in global-building trigger counting: an initially converted expression used an
unqualified/current-player building count where the loop player's count was intended.
Current SAS no longer contains that form. Its trigger scan correctly calls the loop
player (`itPlayer->getBuildingClassCount(...)`).

This is useful refactor-lineage evidence but repaired history, not a present root.

-------------------------------------------------------------------------------
EVENT-EXPIRATION SUBJECT/HOLDER ASYMMETRY — CURRENT DATA MASKS IT
-------------------------------------------------------------------------------
`CvPlayer::checkExpireEvent` is internally asymmetric for quests copied between
players. Most quest-expiration predicates deliberately inspect the originating player
stored in `kTriggeredData.m_ePlayer` (`kPlayer`) — state religion, civic, city, unit,
other player/city. The obsolete-tech branch instead tests:

    GET_TEAM(getTeam()).isHasTech(...)

where `getTeam()` is the player object that currently holds the copied event record.
If a Quest EventInfo were also Global, two holders of the same originating event could
therefore expire it according to different teams' technology states.

Current XML masks the path cleanly: a mechanical EventInfo parse finds ZERO entries
with both:

    <bQuest>1</bQuest>
    <bGlobal>1</bGlobal>

Global EventInfos are non-quests and return from `checkExpireEvent` through the
short-lived non-quest branch before obsolete-tech processing. Current Quest EventInfos
are local, so holder and originator are the same player for the relevant state.

This remains an API/robustness asymmetry, not current F451.

-------------------------------------------------------------------------------
TEAM EVENT PROPAGATION REGRESSION REMAINS DATA-DORMANT
-------------------------------------------------------------------------------
The previously preserved practical-2416 inversion in `setEventOccured` remains real:
its MemberIter loop currently `continue`s when the member is NOT self, retaining only
the originating player instead of the teammates. K-Mod/pre-refactor behavior is the
opposite and correct.

Current SAS still has no EventInfo/TriggerInfo producer with `bTeam=1`, so this branch
is not a shipped gameplay path. Keep as latent refactor debt rather than F451.

-------------------------------------------------------------------------------
OTHER LOW-COVERAGE CONTROLS STARTED
-------------------------------------------------------------------------------
* `changeGoldenAgeTurns` still follows the inherited transition contract. On entering
  or leaving a Golden Age it calls player `updateYield`; changed worked-plot yields go
  through `CvCity::changeBaseYieldRate`, which performs the normal yield-rank/cache and
  dependent-commerce updates. No F445-style missing invalidation survives that callee
  chain.
* `calculatePollution` keeps K-Mod's explicit sign convention: bad-health components
  are negative except population, and the outer subtraction converts them into
  positive pollution weights. No arithmetic sign root found.
* `isValidTriggerCorporation` retains the K-Mod correction requiring actual
  corporation presence and then independently checks Headquarters ownership when
  requested.
* `getTradeLayerColors` and the early `getCultureLayerColors` pass are visual-state
  builders using revealed/visible ownership and plot-group/culture state directly;
  no current gameplay-state contradiction established in this tranche.

-------------------------------------------------------------------------------
CURRENT STONE AFTER C031-WIP161
-------------------------------------------------------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F450 / KI#773 CONFIRMED.
F451 / provisional KI#774 UNUSED.
Latest physical checkpoint: C031-WIP161.
No repository source files modified.

NEXT
----
Continue the mechanically ranked low-coverage `CvPlayer` functions. Highest-value
remaining live/stateful targets include `promoteFreeUnit`, `addFreeUnit`,
`updateGroupCycle`, `doWarnings`, event trigger bookkeeping beyond the now-closed
Global-Quest case, espionage third-party announcement state, and larger player
state/arithmetic mutators not yet represented in the album. Preserve the current
standard: full callee-chain/invalidation and current-data reachability before assigning
F451.


===============================================================================
F451 / provisional KI#774 — countReligionSpreadUnits counts missionaries being
trained on other land areas, so area-specific AI missionary valuation can suppress
needed local missionary production
===============================================================================

STATUS
------
CONFIRMED — Better BTS AI 0.83 area-filter omission inherited through K-Mod and
AdvCiv, still live in current SAS. The recent KI#668 same-team missionary valuation
repair does not create this root; it exposes the same helper more broadly and makes
its intended area-local contract especially explicit.

ROOT
----
`CvPlayer::countReligionSpreadUnits(pArea, eReligion, bIncludeTraining)` correctly
filters already-existing spread units to `pArea`:

    FOR_EACH_UNIT(pLoopUnit, *this)
    {
        if (pLoopUnit->isArea(*pArea))
        {
            if (pLoopUnit->getUnitInfo().getReligionSpreads(eReligion) > 0)
                iCount++;
        }
    }

But the BBAI `bIncludeTraining` extension then counts every city currently training a
matching missionary, with no corresponding area condition:

    if (bIncludeTraining)
    {
        FOR_EACH_CITY(pLoopCity, *this)
        {
            UnitTypes eUnit = pLoopCity->getProductionUnit();
            if (eUnit != NO_UNIT)
            {
                if (GC.getInfo(eUnit).getReligionSpreads(eReligion) > 0)
                    iCount++;
            }
        }
    }

Thus the function's two halves use incompatible geography: completed missionaries are
area-local, while queued missionaries are empire-global.

The parallel corporation helper makes the intended contract unusually clear. Its
training loop explicitly uses:

    if (pArea == NULL || pLoopCity->isArea(*pArea))

before counting an executive in production. Religion has no analogous check.

CURRENT LIVE CONSUMER
---------------------
Current `CvPlayerAI::AI_missionaryValue(eReligion, pArea)` uses the helper with
`bIncludeTraining=true` in the area-specific internal-spread calculation. After the
KI#668 repair it sums the same area-local state for every living team member:

    for (MemberIter itMember(getTeam()); itMember.hasNext(); ++itMember)
    {
        iOurCitiesHave += pArea->countHasReligion(eReligion, itMember->getID()) +
                itMember->countReligionSpreadUnits(pArea, eReligion, true);
        iOurCitiesCount += pArea->getCitiesPerPlayer(itMember->getID());
    }

Then:

    if (iOurCitiesHave < iOurCitiesCount)
        ... positive internal spread value ...
    else
        iSpreadInternalValue = 0;

`CvCityAI::AI_bestSpreadUnit` calls this area-specific value for each religion the city
can spread. If the returned value is <= 0, that religion is skipped before the city
considers a missionary unit type.

DETERMINISTIC SINGLE-PLAYER REPRODUCTION
-----------------------------------------
No team-game interaction is needed.

Let one AI player have:

* Area A with two cities, P and T;
* P already has religion R and can train R's missionary;
* T lacks R;
* Area B with another city B that is currently training an R missionary.

For Area A, the real area-local accounting is:

    cities in Area A:                2
    cities already having R:        1
    R missionaries in Area A:       0
    R missionaries training in A:   0

So Area A still has one uncovered internal spread target.

Current code instead evaluates:

    pAreaA->countHasReligion(R, player)          = 1
    countReligionSpreadUnits(AreaA, R, true)     = 1
        (the +1 incorrectly comes from city B in Area B)
    iOurCitiesHave                               = 2
    iOurCitiesCount                              = 2

Therefore `AI_missionaryValue(R, AreaA)` sets `iSpreadInternalValue = 0`.
`AI_bestSpreadUnit` in producer P then skips religion R entirely, even though T in the
same land area still lacks R and the only missionary being trained is on another land
area.

Existing completed missionaries in Area B do NOT cause this suppression because the
first half of `countReligionSpreadUnits` filters them by `pArea`. The defect is specific
to the queued-unit extension.

CURRENT-DATA REACHABILITY
-------------------------
Current SAS has the ordinary seven religion-spreading missionary unit types:

    UNIT_PAGAN_MISSIONARY
    UNIT_HINDU_MISSIONARY
    UNIT_JEWISH_MISSIONARY
    UNIT_DAOIST_MISSIONARY
    UNIT_BUDDHIST_MISSIONARY
    UNIT_CHRISTIAN_MISSIONARY
    UNIT_ISLAMIC_MISSIONARY

Each has a live `ReligionSpreads` entry. Multi-land-area empires and simultaneous city
production are ordinary supported game states, so no special option, scenario-only
caller or future XML is required.

ANCESTRY
--------
Stock BtS/Civ4CE `countReligionSpreadUnits` contains only the completed-unit area-local
loop; it has no `bIncludeTraining` parameter.

Better BTS AI 0.83 introduced the extension. Its bundled changelog states both:

    "Cities now consider how many missionaries they already have plus those being
     trained when deciding whether to train another"

and:

    "Added option to CvPlayer::countReligionSpreadUnits and
     CvPlayer::countCorporationSpreadUnits to include units being trained"

K-Mod contains that BBAI religion extension unchanged: completed missionaries are
area-filtered, but its city-training loop has no `pArea` test. K-Mod's corporation
sibling, however, explicitly filters training cities with
`pArea == NULL || pLoopCity->area() == pArea`.

Base AdvCiv 1.14 and current SAS retain the same religion omission and corporation
area check. Thus the root is an inherited BBAI 0.83 implementation oversight, not a
SAS regression.

RELATION TO KI#668 / F345
-------------------------
F345/KI#668 diagnosed a different omission in `AI_missionaryValue`: before the SAS
repair, the area branch counted only the current player's cities and therefore dropped
reachable same-team cities entirely. Its repair direction explicitly called for
summing each team member's AREA-LOCAL cities and relevant missionaries so that
teammates do not independently train for the same missing target.

F451 is the lower-level geographic counting bug inside that helper. It already affected
single-player K-Mod/AdvCiv before KI#668: a missionary queued by the same player on
Area B could suppress production on Area A. The KI#668 team loop merely causes each
member's malformed training count to be included as well.

Therefore F451 is not a duplicate or consequence of F345. Fixing F345 does not fix
F451; fixing F451 remains necessary for the repaired F345 logic to actually be
area-local.

It is also separate from F340's missionary-sea carrier validation bug, which concerns
whether a naval transport can carry missionaries at all.

REPAIR DIRECTION
----------------
Make the religion training half obey the same `pArea` scope as the completed-unit half
and the corporation sibling. Conservatively:

    FOR_EACH_CITY(pLoopCity, *this)
    {
        if (!pLoopCity->isArea(*pArea))
            continue;
        UnitTypes eUnit = pLoopCity->getProductionUnit();
        ...
    }

Because the religion helper currently dereferences `pArea` unconditionally for its
existing-unit scan, preserving its current non-NULL contract is the narrowest change.
A broader NULL-compatible refactor analogous to the corporation helper is optional and
not required to repair F451.

The corrected function then consistently answers one question: how many matching
religion-spread units already exist OR are being trained in THIS requested area.

CURRENT STONE AFTER F451
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F451 / KI#774 CONFIRMED.
F452 / provisional KI#775 UNUSED.
Latest physical checkpoint: C031-WIP162.
No repository source files modified.

NEXT
----
Continue Queue 004 from F452 unused. First check the immediate spread-count siblings
and other low-coverage player helpers, but do not manufacture a corporation sibling:
`countCorporationSpreadUnits` already contains the missing area filter. Then return to
the mechanically ranked live `CvPlayer` arithmetic/state-mutating functions and
shipped callers. Keep the WIP159 lesson in force: trace full callees/invalidation and
current-data reachability before promotion.

===============================================================================
C031-WIP163 — Queue 004 continuation — F452 / provisional KI#775 CONFIRMED
===============================================================================

POST-WIP162 CONTROLS
--------------------
`CvPlayer::isSpeedBonusAvailable(BonusTypes, CvPlot const&)` was reopened because its
fallback accepts a bonus from the capital even when the unit's local plot group does
not contain that bonus. That would deserve a network-semantics review if the feature
were live. Current SAS Unit XML, however, contains zero `SpeedBonus` entries. This is
already independently established elsewhere in the album for the related TextMgr
speed-bonus helper. No current unit can therefore execute this movement-bonus path.
Classification: dormant future-data/API behavior; NOT F452.

The current Global + PickPlayer World News path can reveal the picked civilization's
adjective to an observer who knows the event owner but has not met the picked player.
The behavior is inherited from BtS/K-Mod and the strings are explicitly World News;
without a stronger game-information contract, that is not promoted from a visibility
caveat.

A field-level `CvCivicInfo` -> `CvPlayer::processCivics` reconciliation also closed.
Every current persistent player modifier in CivicInfo is processed; the remaining
fields are eligibility/upkeep/anger or runtime/city-dependent quantities queried
through their authoritative paths rather than cached here.

F452 / provisional KI#775 — Culture Globe layer can write a fifth culture color into
another plot's four-color storage (and one-past-end on the final plot)
-------------------------------------------------------------------------------

CURRENT CODE CONTRACT
---------------------
`CvPlayer::getCultureLayerColors` hardcodes:

    const int iColorsPerPlot = 4;
    aColors.resize(kMap.numPlots() * iColorsPerPlot, ...);

So the returned vector reserves exactly four `NiColorA` entries for every plot.

The same function builds `aieOwners` for each revealed owned plot. The territorial
owner is inserted unconditionally, then (when the plot is visible) every other
EVER_ALIVE player with at least 20% of total plot culture is appended.

The source itself explicitly acknowledges that this list can exceed the storage width:

    /* Ideally ==iColorsPerPlot, but my algorithm above can't guarantee that.
       Can be 1 greater than iColorsPerPlot because the territorial owner is
       always included. */
    FAssert(aieOwners.size() <= iColorsPerPlot + 1);

Thus size 5 is considered a supported outcome by the function itself.

The write loop nevertheless uses:

    for (int i = 0;
         i < std::max(iColorsPerPlot, (int)aieOwners.size()); i++)
    {
        ...
        aColors[iPlot * iColorsPerPlot + i] = kCurColor;
        ...
    }

When `aieOwners.size() == 5`, `i == 4` writes:

    aColors[iPlot * 4 + 4]

but the four slots belonging to plot `iPlot` are only offsets 0..3.

For any non-final plot this is the FIRST culture-color slot of plot `iPlot + 1`, so the
current plot can contaminate the next plot's Globe culture coloring. If the next plot
is skipped by the later loop (for example because its revealed owner is NO_PLAYER or
its total culture is zero), the foreign color can remain visible there.

If the five-owner state occurs on the map's final plot, the same write is exactly one
past `aColors.end()`. `operator[]` performs no bounds check, so this is an actual
out-of-bounds write / undefined behavior path. The gameplay/display corruption on
non-final plots establishes the defect without relying on any particular crash or heap
behavior from the final-plot case.

CURRENT REACHABILITY
--------------------
The five-entry case is reachable under current culture semantics; it does not require
future XML.

`CvPlot::calculateCulturalOwner` considers only ALIVE players for ordinary ownership.
`getCultureLayerColors`, deliberately under advc.099, includes EVER_ALIVE players so
culture from defeated civilizations remains represented.

`CvPlayer::setAlive(false)` kills units/cities/deals but does not clear the defeated
player's plot culture. This is consistent with the explicit culture-layer comment
"include defeated."

Therefore a visible tile can legitimately have, for example:

    current living territorial owner: 20 culture
    defeated civ A:                   20 culture
    defeated civ B:                   20 culture
    defeated civ C:                   20 culture
    defeated civ D:                   20 culture

The four defeated players are ignored as ownership candidates because they are dead;
the living player can remain the territorial owner. But the Globe layer inserts that
owner and then all four defeated EVER_ALIVE players, since each has at least 20% of
total culture. `aieOwners.size()` is therefore 5 and the bad fifth write executes.

The exact numbers need not be equal; defeated-player culture allows the territorial
owner to be below several historical culture contributors because only living players
compete for current ownership.

SHIPPED CALLER
--------------
`CvPlayer::getGlobeLayerColors(GLOBE_LAYER_CULTURE, ...)` directly calls
`getCultureLayerColors`. This is the normal active-player Culture Globe layer exposed
by `CvGameInterface`. There is no later DLL-side resizing or remapping that grants a
fifth slot per plot.

ANCESTRY
--------
K-Mod's culture layer allocates four colors per plot and always writes exactly four:

    aColors.resize(numPlots * 4, ...);
    for (int i = 0; i < iColorsPerPlot; ++i)
        aColors[iPlot * iColorsPerPlot + i] = ...;

AdvCiv practical 2086 / commit
`1c5f39d8bebad59307fd420de2cba052a666760e`, titled:

    "Fix a minor (AdvCiv) issue with culture layer coloring"

explicitly changed the prior fixed four-write loop to the current
`std::max(iColorsPerPlot, plot_owners.size())` loop after documenting that the owner
list can contain five entries. The vector allocation remained four entries per plot.

Practical 2416 later only performed iterator/name refactoring in this block; Base
AdvCiv 1.14 and current SAS retain the practical-2086 storage/write mismatch.

DUPLICATE LEDGER
----------------
Album and current WIP searches found no prior root for `getCultureLayerColors`,
`iColorsPerPlot`, the five-owner list, or this cross-plot/out-of-bounds write.
F450 concerns the neighboring UNIT Globe layer's inverted strength arithmetic and is
independent.

REPAIR DIRECTION
----------------
Never write more than the four slots allocated for a plot.

The narrow safety repair is to restore the write bound to:

    i < iColorsPerPlot

If preserving the most relevant four identities when five contributors qualify is
desired, select/truncate the owner list BEFORE writing (e.g. always keep the
territorial owner and choose the three largest remaining culture contributors). That
is a presentation improvement; the correctness requirement is simply that no plot
writes outside its four-slot segment.

CURRENT STONE AFTER F452
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F452 / KI#775 CONFIRMED.
F453 / provisional KI#776 UNUSED.
Latest physical checkpoint: C031-WIP163.
No repository source files modified.

NEXT
----
Continue Queue 004 from F453 unused. Finish the neighboring Culture/Religion/Trade
Globe-layer controls, then resume the mechanically ranked compiled player-state and
arithmetic functions. Keep current-data reachability and full-callee tracing mandatory;
no need to manufacture a sibling of F450/F452 if the remaining layers are clean.

================================================================================
C031-WIP164 — QUEUE 004 CONTINUATION — F453 / KI#776 CONFIRMED
================================================================================

CONTINUATION ANCHOR
-------------------
Resumed Queue 004 `CvGameCoreDLL/CvPlayer.cpp` from C031-WIP163 with F453 unused.
The first post-WIP163 tranche explicitly closed the Religion and Trade Globe-layer
siblings of F452, `updateTradeRoutes`, current CivicInfo processing, and a number of
low-coverage prospective/state helpers before widening to high-signal author caveats
and inherited differential controls.

POST-F452 CLEAN CONTROLS
------------------------
* `getReligionLayerColors` allocates and writes exactly one color per map plot; visible
  overlapping religion radii only update the alpha of that same slot. It does not share
  F452's four-slots-vs-five-writes contract failure.
* `getTradeLayerColors` likewise allocates one color per plot and indexes it only by valid
  map plot indices.
* `updateTradeRoutes` clears this player's prior reservations, sorts the player's cities,
  lets each city claim currently available partners, and recomputes city trade yields after
  assignment. No stale-reservation or missing-yield-refresh root survived.
* `getBestEventTech`, `getDiscoveryTech`, `getEspionageSpending`, `foundCorporation`,
  `isProductionMaxedProject`, capital commerce/yield modifier mutators, corporation-disable
  counters and war-weariness modifier handling all reconciled with their authoritative
  consumers/current data.
* `checkAlert` has a real robustness typo (`iAlertID > size` rather than `>= size`), but all
  shipped alert IDs are 0..3 for a four-element vector and internal iteration is `< size`.
  No current caller reaches the bad `== size` case; do not number it.
* `isSpeedBonusAvailable` remains dormant because current SAS Unit XML has zero SpeedBonus
  entries.
* The Global + PickPlayer World-News observer-visibility edge remains semantically ambiguous:
  some current WorldNews strings can name a picked civilization unknown to an observer, but
  BtS/K-Mod deliberately frame these as world news. No firm hidden-information contract was
  established; keep unnumbered.
* `startingPlotWithinRange` always returning false is intentional inherited BtS/K-Mod
  behavior, not a missing body. Starting-distance pressure was moved into `AI_foundValue` /
  `startingPlotDistanceFactor`; generic AdvCiv explicitly comments out the obsolete Python
  hard-exclusion loop. Some old/custom map scripts still call the wrapper, but that makes
  those calls redundant rather than restoring a DLL contract that the engine intended to use.

F453 / KI#776 — `findPathLength` DOUBLE-COUNTS SHARED PREREQUISITES AND CAN MAKE
`pushResearch` CHOOSE A MORE EXPENSIVE OR-PREQUISITE PATH
--------------------------------------------------------------------------------

ROOT
~~~~
`CvPlayer::findPathLength` recursively sums every AND-prerequisite branch independently and
then adds the shortest OR-prerequisite branch:

    for each AND prereq:
        iPathLength += findPathLength(prereq)

    for each OR prereq:
        iNumSteps = findPathLength(prereq)
        keep the smallest

The source itself contains the inherited K-Mod warning:

    // k146 (note): This will double-count any shared AND-prepreqs.

That caveat is behaviorally significant because `CvPlayer::pushResearch` uses
`findPathLength` to decide WHICH OR prerequisite to queue when a future technology is
selected.

`pushResearch` itself does not double-add a shared prerequisite: after one prerequisite
branch has been pushed, `isResearchingTech` makes the later recursive branch return early.
Therefore the estimator and the queue builder can rank alternatives differently.

CURRENT SAS REPRODUCTION
~~~~~~~~~~~~~~~~~~~~~~~~
The current SAS technology tree contains a concrete reachable reversal.

One simple reproducible state is Carthage (starting Fishing + Pottery), with these additional
technologies legitimately acquired and no existing research queue:

    Mysticism
    Calendar
    Masonry
    Seafaring
    Sailing
    The Wheel
    Writing
    Code of Laws
    Construction
    Mathematics
    Monarchy
    Navigation
    Engineering
    Water Wheel
    Machinery

Together with Fishing + Pottery, that is 17 known technologies. This set is reachable through
ordinary prerequisite-legal research; it does not require an impossible WorldBuilder-only
knowledge combination.

Now select `TECH_LIBERALISM` directly as the future research target.

Liberalism has:

    AND prerequisite: Banking
    OR prerequisites: Naturalism, Democracy

`pushResearch` first recursively queues Banking's missing prerequisite path. At the moment it
then compares Liberalism's two OR alternatives, the relevant queued/researching Banking-side
technologies are already recognized by `findPathLength` and do not affect the comparison.

For the Naturalism alternative, the ACTUAL additional queue is:

    Polytheism          80
    Monotheism         425
    Drama              260
    Sacred Architecture 650
    Aesthetics          850
    Medicine           1100
    Naturalism         1700
                       ----
                       5065

For Democracy, the actual additional queue is:

    Polytheism          80
    Meditation         425
    Civil Service      650
    Feudalism          850
    Printing Press    1400
    Democracy         1700
                      ----
                      5105

So Naturalism is the genuinely cheaper remaining route by 40 beakers.

But `findPathLength(Naturalism)` returns 5145, not 5065. The extra 80 is Polytheism counted
TWICE inside the Sacred Architecture subtree:

* Sacred Architecture has Monotheism as an AND prerequisite and Drama as an OR route;
* Monotheism's path requires Polytheism;
* Drama's path also requires Polytheism;
* `findPathLength` evaluates those branches independently, so each recursion charges the same
  unknown Polytheism;
* real `pushResearch` pushes the Monotheism branch first, making Polytheism `isResearchingTech`,
  so the Drama branch correctly does not queue/pay for Polytheism a second time.

The resulting comparison seen by current code is therefore:

    estimated Naturalism = 5145   (wrong: +80 duplicate Polytheism)
    estimated Democracy  = 5105   (correct for this state)

and `pushResearch` chooses Democracy.

The real unique queue costs are:

    Naturalism = 5065
    Democracy  = 5105

so the automatic future-tech queue selects the MORE EXPENSIVE OR route.

DEFAULT-COST STABILITY
~~~~~~~~~~~~~~~~~~~~~~
This reproduction does not rely on a hidden unusual cost option. Under the ordinary current
SAS Ancient-start / Normal-speed / Standard-world / Medium-sea setup, the relevant global
research-cost multipliers are 100%, current Era `iTechCostModifier` values are 0, and
TECH_COST_MODIFIER plus groundbreaking normalization are disabled/0. Thus the current XML
technology costs above are the corresponding team research costs for the ordinary one-member
human team (apart from any deliberately enabled special game-option modifiers).

SHIPPED CALLER / GAMEPLAY CONSEQUENCE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This is the normal future-tech selection path, not a diagnostic helper.

`CvNetResearch::Execute` handles an ordinary technology click and calls:

    kPlayer.pushResearch(m_eTech, !m_bShift);

`pushResearch` recursively queues mandatory prerequisites and calls `findPathLength` to choose
the shortest OR branch. Therefore clicking a future technology in the normal Tech Chooser can
silently construct a higher-cost research queue than another legal OR path.

The effect is not that the player becomes unable to choose the cheaper technologies manually;
it is that Civ4's automatic prerequisite-queue construction fails its own "shortest path"
contract.

ANCESTRY
~~~~~~~~
This recursive algorithm is inherited from BtS. Civ4CE's reconstructed BtS source contains the
same independent AND-recursion / minimum-OR structure. K-Mod retains it and explicitly added
(or preserved in its source lineage) the warning that shared AND-preprequisites will be
counted twice. Base AdvCiv and current SAS retain the same algorithm.

The current SAS technology graph provides the concrete Liberalism reversal above, turning the
long-standing caveat into a shipped deterministic queue-selection defect.

DUPLICATE LEDGER
~~~~~~~~~~~~~~~~
Album searches for `findPathLength`, shared/double-counted prerequisites, shortest OR paths and
`pushResearch` found no prior numbered root covering cost-estimator double counting or
selection of a non-minimal prerequisite queue.

Earlier `pushResearch` findings concern different contracts (e.g. stale choose-tech snapshots
or Barbarian `canResearch` legality) and do not own this path-cost error.

REPAIR DIRECTION
~~~~~~~~~~~~~~~~
The path estimator must count each prospective technology at most once within one candidate
queue calculation, just as real `pushResearch` does through `isResearchingTech` after a tech is
queued.

A robust direction is an internal path-cost helper that carries a prospective "already
counted / already queued" tech set through AND branches. OR alternatives must be evaluated
with independent copies of that prospective set, and the chosen alternative's resulting set
must then be merged into the caller's path state. This avoids both shared-AND double counting
and accidental cross-contamination between alternatives.

Alternatively, factor the queue-building prerequisite traversal into a non-mutating simulator
and use the resulting unique queued technology set/cost for OR comparisons. The important
requirement is that `findPathLength` and `pushResearch` model the SAME deduplication semantics;
merely adjusting the Liberalism XML would only mask the generic defect.

CURRENT STONE AFTER F453
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F453 / KI#776 CONFIRMED.
F454 / provisional KI#777 UNUSED.
Latest physical checkpoint: C031-WIP164.
No repository source files modified.

NEXT
----
Continue Queue 004 from F454 unused. Use Civ4CE/Base/K-Mod differential fixes as candidate
generators (never as authority), then resume the mechanically ranked compiled `CvPlayer`
state/arithmetic functions. Preserve the current-data + shipped-caller + duplicate-proof
threshold; clean saturation is preferable to manufacturing another root.

===============================================================================
C031-WIP165 — QUEUE 004 CONTINUATION — F454 / KI#777 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed Queue 004 `CvGameCoreDLL/CvPlayer.cpp` from C031-WIP164 with F454 unused.
The first continuation control reopened `CvPlayer::isSpeedBonusAvailable`; current SAS
Unit XML contains no actual SpeedBonus mapping (`<SpeedBonuses/>` is empty), so the
capital-network fallback is dormant and was not promoted. The ranked compiled-state pass
then reached `announceEspionageToThirdParties`, where a current observer-visibility defect
survives.

F454 / KI#777 — THIRD-PARTY CIVIC/RELIGION ESPIONAGE ANNOUNCEMENTS LEAK AN
UNREVEALED TARGET CAPITAL THROUGH RAW MESSAGE COORDINATES
--------------------------------------------------------------------------

ROOT
~~~~
`CvPlayer::announceEspionageToThirdParties(EspionageMissionTypes eMission,
PlayerTypes eTarget)` builds the AdvCiv 120f announcement sent to third-party major civs
that know the espionage target.

It obtains the target capital once:

    int iX = -1, iY = -1;
    CvCity* pCapital = GET_PLAYER(eTarget).getCapital();
    if (pCapital != NULL)
    {
        iX = pCapital->getX();
        iY = pCapital->getY();
    }

and then sends those SAME raw coordinates to every eligible observer:

    for (PlayerIter<MAJOR_CIV,OTHER_KNOWN_TO> it(TEAMID(eTarget)); ...)
    {
        CvPlayer const& kObs = *it;
        if (kObs.getID() != getID())
            gDLL->UI().addMessage(kObs.getID(), ..., iX, iY);
    }

There is no `isRevealed(kObs.getTeam())` test and no observer-aware capital-coordinate
lookup. Merely knowing the target civilization is enough to receive the target capital's
exact current map coordinates.

CURRENT SHIPPED REACHABILITY
~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Both affected mission families are enabled in current SAS
`CIV4EspionageMissionInfo.xml`:

    ESPIONAGEMISSION_SWITCH_CIVIC:
        iCost = 0
        iSwitchCivicCostFactor = 1000

    ESPIONAGEMISSION_SWITCH_RELIGION:
        iCost = 0
        iSwitchReligionCostFactor = 1000

`iCost == 0` is enabled; only `-1` disables a mission in
`getEspionageMissionBaseCost`.

Current `GlobalDefines_advc.xml` also enables the feature by default:

    ANNOUNCE_ESPIONAGE_REVOLUTION = 1

with the defining comment stating that third parties that know the target are notified.

A normal reproduction therefore needs only three major civilizations:

    A = spy owner
    B = espionage target
    C = third-party observer

C has met B but has never revealed B's capital. A successfully performs Switch Civic or
Switch Religion against B. `doEspionageMission` calls
`announceEspionageToThirdParties(eMission, B)` directly in both successful mission
branches. C is in `OTHER_KNOWN_TO(TEAMID(B))`, and unless C is A, C receives the message
with B's raw capital `(x,y)`.

The spy's mission target city need not be B's capital, so the mission itself does not
supply a reason for C to know those capital coordinates.

WHY THE COORDINATES ARE PLAYER-FACING INFORMATION
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvDLLInterfaceIFaceBase::addMessage` forwards `iFlashX/iFlashY` into the EXE message
payload. Its AdvCiv comment explicitly notes that flash information is used for:

    "the text color and coordinates stored in the Turn Log -- not just
     for the (disabled) icon."

Thus the coordinates are not an unused implementation detail. They become message/Turn
Log location data, with the normal Civ4 message-location navigation behavior.

LOCAL CONTROL PROVES THE INTENDED VISIBILITY CONTRACT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The neighboring ordinary religion-change announcement in
`CvPlayer::setLastStateReligion` already handles this exact problem correctly. It sends:

    getCapitalX(kObs.getTeam(), true),
    getCapitalY(kObs.getTeam(), true)

for each observer separately.

`CvPlayer::getCapitalX/Y(TeamTypes eObserver, bool bDebug)` return `-1` when the capital
has not been revealed to that observer. Therefore ordinary conversion news deliberately
avoids revealing a hidden capital, while the AdvCiv espionage-third-party helper bypasses
that observer-aware API and sends raw coordinates.

Other current announcement code likewise tests capital/city reveal state before attaching
coordinates. This makes the 120f behavior an integration omission rather than a necessary
property of Civ4 messages.

EXECUTION PATH
~~~~~~~~~~~~~~
For Switch Civic:

    doEspionageMission
      -> announceEspionageToThirdParties(eMission, eTargetPlayer)
      -> target.setCivics(...)

For Switch Religion:

    doEspionageMission
      -> announceEspionageToThirdParties(eMission, eTargetPlayer)
      -> target.setLastStateReligion(...)

There is no subsequent observer-specific revalidation or coordinate replacement.

ANCESTRY
~~~~~~~~
The third-party announcement helper is an AdvCiv feature marked `advc.120f`. It is absent
from the supplied BtS reconstruction and K-Mod source; Base AdvCiv 1.14 and current SAS
contain the same raw-capital-coordinate implementation. `GlobalDefines_advc.xml` identifies
120f as the feature that announces forced civic/religion espionage to third parties.

The exact local control (`setLastStateReligion` using observer-aware `getCapitalX/Y`) is
also present in current AdvCiv/SAS, so the omission survives in the feature that introduced
the extra announcement rather than being inherited from the stock message path.

DUPLICATE LEDGER
~~~~~~~~~~~~~~~~
Album searches for `announceEspionageToThirdParties`,
`ANNOUNCE_ESPIONAGE_REVOLUTION`, third-party espionage-capital coordinates, and hidden
capital/message-coordinate exposure found no prior numbered root.

The previously parked Global + PickPlayer World News visibility question is different:
it concerns whether a world-news TEXT string may identify an unmet picked civilization.
F454 is an unambiguous MAP-LOCATION leak against a local observer-aware coordinate API.

REPAIR DIRECTION
~~~~~~~~~~~~~~~~
Resolve message coordinates per observer, exactly like `setLastStateReligion`.
Conservatively, inside the observer loop use:

    int iX = GET_PLAYER(eTarget).getCapitalX(kObs.getTeam(), true);
    int iY = GET_PLAYER(eTarget).getCapitalY(kObs.getTeam(), true);

(or equivalently test `pCapital->isRevealed(kObs.getTeam(), true)` and pass `-1,-1` when
hidden).

Keep the third-party text itself unchanged; F454 concerns only attaching information the
observer has not revealed.

CURRENT STONE AFTER F454
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F454 / KI#777 CONFIRMED.
F455 / provisional KI#778 UNUSED.
Latest physical checkpoint: C031-WIP165.
No repository source files modified.

NEXT
----
Continue Queue 004 from F455 unused. Resume the mechanically ranked compiled
`CvPlayer` arithmetic/state-mutating functions and shipped callers. Keep the dormant
`isSpeedBonusAvailable`, Team-event inversion, AI-work-rate branch and World-News text
visibility question as controls rather than manufacturing siblings. Prefer clean
closure over a weak final root.

===============================================================================
C031-WIP166 — QUEUE 004 CONTINUATION — F455 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F454 / KI#777 CONFIRMED.
F455 / provisional KI#778 UNUSED.
Latest physical checkpoint: C031-WIP166.
No repository source files modified.

POST-F454 CLEAN CLOSURE TRANCHE
--------------------------------
This checkpoint preserves the post-WIP165 investigation before continuing deeper. No
additional root is numbered merely to show progress.

1. `CvPlayer::isSpeedBonusAvailable` remains dormant in current SAS data. The current
   UnitInfos contain no actual non-empty SpeedBonus mapping, so the capital-network fallback
   cannot affect a shipped unit's movement.

2. `calculateTotalExports` / `calculateTotalImports` were reconciled with their Python/API
   semantics and current Info Screen use. They intentionally measure the commerce generated
   on the relevant trade-route side, rather than forming a player's ordinary trade-yield
   total. No swapped-import/export or subject mismatch survived.

3. `canTradeNetworkWith` was reconciled against the trade-network relation. Its two symmetric
   city loops intentionally permit a connection through any qualifying city attached to the
   respective capital networks; it is not required to test only capital-to-capital directly.

4. The AdvCiv 2212 negative-research-turn fix remains intact. The only nearby teammate
   overflow concern is the already documented robustness caveat in
   `getResearchTurnsLeftTimes100`: teammate overflow is converted with the subject player's
   research modifier. Current SAS has both human and AI groundbreaking penalties disabled,
   leaving the shipped teammate modifier symmetric for the relevant normal callers.

5. The AdvCiv 2328 team-game trade-route ordering remains intentional: another player's
   anarchy blocks routes before same-team acceptance, while the self case is exempted.

6. Random-event `canDoEvent` versus `applyEvent` was reconciled field by field for the main
   other-player / gold / tech / city-conversion / religion / bonus / war / attitude effects.
   The apparent `iMaxNumReligions` off-by-one is not a bug: both `CvPlayer` and
   `CvCity::canApplyEvent` treat the value as a PRE-EVENT eligibility cap. Thus current
   Inspired Mission's `iMaxNumReligions=1` deliberately permits selecting a city that already
   has one religion and then adding the event religion.

7. Vote/resolution helpers (`getVotes`, `canDoResolution`, `canDefyResolution`, full/voting
   membership) were checked for player/team/master/vassal role confusion. PlayerTypes forms
   passed to overloaded `GET_TEAM` remain type-safe through CoreAI's player overload, and the
   current master-team restrictions are explicit. No new team-game root survived.

8. Civ4CE was added only as an independent candidate generator. Its BtS reconstruction did
   not immediately expose a current SAS `CvPlayer` defect not already covered by the local
   BtS/K-Mod/AdvCiv comparisons. It is not treated as authority.

PARKED, NOT NUMBERED
--------------------
- Global + PickPlayer World News can name a picked civilization to observers who have not met
  that civilization; because these strings are explicitly World News, the intended
  information-visibility contract remains ambiguous. F454 is different and unambiguous
  because it leaks raw hidden capital coordinates against a local observer-aware API.
- The practical-2416 Team-event propagation inversion is real source debt but current SAS has
  no bTeam event/trigger data that enters the branch.
- The inherited/AdvCiv AI `getWorkRate` duplicate `+100` branch can become reachable in AI
  Auto Play but does not affect ordinary worker progress; still below numbered-root threshold.
- Golden-Age UnitClass-vs-UnitType distinctness remains data-masked by current one-GP-type-per-
  class mappings.
- `postProcessMessages` can undercount Turn-Log auto-open messages while permanently at
  NO_TECH, but the messages themselves remain intact; retained as a low-severity UI edge.

NEXT
----
Continue Queue 004 from F455 unused. Prioritize compiled mutator/eligibility families with
real shipped callers: `processBuilding` and its availability/cache counters, production
eligibility, then espionage/research state. Preserve full callee-chain invalidation proof to
avoid repeating the retracted F445 mistake. Clean closure is preferable to a weak finding.

===============================================================================
C031-WIP167 — Queue 004 CvPlayer.cpp — post-WIP166 clean shipped-caller/lifecycle tranche
===============================================================================

Status at this checkpoint:
- Queue 003 CvGameTextMgr.cpp CLOSED.
- Queue 004 CvPlayer.cpp OPEN.
- 32/169 unique files complete.
- F445/KI#768 RETRACTED; do not implement.
- F446 through F454 confirmed.
- F455/KI#778 remains UNUSED.
- No repository source files modified.

Post-WIP166 closure work:

1. Espionage city target-vs-player invariant: current shipped callers mask the generic DLL hole.
   - Generic canDoEspionageMission / mission-cost paths accept eTargetPlayer separately from pPlot/pCity and do not impose one universal pCity->getOwner()==eTargetPlayer check.
   - However CvUnit::espionage derives eTargetPlayer directly from getPlot().getOwner().
   - Both relevant popup phases likewise derive the target player from the spy plot owner.
   - Therefore the normal shipped spy action cannot supply an arbitrary target player paired with another player's city. Keep as robustness/API debt, not F455.

2. Destroy/Buy Unit espionage popup contains a real dormant ID-vs-type mismatch.
   - The target-popup eligibility call passes pTargetUnit->getUnitType() as iExtraData.
   - Espionage mission base-cost code interprets iExtraData for this family as the target player's unit ID via getUnit(iExtraData).
   - The eventual popup button/execution data does use pTargetUnit->getID().
   - Current SAS has iDestroyUnitCostFactor=0 and iBuyUnitCostFactor=0 for all espionage missions, so these mission families are disabled. Do not number under current data.

3. Destroy Building/Project base-cost subject question remains deliberately unnumbered.
   - Mission base cost calls the spy owner's getProductionNeeded(Building/Project), whose AdvCiv implementation includes acting-player handicap/AI production modifiers.
   - This is inherited from BtS/K-Mod and may intentionally be part of espionage/difficulty scaling; no explicit victim-replacement-cost contract was found. Insufficient basis for a finding.

4. Global unit-instance concurrency candidate is current-data dormant.
   - CvPlayer::canTrain's game-wide max check adds only this team's UnitClassMaking to the game-created count, so simultaneous other-team production could in principle be undercounted.
   - Current SAS UnitClassInfos has no finite iMaxGlobalInstances at all. Team-level finite caps were likewise absent in the checked current data. No shipped root.

5. CvPlayer::processBuilding / derived availability-counter pass closes.
   - Free-building count transitions update cities at the zero<->positive boundary.
   - Yield/commerce/specialist-extra-commerces reach their expected city/cache refreshers.
   - Commerce-flexibility loss resets a slider only once all player/team flexibility sources are absent.
   - No missing authoritative cache/state update established.

6. Player alive/turn-active lifecycle controls close in current source.
   - setAlive performs unit cleanup before lowering team AliveCount, preserving the intended blockade/war-state ordering.
   - verifyAlive's AI Auto Play active-player defeat path changes control to another living AI before the old player is finally eliminated; the historical autosave fix remains represented.
   - revival reaches the current UWAI revived-player initialization repair.
   - No new F447-style player-dead/team-alive transition found in this pass.

Current stone:
Queue 004 CvGameCoreDLL/CvPlayer.cpp OPEN · 32/169 unique files complete ·
F445/KI#768 RETRACTED · F446–F454 CONFIRMED · F455/KI#778 UNUSED ·
C031-WIP167 latest physical checkpoint · no repository source files modified.

Next exact continuation:
Continue F455 unused through historical CvPlayer bug-fix seams and paired state counters,
prioritizing compiled normal-game paths. Preserve current-data reachability, caller,
ancestry and duplicate proof before assigning any finding number.

===============================================================================
C031-WIP168 — QUEUE 004 CVPLAYER.CPP CONTINUATION — F455 / KI#778 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED; do not implement.
F446 / KI#769 through F455 / KI#778 CONFIRMED.
F456 / provisional KI#779 UNUSED.
Latest physical checkpoint: C031-WIP168.
No repository source files modified.

F455 / KI#778 — PERMANENT ALLIANCE TEAM REASSIGNMENT DOES NOT RECOUNT
CACHED MILITARY-HAPPINESS GARRISONS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Current shipped Permanent Alliances can change a stationary unit's
`CvUnit::isGarrisonInTeamCity()` / `isMilitaryHappiness()` result without updating the
city's cached `m_iMilitaryHappinessUnits`. A later move can even decrement a cache that
never received the corresponding increment, producing a negative count in release builds
(and tripping the debug assertion).

ROOT
----
Current AdvCiv/SAS military-happiness eligibility is relational rather than simply an XML
unit flag:

    bool CvUnit::isMilitaryHappiness() const
    {
        return (m_pUnitInfo->isMilitaryHappiness() &&
                isGarrisonInTeamCity());
    }

    bool CvUnit::isGarrisonInTeamCity() const
    {
        if (!getPlot().isCity())
            return false;
        CvTeam const& kCityTeam = GET_TEAM(getPlot().getTeam());
        if (isInvisible(kCityTeam.getID(), false))
            return false;
        return (kCityTeam.getID() == getTeam() ||
                kCityTeam.getMasterTeam() == getTeam() ||
                kCityTeam.getID() == GET_TEAM(getTeam()).getMasterTeam());
    }

`CvCity` does not derive its military-happiness unit count on demand. It stores
`m_iMilitaryHappinessUnits`, initialized/rebuilt by:

    CvCity::updateMilitaryHappinessUnits()
      -> plotCount(PUF_isMilitaryHappiness) - cached count

Ordinary unit movement maintains that cache incrementally in `CvUnit::setXY`:

    leaving city:  if (isMilitaryHappiness()) changeMilitaryHappinessUnits(-1);
    entering city: if (isMilitaryHappiness()) changeMilitaryHappinessUnits(+1);

This is only correct if every state transition that can change `isMilitaryHappiness()` for a
stationary unit performs a full recount.

PERMANENT ALLIANCE VIOLATES THAT CACHE CONTRACT
------------------------------------------------
`CvTeam::addTeam(eTeam)` absorbs every player on `eTeam` by calling:

    GET_PLAYER(...).setTeam(getID());

`CvPlayer::setTeam` updates team membership/alive/city/population/land counters and attitude
state, but never calls `updateMilitaryHappinessUnits`.

For stationary units and cities, changing the player's team can immediately change
`isGarrisonInTeamCity()`:

* an absorbed player's unit standing in a survivor-team city was not a same-team garrison
  before the alliance, but becomes one as soon as its owner is reassigned;
* a survivor-team unit standing in an absorbed player's city becomes a same-team garrison
  once the city owner is reassigned and the plot's cached team is refreshed.

`CvTeam::addTeam` later loops across all map plots and calls `kPlot.updateTeam()`, but that
function only assigns the cached plot team from its owner. It does not recount city military
happiness.

A complete current-tree caller search finds full military-happiness recounts only in:

* `CvCity::init` (city initialization), and
* `CvTeam::setVassal`, through `CvTeam::updateMilitaryHappinessUnits` for both sides.

There is no corresponding recount in `CvPlayer::setTeam` or the ordinary independent-team
Permanent Alliance path in `CvTeam::addTeam`.

DETERMINISTIC CURRENT REPRODUCTION
----------------------------------
Current SAS supports the full state without custom data:

* `GAMEOPTION_PERMANENT_ALLIANCES` is visible and supported (default off, user-selectable);
* current `TECH_GAME_THEORY` has `bPermanentAllianceTrading=1`;
* `CIVIC_HEREDITARY_RULE` has `iHappyPerMilitaryUnit=1`;
* ordinary military units such as Warrior, Archer and Rifleman have
  `bMilitaryHappiness=1`.

Example:

1. Player A / Team A owns City A and has no military-happiness unit counted there.
2. Player B / Team B has a Warrior stationed in City A under the pre-alliance diplomatic
   access. Because the teams are independent, `isGarrisonInTeamCity()` is false and City A's
   cached `m_iMilitaryHappinessUnits` correctly remains 0.
3. A and B form a Permanent Alliance; assume Team A survives the merge.
4. `CvTeam::addTeam` calls B's `setTeam(Team A)`. The stationary Warrior's
   `isGarrisonInTeamCity()` now becomes true immediately, but City A's cached count remains 0.
5. Under Hereditary Rule, City A therefore misses the +1 happiness that the now-same-team
   Warrior should provide.
6. If that Warrior subsequently moves out, `CvUnit::setXY` evaluates the CURRENT predicate,
   sees `isMilitaryHappiness()==true`, and executes `changeMilitaryHappinessUnits(-1)`.
   The stale cache goes 0 -> -1. `CvCity::changeMilitaryHappinessUnits` asserts that the count
   is nonnegative, so debug builds expose the invariant violation directly; release builds
   retain the invalid negative count.

The negative count is not merely cosmetic. `CvCity::getMilitaryHappiness()` multiplies the
cached count by the player's `getHappyPerMilitaryUnit`, and several happiness/anger/AI and
occupation checks consume either that result or `getMilitaryHappinessUnits()` directly.

ANCESTRY
--------
K-Mod's `CvUnit::isMilitaryHappiness()` simply returned the XML military-happiness flag; its
city movement bookkeeping therefore did not have this same team-relation cache dependency.

AdvCiv commit `177701cd0a0f4a295a59b4c7bf2d1c8143853763` (2023-09-06,
"Allow vassal/ master to help suppress 3rd-party revolts", change `advc.184`) introduced
`isGarrisonInTeamCity()` and made military-happiness eligibility depend on team/master-vassal
relations.

AdvCiv commit `6c12950422223708a8ec4c1fe0a90834fc10b0cf` (2024-07-19,
"Fix a rare issue with counting of military happiness units") subsequently recognized the
cache-invalidation consequence. It introduced `CvCity::updateMilitaryHappinessUnits`, player
and team wrappers, and explicitly recounts BOTH teams whenever `CvTeam::setVassal` changes a
master/vassal relation.

That fix demonstrates the intended invariant: relational changes that alter
`isGarrisonInTeamCity()` require a full recount. The Permanent Alliance path changes the even
more fundamental same-team relation through `setTeam`, but no analogous recount was added.
Base AdvCiv 1.14 and current SAS retain that omission.

DUPLICATE LEDGER
----------------
Album searches for `advc.184`, `updateMilitaryHappinessUnits`,
`isGarrisonInTeamCity`, military-happiness + Permanent Alliance, and garrison + alliance found
no prior numbered root. Earlier Permanent Alliance findings concern diplomacy/history,
espionage, research/project migration, visibility and other team-keyed state, not the city
military-happiness cache.

REPAIR DIRECTION
----------------
Perform a full military-happiness recount after the Permanent Alliance team reassignment and
after plot team caches have been refreshed.

The safest local placement is in `CvTeam::addTeam`, after the map loop that calls
`kPlot.updateTeam()`, because units in cities formerly owned by the absorbed team need those
plot-team caches to reflect the surviving team before `isGarrisonInTeamCity()` is reevaluated.
At that point the surviving team's `updateMilitaryHappinessUnits()` covers all cities now
owned by both sets of merged players.

Do not merely add an incremental adjustment in `CvPlayer::setTeam`: a team reassignment can
change eligibility for many stationary units in many cities at once, and for absorbed-team
cities the plot's cached team is not yet updated at the moment `setTeam` is called.

POST-F455 CONTROL — ADVANCED-START / advc.181 FoW DEFAULTS
----------------------------------------------------------
The historical `advc.181` Fog-of-War fix remains intact in its primary shipped human action
recommendation/help callers. Some low-level/default APIs (including Advanced Start city-cost
validation) still invoke `canFound` with all-seeing/default semantics, but no sufficiently
clean supported information-leak reproduction was established in this tranche. Keep that as
a caller/API audit lead, not F456.

CURRENT STONE AFTER F455
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN · 32/169 unique files complete ·
F445/KI#768 RETRACTED · F446–F455 CONFIRMED · F456/KI#779 UNUSED ·
C031-WIP168 latest physical checkpoint · no repository source files modified.

NEXT
----
Continue Queue 004 from F456 unused. Resume the historical bug-fix seam sweep, especially
special-commerce cache, Random Personalities, military/state transition siblings and
compiled normal-game player mutators. Keep complete callee-chain invalidation proof before
numbering anything.

===============================================================================
C031-WIP169 -- QUEUE 004 DEEP RE-AUDIT: RANDOM-PERSONALITIES FAVORITE-CIVIC STRATEGY LEAK
Date: 2026-09-01
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp / cross-file CvPlayerAI state audit remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN under the deeper inherited/stateful re-audit.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F455 remain confirmed.
- New confirmed root in this checkpoint: F456 / KI#779.
- Next root number: F457 / KI#780 remains UNUSED.
- No repository source files were modified.

F456 / KI#779 -- Random Personalities: Missionary strategy still uses the hidden personality's favorite civic after advc.130n moved favorite ideology to the displayed leader
---------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
AdvCiv `advc.130n` deliberately centralized favorite-ideology semantics in `CvPlayer`:

    CivicTypes CvPlayer::getFavoriteCivic() const
    {
        return GC.getInfo(getLeaderType()).getFavoriteCivic();
    }

The accompanying source comment is explicit: the distinction matters for Random Personalities;
favorite ideologies are tied to the displayed leader type, while personality-specific numeric
attitude changes remain tied to the randomized hidden personality.

The 2022 commit converted many AI call sites from direct
`GC.getInfo(getPersonalityType()).getFavoriteCivic()` lookups to `getFavoriteCivic()`.
One live runtime lookup was missed in `CvPlayerAI::AI_updateStrategyHash`:

    CivicTypes eCivic = GC.getInfo(getPersonalityType()).getFavoriteCivic();
    if (eCivic != NO_CIVIC && GC.getInfo(eCivic).isNoNonStateReligionSpread())
        iMissionary += 20;

This is the only remaining current DLL runtime direct favorite-civic lookup through
`getPersonalityType()` found by the full-tree search. Therefore the Missionary-strategy score
still lets the SECRET randomized personality's favorite civic leak into a field that AdvCiv
explicitly moved back to the DISPLAYED leader.

CURRENT DATA / SUPPORTED PRODUCER
---------------------------------
`GAMEOPTION_RANDOM_PERSONALITIES` is visible and supported (default off). `changePersonalityType`
can assign any non-Barbarian leader personality, merely reducing the random weight of personalities
already in use; it does not require personality == displayed leader.

Current SAS gives:

- displayed Asoka: FavoriteCivic = `CIVIC_PACIFISM`;
- Isabella personality: FavoriteCivic = `CIVIC_THEOCRACY`;
- Isabella personality: FLAVOR_RELIGION = 10, no Gold/Culture flavor;
- current Theocracy: `bNoNonStateReligionSpread = 1`.

The surrounding flavor terms are correctly personality-driven by design:
`AI_getFlavorValue` reads `GC.getInfo(getPersonalityType()).getFlavorValue(...)`.
The favorite-civic term is the exceptional field whose semantics advc.130n deliberately changed.

DETERMINISTIC REPRODUCTION
--------------------------
Use Random Personalities and a displayed Asoka whose hidden personality is Isabella. Construct an
ordinary AI state with:

1. current state religion present;
2. the AI owns that state religion's Holy City plus one additional Holy City (`iHolyCityCount=2`);
3. exactly one met civilization in a two-civ game (so AdvCiv's lonely-flavor adjustment is false);
4. that known civilization can be peacefully entered and has the same state religion;
5. the AI is not close to a religious victory.

The fixed non-random score terms are then:

    Isabella Religion flavor: 10 * 6 = 60
    one extra Holy City:      (2 - 1) * 5 = 5
    one met civ:              1 * 7 = 7
    same-religion target:     10
    -----------------------------------
    intended displayed-favorite score = 82

Asoka's displayed favorite is Pacifism, which does NOT forbid non-state religion spread, so under
the intended `getFavoriteCivic()` semantics no +20 applies.

Current code instead reads hidden Isabella's favorite Theocracy and adds +20:

    current broken score = 102

The strategy random term is only `(AI_getStrategyRand(9) % 7) * 3`, i.e. 0..18. The activation
threshold is strictly `iMissionary > 100`.

Therefore:

    intended score: 82..100  -> Missionary strategy NEVER activates
    current score:  102..120 -> Missionary strategy ALWAYS activates

This proves a deterministic strategy-bit flip without depending on a lucky strategy RNG result.

GAMEPLAY CONSEQUENCE
--------------------
`AI_STRATEGY_MISSIONARY` is not decorative. Current live consumers include:

- `AI_missionaryValue`: external-spread multiplier 60 when strategy active vs 25 otherwise;
- `CvCityAI::AI_bestSpreadUnit`: state-religion missionary chance is multiplied by 170% while
  other-religion spread is reduced to 65%;
- `AI_bestReligion`: Missionary strategy changes the acceptance path for the selected religion;
- strategy interactions elsewhere also avoid/alter competing strategy choices.

Thus Random Personalities can make the AI's religious expansion strategy depend on the hidden
personality's favorite civic even though favorite ideology is intentionally a displayed-leader
property throughout the rest of current AdvCiv/SAS.

ANCESTRY
--------
K-Mod inherited the Missionary-strategy code with a direct personality favorite-civic lookup.

AdvCiv commit `cbb9fe7ef68b7e8ea9ee5dd3ab5eb9883a83273d` (2022-10-29,
"Fave civics based on displayed leader type", practical commit 3974 / `advc.130n`) introduced
`CvPlayer::getFavoriteCivic()` and systematically converted many AI/UI favorite-civic call sites
from personality/leader-info lookups to that central accessor. The commit's source comment states
the intended displayed-leader semantics explicitly.

The Missionary-strategy line was not converted by that commit. Base AdvCiv 1.14 and current SAS
still contain the inherited K-Mod direct `getPersonalityType()` lookup. A full current DLL search
finds no other live direct favorite-civic lookup through `getPersonalityType()`.

DUPLICATE LEDGER
----------------
Album searches for Random Personalities, favorite/favourite civic, `advc.130n`, Missionary strategy,
`AI_STRATEGY_MISSIONARY`, displayed leader and hidden personality found no prior numbered root for
this lookup. An earlier Queue-001 control notes an odd post-advc.130n incremental ideology-attitude
helper, but that concerns attitude-cache refresh and is explicitly not a finding. It does not own
this live strategy-scoring mismatch.

REPAIR DIRECTION
----------------
Replace the one stale Missionary-strategy lookup with the centralized accessor:

    CivicTypes eCivic = getFavoriteCivic();

Do NOT replace the surrounding `AI_getFlavorValue` calls: those are intentionally based on the
randomized personality and are part of what Random Personalities is supposed to randomize.
The repair should change only the favorite-ideology term, matching every other advc.130n-converted
AI use.

CURRENT STONE AFTER F456
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN · 32/169 unique files complete ·
F445/KI#768 RETRACTED · F446-F456 CONFIRMED · F457/KI#780 UNUSED ·
C031-WIP169 latest physical checkpoint · no repository source files modified.

NEXT
----
Continue Queue 004 from F457 unused. First close the immediate Random-Personalities sibling sweep
(favorite religion and any remaining direct leader/personality semantic fields), then resume the
historical special-commerce/cache and ranked compiled player-state/arithmetic inventory. Preserve
full caller/current-data/ancestry/duplicate proof before numbering anything.

===============================================================================
C031-WIP170 -- QUEUE 004 DEEP RE-AUDIT: TWO-CIV RANDOM-TRIGGER WORLD-NEWS VISIBILITY
Date: 2026-09-01
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F456 remain confirmed.
- New confirmed root in this checkpoint: F457 / KI#780.
- Next root number: F458 / KI#781 remains UNUSED.
- No repository source files were modified.

POST-F456 RANDOM-PERSONALITIES SIBLING CLOSURE
----------------------------------------------
The immediate advc.130n sibling sweep is clean after F456. Current favorite-religion AI uses go
through `CvPlayer::getFavoriteReligion()`, and a full DLL search finds no surviving direct
`getPersonalityType()->FavoriteReligion` lookup. F456's Missionary-strategy favorite-civic line is
the sole current runtime direct favorite-civic lookup through `getPersonalityType()`.

The special-commerce-cache historical seam also remains internally consistent under the current
city->player update chain. No live transition was found that changes city special commerce while
bypassing the player's `m_aiCommerceRateTimes100` delta maintenance.

F457 / KI#780 -- Global PickPlayer trigger World News reveals an unmet picked civilization to observers; sibling event-news path was explicitly fixed in AdvCiv 2422
-----------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
`CvPlayer::setTriggerFired` announces a trigger's pre-expanded `m_szGlobalText` to third-party
observers. For a trigger involving `m_eOtherPlayer`, current code hoists this test OUTSIDE the
observer loop:

    bool bMetOtherPlayer = (kTriggeredData.m_eOtherPlayer == NO_PLAYER ||
            GET_TEAM(kTriggeredData.m_eOtherPlayer).isHasMet(getTeam()));

and then sends the World News text to each living observer when:

    bMetOtherPlayer && observerTeam.isHasMet(getTeam())

The first predicate asks whether the PICKED OTHER PLAYER has met the EVENT OWNER. It does NOT ask
whether the current OBSERVER has met that picked player.

`initTriggeredData` has already expanded the World News string once, including:

    eOtherPlayer != NO_PLAYER ? GET_PLAYER(eOtherPlayer).getCivilizationAdjectiveKey() : L""

Therefore every accepted observer receives the picked civilization's actual adjective even when
that observer has never met the picked civilization.

CURRENT SHIPPED PRODUCER / DETERMINISTIC REPRO
----------------------------------------------
Current `EVENTTRIGGER_FUGITIVE` is:

    bGlobal     = 1
    bPickPlayer = 1
    WorldNews   = TXT_KEY_EVENTTRIGGER_FUGITIVE

The stock BtS text inherited by current SAS is:

    "%s1_civ_adjective authorities are rumored to have captured a fugitive fleeing from
     %s4_civ_adjective justice. No confirmation is available."

So both the event owner's civilization and the picked civilization are explicitly named.

Construct three living major civilizations:

1. A is the player whose Fugitive trigger fires.
2. B is the picked `m_eOtherPlayer`; A and B have met.
3. C has met A but has never met B.

This is a valid trigger state because `CvPlayer::canTrigger` requires the candidate picked player
B to have met the triggering player A:

    if (!GET_TEAM(B).isHasMet(A.team)) return false;

When `setTriggerFired` announces the trigger:

    bMetOtherPlayer = B.hasMet(A) = true
    C.hasMet(A)                    = true

so C receives the already-expanded Fugitive World News text and learns B's civilization adjective,
despite C never having met B.

Other current Global + PickPlayer trigger texts such as `EVENTTRIGGER_FAUX_PAS` and
`EVENTTRIGGER_TEA` also explicitly contain the picked civilization adjective. Fugitive alone is
sufficient for the root.

AUTHORITATIVE CURRENT SIBLING CONTROL
-------------------------------------
This is no longer an ambiguous question about whether "World News" is intentionally omniscient.
AdvCiv itself explicitly established the opposite information contract.

Commit `71a7af8cd9ddd50f5fbbbafd17bec46698339993` (practical 2422), titled:

    "Report random events involving two civs only if both known"

fixed the later `CvPlayer::applyEvent` World News observer loop. The old code checked the other
player against the event owner's team. The fix changed that check to the CURRENT OBSERVER'S team:

    if (pTriggeredData->m_eOtherPlayer != NO_PLAYER &&
        !GET_TEAM(pTriggeredData->m_eOtherPlayer).isHasMet(kObs.getTeam()))
    {
        continue;
    }

Current SAS retains that observer-specific check in `applyEvent`. Thus event-choice World News is
suppressed for C in the reproduction above, while the trigger-level World News emitted earlier by
`setTriggerFired` leaks B's identity.

ANCESTRY
--------
BtS/Civ4CE and K-Mod already contain the flawed trigger-announcement condition: the observer must
know the event owner, but the picked-other-player test is against the event owner rather than the
observer.

An early AdvCiv refactor (practical 1648) hoisted the picked-player test out of the loop as
`bMetOtherPlayer`; practical 2416 corrected an unrelated `||`/`&&` mistake in the refactored
condition but retained the same wrong subject.

Practical 2422 later fixed the parallel event-choice World News path specifically so two-civ event
reports are shown only when BOTH civilizations are known to each observer. It did not apply the
same observer-specific correction to `setTriggerFired`'s trigger World News. Base AdvCiv 1.14 and
current SAS retain this unfixed sibling.

DUPLICATE LEDGER
----------------
The Queue-004 audit had previously parked this exact Global + PickPlayer visibility behavior as an
ambiguous World-News policy question. No prior numbered root owns it. The newly traced practical
2422 history resolves that ambiguity by explicitly stating and implementing the observer-specific
"both known" contract in the sibling event-news path.

F454 is separate: it leaks a hidden target CAPITAL LOCATION through espionage third-party message
coordinates. F457 leaks the IDENTITY/adjective of an unmet picked civilization through random-event
trigger World News.

REPAIR DIRECTION
----------------
Do not hoist the picked-player contact check globally. Resolve it per observer, mirroring the
already-correct `applyEvent` path. Conceptually:

    observer knows event owner
    && (no picked player || observer knows picked player's team)

The existing `applyEvent` observer loop is the direct local repair model.

CURRENT STONE AFTER F457
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN · 32/169 unique files complete ·
F445/KI#768 RETRACTED · F446-F457 CONFIRMED · F458/KI#781 UNUSED ·
C031-WIP170 latest physical checkpoint · no repository source files modified.

NEXT
----
Continue Queue 004 from F458 unused. Resume the historical bug-fix seam / ranked compiled player
state-arithmetic inventory after closing Random Personalities and special-commerce-cache siblings.
Favor shipped current producers and complete callee-chain proof; preserve dormant/API-only defects
as controls rather than numbering them.

===============================================================================
C031-WIP171 -- QUEUE 004 DEEP RE-AUDIT: PERMANENT-ALLIANCE UNIT-SUPPLY CACHE
Date: 2026-09-01
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION / BRIDGE FROM THE FULL-THREAD HANDOFF
-----------------------------------------------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F457 remain confirmed.
- New confirmed root in this checkpoint: F458 / KI#781.
- Next root number: F459 / KI#782 remains UNUSED.
- No repository source files were modified.

Before opening F458, the post-WIP170 paired-counter/state-transition pass closed several
families rather than manufacturing findings:

* the malformed starting-area `VectorPairSecondGreaterComparator` (`a.second >= b.second`)
  remains a real strict-weak-ordering / portability defect, but ordinary equal area-score ties
  do not establish a deterministic Civ4-era gameplay failure; preserve as cleanup debt, not a
  numbered root;
* unit-cost multiplier, strike and anarchy modifier/rescaling paths remain internally coherent;
* player population/land score transitions remove the old Assets/Power/Score contribution,
  mutate the stored count, then add the recomputed contribution;
* religion/corporation city-count changes dirty their dependent building-commerce / assignment
  state;
* `NoForeignTrade` changes deliberately defer the global trade-route rebuild during anarchy,
  and `changeAnarchyTurns` performs that rebuild when the zero/nonzero anarchy boundary changes;
* `changeHurryCount` initially looked suspicious because its derived pop-rush/gold-rush counters
  change by raw `iChange` on a zero/nonzero transition, but its sole runtime producer is
  `processCivics`, whose only callers pass exactly -1 or +1. That branch therefore closes under
  the current caller contract.

F458 / KI#781 -- Permanent Alliance team absorption leaves `m_iNumOutsideUnits` stale, so former allied-border units keep charging unit supply after becoming same-team units
----------------------------------------------------------------------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Current shipped Permanent Alliances can change stationary units from "outside borders"
to "inside borders" without updating the owning player's cached `m_iNumOutsideUnits`. The stale
count feeds authoritative unit-supply expenses, is serialized directly, and has no later full
recount that repairs it.

ROOT / CACHE CONTRACT
---------------------
`CvPlayer` stores the number of units outside friendly/vassal territory in the mutable cache:

    int m_iNumOutsideUnits;

The ordinary unit-supply calculation consumes that cache directly:

    iPaidUnits = std::max(0, getNumOutsideUnits()
            + iExtraOutsideUnits
            - iFreeOutsideUnits);

    iBaseSupplyCost = iPaidUnits * INITIAL_OUTSIDE_UNIT_GOLD_PERCENT;
    iBaseSupplyCost /= 100;

The count is not derived from all units when queried. Current-tree maintenance is entirely
incremental. `CvUnit::setXY` removes/adds one outside unit when leaving/entering a plot whose team
satisfies:

    plotTeam != unitTeam &&
    (plotTeam == NO_TEAM || !GET_TEAM(plotTeam).isVassal(unitTeam))

`CvPlot::setOwner` performs the analogous remove/re-add around an ownership/team change.

This establishes the invariant: any other state transition that changes that relational predicate
for stationary units must also adjust or recount `m_iNumOutsideUnits`.

AUTHORITATIVE SIBLING CONTROL: VASSALAGE ALREADY DOES THE REQUIRED RELATIONAL UPDATE
------------------------------------------------------------------------------------
`CvTeam::setVassal` explicitly handles this exact dependency. Before changing master/vassal state,
it iterates the master's units and decrements `changeNumOutsideUnits(-1)` for units whose current
plots qualify under the old relation. After the relationship changes, it iterates again and adds
them back under the new relation.

That code is present already in stock BtS/Civ4CE, K-Mod, Base AdvCiv 1.14 and current SAS. It proves
that "outside unit" is recognized as relationship-dependent cached player state rather than a
value that can safely be left to future movement.

PERMANENT ALLIANCE VIOLATES THE SAME CONTRACT
---------------------------------------------
`CvDeal` forms a Permanent Alliance by calling `CvTeam::addTeam`; the lower-numbered team survives.
The merge reassigns every absorbed player through:

    GET_PLAYER(...).setTeam(getID());

`CvPlayer::setTeam` updates team membership/alive/city/population/land counters and attitude state,
but does not touch `m_iNumOutsideUnits`.

Later in `CvTeam::addTeam`, every map plot runs:

    kPlot.updateTeam();

Current AdvCiv's `CvPlot::updateTeam` only refreshes the cached team from the plot owner:

    m_eTeam = (isOwned() ? TEAMID(getOwner()) : NO_TEAM);

It does not execute the remove/re-add outside-unit bookkeeping used by `CvPlot::setOwner`.
`CvTeam::addTeam` then updates plot groups, but no outside-unit recount occurs there either.

A complete current DLL reference search finds `changeNumOutsideUnits` only in:

* `CvUnit::setXY` -- ordinary unit movement / placement;
* `CvPlot::setOwner` -- ordinary plot owner change;
* `CvTeam::setVassal` -- explicit relationship transition repair;
* `CvPlayer::changeNumOutsideUnits` itself.

There is no full player/team `updateNumOutsideUnits` helper and no Permanent-Alliance correction.

DETERMINISTIC CURRENT REPRODUCTION
----------------------------------
The state is fully supported by current SAS data:

* `GAMEOPTION_PERMANENT_ALLIANCES` is a shipped game option;
* current `TECH_GAME_THEORY` has `bPermanentAllianceTrading=1`;
* standard BtS `INITIAL_FREE_OUTSIDE_UNITS` is 4, and current Normal speed adds
  `iExtraFreeOutsideUnits=0`;
* `INITIAL_OUTSIDE_UNIT_GOLD_PERCENT` is 50.

Example on Normal speed with no other outside units:

1. Player A / Team A and Player B / Team B are independent friendly civilizations able to form a
   Permanent Alliance.
2. A stations six ordinary units in B-owned territory (Open Borders is enough to allow the units
   to stand there; Open Borders does not make the territory "inside" for unit-supply counting).
3. Before the alliance, all six correctly satisfy the outside-unit predicate, so
   `A.m_iNumOutsideUnits == 6`.
4. A and B form a Permanent Alliance and Team A survives.
5. B's player is reassigned to Team A and B-owned plots are refreshed to Team A. Every one of A's
   six stationary units is now on SAME-TEAM territory and should no longer count as outside.
6. No outside-unit update runs. A's cache remains 6.
7. Runtime supply therefore still computes:

       paid units = max(0, 6 - 4) = 2
       base supply = 2 * 50 / 100 = 1 gold/turn

   even though the six units are now standing in the merged team's own territory.

The symmetric absorbed-player case is also reachable: B units standing in A territory cease being
outside as soon as B is reassigned to Team A, but B's player cache likewise receives no correction.

PERSISTENCE / WHY MOVEMENT DOES NOT SELF-HEAL
---------------------------------------------
This stale state is not tied to a pending per-unit flag that gets cleared later. Once the alliance
exists, an affected unit's current plot is same-team territory. Moving that unit between merged-team
plots performs neither the old-plot decrement nor the new-plot increment because the CURRENT
outside predicate is false on both sides.

Moving it out to genuinely foreign/unowned territory adds one and moving it back subtracts one,
returning to the same stale baseline. Killing/removing it while on merged-team territory likewise
does not subtract the old stale contribution.

`m_iNumOutsideUnits` is read and written directly by `CvPlayer::read` / `CvPlayer::write`; load does
not rebuild it from units. A save made after the alliance therefore preserves the incorrect supply
burden across sessions.

GAMEPLAY CONSEQUENCE
--------------------
`CvPlayer::calculateUnitSupply` is the authoritative runtime supply expense and is included by
`calculatePreInflatedCosts`. Thus F458 is not merely a Finance-Advisor display mismatch: the player
actually pays excess supply from the stale count (subject to normal handicap/inflation handling).
AI economic state consumes the same player cost pipeline.

Even below the first whole-gold threshold, `getNumOutsideUnits` and Finance breakdown data remain
wrong; with six stale units on Normal the shipped values already produce a deterministic +1 base
gold/turn error.

ANCESTRY
--------
This is inherited core BtS behavior, not an AdvCiv-SAS regression.

Stock BtS/Civ4CE already has:

* incremental `changeNumOutsideUnits` maintenance in unit movement and plot owner change;
* the explicit two-pass outside-unit adjustment in `CvTeam::setVassal`;
* `CvTeam::addTeam` player reassignment for Permanent Alliances without an analogous outside-unit
  adjustment/recount.

K-Mod retains that omission. Base AdvCiv 1.14 retains it as well. AdvCiv later cached plot team in
`CvPlot::m_eTeam` and explicitly refreshes it during `addTeam`, but the refresh remains a bare cached
team assignment and still bypasses the outside-unit accounting.

F455 / KI#778 is a later AdvCiv-specific sibling caused by military-happiness eligibility becoming
team/vassal relational. F458 is older and independent: BtS itself already knew unit supply had to be
adjusted for vassal relationship changes, but Permanent Alliance absorption never received the same
cache maintenance.

DUPLICATE LEDGER
----------------
Full album searches for `outside unit`, `NumOutsideUnits`, `changeNumOutsideUnits`, `unit supply`,
Permanent Alliance + supply/outside, and related team-reassignment wording found no prior numbered
root for this cache corruption.

F412 / KI#735 is separate: the Finance Advisor's unit-cost/unit-supply HOVERS ignore anarchy and can
show positive itemized costs while runtime costs are zero. It does not alter `m_iNumOutsideUnits` or
runtime supply.

F455 / KI#778 is separate: Permanent Alliance reassignment leaves the CITY military-happiness cache
stale and can drive that city count negative on later movement. F458 concerns the PLAYER outside-unit
cache and authoritative supply expense.

REPAIR DIRECTION
----------------
Use a full recomputation after Permanent Alliance member reassignment AND after the map loop has
refreshed absorbed territory's plot-team cache.

A robust local design is a player helper that counts all on-map units using the same predicate as
`CvUnit::setXY`, then applies:

    changeNumOutsideUnits(recomputedCount - getNumOutsideUnits());

Call it for every surviving-team member in `CvTeam::addTeam` after all `kPlot.updateTeam()` calls.
That placement repairs both directions at once:

* survivor players with units in formerly absorbed territory;
* absorbed players with units in survivor territory.

Do not try to repair only inside `CvPlayer::setTeam`: at that point absorbed-player plot teams have
not yet all been refreshed, and survivor players whose own team ID never changes can still have
stationary units whose outside status changes when absorbed territory switches team.

IMMEDIATE RELATIONSHIP-CACHE SIBLING SWEEP
------------------------------------------
The neighboring `CvTeam::setVassal` transition list was rechecked after F458. The major relation-
dependent families it explicitly updates are city sight, master-unit outside status, war/war-plan
counters, military-happiness garrisons, culture and plot groups. Permanent-Alliance `addTeam`
already has dedicated visibility/contact/war/culture/plot-group/team-state migration for the latter
families, while F455 and F458 identify the two cached per-city/per-player relational counters that
were missing from that merge path. No third comparably direct current cache omission has yet been
established from this local list.

CURRENT STONE AFTER F458
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN · 32/169 unique files complete ·
F445/KI#768 RETRACTED · F446-F458 CONFIRMED · F459/KI#782 UNUSED ·
C031-WIP171 latest physical checkpoint · no repository source files modified.

NEXT
----
Continue Queue 004 from F459 unused. Finish the remaining paired availability/multi-effect setter
caller boundaries, then resume the ranked low-coverage compiled CvPlayer arithmetic/state helpers
and historical bug-fix seams. Keep the same threshold: current shipped reachability + full callee
chain + ancestry + duplicate proof before assigning a number.


===============================================================================
C031-WIP172 — QUEUE 004 CVPLAYER CONTINUATION — F459 STILL UNUSED / MULTI-EFFECT SETTER SATURATION
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP171 with Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN,
F458/KI#781 confirmed (Permanent Alliance stale outside-unit / unit-supply cache),
and F459/provisional KI#782 unused. No repository source files were modified.

POST-WIP171 CLOSURE / CONTROL PASS
----------------------------------
The next tranche finished several candidates opened by the paired-counter and
relationship-dependent-cache inventory. None currently clears the numbered-root threshold.

CONTROL — TEAM COMMERCE-FLEXIBILITY REMOVAL HAS AN INVARIANT GAP, BUT SHIPPED TECH-REMOVAL PATHS MASK IT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::changeCommerceFlexibleCount` resets a commerce slider to its XML initial percent
when the player's last flexibility source disappears. The corresponding team-level
`CvTeam::changeCommerceFlexibleCount` only mutates the team counter and dirties the UI; it
does not reset a now-illegal player slider.

In isolation, therefore, removing the last team source while Culture/Espionage is allocated
above its initial percentage can leave percentage assigned to a commerce type that
`CvPlayer::isCommerceFlexible` now reports as inflexible.

Current SAS does not establish a shipped normal-game producer for that bad transition:
team flexibility is tech-driven, ordinary gameplay does not remove known technologies,
the Advanced Start refund receiver exists but the shipped tech chooser exposes only tech
purchase (the same reachability control that rejected F084/F085/F088), and the main commerce
sliders are hidden while the interface is in Advanced Start. True Starts tech stripping is a
setup/regeneration operation before ordinary slider use. Preserve this as an API/robustness
asymmetry, not F459.

CONTROL — PROPAGATED EVENT EXPIRY MIXES RECIPIENT AND ORIGINAL TRIGGER OWNER, BUT CURRENT DATA DOES NOT REACH THE MISMATCH
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`setEventOccured` propagates a Global occurrence by copying the same `EventTriggeredData`,
so recipient copies retain the original trigger owner's `m_ePlayer`. Some quest expiry tests
in `checkExpireEvent` combine state from `this` (the player storing the occurrence) with
`kTriggeredData.m_ePlayer` (the original trigger owner). That is structurally fragile for a
propagated Global/Team quest.

Current SAS event data contains no Quest event that is also Global or Team, so no shipped
occurrence reaches a state where the recipient/original-owner distinction changes the quest
expiry result. The already preserved practical-2416 Team-event propagation inversion is
likewise real source debt but remains dormant because current Event/EventTrigger data has no
Team producer. No F459.

CONTROL — SPY DESTROY/BRIBE UNIT TARGET VALIDATION IS DORMANT IN CURRENT MISSION DATA
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`canSpyDestroyUnit(eTarget, kUnit)` does not itself establish that `kUnit.getOwner()` equals
`eTarget`; the generic plot fallback in espionage mission costing can iterate units belonging
to third parties. The analogous bribe path has the same family shape.

Current SAS `CIV4EspionageMissionInfo.xml` has every `iDestroyUnitCostFactor` and
`iBuyUnitCostFactor` set to 0, leaving these mission branches without a shipped data producer.
Keep as data-masked robustness debt, not F459.

CONTROL — WAR-WEARINESS MODIFIER MUTATOR DOES NOT NEED A PLAYER CACHE RECOUNT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::changeWarWearinessModifier` initially looked like a missing recomputation because
it does not refresh `m_iWarWearinessPercentAnger`. Full consumer tracing closes that lead:
`CvCity::getWarWearinessPercentAnger` applies the current player + city war-weariness modifier
dynamically when converting the cached base anger percent into city anger. No stale-cache
root survives.

CONTROL — RESEARCH QUEUE FLAG MAINTENANCE CLOSES UNDER THE NORMAL ACQUISITION CALLER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
`CvPlayer::popResearch(eTech)` clears `m_abResearchingTech[eTech]` even if its queue scan did
not find a node. The normal DLL acquisition path in `CvTeam::setHasTech` first checks
`kMember.isResearchingTech(eTech)` and calls `popResearch` only when that flag is true; queue
construction sets the flag exactly when inserting the node, while `clearResearchQueue`
resets all flags. No ordinary caller was found that desynchronizes node and flag state.
The Python wrapper can invoke the API more loosely, which is not enough for a current root.

CONTROL — `updateExtraYieldThresholds` CONTAINS A REAL ADVC.908a COMPARATOR TYPO, CURRENT TRAIT DATA MASKS IT
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The ordinary threshold branch correctly keeps the smallest positive threshold:

    if (iBestThresh == 0 || iThresh < iBestThresh)
        iBestThresh = iThresh;

The AdvCiv 908a natural-threshold sibling instead says:

    if (iBestNaturalThresh == 0 || iBestNaturalThresh < iBestThresh)
        iBestNaturalThresh = iNaturalThresh;

The second clause plainly compares the current NATURAL best against the unrelated ordinary
best rather than comparing `iNaturalThresh` with `iBestNaturalThresh`. With two active traits
providing different natural thresholds, iteration order can therefore select the wrong value.
Base AdvCiv 1.14 and current SAS contain the same typo; K-Mod predates the natural-threshold
feature and therefore has only the original ordinary-threshold logic.

Current SAS XML masks the defect: only `TRAIT_FINANCIAL` has any positive
`ExtraYieldNaturalThreshold` (Commerce = 3), and there are no positive ordinary
`ExtraYieldThreshold` entries. With only one possible positive natural source, the first
`iBestNaturalThresh == 0` clause always installs the correct 3 and no competing source exists.
Preserve this as a clean latent AdvCiv/data-extension bug and worthwhile narrow cleanup, but
do not consume F459 under the album's current-data threshold.

RELATIONSHIP-CACHE SIBLING RECONCILIATION AFTER F458
----------------------------------------------------
A direct comparison of `CvTeam::setVassal` with the Permanent-Alliance `addTeam`/`setTeam`
path confirms why F455 (military happiness) and F458 (outside-unit supply) are special:
`setVassal` explicitly re-evaluates both relationship-dependent unit predicates across the
transition. The broader connectivity/economic updates are not omitted in the same way:
`addTeam` performs its plot-team refresh and then `CvGame::updatePlotGroups`, whose player
path also verifies production and updates trade routes. No third equally clean player cache
sibling has emerged from that relation transition yet.

CURRENT STONE AFTER C031-WIP172
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F458 / KI#781 CONFIRMED.
F459 / provisional KI#782 UNUSED.
Latest physical checkpoint: C031-WIP172.
No repository source files modified.

NEXT
----
Continue Queue 004 from F459 unused. Keep the mechanical low-coverage pass on normal-game
player state: civic verification/forced-civic transitions, remaining commerce/yield mutators,
research/era bookkeeping not already owned by F453 or rejected Advanced Start roots, then
return to larger lifecycle helpers. Preserve the current-data + shipped-caller + ancestry +
duplicate threshold; latent source typos such as the natural-threshold comparator remain
valuable notes without forcing a number.

END C031-WIP172

================================================================================
C031-WIP173 CONTINUATION CHECKPOINT — Queue 004 CvPlayer.cpp — F459 UNUSED
================================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP172 with F459 / provisional KI#782 unused.  This
tranche deliberately emphasizes closure proof and invariant tracing; no numbered
root is promoted merely because code is unusual or because an API could be made
more defensive.

1. getBestRoute(NULL): Railroad resource prerequisite mismatch — CLOSED AS
   INTENTIONAL ABSTRACTION, NOT F459
------------------------------------------------------------
Current CvPlayer::getBestRoute distinguishes two meanings explicitly:

* with pPlot != NULL, the candidate Build must satisfy canBuild(*pPlot, eBuild),
  which re-enters the normal build/route legality path including resource
  prerequisites;
* with pPlot == NULL, the candidate only needs its Build tech prerequisite.

Railroad therefore can be the no-plot "best route" once Railroad's technology is
known even when the player does not currently have Coal/Oil.  This first looked
like a possible resource-legality leak because RouteInfo itself has those
resource prerequisites.

Full caller/ancestry reconciliation closes it.  Stock BtS/Civ4CE, K-Mod, base
AdvCiv and current SAS all retain the same deliberate no-plot behavior for
CvPlot::updateCityRoute and related abstract route valuation.  Worker route
selection uses the plot-aware form and therefore performs the missing resource
check.  AI technology valuation also has separate missing-resource handling.
Movement through a city tile does not turn an ordinary adjoining Road into
Railroad movement because route movement is constrained by the endpoint route
pair, and city tiles have no ordinary improvement whose RouteYieldChanges would
supply a broader resource bypass.  Treat no-plot getBestRoute as "best
technologically unlocked / automatic city route", not "a route a Worker can
construct on an arbitrary plot".

2. CvPlayer::canContact GET_TEAM(ePlayer) — FALSE POSITIVE RECLOSED
-------------------------------------------------------------------
Two expressions in canContact superficially look as if a PlayerTypes value were
being used as a TeamTypes value:

    GET_TEAM(ePlayer).isAtWar(getTeam())
    GET_TEAM(ePlayer).isAlwaysWar()

This is the same enum-overload trap already proven in earlier album controls.
In the active AdvCiv/SAS helper layer GET_TEAM is routed to overloaded getTeam
functions; a PlayerTypes argument resolves to the PlayerTypes overload and that
overload maps through TEAMID(ePlayer).  The expressions are therefore target
player -> target team, as intended.  Do not resurrect the previously retracted
"player index used as team index" family from this syntax alone.

3. Group-cycle stale-ID dereference lead — CLOSED UNDER LIFECYCLE INVARIANT
---------------------------------------------------------------------------
CvPlayer::refreshGroupCycleList dereferences getSelectionGroup(pNode->m_data)
without a NULL check, and updateGroupCycle similarly assumes every stored ID
still names a live selection group.  The lifecycle chain maintains that
invariant:

* CvSelectionGroup::kill calls removeGroupCycle(getID()) before
  deleteSelectionGroup(getID());
* player teardown clears m_groupCycle before destroying/uninitializing the
  selection-group container;
* save loading reads the serialized group-cycle IDs and then the serialized
  selection-group array without invoking refreshGroupCycleList in between.

No live stale-ID producer was found.  A defensive NULL check could harden
corrupt/noncanonical state, but the current normal lifecycle does not establish
F459.

4. calculateTotalCommerce excludes culture/espionage — INTENTIONAL advc.004s
----------------------------------------------------------------------------
The name is broader than the current semantics: the function returns gold
commerce + gold-per-turn + research commerce, rather than summing all commerce
types.  This is not an accidental omission.  Base AdvCiv's advc.004s rewrite
explicitly replaced K-Mod's all-commerce calculation for Economy history, and
UWAI contains a dedicated comment acknowledging that culture/espionage are
excluded and that Big Espionage economies may therefore be underestimated.
This is known design, not a newly discovered regression.

5. Building-granted CivicOption loss — DEFERRED REVALIDATION, NOTE ONLY
------------------------------------------------------------------------
Current SAS has live building sources for CivicOption (including customized
Colossus -> Government, Mausoleum of Maussollos -> Economy, and Shwedagon Paya
-> Legal).  CvPlayer::processBuilding updates m_aiHasCivicOptionCount, while
changeHasCivicOptionCount itself does not immediately call verifyCivics when the
last source disappears.  Thus losing such a building can transiently leave the
player's current civic in a state that canDoCivics would no longer allow.

The higher-level turn lifecycle bounds this condition: CvPlayer::doTurn calls
verifyCivics every normal turn after city processing/anarchy updates, and
AdvCiv intentionally removed the older "skip verification during anarchy"
guard.  The state therefore does not persist indefinitely; at worst the source
supports a deferred turn-boundary cleanup.  Without stronger evidence that the
intended contract requires immediate invalidation (and given the mutation-heavy
city/building transition where deferral can be deliberate), retain this as a
timing/design robustness note rather than F459.

6. Reconfirmed existing controls encountered during this pass
--------------------------------------------------------------
* changeHurryCount's derived pop/gold-rush cache uses raw iChange across a
  zero/nonzero transition; all live processCivics callers change one civic
  source by +/-1, so the latent |iChange|>1 weakness remains unreachable in
  shipped gameplay.
* checkAlert uses > rather than >= against m_paAlerts.size(), but shipped alert
  IDs and internal loops remain in bounds; equality is API robustness only.
* Team-event propagation in setEventOccured retains the known inverted-member
  condition, but current event XML has no live Team event, so the already-known
  dormant issue is not promoted here.
* checkExpireEvent's boolean test uses bitwise '&' between bool-returning,
  side-effect-free predicates; both sides evaluating does not change gameplay.
* verifyStateReligion remains deliberately disabled in the inherited kekm.10
  path; state religion is represented dynamically through isStateReligion() and
  getLastStateReligion(), and no new current failure was established here.

Current stone at C031-WIP173
----------------------------
Queue 004 CvGameCoreDLL/CvPlayer.cpp OPEN
32/169 unique files complete
F445/KI#768 RETRACTED
F446-KI#769 through F458/KI#781 CONFIRMED
F459 / provisional KI#782 UNUSED
C031-WIP173 latest physical checkpoint
No repository source files modified.

Exact next cursor
-----------------
F459 unused -> continue genuinely low-coverage normal-turn CvPlayer functions,
with emphasis on live bookkeeping/state transitions and complex helper callers
rather than UI-only wrappers -> preserve the same current-data + ancestry +
callee-chain + duplicate threshold before promotion.

================================================================================
C031-WIP174 CONTINUATION CHECKPOINT — Queue 004 CvPlayer.cpp — F459 UNUSED
================================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP173 with F459 / provisional KI#782 unused.  This pass
resumed the genuinely low-coverage normal-turn/lifecycle functions and explicitly
checked the reconciled album before allowing suspicious-looking code to become a new
candidate. No repository source files were modified.

1. Golden-Age UnitClass readiness vs UnitType consumption — PRIOR CONTROL RECONFIRMED,
   NOT A NEW ROOT
--------------------------------------------------------------------------------------
The low-coverage scan naturally rediscovered the mismatch between
`unitsGoldenAgeReady()` (distinct `UnitClassTypes`) and `killGoldenAgeUnits()`
(distinct `UnitTypes`). Full-album duplicate checking showed that C031-WIP158 already
classified this exact seam. K-Mod intentionally changed readiness from unit type to
unit class, but current SAS has exactly one Golden-Age-capable unit type for each Great
Person class and no CivilizationInfos alternate same-class Great Person mapping. Thus
current shipped data cannot make readiness and consumption disagree. Preserve the
existing latent extension/robustness note; do not duplicate it as F459.

2. setCurrentEra -> updateGraphicEra(..., eOldEra) — CLOSED AS EXE/INTERFACE CONTRACT
-------------------------------------------------------------------------------------
`CvPlayer::setCurrentEra(eNewValue)` stores the old era, assigns the new era, then
updates every unit entity with:

    gDLL->getEntityIFace()->updateGraphicEra(pLoopUnit->getEntity(), eOldEra);

Passing the *old* era initially looks backwards. It is not a local SAS/AdvCiv mistake:

* `CvDLLEntityIFaceBase::updateGraphicEra` explicitly names its optional second
  parameter `eOldEra`;
* stock BtS in Civ4CE passes `eOldEra` from the same player-era transition;
* K-Mod retains that exact argument;
* Base AdvCiv 1.14 and current SAS retain it as well.

The DLL is therefore supplying the era being left to an EXE-side graphics transition
whose current/new era is already available through player state. Changing this to
`eNewValue` would contradict the interface contract and stock ancestry. Not F459.

3. canHaveTradeRoutesWith anarchy asymmetry — CLOSED BY CALLER / CITY-DISORDER CHAIN
------------------------------------------------------------------------------------
Current helper code rejects a *different* partner when that partner is in anarchy:

    if (getID() != ePlayer && kOtherPlayer.isAnarchy())
        return false;

but does not explicitly reject `this->isAnarchy()`. Read alone, that can look like an
asymmetric foreign-trade rule. The authoritative city caller closes the apparent gap.
`CvCity::updateTradeRoutes()` begins by clearing existing routes and only builds new
ones when the owner city is not in disorder. Anarchy puts the owner's cities in
disorder, so an anarchic player's own cities do not generate routes at all. From the
other direction, non-anarchic players call `canHaveTradeRoutesWith(anarchicPlayer)`
and are rejected by the partner-anarchy check. The resulting gameplay rule is
symmetric even though the helper only needs to test its argument.

The ancestry supports this purpose: advc.124 introduced the partner-anarchy check in
the trade-route eligibility path while also integrating city-disorder handling. The
related `changeNoForeignTradeCount` deferral until anarchy ends is consistent with
trade routes already being suppressed through disorder during the anarchic interval.
Not F459.

4. isValidTriggerReligion / isValidTriggerCorporation sibling comparison — CLEAN
--------------------------------------------------------------------------------
The event-trigger religion helper was checked because the neighboring corporation
helper carries an explicit K-Mod bugfix. The religion path already has the correct
positive-existence contract in both city and player-wide forms:

* with a selected city, that city must actually have the religion and, when requested,
  must be its Holy City;
* without a selected city, `getHasReligionCount(eReligion)` must be nonzero and the
  owned-Holy-City condition is then checked separately.

There is no analogue of the old corporation helper's inverted/incorrect terminal
logic. Current trigger data adds no contradictory special case. Close this sibling.

5. getProductionTraitModifier unit/building split — CLEAN
---------------------------------------------------------
The two overloads deliberately aggregate both object-specific trait production and,
where applicable, the associated SpecialUnit/SpecialBuilding trait production bonus.
This mirrors the data model rather than double-counting the same field: the direct
UnitInfo/BuildingInfo production-trait array and the special-category production-trait
array are distinct XML sources intended to stack. No current source maps one field
through the other, and ancestry preserves the same additive semantics. No root.

Current stone at C031-WIP174
----------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN
32/169 unique files complete
F445/KI#768 RETRACTED
F446/KI#769 through F458/KI#781 CONFIRMED
F459 / provisional KI#782 UNUSED
C031-WIP174 latest physical checkpoint
No repository source files modified.

Exact next cursor
-----------------
F459 unused -> continue low-coverage authoritative normal-turn helpers, prioritizing
state transitions and arithmetic with live consumers over Advanced Start/UI-only
wrappers -> check the full album before deepening any apparent candidate -> preserve
current-data + shipped-caller + ancestry + duplicate proof before promotion.

END C031-WIP174

===============================================================================
C031-WIP175 — QUEUE 004 CONTINUATION — F459 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 through F458 CONFIRMED.
F459 / provisional KI#782 UNUSED.
Latest physical checkpoint: C031-WIP175.
No repository source files modified.

POST-WIP174 RESEARCH / ESPIONAGE CLOSURE TRANCHE
-------------------------------------------------
1. `getResearchTurnsLeftTimes100` teammate-overflow modifier mismatch is a real generic /
   config-dependent defect, but current SAS masks every live modifier difference.

   The function iterates team members and, for each included member, converts that member's
   normalized `getOverflowResearch()` with:

       kMember.getOverflowResearch() * calculateResearchModifier(eTech) / 100

   where the unqualified `calculateResearchModifier` belongs to the SUBJECT player rather
   than `kMember`. This matters conceptually because `doResearch()` stores overflow in
   normalized form and later applies it with EACH PLAYER'S own current research modifier.
   Therefore a faithful team turns-left estimate should likewise apply each member's own
   modifier.

   The prospective-tech path is genuinely callable: the human tech-choice popup calls
   `getResearchTurnsLeft(candidate,true)`, and an already-researching teammate can therefore
   contribute overflow while the subject has not yet selected that candidate. In generic
   AdvCiv configuration the advc.156 extra-team-member modifier can then differ between the
   subject and that teammate; groundbreaking penalties can also be player-specific.

   Current SAS, however, explicitly sets `RESEARCH_MODIFIER_EXTRA_TEAM_MEMBER` to 0, and the
   earlier audit established that both human and AI groundbreaking penalties are disabled.
   All remaining research-modifier components on this path are team/tech-wide. Consequently
   the wrong receiver expression is numerically masked in current shipped SAS gameplay.

   Classification: real latent/config-dependent source defect; no F459.
   Conservative cleanup if desired later: use `kMember.calculateResearchModifier(eTech)`.

2. `espionageNeededToSee` same-team/master-vassal concern closes at its only shipped caller.
   `canSeeIntel` deliberately grants direct visibility to same-team players and to the
   subject's vassals without requiring a passive espionage mission. In isolation,
   `espionageNeededToSee` would then search mission costs even though visibility came from
   that direct relationship. But the only compiled consumer is the scoreboard power-ratio
   hover, and it returns immediately when active player and subject have the same master
   team (`getMasterTeam()` equality), before calling `espionageNeededToSee` at all.
   Ordinary rival use therefore enters the intended passive-espionage case. No gameplay or
   current UI root survives.

3. The already-known `updateCommerceRates()` repeated largest-remainder correction was
   encountered while probing arithmetic helpers and was rejected immediately as a duplicate:
   it is F444 / KI#767, already fully confirmed in C031-WIP151. It is not a new F459 lead.

NEXT
----
Continue F459 unused through genuinely under-covered authoritative normal-turn helpers.
Keep grepping the reconciled album before promotion. Prefer player-state arithmetic,
eligibility, event selection and live turn-control consumers over Advanced Start / debug /
API-only edges. Preserve dormant/config-dependent defects as controls rather than inflating
current SAS findings.

===============================================================================
C031-WIP176 — QUEUE 004 CONTINUATION — F459 CONFIRMED / F460 UNUSED
===============================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP175 with F459 / provisional KI#782 unused. The
main purpose of this tranche was to finish the previously conservative
building-granted CivicOption-loss seam by proving its actual turn timing, current
SAS data reachability, city-loss mutation path, and ancestry. No repository source
files were modified.

F459 / KI#782 — CONFIRMED
-------------------------
Title: Losing the final building-granted CivicOption can leave an unavailable civic
active through the player's next full turn and through that turn's economy/city
processing.

Root / invariant
~~~~~~~~~~~~~~~~
`CvPlayer::canDoCivics` permits a civic when either the player currently has the
civic's CivicOption source OR the player's team has the civic's prerequisite tech:

    if (!isHasCivicOption(GC.getInfo(eCivic).getCivicOptionType()) &&
        !GET_TEAM(getTeam()).isHasTech(GC.getInfo(eCivic).getTechPrereq()))
        return false;

A building with `<CivicOption>` therefore intentionally lets its owner adopt civics
in that category before researching their individual prerequisite technologies.
`CvPlayer::processBuilding` changes `m_aiHasCivicOptionCount` when such a building is
added or removed, but neither `processBuilding` nor `changeHasCivicOptionCount`
revalidates the currently selected civic when the LAST source disappears.

The current civic is not computed dynamically through `canDoCivics`. `setCivics`
applies/removes its gameplay effects through `processCivics`, which changes stored
player modifiers/counters such as capital yield modifiers, commerce modifiers,
trade routes, free specialists, happiness, war weariness, improvement yields, etc.
Those processed effects therefore remain live until `setCivics` is actually called.

Current SAS reachability
~~~~~~~~~~~~~~~~~~~~~~~~
This is not an API-only possibility. Current SAS deliberately has three live World
Wonder sources:

* `BUILDING_COLOSSUS` at Metal Casting -> `CIVICOPTION_GOVERNMENT`;
* `BUILDING_MAUSOLEUM_OF_MAUSSOLLOS` at Currency -> `CIVICOPTION_ECONOMY`;
* `BUILDING_SHWEDAGON_PAYA` at Meditation -> `CIVICOPTION_LEGAL`.

The latter two never obsolete. All three have `bNeverCapture=0` and
`iConquestProb=100` in current SAS data, so ordinary city loss is a supported source
removal path.

A particularly compact deterministic repro is Shwedagon Paya + Bureaucracy:

1. Player A owns Shwedagon Paya and has not researched Civil Service.
2. Shwedagon's Legal CivicOption makes `canDoCivics(CIVIC_BUREAUCRACY)` true despite
   the missing tech, so A can legitimately adopt Bureaucracy.
3. Current SAS Bureaucracy requires `TECH_CIVIL_SERVICE` and carries +50% capital
   production and +50% capital commerce yield through `CapitalYieldModifiers`.
4. During another player's turn, that player captures A's Shwedagon city.
5. `CvPlayer::acquireCity` calls `pOldCity->kill`; `CvCity::kill` sets every real
   building to zero; the normal `CvCity::processBuilding(...,-1)` ->
   `CvPlayer::processBuilding(...,-1)` chain therefore removes A's final Legal
   CivicOption source immediately.
6. At this point `A.canDoCivics(CIVIC_BUREAUCRACY)` is false, but A's selected civic
   remains Bureaucracy and its `processCivics` modifiers remain applied.

Full-turn consequence
~~~~~~~~~~~~~~~~~~~~~
The old WIP173 classification treated this as a bounded deferred cleanup. The exact
turn lifecycle shows that the bound is nevertheless gameplay-significant.

For normal sequential human turns, `CvPlayer::setTurnActive(true)` does NOT call
`CvPlayer::doTurn`; it prepares the turn, runs team/start/unit work, and gives the
human control. There is no `verifyCivics` call in `acquireCity`, the building-removal
chain, or the normal human turn-activation path.

When that human finally ends the turn, `setTurnActive(false)` calls
`CvPlayer::doTurn()`. And `doTurn` performs, in this order:

    verifyGoldCommercePercent();
    doGold();
    doResearch();
    doEspionagePoints();
    every city -> doTurn();
    ...
    verifyCivics();

Therefore the player gets the ENTIRE following active turn while an objectively
unavailable civic remains selected, and even the end-of-turn economy and every city
turn are processed with that civic still active. Only afterward does
`verifyCivics()` switch the category to a legal civic and remove the stale
`processCivics` effects.

The same omission can also occur through a voluntary city trade during the player's
own active turn; in that case the illegal civic survives through the remainder of
that turn and its end-of-turn processing.

Ancestry / scope
~~~~~~~~~~~~~~~~
This is inherited rather than a SAS-only C++ regression:

* stock BtS/Civ4CE changes the building CivicOption count without immediate civic
  verification and calls `verifyCivics()` only after gold/research/city processing;
* K-Mod retains the same lifecycle;
* AdvCiv 1.14 retains it (advc.004x adds the useful civic-change popup on gaining a
  source, but not loss-side revalidation);
* current SAS retains the code path and expands the practical exposure through its
  customized CivicOption wonders.

Stock BtS/AdvCiv also have building-granted CivicOptions (notably Pyramids ->
Government and Shwedagon Paya -> Religion), so the underlying root can affect base
lineage as well. SAS's current Colossus/Mausoleum/Shwedagon assignments make the
condition especially broad and easy to reach.

Duplicate check
~~~~~~~~~~~~~~~
The full reconciled album contains the exact seam only as the C031-WIP173
"Building-granted CivicOption loss — DEFERRED REVALIDATION, NOTE ONLY" control.
That note explicitly left promotion open pending stronger timing/contract evidence.
No earlier numbered root covers the missing loss-side revalidation. F459 therefore
upgrades that prior control rather than duplicating another finding.

Fix direction (not implemented)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The semantic fix is to ensure that crossing the final CivicOption source from
positive to zero revalidates a currently selected civic that no longer passes
`canDoCivics`. The most direct location is the loss-side zero transition in
`changeHasCivicOptionCount` / `processBuilding`. Because building removal may occur
inside `CvCity::kill`/city transfer, implementation should still check reentrancy of
an immediate `verifyCivics()`/`setCivics` call; if a deferred flag is safer, it must
be consumed before the player can act and before gold/research/city processing, not
at the current late end-of-turn location.

Additional closures in this tranche
-----------------------------------
1. `GET_TEAM(getID())` occurrences in free-unit initialization are not player/team
   index bugs. The active AdvCiv/SAS helper overload resolves a `PlayerTypes`
   argument through the player -> team mapping; this is the already-known enum
   overload trap and remains closed.

2. `getEspionageSpending` remainder allocation is inherited BtS/K-Mod behavior. It
   distributes integer leftovers to the highest-weight eligible targets after the
   proportional division. No arithmetic invariant failure was established.

3. `canReceiveGoody` / hostile-goody minimum handling retains the prior latent
   zero-minimum robustness note, but current hostile goody XML uses positive minima;
   no current F460 lead there.

Current stone at C031-WIP176
----------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN
32/169 unique files complete
F445 / KI#768 RETRACTED
F446 / KI#769 through F459 / KI#782 CONFIRMED
F460 / provisional KI#783 UNUSED
C031-WIP176 latest physical checkpoint
No repository source files modified.

Exact next cursor
-----------------
F460 unused -> continue the genuinely under-covered normal-turn player-state /
arithmetic / eligibility helpers -> prioritize current shipped callers and mutation
paths -> use the reconciled album as a duplicate filter before deepening suspicious
code -> preserve the same current-data + full caller/callee + ancestry threshold.

END C031-WIP176

================================================================================
C031-WIP177 CONTINUATION CHECKPOINT — Queue 004 CvPlayer.cpp — F460 CONFIRMED
================================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP176 with F460 / provisional KI#783 unused.  This
tranche resumed genuinely under-covered normal-game CvPlayer helpers.  Several
inherited/state helpers closed normally; the city-trade culture helper instead exposed
an AdvCiv-introduced execution-order defect with an explicit manual/source contract and
an ordinary shipped producer.  No repository source files were modified.

F460 / KI#783 — city-trade outer-ring culture conversion asks for the traded city's
working priority only after that city has been deleted, so documented full-radius
conversion is silently skipped on ordinary outer-ring plots
----------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED — AdvCiv-introduced `advc.ctr` gameplay bug retained by AdvCiv 1.14 and
current AdvCiv-SAS.  The underlying full-radius conversion feature does not exist in
K-Mod 1.46 / stock BtS-Civ4CE; their older inner-ring-only transfer does not depend on
the deleted city's working priority and therefore does not contain this exact root.

DOCUMENTED ADVC.CTR CONTRACT
----------------------------
The AdvCiv manual's `ctr (cont.) Consequences of city trades` section explicitly says
that, when a city is traded:

* old-owner tile culture is decreased in the **full city radius (21 tiles)**;
* tiles contested by cities not belonging to the new owner are excluded **unless the
  traded city has the higher working priority**, based on distance and age;
* the new owner's tile culture increases by the amount removed from the old owner.

This is also exactly what the source comment in
`CvPlayer::cultureConvertedUponCityTrade` says for the outer circle:

    // Outer circle: Based on plot priority when contested
    CvCity const* pDefaultWorkingCity = kPlot.defaultWorkingCity();
    bConvert = (pDefaultWorkingCity != NULL &&
            /* Plot has to be assigned to the traded city or to
               a city of the new owner */
            (pDefaultWorkingCity->at(kCityPlot) ||
             pDefaultWorkingCity->getOwner() == eNewOwner));

`CvPlot::defaultWorkingCity` was itself factored out of `updateWorkingCity` with the
explicit note `advc: Cut from updateWorkingCity (for advc.ctr)`.  It selects the
highest-priority currently live nearby city, using city-plot priority and then
founding-time / city-ID tie-breakers.  Thus both documentation and code structure say
that the traded city is supposed to be a possible answer here.

ACTUAL ACQUIRECITY ORDER MAKES THAT HALF OF THE TEST IMPOSSIBLE
---------------------------------------------------------------
The ordinary current transfer path in `CvPlayer::acquireCity` is ordered as follows:

1. preserve the old city's data;
2. call

       pOldCity->kill(false, false);
       pOldCity = NULL;

3. only then, for `bTrade`, iterate `CityPlotIter(kCityPlot)` and call

       cultureConvertedUponCityTrade(kCityPlot, *it, eOldOwner, getID());

4. only after the culture-conversion loop call `initCity(...)` for the replacement
   city owned by the recipient.

This is not merely a dangling-pointer concern. `CvCity::kill` performs the decisive
lifecycle work before returning:

* `kPlot.setPlotCity(NULL)` removes the city from its plot;
* `kOwner.deleteCity(getID())` deletes it from the owner's live city container;
* after the ownership/culture updates it calls `GC.getMap().updateWorkingCity()`.

Consequently every plot's working-city assignment is recomputed without the traded
city in existence.  When `cultureConvertedUponCityTrade` subsequently calls
`kPlot.defaultWorkingCity()`, the documented candidate "the traded city" cannot be
returned.  The replacement city also cannot be returned because it has not yet been
created.

For an outer-ring plot (`adjacentOrSame == false`), the first intended condition

    pDefaultWorkingCity->at(kCityPlot)

is therefore dead on the actual transfer path.

DETERMINISTIC ORDINARY REPRODUCTION
-----------------------------------
A minimal source-supported example needs no unusual XML or API use:

1. Player A owns city C; player B is eligible to receive C through an ordinary city
   trade.  Current AdvCiv city-trade rules only require sufficient B city-tile culture,
   so this is normal gameplay.
2. Choose an outer-ring (distance-2) plot P that C can work and for which C is the
   default/highest-priority working city before the transfer.  Let no other A city be
   able to work P.
3. Give P, for example, 100 A tile culture and 40 B tile culture.  P remains owned by A
   across the deletion step, so after C is killed it has no live default working city.
4. Under the intended helper calculation, while C still exists, P qualifies because
   `defaultWorkingCity(P) == C`.  Current conversion arithmetic would therefore move

       min(100 / 2, 2 * 40) = 50

   culture points from A to B, leaving A=50 and B=90 on P.
5. In actual `acquireCity`, C is killed first. `GC.getMap().updateWorkingCity()` then
   removes C from P's assignment.  With no other A city in range,
   `P.defaultWorkingCity()` is NULL.
6. The later outer-ring helper returns 0; P remains A=100, B=40 instead of receiving
   the documented transfer.

The same failure also affects contested outer plots where another old-owner city
becomes the default only *after* deletion.  Even if C had the higher priority before
trade — exactly the case the manual says should convert — C is no longer available to
win that comparison.

The inner ring is not the problem: `adjacentOrSame(kPlot,kCityPlot)` makes those plots
convert without consulting a working city.  F460 is specifically the full-radius /
outer-ring extension introduced by AdvCiv.

LIVE CONSEQUENCES
-----------------
The immediate authoritative consequence is wrong persistent plot culture after a city
trade.  The recipient fails to receive culture that the documented AdvCiv rule says
belongs with the ceded city; the former owner retains it instead.  This can change plot
ownership/border shape, subsequent cultural pressure, workable-tile access and later
revolt/culture calculations.

There is also an AI/runtime mismatch in the same feature family.  `CvPlayerAI` uses
`cultureConvertedUponCityTrade(..., bIgnorePriority=true)` while valuing a peacefully
acquired city, which estimates conversion throughout the radius without depending on
the post-kill working-city state.  Some approximation is deliberate there, so that is
not a separate root; however the execution-order defect means the actual transfer can
systematically grant less outer-ring culture than the city-acquisition valuation is
built around.

ANCESTRY
--------
K-Mod 1.46 and stock BtS/Civ4CE perform the old trade consequence *after* killing the
city too, but their rule only loops the inner 3x3 area and sets old-owner culture to
zero.  That operation needs no old-city priority, so the ordering is harmless there.

AdvCiv's `advc.ctr` replaces that old inner-ring loop with `CityPlotIter` plus
`cultureConvertedUponCityTrade`, explicitly expanding the policy to the full city
radius and introducing a dependence on whether the **traded city** is the
`defaultWorkingCity`.  The old post-kill placement was retained.  AdvCiv 1.14 and
current SAS have the same order and helper logic.

This is therefore an AdvCiv feature-integration/order bug rather than a stock-BtS root
or a SAS-local regression.

DUPLICATE CHECK
---------------
The reconciled album contains earlier `advc.ctr` findings in the diplomacy/AI city
trade family (including city-for-war balancing and request-city selection), but no
prior root for transfer-time tile-culture conversion, `cultureConvertedUponCityTrade`,
or the full-radius working-priority lifetime mismatch.  Full-album searches for the
helper, full-radius trade culture and outer-circle conversion were clean.  F460 is a
new root.

REPAIR DIRECTION — NOT IMPLEMENTED
----------------------------------
Preserve the conversion decision while the old city still exists, then apply the
culture changes at the existing safe point after deletion.  The most conservative
shape is:

* before `pOldCity->kill`, iterate the 21 city plots and snapshot each intended
  conversion amount by calling `cultureConvertedUponCityTrade` while
  `defaultWorkingCity()` can still return the traded city;
* after `kill`, apply those saved amounts to old/new player plot culture exactly where
  the current conversion loop lives;
* then initialize the new city as current code already does.

This avoids changing plot ownership/working assignments prematurely while the old city
is still alive, and it preserves the exact documented priority decision at the moment
when all of its required city objects exist.

Alternatively the helper would need enough pre-kill state passed explicitly to answer
"was the traded city the default working city here?" after deletion.  Simply calling
`defaultWorkingCity()` later cannot recover that information.

A regression test should use an outer-ring plot uniquely assigned to the ceded city,
with nonzero recipient culture, and verify the documented conversion amount survives
the `CvCity::kill`/recreate transfer sequence.  A second test should use an outer plot
contested by two old-owner cities and verify conversion occurs iff the ceded city had
higher pre-transfer priority.

OTHER F460-PASS CONTROLS
------------------------
* `CvPlayer::canSpiesEnterBorders` is inherited intended counterespionage-border
  semantics.  Movement denial and the team-wide spy-ejection hook use the same
  orientation; no reversed-player root survives.
* `CvPlayer::doCaptureGold` retains the established base/turn-held/random scaling;
  no current arithmetic/caller mismatch was established in this pass.
* `CvPlayer::getGrowthThreshold` differs from the older K-Mod whole-formula handicap
  shape because AdvCiv introduced `BaseGrowthThresholdPercent`; no source/data contract
  establishes that the player-handicap component should multiply the population term
  too, so no root was manufactured.
* free-promotion event setters and `promoteFreeUnit` remain legal under current
  promotion data/canAcquirePromotion checks.  The latter's enum-order heuristic is
  fragile but no current data violation was proved.
* `CvPlayer::getEventTriggerWeight` retained the prior corrected handicap-difficulty
  logic; current building-multiply triggers fit the implementation.
* `doGold`, `doResearch`, `doEspionagePoints` and espionage-spending weight setters
  retain their normal bookkeeping contracts; previously classified research-overflow
  and commerce-rounding issues remain under their existing controls/roots.

CURRENT STONE AFTER C031-WIP177
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F460 / KI#783 CONFIRMED.
F461 / provisional KI#784 UNUSED.
Latest physical checkpoint: C031-WIP177.
No repository source files modified.

NEXT
----
Continue Queue 004 from F461 unused.  Resume genuinely under-covered ordinary-game
CvPlayer state/lifecycle helpers: event expiration/selection only where prior event
roots do not already own the seam, religion/civic state counters and capital/yield
mutators, city-trade/capture helpers only for distinct siblings, and remaining
normal-turn arithmetic.  Preserve current-data + shipped-caller + ancestry + duplicate
proof before promotion; do not turn nearby advc.ctr curiosities into siblings without
an independent gameplay consequence.

END C031-WIP177


================================================================================
C031-WIP178 — QUEUE 004 CVPLAYER CONTINUATION — F461 / KI#784 CONFIRMED
================================================================================

CONTINUATION ANCHOR
-------------------
Continued Queue 004 `CvGameCoreDLL/CvPlayer.cpp` directly from C031-WIP177 with
F461 / provisional KI#784 unused.  The first pass reconciled capital yield/commerce
mutators against the earlier F445 retraction and then moved into genuinely
under-covered religion / espionage / persistent-visibility player state.  No
repository source files were modified.

CAPITAL-MUTATOR RECONCILIATION — CLEAN / F445 RETRACTION REMAINS AUTHORITATIVE
-----------------------------------------------------------------------------
`changeCapitalYieldRateModifier` explicitly invalidates the exact affected yield-rank
cache and refreshes capital work assignment. `changeCapitalCommerceRateModifier`
refreshes the capital through `CvCity::updateCommerce()`, whose no-argument form also
invalidates the player's yield-rank caches. Thus the cross-file callee proof that
retracted F445 remains correct; no hidden capital-modifier sibling survives here.

F461 / KI#784 — `m_abEverSeenDemographics` IS LATCHED ONLY BY ESPIONAGE-POINT
CHANGES, SO FULL SCORE HISTORY CAN BECOME VISIBLE WITHOUT EVER BEING RECORDED AS
"EVER SEEN"
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED — AdvCiv-introduced `advc.091` persistent-information bug retained by
AdvCiv 1.14 and current AdvCiv-SAS. K-Mod supplies the population-sensitive
espionage-cost machinery, but it has no `m_abEverSeenDemographics` latch; this exact
root begins when AdvCiv adds the persistent "ever seen" state and updates it from an
incomplete set of threshold-changing transitions.

ADVC.091 CONTRACT IS EXPLICIT
-----------------------------
AdvCiv's Info Screen adds `m_abEverSeenDemographics` specifically to distinguish
players whose **full score history has ever been visible** from players whose full
history has never been visible.

The shipped BUG option text for `Partial Score Graphs` states:

    For players whose full score history has never been visible to you,
    display a score graph that only shows the player's game scores since
    the turn that you've first met.

`CvInfoScreen.showTotalScoreGraph` implements exactly that distinction:

    return ((gameTurn - hasMetTurn >= 5 and PartialScoreGraphs) or
            pActivePlayer.hasEverSeenDemographics(iPlayer))

and score-cache construction uses the same latch to decide whether pre-contact score
history must be hidden.

Therefore this boolean is not merely an optimization hint. Once the active player has
actually met the passive-demographics threshold and the full score history has been
shown, the bit is meant to remain true even if current espionage visibility is later
lost.

THE ONLY LIVE LATCH UPDATE IS TOO NARROW
----------------------------------------
Current `CvPlayer::updateEverSeenDemographics(TeamTypes eTargetTeam)` does the right
predicate test:

    if (!hasEverSeenDemographics(target) && canSeeDemographics(target))
        m_abEverSeenDemographics.set(target, true);

but a whole-tree caller search finds only one normal DLL updater:

    CvTeam::setEspionagePointsAgainstTeam
        -> each member.updateEverSeenDemographics(eIndex)

No call is made from the other state transitions that change
`canSeeDemographics()` / passive mission cost.

That is insufficient because `canSeeDemographics` is not a function solely of the
watcher's current espionage points. It calls `canDoEspionageMission`, which compares
those points against `getEspionageMissionCost`, and that cost includes several
mutable modifiers.

MOST DIRECT CURRENT PRODUCER: COUNTERESPIONAGE EXPIRY
-----------------------------------------------------
The shipped demographics passive mission is live and has:

    ESPIONAGEMISSION_SEE_DEMOGRAPHICS
    iCost = 30
    bIsPassive = 1
    bSeeDemographics = 1

The shipped Counterespionage mission is also live and has:

    iCounterespionageNumTurns = 10
    iCounterespionageMod = 100

K-Mod's mission-cost modifier multiplies passive mission cost by the target team's
counterespionage modifier against the spying team:

    iModifier *= 100 + max(-100,
            kTargetTeam.getCounterespionageModAgainstTeam(getTeam()));
    iModifier /= 100;

Thus the shipped +100 Counterespionage effect doubles the otherwise identical
passive-demographics cost.

The effect expires in `CvTeam::doTurn`:

    changeCounterespionageTurnsLeftAgainstTeam(eOther, -1);
    if (getCounterespionageTurnsLeftAgainstTeam(eOther) == 0)
        setCounterespionageModAgainstTeam(eOther, 0);

`setCounterespionageModAgainstTeam` only stores the new modifier and dirties the
Espionage Advisor. It does **not** call `updateEverSeenDemographics` for members of
the opposing team whose passive visibility may just have become affordable.

Likewise, changing population can alter K-Mod's `CvTeam::getEspionageModifier`, and
therefore the passive threshold, without changing current espionage points. The
Counterespionage expiry case is sufficient by itself and gives the cleanest current
reproduction.

DETERMINISTIC CURRENT-DATA REPRODUCTION
---------------------------------------
Use two met singleton major teams A and B with equal total population and arrange
equal team `getEspionagePointsEver()` values. This makes K-Mod's population/points
ratio modifier exactly 100. Arrange the current directional points so that:

* B currently has 0 EP against A (spent points do not reduce PointsEver);
* A currently has 50 EP against B;
* B has an active +100 Counterespionage modifier against A;
* A has never previously met the demographics threshold, so
  `A.hasEverSeenDemographics(B) == false`.

These values are ordinary reachable state: points-ever persists after active missions
spend current points, and the current Counterespionage mission creates the +100 timed
effect.

With equal team sizes and the setup above:

1. The base passive-demographics cost is 30. B's current EP against A is zero, so the
   passive base-cost term remains 30.
2. Equal population and equal PointsEver give an espionage ratio modifier of 100.
3. While B's Counterespionage is active, the final +100 modifier doubles the cost to
   60. A has only 50 EP, so `canSeeDemographics(B)` is false.
4. If A gains/reaches those 50 points while protection is active,
   `setEspionagePointsAgainstTeam` does call `updateEverSeenDemographics`, but the
   predicate is false and the latch correctly remains false at that moment.
5. B's Counterespionage timer later reaches zero in `CvTeam::doTurn`.
   `setCounterespionageModAgainstTeam(B->A, 0)` lowers A's demographics threshold
   from 60 to 30 without changing A's EP against B.
6. A can now see B's demographics and full Info Screen score history. Opening the
   Info Screen classifies B through the live `canSeeDemographics` check and really
   reveals that history.
7. No `updateEverSeenDemographics` call occurs on the counterespionage-expiry path,
   so `hasEverSeenDemographics(B)` incorrectly remains false despite the history
   having been visible.
8. If B later applies Counterespionage again, A's current visibility can disappear.
   With the default `Partial Score Graphs = False`, `showTotalScoreGraph(B)` now also
   returns false because the supposedly permanent "ever seen" bit was never latched.
   The full score history disappears as though A had never unlocked it.

The same missing latch can be reached whenever a non-EP cost dependency crosses the
threshold. K-Mod's `getEspionageModifier` depends on both teams' current population,
so population changes provide another ordinary source. Counterespionage expiry is the
simpler proof because the shipped +100 modifier creates a large deterministic
threshold change.

LIVE CONSEQUENCE
----------------
This is persistent-information state, not merely an Espionage Advisor redraw issue.
While the threshold is temporarily satisfied, the ordinary Info Screen uses
`canSeeDemographics()` and exposes the target's full graph/history information. Once
current visibility is lost, AdvCiv intends `hasEverSeenDemographics` to preserve the
fact that this history was already revealed. The missed latch can instead revoke the
full Total Score graph later and make partial/full history gating behave as if the
player had never seen it.

The default shipped BUG setting `Partial Score Graphs = False` makes the consequence
particularly direct: after visibility is lost again, a false ever-seen bit removes
the target's Total Score graph rather than falling back to the optional post-contact
partial graph.

ANCESTRY
--------
K-Mod provides the population-sensitive `CvTeam::getEspionageModifier` and timed
Counterespionage cost effect, but does not have AdvCiv's persistent ever-seen score-
graph latch.

AdvCiv practical 2468 / SHA `f91c93bea834f12a3f5da1894d8afaa1a07b4cac`, which
introduced `advc.091` ("Show score graph (Info screen) regardless of espionage"),
added all of the following together:

* `m_abEverSeenDemographics`;
* `updateEverSeenDemographics`;
* Info Screen `hasEverSeenDemographics` graph gating;
* a call to the updater from `CvTeam::setEspionagePointsAgainstTeam`.

The introducing diff contains no updater on Counterespionage modifier/timer changes,
population changes, or a generic "current demographics just became visible" path.
The only later history change touching the call merely added the Barbarian guard
(practical 3437, "Fix minor issue with a failed assertion"). Base AdvCiv 1.14 and
current SAS retain the same incomplete updater set.

This is therefore an AdvCiv integration/invalidation omission: the new persistent
cache was wired to one input (`EspionagePointsAgainstTeam`) although its predicate
has additional mutable inputs inherited from K-Mod.

DUPLICATE CHECK
---------------
The album already contains F079/KI#401 in the same `advc.091` feature family:
Permanent Alliance resets `m_aiHasMetTurn`, which breaks first-contact dates used by
partial score graphs. That is a different stored field, transition and consequence.

The album also contains F078/KI#400 for Permanent Alliance dropping live
Counterespionage state. F461 does not depend on a Permanent Alliance and does not lose
a Counterespionage effect; it concerns the missing persistent-demographics latch when
a legitimate effect expires or another cost input changes.

Full-album searches for `m_abEverSeenDemographics`, `updateEverSeenDemographics`,
Counterespionage-driven demographics visibility and the full-history latch found no
prior root. F461 is distinct.

CONSERVATIVE REPAIR DIRECTION — NOT IMPLEMENTED
-----------------------------------------------
The invariant to preserve is:

    whenever `canSeeDemographics(target)` becomes true for a player who has not
    previously latched it, set `m_abEverSeenDemographics[target] = true`.

A narrow repair should at minimum update opposing player members when a
Counterespionage modifier changes in a direction that can reduce their passive cost,
especially when the modifier expires. However, population-dependent espionage ratios
mean Counterespionage alone is not the complete dependency set.

The more robust design is to centralize/lazily latch this state at a normal place that
actually queries or refreshes demographics visibility, or to call a small updater
from every authoritative transition that can lower the passive-demographics cost.
Care is needed not to turn a read-only const query into surprising persistent mutation
without considering save/OOS behavior.

Regression tests should cover at least:

1. reach the demographics threshold through an EP change -> ever-seen becomes true
   (existing path/control);
2. remain below the protected threshold, let +100 Counterespionage expire without any
   EP change, become able to see demographics -> ever-seen must become true;
3. lose current visibility again -> full-score-history gating must still remember the
   prior reveal;
4. optionally, cross the threshold solely through a population-driven
   `getEspionageModifier` change.

POST-F461 CLEAN / RECONCILED CONTROLS
-------------------------------------
* capital yield/commerce modifier mutators reconcile with their rank/commerce cache
  update chains; F445 remains retracted.
* religion conversion/anarchy helpers inspected in the immediate neighboring pass
  retain their inherited timer/update contracts; no separate F462 candidate was
  established before this checkpoint.
* the previously classified Golden-Age readiness UnitClass vs consumption UnitType
  mismatch remains current-data masked and is not duplicated here.

CURRENT STONE AFTER C031-WIP178
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F461 / KI#784 CONFIRMED.
F462 / provisional KI#785 UNUSED.
Latest physical checkpoint: C031-WIP178.
No repository source files modified.

NEXT
----
Continue Queue 004 from F462 unused. Finish the neighboring demographics/intelligence
invalidation siblings only where they have an independent current consequence, then
resume genuinely under-covered normal-turn player state: religion/corporation state,
strike/turn-active lifecycle, population/land/score bookkeeping, and other persistent
mutators. Keep current SAS data + shipped caller + full callee chain + ancestry +
duplicate proof mandatory before promotion.

END C031-WIP178

===============================================================================
C031-WIP179 — QUEUE 004 CONTINUATION — F462 UNUSED / CLOSURE CHECKPOINT
===============================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP178 with F462 / provisional KI#785 unused. This
tranche deliberately stayed conservative after F461: it reconciled the immediate
intelligence/cache siblings, relationship-sensitive score arithmetic, religion /
corporation counts, delayed-event lifetime handling, and live civilization-change
state before spending another root number. No repository source files were modified.

POST-F461 CONTROL — PERMANENT ALLIANCE TEAM-SIZE SCORE INPUT IS RECOMPUTED
--------------------------------------------------------------------------
`CvPlayer::getPopScore(true)` and `getLandScore(true)` divide the included vassal
contribution by `GET_TEAM(getTeam()).getNumMembers()`. A Permanent Alliance therefore
changes those dynamic score values when the absorbed players move onto the surviving
team, which initially looked like another relationship-cache sibling of F455/F458.

The merge path closes the invariant, however: `CvTeam::addTeam` finishes with an
unconditional `kGame.updateScore(true)` after the player/team reassignment work. The
global player scores and ranks are therefore recomputed using the new team-member
count. No stale-score root survives.

POST-F461 CONTROL — NO ANALOGOUS PERSISTENT "EVER SAW RESEARCH" LATCH
------------------------------------------------------------------------
The immediate intelligence sibling is isolated. AdvCiv stores persistent
`m_abEverSeenDemographics` specifically for score-graph history; research visibility
is queried from current `canSeeResearch` state and has no parallel persistent
"ever-seen research" field whose invalidation could share F461. No independent F462
candidate exists there.

RELIGION / CORPORATION COUNT RECONCILIATION
--------------------------------------------
The low-coverage count/mutator pass remained clean under its authoritative consumers:

* `changeHasReligionCount` and `changeHasCorporationCount` update building commerce
  and assignment AI state;
* corporation presence is additionally filtered dynamically through
  `isActiveCorporation` by `getHasCorporationCount`;
* `changeStateReligionCount` updates maintenance, religion happiness, religion
  commerce, assignment state and Score UI;
* no-corporation / no-foreign-corporation transitions call `updateCorporation`;
* no-foreign-trade has the inherited anarchy deferral paired with the later trade-
  route refresh when anarchy ends.

No missing zero-boundary update with a current independent gameplay consequence was
established.

PLAYER-WIDE YIELD / COMMERCE MUTATOR RECONCILIATION
----------------------------------------------------
The neighboring mutators also close under the actual cache graph:

* `changeSeaPlotYield` -> `updateYield`;
* `changeYieldRateModifier` invalidates the exact yield-rank cache and refreshes
  commerce where the yield is commerce-producing;
* capital yield/commerce modifier changes refresh the capital and ultimately the
  relevant rank caches; the earlier F445 retraction therefore remains correct;
* `changeTradeYieldModifier` -> `updateTradeRoutes`.

The known `updateExtraYieldThresholds` natural-threshold comparator typo remains a
real AdvCiv source defect but current SAS trait data still masks it: only Financial
provides a positive natural threshold and no competing positive ordinary threshold
exists. Do not number it under the current-data rule.

CONTROL — DELAYED EVENT COUNTDOWN HAS A REAL USE-AFTER-ERASE, BUT CURRENT SAS DATA
DOES NOT PRODUCE COUNTDOWNS
--------------------------------------------------------------------------------
`CvPlayer::doEvents()` contains an unambiguous C++ lifetime defect for due delayed
events:

    EventTriggeredData const* pTriggeredData = getEventCountdown(eLoopEvent);
    ...
    applyEvent(eLoopEvent, pTriggeredData->m_iId);
    resetEventCountdown(eLoopEvent);          // std::map::erase of the node
    aCleanup.push_back(pTriggeredData->m_iId); // dereference after erase

`getEventCountdown` returns the address of the `m_mapEventCountdown` node value and
`resetEventCountdown` erases that node, so the final dereference is invalid. The same
ordering is inherited from stock BtS/Civ4CE through K-Mod and base AdvCiv 1.14.

Current SAS nevertheless does not establish an ordinary producer. Every shipped
`<EventTimes>` table in `CIV4EventInfos.xml` is empty, and `applyEvent` calls
`setEventCountdown` only when an AdditionalEvent time is nonzero. A whole-tree search
also finds no Python-facing `setEventCountdown` producer. Thus no shipped current game
creates one of these delayed-countdown map nodes through the event system.

Preserve as a real dormant robustness bug. The narrow lifetime repair is to save
`pTriggeredData->m_iId` into an integer before erasing the countdown and use that saved
ID afterward. F462 remains unused.

CONTROL — LIVE CIVILIZATION CHANGE DOES NOT LEAVE A CURRENT SAS FOUNDER-CLASS COST
ENTRY BEHIND
----------------------------------------------------------------------------------
`CvPlayer::changeCiv` installs the new civilization and calls
`resetCivTypeEffects(false)`. That helper rewrites cached founding-unit extra cost for
founder UnitClasses present in the NEW civilization but does not explicitly erase
founder-class entries belonging only to the OLD civilization. In a more extensively
modded data set, changing between civilizations that use different founding UnitClasses
could therefore leave an old `m_aUnitExtraCosts` entry behind.

Current SAS masks that generic weakness completely: the entire Unit XML contains one
`bFound=1` unit, `UNIT_SETTLER`, and current civilization mappings use the same
`UNITCLASS_SETTLER`. `changeCiv` therefore rewrites the one relevant cache key rather
than leaving a distinct old founder class stale. True Starts uses `changeCiv`; the
Python ChangePlayer utility also reaches it, but neither changes that current-data
fact. No F462.

ADDITIONAL RECONCILED CONTROLS
------------------------------
* `getNewCityProductionValue` / founder-unit extra-cost caching is already owned by
  F448 for the live Rise & Fall human/AI-control invalidation; do not rediscover it
  through civilization-change or handicap-adjacent passes.
* `checkAlert` still has the real `>` vs `>= m_paAlerts.size()` robustness typo, but
  all shipped alert IDs/internal loops remain 0..size-1; this was already preserved
  unnumbered in earlier Queue-004 work.
* `canStealTech`'s `GET_TEAM(eTarget)` remains the already-closed AdvCiv overload trap:
  the `PlayerTypes` overload resolves the target player's real team.
* `killGoldenAgeUnits` UnitType-vs-readiness-UnitClass mismatch remains current-data
  masked because SAS has only one Golden-Age unit type per relevant class.

CURRENT STONE AFTER C031-WIP179
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F461 / KI#784 CONFIRMED.
F462 / provisional KI#785 UNUSED.
Latest physical checkpoint: C031-WIP179.
No repository source files modified.

NEXT
----
Continue Queue 004 from F462 unused using the mechanically ranked under-covered
normal-turn functions. Prefer authoritative production/economy/eligibility/state
paths over Advanced Start/debug/API-only helpers. Reconcile `canRevolution` / current
civic legality only if it yields an independent live path beyond F459, then continue
turn-control, vote/free-promotion, city-name/capture-owner, and remaining arithmetic
helpers. Keep current SAS data + shipped caller + full callee chain + ancestry +
duplicate proof mandatory before promotion.

END C031-WIP179


===============================================================================
C031-WIP180 — QUEUE 004 CVPLAYER.CPP — POST-WIP179 CLOSURE / DUPLICATE CONTROL
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F462 / provisional KI#785 remains UNUSED.
No repository source files modified.

RECONCILIATION — USE C031-WIP179 AS THE TRUE PREDECESSOR
--------------------------------------------------------
The working directory contained C031-WIP178 and C031-WIP179 beyond the earlier
WIP177 handoff. They extend the same reconciled album through F461 / KI#784.
This continuation therefore resumes from WIP179 rather than redoing or
overwriting already-settled work.

CONTROL — TEAM EVENT PROPAGATION CONDITION IS INVERTED, BUT THIS IS ALREADY
OWNED AS A DORMANT WIP158 REGRESSION
---------------------------------------------------------------------------
The current Team-event propagation branch still contains the suspicious
member condition that causes the current player to be revisited rather than
propagating the occurrence to the intended teammates. A fresh trace initially
made this look like an F462 candidate.

Album reconciliation caught it before renumbering: WIP158 already proved the
same practical-2416 regression. Current SAS still has no shipped Team
EventInfo/Trigger producer, so it remains a real dormant regression rather
than a current gameplay root. Do not duplicate it as F462.

CONTROL — SPY DESTROY/BRIBE UNIT OWNER VALIDATION IS ALSO AN EARLIER DORMANT
FINDING
-----------------------------------------------------------------------------
`canSpyDestroyUnit(eTarget, kUnit)` / the analogous bribe helper do not
themselves prove that `kUnit` belongs to `eTarget`; the fallback cost scan in
`getEspionageMissionBaseCost` iterates every unit on the plot. This initially
looked like a live three-civilization wrong-target candidate.

Reconciliation again catches an existing classification: the ownership hole
was already preserved in WIP172, and the current destroy-unit / buy-unit
mission factors are disabled in shipped SAS. Keep it dormant; do not assign
F462.

CONTROL — `OtherPlayerHasTech` IS CHECKED ON THE CORRECT RECEIVER
-----------------------------------------------------------------
The trigger eligibility condition

    GET_TEAM(getTeam()).isHasTech(kTrigger.getOtherPlayerHasTech())

looks suspicious if `CvPlayer::canTrigger` is read as though `this` were the
trigger owner. Full caller orientation resolves it:

    itPlayer->canTrigger(..., getID(), ...)

is invoked on each candidate other player. Inside `canTrigger`, `this` is
therefore the candidate OTHER player and the explicit `ePlayer` argument is
the trigger owner. `GET_TEAM(getTeam())` consequently checks exactly the team
named by `OtherPlayerHasTech`. No defect.

NORMAL-TURN ECONOMY / TRADE CONTROLS
------------------------------------
* `calculateInflationRate` remains within signed 32-bit range under current
  SAS game-speed horizons and modifiers; no overflow root established.
* `getNumTradeBonusImports` is a narrow alert/helper count and its
  player-to-player deal filtering is correctly typed for the shipped caller.
* Embargo / master-team exclusions reviewed in the same block agree with
  current vassal semantics.
* `canTradeWith` performs the team/network precheck and then asks
  `buildTradeTable` in both directions for the human-AI diplomacy caller.
  No independent live contradiction established.
* `getUnitCostMultiplier` itself is straightforward. Following its consumers
  re-exposed the generic `calculateUnitCost(..., iExtraUnits)` problem where
  the generic extra-unit delta also affects the military paid count; that is
  already F446 / KI#769 and must not be renumbered.

VOTE / FREE-PROMOTION / EVENT-SELECTION CONTROLS
-------------------------------------------------
* Event free promotions correctly affect existing matching units and the
  player-level free-promotion tables used by future units.
* `pickTriggerCity` and `pickTriggerUnit` choose only among the maximum-valued
  eligible candidates and randomize ties as intended.
* The Python trigger bridge re-resolves IDs after Python mutation; no stale
  pointer/ID root established in that path.
* `canForceReligion` and the related state-religion counters examined in this
  tranche have not exposed an independent live state-transition defect beyond
  already-owned findings.

CONTROL — `changeHurryCount` RAW `iChange` UPDATE IS SAFE UNDER CURRENT CALLERS
-------------------------------------------------------------------------------
`changeHurryCount` updates the derived pop-rush / gold-rush availability
counters by raw `iChange` when the source count crosses zero. In a generic API
this would be wrong if a single call crossed the boundary by magnitude > 1.

Current compiled callers constrain the delta: hurry availability is processed
through civic boolean sources using +/-1 transitions. The derived counter
therefore changes by exactly one whenever availability flips. No current root.

CONTROL — HOSTILE GOODY MIN-BARBARIAN DIVISION HAS A GENERIC CONFIGURATION
HOLE, BUT CURRENT SAS DATA MASKS IT
---------------------------------------------------------------------------
The hostile-unit block in `receiveGoody` contains:

    int iMinBarbs = max(kGoody.getMinBarbarians(),
        (kGoody.getMinBarbarians() * goodyHutEffectFactor(false)).sqrt().round());
    int iProb = (kGoody.getBarbarianUnitProb() * iMinBarbs) /
        kGoody.getMinBarbarians();

A bad goody configured to enter this block with `MinBarbarians == 0` could
therefore divide by zero.

Current shipped hostile goodies do not satisfy that malformed configuration:
the weak barbarian result has `MinBarbarians = 1`, the strong result has
`MinBarbarians = 2`. The other non-bad outcomes using
`BarbarianUnitProb` participate in AdvCiv's upgrade/additional-outcome logic,
not this zero-min hostile case. Preserve as robustness debt only; no F462.

CONTROL — ABSOLUTE GAME TURN IN ADVC.314 GOODY SCALING IS DESIGN-ALIGNED
------------------------------------------------------------------------
A later-era start begins with nonzero `getGameTurn()` while elapsed turns are
zero, so AdvCiv's absolute-turn checks can make early hut protections weaker
or already expired on turn 0 of Classical/Medieval starts. That initially
looked like an elapsed-vs-absolute-turn regression.

The surrounding feature points the other way. AdvCiv's `advc.314`
`goodyHutEffectFactor` itself deliberately scales from absolute world/game
turn, later-era starts receive correspondingly advanced starting packages,
and current Renaissance+ eras disable goodies entirely. The absolute-turn
tests are therefore coherent with the feature's overall world-age scaling,
not a proven stray substitution. Close; no F462.

TURN-CONTROL / SMALL STATE HELPERS
----------------------------------
`setAutoMoves`, `isTurnDone`, `setStrike`, `getReligionAnarchyLength`,
`setEspionageSpendingWeightAgainstTeam`, and nearby small state helpers were
rechecked through their ordinary callers. No new authoritative gameplay
contradiction has survived. Persistent strike-turn behavior is already
documented policy from earlier Queue-004 work rather than a fresh bug.

CURRENT STONE AFTER C031-WIP180
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F461 / KI#784 CONFIRMED.
F462 / provisional KI#785 UNUSED.
Latest physical checkpoint: C031-WIP180.
No repository source files modified.

NEXT
----
Continue from F462 unused in under-covered authoritative normal-turn code.
Start with the espionage mission-cost path around `getStealCostTech` and
`getEspionageMissionBaseCost`, carefully preserving receiver/target
orientation and checking active XML mission factors before deepening any
candidate. Then continue production/capture/state arithmetic as warranted.
Use the reconciled album as a duplicate filter before assigning any number.

END C031-WIP180


===============================================================================
C031-WIP181 — QUEUE 004 CVPLAYER.CPP — CIVIC AVAILABILITY / CACHE RECONCILIATION
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F462 / provisional KI#785 remains UNUSED.
No repository source files modified.

CONTROL — ORGANIZED RELIGION MONASTERY EXEMPTION DOES NOT LET INVALID
MISSIONARY PRODUCTION CONTINUE
----------------------------------------------------------------------
Current SAS uses the `SpecialBuildingNotRequired` civic mechanism in ordinary
gameplay: Organized Religion makes `SPECIALBUILDING_MONASTERY` unnecessary,
so missionaries whose normal prerequisite is a Monastery can legitimately be
queued without that building.

`setCivics` itself does not immediately call a city-production verifier, so a
queued missionary can remain temporarily present after switching away from
Organized Religion. The full city-turn lifecycle closes the apparent bypass:

* every `CvCity::doTurn()` begins with `doCheckProduction()`;
* `doCheckProduction()` reaches `checkCanContinueProduction(false)`;
* an order that no longer passes its unit/building prerequisites is popped
  with `bFinish=false`;
* this happens before the city can receive normal production for the turn,
  including through a period of civic anarchy.

Therefore loss of the civic exemption does not let an invalid missionary keep
accumulating normal production or complete. No F462.

CONTROL — LOSS OF PLAYER-WIDE SPECIALIST VALIDITY IS CLAMPED BEFORE THE NEXT
PRODUCTIVE TURN
-------------------------------------------------------------------------------
Organized Religion also grants player-wide Priest validity in current SAS.
Losing that civic can temporarily leave assigned Priests above the city's
ordinary slot count.

The player-level mutator dirties city work assignment. On the first
non-anarchy `CvPlayer::doTurn`, `AI_assignWorkingPlots()` reaches each city and
clamps assigned specialists that are no longer valid and exceed the restored
slot cap before gold, research and city processing. During civic anarchy the
cleanup is deferred, but the city is in disorder and gains no productive turn
from the stale assignment. No F462.

ROBUSTNESS NOTE — `canHurry` DOES NOT REVALIDATE THE CURRENT PRODUCTION ORDER
-----------------------------------------------------------------------------
There is a real inherited timing/API hole adjacent to the preceding closure.

After a civic switch makes the current queued unit/building invalid,
`CvCity::canHurry` does not call `canContinueProduction`, `canTrain` or
`canConstruct` for the current order. A human can therefore hurry that stale
order before the end-turn `doCheckProduction()` cleanup.

The DLL accepts the action: population/gold is spent, hurry anger is applied
and hurry production is added. At the subsequent city turn, however,
`doCheckProduction()` removes the still-invalid order with `bFinish=false`;
the unit/building is not created. The accumulated per-type production remains
banked for a future legal build.

A current-data example is a Spiritual player using Organized Religion +
Slavery, queuing a missionary without its Monastery, switching away from
Organized Religion with zero civic anarchy and whipping the now-invalid queue
entry before ending the turn.

This is a genuine inherited action-validation/UX robustness defect, but it
does not bypass the prerequisite or produce the invalid unit/building. The
same player could also have hurried the valid order immediately before the
civic switch. Preserve below the numbered gameplay-root threshold; F462 stays
unused.

CONTROL — `processCivics` FIELD/CACHE RECONCILIATION
-----------------------------------------------------
A field-by-field pass compared civic effects against their mutators and live
consumers rather than assuming every zero-boundary counter needed an explicit
bulk refresh.

Important closures:

* distance, colony, number-of-cities and corporation maintenance modifiers
  call player-wide `updateMaintenance()`, which recomputes every city's
  cached maintenance and the player's total;
* trade-route modifiers refresh trade routes;
* yield/commerce modifiers invalidate or recompute the affected caches;
* happiness/health effects are dynamically queried or dirty city assignment;
* `NoCivicUpkeep` and `NoNonStateReligionSpread` are consumed dynamically;
* `BuildingOnlyHealthy` is dynamically reflected in city health and dirties
  assignment;
* `SpecialBuildingNotRequired` is enforced by the city production check above;
* `SpecialistValid` is clamped through dirty work assignment as described
  above.

No additional stale-economy sibling of F459 was established.

CONTROL — STATIC LOCAL GLOBALDEFINE CACHES ALREADY MECHANICALLY AUDITED
------------------------------------------------------------------------
A fresh SAS-specific scan considered whether local `static const` performance
caches could retain game-dependent values across multiple games in one Civ4
process.

Album reconciliation shows that this exact class was already mechanically
closed in the earlier File-004 control pass: 103 GlobalDefine names read by
current `CvPlayer.cpp` were compared against runtime `setDefineINT`,
`setDefineFLOAT` and `setDefineSTRING` mutation sites in C++ and Python/map
scripts, with no overlap. The current local static values in this file are
overwhelmingly such GlobalDefines, color/info-type lookups and fixed text
lookups rather than player/world/game-speed expressions.

Do not duplicate this as a new investigation root unless a genuinely
game-state-dependent static expression is found.

LOW-COVERAGE MUTATOR CONTROLS
-----------------------------
The neighboring mutators checked in this tranche have not exposed an
independent current gameplay contradiction:

* feature happiness refreshes surrounding city health/happiness;
* luxury modifier is dynamically consumed by bonus happiness and dirties
  assignment;
* largest-city happiness, war-weariness and free-specialist changes dirty the
  appropriate city assignment state;
* state-religion unit/building production modifiers are queried dynamically;
* state-religion free experience is applied from current player state at unit
  completion;
* upkeep, level-experience, city-defense and feature-production modifiers are
  consumed from current state or refreshed through their normal update paths.

CURRENT STONE AFTER C031-WIP181
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F461 / KI#784 CONFIRMED.
F462 / provisional KI#785 UNUSED.
Latest physical checkpoint: C031-WIP181.
No repository source files modified.

NEXT
----
Continue from F462 unused with building-driven player effects and the remaining
production/capture/state arithmetic. In particular, reconcile player-level
effects gained/lost through buildings and obsolescence, then inspect
`resetCivTypeEffects` / civilization-change cleanup for old-civilization state
that can survive a current runtime civilization switch. Use ordinary current
callers and current SAS data first, and the reconciled album as a duplicate
filter before assigning any number.

END C031-WIP181


===============================================================================
C031-WIP182 — QUEUE 004 CVPLAYER.CPP — F462 / KI#785 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F462 / KI#785 is CONFIRMED.
F463 / provisional KI#786 is UNUSED.
No repository source files modified.

POST-WIP181 BUILDING / CIVILIZATION-CHANGE CONTROLS
----------------------------------------------------
The first continuation tranche closed several player-building lifecycle concerns before
F462 surfaced.

1. Building obsolescence player effects are symmetrically processed.

`CvTeam::changeObsoleteBuildingCount` detects the zero/nonzero obsolete-state transition
and calls each affected city's `processBuilding` with `bObsolete=true`, subtracting the
same active city/player/team/game effects when obsolescence begins and adding them back
if obsolescence is removed. No generic "obsolete wonder retains player bonus" root was
established.

2. Runtime civilization-change concrete-building mappings are a real debug/API caveat,
not an ordinary True Starts root.

Some `CvPlayer::processBuilding` effects are stored against the current civilization's
concrete building mapping, e.g. a wonder's `FreeBuildingClass` and
`BuildingHappinessChanges`. `changeCiv` does not reprocess already-existing buildings,
so an already-developed player changed through the cheat ChangePlayer utility can retain
old-civilization concrete-building caches/free-building state.

Current ordinary True Starts does not expose that state:
* `TrueStarts::changeCivs()` runs before `initFreeCivState` / `initFreeUnits` and before
  normal cities/buildings are created;
* scenario True Starts only performs the replacement when the scenario contains no
  cities;
* the Python ChangePlayer UI itself is gated behind cheats.

Preserve as runtime-change/debug robustness debt. It does not consume F462.

3. Permanent Alliance TeamShare-building replay is explicitly handled and current data
masks it anyway.

K-Mod/AdvCiv `CvTeam::shareItems` walks each constituent side's existing cities before
team reassignment. For `isTeamShare()` buildings it replays `CvPlayer::processBuilding`
onto the other side's players in both directions, and processes team building state as
well. Current SAS additionally has no `<bTeamShare>1</bTeamShare>` BuildingInfo entries.
No PA team-shared-building sibling of F455/F458.

4. AdvCiv's shrine cache key is internally consistent under current data.

`CvCity::processBuilding` detects shrine buildings through `getHolyCity()!=NO_RELIGION`
but indexes `m_aiShrine` with `getReligionType()`. All seven current shrine BuildingInfos
set `HolyCity` and `ReligionType` to the same religion, and `CvCity::hasShrine` reads the
cache by that religion. No current wrong-key root.


F462 / KI#785 — FORCE-CIVIC ESPIONAGE USES THE SPY OWNER'S REVOLUTION-COOLDOWN
MODIFIER INSTEAD OF THE TARGET PLAYER'S
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current espionage reaches the defect. This is an AdvCiv refactor
regression: stock BtS/Civ4CE and K-Mod use the target player's modifier explicitly;
Base AdvCiv 1.14 and current SAS call the new helper on the wrong `CvPlayer` receiver.

ROOT
----
On a successful Force Civic espionage mission, current SAS does:

    GET_PLAYER(eTargetPlayer).setCivics(
            GC.getInfo(eCivic).getCivicOptionType(), eCivic);

    GET_PLAYER(eTargetPlayer).setRevolutionTimer(
            getMinTurnsBetweenRevolutions());

The target of the setter is correct, but the unqualified
`getMinTurnsBetweenRevolutions()` is invoked on `this` — the spying player.

The helper is player-specific:

    int getMinTurnsBetweenRevolutions() const
    {
        return std::max(1,
                ((100 + getAnarchyModifier()) *
                MIN_REVOLUTION_TURNS) / 100);
    }

Therefore the victim's post-mission civic cooldown is computed from the SPY OWNER's
`getAnarchyModifier()`.

The immediately neighboring Force Religion branch provides a useful internal control.
It sets the target's conversion timer from:

    GET_PLAYER(eTargetPlayer).getAnarchyModifier()

rather than the spy owner's modifier.

CURRENT SHIPPED REACHABILITY
----------------------------
Current `CIV4EspionageMissionInfo.xml` enables both ideology-changing mission families:

    ESPIONAGEMISSION_SWITCH_CIVIC
        iSwitchCivicCostFactor = 1000
        TechPrereq = NONE

    ESPIONAGEMISSION_SWITCH_RELIGION
        iSwitchReligionCostFactor = 1000
        TechPrereq = NONE

`canForceCivics` permits an ordinary eligible different civic when the target can use
it and either the spy is already using it or the civic's AdvCiv `canAlwaysForce` flag
allows it. The successful mission reaches the code above directly.

The revolution timer is authoritative gameplay state. `CvPlayer::canRevolution` and
`canDoAnyRevolution` both reject civic changes while `getRevolutionTimer() > 0`, and
the timer is decremented through the normal player-turn path.

CURRENT SAS NUMERIC REPRODUCTION
--------------------------------
The inherited current value is:

    MIN_REVOLUTION_TURNS = 5

Current SAS also has ordinary player-level sources of a different anarchy modifier:

    BUILDING_PYRAMID       iAnarchyModifier = -100
    BUILDING_VERSAILLES    iAnarchyModifier = -100

Both have no obsolete technology and 100% conquest retention in current data.

Thus two simple ordinary cases produce deterministic wrong cooldowns.

Case A — spy has Pyramids, target has no anarchy modifier:

    spy getMinTurnsBetweenRevolutions()
        = max(1, (100 - 100) * 5 / 100)
        = 1

    target's correct value
        = max(1, 100 * 5 / 100)
        = 5

Current code forces the target civic and gives the victim a revolution timer of 1
instead of 5. The victim can undo/change civics four turns earlier than its own rules
permit, weakening the espionage effect.

Case B — spy has no modifier, target has Pyramids:

    spy helper = 5
    target helper = 1

Current code gives the victim 5 instead of 1, locking the target out of voluntary
civic changes four turns longer than the target's own anarchy/cooldown modifier says.

No malformed setup, scenario-only state or disabled XML is required.

ANCESTRY
--------
The supplied stock BtS/Civ4CE implementation sets:

    GET_PLAYER(eTargetPlayer).setRevolutionTimer(
        std::max(1,
            ((100 + GET_PLAYER(eTargetPlayer).getAnarchyModifier()) *
            MIN_REVOLUTION_TURNS) / 100));

K-Mod 1.46 retains that target-explicit expression.

AdvCiv introduces/uses the player helper `getMinTurnsBetweenRevolutions()` and replaces
the duplicated arithmetic in this branch with:

    GET_PLAYER(eTargetPlayer).setRevolutionTimer(
        getMinTurnsBetweenRevolutions());

but omits the target receiver. Base AdvCiv 1.14 retains the wrong receiver, and current
SAS inherits it.

This is therefore not a disagreement over espionage policy. The pre-refactor lineage
and the neighboring Force Religion branch both identify the target player as the
intended modifier subject.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains multiple force-civic/religion espionage findings, but
they concern different contracts:
* localization argument/key defects in Queue 003;
* F454's third-party capital-coordinate information leak;
* dormant destroy/bribe unit ownership / popup-ID issues.

No existing root covers the post-Force-Civic revolution timer being computed from the
spying player's anarchy modifier. F462 is independent.

CONSERVATIVE REPAIR
-------------------
Call the helper on the player whose timer is being set:

    CvPlayer& kTarget = GET_PLAYER(eTargetPlayer);
    kTarget.setCivics(GC.getInfo(eCivic).getCivicOptionType(), eCivic);
    kTarget.setRevolutionTimer(kTarget.getMinTurnsBetweenRevolutions());

or retain the existing spelling and qualify only the helper call:

    GET_PLAYER(eTargetPlayer).setRevolutionTimer(
            GET_PLAYER(eTargetPlayer).getMinTurnsBetweenRevolutions());

A regression test should use different spy/target anarchy modifiers and verify that
changing only the SPY's modifier does not change the victim's timer, while changing
the TARGET's modifier does.

NUMBERING
---------
F462 / KI#785 is CONFIRMED.
F463 / provisional KI#786 is the next unused root.

CURRENT STONE AFTER C031-WIP182
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F462 / KI#785 CONFIRMED.
F463 / provisional KI#786 UNUSED.
Latest physical checkpoint: C031-WIP182.
No repository source files modified.

NEXT
----
Continue from F463 unused. Finish the neighboring active espionage-effect subject/timer
audit while the receiver orientation is hot, then return to remaining building-driven
player effects and production/capture/state arithmetic. Keep current SAS XML activation,
ordinary caller reachability, full callee chain, lineage and duplicate proof mandatory
before promotion.

END C031-WIP182


===============================================================================
C031-WIP183 — QUEUE 004 CVPLAYER.CPP — F463 / KI#786 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F463 / KI#786 is CONFIRMED.
F464 / provisional KI#787 is UNUSED.
No repository source files modified.


F463 / KI#786 — K-MOD'S POST-SABOTAGE AI PRODUCTION-RECONSIDERATION HOOKS
TEST THE SPY OWNER'S HUMAN STATUS INSTEAD OF THE SABOTAGED CITY OWNER
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Both affected espionage mission families are enabled in current SAS and
ordinary human-vs-AI espionage reaches the wrong branch decision.

ROOT
----
K-Mod added production-choice invalidation after building/production sabotage so an AI
city can reconsider its current build after the sabotage changes the city's state.

Current SAS inherits these two branches in `CvPlayer::doEspionageMission`:

    if (kMission.getDestroyBuildingCostFactor() > 0 && pCity != NULL)
    {
        ...
        pCity->setNumRealBuilding(... - 1);
        ...
        if (!isHuman() || pCity->isProductionAutomated())
            pCity->setChooseProductionDirty(true);
    }

and:

    if (kMission.getDestroyProductionCostFactor() > 0 && pCity != NULL)
    {
        ...
        pCity->setProduction(0);
        ...
        if (!isHuman())
            pCity->setChooseProductionDirty(true);
    }

`CvPlayer::doEspionageMission` is invoked on the SPY OWNER. The unqualified
`isHuman()` therefore asks whether the attacker is human.

The city being marked dirty belongs to the espionage target. Whether that TARGET city
is AI-controlled is the relevant condition for K-Mod's production replanning.

WHY THE BAD SUBJECT IS NOT REPAIRED ELSEWHERE
----------------------------------------------
`CvCity::setNumRealBuilding` updates the real-building count and processes the
building's city/player/team/game effects, but it does not set
`ChooseProductionDirty`.

`CvCity::setProduction(0)` likewise resets the current order's stored production
without removing the order or forcing a fresh choice.

At the target city's later `doProduction()`:

    if (!isHuman() || isProductionAutomated())
    {
        if (!isProduction() || isProductionProcess() ||
            isChooseProductionDirty())
        {
            AI().AI_chooseProduction();
        }
    }

An AI target with an existing valid production order therefore re-runs
`AI_chooseProduction()` after sabotage only when the dirty bit was set. If the bit is
missing, it simply continues the existing order (with the sabotaged production amount
or changed building environment).

For a building whose destruction makes the current order outright illegal,
`doCheckProduction` can independently remove that illegal order. That does not close
this root: K-Mod's hook deliberately asks the AI to reconsider even when the current
order remains legal, and Destroy Production normally leaves the current order legal.

AUTHORITATIVE K-MOD INTENT
--------------------------
The supplied K-Mod 1.46 changelog explicitly states:

    "The AI will now reconsider its current city production after a building is
     sabotaged."

The corresponding K-Mod source introduces:

    if (!isHuman() || pCity->isProductionAutomated())
        pCity->AI_setChooseProductionDirty(true);

inside the attacker's `CvPlayer::doEspionageMission`, making the receiver mix-up part
of the original K-Mod implementation rather than an AdvCiv/SAS semantic choice.

The Destroy Production branch contains the same attacker-side `!isHuman()` pattern.

CURRENT SAS REACHABILITY
------------------------
Current `CIV4EspionageMissionInfo.xml` enables both affected active missions:

    ESPIONAGEMISSION_DESTROY_BUILDING
        iDestroyBuildingCostFactor = 500
        bIsTwoPhases = 1
        bTargetsCity = 1

    ESPIONAGEMISSION_DESTROY_PRODUCTION
        iDestroyProductionCostFactor = 500
        bTargetsCity = 1

A normal human player can therefore perform either mission against an AI city.

Deterministic control-flow reproduction:

1. AI player B owns a city that currently has a valid production order.
2. Human player A performs Destroy Production against that city.
3. `this == A`, so `!isHuman()` is false.
4. The city's production becomes 0 but `ChooseProductionDirty` is not set.
5. On B's city turn, the order still exists and is valid, so `doProduction` does not
   call `AI_chooseProduction`; B simply continues that pre-sabotage order.

Perform the same mission against the same AI city from an AI-controlled attacker:

1. `this` is now AI, so `!isHuman()` is true.
2. The target city is marked dirty.
3. Its next `doProduction` calls `AI_chooseProduction()` and re-evaluates the build.

The victim's AI behavior therefore changes solely with the controller type of the
ATTACKER, even though the target city and sabotage result are otherwise identical.

Destroy Building has the same subject asymmetry; a human spy suppresses the explicit
K-Mod production-reconsideration hook for an AI target unless the target city happens
to be production-automated.

ANCESTRY
--------
Stock BtS/Civ4CE does not contain these K-Mod production-reconsideration additions.

K-Mod 1.46 adds the two dirtying hooks with the attacker-side `isHuman()` spelling.
Base AdvCiv 1.14 retains them (renaming `AI_setChooseProductionDirty` to the current
setter where appropriate), and current SAS retains the same receiver logic.

This is therefore best classified as an inherited K-Mod wrong-subject integration bug.

DUPLICATE / ALBUM CHECK
-----------------------
No reconciled album root covers attacker human status controlling the target AI city's
post-espionage production replanning. Existing espionage roots cover visibility leaks,
localization/help, Force Civic cooldown subject (F462), and dormant target-unit
ownership/ID issues. F463 is independent.

CONSERVATIVE REPAIR
-------------------
Base the condition on the sabotaged city's owner, not on the spy owner. For example:

    CvPlayer const& kTargetOwner = GET_PLAYER(pCity->getOwner());

    if (!kTargetOwner.isHuman() || pCity->isProductionAutomated())
        pCity->setChooseProductionDirty(true);

Use the same target-owner test in both Destroy Building and Destroy Production
branches. For Destroy Production, including `pCity->isProductionAutomated()` gives the
same sensible human-city automation handling as the building branch.

A regression test should sabotage the same AI city from a human attacker and an AI
attacker and verify that the target city receives identical production-dirty state.

NUMBERING
---------
F463 / KI#786 is CONFIRMED.
F464 / provisional KI#787 is the next unused root.

CURRENT STONE AFTER C031-WIP183
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F463 / KI#786 CONFIRMED.
F464 / provisional KI#787 UNUSED.
Latest physical checkpoint: C031-WIP183.
No repository source files modified.

NEXT
----
Continue from F464 unused. Finish active espionage execution/cost subject symmetry while
the paired-branch audit is open, then resume remaining player-building/state arithmetic,
score/counter lifecycles and low-coverage normal-turn helpers. Preserve current-data,
ordinary-caller, full-callee-chain, ancestry and duplicate proof before promotion.

END C031-WIP183


===============================================================================
C031-WIP184 — QUEUE 004 CVPLAYER.CPP — F464 / KI#787 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F464 / KI#787 is CONFIRMED.
F465 / provisional KI#788 is UNUSED.
No repository source files modified.

RECONCILIATION / CORRECTION — WIP181 TEAMSHARE XML SENTENCE WAS FACTUALLY WRONG
-------------------------------------------------------------------------------
WIP181 included the statement that current SAS had no `<bTeamShare>1</bTeamShare>`
BuildingInfo entries. That sentence is incorrect and must not be carried forward.

Current `CIV4BuildingInfos.xml` does contain TeamShare buildings, including the Three
Gorges Dam and Great Wall family inspected during the Permanent-Alliance area-effect
sweep.

The substantive PA closure survives the correction: `CvTeam::shareItems` explicitly
walks existing cities and replays `CvPlayer::processBuilding` for TeamShare buildings
in both directions before absorbed players are reassigned by `setTeam`. Therefore the
earlier conclusion did not rely on the false "no current TeamShare buildings" premise.

A count-weighting robustness wrinkle remains: TeamShare area counters are incremented
per player, so changing team membership can leave noncanonical signed counts when a
shared building is later removed. Clean Power's zero/nonzero transition logic still
flips correctly. Border-obstacle reactivation would be more fragile after a negative
count, but current Great Wall obsoletes at Gunpowder, well before ordinary Permanent
Alliance timing. Preserve as a PA/cache robustness note, not a numbered current root.

DUPLICATE CONTROL — RELIGION SPREAD UNIT AREA-TRAINING BUG IS ALREADY F451 / KI#774
------------------------------------------------------------------------------------
A fresh pass rediscovered that `countReligionSpreadUnits(pArea, ..., true)` limits
existing missionaries to `pArea` but counts missionaries currently being trained in
all cities on the map, unlike its corporation sibling which applies the area filter to
both existing and training units.

Album reconciliation caught this before renumbering: this is already the confirmed
F451 / KI#774 root, including the ordinary `AI_missionaryValue` area-scoped consumer.
Do not duplicate it as F464.

CONTROL — PERMANENT-ALLIANCE STALE ESPIONAGE WEIGHTS ARE EXCLUDED FROM ALLOCATION
-----------------------------------------------------------------------------------
`m_aiEspionageSpendingWeightAgainstTeam` is player state indexed by TeamTypes and is
not remapped when Permanent Alliance `setTeam` changes a player's team. That creates
stale array entries toward the absorbed/surviving team IDs.

The live allocator closes the concern:

    CvPlayer::getEspionageSpending
      -> TeamIter<CIV_ALIVE,OTHER_KNOWN_TO>(getTeam())

Both the denominator and remainder loops iterate only alive OTHER teams. After
absorption the old team loses its alive members and is excluded; the surviving team is
now the player's own team and is likewise excluded. The stale slots therefore do not
consume current espionage commerce. No F464 from this state family.

CONTROL — SABOTAGE PRODUCTION COST SUBJECT ASYMMETRY REMAINS POLICY-AMBIGUOUS
-----------------------------------------------------------------------------
Destroy Building / Destroy Project mission pricing calls the SPY OWNER's
`getProductionNeeded(Building/Project)`, whose AdvCiv implementation includes
player-specific handicap / AI production-cost adjustments. Thus two attackers can in
principle receive different EP prices for sabotaging the same target object.

BtS and K-Mod already use the acting player's production-cost helper. No local help,
manual or AI valuation contract found in this tranche establishes that the victim's
effective cost is intended instead. Difficulty-normalized espionage pricing is
plausible inherited policy. Preserve the asymmetry as an observation; do not promote it
without an authoritative semantic contradiction.

CONTROL — ACTIVE ESPIONAGE EXECUTION SUBJECT SWEEP AFTER F462/F463
------------------------------------------------------------------
The remaining enabled `doEspionageMission` branches were compared side by side after
the F462/F463 wrong-subject pair.

No further independent wrong-player root was established:
* poison / unhappiness / revolt mutate the target city;
* stolen treasury credits `this` and debits the explicit target;
* tech theft grants the spying team;
* Force Religion explicitly uses the target player's anarchy modifier;
* counterespionage is stored on the spying team against the target team;
* the attacker-humanity check in the revolt branch is diagnostic logging only.

The apparent `GET_TEAM(eTarget)` syntax in project sabotage is also an already-recorded
false positive: current `CvPlayer.cpp` includes `CoreAI.h`, whose overloaded
`GET_TEAM(PlayerTypes)` resolves the player to its actual team.


F464 / KI#787 — ADVC SPACESHIP-LAUNCH MESSAGE COORDINATES LEAK FROM ONE OBSERVER
TO ALL LATER OBSERVERS IN THE PLAYER ITERATION
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current Space Race launch reaches the defect. This is an AdvCiv
`advc.106` refactor regression. Stock BtS/Civ4CE and K-Mod reset the capital coordinates
for each observer; Base AdvCiv 1.14 and current SAS carry them across observers.

ROOT
----
Current SAS `CvPlayer::launch` does:

    CvCity const* pCapital = getCapital();
    CvWString szMsg(...);
    int iPlotX = -1;
    int iPlotY = -1;

    for (PlayerIter<MAJOR_CIV> it; it.hasNext(); ++it)
    {
        CvPlayer const& kObs = *it;
        if (pCapital != NULL && pCapital->isRevealed(kObs.getTeam()))
        {
            iPlotX = pCapital->getX();
            iPlotY = pCapital->getY();
        }

        ...
        gDLL->UI().addMessage(kObs.getID(), ..., iPlotX, iPlotY, true, true);
    }

The coordinate variables are initialized only once. If observer A has revealed the
launching capital, the pair becomes the true capital `(x,y)`. For a later observer B
whose team has NOT revealed the capital, the `if` body is skipped but `iPlotX/iPlotY`
retain A's coordinates. B therefore receives a message carrying the hidden capital's
location.

The reveal predicate itself is correct; the defect is state leaking across iterations.

DETERMINISTIC ORDINARY REPRO
----------------------------
AdvCiv's `PlayerIter<MAJOR_CIV>` uses the `CvAgents::MAJOR_ALIVE` sequence. `CvAgents`
builds and maintains these sequences in ascending player ID order.

Use a normal game in which:

1. Player 0 is a major civilization pursuing Space Race.
2. Player 1 is another living major civilization.
3. Player 1 has never revealed Player 0's capital.
4. Player 0 completes the spaceship requirements and launches.

The current Space Race victory remains enabled in shipped
`CIV4VictoryInfo.xml` (`VICTORY_SPACE_RACE`, 10-turn base delay), with the usual
victory projects in `CIV4ProjectInfo.xml`.

During `Player 0::launch`:

* observer Player 0 is visited before Player 1;
* Player 0's own team has its own capital revealed, so `iPlotX/iPlotY` become the true
  capital coordinates;
* observer Player 1 then fails the `pCapital->isRevealed(Player1Team)` test;
* the code does NOT restore `-1,-1`;
* Player 1 receives `TXT_KEY_VICTORY_TEAM_HAS_LAUNCHED` with Player 0's true capital
  coordinates.

More generally, once any earlier observer has revealed the capital, every later
unrevealed observer inherits the location until another assignment (which is the same
location anyway).

PLAYER-FACING CONSEQUENCE
-------------------------
`CvDLLInterfaceIFaceBase::addMessage` forwards `iFlashX/iFlashY` into the EXE message
payload. Its current AdvCiv comment explicitly says the flash information supplies
"the text color and coordinates stored in the Turn Log -- not just for the (disabled)
icon."

The launch call also requests on/off-screen arrows. Thus `(x,y)` is meaningful
player-facing map/message information, not an unused local.

This is the same visibility contract used in the already-confirmed F454 family:
unrevealed message locations should be `-1,-1`, while revealed observers can receive
the actual map coordinate.

ANCESTRY
--------
Stock BtS/Civ4CE `CvPlayer::launch` declares:

    int plotX = -1;
    int plotY = -1;

INSIDE the `for (i = 0; i < MAX_PLAYERS; ++i)` observer body. Each recipient therefore
starts from hidden coordinates and receives the capital location only if that
recipient's team has revealed it.

K-Mod 1.46 retains the same per-observer initialization.

AdvCiv 1.14 changes this under `advc.106`:

    // Cut from the loop below. Use this for the replay message as well.
    int iPlotX = -1;
    int iPlotY = -1;

The variables are hoisted before `PlayerIter<MAJOR_CIV>`, but no reset is inserted at
the top of each observer iteration. The refactor therefore converts correctly
observer-local state into persistent loop state.

Base AdvCiv 1.14 and current SAS contain the same hoisted implementation.

This ancestry makes intent especially strong: pre-AdvCiv code explicitly preserved
per-recipient reveal privacy; the bug appears exactly when the variables are moved for
replay-message reuse.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album has:
* F454: AdvCiv third-party Force Civic/Religion espionage announcement attaches raw
  target-capital coordinates rather than observer-aware coordinates;
* prior Space-capital/UWAI controls concerning whether a launched Space victory should
  make the CAPITAL an AI military target.

No existing root covers `CvPlayer::launch` carrying one recipient's revealed
coordinates into a later unrevealed recipient's launch message. F464 is independent.

CONSERVATIVE REPAIR
-------------------
Restore per-observer coordinates for the UI message while keeping separate coordinates
for the replay message.

For example:

    CvCity const* pCapital = getCapital();

    int iReplayX = -1;
    int iReplayY = -1;
    if (pCapital != NULL)
    {
        iReplayX = pCapital->getX();
        iReplayY = pCapital->getY();
    }

    for (PlayerIter<MAJOR_CIV> it; it.hasNext(); ++it)
    {
        CvPlayer const& kObs = *it;
        int iPlotX = -1;
        int iPlotY = -1;
        if (pCapital != NULL && pCapital->isRevealed(kObs.getTeam()))
        {
            iPlotX = pCapital->getX();
            iPlotY = pCapital->getY();
        }
        ...
        gDLL->UI().addMessage(..., iPlotX, iPlotY, true, true);
    }

    GC.getGame().addReplayMessage(..., iReplayX, iReplayY);

This restores the BtS/K-Mod recipient contract and preserves AdvCiv's intended replay
location.

A minimal regression test should launch as Player 0 while Player 1 has not revealed
Player 0's capital and verify that Player 1's launch message stores `-1,-1`; after
revealing that capital, the same observer should receive the real coordinates.

NUMBERING
---------
F464 / KI#787 is CONFIRMED.
F465 / provisional KI#788 is the next unused root.

CURRENT STONE AFTER C031-WIP184
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F464 / KI#787 CONFIRMED.
F465 / provisional KI#788 UNUSED.
Latest physical checkpoint: C031-WIP184.
No repository source files modified.

NEXT
----
Continue from F465 unused. Finish the observer-sensitive message/replay sweep while
F464's state-carry pattern is hot (especially other loops that hoist coordinates or
other observer-dependent data), then return to genuinely under-covered normal-turn
player state/arithmetic and lifecycle helpers. Preserve current-data reachability,
full callee-chain consequence, ancestry and duplicate proof before promotion.

END C031-WIP184


===============================================================================
C031-WIP185 — QUEUE 004 CVPLAYER.CPP — LOW-COVERAGE / LIFECYCLE CLOSURE TRANCHE
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F465 / provisional KI#788 remains UNUSED.
No repository source files modified.

POST-WIP184 CONTROL — OBSERVER-SENSITIVE MESSAGE FAMILY NOW LARGELY OWNED
-------------------------------------------------------------------------
Continuing the F464 launch-coordinate sweep exposed no new independent root.

The most tempting sibling, Great Person birth message text being mutated by one
observer and then reused for later observers, is an exact rediscovery of already
confirmed F449 / KI#772. Do not renumber it.

The reconciled observer-message family currently has clear ownership:
* F449 — mutable Great Person birth message text crosses observer iterations;
* F454 — Force Civic/Religion third-party announcement uses raw target-capital
  coordinates;
* F457 — two-civ World News announcement leaks the unmet second civilization;
* F464 — spaceship launch coordinates persist from a revealed observer into later
  unrevealed observers.

The other civic/religion/Golden-Age/city capture/trade/raze/founding,
colony-independence and event announcement loops sampled in this pass recompute their
observer-specific visibility/location state or have already-owned findings.

CONTROL — `getWorkRate(BuildTypes)` DOUBLE-100 EXPRESSION IS ALREADY PARKED
---------------------------------------------------------------------------
The source still contains the conspicuous AI branch:

    iRate *= max(0, 100 + AIWorkRateModifier + 100);

so a zero AIWorkRateModifier yields a factor of 200 rather than 100.

A fresh caller trace confirms the earlier WIP166 classification rather than creating
F465. Ordinary worker build progress uses unit/plot build-rate paths, not this
player-level estimate. The live two-argument `CvPlot::getBuildTurnsLeft(eBuild,
ePlayer)` users found here are human-facing plot/help/warning paths; AI Auto Play can
make the player nonhuman and expose the bad estimate in UI-like behavior, but no
ordinary authoritative worker completion path was established.

Keep the real source typo/latent estimate defect under its existing robustness note;
do not duplicate it as F465.

CONTROL — STARTING-PLOT `GET_TEAM(getID())` IS NOT A PLAYER/TEAM ENUM BUG
------------------------------------------------------------------------
The starting-visibility path contains calls that visually resemble:

    GET_TEAM(getID()).revealSurroundingPlots(...)

Current `CvPlayer.cpp` includes `CoreAI.h`; its overload accepting `PlayerTypes`
resolves the player to that player's actual team. The album already recorded this
control. Team games where PlayerTypes != TeamTypes therefore do not establish a
wrong-team reveal root from this spelling.

CONTROL — `initInGame` PLAYER-ID / TEAM-ID SUSPICION DOES NOT ADD A NEW COLONY ROOT
-----------------------------------------------------------------------------------
The initialization loop contains a comparison of a team-loop integer against
`getID()`, which is suspicious if read as fully generic player/team state.

Ordinary colony creation allocates the colony's fresh team in the corresponding player
slot, preserving the identity assumption on that path. More exotic recycled-player /
team-state divergence is already owned by F447 and its surrounding WIP155 work. No
independent current F465 consequence was established here.

CONTROL — LOW-COVERAGE HISTORICAL COUNTERS
-------------------------------------------
Several definitions had zero literal-name mentions in the album despite their
underlying contracts already being covered. Rechecking their live chains produced no
new root:

* `changeTotalPopulation` removes old Assets/Power/Score contribution, changes the
  stored population and adds the recomputed contribution;
* `changeTotalLandScored` does the analogous old/new Asset/LandScore update;
* `m_iNumUnitGoldenAges` is intentionally cumulative and drives escalating
  Great-Person requirements for later Golden Ages;
* `m_iHighestUnitLevel` is intentionally a historical maximum used for prerequisites;
* unit/building class count and making arrays perform their normal national-limit
  bookkeeping and UI dirties.

CONTROL — `pickConqueredCityOwner` VASSAL CONDITION IS LONGSTANDING RULE
-------------------------------------------------------------------------
The function's automatic return of a conquered city to the liberation candidate only
when the conqueror is that candidate's vassal looks unusual in isolation. Caller and
ancestry checks show the same rule in BtS/K-Mod and identify it as the longstanding
master-city return behavior. No AdvCiv/SAS regression established.

CONTROL — RUNTIME CIVILIZATION-CHANGE RESIDUE REMAINS DEBUG/GENERIC DEBT
-------------------------------------------------------------------------
`changeCiv` / `resetCivTypeEffects` still have generic runtime-change weaknesses where
state indexed by the old civilization's concrete unit/building mappings can survive a
developed-player civilization swap. A related example is old civilization-specific
founder-unit extra-cost entries not being comprehensively zeroed before new mappings
are established.

Ordinary True Starts performs its civilization replacement before developed
city/building state is created, and the current playable civilizations use the normal
Settler class for the relevant founder case. The developed-player ChangePlayer route
is cheat/debug tooling. Preserve the generic/runtime-switch debt, but no F465.

LATENT ADVC REFACTOR BUG — INDEPENDENCE MEMORY USES THE PARENT'S
`FreedomAppreciation` INSTEAD OF THE NEW COLONY'S
----------------------------------------------------------------
Current `CvPlayer::splitEmpire` grants the new colony's AI:

    GET_PLAYER(eNewPlayer).AI_changeMemoryCount(
        getID(), MEMORY_INDEPENDENCE,
        2 * GC.getInfo(getPersonalityType()).getFreedomAppreciation());

`this` is the parent. The memory count belongs to `eNewPlayer`, and the old pre-advc.130r
attitude implementation makes the intended personality subject clear:

    if (getParent() == ePlayer)
        iAttitude += GC.getInfo(getPersonalityType()).getFreedomAppreciation();

That old function executes on the CHILD AI. When advc.130r transformed the permanent
colony-attitude bonus into decaying `MEMORY_INDEPENDENCE`, using the parent's
`getPersonalityType()` therefore changed the semantic subject. The child-side value
would preserve the pre-refactor behavior.

This is a real AdvCiv abstraction/refactor error, but CURRENT SAS DATA MASKS IT:
the generated Sevopedia runtime cache contains 55 playable leader entries and every
one reports:

    getFreedomAppreciation = 10

`LEADER_DEFAULTS` likewise supplies 10; the only explicit 0 in XML is the Barbarian
leader. Therefore choosing the parent or child personality produces the same current
memory amount for every ordinary playable colony relationship, including Random
Personalities.

The AdvCiv manual also describes the converted feature as the old +10 gratitude being
represented as decaying memory, consistent with current uniform data.

Preserve this as a latent/data-dependent AdvCiv bug. Do NOT spend F465 unless shipped
leader data later differentiates `FreedomAppreciation` (or another ordinary producer
creates such differentiation).

A semantically correct cleanup would use:

    2 * GC.getInfo(GET_PLAYER(eNewPlayer).getPersonalityType()).
        getFreedomAppreciation()

if preserving the old child-personality behavior is desired.

CONTROL — `getStateReligionName()` NO-RELIGION GUARD NOT YET REACHABLE
-----------------------------------------------------------------------
`getStateReligionName()` directly indexes `GC.getInfo(getStateReligion())` without an
explicit `NO_RELIGION` guard. It is exported through Python/diplomacy string machinery,
so the generic API is brittle.

The compiled diplomacy use inspected in this pass belongs to religion-demand
placeholder production, whose producer is expected to have a state religion. No
ordinary current caller passing `NO_RELIGION` has been proven. Preserve as API
robustness only.

CURRENT STONE AFTER C031-WIP185
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F464 / KI#787 CONFIRMED.
F465 / provisional KI#788 UNUSED.
Latest physical checkpoint: C031-WIP185.
No repository source files modified.

NEXT
----
Continue from F465 unused with genuinely low-coverage authoritative player state:
remaining score/history/counter lifecycles, player start/death/revival transitions,
production/economic state mutators and any normal-turn arithmetic whose consumers
still depend on stored rather than dynamically queried values. Prefer source-level
subject/invariant contradictions with current SAS data over generic API brittleness.
Run album duplicate checks before deepening or numbering every candidate.

END C031-WIP185


===============================================================================
C031-WIP186 — QUEUE 004 CVPLAYER.CPP — F465 / KI#788 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F465 / KI#788 is CONFIRMED.
F466 / provisional KI#789 is UNUSED.
No repository source files modified.


F465 / KI#788 — PERMANENT ALLIANCE TEAM-MEMBER REASSIGNMENT DOES NOT REFRESH
VASSAL-CITY MAINTENANCE, LEAVING BOTH ALLIES' REAL GOLD COSTS STALE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current Permanent Alliance + Vassal States gameplay reaches the
defect. The root is inherited from stock BtS/Firaxis and remains in Civ4CE, K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT CONTRACT
-------------
City number-of-cities maintenance is cached in `CvCity::m_iMaintenance`; each city
refresh contributes its delta to the owner's cached `CvPlayer::m_iTotalMaintenance`.

Current `CvCity::calculateNumCitiesMaintenanceTimes100` includes a master team's
vassal-city burden and divides that burden among the CURRENT number of master-team
members:

    int iNumVassalCities = 0;
    for (PlayerIter<ALIVE,VASSAL_OF> it(kOwner.getTeam()); it.hasNext(); ++it)
        iNumVassalCities += it->getNumCities();

    iNumVassalCities /=
        std::max(1, MemberIter::count(kOwner.getTeam()));

    int iMaintenanceCities =
        kOwner.getNumCities() + iExtraCities +
        iNumVassalCities / 2;

Thus a change in the number of players on a master team changes every member's
correct number-of-cities maintenance even if:
* no city changes owner;
* the vassal relationship itself does not change;
* no city population or local maintenance modifier changes.

The maintenance is NOT dynamically recomputed when queried. `CvPlayer::doGold`
ultimately uses:

    calculatePreInflatedCosts()
        -> ... + getTotalMaintenance() + ...

and `getTotalMaintenance()` reads the stored player total. `CvCity::doTurn` does not
perform an unconditional maintenance refresh.

THE NORMAL VASSAL TRANSITION ALREADY PROVES THE INVALIDATION REQUIREMENT
------------------------------------------------------------------------
`CvTeam::setVassal` explicitly refreshes every member of the MASTER team at the end of
a vassal-state change:

    for (MemberIter it(eMaster); it.hasNext(); ++it)
        it->updateMaintenance();

The stock BtS implementation has the same refresh (expressed as a player loop over the
master team). K-Mod retains it.

That is authoritative evidence that master-side cached maintenance must be rebuilt
when the set/share of vassal cities relevant to the formula changes.

PERMANENT ALLIANCE CHANGES THE SAME FORMULA INPUT WITHOUT INVALIDATING THE CACHE
--------------------------------------------------------------------------------
A Permanent Alliance is implemented by `CvDeal` calling `CvTeam::addTeam`; the lower
team ID survives.

`CvTeam::addTeam` eventually reassigns every absorbed player:

    if (GET_PLAYER((PlayerTypes)i).getTeam() == eTeam)
        GET_PLAYER((PlayerTypes)i).setTeam(getID());

`CvPlayer::setTeam` changes:
* old/new team member counts;
* alive / ever-alive counts;
* team city/population/land totals;
* leader and attitude state.

It does NOT call `updateMaintenance`.

`CvTeam::addTeam` likewise contains no maintenance refresh after the reassignment.

The cleanest case needs no vassal migration at all:

    Team A / Player A = lower-ID surviving singleton team
    Team B / Player B = absorbed singleton team
    Team C            = already a vassal of Team A

When B joins A:
* C remains a vassal of the SAME surviving Team A;
* therefore no `setVassal` transition for C is needed to repair the cache;
* Team A's member count changes from 1 to 2 through B's `setTeam`;
* that directly changes the divisor in
  `calculateNumCitiesMaintenanceTimes100`;
* A's old city caches are not refreshed;
* B's old city caches are not refreshed.

A should now pay only its per-member share of A's vassal burden, while B should begin
paying its own share. Instead both retain their pre-alliance values.

CURRENT SHIPPED REACHABILITY
----------------------------
Both required systems are normal visible game options:

    GAMEOPTION_PERMANENT_ALLIANCES
        bVisible = 1

    GAMEOPTION_NO_VASSAL_STATES
        bDefault = 0
        bVisible = 1

Therefore a player can enable Permanent Alliances while leaving Vassal States active.

Current SAS enables Permanent Alliance trading through:

    TECH_GAME_THEORY
        bPermanentAllianceTrading = 1

`CvPlayer::canPossiblyTradeItem(TRADE_PERMANENT_ALLIANCE)` requires each alliance
partner team itself not to be a vassal and to have one member. It does NOT prohibit
either partner from being the MASTER of one or more vassal teams.

So a singleton master with vassals can normally form a Permanent Alliance with another
singleton non-vassal team.

DETERMINISTIC CURRENT-SAS NUMERIC REPRO
---------------------------------------
Use:
* Standard world;
* Noble handicap;
* eight civilization players ever alive on Standard, matching the current recommended
  player count of 8, so AdvCiv's crowdedness factor is exactly 1;
* no number-of-cities maintenance modifier;
* no WLTKD / no-maintenance suppression in the example cities;
* Team A and Team B each have five population-10 cities;
* Team C, already a vassal of Team A, has ten cities;
* Team A has the lower team ID and therefore survives the PA.

Current XML gives:

    Standard NumCitiesMaintenancePercent = 30
    Noble    NumCitiesMaintenancePercent = 75

For a population-10 city the base multiplicative factor is therefore:

    (10 + 17) / 18 * 30/100 * 75/100
        = 27/18 * 3/10 * 3/4
        = 27/80
        = 0.3375

The current AdvCiv/SAS city-count cap is far above all counts in this construction
(`MAX_CITY_COUNT_FOR_MAINTENANCE = 31` with the Standard divisor), so the cap does not
mask the difference.

BEFORE THE PA:

Team A has one member, so its ten vassal cities are:

    10 / 1 = 10 vassal cities per member

K-Mod/AdvCiv then half-counts them:

    10 / 2 = 5 effective maintenance cities

Player A's city-count input is therefore:

    own 5 + vassal share 5 = 10

Each population-10 A city has this component proportional to:

    10 * 27/80 = 3.375 gold

Player B has no vassal burden:

    own 5 + 0 = 5

so each B city has:

    5 * 27/80 = 1.6875 gold

AFTER B JOINS A:

Team A now has TWO members. The correct shared vassal input is:

    10 / 2 members = 5
    5 / 2 (reduced-vassal rule) = 2

So EACH member should use:

    own 5 + vassal share 2 = 7

and each population-10 city should have:

    7 * 27/80 = 2.3625 gold

Thus, before local rounding:
* each A city should FALL by 1.0125 gold of number-of-cities maintenance;
* across five A cities that is about 5.06 pre-inflation gold/turn;
* each B city should RISE by 0.675 gold;
* across five B cities that is about 3.38 pre-inflation gold/turn.

Current code performs neither refresh at alliance formation. A stays cached near its
old higher value; B stays cached near its old lower value.

IMMEDIATE REAL-GOLD CONSEQUENCE
-------------------------------
The error does not have to survive for many turns to matter.

A human Player A can accept/form the Permanent Alliance during its ordinary active-turn
input phase. `CvDeal::addTrades` calls `CvTeam::addTeam` immediately.

When A then ends the turn, `CvPlayer::doTurn` executes:

    AI_assignWorkingPlots();
    ...
    doGold();
    doResearch();
    ...
    for each city:
        city->doTurn();

`doGold()` therefore consumes `getTotalMaintenance()` BEFORE A's city turns. There is
no unconditional player/city maintenance rebuild between the alliance merge and this
economic charge.

So A can be charged the stale pre-PA maintenance on the very first economy processing
after the alliance. B later reaches the same problem in its own `doGold`.

The stale values can persist beyond that until a specific maintenance-invalidating
event occurs (population change, government-center/maintenance modifier transition,
corporation/religion/occupation/WLTKD-related refresh, whole-player
`updateMaintenance`, etc.). Maintenance is not refreshed merely because another turn
passes.

ANCESTRY
--------
This root is Firaxis/BtS-inherited.

Stock BtS/Civ4CE `CvCity::calculateNumCitiesMaintenanceTimes100` already:
* sums cities belonging to teams vassalized to the owner team;
* divides them by `GET_TEAM(getTeam()).getNumMembers()`.

Stock BtS also explicitly calls `updateMaintenance()` for the master team's players at
the end of `CvTeam::setVassal`.

But stock `CvTeam::addTeam` reassigns absorbed players with `CvPlayer::setTeam` and
contains no subsequent maintenance refresh, and stock `CvPlayer::setTeam` itself has
none.

K-Mod retains the same invalidation omission. Its 04/sep/10 change reduces the vassal
burden from full count to half count, but the team-member divisor and PA cache problem
already existed in BtS.

Base AdvCiv 1.14 retains the member-count-dependent formula and the PA omission.
Current SAS retains both.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album and current Known Issues were searched for Permanent Alliance +
maintenance, vassal maintenance + alliance, team-member maintenance and related terms.
No same-root numbered finding exists.

Nearby confirmed PA roots are independent:
* F455 / KI#778 — city military-happiness unit cache not recounted after PA team
  reassignment;
* F458 / KI#781 — player outside-unit/supply cache not recounted after PA team
  reassignment;
* earlier PA roots cover relationship history, espionage state, visibility, UWAI,
  deals and other team-keyed state.

F465 specifically concerns cached CITY MAINTENANCE / player total maintenance changing
because the vassal-city burden is divided by current master-team member count.

The existing maintenance roots are also distinct:
* F421 / KI#744 — corporation maintenance cache embeds stale inverse inflation over
  time;
* F399/F400 and other earlier roots concern maintenance UI/valuation/decomposition,
  not PA team-member invalidation.

CONSERVATIVE REPAIR
-------------------
After Permanent Alliance player reassignment is complete, rebuild maintenance for the
members of the surviving merged team:

    for (MemberIter it(getID()); it.hasNext(); ++it)
        it->updateMaintenance();

Placement should be after all absorbed players have received the surviving team ID so
`MemberIter::count(getID())`, owner team, and vassal enumeration all reflect the final
merged identity.

This also naturally repairs both directions:
* original survivor members lose the excess burden caused by the old smaller
  denominator;
* absorbed members acquire the vassal burden associated with their new master team.

A regression test should:
1. create singleton master A with vassal C;
2. create singleton B with no vassals;
3. record A/B `getTotalMaintenanceTimes100`;
4. form an A+B Permanent Alliance;
5. compare cached totals immediately against a forced `updateMaintenance()` result;
6. assert that the PA path itself leaves no difference.

NUMBERING
---------
F465 / KI#788 is CONFIRMED.
F466 / provisional KI#789 is the next unused root.

CURRENT STONE AFTER C031-WIP186
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F465 / KI#788 CONFIRMED.
F466 / provisional KI#789 UNUSED.
Latest physical checkpoint: C031-WIP186.
No repository source files modified.

NEXT
----
Continue from F466 unused. While the relationship/cache seam is hot, finish the
remaining `CvPlayer` cached economic values whose formulas depend on team membership,
master/vassal identity or current team-member count; compare every such formula against
`setTeam` / `CvTeam::addTeam` invalidation. Then return to the mechanically ranked
low-coverage authoritative player lifecycle/state functions.

Keep current-data reachability, ordinary caller consequence, full cache/callee chain,
ancestry and duplicate proof mandatory before promotion.

END C031-WIP186


===============================================================================
C031-WIP187 — QUEUE 004 CVPLAYER.CPP — F466 / KI#789 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F466 / KI#789 is CONFIRMED.
F467 / provisional KI#790 is UNUSED.
No repository source files modified.

POST-F465 CONTROL — PERMANENT ALLIANCE TRADE-ROUTE YIELDS ARE REFRESHED THROUGH
THE `updatePlotGroups` CALLEE CHAIN
-------------------------------------------------------------------------------
A neighboring cache initially looked like another PA sibling.

`CvCity::totalTradeModifier` treats a route as foreign when the two cities' teams
differ and then adds:
* the city foreign-trade-route modifier;
* AdvCiv's peace-duration foreign-trade modifier.

A PA immediately changes old A<->B routes from foreign-team to same-team, while the
route cities and `m_aiTradeYield` are cached. `CvPlayer::setTeam` and `CvTeam::addTeam`
do not visibly call `updateTradeRoutes` at the reassignment site.

The full callee chain closes the candidate:

    CvTeam::addTeam
        -> GC.getGame().updatePlotGroups()
            -> each alive CvPlayer::updatePlotGroups()
                -> verifyCityProduction()
                -> updateTradeRoutes()

This runs after the absorbed players have their new team ID. Thus route selection and
trade yield are rebuilt under the merged same-team identity before the next economic
processing. No F466 from trade-route caching.

This control further isolates F465: `updatePlotGroups` does not rebuild maintenance.


F466 / KI#789 — VASSAL CITY COUNT CHANGES DO NOT INVALIDATE THE MASTER PLAYERS'
CACHED NUMBER-OF-CITIES MAINTENANCE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current vassal founding, conquest and city loss can reach the
defect. This is inherited from stock BtS/Firaxis. K-Mod changes the size of the vassal
burden but retains the missing invalidation; Base AdvCiv 1.14 and current SAS inherit
it.

ROOT CONTRACT
-------------
The same maintenance formula underlying F465 also depends on the TOTAL NUMBER OF
CITIES owned by the player's living vassals:

    int iNumVassalCities = 0;
    for (PlayerIter<ALIVE,VASSAL_OF> it(kOwner.getTeam()); it.hasNext(); ++it)
        iNumVassalCities += it->getNumCities();

    iNumVassalCities /=
        std::max(1, MemberIter::count(kOwner.getTeam()));

    int iMaintenanceCities =
        kOwner.getNumCities() + iExtraCities +
        iNumVassalCities / 2;

Current city maintenance is cached. Each `CvCity::updateMaintenance()` changes the
owner's stored `m_iTotalMaintenance`; `CvPlayer::calculatePreInflatedCosts()` later
reads `getTotalMaintenance()` rather than recomputing every city.

Therefore any vassal city-count change that changes `iNumVassalCities / members / 2`
must invalidate the MASTER players' city-maintenance caches.

NORMAL CITY CREATION / DESTRUCTION DOES NOT DO THAT
----------------------------------------------------
Current `CvCity::init` performs:

    getArea().changeCitiesPerPlayer(getOwner(), 1);
    GET_TEAM(getTeam()).changeNumCities(1);
    GC.getGame().changeNumCities(1);
    ...
    kOwner.updateMaintenance();

The whole-player refresh is only for the city OWNER.

Current `CvCity::kill` similarly performs:

    GET_TEAM(eOwner).changeNumCities(-1);
    GC.getGame().changeNumCities(-1);
    ...
    kOwner.updateMaintenance();

again refreshing only the player who lost the city.

`CvTeam::changeNumCities` itself is only:

    m_iNumCities += iChange;
    FAssert(getNumCities() >= 0);

It has no master-team maintenance invalidation.

A whole-tree inventory of `updateMaintenance()` callers finds no generic hook that
refreshes a vassal's MASTER when the vassal city count changes. `CvPlayer::acquireCity`
also supplies no extra master refresh: the old city is killed and the replacement is
initialized through the same owner-local paths.

THE EXISTING `setVassal` REFRESH CONFIRMS THE DEPENDENCY
--------------------------------------------------------
When a team first becomes or ceases to be a vassal, `CvTeam::setVassal` explicitly
refreshes the master members:

    for (MemberIter it(eMaster); it.hasNext(); ++it)
        it->updateMaintenance();

That correctly initializes/removes the vassal-city burden at the relationship
transition.

The missing sibling is what happens AFTERWARD: as the already-established vassal
founds, captures, loses or razes cities, the numerator used by the master maintenance
formula changes but the master caches are left untouched.

CURRENT SHIPPED REACHABILITY
----------------------------
Vassal States are active by default unless the visible
`GAMEOPTION_NO_VASSAL_STATES` option is selected.

No Permanent Alliance, scenario-only setup, Python mutation or debug action is needed.
Ordinary examples include:
* a voluntary or capitulated vassal founding a new city;
* a vassal conquering a city;
* a vassal losing a city to a third party;
* a vassal razing or otherwise losing a city.

Current K-Mod/AdvCiv's reduced-vassal rule half-counts the per-member vassal city
total, so not every +/-1 city changes the final integer input. Crossing the relevant
integer boundary does.

DETERMINISTIC CURRENT-SAS NUMERIC REPRO
---------------------------------------
Use:
* Standard world;
* Noble handicap;
* eight civ players ever alive on Standard so AdvCiv's crowdedness factor is exactly
  1;
* singleton master Team A / Player A;
* singleton vassal Team C / Player C;
* A owns ten population-10 cities;
* C owns nine cities and then founds or captures its tenth;
* no number-of-cities maintenance modifier and no WLTKD/no-maintenance suppression in
  A's example cities.

Current XML:

    Standard NumCitiesMaintenancePercent = 30
    Noble    NumCitiesMaintenancePercent = 75

For each population-10 A city:

    rNumCitiesFactor
        = (10 + 17) / 18 * 30/100 * 75/100
        = 27/80
        = 0.3375

The current city-count cap is not reached in this construction.

BEFORE C GAINS ITS 10TH CITY:

    vassal cities / master members = 9 / 1 = 9
    reduced-vassal contribution    = 9 / 2 = 4
    A maintenance-city input       = own 10 + 4 = 14

AFTER C GAINS ITS 10TH CITY, the CORRECT input is:

    10 / 1 = 10
    10 / 2 = 5
    own 10 + 5 = 15

So each A population-10 city should gain:

    1 * 27/80 = 0.3375 gold

of number-of-cities maintenance before inflation.

Across A's ten cities the master player's correct pre-inflation maintenance rises by
about:

    10 * 0.3375 = 3.375 gold/turn

before the engine's hundredth/whole-gold rounding.

But the new vassal city calls only C's `updateMaintenance()`. A's ten existing city
caches remain at the old 14-city input and `m_iTotalMaintenance` stays stale.

The reverse 10 -> 9 vassal-city loss produces an analogous overcharge for A.

REAL ECONOMIC CONSEQUENCE / PERSISTENCE
---------------------------------------
`CvPlayer::doTurn()` calls:

    doGold();
    doResearch();
    ...
    each owned city -> doTurn();

There is no unconditional `updateMaintenance()` at player-turn or city-turn start.

For a deterministic sequential-turn case, let lower-ID master A finish its turn and
then let later-turn vassal C found the tenth city. Nothing refreshes A's maintenance.
On A's next turn, when A eventually ends the turn, `doGold()` can consume the stale
master total before any of A's city turns.

The stale value can survive indefinitely in a sufficiently stable master empire; it
is repaired only when some unrelated maintenance invalidator happens to touch the
master's cities (population change, maintenance modifier, capital/government-center
change, religion/corporation/occupation/WLTKD state, explicit whole-player refresh,
etc.).

Thus this is not merely a display delay. It directly changes stored expenses and
gold.

ANCESTRY
--------
Stock BtS/Civ4CE already has the essential root.

Its number-of-cities formula:
* sums cities belonging to vassal teams;
* divides them by master-team member count;
* then counts the FULL resulting vassal share in number-of-cities maintenance.

Stock `CvTeam::setVassal` explicitly refreshes the master players when the
relationship changes.

But stock city creation/destruction refreshes only the city owner, stock
`CvTeam::changeNumCities` has no maintenance invalidation, and the master is not
refreshed when an established vassal's city total changes.

K-Mod 04/sep/10 reduces the vassal burden by half:

    own cities + iNumVassalCities / 2

which means a single city change matters only when it crosses the integer half-count
boundary. It does not add the missing master invalidation.

Base AdvCiv and current SAS retain the same structure.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album and current Known Issues were searched for vassal city-count
maintenance, vassal expansion/loss maintenance, and related wording. No same-root
numbered finding exists.

F465 / KI#788 is a sibling but independent:
* F465 changes the DENOMINATOR (`master team member count`) through Permanent Alliance;
* F466 changes the NUMERATOR (`total vassal city count`) through ordinary vassal city
  lifecycle.

Fixing only PA `addTeam` does not repair normal vassal expansion/loss. Fixing only the
vassal-city lifecycle does not necessarily repair a master-team member-count change.

Existing maintenance findings such as F421 concern other cache dependencies
(corporation inverse inflation) and are not duplicates.

CONSERVATIVE REPAIR
-------------------
When the number of cities on a team changes and that team is currently a vassal,
refresh the current master team's players.

A centralized direction is `CvTeam::changeNumCities`, after changing the stored count:

    if (iChange != 0 && isAVassal())
    {
        for (MemberIter it(getMasterTeam()); it.hasNext(); ++it)
            it->updateMaintenance();
    }

This naturally covers city founding, capture and loss regardless of which high-level
city lifecycle path produced the count change.

Care is still needed around initialization / bulk transitions to avoid unnecessary
work or calling into partially initialized state. A guarded helper or explicit
post-city-change hook can provide the same invariant if initialization timing makes
the centralized form undesirable.

A regression test should:
1. establish A as master of C;
2. record A's cached `getTotalMaintenanceTimes100`;
3. make C cross a half-count boundary by founding/capturing a city;
4. compare A's cached total immediately against the result after a forced
   `A.updateMaintenance()`;
5. assert there is no difference on the repaired path;
6. repeat for vassal city loss.

NUMBERING
---------
F466 / KI#789 is CONFIRMED.
F467 / provisional KI#790 is the next unused root.

CURRENT STONE AFTER C031-WIP187
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F466 / KI#789 CONFIRMED.
F467 / provisional KI#790 UNUSED.
Latest physical checkpoint: C031-WIP187.
No repository source files modified.

NEXT
----
Continue from F467 unused. Finish the remaining dependencies of cached maintenance and
other cached economic player values on vassal/master/team state, now including the
vassal city lifecycle, and determine whether any other normal city/team transition
changes an input without invalidation. Then return to the mechanically ranked
low-coverage authoritative state/lifecycle functions.

Require current SAS reachability, actual stored-state consequence, full callee chain,
ancestry and duplicate proof before promotion.

END C031-WIP187


===============================================================================
C031-WIP188 — QUEUE 004 CVPLAYER.CPP — F467 / KI#790 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F467 / KI#790 is CONFIRMED.
F468 / provisional KI#791 is UNUSED.
No repository source files modified.


POST-F466 CONTROL — COLONY CREATION DOES NOT CHANGE THE FINAL
`CivPlayersEverAlive` MAINTENANCE CROWDEDNESS FACTOR
-------------------------------------------------------------------------------

AdvCiv's number-of-cities maintenance also contains a global crowdedness term based on
`GC.getGame().getCivPlayersEverAlive()`. That initially suggested a much broader cache
root: founding a colony appears to create another civilization and could therefore
change the correct number-of-cities maintenance for every city in the world without a
world-wide maintenance refresh.

The complete colony initialization chain closes that candidate.

`CvGame::addPlayer` initializes/recycles the player through `CvPlayer::initInGame`,
whose reset path clears the recycled player's old `m_bEverAlive` state. When
`setAlive(true)` subsequently runs, a non-parent major civilization can therefore
temporarily increment `m_iCivPlayersEverAlive`.

But `CvPlayer::splitEmpire` calls `setParent(getID())` immediately after `addPlayer`,
before inheriting techs and before any city is transferred to the new colony.
`setParent` deliberately removes colony players from `CivPlayersEverAlive`, undoing
that temporary increment.

Thus:
* existing cities were cached under the same final crowdedness count that applies
  after colony creation;
* the new colony owns no cities during the temporary +1 state;
* recycled defeated slots follow the same reset -> temporary +1 -> parent decrement
  sequence.

No F467 from the global crowdedness factor.


F467 / KI#790 — K-MOD ANARCHY-MODIFIER TIMER RESCALING BECOMES SINGULAR AT
-100%, CREATING NEGATIVE OR 101-TURN REVOLUTION/CONVERSION TIMERS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current AdvCiv-SAS gameplay reaches the defect through its two
shipped -100% anarchy-modifier wonders. One direction stores a NEGATIVE authoritative
cooldown and fires a debug assertion; the reverse direction can inflate the minimum
1-turn cooldown to 101 turns. This is a K-Mod regression inherited by Base AdvCiv and
SAS.

ROOT
----
Current `CvPlayer::changeAnarchyModifier` contains K-Mod's proportional rescaling:

    changeRevolutionTimer(getRevolutionTimer() * iChange /
            std::max(1, 100 + getAnarchyModifier()));
    changeConversionTimer(getConversionTimer() * iChange /
            std::max(1, 100 + getAnarchyModifier()));
    changeAnarchyTurns(getAnarchyTurns() * iChange /
            std::max(1, 100 + getAnarchyModifier()));

    m_iAnarchyModifier += iChange;

For ordinary modifiers above -100, the intended algebra is sensible. If an old timer
`t` was scaled by `100 + oldModifier`, then changing the modifier by `iChange` adds:

    t * iChange / (100 + oldModifier)

which approximates:

    t * (100 + oldModifier + iChange) / (100 + oldModifier).

But the engine's civic/religion cooldowns are explicitly CLAMPED to a positive minimum
even when `100 + getAnarchyModifier()` reaches zero:

    getMinTurnsBetweenRevolutions()
        = max(1, (100 + modifier) * MIN_REVOLUTION_TURNS / 100)

and religion conversion similarly uses `max(1, ...)`.

At `oldModifier == -100`, the true proportional denominator is zero. K-Mod substitutes
`std::max(1, 0) == 1`, treating the one-turn minimum sentinel as though it represented
a normal 1% scale. A +/-100 modifier transition therefore applies +/-100 TIMES the
entire current timer.

Worse, current SAS allows modifiers below -100 by simple addition. With both current
sources active, `m_iAnarchyModifier == -200`; there is no clamp on the stored modifier.

CURRENT SHIPPED DATA MAKES THE SINGULARITY ORDINARY
----------------------------------------------------
A whole-current-XML scan finds exactly two nonzero `iAnarchyModifier` building values:

    BUILDING_PYRAMID       iAnarchyModifier = -100
    BUILDING_VERSAILLES    iAnarchyModifier = -100

For BOTH current SAS buildings:

    ObsoleteTech   = NONE
    bTeamShare     = 1
    iConquestProb  = 100

Pyramids requires Mining; Versailles requires Feudalism.

Therefore a singleton player can ordinarily own one source (-100), later acquire the
other (-200), or own both and lose one (-100). No Python, WorldBuilder, malformed XML
or disabled option is required.

The inherited stock constants are:

    MIN_REVOLUTION_TURNS = 5
    MIN_CONVERSION_TURNS = 5

SAS does not override those stock values.

DETERMINISTIC NEGATIVE-TIMER REPRO — ORDINARY CONQUEST
-------------------------------------------------------
This is the strongest reproduction because it needs no city-trade timing assumptions.

Setup:
* Player A is a singleton team and owns Pyramids.
* Player B owns Versailles in a city that A can capture.
* A's `getAnarchyModifier()` is therefore -100 before the attack.
* Both wonders are current SAS 100%-retained conquest buildings.

On A's active turn:

1. A changes to any different legal civic.

   `getCivicAnarchyLength` multiplies the base anarchy by:

       max(0, 100 + (-100)) / 100 = 0

   so the civic switch causes ZERO anarchy.

   `CvPlayer::revolution` then sets:

       revolutionTimer =
           iAnarchyLength + getMinTurnsBetweenRevolutions()
         = 0 + max(1, 0 * 5 / 100)
         = 1.

2. Before ending the same no-anarchy turn, A captures B's Versailles city.

3. `CvPlayer::acquireCity` snapshots the old buildings, kills the old city and creates
   the new city. Because Versailles has `iConquestProb = 100`, it is restored in the
   captured city through `setNumRealBuildingTimed`.

4. `CvCity::setNumRealBuildingTimed` calls `processBuilding`; because Versailles is
   TeamShare, the owning player receives `CvPlayer::processBuilding`.

5. `CvPlayer::processBuilding` executes:

       changeAnarchyModifier(-100);

   while A's OLD modifier is still -100 and revolution timer is still 1.

6. K-Mod's rescaling computes:

       delta =
           1 * (-100) / max(1, 100 + (-100))
         = -100 / 1
         = -100

       new revolution timer =
           1 + (-100)
         = -99

   and only afterward changes the modifier itself to -200.

`setRevolutionTimer(-99)` stores the negative value and then executes:

    FAssert(getRevolutionTimer() >= 0);

so a debug DLL trips the assertion on completely ordinary gameplay.

RELEASE-BUILD CONSEQUENCE
-------------------------
The release build does not clamp the stored timer.

The normal player-turn decrement is:

    if (getRevolutionTimer() > 0)
        changeRevolutionTimer(-1);

so -99 is NEVER incremented/decremented back toward zero. It is persistent until some
later operation explicitly overwrites/rescales the timer.

Civic eligibility likewise tests only:

    if (isAnarchy() || getRevolutionTimer() > 0)
        return false;

A negative timer therefore behaves as if the cooldown has expired. Immediately after
the capture, the player can satisfy the timer portion of `canRevolution` despite having
changed civics earlier in the SAME active turn.

The religion path is identical. With a 1-turn conversion timer under -100, acquiring
the second -100 source can store `m_iConversionTimer == -99`; `canConvert` rejects only
when `getConversionTimer() > 0`, and the normal decrement likewise ignores negatives.

Thus F467 is simultaneously:
* a reachable debug assertion;
* corruption of a serialized authoritative timer into an invalid negative state;
* a civic/religion cooldown bypass in release builds.

DETERMINISTIC 101-TURN REVERSE DIRECTION
----------------------------------------
The opposite transition shows that this is not merely an underflow issue.

Suppose A owns BOTH Pyramids and Versailles:

    old modifier = -200.

A changes civics. Anarchy is still zero and the revolution timer is again clamped to
1.

During the same active turn A then cedes/trades one eligible non-capital wonder city
to another civilization. City trading does not test the revolution timer.

When the old city is killed, removing one -100 TeamShare source calls:

    changeAnarchyModifier(+100)

while:

    old modifier = -200
    current timer = 1.

The denominator is again:

    max(1, 100 - 200) = 1

and the timer receives:

    +100.

It becomes:

    101

while the resulting player modifier becomes:

    -100.

But a player at -100 has a current minimum revolution cooldown of exactly 1. The code
therefore converts an active one-turn minimum into 101 turns even though the player
still has complete no-anarchy/-100 coverage.

This reverse case removes any ambiguity about what the timer should semantically be:
the final modifier is still -100, so a 101-turn cooldown is plainly inconsistent with
the same player's own `getMinTurnsBetweenRevolutions()` result of 1.

The same 1 -> 101 corruption applies to a current conversion timer.

BUILDING LIFECYCLE / CALLEE-CHAIN PROOF
---------------------------------------
The relevant city acquisition/loss paths process the player modifier synchronously.

`CvCity::kill` loops all buildings and calls:

    setNumRealBuilding(eLoopBuilding, 0);

`setNumRealBuildingTimed` calculates the building-count delta and calls:

    processBuilding(eBuilding, ...);

`CvCity::processBuilding` loops team members and, for a TeamShare building, calls:

    it->processBuilding(eBuilding, iChange, getArea());

`CvPlayer::processBuilding` contains:

    changeAnarchyModifier(kBuilding.getAnarchyModifier() * iChange);

For conquest, `CvPlayer::acquireCity` restores retained buildings in the new city.
Current Pyramids/Versailles have `iConquestProb=100`, so their acquisition side is
deterministic.

No delayed turn hook sits between the one-turn civic/religion cooldown being created
and a same-turn military capture. At -100, the civic/religion change itself creates no
anarchy, so normal unit actions remain available that turn.

ANCESTRY
--------
This root is introduced by K-Mod practical commit 683,
`8e3d6230340dbd99cee5226a672d817d93608a6c`,
dated 2012-04-12.

The commit message says:

    "CvPlayer::changeAnarchyModifier and CvPlayer::changeGoldenAgeModifier will now
     apply their respective adjustments to the existing anarchy / golden age at the
     time the modifier is changed."

It replaces the BtS timer adjustment with the current division through the OLD
`100 + getAnarchyModifier()` scale and adds equivalent adjustment for active anarchy
turns.

Stock BtS/Civ4CE does NOT contain the singular K-Mod formula. Its old code changes the
modifier first and then uses a simple `(100 + iChange)` multiplication for revolution
and conversion timers. That old code may have its own approximation issues (which is
why K-Mod replaced it), but it does not divide through the zero old factor and cannot
produce this -99/101 singularity.

K-Mod's current XML already has a normal -100 source in Cristo Redentor:
* `iAnarchyModifier = -100`;
* `bTeamShare = 1`;
* `iConquestProb = 100`;
* `ObsoleteTech = NONE`.

Base AdvCiv 1.14 inherits K-Mod's rescaling function and likewise has Cristo Redentor
at -100, so the zero-denominator defect is active in base AdvCiv too.

Current SAS inherits the function but moves/duplicates the no-anarchy effect onto
Pyramids and Versailles; Cristo itself is now 0. Having TWO independent -100 sources
makes the negative-timer acquisition reproduction especially direct in SAS.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains no numbered root for the -100 denominator singularity.

The earlier post-WIP170 closure sentence that said:

    "unit-cost multiplier, strike and anarchy modifier/rescaling paths remain
     internally coherent"

was too broad. That pass did not challenge the denominator at the now-shipped -100 /
stacked -200 edge. C031-WIP188 explicitly SUPERSEDES that particular closure for
`changeAnarchyModifier`.

F462 / KI#785 is related only in subject matter, not root cause:
* F462 sets a Force Civic victim's cooldown using the SPY OWNER's modifier;
* F467 corrupts an already-running timer when the SAME player's anarchy modifier
  changes through the -100 singularity.

Fixing either does not fix the other.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do NOT use `max(1, 100 + oldModifier)` as a mathematical substitute for a zero or
negative old scaling factor.

For old factors above zero, K-Mod's proportional rescaling can remain:

    oldScale = 100 + oldModifier;
    newScale = max(0, 100 + oldModifier + iChange);

    newTimer ~= oldTimer * newScale / oldScale;

The oldScale <= 0 case needs explicit semantic handling because the stored positive
timer is already a MINIMUM-CLAMPED value and cannot be inverted proportionally.

For revolution/conversion timers, a robust implementation should at minimum guarantee:
* timers never become negative;
* a transition that remains at <= -100 does not turn the 1-turn minimum into 101;
* when leaving <= -100 for a positive scale, a positive active cooldown is mapped to
  a bounded cooldown consistent with the NEW modifier's minimum rather than multiplied
  by 100.

One reasonable policy for the ordinary positive-active case is:
* update/snapshot the new modifier;
* if oldScale <= 0 and timer > 0, recompute the applicable minimum cooldown under the
  new modifier (or preserve 1 when newScale remains zero);
* keep timer 0 at 0;
* do not proportionally rescale `anarchyTurns` through a zero denominator. Under the
  normal -100 path there is no active civic/religion anarchy in the first place.

The exact policy for exotic Python/debug-created active anarchy at <= -100 can be
chosen separately; it should never rely on division by an invented denominator of 1.

REGRESSION TESTS
----------------
Current-SAS negative case:
1. A owns Pyramids and has `anarchyModifier == -100`.
2. A switches civic; assert revolution timer == 1 and anarchy == 0.
3. Before ending turn, A captures 100%-retained Versailles.
4. Assert revolution timer remains nonnegative and respects the intended cooldown;
   specifically it must NOT become -99.
5. Assert no debug failure.
6. Repeat with a religion conversion / conversion timer.

Reverse case:
1. A owns both Pyramids and Versailles (`anarchyModifier == -200`).
2. A switches civic; timer == 1.
3. During that active turn, remove/cede one -100 source through an ordinary supported
   city-transfer path.
4. Final modifier is -100.
5. Assert timer remains consistent with the -100 minimum (1), not 101.

NUMBERING
---------
F467 / KI#790 is CONFIRMED.
F468 / provisional KI#791 is the next unused root.


SISTER CONTROL — `changeGoldenAgeModifier` HAS THE SAME DENOMINATOR SHAPE BUT
CURRENT SAS CANNOT REACH ITS SINGULARITY
-------------------------------------------------------------------------------
K-Mod practical 683 also added:

    changeGoldenAgeTurns(getGoldenAgeTurns() * iChange /
            std::max(1, 100 + getGoldenAgeModifier()));

A zero/negative old Golden Age modifier could create the same mathematical problem.

A whole current SAS XML scan finds only one nonzero `iGoldenAgeModifier`:

    BUILDING_PYRAMID = +50

No current -100 or <= -100 Golden Age modifier source was found. Therefore the sister
formula cannot reach F467's singular denominator under shipped SAS data.

Preserve this as structural robustness debt. Do not assign F468 without an ordinary
current producer of a sufficiently negative Golden Age modifier.


CURRENT STONE AFTER C031-WIP188
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F467 / KI#790 CONFIRMED.
F468 / provisional KI#791 UNUSED.
Latest physical checkpoint: C031-WIP188.
No repository source files modified.

NEXT
----
Continue from F468 unused.

First finish the immediate modifier-rescaling siblings and timer state invariants while
F467 is hot (including other modifiers that rescale stored durations/counters and any
source that can cross zero/negative boundaries).

Then resume the mechanically ranked low-coverage authoritative CvPlayer functions:
player lifecycle/start/death/revival state, score/history counters, production/economic
mutators and normal-turn stored-state arithmetic.

Require current SAS reachability, full caller/callee consequence, ancestry and
duplicate proof before promotion. Preserve suspicious but masked/generic/API-only
defects as controls rather than manufacturing F468.

END C031-WIP188


===============================================================================
C031-WIP189 — QUEUE 004 CVPLAYER.CPP — F468 / KI#791 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F468 / KI#791 is CONFIRMED.
F469 / provisional KI#792 is UNUSED.
No repository source files modified.


POST-WIP188 CLOSURE — IMMEDIATE F467 SISTER / LOW-COVERAGE STATE PASS
--------------------------------------------------------------------
The post-F467 pass first closed several tempting but unproven siblings rather than
manufacturing F468.

1. `changeGoldenAgeModifier` has the same K-Mod proportional-rescaling denominator
shape as F467:

    changeGoldenAgeTurns(getGoldenAgeTurns() * iChange /
            std::max(1, 100 + getGoldenAgeModifier()));

but current SAS has only one nonzero shipped Golden Age modifier:

    BUILDING_PYRAMID = +50

No current source reaches an old Golden Age modifier of -100 or below, so F467's
zero/negative denominator singularity is unavailable under shipped data. Preserve as
structural robustness debt only.

2. `changeUnhealthyPopulationModifier` does not need a persistent assignment-dirty
cache repair. Current non-anarchy `CvPlayerAI::AI_assignWorkingPlots()` invokes city
assignment logic on every owned city each player turn, so citizen/specialist choices
are recomputed without relying solely on this mutator to set a dirty bit.

3. `changeWarWearinessModifier` is consumed dynamically by city anger. The team-level
war-weariness percentage and the player/city modifier are deliberately separate;
`CvCity::getWarWearinessPercentAnger` applies the current player modifier when queried.
No missing cache invalidation established.

4. Population, land, technology and wonder score transitions dirty/recompute their
normal score components; vassal score contributions are dynamically derived. Assets,
Power, cities-lost, barbarian wins and the unit/building-class count families sampled
here did not expose an independent missing transition.

5. `m_iNumUnitGoldenAges` and `m_iHighestUnitLevel` are intentionally cumulative /
historical state, not current-unit counts that should decrease.

6. The old `m_aiUpkeepCount` accessors are dead legacy state. Current civic upkeep is
calculated from active civics; no compiled gameplay caller was found for the obsolete
counter mutator.

7. Normal research-queue construction, pop/clear and team-tech acquisition keep
`m_abResearchingTech` synchronized with the queue. Generic API looseness did not
produce an ordinary current desynchronization.

8. `setCombatExperience` correctly loops through repeated Great General thresholds;
after spawning one Great General it uses the newly increased threshold for the next
iteration. Current SAS has the expected Great General unit mapping.

CONTROL — `isProductionMaxedBuildingClass(..., bAcquireCity=true)` /
`ExtraPlayerInstances`
--------------------------------------------------------------------------------
The acquisition-side max-count expression initially looks suspicious because passing
`ExtraPlayerInstances` as the requested extra count algebraically cancels the XML
allowance and effectively tests the existing count against `MaxPlayerInstances`.

Current SAS data resolves the intended special case. The only current positive
`iExtraPlayerInstances` entry is:

    BUILDINGCLASS_PALACE
        MaxPlayerInstances   = 1
        ExtraPlayerInstances = 1
        bNoLimit             = 1

The special allowance supports replacement-capital production semantics; acquisition
must not allow a captured second Palace simply because the production-side replacement
slack exists. No general current building class exposes a contradictory acquisition
case. Close; no F468 from this arithmetic.


F468 / KI#791 — ADVC WORKER-BUILD DECAY NEVER ADVANCES ON UNOWNED PLOTS,
SO NEUTRAL PARTIAL ROADS/FORTS CAN RETAIN INVESTED WORK FOREVER
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current worker actions on neutral land reach the defect. The
build-progress decay mechanic is an AdvCiv feature (`advc.011`), and its player-side
dispatcher has excluded unowned plots since the feature's first reachable
implementation. Base AdvCiv 1.14 and current SAS retain it. BtS/Civ4CE and K-Mod do not
have this AdvCiv decay system.

INTENDED RULE / AUTHORITATIVE DESCRIPTION
-----------------------------------------
Current `GlobalDefines_advc.xml` documents:

    DELAY_UNTIL_BUILD_DECAY = 8

as:

    "The number of turns that need to pass without Worker action in a plot before
     progress on unfinished Worker builds starts to decay. Recommended: 8, i.e.
     decay kicks in on the eighth turn without action."

AdvCiv's shipped hint is even more player-facing:

    "If you hold down [CTRL] while issuing a command to a Worker, the work will be
     suspended one turn before completion. Bear in mind though that progress on
     unfinished jobs begins to decay after 8 turns."

The reachable AdvCiv history/manual summary likewise states:

    "When a tile with unfinished worker builds is left alone for more than 7 turns,
     the invested worker turns begin to decay. This is mainly to disincentivize
     pre-chopping. [advc.011]"

None of these contracts restrict the mechanic to culturally owned plots.

ROOT
----
`CvPlayer::doTurn()` invokes decay at the end of each player's normal turn:

    if (GC.getDefineINT(CvGlobals::DELAY_UNTIL_BUILD_DECAY) > 0)
        decayBuildProgress();

But `CvPlayer::decayBuildProgress()` scans only LAND PLOTS OWNED BY THAT PLAYER:

    void CvPlayer::decayBuildProgress()
    {
        CvMap const& kMap = GC.getMap();
        for (int i = 0; i < kMap.numPlots(); i++)
        {
            CvPlot& p = kMap.getPlotByIndex(i);
            if (!p.isWater() && p.getOwner() == getID())
                p.decayBuildProgress();
        }
    }

An unowned plot has:

    p.getOwner() == NO_PLAYER

and therefore fails the ownership test for EVERY player's `decayBuildProgress()` call.

There is no separate map/game/global decay pass. A current whole-tree search finds the
plot-side `CvPlot::decayBuildProgress()` call only in this player dispatcher.

Consequently the plot's interruption counter never advances at all while the plot
remains unowned.

PLOT-SIDE STATE MAKES THE FAILURE PERSISTENT
--------------------------------------------
Worker progress is global plot state:

    m_aiBuildProgress[eBuild]

not progress owned by the worker or player.

Every real work action executes `CvPlot::changeBuildProgress`, which resets:

    m_iTurnsBuildsInterrupted = -1;

and returns immediately when the required build time has not yet been reached.

`CvPlot::decayBuildProgress()` is the ONLY routine in this feature that advances that
interruption state toward the configured delay:

    if (m_iTurnsBuildsInterrupted > NO_BUILD_IN_PROGRESS &&
        m_iTurnsBuildsInterrupted < DELAY_UNTIL_BUILD_DECAY)
    {
        m_iTurnsBuildsInterrupted++;
    }

Once the delay has been reached, it subtracts one point of every positive unfinished
build progress per decay call.

For an unowned plot, however, this function is never called:
* the interruption counter stays at -1 forever;
* `isBuildProgressDecaying()` never becomes true;
* stored build progress never loses even one point.

The partial-build hover uses the same plot state and therefore also never reaches its
decay warning for such a neutral build.

ORDINARY CURRENT REACHABILITY — NEUTRAL ROADS
----------------------------------------------
`CvPlot::canBuild` applies ownership restrictions to normal improvements, but the
Route branch has NO corresponding plot-ownership requirement. Provided technology /
route resource prerequisites are satisfied and the route is an upgrade over the
existing route, the route is a valid build on an unowned plot.

`CvUnit::canBuild` delegates through `CvPlayer::canBuild` to this plot legality and
adds only unit build-capability/domain checks. Workers can therefore build routes on
ordinary neutral land.

Current shipped data:

    BUILD_ROAD
        PrereqTech = TECH_THE_WHEEL
        iTime      = 200
        RouteType  = ROUTE_ROAD

    UNIT_WORKER
        iWorkRate  = 100
        BUILD_ROAD = enabled

On the ordinary Normal / Noble / Ancient combination:

    Noble iBuildTimePercent = 100
    Normal iBuildPercent    = 100
    Ancient iBuildPercent   = 100

and an ordinary human worker with no worker-speed modifier therefore contributes
exactly 100 work per working turn to a Road requiring exactly 200 work.

Deterministic reproduction:

1. Learn The Wheel.
2. Move an ordinary Worker onto an unowned land tile.
3. Spend one turn building Road.
4. The plot now has:
       BUILD_ROAD progress = 100
       required            = 200
       m_iTurnsBuildsInterrupted = -1.
5. Move/cancel the Worker and leave the neutral tile untouched.
6. End 8, 20, 100 or any number of later turns while the tile remains unowned.

Expected from `advc.011`:
* the no-work interruption counter reaches 8;
* on the eighth no-action decay pass, Road progress begins to decrease.

Current code:
* no player's decay dispatcher visits the plot;
* interruption state stays -1;
* Road progress stays 100 indefinitely.

A worker returning arbitrarily far in the future therefore completes the Road with one
more 100-work turn, preserving ancient neutral pre-building forever despite the
explicit decay rule.

ORDINARY CURRENT REACHABILITY — OUTSIDE-BORDERS FORTS
-------------------------------------------------------
This is not limited to routes.

Current `IMPROVEMENT_FORT` has:

    bOutsideBorders = 1

and current `BUILD_FORT` has:

    PrereqTech      = TECH_ENGINEERING
    iTime           = 1000
    ImprovementType = IMPROVEMENT_FORT

For `bOutsideBorders` improvements, `CvPlot::canBuild` explicitly allows construction
when:

    ePlotTeam == NO_TEAM

while forbidding construction inside someone else's culture.

Thus a Worker can normally begin a long multi-turn Fort on neutral land. Any
interrupted partial Fort progress on that still-unowned plot is subject to exactly the
same permanent non-decay.

The current feature is therefore observably broken for two normal supported neutral
build families, not merely for a Python-created or malformed build.

ANCESTRY
--------
This is an AdvCiv feature-integration bug.

BtS/Civ4CE and K-Mod do not contain:
* `CvPlayer::decayBuildProgress`;
* `CvPlot::decayBuildProgress`;
* `m_iTurnsBuildsInterrupted`;
* `DELAY_UNTIL_BUILD_DECAY`.

The first reachable AdvCiv implementation in practical history
(commit f6ff6a182f5187541fff6e5bbbe12f9573b3778c, initial AdvCiv history continuation)
adds `CvPlayer::decayBuildProgress` with the same core restriction:

    if (!p.isWater() && p.getOwnerINLINE() == getID())
        p.decayBuildProgress();

So the unowned-plot hole dates to the original AdvCiv decay dispatcher.

A later AdvCiv commit:
    ce5a750feace3514d4c8e089e5c8a8d8ac60bc4e
    "Refactor build progress decay"
    2020-07-15

rewrites/refines the `CvPlot`-side test/decay implementation and says:
    "(Not quite sure if my old code was correct either.) [advc.011]"

but does not change the owner-only `CvPlayer` dispatcher. The neutral-plot omission
therefore survives the 2020 refactor.

Base AdvCiv 1.14 still contains:

    if(!p.isWater() && p.getOwner() == getID())
        p.decayBuildProgress();

and current SAS inherits it unchanged apart from surrounding style/refactors.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album was searched for:
* `decayBuildProgress`;
* `DELAY_UNTIL_BUILD_DECAY`;
* unfinished Worker build decay;
* neutral/unowned worker build progress.

No numbered root owns this dispatcher omission.

Current Known Issues contains an earlier SAS Worker-AI issue (KI#296) involving
abandoned partial Farm progress being mistaken for an actively assigned future Farm.
That root explicitly relies on the normal rule that interrupted progress is retained
for eight turns before decay begins. It does NOT concern unowned plots whose decay
timer never advances at all.

F468 is independent: the engine's AdvCiv decay mechanism itself is skipped forever
when the partial build resides on an unowned plot.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do NOT simply remove:

    p.getOwner() == getID()

from the existing per-player loop. Doing so would call `p.decayBuildProgress()` once
for every living player turn, accelerating decay by player count.

Build progress is plot-global state, so the clean invariant is one decay update per
GLOBAL game turn for every relevant plot with interrupted progress, regardless of
owner.

A conservative design is to move the map scan to a global game/map turn hook that runs
exactly once per full game turn:

    for each map plot:
        if (!plot.isWater())
            plot.decayBuildProgress();

The exact phase can be chosen to preserve current owned-plot timing as closely as
possible.

If keeping the existing per-player owner pass for owned plots, then unowned plots need
one separately designated once-per-global-turn ownerless pass. A global unified pass
is simpler and less brittle.

Performance can also be improved later by maintaining a set/list of plots with
`m_iTurnsBuildsInterrupted > NO_BUILD_IN_PROGRESS`, but that is optional; correctness
comes first.

REGRESSION TEST
---------------
Current simple Road case:

1. Normal speed, Noble handicap, Ancient start.
2. Human Worker with work rate 100 begins `BUILD_ROAD` on neutral land.
3. After one worker turn:
       progress == 100 / required 200.
4. Move the worker away.
5. Advance seven no-work decay turns:
       progress remains 100.
6. On the eighth no-work decay turn:
       expected progress == 99.
7. Continue one more:
       expected progress == 98.
8. Verify the same behavior whether the plot is:
       a) owned by the worker player;
       b) unowned.
9. Verify decay still occurs only once per global turn in multiplayer/multi-civ games.

Also test a neutral `BUILD_FORT` to cover the `bOutsideBorders` improvement path.

NUMBERING
---------
F468 / KI#791 is CONFIRMED.
F469 / provisional KI#792 is the next unused root.

CURRENT STONE AFTER C031-WIP189
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F468 / KI#791 CONFIRMED.
F469 / provisional KI#792 UNUSED.
Latest physical checkpoint: C031-WIP189.
No repository source files modified.

NEXT
----
Continue from F469 unused.

First finish the immediate worker-build-decay lifecycle siblings:
* ownership changes while a partial build is interrupted;
* water/domain exclusions;
* whether multiple unfinished BuildTypes on one plot share the interruption timer in a
  way that can suppress or accelerate decay incorrectly;
* save/load initialization of `m_iTurnsBuildsInterrupted`.

Then return to the mechanically ranked low-coverage authoritative CvPlayer state:
PBEM/turn activation, delayed Golden Age/strike/revival/death lifecycle, production
and economic stored-state mutators.

Require ordinary current SAS reachability, concrete authoritative consequence,
ancestry and duplicate proof before promotion. Preserve generic/API-only or
data-masked defects as controls rather than manufacturing F469.

END C031-WIP189


===============================================================================
C031-WIP190 — QUEUE 004 CVPLAYER.CPP — F469 / KI#792 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F469 / KI#792 is CONFIRMED.
F470 / provisional KI#793 is UNUSED.
No repository source files modified.


POST-F468 SIBLING CONTROLS — BUILD-PROGRESS DECAY
--------------------------------------------------
The immediate siblings of F468 were traced before leaving the worker-build decay
family.

1. WATER EXCLUSION IS CURRENTLY DATA-MASKED / HARMLESS.

`CvPlayer::decayBuildProgress` skips water plots as well as unowned plots. A whole
current `CIV4BuildInfos.xml` check shows the ordinary water-worker BuildInfos
(Fishing Boats, Whaling Boats and Offshore Platform) use `iTime = 0` and complete
immediately rather than leaving multi-turn partial `m_aiBuildProgress`.

No ordinary current partial water Worker build was found whose decay is suppressed by
the water test. Preserve the structural limitation only; do not assign F469.

2. THE INTERRUPTION TIMER IS DELIBERATELY PLOT-WIDE, NOT PER BUILDTYPE.

`m_iTurnsBuildsInterrupted` is one value per `CvPlot` while unfinished work itself is
stored per BuildType. Any subsequent Worker action on the plot resets the shared
interruption timer.

The shipped define/help contract consistently says "without Worker action in a plot"
and describes a tile with unfinished Worker builds being "left alone." That supports a
tile-level inactivity clock: working any job on the tile means the tile has not been
left alone. No independent per-BuildType timer contract was established.

3. SAVE/LOAD IS SYMMETRIC.

Current `CvPlot::write` and `CvPlot::read` serialize
`m_iTurnsBuildsInterrupted` symmetrically. No current-version persistence mismatch.

4. OWNERSHIP ACQUISITION IS THE SAME F468 ROOT, NOT A NEW ONE.

If neutral partial work later comes inside culture, the existing owner-based player
dispatcher finally begins calling `CvPlot::decayBuildProgress`. The interruption timer
starts advancing only then from its still-stale pre-decay state.

That is a consequence of F468's ownerless-dispatch omission rather than an independent
root.


CONTROL — SAS `CvPlayer::read` REMOVAL OF HISTORICAL `uiFlag` MIGRATION BRANCHES
IS AN EXPLICIT SAVE-COMPATIBILITY POLICY, ALREADY OWNED BY KI#46
-------------------------------------------------------------------------------
A serialization pass compared current SAS `CvPlayer::read/write` against Base AdvCiv.

Base AdvCiv retains numerous historical `uiFlag` migrations for old save layouts.
Current SAS reads/writes its current layout directly and has intentionally removed much
of that compatibility machinery.

This is not a same-version serialization defect:
* current SAS writer and reader agree on the present field order/version;
* SAS documentation explicitly states that savegame compatibility across mod versions
  is generally not supported;
* current Known Issues already owns the cleanup as KI#46 and explains that the old
  `uiFlag` compatibility code was intentionally removed because SAS does not need to
  support those old historical save formats.

Do not reopen unsupported old-save compatibility as F469 unless current writer/reader
symmetry itself breaks.


LOW-COVERAGE TURN/LIFECYCLE CONTROLS
------------------------------------
The next mechanically ranked tranche also closed several thin state families:

* `doCaptureGold` uses the longstanding base/population/random capture-gold formula and
  scales it by turns since the city's last acquisition to prevent rapid-recapture
  exploitation; no new arithmetic contradiction established.
* PBEM turn-activation setters update their normal active-player/game counters.
* scheduled Golden Ages are applied from the existing turn-activation path and their
  serialized counter remains symmetric; earlier Taj/delayed-GA timing work already owns
  the known fragile seams.
* strike persistence remains intentional policy from the earlier Queue-004 closure.
* no new authoritative consequence was established from the remaining thin score/
  history/count wrappers in this tranche.


F469 / KI#792 — ADVC ALIVE-ONLY TEAM-MEMBER MAINTENANCE DIVISOR CHANGES WHEN A
MASTER-TEAM PLAYER IS ELIMINATED, BUT SURVIVING MASTER MEMBERS' CACHED
MAINTENANCE IS NOT REFRESHED
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current team-game + Vassal States gameplay reaches the defect.
The stale-cache condition is introduced by AdvCiv's 2020 AgentIterator refactor:
stock BtS/Civ4CE and K-Mod divide vassal maintenance by the team's persistent
`getNumMembers()` count, whereas AdvCiv replaces that with alive-only
`MemberIter::count`. The formula therefore begins changing on individual player death,
but `CvPlayer::setAlive(false)` never gains a maintenance invalidation for surviving
teammates. Base AdvCiv 1.14 and current SAS retain the mismatch.

ROOT CONTRACT
-------------
Current `CvCity::calculateNumCitiesMaintenanceTimes100` computes the master player's
vassal-city burden as:

    int iNumVassalCities = 0;
    for (PlayerIter<ALIVE,VASSAL_OF> it(kOwner.getTeam()); it.hasNext(); ++it)
        iNumVassalCities += it->getNumCities();

    iNumVassalCities /=
        std::max(1, MemberIter::count(kOwner.getTeam()));

    int iMaintenanceCities =
        kOwner.getNumCities() + iExtraCities +
        iNumVassalCities / 2;

`MemberIter` is explicitly defined as:

    PlayerIter<ALIVE, MEMBER_OF>

in `AgentIterator.h`.

So, unlike the stock/K-Mod `getNumMembers()` divisor, current AdvCiv's divisor is the
number of LIVING players on the master team.

When one member of a multi-player master team is eliminated while another member
survives:
* the team's vassal relationship can remain in force;
* `MemberIter::count(masterTeam)` immediately decreases;
* every surviving master member's correct number-of-cities maintenance can increase;
* their city maintenance and player `m_iTotalMaintenance` remain cached.

PLAYER DEFEAT UPDATES THE ALIVE ITERATOR IMMEDIATELY
-----------------------------------------------------
`CvPlayer::setAlive(false)` sets:

    m_bAlive = false;

then changes the team's alive count and calls:

    GC.getAgents().playerDefeated(getID());

`CvAgents::playerDefeated` explicitly removes the dead player from:

    memberSeqCache(MEMBER_ALIVE, eTeam)

which is the cache used by `MemberIter`.

Thus a later forced maintenance recomputation sees the smaller divisor immediately.

But neither `CvPlayer::setAlive(false)` nor `CvAgents::playerDefeated` calls
`updateMaintenance()` for the surviving members of the dead player's team.

TEAM-LEVEL VASSAL RELATIONSHIP NEED NOT END
-------------------------------------------
This is not masked by `killAllDeals()`.

Team agreements are represented through member-pair proxy deals. `CvDeal::startTeamTrade`
creates the team item between every alive member pair of the two teams.

For a two-player master team A0+A1 and a vassal C, ordinary vassal state therefore has
at least:
* an A0 <-> C proxy;
* an A1 <-> C proxy.

When A1 is eliminated, `CvPlayer::killAllDeals()` kills only deals involving A1.

For a vassal/surrender item whose participant has just died, `CvDeal::endTrade`
computes:

    bAlive =
        GET_PLAYER(eFromPlayer).isAlive() &&
        GET_PLAYER(eToPlayer).isAlive();

and in the TRADE_VASSAL / TRADE_SURRENDER branch:

    if (!bAlive)
        break;

So it deliberately does NOT call `CvTeam::setVassal(..., false, ...)` for that dead
proxy.

The surviving A0<->C team-trade proxy remains, and the team-level vassal relationship
remains active. This matches the obvious gameplay requirement that eliminating one
member of a still-living master team does not release all its vassals.

Therefore the formula genuinely transitions from a 2-living-member divisor to 1 while
the same vassal cities still contribute.

NO MAINTENANCE INVALIDATION ON PLAYER ELIMINATION
--------------------------------------------------
`CvPlayer::setAlive(false)` performs substantial cleanup:
* capital/unit cleanup;
* team alive-count update;
* AgentIterator cache update;
* war-weariness refresh for enemies;
* research/popups/cities/deals cleanup;
* turn deactivation;
* UI/replay/UWAI state.

It does not refresh maintenance for surviving teammates.

The eliminated player's final city loss likewise does not repair them:
`CvCity::kill` refreshes the CITY OWNER's maintenance, not all members of that owner's
team or their master/vassal siblings.

The existing F466 master-maintenance invalidation is about a VASSAL team's city count;
here the changed input is the MASTER team's number of living members.

DETERMINISTIC CURRENT-SAS REPRO
-------------------------------
Use:
* Standard world;
* Noble handicap;
* eight civ players ever alive on Standard so AdvCiv's crowdedness factor is exactly 1;
* master Team A containing two living players, A0 and A1;
* singleton vassal Team C with ten cities;
* A0 owns five population-10 cities;
* A1 is about to lose its final city;
* no number-of-cities maintenance modifier and no WLTKD/no-maintenance suppression in
  A0's example cities.

Current XML again gives:

    Standard NumCitiesMaintenancePercent = 30
    Noble    NumCitiesMaintenancePercent = 75

For a population-10 city:

    factor =
        (10 + 17) / 18 *
        30 / 100 *
        75 / 100
      = 27 / 80
      = 0.3375

BEFORE A1 IS ELIMINATED:

    living master members = 2
    vassal cities per member = 10 / 2 = 5
    reduced-vassal burden    = 5 / 2 = 2
    A0 maintenance-city input = own 5 + 2 = 7

AFTER A1 IS ELIMINATED, CURRENT FORMULA'S CORRECT INPUT IS:

    living master members = 1
    vassal cities per member = 10 / 1 = 10
    reduced-vassal burden    = 10 / 2 = 5
    A0 maintenance-city input = own 5 + 5 = 10

Difference:

    +3 effective maintenance cities.

For each of A0's population-10 cities:

    +3 * 27/80
      = +81/80
      = +1.0125 pre-inflation gold/turn.

Across five A0 cities:

    about +5.0625 pre-inflation gold/turn.

Current code leaves A0's city caches at the old 7-city input after A1's defeat.
A forced `A0.updateMaintenance()` immediately changes them to the 10-city input.

REAL ECONOMIC CONSEQUENCE
-------------------------
The stale total is authoritative state.

`CvPlayer::calculatePreInflatedCosts()` reads:

    getTotalMaintenance()

from the player's cached total, and `doGold()` consumes those costs.

There is no unconditional `updateMaintenance()` at the start of each player turn or
city turn.

A1 can therefore lose its final city and be eliminated during another player's turn;
A0's next economy processing can use the stale lower maintenance and undercharge the
surviving master player.

The stale state persists until some unrelated event happens to rebuild A0 maintenance.

CURRENT SHIPPED REACHABILITY
----------------------------
No Permanent Alliance, Python, WorldBuilder or malformed setup is required.

This needs only:
* a normal multi-player/team game with at least two players on a team;
* Vassal States enabled (default unless explicitly disabled);
* that team having a vassal;
* one master-team player being eliminated while another teammate survives.

The current maintenance code explicitly contains the multi-member divisor, so this is
within the intended supported team/vassal rules.

ANCESTRY
--------
Stock BtS/Civ4CE and K-Mod use:

    iNumVassalCities /=
        std::max(1, GET_TEAM(getTeam()).getNumMembers());

`CvTeam::getNumMembers()` is team membership, not alive-only membership; eliminating a
player does not remove that player from the team or change this value.

Therefore player death is NOT a formula-input transition in the stock/K-Mod version,
and no corresponding death-time maintenance invalidation is needed for this specific
divisor.

AdvCiv commit:

    360e6ee7b0b923d2a98576498f328c8043bc6091
    2020-01-04
    "Refactor CvCity using EnumMap, FOR_EACH_ENUM, AgentIterator"

mechanically refactors the old player/vassal loop and changes the divisor to:

    MemberIter::count(kOwner.getTeam())

`MemberIter` is alive-only.

The commit is described as a broad refactor/performance cleanup, not as a gameplay
change to vassal maintenance or dead teammate cost sharing. No new invalidation is
added to `CvPlayer::setAlive`.

Thus the refactor introduces a new dynamic dependency on player alive state while
leaving the old cache invalidation graph unchanged.

Base AdvCiv 1.14 and current SAS retain the alive-only divisor and the missing
elimination refresh.

Even if one regarded alive-only sharing as a desirable gameplay change, the cache bug
remains objective: the current formula and its cached value disagree after death until
an unrelated refresh occurs.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album was searched for:
* player elimination + maintenance;
* teammate death + maintenance;
* alive member maintenance;
* MemberIter maintenance divisor.

No existing numbered root owns this transition.

It is a sibling of, but distinct from:
* F465 / KI#788 — Permanent Alliance changes the master-team MEMBER divisor by team
  reassignment;
* F466 / KI#789 — ordinary vassal city lifecycle changes the vassal-city NUMERATOR;
* F469 — individual master-player death changes the alive-member divisor introduced
  by AdvCiv.

Fixing F465 at `CvTeam::addTeam` does not repair player elimination.
Fixing F466 at vassal city-count changes does not repair player elimination.

CONSERVATIVE REPAIR
-------------------
If the alive-only `MemberIter::count` semantics are intended, then whenever a player
dies or revives on a still-living team whose members can have vassal-city maintenance,
the surviving/current team members need maintenance refreshed after the alive-member
iterator cache has been updated.

A narrow death-side direction in/after `setAlive(false)` is:

    for (MemberIter it(getTeam()); it.hasNext(); ++it)
        it->updateMaintenance();

with appropriate initialization/teardown guards.

Revival should be checked symmetrically because inserting a revived member into
`MEMBER_ALIVE` increases the divisor and should reduce the burden for existing
teammates.

A more centralized design would invalidate team-member-dependent maintenance whenever
the alive membership of a team changes.

Alternatively, if the 2020 refactor was NOT intended to change stock/K-Mod semantics,
restore a divisor based on persistent team member count. That is a gameplay-policy
decision; regardless, cached maintenance must remain consistent with whichever formula
is chosen.

REGRESSION TEST
---------------
1. Create master team A with living players A0 and A1.
2. Give A a ten-city vassal C.
3. Give A0 five population-10 cities.
4. Record A0 `getTotalMaintenanceTimes100`.
5. Eliminate A1 while A0 survives and C remains A's vassal.
6. Immediately compare A0's cached total with the result after an explicit
   `A0.updateMaintenance()`.
7. On repaired code there must be no difference.
8. Repeat by reviving A1 if runtime revival is supported: the burden should move in the
   opposite direction without needing an unrelated maintenance update.

NUMBERING
---------
F469 / KI#792 is CONFIRMED.
F470 / provisional KI#793 is the next unused root.

CURRENT STONE AFTER C031-WIP190
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F469 / KI#792 CONFIRMED.
F470 / provisional KI#793 UNUSED.
Latest physical checkpoint: C031-WIP190.
No repository source files modified.

NEXT
----
Continue from F470 unused.

First finish the immediate alive-membership symmetry:
* player revival on a multi-member master team;
* whether any other cached formula uses alive-only `MemberIter::count` and therefore
  needs death/revival invalidation;
* team alive-member changes affecting cached team/player economics outside maintenance.

Then return to the mechanically ranked low-coverage authoritative CvPlayer lifecycle
and economic functions, plus selected historical bug-fix seams whose repaired state
invariant may have an unpatched sibling.

Require current SAS reachability, concrete stored-state consequence, full caller/callee
chain, ancestry and duplicate proof before promotion.

END C031-WIP190


===============================================================================
C031-WIP191 — QUEUE 004 CVPLAYER.CPP — F470 / KI#793 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F470 / KI#793 is CONFIRMED.
F471 / provisional KI#794 is UNUSED.
No repository source files modified.


POST-F469 CONTROL — REVIVAL IS THE SAME ALIVE-MEMBER MAINTENANCE INVARIANT,
BUT NO SEPARATE ORDINARY PRODUCER EARNED F470
-------------------------------------------------------------------------------
F469's alive-only `MemberIter::count(masterTeam)` divisor changes symmetrically when
a dead team member is revived.

`CvAgents::playerRevived` reinserts the revived player into `MEMBER_ALIVE`, so the
formula's divisor increases and the surviving teammates' correct vassal-city
maintenance decreases.

However, current normal compiled gameplay has no clean independent ordinary revival
producer beyond special/recycled-player lifecycle territory already owned elsewhere:
* initial setup is not a midgame transition;
* `verifyAlive()` can revive a dead player that has externally regained a city/unit;
* ordinary city liberation enumerates living recipients;
* the malformed recycled-colony revival route is already part of F447/KI#770.

Preserve revival as the symmetric invalidation requirement of F469, but do not spend
a separate numbered root on WorldBuilder/debug/API-only revival.


CONTROL — `MemberIter::count` CACHED-ECONOMY DEPENDENCY IS OTHERWISE NARROW
----------------------------------------------------------------------------
A whole-source search for `MemberIter::count` found only the vassal-city maintenance
formula in `CvCity` plus dynamic AI/war utility consumers. No second cached economic
formula depending on alive team-member count was established.

This keeps F469 narrowly owned rather than suggesting a generic family of hidden
alive-member economic caches.


F470 / KI#793 — HUMAN-INVOLVED PERMANENT ALLIANCES NEVER REFRESH ADVC'S
AGENTITERATOR TEAM/MEMBER CACHES; LATER TEAM TECHS SKIP THE ABSORBED PLAYER'S
PERSISTENT PLAYER-LEVEL EFFECTS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current human-AI or human-human Permanent Alliance diplomacy
reaches the defect.

The root was introduced by AdvCiv's `advc.agent` AgentIterator cache system. AdvCiv
later added an explicit `allianceFormed()` full-cache rebuild to fix Permanent
Alliances, but wired that repair only into the AI-AI diplomacy branch after its direct
`implementDeal()` call. The generic `CvDeal` Permanent Alliance path and exported
`CvGame::implementDeal` path do not invoke the repair.

Base AdvCiv 1.14 and current AdvCiv-SAS retain the omission.

CACHE CONTRACT
--------------
`CvAgents::updateAllCachedSequences()` constructs persistent cached sequences including:

    memberSeqCache(MEMBER, eTeam)
    memberSeqCache(MEMBER_ALIVE, eTeam)
    teamSeqCache(CIV_ALIVE)
    teamSeqCache(MAJOR_ALIVE)
    ...

The per-team member caches are populated from each player's CURRENT `getTeam()`.

AgentIterator exact/superset forms then use those vectors as their source. In
particular:
* `MemberIter` / `MemberAIIter` use the per-team MEMBER/MEMBER_ALIVE cache;
* `PlayerIter<EVER_ALIVE,MEMBER_OF>` uses MEMBER as its superset and cannot discover
  a player absent from that team's cached vector;
* `TeamIter<CIV_ALIVE>` and `TeamIter<MAJOR_CIV>` rely on the corresponding cached
  alive-team sequences.

The caches therefore must be rebuilt or incrementally migrated whenever players move
between teams and an old team dies during a Permanent Alliance.

PERMANENT ALLIANCE REASSIGNS PLAYERS WITHOUT UPDATING `CvAgents`
-----------------------------------------------------------------
Current `CvTeam::addTeam` saves the absorbed team leader and eventually performs:

    for (int i = 0; i < MAX_PLAYERS; i++)
    {
        if (GET_PLAYER((PlayerTypes)i).getTeam() == eTeam)
            GET_PLAYER((PlayerTypes)i).setTeam(getID());
    }

`CvPlayer::setTeam` updates:
* old/new `CvTeam::m_iNumMembers`;
* old/new alive / ever-alive counts;
* team city/population/land totals;
* InitCore team identity;
* leader / attitude state.

It does NOT notify `CvAgents`.

`CvTeam::changeAliveCount`, which can drive the absorbed team's alive count to zero
during those `setTeam` calls, likewise does not update `CvAgents`' global team sequence
caches.

The incremental `CvAgents` API has handlers for:
* player defeat/revival;
* colony creation;
* vassal creation/removal.

There is no player-team reassignment handler.

The intended Permanent Alliance repair is instead:

    CvAgents::allianceFormed()
        -> updateAllCachedSequences();

But that hook is not part of `CvTeam::addTeam`, `CvDeal::addTradeItems`,
`CvGame::implementDeal`, or `CvGame::implementAndReturnDeal`.

THE HOOK IS CALLED ONLY FOR AI-AI DIPLOMACY
--------------------------------------------
The sole current call site is in `CvPlayerAI::AI_doDiplo`.

When the target player is NOT human, the AI directly implements the PA:

    kGame.implementDeal(getID(), ePlayer, weGive, theyGive);
    GC.getAgents().allianceFormed();
    break;

When the target player IS human, the same branch instead opens normal diplomacy:

    gDLL->beginDiplomacy(...);

and does NOT call `allianceFormed`.

If the human accepts, the actual transaction later reaches the DLL's generic/exported
deal implementation. `CvGame.h` exposes:

    DllExport void implementDeal(...);

and its implementation simply creates the `CvDeal`, calls `addTradeItems`, and lets
that deal call `CvTeam::addTeam`. There is no post-alliance AgentIterator rebuild.

The same applies to a human-initiated accepted deal: the generic deal path contains no
hook.

A whole-source search finds exactly one call to `allianceFormed()`: the AI-AI branch
above.

STATE IMMEDIATELY AFTER A HUMAN-INVOLVED PA
-------------------------------------------
Take singleton lower-ID Team A / Player A and singleton higher-ID Team B / Player B,
with both alive when the AgentIterator caches were last valid.

Before the PA:

    MEMBER[A]       = { A }
    MEMBER_ALIVE[A] = { A }
    MEMBER[B]       = { B }
    MEMBER_ALIVE[B] = { B }

After generic `CvTeam::addTeam(B)`:

    A.getTeam() == A
    B.getTeam() == A

and Team B's actual alive/member counts have fallen to zero.

But without `allianceFormed()`:

    MEMBER[A]       is still { A }
    MEMBER_ALIVE[A] is still { A }

    MEMBER[B]       is still { B }
    MEMBER_ALIVE[B] is still { B }

while the global CIV_ALIVE / MAJOR_ALIVE team caches can still contain the now-dead
absorbed Team B as well.

This is not merely a stale count returned once. Those vectors remain the source for
future AgentIterators until a full rebuild, normally game start/load, occurs.

DETERMINISTIC CURRENT-SAS GAMEPLAY CONSEQUENCE — LATER TECHNOLOGY SKIPS THE
ABSORBED PLAYER
-------------------------------------------------------------------------------
The cleanest narrow consequence is `CvTeam::processTech`.

When the merged Team A later acquires a technology, current SAS processes persistent
per-player technology effects through:

    for (PlayerIter<EVER_ALIVE,MEMBER_OF> it(getID()); it.hasNext(); ++it)
    {
        CvPlayer& kMember = *it;
        kMember.changeFeatureProductionModifier(...);
        kMember.changeWorkerSpeedModifier(...);
        kMember.changeTradeRoutes(...);
        kMember.changeExtraHealth(...);
        kMember.changeExtraHappiness(...);
        kMember.changeAssets(...);
        kMember.changePower(...);
        kMember.changeTechScore(...);
        ...
    }

Base AdvCiv 1.14 uses `MemberIter` for the same player-level processing. Both rely on
the stale per-team cache after a human PA.

Current SAS enables Permanent Alliances at:

    TECH_GAME_THEORY
        Era = ERA_MODERN
        bPermanentAllianceTrading = 1

Current `TECH_BIOENGINEERING` is a later Future-era technology with:

    iHealth = 3

and normal `CvTeam::processTech` applies that as:

    kMember.changeExtraHealth(3);

Ordinary reproduction:

1. Human Player B forms/accepts a Permanent Alliance with lower-ID singleton Player A
   after Permanent Alliance trading has been unlocked.
2. Team A survives; B is reassigned to Team A.
3. Because the deal was human-involved, no `CvAgents::allianceFormed()` call occurs.
4. Leave `TECH_BIOENGINEERING` unresearched until after the alliance.
5. The merged team later acquires Bioengineering normally.
6. Team A correctly gains the team technology.
7. `processTech` iterates the stale surviving-team member cache `{A}`.
8. Player A receives `+3` player ExtraHealth.
9. Player B is absent from the iterator and receives NO `+3` ExtraHealth despite being
   a living member of exactly the same team that owns the technology.

`CvCity::goodHealth` / bad-health calculations read the owner's current
`CvPlayer::getExtraHealth()` dynamically. B's cities therefore have three fewer health
points than A's cities solely because B was the absorbed side of a human-involved
Permanent Alliance.

The same skipped loop can omit other current/future technology player effects:
worker-speed modifier, trade routes, happiness, Assets, Power, TechScore and K-Mod
commerce/specialist-commerce modifiers when their XML values are nonzero.

Additional later member-iterator consumers are also exposed, but they are intentionally
not split into separate root numbers here. The missing PA cache rebuild is the shared
root.

PERSISTENCE / SAVE-LOAD DIAGNOSTIC
----------------------------------
A save/load AFTER the missed technology does not retroactively repair the player
effect.

`CvAgents::gameStart(bFromSaveGame)` does call `updateAllCachedSequences()`, so the
member iterator vectors become correct after loading.

But `CvPlayer` serializes the already-processed player fields directly, including:

    m_iWorkerSpeedModifier
    m_iExtraHealth
    m_iAssets
    m_iPower
    m_iTechScore
    ...

The load path reads those values; it does not replay `CvTeam::processTech` for every
already-known technology.

Therefore:
* the AgentIterator membership error itself self-repairs on reload;
* player-level tech effects that were skipped BEFORE the reload remain missing and can
  persist in the save.

This provides a useful diagnostic distinction: forming the PA and immediately
save/reloading before any affected later transition can mask the iterator bug; once an
authoritative per-player transition has already been skipped, the downstream state need
not self-heal.

ANCESTRY / INTENT
-----------------
The local reachable history makes the missed generic call unusually clear.

Practical AdvCiv commit 1816:
    6029d4205ce0e3c6206d816ee2d8b9a6c46442d5
    2019-11-15
    "AgentIterator class for looping over players and teams"
    [advc.agent]

introduced `CvAgents`, the cached member/team sequences and:

    void allianceFormed() // Tbd.: Not currently called
    {
        updateAllCachedSequences();
    }

So the original AgentIterator integration already recognized that a Permanent Alliance
would need a full cache rebuild, but initially had no call site.

Practical AdvCiv commit 1882:
    4d4b92d0c0c65fdbdf550d8b27f16112d07757b4
    2019-12-29
    "Fix some recently introduced problems with Perm. Alliances"

removes the "Not currently called" comment and adds the first/current call:

    g.implementDeal(...);
    GC.getAgents().allianceFormed();

However, the diff places it only in `CvPlayerAI::AI_doDiplo`'s AI-AI direct-deal branch.
It does not put the rebuild in the generic `CvDeal` or `CvTeam::addTeam` transition.

Base AdvCiv 1.14 still has exactly that architecture and exactly one call site.
Current SAS retains it.

This is therefore a partial integration repair: the history explicitly identifies
`allianceFormed()` as the cache rebuild needed after PAs, but only one transaction
producer was wired to it.

CURRENT SAS F216 INTERACTION
----------------------------
Current SAS has already repaired F216/KI#539 by moving the UWAI history merge BEFORE
the absorbed players' `setTeam` reassignment, so that UWAI merge uses the original
survivor-member boundary.

That fix does not repair F470. The AgentIterator caches still need to be rebuilt AFTER
the team reassignment for all subsequent game logic.

F465 RECONCILIATION — REMAINS CONFIRMED
----------------------------------------
F470 changes the immediate behavior of a HUMAN-involved PA enough that the earlier F465
write-up needs a scope refinement, not a retraction.

F465 says PA team-member changes alter the vassal-city maintenance divisor without
refreshing city/player maintenance caches.

AI-AI PAs execute:

    implementDeal(...)
    allianceFormed()

so their AgentIterator member count immediately becomes the correct merged value 2.
No maintenance refresh follows. The F465 stale-maintenance reproduction therefore
remains independently valid and ordinary on the AI-AI path.

Human-involved PAs additionally suffer F470, meaning the maintenance formula's
AgentIterator denominator can itself remain stale until a full cache rebuild. The two
roots can layer; neither fix subsumes the other.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains many PA roots, including:
* F216/KI#539 — UWAI merge recipient boundary after AgentIterator conversion;
* F455/KI#778 — military-happiness cache after PA;
* F458/KI#781 — outside-unit/supply cache after PA;
* F465/KI#788 — maintenance cache after PA member-count change.

No existing root covers the generic `CvAgents` team/member sequence cache remaining
pre-alliance specifically because `allianceFormed()` is called only for AI-AI deals.

The old F216 history is particularly useful as a negative control: it concerns WHEN
UWAI consumes membership during `addTeam`; F470 concerns the missing full AgentIterator
cache rebuild for all later consumers on human PA transactions.

CONSERVATIVE REPAIR
-------------------
The rebuild belongs to the generic Permanent Alliance transaction, not to one AI
producer.

A narrow repair is to call:

    GC.getAgents().allianceFormed();

immediately after the generic `CvTeam::addTeam` call in `CvDeal::addTradeItems`'s
`bAlliance` branch, and remove the AI-AI-only duplicate call from
`CvPlayerAI::AI_doDiplo`.

That guarantees identical cache repair for:
* AI-AI PAs;
* AI-human PAs accepted through diplomacy;
* human-initiated PAs;
* any other transaction reaching the normal deal implementation.

Alternatively, a carefully placed call inside `CvTeam::addTeam` after all absorbed
players have been reassigned can centralize the invariant even more strongly. Because
AgentIterator cache rebuilding can invalidate active iterator backing vectors, placement
should be reviewed against callers that may themselves be iterating agents; the current
AI path already rebuilds and immediately `break`s its diplomacy iteration.

Whichever location is chosen, the invariant is:

    once `CvTeam::addTeam` has returned to ordinary post-transaction gameplay,
    CvAgents MEMBER/MEMBER_ALIVE and alive-team caches must match current
    CvPlayer::getTeam / CvTeam::isAlive state.

REGRESSION TEST
---------------
1. Start with singleton Team A / Player A and singleton Team B / human Player B.
2. Ensure the AgentIterator caches are initialized normally.
3. Form a Permanent Alliance through the human diplomacy path.
4. Immediately assert:
       MemberIter::count(A) == 2
       MemberIter::count(B) == 0
       absorbed Team B is absent from CIV_ALIVE / MAJOR_ALIVE iteration.
5. Acquire `TECH_BIOENGINEERING` afterward.
6. Assert BOTH A and B receive the technology's `+3` player ExtraHealth.
7. Repeat with AI-AI PA to ensure behavior is identical and no double-cache rebuild
   side effect appears.
8. Save/load before and after the post-PA tech acquisition as diagnostic variants.

NUMBERING
---------
F470 / KI#793 is CONFIRMED.
F471 / provisional KI#794 is the next unused root.

CURRENT STONE AFTER C031-WIP191
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F470 / KI#793 CONFIRMED.
F471 / provisional KI#794 UNUSED.
Latest physical checkpoint: C031-WIP191.
No repository source files modified.

NEXT
----
Continue from F471 unused.

First finish the immediate F470 aftermath without multiplying the same cache omission
into artificial roots:
* identify any post-human-PA transition that can crash/assert because a stale member
  cache is later incrementally edited;
* distinguish those consequences from independent invalidation roots;
* verify whether cache rebuild on load plus serialized downstream state creates any
  additional one-way corruption not already covered by F470.

Then resume the mechanically ranked low-coverage authoritative CvPlayer lifecycle and
economic mutators, plus historical-fix sibling seams.

Require ordinary current SAS reachability, concrete authoritative consequence, full
caller/callee proof, ancestry and duplicate check before promotion.

END C031-WIP191


===============================================================================
C031-WIP192 — QUEUE 004 CVPLAYER.CPP — F471 / KI#794 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F471 / KI#794 is CONFIRMED.
F472 / provisional KI#795 is UNUSED.
No repository source files modified.


POST-F470 CONTROL — STALE AGENTITERATOR ASSERTS ARE CONSEQUENCES OF F470,
NOT NEW ROOTS
-------------------------------------------------------------------------------
The immediate F470 aftermath was checked without multiplying one missing cache rebuild
into artificial findings.

For example, after a human-involved Permanent Alliance the absorbed player can still be
absent from the surviving team's stale `MEMBER_ALIVE` vector. If that absorbed player
later dies before a full cache rebuild, `CvAgents::playerDefeated` tries to erase that
player from the CURRENT surviving-team member cache. In a debug DLL, the failed erase
can assert because the player was never inserted there after the PA.

That is a downstream consequence of F470's missing `allianceFormed()` rebuild, not an
independent invalidation root. Likewise, save/load repairs the AgentIterator vectors but
does not retroactively replay already-skipped serialized player-level effects; that
one-way persistence is already part of F470.


LOW-COVERAGE MUTATOR CONTROLS
-----------------------------
The first post-WIP191 low-coverage pass also closed several thin modifier families.

* `changeLevelExperienceModifier` could generically affect the XP threshold of existing
  units without revisiting their levels, but every current shipped trait has
  `iLevelExperienceModifier = 0`; no ordinary current producer was established.
* Great Person/General threshold/rate, hurry, worker speed, improvement-upgrade speed,
  military production, space production and city-defense player modifiers are consumed
  dynamically by their authoritative calculations rather than copied into a separate
  player->city/unit cache requiring bulk invalidation.
* `changeFreeBuildingCount` updates cities on the intended 0<->positive availability
  transition; multiple sources do not represent multiple free copies.
* the positive `ExtraPlayerInstances` acquisition algebra remains the Palace-only
  replacement-capital special case already closed in the prior tranche.

No F471 from these state holders.


F471 / KI#794 — ELIMINATING THE LAST PLAYER OF AN ENEMY TEAM LEAVES THE
SURVIVING ENEMIES' CACHED WAR-WEARINESS ANGER ACTIVE THROUGH ONE MORE FULL
CITY/ECONOMIC TURN
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current conquest reaches the defect.

The underlying stale-until-end-of-turn behavior is inherited from stock BtS/K-Mod.
AdvCiv explicitly attempted to repair war-weariness anger on player elimination, but
the repair runs after AdvCiv's dead-team war teardown and guards on `isAtWar`; the guard
is therefore false exactly when the final team member has been eliminated and the war
has actually ended.

Base AdvCiv 1.14 and current SAS retain this ineffective repair.

AUTHORITATIVE STATE
-------------------
`CvPlayer::updateWarWearinessPercentAnger()` computes and stores:

    m_iWarWearinessPercentAnger

from the player's CURRENT living enemy teams:

    for (TeamIter<MAJOR_CIV,ENEMY_OF> itEnemy(getTeam()); itEnemy.hasNext(); ++itEnemy)
    {
        iNewWarWearinessPercentAnger +=
            GET_TEAM(getTeam()).getWarWeariness(itEnemy->getID(), true) / 100;
    }

It then writes the new cached value.

Cities do not independently rederive the team war list. Their live anger reads this
stored player value:

    int CvCity::getWarWearinessPercentAnger() const
    {
        int iAnger = GET_PLAYER(getOwner()).getWarWearinessPercentAnger();
        ...
        return iAnger;
    }

So when a war ends, the player's cached base anger has to be refreshed before city
assignment/output if the end of that war should take effect immediately.

ADVC PLAYER-ELIMINATION REPAIR
------------------------------
Current `CvPlayer::setAlive(false)` contains an explicit attempted fix:

    // advc.001: CvTeam::makePeace does this, but here they've missed it.
    FOR_EACH_ENUM(CivPlayer)
    {
        CvPlayer& kWarEnemy = GET_PLAYER((PlayerTypes)eLoopCivPlayer);
        if (kWarEnemy.isAlive() && kWarEnemy.getID() != getID() &&
            !kWarEnemy.isMinorCiv() &&
            GET_TEAM(kWarEnemy.getTeam()).isAtWar(getTeam()))
        {
            kWarEnemy.updateWarWearinessPercentAnger();
        }
    }

The comment correctly identifies the contract: normal `CvTeam::makePeace` explicitly
refreshes the members of both former enemy teams after `setAtWar(false)`.

But the elimination repair is ordered too late.

DEAD-TEAM WAR TEARDOWN HAPPENS BEFORE THAT LOOP
-----------------------------------------------
Earlier in the SAME `CvPlayer::setAlive` function:

    m_bAlive = bNewValue;
    ...
    GET_TEAM(getTeam()).changeAliveCount(isAlive() ? 1 : -1);

When this is the LAST living player of the team,
`CvTeam::changeAliveCount(-1)` reaches `m_iAliveCount == 0` and AdvCiv explicitly tears
down every war:

    if (m_iAliveCount == 0)
    {
        for (...)
        {
            ...
            kLoopTeam.setAtWar(getID(), false);
            setAtWar(kLoopTeam.getID(), false);
            ...
        }
    }

So by the time control returns to `CvPlayer::setAlive(false)` and reaches the attempted
war-weariness loop:

    GET_TEAM(kWarEnemy.getTeam()).isAtWar(getTeam())

is already FALSE for every former enemy.

Therefore the only transition that actually requires the immediate refresh -- the
death of the final team member, which ends the war -- skips the refresh.

If a player dies while teammates remain alive, `isAtWar` can still be true and the
loop can run, but the team war itself has NOT ended, so recomputing the enemy's war
weariness does not solve the intended elimination case.

The AdvCiv repair is consequently ineffective for its stated purpose.

ORDINARY CONQUEST REACHES `setAlive(false)` IMMEDIATELY
--------------------------------------------------------
This does not depend on the later global `CvGame::verifyAlive` sweep.

At the end of `CvPlayer::acquireCity`, AdvCiv explicitly calls:

    GET_PLAYER(eOldOwner).verifyAlive();

with the comment that otherwise a dead AI can receive one more turn and even arrange a
vassal agreement.

When the old owner has just lost its last city, `verifyAlive()` normally sets
`bKill = true` once the civilization has founded its first city, and calls:

    setAlive(false);

So final-city conquest can execute the entire team-death/failed-refresh chain
synchronously in the CONQUEROR's active turn.

DETERMINISTIC CURRENT GAMEPLAY REPRO
------------------------------------
Use a normal sequential-turn game:

1. Player A is at war with singleton Team B / Player B.
2. A has accumulated enough war weariness against B that
   `A.getWarWearinessPercentAnger() > 0`; for the cleanest case B is A's only current
   major-civilization enemy.
3. During A's active turn, A captures B's final city.
4. `CvPlayer::acquireCity` immediately calls `B.verifyAlive()`.
5. B is eliminated:
       B.setAlive(false)
         -> Team B changeAliveCount(-1)
         -> Team B alive count becomes 0
         -> A<->B `isAtWar` is cleared.
6. B's `setAlive(false)` then reaches the AdvCiv war-enemy refresh loop.
7. The loop tests:
       Team A.isAtWar(Team B)
   which is now false.
8. A's cached `m_iWarWearinessPercentAnger` therefore remains at its PRE-ELIMINATION
   value even though A has no remaining enemy in this simple reproduction.

A forced:

    A.updateWarWearinessPercentAnger();

at this point would recompute from `TeamIter<MAJOR_CIV,ENEMY_OF>` and set the value to
zero.

ONE FULL CITY/ECONOMIC TURN USES THE STALE ANGER
-------------------------------------------------
The defect is stronger than a transient UI display.

When A ends that same active turn, current `CvPlayer::doTurn()` executes in this order:

    AI().AI_assignWorkingPlots();

    verifyGoldCommercePercent();
    doGold();
    doResearch();
    doEspionagePoints();

    for each city:
        city->doTurn();

    ...
    updateTradeRoutes();
    updateWarWearinessPercentAnger();

The routine refresh occurs only AFTER:
* citizen/specialist reassignment;
* gold/research processing;
* every owned city's `doTurn`.

Thus the city/economic phase immediately following the enemy's elimination still sees
the cached anger from a war that has already ended.

For a city where the stale percentage crosses an unhappy-citizen threshold, this can:
* leave citizens unnecessarily angry/unassigned;
* reduce yields and commerce for that city turn;
* alter starvation/growth and specialist/GPP processing;
* propagate into the player's same-turn gold/research results.

Only near the end of `CvPlayer::doTurn` is the cache finally recomputed under the now-
correct no-enemy state.

The same ordering exists in stock BtS and K-Mod: their routine war-weariness refresh
also comes after AI assignment, economy, and city turns.

OTHER-WAR NUANCE
----------------
If A remains at war with other teams, the correct post-elimination cache need not be
zero. The root still applies: it should immediately DROP the eliminated Team B's
contribution, but retains the old aggregate through the city/economic phase.

The single-enemy reproduction is simply the clearest observable case.

ANCESTRY
--------
Stock BtS/Civ4CE:
* `CvPlayer::setAlive(false)` does not perform an immediate enemy-player
  `updateWarWearinessPercentAnger()` refresh at all;
* its normal `CvPlayer::doTurn` refresh occurs after AI assignment, economy and city
  turns;
* stock `CvTeam::changeAliveCount` frees vassals when the team dies but does not carry
  AdvCiv's explicit war cleanup.

K-Mod retains that player-elimination behavior and the same late normal-turn refresh.

AdvCiv changes team elimination under `advc.003m` so a dead team explicitly stops
being at war:

    kLoopTeam.setAtWar(getID(), false);
    setAtWar(kLoopTeam.getID(), false);

AdvCiv also adds the `advc.001` player-elimination loop saying that `CvTeam::makePeace`
refreshes war-weariness anger and player death had missed it.

However, the refresh loop is placed AFTER `changeAliveCount`, and tests the already-
cleared `isAtWar` relationship. The attempted repair therefore cannot see the former
enemy when the final team member dies.

Base AdvCiv 1.14 and current SAS preserve this ordering.

This is best described as a stock/K-Mod stale-elimination problem with an ineffective
AdvCiv immediate-repair integration.

DUPLICATE / ALBUM CHECK
-----------------------
No reconciled numbered root owns this player-cache transition.

F093 / KI#415 is related only by topic:
* F093 concerns `CvTeam::doWarWeariness` no longer visiting DEAD teams after an
  AdvCiv iterator refactor, so the raw team-vs-team war-weariness history can freeze and
  matter if the dead team later revives.
* F471 concerns a LIVING former enemy player's cached
  `m_iWarWearinessPercentAnger` not being refreshed when team elimination ends the war,
  causing immediate ordinary city/economic effects.

No revival is required for F471.

F327 / KI#650 is an AI civic-valuation wrong-variable bug involving the current
war-weariness modifier; it does not own this cache invalidation.

CONSERVATIVE REPAIR
-------------------
Do not discover former enemies by asking `isAtWar` AFTER the dead team's war state has
already been cleared.

Two narrow approaches:

A. Snapshot former enemy teams/players before the final `changeAliveCount(-1)`, then
   refresh those surviving players after war teardown.

B. Centralize the invariant in the team-death branch:
   when `CvTeam::changeAliveCount` transitions to zero, collect the teams that were at
   war before `setAtWar(false)` and, after clearing the relationships, call
   `updateWarWearinessPercentAnger()` for their living members.

Option B tracks the actual relationship transition and is analogous to
`CvTeam::makePeace`, but placement must avoid AgentIterator/cache mutation hazards
during player death.

A broader but simpler player-side repair after final-team death is to refresh all
living major players; that is correct but more work than necessary.

The important invariant is:

    once team elimination has ended a war, every surviving former enemy's cached
    war-weariness anger must reflect the post-war enemy set BEFORE its next city/
    economy processing.

REGRESSION TEST
---------------
1. A and singleton B are at war; B is A's only enemy.
2. Accumulate positive A-vs-B war weariness and force/update A's cached anger so it is
   positive.
3. During A's active turn, capture B's final city.
4. Assert immediately after B's elimination:
       !TeamA.isAtWar(TeamB)
       A.getWarWearinessPercentAnger() ==
           value produced by A.updateWarWearinessPercentAnger()
   and, in the single-enemy case, the value is 0.
5. End A's turn and verify no city processes an extra turn with the old war anger.
6. Repeat with A still at war with Team C; the cache should retain C's contribution but
   drop B's immediately.
7. Repeat with a multi-player B team where only one member dies; because the war does
   not end, the B contribution should remain.

NUMBERING
---------
F471 / KI#794 is CONFIRMED.
F472 / provisional KI#795 is the next unused root.

CURRENT STONE AFTER C031-WIP192
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F471 / KI#794 CONFIRMED.
F472 / provisional KI#795 UNUSED.
Latest physical checkpoint: C031-WIP192.
No repository source files modified.

NEXT
----
Continue from F472 unused.

First finish the historical player-death / war-state sibling seam while F471 is hot:
* other cached player values that normal `makePeace` refreshes but team elimination
  bypasses;
* unit/plot/trade validity updates that rely on a normal peace/war transition;
* player-death cleanup ordering around `changeAliveCount`, `killCities`,
  `killAllDeals`, AgentIterator and attitude caches.

Then resume the historical-fix x low-coverage function ranking and remaining
authoritative lifecycle/economic mutators.

Require ordinary current SAS reachability, concrete gameplay consequence, full
caller/callee proof, ancestry and duplicate check before promotion.

END C031-WIP192


===============================================================================
C031-WIP193 — QUEUE 004 CVPLAYER.CPP — F472 / KI#795 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F472 / KI#795 is CONFIRMED.
F473 / provisional KI#796 is UNUSED.
No repository source files modified.


POST-F471 PLAYER-DEATH / LIFECYCLE CONTROLS
-------------------------------------------
The immediate player-death seam was continued before moving back to the historical
low-coverage ranking.

1. NORMAL-PEACE SIDE EFFECTS BESIDES WAR-WEARINESS ANGER DID NOT PRODUCE A NEW
   INDEPENDENT ROOT.

`CvTeam::makePeace` performs several network/unit/trade refreshes that are not invoked
through exactly the same path when a team disappears. In the final-team-death case,
however:
* AdvCiv deliberately kills the defeated player's units before clearing team war state
  so active blockades/plunder are lifted while the former at-war relation still exists;
* final-city conquest / city cleanup supplies the relevant ownership/plot updates;
* the dead team has no continuing city/trade network that makes a missing post-peace
  route/plot refresh independently consequential.

War-weariness anger remains distinctive because it is cached state owned by a LIVING
former enemy and AdvCiv's own `advc.001` comment explicitly identifies the missing
`makePeace` side effect. No F472 from the other peace siblings.

2. DEATH-SIDE GLOBAL ATTITUDE UPDATE ASYMMETRY CLOSES UNDER THE GAME UPDATE CADENCE.

`setAlive(true)` explicitly calls global `CvPlayerAI::AI_updateAttitudes()` because
rank-difference attitude between players other than the revived player can change.
`setAlive(false)` has no matching direct call.

Player elimination nevertheless ends with `CvGame::setScoreDirty(true)`.
`CvGame::update()` calls `updateScore()` on normal update slices; when score is dirty,
`updateScore()` rebuilds player ranks/scores and then calls the global
`CvPlayerAI::AI_updateAttitudes()`.

Thus survivor-vs-survivor rank-attitude caches are rebuilt through the existing score
update after elimination. No durable independent F472 was established.

3. `CvTeam::updateLeaderID()` DOES NOT CONSUME THE TEMPORARILY STALE ALIVE-MEMBER
   AGENTITERATOR CACHE.

`CvPlayer::setAlive(false)` changes `m_bAlive`, then calls
`GET_TEAM(getTeam()).updateLeaderID()` before `CvAgents::playerDefeated()` removes the
player from `MEMBER_ALIVE`. This ordering initially looked vulnerable.

`CvTeam::updateLeaderID()` performs a raw `MAX_PLAYERS` scan, filters each player by
the current team, and explicitly weights only `kPlayer.isAlive()`. It therefore sees
the just-updated false alive flag directly and does not rely on the stale
AgentIterator membership vector. Close.

4. FINAL-TEAM VASSAL CLEANUP DOES USE THE PRE-`playerDefeated()` MEMBER CACHE, BUT NO
   BAD INDEPENDENT CONSEQUENCE WAS ESTABLISHED.

`CvTeam::changeAliveCount` can free the dead team's vassals before
`CvAgents::playerDefeated()` removes the just-dead player from alive-member caches.
`CvTeam::setVassal` consequently still sees that member in its MemberIter operations.

That ordering is at least partly useful: relationship sight/unit/city cleanup can still
touch the dying participant before later `killCities` / `killAllDeals` cleanup. No
separate authoritative corruption was proven from this temporary window.


DUPLICATE CONTROL — GOLDEN-AGE READINESS UNITCLASS VS CONSUMPTION UNITTYPE
--------------------------------------------------------------------------
The K-Mod mismatch was reconciled against the earlier album before re-investigation.

`unitsGoldenAgeReady()` intentionally counts distinct `UnitClassTypes`, while
`killGoldenAgeUnits()` marks already-consumed kinds by `UnitTypes`.

This is already preserved in WIP158/WIP174/WIP179 as a latent data-extension defect.
Current SAS has exactly one Golden-Age-capable unit type for each relevant Great Person
class and no civilization mapping that creates two Golden-Age-capable UnitTypes in the
same UnitClass. Current shipped state therefore cannot make readiness and consumption
disagree.

Do not duplicate this as F472.


F472 / KI#795 — ADVANCED START ROUTE PURCHASE IGNORES ROUTE BONUS/RESOURCE
PREREQUISITES, ALLOWING RAILROAD WITHOUT COAL OR OIL
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current shipped Advanced Start gameplay reaches the defect.

The root is inherited from stock BtS/Firaxis through K-Mod and Base AdvCiv. AdvCiv
changed Advanced Start pricing and fixed a separate route-counting issue but retained
the route legality omission. Current AdvCiv-SAS retains it.

CURRENT NORMAL ROUTE LEGALITY
-----------------------------
Current `CvPlayer::canBuild` delegates to `CvPlot::canBuild` for plot/build legality.

For a route Build, `CvPlot::canBuild` validates both mandatory and OR bonus
prerequisites when `bTestVisible == false`:

    if (GC.getInfo(eRoute).getPrereqBonus() != NO_BONUS)
    {
        if (!isAdjacentPlotGroupConnectedBonus(ePlayer,
            GC.getInfo(eRoute).getPrereqBonus()))
        {
            return false;
        }
    }

    bool bFoundValid = true;
    for (int i = 0; i < GC.getInfo(eRoute).getNumPrereqOrBonuses(); i++)
    {
        bFoundValid = false;
        if (isAdjacentPlotGroupConnectedBonus(ePlayer,
            GC.getInfo(eRoute).getPrereqOrBonus(i)))
        {
            bFoundValid = true;
            break;
        }
    }
    if (!bFoundValid)
        return false;

Current `ROUTE_RAILROAD` has:

    iAdvancedStartCost = 18

    PrereqOrBonuses:
        BONUS_COAL
        BONUS_OIL

and current `BUILD_RAILROAD` requires:

    TECH_STEAM_POWER

Therefore an ordinary Worker may build Railroad only when:
* the player has Steam Power; AND
* the plot's adjacent plot-group/network can provide Coal OR Oil.

This is a real strategic-resource prerequisite, not merely help/UI metadata.

ADVANCED START VALIDATOR CHECKS ONLY THE BUILD TECHNOLOGY
---------------------------------------------------------
Current `CvPlayer::getAdvancedStartRouteCost` performs plot ownership/basic-terrain
checks and then:

    FOR_EACH_ENUM(Build)
    {
        if (GC.getInfo(eLoopBuild).getRoute() == eRoute)
        {
            if (!GET_TEAM(getTeam()).isHasTech(
                GC.getInfo(eLoopBuild).getTechPrereq()))
            {
                return -1;
            }
        }
    }

It never checks:
* `CvRouteInfo::getPrereqBonus()`;
* `CvRouteInfo::getPrereqOrBonus(...)`;
* `CvPlot::isAdjacentPlotGroupConnectedBonus(...)`;
* or `CvPlot::canBuild` through the matching route Build.

Thus Steam Power alone makes Railroad return a nonnegative Advanced Start price even
when the player has neither Coal nor Oil available to the plot/network.

EXECUTION DOES NOT REVALIDATE
-----------------------------
`CvPlayer::doAdvancedStartAction` handles a route purchase as:

    int iCost = getAdvancedStartRouteCost(eRoute, bAdd, pPlot);
    if (bAdd && iCost < 0)
        return;

    if (bAdd)
    {
        if (getAdvancedStartPoints() >= iCost)
        {
            pPlot->setRouteType(eRoute);
            changeAdvancedStartPoints(-iCost);
        }
    }

There is no subsequent `canBuild` or route-resource check.

Once the incomplete cost helper approves the purchase, `setRouteType` installs the
Railroad directly.

CURRENT SHIPPED REACHABILITY
----------------------------
Advanced Start is a normal visible option:

    GAMEOPTION_ADVANCED_START
        bDefault = 0
        bVisible = 1

Current start-era data supplies ordinary points, including:

    ERA_MODERN
        iAdvancedStartPoints = 7500

The normal game-setup tech initialization grants every technology from an era EARLIER
than the selected start era.

Current Steam Power is:

    TECH_STEAM_POWER
        Era = ERA_INDUSTRIAL
        iAdvancedStartCost = 100

Therefore a Modern-era Advanced Start begins with Steam Power already known.

Current AdvCiv's worker-build price modifier is:

    ADVANCED_START_WORKER_BUILD_MODIFIER = 67

and Normal-speed BuildPercent is 100. Railroad's base Advanced Start price 18 is
therefore cheap and normally purchasable once a city/owned plot exists.

DETERMINISTIC CURRENT-SAS REPRO
-------------------------------
1. Start a normal Custom Game with:
       GAMEOPTION_ADVANCED_START enabled
       Start Era = Modern
       a normal land map.
2. The player receives 7500 Advanced Start points and automatically knows Industrial-
   era Steam Power.
3. Establish the required first city / owned plot through the ordinary Advanced Start
   interface.
4. Use a position whose player/network has NO connected Coal and NO connected Oil.
5. On that owned non-city land plot compare:
       normal BUILD_RAILROAD legality
   against:
       getAdvancedStartRouteCost(ROUTE_RAILROAD, true, pPlot).

Normal Worker construction:
* fails `CvPlot::canBuild`, because neither Railroad OR-bonus prerequisite is connected.

Advanced Start:
* sees Steam Power;
* returns a nonnegative Railroad price;
* the UI/action accepts the purchase;
* `doAdvancedStartAction` directly executes
      pPlot->setRouteType(ROUTE_RAILROAD).

The player therefore owns a functional Railroad on a plot where its normal workers are
not legally allowed to build Railroad at all.

The same reproduction can be done in an earlier Advanced Start by purchasing Steam
Power first if sufficient points/prerequisites are available; Modern start simply
removes that extra setup variable.

PLAYER-FACING CONSEQUENCE
-------------------------
This bypasses a normal strategic-resource gate during game setup.

A player without Coal or Oil can spend Advanced Start points to construct Railroad
segments anyway, receiving the route's current movement/network behavior despite
failing the ordinary route requirement.

Because the action directly writes the route type, the illegally purchased Railroad
does not disappear when Advanced Start ends. It becomes normal map state for the game.

The AI Advanced Start code also calls the same route-cost helper and dispatches the
same `ADVANCEDSTARTACTION_ROUTE`, so the legality omission is not limited to direct
human Python/UI invocation. A human reproduction is sufficient for reachability.

ANCESTRY
--------
This is inherited from stock BtS.

Stock BtS/Civ4CE:
* `ROUTE_RAILROAD` already has Coal/Oil `PrereqOrBonuses`;
* normal `CvPlot::canBuild` checks route bonus prerequisites;
* `CvPlayer::getAdvancedStartRouteCost` checks the matching Build tech but does not
  check route bonus prerequisites;
* `doAdvancedStartAction` directly installs the route after the cost helper succeeds.

K-Mod 1.46 retains the same Advanced Start route validator/execution structure.

Base AdvCiv 1.14 retains the missing resource validation. Its changes include:
* Advanced Start route/improvement price tuning under `advc.250c`;
* the `advc.001` fix so `AdvancedStartCostIncrease` counts only the current player's
  routes instead of every route on the map.

Neither change adds the missing route bonus legality test.

Current SAS inherits the same omission while moving Railroad's Build prerequisite to
Steam Power; current Railroad still requires Coal OR Oil in `CIV4RouteInfos.xml`.

Thus F472 is best classified as a Firaxis/BtS Advanced Start legality bug inherited
through the lineage.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled C031 album and current Known Issues were searched for:
* Advanced Start Railroad;
* Advanced Start route bonus/resource;
* `getAdvancedStartRouteCost`;
* Railroad + Coal/Oil Advanced Start.

No existing numbered root owns this legality bypass.

The earlier Queue-004 `getBestRoute(NULL)` control is distinct:
* that helper deliberately supplies technologically unlocked automatic CITY routes and
  its resource omission was classified as inherited/intended city-route semantics;
* F472 is an explicit player purchase of a route on a normal plot through Advanced
  Start, where a normal Worker's route legality rejects the exact same state.

AdvCiv manual/history also records a different Advanced Start route bug:
`AdvancedStartCostIncrease` had counted routes/improvements owned by other players.
That was fixed by AdvCiv's owner filter and is not F472.

CONSERVATIVE REPAIR
-------------------
For an ADD operation on a concrete plot, validate through the matching route Build
rather than separately reproducing only part of normal Build legality.

Conceptually:

    bool bFoundBuild = false;
    FOR_EACH_ENUM(Build)
    {
        CvBuildInfo const& kBuild = GC.getInfo(eLoopBuild);
        if (kBuild.getRoute() != eRoute)
            continue;

        if (GET_TEAM(getTeam()).isHasTech(kBuild.getTechPrereq()) &&
            (pPlot == NULL || canBuild(*pPlot, eLoopBuild, false, false, true)))
        {
            bFoundBuild = true;
            break;
        }
    }
    if (!bFoundBuild)
        return -1;

Exact flags should preserve Advanced Start's intended visibility/setup semantics, but
the route's bonus prerequisites must be applied.

If multiple BuildInfos can produce one Route, the correct contract is that at least
one currently legal matching Build suffices. Do not reject a route merely because a
different alternate Build for the same route lacks its tech.

Removal/refund does not need to retroactively demand the resource; removing an already
present route should remain possible.

REGRESSION TEST
---------------
1. Modern Advanced Start, Steam Power known.
2. Own a city and adjacent owned land plot.
3. No connected Coal and no connected Oil.
4. Assert:
       normal canBuild(BUILD_RAILROAD) == false
       getAdvancedStartRouteCost(ROUTE_RAILROAD, true, plot) < 0.
5. Connect Coal or Oil to the appropriate plot group/network.
6. Assert:
       normal canBuild(BUILD_RAILROAD) == true
       Advanced Start Railroad cost is now nonnegative.
7. Purchase Railroad; verify points are charged and route is placed.
8. Test route removal/refund independently.
9. If alternate BuildInfos for one route are ever added, verify any one legal Build is
   sufficient.

NUMBERING
---------
F472 / KI#795 is CONFIRMED.
F473 / provisional KI#796 is the next unused root.

CURRENT STONE AFTER C031-WIP193
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F472 / KI#795 CONFIRMED.
F473 / provisional KI#796 UNUSED.
Latest physical checkpoint: C031-WIP193.
No repository source files modified.

NEXT
----
Continue from F473 unused.

First finish the Advanced Start sibling legality pass while F472 is hot:
* improvement purchase vs Build tech/feature-removal/bonus constraints;
* unit purchase resource/building prerequisites;
* building purchase prerequisite semantics;
* route removal/refund edge cases.

Do not multiply one shared incomplete-validator pattern into separate roots unless the
sibling has an independently enabled current-data consequence and requires a distinct
repair.

Then return to the historical-fix x low-coverage lifecycle/state ranking:
`initFreeState`, `initPlotGroup`, discovery/Great Person actions, production
verification and remaining stored-state/economic mutators.

Require current SAS reachability, concrete gameplay consequence, full caller/callee
chain, ancestry and duplicate proof before promotion.

END C031-WIP193


===============================================================================
C031-WIP194 — QUEUE 004 CVPLAYER.CPP — F473 / KI#796 UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F472 / KI#795 CONFIRMED.
F473 / provisional KI#796 remains UNUSED.
No repository source files modified.

CONTINUATION ANCHOR
-------------------
Continued directly from C031-WIP193 and reconciled the post-WIP193 conversation WIP
against the current 2026-09-01 SAS light source, base AdvCiv 1.14, K-Mod 1.46 and
Civ4CE material supplied for local comparison.

This tranche deliberately records closure and duplicate proof rather than forcing a
new root.

POST-WIP193 ADVANCED START SIBLING RECONCILIATION
--------------------------------------------------
1. Technology removal/refund remains the previously REJECTED F088 / KI#410 backend
   caveat, not F473.

The Advanced Start backend can compute technology-removal prices, and removing an
enabling technology after buying dependent state is therefore an API-level concern.
The shipped Tech Chooser path, however, only submits technology additions; the AI also
does not perform Advanced Start technology refunds. No ordinary current repository
producer exposes the removal action. Preserve the older F088 rejection.

2. Improvement, unit and building purchases remain materially better validated than
   F472's route purchase.

* Improvement purchase resolves a matching Build and enters normal `canBuild` legality.
* Unit purchase enters `canTrain` plus plot/unit placement legality.
* Building purchase enters `canConstruct`.

No independent current-data sibling bypass comparable to Railroad's missing route
bonus prerequisite was established.

3. `getAdvancedStartCityCost` and `getAdvancedStartCultureCost` were re-read as large,
   lightly covered helpers.

City placement goes through `canFound`; no current supported city-placement legality
bypass was established. The culture-level `+1` indexing shape is a generic upper-bound
robustness concern if an external caller attempts to purchase culture beyond the final
level, but current Advanced Start points/culture pricing do not provide an ordinary
route to buying a city beyond Legendary. Keep this below the numbered threshold.

HISTORICAL-FIX x LOW-COVERAGE PASS
----------------------------------
4. `CvPlayer::validateDiplomacy` is already semantically owned by an earlier album
   control and remains closed.

The initially suspicious Defensive Pact stale-offer check examines the first trade
item, but the actual AI Defensive Pact contact producer constructs a one-item DP offer
on each side. There is no supported producer where a later-list DP silently bypasses
that validation. Do not duplicate the earlier control merely because the function has
few literal mentions.

5. `DIPLOEVENT_JOIN_WAR` `GET_TEAM(ePlayer)` is a FALSE POSITIVE under the active SAS
   helper overloads.

At first comparison, K-Mod/Civ4CE spell the declaration target as:

    GET_TEAM(GET_PLAYER(ePlayer).getTeam()).declareWar(...)

whereas AdvCiv/current SAS use:

    GET_TEAM(ePlayer).declareWar(...)

Because `ePlayer` is `PlayerTypes`, this looked especially dangerous after a Permanent
Alliance, where a player's current team can differ from its original player index.

Full helper-layer reconciliation closes it. In the active non-AI gameplay layer:

    #define GET_TEAM(x) CvGamePlay::getTeam(x)

and `CvGamePlay` provides a `getTeam(PlayerTypes)` overload that resolves through:

    CvTeam::getTeam(TEAMID(ePlayer))

Therefore the short AdvCiv spelling maps the player to its CURRENT team, including
after Permanent Alliance reassignment. It is semantically equivalent to K-Mod's more
explicit spelling.

This is the same enum-overload trap already documented elsewhere in the album for
`CvPlayer::canContact` and related false positives. Do not revive a raw
PlayerTypes->TeamTypes bug from `GET_TEAM(ePlayer)` syntax without checking the active
overload context.

6. `CvPlayer::isValidTriggerCorporation` retains K-Mod's explicit corporation-count
   bugfix and closes normally.

For non-city trigger evaluation, current code requires:

    getHasCorporationCount(eCorporation) > 0

before applying optional headquarters ownership checks. The old inverted/early-return
shape remains commented out next to K-Mod's repair. No missing religion/corporation
sibling was found here.

7. `CvPlayer::removeBuildingClass` remains consistent with its normal production
   caller and the Unofficial Patch intent.

The helper removes exactly one real instance of the civilization's mapped building
class. Its normal DLL caller is the ORDER_CONSTRUCT completion path when the class has
become maxed while an already-started order is finishing. The 2010 Unofficial Patch
changed the old behavior from zeroing the building count to subtracting one instance,
which avoids deleting multiple real copies in data sets that permit them.

Current Palace data (`MaxPlayerInstances=1`, `ExtraPlayerInstances=1`, `bNoLimit=1`)
uses this mechanism to move the capital cleanly: the extra slot permits starting the
replacement, and completion removes one old Palace before installing the new one. No
independent wrong-building or count-underflow producer was established.

8. `CvPlayer::setCommercePercent` closes under its two-pass balancing contract.

The first pass redistributes against other flexible commerce types; if forced changes
cannot balance under those restrictions, the second pass broadens the eligible types.
The apparently odd negative `iAdjustment` when total commerce falls below 100 is
intentional: subtracting a negative adjustment raises another eligible percentage.
The final assertion requires the total to return to 100, and the function refreshes
commerce, human commerce weights, assignment dirtiness and relevant UI state after an
actual change. No stale authoritative percentage/cache root was established.

9. `CvPlayer::initFreeUnits` remains setup-heavy rather than exposing a new runtime
   invariant in this pass.

The main historical seams checked were Advanced Start points/visibility, starting-site
Goody removal, AI handicap start-unit multipliers and `addFreeUnitAI` legality. The
normal non-Advanced-Start path selects starting defenders/workers/explorers through
`canTrain`; actual placement funnels through `addFreeUnit`, which rejects missing
starting plots and performs the existing explorer-placement checks. No current normal
setup failure cleared the F473 threshold.

10. `CvPlayer::disbandUnit` remains primarily policy/valuation.

The routine excludes cargo, Golden Age and non-production-cost units, protects the only
military-happiness unit on a plot, then chooses among remaining units by cost,
experience, level, defensive placement and UnitAI weighting. The previously tempting
questions in this area concern tuning choices rather than copied state or a violated
current invariant. No F473.

CURRENT STONE AFTER C031-WIP194
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F472 / KI#795 CONFIRMED.
F473 / provisional KI#796 UNUSED.
Latest physical checkpoint: C031-WIP194.
No repository source files modified.

NEXT
----
Continue from F473 unused.

The next high-signal pass should stay with lightly covered authoritative helpers rather
than raw mention counts:

* `getKnownPlayerColor` / known-identity and observer-privacy helpers;
* `getSplitEmpireLeaders` / colony identity selection and recycled-player lifecycle;
* `keepCity` / occupation-end trait-culture initialization and ownership transitions;
* `getNumGovernmentCenters` callers only if they feed gameplay rather than help text;
* remaining paired stored-state mutators and historical bugfix comments with current
  nonzero XML producers.

Use semantic duplicate search before deep tracing. Require current SAS reachability,
concrete gameplay consequence, full caller/callee proof and ancestry before consuming
F473. Robustness/API-only defects and data-masked bugs remain notes unless current
shipped state activates them.

END C031-WIP194


===============================================================================
C031-WIP195 — QUEUE 004 CVPLAYER.CPP — POST-WIP194 DEEP CLOSURE CHECKPOINT
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F472 / KI#795 remain CONFIRMED.
F473 / provisional KI#796 remains UNUSED.
No repository source files modified.

CONTINUATION ANCHOR
-------------------
Continued directly from C031-WIP194 / F473 unused. This tranche returned to the
lightly covered authoritative/lifecycle functions queued at the prior handoff and then
used the historical bug-fix matrix plus semantic album reconciliation as duplicate
control. Several source-level oddities were real, but every one checked here either
closed under its actual caller/lifecycle or was already preserved as current-data-
masked / dormant robustness debt. No root number is spent merely because a function
has low literal album coverage.

CONTROL — `getKnownPlayerColor` IS AN ACTIVE-OBSERVER UI CONCEALMENT HELPER
---------------------------------------------------------------------------
`CvPlayer::getKnownPlayerColor(TeamTypes eObserver)` initially deserved a privacy
check because it substitutes the Barbarian player color for an unknown civilization.
The current compiled callers resolve the intended observer contract:

* `CvPlot::plotMinimapColor`;
* `CvPlayer::getResourceLayerColors`;
* `CvPlayer::getCultureLayerColors`;
* the external player-color/UI accessor.

These are active-observer presentation paths rather than authoritative diplomatic or
gameplay knowledge stores. The default-observer handling through the active team is
consistent with those callers. No independent information leak or stale-state root was
established.

CONTROL — `getNumGovernmentCenters` IS DYNAMIC / UI-FACING, NOT A STALE CACHE
-----------------------------------------------------------------------------
`getNumGovernmentCenters()` walks the player's current cities and counts
`isGovernmentCenter()` dynamically. Its DLL use is essentially UI/Python-facing; core
maintenance logic consumes city-level government-center state rather than a copied
player count. No invalidation family exists here.

CONTROL — `keepCity` CREATIVE CULTURE HAS THE MATCHING OCCUPATION-END HOOK
---------------------------------------------------------------------------
`CvPlayer::keepCity` deliberately calls `CvCity::initTraitCulture()` only when the
acquired city is not in occupation. That initially looked like a possible permanently
missed trait effect for conquered cities.

The lifecycle closes cleanly: `CvCity::setOccupationTimer` detects the transition from
occupied to non-occupied and calls `initTraitCulture()` before refreshing culture
level. This is exactly the historical advc.908b intent (Creative/free trait culture
starts after occupation ends), not a missing delayed application.

CONTROL — `getSplitEmpireLeaders` PRESERVES THE RANDOM-PERSONALITIES LIBERATION FIX
-----------------------------------------------------------------------------------
The historical practical-2423 repair deliberately changed liberation/colony leader
selection from hidden personality identity to the displayed leader identity, leaving
personality randomization to `CvGame::addPlayer` afterward.

Current `getSplitEmpireLeaders` still excludes civilizations/leaders used by
`EVER_ALIVE` players through `getLeaderType()`, not `getPersonalityType()`. The intended
Random Personalities separation therefore survives. Malformed recycled-player colony
state remains independently owned by F447 / KI#770; no new colony root here.

CONTROL — PLAYER HISTORY OOB REPAIR SURVIVES CURRENT SAS LOGGING / PLAYER SWITCHING
------------------------------------------------------------------------------------
The old AdvCiv PlayerHistory out-of-bounds family was rechecked because current SAS
adds substantial game-record logging and active-player/lifecycle instrumentation.
The present contracts remain coherent:

* `initContainers` grows every PlayerHistory through the current game turn for late-
  start / scenario players;
* `PlayerHistory::grow(iSize)` guarantees storage through index `iSize`;
* normal per-turn history updates cover the seven current history enum values;
* active-player switching fills the departing living player's current entry before
  the switch, preventing the historical player-switch gap;
* SASGameRecord reads through the safe history accessor rather than indexing an
  assumed-current vector directly.

The current history enum is still exactly the set handled by the rewritten switch.
No current logging-induced reopening of practical-2635 was found.

CONTROL — `setFoundedFirstCity` PARENT GUARD IS THE INTENDED COLONIAL-CTD FIX
-----------------------------------------------------------------------------
The UWAI update suppression in `setFoundedFirstCity` for players with a non-NO parent
can look like a missing cache update in isolation. Historical practical-1522 resolves
it: this guard was added specifically to avoid the colonial-vassal creation crash.
The colony initialization pipeline later establishes the new player's broader state.
No independent stale-cache consequence was established.

CONTROL — `initPlotGroup` IS ONLY THE ALLOCATION PRIMITIVE; CURRENT CALLER OWNS THE
REAL NETWORK INVARIANT
------------------------------------------------------------------------------------
`CvPlayer::initPlotGroup` itself merely allocates/initializes a plot group. Its live
caller is `CvPlot::updatePlotGroup`, which performs trade-network eligibility,
adjacent-group joining/recalculation, and creates a new group only after those checks.
The historical bug in which group creation sat inside the wrong branch is no longer
present. No second plot-group initialization defect survived.

ROBUSTNESS NOTE — IMPROVEMENT-UPGRADE RATE CAN GENERICALLY REACH ZERO, BUT CURRENT
SAS DATA DOES NOT PRODUCE THE UNSAFE RATE
----------------------------------------------------------------------------------
AdvCiv's improvement-upgrade rewrite expresses the authoritative player upgrade rate
in percent and clamps it to nonnegative. Some downstream upgrade-time arithmetic no
longer carries the old explicit zero-rate guard. A future producer of a total
ImprovementUpgradeRateModifier <= -100 could therefore make that path unsafe.

Current SAS XML masks the structural concern: the only shipped nonzero civic producer
is positive (+100), and no ordinary current transition drives the rate to zero. Keep
as configuration/robustness debt rather than F473.

CONTROL — `killGoldenAgeUnits` DELAYED DEATH DOES NOT USE A DETACHED / DELETED UNIT
----------------------------------------------------------------------------------
The ordering initially looks dangerous:

    pBestUnit->kill(true);
    if (pBestUnit->getPlot().isActiveVisible(false))
        pBestUnit->getGroup()->pushMission(MISSION_GOLDEN_AGE, 0);

Full `CvUnit::kill(true)` tracing closes the lifetime concern. The delayed path calls
`startDelayedDeath()` and returns before `setXY`, `joinGroup(NULL)`, or object deletion.
The unit therefore still owns its plot/group when the animation mission is queued.
The nearby comment saying kill removes the unit from groups is misleading for this
delayed path, but no UAF/null-group root exists.

The separate readiness-UnitClass vs consumption-UnitType distinction is also NOT new:
the album already preserves it as current-data-masked. Current SAS has one Golden-Age
unit type per relevant UnitClass, so the distinction cannot alter shipped behavior.

CONTROL — `getWorkRate(BuildTypes)` DOUBLE BASE-100 IS REAL SOURCE DEBT BUT ALREADY
PARKED AND NOT AUTHORITATIVE AI WORK PROGRESS
----------------------------------------------------------------------------------
The player-level predictor contains the conspicuous inherited expression:

    iRate *= max(0, 100 + AIWorkRateModifier + 100);

where actual `CvUnit::workRate` uses the ordinary single-base form. With current SAS
handicap data (`AIWorkRateModifier == 0`) the AI branch of this PLAYER predictor would
indeed estimate twice the ordinary base work rate.

This is an exact existing album control, not F473. Caller tracing reconfirms why:

* live human build-turn help/warning paths use the active human player, so the bad AI
  branch is not taken;
* AI worker planning uses overloads fed by actual `CvUnit::workRate` or iterates
  actual workers rather than this player-only predictor;
* SASGameRecord likewise uses actual-unit work-rate/build-turn data.

AI Auto Play can expose an inaccurate UI-style estimate, but authoritative worker
progress is not doubled. Preserve the already-parked source defect; do not duplicate.

CONTROL — SPY DESTROY/BRIBE TARGET-OWNER VALIDATION IS THE EARLIER DORMANT FINDING
----------------------------------------------------------------------------------
A fresh low-coverage pass over `canSpyDestroyUnit` / `canSpyBribeUnit` noticed the
same structural ownership weakness preserved in WIP172/WIP179: the helper does not
itself prove that `kUnit.getOwner()` is `eTarget`, while the generic espionage costing
fallback can iterate third-party units on the plot.

Current shipped SAS still has all `iDestroyUnitCostFactor` and `iBuyUnitCostFactor`
values at zero. The affected mission families therefore have no current gameplay data
producer. This remains dormant robustness debt, not F473.

HISTORICAL DEATH / BONUS-CACHE RECHECK
--------------------------------------
Practical-2437 was revisited because it fixed a player-defeat crash through several
closely related changes:

* `setAlive(false)` clears the capital before killing components;
* bonus import/export updates skip `AI_updateBonusValue` while the player is dying;
* deal destruction is deferred until `m_bAlive` is false because that cleanup depends
  on death state.

Current source retains those guards/orderings. The currently known elimination-order
war-weariness failure is independently F471; this historical sibling pass did not
expose another death-time dereference/cache update that survives current ordering.

CONTROL — `changeHurryCount` RAW `iChange` ALGEBRA REMAINS SAFE UNDER SHIPPED
PRODUCERS
-----------------------------------------------------------------------------
The function stores per-Hurry source counts and maintains derived pop-rush / gold-rush
availability counters. On a 0 <-> positive boundary it adds raw `iChange` to the
derived counter. Generic API calls of magnitude >1 would make that algebra wrong.

Current compiled producer is civic processing. Hurry availability is a boolean XML
source and `processCivics` calls `changeHurryCount` with exactly +1 or -1 when that
civic is installed/removed. Current XML has one population-hurry civic and one gold-
hurry civic. Therefore every real boundary transition changes the derived counter by
exactly one. This matches the earlier album control; no F473.

PAIRED STATE / YIELD / RELIGION / CORPORATION RECONCILIATION
-------------------------------------------------------------
The neighboring low-literal-coverage mutators were rechecked against the already
settled WIP179-WIP189 cache graph rather than re-numbered from function-name scarcity.
The important chains remain present:

* religion/corporation city-count mutations refresh building commerce and assignment
  AI; corporation availability is dynamically filtered through `isActiveCorporation`;
* no-corporation / no-foreign-corporation transitions call `updateCorporation`;
* state-religion count changes refresh maintenance, religion happiness/commerce and
  assignment state;
* player yield/commerce modifier paths invalidate/recompute the appropriate rank or
  city/player commerce aggregates;
* building-only-healthy and specialist-valid transitions dirty assignment logic;
* population/land/score stored-state arithmetic remains paired old-contribution ->
  mutate -> new-contribution where required.

No independent current zero-boundary/cache sibling was established.

HISTORICAL-FIX RANKING / METHOD NOTE
------------------------------------
A compact local matrix of AdvCiv pre-SAS bug/fix commits touching `CvPlayer.cpp` was
used to cross-rank lightly covered functions. This continues to be more reliable than
raw literal function-name counts: several functions with zero literal mentions are
already semantically audited through a caller/root family, while historical comments
sometimes list a function only because it was context in a larger patch.

Notably, practical-4139's `getIntroMusicScriptId` repair (use displayed leader rather
than hidden personality) remains present, and no new `markTradeOffers` state defect was
established from that patch. Likewise practical-1385's apparent
`changeImprovementYieldChange` mention is merely nearby/contextual cleanup; the current
mutator follows the dynamic yield-update path already audited elsewhere.

CURRENT STONE AFTER C031-WIP195
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F472 / KI#795 CONFIRMED.
F473 / provisional KI#796 UNUSED.
Latest physical checkpoint: C031-WIP195.
No repository source files modified.

NEXT
----
Continue from F473 unused. Use the historical-fix x low-semantic-coverage matrix to
prioritize remaining authoritative normal-game helpers rather than UI/setup wrappers.
Highest-value next lenses are:

1. remaining player-owned persistent state whose consumers live outside CvPlayer and
   therefore may hide a missing invalidation;
2. ownership/death/revival/team transitions not already owned by F447/F455/F469-F471;
3. event/vote/religion paths only after semantic duplicate control, because this album
   already contains several dormant data-masked event defects and active espionage/
   vote roots;
4. arithmetic helpers with current nonzero XML/define producers, rejecting generic
   malformed/config-only cases;
5. compare K-Mod/Civ4CE/base AdvCiv whenever a historical fix suggests an unpatched
   sibling.

Require current SAS caller + current data + full callee/lifecycle consequence +
ancestry + duplicate proof before spending F473. Create WIP196 at the next meaningful
settled tranche, whether that tranche confirms F473 or closes further high-signal
territory.

END C031-WIP195


===============================================================================
C031-WIP196 — QUEUE 004 CVPLAYER.CPP — F473 / KI#796 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F473 / KI#796 CONFIRMED.
F474 / provisional KI#797 is UNUSED.
Latest physical checkpoint: C031-WIP196.
No repository source files modified.


POST-WIP195 CONTROL — TEAM-EVENT PROPAGATION CONDITION IS INVERTED, BUT CURRENT SAS
HAS NO TEAM EVENTS
-------------------------------------------------------------------------------
`CvPlayer::setEventOccured(..., bOthers=true)` contains a real inherited AdvCiv
iterator-refactor regression in the team-event propagation branch:

    else if (GC.getInfo(eEvent).isTeam())
    {
        for (MemberIter itOtherMember(getTeam()); itOtherMember.hasNext();
             ++itOtherMember)
        {
            if (itOtherMember->getID() != getID())
                continue;
            itOtherMember->setEventOccured(eEvent, kEventTriggered, false);
        }
    }

The condition retains only `this` player instead of the OTHER teammates. K-Mod and
Civ4CE/BtS use the opposite semantic condition and propagate the occurrence to the
other team members.

This is nevertheless NOT F473. Current SAS EventInfos contain no `bTeam=1` event, so
there is no shipped gameplay producer for the broken branch. The giant album had also
already preserved this exact source defect as dormant/data-masked robustness debt.


POST-WIP195 CONTROL — TEAM RESEARCH OVERFLOW USES CALLER MODIFIER, BUT CURRENT
PLAYER-SPECIFIC RESEARCH MODIFIERS ARE DISABLED
-------------------------------------------------------------------------------
`CvPlayer::getResearchTurnsLeftTimes100` correctly asks every teammate for that
teammate's ordinary research rate, but scales each teammate's stored overflow through
`this->calculateResearchModifier(eTech)` rather than
`kMember.calculateResearchModifier(eTech)`.

That is generically fragile for team games. Current SAS, however, has both human and AI
Groundbreaking penalty toggles disabled; that penalty is the only player-specific
component presently capable of making the relevant research modifier differ between
teammates. Current data therefore keep the modifiers symmetric. This exact generic
weakness was already preserved earlier in the album. No F473.


POST-WIP195 CONTROL — EVENT CLEAR LIST MULTIPLICITY REMAINS CURRENT-DATA MASKED
-------------------------------------------------------------------------------
The event-clear code has ordering semantics that become awkward if one event can clear
multiple prior events with independently meaningful percentages. Current SAS XML does
not exercise that shape: every event with a positive ClearEvents entry has only one
positive clear target in shipped data. Preserve the generic configuration concern;
no current root.


POST-WIP195 DUPLICATE CONTROL — AREA-LOCAL MISSIONARY TRAINING OMISSION IS F451
-------------------------------------------------------------------------------
`CvPlayer::countReligionSpreadUnits(pArea, ..., bIncludeTraining=true)` filters
existing missionary units by area but counts missionaries being trained in every city
without applying the same area test. Its corporation sibling filters both the unit and
production sides.

The current AI caller genuinely uses the religion helper for an area-local missionary
coverage estimate, so this looked strong on a fresh low-literal-coverage pass. Semantic
album reconciliation shows that it is exactly F451 / KI#774, already confirmed with
its ordinary `AI_missionaryValue` consequence. Do not duplicate it as F473.


F473 / KI#796 — BBAI `initInGame` TREATS DEAD ASSIGNED TEAMMATES AS AN EXISTING TEAM,
SO A RECYCLED COLONY CAN REVIVE A FULLY DEFEATED MULTI-MEMBER TEAM'S STALE TECHNOLOGY
STATE AND MISS PLAYER-LEVEL TECH EFFECTS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. This is a Better BTS AI / K-Mod lifecycle regression inherited by Base
AdvCiv and current AdvCiv-SAS. It is distinct from F447's recycled-player / LIVE-team
mismatch: here the selected player's old team is fully DEAD, so `CvGame::addPlayer`
does run, but `CvPlayer::initInGame` fails to reset the dead multi-member team because
another dead player is still assigned to it.


ROOT — `initInGame` TESTS ASSIGNED MEMBERSHIP, NOT LIVE TEAM EXISTENCE
---------------------------------------------------------------------
`CvGame::addPlayer` recycles a slot by changing its leader/civilization/status/color
and then calling:

    GET_PLAYER(eNewPlayer).initInGame(eNewPlayer);

`CvPlayer::initInGame` first resets the PLAYER:

    reset(eID);

`CvPlayer::reset` clears the player's serialized gameplay state, including:

    m_iExtraHealth = 0;
    ...
    m_bAlive = false;
    m_bEverAlive = false;

but it then restores `m_eTeamType` from `CvInitCore` through `updateTeamType()`. It does
not move the recycled slot to a new team and does not reset the old `CvTeam` object.

The BBAI team-reset logic then counts every other PLAYER ASSIGNED to that team,
regardless of whether the player is alive:

    int iOtherTeamMembers = 0;
    for (int i = 0; i < MAX_CIV_PLAYERS; i++)
    {
        if (i != getID() && TEAMID((PlayerTypes)i) == getTeam())
            iOtherTeamMembers++;
    }

and resets the team only when there are no other assigned members, or when the team's
stored total member count is zero:

    if (iOtherTeamMembers == 0 || GET_TEAM(getTeam()).getNumMembers() == 0)
    {
        bTeamInit = true;
        GET_TEAM(getTeam()).init(getTeam());
        ...
    }

This is not equivalent to asking whether the team still has a live identity.

Player death calls `CvTeam::changeAliveCount(-1)`. It does NOT decrement
`m_iNumMembers`. Current source has only player initialization and `CvPlayer::setTeam`
as producers of `changeNumMembers`; ordinary elimination leaves defeated players
assigned to their old team.

Therefore, for an old two-player Team T whose members X and Y are both dead:

    T.getAliveCount()  == 0
    T.isAlive()        == false
    T.getNumMembers()  == 2

If X is recycled, `initInGame` sees Y through `CvInitCore` and computes:

    iOtherTeamMembers == 1
    T.getNumMembers() == 2

Both reset conditions are false. `CvTeam::init(T)` is skipped even though Team T has
no living members.

`initOtherData()` then calls `setAlive(true)`, reviving X and thereby reviving Team T
without ever clearing the old defeated team's serialized team state.


WHY DEAD TEAM STATE SURVIVES
----------------------------
`CvTeam::changeAliveCount` performs elimination teardown when AliveCount reaches zero:
it releases vassals, exposes the dead team's identity, clears wars and war plans, and
updates related lifecycle state. It does NOT call `CvTeam::reset` and does not clear
technology ownership, research progress, project counts or the other serialized team
arrays.

By contrast, `CvTeam::reset` / `CvTeam::init` explicitly clears, among many fields:

    m_iNumMembers
    m_iAliveCount
    m_iTechCount
    m_aiProjectCount
    m_aiResearchProgress
    m_abHasTech

The skipped team initialization is therefore gameplay-significant, not merely stale
metadata.


DETERMINISTIC CURRENT-SAS CONSEQUENCE — BIOENGINEERING IS KNOWN BUT ITS +3 HEALTH IS
MISSING FROM THE NEW COLONY PLAYER
-------------------------------------------------------------------------------
Current SAS `TECH_BIOENGINEERING` is a Future Era technology with:

    <iHealth>3</iHealth>

When a technology is newly processed, `CvTeam::processTech` applies persistent
player-level effects to team members, including:

    kMember.changeFeatureProductionModifier(...);
    kMember.changeWorkerSpeedModifier(...);
    kMember.changeTradeRoutes(...);
    kMember.changeExtraHealth(kTech.getHealth() * iChange);
    kMember.changeExtraHappiness(...);
    kMember.changeAssets(...);
    kMember.changePower(...);
    kMember.changeTechScore(...);
    ...

Use this supported state:

1. Human player P has an eligible colony area.
2. No never-alive player slot remains, so `getSplitEmpirePlayer` must use AdvCiv's
   human-only defeated-player recycling fallback. The same supported scenario
   reachability already established for F447 applies: all 48 player slots can have
   been ever alive while scenario/custom setup repeats civilizations and leaves at
   least one valid unused civilization/leader combination for colony creation.
3. X is the first recyclable dead player.
4. X and another dead player Y are both still assigned to old Team T. T is fully
   defeated: `T.isAlive()==false`, but `T.getNumMembers()==2`.
5. Before defeat, Team T acquired Bioengineering, so its surviving team state still
   has `m_abHasTech[BIOENGINEERING] == true`.
6. Parent P also knows Bioengineering.
7. P creates the colony.

`CvPlayer::splitEmpire` correctly sees that T is dead:

    bool bPlayerExists = GET_TEAM(eNewPlayer).isAlive();

so this time, unlike F447, `bPlayerExists == false` and `CvGame::addPlayer` DOES run.

`initInGame` resets X's player state, clearing the previous Bioengineering-derived
player health, but skips `CvTeam::init` for T because dead teammate Y remains assigned.
Thus Team T still says Bioengineering is known.

The colony-inheritance loop later executes:

    if (GET_TEAM(getTeam()).isHasTech(eLoopTech))
        kNewTeam.setHasTech(eLoopTech, true, eNewPlayer, ...);

For Bioengineering, `CvTeam::setHasTech` immediately returns at:

    if (isHasTech(eTech) == bNewValue)
        return;

because stale Team T already has the tech. `processTech` is never replayed for the
newly reset X.

The result is internally inconsistent:

* Team T / colony X owns Bioengineering;
* X's reset player state lacks Bioengineering's +3 `m_iExtraHealth` contribution;
* colony cities use `GET_PLAYER(getOwner()).getExtraHealth()` directly in their health
  calculation, so the missing effect is immediately gameplay-visible.

The same mechanism can skip other player-level effects for any old team technology
that the parent also owns. Conversely, old Team T can also retain technologies that
P does NOT know because the colony inheritance loop only adds parent technologies; it
does not remove old-team technologies.

The Bioengineering +3-health case is enough to prove a concrete current-data failure
without relying on the broader stale-state surface.


THE MISMATCH SURVIVES SAVE/LOAD
-------------------------------
The incorrect state is serialized on both sides:

* `CvTeam::write/read` stores `m_abHasTech` directly;
* `CvPlayer::write/read` stores `m_iExtraHealth` directly.

Loading does not replay every already-known team technology through `processTech`.
Therefore a save made after the malformed colony is created preserves both “team knows
Bioengineering” and “player is missing its +3 health contribution”; save/load does not
self-heal the mismatch.


ANCESTRY — STOCK BTS RESETS THE TEAM; BBAI INTRODUCED THE CONDITIONAL RESET
-------------------------------------------------------------------------------
The root is not stock BtS colony behavior.

Civ4CE's reconstructed BtS `CvGame::addPlayer` obtains the selected player's old team
and unconditionally does:

    GET_TEAM(eTeam).init(eTeam);
    GET_PLAYER(eNewPlayer).init(eNewPlayer);

Thus a fully dead recycled team is cleared before the replacement player is
initialized.

Better BTS AI changed `CvGame::addPlayer` on 2008-12-30. K-Mod retains the patch and
its comments:

    // Team init now handled when appropriate in player initInGame
    // Standard player init is written for beginning of game ...
    GET_PLAYER(eNewPlayer).initInGame(eNewPlayer);

K-Mod's `initInGame` contains the same `iOtherTeamMembers` / `getNumMembers()`
conditional team-reset logic shown above. Base AdvCiv 1.14 retains it, and current SAS
retains it.

So F473 is an inherited BBAI/K-Mod lifecycle regression caused by replacing BtS's
unconditional recycled-team reset with a conditional test that treats dead assigned
teammates as evidence that the old team should be preserved.


DUPLICATE / ALBUM RECONCILIATION
--------------------------------
F473 is independent of the surrounding confirmed lifecycle roots.

F447 / KI#770:
* selected player X is dead;
* X's OLD TEAM IS STILL ALIVE because another teammate survives;
* `splitEmpire` therefore sets `bPlayerExists=true` and SKIPS `addPlayer/initInGame`
  entirely;
* city transfer later revives the old player identity through `verifyAlive`.

F473 / KI#796:
* selected player X is dead;
* X's OLD TEAM IS FULLY DEAD because every teammate is dead;
* `splitEmpire` therefore sets `bPlayerExists=false` and DOES call
  `addPlayer/initInGame`;
* the PLAYER is reset/rebranded, but `initInGame` skips resetting the dead TEAM because
  another dead assigned member still counts toward `iOtherTeamMembers` / NumMembers;
* stale team technology state then conflicts with the new player's reset persistent
  tech-derived state.

F094 / KI#416 is also different. F094 was AdvCiv practical-1837's alive-only
technology-processing regression: a dead member of a STILL-LIVING team could miss
tech effects acquired while dead. Current SAS has repaired that by processing
EVER_ALIVE assigned members. F473 happens later when the recycled player's own state
is explicitly RESET while its fully dead team's already-known technologies survive;
the inheritance call then no-ops because those technologies are already marked known.

F470 / KI#793 concerns missing `CvAgents` cache rebuilding after HUMAN-involved
Permanent Alliances and subsequent iterator consumers. F473 does not depend on stale
agent iterators or Permanent Alliances.

The earlier album control about `initInGame` comparing a team-loop integer with
`getID()` concerned minor-team declaration logic and did not audit this conditional
team-reset invariant.

No prior album root owns the fully-dead-multi-member-team / recycled-player reset
split above.


CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not fix this merely by changing the existing condition to “no other ALIVE member”
and then blindly resetting a multi-member team. If dead Y remains assigned to T while
`CvTeam::reset` changes `m_iNumMembers` back to zero/one, team membership bookkeeping
can become inconsistent and a later revival of Y creates another lifecycle problem.

For colony recycling, the safest narrow policy is to refuse a defeated player slot
whose old team has any other assigned member. Conceptually:

    recyclable dead player must belong to an old singleton team

This single restriction also complements F447's live-team case: a defeated member of
any multi-member old team is not a safe identity to repurpose as a fresh colonial
civilization without explicit team detachment/reassignment.

If full multi-member-slot recycling is desired instead, move the replacement player
onto a genuinely clean team (or explicitly detach/reconcile all old team membership)
before `addPlayer/initInGame`, then initialize that team and rebuild all dependent
team/member caches. Merely forcing `CvTeam::init` on T while leaving other dead players
assigned to T is incomplete.

Regression coverage should include:

* no never-alive slots remaining;
* recyclable X and dead teammate Y assigned to the same fully defeated team;
* old team and parent both owning a technology with a player-level persistent effect,
  e.g. current SAS Bioengineering +3 health;
* colony creation must either reject the unsafe recycled slot or produce a clean
  colony whose team technologies and player-level effects agree.


SECONDARY ROBUSTNESS NOTE — `initInGame` MINOR-TEAM SELF SKIP COMPARES TEAM LOOP ID
WITH PLAYER ID
-------------------------------------------------------------------------------
The later initialization loop iterates TeamTypes but skips:

    if (i == getID() || !kTarget.isAlive())
        continue;

rather than comparing `i` with `getTeam()`.

For normal one-player/one-corresponding-team colony creation, PlayerTypes and TeamTypes
coincide and this is masked. Current WorldBuilder/scenario data can assign players to
arbitrary team IDs, so a recycled/custom slot where player ID and team ID differ can
make the loop skip an unrelated same-numbered team instead of the new player's team.
In combination with a live minor team this can omit the setup declaration of permanent
war that the loop is meant to establish.

This is real generic/scenario robustness debt but is not needed for F473 and remains
below the numbered threshold here. The stronger fully-dead-team reset defect above has
a concrete persistent current-data consequence.


NUMBERING
---------
F473 is CONFIRMED and consumes KI#796.
F474 / provisional KI#797 is the next unused root slot.


CURRENT STONE AFTER C031-WIP196
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F473 / KI#796 CONFIRMED.
F474 / provisional KI#797 UNUSED.
Latest physical checkpoint: C031-WIP196.
No repository source files modified.

NEXT
----
Continue from F474 unused. First close the immediate `initInGame` / `CvGame::addPlayer`
sibling family around F473: team membership accounting, dead teammate reassignment,
agent caches, minor-team setup, event inheritance and any other state that assumes a
fresh singleton colony team. Keep F447/F473 as explicit duplicate controls.

Then return to the remaining low-semantic-coverage authoritative player state whose
consumers live outside `CvPlayer.cpp`, followed by event/vote/religion and arithmetic
helpers with current nonzero data producers.

Require current SAS caller + current data/reachability + full lifecycle/callee
consequence + ancestry + semantic duplicate proof before spending F474. Create
C031-WIP197 at the next meaningful settled tranche whether F474 is confirmed or remains
unused.

END C031-WIP196



===============================================================================
C031-WIP197 — QUEUE 004 CVPLAYER.CPP — F474 / KI#797 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F474 / KI#797 is CONFIRMED.
F475 / provisional KI#798 is UNUSED.
No repository source files modified.


POST-F473 SIBLING CONTROL — TEAM MEMBERSHIP / UWAI ORDERING
-----------------------------------------------------------
The immediate `initInGame` / `addPlayer` sibling family was continued before widening
back out of colony lifecycle code.

The team membership counter does not independently repair F473. `initInGame` decides
whether to reset/increment the selected team from its preexisting assigned-member state;
that is part of F473's stale fully-dead multi-member team root rather than a second
counter defect.

The apparent UWAI ordering seam also closes. `initInGame` calls:

    getUWAI().initNewPlayerInGame(getID());

before `splitEmpire` later installs the colony parent and vassal state, but that helper
only initializes the new player/team UWAI objects and clears the WarEvaluator cache.
The substantive:

    getUWAI().processNewPlayerInGame(eNewPlayer);

call occurs near the END of `splitEmpire`, after parent assignment, inherited techs,
`assignVassal`, city transfer, plot-group rebuilding and free-defender creation. That
later update is the point that populates the new player's UWAI cache and runs team
`turnPre`. No independent parent/vassal-dependent stale UWAI state was established.

The minor-team `initInGame` self-skip (`i == getID()` in a TeamTypes loop) remains the
scenario/robustness note preserved in WIP196; no ordinary colony producer was found
that promotes it.


F474 / KI#797 — RECYCLING ANY DEFEATED COLONY SLOT DUPLICATES ADVC'S
`CIV_EVER_ALIVE` AGENT CACHES; THE COLONY CAN LOSE FIRST-DISCOVERY REWARDS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. This is an AdvCiv `advc.agent` integration defect reached by the same
supported human colony-slot recycling fallback that underlies the broader recycled
player family, but it does NOT require F473's stale multi-member-team condition.
A defeated singleton-team slot is enough.

ROOT
----
`CvAgents::playerDefeated` deliberately removes a defeated player/team only from the
ALIVE caches. It correctly leaves the historical EVER_ALIVE sequences intact:

    eraseFromSeqCache(playerSeqCache(CIV_ALIVE), eDeadPlayer);
    ...
    if (!GET_TEAM(eTeam).isAlive())
        eraseFromSeqCache(teamSeqCache(CIV_ALIVE), eTeam);

There is no removal from:

    playerSeqCache(CIV_EVER_ALIVE)
    teamSeqCache(CIV_EVER_ALIVE)

because the player/team really has been alive historically.

When a new colony uses a never-used slot, `CvPlayer::initInGame` eventually calls:

    GC.getAgents().colonyCreated(getID());

and `CvAgents::colonyCreated` does:

    playerSetAliveInGame(eNewPlayer, false);

`playerSetAliveInGame(..., false)` unconditionally performs:

    insertIntoVector(teamSeqCache(CIV_EVER_ALIVE), pTeam, false);
    ...
    insertIntoVector(playerSeqCache(CIV_EVER_ALIVE), pPlayer, false);

The final `false` merely suppresses the duplicate assertion. It does NOT request
unique insertion. `insertIntoVector` always inserts the pointer after walking to its
sorted position:

    FAssert(!bAssertSuccess || iPosID != iAgentID);
    ...
    v.insert(pos, pAgent);

So for a truly never-alive player/team this is correct, but for a RECYCLED defeated
slot both pointers are ALREADY present in their EVER_ALIVE vectors and are inserted a
second time.

The comment above `colonyCreated` explicitly says:

    // Normally, players shouldn't be reused; if reuse does occur, call should still succeed.

but the suppressed assertion makes reuse silently duplicate the historical sequences
instead of making it safe.

WHY THIS DOES NOT REQUIRE F473
-------------------------------
Use a defeated player X whose OLD TEAM WAS A SINGLETON.

On recycling:
* `splitEmpire` sees X's old team dead, so `CvGame::addPlayer` DOES run;
* `initInGame` sees no other assigned teammate, so it correctly resets that team;
* player and team state are therefore clean — F473 is absent;
* nevertheless `playerDefeated` never removed X/T from the `CIV_EVER_ALIVE` caches;
* `colonyCreated(..., false)` inserts X and T into those same caches again.

Thus F474 survives even when the F473 team-reset invariant is completely satisfied.

The ALIVE sequences are different: defeat DID remove X/T from those caches, so their
new insertion is expected. The defect is specifically the duplicate historical
EVER_ALIVE entries.

DETERMINISTIC CURRENT-SAS CONSEQUENCE — TRUE FIRST DISCOVERY IS COUNTED AS TWO TEAMS
-------------------------------------------------------------------------------------
`CvGame::countKnownTechNumTeams` was converted by AdvCiv from the stock/K-Mod raw
MAX_TEAMS scan to:

    int iCount = 0;
    for (TeamIter<EVER_ALIVE> itTeam; itTeam.hasNext(); ++itTeam)
    {
        if (itTeam->isHasTech(eTech))
            iCount++;
    }

After recycled singleton Team T is duplicated in `CIV_EVER_ALIVE`, any technology T
owns is counted TWICE by this routine.

That directly feeds `CvTeam::setHasTech`'s first-discovery contract. After setting the
technology true, current code calculates:

    bool const bFirstToDiscover =
        (kGame.countKnownTechNumTeams(eTech) == 1);

and all first-discovery rewards are gated by that boolean.

Current SAS provides a particularly clean late-game reproducer:

    TECH_ROMANTICISM
        Era = ERA_INDUSTRIAL
        FirstFreeUnitClass = UNITCLASS_GREAT_ARTIST

Ordinary reproduction shape:

1. Exhaust all never-alive player slots while preserving at least one valid unused
   colony civilization/leader combination, as already source-proven possible for the
   supported human recycling path in F447/F473.
2. Let singleton Player X / Team T be eliminated.
3. A human later creates a colony; `getSplitEmpirePlayer` selects X as the recyclable
   dead slot.
4. Because T is fully dead, `addPlayer/initInGame` executes normally. Because T was a
   singleton, its team is also reset normally — no F473 dependency.
5. `CvAgents::colonyCreated(X)` duplicates X and T in `CIV_EVER_ALIVE`.
6. Leave `TECH_ROMANTICISM` unknown to every team until after the colony exists.
7. Let the colony become the TRUE FIRST team in the world to research Romanticism.
8. `setHasTech` marks T as owning Romanticism, then calls
   `countKnownTechNumTeams(TECH_ROMANTICISM)`.
9. The EVER_ALIVE iterator encounters the same Team T pointer twice and returns 2.
10. `bFirstToDiscover` is therefore false even though no other team owns the tech.
11. `CvPlayer::getTechFreeUnit` would map Romanticism's
    `UNITCLASS_GREAT_ARTIST` to the colony civilization's Great Artist, but the entire
    first-discovery block is skipped.
12. The colony permanently loses the shipped free Great Artist reward.

Current SAS also has many other Industrial/Modern/Future technologies with non-NONE
`FirstFreeUnitClass` (and Theory of Evolution with a first-free-tech reward), so the
issue is not tied to an obsolete Ancient-only race that a late colony could never win.
Romanticism is simply the smallest current-data example.

PERSISTENCE / SAVE-LOAD
-----------------------
`CvAgents::gameStart(true)` calls `updateAllCachedSequences()`, which reconstructs the
vectors from current player/team state and therefore removes the duplicate pointers.
So saving and loading immediately after colony creation can MASK F474.

But if the colony completes Romanticism before that rebuild, the first-discovery
reward has already been skipped. Loading does not replay `setHasTech`'s first-discovery
reward block for technologies already known. The missed Great Artist therefore does
not self-heal.

This gives a clean diagnostic:
* recycle colony -> save/load -> discover first-perk tech: iterator cache repaired;
* recycle colony -> discover first-perk tech -> save/load: missed reward persists.

ANCESTRY / COMPARISON
---------------------
Base AdvCiv 1.14 contains the same `CvAgents::playerDefeated`,
`playerSetAliveInGame`, `colonyCreated` and `insertIntoVector` behavior. Current SAS
retains it.

K-Mod 1.46 and stock BtS/Civ4CE do NOT have the `advc.agent` cached sequence layer.
Their `CvGame::countKnownTechNumTeams` scans each TeamTypes slot exactly once:

    for (int iI = 0; iI < MAX_TEAMS; iI++)
        if (GET_TEAM((TeamTypes)iI).isEverAlive() &&
            GET_TEAM((TeamTypes)iI).isHasTech(eTech))
            iCount++;

So recycling a player cannot duplicate the same team in that calculation upstream.
F474 is specifically an AgentIterator cache-integration regression introduced after
K-Mod's direct index scans.

DUPLICATE / ALBUM RECONCILIATION
--------------------------------
F470 / KI#793 is a different `CvAgents` root. F470 leaves MEMBER and ALIVE-team caches
stale after HUMAN-involved Permanent Alliances because the generic transaction omits
`allianceFormed()` / `updateAllCachedSequences()`.

F474 does not involve Permanent Alliances or team reassignment. It creates literal
DUPLICATE pointer entries in otherwise correctly selected EVER_ALIVE vectors when an
existing historical identity is recycled as a colony.

F473 / KI#796 is also independent:
* F473 = dead multi-member old team is not reset, preserving stale authoritative team
  state while the player is reset;
* F474 = historical AgentIterator entries are inserted twice; singleton-team recycling
  with a clean team reset is sufficient.

F447 / KI#770 skips `addPlayer/initInGame` entirely when the recycled dead player's old
team is still alive, so `colonyCreated` is not reached on that malformed branch. F474
therefore belongs specifically to the fully-dead-team recycling branch where
`addPlayer/initInGame` DOES run.

No previous album root covers duplicate `CIV_EVER_ALIVE` entries from recycled colony
creation or the resulting first-discovery miscount.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The EVER_ALIVE insertions must be uniqueness-preserving.

A narrow repair is for `playerSetAliveInGame(..., bRevive=false)` to insert the player
and team into `CIV_EVER_ALIVE` only when the corresponding pointer is not already
present. This preserves the intended fresh never-alive colony behavior while making
recycled identities idempotent.

Equivalent safe options include:
* a dedicated unique-insert helper;
* erase-then-insert for the two EVER_ALIVE sequences;
* rebuilding all AgentIterator sequences when a recycled slot is detected.

Do not simply change the `false` argument to `true`: that would only turn the existing
silent duplication into a debug assertion while release behavior would still insert.
The operation itself must avoid the duplicate.

Because F473/F447 show that recycling multi-member old-team identities has deeper
problems, the broader conservative colony policy can still reject such slots entirely.
F474 nevertheless needs its own invariant if singleton defeated slots remain recyclable.

REGRESSION TEST
---------------
1. Start with a defeated singleton Player X / Team T already represented once in
   `CIV_EVER_ALIVE` player/team caches.
2. Recycle X through human colony creation.
3. Assert immediately that X and T each occur exactly once in the corresponding
   EVER_ALIVE sequence and exactly once in their expected ALIVE sequence.
4. Ensure no team knows `TECH_ROMANTICISM`.
5. Let recycled Team T research Romanticism first.
6. Assert `countKnownTechNumTeams(TECH_ROMANTICISM) == 1` at first acquisition and
   that X receives the free Great Artist.
7. Repeat with save/load before discovery and after discovery.
8. Also test a genuinely never-alive colony slot to ensure normal insertion still
   occurs exactly once.

NUMBERING
---------
F474 / KI#797 is CONFIRMED.
F475 / provisional KI#798 is the next unused root.


CURRENT STONE AFTER C031-WIP197
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F474 / KI#797 CONFIRMED.
F475 / provisional KI#798 UNUSED.
Latest physical checkpoint: C031-WIP197.
No repository source files modified.

NEXT
----
Continue from F475 unused.

First finish the remaining F474 / `CvAgents::colonyCreated` aftermath without splitting
one duplicate-cache root into artificial findings. Check new-player ALIVE/MAJOR cache
assumptions, vassal cache timing, parent assignment, event-trigger inheritance and any
consumer whose state can become one-way corrupted before a save/load rebuild.

Then resume the historical-fix x low-semantic-coverage authoritative `CvPlayer.cpp`
pass, prioritizing persistent state with external consumers, death/revival/team
transitions, event/vote/religion helpers and current-data arithmetic.

Require current SAS reachability + concrete gameplay consequence + ancestry/comparison
+ semantic duplicate proof before spending F475. Create C031-WIP198 at the next
meaningful settled tranche whether F475 is confirmed or remains unused.

END C031-WIP197


===============================================================================
C031-WIP198 — QUEUE 004 CVPLAYER.CPP — F475 / KI#798 CONFIRMED
===============================================================================

POST-WIP197 COLONY / UWAI ORDERING CLOSURE
------------------------------------------
The immediate F474 aftermath first produced a plausible UWAI ordering concern:
`CvPlayer::initInGame` calls:

    getUWAI().initNewPlayerInGame(getID());

before `splitEmpire` assigns the new player's parent, makes the new team a vassal,
inherits the parent's technologies or transfers colony cities.

The full call chain closes that concern. `initNewPlayerInGame` only clears a shared war
evaluation cache and initializes the new team/player UWAI objects. The substantive new
player reconciliation occurs much later in `splitEmpire` through:

    getUWAI().processNewPlayerInGame(eNewPlayer);

By then parent assignment, inherited techs, vassalization, city transfer, plot-group
updates and free defenders have all happened. `processNewPlayerInGame` performs the
forced new-player cache update and team `turnPre` pass. No independent F475 arises
from the apparent early UWAI initialization.


-------------------------------------------------------------------------------
F475 / KI#798 — MIDGAME PLAYER INITIALIZATION FORGETS OLD GLOBAL NON-RECURRING
TRIGGERS ONCE THEIR SHORT-LIVED EVENT RECORDS EXPIRE, ALLOWING GLOBAL EVENTS TO
BECOME ELIGIBLE A SECOND TIME
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. This is an inherited BtS/BBAI/K-Mod midgame-player random-event state
initialization defect. It does NOT require recycled player slots: an ordinary colony
created in a genuinely NEVER-ALIVE slot is sufficient.

ROOT
----
Global trigger state has two very different lifetimes in `CvPlayer`:

1. the durable trigger-fired identity in `m_triggersFired`;
2. the particular EventInfo occurrence record in `m_mapEventsOccured`.

For a non-recurring trigger, authoritative future eligibility uses the FIRST state:

    if (!kTrigger.isRecurring())
    {
        if (isTriggerFired(eTrigger))
            return 0;
    }

When a trigger actually fires, `CvPlayer::applyEvent` calls:

    setTriggerFired(*pTriggeredData, true);

before applying the chosen EventInfo. For a GLOBAL trigger,
`CvPlayer::setTriggerFired(..., bOthers=true)` propagates that fired bit to EVERY
player slot:

    if (kTrigger.isGlobal())
    {
        for (int i = 0; i < MAX_CIV_PLAYERS; i++)
        {
            if (i != getID())
                GET_PLAYER((PlayerTypes)i).setTriggerFired(
                        kTriggeredData, false, false);
        }
    }

That loop does not require the destination player to be alive or ever alive. Thus a
never-used future colony slot correctly receives the durable global fired flag at the
time of the original event.

The associated EventInfo occurrence has deliberately SHORT lifetime. In
`CvPlayer::checkExpireEvent`:

    if (!kEvent.isQuest())
        return (GC.getGame().getGameTurn() - kTriggeredData.m_iTurn > 2);

and `CvPlayer::doEvents` then calls `expireEvent` and erases the occurrence from
`m_mapEventsOccured`. This does NOT clear `m_triggersFired`; existing players continue
to remember the non-recurring trigger permanently, as intended.

The defect appears when a new player is initialized in the middle of the game.
`CvPlayer::initInGame` first calls `reset(eID)`, whose reset path clears:

    m_mapEventsOccured.clear();
    ...
    m_triggersFired.clear();

It then explicitly attempts to rebuild already-fired global/team trigger state:

    // Reset all triggers at first, set those whose events have fired in next block
    FOR_EACH_ENUM(EventTrigger)
        resetTriggerFired(eLoopEventTrigger);

    FOR_EACH_ENUM(Event)
    {
        EventTriggeredData const* pEvent = NULL; // Has global trigger fired already?
        for (int i = 0; i < MAX_CIV_PLAYERS; i++)
        {
            if (i == getID())
                continue;

            pEvent = GET_PLAYER((PlayerTypes)i).getEventOccured(eLoopEvent);
            if (pEvent == NULL)
                continue;

            CvEventTriggerInfo& kTrigger = GC.getInfo(pEvent->m_eTrigger);
            if (kTrigger.isGlobal())
            {
                setTriggerFired(*pEvent, false, false);
                break;
            }
            else if (kTrigger.isTeam() && TEAMID((PlayerTypes)i) == getTeam())
            {
                setTriggerFired(*pEvent, false, false);
                break;
            }
        }
        resetEventOccured(eLoopEvent, false);
    }

The comment states the intended contract correctly — preserve global triggers that
have fired already — but the reconstruction source is wrong. It searches the
EPHEMERAL `m_mapEventsOccured` records instead of the DURABLE trigger-fired state.

Therefore:
* during the first two turns after a non-quest event, another player's occurrence
  record can allow `initInGame` to reconstruct the global fired flag;
* once `checkExpireEvent` has expired that record, the same already-fired global
  trigger becomes invisible to `initInGame` even though all existing players still
  have it in `m_triggersFired`;
* the newly initialized player's cleared fired flag remains absent.

This is a direct lifetime mismatch between the state being reconstructed and the state
used as its reconstruction source.

ORDINARY NEVER-ALIVE COLONY REACHABILITY
----------------------------------------
No F447/F473/F474 recycling setup is necessary.

`CvPlayer::getSplitEmpirePlayer` explicitly prefers a slot that has never been alive:

    for (PlayerIter<> itPlayer; itPlayer.hasNext(); ++itPlayer)
    {
        if (!itPlayer->isEverAlive())
        {
            eNewPlayer = itPlayer->getID();
            break;
        }
    }

`splitEmpire` then calls:

    kGame.addPlayer(eNewPlayer, eBestLeader, eBestCiv);

and current `CvGame::addPlayer` deliberately uses the BBAI middle-of-game initializer:

    /* Team init now handled when appropriate in player initInGame.
       Standard player init is written for beginning of game,
       it resets global random events for this player only among other flaws. */
    GET_PLAYER(eNewPlayer).initInGame(eNewPlayer);

So the very routine designed to avoid losing global event state during ordinary colony
creation still loses it whenever the EventInfo occurrence has aged out.

CURRENT-SAS DATA — MANY LIVE GLOBAL NON-RECURRING TRIGGERS
----------------------------------------------------------
This is not a dormant API branch.

A mechanical parse of current SAS `CIV4EventTriggerInfos.xml` finds 116 trigger
entries satisfying all of:

    bGlobal = 1
    bRecurring = 0
    iPercentGamesActive > 0
    iWeight != 0

Many are ordinary positive-weight random events.

For example current SAS `EVENTTRIGGER_FEDERAL_RESERVE` has:

    iPercentGamesActive = 90
    iWeight = 200
    Civic = CIVIC_FREE_MARKET
    iMinTreasury = 1000
    OrPreReq = TECH_CORPORATION
    bRecurring = 0
    bGlobal = 1

and no PythonCanDo gate.

Its two EventInfos are explicitly NON-QUEST:

    EVENT_FEDERAL_RESERVE_1: bQuest = 0, iInflationMod = -10
    EVENT_FEDERAL_RESERVE_2: bQuest = 0, iGold = -200,
                            iInflationMod = -25

So its EventInfo occurrence is guaranteed to become eligible for the ordinary
`> 2 turns` expiration rule while the global non-recurring trigger-fired bit is meant
to persist indefinitely.

Current XML also gives a dramatic 100%-active example:

    EVENTTRIGGER_MARATHON
        iPercentGamesActive = 100
        iWeight = 800
        bRecurring = 0
        bGlobal = 1

whose non-quest `EVENT_MARATHON_1` grants a Golden Age. Its Python trigger conditions
are narrower (first turn of a war with an enemy unit inside the player's territory),
so Federal Reserve is the cleaner arithmetic reproducer; Marathon demonstrates that
the affected live data is not limited to 90%-active entries or small rewards.

DETERMINISTIC CURRENT-SAS CONSEQUENCE — FEDERAL RESERVE WEIGHT BECOMES 200 INSTEAD
OF 0
------------------------------------------------------------------------------------
Use a game in the normal 90% set where `EVENTTRIGGER_FEDERAL_RESERVE` is active.

1. Player A satisfies Federal Reserve and the trigger fires normally.
2. Because the trigger is global, `setTriggerFired(..., true)` marks it fired on every
   PlayerTypes slot, including slots that have never yet been alive.
3. A chooses either Federal Reserve event. The chosen EventInfo is non-quest.
4. Let more than two turns pass. `doEvents/checkExpireEvent` erases A's
   `m_mapEventsOccured` record. A and all other existing slots still retain the durable
   `EVENTTRIGGER_FEDERAL_RESERVE` entry in `m_triggersFired`.
5. Later an ordinary player releases a valid overseas colony while a never-alive slot
   X remains available. `getSplitEmpirePlayer` deliberately chooses X.
6. `addPlayer -> X.initInGame(X) -> reset(X)` clears X's already-propagated durable
   Federal Reserve fired bit.
7. The `initInGame` reconstruction scan searches other players' EventInfo occurrence
   records. The Federal Reserve occurrence expired in step 4, so X does NOT regain the
   fired bit.
8. The parent can pass `TECH_CORPORATION` to the colony through normal colony tech
   inheritance. Later let X adopt Free Market and accumulate at least 1000 gold.
9. Call the authoritative trigger eligibility weight calculation.

Correct global non-recurring state requires:

    getEventTriggerWeight(EVENTTRIGGER_FEDERAL_RESERVE) == 0

because this trigger already fired once for the game.

Current state instead reaches the XML base weight and returns:

    200

provided its ordinary current conditions are met, because X's
`isTriggerFired(EVENTTRIGGER_FEDERAL_RESERVE)` is false.

That weight mismatch is deterministic. The subsequent random-event selection is of
course stochastic, but the supposedly globally exhausted trigger has been returned to
the live candidate pool.

If selected again, X can obtain a SECOND occurrence of the globally non-recurring
Federal Reserve event and permanently change its inflation modifier by -10, or pay 200
gold for -25. `applyEvent` changes the stored player inflation modifier directly, so
this is gameplay state, not merely an announcement or UI discrepancy.

SAVE/LOAD DOES NOT REPAIR THE NEW PLAYER
-----------------------------------------
`m_triggersFired` itself is serialized explicitly:

    uint iSize = m_triggersFired.size();
    pStream->Write(iSize);
    ...

and read back directly into the vector.

Therefore, once `initInGame` has omitted the old global trigger from colony X, saving
and loading simply serializes/restores that MISSING entry. Loading does not rerun the
midgame global-trigger reconstruction from other players.

This differs usefully from F474, whose malformed AgentIterator cache can be rebuilt by
save/load before the damaging tech discovery. F475's lost per-player fired state is
normal serialized state and survives a reload.

ANCESTRY / INTENT
-----------------
K-Mod 1.46 contains the same middle-of-game trigger reconstruction block: reset every
trigger, iterate EventInfos, query other players' `getEventOccured`, and call
`setTriggerFired` only when an extant occurrence belongs to a global or same-team
trigger. Current AdvCiv/SAS retains that BBAI lineage.

The current `CvGame::addPlayer` comment states why BBAI introduced this special path:
standard beginning-of-game player initialization was unsuitable in midgame because,
among other flaws, it "resets global random events for this player only."

Stock BtS/Civ4CE still follows the older pattern:

    GET_TEAM(eTeam).init(eTeam);
    GET_PLAYER(eNewPlayer).init(eNewPlayer);

so the broader late-player/global-trigger problem originates upstream. BBAI/K-Mod
attempted to repair it with `initInGame`, but reconstructed durable trigger identity
from short-lived occurrence records and therefore only repaired the first two turns
after ordinary non-quest events.

F475 is best classified as an inherited midgame-player event-state defect with an
incomplete BBAI repair, retained by K-Mod -> AdvCiv -> SAS.

DUPLICATE / ALBUM RECONCILIATION
--------------------------------
The album already contains a different event-expiration robustness note: for copied
QUEST records, `checkExpireEvent`'s obsolete-tech branch can inspect the holder's team
rather than the event originator's team. Current XML masks that path because no
EventInfo is both Quest and Global. F475 uses ordinary NON-QUEST events and does not
rely on that asymmetry.

The album also preserves the data-dormant `setEventOccured` team-propagation iterator
inversion. F475 is GLOBAL-trigger state, is live under current XML, and occurs even
when EventInfo records are intentionally local.

F457 / KI#780 is the World News identity leak in global PickPlayer trigger
announcements. It concerns observer visibility of an event that legitimately fires.
F475 instead loses the durable global one-shot gate for a newly initialized player and
can make the trigger eligible to fire again.

F447/F473/F474 concern recycled colonial identities. F475 deliberately reproduces
with a NEVER-ALIVE player slot, so none of those roots is required.

No previous album root covers `initInGame` reconstructing persistent global trigger
state from EventInfo records that normally expire after two turns.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not reconstruct durable trigger-fired identity from `m_mapEventsOccured`.

For each EventTriggerType during `initInGame`, derive inheritance from the durable
`isTriggerFired(eTrigger)` state of other players:

* if the trigger is global, preserve it for the new player when any authoritative
  existing player/slot records it as fired;
* if the trigger is team-scoped, preserve it when an appropriate teammate records it
  as fired;
* local trigger-fired history from a recycled old identity must NOT be inherited just
  because that slot once fired it.

A small dedicated helper that records the trigger type without replaying announcements
or Python after-event callbacks would be cleaner than fabricating an old
`EventTriggeredData` merely to call `setTriggerFired`.

An even stronger architecture would keep game-global and team-global one-shot trigger
state at game/team scope instead of duplicating it into each player's vector, but a
narrow `initInGame` repair is sufficient for this root.

REGRESSION TEST
---------------
1. Enable events and choose a setup where `EVENTTRIGGER_FEDERAL_RESERVE` is active.
2. Fire Federal Reserve for Player A and assert every relevant player slot reports
   `isTriggerFired(EVENTTRIGGER_FEDERAL_RESERVE)` true.
3. Advance at least three turns until A no longer has either Federal Reserve EventInfo
   in `m_mapEventsOccured`.
4. Create an ordinary colony in a never-alive Player X slot.
5. Immediately assert X still reports the global Federal Reserve trigger as fired.
6. Give/retain Corporation, adopt Free Market and set X gold >= 1000.
7. Assert `getEventTriggerWeight(EVENTTRIGGER_FEDERAL_RESERVE) == 0`.
8. Save/load after colony creation and repeat the assertion.
9. As a positive control, ensure genuinely local non-recurring triggers from an old
   recycled identity are not inherited into a fresh colony merely because that old
   identity once fired them.
10. Repeat with a current bGlobal=1 / bRecurring=0 100%-active trigger such as Marathon
    to ensure the fix is generic rather than Federal-Reserve-specific.

NUMBERING
---------
F475 / KI#798 is CONFIRMED.
F476 / provisional KI#799 is the next unused root.


CURRENT STONE AFTER C031-WIP198
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F475 / KI#798 CONFIRMED.
F476 / provisional KI#799 UNUSED.
Latest physical checkpoint: C031-WIP198.
No repository source files modified.

NEXT
----
Continue from F476 unused.

First finish the remaining `initInGame` random-event inheritance siblings around F475:
team-trigger durability, countdown/AdditionalEvents state, trigger callbacks during
reconstruction and any global-vs-local state that can be one-way lost when a normal
midgame player is created. Keep the already-data-dormant team EventInfo propagation
regression separate unless current producers emerge.

Then return to the historical-fix x low-semantic-coverage authoritative `CvPlayer.cpp`
pass, prioritizing persistent state with external consumers, death/revival/team
transitions, vote/religion helpers and current-data arithmetic.

Require current SAS reachability + concrete gameplay consequence + ancestry/comparison
+ semantic duplicate proof before spending F476. Create C031-WIP199 at the next
meaningful settled tranche whether F476 is confirmed or remains unused.

END C031-WIP198



======================================================================
C031-WIP199 — QUEUE 004 CVPLAYER DEEP RE-AUDIT: POST-F475 EVENT CLOSURE + F476 / KI#799 STATE-RELIGION BUILDING-COMMERCE CACHE INVALIDATION
======================================================================

STATUS
------
Protected deep-method re-audit Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete in overall album accounting.
F476 / provisional KI#799 CONFIRMED below.
F477 / provisional KI#800 is now UNUSED.
Latest physical checkpoint: C031-WIP199.
No repository source files modified.

POST-F475 IMMEDIATE EVENT-INHERITANCE SIBLING CLOSURE
-----------------------------------------------------
Before leaving the `initInGame` random-event family, the immediate F475 siblings were
reconciled against current SAS data rather than multiplied into artificial roots.

* Current SAS has ZERO `bTeam=1` EventTriggerInfos and ZERO `bTeam=1` EventInfos.
  The analogous team-state reconstruction branch therefore remains dormant under
  shipped data.
* Current SAS EventTriggerInfo XML has ZERO nonempty trigger-level PythonCallback
  entries. Reconstructing a fired trigger through `setTriggerFired` therefore cannot
  currently replay a second trigger callback as a separate live consequence.
* Event countdowns / AdditionalEvents are player-local follow-up state belonging to
  the player that actually received the event. They are not a second game-global
  one-shot identity that a later colony should inherit.

So F475 remains the live durable trigger-state root from this family. F476 was kept
unused while the audit returned to low-semantic-coverage authoritative player state.

F476 / KI#799 — STATE-RELIGION BUILDING-COMMERCE CACHE IS NOT INVALIDATED WHEN THE PLAYER'S STATE RELIGION OR EMPIRE-WIDE BONUS CHANGES
-------------------------------------------------------------------------------------------------------------------------------------

ROOT / CACHED FORMULA
---------------------
`CvCity::getBuildingCommerceByBuilding` includes an empire/player dependency inside the
city's cached building-commerce total:

    if (kBuilding.getReligionType() != NO_RELIGION &&
        kBuilding.getReligionType() == GET_PLAYER(getOwner()).getStateReligion())
    {
        iCommerceRate += GET_PLAYER(getOwner()).
                getStateReligionBuildingCommerce(eCommerce) *
                getNumActiveBuilding(eBuilding);
    }

The result is accumulated by `CvCity::updateBuildingCommerce(eCommerce)` into:

    m_aiBuildingCommerce[eCommerce]

and the ordinary commerce-rate path subsequently reads that cached value through
`getBuildingCommerce(eCommerce)`.

Two different player-level inputs can therefore change the correct cached value in
EVERY city without changing the city's own buildings:

1. `CvPlayer::getStateReligion()` — which religion's buildings qualify;
2. `CvPlayer::getStateReligionBuildingCommerce(eCommerce)` — how much qualifying
   commerce each active state-religion building receives.

Current code fails to invalidate/recompute `m_aiBuildingCommerce` for BOTH transitions.

MISSING INVALIDATION 1 — STATE RELIGION CHANGES
-----------------------------------------------
`CvPlayer::setLastStateReligion` changes the effective player religion and refreshes:

    updateMaintenance();
    updateReligionHappiness();
    updateReligionCommerce();

but does NOT call `updateBuildingCommerce()`.

`updateReligionCommerce()` is a different cache. Its city implementation recomputes
`m_aiReligionCommerce` from ReligionInfo state/holy-city commerce and then calls
`updateCommerce`; it never recomputes `m_aiBuildingCommerce`.

Therefore a temple/monastery/etc. of the OLD state religion can continue contributing
an empire-wide state-religion-building bonus from the stale building cache, while a
religious building of the NEW state religion can remain missing that bonus.

MISSING INVALIDATION 1B — CIVICS CHANGE THE EFFECTIVE STATE RELIGION WITHOUT CHANGING `m_eLastStateReligion`
-------------------------------------------------------------------------------------------------------------
`getStateReligion()` is defined as:

    return (isStateReligion() ? getLastStateReligion() : NO_RELIGION);

so the same cached building formula also changes when a civic transition toggles
whether the player is allowed to have a state religion.

`CvPlayer::setCivics` even snapshots the old effective religion before changing the
civic and logs the old/new effective state religion afterward. Its `processCivics`
path calls:

    changeStateReligionCount(civic.isStateReligion() ? iChange : 0);

`changeStateReligionCount` refreshes maintenance, religion happiness and religion
commerce, but — like `setLastStateReligion` — never calls `updateBuildingCommerce`.

Current SAS Religion civics make this live: Prehistoric Religion, Organized Religion,
Pacifism and Theocracy have `bStateReligion=1`, while Free Religion has
`bStateReligion=0`. A zero-anarchy switch into or out of Free Religion therefore changes
`getStateReligion()` immediately while leaving the religious-building commerce cache on
the old effective state.

This is the same F476 cache invariant, not a separate F477: the external input to
`getBuildingCommerceByBuilding` is the player's EFFECTIVE state religion, and both
`setLastStateReligion` and `changeStateReligionCount` are producers of that input.

MISSING INVALIDATION 2 — EMPIRE-WIDE BONUS AMOUNT CHANGES
---------------------------------------------------------
`CvPlayer::processBuilding` changes the player-wide source through:

    changeStateReligionBuildingCommerce(c,
        kBuilding.getStateReligionCommerce(c) * iChange);

but the mutator only does:

    m_aiStateReligionBuildingCommerce.add(eCommerce, iChange);
    updateCommerce(eCommerce);

`CvPlayer::updateCommerce(eCommerce)` merely calls `CvCity::updateCommerce` for every
city. That recomputes the final rate from the city's EXISTING cached
`m_aiBuildingCommerce`; it does not call `CvCity::updateBuildingCommerce`.

When a city itself gains/loses a building, `CvCity::processBuilding` eventually calls:

    updateBuildingCommerce();

at the end of that ONE city's processing. This repairs the producing/acquired city,
but not the owner's other cities whose religious buildings are affected by the new
player-wide bonus.

The team and game `processBuilding` hooks do not repair this: they only process their
own vote/team-wide building effects and do not perform an empire-wide building-commerce
recount.

WHY ORDINARY WONDER COMPLETION ALONE IS NOT THE BEST REPRO
----------------------------------------------------------
There is an important self-heal control that prevents overstatement.

`CvGame::setGameTurn` calls:

    updateBuildingCommerce();

which loops all alive players and all their cities. Thus a stale building-commerce
cache is globally rebuilt at each game-turn boundary.

Also, in sequential turns `CvPlayer::doTurn` books gold/research BEFORE city
`doTurn` / `doProduction`. Therefore simply completing Spiral Minaret or Sankore at
end-of-turn normally creates a transient stale state after that turn's economy has
already been booked, and the next global turn-boundary refresh can repair it before the
owner's next normal economic turn.

That closure is why F476 is NOT based merely on "build wonder in City A, City B stale."

CURRENT LIVE REPRO 1 — ZERO-ANARCHY STATE-RELIGION CONVERSION BOOKS WRONG GOLD
-------------------------------------------------------------------------------
The state-religion transition happens at a different and gameplay-relevant time.

Current turn handling for a normal human sequential turn is:

* player is active and may issue a religion conversion during the active turn;
* on ending the turn, `CvPlayer::setTurnActive(false, true)` calls `CvPlayer::doTurn()`;
* `CvPlayer::doTurn()` performs:

      AI_assignWorkingPlots();
      verifyGoldCommercePercent();
      doGold();
      doResearch();
      doEspionagePoints();
      FOR_EACH_CITY ... city->doTurn();

* only the later game-round transition reaches `CvGame::setGameTurn` and its global
  `updateBuildingCommerce()` self-heal.

Current SAS has straightforward zero-anarchy conversion routes:

* `TRAIT_SPIRITUAL` has `<iMaxAnarchy>0</iMaxAnarchy>`, and
  `getReligionAnarchyLength()` immediately returns 0 when `getMaxAnarchyTurns()==0`;
* independently, `getReligionAnarchyLength()` returns 0 during a Golden Age.

A compact current-SAS reproduction is therefore:

1. Own `BUILDING_SPIRAL_MINARET`.
2. Use Hinduism as the current state religion.
3. Have one active `BUILDING_HINDU_TEMPLE` in an ordinary city. Current XML gives that
   building `ReligionType=RELIGION_HINDUISM` and `ObsoleteTech=NONE`.
4. Have Judaism present somewhere so conversion to Judaism is legal, but do not need a
   Jewish temple for the repro.
5. Be Spiritual or be in a Golden Age, so the conversion creates zero anarchy.
6. During the player's active turn, convert from Hinduism to Judaism.
7. `setLastStateReligion(JUDAISM)` changes the state religion but does not rebuild city
   building commerce. The Hindu Temple's cached building-commerce contribution still
   includes Spiral Minaret's old +2 Gold bonus.
8. End the turn. Before any game-turn-boundary building-commerce rebuild,
   `CvPlayer::doTurn -> doGold -> calculateGoldRate` consumes the cached player/city
   commerce rate.

With no other Gold-rate modifiers in the test city, the player is credited 2 Gold that
should have disappeared when Hinduism stopped being the state religion. More old-state
religion buildings or city Gold modifiers can enlarge the error.

The next turn-boundary recount repairs the CACHE, but it does not subtract the Gold
already booked in step 8. The one-turn economic error is therefore a permanent gameplay
consequence, not merely a stale tooltip.

The same root can invert direction: buildings of the new state religion remain missing
the bonus until the recount. Current Sankore (+3 Research) and Natya Shastra (+2
Culture) allow analogous research/culture manifestations.

CURRENT LIVE REPRO 1B — ZERO-ANARCHY SWITCH TO FREE RELIGION LEAVES OLD STATE-RELIGION BUILDING BONUSES ACTIVE
----------------------------------------------------------------------------------------------------------------
The civic producer gives an equally direct current path without changing
`m_eLastStateReligion` at all.

1. Own Spiral Minaret and an active building of the remembered state religion.
2. Run any current `bStateReligion=1` Religion civic.
3. During a Spiritual/Golden-Age zero-anarchy window, switch the Religion civic to
   current `CIVIC_FREE_RELIGION` (`bStateReligion=0`).
4. `processCivics -> changeStateReligionCount(-1)` makes `getStateReligion()` become
   `NO_RELIGION`, but only refreshes maintenance/religion-happiness/religion-commerce.
5. The old religious building remains in `m_aiBuildingCommerce` with the Spiral bonus.
6. Ending the turn reaches `doGold()` before the next game-turn-wide recount and books
   the stale Gold exactly as in repro 1.

The reverse transition can leave the remembered religion's buildings missing the bonus
until the next recount.

CURRENT LIVE REPRO 2 — CAPTURING SPIRAL MINARET MID-TURN MISSES ITS BONUS IN OTHER CITIES
------------------------------------------------------------------------------------------
The bonus-AMOUNT mutator itself also has an ordinary mid-turn producer.

Current `BUILDING_SPIRAL_MINARET` has:

    bNeverCapture = 0
    iConquestProb = 100
    StateReligionCommerces = [2]

and `CommerceTypes` order begins:

    COMMERCE_GOLD,
    COMMERCE_RESEARCH,
    COMMERCE_CULTURE,
    COMMERCE_ESPIONAGE

so `[2]` is +2 Gold per active building of the player's state religion.

During `CvPlayer::acquireCity`, surviving captured buildings are restored with
`CvCity::setNumRealBuildingTimed`. That immediately calls `CvCity::processBuilding`,
which reaches the new owner's `CvPlayer::processBuilding` and therefore
`changeStateReligionBuildingCommerce(+2)`.

Suppose the capturing player already has a state-religion temple in another city.
Capturing the Spiral Minaret city during the active military phase changes the correct
player-wide temple bonus immediately, but the mutator performs only `updateCommerce`.
The captured city later refreshes its OWN building-commerce cache at the end of its
building processing; the other city does not.

When the human ends the same turn, `doGold()` therefore books Gold before the next
`setGameTurn` global refresh, and the other city's temple can miss the newly acquired
+2 Gold for that turn.

Current Sankore and Natya Shastra are likewise fully capturable (`bNeverCapture=0`,
`iConquestProb=100`) and exercise the same cache dependency for Research/Culture.

CURRENT SAS DATA
----------------
Current `CIV4BuildingInfos.xml` has three live nonzero
`StateReligionCommerces` producers:

    BUILDING_SPIRAL_MINARET   [2]       -> +2 Gold
    BUILDING_SANKORE          [0,3]     -> +3 Research
    BUILDING_NATYA_SHASTRA    [0,0,2]   -> +2 Culture

All three have `ObsoleteTech=NONE` in current SAS.

Religious buildings are likewise live ordinary data. For example
`BUILDING_HINDU_TEMPLE` has:

    ReligionType   = RELIGION_HINDUISM
    PrereqReligion = RELIGION_HINDUISM
    ObsoleteTech   = NONE

so the cached formula has direct current producers/consumers without hypothetical XML.

CACHE-CONTROL COMPARISON INSIDE CURRENT CODE
--------------------------------------------
The omission is especially persuasive because neighboring dependencies of the SAME
`getBuildingCommerceByBuilding` formula already trigger the stronger refresh.

Global religion/corporation building commerce depends on world religion/corporation
counts. Current player count mutators call:

    CvGame::updateBuildingCommerce();

which deliberately recomputes every alive player's city cache.

City-specific `setBuildingCommerceChange` calls:

    updateBuildingCommerce(eCommerce);

when an active building is affected.

So the codebase already treats this formula as a derived cache requiring explicit
recomputation when its external inputs change. The state-religion identity and
StateReligionCommerce amount are the missed inputs.

SERIALIZATION / SELF-HEAL CHARACTERIZATION
-------------------------------------------
`m_aiBuildingCommerce` is serialized directly in `CvCity::read/write`; however F476
does not need save/load persistence to clear the gameplay threshold.

The global `CvGame::setGameTurn -> updateBuildingCommerce` call gives a normal eventual
self-heal. The important point is that the zero-anarchy conversion and captured-wonder
paths can occur AFTER the current game-turn refresh and BEFORE that player's
`doGold`/`doResearch` / city culture processing. The stale value can therefore be
CONSUMED and booked before the self-heal. Rebuilding the cache on the following turn
cannot refund/reverse that already-applied output.

ANCESTRY
--------
This is inherited engine behavior rather than a SAS-created regression.

Current base AdvCiv 1.14 has the same
`changeStateReligionBuildingCommerce -> updateCommerce` implementation and the same
state-religion-dependent building-commerce formula.

K-Mod 1.46 likewise changes `m_aiStateReligionBuildingCommerce` and calls only
`updateCommerce(eIndex)`, while `setLastStateReligion` refreshes maintenance, religion
happiness and religion commerce but not building commerce.

Civ4CE/stock-lineage code contains the same building-commerce dependency and the same
`changeStateReligionBuildingCommerce -> updateCommerce` pattern. Thus the missing
cache invalidation predates AdvCiv/SAS and survives through the upstream lineage.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album contains no previous root for:

* `changeStateReligionBuildingCommerce`;
* `m_aiBuildingCommerce` invalidation from state-religion identity;
* Spiral Minaret / Sankore state-religion-building bonuses staying on the wrong
  religious buildings;
* mid-turn wonder acquisition leaving other cities' state-religion building commerce
  stale.

Earlier `setLastStateReligion` mentions concern conversion legality, diplomacy/message
coordinates and other religion-state behavior, not this city building-commerce cache.

The earlier state-religion BUILDING HAPPINESS audit is also a different derived value
and had its own refresh chain. F476 specifically concerns cached building COMMERCE.

F445 and other rank/cache findings concern different city/player caches and different
invalidators. F476 has its own current data and direct economic consumer.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Both external inputs to the cached formula need explicit building-commerce
recomputation.

1. After changing `m_eLastStateReligion` in `setLastStateReligion`, call player-wide
   `updateBuildingCommerce()` (after the new religion is stored). This makes every
   city's religious-building qualification match the new state religion immediately.

2. When `changeStateReligionCount` crosses a boundary that changes the effective
   `getStateReligion()` (most notably state-religion civic <-> Free Religion), also
   rebuild player building commerce. Calling the refresh for every raw +1/-1 during
   civic processing is safe but can be avoided by comparing old/new effective state
   religion once in `setCivics` and refreshing only if it changed.

3. In `changeStateReligionBuildingCommerce(eCommerce, iChange)`, recompute building
   commerce for every city for that commerce type rather than merely calling
   `updateCommerce(eCommerce)`. A narrow per-commerce player helper would avoid
   recomputing unrelated commerce types; the existing full `updateBuildingCommerce()`
   is the simplest conservative repair if this rare transition does not warrant a new
   overload.

Do not remove the existing turn-boundary global recount; it also services time-based
building-commerce dependencies such as delayed commerce doubling and remains a useful
safety rebuild.

REGRESSION TESTS
----------------
A. Religion-switch test:

1. Own Spiral Minaret and one Hindu Temple; have Judaism available as a legal target.
2. Ensure zero religion anarchy (Spiritual or Golden Age).
3. Populate/verify the Hindu Temple's building-commerce cache while Hinduism is state
   religion.
4. Convert to Judaism during the active turn.
5. Immediately assert the city's Gold building commerce has lost the +2 Spiral bonus.
6. End turn and verify `doGold` does not credit the stale +2.
7. Add a Jewish religious building as a positive-control variant and verify the bonus
   moves to it immediately.

B. Civic effective-religion test:

1. Own Spiral Minaret and a building of the remembered state religion.
2. In a zero-anarchy window, switch from a current `bStateReligion=1` Religion civic
   to `CIVIC_FREE_RELIGION`.
3. Assert the old religious building immediately loses the state-religion building
   bonus and that end-turn Gold does not book it.
4. Switch back in another zero-anarchy window and verify the remembered religion's
   buildings immediately regain the bonus.

C. Captured-wonder test:

1. Capturer has a state religion and a matching temple in City B.
2. Enemy city contains Spiral Minaret.
3. Capture that city during the capturer's active turn; current XML guarantees the
   wonder is capturable at 100% conquest probability.
4. Immediately assert City B's building-commerce cache gains +2 Gold.
5. End turn and verify the bonus is included in that turn's booked Gold.
6. Repeat with Sankore / Research to ensure the fix is commerce-type generic.

D. Turn-boundary control:

After deliberately producing the old stale state in an unpatched build, verify that
`CvGame::setGameTurn` does repair the cache on the next turn. This preserves the
important distinction between F476's consumed one-turn error and an indefinitely stale
cache.

NUMBERING
---------
F476 / KI#799 is CONFIRMED.
F477 / provisional KI#800 is the next unused root.

CURRENT STONE AFTER C031-WIP199
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F476 / KI#799 CONFIRMED.
F477 / provisional KI#800 UNUSED.
Latest physical checkpoint: C031-WIP199.
No repository source files modified.

NEXT
----
Continue from F477 unused.

First finish the immediate `m_aiBuildingCommerce` dependency siblings around F476:
other player/team/game state read inside `getBuildingCommerceByBuilding`, acquisition /
loss / obsolescence timing, state-religion civic transitions and whether any external
input lacks an invalidator independent of the two F476 producers. Keep the per-turn
`CvGame::updateBuildingCommerce` safety rebuild as an explicit control and require a
pre-rebuild gameplay consumer before promoting any sibling.

Then return to the historical-fix x low-semantic-coverage authoritative `CvPlayer.cpp`
pass, prioritizing persistent state with external consumers, death/revival/team
transitions, vote/religion helpers and current-data arithmetic. Continue semantic
album duplicate checking rather than relying on literal function-name coverage.

Require current SAS reachability + concrete gameplay consequence + ancestry/comparison
+ semantic duplicate proof before spending F477. Create C031-WIP200 at the next
meaningful settled tranche whether F477 is confirmed or remains unused.

END C031-WIP199

===============================================================================
C031-WIP200 — POST-WIP199 CONTINUATION
Queue 004: CvGameCoreDLL/CvPlayer.cpp
F477 / KI#800 CONFIRMED
F478 / provisional KI#801 UNUSED
===============================================================================

POST-WIP199 CURSOR RECONCILIATION
---------------------------------
The immediate continuation was the recycled-player cleanup path, specifically the
BETTER_BTS_AI_MOD `CvPlayer::resetPlotAndCityData()` routine used by
`CvGame::addPlayer -> CvPlayer::initInGame` when a mid-game colony reuses a dead
PlayerTypes slot.

The audit first rechecked the intended cleanup surface. Current SAS still carries the
12/30/08 BBAI routine with the explicit contract/comment:

    Reset all data for this player stored in plot and city objects

For every plot it clears:

    plot culture indexed by recycled player
    found value indexed by recycled player

and, for every city, it clears:

    city culture indexed by recycled player
    revolt count indexed by recycled player
    EverOwned indexed by recycled player
    TradeRoute indexed by recycled player

That is useful cleanup, but it only handles fields whose KEY is the recycled
PlayerTypes. It does not scrub persistent city/plot fields whose STORED VALUE is that
PlayerTypes identity.

This difference matters because a recycled player slot does not receive a generation
number or historical identity token. If extinct old player X and unrelated new colony
Z use the same numeric PlayerTypes slot, any untouched historical field containing X
now compares equal to Z.

F477 / KI#800 — RECYCLED DEAD PLAYER SLOTS ALIAS PERSISTENT HISTORICAL PlayerTypes
                  REFERENCES TO THE NEW COLONY
----------------------------------------------------------------------------------
STATUS: CONFIRMED.

ROOT
----
`CvPlayer::getSplitEmpirePlayer` first tries a never-alive player slot. If none exists,
current AdvCiv-SAS deliberately allows a HUMAN splitter to reuse an `EVER_ALIVE` dead
player:

    /* <kekm.24> Reusing a defeated player might not work correctly. advc:
       Allow human players to try it, but don't let the AI wreck the game somehow. */
    ...
    if (!itPlayer->isAlive())
        eNewPlayer = itPlayer->getID();

The reuse path changes the slot's leader/civilization and calls
`CvGame::addPlayer -> CvPlayer::initInGame -> resetPlotAndCityData`.

However, map/city objects can contain bare PlayerTypes VALUES that refer to the old
historical identity. Important current examples include:

    CvCity::m_eOriginalOwner
    CvCity::m_ePreviousOwner
    CvCity::m_aeBuildingOriginalOwner[BuildingTypes]
    CvPlot::m_aiRevealedOwner[TeamTypes]

`resetPlotAndCityData()` does not search for values equal to the recycled slot and does
not clear/remap them. The old numeric identity therefore aliases the new colony.

This root is independent of the immediately preceding recycled-player findings. A dead
SINGLETON old team is sufficient here, so F447's live-team skip and F473's dead
multi-member-team reset failure need not occur. The concrete consequences below do not
need F474's AgentIterator duplicate, F475's event reconstruction error, or F476's
building-commerce invalidation problem.

PRIMARY CURRENT-SAS REPRO A — CAPTURED GREAT LIBRARY BECOMES "ORIGINAL" TO AN
UNRELATED RECYCLED COLONY
--------------------------------------------------------------------------------
Current city building commerce contains this authoritative gate:

    if (kBuilding.isCommerceChangeOriginalOwner(eCommerce) &&
        getBuildingOriginalOwner(eBuilding) != getOwner())
    {
        return 0;
    }

Building original owner is stored as a serialized PlayerTypes in
`m_aeBuildingOriginalOwner`.

`CvPlayer::acquireCity` explicitly copies each surviving building's original owner from
the old city and restores it to the new city through `setNumRealBuildingTimed`. So
capture intentionally preserves the historical builder identity.

Current SAS XML gives this path abundant live data. 51 BuildingInfos have a nonzero
`CommerceChangeOriginalOwners` entry; 50 of those are fully capturable with
`iConquestProb=100` and `bNeverCapture=0`.

A particularly simple stable example is:

    BUILDING_GREAT_LIBRARY
    CommerceChangeOriginalOwners = [0, 0, 1]
    ObsoleteSafeCommerceChanges = [0, 0, 6]
    iConquestProb = 100
    bNeverCapture = 0
    ObsoleteTech = NONE

Commerce order is Gold, Research, Culture, Espionage, so the Great Library has +6
Culture that is granted only when the city's current owner equals the building's
stored original-owner PlayerTypes.

Supported repro:

1. Old player X builds the Great Library.
2. Player Y captures that city. The building survives deterministically under current
   XML and retains `BuildingOriginalOwner = X`.
3. X is later fully eliminated while Y still owns the Great Library city.
4. Exhaust all never-alive player slots, then have a human player split a colony.
   `getSplitEmpirePlayer` may recycle dead X's PlayerTypes slot into unrelated new
   civilization Z. Use a dead singleton old team so F447/F473 are not prerequisites.
5. BBAI `resetPlotAndCityData()` clears X-indexed culture/revolt/etc., but does not
   touch Y's city's stored `BuildingOriginalOwner = X`.
6. Z later captures the Great Library city from Y. `acquireCity` again preserves the
   building original-owner value.
7. Numerically, `getBuildingOriginalOwner(GREAT_LIBRARY) == getOwner()` now succeeds,
   because both are the recycled X/Z PlayerTypes slot.
8. Z therefore receives the Great Library's +6 Culture as though Z had historically
   built it, despite being a completely different civilization created after X's
   extinction.

The bad value is serialized through `m_aeBuildingOriginalOwner.read/write`, so save/load
does not distinguish old X from new Z or self-heal the alias.

PRIMARY CURRENT-SAS REPRO B — STALE CITY ORIGINAL OWNER CAN DRIVE NEW COLONY'S
`CitiesLost` NEGATIVE
--------------------------------------------------------------------------------
`CvCity::m_eOriginalOwner` is another serialized PlayerTypes value intentionally
preserved across capture. `CvPlayer::acquireCity` restores it into the replacement city
with:

    kNewCity.setOriginalOwner(eOriginalOwner);

The city-loss bookkeeping then contains:

    if (pOldCity->getOriginalOwner() == pOldCity->getOwner())
        GET_PLAYER(pOldCity->getOriginalOwner()).changeCitiesLost(1);
    else if (pOldCity->getOriginalOwner() == getID())
        GET_PLAYER(pOldCity->getOriginalOwner()).changeCitiesLost(-1);

Player reset correctly sets the new/recycled player's `m_iCitiesLost = 0`.
`changeCitiesLost` simply adds its delta; it has no generation check, clamp or
historical-owner validation.

Supported repro:

1. Old X founds City C.
2. Y captures C, preserving `OriginalOwner = X`.
3. X is fully eliminated.
4. A later human colony Z reuses X's dead PlayerTypes slot.
5. Z starts with `CitiesLost = 0`, but City C still stores original owner X.
6. Z captures C from Y.
7. The second `acquireCity` branch sees the stale numeric X value as equal to new Z's
   `getID()` and executes `changeCitiesLost(-1)`.
8. Z now has `CitiesLost = -1` despite never having lost a city.

This counter is serialized directly. It is also authoritative gameplay state when Max
City Elimination is enabled:

    getCitiesLost() >= getMaxCityElimination()

can eliminate a player. Starting Z at -1 means it can survive one additional qualifying
loss compared with the configured limit once it later founds and loses its own cities.

SAME-ROOT SUPPORTING MANIFESTATIONS
-----------------------------------
A. `CvCity::m_ePreviousOwner`

`m_ePreviousOwner` is serialized and is not scrubbed by player-slot recycling.
Current `CvCity::canCultureFlip` uses it under the visible/supported
`GAMEOPTION_NO_FLIPPING_AFTER_CONQUEST` rule:

    getPreviousOwner() == NO_PLAYER ||
    TEAMID(getPreviousOwner()) != TEAMID(eToPlayer)

A city directly captured from extinct X can therefore later treat unrelated recycled
Z as its previous owner solely because Z reused X's PlayerTypes slot. The AI city-trade
denial code also tests `kCity.getPreviousOwner() == eToPlayer`, so the alias is not
merely historical UI metadata.

B. `CvPlot::m_aiRevealedOwner[TeamTypes]`

This serialized fog-of-war memory stores a PlayerTypes VALUE. Team-side recycled-player
cleanup clears only the row for the recycled TEAM:

    kPlot.setRevealedOwner(getID(), NO_PLAYER);

It does not scan other observers' stored revealed-owner values for the extinct player.
Therefore another team O can have a fogged plot whose last-seen owner is old X. After
X's slot becomes Z, that unchanged numeric value resolves through `GET_PLAYER` to Z.

Current plot hover code does exactly that when a revealed tile is not currently
visible:

    CvPlayer const& kRevealOwner = GET_PLAYER(eRevealedOwner);
    ... kRevealOwner.getCivilizationDescription()

and minimap coloring likewise resolves the remembered owner through
`GET_PLAYER(getRevealedOwner(...)).getKnownPlayerColor()`.

Thus old fog memory can spontaneously relabel extinct X's observed territory with the
new colony Z's identity/color. Multiple AI revealed-owner consumers also receive the
same aliased identity.

These are manifestations of the same missing historical-ID sanitization, not separate
F numbers.

ANCESTRY / WHY THE ROOT EXISTS
------------------------------
Stock BtS/Civ4CE already supports recycling a dead PlayerTypes slot when no never-alive
slot remains in `getSplitEmpirePlayer`. Stock `CvGame::addPlayer` reinitializes the
selected team/player but bare historical PlayerTypes values in other cities/plots have
no generation mechanism.

The 12/30/08 BETTER_BTS_AI change introduced `resetPlotAndCityData()` as an explicit
mid-game recycled-player cleanup. K-Mod 1.46 still has the same field list and comment;
AdvCiv 1.14 and current SAS retain it.

That cleanup repairs per-player-indexed state but never became a comprehensive scrub of
PlayerTypes-valued historical references. AdvCiv's own `<kekm.24>` comment now warns
that reusing a defeated player "might not work correctly" and restricts the fallback
to human splitters, but the supported human path remains live.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album through WIP199 contains no prior root for:

* `CommerceChangeOriginalOwners` aliasing after PlayerTypes reuse;
* building original-owner IDs becoming a new colony;
* `m_eOriginalOwner` decrementing the recycled colony's `CitiesLost` counter;
* `m_ePreviousOwner` or other observers' `m_aiRevealedOwner` changing semantic
  identity after dead-slot reuse.

F447 is different: a dead PLAYER on a still-alive team can bypass `addPlayer` entirely.
F473 is different: a recycled player on a fully dead MULTI-MEMBER old team enters
`initInGame`, but the team reset is skipped due to dead assigned teammates.
F474 is a duplicate AgentIterator EVER_ALIVE insertion.
F475 is lost global trigger-fired inheritance.
F476 is state-religion building-commerce cache invalidation.

F477 can be reproduced with a dead singleton old team and therefore does not depend on
any of those roots.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The safest fix is architectural rather than an ever-growing scrub list:

1. Do not recycle an `EVER_ALIVE` PlayerTypes slot for a new civilization identity.
   If no never-alive slot exists, return `NO_PLAYER` from the fallback instead of
   reusing a defeated player.

This avoids generation-alias problems in every present and future PlayerTypes-valued
historical reference, including structures outside CvPlayer's 2008 cleanup inventory.

If dead-slot reuse must remain supported, then the engine needs a comprehensive
sanitization/remapping pass BEFORE the slot is given the new identity. At minimum audit
and clear/remap every persistent external value equal to the recycled PlayerTypes,
including:

    city original owner
    city previous owner
    building original owner for every building
    every team's plot revealed-owner memory

and then audit other derived/persistent map state such as second-owner and plot-group
references. Because PlayerTypes has no generation tag, adding only the four currently
proven cases risks leaving another identity alias behind.

REGRESSION TESTS
----------------
A. Great Library identity test

1. X builds Great Library; Y captures it.
2. Eliminate X.
3. Exhaust never-alive slots; human colony creation attempts to recycle X.
4. Under the preferred fix, X must not be reused at all.
5. If recycle support is retained, let new Z reuse X and later capture the Great
   Library city.
6. Assert `getBuildingOriginalOwner(GREAT_LIBRARY)` does not become semantically Z and
   that Z receives 0 original-owner-only Great Library Culture rather than +6.

B. CitiesLost test

1. X founds C; Y captures C; eliminate X.
2. Recycle X into new Z if recycle support remains.
3. Assert Z starts at `CitiesLost == 0`.
4. Z captures C from Y.
5. Assert Z remains at `CitiesLost == 0`, never -1.
6. Repeat with Max City Elimination N and verify Z gets exactly N qualifying future
   losses, not N+1.

C. PreviousOwner control

With No City Flipping after Conquest enabled, retain a city whose previous owner was old
X, recycle X into unrelated Z, and assert Z is not treated as that city's historical
previous owner for flip/trade-denial logic.

D. RevealedOwner control

Let observer O reveal an X-owned plot, lose current visibility, eliminate X, then
recycle X into Z. Assert O's old fog memory does not display/AI-resolve that plot as Z.

NUMBERING
---------
F477 / KI#800 is CONFIRMED.
F478 / provisional KI#801 is the next unused root.

CURRENT STONE AFTER C031-WIP200
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F477 / KI#800 CONFIRMED.
F478 / provisional KI#801 UNUSED.
Latest physical checkpoint: C031-WIP200.
No repository source files modified.

NEXT
----
Continue from F478 unused.

First finish the immediate F477 sibling inventory instead of assuming the four proven
fields are exhaustive. In particular inspect:

* `CvPlot::m_eSecondOwner` under `OWN_EXCLUSIVE_RADIUS` and whether clearing recycled
  player culture without `updateCulture` can leave it aliased;
* `CvPlot::m_aiPlotGroup[PlayerTypes]`, whose old numeric group id is not explicitly
  cleared by `CvPlayer::resetPlotAndCityData`, and whether `updatePlotGroups` reliably
  destroys/replaces any stale mapping after slot reuse;
* `m_aiPlayerCityRadiusCount` / `m_aaiCultureRangeCities` and other player-keyed
  derived plot data, checking whether ordinary city removal/acquisition already
  balances them before recycle;
* any game/team/city serialized PlayerTypes VALUE not covered by the root discussion.

Treat additional bare historical PlayerTypes aliases as F477 manifestations when the
repair is the same dead-slot-reuse invariant. Spend F478 only for an independent state
transition/root with its own repair requirement.

Once the recycle sibling family is reconciled, return to the low-semantic-coverage
`CvPlayer.cpp` authoritative-function pass. Require current SAS reachability, concrete
consequence, ancestry/comparison and semantic duplicate proof before promotion. Freeze
C031-WIP201 at the next meaningful settled tranche whether F478 is confirmed or remains
unused.

END C031-WIP200

===============================================================================
C031-WIP201 — POST-WIP200 CONTINUATION
Queue 004: CvGameCoreDLL/CvPlayer.cpp
F478 / KI#801 CONFIRMED
F479 / provisional KI#802 UNUSED
===============================================================================

IMMEDIATE F477 SIBLING RECONCILIATION
-------------------------------------
The first post-WIP200 pass stayed inside the BBAI recycled-player cleanup family rather
than immediately returning to unrelated CvPlayer helpers.

`m_eSecondOwner` remains worth including in the comprehensive "do not recycle identity"
audit because `resetPlotAndCityData()` clears recycled-player plot culture with
`bUpdate=false`, but the strongest second-owner gameplay consumers are primarily tied
to AdvCiv's `OWN_EXCLUSIVE_RADIUS` system (recommended/default current value 0). No
independent F478 was spent on that field.

`m_aiPlayerCityRadiusCount` and `m_aaiCultureRangeCities` are derived player-keyed plot
counts whose ordinary city kill/acquisition/culture-level hooks explicitly decrement
and increment them. The pass did not establish an independent recycled-slot failure in
those counters.

The remaining `m_aiPlotGroup[PlayerTypes]` field did survive as a different root.

F478 / KI#801 — RECYCLED PLAYER RETAINS PER-PLOT PLOT-GROUP IDS; NEW FREE-LIST IDS
                  CAN REBIND OLD PLOTS INTO THE COLONY'S NEW TRADE NETWORK
---------------------------------------------------------------------------------
STATUS: CONFIRMED.

ROOT
----
Each plot stores a per-player plot-group ID:

    ArrayEnumMap<PlayerTypes,int,...> m_aiPlotGroup;

and resolves it dynamically through the CURRENT player's free-list:

    return GET_PLAYER(ePlayer).getPlotGroup(m_aiPlotGroup.get(ePlayer));

`CvPlayer::resetPlotAndCityData()` does not clear this field.

When a recycled player is reset, `CvPlayer::uninit()` destroys the player's
`m_plotGroups` free-list:

    m_plotGroups.uninit();

but no corresponding pass invalidates the IDs already stored in every `CvPlot`.
Those integers therefore survive after the old objects they named have been destroyed.

This would be less dangerous if new plot-group object IDs could never equal old ones,
but the current free-list deliberately restarts its ID sequence on a fresh init:

    m_iCurrentID = FLTA_MAX_BUCKETS;
    ...
    pData->setID(m_iCurrentID + iIndex);

K-Mod's free-list has the same reset/reuse behavior. Thus the new colony's first,
second, ... plot groups can receive exactly the same full IDs as the extinct player's
first, second, ... groups.

DEFEAT ORDER MAKES STALE MAP-SIDE IDS REACHABLE
-----------------------------------------------
Current `CvPlayer::setAlive(false)` sets:

    m_bAlive = false;

BEFORE it runs the later dead-player `killCities()` cleanup.

`killCities()` kills each city with `bUpdatePlotGroups=false` and then calls:

    GC.getGame().updatePlotGroups();

but `CvGame::updatePlotGroups()` iterates only `PlayerIter<ALIVE>`. The just-defeated
player is already absent, so this global pass does not recalculate or clear that
player's map-side plot-group mappings.

A particularly deterministic supported setup is Max City Elimination. If X reaches the
configured city-loss limit while it still owns additional cities/network plots,
`verifyAlive()` kills X immediately. The remaining cities are then removed only after X
has become non-alive, guaranteeing that the final global plot-group rebuild excludes X.
This avoids relying on a civ that naturally reached zero cities having any residual
network.

RECYCLING / ID REBINDING
------------------------
Later, with no never-alive slot available, a human splitter can recycle dead X into
colony Z through the already-supported `getSplitEmpirePlayer` fallback.

`initInGame -> reset` destroys X's `m_plotGroups` objects, but BBAI's map/city reset does
not invalidate `CvPlot::m_aiPlotGroup[X]`.

For a singleton old team, the new team reset also clears the new team's revealed map.
The initial `setAlive(true) -> updatePlotGroups()` therefore cannot normally rebuild a
real network yet. During `splitEmpire`, the parent then supplies the colony's map
knowledge and transfers cities/plots; the final:

    kGame.updatePlotGroups();

constructs the new colony's plot groups from scratch.

The first new group again receives the same first free-list ID as old X's first group.
Any old plot that still stores that numeric ID now makes:

    getPlotGroup(Z)

return Z's newly allocated group object, even though the plot was never inserted in that
object's plot list and can be geographically disconnected.

WHY THE FINAL `updatePlotGroups` DOES NOT GUARANTEE REPAIR
---------------------------------------------------------
`CvPlayer::updatePlotGroups()` first recalculates the NEW groups' own internal plot
lists, then walks every map plot with:

    updatePlotGroup(getID(), false, false);

The second parameter is `bRecalculate=false`.

`CvPlot::updatePlotGroup` begins by resolving the map-side stored ID. If the stale ID
has now rebound to a valid new group, `pPlotGroup != NULL`; because `bRecalculate` is
false, the function SKIPS the connectivity/membership validation branch that would
remove a disconnected plot from that group. A non-network plot can even return later
without invalidating the stale ID.

So the safety rebuild can preserve the rebinding instead of repairing it.

CURRENT GAMEPLAY CONSEQUENCE — FALSE TRADE NETWORK / RESOURCE TRADE
-------------------------------------------------------------------
Current `CvPlot::isConnectedTo(CvCity const&)` treats equal per-player plot-group IDs as
physical connectivity:

    isSamePlotGroup(kCity.getPlot(), getOwner()) ||
    isSamePlotGroup(kCity.getPlot(), kCity.getOwner())

and `CvPlayer::canTradeNetworkWith` uses those city connections authoritatively.
Resource trade availability then directly returns:

    case TRADE_RESOURCES: return canTradeNetworkWith(eWhoTo);

Ongoing resource deals likewise call `canTradeNetworkWith` from `CvDeal::verify`.

Deterministic supported repro construction:

1. Enable Max City Elimination = 1.
2. Old player X has more than one city and a valid first plot group G0. Arrange that
   future colony-parent P's capital is part of X's G0 trade network (e.g. normal
   revealed/peaceful connected trade network).
3. X loses one qualifying city. `CitiesLost` reaches 1, `verifyAlive()` eliminates X
   while X still has other cities/network state.
4. Because `m_bAlive` is already false when those remaining cities are killed, the
   final `CvGame::updatePlotGroups()` excludes X. P's capital retains its stored
   `m_aiPlotGroup[X] = G0`.
5. Exhaust never-alive slots. Later human P splits a geographically disconnected
   colony and `getSplitEmpirePlayer` recycles X into new player Z.
6. X's old plot-group objects are destroyed. The old map-side G0 integer on P's capital
   is not cleared.
7. Arrange map/index/trade-network layout so Z's actual colony network is the first new
   plot group created by the final rebuild. Fresh free-list initialization assigns it
   the same G0 ID.
8. When the later map walk reaches P's physically disconnected capital, its stale
   `m_aiPlotGroup[Z] = G0` now resolves to Z's real colony group. With
   `bRecalculate=false`, the false membership is not removed.
9. `Z.canTradeNetworkWith(P)` tests Z's capital against P's cities. The P capital and Z
   capital now compare equal under Z's plot-group mapping, so the function can return
   true despite there being no physical trade connection.
10. `canPossiblyTradeItem(..., TRADE_RESOURCES)` therefore permits resource trade that
    should be unavailable; `CvDeal::verify` can continue accepting the same false
    network afterward.

The geographical disconnection can be made explicit in a supported scenario (separate
route/river networks with no connecting network terrain/route and no applicable water
trade connection). P and Z are parent/vassal, so contact/relationship ambiguity is not
needed for the repro.

SERIALIZATION / STABILITY
-------------------------
`CvPlot::m_aiPlotGroup` is serialized directly in `CvPlot::read/write`.

The numbered issue does not depend on saving during the narrow reset window: once the
stale ID has rebound to a valid new Z plot-group ID, ordinary `getPlotGroup` calls
cannot distinguish it from a legitimate mapping. The standard final
`updatePlotGroups()` itself can preserve it for the reason above.

A later event that forces the specific plot through a true `bRecalculate=true`
connectivity check can repair that mapping, but there is no guarantee of such a change
before resource-trade/network consumers use the false connection.

ANCESTRY
--------
The key ingredients predate SAS:

* K-Mod's BBAI `resetPlotAndCityData()` has the same field list and does not clear
  `m_aiPlotGroup`.
* K-Mod's `FFreeListTrashArray::init` also resets `m_iCurrentID` to
  `FLTA_MAX_BUCKETS`, so dead-player object IDs are reusable after a fresh free-list.
* AdvCiv 1.14 retains the same recycled-player reset approach and its
  `CvPlayer::updatePlotGroups` map walk likewise calls
  `updatePlotGroup(getID(), false, ...)`.
* Current SAS inherits those mechanics.

The defect is therefore an inherited recycled-player cleanup/lifetime bug rather than a
SAS-specific regression.

DUPLICATE / RELATION TO F477
----------------------------
The reconciled album before this tranche has no root for recycled `m_aiPlotGroup` IDs,
free-list plot-group ID reuse, or a false trade network produced by player-slot recycle.

F477 and F478 share the dangerous feature "reuse a dead PlayerTypes slot," but the
stored aliases and direct repair requirements differ:

F477:
    external fields store the OLD PlayerTypes identity itself; after reuse they
    semantically name the new civilization.

F478:
    a field keyed by that player stores an OLD plot-group OBJECT ID; player reset
    destroys the object, then the new free-list reuses that numeric object ID and old
    map entries bind to the wrong new object.

A blanket policy of never recycling EVER_ALIVE player slots fixes both, but if recycle
support remains, F478 requires explicit plot-group-map invalidation in addition to
F477's historical-identity scrub.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preferred architectural repair remains:

1. Do not recycle EVER_ALIVE player slots into a new civilization identity.

If recycle support is retained:

2. Explicitly invalidate every plot's raw `m_aiPlotGroup[eRecycledPlayer]` entry before
   any new plot group can be allocated for that slot.

Be careful with the existing `CvPlot::setPlotGroup(ePlayer, NULL)` API AFTER player
reset. It first resolves the old ID through the player's current free-list; when the old
object has already been destroyed, both resolved old pointer and requested new pointer
can be NULL, causing the setter to return early without overwriting the stale integer.
A dedicated raw invalidation helper (set the enum-map entry to
`FFreeList::INVALID_INDEX`) or cleanup performed BEFORE `m_plotGroups.uninit()` is safer.

3. After clearing raw IDs, perform the normal new-player plot-group rebuild and assert
   that every plot whose stored group is non-invalid is actually contained in the
   resolved group's plot list / is connected under the owning team.

REGRESSION TESTS
----------------
A. Raw reset test

1. Give X at least one plot group and record a representative plot's stored group ID.
2. Eliminate X through Max City Elimination while other X cities still exist.
3. Recycle X into Z.
4. Before allocating Z groups, assert every plot's raw group ID for Z is INVALID.

B. ID-reuse test

1. Confirm old X's first group ID G0.
2. Recycle X and create Z's first group.
3. Confirm the free-list is allowed to reuse G0, but no unrelated old plot still stores
   G0 for Z.

C. False-network test

1. Old X's G0 includes parent P's capital.
2. Eliminate X as above and recycle it into physically disconnected colony Z.
3. Force Z's real first group to receive G0 before the map walk reaches P's capital.
4. Run the normal final `CvGame::updatePlotGroups()`.
5. Assert P capital's plot is NOT in / does not resolve to Z's colony group.
6. Assert `Z.canTradeNetworkWith(P) == false` and a resource trade is unavailable.

D. Rebuild invariant test

After the repair, run a full `updatePlotGroups` and for every alive player/plot with a
non-null `getPlotGroup(player)`, verify that the plot is present in that group's actual
plot list and satisfies the intended connectivity component.

NUMBERING
---------
F478 / KI#801 is CONFIRMED.
F479 / provisional KI#802 is the next unused root.

CURRENT STONE AFTER C031-WIP201
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F478 / KI#801 CONFIRMED.
F479 / provisional KI#802 UNUSED.
Latest physical checkpoint: C031-WIP201.
No repository source files modified.

NEXT
----
Continue from F479 unused.

Close the remaining recycled-player plot/city derived-data siblings first, especially
city-radius/culture-range balancing and `m_eSecondOwner`, but do not split further
findings from F477/F478 unless they have a genuinely independent root/repair.

Then leave the heavily productive recycled-player lifecycle seam and return to the
historical-fix x low-semantic-coverage authoritative `CvPlayer.cpp` pass. Prioritize
stored counters/state with external consumers, event/vote/religion state, diplomacy
transitions and nonzero current-XML arithmetic. Continue semantic duplicate checking
against the full album before deep tracing.

Require current SAS reachability + concrete gameplay consequence + ancestry/comparison
+ independent-root proof before spending F479. Create C031-WIP202 at the next meaningful
settled tranche whether F479 is confirmed or remains unused.

END C031-WIP201


===============================================================================
C031-WIP202 — QUEUE 004 CVPLAYER.CPP — F479 / KI#802 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F478 / KI#801 remain CONFIRMED.
F479 / provisional KI#802 remains UNUSED.
No repository source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Resume from C031-WIP201 after rejecting the zero-city Advanced Start religion-founding
lead. Continue the low/near-zero semantic-coverage pass with preference for executable
authoritative state and historical bug-fix seams. Keep semantic duplicate checking
ahead of promotion; closures and rejected leads remain useful audit output.

CONTROL — `calculatePollution` SIGN CONVENTION STILL COHERENT
-------------------------------------------------------------
The K-Mod pollution helper remains internally consistent with the health API it reads.
Building, bonus and power bad-health components are represented with the opposite sign
from population unhealth in this calculation, and the subtract/add arrangement matches
that documented convention. Current gameplay consumers are the global-warming code;
SASGameRecord also reads the same helper for diagnostics. No new arithmetic/sign root
was established.

CONTROL — ESPIONAGE GOLD / BULK-UPGRADE HELPERS
------------------------------------------------
`getEspionageGoldQuantity` was rechecked through its execution, AI and help callers.
No mismatch between eligibility, computed amount and applied gold was established in
current data.

The bulk upgrade price / XP-change helpers are chiefly UI/estimate aggregators. Their
per-unit checks delegate to the actual unit upgrade legality/price/XP helpers. No
separate authoritative state bug was found in the player aggregation layer.

DUPLICATE CONTROL — SPACESHIP LAUNCH OBSERVER COORDINATE LEAK IS F464
----------------------------------------------------------------------
A suspicious `CvPlayer::launch` observer-message coordinate lifetime was rediscovered,
but the semantic duplicate gate caught it immediately: the reused capital-coordinate
state across observers and resulting fog-of-war location disclosure is already the
confirmed F464 / KI#787 root. Do not spend F479 on it.

CONTROL — DEAD PLAYER STALE `m_aVote` ID IS NOT AN INDEPENDENT F479
-------------------------------------------------------------------
`CvGame::setVoteOutcome` clears `CvPlayer::m_aVote` only for currently alive players,
while triggered-vote objects come from a recyclable free-list. Therefore a dead player
can retain an old ballot keyed by an ID after the triggered vote object is deleted.

However, ordinary vote resolution iterates living major civilizations, so the stale
ballot is inert while the player remains dead. Bringing that exact CvPlayer object back
without `reset()` requires the malformed recycled-colony revival already owned by F447
(or debug/WorldBuilder-style revival). A later reused vote ID would therefore be a
downstream consequence of F447, not an independent current root. Preserve the stale-ID
lifetime asymmetry as robustness debt; F479 remains unused.

CONTROL — `setLoyalMember` / VOTE-SOURCE BONUS TOGGLE ORDER IS CORRECT
---------------------------------------------------------------------
`CvPlayer::setLoyalMember` intentionally calls `processVoteSource(..., false)` before
changing the loyalty bit and calls it again with `true` afterward. `CvCity::processVoteSource`
first checks the current `isLoyalMember` state. Consequently:

* true -> false: the pre-toggle call sees loyalty and removes the bonuses; the post-toggle
  add call is ignored;
* false -> true: the pre-toggle removal is ignored; the post-toggle call sees loyalty and
  adds the bonuses.

The ordering is therefore deliberate and symmetric, not a missed refresh.

CONTROL — SPECIALIST / IMPROVEMENT YIELD MUTATORS REFRESH THEIR LIVE CONSUMERS
------------------------------------------------------------------------------
`changeSpecialistExtraYield` updates the stored specialist/yield value, then runs the
empire-wide extra-specialist-yield refresh and assignment-AI dirtying.
`changeImprovementYieldChange` updates its stored value and calls `updateYield`, which
recomputes affected city/plot yield state. No copied-state sibling was established.

CONTROL — ADVANCED-START RELIGION-FOUNDING LEAD REMAINS REJECTED
----------------------------------------------------------------
The prior WIP201 candidate stays closed. Advanced Start tech purchase calls
`CvTeam::setHasTech(..., bFirst=false, ..., TECH_ACQUISITION_ADVANCED_START)`; the
religion first-discovery block requires `bFirst && bFirstToDiscover`. A legal zero-city
Advanced Start teammate therefore does not enter `foundReligion` through that producer.

CURRENT STONE AFTER C031-WIP202
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F478 / KI#801 CONFIRMED.
F479 / provisional KI#802 UNUSED.
Latest physical checkpoint: C031-WIP202.
No repository source files modified.

NEXT
----
F479 unused -> continue larger low-semantic-coverage execution functions before thin
setters -> prioritize authoritative state/free-list/lifecycle and historical bug-fix
seams -> then SAS-specific behavioral deltas with real current XML producers -> full
callee chain + current caller/data + ancestry + semantic duplicate proof before any
promotion -> create C031-WIP203 at the next meaningful settled tranche.

END C031-WIP202


===============================================================================
C031-WIP203 — QUEUE 004 CVPLAYER.CPP — F479 / KI#802 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F479 / KI#802 are CONFIRMED.
F480 / provisional KI#803 is the next UNUSED root.
No repository source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Resume from C031-WIP202 with a structural player-slot lifecycle-completeness pass rather
than continuing to rank thin setters literally. Compare persistent player/team keyed
state stored outside CvPlayer against the BBAI mid-game reset/recycle path. Preserve
same-root consequences under the existing finding that owns them and promote only an
independent stale-state mechanism with a current supported consequence.

F477 SCOPE REFINEMENT — `m_eParent` IS ANOTHER BARE PLAYER-ID ALIAS
-------------------------------------------------------------------
The recycled-identity root already owned by F477 also reaches a surviving colony's
serialized `CvPlayer::m_eParent`.

`setParent` deliberately treats parenthood as permanent for the life of that player;
there is no later clearing when the parent dies. Thus:

1. Old parent X creates colony C, so C stores `m_eParent = X`.
2. X later becomes extinct while C survives.
3. Human dead-slot colony fallback eventually recycles PlayerTypes X into unrelated
   civilization Z.
4. C's unchanged numeric parent value now resolves as Z.

A concrete gameplay consumer is `CvPlayer::getSplitEmpirePlayer(CvArea const&)`:

    for (PlayerIter<MAJOR_CIV> itOldDeriv; itOldDeriv.hasNext(); ++itOldDeriv)
    {
        if (itOldDeriv->getParent() != getID())
            continue;
        if (itOldDeriv->hasCapital() &&
            itOldDeriv->getCapital()->isArea(kArea))
            return NO_PLAYER;
    }

Z can therefore treat old colony C as Z's own derivative and refuse a later split on
C's capital landmass even though Z never created C. `CvCity::getLiberationPlayer`,
`CvTeam::isParent` and AI city-trade trust checks also consume the aliased relation.

This is not F479. It is another manifestation of F477's already-confirmed architectural
problem: an EVER_ALIVE PlayerTypes slot is reused as a new identity while durable bare
PlayerTypes references elsewhere have no generation/version tag.

CONTROL — `CvArea::m_aiBestFoundValue[player]` SELF-HEALS BEFORE AI USE
------------------------------------------------------------------------
`CvArea::m_aiBestFoundValue` is serialized and is not explicitly scrubbed by
`CvPlayer::resetPlotAndCityData`, so it was a reasonable recycle candidate.

However, `CvPlayerAI::AI_doTurnUnitsPre()` begins with `AI_updateFoundValues()`. That
function first sets every area's `BestFoundValue(getID())` to zero and then recomputes
all current plot found values and area maxima. The new colony has no ordinary consumer
of the stale old-player area maximum between `addPlayer` and that mandatory refresh.

Therefore this is stale storage but not a demonstrated current gameplay defect.

PARKED ROBUSTNESS LEAD — `CvArea::m_aTargetCities[player]`
----------------------------------------------------------
Area target-city state is a different shape: `CvArea::m_aTargetCities[PlayerTypes]` is
serialized, player reset does not clear it, and `AI_reset` resets the city-target TIMER
but not the per-area target pointers. A surviving foreign target city can therefore
remain attached to recycled player slot X after X becomes unrelated colony Z.

Unlike best-found values, the normal target refresh is not unconditional:
`AI_doTurnUnitsPre` calls `AI_updateAreaTargets()` only when the timer is zero AND a
1-in-8 synchronous random check succeeds. Many unit-AI and some player/city-AI routines
consult `AI_getTargetCity`.

This is real stale derived state and deserves continued robustness attention. It is not
numbered here because the fresh new-team area strategy is recomputed before ordinary
turn decisions, the colony begins with a restricted unit set, and this tranche did not
establish a sufficiently direct current consequence independent of later refresh/RNG.
F479 is reserved for the stronger blockade-state failure below.

F479 / KI#802 — CONFIRMED — RECYCLED TEAM INHERITS PERMANENT STALE BLOCKADE COUNTS
----------------------------------------------------------------------------------

ROOT
----
`CvPlot::m_aiBlockadedCount[TeamTypes]` is persistent/serialized map-side state. It is
maintained by blockading units, but the BBAI mid-game team recycle cleanup does not
clear the recycled team's entries:

    CvTeam::resetPlotAndCityData()
    {
        ...
        kPlot.setRevealedOwner(getID(), NO_PLAYER);
        kPlot.setRevealedImprovementType(getID(), NO_IMPROVEMENT);
        kPlot.setRevealedRouteType(getID(), NO_ROUTE);
        kPlot.setRevealed(getID(), false, ...);
        ...
    }

There is no corresponding removal of:

    kPlot.getBlockadedCount(getID())

The old count can moreover become permanently stranded when the old team dies.

WHY ELIMINATION STRANDS THE COUNT
---------------------------------
Suppose naval unit U of Team Y is legally blockading Team X. `CvUnit::updatePlunder(+1)`
has incremented `m_aiBlockadedCount[X]` on water plots in U's blockade range.

When the last player of X dies:

1. `CvPlayer::setAlive(false)` kills X's own units before lowering X's team AliveCount.
   This is important for blockades maintained BY X, but U belongs to Y and survives.
2. `CvTeam::changeAliveCount(-1)` reaches zero and clears bilateral war state directly
   through `setAtWar(..., false)`; it does not first call U's `updatePlunder(-1)`.
3. On U's later turn, `canPlunder` is now false and `setBlockading(false)` correctly
   invokes `updatePlunder(-1)`.
4. But `updatePlunder(-1)` enumerates target teams through:

       TeamIter<ALIVE,KNOWN_POTENTIAL_ENEMY_OF>

   Dead X is already excluded. Therefore the old positive
   `m_aiBlockadedCount[X]` entries are never decremented.

`CvPlot::m_aiBlockadedCount` is serialized in `CvPlot::read/write`, so saving/loading
does not repair the orphaned count.

CURRENT SUPPORTED RECYCLE CONSEQUENCE
-------------------------------------
Use the same current human dead-slot fallback already source-proved for F477/F478.
A fully ordinary concrete setup is:

1. Team X owns a coastal city C and is at war with Teams P and Y.
2. Y has a naval unit legally blockading water tiles adjacent to C, so those tiles have
   positive `getBlockadedCount(X)`.
3. Human P captures X's final city C and eliminates X while Y's blockader survives.
4. X's war state disappears. Y's blockader later leaves blockade state, but the dead-X
   counts cannot be decremented because the removal loop sees only alive teams.
5. P keeps C/the former X area. Once no never-alive player slot remains, P uses the
   supported human colony fallback and that defeated singleton X slot/team is selected
   for the new colony Z.
6. `initInGame` reinitializes singleton Team X and calls
   `CvTeam::resetPlotAndCityData`, but that routine does not clear the old blockade
   counts. P then transfers C/the area to Z.

Z now owns a coastal city beside water plots whose `getBlockadedCount(Z.getTeam())` is
positive solely because Z reused dead Team X's numeric ID. There need be no current
blockading unit and no current enemy responsible for the value.

DIRECT GAMEPLAY EFFECT
----------------------
This is not cosmetic. `CvCity::canWork` rejects a water tile immediately when:

    kPlot.getBlockadedCount(getTeam()) > 0

and `CvCity::isBlockaded()` returns true if any adjacent plot has a positive count.
Trade-network logic also consults plot blockade counts.

Thus the recycled colony can be unable to work valid coastal tiles and can be treated
as blockaded by a blockade that ended with an unrelated extinct civilization.

INDEPENDENCE / DUPLICATE CONTROL
--------------------------------
The reconciled album through WIP202 contains substantial blockade coverage, including
F144/KI#467's group-wide cancellation problem, but no prior root for orphaned
team-indexed `m_aiBlockadedCount` surviving team death and later slot recycling.

This is also distinct from the recent recycle roots:

* F477: bare PlayerTypes historical VALUE aliases become the new civilization identity.
* F478: stale per-player plot-group object IDs are rebound to newly allocated groups.
* F479: an independently maintained TeamTypes-indexed blockade COUNTER is orphaned on
  death and omitted from the team recycle cleanup.

F479 does not require F473's dead multi-member-team reset omission. A defeated singleton
team is sufficient and follows the normal `bTeamInit` + `resetPlotAndCityData` path.

ANCESTRY / INTEGRATION CONTEXT
------------------------------
The failure lives in inherited BBAI/K-Mod mid-game player/team recycling architecture.
The 12/30/08 BBAI path explicitly introduced plot/city cleanup for recycled identities;
K-Mod's locally available `CvPlayer::resetPlotAndCityData` still has that same limited
field list and AdvCiv/SAS retain it. Current SAS also labels the matching team cleanup
as BETTER_BTS_AI_MOD code.

The exact historical first revision in which the team cleanup omitted blockade counts
was not independently reconstructed from a full local K-Mod CvTeam history in this
tranche, so do not overclaim a precise upstream commit. The present inherited omission
and current execution chain are source-complete.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
When a team slot is reinitialized for a genuinely new civilization identity, clear its
map-side blockade counts as part of `CvTeam::resetPlotAndCityData`, e.g. for every plot
remove the full current `getBlockadedCount(getID())` through the normal mutator (or an
equivalent reset that preserves working-city dirtying / trade-network invalidation).

Architecturally, F477's broader recommendation of never reusing an EVER_ALIVE player
identity would also prevent this recycle manifestation, but the blockade counter is a
separate stale-state invariant and can be repaired directly.

REGRESSION TESTS
----------------
A. Orphan-on-death test

1. Let Y blockade X's coastal tile and assert `BlockadedCount[X] > 0`.
2. Eliminate X while Y's blockader survives.
3. Advance Y until the unit exits blockade state.
4. Assert the dead X entry returns to zero. Current code fails this assertion.

B. Recycle cleanup test

1. Preserve a positive dead-X count as above.
2. Recycle singleton X into a new colony.
3. Immediately after team/player initialization, before city transfer, assert every
   plot has `getBlockadedCount(Xteam) == 0`.

C. Gameplay test

1. Have parent P own former-X coastal city C after X's elimination.
2. Recycle X into colony Z and transfer C.
3. With no legal blockader in range, assert C can work otherwise valid water tiles and
   `C.isBlockaded() == false`.

NUMBERING
---------
F479 / KI#802 is CONFIRMED.
F480 / provisional KI#803 is the next unused root.

CURRENT STONE AFTER C031-WIP203
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F479 / KI#802 CONFIRMED.
F480 / provisional KI#803 UNUSED.
Latest physical checkpoint: C031-WIP203.
No repository source files modified.

NEXT
----
F480 unused -> finish the immediate map-side recycled-team sibling sweep (stolen/
visibility/blockade-adjacent team-indexed state and the parked per-player area target)
without splitting same-root consequences -> then leave recycle lifecycle again and
resume larger low-semantic-coverage authoritative CvPlayer functions / historical-fix
seams -> current SAS caller/data + concrete consequence + ancestry + semantic duplicate
proof mandatory -> create C031-WIP204 at the next meaningful settled tranche.

END C031-WIP203


===============================================================================
C031-WIP204 — QUEUE 004 CVPLAYER.CPP — F480 / KI#803 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F480 / KI#803 are CONFIRMED.
F481 / provisional KI#804 is the next UNUSED root.
No repository source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Resume from C031-WIP203 / F480 unused. Finish the immediate recycled-team map-state
siblings, preserve current-data controls rather than promoting generic dormant defects,
then return to the historical-fix x low-semantic-coverage authoritative CvPlayer pass.
The strongest new live path is an old team-game religion-founder invariant that current
SAS's GoodyTech/religion data makes normally reachable.

CONTROL — RECYCLED STOLEN-VISIBILITY STATE IS REAL GENERIC DEBT BUT CURRENT-DATA DORMANT
-----------------------------------------------------------------------------------------
`CvPlot::m_aiStolenVisibilityCount[TeamTypes]` has a genuine death/recycle lifetime
problem in generic source. If a team that is stealing another team's visibility dies
while its stolen-visibility timer is active, then:

* dead teams no longer run `CvTeam::doTurn`, so their timer stops ticking;
* source-team visibility changes propagate stolen visibility only to alive stealing
  teams;
* later team reset raw-resets the timer but does not statefully drain old plot counts.

A recycled numeric TeamTypes slot could therefore inherit ghost visibility.

However, current SAS has no normal producer. Repository-wide shipped-data search in the
2026-09-01 light source finds zero units with `bStealPlans=1`, and the DLL's positive
producer of `changeStolenVisibilityTimer` is the legacy `CvUnit::stealPlans()` action.
Python exposes query state but not an ordinary setter. This remains robustness debt,
not F480.

This current-data fact also narrows runtime reachability of the already-fixed F099
Permanent-Alliance stolen-visibility bookkeeping root. F099 is not renumbered or
retracted here; retain this as a current-data annotation only.

CONTROL — OTHER IMMEDIATE RECYCLED TEAM PLOT STATE
---------------------------------------------------
The neighboring real visibility and invisible-detection counts are drained by ordinary
unit/city lifecycle. Recycled-team revealed-map/owner/improvement/route/city espionage
state is explicitly cleared by the team reset path. No additional live current root was
established there.

The cross-player AI reset is also comparatively thorough: surviving players' persistent
religion/favorite-civic counters, trade/grant/gold values, attitude extras, memories,
contact timers, first-contact state and related player-indexed AI state are scrubbed in
both directions when a player slot is reset. The core reset similarly clears surviving
players' GPT/demographics/espionage-weight entries involving the recycled identity.

F480 / KI#803 — CONFIRMED — TEAM FIRST-DISCOVERY CAN SELECT A CITYLESS MEMBER AND PERMANENTLY CONSUME A RELIGION SLOT
-------------------------------------------------------------------------------------------------------------------

ROOT
----
The team first-discovery religion selector in `CvTeam::setHasTech` chooses among alive
team members but does not require the selected `eBestPlayer` to own a city:

    for (MemberIter it(getID()); it.hasNext(); ++it)
    {
        CvPlayer const& kMember = *it;
        int iValue = 10 + SyncRandNum(10);
        ...
        if (kMember.getCurrentResearch() != eTech)
            iValue *= 10;
        if (iValue < iBestValue)
            eBestPlayer = kMember.getID();
    }

Once a member is selected, the slot is reserved before actual city placement:

    kGame.setReligionSlotTaken(eLoopReligion, true);
    ...
    GET_PLAYER(eBestPlayer).foundReligion(eLoopReligion, eLoopReligion, true);

`CvPlayer::foundReligion` independently sets the slot taken again, searches only the
selected player's cities, and then simply returns if there is none:

    kGame.setReligionSlotTaken(eSlotReligion, true);
    ...
    if (pBestCity == NULL)
        return;

Thus the team-level selection contract and player-level placement contract disagree:
being an alive team member is sufficient to receive the entitlement, but having a city
is necessary to realize it.

WHY THE EARLIER ADVANCED-START LEAD WAS CORRECTLY REJECTED
-----------------------------------------------------------
C031-WIP201/202 rejected a zero-city Advanced Start teammate as the producer because
Advanced Start tech purchase calls `CvTeam::setHasTech(..., bFirst=false, ...)`, while
the religion first-discovery block requires `bFirst && bFirstToDiscover`.

That rejection remains correct. F480 uses a different ordinary producer whose call does
set `bFirst=true`: full technology completion from a tribal-village technology result.

CURRENT SAS DATA MAKES THE GOODY PRODUCER LIVE
-----------------------------------------------
Current SAS has religion prerequisites that are also GoodyTechs:

* Hinduism -> `TECH_CALENDAR` (`bGoodyTech=1`, Ancient, base cost 80)
* Daoism -> `TECH_PAPER` (`bGoodyTech=1`)
* Islam -> `TECH_NAVIGATION` (`bGoodyTech=1`)

Current `GOODY_TECH` supplies 23 base research plus two random 0..19 additions, i.e.
23..61 under the XML random convention used by the goody code. AdvC's modified goody
logic converts that progress into a full discovery when it is at least 80% of the
remaining team research.

`CvPlayer::isGoodyTech` contains an important safeguard, but only for the PLAYER
receiving the hut:

    if (!isFoundedFirstCity() && ...)
    {
        if (unfounded religion/corporation uses eTech)
            return false;
    }

Therefore the hut owner cannot personally obtain an immediately founding tech before
founding its first city, but the check says nothing about a different cityless teammate
whom `CvTeam::setHasTech` may later select as the religion founder.

DETERMINISTIC SUPPORTED REPRO
-----------------------------
Use ordinary team-game state with Pick Religion disabled; no death, recycle, debug or
malformed scenario state is required.

1. A and B are alive members of the same team.
2. A has founded at least one city. B has not yet founded a city and still has its
   settler/unit, so B is alive and remains a normal team member.
3. Hinduism is unfounded and its Calendar slot is untaken.
4. Make Calendar the only eligible GoodyTech for the hut result (or simply condition on
   Calendar being selected by the normal synchronized goody roll).
5. Leave at most 28 research points on Calendar. This guarantees even the minimum
   23-point GOODY_TECH award satisfies the 80%-of-remaining full-discovery threshold.
6. B keeps Calendar as its current research. A switches its current research to another
   technology before A receives the goody.
7. A receives GOODY_TECH. A passes `isGoodyTech` because A has founded its first city,
   and Calendar is completed through:

       setHasTech(Calendar, true, A, true, true, false, TECH_ACQUISITION_GOODY)

8. In the religion-founder member selection, B's score is 10..19. A is not currently
   researching Calendar, so A's score is multiplied by 10 and is at least 100 before
   any additional religion-count penalty. B is therefore selected deterministically.
9. The Hinduism slot is marked taken. With Pick Religion disabled,
   `B.foundReligion(Hinduism, Hinduism, true)` executes immediately.
10. B has no city, so `pBestCity == NULL` and `foundReligion` returns without calling
    `CvGame::setHolyCity`.

PERSISTENT CONSEQUENCE
----------------------
The slot reservation is persistent game state (`m_abReligionSlotTaken` is serialized),
while no Holy City and no founded-religion turn were created.

`CvGame::doHolyCity` cannot repair this later because its first gate is:

    if (isReligionSlotTaken(eReligion))
        continue;

So Hinduism's founding opportunity is permanently consumed even after B later settles a
city. Saving/loading preserves the bad state. The team has also already consumed the
first-discovery branch and will not replay it for Calendar.

This is gameplay-significant: the game can permanently lose one religion/Holy City and
its normal founding reward solely because the team selector chose an alive cityless
member.

DISTINCTNESS / DUPLICATE CONTROL
--------------------------------
The reconciled album contains F320 / KI#643, the Pick Religion pending-popup reservation
loss. F480 is independent:

* Pick Religion can be OFF.
* No popup or deferred network choice exists.
* No player elimination occurs.
* The slot is consumed synchronously because `CvTeam::setHasTech` selects a cityless
  teammate and `CvPlayer::foundReligion` cannot realize the already-reserved entitlement.

The WIP201/202 Advanced Start lead is likewise not duplicated: that producer was
correctly rejected because `bFirst=false`; the goody full-tech producer uses `bFirst=true`.

No prior album root was found for this team-member city-presence contract mismatch.

ANCESTRY / INTEGRATION CONTEXT
------------------------------
The city-agnostic team-member selection and `foundReligion` no-city early return are
inherited from Firaxis BtS/Civ4CE through K-Mod and AdvCiv. K-Mod and Civ4CE show the
same first-discovery player scoring without a `getNumCities() > 0` filter.

AdvC later added `isGoodyTech` safeguards to prevent the goody OWNER from immediately
receiving an unfounded religion/corporation tech before founding a first city, but the
safeguard is player-local and therefore does not cover a different cityless teammate.
Current SAS's religion-tech rework makes this inherited gap materially reachable because
Calendar, Paper and Navigation are both religion prerequisites and GoodyTechs.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The narrow invariant repair belongs at the team first-discovery selection boundary:
only team members capable of realizing the founding entitlement should be candidates.
For religion/corporation founding, require `kMember.getNumCities() > 0` (or an equivalent
city-capability predicate) when choosing `eBestPlayer`.

If no team member has a city, do not reserve/consume the slot yet. Let the existing
`CvGame::doHolyCity`/doHeadquarters-style fallback claim it once a valid city owner
exists, or explicitly defer the entitlement in synchronized team/game state.

A secondary defensive guard could move slot reservation in `foundReligion` until after
`pBestCity` is known, but by itself that does not fix the earlier reservation in
`CvTeam::setHasTech`; the team-level contract is the primary repair boundary.

REGRESSION TESTS
----------------
A. Team cityless-member selector test

1. Team A+B; A owns a city, B owns none but is alive.
2. B's current research is the founding tech; A's is not.
3. Complete the founding tech with `bFirst=true`.
4. Assert the founder chosen has at least one city, or that the slot remains untaken if
   no valid member exists.

B. Current Calendar goody test

1. Hinduism unfounded; Calendar nearly complete and only eligible GoodyTech.
2. A owns a city; cityless teammate B selects Calendar; A selects another tech.
3. A receives GOODY_TECH and completes Calendar.
4. Assert Hinduism receives a valid Holy City and `isReligionFounded(Hinduism)` becomes
   true; current code consumes the slot without a Holy City.

C. Persistence test

1. Produce the current bad state.
2. Save/load and advance turns after B settles a city.
3. Assert an unfounded but taken religion slot cannot remain permanently orphaned.

NUMBERING
---------
F480 / KI#803 is CONFIRMED.
F481 / provisional KI#804 is the next unused root.

CURRENT STONE AFTER C031-WIP204
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F480 / KI#803 CONFIRMED.
F481 / provisional KI#804 UNUSED.
Latest physical checkpoint: C031-WIP204.
No repository source files modified.

NEXT
----
F481 unused -> finish immediate first-discovery siblings (corporation/current data,
free-unit/free-tech rewards, zero-city team-member assumptions) without splitting the
same contract -> then resume historical-fix x low-semantic-coverage authoritative
CvPlayer functions, larger execution bodies before thin setters -> current SAS caller /
data + concrete consequence + ancestry + semantic duplicate proof mandatory -> create
C031-WIP205 at the next meaningful settled tranche.

END C031-WIP204


===============================================================================
C031-WIP205 — FIRST-DISCOVERY REWARD SIBLING; F481 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP204 with F480/KI#803 confirmed and F481/KI#804 unused.
The immediate task was to finish the other first-discovery rewards before leaving the
cityless-player seam: corporation founding/current data, first-free-unit placement and
first-free-tech rewards.

F481 / KI#804 — CONFIRMED — A NEVER-SETTLED PLAYER CAN FULL-DISCOVER A FIRST-FREE-UNIT TECH FROM A GOODY HUT AND SILENTLY LOSE THE ONE-TIME UNIT
------------------------------------------------------------------------------------------------------------------------------------------------

ROOT
----
`CvTeam::setHasTech` awards a technology's `FirstFreeUnitClass` specifically to the
`ePlayer` credited with the first acquisition, and assumes that player's capital is a
valid placement site:

    UnitTypes eFreeUnit = GET_PLAYER(ePlayer).getTechFreeUnit(eTech);
    if (eFreeUnit != NO_UNIT)
    {
        bFirstPerk = true;
        bAnnounceFirst = true;
        CvCity* pCapital = GET_PLAYER(ePlayer).getCapital();
        if (pCapital != NULL)
            pCapital->createGreatPeople(eFreeUnit, false, false);
    }

If the first discoverer is alive but has never founded a city, `getCapital()` is NULL.
The code nevertheless records the first-perk/announcement state and simply skips unit
creation. There is no deferred entitlement and no later replay of the first-discovery
block.

This is a different contract from F480. F480's organization block independently chooses
some team member as founder and can choose a cityless teammate even though the actual
tech-acquisition player has a city. F481 uses the actual `ePlayer` acquisition owner;
the defect is that a perfectly valid cityless acquisition owner can reach a reward
whose only placement path assumes a capital.

ORDINARY CITYLESS PLAYER STATE IS SUPPORTED
--------------------------------------------
`CvPlayer::verifyAlive` does not eliminate a never-settled player merely for having zero
cities. With Advanced Start inactive, it kills a zero-city player only when either:

    getNumUnits() <= 0

or

    isFoundedFirstCity()

is already true.

Therefore a teammate that has never founded a city and still owns a settler/scout/other
unit can remain alive while another teammate develops the team through the technology
tree. This needs no debug/WorldBuilder revival and no recycled-player defect.

CURRENT-SAS PRODUCER: ROMANTICISM + GOODY_TECH
-----------------------------------------------
Current SAS `TECH_ROMANTICISM` has all of the properties needed for a live supported
producer:

    Era = ERA_INDUSTRIAL
    FirstFreeUnitClass = UNITCLASS_GREAT_ARTIST
    bGoodyTech = 1
    prerequisite = TECH_LIBERALISM

`CvPlayer::getTechFreeUnit(TECH_ROMANTICISM)` therefore resolves the player's Great
Artist unit normally (absent the unrelated No-Espionage special case for spy-like free
units).

Current `GOODY_TECH` grants research progress and can convert that progress into a full
technology discovery. `CvPlayer::receiveGoody` performs:

    if (iGold <= 0 || 0.8 * researchLeft <= iGold)
        kOurTeam.setHasTech(eBestTech, true, getID(), true, true, false,
                TECH_ACQUISITION_GOODY);

The important arguments are:

* `ePlayer = getID()` — the unit owner who opened the hut;
* `bFirst = true` — unlike Advanced Start, so first-discovery perks execute.

`CvPlayer::canResearch` has no personal-city requirement; prerequisites are team-wide.
So a never-settled member can research/receive Romanticism once its team has Liberalism.

`CvPlayer::isGoodyTech` also does NOT reject Romanticism for a cityless player. Its
special no-first-city guard only rejects a full goody discovery when that technology is
an unfounded religion or corporation prerequisite. Romanticism is neither, and its
explicit `bGoodyTech=1` passes the normal goody-tech gate.

Current `GOODY_TECH` starts from 23 + two SyncRandNum(20) research components and then
applies the normal goody-hut effect factor. Thus a test can simply leave Romanticism
close enough to completion that even the minimum current goody progress satisfies the
80% completion test. The exact late-game buff is not needed for the root; it only makes
completion easier.

DETERMINISTIC CURRENT-SAS REPRO SHAPE
--------------------------------------
A controlled supported team-game test can be made deterministic with respect to the
critical branch:

1. Team A+B begins normally. A founds/develops cities. B NEVER founds a city but keeps
   at least one unit alive, so `verifyAlive` leaves B alive.
2. Through A's economy, the shared team reaches Liberalism and all other prerequisites.
3. Keep an unpopped tribal village reachable by B until the Industrial era. This is rare
   on an ordinary playthrough but fully supported game state; no debug/API action is
   required.
4. Know every other currently eligible GoodyTech (or otherwise arrange Romanticism as
   the sole eligible one), leaving only `TECH_ROMANTICISM` eligible for the tech goody.
5. Put team research progress on Romanticism sufficiently close to completion that the
   minimum `GOODY_TECH` progress satisfies the 80%-of-research-left full-discovery test.
6. B opens the hut and receives `GOODY_TECH`.
7. `receiveGoody` calls `setHasTech(ROMANTICISM, true, B, true, ..., GOODY)`.
8. Assume no other team knows Romanticism, so `bFirstToDiscover` is correctly true.
9. The first-free-unit block resolves B's Great Artist and sets `bFirstPerk=true`.
10. `B.getCapital()` is NULL because B has never settled.
11. `createGreatPeople` is skipped. No Great Artist is created anywhere.
12. The technology is already team-owned and the first-discovery condition will never
    execute again, so B later founding a city does not recover the reward.

The random roll that chooses which goody outcome is received is not part of the defect;
once the supported GOODY_TECH outcome occurs, the XML/eligibility setup above makes the
technology/reward path deterministic. A regression test can invoke the same ordinary
`receiveGoody`/setHasTech path directly at that point.

PERSISTENCE / CONSEQUENCE
-------------------------
The missed Great Artist is a one-shot first-discovery reward. There is no stored
"free unit owed" state. Saving/loading cannot replay the reward because Romanticism is
already known and `countKnownTechNumTeams` will no longer represent a new first
acquisition. Settling B's first city later likewise does not call the first-discovery
block.

So this is not a temporary presentation issue: the world's true first discoverer can
permanently lose a shipped technology reward.

DISTINCTNESS / DUPLICATE CONTROL
--------------------------------
F474 / KI#797 also has a Romanticism/Great-Artist consequence, but its root is entirely
different. F474 duplicates a recycled team's pointer in the AdvCiv `CIV_EVER_ALIVE`
iterator, causing `countKnownTechNumTeams` to return 2 for the true first discoverer;
the whole first-discovery block is skipped.

F481 requires no player recycling and no AgentIterator defect. `bFirstToDiscover` is
correctly TRUE and the reward block executes; the free unit alone is dropped because
its placement code silently accepts a NULL capital.

F480 / KI#803 is also independent. F480 selects a different cityless TEAM MEMBER to
receive a religion-founding entitlement. F481 does no member selection at all: the
actual acquisition owner B legitimately obtains the tech, and the first-free-unit code
has no valid placement fallback.

No prior album root was found for a cityless acquisition owner silently losing the
first-free-unit perk.

ANCESTRY / COMPARISON
---------------------
Civ4CE/stock-style code and K-Mod contain the same inherited pattern:

    eFreeUnit = GET_PLAYER(ePlayer).getTechFreeUnit(eIndex);
    if (eFreeUnit != NO_UNIT)
    {
        bFirstBonus = true;
        pCapitalCity = GET_PLAYER(ePlayer).getCapitalCity();
        if (pCapitalCity != NULL)
            pCapitalCity->createGreatPeople(eFreeUnit, false, false);
    }

AdvCiv/SAS retain the same NULL-capital skip. SAS makes the inherited assumption
materially easy to connect to a current full-tech goody path because Romanticism is
explicitly both `bGoodyTech=1` and `FirstFreeUnitClass=UNITCLASS_GREAT_ARTIST`.

IMMEDIATE SIBLING RECONCILIATION
--------------------------------
* First-free-tech rewards do not share the capital assumption. AI players receive them
  through `AI_chooseFreeTech`; human players receive `chooseTech`. No city placement is
  required, so no F481 sibling was established there.
* Technology-triggered corporation founding is current-data unused in SAS: shipped
  corporation infos have no technology prerequisite, and AdvCiv itself labels this
  `setHasTech` corporation branch unused/not properly tested. It therefore does not
  provide another current cityless founding root.
* Religion founding is owned separately by F480 because its problem is the team member
  selection and pre-reservation contract, not `ePlayer` free-unit placement.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not silently consume `FirstFreeUnitClass` when the credited first discoverer has no
capital. Reasonable synchronized policies include:

1. choose another valid city owned by `ePlayer` if one exists (capital should already
   exist whenever any city exists, but this is a defensive fallback);
2. if `ePlayer` has no city, defer the free-unit entitlement until its first city is
   founded; or
3. if design policy permits team sharing of a first-discovery unit, explicitly choose a
   valid teammate city/recipient rather than silently discarding it.

The safest semantic choice should be decided deliberately; the essential invariant is
that a valid first discovery must not mark the one-shot perk consumed without either
creating the reward or persisting an owed entitlement.

REGRESSION TESTS
----------------
A. Cityless first-discoverer test

1. Keep B alive with one unit and zero cities; `isFoundedFirstCity()==false`.
2. Give Team A+B Liberalism and leave Romanticism unknown globally.
3. Bring Romanticism close enough to completion for a minimum GOODY_TECH completion.
4. B receives the tech goody and becomes the true first discoverer.
5. Assert exactly one appropriate Great Artist reward is created or explicitly deferred.
   Current code creates zero.

B. Capital-present control

Repeat with B owning a capital and assert current behavior still creates exactly one
Great Artist there.

C. Save/load/deferred-entitlement test (if repair defers)

Trigger the cityless first discovery, save/load, then found B's first city. Assert the
owed Great Artist is created exactly once and cannot be duplicated by reload or city
transfer.

NUMBERING
---------
F481 / KI#804 is CONFIRMED.
F482 / provisional KI#805 is the next unused root.

CURRENT STONE AFTER C031-WIP205
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F481 / KI#804 CONFIRMED.
F482 / provisional KI#805 UNUSED.
Latest physical checkpoint: C031-WIP205.
No repository source files modified.

NEXT
----
F482 unused -> close remaining immediate first-discovery acquisition-owner siblings
(first-free-tech / announcements / event hooks and any other city-placement assumptions)
without splitting F481 -> then leave the first-discovery seam and resume historical-fix
x low-semantic-coverage authoritative CvPlayer functions, larger execution bodies before
thin setters -> current SAS caller/data + concrete consequence + ancestry + semantic
duplicate proof mandatory -> create C031-WIP206 at the next meaningful settled tranche.

END C031-WIP205


===============================================================================
C031-WIP206 — MIXED HUMAN/AI TEAM FIRST-FREE-TECH ROUTING; F482 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP205 with F481/KI#804 confirmed and F482/KI#805 unused.
The immediate first-discovery sibling sweep continued through first-free-tech choice,
announcement/replay behavior and the player-vs-team human routing contract.

F482 / KI#805 — CONFIRMED — MIXED HUMAN/AI TEAM ROUTES AN AI DISCOVERER'S FIRST-FREE-TECH REWARD INTO A HUMAN-ONLY POPUP ON THE AI PLAYER, STRANDING THE REWARD
----------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT
----
`CvTeam::setHasTech` handles `iFirstFreeTechs` according to TEAM humanity:

    if (!isHuman())
    {
        for (...)
            GET_PLAYER(ePlayer).AI_chooseFreeTech();
    }
    else
    {
        ...
        GET_PLAYER(ePlayer).chooseTech(kTech.getFirstFreeTechs(), ...);
    }

But the reward is player-specific: `ePlayer` is the player credited with the technology
acquisition. In a mixed human+AI team, `CvTeam::isHuman()` is true whenever the team has
a human leader/member, even when `ePlayer` is an AI teammate.

Current AdvC optimizes `CvTeam::isHuman()` to:

    return (getLeaderID() != NO_PLAYER && GET_PLAYER(getLeaderID()).isHuman());

and `updateLeaderID()` explicitly prefers a human member. K-Mod/Civ4CE use a full member
scan and likewise return true if ANY teammate is human. Thus this is not a questionable
SAS interpretation of mixed-team semantics; the branch really is team-human while the
reward recipient can be AI.

NORMAL RESEARCH CAN CREDIT THE AI TEAMMATE
------------------------------------------
Each player executes `CvPlayer::doResearch` independently and contributes its own research
rate to the shared team technology through:

    GET_TEAM(getTeam()).changeResearchProgress(eCurrentTech, ..., getID(),
            TECH_ACQUISITION_RESEARCH);

If AI teammate B's contribution crosses the team research cost, `setResearchProgress`
passes B straight through to:

    setHasTech(eTech, true, ePlayer, true, true, true, eCause);

So ordinary team research can make `ePlayer == B` even though human teammate H makes the
team satisfy `CvTeam::isHuman()`.

HUMAN-ONLY CHOOSETECH IS THEN CALLED ON THE AI
-----------------------------------------------
`CvPlayer::chooseTech` begins with the contract:

    FAssert(isHuman());

and, for a free choice, increments that PLAYER'S local free-tech token:

    changeChoosingFreeTechCount(1);

It then queues a `BUTTONPOPUP_CHOOSETECH` popup specifically for that player ID.

Under F482, B is AI, so:

* a debug build hits the `FAssert(isHuman())` contract violation;
* release code increments B's `m_iChoosingFreeTechCount` anyway;
* the popup is queued to AI B rather than human teammate H;
* the normal AI reward path `AI_chooseFreeTech()` was skipped solely because the TEAM is
  human.

THE UI CANNOT RESCUE B'S TOKEN
------------------------------
The only current DLL decrement/consumer of `m_iChoosingFreeTechCount` is the research
widget path in `CvDLLWidgetData::doResearch`, and it always operates on:

    CvPlayer& kActivePlayer = GET_PLAYER(getActivePlayer());

For a free-tech click it checks/decrements `kActivePlayer.isChoosingFreeTech()`.

Likewise `CvDLLButtonPopup::OnFocus` for `BUTTONPOPUP_CHOOSETECH` checks the ACTIVE player's
free-tech state and cancels a free-tech popup when the active player is not choosing one.
The popup click path also uses the active player/UI research flow.

There is no AI-turn consumer of `m_iChoosingFreeTechCount` anywhere else in the current
DLL. Therefore B's token is not converted into a technology by AI logic and is not
transferable to H. It can simply remain serialized on the AI while the one-shot first-
discovery reward has already been claimed.

CURRENT-SAS PRODUCER
--------------------
Current SAS `TECH_THEORY_OF_EVOLUTION` is a normal Industrial technology with:

    iFirstFreeTechs = 1

and ordinary research prerequisites. No special goody/recycle setup is required.

Supported reproduction:

1. Create an ordinary team game with human H and AI B on the same team.
2. Leave Theory of Evolution unknown to all teams.
3. Let H and B both contribute to the shared research race normally.
4. Arrange the final required beakers so B's `doResearch()` contribution crosses the
   Theory of Evolution cost first.
5. `setHasTech(..., ePlayer=B, bFirst=true)` sees the team as human.
6. Instead of `B.AI_chooseFreeTech()`, current code calls `B.chooseTech(1, ...)`.
7. Debug: assertion because B is not human. Release: B gets a free-tech-choice counter
   and an AI-addressed popup, but no AI free tech is selected.
8. Human H does not own B's token; normal active-player popup/widget logic cannot spend it.
9. Theory of Evolution is already known, so the first-discovery block is never replayed.

The free technology is therefore stranded/lost rather than awarded.

PERSISTENCE
-----------
`m_iChoosingFreeTechCount` is serialized in `CvPlayer::read/write`. Saving/loading can
therefore preserve the malformed AI free-tech-choice state, but loading does not convert
it into `AI_chooseFreeTech()` and does not replay Theory of Evolution's first-discovery
reward. The missed reward is not self-healing.

DISTINCTNESS / DUPLICATE CONTROL
--------------------------------
F313 / KI#636 is different. F313 starts with a HUMAN player who legitimately owns a
free-tech popup; while that popup is open a teammate acquires one displayed technology,
and clicking the stale entry decrements the human token before the invalid selection is
rejected. F313 is stale popup CONTENT after valid routing.

F482 is wrong RECIPIENT/CONTROL routing before a valid popup exists. The team-level
`isHuman()` branch calls the human-only API on AI `ePlayer`, leaving the token on the AI
and bypassing `AI_chooseFreeTech()` entirely.

F481 is also independent: it concerns a cityless acquisition owner's first-free-unit
placement at a NULL capital, not free-tech UI ownership.

No prior album root was found for this mixed-team first-free-tech routing mismatch.

ANCESTRY / COMPARISON
---------------------
The same structural mismatch exists in Civ4CE/stock-style and K-Mod code: team
`isHuman()` is true if any member is human, while the first-free-tech block invokes
`GET_PLAYER(ePlayer).chooseTech(...)` on the credited acquisition player. AdvC/SAS retain
that inherited design (with a faster human-team test based on the human-preferred leader).

Current SAS supplies an explicit live first-free-tech producer through its reworked
`TECH_THEORY_OF_EVOLUTION` (`iFirstFreeTechs=1`).

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Route the reward according to the RECIPIENT player's control type, not the team:

    if (GET_PLAYER(ePlayer).isHuman())
        GET_PLAYER(ePlayer).chooseTech(...);
    else
        GET_PLAYER(ePlayer).AI_chooseFreeTech();

If team design instead intends a human teammate to control all team first-discovery free
techs, then an explicit human recipient must be selected and the free-tech token/reward
must be assigned to that human player; do not call a human-only popup method on an AI.

The narrow per-recipient branch is the least surprising match for current `ePlayer`
semantics and mirrors the ownership of `m_iChoosingFreeTechCount`.

REGRESSION TESTS
----------------
A. Mixed-team AI completion

1. Human H + AI B share a team.
2. Theory of Evolution is globally unknown and one beaker short before B's research turn.
3. Let B complete it.
4. Assert B receives exactly one AI-selected free technology, no human-only assertion is
   hit, and B's `m_iChoosingFreeTechCount` remains zero.

B. Mixed-team human completion

Repeat with H supplying the final beakers. Assert H receives exactly one normal free-tech
choice popup/token.

C. Save/load malformed-state prevention

After B completes the tech, save/load. Assert no unresolved AI `ChoosingFreeTechCount`
exists and the awarded free technology remains exactly one.

NUMBERING
---------
F482 / KI#805 is CONFIRMED.
F483 / provisional KI#806 is the next unused root.

CURRENT STONE AFTER C031-WIP206
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F482 / KI#805 CONFIRMED.
F483 / provisional KI#806 UNUSED.
Latest physical checkpoint: C031-WIP206.
No repository source files modified.

NEXT
----
F483 unused -> finish the immediate mixed-team first-discovery notification/perk siblings
without splitting the same human-vs-recipient contract -> then deliberately leave the
first-discovery seam -> resume historical-fix x low-semantic-coverage authoritative
CvPlayer.cpp execution functions, larger/stateful bodies before thin accessors -> current
SAS caller/data + concrete consequence + ancestry + semantic duplicate proof mandatory ->
create C031-WIP207 at the next meaningful settled tranche.

END C031-WIP206


===============================================================================
C031-WIP207 — POST-F482 RECONCILIATION / BROAD COVERAGE TRANCHE; F483 UNUSED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP206 with F482/KI#805 confirmed and F483/KI#806 unused.
This checkpoint deliberately preserves a substantial closure tranche rather than
manufacturing another first-discovery or lifecycle number.

F482 HISTORICAL-SIBLING STRENGTHENING
-------------------------------------
The remainder of `CvTeam::setHasTech` gives an unusually strong internal contract control
for F482. Later in the SAME function, ordinary post-acquisition human UI handling contains
an explicit advc.001 repair:

    // Humans always do the choosing in human-AI teams
    if (!GET_PLAYER(ePlayer).isHuman())
    {
        CvPlayer& kLeader = GET_PLAYER(getLeaderID());
        ...
        ePlayer = kLeader.getID();
        ...
    }

That code recognizes precisely the distinction F482 needs: a TEAM can be human while the
credited technology-acquisition PLAYER is AI, and human-only UI must not simply be invoked
on that AI player.

The earlier `iFirstFreeTechs` block does not perform this reconciliation. It tests
`CvTeam::isHuman()` and then calls `GET_PLAYER(ePlayer).chooseTech`, whose own contract is
`FAssert(isHuman())`. This makes F482 look less like a debatable team-reward policy and more
like a missed sibling of an already-recognized mixed human/AI team UI rule.

IMMEDIATE FIRST-DISCOVERY SIBLINGS AFTER F482 — CLOSED
------------------------------------------------------
The remaining first-discovery notification/perk code was traced before leaving the seam.
No independent F483 was established:

* first-to-tech replay messages use the credited discoverer only for identity/attribution;
  no player-local consumable state is written;
* observer messages correctly choose known/unknown discoverer text and use team-relative
  coordinates; a city/capital is not required for the entitlement itself;
* competitor AI research-queue invalidation is deliberately keyed to `bFirstPerk` and only
  asks competing AI players to reconsider a now-claimed reward tech;
* `techAcquired`/Python/event reporter calls receive the actual credited player and do not
  route a human-only control token through that player;
* ordinary next-research/tech-splash handling has the explicit mixed-team redirect quoted
  above.

So F480 (organization founder capability), F481 (NULL-capital free-unit placement), and
F482 (team-human/player-AI free-tech UI routing) remain three independent current contracts;
there is no justified F483 merely because they share the first-discovery block.

HANDLEDIPLOEVENT / CANTRADEWITH HISTORICAL-FIX PASS
---------------------------------------------------
The previously ranked low-semantic-coverage `CvPlayer::handleDiploEvent` and
`CvPlayer::canTradeWith` families were re-read against current SAS semantics and existing
album ownership.

Controls retained:

* `GET_TEAM(ePlayer)` in join-war / stop-trading branches is the known overloaded
  PlayerTypes form, not a raw TeamTypes enum-index bug. Do not reflag it.
* the custom voluntary-vassal joint-war target and sponsor-coalition gates are the already
  owned KI#373 / KI#612 repair family.
* demand/help memory direction follows the explicit advc.130j/advc.130o comments and
  existing AI memory contracts; no inverted current producer was established.
* `DIPLOEVENT_TARGET_CITY` validates the city pointer and only stores a target when the
  requested city is revealed to the requesting team.
* generic `canTradeWith` first applies team/network availability and then builds both
  directions' concrete trade tables. No live item was found that bypasses its own
  `canTradeItem`/`canPossiblyTradeItem` legality merely because the broad gate is permissive.

No F483.

RECYCLED PLAYER-INDEXED CITY/PLOT ARRAY COMPLETENESS — CLOSED AROUND OWNED ROOTS
-------------------------------------------------------------------------------
A final player-indexed persistent-field inventory was run after the F477-F479 lifecycle
family.

`CvPlayer::resetPlotAndCityData` explicitly scrubs the recycled player's indexed state in
surviving map objects for:

* plot culture;
* plot found values;
* city culture;
* city revolt counts;
* city EverOwned flags;
* city trade-route flags.

The remaining player-indexed city-radius/culture-range counters are live-city-derived and
are removed through ordinary city kill/transfer lifecycle hooks; no independent stale
recycled value was proven.

Historical bare PlayerTypes VALUES such as city OriginalOwner/PreviousOwner,
BuildingOriginalOwner, Parent and revealed-owner identity are already the F477 identity
alias family. Per-player plot-group object IDs are F478. Team-indexed blockade counts are
F479. No F483 was split from those already-owned invariants.

SETCOMMERCEPERCENT / FLEXIBILITY TRANSITION PASS — CLOSED
---------------------------------------------------------
`CvPlayer::setCommercePercent` has a superficially suspicious second balancing pass that,
when `bForce == false`, is allowed to revisit the requested flexible commerce type itself.
If that pass restored the target to its old value after changing other channels, the early:

    if (iOldValue == getCommercePercent(eCommerce))
        return false;

could theoretically leave changed sibling percentages without `updateCommerce()`.

Current commerce data closes the producer. Gold and Research are both permanently
`bFlexiblePercent=1`. Culture and Espionage become adjustable only through explicit
flexibility grants, and a channel losing flexibility is forced back to its initial percent.
Starting from the required total of 100%, the first balancing pass therefore always has
sufficient OTHER flexible Gold/Research capacity to absorb a legal non-forced slider change;
the problematic fallback-to-self shape is not reached in current supported state.

Forced changes can intentionally redistribute otherwise inflexible channels and do not use
the same early-return hazard. No current F483.

OTHER LOW-COVERAGE CONTROLS IN THIS TRANCHE
-------------------------------------------
* `isValidTriggerReligion` / neighboring corporation-event validity helpers keep religion/
  holy-city or corporation/HQ checks aligned with whether a concrete city was supplied.
* `changeStrikeTurns` itself is a thin nonnegative counter; strike activation/deactivation
  and unit disband behavior live in separately audited execution paths.
* `updateHistory` retains the inherited team-espionage-history interpretation already
  classified earlier; no new player/team subject inversion.
* `promoteFreeUnit`'s odd-looking Combat-I filter is part of a compound promotion-category
  predicate; it does not reject every nonzero promotion enum as a first reading might imply.
* third-party espionage announcements disclose the target identity only to observers already
  selected through the relevant known-civ iterator; no new visibility leak established.

NUMBERING
---------
F483 / KI#806 remains UNUSED.

CURRENT STONE AFTER C031-WIP207
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F482 / KI#805 CONFIRMED.
F483 / provisional KI#806 UNUSED.
Latest physical checkpoint: C031-WIP207.
No repository source files modified.

NEXT
----
F483 unused -> resume large-body x low-semantic-coverage authoritative ranking outside the
first-discovery/recycle seams -> prioritize stateful economic/diplomatic/event execution over
UI/name/setup helpers -> historical-fix sibling scan + current SAS caller/data + concrete
consequence + ancestry + semantic duplicate proof mandatory -> preserve closures and create
C031-WIP208 at the next meaningful settled tranche.

END C031-WIP207


===============================================================================
C031-WIP208 — QUEUE 004 CVPLAYER.CPP — F483 / KI#806 CONFIRMED
===============================================================================

CONTINUATION ANCHOR
-------------------
Resumed from C031-WIP207 with F482/KI#805 confirmed and F483/KI#806 unused.
The first-discovery / handleDiploEvent seams were left deliberately saturated. This
tranche returned to larger authoritative CvPlayer state, then used serialized-field /
reset completeness and historical-BtS/K-Mod comparison to look for an independent live
root rather than spending a number on thin setters or UI helpers.

No repository source files were modified.


POST-WIP207 CLOSURE — PLAYER-OWNED SERIALIZED RESET COMPLETENESS
----------------------------------------------------------------
A mechanical inventory compared serialized CvPlayer members against reset/uninit.
The superficially unmatched containers (city names, group cycle, research queue,
messages/popups/diplomacy lists, etc.) are cleared by uninit(), which CvPlayer::reset
calls first. m_eTeamType is rebuilt from InitCore. The scalar / enum-map state written
by CvPlayer::write is otherwise reset explicitly.

So this pass did not expose another F473-F479-style defect where the recycled player's
OWN serialized field simply survives reset. The stronger remaining recycle omissions
continue to be state stored outside the player (plot/team IDs, historical bare PlayerTypes
values, etc.), which are already owned by F477-F479 or by parked robustness notes.


POST-WIP207 CLOSURE — AREA DERIVED STATE IS STALE-STORAGE, NOT YET A LIVE ROOT
-------------------------------------------------------------------------------
The remaining CvArea per-player/team arrays were rechecked against defeat / recycle:

* best-found values are explicitly zeroed/recomputed by AI_updateFoundValues before
  ordinary new-player settler AI decisions;
* AreaAIType is recomputed by CvTeamAI::AI_doTurnPre before the recycled team's normal
  turn execution;
* stale target-city storage remains structurally uncomfortable but no supported
  pre-refresh decision was established in this pass;
* area city/unit/building aggregates are decremented through normal city/unit/building
  teardown rather than relying on CvPlayer::reset.

No F483 was spent on stale storage without a current concrete consumer.


POST-WIP207 EVENT CONTROL — EMPIRE-WIDE MAXPILLAGE CITY FILTER IS CURRENT-DATA DORMANT
---------------------------------------------------------------------------------------
CvPlayer::applyEvent contains a suspicious empire-wide pillage loop:

    if (pPlot == NULL || pPlot->getOwner() != getID() || !pPlot->isCity())
        continue;
    if (pPlot->isImproved() && !...isPermanent())
        ...

Ordinary city founding explicitly calls setImprovementType(NO_IMPROVEMENT), so a live
city plot normally cannot provide the pillageable improvement this branch asks for.

However, current SAS does NOT route its live Looters outcomes through this player-wide
branch. EVENT_LOOTERS_1/2/3 all have bPickOtherPlayerCity=1; CvEventInfo treats that as
an other-player-city effect, and CvPlayer::applyEvent dispatches the material pillage
work to CvCity::applyEvent instead. The live Looters range defect is already owned by
F413/KI#736 (inclusive iMaxPillage is unreachable because Rand(max-min) omits the upper
endpoint).

No current empire-wide non-city EventInfo with positive iMaxPillage was established.
Therefore the backwards city filter is preserved as generic robustness / mod-data debt,
not F483.


F483 / KI#806 — CONFIRMED — A SURVIVING MASTER TEAM LOSES PART OF ITS VASSAL POPULATION/LAND SCORE WHEN ONE MASTER TEAMMATE DIES
--------------------------------------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current team-game + Vassal States gameplay reaches the defect.
It is inherited from the Warlords/BtS vassal-score implementation through Civ4CE,
K-Mod, Base AdvCiv 1.14 and current AdvCiv-SAS.


ROOT CONTRACT
-------------
CvPlayer::getPopScore(true) and getLandScore(true) implement the vassal-score transfer
in two complementary halves:

1. A player whose TEAM is a vassal returns only half of its own raw population / land
   score:

       if (bIncludeVassals && isAVassal())
           return m_iPopulationScore / 2;

   (and analogously for land).

2. A master player sums half of every living vassal player's raw score, then divides
   that master-team contribution by the master's team-member count:

       iVassalScore += itVassal->getPopScore(false) / 2;
       ...
       return m_iPopulationScore +
           iVassalScore / std::max(1, GET_TEAM(getTeam()).getNumMembers());

The division is necessary in a multi-player master team: without it, every teammate
would receive the full transferred half and CvGame's team-score sum would multiply the
vassal contribution by the number of master players.

When all master teammates are alive, the implementation therefore has a clear invariant:
collectively the master TEAM receives one half of the vassal's population/land score,
independent of the number of master-team members.


BROKEN DEATH TRANSITION
-----------------------
CvTeam::getNumMembers() is persistent team membership. Player death changes
CvTeam::m_iAliveCount through changeAliveCount, but does NOT remove the dead player from
m_iNumMembers; membership changes only through player/team initialization or
CvPlayer::setTeam.

At the same time, CvPlayer::calculateScore explicitly returns zero for a dead player:

    if (!isAlive() || GET_TEAM(getTeam()).getNumMembers() <= 0)
        return 0;

CvGame::updateScore then computes each team score by summing the cached player scores of
all players assigned to that team. Thus a dead teammate contributes zero to the team
score, including zero of the vassal share that had been allocated to that teammate.

But every surviving master member still divides the vassal contribution by the old,
persistent getNumMembers() count. The dead member's fraction is therefore not reassigned
to the survivors; it simply disappears from the master team's score.


DETERMINISTIC TWO-MASTER EXAMPLE
--------------------------------
Let master Team M contain living players A and B, and let living vassal V have a raw
population-score input of 100 (the same arithmetic applies to land).

Before B dies:

* V's own getPopScore(true) contributes 50 because V is a vassal.
* Each master member computes half of V's raw score (50), then divides by
  Team M getNumMembers()==2, receiving +25.
* A+B collectively therefore receive +50, exactly the other half of V's score.

Now eliminate B while A survives:

* Team M remains alive and its vassal relationship remains; CvTeam::changeAliveCount
  only performs full-team death cleanup when the team's alive count reaches zero.
* Team M getNumMembers() remains 2.
* B's calculateScore becomes 0 because B is dead.
* A still receives only +25 because it divides by persistent NumMembers==2.

The master team therefore receives only one quarter of V's raw population contribution
instead of the intended one half. The missing +25 is not credited anywhere. The same
loss occurs independently in land score.

With N original master members but only L currently alive, the master team's collective
vassal share is effectively multiplied by L/N.


CURRENT GAMEPLAY CONSEQUENCE
----------------------------
CvPlayer::setAlive marks the game score dirty. CvGame::updateScore recomputes dead B as
zero and recomputes A with the unchanged persistent member divisor, then builds
m_aiTeamScore by summing the per-player scores assigned to Team M.

Current SAS ships VICTORY_TIME with bEndScore=1. The resulting team score is therefore
not merely scoreboard presentation: an eliminated teammate can lower the surviving
master team's end-score / Time-victory ranking by deleting part of its still-valid
vassal population and land contribution. Team-score consumers in AI/ranking logic see
the same reduced m_aiTeamScore.

No debug, WorldBuilder, revival, recycled-player defect or Permanent Alliance is needed.
A normal multi-player/team setup in which one member is eliminated while another member
of the same team survives, with an existing living vassal, is sufficient.


WHY THIS IS NOT F469 OR THE EARLIER PA SCORE CONTROL
----------------------------------------------------
F469/KI#792 concerns an AdvCiv-specific cached CITY-MAINTENANCE formula whose divisor was
changed to alive-only MemberIter::count without adding death invalidation. F483 is the
opposite shape: score is dynamically recomputed correctly on death, but the live formula
itself uses the wrong persistent membership domain for distributing a team-level vassal
share among the players who actually contribute to team score.

C031-WIP179 previously checked Permanent Alliance team-size score changes and correctly
closed that transition because CvTeam::addTeam calls updateScore(true) after membership
changes. F483 is not stale score after a PA. Death dirties/recomputes score too; the
recomputed value is itself wrong because dead members remain in getNumMembers().

Semantic duplicate search found no prior album root for the eliminated-master-member
vassal-share loss.


ANCESTRY
--------
The same design is present in the local Civ4CE Warlords and BtS sources:

    vassal player -> own population/land score / 2
    master player -> (sum vassal raw / 2) / team getNumMembers()

K-Mod retains it. Base AdvCiv 1.14 converts the vassal-player loop to its iterator form
but retains getNumMembers() as the divisor. Current SAS inherits the same arithmetic.

This is therefore a stock Warlords/BtS edge-case defect, not a SAS-local regression and
not an AdvCiv AgentIterator regression.


REPAIR DIRECTION
----------------
Distribute the master-team vassal share across currently ALIVE master members, i.e. use
an authoritative alive-member count rather than persistent membership for the divisor:

    std::max(1, GET_TEAM(getTeam()).getAliveCount())

in both getPopScore(true) and getLandScore(true).

Using CvTeam::getAliveCount is preferable to relying on a cached member iterator for this
repair: it directly matches calculateScore's alive-player participation contract and is
not exposed to the human-PA AgentIterator invalidation family from F470.

A regression test should use a two-member master team with one vassal, record the
collective master vassal population/land contribution, eliminate one master member, and
assert that the surviving master receives the full remaining team allocation rather than
letting half of the transfer disappear.


NUMBERING
---------
F483 / KI#806 is CONFIRMED.
F484 / provisional KI#807 is the next UNUSED root.


CURRENT STONE AFTER C031-WIP208
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F483 / KI#806 CONFIRMED.
F484 / provisional KI#807 UNUSED.
Latest physical checkpoint: C031-WIP208.
No repository source files modified.


EXACT CONTINUATION
------------------
F484 unused -> finish the immediate score-domain siblings (vassal tech/wonder score is
not shared through this formula; team score / domination inputs as duplicate controls) ->
resume historical-fix x low-semantic-coverage authoritative CvPlayer execution ->
prioritize normal-turn economic/diplomatic/lifecycle state over UI/name/setup helpers ->
current SAS caller/data + concrete consequence + ancestry + semantic duplicate proof
mandatory -> create C031-WIP209 at the next meaningful settled tranche.

END C031-WIP208

===============================================================================
C031-WIP209 — QUEUE 004 CONTINUATION — F484 UNUSED / CLOSURE CHECKPOINT
===============================================================================

Continuation anchor
-------------------
Continued from C031-WIP208 with Queue 004 `CvGameCoreDLL/CvPlayer.cpp` OPEN,
F483/KI#806 confirmed and F484/KI#807 unused. No repository source files were
modified.

POST-WIP208 CONTROL — PERSISTENT TEAM SIZE IN RESEARCH / ESPIONAGE IS PAIRED POLICY, NOT F484
---------------------------------------------------------------------------------------------
The immediate F483 sibling check compared other uses of `CvTeam::getNumMembers()`.
`CvTeam::getResearchCost` deliberately scales technology cost by persistent team
membership, and AdvCiv's `CvPlayer::adjustMissionCostToTeamSize` deliberately derives
its espionage team-size adjustment from the same TECH_COST_EXTRA_TEAM_MEMBER_MODIFIER
and the same persistent member counts.

Unlike F483, there is no conservation identity here saying that only players who still
contribute a score may remain in the divisor. Making these costs shrink when a teammate
dies would itself create an abrupt mid-research / mid-game discount and would change an
established paired balance rule. No upstream contract or current caller proved that the
team-size handicap is intended to follow alive count. Keep the F483 `getAliveCount()`
repair local to the vassal-score distribution; no F484.

POST-WIP208 DUPLICATE CONTROL — PROJECTED UNIT-COST ASYMMETRY IS F446
--------------------------------------------------------------------
A fresh arithmetic pass rediscovered that `calculateUnitCost(..., iExtraUnits)` applies
its generic projected unit delta in a way that also distorts the projected military paid
count. Semantic album reconciliation shows this is exactly F446/KI#769. Do not duplicate
it as F484.

POST-WIP208 COMMERCE / CAPITAL / PROJECT CONTROLS
-------------------------------------------------
* `setCommercePercent`'s balancing behavior was already explicitly reconciled in WIP194.
* `updateCommerceRates`'s repeated-largest-rounding-remainder defect is exactly
  F444/KI#767, not a new CvPlayer root.
* `removeBuildingClass` / `findNewCapital` continue to reconcile with the earlier Palace
  relocation and capital-yield/cache findings; no independent current state failure was
  established in this pass.
* `canCreate(ProjectTypes,...)` remains consistent with the inherited project prerequisite,
  project-limit, victory-countdown and No-Nukes contracts. No current project-data sibling
  cleared the finding threshold.

POST-WIP208 TEAMSHARE REVALIDATION — CURRENT XML IS LIVE, BUT THE REMOVAL COUNT WRINKLE REMAINS ROBUSTNESS DEBT
--------------------------------------------------------------------------------------------------------------
The latest September 1 SAS Building XML was rechecked rather than relying on old
"current-data masked" statements. `BUILDING_THREE_GORGES_DAM` and
`BUILDING_GREAT_WALL` are live `bTeamShare=1` buildings. This is already corrected in
the cumulative album by WIP184; do not revive the earlier WIP181 statement that there
were no TeamShare buildings.

Permanent Alliance formation explicitly replays TeamShare building effects across the
new team, so the live post-merge boolean effects are present. A count-weighting wrinkle
remains when a pre-merge TeamShare area effect is later removed: absorbed-team increments
were not migrated to the survivor's team-keyed count, while removal can decrement for all
merged members and leave a negative dormant count. Current data do not provide a later
source that makes that negative residue produce a wrong live effect (Great Wall is then
obsolete; Three Gorges Dam is a one-off World Wonder). Preserve as robustness/accounting
debt already owned by the prior TeamShare reconciliation; no F484.

POST-WIP208 LATEST-DATA RECHECK — EARLIER MASKED EVENT / MODDABILITY HAZARDS REMAIN MASKED
------------------------------------------------------------------------------------------
Selected earlier data-masked controls were rechecked against the latest source rather
than assumed unchanged. The important sampled cases remain non-producing under shipped
SAS data, including the Global/Team quest lifetime asymmetries and the legacy spy
Destroy/Buy Unit mission validation family. Current random-Gold events still use positive
base gold with zero tech-cost percent, so the inherited negative-range help typo remains
unreachable through shipped event data.

POST-WIP208 GREAT-PEOPLE THRESHOLD ZERO LEAD — CLOSED BY PRODUCER GRAPH
------------------------------------------------------------------------
`greatPeopleThreshold` defensively clamps `(thresholdModifier + 100)` at zero, which
initially raised the question whether a live <= -100 modifier could produce a zero
threshold and destabilize the Great General / Great Person award loops.

Current DLL producer tracing closes the lead. Outside the two setter definitions,
`changeGreatPeopleThresholdModifier` and `changeGreatGeneralsThresholdModifier` are only
called from `createGreatPeople`, where the threshold is increased as Great People / Great
Generals are created (including the corresponding team increment). No ordinary current
negative XML/state producer was found. The zero clamp is defensive/moddability behavior,
not a current F484 path.

POST-WIP208 LOW-SEMANTIC-COVERAGE / SAS-DELTA PASS
--------------------------------------------------
A current SAS-vs-AdvCiv changed-function ranking was cross-referenced with cumulative
album mentions. Most zero-name SAS deltas are UI/naming/logging helpers. The larger live
stateful candidates already examined here (`processBuilding`, `canCreate`,
`greatPeopleThreshold`, `setCurrentEra`, `getTypicalUnitValue`, `updateHuman` and nearby
turn-state helpers) have so far either matched intentional inherited policy, prior album
ownership, or current-data-safe behavior. Continue to rank by authoritative consequence,
not literal function-name novelty.

CURRENT STONE AFTER C031-WIP209
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F483 / KI#806 CONFIRMED.
F484 / KI#807 UNUSED.
Latest physical checkpoint: C031-WIP209.
No repository source files modified.

NEXT
----
Continue Queue 004 from F484 unused. Keep rechecking old current-data-masked controls
only when latest SAS content may have activated them, but use semantic duplicate/scope
reconciliation before promotion. Resume larger authoritative low-coverage CvPlayer
bodies and historical bug-fix siblings, prioritizing lifecycle/alive-vs-persistent,
ownership/team-state and player-wide cached/derived effects over UI/setup wrappers.
Require current SAS producer/data + deterministic consequence + ancestry + semantic
duplicate proof before F484. Create WIP210 at the next meaningful settled tranche.

END C031-WIP209


AdvCiv-SAS C++ file audit album
C031-WIP210 continuation checkpoint
Date: 2026-09-01

STATE
Queue 004: CvGameCoreDLL/CvPlayer.cpp OPEN
32/169 unique files complete
F445 / KI#768 RETRACTED
F446 / KI#769 through F483 / KI#806 CONFIRMED
F484 / KI#807 UNUSED
Previous physical checkpoint: C031-WIP209
Current physical checkpoint: C031-WIP210
Repository source files modified: NO

PURPOSE OF THIS CHECKPOINT
This checkpoint preserves the settled tranche after C031-WIP209. It is intentionally conservative: several plausible leads were traced through current SAS callers/data, ancestry and semantic-duplicate controls and then closed. F484 remains unused.

PRIOR ANCHOR
F483 / KI#806 remains CONFIRMED: getPopScore(true) and getLandScore(true) split the master's transferred vassal half using persistent CvTeam::getNumMembers(), while dead players contribute zero player score. If only L of N persistent master-team members remain alive, the master team's intended vassal share is effectively multiplied by L/N. Current VICTORY_TIME uses end score, so the error can affect victory ranking. This is an inherited Civ4CE/BtS -> K-Mod -> AdvCiv -> SAS edge case, distinct from F469's stale city-maintenance cache. Natural repair candidate: use alive membership for this score-allocation divisor.

POST-WIP209 TRANCHE

1. Persistent-team-size research / espionage scaling: CLOSED AS POLICY
The earlier follow-up from F483 was completed. Research and espionage team-size modifiers deliberately share the persistent team-size concept. Unlike F483's explicit conservation contract (one transferred half split among actual scorers), no source contract was found saying these balance modifiers must shrink when a teammate dies. Changing them on death would itself create a mid-tech / mid-game balance discontinuity. No F484.

2. calculateUnitCost(..., iExtraUnits) asymmetry: DUPLICATE
A fresh arithmetic pass rediscovered the civilian/military projected-unit asymmetry, but it is exactly F446 / KI#769. Excluded from new numbering.

3. TeamShare building revalidation against latest 2026-09-01 data: ROBUSTNESS DEBT, NOT CURRENT WRONG EFFECT
Current SAS now has live TeamShare buildings (Three Gorges Dam and Great Wall), so an older masked control had to be rechecked. Permanent-alliance/team replay gives the merged team the intended live boolean effect. A later removal can over-decrement an old team-keyed area count because absorbed members' pre-merge increments are not migrated; with current content this leaves only a negative dormant count after the sole source disappears/obsoletes, not a wrong live boolean effect. No F484.

4. Great Person / Great General threshold zero: CLOSED
Threshold calculation defensively clamps modifier+100 at zero. Current producer tracing shows the threshold modifiers are changed only by the normal escalating GP/GG creation path; no shipped negative producer can drive the threshold multiplier to zero. No award loop under current data.

5. Cityless Great General under Complete Kills: CLOSED
AdvCiv no longer implements ordinary post-city Complete Kills survival semantics in verifyAlive. The supported cityless-alive state is essentially pre-first-city. If combat XP crosses the GG threshold there, the XP is not consumed; it remains banked and can award after a city exists and combat XP changes again. Delayed award, not lost award. No F484.

6. Team Power / Assets alive-vs-persistent membership: CLOSED
MemberIter is PlayerIter<ALIVE, MEMBER_OF>. CvTeam::getPower() and getAssets() therefore stop summing an eliminated teammate automatically, even if some serialized player-level quantities survive teardown. This is another reason F483 is a special domain mismatch rather than a general getNumMembers/getAliveCount replacement campaign.

7. Event propagation name collision kept separated
setTriggerFired team propagation is correct. The previously identified inverted predicate is specifically setEventOccured's Team EventInfo propagation. Current SAS has zero Team EventInfos, so that source defect remains dormant. Do not repair setTriggerFired by analogy.

8. getEventTriggerWeight building probability multiplier: CURRENT-DATA CLOSED
The minimum required building count gates eligibility; BuildingsRequired defines the building classes counted by the multiplier. More decisively, current SAS ships zero triggers with bProbabilityBuildingMultiply=1. No F484.

9. isValidEventTech apparent PlayerTypes/TeamTypes mismatch: FALSE POSITIVE, IMPORTANT ADV-CIV ACCESSOR RULE
Current SAS has:
    if (eOtherPlayer != NO_PLAYER && !GET_TEAM(eOtherPlayer).isHasTech(eTech))
Literal comparison with K-Mod/Civ4CE looks suspicious because they write the explicit player->team conversion. However AdvCiv deliberately changed GET_TEAM into overloaded accessors in CvGamePlay.h:
    #define GET_TEAM(x) CvGamePlay::getTeam(x)
    #define TEAMID(x) GET_PLAYER(x).getTeam()
    getTeam(TeamTypes) -> team directly
    getTeam(PlayerTypes) -> CvTeam::getTeam(TEAMID(ePlayer))
Therefore GET_TEAM(eOtherPlayer) is type-correct and resolves the player's team. This also protects similarly written AdvCiv/SAS calls elsewhere. K-Mod/Civ4CE needed the explicit GET_PLAYER(...).getTeam() spelling because they lack this overload. Do NOT classify these lines by syntax ancestry alone.
Current XML even supplies a live tech+other-player route (Airliner Crash), so this was worth proving rather than dismissing as dormant. No F484.

10. expireEvent / checkExpireEvent / persistent OtherPlayerCityId quest references: SHIPPED QUEST GRAPH CLOSED
The earlier concern was that a persistent quest record containing another player's city ID might expire incorrectly when that city changes owner. Current shipped quest tracing narrows the important other-player-city case to Crusade. CvPlayer::acquireCity contains an explicit inherited repair for the acquirer: if an event record points to the old owner's captured city ID, m_iOtherPlayerCityId is reset to -1 so the event does not expire merely because the acquirer conquered the target. Crusade's Python expiry logic separately treats a third-party capture / wrong owner as quest failure. That matches the intended distinction. No independent F484.

11. getDiscoveryTech / popResearch: CLOSED
getDiscoveryTech keeps the inherited Great-Person discovery behavior: among currently researchable technologies, choose the highest positive flavor overlap; no source-proven current defect was established.
popResearch removes the matching queue node when present and then clears the player's researching-tech boolean. That state is a boolean set, not a decrementing counter. The live CvTeam::setHasTech acquisition caller first checks isResearchingTech(eTech) before popResearch. clearResearchQueue removes the full list and clears all corresponding booleans. No negative-count or unrelated-tech corruption route found. Research-path selection remains semantically owned by earlier F453; do not duplicate it here.

12. foundCorporation: CURRENT-DATA DORMANT
Current SAS ships seven corporations and all seven have TechPrereq=NONE. CvGame::doHeadquarters explicitly skips corporations with NO_TECH prerequisite in the tech-triggered founding path; CvTeam::setHasTech likewise can only found a corporation whose prerequisite equals the newly acquired tech. Thus CvPlayer::foundCorporation's tech-triggered path is dormant under shipped corporation data. No current F484.

13. getModifiedWarWearinessPercentAnger: SAS CHANGE IS PERFORMANCE-ONLY; NO NEW DEFECT
The current SAS body is functionally the AdvCiv body with a cached CvGame reference. The player/building/civic war-weariness modifier is applied later in city anger calculation rather than omitted here. No current independent state error established.

14. changeGoldenAgeTurns / changeGoldenAgeModifier: CLOSED
SAS differences are logging and cached color lookups, not gameplay arithmetic changes. Decrement callers are guarded by positive turns; event/GP additions use valid golden-age lengths. Current XML's live GoldenAgeModifier source is positive (e.g. Pyramids +50), with no shipped extreme negative producer that would establish a current bad rescale path. No F484.

15. changePersonalityType: CLOSED
Current SAS change is a static cache of BARBARIAN_LEADER. Random-personality selection semantics remain inherited. No gameplay regression established.

16. disbandUnit: CLOSED FOR THIS PASS
Current SAS functional differences from AdvCiv are the renamed UNITAI_GREAT_* enums plus a cached message color; selection behavior is inherited. No current independent source defect survived caller/data review. Great-person UnitAIs are deliberately protected. No F484.

17. validateDiplomacy: CLOSED FOR THIS PASS
The removal loops are structurally safe: invalid offer deletion is followed by break/restart where needed rather than iterator reuse. Stop-trading, defensive-pact-after-war, and peace/surrender validation paths were reviewed; no independent current defect was established.

18. spy helper/current espionage XML reachability: CLOSED FOR UNIT BRIBE/DESTROY PATHS
canSpyDestroyUnit and canSpyBribeUnit may look worth auditing in isolation, but current CIV4EspionageMissionInfo.xml has no mission with nonzero iDestroyUnitCostFactor or iBuyUnitCostFactor. The live destroy-building and destroy-project missions use their own checks and cost branches. No F484 from the dormant unit paths.

19. Advanced Start improvement/route build-prerequisite multiplicity: CURRENT-DATA MASKED, NO F484
getAdvancedStartImprovementCost and getAdvancedStartRouteCost iterate Builds that produce the requested improvement/route and require their prerequisite technologies. This would need careful OR semantics if multiple alternative Builds produced the same target. Current SAS BuildInfos has no improvement with more than one Build; ROUTE_ROAD has only BUILD_ROAD and ROUTE_RAILROAD only BUILD_RAILROAD. Thus no shipped current mismatch. Advanced Start tech-removal territory also has prior semantic ownership (including F088 controls), so duplicate gate remains mandatory.

20. PARKED LOW-SEVERITY UI ISSUE REMAINS AVAILABLE, STILL UNNUMBERED
postProcessMessages() subtracts one from m_iNewMessages whenever getCurrentResearch()==NO_TECH, using that as a proxy for 'a technology just completed'. After all technologies are known, NO_TECH can persist indefinitely. Therefore exactly MessageLimit+1 genuine interturn messages can be treated as only MessageLimit, preventing the Turn Log from auto-opening. Deterministic and reachable, but UI-only/low severity. Leave unnumbered while stronger gameplay/state territory remains.

CURRENT DECISION
F484 / KI#807 remains UNUSED.
This tranche is valuable mainly because it closes several plausible stateful paths and establishes the AdvCiv GET_TEAM(PlayerTypes) overload as a durable false-positive control.

NEXT EXACT CONTINUATION
F484 unused
-> finish semantic filtering of the remaining larger authoritative CvPlayer.cpp gameplay bodies
-> prioritize current gameplay execution over UI/setup
-> continue espionage mission execution/cost invariants and other stored-state transitions where current XML has live producers
-> use latest SAS callers/XML, full callee chain and current option routes
-> compare K-Mod / Civ4CE / AdvCiv ancestry without treating syntax changes as bugs when AdvCiv accessor contracts changed
-> mandatory semantic duplicate proof against existing F446-F483 and older album findings
-> only if substantive gameplay/state territory closes, reconsider the parked postProcessMessages Turn-Log issue
-> freeze C031-WIP211 at the next genuinely settled tranche or immediately after a new confirmed F484, whichever comes first.

SOURCE MATERIAL USED LOCALLY
- AdvCiv-SAS_light_source_20260901T233824.zip
- AdvCiv-1.14(1).zip
- Civ4-K-Mod-master-1.46(20260901-213852).zip
- Civ4CE-main.zip
- chatgpt_durable_wip_15_full.txt / C031-WIP209 handoff

No repository source files were modified.


AdvCiv-SAS C++ file audit album
C031-WIP211 continuation checkpoint
Date: 2026-09-01

STATE
Queue 004: CvGameCoreDLL/CvPlayer.cpp OPEN
32/169 unique files complete
F445 / KI#768 RETRACTED
F446 / KI#769 through F484 / KI#807 CONFIRMED
F485 / KI#808 UNUSED
Previous physical checkpoint: C031-WIP210
Current physical checkpoint: C031-WIP211
Repository source files modified: NO

PURPOSE OF THIS CHECKPOINT
This checkpoint supersedes C031-WIP210 as the latest physical handoff. The post-WIP210 espionage-cost pass produced a new current gameplay finding strong enough to consume F484 / KI#807. The finding is inherited from AdvCiv's integration of two individually sensible changes: extraction of the generic steal-tech cost helper and pricing stolen technologies by OUR remaining research rather than their full research cost.

C031-WIP210 CONTROLS REMAIN IN FORCE
All closures preserved in WIP210 remain valid, especially:
- persistent research/espionage team-size scaling remains policy rather than an F483 sibling;
- GET_TEAM(PlayerTypes) is an intentional AdvCiv overload and must not be treated as a PlayerTypes/TeamTypes bug by literal ancestry comparison;
- current destroy-unit / bribe-unit espionage branches are dormant because shipped XML has zero corresponding cost factors;
- the postProcessMessages Turn-Log issue remains parked as low-severity UI-only material while stronger gameplay roots remain.

================================================================================
C031-WIP211 — F484 / KI#807 CONFIRMED
================================================================================

F484 / KI#807 — AdvCiv's generic Steal Technology affordability check chooses the
candidate by FULL research cost after actual steal pricing was changed to REMAINING
research cost, so the human espionage popup can hide the entire mission even when a
specific stealable technology is currently affordable

STATUS
CONFIRMED current gameplay root.
Inherited by current AdvCiv-SAS from AdvCiv 1.14.
Not present with this contradiction in K-Mod / Civ4CE-BtS, because those ancestors
priced both generic and specific technology theft by full research cost.

ROOT
Current CvPlayer::getStealCostTech(eTargetPlayer) scans all stealable technologies and
selects the one with the smallest FULL research cost:

    int iProdCost = MAX_INT;
    FOR_EACH_ENUM(Tech)
    {
        if (canStealTech(eTargetPlayer, eLoopTech))
        {
            int iCost = GET_TEAM(getTeam()).getResearchCost(eLoopTech);
            if (iCost < iProdCost)
            {
                iProdCost = iCost;
                eR = eLoopTech;
            }
        }
    }

That helper was extracted by advc.120d from the old generic branch of
getEspionageMissionBaseCost.

But the actual Buy/Steal Tech mission-cost branch no longer prices the selected tech by
full research cost. AdvCiv's advc.120i deliberately changed it to OUR REMAINING research:

    if (eTech == NO_TECH)
        eTech = getStealCostTech(eTargetPlayer);
    int iProdCost = (eTech == NO_TECH ? -1 : GET_TEAM(getTeam()).
            getResearchLeft(eTech)); // advc.120i: Count own progress

    if (eTech != NO_TECH && canStealTech(eTargetPlayer, eTech))
    {
        iMissionCost = iBaseMissionCost +
            ((100 + kMission.getBuyTechCostFactor()) * iProdCost) / 100;
    }

Therefore the generic NO_TECH path now combines TWO DIFFERENT ordering metrics:

    candidate choice:  minimum getResearchCost(tech)
    actual price:      getResearchLeft(chosen tech)

A more expensive technology that the player has already mostly researched can have a
much LOWER actual espionage cost than the lower-full-cost technology selected by the
helper.

CURRENT HUMAN CALLER — THIS IS AN ACTION-AVAILABILITY BUG, NOT ONLY A DISPLAY BUG
CvDLLButtonPopup::launchDoEspionagePopup constructs the first espionage mission popup.
For every active mission, including two-phase missions, it first calls:

    canDoEspionageMission(eLoopMission, target, plot, -1, pUnit)

and skips the mission entirely if that returns false.

For ESPIONAGEMISSION_BUY_TECH, -1 is NO_TECH. canDoEspionageMission therefore reaches
the generic cost path above. Its default bCheckPoints is true, so after calculating the
generic mission cost it requires current espionage points to be at least that cost.

Only AFTER the player is allowed to choose the first-phase "Steal Technology" button
does CvDLLButtonPopup::launchDoEspionageTargetPopup iterate every technology and call:

    canDoEspionageMission(..., eLoopTech, pSpyUnit)

for each specific target. Those specific calls correctly price each technology using its
own getResearchLeft(eLoopTech).

Thus the first-phase generic gate can suppress access to a valid second-phase choice.

DETERMINISTIC CURRENT-DATA EXAMPLE
Current shipped XML keeps ESPIONAGEMISSION_BUY_TECH active with:

    iCost = 0
    bIsTwoPhases = 1
    bTargetsCity = 1
    TechPrereq = NONE
    iBuyTechCostFactor = 50

Current shipped technology data includes, for example:

    TECH_IRON_WORKING   iCost = 260, ERA_CLASSICAL, prereq Bronze Working
    TECH_PAPER          iCost = 425, ERA_CLASSICAL, prereqs Literature + Construction

A normal player can satisfy both prerequisite sets while knowing neither technology,
and an espionage target can know both. canStealTech merely requires the target team to
have the tech and our player still to be able to research it, so partial own research is
fully compatible with the technology remaining stealable.

Take a state where:
- both Iron Working and Paper are currently stealable from the target;
- our team has no meaningful progress on Iron Working;
- our team has partially researched Paper until Paper has only a small amount left;
- current espionage points lie between the resulting specific Paper steal cost and the
  resulting Iron Working steal cost.

Because current SAS has the relevant groundbreaking penalties disabled and both example
techs are in the same era, their normal full-cost ordering follows the XML ordering:
Iron Working is the lower-full-cost candidate. getStealCostTech therefore returns Iron
Working.

Yet the actual Buy Tech base formula is 150% of RESEARCH LEFT (base cost 0 and factor
50). If, illustratively, Paper has only 25 research points left while Iron Working still
has its much larger remainder, Paper's specific pre-location cost is tiny compared with
Iron Working's. The team-size adjustment and espionage mission modifier are the same
for both choices at the same spy/plot/target; getEspionageMissionCostModifier does not
use iExtraData. The ordering therefore survives the common modifiers.

Choose current EP in that interval:

    specific Paper cost <= EP < generic-selected Iron Working cost

Then:
1. Paper is a legal, affordable steal target.
2. The first espionage popup evaluates Buy Tech with iExtraData=-1.
3. getStealCostTech selects Iron Working from full cost.
4. Generic cost is computed from Iron Working's larger research LEFT.
5. canDoEspionageMission returns false because EP is below that representative cost.
6. launchDoEspionagePopup skips the entire Steal Technology button.
7. The second popup is never reached, so its correct specific Paper affordability check
   cannot rescue the action.

The human is therefore prevented from performing a legal, currently affordable
espionage mission solely because generic candidate selection still uses the old metric.

SECOND CURRENT CONSUMER — FOREIGN ADVISOR MISREPRESENTATION
Current CvForeignAdvisor.py also uses the same generic pair:

    getEspionageMissionCost(..., -1)
    getStealCostTech(target)

for the active mission row. It can therefore display the wrong representative technology
and a non-minimal cost under the same partial-research state. This is secondary to the
blocked gameplay action but confirms the helper is intended to represent generic steal
cost information.

AI CONTROL — DO NOT OVERSTATE THE SCOPE
Current CvUnitAI::AI_bestPlotEspionage does NOT rely on getStealCostTech for Buy Tech.
When getBuyTechCostFactor() > 0 it iterates every technology ID and prices each specific
candidate with the acting Spy. Therefore F484's direct availability failure is a human
first-popup / generic-cost contract bug, plus advisor representation; do not describe it
as the same AI target-selection defect as F349.

SIBLING CONTRACT CONTROL
Other two-phase generic target families reinforce the intended contract rather than
weakening the finding:
- switch-civic generic cost explicitly loops all forceable civics and keeps iMinCost;
- switch-religion generic cost explicitly loops all forceable religions and keeps
  iMinCost;
- destroy-project / destroy-building generic branches choose the cheapest currently
  valid target using the same production metric that their specific price formula uses.

The generic first-phase price is therefore supposed to stand in for an actually usable
cheap target. Buy Tech became inconsistent only because advc.120i changed the specific
price metric after the generic candidate-selection logic had been factored out.

ANCESTRY
Civ4CE/BtS and K-Mod keep the entire Buy Tech generic selection inside
getEspionageMissionBaseCost. In both lineages:
- the NO_TECH scan chooses the stealable technology with minimum getResearchCost;
- the chosen/specific technology is also priced with getResearchCost.

The selection metric and pricing metric therefore agree in those ancestors.

AdvCiv introduces two relevant changes retained by current SAS:
1. advc.120d extracts the old generic scan into getStealCostTech, still minimizing
   getResearchCost;
2. advc.120i changes actual mission pricing to getResearchLeft with the explicit comment
   "Count own progress".

The integration failed to update the extracted generic helper to the new metric. This is
best classified as an AdvCiv-origin regression inherited by SAS, not a vanilla/K-Mod bug
and not an SAS-local regression.

SEMANTIC DUPLICATE PROOF
The reconciled C031-WIP209 album contains no earlier finding owning getStealCostTech or
this full-cost-vs-research-left generic gate. In fact, the prior Queue-004 cursor
explicitly named getStealCostTech/getEspionageMissionBaseCost as the next unclosed path.

Keep F484 separate from:
- F315 / KI#638: selection-bound modal popups can rebind to a replacement unit/group
  after K-Mod Rapid Unit Cycling. F315 is launch-time ACTOR/TARGET identity loss. F484
  occurs with a perfectly stable Spy/selection and is a generic COST/AVAILABILITY metric
  mismatch before the first target popup is even opened.
- F348 / KI#671: AI_espionageVal's unitless current-EP gate defeats AI save-for-later
  valuation and stationary discounts. F348 is an AI valuation/context contract. F484 is
  the human first-popup generic technology-choice metric after partial research.
- F349 / KI#672: AI_doCommerce reuses one tech-theft accumulator across rival targets.
  F349 is AI strategic target-state contamination. F484 neither shares that state nor
  depends on AI_doCommerce.

REPAIR DIRECTION
The smallest coherent repair is to make getStealCostTech minimize the same quantity that
Buy Tech actually prices now:

    int iCost = GET_TEAM(getTeam()).getResearchLeft(eLoopTech);

rather than getResearchCost(eLoopTech).

That makes the helper return the cheapest current steal target under advc.120i and keeps
the Foreign Advisor's representative technology consistent with its displayed generic
cost.

A more structural alternative is for the NO_TECH Buy Tech branch to iterate all
stealable technologies and directly take the minimum base mission cost, matching the
switch-civic / switch-religion generic pattern. But because getStealCostTech is also a
current Python/UI helper, updating its metric is the simpler coherent fix unless some
caller intentionally needs "lowest full tech cost" semantics. Current callers found are
only the generic C++ mission-cost branch and the Foreign Advisor row, both of which want
cost representation rather than an abstract full-cost ranking.

NUMBERING
F484 / KI#807 CONFIRMED.
F485 / provisional KI#808 is now UNUSED.

POST-F484 IMMEDIATE CONTROL
The first sibling check is already clean: the acting AI Spy path iterates specific techs,
and the other current two-phase target families compute a generic minimum using the same
metric as their specific prices. This makes the mismatch sharply localized rather than a
general "two-phase espionage uses arbitrary target" issue.

CURRENT STONE
Queue 004 CvGameCoreDLL/CvPlayer.cpp OPEN
32/169 unique files complete
F445/KI#768 RETRACTED
F446/KI#769 through F484/KI#807 CONFIRMED
F485/KI#808 UNUSED
C031-WIP211 latest physical checkpoint
No repository source files modified.

NEXT EXACT CONTINUATION
F485 unused
-> finish immediate getEspionageMissionBaseCost / doEspionageMission siblings
-> compare generic NO_* representative semantics against specific target execution for
   all current live two-phase mission families
-> then resume remaining under-covered authoritative CvPlayer.cpp state transitions
-> latest SAS callers/XML and option routes first
-> ancestry + concrete consequence + semantic duplicate proof mandatory
-> keep parked postProcessMessages Turn-Log issue below substantive gameplay/state roots
-> freeze WIP212 at the next settled tranche or immediately after a new confirmed F485.

SOURCE MATERIAL USED LOCALLY
- AdvCiv-SAS_light_source_20260901T233824.zip
- AdvCiv-1.14(1).zip
- Civ4-K-Mod-master-1.46(20260901-213852).zip
- Civ4CE-main.zip
- full reconciled library album AdvCiv-SAS_cpp_file_audit_album_C031-WIP209.txt
- C031-WIP210 local continuation checkpoint

No repository source files were modified.


================================================================================
C031-WIP212 — QUEUE 004 CVPLAYER.CPP — F485 / KI#808 CONFIRMED
================================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F485 / KI#808 CONFIRMED.
F486 / provisional KI#809 UNUSED.
Latest physical checkpoint: C031-WIP212.
No repository source files modified.

F485 / KI#808 — ADVANCED START UNIT/ROUTE REMOVAL FALLBACKS CAN DESTROY ANOTHER
PLAYER'S UNIT OR ROUTE AFTER THE INITIAL PLOT-AWARE VALIDATION HAS ALREADY FAILED
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current supported gameplay root.

The destructive fallback contract is inherited from stock BtS through K-Mod and Base
AdvCiv. AdvCiv partially hardened the unit fallback under `kekm.11` by preserving the
plot in its second cost query, but it still kills the unit after that query reports
`-1`. The route fallback remains the stock no-plot form and therefore bypasses the
ownership test completely. Current SAS retains both branches.

ROOT — THE UI EXPOSES REMOVAL ON REVEALED FOREIGN PLOTS
-------------------------------------------------------
Current SAS's repository manifest contains its tracked `Assets/Python/Screens` files but
contains no `CvWorldBuilderScreen.py`. It therefore does not override the BtS Advanced
Start screen; the supplied Civ4CE/BtS reconstruction provides the inherited screen.

`CvWorldBuilderScreen.removeObject()` first requires only that the clicked plot be
REVEALED to the active team. For the Unit tab it then sends, without an owner check:

    sendAdvancedStartAction(ADVANCEDSTARTACTION_UNIT,
        currentPlayer, plotX, plotY, selectedUnitType, false)

For the Route list it likewise sends, without an owner check:

    sendAdvancedStartAction(ADVANCEDSTARTACTION_ROUTE,
        currentPlayer, plotX, plotY, selectedRouteType, false)

This lack of Python-side ownership filtering is not itself enough to be a bug: the DLL
cost helpers deliberately own authoritative validation. In fact the same Python screen
performs explicit owner checks before removing population/buildings, and entirely
disables City and Visibility removal with comments warning about exploits. Unit and
Route therefore reach the DLL and depend on its plot-aware validators.

CURRENT SUPPORTED MULTIPLAYER/HOTSEAT PRODUCER
----------------------------------------------
The state does not require WorldBuilder or a forged network message.

Advanced Start is per-player state. `CvGame::update` automatically re-enters Advanced
Start whenever the newly active player still has nonnegative Advanced Start points:

    if (getActivePlayer() != NO_PLAYER &&
        GET_PLAYER(getActivePlayer()).getAdvancedStartPoints() >= 0 &&
        !UI.isInAdvancedStart())
    {
        UI.setInAdvancedStart(true);
        UI.setWorldBuilder(true);
    }

Thus a supported Hotseat / sequential-multiplayer sequence can have human A complete
part of its Advanced Start setup, then human B become active and perform B's own
Advanced Start while A's already-created city, units and routes remain ordinary map
state.

B can purchase Visibility outward until A's relevant plot is revealed. The inherited
`removeObject` condition is merely `isRevealed(activeTeam,false)`, so the foreign plot
then becomes a legal UI input to the Unit/Route removal messages above.

MANIFESTATION A — FOREIGN UNIT DELETION
---------------------------------------
`CvPlayer::getAdvancedStartUnitCost(eUnit,false,pPlot)` correctly enforces ownership.
With the default city-placement form it requires an own city:

    pCity = pPlot->getPlotCity();
    if (pCity == NULL || pCity->getOwner() != getID())
        return -1;

If units-outside-cities is enabled instead, it still requires:

    if (pPlot->getOwner() != getID())
        return -1;

Therefore any attempted Advanced Start removal on another player's city/plot produces
`iCost == -1`, exactly as intended.

But `doAdvancedStartAction(ADVANCEDSTARTACTION_UNIT)` treats negative cost specially
only for ADD actions:

    int iCost = getAdvancedStartUnitCost(eUnit, bAdd, pPlot);
    if (bAdd && iCost < 0)
        return;

On REMOVE, a negative initial cost skips the matching-unit loop and enters the fallback:

    CLLNode<IDInfo>* pNode = pPlot->headUnitNode();
    if (pNode != NULL)
    {
        CvUnit* pUnit = ::getUnit(pNode->m_data);
        iCost = getAdvancedStartUnitCost(pUnit->getUnitType(), false, pPlot); // kekm.11
        FAssertMsg(iCost != -1, "Trying to delete a unit which shouldn't exist");
        pUnit->kill(false);
        changeAdvancedStartPoints(iCost);
    }

On a foreign plot the second, actual-unit cost is STILL `-1`, because `kekm.11`
correctly preserved `pPlot`. The code merely asserts. In an ordinary release DLL the
assertion does not protect execution: `pUnit->kill(false)` still deletes the foreign
unit and `changeAdvancedStartPoints(-1)` merely charges the attacker one Advanced Start
point.

This is an unusually clear validation/execution contradiction: the authoritative cost
API says the unit MUST NOT be removable, the source assertion explicitly recognizes
that state as invalid, and the release branch performs the destructive action anyway.

DETERMINISTIC UNIT REPRODUCTION
-------------------------------
1. Start a supported two-human Hotseat game with Advanced Start enabled and enough
   points (a later start era makes the setup especially easy).
2. Human A founds a city, buys exactly one unit in it, finishes Advanced Start and then
   proceeds until human B becomes active.
3. B founds B's own required first city and uses Advanced Start Visibility purchases to
   reveal A's city.
4. B opens the Unit tab, selects any unit entry available in B's palette and right-clicks
   A's revealed city.
5. Stock `CvWorldBuilderScreen.removeObject` sends UNIT / bAdd=false without checking
   ownership.
6. B's initial plot-aware unit cost is `-1` because A owns the city.
7. The fallback obtains A's sole unit, recomputes its plot-aware removal cost, again gets
   `-1`, hits only the debug assertion, and calls `kill(false)` anyway.
8. A's unit is permanently removed. In release, B also loses 1 Advanced Start point.

The selected unit type need not equal A's unit type. The fallback exists specifically
for the case where the requested type was not accepted/found and deletes the first unit
on the plot.

MANIFESTATION B — FOREIGN ROUTE DELETION + BOGUS REFUND
-------------------------------------------------------
`CvPlayer::getAdvancedStartRouteCost(eRoute,false,pPlot)` likewise correctly requires:

    if (pPlot->getRouteType() != eRoute)
        return -1;
    ...
    if (pPlot->getOwner() != getID())
        return -1;

However the Route removal fallback loses the plot entirely:

    int iCost = getAdvancedStartRouteCost(eRoute, bAdd, pPlot);
    ...
    if (pPlot->getRouteType() != eRoute)
    {
        eRoute = pPlot->getRouteType();
        iCost = getAdvancedStartRouteCost(eRoute, bAdd); // pPlot omitted
    }
    if (iCost < 0)
        return;
    pPlot->setRouteType(NO_ROUTE);
    changeAdvancedStartPoints(iCost);

When `pPlot == NULL`, the helper cannot perform the route-presence or plot-owner checks.
The fallback therefore converts an initially invalid foreign/mismatched request into a
positive generic refund and then mutates the original foreign plot.

Current SAS has exactly the convenient pair needed for a deterministic reproduction:
`ROUTE_ROAD` and `ROUTE_RAILROAD`. At Normal speed, Road's current base Advanced Start
cost 12 with `ADVANCED_START_WORKER_BUILD_MODIFIER = 67` becomes 8 points before any
zero cost-increase (12 * 67 / 100 truncates to 8; Normal BuildPercent and speed are
100%).

DETERMINISTIC ROUTE REPRODUCTION
--------------------------------
1. Use a Modern Advanced Start Hotseat game so both humans know the ordinary Road and
   Railroad enabling technologies and both route entries are straightforwardly
   available.
2. Human A founds a city and buys a Road on an owned non-city plot, then later human B
   becomes active for B's Advanced Start.
3. B founds B's city and purchases visibility until A's Road plot is revealed.
4. B selects RAILROAD in the Advanced Start Route list, then right-clicks A's ROAD.
5. Python sends `ADVANCEDSTARTACTION_ROUTE(..., ROUTE_RAILROAD, false)` with no owner
   precheck.
6. The initial plot-aware query returns `-1` because the actual route is Road rather than
   Railroad (and the plot is foreign in any case).
7. The fallback replaces `eRoute` with the actual Road but calls
   `getAdvancedStartRouteCost(ROAD,false)` WITHOUT `pPlot`.
8. The generic Road cost is positive because B knows the Road technology.
9. `setRouteType(NO_ROUTE)` deletes A's Road even though B does not own the plot.
10. B receives the positive Road refund (8 points under the Normal-speed current values
    above) despite never having purchased that route.

If B selects the same route type as the foreign plot, the initial plot-aware cost stays
negative and the later `if(iCost < 0) return` protects the route. The exploit therefore
specifically uses the stock fallback's advertised purpose: replace a mismatching
selected route type with the route actually present. Current two-route data makes that
state ordinary rather than hypothetical.

PAIRED CONTROL — IMPROVEMENT REMOVAL DOES THIS CORRECTLY
--------------------------------------------------------
The adjacent Improvement branch demonstrates the intended safe fallback contract:

    if (pPlot->getImprovementType() != eImprovement)
    {
        eImprovement = pPlot->getImprovementType();
        iCost = getAdvancedStartImprovementCost(eImprovement, bAdd, pPlot);
    }
    if (iCost < 0)
        return;
    pPlot->setImprovementType(NO_IMPROVEMENT);

It preserves `pPlot`, then ACTUALLY ABORTS on negative validation before mutation.
Foreign Improvement removal therefore remains blocked. This paired branch is strong
local evidence that Unit/Route are implementation defects rather than an intentional
policy permitting removal of arbitrary revealed foreign assets.

ANCESTRY
--------
Stock BtS/Civ4CE already contains both unsafe fallback structures.

UNIT:
- stock/K-Mod first use a plot-aware cost;
- if that fails or the selected type is absent, they pick the first unit;
- they then recompute `getAdvancedStartUnitCost(actualType,false)` WITHOUT `pPlot`;
- the generic removal query has no owner/presence context, so it can return a positive
  refund for a foreign unit;
- they assert, kill and refund.

Base AdvCiv's `kekm.11` changes that second unit query to:

    getAdvancedStartUnitCost(actualType, false, pPlot)

This is a genuine partial hardening: the foreign target now produces `-1` instead of a
positive generic refund. But the caller still treats the assertion as if it were a
runtime guard. It never checks the negative result before `kill(false)`. Current SAS
therefore inherits unauthorized foreign-unit deletion with a 1-point attacker charge
instead of stock's unauthorized deletion plus refund.

ROUTE:
The no-plot fallback is unchanged across Civ4CE/BtS, K-Mod, Base AdvCiv and current
SAS. All retain the plot-aware initial cost, then generic no-plot cost after replacing a
mismatching requested route with the actual route.

The best attribution is consequently an inherited Firaxis/BtS Advanced Start destructive
fallback bug, with AdvCiv partially but incompletely repairing the unit half.

CURRENT SAS UI / REPOSITORY PROOF
---------------------------------
The current light-source snapshot's `_SNAPSHOT_CONTEXT/repo_file_manifest.txt` lists the
tracked Python Screens and does not contain `Assets/Python/Screens/CvWorldBuilderScreen.py`.
Current SAS therefore has no mod-level replacement for this screen in the repository.
Base AdvCiv 1.14 likewise ships no `CvWorldBuilderScreen.py`, leaving the stock BtS
screen in effect through the normal asset fallback chain. The supplied Civ4CE/BtS file
is therefore the appropriate local control for the live UI producer.

DUPLICATE / ROOT SEPARATION
---------------------------
The full reconciled C031-WIP209 album was searched for the exact fallback comments,
assertion text, Advanced Start unit removal, foreign-unit removal, foreign-route removal
and route-refund wording. No existing F/KI owns this destructive removal contract.

Keep F485 distinct from:
- F440/KI#763 and F441/KI#764: Advanced Start military-cap help/generic-availability
  mismatches. Those affect ADD availability, not destructive removal or ownership.
- F472/KI#795: Advanced Start route ADD ignores Railroad's Coal/Oil prerequisite. F472
  creates a route on the acting player's plot; F485 removes another player's route
  after failed plot-aware validation.
- rejected F084/F085/F088: dormant technology-refund backend paths with no shipped
  removal UI. F485 has an explicit stock right-click UI producer for Unit and Route.
- the current post-WIP211 city/visibility refund leads: stock UI explicitly disables City
  and Visibility removal, so those remain backend-only and are not folded into F485.

CONSERVATIVE REPAIR
-------------------
Preserve the UI convenience of resolving the actual asset when the selected palette type
is not what the clicked plot contains, but make the substituted target pass the SAME
plot-aware validation before destructive mutation.

Unit direction:

    CvUnit* pUnit = resolve intended matching unit / fallback first unit;
    if (pUnit == NULL)
        return;
    iCost = getAdvancedStartUnitCost(pUnit->getUnitType(), false, pPlot);
    if (iCost < 0)
        return;
    pUnit->kill(false);
    changeAdvancedStartPoints(iCost);

The runtime guard must exist even in assertion-enabled builds; `FAssertMsg` is diagnostic,
not authorization.

Route direction:

    if (pPlot->getRouteType() != eRoute)
        eRoute = pPlot->getRouteType();
    iCost = getAdvancedStartRouteCost(eRoute, false, pPlot);
    if (iCost < 0)
        return;
    pPlot->setRouteType(NO_ROUTE);
    changeAdvancedStartPoints(iCost);

Do not omit `pPlot` from the fallback recomputation.

Defense in depth could also add Python-side owner checks for Unit/Route removal, matching
Population/Building, but DLL validation should remain authoritative because network/API
callers can bypass Python.

POST-WIP211 CLOSURES
--------------------
1. First-city partial-charge/full-refund lead: backend asymmetry exists, but stock
   `CvWorldBuilderScreen.removeObject` explicitly returns before City removal and labels
   it exploit-prone. No ordinary current removal producer; do not number.
2. Free-starting-visibility refund lead: backend would have no provenance distinction,
   but the same stock screen explicitly returns before Visibility removal and labels it
   exploit-prone. No ordinary current removal producer; do not number.
3. The Unit/Route pair survives exactly because stock UI DOES expose those removal
   messages and delegates legality to the DLL.

NUMBERING
---------
F485 / KI#808 CONFIRMED.
F486 / provisional KI#809 is now UNUSED.

CURRENT STONE AFTER C031-WIP212
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F485 / KI#808 CONFIRMED.
F486 / provisional KI#809 UNUSED.
Latest physical checkpoint: C031-WIP212.
No repository source files modified.

NEXT
----
Continue from F486 unused. Finish the remaining Advanced Start REMOVE transaction
siblings now that the caller contract is known (population/building/culture exact-refund
and ownership behavior), but do not re-open City/Visibility without a new supported
producer. Then return to under-covered authoritative normal-turn `CvPlayer.cpp` state,
prioritizing persistent state transitions / arithmetic / ownership over help-only or
setup-only material. Preserve current-data, shipped-caller, full callee-chain, ancestry
and duplicate proof before promotion.

END C031-WIP212


===============================================================================
C031-WIP213 — QUEUE 004 CVPLAYER.CPP — F486 / KI#809 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F486 / KI#809 CONFIRMED.
F487 / provisional KI#810 UNUSED.
No repository source files modified.

POST-WIP212 ADVANCED START REMOVE-SIBLING CLOSURES
---------------------------------------------------
The remaining removal/refund siblings were finished before leaving Advanced Start.
They do NOT broaden F485 and do not consume F486.

1. Population add/remove cost is transactionally symmetric at the same population step.
   The add cost from population N uses the growth threshold for N and the current
   cost-increase term based on the pre-purchase population. Removal from N+1 decrements
   the test population to N before computing the refund and reaches the same terms.
   No buy-low/refund-high arithmetic mismatch was established.

2. Building removal retains an authoritative owner boundary and dependency checks.
   The concrete city must belong to the acting player; free start-era buildings are not
   refundable; dependent currently-owned buildings can block removal. The action does
   not share F485's validate-one-target / mutate-fallback-target structure.

3. Culture removal remains intentionally unavailable through the stock Advanced Start
   screen. There is no ordinary shipped UI producer to turn the backend's generic remove
   support into a current gameplay root.

4. Advanced Start initialization itself does not grant ordinary free starting units and
   then expose them to a refund. `initFreeUnits` supplies Advanced Start points and the
   setup visibility package; normal free starting-unit creation belongs to the
   non-Advanced-Start branch. No free-unit-refund sibling survives.

5. `CvPlayer::disbandUnit` was then re-read at normal-turn depth. Its destructive
   eligibility boundaries remain coherent: cargo / Golden-Age / zero-production-cost
   exclusions and the sole military-happiness protection are explicit. The remaining
   candidate-ordering questions are policy/valuation, not a violated authoritative
   state invariant. Preserve the earlier Queue-004 closure; no F486 there.


F486 / KI#809 — RANDOM-EVENT REPLY VALIDATION TREATS A VANISHED UNIT TARGET AS
LEGAL, SO A UNIT-ONLY EVENT CAN BE RECORDED / GLOBALLY CONSUMED WITHOUT APPLYING
ANY OF ITS EFFECTS
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Current shipped random-event data plus supported simultaneous multiplayer
produce the defect without malformed packets, WorldBuilder-only data or a mod-mod.

The source root is inherited unchanged from stock BtS through K-Mod, Base AdvCiv 1.14
and current AdvCiv-SAS.

ROOT — UNIT TARGET EXISTENCE IS OPTIONAL AT REPLY-TIME VALIDATION
---------------------------------------------------------------
A human random event stores its concrete target unit as the player-local ID in
`EventTriggeredData::m_iUnitId`.

`CvPlayer::canDoEvent` performs strict existence checks for concrete city effects:

    if (kEvent.isCityEffect())
    {
        CvCity* pCity = getCity(kTriggeredData.m_iCityId);
        if (pCity == NULL || !pCity->canApplyEvent(eEvent, kTriggeredData))
            return false;
    }

and likewise rejects a missing other-player city.

The concrete unit path is materially weaker:

    CvUnit* pUnit = getUnit(kTriggeredData.m_iUnitId);
    if (pUnit != NULL && !pUnit->canApplyEvent(eEvent))
        return false;

If the stored target unit no longer exists, `pUnit == NULL` simply skips the test and
`canDoEvent` continues as though the unit portion were valid.

This is not merely a loose helper contract. `applyEvent` re-runs `canDoEvent` on the
synchronized reply path and then later applies the unit-local effect only when that same
ID still resolves:

    CvUnit* pUnit = getUnit(pTriggeredData->m_iUnitId);
    if (pUnit != NULL)
    {
        FAssert(pUnit->canApplyEvent(eEvent));
        pUnit->applyEvent(eEvent);
    }

So the two layers together encode the problematic state explicitly:

    missing stored target unit
        -> canDoEvent says YES
        -> event bookkeeping / non-unit effects proceed
        -> unit-local effect is silently skipped

CURRENT CLEAN REPRODUCER — `AT THE SWORD`
-----------------------------------------
Current shipped `CIV4EventTriggerInfos.xml` gives a particularly unambiguous event:

    EVENTTRIGGER_AT_THE_SWORD
        bSinglePlayer = 0
        iPercentGamesActive = 100
        iWeight = 500
        bRecurring = 0
        bGlobal = 1
        iNumUnits = 1
        UnitsRequired = UNITCLASS_SWORDSMAN
        iUnitDamagedWeight = 1
        iUnitDistanceWeight = 0
        iUnitExperienceWeight = 0
        bShowPlot = 1

`CvUnit::getTriggerValue` makes the damaged-unit requirement authoritative:

    if (getDamage() == 0 && kTrigger.getUnitDamagedWeight() > 0)
        return MIN_INT;

and restricts the class through `UnitsRequired`. Thus the stored target is a concrete,
living, DAMAGED Swordsman when the trigger is constructed.

The sole event outcome is:

    EVENT_AT_THE_SWORD_1
        iGold = 0
        iUnitExperience = 3
        UnitName = TXT_KEY_EVENT_UNIT_AT_THE_SWORD
        bDisbandUnit = 0

There are no independent gold/city/empire/tech effects that could justify applying a
partial result after the target disappears. Its runtime substance is unit-local.

`CvUnit::applyEvent` handles `iUnitExperience` by:

    setDamage(0);
    changeExperience(kEvent.getUnitExperience());

and applies the event unit name as well. Consequently the outcome's concrete effects
are: fully heal the stored damaged Swordsman, grant +3 XP and give it the event name.
(The hidden-heal help omission is already separately owned by F414 / KI#737; do not
merge that UI root with F486.)

SUPPORTED POPUP / DISAPPEARANCE WINDOW
--------------------------------------
Current player-turn ordering gives random events a genuine delayed-decision lifetime.
`CvPlayer::doTurn` explicitly documents that it runs at the end of a human turn and
calls `doEvents()` near the end of that sequence. For a human, `trigger(kData)` queues:

    CvPopupInfo(BUTTONPOPUP_EVENT, kData.getID())

rather than applying an outcome immediately.

The earlier Queue-019 F307 archaeology already established the ordinary lifecycle:
random events are generated at the end of a human player's turn and their choice popup
can be handled later. More importantly for F486, after the popup has actually launched,
it remains an ordinary modal human choice until the player answers it.

Current `EVENTTRIGGER_AT_THE_SWORD` explicitly has `bSinglePlayer=0`, so it is enabled
in multiplayer. Under supported simultaneous turns, another human can continue issuing
synchronized game actions while the local event popup is unanswered. This is the same
class of supported modal/remote-action timing already proved independently for F315;
no fabricated UI mutation is required.

A damaged Swordsman chosen by the trigger can therefore remain in an exposed combat
position. A remote enemy can kill that exact Swordsman after the event popup is shown
but before its owner clicks the sole outcome.

DETERMINISTIC CONTROL-FLOW REPRODUCTION
---------------------------------------
Use a simultaneous-multiplayer game with random events enabled and a player owning a
currently eligible damaged Swordsman.

1. `EVENTTRIGGER_AT_THE_SWORD` fires for player A and stores Swordsman S's player-local
   unit ID in `m_iUnitId`.
2. The normal human `BUTTONPOPUP_EVENT` is launched and displays its sole At the Sword
   outcome while S is still alive.
3. Leave the modal unanswered.
4. Remote player B attacks and kills S through ordinary synchronized combat while the
   simultaneous turn remains live.
5. A now clicks the already-displayed At the Sword outcome.
6. `CvMessageControl::sendEventTriggered` reaches the synchronized event receiver and
   `CvPlayer::applyEvent` for A's original trigger ID.
7. `applyEvent` calls `canDoEvent(EVENT_AT_THE_SWORD_1, oldTriggeredData)`.
8. `getUnit(old.m_iUnitId)` returns NULL. Because the condition is only
      `pUnit != NULL && !pUnit->canApplyEvent(...)`,
   missing S does NOT invalidate the event.
9. `applyEvent` calls `setTriggerFired` and `setEventOccured`, then runs the outcome.
10. When it reaches the unit block, `getUnit(old.m_iUnitId)` is still NULL, so healing,
    +3 XP and renaming are all skipped.

The player has therefore successfully "applied" a random-event outcome whose entire
runtime effect target no longer exists.

PERSISTENT / GLOBAL CONSEQUENCE
-------------------------------
This is stronger than a one-frame no-op.

`EVENTTRIGGER_AT_THE_SWORD` is both:

    bRecurring = 0
    bGlobal = 1

`applyEvent(..., bUpdateTrigger=true)` executes `setTriggerFired(*pTriggeredData, true)`
before applying the event's payload. `CvPlayer::setTriggerFired`, when the trigger is
global, propagates the fired state to every player slot.

Therefore the stale reply can globally consume the non-recurring At the Sword trigger
even though its target died and none of the event's advertised/runtime effects occurred.
The trigger will not later fire for a valid damaged Swordsman merely because the stale
popup produced no unit effect.

A second current-data manifestation exists through other unit-local event outcomes. For
example Toxcatl's second option combines `iGold=-65` with `iUnitImmobileTurns=3`. If its
stored unit disappears after popup launch, current validation can still charge the gold
while silently omitting the immobilization. At the Sword is the preferred proof because
its sole outcome is entirely unit-local and therefore avoids any ambiguity about a
partially meaningful non-unit remainder.

WHY F307 DOES NOT OWN THIS ROOT
-------------------------------
F307 / KI#630 concerns Base AdvCiv's `launchEventPopup` "revalidation" calling
`initTriggeredData` as a side-effectful constructor:
- it allocates/leaks a second EventTriggeredData;
- consumes synchronized RNG / runs construction hooks;
- validates some freshly rerolled trigger instance instead of the original queued one.

F486 survives independently AFTER the original popup has already launched with a valid,
live target. A unit can disappear during the unanswered modal's lifetime. No launch-time
pure-validator repair can prevent that later transition.

The synchronized reply path is supposed to provide the final safety net:
`applyEvent` deliberately re-resolves the old trigger ID and re-runs `canDoEvent` before
mutation. F486 is that receiver-side validator accepting a missing concrete unit target.

Likewise this is separate from F315 / KI#638. F315 concerns selection-bound modal actions
being rebound to a DIFFERENT current selection after Rapid Unit Cycling; the popup lacks
launch-time actor identity. Random-event EventTriggeredData DOES retain stable target
identity (`m_iUnitId`). F486 is instead the validator treating failure to resolve that
stored identity as permission to continue.

ANCESTRY
--------
The exact permissive unit-existence pattern is present in all supplied lineage controls:

Civ4CE/BtS:

    CvUnit* pUnit = getUnit(kTriggeredData.m_iUnitId);
    if (NULL != pUnit)
    {
        if (!pUnit->canApplyEvent(eEvent))
            return false;
    }

K-Mod 1.46 retains the same structure.
Base AdvCiv 1.14 condenses it to the current form:

    CvUnit* pUnit = getUnit(kTriggeredData.m_iUnitId);
    if (pUnit != NULL && !pUnit->canApplyEvent(eEvent))
        return false;

Current SAS retains that Base AdvCiv form unchanged in substance.

This is therefore an inherited Firaxis/BtS event reply-validation defect, not a SAS-local
regression.

DUPLICATE / ALBUM CONTROL
-------------------------
The reconciled album was searched for:
- `m_iUnitId` in random-event validation;
- missing / vanished event units;
- At the Sword target lifetime;
- event reply `canDoEvent` target existence.

No existing root owns this missing-target acceptance.

Related roots remain distinct:
- F307/KI#630: launch-time event re-randomization/state leak.
- F315/KI#638: selection-bound popup actor identity / Rapid Unit Cycling.
- F414/KI#737: At the Sword/Friendly Locals XP help omits the runtime full heal.
- existing event AI/value/count roots concern effect valuation or help/runtime amounts,
  not concrete target lifetime at synchronized reply.

CONSERVATIVE REPAIR
-------------------
Do not globally require every EventTriggeredData with an old unit ID to retain a unit:
some triggers may pick a unit only as context while a particular event outcome has no
unit-local payload.

Instead make target existence part of legality whenever the chosen EventInfo actually
requires a concrete unit effect. A helper makes that contract explicit, e.g. treat the
event as unit-targeted when any of these are active:
- `bDisbandUnit`;
- nonzero `iUnitExperience`;
- nonzero `iUnitImmobileTurns`;
- nonempty `UnitPromotion`;
- nonempty `UnitName`;
- any other field whose runtime implementation is specifically applied through
  `pUnit->applyEvent` rather than globally.

Then:

    CvUnit* pUnit = getUnit(kTriggeredData.m_iUnitId);
    if (eventRequiresConcreteUnit(eEvent))
    {
        if (pUnit == NULL || !pUnit->canApplyEvent(eEvent))
            return false;
    }
    else if (pUnit != NULL && !pUnit->canApplyEvent(eEvent))
        return false; // preserve any intentional contextual validation

The exact helper should be derived from the fields consumed by `CvUnit::applyEvent` so
future unit-local XML additions cannot silently bypass the existence requirement.

The synchronized DLL-side `canDoEvent` check is the authoritative repair boundary; UI
popup cancellation/rebuild can improve presentation but cannot substitute for receiver
validation in multiplayer.

REGRESSION TEST
---------------
1. Construct / force `EVENTTRIGGER_AT_THE_SWORD` on a damaged Swordsman and preserve the
   triggered-data ID.
2. Confirm the outcome is initially legal.
3. Kill that exact target before `applyEvent`.
4. Assert `canDoEvent(EVENT_AT_THE_SWORD_1, oldData) == false`.
5. Assert applying the stale reply does not mark the global trigger fired / event occurred.
6. Positive control: with the target alive and eligible, the event remains legal and
   applies heal +3 XP + rename normally.
7. Non-unit-context control: an event whose trigger happens to retain a unit ID but whose
   chosen outcome has no unit-local payload should retain its intended semantics rather
   than being blanket-cancelled solely by a historical context unit disappearing.

NUMBERING
---------
F486 / KI#809 CONFIRMED.
F487 / provisional KI#810 is now UNUSED.

CURRENT STONE AFTER C031-WIP213
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F486 / KI#809 CONFIRMED.
F487 / provisional KI#810 UNUSED.
Latest physical checkpoint: C031-WIP213.
No repository source files modified.

NEXT
----
Continue from F487 unused. Stay briefly in random-event target lifetime while the
producer/receiver matrix is loaded: challenge missing plot/context targets and unit-ID
reuse/ownership transitions, but normalize anything that is merely another manifestation
of F486 rather than multiplying the same concrete-target validation root. Then resume the
remaining lightly covered normal-turn `CvPlayer.cpp` state transitions / arithmetic /
ownership paths. Preserve semantic duplicate checking before promotion and prefer a clean
closure over a weak finding.

END C031-WIP213


===============================================================================
C031-WIP214 — QUEUE 004 CVPLAYER.CPP — F487 / KI#810 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 UNUSED.
No repository source files modified.

POST-WIP213 TARGET-LIFETIME / EVENT-STATE CONTROLS
--------------------------------------------------
Before spending F487, the remaining event-lifetime siblings were challenged against
current XML and the reconciled album.

1. Missing stored OTHER-player target remains below the numbered threshold.

   Several shipped multiplayer PickPlayer triggers can retain an `m_eOtherPlayer` that
   later dies. However the enabled event choices either revalidate through concrete
   city/network/war conditions, or mutate relationship / espionage state whose ordinary
   behavioral consequence disappears with the dead target. No current effect comparable
   to F486's consumed event with a missing required payload survived. Keep this as
   target-lifetime robustness debt only.

2. `checkExpireEvent`'s ObsoleteTech receiver is generically fragile but current-data safe.

   The function resolves the original trigger owner as:

       CvPlayer& kPlayer = GET_PLAYER(kTriggeredData.m_ePlayer);

   and uses `kPlayer` for the stored religion/civic/city/unit/other-player lifetime checks,
   but the later obsolete-tech loop asks `GET_TEAM(getTeam())` on `this` rather than
   `GET_TEAM(kPlayer.getTeam())`.

   That could matter if a copied team/global QUEST occurrence were expired on a recipient
   rather than the original trigger owner. Current SAS closes the producer: every shipped
   quest EventInfo has `bGlobal=0` and `bTeam=0`. Quest occurrence state is player-local,
   so current callers run the test on the original owner and the two team expressions are
   equivalent. No F487.

3. `setEventOccured` contains a real reversed Team-event propagation loop, but current XML
   has no Team EventInfo producer.

   Current source says:

       else if (GC.getInfo(eEvent).isTeam())
       {
           for (MemberIter itOtherMember(getTeam()); itOtherMember.hasNext(); ++itOtherMember)
           {
               if (itOtherMember->getID() != getID())
                   continue;
               itOtherMember->setEventOccured(eEvent, kEventTriggered, false);
           }
       }

   This retains self and skips every other teammate — the opposite of the Global branch's
   propagation shape. It is therefore real generic/moddability source debt. A mechanical
   parse of current `CIV4EventInfos.xml`, however, finds zero `<bTeam>1</bTeam>` EventInfos;
   current EventTriggerInfos likewise have no Team-trigger producer requiring this copied
   occurrence state. Do not number under the current-data rule.

4. Delayed-event countdown use-after-erase is an exact album duplicate and remains dormant.

   The source obtains a pointer to a value inside `m_mapEventCountdown`, applies the due
   event, erases that map node through `resetEventCountdown`, then dereferences the old
   pointer for cleanup. WIP209 already preserved this exact C++ lifetime defect and its
   BtS -> K-Mod -> AdvCiv ancestry. Current SAS has empty EventTimes tables and no ordinary
   producer of countdown nodes, so it remains dormant robustness debt rather than F487.

5. Global EventInfo ClearEvents propagation closes normally.

   `applyEvent` explicitly resets a cleared Global EventInfo occurrence on every
   non-Barbarian player and a Team EventInfo occurrence on every team member. Thus the
   ordinary ClearEvents path does not leave propagated global occurrence records stale on
   observer players merely because `resetEventOccured` itself is player-local.


F487 / KI#810 — APPLYEVENT MARKS A TRIGGER FIRED BEFORE REPLY-TIME EVENT VALIDATION,
SO A STALE INVALID HUMAN EVENT CHOICE CAN PERMANENTLY CONSUME A GLOBAL NON-RECURRING
TRIGGER WITHOUT APPLYING ANY EVENT
-------------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current shipped simultaneous-multiplayer random-event flow reaches
this inherited Firaxis/BtS ordering defect. Current global/non-recurring city-targeted
events provide a direct permanent gameplay consequence.

ROOT
----
`CvPlayer::applyEvent` resolves the stored EventTriggeredData and, for an ordinary chosen
triggered event (`bUpdateTrigger=true`), performs these operations in this order:

    if (bUpdateTrigger)
        setTriggerFired(*pTriggeredData, true);

    if (!canDoEvent(eEvent, *pTriggeredData))
    {
        if (bUpdateTrigger)
            deleteEventTriggered(iEventTriggeredId);
        return;
    }

    setEventOccured(eEvent, *pTriggeredData);
    ... apply event payload ...

The durable trigger identity is therefore committed BEFORE the authoritative synchronized
reply-time legality check.

For a Global trigger, `setTriggerFired(..., true)` immediately propagates the fired state
through all player slots. For a non-recurring trigger, future trigger eligibility rejects
that trigger whenever `isTriggerFired(eTrigger)` is true. There is no rollback when the
subsequent `canDoEvent` call fails.

Consequently a stale choice can take the failure path in a contradictory state:

    trigger is permanently recorded as fired
    EventInfo occurrence is NOT recorded
    EventInfo payload is NOT applied
    triggered-data record is deleted

The trigger has been consumed even though the chosen event was rejected.

INTENT CONTROL — THE POPUP LAUNCHER ALREADY SAYS INVALID EVENTS MUST NOT COUNT
-------------------------------------------------------------------------------
This is not merely a debatable interpretation of when a trigger should count as fired.
Current AdvCiv/SAS `CvDLLButtonPopup::launchEventPopup` performs a pre-display availability
pass over the same EventInfos. If no choice is legal, it executes:

    if (!bEventAvailable)
    {   // advc.001: Don't count this as having been triggered
        kActivePlayer.deleteEventTriggered(pTriggeredData->getID());
        return false;
    }

The comment makes the intended policy explicit: if current state invalidates every event
choice, cancel the pending instance WITHOUT counting the trigger as fired.

The synchronized reply path violates that same policy only because its transaction order
commits `setTriggerFired` before re-running `canDoEvent`.

CURRENT SHIPPED REPRODUCTION — HORTICULTURE
-------------------------------------------
Current `EVENTTRIGGER_HORTICULTURE` gives a compact live state:

    bSinglePlayer = 0
    iPercentGamesActive = 80
    iWeight = 200
    bRecurring = 0
    bGlobal = 1
    bPickCity = 1
    OrPreReq = TECH_CALENDAR
    requires one owned Forest/Jungle plot

Its selectable current EventInfos include `EVENT_HORTICULTURE_1` and
`EVENT_HORTICULTURE_2`; both are concrete own-city effects (`bPickCity=1`). The first
also applies +1 commerce to the selected plot, and the second has its own current city /
follow-up payload. Either provides an ordinary legal button while its stored city exists.

Use a simultaneous-turn multiplayer game in which human A has a Horticulture instance:

1. A's ordinary random-event flow constructs EventTriggeredData with a concrete
   `m_iCityId` and queues the Event popup.
2. `launchEventPopup` resolves that original instance and runs `canDoEvent` for its choices.
   With the city still alive, at least one Horticulture button is available and the popup
   is displayed.
3. A leaves the modal unanswered. Simultaneous multiplayer allows remote human B to keep
   acting while A's local popup waits — the same supported timing class already established
   for the F315/F486 popup-lifetime reproductions.
4. B captures A's selected Horticulture city before A clicks the still-visible event
   choice.

City acquisition gives an exact identity-lifetime proof. `CvPlayer::acquireCity` saves the
old owner-local city ID, then calls:

    pOldCity->kill(false, false);
    pOldCity = NULL;

before the acquiring player initializes its replacement city. Thus A's stored old
`m_iCityId` no longer resolves through `A.getCity(oldId)` after conquest.

5. A clicks the event button that was valid when the popup was rendered. The network event
   reply reaches synchronized `CvPlayer::applyEvent(..., bUpdateTrigger=true)`.
6. `applyEvent` FIRST calls `setTriggerFired`. Because Horticulture is Global, the fired
   state is propagated to every player slot.
7. `applyEvent` THEN calls `canDoEvent` on the stale triggered data. Horticulture's city
   event validation performs:

       CvCity* pCity = getCity(kTriggeredData.m_iCityId);
       if (pCity == NULL || !pCity->canApplyEvent(...))
           return false;

   A's old city is gone, so this correctly returns false.
8. `applyEvent` deletes the triggered-data object and returns BEFORE `setEventOccured` and
   before any Horticulture payload executes.
9. Nevertheless Horticulture's durable global trigger-fired bit remains set. Because the
   trigger is non-recurring, future `getEventTriggerWeight` / event eligibility rejects it
   for the rest of the game.

The result is therefore not merely a stale popup no-op. A current global event that never
actually occurred has been permanently removed from the event pool for every player.

BROADER CURRENT SURFACE
-----------------------
Horticulture is only a deterministic current example. Current SAS contains many active,
multiplayer-enabled, Global, non-recurring triggers with stored city/player/unit context.
Any state change between popup rendering and synchronized reply that makes the chosen
EventInfo fail `canDoEvent` enters the same commit-before-validation ordering.

City capture/destruction is especially clean because `canDoEvent` already has the correct
strict missing-city rejection. F487 does not depend on F486's separate missing-unit
acceptance bug.

ANCESTRY
--------
The exact order is present in every supplied lineage control:

Civ4CE/BtS:

    if (bUpdateTrigger)
        setTriggerFired(*pTriggeredData, true);
    if (!canDoEvent(eEvent, *pTriggeredData)) ...

K-Mod 1.46 retains the same order.
Base AdvCiv 1.14 retains the same order.
Current AdvCiv-SAS retains it unchanged in substance.

This is therefore an inherited Firaxis/BtS event transaction-ordering defect, not a
SAS-local regression.

AdvCiv later added stronger popup-launch revalidation (`advc.001` / practical 4304
history, already investigated under F307), but did not change the synchronized
`applyEvent` ordering. The newer launcher comment provides especially strong evidence
that a now-invalid instance is intended to be cancelled without counting as fired.

DUPLICATE / ROOT SEPARATION
---------------------------
The reconciled album was searched for `setTriggerFired`, stale event choices, failed
`canDoEvent`, and trigger consumption. No existing root owns this commit-before-validation
ordering.

Keep related findings separate:

- F307 / KI#630: AdvCiv's popup LAUNCH revalidation incorrectly calls the state-creating /
  randomizing `initTriggeredData`, leaking a second record and touching SyncRand.
- F486 / KI#809: reply-time `canDoEvent` treats a MISSING REQUIRED UNIT as legal, so a
  unit-targeted event can be recorded/applied while its unit payload silently no-ops.
- F487 / KI#810: `canDoEvent` CAN CORRECTLY REJECT the stale instance, but the trigger was
  already durably marked fired before that rejection.
- F475 / KI#798: midgame new-player initialization reconstructs durable global trigger
  state from short-lived EventInfo occurrence records and can forget an OLD truly-fired
  trigger. F487 is the opposite transaction problem: it creates a durable fired flag for
  an event that DID NOT pass execution validation.

The roots require different repairs and produce different state shapes.

CONSERVATIVE REPAIR
-------------------
Make the reply transaction validate before committing trigger-fired state:

    EventTriggeredData* pTriggeredData = getEventTriggered(iEventTriggeredId);
    if (pTriggeredData == NULL)
        return;

    if (!canDoEvent(eEvent, *pTriggeredData))
    {
        if (bUpdateTrigger)
            deleteEventTriggered(iEventTriggeredId);
        return;
    }

    if (bUpdateTrigger)
        setTriggerFired(*pTriggeredData, true);

    setEventOccured(eEvent, *pTriggeredData);
    ...

This preserves the existing semantics for valid event choices while making reply-time
stale cancellation agree with `launchEventPopup`'s explicit "Don't count this as having
been triggered" contract.

Do not try to repair F487 solely in `CvDLLButtonPopup`: event replies are synchronized
network actions and must remain safe even if a stale or custom caller reaches the DLL.
`CvPlayer::applyEvent` is the authoritative transaction boundary.

F486's missing-unit validation still requires its independent repair. Reordering F487
alone would not help F486 because its current `canDoEvent` incorrectly returns true when
the required unit is gone.

REGRESSION TEST
---------------
1. Create a valid human Global, non-recurring city event such as Horticulture.
2. Preserve its triggered-data ID and verify the chosen EventInfo is currently legal.
3. Remove/capture the stored city before executing the event reply.
4. Assert `canDoEvent(event, oldData) == false`.
5. Execute the stale reply.
6. Assert the trigger is NOT marked fired for the owner or propagated global recipients.
7. Assert no EventInfo occurrence is recorded and no payload is applied.
8. Positive control: with the city still valid, execute the same event and assert normal
   trigger-fired propagation + EventInfo occurrence + payload.
9. Repeat with F486's missing-unit At-the-Sword case after that independent validator fix,
   confirming that both stale-target families now cancel before trigger commitment.

NUMBERING
---------
F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 is now UNUSED.

CURRENT STONE AFTER C031-WIP214
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 UNUSED.
Latest physical checkpoint: C031-WIP214.
No repository source files modified.

NEXT
----
Continue from F488 unused. Leave the event target-lifetime cluster unless a genuinely
independent current transaction/state root appears; F486 and F487 now own the two major
reply-time target/commit failures. Resume the low-semantic-coverage authoritative
`CvPlayer.cpp` pass, prioritizing normal-turn stored-state mutators, owner/team boundaries,
release-only assertion guards and validate-then-substitute patterns. Reconcile every lead
against the full album before deepening it. A clean closure remains preferable to a weak
finding.

END C031-WIP214



===============================================================================
C031-WIP215 — QUEUE 004 CVPLAYER.CPP — F488 UNUSED / LIFETIME + COUNTER + SERIALIZATION CLOSURE
===============================================================================

CONTINUATION
------------
Resumed from C031-WIP214 with F487/KI#810 confirmed and F488/KI#811 unused.
This tranche deliberately challenged structural bug classes exposed by the immediately
preceding findings rather than searching for a number to fill:

* release-only assertions followed by destructive mutation;
* validate-one-object / substitute-another-object fallbacks;
* erase-then-use and pointer/container lifetime mistakes;
* paired research-queue shadow state;
* current-format CvPlayer serialization symmetry;
* primary counters with derived boolean/count caches;
* low-coverage normal-game helpers with persistent state consumers.

No repository source file was modified.

LIFETIME / SUBSTITUTE SWEEP — CLEAN OUTSIDE ALREADY-OWNED ROOTS
---------------------------------------------------------------
A whole-file pass over current `CvPlayer.cpp` did not establish a new F488 root.

The important controls are:

1. The Advanced Start Unit/Route destructive fallbacks remain F485/KI#808. Their exact
   defect shape — initial validation fails, a fallback target is substituted, and mutation
   occurs without equivalent validation — was not found again in an ordinary normal-turn
   player-state path.

2. The delayed-event countdown still contains the already-recorded C++ lifetime defect:

       EventTriggeredData const* pTriggeredData = getEventCountdown(eLoopEvent);
       ...
       applyEvent(eLoopEvent, pTriggeredData->m_iId);
       resetEventCountdown(eLoopEvent);
       aCleanup.push_back(pTriggeredData->m_iId);

   `resetEventCountdown` erases the `std::map` element whose value address is held by
   `pTriggeredData`; the final dereference is therefore invalid. This exact dormant root
   is already present in the reconciled album. Current shipped EventInfo data has empty
   EventTimes, so there is still no ordinary current countdown producer. Do NOT renumber.

3. Selection-group cycle IDs remain protected by the previously-audited lifecycle:
   group destruction removes the cycle entry before deleting the group, teardown clears
   the cycle before uninitializing the container, and save loading preserves ordering.

4. The remaining explicit `erase` sites either take the returned iterator or immediately
   break/return after erasing. `m_listGameMessages`, spaceship-popup cleanup, event maps,
   free-promotion vectors, vote storage and unit-extra-cost storage therefore do not expose
   a second erase-then-use bug in this pass.

5. `CvPlayer::disbandUnit` was re-entered only as a destructive-control target. Its
   eligibility guards and scoring remain policy/valuation, as already recorded; do not
   import the separate SAS `CvUnit::scrap()` caller-contract root into forced financial
   disbanding.

RESEARCH QUEUE SHADOW STATE — RECLOSED
--------------------------------------
`m_abResearchingTech` remains synchronized with `m_researchQueue` under current ordinary
callers:

* `pushResearch` first rejects already-owned/already-queued techs and `canEverResearch`
  failures, recursively pushes prerequisites, then inserts the tech and sets the flag;
* `popResearch` removes the matching queue node and clears the flag;
* `clearResearchQueue` clears the queue and resets every research flag;
* normal team-tech acquisition calls `popResearch` only for members whose flag says they
  are researching the newly acquired tech.

A theoretical partial-recursion shape exists if a prerequisite push mutates the queue and
some later prerequisite unexpectedly fails `canEverResearch`, but current shipped tech
and Python research legality does not produce that sequence. The already-recorded generic
Python/API looseness remains insufficient for a current root. No F488.

CURRENT-FORMAT CVPLAYER SERIALIZATION — PAIRED
----------------------------------------------
The current `CvPlayer::read` / `CvPlayer::write` field sequence was mechanically compared
again because persistent-state corruption would outrank many normal-turn arithmetic bugs.
No supported same-version mismatch survived manual reconciliation.

Apparent parser mismatches reduce to deliberate representation details, notably:

* enum-like fields read through a temporary integer and cast back;
* manually serialized containers such as `m_playerHistory`;
* node-local/member names that crude token pairing mistakes for top-level fields.

The current writer and reader still agree on the modern layout. Historical `uiFlag`
migration branches removed by SAS remain the explicit KI#46 policy: cross-version save
migration is not promised. Do not revive unsupported old-save compatibility as F488.

The pointer-owning popup/diplomacy blocks were checked as a related count-vs-payload seam.
They write container size and skip NULL entries when serializing individual objects, which
would misalign a stream if NULL were an ordinary stored element. No ordinary NULL producer
was established:

* `CvPlayer::addPopup` dereferences the incoming pointer for its popup type before queueing
  it, so a NULL pointer cannot enter through this API;
* `CvPlayer::addDiplomacy` asserts a non-NULL pointer and all normal producers allocate a
  real `CvDiploParameters` object;
* displayed-popup extraction is an EXE/UI object list, not a demonstrated NULL source.

Keep this as defensive-format hardening only, not F488.

DERIVED COUNTER TRANSITIONS
---------------------------
The highest-signal member in this family is `changeHurryCount`:

    int const iOldHurryCount = m_aiHurryCount.get(eHurry);
    m_aiHurryCount.add(eHurry, iChange);
    ...
    if ((iOldHurryCount > 0) != (m_aiHurryCount.get(eHurry) > 0))
    {
        if (ProductionPerPopulation > 0)
            m_iPopRushHurryCount += iChange;
        if (GoldPerProduction > 0)
            m_iGoldRushHurryCount += iChange;
    }

The derived fields count ENABLED hurry TYPES, not the source multiplicity. Consequently a
generic transition `0 -> 2` would incorrectly add 2 rather than 1, and `2 -> 0` would
subtract 2. That generic implementation is fragile.

Current compiled gameplay callers nevertheless mask it completely. The only producer is
`processCivics`, and `setCivics` calls:

    processCivics(eOldCivic, -1);
    processCivics(eNewCivic, +1);

`processCivics` passes that exact +/-1 only when the civic enables the Hurry type. Thus any
actual threshold crossing is +/-1 and the derived counters remain correct today. Preserve
as generic API/mod-mod hardening; no F488.

The neighboring counter families close normally:

* `changeCommerceFlexibleCount` stores the true source multiplicity and only resets a
  commerce percentage once combined player/team flexibility is actually lost;
* `changeFreeBuildingCount` deliberately models 0-vs-positive presence in each city, so a
  larger source-count delta does not require installing multiple free copies;
* Civic booleans (`NoForeignTrade`, `NoCorporations`, `StateReligion`, special-building
  exemptions, valid specialists, etc.) are fed by the same +/-1 civic processing contract;
* religion/corporation city counts and numeric building/civic modifiers consume the full
  arithmetic value rather than maintaining a one-bit derivative.

LOW-COVERAGE NORMAL-GAME HELPERS — CLEAN CONTROLS
-------------------------------------------------
Several functions with zero literal mention in the prior album were inspected to avoid
mistaking low textual coverage for high semantic risk:

* `CvPlayer::setVote`: updates an existing `(vote-id, PlayerVote)` pair or erases it and
  immediately returns; otherwise inserts a non-NO vote. Iterator lifetime is sound.
* `CvPlayer::isSignificantDiscovery`: a bounded K-Mod/AdvCiv diplomacy-memory predicate
  comparing tech era and remaining research; no stored-state mutation or subject inversion.
* `CvPlayer::setGold`: intentionally raw state assignment plus UI dirtying; scenario Python
  also uses it for starting gold. Negative intermediate economic states are handled by the
  surrounding economy/strike pipeline rather than forbidden by this setter.
* `CvPlayer::upgradeAllPrice`: UI aggregation over this player's units of the requested
  source type, and only includes units whose individual `canUpgrade(..., true)` succeeds.
* `CvPlayer::setTurnActiveForPbem`: intentionally only flips the serialized turn-active bit,
  game active-turn count and danger-cache validity so the remaining turn can resume after
  PBEM load; it is not the full `setTurnActive` execution path by design.
* `getFoodKept`: current body is a direct BuildingInfo accessor; the old No Slavery scaling
  experiment is compiled out.

MUTATION-BEFORE-VALIDATION PASS
-------------------------------
F487 motivated a second structural search for persistent mutation followed by `can*` /
legality failure. The surviving textual hits close as follows:

* `found` calls `canFound` before city creation. Its later `canConstruct` checks only decide
  which free start-era buildings to add to an already-valid founded city.
* `processBuilding` changes already-authorized building effects; its capability calls govern
  secondary civic-popup/UI behavior rather than validation of the building mutation.
* `pushResearch` is covered by the research-queue control above.
* `updateGroupCycle` is covered by the already-proven group-cycle lifecycle invariant.
* `buildTradeTable` constructs offer inventory; it does not commit a trade before its
  `canTradeItem` predicates.
* `doTurn` contains many normal per-turn updates before later independent legality checks;
  no single transaction was found whose failure leaves a partially committed action.

`doGoody` is the one interesting policy-shaped exception. It removes the hut before trying
up to `NUM_DO_GOODY_ATTEMPTS` random reward candidates. Therefore ten ineligible random
rolls can theoretically consume a hut without `receiveGoody`. Stock BtS uses the same
finite-attempt design (BtS GlobalDefines: NUM_DO_GOODY_ATTEMPTS=10), and current handicap
lists always include independently valid reward classes such as gold. The source provides
no authoritative invariant that every hut must produce an outcome; the finite attempt cap
itself appears intentional. Treat as stochastic design/policy unless stronger evidence says
otherwise. Do not spend F488 on it.

TOTAL CULTURE INTEGER RANGE NOTE — ROBUSTNESS / UI, NOT F488
------------------------------------------------------------
`countTotalCulture()` sums each city's `getCultureTimes100(getID())` into a 32-bit `int`
and divides by 100 only after the sum. It can therefore overflow once empire-wide summed
culture exceeds roughly 21.47 million culture points even though the returned whole-culture
value itself would still fit in `int`.

Current shipped victory XML has `iTotalCultureRatio = 0` for every VictoryInfo, so the old
team-total-culture victory path is inactive. The principal current consumer reached in this
pass is `updateHistory(PLAYER_HISTORY_CULTURE)`, i.e. historical/UI state. Extremely large
empires or exceptionally long games could therefore corrupt culture history/rank display,
but a normal current gameplay decision has not been demonstrated. Keep as integer-range
hardening rather than F488.

CURRENT STONE AFTER C031-WIP215
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 UNUSED.
Latest physical checkpoint: C031-WIP215.
No repository source files modified.

NEXT
----
Continue from F488 unused. The immediate structural classes above are now saturated enough
that another pass over them would be low-yield. Return to authoritative arithmetic/identity
helpers and low-coverage normal-turn state with external gameplay consumers. Good next
priorities are:

* current-vs-owner/team subject selection in cost, religion, espionage and trade helpers;
* arithmetic boundaries whose current XML/options make the result gameplay-active;
* death/revival and city acquisition state not already owned by the PA/event roots;
* persistent counters whose consumer is in another class and therefore escaped local
  transition review;
* historical bugfix comments where the repaired predicate has since gained new SAS data.

Continue semantic duplicate search before promotion. F488 remains genuinely unused; a clean
closure is preferable to manufacturing KI#811.

END C031-WIP215


===============================================================================
C031-WIP216 — QUEUE 004 CVPLAYER.CPP — F488 / KI#811 REMAINS UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 remains UNUSED.
No repository source files modified.

POST-WIP215 PERMANENT-ALLIANCE RESOURCE/GPT DEAL CLOSURE
--------------------------------------------------------
The unfinished Permanent-Alliance annual-deal question was traced end-to-end rather
than inferred from the fact that `CvTeam::addTeam` kills only selected team-level
agreements.

`CvTeam::addTeam` intentionally performs player reassignment first, then kills only
same-team deals containing obsolete TEAM-level items:

    TRADE_OPEN_BORDERS
    TRADE_DEFENSIVE_PACT
    TRADE_PEACE_TREATY
    TRADE_VASSAL
    TRADE_SURRENDER
    TRADE_DISENGAGE

It deliberately does not kill ordinary player-level annual items such as
`TRADE_RESOURCES` or `TRADE_GOLD_PER_TURN` merely because the two players have become
members of the same team.

That is coherent with the rest of the current trade contract:

* `CvPlayer::canPossiblyTradeItem(..., TRADE_RESOURCES)` does not reject same-team
  players; it asks whether their capitals/cities have a valid trade network.
* `CvPlayer::canTradeItem` likewise permits a resource from any positive tradeable copy
  (`bCanTradeAll=true` under AdvCiv's current resource-trade rules).
* `CvDeal::verify` continues to validate every saved resource item. It cancels the deal
  if the giver's tradeable count becomes negative, the post-merge trade network is no
  longer valid, or either team's technology makes the resource obsolete.
* If the annual deal survives those checks, its `changeBonusExport(+1)` /
  `changeBonusImport(+1)` state remains the mechanism by which that explicit player-level
  resource transfer continues.
* When the deal later ends normally, `CvDeal::endTrade` still executes the ordinary
  `changeBonusImport(-1)` / `changeBonusExport(-1)` teardown even when both players now
  share a team. The same-team early return added for KI#613 is deliberately restricted
  to the obsolete team-level agreement types above and does not swallow player-level
  annual components.
* `TRADE_GOLD_PER_TURN` follows the same player-level pattern: the surviving saved deal
  retains its per-player GPT transfers, and ordinary teardown reverses them later.

Thus there is no stale import/export or GPT counter merely because a Permanent Alliance
forms. The surviving annual trade can still be a valid explicit transfer between team
members, and current `CvDeal::verify` remains able to cancel a resource whose network or
backing copy later disappears.

Classification: clean control. No F488.

CROSS-CLASS PLAYER-MODIFIER CACHE PASS
--------------------------------------
A follow-up pass sampled zero/low-mention player mutators whose values are consumed by
cities or economy code, looking specifically for a player-side mutation that fails to
refresh a copied city/player cache.

The main live families close normally:

* `changeCoastalTradeRoutes` and `changeTradeRoutes` call `updateTradeRoutes()`.
* `changeCommerceRateModifier` calls `updateCommerce(eCommerce)` and dirties citizen
  assignment; capital-only commerce modifiers directly update the capital.
* `changeNumCitiesMaintenanceModifier`, `changeColonyMaintenanceModifier` and
  `changeCorporationMaintenanceModifier` call `updateMaintenance()`.
* `changeStateReligionHappiness` / `changeNonStateReligionHappiness` call
  `updateReligionHappiness()`.
* `changeFeatureHappiness` calls `updateFeatureHappiness()`.
* free-specialist / specialist-validity / health / happiness holder changes dirtied
  assignment where their authoritative city calculations consume the player value.
* Great Person/General, hurry, worker/improvement speed, military/space production and
  similar modifiers sampled here remain dynamically consumed or were already closed in
  the earlier F471 low-coverage mutator pass.

No new copied-cache invalidation root survived.

`changeLuxuryModifier` deserves a robustness note but does not meet current-data
promotion. The mutator clamps the STORED accumulator after every delta:

    m_iLuxuryModifier += iChange;
    m_iLuxuryModifier = range(m_iLuxuryModifier, -100, 1000);

That is mathematically non-invertible if independent sources ever stack past a bound:
clamped-away excess would not reappear when one source is later removed. Current shipped
SAS, however, has only one nonzero XML `iLuxuryModifier` source, the +25 civic entry.
No normal current source reaches either clamp, so the weakness is data-masked generic
extension debt, not F488.

LOW-COVERAGE UI/IDENTITY HELPERS
--------------------------------
Several genuinely zero-mention helpers were inspected rather than assumed unsafe:

* `getNewCityName` / `getCivilizationCityName` / `isCityNameValid` retain the current
  KI#325 stable-key + rendered-name duplicate protection. Current canonical city-name
  generation avoids past/current key duplicates and current display duplicates; no new
  language/manual-name collision survived.
* `setFlagDecal`'s reference-identity early return only suppresses work when the caller
  literally passes the already-stored InitCore string; no changed-value update is lost.
* `setTurnActiveForPbem` updates `m_bTurnActive`, the game's active-turn count and the
  active-player safe-range cache; this matches the earlier PBEM lifecycle closure.
* `showForeignPromoGlow` is graphics/UI state and its current call pair (`doWarnings`
  true, end-turn false) did not expose gameplay-state mutation.
* `getRealPopulation` deliberately accumulates city population in 64-bit and then uses
  `truncIntCast<int>`, which clamps safely to the public 32-bit API range. Current
  consumers are demographics/advisor/UI paths. Very large empires can saturate the
  displayed aggregate at INT_MAX, but no ordinary gameplay-state consequence or stronger
  current contract was established; keep as a range/API limitation, not F488.

DUPLICATE CONTROL — ANARCHY/GOLDEN-AGE RESCALING
-------------------------------------------------
The nearby K-Mod modifier-rescaling formulas were not reopened. The reconciled album
already owns `changeAnarchyModifier`'s reachable -100 / stacked-negative singularity as
F467 / KI#790. The sister `changeGoldenAgeModifier` denominator shape was already closed
as current-data masked because shipped SAS has only a +50 Golden Age modifier source.

CURRENT STONE AFTER C031-WIP216
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F487 / KI#810 CONFIRMED.
F488 / provisional KI#811 UNUSED.
Latest physical checkpoint: C031-WIP216.
No repository source files modified.

NEXT
----
Continue from F488 unused.

Return to cross-class authoritative state rather than repeatedly sampling trivial
wrappers. Highest-value remaining angles:

* player values whose owner/team meaning changes across city acquisition, player death,
  vassalization or team merge, while the consumer lives outside `CvPlayer.cpp`;
* historical AdvCiv/K-Mod fixes whose repair updates one transition producer but may
  omit an equivalent normal producer;
* player-owned delayed/persistent state whose identity is stored as player-local city,
  unit or team IDs and later consumed after ownership/team transitions;
* arithmetic accumulators with multiple current nonzero sources where add/remove
  symmetry can be proven or disproven under shipped XML.

Require current shipped reachability, authoritative consequence, ancestry/comparison and
semantic duplicate proof before consuming F488. A deep clean closure remains preferable
to manufacturing KI#811.

END C031-WIP216

================================================================================
C031-WIP217 — QUEUE 004 CVPLAYER.CPP — F488 / KI#811 CONFIRMED
================================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
No repository source files modified.

POST-WIP216 LOW-COVERAGE / DUPLICATE CONTROLS
---------------------------------------------
The immediate continuation first finished the zero/near-zero-mention helper tranche
without turning heuristic or already-owned behavior into new roots.

* `isNoResearchAvailable`, `canChangeReligion`, holy-city/HQ counts and neighboring
  religion/corporation helpers did not expose a new authoritative state contradiction
  under their current compiled callers.
* `unitsGoldenAgeReady` versus `killGoldenAgeUnits` rediscovered the already-preserved
  UnitClass-vs-UnitType extension mismatch. Current SAS still has one Golden-Age-capable
  UnitType per relevant Great Person class, so the mismatch remains data-masked and is
  not F488.
* `getWorkRate(BuildTypes)` still contains the previously parked doubled-base AI
  estimate (`100 + AIWorkRateModifier + 100`). Its compiled consumers use it as an
  estimate/predictor; real Worker build progress uses unit work rate. This remains the
  prior robustness/UI-estimator control, not a new gameplay root.
* `updateCommerceRates`' repeated-largest-remainder bug was rediscovered mechanically
  and rejected immediately as a duplicate. It is already F444 / KI#767.
* The Permanent-Alliance TeamTypes-indexed espionage-spending-weight slots are likewise
  already an explicit album closure: the live allocator visits only alive OTHER teams,
  excluding both the absorbed dead team and the player's new own team.

The next genuinely zero-mention helper, `CvPlayer::isSignificantDiscovery`, exposed a
new current rule-state defect below.


F488 / KI#811 — ADVC SIGNIFICANT-DISCOVERY CHECKS RUN AFTER NON-TRADE TECH
ACQUISITION, SO STOLEN / INTERNET / GOODY / COLONY TECHS CAN BYPASS TECH-BROKERING
RESTRICTIONS
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary current shipped gameplay reaches the defect through the enabled
Steal Technology espionage mission. Current The Internet and colonial-vassal creation
provide additional deterministic/current manifestations; full Goody-tech discovery is
another reachable manifestation.

This is an AdvCiv integration/order regression. The helper's documented input is the
recipient's PRE-ACQUISITION research progress. The ordinary technology-trade caller
correctly snapshots that value before granting the technology. Every current non-trade
caller instead evaluates the helper only after the relevant team already knows the
technology (or, for colony inheritance, asks the already-knowing parent), forcing the
research-left part of the test to false.

AUTHORITATIVE RULE / INTENT
---------------------------
AdvCiv change `advc.550e` deliberately softens the ordinary BtS/K-Mod tech-brokering
restriction for an "insignificant" discovery. Current `CvPlayer::isSignificantDiscovery`
defines significance as:

    bool CvPlayer::isSignificantDiscovery(TechTypes eTech) const
    {
        return (GC.getInfo(eTech).getEra() >= getCurrentEra() - 1 &&
                GET_TEAM(getTeam()).getResearchLeft(eTech) >
                GET_TEAM(getTeam()).getResearchCost(eTech) / 3);
    }

So a received technology is significant when BOTH:

* it is not at least two eras behind the recipient; and
* the recipient still had more than one third of the technology left to research
  (equivalently, had not already completed at least two thirds).

The AdvCiv manual is explicit about how that predicate interacts with brokering:

* insignificant tech trades are unaffected by the one-turn brokering delay and by
  `GAMEOPTION_NO_TECH_BROKERING`;
* insignificant technology received through mechanisms OTHER than trade may likewise
  be traded immediately, with The Internet and colonial-vassal creation given as
  examples;
* the contrasting baseline is that technology not discovered through the civilization's
  own research / Great People is subject to the normal brokering restriction unless the
  discovery is insignificant.

The 2019-04-22 AdvCiv history is even more direct:

    "Insignificant discoveries (2 eras behind or already had 2/3 progress) are not
     subject to tech brokering restrictions. This had already been the case for tech
     trade and now also applies to stolen tech, the Internet and tech shared upon the
     creation of a colonial vassal. [advc.550e]"

The current conditions are therefore meant to distinguish significant from insignificant
received technology. They are not dead compatibility decorations.

WHY THE HELPER MUST BE CALLED BEFORE ACQUISITION
------------------------------------------------
Current `CvTeam::getResearchLeft` deliberately returns zero for any already-known
non-repeat technology:

    int CvTeam::getResearchLeft(TechTypes eTech) const
    {
        if (isHasTech(eTech) && !GC.getInfo(eTech).isRepeat())
            return 0;
        return max(0, getResearchCost(eTech) - getResearchProgress(eTech));
    }

Therefore, for an ordinary non-repeat technology:

    setHasTech(eTech, true, ...);
    isSignificantDiscovery(eTech);

can NEVER pass the second significance condition afterward:

    getResearchLeft(eTech) == 0
    0 > getResearchCost(eTech) / 3   // false

This is deterministic and independent of actual pre-acquisition research progress.

CORRECT CONTROL — NORMAL TECHNOLOGY TRADE SNAPSHOTS FIRST
----------------------------------------------------------
`CvDeal::startTrade` demonstrates the intended transaction order:

    bool const bSignificantTech = kToPlayer.isSignificantDiscovery(eTech);
    GET_TEAM(eToPlayer).setHasTech(eTech, true, ...);
    if (bSignificantTech)
        GET_TEAM(eToPlayer).setNoTradeTech(eTech, true);

This preserves the recipient's research-left state before `setHasTech` destroys it and
uses the saved result after acquisition.

CURRENT BROKEN CALLER A — ESPIONAGE TECHNOLOGY THEFT
-----------------------------------------------------
The enabled Steal Technology mission executes:

    GET_TEAM(getTeam()).setHasTech(eTech, true, getID(), false, true, false,
            TECH_ACQUISITION_ESPIONAGE);
    if (isSignificantDiscovery(eTech))
        GET_TEAM(getTeam()).setNoTradeTech(eTech, true);

The significance check therefore always sees zero research left for the newly stolen
non-repeat technology and never sets its no-trade flag.

This path is fully live in current SAS. `ESPIONAGEMISSION_BUY_TECH` has:

    bIsPassive = 0
    bIsTwoPhases = 1
    bTargetsCity = 1
    TechPrereq = NONE
    iBuyTechCostFactor = 50

and `CvPlayer::canStealTech` permits any technology the target knows and the spy owner
can currently research.

DETERMINISTIC CURRENT PAPER REPRODUCTION
----------------------------------------
Use three ordinary major civilizations A, B and C and enable:

    GAMEOPTION_NO_TECH_BROKERING

while leaving No Tech Trading disabled.

Prepare A so that:

* A knows current `TECH_WRITING`, which enables technology trading;
* A knows Paper's current prerequisites (`TECH_LITERATURE` and `TECH_CONSTRUCTION`);
* A does NOT know `TECH_PAPER` and has 0 research progress toward Paper;
* A's era is no more than one era ahead of Paper (an ordinary Ancient/Classical state
  suffices);
* A has an eligible Spy / espionage points against B.

Current Paper has:

    Era    = ERA_CLASSICAL
    iCost  = 425
    bTrade = 1

and B knows Paper.

Before theft, for A:

    Paper era condition = true
    research left       = research cost = 425
    425 > 425 / 3       = true

so Paper is unambiguously a SIGNIFICANT discovery under AdvCiv's own helper.

Now steal Paper from B through the ordinary Steal Technology mission:

1. `doEspionageMission` calls `A.team.setHasTech(Paper, true, ...)`.
2. Paper is now known by A's team.
3. `isSignificantDiscovery(Paper)` runs afterward.
4. `getResearchLeft(Paper)` returns 0 because Paper is a known non-repeat technology.
5. `0 > 425 / 3` is false.
6. `setNoTradeTech(Paper, true)` is skipped.

Give third party C Paper's prerequisites and leave C without Paper. Current
`CvPlayer::canTradeItem(TRADE_TECHNOLOGIES)` accepts a technology when:

    tech.isTrade()
    source team has tech
    !sourceTeam.isNoTradeTech(tech)
    recipient team lacks tech
    recipient canResearch(tech, true)

and `canPossiblyTradeItem(TRADE_TECHNOLOGIES)` is true when No Tech Trading is off and
either side has technology trading enabled.

All those conditions can hold after the theft. In particular, the broken post-acquisition
check leaves:

    !A.team.isNoTradeTech(Paper) == true

so A can broker the significant stolen Paper to C despite No Tech Brokering being
enabled.

This is a real game-rule consequence, not an AI-value or advisor mismatch.

BROKERING FLAG LIFETIME MAKES THE CONSEQUENCE PERSISTENT
--------------------------------------------------------
`CvTeam::isNoTradeTech` is the direct stored flag consumed by technology-trade legality.
`CvTeam::doTurn` clears all no-trade-tech flags only when
`GAMEOPTION_NO_TECH_BROKERING` is OFF:

    if (!GC.getGame().isOption(GAMEOPTION_NO_TECH_BROKERING))
        for each tech
            setNoTradeTech(tech, false);

Thus:

* with No Tech Brokering ON, a significant received tech is supposed to remain
  non-brokerable, but F488 never sets the flag at all;
* with No Tech Brokering OFF, the same flag implements the normal temporary/one-turn
  brokering delay. F488 also removes that delay from these significant non-trade
  acquisitions, allowing immediate brokerage.

The defect therefore matters both as an option-rule bypass and as a same-turn brokering
restriction bypass.

BROKEN CALLER B — THE INTERNET / TECH SHARE
--------------------------------------------
Current SAS still has a live globally unique Tech Share source:

    PROJECT_THE_INTERNET
        TechPrereq = TECH_COMPUTERS
        iTechShare = 2

`CvTeam::updateTechShare` currently does:

    setHasTech(eTech, true, NO_PLAYER, true, true, false,
            TECH_ACQUISITION_TECH_SHARE);
    if (GET_PLAYER(getLeaderID()).isSignificantDiscovery(eTech))
        setNoTradeTech(eTech, true);

Again the helper runs only after the receiving team already knows the non-repeat tech,
so `getResearchLeft` is zero and the significant branch is unreachable.

This directly defeats the `advc.550e` history statement that the insignificant-discovery
EXEMPTION "now also applies to ... the Internet": significant Internet grants should
retain the normal restriction; current code instead treats every Internet grant as if it
were insignificant.

BROKEN CALLER C — COLONIAL-VASSAL TECHNOLOGY INHERITANCE
---------------------------------------------------------
`CvPlayer::splitEmpire` grants each parent-team technology to the new colony team and then
checks:

    if (parentTeam.isNoTradeTech(eTech) ||
        (NO_TECH_BROKERING && isSignificantDiscovery(eTech)))
    {
        newTeam.setNoTradeTech(eTech, true);
    }

The `isSignificantDiscovery` call is on `this`, i.e. the PARENT player whose team already
knows `eTech`. Therefore its `getResearchLeft(eTech)` is already zero regardless of the
new colony's pre-inheritance state, and the significance clause is dead.

Consequently, under No Tech Brokering:

* technology for which the parent already carries a no-trade flag still transfers that
  flag through the first clause;
* a significant technology that the parent discovered normally itself has no such flag,
  and the dead significance clause fails to mark the newborn colony's inherited copy.

The new colony can therefore broker inherited significant technology that it did not
research, contrary to the manual/history's explicit colonial-vassal `advc.550e` rule.

This manifestation also shows that the conservative repair is not merely "move the call
one line upward" everywhere: the significance subject for inheritance must be the
RECEIVING colony before its tech is granted, not the already-knowing parent.

BROKEN CALLER D — FULL GOODY-TECH DISCOVERY
--------------------------------------------
Current `receiveGoody` has the same ordering:

    if (goody progress is enough to complete the selected tech)
    {
        ourTeam.setHasTech(eBestTech, true, ... TECH_ACQUISITION_GOODY);
        if (isSignificantDiscovery(eBestTech))
            ourTeam.setNoTradeTech(eBestTech, true);
    }

The condition is again guaranteed false after full discovery.

This is reachable under shipped data rather than a dormant generic branch. Current
`GOODY_TECH` grants research progress (base 23 plus two current random components), and
current tradeable `bGoodyTech=1` technologies include 50-cost Ancient technologies such
as Fishing, Hunting, Mining, Mysticism and Pottery. When the rolled progress reaches the
full-discovery threshold, `setHasTech` executes and the post-acquisition significance
check loses the pre-hut state.

The espionage reproduction is preferred for a deterministic regression test, but the
Goody path is another current manifestation of the same caller-contract root.

ALL CURRENT CALLERS — ONE CORRECT, FOUR BROKEN
-----------------------------------------------
A whole current-source caller census for `isSignificantDiscovery` finds exactly these
runtime uses:

1. `CvDeal::startTrade` — CORRECT: snapshots before `setHasTech`.
2. `CvPlayer::receiveGoody` — BROKEN: checks after `setHasTech`.
3. `CvPlayer::doEspionageMission` — BROKEN: checks after `setHasTech`.
4. `CvTeam::updateTechShare` — BROKEN: checks after `setHasTech`.
5. `CvPlayer::splitEmpire` — BROKEN: checks the already-knowing parent after/while
   granting inherited tech to the child.

This strongly localizes the defect: the helper has a pre-acquisition-state contract, and
all of its later non-trade integrations violate that contract in the same semantic way.

ANCESTRY / INTRODUCTION
-----------------------
Stock BtS/Civ4CE and the supplied K-Mod baseline demonstrate the older conservative
brokering behavior:

* stolen technology is granted and then unconditionally marked `NoTradeTech`;
* a full Goody technology is unconditionally marked `NoTradeTech`;
* under No Tech Brokering, technology inherited by a new colony is marked no-trade.

K-Mod's ordinary tech-trade path already computes the "significant tech" heuristic BEFORE
`setHasTech` for technology-memory purposes, but still marks received trade technology
no-trade unconditionally.

AdvCiv change `advc.550e` intentionally turns that unconditional behavior into the
significant-vs-insignificant distinction. Its trade path gets the ordering right.

The Goody integration appeared earlier in AdvCiv practical 1301 / commit
`7afb62a4bbe8912f4ee0ac52a649bcf886b8ed0e` (2018-04-15, v0.92c) as part of the
`advc.314` Goody overhaul; that diff already contains the post-`setHasTech`
`isSignificantDiscovery` call.

AdvCiv practical 1481 / commit
`4a65bf5c0fb187fd25d4908346abb462354a9172` (2019-04-22) explicitly extends
`advc.550e` to:

* stolen technology;
* The Internet / Tech Share;
* colonial-vassal inherited technology.

The introducing diff replaces the older unconditional no-trade behavior with the current
conditional calls but places those calls after acquisition / on the already-knowing
parent. The commit message states the desired exemption exactly, so this is a pinpoint
integration-order regression rather than an inferred design preference.

Base AdvCiv 1.14 retains the same broken call ordering. Current AdvCiv-SAS inherits it.
The SAS `TechAcquisitionCause` additions only annotate/report acquisition provenance and
do not set or repair `m_abNoTradeTech` automatically.

DUPLICATE / ALBUM CHECK
-----------------------
The full reconciled C031-WIP209 album was searched literally and semantically for:

* `isSignificantDiscovery`;
* No Tech Brokering / brokering;
* `setNoTradeTech` / no-trade-tech state;
* stolen-tech brokerage;
* Internet / Tech Share brokering;
* significant discovery.

No numbered root owns this ordering failure.

The closest prior notes are distinct:

* Queue-013 records a robustness-only possibility of a stale TRUE `m_abNoTradeTech`
  after unsupported/external tech removal and reacquisition. F488 is the opposite live
  failure: a required TRUE flag is never set after ordinary acquisition.
* KI#312 concerns SAS master/vassal AI technology-supply inference and correctly uses
  `canTradeItem` to respect an EXISTING no-broker flag. It does not create missing
  no-trade state after acquisition.
* F414/KI#? and related Internet findings concern Tech Share contact/count semantics,
  not the brokering status of a technology that has already been granted.
* F484 / KI#807 concerns generic Steal Technology affordability selection and is
  independent of the post-theft brokering flag.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve the existing `advc.550e` policy; fix transaction ordering/subject only.

Espionage:

    bool const bSignificant = isSignificantDiscovery(eTech);
    team.setHasTech(eTech, true, ...);
    if (bSignificant)
        team.setNoTradeTech(eTech, true);

Goody full discovery: same pre-acquisition snapshot before `setHasTech`.

Tech Share: compute significance on a receiving-team player BEFORE the team receives the
technology, then grant and apply the saved flag. Because research progress and era are
team/player state respectively, use the same intended recipient/leader convention as the
current helper call, just before `setHasTech`.

Colony inheritance: evaluate significance for the RECEIVING colony/new team BEFORE that
technology is granted. Do not ask the parent, because the parent already knows the tech
and therefore cannot supply meaningful "research left" for the recipient. If newborn
colony research progress is intentionally initialized to zero, then contemporary/recent
inherited technology is significant while sufficiently old technology remains exempt by
the era clause, matching the documented `advc.550e` rule.

A helper design that makes misuse harder would be to expose a pre-acquisition predicate
whose name/arguments make recipient state explicit, or to pass the precomputed research
left/progress into the significance calculation. The minimal fix, however, is simply to
snapshot before mutation at every receiving path, matching the already-correct
`CvDeal::startTrade` pattern.

REGRESSION TESTS
----------------
Primary deterministic espionage test:

1. Enable No Tech Brokering; leave No Tech Trading disabled.
2. A knows Writing, Literature and Construction; A does not know Paper and has 0 Paper
   progress. B knows Paper. C can research Paper.
3. Assert before theft:
       A.isSignificantDiscovery(Paper) == true
4. Steal Paper from B through `ESPIONAGEMISSION_BUY_TECH`.
5. Assert immediately after acquisition:
       A.team.isHasTech(Paper) == true
       A.team.isNoTradeTech(Paper) == true
6. Assert A cannot offer Paper to C through `canTradeItem` while No Tech Brokering is on.
7. Repeat with A already at >= 2/3 Paper progress before theft; the resulting Paper
   should be insignificant and brokerable under the AdvC rule.
8. Repeat with a technology at least two eras behind A; it should likewise be exempt.

No-option timing control:

9. Disable No Tech Brokering and steal a significant tech.
10. It should receive the ordinary temporary no-trade state immediately after theft,
    then become tradeable when the normal team-turn clearing rule removes the flag.

Tech Share:

11. Build current `PROJECT_THE_INTERNET` and receive a recent tech known by the required
    other players while the recipient has <2/3 progress.
12. Verify the significant Internet grant receives no-trade state; repeat with >=2/3
    progress to verify the intended exemption.

Colony:

13. Under No Tech Brokering, create a colonial vassal while the parent owns a recent
    self-researched tradeable technology.
14. Verify the colony's inherited copy is no-trade even though the parent's own copy is
    not, while sufficiently old inherited technology remains exempt under `advc.550e`.

Goody:

15. Force/seed a full-discovery GOODY_TECH for a recent tradeable technology with less
    than 2/3 pre-hut progress and verify no-trade state is installed before the ordinary
    later clearing policy applies.

NUMBERING
---------
F488 / KI#811 is CONFIRMED.
F489 / provisional KI#812 is the next unused root.

CURRENT STONE AFTER C031-WIP217
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
Latest physical checkpoint: C031-WIP217.
No repository source files modified.

NEXT
----
Continue from F489 unused.

First finish the immediate technology-acquisition sibling sweep while F488 is hot:

* every other persistent acquisition/provenance flag that is computed before/after
  `CvTeam::setHasTech`;
* free-tech / Great-Person / first-discovery paths and whether their no-trade status is
  intentionally self-discovered or externally received;
* tech removal/reacquisition only as a control unless a current ordinary producer exists;
* AI/UI trade-table caches only if a missing F488 flag creates an additional independent
  state root rather than merely exposing F488.

Then return to the remaining low-coverage authoritative `CvPlayer` state and cross-class
owner/team transition consumers. Require current shipped reachability, concrete gameplay
consequence, ancestry/comparison and semantic duplicate proof before consuming F489.

END C031-WIP217



===============================================================================
C031-WIP218 — QUEUE 004 CVPLAYER.CPP — POST-F488 ACQUISITION + LOW-COVERAGE CONTROLS
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
F489 / provisional KI#812 remains UNUSED.
No repository source files modified.

POST-F488 CONTROL — FREE-CHOICE TECHNOLOGIES ARE NOT ESTABLISHED AS A MISSING
NO-TECH-BROKERING RESTRICTION
----------------------------------------------------------------------------
The immediate sibling sweep challenged Oracle / first-free-tech / Liberalism-style
`TECH_ACQUISITION_FREE_CHOICE` grants because both the human network receiver and AI
free-tech chooser grant the selected technology through `CvTeam::setHasTech` without
calling `setNoTradeTech`.

Mechanically this is real: unlike the external-acquisition paths in F488, there is no
post-grant no-trade attempt to misorder. However, the policy evidence does not justify
classifying free choice as another `advc.550e` omission.

The initial lead came from a sentence in the AdvC manual saying that tech-brokering
restrictions apply to technologies not discovered through own research or Great People.
A full-context reread corrects that interpretation: the sentence belongs to the K-Mod
comparison column of the two-column AdvC / K-Mod table. The AdvC column instead discusses
technologies RECEIVED through external mechanisms, specifically e.g. the Internet and
colonial-vassal creation, and `advc.550e` history explicitly adds special handling for
stolen technology, Internet sharing and colonial inheritance.

Stock BtS/Civ4CE, K-Mod, Base AdvCiv and current SAS all leave the free-choice network
path brokerable. The network payload itself describes the choice as `Discover`, and the
ordinary sources are earned discovery rewards rather than receipt from another
civilization. No AdvC rule text or history was found that deliberately reclassifies such
free choices as externally received technology.

Great-Person technology acquisition is separately and explicitly exempt in the K-Mod
comparison rule and reaches research completion through the Great-Person acquisition
cause. Random-event beakers likewise add research progress rather than handing over a
fully external technology copy.

Conclusion: preserve Oracle/free-choice behavior as an intentional/self-discovery policy
category unless stronger contrary evidence appears. Do not spend F489 on the absence of
`setNoTradeTech` in `TECH_ACQUISITION_FREE_CHOICE`.

This also sharpens F488: that root remains specifically about the external-acquisition
callers that DO invoke `isSignificantDiscovery`, but invoke it only after the grant has
destroyed the pre-acquisition research-left input.

LOW-COVERAGE CONTROL — `coastRiverStartingAreaScore`
----------------------------------------------------
The largest genuinely zero-name-coverage `CvPlayer` helper was audited next.
`coastRiverStartingAreaScore(CvArea const&)` scans viable non-peak plots in the candidate
area, asks the player's team-sensitive natural-yield helpers whether the tile is usable,
and weights coastal land and river access for start-placement scoring.

The player/team subject matches the `findStartingAreas` caller and no wrong-area,
wrong-owner or arithmetic transition was established. The nearby starting-area sort
comparator using `a.second >= b.second` is a real strict-weak-ordering / portability
wrinkle already preserved earlier in the album; no deterministic current Civ4 gameplay
failure has been established from equal-score ties, so it is not recycled into F489.

LOW-COVERAGE CONTROL — `CvPlayer::getWorkRate(BuildTypes)` HAS A SUSPICIOUS AI
MULTIPLIER BUT NO CURRENT ORDINARY AI GAMEPLAY CONSUMER
-------------------------------------------------------------------------------
The player-level work-rate estimator contains:

    if (!isHuman() && !isBarbarian())
    {
        iRate *= max(0, 100 + getHandicapInfo().getAIWorkRateModifier() + 100);
        iRate /= 100;
    }

which algebraically applies `200 + AIWorkRateModifier` rather than the ordinary
`100 + modifier` shape used by actual `CvUnit::workRate`. Base AdvC 1.14 retains the same
expression, while K-Mod's real unit work-rate calculation uses the conventional formula.

The caller trace closes the current gameplay route, however. `CvPlot::getBuildTurnsLeft`
can use this player estimator, but the selection-group gameplay use that can influence
orders is explicitly gated to HUMAN groups; the other ordinary current consumer is
active-player partial-build hover/help. A normal AI does not use this helper to advance
Worker builds or choose orders through the inspected paths.

Thus the AI branch is strong latent/helper correctness debt, but no ordinary current SAS
AI gameplay consequence was established. Do not assign F489 without a real producer.

LOW-COVERAGE CONTROL — `checkAlert` BOUNDARY GUARD IS DEFENSIVELY OFF BY ONE,
BUT SHIPPED ALERT IDS NEVER REACH THE BAD VALUE
-------------------------------------------------------------------------
Current `CvPlayer::checkAlert` guards:

    if (iAlertID < 0 || iAlertID > (int)m_paAlerts.size())
        return;
    m_paAlerts[iAlertID]->check(...);

The defensive comparison should generically be `>=`; an ID exactly equal to the vector
size would otherwise index one past the end.

Current producers close normal reachability. `initAlerts` installs exactly four alert
objects, the internal loop calls IDs strictly below `size()`, and shipped `Civ4lerts.py`
subclasses expose IDs 0, 1, 2 and 3. No ordinary producer supplies boundary ID 4.
Preserve as API/defensive hardening only; not F489.

DESTRUCTIVE-CONTAINER CONTROL — `killUnits()` IS SAFE UNDER FFREEList ITERATION
-------------------------------------------------------------------------------
`CvPlayer::killUnits` looks dangerous at first sight:

    FOR_EACH_UNIT_VAR(pLoopUnit, *this)
        pLoopUnit->kill(false);

because each body call removes the current object from the player's `FFreeListTrashArray`.
The actual iterator contract is deletion-safe. `FOR_EACH_UNIT_VAR` expands through
`firstUnit/nextUnit`, and `FFreeListTrashArray::nextIter` increments the numeric iteration
position BEFORE returning a live object:

    T* pObj = getAt(*pIterIdx);
    if (pObj != NULL)
    {
        (*pIterIdx)++; // prime for next call
        return pObj;
    }

Deleting the returned current unit therefore cannot invalidate the already-primed next
index. If unit death recursively removes cargo or other later units, the next scan simply
skips those now-NULL slots. No current elimination-time iterator invalidation survives.

This is consistent with the separate linked-list `_VAR_IN` traversal helper, which
explicitly advances its node before entering a body that may delete the current unit.

CURRENT STONE AFTER C031-WIP218
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
Latest physical checkpoint: C031-WIP218.
No repository source files modified.

NEXT
----
Continue from F489 unused.

Resume the mechanically ranked low-coverage authoritative player state rather than
reopening the now-closed F488 acquisition siblings. Prioritize functions whose stored
player state is consumed by another class or whose destructive/lifecycle action crosses
an ownership/team boundary. Continue semantic duplicate filtering against the full
reconciled album before deepening any lead. Preserve current-data-masked, API-only and
heuristic-policy wrinkles as controls instead of manufacturing KI#812.

END C031-WIP218


===============================================================================
CHECKPOINT C031-WIP219 -- QUEUE 004 CVPLAYER.CPP -- F489 / KI#812 UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
No repository source files modified.

POST-WIP218 LOW-COVERAGE / CROSS-CLASS CONTROLS
------------------------------------------------
The continuation deliberately kept F489 unused while challenging under-covered
CvPlayer helpers whose state meaning crosses into CvCity, CvTeam, diplomacy,
religion/corporation, production or espionage code. The point of this tranche was to
settle plausible state contradictions rather than manufacture another numbered root.

1. `canChangeReligion()` DOES NOT NEED TO CONSIDER `NO_RELIGION` UNDER ITS ACTUAL CALLERS
---------------------------------------------------------------------------------------
`CvPlayer::canConvert(NO_RELIGION)` is a separately representable query, so the fact
that `canChangeReligion()` loops only real `ReligionTypes` initially looked like a
possible UI lockout when renouncing the current state religion was the only legal
change.

The caller contract closes that concern. `BUTTONPOPUP_CHANGERELIGION` is not a general
state-religion selection dialog; it is the concrete "a religion has spread / convert to
this religion?" popup, whose launcher requires a real ReligionTypes payload and rejects
`NO_RELIGION`. The AI religion chooser similarly uses `canChangeReligion()` to ask
whether another concrete religion can be adopted. Under those consumers, omitting the
sentinel is intentional domain restriction rather than missing legality.

2. `setEndorsedResolution` UNUSED PAYLOAD IS BENIGN
---------------------------------------------------
`CvPlayer::setEndorsedResolution(VoteSourceTypes, VoteSelectionSubData const&)` does
not inspect the resolution payload. Its live caller invokes it only after a non-team
resolution has passed and only for a voting member that endorsed the resolution. The
function's purpose is to restore that player's `LoyalMember` state for the vote source;
the specific passed resolution payload is irrelevant to that loyalty transition. No
wrong-resolution or stale vote state was established.

3. `verifyUnitStacksValid()` DECLARER-SIDE SCOPE IS SUFFICIENT
-------------------------------------------------------------
On war declaration the engine first runs the global `verifyUnitValidPlot()` legality
repair and then invokes the declaring player's `verifyUnitStacksValid()`. A stack repair
jumps a unit when its plot now contains a visible enemy unit. Once one side of an
illegal post-DOW co-location is displaced, the shared stack is legal; repeating the
same pass for the target player is not required. No asymmetric persistent unit-state
failure survived the call-order trace.

4. PLAYER->CITY MODIFIER INVALIDATION CLUSTER CLOSES
----------------------------------------------------
Several small player mutators were challenged for missing city-side cache refreshes.
Current implementations and consumers remain coherent:

* `changeStateReligionHappiness` calls `updateReligionHappiness`;
* `changeCoastalTradeRoutes` calls `updateTradeRoutes`;
* commerce-rate modifier changes call `updateCommerce` and mark assignment state dirty;
* BuildingOnlyHealthy / FreeSpecialist-style values are read dynamically by city
  calculations and mark citizen assignment state dirty where needed;
* `changeFreeBuildingCount` already updates every city on the relevant 0<->positive
  availability transition.

No current player modifier was found whose shipped gain/loss changes an authoritative
cached city result without the required refresh.

5. RECYCLED `m_abEverSeenDemographics` STATE IS ALREADY SCRUBBED
---------------------------------------------------------------
A new-colony/player-slot reuse initially looked capable of inheriting another
civilization's old `m_abEverSeenDemographics[PlayerTypes]` intelligence bit. Full album
reconciliation caught this before promotion. The recycled-player reset path explicitly
clears surviving players' GPT and demographics entries indexed by the reused PlayerTypes
slot and also clears espionage spending weights indexed by the recycled TeamTypes slot.
This is already part of the recycled-player cleanup controls; no new historical-ID root.

6. `changeSpecialBuildingNotRequiredCount` DOES NOT ALLOW ILLEGAL QUEUED PRODUCTION
----------------------------------------------------------------------------------
Current `CIVIC_ORGANIZED_RELIGION` makes `SPECIALBUILDING_MONASTERY` not required, and
current Missionary units use Monastery-class prerequisite buildings. The player mutator
itself does not immediately run `verifyProduction`, so switching away from Organized
Religion can leave a now-illegal Missionary order visible in the queue for the rest of
the immediate transaction.

That does NOT permit authoritative production to continue. Every city `doTurn()` calls
`doCheckProduction()` before `doProduction()`. The production check reaches
`checkCanContinueProduction` / `canContinueProduction` / `canTrain(..., bContinue=true)`,
where the building prerequisite is enforced again under the player's CURRENT exemption
state. The invalid order is therefore removed before receiving another hammer. At most
there is a short in-active-turn UI queue lag; no production-legality bypass earns F489.

7. `adjustMissionCostToTeamSize` DEAD-MEMBER COUNT IS CONSISTENT WITH ITS EXPLICIT TECH-COST CONTRACT
------------------------------------------------------------------------------------------------------
`CvPlayer::adjustMissionCostToTeamSize` uses persistent `CvTeam::getNumMembers()` for
both acting and target team sizes. Since player death does not decrement this membership
count, a defeated teammate continues affecting the espionage team-size adjustment.

This initially resembled the alive-vs-persistent team-member failures already found in
maintenance and scoring. Here, however, the source explicitly says:

    // Tie it to the tech cost modifier

and uses `TECH_COST_EXTRA_TEAM_MEMBER_MODIFIER` in its square-root ratio. The
referenced authoritative `CvTeam::getResearchCost(..., bTeamSizeModifiers=true)` uses
that SAME define multiplied by:

    getNumMembers() - 1

rather than alive-member count. Therefore a defeated teammate continues affecting the
team's technology cost and the espionage normalization coherently. Replacing only the
espionage side with an alive-member count would break the stated symmetry. No F489 from
this persistent-membership behavior.

8. HOLY-CITY / CORPORATION-HQ COUNTERS ARE DYNAMIC CURRENT-OWNER QUERIES
------------------------------------------------------------------------
`countHolyCities()` and `countHeadquarters()` contain no cached per-player ownership
state. Each iteration asks the game for the CURRENT holy-city/headquarters city pointer
and compares that city's CURRENT owner with `getID()`. Conquest therefore changes the
result automatically; dead/recycled historical owners are not consulted. AI consumers
receive current ownership without an invalidation requirement.

9. `getAveragePopulation()` CLOSES FOR THE AUTHORITATIVE STEAL-TREASURY CONSUMER
--------------------------------------------------------------------------------
`getAveragePopulation()` returns zero only when the player has no cities; otherwise it
returns the rounded total-population / city-count average introduced by `advc.131`.
`getEspionageGoldQuantity`, the important gameplay consumer, requires a concrete city
whose owner is the target player before using the target's average population. Thus the
cityless zero case cannot enter the Steal Treasury denominator. The remaining AI callers
use the value as an intentionally rounded empire statistic. No division or wrong-state
root established.

10. `isNoResearchAvailable()` MEANS CURRENT LEGAL TECH EXHAUSTION UNDER SHIPPED RULES
-------------------------------------------------------------------------------------
The helper first returns false if there is already current research, then scans all
technologies and returns false if any `canResearch` succeeds. Its important consumers
are K-Mod/AdvC AI commerce/science decisions plus SAS GameRecord diagnostics.

A potential concern was that a temporary player state could make every `canResearch`
query false for one turn and cause the AI to treat research as permanently exhausted.
Current `canResearch` does not impose anarchy or another ordinary short-lived global
research-disable state; it tests actual technology ownership/prerequisites/options and
related eligibility. Current Python research override callbacks are disabled. Under
shipped gameplay, therefore, `isNoResearchAvailable` answers the question its AI callers
need: whether any technology is presently legally researchable. No transient-lockout
root found.

ADDITIONAL PREVIOUSLY-OWNED / MASKED CONTROLS ENCOUNTERED
---------------------------------------------------------
* `isSpeedBonusAvailable` resource/network shape remains data-masked because current
  shipped UnitInfos contain no SpeedBonus entries; already preserved in earlier album
  controls.
* `getRealPopulation` still has the previously noted large-empire INT_MAX saturation,
  but no strong authoritative gameplay consequence has been established.
* `changeLuxuryModifier` remains generically non-invertible at its storage clamp, but
  current shipped civic data cannot approach the clamp; robustness debt only.
* `updateCommerceRates` repeated-largest-remainder redistribution is exactly
  F444/KI#767 and was rejected immediately as a duplicate when rediscovered.

CURRENT STONE AFTER C031-WIP219
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
Latest physical checkpoint: C031-WIP219.
No repository source files modified.

NEXT
----
Continue from F489 unused through mechanically ranked low-coverage authoritative
CvPlayer state. Prefer persistent state whose meaning can change independently of its
stored value: owner/team identity, one-shot historical flags, production/religion/
corporation transitions, and cross-class cached state. Duplicate-check every strong
lead against the full reconciled album before promotion. Preserve policy, dormant XML,
API-only and extreme-range concerns as controls unless a current shipped producer and
concrete gameplay consequence are established.

END C031-WIP219


===============================================================================
CHECKPOINT C031-WIP220 -- QUEUE 004 CVPLAYER.CPP -- F489 / KI#812 UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
No repository source files modified.

POST-WIP219 HISTORICAL / IDENTITY / ECONOMIC CONTROLS
-----------------------------------------------------

1. PLAYER OPTIONS ARE NOT LOST ON SUPPORTED HUMAN-SLOT TRANSFERS
---------------------------------------------------------------
`CvPlayer::reset` clears `m_abOptions`, which initially looked dangerous because player
options are user preferences rather than civilization-history state. The supported
lifecycle closes the concern:

* recycled colony slots are AI-controlled, so clearing their player options is harmless;
* when human control is actually moved between player slots, `CvGame::changeHumanPlayer`
  explicitly copies every `PlayerOptionTypes` value from the old human to the new human;
* the displaced side is restored from the profile/default option state as appropriate.

Thus reset/recycling does not silently erase a human user's active player-option state.

2. `setFoundedFirstCity(false)` ON LAST-CITY DISBAND IS DELIBERATE INHERITED SEMANTICS
------------------------------------------------------------------------------------
`CvPlayer::disband(CvCity&)` contains:

    if (getNumCities() == 1)
        setFoundedFirstCity(false);

and `verifyAlive()` later allows a zero-city player with units to survive only while
`isFoundedFirstCity()==false`. Read literally as historical wording, this looked like a
player that had already founded a city could return to a never-settled state and evade
normal cityless elimination.

The full producer/ancestry trace closes it as intentional Firaxis behavior:

* the exact reset is already present in BtS/Civ4CE and retained by K-Mod, Base AdvCiv and
  SAS;
* ordinary `TASK_DISBAND` is wired through `BUTTONPOPUP_DISBANDCITY`;
* that popup is produced by the city-acquisition path for a human when the acquired city
  is neither handled as an ordinary conquest-raze choice nor a normal trade-kept city;
* the player is being allowed to decline/abandon that involuntarily acquired city.

Resetting FoundedFirstCity when that temporary acquisition was the player's sole city
therefore prevents mere temporary receipt-and-rejection of a city from permanently
changing the player's settlement/elimination status. This is inherited rules semantics,
not F489.

3. CURRENT CITY-NAME ALLOCATION IS CONSISTENT WITH THE KI#325 CANONICAL-IDENTITY REPAIR
--------------------------------------------------------------------------------------
The zero-coverage `getNewCityName`, `getCivilizationCityName` and `isCityNameValid`
family was challenged because SAS recently repaired a language-dependent past-city-name
identity bug.

Current code now uses the same stable representation throughout:

* explicit/civilization candidate selection passes the stored city-name KEY to
  `isCityNameValid`;
* `isCityNameValid` compares both the stable key (`getNameKey`) and current rendered
  display text (`getName`) so translated stock names and literal/manual names cannot
  collide visibly;
* when a city is disbanded, the past-name registry receives the raw canonical key rather
  than the translated display string.

The allocator therefore does not reopen KI#325 through a different naming path.

4. `getUnitCostMultiplier` / NORMAL UNIT-COST SCALING CLOSES
-----------------------------------------------------------
The under-covered K-Mod helper composes:

* player handicap UnitCostPercent;
* AI handicap AIUnitCostPercent for nonhuman nonbarbarian players;
* game-speed UnitCostPercent;
* world-size UnitCostPercent.

`calculateUnitCost` then applies that multiplier only to the ordinary paid-unit component;
military-unit cost and ExtraUnitCost are deliberately separate K-Mod/AdvC components.
No player/team identity transition or cached dependent value exists here, and current
supported map/player/unit/data bounds do not create a practical signed-int overflow in
this path. No F489 arithmetic or invalidation root established.

CURRENT STONE AFTER C031-WIP220
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
Latest physical checkpoint: C031-WIP220.
No repository source files modified.

NEXT
----
Continue F489 unused through the remaining substantial low-coverage functions, starting
with `getNextGroupInCycle`, Advanced Start city/pop-cost helpers and project-max/creation
checks. Keep prioritizing authoritative gameplay state over UI-only helpers; reconcile
against earlier Advanced Start and group-cycle findings before promotion.

END C031-WIP220


===============================================================================
CHECKPOINT C031-WIP221 -- QUEUE 004 CVPLAYER.CPP -- F489 / KI#812 UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
No repository source files modified.

POST-WIP220 GROUP-CYCLE / ADVANCED-START CONTROLS
-------------------------------------------------

1. `getNextGroupInCycle` RAW GROUP-ID DEREFERENCE RELIES ON AN ALREADY-PROVED INVARIANT
--------------------------------------------------------------------------------------
The larger selection-cycle helper dereferences `getSelectionGroup(groupCycleID)` without
an immediate NULL guard. This is not a fresh lifetime defect. Earlier Queue-004 lifecycle
tracing established the same invariant for `updateGroupCycle`:

* `CvSelectionGroup::kill` removes the group's cycle ID before deleting the selection
  group object;
* player teardown clears the cycle list before destroying/uninitializing the group
  container;
* save loading restores the serialized cycle IDs and selection-group array together
  without an intervening cycle traversal.

The helper's wrap behavior is also deliberate. The first pass excludes groups recorded
as already cycled; after returning to the first node it sets `bWrap=true`, allowing a
second pass to return an otherwise-ready previously-cycled group instead of prematurely
returning NULL. No independent stale-ID, wraparound or wrong-group root established.

2. ADVANCED START POPULATION ADD/REMOVE PRICING IS TRANSACTIONALLY SYMMETRIC
---------------------------------------------------------------------------
`getAdvancedStartPopCost` initially looks asymmetric because the removal path decrements
`iPopulation` before calculating the growth threshold, and the cost-increase block then
decrements it once more for both add and remove.

The two directions reconcile exactly for the same edge:

* adding population 2 -> 3 starts with `iPopulation=2`; growth cost uses threshold(2),
  then cost-increase tier uses 1;
* removing population 3 -> 2 first decrements 3 -> 2; growth cost again uses
  threshold(2), then cost-increase tier again uses 1.

The minimum-population removal guard additionally prevents refunding the free starting
population. The action path uses the same helper, so no add/refund arbitrage or wrong-tier
root survives.

3. ADVANCED START CITY REMOVAL RANGE RECHECK DOES NOT CREATE A CURRENT TRAP
--------------------------------------------------------------------------
`getAdvancedStartCityCost(false, pPlot)` applies the same configured starting-plot range
block used for additions, even though a removal does not logically need to establish a
new valid placement. In isolation this is broader validation than necessary.

Under the ordinary supported Advanced Start lifecycle, however, every city that can be
removed was itself purchased while the same setup phase and same starting plots were in
force and therefore already passed that range rule. The player's starting plot does not
normally migrate during Advanced Start. No current supported producer was found that
creates a player's own Advanced Start city legally and then changes only the range
identity so that the city becomes impossible to refund/remove. Preserve as possible API/
robustness simplification, not F489.

The existing Advanced Start numbered roots (route legality, unit/route removal fallback,
AI purchase-accounting and affordability/control-flow issues, palette/cap mismatches)
remain the authoritative defects; no duplicate was created here.

CURRENT STONE AFTER C031-WIP221
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F488 / KI#811 CONFIRMED.
F489 / provisional KI#812 UNUSED.
Latest physical checkpoint: C031-WIP221.
No repository source files modified.

NEXT
----
Continue F489 unused with `isProductionMaxedProject` / project creation-limit state and
remaining substantial low-coverage helpers, then return to persistent one-shot/player-
identity fields. Preserve the same current-producer + concrete-consequence + duplicate
bar.

END C031-WIP221


===============================================================================
CHECKPOINT C031-WIP222 -- QUEUE 004 CVPLAYER.CPP -- F489 / KI#812 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F489 / KI#812 CONFIRMED.
F490 / provisional KI#813 UNUSED.
No repository source files modified.

POST-WIP221 PROJECT-LIMIT CONTROL -- PRIOR CLOSURE RECONFIRMED
-------------------------------------------------------------
The requested `isProductionMaxedProject` / project creation-limit tranche did not produce
F489.

`CvPlayer::canCreate(ProjectTypes,...)` and `CvPlayer::isProductionMaxedProject` answer
intentionally different questions:

* `canCreate` is queue legality. It includes current team/global project-making counts so
  two cities cannot legally queue past a finite project limit;
* `isProductionMaxedProject` asks whether the project is already completed-maxed. The
  principal `CvCity::doCheckProduction` consumer uses that narrower fact to discard
  obsolete stored production / resolve fail-gold state after somebody actually completes
  the last allowed instance.

A stronger duplicate check then found that the full reconciled album had already closed
this exact family in C031-WIP164:

    getBestEventTech, getDiscoveryTech, getEspionageSpending, foundCorporation,
    isProductionMaxedProject, capital commerce/yield modifier mutators, ...
    all reconciled with their authoritative consumers/current data.

Do not reopen that prior clean result merely because the exact helper name had low recent
coverage.


F489 / KI#812 -- A 2020 `getResearchLeft` SAFETY REFACTOR MAKES ADVC'S
POST-ACQUISITION `isSignificantDiscovery` TEST ALWAYS FALSE FOR NON-REPEAT TECHS,
SO SIGNIFICANT STOLEN / GOODY / TECH-SHARE DISCOVERIES BYPASS NO TECH BROKERING
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Current shipped gameplay reaches the defect through at least Steal Technology,
full technology Tribal Village rewards and The Internet / Tech Share. The underlying
contract break is inherited from Base AdvCiv: the `advc.550e` significant-discovery
callers were meaningful when written, but practical 2082 later changed
`CvTeam::getResearchLeft` so every already-known non-repeat technology reports zero.
The normal technology-trade path survives because it snapshots significance BEFORE
`setHasTech`; the affected non-trade acquisition paths perform the same classification
AFTER `setHasTech`.

ROOT CONTRACT -- `isSignificantDiscovery` REQUIRES PRE-ACQUISITION RESEARCH STATE
--------------------------------------------------------------------------------
Current `CvPlayer::isSignificantDiscovery` is:

    // advc.550e:
    bool CvPlayer::isSignificantDiscovery(TechTypes eTech) const
    {
        /* ... Only adjust tech_from_any memory if this is a tech from a recent era
           and the team receiving the tech isn't already more than 2/3 of the way
           through. ... */
        return (GC.getInfo(eTech).getEra() >= getCurrentEra() - 1 &&
                GET_TEAM(getTeam()).getResearchLeft(eTech) >
                GET_TEAM(getTeam()).getResearchCost(eTech) / 3);
    }

The two conditions therefore mean:

1. the technology is recent relative to the recipient; and
2. the recipient still has more than one third of its research cost left, i.e. had NOT
   already completed at least two thirds of the research itself.

That second condition needs the recipient team's progress from BEFORE an external full
acquisition changes the technology to known.

CURRENT `getResearchLeft` DESTROYS THAT INFORMATION FOR A KNOWN NON-REPEAT TECH
------------------------------------------------------------------------------
Current `CvTeam::getResearchLeft` contains an AdvCiv safety shortcut:

    int CvTeam::getResearchLeft(TechTypes eTech) const
    {
        // advc: Safer, cleaner this way. (NB: NO_TECH isn't allowed here.)
        if (isHasTech(eTech) && !GC.getInfo(eTech).isRepeat())
            return 0;
        return std::max(0, getResearchCost(eTech) - getResearchProgress(eTech));
    }

Therefore for every ordinary non-repeat technology:

    setHasTech(eTech, true, ...);
    isSignificantDiscovery(eTech);

necessarily evaluates its second condition as:

    0 > researchCost / 3

which is false for every current positive-cost technology.

This is independent of how much research the recipient had actually invested before
acquisition. Zero progress, 10% progress and 99% progress all collapse to the same zero
`getResearchLeft` result once `isHasTech` becomes true.

CONTROL -- `setHasTech` ITSELF DOES NOT OVERWRITE NON-REPEAT RESEARCH PROGRESS
-----------------------------------------------------------------------------
This matters for ancestry. For non-repeat technologies, `CvTeam::setHasTech` sets the
known-tech bit and processes the technology but does not rewrite the stored research
progress. Before the 2020 `getResearchLeft` shortcut, a post-`setHasTech` call could still
observe:

    researchCost - the recipient's pre-acquisition researchProgress

and therefore the old post-acquisition `isSignificantDiscovery` callers worked despite
their apparently odd ordering.

Repeat technologies are separately handled and reset their research progress, but they
are outside the ordinary No Tech Brokering manifestations here.

CORRECT CONTROL -- NORMAL TECH TRADE SNAPSHOTS SIGNIFICANCE BEFORE ACQUISITION
-----------------------------------------------------------------------------
Current `CvDeal::startTrade` does the safe thing:

    bool const bSignificantTech = kToPlayer.isSignificantDiscovery(
            (TechTypes)trade.m_iData);
    GET_TEAM(eToPlayer).setHasTech((TechTypes)trade.m_iData, true, eToPlayer,
            true, true, false, TECH_ACQUISITION_TRADE);
    if (bSignificantTech)
        GET_TEAM(eToPlayer).setNoTradeTech((TechTypes)trade.m_iData, true);

The classifier runs while the recipient still does NOT know the technology, so
`getResearchLeft` still exposes the intended remaining-research quantity. The result is
saved across `setHasTech` and then used for No Tech Brokering state and AI memories.

This is a particularly strong local intent control: the same helper is known to be valid
when called before acquisition, and the one working acquisition path already preserves
exactly that ordering.

MANIFESTATION A -- STEAL TECHNOLOGY ALWAYS MISCLASSIFIES A FULL THEFT AS INSIGNIFICANT
-------------------------------------------------------------------------------------
Current `CvPlayer::doEspionageMission` executes the enabled Steal Technology mission as:

    GET_TEAM(getTeam()).setHasTech(eTech, true, getID(), false, true, false,
            TECH_ACQUISITION_ESPIONAGE);
    if (isSignificantDiscovery(eTech)) // advc.550e
        GET_TEAM(getTeam()).setNoTradeTech(eTech, true);

The order is opposite the working `CvDeal` control.

For any stolen non-repeat technology:

1. the spying team becomes the technology owner;
2. `isSignificantDiscovery` immediately calls current `getResearchLeft`;
3. `getResearchLeft` sees `isHasTech==true` and returns 0;
4. the significant-discovery condition is false;
5. `setNoTradeTech(eTech,true)` is skipped.

Current XML makes this ordinary shipped gameplay:

    ESPIONAGEMISSION_BUY_TECH
        iBuyTechCostFactor = 50

No dormant XML or Python-only producer is required.

DETERMINISTIC STEAL-TECH REPRODUCTION
-------------------------------------
Use a normal game with technology trading available and `GAMEOPTION_NO_TECH_BROKERING`
enabled.

1. Player A and Player B are ordinary major civilizations; B knows a recent tradeable
   non-repeat technology T that A can steal.
2. A has no research progress in T. Choose T from A's current or immediately preceding
   era, so BEFORE theft:

       A researchLeft(T) == researchCost(T)
       isSignificantDiscovery(T) == true.

   Current Industrial `TECH_STEAM_POWER`, for example, is `bTrade=1`; any currently legal
   same-era steal fixture with an eligible third party is sufficient.
3. Give A enough espionage points and execute Steal Technology for T against B.
4. The mission calls `setHasTech(T,true,...)` first.
5. The following significance test sees `researchLeft(T)==0`, so A's team never receives
   `NoTradeTech(T)`.
6. Let third-party Player C not know T but satisfy `canResearch(T,true)` and ordinary tech
   trading prerequisites.
7. A's `canTradeItem(C, TRADE_TECHNOLOGIES/T)` can now accept T because the live gate is:

       isHasTech(T) &&
       !isNoTradeTech(T) &&
       !Cteam.isHasTech(T) &&
       C.canResearch(T,true).

The significant stolen technology can therefore be brokered even though `advc.550e`
explicitly intended significant stolen discoveries to remain subject to the brokering
restriction.

MANIFESTATION B -- FULL TECHNOLOGY TRIBAL-VILLAGE REWARD HAS THE SAME ORDERING
-----------------------------------------------------------------------------
Current `CvPlayer::receiveGoody` has a full-discovery branch:

    kOurTeam.setHasTech(eBestTech, true, getID(), true, true, false,
            TECH_ACQUISITION_GOODY);
    if (isSignificantDiscovery(eBestTech))
        kOurTeam.setNoTradeTech(eBestTech, true);

Again the classifier runs after the non-repeat technology is known and can never pass its
remaining-research test.

This is reachable current state, not a hypothetical GoodyInfo combination. Current
Tribal Village technology rewards are live, and the existing Queue-004 goody investigation
already established current full-technology acquisition paths. F481/KI#804 concerns a
very different one-time first-free-unit reward after such an acquisition; F489 concerns
the provenance / brokering flag on the acquired technology itself.

MANIFESTATION C -- THE INTERNET / TECH SHARE ALSO TESTS ONLY AFTER `setHasTech`
-----------------------------------------------------------------------------
Current `CvTeam::updateTechShare(TechTypes eTech)` reaches:

    setHasTech(eTech, true, NO_PLAYER, true, true, false,
            TECH_ACQUISITION_TECH_SHARE);
    if (GET_PLAYER(getLeaderID()).isSignificantDiscovery(eTech)) // advc.550e
        setNoTradeTech(eTech, true); // kekm.31

The leader belongs to the very team that `setHasTech` has just changed. Current
`getResearchLeft` therefore returns 0 and the `NoTradeTech` assignment is unreachable
for ordinary non-repeat Tech Share acquisitions.

This feature is live in current data:

    PROJECT_THE_INTERNET
        iTechShare = 2

Thus a team that obtains a recent technology through The Internet while having less than
two thirds of the research completed can still broker it under No Tech Brokering.

COLONIAL-INHERITANCE SIBLING -- THE SAME REFACTOR ALSO COLLAPSES THE STORED-PROGRESS
DISTINCTION USED BY `splitEmpire`
------------------------------------------------------------------------------------
Current fresh-colony technology inheritance contains:

    kNewTeam.setHasTech(eLoopTech, true, eNewPlayer, false, false, false,
            TECH_ACQUISITION_INHERITANCE);
    if (GET_TEAM(getTeam()).isNoTradeTech(eLoopTech) ||
        (kGame.isOption(GAMEOPTION_NO_TECH_BROKERING) &&
         isSignificantDiscovery(eLoopTech)))
    {
        kNewTeam.setNoTradeTech(eLoopTech, true);
    }

Here `isSignificantDiscovery` is evaluated on the PARENT player (`this`). The parent team
already knows every inherited technology by definition. Before the 2020 refactor,
`getResearchLeft` still exposed the parent's stored progress for a known technology, so
this code could distinguish a tech the parent had substantially researched from a tech
acquired with little own progress. After practical 2082 every known non-repeat parent
technology reports zero research left, so the second clause can never fire.

The first clause still correctly propagates an already-existing parent `NoTradeTech`
flag. This means colony inheritance is partly protected when the parent provenance was
already recorded correctly. It does NOT rescue technologies whose significant external
acquisition has itself missed the flag through the Steal / Goody / Tech Share manifestations
above, and it also destroys the explicit stored-progress distinction that `advc.550e`
placed in the colony path. Keep these as manifestations of the same contract break rather
than manufacturing a second root number.

AUTHORITATIVE CONSEQUENCE -- `NoTradeTech` DIRECTLY GATES TECHNOLOGY TRADE
-------------------------------------------------------------------------
Current `CvPlayer::canTradeItem` handles a technology as:

    if (GC.getInfo(eTech).isTrade() && kOurTeam.isHasTech(eTech) &&
        !kOurTeam.isNoTradeTech(eTech) && !kToTeam.isHasTech(eTech) &&
        GET_PLAYER(eWhoTo).canResearch(eTech, true))
    {
        bValid = true;
    }

So the missing flag changes the actual diplomacy/trade legality of the technology. It is
not UI metadata, an AI valuation hint or a logging-only provenance field.

The state is also durable while No Tech Brokering is enabled. `CvTeam::doTurn` clears
all `NoTradeTech` flags only when the option is NOT enabled:

    if (!GC.getGame().isOption(GAMEOPTION_NO_TECH_BROKERING))
    {
        FOR_EACH_ENUM(Tech)
            setNoTradeTech(eLoopTech, false);
    }

With the option enabled, an acquisition that incorrectly misses the flag remains
brokerable until some other explicit state change occurs.

CURRENT OPTION / DATA REACHABILITY
----------------------------------
`GAMEOPTION_NO_TECH_BROKERING` is a normal visible Custom Game option in current
`CIV4GameOptionInfos.xml`:

    bDefault = 0
    bVisible = 1

This is therefore a supported rules combination. The three primary producers above are
also all current:

* Steal Technology: enabled espionage mission, `iBuyTechCostFactor=50`;
* Tribal Village technology: current GoodyTech/full-acquisition path;
* The Internet: current project with `iTechShare=2`.

No malformed XML, WorldBuilder, Python API or unsupported old save is needed.

HISTORICAL INTENT -- ADVC EXPLICITLY WANTED ONLY *INSIGNIFICANT* EXTERNAL DISCOVERIES
TO ESCAPE BROKERING RESTRICTIONS
-------------------------------------------------------------------------------------
The reachable Base AdvCiv history is unusually explicit.

Practical 1481, commit:

    4a65bf5c0fb187fd25d4908346abb462354a9172
    2019-04-22

states in its commit message:

    Insignificant discoveries (2 eras behind or already had 2/3 progress) are not
    subject to tech brokering restrictions. This had already been the case for tech
    trade and now also applies to stolen tech, the Internet and tech shared upon the
    creation of a colonial vassal. [advc.550e]

That commit changes the then-unconditional stolen-tech and Internet `setNoTradeTech`
behavior into the `isSignificantDiscovery` gates shown above. At that time,
`CvTeam::getResearchLeft` was still simply:

    max(0, researchCost - researchProgress)

for a known or unknown ordinary tech. Therefore the post-`setHasTech` significance tests
still observed the recipient's pre-acquisition stored progress and functioned as intended.

The bug is consequently NOT that `advc.550e` chose the wrong threshold or that every
externally acquired technology should always be non-brokerable. The intended AdvCiv rule
is specifically:

* insignificant external discovery -> may escape brokering restriction;
* significant external discovery -> receives `NoTradeTech`.

Current code accidentally turns the affected acquisition families into:

* every already-known non-repeat technology -> `researchLeft==0` -> insignificant.

PINPOINT REGRESSION -- PRACTICAL 2082 CHANGED THE CALLEE CONTRACT WITHOUT UPDATING THE
POST-ACQUISITION CALLERS
-------------------------------------------------------------------------------------
The exact break appears in Base AdvCiv practical 2082:

    commit 5a6bff0b8fbdb037f0c875aab7d3d6ed61beba09
    2020-04-01 18:00:48 +0200
    "Some misc. refactoring"

Its commit message includes:

    Cleaner/ safer handling of research progress checks for techs that have already
    been discovered.

and changes `CvTeam::getResearchLeft` from:

    return max(0, getResearchCost(eTech) - getResearchProgress(eTech));

to:

    if (isHasTech(eTech))
        return 0;
    return max(0, getResearchCost(eTech) - getResearchProgress(eTech));

Later practical 2088 merely refines the guard to exempt repeat technologies:

    if (isHasTech(eTech) && !GC.getInfo(eTech).isRepeat())
        return 0;

No corresponding update moves the Goody, Espionage, Tech Share or colony significance
checks before acquisition / onto an explicit pre-acquisition snapshot.

This makes F489 a classic callee-contract refactor regression: the new `getResearchLeft`
semantics are locally reasonable for normal callers asking "how much is left now?", but
several existing `advc.550e` callers intentionally needed the historical pre-acquisition
progress of a technology immediately after it became known.

ANCESTRY / CONTROLS
-------------------
BtS/Civ4CE and Karadoc K-Mod do NOT contain AdvCiv's known-tech-zero shortcut in
`getResearchLeft`; they return:

    max(0, researchCost - researchProgress)

regardless of the known-tech bit.

Their inherited direct acquisition behavior also provides useful controls:

* BtS/Civ4CE and K-Mod full Goody technology acquisition mark the tech `NoTradeTech`
  unconditionally;
* BtS/Civ4CE and K-Mod Steal Technology mark the stolen tech `NoTradeTech`
  unconditionally;
* BtS/Civ4CE and K-Mod colony inheritance under No Tech Brokering marks inherited
  technologies `NoTradeTech` without AdvCiv's significant-discovery exemption.

AdvCiv intentionally relaxed those restrictions for insignificant discoveries under
`advc.550e`, then practical 2082 accidentally made the affected post-acquisition tests
incapable of recognizing any significant ordinary technology.

The Internet lineage is slightly different: the no-brokering treatment came through a
Kek-Mod update merged by AdvCiv, then `advc.550e` made it conditional on significance.
The current AdvCiv/SAS regression is still the same 2020 `getResearchLeft` contract
break, not a stock BtS Internet rule.

SAS PROVENANCE
--------------
Current SAS retains Base AdvCiv's significant-discovery helper, the post-acquisition
caller ordering and the known-tech-zero `getResearchLeft` rule.

SAS has since added `TechAcquisitionCause` arguments such as:

    TECH_ACQUISITION_GOODY
    TECH_ACQUISITION_ESPIONAGE
    TECH_ACQUISITION_TECH_SHARE
    TECH_ACQUISITION_INHERITANCE

for SASGameRecord provenance. Those logging/provenance arguments do not change the
ordering or the `NoTradeTech` decision. F489 is therefore inherited Base AdvCiv, not a
SAS introduction.

DUPLICATE / ALBUM CHECK
-----------------------
The full reconciled C031-WIP209 album was searched explicitly for:

* `isSignificantDiscovery`;
* `No Tech Brokering` / brokering;
* `setNoTradeTech` / no-trade-tech;
* the known-tech `getResearchLeft` shortcut;
* the current technology-acquisition provenance names.

No prior numbered root owns this caller/callee contract regression.

Nearby findings remain distinct:

* F447 / KI#770: malformed recycled-colony path skips `addPlayer/initInGame` and therefore
  skips technology/no-trade inheritance wholesale. F489 occurs in ordinary acquisition
  paths that DO execute their intended inheritance/provenance code but classify
  significance after the callee contract changed.
* F481 / KI#804: a cityless first discoverer can lose a first-free-unit technology
  reward after a Goody completion. It does not concern `NoTradeTech`.
* F484 / KI#807: Steal Technology's generic affordability helper can hide the human
  mission before a specific affordable tech is selected. F489 begins after a Steal Tech
  mission has actually succeeded and concerns the acquired tech's brokering state.
* Tech Share / Internet roots elsewhere in the album concern counting/recipient/contact
  behavior, not significant-discovery provenance after `setHasTech`.

The duplicate boundary is therefore clean.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do NOT "fix" F489 by removing the known-tech zero guard from `CvTeam::getResearchLeft`.
That 2020 behavior is a broad current API contract used by many normal callers and is
locally sensible: a non-repeat technology already owned by the team has zero research
left.

Instead preserve `isSignificantDiscovery`'s required PRE-ACQUISITION state at its
callers.

For Goody, Espionage and Tech Share, the narrow pattern is:

    bool const bSignificant = isSignificantDiscovery(eTech); // while still unknown
    setHasTech(eTech, true, ...);
    if (bSignificant)
        setNoTradeTech(eTech, true);

This is exactly the architecture already used by `CvDeal::startTrade`.

For `CvTeam::updateTechShare`, evaluate through the eventual recipient/leader before
`setHasTech` and carry the bool across the mutation.

`splitEmpire` deserves a deliberately separate review of intended `advc.550e` subject
semantics before changing it mechanically. The old code intentionally evaluated the
parent's stored progress while the parent already knew the tech; practical 2082 erased
that information. A repair can either:

* snapshot/derive the intended parent-side progress significance without relying on
  `getResearchLeft`'s current known-tech API; or
* redefine colonial significance explicitly for the new team if that better matches the
  desired present-day rule.

Do not silently switch subject from parent to colony without deciding that policy; the
historical `advc.550e` code used the parent on purpose or at least consistently from its
introduction.

REGRESSION TESTS
----------------
A. STEAL TECHNOLOGY / PRIMARY TEST

1. Enable No Tech Brokering and normal Tech Trading.
2. A has 0 progress in recent tradeable non-repeat tech T; B knows T; C can research T.
3. Assert before theft:

       A.isSignificantDiscovery(T) == true.

4. A steals T from B.
5. Assert immediately:

       Ateam.isHasTech(T) == true
       Ateam.isNoTradeTech(T) == true.

6. Assert A cannot offer T to C under No Tech Brokering.
7. Repeat with A already more than 2/3 through T; that deliberately insignificant theft
   should remain brokerable according to `advc.550e`.

B. GOODY FULL-DISCOVERY TEST

1. Arrange a current GoodyTech T close enough for the full-tech reward branch, but with
   more than one third research left before the reward.
2. Record pre-acquisition significance.
3. Receive the full technology from the Tribal Village.
4. Under No Tech Brokering, assert significant T receives `NoTradeTech`.
5. Repeat with <= one third research left; the `advc.550e` exception should remain.

C. THE INTERNET / TECH SHARE TEST

1. Team A owns current PROJECT_THE_INTERNET (`iTechShare=2`).
2. Arrange for the required met teams to know recent T while A has <2/3 progress.
3. Trigger `updateTechShare(T)`.
4. Assert T becomes known AND `NoTradeTech(T)` becomes true under the significant case.
5. Repeat with A already >=2/3 complete to preserve the intended insignificant exception.

D. NORMAL TRADE NEGATIVE CONTROL

1. Trade recent T normally to a recipient with <2/3 progress.
2. Confirm the existing pre-acquisition `bSignificantTech` snapshot still marks it
   no-trade.
3. Repeat with >=2/3 progress and confirm the intended exception.

E. COLONY INHERITANCE

Add a focused test after deciding the intended parent-vs-colony significance semantics.
At minimum, verify practical-2082's known-tech shortcut no longer causes the second
`splitEmpire` `advc.550e` clause to be dead for every non-repeat inherited tech.

NUMBERING
---------
F489 / KI#812 is CONFIRMED.
F490 / provisional KI#813 is the next unused root.

CURRENT STONE AFTER C031-WIP222
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F489 / KI#812 CONFIRMED.
F490 / provisional KI#813 UNUSED.
Latest physical checkpoint: C031-WIP222.
No repository source files modified.

NEXT
----
Continue from F490 unused with a narrow post-F489 sibling sweep before leaving technology
provenance:

* inspect every `setHasTech` caller for another predicate that intentionally consumes
  pre-acquisition research/provenance AFTER the known-tech mutation;
* inspect `getResearchLeft` callers for any other code that expects stored historical
  progress of already-known non-repeat techs;
* keep consequences caused solely by F489's missed `NoTradeTech` flag under F489 rather
  than multiplying roots;
* then return to the remaining persistent one-shot/player-identity and normal-turn
  low-coverage CvPlayer state.

Require current shipped reachability, concrete authoritative consequence, ancestry and
full-album duplicate proof before F490 is spent.

END C031-WIP222


===============================================================================
CHECKPOINT C031-WIP223 -- QUEUE 004 CVPLAYER.CPP -- F490 / KI#813 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 UNUSED.
No repository source files modified.

POST-F489 MESSAGE-LIFETIME CONTROLS
------------------------------------
The retained-message subsystem was re-extracted directly from the supplied September-1
SAS source and compared with supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS.

* `showMessageCopies` does not create an earlier independent use-after-free. In
  `postProcessMessages`, the major-message display and HotSeat-missed-message display are
  `if` / `else if`; a doubly-classified message is shown through only one container
  before the common cleanup.
* reset, AI-Auto-Play and turn-activation cleanup all call the same
  `clearMessageCopies()` owner cleanup. They are additional detonation sites for F490,
  not separate ownership producers.
* copied HotSeat INFO/MINOR messages are deliberately constructed with
  `MESSAGE_TYPE_MAJOR_EVENT` for retained display. No authoritative state or independent
  heap-lifetime failure was established from that type substitution.
* the earlier `postProcessMessages` `getCurrentResearch()==NO_TECH` completion-message
  proxy remains a live Turn-Log counting heuristic edge, but not an ownership defect and
  not promoted here.


F490 / KI#813 -- ADVC HOTSEAT MISSED-MESSAGE SUPPORT STORES THE SAME
`CvTalkingHeadMessage*` IN TWO OWNING VECTORS; COMMON CLEANUP DELETES IT TWICE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. Ordinary sequential HotSeat play can produce the exact alias. Base AdvCiv
1.14 and current SAS contain the same ownership logic. Supplied K-Mod 1.46 and
Civ4CE/BtS do not contain the extra retained-message owning vectors.

OWNERSHIP ROOT
--------------
Current SAS `CvPlayer::addMessage` computes:

    bool const bMissedMsg = (kGame.isHotSeat() && isActive() &&
            (eMessage == MESSAGE_TYPE_MINOR_EVENT ||
             eMessage == MESSAGE_TYPE_MAJOR_EVENT ||
             eMessage == MESSAGE_TYPE_INFO));

    if (eMessage == MESSAGE_TYPE_MAJOR_EVENT || bMissedMsg)
    {
        CvTalkingHeadMessage* pCopy = new CvTalkingHeadMessage(...);
        if (eMessage == MESSAGE_TYPE_MAJOR_EVENT)
            m_aMajorMsgs.push_back(pCopy);
        if (bMissedMsg)
            m_aHotSeatMsgs.push_back(pCopy);
    }

For a HotSeat `MESSAGE_TYPE_MAJOR_EVENT` delivered while the recipient is still the
active UI player, both predicates are true. ONE heap allocation is inserted into both:

    m_aMajorMsgs
    m_aHotSeatMsgs

Both vectors are subsequently treated as owners.

DELETION ROOT
-------------
The default cleanup independently clears both vectors:

    clearMessageCopies(&m_aMajorMsgs);
    clearMessageCopies(&m_aHotSeatMsgs);

and each concrete clear does:

    for (...)
        SAFE_DELETE((*pContainer)[i]);
    pContainer->clear();

`SAFE_DELETE` nulls only the element in the vector currently being traversed. The other
vector contains a distinct raw-pointer slot holding the same address. The first owner
therefore deletes the allocation; the second owner later calls delete on that same
allocation again.

This is direct raw-pointer double ownership and double deletion / heap corruption.

ORDINARY HOTSEAT REACHABILITY
-----------------------------
The `isActive()` part of `bMissedMsg` is reachable in the exact local HotSeat window the
workaround is intended to cover.

Current `CvPlayer::isActive()` is simply:

    GC.getInitCore().getActivePlayer() == getID()

Current `CvPlayer::doTurn()` sets:

    if (!kGame.isNetworkMultiPlayer())
        kGame.setInBetweenTurns(true);

when the just-finished local/sequential turn enters between-turn processing.

Current `CvPlayer::setTurnActive(true)` changes `ActivePlayer` for HotSeat/PBEM only when
the NEWLY activated participant is HUMAN:

    if ((kGame.isHotSeat() || kGame.isPbem()) && isHuman() && bDoTurn)
        kGame.setActivePlayer(getID());

Thus in a normal order Human H0 -> AI A -> Human H1:

1. H0 ends its turn; local `doTurn` sets `inBetweenTurns=true`.
2. AI A becomes turn-active.
3. A is not human, so activation does NOT replace the UI ActivePlayer.
4. H0 remains `isActive()==true` while A acts.
5. A generates a global major event addressed to H0.
6. H0's `addMessage` sees HotSeat + active H0 + between-turn processing + MAJOR_EVENT.
7. The early `if (!isInBetweenTurns() && isActive()) return` does not fire.
8. The same `pCopy` enters both owning vectors.

CURRENT ORDINARY PRODUCER
-------------------------
No debug/Python malformed message is required. Current `CvGame::setHolyCity` loops all
major-civilization observers and sends:

    MESSAGE_TYPE_MAJOR_EVENT

for an ordinary announced religion founding. Corporation founding, global project
completion and multiple other current engine paths also use major-event messages.

A controlled ordinary reproduction can therefore arrange H0 -> AI A -> H1 and have A
found a religion during its intervening AI turn. H0 receives the world announcement in
the exact alias-producing HotSeat state.

DISPLAY / CLEANUP CONSEQUENCE
-----------------------------
At H0's next message processing, only one display branch runs:

* if the Turn Log auto-opens, `m_aMajorMsgs` is shown;
* else in HotSeat, `m_aHotSeatMsgs` is shown.

Then `postProcessMessages` unconditionally calls:

    clearMessageCopies();

which independently deletes both owners and double-frees the shared allocation.

The same bad ownership can also reach reset / other common cleanup calls; these are
consequences of the same root.

ANCESTRY / CONTROL
------------------
Direct supplied-source comparison:

* current SAS: contains `m_aMajorMsgs`, `m_aHotSeatMsgs`, the one-allocation/two-push
  logic and independent deleting cleanup;
* Base AdvCiv 1.14: same relevant code;
* K-Mod 1.46: `CvPlayer::addMessage` only pushes the value into the ordinary game-message
  queue; there are no retained raw-pointer vectors;
* Civ4CE/BtS: same stock-style absence of the AdvC retained-copy ownership machinery.

A prior locally traced history branch recorded the introduction as AdvC practical 4171,
commit `8bfa61dfd7fc7f43b47519a34ac893066517907d`, "Work around messages missed at the end
of Hotseat turns". The current classification does not depend on that historical
metadata: the supplied Base-vs-K-Mod/BtS source comparison independently establishes the
AdvC-specific lineage.

DUPLICATE / ALBUM CHECK
-----------------------
The full reconciled WIP209 album was searched for:

* `m_aHotSeatMsgs`;
* `m_aMajorMsgs`;
* `clearMessageCopies`;
* `double free` / retained HotSeat message ownership.

No existing numbered or latent root owns this raw-pointer alias. The nearby Turn-Log
message-count heuristic is independent and has no heap-ownership component.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Restore an unambiguous ownership invariant.

For a minimal VC2003-era patch, the most local repair is to allocate an INDEPENDENT
`CvTalkingHeadMessage` object for each owning vector when both classifications apply.
Then the existing independent cleanup remains correct.

Alternatives are one canonical owner plus explicit non-owning classification, or a
shared-ownership model compatible with the old toolchain. Do not merely deduplicate raw
addresses inside `clearMessageCopies`; that masks the ownership violation only at one
sink and leaves future show/remove/reset code with ambiguous ownership.

REGRESSION TEST
---------------
1. Sequential HotSeat order Human H0 -> AI A -> Human H1.
2. During A's turn, trigger an ordinary global major event (religion founding is a clean
   current producer).
3. Assert H0 remains the active UI player while A acts.
4. Assert repaired retained-message storage never has the SAME raw address owned by both
   retained vectors.
5. Exercise both display branches: Turn Log auto-open and ordinary HotSeat missed-message
   replay.
6. Run common cleanup and assert no double delete / allocator failure / crash.
7. Verify non-HotSeat major-event retention remains unchanged.

NUMBERING
---------
F490 / KI#813 is CONFIRMED.
F491 / provisional KI#814 is the next unused root.

CURRENT STONE AFTER C031-WIP223
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 UNUSED.
Latest physical checkpoint: C031-WIP223.
No repository source files modified.

NEXT
----
Continue from F491 unused.

First finish the immediate retained-message sibling/lifetime sweep without multiplying
F490, then resume the under-covered authoritative CvPlayer state/lifecycle inventory.
Prioritize persistent counters, one-shot provenance, owner/team transitions and
validate-one-object -> mutate-another patterns. Duplicate-check every lead against the
full WIP209 album before promotion.

END C031-WIP223


===============================================================================
CHECKPOINT C031-WIP224 -- QUEUE 004 CVPLAYER.CPP -- F491 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 remains UNUSED.
No repository source files modified.

POST-F490 RETAINED-MESSAGE / POINTER-LIFETIME SIBLING SWEEP
-----------------------------------------------------------
The immediate ownership family was continued before moving on. No second root survived.

1. `showMessageCopies` does not independently double-consume or remove an aliased message.

`postProcessMessages` chooses the retained-message display with an `if` / `else if`:

    if (auto-open Turn Log)
        showMessageCopies(&m_aMajorMsgs);
    else if (HotSeat)
        showMessageCopies(&m_aHotSeatMsgs);

Only one retained vector is displayed in a given pass. The actual failure remains F490's
one-allocation/two-owning-vectors cleanup. Do not multiply display consequences into
F491.

2. Reset / AI-Auto-Play / turn-transition cleanup are additional F490 sinks, not new
ownership producers.

All of them eventually use the same `clearMessageCopies()` ownership model. A shared raw
address inserted by F490 can therefore fail at more than one cleanup site, but there is
still only one creation-time invariant violation.

3. Popup ownership remains coherent in the sampled queue paths.

`addPopup` owns the pointer while queued. `popFrontPopup` removes it from the owning queue
before returning it to the caller/EXE side. `clearPopups` deletes only pointers still in
the queue. The spaceship-popup selective cleanup likewise erases/deletes the queued item
rather than retaining a second owning alias. No F491 from ordinary popup transfer.

4. Diplomacy ownership remains coherent in the sampled validation paths.

`addDiplomacy` queues one owning pointer. `popFrontDiplomacy` removes before transfer.
`validateDiplomacy` removes invalid pointers from `m_listDiplomacy` before deleting them,
and the later `apInvalid` vector is only a temporary non-owning collection of pointers
still present in that one queue until the explicit remove+delete loop. No independent
same-pointer/two-owner state was established.

5. `refreshGroupCycleList`'s temporary `std::vector<CvSelectionGroup*>` is non-owning and
survives the list-node edits it was designed to decouple.

The routine removes group-cycle LIST NODES while retaining `CvSelectionGroup*` objects in
`update_list`; it does not delete the selection groups. The later `updateGroupCycle`
therefore receives live group objects under the already-audited group lifecycle invariant.
This is inherited from K-Mod/AdvCiv structure and does not reproduce F490-like lifetime
aliasing.

DUPLICATE CONTROL -- `postProcessMessages` NO_TECH MESSAGE DISCOUNT
-------------------------------------------------------------------
The retained-message pass naturally re-exposed:

    if (getCurrentResearch() == NO_TECH)
        m_iNewMessages--;

This is a real low-severity AdvC UI heuristic defect: persistent `NO_TECH` can exist
without a just-finished-tech message, so the auto-open count can be one too low. The full
reconciled album already records this exact current deterministic candidate and
intentionally leaves it unnumbered because the Turn Log entries themselves are preserved
and only the auto-open threshold is affected.

Do not rediscover it as F491.

GROUP-CYCLE STALE-ID CONTROL RECONFIRMED
-----------------------------------------
The whole-file pointer sweep also revisited the apparently unchecked:

    getSelectionGroup(pNode->m_data)->...

shape in cycle-list consumers. The reconciled album already proved the lifecycle
invariant: `CvSelectionGroup::kill` removes its ID from the group-cycle list before
`deleteSelectionGroup`, player teardown clears the cycle list before group destruction,
and save loading restores the paired structures without an intervening refresh. No new
release-only null dereference was established.

ANCESTRY / DUPLICATE DISCIPLINE
--------------------------------
The pointer/lifetime controls above were compared against the supplied current SAS,
Base AdvCiv 1.14 and K-Mod/Civ4CE where useful. Nothing besides F490 establishes a new
current supported ownership failure in this immediate family.

The full reconciled WIP209 album was also checked before promotion. In particular:
* the NO_TECH Turn-Log undercount is already recorded as a low-severity candidate;
* `validateDiplomacy`'s stale Defensive Pact offer shape is already closed against its
  one-item producer;
* group-cycle stale-ID lifetime was already closed by the deletion-order invariant.

No F491 is spent here.

CURRENT STONE AFTER C031-WIP224
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 UNUSED.
Latest physical checkpoint: C031-WIP224.
No repository source files modified.

NEXT
----
Continue from F491 unused.

Move out of the now-saturated retained-message ownership family and resume the remaining
under-covered authoritative `CvPlayer` state. Prioritize:

* persistent counters whose mutators assert but do not runtime-guard underflow/overflow;
* owner/team/lifecycle transitions where a cached value depends on another player's or
  team's membership/alive state;
* one-shot provenance flags and historical IDs that survive capture, death, recycling or
  reacquisition;
* validate-one-object -> substitute/mutate-another-object patterns analogous to F485;
* callbacks or object-killing calls after which a raw `CvCity*`, `CvUnit*`, group or event
  pointer is reused.

Require ordinary current SAS reachability, an authoritative gameplay/state consequence,
ancestry and full-album duplicate proof before assigning F491.

END C031-WIP224


===============================================================================
CHECKPOINT C031-WIP225 -- QUEUE 004 CVPLAYER.CPP -- F491 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 remains UNUSED.
No repository source files modified.

POST-WIP224 LIFECYCLE / CALLBACK / ONE-SHOT STATE PASS
------------------------------------------------------
The audit moved out of retained-message ownership and resumed low-coverage lifecycle
state. Several initially suspicious seams were traced to a clean closure. F491 is not
spent merely to keep numbering moving.

1. `disband(last city)` -> `setFoundedFirstCity(false)` is inherited intentional
   pre-first-city / acquisition-disband semantics, not a new historical-flag corruption.

`CvPlayer::disband(CvCity&)` contains:

    if (getNumCities() == 1)
        setFoundedFirstCity(false);
    ...
    kCity.kill(true);

and `verifyAlive` later uses:

    if (getNumCities() == 0 && getAdvancedStartPoints() < 0)
    {
        if (getNumUnits() <= 0 || isFoundedFirstCity())
            bKill = true;
    }

At first glance this looks like a reversible historical flag allowing a once-settled
civilization to become "never settled" again. The caller/lifecycle context closes the
ordinary-current concern:

* the compiled DLL caller of `disband` is the acquired-city disband task (plus the
  intentionally exposed Python API);
* `BUTTONPOPUP_DISBANDCITY` is created for a HUMAN city acquisition that is neither
  conquest nor trade -- the stock-style choice to reject/disband a just-acquired city;
* ordinary established civilizations that have already lost all prior cities do not
  remain freely alive waiting to exploit this reset: `verifyAlive` eliminates a
  zero-city player once `FoundedFirstCity` is true (AdvC has removed Complete Kills);
* the reset therefore principally preserves the longstanding ability of a still-
  unsettled player to reject its first non-founded acquisition and return to the
  pre-first-city state rather than being defeated solely because that transient city
  existed.

The exact reset is inherited through Civ4CE/BtS -> K-Mod -> Base AdvCiv -> SAS. No
independent current path was established where an ordinarily settled surviving player
uses this call to erase genuine settlement history and gain a new lifecycle exemption.
Preserve as intentional inherited semantics; no F491.

2. `AI_conquerCity(kNewCity, ...)` and auto-raze can delete the newly acquired city, but
   `CvPlayer::acquireCity` deliberately stops dereferencing that city afterward.

Current acquisition explicitly comments:

    else if (!isHuman())
    {   // May delete kNewCity!
        AI().AI_conquerCity(kNewCity, ...);
    }

The same branch can call `kNewCity.doTask(TASK_RAZE)`, which also destroys the city.
A continuation audit verified that after these potentially destructive calls the
function no longer dereferences `kNewCity`. Remaining work uses durable snapshots or
independent objects:

* old event IDs (`eOldOwner`, `iOldCityId`);
* `kCityPlot`, a map plot object independent of the killed city allocation;
* saved reveal-state bits;
* player/team identities and caches.

Thus the "May delete" comment is backed by a real lifetime boundary, and the code honors
it. No post-kill city UAF is hidden immediately below the branch.

3. `cityAcquired` Python/event reporting occurs before the later `canRaze` / AI conquest
   decision, but CURRENT shipped Python handlers do not destroy or transfer the city.

The event reporter forwards the live city to Python. Current SAS `CvEventManager`'s
`onCityAcquired` only records SAS battle-history metadata and optionally logs the city;
installed Contrib handlers are notification/logging consumers. No current shipped
handler was found that razes, gifts, transfers or otherwise destroys the city during
this callback.

A modmod/Python extension could violate that implicit callback lifetime contract, but an
API-extensibility hazard without a current producer is below the threshold for F491.
Do not manufacture a callback UAF from what current SAS does not do.

4. The nearby `cityRazed` reporter likewise does not establish a current callback-kill
   race before native `disband(kCity)`.

Current raze ordering is:

    kCity.doPartisans();
    CvEventReporter::getInstance().cityRazed(&kCity, getID());
    disband(kCity);

The shipped Python cityRazed path is notification/logging-oriented; no current handler
was found that destroys the city before native disband. The native order therefore
retains one authoritative destruction point in shipped SAS.

5. `changeHurryCount`'s transition-side aggregate update is safe under its only compiled
   producer.

The method changes `m_iPopRushHurryCount` / `m_iGoldRushHurryCount` by the full `iChange`
when an individual HurryTypes count crosses zero. This would be suspicious if callers
could jump a hurry source count by magnitude >1 in one call: a 0->2 transition should
add one enabled hurry TYPE to those aggregate counts, not two.

A whole current C++ caller search finds the only gameplay producer in `processCivics`:

    changeHurryCount(eLoopHurry,
        GC.getInfo(eCivic).isHurry(eLoopHurry) ? iChange : 0);

Civic processing applies/removes one civic source at a time with `iChange` +/-1. The
problematic magnitude >1 transition has no ordinary compiled producer. Keep as generic
API robustness only; no F491.

6. Advanced Start validate/substitute siblings remain closed apart from F485.

The re-loop compared the F485 unit/route fallback pattern with neighboring actions:

* Improvement removal substitutes the actual plot improvement when the requested type
  differs, but crucially recomputes `getAdvancedStartImprovementCost(..., pPlot)` with
  the SAME concrete plot and aborts on a negative result before mutation.
* Population / culture / building actions keep the validated concrete city object and
  abort on negative plot/city-aware cost.
* City removal obtains a nonnegative plot-aware city-removal cost before killing the
  plot city; no substitute object is introduced afterward.

So the dangerous "validate A -> substitute B -> recompute generically -> mutate B"
shape remains localized to the already-confirmed F485 route fallback (and F485's unit
release-assert path). No F491 sibling was established here.

DUPLICATE / ALBUM DISCIPLINE
----------------------------
Before promotion, the reconciled WIP209 album was checked for the first-city/disband
flag terminology and existing verifyAlive lifecycle roots. No prior numbered root owns
this exact candidate, but the candidate itself closes under inherited caller semantics.
That absence is not a reason to promote it.

The callback/lifetime pass also remains distinct from the already-known delayed-event
countdown UAF: that event pointer is genuinely dereferenced after erasing its owning map
node, whereas the acquired-city code snapshots/changes object usage after the explicitly
potentially destructive AI/raze calls.

CURRENT STONE AFTER C031-WIP225
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 UNUSED.
Latest physical checkpoint: C031-WIP225.
No repository source files modified.

NEXT
----
Continue from F491 unused.

The next tranche should stay on authoritative stored state rather than UI-only helpers:

* class-making / building-making / religion/corporation and enabled-option counters --
  trace all +/- producers for asymmetric removal or multi-source transition errors;
* player/team membership transitions against cached city/player values not already
  covered by PA maintenance, outside-unit, military-happiness and AgentIterator roots;
* one-shot state such as first-GPP, religion/corporation founding/ownership provenance
  and acquisition/reacquisition boundaries;
* remaining object-pointer lifetime across reporter/Python callbacks only where CURRENT
  shipped handlers can mutate the target;
* continue the validate-one-object -> mutate-another pattern outside Advanced Start.

Require ordinary current reachability, concrete authoritative consequence, ancestry and
full-album duplicate proof. A clean F491-unused continuation remains a valid result.

END C031-WIP225


===============================================================================
CHECKPOINT C031-WIP226 -- QUEUE 004 CVPLAYER.CPP -- F491 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 remains UNUSED.
Fresh audit source switched to user-supplied Sep-2 light source:
`AdvCiv-SAS_light_source_20260902T161104.zip`.
No repository source files modified.

POST-WIP225 COUNTER / ONE-SHOT / CALLBACK PASS
----------------------------------------------
This tranche followed WIP225's requested stored-state families against the fresh Sep-2
source. It produced useful closures and one strong rediscovery that was rejected by the
mandatory full-album duplicate filter. F491 is deliberately not spent.

1. RELIGION-SLOT CITYLESS-FOUNDER LEAD IS EXACTLY F480 / KI#803 -- DO NOT DUPLICATE.

The fresh current source still contains the striking contract mismatch:

`CvTeam::setHasTech` chooses the first-discovery religion recipient from every ALIVE
member of the team, with no `getNumCities() > 0` filter, then reserves the slot:

    for (MemberIter it(getID()); it.hasNext(); ++it)
    {
        CvPlayer const& kMember = *it;
        ...
        if (iValue < iBestValue)
            eBestPlayer = kMember.getID();
    }
    ...
    kGame.setReligionSlotTaken(eLoopReligion, true);
    ...
    GET_PLAYER(eBestPlayer).foundReligion(...);

`CvPlayer::foundReligion` again marks the slot taken, searches only that player's own
cities, and returns when none exists:

    kGame.setReligionSlotTaken(eSlotReligion, true);
    ...
    if (pBestCity == NULL)
        return;

`CvGame::doHolyCity` later begins each religion candidate with:

    if (isReligionSlotTaken(eReligion))
        continue;

so the generic shape is a permanent lost religion if the selected team member is alive
but cityless.

Before promotion, the reconciled WIP209 album was checked for `setReligionSlotTaken`,
`foundReligion`, cityless founding and Holy City wording. It already contains the full
confirmed F480/KI#803 proof, including the deterministic current Calendar/GOODY_TECH
producer, persistent serialized consequence, ancestry and repair direction.

Fresh ancestry recheck confirms the same city-agnostic member scoring and no-city
`foundReligion` behavior in Civ4CE/BtS, K-Mod 1.46 and Base AdvCiv 1.14. This is therefore
only a current-source reconfirmation of F480, not F491.

2. UNIT / BUILDING CLASS `Making` COUNTERS REMAIN SYMMETRIC UNDER CURRENT ORDER QUEUE
   PRODUCERS.

`CvCity::pushOrder` increments player class-making state only after `canTrain` /
`canConstruct` succeeds (or explicit `bForce`):

    changeUnitClassMaking(unitClass, +1)
    changeBuildingClassMaking(buildingClass, +1)

`CvCity::popOrder` locates a concrete existing queue node and decrements the class encoded
in that exact node before deleting it. `clearOrderQueue` repeatedly reaches the same
paired pop path.

Saved-repeat orders deserve special attention because `popOrder(..., bFinish=true)`
first calls `pushOrder` to append the saved replacement and only afterward decrements the
old node. That creates a temporary +1 then -1, preserving the correct net count of one
queued copy. No missing decrement or duplicate persistent increment was established.

`CvCity::upgradeProduction` also transfers class-making state explicitly around queue
mutation:

    old class -1
    order.iData1 = upgrade unit
    new class +1

and updates UnitAI training counters if the old AI role is invalid for the replacement.
No independent class-making desynchronization survived this pass.

3. `changeHurryCount` MAGNITUDE TRANSITION REMAINS GENERIC API ROBUSTNESS ONLY.

WIP225 noted that the transition-side aggregates use the full `iChange`:

    if ((old > 0) != (new > 0))
        m_iPopRushHurryCount += iChange;

A hypothetical 0->2 call would therefore add 2 even though only one HurryTypes entry
became enabled. The fresh whole compiled caller check still finds the gameplay producer
only in `processCivics`, which supplies exactly +/-1 for a civic source:

    changeHurryCount(eHurry,
        civic.isHurry(eHurry) ? iChange : 0);

Civic processing itself applies one civic source with `iChange` +/-1. No ordinary current
magnitude>1 producer exists. Preserve as API hardening only; no F491.

4. SPECIAL-BUILDING / SPECIALIST / CIVIC-OPTION ENABLE COUNTS HAVE PAIRED CURRENT
   PRODUCERS; CURRENT XML FURTHER MASKS THE BUILDING CIVIC-OPTION PATH.

`changeSpecialBuildingNotRequiredCount` and `changeSpecialistValidCount` are fed by
`processCivics` with +/-1 per active civic source. The specialist path dirties assignment;
the special-building prerequisite path was already traced in the earlier Queue-004 pass
and production continuation revalidates it.

`changeNoCivicUpkeepCount` is fed by trait processing with +/-1 per trait source.

`changeHasCivicOptionCount` is fed by `processBuilding`, so the count tracks concrete
building effects through building add/remove processing. A fresh current
`CIV4BuildingInfos.xml` scan finds no shipped BuildingInfo with a non-NONE `CivicOption`.
Thus even hypothetical TeamShare/count-composition wrinkles in this field are presently
data-masked and do not earn F491.

5. RELIGION / CORPORATION CITY COUNTS ARE UPDATED THROUGH THE CONCRETE CITY STATE
   TRANSITION, NOT THROUGH AN INDEPENDENT ESTIMATE.

Current city setters call:

    kOwner.changeHasReligionCount(eReligion,
        isHasReligion(eReligion) ? +1 : -1);

and analogously for corporations after the city's actual boolean state transition.
The player mutators update building commerce and assignment state after the count change.
No different identity is substituted between validation and decrement, and no current
asymmetric add/remove producer was established.

6. `foundCorporation` CITYLESS FAILURE DOES NOT HAVE F480'S PERMANENT SLOT-CONSUMPTION
   CONSEQUENCE.

The adjacent corporation first-tech selector also lacks a city filter and can call
`foundCorporation` on a cityless team member. But `foundCorporation` simply fails to set a
Headquarters when `pBestCity == NULL`; it does not set a persistent equivalent of
`m_abReligionSlotTaken` first.

The current code itself labels this setHasTech corporation block unused/not properly
tested and points to the normal `CvGame::doHeadquarters` mechanism. With no consumed
founding-slot state analogous to F480, this sibling did not establish a current permanent
loss root in this tranche. Keep the city-capability mismatch as robustness/context, not
F491 without a stronger current consequence.

7. `pushOrder` FIRES `cityBuildingUnit` / `cityBuildingBuilding` TWICE, BUT CURRENT
   SHIPPED HANDLERS MAKE THIS A LOGGING/UI QUALITY WRINKLE, NOT A QUEUE-004 GAMEPLAY ROOT.

Fresh cross-file inspection noticed that `CvCity::pushOrder` calls the reporter once
inside the validated ORDER_TRAIN / ORDER_CONSTRUCT branch and then again after inserting
the order. The second call is accompanied by the old Warlords-era comment:

    // Why does this cause a crash???
    // <advc.test> Uncommented. Was added and commented out by the Warlords expansion.

Current shipped `CvEventManager.onCityBuildingUnit/Building` only optionally prints a
log line. BUG Autolog registers the same events and, when Build Started logging is
enabled, writes the corresponding production-start record. Therefore current SAS can
emit duplicate build-start logging for one pushed order.

No shipped handler found in this pass mutates the city/order queue, kills the city, or
changes authoritative gameplay state. The earlier callback-lifetime concern therefore
does not turn into a current UAF/queue corruption through these handlers. This is worth a
future UI/logging cleanup note, but below the current gameplay/state threshold for F491.

CURRENT STONE AFTER C031-WIP226
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 UNUSED.
Latest physical checkpoint: C031-WIP226.
No repository source files modified.

NEXT
----
Continue from F491 unused against the Sep-2 source.

Next priorities:
* persistent scalar counts whose correctness depends on city/unit destruction order,
  especially nuke/military/outside-unit/health/happiness aggregates not already owned by
  PA or elimination findings;
* one-shot/historical state beyond F480/F481 and recycled-ID roots, especially values
  that are serialized but are not reconstructed from live cities/units;
* caller-visible state changed by `processBuilding` / `processCivics` where a TeamShare,
  obsolescence, capture or civic switch can remove through a different source boundary;
* pointer lifetime after current shipped reporter/Python callbacks only where a handler
  has authority to mutate the target;
* continue validate-one-object -> mutate-another-object substitutions outside the
  already-saturated Advanced Start family.

Require ordinary current SAS reachability, authoritative consequence, ancestry and
full-album duplicate proof. A further clean F491-unused checkpoint remains valuable.

END C031-WIP226


===============================================================================
CHECKPOINT C031-WIP227 -- TEMPORARY THREAD-SAFETY HANDOFF -- F491 CANDIDATE OPEN
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 remains UNUSED at this checkpoint.
Current audit source: user-supplied Sep-2 light source
`AdvCiv-SAS_light_source_20260902T161104.zip`.
No repository source files modified.

This is intentionally a TEMPORARY physical handoff because the conversation may fill.
The strongest current lead is preserved below as a candidate, not yet promoted. The
checkpoint also corrects one factual sentence in WIP226.

WIP226 CORRECTION -- CURRENT BUILDING-GRANTED CIVICOPTION XML IS LIVE
---------------------------------------------------------------------
WIP226 section 4 said a fresh `CIV4BuildingInfos.xml` scan found no current BuildingInfo
with a non-NONE `CivicOption`. That sentence is WRONG and must not be carried forward.

Current Sep-2 SAS still has the three live building CivicOption sources already proved in
the older reconciled album:
* Colossus -> Government;
* Mausoleum of Maussollos -> Economy;
* Shwedagon Paya -> Legal.

The associated last-source-loss problem is not new: it is exactly F459 / KI#782. The
reconciled album already proves that losing the final building-granted CivicOption can
leave a now-unavailable civic active through the player's next full turn and its economy /
city processing. Therefore this correction changes no numbering and does NOT make F491.

POST-WIP226 CLEAN COUNTER / TRANSITION CONTROLS SO FAR
------------------------------------------------------
The resumed Sep-2 pass continued through low-coverage source-count families instead of
assuming that a missing literal album mention implied a defect.

* Concrete unit military/nuke/extra-cost counters continue to pair initialization with
  unit destruction; no independent stale player aggregate was established.
* Plot improvement ownership bookkeeping removes the old player's improvement count
  before owner identity changes and adds the new player's count afterward.
* Outside-unit accounting similarly has explicit old/new side adjustments at plot/team
  ownership transitions; existing Permanent-Alliance outside-unit roots remain the
  relevant exceptional family rather than a new generic asymmetry.
* `changeNoCorporationsCount` and `changeNoForeignCorporationsCount` both call the
  player's full `updateCorporation()` immediately when the CIVIC source count changes.
  Ordinary civic entry/exit therefore rebuilds every owned city's corporation state.
* Draft-count / historical AnyGPPEver / nearby scalar state sampled here did not expose
  another authoritative source-boundary failure.
* TeamShare building replay can still create the already-recorded noncanonical raw area
  counter shape after Permanent Alliance membership changes. Current concrete sources
  remain masked by the same lifecycle/data controls already recorded (notably Great Wall
  obsolescence timing and one-way wonder lifecycle). No new independent consequence was
  promoted.

F491 / KI#814 CANDIDATE -- HQ OWNER TRANSFER CAN CHANGE PROTECTIONISM CORPORATION
ACTIVITY WITHOUT REBUILDING THE OWNERS' OTHER CORPORATION CITIES
-------------------------------------------------------------------------------
STATUS: STRONG CURRENT CANDIDATE; NOT YET CONFIRMED / NUMBERED.

ROOT SHAPE
----------
Current player corporation policy is dynamic:

    bool CvPlayer::isActiveCorporation(CorporationTypes eCorp) const
    {
        if (isNoCorporations())
            return false;
        if (isNoForeignCorporations() && !hasHeadquarters(eCorp))
            return false;
        return true;
    }

Current `CIVIC_PROTECTIONISM` has:

    bNoCorporations         = 0
    bNoForeignCorporations  = 1

so under Protectionism a corporation is active for a player exactly when that player owns
its HQ (subject to the city having the corporation/resource prerequisites).

Changing the civic itself is handled correctly: `changeNoForeignCorporationsCount` calls
`CvPlayer::updateCorporation()`, rebuilding every city. The candidate is a DIFFERENT input
to the same predicate: HQ OWNER IDENTITY.

`CvPlayer::hasHeadquarters` is not cached player state. It resolves the current global HQ
city and compares its CURRENT owner:

    CvCity const* pHeadquarters = GC.getGame().getHeadquarters(eCorporation);
    return (pHeadquarters != NULL && pHeadquarters->getOwner() == getID());

Thus transferring the HQ between players changes `isActiveCorporation` immediately even
if neither player's civic changes.

`CvGame::setHeadquarters`, however, only updates the OLD HQ CITY and the NEW HQ CITY:

    m_aeHeadquarters.set(...);
    if (pOldCity != NULL)
        pOldCity->updateCorporation();
    ...
    pHeadquarters->setHasCorporation(...);
    pHeadquarters->updateCorporation();

It does NOT call `oldOwner.updateCorporation()` or `newOwner.updateCorporation()` for all
of either player's other cities.

WHY OTHER CITIES MATTER
-----------------------
`CvCity::updateCorporation` rebuilds authoritative stored state:
* corporation-produced bonuses (`m_aiNumCorpProducedBonuses`);
* building commerce affected by those bonuses;
* corporation yields (`m_aiCorporationYield`), applied through base-yield changes;
* corporation commerce (`m_aiCorporationCommerce`);
* maintenance.

The per-corporation yield/commerce calculations call `isActiveCorporation`, so a changed
HQ owner is a direct input to those caches. A branch city that is not itself the old/new
HQ therefore needs a rebuild when its owner's HQ status flips under Protectionism.

ORDINARY ACQUIRECITY TRANSFER SEQUENCE -- PROVED TO THE CURRENT CHECKPOINT
-------------------------------------------------------------------------
`CvPlayer::acquireCity` snapshots both corporation presence and HQ identity from the old
city:

    abHasCorporation[eCorp] = pOldCity->isHasCorporation(eCorp);
    abHeadquarters[eCorp]   = pOldCity->isHeadquarters(eCorp);

It then kills the old city. `CvCity::kill` removes the corporation and, if this is the HQ,
calls:

    GC.getGame().setHeadquarters(eCorp, NULL, false);

This clears the global HQ pointer. If old owner O is running Protectionism, O's player-
level `isActiveCorporation(eCorp)` therefore flips TRUE -> FALSE immediately. The setter
updates the dying old HQ city only; O's surviving branch cities are not globally rebuilt.

The replacement city is then created for new owner N. `acquireCity` restores:

    kNewCity.setHasCorporation(eCorp, true, false);
    kGame.setHeadquarters(eCorp, &kNewCity, false);

Before the second call, N does not yet own the HQ, so under Protectionism the restored
corporation in the new city is initially evaluated as foreign/inactive. Restoring the HQ
then makes N's player-level predicate flip FALSE -> TRUE and refreshes the NEW HQ city.
N's pre-existing branch cities are again not globally rebuilt.

Consequently the same ordinary HQ capture/trade can create BOTH directions of stale
branch state:
* OLD owner O can retain cached active corporation yields/commerce/produced bonuses /
  maintenance in surviving branches after losing the HQ;
* NEW owner N can retain cached INACTIVE/zero corporation state in pre-existing branches
  after gaining the HQ.

PLOT-GROUP REBUILD DOES NOT GENERALLY CLOSE THE ROOT
----------------------------------------------------
`acquireCity` later calls `CvGame::updatePlotGroups()` when requested. This does not imply
a full corporation rebuild. `CvPlayer::updatePlotGroups` asks each existing plot group to
`recalculatePlots`; `CvPlotGroup::recalculatePlots` returns immediately when all plots in
that group remain connected. A pre-existing branch city can be placed in an unrelated /
separate stable plot group from the captured HQ city. Its bonus counts therefore need not
change and no incidental `changeNumBonuses -> updateCorporation` is guaranteed.

Even when a plot group is rebuilt, corporation updates arise from actual bonus-count
changes, not from an explicit HQ-owner dependency invalidation. The missing old/new-owner
rebuild therefore remains a live architectural concern rather than being automatically
repaired by `updatePlotGroups`.

CURRENT DATA SUPPORT -- PARTIALLY SETTLED
-----------------------------------------
Protectionism is a current normal Economy civic (Banking prerequisite) with
`bNoForeignCorporations=1`.

Current corporations have ordinary nonzero branch outputs. For example `CORPORATION_1`
(Cereal Mills in the existing album terminology) consumes grain bonuses and has
`iYieldProduced=100`, giving a simple positive branch-yield state when prerequisites are
connected. Other current corporations produce commerce and/or corporation-created bonus
resources, so the stale-state family is not XML-dormant.

A deterministic reproduction can therefore be shaped without debug/Python state:
1. Corporation C has HQ owner O and at least one non-HQ branch city O1 with a qualifying
   resource.
2. O runs Protectionism. Because O owns the HQ, C is active and O1's corporation cache is
   positive/current.
3. Player N also runs Protectionism and has a pre-existing C branch N1. Because N does NOT
   own the HQ, its civic-change rebuild has made C inactive there (zero corporation
   output/cache).
4. N captures or otherwise acquires O's HQ city through ordinary city transfer.
5. `acquireCity` performs the HQ NULL -> new-HQ transition described above.
6. Correct post-transfer state:
      O1 should become inactive;
      N1 should become active.
7. Candidate current code leaves those remote branch caches unchanged until some unrelated
   corporation/bonus/occupation/civic/anarchy refresh happens.

OPEN PROOF ITEMS BEFORE F491 MAY BE PROMOTED
--------------------------------------------
Do NOT call this confirmed yet. Still finish:
* verify no later acquire/keep-city/global turn hook unconditionally rebuilds corporation
  state for every city of BOTH old and new owners after the HQ pointer transition;
* pin the simplest current corporation/resource numeric fixture for an undeniable stored
  yield/commerce difference;
* trace exactly which same-turn/next-turn authoritative consumer books the stale value and
  characterize eventual self-heal timing;
* compare BtS/Civ4CE, K-Mod and Base AdvCiv `setHeadquarters` + no-foreign-corporation
  semantics to classify ancestry precisely;
* complete semantic duplicate search against the full reconciled album (not merely exact
  `Protectionism`/`setHeadquarters` spelling), especially corporation cache, HQ ownership,
  capture, civic and maintenance findings;
* confirm city trade as well as conquest only if useful; one ordinary producer is enough.

CURRENT STONE AT TEMPORARY C031-WIP227
--------------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F490 / KI#813 CONFIRMED.
F491 / provisional KI#814 remains UNUSED pending the proof items above.
Latest physical checkpoint: C031-WIP227 TEMPORARY.
No repository source files modified.

NEXT
----
Resume directly inside the HQ-transfer candidate proof. If it fails any authoritative
consumer/self-heal/duplicate/current-data requirement, retain it as a control and keep
F491 unused. If it survives, promote only after ancestry and full duplicate proof are
finished. Then return to the remaining signed health/happiness and processBuilding /
processCivics source-boundary counts.

END C031-WIP227 TEMPORARY


===============================================================================
CHECKPOINT C031-WIP228 -- QUEUE 004 CVPLAYER.CPP -- F491 / KI#814 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F491 / KI#814 CONFIRMED.
F492 / provisional KI#815 is now UNUSED.
Current audit source: Sep-2 user light source
`AdvCiv-SAS_light_source_20260902T161104.zip`.
No repository source files modified.

C031-WIP227 was deliberately frozen while the HQ-transfer lead was still provisional.
The remaining current-data, authoritative-consumer, self-heal, ancestry and semantic
full-album checks are now complete. The candidate survives and is promoted as F491.

F491 / KI#814 -- PROTECTIONISM HQ TRANSFER CHANGES CORPORATION ACTIVITY FOR BOTH
PLAYERS BUT REFRESHES ONLY THE HQ CITIES, LEAVING OTHER BRANCH CACHES WRONG
-------------------------------------------------------------------------------

STATUS / CLASSIFICATION
-----------------------
CONFIRMED current gameplay defect.

The core bug is inherited from stock BtS/Civ4CE through K-Mod and Base AdvCiv. Current
AdvCiv-SAS keeps the same DLL contract and has a directly live current civic/data
reproduction through Protectionism and ordinary corporation branch output.

ROOT -- HQ OWNERSHIP IS A LIVE INPUT TO PLAYER CORPORATION ACTIVITY
------------------------------------------------------------------
Current `CvPlayer::isActiveCorporation` is:

    bool CvPlayer::isActiveCorporation(CorporationTypes eCorp) const
    {
        if (isNoCorporations())
            return false;
        if (isNoForeignCorporations() && !hasHeadquarters(eCorp))
            return false;
        return true;
    }

Current `CIVIC_PROTECTIONISM` has:

    TechPrereq               = TECH_BANKING
    bNoCorporations          = 0
    bNoForeignCorporations   = 1

Therefore, while a player runs Protectionism:
* a corporation whose HQ that player OWNS is active;
* the same corporation is foreign/inactive for a player who does NOT own its HQ.

`hasHeadquarters` is dynamic, not a separately maintained player flag:

    CvCity const* pHeadquarters = GC.getGame().getHeadquarters(eCorporation);
    return (pHeadquarters != NULL && pHeadquarters->getOwner() == getID());

So changing the global HQ city owner immediately changes the correct active-corporation
state for the OLD owner and NEW owner even though neither player's civic changed.

CONTRAST CONTROL -- CIVIC POLICY CHANGE DOES INVALIDATE THE WHOLE PLAYER
-----------------------------------------------------------------------
The existing civic-source transition is correctly wired:

    CvPlayer::changeNoForeignCorporationsCount
        -> updateCorporation()
        -> every owned city -> CvCity::updateCorporation()

Thus entering/leaving Protectionism rebuilds every city. This proves that corporation
activity is treated as derived city state requiring explicit invalidation.

The missed dependency is the OTHER input to the same predicate: `hasHeadquarters`.

BROKEN INVALIDATION -- `CvGame::setHeadquarters` UPDATES ONLY TWO CITIES
-----------------------------------------------------------------------
Current setter:

    CvCity* pOldCity = getHeadquarters(eCorp);
    ...
    m_aeHeadquarters.set(...);
    if (pOldCity != NULL)
        pOldCity->updateCorporation();
    ...
    CvCity* pHeadquarters = getHeadquarters(eCorp);
    if (pHeadquarters == NULL)
        return;
    pHeadquarters->setHasCorporation(eCorp, true, bAnnounce);
    pHeadquarters->updateCorporation();

The OLD HQ city is rebuilt and the NEW HQ city is rebuilt. There is no:

    GET_PLAYER(eOldOwner).updateCorporation();
    GET_PLAYER(eNewOwner).updateCorporation();

and no equivalent scan over their other branch cities.

That is insufficient whenever HQ owner identity changes under a NoForeignCorporations
policy, because the correct corporation activity changes simultaneously in EVERY city of
both affected players that contains that corporation and its required resources.

WHAT `CvCity::updateCorporation` STORES
---------------------------------------
This is not merely a dynamic-query/UI mismatch. The routine reconstructs authoritative
stored state:

    updateCorporationBonus();
    updateBuildingCommerce();
    for each Yield:
        updateCorporationYield();
    for each Commerce:
        updateCorporationCommerce();
    updateMaintenance();

Those fields include:
* corporation-produced resource counts (`m_aiNumCorpProducedBonuses`);
* corporation yield (`m_aiCorporationYield`);
* corporation commerce (`m_aiCorporationCommerce`);
* dependent building commerce;
* cached city maintenance.

`updateCorporationYield` ultimately changes `m_aiBaseYieldRate` through
`changeBaseYieldRate`. Once stale, normal `getYieldRate` reads the wrong base yield until
some later corporation invalidator happens.

ORDINARY CITY ACQUISITION PROVES BOTH DIRECTIONS
------------------------------------------------
`CvPlayer::acquireCity` explicitly preserves headquarters identity across ownership
transfer. Before deleting the old city it snapshots:

    abHasCorporation[eCorp] = pOldCity->isHasCorporation(eCorp);
    abHeadquarters[eCorp]   = pOldCity->isHeadquarters(eCorp);

It then executes:

    pOldCity->kill(false, false);

`CvCity::kill` removes corporation presence and, for an HQ, calls:

    GC.getGame().setHeadquarters(eCorp, NULL, false);

At that moment the global HQ pointer becomes null. If old owner O runs Protectionism,
O's correct player-level corporation activity flips:

    TRUE -> FALSE

because O no longer satisfies `hasHeadquarters(eCorp)`. `setHeadquarters` refreshes the
old/dying HQ city, but does not refresh O's other surviving branch cities.

`acquireCity` then recreates the city for new owner N and restores:

    kNewCity.setHasCorporation(eCorp, true, false);
    kGame.setHeadquarters(eCorp, &kNewCity, false);

Before the second line N does not own the HQ, so under Protectionism the restored city
initially treats the corporation as foreign/inactive. After the second line N DOES own
the HQ and N's correct player-level corporation activity flips:

    FALSE -> TRUE

The new HQ city is explicitly rebuilt. N's pre-existing branch cities are not.

Thus one ordinary HQ capture/trade can create both stale directions at once:

OLD owner O, other branches:
    correct after transfer = inactive / zero corporation output
    cached current result  = still active / old positive corporation output

NEW owner N, other branches:
    correct after transfer = active / corporation output present
    cached current result  = still inactive / zero corporation output

CURRENT-SAS DETERMINISTIC NUMERIC REPRO -- CEREAL MILLS
-------------------------------------------------------
Current `CORPORATION_1` (Cereal Mills in the existing album terminology) has grain
prerequisites and:

    iYieldProduced[FOOD] = 100

Current Standard world size has:

    iCorporationMaintenancePercent = 100

Therefore a normal non-disordered Cereal Mills branch with exactly one qualifying Wheat
and no other grain contributes exactly:

    100 * 1 * 100 / 100 = 100 hundredths
    intdiv::uceil(100, 100) = +1 FOOD

Supported reproduction:

1. Player O owns the Cereal Mills HQ and a separate non-HQ branch city O1.
2. Player N owns a separate Cereal Mills branch city N1 but not the HQ.
3. Give O1 and N1 exactly one connected Wheat relevant to the corporation.
4. Place O1/N1 in stable plot groups unrelated to the HQ city's plot group.
5. Both players adopt Protectionism and finish any anarchy so all corporation caches are
   freshly rebuilt under stable pre-transfer state.
6. Pre-transfer authoritative cache state is therefore:

       O owns HQ -> O1 corporation FOOD = +1
       N no HQ   -> N1 corporation FOOD = 0

7. During N's ordinary active turn, N captures O's Cereal Mills HQ city.
8. Acquisition clears the old HQ pointer and later restores it on N's replacement city.
9. Dynamic player policy now correctly says:

       O.isActiveCorporation(Cereal Mills) = false
       N.isActiveCorporation(Cereal Mills) = true

10. But only old/new HQ city corporation caches were rebuilt, leaving:

       O1 cached corporation FOOD = +1   // should now be 0
       N1 cached corporation FOOD = 0    // should now be +1

A forced:

    O.updateCorporation();
    N.updateCorporation();

immediately produces the correct opposite values, demonstrating that the live cache and
its own current formula disagree solely because the ownership transition missed the
invalidator.

AUTHORITATIVE GAMEPLAY CONSEQUENCE -- STALE FOOD IS BOOKED
----------------------------------------------------------
`CvCity::setCorporationYield` / `updateCorporationYield` apply corporation yield to the
stored base yield through `changeBaseYieldRate`.

Normal `CvCity::doTurn` does NOT call `updateCorporation`. Its ordinary sequence includes:

    doGrowth();
    doCulture();
    doProduction(...);

`doGrowth` calls:

    int iDiff = foodDifference();
    changeFood(iDiff);

and `foodDifference` reads current `getYieldRate(YIELD_FOOD)`, which includes the stored
base-yield cache.

Therefore the stale state is consumed, not merely displayed:
* N1 can miss the +1 food it should receive on the SAME turn N captured the HQ;
* O1 can keep receiving +1 food after O has lost the HQ, including on O's next turn.

Choose branch food totals exactly at consumption if a maximally visible test is desired:
correct HQ ownership then changes foodDifference by exactly one. Over enough turns the
same error can change growth/starvation timing. Current corporations with production or
commerce output provide parallel authoritative consequences.

NO UNCONDITIONAL TURN SELF-HEAL
-------------------------------
A complete current `updateCorporation` caller scan finds no unconditional city-turn or
player-turn rebuild.

Whole-player refreshes occur only on specific unrelated inputs/events, e.g.:
* entering/leaving anarchy;
* NoCorporations / NoForeignCorporations civic count changes;
* relevant technology processing.

City-local refreshes occur on specific corporation/bonus/occupation changes.

Consequently an unaffected branch can remain wrong until some unrelated invalidator
fires. Advancing the turn alone does not restore the HQ-dependent cache.

WHY `acquireCity -> updatePlotGroups` DOES NOT RESCUE THE BRANCHES
-----------------------------------------------------------------
The acquisition path can later call global `updatePlotGroups`, but this is not a full
corporation rebuild.

`CvPlayer::updatePlotGroups` asks each existing group to `recalculatePlots`.
`CvPlotGroup::recalculatePlots` explicitly returns immediately when its existing plots
remain connected:

    if (iCount == iOldCount)
        return;

So O1 and N1 can be placed in separate stable plot groups unrelated to the transferred
HQ. Their bonus counts do not need to change and no incidental
`changeNumBonuses -> updateCorporation` occurs.

Even a group recalculation updates corporation state only through actual resource-count
transitions. There is no explicit dependency from HQ owner identity to all branch caches.
The plot-group step therefore cannot serve as the required invalidation contract.

ANCESTRY
--------
This is inherited from stock BtS.

Civ4CE/BtS has the same player predicate:

    if (isNoForeignCorporations() && !hasHeadquarters(eIndex))
        return false;

and stock `CvGame::setHeadquarters` likewise updates only:
* `pOldValue->updateCorporation()`;
* `pHeadquarters->updateCorporation()`.

Stock current data already makes the dependency live through `CIVIC_MERCANTILISM`, which
has both:

    bNoCorporations        = 0
    bNoForeignCorporations = 1

K-Mod 1.46 retains the same predicate and the same old/new-HQ-only setter.
Base AdvCiv 1.14 retains both unchanged in substance.
Current SAS retains the DLL architecture while its current Economy civic
`CIVIC_PROTECTIONISM` carries the same NoForeignCorporations mechanism.

Thus F491 is a Firaxis/BtS corporation-cache invalidation bug inherited through the full
lineage, with current SAS exposure through Protectionism.

DUPLICATE / RECONCILIATION CHECK
--------------------------------
The full WIP209 album was searched semantically and literally for:
* HQ/headquarters owner or ownership transfer;
* NoForeignCorporations / Mercantilism / Protectionism;
* corporation activity and branch-city state;
* corporation yield/commerce/cache and HQ capture.

The current WIP210-WIP226 physical chain was also searched locally for the same family.
No existing numbered root owns HQ-owner identity as a missed input to branch corporation
cache invalidation.

Important neighboring roots/controls are distinct:

* F411 / KI#734 -- corporation city HOVER reconstructs output/maintenance while disorder
  suppresses live runtime effects. F491 is authoritative stored city state after HQ owner
  transfer in normal non-disordered branch cities.
* F421 / KI#744 -- corporation maintenance cache embeds stale inverse inflation over time.
  F491 changes whether the corporation should be active at all and affects yield,
  commerce, produced resources and maintenance immediately.
* F429 / KI#752 -- prospective spread help evaluates prerequisite resources before a
  competing corporation is removed. F491 is post-execution runtime state, not preview.
* WIP179/WIP195 corporation-count closure -- correctly notes that CHANGING the
  NoForeignCorporations civic count calls `updateCorporation`. F491 identifies a second
  independent input to the same active predicate (`hasHeadquarters`) that the HQ setter
  does not invalidate. This checkpoint supersedes only the breadth of the old generic
  closure; the civic-count statement itself remains correct.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Whenever `CvGame::setHeadquarters` changes the HQ OWNER for a corporation, rebuild
corporation state for the affected old/new owners' other cities after the global HQ
pointer has reached the new state.

The invariant is:

    if HQ ownership changes and a player's corporation activity can depend on HQ
    ownership, every affected branch city's corporation cache must match
    CvPlayer::isActiveCorporation before normal city/economy processing resumes.

Implementation should respect the city-kill path. `setHeadquarters(eCorp, NULL)` can be
called while the old HQ city is partway through `CvCity::kill`; blindly calling a full
old-owner `updateCorporation()` that includes the dying city may introduce reentrancy
risk. Safe approaches include:

* in `setHeadquarters`, snapshot old/new PlayerTypes and refresh the affected owners'
  OTHER cities while skipping the explicitly handled old/new HQ objects; or
* defer a narrowly tracked affected-player corporation rebuild to a stable point after
  city deletion/acquisition, while also covering generic non-capture HQ changes.

Do not fix only the new owner: losing the HQ under Protectionism is equally a TRUE->FALSE
transition for the old owner's surviving branches.

REGRESSION TESTS
----------------
A. Two-direction Cereal Mills capture test
1. Standard map; O and N both Protectionism.
2. Cereal Mills HQ belongs to O.
3. O1 and N1 are ordinary non-HQ branch cities with one qualifying Wheat each and stable
   plot groups away from the HQ city.
4. Force corporation caches current.
5. Assert before capture:
       O1 corporation FOOD == 1
       N1 corporation FOOD == 0
6. N captures HQ city.
7. Immediately assert dynamic policy:
       !O.isActiveCorporation(Cereal Mills)
        N.isActiveCorporation(Cereal Mills)
8. Without an explicit repair call, assert branch caches already match:
       O1 corporation FOOD == 0
       N1 corporation FOOD == 1
   Current code fails both.

B. Gameplay booking test
1. Configure O1/N1 with non-corporation food exactly equal to consumption.
2. Perform the HQ capture before N's end-of-turn city processing.
3. Repaired N1 gains exactly +1 food from Cereal Mills; current N1 gains 0.
4. On O's next city turn, repaired O1 gains 0 corporation food; current O1 still gains
   the stale +1 until an unrelated corporation refresh occurs.

C. Control without NoForeignCorporations
Repeat while both players use a civic where `isNoForeignCorporations()==false`.
HQ owner transfer should not change branch active status merely from owner identity.
This protects against gratuitous behavior changes outside the relevant dependency.

NUMBERING
---------
F491 / KI#814 is CONFIRMED.
F492 / provisional KI#815 is the next UNUSED root.

CURRENT STONE AFTER C031-WIP228
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F491 / KI#814 CONFIRMED.
F492 / provisional KI#815 UNUSED.
Latest physical checkpoint: C031-WIP228.
No repository source files modified.

NEXT
----
Continue from F492 unused.

First finish immediate F491 siblings without multiplying one invalidation omission into
artificial roots:
* HQ relocation with the SAME owner (should not change active-corporation policy);
* corporation-produced resource dependency cascades after HQ owner flip (same F491 root);
* old/new owner maintenance and building-commerce symptoms (same root unless a separate
  invalidation exists);
* HQ destruction / corporation replacement paths beyond ordinary capture, classifying
  them as same-root manifestations or clean controls.

Then resume the low-coverage signed health/happiness and `processBuilding` /
`processCivics` source-boundary counter sweep with F492 unused. Continue requiring
current data, ordinary producer, authoritative consequence, ancestry and semantic
full-album duplicate proof before promotion.

END C031-WIP228

===============================================================================
C031-WIP229 — QUEUE 004 CONTINUATION FROM F492 UNUSED
===============================================================================

POST-WIP228 CLOSURES BEFORE F492
--------------------------------
The immediate F491 corporation/HQ sibling pass was completed without splitting the
confirmed HQ-owner cache invalidation into artificial extra roots:

* `replaceCorporation` with a competing HQ is not reached by ordinary current SAS
  executive play. Executives reject competing-HQ targets; current corporations use the
  start/founding path, and `foundCorporation` skips cities that already contain an HQ.
  Remaining entry is Python/WorldBuilder/API robustness territory.
* Protectionism's current `iTradeRoutes = -1` is a valid offset against the inherited
  `INITIAL_TRADE_ROUTES = 1` baseline. The player counter stays nonnegative and removal
  restores the baseline.
* `killGoldenAgeUnits` calling `kill(true)` before animation/group access is safe under
  Civ4 delayed-death semantics and is already an album control; `kill(true)` does not
  immediately detach/delete the unit.
* Sampled signed health/happiness, military/support, conscription, free-specialist and
  related civic source counters continued to pair correctly or are dynamically consumed.
* The TeamShare area-count oddity remains the existing robustness issue; no new shipped
  consequence justified a root.

The AI Auto Play / human-identity sweep also closed several initial concerns:

* `CvPlayer::setIsHuman` does not itself call `updateHuman`, but
  `CvInitCore::setSlotStatus` immediately calls `GET_PLAYER(eID).updateHuman()` once
  player statics exist. There is no stale `m_bHuman` window from that setter.
* `AgentIterator` cached sequences do not classify human/AI status.
* `CvTeam::isHuman()` dynamically tests the current `isHuman()` state of the cached team
  leader. `updateLeaderID` intentionally uses InitCore's persistent human identity so the
  preferred human leader remains stable through Auto Play while team human-ness itself
  changes dynamically.
* `CvPlayerAI::AI_setHuman` refreshes first-impression attitudes and victory weights on
  the transition. UWAI city membership is rebuilt in the ordinary team/player `turnPre`
  cache update before war planning on the AI turn, so no independent persistent UWAI
  cache root was established from the human-status flip.

F492 / KI#815 — NO-SLAVERY SACRIFICIAL-ALTAR POP-RUSH ENTITLEMENT IS MIS-CACHED WHEN THE BUILDING IS COMPLETED DURING AI AUTO PLAY
-----------------------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited AdvCiv human-status/source-processing bug, live in current SAS.

ROOT
----
AdvCiv's No Slavery feature gives human players a special city-local population-rush
permission from a building with negative HurryAngerModifier. The source is processed in
`CvCity::processBuilding` only while the owner is CURRENTLY human:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) && kOwner.isHuman() &&
        kBuilding.getHurryAngerModifier() < 0)
    {
        changePopRushCount(iChange);
    }

That is source-time cached state. `CvCity::canPopRush()` later consumes the stored count:

    return (m_iPopRushHurryCount > 0 || GET_PLAYER(getOwner()).canPopRush());

and `m_iPopRushHurryCount` is serialized in the savegame.

AI Auto Play deliberately changes the active human to `isHuman()==false` by
`CvPlayer::setHumanDisabled(true)`. If the relevant building is completed while Auto
Play controls the player, `processBuilding(...,+1)` sees a non-human owner and therefore
does NOT add the building's No-Slavery pop-rush source.

When Auto Play ends, `setHumanDisabled(false)` restores human status, updates AI-facing
state and messages, but performs no city rebuild and no `m_iPopRushHurryCount`
recalculation. The building remains present while the city-local permission remains 0.

CURRENT SAS DATA / ORDINARY REPRODUCTION
----------------------------------------
Current SAS has a direct live producer:

    GAMEOPTION_NO_SLAVERY
        visible = true

    BUILDING_AZTEC_SACRIFICIAL_ALTAR
        BuildingClass = BUILDINGCLASS_MONUMENT
        PrereqTech = TECH_MYSTICISM
        Cost = 30
        HurryAngerModifier = -50
        ObsoleteTech = TECH_LIBERALISM

The No Slavery help explicitly states that human players cannot change to Slavery and
that ONLY the Aztec Sacrificial Altar allows humans to sacrifice population.

Deterministic gameplay sequence:

1. Start a normal human Aztec game with No Slavery enabled and remain in a non-pop-rush
   Labor civic.
2. Begin constructing a Sacrificial Altar but start AI Auto Play before completion.
3. The AI completes the Altar while `isHuman()==false` and `isHumanDisabled()==true`.
4. `CvCity::processBuilding` installs every ordinary building effect but skips
   `changePopRushCount(+1)` solely because the owner is temporarily non-human.
5. End Auto Play and return control to the same player.
6. The city owns a live, non-obsolete Sacrificial Altar with negative
   HurryAngerModifier, but `m_iPopRushHurryCount` is still 0.
7. With the player's civic not otherwise granting HURRY_POPULATION,
   `CvCity::canPopRush()` is false. The returned human therefore cannot use the exact
   exception promised by the No Slavery option.

This does not require a rare API producer, WorldBuilder mutation, save editing or an AI
valuation assumption. Building completion during current SAS Auto Play is an ordinary
shipped path.

PERSISTENCE / NO SELF-HEAL
--------------------------
`setHumanDisabled(false)` does not call `processBuilding` for existing cities/buildings
or otherwise rebuild `m_iPopRushHurryCount`.

The count is not dynamically recomputed from buildings by `canPopRush`; it is read
straight from stored city state. It is also serialized. Thus an Altar completed during
Auto Play can remain present and wrong after control returns, across turns and saves,
until some later source transition happens to process that building again.

The inverse source transition is vulnerable for the same reason: if a qualifying source
that had been counted while human is removed/obsoleted while the player is human-disabled,
the `iChange < 0` call is gated out too and can leave an entitlement that should have
been removed. The completion case alone is sufficient for confirmation and is the clean
regression target.

ANCESTRY
--------
This root is inherited from AdvCiv, not stock BtS/K-Mod's base rules.

Base AdvCiv 1.14 contains the same No Slavery building hook:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) && kOwner.isHuman() &&
        kBuilding.getHurryAngerModifier() < 0)
        changePopRushCount(iChange);

and the same `setHumanDisabled` human-status transition with no city pop-rush rebuild.
The No Slavery feature (`advc.912d`) was added by AdvCiv on top of the earlier AI Auto
Play machinery. Current SAS retains that architecture and supplies a live current
Sacrificial Altar with `iHurryAngerModifier = -50`.

DUPLICATE / RECONCILIATION CHECK
--------------------------------
The current full album was searched for No Slavery, `GAMEOPTION_NO_SLAVERY`, Slavery,
Sacrificial Altar, population rush and the human/Auto-Play combination.

The only direct No-Slavery numbered neighbor is F396 / KI#719, which is a player-context
TECH HELP contradiction: Masonry can advertise that it enables Slavery even though the
option blocks human adoption. F492 is authoritative runtime city state after an ordinary
building completion during Auto Play. It changes whether the player can actually hurry
production and persists in the savegame. It is not a help/UI duplicate.

No existing root in the reconciled album owns `m_iPopRushHurryCount` being conditioned
on transient `isHuman()` at source-processing time without a human-status invalidation.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
For the No Slavery building exception, treat the temporarily AI-controlled human as the
same policy subject as the human player. The minimal source-side repair is conceptually:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) &&
        (kOwner.isHuman() || kOwner.isHumanDisabled()) &&
        kBuilding.getHurryAngerModifier() < 0)
    {
        changePopRushCount(iChange);
    }

This keeps add/remove/obsolescence processing symmetric throughout Auto Play and avoids
needing a whole-city rebuild when control toggles.

The pattern is supported by AdvCiv's own old `getFoodKept` No-Slavery experiment, whose
comment explicitly warned that toggling human state during AI Auto Play could make
stored city data inconsistent and therefore used `(isHuman() || isHumanDisabled())`.
That old food-retention rule is disabled today, but the lifecycle principle applies
exactly to the still-live pop-rush counter.

Do not repair only Auto Play END: that would need to reconstruct source multiplicity and
would still leave state wrong during Auto Play. Keeping source processing stable across
human-disabled status is narrower and symmetric.

REGRESSION TESTS
----------------
A. Completion during Auto Play
1. Human Aztec; No Slavery enabled; Labor civic does not grant HURRY_POPULATION.
2. City has no Sacrificial Altar and `m_iPopRushHurryCount == 0`.
3. Start Auto Play.
4. Complete BUILDING_AZTEC_SACRIFICIAL_ALTAR while human-disabled.
5. Assert immediately that the city-local pop-rush source is 1 despite temporary AI
   control.
6. End Auto Play.
7. Assert `CvCity::canPopRush()` is true solely from the Altar exception.

B. Removal/obsolescence symmetry
1. Human Aztec with No Slavery and a counted Altar source.
2. Start Auto Play.
3. Trigger the ordinary building removal or obsolescence source transition.
4. Assert the city-local pop-rush count loses the source while human-disabled.
5. End Auto Play and assert no stale population-rush permission remains.

C. Control without No Slavery
Repeat the building add/remove with GAMEOPTION_NO_SLAVERY disabled. The special city
counter should remain untouched; ordinary civic-based population rushing continues to
be governed by the player's hurry counts.

NUMBERING
---------
F492 / KI#815 is CONFIRMED.
F493 / provisional KI#816 is the next UNUSED root.

OPEN SIBLING — NOT YET NUMBERED
--------------------------------
The same human-status sweep exposed a separate No-Slavery policy path that still needs
final classification before F493 is spent:

`CvPlayer::canDoCivics` blocks population-rush civics only when
`GAMEOPTION_NO_SLAVERY && isHuman()`. During Auto Play the active player becomes
non-human, `CvPlayerAI::AI_bestCivic` considers every `canDoCivics` civic, and current
SAS AI is demonstrably capable of selecting Slavery in ordinary autoplay tests. If the
AI adopts Slavery for the temporarily disabled human, `setHumanDisabled(false)` does not
validate or replace the now-human-forbidden active civic; `setCivics/processCivics`
leaves HURRY_POPULATION in the player's hurry count, so the returned human may be able
to whip despite the No Slavery option.

This is a strong candidate, but it is intentionally NOT F493 yet. Still to finish:
* exact current ordinary reproduction/control setup for the active human rather than a
  generic AI;
* whether any Auto Play end/controller hook normalizes civics indirectly;
* whether the intended option contract deliberately permits AI Auto Play to hand back a
  normally forbidden civic (unlikely, but needs historical/commentary control);
* semantic duplicate check against any broader option/state-transition root.

CURRENT STONE AFTER C031-WIP229
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F492 / KI#815 CONFIRMED.
F493 / provisional KI#816 UNUSED.
Latest physical checkpoint: C031-WIP229.
No repository source files modified.

NEXT
----
Finish the No-Slavery civic-adoption Auto Play sibling under F493 unused. Then continue
the remaining human/AI identity transition sweep for other source-time cached state
before returning to the low-coverage `processBuilding` / `processCivics` counter pass.

END C031-WIP229

================================================================================
C031-WIP230 CONTINUATION CHECKPOINT — Queue 004 CvPlayer.cpp — F493 CONFIRMED
================================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP229 with F493 / provisional KI#816 unused. This tranche
finished the No-Slavery civic-adoption sibling exposed by the AI Auto Play human-status
sweep. The candidate survived current-data, ordinary-producer, end-hook, ancestry and
semantic-duplicate checks. No repository source files were modified.

F493 / KI#816 — NO-SLAVERY AI AUTO PLAY CAN ADOPT SLAVERY FOR THE TEMPORARILY DISABLED HUMAN AND RETURN CONTROL WITH THE FORBIDDEN CIVIC STILL ACTIVE
------------------------------------------------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED — AdvCiv-introduced gameplay bug retained by AdvCiv 1.14 and current SAS.
The root combines AdvCiv's human-only No-Slavery legality predicate with the inherited
AI Auto Play mechanism that temporarily makes the human slot report `isHuman()==false`.

ROOT / CONTRACT
---------------
AdvCiv's visible No Slavery option explicitly says:

    Human players cannot change their civics to Slavery.
    Only the Aztec Sacrificial Altar allows humans to sacrifice population.

The runtime legality hook in `CvPlayer::canDoCivics` is:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) && isHuman())
    {
        FOR_EACH_ENUM(Hurry)
        {
            if (GC.getInfo(eCivic).isHurry(eLoopHurry) &&
                GC.getInfo(eLoopHurry).getProductionPerPopulation() > 0)
            {
                return false;
            }
        }
    }

For an ordinary human this correctly rejects `CIVIC_SLAVERY`, whose current XML grants
`HURRY_POPULATION` with positive production per population.

AI Auto Play deliberately changes that identity test. `CvGame::setAIAutoPlay` calls
`CvPlayer::setHumanDisabled(true)` for the active player; `setHumanDisabled` sets
`m_bDisableHuman`, calls `updateHuman`, and `updateHuman` then stores `m_bHuman=false`.
The same slot now has:

    isHuman()         == false
    isHumanDisabled() == true

Therefore the No-Slavery branch in `canDoCivics` is bypassed during Auto Play.

ORDINARY AI PRODUCER
--------------------
This is not merely an API route. `CvPlayerAI::AI_doCivics` runs for the temporarily
AI-controlled player and calls `AI_bestCivic` for each CivicOption. `AI_bestCivic`
considers every civic for which `canDoCivics` returns true, so Slavery becomes a normal
AI candidate while the human is disabled.

Current SAS data make the selection especially clean. `CIVIC_TRIBALISM` and
`CIVIC_SLAVERY` have the same Labor option, LOW upkeep, AI weight 0 and otherwise the
same gameplay fields; their only gameplay differences are:

    Tribalism: TechPrereq NONE, no Hurry source
    Slavery:   TechPrereq TECH_MASONRY, HURRY_POPULATION = true

`AI_civicValue` assigns a positive value to population rushing based on city count,
current production-per-population and growth threshold. Thus, once Masonry is known,
Slavery has a strictly higher civic value than Tribalism. In an ordinary several-city
empire the hurry term clears SAS's minimum civic-switch slack, so the disabled human's
AI can and does have a normal path to selecting Slavery. No Python, WorldBuilder or
forced `setCivics` call is needed.

HAND-BACK / PERSISTENCE PROOF
-----------------------------
The Auto Play end path does not normalize civics.

`AIAutoPlay.py::checkPlayer` calls `game.endAIAutoPlay(...)`. The DLL then executes
`CvGame::setAIAutoPlay(0, true, ...)`, which in single player calls only:

    GET_PLAYER(getActivePlayer()).setHumanDisabled(false);

`CvPlayer::setHumanDisabled(false)` restores `isHuman()==true`, updates AI-facing human
state and UI/replay bookkeeping, but it does NOT call `verifyCivics`, `setCivics`,
`processCivics` or any No-Slavery-specific validation.

The later Python fallback `if not pPlayer.isHuman(): ... setIsHuman(True)` normally does
not run for the surviving active human because the DLL has already restored human
status.

Consequently a player who adopted Slavery during Auto Play returns to human control
with:

    GAMEOPTION_NO_SLAVERY == true
    isHuman()             == true
    canDoCivics(SLAVERY)  == false
    getCivics(LABOR)      == CIVIC_SLAVERY
    HURRY_POPULATION source from Slavery still processed and active

The state is not permanent, but it is gameplay-significant. `CvPlayer::verifyCivics`
would replace the now-illegal civic, yet the normal `CvPlayer::doTurn` calls
`verifyCivics()` only AFTER gold/research processing and every city `doTurn`. More
importantly for scheduled Auto Play, the Python controller deliberately ends automation
on the preceding alive player's turn so that the disabled human is restored before its
next turn. The human therefore receives an ordinary active turn while Slavery is still
selected and can use the population-rush entitlement that the game option says humans
cannot obtain from civics. Verification happens only when that turn later completes.

COMPACT CURRENT REPRODUCTION
----------------------------
1. Start a human game with GAMEOPTION_NO_SLAVERY enabled.
2. Remain in CIVIC_TRIBALISM and acquire TECH_MASONRY. Use an ordinary developed empire
   with enough cities for Slavery's positive hurry valuation to clear the civic-switch
   slack; do not unlock a stronger competing Labor civic.
3. Start AI Auto Play for enough turns that the AI's civic timer permits a reevaluation.
4. While human-disabled, `canDoCivics(CIVIC_SLAVERY)` becomes true and AI civic choice
   can adopt Slavery through the normal `AI_doCivics -> AI_bestCivic -> revolution`
   path.
5. Let scheduled Auto Play end immediately before the human's next turn.
6. On hand-back, assert `isHuman()==true`, `canDoCivics(CIVIC_SLAVERY)==false`, but the
   selected Labor civic is still Slavery and `getHurryCount(HURRY_POPULATION)>0`.
7. Before ending that human turn, a city can population-rush through the civic source,
   contrary to the visible No-Slavery contract.
8. End the turn; the late `verifyCivics()` then selects a legal Labor civic. This late
   self-heal does not erase the already-exposed human turn or its possible whip.

ANCESTRY
--------
The underlying AI Auto Play identity mechanism predates AdvCiv and exists in K-Mod.
`GAMEOPTION_NO_SLAVERY` and the human-only `canDoCivics` restriction are AdvCiv feature
`advc.912d`; Base AdvCiv 1.14 contains the same `&& isHuman()` test and the same
`setHumanDisabled` hand-back without civic validation. Current SAS retains both.

Stock BtS/K-Mod do not have the No Slavery game option, so this exact root begins in
AdvCiv rather than stock BtS.

DUPLICATE / RECONCILIATION CHECK
--------------------------------
The full reconciled album was searched for No Slavery, Slavery, canDoCivics,
human-disabled, AI Auto Play civic changes and illegal/forbidden current civics.

* F396 / KI#719 is help/UI only: Masonry tech help can claim Slavery is enabled while
  the No Slavery option blocks human adoption.
* F492 / KI#815 is a distinct city-source cache bug: a Sacrificial Altar added/removed
  while human-disabled fails to update the city-local No-Slavery pop-rush counter.
* F459 / KI#782 also leaves an unavailable civic temporarily selected, but its root is
  loss of the final building-granted CivicOption source without immediate revalidation.
  F493 does not lose a civic source. Instead transient `isHuman()==false` changes the
  legality predicate itself and creates a new civic selection that should never have
  been legal for the same human policy subject. The common late `verifyCivics` cleanup
  is a shared consequence, not the root.

No prior numbered finding owns this human-status legality transition.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The narrow source-side repair is to treat a temporarily disabled human as human for the
No-Slavery policy test, matching the lifecycle principle already used elsewhere in
AdvCiv:

    if (kGame.isOption(GAMEOPTION_NO_SLAVERY) &&
        (isHuman() || isHumanDisabled()))
    {
        ... reject population-rush civics ...
    }

This prevents the invalid civic from being selected in the first place, keeps AI Auto
Play from changing the human's policy envelope, and is consistent with the old
`getFoodKept` advc.912d comment that explicitly used `(isHuman() ||
isHumanDisabled())` to avoid Auto-Play state inconsistencies.

A hand-back-only `verifyCivics()` would reduce the exposed turn but would be broader and
would still allow Auto Play to run the human civilization under a civic that the game
option specifically forbids that human player from changing into. Source-side policy
stability is cleaner.

REGRESSION TESTS
----------------
A. Auto Play legality
1. Human + No Slavery + Masonry + Tribalism.
2. Assert Slavery is illegal before Auto Play.
3. Enable Auto Play.
4. Assert Slavery remains illegal while `isHumanDisabled()==true`.
5. Let AI civic evaluation run and assert it cannot select Slavery.

B. Ordinary AI control
1. Separate genuine AI player + No Slavery + Masonry.
2. Assert Slavery remains legal for that AI, preserving the feature's documented
   human-only restriction.

C. Hand-back invariant
1. Run Auto Play across civic reevaluation.
2. End Auto Play before the human turn.
3. Assert every selected civic passes `canDoCivics` and no civic-derived
   HURRY_POPULATION entitlement exists unless supplied by an independently legal source.

NUMBERING / CURRENT STONE
-------------------------
F493 / KI#816 is CONFIRMED.
F494 / provisional KI#817 is the next UNUSED root.

Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F493 / KI#816 CONFIRMED.
F494 / provisional KI#817 UNUSED.
Latest physical checkpoint: C031-WIP230 (this file, appended after WIP229).
No repository source files modified.

NEXT
----
Continue the remaining human/AI identity-transition sweep under F494 unused. Search for
other state whose source processing or legality is conditioned on transient `isHuman()`
without `(isHuman() || isHumanDisabled())`, then return to low-coverage
`processBuilding` / `processCivics` counters if that seam closes.

END C031-WIP230


================================================================================
C031-WIP231 — F494/KI#817 confirmed; human/AI production-order hand-back seam
================================================================================

Continuation anchor
-------------------
Continued directly from C031-WIP230 with F494 / provisional KI#817 unused. The remaining
AI Auto Play / human-identity sweep found a K-Mod production-order transition whose
metadata is intentionally created only for human-selected Culture processes, but an
AI-created process can survive the human-disabled -> human hand-back without being
normalized. Current SAS data and ordinary AI production provide a live producer. No
repository source files were modified.

F494 / KI#817 — AI AUTO PLAY CAN HAND A HUMAN A CULTURE PROCESS WITHOUT K-MOD'S BORDER-EXPANSION STOP MARKER
----------------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED — K-Mod-introduced gameplay/production-control bug inherited by Base AdvCiv
1.14 and current SAS. It requires the existing AI Auto Play human-status transition and
an ordinary Culture process selected by the temporary AI.

ROOT / K-MOD CONTRACT
---------------------
K-Mod replaced BtS's direct "stop Build Culture when borders expand" behavior with
per-order metadata. When a Culture process is pushed, `CvCity::pushOrder` snapshots the
CURRENT culture level into `OrderData::iData2` — but only if the city is human:

    if (isHuman() && GC.getInfo(eProcess).
        getProductionToCommerceModifier(COMMERCE_CULTURE) > 0)
    {
        FAssert(iData2 == -1);
        iData2 = getCultureLevel();
    }

Later in `CvCity::doProduction`, an existing Culture process is stopped after a border
expansion only when that saved marker is positive and the city has advanced beyond it:

    if (kOrder.iData2 > 0 && GC.getInfo((ProcessTypes)kOrder.iData1).
        getProductionToCommerceModifier(COMMERCE_CULTURE) > 0 &&
        getCultureLevel() > kOrder.iData2)
    {
        popOrder(0, false, ALL_CHOOSE);
    }

That human-only producer is normally coherent. AI cities do not need the snapshot
because `CvCity::doProduction` deliberately calls `AI_chooseProduction()` whenever an
AI city is running a process. `AI_chooseProduction` clears/reconsiders the current
process and can push a fresh AI order each turn.

AI Auto Play creates a third state that the pair does not cover: the SAME city can have
an order created while `isHuman()==false` and then become an ordinary non-automated
human city while that order remains at the head of its queue.

HAND-BACK TRANSITION
--------------------
During AI Auto Play the active human slot has:

    isHuman()         == false
    isHumanDisabled() == true

If its AI selects `PROCESS_CULTURE`, `pushOrder` leaves the default `iData2 == -1`.
When scheduled Auto Play ends, `CvGame::setAIAutoPlay(0,true,...)` calls
`CvPlayer::setHumanDisabled(false)`. That restores human identity but does not clear,
re-push, dirty or normalize city production orders.

The Python end path also does no city-production normalization. `AIAutoPlay.py` calls
`game.endAIAutoPlay(...)`; after the DLL has restored the surviving active player to
human status, the later Python `if not pPlayer.isHuman()` fallback normally does not run.

The remaining generic production checks do not repair the metadata:

* `CvCity::doCheckProduction()` sees the existing `ORDER_MAINTAIN` as valid and does not
  open choose-production because the city already has production.
* `checkCanContinueProduction` only asks whether the process can still be maintained.
* `m_bAutoPlayJustEnded` is used for alert/message suppression, not queue normalization.
* Once human again, `CvCity::doProduction` no longer calls `AI_chooseProduction()` for a
  normal non-automated city, so the AI's per-turn process reconsideration disappears.

The inherited process therefore crosses the human-status boundary intact with
`iData2 == -1`.

CURRENT SAS DATA / CLEAN ORDINARY REPRODUCTION
----------------------------------------------
Current SAS has a live Culture process:

    PROCESS_CULTURE
        TechPrereq = TECH_DRAMA
        ProductionToCommerceModifier(COMMERCE_CULTURE) = 50

`CvCityAI::AI_bestProcess` evaluates every maintainable process. `AI_processValue`
explicitly adds the Culture conversion value for first-ring/low-culture expansion while
`getCultureLevel() <= 1`, and ordinary `AI_chooseProduction` has both a process-by-value
choice and a final process fallback that push:

    pushOrder(ORDER_MAINTAIN, eBestProcess);

The strongest differential setup uses CULTURE LEVEL 1 rather than level 0. A human who
manually starts Culture at level 1 receives `iData2 = 1`, and K-Mod's stop condition
therefore fires when the city reaches level 2. The temporary AI instead creates the
same process with `iData2 = -1`.

Compact reproduction:

1. Human player has TECH_DRAMA and a city at culture level 1.
2. Start AI Auto Play. Let the temporary AI select PROCESS_CULTURE through its ordinary
   city-production logic while the city remains at culture level 1.
3. Arrange the scheduled hand-back while the city is still below the level-2 culture
   threshold and the Culture process remains its head order. This is an ordinary timing
   state: AI process orders are re-evaluated/re-pushed each AI production turn.
4. Auto Play ends before the human's next active turn. The city is now human-controlled,
   non-production-automated and still running PROCESS_CULTURE, but its head order has
   `iData2 == -1` rather than the human marker 1.
5. On the returned human turn, `CvCity::doTurn` performs `doCulture()` before
   `doProduction()`. The process's culture can therefore raise the city from culture
   level 1 to level 2 on that turn.
6. `setCultureTimes100 -> updateCultureLevel` updates the level immediately. When
   `doProduction` runs afterward, the correct human-created order would satisfy
   `iData2 > 0 && getCultureLevel() > iData2` and be popped.
7. The inherited AI order has `iData2 == -1`, so the stop condition fails solely because
   the human stop metadata was never installed. Build Culture continues beyond the
   border expansion until the player notices and manually changes production (or some
   unrelated queue transition replaces it).

This costs real city production/commerce allocation and defeats the explicit K-Mod
human convenience behavior. It is not merely a hidden metadata discrepancy.

WHY AI'S NORMAL REEVALUATION DOES NOT SELF-HEAL AFTER HAND-BACK
---------------------------------------------------------------
At the start of `CvCity::doProduction`:

    if (!isHuman() || isProductionAutomated())
    {
        if (!isProduction() || isProductionProcess() || isChooseProductionDirty())
            AI().AI_chooseProduction();
    }

This explains why `pushOrder` originally needed the marker only for humans: a genuine
AI running a process comes back through `AI_chooseProduction` every turn. But it also
makes the Auto Play boundary decisive. Once `setHumanDisabled(false)` changes the city
back to an ordinary human city, that reevaluation branch stops immediately while the
last AI process order remains.

ANCESTRY
--------
Stock BtS/Civ4CE does not use this per-order marker. On culture-level increase it
contains the direct logic:

    //Stop Build Culture
    if (isProductionProcess())
    {
        if (GC.getProcessInfo(getProductionProcess()).
            getProductionToCommerceModifier(COMMERCE_CULTURE) > 0)
        {
            popOrder(0, false, true);
        }
    }

K-Mod replaced that direct pop with the current `pushOrder` culture-level snapshot plus
`doProduction` comparison, explicitly commenting that the new mechanism avoids the old
overflow/OOS problems. K-Mod also contains AI Auto Play and the human-disable mechanism.
Its `pushOrder` and `doProduction` have the same human-only marker architecture as
current SAS.

Base AdvCiv 1.14 retains both K-Mod snippets essentially unchanged and has its evolved
`setHumanDisabled` hand-back with no city-production normalization. Current SAS retains
the same architecture. Therefore this exact root begins with the K-Mod redesign rather
than stock BtS.

DUPLICATE / RECONCILIATION CHECK
--------------------------------
The reconciled album was searched for Culture process, `ORDER_MAINTAIN`, `iData2`,
border expansion, Build Culture and AI Auto Play. Existing process/help findings concern
process valuation/help or generic order semantics; none owns a Culture process created
while human-disabled crossing back to human control without K-Mod's stop marker.

F492 and F493 are nearby only because the same Auto Play identity transition exposed
them:

* F492 / KI#815 is a persistent city pop-rush source counter skipped while human-disabled.
* F493 / KI#816 is No-Slavery civic legality bypassed while human-disabled.
* F494 / KI#817 is production-order metadata whose producer intentionally differs for
  AI vs human, followed by an unhandled AI-order -> human-order ownership transition.

The roots and affected state are distinct.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not automatically change the `pushOrder` test to `(isHuman() || isHumanDisabled())`
without considering AI behavior. Unlike the No-Slavery rules, K-Mod intentionally gives
genuine AI process orders different semantics because AI production is reconsidered
every turn; forcing human stop metadata onto all temporary-AI process decisions could
change Auto Play AI behavior before hand-back.

The narrow semantic repair is at the hand-back boundary: when a disabled human becomes
human again, normalize any CURRENT head Culture process that came from AI semantics.
For a non-production-automated city with a maintain order whose Culture conversion is
positive and whose marker is negative, initialize its stop baseline to the city's
current positive culture level. That preserves K-Mod's human behavior from the moment
human control resumes without changing how the AI manages processes during Auto Play.

If implementation prefers a shared helper, the invariant to establish is:

    ordinary human + active Culture process + current culture level > 0
        => head-order iData2 is a valid baseline culture level

Care is needed not to overwrite a legitimate existing human marker and not to arm the
known level-0 case, because K-Mod's current stop test itself requires `iData2 > 0`.

REGRESSION TESTS
----------------
A. Differential human marker
1. Human city at culture level 1 with Drama.
2. Push PROCESS_CULTURE normally as human; assert head `iData2 == 1`.
3. Cross to level 2 through process Culture and assert the process is popped.

B. Auto Play hand-back
1. Same city/state, but let AI Auto Play queue PROCESS_CULTURE at level 1.
2. Before hand-back assert head `iData2 < 0` while human-disabled.
3. End Auto Play while Culture remains the head process.
4. Assert hand-back normalization establishes the human stop baseline at level 1.
5. On the next human turn let process Culture cross to level 2 and assert the process is
   popped exactly as for a human-created order.

C. Genuine AI control
1. Separate genuine AI city running PROCESS_CULTURE.
2. Ensure the repair does not force persistent human stop metadata onto that AI and its
   ordinary per-turn `AI_chooseProduction` behavior is unchanged.

D. Production automation control
1. Human city with production automation intentionally enabled.
2. Ensure any normalization policy does not conflict with governor/AI production
   semantics chosen for production-automated human cities.

NUMBERING / CURRENT STONE
-------------------------
F494 / KI#817 is CONFIRMED.
F495 / provisional KI#818 is the next UNUSED root.

Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F494 / KI#817 CONFIRMED.
F495 / provisional KI#818 UNUSED.
Latest physical checkpoint: C031-WIP231 (this file, appended after WIP230).
No repository source files modified.

NEXT
----
Continue the remaining human/AI identity-transition sweep under F495 unused, with special
attention to stateful order/queue metadata and source-time `isHuman()` gates. If that
seam closes, return to low-coverage persistent counters and source-boundary transitions
in `CvPlayer::processBuilding`, `CvPlayer::processCivics` and their city/team consumers.

END C031-WIP231


===============================================================================
CHECKPOINT C031-WIP232 -- QUEUE 004 CVPLAYER.CPP -- F495 / KI#818 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F494 / KI#817 CONFIRMED.
F495 / provisional KI#818 remains UNUSED.
No repository source files modified.

POST-F494 HUMAN/AI IDENTITY / PROCESSBUILDING / LOW-COVERAGE COUNTER PASS
------------------------------------------------------------------------
The continuation deliberately kept F495 unused while widening from F494's AI-Auto-Play
hand-back boundary. The remaining `isHuman()` sites and lightly named persistent counters
were challenged as transition-sensitive state rather than assumed buggy from surface
asymmetry.

1. RANDOM-EVENT `OtherPlayerAI` HUMAN-IDENTITY SEAM IS CURRENT-DATA MASKED.

`CvPlayer::canTrigger(EventTriggerTypes, PlayerTypes ePlayer, ...)` contains:

    if (isHuman() && kTrigger.isOtherPlayerAI())
        return false;

Caller orientation was re-traced: when one player searches for the trigger's other-player
target, it invokes `candidateOtherPlayer.canTrigger(..., initiatingPlayer, ...)`, so the
`isHuman()` predicate classifies the candidate target, not an accidentally inverted caller.
A namespace-aware scan of current `CIV4EventTriggerInfos.xml` finds no trigger with
`bOtherPlayerAI=1`. Therefore AI Auto Play cannot currently expose a shipped trigger whose
eligibility permanently differs through this flag. Generic/modmod sensitivity remains, but
there is no current F495 producer.

2. THE REMAINING HUMAN/AI GATES ARE MOSTLY TEMPORARY CONTROL-MODE POLICY, NOT CACHED STATE.

The broad `CvPlayer.cpp` / `CvCity.cpp` identity sweep closes the obvious neighboring
families:

* AI handicap research/growth/upkeep/worker/training modifiers deliberately apply while
  the player is AI-controlled;
* religion, event, production and diplomacy decisions become AI decisions while Auto Play
  owns the turn;
* human popups/reminders are deliberately suppressed, with comments explicitly accounting
  for Auto Play in several paths;
* production decay / AI production reevaluation differences are control-mode semantics;
  F494 was special because the AI-created Culture process survives the boundary into the
  later human regime without K-Mod's required human stop metadata.

No second persistent source comparable to F492/F494 was established from these generic
policy gates.

3. `processBuilding` / `processCivics` SOURCE-COUNTER RECONCILIATION REMAINS CLEAN APART
   FROM ALREADY-OWNED ROOTS.

The current building-effect producer set was re-enumerated against the player mutators and
the earlier WIP181/WIP182/WIP184 controls. Live sources either use dynamic consumers or
perform the needed cache/assignment refresh. Obsolescence replays building effects
symmetrically through `CvTeam::changeObsoleteBuildingCount` -> city `processBuilding`.

Current notable producers checked include:

* CivicOption grants: Colossus -> Government, Mausoleum -> Economy,
  Shwedagon Paya -> Legal (the WIP226 sentence claiming none exist was already corrected
  in WIP228); their LOSS-side problem is F459/KI#782, not a new root;
* global GP/GG/anarchy/Golden-Age/war-weariness modifiers;
* Statue of Liberty / Notre Dame free-specialist effects;
* Great Lighthouse coastal trade route and sea-plot yield effects;
* global health/happiness and Three Gorges Dam area clean power;
* specialist extra commerce/yield and state-religion building commerce.

The known state-religion building-commerce invalidation is F476/KI#799. TeamShare
area-counter noncanonical signed counts remain the earlier robustness note. No independent
F495 source/cache mismatch survived.

4. LOW-COVERAGE HISTORICAL / LIVE COUNTERS RECONFIRMED THEIR CONTRACTS.

* `m_iNumUnitGoldenAges` is intentionally monotonic. `CvUnit::goldenAge` consumes the
  required distinct Great People and then increments it once; `unitsRequiredForGoldenAge`
  uses the count to raise the price of later unit-triggered Golden Ages.
* `m_iHighestUnitLevel` is likewise a historical maximum, not a live-unit count; prior
  audit already covered its prerequisite use.
* unit/building class Count/Making arrays remain paired through unit init/kill and city
  push/pop/upgrade queue transitions (reconfirmed against the WIP226 class-Making pass).
* `m_aiImprovementCount` is paired by plot owner/improvement transitions; the separate
  F181 issue is its misuse as a build-capability proxy in CitySiteEvaluator, not a stale
  count producer in CvPlayer.
* `m_aiUpkeepCount` is dead legacy state guarded by `FErrorMsg`; current civic upkeep is
  derived from active civics and has no ordinary compiled mutator consumer.
* cities-lost is already inside F477's recycled-PlayerTypes identity family; it is not a
  fresh counter-lifecycle root here.
* StrikeTurns execution was already audited earlier; the thin mutator itself adds no new
  transition defect.

5. PLAYER-SLOT / TEAM-SLOT INDEXED ARRAYS ARE ALREADY SCRUBBED ON RESET.

A reset/serialization re-loop specifically challenged indexed player/team arrays. Current
`CvPlayer::reset` clears this player's arrays and also clears other players' entries keyed
by the recycled identity where necessary:

    other.m_aiGoldPerTurnByPlayer.resetVal(getID());
    other.m_abEverSeenDemographics.resetVal(getID());
    other.m_aiEspionageSpendingWeightAgainstTeam.resetVal(getTeam());

The espionage-weight recycle concern was already recorded as a clean control in WIP219.
No new recycled-slot alias was found in these arrays.

6. CITY-TRADE ANGER-TIMER COPY DURING AUTO PLAY IS NOT PROMOTED.

`CvPlayer::acquireCity` snapshots Hurry / Conscript / Defy-Resolution anger timers but,
for `bTrade`, AdvCiv copies them into the replacement city only when:

    isHuman() && (old owner's team is human || old owner is on our team)

A temporary human-disabled player therefore follows the AI branch and can receive a traded
city without those timers. This is persistent and initially resembles F492/F494.

The semantic challenge does not establish a hand-back invariant, however. The AdvCiv
change is explicitly transactional anti-exploit policy: it preserves timers only when the
RECIPIENT is human at the moment of the trade and the counterpart relationship makes a
reset exploitable. AI recipients intentionally get the AI transfer semantics. Auto Play
also legitimately lets that AI make irreversible city/diplomacy decisions. Unlike F492
(current-human building entitlement) and F494 (a surviving process later governed by human
queue semantics), there is no later calculation whose correctness says these historical
transfer timers must be reconstructed when control changes back. Treat this as intended
control-at-action-time behavior unless a stronger explicit Auto-Play contract or ordinary
reproduction proves otherwise; no F495.

7. `m_cancelingExport` IS TRANSIENT TRADE-UI BOOKKEEPING, NOT A PERSISTENT IDENTITY ROOT.

The list remembers resource exports whose cancellation popup is expected so the renegotiate
trade table does not temporarily hide the just-canceled resource. It is rebuilt from saved
CANCEL_DEAL diplomacy popups on load and cleared at player-turn end (explicitly because the
importer may have died). It does not drive authoritative trade state; the actual deal has
already been killed. A recycled PlayerTypes alias in this transient window was therefore
not promoted without a concrete authoritative consequence.

8. CVPLAYER RESET / SERIALIZATION COVERAGE CHECK FOUND ONLY INTENTIONAL TRANSIENTS.

A mechanical comparison of CvPlayer header members against reset/read/write found no new
persistent member omitted from both initialization and serialization. Fields absent from
serialization are expected transient/derived state such as `m_bDisableHuman`,
`m_bAutoPlayJustEnded`, popup-relaunch count, start-time/UI flags, replay-save state,
scoreboard expansion, message counts and bonus-help caches. `m_bHuman` itself is derived
from InitCore slot status via `updateHuman` rather than serialized as authoritative player
state.

NUMBERING
---------
F495 / provisional KI#818 remains UNUSED.

CURRENT STONE AFTER C031-WIP232
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F494 / KI#817 CONFIRMED.
F495 / provisional KI#818 UNUSED.
Latest physical checkpoint: C031-WIP232.
No repository source files modified.

NEXT
----
Continue from F495 unused. Move past the now-saturated Auto-Play identity cluster and
resume under-covered authoritative CvPlayer state. Prioritize one-shot provenance/event
state and cross-object counters with ordinary add/remove producers; challenge any
validate-one-object -> mutate-another-object or owner/team transition; then revisit later
large CvPlayer event/diplomacy helpers only where current data can make a stored-state
mismatch authoritative. Require current shipped reachability, consequence, ancestry and
full-album duplicate proof before assigning F495.

END C031-WIP232

=== C031-WIP233 continuation checkpoint (2026-09-02) ===

Status remains: Queue 004 CvGameCoreDLL/CvPlayer.cpp OPEN; 32/169 unique files complete; F446/KI#769 through F494/KI#817 CONFIRMED; F495/provisional KI#818 UNUSED; no repository source modified.

Post-WIP232 closures / controls:

1. Human-disabled defeat in CvPlayer::acquireCity is deliberate deferred cleanup, not an orphaned player-state bug.
   - acquireCity skips oldOwner.verifyAlive() only when the old owner isHumanDisabled(), with the local AdvCiv comment explaining that immediate Auto-Play defeat handling can crash when the Unit map layer is enabled.
   - The normal game update subsequently runs updateMoves() and then unconditional CvGame::testAlive(); testAlive calls verifyAlive() for every player.
   - CvPlayer::verifyAlive has a dedicated human-disabled Auto-Play defeat branch that transfers the disabled-human identity to another alive AI slot and kills the defeated slot before normal hand-back.
   - AIAutoPlay.py itself treats CvPlayer::verifyAlive as the normal transfer mechanism; its popup path is fallback behavior.
   - Therefore the acquireCity guard postpones elimination to the safe game-update phase rather than losing the lifecycle transition. No F495.

2. CvGame::setVoteSourceReligion has a generic cache-invalidation omission, but current ordinary SAS gameplay has no producer for the unsafe transition.
   - CvCity::processVoteSource caches vote-source free specialists and religion-building yield/commerce according to CvGame::getVoteSourceReligion().
   - setVoteSourceReligion merely replaces m_aeVoteSourceReligion and optionally announces; it does not remove old vote-source effects or add new ones. A generic API/Python change from one non-NO religion to another while a vote source is active could therefore leave old cached city effects and miss new ones.
   - Current compiled gameplay only assigns a vote-source religion when a state-religion vote-source building is first created and the stored religion is still NO_RELIGION. That assignment occurs before processBuilding/changeDiploVote activates the vote source, so the ordinary first-activation path caches the correct religion.
   - Full current source search found no shipped Python call that later changes an already-active vote source to a different religion. Existing ordinary membership/religion/diploVote transitions wrap processVoteSource correctly.
   - Preserve as API/robustness knowledge only; current shipped reachability does not justify F495.

F495/KI#818 remains UNUSED. Next: continue authoritative CvPlayer state with emphasis on current shipped producers, validate-one/mutate-another seams, owner/team identity boundaries, and later diplomacy/vote/event helpers while aggressively duplicate-checking against prior Queue 019 and event findings.


===============================================================================
C031-WIP234 — QUEUE 004 CVPLAYER.CPP — F495 / KI#818 STILL UNUSED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F494 / KI#817 CONFIRMED.
F495 / provisional KI#818 remains UNUSED.
No repository source files modified.

CONTINUATION AFTER WIP233
-------------------------
The pass continued from the saturated AI-Auto-Play identity cluster into later
CvPlayer vote/event/diplomacy state and persistent map/vector lifetime. The duplicate
threshold remained strict: rediscovered Team-event and Permanent-Alliance defects were
kept as controls rather than renumbered.

1. TEAM EVENT OCCURRENCE PROPAGATION INVERSION IS AN EXACT EXISTING CONTROL.

`CvPlayer::setEventOccured` still contains the reversed Team-event member predicate:

    for (MemberIter itOtherMember(getTeam()); itOtherMember.hasNext(); ++itOtherMember)
    {
        if (itOtherMember->getID() != getID())
            continue;
        itOtherMember->setEventOccured(eEvent, kEventTriggered, false);
    }

This only writes the occurrence back to self instead of the other team members. The
album already preserves this source defect repeatedly, and current SAS XML still has no
Team EventInfo producer that reaches it. No F495 duplicate was created.

2. DELAYED EVENT COUNTDOWN USE-AFTER-ERASE IS ALSO AN EXACT EXISTING DORMANT CONTROL.

`CvPlayer::doEvents` retains:

    EventTriggeredData const* pTriggeredData = getEventCountdown(eLoopEvent);
    ...
    applyEvent(eLoopEvent, pTriggeredData->m_iId);
    resetEventCountdown(eLoopEvent);
    aCleanup.push_back(pTriggeredData->m_iId);

`resetEventCountdown` erases the map node whose value address is `pTriggeredData`, so
the last dereference is invalid C++. This had already been fully recorded in the album;
current SAS event data does not provide a live AdditionalEventTime producer. No F495.

3. `setLoyalMember` VOTE-SOURCE CACHE ORDER RECONFIRMED CORRECT.

Defying or endorsing an AP/UN-style resolution changes `m_abLoyalMember`, and it was
worth rechecking whether city vote-source bonuses could survive stale. The setter is
explicitly transactional:

    processVoteSource(eVoteSource, false);
    m_abLoyalMember.set(eVoteSource, bNewValue);
    processVoteSource(eVoteSource, true);

`CvCity::processVoteSource` consults the player's current loyalty. Thus true->false
removes the old effects before the flag changes and does not re-add them; false->true
does not remove anything and then correctly adds them. No stale city source was found.

4. CANCELLED TRIGGERED VOTES LEAVE OLD `m_aVote` ENTRIES, BUT NORMAL FREE-LIST ID
   GENERATIONS PREVENT THEM FROM ALIASING A LATER VOTE.

A strong-looking candidate arose in `CvGame::doVoteResults`. If a resolution becomes
invalid between launch and result processing:

    if (!canDoResolution(eVoteSource, subdata))
    {
        ... election cancelled messages ...
    }
    ...
    deleteVoteTriggered(pVoteTriggered->getID());

Unlike the normal result path, this branch does not call `setVoteOutcome`, and therefore
does not clear each living player's `CvPlayer::m_aVote` entry for the cancelled vote.
This is genuine leftover state.

However, it does NOT create the initially suspected ordinary stale-ballot alias.
`m_votesTriggered` is an `FFreeListTrashArray`. Reusing a freed physical slot does not
reuse the same public ID. `FFreeListTrashArray::add()` assigns:

    pData->setID(m_iCurrentID + iIndex);
    m_iCurrentID += FLTA_MAX_BUCKETS;

while `CvPlayer::getVote` compares the complete stored integer ID. A later triggered
vote that reuses the same array index therefore has a different generation-bearing ID,
and the old `m_aVote` pair does not match it. This is untidy/stale storage and could be
cleaned on cancellation, but no current authoritative wrong vote was established.

This also refines the earlier dead-player stale-vote control: the free-list is recyclable
at the slot level, but ordinary generation-tagged IDs prevent direct old->new aliasing.
Do not describe the normal vote ID as simply reused without this qualification.

5. VOTE/RESOLUTION PLAYER HELPERS IN THIS BLOCK ARE PRIMARILY DYNAMIC CONSUMERS.

`getVotes`, `isVotingMember`, `isFullMember`, `canDoResolution`, `canDefyResolution`,
`canHaveTradeRoutesWith`, and the force-civic/religion helpers derive their decisions
from current player/team/city state. Apart from the separately recorded stale candidate
eligibility findings in Queue 019 and the vote-source religion API-only invalidation
note from WIP233, this re-loop did not expose a new CvPlayer-owned cached source requiring
F495.

NUMBERING
---------
F495 / provisional KI#818 remains UNUSED.

CURRENT STONE AFTER C031-WIP234
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F494 / KI#817 CONFIRMED.
F495 / provisional KI#818 UNUSED.
Latest physical checkpoint: C031-WIP234.
No repository source files modified.

NEXT
----
Continue from F495 unused. Leave the heavily saturated random-event and diplomatic-vote
families unless a new current-data producer changes reachability. Resume low-coverage
authoritative player state, especially one-shot historical/provenance fields and
cross-object counters whose source can migrate on city/unit/player ownership changes.
Then continue the remaining later CvPlayer execution/diplomacy helpers with the same
current caller + current data + consequence + ancestry + semantic duplicate gate.

END C031-WIP234


===============================================================================
C031-WIP235 — QUEUE 004 CVPLAYER.CPP — F495 / KI#818 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F495 / KI#818 CONFIRMED.
F496 / provisional KI#819 UNUSED.
No repository source files modified.

F495 / KI#818 — CONFIRMED
-------------------------
TITLE:
ADVCIV `acquireCity` INDEXES THE OLD-CITY BUILDING SNAPSHOT BY THE RECIPIENT'S
MAPPED BUILDING TYPE, SO ORDINARY CROSS-CIVILIZATION UNIQUE-BUILDING
CONVERSIONS DISAPPEAR ON CITY TRANSFER

ROOT
----
`CvPlayer::acquireCity` correctly snapshots the OLD city's concrete building types:

    FOR_EACH_ENUM2(Building, eBuilding)
    {
        aiNumRealBuilding.set(eBuilding, pOldCity->getNumRealBuilding(eBuilding));
        aeBuildingOriginalOwner.set(eBuilding,
                pOldCity->getBuildingOriginalOwner(eBuilding));
        aiBuildingOriginalTime.set(eBuilding,
                pOldCity->getBuildingOriginalTime(eBuilding));
    }

Later, while rebuilding the replacement city, it iterates each SOURCE building type
`eLoopBuilding`, derives its BuildingClass, and maps that class to the acquiring
civilization's concrete type:

    BuildingClassTypes eBuildingClass = GC.getInfo(eLoopBuilding).
            getBuildingClassType();
    BuildingTypes const eBuilding = (GC.getInfo(eLoopBuilding).isWorldWonder() ?
            eLoopBuilding : getCivilization().getBuilding(eBuildingClass));

That mapping is required: a foreign unique building must become the recipient's
civilization-equivalent building of the same class.

The AdvCiv/SAS refactor then uses the DESTINATION type `eBuilding` to read the OLD-city
snapshot:

    kNewCity.setNumRealBuildingTimed(eBuilding,
            ...
            kNewCity.getNumRealBuilding(eBuilding) +
            aiNumRealBuilding.get(eBuilding),
            false,
            aeBuildingOriginalOwner.get(eBuilding),
            aiBuildingOriginalTime.get(eBuilding));

When source and destination concrete BuildingTypes differ, the snapshot is normally
stored under `eLoopBuilding`, not `eBuilding`. The destination entry was absent from the
old city and is normally zero / unknown. `setNumRealBuildingTimed` therefore receives no
positive count; its `iChange == 0` early return performs no conversion.

The result is not merely lost provenance metadata. The building itself disappears.

DETERMINISTIC CURRENT-DATA REPRO — MAYA BALL COURT -> COLOSSEUM BY CITY TRADE
-----------------------------------------------------------------------------
Current SAS data has:

    CIVILIZATION_MAYA:
        BUILDINGCLASS_COLOSSEUM -> BUILDING_MAYA_BALL_COURT

while the default BuildingClass mapping is:

    BUILDINGCLASS_COLOSSEUM -> BUILDING_COLOSSEUM

Both current concrete buildings have `bNeverCapture=0` and `iConquestProb=66`.

Use an ordinary legal city gift/trade, which reaches:

    CvDeal::startTrade(TRADE_CITIES)
      -> CvCity::doTask(TASK_GIFT / TASK_CEDE)
      -> recipient CvPlayer::acquireCity(city, false, true, ...)

`bTrade=true` makes the example deterministic; no conquest-retention roll is involved.

Suppose Maya owns a non-capital city with one real Ball Court and transfers it to a
non-Maya civilization whose Colosseum-class mapping is the ordinary Colosseum.
The snapshot contains:

    aiNumRealBuilding[BUILDING_MAYA_BALL_COURT] = 1
    aiNumRealBuilding[BUILDING_COLOSSEUM] = 0

During restoration the source loop reaches `BUILDING_MAYA_BALL_COURT`, maps its class to
recipient type `BUILDING_COLOSSEUM`, and then incorrectly reads:

    aiNumRealBuilding.get(BUILDING_COLOSSEUM) == 0

instead of the source snapshot entry. The new city therefore receives zero Colosseums.
The Ball Court is gone with the killed old city, and no later acquisition pass converts
or reconstructs it.

The reverse direction fails symmetrically. If Maya receives an ordinary Colosseum city,
the source snapshot has Colosseum=1 and BallCourt=0; the mapped destination is Ball Court,
so the code reads BallCourt=0 and loses the class building rather than converting it.

CURRENT DATA BREADTH
--------------------
A full current SAS CivilizationInfo/BuildingClass scan finds 43 non-default concrete
BuildingType mappings, 36 of them on non-Barbarian civilizations. Therefore this is not
a one-off Maya-data quirk. Any transfer in which an actually present non-world building
maps to a different concrete type for the recipient exercises the same wrong snapshot
key.

Representative current major-civilization mappings include:

    Maya      Colosseum  -> Ball Court
    America   Courthouse -> Federal Courthouse
    China     Market     -> Changpingcang
    Celts     Forge      -> La Tene Smithy
    Ireland   Aqueduct   -> Muileann Uisce
    Korea     Library    -> Gyeongdang
    Rome      Market     -> Forum
    Zulu      Barracks   -> Ikhanda

The exact gameplay effect depends on whether the city transfer/conquest otherwise keeps
that building, but legal `bTrade=true` transfers provide a deterministic ordinary path.

NO NORMAL SELF-HEAL
-------------------
The old `CvCity` is killed before the replacement city is initialized. The only generic
building restoration is this `acquireCity` loop. `CvCity::setNumRealBuildingTimed`
returns immediately when the requested count equals the current count, and there is no
later BuildingClass reconstruction pass that notices the missing unique/base equivalent.
The transferred city therefore permanently loses the building until it is rebuilt by
normal gameplay.

ANCESTRY — ADVCIV REGRESSION
-----------------------------
Stock/BtS-lineage Civ4CE and K-Mod 1.46 both keep the source loop index for OLD-city data.
Their acquisition loop conceptually does:

    source = (BuildingTypes)iI;
    destination = recipient civilization building for source BuildingClass;
    ...
    iNum += paiNumRealBuilding[iI];
    setNumRealBuildingTimed(destination, ...,
            paiBuildingOriginalOwner[iI], paiBuildingOriginalTime[iI]);

They also test the source concrete building's `isNeverCapture` and
`getConquestProbability` while using the recipient building only for the destination
location/class checks.

Base AdvCiv 1.14 contains the same destination-key snapshot reads as current SAS. Thus
the confirmed count/provenance mix-up was introduced in the AdvCiv refactor lineage
between K-Mod and current base AdvCiv and then inherited by SAS; it is not stock BtS or
K-Mod behavior.

ADJACENT SOURCE/DESTINATION METADATA NOTE
-----------------------------------------
The same AdvCiv refactor also changed conquest policy lookups from the SOURCE concrete
building to the mapped DESTINATION `kBuilding`:

    kBuilding.isNeverCapture()
    kBuilding.getConquestProbability()

where K-Mod/BtS use the source type. Current major-civ UB mappings happen to match their
base buildings for these two fields, so the count-loss bug is the independently live
major-civ consequence. Current Barbarian Granary/Harbor/Lighthouse mappings do differ
(33% vs the normal 66% conquest probability), so once the count-key bug is repaired the
source/destination policy choice must also be restored or deliberately specified rather
than silently left on the recipient type. Treat this as an immediate F495 sibling, not
a separate F496 while the primary count bug masks its conversion consequence.

DUPLICATE / ALBUM CHECK
-----------------------
The reconciled album has no prior numbered root for `aiNumRealBuilding` being read under
the mapped recipient BuildingType during `acquireCity`, nor for unique/base building
conversion disappearing on ownership transfer.

One earlier Queue 003 city-bar control sentence stated that non-Wonder foreign UBs are
converted to the new owner's civilization-equivalent BuildingClass entry during
acquisition. That statement assumed the acquisition loop's intended behavior while
checking city-bar iteration; it did not inspect the source-vs-destination snapshot key.
C031-WIP235 explicitly SUPERSEDES that narrow control sentence. It is not a duplicate
finding.

Earlier acquisition findings involving state-religion building-commerce refresh,
TeamShare building effects, BuildingOriginalOwner PlayerTypes recycling and corporation
HQ transfer have different roots. F495 concerns the concrete building restoration key
itself.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Keep source and destination identities distinct throughout the loop.

Suggested shape:

    BuildingTypes const eSourceBuilding = eLoopBuilding;
    BuildingClassTypes const eBuildingClass =
            GC.getInfo(eSourceBuilding).getBuildingClassType();
    BuildingTypes const eDestBuilding =
            (GC.getInfo(eSourceBuilding).isWorldWonder() ?
            eSourceBuilding : getCivilization().getBuilding(eBuildingClass));

Then:

* read old-city count from `aiNumRealBuilding[eSourceBuilding]`;
* read original owner/time from the SOURCE entries;
* set the surviving count on `eDestBuilding`;
* use destination type for destination-validity/location and destination count;
* preserve/restore the inherited source-building conquest policy unless a deliberate
  design change is documented (`isNeverCapture`, `getConquestProbability`).

This is essentially the K-Mod/BtS source/destination separation expressed through the
AdvCiv enum-map refactor.

REGRESSION TESTS
----------------
A. Deterministic Maya -> normal trade:
1. Maya city has one Ball Court and no ordinary Colosseum concrete entry.
2. Legally gift/trade the city to a civilization using ordinary Colosseum.
3. Assert the replacement city has one real Colosseum.
4. Assert original owner/time metadata came from the Ball Court source record.

B. Deterministic normal -> Maya trade:
1. Non-Maya city has one ordinary Colosseum.
2. Legally transfer it to Maya.
3. Assert the replacement city has one real Ball Court.

C. Conquest source-policy test after A/B are fixed:
Use a source/destination pair whose conquest metadata intentionally differs (current
Barbarian Granary is a direct data control) and verify the chosen retention policy uses
the documented/source semantics rather than accidentally switching with recipient civ.

NUMBERING
---------
F495 / KI#818 CONFIRMED.
F496 / provisional KI#819 is now UNUSED.

CURRENT STONE AFTER C031-WIP235
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F495 / KI#818 CONFIRMED.
F496 / provisional KI#819 UNUSED.
Latest physical checkpoint: C031-WIP235.
No repository source files modified.

NEXT
----
Continue from F496 unused. First close the immediate `acquireCity` building-restoration
siblings so F495 symptoms are not split into fake findings: source/destination capture
metadata, free-building interactions, world/national/team wonder exceptions, maxed-class
and location checks, and building-class extra yield/commerce/happy/health restoration.
Then return to the remaining low-coverage CvPlayer source/provenance transitions.

END C031-WIP235

===============================================================================
C031-WIP236 — F495 EXACT REGRESSION HISTORY + F496 STARTING-PLOT CACHE CONTROL
===============================================================================

F495 / KI#818 — EXACT REACHABLE-HISTORY REFINEMENT
--------------------------------------------------
The reachable AdvCiv history now identifies the regression much more precisely than the
broad K-Mod-vs-AdvCiv ancestry proof in WIP235.

1. Practical commit 1582, `ad0fd03d9476e947b828098800d566c605ec896b`,
   "Walls and Castles are destroyed upon reconquest", refactored the acquisition building
   loop and changed conquest-policy lookups to the mapped destination `kBuilding`.
   Crucially, however, OLD-city count and provenance were still read with the SOURCE loop
   index:

       paiNumRealBuilding[iI]
       paiBuildingOriginalOwner[iI]
       paiBuildingOriginalTime[iI]

   Thus foreign-UB conversion itself was still correct after this commit.

2. Practical commit 2416, `b9b15b001b7f47a1bef8f264332988026f87dedb`,
   "Use EnumMap in CvPlayer; and other refactoring changes.", converted those raw arrays
   to EnumMaps. The old source-indexed reads became:

       aiNumRealBuilding.get(eBuilding)
       aeBuildingOriginalOwner.get(eBuilding)
       aiBuildingOriginalTime.get(eBuilding)

   while the same loop also repurposed/mapped `eBuilding` to the RECIPIENT's concrete
   building type. This is the actual source-vs-destination snapshot-key regression behind
   F495.

3. Practical commit 2438, `8aab6ddd80272abaf81e271207f05fc1d43d3690`,
   "Fix recently introduced freeze on city conquest", introduced a separate source loop
   variable `eLoopBuilding` and correctly changed the early source-count test to:

       aiNumRealBuilding.get(eLoopBuilding)

   but left the later restoration reads indexed by mapped destination `eBuilding`.
   The freeze was fixed while the quieter UB-conversion data-loss bug survived.

This exact history also explains why the current code looks almost correct: the source
identity is already available as `eLoopBuilding`; the final restoration block simply
uses the wrong one of the two concrete BuildingTypes for OLD-city data.

IMMEDIATE F495 SIBLING CLOSURES
-------------------------------
* `CvPlot::canConstruct(eBuilding)` in this acquisition context is the destination
  building-location/geometry validity test (water/river/min-area etc.), corresponding to
  the old K-Mod city-location check; it does not add a second source-key bug.
* BuildingClass extra yield/commerce/happiness/health snapshots are keyed by
  `BuildingClassTypes`, so source/destination concrete BuildingType remapping does not
  misindex those arrays. Their missing effect when a UB disappears is an F495 symptom.
* World wonders keep source == destination and are therefore not exposed to F495.
* Free-building state is conceptually separate: the acquisition snapshot intentionally
  records real buildings, while recipient player/trait/free-building sources govern the
  replacement city.
* `isProductionMaxedBuildingClass` is class-keyed and appropriate for this conversion.
* Source/destination original-owner/time loss remains part of F495's same wrong-key root,
  presently masked whenever the wrong destination count is zero.

F496 CANDIDATE CONTROL — setStartingPlot REBUILDS THE OLD AREA TOO EARLY
------------------------------------------------------------------------
`CvPlayer::setStartingPlot(pNewValue, true)` has an inherited ordering oddity. When an
existing start moves, it decrements the old area's starting-plot count and immediately
calls:

    GC.getMap().updateMinOriginalStartDist(pOldStartingPlot->getArea());

BEFORE `m_pStartingPlot` is changed. `CvMap::updateMinOriginalStartDist` clears the area
cache and then scans every player's current `getStartingPlot()`. The moving player still
points to the old plot, so the old-area rebuild reintroduces distances from a start that
is about to leave. If the new start is on another area, the second rebuild touches only
the new area; clearing a start to NULL has no second rebuild at all.

This ordering is inherited unchanged in substance from BtS/Civ4CE through K-Mod and
AdvCiv.

CURRENT-CONSEQUENCE CHECK
-------------------------
The stale-cache state is real, but current AdvCiv no longer has the old C++ AI consumer
of `CvPlot::getMinOriginalStartDist()`. In current SAS source, executable consumers are
only the shipped Highlands.py, Boreal.py and Rainforest.py start-assignment routines.
Those scripts consult the value while assigning previously-unassigned players and call
`setStartingPlot(..., true)` from a NULL starting plot; the bad OLD-area branch is not
entered.

Other shipped scripts do reassign existing starts with `bUpdateStartDist=true`:
* BTG_Lagoon.py and BTG_Grid.py swap starts during team-position normalization;
* Equal_Islands_V2_beta.py relocates already-assigned starts while copying the selected
  template island/region.

However, none of those scripts subsequently reads `getMinOriginalStartDist()`. Equal
Islands performs the move in `normalizeAddExtras`, after its assignment phase; the BTG
normalization/resource code likewise has no such consumer. The now-stale cache therefore
has no demonstrated shipped gameplay consumer after those moves.

`CvPlayer::startingPlotWithinRange`, still Python-exported, is also not a hidden consumer
in current SAS: AdvCiv reduced it to a compatibility wrapper returning false.

DISPOSITION
-----------
Do NOT number this as F496. Preserve it as an inherited robustness/API/cache-ordering
control. If future map code begins consuming `MinOriginalStartDist` after a cross-area
start move, the safe ordering would be to change `m_pStartingPlot` first and then rebuild
both distinct affected areas (or otherwise exclude the departing start from the old-area
rebuild).

NUMBERING / STONE
-----------------
F495 / KI#818 remains CONFIRMED.
F496 / provisional KI#819 remains UNUSED.
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN, 32/169 unique files complete.
No repository source files modified.
Latest physical checkpoint: C031-WIP236.

NEXT
----
Continue from F496 unused. Resume the low-coverage CvPlayer source/provenance sweep,
prioritizing state whose source can change eligibility, identity or ownership without
replaying its mutator rather than manufacturing findings from ordinary signed counters.

END C031-WIP236

======================================================================
C031-WIP237 — POST-WIP236 F496 CONTINUATION CHECKPOINT
======================================================================

STATIC-LOCAL HOIST RECHECK — DUPLICATE/CLOSED
---------------------------------------------
The post-WIP236 SAS-specific scan re-enumerated local `static` uses in current
`CvPlayer.cpp`, looking specifically for a game/player-dependent expression accidentally
frozen by a performance hoist. Executable local statics are overwhelmingly immutable
GlobalDefine reads, fixed color/type IDs or translation constants. The one mutable replay
name scratch `static CvWString` is explicitly reassigned on each call.

A mechanical comparison of the GlobalDefine names read by these statics against current
runtime `setDefineINT/FLOAT/STRING/BOOL` writers found no overlap. More importantly, album
reconciliation shows this exact source class was already audited and recorded in WIP181
(`CONTROL — STATIC LOCAL GLOBALDEFINE CACHES ALREADY MECHANICALLY AUDITED`). Therefore
this is a duplicate closure, not F496. Do not repeat it unless a genuinely game-state-
dependent static expression is found later.

TRUE STARTS / processCivics CONCRETE-BUILDING CACHE CANDIDATE — CURRENT DATA CLOSES
-----------------------------------------------------------------------------------
A narrower runtime-civilization-change concern survived WIP182's generic developed-player
`changeCiv` caveat long enough to deserve a current-data check.

`CvPlayer::processCivics` stores civic BuildingHappinessChanges / BuildingHealthChanges
against the CURRENT civilization's concrete BuildingTypes. Player initialization installs
initial civics before `CvGame::setInitialItems` reaches `TrueStarts::changeCivs()`, while
`CvPlayer::changeCiv` calls `resetCivTypeEffects(false)` and does not remove/reapply civics
for a living player. In principle, therefore, a pre-installed civic with a BuildingClass
modifier could leave its concrete-building cache keyed to the civilization that existed
before True Starts even though no city exists yet; later construction of a replacement
civilization's UB would then consult another concrete key.

The current shipped XML masks this architecture completely:
* all 38 current civilizations have the same five initial civics:
  CIVIC_DESPOTISM, CIVIC_BARBARISM, CIVIC_TRIBALISM, CIVIC_DECENTRALIZATION and
  CIVIC_PREHISTORIC_RELIGION;
* none of those five has any BuildingHappinessChanges or BuildingHealthChanges;
* the only current civic BuildingHappinessChanges are on CIVIC_FREE_SPEECH
  (University), CIVIC_NATIONHOOD (Colosseum/Courthouse/Jail), CIVIC_TRADE_BLOC
  (Custom House), and CIVIC_FREE_MARKET (Supermarket); those are not initial civics.

Therefore ordinary current True Starts cannot pre-cache one of these class-to-concrete-
building modifiers before changing civilization. The broader developed-player/debug
`changeCiv` weakness remains the WIP182 robustness caveat; no current shipped True Starts
root consumes F496.

ADDITIONAL POST-WIP236 RECONCILIATION CONTROLS
----------------------------------------------
* `CvPlayer::checkAlert` has a defensively wrong upper-bound guard (`iAlertID > size`
  rather than `>= size`), but current shipped Civ4lerts uses IDs 0..3 for four alert
  objects and the internal iterator is bounded. Preserve as API/robustness only.
* `showSpaceShip` observer-coordinate carry-over was rediscovered during low-coverage
  scanning but is exactly existing F464/KI#787; do not renumber it.
* the teammate research-overflow modifier asymmetry re-closes under current SAS data;
  the modifier differences required for a shipped consequence are disabled.

NUMBERING / STONE
-----------------
F495 / KI#818 remains CONFIRMED.
F496 / provisional KI#819 remains UNUSED.
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN, 32/169 unique files complete.
No repository source files modified.
Latest physical checkpoint: C031-WIP237.

NEXT
----
Continue from F496 unused with a coverage-driven low-mention stateful-function sweep.
Prioritize state that maintains a second derived invariant or whose source object can
migrate/disappear/change eligibility without replaying the owning mutator. Duplicate-gate
against the already large File-004 controls before numbering anything.

END C031-WIP237


===============================================================================
C031-WIP238 — QUEUE 004 CVPLAYER.CPP — HISTORICAL PARTIAL-FIX / LIFECYCLE CONTROL PASS
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F495 / KI#818 CONFIRMED.
F496 / provisional KI#819 UNUSED.
No repository source files modified.


CONTROL — COLONY CREATION WITH RANDOM PERSONALITIES PERFORMS A REDUNDANT DISCARDED
RANDOMIZATION, BUT ONLY SYNCHRONIZED RNG-STREAM CONSUMPTION SURVIVES
-------------------------------------------------------------------------------
A historical partial-fix audit found a real AdvCiv regression in the colony/player-
initialization chain, but the surviving consequence does not meet the present threshold
for F496.

Reachable-history practical commit 2423,
`386a1a5d3a44dc71e40ef82f4922f9ed8970bd6`, "Fix potential issues with Random
Personalities and liberation", added an explicit

    GET_PLAYER(eNewPlayer).changePersonalityType();

immediately before

    GET_PLAYER(eNewPlayer).initInGame(eNewPlayer);

inside `CvGame::addPlayer`.

Later practical 2565 / original-history 2576 repaired Random-Personalities
initialization by making `CvPlayer::initOtherData` call `changePersonalityType()`.
Current colony creation therefore performs:

    CvGame::addPlayer
      -> changePersonalityType()          // first randomization
      -> CvPlayer::initInGame
           -> reset
           -> initOtherData
                -> setAlive(true)
                -> changePersonalityType() // actual surviving randomization

The first randomization is discarded by `reset`. With the current SAS leader table it
nevertheless consumes the synchronized personality-selection draws (56 candidate
leader draws for the 57 current leader entries after the Barbarian exclusion), plus
any synchronized PeaceWeight randomization performed by the selected personality.

The non-reset-side-effect challenge closes the stronger interpretations:
* `reset`/`AI_reset` clears the personality-derived AI state written by the first pass,
  including PeaceWeight, espionage weight and victory-weight caches;
* the final personality is independently and validly randomized by the second pass;
* multiplayer peers consume the same synchronized RNG draws, so this is not a desync;
* the only surviving effect established is perturbation of the later synchronized RNG
  stream.

K-Mod does not contain the extra pre-`initInGame` randomization. This is therefore a
real AdvCiv redundant-RNG regression, but presently preserve it as a low-value
historical/control observation rather than manufacturing F496 from an otherwise valid
final player state.

A conservative cleanup would remove the obsolete `CvGame::addPlayer` pre-init
`changePersonalityType()` call and leave personality initialization owned by
`initOtherData`.


DUPLICATE CLOSURE — SERIALIZED/RESET PLAYER-SLOT SCRUB AUDIT WAS ALREADY WIP207
-------------------------------------------------------------------------------
A fresh serialization/reset inventory initially looked promising: recycled player
slots depend on `CvPlayer::reset` plus `resetPlotAndCityData` to clear player-indexed
state both inside `CvPlayer` and in external plot/city storage.

Semantic album reconciliation shows that C031-WIP207 already performed this exact
mechanical audit, including the external player-indexed plot/city arrays. That work
produced F477-F479 and preserved the clean fields. Do not repeat this surface as new
coverage merely because later function-name searches look sparse.

In particular, raw parent/player provenance and recycled-slot identity belong to the
existing F477 family unless a new independent consumer/root is proved.


HISTORICAL PARTIAL-FIX CLOSURES
-------------------------------
Several reachable-history fixes near known lifecycle/acquisition roots were challenged
for incomplete repairs.

1. Practical 2464, "Fix a couple issues with prev. owner in raze-city AI code":
   acquisition snapshots whether the recipient had ever owned the city BEFORE the
   transfer and passes that saved value into `AI_conquerCity`. The newly created city's
   own ever-owned state is correctly updated separately. The remaining EXE ABI wrapper
   uses the legacy default argument, but no independent ordinary DLL gameplay producer
   was established from it. No F496.

2. Practical 2450, `CvPlayer::applyEvent` recently-introduced bug repair: the repaired
   event-gold expression is narrow; the surrounding event family has already received
   extensive Queue-004 coverage. No partial-fix sibling established.

3. Practical 2536's yield/commerce rank-cache invalidation family is already owned by
   earlier cache work; the previously suspected F445 root was retracted precisely
   because the callee invalidates correctly.

4. Practical 3143's city-culture/city-trade refactor leads back to existing F460, whose
   execution-order root is the old city being killed before the full-radius culture
   conversion helper can use it. Do not duplicate.

5. Practical 4125 `findNewCapital` timing and the surrounding capital-loss lifecycle
   are already heavily audited and owned by earlier controls/findings.

6. Practical 4205's rare military-happiness counting repair is existing F455.

7. Practical 1936 player/team-ever-alive accounting is already covered by the later
   recycled-player/team and crowdedness-factor audits (including F473 and the WIP188
   colony-count control). No fresh root from re-reading the historical patch.

8. Practical 3254 map-regeneration/founding-city repairs sit inside map-regeneration and
   capital lifecycles that have already been deeply audited; no independent partial
   repair was established in this pass.


DUPLICATE CLOSURE — PRACTICAL 2208 DELAYED TAJ-MAHAL GOLDEN AGE
--------------------------------------------------------------
Practical 2208 introduced `m_iScheduledGoldenAges` so a wonder-completion Golden Age is
applied at the owner's next turn rather than during the end-of-turn production sequence.

This looked under-covered by literal function-name searches, but C031-WIP189 already
reconciled the delayed-GA counter, turn-activation application and serialization. WIP188
also covered the active Golden-Age modifier-rescaling arithmetic edge and recorded the
current-data-masked singularity sister.

Current `m_iScheduledGoldenAges` is serialized symmetrically, reset normally and
consumed/cleared from the existing turn-activation path. No independent practical-2208
partial repair was found. Preserve the prior control; do not reopen as F496.


CONTROL — PRACTICAL 2328 OTHER-PLAYER ANARCHY TRADE-ROUTE DEPENDENCY HAS A GLOBAL
INVALIDATION HOOK
-------------------------------------------------------------------------------
AdvCiv's `CvPlayer::canHaveTradeRoutesWith` deliberately makes another player's anarchy
invalidate trade with that player (including teammate trade except self-trade). Because
city trade-route partners are cached, this initially suggested a cross-player stale-
route root when B enters/leaves anarchy.

The complete transition closes it. `CvPlayer::changeAnarchyTurns` detects the actual
anarchy-state edge and performs:

    updateCommerce();
    updateMaintenance();
    updateTradeRoutes();
    updateCorporation();
    GC.getGame().updateTradeRoutes();

The global call iterates the living players and rebuilds their trade routes, so A's
cached routes to B are invalidated when B's anarchy status changes. This is an explicit
`advc.124` dependency repair, not a missing cross-player invalidator.

The same semantic family had already been noted earlier in the album; this pass adds the
historical practical-2328 connection rather than a new finding.


ADDITIONAL CONTROLS / REDISCOVERIES
-----------------------------------
* `CvPlayer::checkAlert` has the defensive `>` vs `>= size()` boundary hole, but shipped
  alert IDs are exactly 0-3 for the four-entry vector; API/robustness only.
* `showSpaceShip` observer-coordinate leak was rediscovered and is exactly existing
  F464 / KI#787.
* the teammate research-overflow modifier mismatch remains current-data closed under
  current SAS modifiers.
* `GET_TEAM(getID())` in starting-sight code is type-safe through the PlayerTypes
  overload; it resolves to the player's actual team even when PlayerTypes != TeamTypes.
* practical 2173's goody-map predicate fix and practical 2205's Advanced-Start `pPlot ==
  NULL` repair are historical one-line fixes; no current partial sibling established in
  this tranche.
* practical 2417's team-event required-building count repair is separate from the later
  AdvCiv EnumMap team-event propagation regression already preserved as a current-data-
  dormant control (no shipped `<bTeam>1</bTeam>` event).


CURRENT STONE AFTER C031-WIP238
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F495 / KI#818 CONFIRMED.
F496 / provisional KI#819 UNUSED.
Latest physical checkpoint: C031-WIP238.
No repository source files modified.

NEXT
----
Continue from F496 unused.

Do not return to the WIP207 serialized-slot sweep or the WIP188/WIP189 Golden-Age
families unless a genuinely new dependency appears.

Continue the coverage-driven/historical-partial-fix pass by tracing stateful
`CvPlayer` dependencies not already semantically owned by the album. Favor transitions
where one player's state is cached by another object/player, source identity changes,
or an AdvCiv fix moved responsibility between callers. Require ordinary current SAS
reachability, concrete consequence, ancestry, self-heal timing and semantic duplicate
proof before promotion.

END C031-WIP238


===============================================================================
C031-WIP239 — QUEUE 004 CVPLAYER.CPP — F496 CONFIRMED: CORPORATION-SUPPRESSION
CIVIC TRANSITIONS LEAVE OTHER PLAYERS' HQ BUILDING-COMMERCE CACHES STALE
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F496 / KI#819 CONFIRMED.
F497 / provisional KI#820 UNUSED.
No repository source files modified.


POST-WIP238 CONTROL — `setCurrentEra` DOES NOT ESTABLISH A LIVE STALE-UWAI CACHE
-------------------------------------------------------------------------------
The final current-era cache challenge was completed before widening again.

`CvPlayer::setCurrentEra` changes the authoritative era and immediately refreshes the
player's AI era factor. A few UWAI cache values are era-sensitive (for example building
production valuation and deployment-distance calculations), so the lack of a direct
full `UWAICache::update()` initially looked like a missing invalidation.

The consumer timing closes the stronger interpretation. UWAI's war/deployment cache is
rebuilt during the next team/player turn preprocessing before the corresponding war-
evaluation consumers execute. Normal research can cross into a new era later in
`CvPlayer::doTurn`, but sampled post-research city/unit/gameplay formulas read
`getCurrentEra()` dynamically rather than consuming the old cached UWAI era values. No
same-turn authoritative consumer of a stale era-sensitive UWAI field was established.

Preserve as a clean timing/invalidation control; no F496 from `setCurrentEra`.


POST-WIP238 CONTROL — HURRY-DERIVED COUNTER ARITHMETIC IS CURRENTLY UNIT-DELTA ONLY
-----------------------------------------------------------------------------------
`changeHurryCount` is generically fragile because on a zero/nonzero edge it changes the
player's derived population/gold-rush counter by the caller's full `iChange` rather than
normalizing the edge to +/-1. A magnitude greater than one could therefore inflate the
derived count.

Current compiled gameplay has exactly one producer, `processCivics`, and it supplies
only `+1`, `-1` or `0` from a civic boolean. The reconciled album had already proved the
same source constraint. Keep this as API/configuration robustness only; no F496.


POST-WIP238 CONTROL — HIRED TRADE-EMBARGO MEMORY ATTRIBUTION IS POLICY, NOT A TYPE BUG
--------------------------------------------------------------------------------------
The AdvCiv comment in `handleDiploEvent(DIPLOEVENT_STOP_TRADING)` wondering whether all
members of the requesting player's team should receive `MEMORY_HIRED_TRADE_EMBARGO`
looked suspicious in team games.

Reconstructing the event roles shows that `getID()` is the individual leader who made
the diplomatic request. Player-specific memory against that requesting leader is
internally coherent, and the analogous hired-war memory also attributes the request to
the requesting player rather than automatically to every teammate. Extending resentment
to the whole team would be a design-policy choice, not a correctness invariant. No F496.


F496 / KI#819 — CORPORATION-SUPPRESSION CIVIC TRANSITIONS CHANGE GLOBAL CORPORATION
LEVELS WITHOUT INVALIDATING OTHER PLAYERS' HQ BUILDING-COMMERCE CACHES
--------------------------------------------------------------------------------------
STATUS: CONFIRMED CURRENT GAMEPLAY BUG.
ANCESTRY: inherited Firaxis/BtS -> K-Mod -> Base AdvCiv -> SAS.
CURRENT SAS PRODUCERS: `CIVIC_PROTECTIONISM` and `CIVIC_STATE_PROPERTY`.

ROOT INVARIANT
--------------
Global corporation headquarters commerce is not based on raw branch presence. The
player accessor deliberately filters raw corporation counts through current corporation
activity:

    int CvPlayer::getHasCorporationCount(CorporationTypes eCorp) const
    {
        if (!isActiveCorporation(eCorp))
            return 0;
        return m_aiHasCorporationCount.get(eCorp);
    }

    bool CvPlayer::isActiveCorporation(CorporationTypes eCorp) const
    {
        if (isNoCorporations())
            return false;
        if (isNoForeignCorporations() && !hasHeadquarters(eCorp))
            return false;
        return true;
    }

`CvGame::countCorporationLevels` then sums exactly those FILTERED counts across all
living players:

    for (PlayerIter<ALIVE> itPlayer; itPlayer.hasNext(); ++itPlayer)
        iCount += itPlayer->getHasCorporationCount(eCorporation);

That world count is a direct input to corporation-HQ building commerce. For every active
building with `GlobalCorporationCommerce`:

    iCommerceRate +=
        GC.getInfo(eGlobalCommerceCorp).getHeadquarterCommerce(eCommerce) *
        GC.getGame().countCorporationLevels(eGlobalCommerceCorp) *
        getNumActiveBuilding(eBuilding);

The result is NOT recomputed on every query. `CvCity::updateBuildingCommerce` stores it
in serialized `m_aiBuildingCommerce`, and normal base-commerce calculation reads that
stored cache through `getBuildingCommerce(eCommerce)`.

MISSED INVALIDATION
-------------------
Raw religion/corporation population changes already establish the intended dependency.
`CvPlayer::changeHasReligionCount` and `changeHasCorporationCount` both call:

    GC.getGame().updateBuildingCommerce();

which rebuilds building commerce for every living player's cities because changing a
world religion/corporation level can alter somebody else's shrine/HQ income.

Changing corporation ACTIVITY through civics alters the same effective world-level
input but does not perform that global rebuild:

    void CvPlayer::changeNoCorporationsCount(int iChange)
    {
        ...
        m_iNoCorporationsCount += iChange;
        ...
        updateCorporation();
    }

    void CvPlayer::changeNoForeignCorporationsCount(int iChange)
    {
        ...
        m_iNoForeignCorporationsCount += iChange;
        ...
        updateCorporation();
    }

`CvPlayer::updateCorporation()` only loops that player's own cities. Each
`CvCity::updateCorporation()` does rebuild THAT CITY'S building-commerce cache, but it
does not call `CvGame::updateBuildingCommerce`. Therefore headquarters buildings owned
by OTHER players remain stale when this player's active branch count changes solely
because of corporation-suppression policy.

`processCivics` directly drives both mutators. `setCivics` / `revolution` contains no
later global building-commerce rebuild that closes the dependency.

CURRENT SAS DATA MAKES BOTH POLICY EDGES LIVE
---------------------------------------------
Current Economy civic XML has:

    CIVIC_PROTECTIONISM
        bNoCorporations        = 0
        bNoForeignCorporations = 1

    CIVIC_STATE_PROPERTY
        bNoCorporations        = 1
        bNoForeignCorporations = 0

Current SAS also has all seven corporation HQ buildings:

    BUILDING_CORPORATION_1 ... BUILDING_CORPORATION_7

with matching:

    GlobalCorporationCommerce = CORPORATION_1 ... CORPORATION_7
    ObsoleteTech              = NONE

and every current corporation has headquarters commerce:

    HeadquarterCommerces = [4 Gold, 0 Research, 0 Culture]

Thus every active corporation level is worth exactly +4 cached Gold in that
corporation's HQ building under current data.

DETERMINISTIC CURRENT-SAS REPRO — PROTECTIONISM, ONE FOREIGN CEREAL-MILLS BRANCH
---------------------------------------------------------------------------------
A minimal ordinary-game shape avoids any State-Property/HQ-owner local-refresh nuance:

1. Player A owns `CORPORATION_1` / Cereal Mills HQ.
2. Player B does NOT own that HQ and has exactly one Cereal Mills branch city.
3. B begins on a civic where foreign corporations are active.
4. All corporation and building-commerce caches are first allowed to settle.
5. B adopts `CIVIC_PROTECTIONISM` during B's active turn.

Before the switch, B contributes one level to:

    CvGame::countCorporationLevels(CORPORATION_1)

and A's HQ building therefore contains +4 Gold attributable to B's branch.

After the switch, the dynamic truth changes immediately:

    B.isNoForeignCorporations() == true
    B.hasHeadquarters(CORPORATION_1) == false
    B.isActiveCorporation(CORPORATION_1) == false
    B.getHasCorporationCount(CORPORATION_1) == 0

so the mathematically correct world corporation level falls by one and A's HQ building
should lose exactly 4 Gold.

B's `changeNoForeignCorporationsCount` calls `B.updateCorporation()`, correctly zeroing
B's own foreign-branch corporation output. A's HQ city is not part of B's city loop, so
A's `m_aiBuildingCommerce[GOLD]` remains +4 too high.

AUTHORITATIVE SAME-ROUND CONSEQUENCE
------------------------------------
This is not just stale display state.

`CvCity::getBaseCommerceRateTimes100` includes the stored:

    getBuildingCommerce(COMMERCE_GOLD)

and `CvPlayer::calculateGoldRate` consumes the player's city commerce. `CvPlayer::doGold`
then books that rate through `changeGold`.

`CvGame::setGameTurn` does perform a global `updateBuildingCommerce`, so there is an
eventual next-round self-heal. But a civic switch occurs after the current game-turn
refresh. In an ordinary sequential single-player game, use the human B as an earlier
player and the HQ owner A as a later AI player. B can adopt Protectionism before ending
its active turn; A's later `doGold()` in the same round can then book the stale extra
+4 Gold from B's now-inactive branch before the next `setGameTurn` refresh.

With N foreign B branches, the error is exactly:

    stale HQ Gold = +4 * N per affected corporation HQ

for that consumption window. Leaving Protectionism produces the reverse direction:
A's HQ can remain 4*N Gold too LOW until the next global building-commerce invalidator.

`CIVIC_STATE_PROPERTY` supplies the same root for every corporation branch of the
switching player. If an affected HQ belongs to another player, that HQ city's cache is
likewise outside the switching player's local `updateCorporation()` loop.

WHY THIS IS NOT F491 / KI#814
-----------------------------
F491 and F496 share the dynamic `isActiveCorporation` predicate but have different
missed inputs and different authoritative caches.

F491:
* producer/input change: HQ OWNER identity (`CvGame::setHeadquarters`);
* missed targets: old/new owners' OTHER branch-city corporation caches;
* stale state: corporation yield, corporation commerce, produced resources, maintenance;
* typical current exposure: HQ capture/trade while Protectionism is active.

F496:
* producer/input change: NoCorporations / NoForeignCorporations CIVIC activity state;
* missed targets: OTHER PLAYERS' HQ building-commerce caches;
* stale state: `m_aiBuildingCommerce`, specifically global corporation/HQ commerce;
* current exposure: ordinary Protectionism or State Property civic entry/exit with
  foreign branches contributing to another player's HQ.

The prior WIP179/WIP195/WIP226 closure that civic changes call `updateCorporation()`
remains correct for the switching player's OWN corporation-city output. F496 narrows the
missing second invariant: the same activity transition also changes GLOBAL corporation
levels, which require a global HQ building-commerce invalidation.

The album's F476 cache-control discussion explicitly notes that raw
religion/corporation count mutations call global `CvGame::updateBuildingCommerce()`.
F496 identifies an additional input to that same global formula that those earlier
controls did not test.

ANCESTRY
--------
This root is inherited from stock BtS.

Civ4CE/BtS has the same four required pieces:
* `getHasCorporationCount` returns zero when the corporation is inactive;
* `CvGame::countCorporationLevels` sums that filtered accessor;
* HQ building commerce multiplies `HeadquarterCommerce` by that world count;
* `changeNoCorporationsCount` and `changeNoForeignCorporationsCount` call only the
  player's `updateCorporation`, while raw `changeHasCorporationCount` explicitly calls
  global `updateBuildingCommerce`.

Stock current data already exposes it through:
* `CIVIC_MERCANTILISM` -> `bNoForeignCorporations=1`;
* `CIVIC_STATE_PROPERTY` -> `bNoCorporations=1`.

K-Mod 1.46 preserves the same architecture and live civics.
Base AdvCiv 1.14 preserves the same architecture; current SAS changes the foreign-
corporation civic from Mercantilism to Protectionism but retains the dependency.

The global next-game-turn self-heal (`CvGame::setGameTurn -> updateBuildingCommerce`)
is likewise inherited, which explains why the bug is transient but still economically
consumable within the round.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
When `changeNoCorporationsCount` or `changeNoForeignCorporationsCount` crosses a state
edge that changes corporation activity, rebuild global building commerce after the
player's corporation state has been updated, e.g. conceptually:

    updateCorporation();
    GC.getGame().updateBuildingCommerce();

The global call should occur only when the effective boolean state actually changes if
future code ever allows multiple stacked sources; current compiled producers are civic
booleans and therefore normal +/-1 edges.

Do not replace the local `updateCorporation()`: that rebuild is still needed for the
switching player's branch yields/commerce/resources/maintenance. F496 is the missing
SECOND invalidation for other players' global HQ building commerce.


CURRENT STONE AFTER C031-WIP239
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F496 / KI#819 CONFIRMED.
F497 / provisional KI#820 UNUSED.
Latest physical checkpoint: C031-WIP239.
No repository source files modified.

NEXT
----
Continue from F497 unused.

First inspect immediate F496 siblings without splitting symptoms:
* HQ-owner changes can also alter `countCorporationLevels` under Protectionism; classify
  any resulting stale HQ building commerce as an F491 sibling unless an independent
  producer/invalidation root is proved;
* raw `changeHasCorporationCount` and religion-count changes are clean controls because
  they already trigger global building-commerce rebuilds;
* check whether any other dynamic input to `countCorporationLevels` / global building
  commerce changes without the matching global invalidator.

Then resume the inherited state-mutator / responsibility-transfer ranking under F497
unused, prioritizing cross-player/global caches and source-identity transitions.

END C031-WIP239


================================================================================
C031-WIP240 - F497 / KI#820: domestic resource changes leave AdvCiv bonus-value cache stale
================================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F497 / KI#820 CONFIRMED.
F498 / provisional KI#821 UNUSED.
No repository source files modified.

SOURCE REFRESH NOTE
-------------------
This tranche switched the current-SAS basis to the user-supplied
`AdvCiv-SAS_light_source_20260902T232045.zip`. Its refreshed
`LLM_Helpers/context/commit_diffs` and renamed `mapscript_refs` are now the current
local history/reference basis. Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE remain ancestry
controls.

F497 / provisional KI#820 - CONFIRMED CURRENT ADVCIV CACHE-INVALIDATION REGRESSION
----------------------------------------------------------------------------------
TITLE
    Domestic/capital-network resource availability changes do not invalidate
    `CvPlayerAI`'s cached bonus values, even though AdvCiv made one bonus's value depend
    on whether OTHER substitute bonuses are currently available.

ROOT
----
`CvPlayerAI::AI_baseBonusVal` caches one value per BonusType in `m_aiBonusValue`
(and a trade variant in `m_aiBonusValueTrade`). AdvCiv practical commit 1291,
8493e0199b0dcf756b92ac0bd17351d7e0a4e88c (`advc.036`, "Toward v0.92"), explicitly
re-enabled K-Mod's disabled cross-resource dependency inside `AI_baseBonusUnitVal`:

    iOrBonusesWeHave += (ePrereqBonus != eBonus &&
            getNumAvailableBonuses(ePrereqBonus) > 0) ? 1 : 0;

K-Mod had deliberately left this line commented out and documented the reason: using
state-dependent `NumAvailableBonuses` inside a locally callable cached valuation could
make the cache unsafe / OOS-prone. AdvCiv's same `advc.036` work changed the cache policy:
in network multiplayer `AI_updateBonusValue` eagerly recomputes synchronized values;
in single-player it invalidates and lets later authoritative calls repopulate lazily.

That makes current resource availability a semantic input to EVERY cached resource whose
unit value includes OR-resource substitutes.

INVALIDATION ASYMMETRY
----------------------
AdvCiv does explicitly invalidate all bonus values for import/export changes:

    CvPlayer::changeBonusExport -> AI().AI_updateBonusValue();
    CvPlayer::changeBonusImport -> AI().AI_updateBonusValue();

and performs full turn-pre refresh/invalidation:

    CvPlayerAI::AI_doTurnPre -> AI_updateBonusValue();

Relevant civic/tech/team/lifecycle paths also have explicit refreshes.

Ordinary DOMESTIC resource availability, however, changes through the plot-group path:

    improvement / route / plot-group topology change
      -> CvPlot::updatePlotGroupBonus
      -> CvPlotGroup::changeNumBonuses
      -> CvCity::changeNumBonuses
      -> CvCity::processBonus / production verification

The complete current `AI_updateBonusValue` caller set contains no call in this domestic
connection/disconnection path. `CvCity::processBonus`, `CvCity::verifyProduction`, and
the plot-group rebuild likewise do not repair the player AI cache.

Thus resource trades are treated as cache-invalidating availability changes, but the
same effective availability transition caused by workers, pillaging/repair, routes,
plot-group splits/merges, free bonuses or capital-network topology is not.

CURRENT SAS DATA - COPPER / IRON SUBSTITUTE PROOF
--------------------------------------------------
Current UnitInfos contain many units with exactly:

    PrereqBonuses = BONUS_COPPER OR BONUS_IRON

including ordinary Axemen, Spearmen and Macemen plus several UUs (Vulture, Gallic
Warrior, Legionary, Immortal, Quechua, Samurai, Viking, Impi, Hoplite, Zhuge Nu,
Disciplined Crossbowman, Houfnice) and Privateer.

For such a unit, while evaluating Iron in `AI_baseBonusUnitVal`:

    iOrBonuses = 2

If Copper is NOT available:

    iOrBonusesWeHave = 0
    resource-enabling component = iBaseValue * 2 / (1 + 2 + 0)
                                ~= 2/3 * iBaseValue

Once Copper IS available:

    iOrBonusesWeHave = 1
    correct component          = iBaseValue * 2 / (1 + 2 + 2)
                                = 2/5 * iBaseValue

So connecting Copper materially reduces the marginal military value of Iron. This is
precisely the cross-resource redundancy effect AdvCiv intentionally uncommented.

DETERMINISTIC NETWORK-MULTIPLAYER SAME-TURN REPRODUCTION
--------------------------------------------------------
Network multiplayer supplies a particularly clean cache-population proof because
`AI_doTurnPre -> AI_updateBonusValue()` eagerly computes and stores every bonus value.
An ordinary game can include AI players, so no debug/API producer is required.

1. AI player P has the relevant military technologies but no connected Copper. Iron's
   cached `AI_baseBonusVal` is eagerly computed at P's turn start using
   `iOrBonusesWeHave == 0` for the Copper-or-Iron units.
2. P has a Worker with an already queued legal improvement/route mission that connects
   Copper during this active turn.
3. K-Mod/AdvCiv movement ordering gives land Workers priority 9. Ordinary fighting
   groups are centered around priority 65, so the Worker completes first.
4. The Copper connection updates plot-group/city bonus counts, making
   `getNumAvailableBonuses(BONUS_COPPER) > 0`, but no `AI_updateBonusValue` call occurs.
5. `CvPlayerAI::AI_unitUpdate` continues through later groups in priority order. Reserve,
   defender and related routines can call `CvUnitAI::AI_guardBonus`.
6. `AI_guardBonus` values candidate resource plots with:

       GET_PLAYER(getOwner()).AI_bonusVal(eBonus, 0)

   so an Iron guard/ranking candidate reads the still-cached PRE-Copper Iron value.
7. Correct Iron marginal value has already fallen through the current Copper-or-Iron
   unit dependencies from roughly 2/3 to 2/5 of each relevant base component. The stale
   higher value feeds `AI_guardBonus`'s threshold/ranking/path scoring and can therefore
   alter which resource plot a later military group guards or whether the routine acts.

The next turn-pre cache refresh eventually self-heals the value. The defect is a real
same-turn AI decision window, not an indefinitely sticky cache.

SINGLE-PLAYER EXPOSURE
----------------------
Single-player `AI_doTurnPre` resets values to -1 and later calls populate them lazily.
The same defect occurs whenever the affected bonus was evaluated earlier in the active
turn before the domestic connection/disconnection, then consumed again afterward. The
network-multiplayer sequence is used as the deterministic proof because it guarantees
prepopulation of every bonus cache entry without needing to force an earlier evaluator.

ANCESTRY / REGRESSION BOUNDARY
------------------------------
Stock BtS/Civ4CE does not have this exact AdvCiv cache/dependency combination.

K-Mod 1.46 already has the bonus-value cache, but deliberately DISABLES the
`iOrBonusesWeHave` availability dependency and leaves the warning explaining why.

AdvCiv practical 1291 (`advc.036`) explicitly uncommented that state-dependent term and
changed multiplayer cache handling so cached values are eagerly recomputed at explicit
invalidation points. Current Base AdvCiv 1.14 retains the re-enabled dependency and the
same missing domestic-resource invalidation. Current SAS retains it unchanged.

Classification: inherited Base-AdvCiv regression introduced when `advc.036` made the
cache depend on mutable cross-resource availability without covering the domestic
availability mutation family.

WHY THIS IS NOT AN EXISTING RESOURCE FINDING
--------------------------------------------
* F214/KI#537: UWAI `MilitaryBranch::m_eTypicalUnit` fails to clear when no current
  trainable candidate exists. Different cache and refresh contract.
* F321/KI#644 and neighboring CvPlayerAI findings: prospective trade/resource pricing
  uses the wrong pre/post-change state. F497 is stale persistent `m_aiBonusValue` after
  the authoritative availability state has already changed.
* F434/KI#757: resource-layer UI help (`m_aszBonusHelp`) is stale after resource state
  changes. F497 is authoritative AI valuation used by gameplay decisions.
* F491/F496: corporation activity/HQ cache invalidation. Different state family.

The reconciled WIP239 album contains no prior root for `m_aiBonusValue` missing domestic
capital-network availability invalidation.

SCOPE / SAME-ROOT SIBLINGS
--------------------------
Copper -> Iron is the clean shipped-data proof, but the root is not metal-specific.
Any domestic availability transition that changes `getNumAvailableBonuses(X)` can
change cached valuation of another bonus Y when a current unit treats X and Y as OR
substitutes. Plot-group splits/merges, pillage/repair, route completion/removal, free
bonus changes and similar capital-network transitions are same-root producers when they
change effective availability.

Do not split connection and disconnection directions into separate findings.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Invalidate/recompute the owning player's complete bonus-value cache whenever effective
CAPITAL-NETWORK bonus availability changes, because one changed bonus can alter the
cached value of OTHER bonuses.

Do NOT naively call the expensive full refresh inside every
`CvPlotGroup::changeNumBonuses`: K-Mod documents that plot-group rebuilding can make
those counts temporarily negative/intermediate, and network multiplayer eagerly
recomputes the whole cache. Hook the refresh at a stable post-recalculation boundary (or
track a dirty generation and rebuild before the next authoritative AI consumer) so the
cache sees the final synchronized resource state.

Preserve AdvCiv's multiplayer requirement that locally-triggerable help/UI calls cannot
install state-dependent cache values from an unsynchronized local path.

CURRENT STONE AFTER C031-WIP240
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F497 / KI#820 CONFIRMED.
F498 / provisional KI#821 UNUSED.
Latest physical checkpoint: C031-WIP240.
No repository source files modified.

NEXT
----
Continue from F498 unused.

First inspect immediate F497 siblings without splitting the same cache root:
* classify other `getNumAvailableBonuses`-dependent pieces inside `AI_baseBonusVal` and
  whether current domestic transitions can stale them;
* verify that imports/exports, tech/team and civic changes remain clean invalidation
  controls;
* look for another cached AI/player value whose semantic input is a capital-network or
  plot-group state transition but whose invalidation responsibility was only added for
  trade changes.

Then resume Queue 004's inherited state-mutator / cross-object responsibility ranking,
using the refreshed Sep-2 commit history and current source.

END C031-WIP240


================================================================================
C031-WIP241 - TEMP SAFETY CHECKPOINT AFTER F497; F498 STILL UNUSED
================================================================================

PURPOSE / SOURCE BASIS
----------------------
Temporary thread-safety checkpoint requested before the chat fills.

Authoritative current SAS source for this tranche:
    AdvCiv-SAS_light_source_20260902T232045.zip

This is the refreshed Sep-2 light source supplied by the user, including the expanded
commit_diffs material (roughly the previously missing 6100s through almost 6600s range)
and renamed mapscripts_refs material. Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE remain
ancestry/control sources.

No repository source files modified.

FINDING NUMBER STATE
--------------------
F497 / KI#820 remains CONFIRMED as frozen in C031-WIP240.
F498 / provisional KI#821 remains UNUSED.

This checkpoint records post-WIP240 investigation only. No candidate below is promoted
merely because the thread may fill.

POST-WIP240 CLOSURES / COVERAGE
-------------------------------

1. Ownership-maintained player mirrors/counters: no new root

Rechecked the player-owned mirror/counter family after F497:
* gold-per-turn-by-player live deal accounting and recycled-slot scrubbing;
* UnitClass / BuildingClass Count and Making arrays;
* improvement counts;
* related ownership-maintained mirrors.

These re-close against earlier album work (notably WIP207/WIP226/WIP232 and the deal
accounting passes). No F498.

2. K-Mod -> AdvCiv responsibility-removal scan: mostly responsibility moves

An automated comparison reduced the functions where an update/process/verify-style
call disappears by name to a small set (about 14 before semantic filtering). The
nontrivial samples inspected so far are either:
* renamed/moved responsibilities;
* already-owned album findings/caveats (e.g. setLastStateReligion, changeCiv, setTeam,
  acquisition families);
* clean initialization/setup refactors.

No second partial repair comparable to F495 has yet emerged.

3. Starting-area / starting-plot cluster: closes for current shipped use

The refreshed mapscripts_refs material was used to re-check findStartingArea(s),
startingPlotDistanceFactor and map-script consumers.

The old Python -1 compatibility behavior remains compatibility-only.
The non-strict >= sort comparator remains formally poor C++ ordering and is already
preserved in the album, but no deterministic shipped failure from equal-valued ties was
proved. No F498.

4. processBuilding / processCivics external-consumer replay: substantially saturated

A renewed pass focused only on effects whose meaning is consumed outside CvPlayer.
Fresh-looking candidates reduce to already-owned roots/caveats (including F459, F476,
F496, changeCiv concrete-BuildingTypes mapping caveats and TeamShare robustness) or
have a direct/dynamic invalidation path.

5. Permanent-Alliance setTeam score/rank -> attitude concern: closes

CvPlayer::setTeam can refresh attitudes while score/rank state still reflects the old
team arrangement, which initially looked capable of baking stale rank-based attitude.
However CvTeam::addTeam ends with forced CvGame::updateScore(true), and updateScore
recomputes ranks and then globally rebuilds AI attitudes. The intermediate calculation
cannot persist. No F498.

6. processTraits runtime replay: closes for this pass

Runtime trait removal/reapplication through changeLeader and initialization/reset was
rechecked. Live city-facing yield/commerce/trade-route/happiness effects have the needed
update paths.

Exceptional-looking pieces are already owned:
* natural-threshold comparator typo: existing current-data-masked robustness issue;
* founding/new-city extra-cost refresh: existing cache family already covered by the
  Rise & Fall finding.

No independent F498.

7. Pending-event city/unit ID reuse: generation-safe closure

EventTriggeredData stores integer city/unit IDs, and deleteCity/deleteUnit do not scrub
every pending event record. This superficially resembles stale-reference rebinding.

However cities and units use FFreeListTrashArray generation-bearing full IDs. Reusing
the same physical slot advances the public ID generation; getCity/getUnit -> getAt
checks the full ID. A later object in the same slot cannot satisfy the stale event ID.
This closes analogously to the earlier cancelled-vote-ID control. No F498.

8. Organized Religion / SpecialBuildingNotRequired queued-production candidate: closes

Current Organized Religion can make SPECIALBUILDING_MONASTERY not required, and
processCivics changes m_aiSpecialBuildingNotRequiredCount without immediately calling
verifyCityProduction(). A queued missionary therefore looked capable of surviving a
civic switch that removed its exemption.

Mandatory city-turn verification closes it: CvCity::doCheckProduction runs before
production, checkCanContinueProduction re-runs canTrain, and an invalid queued unit is
removed before that turn's production is applied. No F498.

9. War-weariness civic-modifier cache candidate: closes

m_iWarWearinessPercentAnger is a cached/base player value, but civic/player/city
WarWearinessModifier values are applied dynamically by CvCity::getWarWearinessPercentAnger.
Changing the civic modifier does not require rebuilding that cached base value. No F498.

10. setCurrentEra / UWAI era-cache challenge: closes for persistent state

The remaining era-sensitive UWAI values traced are refreshed before the next war/deploy
planning consumers. Ordinary post-research city/unit AI consumers sampled read current
era dynamically. No same-turn persistent old-era cache consequence was found.

CURRENT UNCONFIRMED F498 LEAD
-----------------------------

Capital identity -> stale city YIELD-RANK caches under CapitalYieldModifier

STATUS: STRONG CANDIDATE ONLY. NOT CONFIRMED. F498/KI#821 remains UNUSED.

Current CvPlayer::setCapital does:
* update capital-network plot-group bonuses when needed;
* change m_iCapitalCityID;
* update maintenance;
* update trade routes;
* call updateCommerce() and setInfoDirty(true) on old/new capitals.

It does NOT call CvPlayer::invalidateYieldRankCache().

This matters because CvCity::getBaseYieldRateModifier explicitly includes:

    if (isCapital())
        iModifier += GET_PLAYER(getOwner()).getCapitalYieldRateModifier(eYield);

and immediately documents the cache contract:

    player->invalidateYieldRankCache() must be called for anything checked here

The actual yield rate therefore changes dynamically and correctly as capital identity
moves. The potential stale state is the K-Mod/AdvCiv cached city yield ranking:
CvCity::getYieldRateRank / getBaseYieldRateRank store ranks behind
m_abYieldRankValid / m_abBaseYieldRankValid, and capital identity is one of the inputs
to the resulting getYieldRate through the modifier above.

Current SAS data makes the input LIVE:
CIVIC_BUREAUCRACY has CapitalYieldModifiers:
    YIELD_FOOD       0
    YIELD_PRODUCTION +50%
    YIELD_COMMERCE   +50%

So while Bureaucracy is active, moving the capital changes old/new capital production
and commerce-yield modifiers by 50 percentage points. setCapital explicitly refreshes
commerce accounting but currently appears not to invalidate cached YIELD RANKS.

UNFINISHED BEFORE PROMOTION
---------------------------
Need to prove before spending F498:
* ordinary shipped capital-relocation producer(s) and exact ordering;
* that at least one old/new city yield-rank cache can already be valid before the move;
* a real gameplay/AI/UI consumer can read the stale rank before some unrelated yield
  change invalidates it;
* self-heal cadence, if any;
* exact ancestry (BtS/K-Mod/Base AdvCiv/SAS) and regression/inheritance boundary;
* exhaustive semantic album duplicate search;
* whether commerce rank and yield rank interactions create same-root symptoms or are
  separately repaired by setCapital::updateCommerce().

Do NOT number this candidate merely because current Bureaucracy data makes it reachable.

CURRENT STONE AFTER C031-WIP241
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` - OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F497 / KI#820 CONFIRMED.
F498 / provisional KI#821 UNUSED.
Latest physical checkpoint: C031-WIP241.
No repository source files modified.

NEXT
----
Resume exactly at the capital-identity -> yield-rank-cache candidate under F498 unused.
Finish producer/consumer/self-heal/ancestry/dedup proof first.

If it closes, resume the historical "new state-dependent cached input / incomplete
invalidation" mining using the refreshed Sep-2 commit_diffs, then return to inherited
cross-object player mutators with external cached consumers.

END C031-WIP241


================================================================================
C031-WIP242 - F498 / KI#821 CONFIRMED; WIP241 CAPITAL LEAD CLOSED
================================================================================

PURPOSE / SOURCE BASIS
----------------------
Continuation from reconciled C031-WIP241 using the user's refreshed Sep-3 source:
    AdvCiv-SAS_light_source_20260903T070917.zip
with local ancestry controls:
    AdvCiv 1.14
    K-Mod 1.46
    Civ4CE/BtS

No repository source file was modified.

FINDING NUMBER STATE
--------------------
F497 / KI#820 remains CONFIRMED from C031-WIP240.
F498 / KI#821 is now CONFIRMED.
F499 / provisional KI#822 is UNUSED.

WIP241 CAPITAL-YIELD-RANK CANDIDATE - CLOSED / OLD F445 RETRACTION REDISCOVERED
--------------------------------------------------------------------------------
The WIP241 candidate was challenged before spending F498.

The candidate had argued that changing capital identity while Bureaucracy is active can
change old/new capital production and commerce yields without an explicit
CvPlayer::invalidateYieldRankCache() call in CvPlayer::setCapital.

That argument misses a transitive invalidator already proved when old F445/KI#768 was
retracted:

    CvPlayer::setCapital
        -> old/new capital CvCity::updateCommerce()
            -> GET_PLAYER(getOwner()).invalidateYieldRankCache()

Current CvCity::updateCommerce begins by invalidating the owning player's yield-rank
cache. Therefore the first old/new-capital commerce refresh in setCapital invalidates
all city yield/base-yield ranks before a persistent stale Bureaucracy capital rank can
be consumed.

This is exactly the reason F445 was previously retracted. The WIP241 lead is therefore
not new and does not consume F498.

F498 / KI#821 - CONFIRMED
-------------------------
Title:
K-Mod's city construction-value cache is cleared before AI research valuation but can
survive later same-turn commerce/policy changes and then be reused by city production
after the newly selected tech completes

ROOT
----
K-Mod introduced a city construction/building-value cache:

    CvCityAI::AI_buildingValue(...)
        if (bUseConstructionValueCache &&
            m_aiConstructionValue[eBuildingClass] != -1)
            return m_aiConstructionValue[eBuildingClass];
        ...
        m_aiConstructionValue[eBuildingClass] = iValue;

The cache is intentionally reused by neutral building-value calls. The player-level
turn setup clears it only once, before research selection:

    CvPlayerAI::AI_doTurnPre()
        AI_updateBonusValue();
        AI_updateCommerceWeights();
        [periodic AI_updateGreatPersonWeights()]
        AI_doEnemyUnitData();
        AI_ClearConstructionValueCache();
        if (isHuman()) return;
        AI_doResearch();
        AI_doCommerce();
        AI_doMilitary();
        AI_doCivics();
        AI_doReligion();
        AI_doCheckFinancialTrouble();

Thus values installed by AI_doResearch can survive all later state changes in
AI_doTurnPre.

RESEARCH POPULATES THE CACHE FOR BUILDINGS THAT ARE NOT YET TECH-UNLOCKED
-------------------------------------------------------------------------
The ordinary synchronized/randomized AI research path reaches:

    AI_bestTech
        -> AI_techValue(... bRandomize=true)
            -> AI_techBuildingValue(eTech, bAsync || !bRandomize, ...)

For the normal synchronous randomized path, bConstCache is false.
AI_techBuildingValue evaluates candidate-tech buildings while deliberately ignoring the
tech prerequisite and calls AI_buildingValue. Therefore it can install a construction
value for a building before the player actually knows the enabling tech.

MUTABLE INPUT AFTER CACHE POPULATION
------------------------------------
AI_buildingValue reads mutable empire state. A clean inherited input is the current
commerce slider. Its commerce-modifier valuation contains the live
getCommercePercent(eLoopCommerce) value.

But AI_doCommerce runs after AI_doResearch and can change those percentages, including:

    setCommercePercent(COMMERCE_RESEARCH, ...)
    changeCommercePercent(COMMERCE_GOLD, ...)

CvPlayer::setCommercePercent updates the actual commerce state, assignment/UI dirtiness
and human-side commerce-weight state, but it does not clear the AI construction cache.

A complete current-DLL search finds AI_ClearConstructionValueCache only at city
initialization/reset and the single pre-research player-turn boundary above; slider,
civic, religion and tech-acquisition transitions do not supply a second clear.

SAME-TURN CONSUMER
------------------
The stale value is not merely carried to an abstract future turn.

CvPlayer::doTurn runs AI_doTurnPre first. Later in that same turn CvPlayer::doResearch
applies current research and overflow, so the tech selected/evaluated earlier can
complete on that turn. City turns then run, and an empty/dirty production queue reaches:

    CvCity::doProduction
        -> CvCityAI::AI_chooseProduction
            -> AI_bestBuildingThreshold
                -> AI_buildingValue

A building that was tech-locked when its value was cached during AI_doResearch can now
be legally constructible. The ordinary neutral AI_buildingValue lookup can therefore
return the pre-slider/pre-policy cached value before the next turn's clear.

CLEAN INHERITED MARKET / CURRENCY REPRODUCTION
-----------------------------------------------
Market and Currency provide a stock/K-Mod-compatible example.

1. An AI begins a turn without a current research target.
2. AI_doResearch / AI_bestTech evaluates Currency and caches the Market value while
   Currency is still unknown.
3. AI_doCommerce subsequently changes the Gold/Research slider.
4. CvPlayer::doResearch later that same turn completes Currency from the current turn's
   research plus overflow.
5. A city with an empty/dirty queue reaches AI_chooseProduction.
6. Market is now constructible, but AI_buildingValue can return the value computed
   before the slider change.
7. That stale value can alter the building ranking and therefore the chosen production.

The slider contribution is numerically real. With 100 raw commerce, the K-Mod
flexibility term used by the building commerce-modifier valuation changes from roughly
4 at a floored 5% Gold share to 9 at a 10% Gold share. For a +25% Gold Market, that
piece alone changes building value by 5 points, before the larger direct base-Gold
change and other terms are included.

POLICY / RELIGION SAME-ROOT PRODUCERS
-------------------------------------
The root is broader than slider movement. AI_doCivics and AI_doReligion also run after
research-time cache population, while AI_buildingValue reads policy-sensitive state
such as health, state religion and specialist extra commerce.

Current SAS supplies additional live examples (e.g. Organized Religion extra health;
Representation specialist extra commerce). These are manifestations of the same cache
lifetime and must not be split into separate findings.

ANCESTRY
--------
Civ4CE/BtS does not contain m_aiConstructionValue / AI_ClearConstructionValueCache.
K-Mod introduces the construction cache and the relevant turn ordering. Its standard
Market/Currency data makes the slider reproduction live without SAS XML changes.
Base AdvCiv retains the K-Mod cache and ordering. Current SAS retains them and adds
further mutable valuation inputs.

F498 is therefore a K-Mod-introduced cache-lifetime defect inherited by AdvCiv and SAS.

DUPLICATE / ROOT SEPARATION
---------------------------
The reconciled album contains many building-value, civic, production-dirty and cache
findings but no prior root for m_aiConstructionValue surviving state changes after its
single pre-research clear.

F498 is separate from F497/KI#820, which concerns CvPlayerAI bonus-value cache
invalidation after domestic resource availability transitions.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Retain the existing pre-research clear because AI research valuation needs a fresh cache,
but ensure city production cannot consume values computed before later mutable player
state changes.

The simplest robust shape is a second AI_ClearConstructionValueCache() after the
commerce/civic/religion policy-changing work and before city production/other
post-policy consumers. More targeted invalidation is possible but must cover every
semantic input to AI_buildingValue and is therefore easier to make incomplete.

Do not remove the first clear merely to move it later; that would let research valuation
itself begin from previous-turn values.

UNNUMBERED CACHE-KEY ROBUSTNESS NOTE
------------------------------------
AI_buildingValue's cache key is only the building class even though the function also
has semantic flags such as bAllowRecursion, bIgnoreSpecialists and bObsolete.
AI_doHurry deliberately requests restricted variants.

A current shipped-data collision was not proved. Research-time entries are normally for
tech-locked buildings, and the current building dependency graph does not provide the
ordering/recursive chain required for a cached full-recursion child value to contaminate
a later bAllowRecursion=false recursive evaluation. Keep this as robustness debt rather
than allocating a finding.

CURRENT STONE AFTER C031-WIP242
-------------------------------
Queue 004 CvGameCoreDLL/CvPlayer.cpp - OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F498 / KI#821 CONFIRMED.
F499 / provisional KI#822 UNUSED.
No repository source files modified.

NEXT
----
Challenge immediate F498 siblings without splitting symptoms from the cache-lifetime
root, then resume state-dependent cache/invalidation mining under F499 unused.

END C031-WIP242


================================================================================
C031-WIP243 - POST-F498 SIBLING / CACHE CHALLENGE; F499 STILL UNUSED
================================================================================

PURPOSE / SOURCE BASIS
----------------------
Safety checkpoint after continuing from F498 on the Sep-3 source. This checkpoint also
supersedes the transient WIP242 file lost when the working runtime remounted the user
uploads; the full F498 durable text is included immediately above.

No repository source file was modified.

FINDING NUMBER STATE
--------------------
F498 / KI#821 remains CONFIRMED.
F499 / provisional KI#822 remains UNUSED.

F498 SIBLING - PERIODIC GREAT-PERSON WEIGHT REFRESH CAN READ THE SAME OLD
CONSTRUCTION CACHE BEFORE THE TURN'S CLEAR
-------------------------------------------------------------------------------
A second consumer strengthens F498 without creating a separate finding.

AI_doTurnPre orders the periodic Great Person calculation before the construction-cache
clear:

    AI_updateCommerceWeights();
    if ((gameTurn + strategyRand) % 3 == 0)
        AI_updateGreatPersonWeights();
    ...
    AI_ClearConstructionValueCache();

AI_updateGreatPersonWeights evaluates special Great-Person-created buildings through
AI_buildingValue. Several shipped examples are cost -1 special buildings with ordinary
tech prerequisites, including:

    Academy            -> Literature
    Jewish Shrine      -> Code of Laws
    Military Academy   -> Military Science
    Scotland Yard      -> Monarchy
    corporation HQs    -> their corporation-era techs

Because the construction cache is not cleared until after this periodic GP pass, a
neutral value installed during an earlier turn's tech/building evaluation can be read by
AI_updateGreatPersonWeights before the clear. This is another consequence of F498's
cache lifetime, not F499.

GREAT-PERSON WEIGHTS' THREE-TURN CADENCE - CONTROL / INTENTIONAL HEURISTIC LAG
-------------------------------------------------------------------------------
The Great Person weight map itself is cleared/recomputed only once every three turns.
That means mutable city/civic state can naturally change between refreshes.

K-Mod explicitly documents this cadence as a performance tradeoff:

    "GP weights can take a little bit of time,
     so lets only do it once every 3 turns."

Without a stronger violated correctness/invalidation contract, ordinary up-to-three-turn
heuristic lag is intentional policy, not an independent F499.

AI_buildingValue SEMANTIC-FLAG CACHE KEY - CURRENT SHIPPED COLLISION NOT PROVED
-------------------------------------------------------------------------------
The cache lookup does not include bAllowRecursion, bIgnoreSpecialists or bObsolete.
AI_doHurry can ask for restricted values, which superficially makes the key too coarse.

The live building prerequisite/free-building graph was checked for the ordering needed
to turn this into a shipped wrong-value collision. Current data does not provide a case
where a previously cached child both precedes its prerequisite in the relevant scan and
itself has recursive downstream value so that full-recursion and no-recursion values
actually differ at the collision. Keep this as hardening/robustness debt only.

AI_updateCommerceWeights AFTER AI_doCommerce - CONTROL
------------------------------------------------------
K-Mod comments that commerce weights are updated before Great Person weights and asks
whether they perhaps should be updated again after AI_doCommerce or when sliders change.
The current implementation was traced before treating that comment as evidence of a bug.

For current AI players:
* Gold and Research AI_commerceWeight values are calculated dynamically and read the
  current slider/financial state directly rather than coming from AI_updateCommerceWeights.
* The cached per-city Culture weight no longer infers an AI cultural-victory plan from
  the live Culture slider; current SAS intentionally keeps that slider inference for
  humans only, while AI culture targeting uses victory-strategy state.
* The cached player Espionage weight is derived from espionage-point, opponent, attitude
  and strategy state rather than the Gold/Research slider changed by AI_doCommerce.

Therefore the nearby comment currently describes heuristic/timing possibilities, not a
proved stale slider cache affecting an AI consumer. No F499.

NEXT CACHE FAMILY OPENED
------------------------
The audit is widening from the explicitly periodic/heuristic caches to state with a
stronger freshness or invalidation responsibility. The next targets include the
per-turn empire average/available-income caches and other K-Mod/AdvCiv cached state,
with special attention to transitions that occur after cache refresh but before a
consumer and to cache keys that discard per-object state.

Do not allocate F499 merely because a cache is intentionally sampled once per turn or
once every three turns; require a concrete violated contract, shipped producer and
consumer, and duplicate/ancestry proof.

CURRENT STONE AFTER C031-WIP243
-------------------------------
Queue 004 CvGameCoreDLL/CvPlayer.cpp - OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F498 / KI#821 CONFIRMED.
F499 / provisional KI#822 UNUSED.
Latest physical checkpoint: C031-WIP243.
No repository source files modified.

NEXT
----
Resume under F499 unused.

1. Challenge AI_updateCacheData's empire averages / available-income state only where a
   transition after the refresh creates a stronger-than-intended stale-state contract;
   plain one-turn heuristic sampling is a control.
2. Continue the inherited CvPlayer state-mutator / cross-object cache responsibility
   ranking from Queue 004, preferring ownership/source changes and omitted invalidators.
3. Reuse the K-Mod-vs-AdvCiv responsibility-removal comparison pattern that exposed
   prior partial repairs, and preserve closures rather than forcing a finding.

END C031-WIP243


================================================================================
C031-WIP244 - F499 / KI#822 CONFIRMED
================================================================================

F499 / KI#822 - GPT CHANGES IMMEDIATELY RUN AI_doCommerce WITHOUT REFRESHING
K-MOD'S CACHED AVAILABLE INCOME, SO THE EVENT-DRIVEN COMMERCE RESPONSE CAN USE THE
PRE-DEAL FINANCIAL-TROUBLE STATE
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED - K-Mod-introduced cache/event-response defect inherited by Base AdvCiv 1.14
and current AdvCiv-SAS. Civ4CE/BtS has the immediate GPT-triggered commerce response but
calculates financial trouble from live income instead of K-Mod's stored available-income
cache.

ROOT
----
`CvPlayer::changeGoldPerTurnByPlayer` mutates the authoritative recurring-gold state and
then deliberately asks an AI player to reconsider its sliders immediately:

    m_iGoldPerTurn += iChange;
    m_aiGoldPerTurnByPlayer.add(ePlayer, iChange);
    ...
    if (!isHuman() && isAlive())
        AI().AI_doCommerce();

Ordinary `CvDeal::startTrade` and `CvDeal::endTrade` use this mutator for
`TRADE_GOLD_PER_TURN`, so creating or cancelling a normal GPT deal is a shipped producer.

K-Mod changed `AI_isFinancialTrouble` to use:

    int iNetCommerce = AI_getAvailableIncome();

where `AI_getAvailableIncome()` is only:

    return m_iAvailableIncome;

and `AI_updateAvailableIncome()` explicitly incorporates the live GPT balance:

    m_iAvailableIncome = ...;
    m_iAvailableIncome += std::max(0, getGoldPerTurn());
    ...

The problem is lifetime. Current source has no refresh of `m_iAvailableIncome` in
`changeGoldPerTurnByPlayer` or in `AI_doCommerce`. The ordinary updater is called from
`AI_updateCacheData`, which runs at player turn activation. Thus a GPT deal created or
cancelled after that refresh changes the state represented by the cache and then invokes
the exact consumer that assumes the old cached value.

CLEAN +GPT FINANCIAL-STATE FLIP
-------------------------------
The receiving side gives the cleanest arithmetic because a positive GPT change raises
the correct available-income numerator while leaving the expense term unchanged.

Choose a normal non-Barbarian state in which the effective financial-trouble safety
threshold is 35 percent (no focus-war / repeat-research / avoid-science adjustment, and
an era/city-count state where the base threshold remains 35), treasury is low enough not
to trigger AdvCiv's large-cash safe override, and the turn-activation cache contains:

    m_iAvailableIncome = 100
    calculateInflatedCosts() + max(0, -getGoldPerTurn()) = 66

The cached state gives:

    funded = 100 * (100 - 66) / 100 = 34 percent
    margin = 34 - 35 = -1
    AI_isFinancialTrouble() = true

Now the AI receives an ordinary +20 GPT deal. The authoritative `getGoldPerTurn()` is
updated immediately. If `AI_updateAvailableIncome()` were recomputed, all other inputs
being unchanged, its explicit `max(0, getGoldPerTurn())` term raises available income to
120 while expenses remain 66:

    funded = 100 * (120 - 66) / 120 = 45 percent
    margin = 45 - 35 = +10
    AI_isFinancialTrouble() = false

But the call to `AI_doCommerce()` made from `changeGoldPerTurnByPlayer(+20)` still reads
the cached 100 and therefore executes as financially troubled.

CONCRETE COMMERCE-DECISION CONSEQUENCE
--------------------------------------
The stale boolean is not diagnostic-only. `AI_doCommerce()` consumes it immediately.

First, `AI_goldTarget()` scales the empire-wide unit-upgrade reserve by the financial
state. In peace and outside Culture stage 4, a raw 400-gold upgrade estimate contributes:

    stale trouble:   400 / 10 = 40 gold
    correct healthy: 400 /  4 = 100 gold

All common non-upgrade components can be denoted C. A post-deal full-science forecast
between C+40 and C+99 therefore produces opposite slider behavior: the stale call sees
its smaller gold target already covered, while the correctly refreshed call still enters
the Gold-increase loop. Such a forecast interval is directly reachable because the same
GPT payment changes `calculateGoldRate()` immediately.

There is an even more direct branch consequence later in `AI_doCommerce`: active control
of the Espionage slider requires `!bFinancialTrouble`. In any ordinary state with a
positive espionage target and the other documented gates satisfied, the stale pre-deal
`true` suppresses the allocation that the correct post-deal `false` state permits.

The event-driven recalculation can therefore choose different commerce percentages
solely because the GPT-dependent cache was not refreshed before the recalculation.

SHIPPED PRODUCER / TIMING
-------------------------
No special Python or WorldBuilder path is required.

`CvDeal::startTrade` handles `TRADE_GOLD_PER_TURN` with:

    kFromPlayer.changeGoldPerTurnByPlayer(eToPlayer, -trade.m_iData);
    kToPlayer.changeGoldPerTurnByPlayer(eFromPlayer, trade.m_iData);

and `CvDeal::endTrade` applies the reverse deltas. Each nonhuman living player therefore
runs `AI_doCommerce()` immediately when its annual payment changes.

The stale-cache window is ordinary:

1. player turn activation runs `AI_updateCacheData()` and stores available income;
2. later, a GPT deal starts or ends (during the AI turn or another player's diplomacy);
3. `changeGoldPerTurnByPlayer` updates the live GPT balance;
4. without refreshing available income, it calls `AI_doCommerce()`;
5. financial-trouble-sensitive slider logic reads the pre-change cached value.

If the deal is signed during another player's turn, the cache can be older still; that is
not needed for the root, because a same-turn post-activation deal already suffices.

ANCESTRY
--------
Civ4CE/BtS already contains the same immediate response in
`CvPlayer::changeGoldPerTurnByPlayer`:

    if (!isHuman())
        AI_doCommerce();

However BtS `AI_isFinancialTrouble()` derives its net commerce directly from live
`getCommerceRate(...)` and `getGoldPerTurn()` on each call. It has no
`m_iAvailableIncome` member.

K-Mod retains the BtS immediate `AI_doCommerce()` call but introduces:

* `m_iAvailableIncome`;
* `AI_updateAvailableIncome()`;
* `AI_getAvailableIncome()` in financial trouble; and
* the turn-activation `updateCacheData()` cadence.

That combination creates the stale event-response boundary. Base AdvCiv 1.14 preserves
it, adding only the alive guard around the immediate response. Current SAS retains the
same lifetime and financial-trouble dependency.

F499 is therefore K-Mod-introduced and inherited by AdvCiv/SAS, not a SAS regression.

DUPLICATE / ROOT SEPARATION
---------------------------
A full album semantic search found no prior root for GPT mutation leaving
`m_iAvailableIncome` stale during the immediate `AI_doCommerce()` response.

F499 is independent of:

* F338/KI#661 - AdvCiv's queued human-deal cancellation remains live during a later
  per-rival GPT-cap enforcement pass. That is transaction projection/order inside
  `AI_doDeals`, not the K-Mod available-income cache.
* F364/KI#687 - entering financial trouble can lose an intended production-dirty refresh
  when a shared cooldown suppresses it. F499 can make the observed trouble state itself
  wrong during GPT-triggered commerce recalculation before that edge scheduler matters.
* F365/KI#688 - unit-upgrade reserve estimation has a UnitType cache-key defect. F499
  merely uses the ordinary reserve scaling as one concrete downstream decision boundary.

REPAIR DIRECTION
----------------
At minimum, refresh the GPT-dependent available-income cache after changing
`m_iGoldPerTurn` and before the immediate AI commerce recalculation.

A targeted repair is preferable to recomputing all `AI_updateCacheData()` state:

    change live GPT state
    -> AI_updateAvailableIncome()
    -> AI_doCommerce()

The method's visibility/API shape may need a small wrapper if the refresh remains an
AI-private helper.

A broader transaction-aware design could defer the immediate `AI_doCommerce()` until a
whole deal has been installed/removed, which would also avoid reacting to a partially
applied multi-item deal. That is a sibling hardening direction and should not be required
to fix F499's narrower stale-GPT cache root.

CURRENT STONE AFTER C031-WIP244
-------------------------------
Queue 004 CvGameCoreDLL/CvPlayer.cpp - OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F499 / KI#822 CONFIRMED.
F500 / provisional KI#823 UNUSED.
Latest physical checkpoint: C031-WIP244.
No repository source files modified.

NEXT
----
Resume under F500 unused.

1. Define the F499 sibling boundary around deal installation/removal: test whether the
   per-item immediate `AI_doCommerce()` can observe a partially applied multi-item deal
   in a way that changes a real later decision independently of stale available income.
2. Continue K-Mod per-turn empire caches, but promote only event-driven consumers whose
   code explicitly requests immediate reconsideration after a state mutation.
3. Return to the Queue-004 mutator-vs-invalidator comparison, prioritizing cross-object
   and owner/source transitions rather than intentional one-turn heuristic sampling.

END C031-WIP244

================================================================================
C031-WIP245 ADDENDUM — Queue 004 CvPlayer.cpp / CvDeal transaction sibling
F500 / KI#823 CONFIRMED; F501 / KI#824 UNUSED
================================================================================

F500 / KI#823
Title:
  A GPT item can run AI_doCommerce in the middle of a multi-item deal before a
  later resource item changes happiness, leaving the AI's commerce sliders based
  on a partial transaction state.

Root:
  CvDeal applies trade bundles item-by-item. All non-vassal items in the first
  player's list are started before the second player's list. TRADE_GOLD_PER_TURN
  has an unusual synchronous side effect: changeGoldPerTurnByPlayer immediately
  calls AI_doCommerce for each living AI whose GPT changes. Later
  TRADE_RESOURCES items update bonus import/export state, plot-group bonus counts,
  city bonus happiness/health and AI bonus valuation, but do not rerun
  AI_doCommerce. Thus a transaction can commit a slider decision while only the
  first half of the accepted bundle exists.

Current SAS source path:
  CvDeal::addTradeItems
      first-player list:
          startTrade(item, first, second, ...)
      then second-player list:
          startTrade(item, second, first, ...)

  CvDeal::startTrade(TRADE_GOLD_PER_TURN)
      kFromPlayer.changeGoldPerTurnByPlayer(eToPlayer, -GPT)
      kToPlayer.changeGoldPerTurnByPlayer(eFromPlayer, +GPT)

  CvPlayer::changeGoldPerTurnByPlayer
      ... mutate live GPT state ...
      if (!isHuman() && isAlive())
          AI().AI_doCommerce();

  CvDeal::startTrade(TRADE_RESOURCES)
      kFromPlayer.changeBonusExport(bonus, +1)
      kToPlayer.changeBonusImport(bonus, +1)

  CvPlayer::changeBonusImport
      rebuilds plot-group bonus state
      AI().AI_updateBonusValue()
      // no AI_doCommerce

  The same first-list-then-second-list ordering exists in killSilent/endTrade, so
  teardown can expose the reverse partial-state shape as well.

Ordinary AI-AI producer:
  CvPlayerAI::AI_proposeResourceTrade can have AI A seek a resource from AI B.
  The counterproposal machinery is explicitly allowed to add compensation from A,
  including TRADE_GOLD_PER_TURN. The accepted lists are then passed as:

      kGame.implementDeal(A, B, weGive, theyGive)

  Therefore an ordinary result can be:
      A / first list:  GPT payment
      B / second list: happiness resource

  No malformed save, Python-only producer or human-human-only bundle is needed.

Concrete shipped-data reproduction:
  Use an AI A that has researched Drama, is not pursuing a Culture victory stage
  that adds its own slider target, is not avoiding science, and has one city with
  average culture pressure 100.

  Current SAS data:
      COMMERCE_CULTURE iInitialHappiness = 10
      TECH_DRAMA makes COMMERCE_CULTURE flexible
      BONUS_GOLD has iHappiness = +1 and is tradable after Mining

  CvCity::getCommerceHappinessByType computes:
      getCommerceHappinessPer(CULTURE) * culturePercent / 100

  Hence 10% Culture produces +1 happiness in an ordinary city.

  Arrange A's city to have exactly one angry citizen before receiving Gold and no
  other Culture-slider target. A agrees to pay B GPT for B's Gold resource.

  1. A's first-list GPT item executes.
  2. changeGoldPerTurnByPlayer immediately calls A.AI_doCommerce().
  3. Gold has not been imported yet, so angryPopulation() is still 1.
  4. AI_doCommerce computes the happiness target as
         1 * 100 / 10 = 10%
     and sets Culture to 10% (with the normal 10-point commerce increment).
  5. The second-list Gold item then executes.
  6. changeBonusImport -> plot-group/city bonus processing grants the +1 resource
     happiness; the correct final-deal happiness target is now 0% Culture.
  7. No final AI_doCommerce call occurs, so A exits the deal transaction at 10%
     Culture despite the completed deal no longer requiring that allocation.

Real consumer before self-heal:
  AI-AI deal proposals occur in AI_doTurnPost, after doGold, doResearch and city
  turns. The wrong slider can therefore survive the turn boundary.

  A particularly clean sequence is to let A finish its final queued technology
  earlier in that same turn. CvTeam::setHasTech pops the acquired technology from
  the research queue; current SAS explicitly documents that a completed final
  queue target normally leaves currentResearch == NO_TECH until the AI chooses
  again.

  On A's next turn:
      CvPlayer::doTurn
          verifyDeals()
          AI_doTurnPre()
              AI_ClearConstructionValueCache()
              AI_doResearch()   <-- consumer
              AI_doCommerce()   <-- eventual self-heal

  Thus AI_doResearch chooses a new technology while the stale 10% Culture /
  reduced Research allocation is still authoritative. This does not depend on
  F498's stale construction cache: that cache has just been cleared.

  AI_chooseResearch -> AI_bestTech -> AI_techBuildingValue -> AI_buildingValue
  reads live commerce percentages. In the general building-value commerce-
  modifier term K-Mod explicitly uses getCommercePercent(eLoopCommerce) when
  inflating base commerce for slider flexibility. Current Universities, for
  example, have +25% Research and are unlocked by Broader Education. With a city
  producing 100 raw commerce and no complicating modifiers, 90% Research gives a
  research base of roughly 90 + 9 flexibility = 99 for this term, whereas the
  correct 100% Research state gives 100 + 0 = 100. The +25% modifier therefore
  contributes 99 versus 100 before later tech-value scaling. Across relevant
  cities, or at an otherwise close tech comparison, the wrong post-deal slider
  can change the selected research target.

  The exact University is only a compact numerical witness; AI_bestTech has many
  other slider-dependent building valuations, so the root is not specific to that
  building.

Teardown sibling:
  killSilent ends all first-list items before all second-list items. If A's GPT
  payment is first and B's happiness resource second, ending GPT can run
  AI_doCommerce while A still temporarily retains the soon-to-be-removed import.
  The resource is then removed without a final commerce pass. This is the same
  transaction-boundary root in the opposite direction, not a separate finding.

Ancestry:
  In Civ4CE/BtS:
    * CvDeal already applies the first list item-by-item before the second list;
    * TRADE_GOLD_PER_TURN already calls changeGoldPerTurnByPlayer;
    * changeGoldPerTurnByPlayer already immediately invokes AI_doCommerce for AI;
    * resource start/end changes import/export state without a final commerce
      recomputation;
    * Drama makes Culture flexible and COMMERCE_CULTURE has
      iInitialHappiness = 10.

  K-Mod retains this transaction shape, Base AdvCiv 1.14 retains it, and current
  SAS retains it. SAS's current Gold bonus supplies a direct +1-happiness witness.
  The root is therefore inherited from Firaxis/BtS rather than introduced by SAS.

Dedup / sibling boundary:
  * F499 / KI#822 is stale m_iAvailableIncome during the GPT-triggered
    AI_doCommerce call. F500 survives a targeted F499 repair: even if available
    income is refreshed perfectly, AI_doCommerce is still running before the
    later resource item exists.
  * F292 / KI#615 is a transaction-precondition bug: a mixed city+resource bundle
    passes validation although the city transfer destroys the resource promise.
    F500's deal remains fully valid; the defect is a mid-transaction side effect
    whose decision is not recomputed after the remaining valid item is applied.
  * F287 and neighboring queue-042 findings concern persistent-deal teardown
    granularity / peace-item lifetime, not an AI decision committed from a partial
    transaction state.
  * No prior album root for deal-item -> AI_doCommerce -> later resource-state
    mutation was found in the semantic duplicate sweep.

Repair boundary:
  Treat AI commerce recomputation as a transaction-level consequence of a deal,
  not as a per-GPT-item side effect while the bundle is half-applied.

  Preferred direction:
    * while CvDeal is installing or removing a bundle, defer the GPT-triggered
      AI_doCommerce response for affected AI players;
    * after every item in both lists has reached its final state, refresh any
      state required by that response (including F499's available-income cache)
      and run AI_doCommerce once per affected AI.

  A simpler "run it again at the end" repair would correct the final sliders, but
  still allows the premature pass to perform other side effects (working-plot
  assignment, cache population, etc.). Deferring the response is the cleaner
  transaction boundary.

Status after this pass:
  Queue 004 / CvGameCoreDLL/CvPlayer.cpp remains OPEN.
  32 / 169 unique files complete.
  F445 / KI#768 remains retracted.
  F446 / KI#769 through F500 / KI#823 are confirmed.
  F501 / KI#824 is UNUSED.
  Latest physical checkpoint: C031-WIP245.
  No repository source modified.

NEXT:
  1. Bound F500 siblings: audit other startTrade/endTrade item side effects that
     invoke substantial AI/player recomputation before the bundle is complete;
     absorb symptoms under F500 when the same partial-transaction root owns them.
  2. In particular inspect tech/civic/religion/city/war-peace items adjacent to
     GPT/resource bundles for synchronous AI cache/production/diplomacy decisions.
  3. Then resume Queue-004 mutator/invalidation ranking under F501 unused, with
     priority on event-driven recalculations whose prerequisites can change again
     before the enclosing transaction finishes.
================================================================================

================================================================================
C031-WIP246 - POST-F500 COVERAGE SWEEP + F501 / KI#824 CONFIRMED
================================================================================

Scope / continuity
------------------
Continuation from C031-WIP245 on the Sep-3 AdvCiv-SAS source, with Base AdvCiv
1.14, K-Mod 1.46 and Civ4CE/BtS available locally for ancestry checks.

Queue 004 / CvGameCoreDLL/CvPlayer.cpp remains OPEN.
No repository source was modified.

POST-F500 SIBLING / LOW-COVERAGE CONTROLS
-----------------------------------------
Several tempting paths were challenged before spending F501.

1. F500 remains specifically a partial-deal decision-side-effect root.
   TRADE_CIVIC / TRADE_RELIGION and neighboring deal items can mutate substantial
   player state, but they do not themselves launch a comparable full
   AI_doCommerce decision while the bundle is only partially installed. Do not
   split generic item-by-item deal execution into extra findings without a real
   decision-producing side effect.

2. Cached attitude remains deliberately maintained in the sampled transition
   family. Memory changes adjust the cached value by delta; civic, religion and
   team identity changes update the relevant directions; long-lived
   religion/favorite-civic/trade counters advance before the deliberately delayed
   AI_doTurnPost attitude refresh. No F501.

3. The WIP241 capital/yield-rank family remains closed. Direct
   changeYieldRateModifier / changeCapitalYieldRateModifier paths invalidate the
   player's yield-rank cache, so civic yield modifiers do not reopen the retracted
   F445 root.

4. setGwPercentAnger explicitly calls AI_makeAssignWorkDirty. Its happiness change
   therefore does not silently preserve an obsolete citizen assignment.

5. updateExtraYieldThresholds still contains the already-recorded suspicious
   comparison involving iBestNaturalThresh / iBestThresh, but current shipped data
   continues to mask a second gameplay consequence. This remains robustness/data-
   extension territory, not F501.

6. CURRENT-DATA CORRECTION / STATE-RELIGION BUILDING HAPPINESS CONTROL:
   An older album control said current SAS had no nonzero
   iStateReligionHappiness buildings. That statement is now stale: Sep-3 current
   XML again has seven cathedral-style buildings with +2 state-religion happiness,
   matching stock BtS data.

   This does NOT expose a stale building-happiness cache. processBuilding stores
   the effect in CvCity::m_aiStateReligionHappiness keyed by the building's
   religion, and happyLevel / unhappyLevel call getCurrentStateReligionHappiness,
   which dynamically selects the bucket for the owner's current state religion.
   Changing religion therefore selects the right existing bucket without a rebuild.
   No independent cache finding here.

F501 / KI#824 - CONFIRMED
-------------------------
AI COMMERCE SLIDERS ARE CHOSEN BEFORE ZERO-ANARCHY CIVIC / RELIGION CHANGES THAT
CAN INVALIDATE THE SAME-TURN COMMERCE DECISION

Root
----
The AI turn-pre policy order is:

    CvPlayerAI::AI_doTurnPre
        AI_doResearch()
        AI_doCommerce()
        AI_doMilitary()
        AI_doCivics()
        AI_doReligion()
        AI_doCheckFinancialTrouble()

AI_doCommerce commits Gold / Research / Culture / Espionage percentages from the
player and city state that exists BEFORE the AI's own civic and religion decisions.

A civic revolution or religion conversion then mutates inputs used by
AI_doCommerce -- happiness, maintenance, trade routes, commerce modifiers,
specialist commerce, war weariness, state-religion effects, etc. -- but neither
setCivics/revolution nor convert/setLastStateReligion invokes AI_doCommerce again.

For paid anarchy this ordering can be less immediately consequential because the
empire is entering anarchy. It is a correctness defect for normal ZERO-ANARCHY
switches: Spiritual and Golden Age paths apply the new policy immediately and then
process the rest of the same economic turn with sliders chosen for the old policy.

Clean current-SAS civic reproduction
------------------------------------
Use a normal Spiritual AI with Drama and Hereditary Rule.

Current shipped facts:
  * TRAIT_SPIRITUAL has iMaxAnarchy = 0.
  * TECH_DRAMA makes COMMERCE_CULTURE flexible.
  * COMMERCE_CULTURE has iInitialHappiness = 10, i.e. 10% Culture supplies
    +1 happiness in an ordinary city before building-specific additions.
  * CIVIC_HEREDITARY_RULE has iHappyPerMilitaryUnit = 1.
  * AdvCiv-SAS's advc.912c rule interprets that as +1 happiness per two eligible
    military-happiness units; CvCity::getMilitaryHappiness divides the unit count
    contribution by two.
  * Police State (and other alternative government civics) has no
    iHappyPerMilitaryUnit benefit and can legitimately beat Hereditary Rule in
    AI_civicValue under suitable war / production / war-weariness conditions.

Construct a one-city case with exactly two eligible garrison units and tune other
happiness so that Hereditary Rule's +1 military happiness is exactly what keeps the
city from having one angry citizen. Avoid Culture-victory / border-pressure reasons
for a positive Culture slider.

Turn sequence:
  1. AI_doCommerce runs while Hereditary Rule is active.
  2. angryPopulation() is 0, so the happiness component of the Culture target is
     0%; Culture remains 0%.
  3. AI_doCivics chooses a sufficiently better government civic, e.g. Police State.
  4. Spiritual makes getCivicAnarchyLength(...) return 0, so revolution applies
     the new civic immediately with no anarchy turn.
  5. processCivics removes Hereditary Rule's military-happiness source through
     changeHappyPerMilitaryUnit(-1). That function correctly dirties citizen
     assignment, so city happiness now reflects the new civic.
  6. No AI_doCommerce rerun occurs. The correct post-switch happiness target is
     now 10% Culture, but the stored slider remains 0%.

Same-turn consumer
------------------
CvPlayer::doTurn calls AI_doTurnPre first and then continues:

    AI_doTurnPre()            <-- policy switch occurs here
    AI_assignWorkingPlots()   <-- sees new civic + angry citizen, old 0% Culture
    verifyGoldCommercePercent()
    doGold()
    doResearch()
    doEspionagePoints()
    each city->doTurn()

AI_assignWorkingPlots can repair citizen assignments, but it cannot invent the
missing Culture allocation. The city therefore loses the citizen / associated
working yield that the correct post-civic 10% Culture allocation would have kept
content. Gold, research and city processing then use the old-policy slider during
that same turn. This is a consumed economic error, not merely a one-turn display or
next-turn heuristic lag.

AI_doReligion sibling - SAME ROOT, NOT F502
-------------------------------------------
AI_doReligion is scheduled after AI_doCommerce for the same reason and has the same
zero-anarchy route through Spiritual or Golden Age.

Current SAS now again has the stock-style +2 iStateReligionHappiness cathedral
buildings. Their happiness is correctly stored per religion and selected dynamically,
so there is no cache bug; however a zero-anarchy conversion can immediately change
which +2 bucket counts toward happyLevel after the commerce slider has already been
chosen. Other state-religion happiness/economic effects can do likewise.

This is another producer under F501's single scheduling root: commerce is committed
before same-turn AI policy changes. Do not split it into F502.

Ancestry
--------
Civ4CE/BtS already uses the same relevant AI_doTurnPre order:

    AI_doResearch();
    AI_doCommerce();
    AI_doMilitary();
    AI_doCivics();
    AI_doReligion();

Stock BtS also supplies the required live mechanisms:
  * Spiritual iMaxAnarchy = 0;
  * Hereditary Rule iHappyPerMilitaryUnit = 1;
  * Drama enables the Culture slider;
  * COMMERCE_CULTURE iInitialHappiness = 10;
  * cathedral buildings have +2 iStateReligionHappiness;
  * civic/religion switching does not run a final AI_doCommerce pass.

K-Mod retains this ordering, Base AdvCiv 1.14 retains it, and current SAS retains it.
AdvCiv-SAS changes some civic numbers and the HR happiness scaling, but does not create
the scheduling defect. F501 is inherited from Firaxis/BtS.

Duplicate / root separation
---------------------------
  * F498 / KI#821 is the K-Mod m_aiConstructionValue cache surviving mutable
    slider/civic/religion state. F501 remains after a perfect F498 cache repair:
    the actual commerce percentages themselves are not recomputed after policy.
  * F500 / KI#823 is AI_doCommerce executing from a PARTIALLY APPLIED multi-item
    deal. F501 occurs in the ordinary fixed AI turn schedule with no deal involved.
  * F499 / KI#822 is stale m_iAvailableIncome during the GPT event-driven commerce
    response; F501 needs neither GPT nor that cache.
  * F323 / KI#646 concerns AI_doCivics's final-pass control state bypassing
    paid-anarchy safeguards. It does not concern post-revolution commerce sliders.
  * F476 / KI#799 concerns cached state-religion BUILDING COMMERCE invalidation,
    not the player's commerce-percentage decision.

No earlier album root for AI_doCommerce -> zero-anarchy AI policy change -> same-turn
stale slider was found in the semantic duplicate sweep.

Conservative repair direction
-----------------------------
The robust invariant is that a zero-anarchy policy change made by AI_doCivics or
AI_doReligion must not leave the rest of the turn using sliders chosen under the old
policy.

Preferred shape:
  * track whether AI_doCivics / AI_doReligion actually changed effective civic or
    state-religion state while the player remains out of anarchy;
  * after those policy decisions finish, run one final AI_doCommerce() if such a
    change occurred.

A broader unconditional second commerce pass after AI_doReligion would be simpler but
wastes work on the overwhelmingly common no-change turns. Moving the sole existing
AI_doCommerce call later is riskier because AI_doCivics / AI_doReligion themselves may
expect the current turn's ordinary pre-policy commerce state for valuation. A targeted
final rerun preserves current ordering while repairing the final-state invariant.

Status after this pass
----------------------
Queue 004 / CvGameCoreDLL/CvPlayer.cpp remains OPEN.
32 / 169 unique files complete.
F445 / KI#768 remains retracted.
F446 / KI#769 through F501 / KI#824 are confirmed.
F502 / KI#825 is UNUSED.
Latest physical checkpoint: C031-WIP246.
No repository source modified.

NEXT
----
1. Bound F501 siblings without splitting the same post-commerce policy-scheduling
   root. In particular inspect policy changes that affect commerce flexibility or
   force percentages themselves, because those may have stronger or different
   end-state semantics.
2. Resume the coverage-ranked CvPlayer mutator sweep under F502 unused, prioritizing
   cross-object cached consumers and event-driven recalculations.
3. Recheck current XML changes against older DATA-MASKED album controls; the restored
   state-religion-happiness cathedral data showed that previously dormant surfaces can
   become live even when their old code proof was only a control.
================================================================================

================================================================================
C031-WIP247 - F501 SIBLING BOUNDARY + F502 / KI#825 CONFIRMED
================================================================================

Scope / continuity
------------------
Continuation from C031-WIP246 on the Sep-3 AdvCiv-SAS source, with Base AdvCiv
1.14, K-Mod 1.46 and Civ4CE/BtS available locally for ancestry checks.

Queue 004 / CvGameCoreDLL/CvPlayer.cpp remains OPEN.
No repository source was modified.

F501 SIBLING - AI_doMilitary FINANCIAL SCRAPPING IS THE SAME POST-COMMERCE ROOT
--------------------------------------------------------------------------------
The first F502 boundary challenge was AI_doMilitary, which is scheduled after the
first AI_doCommerce pass but before AI_doCivics / AI_doReligion and before the
player's doGold / doResearch accounting.

AI_doMilitary can enter its K-Mod financial-disband path, scrap one or more units,
and thereby lower calculateUnitCost / calculateInflatedCosts after the commerce
slider has already been chosen. The remaining turn can therefore use a slider that
was chosen for unit expenses which no longer exist.

This is a real additional manifestation of F501, but NOT a separate F502 root.
F501's repair boundary already called for a final commerce pass after the AI's
post-commerce state-changing decisions and before AI_doTurnPre returns. Placing that
final pass after AI_doMilitary, AI_doCivics and AI_doReligion naturally observes the
post-scrap unit costs as well as the final civic/religion state. Numbering the
AI_doMilitary case separately would split one scheduling invariant into symptoms.

DATA-MASKED CONTROL RECHECK
---------------------------
A fresh Sep-3 XML pass rechecked several older dormant surfaces after the restored
state-religion-happiness cathedral data showed that old masking assumptions can age.
The following remain dormant in current shipped data:

  * Unit iExtraCost: zero live entries.
  * Unit SpeedBonuses: zero live entries.
  * Building CommerceFlexibles: zero live entries.
  * Civic bMilitaryFoodProduction: zero live entries.
  * Repeatable technologies: zero live entries.
  * EventInfo bTeam and EventTriggerInfo bTeam: zero live entries.
  * Espionage destroy-unit and buy-unit cost factors: zero live entries.

The expected live exceptions were reconfirmed:

  * Pyramids iGoldenAgeModifier = +50.
  * Pyramids and Versailles iAnarchyModifier = -100.
  * Seven cathedral-style buildings iStateReligionHappiness = +2.

No independent F502 arose from this control batch.

F502 / KI#825 - CONFIRMED
-------------------------
ZERO-ANARCHY CIVIC CHANGES CAN CHANGE K-MOD'S AVAILABLE-INCOME INPUTS AFTER THE
TURN-ACTIVATION CACHE REFRESH, SO THE SAME TURN'S FINANCIAL-TROUBLE CHECK AND CITY
PRODUCTION USE A PRE-CIVIC INCOME NUMERATOR

Root
----
K-Mod introduced m_iAvailableIncome as a stored approximation of the empire's income
at 100% Gold. Current AdvCiv-SAS refreshes it only from AI_updateCacheData at player
turn activation:

    AI_updateCacheData()
        ...
        AI_updateAvailableIncome();

AI_updateAvailableIncome derives the value from mutable empire state:

    int iTotalRaw = calculateTotalYield(YIELD_COMMERCE);
    m_iAvailableIncome = iTotalRaw * AI_averageCommerceMultiplier(COMMERCE_GOLD) / 100;
    m_iAvailableIncome += max(0, getGoldPerTurn());
    m_iAvailableIncome += getCommerceRate(COMMERCE_GOLD)
        - estimated slider-derived Gold;

AI_getAvailableIncome is then only an inline return of m_iAvailableIncome.

The ordinary AI turn subsequently runs:

    AI_doResearch();
    AI_doCommerce();
    AI_doMilitary();
    AI_doCivics();
    AI_doReligion();
    AI_doCheckFinancialTrouble();

A zero-anarchy civic switch in AI_doCivics can immediately alter any of several
inputs represented by the available-income cache: raw Commerce yield, Gold commerce
from specialists, trade routes, commerce modifiers, etc. CvPlayer::processCivics
correctly applies those authoritative city/player changes, but neither processCivics,
setCivics nor revolution refreshes m_iAvailableIncome.

AI_doCheckFinancialTrouble therefore immediately evaluates the new civic state with
the old pre-civic income numerator. The stale value then persists through the
remaining same-turn city processing until the next player-turn cache refresh.

Clean CURRENT SAS numerical witness
-----------------------------------
Current AdvCiv-SAS supplies an especially clean same-upkeep transition:

    CIVIC_UNIVERSAL_SUFFRAGE: UPKEEP_HIGH, no SpecialistExtraCommerces
    CIVIC_REPRESENTATION:     UPKEEP_HIGH, SpecialistExtraCommerces = +1 Gold,
                              +1 Research, +1 Culture per specialist

Use a normal Spiritual AI (zero civic anarchy), already able to run either civic,
with exactly two existing specialists in cities whose Gold commerce multiplier is
100%. Tune the otherwise ordinary empire state so that the turn-activation cache and
expenses are:

    m_iAvailableIncome = 100
    calculateInflatedCosts() + max(0, -getGoldPerTurn()) = 66

and use the ordinary 35% financial-trouble safety threshold with low enough treasury
that the large-cash safe override does not apply.

Before the switch:

    funded = floor(100 * (100 - 66) / 100) = 34%
    margin = 34 - 35 = -1
    AI_isFinancialTrouble() = true

AI_doCivics can legitimately prefer Representation under a specialist-favoring
state. Spiritual makes the revolution immediate. processCivics applies
changeSpecialistExtraCommerce(COMMERCE_GOLD, +1), and that mutator calls
updateCommerce(COMMERCE_GOLD) for every city. The authoritative Gold commerce is
therefore already post-Representation.

With two existing specialists and no Gold multiplier beyond the 100% baseline, a
correct AI_updateAvailableIncome recomputation raises the non-slider Gold component
by exactly 2 while the same-HIGH civic upkeep leaves the chosen expense witness
unchanged:

    correct available income = 102
    funded = floor(100 * (102 - 66) / 102) = 35%
    margin = 35 - 35 = 0
    correct AI_isFinancialTrouble() = false

But m_iAvailableIncome remains 100, so the immediately following
AI_doCheckFinancialTrouble still sees financial trouble.

Concrete same-turn production consequence
------------------------------------------
This is not merely an internal flag being one turn late.

AI_doCheckFinancialTrouble reacts to a false->true observation by calling
AI_makeProductionDirty (subject to its existing cooldown). AI_makeProductionDirty
sets choose-production dirty on every city.

Thus in the witness above, with the cooldown available, the stale pre-civic numerator
can cause an entirely spurious financial-trouble entry and dirty every AI city even
though the correct post-civic state is healthy.

Later in the same CvPlayer::doTurn, every city runs CvCity::doTurn. CvCity::doProduction
calls AI_chooseProduction whenever isChooseProductionDirty() is true. The production
AI itself has many live AI_isFinancialTrouble branches. Since m_iAvailableIncome is
still stale for the remainder of the turn, both the decision to force reconsideration
and the resulting production choice can be taken under the wrong financial state.

The opposite direction is equally possible: a civic transition that lowers raw or
non-slider income can leave the cached numerator too high, causing
AI_doCheckFinancialTrouble to miss a real entry and later city AI to behave as though
healthy.

Stock K-Mod / Base AdvCiv witness and ancestry
----------------------------------------------
The defect does not depend on SAS's custom Representation data.

Stock BtS/K-Mod/Base AdvCiv civic data supplies another clean zero-anarchy producer:

    CIVIC_VASSALAGE:  UPKEEP_HIGH
    CIVIC_BUREAUCRACY: UPKEEP_HIGH, CapitalYieldModifiers includes +50% YIELD_COMMERCE

For a Spiritual AI, Vassalage -> Bureaucracy can therefore change the capital's raw
Commerce yield immediately with no civic-upkeep-category change. processCivics calls
changeCapitalYieldRateModifier(YIELD_COMMERCE, +50), which updates the capital's
commerce state. A capital whose affected Commerce contribution rises by 2 can reproduce
the same 100 -> 102 available-income threshold flip above, but K-Mod's stored
m_iAvailableIncome remains at 100 until the next turn activation.

Civ4CE/BtS already has the relevant civic switching and dynamic commerce updates, but
its AI_isFinancialTrouble computes its net commerce from current values rather than a
stored m_iAvailableIncome cache.

K-Mod introduces m_iAvailableIncome / AI_updateAvailableIncome and refreshes it on the
turn cache cadence while retaining AI_doCivics followed by AI_doCheckFinancialTrouble.
That combination creates the stale post-civic financial state. Base AdvCiv 1.14 and
current SAS inherit the cache lifetime. SAS changes civic numbers and provides the
particularly compact Representation witness, but does not create the code root.

F502 is therefore K-Mod-introduced and inherited by AdvCiv/SAS.

Duplicate / root separation
---------------------------
No earlier album root for civic mutation -> stale m_iAvailableIncome -> scheduled
AI_doCheckFinancialTrouble / same-turn city production was found.

F502 is independent of:

  * F499 / KI#822: same underlying K-Mod cache, but a different missing invalidation
    boundary. F499 is specifically GPT mutation -> immediate GPT-triggered
    AI_doCommerce. Its targeted repair refreshes available income inside
    changeGoldPerTurnByPlayer and does not repair civic changes.
  * F501 / KI#824: actual commerce percentages are chosen before post-commerce
    military/civic/religion state changes. A final AI_doCommerce pass alone does NOT
    repair F502 because that pass itself calls AI_isFinancialTrouble and would still
    read stale m_iAvailableIncome unless the income cache is refreshed first.
  * F364 / KI#687: the production-dirty cooldown can lose a correctly observed
    financial-trouble transition. F502 makes the financial-trouble observation itself
    wrong before that transition scheduler is evaluated.

Repair boundary
---------------
The invariant should be that any decision explicitly made after a state transition
which changes AI_updateAvailableIncome inputs must not consume the old cache.

For the ordinary turn-pre policy sequence, the clean repair pairing with F501 is:

    AI_doMilitary();
    AI_doCivics();
    AI_doReligion();
    if relevant state changed while still out of anarchy:
        AI_updateAvailableIncome();
        AI_doCommerce();
    AI_doCheckFinancialTrouble();

The precise trigger need not be limited to civics if post-cache state changes can alter
raw Commerce or non-slider Gold. A robust invalidation/dirty mechanism for
m_iAvailableIncome would be safer than maintaining an ever-growing hand list of
mutators, provided deterministic savegame/OOS behavior is preserved.

F499 still needs its event-driven GPT path repaired separately because a GPT deal can
occur outside this turn-pre sequence and deliberately requests immediate commerce
recalculation.

Status after this pass
----------------------
Queue 004 / CvGameCoreDLL/CvPlayer.cpp remains OPEN.
32 / 169 unique files complete.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F502 / KI#825 are CONFIRMED.
F503 / KI#826 is UNUSED.
Latest physical checkpoint: C031-WIP247.
No repository source modified.

NEXT
----
1. Bound F502 siblings: identify other ordinary post-cache mutations of raw Commerce /
   non-slider Gold and absorb them under F502 when the same available-income lifetime
   root owns them.
2. In particular challenge religion changes, city assignment/free-specialist changes,
   unit scrapping and same-turn building/city events against AI_updateAvailableIncome's
   inputs; distinguish deliberate turn-sampling from event-driven consumers.
3. Resume Queue-004 low-coverage mutator / cross-object invalidation ranking under
   F503 unused once the F502 boundary is stable.
================================================================================


======================================================================
C031-WIP248 — Queue 004 continuation after WIP247
2026-09-03 — ChatGPT-5.6-Sol
======================================================================

POST-F502 BOUNDARY / CONTROL PASS
---------------------------------
F503 / KI#826 began UNUSED.

The first pass bounded F502 rather than splitting symptoms into new numbers:

* state-religion conversion can change happiness and therefore citizen assignment,
  but current AdvCiv religion commerce itself is generally selected as the best
  religion commerce present rather than being keyed directly to the chosen state
  religion (advc.172);
* any raw-Commerce change caused by post-cache policy-driven reassignment is the
  same m_iAvailableIncome lifetime root already owned by F502;
* the restored current SAS +2 state-religion-happiness cathedral data are not a
  stale cache: processBuilding stores per-religion buckets and happyLevel reads the
  current state's bucket dynamically;
* AI_doMilitary can remove upkeep after the initial AI_doCommerce pass, but that is
  an F501 scheduling sibling because F501's proposed final commerce pass naturally
  covers it.

No new number was spent on those paths.

IMPROVEMENT-UPGRADE-RATE HISTORY CHALLENGE — CLOSED
----------------------------------------------------
AdvCiv commit-history ranking highlighted the old "Fix problems with improvement
upgrade rate" family. The generic zero-rate concern had already been recorded as a
robustness/data-extension control in the album, and current code has a later guard:
getUpgradeTimeLeft maps a zero upgrade rate to a negative "never" result rather than
dividing by zero. Current SAS's only ordinary nonzero civic producer is +100, not
-100. Ownership/rate transitions deliberately preserve accumulated improvement
maturity and change only the future increment rate. No shipped F503 emerged here.

TURN-CACHE / HISTORY-RANKED CONTROL PASS — CLOSED
--------------------------------------------------
The wider K-Mod activation-cache sweep likewise did not justify a new number:
important average-yield/commerce strategic consumers run before the later civic
switch; ordinary later heuristic lag is part of the sampled-turn-cache design. The
post-civic correctness contamination of available income is already F502. The old
delayed Taj Mahal Golden Age lifecycle fix also remains structurally intact.


F503 / KI#826 — CONFIRMED
=========================

TITLE
-----
Production-yield modifier changes do not rebuild cached production-to-commerce
output, so Wealth / Research / Culture can keep using an old hammer rate.

ROOT
----
CvCity::updateCommerce caches both ordinary Commerce-derived output and process
conversion:

    iNewRate += (getBaseCommerceRateTimes100(eCommerce) *
            getTotalCommerceRateModifier(eCommerce)) / 100;
    iNewRate += getYieldRate(YIELD_PRODUCTION) *
            getProductionToCommerceModifier(eCommerce);

The second term means that PRODUCTION yield is also an input to the cached city
commerce rate whenever a production process is active.

However, the modifier mutators only recognize YIELD_COMMERCE as requiring a
commerce refresh. Current SAS examples are:

    CvCity::changeYieldRateModifier
    CvCity::changePowerYieldRateModifier
    CvCity::changeBonusYieldRateModifier
    CvArea::changeYieldRateModifier
    CvPlayer::changeYieldRateModifier
    CvPlayer::changeCapitalYieldRateModifier

They invalidate the affected yield-rank cache, but update commerce only when the
changed yield is YIELD_COMMERCE. A YIELD_PRODUCTION modifier can therefore change
getYieldRate(YIELD_PRODUCTION) immediately while m_aiCommerceRate / the player's
aggregated commerce rate retain the process result calculated from the old hammer
rate.

This is distinct from base-yield changes: CvCity::setBaseYieldRate correctly calls
updateCommerce() unconditionally. The missing dependency is specifically the
modifier side of the production-yield graph.

STOCK / CURRENT SHIPPED REPRODUCTION — IRON WORKS
--------------------------------------------------
Iron Works supplies an especially clean ordinary-game producer. Current SAS, Base
AdvCiv 1.14 and the supplied BtS/Civ4CE data all give Iron Works:

    Coal -> +50% Production
    Iron -> +50% Production

The relevant ordinary resource path is:

    CvPlot::updatePlotGroupBonus
      -> CvPlotGroup::changeNumBonuses
      -> CvCity::changeNumBonuses
      -> CvCity::processBonus
      -> CvCity::changeBonusYieldRateModifier(YIELD_PRODUCTION, +/-50)

changeNumBonuses performs no unconditional updateCommerce afterward. It only calls
updateCorporation for a bonus that is currently feeding an active corporation in
that city. Therefore choose an Iron Works city without an applicable corporation
branch; Iron/Coal then reach the stale path directly.

A deterministic test avoids every incidental repair:

1. Build Iron Works and have both Coal and Iron connected.
2. Let the Iron Works city run PROCESS_WEALTH.
3. Put the relevant Iron resource outside that city's worked BFC but in the same
   resource network, so changing the connection does not alter the city's worked
   plot base yields.
4. Pillage/disconnect that Iron while Wealth remains selected.
5. The Iron Works +50% Iron modifier is removed immediately through processBonus.
6. The city's live Production yield therefore falls immediately.
7. changeBonusYieldRateModifier does not call updateCommerce for Production, so the
   cached Wealth Gold remains based on the old Production yield.
8. The process itself stays valid; verifyProduction is not a commerce rebuild.

NUMERICAL WITNESS
-----------------
Use a city with 20 base Production and no other production modifiers for the simple
component calculation:

    Coal only:
        20 * 150% = 30 Production
        Wealth at 50% = 15 Gold

    Coal + Iron:
        20 * 200% = 40 Production
        Wealth at 50% = 20 Gold

If Iron is pillaged while Wealth is active, the live Production correctly falls
from 40 to 30, but the cached process contribution can remain 20 Gold instead of
15. The city/player therefore overstates Gold by 5. Reconnecting Iron from the
Coal-only state gives the reverse error: live Production rises to 40 while cached
Wealth can remain 15, understating Gold by 5.

The exact same root applies to PROCESS_RESEARCH and PROCESS_CULTURE because current
SAS has all three standard 50% production-to-commerce processes.

REAL CONSUMER / SELF-HEAL CHALLENGE
-----------------------------------
This is not just a UI cache.

CvCity::setCommerceRateTimes100 propagates city changes into the player's cached
commerce aggregate. If updateCommerce is skipped, that player aggregate remains
wrong too. CvPlayer::calculateGoldRate then reads getCommerceRate(COMMERCE_GOLD),
and CvPlayer::doGold books that value into the treasury.

For the pillage reproduction, a human owner is especially deterministic. A remote
Iron tile can be pillaged during another player's turn. On the owner's later
CvPlayer::doTurn path, ordinary ordering is:

    AI_doTurnPre
    AI_assignWorkingPlots
    verifyGoldCommercePercent
    doGold
    doResearch
    ...
    city doTurn loops later

There is no unconditional city-commerce rebuild before doGold. Human citizens can
remain manually assigned; placing the Iron outside the Iron Works city's BFC also
ensures the resource transition itself does not change that city's base worked-tile
yields. Thus the stale Gold process output can be actually booked before any
unrelated future refresh.

The stale state can also persist beyond one turn: city doTurn has no unconditional
updateCommerce. Game-turn-wide building-commerce maintenance only calls through to
updateCommerce when the separately cached building-commerce amount itself changes;
it is not a general process-output refresh.

ANCESTRY
--------
The defect is inherited from Firaxis BtS.

Supplied BtS/Civ4CE already has both sides of the mismatch:

* CvCity::updateCommerce adds
  getYieldRate(YIELD_PRODUCTION) * getProductionToCommerceModifier(...);
* CvCity::changeBonusYieldRateModifier refreshes commerce only for
  YIELD_COMMERCE;
* BtS Iron Works has the same +50% Production from Coal and +50% from Iron.

K-Mod retains the same updateCommerce dependency and the same modifier-side
condition. Its shipped XML also retains the Iron Works Coal/Iron +50 modifiers.
Base AdvCiv 1.14 retains the same code and XML behavior. Current SAS inherits the
same structure and additionally has player-level Production modifier producers
such as the reworked State Property, giving extra manifestations of the same root.

The current SAS State Property path is therefore an alternate witness, not a new
finding: a zero-anarchy civic switch can alter player/capital Production modifiers
while a city is running a process, and the player/capital modifier mutators likewise
refresh commerce only for YIELD_COMMERCE.

SIBLING / SCOPE BOUNDARY
------------------------
The common invariant is:

    any mutation that changes getYieldRate(YIELD_PRODUCTION)
    while a production-to-commerce modifier is active
    must rebuild the affected cached commerce result.

Base-yield mutations already honor it through setBaseYieldRate -> updateCommerce.
The missing family is yield MODIFIERS. The city, area, player and capital variants
should therefore be treated as one F503 root, not separate findings.

Power transitions often receive an incidental updateCommerce from
changePowerCount when isPower changes; that can mask some
changePowerYieldRateModifier manifestations. It does not repair the general
modifier contract and does not affect the clean Iron Works bonus witness.

DEDUP / PRIOR CONTROL CORRECTION
--------------------------------
The full album semantic search found process-output telemetry findings (notably
F060), culture-process lifecycle work, and the earlier generic player yield/commerce
mutator reconciliation, but no root for stale process commerce after a Production
modifier change.

WIP179's reconciliation sentence said changeYieldRateModifier "refreshes commerce
where the yield is commerce-producing." That closure implicitly treated only
YIELD_COMMERCE as commerce-producing and missed the explicit Production-to-Commerce
process term in CvCity::updateCommerce. F503 therefore qualifies/corrects that
control; it is not a duplicate of it.

This is also independent of the recent AI-only cache findings F498/F499/F502. F503
is authoritative game-state commerce output: a human player's actual Gold/Research/
Culture process result can be stale and booked incorrectly.

REPAIR BOUNDARY
---------------
The minimal semantic fix is to treat Production as a commerce dependency for these
modifier mutators too. Conceptually, wherever a yield-modifier changer currently
has:

    if (eYield == YIELD_COMMERCE)
        updateCommerce(...);

it should also refresh the affected city/cities for YIELD_PRODUCTION (ideally only
when a production-to-commerce process is active if the code wants to avoid needless
work).

That responsibility needs to cover the city-local, bonus, power, area, player-wide
and capital yield-modifier variants. A narrow patch only in processBonus would fix
the Iron Works witness but leave the same dependency hole for civic/global/area
Production modifiers.

Confidence: VERY HIGH.
Classification: inherited Firaxis BtS gameplay/cache defect; live in current SAS.


POST-F503 CONTINUATION STONE
============================
Queue 004  CvGameCoreDLL/CvPlayer.cpp  OPEN
Unique files complete: 32 / 169
F445 / KI#768: RETRACTED
F446 / KI#769 through F503 / KI#826: CONFIRMED
F504 / KI#827: UNUSED
Latest physical checkpoint: C031-WIP248
Repository source modified: NO

NEXT
----
1. Bound F503 siblings first: production-modifier sources in processBuilding,
   CvArea and CvPlayer; do not split manifestations that share the same missing
   Production -> cached-commerce dependency.
2. Check whether any other derived cache takes getYieldRate(PRODUCTION) as an input
   while its modifier mutators invalidate only the production/yield side.
3. Resume the coverage-ranked CvPlayer mutator / cross-object derived-value sweep
   under F504 unused.
4. Continue to prefer closures over manufactured findings.



===============================================================================
C031-WIP249 — POST-F503 CONTROL + F504 CONFIRMED
===============================================================================

PRE-F504 CONTROL — LARGEST-CITY HAPPINESS / POPULATION-RANK IDENTITY
-------------------------------------------------------------------
The low-coverage `LargestCityHappiness` / biggest-city identity challenge closes.
`CvCity::getLargestCityHappiness()` does not store a separate "largest city" identity;
it asks `findPopulationRank()` dynamically and applies the player value only when the
city's current rank is within the world-size `TargetNumCities` cutoff.

The K-Mod population-rank cache has the required invalidation on the important normal
producer. `CvCity::setPopulation` calls:

    GET_PLAYER(getOwner()).invalidatePopulationRankCache();

before later happiness / assignment consumers, invalidating every city of the owner.
City destruction also passes through `setPopulation(0)` before the city is detached, so
the surviving owner's ranks remain invalid. `changeLargestCityHappiness` independently
calls `AI_makeAssignWorkDirty()` when the civic aggregate itself changes.

No stale largest-city identity or independent F504 consequence survived. This family is
therefore a control.


F504 / KI#827 — K-MOD'S EAGER UNIT-UPGRADE-GOLD CACHE IS COMPUTED BEFORE THE AI
ACTUALLY UPGRADES ITS UNITS, THEN REMAINS STALE FOR THE LATER COMMERCE/HURRY PHASE
-------------------------------------------------------------------------------
STATUS
------
CONFIRMED — K-Mod-introduced AI cache-lifetime defect, inherited by Base AdvCiv 1.14
and current AdvCiv-SAS.

ROOT
----
K-Mod changed BtS's lazy per-game-turn upgrade-cost cache into an eagerly refreshed
turn-activation value.

Current SAS `CvPlayer::setTurnActive(true)` performs:

    AI().AI_updateCacheData();
    ...
    doTurnUnits();

and `AI_updateCacheData()` contains:

    AI_updateGoldToUpgradeAllUnits();

The resulting `m_iUpgradeUnitsCachedGold` is therefore calculated from the army that
exists *before* that turn's unit phase.

But `doTurnUnits()` ends in `CvPlayerAI::AI_doTurnUnitsPost()`, whose normal AI path
actually spends Gold upgrading units. Current SAS derives its upgrade-spending budget
from `AI_goldTarget(...)`, iterates the physical units and calls:

    pLoopUnit->AI_upgrade(iMaxUpgradePrice);

Successful upgrades replace the old unit with its upgraded type and subtract the real
upgrade price from the treasury. Scrapping/death in the same phase can likewise remove
units that contributed to the activation-time estimate.

There is no corresponding `AI_updateGoldToUpgradeAllUnits()` call or invalidation after
an upgrade, after the upgrade pass, or in the unit replacement path. A full DLL caller
search finds the updater only in initialization/turn-cache setup plus its own definition.
Thus `m_iUpgradeUnitsCachedGold` continues to describe the *pre-unit-phase* army.

REAL SAME-TURN CONSUMER
-----------------------
For an ordinary sequential AI turn the important ordering is:

    setTurnActive(true)
      -> AI_updateCacheData()
         -> AI_updateGoldToUpgradeAllUnits()       [pre-upgrade cache]
      -> doTurnUnits()
         -> AI_doTurnUnitsPost()
            -> actual unit upgrades spend Gold

    ... AI finishes its active unit phase ...

    setTurnActive(false)
      -> CvPlayer::doTurn()
         -> AI_doTurnPre()
            -> AI_doResearch()
            -> AI_doCommerce()                     [reads stale upgrade cache]
         -> doGold / doResearch
         -> city doTurn()
            -> CvCityAI::AI_doHurry()              [also reads stale cache]

`AI_goldTarget()` begins with:

    int iUpgradeBudget = AI_getGoldToUpgradeAllUnits();

and scales that value according to war / financial state before adding it to the empire's
Gold target. In peace and outside Culture stage 4, for example, a healthy AI divides the
raw estimate by 4; a financially troubled AI divides it by 10.

Therefore the post-upgrade commerce phase can reserve Gold for an upgrade that has
already been completed earlier in the same AI turn.

CLEAN SHIPPED REPRODUCTION
--------------------------
A single ordinary upgrade candidate is sufficient. One straightforward current SAS
shape is a Rifleman with Assembly Line known and Infantry available, while later
Infantry upgrade technologies are not yet known.

1. At turn activation, the Rifleman contributes its positive Infantry upgrade price U
   to `m_iUpgradeUnitsCachedGold`.
2. Give the AI enough Gold and an ordinary upgrade-eligible situation so
   `AI_doTurnUnitsPost()` upgrades that Rifleman to Infantry.
3. After the upgrade there is no longer a Rifleman->Infantry cost to reserve; with no
   other upgrade candidates, the correct aggregate is 0.
4. The stored aggregate nevertheless remains U through the later `AI_doCommerce()` and
   city hurry decisions.

The exact unit type is not essential. The strongest invariant is self-staleness: an item
counted by the cache is then consumed/removed by the very turn's upgrade executor, but
the cache is not rebuilt afterward.

CONCRETE DECISION DIVERGENCE
----------------------------
The stale amount is behaviorally live, not diagnostic-only.

For commerce allocation, write C for all non-upgrade components of `AI_goldTarget()`.
In a peaceful, healthy state the completed upgrade leaves:

    correct post-upgrade Gold target:  C
    stale post-upgrade Gold target:    C + U/4

Any full-science forecast whose projected treasury lies in `[C, C + U/4)` causes
opposite behavior in `AI_doCommerce()`'s Gold-increase loop: the correctly refreshed
state accepts the forecast while the stale state raises the Gold slider to rebuild a
reserve for the already-completed upgrade.

`CvCityAI::AI_doHurry()` supplies a second direct consumer. For a Gold hurry it first
checks:

    if (iHurryGold > kOwner.getGold() - kOwner.AI_goldTarget(true))
        continue;

After the sole upgrade has completed, the correct upgrade-only target is 0. The stale
target remains a positive fraction of U. For any post-upgrade treasury G and hurry cost
H satisfying:

    G - staleUpgradeTarget < H <= G

the stale cache rejects a hurry that the correctly refreshed state can afford while
preserving the real remaining upgrade requirement (zero).

ANCESTRY — INTRODUCED BY K-MOD'S EAGER CACHE LIFETIME
----------------------------------------------------
BtS/Civ4CE already has a per-game-turn cache for `AI_goldToUpgradeAllUnits`, but its
contract is lazy:

    if (m_iUpgradeUnitsCacheTurn == currentTurn ...)
        return cached;
    ... compute from current units ...

A full BtS call-site search finds ordinary `AI_goldToUpgradeAllUnits` use through
`AI_goldTarget`, and the first normal AI `AI_goldTarget` consumers occur after the unit
upgrade phase (commerce / city hurry / later diplomacy). BtS does not prepopulate this
cache at player turn activation. Thus the post-upgrade first read computes from the
post-upgrade army.

K-Mod explicitly disables that lazy turn-check machinery and instead calls
`AI_updateGoldToUpgradeAllUnits()` from `updateCacheData()`. K-Mod also calls that cache
setup from `CvPlayer::setTurnActive(true)` before `doTurnUnits()`. It thereby moves the
snapshot across the mutation boundary: the cached amount is now frozen before the AI
executes the upgrades whose remaining cost the value is supposed to represent.

Base AdvCiv 1.14 preserves the K-Mod eager updater in turn-activation cache setup, and
current SAS retains the same lifetime. F504 is therefore K-Mod-introduced rather than an
inherited Firaxis BtS defect.

DUPLICATE / ROOT SEPARATION
---------------------------
The full album search found no previous root for the upgrade reserve remaining stale
after the unit-upgrade executor consumes its own candidates.

F504 is distinct from F365/KI#688. F365 proves that *while calculating* the aggregate,
`AI_updateGoldToUpgradeAllUnits` incorrectly memoizes a physical unit's upgrade price by
UnitType despite per-unit upgrade discounts / UnitAI / area context. F504 survives a
perfect F365 repair: even if the activation-time total is exact for every unit, that
exact total becomes stale when those units are upgraded or removed later in the turn.

F504 is also separate from KI#160's SAS upgrade-spending cap. KI#160 controls how much of
the precomputed budget the executor may actually spend; it does not refresh the strategic
reserve after spending has changed the set of remaining upgrades.

SIBLING / SCOPE BOUNDARY
------------------------
The same lifetime root can be triggered by any post-activation mutation that changes the
set or price of upgrade candidates before later cache consumers: successful upgrades are
the clean self-contained witness; unit death/scrapping/capture, resource/city eligibility
changes, and some later tech-state changes can be additional manifestations.

Do not split those into separate findings unless a producer has an independent cache or
state-transition defect. The common invariant is that `m_iUpgradeUnitsCachedGold` must
represent the army at the point where `AI_goldTarget` consumes it, not merely the army at
turn activation.

REPAIR DIRECTION
----------------
The narrowest low-risk repair is to refresh the aggregate after the AI's unit-upgrade
phase, once all upgrade/scrap attempts for the active turn are complete and before the
later commerce/hurry consumers run:

    AI_doTurnUnitsPost()
      -> perform upgrades/scraps
      -> AI_updateGoldToUpgradeAllUnits()

A more general invalidation/lazy-recompute design would restore BtS's useful property
that structural unit/eligibility changes after activation cannot leave the stored value
stale, but the post-upgrade refresh alone fixes the deterministic shipped witness with
one bounded recomputation per AI turn.

Confidence: VERY HIGH.
Classification: K-Mod-introduced AI strategic-cache lifetime defect; inherited by
AdvCiv and current AdvCiv-SAS.


POST-F504 CONTINUATION STONE
============================
Queue 004  CvGameCoreDLL/CvPlayer.cpp  OPEN
Unique files complete: 32 / 169
F445 / KI#768: RETRACTED
F446 / KI#769 through F504 / KI#827: CONFIRMED
F505 / KI#828: UNUSED
Latest physical checkpoint: C031-WIP249
Repository source modified: NO

NEXT
----
1. Bound F504 siblings first. In particular, check post-activation unit death/capture,
   tech unlocks and resource/city trainability changes, but keep them under F504 when
   they merely stale the same upgrade-gold snapshot.
2. Continue ranking the remaining persistent `CvPlayerAI` members by mutable
   dependencies. Prefer event-driven / same-turn consumers over intentionally sampled
   strategic estimates.
3. Return to cross-object derived city/team/game caches under F505 unused.
4. Keep using the Base AdvCiv / K-Mod / Civ4CE local trees for ancestry; no external
   fetch is required unless a source boundary cannot be resolved locally.


================================================================================
C031-WIP250 — QUEUE 004 CONTINUATION — F505 / KI#828 CONFIRMED
================================================================================

CONTINUATION ANCHOR
-------------------
Resumed Queue 004 `CvGameCoreDLL/CvPlayer.cpp` from C031-WIP249 with F505 unused.
This tranche first bounded F504's siblings and closed several intentionally sampled/dynamic
cache families, then switched back to the higher-signal cross-object derived-value method.
That produced an inherited trade-route cache defect in which one city's population is an
input to another player's cached trade yield without a corresponding cross-player
invalidation.

POST-F504 BOUNDARY / CLEAN CONTROLS
-----------------------------------
1. F504's cache-lifetime root is broad enough to absorb ordinary unit-phase mutations of the
   upgrade snapshot: successful upgrades, deaths/scraps/captures, trainability/resource
   changes, and later same-turn upgrade-path changes are symptoms of the same
   `m_iUpgradeUnitsCachedGold` lifetime. Do not split them into F505.

2. `m_bDangerFromSubmarines` initially looked vulnerable because activation-time cache setup
   happens before the team turn. The actual consumer, however, is in the air-unit phase, and
   normal AI war-plan processing does not itself declare war there. `doTurnUnits` processes
   air before the land-unit movement that ordinarily turns a war plan into a declaration.
   The next activation refresh therefore sees the new enemy before the next air consumer.
   Preserve this as a correct turn-snapshot control.

3. `changeGreatPeopleRateModifier` and
   `changeStateReligionGreatPeopleRateModifier` require no city cache invalidation.
   `CvCity::getTotalGreatPeopleRateModifier()` reads both player fields dynamically and the
   actual city-turn GPP increment calls `getGreatPeopleRate()` directly.

4. Player-wide military / spaceship / wonder production modifiers are likewise dynamically
   read by `CvCity::getProductionModifier(Unit/Building/Project)`. Any AI construction-value
   staleness after such a mutation belongs to F498, and Production-yield changes feeding
   cached Wealth/Research/Culture output belong to F503. No third production cache root was
   established here.

5. The largest-city-happiness identity control from WIP249 remains closed: population-rank
   invalidation happens globally for the owner, and largest-city eligibility is queried from
   the rank dynamically.

F505 / KI#828 — PARTNER-CITY POPULATION CHANGES DO NOT INVALIDATE OTHER PLAYERS'
CACHED TRADE ROUTES / TRADE YIELD
--------------------------------------------------------------------------------

ROOT
~~~~
A city's trade profit depends directly on the POPULATION OF ITS PARTNER CITY:

    int CvCity::getBaseTradeProfit(CvCity const* pCity) const
    {
        int iProfit = std::min(
            pCity->getPopulation() * THEIR_POPULATION_TRADE_PERCENT,
            plotDistance(...) * WorldInfo::getTradeProfitPercent());
        iProfit *= TRADE_PROFIT_PERCENT;
        iProfit /= 100;
        return std::max(100, iProfit);
    }

`CvCity::updateTradeRoutes()` uses that value both to choose the best partner cities and to
recompute the cached `m_aiTradeYield`. `setTradeYield` then pushes the resulting delta into
base yield / commerce.

But `CvCity::setPopulation()` does NOT rebuild trade routes globally, or even notify other
players whose cities currently use (or could newly prefer) this city as a trade partner. Its
relevant side effects are owner population-rank invalidation, population totals, plot yield,
maintenance and owner assignment dirtiness. There is no `updateTradeRoutes()` and no
`GC.getGame().updateTradeRoutes()`.

The growing/shrinking city's OWN player eventually runs the ordinary end-of-turn
`CvPlayer::updateTradeRoutes()`, but that function clears and rebuilds only that player's
cities. It cannot repair another player's cached routes to this city.

Thus a remote city can retain both:

* an obsolete trade-yield amount for an existing route, and
* an obsolete partner selection when the population change should alter which eligible city
  is the most profitable route.

SHIPPED SEQUENTIAL-TURN CONSUMER
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This is not merely a hover/display delay.

The ordinary player-turn economic order is:

    AI_assignWorkingPlots / verifyGoldCommercePercent
    doGold()
    doResearch()
    ...
    city doTurn() (including growth/starvation)
    ...
    updateTradeRoutes()          // near the END of this player's turn

Suppose player B acts before player A in a sequential round. During B's city turn a B city
changes population. A's trade-route cache was last rebuilt at A's previous turn end, before
that B population change. No cross-player invalidator runs when B grows/shrinks.

When A now reaches `doGold()`, A therefore consumes its old city trade yield. Only later, near
A's own turn end, does A's routine `updateTradeRoutes()` finally notice B's new population.
One economic application has already been booked with the obsolete remote-city state.

The same defect exists regardless of player ordering: any partner population change occurring
between one player's end-of-turn route rebuild and that player's next economic processing can
survive into that next `doGold()`.

DETERMINISTIC CURRENT-DATA NUMERIC WITNESS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A simple ordinary-data witness can make the stale value cross an integer Commerce boundary.
Use a Standard map and an A city of population 10 with one sufficiently distant, mature
foreign trade route to B. Keep the cities far enough apart that the partner-population term,
not the distance cap, is limiting (on Base AdvCiv Standard, world trade-profit percent is 48,
so distance >= 16 is sufficient through population 15).

Inherited/current values include:

    THEIR_POPULATION_TRADE_PERCENT = 50
    TRADE_PROFIT_PERCENT           = 20
    CAPITAL_TRADE_MODIFIER         = 25
    FOREIGN_TRADE_MODIFIER         = 150
    YIELD_COMMERCE trade modifier  = 100

Choose A's route city so its own population-trade modifier is 0 at size 10 and it receives the
normal capital-connection modifier. With mature foreign trade, the total trade modifier is
then 100 + 25 + 150 = 275.

For B population 14:

    raw partner term       = 14 * 50 = 700
    after TRADE_PROFIT 20% = 140 hundredths
    after 275% modifier    = 385 hundredths
    integer trade profit   = 3 Commerce

For B population 15:

    raw partner term       = 15 * 50 = 750
    after TRADE_PROFIT 20% = 150 hundredths
    after 275% modifier    = 412 hundredths
    integer trade profit   = 4 Commerce

So B growing 14 -> 15 should immediately make A's established route worth 4 instead of 3.
`B.setPopulation(15)` leaves A's `m_aiTradeYield` at 3. A can then book that stale 3 through
`doGold()` before A's own late route rebuild corrects it.

The reverse 15 -> 14 starvation path can overpay A by 1 Commerce for an economic application.
Larger/multiple routes can amplify the amount, and partner ranking can change independently of
this single-route numeric example.

SELF-HEAL / INVALIDATION CHALLENGE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
A full call-site check found no unconditional game-wide trade-route refresh between a remote
city's ordinary population change and another player's next `doGold()`.

`CvGame::updateTradeRoutes()` exists and is correctly used for truly global eligibility
transitions such as Free Trade and AdvCiv's cross-player anarchy dependency (the latter is the
existing WIP238 control). Population changes do not call it.

`CvPlayer::updateTradeRoutes()` is intentionally player-local. Its normal end-of-turn call is
therefore too late to prevent the stale economic application described above.

ANCESTRY
~~~~~~~~
The defect is inherited from BtS.

Civ4CE/BtS `CvCity::setPopulation()` has the same absence of a trade-route invalidator while
`getBaseTradeProfit` already uses the partner city's population and city trade yields are
cached through route rebuilding.

K-Mod retains that dependency and the same player-local end-of-turn route refresh. Base
AdvCiv and current SAS retain it as well. AdvCiv's later cross-player route fixes (e.g. the
`advc.124` global refresh when another player's anarchy changes eligibility) do not cover
partner population changes.

DUPLICATE LEDGER
~~~~~~~~~~~~~~~~
The album was searched for `getBaseTradeProfit`, partner-city population, population/trade
cache, stale trade yield and equivalent semantic combinations.

Relevant prior items are distinct:

* F401 concerns the exactly-one-route Finance hover text contract, not trade-state freshness.
* the WIP164 `updateTradeRoutes` control establishes that a rebuild itself clears reservations
  and recomputes yields correctly; F505 is about a missing cross-player trigger for that good
  rebuild.
* the practical-2328/WIP238 anarchy control proves a different cross-player dependency is
  explicitly repaired through `GC.getGame().updateTradeRoutes()`; that contrast strengthens
  F505 rather than duplicating it.
* the PA/team-change trade-route controls cover identity/eligibility transitions with explicit
  rebuild paths, not partner-population valuation.

No prior numbered root owns this missing partner-population invalidation.

REPAIR DIRECTION
~~~~~~~~~~~~~~~~
A direct `GC.getGame().updateTradeRoutes()` inside every `setPopulation` call would be correct
but unnecessarily expensive because multiple cities can grow/starve in one player turn.

A better repair is to mark game-wide trade routes dirty when a city's population actually
changes, then flush ONE global rebuild after the city's/player's population-changing phase and
before any subsequently active player's `doGold()`. In sequential play, the current player's
end-of-turn boundary is a natural batching point.

Any narrower repair must account not only for players ALREADY trading with the changed city,
but also for players whose route selection could newly switch TO the city because its
population increased. Merely refreshing current route users is therefore insufficient in the
general case.

The invariant to preserve is:

    before a player's economic commerce is booked,
    its cached trade-route partner/value set must reflect every eligible partner city's
    current population.

CURRENT STONE AFTER F505
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F505 / KI#828 CONFIRMED.
F506 / provisional KI#829 UNUSED.
Latest physical checkpoint: C031-WIP250.
No repository source files modified.

NEXT
----
Continue Queue 004 from F506 unused.

First bound F505's trade-route-cache siblings rather than splitting the same missing
cross-object invalidation root: partner-city disorder/plunder state, partner-city ownership /
team identity, route-valued population and peace-duration inputs, and any already explicit
global route invalidators.

Then continue dependency inversion across stored city aggregates: start from cached trade
routes/yields, maintenance, corporation output and commerce, enumerate external player/team/
game inputs, and verify that every mutable external input has an appropriate local/global
rebuild trigger. Prefer a cross-object invariant failure over deliberately sampled AI cache
lag, and keep F506 unused until a distinct shipped producer/consumer survives full ancestry
and album dedup.

===============================================================================
C031-WIP251 -- Queue 004 CvPlayer.cpp continuation
Date: 2026-09-03
STATUS: INCOMPLETE SAFETY CHECKPOINT -- Queue 004 remains OPEN
===============================================================================

This checkpoint continues directly from C031-WIP250. It broadens F505 with a
second partner-state manifestation and confirms the recurring peace-duration
trade-route cache defect as F506 / KI#829. F507 / provisional KI#830 is UNUSED.
No repository source file was modified.

POST-F505 SIBLING / DEDUP BOUNDARY
----------------------------------

1. Partner-city disorder / occupation belongs under F505.

`CvCity::updateTradeRoutes()` excludes a prospective partner whenever
`pLoopCity->isDisorder()` is true. `setOccupationTimer()` updates the affected
city's own state and eventually its owner's own routes, but it does not rebuild
other players' route caches when that city enters/leaves disorder.

Thus another player can temporarily retain a route to a now-disordered city or
fail to select a newly recovered city. This has the same cross-object invariant
and repair boundary as F505: an external partner-city property that participates
in route selection/value changes without notifying every route owner whose
cached selection could change. Do not split this into F506.

2. The apparent vassal-city-count -> master maintenance omission is exactly the
already-confirmed F466 / KI#789. The old finding already proves the same
`iNumVassalCities` dependency, owner-only city-count refresh, sequential `doGold`
consumer, ancestry and repair direction. No new number.

3. State-religion -> building-commerce cache work encountered the already-
confirmed F476 / KI#799. Current advc.172 religion commerce itself is not keyed
to current state-religion identity: `updateReligionCommerce` takes the maximum
religion commerce present. Holy-city pointer changes refresh the affected
religion-commerce city state. No F506 from that branch.


F506 / KI#829 -- FOREIGN TRADE PEACE-DURATION CREDIT IS SYSTEMATICALLY ONE TURN
BEHIND BECAUSE THE PEACE COUNTER ADVANCES AFTER ROUTES WERE CACHED
--------------------------------------------------------------------------------

ROOT
~~~~
Foreign trade profit includes a time-dependent peace-history modifier:

    int CvCity::getPeaceTradeModifier(TeamTypes eTeam) const
    {
        ...
        int iPeaceTurns = std::min(iFullTurns,
            GET_TEAM(getTeam()).getTurnsAtPeace(eTeam));
        ...
        return (FOREIGN_TRADE_MODIFIER * iPeaceTurns) / iFullTurns;
    }

The result is consumed only when `CvCity::updateTradeRoutes()` rebuilds the
city's cached route selection and `m_aiTradeYield`.

Current K-Mod/AdvCiv/SAS turn scheduling creates a recurring lifetime mismatch:

* near the END of player A's turn, `CvPlayer::updateTradeRoutes()` caches A's
  routes using A-team's current TurnsAtPeace value N;
* on A-team's next activation, `CvTeam::doTurn()` advances
  `changeTurnsAtPeace(eOther, 1)` to N+1;
* A's ordinary economic processing later executes `doGold()` before A's next
  end-of-turn `updateTradeRoutes()`;
* therefore A books cached trade yield calculated from N although the live
  peace-history state is already N+1.

The end-of-turn route rebuild repairs the value for N+1 only after the economy
has been booked. On the next activation the counter advances again, recreating
the same stale state. The lag repeats every peaceful turn until full foreign-
trade credit is reached.

DETERMINISTIC NORMAL-SPEED INTEGER WITNESS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The inherited global values are:

    FOREIGN_TRADE_MODIFIER                  = 150
    FOREIGN_TRADE_FULL_CREDIT_PEACE_TURNS = 50
    CAPITAL_TRADE_MODIFIER                 = 25

On Normal speed AdvCiv/SAS FullTradeCreditPercent defaults to 100%, so full
credit remains 50 turns. Peace credit therefore rises by exactly 3 percentage
points per peaceful turn before turn 50.

Choose a same-area foreign route with:

* minimum base trade profit = 100 hundredths (= 1 base Commerce), e.g. a small
  partner/short-distance route where `getBaseTradeProfit` hits its 100 floor;
* route owner's own population modifier = 0 (population <= 10 under the stock
  OUR_POPULATION_* values);
* connected-to-capital bonus = +25;
* no additional building/civic foreign-route modifier.

At TurnsAtPeace = 24:

    peace modifier = 150 * 24 / 50 = 72
    total modifier = 100 + 25 + 72 = 197
    profit times100 = 100 * 197 / 100 = 197
    cached route yield = 197 / 100 = 1 Commerce

At the next team activation TurnsAtPeace becomes 25:

    peace modifier = 150 * 25 / 50 = 75
    correct total modifier = 100 + 25 + 75 = 200
    correct profit times100 = 200
    correct route yield = 2 Commerce

But no route rebuild occurs between the team counter increment and A's
`doGold()`. A therefore books 1 Commerce from this route when the current game
state says 2. The late route rebuild then stores 2, after which the next peace-
turn increment can make it stale again at a later integer boundary.

This witness uses no special building, civic, corporation, war transition or
SAS-only content.

TURN-ORDER / SELF-HEAL CHALLENGE
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Current sequential AdvCiv/SAS activation order is:

    CvPlayer::setTurnActive(true)
      AI_updateCacheData()
      if team leader: CvTeam::doTurn()
        AI_doTurnPre()
        ...
        if !atWar: changeTurnsAtPeace(+1)
        ...
      doTurnUnits()

Later, when the player ends the turn, `CvPlayer::doTurn()` performs the economic
phase including `doGold()` and only near the end calls `updateTradeRoutes()`.
There is no intervening trade-route rebuild triggered by `setTurnsAtPeace` or
`changeTurnsAtPeace`.

For multi-member teams the team counter is advanced when the team leader is
activated. Every member's cities whose prior route cache was built under N can
therefore consume that stale cache before their own subsequent route rebuild.

The existing game-wide/player-local trade-route invalidators for Free Trade,
anarchy, plot-group/connection changes, etc. are event-specific. There is no
unconditional refresh after the peace counter changes and before economic
booking.

ANCESTRY
~~~~~~~~
The defect is inherited from BtS.

BtS already computes `getPeaceTradeModifier` from
`CvTeamAI::AI_getAtPeaceCounter`. At the start of `CvGame::doTurn`, BtS calls
every alive team's `doTurn`; `CvTeamAI::AI_doTurnPre -> AI_doCounter` increments
`AI_getAtPeaceCounter` for peaceful teams. Player trade routes, however, were
cached at the player's previous end-of-turn `updateTradeRoutes`, and the next
player `doGold()` precedes the next route rebuild. Thus the same N -> N+1 stale
booking exists there.

K-Mod moves `CvTeam::doTurn` from the global round start to team/player
activation to reduce turn-order imbalances, but still executes it before the
player's economic turn and leaves route rebuilding near player turn end. The
lifetime defect therefore remains.

AdvCiv change advc.130k replaces the randomized AI at-peace counter as the
trade-profit input with deterministic `CvTeam::m_aiTurnsAtPeace` and later
scales full-credit turns by game speed. That fixes the semantic quality of the
counter, not the cache timing: `m_aiTurnsAtPeace` is still incremented in
`CvTeam::doTurn` before economy with no route invalidation. Base AdvCiv 1.14 and
current SAS inherit this structure.

DUPLICATE LEDGER
~~~~~~~~~~~~~~~~
The album was searched for `TurnsAtPeace`, `getPeaceTradeModifier`, sustained
peace trade credit, foreign-trade cache, trade-route timing, and one-turn route
lag.

Relevant prior roots are distinct:

* F098 / KI#420 concerns Permanent-Alliance MERGING of AdvCiv's exact
  `m_aiTurnsAtPeace` state asymmetrically. It can make the underlying counter
  itself wrong. F506 occurs with perfectly correct bilateral peace counters and
  concerns failure to rebuild cached city trade output after the ordinary
  per-turn counter increment.
* F505 / KI#828 concerns event-driven external PARTNER-CITY state (population,
  and the disorder/occupation sibling above) changing another player's cached
  route selection/value without cross-object invalidation. F506 is a recurring
  owner-team time-state transition that predictably happens every peaceful team
  turn even if no city changes at all.
* existing route-rebuild controls show that `updateTradeRoutes()` correctly
  recomputes the route once called; F506 is specifically the missing scheduling
  point between peace-counter advancement and economic consumption.

No earlier numbered root owns this recurring peace-credit cache lifetime.

REPAIR DIRECTION
~~~~~~~~~~~~~~~~
The strongest structural repair is to ensure each player's route cache is
rebuilt from current external/team state BEFORE that player's economic commerce
is booked, rather than relying on the previous turn's end-of-turn snapshot.

For current K-Mod/AdvCiv sequencing, a player-local `updateTradeRoutes()` after
its team's `doTurn()` has advanced peace counters and before `doGold()` would
repair F506. More broadly, moving/batching the routine route refresh to a
start-of-economic-turn boundary can also naturally absorb F505-style changes
that happened during other players' intervening turns.

A direct rebuild from every `changeTurnsAtPeace(+1)` would be functionally
correct but potentially wasteful, especially for teams with multiple relations.
Prefer one batched refresh after the team counter phase.

The invariant is:

    before `doGold()` consumes cached city trade yield,
    that cache must have been rebuilt after the latest TurnsAtPeace increment
    for the route owner's team.

Classification: inherited BtS trade-route cache lifetime / turn-order bug.


CURRENT STONE AFTER F506
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F506 / KI#829 CONFIRMED.
F507 / provisional KI#830 UNUSED.
Latest physical checkpoint: C031-WIP251.
No repository source files modified.

NEXT
----
Continue Queue 004 from F507 unused.

Finish the remaining trade-route dependency graph before leaving it: war/peace
reset transitions, open-borders/free-trade/connection eligibility, blockade /
plunder state, ownership/team changes, and route-owner modifiers. Dedup anything
sharing F505/F506's cache root or an older route finding.

Then resume stored-city-aggregate dependency inversion across corporation,
building commerce, maintenance and other cached outputs, prioritizing mutable
external player/team/game inputs whose mutator lacks a corresponding cross-
object rebuild.
===============================================================================

===============================================================================
C031-WIP252 -- Queue 004 CvPlayer.cpp continuation
Date: 2026-09-03
STATUS: INCOMPLETE SAFETY CHECKPOINT -- Queue 004 remains OPEN
===============================================================================

This checkpoint continues directly from C031-WIP251. It broadens F505 with the
AdvCiv revealed-partner eligibility sibling, closes several remaining route-state
transitions, and confirms the inherited trade-network-topology cache defect as
F507 / KI#830. F508 / provisional KI#831 is UNUSED. No repository source file
was modified.

POST-F506 TRADE-ROUTE DEPENDENCY BOUNDARY
-----------------------------------------

1. War declaration / peace creation are not new route-cache defects.
`CvTeam::declareWar` and `CvTeam::makePeace` both update plot groups and then call
`updateTradeRoutes()` for the members of both involved teams. The peace-history
counter reset and the relationship/connection eligibility change are therefore
reconciled immediately on those transitions.

2. Player/city route-count and route-value modifiers are locally maintained:
`changeTradeRoutes`, `changeCoastalTradeRoutes`, `changeTradeYieldModifier`,
`changeExtraTradeRoutes`, `changeTradeRouteModifier`, and
`changeForeignTradeRouteModifier` all rebuild the affected owner's/city's routes.
`setPlundered` likewise rebuilds the affected city's routes.

3. AdvCiv `advc.124` CITY-REVEALED eligibility is a further F505 manifestation,
not F507.

Current `CvCity::updateTradeRoutes()` excludes a prospective partner when:

    !pLoopCity->isRevealed(getTeam())

AdvCiv's own comment explains the important state: a network connection along
revealed plots guarantees the city tile is revealed, but not that a city founded
there later is itself revealed. Ordinary unit movement calls visibility/revelation
with plot-group updates. `CvCity::setRevealed(eTeam, true)` updates attitude /
visibility state but does not rebuild the revealing team's players' cached trade
routes.

Thus a foreign city can already be network-connected but excluded only because
the city object is unknown; a unit then reveals it during the unit phase, making
it a legal route partner before `doGold()`, while the route owner's cache remains
on the pre-reveal selection until the late end-of-turn rebuild.

This is the same F505 external-partner-state invariant as population and
occupation/disorder: a property of a prospective partner changes eligibility /
value without notifying all route-owner caches that may select it. Do not split
it into a separate finding.


F507 / KI#830 -- ROAD / ROUTE-NETWORK TOPOLOGY CHANGES UPDATE PLOT GROUPS BUT NOT
CACHED TRADE ROUTES BEFORE ECONOMIC BOOKING
--------------------------------------------------------------------------------

ROOT
~~~~
`CvCity::updateTradeRoutes()` still carries the inherited source comment:

    // XXX eventually, this needs to be done when roads are built/destroyed...

The TODO is semantically live.

Trade-route eligibility requires the candidate partner city to belong to the
same player-specific trade-network plot group as the route-owning city:

    if (!IGNORE_PLOT_GROUP_FOR_TRADE_ROUTES &&
        pLoopCity->plotGroup(kOwner.getID()) != plotGroup(kOwner.getID()))
    {
        continue;
    }

Road/route creation and destruction can change that predicate immediately.
`CvPlot::setRouteType` correctly changes the route and, when route presence
changes, calls `updatePlotGroup()`. `CvPlot::updatePlotGroup` /
`CvPlotGroup::{addPlot,removePlot,recalculatePlots}` then repair the actual
player-specific network topology and connected bonus state.

But none of those topology mutators calls `CvPlayer::updateTradeRoutes`,
`CvGame::updateTradeRoutes`, or the affected cities' `updateTradeRoutes`.
The city therefore retains its old `m_aTradeCities` selection and
`m_aiTradeYield` after the authoritative connectivity state has changed.

ORDINARY BUILD PATH
~~~~~~~~~~~~~~~~~~~
Worker route completion reaches the omission directly:

    CvPlot::changeBuildProgress(...)
      if (kBuild.getRoute() != NO_ROUTE)
          setRouteType(kBuild.getRoute());

`setRouteType` uses its default `bUpdatePlotGroups=true`, so the newly completed
Road/Railroad is incorporated into plot groups immediately. There is no trade-
route rebuild afterward.

Current AI units perform their automated movement/build phase from
`CvPlayer::setTurnActive(true) -> doTurnUnits()` before the later end-of-turn
`CvPlayer::doTurn()` economic phase. Human Workers can likewise complete the
build before the human ends the turn.

At `CvPlayer::doTurn()` the order is:

    AI_assignWorkingPlots();
    verifyGoldCommercePercent();
    doGold();
    doResearch();
    ... city doTurn ...
    ...
    updateTradeRoutes();       // late

So the routine refresh is too late to affect that turn's Gold/Research booking.

DETERMINISTIC CONNECTION WITNESS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Use ordinary current data:

* Player A has at least one available trade-route slot.
* A and B can otherwise trade (e.g. Open Borders / normal foreign-trade state).
* B has an eligible city that would yield at least the inherited minimum 1
  Commerce once connected.
* One missing Road segment keeps B's network outside A's city plot group.
* An A Worker completes that final Road segment during A's unit phase.

After `setRouteType(ROAD)` and plot-group merging, the live connectivity test in
`updateTradeRoutes()` would now accept B's city. Yet A's cached route set remains
unchanged. A's subsequent `doGold()` therefore books 0 from that newly legal
route (or retains a worse old partner) before the late route rebuild finally
selects B's city.

The route benefit begins one economic turn later than the authoritative network
state permits.

REVERSE / OVERPAYMENT WITNESS
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The reverse makes the stale-cache consequence even clearer.

Suppose A currently has a cached route to B through a single critical Road
segment. Between A's route rebuild and A's next economic phase, an enemy unit
pillages the segment. Ordinary `CvUnit::pillageRoute()` calls:

    getPlot().setRouteType(getRoutePillage());

and air bombing uses the same route setter. Plot groups immediately split, so
B's city no longer passes the live connectivity predicate. No trade-route cache
rebuild follows.

A can therefore reach its next `doGold()` still carrying the disconnected
route's old `m_aiTradeYield`, overbooking its Commerce once. Its late
`updateTradeRoutes()` then removes/replaces the route.

This reverse path does not require the route-owning player itself to perform a
Worker action and is therefore also a clean sequential cross-turn reproduction.

SELF-HEAL / REPAIR BOUNDARY
~~~~~~~~~~~~~~~~~~~~~~~~~~~
The normal end-of-turn `CvPlayer::updateTradeRoutes()` is a self-heal, but it is
provably too late because `doGold()` and `doResearch()` precede it.

The best structural repair is not necessarily to rebuild routes from every
single `setRouteType` call. Multiple Worker builds/pillages can occur during a
unit phase, and plot-group changes can touch several players. A batched dirty
flag or a single route refresh immediately before the player's economic phase
would be both safer and cheaper.

This dovetails with F505/F506's scheduling observations: moving the routine
player route refresh to a pre-economic boundary after unit actions and current
team/external state are settled would repair topology changes as well as many
other stale-route windows. Event-specific invalidators are still useful for
mid-turn consumers/UI, but the economy should not rely on a snapshot taken at
the previous turn's end.

The invariant is:

    before `doGold()` / `doResearch()` consume cached trade yield,
    each city route cache must reflect the owner's CURRENT trade-network plot
    groups.

ANCESTRY
~~~~~~~~
This is inherited from stock BtS and is explicitly acknowledged there by the
same TODO comment.

Civ4CE/BtS `CvCity::updateTradeRoutes` contains:

    // XXX eventually, this needs to be done when roads are built/destroyed...

BtS `CvPlot::setRouteType` updates plot groups when route presence changes but
never updates trade routes. Player economics precede the player's routine
`updateTradeRoutes()` there as well.

K-Mod retains both the TODO and the setter structure (adding only unrelated
border-danger invalidation when a route is removed). Base AdvCiv 1.14 retains
the TODO and the same plot-group-without-route-refresh contract. Current SAS
retains all of it.

DUPLICATE LEDGER
~~~~~~~~~~~~~~~~
The album was searched for roads/routes built or destroyed, `setRouteType`,
plot-group connection changes, route pillaging, trade-network cache, and the
literal inherited TODO.

No existing numbered root owns this topology transition.

Relevant nearby roots are distinct:

* F505 / KI#828: prospective PARTNER-CITY state (population, disorder,
  revealed-city eligibility) changes another player's cached route
  selection/value.
* F506 / KI#829: the ROUTE OWNER TEAM's sustained-peace time counter advances
  before economy while cached trade yield contains the previous counter.
* F507: the authoritative PLAYER-SPECIFIC TRADE NETWORK / PLOT GROUP itself
  changes while the city retains route selection/yield from the old network.

All three expose the broader weakness of a late routine route refresh, but they
have different producers and event-driven invalidation responsibilities. F507
also survives if F505 partner-state notifications and F506 peace-counter
refreshes are individually repaired.

Classification: inherited BtS acknowledged trade-network / trade-route cache
lifetime bug.


CURRENT STONE AFTER F507
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F507 / KI#830 CONFIRMED.
F508 / provisional KI#831 UNUSED.
Latest physical checkpoint: C031-WIP252.
No repository source files modified.

NEXT
----
Continue Queue 004 from F508 unused.

The trade-route family is now well bounded. Before leaving it, only close any
remaining cheap eligibility transitions (ownership/team identity, Free Trade /
Open Borders, blockade and terrain/river-trade tech changes) and do not split
symptoms already owned by F505-F507.

Then return to stored-city-aggregate dependency inversion. Corporation output
currently looks well maintained for local bonus changes, occupation/anarchy,
team technology / bonus-obsolescence transitions and branch/HQ state already
covered by F491/F496. Continue through maintenance / commerce / health-happiness
and other persistent aggregates, prioritizing mutable EXTERNAL inputs with no
matching cross-object rebuild.
===============================================================================


===============================================================================
C031-WIP253 -- QUEUE 004 CONTINUATION AFTER WIP252
===============================================================================

Continued Queue 004 `CvGameCoreDLL/CvPlayer.cpp` directly from C031-WIP252 with
F508 / provisional KI#831 unused. The stored-city-aggregate dependency inversion
first finished the pending passive espionage-city-visibility candidate. No
repository source files were modified.


F508 / KI#831 -- DIRECTIONAL ESPIONAGE-POINT / PASSIVE-COST CHANGES DO NOT
REFRESH CACHED CITY ESPIONAGE VISIBILITY, SO REAL MAP SIGHT CAN SURVIVE AFTER
PAYMENT OR FAIL TO APPEAR AFTER NEW EP
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- inherited BtS passive-espionage visibility lifetime bug retained by
K-Mod, Base AdvCiv 1.14 and current AdvCiv-SAS.

ROOT / STORED GAMEPLAY STATE
----------------------------
Each city stores one real passive-espionage visibility bit per observing team:

    CvCity::m_abEspionageVisibility
    CvCity::getEspionageVisibility(eTeam)

`CvCity::setEspionageVisibility` does not merely update UI state. When the bit
changes it removes the city's old sight, stores the bit, and adds the new sight:

    getPlot().updateSight(false, bUpdatePlotGroups);
    m_abEspionageVisibility.set(eTeam, bNewValue);
    getPlot().updateSight(true, bUpdatePlotGroups);

`CvPlot::updateSight` consumes that bit and grants/removes adjacent map sight:

    if (pCity->getEspionageVisibility(it->getID()))
        changeAdjacentSight(it->getID(), PLOT_VISIBILITY_RANGE, ...);

Thus a stale bit means genuinely incorrect team map visibility, not a stale
advisor display.

The correct bit is recomputed by `CvCity::updateEspionageVisibility`. For every
other alive team it scans live passive missions with `VisibilityLevel > 0` and
asks an observing-team member:

    canDoEspionageMission(passiveMission, cityOwner, city.plot())

`canDoEspionageMission` compares the observing team's CURRENT directional EP
against the CURRENT mission cost. Therefore the stored city bit depends directly
on `CvTeam::getEspionagePointsAgainstTeam(targetTeam)` and on every mutable input
to the passive mission cost.

CURRENT SHIPPED PASSIVE MISSION
-------------------------------
Current SAS retains the normal live passive mission:

    ESPIONAGEMISSION_CITY_VISIBILITY
    iCost = 120
    bIsPassive = 1
    bTargetsCity = 1
    iVisibilityLevel = 1

So this stored state is actively used by shipped gameplay.

MISSING EP INVALIDATOR
----------------------
Directional EP is mutated centrally through:

    CvTeam::changeEspionagePointsAgainstTeam
      -> CvTeam::setEspionagePointsAgainstTeam

Current SAS `setEspionagePointsAgainstTeam` performs:

    m_aiEspionagePointsAgainstTeam.set(eIndex, iValue);
    verifySpyUnitsValidPlot();
    GET_TEAM(eIndex).verifySpyUnitsValidPlot();
    ... advc.091 updateEverSeenDemographics ...

but never visits the target team's cities and never calls
`CvCity::updateEspionageVisibility` / `setEspionageVisibility`.

A whole-tree caller search finds the routine city-visibility refresh only at:

* city creation / acquisition setup; and
* each target city's own `CvCity::doTurn()`.

There is no event-driven refresh attached to ordinary EP spending or gain.

CONSTRUCTION-INDEPENDENT THRESHOLD PROOF
----------------------------------------
No special choice of distance/population/culture modifiers is required.

Let:

    P = the actual current final cost of City Visibility at B's city;
    A = the actual current positive final cost of any legal active mission
        against B.

Both are concrete positive integer engine results after all currently applicable
mission modifiers.

Arrange A-team's current directional EP against B-team to be exactly:

    P + A - 1

This is an ordinary reachable integer state.

Before the active mission:

* EP >= P, so City Visibility is legitimately active and the city's stored bit
  can be true;
* EP >= A, so the active mission is affordable.

A successful active mission deducts exactly `A` through:

    CvPlayer::doEspionageMission
      -> GET_TEAM(getTeam()).changeEspionagePointsAgainstTeam(
             eTargetTeam, -iMissionCost)

leaving:

    (P + A - 1) - A = P - 1

The passive City Visibility predicate is therefore immediately false by
construction. Its mission cost itself is not made stale by this deduction:
K-Mod's EP-ratio modifier uses `getEspionagePointsEver`, while active spending
reduces the current directional points rather than the historical points-ever
counter.

Yet `setEspionagePointsAgainstTeam` does not refresh B's city visibility. The
stored bit remains true and `CvPlot` continues granting A-team adjacent sight
around B's city despite A now having one EP less than the required threshold.

CURRENT ACTIVE-MISSION PRODUCER
-------------------------------
The reproduction does not depend on an obscure Python/API transition. Current
XML ships multiple positive-cost active missions. A particularly simple one is:

    ESPIONAGEMISSION_DESTROY_IMPROVEMENT
    iCost = 75
    bIsPassive = 0
    bSelectPlot = 1
    bDestroyImprovement = 1

When it successfully destroys an improvement or route, `doEspionageMission`
sets `bSomethingHappened=true` and then deducts the computed final mission cost
through the directional-EP mutator above.

The symbolic `P + A - 1` construction deliberately uses the engine's final
computed costs, so it remains valid under any ordinary distance, population,
culture, stationary-spy, spending-ratio or counterespionage modifiers present in
the chosen game state.

REAL SAME-UNIT-PHASE CONSEQUENCE
--------------------------------
The wrong bit feeds actual plot visibility immediately. If the EP payment occurs
during A's unit phase, then after the mission A can continue to see the city and
its adjacent plots for the rest of that phase even though the live passive
predicate has already failed.

This can expose foreign unit positions / movement and any other information
normally gated by `CvPlot::isVisible` until B's city eventually executes its own
`updateEspionageVisibility`.

The stale city bit is also read directly by current AdvCiv AI valuation. For
example `CvPlayerAI::AI_assetVal` counts settled Great People in a foreign city
when:

    bOwn || c.getEspionageVisibility(getTeam())

so later AI decisions can consume information under a passive-visibility right
that has already expired.

The target city's next normal `CvCity::doTurn()` is only a delayed self-heal. If
that city already took its turn earlier in the round, the wrong sight can persist
across the remainder of the spying player's turn and until the target's next
city turn.

REVERSE / GREAT-SPY UNDER-VISIBILITY
------------------------------------
The same missing invalidator operates in the opposite direction.

Current `CvUnit::infiltrate()` adds Great Spy infiltration points directly via:

    GET_TEAM(getTeam()).changeEspionagePointsAgainstTeam(
        TEAMID(getPlot().getOwner()), iPoints);

If this addition takes the spying team from below P to at least P during the unit
phase, `canDoEspionageMission(CITY_VISIBILITY, ...)` becomes true immediately,
but the target city's stored visibility bit remains false. The spying team
therefore fails to receive map sight that it already qualifies for until the
city's later routine refresh.

This gives both directions from normal shipped unit actions:

* active mission spends EP -> too much stale sight;
* Great Spy infiltration gains EP -> missing legitimate sight.

BROADER COST-INPUT SIBLINGS
---------------------------
Directional EP is sufficient to prove F508, but `m_abEspionageVisibility` also
depends on the passive mission's current COST. Therefore non-EP threshold changes
can produce the same stale city-sight state.

The F461/KI#784 investigation already established ordinary examples of those
cost inputs: timed Counterespionage expiry and K-Mod's population-sensitive
espionage modifier can change passive thresholds without changing current EP.
Those transitions likewise do not call `CvCity::updateEspionageVisibility`.

They are F508 siblings for the live city-sight cache. F461 remains distinct
because it concerns AdvCiv's separate persistent `m_abEverSeenDemographics`
history latch and its incomplete threshold-crossing updater.

ANCESTRY
--------
Civ4CE/BtS has the same core contract:

* `CvCity::updateEspionageVisibility` is the stored passive-sight refresh;
* city `doTurn()` invokes it routinely;
* `CvTeam::setEspionagePointsAgainstTeam` changes directional EP and verifies Spy
  plots but does not refresh target-city espionage visibility.

K-Mod retains that structure. Base AdvCiv 1.14 retains it and adds only the
`advc.091` `updateEverSeenDemographics` side effect to the EP setter; it does not
refresh `m_abEspionageVisibility`. Current SAS inherits the same arrangement.

The defect is therefore inherited from stock BtS, not introduced by the later
AdvCiv demographics latch.

DUPLICATE LEDGER
----------------
Full-album searches for `m_abEspionageVisibility`, `getEspionageVisibility`,
City Visibility, passive visibility, directional EP and the Permanent-Alliance
espionage findings found no existing numbered root for ordinary EP/cost changes
leaving the live per-city passive-sight bit stale.

Nearby findings are distinct:

* F461 / KI#784: AdvCiv's persistent `m_abEverSeenDemographics` "ever seen"
  information latch misses passive-cost threshold transitions. Different stored
  field and consequence.
* F078-era Permanent Alliance espionage material: lifecycle/merge cleanup of
  city visibility or stolen-map-visibility relations when teams are absorbed.
  Different producer and transition.
* Other espionage findings around mission actors/cost/help affect legality,
  presentation or mission execution, not the live city passive-sight cache after
  the authoritative EP value has changed.

REPAIR DIRECTION
----------------
The invariant should be:

    whenever a team's current passive City Visibility entitlement against another
    team can cross a threshold, the affected target cities' stored espionage
    visibility bits must be refreshed before map sight is next consumed.

At minimum, directional EP changes should notify the target team's cities. A
naive repair could iterate target-team cities from
`setEspionagePointsAgainstTeam` and update their espionage visibility, but the
current updater recalculates all observing teams and may be unnecessarily broad.
A targeted observer-team refresh / dirty mechanism would likely be cheaper.

A complete repair also has to account for non-EP cost inputs such as
Counterespionage modifier expiry and population-driven espionage modifiers; fixing
only the EP setter would repair the active-mission / infiltration reproductions
but leave those F508 siblings.

Classification: inherited BtS passive-city-espionage-visibility cache lifetime /
invalidation bug.


CURRENT STONE AFTER F508
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F508 / KI#831 CONFIRMED.
F509 / provisional KI#832 UNUSED.
Latest physical checkpoint: C031-WIP253.
No repository source files modified.

NEXT
----
Continue Queue 004 from F509 unused.

First bound F508 cheaply: inspect other passive-espionage stored outputs
(Investigate City / research visibility / demographics) and separate values that
are calculated dynamically from values that are actually persisted in city/plot
state. Do not split F508 threshold-input siblings.

Then resume the stored-city-aggregate dependency inversion after health/happiness,
power-health and espionage visibility. Prioritize persistent aggregates or bits
whose calculators read mutable EXTERNAL player/team/game state while their
mutators notify only the source object.
===============================================================================


===============================================================================
C031-WIP254 — QUEUE 004 CVPLAYER.CPP — F509 / KI#832 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F509 / KI#832 are CONFIRMED.
F510 / provisional KI#833 is UNUSED.
No repository source files modified.

POST-WIP253 F508 SIBLING BOUNDARY
---------------------------------
The remaining passive-espionage surfaces do not justify splitting F508.

`CvPlayer::canSeeResearch`, `CvPlayer::canSeeDemographics` and the passive
Investigate City checks reach `canDoEspionageMission` dynamically when their
information is requested. They therefore follow current directional EP and
current passive-mission cost without an equivalent long-lived per-city
entitlement bit.

F508 remains specifically rooted in the exceptional persisted
`CvCity::m_abEspionageVisibility` state and its real `CvPlot` sight side effect.

POST-WIP253 STORED-AGGREGATE CONTROLS
-------------------------------------
A candidate involving corporation maintenance and current inflation was
rejected as a duplicate. It is exactly the already-confirmed F421/KI#744:
corporation maintenance contains an inverse-inflation component while the city
maintenance result is cached without a turn-based inflation invalidator.
F509 therefore remained unused through that pass.

The surrounding-health/happiness dependency inversion then exposed a distinct
AdvCiv-specific root.

-------------------------------------------------------------------------------
F509 / KI#832 — CONFIRMED
ADVC PEACEFUL-ACCESS / PLOT-OWNERSHIP CHANGES DO NOT INVALIDATE CACHED
SURROUNDING HEALTH AND HAPPINESS
-------------------------------------------------------------------------------

ROOT
----
AdvCiv's `advc.901` surrounding health/happiness cache deliberately makes
POSITIVE feature/improvement effects conditional on whether the city team can
access the plot peacefully:

    CvCity::updateSurroundingHealthHappiness()
      -> CvTeam::canAccessHappyHealth(kPlot, iHealthOrHappy)

and:

    bool CvTeam::canAccessHappyHealth(CvPlot const& kPlot,
                                      int iHealthOrHappy) const
    {
        return (iHealthOrHappy <= 0 || !kPlot.isOwned() ||
                canPeacefullyEnter(kPlot.getTeam()));
    }

`canPeacefullyEnter` in turn depends on mutable RELATION / IDENTITY state:

    isOpenBorders(eTerritoryOwner)
    || getID() == eTerritoryOwner
    || getTeam(eTerritoryOwner).isVassal(getID())

Thus the mathematically correct cached city value can change when:

* Open Borders with the plot owner begins or ends;
* a relevant master-vassal relationship begins or ends;
* the plot changes owner (including owned <-> unowned or between teams).

None of those transitions changes the feature or improvement itself.

STORED CITY STATE
-----------------
`CvCity::updateSurroundingHealthHappiness()` stores four derived values:

    m_iSurroundingGoodHappiness
    m_iSurroundingBadHappiness
    m_iSurroundingGoodHealth
    m_iSurroundingBadHealth

The positive values are used by the ordinary authoritative city happiness and
health calculations:

    happyLevel / angryPopulation
    goodHealth / healthRate
    foodConsumption / foodDifference

so this is gameplay state rather than tooltip-only information.

INVALIDATION ASYMMETRY
----------------------
AdvCiv explicitly event-refreshes this cache when the LOCAL SOURCE changes:

* `CvPlot::setFeatureType` iterates nearby cities and calls
  `updateSurroundingHealthHappiness()`;
* `CvPlot::setImprovementType` does the same;
* player feature-happiness changes call `CvPlayer::updateFeatureHappiness`,
  which refreshes every city through the same surrounding updater.

But the other inputs to the same calculator are not wired to it:

* `CvTeam::setOpenBorders` updates attitude, unit validity, paths and trade
  routes, but not surrounding health/happiness;
* `CvTeam::setVassal` updates sight, unit/outside-unit state, war relations,
  military happiness, culture/plot groups etc., but not surrounding
  health/happiness;
* ordinary non-city `CvPlot::setOwner` updates ownership, sight, working city,
  yields and plot groups, but not nearby surrounding health/happiness.

The complete current caller set for `updateSurroundingHealthHappiness` is only:

* city initialization;
* the late once-per-city-turn refresh;
* the player feature-happiness wrapper;
* feature changes;
* improvement changes.

There is therefore no hidden relation/ownership invalidator.

CURRENT SHIPPED NUMERIC REPRODUCTION — OPEN BORDERS / FOREST HEALTH
-------------------------------------------------------------------
Current SAS ships the standard Forest feature with:

    FEATURE_FOREST
    iHealthPercent = 50

Arrange an A-owned city whose city radius contains TWO B-owned Forest plots.
The feature itself does not change during the reproduction.

While A and B have Open Borders:

    A.canPeacefullyEnter(B) == true

so both positive Forest-health contributions are accessible and the city caches:

    50 + 50 = 100 percent -> +1 surrounding good health.

Now end Open Borders.

The live access predicate immediately becomes false for B-owned plots, so the
correct surrounding health contribution from those two Forests is now 0.
However `CvTeam::setOpenBorders` does not refresh A's city, leaving the cached
+1 health in place.

Choose the rest of the city state so this point of health is exactly the
boundary between:

    correct post-OB healthRate = -1
    stale cached healthRate    =  0

The stale city therefore has a directly different food-consumption result.

REAL NEXT-TURN CONSUMER BEFORE SELF-HEAL
----------------------------------------
The routine city refresh is too late to prevent gameplay consumption.

At the start/end-processing of A's next player turn, `CvPlayer::doTurn()` runs:

    AI_doTurnPre
    AI_assignWorkingPlots
    doGold
    doResearch
    ...
    each CvCity::doTurn

Inside `CvCity::doTurn`, the order begins:

    AI().AI_doTurn()
    doCheckProduction()
    doGrowth()
    doCulture()
    doProduction()
    ...

and only much later executes:

    updateSurroundingHealthHappiness();

Therefore the stale +1 health survives through `doGrowth()`.
`foodDifference()` consumes `foodConsumption()`, which consumes `healthRate()`;
with the boundary state above, the city books ONE EXTRA FOOD before the late
refresh finally removes the inaccessible Forest health.

This provides a deterministic authoritative gameplay consequence, not merely
an AI-value, citizen-assignment or UI mismatch.

HAPPINESS SIBLING — FOREST PRESERVE
-----------------------------------
Current SAS also ships:

    IMPROVEMENT_FOREST_PRESERVE
    iHappiness = +1

A B-owned Forest Preserve inside A's city radius therefore gives A +1 cached
surrounding happiness while A can peacefully access B's territory, and should
stop doing so when access is lost. The same missing invalidator can leave the
+1 happiness active through A's next global citizen-assignment pass and the
city's early AI/growth/production work.

The Forest-health reproduction is preferred as the primary proof because its
+1 point can be made to alter `doGrowth()` directly without relying on an AI
assignment choice.

OTHER INPUT SIBLINGS UNDER THE SAME ROOT
----------------------------------------
Plot ownership
~~~~~~~~~~~~~~
`canAccessHappyHealth` treats an unowned plot as accessible and an owned plot as
accessible only according to the city team's peaceful-entry relation. Ordinary
`CvPlot::setOwner` therefore changes an input to the surrounding cache even
when the feature/improvement is unchanged, but it has no surrounding-cache
refresh for nearby cities.

Master-vassal access
~~~~~~~~~~~~~~~~~~~~
`canPeacefullyEnter` lets a master access territory owned by its vassal.
`CvTeam::setVassal` can therefore add/remove positive surrounding effects from
master cities whose radii overlap vassal-owned plots, but it likewise has no
surrounding-cache refresh.

These are not separate findings: they mutate different inputs to the same
`canAccessHappyHealth` dependency and require the same class of missing
invalidation.

ANCESTRY / INTRODUCTION BOUNDARY
--------------------------------
This root is AdvCiv-specific rather than inherited from BtS/K-Mod.

Current K-Mod and Civ4CE/BtS do not contain `canAccessHappyHealth` or the older
`canAccessImprovement` access gate.

AdvCiv practical commit 1732 (`d595a1df9e`, title:
"Health tag for improvements; merge xml-tags-as-enums branch.") introduced the
`advc.901` combined surrounding cache and `CvTeam::canAccessImprovement`, making
positive IMPROVEMENT health/happiness conditional on `canPeacefullyEnter`.
That commit added feature/improvement source refreshes, but no Open Borders,
vassal or ownership refresh.

AdvCiv practical commit 2203 (`7d75582d60`, title:
"No Forest health from closed-borders rivals") deliberately generalized the
same rule into `canAccessHappyHealth` and applied it to positive FEATURE health
and happiness too. Again, no corresponding relation-state invalidator was
added.

Base AdvCiv 1.14 retains that dependency and its `setOpenBorders` lacks a
surrounding-health/happiness update. Current SAS inherits the same structure.

Thus the bug was introduced with AdvCiv's `advc.901` access-dependent
surrounding-effect design and broadened when Forest health was explicitly made
closed-border-sensitive.

DUPLICATE LEDGER
----------------
Full-album searches for surrounding health/happiness, `canAccessHappyHealth`,
closed borders, peaceful access, Forest health, Forest Preserve happiness,
Open Borders + health/happiness, plot ownership + health and vassal + health
found no existing numbered root for relation/ownership transitions leaving the
runtime city cache stale.

The closest old material is a Queue-003 CONTROL around
`setPlotHealthHappyHelp` / `setHealthHappyBuildActionHelp`. That control proved
only that the UI access rule through `CvTeam::canAccessHappyHealth` agrees with
the runtime surrounding-health/happiness CALCULATOR. It did not inspect the
lifetime/invalidation of the stored runtime city values when access changes.

Other nearby player-mutator controls established that direct
`changeFeatureHappiness` correctly calls `updateFeatureHappiness`; that is the
SOURCE-change side of the asymmetry and strengthens rather than duplicates
F509.

REPAIR DIRECTION
----------------
The cache invariant is:

    whenever ownership or diplomatic-access state changes whether a city team
    can peacefully access a plot in that city's radius, the affected city's
    cached surrounding health/happiness must be rebuilt before health/happiness
    is next consumed.

Natural repair hooks are therefore:

1. Open Borders changes:
   refresh member cities of the affected teams after the relation bit changes
   (or use a targeted nearby/dirty mechanism if performance warrants it).

2. Vassal relation changes:
   refresh the master-side cities whose access to vassal territory can change;
   a conservative affected-team refresh is simpler and safer.

3. Plot ownership changes:
   after non-city ownership has settled, call
   `updateSurroundingHealthHappiness()` for nearby cities, matching the existing
   feature/improvement hooks.

A centralized "surrounding access state changed" helper would reduce the risk
of repairing Open Borders while leaving ownership/vassal siblings behind.

Classification: AdvCiv-introduced cross-object surrounding-health/happiness
cache invalidation bug.


CURRENT STONE AFTER F509
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F509 / KI#832 CONFIRMED.
F510 / provisional KI#833 UNUSED.
Latest physical checkpoint: C031-WIP254.
No repository source files modified.

NEXT
----
Continue Queue 004 from F510 unused.

First bound F509 cheaply against other `canPeacefullyEnter`-dependent cached
city state so the same diplomatic-access omission is not split into symptoms.
Then resume the stored-city-aggregate dependency inversion. Prefer aggregates
whose calculators read mutable external player/team/game state while their
source mutators notify only the local object.
===============================================================================

===============================================================================
C031-WIP255 -- QUEUE 004 DEEP RE-AUDIT: WAR-PLAN TRANSITIONS LEAVE PLAYER STRATEGY HASH STALE
Date: 2026-09-03
Model: ChatGPT-5.6-Sol
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F509 remain confirmed.
- New confirmed root in this checkpoint: F510 / KI#833.
- Next root number: F511 / KI#834 remains UNUSED.
- No repository source files were modified.

POST-F509 BOUNDARY CLOSURE
--------------------------
The immediate `canPeacefullyEnter` sibling sweep is clean. Its other current uses are
live movement / legality / pathfinding / diplomacy predicates rather than persistent
city aggregates. `canAccessHappyHealth` is the relevant place where peaceful-access
identity is copied into long-lived city health/happiness state, so F509 does not need
to absorb unrelated movement-state behavior.

F510 / KI#833 -- War-plan transitions do not refresh the cached player strategy hash, so a newly chosen war can enter the same unit phase with pre-war CRUSH / PRODUCTION strategy bits
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
Current AdvCiv/SAS stores the whole AI strategy set in `CvPlayerAI::m_eStrategyHash`.
K-Mod changed the old BtS lazy getter into an explicit `AI_updateStrategyHash()` refresh,
and current sequential player activation runs that refresh BEFORE the team's war-planning
turn:

    CvPlayer::setTurnActive(true)
        -> AI().AI_updateCacheData()
            -> AI_updateStrategyHash()
        -> GET_TEAM(getTeam()).doTurn()          // team leader only
            -> CvTeamAI::AI_doTurnPost()
                -> AI_doWar()
                    -> AI_setWarPlan(...)
        -> doTurnUnits()
            -> AI().AI_doTurnUnitsPre()

`CvTeamAI::AI_setWarPlan()` changes strategic state immediately. It:

    * updates cached war-plan counts;
    * stores the new plan;
    * resets the plan-state counter;
    * recalculates area strategies;
    * dirties every AI member's city production;

but it never recomputes / invalidates any member's strategy hash.

This is a real missing dependency because current `AI_updateStrategyHash()` directly reads
war-plan state for multiple strategy bits, most clearly `AI_STRATEGY_CRUSH` and
`AI_STRATEGY_PRODUCTION`.

Therefore the normal scheduler can deliberately install a new war plan and dirty production
so that the plan affects same-turn behavior, while all player strategy queries in that same
unit/player turn still see the hash computed before the plan existed.

CURRENT CRUSH THRESHOLD REPRODUCTION
------------------------------------
A compact current-data witness can be built with a one-member Bismarck team.
Current SAS XML gives Bismarck:

    FLAVOR_MILITARY = 10

so the CRUSH random term is:

    AI_getStrategyRand(13) % (4 + 10/2)
    = AI_getStrategyRand(13) % 9

Use an ordinary state where:

1. the pre-turn strategy hash already has `AI_STRATEGY_DAGGER`;
2. the team has no existing war plan;
3. there is exactly one close, non-vassal eligible target;
4. current war-success contribution is 0;
5. `AI_getStrategyRand(13) % 9 == 4`.

The random remainder is reachable by the real strategy-random generator; e.g. in the
Ancient-era shift, multiple legal stored strategy-rand values produce remainder 4.

Before the team war decision:

    iWarCount = 0

so CRUSH cannot activate regardless of the score.

During `AI_doWar()`, the existing Dagger strategy is itself one of the values read from the
already-computed hash. If total-war target selection succeeds, current code deliberately uses
Dagger to skip preparation and installs a direct total plan:

    AI_setWarPlan(eBestTarget,
        iDaggerCount > 0 ? WARPLAN_TOTAL : WARPLAN_PREPARING_TOTAL);

Re-evaluating the CRUSH formula against the now-correct post-plan state gives:

    strategy random                         +4
    current Dagger                          +3
    new WARPLAN_TOTAL (6, halved by advc)  +3
    ------------------------------------------
    iCrushValue                            10

The one close target now also gives:

    iWarCount = 1

and the current activation condition is:

    iWarCount == 1 && iCrushValue >= 10

Therefore the mathematically correct post-plan player strategy is CRUSH = true.
The stored `m_eStrategyHash`, however, remains the pre-plan value CRUSH = false.

This fixture does not need the optional +2 typical-defender advantage term. It also does not
depend on a future XML extension: Bismarck's Military flavor 10, Dagger, direct total-war
selection and the threshold are all live current code/data.

IMMEDIATE SAME-UNIT-PHASE CONSEQUENCE
-------------------------------------
The stale bit is consumed before any later turn can repair it.
Immediately after the team turn, `CvPlayer::doTurnUnits()` starts with:

    CvPlayerAI::AI_doTurnUnitsPre()

which contains:

    if (AI_isDoStrategy(AI_STRATEGY_CRUSH) && AI_isFocusWar())
        AI_convertUnitAITypesForCrush();

The two predicates now disagree about time:

    * `AI_isFocusWar()` reads the LIVE new war-plan/area state and can already return true;
    * `AI_isDoStrategy(AI_STRATEGY_CRUSH)` reads the stale pre-plan strategy hash.

Thus the newly chosen total war can correctly qualify as a focus war but still skip the
CRUSH conversion pass solely because the cached hash was not refreshed.

`AI_convertUnitAITypesForCrush()` is state-changing gameplay code, not logging or a minor
score. It scans spare attack-capable Reserve / Collateral / suitable city-defense units,
ranks them for `UNITAI_ATTACK_CITY`, and directly executes:

    AI_setUnitAIType(UNITAI_ATTACK_CITY);

for qualifying units. Missing the CRUSH bit therefore changes unit roles before any selection
group performs its ordinary movement turn.

There is also a clean later tactical threshold consequence in `CvUnitAI` city attack logic.
Current SAS has:

    BBAI_ATTACK_CITY_STACK_RATIO = 122

and the war-strategy adjustment begins at +5 but subtracts 10 when CRUSH is active.
Holding the other optional adjustments at zero gives:

    stale non-CRUSH required ratio = 122 + 5  = 127
    correct CRUSH required ratio   = 122 - 5  = 117

A stack comparison of 120 therefore refuses the attack under the stale hash but accepts it
under the correct post-plan CRUSH state. This is a secondary manifestation; the earlier
UnitAI conversion already proves a same-unit-phase behavioral divergence.

PRODUCTION-STRATEGY SIBLING
---------------------------
K-Mod added a further direct war-plan dependency in `AI_STRATEGY_PRODUCTION`.
Current AdvCiv/SAS computes:

    if (AI_isFocusWar())
    {
        iProductionValue++;
        if (WARPLAN_TOTAL or WARPLAN_PREPARING_TOTAL exists)
            iProductionValue += 3;
    }

so installing a qualifying total/preparing-total plan after the hash refresh can increase the
correct Production-strategy score by +4. `AI_setWarPlan()` simultaneously calls
`AI_makeProductionDirty()` for AI members, showing that same-turn production is specifically
intended to respond to the new war-plan state, yet `AI_STRATEGY_PRODUCTION` can remain the old
bit while those dirty cities are reconsidered.

This is the same strategy-hash invalidation root as CRUSH, not a separate finding.

BROADER SAME-ROOT TRANSITIONS
-----------------------------
The defect is not restricted to adding the first war plan.
`AI_setWarPlan()` is also used for:

    * PREPARING -> active LIMITED/TOTAL transitions;
    * plan cancellation / timeout;
    * ATTACKED / ATTACKED_RECENT changes;
    * dogpile changes;
    * declaration/post-declaration updates;
    * selected unit-AI plan changes in later tactical code.

Any such transition can change a direct war-plan input to CRUSH/PRODUCTION, and the
`AI_updateAreaStrategies()` call can additionally change area-AI inputs used by other strategy
calculations (e.g. Dagger). These are siblings of one missing strategy invalidation contract.

ANCESTRY
--------
The root begins in BtS, while K-Mod makes the stale window deterministic and adds more affected
strategy inputs.

BtS / Civ4CE
~~~~~~~~~~~~
BtS stores `m_iStrategyHash` lazily for a game turn. `AI_getStrategyHash()` returns the cached
value whenever `m_iStrategyHashCacheTurn == current game turn`; the explicit invalidator is
`AI_forceUpdateStrategies()`.

BtS `CvTeamAI::AI_setWarPlan()` changes the plan, updates area strategies and dirties member
production, but does NOT call `AI_forceUpdateStrategies()`.

Moreover, in the ordinary no-war-plan branch of BtS `AI_doWar()`, the team first queries each
member's:

    AI_STRATEGY_DAGGER
    AI_STRATEGY_GET_BETTER_UNITS
    AI_STRATEGY_FINAL_WAR

before it selects and installs a new plan. The first such query can populate the entire lazy
strategy hash from the pre-plan state. BtS CRUSH already directly scans war plans (with an even
larger +6 Preparing/early-Total contribution), so the newly installed plan can make the correct
CRUSH state differ while the same-turn lazy hash remains cached.

Thus the fundamental missing `AI_setWarPlan` -> strategy-cache invalidation is inherited from
BtS.

K-Mod
~~~~~
K-Mod removes the BtS lazy cache-turn invalidation mechanism and makes strategy hashing eager in
`AI_updateCacheData()`. It also restructures sequential turn order so the player's cache update
runs at activation and the leader team's `CvTeam::doTurn()` follows immediately afterward.
This turns the old possible lazy-staleness pattern into a straightforward structural order:

    strategy snapshot -> team war-plan mutation -> player unit phase.

K-Mod additionally adds the war-plan-sensitive Production-strategy calculation.

Base AdvCiv 1.14 and SAS
~~~~~~~~~~~~~~~~~~~~~~~
Both retain K-Mod's eager activation refresh and the later team war turn. AdvCiv refines the
CRUSH/PRODUCTION formulas, adds cached war-plan counts and area/focus-war helpers, but still gives
`AI_setWarPlan()` no strategy-hash invalidation. Current SAS inherits the same lifetime.

DUPLICATE LEDGER
----------------
Full-album searches for `AI_updateStrategyHash`, strategy hash/cache, `AI_STRATEGY_CRUSH`,
CRUSH conversion, war-plan strategy, `AI_setWarPlan` and stale strategy found no prior numbered
root for a WAR-PLAN TRANSITION leaving the player strategy hash stale.

Nearby findings are distinct:

* F042 / war-plan-count cache concerns vassal target counting and disagreement between
  `m_aiWarPlanCounts` / `m_bAnyWarPlan` and raw plans. F510 reproduces with one ordinary
  non-vassal target and correct war-plan counts.
* F344 concerns a local `AI_isFocusWar` multiple-pushover flag typo. F510 can use a single
  ordinary close non-pushover target and assumes `AI_isFocusWar` itself is correct.
* F456 concerns one wrong favorite-civic INPUT inside Missionary strategy calculation under
  Random Personalities. F510 concerns strategy-hash LIFETIME after a later state transition.
* Earlier Queue-001 strategy-hash controls compared current formula content / cached defines;
  they did not audit war-plan mutation after hash computation.

Therefore F510 is not an alternate symptom of an existing strategy-calculation finding.

REPAIR DIRECTION
----------------
The required invariant is:

    after a war-plan / area-strategy transition changes inputs to a player's strategy hash,
    all affected AI members must see a refreshed hash before the next strategy consumer.

Potential implementations:

1. Add a real strategy-cache dirty/invalidation mechanism and mark all AI team members dirty in
   `AI_setWarPlan()` after the plan and area-strategy state have settled. Recompute lazily on the
   next `AI_isDoStrategy` query.

2. With the current eager architecture, minimally refresh all AI member strategy hashes once
   after the team's war-planning turn and before any member unit phase. This fixes the ordinary
   sequential path but is less comprehensive for later mid-turn `AI_setWarPlan()` callers.

3. Recompute member strategy hashes directly from `AI_setWarPlan()` after
   `AI_updateAreaStrategies()`. This is simple semantically but may repeat a relatively expensive
   full strategy calculation multiple times while `AI_doWar()` changes several plans.

A dirty/lazy member-level invalidation is the broadest repair because it also covers declaration,
plan cancellation and tactical later callers without forcing repeated eager recomputation.

Do NOT merely dirty city production: current code already does that, and the demonstrated first
bad consumer is `AI_doTurnUnitsPre()` before city production.

Classification: BtS-inherited strategy-cache invalidation defect, made structurally deterministic
by K-Mod's eager activation cache and broadened by K-Mod's additional war-plan-sensitive strategy
bits.


CURRENT STONE AFTER F510
------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 RETRACTED.
F446 / KI#769 through F510 / KI#833 CONFIRMED.
F511 / provisional KI#834 UNUSED.
Latest physical checkpoint: C031-WIP255.
No repository source files modified.

NEXT
----
Continue Queue 004 from F511 unused.

First bound F510 against other team-turn mutations that feed the cached strategy hash (war
success, area strategy, victory-stage inputs, current research/era) so one missing refresh root
is not split into symptoms. Then resume the persistent `CvPlayerAI` cache dependency matrix and
the external-input city/team/game cache inversion. Prefer event-driven caches whose callers imply
immediate freshness rather than intentionally sampled heuristic cadence.
===============================================================================

===============================================================================
C031-WIP256 -- QUEUE 004 DEEP RE-AUDIT: TEAM DIPLOMACY COUNTERS / ATTITUDE REFRESH LEAVE DAGGER STRATEGY STALE
Date: 2026-09-03
Model: ChatGPT-5.6-Sol
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F510 remain confirmed.
- New confirmed root in this checkpoint: F511 / KI#834.
- Next root number: F512 / KI#835 remains UNUSED.
- No repository source files were modified.

F510 SIBLING BOUNDARY
---------------------
The first post-F510 team-turn mutation challenge separates into a distinct root rather than
another F510 symptom.

F510's producer is a WAR-PLAN mutation after the strategy hash has already been computed.
The new war plan itself changes strategy-hash inputs such as CRUSH and PRODUCTION; the bad
consumers occur after `AI_setWarPlan`.

The new root below happens earlier. TEAM DIPLOMACY COUNTERS change and the full attitude
cache is refreshed before `AI_doWar`, but the strategy hash is not. `AI_doWar` therefore
simultaneously has the correct new attitude and a Dagger strategy bit calculated from the
old attitude. Invalidating only from `AI_setWarPlan` (the natural F510 repair) does not fix
this earlier contradiction.

F511 / KI#834 -- K-Mod snapshots the player strategy hash before the team updates diplomacy counters / attitudes, so same-turn war planning can use a stale DAGGER bit and skip war preparation
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / ORDERING CONTRACT
------------------------
Current sequential AdvCiv/SAS player activation performs these relevant steps in order:

    CvPlayer::setTurnActive(true)
        -> CvPlayerAI::AI_updateCacheData()
            -> AI_updateStrategyHash()
        -> GET_TEAM(getTeam()).doTurn()       // team leader activation
            -> CvTeamAI::AI_doTurnPre()
                -> AI_doCounter()
            -> CvTeamAI::AI_doTurnPost()
                -> every member:
                    AI_updateCloseBorderAttitude()
                    AI_updateAttitude()
                -> AI_updateAreaStrategies(false)
                -> AI_doWar()

The source comment in `AI_doCounter()` explicitly acknowledges the split:

    "Attitude isn't updated here. Should be soon enough in AI_doTurnPost."

And `AI_doTurnPost()` explicitly performs the full K-Mod attitude-cache refresh before
calling `AI_doWar()`.

That ordering is internally inconsistent with the eager strategy cache because current
`AI_updateStrategyHash()` itself reads diplomatic attitude while deciding whether a close
rival is eligible for `AI_STRATEGY_DAGGER`:

    if (kTeam.AI_getWarPlan(itRival->getTeam()) != NO_WARPLAN ||
        (AI_playerCloseness(itRival->getID()) > 0 &&
         GET_TEAM(getTeam()).AI_noWarProbAdjusted(itRival->getTeam()) < 90))
    {
        iCloseTargets++;
    }

`AI_noWarProbAdjusted()` obtains the team's current attitude, and current player attitudes
are stored in `m_aiAttitude` until `AI_updateAttitude()` refreshes them.

Thus the normal scheduler does:

    1. compute Dagger using YESTERDAY / pre-counter cached attitude;
    2. increment team diplomatic counters;
    3. recompute the member's attitude from those new counters;
    4. enter `AI_doWar()` with the NEW attitude cache;
    5. nevertheless read Dagger from the OLD strategy hash.

This is not merely a generic one-turn heuristic cadence. `AI_doWar()` is deliberately
positioned after the attitude refresh so current diplomatic relations govern the team's
war decision, yet one of its major aggression inputs still encodes the pre-refresh relation.

CURRENT GENGHIS OPEN-BORDERS THRESHOLD WITNESS
----------------------------------------------
Current SAS provides a compact shipped-data threshold crossing with Genghis Khan.
His current leader XML has:

    iMaxWarRand = 50
    FLAVOR_MILITARY = 10

and no-war probabilities:

    ATTITUDE_ANNOYED  = 10
    ATTITUDE_CAUTIOUS = 50
    ATTITUDE_PLEASED  = 90

The AdvCiv-SAS leader defaults inherited by ordinary leaders include:

    iOpenBordersAttitudeDivisor   = 25
    iOpenBordersAttitudeChangeLimit = 2

and current `RELATIONS_THRESH_PLEASED = 4`.

Use a one-member Genghis team and one close known potential rival under Open Borders.
Arrange the already-cached raw attitude toward that rival at exactly 3 (Cautious) and the
team Open Borders counter at 24 at player activation. This is ordinary long-lived
relationship state; no debug-only API is required.

At activation-time strategy computation:

    cached raw attitude = 3 -> ATTITUDE_CAUTIOUS
    Genghis no-war probability = 50
    `AI_noWarProbAdjusted` returns 50 while Open Borders is active
    Dagger close-target gate: 50 < 90 -> TRUE

During the immediately following team `AI_doCounter()`, the Open Borders counter update is
an ordinary supported `AI_randomCounterChange` (binomial 0..2) whose positive outcome can
advance 24 -> 25. At counter 25, `AI_getOpenBordersAttitude()` changes from:

    24 / 25 = 0

to:

    25 / 25 = +1

so the subsequent full `AI_updateAttitude()` changes the raw attitude from 3 to 4:

    raw 4 -> ATTITUDE_PLEASED
    Genghis no-war probability = 90
    Dagger close-target gate: 90 < 90 -> FALSE

The correct post-counter/post-refresh state therefore no longer has this rival as a Dagger
close target. The stored strategy hash still contains the result computed when the same
rival was Cautious.

This witness does not rely on assuming that the Open Borders counter always increments by
one. `AI_randomCounterChange` explicitly permits +1 (and +2); a supported ordinary random
outcome reaching the relationship threshold is sufficient to prove the lifetime defect.

DAGGER SCORE CAN FLIP ON THAT TARGET ALONE
------------------------------------------
The target-eligibility change can independently flip the actual Dagger bit rather than only
perturb a score.

For Genghis:

    12000 / max(100, 50 + MaxWarRand)
      = 12000 / 100
      = 120

Choose a legal stored strategy-random state for which:

    AI_getStrategyRand(12) % 11 == 5

The real current generator has many such reachable states (for example legal stored
`m_iStrategyRand` values 6, 28, 37, 46, 55, 64, ... in the Ancient-era shift produce
remainder 5).

The random Dagger term is then:

    120 * 5 / 10 = 60

Military flavor contributes:

    5 * min(8, 10) = 40

Subtotal:

    60 + 40 = 100

which already meets the normal Dagger threshold before any unique-unit, handicap or
area-strategy bonus is needed.

Therefore a state can be chosen where the PRE-COUNTER close-target test is the only reason
Dagger evaluation runs, and it sets Dagger=true. After the counter/attitude refresh, the
correct current state has zero close targets and should have Dagger=false.

IMMEDIATE SAME-TEAM-TURN WAR-PLANNING CONSEQUENCE
-------------------------------------------------
`CvTeamAI::AI_doWar()` runs only after the full attitude refresh, but it counts Dagger from
the stale member strategy hash:

    int iDaggerCount = 0;
    ...
    bool const bDagger =
        kMember.AI_isDoStrategy(AI_STRATEGY_DAGGER) || ...;
    if (bDagger)
    {
        iDaggerCount++;
        bAggressive = true;
    }

So the stale bit immediately alters the team's war-planning mode even though the same
function's direct attitude reads already use the updated Pleased relation.

Most strongly, when total-war target selection succeeds, current code stores:

    AI_setWarPlan(eBestTarget,
        iDaggerCount > 0 ? WARPLAN_TOTAL : WARPLAN_PREPARING_TOTAL);

A stale Dagger=true therefore installs DIRECT `WARPLAN_TOTAL`, while the correct current
Dagger=false state installs `WARPLAN_PREPARING_TOTAL`.

A clean fixture can make the newly Pleased close rival the only valid total-war target and
satisfy the ordinary power / war-random / `AI_startWarVal` checks. The bug then causes the
team to skip the intended preparation phase because of an attitude classification that the
team itself has already refreshed away earlier in the same `AI_doTurnPost()`.

This is a state transition, not merely a +/- few-point valuation difference. It changes the
stored war-plan type and immediately feeds the F510 family of focus-war/area/unit/production
behavior.

K-MOD ORIGIN / BTS NEGATIVE CONTROL
-----------------------------------
This particular cache-order root is K-Mod-introduced.

BtS/Civ4CE:
- `CvPlayerAI::AI_getAttitudeVal()` computes attitude from live diplomatic counters on every
  call; there is no K-Mod full `m_aiAttitude` cache.
- the strategy hash is lazy per game turn (`m_iStrategyHashCacheTurn`);
- `CvTeamAI::AI_doCounter()` runs before `AI_doWar()`;
- therefore the first ordinary Dagger strategy query made by the war planner after the team
  counter update reads the updated counters through live attitude calculation.

BtS does have the separate F510-class issue where a strategy hash already populated earlier
in the turn can become stale after a WAR-PLAN mutation. That does not create this specific
counter/attitude-order contradiction.

K-Mod changes both sides needed for F511:
1. it introduces the full `m_aiAttitudeCache`, and `AI_getAttitudeVal()` becomes a direct
   cache read;
2. it moves the full member attitude-cache refresh to team `AI_doTurnPost()` after
   `AI_doCounter()`;
3. independently, it makes `AI_updateStrategyHash()` eager through player `updateCacheData()`.

Current sequential activation computes that eager player strategy snapshot before the team
turn. K-Mod's own Dagger calculation already uses the cached attitude to decide whether a
close rival has enough war probability to count (`iAttitudeWarProb > 10`, equivalent to a
no-war probability below 90). AdvCiv later rewrites the close-target block but preserves the
same semantic threshold through `AI_noWarProbAdjusted(...) < 90`.

Base AdvCiv 1.14 and current SAS retain the K-Mod cache/order architecture.

SEMANTIC DEDUP
--------------
Full-album searches for Dagger, `iDaggerCount`, `AI_noWarProbAdjusted`, OpenBordersCounter,
attitude-cache timing, strategy hash and close-target logic found no same-root finding.

Nearest but distinct roots/controls:
- F035 / KI#356: `AI_doWar` used invalid set subtraction between total Dagger members and
  independent FinancialTrouble / GetBetterUnits populations. That is team-population
  arithmetic, not Dagger cache freshness.
- F049 / KI#370: Permanent Alliance reverse merge omitted outsider-side OpenBordersCounter.
  That loses relationship history at team merge; F511 uses a correctly maintained ordinary
  Open Borders counter but snapshots strategy before its same-turn increment is reflected.
- F296 / KI#619: one direction of recurring annual deal processing refreshed third-party
  attitude caches prematurely inside `CvDeal::doTurn`. That is deal-loop refresh timing and
  rival-trade resentment, not strategy/war planning.
- F456: wrong favorite-civic/personality input inside strategy calculation; content error,
  not strategy lifetime.
- F510 / KI#833: war-plan mutation after strategy computation leaves CRUSH/PRODUCTION bits
  stale. Different producer, earlier/later boundary and repair hook.

No existing numbered root was found for TEAM DIPLOMACY COUNTERS / ATTITUDE REFRESH occurring
after the eager strategy snapshot while the same `AI_doWar()` consumes stale Dagger.

REPAIR BOUNDARY
---------------
The architectural invariant should be:

    before `AI_doWar()` consumes member strategies, every strategy bit that depends on
    diplomatic counters / cached attitudes must correspond to the attitude state that
    `AI_doWar()` itself will use.

Safe repair directions to evaluate:

1. Preferred normal-turn boundary:
   after `AI_doTurnPost()` refreshes every member's close-border/full attitude cache (and,
   ideally, after other pre-war team state that strategy calculation reads is settled),
   refresh/invalidate each AI member's strategy hash BEFORE `AI_doWar()`.

   This naturally repairs F511 and can also cover normal-turn portions of F510 if positioned
   after area-strategy refresh; however F510 still needs a true invalidation contract for
   war-plan mutations that occur after this point or through other callers.

2. More general dependency mechanism:
   restore a real strategy-dirty/lazy-refresh contract and dirty affected member strategies
   whenever attitude-affecting team counters / attitude cache state changes. This is broader
   and safer for nonstandard midturn callers but potentially more invasive.

Do not merely special-case the Open Borders counter. The same ordering can apply whenever
`AI_doCounter()` changes a relationship component that crosses an attitude/no-war threshold
(Defensive Pact, Share War and other counter-derived attitude inputs). Open Borders supplies
the compact numeric witness, not the full dependency boundary.

Do not fix only `iDaggerCount` inside `AI_doWar()` by recomputing one ad-hoc Dagger condition.
Dagger is a player strategy with multiple consumers, and the root is the freshness contract
between the strategy snapshot and refreshed diplomatic state.

POST-F511 CURSOR
----------------
F512 / KI#835 remains UNUSED.

First bound F511 against other strategy-hash inputs mutated by the same team turn:
- `AI_updateAreaStrategies(false)` runs after the attitude refresh and before `AI_doWar`;
  current Dagger / paranoia / Production strategy calculations read area/focus-war state.
  Decide which are F510/F511 siblings versus a distinct cache producer.
- `m_bLonely` is updated in team `AI_doTurnPost()` after the player's strategy snapshot;
  `AI_getFlavorValue()` changes several flavors when lonely, so determine whether a
  loneliness transition can change a strategy bit before same-turn `AI_doWar` / unit use.
- team worst-enemy / strength-memory changes and any victory/tech-team inputs should be
  challenged only where the strategy calculator reads them and an immediate consumer exists.

Then resume the persistent-CvPlayerAI cache dependency ranking under F512 UNUSED rather than
manufacturing a new root from intentionally sampled heuristics.
===============================================================================

===============================================================================
C031-WIP257 -- QUEUE 004 DEEP RE-AUDIT: ADVc LONELINESS CACHE FLIPS AFTER STRATEGY SNAPSHOT
Date: 2026-09-03
Model: ChatGPT-5.6-Sol
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F511 remain confirmed.
- New confirmed root in this checkpoint: F512 / KI#835.
- Next root number: F513 / KI#836 remains UNUSED.
- No repository source files were modified.

POST-F511 STRATEGY-INPUT BOUNDARY
---------------------------------
The routine `AI_updateAreaStrategies(false)` that runs before `AI_doWar()` can also change
strategy-hash inputs after the activation snapshot. That belongs to F510's already recorded
WAR-PLAN / AREA-STRATEGY freshness boundary rather than deserving a new number.

The AdvCiv loneliness state is different. It is an independently cached team field with a
separate producer/history and a separate input contract (`AI_getFlavorValue`). It can flip
after player strategy computation even without a war-plan or attitude transition, and an
F510 war-plan invalidator or F511 attitude/counter invalidator would not repair it.

F512 / KI#835 -- AdvCiv updates cached team loneliness after the player's eager strategy snapshot, so a newly non-start-era isolated AI can enter same-turn war planning with non-lonely flavor / DAGGER values
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CURRENT ORDERING
-----------------------
Current AdvCiv/SAS makes leader flavors depend on cached team loneliness:

    int CvPlayerAI::AI_getFlavorValue(FlavorTypes eFlavor) const
    {
        ...
        int r = GC.getInfo(getPersonalityType()).getFlavorValue(eFlavor);
        if (GET_TEAM(getTeam()).AI_isLonely())
        {
            if (eFlavor == FLAVOR_SCIENCE)
                r += 3;
            else if (r > 1)
                r /= 2;
        }
        return r;
    }

`AI_isLonely()` is just the stored team field `m_bLonely`.

But normal sequential activation computes the player's whole strategy hash BEFORE the team
recomputes that field:

    CvPlayer::setTurnActive(true)
        -> CvPlayerAI::AI_updateCacheData()
            -> AI_updateStrategyHash()
                -> AI_getFlavorValue(...)       // reads old m_bLonely
        -> GET_TEAM(getTeam()).doTurn()
            -> CvTeamAI::AI_doTurnPost()
                -> if (getCurrentEra() > game start era)
                       m_bLonely = ...           // recomputed here
                -> AI_updateAreaStrategies(false)
                -> AI_doWar()                    // consumes stale strategies

Current loneliness calculation is:

    int iHasMet = getHasMetCivCount(false);
    int iYetToMeet = GC.getGame().countCivTeamsAlive() - iHasMet;
    m_bLonely = (iYetToMeet > iHasMet + 1 && iHasMet <= 2);

The previously audited `+1` arithmetic here is intentional self-count compensation; F512 is
not an off-by-one in this predicate. The defect is the cache lifetime around an otherwise
valid predicate.

DETERMINISTIC FIRST-POST-START-ERA TRANSITION
---------------------------------------------
The cleanest producer does not require a random contact event.

`m_bLonely` resets to false. While the team remains in the game start era,
`AI_doTurnPost()` deliberately does not recompute it at all:

    if (getCurrentEra() > GC.getGame().getStartEra())
        ... update m_bLonely ...

Take a normal Ancient-start game with enough living civilizations that a team which has met
only one rival satisfies the loneliness predicate once the guard becomes active.

During player A's previous turn, `doResearch()` completes a tech that advances A/team from
Ancient into Classical. `CvPlayer::setCurrentEra()` immediately updates the player's era
factor, but the team's `m_bLonely` is still false because that turn's team update already
occurred at activation while the team was Ancient.

On A's next activation:

    1. player `AI_updateStrategyHash()` runs in Classical and still reads m_bLonely=false;
    2. the immediately following team `AI_doTurnPost()` now passes the post-start-era guard
       and recomputes m_bLonely=true;
    3. `AI_doWar()` runs after that flip while the player strategy hash remains the
       non-lonely snapshot.

Thus this is a deterministic cache transition tied to normal era progression; no save/load,
Python, autoplay identity switch or unsupported API is needed.

CURRENT GENGHIS / MONGOL DAGGER NUMERIC WITNESS
-----------------------------------------------
Current shipped SAS provides an exact threshold flip with the normal Genghis/Mongolia pair.
Relevant current data/code:

    Genghis iMaxWarRand       = 50
    Genghis FLAVOR_MILITARY  = 10
    Noble iAITrainPercent    = 100
    Mongol UU                = UNIT_MONGOL_KHISHIGTEN
    Khishigten prereq tech   = TECH_CHIVALRY (Medieval)
    Khishigten AND bonus     = Horse
    Khishigten OR bonus      = Iron

Use the first Classical activation above, a neutral capital AreaAI state, non-Aggressive-AI
game option, one ordinary close Dagger-eligible rival, no Culture2/Missionary blocker, and
no Horse or Iron. Chivalry is not yet researched.

Choose a legal stored strategy-random state with:

    AI_getStrategyRand(12) % 11 == 5

(the same real-generator reachability proved in F511; many stored values produce this).

Random Dagger term:

    12000 / max(100, 50 + 50) = 120
    120 * 5 / 10 = +60

NORMAL NON-LONELY flavor term used by the stale snapshot:

    5 * min(8, MilitaryFlavor 10) = +40

Once `m_bLonely=true`, the correct live Military flavor is halved:

    10 / 2 = 5
    5 * min(8, 5) = +25

The current Khishigten unavailable-UU block contributes exactly +10 in this fixture:

    Chivalry is Medieval and therefore within current Classical era + 1,
    but not yet known                                      +30
    generic unavailable-UU allowance                       +20
    missing required Horse                                 -20
    at least one OR bonus exists but Iron is also absent   -20
    -----------------------------------------------------------
    Khishigten contribution                                +10

Noble's handicap term is:

    range(100 - iAITrainPercent(100), 0, 15) = 0

and the neutral capital area gives no area-strategy Dagger bonus.

Therefore:

    stale pre-team non-lonely Dagger = 60 + 40 + 10 = 110  -> ACTIVE
    correct post-team lonely Dagger  = 60 + 25 + 10 = 95   -> INACTIVE

The normal threshold is 100. The same player's correct flavor state therefore flips Dagger
off before `AI_doWar()`, while the stored strategy hash still says Dagger=true.

The Classical Dagger era gate is also live: absent a custom `iAIEraFactor`, AdvCiv maps era
index to `100 * era`, so Classical has AI era factor 1 and satisfies the current early-rush
era condition.

SAME-TURN GAMEPLAY CONSEQUENCE
------------------------------
Exactly as in the independent F511 producer, the immediately following team `AI_doWar()`
counts each member's stored Dagger strategy:

    if (bDagger)
    {
        iDaggerCount++;
        bAggressive = true;
    }

The stale non-lonely Dagger therefore makes a team aggressive even though the team's own
`m_bLonely` flag has already become true.

Most strongly, when a valid total-war target is selected:

    AI_setWarPlan(eBestTarget,
        iDaggerCount > 0 ? WARPLAN_TOTAL : WARPLAN_PREPARING_TOTAL);

The stale flavor snapshot can therefore cause newly Classical but still isolated Genghis to
skip the preparation phase and enter immediate `WARPLAN_TOTAL`; a strategy recomputation
using the already-current lonely flag would use `WARPLAN_PREPARING_TOTAL` in the fixture.

This is not just an exploration/tech-priority effect of loneliness. The stale cached flavor
state changes a stored war-plan transition in the very same team turn.

HISTORY / ORIGIN
----------------
The reachable AdvCiv history makes the regression boundary unusually clear.

Practical 1253 (`f6ff6a182f...`, "Initial commit (cont.)") contains the early `advc.109`
implementation as a DYNAMIC player function:

    bool CvPlayerAI::isLonely() const
    {
        if (getCurrentEra() == game start era)
            return false;
        ... compute hasMet / yetToMeet live ...
    }

and `AI_getFlavorValue()` calls that live function. Under that form, a strategy calculation
cannot read an older separately stored loneliness state.

AdvCiv practical 1386 (`dc9c167dfc...`,
"Refactoring and a couple of minor corrections in AdvCiv code") changes the contract:

    * removes the dynamic `CvPlayerAI::isLonely()` flavor query;
    * changes `AI_getFlavorValue()` to `GET_TEAM(getTeam()).AI_isLonely()`;
    * adds cached team `m_bLonely = false`;
    * recomputes `m_bLonely` in `CvTeamAI::AI_doTurnPost()`.

That refactor preserves K-Mod's eager player `AI_updateStrategyHash()` earlier in activation.
Therefore practical 1386 is the clean origin of the stale-loneliness strategy window.

K-Mod and BtS/Civ4CE do not have AdvCiv's `m_bLonely`/lonely-flavor mechanism, so they cannot
contain this root. Base AdvCiv 1.14 and SAS retain it.

SEMANTIC DEDUP
--------------
Full-album and current Known-Issues searches for `m_bLonely`, `AI_isLonely`, lonely flavor,
strategy/loneliness and related terms found no same-root numbered issue.

Existing Album mentions are controls / other contexts:
- Permanent-Alliance/team-merge audits note that lonely state is derived/recomputed rather
  than copied. That says nothing about when the recomputation occurs relative to strategy.
- F456's deterministic Random-Personalities Missionary fixture deliberately uses a two-civ
  game where loneliness is false; it is a favorite-civic input bug, not loneliness lifetime.
- F510 is war-plan/area-strategy mutation after strategy hashing.
- F511 is diplomacy-counter / attitude refresh after strategy hashing.

F512 has a separate producer (`m_bLonely` recomputation), separate historical origin
(advc.109 practical-1386 cache migration), and is not repaired by only invalidating on war
plans or attitude-counter changes.

REPAIR BOUNDARY
---------------
The invariant should be:

    any cached player value built from `AI_getFlavorValue()` must observe the current
    loneliness classification for the turn in which it is consumed.

Repair directions to evaluate:

1. Recompute team loneliness BEFORE member strategy/victory/other flavor-dependent caches
   are built at activation. Because loneliness depends only on current era and met-team
   population, this is cheap and conceptually clean.

2. Alternatively, after `m_bLonely` changes in `AI_doTurnPost()`, invalidate/recompute every
   member cache whose construction reads `AI_getFlavorValue()` before those caches are
   consumed. At minimum this includes the strategy hash; a broader audit should check any
   other persistent flavor-dependent values rather than hard-coding Dagger only.

3. Reverting loneliness to a cheap dynamic predicate, as the earlier advc.109 implementation
   did, would remove the stale field entirely but may have been refactored for team semantics /
   reuse and should be weighed against the current architecture.

Do not fix only the first Classical transition or only Dagger. Contact changes can also flip
`m_bLonely` true<->false between team recomputations, and the flavor transformation affects
Science plus every non-Science flavor above 1.

POST-F512 CURSOR
----------------
F513 / KI#836 remains UNUSED.

First finish the loneliness sibling boundary:
- identify other PERSISTENT caches built before `m_bLonely` refresh that read
  `AI_getFlavorValue()` (strategy is confirmed; determine whether victory-stage or other
  activation caches do too). Absorb same producer/cache-lifetime symptoms into F512.
- check contact transitions (meet during prior unit phase) as additional F512 manifestations,
  not new numbers.

Then resume remaining team-turn mutations after the strategy snapshot:
- worst-enemy changes / strength-memory decay / nuke-memory clearing only if a current
  strategy bit directly reads them before an immediate consumer;
- otherwise return to the persistent `CvPlayerAI` cache dependency inventory under F513
  UNUSED.
===============================================================================


===============================================================================
C031-WIP258 — F513 CONFIRMED
===============================================================================

F513 / KI#836 — AdvCiv's cached AI_neededExplorers snapshot is built before unit
exploration, so newly revealed tiles can lower the correct explorer target while later
same-turn city production still uses the old higher target

STATUS
------
CONFIRMED — AdvCiv performance-cache regression introduced in v0.87 / practical 1264
(commit 0884de493c5d4034cfa833d053e3234bf647ddc6) and retained in Base AdvCiv 1.14 and
current SAS. BtS/Civ4CE and K-Mod calculate AI_neededExplorers dynamically and therefore
do not have this cache-lifetime root.

ROOT
----
K-Mod/BtS calculate explorer demand directly from current map revelation each time:

    AI_neededExplorers(area)
        -> area.getNumUnrevealedTiles(getTeam())
        -> current demand

AdvCiv v0.87 changed this into an activation-time performance cache. The introducing diff
explicitly says:

    "Now cached. advc.017b adds another call to AI_neededExplorers,
     and it gets called by several UnitAI members too. Probably no noticeable
     difference in performance, but who knows."

It adds:

    AI_updateCacheData()
        -> AI_updateNeededExplorers()

and replaces the former dynamic AI_neededExplorers body with a lookup in
m_neededExplorersByArea.

Current SAS retains exactly that architecture:

    CvPlayer::setTurnActive(true)
        -> AI().AI_updateCacheData()
            -> AI_updateNeededExplorers()
        -> team doTurn()
        -> doTurnUnits()

whereas the economic/city turn comes later:

    CvPlayer::doTurn()
        -> AI_doTurnPre()
        -> ...
        -> city doTurn / AI production

A complete current-source caller search finds no refresh of m_neededExplorersByArea when
plots are revealed. Outside initialization/save reconstruction, AI_updateNeededExplorers is
called only by AI_updateCacheData at activation.

MUTABLE INPUT AFTER THE SNAPSHOT
--------------------------------
The cached land demand still depends directly on live unexplored-tile count:

    iNeeded = min(
        area.getNumUnrevealedTiles(getTeam()) /
            max(150 - 20 * getCurrentEra(), 50),
        min(3, getNumCities() / 3 + 2));

Exploration units move only AFTER the cache has been populated. Normal unit sight then
reveals tiles and immediately lowers area.getNumUnrevealedTiles(getTeam()), but no cache
invalidator/recompute accompanies that revelation.

CURRENT DETERMINISTIC THRESHOLD WITNESS
---------------------------------------
Use an ordinary non-Pangaea Ancient game, one city in a large land area, exactly one
UNITAI_EXPLORE unit, no land war/danger/financial trouble, and arrange the area's revealed
frontier so that the activation snapshot contains exactly 300 unrevealed land tiles.

Ancient divisor:

    max(150 - 20 * 0, 50) = 150

One-city cap:

    min(3, 1 / 3 + 2) = 2

Therefore at activation:

    300 / 150 = 2
    cached AI_neededExplorers(area) = 2

The existing Explorer then moves during doTurnUnits and reveals at least one of those
frontier tiles. Once unrevealed tiles fall to 299 or below (while remaining at least 150),
the mathematically correct target is:

    299 / 150 = 1
    correct AI_neededExplorers(area) = 1

The cache nevertheless remains 2 for the rest of the turn.

SAME-TURN PRODUCTION CONSEQUENCE
--------------------------------
Current city production has an explicit missing-explorer branch:

    int iMissingExplorers = kPlayer.AI_neededExplorers(kArea) -
            kPlayer.AI_totalAreaUnitAIs(kArea, UNITAI_EXPLORE);
    if (iMissingExplorers > 0)
        AI_chooseUnit(UNITAI_EXPLORE, 34 * iMissingExplorers);

After the Explorer has moved/revealed tiles, the physical explorer count is still one.
Thus:

    stale cached target = 2, existing = 1 -> iMissingExplorers = 1
    correct live target = 1, existing = 1 -> iMissingExplorers = 0

The AdvCiv cache therefore turns a state that should make NO missing-explorer build attempt
into a 34% Explorer-production attempt later in the same turn.

Current SAS also has a central area-usefulness gate immediately before ordinary explorer
production:

    if (AI_neededExplorers(area) <= AI_totalAreaUnitAIs(area, UNITAI_EXPLORE))
        reject UNITAI_EXPLORE;

The same stale target changes this hard gate from:

    correct: 1 <= 1 -> reject extra Explorer

to:

    stale:   2 <= 1 -> false -> permit the extra Explorer

so the defect is not merely a soft valuation difference. The cache can admit an explorer
whose demand disappeared during the preceding unit phase.

The water-area version has the same lifetime issue at its 400-tile thresholds; the land
fixture is enough to establish the root.

PRE-CACHE SEMANTICS / ANCESTRY
------------------------------
BtS/Civ4CE and K-Mod both retain a dynamic AI_neededExplorers function that reads
getNumUnrevealedTiles at each call. Consequently their city-production call after unit
exploration observes the newly revealed map state.

AdvCiv practical 1264 / commit 0884de493c (v0.87) moves the old function body into
AI_neededExplorersBulk, adds m_neededExplorersByArea, calls AI_updateNeededExplorers from
updateCacheData, and makes AI_neededExplorers return the stored map entry. The change is
explicitly tagged as caching/performance work rather than an intended one-turn gameplay
policy.

Base AdvCiv and current SAS preserve the cache and the pre-unit activation update point.
This is therefore an AdvCiv performance optimization that changes observable AI behavior
within the turn.

DUPLICATE / ROOT SEPARATION
---------------------------
Full album and current Known-Issues searches found no existing root for
m_neededExplorersByArea becoming stale after exploration/revelation.

Nearby explorer findings are distinct:

- F341 / KI#664: AI_isExcessSeaExplorers subtracts a PLAYER-GLOBAL queued explorer count
  from a WATER-AREA-LOCAL count. That is a scope mismatch between disconnected seas, not
  a cache lifetime issue.
- F342 and the later sea-explorer retirement work concern UnitAI transformation/retirement
  once a target is evaluated, not when the target itself becomes stale.
- F366 concerns Advanced Start assigning a sea explorer the wrong UNITAI_EXPLORE role.
- Current SAS naval-trade-fallback fixes concern fully explored water and physical ship
  counting in a separate fallback path; they do not refresh m_neededExplorersByArea.
- KI#192's central land-area usefulness gate actually makes F513 more observable because
  that hard gate trusts the same stale target; it does not cause the root.

The earlier Queue-001 AI_doEnemyUnitData own-unit question also remains only a control and
is unrelated to this cache.

REPAIR BOUNDARY
---------------
The invariant should be:

    explorer demand used after unit exploration must reflect the map revelation state that
    exists when the demand is consumed.

Possible repairs:

1. Restore dynamic AI_neededExplorers calculation. The original AdvCiv comment itself says
   the performance gain is probably negligible, and current callers are not obviously hot
   enough to justify semantic staleness.

2. Keep the cache but invalidate/recompute the affected player's area entry when that team
   reveals tiles. This is more surgical but risks putting work into the very hot sight/
   revelation path that the cache presumably meant to avoid.

3. Keep activation-time caching for unit-AI decisions if desired, but perform one cheap
   AI_updateNeededExplorers refresh after doTurnUnits and before city/economic production.
   This preserves most of the cache benefit while restoring the pre-cache semantics for the
   important production consumers.

Do not repair only the 300->299 example. Any threshold crossing in land or water demand,
and contact/city-count/era inputs that change after the snapshot, should obey the same
freshness rule.

POST-F513 CONTROLS / CURSOR
---------------------------
The unit-class/combat-weight cache family was challenged first. Its apparent no-visible-
enemy early-return problem is mostly neutralized by an old, already-recorded quirk:
AI_doEnemyUnitData gives every visible fighting unit a baseline value of 1 and does not
exclude the player's own visible combat units, so normal own military keeps iTotal positive
and the normal 25% decay running even with no visible rival. The old own-unit question
remains unnumbered because the baseline may be intentional smoothing/prior behavior.

m_aeBestTechs was also sampled. It is rebuilt by every synchronous AI_bestTech search and
is consumed only as a soft percentile modifier in AI_techTradeVal. No sufficiently strict
event-driven freshness contract / hard same-turn contradiction has yet survived; keep it
as a control unless a stronger producer is found.

F514 / KI#837 remains UNUSED.

NEXT
----
1. Continue the persistent CvPlayerAI member inventory under F514 UNUSED:
   - finish m_eVictoryStageHash lifetime vs post-snapshot team/player mutations;
   - inspect m_bWasFinancialTrouble / m_iTurnLastProductionDirty only for distinct stale
     state not already owned by F364/F499/F502;
   - inspect remaining relationship and unit-role caches with explicit mutator contracts.
2. Treat one-turn heuristic sampling as intentional unless an optimization/refactor changed
   former live semantics or a same-turn producer/consumer contract proves otherwise.
3. Resume Queue 004 CvPlayer.cpp cross-object mutator sweep once the remaining player-AI
   persistent members are exhausted.

CURRENT AUDIT STATE
-------------------
Queue 004 CvPlayer.cpp — OPEN
Unique files complete — 32/169
Confirmed deep/current roots — F446 through F513
F514 / KI#837 — UNUSED
Latest physical checkpoint — C031-WIP258
No repository source file modified.


===============================================================================
C031-WIP259 -- QUEUE 004 DEEP RE-AUDIT: K-MOD170 REVERSED STRATEGY/VICTORY CACHE ORDER
Date: 2026-09-03
Model: ChatGPT-5.6-Sol
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F513 remain confirmed.
- New confirmed root in this checkpoint: F514 / KI#837.
- Next root number: F515 / KI#838 remains UNUSED.
- No repository source files were modified.

F514 / KI#837 -- K-Mod practical 170 reversed strategy/victory cache update order, so Conquest staging uses the previous strategy hash even though current CRUSH is computed moments later
------------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CURRENT ORDER
--------------------
Current AdvCiv-SAS eagerly rebuilds both the victory-stage hash and the ordinary strategy
hash during player activation:

    CvPlayer::setTurnActive(true)
        -> CvPlayerAI::AI_updateCacheData()
            -> AI_calculateAverages()
            -> AI_updateVictoryStageHash()
            -> AI_updateStrategyHash()
            -> ...

The order matters because current Conquest staging has a live dependency on the ordinary
strategy hash:

    CvPlayerAI::AI_calculateConquestVictoryStage()
        ...
        iValue += (iDoWs > 1 || AI_isDoStrategy(AI_STRATEGY_CRUSH) ? 10 : 0);

`AI_isDoStrategy` is not lazy in current AdvCiv/SAS; it reads the stored `m_eStrategyHash`.
Consequently the Conquest calculation in `AI_updateVictoryStageHash()` reads the PREVIOUS
strategy snapshot. Only after the victory hash has been permanently stored for the new turn
does `AI_updateStrategyHash()` compute the current CRUSH state.

This is not merely the unavoidable existence of a dependency cycle. Current AdvCiv explicitly
removed the opposite Conquest3 -> CRUSH edge:

    /* Not clear that conq3 should make the AI more willing to neglect
       its defense. Also, once crush is adopted, the AI becomes more inclined
       towards conquest; should only work one way. */
    /* if (AI_atVictoryStage(AI_VICTORY_CONQUEST3))
           iCrushValue += 1; */

The remaining intended one-way edge is therefore CRUSH -> Conquest. The current update order
makes precisely that live edge one turn late.

HISTORY / EXACT REGRESSION BOUNDARY
-----------------------------------
This ordering was changed by a K-Mod cache refactor, and the reachable history lets the
regression be identified unusually precisely.

Before practical 170, after K-Mod had already replaced the old lazy getters with explicit
update functions, `CvPlayerAI::AI_doTurnPre()` ran:

    AI_calculateAverages();
    AI_updateStrategyHash();
    AI_updateVictoryStrategyHash();
    AI_updateGoldToUpgradeAllUnits();

Thus the rewritten K-Mod Conquest-stage calculation saw the strategy hash freshly computed
for that same player turn.

K-Mod practical 170 / commit `8bd260f741645fd4b5410e7470519f7a56be5ecb`, titled:

    "Cache data is now updated when player is initialized, and when turn becomes active..."

moved the cache work out of `AI_doTurnPre()` into `updateCacheData()`. In THE SAME DIFF it
removed the old:

    AI_updateStrategyHash();
    AI_updateVictoryStrategyHash();

order and introduced:

    AI_updateVictoryStrategyHash();
    if (!isHuman())
        AI_updateStrategyHash();

The refactor therefore reversed the semantic dependency order while relocating the work.
Final K-Mod 1.46 retains victory-before-strategy. Base AdvCiv 1.14 and current SAS retain the
same order (`AI_updateVictoryStageHash()` followed by `AI_updateStrategyHash()`).

K-Mod's Conquest-victory function is itself explicitly marked "completely rewritten for
K-Mod" and contains the CRUSH +10 dependency. Civ4CE/BtS does not contain this K-Mod
victory-stage/cache architecture. Classification: K-Mod practical-170 cache-refactor
ordering regression, inherited by AdvCiv and SAS.

CURRENT NORMAL-GAME NUMERIC WITNESS
-----------------------------------
A current-data fixture can make BOTH transitions land exactly on their thresholds without
using F510's war-plan-transition invalidation root.

Use Shaka in the Classical era with:

- current XML Conquest victory weight = 60;
- current Military flavor = 10;
- UWAI enabled, so the old Dagger branch is suppressed by `bNoDagger`;
- exactly one close known potential enemy under `WARPLAN_PREPARING_TOTAL`;
- nonnegative war success (so the `min(0, warSuccess/15)` term contributes 0);
- the target still using Archer as its typical city defender;
- Shaka finishing Construction during the PREVIOUS player turn;
- a reachable stored strategy-rand value such as 578 in the Classical-era shifts.

For that legal random value, the current generator yields simultaneously:

    AI_getStrategyRand(4)  % 100 = 40    // Conquest random
    AI_getStrategyRand(13) % 9   = 5     // CRUSH random, Military flavor 10

The one Preparing Total plan contributes 6 in K-Mod's CRUSH logic, halved by AdvCiv to +3.

Before Construction was completed, both Shaka and the selected rival can use Archer as their
typical city defender. Current Archer data is strength 2, 1 first strike, 1 chance first
strike, so the CRUSH defender-advantage test:

    100 * ourTypicalDefence >= 110 * rivalTypicalDefence

is false when the two typical defenders are equal. The correct pre-Construction CRUSH score
is therefore:

    random 5 + Preparing Total 3 = 8 < 10  -> CRUSH false

Construction completes only later in that previous turn, after that turn's strategy snapshot.
Current Longbowman data is:

    strength 4
    first strikes 1
    chance first strikes 1
    prereq TECH_CONSTRUCTION
    no resource prerequisite
    UNITAI_CITY_DEFENSE enabled

so on the next activation `getTypicalUnitValue(UNITAI_CITY_DEFENSE)` can jump from Archer to
Longbowman. Because Archer and Longbow have the same first-strike profile while Longbow has
double base strength, the 110% comparison is now comfortably true and contributes +2:

    random 5 + Preparing Total 3 + defender advantage 2 = 10  -> CRUSH true

This is an ordinary between-turn tech/trainability input to the strategy calculation. It does
not depend on F510's same-turn `AI_setWarPlan` invalidation omission; the war plan can already
exist throughout both turns.

CONQUEST STAGE FLIP
-------------------
Use three alive major civ teams, all relevant rivals known, no conquered rivals, exactly one
DECLARED_WAR memory for Shaka, and ordinary strength/attitude values chosen so that:

- Shaka is top-ranked in power but not >2x average rival power;
- `bVeryStrong`, `bWarmonger`, `bHateful` and both -30 penalties are false;
- `bTopRank` is true;
- `iDoWs == 1` (enough for the level-1 readiness condition, but NOT enough for the separate
  `iDoWs > 1` +10 shortcut).

Then the Conquest-interest value is:

    personality / redistributed Conquest weight  60
    strategy random                              +40
    ------------------------------------------------
    without current CRUSH                        100
    with current CRUSH                           110

With more than two alive civ teams, the current entry gate returns stage 0 when `iValue < 110`.
At 110 it proceeds. The level-1 readiness condition succeeds because `iDoWs > 0` and
`bTopRank` is true, while the level-2 condition can remain false. Therefore:

    previous/stale CRUSH=false -> Conquest stage 0
    correct current CRUSH=true -> Conquest stage 1

Current cache order nevertheless executes:

    AI_updateVictoryStageHash()
        -> Conquest sees stale CRUSH=false
        -> stores Conquest0
    AI_updateStrategyHash()
        -> now correctly computes CRUSH=true

The player thus begins the rest of its turn in a self-inconsistent state: current strategy
says CRUSH while the stored Conquest stage was just computed as though CRUSH were false.

SAME-TURN GAMEPLAY CONSEQUENCES
-------------------------------
The stale victory stage is consumed immediately before any next-turn refresh can rescue it.

1. Research valuation
~~~~~~~~~~~~~~~~~~~~~
`AI_doTurnPre()` runs `AI_doResearch()` after activation cache setup. `AI_techUnitValue()`
boosts military unit value when Conquest stage 1 (or Domination stage 2) is active:

    iMultiplier = 100;
    if (AI_atVictoryStage(AI_VICTORY_CONQUEST1 | AI_VICTORY_DOMINATION2) || ...)
        iMultiplier += 25;
    ...
    iMilitaryValue *= iMultiplier;
    iMilitaryValue /= 100;

Holding the other strategy gates neutral, the stale Conquest0 therefore values the military
portion of candidate technologies at 100% where the correct Conquest1 state values it at 125%.
This can alter the technology selected in the same `AI_doResearch()` pass.

2. City military-production spending cap
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Later same-turn city production calls `AI_maxUnitCostPerMil()`. Current code adds:

    if (AI_atVictoryStage(AI_VICTORY_CONQUEST4))
        iMaxUnitSpending += 30;
    else if (AI_atVictoryStage(AI_VICTORY_MILITARY3))
        iMaxUnitSpending += 20;
    else if (AI_atVictoryStage(AI_VICTORY_CONQUEST1))
        iMaxUnitSpending += 10;

Let all other terms produce a baseline cap M. Then:

    stale Conquest0 cap   = M
    correct Conquest1 cap = M + 10

City production contains hard gates such as:

    if (!bUnitExempt && iUnitSpending < iMaxUnitSpending + 5)
        ... choose floating defender ...

For an ordinary funding state `iUnitSpending = M + 7`, the stale stage fails this gate while
the correct Conquest1 state passes it (`M+7 < M+15`). Thus the order bug can directly suppress
a military build that the current strategy/victory state is meant to permit.

The same Conquest-stage bit has many additional AI consumers, but these two are enough to show
that the mismatch is live gameplay state rather than logging/UI drift.

DUPLICATE / ROOT SEPARATION
---------------------------
Full album and current Known-Issues searches for victory/strategy update ordering, stale
Conquest stages, `AI_updateVictoryStageHash`, `AI_updateVictoryStrategyHash`, CRUSH->Conquest,
and K-Mod cache ordering found no existing numbered root for this reversal.

Nearby findings are distinct:

- F510 / KI#833: a WAR-PLAN MUTATION occurs AFTER an already-computed strategy hash and does
  not invalidate that strategy hash. F514 reproduces with the war plan already present and
  changes CRUSH through Construction/typical-defender trainability. Even a complete F510
  `AI_setWarPlan` invalidation repair therefore leaves F514 intact.
- F511 / KI#834: team diplomacy counters/attitude become fresh after strategy snapshot while
  stale Dagger is used by `AI_doWar`.
- F512 / KI#835: AdvCiv cached loneliness is refreshed after strategy snapshot, leaving flavor-
  dependent strategy stale.
- F373 / KI#696: SAS-local Culture competing-victory logic was evaluated before Diplomacy was
  inserted into the victory hash. Current SAS has already repaired that specific intra-victory
  ordering by calculating Diplomacy before Culture. It does not address victory-vs-strategy
  order.
- Earlier Queue-001 strategy/victory controls audited formula content, disabled defines and
  individual stage behavior; they did not identify the K-Mod170 order reversal.

REPAIR BOUNDARY / CAUTION
-------------------------
The required invariant for the live CRUSH -> Conquest dependency is:

    the Conquest victory stage stored for a player turn must be based on that turn's current
    CRUSH state, not the previous strategy snapshot.

The most historically conservative repair is to restore K-Mod's pre-practical-170 order:

    AI_updateStrategyHash();
    AI_updateVictoryStageHash();

That directly restores the semantics that existed immediately before the cache relocation and
matches AdvCiv's own comment that CRUSH should influence Conquest, not vice versa.

However, strategy calculations also read some victory-stage bits (Culture2 gates Dagger;
Conquest4/Space4 contribute to Production/Final-War logic). Therefore do NOT blindly reorder
without regression-testing those dependencies. The two hashes form a partially cyclic heuristic
system. A two-pass or explicitly staged dependency design may be preferable if current-turn
freshness is desired on both sides.

At minimum, any repair must preserve the intentional one-way `advc.018` relationship and prove
that the final stored victory hash no longer reads a previous-turn CRUSH bit.

POST-F514 CURSOR
----------------
F515 / KI#838 remains UNUSED.

Next:
1. Bound F514 against the other current strategy->victory and victory->strategy dependencies;
   determine whether they are manifestations of the same K-Mod170 order reversal or deliberate
   one-turn feedback.
2. Continue the persistent CvPlayerAI inventory:
   - `m_bWasFinancialTrouble` only for state not already owned by F364/F499/F502;
   - `m_iTurnLastProductionDirty` / production-dirty lifecycle;
   - remaining relationship and unit-role caches with explicit event-driven contracts.
3. Return to Queue 004 `CvPlayer.cpp` cross-object mutator sweep once the remaining player-AI
   persistent members are exhausted.

CURRENT AUDIT STATE
-------------------
Queue 004 `CvPlayer.cpp` -- OPEN
Unique files complete -- 32/169
Confirmed deep/current roots -- F446 through F514
F515 / KI#838 -- UNUSED
Latest physical checkpoint -- C031-WIP259
No repository source file modified.


===============================================================================
C031-WIP260 APPEND -- 2026-09-03 -- post-F514 persistent-player-cache pass
===============================================================================

F514 SIBLING / REPAIR-BOUNDARY ADDENDUM
---------------------------------------
The K-Mod practical-170 victory-before-strategy order reversal confirmed in F514 has another
live current-SAS strategy -> victory dependency beyond CRUSH -> Conquest.

Culture stage-4 countdown logic reads the current GET_BETTER_UNITS strategy:

    bool const bGetBetterUnits = AI_isDoStrategy(AI_STRATEGY_GET_BETTER_UNITS);
    if (bGetBetterUnits)
        iDemoninator += 80;

Because `AI_updateVictoryStageHash()` still runs before `AI_updateStrategyHash()`, this Culture
calculation also sees the previous strategy snapshot. This is the SAME K-Mod170 cache-order
root as F514, not a new finding. Any F514 repair/regression test should therefore cover both:

    current CRUSH -> Conquest staging
    current GET_BETTER_UNITS -> Culture countdown/staging

This reinforces the caution already recorded in WIP259: victory and strategy have multiple
cross-dependencies, so a repair must make the intended dependency staging explicit rather
than treating one CRUSH symptom in isolation.

POST-F514 FINANCIAL / PRODUCTION-DIRTY CONTROLS
-----------------------------------------------
F515 was NOT spent on the remaining financial-state members.

`m_bWasFinancialTrouble` is already the state-machine root repaired under F364/KI#687.
Current `AI_doCheckFinancialTrouble()` correctly distinguishes entry into trouble from an
already-consumed trouble state and only consumes the transition after the production-dirty
cooldown can actually fire. Wrong `AI_isFinancialTrouble()` answers caused by stale
`m_iAvailableIncome` are already F499/F502, not a new member-lifetime root.

`m_iTurnLastProductionDirty` is also used as an intentional shared 10-turn throttle for
tech/financial production re-evaluation. No independent lost-state contract survived the
current pass. These are therefore controls, not F515.

-------------------------------------------------------------------------------
F515 / KI#838 -- CONFIRMED
INHERITED BTS CITY-SITE CACHE SURVIVES CONSUMPTION BY CITY FOUNDING
-------------------------------------------------------------------------------

ROOT
----
`CvPlayerAI::AI_doTurnUnitsPre()` rebuilds the player's persistent city-site list once before
any unit moves:

    AI_updateFoundValues();

`AI_updateFoundValues()` clears/rebuilds `m_aeAICitySites` (BtS/K-Mod name:
`m_aiAICitySites`) from the currently legal/revealed found-value landscape. The list is then
read throughout the unit phase and later player/city AI.

An ordinary Settler can consume one of those cached sites during that same unit phase:

    CvUnit::found()
        -> CvPlayer::found(x, y)
        -> initCity(...)
        -> Settler kill(true)

But neither `CvUnit::found`, `CvPlayer::found`, city initialization, nor the normal plot-owner /
city-placement path calls:

    AI_invalidateCitySites(...)
    AI_updateFoundValues(...)
    AI_recalculateFoundValues(...)

for this transition. Source-wide current call-site enumeration leaves ordinary refreshes at
unit-pre (plus setup/Advanced-Start/special first-city paths); there is no post-founding
ordinary refresh.

The cached vector therefore continues to contain the just-consumed city plot. Its stored
FoundValue also remains the pre-founding value; ordinary founding contains no `setFoundValue`
or surrounding found-value recalculation hook.

This is a semantic cache-invalidation defect, not merely an old coordinate that later code
always validates dynamically. The raw vector count/value is itself consumed directly.

CLEAN CURRENT-SAS 1 -> 0 SITE WITNESS
-------------------------------------
Use an ordinary AI empire with:

- one currently legal/high-value site S in the relevant land area;
- one existing Settler able to found S during the unit phase;
- no other valid land or water city site after S is founded;
- no financial trouble / defensive emergency needed for the normal expansion branch.

At `AI_doTurnUnitsPre()`:

    m_aeAICitySites = { S }
    AI_getNumAreaCitySites(...) = 1

The Settler then founds S. Correct post-founding expansion state is:

    valid city sites = 0
    existing Settlers = 0          // founding Settler has died

but the cache remains:

    m_aeAICitySites = { S }
    cached area-site count = 1
    S FoundValue = old high value

Suppose the empire had one city before founding S. It now has two. Later same-turn
`CvCityAI::AI_chooseProduction()` computes:

    iNumAreaCitySites = 1                  // stale; correct 0
    iNumSettlers = 0
    iMaxSettlers = min((iNumCities + 1)/2,
                       iNumAreaCitySites + iNumWaterAreaCitySites)
                 = min((2 + 1)/2, 1)
                 = 1

Thus the expansion logic sees room for another Settler although the cached site's very
purpose has just been consumed and the correct maximum is zero.

CURRENT SAS'S EXTRA SETTLER SAFETY GATE DOES NOT SELF-HEAL
-----------------------------------------------------------
SAS added `SAS_getSettlerBuildSiteStatus` / `SAS_isSettlerBuildWorthwhile` as an additional
production-side safeguard. That is useful generally but cannot rescue this lifetime defect,
because it trusts the SAME persistent data:

    AI_getNumAreaCitySites(...)
    AI_getNumAdjacentAreaCitySites(...)
    AI_getCitySite(...).getFoundValue(...)

It does not re-check `canFound()` for every stored entry. Because the consumed plot S still
has its old positive FoundValue and remains in the vector, the helper can report a valid/high-
value build site even though S now contains the newly founded city and no real site remains.

The stale state therefore reaches both inherited K-Mod expansion logic and SAS's newer
anti-bad-Settler production gate.

OTHER LIVE SAME-ROOT CONSUMERS
------------------------------
The stale site is not limited to the `iMaxSettlers` calculation. Current same-turn consumers
include, among others:

- `AI_getNumCitySites()` / `AI_getNumAreaCitySites()` expansion and Settler-production gates;
- `AI_calculateSettlerPriority` inputs;
- Settler-sea transport demand / overseas-site counts;
- the early `AI_chooseProduction` rule that keeps food-production Settlers when
  `AI_getNumCitySites() > 0`;
- expansion-related building-value and chop heuristics that ask whether area/city sites
  remain;
- unit/city-site helper code that iterates the vector directly.

Settler movement itself often performs a fresh `canFound`/path/mission legality check, so this
finding does NOT claim that a later Settler must literally found on top of the new city. The
confirmed bug is that non-validating count/value consumers continue to treat the consumed
site as available and can consequently make wrong production/expansion decisions.

TURN-ORDER / REAL CONSUMER PROOF
--------------------------------
This survives the normal sequential AI turn architecture.

BtS, K-Mod, Base AdvCiv and current SAS all run unit actions while the player turn is active:

    setTurnActive(true)
        -> doTurnUnits()
            -> AI_doTurnUnitsPre()
            -> Settler can found city

The sequential player's `doTurn()` is then performed when its turn ends. That function runs
AI turn-pre and subsequently each city `doTurn`, where production can be reconsidered. Hence
the stale site vector created by unit-phase founding has a normal later same-turn consumer;
it is not necessarily overwritten first.

ANCESTRY
--------
Civ4CE/BtS already has all essential pieces:

- persistent `m_aiAICitySites`;
- `AI_doTurnUnitsPre()` calls `AI_updateFoundValues()` once before unit movement;
- `CvPlayer::found()` does not invalidate/rebuild that list;
- `CvCityAI` uses cached area-site counts in expansion/Settler logic;
- sequential turn activation runs units before the end-turn player/city processing.

K-Mod preserves that architecture and makes the raw site-count impact especially explicit via
its `iMaxSettlers = min((numCities+1)/2, numAreaSites+numWaterSites)` logic. K-Mod also changes
the site invalidation implementation, but does not add a founding hook.

Base AdvCiv 1.14 retains the same lifecycle. Current SAS retains it and adds stronger Settler
production gates that still consume the same cached vector/value.

Classification: inherited BtS city-site cache invalidation defect; K-Mod/AdvCiv/SAS retain
and expose concrete production consequences.

DUPLICATE / CONTROL CHECK
-------------------------
Full album and current Known-Issues searches for `m_aeAICitySites`, `m_aiAICitySites`,
`AI_updateFoundValues`, stale/consumed city sites, Settler-site counts and `iMaxSettlers`
found no existing numbered root for a JUST-FOUNDED own city failing to invalidate the
persistent site list.

Nearby material is distinct:

- F385 concerns duplicate guard missions to a PLANNED future city site.
- F513 concerns AdvCiv's cached *needed Explorer count* becoming stale after exploration.
- the many CitySiteEvaluator findings concern how FoundValue is CALCULATED, not failure to
  invalidate a site after it is consumed by founding.
- KI#180/KI#181/KI#185 and related SAS Settler work change selection/quality/production policy;
  they do not add a general post-founding city-site-cache refresh.
- the old `CvArea::m_aiBestFoundValue` control self-heals at the next unit-pre recomputation;
  F515 specifically proves consumers in the interval after the unit-pre rebuild and before
  that next refresh.

REPAIR BOUNDARY
---------------
Required invariant:

    once a city is founded, no later consumer may continue to count the founded plot (or
    nearby now-invalidated/reranked plots) as an available cached city site.

Simply erasing S from `m_aeAICitySites` would fix the narrow 1->0 stale coordinate but is not
fully equivalent to recomputation: a new city changes `canFound` spacing, ownership/culture,
city-count-dependent found valuation and rankings of nearby sites. Clearing the whole vector
would avoid false-positive expansion but can create a false-negative until the next turn.

The robust direction is therefore an event-driven found-value/site refresh for the founding
player after the new city has been fully established (or an equivalent targeted invalidation
that also recomputes all affected candidates). `AI_updateFoundValues()` is expensive, but city
founding is rare enough that correctness may justify the event cost; the exact safe hook should
be tested because calling too early during `initCity` could observe partially initialized city /
plot state.

Regression tests should include:

1. one cached site -> found it -> zero legal sites -> no new Settler demand later that turn;
2. multiple nearby cached sites -> founding one invalidates/reranks overlapping candidates;
3. overseas/adjacent-area site counts and Settler-sea demand;
4. ordinary BtS/K-Mod-style expansion logic and current SAS minimum-found-value gate;
5. simultaneous/network-turn behavior, where event-driven invalidation is preferable to
   relying on turn-boundary refresh ordering.

POST-F515 CURSOR
----------------
F516 / KI#839 remains UNUSED.

Next:
1. Bound F515 against city acquisition/razing/ownership changes that also alter legal found
   sites; absorb only transitions sharing the same `m_aeAICitySites` invalidation root.
2. Continue remaining persistent CvPlayerAI members / relationship caches after the city-site
   vector is exhausted.
3. Resume Queue 004 `CvPlayer.cpp` cross-object mutator/derived-cache sweep with the same
   source -> stored dependency -> invalidator -> consumer method.

CURRENT AUDIT STATE
-------------------
Queue 004 `CvPlayer.cpp` -- OPEN
Unique files complete -- 32/169
Confirmed deep/current roots -- F446 through F515
F516 / KI#839 -- UNUSED
Latest physical checkpoint -- C031-WIP260
No repository source file modified.


================================================================================
C031-WIP261 APPEND -- 2026-09-03 -- F515 city-removal boundary / F516 persistent-cache continuation
================================================================================

F515 SIBLING / CITY-REMOVAL BOUNDARY
------------------------------------
The first post-WIP260 boundary check broadens F515 slightly, but does NOT spend F516.

A city razed during the active AI's unit phase is another producer of the SAME persistent
`m_aeAICitySites` / FoundValue lifetime root already proved by F515.

Normal sequential ordering is:

    setTurnActive(true)
        -> doTurnUnits()
            -> AI_doTurnUnitsPre()
                -> AI_updateFoundValues()          // city-site vector rebuilt here
            -> selection groups act
                -> attacker captures city
                    -> CvUnit::setXY / conquest path
                        -> acquireCity(...)
                            -> AI_conquerCity(...)
                                -> raze/disband city
            -> later groups may still act
        ...
        -> player doTurn() / later same-turn production consumers

`AI_updateFoundValues()` has no city-raze/acquisition event hook; its ordinary runtime rebuild
is the single pre-unit call. Therefore, if an enemy city had blocked otherwise attractive
settlement locations, capturing and razing that city can make those plots newly legal after
`m_aeAICitySites` was already built. The cached vector cannot discover those newly legal sites
until the next rebuild. A later Settler group in the same unit phase, or later expansion /
production logic in the same player turn, can consequently see a false-negative "no site" or
an obsolete ranking.

This is the mirror image of F515's founding witness:

- founding can leave a consumed / newly invalid site falsely PRESENT;
- razing can leave newly legal / newly valuable sites falsely ABSENT.

Both are repaired by the same invariant: city-topology changes after the pre-unit FoundValue
snapshot must invalidate or refresh the affected player city-site state before later consumers.
Accordingly this is an F515 scope addendum, not F516.

CITY ACQUISITION WITHOUT RAZING -- CONTROL
-----------------------------------------
Keeping a captured city does not provide an equally clean city-spacing witness because the
city already existed on that plot before ownership changed. Acquisition/cession can still
change ownership, culture, FoundValue and candidate ranking nearby, so an event-driven F515
repair should treat ownership changes conservatively; however, this pass does not promote a
separate hard bug solely from kept-city reranking.

The strong same-turn proof is city REMOVAL by razing, where actual canFound legality can change
from false to true after the vector snapshot.

POST-BOUNDARY PERSISTENT-MEMBER CONTROLS
----------------------------------------
The next persistent `CvPlayerAI` members remain under F516 unused.

Initial controls from the current pass:

- `m_aiVictoryWeights` has explicit refreshes at player initialization, game finalization,
  `CvGame::setVictoryValid`, and `AI_setHuman`; no missing ordinary mutable dependency has yet
  survived.
- `m_GreatPersonWeights` is explicitly a sampled heuristic cache (updated one turn in three),
  so ordinary between-sample changes are not by themselves an invalidation defect.
- the religion / favorite-civic / bonus-trade relationship counters are persistent state, not
  derived caches. Their ordinary end-turn mutation is followed by a full own-attitude refresh
  through `AI_updateExpansionistHate() -> AI_updateAttitude()` before `AI_doDiplo()`; no new
  attitude-cache root is claimed from that path.
- `m_aiNumTrainAIUnits` / `m_aiNumAIUnits` are event-maintained counts and remain to be checked
  specifically at unit ownership / AI-role transition boundaries rather than treated as sampled
  cache data.

F516 / KI#839 remains UNUSED.

NEXT
----
1. Finish the event-maintained unit-role count audit (`m_aiNumTrainAIUnits`, `m_aiNumAIUnits`):
   init, kill, capture/ownership transfer, upgrade, and `AI_setUnitAIType` transitions.
2. Finish relationship-cache mutator checks only where a setter bypasses the documented
   incremental/full attitude refresh contract.
3. Continue Queue 004 `CvPlayer.cpp` cross-object mutator sweep once the persistent-player
   member inventory is exhausted.

CURRENT AUDIT STATE
-------------------
Queue 004 `CvPlayer.cpp` -- OPEN
Unique files complete -- 32/169
Confirmed deep/current roots -- F446 through F515
F516 / KI#839 -- UNUSED
Latest physical checkpoint -- C031-WIP261
No repository source file modified.

================================================================================
WIP262 CONTINUATION -- PERSISTENT PLAYER UNIT-ROLE COUNTS CONTROL
================================================================================

Status entering this tranche:
- Queue 004 CvPlayer.cpp OPEN.
- 32/169 unique files complete.
- F446-F515 confirmed.
- F516 / KI#839 UNUSED.
- No repository source modified.

m_aiNumAIUnits / m_aiNumTrainAIUnits lifetime audit -- CONTROL, no new finding:

1. Live-unit role counts are event-maintained at all normal structural transitions.
   - CvUnitAI::finalizeInit increments both the owning player's m_aiNumAIUnits role and the area's corresponding count.
   - CvUnitAI::AI_setUnitAIType decrements the old player/area role, changes m_eUnitAIType, then increments the new role.
   - CvUnit::setXY maintains area-role counts when a unit moves between areas.
   - CvUnit::kill(false) decrements the owning player's AI role before removing the unit.
   - CvPlayer::initUnit normalizes NO_UNITAI to the unit's default AI before initialization, so ordinary initialized units do not enter the count under an undefined role.

2. Upgrade looked briefly suspicious but closes as a transient internal state, not an escaped stale-count defect.
   - CvUnit::upgrade creates the replacement first through initUnit, so the replacement increments the same player/role count before CvUnit::convert calls kill(true) on the old unit.
   - kill(true) only marks delayed death and returns before the old count is decremented, therefore old+replacement are temporarily both represented.
   - Crucially, the normal AI caller CvUnitAI::AI_upgrade immediately calls doDelayedDeath() on the old unit directly after upgrade(). For a non-fighting upgrade this executes kill(false), removes the old count, and only then does AI_upgrade continue with post-upgrade grouping logic / return to CvPlayerAI::AI_doTurnUnitsPost.
   - Human COMMAND_UPGRADE / gift command handling also ends CvUnit::doCommand with getGroup()->doDelayedDeath().
   - I found no AI decision consumer in the tiny replacement-init -> old-doDelayedDeath interval. Therefore the temporary double count is an implementation detail, not F516.

3. Gift / ownership transfer follows the same create-new / delayed-remove pattern, with the new unit initialized for the recipient and the old unit later removed from the donor. The ordinary command path performs delayed-death cleanup before returning. No persistent cross-owner count drift was found.

4. Queued-training counts are similarly balanced.
   - CvCity::pushOrder(ORDER_TRAIN) increments city-area getNumTrainAIUnits and owner AI_changeNumTrainAIUnits for the order's UnitAI.
   - CvCity::popOrder decrements both before removing/completing the order.
   - queued-unit automatic upgrade adjusts the old and replacement UnitAI counters when the old role is no longer valid.
   - CvCity::kill calls clearOrderQueue(), which repeatedly popOrder()s queued orders, so conquest/raze/city transfer does not strand the former owner's queued UnitAI counts.

Conclusion:
The persistent unit-role count family is tightly event-maintained and the one apparent upgrade double-count window is synchronously closed by AI_upgrade/doDelayedDeath before a relevant consumer. Do not spend F516 here.

Next:
- relationship/value mutators whose stored counters feed cached attitude or diplomacy;
- distinguish mutators that adjust m_aiAttitude incrementally from annual counters deliberately refreshed later in AI_doTurnPost;
- then return to remaining CvPlayerAI persistent members / CvPlayer.cpp sweep.

Durable state after WIP262:
Queue 004 CvPlayer.cpp OPEN | 32/169 complete | F446-F515 confirmed | F516/KI#839 UNUSED | WIP262 physical checkpoint | no repository source modified.

================================================================================
WIP263 CONTINUATION -- CLOSE-BORDER ATTITUDE CACHE CANDIDATE (F516 STILL UNUSED)
================================================================================

Status entering this tranche:
- Queue 004 CvPlayer.cpp OPEN.
- 32/169 unique files complete.
- F446-F515 confirmed.
- F516 / KI#839 UNUSED.
- No repository source modified.

RELATIONSHIP / VALUE MUTATOR CONTROLS
-------------------------------------
The general attitude-mutator sweep did not produce a new root:

- AI_setAttitudeExtra adjusts the cached full attitude immediately.
- AI_setMemoryCount measures the memory-attitude delta and applies AI_changeCachedAttitude.
- same/different-religion, favorite-civic and bonus-trade annual counters are changed during
  the team counter phase and followed by the normal full attitude refresh before war/diplo.
- expansionist-hate is itself followed by / coupled to a full own-attitude refresh.
- gold-traded-to is persistent relationship state but is not a direct attitude component.
- m_bWasFinancialTrouble is already owned by F364; m_iTurnLastProductionDirty remains a control.

LIVE F516 CANDIDATE -- CLOSE-BORDER SUBCACHE AFTER LATE BORDER-GEOGRAPHY CHANGE
-------------------------------------------------------------------------------
Current SAS has a sharper cross-object lifetime mismatch:

1. At the beginning of the team's active turn, CvTeamAI::AI_doTurnPost refreshes every member's
   m_aiCloseBordersAttitude and then the full m_aiAttitude.
2. The player later reaches CvPlayer::doTurn. Each CvCity::doTurn executes doCulture() and
   doPlotCultureTimes100(...), which can change plot culture / ownership and therefore the live
   close-border geometry.
3. There is no generic city-culture / plot-ownership invalidation hook among the current
   AI_updateCloseBorderAttitude call sites. The visible refresh sites are the team-turn sweep
   plus special war-state / vassal paths.
4. CvPlayerAI::AI_doTurnPost then reaches AI_updateExpansionistHate() / AI_updateAttitude and
   AI_doDiplo(), but the full attitude rebuild does NOT recompute the close-border subcache. It
   simply reads AI_getCloseBordersAttitude(), i.e. the already-stored m_aiCloseBordersAttitude.

Therefore the apparent late full-attitude refresh is not a repair when border geography itself
changed after the team snapshot: it can rebuild m_aiAttitude from a stale subcomponent and
AI_doDiplo immediately consumes that stale result.

NORMAL NUMERIC GEOMETRY WITNESS UNDER CONSTRUCTION
---------------------------------------------------
Current Genghis has iCloseBordersAttitudeChange = -4. On a Standard map, target city count is 5.
For a small player with total land 5, the close-border land factor is clamped to 1, making the
adjacent-land weight 5.

A clean threshold is therefore adjacent foreign-team land 5 -> 6:
- at 5, the special adjacent-land contribution is absent because the code requires > 5;
- at 6, it contributes +30 percentage points;
- with Genghis's -4 close-border limit, that is enough to change the component by -1.

Thus a cached raw attitude of exactly +4 (Pleased) can correctly become +3 (Cautious) after one
ordinary culture-driven ownership/adjacency transition, while the stored subcache still says 0
and the late full-cache rebuild incorrectly preserves Pleased.

A first multi-member-team sibling also survives structurally: because the team-wide close-border
refresh runs only once, a first member's later cultural border change can leave stale member
attitudes for a later teammate's eager strategy snapshot. The later teammate's Dagger gate can
therefore use a stale team-averaged Pleased/Cautious category. This remains secondary until the
simpler single-member diplomacy consumer is fully proved.

ANCESTRY STATUS -- NOT YET CLASSIFIED
--------------------------------------
K-Mod definitely contains the lifetime mismatch: it moved close-border attitude maintenance to
an eager team-turn refresh, before subsequent player city turns, and does not generically refresh
it after culture changes.

BtS already has a lazy m_aiCloseBordersAttitudeCache and invalidates it in AI_doTurnPre before
city turns. The unresolved ancestry question is whether an ordinary/reachable pre-city consumer
repopulates that lazy cache after invalidation but before city culture changes. If so, the root is
BtS-inherited; if not, K-Mod's eager team snapshot is the regression boundary.

REMAINING PROOF BEFORE SPENDING F516
------------------------------------
1. Identify a concrete AI_doDiplo branch whose state-changing proposal/action differs at
   Cautious vs Pleased, using current SAS personality data.
2. Finish the BtS lazy-cache population trace between AI_doTurnPre invalidation and CvCity::doTurn.
3. Compare K-Mod / AdvCiv ancestry and search the album/Known Issues for semantic duplication.
4. Only then promote F516/KI#839, or close the candidate if the consumer/ancestry challenge fails.

Durable state after WIP263:
Queue 004 CvPlayer.cpp OPEN | 32/169 complete | F446-F515 confirmed | F516/KI#839 UNUSED | WIP263 physical checkpoint | no repository source modified.

================================================================================
C031-WIP264 -- F516 CONFIRMED: LATE BORDER-GEOGRAPHY CHANGES LEAVE CLOSE-BORDER ATTITUDE STALE
================================================================================

Status entering this tranche:
- Queue 004 CvPlayer.cpp OPEN.
- 32/169 unique files complete.
- F446-F515 confirmed.
- F516 / KI#839 was unused at WIP263.
- No repository source modified.

F516 / KI#839 -- BtS-inherited close-border attitude cache is not invalidated/refreshed after same-turn culture / plot-ownership changes, so later diplomacy and strategy consumers can use an obsolete attitude category
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

CURRENT SAS PRODUCER / CONSUMER ORDER
-------------------------------------
Current sequential SAS has a deterministic stale window.

At activation, the team leader executes the team turn. CvTeamAI::AI_doTurnPost refreshes each
member in this order:

    AI_updateCloseBorderAttitude();
    AI_updateAttitude();
    ...
    AI_doWar();

The active player's units then act. At deactivation, CvPlayer::doTurn runs. Its relevant order is:

    AI_doTurnPre();
    ...
    for each city:
        CvCity::doTurn()
            AI_doTurn();
            doGrowth();
            doCulture();
            doPlotCultureTimes100(...);
            doProduction(...);
    ...
    AI_doTurnPost();
        for each other player:
            other.AI_updateAttitude(getID());
        AI_updateExpansionistHate();
            ...
            AI_updateAttitude();
        AI_doDiplo();

CvPlot::updateCulture can call setOwner(calculateCulturalOwner(...)). Plot-owner/culture code has
no generic close-border-attitude invalidation hook.

The current generic close-border refresh call sites are instead limited to:
- the once-per-team sweep in CvTeamAI::AI_doTurnPost;
- special war-state contested-plot maintenance in CvTeam.cpp;
- a vassal/capitulation-specific CvTeam.cpp path.

Therefore ordinary city culture can change the exact map geometry from which
m_aiCloseBordersAttitude is derived after that cache was snapshotted and before AI_doDiplo.

WHY THE LATE FULL-ATTITUDE REFRESH DOES NOT REPAIR IT
-----------------------------------------------------
Current AI_updateExpansionistHate ends by calling AI_updateAttitude(), but AI_updateAttitude does
not calculate close borders from the map. It simply adds:

    AI_getCloseBordersAttitude(ePlayer)

and current AI_getCloseBordersAttitude is a direct return of stored m_aiCloseBordersAttitude.
Thus the late full-attitude rebuild can faithfully rebuild m_aiAttitude from an already stale
subcache and preserve the wrong category immediately before diplomacy.

CURRENT NUMERIC / GEOMETRIC WITNESS
-----------------------------------
Current Genghis Khan has:

    iCloseBordersAttitudeChange = -4

Current Standard world has:

    iTargetNumCities = 5

For a Genghis player with 5 total land tiles, AdvCiv's close-border land factor is clamped to 1,
so the adjacent-land weight remains 5. The adjacent-land contribution is only applied when
AI_calculateAdjacentLandPlots(eTheirTeam) > 5.

Use an ordinary same-continent rival for which AI_hasCitiesInPrimaryArea is true and arrange the
other close-border percentage contributions below the first -1 threshold. In the cleanest map
shape, a cultural expansion claims a previously neutral frontier plot and thereby exposes one
additional rival-owned land plot to team adjacency:

    adjacent rival land: 5 -> 6

At 5 there is no adjacency contribution. At 6 the code adds:

    6 * 5 = 30 percentage points

With limit -4, +30% is enough to change the close-border component by -1. The neutral-plot
frontier shape can keep the newly exposed rival plots outside the player's city radius, so the
stolen-radius component need not change at all; more generally any preexisting subtotal below
25% gives the same threshold witness.

Current relation thresholds make raw +4 Pleased and +3 Cautious. Therefore arrange all other
attitude components so the cached pre-culture relation is exactly +4:

    team-turn snapshot: close-border component 0 -> raw +4 -> Pleased
    after culture:      correct close-border -1 -> raw +3 -> Cautious

The late AI_updateAttitude still reads stored close-border 0, so m_aiAttitude remains +4/Pleased.

DIRECT SAME-TURN GAMEPLAY CONSEQUENCE
-------------------------------------
Current AI_doDiplo has a concrete state-changing Pleased/Cautious gate in its give-help branch.
For a sufficiently weaker known rival who can receive technology and is not excluded by the
victory/vassal checks, it requires:

    AI_getAttitude(ePlayer) > getNoGiveHelpAttitudeThreshold()

AdvCiv's LEADER_DEFAULTS sets NoGiveHelpAttitudeThreshold = ATTITUDE_CAUTIOUS, and ordinary
leaders such as current Genghis inherit that value when they do not override it.

Choose CONTACT_GIVE_HELP's reachable success roll, contact timer 0, and at least one tradable
technology. Then:

    stale cached Pleased  > Cautious  -> branch is allowed; AI target can receive a real tech gift
    correct live Cautious > Cautious  -> false; the gift branch is forbidden

For an AI recipient, the successful branch reaches implementDeal and actually transfers the
technology in the same player post-turn. This is an immediate state-changing consumer, not a
mere evaluation/scoring difference.

OTHER NORMAL PRODUCERS / SIBLINGS
---------------------------------
Culture is the clean automatic witness, but the dependency boundary is broader. Any event after
the team close-border snapshot that changes territory / border geometry can stale the same
subcache, including suitable city founding, acquisition, razing and plot-ownership transitions.
Those are F516 producers even when another cache family (e.g. F515 city sites) is independently
staled by the same event.

Multi-member teams also extend the window: the team-wide close-border/full-attitude sweep runs
once, then one member can change territory during its own turn before a later teammate activates.
That later teammate can build eager strategy state from the stale team/member attitudes. This is
a sibling consumer of the same close-border invalidation root, not a separate finding.

BTS / K-MOD / ADVCIV ANCESTRY
-----------------------------
This root is BtS-inherited.

BtS already stores a mutable lazy m_aiCloseBordersAttitudeCache. Its normal AI_doTurnPre does:

    AI_invalidateCloseBordersAttitudeCache();
    ...
    AI_doCommerce();
    ...

But the invalidation is before the cities act, not after them. A reachable ordinary pre-city
path repopulates the cache immediately: when espionage commerce is flexible and a met rival is
eligible for espionage weighting, AI_doCommerce calls team AI_getAttitudeVal; the team routine
calls member AI_getAttitudeVal; player attitude calculation calls AI_getCloseBordersAttitude,
which fills the lazy cache.

BtS then runs city doCulture / doPlotCulture, and its CvPlot ownership/culture paths contain no
AI_invalidateCloseBordersAttitudeCache hook. BtS AI_doTurnPost subsequently runs AI_doDiplo, whose
live AI_getAttitude calculation now incorporates the stale already-populated close-border cache.
BtS therefore already permits the same cache-lifetime contradiction.

K-Mod did not originate the dependency, but made it deterministic and longer-lived:

- K-Mod practical 793 / commit bd52939528, "Changed the close-border attitude cache system",
  disables the lazy MAX_INT invalidation scheme and changes close-border attitude into an eager
  stored cache.
- K-Mod practical 804 / commit 8efe75ac36, "Rearranged calls to CvTeam::doTurn to fix a balance
  problem", moves the close-border/full-attitude refresh into CvTeamAI::AI_doTurnPost.

Base AdvCiv 1.14 and current SAS retain this K-Mod eager architecture, with AdvCiv adding richer
close-border inputs such as weighted adjacent land but no generic post-geography invalidation.

SEMANTIC DEDUP
--------------
Full album and Known-Issues searches for close-border cache, m_aiCloseBordersAttitude,
AI_updateCloseBorderAttitude, stolen-city-radius and adjacent-land roots found no numbered finding
with this producer/consumer lifetime.

Nearest distinct findings:
- F511 / KI#834: team diplomacy counters/full-attitude refresh occur after the eager strategy
  snapshot, so AI_doWar can consume stale Dagger. F516 instead changes a SUBCACHE INPUT -- border
  geography -- after both close-border and full-attitude caches were refreshed.
- F515 / KI#838: city founding/removal leaves m_aeAICitySites stale. The same city event can be a
  producer for both findings, but the stored object, consumer and repair contract are different.
- F296 / KI#619: annual-deal processing refreshed attitude at the wrong deal-loop boundary; it is
  not a geographic close-border invalidation problem.

REPAIR BOUNDARY
---------------
The required invariant is:

    before an attitude/diplomacy/strategy consumer relies on close-border attitude, the stored
    close-border component must correspond to the current territory geometry.

A minimal current-turn repair for the direct single-member witness is to refresh the current
AI player's close-border cache after city/territory mutations and before the final full-attitude
refresh / AI_doDiplo.

However, because team territory changes can affect every member's border relation and a later
teammate can activate before the next team-turn sweep, a robust repair should not update only one
player in isolation. Practical safe directions are:

1. refresh affected team-member close-border caches (and then their full attitudes) at a
   post-territory boundary before later same-team consumers; or
2. introduce a true dirty/invalidation contract on plot/city ownership changes and lazily/eagerly
   rebuild close-border + dependent full attitude before consumption.

If strategy remains eagerly cached, any F516-driven attitude correction must also participate in
the strategy invalidation contract established by F511 rather than leaving Dagger/other
attitude-dependent strategy bits frozen.

F516 is therefore CONFIRMED as KI#839.

CURRENT AUDIT STATE AFTER F516
------------------------------
Queue 004 CvPlayer.cpp -- OPEN
Unique files complete -- 32/169
Confirmed roots -- F446 through F516
Next finding number -- F517 / KI#840 UNUSED
Latest physical checkpoint -- C031-WIP264
No repository source file modified.

NEXT
----
Continue the remaining CvPlayerAI persistent-state / CvPlayer.cpp cross-object mutator sweep from
F517 unused. First finish other derived attitude subcomponents whose mutable map/team inputs can
change after their maintenance boundary, then move back to the remaining persistent members and
external-input cache inversions. Aggressively dedup against F510-F516 rather than splitting one
cache-invalidation root into symptoms.


================================================================================
C031-WIP265 -- F517 CANDIDATE: PLAYER RELATIONSHIP COUNTERS MOVE AFTER TEAM ATTITUDE REFRESH
================================================================================
Date: 2026-09-03
Model: ChatGPT-5.6-Sol

STATUS AT THIS SAFETY CHECKPOINT
--------------------------------
- F446-F516 remain confirmed.
- F517 / KI#840 remains UNUSED at this checkpoint.
- Queue 004 CvPlayer.cpp remains OPEN at 32/169 unique files complete.
- No repository source file was modified.
- This checkpoint records one closed control and one live candidate before promotion/dedup.

POST-F516 WORST-ENEMY CONTROL -- CLOSED, NO NEW FINDING
------------------------------------------------------
A first sibling challenge asked whether the full attitude cache could become stale immediately
inside the normal team refresh because AI_updateAttitude runs before AI_updateWorstEnemy, while
AdvCiv rival-pact attitude terms read the current worst enemy.

That apparent order contradiction is explicitly repaired by current CvTeamAI::AI_updateWorstEnemy.
When m_eWorstEnemy changes, the function comments that EnemyPeacetime-driven attitude updates were
still based on the old worst enemy, then reruns AI_updateAttitude(..., false) against every known
rival. Thus a changed worst enemy does not leave the just-computed member attitudes inconsistent.
This family is a useful control and does not spend F517.

LIVE F517 CANDIDATE -- ORDERING ROOT
-----------------------------------
Current sequential AdvCiv/SAS ordering exposes a sharper cache boundary:

    CvPlayer::setTurnActive(true)
        -> AI_updateCacheData()
        -> team leader: GET_TEAM(getTeam()).doTurn()
            -> CvTeamAI::AI_doTurnPre()
                -> team AI_doCounter()
            -> CvTeamAI::AI_doTurnPost()
                -> each member:
                    AI_updateCloseBorderAttitude()
                    AI_updateAttitude()
                -> ...
                -> AI_doWar()
        -> doTurnUnits()

Later, when the player's own doTurn runs, CvPlayerAI::AI_doTurnPre performs:

    AI_doCounter();

    /* K-Mod note: attitude cache is not refreshed here because there are still
       some attitude-affecting changes to come, in CvTeamAI::AI_doCounter. ... */

    AI_updateBonusValue();
    AI_updateCommerceWeights();
    ...
    AI_doResearch();
    AI_doCommerce();
    AI_doMilitary();
    AI_doCivics();
    AI_doReligion();

In the current sequential scheduler, that inherited K-Mod comment no longer describes the actual
order: the team's AI_doCounter and its full member-attitude refresh already happened earlier at
player activation. The later PLAYER AI_doCounter therefore changes attitude inputs after the full
m_aiAttitude snapshot, then immediately enters multiple AI consumers without rebuilding it.

PLAYER COUNTERS THAT MUTATE WITHOUT FULL-ATTITUDE MAINTENANCE
-------------------------------------------------------------
Current CvPlayerAI::AI_doCounter changes, among other state:

- m_aiSameReligionCounter;
- m_aiDifferentReligionCounter;
- m_aiFavoriteCivicCounter;
- m_aiBonusTradeCounter;
- m_aiPeacetimeGrantValue;
- m_aiPeacetimeTradeValue.

Their ordinary set/change helpers modify the stored counter/value only; they do not generally
call AI_updateAttitude or AI_changeCachedAttitude. Memory changes are a partial control because
memory setters have their own attitude-cache delta maintenance and must not be used to overstate
the root.

Current AdvCiv later added an advc.001 player-post repair before diplomacy: other players refresh
their attitude toward this player, and AI_updateExpansionistHate also refreshes this player's own
full attitude before AI_doDiplo. That narrows the current-SAS window, but it happens only AFTER
AI_doResearch / AI_doCommerce / AI_doMilitary / AI_doCivics / AI_doReligion and therefore cannot
repair those earlier same-player consumers.

CLEAN CURRENT-DATA THRESHOLD WITNESS UNDER CONSTRUCTION
-------------------------------------------------------
Genghis Khan is a convenient shipped personality. Current XML inherits:

    iSameReligionAttitudeChange  = +1
    iSameReligionAttitudeDivisor = 10

and Genghis sets:

    iSameReligionAttitudeChangeLimit = 3

For two known major players sharing a state religion, current CvPlayerAI::AI_doCounter calls
AI_changeSameReligionCounter with the team's AI_randomCounterChange(). The helper is explicitly a
2-trial binomial whose legal result includes +1.

Arrange a single-member Genghis team whose already-cached raw attitude toward one rival is exactly
+3 (Cautious) while SameReligionCounter == 9. A supported +1 counter result gives 9 -> 10. Provided
the ordinary ideology-prevalence adjustment leaves at least the first positive step available,
AI_getSameReligionAttitude gains +1 at divisor 10, so the correct live raw attitude becomes +4
(Pleased). The stored m_aiAttitude remains the pre-counter +3/Cautious value.

DIRECT CONSUMER LEAD -- AI_doCommerce ESPIONAGE
------------------------------------------------
The immediately following AI_doCommerce reads team attitude. On a one-member team,
CvTeamAI::AI_getAttitude/AI_getAttitudeVal is directly based on this player's stale m_aiAttitude.
Its espionage loop has a categorical branch:

    if (iTheirEspPoints > iDesiredMissionPoints &&
        kOurTeam.AI_getAttitude(eRival) <= ATTITUDE_CAUTIOUS)
    {
        ...
        iDesiredMissionPoints = max(iTheirEspPoints-adjusted, iDesiredMissionPoints);
    }

Thus the candidate can cross an exact branch boundary:

    stale cache:   raw +3 -> Cautious -> espionage catch-up branch ENTERED
    correct state: raw +4 -> Pleased  -> branch SKIPPED

With no war plan, no preexisting desired mission target, positive rival espionage points and low
own espionage points, the stale branch can create a positive aiTarget where the correct Pleased
state leaves the target at zero. AI_doCommerce then writes per-team espionage spending weights
through setEspionageSpendingWeightAgainstTeam and uses the aggregate target rate when deciding
whether to move commerce from research into espionage. This is a direct same-call AI-state / slider
consumer; exact arithmetic is still being tightened before F517 is spent.

K-MOD / BTS ANCESTRY STATUS
---------------------------
K-Mod 1.46 has the same player pre-turn structure and even the same explanatory comment: player
AI_doCounter runs first; attitude refresh is omitted because team AI_doCounter is said to be
"still" forthcoming; then commerce/military/civic/religion AI runs. Its sequential scheduler,
however, calls the team's doTurn when the team leader is activated before doTurnUnits, while the
player doTurn/AI_doTurnPre occurs later. K-Mod team AI_doTurnPost eagerly updates every member's
close-border and full attitude caches before AI_doWar. This therefore already contains the
ordering inversion needed by the candidate.

BtS/Civ4CE is the negative control for the FULL-ATTITUDE-cache root. BtS AI_doTurnPre likewise
runs player AI_doCounter before AI_doCommerce, but CvPlayerAI::AI_getAttitudeVal computes the
attitude dynamically from live relationship counters on every call; there is no K-Mod full
m_aiAttitude cache. After SameReligionCounter changes, the subsequent commerce evaluation therefore
sees the changed counter. BtS has the separate lazy close-border-cache lifetime issue recorded as
F516, but not this full-attitude snapshot contradiction.

Current classification is therefore likely K-Mod-introduced and inherited by Base AdvCiv 1.14 /
SAS, pending final ancestry wording.

SEMANTIC DEDUP STATUS
---------------------
Initial full-album / Known-Issues searches found no numbered finding for PLAYER AI_doCounter
changing religion/civic/trade relationship components AFTER the team full-attitude refresh and
before same-player commerce/other AI consumers.

Nearest distinct roots:
- F511 / KI#834: TEAM diplomacy counters become current after the eager STRATEGY snapshot; the
  refreshed full attitude is correct when AI_doWar runs, but Dagger is stale. The live candidate
  instead changes PLAYER relationship counters after that full-attitude refresh; the stale stored
  object is m_aiAttitude itself and direct commerce/etc. consumers read it.
- F296 / KI#619: recurring annual deals refreshed only one trade direction prematurely inside a
  deal batch. Different producer/batch boundary.
- F516 / KI#839: close-border SUBCACHE input changes after geography mutations. Different stored
  object/input and invalidation contract.

REMAINING PROOF BEFORE PROMOTION
--------------------------------
1. Tighten the same-religion 9 -> 10 fixture through AI_ideologyAttitudeChange's prevalence/limit
   adjustment so the +1 step is unquestionably reachable in ordinary current data.
2. Finish explicit AI_doCommerce arithmetic showing stale Cautious creates a different stored
   espionage weight and, with a reachable target-rate setup, can change the research/espionage
   slider in the same call.
3. Finish K-Mod/BtS ancestry wording and verify Base AdvCiv keeps the same cache/order architecture.
4. Run final semantic dedup across album and Known Issues; broaden only to the same full-attitude
   freshness root rather than splitting religion/civic/trade-counter symptoms.
5. Only then promote to F517 / KI#840 or close as a control.

CONTINUATION STONE
------------------
Queue 004 CvPlayer.cpp OPEN | 32/169 unique files complete | F446-F516 confirmed |
F517/KI#840 UNUSED | WIP265 latest physical checkpoint | no repository source modified.

Resume: finish player-AI_doCounter -> stale m_aiAttitude -> AI_doCommerce proof; then ancestry /
dedup; either confirm F517 or close it without spending the number.


================================================================================
C031-WIP266 -- F517 CONFIRMED: PLAYER COUNTERS CHANGE AFTER FULL-ATTITUDE SNAPSHOT
================================================================================
Date: 2026-09-03
Model: ChatGPT-5.6-Sol

F517 / KI#840 -- K-Mod's player relationship counters advance after the team full-attitude snapshot, so same-turn commerce can act on yesterday's attitude
-----------------------------------------------------------------------------------------------------------------------------------------------

ROOT / SCHEDULER CONTRADICTION
------------------------------
F517 is confirmed. In ordinary non-simultaneous turns, current SAS performs the relevant work in
this order for an AI player:

    CvPlayer::setTurnActive(true)
        -> AI_updateCacheData()
        -> team leader: CvTeam::doTurn()
            -> CvTeamAI::AI_doTurnPre()
                -> CvTeamAI::AI_doCounter()
            -> CvTeamAI::AI_doTurnPost()
                -> each member:
                    AI_updateCloseBorderAttitude()
                    AI_updateAttitude()
                -> ...
                -> AI_doWar()
        -> doTurnUnits()

    CvPlayer::setTurnActive(false)
        -> CvPlayer::doTurn()
            -> CvPlayerAI::AI_doTurnPre()
                -> CvPlayerAI::AI_doCounter()
                -> AI_updateCommerceWeights()
                -> ...
                -> AI_doResearch()
                -> AI_doCommerce()
                -> AI_doMilitary()
                -> AI_doCivics()
                -> AI_doReligion()

The K-Mod comment immediately after PLAYER AI_doCounter says attitude is deliberately not refreshed
there because attitude-affecting changes in CvTeamAI::AI_doCounter are "still" to come. Under the
sequential scheduler, that premise is inverted: the TEAM counter pass and full member-attitude
refresh already ran earlier at activation. The PLAYER counter pass therefore mutates relationship
inputs AFTER m_aiAttitude was snapshotted.

The affected ordinary player-state inputs include SameReligionCounter, DifferentReligionCounter,
FavoriteCivicCounter, BonusTradeCounter and the peacetime trade/grant values. Their ordinary
set/change helpers mutate stored state without rebuilding full attitude. Memory changes are not a
blanket example because memory mutators have dedicated cached-attitude delta maintenance.

AdvCiv practical advc.001 later recognizes that player turns occur after the team turn and adds a
late player-post attitude repair before diplomacy. Current AI_doTurnPost refreshes other players'
attitude toward this player and AI_updateExpansionistHate refreshes this player's own full attitude
before AI_doDiplo. That prevents the stale full attitude from necessarily reaching current SAS
post-turn diplomacy, but it occurs only after the research/commerce/military/civic/religion AI
above and therefore does not repair F517's earlier consumers.

CURRENT SHIPPED-DATA +3 -> +4 RELIGION WITNESS
----------------------------------------------
Current Genghis Khan provides a compact ordinary threshold. The inherited leader defaults give:

    iSameReligionAttitudeChange  = +1
    iSameReligionAttitudeDivisor = 10

and Genghis sets:

    iSameReligionAttitudeChangeLimit = 3

Current relation thresholds classify raw +3 as Cautious and raw +4 as Pleased
(RELATIONS_THRESH_PLEASED = 4).

Use a one-member Genghis team and one known rival sharing Genghis's state religion. Arrange the
already-cached attitude subtotal at raw +3 and SameReligionCounter == 9 at activation. Current
player AI_doCounter calls:

    AI_changeSameReligionCounter(ePlayer, kOurTeam.AI_randomCounterChange());

AI_randomCounterChange is explicitly a two-trial binomial and +1 is an ordinary legal result.
Choose that supported result, so the player counter advances 9 -> 10.

The current AdvCiv ideology-prevalence adjustment cannot eliminate Genghis's FIRST positive step
in this fixture. For SAME_RELIGION the positive limit is reduced by at most:

    round(0.5 * ratio * 3)

with ratio <= 1. Even at ratio == 1 this removes at most 2 from limit 3, leaving limit >= 1.
Therefore counter 10 reaches the first divisor and AI_ideologyAttitudeChange adds +1.

The correct live attitude consequently becomes:

    before player counter: raw +3 -> Cautious
    after  player counter: raw +4 -> Pleased

but m_aiAttitude remains the team-turn +3/Cautious snapshot throughout the immediately following
AI_updateCommerceWeights / AI_doResearch / AI_doCommerce sequence.

EXACT AI_doCommerce ESPIONAGE WITNESS
-------------------------------------
AI_doCommerce supplies a direct state-changing consumer with an exact categorical boundary. On a
one-member team, CvTeamAI::AI_getAttitude and AI_getAttitudeVal ultimately read this same player's
stored full-attitude cache.

Inside the no-war-plan espionage loop, current code first obtains:

    int iAttitude = range(kOurTeam.AI_getAttitudeVal(eRival), -12, 12);

and later has:

    if (iTheirEspPoints > iDesiredMissionPoints &&
        kOurTeam.AI_getAttitude(eRival) <= ATTITUDE_CAUTIOUS)
    {
        ... catch-up target calculation ...
    }

Use an ordinary midgame setup with:

    game speed / target turns          = Normal / 4
    no war plan                        = yes
    rival current EP against us        = 360
    our current EP against rival       = 0
    rival espionage points ever        = 360
    our espionage points ever          = 0 (the code floors this total to 4)
    our UNITAI_SPY count               = 0
    rival has no cities in our primary area
    no cheap-tech-steal override       = yes
    gold commerce percent              = 0
    personality espionage weight       = 100 (current Genghis)

With the stale raw +3/Cautious cache:

1. No-war desired mission points stay 0 because the ordinary demographics branch requires
   iAttitude < 3, while stale iAttitude is exactly 3.

2. The base rate divisor is:

       4 target turns * 3 = 12

   The raw-attitude correction is integer 12 / 20 = 0; no spy adds +4; no primary-area cities
   adds +2. Therefore:

       iRateDivisor = 18

3. The Cautious catch-up gate ENTERS. Rival current points are relation-adjusted:

       360 * (34 - 3) / 36
     = 360 * 31 / 36
     = 310

4. With own total floored to 4 and rival total 360, the long-run scaling gives:

       310 * (3*4 + 360) / (2*4 + 4*360)
     = 310 * 372 / 1448
     = 79   (integer result)

5. The stale rival target is therefore:

       aiTarget = (79 - 0) / 18 = 4

   and aggregate iEspionageTargetRate becomes 4.

The rival's initial spending weight is 6, and iAttitude/3 subtracts 1, leaving 5. Because this is
the only positive target:

    aiWeight += 100 * 4 / max(4, 4)
              = 100

so current code stores:

    stale Cautious spending weight = 105

With the CORRECT post-counter raw +4/Pleased state, the catch-up gate is false. There are still no
ordinary desired mission points in this controlled setup, therefore:

    correct aiTarget              = 0
    correct iEspionageTargetRate  = 0
    correct stored spending weight = 5

The stale full-attitude cache thus changes a persistent rival espionage allocation from 5 to 105
in the same AI_doCommerce call.

DIRECT COMMERCE-SLIDER CONSEQUENCE
----------------------------------
The same fixture can make the consequence immediately player-economic rather than only a stored
weight difference. AI_doCommerce controls the espionage slider when research and espionage are
flexible and the player is not in financial trouble, not researching the first technology and not
at Culture-4. Arrange those ordinary conditions, begin with 100% research / 0% espionage, and keep
gold safely above the target check.

For stale Cautious, iEspionageTargetRate == 4 and current espionage commerce rate is 0, so the loop
executes and first performs:

    changeCommercePercent(COMMERCE_RESEARCH,  -10);
    changeCommercePercent(COMMERCE_ESPIONAGE, +10);

The subsequent science-ratio guard does not undo that first step. With 90 research / 10 espionage,
its test is 180 <= 60, false.

For correct Pleased, iEspionageTargetRate == 0, so the loop condition 0 < 0 is false and NO
research-to-espionage step occurs.

Thus the F517 cache-order defect can directly spend 10 percentage points of commerce on espionage
that the live relationship state would not request. This is a same-call state change, not merely a
one-point heuristic approximation.

SIBLING CONSUMERS / SCOPE
-------------------------
AI_updateCommerceWeights also runs immediately after player AI_doCounter and reads team attitude;
it is another same-root consumer. AI_doResearch, AI_doMilitary and other subsequent AI routines
contain additional attitude-sensitive paths. They do not warrant separate numbers: the shared
root is the missing full-attitude synchronization after PLAYER relationship-counter maintenance.

The producer family should likewise not be split by counter type. SameReligionCounter is the clean
numeric witness, while DifferentReligionCounter, FavoriteCivicCounter, BonusTradeCounter and
peacetime trade/grant-value decay are other ways to make the stored full attitude disagree with
its live inputs at the same boundary.

K-MOD ORIGIN / BTS NEGATIVE CONTROL / ADVCIV INHERITANCE
--------------------------------------------------------
This full-attitude-cache root is K-Mod-introduced.

BtS/Civ4CE has the same broad player ordering -- AI_doCounter before AI_doCommerce -- but
CvPlayerAI::AI_getAttitudeVal calculates the complete attitude dynamically from current counters
on every call. It has no K-Mod full m_aiAttitude cache. Therefore the same 9 -> 10 religion-counter
change is visible to the immediately following BtS commerce evaluation. BtS's separate lazy
close-border subcache can be stale under F516, but that does not create F517's full-attitude
snapshot contradiction.

K-Mod 1.46:
- stores full player attitude in m_aiAttitudeCache and AI_getAttitudeVal returns it directly;
- refreshes member close-border/full-attitude caches in CvTeamAI::AI_doTurnPost;
- under sequential turns, invokes CvTeam::doTurn at team-leader ACTIVATION, while player doTurn /
  AI_doTurnPre occurs later when that player is deactivated;
- retains the AI_doTurnPre comment claiming team AI_doCounter is still forthcoming after player
  AI_doCounter, even though the scheduler now does the reverse.

K-Mod's v1.32 changelog explicitly says it rewrote attitude and close-border caches to update only
at explicitly specified times, accepting that cache inaccuracies could otherwise manifest as a
delay rather than OOS; the same v1.32 turn-mechanics work rearranged team-turn timing. In the live
code, the player-counter/full-attitude ordering is therefore a systematic explicit-snapshot
freshness gap rather than BtS dynamic-attitude behavior.

Base AdvCiv 1.14 retains the same scheduler, eager full-attitude member refresh and player-pre
counter ordering. Its advc.001 late player-post refresh reduces the lifetime before diplomacy but
does not move the snapshot behind the earlier commerce/research/military consumers. Current SAS
inherits that architecture.

SEMANTIC DEDUP
--------------
Full-album and Known-Issues searches found no existing root with this producer / stored object /
consumer boundary.

Nearest distinct findings:
- F511 / KI#834: TEAM relationship counters and the full attitude cache become current AFTER the
  eager STRATEGY snapshot, so AI_doWar sees fresh attitude but stale Dagger. F517 is the opposite
  cache layer: PLAYER relationship counters change AFTER the full-attitude snapshot, so direct
  player-AI consumers see stale m_aiAttitude itself. Refreshing strategy at F511's team-war
  boundary does not repair F517.
- F516 / KI#839: territory changes stale the close-border SUBCACHE after its refresh. F517 needs no
  geographic mutation; its full-attitude inputs themselves change through the ordinary player
  counter pass.
- F296 / KI#619: annual deal processing refreshed one direction at the wrong deal-loop boundary.
  Different producer and synchronization batch.
- F362 / KI#? BonusTrade ceiling work concerns how high a relationship counter can accumulate;
  F517 remains even when every counter's numerical evolution is otherwise correct.

REPAIR BOUNDARY
---------------
The required invariant is:

    after CvPlayerAI::AI_doCounter changes relationship components and before any player AI
    consumer reads attitude, m_aiAttitude must represent those new components.

The direct narrow repair is to rebuild the current AI player's full attitude immediately after
AI_doCounter (close-border need not be recomputed merely for religion/civic/trade-counter changes)
before AI_updateCommerceWeights / AI_doResearch / AI_doCommerce.

An architectural alternative is a true full-attitude dirty/lazy contract in which the affected
counter/value mutators mark the relevant relationship dirty and the next consumer rebuilds it.
That is broader but avoids future event-order assumptions.

Do not merely refresh before AI_doDiplo: current AdvCiv already effectively does that and F517's
commerce consequence occurs much earlier. Do not patch only the espionage Cautious gate either;
the stale object is general full attitude and has multiple consumers.

F517 is therefore CONFIRMED as KI#840.

CURRENT AUDIT STATE AFTER F517
------------------------------
Queue 004 CvPlayer.cpp -- OPEN
Unique files complete -- 32/169
Confirmed roots -- F446 through F517
Next finding number -- F518 / KI#841 UNUSED
Latest physical checkpoint -- C031-WIP266
No repository source file modified.

NEXT
----
Continue from F518 unused. First finish the remaining player-counter/cache siblings to ensure no
separate persistent object is also snapshotted before AI_doCounter (especially derived commerce /
strategy-like state not already covered by F511/F517), then return to the remaining CvPlayerAI /
CvPlayer.cpp persistent-member and cross-object mutator inventory. Keep event-order approximations
that are deliberately sampled as controls unless a concrete stale-state contract and consumer are
proved.


===============================================================================
C031-WIP267 -- POST-F517 SIBLING BOUNDARY / F513 INPUT-SCOPE EXPANSION
Date: 2026-09-03
Model: ChatGPT-5.6-Sol
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvPlayer.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
- Unique completed files remain 32/169.
- F445 / KI#768 remains RETRACTED and must not be implemented.
- F446-F517 remain confirmed.
- F518 / KI#841 remains UNUSED.
- No repository source file was modified.

POST-F517 SIBLING BOUNDARY
--------------------------
The immediate sibling sweep does not justify another finding number.

1. `AI_updateCommerceWeights()` is another SAME-ROOT F517 consumer. It executes directly
   after player `AI_doCounter()` and can query team/player attitude while the full attitude
   snapshot still predates the just-advanced player relationship counters. This broadens the
   repair invariant but not the root: F517 should be understood as

       player relationship counters mutate
           -> full `m_aiAttitude` snapshot is not rebuilt
           -> attitude-dependent player AI runs in the same player turn.

   The explicit espionage-spending/slider witness remains the strongest deterministic proof.

2. Any strategy consequence of the same player-counter transition belongs to the existing
   strategy-freshness family (especially F511), not to F518. Repairing the full attitude cache
   after player `AI_doCounter()` must be coordinated with strategy invalidation/recomputation
   wherever a later same-turn strategy consumer is expected to observe that newly current
   attitude. Do not split this cache-layer interaction into another number merely because a
   second derived cache can inherit the first one's stale input.

3. Victory-stage/average caches did not expose an independent player-relationship dependency
   in this pass. Memory state is also a control because ordinary memory mutators have explicit
   cached-attitude delta maintenance. F518 remains unused.

EXPANSIONIST-HATE CACHE -- INTENTIONAL SAMPLED CONTROL
------------------------------------------------------
A cross-player candidate involving `m_arExpansionistHate` was challenged and CLOSED.

At first glance the stored ratio looks vulnerable because it is refreshed only at selected
maintenance points, while later team attitude / war decisions can run before another refresh.
The actual semantic split is important:

- `AI_getExpansionistAttitude(ePlayer)` dynamically rescans the rival's CURRENT revealed
  cities, highest-culture team and culture percentages every time the full attitude value is
  calculated. Thus city founding/capture/culture changes are not hidden behind
  `m_arExpansionistHate`.
- The stored `m_arExpansionistHate` value is only a PERSONAL HOSTILITY MULTIPLIER derived
  primarily from peace-weight/warmonger personality and relative team power.
- Current AdvCiv explicitly documents this as intentional sampling:

      "Can't update the AI attitude cache whenever the power ratio shifts;
       that happens too frequently. Instead cache the expansionist hate ratio,
       and update the cached ratio only at certain times."

- Major city acquisition already triggers global expansionist-hate maintenance for met
  players because it is treated as a major power shift.

Therefore a rival expanding between snapshots is still seen by the next full-attitude
calculation through the live city/culture scan; only the frequently changing power multiplier
is deliberately sampled. No event-driven cache contract comparable to F516/F517 is violated.
Do not spend F518 on this family absent a different producer that contradicts the documented
sampling contract.

F513 SIBLING SCOPE -- SAME `m_neededExplorersByArea` INVALIDATION ROOT
---------------------------------------------------------------------
F513 proved the AdvCiv regression created when dynamic `AI_neededExplorers(area)` was replaced
with an activation snapshot in `m_neededExplorersByArea`, using unit exploration/revelation as
its clean producer. Re-reading `AI_neededExplorers_bulk` shows that MAP REVELATION is not its
only mutable input. The cached result also depends on:

- `getCurrentEra()` through the land divisor `max(150 - 20 * era, 50)`;
- `getNumCities()` through the land/water explorer caps;
- `countCivTeamsAlive()` and the team's current met-civilization count;
- capital existence/location and whether an unmet major rival has a capital in the same land
  area;
- Pangaea classification for the water branch (normally stable during ordinary play).

No additional `AI_updateNeededExplorers()` caller was found for ordinary changes to these
inputs; current SAS still rebuilds the map only from `AI_updateCacheData()` at activation.
Consequently ordinary same-turn changes in these inputs are additional manifestations of F513,
not new cache roots.

Two especially clean siblings are now bounded:

A. ERA CHANGE DURING RESEARCH

`CvTeam::setHasTech` synchronously raises each team member's `CurrentEra` when a newly acquired
tech has a later era. This can happen in the player's `doResearch()` after the activation-time
explorer snapshot but before city turns/production. No explorer-cache rebuild follows.

For example, with one city and exactly 260 unrevealed land tiles:

    Ancient snapshot divisor = 150 -> 260 / 150 = 1
    Classical live divisor   = 130 -> 260 / 130 = 2
    one-city cap             = 2

If the player enters Classical through that research completion, the stored target can remain
1 while the correct current target is 2. Later city production in the same player turn can
therefore suppress an Explorer that the live post-tech state calls for. This is the same
stored object, same missing invalidation contract and same city-production consumer as F513.

B. FIRST CONTACT DURING UNIT MOVEMENT

When the ordinary tile-count term is 0, the land branch can still request one Explorer if the
player has not met all major civilization teams and an unmet rival capital lies in the
player's capital area. A unit can establish first contact after the activation snapshot.
The team then immediately knows that rival, so the live unmet-capital reason can disappear,
but there is no `AI_updateNeededExplorers()` call. Later unit/city consumers can still read the
old target of 1. Again this is F513, not F518.

City-count/capital-area changes from founding/capture/raze are governed by the same principle:
when they change an input to `AI_neededExplorers_bulk` after activation, the stored area target
cannot follow until the next cache rebuild. They should be absorbed into F513 unless a future
case exposes a genuinely different cached object or repair boundary.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F517 / KI#840 -- CONFIRMED.
F518 / KI#841 -- UNUSED.
Latest physical checkpoint -- C031-WIP267.
No repository source file modified.

NEXT
----
Resume the higher-signal CvPlayer/CvPlayerAI dependency inversion under F518 unused:
1. prioritize persistent or city/player aggregate caches with mutable FOREIGN player/team/map
   inputs and owner-local invalidation only;
2. aggressively dedup trade-route external inputs against F505-F507 and espionage visibility
   against F508;
3. prefer event-driven current-state contracts and same-turn economic/war/unit consumers over
   explicitly sampled heuristic estimates;
4. keep freezing physical checkpoints before promotion of any new finding.


================================================================================
C031-WIP268 APPEND -- 2026-09-03 -- F518 empire-average cache candidate / practical-170 boundary
================================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F517 / KI#840 remain CONFIRMED.
F518 / KI#841 remains UNUSED in this checkpoint.
No repository source file was modified.

F518 LIVE CANDIDATE -- `AI_calculateAverages()` MOVED BEFORE UNIT-PHASE CITY ACQUISITION
----------------------------------------------------------------------------------------
The first genuinely different cache family after WIP267 is the empire-average snapshot built
by `CvPlayerAI::AI_calculateAverages()`.

Current SAS activation order is:

    CvPlayer::setTurnActive(true)
        -> AI_updateCacheData()
            -> AI_calculateAverages()
        -> team turn
        -> doTurnUnits()
            -> ordinary AI unit movement / combat / city capture / city founding
        ...
    CvPlayer::doTurn()
        -> AI_doTurnPre()
            -> AI_doResearch()
            -> AI_doCommerce()
        -> doGold()
        -> doResearch()
        -> doEspionagePoints()
        -> city turns

The current average getters are simple stored accessors. No ordinary acquisition/founding
hook calls `AI_calculateAverages()`; in the fresh source the only live ordinary call is the
activation-time call in `AI_updateCacheData()`.

The snapshot contains a WHOLE FAMILY of current-city-set aggregates:

- `m_aiAverageYieldMultiplier`;
- `m_aiAverageCommerceMultiplier`;
- `m_iAverageGreatPeopleMultiplier`;
- `m_aiAverageCommerceExchange`;
- K-Mod `m_iAverageCulturePressure`.

All are recomputed by iterating the player's current cities. A city acquired or founded after
the activation snapshot therefore changes the correct input set while every stored aggregate
continues to describe the pre-unit empire until the next activation. F518, if confirmed, should
own this whole `AI_calculateAverages` city-set lifetime root rather than numbering one finding
per average member.

HISTORY -- EXACT K-MOD RELOCATION BOUNDARY
-----------------------------------------
K-Mod practical 56 / commit `2034aebc356ff90d54dd05effd2610154d5d887c`
introduced the cached average culture-pressure member and a lazy same-game-turn getter.
The commit log explicitly says:

    Average (K-Mod) culture pressure factor is now cached (although it isn't being used yet).

Practical 57 / `cdfa07682c2a7a87a07bb384e21924345ba3f2d4` immediately began using that
average in culture valuation, and practical 103 / `5750806e12638791999cb664ed82602822e05e6b`
added the still-current culture-slider consumer:

    iIdealPercent += (AI_averageCulturePressure() - 100) / 10;
    iIdealPercent = max(iIdealPercent,
            (AI_averageCulturePressure() - 100) / 5);

Practical 165 / `f2c41e4abdd234768bd14062c99c97c7c82a15e5` disabled the lazy getter refreshes
and explicitly called `AI_calculateAverages()` from `AI_doTurnPre()`.

That ordering is significant: under the sequential K-Mod/AdvCiv scheduler,
`AI_doTurnPre()` belongs to the player's later `doTurn()`, after `doTurnUnits()`. Thus a city
captured/founded during the active unit phase was already part of the average snapshot before
later research/commerce consumers.

K-Mod practical 170 / commit `8bd260f741645fd4b5410e7470519f7a56be5ecb`, titled:

    Cache data is now updated when player is initialized, and when turn becomes active.
    (this fixes the problem of cache not being initialized on the first turn, and it also
    fixes the problem of cache data being 1 turn old.)

moved `AI_calculateAverages()` out of `AI_doTurnPre()` into activation-time `updateCacheData()`.
That fixed between-turn age but reversed the city-acquisition freshness boundary: averages now
snapshot the empire BEFORE the unit phase and are consumed AFTER it. Current K-Mod ancestry,
Base AdvCiv 1.14 and current SAS retain this activation-time architecture.

CULTURE-PRESSURE SLIDER WITNESS -- NUMERIC CORE NOW PROVED
----------------------------------------------------------
The strongest deterministic consumer is `AI_doCommerce()`.

Construct a normal sequential AI turn with one pre-existing city A when activation starts:

    city A culture commerce rate = R > 0
    city A `AI_culturePressureFactor()` = 100

Activation stores:

    m_iAverageCulturePressure = 100

During `doTurnUnits()`, the AI recaptures and keeps border city B. `CvPlayer::acquireCity`
preserves city culture; a recapture bypasses the normal conquest building-loss roll; and
occupation is added only when current team city culture is below the inherited
`OCCUPATION_CULTURE_PERCENT_THRESHOLD` (75). Thus a high-own-culture recapture can immediately
be non-disordered and retain ordinary nonzero culture output.

Arrange B to have the same culture commerce rate R and live culture-pressure factor 200.
Then the current correct empire average after acquisition is exactly:

    (R * 100 + R * 200) / (R + R) = 150

but the stored activation snapshot remains 100.

Pressure 200 itself is reachable with ordinary current arithmetic. On a Normal game started
from the first turn, current GameSpeed XML contains exactly 500 turns and
`getEstimateEndTurn()` is initialized to that sum. At game turn 100, one BFC plot with 15%
our culture and 85% culture from a third major rival, with both cultures in range and culture
flipping allowed, contributes:

    foreign share = floor(100 * 85 / (85 + 15)) = 85
    squared        = 7225
    late-game dull = floor(7225 * 500 / (100 + 500)) = 6020
    factor         = 100 + floor(6020 / 60) = 200

All other BFC plots can have zero foreign culture. Using a third rival for the pressure avoids
any `canCultureFlip` exclusion tied to the city's immediate previous owner.

With no angry citizens, no Culture2/3/4 victory-stage bonus and no `AI_avoidScience`, current
`AI_doCommerce()` therefore gives:

    stale pressure 100:
        ideal = max(0 + 0, 0) = 0 -> 0% culture

    correct pressure 150:
        ideal = 0 + 5
        max(5, 10) = 10
        10-point commerce increment keeps 10 -> 10% culture

So the stale activation snapshot can produce an actual 0% instead of 10% culture slider in
that same player turn. `CvPlayer::doTurn()` then immediately executes `doGold()`, `doResearch()`
and `doEspionagePoints()` with that commerce allocation, giving a direct same-turn economic
consequence rather than merely a valuation difference.

DEDUP / REMAINING PROOF OBLIGATIONS
-----------------------------------
Current album search found prior uses of average-commerce/culture values but no existing root
for `AI_calculateAverages` being built before unit-phase city-set mutation. In particular:

- the old `AI_doCommerce` SAS-delta control only proved that locally reusing the already-cached
  culture-pressure getter is behaviorally identical; it did not test snapshot lifetime;
- F513 is a different activation snapshot (`m_neededExplorersByArea`) with different inputs,
  consumers and repair boundary;
- F514 is also born in practical 170, but is the independent victory-before-strategy dependency
  reversal inside the cache update itself. Repairing F514's ordering does not make empire
  averages current after unit movement;
- F515 owns stale FoundValue/site state after city topology changes, not empire output averages.

Before F518 is spent, finish:
1. final semantic dedup against all city-acquisition/cache findings and known issues;
2. confirm no indirect acquisition/founding hook reaches `AI_calculateAverages()` (current call
   inventory already strongly indicates none);
3. classify all `AI_calculateAverages` members under the same root while keeping the culture
   slider as the deterministic witness;
4. only then promote F518 / KI#841 and freeze the next checkpoint.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F517 / KI#840 -- CONFIRMED.
F518 / KI#841 -- UNUSED.
Latest physical checkpoint -- C031-WIP268.
No repository source file modified.


================================================================================
C031-WIP269 APPEND -- 2026-09-03 -- F518 / KI#841 CONFIRMED
================================================================================

F518 / KI#841 -- K-MOD PRACTICAL 170 MOVED `AI_calculateAverages()` BEFORE UNIT ACTIONS,
SO SAME-TURN CITY-SET CHANGES LEAVE THE EMPIRE-AVERAGE SNAPSHOT STALE FOR LATER AI DECISIONS
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- current deterministic ordering regression created by K-Mod's practical-170 cache
relocation, inherited by Base AdvCiv 1.14 and current AdvCiv-SAS. The underlying average-cache
family originates in BtS; K-Mod added the culture-pressure member and later converted the lazy
same-game-turn cache into an explicit snapshot. Very old lazy-cache versions can in principle
have conditional intra-turn staleness if a getter populates the shared averages before a city
change; practical 170 is the exact boundary that makes the CURRENT pre-unit snapshot window
structural after practical 165 had explicitly refreshed averages after the unit phase.

ROOT / CURRENT ORDER
--------------------
Current SAS executes, in an ordinary sequential AI turn:

    CvPlayer::setTurnActive(true)
        -> CvPlayerAI::AI_updateCacheData()
            -> AI_updateNeededExplorers()
            -> AI_calculateAverages()              // snapshots CURRENT city set here
            -> victory / strategy / other caches
        -> team turn
        -> CvPlayer::doTurnUnits()
            -> Settlers can found cities
            -> combat units can acquire/keep cities
        ...
    CvPlayer::doTurn()
        -> CvPlayerAI::AI_doTurnPre()
            -> AI_doCounter()
            -> AI_updateBonusValue()
            -> AI_updateCommerceWeights()
            -> AI_doResearch()
            -> AI_doCommerce()                     // consumes cached averages
        -> doGold()
        -> doResearch()
        -> doEspionagePoints()
        -> city turns

The current getters simply return stored values; their old lazy refresh checks are commented
out. A whole-core current-source call inventory finds exactly one live ordinary
`AI_calculateAverages()` call, from `AI_updateCacheData()`. The only live ordinary callers of
`AI_updateCacheData()` are player initialization and turn activation. City founding,
acquisition, keeping, razing and ordinary city initialization do not refresh these averages.

The single snapshot contains:

    m_aiAverageYieldMultiplier
    m_aiAverageCommerceMultiplier
    m_iAverageGreatPeopleMultiplier
    m_aiAverageCommerceExchange
    m_iAverageCulturePressure

Each value is derived by iterating the player's current cities (culture pressure uses the
current culture-commerce rates and each city's live `AI_culturePressureFactor`). Therefore a
city founded or acquired during the unit phase can change the correct input set after the
snapshot but before research/commerce and other later consumers.

CLEAN CURRENT-SAS NORMAL-GAME WITNESS -- CREATIVE SETTLER FOUNDING
------------------------------------------------------------------
The conquest/recapture witness from WIP268 remains valid as a secondary producer, but current
SAS provides a simpler ordinary founding witness that needs no occupation/building-survival
assumptions.

Current `TRAIT_CREATIVE` has:

    iFreeCityCulture = 10
    CommerceChanges[COMMERCE_CULTURE] = +1

`CvCity::init` calls `initTraitCulture()` for a newly founded city before updating its culture
level. On Normal speed the current Fledgling threshold is exactly 10 culture, so a Creative
new city immediately has the corresponding cultural footprint. The player's +1 free city
culture commerce is also part of the city's base commerce calculation. Ordinary city-yield
initialization updates commerce, so the new city is a real nonzero-culture-rate member before
the later `AI_doCommerce()` pass.

Use a Creative AI on current Normal speed at game turn 100, with Drama or any other current
condition making Culture commerce flexible, not pursuing a Culture2+ victory stage,
`AI_avoidScience()` false and no angry-population culture demand.

At activation it owns only city A, arranged with:

    culture commerce rate = 100 (the Creative +1 alone is sufficient)
    AI_culturePressureFactor(A) = 100

Thus activation stores:

    m_iAverageCulturePressure = 100

During `doTurnUnits()`, a normal Settler founds border city B. B likewise has culture commerce
rate 100 from Creative. Arrange one plot in B's cultural footprint at 15 own / 85 rival plot
culture, with the rival also in culture range, not a vassal, and normal city flipping allowed;
other relevant plots have no rival culture.

Current Normal GameSpeed XML has exactly 500 turns, and `CvGame::init` initializes
`getEstimateEndTurn()` to the sum of those turn increments. At game turn 100 the current
`AI_culturePressureFactor()` integer arithmetic is therefore:

    rival share = floor(100 * 85 / (85 + 15)) = 85
    range/flip divisor = 2, so 85 * 2 / 2 = 85
    squared pressure contribution = 85^2 = 7225
    late-game dulling = floor(7225 * 500 / (100 + 500)) = 6020
    city factor = 100 + floor(6020 / 60) = 200

No malformed map state is needed: the Settler can found on an own/unowned city tile while a
frontier BFC plot already carries strong culture from a neighboring rival. Creative's free
10 culture immediately establishes the new city's current range.

The correct post-founding empire average is now exactly:

    (100 culture-rate * 100 pressure +
     100 culture-rate * 200 pressure) /
    (100 + 100)
      = 150

but the stored activation snapshot remains 100.

DIRECT SAME-TURN SLIDER CONSEQUENCE
-----------------------------------
Current SAS retains K-Mod's `AI_doCommerce()` pressure terms:

    iIdealPercent += (iAverageCulturePressure - 100) / 10;
    iIdealPercent = max(iIdealPercent,
            (iAverageCulturePressure - 100) / 5);

The inherited commerce increment is 10 and current SAS contains no XML override of that BtS
default.

Under the neutral conditions above:

    stale cached pressure 100:
        ideal = 0
        rounded target = 0% Culture

    correct live pressure 150:
        +5 from /10 term
        max(5, 10 from /5 term) = 10
        10-point increment preserves 10
        ordinary cap 20 preserves 10
        target = 10% Culture

This is an actual state mutation, not just a valuation perturbation. `AI_doCommerce()` first
zeros flexible Culture/Espionage and then installs the calculated Culture target. Immediately
after `AI_doTurnPre()`, `CvPlayer::doTurn()` runs `doGold()`, `doResearch()` and
`doEspionagePoints()`, so the stale 0% rather than correct 10% culture allocation changes the
same player's current-turn economy/research allocation as well.

A kept city acquisition is a second producer of the same root and can make the other empire
averages especially different through captured buildings, population, specialists and yield /
commerce modifiers. It does not need its own finding number: all members are populated by the
same `AI_calculateAverages()` snapshot and share the same missing post-unit refresh boundary.

HISTORY / ANCESTRY
------------------
BtS/Civ4CE already contains the shared average cache for yield, commerce, Great-People and
commerce-exchange values. Its getters are lazy per-game-turn caches: when
`m_iAveragesCacheTurn` differs from the current turn, the first getter calls
`AI_calculateAverages()`.

K-Mod practical 56 / `2034aebc356ff90d54dd05effd2610154d5d887c` added
`m_iAverageCulturePressure` to that shared lazy cache. Its log says:

    Average (K-Mod) culture pressure factor is now cached (although it isn't being used yet).

Practical 57 / `cdfa07682c2a7a87a07bb384e21924345ba3f2d4` began using it in Culture
weighting. Practical 103 / `5750806e12638791999cb664ed82602822e05e6b` added the direct
culture-slider terms that current SAS still uses.

Practical 165 / `f2c41e4abdd234768bd14062c99c97c7c82a15e5` disabled the lazy getter refresh
checks and explicitly rebuilt averages in `AI_doTurnPre()`:

    AI_calculateAverages();
    AI_updateStrategyHash();
    AI_updateVictoryStrategyHash();
    AI_updateGoldToUpgradeAllUnits();

K-Mod's sequential player schedule already placed `doTurnUnits()` at activation and ordinary
`CvPlayer::doTurn()` when the player turn deactivates. The initial K-Mod history itself shows
that ordering. Therefore practical 165's explicit average refresh occurred AFTER the AI's unit
moves and same-turn city founding/capture.

Practical 170 / `8bd260f741645fd4b5410e7470519f7a56be5ecb` is titled:

    Cache data is now updated when player is initialized, and when turn becomes active.
    (this fixes the problem of cache not being initialized on the first turn, and it also
    fixes the problem of cache data being 1 turn old.)

The same diff adds `updateCacheData()` at turn activation, puts `AI_calculateAverages()` inside
it, and removes `AI_calculateAverages()` from `AI_doTurnPre()`. This correctly solves
between-turn age / first-turn initialization but creates the current post-unit mutation gap:
the averages are now guaranteed to be from before the active unit phase and are not rebuilt
before later player-AI decisions.

Final K-Mod 1.46, Base AdvCiv 1.14 and current SAS all retain this activation-time average
refresh and the K-Mod culture-slider consumer. Classification for the deterministic current
witness: K-Mod practical-170 cache-relocation regression, inherited by AdvCiv and SAS, on top
of a BtS-origin shared average-cache family plus K-Mod's culture-pressure extension.

SEMANTIC DEDUP
--------------
Album and Known-Issues searches are clear.

- The earlier `AI_doCommerce` SAS-delta control only proved that calling the stored
  `AI_averageCulturePressure()` accessor once and reusing the local value is equivalent to
  calling that same non-refreshing accessor twice. It explicitly did not inspect the lifetime
  of `m_iAverageCulturePressure` itself.
- F499 is stale `m_iAvailableIncome` across an event-driven GPT change and immediate
  `AI_doCommerce`; different stored object, producer and repair boundary.
- F513 is the separate AdvCiv `m_neededExplorersByArea` activation snapshot.
- F514 was also exposed by practical 170 but is an internal cache dependency reversal:
  victory stage is calculated before current strategy. Fixing victory/strategy order leaves
  `AI_calculateAverages()` before units and does not repair F518.
- F515 owns `m_aeAICitySites` / FoundValue invalidation after founding/razing. The same city
  founding can be a producer for both, but the stored object and consumers are independent:
  refreshing FoundValue sites does nothing to current empire averages, and vice versa.
- The earlier UWAI city-set findings concern UWAICache asset/geometry wrappers, not
  `CvPlayerAI` economy-average state.

No existing KI owns this current `AI_calculateAverages` post-unit city-set lifetime root.

REPAIR BOUNDARY
---------------
Do not merely fix the culture slider locally; that would leave the other members from
`AI_calculateAverages()` stale.

Two principled directions are:

1. keep practical 170's activation refresh for first-turn/unit-phase users, but refresh the
   average family again before the later player-AI consumers (restoring practical 165's
   post-unit freshness contract); or
2. add a dirty/invalidation contract for city-set / relevant city-average mutations and make
   the first later consumer rebuild the whole average family.

A second unconditional full `AI_calculateAverages()` at the start of `AI_doTurnPre()` is the
simplest correctness reference but may duplicate an expensive culture-pressure city/plot/player
scan every turn. A dirty flag can avoid that cost, but its invalidation scope must cover all
inputs to all five stored aggregate families, not merely city count.

Regression tests should include:

1. Creative AI has one pressure-100 city at activation, founds a pressure-200 border city,
   then reaches `AI_doCommerce()` with expected live average 150 and 10% Culture target;
2. same fixture with no city founded remains pressure 100 / 0% Culture;
3. kept conquest with materially different city yield/commerce/GP modifiers updates the other
   `AI_calculateAverages` members before later research/commerce valuation;
4. city raze/no-net-new-city paths do not spuriously retain a transient acquired city in the
   rebuilt averages;
5. first-turn initialization and any unit-phase consumers that motivated practical 170 remain
   valid after repair.

PROMOTION
---------
F518 / KI#841 is CONFIRMED.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F518 / KI#841 -- CONFIRMED.
F519 / KI#842 -- UNUSED.
Latest physical checkpoint -- C031-WIP269.
No repository source file modified.

NEXT
----
Resume under F519 unused:
1. close F518 sibling scope for non-city-set mutations of the same averages rather than
   double-numbering them;
2. continue the remaining persistent `CvPlayerAI` / `CvPlayer.cpp` aggregate inventory,
   preferring stored state whose mutable producer occurs after activation but before a direct
   same-turn consumer;
3. keep practical-170 cache relocation siblings aggressively separated by stored object and
   repair boundary (F514 strategy/victory, F518 empire averages, existing F513 explorers).


================================================================================
C031-WIP270 APPEND -- 2026-09-03 -- F519 / KI#842 CONFIRMED
================================================================================

F518 SIBLING-SCOPE CLOSURE -- NON-CITY-SET MUTATIONS STAY UNDER F518
--------------------------------------------------------------------
Before spending another number, the remaining same-turn producers for the
`AI_calculateAverages()` family were challenged. City-set mutation remains the cleanest F518
witness, but it is not the only way the activation snapshot can become old.

In particular, Great-Person actions occur during `doTurnUnits()` after the activation-time
`AI_calculateAverages()` call and before the later `AI_doTurnPre()` / commerce-research
consumers. A Join changes specialist/output state; Construct can add a building with yield,
commerce or Great-People modifiers; Great Work can change culture-pressure inputs. Whenever
such an action changes an input to one of the stored empire averages, the old aggregate remains
until the next activation for exactly the same reason as the F518 founding witness.

These are additional PRODUCERS of F518, not F519 findings: the stored objects, stale lifetime,
consumer boundary and repair contract are all the same `AI_calculateAverages()` family. No
extra number is spent on them.


F519 / KI#842 -- ADVCIV PRACTICAL 2093 INITIALIZES A NEW CITY AS `CITYSAFETY_SAFE`
WITHOUT COMPUTING ITS REAL SAFETY, SO A SAME-TURN FOUNDED THREATENED CITY CAN BE TREATED AS
SAFE BY LATER UNIT AI
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- AdvCiv practical-2093 regression, retained by Base AdvCiv 1.14 and current
AdvCiv-SAS. BtS/Civ4CE and final K-Mod 1.46 do not contain AdvCiv's `AI_isSafe` /
`AI_updateSafety` city-safety cache, so this exact root is AdvCiv-specific.

The strongest witness is ordinary AI city founding during the land-unit pass. A freshly
created `CvCityAI` starts with the optimistic enum value `CITYSAFETY_SAFE`; neither `initCity`,
`found` nor `AI_cityCreated` computes its actual safety. A later Great-Person group in the same
land-unit pass hard-filters candidate cities through `AI_isSafe()` and can therefore Join,
Construct or Hurry in the new city even when an immediate `AI_updateSafety()` would classify
that city as threatened/evacuating and exclude it outright.

ROOT / CURRENT ORDER
--------------------
At turn activation current SAS computes the normal city-importance/safety bundle only for the
cities that already exist:

    CvPlayer::setTurnActive(true)
        -> CvPlayerAI::AI_updateCacheData()
            -> collect `AI_assetVal` for CURRENT cities
            -> AI_setCityValPercent(...)
            -> AI_updateSafety()

Later:

    CvPlayer::doTurnUnits()
        -> DOMAIN_LAND pass
            -> Settler group can found a city
                -> CvPlayer::found(...)
                    -> initCity(...)
                        -> CvCityAI constructor / init
                    -> AI_cityCreated(...)
            -> another land group can act afterward
                -> Great Person -> AI_greatPersonMove()

Current `CvCityAI` construction contains:

    m_iCityValPercent = 0;          // advc.139
    m_eSafety = CITYSAFETY_SAFE;    // advc.139

`CvCityAI::init()` then calls base city initialization, working-plot assignment, best-build
refresh and worker-count refresh. It does NOT call `AI_updateSafety()` and does not assign a
real city-value percentile.

The current `CvPlayer::found` / `CvPlayer::initCity` path likewise contains no city-safety or
city-percentile refresh. The generic AdvCiv city-created observer is only a UWAI notification:

    CvPlayerAI::AI_cityCreated(CvCity& kCity)
    {
        if (getUWAI().isEnabled() || getUWAI().isEnabled(true))
            uwai().getCache().reportCityCreated(kCity);
    }

So, unlike the cities present at activation, a city born during units enters the remainder of
that same turn with constructor defaults rather than a computed `advc.139` state.

CLEAN CURRENT-SAS NORMAL-GAME WITNESS -- ESCORTED SETTLER + GREAT PERSON
------------------------------------------------------------------------
Use an AI that already owns at least one city elsewhere. On another land area, have:

1. an escorted Settler group on a valid cached founding site;
2. a separate Great Scientist/Engineer/other ordinary Great-Person land group on the same
   tile (or otherwise zero/short path to the new city);
3. an enemy land stack on an adjacent tile, strong enough that the new city's live
   `AI_updateSafety()` comparison would fail the safe test once the city exists;
4. no enemy unit on the founding tile itself.

This is reachable under the real Settler code. `AI_found()` considers a Settler group safe when
its group can defend (or is invisible), and its enemy-unit exclusion is about the candidate
SITE itself. It does not impose a generic "no adjacent hostile unit" rule. Thus an escorted
Settler can legally found while a materially stronger hostile stack is adjacent.

When the city is created, the owner now has at least two cities, so a real
`AI_updateSafety()` would not take the one-city shortcut. It would calculate adjacent hostile
land attack strength and local defense strength. Choose ordinary strengths satisfying:

    iAttStrength * 2 >= iDefStrength

and the newly founded city is not safe (THREATENED or, under the additional evacuation
conditions, EVACUATING).

But no such update occurs. The new object simply keeps:

    m_eSafety == CITYSAFETY_SAFE

for the rest of the current unit pass unless some unrelated explicit refresh happens.

The land-pass ordering needed for the witness is ordinary and reachable: `doTurnUnits()`
iterates the player's existing selection groups for the DOMAIN_LAND pass. A Settler group can
therefore execute its Found mission before a separately existing Great-Person group later in
that same pass.

DIRECT SAME-TURN STATE-CHANGING CONSUMER -- `AI_greatPersonMove`
----------------------------------------------------------------
Base AdvCiv 1.14 and current SAS `CvUnitAI::AI_greatPersonMove()` begin the city target loop with
this hard gate:

    FOR_EACH_CITYAI(pLoopCity, kOwner)
    {
        if (!pLoopCity->AI_isSafe())
            continue;
        ...
        // Join / Construct / Hurry candidates
    }

The check precedes the candidate-value calculations. It is therefore not merely a safety score
modifier: correct unsafe state removes the city from the Join/Construct/Hurry action set.

Make the just-founded city the Great Person's only land-reachable owned city (for example, the
older city is on another landmass). If the Great Person is already on the founding tile,
`generatePath` to the new city succeeds trivially. Configure an ordinary high-value slow action
such as a legal Academy/other Great-Person Construct or permanent-specialist Join so that the
slow mission wins the existing mission comparison.

With the stale constructor state:

    AI_isSafe() == true
        -> new city enters slow-action candidate loop
        -> Join/Construct/Hurry candidate can win
        -> because the GP is already at the target, the corresponding MISSION_JOIN /
           MISSION_CONSTRUCT / MISSION_HURRY is pushed immediately
        -> the Great Person can be consumed in the actually threatened city that same unit pass

With a correct post-founding safety computation:

    AI_updateSafety()
        -> CITYSAFETY_THREATENED or CITYSAFETY_EVACUATING
        -> AI_isSafe() == false
        -> city is skipped before any slow-action candidate is considered
        -> that same GP action cannot be selected there

This is an immediate state-changing gameplay consequence, not a one-turn display/cache
approximation.

A second current-SAS consequence exists in the KI#204 Great-General birth hardening:
`CvPlayer::setCombatExperience()` now prefers cached-safe cities when selecting a Great-General
birth city. A combat later in the same unit phase can therefore also see a newly founded
threatened city as safe. That consumer is SAS-specific and is only extra scope evidence; the
inherited Great-Person hard filter above is the primary proof.

WHY THE EXISTING TACTICAL REFRESH HOOKS DO NOT REPAIR IT
--------------------------------------------------------
AdvCiv has tactical city-safety refresh hooks, but none establishes a fresh city's initial
state.

`CvPlayerAI::AI_attackMadeAgainst()` is deliberately one-directional. Its own comment says an
attack made from a city is unlikely to tip the city from safe to unsafe, and it calls
`AI_updateSafety(false)` only when the cached city is already NOT safe -- i.e. primarily to
notice possible relief after an attacker is killed. A newly founded city whose constructor says
SAFE therefore fails the hook's refresh condition.

`AI_humanEnemyStackMovedInTerritory()` reacts to human enemy movement and has its own local
transition tests; it is not a generic AI-city-created initialization hook.

A whole-current-core call inventory finds no `AI_updateSafety()` invocation in `initCity`,
`found`, city acquisition initialization or `AI_cityCreated`. The ordinary comprehensive
refresh remains the activation-time `AI_updateCacheData()` pass, which is already over when the
witness city is founded.

HISTORY / ANCESTRY -- PRACTICAL 2093 IS THE REGRESSION BOUNDARY
---------------------------------------------------------------
BtS/Civ4CE and final K-Mod 1.46 have no matching AdvCiv `AI_isSafe` / `m_eSafety` city-safety
cache, so they are negative ancestry controls for this exact defect.

Earlier AdvCiv `advc.139` used separate booleans. Immediately before practical 2093, the fresh
`CvCityAI` constructor/reset initialized them conservatively:

    m_bEvacuate = m_bSafe = false; // advc.139

Therefore an unrefreshed just-created city was NOT considered safe by `AI_isSafe()` consumers.
That was imperfect as an initialization policy, but it failed conservatively in the direction
needed by Great-Person safety filters.

AdvCiv practical 2093 / commit
`e3b5a1f536bb761f065c9cffaef06faac52b7a7d` is titled:

    Replace CvCityAI::m_bSafe and m_bEvacuating with an enum

Its constructor diff is exactly:

    - m_bEvacuate = m_bSafe = false; // advc.139
    + m_eSafety = CITYSAFETY_SAFE;    // advc.139

The same change rewrites the normal recomputation routine to start from SAFE and then derive
PERFECT / THREATENED / EVACUATING as appropriate, but it does NOT add a first recomputation on
city creation. Thus a refactor intended to represent the existing safety states also silently
changed the semantics of every freshly constructed, not-yet-refreshed city from "not safe" to
"safe".

Base AdvCiv 1.14 retains the practical-2093 constructor state and the inherited Great-Person
`AI_isSafe()` hard gate. Current SAS retains both. Classification: AdvCiv practical-2093
initial-state regression, inherited by SAS.

SEMANTIC DEDUP
--------------
Album and Known-Issues searches are clear.

- KI#172 / the existing SAS change to `AI_setCityValPercent` concerns the DIRECTION of the
  city-value percentile calculation. F519 concerns a newly created city that never receives a
  current percentile/safety calculation at all, and especially the practical-2093 optimistic
  safety default.
- KI#204 repairs CONSUMER-side Great-General issues: Great-General birth previously ignored
  safety and an inherited Great-Person/General construction test had an inverted safety
  condition. F519 instead proves that even a correctly written `AI_isSafe()` consumer can be
  fed an incorrect/default-safe producer state.
- F515 owns stale FoundValue / `m_aeAICitySites` after city topology mutation.
- F518 owns stale `AI_calculateAverages()` empire aggregates after unit-phase mutation.
  Founding is a shared producer event, but neither repair updates `CvCityAI::m_eSafety` or city
  value percentiles.
- The earlier tactical-safety album controls concern how existing cities are refreshed or how
  safety is consumed; none owns practical 2093's fresh-city constructor semantic change.

No existing finding owns the root:

    city born after activation -> no initial advc.139 refresh -> constructor says SAFE ->
    later same-turn safety consumer trusts SAFE

REPAIR BOUNDARY
---------------
Do not repair only `AI_greatPersonMove()`. Many consumers intentionally rely on cached
`AI_isSafe()`, including current Great-General placement and multiple movement/evacuation paths.
The producer state must become valid.

The correctness reference is to establish the `advc.139` city-importance/safety bundle after a
city-set mutation has completed enough initialization for its real inputs to exist:

1. after founding/acquisition is fully initialized, ensure the new city receives a real
   `AI_updateSafety()` before later same-turn AI consumers;
2. because `m_iCityValPercent` is an empire percentile and city-set changes alter the rank set,
   rebuild/dirty the city-value percentiles for all owned cities as part of the same event
   boundary, then refresh safety where percentile affects evacuation classification;
3. city loss/raze should likewise dirty/rebuild surviving-city percentiles rather than leaving
   ranks from the prior city set;
4. avoid calling the full calculation too early in `initCity`, before ownership, culture,
   units and acquisition/founding effects have reached their final state;
5. as a defensive fallback, a freshly constructed city should not advertise optimistic SAFE
   to gameplay consumers before its first real computation. Restoring a conservative default
   can protect the gap, but it is not a substitute for maintaining current percentiles/safety.

A dirty/event-driven bundle is preferable if unconditional whole-empire percentile + safety
recalculation on every city mutation is considered too expensive.

REGRESSION TESTS
----------------
1. AI owns another city, escorted Settler founds beside a stronger hostile land stack; after
   founding, the new city's safety must equal an immediate `AI_updateSafety()` result rather
   than constructor SAFE.
2. Separate Great Person on the founding tile acts later in the same land pass; when the new
   city is threatened, the city must be excluded from Join/Construct/Hurry candidates.
3. Identical unthreatened founding should obtain SAFE/PERFECT according to the real safety
   calculation, proving the repair is not merely "always unsafe for new cities."
4. Kept conquest / transfer producing a front-line city should establish current safety before
   later same-turn consumers.
5. City gain/loss should rebuild city-value percentiles for surviving cities, preserving
   KI#172's corrected "higher = more valuable" direction.
6. Current KI#204 Great-General safe-city birth selection should receive current safety rather
   than a constructor default.
7. Save/load and OOS behavior should remain deterministic; no serialized-layout change is
   required merely to add correct refresh/invalidation.

PROMOTION
---------
F519 / KI#842 is CONFIRMED.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F519 / KI#842 -- CONFIRMED.
F520 / KI#843 -- UNUSED.
Latest physical checkpoint -- C031-WIP270.
No repository source file modified.

NEXT
----
Resume under F520 unused:
1. finish the F519 sibling boundary for existing-city `m_eSafety` / `m_iCityValPercent`
   mutations and absorb them into F519 when they share the same advc.139 invalidation repair;
2. distinguish any independent city-safety producer whose repair boundary is not city-set /
   initial-state maintenance before spending F520;
3. return to the remaining persistent `CvPlayerAI` / `CvPlayer.cpp` aggregate inventory and
   continue the same mutable-producer -> stale member -> direct-consumer proof discipline.


================================================================================
C031-WIP271 APPEND -- 2026-09-03 -- F520 / KI#843 CONFIRMED
================================================================================

F519 SIBLING BOUNDARY -- CITY-SET PERCENTILES STAY UNDER F519
-------------------------------------------------------------
The immediate post-F519 sibling check confirms that city-set changes also make existing
`m_iCityValPercent` ranks stale until the next full player cache update. Gaining a city adds a
rank member; losing/razing one removes a rank member; the new city's constructor percentile is
0. Because `AI_updateSafety()` uses `AI_getCityValPercent()` when deciding EVACUATING versus
THREATENED, the percentile and safety state are one event-maintained `advc.139` bundle at this
city-set boundary.

That scope is already owned by F519's repair contract. It does not consume another finding:
F519's producer is city creation / city-set mutation after the activation refresh, and the
correct repair is to dirty/rebuild percentiles and establish the new/current city safety state
once the mutation is complete.

The next issue is different: an EXISTING city's local military inputs can change during the
unit phase even when the city set is unchanged. AdvCiv added a tactical refresh hook for this,
but the hook is explicitly asymmetric and misses a clean SAFE -> UNSAFE transition.


F520 / KI#843 -- ADVCIV `AI_attackMadeAgainst()` ONLY REFRESHES ALREADY-UNSAFE CITIES,
SO AN OWN ATTACK THAT REMOVES A DEFENDER CAN LEAVE AN EXISTING CITY STALE-SAFE FOR LATER
SAME-TURN UNIT AI
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- AdvCiv `advc.139` tactical city-safety invalidation defect, present in Base
AdvCiv 1.14 and current AdvCiv-SAS. BtS/Civ4CE and final K-Mod 1.46 have no matching AdvCiv
city-safety cache/hook. AdvCiv practical 1687 explicitly introduced the current
`AI_attackMadeAgainst` "possible relief" update philosophy and hard-coded the one-directional
condition "It's only going to get safer"; later AdvCiv refactors preserve that asymmetry.

This is independent of F519. F519 is a new-city INITIAL-STATE regression created by practical
2093. F520 uses a city whose activation safety state was correct, then becomes wrong because a
same-turn tactical mutation removes friendly defensive strength and the dedicated combat hook
refuses to refresh cached-SAFE cities.

CURRENT ORDER / INVALIDATION ASYMMETRY
--------------------------------------
At activation current SAS runs:

    CvPlayerAI::AI_updateCacheData()
        -> for every existing city
            -> AI_setCityValPercent(...)
            -> AI_updateSafety()

`AI_updateSafety()` bases SAFE versus unsafe in part on:

    iAttStrength = AI_localAttackStrength(city, ..., range 1, ...)
    iDefStrength = AI_localDefenceStrength(city, ownerTeam,
            DOMAIN_LAND, range 3, ..., bCheckMoves=true, ...)

and uses:

    bSafe = (iAttStrength * 2 < iDefStrength);

Friendly land units in range therefore contribute directly to cached city safety.

During the later unit phase, combat can remove one of those friendly units. AdvCiv practical
1687 added an immediate post-combat hook specifically because the cached city state may have
changed:

    GET_PLAYER(getOwner()).AI_attackMadeAgainst(*pDefender);

Current `AI_attackMadeAgainst()` scans owned cities around the attacked defender but contains:

    /* Unlikely that an attack from within the city will
       tip the scales from safe to unsafe */
    if (!kCity.AI_isSafe())
        kCity.AI_updateSafety(false);

So it deliberately repairs only cities whose OLD cache already says unsafe. That catches the
intended "possible relief" case when killing an enemy attacker makes a threatened city safer,
but it cannot detect the opposite transition even though an own attack can remove defensive
strength from the city.

CLEAN SAME-TURN NUMERIC WITNESS -- TWO DEFENDERS, ONE EQUAL ATTACKER
--------------------------------------------------------------------
Use an existing AI border city C whose activation-time safety computation sees:

- two friendly full-health land combat units A and B that have equal effective defensive
  strength S for C;
- one adjacent hostile full-health ordinary land attacker E with equal effective attack
  strength S against C;
- no other relevant nearby attackers/defenders; neutral enough promotions/terrain/defense that
  the equal-S construction holds (or any nearby integer strengths satisfying the same
  inequalities).

`AI_localDefenceStrength` applies AdvCiv's small multi-defender stack factor. With two defenders:

    iDefStrength = (2S * (75 + (2 - 1))) / 75
                 = 152S / 75
                 > 2S

while the single hostile attacker contributes:

    iAttStrength = S

Therefore activation correctly records:

    iAttStrength * 2 = 2S < 152S / 75
    -> CITYSAFETY_SAFE (or PERFECT if the optional perfect-safety condition also holds)

Now let unit A, in its own land selection group, attack adjacent E. Choose the ordinary combat
outcome in which A dies and E survives with more than half of its original effective strength.
This requires no abnormal state; it is a normal reachable combat result between comparable
units.

Inside `CvUnit::resolveCombat`, after A has become dead, AdvCiv immediately calls the attack
hook. A fresh `AI_updateSafety()` at that exact point would no longer count A because
`AI_localDefenceStrength` explicitly skips `kUnit.isDead()`. Only B remains, so approximately:

    fresh iDefStrength = S
    surviving E effective attack E' > S/2
    2E' > S
    -> city is NOT safe

The exact unit strength need not be identical after combat; any E' >= S/2 produces the same
flip. The defender E is adjacent to C, ordinary DOMAIN_LAND, able to attack, not
mostly-defensive and not standing in a city, so it passes the current
`AI_attackMadeAgainst()` eligibility checks and its scan reaches C.

But C still has its activation cache:

    kCity.AI_isSafe() == true

and therefore the hook executes NO `AI_updateSafety(false)` call. The correct SAFE ->
THREATENED/EVACUATING transition is missed immediately after the combat that caused it.

DIRECT SAME-TURN STATE-CHANGING CONSUMER
----------------------------------------
Use a separate ordinary Great-Person group that is still waiting to act in the same DOMAIN_LAND
pass and is in city C. The same inherited `AI_greatPersonMove()` hard gate used for F519 is a
particularly clean consumer:

    FOR_EACH_CITYAI(pLoopCity, kOwner)
    {
        if (!pLoopCity->AI_isSafe())
            continue;
        // consider Join / Construct / Hurry
    }

Make C the GP's only land-reachable owned city and give the GP a legal high-value slow action
(e.g. an Academy/other Construct or permanent-specialist Join) that wins its ordinary mission
comparison.

Stale path:

    activation says C SAFE
        -> A attacks and dies
        -> live state is unsafe
        -> AI_attackMadeAgainst sees cached SAFE and skips refresh
        -> later GP still sees AI_isSafe()==true
        -> C remains in Join/Construct/Hurry candidate set
        -> GP already in C can push MISSION_JOIN / MISSION_CONSTRUCT / MISSION_HURRY
           immediately and be consumed there

Correct path:

    after A dies, recompute C safety
        -> C becomes THREATENED/EVACUATING
        -> later AI_greatPersonMove hard-skips C
        -> that same GP action cannot be selected there

Thus the asymmetric tactical invalidation has an immediate same-turn gameplay effect. It is
not merely a cached diagnostic or a next-turn approximation.

WHY THE EXISTING HOOK IS NOT A DEFENSE AGAINST THE WITNESS
----------------------------------------------------------
The hook runs at the right time but checks the wrong OLD-state direction for this case.

`resolveCombat()` calls `AI_attackMadeAgainst()` after death has been established, so a fresh
local-defense calculation would already exclude the dead attacker A. The problem is not call
timing. It is the guard:

    if (!kCity.AI_isSafe())

which prevents the calculation precisely when the old cache says SAFE and a defensive
contributor has just disappeared.

The rough `bCheckMoves` implementation in `AI_localDefenceStrength` is also not an obstacle to
this witness: regardless of how current movement points are approximated, a dead unit is
explicitly skipped. The proof therefore does not depend on route/move-point edge behavior.

Other ordinary same-turn friendly-unit movements can plausibly create the same stale direction
by moving defensive strength away without combat. Those are additional manifestations of the
same local-safety invalidation family if proven; the combat-death witness alone is sufficient
because it passes through AdvCiv's dedicated refresh hook and demonstrates the asymmetry
without requiring a generic movement-callback argument.

HISTORY / ANCESTRY
------------------
BtS/Civ4CE and final K-Mod 1.46 do not contain AdvCiv's `AI_isSafe` /
`AI_updateSafety` cache, so this exact issue is AdvCiv-specific.

The AdvCiv safety cache (`advc.139`) already exists before practical 1687. Practical 1687 /
commit `ef52c71628f14cf9751a2246ef520fce41617c28`, titled:

    Improvements to AI city safety, stack strength evaluation.

strengthened the safety calculation and ADDED `CvPlayerAI::AI_attackMadeAgainst`. The
introducing diff documents its intended direction explicitly:

    /* May have to update city safety status after one of our units
       has attacked (possible relief). ... */

and then:

    if (!kCity.AI_isSafe()) // It's only going to get safer
        kCity.AI_updateSafety();

That assumption is false whenever the attacking unit itself was part of the city's defensive
strength and is removed (death is the cleanest case). Later AdvCiv changes refactor the scan
and enum representation but preserve the same one-directional guard and even the current
comment that safe -> unsafe is merely "unlikely."

Therefore the exact current defect is best classified as an AdvCiv `advc.139` invalidation
asymmetry, explicitly codified by practical 1687, inherited by Base AdvCiv 1.14 and SAS. This
is older and independently rooted from F519's practical-2093 constructor regression.

SEMANTIC DEDUP
--------------
Album and current Known-Issues searches are clear.

- F519 / KI#842: new city is born with optimistic `CITYSAFETY_SAFE` and receives no first
  computation. F520: an existing city's activation safety was CORRECT, then a friendly combat
  death changes its live defense and the tactical invalidator refuses SAFE -> UNSAFE refresh.
  Repairing new-city initialization alone does nothing to F520.
- KI#172: city-value percentile direction; no tactical local-defense invalidation.
- KI#204: consumer-side Great-General safety filters / inverted test; F520 proves a correct
  safety consumer can still receive stale producer state.
- F515/F518: different cached objects (`m_aeAICitySites`, empire averages) that can share a
  unit-phase event but not this local military-strength dependency.
- Earlier album safety controls discuss safety consumers and tactical approximations, but no
  numbered finding owns the practical-1687 one-way `AI_attackMadeAgainst` invalidation root.

No existing finding repairs:

    existing city correctly SAFE -> own attack removes defense -> dedicated hook sees OLD SAFE
    and skips -> later same-turn unit AI consumes stale SAFE

REPAIR BOUNDARY
---------------
The minimum correction for the proven combat path is to stop assuming the transition direction
inside `AI_attackMadeAgainst()`. For owned cities in the affected scan, recompute safety even
when the cached state is SAFE (or establish a precise condition proving that the attacking unit
could not have contributed to their local defense).

A broader robust design should treat city safety as dirty when relevant local attack/defense
inputs change during the unit phase, including friendly unit death/departure and hostile
movement/death. Then refresh before the first safety-sensitive consumer. That would avoid
trying to enumerate SAFE->UNSAFE and UNSAFE->SAFE cases separately.

If preserving the cheap tactical hook rather than a dirty system:

1. `AI_attackMadeAgainst` must cover both transition directions for nearby affected cities;
2. the scan should be based on geometry that can affect the same local-strength ranges used by
   `AI_updateSafety`;
3. ordinary non-combat defender movement should be reviewed separately, because no attack hook
   fires there;
4. do not fold F519's city-created initialization into a combat-only repair -- both producer
   boundaries need coverage.

REGRESSION TESTS
----------------
1. Existing city has two equal-strength friendly defenders and one equal-strength adjacent
   enemy; activation safety is SAFE through the multi-defender factor.
2. One friendly defender attacks that enemy, dies; enemy survives >50% effective strength;
   city safety must immediately become non-safe.
3. Same combat with enemy killed/relieved should still support the intended unsafe->safer
   refresh path from practical 1687.
4. Great Person in the city acts later in the same land pass; after the defender death, unsafe
   city must be excluded from Join/Construct/Hurry.
5. Current KI#204 Great-General safe-city placement should likewise see current safety after
   combat.
6. Non-combat friendly defender departure should be tested while evaluating whether the repair
   is generalized to a safety-dirty contract.
7. No synchronized-RNG/OOS behavior should depend on whether a diagnostic-only refresh occurs;
   the safety computation itself must remain deterministic.

PROMOTION
---------
F520 / KI#843 is CONFIRMED.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F520 / KI#843 -- CONFIRMED.
F521 / KI#844 -- UNUSED.
Latest physical checkpoint -- C031-WIP271.
No repository source file modified.

NEXT
----
Resume under F521 unused:
1. close the non-combat friendly-movement and hostile-movement siblings under F520 when they
   share the same local-safety invalidation repair rather than double-numbering them;
2. bound `m_iCityValPercent` to F519's city-set repair unless a genuinely different mutable
   producer / consumer contract appears;
3. return to the remaining persistent `CvPlayerAI` / `CvPlayer.cpp` member inventory for a new
   independent stored-state root.


================================================================================
C031-WIP272 APPEND -- 2026-09-03 -- POST-F520 SAFETY-SCOPE CLOSURE / F521 UNUSED
================================================================================

F520 SIBLING SCOPE -- ORDINARY UNIT MOVEMENT
--------------------------------------------
The immediate local-safety sibling sweep does not spend F521.

The F520 combat-death witness is the cleanest proof because AdvCiv's dedicated
`AI_attackMadeAgainst()` callback actually runs and demonstrates its one-way old-state guard.
But the same cached-object lifetime is broader:

- a friendly defensive group can simply move away from an existing city during the land-unit
  pass, reducing the live `AI_localDefenceStrength` input after the activation safety snapshot;
  no generic friendly-movement safety callback runs, so SAFE can remain stale;
- a reinforcing friendly group can move into range, increasing live defensive strength while a
  cached THREATENED/EVACUATING city remains stale in the opposite direction;
- hostile unit movement/death can likewise change the local attack-strength side; AdvCiv has a
  narrow human-enemy movement hook and the "possible relief" combat hook, which itself shows
  that tactical freshness is intended, but these are not a complete invalidation contract.

All of those manifestations share F520's stored object (`m_eSafety`), mutable local military
inputs and robust repair direction: dirty/recompute affected city safety when local
attack/defense state changes, or before the next safety-sensitive consumer. They must not be
split into extra finding numbers merely by choosing a different unit movement that changes the
same live strength balance.

F519's `m_iCityValPercent` city-set lifetime remains separate and already scoped there. No
independent non-city-set producer changes a city's relative empire asset percentile without
also changing the city/output valuation itself in a way that warrants a distinct proven
invalidation contract in this pass.

POST-SAFETY CURSOR
------------------
The persistent `CvPlayerAI` member inventory is now substantially exhausted:

- strategy / victory: F510-F512 and F514;
- averages: F518;
- available income / upgrade gold / bonus values: earlier F497/F499/F504 families;
- explorer targets: F513;
- city sites: F515;
- close-border / full attitude: F516/F517;
- city value / safety: F519/F520;
- unit-role counts: checked and closed as event-maintained controls;
- unit-class/combat enemy weights: checked and closed as the earlier own-unit baseline control;
- Great-Person weights: explicitly sampled heuristic cache;
- best-tech shortlist: synchronous-search / soft-modifier control;
- victory weights: explicit lifecycle refresh control;
- financial-trouble and production-dirty members: already owned by earlier findings/controls.

Accordingly the audit now returns to Queue 004's under-covered `CvPlayer.cpp` method/mutator
inventory rather than forcing further cache siblings.

CURRENT STONE
-------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` -- OPEN.
32/169 unique files complete.
F445 / KI#768 -- RETRACTED.
F446 / KI#769 through F520 / KI#843 -- CONFIRMED.
F521 / KI#844 -- UNUSED.
Latest physical checkpoint -- C031-WIP272.
No repository source file modified.

NEXT
----
Resume the mechanically ranked low-coverage `CvPlayer.cpp` ordinary-game functions under F521
unused. Prefer arithmetic, state-transition and ownership/lifecycle functions with shipped
callers; treat UI-only / scenario-only / explicit heuristic sampling as lower priority. Compare
current SAS to Base AdvCiv, K-Mod and Civ4CE when a candidate depends on ancestry.

===============================================================================
C031-WIP273 — QUEUE 004 CONTINUATION — POST-F520 MECHANICAL MUTATOR CLOSURES
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F445 / KI#768 remains RETRACTED.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Resume from WIP272 / F521 unused after leaving the city-safety cache family. Re-run the
mechanically under-covered ordinary-game `CvPlayer.cpp` arithmetic/mutator ranker, but
first reconcile each apparent low-mention target against the full album so old controls
are not accidentally rediscovered as new findings.

PRIOR-CONTROL RECONCILIATION
----------------------------
1. `calculateTotalExports` / `calculateTotalImports` are not new audit ground.
   C031-WIP166 already reconciled their Python/API semantics and current Info Screen use;
   no swapped-side or subject mismatch survived. Do not reopen merely because the
   mechanical ranker reports few local mentions.

2. `removeBuildingClass` is likewise already closed in C031-WIP194. Its ORDER_CONSTRUCT
   completion caller, one-instance removal semantics, Unofficial Patch ancestry and Palace
   transfer use were all reconciled. No F521.

CONTROL — `getCivicPercentAnger` TEAMMATE / UNMET-CIV FILTER IS INTENTIONAL
---------------------------------------------------------------------------
The current AdvCiv/SAS loop differs deliberately from K-Mod: it iterates alive players
known to our team, excludes only self, and therefore includes known teammates while
excluding unmet civilizations.

Reachable history makes the intent explicit. AdvCiv practical 2381,
commit 473d5c468d8e89cf217df9df8574cf276bf722f2, is titled:

    "Emancipation anger from teammates; none from unmet civs."

The patch replaces K-Mod's `kOther.getTeam() == getTeam()` exclusion with exactly the
current `PlayerIter<CIV_ALIVE,KNOWN_TO>` + self-only exclusion. The current implementation
therefore matches the authored rule rather than exposing an accidental team-role drift.
No F521.

CONTROL — POLLUTION / RESEARCH ARITHMETIC REMAINS PREVIOUSLY COVERED
---------------------------------------------------------------------
`calculatePollution` was already explicitly rechecked in WIP161 and WIP202. Its bad-health
sign convention is coherent with the city health APIs and current global-warming callers.
The only SAS-local changes in the current helper are static define caches; no arithmetic
semantic change was introduced.

`calculateResearchRate` is semantically identical to Base AdvCiv 1.14 in the current tree.
The nearby teammate-overflow receiver mismatch in `getResearchTurnsLeftTimes100` was already
preserved as a real generic/config-dependent source defect in WIP175, but current SAS masks
it because `RESEARCH_MODIFIER_EXTRA_TEAM_MEMBER=0` and both AI/human groundbreaking
penalties are disabled. The tooltip groundbreaking interaction was likewise already bounded
under current defines. No new F521 from this family.

CONTROL — PLAYER `BuildingOnlyHealthy` TOGGLE IS LIVE-READ, NOT A STALE HEALTH CACHE
------------------------------------------------------------------------------------
`CvPlayer::changeBuildingOnlyHealthyCount` initially looked suspicious because it changes a
player-wide health rule but only calls `AI_makeAssignWorkDirty()` rather than recomputing
all city health members.

The full callee/read chain closes the concern:

* `CvCity::isBuildingOnlyHealthy()` dynamically reads
  `GET_PLAYER(getOwner()).isBuildingOnlyHealthy()` before falling back to the city-local
  count;
* `CvCity::totalBadBuildingHealth()` dynamically returns 0 whenever that predicate is true;
* per-building bad-health access (`getBuildingBadHealth`) is dynamically masked the same way;
* health consumers therefore observe the changed player count without needing a stored
  bad-health rebuild.

The assignment dirtying is sufficient for population reassignment. This is a useful
negative cache-control and not F521.

CURRENT STONE AFTER C031-WIP273
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP273.
No repository source modified.

NEXT
----
Continue from F521 unused through genuinely low-semantic-coverage mutators rather than
already-reconciled arithmetic. Prioritize authoritative state whose setter has fewer
refresh/invalidation side effects than neighboring setters. Current ranked candidates
include research-selection state, free-promotion/event persistence, religion-spread and
trade-route gating counters, production/upkeep modifiers and other player-wide counters.
For each: prove a live consumer and missing refresh/invariant before promotion; dynamic
getter masking and deliberate sampled state are controls.

END C031-WIP273

===============================================================================
C031-WIP274 — QUEUE 004 CONTINUATION — THIN PLAYER-STATE LIFECYCLE CONTROLS
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

POST-WIP273 THIN-STATE CONTROLS
--------------------------------
1. Event-persistent free promotions close under the full application path.
   `applyEvent` first grants the event promotion to every currently matching unit and then
   calls `setFreePromotion(UnitCombat/UnitClass, promotion, true)`. The setter de-duplicates
   exact pairs and preserves the grant for future units. There is no existing-unit omission
   hidden behind the thin setter.

2. `changeNoForeignTradeCount` deliberately skips the expensive global trade-route rebuild
   while the player is in anarchy. This is not a missing permanent refresh: the anarchy
   transition handler `changeAnarchyTurns` calls both player `updateTradeRoutes()` and
   `GC.getGame().updateTradeRoutes()` whenever the anarchy boolean changes. Thus trade routes
   are rebuilt when anarchy ends, matching the source comment.

3. `updateMaxAnarchyTurns` initially appeared dangerously init-only because `m_iMaxAnarchyTurns`
   is derived from traits. The lifecycle closes the concern. `CvPlayer::processTraits` calls
   `updateMaxAnarchyTurns()` after processing trait effects, and live leader replacement
   `changeLeader` invokes `processTraits(-1)`, swaps the leader, then `processTraits(1)`.
   Therefore a live trait-set change refreshes the derived maximum-anarchy cache on both sides
   of the transition. No F521.

4. `setResearchingTech` remains a thin mirror of research-queue membership rather than an
   independent authority. `pushResearch`, `popResearch` and `clearResearchQueue` update it in
   lockstep, and duplicate queue entries are prevented by the existing `isResearchingTech`
   gate. No standalone stale-state root established.

5. `hasReadyUnit` / `hasBusyUnit` / `setAutoMoves` were reopened because they have near-zero
   semantic album coverage. Their K-Mod turn-control contract remains coherent in this pass:
   automated groups are intentionally excluded from manual-ready detection; `isBusy` already
   handles empty groups; and `CvGame::updateMoves` clears AutoMoves only after all automatic
   missions/automated-unit AI are no longer busy. No premature-turn-completion witness was
   established.

CURRENT STONE AFTER C031-WIP274
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP274.
No repository source modified.

NEXT
----
Continue F521 unused with the remaining low-semantic-coverage nontrivial helpers rather than
boilerplate counters. Highest-interest current targets: population/land authoritative score
mutators and their cached AI power consumers; corporation/religion count helpers feeding AI
spread decisions; `canForceReligion` / spy-border diplomacy eligibility; and player identity
or turn-control helpers where AdvCiv changed team/player semantics. Require an ordinary
state-changing consumer and current-data reachability before promotion.

END C031-WIP274

===============================================================================
C031-WIP275 — QUEUE 004 CONTINUATION — F521 TYPE-AWARE / LOW-COVERAGE CONTROL PASS
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Resume from WIP274 with the remaining mechanically low-semantic-coverage player helpers,
but aggressively reconcile every apparent candidate against the cumulative album before
spending a finding number. The current pass also switches from raw grep-based enum suspicion
to include-context/type-aware review, because AdvCiv's CoreAI overloads deliberately accept
both PlayerTypes and TeamTypes in several accessors.

RECONCILIATION — MECHANICAL RANKER REPEATED OLD GROUND
------------------------------------------------------
Several WIP274 priority labels proved broader than the semantic history:

* `changeTotalPopulation` / `changeTotalLandScored` were already explicitly rechecked as
  authoritative remove-old/change/add-new Asset/Power/Score bookkeeping. No F521.
* the area-local religion spread helper is already F451 / KI#774; do not rediscover its
  queued-missionary area-filter omission through the low-mention ranker.
* `canForceReligion` had already been challenged without a new live state-transition root.
* `canSpiesEnterBorders` is the inherited intended counterespionage-border orientation;
  movement denial and the spy-ejection consumer agree on the same subject/target contract.
* the teammate research-turn modifier receiver asymmetry remains a generic/configuration
  concern already preserved elsewhere; current SAS masks the needed per-member modifier
  difference because the relevant teammate and groundbreaking modifiers are disabled.

TYPE-AWARE CONTROL — `GET_TEAM(PlayerTypes)` IS NOT A PLAYER/TEAM-ID BUG
-------------------------------------------------------------------------
A renewed direct-cast/accessor scan briefly made `CvPlayer::canStealTech` look severe:

    GET_TEAM(eTarget).isHasTech(eTech)

where `eTarget` is a PlayerTypes. This is the exact false positive already rejected in
WIP152. `CvPlayer.cpp` includes `CoreAI.h`, whose `GET_TEAM(x)` macro dispatches to overloaded
`CoreAI::getTeam`; the PlayerTypes overload converts through TEAMID(ePlayer). The analogous
`canSpyDestroyProject` spelling is safe for the same reason. BtS/K-Mod spell the conversion
explicitly; AdvCiv deliberately changed the source spelling during the CoreAI accessor
refactor without changing semantics.

This control is important for the remainder of Queue 004: do not diagnose a numeric player/
team-ID collision from `GET_TEAM(variable)` until the variable's static type and the active
CoreAI overload are resolved.

CURRENT DIRECT CAST / PAYLOAD SWEEP — CLEAN SO FAR
--------------------------------------------------
The explicit `(TeamTypes)iData1` / `(TeamTypes)item.m_iData` sites reviewed in the high-risk
normal-game paths are encoded TeamTypes payloads rather than disguised PlayerTypes:

* `handleDiploEvent` JOIN_WAR / STOP_TRADING / SET_WARPLAN receives a target team in iData1;
  the surrounding player/member lookups consistently compare against that target team.
* TRADE_PEACE / TRADE_WAR / TRADE_EMBARGO store a target team in TradeData.m_iData and assert
  the MAX_CIV_TEAMS bounds before conversion in structural validity checks.
* the corresponding diplomacy text rows decode the same TeamTypes payload and resolve the
  target team's live leader only for display/icon selection.

No player/team enum-domain root survives this subset.

AUTHORITATIVE PLAYER COUNTER CONTROLS
-------------------------------------
The first zero/near-zero album-mention state-mutator pass also closes rather than producing
F521:

* `changeImprovementCount` is maintained directly by plot ownership/improvement transitions;
  AI improvement/technology valuation reads the current player count rather than a second
  cached mirror requiring invalidation.
* `changeNumNukeUnits` is maintained by unit init/death and team nuclear capability sums the
  current player counts. No separate stale team cache was found.
* total-land and the previously rechecked population/land score mirrors use their ordinary
  remove-old/change/add-new bookkeeping at ownership changes.
* the current SAS-vs-Base-AdvCiv executable-delta scan of lightly mentioned CvPlayer methods
  mostly resolves to already-owned SAS repairs, intentional static GlobalDefine/info caching,
  or semantic no-op cleanup; no independent F521 emerged from that delta set.

ADDITIONAL LOW-COVERAGE CONTROLS
--------------------------------
* `doChangeCivicsPopup`'s delayed reminder is an explicit AdvCiv feature: when revolution is
  temporarily impossible, the civic-availability reminder is intentionally postponed until
  revolution is possible again. The execution path still rechecks revolution legality.
* `markTradeOffers` has already been examined in the historical-fix/low-coverage matrix; no
  persistent trade-offer state defect was established.
* `updateTeamType` participates in the heavily audited player/team reassignment lifecycle;
  `CvInitCore::setTeam` updates init-core state and calls the player refresh before later team
  access. No new stale identity window has been proved here.

CURRENT STONE AFTER C031-WIP275
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP275.
No repository source modified.

NEXT
----
Continue F521 unused with a type-aware rather than grep-aware sweep:
1. finish direct enum casts / encoded payloads, especially mixed PlayerTypes/TeamTypes and
   building/unit/class ID domains not protected by CoreAI overloads;
2. continue genuinely low-coverage authoritative counters only where an external cached
   consumer exists (production/upkeep, religion/corporation/global state, identity setters);
3. cross-rank suspicious comments and AdvCiv responsibility-removal history against methods
   not already semantically owned by the album;
4. require an ordinary current-data producer plus a state-changing consumer before F521.

END C031-WIP275

===============================================================================
C031-WIP276 — QUEUE 004 CLOSURE — FINAL ZERO-MENTION / EXECUTION-CONTRACT SATURATION
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` is CLOSED for the planned deep-method re-audit.
32/169 unique files remain complete in the outer queue; the current re-audit cursor advances to Queue 005.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

POST-WIP275 EXECUTION-CONTRACT CLOSURES
---------------------------------------
1. `canSpyDestroyBuilding(PlayerTypes eTarget, BuildingTypes eBuilding)` has an unused
   `eTarget` parameter, but the live Destroy Building mission does not rely on that helper
   alone for target-city legality.

   `getEspionageMissionBaseCost` requires a concrete city and verifies
   `pCity->getNumRealBuilding(eBuilding) > 0` for both the generic and selected-building
   branches before producing a nonnegative mission cost. `doEspionageMission` then begins by
   re-running `canDoEspionageMission`, which re-runs that exact cost/legality chain, before
   it decrements the building. `CvPlayerAI::AI_espionageVal` independently applies the same
   city/building-presence gate. Current Destroy Building XML is live, but no stale/modal or
   wrong-owner execution hole survives this chain. The unused parameter is redundant API
   shape, not F521.

2. The apparent `AI_doCommerce -> AI_doCivics/AI_doReligion` late-policy slider bug was
   independently rediscovered, then semantically deduplicated to existing F501 / KI#824.
   F501 already owns the fixed turn-pre order, zero-anarchy Spiritual/Golden-Age producer,
   same-turn happiness/economic consequence, BtS ancestry and final-commerce-rerun repair
   boundary. Do not renumber this as F521.

3. State-religion production legality remains explicitly repaired by `advc.064d`.
   `setLastStateReligion` still calls `verifyCityProduction()` after changing religion and
   before the Python event. Later compact state-religion production-modifier setters are
   authoritative live modifiers; the production-legality responsibility was not lost by
   refactoring.

FINAL ZERO-MENTION CURRENT-SAS DIFFERENTIAL CHECK
-------------------------------------------------
A mechanical current-source parser identifies 600 out-of-line `CvPlayer` definition bodies
representing 589 unique method names. 164 parsed definition names have zero literal occurrence
in the cumulative album text. Literal absence is not itself lack of semantic coverage, so the
zero-mention set was cross-checked against Base AdvCiv 1.14 and then high-risk inherited changes
against K-Mod/history.

After stripping comments and whitespace, only THREE zero-mention current methods differ
semantically from Base AdvCiv 1.14:

* `addEspionageReminderMsg` — SAS hoists the WHITE color lookup into a static const;
* `getUnitArtInfo` — SAS hoists BARBARIAN_CIVILIZATION into a static const;
* `getStartOfTurnMessageLimit` — SAS hoists MESSAGE_LIMIT_WITHOUT_MPU into a static const.

All three are performance/static-lookup edits already covered by the global runtime-define
mutation control. None changes gameplay state or caller contracts.

HIGH-RISK INHERITED ZERO-MENTION ADVC DELTAS — CONTROLS
-------------------------------------------------------
The zero-mention methods that differ between K-Mod and Base AdvCiv were not dismissed merely
because current SAS matches Base. The higher-risk semantic samples close:

* `countNumCoastalCities` / `countNumCoastalCitiesByArea`: AdvCiv's parameterless
  `CvCity::isCoastal()` uses the default -1, which `CvPlot::isCoastalLand` resolves to the
  same `MIN_WATER_SIZE_FOR_OCEAN` define K-Mod passed explicitly. No water-size semantic
  regression.
* `invalidateCommerceRankCache(eCommerce)`: AdvCiv intentionally narrows invalidation to the
  commerce bucket just recomputed by `CvCity::updateCommerce(eCommerce)`. A full update loops
  every commerce type; no sibling rank remains valid across a changed value.
* `expireMessages`: the `getExpireTurn(isHuman() || isHumanDisabled())` change is explicit
  Rise & Fall / advc.700 policy so temporarily AI-controlled human players retain human-style
  message history. It is not accidental identity inversion.
* `getUnitArtInfo`: advc.001 deliberately redirects city-screen unit art to the selected
  city's owner. The later SAS change only caches the Barbarian civilization define.
* compact counter/setter changes (`setOverflowResearch`, `setConversionTimer`, free-unit and
  military-unit cost/support fields, state-religion production modifiers, score mirrors,
  upkeep legacy state) preserve inherited authoritative fields or UI dirties; their live
  gameplay consumers read those fields dynamically or are covered by the established
  production/economic refresh families. No independent stale derived state survived.

FINAL TYPE / PAYLOAD / SENTINEL CONTROLS
----------------------------------------
* The CoreAI `GET_TEAM(PlayerTypes)` overload false-positive is now a standing closure rule:
  `GET_TEAM(ePlayer)` converts through TEAMID and is not a raw enum-index collision.
* Direct TeamTypes casts in JOIN_WAR / STOP_TRADING / SET_WARPLAN and TRADE_PEACE / WAR /
  EMBARGO payloads were traced to fields whose producers actually encode team IDs.
* The late helper/sentinel pass found no new enum-vs-NULL or negative-sentinel execution bug
  beyond already-owned families (notably KI#74 in CvCityAI and earlier dormant espionage-unit
  helper debt).

QUEUE 004 CLOSURE DECISION
--------------------------
The stronger re-audit has now exercised:

* current-SAS vs Base AdvCiv executable deltas;
* K-Mod / Civ4CE ancestry for candidate semantic changes;
* persistent-player/cache lifetimes and same-turn producer/consumer ordering;
* player/team identity and encoded payload domains;
* arithmetic/divisor/sentinel/default-assert families;
* event, diplomacy, espionage, research, production, civic/religion and city ownership
  transitions;
* low-literal-coverage and zero-literal-coverage method inventory;
* mandatory semantic collision against the cumulative album before every late candidate.

No independent root survives for F521. Leaving F521 unused is the correct saturation result.

CURRENT STONE AFTER C031-WIP276
-------------------------------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` — DEEP-METHOD RE-AUDIT CLOSED.
32/169 unique files complete in the outer queue.
F446 / KI#769 through F520 / KI#843 CONFIRMED from the Queue-004 re-audit tranche.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP276.
No repository source modified.

NEXT
----
Advance to Queue 005 `CvGameCoreDLL/CvCityAI.cpp` and perform the same stronger deep-method
re-audit used for Queues 001-004. Treat the earlier File-005 review as a baseline, not closure:
reconcile its broadened existing roots (KI#213, KI#48.3, KI#306, KI#70, KI#74, KI#11), then
rank current methods by semantic delta, cache/state mutation, caller reachability and low prior
coverage. F521 / provisional KI#844 remains the next unused finding slot.

END C031-WIP276


===============================================================================
C031-WIP277 — QUEUE 005 DEEP-METHOD RE-AUDIT OPEN — FIRST SAS-LOCAL HELPER CONTROL
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains DEEP-METHOD RE-AUDIT CLOSED at WIP276.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` is now OPEN under the stronger deep-method/cache/order re-audit.
32/169 unique files remain complete while Queue 005 is open.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED from Queue 004.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

QUEUE-005 BASELINE / METHOD
---------------------------
The old file-oriented review of CvCityAI.cpp remains useful baseline evidence, especially its
broadening/reopening of KI#213, KI#48.3, KI#306, KI#70, KI#74 and KI#11. It is not treated as
saturation proof. Queue 005 is now being re-read with the stronger method used for Queues
001-004: parsed method inventory, current-SAS-vs-Base semantic ranking, mutable-state/cache
lifetime and same-turn ordering checks, exact caller/consumer tracing, current-data reachability,
ancestry, and mandatory semantic dedup before any new F-number is spent.

Current parser inventory: 99 out-of-line CvCityAI definition bodies / 95 unique method names.
The highest-interest under-covered current deltas are concentrated in the SAS-local fallback
helper, heavily rewritten AI_chooseUnit / AI_chooseProduction, citizen assignment/juggling,
and several production/process helpers.

CONTROL — `SAS_AI_findBestFallbackUnit` SUCCESS / SIEGE-CAP CONTRACT SURVIVES FIRST DEEP PASS
---------------------------------------------------------------------------------------------
This entirely SAS-local helper had essentially no literal Album coverage and feeds both the hard
CvCity::doTurn no-production safety net and AI_chooseUnit's concrete-unit replacement path, so it
was audited first.

The helper's output contract is internally coherent in the current tree:
* outputs start at NO_UNIT / NO_UNITAI;
* every candidate must pass canTrain, be a fighting land unit, and have one of the explicitly
  accepted offensive/defensive default UnitAI roles;
* `eSkipUnit` is excluded before bucket selection;
* siege / trebuchet-like gates are applied before both normal and absolute-cheapest candidates
  are recorded, so the cheapest emergency backup does not bypass a rejected siege category;
* the XML-cost cap is intentionally only a preferred-unit cap; the absolute-cheapest above-cap
  fallback is enabled only by callers that explicitly request that final safety net;
* return true requires both a concrete UnitTypes and UnitAITypes result.

A tempting multi-city cap-overrun concern is INTENTIONAL POLICY rather than a new root.
`AI_countUnitsByCombat*` and `AI_countTrebuchetsLike` deliberately count only completed owned
units, not units currently being trained. The source comment explicitly explains that counting
'makings' can create queue cancellation/reshuffle oscillation and accepts a little cap overshoot
as the tradeoff. Therefore simultaneous cities queuing siege beyond the completed-unit threshold
does not by itself violate the authored contract. Do not promote that smoothing choice as F521.

No success-without-pick, siege-category bypass, or caller legality mismatch has been established
from this helper so far. Preserve it as a Queue-005 control unless a later caller interaction
reveals a different root.

CURRENT STONE AFTER C031-WIP277
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP277.
No repository source modified.

NEXT
----
Continue Queue 005 with the largest/currently most semantic SAS-local surface: `AI_chooseUnit`,
then `AI_chooseProduction` and citizen assignment/juggling. Reconcile every apparent issue against
the old File-005 controls and the much larger cumulative Album before assigning F521.

END C031-WIP277


===============================================================================
C031-WIP278 — QUEUE 005 AI_chooseUnit DEEP PASS — SPY POLICY CONTROL / LIVE WORKER-CAP CANDIDATE
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED at this safety checkpoint.
No repository source files modified.

AI_chooseUnit CONTROL — LAND-SPY WAR-PLAN / MAP GATE LOOKS REVERSED IN ISOLATION, BUT IS AUTHORED SAS POLICY
-----------------------------------------------------------------------------------------------------------
The concrete-unit SAS gate for `UNITAI_SPY` initially looked like a strong copy/paste defect.
Current code says that land Spies are valuable "especially on land maps", permits them in calm
conditions, but when `bWarPlan = AI_isFocusWar()` is true it rejects them specifically when the
map is NOT naval-heavy. The same condition was introduced in practical 4957 for both Spy Sea and
land Spy; it is naturally intuitive for Spy Sea but looks backwards for a land unit.

The inherited higher-level production logic makes the contradiction look even stronger:
`AI_chooseProduction` deliberately gives Spy production +10 odds for
`WARPLAN_PREPARING_TOTAL` and +5 for `WARPLAN_PREPARING_LIMITED`, while those plans are chosen-war
plans and can make `AI_isFocusWar()` true before actual war. On an ordinary Pangaea map
(`bNavalHeavyMapname=false`), a calm city below its Spy need can therefore pass the inherited
war-preparation Spy roll and then have the concrete SAS gate reject the resolved `UNIT_SPY`.

However, the current authored KI#53 documentation explicitly records this exact resulting rule:

    Spies: ... Block if bAtWar || bEnemyStrong || bDanger;
    if bWarPlan && !NavalHeavy -> block
    rationale: "Don't stall land wars with spy spam"

That documentation is specific enough to establish an intentional SAS policy override rather
than an accidental Player/Team/map-orientation invariant. The nearby source comments are
internally confusing/stale (especially "useless or ineffective at land warfare" inside the land
Spy block), and the policy deliberately overrides AdvCiv's inherited preparing-war Spy boost,
but the audit must not renumber an authored tuning choice merely because the older caller points
in the opposite strategic direction. No F521 from this branch.

LIVE F521 CANDIDATE — AREA-LOCAL WORKER FLOOR CAN BE VETOED BY AREA-DEPENDENT CAP APPLIED TO AN EMPIRE-WIDE WORKER COUNT
----------------------------------------------------------------------------------------------------------------------
The Worker branch exposes a materially different contract mismatch.

Current production establishes an explicit AREA-LOCAL minimum before ordinary priorities:

    iExistingWorkers = AI_totalAreaUnitAIs(kArea, UNITAI_WORKER)
    iMinimumAreaWorkers = AI_getSASMinimumAreaWorkers(kArea)
    if (!bDanger && iExistingWorkers < iMinimumAreaWorkers)
        AI_chooseUnit(UNITAI_WORKER, 100)

The shared helper is intentionally area-local. For the primary area it applies the current
tunable 200%-per-city floor with the signed reserve (-1 by default); secondary areas are bounded
by their own calculated demand and may fall to zero when truly complete.

But the later concrete `AI_chooseUnit(UnitTypes, UnitAITypes)` Worker gate uses a GLOBAL count:

    iMaxUnits = era-decayed empire cap (~2.5 * total cities, floor 60%)
    iMaxUnits = max(iMaxUnits, AI_getSASMinimumAreaWorkers(getArea()))
    iTotalUnitAIs = AI_totalUnitAIs(UNITAI_WORKER)   // empire-wide, includes training
    if (iTotalUnitAIs >= iMaxUnits)
        return false

The attempted synchronization is dimensionally inconsistent: an area-specific minimum is used to
raise a cap that is compared against the entire empire's Worker population. The effective empire
cap therefore depends on WHICH AREA'S CITY happens to call the function.

Clean current-default shape under final challenge:
* 10-city AI empire in the Future era.
* Primary land area has 9 cities; one secondary landmass has 1 city and real Worker work.
* Current primary floor = 2*9 - 1 = 17 Workers.
* Current era-decayed generic empire cap = 2.5*10*60% = 15 Workers.
* Primary-area production is allowed to rebuild up to 17 because its concrete call executes
  max(15, primaryMinimum=17).
* Put those 17 existing/queued Workers on the primary landmass and none on the secondary one.
* The secondary area's `AI_neededWorkers` is positive, so its shared minimum is 1 and the
  high-level area-floor branch deterministically asks for a Worker with iOdds=100.
* In the concrete secondary-area call, however, max(15, secondaryMinimum=1) is only 15.
  `AI_totalUnitAIs(WORKER)` is the empire-wide 17, so the request is rejected before the later
  area-usefulness gate can approve the genuine local shortage.

This is stronger than a heuristic difference: the same shared minimum that primary-area cities
were allowed to build can make a secondary area's own minimum impossible to rebuild. The final
area-usefulness Worker check does not repair it because it runs after the global-cap return; if
reached, it would correctly see `AI_neededWorkers(area) > AI_totalAreaUnitAIs(area)` and allow the
Worker.

HISTORY BOUNDARY SO FAR
-----------------------
Practical 4957 (`fe58e134631681ba9e79e481dd29a761b8820696`) introduced the generic empire-wide
Worker production cap in the large SAS AI_chooseUnit production/scrapping rewrite.

Practical 5916 (`9f18ea297837942da844afac467278d9927d1dc7`), titled "Ensure AI rebuilds a minimum
worker force...", introduced `AI_getSASMinimumAreaWorkers` and the explicit area-local production
floor, but left the concrete global Worker cap in place. This appears to be the birth of the
cross-layer contradiction.

Practical 6093 (`b3b83cf78fc58a509bc639573445452f0903e9b1`) strengthened KI#198's primary-area floor
and explicitly tried to keep the generic Worker cap from vetoing it by adding:

    iMaxUnits = max(iMaxUnits, AI_getSASMinimumAreaWorkers(getArea()))

That fixes a same-area late-era floor/cap collision only while the caller's area supplies the
largest relevant minimum. It does not reconcile an empire-wide count with multiple area-local
minimums, and in the clean 9+1-city example it creates two different effective empire caps (17
from a primary-area city, 15 from a secondary-area city).

DEDUP / CLASSIFICATION STATUS AT THIS SAFETY CHECKPOINT
-------------------------------------------------------
This does NOT currently look like existing F006 / KI#213. F006 is the legacy city-best-build vs
custom Worker-evaluator split that can make `AI_neededWorkers` falsely report no demand. The new
candidate assumes the area demand/minimum is CORRECT and demonstrates that a later independent
concrete production cap vetoes it. Repairing the demand source under KI#213 would not remove this
mismatch.

The earlier Queue-001 re-audit called the KI#192/KI#198 shared minimum coherent after checking the
helper and its production/usefulness/scrap consumers, but did not inspect this later CvCityAI
empire-wide cap interaction. No cumulative Album wording found so far owns the exact
"area-local minimum vs empire-global cap" root.

F521 remains deliberately UNUSED until the following are finished:
1. final current-data arithmetic/caller proof including the high-level iOdds=100 -> concrete call;
2. verify no Worker transport/reassignment mechanism constitutes an immediate guaranteed repair
   of the area-local production contract (it need not if no transport is available, but trace it);
3. exact semantic collision against KI#198/F006 and other Worker production findings;
4. finish ancestry around practicals 5916/6093 and check for a later authored statement that
   explicitly accepts this cross-area cap behavior;
5. establish the narrow repair invariant before promotion.

CURRENT STONE AFTER C031-WIP278
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP278.
No repository source modified.

NEXT
----
Finish the Worker area-floor/global-cap candidate before spending F521. If it survives, promote
one SAS cross-layer Worker-cap root with practical 5916 as the likely birth and practical 6093 as
an incomplete attempted synchronization; then continue the remaining `AI_chooseUnit` role/cap
surface. If it fails, record the control and continue without consuming F521.

END C031-WIP278


================================================================================
C031-WIP279 — QUEUE 005 DEEP RE-AUDIT — KI#198 REOPENED / WORKER AREA-FLOOR VS GLOBAL-CAP REPAIR GAP
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

KI#198 REOPENED/BROADENED — AREA-LOCAL LAND-WORKER MINIMUM CAN BE VETOED BY THE EMPIRE-WIDE CONCRETE WORKER CAP
--------------------------------------------------------------------------------------------------------------
Status: CONFIRMED current SAS implementation gap, but classified under existing KI#198 rather than a new F521 root because it violates KI#198's own explicit cross-subsystem synchronization contract.

ROOT / CURRENT ORDER
--------------------
`AI_chooseProduction` correctly establishes an area-local standing Worker floor before ordinary priorities:

    iExistingWorkers = kPlayer.AI_totalAreaUnitAIs(kArea, UNITAI_WORKER);
    iMinimumAreaWorkers = kPlayer.AI_getSASMinimumAreaWorkers(kArea);
    if (!bDanger && iExistingWorkers < iMinimumAreaWorkers)
        AI_chooseUnit(UNITAI_WORKER, 100);

`AI_getSASMinimumAreaWorkers` is intentionally area-local. The primary area receives the XML-tuned standing force; secondary areas remain demand-driven and can legitimately return zero once complete.

The later concrete `AI_chooseUnit(UnitTypes,UnitAITypes)` Worker gate instead combines an area-local minimum with an empire-global count:

    iMaxUnits = era-decayed generic empire Worker cap;
    iMaxUnits = max(iMaxUnits, kPlayer.AI_getSASMinimumAreaWorkers(getArea()));
    iTotalUnitAIs = kPlayer.AI_totalUnitAIs(UNITAI_WORKER); // empire-wide, includes training
    if (iTotalUnitAIs >= iMaxUnits)
        return false;

That synchronization is dimensionally incomplete. The same empire has a different effective global cap depending on which land area's city is making the call.

DETERMINISTIC CURRENT-DEFAULT WITNESS
-------------------------------------
Use a Future-era AI with 10 cities:
* 9 cities on the primary land area;
* 1 city on a disconnected secondary land area with real Worker work and no Worker.

Current default arithmetic:
* generic empire Worker maximum before the area floor: 2.5 * 10 = 25;
* Future-era decay clamps at 60%, so generic cap = 15;
* primary 9-city Worker floor with current 200% and -1 reserve = 17.

A primary-area city therefore uses:

    max(globalCap 15, primaryAreaMinimum 17) = 17

and can legitimately rebuild/retain the primary standing force up to 17. Worker scrapping is also area-aware and refuses to scrap when the primary area's Worker count is at or below its local minimum.

Now consider the secondary city's genuine local shortage:
* secondary area `AI_neededWorkers` is positive;
* `AI_getSASMinimumAreaWorkers(secondary)` = 1;
* area Worker count = 0;
* `AI_chooseProduction` deterministically asks for `UNITAI_WORKER` with odds 100.

The concrete call then uses:

    max(globalCap 15, secondaryAreaMinimum 1) = 15
    empire AI_totalUnitAIs(WORKER) = 17

so `17 >= 15` rejects the Worker before the later central area-usefulness gate is reached. If that later gate were reached, it would correctly see `AI_neededWorkers(area) > AI_totalAreaUnitAIs(area,WORKER)` and approve the local shortage.

The result is therefore not merely a soft tuning disagreement: Workers that the primary-area KI#198 floor is explicitly allowed to create/protect can make a secondary area's own positive local minimum impossible to rebuild through the city production path that requested it.

NO GUARANTEED TRANSPORT SELF-HEAL
---------------------------------
Current overseas Worker transport logic is useful but contingent:
* Settler transports can carry/reload Workers when a destination area has a positive `AI_neededWorkers` deficit;
* destination-aware unload logic avoids excess cargo and can service an island when a suitable transport/mission exists;
* completed-area Worker rescue handles the opposite zero-demand case.

None of those paths guarantees an immediate Worker for a newly founded/disconnected secondary area. A supported state with no available Worker transport remains possible. The explicit city-side local production floor is therefore a real producer contract and cannot rely on a future transport being available to repair this veto.

SAME-FILE NEGATIVE CONTROL — WORKER SEA ALREADY USES THE CORRECT GLOBAL-CAP/LOCAL-DEFICIT SHAPE
-----------------------------------------------------------------------------------------------
The current `UNITAI_WORKER_SEA` concrete gate has the same conceptual problem and already contains the appropriate solution pattern:

    iTotalUnitAIs = kPlayer.AI_totalUnitAIs(UNITAI_WORKER_SEA); // global
    iLocalAvailableSeaWorkers = ... current city's primary/secondary water areas ...;
    iLocalNeededSeaWorkers = AI_neededSeaWorkers();
    bLocalSeaWorkerDeficit = (iLocalNeededSeaWorkers > iLocalAvailableSeaWorkers);
    if (iTotalUnitAIs >= iMaxUnits && !bLocalSeaWorkerDeficit)
        return false;

Its source comment explicitly explains why: the empire-global Work Boat cap was reached elsewhere even though the current city's own water areas still needed a Work Boat. The cap remains for idle/excess boats, but a real local deficit bypasses it.

Land Workers need the analogous invariant. The current land branch instead performs an unconditional global-cap rejection before its area-local usefulness/demand check.

HISTORY / AUTHORED CONTRACT
---------------------------
Practical 4957 (`fe58e134631681ba9e79e481dd29a761b8820696`) introduced the broad SAS concrete-unit production caps, including the empire-wide Worker maximum.

Practical 5916 (`9f18ea297837942da844afac467278d9927d1dc7`), 2026-06-05, is titled:

    Ensure AI rebuilds a minimum worker force

Its commit text says it adds "a shared AdvCiv-SAS minimum worker floor per area" and uses it "in both city production and worker scrapping guards" so AIs reliably rebuild Workers while keeping "production and scrap thresholds synchronized."

The current KI#198 documentation later makes the synchronization contract even more explicit:

    `AI_neededWorkers`, the relevant production ceilings and Worker-scrapping safeguards all respected the same minimum so one subsystem did not immediately undo another.

Practical 6093 (`b3b83cf78fc58a509bc639573445452f0903e9b1`) strengthened KI#198 and explicitly says the same minimum is synchronized through `AI_neededWorkers`, production ceilings and scrapping. The added concrete line

    iMaxUnits = max(iMaxUnits, kPlayer.AI_getSASMinimumAreaWorkers(getArea()));

is therefore an attempted implementation of that contract. It repairs the same-area late-era collision, but not cross-area empires because the compared Worker count is empire-wide while the floor used to raise the cap belongs only to the current area.

This is SAS-local; Base AdvCiv/K-Mod do not contain this SAS floor/cap combination.

DEDUP / CLASSIFICATION
----------------------
Do NOT allocate F521.

* F006 / KI#213 is a different Worker-demand architecture root: `AI_neededWorkers` can become falsely low because the legacy city best-build cache does not see jobs handled by the custom Worker evaluator. The present witness assumes demand/minimum is correctly POSITIVE and gets vetoed afterward by an unrelated concrete global cap.
* Nevertheless, the new cross-area veto is best classified as an incomplete KI#198 repair rather than an independent KI family. KI#198 expressly owns the standing Worker minimum and the requirement that its demand, production ceilings and scrapping safeguards agree. The narrow repair completes exactly that promise.

NARROW REPAIR INVARIANT
-----------------------
Preserve the empire-wide cap for excess Workers, but do not let it veto a current-area shortage below the shared local minimum. Conceptually:

    iTotalWorkers = AI_totalUnitAIs(UNITAI_WORKER);
    iAreaWorkers = AI_totalAreaUnitAIs(getArea(), UNITAI_WORKER);
    iAreaMinimum = AI_getSASMinimumAreaWorkers(getArea());

    if (iTotalWorkers >= iGlobalMax && iAreaWorkers >= iAreaMinimum)
        return false;

The exact implementation can retain the existing era-decayed global cap and logging. The important contract is the same one already implemented by Worker Sea: global excess limits apply only once the current locality's authoritative minimum/deficit has been satisfied.

A more global alternative would raise the empire cap to the sum of all current area minima, but that changes policy more broadly and is unnecessary for the narrow defect. A local-deficit bypass is simpler and follows the existing Worker Sea precedent.

CURRENT STONE AFTER C031-WIP279
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP279.
No repository source modified.

NEXT
----
Continue the remaining concrete `AI_chooseUnit` role/cap surface under F521 UNUSED. In particular,
challenge other empire-global civilian caps that sit before area-local demand/usefulness gates
(Explorer, Missionary, Spy) without assuming that the KI#198 Worker contract generalizes to them;
only promote a new root when an authored/local-demand invariant survives policy and dedup checks.
Then continue `AI_chooseProduction`, citizen assignment/juggling and the remaining high-delta
CvCityAI methods under the stronger deep-method approach.

END C031-WIP279


===============================================================================
C031-WIP280 — QUEUE 005 AI_chooseUnit DEEP PASS — FRESH-SNAPSHOT RECONCILIATION / FALLBACK CONTROLS
===============================================================================

STATUS
------
Authoritative current source for this tranche: user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.
The snapshot's own audit album already contains C031-WIP279 verbatim, so no handoff reconstruction or divergent merge was required.
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains DEEP-METHOD RE-AUDIT CLOSED.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files remain complete while Queue 005 is open.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
KI#198 remains reopened/broadened by WIP279's cross-area Worker-floor / global-cap mismatch.
No repository source files modified.

POST-WIP279 AI_chooseUnit — FINAL ROLE / SUBSTITUTION MECHANICS CONTROL
---------------------------------------------------------------------
The concrete overload stores `eChangedUnit` / `eChangedUnitAI`, performs the early strict-defender replacement/hijack, refreshes `pUnitInfo` when the concrete unit changes, and only THEN builds the naval / land-civilian UnitAI classification booleans. Therefore the earlier cautionary source comment saying those booleans are not recomputed is stale: the relevant downstream booleans are first computed from the FINAL role after replacement. Earlier siege/defender checks are consumed before replacement. The old File-005 control remains valid; no stale-final-UnitAI branch was established.

The replacement helper itself only returns a concrete trainable fighting LAND unit with an accepted offensive/defensive DEFAULT UnitAI. A replaced unit therefore cannot silently turn into a civilian/naval/air candidate and bypass those later category gates. The UnitAI-hijack fallback checks `getUnitAIType(...)` before changing the role. No illegal final role was established.

CONTROL — APPARENT WRONG NON-TREBUCHET COUNT ARGUMENT IS REDUNDANT AT THE CURRENT CALLER
----------------------------------------------------------------------------------------
A fresh type/parameter audit noticed that the helper parameter is explicitly named and consumed as:

    iSiegesAllNonTrebuchetsLike

whereas the strict-defender replacement caller supplies:

    iSiegesAll = AI_countUnitsByCombat(UNITCOMBAT_SIEGE)

rather than the dedicated:

    AI_countUnitsByCombatNoTrebuchetsLike(...)

The sibling `CvCity::doTurn` fallback caller DOES use the dedicated non-Trebuchet count, so this initially looked like a strong wrong-aggregate bug.

At the strict-defender caller, however, the same total-siege bound is already encoded one level earlier:

    bEnoughSiegeAlready = (iSiegesAll >= iNumCities)
    bAllowSiege = (!bEnoughSiegeAlready && !bNoNewSiegeRightNow)

and the helper immediately rejects every siege candidate when `!bAllowSiege`. Therefore whenever a non-Trebuchet candidate reaches the supposedly wrong `iSiegesAll >= iNumCities` helper comparison, `bAllowSiege` has already proved the opposite (`iSiegesAll < iNumCities`). The broad count passed into that particular parameter is redundant under this caller's current gates and does not itself create a live rejection. Do not promote the misleading parameter mismatch alone.

PRACTICAL-5329 REFACTOR SEMANTIC DELTA — REAL HISTORY DRIFT, CURRENT DATA MASKS THE UNIT CHOICE
-----------------------------------------------------------------------------------------------
Reachable commit diff `SASBranch_5329_8fb9a5bb93.diff` provides a useful historical control. Its commit message says the new shared `SAS_AI_findBestFallbackUnit` helper consolidates the duplicated `CvCity::doTurn` and `CvCityAI::AI_chooseUnit` loops while "preserving the existing caps and siege gating."

The old strict-defender replacement loop did this for every siege candidate:

    if (bEnoughSiegeAlready || bEnoughTrebsLikeAlready || bNoNewSiegeRightNow)
        continue;

with:

    bEnoughSiegeAlready = (iSiegesAll >= iNumCities)
    bEnoughTrebsLikeAlready = (iTrebsLike >= 3)

Thus three Trebuchet-like units blocked ALL further siege fallback choices, including Catapult-like units.

Practical 5329 rewrote this as two category controls:

    bAllowSiege = !bEnoughSiegeAlready && !bNoNewSiegeRightNow
    bAllowTrebuchetsLike = !bEnoughTrebsLikeAlready && !bNoNewSiegeRightNow

and the helper only applies `bAllowTrebuchetsLike` to Trebuchet-like candidates. Therefore, for e.g. five cities / three Trebuchets / fewer than five total siege, the current helper may consider a Catapult whereas the pre-refactor loop would have rejected every siege fallback. No later helper-history diff found intentionally documenting this as a strategic change; it is a genuine failure to preserve the exact old predicate promised by the refactor commit.

However this does NOT clear the current-gameplay finding threshold. Current shipped data make Trebuchets available at `TECH_ENGINEERING`; that same ordinary state also makes stronger no-resource offensive-default alternatives available (notably Crossbowman, cost 65, default `UNITAI_COUNTER`, versus Catapult cost 50). The helper is offense-default-only here and selects the highest cost/UU-inflated score within its generous era cap. Consequently admitting the Catapult under the refactored category split does not change the selected fallback in the ordinary current data state examined. Other current no-resource offensive lines similarly prevent a clean shipped "old chooses no/other unit, new chooses Catapult" witness.

Classification: preserve as a practical-5329 refactor-semantic / future-data robustness note associated with the KI#53.3 siege-production family, not F521. A future data change that removes the stronger concurrent alternative or changes the fallback score could expose it. The narrow historical-preservation repair, if desired, is to retain an explicit "Trebuchet cap reached => no siege fallback" gate for this caller rather than relying on the helper's category-specific parameters.

CURRENT STONE AFTER C031-WIP280
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
KI#198 remains reopened/broadened from WIP279.
Latest physical checkpoint: C031-WIP280.
No repository source modified.

NEXT
----
Leave the fallback helper / strict-defender substitution family unless a genuinely new current-data producer appears. Continue the large `AI_chooseProduction` current-SAS semantic surface under F521 UNUSED, prioritizing hard priority/order interactions, stale precomputed state consumed after queue/city changes, unit-role request versus concrete-veto contradictions, and any branch whose authored comments/defines establish a stronger invariant than strategic tuning. Then continue citizen assignment/juggling and the remaining high-delta CvCityAI methods with mandatory dedup against the old File-005 baseline and cumulative album.

END C031-WIP280


===============================================================================
C031-WIP281 — QUEUE 005 AI_chooseProduction DEEP PASS — KI#197.11 MULTI-WATER NAVAL-FLOOR REOPENING
===============================================================================

STATUS
------
Authoritative current source remains user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files remain complete while Queue 005 is open.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
KI#198 remains reopened/broadened from WIP279.
No repository source files modified.

POST-WIP280 AI_chooseProduction — CROSS-ROOT / DUPLICATE CONTROL
---------------------------------------------------------------
The resumed production-call audit immediately re-encountered the inherited late
`UNITAI_MISSIONARY_SEA` request under financial trouble. This is not a fresh Queue-005
root. The same ordinary `AI_chooseProduction -> AI_chooseUnit(UNITAI_MISSIONARY_SEA)`
caller is already part of F340's proof that the generic carrier UnitAI validation can
select ships whose SpecialCargo cannot carry the requested missionary. Keep the
caller under F340; do not renumber it merely because the production branch itself
looks odd in isolation.

KI#197.11 REOPENED/BROADENED — SECURED NAVAL PRODUCTION SHARE IS MEASURED PER WATER AREA AGAINST AN EMPIRE-WIDE DENOMINATOR, SO MULTIPLE DISCONNECTED SEAS CAN EACH FORCE THE FULL 'EMPIRE' NAVAL PERCENTAGE
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

STATUS / CLASSIFICATION
-----------------------
CONFIRMED implementation mismatch inside the recently introduced KI#197.11 secured-
primary-area naval-floor feature. Reopen/broaden KI#197.11 rather than consume F521:
this is the same production-share floor, same configuration contract and same narrow
repair surface.

AUTHORITATIVE DECLARED CONTRACT
-------------------------------
Practical 6389 / commit `679d3f6b96e2c7e7eae819e002aac7de7ca3cc1e`, titled for
KI#197.11, introduced the secured land/naval split. The accompanying XML does not
describe a separate quota for every ocean. It explicitly defines the secured naval
curve as a minimum share of EMPIRE production:

    "Minimum secured-profile naval production share by era."

and the secured land curve as:

    "Minimum share of empire base production ..."

The naval documentation further says that the defaults complement the secured land
values so the TOTAL Medieval+ floor remains 20 / 30 / 35 / 40 / 40 on naval-heavy
maps, merely shifting part of that existing total from land attackers into usable
naval logistics.

The KI#197.11 Known-Issues explanation uses the same semantics: after a primary
landmass becomes secure, "part of the released land share is redirected" to invasion
transports / escorts / attack ships, and the land + naval curves preserve the same
total floor. This establishes one empire-production percentage, not N independent
full percentages for N disconnected water areas.

CURRENT IMPLEMENTATION
----------------------
Practical 6389 replaced the old global land-only helper with:

    SAS_getMilitaryProductionCapacityPercent(kPlayer, pNavalWaterArea, ...)

The denominator is always every productive city in the player empire:

    FOR_EACH_CITY(...)
        iBaseProduction = getBaseYieldRate(YIELD_PRODUCTION)
        iTotalBaseProduction += iBaseProduction

For the NAVAL call, however, the numerator accepts a producing city only when:

    pLoopCity->waterArea(true) == pNavalWaterArea &&
    SAS_isNavalOffensiveProductionAI(eProductionAI)

Thus:

    navalCapacity(this sea)
        = base production currently making Assault/Escort/Attack ships on THIS SEA
          / base production of ALL productive cities in the empire

Production of the same tracked naval roles on every other disconnected sea is absent
from the numerator while remaining present in the denominator.

`AI_chooseProduction` then compares that per-sea value directly against the XML
`iNavalTargetPercent` and may force another ship whenever:

    iNavalCapacityPercent < iNavalTargetPercent

Every city therefore evaluates the full empire percentage separately for whichever
`pWaterArea` it happens to use.

CLEAN CURRENT-DEFAULT NUMERIC WITNESS
-------------------------------------
Use an ordinary naval-heavy-map AI whose secured primary land area touches two
DISCONNECTED useful water areas, East and West. This is a normal map topology: one
continuous landmass can have ports on water bodies that cannot reach each other.

Take a Modern/Future secured-profile state with the current 20% naval target and ten
productive cities, each supplying 10 base Production. Total empire base production is
therefore 100.

Have at least two port cities on East and two on West. Before the two later cities
choose production:

    E1 is producing UNITAI_ASSAULT_SEA / ESCORT_SEA / ATTACK_SEA: 10 base hammers
    W1 is producing one of the same tracked naval roles:             10 base hammers

The empire is already devoting:

    (10 + 10) / 100 = 20%

to exactly the secured naval roles. Under the declared empire-share contract, the
20% naval floor is satisfied.

Now E2 reaches the KI#197.11 block. Its helper call uses East as `pWaterArea`, so W1's
perfectly valid naval production disappears from the numerator:

    East reported naval capacity = 10 / 100 = 10% < 20%

The floor is therefore eligible and can force E2 into another tracked naval unit,
raising actual empire naval commitment to 30%.

When W2 later evaluates the same turn/production state, its West-only numerator sees
W1 but not E1/E2. In the simple equal-production shape it likewise remains below the
20% target and can force another tracked naval unit. The empire can now be committing
roughly 40% of its base production to the secured naval roles even though the XML
setting and KI#197.11 rationale call for a 20% empire share.

This is not an abstract many-ocean limit. Two disconnected coastal water areas are
sufficient. More independently productive seas can compound the same dimensional
mismatch further.

WHY THE PEACEFUL STOCK GUARD DOES NOT REPAIR THE SHARE MATH
-----------------------------------------------------------
The later practical-6391 stock guard is deliberately a separate anti-accumulation
brake. It stops the EXTRA secured naval floor only after the relevant water area has
reached:

    portCitiesInThisWaterArea * 5 tracked ships   [current default]

At the low-stock production-share fixture above, each two-port sea has a stock limit
of 10 tracked ships; one/few existing or queued ships are far below that threshold.
The stock guard therefore remains open while the duplicate production-share floor is
already forcing excess current production.

During actual war/assault projection the stock guard is intentionally disabled, so it
cannot serve as a general correction for the percentage denominator either.

The guard itself being water-area-local is reasonable because actual fleet stock and
reachable ports are geographically local. That does NOT imply the configured
production PERCENTAGE is local: its explicit contract says empire base production and
total secured floor.

BIRTH / ANCESTRY
----------------
This defect is born with KI#197.11 practical 6389. Before that commit the feature had
one global land-offense capacity numerator and one global empire-production
denominator. Practical 6389 generalized the helper so naval matches are restricted by
`pLoopCity->waterArea(true) == pNavalWaterArea` but retained the original empire-wide
denominator unchanged.

The same birth commit's XML and Known-Issues rationale establish that the new naval
percentage was intended to redirect part of the existing EMPIRE production share,
which makes the numerator/denominator scope mismatch locally provable rather than a
subjective naval-tuning preference.

Practical 6391 added the per-water stock guard after an empirical excessive-fleet case
but did not change the production-capacity numerator/denominator, so the multi-water
share defect remains in the current 20:00 source snapshot.

DEDUP / ROOT BOUNDARY
---------------------
No prior album entry was found for `SAS_getMilitaryProductionCapacityPercent` or this
per-water numerator / empire denominator mismatch.

Do NOT assign F521:
- KI#197.9 / .10 created and tuned the offensive production-capacity FLOOR itself;
- KI#197.11 changes that SAME floor after the primary landmass is secured, including
  the naval redirection whose percentage is wrong here;
- the narrow repair belongs in the same helper/feature and does not require changing
  any independent game-state architecture.

This is distinct from WIP279/KI#198: WIP279 concerns an empire-wide Worker COUNT cap
undoing an explicit AREA Worker minimum. The present issue is a dimensional mismatch
inside one percentage calculation for the secured naval floor.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
If `SAS_AI_CHOOSE_PRODUCTION_SECURED_NAVAL_OFFENSE_CAPACITY_*_PERCENT` is retained as
an EMPIRE production-share target (the current documentation), count all empire
production currently committed to the tracked secured naval roles in the numerator;
do not filter that production to the caller's one `pWaterArea` when comparing with the
empire-wide denominator.

Geographic water-area information can still be used AFTER the global shortage is
established to decide WHICH sea should receive the next ship and which role is most
useful there. The existing `SAS_rankNavalProductionUnitAIs`, port/stock guard and
`AI_chooseUnit` locality can provide that second-stage allocation.

Alternatively, if the desired design is genuinely one quota per disconnected sea,
then both numerator and denominator/configuration/documentation must be made local;
e.g. compare naval production on a water area against production capacity of cities
that can actually serve that water area. The current hybrid (local numerator / global
denominator while documenting an empire percentage) should not remain.

CURRENT STONE AFTER C031-WIP281
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
KI#198 remains reopened/broadened from WIP279.
KI#197.11 is now reopened/broadened by this multi-water naval production-share denominator mismatch.
Latest physical checkpoint: C031-WIP281.
No repository source modified.

NEXT
----
Continue `AI_chooseProduction` under F521 UNUSED. Finish the secured-primary-area / overseas /
Settler-production blocks for other hard scope/quantity mismatches, then the remaining large
production branch order. Keep same-feature defects under KI#197.* / KI#193.* / KI#198 rather than
renumbering. After `AI_chooseProduction` is saturated, continue citizen assignment/juggling and the
remaining high-delta `CvCityAI` methods under the deep-method inventory.

END C031-WIP281


===============================================================================
C031-WIP282 — QUEUE 005 AI_chooseProduction DEEP PASS — KI#194 SECOND-WATER-AREA REOPENING
===============================================================================

STATUS
------
Authoritative current source remains user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files remain complete while Queue 005 is open.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
KI#198 remains reopened/broadened from WIP279.
KI#197.11 remains reopened/broadened from WIP281.
No repository source files modified.

KI#194 REOPENED/BROADENED — CENTRAL SETTLER WATER-AREA REPAIR IGNORES `secondWaterArea()`, SO A COASTAL/ISTHMUS CAPITAL CAN STILL MISS WORTHWHILE ISLANDS REACHABLE THROUGH ITS OTHER SEA
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

STATUS / CLASSIFICATION
-----------------------
CONFIRMED incompleteness in the KI#194 repair helper. Keep this under KI#194 rather
than spend F521: the same central `SAS_getSettlerWaterArea` / Settler-site gate that
was added to stop reachable overseas sites from disappearing still drops another
ordinary reachable-water topology.

CURRENT WATER-AREA CONTRACT
---------------------------
For a land city/plot, `CvPlot::waterArea(bool)` scans adjacent passable water plots and
returns only the LARGEST adjacent water `CvArea` by tile count. `CvCity::waterArea`
just forwards to that helper.

AdvCiv/BBAI separately exposes:

    CvCity::secondWaterArea()
        -> CvPlot::secondWaterArea()

which returns the largest OTHER adjacent passable water area, explicitly excluding the
primary `waterArea()` result. Thus a normal coastal/isthmus/canal-style city can border
two disconnected usable seas. The engine models both; the second one is not an
unsupported geometry.

`CvPlayerAI::AI_getNumAdjacentAreaCitySites(...)` is likewise one-water-area-specific:
it counts cached city sites whose plots are adjacent to the ONE `CvArea` passed to it.
A valid site on the second sea is not recovered when the caller passes only the first.

KI#194 REPAIR BIRTH
-------------------
Practical 6066 / commit `6210495792304d589225485f197d8d8954f5fde4`, titled
"Fix AdvCiv-SAS Settler regressions blocking worthwhile island expansion (KI#179,
KI#194)", introduced:

    SAS_getSettlerWaterArea(CvCityAI const& kCity)

because the KI#181 final concrete Settler gate could report no water sites even though
existing Settler transports made a worthwhile island reachable.

The current helper is:

    pWaterArea = kCity.waterArea(true);
    if (pWaterArea != NULL && !team.AI_isWaterAreaRelevant(*pWaterArea))
        pWaterArea = NULL;
    if (pWaterArea == NULL)
    {
        pWaterArea = map.findBiggestArea(true);
        if (pWaterArea != NULL &&
            AI_totalWaterAreaUnitAIs(*pWaterArea, UNITAI_SETTLER_SEA) <= 0)
            pWaterArea = NULL;
    }
    return pWaterArea;

There is no `secondWaterArea()` test.

Practical 6066 deliberately changed BOTH `SAS_getSettlerBuildSiteStatus` (the final
concrete `AI_chooseUnit(UNITAI_SETTLE)` safety gate) and the surrounding broad
`AI_chooseProduction` water-site setup to use this shared result. The omission is
therefore centralized and deterministic rather than one caller being rescued by the
other.

CLEAN CURRENT-GAME REPRODUCTION
-------------------------------
Use a capital on a narrow isthmus whose city plot touches two passable disconnected
water areas:

    East = larger adjacent sea -> `waterArea(true)`
    West = smaller adjacent sea -> `secondWaterArea()`

Let both seas be strategically relevant. East contains no qualifying adjacent cached
city site. West contains a worthwhile revealed island site whose found value is above
`SAS_getSettlerBuildMinFoundValue`, and the player already owns a usable
`UNITAI_SETTLER_SEA` transport on West. Leave the capital's own land area without a
qualifying site, so the overseas opportunity is the reason to build the Settler.

Current execution:

1. `SAS_getSettlerWaterArea(capital)` obtains East because `waterArea(true)` chooses the
   larger adjacent sea.
2. East is relevant, so the helper keeps it and never enters the biggest-water
   fallback.
3. West / `secondWaterArea()` is never queried, despite the existing Settler transport
   and qualifying island there.
4. `AI_getNumAdjacentAreaCitySites(... East ...)` returns zero useful water sites.
5. `SAS_getSettlerBuildSiteStatus` therefore has no land site and no water site and
   returns false.
6. The broad `AI_chooseProduction` Settler-site quantities are built from the same
   helper, so the ordinary expansion logic is blind to West too.
7. Even if some other production path tries a concrete `UNITAI_SETTLE`, the final
   `SAS_isSettlerBuildWorthwhile` / site-status gate repeats the same omission and can
   reject it before the push.

The existing transport network and worthwhile island can therefore remain unused for
the same fundamental reason KI#194 was intended to fix: the production gate's chosen
water-area proxy does not represent all reachable adjacent overseas opportunities.

A second variant exists when the primary adjacent sea is irrelevant: the helper jumps
to the GLOBAL largest water area, not necessarily the city's valid second adjacent
sea. If the useful second sea is not that global largest area, it can still be missed.
The primary-relevant East/West fixture above is cleaner because it requires no fallback
subtlety.

WHY THIS IS KI#194, NOT F521
----------------------------
KI#194's documented root is specifically a mismatch where the new weak-site/Settler
production gate ignores a worthwhile island that existing Settler transports can
reach. Practical 6066's stated cure is a shared water-area helper so the broad
production logic and final concrete gate see the same reachable overseas opportunity.

This finding does not introduce a new cache, lifecycle or unrelated production rule.
It shows that the shared KI#194 helper still selects only one of multiple engine-
supported adjacent seas. The narrow repair modifies the same helper/site aggregation
and necessarily repairs both affected production layers together.

No prior cumulative album entry was found for a `secondWaterArea()` Settler-site
omission or this isthmus-capital variant.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not force one `CvArea*` to represent a potentially multi-water Settler opportunity.
For the build-worthwhile/site-status contract, evaluate all directly adjacent usable
water areas that the city can serve (at least `waterArea(true)` and
`secondWaterArea()`), and aggregate/count the qualifying adjacent-area sites without
double-counting the same site if it touches both seas.

For an inland city, preserve KI#194's existing-transport fallback to a non-adjacent
main sea; that is a different reachability case. A clean design would therefore be:

1. direct adjacent seas first (primary + second, each relevance/transport semantics as
   intended);
2. only when no direct sea can supply the opportunity, use the KI#194 inland/global
   fallback backed by existing Settler transport capacity.

If the production code still needs one water area for choosing/producing a
`UNITAI_SETTLER_SEA`, carry the best/selected opportunity's area separately from the
boolean "does any worthwhile reachable overseas site exist?" contract rather than
collapsing site existence to one arbitrary largest adjacent sea.

CURRENT STONE AFTER C031-WIP282
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
KI#198 remains reopened/broadened from WIP279.
KI#197.11 remains reopened/broadened from WIP281.
KI#194 is now reopened/broadened by the second-adjacent-water-area Settler-site omission.
Latest physical checkpoint: C031-WIP282.
No repository source modified.

NEXT
----
Leave the immediate Settler-water helper family unless another independent contract appears. Continue
the remaining `AI_chooseProduction` military/naval/air/project/process ordering and request-vs-veto
surface under F521 UNUSED, then citizen assignment/juggling and the other high-ranked `CvCityAI`
methods. Maintain strict dedup into KI#197.*, KI#193.*, KI#194, KI#198 and earlier F-roots when the
narrow repair is shared.

END C031-WIP282


================================================================================
C031-WIP283 APPEND -- 2026-09-03 -- QUEUE 005 DEEP RE-AUDIT -- KI#193.2 DUAL-COAST DOCKED-TRANSPORT GAP / F521 UNUSED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
KI#198, KI#197.11 and KI#194 remain reopened/broadened from WIP279/WIP281/WIP282.
No repository source files modified.

KI#193.2 REOPENED/BROADENED -- DOCKED ASSAULT TRANSPORTS AT A DUAL-COAST PORT ARE
MISSED WHEN THE TARGET SEA IS THAT PORT'S `secondWaterArea()`
--------------------------------------------------------------------------------
Status: CONFIRMED INCOMPLETE REPAIR OF EXISTING KI#193.2; do not consume F521.

ROOT / CURRENT CODE
-------------------
KI#193.2 added a narrow target-backed assault-transport production request. Once a
specific overseas city has been selected through the producing city's relevant water
area, the repair deliberately calculates how much usable assault cargo space already
exists before asking for another transport.

The current actual-unit capacity scan is:

    FOR_EACH_UNITAI(pLoopUnit, kPlayer)
    {
        if (pLoopUnit->AI_getUnitAIType() != UNITAI_ASSAULT_SEA)
            continue;
        CvCity const* pPortCity = pLoopUnit->getPlot().getPlotCity();
        if (!pLoopUnit->isArea(*pWaterArea) &&
            (pPortCity == NULL || pPortCity->waterArea() != pWaterArea))
        {
            continue;
        }
        iOpportunisticExistingCapacity += pLoopUnit->cargoSpace();
        iOpportunisticExistingTransports++;
    }

This correctly repaired the ordinary docked-ship problem: a sea unit in a city sits on
the city's LAND plot, so `pLoopUnit->isArea(*pWaterArea)` alone is false. But the port
fallback recognizes only the city's PRIMARY `waterArea()`.

The engine explicitly supports a second disconnected passable sea adjacent to a city:

    CvCity::waterArea(...)       -> CvPlot::waterArea(...)
    CvCity::secondWaterArea()    -> CvPlot::secondWaterArea()

`CvPlot::waterArea()` chooses the largest adjacent water area. `secondWaterArea()`
chooses the largest OTHER adjacent passable water area. `CvCity::sharedWaterArea()`
then explicitly considers all primary/second combinations when deciding whether two
cities share a usable water area. The second sea is therefore real engine topology,
not a diagnostic fiction.

The KI#193.2 target finder likewise already accepts a foreign coastal target whose
PRIMARY OR SECOND water area equals the concrete target water area:

    if (pTargetCity->waterArea(true) != &kWaterArea &&
        pTargetCity->secondWaterArea() != &kWaterArea)
    {
        continue;
    }

Thus the feature itself understands dual-coast connectivity on the TARGET side, while
its existing-capacity repair recognizes only the primary sea on the OWN-PORT side.

SHARED HELPER DOES NOT RESCUE THE COUNT
---------------------------------------
The following queued-capacity estimate uses inherited
`CvPlayerAI::AI_totalWaterAreaUnitAIs`:

    int const iWaterAreaTransports =
        kPlayer.AI_totalWaterAreaUnitAIs(*pWaterArea, UNITAI_ASSAULT_SEA);
    iOpportunisticTrainingTransports =
        max(0, iWaterAreaTransports - iOpportunisticExistingTransports);

That helper is inherited all the way from BtS/BBAI -> K-Mod -> AdvCiv. Its special
city scan exists precisely to add ships docked on land city plots and the owner's
ships currently being trained. But it too requires:

    pLoopCity->waterArea() == &kArea

and never tests `secondWaterArea()`.

Consequently the same dual-coast port is absent from BOTH the exact actual-cargo scan
and the queued-ship augmentation. The second calculation cannot accidentally self-heal
the first omission.

CLEAN CURRENT-GAME REPRODUCTION
-------------------------------
Use two owned coastal cities and one worthwhile nearby overseas target:

    P = producing city. Its primary/relevant sea is West.
    Q = an isthmus / dual-coast owned port.
        Q's larger adjacent sea is East  -> Q.waterArea() / primary.
        Q's smaller adjacent sea is West -> Q.secondWaterArea().
    T = revealed worthwhile enemy/Barbarian overseas city reachable through West.

Let the normal KI#193.2 conditions pass at P: P is safe, the land area is not
AREAAI_DEFENSIVE, the target passes long-term retention / distance / attacker-strength
checks, and P can train an assault transport.

Dock at Q one `UNITAI_ASSAULT_SEA` transport whose real cargo space is already enough
for the target-backed `iOpportunisticTargetCapacity`. A sea unit on Q's city plot can
leave Q into the adjacent West sea; the engine's second-water/shared-water topology is
exactly what represents that dual-coast connection.

Current evaluation at P:

1. `pWaterArea` is West.
2. `SAS_findNearbyOverseasConquestTarget(P, West, ...)` finds T.
3. Capacity scan reaches the docked transport at Q.
4. `transport.isArea(West)` is false because Q is a LAND city plot.
5. `Q.waterArea() == West` is false because Q's larger/primary sea is East.
6. The code never tests `Q.secondWaterArea() == West`, so the real cargo capacity is
   skipped.
7. `AI_totalWaterAreaUnitAIs(West, UNITAI_ASSAULT_SEA)` also ignores Q for the same
   primary-only city test.
8. `iOpportunisticExistingCapacity` can therefore remain below the target despite the
   already-usable docked transport.
9. `bOpportunisticAssaultTransportDemand` becomes true and P can order another assault
   transport solely because Q's connected port exposes West as its secondary sea.

This is the same duplicate-transport symptom that KI#193.2's docked-capacity follow-up
was specifically written to prevent, reached through an ordinary supported map shape
rather than malformed state.

DOCUMENTED CONTRACT / CLASSIFICATION
------------------------------------
KI#193.2 explicitly promises:

    "real transports at sea or docked in a connected port count by their actual cargo
     space, and ships currently being trained count by their expected cargo space"

and:

    "once enough existing or queued capacity is found, other cities do not duplicate
     the request"

A port whose `secondWaterArea()` is the selected target sea is a connected port under
the engine's own multi-water-area city topology. Therefore this is a direct incomplete
implementation of KI#193.2's existing capacity contract.

Birth/current lineage:
- practical 6065 / commit c54b256ece7dc339eb5420ad484bf9903c6c4022 introduced
  KI#193.2 and the current docked-capacity repair shape;
- the same birth code already used target `secondWaterArea()` when validating target
  reachability, but own docked capacity used only `pPortCity->waterArea()`;
- Base AdvCiv/K-Mod/BtS already have the primary-only
  `AI_totalWaterAreaUnitAIs` city augmentation, so the inherited helper shares the
  blind spot but did not create this SAS target-backed production promise.

Keep this under KI#193.2 rather than assigning F521. The narrow KI#193.2 repair owns the
exact selected-water-area capacity calculation and necessarily fixes this duplicate
request.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
For the target-specific actual-capacity scan, a docked transport belongs to the selected
`pWaterArea` when its port has that area as EITHER primary or second:

    bool bPortServesWater =
        pPortCity != NULL &&
        (pPortCity->waterArea() == pWaterArea ||
         pPortCity->secondWaterArea() == pWaterArea);

Then count actual cargo space when `unit.isArea(targetSea) || bPortServesWater`.

Queued capacity needs the same selected-sea semantics. Do NOT blindly change the shared
`AI_totalWaterAreaUnitAIs` helper to count every dual-coast docked/training ship in BOTH
water areas without auditing all callers: one flexible docked ship cannot satisfy two
simultaneous independent deficits at once, so unconditional double attribution can
create the opposite overcount. A safer KI#193.2-local implementation can count queued
assault transports from ports that serve the selected sea, or a future shared helper can
make its allocation semantics explicit.

SHARED-HELPER AUDIT NOTE -- NOT YET F521
----------------------------------------
`CvPlayerAI::AI_totalWaterAreaUnitAIs` is primary-water-only in BtS, K-Mod, Base
AdvCiv and current SAS. Current SAS has multiple callers that explicitly query/sum both
primary and second water areas for local Work Boat / Spy-transport decisions, so the
second water area is behaviorally meaningful. However the correct generic attribution
of a docked or queued ship at a dual-coast port is not simply "count it in both": that
could double-credit one flexible unit against simultaneous demands.

No independent generic helper root is therefore promoted from this observation yet.
Continue caller-by-caller and require a target/locality contract whose correct ownership
of the unit is unambiguous. KI#193.2 above meets that bar because one concrete target sea
has already been selected.

CURRENT STONE AFTER C031-WIP283
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
KI#198 remains reopened/broadened from WIP279.
KI#197.11 remains reopened/broadened from WIP281.
KI#194 remains reopened/broadened from WIP282.
KI#193.2 is now reopened/broadened by the dual-coast docked-transport capacity omission.
Latest physical checkpoint: C031-WIP283.
No repository source modified.

NEXT
----
Continue the inherited `AI_totalWaterAreaUnitAIs` caller inventory only long enough to
separate unambiguous selected-area defects from deliberate/flexible primary-area
allocation. Then leave this naval locality family and resume the remaining late
`AI_chooseProduction` military/naval/air/project/process branches, followed by citizen
assignment/juggling and other high-ranked `CvCityAI` methods. Keep F521 unused unless a
truly independent root survives current-data, intent and semantic-dedup challenges.

END C031-WIP283


================================================================================
C031-WIP284 APPEND -- 2026-09-03 -- QUEUE 005 DEEP RE-AUDIT -- KI#197.6 DUAL-COAST NAVAL-TRADE CAP GAP / F521 UNUSED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
KI#193.2 remains reopened/broadened from WIP283.
No repository source files modified.

KI#197.6 REOPENED/BROADENED -- CORRECTED NAVAL-TRADE `<3` SEA-UNIT CAP STILL
MISSES SHIPS DOCKED AT A PORT WHERE THE RELEVANT SEA IS `secondWaterArea()`
--------------------------------------------------------------------------------
Status: CONFIRMED INCOMPLETE REPAIR OF EXISTING KI#197.6; do not consume F521.

ROOT / CURRENT CODE
-------------------
KI#197.6 repaired AdvCiv's special naval-trade sea-explorer fallback. The inherited
branch deliberately ignores the ordinary sea-explorer target and may request a
`UNITAI_EXPLORE_SEA` with 25% probability while the relevant sea has a tiny navy. The
repair changed the old physical-water-area count into an explicit count of actual
DOMAIN_SEA ships, including docked ships and ships being trained, and added an
unrevealed-water prerequisite.

The current decision computes one concrete `pWaterArea` for the producing city and then
counts:

    FOR_EACH_UNIT(pLoopUnit, kMember)
    {
        if (pLoopUnit->getDomainType() != DOMAIN_SEA)
            continue;
        if (&pLoopUnit->getArea() == pWaterArea)
            iSeaUnitsAtSea++;
        if (pLoopUnit->getPlot().waterArea() == pWaterArea)
            iSeaUnitsIncludingDocked++;
    }

    FOR_EACH_CITY(pLoopCity, kMember)
    {
        if (pLoopCity->waterArea() != pWaterArea)
            continue;
        ... count trained sea UnitAIs ...
    }

    iWaterUnits = iSeaUnitsIncludingDocked + iSeaUnitsTraining;
    bEnoughWaterUnits = (iWaterUnits >= 3);
    bSeaExplorerEligible =
        (pWaterArea->getNumUnrevealedTiles(getTeam()) > 0 && !bEnoughWaterUnits);

Both the actual-docked and training-city tests use only PRIMARY `waterArea()`.
`secondWaterArea()` is ignored.

The outer branch also uses:

    iSeaExplorersNow =
        AI_totalWaterAreaUnitAIs(*pWaterArea, UNITAI_EXPLORE_SEA);

and requires `iSeaExplorersNow == 0`. As recorded in WIP283, the inherited helper's
city augmentation is primary-water-only too, so it does not rescue a secondary-sea
port.

CLEAN CURRENT-GAME REPRODUCTION
-------------------------------
Use:

    P = producing coastal city; P's relevant/primary sea is West.
    Q = owned isthmus / dual-coast port.
        Q.waterArea()       = East (larger primary sea)
        Q.secondWaterArea() = West (smaller disconnected sea)

Leave at least one West tile unrevealed and satisfy the ordinary naval-trade branch
conditions: P is not in danger, its area is not defensive, naval trade is available,
and the ordinary sea-explorer count for West is zero.

Dock THREE already-built DOMAIN_SEA ships in Q. They need not be explorer-role ships;
KI#197.6 deliberately retains the inherited safety cap of three actual sea-domain ships,
not three explorers. A ship on Q's city plot can move into adjacent West through the
engine's dual-coast city topology, so these are real sea units available to the relevant
West water area.

Current evaluation at P:

1. Each docked ship is physically on Q's LAND city plot, so
   `&ship.getArea() == West` is false.
2. `ship.getPlot().waterArea()` resolves Q's larger PRIMARY East sea, so it is not West.
3. The code never tests Q's `secondWaterArea() == West`.
4. `iSeaUnitsIncludingDocked` therefore receives 0 from all three usable ships.
5. With no West training orders, `iWaterUnits` remains 0 instead of the correct >=3.
6. `bEnoughWaterUnits` is false and the branch remains eligible as long as West has one
   unrevealed tile.
7. The inherited 25% roll can therefore request another `UNITAI_EXPLORE_SEA` despite the
   already-satisfied three-ship safety cap.

No queued-ship attribution ambiguity is needed for this witness. Three actual ships are
already present and all can enter the selected West sea directly from Q.

BIRTH / DOCUMENTED CONTRACT
---------------------------
Practical 6090 / commit `1a80ca7cff...` introduced KI#197.6. Its patch replaces the
old `CvArea::getNumAIUnits(..., NO_UNITAI)` cap with the current explicit loops.
The birth implementation already used:

    pLoopUnit->getPlot().waterArea() == pWaterArea
    pLoopCity->waterArea() == pWaterArea

and therefore omitted second-water ports from day one.

KI#197.6's current documentation states that the corrected branch:

    "counts actual sea-domain ships associated with the relevant water area, including
     ships docked in coastal cities and sea-domain UnitAIs already being trained there"

and that every confirming choice had fewer than three corrected sea units.

A docked ship at a dual-coast city whose secondary sea is the concrete relevant water
area is associated with that sea under Civ4's engine topology. The primary-only test is
therefore an incomplete implementation of the same corrected cap, not a new strategic
policy.

CLASSIFICATION / DEDUP
----------------------
Keep this under KI#197.6, not F521.

- KI#197.6 owns the exact naval-trade `<3` corrected sea-unit safety cap and its docked /
  queued accounting.
- WIP283's KI#193.2 broadening concerns assault TRANSPORT CARGO CAPACITY for one selected
  conquest target. The same dual-coast topology exposes both, but their consumers and
  narrow repairs are separate feature-local accounting blocks.
- The inherited `AI_totalWaterAreaUnitAIs` primary-only city scan remains a shared audit
  note rather than a generic F521 root. The KI#197.6 actual decision proves its defect
  independently without relying on that helper.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
For KI#197.6's selected `pWaterArea`, count a docked sea unit when the city plot serves
that water area as either primary OR second. Likewise, if training orders are intended
to count as available to either directly adjacent sea, include training at a city whose
`waterArea()` or `secondWaterArea()` matches. The actual-ship witness should be repaired
even if queued-ship allocation is kept more conservative.

A helper local to this one selected sea can use a predicate such as:

    city.waterArea() == pWaterArea || city.secondWaterArea() == pWaterArea

Do not globally double-count dual-coast ships in every `AI_totalWaterAreaUnitAIs` caller
without deciding simultaneous-demand allocation semantics first.

CURRENT STONE AFTER C031-WIP284
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
KI#198 reopened/broadened (WIP279).
KI#197.11 reopened/broadened (WIP281).
KI#194 reopened/broadened (WIP282).
KI#193.2 reopened/broadened (WIP283).
KI#197.6 now reopened/broadened by the dual-coast docked-ship omission.
Latest physical checkpoint: C031-WIP284.
No repository source modified.

NEXT
----
Stop expanding the same second-water-area family unless a caller has a genuinely
independent contract not owned by its existing KI. Return to the remaining late
`AI_chooseProduction` military/naval/air/project/process branches, then citizen
assignment/juggling and the other high-ranked `CvCityAI` methods. Keep F521 unused
unless a new root survives current-data, authored-intent and semantic-dedup checks.

END C031-WIP284


================================================================================
C031-WIP285 — QUEUE 005 DEEP RE-AUDIT — KI#44.5 MANDATORY-CITIZEN FALLBACK GAP
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

KI#44.5 REOPENED/BROADENED — THE "UNLESS ABSOLUTELY NECESSARY" CITIZEN EXCEPTION
IS NOT IMPLEMENTED, AND THE RESULT DIRECTLY EXPLAINS THE RECORDED ASSIGNMENT ASSERT CASCADE
--------------------------------------------------------------------------------

CURRENT ROOT
------------
`CvCityAI::AI_addBestCitizen` still uses the inherited K-Mod chooser contract:

    int iBestValue = -1;
    ...
    if (isSpecialistValid(eLoopSpecialist, 1))
    {
        int iValue = AI_specialistValue(...);
        if (iValue > iBestValue)
            eBestSpecialist = eLoopSpecialist;
    }

`AI_specialistValue(..., bRemove=false)` delegates directly to:

    AI_jobChangeValue((specialist,new), (none,old), ...)

Current SAS then intercepts the engine default specialist (Citizen) at the start of
`AI_jobChangeValue` and, for every AI player, returns:

    SAS_AI_JOB_CHANGE_VALUE_AI_JOB_FORBIDDEN = -100000

The same effective block is enabled for human governor assignment when
`SAS_CONVENIENCE_HUMAN_NO_AUTO_CITIZEN_SPECIALIST` is enabled.

Because `-100000` can never satisfy `iValue > -1`, the default Citizen is not merely
"strongly disfavored". It is impossible for `AI_addBestCitizen` to select even when it
is the ONLY remaining legal job.

ENGINE DEFAULT-SPECIALIST CONTRACT
----------------------------------
`CvCity::isSpecialistValid` explicitly makes the default specialist unlimited:

    return (assigned + extra <= maxCount ||
            player.isSpecialistValid(eSpecialist) ||
            eSpecialist == eDefaultSpecialist);

Thus the default Citizen is the engine's unconditional sink for otherwise unassigned
population. Ordinary specialist slot limits do not apply to it.

The surrounding city code also relies on this fallback contract. For example,
`CvCity::alterSpecialistCount` can directly convert a removed non-default specialist to
the default specialist, and its later fallback branches call `AI_addBestCitizen` when a
citizen needs reassignment.

DIRECT ASSERT-CASCADE CONSEQUENCE
---------------------------------
`AI_assignWorkingPlots` first calls `verifyWorkingPlots()`, then repeatedly assigns every
remaining unassigned population:

    while (extraPopulation() > 0)
    {
        if (!AI_addBestCitizen(true, true))
        {
            FErrorMsg("failed to assign extra population");
            break;
        }
    }

It then similarly attempts any `extraSpecialists()` and ultimately asserts that no extra
population remains.

If every normal plot/specialist candidate is unavailable while extra population remains,
current SAS behaves as follows:

1. The default Citizen passes `isSpecialistValid(...,1)` because it is the default.
2. `AI_specialistValue(Citizen,...)` reaches `AI_jobChangeValue`.
3. The SAS Citizen guard returns -100000.
4. `AI_addBestCitizen` began from -1, so Citizen is never selected.
5. No plot or ordinary specialist wins either.
6. `AI_addBestCitizen` returns false.
7. `AI_assignWorkingPlots` emits exactly the already-recorded
   `failed to assign extra population` failure; remaining extra specialist / final
   population assertions can then cascade.

This gives a direct source explanation for the KI#44.5 Known-Issues note that had already
recorded the three-assert cascade while saying it was unknown whether the cascade was
caused by Citizen no longer being usable as a fallback.

ORDINARY CURRENT-GAME REACHABILITY
----------------------------------
A giant city is not required.

`CvCity::canWork` rejects a BFC plot when:

    kPlot.plotCheck(PUF_canSiege, getOwner()) != NULL

and rejects water plots while blockaded. Therefore ordinary war can suddenly remove
several formerly valid citizen jobs from a moderate-population city. `verifyWorkingPlots`
then removes those assignments.

Construct an ordinary AI city where:
- population was fully assigned before the tactical change;
- ordinary specialist slots are absent or already full;
- enemy siege-capable units occupy enough remaining BFC plots and/or naval blockade makes
  the water jobs unavailable so no unworked normal plot remains;
- at least one citizen consequently remains unassigned.

The default Citizen is still valid by engine rule and is precisely the last-resort job
that can absorb that population. Current SAS rejects it below `AI_addBestCitizen`'s -1
selection floor and therefore leaves population unassigned.

The same condition can also occur naturally in very large cities after ordinary workable
plots and limited specialist slots are exhausted, but the wartime plot-loss fixture is
cleaner and needs only moderate population.

ANCESTRY — THE REGRESSION BOUNDARY IS ESPECIALLY CLEAR
------------------------------------------------------
K-Mod and Base AdvCiv 1.14 have the same `AI_addBestCitizen` `iBestValue=-1` architecture,
but they do NOT contain SAS's unconditional Citizen early-return. The default specialist
receives ordinary `AI_jobChangeValue` valuation and remains selectable.

SAS practical 4919 / commit `fcf32edcd9...` first added the anti-Citizen gate. Crucially,
its birth implementation returned `1`, and the comment explicitly documented the intended
last-resort semantics:

    "This ensures that the AI will only pick it if literally no other valid option exists"

Because 1 > -1, that implementation preserved `AI_addBestCitizen`'s emergency fallback.

SAS practical 5130 / commit `710c78523c...` reintroduced/hardened the gate after it had
been lost and changed the Citizen result to a large negative forbidden value. The new
comment says "never *hire* a citizen" and the current define is -100000. No corresponding
change was made to `AI_addBestCitizen`'s -1 acceptance threshold or to provide a separate
last-resort fallback.

Thus practical 5130 converted a strong preference into an absolute impossibility while
leaving both the engine and the Known-Issues design language expecting an exception.

DOCUMENTED INTENT / CLASSIFICATION
----------------------------------
Keep this under KI#44.5, not F521.

The issue title itself is:

    "Disallow citizen specialist as it is a really bad choice (unless absolutely necessary)"

and its recorded cascade explicitly asks whether Citizen no longer being usable as a
fallback is the cause. The present deep pass answers that question: yes, the current
`-100000` guard removes the inherited fallback that practical 4919 originally preserved.

For human automated assignment, the same mechanism is also a sibling of KI#44.6 when
`SAS_CONVENIENCE_HUMAN_NO_AUTO_CITIZEN_SPECIALIST` is enabled. Manual assignment is a
separate path and need not be weakened.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve the intended anti-Citizen policy for ordinary ranking, but restore the
"absolutely necessary" fallback explicitly.

A robust shape is:
1. rank normal workable plots and non-default specialists exactly as now;
2. do NOT let Citizen compete normally;
3. if and only if no normal candidate was selected and the default specialist is valid,
   assign one default Citizen as the terminal fallback.

This is safer than simply returning `1` again from `AI_jobChangeValue`, because the
current code now has many more job-comparison/juggling callers than the original practical
4919 change; a dedicated fallback in `AI_addBestCitizen` keeps Citizen out of ordinary
swap/value competition while preserving population conservation.

For human auto-assignment with the convenience define enabled, the same terminal fallback
should apply only when leaving population unassigned would otherwise be necessary. Manual
Citizen choices remain untouched.

CURRENT STONE AFTER C031-WIP285
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Queue-005 existing families reopened/broadened so far:
- KI#198 (WIP279)
- KI#197.11 (WIP281)
- KI#194 (WIP282)
- KI#193.2 (WIP283)
- KI#197.6 (WIP284)
- KI#44.5 mandatory default-Citizen fallback (this WIP; KI#44.6 human-auto sibling)
Latest physical checkpoint: C031-WIP285.
No repository source modified.

NEXT
----
Continue the citizen-assignment deep pass with F521 unused:
1. finish `AI_juggleCitizens` and `AI_jobChangeValue` temporary-state / early-return
   restoration and forced/free-specialist accounting;
2. challenge the per-turn direct-Artist-culture cache for a hard same-turn consequence;
3. inspect remaining high-ranked `CvCityAI` methods and current SAS semantic deltas;
4. only allocate F521 if a genuinely independent root survives authored intent, current
   data reachability and semantic dedup.

END C031-WIP285


================================================================================
C031-WIP286 — QUEUE 005 DEEP RE-AUDIT — CITIZEN CONTROLS + KI#197 VASSAL-DENOMINATOR GAP
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.

POST-WIP285 CITIZEN CONTROLS
----------------------------
1. `AI_jobChangeValue` does NOT temporarily mutate city assignment state in current
   K-Mod/AdvCiv/SAS. It computes yield/commerce/GPP deltas analytically. The custom early-return
   specialist guards therefore cannot leak a half-applied plot/specialist mutation. Actual
   `changeSpecialistCount` / `setWorkingPlot` mutations occur in `AI_juggleCitizens` only after a
   swap has passed the starvation, free-specialist, forced-specialist, no-undo and direct-context
   checks. No independent F521 from early-return restoration.

2. The broader SAS population<=4 and growth-first specialist guards can also make ordinary
   specialist candidates forbidden, but the population-conservation failure still has the same
   terminal repair boundary as KI#44.5: rank ordinary jobs under those policies, then permit the
   engine default Citizen only if no ordinary job can absorb the citizen. Do not reopen KI#45 as
   a separate assignment-conservation root merely because its policy can contribute to reaching
   the same last-resort state.

3. `SAS_AI_directArtistCultureValuePercent` retains the previously recorded one-turn cache-lag
   observation but does not cross the numbered-root threshold in this pass. The current clean
   non-victory value is 50%, whereas a pre-culture result can be 100%; a same-turn culture/border
   improvement can therefore leave Artist direct culture temporarily overvalued. However the
   cache is only one global last-call slot keyed by turn/owner/city, so any intervening evaluation
   for another city evicts it, and the demonstrated consequence remains a one-turn delay in
   dampening rather than a legality/accounting failure. Preserve the existing audit note.

ZERO-MENTION / LOW-COVERAGE METHOD CONTROLS
-------------------------------------------
A renewed current-vs-Base semantic pass closed several methods whose literal names were absent
or rare in the album:

- `AI_bestPlotBuild`: after ignoring whitespace/signature collapsing, the current SAS body is
  semantically identical to Base AdvCiv 1.14.
- `AI_defensiveBuildingValue`, `AI_updateSpecialYieldMultiplier`, `AI_clearFeatureValue`,
  `AI_countGoodSpecialists`, `AI_removeWorstCitizen`, `AI_growthValuePerFood`,
  `AI_countNumBonuses`, and `AI_calculateSettlerPriority`: current differences are cached local
  references/static info lookups or formatting only.
- `AI_buildGovernorChooseProduction`: semantic addition is diagnostics plus a defensive
  `eBestSpreadUnit != NO_UNIT` guard around the already-owned KI#74 helper-contract defect.
- `AI_barbChooseProduction`: its main semantic addition is the known KI#195 Barbarian Work-Boat
  docked/queued accounting repair. No independent new root established here.
- `AI_processValue`: the material SAS delta is the documented Culture-process restriction for
  clean low-pressure non-victory cities; no contract failure established in this pass.
- `AI_minDefenders`: the added early anti-Barbarian third-defender window and elapsed-turn cutoff
  are explicit KI#49 tuning/policy rather than an invariant failure.
- `AI_updateWorkersHaveAndNeeded`: semantic changes remain in the KI#192/KI#198 Worker-demand
  family already being reopened elsewhere in this Queue-005 pass.

KI#197 REOPENED/BROADENED — POWER-PER-CITY CATCH-UP COUNTS VASSAL POWER BUT NOT VASSAL CITIES
---------------------------------------------------------------------------------------------

ROOT
----
The KI#197 under-strength catch-up computes the city-weighted rival average through:

    iRivalPower = kRival.getPower(true);
    iRelevantRivalPower += iRivalPower;
    iRelevantRivalCities += kRival.getNumCities();
    rivalPowerPerCity = 100 * iRelevantRivalPower / iRelevantRivalCities;

The numerator and denominator describe different populations whenever a relevant free rival has
vassals.

`CvTeam::getPower(true)` explicitly sums:
- every member of `kRival`, AND
- every living player that is a vassal of `kRival`.

`CvTeam::getNumCities()` returns that team's own `m_iNumCities`. City creation/destruction updates
`changeNumCities` on the city's owning team; it does not fold vassal cities into the master's
stored city count.

Therefore the current metric can divide master+vassal coalition power by MASTER-ONLY cities.

DOCUMENTED FEATURE CONTRACT
---------------------------
KI#197 and practical 6087 describe the new signal as:

    "power per city"
    "city-weighted average power per city of relevant rivals"

with the explicit rationale that military density, rather than raw total army size, should drive
catch-up. The implementation's mixed coalition numerator / master-only denominator violates that
unit of measure.

CLEAN CURRENT-GAME REPRODUCTION
-------------------------------
Use one relevant free rival master M with a normal living vassal V:

    M: 5 cities, 500 power
    V: 5 cities, 500 power

Let our team A have:

    A: 5 cities, 600 power

Both coherent interpretations say A is NOT under-strength per city:

1. compare free teams only:
       M own density = 500 / 5 = 100
       A density     = 600 / 5 = 120

2. compare the rival master/vassal coalition as one threat:
       M+V density = (500 + 500) / (5 + 5) = 100
       A density   = 600 / 5 = 120

Current code instead evaluates:

       rival numerator   = M.getPower(true) = 1000
       rival denominator = M.getNumCities() = 5
       reported density  = 200

and therefore invents:

       100 - (100 * 120 / 200) = 40% power-per-city deficit

for A, despite A actually having the higher military density.

With ordinary KI#197 gates satisfied (not in financial/economy suppression, not waiting for
better units, war possible, inherited high-power throttle inactive), that false deficit raises
`AI_buildUnitProb` by up to 40% multiplicatively in this fixture. KI#197.8 can then add its
separate affordable absolute response from the same false deficit, making the state-changing
production bias stronger.

REACHABILITY
------------
No malformed scenario or unusual API state is needed. Voluntary vassals and capitulated vassals
are ordinary supported game states, and the KI#197 loop intentionally iterates FREE relevant rival
teams, i.e. precisely the master team from which `getPower(true)` can pull vassal power.

ANCESTRY / CLASSIFICATION
-------------------------
The inherited K-Mod/Base-AdvCiv high-power throttle already used `kRival.getPower(true)` to value
how strong the rival threat is in TOTAL. Practical 6087 / commit `6c8d14b04e...` reused that same
value while adding the new power-PER-CITY average, then paired it with `kRival.getNumCities()`.
Thus the mismatch was born with the SAS KI#197 density feature; it is not a stock BtS/K-Mod
power-counting bug.

Keep this under KI#197, not F521. KI#197 owns the exact under-strength power-per-city metric;
KI#197.8 merely adds a second response from that metric and therefore inherits the bad input.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Make numerator and denominator describe the same population.

Two coherent designs are available:

A. FREE-TEAM DENSITY:
   - use `kRival.getPower(false)` with `kRival.getNumCities()`.
   - This compares each free rival team's own military density and leaves vassal strength to the
     inherited total-threat throttle.

B. MASTER/VASSAL-COALITION DENSITY:
   - keep `kRival.getPower(true)`;
   - add the living vassal players' city counts to the denominator as well.
   - This treats the master locus as one military coalition consistently.

Because the inherited high-power target deliberately includes vassal power, coalition density may
fit the surrounding threat model better, but the policy choice should be explicit. Do not keep the
current mixed population.

CURRENT STONE AFTER C031-WIP286
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Queue-005 existing families reopened/broadened so far:
- KI#198 (WIP279)
- KI#197.11 (WIP281)
- KI#194 (WIP282)
- KI#193.2 (WIP283)
- KI#197.6 (WIP284)
- KI#44.5 mandatory default-Citizen fallback (WIP285; KI#44.6 human-auto sibling)
- KI#197 power-per-city rival-vassal numerator/denominator mismatch (this WIP; KI#197.8 inherits input)
Latest physical checkpoint: C031-WIP286.
No repository source modified.

NEXT
----
Continue F521 unused through the remaining semantic `CvCityAI` deltas. Highest-interest next
areas after the above controls are the KI#197.9/.10 offensive production-capacity floor and its
area/production aggregation, `AI_buildingValue`/threshold cache interactions not already owned by
KI#48/KI#306, and remaining Worker/production helpers. Then return to final citizen/cache and
serialization/sentinel saturation before closing Queue 005.

END C031-WIP286

===============================================================================
C031-WIP287 — QUEUE 005 DEEP RE-AUDIT — KI#53.3 CONCRETE-VETO / NO-RETRY BOUNDARY
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains CLOSED.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 remain CONFIRMED.
F521 / provisional KI#844 remains UNUSED.
No repository source files modified.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.

KI#53.3 BROADENING — A CONCRETE SIEGE VETO CAN SUPPRESS A LEGAL SAME-ROLE ALTERNATIVE
------------------------------------------------------------------------------------
This pass does NOT spend F521. It identifies a same-feature weakness in the centralized
KI#53/KI#53.3 production-cap architecture.

Current role-selection path:

    AI_chooseUnit(UnitAI)
        -> AI_bestUnitAI(UnitAI)        // selects exactly one concrete UnitTypes
        -> AI_chooseUnit(UnitTypes, UnitAI)
             -> SAS concrete composition/cap checks
             -> pushOrder only if selected concrete unit survives

If the concrete unit is rejected, `AI_chooseUnit(UnitAI)` returns false immediately. It does
not retry the second-best trainable unit for the same UnitAI role.

Current shipped ATTACK_CITY control:
* `UNIT_TREBUCHET` and `UNIT_CATAPULT` are both ordinary no-resource
  `UNITAI_ATTACK_CITY` candidates.
* Trebuchet has +100% city attack and 16 bombard versus Catapult 0% city attack and
  8 bombard, so K-Mod's inherited `AI_unitValue(..., UNITAI_ATTACK_CITY, ...)` can rank
  Trebuchet first. Existing stored Trebuchet production can make that selection especially
  strong because `AI_bestUnitAI` explicitly discounts remaining production cost.
* Catapult remains a positive candidate under the inherited ATTACK_CITY valuation; the
  raw value difference does not inherently remove it from the best-unit candidate set.
* KI#53.3's later concrete gate can nevertheless reject the chosen Trebuchet through
  `TREBUCHET_SHARE_CAP` while the same Trebuchet-specific condition does not reject a
  Catapult.
* The role request then returns false instead of considering Catapult.

Live caller consequence:
The advc.300 Barbarian-city attacker branch explicitly computes a needed
`UNITAI_ATTACK_CITY` count and calls `AI_chooseUnit(UNITAI_ATTACK_CITY)` when below that
quota. A Trebuchet-first / Trebuchet-cap state can therefore make that request fail even
though a legal Catapult could satisfy the requested city-attacker role. The generic early
city-hunting ATTACK_CITY branch has the same shape.

Important negative control — not a hard KI#197.9 floor failure:
The newer offensive production-capacity floor uses `AI_chooseLeastRepresentedUnit` with
both `UNITAI_ATTACK` and `UNITAI_ATTACK_CITY`. That helper retries the next UnitAI when the
first role fails. Catapult is also a valid ATTACK candidate and its city-attack disadvantage
no longer helps Trebuchet in that role. Thus this pass does not prove that the hard empire
offensive-production floor collapses merely because ATTACK_CITY's preferred Trebuchet is
vetoed.

CLASSIFICATION / INTENT
-----------------------
Keep under KI#53.3 rather than allocate F521.

The parent KI#53 documentation explicitly says:

    "The 'best unit' picker remains the same; only the final 'should we actually queue it?'
     logic is centralized."

KI#53.3 intentionally accepts some siege underproduction as a tradeoff while suppressing
Trebuchet/siege excess. Current source also explicitly describes Catapults as a more
versatile valid alternative in some early no-resource compositions. The no-retry behavior
therefore looks like an architectural weakness of the same post-selection cap feature,
not a separate gameplay subsystem/root.

Conservative improvement:
Do not weaken the Trebuchet or all-siege caps. When the selected concrete unit is rejected
solely by a KI#53 composition/cap rule, allow the role chooser to consider the remaining
`AI_bestUnitAI` candidates for that same UnitAI, in descending value order, until one
survives or none do. Preserve random/production valuation semantics carefully; avoid a
blanket retry after unrelated failures such as role-level strategic vetoes or odds failure.

CURRENT STONE AFTER C031-WIP287
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN.
32/169 unique files complete.
F446 / KI#769 through F520 / KI#843 CONFIRMED.
F521 / provisional KI#844 UNUSED.
Latest physical checkpoint: C031-WIP287.
No repository source modified.

NEXT
----
Leave the KI#53 concrete-selector family and resume genuinely independent Queue-005
surfaces. Highest-priority next lens: `AI_buildingValue` hard prefilters / force-zero gates
for current multi-role buildings, while excluding already-owned KI#48/KI#306 cache and
food-signal roots. Then continue remaining semantic helpers and final method saturation.

END C031-WIP287

===============================================================================
C031-WIP288 — QUEUE 005 DEEP RE-AUDIT — F521 / KI#844 CONFIRMED
===============================================================================

STATUS
------
Queue 004 `CvGameCoreDLL/CvPlayer.cpp` remains CLOSED.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F521 / KI#844 are CONFIRMED.
F522 / provisional KI#845 is UNUSED.
No repository source files modified.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.

POST-WIP287 INHERITED-METHOD SATURATION CONTROLS
------------------------------------------------
1. `AI_getYieldMultipliers` is semantically identical to Base AdvCiv 1.14; current SAS only
   collapsed the function signature onto one line. Its dangerous-looking reciprocal/divisor
   chain does not produce a current zero divisor under shipped data: city yield multipliers
   remain positive under current modifiers and the player-average getters assert/rebuild
   positive values. The old ordinary `ExtraYieldThreshold` projected-yield `>`/`>=` shape
   is also data-dead in current SAS because Financial now uses only
   `ExtraYieldNaturalThreshold` and every shipped ordinary `ExtraYieldThreshold` is zero.

2. `AI_healthHappyImprovementValue` contains a real inherited AdvCiv receiver bug, but it is
   CURRENT-SAS MASKED and therefore does not spend F521. The `advc.901` loop says that other
   affected team cities matter, finds `pCity`, but then calls the helper again on `this`
   instead of on `pCity`. With its default `bIgnoreOtherCities=true`, each neighbor therefore
   contributes another copy of the evaluating city's marginal health/happiness value. The bug
   was introduced in AdvCiv practical 1732 (`d595a1df9e...`, "Health tag for improvements;
   merge xml-tags-as-enums branch."). Current Forest Preserve (+1 happiness) makes the
   arithmetic visibly wrong in Base AdvCiv: e.g. a happy-surplus city can value +1 at about
   5-6 while a neighboring city at the happiness boundary values it at about 600-800, yet
   current code duplicates the first city's value. Current SAS, however, deliberately returns
   zero from `AI_getImprovementValue` for every LAND plot and delegates authoritative land
   Worker choices to the custom `CvUnitAI::AI_bestCityBuild`; current WATER improvements have
   no happiness/health source. Thus the source defect is live in Base AdvCiv architecture but
   lacks an authoritative current-SAS gameplay producer. Preserve as a masked upstream note;
   do not report it as F521. A future repair is `pCity->AI_healthHappyImprovementValue(...)`
   with recursion suppressed.

3. `AI_emphasizeIrrigatingPlot`, `AI_buildingSeaYieldChangeWeight` and
   `AI_finalImprovementYieldDifference` were re-read as inherited low-coverage helpers. No
   independent current contract failure survived. In particular, the land-improvement path
   containing the irrigation helper is largely superseded by SAS's custom land Worker
   evaluator, while sea-yield weighting remains internally consistent with its K-Mod caller.

-------------------------------------------------------------------------------
F521 / KI#844 — CONFIRMED
ADVC EARLY FEATURE-CLEAR TECH BONUS COMPARES PLOT `PlayerTypes` OWNER TO CITY OBJECT ID,
SO `AI_countOvergrownBonuses` ALWAYS RETURNS ZERO AND THE BLOCKED-RESOURCE TECH BONUS IS DEAD
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current SAS gameplay/AI valuation defect.
Inherited from Base AdvCiv; not present in BtS/Civ4CE or K-Mod because the
`advc.129` blocked-resource tech-value helper is an AdvCiv addition.

ROOT
----
Current `CvCityAI::AI_countOvergrownBonuses(FeatureTypes eFeature)` scans the city radius
for an owned, unimproved feature tile containing a resource that could be improved once the
feature is removed. Its ownership gate is:

    if (p.getOwner() == getID() && p.getFeatureType() == eFeature && !p.isImproved())

The two sides are DIFFERENT ID DOMAINS:

    p.getOwner()   -> PlayerTypes
    getID()        -> CvCity object ID

The intended receiver is plainly the city's owner:

    p.getOwner() == getOwner()

This is not a numerically-accidental player/city coincidence in the current engine.
`CvPlayer::initCity` allocates cities from `FFreeListTrashArray` and passes
`pCity->getID()` into `CvCity::init`. `FFreeListTrashArray::init` starts
`m_iCurrentID = FLTA_MAX_BUCKETS`, and:

    FLTA_ID_SHIFT   = 13
    FLTA_MAX_BUCKETS = 1 << 13 = 8192

The first allocated object receives:

    m_iCurrentID + iIndex >= 8192

whereas current SAS has:

    MAX_CIV_PLAYERS = 48
    BARBARIAN_PLAYER = 48
    MAX_PLAYERS = 49

Thus an ordinary plot owner is 0..48 while every ordinary generated city ID is at least
8192. `p.getOwner() == getID()` is therefore impossible. The helper returns zero for every
ordinary city regardless of how many qualifying blocked resources actually exist.

LIVE CONSUMER / AUTHORED PURPOSE
--------------------------------
`CvPlayerAI::AI_techValue` contains the only current gameplay caller. While the player has
at most two cities, whenever the technology being evaluated removes a feature it does:

    FOR_EACH_CITYAI(c, *this)
        iBuildValue += 30 * c->AI_countOvergrownBonuses(eFeature);

The surrounding source comment states the intended rule explicitly:

    "Very early game: Is the feature blocking a resource?
     (especially Silver, which can now appear on Grassland Forest)"

So one qualifying blocked resource is deliberately worth +30 tech-value points; two are
+60, etc. The broken ownership comparison removes that entire authored term for every AI.
This feeds normal `AI_techValue` / `AI_bestTech` research ranking, so it is not diagnostic
or UI-only state.

DETERMINISTIC CURRENT-SAS DATA WITNESS — FOREST SILVER / MASONRY
----------------------------------------------------------------
The original AdvCiv comment discussed Bronze Working, but current SAS's reworked build techs
move ordinary Forest removal to Masonry. The same generic helper therefore remains fully
live with an updated current-data witness.

Current shipped facts:

    BONUS_SILVER
        TechReveal = NONE
        can occur on FEATURE_FOREST + TERRAIN_GRASS

    IMPROVEMENT_MINE
        BonusTypeStruct includes BONUS_SILVER with bBonusMakesValid=1

    BUILD_MINE
        PrereqTech = TECH_MINING
        FEATURE_FOREST removal PrereqTech = TECH_MASONRY

    TECH_MINING  era = ERA_ANCIENT
    TECH_MASONRY era = ERA_ANCIENT

Use any ordinary non-Barbarian AI with one or two cities and a Silver resource on an owned
Grassland Forest tile in a city radius, without an improvement. Let Mining already be known
and evaluate Masonry while still in the Ancient era.

During `AI_techValue(TECH_MASONRY)`:

1. The Feature loop sees that a Build removes `FEATURE_FOREST` at Masonry and enters the
   blocked-resource valuation block.
2. For that city, the Silver/Forest plot satisfies the intended semantic conditions:
   owned by this player, Forest, unimproved, visible Silver.
3. The inner Build scan can use `BUILD_MINE`: Mine makes Silver valid, and its ordinary
   prerequisite Mining is Ancient/current-era-compatible.
4. Correct `AI_countOvergrownBonuses(FEATURE_FOREST)` therefore includes this plot once.
5. Correct Masonry `iBuildValue` gains +30.
6. Current code first tests `PlayerTypes owner == city object ID >=8192`, which is false.
7. Current count is 0 and Masonry receives +0 from the entire feature.

The error therefore deterministically removes 30 valuation points per qualifying resource.
`AI_bestTech` chooses among technologies from this score, so the defect can alter early
research order precisely in the resource-blocking situation that `advc.129` introduced the
bonus to correct.

ANCESTRY
--------
BtS/Civ4CE and K-Mod do not contain `AI_countOvergrownBonuses` / the corresponding
blocked-resource +30 tech-value feature.

AdvCiv practical 1389, commit:

    5c007429ae034192cad8bf8032bf499970255ff3
    "Some AI tweaks concerning ships; Japan added to EuroWorld scenario"

introduced the helper under the old name `AI_countBonusesToClear` and the +30-per-city call
in `AI_techValue`. The very first implementation already contained:

    p->getOwnerINLINE() == getID()

so the wrong receiver/domain is a birth defect in the feature, not a later SAS regression.

Practical 1910 (`32028ef437...`, "CityPlotIterator (refactor city radius loops)") renamed it
to `AI_countOvergrownBonuses` and replaced the raw plot loop with `CityPlotIter`, but
preserved the same `p.getOwner() == getID()` condition. Base AdvCiv 1.14 and current SAS
still retain it.

The reachable AdvCiv log describes the intended feature as:

    "Increased AI tech value for Bronze Working when a Forest blocks a resource near the
     capital (e.g. Silver or Pig). [advc.129]"

Current SAS's Masonry-based Forest-removal tech tree changes the concrete tech in the
witness, not the helper's generic purpose.

DUPLICATE / ROOT SEPARATION
---------------------------
Full album / Known-Issue searches for `AI_countOvergrownBonuses`, overgrown/blocked bonus,
city-ID vs plot-owner, and the nearby-resource tech bonus found no existing root for this
helper.

This is independent of:
* F165/KI#488, which concerns SAS cautious-health scoring dropping Forest/Jungle HEALTH when
  the plot contains a visible bonus;
* Worker/job-demand and KI#198/KI#213 families, which concern improving resources after a
  technology is known rather than valuing the technology that unlocks feature removal;
* the Queue-004 player-vs-team/ID-domain findings, which involve different objects and
  callers.

The narrow repair for those roots does not change this impossible CityID/PlayerID equality.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Replace the wrong ownership receiver only:

    p.getOwner() == getOwner()

Do not broaden the radius, resource eligibility, early-game `getNumCities() <= 2` gate,
+30 weight, build prerequisite approximation or feature-removal policy as part of this fix.
Those are deliberate heuristic choices and can be tuned independently.

Suggested regression check:
1. Create an AI player with <=2 cities and one owned unimproved Forest Silver in a city BFC.
2. Assert `AI_countOvergrownBonuses(FEATURE_FOREST) == 1` after the ownership fix.
3. Add/remove a second qualifying blocked resource and assert the helper follows 2/1.
4. Compare `AI_techValue` for the current Forest-removal tech and verify the resource term
   changes by exactly 30 per qualifying plot before later/randomized ranking terms.
5. Run the same fixture for a nonzero player slot to ensure no ID-domain coincidence is
   involved.

CURRENT STONE AFTER C031-WIP288
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — OPEN.
32/169 unique files complete.
F446 / KI#769 through F521 / KI#844 CONFIRMED.
F522 / provisional KI#845 UNUSED.
Latest physical checkpoint: C031-WIP288.
No repository source modified.

NEXT
----
Continue Queue-005 inherited/unchanged-method saturation under F522 UNUSED. Highest-value
remaining low-coverage methods include `AI_doEmphasize`, `AI_foodAvailable`,
`AI_neededCultureDefenders`, `AI_estimateReligionBuildings`, `AI_updateRouteToCity`,
`AI_calculateWaterWorldPercent`, `AI_buildingSeaYieldChangeWeight` caller semantics and the
remaining small arithmetic/object-identity helpers. Keep current-data reachability and
semantic dedup mandatory; do not manufacture F522 after the strong F521 discovery.

END C031-WIP288


================================================================================
C031-WIP289 -- QUEUE 005 DEEP RE-AUDIT -- F522 / KI#845 CONFIRMED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under the deeper method-by-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F522 / KI#845 are CONFIRMED.
F523 / provisional KI#846 is UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260903T200009.zip`.
No repository source files modified.

F522 / KI#845 -- ADVC PRACTICAL 1675 CHANGED HIGH-PRODUCTION EMPHASIS FROM
CLEARING OLD AI EMPHASIS TO FREEZING IT, SO STALE COMMERCE/RESEARCH EMPHASIS
CAN OVERRIDE THE SPECIAL-PRODUCTION GOVERNOR DURING WORKER/SETTLER/WONDER BUILDS
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED current gameplay bug. Inherited by current SAS from Base AdvCiv.
The semantic regression was introduced by AdvCiv practical 1675,
commit `fedca1cf525955e008cfd75ce5ae3dd68de17570`, titled
`Remove pure virtual functions from CvCity`.

ROOT / PRE-REGRESSION CONTRACT
------------------------------
`CvCityAI::AI_doEmphasize` is legacy K-Mod/BBAI governor state. Its own comment says
emphasis is clumsy, has mostly been replaced by `AI_specialYieldMultiplier`, and ideally
AI cities should never need emphasis.

Immediately BEFORE practical 1675, the loop had this effective shape:

    bool bEmphasize = false;
    if (AI_specialYieldMultiplier(YIELD_PRODUCTION) < 50)
    {
        ... possibly set bEmphasize = true ...
    }
    AI_setEmphasize(eEmphasize, bEmphasize);

Therefore a special-production state of 50 or more explicitly executed
`AI_setEmphasize(..., false)` for every Emphasize type. Any old Commerce/Research/GP
emphasis was cleared before the city continued operating under the newer special-yield
system.

PRACTICAL-1675 REGRESSION
-------------------------
Practical 1675 mechanically flattened/refactored `CvCityAI` while removing pure virtual
functions. In the same hunk it changed that control flow to:

    bool bEmphasize = false;
    if (AI_specialYieldMultiplier(YIELD_PRODUCTION) >= 50)
        continue;
    ...
    AI_setEmphasize(eEmphasize, bEmphasize);

That is not equivalent. `continue` skips the setter entirely, changing:

    high special-production -> CLEAR old emphasis

to:

    high special-production -> KEEP/FREEZE whatever emphasis bit was already set.

The commit title/log is a structural refactor, not an authored AI-policy change that
explains this semantic reversal. Base AdvCiv 1.14 and current SAS retain the post-1675
`continue` shape.

NORMAL PRODUCER / LIFETIME
--------------------------
The stale state is ordinary and is consumed before any repair:

1. While pursuing a technology for which `AI_isFirstTech` is true, `AI_doEmphasize`
   enables `EMPHASIZE_COMMERCE` (and, for a sufficiently high-ranked city, Research).
   The inherited EmphasizeInfo data gives `EMPHASIZE_COMMERCE` +1 Commerce yield
   emphasis.
2. `AI_setEmphasize(true)` persists that bit/count and immediately calls
   `AI_assignWorkingPlots`.
3. Later production can switch to a Worker, Settler or Work Boat; World Wonder/project
   production also creates the high-production mode.
4. At the next citizen-assignment pass, `AI_updateSpecialYieldMultiplier()` is called
   BEFORE citizen values are evaluated. Worker/Settler/Worker-Sea production contributes
   +50 Production and -25 Commerce before the rest of the special-yield adjustments.
5. The OLD emphasis bit is still present while this new special-production multiplier is
   consumed by `AI_yieldValue` / `AI_plotValue` / `AI_jobChangeValue`.
6. Only later, in `CvCity::doTurn -> CvCityAI::AI_doTurn -> AI_doEmphasize`, would old
   code have cleared emphasis. Current code instead `continue`s when Production special
   multiplier >=50 and preserves it again.

A current normal way to cross 50 despite advc.121 inertia is a Production-flavor leader.
Current `LEADER_FREDERICK` has `FLAVOR_PRODUCTION=10`, which contributes +25 special
Production. During Worker production the raw pre-inertia production special value is at
least 75 in an otherwise neutral setup. If the old value has converged near the
pre-Worker +25 state, successive updates are approximately:

    first Worker update:  (75 + ~24) / 2 = 49
    second Worker update: (75 + 49)  / 2 = 62

Thus a multi-turn Worker normally reaches the >=50 freeze. Keeping the first-discovery
research state through the first Worker turn preserves the emphasis until that point; if
research then completes during the later player economic phase, the subsequent city
`AI_doEmphasize` cannot clear it because the >=50 `continue` fires first.

DETERMINISTIC CURRENT-DATA CITIZEN FLIP
---------------------------------------
Use a Worker-producing AI city in the simple normalized state below:

- current Production special multiplier = 62;
- current Commerce special multiplier = -25 (the Worker contribution; avoid unrelated
  strategy shifts for the fixture);
- base and empire-average yield multipliers = 100;
- no extra production modifier;
- 100% Research slider;
- Research AI commerce weight = current XML/default 105;
- no happiness-driven hammer divisor relevant here (Worker is food-production);
- compare two ordinary current plot yields:

    A: Plains + Forest = 1 food, 2 hammers, 0 commerce
    B: Plains + Village = 1 food, 1 hammer, 3 commerce

Current XML supports exactly those components:
- Plains = 1F/1H;
- Forest = +1H;
- Village = +3 Commerce.

Because Worker production is food-production, the 1 food is also counted into production
value. With the normal base production value 8 and base commerce value 4:

NO STALE COMMERCE EMPHASIS (the pre-1675 intended high-production state)

    A raw production value = (1F + 2H) * 8 = 24
      after +62% special Production = floor(24 * 162 / 100) = 38

    B raw production value = (1F + 1H) * 8 = 16
      after +62% special Production = floor(16 * 162 / 100) = 25

    B raw weighted Research commerce:
      floor(105 * 300 * 4 / 10000) = 12
      after -25% special Commerce = floor(12 * 75 / 100) = 9

    B total = 25 + 9 = 34

Therefore the intended high-production governor prefers:

    Plains Forest 38 > Plains Village 34.

CURRENT STALE `EMPHASIZE_COMMERCE`

`AI_yieldValue` applies Commerce emphasis BEFORE the special-yield multipliers:
- Production value is reduced to 75%;
- Commerce value is raised to 140%.

    A: floor(24 * 75 / 100) = 18
       floor(18 * 162 / 100) = 29

    B production: floor(16 * 75 / 100) = 12
                  floor(12 * 162 / 100) = 19

    B commerce: floor(12 * 140 / 100) = 16
                floor(16 * 75 / 100) = 12

    B total = 19 + 12 = 31

The stale legacy emphasis therefore reverses the citizen preference:

    Plains Village 31 > Plains Forest 29.

This is not just a standalone-value illustration. `AI_juggleCitizens` fills its worked
and unworked job lists with `AI_plotValue`, sorts them by those values, and when the
candidate outranks the worked plot performs the final `AI_jobChangeValue` comparison
under the SAME emphasis/special-yield state. Both fixture plots supply equal food, so the
starvation guard does not block the swap.

GAMEPLAY CONSEQUENCE
--------------------
The obsolete legacy Commerce emphasis can therefore make a city work a more commercial
plot instead of the higher-hammer plot while the newer special-yield system is explicitly
trying to emphasize Production for Worker/Settler/Wonder/project completion. The wrong
assignment can persist for the whole high-production order because every later
`AI_doEmphasize` call freezes the stale bit again rather than clearing it.

ANCESTRY
--------
- K-Mod / pre-practical-1675 AdvCiv: high special Production did not evaluate new emphasis
  but still reached `AI_setEmphasize(..., false)`, clearing old state.
- AdvCiv practical 1675 / `fedca1cf52`: broad CvCity pure-virtual/refactor commit inserts
  the `>=50 -> continue` shape, unintentionally changing the lifetime semantics.
- Base AdvCiv 1.14: retains the regression.
- Current AdvCiv-SAS 20:00 source: retains the same `continue` and the same live emphasis
  consumers, with current Worker/Settler production and Production-flavor data providing
  an ordinary producer.

DEDUP / ROOT SEPARATION
-----------------------
This is independent of:
- KI#44.5: missing terminal default-Citizen fallback / population conservation;
- KI#70: forced-specialist target sanitation;
- KI#34/KI#40/KI#320: SAS food-vs-hammer citizen valuation tuning;
- the low-impact direct-Artist-culture per-turn cache note;
- F501/KI#824: player COMMERCE SLIDER scheduling before zero-anarchy civic/religion
  changes. F522 concerns persistent CITY EMPHASIS bits inside citizen valuation.
No prior album root owns practical-1675's high-production emphasis clear->freeze
semantic regression.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Restore the old state-transition contract without reviving legacy emphasis during strong
special-production modes. For example, before skipping the detailed emphasis evaluation,
explicitly clear any active AI emphasis entries:

    if (AI_specialYieldMultiplier(YIELD_PRODUCTION) >= 50)
    {
        AI_setEmphasize(eLoopEmphasize, false);
        continue;
    }

However, note that `AI_setEmphasize` triggers `AI_assignWorkingPlots` on every actual
change. A more efficient implementation should preferably clear all relevant bits/counts
through a batched helper and perform one assignment refresh, or restructure `AI_doEmphasize`
so `bEmphasize=false` still reaches the common setter as before practical 1675.
Preserve the authored special-yield system; do not weaken the >=50 production threshold.

REGRESSION TEST
---------------
1. Give an AI city Commerce emphasis from a first-discovery research state.
2. Start a multi-turn Worker under a Production-flavor leader/state until
   `AI_specialYieldMultiplier(PRODUCTION) >= 50`.
3. End/replace the first-discovery condition while Worker production remains active.
4. Assert after `AI_doEmphasize` that stale AI Commerce/Research emphasis is false.
5. In the normalized Plains-Forest vs Plains-Village fixture above, assert that the
   Worker-production assignment favors the Forest under the special-production state.

CURRENT STONE AFTER C031-WIP289
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` -- OPEN.
32/169 unique files complete.
F446 / KI#769 through F522 / KI#845 CONFIRMED.
F523 / provisional KI#846 UNUSED.
Latest physical checkpoint: C031-WIP289.
No repository source modified.

NEXT
----
Continue the inherited low-literal-coverage `CvCityAI` sweep under F523 unused. Current
next targets: `AI_neededCultureDefenders`, `AI_estimateReligionBuildings`,
`AI_updateRouteToCity`, remaining route/religion/defender arithmetic and wrong-object
checks, then final semantic inventory/dedup before deciding Queue-005 closure.

END C031-WIP289


================================================================================
C031-WIP290 -- QUEUE 005 DEEP RE-AUDIT -- F523 CONFIRMED + INHERITED HELPER CONTROLS
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under the deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F523 / KI#846 CONFIRMED.
F524 / provisional KI#847 UNUSED.
Authoritative current source: `AdvCiv-SAS_light_source_20260903T200009.zip`.
No repository source files modified.

POST-WIP289 INHERITED-HELPER CONTROLS
-------------------------------------
1. `AI_neededCultureDefenders` was challenged against the current revolt/culture-defense
   arithmetic. No independent divisor/object/sentinel mismatch survived.

2. `AI_updateRouteToCity`'s apparently inverted path condition is intentional: it seeks a
   same-area team city that is NOT already connected by the best currently available route.
   The path predicate therefore expresses the missing-route target rather than a reversed
   success test. No F523.

3. `AI_estimateReligionBuildings` remains approximate but its owner/team orientation is
   coherent with the Apostolic-Palace / religion-building valuation caller. No wrong-owner
   root survived.

4. `AI_getEmphasizeCommerceCount()` is poorly named/typed: it returns
   `(m_aiEmphasizeCommerceCount[eCommerce] > 0)` despite its `int` return type, whereas the
   Yield sibling returns the raw count. Current whole-DLL callers use it only through the
   Boolean `AI_isEmphasizeCommerce` contract (plus nonnegative assertions), and ordinary
   toggling does not expose a consumer that needs magnitude. Preserve as API/clarity debt,
   not F523.

5. `AI_growthValuePerFood` uses `std::partial_sort(..., std::less_equal<int>())` for the
   worst worked jobs. `less_equal` is not a strict weak ordering and is formally invalid for
   the standard sorting contract, especially when equal-valued jobs occur. No deterministic
   current gameplay failure was established in this pass, so preserve as C++ robustness /
   portability debt rather than a numbered root.

6. `AI_countNumImprovableBonuses` can pass `NO_TECH` from a bonus `TechCityTrade=NONE` to
   `CvTeam::isHasTech`, but the team accessor deliberately treats `NO_TECH` as true. Current
   seafood entries therefore exercise a supported sentinel contract rather than an invalid
   lookup. No F523.

7. `AI_neededAirDefenders` can distribute zero local need to every city when all floating-
   defender weights are zero even though the empire-wide desired fighter count is positive.
   This does not make existing `UNITAI_DEFENSE_AIR` units inert: `AI_defenseAirMove` still
   reaches `MISSION_AIRPATROL` whenever interception is possible. Keep as documented
   allocation approximation, not a correctness root.

8. `AI_playerCloseness` is a genuine per-turn cache whose inputs (city set, populations and
   positions) can change later in the same turn through founding/capture. Current proven
   consumers in this pass are heuristic threat/staging/assault weights rather than a hard
   legality/accounting contract, and K-Mod deliberately designed this family as a per-turn
   performance cache. Preserve as a cache-granularity audit note unless a harder consumer is
   found; do not merge it into F515/F516/F519/F520 without a distinct proven consequence.

-------------------------------------------------------------------------------
F523 / KI#846 -- CONFIRMED
K-MOD/BBAI `AI_stealPlots` NEVER RELEASES A SHARED-PLOT WORKING-CITY OVERRIDE WHEN
THE CULTURAL-VICTORY CITY THAT STOLE IT LATER LOSES ITS SPECIAL IMPORTANCE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current gameplay/state-lifetime defect.
Inherited from the BBAI/K-Mod city-importance/plot-stealing feature; present in K-Mod,
Base AdvCiv 1.14 and current AdvCiv-SAS. Stock BtS/Civ4CE does not contain this
`AI_stealPlots` feature.

ROOT
----
Every AI city calls `AI_stealPlots()` during `CvCityAI::AI_doTurn`.

Current `AI_getCityImportance(true, false)` returns a positive value only when:
- the owner is currently at Culture victory stage 3; and
- this city currently ranks within the number of cities required for Cultural Victory.

Current shipped Cultural Victory requires 3 culture cities.

When importance is positive, `AI_stealPlots` may reserve a same-owner shared BFC plot for
this city:

    if (iImportance > pWorkingCity->AI_getCityImportance(true, false))
        kPlot.setWorkingCityOverride(this);

The cleanup, however, is unrelated to the priority that created the override:

    if (kPlot.getWorkingCityOverride() == this &&
        kPlot.getOwner() != getOwner())
    {
        kPlot.setWorkingCityOverride(NULL);
    }

Thus the override is cleared only when plot ownership stops matching the city owner.
If the city later loses Culture3/top-N importance while the plot remains owned, its old
priority reservation is never reconsidered.

PERSISTENT ENGINE CONSEQUENCE
-----------------------------
`CvPlot::setWorkingCityOverride(A)` is authoritative state, not a soft AI preference.
It calls `updateWorkingCity`, which selects the override before the normal/default working
city for a non-city plot:

    pBestCity = getWorkingCityOverride();
    ...
    if (pBestCity == NULL && isOwned())
        pBestCity = defaultWorkingCity();

The result is stored as the plot's actual `workingCity`.

`CvCity::canWork` then begins with:

    if (kPlot.getWorkingCity() != this)
        return false;

Therefore a stale override pointing to city A makes the plot unavailable to city B's
governor even if B would naturally own the shared plot and urgently needs it.

NORMAL CURRENT-DATA REPRODUCTION
--------------------------------
Use one AI player pursuing Culture3 with at least four cities A, B, C, D.
Current Cultural Victory requires the top 3 culture-rate cities.

1. Let A be culture-rate rank 3 and B rank 4.
2. Give A and B a shared same-owner BFC plot P whose natural `defaultWorkingCity()` is B
   (ordinary city-plot priority / founding-order rules can establish this).
3. While A is rank 3, A has positive city importance while B has zero. During `AI_stealPlots`,
   A sees B as P's current working city and calls `setWorkingCityOverride(A)`.
4. Later, keep Culture3 active but let city C's current culture rate overtake A. Rank-cache
   invalidation follows the normal commerce-update path, so A now ranks 4 and
   `AI_getCityImportance(true,false)` correctly becomes 0.
5. On every later A city turn, `iImportance == 0` skips the stealing/recomparison block.
   The only cleanup sees that P is still owned by the same player and leaves the override.
6. B is also non-important and therefore does not reclaim P through `AI_stealPlots`.
7. Arrange A to have better jobs and leave P unworked, while B needs P (e.g. P is a strong
   food tile and B is stagnant/starving). `B.canWork(P)` remains false solely because the
   stale working-city override still points at A.

The lost plot assignment can persist indefinitely. Ordinary culture-rate/rank changes do
not clear it. Current generic cleanup sites are ownership change, city death, and explicit
manual/UI override operations -- none is a normal lifecycle response to the AI importance
that originally created the reservation disappearing.

This is therefore a persistent gameplay-state error, not a one-turn heuristic lag.

ANCESTRY / INTENT
-----------------
K-Mod's initial reachable-history commit (`2414225e6415911ac28483d2bdac67b4100d36ff`,
"Initial commit. K-Mod, 27/dec/10") already contains `AI_stealPlots` with this exact
lifetime shape: set an override when our importance exceeds the current working city's,
but clear it only after ownership changes.

That initial K-Mod source carries the Better BTS AI victory-strategy code and originally
used Culture2 importance.

K-Mod commit `009357153d370c9ca0072d0eee299a36a98bfd6a` (2012-10-31,
"Made a minor tweak to AI_getCityImportance and added some comments") explicitly says
`AI_getCityImportance` is currently used by `AI_stealPlots` "in a very binary way" and
changes its culture gate from Culture2 to Culture3. It does not add any inverse/cleanup
transition when that binary state later returns to zero.

Subsequent K-Mod refactors found in reachable history preserve the same `AI_stealPlots`
state shape. Base AdvCiv and current SAS retain it substantively unchanged.

The feature's own binary-importance model makes the missing inverse transition especially
clear: the code knows when a city is no longer special, but persistent state installed on
the earlier positive transition is not removed.

DEDUP / ROOT SEPARATION
-----------------------
No prior Album entry mentions `AI_stealPlots`, `workingCityOverride`, or this stale
Culture-priority override lifetime.

This is independent of:
- F375 and Worker/shared-BFC accounting findings: those concern custom Worker job/readiness
  accounting and which plots are counted, not persistent CvPlot working-city authority;
- KI#44.5 / KI#70: citizen/specialist assignment legality inside a city;
- F522/KI#845: stale legacy emphasis bits altering job VALUE, not which city is legally
  permitted to work the plot;
- F515/F516/F519/F520: different cached city-site/attitude/safety state families.

F523's root is the missing inverse transition for persistent `CvPlot::workingCityOverride`
state created by `AI_stealPlots`.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not merely add more one-way stealing rules. The AI needs a symmetric lifecycle for its
shared-plot overrides.

At minimum, when a plot override points to `this`, clear it whenever this city's CURRENT
importance no longer strictly beats the competing/natural city's current importance. In
particular, `iImportance <= 0` must release an AI-created cultural-priority override while
ownership remains unchanged.

A more robust design is a player-level/shared-plot recomputation:
- determine current importance for all same-owner cities that can work the plot;
- reserve the plot only for a uniquely/higher important city;
- otherwise clear the override so `defaultWorkingCity()` regains authority.

That avoids sequential city-turn order preserving an obsolete reservation. Be careful not
to introduce blind clearing of human/manual overrides; the repair should apply to AI-owned
AI-managed cultural-priority overrides or structurally recompute AI shared-plot assignment.

REGRESSION TEST
---------------
1. AI player at Culture3 with A rank3, B rank4; A/B share P and defaultWorkingCity(P)=B.
2. Run A `AI_stealPlots`; assert `P.getWorkingCityOverride()==A` and `P.getWorkingCity()==A`.
3. Increase C's culture rate enough that A becomes rank4 while Culture3 remains active.
4. Run subsequent AI city turns.
5. Assert stale-state repair:
       A.AI_getCityImportance(true,false)==0
       P.getWorkingCityOverride()!=A
       P.getWorkingCity()==B  (assuming no other current higher-priority override)
       B.canWork(P)==true.
6. Give A a better alternative job and make B depend on P; assert P can be assigned to B
   rather than remaining unavailable/unworked.

CURRENT STONE AFTER C031-WIP290
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` -- OPEN.
32/169 unique files complete.
F446 / KI#769 through F523 / KI#846 CONFIRMED.
F524 / provisional KI#847 UNUSED.
Latest physical checkpoint: C031-WIP290.
No repository source modified.

NEXT
----
Continue the inherited low-coverage `CvCityAI` sweep under F524 unused. Resume after
`AI_stealPlots` with the remaining live arithmetic/object-identity helpers and the compact
semantic-delta matrix. Keep the `AI_playerCloseness` same-turn cache and the non-strict
`partial_sort` comparator as unnumbered notes unless a stronger current consequence is
proved. Then return to `AI_buildingValue`/production/citizen families only for independent
contracts not already owned by KI#48/KI#53/KI#70/KI#193-KI#198 and finish Queue-005
coverage/dedup before closure.

END C031-WIP290


================================================================================
C031-WIP291 - QUEUE 005 DEEP RE-AUDIT - F524 / KI#847 CONFIRMED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F524 / KI#847 CONFIRMED.
F525 / provisional KI#848 UNUSED.
Current source basis: `AdvCiv-SAS_light_source_20260903T225830.zip`.
Base comparison: AdvCiv 1.14; ancestry comparison: K-Mod reachable history;
stock control: Civ4CE BtS source.
No repository source file was modified.

-------------------------------------------------------------------------------
F524 / KI#847 - CONFIRMED
AI_citizenSacrificeCost COUNTS EACH SPECIALIST TYPE'S YIELDS ONCE WHILE COUNTING
EVERY ASSIGNED SPECIALIST IN THE JOB-LOSS DENOMINATOR, SO MULTIPLE SAME-TYPE
SPECIALISTS CAN TURN A REAL POPULATION-SACRIFICE COST INTO A NEGATIVE SUBSIDY
-------------------------------------------------------------------------------

ROOT
----
`CvCityAI::AI_citizenSacrificeCost` builds two related descriptions of the city's
citizen jobs when estimating the cost of a population hurry:

1. `job_scores` plus `iTotalScore`: which citizens/jobs exist and which of them are
   expected to be lost;
2. `iYields[]`: aggregate output of those jobs, used to estimate the per-turn value
   lost while the city regrows.

Worked plots are accumulated consistently: every worked plot contributes once to
`job_scores`, once to `iTotalScore`, and once to `iYields[]`.

The specialist branch is asymmetric. For each specialist TYPE with one or more
assigned specialists, current code does essentially:

    int iSpecScore = 0;
    ...
    iYields[j] += y;
    ...
    iYields[YIELD_COMMERCE] += c;
    ...
    iTotalScore += iSpecScore * getSpecialistCount(eLoopSpecialist);
    job_scores.resize(job_scores.size() +
            getSpecialistCount(eLoopSpecialist), iSpecScore);

Thus `iTotalScore` and `job_scores` correctly represent N assigned Scientists as N
citizens, but `iYields[]` represents their output as though only ONE Scientist exists.
The mismatch occurs for every specialist type whose assigned count exceeds one.

That is not merely a rough-weight disagreement. `iYields[]` is subsequently treated as
aggregate job output:

    iBaseCostPerTurn += 4 * iYields[j] * AI_yieldMultiplier(...) *
            kOwner.AI_yieldWeight(...) / 100;

and then scaled by the lost-job score fraction:

    iCostPerTurn *= iScoreLoss;
    iCostPerTurn /= max(1, iTotalScore * 100);

So the numerator can omit most specialist output while the denominator and lost-job
fraction still count those omitted specialist citizens. The more same-type specialists
there are, the larger the downward bias in sacrifice cost.

CURRENT-SAS DATA REPRODUCTION
-----------------------------
A compact shipped-data witness exists through Korea's current Library replacement,
`BUILDING_KOREAN_GYEONGDANG`:

- available at Writing;
- +25% Research;
- 3 Scientist slots.

Current `SPECIALIST_SCIENTIST` supplies +3 Research. The sacrifice helper intentionally
uses a rough `*3/2` specialist-output estimate, so each Scientist contributes 4 rough
commerce and a job score of 16 with the helper's commerce weight 4.

Use an ordinary AI city with the following state:

- population 4;
- Slavery active (`HURRY_POPULATION` is enabled in current CIV4CivicInfos.xml);
- Gyeongdang already built;
- three assigned Scientists;
- one ordinary worked plot yielding 4 Food and no other relevant yield;
- city center supplies the inherited 2 Food minimum, so total city Food is 6;
- healthy (`healthRate()==0`);
- happiness surplus `iHappyLevel=2` and no pre-existing hurry anger;
- 100% Research;
- Normal game speed / Noble-like 100% growth factors;
- current early-game yield weights: Food 275, Commerce 100;
- Gyeongdang makes `AI_yieldMultiplier(YIELD_COMMERCE)==125` in the simple
  100%-Research case;
- current production state requires a legal 2-pop population hurry.

A size-4 city may legally sacrifice 2 population because `maxHurryPopulation()` is
population/2 (the current MAX_HURRY_POP define is effectively non-binding at 1000).
The pre-cost "whip to reduce food loss" shortcut does not consume this fixture:
foodDifference is exactly -2 and that shortcut requires `< -iHurryPopulation`, i.e.
strictly less than -2.

The helper's rough job data are:

    worked 4-Food plot score = 4 * 9 = 36
    each Scientist score     = 4 * 4 = 16
    iTotalScore              = 36 + 3*16 = 84
    job_scores               = [36,16,16,16]

For a 2-pop loss, the two lowest jobs are the Scientists. After the helper's flattening
against the average score, each lost job scores 18, so initial `iScoreLoss=36`.

BUGGY aggregate yields:

    iYields = [4 Food, 0 Production, 4 Commerce]

because all three Scientists add their count to `job_scores`/`iTotalScore`, but their
commerce enters `iYields[YIELD_COMMERCE]` only once.

Count-consistent aggregate yields would be:

    iYields = [4 Food, 0 Production, 12 Commerce]

With the current formula and the state above:

    buggy iBaseCostPerTurn     = 6400
    count-aware base cost      = 10400

First regrowth step (2 citizens missing):

    iFoodLoss = 25
    iFoodRate = 0
    buggy iCostPerTurn = -13
    corrected            = +4

Second regrowth step (1 citizen missing):

    iFoodLoss = 27
    iFoodRate = -1
    buggy iCostPerTurn = -7
    corrected            = +2

Total helper result:

    current buggy AI_citizenSacrificeCost = -514   (units of 4x commerce)
    count-aware result                    = +154

`AI_doHurry` then divides the helper result by 4 using C++ integer division:

    current buggy population cost = -128
    count-aware population cost   = +38

So this is not a small underpricing. In a fully current, legal specialist-heavy city,
the missing `* specialistCount` term can reverse the sign: sacrificing two productive
citizens is represented as a 128-commerce BENEFIT instead of a positive cost.

LIVE AI_doHurry CONSEQUENCE
---------------------------
`AI_doHurry` does not clamp a negative `iPopCost` to zero. After adding any gold-hurry
component it forms:

    iHurryCost = iPopCost + iGoldCost;

For a population-only whip in the fixture, `iGoldCost==0`, so the wrong negative value
flows directly into production decisions.

The unit branch ultimately accepts when:

    iHurryCost < iValue + iOverflow

and the building branch first tests:

    iProductionAdded + iOverflow > iHurryCost

then later:

    iValue + iOverflow > iHurryCost

A negative `iHurryCost` therefore makes these gates dramatically easier to satisfy. The
bug can cause a real AI population whip that the same decision with count-consistent
specialist output would reject. The exact choice still depends on the current unit or
building value, but the 166-point difference (-128 versus +38 after `/4`) creates a
large ordinary decision interval where the result flips.

The root is especially dangerous in specialist-heavy cities because the undercount
increases precisely when multiple citizens of one type are assigned -- i.e. when the
population being sacrificed can be economically valuable despite low worked-plot count.

ANCESTRY
--------
This is inherited K-Mod arithmetic, not a SAS regression and not a stock-BtS helper.

K-Mod practical 485 / commit
`1f464176a18621c512e7e9715bacd8b78b83266d` (2012-02-05,
"Heaps of changes to CvCityAI::AI_doHurry -- laying the foundations for the new
slave-whip / hurry method") introduced `AI_citizenLossCost` with the same specialist
asymmetry already present:

    iYields[...] += specialist output                    // once per TYPE
    iTotalScore += iSpecScore * getSpecialistCount(...) // multiplied by COUNT
    job_scores.resize(... + getSpecialistCount(...))    // multiplied by COUNT

K-Mod practical 517 / commit
`c3d1c81aec3c0e8dba0a5c0c5e52111022e453d4` (2012-02-16) renamed the helper to
`AI_citizenSacrificeCost`, added anger/recovery accounting and explicitly "fixed some
mistakes", but retained the same one-per-type `iYields` accumulation while continuing
to multiply `iTotalScore` and `job_scores` by assigned count.

The immediate practical-518 food-component repair (`7143f627e521...`) changed how
`iYields[]` feeds `iBaseCostPerTurn` and food cost; it did not repair specialist
multiplicity. Later AdvCiv iterator/refactor work likewise preserves the semantic
mismatch. Base AdvCiv 1.14 and current SAS still contain it. Civ4CE's stock BtS tree does
not contain `AI_citizenSacrificeCost` / the K-Mod helper.

DEDUP / ROOT SEPARATION
-----------------------
The reconciled album contains no prior root for `AI_citizenSacrificeCost` or this
one-per-specialist-TYPE versus per-specialist-CITIZEN aggregate-yield mismatch.

It is distinct from:

- F324 / KI#647: `AI_civicValue` reuses one finite population pool across multiple
  unlimited-specialist TYPES when valuing Caste System. F524 concerns already-assigned
  multiple citizens of ONE type inside hurry-cost arithmetic.
- F428 / KI#751: UI help for free specialists omits Great-Person points. F524 is an AI
  gameplay decision and counts assigned specialist commerce/yields incorrectly.
- F504 and other `AI_doHurry` findings: those concern stale upgrade-budget/cache inputs;
  F524 is an independent local population-value numerator/denominator contradiction.
- citizen assignment / emphasis roots such as KI#70 and F522: those decide which jobs
  are worked; F524 misprices population loss after the jobs already exist.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Make specialist output multiplicity match the multiplicity already used by
`iTotalScore` and `job_scores`:

    int const iCount = getSpecialistCount(eLoopSpecialist);
    if (iCount <= 0)
        continue;

    ...
    iYields[j] += y * iCount;
    ...
    iYields[YIELD_COMMERCE] += c * iCount;
    ...
    iTotalScore += iSpecScore * iCount;
    job_scores.resize(job_scores.size() + iCount, iSpecScore);

Do not multiply `iSpecScore` itself before inserting jobs; each job entry should remain
one specialist's score. Only the aggregate-yield accumulator is missing the count.

A small local variable also avoids repeated `getSpecialistCount` calls and makes the
contract visually obvious.

REGRESSION TEST
---------------
Use a deterministic specialist-heavy sacrifice-cost fixture, preferably the current
Korean Gyeongdang case above or an equivalent synthetic city state:

1. population 4, 3 same-type Scientists, one 4-Food worked plot;
2. 2-pop legal hurry; healthy, happiness surplus 2, no existing hurry anger;
3. 100% Research, +25% Research city modifier;
4. call `AI_citizenSacrificeCost(2, 2, HURRY_POP_ANGER, hurryAngerLength)`;
5. before repair, reproduce the negative current result (about -514 under the stated
   Normal/Noble baseline);
6. after repair, assert the result is positive (about +154 under the same baseline);
7. add a multiplicity check: increasing assigned same-type specialists must increase
   aggregate specialist output in the cost model rather than changing only the
   `job_scores`/denominator side.

For an integration test, put an `AI_doHurry` unit/building value between the buggy and
corrected thresholds and assert that the negative-cost version hurries while the repaired
version declines.

-------------------------------------------------------------------------------
POST-F524 SHORTLIST CONTROLS
-------------------------------------------------------------------------------

AI_experienceWeight - NEGATIVE-SENTINEL LEAD CLOSED
---------------------------------------------------
Current AdvCiv/SAS intentionally returns `-2` for a city with no production/domain free
experience, with the explicit advc.017 comment that cities without Barracks should be
more hesitant to train units.

A first-pass concern was that `AI_chooseUnit(UnitAI, iOdds)` treats `iOdds < 0` as an
unconditional-build sentinel. That does not form a live inversion here:

- ordinary city military probability uses `AI_buildUnitProb`, which starts from the
  leader BuildUnitProb plus the XP weight and then applies explicit caps/floors;
- when not greatly reduced it raises early military probability to
  `25 - 2*numCities`, and the final scaled probability is capped normally before
  returning an integer percent;
- current real leader XML has positive BuildUnitProb values (the 10-point
  `LEADER_DEFAULTS` template is not an ordinary selected personality; Gandhi is the
  lowest ordinary leader at 25);
- the separate governor-production caller adds `AI_experienceWeight()` to a local odds
  value and calls `SyncRandSuccess100` directly rather than passing that value through
  `AI_chooseUnit`'s negative sentinel.

Thus the `-2` remains a real hesitation as documented, not an accidental unconditional
unit request. No F525.

AI_doDraft - CONSCRIPT POPULATION FORMULA CONTROL
-------------------------------------------------
`AI_doDraft` locally recomputes:

    max(1, conscriptUnitProductionCost / CONSCRIPT_POPULATION_PER_COST)

The authoritative `CvCity::getConscriptPopulation()` uses the same integer formula after
checking for NO_UNIT / zero divisor. `AI_doDraft` has already returned unless
`canConscript()` succeeds, so those exceptional states are excluded before the local
calculation. No population-cost contract mismatch was found.

The remaining draft gates in this pass are explicit strategic policies (unit-spending,
city shrinkage, war/danger, happiness and BuildUnitProb), not hidden arithmetic/object
identity failures. No F525.

AI_citizenValue - CONTROL
-------------------------
The helper remains the small inherited AdvCiv era-scaled heuristic:

    6 + ownerCurrentEra

Its live uses treat the value as a positive weight/cost term. With current nonnegative
era indices there is no zero divisor, sentinel collision or wrong-owner receiver. No
independent root found.

CURRENT STONE AFTER C031-WIP291
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` -- OPEN.
32/169 unique files complete.
F446 / KI#769 through F524 / KI#847 CONFIRMED.
F525 / provisional KI#848 UNUSED.
Latest physical checkpoint: C031-WIP291.
No repository source modified.

NEXT
----
Continue under F525 unused with the remaining inherited live sentinel/divisor/object-
identity checks and final zero-literal-coverage sweep. Revisit the non-strict
`AI_growthValuePerFood` `partial_sort` comparator and `AI_playerCloseness` same-turn cache
only if a stronger deterministic current consequence appears. Then complete semantic
coverage/dedup for Queue 005 and decide whether `CvCityAI.cpp` is genuinely saturated or
whether another independent root survives.

END C031-WIP291


================================================================================
C031-WIP292 — QUEUE 005 DEEP RE-AUDIT — KI#195 BARBARIAN WORK-BOAT COUNTING REOPENED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F524 / KI#847 remain CONFIRMED.
F525 / provisional KI#848 remains UNUSED.
No repository source files modified.

WHY THE POST-F524 LOW-COVERAGE PASS REOPENED KI#195
---------------------------------------------------
The low-covered SAS delta in `AI_barbChooseProduction` had previously been stamped as the known
KI#195 Barbarian Work-Boat accounting repair. Directly opening the helper used by that repair now
shows that the second half of KI#195 rests on an incorrect helper contract.

Current SAS says:

    int const iWaterAreaSeaWorkers = (pProductionWaterArea == NULL ? 0 :
        kPlayer.AI_totalWaterAreaUnitAIs(*pProductionWaterArea, UNITAI_WORKER_SEA));
    int const iExistingSeaWorkers = iWaterAreaSeaWorkers +
        kPlot.plotCount(PUF_isUnitAIType, UNITAI_WORKER_SEA, -1, getOwner()) +
        getNumTrainUnitAI(UNITAI_WORKER_SEA);

The accompanying KI#195 comment says `AI_totalWaterAreaUnitAIs` finds docked/queued ships by
iterating major-civilization cities and therefore misses the Barbarian player's own cities. It
also says the intended composition is:

- Work Boats already sailing in the relevant water area, plus
- Work Boats docked or training in THIS Barbarian city,
- while docked/training boats in unrelated Barbarian cities must not suppress this city's local
  demand.

The actual helper does something different.

ACTUAL `AI_totalWaterAreaUnitAIs` CONTRACT
-----------------------------------------
Current `CvPlayerAI::AI_totalWaterAreaUnitAIs` first calls `AI_totalAreaUnitAIs(kArea, ...)`, then:

    for (PlayerIter<ALIVE> it; it.hasNext(); ++it)
    {
        CvPlayer const& kOwner = *it;
        ...
        FOR_EACH_CITY(pLoopCity, kOwner)
        {
            if (pLoopCity->waterArea() != &kArea)
                continue;
            iCount += pLoopCity->getPlot().plotCount(
                PUF_isUnitAIType, eUnitAI, -1, getID());
            if (kOwner.getID() == getID())
                iCount += pLoopCity->getNumTrainUnitAI(eUnitAI);
        }
    }

`PlayerIter<ALIVE>` is explicitly NOT major-only. `AgentIterator.h` uses the `CIV_ALIVE` cache for
this template specialization and `bADD_BARBARIANS` adds the Barbarian agent because `ALIVE <
CIV_ALIVE`. `ExplicitAgentIterator::passFilters` for `ALIVE` only requires `isAlive()`; it does not
exclude Barbarians. K-Mod's pre-AgentIterator version is even more explicit: it loops every
`iI < MAX_PLAYERS`, checks `GET_PLAYER(iI).isAlive()`, then iterates that player's cities.

Therefore an alive Barbarian player's own coastal cities ARE in this helper. For each own city on
the tested water area it already counts:

1. own `UNITAI_WORKER_SEA` units docked on that city plot, and
2. own `UNITAI_WORKER_SEA` training orders in that city.

So the KI#195 statement that Base AdvCiv's helper only scans major-civilization cities is false.
The helper inherited from K-Mod/AdvCiv was specifically written to scan all alive players' cities.

WHAT THE CURRENT KI#195 ADDITION ACTUALLY DOES
----------------------------------------------
In the ordinary case where `waterArea(true)` equals the city's normal `waterArea()`, the helper
already includes this city's docked and queued Work Boats. The extra:

    + kPlot.plotCount(...UNITAI_WORKER_SEA...)
    + getNumTrainUnitAI(UNITAI_WORKER_SEA)

therefore counts those current-city objects a second time.

That duplicate does not by itself change the present `iExistingSeaWorkers <= 0` Boolean test: a
positive count stays positive. It is nevertheless proof that the implemented formula does not
match its documented decomposition.

More importantly, `AI_totalWaterAreaUnitAIs` also counts docked and queued Barbarian Work Boats in
OTHER Barbarian cities whose ordinary `waterArea()` is the same `kArea`. Therefore the current
code does NOT satisfy KI#195's explicit local-independence contract. A queued/docked boat in city A
can make `iExistingSeaWorkers > 0` in city B even when B has its own positive city-local
`AI_neededSeaWorkers()` demand.

The helper's initial `AI_totalAreaUnitAIs(kArea, UNITAI_WORKER_SEA)` also counts Work Boats physically
in the water area, so a sailing Work Boat suppresses another production request as intended by the
KI#195 text. The problem is that `AI_totalWaterAreaUnitAIs` adds city-based dock/queue accounting on
top of that; it is not a "sailing boats only" primitive.

EDGE CASE THAT EXPLAINS WHY THE EXTRA LOCAL TERMS CAN SOMETIMES MATTER
---------------------------------------------------------------------
`AI_barbChooseProduction` passes `waterArea(true)` to `AI_totalWaterAreaUnitAIs`, while that helper
matches cities using `pLoopCity->waterArea()` with the default `bNoImpassable=false`.

`CvPlot::waterArea(true)` excludes impassable adjacent water plots when choosing the largest water
area; `waterArea()` does not. If an impassable-Ice-adjacent larger sea and an accessible smaller sea
compete, a city's `waterArea(true)` and `waterArea()` can differ. In that case the helper may indeed
miss that city's docked/queued unit for the accessible production water area. Adding the current
city's plot/queue terms repairs THAT mismatch. It does not establish the broad "Barbarian cities
are absent from the helper" diagnosis recorded by KI#195.

This also connects naturally to the later KI#216 discovery about `waterArea(true)` / secondary-area
identity; water-area accessor semantics matter here and are not interchangeable.

CURRENT GAMEPLAY CONSEQUENCE / CLEAN FIXTURE
--------------------------------------------
Use two alive Barbarian coastal cities A and B sharing the same ordinary accessible water area.
Give each city one owned, buildable, reachable unimproved seafood plot assigned to its own BFC, so:

    A.AI_neededSeaWorkers() == 1
    B.AI_neededSeaWorkers() == 1

Let A already have one `UNITAI_WORKER_SEA` training order and let B have no local docked or queued
boat.

When B evaluates its KI#195 production branch, `AI_totalWaterAreaUnitAIs` visits A because A is an
alive Barbarian city on the same water area and adds A's training order. Therefore B sees
`iExistingSeaWorkers > 0` and skips its local Work Boat even though B's own local demand is valid.
The same happens if A's boat is docked. Once the boat is physically at sea, the initial area count
also suppresses B until the boat is consumed or otherwise leaves the relevant UnitAI/area.

Thus the current branch can serialize local Barbarian seafood servicing across a shared sea rather
than allowing the per-city demand model promised by KI#195. With several Barbarian cities/targets
on one sea this can delay otherwise parallel improvements substantially.

ANCESTRY / SOURCE CROSS-CHECK
-----------------------------
- Current SAS and Base AdvCiv 1.14 both have the all-alive-city
  `AI_totalWaterAreaUnitAIs` helper.
- Current K-Mod has the same semantics in older explicit form: loop `iI=0..MAX_PLAYERS`, require
  `isAlive()`, count docked units in every city on the water area, and count queues in the current
  player's cities.
- KI#195 was introduced by SAS practical 6074 / commit
  `f03333ae1541517390a59da1afd60d73841f74da` (2026-07-22). Its patch adds the incorrect
  major-civilization-only comment and combines `AI_totalWaterAreaUnitAIs` with extra local
  plot/queue counts.
- The first half of KI#195 remains valid: Base AdvCiv's Barbarian `AI_neededSeaWorkers` used only
  `FOR_EACH_ADJ_PLOT`, so outer BFC seafood demand really was omitted. This reopening concerns the
  second "docked/queued boats" diagnosis/cure only.

NATURAL REPAIR DIRECTION
------------------------
If the intended KI#195 policy is exactly the one its docs state — boats physically sailing in the
relevant water area + docked/queued boats in the CURRENT Barbarian city, but not unrelated
Barbarian-city dock/queues — then `AI_totalWaterAreaUnitAIs` is the wrong primitive.

The natural existing primitive for the first component is:

    kPlayer.AI_totalAreaUnitAIs(*pProductionWaterArea, UNITAI_WORKER_SEA)

For a water area this counts sea units physically in that area (and area-attributed training, if
any), without the extra cross-city `waterArea()` scan that `AI_totalWaterAreaUnitAIs` performs.
Then add the current city's docked and queued counts explicitly, as KI#195 already does.

The exact final patch should still be validated against area-attributed training semantics and the
Ice/waterArea(true) edge before changing repository source. The important audit result is the
helper-contract mismatch, not a premature patch.

CLASSIFICATION
--------------
Do NOT spend F525 on this. It is not a genuinely independent Queue-005 root: it reopens the exact
Barbarian Work-Boat availability/accounting feature already owned by KI#195. Treat it as a KI#195
second-half diagnosis/cure correction, analogous to earlier broadening/repair cases where the next
F-number stayed unused.

The prior WIP286 sentence calling `AI_barbChooseProduction`'s semantic addition a known KI#195
repair is therefore superseded by this deeper helper-contract audit.

POST-F524 CONTROL PROGRESS
--------------------------
The same tranche also rechecked several low/zero-coverage methods without finding an independent
F525:

- `AI_splitEmpireValue`: current SAS differs from Base only by const/reference spelling; no semantic
  SAS delta.
- `AI_specialPlotImprovementValue`: cached owner/team references only; upgrade-time divisor is
  guarded by `max(1, ...)`.
- `AI_chooseProcess`: material SAS difference is logging; selection semantics unchanged.
- `AI_defensiveBuildingValue`: current changes are cached game/player/team references; nuke-danger
  divisor is guaranteed positive (1/10/15/20) by `CvPlayerAI::AI_nukeDangerDivisor`.
- `AI_clearFeatureValue`, `AI_countGoodSpecialists`, `AI_removeWorstCitizen`: SAS deltas are cached
  references/static default-specialist lookup only.
- `AI_projectValue`: sampled-tech sentinel is protected by `isHasTech(NO_TECH)` semantics; reviewed
  project/victory divisors did not expose a live zero divisor in current data/contracts.
- `AI_cityThreat`: the old K-Mod comment saying rival attitude "should probably be their attitude"
  is already repaired by AdvCiv advc.022; current code computes `eTowardUs` and switches on it.
- `AI_growthValuePerFood`: the inherited `std::less_equal<int>` comparator in `partial_sort` remains
  a formal strict-weak-ordering defect/note, but no deterministic current numeric gameplay failure
  has yet been established; do not spend F525 on formal STL-contract pedantry alone.

DURABLE STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN · 32/169 complete · F446-F524 confirmed ·
F525/KI#848 UNUSED · WIP292 latest physical checkpoint · repository source untouched.

NEXT CURSOR
-----------
Continue under F525 unused:
1. finish remaining low-covered inherited/current-delta methods and explicit wrong-receiver loops;
2. prioritize persistent state, wrong enum/ID domains, sentinel ordering and hard arithmetic;
3. retain KI#195 as reopened/broadened unless a truly separate Work-Boat root appears;
4. revisit the `AI_growthValuePerFood` non-strict comparator only if a concrete MSVC/current-game
   consequence can be demonstrated;
5. decide whether Queue 005 has saturated or whether one more independent root survives.

END C031-WIP292

================================================================================
WIP293 - Queue 005 CvCityAI.cpp continuation under F525/KI#848 UNUSED
================================================================================

Durable state at this checkpoint
--------------------------------
- Queue 005: CvGameCoreDLL/CvCityAI.cpp OPEN (deep re-audit).
- 32/169 unique files complete.
- F446/KI#769 through F524/KI#847 remain confirmed.
- F525 / provisional KI#848 remains UNUSED.
- KI#195 remains reopened/broadened from WIP292.
- Repository source remains untouched.

F525 arithmetic control - losing-war production odds denominator is safe
------------------------------------------------------------------------
Current AI_chooseProduction contains the losing-war adjustment:

    int iOdds = iBuildUnitProb;
    if (iWarSuccessRating < -50)
        iOdds -= iWarSuccessRating / 3;

    iOdds *= (-iWarSuccessRating + 20 + iBestBuildingValue);
    iOdds /= (-iWarSuccessRating + 2 * iBestBuildingValue);

This initially deserved a divisor/sign audit. The live branch requires
`iWarSuccessRating < -10`. CvTeamAI::AI_getWarSuccessRating() clamps its result
to [-99, 99]. `iBestBuildingValue` is produced by AI_bestBuildingThreshold();
that helper rejects candidate buildings whose underlying AI_buildingValue is
<= 0 and only positively rescales accepted values. Consequently the denominator
is at least 11 in the live branch and cannot be zero or negative. The numerator
is likewise positive. No divide-by-zero, sign inversion or negative-sentinel
failure is present here.

F525 false positive killed - GET_TEAM(getOwner()) is type-safe by design
------------------------------------------------------------------------
Several inherited sites initially looked like a very strong PlayerTypes /
TeamTypes domain bug, e.g. AI_bestProject and AI_bestUnit use:

    GET_TEAM(getOwner())

`CvCity::getOwner()` returns PlayerTypes, and a superficial reading of GET_TEAM
would therefore suggest that the player slot is being mistaken for a team slot.
That reading is WRONG in current AdvCiv/SAS. CoreAI.h defines GET_TEAM(x) through
an overloaded CoreAI::getTeam, including a PlayerTypes overload which converts
through TEAMID(ePlayer):

    CoreAI::getTeam(TeamTypes)
    CoreAI::getTeam(PlayerTypes) -> CvTeamAI::AI_getTeam(TEAMID(ePlayer))

History confirms this was deliberate preservation of the old TEAMREF(ePlayer)
convenience when practical 1679 removed/refactored the old macro layer. Thus the
naval-terrain-trade, capitulation/unit, World-Wonder and project sites all resolve
the owner's actual team correctly even when player ID != team ID. Do NOT report
or rediscover these as wrong-ID bugs.

Inherited chop-value arithmetic note - advertised relative-production boost dead
-------------------------------------------------------------------------------
AI_bestPlotBuild contains inherited K-Mod arithmetic:

    // Increase the value if it is large compared to city's natural production rate
    // note iValue = chop production * 10
    iValue += iValue / (getBaseYieldRate(YIELD_PRODUCTION) * 20 + iValue);

For positive chop `iValue` and positive base production, the denominator is
strictly greater than iValue, so integer division contributes exactly 0. If base
production is zero, it contributes exactly 1. Therefore the source-described
"large compared to natural production" boost is effectively dead. This is a
real upstream arithmetic defect/robustness note, but current SAS's authoritative
land Worker planning bypasses this legacy evaluator. No sufficiently independent
current gameplay consequence has yet justified F525.

KI#213 reconfirmed - Advanced Start still consumes disabled legacy land cache
-----------------------------------------------------------------------------
Following that legacy best-build cache into its consumers reconfirmed an existing
cross-file architecture root rather than a new finding. SAS deliberately makes
AI_getImprovementValue return 0 / NO_BUILD for every land plot so that the custom
CvUnitAI::AI_bestCityBuild system owns ordinary land Worker planning. Advanced
Start still calls AI_updateBestBuild and purchases improvements through
AI_getBestBuildValue / AI_getBestBuild. Therefore custom land improvement policy
is invisible to Advanced Start. This exact disconnect is already owned by KI#213
in the reconciled album; do not duplicate it as F525. (Advanced Start F370-F372
are separate already-confirmed action/termination bugs.)

F525 control - AI_isAirDefended ignores bCountLand deliberately
---------------------------------------------------------------
Current helper:

    bool CvCityAI::AI_isAirDefended(bool bCountLand, int iExtra) const
    {
        return (getPlot().plotCount(PUF_isAirIntercept, -1, -1, getOwner()) +
                iExtra >= AI_neededAirDefenders());
    }

The only live current caller found is CvUnitAI::AI_defenseAirMove, which invokes
`AI_isAirDefended(true, -1)` while deciding whether the current aircraft must
remain on Air Patrol under a moderate direct land threat. BBAI's disabled helper
once used bCountLand to add land-unit interception probability. K-Mod explicitly
reverted AI_isAirDefended to simple aircraft counting because its redesigned
AI_neededAirDefenders was not suitable for the BBAI probability-based version,
retained the bool parameter only to match BBAI usage, and AdvCiv documents:
"Either way, only aircraft are currently counted." This is acknowledged design /
architectural debt, not an accidental current regression.

Exact continuation cursor
-------------------------
F525/KI#848 UNUSED -> continue zero/low-literal inherited helper sweep -> focus
on independent arithmetic/divisor/sentinel/object-identity and persistent-state
errors -> dedup every legacy best-build/cache consequence against KI#213 ->
promote only if a live independent current root survives.


================================================================================
WIP294 - Queue 005 CvCityAI.cpp - F525 / KI#848 CONFIRMED
================================================================================

Durable state at this checkpoint
--------------------------------
- Queue 005: CvGameCoreDLL/CvCityAI.cpp OPEN (deep re-audit).
- 32/169 unique files complete.
- F446/KI#769 through F525/KI#848 CONFIRMED.
- F526 / provisional KI#849 is the next UNUSED root.
- KI#195 remains reopened/broadened from WIP292.
- Repository source remains untouched.

Post-WIP293 control - AI_bestProject capitulation early return violates its
out-parameter convention but has no demonstrated gameplay consequence
---------------------------------------------------------------------------
AI_bestProject(int* piBestValue, ...) normally writes *piBestValue even when
there is no best project; its own exit comment explicitly documents that
convention. The advc.014 capitulated-vassal early return happens before the
write:

    if (GET_TEAM(getOwner()).isCapitulated())
        return NO_PROJECT;

Thus an input value such as AI_chooseProduction's -1 "not calculated" sentinel
survives the call rather than becoming the normal no-project value 0. This is a
real API-contract/performance blemish, but all current live consumers close
safely:

- callers that need a concrete project gate value use on eBestProject != NO_PROJECT;
- AI_chooseProduction's intermediate weighting uses
  max(iProjectValue, iBestBuildingValue), and iBestBuildingValue is guaranteed
  nonnegative, so max(-1, B) == max(0, B);
- the late `if (iProjectValue < 0)` path merely calls AI_bestProject again for a
  capitulated city and receives NO_PROJECT again; the subsequent project-value
  comparison remains false.

No current state-changing consequence justifies a finding. Retain as inherited
out-parameter/performance debt; F525 was not spent here.

Post-WIP293 state-mutation / settler controls
---------------------------------------------
A low-coverage state-mutation pass also killed two plausible false positives:

1. AI_forceEmphasizeCulture is commented-out legacy code, and the live strong-
   emphasis state is reset when emphasis settings change; no F523-like permanent
   state twin was found there.

2. Current SAS AI_bestUnit unconditionally zeros UNITAI_SETTLE (and initially
   UNITAI_SETTLER_SEA) after practical 4932 removed the old bWarPossible scope.
   History initially made this look like a SAS refactor regression because
   practical 4925 had placed those zeroes inside `if (bWarPossible)`, while
   practical 4932 commented/removed that almost-always-true condition and left
   the land-Settler zero behind. The apparent human-governor/recommendation
   consequence is not a regression: Base AdvCiv/K-Mod separately and explicitly
   zero UNITAI_SETTLE and UNITAI_WORKER for human players because humans do not
   choose UnitAI roles. For civilization AI, later SAS production architecture
   deliberately centralizes Settlers in dedicated AI_chooseProduction ->
   AI_chooseUnit(UNITAI_SETTLE) gates so generic AI_bestUnit paths cannot bypass
   the custom site/economic/size policy. The nearby old comment is misleading,
   but no independent F525 root survives.

F525 / KI#848 - K-Mod human auto-production governor validates one XP building
but queues a different building variable
------------------------------------------------------------------------------
STATUS: CONFIRMED CURRENT BUG (inherited K-Mod -> AdvCiv -> SAS)

CURRENT ROOT
------------
In CvCityAI::AI_buildGovernorChooseProduction, the military branch first picks
its desired unit and then deliberately searches for the best experience-focused
building for that exact unit:

    BuildingTypes eExperienceBuilding = AI_bestBuildingThreshold(
            BUILDINGFOCUS_EXPERIENCE);
    if (eExperienceBuilding != NO_BUILDING)
    {
        const CvBuildingInfo& kBuilding = GC.getInfo(eExperienceBuilding);
        if (kBuilding.getFreeExperience() > 0 ||
            kBuilding.getUnitCombatFreeExperience(kUnit.getUnitCombatType()) > 0 ||
            kBuilding.getDomainFreeExperience(kUnit.getDomainType()) > 0)
        {
            // This building helps the unit we want
            // ...so do the building first.
            pushOrder(ORDER_CONSTRUCT, eBestBuilding); // WRONG OBJECT
            return;
        }
    }

The object whose effects were tested is `eExperienceBuilding`; the object queued
is the earlier, unrelated general-purpose `eBestBuilding` computed at the top of
the governor. The branch therefore establishes a property about building X and
then acts on building Y.

HARD CONTRACT / INVARIANT
-------------------------
Once the branch has selected `eExperienceBuilding` and verified that this same
building grants free experience to the chosen unit's exact unit-combat or domain,
the ORDER_CONSTRUCT target must be `eExperienceBuilding`.

This is not a tuning preference. The branch comments explicitly say "This
building helps the unit we want ... so do the building first," and all three
predicates immediately above refer to the local `kBuilding` obtained from
`eExperienceBuilding`.

LIVE CURRENT CONSEQUENCE
------------------------
AI_buildGovernorChooseProduction is the production-automation governor for human
cities. CvCity::doProduction calls AI_chooseProduction for a human city when
`isProductionAutomated()` is true; AI_chooseProduction then dispatches human
cities directly to AI_buildGovernorChooseProduction.

The two building searches are intentionally different and can normally disagree:

- `eBestBuilding = AI_bestBuildingThreshold()` performs the ordinary all-role
  building evaluation;
- `eExperienceBuilding = AI_bestBuildingThreshold(BUILDINGFOCUS_EXPERIENCE)`
  uses the focus path, whose first pass specifically values XP effects and can
  reject buildings with no experience contribution before the ordinary second
  pass.

Current SAS supplies straightforward live data for the distinction. For example,
BUILDING_BARRACKS is a normal buildable 50-hammer Hunting building with +3 land
DomainFreeExperience, while ordinary economic buildings such as the 45-hammer
Granary have strong non-XP value (40% food kept) but no XP. In any ordinary
production-automated human city state where the general building scorer ranks a
non-XP building above Barracks while the focused scorer selects Barracks for the
chosen land combat unit, a successful military-governor roll reaches this block,
validates Barracks, and then queues the unrelated general building instead.

The consequence is state-changing and persistent until the next production
choice: automated production delays the military unit for the wrong building,
while the very XP building that justified the delay is not what was ordered.
This can recur every time the governor revisits the same decision state.

The defect does not require `eBestBuilding` to be invalid. The common and clean
failure is simply X != Y: an economic/general building is legal and valuable, an
XP building is also legal and specifically useful for the chosen unit, and the
branch queues the former after proving the latter.

ANCESTRY / BIRTH BOUNDARY
-------------------------
The wrong variable is born cleanly in K-Mod practical 251,
commit ce5a1952a160a27345e555e640c6c5be9d7b3bb2,
titled:

    "Many and various numerical adjustments. Changed the way the AI refuses
     peace when going for conquest - again. (the previous method also prevented
     capitualtion)"

Immediately before that commit, the governor's military branch simply did:

    if (AI_chooseBuilding(BUILDINGFOCUS_EXPERIENCE, 10))
        return;
    if (AI_chooseUnit(NO_UNITAI))
        return;

Practical 251 deliberately made this more selective: it first chose the exact
best unit, introduced the new local `eExperienceBuilding`, checked whether that
building actually grants XP applicable to that unit, and intended to build it
before the unit. But the newly introduced success block accidentally retained
`eBestBuilding` as the order target:

    + BuildingTypes eExperienceBuilding = AI_bestBuildingThreshold(...);
    + ... inspect eExperienceBuilding ...
    + pushOrder(ORDER_CONSTRUCT, eBestBuilding, ...);

That is a textbook variable-substitution typo introduced in the feature's birth,
not an inferred strategy disagreement.

K-Mod practical 365, commit 5dc0ad28676fbf3aeb3e52d551fb20a28a275dbb,
was specifically titled:

    "Fixed a rare bug which could potentially cause the auto-production governor
     to crash the game."

It added the current `kUnit.getUnitCombatType() != NO_UNITCOMBAT` guard around
this exact block, but retained the wrong `eBestBuilding` queue target. Thus the
nearby crash was repaired while this separate wrong-object bug survived.

UPSTREAM / CURRENT BOUNDARY
---------------------------
- K-Mod current source retains the wrong `eBestBuilding` target.
- Base AdvCiv 1.14 retains the same wrong target.
- Current AdvCiv-SAS retains the same wrong target; SAS changes in this function
  are diagnostics/caching and do not repair the object mismatch.
- Stock BtS / current Civ4CE BtS does not contain this K-Mod targeted-unit /
  targeted-XP-building rewrite; its older governor directly calls an experience-
  focused building chooser. Therefore this exact root begins in K-Mod, not stock
  Civ4 BtS.

DUPLICATE / SEMANTIC COLLISION CHECK
------------------------------------
The reconciled album was searched for `eExperienceBuilding`, "helps the unit",
human governor, production automation, wrong building and related order-target
language. Existing human/AI production findings (including F494's Auto Play
Culture-process metadata transition) concern different state and ownership
contracts. No prior finding owns "validate experience building X, queue general
building Y."

This is also distinct from KI#53.3's post-selection unit veto/no-second-best
issue: F525 is not a failed retry policy. The branch has already selected and
validated the intended building; it simply passes the wrong variable to
pushOrder.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The surgical repair is the one-token semantic substitution:

    pushOrder(ORDER_CONSTRUCT, eExperienceBuilding);

Preserve all existing branch conditions, unit-combat guard, building predicates,
RNG timing and the subsequent unit order. No broader governor rewrite is needed
to fix this root.

REGRESSION TESTS
----------------
A. Distinct-building target
1. Human city with production automation enabled and ordinary primary-area
   military-governor conditions satisfied.
2. Make a legal land combat unit the result of AI_bestUnit.
3. Ensure the ordinary `AI_bestBuildingThreshold()` result is a non-XP building
   X while `AI_bestBuildingThreshold(BUILDINGFOCUS_EXPERIENCE)` returns a
   different building Y that grants XP to that unit (Barracks is a current land
   example).
4. Let the military-governor roll pass.
5. Assert the new head order is ORDER_CONSTRUCT Y, never X.

B. Same-building control
1. Arrange the same branch with general best building X == experience building Y.
2. Assert behavior is unchanged by the repair.

C. No-applicable-XP-building control
1. Let eExperienceBuilding be NO_BUILDING, or let its XP not apply to the chosen
   unit.
2. Assert the governor falls through and queues the chosen unit as before.

D. NO_UNITCOMBAT crash-fix control
1. Use a best unit with NO_UNITCOMBAT if a legal test fixture can expose one.
2. Assert practical-365's guard continues to skip the XP-building inspection and
   no invalid UnitCombat lookup is reintroduced.

NUMBERING / CURRENT STONE
-------------------------
F525 / KI#848 is CONFIRMED.
F526 / provisional KI#849 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP294 (this file).

NEXT
----
Continue under F526 unused through the remaining low/zero-coverage inherited
helpers, prioritizing wrong-object/receiver variables, state lifecycle, hard
sentinel/divisor contracts and later K-Mod/AdvCiv bug-fix neighborhoods. Keep
legacy best-build/cache consequences deduplicated against KI#213 and promote only
a live independent current root.

END C031-WIP294


================================================================================
WIP295 - Queue 005 CvCityAI.cpp - F526 / KI#849 CONFIRMED
================================================================================

HANDOFF / NUMBERING
-------------------
- Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
- F446/KI#769 through F526/KI#849 CONFIRMED.
- F527 / provisional KI#850 is the next UNUSED root.
- 32/169 unique files complete.
- Repository source untouched.
- Local light-source/Base AdvCiv/K-Mod/Civ4CE material only; no web lookup used.

F526 / KI#849 - AI_neededFloatingDefenders cache ignores bIgnoreCulture,
so AIR->LAND call order can feed culture-inclusive floating demand into the
culture-excluding AI_neededDefenders calculation
--------------------------------------------------------------------------------

ROOT
----
Current Base AdvCiv 1.14 and SAS expose two semantically different calculations
through the same helper:

    int AI_neededFloatingDefenders(
        bool bIgnoreEvac = false,
        bool bConstCache = false,
        bool bIgnoreCulture = false) const;

`AI_calculateNeededFloatingDefenders` first computes the ordinary floating-defender
share F. It then conditionally applies the culture-garrison floor:

    if (!bIgnoreCulture)
    {
        int iCultureDefendersNeeded =
            AI_neededCultureDefenders() - AI_minDefenders();
        iFloatingDefenders = std::max(
            iFloatingDefenders, iCultureDefendersNeeded);
    }

Thus, for the same city and game state, there are legitimately two answers:

    ignore-culture answer  = F
    include-culture answer = max(F, C - M)

where C = AI_neededCultureDefenders() and M = AI_minDefenders().

But the cache has only one value and one key component:

    m_iNeededFloatingDefenders
    m_iNeededFloatingDefendersCacheTurn

and lookup is only:

    if (m_iNeededFloatingDefendersCacheTurn != kGame.getGameTurn())
        calculate(... bIgnoreCulture ...);
    return m_iNeededFloatingDefenders;

`bIgnoreCulture` is not part of the cache identity. Therefore whichever semantic
variant first performs a non-const calculation in a turn becomes the answer for
both variants for the rest of that cache lifetime.

`bIgnoreEvac` does not create the same problem: evacuation is checked before the
cache and a non-ignored evacuating city returns 0 immediately. `bConstCache` is a
cache-write policy, not part of the mathematical value. The missing semantic key
is specifically `bIgnoreCulture`.

WHY AI_neededDefenders DELIBERATELY REQUESTS THE OTHER VALUE
------------------------------------------------------------
`AI_neededDefenders` calls:

    int iNeededFloating = AI_neededFloatingDefenders(
        bIgnoreEvac, bConstCache, true);

The comment at that call is explicit:

    // To save time (avoid calling AI_neededCultureDefenders 2x)

The function later applies the culture requirement exactly once as the final floor:

    iDefenders = std::max(iDefenders, AI_neededCultureDefenders());

So this is not an interchangeable approximation. The `true` call intentionally
means: return floating demand WITHOUT the culture component, because culture will
be applied separately at the end.

If the cache instead supplies the default include-culture answer, the culture
requirement is partly added into `iDefenders` and then applied again as a floor.
That changes the result whenever non-culture defender terms already put the city
above its minimum-defender baseline.

DETERMINISTIC NORMAL-TURN CALL ORDER
------------------------------------
The strongest reproduction does not rely on an uncertain caller ordering.

For ordinary sequential turns, `CvPlayer::setTurnActive(true, bDoTurn=true)` runs:

    doTurnUnits();

and `CvPlayer::doTurnUnits()` processes unit domains in fixed passes:

    pass 1: DOMAIN_AIR
    pass 2: DOMAIN_SEA
    pass 3: DOMAIN_LAND

A `UNITAI_DEFENSE_AIR` executes `CvUnitAI::AI_defenseAirMove`. In an ordinary
valid city it calls:

    pCity->AI_neededAirDefenders();

with default `bConstCache=false`.

`AI_neededAirDefenders` then loops over EVERY city owned by the player and calls:

    pLoopCity->AI_neededFloatingDefenders(false, bConstCache);

The third argument defaults to `bIgnoreCulture=false`. Consequently one defensive
aircraft can populate the shared floating-defender cache of every city with the
CULTURE-INCLUSIVE answer during the AIR pass.

The LAND pass follows later in the same `doTurnUnits`. A normal city-defense unit
runs `AI_cityDefenseMove()`, then `AI_guardCityMinDefender(true)`. Its city search
calls:

    pLoopCity->AI_neededDefenders(true);

`AI_neededDefenders` requests `AI_neededFloatingDefenders(..., true)` -- the
CULTURE-EXCLUDING answer -- but the game-turn cache is already valid from the air
pass. No recalculation occurs and the culture-inclusive value is returned instead.

This is therefore a normal, deterministic AIR -> LAND contamination sequence for
an AI player that owns a defensive aircraft. The aircraft does not even have to be
based in the affected culture city, because `AI_neededAirDefenders` scans all of
the player's cities and can prime all of their floating-defender caches.

CONCRETE GARRISON ARITHMETIC FLIP
---------------------------------
Use a legal culture-pressured city state with:

    ordinary floating share F                         = 0
    AI_minDefenders M                                 = 2
    AI_neededCultureDefenders C                       = 5

and ordinary non-culture city-defense conditions that make the other defender
terms total 3 by the time the final culture floor is applied. One simple shape is
an important city (capital / active World Wonder / Holy City adds one defender)
plus the ordinary flat-city defensive-war increment. The exact identities are not
important; the current formula contains several independent ways for the
non-culture baseline to exceed M.

Correct `AI_neededDefenders` path:

    ignore-culture floating = F = 0
    non-culture defender total = 3
    final culture floor = max(3, C=5)
    correct need = 5

AIR-primed contaminated path:

    cached include-culture floating
        = max(F=0, C-M = 5-2)
        = 3

    the same non-culture terms now total 6
    final culture floor = max(6, C=5)
    contaminated need = 6

So a city that should request 5 defenders can request 6 solely because an air-unit
calculation happened first. This is a hard parameter/cache-contract error, not a
strategy preference.

The general failure condition is straightforward. Let B be the non-floating,
non-culture defender contribution at the final culture-floor point. If:

    C - M > F
    and B > M

then reusing the include-culture floating value inside the ignore-culture path can
make the culture requirement additive above its intended floor.

LIVE CONSEQUENCES
-----------------
The wrong result is consumed in ordinary land-unit allocation immediately in the
same LAND pass:

- `AI_guardCityMinDefender(true)` compares current defenders with
  `AI_neededDefenders(true)` and can pull an unnecessary extra defender toward the
  contaminated city.
- `AI_guardCity(...)` also uses `AI_neededDefenders` for current-city stay/leave
  decisions and candidate-city shortfalls.
- Additional `CvUnitAI` and `CvPlayerAI` callers use the same required-defender
  count for reinforcement and city-defense allocation.

The opposite contamination direction is also possible in other call orders: if
`AI_neededDefenders` first stores the ignore-culture answer F, a later ordinary
`AI_neededFloatingDefenders(..., false)` or `AI_neededAirDefenders` call receives
F and loses the culture-pressure component. That can underweight defensive-air
allocation to culture-sensitive cities. The deterministic AIR-before-LAND witness
above is sufficient by itself; this reverse direction shows that the cache cannot
correctly represent both API modes in either order.

ANCESTRY / BIRTH
----------------
K-Mod and stock BtS/Civ4CE have only one semantic floating-defender calculation,
so their game-turn cache does not have this parameter-key problem.

Base AdvCiv practical 1902, commit:

    9460039260f5d8a152616c7396607845b9ad8868

Title:

    "Performance tweak to the AI culture garrison code"

introduced `bIgnoreCulture` specifically to avoid calling
`AI_neededCultureDefenders` twice from `AI_neededDefenders`.

The local preserved commit diff shows that practical 1902 simultaneously:

1. changed `AI_neededDefenders` from

       AI_neededFloatingDefenders(bIgnoreEvac, bConstCache)

   to

       AI_neededFloatingDefenders(bIgnoreEvac, bConstCache, true)

2. added the `bIgnoreCulture` parameter to both
   `AI_neededFloatingDefenders` and `AI_calculateNeededFloatingDefenders`, and

3. wrapped the culture contribution in:

       if (!bIgnoreCulture)

but DID NOT add that new semantic dimension to
`m_iNeededFloatingDefendersCacheTurn` or provide a second cache value.

So practical 1902 changed one cached mathematical function into two functions of
`bIgnoreCulture` while retaining the original one-dimensional game-turn cache.
The bug is born exactly in that performance tweak.

UPSTREAM / CURRENT BOUNDARY
---------------------------
- Stock BtS / current Civ4CE BtS: one floating-defender mode; no such missing
  `bIgnoreCulture` cache key.
- K-Mod current source: one floating-defender mode; no such missing semantic key.
- Base AdvCiv 1.14: bug present.
- Current AdvCiv-SAS: bug present and semantically inherited. SAS only caches the
  `CvGame` reference locally in these helpers; it does not alter the cache identity
  or repair the mode collision.

DUPLICATE / ROOT-SEPARATION CHECK
---------------------------------
The reconciled album was searched for `AI_neededFloatingDefenders`, floating
Defenders, `bIgnoreCulture`, culture-garrison cache and related cache-key language.
No prior numbered root owns this missing parameter dimension.

Nearby cache findings are distinct:

- the AI_playerCloseness same-turn cache note concerns mutable city geometry and
  currently has only softer heuristic consequences;
- F514/F515/F516/F519/F520 concern strategy/victory/team cache freshness and
  ordering, not a function parameter omitted from the cache identity;
- F523 is persistent `workingCityOverride` lifecycle state, not a cache;
- KI#213 is the intentionally empty legacy land best-build cache used by Advanced
  Start and is unrelated to defender allocation.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The cache must distinguish the two semantic modes.

Preferred robust design: keep separate per-turn cached values (and validity turns)
for:

    include culture  (bIgnoreCulture=false)
    ignore culture   (bIgnoreCulture=true)

A smaller surgical repair can add `bIgnoreCulture` to the cache identity and
recalculate whenever the requested mode differs from the stored mode. That is
correct but can thrash if callers alternate modes; two slots preserve the original
performance intent better.

Do not simply remove the `true` argument from `AI_neededDefenders`: that would
restore the duplicate culture calculation and, more importantly, would make the
culture component partially additive before the function's explicit final culture
floor. The intended semantics of practical 1902 are sound; the cache identity is
what is incomplete.

REGRESSION TESTS
----------------
A. Two-mode cache identity
1. Arrange one city with F != max(F, C-M).
2. Call `AI_neededFloatingDefenders(false, false, false)` and record include value.
3. In the same game turn call `AI_neededFloatingDefenders(false, false, true)`.
4. Assert the second result is the independently calculated ignore-culture value,
   not the first cached value.
5. Repeat in reverse order.

B. AIR -> LAND ordinary-turn reproduction
1. Sequential-turn AI player owns at least one `UNITAI_DEFENSE_AIR` and one land
   city-defense unit.
2. Give a city culture pressure with C-M > F and non-culture defender terms B>M.
3. Let AIR pass call `AI_neededAirDefenders`, thereby priming city floating caches.
4. Let LAND pass reach `AI_guardCityMinDefender(true)` / `AI_neededDefenders(true)`.
5. Assert required defenders equal the fresh ignore-culture calculation followed
   by one final culture floor; result must not depend on the earlier air call.

C. Exact arithmetic control
For F=0, M=2, C=5 and a non-culture final baseline of 3:

    correct AI_neededDefenders = 5

and assert that an earlier include-culture cache population cannot change it to 6.

D. No-culture-pressure control
If C <= M or C-M <= F, both semantic floating results are equal. Assert behavior
remains unchanged and no unnecessary recalculation changes the result.

E. Const-cache control
Calls with `bConstCache=true` must continue to avoid mutating either cache slot.

NUMBERING / CURRENT STONE
-------------------------
F526 / KI#849 is CONFIRMED.
F527 / provisional KI#850 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP295 (this file).

NEXT
----
Continue under F527 unused through the remaining low/zero-coverage live helpers,
with emphasis on object/variable identity, parameterized caches, persistent state,
sentinel/divisor domains and nearby upstream bug-fix/refactor history. Promote only
an independent live current root; keep weaker heuristic/cache-granularity notes as
controls.

END C031-WIP295


================================================================================
C031-WIP296 APPEND -- 2026-09-04 -- F527 / KI#850 CONFIRMED
================================================================================

F527 / KI#850 -- `AI_culturePressureFactor()` MULTIPLIES AN ALREADY MULTI-RIVAL
PRESSURE SUM BY `getEstimateEndTurn()` IN 32-BIT `int`, SO SUPPORTED HIGH-PRESSURE
GAMES CAN OVERFLOW AND TURN MAXIMUM CULTURE PRESSURE INTO A LARGE NEGATIVE VALUE
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- inherited K-Mod arithmetic overflow with a direct current SAS gameplay
consumer. Current SAS's 48-player DLL and added Slow / Very Slow game speeds enlarge the
reachable state substantially, but the unsafe multiplication itself was introduced upstream.
This is independent of F518's stale average-culture-pressure snapshot: F527 corrupts the live
per-city pressure value before any caching/averaging occurs.

CURRENT CODE / ROOT
-------------------
Current `CvCityAI::AI_culturePressureFactor()` accumulates one squared pairwise culture
pressure contribution for every relevant BFC plot and every alive rival civilization:

    iAnswer += iForeignCulture * iForeignCulture;

where `iForeignCulture` is bounded to at most 100 for one rival/plot pair. Thus one pair can
add 10,000. After the nested loops, K-Mod's late-game dulling performs:

    iAnswer *= kGame.getEstimateEndTurn();
    iAnswer /= kGame.getGameTurn() + kGame.getEstimateEndTurn();

Both `iAnswer` and `getEstimateEndTurn()` are ordinary signed 32-bit `int`. The mathematically
intended result is later capped to 500, but the cap occurs only AFTER the unsafe product.
Therefore the cap cannot protect the intermediate multiplication.

CURRENT SUPPORTED BOUNDS
------------------------
Current SAS has:

    MAX_CIV_PLAYERS = 48

so one city can theoretically be evaluated against as many as 47 non-team major rivals.
`CityPlotIter(*this)` covers the ordinary 21-city-plot BFC. The accumulator itself therefore
has a current structural upper bound of roughly:

    21 plots * 47 rivals * 100^2
      = 9,870,000

which still fits in signed 32-bit integer storage.

`CvGame::setStartTurnYear` initializes `getEstimateEndTurn()` from the sum of the selected
GameSpeed XML turn increments. Current SAS totals are:

    Nitro       165
    Turbo       250
    Quick       330
    Normal      500
    Epic        750
    Marathon   1000
    Slow       1500
    Very Slow  2000

Consequently the safe pre-scale ceiling `INT_MAX / estimateEndTurn` is only about:

    Normal       4,294,967
    Epic         2,863,311
    Marathon     2,147,483
    Slow         1,431,655
    Very Slow    1,073,741

The current maximum pressure sum is therefore far above the safe product range on the longer
supported speeds.

CLEAN CURRENT VERY-SLOW DISCONTINUITY WITNESS
---------------------------------------------
Use a current 48-player-capable Very Slow game (`estimateEndTurn = 2000`) at game turn 500.
Construct a valid old, heavily contested cultural intersection around city A. On each of its
21 BFC plots, let A have plot culture 10 from its owner and let seven alive rival civilizations
each have plot culture 90. Each rival is in culture range on the relevant plots and at A's city
plot, is not a vassal of A's team, and city flipping is allowed. These are ordinary legal plot-
culture values: the helper compares each rival only against A (`90 / (90+10)`), not against
the total culture from all rivals.

For each rival/plot pair current arithmetic gives:

    foreign share = floor(100 * 90 / (90 + 10)) = 90
    range/flip divisor = 2
    adjusted share = 90
    squared contribution = 8,100

With six such rivals:

    pre-scale iAnswer = 21 * 6 * 8,100 = 1,020,600
    product           = 1,020,600 * 2,000 = 2,041,200,000

which still fits signed 32-bit. At turn 500:

    dulled = 2,041,200,000 / 2,500 = 816,480
    factor = min(500, 100 + 816,480 / 60) = 500

So the city correctly reaches the intended maximum-pressure cap.

Add only the seventh otherwise-identical rival:

    pre-scale iAnswer = 21 * 7 * 8,100 = 1,190,700
    mathematical product = 2,381,400,000

This exceeds `INT_MAX` (2,147,483,647). Signed overflow is undefined by the language; on the
actual legacy x86/MSVC arithmetic model used by Civ4 the runtime multiply wraps modulo 2^32:

    wrapped product = -1,913,567,296

Then current integer arithmetic at turn 500 gives approximately:

    -1,913,567,296 / 2,500 = -765,426
    pressure factor = min(500, 100 + (-765,426 / 60))
                    = -12,657

Thus ADDING foreign culture changes the same city discontinuously from the intended maximum
pressure factor 500 to a huge negative pressure factor. This violates the helper's monotonic
meaning and its own lower baseline of 100 in ordinary non-overflow states.

DIRECT CURRENT GAMEPLAY CONSEQUENCE -- CULTURE WEIGHT BECOMES NEGATIVE
-----------------------------------------------------------------------
`CvPlayerAI::AI_updateCommerceWeights()` consumes the live city pressure directly:

    int iPressureFactor = pCity->AI_culturePressureFactor();
    int iPressureWeight = iWeight * (iPressureFactor - 100) / 100;
    ...
    iWeight += iPressureWeight;
    pCity->AI_setCultureWeight(iWeight);

Current COMMERCE_CULTURE XML AI weight is 30. Choose otherwise neutral conditions: no cultural
victory weighting, no war-plan scaling, and an era/state that does not enter the late no-
pressure reduction branch.

Six-rival safe state:

    pressure = 500
    pressureWeight = 30 * (500 - 100) / 100 = +120
    final culture weight = 30 + 120 = 150

Seven-rival overflowing state:

    pressure ~= -12,657
    pressureWeight = 30 * (-12,657 - 100) / 100 ~= -3,827
    final culture weight ~= -3,797

The normal per-city culture-commerce consumer contains:

    iWeight = pCity->AI_getCultureWeight();
    FAssert(iWeight >= 0);

so the overflow does not merely perturb an internal score: it can directly violate the
consumer's asserted invariant and makes Culture commerce strongly NEGATIVE in precisely the
city under the greatest foreign cultural pressure.

CURRENT SAS CULTURE-PROCESS CONSEQUENCE
---------------------------------------
Current SAS additionally uses `AI_culturePressureFactor()` in `AI_processValue` to decide
whether Culture process is justified as a local emergency. The current define is:

    SAS_AI_CULTURE_PROCESS_MIN_PRESSURE_FACTOR = 150

For a non-cultural-victory city above first-ring expansion, the correct pressure 500 preserves
the emergency Culture-process path. The overflowing -12,657 is instead `< 150`, so the same
extreme pressure can make SAS classify Culture process as non-emergency and skip it unless the
separate cultural-victory-candidate exception applies. This is the opposite of the intended
local-border-pressure policy.

`SAS_AI_directArtistCultureValuePercent()` also calls the pressure helper, but its later
full-BFC-control / culture-percent checks independently reject the extreme contested fixture;
therefore it is NOT needed as a reproduction and should not be claimed as an additional direct
consequence for this witness.

OTHER LIVE PROPAGATION
----------------------
The same corrupted pressure also feeds:

- `AI_calculateAverages()` via culture-rate-weighted `m_iAverageCulturePressure`;
- culture spreading / espionage mission valuation;
- culture victory / city culture-weight calculations.

These can amplify the error, but the direct negative city-culture-weight invariant and Culture
process inversion above are sufficient. Do not split downstream consumers into separate roots.

ANCESTRY / BIRTH
----------------
K-Mod introduced the culture-pressure feature earlier, initially dulling pressure by era. The
critical multiplication is born in K-Mod practical 730, commit:

    a38e2befe3e774ac8966f5bc72643c9167de2217

That diff changes the previous safe small-era scaling:

    iAnswer *= GC.getNumEraInfos();
    iAnswer /= GET_PLAYER(getOwner()).getCurrentEra() + GC.getNumEraInfos();

into the still-current turn-horizon formula:

    iAnswer *= GC.getGame().getEstimateEndTurn();
    iAnswer /= GC.getGame().getGameTurn() + GC.getGame().getEstimateEndTurn();

without widening the multiplication. The final K-Mod source retains the same 32-bit formula in
`CvCity::culturePressureFactor`. Base AdvCiv later moved the helper to `CvCityAI` but preserves
the arithmetic. Base AdvCiv 1.14 still has `MAX_CIV_PLAYERS = 18` and current Marathon length
1250, so sufficiently multi-rival culture intersections can already overflow upstream; SAS's
48-player DLL and 1500/2000-turn speeds make the reachable range substantially wider.

UPSTREAM / CURRENT BOUNDARY
---------------------------
- Stock BtS / Civ4CE: does not have this K-Mod culture-pressure formula.
- K-Mod: unsafe turn-horizon multiplication present.
- Base AdvCiv 1.14: unsafe multiplication present, with 18-player cap.
- Current AdvCiv-SAS: unsafe multiplication present; 48-player support and longer Slow / Very
  Slow speeds materially increase exposure. SAS only caches `CvGame` / owner locally around
  this helper and does not change the arithmetic root.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album searches for `AI_culturePressureFactor`, culture pressure, integer overflow and
related terms found no prior numbered root for this live per-city arithmetic overflow.

Important nearby findings are distinct:

- F518 / KI#841: `AI_calculateAverages()` snapshots city-set averages before same-turn city
  founding/acquisition. That is a CACHE-LIFETIME / ordering root; F527 can corrupt the live
  pressure even with a perfectly fresh average.
- F526 / KI#849: `AI_neededFloatingDefenders` omits `bIgnoreCulture` from its cache identity.
  That concerns culture GARRISON demand, not this commerce-pressure arithmetic.
- the older `countTotalCulture()` range note was retained as UI/history hardening because no
  current gameplay decision was proved. F527 has direct production/commerce consumers and an
  asserted nonnegative invariant.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Widen the late-game dulling product BEFORE multiplication, e.g. with the legacy compiler's
supported `__int64`:

    __int64 iDulled = (__int64)iAnswer * kGame.getEstimateEndTurn();
    iDulled /= kGame.getGameTurn() + kGame.getEstimateEndTurn();
    iAnswer = (int)iDulled;

The mathematically intended dulled value is bounded by the pre-scale `iAnswer`, so converting
back to int after division is safe under the current accumulator bounds. Keep the existing
500 cap and formula semantics.

A secondary hardening pass could separately consider widening `100 * iForeignCulture` if plot
culture itself can grow to extreme integer values, but that is NOT required to fix or prove
F527 and should not be conflated with this confirmed root.

REGRESSION TESTS
----------------
A. Six-vs-seven-rival Very Slow monotonicity
1. Very Slow game, estimate end turn 2000, current turn 500.
2. 21 relevant BFC plots; owner culture 10 on each.
3. Six qualifying rivals each culture 90 on each plot.
4. Assert `AI_culturePressureFactor() == 500`.
5. Add a seventh identical qualifying rival.
6. Assert factor remains 500 (never decreases and never becomes negative).

B. 64-bit formula control
For pre-scale 1,190,700 at turn 500 / end 2000, assert widened arithmetic computes:

    1,190,700 * 2,000 / 2,500 = 952,560
    min(500, 100 + 952,560 / 60) = 500

C. Culture-weight invariant
Run `AI_updateCommerceWeights()` on the overflowing fixture under neutral non-victory/no-war
conditions. Assert stored per-city Culture weight is nonnegative and matches the widened
pressure result; `AI_commerceWeight(COMMERCE_CULTURE, city)` must not trigger its
`FAssert(iWeight >= 0)`.

D. Culture-process emergency
For the same non-victory pressured city above culture level 1, assert the widened pressure
factor is >= current emergency threshold 150 so overflow cannot suppress Culture process.

E. Ordinary control
Low-pressure and Normal-speed states below the product threshold must be bit-for-bit unchanged.

NUMBERING / CURRENT STONE
-------------------------
F527 / KI#850 is CONFIRMED.
F528 / provisional KI#851 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP296 (this file).

NEXT
----
Continue under F528 unused through the remaining low/zero-coverage `CvCityAI.cpp` helpers.
Prioritize arithmetic that multiplies accumulated multi-player / multi-plot totals before
normalization, then persistent-state / wrong-object / sentinel contracts. Keep current XML and
supported map/player/game-speed bounds in every overflow challenge; do not promote merely
formal signed-overflow possibilities without a current producer.

END C031-WIP296

================================================================================
WIP297 - Queue 005 CvCityAI.cpp continuation; F528/KI#851 CONFIRMED
================================================================================

Durable state at this checkpoint
--------------------------------
- Queue 005: CvGameCoreDLL/CvCityAI.cpp OPEN (deep re-audit).
- 32/169 unique files complete.
- F446/KI#769 through F528/KI#851 confirmed.
- F529 / provisional KI#852 is UNUSED.
- Repository source remains untouched.

POST-WIP296 CONTROL PROGRESS
----------------------------
The first continuation tranche under F528 closed several tempting edges rather than numbering
new roots:

- `AI_bestSpreadUnit`: its corporation value is mutated while scanning units, but current shipped
  XML has only one Executive class per corporation. There is no second same-corporation unit
  candidate that can inherit the first candidate's modified value. Keep as API/robustness debt.
- Worker accounting: `AI_updateWorkersHaveAndNeeded()` recomputes both worker counters from scratch
  each city turn; `AI_changeWorkersHave()` maintains the value between that refresh and unit
  completion/reassignment. No unmatched persistent transition was established.
- `AI_growthValuePerFood`: the transient negative `iAvailable = maxSpecialists - assigned` state
  that can occur after specialist capacity disappears is safe in this helper. Negative values are
  not used as insertion counts, and `isSpecialistValid(e, iAvailable)` reduces the current count
  exactly to the max-count boundary. Do not split this from the already-owned forced-specialist
  availability work.
- `AI_getTargetPopulation`: although its raw helper result can conceptually fall low under severe
  unhappiness, live callers clamp/range it before a population-loop count or use the low result as
  an intentional don't-grow signal. No negative-count/root consequence established.

F528 / KI#851 - AI_removeWorstCitizen fallback removes a forced specialist but leaves its force target alive
-----------------------------------------------------------------------------------------------------------
Status: CONFIRMED inherited AdvCiv regression; current human citizen automation consequence.

CURRENT CODE SHAPE
------------------
`CvCityAI::AI_removeWorstCitizen(SpecialistTypes eIgnoreSpecialist)` has two specialist-removal
passes.

The ordinary pass refuses to choose an ignored or fully forced specialist:

    if (eIgnoreSpecialist == eLoopSpecialist ||
        getSpecialistCount(eLoopSpecialist) <= getForceSpecialistCount(eLoopSpecialist))
    {
        continue;
    }

After that pass chooses a specialist, current Base AdvCiv/SAS performs:

    changeSpecialistCount(eWorstSpecialist, -1);
    // K-Mod. If we had to remove a forced specialist, reduce the force count to match what we did.
    if (getSpecialistCount(eWorstSpecialist) < getForceSpecialistCount(eWorstSpecialist))
        setForceSpecialistCount(eWorstSpecialist, getSpecialistCount(eWorstSpecialist));

But the condition is mathematically dead in this branch. Selection required:

    assigned > forced

and after exactly one removal that becomes:

    assigned_after >= forced

so `assigned_after < forced` cannot occur.

If no ordinary removable specialist or worked plot exists, the function deliberately retries all
assigned specialists, this time ignoring neither `eIgnoreSpecialist` nor force counts:

    if (extraFreeSpecialists() < 0)
    {
        FOR_EACH_ENUM(Specialist)
        {
            if (getSpecialistCount(eLoopSpecialist) <= 0)
                continue;
            ... choose worst specialist ...
        }
    }

    if (eWorstSpecialist != NO_SPECIALIST)
    {
        changeSpecialistCount(eWorstSpecialist, -1);
        return true;
    }

This fallback is the branch that CAN remove a fully forced specialist, but current AdvCiv/SAS no
longer lowers its force count after doing so.

ENGINE / FORCE-TARGET CONTRACT
------------------------------
`CvCity::alterSpecialistCount` is the ordinary TASK_CHANGE_SPECIALIST handler used by the city UI.
When Citizens Automated is enabled, a positive user specialist adjustment first raises that
specialist's force target, then tries to make room while explicitly telling
`AI_removeWorstCitizen` not to remove the newly requested type:

    changeForceSpecialistCount(eSpecialist, ...);
    ...
    if (extraPopulation() > 0 || AI().AI_removeWorstCitizen(eSpecialist))
    {
        if (isSpecialistValid(eSpecialist, 1))
            changeSpecialistCount(eSpecialist, 1);
    }

`extraPopulation()` is based on ACTUAL worked/assigned citizens:

    visiblePopulation() + min(0, extraFreeSpecialists())

and `extraFreeSpecialists()` is:

    totalFreeSpecialists() - getSpecialistPopulation()

Neither subtracts or otherwise accounts for unfulfilled force targets. Therefore removing an
assigned forced specialist without lowering the target leaves a persistent latent demand rather
than a self-correcting population count.

CURRENT SHIPPED REPRODUCTION - CASTE SYSTEM, POPULATION 1
---------------------------------------------------------
Current SAS Caste System (`CIVIC_CASTE_SYSTEM`) retains player-wide unlimited validity for:

- SPECIALIST_ARTIST
- SPECIALIST_SCIENTIST
- SPECIALIST_MERCHANT

so no building-dependent specialist-slot edge is needed.

Use a human population-1 city under Caste System with Citizens Automated ON, no relevant free
specialists, and no non-home worked plot because its sole population citizen is a specialist.
Start with:

    assigned Scientist = 1
    forced Scientist   = 1
    assigned Merchant  = 0
    forced Merchant    = 0

The user clicks +1 Merchant (TASK_CHANGE_SPECIALIST -> alterSpecialistCount).

1. `alterSpecialistCount(Merchant,+1)` raises `forced Merchant` to 1.
2. `extraPopulation()` is 0, so it calls `AI_removeWorstCitizen(Merchant)` to make room.
3. The ordinary specialist pass skips Scientist because `assigned Scientist <= forced Scientist`.
   Merchant is the ignored requested type. There is no non-home worked population plot to remove.
4. The fallback pass now considers ALL assigned specialists and can choose Scientist.
5. It decrements `assigned Scientist` from 1 to 0 and returns WITHOUT lowering
   `forced Scientist`, which remains 1.
6. Merchant is valid under Caste System and is added. Immediate state becomes:

       population          = 1
       assigned Scientist  = 0
       forced Scientist    = 1
       assigned Merchant   = 1
       forced Merchant     = 1

   The sum of forced targets is now 2 for a population-1 city.

NEXT ASSIGNMENT CYCLE - PERSISTENT IMPOSSIBLE TARGET
----------------------------------------------------
`AI_assignWorkingPlots()` later contains a pass whose explicit purpose is:

    // make sure at least the forced amount of specialists are assigned

For Scientist it sees `assigned 0 < forced 1`. Under Caste System the missing Scientist is fully
legal by specialist capacity, so it executes:

    setSpecialistCount(Scientist, 1);

Now the population-1 city temporarily has both Scientist=1 and Merchant=1. The newly-forced flag
causes the overpopulation trim:

    while (extraPopulation() < 0)
        AI_removeWorstCitizen();

Both specialists are exactly at their force targets and there is no removable non-home plot, so
the ordinary pass again has no candidate. The fallback removes one of the forced specialists and,
again, leaves that specialist's force target unchanged.

After the call the city can be population-consistent again (one assigned specialist), but it still
has two impossible force targets. On every later citizen reassignment, the missing target is
restored and another forced specialist is removed. The user/governor state therefore remains
permanently inconsistent instead of resolving the replacement request.

This is a persistent lifecycle defect, not merely one transient frame with assigned < forced.

K-MOD INTENT AND THE LOST SAFEGUARD
-----------------------------------
K-Mod practical 1141, commit:

    ce9fd13beda19a871da4cc9ab333b9ce0536809b

is titled exactly:

    Forced specialist count can no longer exceed total population.

That commit adds the following repair to the FINAL FALLBACK removal branch:

    changeSpecialistCount(eWorstSpecialist, -1);
    // K-Mod. If we had to remove a forced specialist, reduce the force count to match what we did.
    if (getSpecialistCount(eWorstSpecialist) < getForceSpecialistCount(eWorstSpecialist))
        setForceSpecialistCount(eWorstSpecialist, getSpecialistCount(eWorstSpecialist));

That is precisely the branch and invariant required by the current reproduction.

ADVCIV REGRESSION BOUNDARY
--------------------------
The first reachable AdvCiv continuation snapshot, practical 1253 / commit:

    f6ff6a182f...

contains an explicit code-motion diff inside `AI_removeWorstCitizen`:

1. it ADDS the K-Mod force-count repair after the EARLIER ordinary specialist removal;
2. it REMOVES the same four-line repair from the FINAL fallback removal.

The relevant diff therefore does not merely omit an upstream fix accidentally; it physically
moves the safeguard from the branch that can remove forced specialists to the branch whose
`assigned > forced` selection predicate makes `assigned_after < forced` impossible.

Later AdvCiv refactors preserve that placement. Base AdvCiv 1.14 and current SAS are substantively
identical at the two removal exits (apart from SAS cached/default-specialist spelling).

The later AdvCiv `advc.121` change (`5cac5b3ad38b64d34d6f86fdc5f21e9101231dd0`, practical 3599,
"Smarter interpretation of addtl. forced specialists") strengthens the UI-side meaning of human
positive specialist requests by force-preserving previously assigned specialists of the requested
same type, but it does not restore the cross-type fallback repair. It therefore does not cure this
root.

UPSTREAM / CURRENT BOUNDARY
---------------------------
- K-Mod final source: correct fallback repair present.
- First reachable AdvCiv continuation snapshot: repair moved to wrong/dead branch.
- Base AdvCiv 1.14: regression remains.
- Current AdvCiv-SAS: regression remains; SAS changes in `AI_removeWorstCitizen` are only cached
  lookup/spelling changes and do not repair the fallback.
- Current SAS Caste System makes the two-specialist legal reproduction especially simple.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album search found only inventory-level mentions of `AI_removeWorstCitizen`; this exact
fallback lifecycle was not already numbered.

Keep F528 separate from KI#70:

- KI#70 concerns a forced target that exceeds a specialist TYPE'S CURRENT LEGAL CAPACITY after a
  slot/civic/building availability change; its repair sanitizes assigned and forced counts against
  `isSpecialistValid` / max-specialist capacity independently.
- F528 uses specialists that remain completely legal (Caste System unlimited Scientist/Merchant).
  The failure is that a population-replacement fallback removes an actually assigned FORCED
  citizen but fails to lower the corresponding force target, allowing the sum of valid force
  targets to exceed available population and repeat indefinitely.

The producer, invariant and natural repair are therefore distinct even though both live in the
forced-specialist subsystem.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Restore the K-Mod practical-1141 safeguard at the fallback exit:

    if (eWorstSpecialist != NO_SPECIALIST)
    {
        changeSpecialistCount(eWorstSpecialist, -1);
        if (getSpecialistCount(eWorstSpecialist) < getForceSpecialistCount(eWorstSpecialist))
            setForceSpecialistCount(eWorstSpecialist,
                getSpecialistCount(eWorstSpecialist));
        return true;
    }

The duplicate repair at the ordinary exit is harmless but mathematically redundant under its
current `assigned > forced` candidate filter; it can remain as defense-in-depth or be simplified
separately. Do not change source during the audit.

REGRESSION TESTS
----------------
A. Population-1 Caste replacement
1. Human city, population 1, Caste System, Citizens Automated ON.
2. Assign/force one Scientist.
3. Click +1 Merchant.
4. Assert Merchant becomes assigned/forced and Scientist's force count is lowered if Scientist was
   the citizen removed to make room.
5. Assert sum of force targets does not remain 2 for population 1.

B. Reassignment stability
After A, force a citizen reassignment (`AI_assignWorkingPlots`). Assert the removed old specialist
is not automatically resurrected solely from a stale force target and no forced-specialist
remove/restore cycle repeats.

C. Ordinary unforced-removal control
When `assigned > forced` for a specialist, ordinary removal behavior is unchanged and never lowers
a still-satisfied force target.

D. Slot-capacity control
Run the existing KI#70 unavailable-specialist fixture separately. F528's repair must not replace
KI#70's independent max-capacity sanitizer.

NUMBERING / CURRENT STONE
-------------------------
F528 / KI#851 is CONFIRMED.
F529 / provisional KI#852 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP297 (this file).

NEXT
----
Continue under F529 unused. Resume the low-coverage mechanical pass after
`AI_removeWorstCitizen`, prioritizing persistent/transient city-AI state that can survive save/load
or turn boundaries, remaining wrong-variable/out-parameter contracts, and hard arithmetic in
Worker/improvement helpers. Revisit the unsaved SAS culture-targeting fields only if a normal
save/load or human-automation consumer can observe them before `AI_updateCommerceWeights`
refreshes them; do not promote a merely transient/cache-design smell.

END C031-WIP297

================================================================================
C031-WIP298 - F529 / KI#852 CONFIRMED
CvCityAI::AI_updateSpecialYieldMultiplier - project production emphasis is unreachable
================================================================================

STATUS
------
F529 / KI#852 CONFIRMED.
F530 / provisional KI#853 is the next UNUSED root.
Queue 005 CvGameCoreDLL/CvCityAI.cpp remains OPEN.
Repository source untouched.

ROOT
----
`AI_updateSpecialYieldMultiplier()` intends to give major production work the same special-yield
push when a city is producing either a World Wonder OR a Project:

    BuildingTypes eProductionBuilding = getProductionBuilding();
    if (eProductionBuilding != NO_BUILDING)
    {
        if (GC.getInfo(eProductionBuilding).isWorldWonder() || isProductionProject())
        {
            m_aiSpecialYieldMultiplier[YIELD_PRODUCTION] += 50;
            m_aiSpecialYieldMultiplier[YIELD_COMMERCE] -= 25;
        }
        ...
    }

The `isProductionProject()` half is nested inside `eProductionBuilding != NO_BUILDING`.
A city producing a Project has no production Building, so `getProductionBuilding()` returns
NO_BUILDING and the outer block is skipped. Therefore `isProductionProject()` can never cause the
+50 Production / -25 Commerce project emphasis.

This is not a policy/tuning disagreement. It is an unreachable half of an explicit `A || B`
condition caused by putting the `B` test under a prerequisite that excludes B's state.

LIVE CALLER / STATE
-------------------
`AI_assignWorkingPlots()` calls `AI_updateSpecialYieldMultiplier()` before forced-specialist
repair, population assignment and `AI_juggleCitizens()`. `AI_yieldValue()` then consumes:

    AI_specialYieldMultiplier(YIELD_PRODUCTION)
    AI_specialYieldMultiplier(YIELD_COMMERCE)

when ranking worked/unworked plots and specialists.

Thus the missing project branch directly changes ordinary citizen allocation throughout Project
production. It is not dead UI/debug state.

CURRENT ADVCIV/SAS INERTIA DETAIL
---------------------------------
AdvCiv practical 121 added inertia only to the Production special multiplier:

    m_aiSpecialYieldMultiplier[YIELD_PRODUCTION] += iOldProductionMult;
    m_aiSpecialYieldMultiplier[YIELD_PRODUCTION] /= 2;

Therefore, from a neutral previous Production multiplier of 0, a correctly reachable project
+50 would become +25 on the first assignment refresh, then +37, +43, ... on subsequent refreshes.
The Commerce side is not averaged and would immediately receive -25.

The current bug instead contributes 0 / 0 from the Project-specific branch on every refresh.
Other strategy/flavor modifiers can coexist, but they do not restore the missing project-specific
term.

DETERMINISTIC CITIZEN-CHOICE FLIP
---------------------------------
Use a neutral controlled city where all food/growth terms are equal between two candidate jobs,
base/average modifiers are 100%, Commerce weight is 100, and no unrelated emphasis differs.
`AI_yieldValue()` uses base values:

    Production: 8 per hammer
    Commerce:   4 per commerce

Compare equal-food jobs with relevant yields:

    Job H: 2 hammers
    Job C: 1 hammer + 3 commerce

CURRENT BUGGED PROJECT STATE (no project special multiplier):

    H = 2 * 8 = 16
    C = 1 * 8 + 3 * 4 = 20

so the governor prefers C.

CORRECT FIRST PROJECT REFRESH FROM NEUTRAL PRIOR STATE:
Project +50 Production is averaged by AdvCiv inertia to +25; Commerce receives -25 immediately.

    H = 16 * 1.25 = 20
    C = (8 * 1.25) + (12 * 0.75) = 10 + 9 = 19

so the governor prefers H.

The unreachable branch therefore produces an actual assignment flip on the very first refresh;
subsequent project turns would strengthen the Production bias further as the inertial multiplier
approaches +50.

This is exactly the purpose implied by the source: major long-term production (World Wonder OR
Project) should make the city temporarily emphasize Production over Commerce. World Wonders get
that behavior; Projects do not.

ANCESTRY / BOUNDARY
-------------------
The defect predates K-Mod.

- Civ4CE BtS reconstruction: same nesting and same unreachable `|| isProductionProject()` half.
- K-Mod 1.46: same nesting; K-Mod retains the explicit World Wonder OR Project comment/behavior.
- Base AdvCiv 1.14: same nesting; later AdvCiv refactors/inertia do not repair it.
- Current AdvCiv-SAS: same semantic defect, with only surrounding refactors/custom changes.

So this is an inherited stock-BtS root that survives through K-Mod, AdvCiv and SAS.

DEDUP
-----
Whole-album literal/semantic search found no existing root for Project production being excluded
from `AI_updateSpecialYieldMultiplier` by the Building-only outer guard.

Do not merge with F522 / KI#845:
- F522 is stale Emphasize state preserved by the practical-1675 clear->continue refactor.
- F529 is the Project-specific producer term never being generated at all, even with clean emphasis
  state and a fresh multiplier calculation.

Do not merge with project/building-choice roots: this bug occurs after a Project is already in the
production queue and changes citizen yield assignment, not whether the Project gets selected.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Separate the Project test from the Building-only block, e.g. conceptually:

    BuildingTypes eProductionBuilding = getProductionBuilding();
    if ((eProductionBuilding != NO_BUILDING &&
         GC.getInfo(eProductionBuilding).isWorldWonder()) ||
        isProductionProject())
    {
        m_aiSpecialYieldMultiplier[YIELD_PRODUCTION] += 50;
        m_aiSpecialYieldMultiplier[YIELD_COMMERCE] -= 25;
    }

    if (eProductionBuilding != NO_BUILDING)
    {
        // Building-specific FoodKept adjustment only.
        ...
    }

Do not modify source during the audit.

REGRESSION TESTS
----------------
A. Project multiplier generation
1. City with neutral old special multipliers begins a Project.
2. Run `AI_updateSpecialYieldMultiplier()`.
3. Assert the Project contributes the same raw +50 Production / -25 Commerce term as the existing
   World-Wonder branch (before AdvCiv Production inertia).

B. Building-only control
A normal non-Wonder building must not receive the Project/Wonder +50/-25 term.

C. World-Wonder control
World Wonder behavior must remain unchanged.

D. Citizen-choice flip
Use the equal-food 2H vs 1H+3C controlled job pair above. On the first Project refresh from neutral
prior Production multiplier, assert the intended Project term changes the relative ranking from
C > H to H > C.

E. Inertia control
After repeated assignment refreshes while Project production persists, assert the Production
special multiplier follows existing AdvCiv inertia rather than jumping directly to +50; the repair
must restore the producer term, not bypass inertia.

POST-F529 CONTROLS / CURSOR
---------------------------
Before finding F529, the following F529-era leads were checked and closed/merged:

- Worker `m_iWorkersHave` lifecycle: no missed increment/decrement witness yet; the full city-turn
  update recomputes from scratch and incremental hooks bridge production/mission changes.
- `AI_updateWorkersHaveAndNeeded` temporary `AI_addBestCitizen`: exact added plot/specialist is
  returned through out-parameters and immediately reversed; forced-specialist targets are not
  changed, so this does not duplicate F528.
- `AI_growthValuePerFood` transient negative specialist availability: the function already avoids
  inserting a negative number of jobs; the odd Debug assertion shape is not yet a live gameplay
  root.
- Legacy best-build consumers remain owned by KI#213 when their issue derives from SAS deliberately
  disabling the ordinary land `AI_getImprovementValue` path.

NUMBERING / CURRENT STONE
-------------------------
F529 / KI#852 is CONFIRMED.
F530 / provisional KI#853 is UNUSED.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP298 (this file).

NEXT
----
Continue under F530 unused. Stay first in `AI_updateSpecialYieldMultiplier` / citizen-yield state
for adjacent unreachable-state or stale-lifetime defects, then resume remaining low-coverage
Worker/improvement arithmetic and wrong-variable/out-parameter checks. Do not promote merely
subjective strategy differences or best-build consequences already owned by KI#213.

END C031-WIP298

================================================================================
C031-WIP299 - F530 / KI#853 CONFIRMED
CvCityAI::AI_yieldValue - anger-expiry modulo maps exact full anger cycles to zero turns
================================================================================

STATUS
------
F530 / KI#853 CONFIRMED.
F531 / provisional KI#854 is the next UNUSED root.
Queue 005 CvGameCoreDLL/CvCityAI.cpp remains OPEN.
Repository source untouched.

ROOT
----
While valuing food for future growth, `AI_yieldValue()` tries to estimate whether one point of
temporary anger will disappear before the city grows. For all three stacked anger timers it uses
plain modulo:

    int iTurnsUntilAngerIsReduced = iHurryAngerTimer % flatHurryAngerLength();
    int iTurnsUntilAngerIsReduced = iConscriptAngerTimer % flatConscriptAngerLength();
    int iTurnsUntilAngerIsReduced = iDefyResolutionAngerTimer %
            flatDefyResolutionAngerLength();

and then:

    if (iTurnsUntilAngerIsReduced <= iApproxTurnsToGrow)
        iFutureHappy++;

That modulo is wrong at every exact multiple of the anger-stack length. In particular, for the
extremely ordinary state `timer == length`, it returns 0 and says the next unhappy point is
reduced immediately.

The engine's authoritative unhappy-count formula proves the opposite. `CvCity` computes Hurry,
Conscript and Defy anger stacks as:

    ((timer - 1) / length) + 1

(i.e. `ceil(timer / length)` for positive timers). Therefore when `timer == length`, the current
anger point remains for all `length` decrements; it does not disappear at turn 0.

For a positive timer, the correct number of decrements until the NEXT anger stack disappears is:

    ((timer - 1) % length) + 1

not:

    timer % length

This is an arithmetic boundary error, not an AI-policy disagreement.

NORMAL PRODUCER - FIRST WHIP / FIRST DRAFT
-----------------------------------------
This boundary is not rare or synthetic.

`CvCity::hurry()` does:

    int iHurryAngerLength = hurryAngerLength(eHurry);
    ...
    changeHurryAngerTimer(iHurryAngerLength);

So when a city had no previous whip anger, its first anger-causing whip creates exactly:

    hurryTimer == hurryAngerLength

`CvCity::conscript()` likewise does:

    int const iAngerLength = flatConscriptAngerLength();
    ...
    changeConscriptAngerTimer(iAngerLength);

so a first draft creates exactly the same `timer == length` boundary.

On stock/current Normal speed with the ordinary `HURRY_ANGER_DIVISOR = 10` and
`HurryConscriptAngerPercent = 100`, a normal zero-modifier city therefore has a 10-turn anger
length. Immediately after the first whip/draft, `AI_yieldValue` predicts 0 turns until the next
anger point disappears although `CvCity::doTurn` has to decrement the timer ten times before the
one anger stack reaches zero.

Other current game speeds simply change the cycle length; the exact-multiple error remains.
Defy-resolution anger has the same formula and can also hit exact multiples (including ordinary
full-length additions when not religion-divided), but Hurry/Draft alone are enough to prove F530.

LIVE GROWTH-VALUATION CONSEQUENCE
--------------------------------
The false zero is consumed in the normal food-growth path, not merely logged.

Use a healthy, non-disordered city with no food-production order and no food/avoid-growth emphasis.
Immediately after a first whip:

- before the whip the city had one spare happy;
- the new whip anger leaves `iHappinessLevel == 0`;
- the food bar is already above the `bBarFull` 80% threshold;
- the city can grow in fewer than the real anger-length turns;
- at least one genuinely useful future job exists;
- `iGrowthValue > 0`.

BUGGED calculation:

    timer % length == 0
    0 <= iApproxTurnsToGrow
    => iFutureHappy += 1
    => iPopToGrow = 1

CORRECT calculation:

    ((timer - 1) % length) + 1 == length
    length > iApproxTurnsToGrow
    => no future happiness yet
    => iPopToGrow = 0

Because the food bar is already >80% full, the correct `iPopToGrow == 0` state does not enter the
fallback `bFillingBar` food-growth valuation. The bugged state does enter ordinary growth
valuation. Food therefore receives a positive `iFoodGrowthValue` for growth that the city's
current happiness cap cannot actually support before the anger expires.

CONTROLLED JOB FLIP
-------------------
The effect is large enough to change citizen ranking, not just a hidden counter.

Take neutral 100% yield modifiers, no process, no Slavery contribution, no emphasis, and pass a
controlled positive `iGrowthValue = 10` to the same `AI_yieldValue` path. Current SAS uses:

    iBaseProductionValue = 8

and its ordinary non-emphasized happy/non-angry food baseline adds 3 points for one food.

Compare otherwise-neutral candidate jobs:

    Job F: +1 food
    Job H: +1 hammer

At `iAdjustedFoodPerTurn = 0`:

CORRECT full-bar / no-growth-headroom state:

    F = 3
    H = 8
    => H wins

BUGGED invented `iPopToGrow = 1` state:

    devalueRate = 18 - 1*3 = 15
    one food receives about +10 growth value at this controlled scale
    F ~= 13
    H = 8
    => F wins

The exact numeric growth score varies with ordinary city context, but the branch-level difference
is deterministic: at the exact anger-cycle boundary, the current code can activate positive growth
food value when the authoritative anger timer says no happiness will recover before growth. The
citizen governor, Worker/job comparisons and other `AI_yieldValue` consumers can observe that
misvaluation.

UPSTREAM / CURRENT BOUNDARY
---------------------------
The defect is inherited from stock BtS rather than introduced by SAS.

- Civ4CE BtS reconstruction: same `% flat...AngerLength()` formulas for Hurry, Conscript and Defy.
- K-Mod 1.46: same three formulas.
- Base AdvCiv 1.14: same three formulas.
- Current AdvCiv-SAS: same arithmetic; SAS adds surrounding food-growth tuning but does not repair
  the expiry calculation. Those SAS growth incentives can make the wrong headroom more visible,
  but they are not the root.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album searches for `iTurnsUntilAngerIsReduced`, `% flatHurryAngerLength`, anger modulo and
future-happiness expiry found no prior numbered root for this arithmetic boundary.

Do not merge with Hurry-policy / sacrifice-cost findings:
- F524 / KI#847 concerns specialist-count accounting inside `AI_citizenSacrificeCost` and can make
  population sacrifice itself look like a subsidy.
- F530 begins AFTER valid anger already exists and predicts the expiry of that anger incorrectly
  while valuing future city growth.

The producer, variable, invariant and repair are independent.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
For each positive timer, replace plain modulo with the positive-cycle remainder:

    int iTurnsUntilAngerIsReduced =
            ((iHurryAngerTimer - 1) % flatHurryAngerLength()) + 1;

and analogously for Conscript and Defy anger.

The surrounding `timer > 0` guards already make `timer - 1` safe. `flat...AngerLength()` is clamped
to at least 1 by the corresponding city helpers. Do not alter growth policy, happiness weights or
anger duration itself.

REGRESSION TESTS
----------------
A. First-whip exact-cycle boundary
1. City with zero hurry anger; perform an anger-causing whip.
2. Assert `timer == flatHurryAngerLength()`.
3. Assert predicted turns until the next anger point disappears equals `length`, not 0.

B. First-draft exact-cycle boundary
Repeat A with `conscript()` and `flatConscriptAngerLength()`.

C. General cycle arithmetic
For length 10, assert:

    timer  1 -> 1 turn
    timer  9 -> 9 turns
    timer 10 -> 10 turns
    timer 11 -> 1 turn
    timer 20 -> 10 turns
    timer 21 -> 1 turn

D. Growth-headroom control
At `iHappinessLevel=0`, full food bar, growth in fewer than `length` turns and `timer==length`,
assert the anger source does NOT add `iFutureHappy`; `iPopToGrow` must not become positive solely
from that timer.

E. Citizen-value control
With the controlled +1F vs +1H fixture above, assert an exact-cycle anger timer does not activate
the premature growth-food term. Non-boundary timers must preserve their existing intended behavior.

NUMBERING / CURRENT STONE
-------------------------
F530 / KI#853 is CONFIRMED.
F531 / provisional KI#854 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP299 (this file).

NEXT
----
Continue under F531 unused. First inspect adjacent `AI_yieldValue` boundary arithmetic (growth,
slavery and happiness timing) for the same exact-cycle / denominator / sign class, then resume the
remaining low-coverage Worker/improvement and persistent-state/out-parameter pass. Keep KI#213 as
the owner of consequences caused solely by SAS's deliberately disabled legacy land best-build
cache; do not split those into new roots.

END C031-WIP299

================================================================================
C031-WIP300 - F531 / KI#854 CONFIRMED
CvCityAI::AI_yieldValue - anger recovery is compared to growth before the anger decrement occurs
================================================================================

STATUS
------
F531 / KI#854 CONFIRMED.
F532 / provisional KI#855 is the next UNUSED root.
Queue 005 CvGameCoreDLL/CvCityAI.cpp remains OPEN.
Repository source untouched.

ROOT
----
F530 corrected the number of anger-timer decrements required for the next unhappy point to vanish.
There is a second independent error in the immediately following comparison:

    if (iTurnsUntilAngerIsReduced <= iApproxTurnsToGrow)
        iFutureHappy++;

`iApproxTurnsToGrow` is the number of upcoming `doGrowth()` executions needed to reach the growth
threshold. In the ordinary player/city turn sequence, however, the city's anger timers are
decremented AFTER `doGrowth()`, not before it.

Therefore an anger point requiring N decrements is gone in time for growth only when growth occurs
after MORE than N upcoming growth executions:

    iTurnsUntilAngerIsReduced < iApproxTurnsToGrow

not when the two counts are equal.

NORMAL TURN ORDER PROOF
-----------------------
`CvPlayer::doTurn()` performs:

    AI().AI_doTurnPre();
    ...
    AI().AI_assignWorkingPlots();
    ...
    FOR_EACH_CITY_VAR(pLoopCity, *this)
        pLoopCity->doTurn();

Thus the ordinary citizen assignment / `AI_yieldValue` pass happens before the city's own turn.

Inside `CvCity::doTurn()` the relevant order is:

    AI().AI_doTurn();
    ...
    doGrowth();
    doCulture();
    ...
    doProduction(...);
    ...
    if (getHurryAngerTimer() > 0)
        changeHurryAngerTimer(-1);
    if (getConscriptAngerTimer() > 0)
        changeConscriptAngerTimer(-1);
    if (getDefyResolutionAngerTimer() > 0)
        changeDefyResolutionAngerTimer(-1);

So every upcoming `doGrowth()` sees the anger timer BEFORE the decrement belonging to that same
city turn.

DETERMINISTIC LAST-ANGER-TURN WITNESS
------------------------------------
Use the corrected F530 remainder calculation and an ordinary city whose current anger timer is 1.
That state occurs naturally on the final turn of any whip/draft anger stack because the timer is
decremented by one each city turn.

Suppose the current food state will grow on the upcoming `doGrowth()`:

    iTurnsUntilAngerIsReduced = 1
    iApproxTurnsToGrow = 1

CURRENT comparison:

    1 <= 1  -> true
    => iFutureHappy++

ACTUAL sequence:

    citizen valuation sees timer 1
    -> upcoming doGrowth runs while timer is still 1
    -> only afterward timer changes 1 -> 0

The extra happiness therefore is NOT available to the growth being evaluated.

The next case proves the exact strict relation rather than merely saying there is some ordering
ambiguity:

    timer-decrements-needed = 1
    iApproxTurnsToGrow = 2

Current city turn:
    first doGrowth does not yet grow
    then timer 1 -> 0

Next city turn:
    second doGrowth occurs with the anger already gone

So 1 < 2 must count the future happiness, while 1 == 1 must not.

GENERAL PHASE TABLE
-------------------
Let D be decrements needed for the next anger point to disappear and G be upcoming `doGrowth()`
executions until growth.

    D=1, G=1 -> anger still active at growth   -> false
    D=1, G=2 -> anger gone before growth       -> true
    D=2, G=2 -> anger still active at growth   -> false
    D=2, G=3 -> anger gone before growth       -> true

The correct condition for the normal assignment-before-city-turn path is therefore `D < G`.

LIVE CITIZEN-VALUATION CONSEQUENCE
----------------------------------
As with F530, the false future happiness feeds directly into `iPopToGrow` and therefore the food
growth component of `AI_yieldValue`.

A particularly clean ordinary state is:

- one anger stack remains with timer 1;
- without that stack the city would have one spare happiness, so current `iHappinessLevel == 0`;
- food is already above the 80% `bBarFull` threshold;
- the city will grow on the upcoming `doGrowth()` (`iApproxTurnsToGrow == 1`);
- no avoid-growth / food-production / severe-unhealth exception applies;
- a useful future job exists and `iGrowthValue > 0`.

BUGGED phase test:

    1 <= 1
    => iFutureHappy = 1
    => iPopToGrow = 1
    => normal positive growth-food valuation remains active

CORRECT phase test:

    1 < 1 is false
    => no recovered happiness yet
    => iPopToGrow = 0

Because the food bar is already full enough to disable the fallback `bFillingBar` path, the
correct state does not give food a growth bonus for a population increase that will occur before
anger expires. The bugged state does. The same +1F versus +1H controlled ranking used for F530 can
therefore flip here as well, but on a NON-multiple timer value; this is why F531 is not merely an
extension of F530's modulo boundary.

UPSTREAM / CURRENT BOUNDARY
---------------------------
The `<= iApproxTurnsToGrow` comparison is inherited throughout the same code lineage:

- Civ4CE BtS reconstruction: present.
- K-Mod 1.46: present.
- Base AdvCiv 1.14: present.
- Current AdvCiv-SAS: present.

SAS does not change the relevant city-turn ordering: normal player citizen assignment still occurs
before city `doGrowth`, and the three anger timers still decrement later in `CvCity::doTurn`.

DEDUP / ROOT SEPARATION
-----------------------
F531 is adjacent to but independent from F530 / KI#853.

F530:
- expression defect: `timer % length`;
- maps exact cycle multiples (e.g. timer 10 at length 10) to 0 instead of 10;
- remains wrong even if comparison uses strict `<`.

F531:
- event-phase defect: `D <= G` despite anger decrement occurring after each `doGrowth`;
- fails on ordinary non-boundary states such as timer 1 / growth 1;
- remains wrong even after replacing modulo with F530's correct positive-cycle remainder.

The two bugs require independent corrections and regression cases, so keep separate roots despite
sharing the same future-happiness block.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
After F530's corrected positive-cycle remainder is calculated, use a strict comparison for the
normal pre-city-turn valuation path:

    if (iTurnsUntilAngerIsReduced < iApproxTurnsToGrow)
        iFutureHappy++;

Because `AI_yieldValue` is callable from multiple contexts, implementation should verify that no
important caller deliberately invokes this specific future-growth estimate after the current
turn's anger decrement but before the next growth. If such a context exists, encode the event
phase explicitly rather than silently preserving the wrong normal-turn result. The confirmed root
is the ordinary `CvPlayer::doTurn -> AI_assignWorkingPlots -> CvCity::doTurn -> doGrowth -> anger
decrement` path.

REGRESSION TESTS
----------------
A. Last anger turn / immediate growth
1. Set one anger source to timer 1.
2. Arrange growth on the upcoming `doGrowth` (`iApproxTurnsToGrow=1`).
3. During the normal pre-city-turn citizen assignment, assert that source does NOT add future
   happiness.

B. Last anger turn / growth one city turn later
1. Timer 1.
2. `iApproxTurnsToGrow=2`.
3. Assert the source DOES add future happiness because the timer reaches zero after the first
   non-growing `doGrowth` and before the second.

C. Two-decrement boundary
Assert D=2/G=2 is false and D=2/G=3 is true.

D. Combined F530+F531 matrix
With length 10, use F530's corrected decrement counts and then the strict phase comparison:

    timer 10, growth 10 -> false
    timer 10, growth 11 -> true
    timer  1, growth  1 -> false
    timer  1, growth  2 -> true
    timer 11, growth  1 -> false
    timer 11, growth  2 -> true

E. Citizen ranking
At current happiness balance 0 and an already-full food bar, verify that timer 1 / immediate growth
does not activate the premature growth-food value and cannot flip a neutral +1H job to +1F solely
through predicted anger recovery.

NUMBERING / CURRENT STONE
-------------------------
F531 / KI#854 is CONFIRMED.
F532 / provisional KI#855 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP300 (this file).

NEXT
----
Continue under F532 unused. Finish adjacent `AI_yieldValue` timing arithmetic, especially anger
stacking / approximate growth / Slavery devalue terms, then resume the remaining low-coverage
Worker/improvement and persistent-state/out-parameter scan. Do not split another finding from this
future-happiness block unless it survives after BOTH F530 and F531 repairs.

END C031-WIP300

================================================================================
C031-WIP301 — POST-F531 F532 CONTROLS / SAS FILE-LOCAL HELPER COVERAGE
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F531 confirmed.
F532 / provisional KI#855 remains UNUSED.
Repository source untouched.

This checkpoint preserves a negative-control / broadening tranche after WIP300. No new root is
numbered here.

1. AI_yieldValue ADJACENT SLAVERY / HURRY ARITHMETIC — CLOSED
-------------------------------------------------------------
After F530/F531, the remaining obvious Slavery denominator and hurry-loop concerns were challenged
against current shipped XML.

`AI_yieldValue` can divide its population-rush value by `getHurryCostModifier(true)`. The generic
engine formula could mathematically reach zero if XML/player hurry modifiers reached -100, but
current SAS has no negative `iHurryCostModifier` / `iGlobalHurryModifier` data that supplies such a
state. Current `CIV4HurryInfo.xml` has:

- HURRY_POPULATION: 27 production per population, anger enabled;
- HURRY_GOLD: 0 production per population, 3 gold per production, no anger.

The unusual `kOwner.canHurry(eHurry) || canPopRush()` loop can therefore inspect Gold hurry while
population rushing is available, but the inaccessible Gold entry contributes zero production per
population and cannot win the maximum. No current divide-by-zero or valuation collision was proved.
Keep as robustness/style debt only.

2. AI_timeWeightedImprovementYields UPGRADE NORMALIZATION — CLOSED
-----------------------------------------------------------------
The apparent `1-exp(0)` denominator risk does not occur with current improvement data. The shipped
upgrade graph is Cottage -> Hamlet -> Village -> Town with positive upgrade times (10/20/40), while
`iTimeScale == 0` has an explicit never-upgrades path. No zero-time upgrade cycle / live zero
denominator witness exists.

3. AI_growthValuePerFood TRANSIENT SPECIALIST COUNTS — CLOSED
-------------------------------------------------------------
The inherited comment about free specialists and a possible negative
`getMaxSpecialistCount - getSpecialistCount` state was challenged. A transient negative
`iAvailable` does not append phantom jobs; the assertion path is tolerant because negative extra
specialists can satisfy `isSpecialistValid`, and only positive availability is appended to the
unworked-job list. Free specialists do not consume ordinary population jobs. No current gameplay
root was established.

The separate `std::partial_sort(..., std::less_equal<int>())` non-strict-comparator issue remains a
formal STL robustness note already recorded earlier; no deterministic current gameplay witness was
added here.

4. SAS FILE-LOCAL HELPER COVERAGE — KI#197.11 BROADENING, NOT F532
-----------------------------------------------------------------
The previously zero-literal-coverage SAS file-local helpers were audited before returning to the
large inherited bodies.

`SAS_countPortCitiesForWaterArea` counts only:

    pLoopCity->waterArea(true) == &kWaterArea

and ignores `secondWaterArea()`. In a dual-coast city whose relevant selected sea is its secondary
water area, the secured peaceful naval profile undercounts local ports. Since the stock ceiling is:

    navalPortCities * MAX_TRACKED_UNITS_PER_PORT_CITY

this can lower the peaceful stock limit and shut the forced secured-naval production floor off too
early on that sea.

`SAS_getMilitaryProductionCapacityPercent` has the same primary-water-only topology when called for
a selected naval water area: a dual-coast city producing a tracked naval role for its secondary sea
does not contribute to the selected-sea numerator.

These are real topology/accounting broadenings, but DO NOT spend F532. They belong to the existing
KI#197.11 secured-naval-production-share / selected-water-area accounting family (and overlap the
already documented second-water topology family around KI#193.2 / KI#197.6). Keep one root and
broaden its repair/test matrix.

5. AI_strictAdditionalHappy GLOBAL HAPPINESS OMISSION — CURRENTLY MASKED / CLOSED
-------------------------------------------------------------------------------
The custom helper intentionally counts reliable local happiness channels but does not add
`CvBuildingInfo::getGlobalHappiness()`.

Current XML contains exactly two positive global-happiness buildings discovered in this pass:

- Hagia Sophia: +2 global happiness;
- Eiffel Tower: +1 global happiness.

Both are Wonders. Crucially, every live use of `AI_strictAdditionalHappy` sits inside:

    if (!bWonder && SAS_AI_BUILDING_VALUE_REGULAR_BUILDINGS_OPTIMIZE)

Therefore neither shipped global-happiness building can reach the strict regular-building
prefilter at all. The omission cannot currently misclassify a live building in this code path.
Treat as future-proofing debt only. If a future non-Wonder receives `iGlobalHappiness > 0`, extend
the helper or re-evaluate the gate.

6. PERSISTENT CvCityAI MEMBER / SAVE-LOAD INVENTORY — CLOSED
------------------------------------------------------------
Decision-bearing `CvCityAI` members were checked across constructor initialization and read/write
serialization. The new culture-victory rank and investment fields are explicitly transient and
intentionally not serialized. Normal AI turn start calls `CvPlayerAI::AI_updateCommerceWeights()`
before that AI's research/commerce/military/production work, refreshing those fields before the
normal AI decision path. Other persistent decision state has matching constructor/read/write
handling; obvious caches are intentionally serialized for deterministic mid-turn/replay behavior.
No independent save/load discontinuity was proved.

7. AI_projectValue LOW-COVERAGE LARGE BODY — CURRENT SAS DELTA CLOSED
--------------------------------------------------------------------
`AI_projectValue` had only one literal album mention despite being a large live project-production
valuation helper. A direct Base AdvCiv 1.14 vs current SAS semantic diff shows that SAS changes in
this body are reference caching only (`kGame`, `kTeam`, `kOwner`) and do not alter the project
formula. The inherited TechShare, SDI, Manhattan and Space-victory arithmetic was challenged for
obvious invalid sentinels / zero divisors / wrong receiver use; no live independent F532 candidate
survived this tranche.

CURRENT STONE
-------------
F532 / KI#855 remains UNUSED.

NEXT
----
Continue the inherited low-coverage mechanical scan rather than revisiting the closed SAS-local
helper tranche. Prioritize:

- remaining denominator / multiply-before-divide expressions with broad runtime domains;
- wrong-variable / wrong-receiver and output-parameter contracts;
- AI_calculatePlayerCloseness / AI_cityThreat adjacent mechanics only where a hard current
  consequence can be shown;
- Worker/best-build consequences only if independent of already-owned KI#213.

Do not create a new root for additional primary/second-water omissions inside the already-owned
secured-naval / assault-transport feature families.

END C031-WIP301

================================================================================
C031-WIP302 — F532 / KI#855 CONFIRMED: NATURAL-YIELD THRESHOLD OMITTED FROM
FINAL-IMPROVEMENT PROJECTION; FINANCIAL COTTAGE JOBS CAN BE RANKED BELOW WORSE JOBS
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F532 confirmed.
F533 / provisional KI#856 is the next UNUSED root.
Repository source untouched.

This checkpoint first closes the temporary-citizen Worker-demand concern opened after WIP301, then
promotes a separate inherited AdvCiv/K-Mod projection defect that survives a current-SAS XML fixture
and a direct ordinary citizen-assignment flip.

1. AI_updateWorkersHaveAndNeeded TEMPORARY AI_addBestCitizen MUTATION — CLOSED
----------------------------------------------------------------------------
`AI_updateWorkersHaveAndNeeded()` temporarily models the extra citizen expected from future growth by
calling:

    CityPlotTypes eBestPlot = NO_CITYPLOT;
    SpecialistTypes eBestSpecialist = NO_SPECIALIST;
    if (angryPopulation() == 0)
        AI_addBestCitizen(true, true, &eBestPlot, &eBestSpecialist);

It later removes exactly the job returned through those out-parameters:

    if (eBestPlot != NO_CITYPLOT)
        setWorkingPlot(eBestPlot, false);
    if (eBestSpecialist != NO_SPECIALIST)
        changeSpecialistCount(eBestSpecialist, -1);

The full `AI_addBestCitizen()` body was checked rather than assuming the out-parameters were merely
advisory. Every successful mutation reports the object it actually added:

- a plot mutation sets `eBestPlot` and works that plot;
- a specialist mutation sets `eBestSpecialist` and increments that specialist;
- when no job can be added, the helper returns false without creating an unreported citizen job.

The secondary setter effects were then checked. `setWorkingPlot(true/false)` symmetrically changes
working population and plot-yield accounting; the extra `updatePlotBuilder()` call is graphical
plot-builder maintenance and does not alter authoritative AI/gameplay state. Specialist assignment
similarly updates specialist population, yields/commerce and Great-Person rate through the ordinary
specialist processing path, and the inverse `changeSpecialistCount(...,-1)` restores those values.
No gameplay-side state that survives the rollback was found.

Conclusion: this is an intentional inherited K-Mod simulation of the city's next citizen, not an
unbalanced persistent mutation. Do not spend a finding on it.

2. F532 ROOT — ADVC NATURAL-THRESHOLD MIGRATION DID NOT REACH K-MOD'S FINAL-UPGRADE
   PROJECTION
----------------------------------------------------------------------------------
Current SAS Financial no longer uses the original ordinary extra-yield threshold. Its trait XML is:

    <ExtraYieldThresholds/>
    <ExtraYieldNaturalThresholds>
        <iExtraYieldNaturalThreshold>0</iExtraYieldNaturalThreshold>
        <iExtraYieldNaturalThreshold>0</iExtraYieldNaturalThreshold>
        <iExtraYieldNaturalThreshold>3</iExtraYieldNaturalThreshold>
    </ExtraYieldNaturalThresholds>

The source comment identifies this as `advc.908a`: the original Financial +1-commerce threshold was
replaced with a "natural" commerce threshold of 3.

The authoritative tile-yield calculation in `CvPlot::calculateYield()` knows about BOTH mechanisms.
After the ordinary `getExtraYieldThreshold` test, AdvCiv separately applies:

    int iThresh = GET_PLAYER(ePlayer).getExtraYieldNaturalThreshold(eYield);
    if (iThresh > 0)
    {
        if (iYield >= iThresh || iNatureYield + 1 >= iThresh)
            iYield += EXTRA_YIELD;
    }

Thus actual current Financial plot yields correctly receive the extra commerce whenever the AdvCiv
natural-threshold rule is met.

`CvCityAI::AI_finalImprovementYieldDifference()`, however, is still structurally the old K-Mod
projection. It calculates the raw current-improvement -> final-upgrade yield difference and then
contains only this threshold correction:

    int extraYieldThresh = kOwner.getExtraYieldThreshold(eLoopYield);
    if (extraYieldThresh > 0 &&
        kPlot.calculateNatureYield(eLoopYield, getTeam()) < extraYieldThresh)
    {
        if (kPlot.getYield(eLoopYield) > extraYieldThresh)
            iYieldDiff -= EXTRA_YIELD;
        if (kPlot.getYield(eLoopYield) + iYieldDiff > extraYieldThresh)
            iYieldDiff += EXTRA_YIELD;
    }

There is no `getExtraYieldNaturalThreshold()` branch.

With current XML, every ordinary `ExtraYieldThreshold` is zero for Financial, so this entire
correction is dead precisely where the live Financial rule is now stored. The helper therefore
projects only the raw improvement-yield delta and can omit the +1 commerce that the final upgrade
will really receive under `calculateYield()`.

3. WHY THIS IS NOT THE OLD WIP288 EXTRA-YIELD CONTROL
----------------------------------------------------
WIP288 challenged an apparent `>` / `>=` asymmetry in the older ordinary
`ExtraYieldThreshold` projection path and correctly closed that specific issue for current data:
Financial had moved to `ExtraYieldNaturalThreshold`, while shipped ordinary thresholds were zero.

That negative control was too narrow. The exact reason the ordinary-threshold comparator was
currently data-dead creates the new live defect: the replacement natural-threshold mechanism was
never added to K-Mod's projected final-improvement calculation.

Therefore WIP288's conclusion should be read narrowly as:

    ordinary ExtraYieldThreshold comparator edge -> current-data dead

NOT as:

    all current Financial projected-upgrade threshold handling -> correct.

F532 corrects that audit boundary rather than colliding with the old note.

There is also an earlier Queue-004 AdvCiv defect in `CvPlayer::updateExtraYieldThresholds`: the
natural-threshold minimum-selection condition compares against the unrelated ordinary threshold.
That source typo is currently masked because only Financial supplies one positive natural threshold.
F532 is independent: even when the player's stored natural threshold is the correct value 3,
`AI_finalImprovementYieldDifference()` never reads it at all.

4. CURRENT SAS PLAINS COTTAGE -> TOWN WITNESS
---------------------------------------------
Use a Financial AI city after Machinery but before Printing Press and Steam Power, with no river on
the candidate plots.

Current SAS XML gives:

Plains terrain:

    1 food, 1 hammer, 0 commerce

Cottage:

    +1 commerce
    upgrades through Hamlet / Village to Town

Town:

    +4 commerce

Financial natural threshold:

    +1 commerce when the AdvCiv natural-threshold rule reaches Commerce 3

Therefore a non-river Plains Cottage currently yields:

    1 food, 1 hammer, 1 commerce

It does not yet meet the Financial threshold.

Its true eventual Financial Town yield is:

    base Plains                  1F / 1H / 0C
    Town raw improvement                    +4C
    Financial natural threshold             +1C
    --------------------------------------------
    actual eventual Town         1F / 1H / 5C

But `AI_finalImprovementYieldDifference()` sees only the raw Cottage->Town improvement change:

    Town raw 4C - Cottage raw 1C = +3C

and never adds the natural-threshold crossing. Starting from the currently worked Cottage's 1C, its
projected final job is therefore only:

    BUG projection:              1F / 1H / 4C
    correct projection:          1F / 1H / 5C

The projection is one commerce low exactly because the upgrade chain crosses the current Financial
natural threshold.

5. CURRENT SAS SAME-FOOD COMPETITOR
-----------------------------------
Current SAS moved Lumbermill construction to Machinery. Before Steam Power, a non-river Plains
Forest + Lumbermill has:

    Plains             1F / 1H / 0C
    Forest                  +1H
    Lumbermill                       +1C
    ------------------------------------
    total              1F / 2H / 1C

The Lumbermill has no upgrade chain and, in a no-bonus fixture, no special-improvement value that is
needed to create the comparison. A fresh Cottage can likewise use zero upgrade-progress bonus for
the controlled comparison.

The two jobs therefore have equal food, which removes starvation/growth-food differences from the
ranking and lets the omitted future commerce determine the result directly.

6. DIRECT AI_jobChangeValue / AI_addBestCitizen RANKING FLIP
------------------------------------------------------------
Use a controlled normal production/research state:

- city is producing;
- 100% Research;
- current Research commerce AI weight 105;
- ordinary/average yield modifiers 100%;
- no extra production modifier;
- no emphasis or special-yield multiplier changing these two jobs;
- enough happiness/production to avoid unrelated low-production or excess-happiness adjustments;
- neither a specialist nor another plot outranks the two candidates.

Under this state, the non-food portion of `AI_yieldValue` is:

    1 hammer   -> 8
    1 commerce -> 4 at Research weight 105 after integer arithmetic

So:

Current Financial Plains Cottage:

    1H + 1C = 8 + 4 = 12

BUG projected final Town:

    1H + 4C = 8 + 16 = 24

CORRECT projected Financial Town:

    1H + 5C = 8 + 21 = 29

Plains Forest Lumbermill:

    2H + 1C = 16 + 4 = 20

K-Mod deliberately blends an improving plot's current and final job values. When the final value is
higher, `AI_jobChangeValue()` uses:

    (40 * current + 60 * final) / 100

Therefore:

    BUG Cottage     = floor((40*12 + 60*24) / 100) = 19
    CORRECT Cottage = floor((40*12 + 60*29) / 100) = 22
    Lumbermill      = 20

The common food contribution is identical and cancels.

Current code therefore ranks:

    Lumbermill 20 > Cottage 19

while the same city's actual Financial rule requires:

    Cottage 22 > Lumbermill 20.

This is a direct citizen-job reversal, not merely a displayed projected yield being one point low.
`AI_addBestCitizen()` normally ranks unworked plots through `AI_plotValue()` / `AI_jobChangeValue()`
during `AI_assignWorkingPlots()`, and `AI_juggleCitizens()` uses the same valuation path for swaps.
An ordinary Financial city can consequently choose the Lumbermill when the K-Mod long-term
improvement policy, evaluated with the real current trait rule, would choose the Cottage.

7. SECONDARY CURRENT POLICY WITNESS — GOOD-TILE THRESHOLD
---------------------------------------------------------
A second current path demonstrates that the missing commerce can cross a hard heuristic threshold,
although the direct Plains-Cottage assignment flip above is the stronger primary witness.

Current SAS Tundra has 1F / 0H / 1C. On a NON-river fresh-water Tundra Cottage (fresh water can make
Cottages valid), the current Financial plot has 2 commerce and the true Town reaches 6 commerce;
the stale final-improvement projection reaches only 5.

Under the healthy `AI_getPlotMagicValue` arithmetic used by `AI_countGoodTiles`, this can move the
plot approximately from:

    BUG:     95
    CORRECT: 135

across the live `AI_countGoodTiles(..., 100)` boundary used by Great-Person emphasis logic.

Do not use a riverside Tundra fixture for this witness: riverside natural commerce can make the
Financial bonus apply to the current Cottage already, changing the threshold-crossing delta.

8. ANCESTRY / ROOT BOUNDARY
---------------------------
K-Mod introduced `AI_jobChangeValue`, `AI_finalImprovementYieldDifference` and the surrounding
long-term improvement-job valuation in its 2014 lineage (commit
`313e2ae739864fd5968c5c0de3c80487cdee6f08`). At that time the ordinary
`ExtraYieldThreshold` was the Financial mechanism, so consulting that API was appropriate for
K-Mod's data model.

AdvCiv later introduced the nerfed/natural Financial rule under `advc.908a` (earlier lineage around
`a44438ed380640a052f574cd47c299e00f91020b`) and ultimately separated it into the dedicated
`ExtraYieldNaturalThreshold` XML/API while restoring the original semantics of
`ExtraYieldThreshold` (commit `d01463b2942fc623578fadd059c92b302c83d29b`).

The authoritative `CvPlot::calculateYield()` was migrated to the new natural-threshold API, but the
K-Mod projection helper in `CvCityAI` was not. Base AdvCiv 1.14 and current SAS therefore inherit
the stale projection. K-Mod itself predates the separate natural-threshold feature; Civ4CE/BtS do
not provide this AdvCiv rule in the same form.

The most useful root description is therefore an AdvCiv feature-migration omission, not "K-Mod
implemented Financial incorrectly."

9. SAME-ROOT PARALLEL PROJECTION SITES
--------------------------------------
The audit also found parallel projection code that should be repaired/tested with F532 rather than
numbered separately:

A. `CvPlot::getYieldWithBuild()`

This helper preserves `iNatureYield`, calculates projected build/improvement yields, and then has
only the old K-Mod ordinary-threshold block:

    int iThreshold = GET_PLAYER(getOwner()).getExtraYieldThreshold(eYield);
    if (iThreshold > 0 &&
        (iYieldRate > iThreshold || iNatureYield >= iThreshold))
        iYieldRate += EXTRA_YIELD;

It never queries `getExtraYieldNaturalThreshold()`. Water-side Worker/Work-Boat projections can
therefore also be one commerce low when a build crosses Financial's natural threshold. SAS's custom
land-Worker evaluator masks many land consequences of this helper, but it does not make the shared
projection rule correct.

B. `CvCityAI::AI_timeWeightedImprovementYields()`

The source contains an explicit inherited TODO:

    todo: Extra yield for finacial civs
    (see AI_finalImprovementYieldDifference for guidence.)

This routine time-weights raw improvement yield changes without applying either current Financial
threshold mechanism. Audit/fix it as the same projected-yield semantic family rather than spending
a new finding unless a genuinely separate implementation root appears.

10. REPAIR DIRECTION
--------------------
Do not merely replace `getExtraYieldThreshold()` with `getExtraYieldNaturalThreshold()`: both APIs
remain legitimate and have different semantics.

For `AI_finalImprovementYieldDifference`, determine the trait-extra contribution for the CURRENT and
FINAL projected yields under both rules, then add only the difference:

    projected threshold delta =
        final extra-yield contribution - current extra-yield contribution

For the natural rule, mirror `CvPlot::calculateYield()` exactly:

    qualifies if projected total yield >= natural threshold
              OR nature yield + 1 >= natural threshold

The nature term and feature-removal state must correspond to the projected plot state. Preserve the
ordinary `ExtraYieldThreshold` semantics independently. Avoid double-counting when a trait/config
uses both mechanisms.

Prefer a shared projected-threshold helper if practical so `calculateYield`, `getYieldWithBuild`,
`AI_finalImprovementYieldDifference` and time-weighted improvement valuation cannot drift apart
again.

11. REGRESSION MATRIX
---------------------
A. Direct current Financial citizen assignment

After Machinery, before Printing Press / Steam Power:

    non-river Plains Cottage, fresh upgrade progress
    vs
    non-river Plains Forest + Lumbermill

under the controlled 100%-Research production state above.

Assert:

    current Cottage job value      = 12
    projected true Financial Town  = 29
    blended Cottage                = 22
    Lumbermill                     = 20

and that the governor chooses the Cottage. The pre-fix stale projection should reproduce Cottage 19
and the wrong Lumbermill choice.

B. Threshold-crossing delta

For a Financial upgrading plot where the current improvement is below Commerce 3 and the final
upgrade is at/above it, assert exactly one `EXTRA_YIELD` is added to the projected final-current
difference.

C. Already-qualified current plot

Use a state where the natural threshold applies both before and after the upgrade. Assert the trait
contributes zero to the upgrade delta; do not add +1 twice.

D. Ordinary threshold compatibility

Use a synthetic/data-test trait with `ExtraYieldThreshold` and verify the old ordinary semantics
remain correct independently of `ExtraYieldNaturalThreshold`.

E. Parallel build projection

Financial Ocean/resource + Work-Boat/Offshore-Platform projection crossing total Commerce 3 should
match the commerce produced by authoritative `calculateYield()` after the build.

F. Golden Age / route / feature controls

Ensure projected threshold handling does not accidentally double-count Golden Age yield and uses
the same nature/feature-removal assumptions as the target projection helper.

DEDUP / CLASSIFICATION
----------------------
F532 is independent of:

- WIP288's current-data-dead ordinary-threshold comparator edge;
- the Queue-004 `updateExtraYieldThresholds` natural-threshold minimum-selection typo, which is
  currently masked by having only one natural-threshold trait source;
- KI#213's SAS land Worker/best-build-cache architecture split;
- the current land `AI_getImprovementValue` bypass, because the primary F532 witness values an
  ALREADY EXISTING upgrading Cottage through live citizen assignment.

The `getYieldWithBuild` and `AI_timeWeightedImprovementYields` omissions should initially be treated
as F532 broadenings: they are parallel stale projection consumers of the same AdvCiv natural-yield
feature migration.

NUMBERING / CURRENT STONE
-------------------------
F532 / KI#855 is CONFIRMED.
F533 / provisional KI#856 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP302 (this file).

NEXT
----
Continue under F533 unused.

First perform a same-root sweep of remaining `getExtraYieldThreshold` / projected-yield sites so no
second number is spent on the advc.908a migration family. Then resume the remaining low-coverage
mechanical scan:

- wrong-variable / wrong-receiver / output-parameter contracts;
- broad integer products / divisors with live current domains;
- persistent state or cache keys with semantic parameters;
- inherited Worker/improvement mechanics only where independent of KI#213 and F532.

Promote F533 only for a live independent root with a concrete current consequence.

END C031-WIP302

===============================================================================
C031-WIP303 APPEND — QUEUE 005 / CvCityAI.cpp
===============================================================================

-------------------------------------------------------------------------------
F533 / KI#856 — ADVC PRACTICAL 1808 LOOP-STYLE REFACTOR REPLACED K-MOD'S
PREREQUISITE-SHORTAGE STATE UPDATE WITH `break`, SO OUTSIDE-TARGET CULTURE
CITIES ARE TREATED AS HAVING ENOUGH TEMPLE CAPACITY FOR EXTRA CATHEDRALS
-------------------------------------------------------------------------------
Status: CONFIRMED
Primary file: `CvGameCoreDLL/CvCityAI.cpp`
Supporting files: current `CIV4BuildingInfos.xml`, `CIV4BuildingClassInfos.xml`,
`CIV4WorldInfo.xml`, `CIV4VictoryInfo.xml`, `CvPlayer.cpp`, and the retained
AdvCiv practical-1808 diff/history.
Classification: inherited AdvCiv practical-1808 mechanical refactor regression;
K-Mod source is correct; Base AdvCiv 1.14 and current SAS retain the regression.
Ordinary current gameplay under a Cultural-victory strategy.

ROOT
----
Inside `AI_buildingValue`, K-Mod deliberately devalues a culture-multiplier
building in a city outside the required Cultural-victory target set when the
empire does not yet have enough prerequisite buildings to support an EXTRA copy
while reserving copies for the target culture cities.

K-Mod's loop keeps the shortage state explicitly:

    bool bHaveEnough = true;
    for (...; bHaveEnough && ...; ...)
    {
        ...
        if (kOwner.getBuildingClassCount(prereqClass) < iPrereqBuildings)
            bHaveEnough = false;
    }

and immediately consumes it:

    if (bHaveEnough &&
        iCultureRank - iCulturalVictoryNumCultureCities <= 3)
    {
        iCommerceMultiplierValue /= 20;
    }
    else
        iCommerceMultiplierValue /= 50;

AdvCiv practical 1808 / commit
`9fa16c2c7f2b709d0502354b8a7ea1f6bd8827b5`, titled
"Some style changes in CvCityAI (loops)", mechanically converted the indexed
loop to `FOR_EACH_ENUM(BuildingClass)`. In the same diff it changed the decisive
line from:

    bHaveEnough = false;

to:

    break;

but did not change the consumer after the loop. Therefore a prerequisite
shortage terminates the scan while `bHaveEnough` remains `true`.

This is not an interpretation of the old code: the retained diff shows the
semantic change directly, and the commit title describes loop/style cleanup,
not a change in Cultural-victory building policy.

CURRENT SAS DATA / PREREQUISITE ARITHMETIC
------------------------------------------
Current Cultural victory requires 3 Legendary cities.

All seven current Cathedral-style buildings are live non-limited buildings
with:

    +50% culture
    raw prerequisite = 2 matching Temples
    cost = 220

On `WORLDSIZE_STANDARD`, current
`iBuildingClassPrereqModifier = 38`, and `CvPlayer::getBuildingClassPrereqBuilding`
rounds upward:

    ceil(2 * 138 / 100) = 3 Temples per Cathedral step.

For a rank-4 culture-rate city with zero Cathedrals of the class completed,
the K-Mod reserve check passes:

    iExtra = required culture cities - completed Cathedrals
           = 3 - 0
           = 3

and, because Cathedrals are not limited, `getBuildingClassPrereqBuilding`
requires prerequisite capacity for:

    completed + iExtra + the candidate
    = 0 + 3 + 1
    = 4 Cathedrals

hence:

    4 * 3 = 12 matching Temples.

With only 3 matching Temples, the first Cathedral is perfectly legal to start,
but there is plainly NOT enough prerequisite capacity to spend that copy in an
outside-target city while preserving capacity for the three intended culture
cities.

K-Mod therefore sets `bHaveEnough = false` and divides the culture-modifier
component by 50. Current AdvCiv/SAS hits `break`, leaves `bHaveEnough = true`,
and (for ranks 4 through 6) divides by only 20.

Thus that culture-multiplier component is 2.5 times the intended value:

    current / correct = (1/20) / (1/50) = 2.5.

CLEAN CURRENT TWO-CATHEDRAL CONTROL
-----------------------------------
The current XML supplies a particularly clean same-city comparison.
`BUILDING_BUDDHIST_CATHEDRAL` and `BUILDING_DAOIST_CATHEDRAL` are structurally
identical in every gameplay field apart from their religion / building-class /
matching-Temple identity (and presentation identifiers); both even use Copper
for the 25% production bonus. Each has +50% culture, +2 state-religion
happiness, +1 happiness with Incense, two Priest slots and cost 220.

Construct a rank-4 city that contains both religions and can build both:

    Buddhist Temples empire-wide = 3
    Daoist Temples empire-wide   = 12
    completed Cathedrals of each = 0

The Buddhist Cathedral is legal but scarce: correct K-Mod policy applies `/50`.
The Daoist Cathedral has enough reserved prerequisite capacity: `/20` is valid.

Current SAS applies `/20` to BOTH because the Buddhist shortage merely executes
`break`. The raw `AI_buildingValue` therefore loses the intended distinction
between two otherwise-equal candidates. Generic `AI_bestBuildingThreshold`
(and the several focus-0 `AI_chooseBuilding` production gates) consume that
value, so the scarce outside-target Cathedral can remain competitive or be
selected when K-Mod deliberately meant to suppress it.

This fixture also avoids relying on the focused Culture-only path. That path
still contains a separate, correct `bAvoid = true` prerequisite-reservation
check. F533 is specifically the broken GENERAL commerce-multiplier valuation
used by generic building selection.

PLACEMENT-GUARD CHALLENGE
-------------------------
`AI_bestBuildingThreshold` has a later K-Mod placement guard for non-limited
buildings whose quantity is constrained by prerequisite classes. That guard can
sometimes prevent a lower-value city from consuming the only currently
available Cathedral slot when another city can already construct the same
Cathedral and values it more than 25% higher.

It does NOT make F533 data-dead:

* the outside-target city can be the only current city containing the relevant
  religion, while the three eventual Cultural-victory target cities do not yet
  have that religion and therefore fail `canConstruct`;
* the three Temples making the first Cathedral legal can reside in the rank-4
  city and other non-target cities;
* the very purpose of the K-Mod prerequisite-reserve branch is to avoid
  consuming capacity before the target cities are ready, including before
  religion spread makes their Cathedral construction possible.

Thus the broken general valuation has an ordinary reachable consumer and is not
made unreachable by the later placement heuristic.

ANCESTRY
--------
Retained source comparison:

* K-Mod 1.46: correct `bHaveEnough = false` state update;
* AdvCiv practical 1808 / `9fa16c2c7f...`: the loop/style refactor changes that
  update to `break` in the exact diff;
* Base AdvCiv 1.14: still has `break` and the unchanged `if (bHaveEnough ...)`
  consumer;
* current SAS: substantively retains the same regression.

DEDUP / BOUNDARY
----------------
No existing album root was found for this `bHaveEnough` state-loss.

It is independent of:

* F442 / KI#765, which is a queued-building UI prerequisite-counting mismatch;
* F532 / KI#855, which is Financial projected-yield threshold migration;
* the correct `BUILDINGFOCUS_CULTURE` `bAvoid` check later in
  `AI_buildingValue`;
* generic building cache/invalidation findings.

Do not broaden F533 into a claim that Cathedral legality is wrong. Runtime
`canConstruct` correctly enforces the currently required Temple count. The bug
is specifically the AI's reserve-aware VALUE policy for an extra culture
multiplier outside the Cultural-victory target cities.

CONSERVATIVE REPAIR
-------------------
Restore the state transition, preserving the current enum loop:

    if (kOwner.getBuildingClassCount(eLoopBuildingClass) < iPrereqBuildings)
    {
        bHaveEnough = false;
        break; // optional after setting state
    }

or make the loop condition/consumer carry an equivalent explicit shortage
boolean. Do not merely remove `break`: the scan may stop once any prerequisite
shortage is found, but that fact must survive the loop.

REGRESSION TESTS
----------------
1. Standard-size Cultural-victory strategy, 3 target cities, rank-4 evaluator,
   zero matching Cathedrals, exactly 3 matching Temples:
   assert `bHaveEnough == false` after the reserve check.

2. Same state with 12 matching Temples:
   assert `bHaveEnough == true`.

3. Buddhist/Daoist paired fixture above:
   with 3 Buddhist Temples and 12 Daoist Temples, assert the Buddhist culture
   multiplier uses `/50` and Daoist uses `/20` before later generic valuation.

4. Top-three culture city control:
   assert the outside-target reserve branch is not used there; preserve the
   existing `/15` target-city logic.

5. `BUILDINGFOCUS_CULTURE` control:
   preserve its independent `bAvoid` prerequisite check.

NUMBERING / CURRENT STONE
-------------------------
F533 / KI#856 is CONFIRMED.
F534 / provisional KI#857 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP303 (this file).

NEXT
----
Continue under F534 unused.

First finish the practical-1808 semantic-refactor sweep, looking specifically
for other state-carrying loop rewrites where `break` / `continue` replaced an
assignment or where moved code changed which loop an exit applies to. Keep any
irrigation/legacy land-worker consequences deduplicated against KI#213 and the
current SAS land-evaluator bypass. Then resume the remaining low-coverage
mechanical scan (wrong object/variable, output contracts, integer domains,
cache/state lifecycle). Promote F534 only for an independent live current root.

END C031-WIP303


===============================================================================
C031-WIP304 — F534 / KI#857 CONFIRMED
CvCityAI::AI_doPanic accepts / force-hurries sea combat units for a land-only
panic deficit, then returns without selecting a land defender
===============================================================================

STATUS
------
F534 / KI#857 CONFIRMED.
F535 / provisional KI#858 is the next UNUSED root.

ROOT
----
`CvCityAI::AI_doPanic()` decides whether the city is in immediate panic from a
LAND-war / LAND-strength comparison, but its shortcut for an already-produced
combat unit tests only generic `UnitInfo::getCombat() > 0`:

    int iOurDefense = GET_PLAYER(getOwner()).AI_localDefenceStrength(
        plot(), getTeam());
    int iEnemyOffense = GET_PLAYER(getOwner()).AI_localAttackStrength(plot());
    ...
    UnitTypes eProductionUnit = getProductionUnit();
    if (eProductionUnit != NO_UNIT && getProduction() > 0 &&
        GC.getInfo(eProductionUnit).getCombat() > 0)
    {
        AI_doHurry(true);
        return;
    }

The default domain parameter of BOTH strength helpers is `DOMAIN_LAND`:

    AI_localDefenceStrength(..., DomainTypes eDomainType = DOMAIN_LAND, ...)
    AI_localAttackStrength (..., DomainTypes eDomainType = DOMAIN_LAND, ...)

and the function is entered only when `AI_isLandWar(getArea())` is true.

`getCombat() > 0`, however, is not a land-unit predicate. Current shipped sea
units including Galley, Trireme, Privateer, Frigate and Destroyer are
`DOMAIN_SEA` and have positive `iCombat` (e.g. current Frigate: DOMAIN_SEA,
iCombat=8).

Consequently a coastal city that is losing the LAND-strength comparison can
classify a ship in its queue as if it were already producing a relevant panic
defender.

CURRENT DETERMINISTIC FIXTURE
-----------------------------
Use an ordinary AI coastal city with:

* an active land war in its area;
* enemy local LAND attack strength greater than own local LAND defence strength;
* a partially built Frigate (`getProduction() > 0`);
* Slavery active and enough population for `canHurry(HURRY_POPULATION)`.

Current `UNIT_FRIGATE` is DOMAIN_SEA with positive combat, so the shortcut is
entered. `AI_doHurry(true)` loops the legal hurry types and, in force mode,
immediately executes the first legal one:

    if (bForce)
    {
        hurry(eHurry);
        break;
    }

The city therefore pays the Population-hurry cost / anger to accelerate the
Frigate and `AI_doPanic()` returns immediately.

It never reaches its actual emergency land-unit chooser:

    AI_chooseUnit(UNITAI_CITY_COUNTER)
    AI_chooseUnit(UNITAI_CITY_DEFENSE)
    AI_chooseUnit(UNITAI_ATTACK)

The rushed Frigate is outside the DOMAIN_LAND defence value whose shortage
triggered the panic and cannot defend the city against the land assault. Thus
this is not a subjective choice between two useful defenders: the shortcut's
accepted object is outside the domain of the quantity the routine is trying to
repair.

NO-HURRY CONTROL / BOUNDARY
---------------------------
If no hurry method is currently legal, `AI_doHurry(true)` simply finds no
`canHurry` entry and returns; `AI_doPanic()` nevertheless returns as well.
That can also leave the city continuing its current combat unit instead of
choosing a panic defender.

Do NOT make that failed-hurry behavior a separate finding here. K-Mod / BtS
already treat an in-progress generic combat unit as a handled panic case, and
for a LAND combat unit retaining the current build can be an arguable authored
policy. F534's hard correctness boundary is narrower: DOMAIN_SEA units pass the
same shortcut even though the panic trigger is explicitly DOMAIN_LAND.

CURRENT TURN CONSEQUENCE
------------------------
`CvCity::doTurn()` calls `AI_doTurn()` before growth and production.
`AI_doTurn()` calls `AI_doPanic()` for AI cities before the normal draft/hurry/
emphasis calls. Therefore the forced population hurry is an authoritative
same-turn state change: population / hurry anger are spent before production is
processed later in that city turn.

Later generic production logic does not undo that cost. Even if another SAS
emergency/fallback mechanism subsequently chooses a land defender, the panic
routine has already consumed scarce population / hurry capacity on a ship and
failed to use its own land-defender branch.

ANCESTRY
--------
This is inherited rather than SAS-authored:

* stock BtS source as preserved by Civ4CE contains the same
  `getCombat() > 0 -> AI_doHurry(true) -> return true` shortcut;
* K-Mod 1.46 retains the same generic-combat condition while replacing the
  local-strength implementation;
* the earliest retained K-Mod import (`KMod_3_2414225e64...`) already contains
  the same logic;
* AdvCiv practical 2179 / `3676ec243b...` refactors `AI_doPanic` from bool to
  void and flattens the body, but preserves the same generic-combat shortcut;
* Base AdvCiv 1.14 and current SAS retain it.

The current SAS land-strength API makes the mismatch especially explicit
because `AI_localDefenceStrength` and `AI_localAttackStrength` both document
DOMAIN_LAND as their default domain.

DEDUP / INDEPENDENCE
--------------------
No prior album root was found for `AI_doPanic`, panic hurrying a ship, or this
land/sea domain mismatch.

This is independent of:

* KI#213 legacy land best-build / Worker-cache architecture;
* KI#193 / KI#197 dual-coast and naval-cap accounting families;
* ordinary `AI_doHurry` population-cost valuation findings (the panic call uses
  force mode and the root is the wrong unit domain selected before hurrying);
* generic naval-production tuning.

CONSERVATIVE REPAIR
-------------------
Only take the "current combat unit already helps the panic" shortcut for a
LAND-domain unit that can actually contribute to the land defence problem, for
example at minimum:

    CvUnitInfo const& kUnit = GC.getInfo(eProductionUnit);
    if (eProductionUnit != NO_UNIT && getProduction() > 0 &&
        kUnit.getDomainType() == DOMAIN_LAND && kUnit.getCombat() > 0)
    {
        AI_doHurry(true);
        return;
    }

A stronger future repair could test actual defensive usefulness / UnitAI rather
than generic combat strength, but that is a policy broadening and is not needed
to fix F534. The minimal correctness repair is to exclude DOMAIN_SEA (and any
other non-land domain) from a land-panic shortcut.

REGRESSION TESTS
----------------
1. Coastal city, land-war deficit, partially built Frigate, legal Population
   hurry: assert `AI_doPanic` does NOT force-hurry the Frigate merely because
   `iCombat > 0`, and continues to a land-defender choice.

2. Same fixture with a partially built ordinary land combat defender: preserve
   the existing shortcut / forced-hurry behavior.

3. No legal hurry method + sea combat unit: assert the sea unit does not block
   the panic land-defender chooser.

4. Air-unit control: preserve existing behavior unless separately redesigned;
   current air units are not the F534 witness because their `iCombat` field is
   generally not the positive land/sea combat field used by this shortcut.

NUMBERING / CURRENT STONE
-------------------------
F534 / KI#857 is CONFIRMED.
F535 / provisional KI#858 is the next UNUSED root.

Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP304 (this file).

NEXT
----
Continue under F535 unused.

First finish the remaining zero-/low-coverage production helpers adjacent to
`AI_doPanic` (`AI_chooseDefender`, `AI_commerceToProductionMultiplierShift`,
`AI_getHappyFromHurry`, `AI_highestTeamCloseness`, etc.) only for independent
mechanical errors. Then resume the broader wrong-object/output/integer/cache
pass. Keep land best-build consequences under KI#213 and naval topology/cap
consequences under KI#193/KI#197 unless a genuinely separate root survives.

END C031-WIP304


===============================================================================
C031-WIP305 — POST-F534 F535 CONTROLS / REMAINING LOW-COVERAGE PRODUCTION HELPERS
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F534 confirmed.
F535 / provisional KI#858 remains UNUSED.
Repository source untouched.

This checkpoint preserves the first independent-helper saturation tranche after
F534. No new root is numbered here.

1. AI_doPanic FAILED-HURRY BROADENING — NOT A SEPARATE ROOT
-----------------------------------------------------------
F534's hard root is the DOMAIN_SEA acceptance inside a DOMAIN_LAND panic.

When the current positive-combat unit cannot legally be hurried,
`AI_doHurry(true)` can return without changing production, after which
`AI_doPanic()` still returns. That behavior is inherited from BtS/K-Mod and can
also delay the explicit panic defender chooser. However, for a LAND combat unit
continuing the already-invested build can be an arguable authored policy.
Do not split this into F535 without a separate mechanical invariant.

Keep it as a regression-test broadening for F534, especially the stronger sea
unit case where the current production cannot contribute to the land-strength
deficit at all.

2. AI_chooseDefender — CLOSED
-----------------------------
The helper first tries one CITY_SPECIAL if none is currently stationed on the
city plot, then one CITY_COUNTER under the same condition, then falls back to
CITY_DEFENSE. Its owner/plot/UnitAI orientation is consistent with the helper's
purpose; no wrong domain / stale out-parameter / sentinel issue was found.

3. AI_updateAssignWork — CLOSED
-------------------------------
`AI_updateAssignWork()` is intentionally a small dirty-bit dispatcher:

    if (AI_isAssignWorkDirty())
        AI_assignWorkingPlots();

The authoritative plot/specialist/happiness/health setters examined in this
pass mark assignment work dirty through their existing city-state update paths.
No separate missed-reset or stale-assignment lifecycle was established here.

4. AI_citizenValue — CLOSED
---------------------------
The low-covered helper is simply:

    return 6 + GET_PLAYER(getOwner()).getCurrentEra();

Its live consumers use it as the documented approximate citizen opportunity
cost. Current era values are nonnegative and small; no sign, overflow, enum or
receiver defect exists in the helper itself.

5. AI_nukeEplosionValue — CLOSED AS ROUGH AUTHORED ESTIMATE
-----------------------------------------------------------
Despite its misspelled historical name, the helper is a bounded local estimate:

    10 + (production * 5 + commerceYield * 3) / 2

It is deliberately described by K-Mod/AdvCiv as a rough proxy used for both
nuclear defense and project valuation. The fact that a project-building city's
own output is used as a proxy for target-city nuclear damage is explicitly
acknowledged by the source comment; no accidental wrong receiver or invalid
sentinel was uncovered.

6. AI_calculateMilitaryOutput — CLOSED
--------------------------------------
The formula

    baseProduction * (100 + militaryProductionModifier
                      + 10 * productionExperience) / 100

has safe current shipped modifier / XP ranges and uses the intended city's
production state. No zero divisor, negative-domain transition, or wrong object
was found.

7. AI_highestTeamCloseness — CLOSED
-----------------------------------
The helper initializes to -1 and takes the max player-closeness among team
members. Its live team-war valuation caller supplies a valid living team; a
living team has a member, so the -1 empty-team sentinel is not a normal live
return there. Player/team orientation is correct.

8. AI_getHappyFromHurry — NO NEW MECHANICAL ROOT
------------------------------------------------
The helper's `getHurryAngerTimer() <= 1` restriction is conservative policy
about when population whipping should be credited as a happiness remedy. The
net immediate population-vs-new-anger arithmetic itself is coherent with the
engine's stacked hurry anger. Do not merge it into F530/F531 merely because it
reads the same timer: those findings are exact expiry / city-turn phase-order
errors in future-growth prediction, whereas no independent wrong phase or sign
was proved here.

9. AI_growthValuePerFood FREE-SPECIALIST WARNING — RETAINS PRIOR CONTROL STATUS
-------------------------------------------------------------------------------
The K-Mod comment that worked-specialist accounting "could get messed up by free
specialists" remains a real modeling caveat, and current SAS has generic free
specialists (e.g. Protectionism) as well as specific free-specialist building
effects. This pass did not yet derive a deterministic current citizen/job flip
that is independent of the already-recorded growth/specialist findings.

Do not promote from the comment alone. If revisited, distinguish generic free
specialist capacity (`totalFreeSpecialists`) from explicit typed
`getFreeSpecialistCount`, because the latter is stored/processed separately and
is not the same population-accounting mechanism.

CURRENT STONE
-------------
F535 / KI#858 remains UNUSED.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
Repository source untouched.
Latest physical checkpoint: C031-WIP305 (this file).

NEXT
----
Continue under F535 unused.

The broad zero-literal helper list is now mostly exhausted or mapped to existing
families (especially KI#213 for the disabled legacy land best-build cache).
Switch back to high-yield mechanical patterns across the remaining file:

* state-carrying loop refactors / wrong variable after a loop;
* PlayerTypes / TeamTypes / DomainTypes object mismatches;
* output parameters with early returns;
* multiply-before-divide integer domains;
* semantic cache keys and persistent state transitions.

Revisit the generic-free-specialist growth warning only if an exact current
job/growth decision reversal can be proved. Promote F535 only for an independent
live root.

END C031-WIP305


===============================================================================
C031-WIP306 — F535 UNUSED / OLDER CvCityAI STYLE-REFACTOR SEMANTIC SWEEP PARTIAL
===============================================================================

STATUS
------
F535 / provisional KI#858 remains UNUSED.
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
Repository source untouched.

After WIP305, the audit switched to AdvCiv's older explicitly style-oriented
CvCityAI commits because F533 demonstrated that mechanical control-flow cleanup
can silently lose semantic state.

ADV CIV 2019-05-07 STYLE COMMIT — PARTIAL CONTROL
-------------------------------------------------
Commit `20e0f168829b6befe2de4dc2f77f123c877c1bc0` is described in the retained
Base-AdvCiv log as "Many style changes" across CvCityAI and other files.
The retained diff is:

    LLM_Helpers/context/commit_diffs/
    AdvCivPreSAS_1497_20e0f16882.diff

A CvCityAI-only diff slice was generated locally for semantic inspection.
The first high-risk patterns checked so far are controls:

* early `continue` conversions in `AI_totalBestBuildValue` preserve the old
  nested `CITY_HOME_PLOT`, null-plot, area and Safe Automation predicates;
* `AI_countNumBonuses` and `AI_countNumImprovableBonuses` flatten nested guards
  into early continues without changing the owner / neutral / bonus conditions;
* the route/chop loop rewrites inspected so far retain the same positive-value
  and can-build gates;
* variable renames in `AI_estimateReligionBuildings` preserve the old obsolete
  building test;
* the then-existing Cathedral prerequisite code still retains
  `bHaveEnough = false` at this historical point. The F533 semantic loss is NOT
  born here; it occurs later in practical 1808 when that assignment is changed
  to `break`.

No F535 candidate has survived this partial pass yet.

POTENTIAL-FALSE-POSITIVE CONTROL
--------------------------------
The style diff also changes the capitulated-vassal World-Wonder
`AI_buildingValue` early return from `-1000` to `0`. That is not a style-only
semantic accident hidden by this audit: current callers treat nonpositive
building value as rejected, and later AdvCiv policy consistently uses 0 as the
hard "do not build" value. Do not promote it merely because the numeric sentinel
changed.

CURRENT EXACT CURSOR
--------------------
Continue the CvCityAI-only semantic sweep of `AdvCivPreSAS_1497_20e0f16882.diff`
from the remaining state-carrying control-flow hunks, especially:

* `bWait` / drafting refactor around the later third of the diff;
* `AI_stealPlots` flattening (dedup against F523 persistent override root);
* special-yield multiplier block (dedup against F529/F522 as appropriate);
* any `break` / `continue` that replaces an assignment or changes which loop is
  exited.

Then inspect the other Base-AdvCiv log entry described as style changes around
`CvCityAI::AI_updateWorkersNeededHere` only if its retained diff actually
contains meaningful CvCityAI semantics.

Promote F535 only for an independent current gameplay root; otherwise freeze the
style-commit sweep as a negative control and return to wrong-domain/out-param/
integer/cache lifecycle patterns.

NUMBERING / STONE
-----------------
F446-F534 confirmed.
F535 / KI#858 UNUSED.
32/169 unique files complete.
Latest physical checkpoint: C031-WIP306 (this file).
Repository source untouched.

END C031-WIP306


================================================================================
C031-WIP307 HANDOFF -- Queue 005 CvCityAI.cpp continued
F446-F534 confirmed; F535 / KI#858 UNUSED
Repository source untouched.
================================================================================

CONTINUATION FROM WIP306

This checkpoint preserves the post-WIP306 saturation work. No speculative
candidate in this tranche has been promoted.

1. ADVCIV PRACTICAL 1497 / 20e0f16882... STYLE-REFACTOR SWEEP -- CLOSED CONTROL

The remaining CvCityAI hunks were reconstructed semantically rather than judged
from indentation alone.

- AI_doDraft / bWait:
  The old and new code preserve every bWait=false assignment, the danger
  override, the probabilistic draft gate, logging and the final conscript()
  condition. No state was lost by the loop/control-flow flattening.

- AI_updateWorkersNeededHere / Worker-needed refactor:
  Mission-Worker counts, unimproved/working-plot handling, yield-value
  calculations and the temporary AI_addBestCitizen rollback are preserved.

- AI_stealPlots and special-yield flattening:
  The practical-1497 edits are structural. The later persistent override defect
  is already F523; the later emphasis/project defects are F522/F529 and have
  different birth points.

Result: practical 1497 does not contain another current independent root.

2. OTHER STYLE-ORIENTED HISTORY -- NEGATIVE CONTROLS

A style-commit inventory was checked rather than assuming "style" meant
semantic neutrality.

- AdvCiv practical 2146: AI_bestUnit changes are formatting/type cleanup.
- K-Mod practical 1140: AI_assignWorkingPlots specialist-enum rewrite preserves
  the old semantics.
- K-Mod practical 183: local-declaration/style cleanup includes an already-fixed
  historical AI_buildingValue issue, not a surviving current sibling.
- AdvCiv practical 2319's naval-assault threat hunk deliberately changes
  reveal/enemy iteration and fixed-point arithmetic for fog-of-war behavior;
  no wrong-object or lost-state regression was established.

The practical-1808 Cathedral bHaveEnough loss remains isolated as F533.

3. OUT-PARAMETER / EARLY-RETURN INVENTORY -- CLOSED EXCEPT KNOWN DEBT

Every live CvCityAI helper that exposes result state through pointer outputs was
checked against early exits and callers.

- AI_permanentSpecialistValue initializes its optional outputs.
- AI_bestProject still has the already-recorded capitulated-vassal early return
  that leaves piBestValue at the caller's sentinel instead of normal no-project
  0. Live callers either gate on NO_PROJECT or use max(-1, nonnegative), so this
  remains API/performance debt rather than a gameplay finding.
- AI_bestSpreadUnit does not initialize its outputs internally, but both current
  callers initialize eBestSpreadUnit=NO_UNIT and value=-1. Its old NULL-vs-
  NO_UNIT return bug is already cured as KI#74.
- AI_addBestCitizen's output-consuming Worker-demand caller initializes both
  sentinels, and the mutation/rollback path was previously proved reversible.
- AI_bestPlotBuild and AI_getImprovementValue write their outputs on their live
  success/return paths.
- AI_finalImprovementYieldDifference's piYields is intentionally in/out.
- SAS_AI_findBestFallbackUnit does not expose a new stale output contract.

No F535 here.

4. REMAINING LOW-COVERAGE CURRENT SAS DELTAS -- CLOSED CONTROLS

A fresh function-level SAS-vs-Base matrix was used to avoid broad rereads.

- AI_splitEmpireValue: current SAS delta is const/reference style only.
- AI_defensiveBuildingValue: current delta is cached kGame/kOwner/kTeam refs.
- AI_clearFeatureValue: cached-reference/performance delta only.
- AI_countGoodSpecialists: static default-specialist lookup only.
- AI_cityThreat: cached-reference/formatting delta; AdvCiv already corrected
  K-Mod's wrong-direction attitude concern.
- AI_calculateSettlerPriority: cached kTeam reference only.
- AI_permanentSpecialistValue's substantive custom Military Instructor formula
  remains the already-challenged control from WIP287/WIP290-era work.
- AI_processValue's custom Culture-process restriction was re-read for enum /
  validity interactions; no independent mechanical failure was established in
  this tranche. F527 remains the separate culture-pressure overflow root.

5. K-MOD PRACTICAL 1030 / 418581f6bd... AI_buildingValue REWRITE -- BOUNDED

Commit:
  418581f6bd5c51570f664e9ddee46cdd3bcb53f0
Title:
  "Made some large style adjustments to CvCityAI::AI_buildingValue"

The huge-looking diff was compressed by multiset-normalizing moved statements.
Almost all of the apparent ~2800-line semantic surface is literal code movement.

True semantic changes that matter:

A) BUILDINGFOCUS_WORLDWONDER implies WONDEROK.
   Intended by the commit.

B) Construction-value cache accepts neutral/WONDEROK/WORLDWONDER flag sets.
   This initially looked like another semantic-cache-key bug, but the
   WORLDWONDER eligibility/production-rank veto executes BEFORE the cache read.
   Once a building survives that veto, WONDEROK/WORLDWONDER do not alter the
   numerical valuation. WONDEROK itself is caller/filter permission.
   Therefore this specific cache permission is safe.

C) The World-Wonder top-production-city comparator introduced by practical
   1030 is the already source-documented KI#205 issue and is already fixed in
   current SAS (current comment records the old <= inversion). Do not spend
   F535 rediscovering it.

D) Focused pass cutoff:
   old code had an iValue < iThreshold pass-0 check; the refactor's consolidated
   cutoff uses iValue <= iThreshold. The only live positive iMinThreshold caller
   is AI_bestAdvancedStartBuilding (20/15/10/5/0 heuristic passes). There is no
   hard contract that an exact-equality heuristic score must qualify, and the
   surrounding selector itself consumes only positive values. This remains a
   threshold-policy boundary, not F535.

E) Other experience/health/etc. sections in the practical-1030 diff are either
   moved-identical statements or explicitly authored valuation rewrites.

Result: no new surviving practical-1030 regression beyond already-owned KI#205.

6. ASSERTION / INVARIANT SWEEP -- STARTED

108 live FAssert/FAssertMsg sites were inventoried and mapped to their containing
CvCityAI methods. Low-coverage assertions are being challenged against actual
call/data domains rather than treated as bugs from syntax alone.

Controls already closed:
- AI_neededAirDefenders iNearbyCities>0 is safe because the city itself is in
  the owner's city loop at distance 0.
- AI_projectValue's eSampleTech fallback cannot normally be empty for the live
  TechShare project path once the project is constructible; the team necessarily
  has project-enabling tech state.
- AI_setCityValPercent receives percentile-rank output; no negative caller path
  has been established.
- AI_countNumBonuses bLand||bWater callers use valid combinations.

Building-domain XML cross-check:
The AI_buildingValue experience block's DOMAIN_SEA branch would leave a tiny raw
sea-XP value in a noncoastal city if such a building were constructible there.
Current shipped XML has only one building with nonzero DOMAIN_SEA free XP:
  BUILDING_DRYDOCK, +3 DOMAIN_SEA XP.
Drydock is water/coastal-gated (bWater=1), so no current inland collision exists.
Keep this as a source-shape curiosity, not F535.

CURRENT DURABLE STONE

Queue 005 CvCityAI.cpp OPEN
32/169 unique files complete
F446-F534 confirmed
F535 / KI#858 UNUSED
WIP307 latest physical checkpoint
Repository source untouched

EXACT NEXT CURSOR

Continue the live assertion/invariant sweep from WIP307, prioritizing assertions
whose condition protects release arithmetic or object-domain assumptions:
- AI_bestBuildingThreshold limited-building iPrereqEach/iLimit invariants;
- AI_growthValuePerFood / free-specialist transient state (dedup prior note);
- AI_updateSpecialYieldMultiplier income/range controls;
- AI_calculateSettlerPriority site-count assumptions;
- any assertion whose failure exposes a release-mode wrong sign, divide-by-zero,
  stale object, or invalid enum.

If assertions saturate, return to high-yield mechanical patterns:
wrong object/enum domains, state lost across refactors, semantic cache keys,
multiply-before-divide overflow, early-return outputs.

Do not promote F535 merely for Debug-only assertions, heuristic equality
boundaries, or current-data-dead source smells.
================================================================================


================================================================================
C031-WIP308 HANDOFF -- Queue 005 CvCityAI.cpp continued
F446-F535 confirmed; F536 / KI#859 UNUSED
Repository source untouched.
================================================================================

CONTINUATION FROM WIP307

This checkpoint preserves the completed assertion/refactor controls after WIP307
and promotes one independent inherited growth/happiness defect as F535.

1. AI_bestBuildingThreshold LIMITED-PREREQ ASSERTIONS -- CLOSED CONTROL

The apparently Release-relevant assertions:

    FAssert(iPrereqEach > 0);
    ...
    FAssert(iLimit > 0);

were challenged against the actual producer/caller contract rather than assumed
safe from the assertions alone.

CvPlayer::canConstruct(eLoopBuilding, bContinue=false) already applies the same
prerequisite-building requirement, including buildings currently being made.
If a prereq-scaled building reaches the later limited-placement calculation,
available prerequisite capacity is necessarily sufficient for at least one
additional slot beyond built+making.

Thus:
- iPrereqEach is positive for the prerequisite class that caused this branch;
- iPrereqBuilt / iPrereqEach - iBuilt - iBuilding is at least 1.

No divide-by-zero / nonpositive-slot Release witness exists here.

2. AI_growthValuePerFood FREE-SPECIALIST / PARTIAL_SORT CONTROL

The K-Mod warning about free specialists was rechecked.

Free specialists are stored separately from getSpecialistCount(), so they are
correctly absent from the population-consuming worked-job list. The known
transient state in which getMaxSpecialistCount(type)-getSpecialistCount(type)
can become negative after obsolescence does not create invalid indexing or a
Release arithmetic failure in this helper.

The use of std::less_equal<int> as a partial_sort comparator remains formally
non-strict and therefore poor STL practice / robustness debt. Equal-valued jobs
are common, but no deterministic current gameplay consequence beyond unstable
selection among equivalent/worst jobs has been established. Do not promote it.

3. K-MOD PRACTICAL 1041 / 160f3e2819... AI_bestBuildingThreshold REFACTOR
   -- CLOSED SEMANTIC CONTROL

Commit:
  160f3e2819b6e4b3152f171e3316542f8974219d
Title:
  "Rearranged lots of code in CvCityAI::AI_bestBuildingThreshold."

The diff was normalized by cancelling moved-identical statements. The remaining
semantic changes match the commit message exactly:

- WORLDWONDER focus now skips non-world-wonder buildings early.
- Automated cities now reject TEAM wonders in addition to world/national
  wonders.
- Existing building production contribution is deliberately reduced from:
      + getBuildingProduction(eLoopBuilding)
  to:
      + getBuildingProduction(eLoopBuilding) / 4
- Large nested eligibility/value blocks are converted to equivalent early
  continue conditions.

Specific equivalence checks:
- old bWonderOk / !isLimitedWonderClass condition equals the new limited-wonder
  branch for ordinary focus flags;
- automated prereq-building rejection still occurs after canConstruct and before
  any candidate can win;
- iValue>0 nested body equals new iValue<=0 continue;
- wonder placement, rank/rand and iMaxTurns/hurry checks retain their logic.

No lost state/scope sibling of F533 exists in this refactor.

4. K-MOD PRACTICAL 1139 / 7d9e34dc78... GROWTH/HAPPINESS REWRITE

Commit:
  7d9e34dc788433a3d9ce3795bfcfe73dcedca934
Title:
  "Tweaked the handling of happiness and growth in AI_yieldValue."

This commit is the birth of F531's < -> <= change and was audited for adjacent
siblings.

The already-owned defects remain:
- F530: exact anger-cycle modulo maps a full remaining cycle to zero.
- F531: <= compares anger expiry to a growth event that happens BEFORE that
  turn's anger decrement.

A third, independent pre-gate defect survives.

===============================================================================
F535 / KI#858 CONFIRMED
CvCityAI::AI_yieldValue -- future-happiness pre-gate says at most +2 although
three independent anger-expiry channels can each add +1
===============================================================================

CURRENT SOURCE SHAPE
--------------------

Current SAS around AI_yieldValue:

    int const kMaxHappyIncrease = 2;

    // if happy is large enough so that it will be over zero after we do the checks
    if (iHappinessLevel + kMaxHappyIncrease > 0)
    {
        ... Hurry anger expiry ...
            iFutureHappy++;

        ... Conscript anger expiry ...
            iFutureHappy++;

        ... Defy-resolution anger expiry ...
            iFutureHappy++;
    }

The pre-gate therefore claims the following block can recover at most +2, while
the block visibly contains THREE independent +1 recovery channels.

EARLIEST REACHABLE HISTORY
--------------------------

The contradiction is not a later SAS/AdvCiv edit. It is already present in the
earliest reachable K-Mod import (2414225e64...), whose source comment says:

    // currently we can at most increase happy by 2 in the following checks
    const int kMaxHappyIncrease = 2;

and then immediately checks:
1. hurry anger;
2. conscript anger;
3. defy-resolution anger;

with each successful expiry incrementing the modeled happiness by one.

K-Mod 1.46, Base AdvCiv 1.14, Civ4CE's BtS reconstruction and current SAS all
retain the same mismatch.

CURRENT-DATA REACHABILITY
-------------------------

All three anger sources can coexist in current SAS.

- CIVIC_NATIONHOOD is a LEGAL civic and has iMaxConscript=3.
- CIVIC_SLAVERY is a separate LABOR civic and enables HURRY_POPULATION.
  Nationhood and Slavery can therefore be active simultaneously.
- Defy-resolution anger is independent city state from Apostolic/UN resolution
  defiance and can coexist with both.

Thus a city can legitimately have positive Hurry, Conscript and Defy-resolution
anger timers at once.

CLEAN INDEPENDENT FIXTURE
-------------------------

Use a healthy ordinary city with:

    current iHappinessLevel = -2

and all three anger sources due to lose their next unhappy point before the
candidate growth event.

To make independence from F530/F531 explicit, use the corrected timing model:

    each timer = 1
    iApproxTurnsToGrow = 2

After the F530 positive-cycle correction:
    decrements needed for each next anger drop = 1

After the F531 phase correction:
    1 < 2
so each of the THREE recoveries is genuinely available before growth.

INTENDED result:

    iFutureHappy = +3
    -2 + 3 = +1
    => at least one population of future happiness headroom exists

CURRENT pre-gate:

    -2 + kMaxHappyIncrease(2) = 0
    0 > 0 is false
    => the code skips ALL three timer checks
    => iFutureHappy remains 0 from these sources
    => -2 stays nonpositive

So the micro-optimization suppresses a real transition from unhappy/no-growth
headroom to +1 future happiness.

LIVE AI CONSEQUENCE
-------------------

The value feeds directly into:

    iPopToGrow = max(0, iHappinessLevel + iFutureHappy)

and therefore into the ordinary food-growth branch of AI_yieldValue.

With the bug:
    iPopToGrow = 0

With all three legitimate recoveries:
    iPopToGrow = 1

Because current happiness is negative, the bFillingBar fallback also does not
rescue the bugged state (it requires iHappinessLevel >= 0). The bug can therefore
remove growth-food value entirely when the city actually has enough stacked
anger expiry to become happy by the time it grows.

As in the controlled F530/F531 +1-food vs +1-hammer fixtures, any sufficiently
positive ordinary iGrowthValue can make the correct iPopToGrow=1 state prefer a
food job while the bugged iPopToGrow=0 state prefers the hammer/non-growth job.
The important deterministic root is the branch/state difference itself; it does
not depend on a subjective strategic threshold.

DEDUP / INDEPENDENCE
--------------------

Whole-album search found no prior root for kMaxHappyIncrease / the false "+2"
upper bound.

F535 is adjacent to but independent from F530 and F531:

F530:
- wrong number of decrements at exact timer multiples;
- can be fixed while F535 remains.

F531:
- wrong growth-vs-decrement phase comparison;
- can be fixed while F535 remains.

F535:
- even with both corrected decrement counts and strict phase comparison, the
  outer pre-gate can prevent all three correct checks from running when
  iHappinessLevel == -2.

CONSERVATIVE REPAIR DIRECTION
-----------------------------

At minimum, make the upper bound match the following channels:

    int const kMaxHappyIncrease = 3;

A more future-proof repair is to remove this tiny optimization or derive the
upper bound from the channels being evaluated so a future fourth/removed anger
source cannot silently desynchronize it again.

REGRESSION TEST
---------------

1. Set iHappinessLevel=-2.
2. Give Hurry, Conscript and Defy timers each a next anger drop one full city
   turn before growth (e.g. corrected D=1, G=2).
3. Assert all three recovery checks are evaluated.
4. Assert modeled future happiness from these sources is +3.
5. Assert iPopToGrow becomes at least 1 rather than remaining 0.
6. Controls:
   - only two expiring sources at happiness -2 should NOT create positive
     growth headroom;
   - three sources whose next drops occur at/after growth should not count;
   - F530 exact-cycle and F531 D==G tests remain separate.

STATUS
------

F535 / KI#858 CONFIRMED.
F536 / provisional KI#859 is now UNUSED.

CURRENT DURABLE STONE

Queue 005 CvCityAI.cpp OPEN
32/169 unique files complete
F446-F535 confirmed
F536 / KI#859 UNUSED
WIP308 latest physical checkpoint
Repository source untouched

EXACT NEXT CURSOR

Continue under F536.

First finish the remainder of the same AI_yieldValue growth/happiness historical
block, because three adjacent independent defects (F530/F531/F535) have now been
proved there. Challenge only hard/mechanical questions:
- fixed upper bounds that no longer match modeled channels;
- timer/event ordering;
- integer boundary arithmetic;
- a variable described as conservative/optimistic but used with the opposite
  sign or scope.

Then return to the remaining assertion/invariant sweep and low-coverage
mechanical patterns. Do not promote mere growth tuning approximations.
================================================================================


================================================================================
C031-WIP309 HANDOFF -- Queue 005 CvCityAI.cpp continued
F446-F536 confirmed; F537 / KI#860 UNUSED
Repository source untouched.
================================================================================

CONTINUATION FROM WIP308

The post-WIP308 cursor was K-Mod practical 199 / obsolete-building defensive
valuation. That investigation produced an independent, live obsolete-tech-value
root. F536 is confirmed below.

===============================================================================
F536 / KI#859 CONFIRMED
AI_obsoleteBuildingPenalty / AI_buildingValue bObsolete mode values building
effects that explicitly survive obsolescence as though the tech removed them
===============================================================================

ROOT CONTRACT
-------------

CvPlayerAI::AI_obsoleteBuildingPenalty states that it returns the positive value
of:

    "the buildings we will lose by researching eTech."

Current AdvCiv/SAS calls, for each currently active building obsoleted by eTech:

    pLoopCity->AI_buildingValue(
        eLoopBuilding, 0, 0, bConstCache,
        true, false, /*bObsolete=*/true);

The intended quantity is therefore the DELTA in building effects caused by
CvCity::processBuilding(..., bObsolete=true), not the value of the whole
building.

But AI_buildingValue's bObsolete parameter is used only in the defensive helper.
The rest of AI_buildingValue still values effects that the engine deliberately
retains after obsolescence.

CURRENT ENGINE SEMANTICS
------------------------

CvCity::processBuilding(BuildingTypes eBuilding, int iChange, bool bObsolete)
splits building effects into two groups.

The main block executes on an obsolescence transition and removes ordinary active
effects such as:
- happiness / health;
- trade routes;
- commerce/yield modifiers;
- specialist slots;
- air / nuke modifiers;
- many player/team effects.

But the later:

    if (!bObsolete)

block is intentionally skipped for an obsolescence transition. It retains,
among other things:
- local building defense;
- local building bombard defense;
- Great Person point generation from the building;
- building/class/wonder existence bookkeeping.

Separately, obsolete-safe commerce is explicitly independent of active-building
state. CvCity::getBuildingCommerceByBuilding starts from:

    kBuilding.getObsoleteSafeCommerceChange(eCommerce) *
        getNumBuilding(eBuilding);

and only adds normal commerce inside the getNumActiveBuilding() branch.

Thus ObsoleteSafeCommerceChange is, by definition, not lost when the building
obsoletes.

CLEAN CURRENT MONUMENT WITNESS
------------------------------

Current SAS BUILDING_MONUMENT:

    ObsoleteTech = TECH_LIBERALISM
    iCost = 30
    ObsoleteSafeCommerceChanges:
        +1 culture

It has no ordinary CommerceChanges and no other gameplay effect needed for this
witness.

Before Liberalism:
- getNumActiveBuilding(Monument) > 0, so AI_obsoleteBuildingPenalty evaluates it.

After Liberalism:
- the Monument remains physically present;
- its +1 ObsoleteSafe culture remains, because obsolete-safe commerce is based
  on getNumBuilding(), not getNumActiveBuilding();
- therefore this +1 culture suffers ZERO actual loss from Liberalism.

Current AI_buildingValue nevertheless contains, in its generic commerce
valuation:

    iTempValue = kBuilding.getCommerceChange(eLoopCommerce) * 4;
    iTempValue +=
        kBuilding.getObsoleteSafeCommerceChange(eLoopCommerce) * 4;

and equivalent focused-commerce additions.

bObsolete does not gate those terms.

Therefore every current active Monument contributes positive "lost building"
value to Liberalism even though its sole relevant +1 culture survives the tech.

This is a deterministic false obsolete-tech penalty, not a heuristic
disagreement.

CURRENT UNIQUE-BUILDING WITNESSES
---------------------------------

The same current Liberalism path is also live for Monument replacements with
+1 ObsoleteSafe culture:
- Egyptian Obelisk;
- Aztec Sacrificial Altar;
- Sumerian Ziggurat.

Some replacements have additional effects that really do obsolete, but their
ObsoleteSafe culture is still incorrectly included in the loss value.

CURRENT CASTLE / DEFENSE BROADENING
-----------------------------------

AdvCiv practical 1777 explicitly noticed one part of the root:

    "Defense abilities don't go obsolete. Looks like K-Mod had gotten that
     wrong. Tagging advc.001 (bugfix)."

It added:

    bool bRemoveDef = (bRemove && !bObsolete);

This correctly prevents the obsolete mode from entering the "remove local
defense" branch.

However, bRemoveDef=false then falls through the NORMAL ADD-BUILDING branch.
For an already-present obsolete Castle, that does not necessarily produce zero.

Current BUILDING_CASTLE:
    ObsoleteTech      = TECH_MILITARY_SCIENCE
    iRaiseDefense     = 100
    iBombardDefense   = 25
    prerequisite      = Walls

Current BUILDING_WALLS:
    iRaiseDefense     = 50
    iBombardDefense   = 50

No current SAS unit has bIgnoreBuildingDefense=1, so the local defensive
valuation is live in a normal non-isolated area.

Ordinary city with Walls + Castle:
    current city defense          = 100
    current building bombard def  = 50 + 25 = 75

For bRemove=true, bObsolete=true:
    bRemoveDef = false
    iOldDefenseModifier = 100
    iNewDefenseModifier = 100

The RaiseDefense term therefore happens to add 0 in this fixture.

But bombard valuation computes:

    iOldBombardMod = 75
    100 * min(25, 100 - 75) /
        max(80, 8 * (100 - 75))
    = 100 * 25 / 200
    = 12

Then:
    peaceful weighting -> 12 * 2 / 3 = 8
    war-plan weighting -> 12 * 3 / 3 = 12

So obsolete-mode valuation assigns +8 or +12 raw defensive value to Castle
bombard protection that CvCity::processBuilding explicitly DOES NOT REMOVE on
obsolescence.

For a science-flavor-0 leader, AI_obsoleteBuildingPenalty's normal 50% scale
turns that phantom local-defense component alone into roughly 4 or 6 tech-value
penalty per Castle, before legitimate lost Castle effects are counted.

The important root is not the exact number: retained defense should contribute
zero to the obsolete delta.

RETAINED GREAT-PERSON-POINT BROADENING
--------------------------------------

The same engine if (!bObsolete) block preserves building Great Person point
generation.

Current obsoleting Wonders such as:
- Great Lighthouse;
- Colossus;
- Great Wall

have GreatPeopleRateChange = +2 and ObsoleteSafe culture (+4 in these examples).

AI_buildingValue's generic GPP valuation and ObsoleteSafe commerce valuation do
not test bObsolete, so AI_obsoleteBuildingPenalty can count both retained
components as though the obsoleting tech destroyed them.

This is the same root, not additional finding numbers.

ANCESTRY
--------

Stock BtS / current Civ4CE does not contain AI_obsoleteBuildingPenalty.

K-Mod practical 199 / a865535e85... introduced the meaningful obsolete-building
penalty and calls the ordinary AI_buildingValue of each building that the tech
will obsolete. K-Mod therefore introduced the basic whole-building-vs-
actually-lost-effects mismatch.

K-Mod itself also treated Walls/Castles' local defense as lost.

AdvCiv practical 1777 / c0439987e0... introduced bObsolete and explicitly tried
to repair that defense mistake. It:
- passes bObsolete=true from AI_obsoleteBuildingPenalty;
- adds bObsolete to AI_defensiveBuildingValue;
- changes bRemoveDef to bRemove && !bObsolete.

But the repair is partial:
- bObsolete is not used to exclude ObsoleteSafe commerce;
- it is not used to exclude retained building GPP;
- the retained-defense path falls into add-mode and can still produce phantom
  bombard-defense value.

Base AdvCiv 1.14 and current SAS inherit this partial repair.

DEDUP / ROOT SEPARATION
-----------------------

Whole-album searches for:
- AI_obsoleteBuildingPenalty;
- obsolete building defense;
- Castle obsolescence;
- bObsolete;
- ObsoleteSafe commerce

found no prior finding for valuing retained building effects as obsolete losses.

This is separate from:
- F498 construction-cache lifetime/invalidation;
- WIP242's unnumbered semantic-cache-key note (cache ignores bObsolete);
- prior CvPlayer building-obsolescence lifecycle controls, which correctly proved
  that processBuilding removes/adds the effects that are SUPPOSED to obsolete.

F536 concerns the AI valuation model of that already-correct engine transition.

CONSERVATIVE REPAIR DIRECTION
-----------------------------

bObsolete mode should value the actual effect delta produced by:

    processBuilding(eBuilding, -1, /*bObsolete=*/true)

rather than treating the whole building as lost.

At minimum:
1. ObsoleteSafeCommerceChange must contribute 0 to obsolete-loss valuation.
2. GreatPeopleRateChange that survives obsolescence must contribute 0.
3. Local defense / bombard defense must contribute 0 when bObsolete=true;
   do not route retained defense through the normal add-building branch.
4. Preserve ordinary bRemove / spy-removal behavior when bObsolete=false,
   because physically removing a building really does lose those retained
   effects too.

A future-proof implementation should make the bObsolete valuation mirror
CvCity::processBuilding's obsolete-vs-remove split rather than maintain an ad-hoc
list that can drift again.

REGRESSION TESTS
----------------

A. Monument / Liberalism
- Own an active current Monument.
- Evaluate Liberalism obsolete-building penalty.
- Assert the Monument's +1 ObsoleteSafe culture contributes exactly 0 lost
  value.

B. Castle / Military Science
- Normal non-isolated city with Walls + Castle.
- Assert Castle's retained RaiseDefense and BombardDefense contribute 0 to
  Military Science obsolete penalty.
- Assert actually obsolete Castle effects (e.g. trade-route / active commerce
  modifier effects) can still contribute.

C. Obsoleting Wonder
- Great Lighthouse / Colossus / Great Wall.
- Assert retained ObsoleteSafe culture and retained GPP are not counted as lost.
- Assert effects that really deactivate on obsolescence remain valued as losses.

D. Spy/remove control
- Evaluate actual building removal with bRemove=true, bObsolete=false.
- Assert local defense, safe commerce and GPP are still valued as losses when
  the physical building itself is removed.

STATUS
------

F536 / KI#859 CONFIRMED.
F537 / provisional KI#860 is now UNUSED.

CURRENT DURABLE STONE

Queue 005 CvCityAI.cpp OPEN
32/169 unique files complete
F446-F536 confirmed
F537 / KI#860 UNUSED
WIP309 latest physical checkpoint
Repository source untouched

EXACT NEXT CURSOR

Continue under F537.

First finish the remainder of obsolete/remove-mode AI_buildingValue now that
F536 establishes the correct engine delta. Look specifically for:
- an effect that really DOES obsolete but bRemove/bObsolete causes it to be
  retained or sign-reversed in valuation;
- physical spy-removal paths accidentally using obsolete semantics;
- bRemove-dependent arithmetic where existing-city state is double-added.

Merge any "retained effect counted as obsolete loss" manifestations into F536.

Then resume the assertion/invariant sweep and remaining late mechanical patterns:
wrong object/enum domains, state lost across refactors, semantic cache keys,
multiply-before-divide overflow and early-return outputs.

Do not allocate F537 for additional ObsoleteSafe/GPP/defense manifestations of
F536.
================================================================================


================================================================================
C031-WIP310 HANDOFF -- Queue 005 CvCityAI.cpp continued
F446-F537 confirmed; F538 / KI#861 UNUSED
Repository source untouched.
================================================================================

CONTINUATION FROM WIP309

This checkpoint first closes the post-F536 bRemove/bObsolete and SAS file-local
helper scans, then promotes one independent future-happiness counting defect in
AI_yieldValue as F537.

1. F536 BOUNDARY -- REST OF bRemove/bObsolete CLOSED INTO SAME ROOT
------------------------------------------------------------------

After establishing F536's whole-building-vs-actually-lost-effects mismatch,
the remainder of the bRemove/bObsolete plumbing was checked for a second
independent sign/scope failure.

No second root survives.

- ObsoleteSafe commerce retained by the engine but counted as obsolete loss:
  F536.
- Building Great-Person points retained on obsolescence but counted as lost:
  F536.
- Local defense/bombard retained on obsolescence but the partial AdvCiv repair
  falls through add-mode valuation:
  F536.
- Physical building removal / spy destruction with bRemove=true,
  bObsolete=false is intentionally broader: destroying the actual building does
  remove these retained-on-obsolescence effects and they SHOULD be valued.

Do not spend a later F-number on additional retained-effect manifestations of
F536.

2. POST-F536 ASSERTION / DIVISOR CONTROLS
-----------------------------------------

The first release-relevant assertion guards challenged after WIP309 survive
their producer domains:

- AI_bestBuildingThreshold prerequisite/limit arithmetic has positive
  constructibility-derived denominators;
- route/build values reach multiply/divide blocks only with positive values;
- AI_updateSpecialYieldMultiplier's available-income denominator is
  `1 + AI_getAvailableIncome()`, whose producer models full-slider gross income
  and current shipped modifiers do not yield a negative value;
- AI_citizenSacrificeCost recovery-turn arithmetic has positive current
  FoodKept bounds. Current stackable iFoodKept data is Granary-class 40/50 plus
  China's separate Market-class +15, far below the 105% boundary used by the
  recovery approximation.

No F537 arose from these guards.

The stale `FAssert(MAX_INT / 1000 > iValue)` in AI_bestUnit is stronger than
the current Release multiplication actually requires (the old *1000 was
reduced to *10 by AdvCiv). It is Debug robustness debt, not a current Release
overflow proof.

3. SAS FILE-LOCAL HELPER PASS -- CLOSED / EXISTING FAMILIES ONLY
----------------------------------------------------------------

The current file-local behavior helpers at the top of CvCityAI.cpp were
rechecked because member-function coverage does not see them.

SAS_getPreferredNonCombatFoodProductionCity
- getProductionTurnsLeft(eUnit,0) is a valid hypothetical unit completion
  estimate even when the candidate city is not currently building the unit;
- outputs are initialized on entry;
- every candidate must be same land area, safe, canTrain, finite-positive and
  no slower than the current city;
- final population/stagnation/turn tie-breaking is mutation-free.
No wrong-object, stale-output or impossible-progress root was established.

SAS_isSettlerProductionCandidate
- deliberately catches either an explicit UNITAI_SETTLE request OR a concrete
  unit whose XML default UnitAI is UNITAI_SETTLE;
- this makes the final concrete pre-push site gate robust to UnitAI
  substitutions rather than bypassable.
No independent root.

SAS_countIndependentRivalTeamsInArea
- PlayerIter<MAJOR_CIV> is the cached alive, non-Barbarian, non-minor set;
- master-team dedup correctly treats teammates/master-vassal blocs as one bloc;
- rival-city count intentionally includes all cities belonging to those blocs.
No dead-player or PlayerTypes/TeamTypes mismatch.

SAS_findNearbyOverseasConquestTarget
- target eligibility uses the city's selected water area including target
  secondWaterArea;
- non-Barbarian attackability is checked from our team through AI_mayAttack /
  war-plan state;
- available units are land ATTACK/ATTACK_CITY plus half COUNTER, and target
  defenders are city units that can defend, so there is no F534-style sea-vs-
  land domain mismatch in this trigger;
- score magnitudes from AI_targetCityValue are far below 32-bit overflow in
  current use.

Remaining primary/second-water capacity omissions in this production family
are already owned by KI#193.* / KI#194 / KI#197.11 and were not renumbered.

4. ADJACENT AI_yieldValue RECHECK AFTER F530/F531/F535
======================================================

The three previous findings corrected:
- F530: wrong next-drop remainder at exact anger-cycle multiples;
- F531: wrong <= phase relation because growth occurs before that turn's timer
  decrement;
- F535: hard-coded kMaxHappyIncrease=2 despite three independent anger sources.

A fourth independent issue survives even if all three are repaired.

===============================================================================
F537 / KI#860 CONFIRMED
AI_yieldValue counts at most one future happiness recovery per anger SOURCE,
even when multiple stacked unhappy layers of that same source will expire
before the projected growth
===============================================================================

CURRENT SOURCE SHAPE
--------------------

For each temporary-anger timer, AI_yieldValue asks only when the NEXT anger
point is reduced and then increments future happiness once:

    if (iHurryAngerTimer > 0)
    {
        int iTurnsUntilAngerIsReduced =
            iHurryAngerTimer % flatHurryAngerLength();
        if (iTurnsUntilAngerIsReduced <= iApproxTurnsToGrow)
            iFutureHappy++;
    }

Conscript and Defy-resolution anger use the same one-increment shape.

No loop or quotient accounts for a second/third stacked unhappy point of the
same source disappearing before growth.

AUTHORITATIVE ENGINE STACK COUNT
--------------------------------

CvCity's actual hurry anger count uses:

    (((getHurryAngerTimer() - 1) / flatHurryAngerLength()) + 1)

for positive timers, i.e. ceil(timer / length).

Conscript and Defy use the same structure.

Thus timer=19 with length=10 is TWO current unhappy layers, not one.

NORMAL PRODUCER
---------------

Repeated whipping naturally creates exactly such a state.

CvCity::hurry does:

    changeHurryAngerTimer(iHurryAngerLength);

It ADDS one full length to the existing timer; it does not replace it.

Normal-speed zero-modifier example:
1. first whip -> timer 10;
2. city turn later -> timer 9;
3. second legal whip -> timer 19.

The city now has:
    ceil(19 / 10) = 2
hurry-anger layers.

Repeated drafting produces the same stacked-timer structure because conscript()
also adds flatConscriptAngerLength() to the existing timer. Defy-resolution
anger can stack analogously. Hurry alone is enough for the proof.

CLEAN INDEPENDENT FIXTURE
-------------------------

Use:
    anger length L = 10
    Hurry timer T = 19
    no Conscript/Defy anger
    current iHappinessLevel = -1
    projected next growth G = 20 doGrowth executions away

This avoids every adjacent finding:

F530:
    T=19 is NOT an exact multiple of 10.
    The next-drop remainder is genuinely 9.

F531:
    next drop at 9 and final drop at 19 are both STRICTLY before G=20.
    No equality / phase-boundary ambiguity exists.

F535:
    outer gate with the old constant already passes:
        -1 + 2 > 0
    so the missing recovery is not caused by the +2 prefilter.

ACTUAL TIMER / GROWTH ORDER
---------------------------

Citizen valuation occurs before the upcoming city doGrowth. Each city turn:
    doGrowth()
    ...
    anger timer -= 1

Starting at timer 19:
- after the 9th growth/decrement cycle, timer becomes 10 and the first unhappy
  layer has disappeared;
- after the 19th growth/decrement cycle, timer becomes 0 and the second layer
  disappears;
- the 20th projected growth therefore occurs with BOTH anger layers gone.

Actual future happiness recovered before G=20:
    +2

CURRENT MODEL
-------------

Current code computes only the next reduction:
    19 % 10 = 9
    9 <= 20
    => iFutureHappy += 1

There is no second increment for the same Hurry source.

So:

CURRENT:
    iHappinessLevel + iFutureHappy
    = -1 + 1
    = 0
    => iPopToGrow = 0

CORRECT:
    -1 + 2
    = +1
    => iPopToGrow = 1

This is a deterministic state difference in the exact quantity that the
future-happiness block is trying to estimate.

LIVE CITIZEN/JOB CONSEQUENCE
----------------------------

Because current happiness is -1, the bugged iPopToGrow=0 state cannot enter the
fallback bFillingBar path either; that path requires iHappinessLevel >= 0.

Therefore the bug can remove growth-food value entirely.

Using the same controlled scale as the F530/F531 regression tests:
    iGrowthValue = 10
    ordinary production value of +1 hammer = 8

At current happiness -1:
- bugged +1 food gets only its small current-food baseline (~1) and loses to
  +1 hammer (8);
- correct iPopToGrow=1 activates positive growth-food value, pushing the +1
  food job above the hammer job at this controlled scale.

The direct branch difference is deterministic even though exact live
iGrowthValue varies by city context.

ROOT / LINEAGE
--------------

The one-recovery-per-source logic is already present in the Civ4CE BtS
reconstruction. K-Mod, Base AdvCiv 1.14 and current SAS retain it.

K-Mod later refactored the recovered happiness into iFutureHappy and changed
other timing details, but never generalized one source to multiple expiring
layers.

This is therefore an inherited BtS-era growth-forecasting defect, not a SAS
regression.

DEDUP / INDEPENDENCE
--------------------

Whole-album searches found no prior root for multiple anger layers of one source
expiring before growth.

It is independent of:
- F530 next-drop modulo;
- F531 growth/decrement event order;
- F535 outer maximum-happiness prefilter.

All three can be fixed while F537 remains:
with T=19, G=20, a correct next-drop/phase calculation still adds only +1
because the source is visited once.

CONSERVATIVE REPAIR DIRECTION
-----------------------------

For each anger source, compute the number of unhappy LAYERS present now and the
number still present immediately before the projected growth.

For positive timer T, length L:

    layersNow = ceil(T / L)

Given G upcoming doGrowth executions until the growth being evaluated, timers
have received only G-1 decrements BEFORE that growth:

    timerAtGrowth = max(0, T - (G - 1))
    layersAtGrowth =
        timerAtGrowth <= 0 ? 0 : ceil(timerAtGrowth / L)

Then:

    recoveredBeforeGrowth = layersNow - layersAtGrowth

and add that full count to iFutureHappy.

This formulation naturally handles:
- F530 exact-cycle boundaries;
- F531 phase ordering;
- F537 multiple layers.

The F535 outer gate should then be removed or derived from the true maximum
recoverable count rather than remain a stale constant.

REGRESSION TESTS
----------------

A. Hurry two-stack:
    L=10, T=19, G=20
    assert recovered happiness = 2, not 1.

B. Phase control:
    L=10, T=19, G=19
    timer before the 19th growth is still 1;
    assert only 1 layer has recovered.

C. Single-stack:
    L=10, T=9, G=10
    assert one recovery.

D. F530 control:
    L=10, T=10; verify exact-cycle handling remains correct.

E. Repeat for Conscript; optionally Defy.

STATUS
------

F537 / KI#860 CONFIRMED.
F538 / provisional KI#861 is now UNUSED.

CURRENT DURABLE STONE

Queue 005 CvCityAI.cpp OPEN
32/169 unique files complete
F446-F537 confirmed
F538 / KI#861 UNUSED
WIP310 latest physical checkpoint
Repository source untouched

EXACT NEXT CURSOR

Continue under F538.

First finish the remainder of AI_yieldValue's future-growth prediction after
the four now-confirmed temporary-happiness defects. Challenge only independent
mechanical contracts:
- multiple future changes collapsed to one;
- current-vs-future population/health counts using the wrong event phase;
- arithmetic boundaries that can reverse sign or disable the branch.

Then return to remaining release-relevant assertion/invariant checks. The
file-local SAS helper tranche is considered saturated except for broadenings
already owned by KI#193/KI#194/KI#197.

Do not spend F538 on another manifestation of the same stacked-anger counting
root.
================================================================================

================================================================================
C031-WIP311 - F538 / KI#861 CONFIRMED
CvCity worker-availability cache double-counts a Worker on its completion turn
================================================================================

STATUS
------
F538 / KI#861 CONFIRMED.
F539 / provisional KI#862 is the next UNUSED root.
Queue 005 CvGameCoreDLL/CvCityAI.cpp remains OPEN.
32/169 unique files complete.
Repository source untouched.

ROOT
----
`CvCityAI::AI_updateWorkersHaveAndNeeded()` deliberately includes a Worker that is
already close to completion in the city's cached available-Worker count:

    if (getProductionUnit() != NO_UNIT)
    {
        if (getProductionUnitAI() == UNITAI_WORKER)
        {
            if (getProductionTurnsLeft() <= 3)
                iWorkersHave++;
        }
    }

That refresh runs at the beginning of `CvCity::doTurn()`, through
`CvCityAI::AI_doTurn()`, before `CvCity::doProduction()`.

When the same queued Worker finishes later in that city turn, `CvCity::popOrder()`
performs another unconditional cache increment before removing the training order:

    /* advc.113b: So that the new worker can already be taken into account
       for choosing the next order */
    if (eTrainAIUnit == UNITAI_WORKER)
        AI().AI_changeWorkersHave(1);

For an ordinary Worker that was already <=3 turns from completion at the morning
refresh, these are not two different Workers. The first +1 is the near-complete
queued Worker; the second +1 is that exact Worker transitioning from queued to
completed. The cache therefore becomes one too high until the next full
`AI_updateWorkersHaveAndNeeded()` refresh.

DETERMINISTIC TURN TIMELINE
---------------------------
Let a city begin its turn with N live/assigned Workers and one UNITAI_WORKER order
that will complete this turn.

1. `CvCity::doTurn()` calls `AI().AI_doTurn()`.
2. `AI_updateWorkersHaveAndNeeded()` sees `getProductionTurnsLeft() == 1` and stores
   `m_iWorkersHave = N + 1`.
3. Later in the same `CvCity::doTurn()`, `doProduction()` completes that same Worker.
4. `popOrder()` calls `AI_changeWorkersHave(1)`, producing `m_iWorkersHave = N + 2`.
5. Only N + 1 Workers actually exist / are represented by that cache contract.
6. If the order queue is now empty, `popOrder()` subsequently invokes
   `AI_chooseProduction()` before any full Worker-cache refresh repairs the value.

Thus the overcount is directly observable by the exact next-production decision
that the completion increment was intended to inform.

LIVE PRODUCTION CONSEQUENCE
---------------------------
Normal `AI_chooseProduction()` reads the local cache in its high-priority Worker
branch:

    bValid = (3 * (AI_getWorkersHave() + 1) / 2 <
              AI_getWorkersNeeded() + 1 && ...);

A clean boundary example with local Worker need 3 is:

    correct completed-worker cache: have = 1
        3 * (1 + 1) / 2 = 3;  3 < 4 -> true

    bugged completion-turn cache: have = 2
        3 * (2 + 1) / 2 = 4;  4 < 4 -> false

So, when the area still has unmet Worker demand, the double count can demote what
would have been a deterministic local Worker-production qualification into the
fallback probabilistic path, changing the city's immediate next build choice.
Other same-turn `AI_getWorkersHave()` consumers can likewise observe the inflated
cache before the next city turn repairs it.

ANCESTRY
--------
The root is AdvCiv-specific, introduced by practical commit 1583
`715fa87579778a04e8da1adb5a6f1170f141aed5` ("Several improvements to AI worker
production and assignment"). That commit added the completion-time
`AI_changeWorkersHave(1)` hook as part of advc.113b while retaining the existing
near-complete production Worker in `AI_updateWorkersHaveAndNeeded()`.

Controls:
- Civ4CE BtS reconstruction retains the near-complete Worker count (<=2) but has no
  completion-time `AI_changeWorkersHave(1)` hook.
- K-Mod 1.46 likewise retains the near-complete count (<=2) without the AdvCiv
  completion increment.
- Base AdvCiv 1.14 has both sides of the contradiction (with <=3 after advc.113),
  as does current AdvCiv-SAS.

This therefore appears to be an AdvCiv practical-1583 regression inherited by SAS,
not a BtS/K-Mod defect.

DEDUP
-----
Whole-album and current Known-Issues / archaeology searches found no existing root
for the same near-complete Worker being counted once by the morning cache refresh
and again by the completion hook.

Do not merge with KI#198 or KI#213:
- KI#198 concerns Worker demand floors / worked featureless land and the later
  area-vs-empire cap interaction.
- KI#213 concerns the split city-side vs Worker-side best-build policy hiding valid
  custom land jobs.
- F538 is a lifecycle/cache transition error even when Worker demand and best-build
  policy are both otherwise perfect.

REPAIR DIRECTION
----------------
Do not blindly remove the completion hook: a Worker can in principle complete after
an event not represented by the morning <=3 prediction (most notably same-turn
hurry/production-state changes), and advc.113b intentionally wants completion to be
visible before the next order is chosen.

The safest repair is to re-establish the authoritative Worker-availability cache
when a Worker completes before `AI_chooseProduction()` consumes it, or otherwise
track whether the current production Worker was already included in the most recent
refresh and increment only when it was not. A localized full recomputation on Worker
completion is simpler and harder to desynchronize than trying to infer the old
morning prediction from post-production state.

REGRESSION TESTS
----------------
A. Ordinary one-turn completion
- City starts with N live/assigned Workers and a UNITAI_WORKER at 1 turn left.
- Morning refresh must report N+1.
- After completion and before choosing the next order, the cache must still report
  N+1, not N+2.

B. Three-turn prediction control
- A Worker at 2-3 turns left should remain represented exactly once while queued.

C. Unexpected/same-turn accelerated completion
- If a Worker was not included by the prior refresh but completes before the next
  order choice (e.g. through a valid hurry/production change), the completed Worker
  must nevertheless be counted once.

D. Production-choice boundary
- With local Worker need 3 and one newly completed Worker, verify the local
  `3 * (have + 1) / 2 < needed + 1` test receives have=1 rather than the phantom 2.

POST-F538 OPEN WORKER-ACCOUNTING LEADS
--------------------------------------
Two additional consistency leads were identified but are NOT numbered yet:

1. Old assignment vs physical-city mismatch in `AI_workerMove(bool)`:
   the full cache attributes a non-cargo Worker with a MissionAI target to the
   mission target's working city, but the wrapper snapshots `pOldCity` only through
   `AI_getCityToImprove()` (physical plot city/working city). K-Mod turn-start danger
   handling can clear the queued mission and force reassessment while retaining the
   old MissionAI target, so reassignment may decrement the wrong city / fail to
   decrement the formerly targeted city until the next full refresh.

2. Range mismatch:
   the full group refresh counts an off-city MISSIONAI_BUILD Worker only when its
   mission target is within the era-scaled `iRange = 9 * (1 + eraFactor / 2)`, while
   the incremental wrapper immediately adds the newly targeted city's cache with no
   equivalent range test. Base AdvCiv's `AI_nextCityToImprove` and current SAS can
   select same-area targets without that maximum-distance bound.

Both are plausible transient same-turn cache inconsistencies. Continue under F539
unused and promote only if an independent gameplay consequence survives the full
contract/dedup proof; otherwise retain as robustness/control notes.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F538 CONFIRMED.
F539 / provisional KI#862 UNUSED.
Latest physical checkpoint: C031-WIP311.
Repository source untouched.

NEXT
----
Continue F539 unused by finishing the worker-have count-domain proof:
- old MissionAI target vs physical-city transition;
- full-refresh era-distance filter vs incremental assignment;
- grouped/cargo Worker count-domain parity;
- dedup against KI#198/KI#213 and the earlier ferry-count notes.
Then resume the remaining release-invariant / arithmetic sweep in CvCityAI.cpp.

END C031-WIP311

================================================================================
C031-WIP312 - F539 / KI#862 CONFIRMED
AI_workerMove incremental Worker cache forgets the old MissionAI target city
================================================================================

STATUS
------
F539 / KI#862 CONFIRMED.
F540 / provisional KI#863 is the next UNUSED root.
Queue 005 CvGameCoreDLL/CvCityAI.cpp remains OPEN.
32/169 unique files complete.
Repository source untouched.

ROOT
----
AdvCiv 113b updates `m_iWorkersHave` incrementally around every land-Worker AI
reassessment:

    if (bUpdateWorkersHave)
    {
        CvCityAI* pOldCity = AI_getCityToImprove();
        AI_workerMove(false);
        CvCityAI* pNewCity = NULL;
        if (plot() != NULL)
        {
            CvPlot* pNewMissionPlot = AI_getGroup()->AI_getMissionAIPlot();
            if (pNewMissionPlot != NULL)
                pNewCity = pNewMissionPlot->AI_getWorkingCity();
            if (pNewCity == NULL)
                pNewCity = AI_getCityToImprove();
        }
        if (pOldCity != pNewCity)
        {
            if (pOldCity != NULL)
                pOldCity->AI_changeWorkersHave(-1);
            if (pNewCity != NULL)
                pNewCity->AI_changeWorkersHave(1);
        }
        return;
    }

The OLD side and the authoritative full refresh do not use the same ownership
contract.

`AI_getCityToImprove()` looks only at the Worker's current physical plot:

    if (getPlot().getOwner() != getOwner() || isCargo())
        return NULL;
    CvCityAI* pCity = getPlot().AI_getPlotCity();
    if (pCity == NULL)
        pCity = getPlot().AI_getWorkingCity();
    return pCity;

But `AI_updateWorkersHaveAndNeeded()` deliberately attributes a non-cargo Worker
with a build MissionAI target to the TARGET city's Worker cache. Its city-plot
predicate `PUF_isMissionPlotWorkingCity` substitutes the group's MissionAI plot for
the physical plot when one exists, and the later group scan counts an off-city
`MISSIONAI_BUILD` group for the target city when it is inside the refresh range.

Therefore an en-route Worker can be represented in city B's `m_iWorkersHave` while
`AI_workerMove(true)` records its `pOldCity` as city A or NULL. If the mission is
then cancelled/replaced during that reassessment, B is never decremented.

DETERMINISTIC INTERRUPTION PATH
-------------------------------
No hypothetical external mission mutation is needed. The normal AI group turn has
an explicit interruption path:

1. At the start of the player turn, city B's
   `AI_updateWorkersHaveAndNeeded()` sees Worker W en route to a BFC build target
   with `MISSIONAI_BUILD` and counts W for B.
2. Later, land groups receive `CvSelectionGroup::doTurn()`.
3. For an AI-controlled noncombat Worker group, danger can enter:

       setForceUpdate(true);
       clearMissionQueue();
       AI().AI_cancelGroupAttack();

4. `clearMissionQueue()` removes queued missions but does NOT call
   `AI_setMissionAI(NO_MISSIONAI, ...)`; the old B target remains stored.
5. `readyToMove()` explicitly treats `isForceUpdate()` as sufficient to re-enter
   AI evaluation, and `CvSelectionGroupAI::AI_update()` clears the force marker
   only after entering its update loop.
6. The Worker therefore reaches `AI_workerMove(true)` while its old MissionAI
   target is still B.
7. The wrapper snapshots `pOldCity` from the physical plot instead of that old B
   assignment.
8. `AI_workerMove(false)` can retreat, wait, or choose a different build target,
   replacing/clearing the old MissionAI target.
9. The wrapper adjusts the physical city (if any) and the new city, but never B.
   B retains a phantom Worker until its next full city-turn refresh.

This is especially clear for W traveling between city radii: `pOldCity == NULL`
although B's cache contains W. After the interrupted mission is replaced, there is
literally no decrement operation capable of removing W from B.

LIVE SAME-TURN CONSEQUENCE
--------------------------
The stale value is transient across turn boundaries because the next full city
refresh repairs it, but advc.113b exists specifically to make Worker availability
meaningful WITHIN the turn. Later Worker groups in the same AI unit-update pass read
`AI_getWorkersHave()` while choosing destinations and redistribution actions.

Base AdvCiv's `AI_nextCityToImprove()` gives a direct numerical witness. For a city
whose authoritative `AI_getWorkersNeeded()` is 1, the function first relaxes the
movement target to `iWorkersNeeded = 2` and scores Worker shortage roughly as:

    iValue = max(0, iWorkersNeeded - iWorkersHave) * 100;
    iValue += iWorkersNeeded * 10;
    iValue *= iWorkersNeeded + 1;
    iValue /= iWorkersHave + 1;

If interrupted W was B's only assigned Worker:

    correct after cancellation: have = 0
        (200 + 20) * 3 / 1 = 660

    stale cache: have = 1
        (100 + 20) * 3 / 2 = 180

before the common path-distance scaling. Thus a stale +1 can reduce B's Worker
redistribution score from 660 to 180 and readily flip the next Worker's target to a
competing city. Current SAS also consumes the cache in its cross-city readiness
scoring, so the root remains live after the custom Worker rewrite.

ANCESTRY
--------
The mismatch was introduced by AdvCiv practical 1583
`715fa87579778a04e8da1adb5a6f1170f141aed5` together with the advc.113b incremental
Worker-availability wrapper.

- Civ4CE BtS and K-Mod do not contain this wrapper and therefore do not contain
  this specific old-snapshot regression.
- Base AdvCiv 1.14 retains the physical `pOldCity = AI_getCityToImprove()` snapshot
  while its full refresh attributes incoming build Workers by MissionAI target.
- Current SAS retains the same old-side contract and the same full-refresh target
  attribution.

So F539 is an AdvCiv regression inherited by SAS.

DEDUP
-----
No matching root was found in the album, current Known Issues, or historical
archaeology.

Do not merge with:
- F538/KI#861: same cache family, but F538 double-counts one Worker during the
  production-order -> completed-unit transition; F539 loses the OLD target during
  an existing Worker's mission reassignment.
- KI#198: Worker-demand/floor calculation.
- KI#213: split best-build policy.
- the older ferry note around F383: cargo delivery/cache-range handling, not a land
  Worker's old target transition.

REPAIR DIRECTION
----------------
The robust repair is to centralize the definition of "which city currently counts
this Worker" and use that same helper both for full refresh and for the before/after
incremental transition.

At minimum, the OLD snapshot must inspect the pre-recursion MissionAI assignment
before falling back to the physical city; simply calling `AI_getCityToImprove()` is
not sufficient. The helper should also preserve the full-refresh cargo, mission-type
and distance rules rather than treating every MissionAI plot as equivalent (the
remaining range/new-side mismatch is still under F540 review).

REGRESSION TESTS
----------------
A. Interrupted incoming build Worker
- W is off-city, within the authoritative incoming range, and has MISSIONAI_BUILD to
  city B.
- Morning city refresh reports B WorkersHave += 1.
- Cancel/replace W's mission through the normal AI danger force-update path.
- Before any next-turn city refresh, B must lose exactly that +1.

B. Physical-city mismatch
- Place W physically in city A's BFC while its build mission targets city B.
- Full refresh must attribute W according to the same rule that the incremental OLD
  snapshot later uses; reassignment must not decrement A if A never counted W.

C. Later Worker decision
- A second Worker processed later in the same AI unit-update pass must see B's
  post-cancellation Worker count, not the stale pre-cancellation value.

D. Uninterrupted mission control
- If W remains assigned to the same B target, the before/after cache delta is zero.

POST-F539 CURSOR
----------------
The NEW side of the same wrapper still has a separate unresolved contract mismatch:
full refresh counts a remote MISSIONAI_BUILD Worker only inside an era-scaled target
range, whereas the wrapper immediately increments any target working city without
that range (and without matching all full-refresh mission-type rules). Keep that
under F540 unused until its independent consequence / intended-range semantics are
settled.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F539 CONFIRMED.
F540 / provisional KI#863 UNUSED.
Latest physical checkpoint: C031-WIP312.
Repository source untouched.

NEXT
----
Continue F540 unused through the remaining Worker-have count-domain parity:
- full-refresh era-distance filter vs immediate new-target increment;
- MISSIONAI_BUILD vs other MissionAI types;
- grouped Worker `iSize` vs one-head incremental delta;
- cargo/ferry boundaries.
Then return to the remaining release-invariant / arithmetic sweep.

END C031-WIP312

===============================================================================
C031-WIP313 — QUEUE 005 CONTINUATION — F540 / KI#863 CONFIRMED
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F540 are CONFIRMED.
F541 / provisional KI#864 is UNUSED.
Repository source remains untouched.

POST-WIP312 WORKER-PARITY CONTROLS
----------------------------------
The remaining NEW-side Worker-cache mismatches did not justify another number.

- The full refresh counts a remote `MISSIONAI_BUILD` Worker only inside an
  era-scaled distance, whereas `AI_workerMove(true)` immediately increments any
  new target working city. This is inconsistent across refresh boundaries, but an
  immediate far-target increment can plausibly be intentional within-turn collision
  avoidance: it stops multiple Workers from selecting the same distant city before
  the next authoritative refresh. No hard current correctness contract was proved.
- The wrapper's broader MissionAI handling, grouped-Worker `iSize` versus one-head
  delta, and cargo/ferry edges likewise lacked a normal current AI producer that
  establishes an independent failure. Keep them as robustness/parity notes unless a
  concrete live path emerges.

The audit therefore returned to untouched release-relevant `CvCityAI` helpers rather
than forcing F540 from the Worker family.

-------------------------------------------------------------------------------
F540 / KI#863 — CONFIRMED
`AI_countGoodTiles` treats `getCultureTurnsLeft()==-1` (NO CULTURE / NO ETA) as
"border expansion within 5 turns", which can make the human production governor
suppress Build Culture indefinitely
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current gameplay / production-governor defect.
Inherited from Base AdvCiv; absent from K-Mod before AdvCiv's `advc.113` border-
anticipation addition.

ROOT
----
`CvCity::getCultureTurnsLeft()` has an explicit negative sentinel contract:

    int iCultureRateTimes100 = getCommerceRateTimes100(COMMERCE_CULTURE);
    if (iCultureRateTimes100 <= 0)
        return -1;
    ...
    if (iCultureLeftTimes100 <= 0)
        return -1;

So `-1` means there is no positive current ETA to the next culture threshold.

Current `CvCityAI::AI_countGoodTiles()` nevertheless tests:

    bool bValid = (kPlot.getWorkingCity() == this);
    if (!bValid && kPlot.getOwner() == NO_PLAYER &&
        getCultureLevel() == 1 && getCultureTurnsLeft() <= 5)
    {
        bValid = true;
    }

There is no `getCultureTurnsLeft() > 0` guard.

Therefore:

    culture rate = 0
    getCultureTurnsLeft() = -1
    -1 <= 5              = true

and every otherwise-good unowned city-radius plot is treated as though the border
will reach it within five turns, even though current culture output will NEVER reach
the next threshold.

This is not merely a misleading helper name. The exact positive-look-ahead branch
was added by AdvCiv to "Anticipate border expansion", and its intended finite horizon
is encoded by the literal 5. The negative no-ETA sentinel violates that contract.

CLEAN LIVE GOVERNOR CONSEQUENCE
-------------------------------
The strongest deterministic consumer is `AI_buildGovernorChooseProduction()`, used
for human cities whose production is automated.

Its border-pop block first establishes that the city still needs culture to work the
full radius:

    if (AI_needsCultureToWorkFullRadius())
    {
        ...
        if (AI_countGoodTiles(true, true, 60) == 0 &&
            AI_chooseProcess(COMMERCE_CULTURE))
        {
            return;
        }
    }

`AI_needsCultureToWorkFullRadius()` is true for a non-disordered culture-level-1 city
with zero Culture output:

    !isDisorder()
    getCommerceRate(COMMERCE_CULTURE) <= 0
    getCultureLevel() + 1 <= CITY_PLOTS_RADIUS

Construct a production-automated human city with:

- culture level = 1;
- current Culture commerce rate = 0, hence `getCultureTurnsLeft() == -1`;
- no currently workable/unworked plot above the magic-value threshold 60;
- one good UNOWNED outer-radius plot, e.g. a healthy 3-food plot (magic value about
  85), which is not currently workable;
- Culture process available;
- no qualifying best Culture building taking the earlier building branch.

Correct finite-look-ahead behavior:

    currently usable good unworked tiles = 0
    => AI_countGoodTiles(..., true, 60) == 0
    => governor chooses the Culture process
    => culture output becomes positive and the city works toward the needed border pop.

Current behavior:

    getCultureTurnsLeft() == -1
    -1 <= 5
    => the unowned 3-food plot is counted early
    => AI_countGoodTiles(..., true, 60) == 1
    => the `== 0` Culture-process gate fails
    => governor falls through to unrelated production.

Nothing about that choice changes the zero culture rate. On the next governor
production decision the same `-1 <= 5` test can repeat, so the supposedly imminent
plot can suppress the very Culture process needed to make it reachable. This is a
self-sustaining wrong-state loop, not just a small valuation bias.

The state is ordinary and reachable. A human-controlled city can be at culture level
1 while currently producing zero Culture (for example after prior/manual culture,
recapture with retained owner culture, or loss/change of the source that produced the
existing culture), and production automation then uses this governor path.

SECOND LIVE EFFECT: GROWTH / CITIZEN VALUATION
----------------------------------------------
The same helper is called from `AI_yieldValue()` with `bUnworkedOnly=true` to cap
`iPopToGrow` by the number of future useful jobs. A level-1 zero-culture city can
therefore count inaccessible outer plots as imminent jobs indefinitely.

At population >=3, with positive happiness and the food bar already above the
`bFillingBar` threshold, one phantom good tile can change:

    correct iGoodTiles = 0 -> iPopToGrow = 0 -> no growth-food branch
    bugged  iGoodTiles = 1 -> iPopToGrow = 1 -> growth-food value enabled

so the sentinel misuse can also alter citizen/job selection. The governor deadlock is
the cleaner primary witness because it directly suppresses the action that would
repair the missing culture.

ANCESTRY
--------
K-Mod's `AI_countGoodTiles()` only accepts plots whose `getWorkingCity() == this`; it
does not contain this future-border branch.

AdvCiv practical 1568, commit
`b96f30c2d50017ebf596b496c49a6e41b1b1cf7a`
("Several AI changes concerning workers") added the `advc.113` block:

    if(!bValid && pLoopPlot->getOwner() == NO_PLAYER &&
        getCultureLevel() == 1 && getCultureTurnsLeft() <= 5)
        bValid = true;

Base AdvCiv 1.14 retains it, and current SAS is semantically identical here.
Therefore F540 is an AdvCiv regression inherited by SAS.

DEDUP / RELATED SENTINEL CHECK
------------------------------
Whole-album and Known-Issues searches found no existing root for
`getCultureTurnsLeft() == -1` being interpreted as an imminent border expansion in
`AI_countGoodTiles`.

Do not merge with:
- F494/KI#817, which concerns Culture-process order metadata across AI Auto Play ->
  human hand-back;
- F342/KI#665, which concerned positive sea-resource look-ahead switching `canBuild`
  into visible/future-tech mode before an immediate Work Boat role conversion;
- the future-happiness F530/F531/F535/F537 family, which is unrelated timer arithmetic.

A cross-file sibling exists in `CvPlayerAI::AI_countUnimprovedBonuses`: its positive
`iLookAhead` branch similarly fails to reject negative `getCultureTurnsLeft()`. That
is a genuine API-contract smell, but current SAS's authoritative internal Work Boat
callers have already been repaired to pass look-ahead 0 (including the F342/KI#665
role-transition fix), so no separate current F541 is promoted from that sibling at
this checkpoint.

CONSERVATIVE REPAIR
-------------------
Require a real positive finite ETA before treating an unowned plot as imminent:

    int const iCultureTurnsLeft = getCultureTurnsLeft();
    if (!bValid && kPlot.getOwner() == NO_PLAYER &&
        getCultureLevel() == 1 &&
        iCultureTurnsLeft > 0 && iCultureTurnsLeft <= 5)
    {
        bValid = true;
    }

This preserves the authored five-turn anticipation exactly while making the API's
negative "no ETA" sentinel mean what it says.

For consistency, any future positive-look-ahead use of
`CvPlayerAI::AI_countUnimprovedBonuses` should apply the same `turns > 0 && turns <=
horizon` rule rather than relying on a one-sided upper-bound comparison.

REGRESSION TESTS
----------------
A. No-culture governor fixture
- Human production-automated city, culture level 1, Culture/turn = 0.
- No currently usable good unworked plot; one unowned outer 3-food plot.
- Culture process available; no earlier qualifying Culture building.
- Assert `AI_countGoodTiles(true, true, 60)` does NOT count the outer plot and the
  governor chooses the Culture process.

B. Genuine imminent expansion
- Same geometry, but positive Culture/turn and `getCultureTurnsLeft() == 5`.
- Preserve current anticipation: the outer plot may count as good.

C. Six-turn boundary
- `getCultureTurnsLeft() == 6` must not count the unowned plot.

D. Growth valuation
- Population >=3, positive happiness, food bar >80%, no real good unworked jobs.
- With Culture/turn = 0, assert an unowned outer good tile cannot by itself raise
  `iPopToGrow` from 0 to 1.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F540 CONFIRMED.
F541 / provisional KI#864 UNUSED.
Latest physical checkpoint: C031-WIP313.
Repository source untouched.

NEXT
----
Continue under F541 unused. The `AI_isGoodPlot` / magic-value helpers themselves are
now bounded controls; the next high-yield lens is the remaining low-covered
culture/closeness/plot arithmetic plus release assertions whose guarded condition can
differ in release builds. Do not spend F541 on the dormant positive-look-ahead
`AI_countUnimprovedBonuses` sibling unless a current authoritative caller with
`iLookAhead > 0` is reintroduced or found.

END C031-WIP313


================================================================================
C031-WIP314 — QUEUE 005 CVcityAI.cpp — POST-F540 LOW-COVERAGE / RELEASE-INVARIANT SATURATION CONTROL
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F540 CONFIRMED.
F541 / provisional KI#864 remains UNUSED.
Authoritative current source: user-supplied
`AdvCiv-SAS_light_source_20260904T131734.zip`.
Repository source remains completely untouched.

PURPOSE OF THIS CHECKPOINT
--------------------------
Resume exactly from C031-WIP313's F541-unused cursor and finish the promised
low-covered helper / release-arithmetic tranche without manufacturing a finding from
heuristic policy or rediscovering an older album control.

This tranche deliberately challenged:
- zero-/near-zero-literal-mention helper contracts;
- release arithmetic protected mainly by FAssert;
- variable divisors / sentinel returns;
- the under-strength / high-power unit-production throttle;
- current file-local Barbarian Work Boat accounting comments;
- the legacy best-build / chop path;
- Worker / target-population / defender siblings after F538-F540.

No independent F541 survived this tranche.

1. `AI_defianceAngerCost` — COARSE BUT INTERNALLY INTENTIONAL
------------------------------------------------------------
The helper was reopened because it translates the global Defy Resolution anger rule
into a city-level cost and therefore looked like a plausible same-concept/different-unit
seam.

Current shape:
- a religious resolution contributes no city cost when the city lacks the resolution
  religion;
- cities that cannot become unhappy return 0;
- `iUnhappyIncr` measures the incremental angry population caused by one new defiance;
- an already-active Defy timer doubles the resulting cost.

The final `*2` does not model the exact number/timing of stacked anger layers. However,
the surrounding source explicitly uses it as extra discouragement of repeated denial;
it is not presented as an exact forward simulation like the F530/F531/F535/F537
`AI_yieldValue` block. No wrong-object, divide-by-zero or sign inversion survived.
Preserve as heuristic policy, not F541.

2. `AI_updateSpecialYieldMultiplier` INCOME ASSERTION — CLOSED
--------------------------------------------------------------
The Release-relevant-looking sequence is:

    int iIncome = 1 + kOwner.AI_getAvailableIncome();
    ...
    FAssert(iIncome > 0);
    int iRatio = (100 * iExpenses) / iIncome;

`AI_getAvailableIncome()` was traced back to the cached player calculation: raw
commerce, the average Gold multiplier, nonnegative incoming GPT and the actual Gold
commerce correction. In ordinary current data/state this does not provide a live
`<= -1` producer, and the local `+1` covers zero income.

No current divide-by-zero / negative-divisor witness was established.

3. TARGET POPULATION / GROUP-HEAD DEFENDERS — DEDUP CONTROLS
------------------------------------------------------------
Two initially tempting small helpers were explicitly cross-checked against prior
stones before any numbering:

- `AI_getTargetPopulation()` can produce a surprisingly low target under severe
  unhappiness/ill-health, but WIP297 already followed all live consumers and bounded
  that behavior as an intentional don't-grow signal or caller-clamped value.

- `AI_countExcessDefenders()` counts `PUF_canDefendGroupHead`. The broader group-head
  defender model was already deeply challenged in WIP73; the local count difference
  can be neutralized by downstream city-guard/group semantics and no independent
  current hard consequence was proved.

Neither is reopened as F541.

4. `AI_updateBestBuild` / LEGACY CHOP VALUE — OLD NOTE, NOT A NEW ROOT
--------------------------------------------------------------------
The current `AI_updateBestBuild()` body is semantically identical to Base AdvCiv.
Following it into the legacy `AI_bestPlotBuild()` evaluator again exposes K-Mod's
commented intent to increase chop value when a chop is large compared with natural
production, while the expression is:

    iValue += iValue / (getBaseYieldRate(YIELD_PRODUCTION) * 20 + iValue);

For positive city production the integer quotient is normally 0. This is a real
near-no-op arithmetic oddity, but WIP291 already preserved it as upstream robustness /
tuning debt. Current SAS's ordinary land-Worker planner bypasses this legacy evaluator,
so no new authoritative current gameplay consequence exists to justify F541.

5. BARBARIAN WORK-BOAT COMMENT / DOUBLE COUNT — BEHAVIORALLY HARMLESS CURRENTLY
-------------------------------------------------------------------------------
The recent `AI_barbChooseProduction()` comment says Base AdvCiv's
`AI_totalWaterAreaUnitAIs` iterates major-civilization cities and therefore misses
Barbarian Work Boats docked or being trained. The actual iterator contract was checked.

`CvPlayerAI::AI_totalWaterAreaUnitAIs` uses `PlayerIter<ALIVE>`. The AgentIterator
status machinery adds the Barbarian player for this status, so a Barbarian player's
own docked and queued Work Boats are already included by the inherited helper.

Current SAS then adds:
- local Barbarian Worker-Sea units on the city plot; and
- the city's Worker-Sea training count
again.

So positive existing counts can be numerically double-counted and the comment's stated
reason is inaccurate.

However the value is consumed only through:

    if (iNeededSeaWorkers > 0 && iExistingSeaWorkers <= 0)

For the current branch, double-counting a positive value stays positive and zero stays
zero. Therefore the production decision is unchanged. The discrepancy affects the
reported magnitude/comment, not current gameplay. Preserve as diagnostic/comment debt;
do not spend F541.

6. `AI_buildUnitProb` ZERO-RELEVANT-RIVAL THROTTLE — INTENTIONAL ANCESTRY
------------------------------------------------------------------------
A stronger-looking candidate arose in the inherited high-power military throttle.
The relevant-rival loop initializes:

    int iHighestRivalPow = 0;

and, if no known rival satisfies the war/relevance predicates, later computes:

    rTargetPow = (1 + rTargetAdvantage) * 0;
    rTargetPow.increaseTo(scaled::epsilon());
    rPowRatio = ourPower / epsilon;

which normally reaches the strongest `r /= 4` throttle.

At first glance this seems to contradict the nearby comment:

    "Don't throttle at all until we're clearly ahead of the best rival"

Historical reconstruction closes it as deliberate policy.

AdvCiv practical 3439 / commit
`8ac818c755148af30ba928d1d26f3140e2d3fc45`, titled:

    "AI more reluctant to throttle military production"

changed the old floor:

    int iHighestRivalPow = 1;
    rPowRatio = ourPower / iHighestRivalPow;

to:

    int iHighestRivalPow = 0;
    ...
    rTargetPow.increaseTo(scaled::epsilon());

while adding the target-advantage buffer. Thus the predecessor ALREADY treated an empty
relevant-rival set as overwhelming relative military superiority; the commit deliberately
preserved that policy while making a real rival require roughly +25% advantage before
throttling.

The semantic reading is therefore "ahead of the best RELEVANT rival"; with no relevant
military rival, the policy intentionally de-emphasizes military production. No F541.

7. `AI_getEmphasizeCommerceCount` — REDISCOVERY CORRECTED TO WIP290
-------------------------------------------------------------------
A fresh mechanical getter scan noticed:

    int CvCityAI::AI_getEmphasizeCommerceCount(...)
    {
        return (m_aiEmphasizeCommerceCount[eIndex] > 0);
    }

whereas the Yield sibling returns the raw count. This is indeed a poor integer-count
contract and makes the local nonnegative assertion toothless for a negative underlying
counter.

Before treating it as new, the album was searched more deeply. WIP290 already records
this exact discrepancy and already proves that every current whole-DLL gameplay consumer
uses it only as Boolean `>0`; ordinary emphasis toggling does not need the magnitude.
It remains inherited BtS API/clarity debt, not F541.

8. OTHER LOW-COVERAGE HELPER DEDUP / CONTROLS
---------------------------------------------
The following tempting paths were similarly closed against earlier stones:

- `AI_updateRouteToCity`: WIP290 already proved that the apparently inverted
  `!GeneratePath` branch intentionally selects a same-area team city NOT yet connected
  by the best available route.
- `AI_calculatePlayerCloseness`: WIP290 already records the same-turn cache granularity;
  consumers remain heuristic and no hard state/accounting contract was added.
- `AI_estimateReligionBuildings`: WIP290 already bounded owner/team orientation.
- `AI_growthValuePerFood`'s `std::less_equal<int>` partial-sort comparator: WIP290 already
  preserves the formal strict-weak-ordering debt without a deterministic current value
  error.
- `AI_getYieldMultipliers`: WIP288 and later assertion passes already closed current
  shipped-data denominator chains.
- `AI_isGoodPlot`, `AI_getPlotMagicValue`, `AI_getYieldMagicValue` and the surrounding
  small magic-value wrappers do not add a separate root beyond F532/F540 and existing
  final-improvement controls.

9. VARIABLE-DIVISOR / RELEASE-INVARIANT SWEEP — NO NEW LIVE FAILURE
-------------------------------------------------------------------
A mechanical scan of variable `/` and `%` operations in the live file was filtered
against:
- explicit `std::max(1, ...)` guards;
- city-existence invariants (`owner.getNumCities() > 0` for a live city);
- positive XML production / route-time contracts;
- already-proved MAX_INT handling in hypothetical production-turn estimates;
- F530/F531/F535/F537 anger-time arithmetic;
- F527 multiply-before-divide culture-pressure overflow;
- WIP307/WIP308 assertion controls.

No additional unguarded current divisor or release-only wrong-sign witness survived.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F540 CONFIRMED.
F541 / provisional KI#864 UNUSED.
Latest physical checkpoint: C031-WIP314.
Repository source untouched.

EXACT NEXT CURSOR
-----------------
Resume from WIP314 / F541 unused with a DIFFERENT late-stage lens rather than repeating
small arithmetic:

1. historical semantic-refactor seams not already covered by WIP306-WIP308, prioritizing
   commits whose titles imply mechanical cleanup but whose CvCityAI hunks move or remove
   state;
2. producer/consumer symmetry where one helper's cached/aggregate unit differs from the
   quantity a caller assumes (especially production / culture / city-radius state);
3. stale per-turn/member state that can survive ownership, capture, production-head or
   mission-target transitions;
4. only after those, decide whether Queue 005 is saturated enough to close without F541
   and move to the next unique source file.

Do not promote the Barbarian Work Boat magnitude, commerce-count getter, zero-rival
throttle, legacy chop quotient or closeness cache unless a new state-changing consumer is
proved.

END C031-WIP314

===============================================================================
C031-WIP315 APPEND -- 2026-09-04
Queue 005 CvGameCoreDLL/CvCityAI.cpp deep-method re-audit continuation
===============================================================================

STARTING STONE
--------------
C031-WIP314 was authoritative on entry:
- Queue 005 CvCityAI.cpp OPEN.
- 32/169 unique files complete.
- F446-F540 confirmed.
- F541 / provisional KI#864 unused.
- Repository source untouched.

This tranche changed only the audit album/checkpoint. Repository source remains untouched.

POST-WIP314 HISTORICAL SEMANTIC-REFACTOR SWEEP
==============================================

1. PRACTICAL 4214 / d5e9dbfea7 -- CLEAN REFACTOR CONTROL
---------------------------------------------------------
The large-looking CvCityAI refactor was semantically compressed rather than judged by
its diff size.

Checked true semantic surfaces include:
- irrigation helper extraction: old compound gate -> helper/De-Morgan equivalent;
- chop candidate loop: `if (valid) { body }` -> `if (!valid) continue; body`;
- shared commerce-to-production shift helper and its city-rank boundary.

The apparent old `rank <= cities / 3` versus new `3 * rank <= cities` issue is the same
integer boundary for nonnegative integer ranks under the actual surrounding conditions.
No state was lost and no current behavior difference survived.

2. PRACTICAL 4013 / f366263685 -- vector<pair> -> ListEnumMap ZERO-ENTRY CONTROL
-------------------------------------------------------------------------------
This conversion has a REAL generic semantic distinction:
- the old vector can retain an explicit `(UnitAI, 0)` entry;
- `FOR_EACH_NON_DEFAULT_PAIR` over ListEnumMap omits an entry whose stored value is the
  map default 0.

Therefore a zero-weight role could formerly survive as a last-place candidate and then
vanish entirely after the refactor.

All converted CvCityAI producers were checked rather than assuming the distinction was
harmless:
- assault/naval/paradrop/barbarian weights have positive lower bounds in their live
  insertion states;
- the only mathematically zero-capable converted expression is the ICBM weight
  `20 * AI_nukeWeight() / 100`;
- `AI_nukeWeight()` returns zero only in conditions where the nuke role is unavailable
  or strategically inapplicable; when a meaningful nuke choice exists its calculation
  has a positive floor large enough to keep the converted integer weight positive;
- current SAS custom naval ranking inserts only weights `> 0`.

So the container-semantic difference is real API/refactor debt but there is no live
CvCityAI old-vs-new candidate loss. No finding.

3. SMALLER REFACTOR CONTROLS
----------------------------
- practical 4213 / 68a882c3a1: removal of AI_clearFeatureValue's local feature assertion
  is safe because its sole live caller is already inside `if (kPlot.isFeature())`.
- practical 3996 / f2ad28b491: AI_setEmphasize flatten/early-return preserves old state
  updates and updateAssignments timing.
- practical 3906 / 5195a83889: canDiscoverBonus helper preserves the old reveal-tech
  ownership predicate.
- practical 3613: constructor best-build initialization and related cleanup are repairs,
  not regressions.
- practical 3569/3504: no state-changing CvCityAI semantic drift survived.

4. PRACTICAL 3561 / ee7bb6c81d -- HISTORICAL CORRECTNESS REPAIR
---------------------------------------------------------------
A supposedly small comment/refactoring commit added the important `iValue > 0` guard
before shrinking a National Wonder's value because few National-Wonder slots remain.
Without the guard, multiplying a negative value by a fraction makes it less negative and
therefore perversely improves a bad building. Current code already has the guard.
Useful evidence that "minor refactoring" commits deserve semantic auditing, but not a
current F541.

5. PRACTICAL 2842 / 7257590b64 -- TEMPORARY FACTOR-100 REGRESSION, LATER REPAIRED
---------------------------------------------------------------------------------
`AI_specialPlotImprovementValue()` changed the improvement-upgrade progress bonus from:

    100 * getUpgradeProgress() / getImprovementUpgradeTime(...)

to:

    getUpgradeProgress() / getImprovementUpgradeTime(...)

At practical 2842's then-current progress units, this temporarily collapsed ordinary
pre-upgrade progress to integer 0. However practical 2850 / ccefc3a049 subsequently
changed improvement progress itself to times-100 precision (baseline upgrade rate 100,
threshold `upgradeTime * 100`). Under the present unit contract, the no-leading-100
formula is mathematically equivalent to K-Mod's original scale. Current SAS is therefore
repaired; do not number the historical transient defect.

===============================================================================
F541 / KI#864 CONFIRMED
AI_neededCultureDefenders UNDERCOUNTS AFTER LOST ceil() IN PRACTICAL 2723
===============================================================================

STATUS: CONFIRMED CURRENT AdvCiv/SAS GAMEPLAY BUG
ORIGIN: Base AdvCiv practical 2723 / 97f40e51e5
TITLE: "Some minor refactoring"
SCOPE: Current Base AdvCiv 1.14 and current AdvCiv-SAS retain the regression.
ROOT: a deliberate upward rounding step was dropped during the double -> scaled-number
      conversion; final nearest-integer rounding is not equivalent.

A. EXACT ANCESTRY
-----------------
The culture-garrison helper was introduced with an explicit ceiling. Practical 2719
still contains:

    double const dAIEraFactor = kOwner.AI_getCurrEraFactor().getDouble();
    double r = ::ceil(targetGarrisonStr /
            std::max((dAIEraFactor + 0.5) *
            (dAIEraFactor < 3.5 ? 3 : 4), 3.0));
    ...
    return ::round(r);

The nearby practical-2719 comment explicitly says:

    Tbd.: Use scaled instead of double

Practical 2723 performs exactly that advertised conversion. It changes the intermediate
to:

    scaled r = rTargetGarrisonStr / scaled::max(3,
            (rAIEraFactor + fixp(0.5)) *
            (rAIEraFactor < fixp(3.5) ? 3 : 4));

and eventually returns:

    return r.round();

The `::ceil(...)` operation itself disappears. The commit title contains no stated AI
policy/defender rebalance; the hunk is part of a representation/refactoring conversion.
No later commit restores the ceiling. Current Base AdvCiv and SAS retain this form.

B. THIS IS NOT A SCALED-NUMBER EQUIVALENCE
-------------------------------------------
`ScaledNum` has separate, explicit `round()` and `ceil()` APIs. Its own tests assert,
for example, that `fixp(2.001).ceil() == 3`, `fixp(2.5).ceil() == 3`, etc.

Thus:
- old intermediate semantics: any positive fraction above integer N becomes N+1;
- current semantics at function return: fractions below N+0.5 become N.

The later war/occupation modifiers do not make those two operations interchangeable.
The old code intentionally established an integer minimum first, then applied those
modifiers, and finally rounded the post-modifier result.

C. CLEAN NUMERIC WITNESS
------------------------
For Ancient-era factor 0, the denominator is exactly:

    max(3, (0 + 0.5) * 3) = 3

When the city is in a will-flip state, `rTargetProb` is explicitly set to 0, so:

    rTargetGarrisonStr = iCultureStr

Take any legal culture-strength result of 4. Then before occupation/war rescaling:

    raw requirement = 4 / 3 = 1.333...

Historical intended result:

    ceil(4 / 3) = 2

Current result in the ordinary no-extra-scaling branch:

    round(4 / 3) = 1

This is not dependent on an overflow, sentinel or unsupported XML value.
`cultureStrength()` itself computes a fixed-point blend of population, era, inner-ring
culture, culture ratios, anger/grievance/religion and handicap factors and finally
returns `rStrength.round()`. There is no invariant that constrains its integer output to
multiples of 3. Therefore values in the affected residue/fraction classes are ordinary
reachable outputs, not a forbidden synthetic state.

More generally, every positive pre-modifier quotient in an interval:

    (N, N + 0.5)

is one defender lower after practical 2723 than under the original ceiling semantics.

D. LIVE CONSEQUENCES
--------------------
The value is not UI-only.

1. `AI_neededDefenders()` applies:

    iDefenders = std::max(iDefenders, AI_neededCultureDefenders());

so the lost ceiling can directly reduce the requested city garrison by one when other
floors do not already dominate.

2. `AI_calculateNeededFloatingDefenders()` applies:

    int iCultureDefendersNeeded =
            AI_neededCultureDefenders() - AI_minDefenders();
    iFloatingDefenders = std::max(iFloatingDefenders,
            iCultureDefendersNeeded);

so the same undercount can remove a culture-driven floating-defender requirement.

For a clean early-game SAS boundary, `AI_minDefenders()` starts at 1; with No Barbarians
the custom early anti-Barbarian increment is disabled, and Ancient era does not meet the
later-era defender thresholds. Thus the historical 2 versus current 1 culture floor can
remain decision-visible rather than being automatically masked.

This can change city-defense demand, unit allocation and military production/guard
behavior in culturally pressured cities.

E. DEDUP
--------
This is independent of F526.
- F526 is cache-key contamination between culture-inclusive and culture-excluding
  `AI_neededFloatingDefenders` estimates.
- F541 changes the authoritative culture-defender quantity itself before that cache is
  involved.

It is also distinct from the generic WIP290 arithmetic check of this helper, which
challenged current divisors/object/sentinel contracts but did not reconstruct the lost
historical `ceil()` operation.

F. CONSERVATIVE REPAIR DIRECTION
--------------------------------
Restore the old upward rounding at the same semantic stage, e.g. compute the quotient as
scaled and convert it through `ceil()` before applying the subsequent occupation / war
adjustments, preserving the historical ordering. Do NOT merely replace the final
`r.round()` with `r.ceil()`: the old implementation first ceiled the base requirement,
then applied later modifiers, then rounded the final result.

A minimal representation-preserving shape would therefore be conceptually:

    scaled r = scaled((rTargetGarrisonStr / rDenom).ceil());
    ... existing occupation/war scaling ...
    return r.round();

Exact local style/type spelling should be chosen only when implementing; audit source is
still untouched.

===============================================================================
CURRENT STONE
===============================================================================
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F541 CONFIRMED.
F542 / provisional KI#865 UNUSED.
Latest physical checkpoint: C031-WIP315.
Repository source untouched.

EXACT NEXT CURSOR
-----------------
Resume from WIP315 / F542 unused:
1. continue historical "minor/misc/refactor" CvCityAI semantic archaeology below
   practical 2723, prioritizing double->scaled, iterator/container and condition-moving
   conversions;
2. explicitly compare each changed rounding stage (ceil/floor/round/truncation) rather
   than only final values -- F541 shows why representation refactors are high-yield;
3. then return to producer/consumer stale-state seams if no further historical survivor
   appears;
4. promote F542 only for an independent current behavior root; otherwise freeze the next
   control checkpoint.

END C031-WIP315


================================================================================
C031-WIP316 — QUEUE 005 CONTINUATION — F542 / KI#865 CONFIRMED
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446-F542 confirmed.
F543 / provisional KI#866 is the next UNUSED root.
Repository source untouched.

This checkpoint continues from C031-WIP315 / F542 unused. It first completes the older
mechanical-refactor archaeology opened after F541, then changes lenses to current incremental
CvCityAI member lifetime. The broad persistent constructor/read/write inventory had already been
completed in WIP301 and is not duplicated here.

POST-WIP315 HISTORICAL REFACTOR CONTROLS
----------------------------------------

1. PRACTICAL 1910 ITERATOR / CITY-CROSS SET EQUIVALENCE — CURRENT CLEAN

The old `cityCross(kPlot, ...)` helper always included the center plot; the converted
`CityPlotIter(kPlot, false)` explicitly excludes it. That is a real set difference, but it does
not change the live `AI_healthHappyImprovementValue` behavior:

* normal `AI_getImprovementValue` reaches the helper from `AI_bestPlotBuild` on
  `WorkablePlotIter(*this, false)`, so `kPlot` is not the city center;
* in the debug/UI path, if `kPlot` itself is a city, `AI_getPlotCity()` resolves to the same
  city and the recursion already skips `pCity == this`;
* `plot` vs `plotValidXY` has the same wrapping/off-map behavior for the city-radius offsets.

The remaining Workable/Working iterator conversions preserve the old
`getWorkingCity()==this` / `isWorkingPlot` filters. The one genuine practical-1910 state bug —
a newly scoped/uninitialized improvement-yield array used by the second calculation — is the
historical OOS defect already repaired by practical 2035. Current SAS contains the repair.

2. PRACTICAL 1908 — ENUM/SPECIALIST REFACTOR CLEAN

The specialist loops converted to enum iterators and the `isSpecialistForced()` helper preserve
the old forced-specialist state contract. The second-loop mutation of the old temporary
`bForcedSpecAvailable` state was redundant once a valid forced specialist had already been
identified by the first pass. No current semantic survivor.

3. PRACTICAL 1900 — HISTORICAL TERNARY-PRECEDENCE REGRESSION, ALREADY REPAIRED

The area pointer->reference conversions are semantically safe. However practical 1900 also
introduced a genuine transient bug in the `UNITAI_RESERVE_SEA` weight while reformatting:

    base + (bPrimaryArea ? 1 : 0)

became effectively:

    (base + bPrimaryArea) ? 1 : 0

because the parentheses around the ternary were lost. Practical 2571 later restores the exact
needed parentheses; Base AdvCiv 1.14 and current SAS contain the repair. This updates the earlier
2571 control record: that commit is not merely `uiFlag`/style cleanup; it also repairs this real
precedence regression.

4. RIVAL DIPLO-VICTORY STAGE — HISTORICAL SAS REPAIR, NOT F542

An older rival-progress loop tested `kOwner.AI_atVictoryStage(DIPLO2)` where the surrounding
calculation was explicitly about `kRival`. Current SAS already uses `kRival`. The exact one-line
repair is SAS practical 4365 / `299d90a56a`. No current root.

5. PRACTICALS 1918 / 1913 / 1899 / 1794 / 1764 / 1753 / 1651 / 1565 / 1493 /
   1414 / 1360 — CONTROLS

* 1918's CvCityAI part is wonder-accessor centralization plus enum-loop substitution; it does
  not change the visitation set/order in a gameplay-relevant way.
* 1913 removes redundant parentheses around the floating-defender threshold.
* 1899 intentionally introduces the culture-garrison model and still contains the explicit
  `ceil`; F541 appears only much later when practical 2723 drops it.
* 1794 `getNumTotalBonuses() <= 0` -> `!isAnyBonus()` is equivalent.
* 1764 raw plot-yield pointer -> `CvPlot::getYield` preserves the food-availability algorithm.
* 1753 `GC.getXInfo` -> overloaded `GC.getInfo` substitutions remain enum-type matched in every
  nontrivial CvCityAI hunk checked; no wrong overload survived.
* 1651's CvCityAI changes are pointer->reference plot-danger overloads, not topology changes.
* 1565 intentionally replaces global Always-Peace reasoning with team-specific
  `!AI_isWarPossible()` policy.
* 1493 `GC.getGame()` -> `getGameINLINE`/const cleanup is semantic-neutral.
* 1414 naval-XP filters reorder equivalent conditions.
* 1360's hoisted city iterator state remains initialized by each `firstCity(&iLoop)` call; the
  later route-target code also eventually receives safer iterator refactors.

6. HISTORY-WIDE SEMANTIC-DELTA SIEVE — NO SECOND F541-CLASS SURVIVOR

A mechanical scan over all reachable CvCityAI historical diffs looked specifically for:

* lost `ceil/floor/round` stages;
* `<` / `<=` boundary changes;
* removed initializers;
* moved `break/continue/return`;
* ternary-parenthesis changes;
* assignment-target substitutions;
* iterator/container substitutions whose visited set changes.

The major apparent hits either reduce to already-numbered findings, explicit historical repairs,
or deliberate precision improvements (`AI_updateSafety`, `AI_buildUnitProb`, the broad scaled
migrations). Nothing else in this pass matches F541's isolated lost-operator pattern.

PERSISTENT MEMBER SAVE/LOAD REDISCOVERY — CLOSED BY WIP301
---------------------------------------------------------

The first current-member pass noticed that the custom culture-victory rank and culture-victory
investment-percent fields affect production/process/Artist decisions, are refreshed by
`AI_updateCommerceWeights`, and are intentionally not serialized while `m_iCultureWeight` is.

Before spending a finding this was deduplicated against the album. WIP301 already performed the
full persistent `CvCityAI` constructor/read/write inventory and explicitly challenged these exact
transient culture fields. Normal AI turn start refreshes them before the normal AI decision path;
no independent mid-turn/load consumer was proved. Do not reopen this as F542.

The lifecycle lens was therefore narrowed to members whose value can become wrong BETWEEN full
refreshes. That class already produced F538/F539 for Worker accounting and now produces F542.


================================================================================
F542 / KI#865 — `m_routeToCity` IS A PREVIOUS-END-OF-TURN SNAPSHOT; ROUTE TOPOLOGY CAN CHANGE
BEFORE THE NEXT WORKER PHASE, SO `AI_routeCity()` CAN MISS OR CHASE A CONNECTION FOR A FULL
WORKER PHASE
================================================================================

STATUS
------
CONFIRMED — inherited K-Mod/BBAI city-route-target cache lifetime defect, retained by Base
AdvCiv 1.14 and current AdvCiv-SAS. Stock BtS/Civ4CE does not contain this exact
`CvCityAI::m_routeToCity` cache / `AI_routeCity` architecture.

ROOT
----
`CvCityAI::AI_updateRouteToCity()` computes one same-area TEAM city that is not connected to the
source city by the player's best currently available Route type. It stores that target in:

    IDInfo m_routeToCity;

and `CvUnitAI::AI_routeCity()` later trusts the cached result through:

    CvCityAI* pRouteToCity = kLoopCity.AI_getRouteToCity();

The cache has only two current update paths:

1. `CvCityAI::AI_doTurn()` -> `AI_updateRouteToCity()`;
2. `CvPlayerAI::AI_processNewBuild()` when a newly available Build type is processed.

There is NO update/invalidation from `CvPlot::setRouteType`, Worker build completion, pillaging,
air bombing, plot-group changes or other ordinary route-topology mutations.

The timing is the critical part. In sequential turns:

    CvPlayer::setTurnActive(true)
        -> AI_updateCacheData()
        -> ...
        -> doTurnUnits()                 // Workers consume m_routeToCity here

and only when the player's turn is deactivated does:

    CvPlayer::doTurn()
        -> ...
        -> FOR_EACH_CITY city->doTurn()
             -> CvCityAI::AI_doTurn()
                  -> AI_updateRouteToCity()

run.

Thus the Worker phase generally consumes a route-target snapshot computed at the PREVIOUS end of
that player's turn. Any relevant topology change after that snapshot can invalidate it before the
next Worker phase.

WHY THE STORED PREDICATE REALLY CHANGES WHEN A RAILROAD IS DOWNGRADED
--------------------------------------------------------------------
`AI_updateRouteToCity()` uses `GC.getRouteFinder()`. Its `routeValid` callback accepts a plot only
when:

    kNewPlot.getRouteType() == GET_PLAYER(ePlayer).getBestRoute(&kNewPlot)

for owned/team territory.

Current SAS XML gives:

    ROUTE_ROAD     iValue = 1
    ROUTE_RAILROAD iValue = 2
    Railroad RoutePillage = ROUTE_ROAD

Once Railroad is available/buildable on a plot, `CvPlayer::getBestRoute()` therefore selects
Railroad. A Road left by pillaging is still enough for ordinary trade-network connectivity, but
it is NOT valid for the best-route path used by `AI_updateRouteToCity()`.

CLEAN SINGLE-PLAYER / SINGLE-MEMBER-TEAM WITNESS — ENEMY DOWNGRADES ONE RAIL TILE
---------------------------------------------------------------------------------
Set up AI player A with:

* at least two own cities in the same land area;
* both cities connected to the capital and to each other through a complete Railroad path;
* Railroad still being A's best buildable route;
* one critical Railroad segment on an OWNED, NO-IMPROVEMENT, NO-BONUS tile;
* at least one ordinary Worker able to reach that segment;
* no higher-priority Worker job that would mask the route decision.

At the end of A's turn N, `AI_updateRouteToCity()` sees a complete best-route path and stores no
missing target for this pair (e.g. `m_routeToCity` is null).

Later in the sequential round, enemy B pillages the critical Railroad. Current XML downgrades it
to Road. `CvPlot::setRouteType()` repairs plot groups but does NOT call
`AI_updateRouteToCity()`.

At A's next activation / Worker phase:

1. the two cities remain ordinarily connected to the capital by ROAD, therefore
   `AI_connectCity()` has no disconnected-city job;
2. the damaged segment has no improvement, therefore the earlier
   `AI_routeTerritory(true)` pass gives it value 0 and skips it;
3. it has no bonus, so `AI_connectBonus(false)` does not select it;
4. a fresh `AI_updateRouteToCity()` WOULD now fail the best-route path across the Road segment
   and store the other city as a Railroad-connection target;
5. but no refresh has occurred, so `AI_routeCity()` reads the stale null old target and returns
   false;
6. immediately after that failed call, current `AI_workerMove()` executes:

       bCanRoute = false; // advc.opt: Don't try again

   which suppresses the later generic `AI_routeTerritory()` fallback for this Worker.

Therefore the Worker cannot select the needed Railroad repair through the city-route logic in
this Worker phase. Only at A's end-of-turn city processing does `AI_updateRouteToCity()` finally
notice the damaged best-route connection. The Worker can then react on the NEXT activation — a
full Worker phase late.

This is a decision difference, not merely a stale debug value:

    fresh cache -> AI_routeCity() pushes a MISSION_ROUTE_TO repair sequence;
    stale cache -> AI_routeCity() cannot select that repair at all this turn.

SAME-TURN REVERSE WITNESS — REDUNDANT ROUTE MISSION AFTER ANOTHER WORKER FINISHES THE PAIR
------------------------------------------------------------------------------------------
The inverse exists within one Worker phase as additional scope evidence.

Suppose the previous snapshot correctly stores city B as city A's missing best-route target.
Worker 1 completes the final missing route segment early in the unit pass. There is no
`m_routeToCity` invalidation. Worker 2 later reaches `AI_routeCity()` after Worker 1's completed
mission no longer provides collision protection.

The stale non-null pair can still be selected. `MISSION_ROUTE_TO` does not first re-test whether
the two cities are already connected by the desired route. `groupRoadTo()` builds only when the
current tile needs a route; otherwise it calls `groupPathTo()` toward the cached endpoint. Thus a
second Worker can spend movement following an already completed connection until the stale
mission self-terminates at an endpoint.

The enemy-Railroad-downgrade witness is the primary reproduction because it requires only one
Worker and avoids dependence on same-pass Worker ordering.

WHY EXISTING REFRESHES DO NOT CLOSE THE WINDOW
----------------------------------------------
Whole-core call inventory finds only:

    CvCityAI::AI_doTurn -> AI_updateRouteToCity
    CvPlayerAI::AI_processNewBuild -> per-city AI_updateRouteToCity

`AI_processNewBuild` responds to acquiring a new Build capability, not to completing or destroying
one instance of a route. `CvPlot::setRouteType` updates plot groups and connected-resource state
but not this AI cache.

The ordinary city `AI_doTurn` refresh is deterministic self-heal, but it occurs after the current
unit phase in sequential-turn play and is therefore one phase too late for Worker decisions.

ANCESTRY
--------
Civ4CE/BtS does not contain this exact cache/helper architecture.

The K-Mod history's initial 2010 code already contains:

* `m_routeToCity`;
* `AI_updateRouteToCity()` in city AI turn processing;
* `AI_routeCity()` as a Worker consumer;
* serialization of the cached ID.

Final K-Mod 1.46 retains the architecture. Base AdvCiv 1.14 retains it, with later advc.121
refinement of the route choice and mission sequencing. Current SAS retains it.

Because the available K-Mod history begins with the large initial K-Mod snapshot, this pass does
not claim a narrower post-initial K-Mod practical regression boundary. Classification is an
inherited K-Mod/BBAI route-target cache lifetime defect.

DEDUP
-----
This root is independent of prior route findings.

* WIP290 challenged `AI_updateRouteToCity()`'s apparently inverted `!GeneratePath` condition and
  correctly closed it: the helper is SUPPOSED to store a city for which a best-route path is
  missing. F542 concerns the lifetime of that correctly computed predicate after topology changes.
* F507 / KI#830 concerns cached TRADE ROUTE partner/yield state after roads/routes change. F542
  shares the physical route-topology producer but uses a different stored object
  (`CvCityAI::m_routeToCity`), a different consumer (`CvUnitAI::AI_routeCity`) and a different
  direct consequence (Worker mission selection rather than economic booking).
* F538/F539 concern incremental Worker-count accounting, not route topology/target caching.
* The prior `AI_routeCity` WIP controls audited advc.121 midpoint/queued-mission semantics once a
  target has been chosen; they did not establish that `m_routeToCity` stays current before target
  selection.

REPAIR BOUNDARY
---------------
The invariant should be:

    before Worker AI consumes m_routeToCity, the cached target must reflect CURRENT best-route
    topology for the relevant team cities.

Conservative repair options include:

1. refresh each owned city's `AI_updateRouteToCity()` at player turn activation before
   `doTurnUnits()` (batched and simple; avoids per-route-build invalidation churn);
2. dirty the route-target caches when a route topology change can affect the owner/team, then
   lazily recompute on `AI_getRouteToCity()` / before Worker use;
3. as a defensive consumer check, revalidate a non-null cached pair before pushing missions — but
   this alone does NOT fix the stale-null case after route destruction, so a dirty/pre-unit
   refresh is still needed.

Refreshing only after a Worker completes a route is insufficient because enemy pillage/air bombing
and teammate route changes can invalidate the cache between turns.

REGRESSION TESTS
----------------
1. Complete Railroad pair at A end-turn -> enemy pillages an unimproved/no-bonus Rail tile to Road
   -> A Worker next activation must identify/rebuild the missing Railroad connection in that SAME
   Worker phase, not one turn later.
2. Verify the cities remain road-connected to the capital so `AI_connectCity()` cannot mask the
   test.
3. Put no improvement/bonus on the damaged tile so `AI_routeTerritory(true)` and
   `AI_connectBonus(false)` cannot mask the test.
4. Two Workers: Worker 1 finishes the final missing best-route segment; Worker 2 later in the same
   pass must not receive a stale redundant city-route mission.
5. Team-member city connection: external teammate route changes should also dirty/recompute the
   relevant owner cache.
6. New route technology/build acquisition must continue to refresh correctly through
   `AI_processNewBuild`.
7. Save/load determinism should remain unchanged; no serialization layout change is required for
   a dirty/pre-unit refresh repair.

PROMOTION
---------
F542 / KI#865 is CONFIRMED.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F542 confirmed.
F543 / provisional KI#866 UNUSED.
Latest physical checkpoint: C031-WIP316.
Repository source untouched.

NEXT
----
Continue under F543 unused with the current incremental-member/event-maintenance lens, while
avoiding WIP301's already-completed persistent serialization inventory and F519/F520's city-safety
cache family. Prioritize:

1. other city AI snapshots recomputed at city end-turn but consumed earlier in the next unit phase;
2. `m_aiBestBuildValue` / `m_aeBestBuild` only where independent of the already-owned KI#213
   custom-Worker-vs-legacy-best-build architecture;
3. city emphasis/special-yield state only for incremental producer/consumer mismatches not already
   covered by WIP290/WIP305;
4. route-target sibling invalidation (city creation/loss/team ownership) only if it has an
   independent consequence beyond F542's same cache-lifetime repair boundary;
5. otherwise return to remaining current CvCityAI output/state contracts and promote F543 only for
   a separate live root.

END C031-WIP316


================================================================================
C031-WIP317 — QUEUE 005 INCREMENTAL-STATE SWEEP — STALE WORKER-DEMAND SNAPSHOT AFTER PLOT MUTATION
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F543 / KI#866 CONFIRMED.
F544 / provisional KI#867 UNUSED.
Repository source untouched.

F543 / KI#866 — m_iWorkersNeeded CAN STAY STALE THROUGH THE NEXT UNIT PHASE AFTER WORKABLE-PLOT STATE CHANGES
------------------------------------------------------------------------------------------------------------
Status: CONFIRMED inherited Worker-demand snapshot lifetime defect.

ROOT
----
`CvCityAI::AI_updateWorkersHaveAndNeeded()` computes and stores:

    m_iWorkersNeeded = iWorkersNeeded;
    m_iWorkersHave = iWorkersHave;

The normal refresh runs from `CvCityAI::AI_doTurn()`, which is called from `CvCity::doTurn()`.
In sequential-turn play, that city turn is the owner's END-turn processing. The owner's next
activation instead starts by calling `CvPlayer::doTurnUnits()` before any new city `AI_doTurn()`.

Therefore `m_iWorkersNeeded` is intentionally carried from the previous city turn into the next
unit phase. That is safe only if the plots/tasks on which it depends cannot change in between.
They can.

Whole-source call inventory for `AI_updateWorkersHaveAndNeeded()` finds only:

* city initialization;
* the ordinary `CvCityAI::AI_doTurn()` refresh;
* two old commented-out calls beside that refresh.

`CvPlot::setImprovementType()` updates yields, plot-group bonus state, health/happiness and UI
state, but does not dirty or refresh Worker demand. Enemy pillage, bombardment/event destruction,
feature/improvement changes and similar external plot mutations can therefore make the stored city
demand stale until the owner's NEXT end-turn city refresh.

DETERMINISTIC CURRENT-SAS WITNESS — ENEMY PILLAGES A WORKED IMPROVEMENT
------------------------------------------------------------------------
Use a civilization's SECONDARY coastal land area so the SAS primary-area standing Worker floor
does not mask zero task demand.

Initial owner end-turn state:

* coastal city C has no local Worker;
* every useful local Worker task is complete;
* no live bonus/route/future-task source keeps `AI_neededWorkers(area)` positive;
* `AI_updateWorkersHaveAndNeeded()` therefore stores `C.m_iWorkersNeeded = 0`;
* the area can legitimately report `AI_neededWorkers(area) = 0` under the current KI#192 policy.

After that city refresh, but before C's owner's next activation, an enemy pillages one IMPROVEMENT
on a currently worked, featureless, owned land tile in C's workable radius.

The current SAS city-demand code contains the explicit KI#198 worked-land fallback:

    if (!kPlot.isImproved() && kPlot.isBeingWorked())
    {
        bool const bSASWorkedLandDemand =
            (!kPlot.isWater() && !kPlot.isFeature() &&
             kPlot.getOwner() == getOwner());
        if ((AI_getBestBuild(ePlot) != NO_BUILD || bSASWorkedLandDemand) &&
            isArea(kPlot.getArea()))
        {
            iUnimprovedWorkedPlotCount++;
        }
    }

So an immediate fresh recomputation does NOT depend on the legacy best-build cache for this
fixture. It deterministically gets:

    iUnimprovedWorkedPlotCount = 1
    iWorkersNeeded += 2 * 1 = 2

The later ordinary reduction gives at least:

    (2 + 1) / 3 = 1

and the final lower bound

    max((iUnimprovedWorkedPlotCount + 1) / 2, iWorkersNeeded)

also preserves at least 1. Thus a fresh city snapshot is definitively:

    C.AI_getWorkersNeeded() >= 1

The actual snapshot remains the pre-pillage 0 throughout the next unit phase.

DIRECT CONSUMER — AI_ferryWorkers HARD-SKIPS THE CITY
-----------------------------------------------------
Now place a valid `UNITAI_SETTLER_SEA` transport carrying at least one `UNITAI_WORKER` in the
reachable water network. There is no Worker already on C's land area.

`AI_settlerSeaMove()` calls `AI_ferryWorkers()` whenever Worker cargo is present and no competing
load-settler assignment blocks it.

`AI_ferryWorkers()` loops owned coastal cities and begins each candidate with:

    int iHave = pLoopCity->AI_getWorkersHave();
    ...
    int iValue = pLoopCity->AI_getWorkersNeeded() - (2 * iHave + 1) / 3;
    if (iValue <= 0)
        continue;

With the actual stale state:

    cached needed = 0
    local have = 0
    iValue = 0 - 0 = 0
    -> city rejected before pathfinding

With the fresh post-pillage state:

    needed >= 1
    local have = 0
    iValue >= 1
    -> city passes the first gate

The area-level hard capacity gate also separates cleanly. With city demand >=1,
`CvPlayerAI::AI_neededWorkers(destinationArea)` receives `3 * AI_getWorkersNeeded()` from C and
returns a positive land-area demand; with no destination-area Worker, `iAreaHave < iAreaNeeded`.
The transport can therefore path to and select C. In the stale state it never reaches that test.

This is a real unit-mission decision difference in the immediately following activation, not a
debug-only or eventually-consistent statistic. The city self-heals only at its next END turn,
so the transport opportunity is delayed by a full unit phase.

CULTURE-EXPANSION SIBLING
-------------------------
The same lifetime window exists for natural border expansion:

    CvCity::doTurn:
        AI().AI_doTurn();   // Worker demand/best-build snapshot
        doGrowth();
        doCulture();        // can acquire new workable plots afterward

The next player activation executes `doTurnUnits()` before another city refresh. Advanced Start
contains an explicit `pCity->AI_updateBestBuild(); // to account for culture expansion`, which is
useful evidence that culture expansion can invalidate city build planning. Ordinary `doCulture`
has no corresponding Worker-demand refresh.

The pillage witness is preferred for F543 because SAS's worked-land fallback proves the fresh
Worker need without relying on an immediate best-build-cache refresh after the border change.

ANCESTRY
--------
Civ4CE/BtS does not expose this exact BBAI/K-Mod city Worker-demand cache architecture.

The earliest reachable K-Mod snapshot (practical 3 / 2414225e64...) already contains:

* `m_iWorkersNeeded` / `AI_getWorkersNeeded()`;
* `AI_updateWorkersNeededHere()` in city AI turn processing;
* the city-before-culture ordering in `CvCity::doTurn()`;
* `AI_settlerSeaFerry()` consuming `AI_getWorkersNeeded()` as a hard positive-city gate.

Final K-Mod retains the architecture. AdvCiv renames/extends it to
`AI_updateWorkersHaveAndNeeded()` and `AI_ferryWorkers()`, but still refreshes the snapshot in the
city turn and consumes it in the next unit phase. Current SAS retains that lifetime model while
making the ferry capacity checks even more explicit.

Because the available K-Mod history begins with the initial large K-Mod snapshot, this pass does
not claim a narrower post-initial practical birth. Classification: inherited BBAI/K-Mod Worker-
demand cache lifetime defect.

DEDUP
-----
F543 is independent of the existing Worker families:

* KI#213 / F006 is the SPLIT JOB-MODEL defect: the city-side demand calculation can be wrong even
  when freshly recomputed because it does not see every job handled by SAS's custom Worker
  evaluator. F543's primary witness deliberately uses `bSASWorkedLandDemand`, so a FRESH current
  SAS computation is provably correct (>=1); only its lifetime is wrong.
* KI#198 owns the standing Worker floor and cross-subsystem minimum policy. F543 uses a secondary
  area where that floor is intentionally allowed to be zero before new work appears.
* F538 is completion-turn Worker-HAVE double counting.
* F539 is stale old-city Worker-HAVE assignment after mission interruption.
* F542 is `m_routeToCity` route-topology snapshot lifetime. It establishes the same general lesson
  (end-turn city cache consumed next unit phase) but stores a different object, has different
  producers/consumers and needs a different refresh payload. F543 concerns plot-improvement/task
  demand and Worker transport/production decisions rather than city-pair route targeting.

REPAIR BOUNDARY
---------------
Required invariant:

    before unit AI uses city/area Worker demand, m_iWorkersNeeded must describe current workable
    plot/task state rather than the previous end-turn snapshot.

Conservative approaches:

1. At player turn activation before `doTurnUnits()`, refresh the Worker-planning city state used by
   unit AI. Because ordinary city demand depends partly on `m_aeBestBuild`, this should normally
   refresh `AI_updateBestBuild()` before `AI_updateWorkersHaveAndNeeded()` (matching city turn
   order), rather than refreshing only the latter.
2. Introduce a dirty flag for plot/culture/task changes affecting an owned city's workable radius
   and lazily rebuild best-build + Worker-demand state before a demand consumer.
3. Event-side targeted invalidation can reduce work, but must cover enemy pillage/destruction,
   culture/working-city changes, improvement completion/removal, relevant build-tech changes and
   other task producers; a pre-unit batched refresh is simpler and harder to miss.

Refreshing only `m_iWorkersHave` does not repair this root. Likewise, patching `AI_ferryWorkers`
alone would leave the same stale demand visible to area Worker counts, production, relocation,
evacuation and other consumers.

REGRESSION TESTS
----------------
1. Secondary coastal city, no Worker, fully developed -> end-turn demand 0 -> enemy pillages a
   worked featureless owned improvement -> next activation Worker-carrying Settler transport must
   recognize the city/area shortage in that SAME unit phase.
2. Confirm no bonus on the pillaged plot so `AI_countUnimprovedBonuses` cannot independently mask
   stale city demand.
3. Confirm the area is secondary so the primary-area KI#198 Worker floor cannot mask the 0->1
   transition.
4. Natural border expansion after the city AI refresh should likewise expose new Worker tasks to
   the next unit phase.
5. Worker completion that removes the final task should not leave a stale positive demand that
   attracts unnecessary ferrying/retention through the next phase.
6. Save/load and human automation behavior must preserve deterministic cache contents.

PROMOTION
---------
F543 / KI#866 is CONFIRMED.

CURRENT STONE
-------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` OPEN.
32/169 unique files complete.
F446-F543 confirmed.
F544 / provisional KI#867 UNUSED.
Latest physical checkpoint: C031-WIP317.
Repository source untouched.

NEXT
----
Continue under F544 unused with the incremental-member/event-maintenance lens. The strongest next
checks are:

1. distinguish stale-positive `m_iWorkersNeeded` after Worker task completion from the same F543
   repair boundary rather than renumbering it;
2. inspect `m_iWorkersHave` external producers not already covered by F538/F539;
3. inspect `m_eBestBuild` / construction-value and safety/defender snapshots only for external
   mutation windows not already owned by KI#213, F519/F520 or WIP301;
4. then switch to cross-file callers that treat a `CvCityAI` cached scalar as a hard gate and ask
   whether its producer can change between city end-turn and next unit phase.

END C031-WIP317


================================================================================
C031-WIP318 - POST-WIP317 COVERAGE / KI#213 BROADENING; F544 / KI#867 CONFIRMED
================================================================================

PURPOSE / SOURCE BASIS
----------------------
Continuation from C031-WIP317 using the locally extracted current AdvCiv-SAS light source,
Base AdvCiv 1.14, K-Mod 1.46, Civ4CE/BtS reconstruction, and retained AdvCiv-SAS commit diffs.

No repository source file was modified.

FINDING NUMBER STATE
--------------------
F446 / KI#769 through F543 / KI#866 remain CONFIRMED.
F544 / KI#867 is now CONFIRMED.
F545 / provisional KI#868 is UNUSED.

POST-F543 SIBLING - STALE m_iWorkersHave AFTER WORKER DEATH/CAPTURE
-------------------------------------------------------------------
The same refresh-lifetime root proved for m_iWorkersNeeded also applies to m_iWorkersHave.

A city can finish its city turn with:

    m_iWorkersHave   = 1
    m_iWorkersNeeded = 1

because one Worker is physically/mission-assigned to it. If an enemy kills or captures that Worker
before the owner's next activation, no death/capture path performs AI_changeWorkersHave(-1).
The next unit phase can therefore still evaluate the city through AI_ferryWorkers with stale have=1.
Its replacement shortage becomes:

    needed - (2*have + 1)/3
    1      - (2*1    + 1)/3
    0

and the city is rejected. A fresh combined Worker planning refresh gives have=0, needed=1 and accepts
the shortage.

Do NOT number this separately. It has the same stale end-turn Worker-planning snapshot, the same
pre-unit-phase consumer and the same robust repair boundary as F543. F543 should therefore be read as
covering both m_iWorkersNeeded and m_iWorkersHave external-mutation staleness, while F538/F539 retain
their distinct intra-refresh/incremental accounting roots.

LOW-COVERAGE LIVE-FUNCTION INVENTORY - CONTROLS
------------------------------------------------
A function-coverage pass against the album was used to avoid repeatedly auditing already saturated
large helpers.

Several apparent zero-mention methods are dead/commented BtS/K-Mod residue in current SAS, including
AI_potentialPlot, AI_foodAvailable, AI_ignoreGrowth, AI_chooseProject and old culture helper blocks.
They do not define a current gameplay surface.

AI_countBestBuilds:
- current loop is CityPlotIter(*this, false), matching the old non-center city-radius set;
- current core C++ use is essentially the Python exposure; CvAdvisorUtils uses it for an improvement
  warning;
- no independent iterator/set defect survives.

AI_countWorkedPoorPlots:
- current semantics are inherited K-Mod semantics: a worked plot is poor when !AI_isGoodPlot;
- no separate local defect survives once the shared best-build-policy split below is recognized.

AI_emphasizeIrrigatingPlot:
- AdvCiv practical 4214 extracted the old inline K-Mod condition into the helper;
- the extraction preserves the captured plot/city conditions (later 4216 barbarian exclusions are
  intentional);
- no lost local state or changed plot identity survives.

AI_setStrongEmphasis:
- persistent-until-emphasis-change behavior is deliberate;
- it is serialized and its setter immediately reassigns plots;
- no lifecycle defect found.

BBAI SETTLER-COMPLETION TODO - CLOSED POSITIVELY
------------------------------------------------
AI_chooseProduction contains the old comment:

    // BBAI TODO: Does this work right after settler is built???

The completion ordering answers yes. CvCity::popOrder(ORDER_TRAIN) calls owner.initUnit(...) before
the empty-queue AI_chooseProduction() call. Therefore the just-completed Settler already exists when
AI_totalUnitAIs(UNITAI_SETTLE) and the city-plot Settler count are queried. No stale completion-count
bug exists there.

KI#213 ARCHITECTURAL BROADENING - DO NOT SPEND F544
---------------------------------------------------
The existing pending KI#213 split between the legacy CvCityAI best-build cache and the custom
CvUnitAI land-Worker policy affects more than AI_updateWorkersHaveAndNeeded.

AI_isGoodPlot projects a plot through m_aeBestBuild / m_aiBestBuildValue only when the legacy city
cache supplies a best build. Ordinary SAS land jobs can deliberately be absent from that cache even
when CvUnitAI::AI_bestCityBuild would choose a valid custom improvement. AI_isGoodPlot then evaluates
only current yields.

A simple family is an unimproved flat Plains plot:

    current 1 food + 1 production -> magic value 17 -> not good (land threshold >21)
    Farmed  2 food + 1 production -> magic value 27 -> good

when the current SAS Worker policy legitimately chooses the Farm under food pressure.

Consequences of the same architectural split include:
- AI_countGoodPlots -> AI_getTargetPopulation;
- target population -> food-kept building value, AI_splitEmpireValue and future Worker-demand math;
- AI_countWorkedPoorPlots -> target-population correction and draft/growth heuristics;
- CitySiteEvaluator plot-sharing protection via AI_isGoodPlot / AI_countGoodPlots;
- CvAdvisorUtils's AI_countBestBuilds-based improvement warning can likewise undercount ordinary
  custom land opportunities.

All of these belong to KI#213. The shared-policy repair already intended for KI#213 should make the
city-side projection and Worker-side choice consume one build policy. Do not allocate new finding
numbers to individual consequences.

================================================================================
F544 / KI#867 - SAS TRANSIENT CULTURE-VICTORY CITY RANK/INVESTMENT IS REFRESHED
BEFORE CITY PRODUCTION BUT CAN BECOME STALE AFTER A CULTURE BUILDING COMPLETES;
THE SAME popOrder THEN USES THE OLD ROLE TO GATE PRODUCTION AND CULTURE PROCESS
================================================================================

STATUS
------
CONFIRMED - AdvCiv-SAS-specific cache/snapshot lifetime defect introduced with the transient
culture-victory city-targeting state (practical 5982 / db0d76c10e...). Base AdvCiv/K-Mod/BtS do not
contain these two SAS fields or the SAS hard process/culture-city gates that consume them.

ROOT
----
SAS practical 5982 introduced two transient per-city fields:

    m_iCultureVictoryRank
    m_iCultureVictoryInvestmentPercent

They are recomputed empire-wide only by CvPlayerAI::AI_updateCommerceWeights(). For an AI turn,
AI_doTurnPre runs that refresh before city production.

That is correct at the refresh point, but the stored values depend on mutable city culture/culture
rate. City production can change those inputs later in the same player turn.

A completed culture building follows:

    CvCity::popOrder(ORDER_CONSTRUCT)
        -> setNumRealBuilding(...)
            -> processBuilding(...)
                -> updates the building's actual city commerce/modifier effects
        -> ...
        -> if queue empty:
             CvCityAI::AI_chooseProduction()

There is no AI_updateCommerceWeights() between setNumRealBuilding/processBuilding and this immediate
AI_chooseProduction(). A complete current call inventory finds AI_updateCommerceWeights at normal
player AI pre-turn refresh, human slider handling, and Great Work handling; ordinary AI building
completion does not refresh it.

CLEAN CURRENT-SAS HERMITAGE FIXTURE
-----------------------------------
Hermitage is a shipped current building with +50% Culture commerce modifier.

Take an AI already at Culture-2 with the standard three required Legendary cities and at least four
cities. At the pre-turn AI_updateCommerceWeights pass, suppose the four projected Legendary
countdowns are:

    city 1 = 10
    city 2 = 20
    city 3 = 30
    city 4 = 39

City 4 is stored as:

    culture-victory rank       = 4
    victory investment percent = 0

Later in that city's city turn, it completes Hermitage. In a city whose projected culture output is
substantially modifier-driven, adding +50 percentage points can reduce the projected countdown from
roughly 39 to roughly 26. The fresh empire ordering is then:

    10, 20, 26, 30

City 4 is now rank 3. As the new required bottleneck candidate, the current balancing formula gives
it a positive investment percentage (indeed 100% when its countdown is the required-city
bottleneck). There is no divisibility/ranking invariant preventing this crossing; the rank list is
explicitly sorted from mutable projected city countdowns.

IMMEDIATE HARD CONSUMERS
------------------------
The stale value is consumed in the SAME CvCity::popOrder call.

1. AI_chooseProduction culture-city role

Current SAS requires:

    AI_atVictoryStage(CULTURE2)
    stored rank > 0
    stored rank <= required culture cities
    stored investment > 0

before setting bCultureCity (subject to the separate small-empire/site exception).

Choose the fixture with enough cities / no expansion exception so that this secondary condition is
inert. Fresh post-Hermitage state sets bCultureCity=true; current stale state rank=4/investment=0
sets it false.

bCultureCity is not cosmetic. It gates or scales numerous production branches, including generic
land/naval production-capacity logic, assault/naval/air/carrier choices and invader weighting.

2. AI_processValue culture-process permission

For an AI above first-ring culture and below the configured local-pressure threshold, current SAS
hard-skips production-to-Culture conversion unless:

    Culture-2 && rank in required set && investmentPercent > 0

Fresh post-Hermitage state permits the candidate's Culture process; current stale state suppresses
its culture conversion. AI_chooseProduction calls AI_bestProcess/AI_processValue during the
immediate next-order choice, so this is a direct same-turn consumer rather than merely next-turn
heuristic lag.

ANCESTRY
--------
Practical 5982 (`db0d76c10e5e...`) introduced the transient rank/investment fields, their
AI_updateCommerceWeights producer and the required-candidate culture-process gate. Later SAS work
also consumes the fields in the bCultureCity production role.

Base AdvCiv 1.14, K-Mod 1.46 and BtS/Civ4CE do not contain this SAS transient state, so F544 is
SAS-specific.

DEDUP / ROOT SEPARATION
-----------------------
WIP301 explicitly challenged these fields for SAVE/LOAD discontinuity and correctly closed that
question: normal AI turn start recomputes them before the ordinary AI decision path.

F544 is different. The pre-turn refresh has already happened correctly; then a city mutation changes
the ranking inputs before a same-turn hard consumer. Serialization changes do not fix this.

F544 is also distinct from:
- F498, which concerns K-Mod's m_aiConstructionValue cache surviving player policy/commerce changes;
- F519/F520, stored city safety/value state;
- F542/F543, route/Worker planning snapshots;
- KI#213, split city-vs-Worker best-build policy.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not merely serialize the fields; that addresses the wrong lifetime.

Robust options are:
1. mark the player-wide culture-targeting snapshot dirty whenever any event materially changes a
   city's culture, culture rate, culture modifier or city set, and refresh lazily before a hard
   rank/investment consumer; or
2. at minimum, refresh AI_updateCommerceWeights after an AI completes a building that can change
   the culture-ranking inputs and before immediate AI_chooseProduction.

The first approach is safer because buildings are not the only possible post-refresh mutation source.
Any repair must avoid recursive/unbounded refresh from within AI_updateCommerceWeights itself.

CURRENT STONE AFTER C031-WIP318
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F544 / KI#867 CONFIRMED.
F545 / provisional KI#868 UNUSED.
Latest physical checkpoint: C031-WIP318.
No repository source files modified.

NEXT
----
Continue under F545 unused by broadening F544 only for additional mutations of the SAME stored
culture-targeting snapshot (Great Work is already explicitly refreshed and is a useful positive
control). Then resume production-completion/event-boundary auditing for independent stored state:
- project/building completion changing empire/city strategy inputs before immediate next order;
- hard consumers that run before their next normal AI_doTurnPre / city AI refresh;
- dedup construction-value cache cases against F498 and culture-targeting cases against F544.

END C031-WIP318


================================================================================
C031-WIP319 — QUEUE 005 CONTINUATION — SWIFT-BORDER/HARD-GATE CONTROLS + F545
ZERO-DEFENDER COMPLETION / STALE EVACUATION SAFETY
================================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F545 / KI#868 are CONFIRMED.
F546 / provisional KI#869 is UNUSED.
No repository source files modified.

POST-WIP318 CONTROLS
--------------------
1. `AI_isSwiftBorderExpansion()` does not supply F545.

Current helper:

    return (GC.getNumCultureLevelInfos() >= 2 &&
        2 * iProductionTurns <=
        3 * GC.getGame().getCultureThreshold((CultureLevelTypes)2));

Practical 3242 / advc.192 extracted this from K-Mod's old inline first-border-pop heuristic and
intentionally relaxed the production-time cutoff from 1.0x to 1.5x. Current SAS culture level 2 is
the first real border-pop threshold (10 Culture on Normal), and shipped SAS keeps ConstructPercent
and CulturePercent equal on every game speed, so the comparison is a coherent rough timing heuristic
rather than an accidental unit mismatch.

Robustness note only: `getNumCultureLevelInfos() >= 2` is formally insufficient before indexing
entry 2; exactly two CultureLevel entries would be out of range. K-Mod already had the same latent
assumption and shipped SAS has seven entries. No live finding allocated.

2. Human production-governor experience-building wrong-variable branch was rediscovered and
immediately deduplicated to existing F525 / KI#848. Do not allocate another number.

3. `AI_bestBuildingThreshold()` prerequisite-limit arithmetic intentionally uses both target-class
and prerequisite-class counts. The target class count is passed negatively into
`getBuildingClassPrereqBuilding` to reduce the formula to the per-copy prerequisite requirement;
this is not a wrong-enum defect.

4. The nearby culture-victory `bHaveEnough` loop that breaks without setting the flag false is the
already-confirmed F533 / KI#856 practical-1808 lost-state regression. Do not duplicate it.

5. Negative-sentinel scan after F540 found no second live `-1`-as-best comparison in current
CvCityAI pathfinding/culture ETA consumers. In particular, `AI_calculatePlayerCloseness` only adopts
path distance when `iPathDistance > 0`.

6. `SAS_AI_directArtistCultureValuePercent()`'s one-entry per-turn cache was not reopened. The album
already records and rechecks its same-turn one-turn-lag behavior as an audit note; the established
consequence remains temporary overvaluation/delayed dampening rather than a new hard invariant root.

7. Construction/player-finance post-completion stale-state candidates continue to deduplicate to
F498 / F502 unless they expose a separate producer/consumer contract.

================================================================================
F545 / KI#868 - AI_updateSafety TRIES TO COUNT A DEFENDER FINISHING THIS TURN BUT
SKIPS THE ZERO-DEFENDER CASE; THE NEWLY COMPLETED DEFENDER CAN THEN OBEY STALE
EVACUATION STATE AND RETREAT OUT OF THE CITY IT WAS BUILT TO DEFEND
================================================================================

STATUS
------
CONFIRMED - inherited AdvCiv city-safety defect, born in practical 1687
`ef52c71628f14cf9751a2246ef520fce41617c28` ("Improvements to AI city safety, stack strength
evaluation."). Base AdvCiv 1.14 and current SAS retain the zero-defender exclusion. SAS's default
all-land-unit evacuation policy makes the direct unit-phase consequence especially sharp.

ROOT
----
`CvCityAI::AI_updateSafety()` explicitly tries to account for a combat unit that will finish at the
end of the current turn:

    iDefenders = getPlot().getNumDefenders(getOwner());

    // Check if we'll finish a defender at the end of our turn
    if (iDefenders > 0 && isProductionUnit() && getProductionTurnsLeft() == 1)
    {
        CvUnitInfo const& kProductionUnit = GC.getInfo(getProductionUnit());
        if (kProductionUnit.getCombat() > 0)
            iDefStrength = (iDefStrength * (iDefenders + 1)) / iDefenders;
    }

The `iDefenders > 0` guard is needed only by that averaging formula's division. Semantically it
excludes precisely the case in which the incoming defender matters most: a city with ZERO current
defenders.

In that state the completing combat unit contributes neither to `iDefStrength` nor to the
one-turn-doom classification. The subsequent evacuation test still uses the zero current-defender
snapshot:

    if (iAttackers + 1 >= iDefenders)
        bEvac = (scaled(iAttStrength, iDefStrength + 1) > rThresh);

With no current local defense, any ordinary adjacent attack force readily makes the city look doomed.

TURN ORDER MAKES THE OMITTED DEFENDER REAL BEFORE THE SAFETY STATE IS CONSUMED
-------------------------------------------------------------------------------
This is not a forecast whose unit might fail to appear.

At player activation, before the player's normal turn work:

    CvPlayer::setTurnActive
        -> AI().AI_updateCacheData()
            -> every city AI_updateSafety()

Only afterward does the player turn run. Each city performs:

    CvCity::doTurn
        -> AI_doTurn()
        -> doGrowth()
        -> doCulture()/doPlotCultureTimes100()
        -> doProduction()

Then, after the player's city turn work, `CvPlayer::setTurnActive` calls:

    doTurnUnits()

Therefore a city whose combat unit has `getProductionTurnsLeft()==1` when safety is computed really
can complete that unit in `doProduction()` BEFORE the new unit's unit-AI phase.

CLEAN FIXTURE
-------------
Take an AI city with:

- zero current defenders;
- one visible hostile land attacker close enough to enter the city-safety attack-strength query;
- a strong ordinary combat defender (e.g. Longbow-class city defender) at exactly 1 production turn
  remaining;
- no nearby friendly defenders contributing enough defense to mask the case.

At activation-time `AI_updateSafety`:

    iDefenders = 0

The explicit finishing-defender block is skipped solely because `iDefenders > 0` is false. The city
can consequently become `CITYSAFETY_EVACUATING` from the zero-defense strength/doom calculation.

Later in the SAME player turn, `doProduction()` creates the defender. A fresh safety calculation at
this point sees the actual defender; choose the produced defender / enemy-strength fixture so that
its real local defense changes the city from Evacuating to Threatened or Safe (a strong defender
versus a weak nearby attacker makes this straightforward).

Current code performs no general post-production safety refresh, so `m_eSafety` remains the stale
pre-production EVACUATING value through the immediately following unit phase.

DIRECT CURRENT-SAS CONSEQUENCE
------------------------------
`CvUnitAI::AI_update()` checks evacuation before ordinary UnitAI movement for the principal land
combat roles, including:

    UNITAI_ATTACK
    UNITAI_ATTACK_CITY
    UNITAI_COLLATERAL
    UNITAI_PILLAGE
    UNITAI_RESERVE
    UNITAI_COUNTER
    UNITAI_CITY_DEFENSE
    UNITAI_CITY_COUNTER
    UNITAI_CITY_SPECIAL

and calls `AI_evacuateCity()` when the unit stands in its own city.

Current SAS ships:

    SAS_AI_EVACUATE_CITY_ALL_LAND_UNITS_WHEN_DOOMED_ENABLE = 1

Under that default, an eligible unit in a city still marked EVACUATING immediately attempts
`AI_retreatToCity()` rather than using Base AdvCiv's probabilistic leave/stay choice.

So the newly completed defender can be born in an allegedly doomed undefended city, enter its unit
AI later in the same activation, read the obsolete zero-defender safety classification, and attempt
to LEAVE the city it has just made defensible.

This is a direct gameplay consequence, not merely a pessimistic score. The same stale evacuation
state also rejects/redirects reinforcement, retreat, route and other city-target decisions, but the
new defender evacuating itself is sufficient as the clean witness.

ANCESTRY
--------
Practical 1687:

    ef52c71628f14cf9751a2246ef520fce41617c28
    "Improvements to AI city safety, stack strength evaluation."

introduced the prospective-production adjustment together with the exact guard:

    if (iDefenders > 0 && isProductionUnit() && getProductionTurnsLeft() == 1)

and the comment:

    // Check if we'll finish a defender at the end of our turn

Later AdvCiv refactors retain it unchanged apart from accessor/type/style updates. Base AdvCiv 1.14
and current SAS still contain the same zero-defender exclusion.

The defect is therefore not a later SAS customization, although SAS's enabled all-land-unit
forced-evacuation policy amplifies its current consequence.

DEDUP / ROOT SEPARATION
-----------------------
F545 is distinct from:

- F519/F520 and KI#172-related city-safety/value findings, which concern different safety/value
  inputs or percentile logic;
- F526 culture/floating-defender cache behavior;
- F538/F539/F543 Worker counting/snapshot defects;
- F544 culture-victory role snapshot;
- the already-audited `AI_updateSafety` / local-strength assertion and border-danger families.

The unique root here is the explicit prospective defender adjustment being undefined for zero
existing defenders and therefore omitted exactly at the production-to-unit-phase boundary.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Avoid inventing an approximate zero-defender average merely to bypass the divide-by-zero.

The cleanest repair is to refresh city safety after a combat defender actually completes, before
its same-turn unit AI can consume the cached state. This uses the real created unit, promotions,
city modifiers and current local-defense computation instead of trying to synthesize an average from
zero existing defenders.

For performance, the refresh can be restricted to AI cities whose previous safety is not already
safe/perfect and to completion of a land combat unit that can materially defend the city. A more
general event-dirty safety model would also work if maintained consistently.

If retaining the predictive pre-production optimization, its zero-defender branch must use a
well-defined defense estimate rather than silently pretending that the finishing unit does not
exist.

CURRENT STONE AFTER C031-WIP319
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F545 / KI#868 CONFIRMED.
F546 / provisional KI#869 UNUSED.
Latest physical checkpoint: C031-WIP319.
No repository source files modified.

NEXT
----
Continue under F546 unused:
1. finish the history-wide removed-assignment / changed-target semantic sieve, excluding F533 and
   already-repaired historical regressions;
2. finish the current production/event-boundary safety-state family, folding same-root stale-safety
   mutations into F545 rather than allocating siblings;
3. inspect other hard cached states consumed in `doTurnUnits()` after city production/culture
   mutation, deduplicating F498/F502/F542/F543/F544/F545;
4. promote F546 only for an independent present-day root with a concrete consumer.

END C031-WIP319


================================================================================================
C031-WIP320 CONTINUATION - F545 SIBLING BOUNDED; F546 / KI#869 CONFIRMED
================================================================================================

This checkpoint continues C031-WIP319 without modifying repository source. It preserves the
post-WIP319 safety-family work that had not yet been written into the album.

------------------------------------------------------------------------------------------------
F545 / KI#868 SAME-ROOT BROADENING - PROSPECTIVE NAVAL UNIT CAN BE INVENTED AS LAND DEFENSE
------------------------------------------------------------------------------------------------

F545's prospective-production branch has a second current defect under the SAME producer and repair
boundary, so it does not receive a new F-number.

Current `AI_updateSafety` uses:

    if (iDefenders > 0 && isProductionUnit() && getProductionTurnsLeft() == 1)
    {
        CvUnitInfo const& kProductionUnit = GC.getInfo(getProductionUnit());
        if (kProductionUnit.getCombat() > 0) // Assume an average defender
            iDefStrength = (iDefStrength * (iDefenders + 1)) / iDefenders;
    }

The local defense being forecast immediately above is explicitly LAND defense:

    AI_localDefenceStrength(..., DOMAIN_LAND, ...)

but the production test asks only whether the completing unit has positive combat strength. It does
not require `DOMAIN_LAND`, city-defense applicability or even that the completed unit can defend the
city plot as a land defender.

Current shipped SAS provides ordinary witnesses such as Galley and Destroyer: they have positive
`iCombat` but `DOMAIN_SEA`. In a coastal city with at least one current land defender, finishing such
a ship therefore multiplies the LAND-defense forecast as though one additional average land defender
were about to appear.

For one current land defender of effective strength S, the branch changes the forecast from S to 2S.
Choose a nearby enemy land attack strength A such that:

    S / 2 <= A < S

Without the phantom ship:

    2A >= S       -> not Safe

With the current production predictor:

    2A < 2S       -> Safe

So the same F545 branch is bidirectionally wrong:

- with zero current defenders it omits a real completing land defender;
- with one or more current defenders it can invent a phantom land defender from a completing naval
  combat unit.

A post-production refresh using the real completed unit fixes both errors naturally. If the predictive
branch is retained, it must at minimum test that the production unit belongs to the same defensive
domain / can materially contribute to the LAND-defense quantity being forecast.

CONTROL - CURRENT DEFENDER COUNT DOES NOT COUNT ORDINARY DOCKED SHIPS
----------------------------------------------------------------------
A nearby suspicion closes cleanly and is recorded to prevent rediscovery. `iDefenders` is obtained by
`getPlot().getNumDefenders(getOwner())`, whose `PUF_canDefend` ultimately calls `CvUnit::canDefend`.
`canDefend` enforces valid domain for the plot. A normal city plot is land, so ordinary DOMAIN_SEA and
DOMAIN_AIR units do not inflate this count merely by being stationed in the city. The F545 naval bug
therefore belongs specifically to the prospective-production `getCombat() > 0` test above, not to the
current defender count.

------------------------------------------------------------------------------------------------
F546 / KI#869 - THE SOLE-CITY SHORTCUT CONFLATES "DO NOT EVACUATE THE LAST CITY" WITH "CITY IS SAFE"
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- inherited AdvCiv `advc.139` safety-model defect, retained by Base AdvCiv 1.14 and current
AdvCiv-SAS. Distinct from F519/F520/F545 because the refresh DOES run; its producer deliberately
returns the wrong generalized state for a one-city empire.

ROOT
----
Current `CvCityAI::AI_updateSafety` begins:

    m_eSafety = CITYSAFETY_SAFE;
    CvPlayerAI const& kOwner = GET_PLAYER(getOwner());
    if (kOwner.getNumCities() <= 1)
        return;

Thus a player's sole city is unconditionally `CITYSAFETY_SAFE`. No attacker count, local attack
strength, local defense strength, wider-range attack or threatened/evacuating classification is ever
computed.

The historical origin explains how this happened.

Before `isSafe` existed, `advc.139` had an evacuation-only routine. Its early return for
`getNumCities() <= 1` was sensible: do not evacuate the empire's last city because there is no other
city to preserve instead.

Practical 1376 / `525e919195916c8e061678b29cf21817eb9f07b8` expanded that routine into the general
`updateSafety` / `isSafe` model. The same change introduced:

    bSafe = true;
    if (o.getNumCities() <= 1)
        return;

while also computing `bSafe` from hostile attack versus defense for every multi-city empire and
adding new callers that treat `isSafe` as a general tactical predicate. Later enum conversion merely
preserved the behavior as `CITYSAFETY_SAFE`; it did not create the root.

So an old rule with the meaning:

    "never set EVACUATING for the last city"

was carried into a broader state variable where it now means:

    "the last city is tactically SAFE."

Those statements are not equivalent. A last city can be THREATENED even though evacuation is not a
valid strategic response.

Practical 1687 / `ef52c71628f14cf9751a2246ef520fce41617c28` later made the consequence especially
explicit by replacing Great-Person target checks with hard `AI_isSafe()` gates and expanding other
city-safety consumers.

CLEAN CURRENT-SAS WITNESS
-------------------------
Use an AI that owns exactly one city. Put an enemy land stack adjacent to that city and make the
strengths satisfy the normal multi-city unsafe condition, e.g.:

    iAttStrength * 2 >= iDefStrength

with the attack close enough for the range-1 `AI_localAttackStrength` query.

A mechanically identical city owned by a two-city AI reaches the normal calculation and becomes
THREATENED or EVACUATING according to the later threshold logic.

The one-city AI instead returns before reading any attacker and remains:

    AI_isSafe() == true

regardless of how overwhelming the adjacent stack is.

DIRECT STATE-CHANGING CONSUMER - GREAT PERSON
---------------------------------------------
Current `CvUnitAI::AI_greatPersonMove` hard-filters every slow city action with:

    FOR_EACH_CITYAI(pLoopCity, kOwner)
    {
        if (!pLoopCity->AI_isSafe())
            continue;
        ...
        // Join / Construct / Hurry
    }

Thus a Great Person already in the besieged sole city may retain Join, Construct or Hurry as normal
candidate actions even though the exact same tactical strengths make the city ineligible as soon as
the empire owns a second city.

This is not merely a preference about whether a Great Person should physically flee. The safety
predicate was added precisely to prevent permanent slow-action investment in unsafe cities. The sole-
city shortcut bypasses that protection before the action-value comparison even begins.

Current SAS Great-General handling makes the contract even more explicit. `AI_join` rejects a city
when `!AI_isSafe()`, and SAS deliberately permits an immediate Military-Instructor join despite
radius-2 danger only when the General is already in an `AI_isSafe` city. A besieged sole capital
therefore receives the special "safe city" permission solely because the empire has no second city,
not because the threat calculation says the city is safe.

OTHER CURRENT CONSUMERS
-----------------------
The false Safe state also reaches broader tactical code. For example, non-land
`AI_isThreatenedFromLand()` returns `NO_PROBABILITY` immediately for an undamaged unit standing in an
`AI_isSafe` city. Retreat/upgrade/city valuation and other consumers likewise distinguish Safe from
Threatened independently of whether evacuation itself is permitted.

These consumers reinforce the semantic mismatch, but the permanent Great-Person action gate is
already sufficient as the concrete state-changing witness.

ANCESTRY
--------
The exact conceptual regression is visible in practical 1376:

old evacuation-only model:

    bEvacuate = false;
    if (o.getNumCities() <= 1)
        return;

new generalized safety model:

    bEvacuate = false;
    bSafe = true;
    if (o.getNumCities() <= 1)
        return;
    ...
    bSafe = (attStr * 2 < defStr);

The early return was retained while the meaning of the cached state widened from evacuation-only to
Safe/unsafe. Practical 1687 then added the direct Great-Person safety gate and other hard consumers.
Base AdvCiv and current SAS preserve the unconditional sole-city return.

DEDUP / ROOT SEPARATION
-----------------------
F546 is distinct from:

- F519: a newly founded city retains constructor-default Safe because it has not yet received its
  first safety computation;
- F520: an existing city's previously correct safety becomes stale after tactical unit-state changes
  because invalidation/refresh is incomplete;
- F545: `AI_updateSafety` does perform the strength calculation but its prospective completing-unit
  adjustment uses the wrong defender model;
- the evacuation-policy itself: F546 does NOT propose evacuating the last city.

The F546 producer is the `numCities <= 1` early return inside a real safety refresh. The repair is
therefore different from initialization, invalidation or production-completion refresh fixes.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not return `CITYSAFETY_SAFE` merely because the player has one city.

Instead, run the ordinary attack/defense classification and allow the sole city to become
`CITYSAFETY_THREATENED` when tactically unsafe, while separately suppressing only the
`CITYSAFETY_EVACUATING` outcome for a one-city empire.

Conceptually:

    compute Safe vs unsafe normally;
    if unsafe:
        if more than one city and evacuation threshold is met:
            EVACUATING
        else:
            THREATENED

That preserves the original strategic intent -- never abandon the last city -- without lying to all
other `AI_isSafe()` consumers about the tactical state.

CURRENT STONE AFTER C031-WIP320
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F546 / KI#869 CONFIRMED.
F547 / provisional KI#870 UNUSED.
Latest physical checkpoint: C031-WIP320.
No repository source files modified.

NEXT
----
Continue under F547 unused:
1. finish the remaining `AI_updateSafety` contract/domain checks, folding prospective-production
   siblings into F545 and sole-city semantic effects into F546 rather than splitting them;
2. resume the history-wide removed-assignment / changed-target sieve after excluding known F533 and
   repaired historical regressions;
3. continue hard post-city-turn -> unit-phase cache/decision boundaries not already owned by
   F498/F502/F519/F520/F542/F543/F544/F545/F546;
4. promote F547 only for a separate present-day root with a concrete live consumer.

END C031-WIP320


================================================================================================
C031-WIP321 CONTINUATION - SPECIAL-YIELD ORDERING CONTROL; F547 / KI#870 CONFIRMED
================================================================================================

This checkpoint continues C031-WIP320 without modifying repository source. It preserves the
post-WIP320 producer/consumer ordering check and the newly confirmed prerequisite-building valuation
root.

------------------------------------------------------------------------------------------------
CONTROL - `m_aiSpecialYieldMultiplier` IS REFRESHED BEFORE `AI_doEmphasize` CONSUMES IT
------------------------------------------------------------------------------------------------

The WIP320 continuation cursor challenged whether the cached special-yield multipliers could be stale
when `AI_doEmphasize()` reads them.

The normal AI turn ordering closes this concern:

    CvPlayerAI::AI_doTurnPre(...)
    ...
    CvPlayer::AI_assignWorkingPlots()
        -> each city AI_assignWorkingPlots()
            -> AI_updateSpecialYieldMultiplier()
    ...
    CvCity::doTurn()
        -> CvCityAI::AI_doTurn()
            -> AI_doEmphasize()

Thus the multipliers are refreshed before the normal city-AI emphasis consumer in each activation.
Moreover, changing an emphasis through `AI_setEmphasize()` calls `AI_assignWorkingPlots()` again, so
an emphasis-state mutation also triggers a new multiplier calculation rather than leaving the old
value in place.

No independent stale-cache F547 exists here.

------------------------------------------------------------------------------------------------
F547 / KI#870 - PREREQUISITE-BUILDING VALUE CAN REWARD AN IMPOSSIBLE NATIONAL-WONDER UNLOCK
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- inherited K-Mod prerequisite-building valuation defect. K-Mod itself later left an
explicit source comment acknowledging the national-wonder-limit flaw, and current AdvCiv/SAS still
retain both the permissive visible-only test and shipped XML witnesses.

ROOT
----
`CvCityAI::AI_buildingValue()` adds extra value to a building when constructing it would enable a
valuable later building. Before valuing the later building, current code deliberately uses
`bTestVisible=true` because the prerequisite building itself must be ignored while asking whether the
future target could eventually appear in the production list.

Current civ-wide target-city loop:

    if (pLoopCity->getProductionBuilding() != eLoopBuilding &&
        pLoopCity->canConstruct(eLoopBuilding, false, true))
    {
        iHighestValue = std::max(
            pLoopCity->AI_buildingValue(eLoopBuilding, 0, 0,
                bConstCache, false),
            iHighestValue);
    }

The adjacent AdvCiv comment accurately states the contract:

    bVisible=true means: check only if the building appears in the production list.
    In particular, prereq buildings aren't checked, which is good. However, the
    national wonder limit also isn't checked, which is bad.

`CvCity::canConstruct()` confirms why. The per-city world/team/national-wonder-limit checks are all
inside:

    if (!bTestVisible)
    {
        ...
        if (kBuildingClass.isNationalWonder())
        {
            if (isNationalWondersMaxed())
                return false;
        }
        ...
    }

So `canConstruct(..., false, true)` can call a national wonder a valid future target even when that
city has no national-wonder slot left.

The same problem exists even more directly in the LOCAL prerequisite branch. That branch first uses
only the player-level visible construction test for the future building, and then, when the current
city lacks the required local building, directly evaluates:

    AI_buildingValue(eLoopBuilding, ..., bAllowRecursion=false)

without a normal `CvCity::canConstruct(eLoopBuilding)` check for the current city.

CLEAN CURRENT-SAS SHIPPED-DATA WITNESS - BARRACKS -> HEROIC EPIC
-----------------------------------------------------------------
Current SAS retains:

    BUILDING_HEROIC_EPIC
        class: BUILDINGCLASS_HEROIC_EPIC
        iMaxPlayerInstances = 1       // national wonder
        iMilitaryProductionModifier = 50
        BuildingClassNeeded in city:
            BUILDINGCLASS_BARRACKS

SAS does not override the stock BtS `MAX_NATIONAL_WONDERS_PER_CITY`; Civ4CE's authoritative BtS XML
has that value as 2 (OCC uses 5). Current `CvCity::getNumNationalWondersLeft()` therefore enforces the
normal inherited two-slot city limit for an AI.

Construct the following ordinary state:

1. The AI otherwise satisfies the player/team requirements for Heroic Epic.
2. A city already contains two national wonders, so `isNationalWondersMaxed()` is true.
3. The city does not yet have a Barracks.
4. The city is not in the SAS danger state that would independently zero Heroic Epic's military-
   production value, and it has met another civilization so the ordinary +50% military-production
   valuation is active.

A real `canConstruct(BUILDING_HEROIC_EPIC)` call is false in that city because the national-wonder
slots are exhausted.

But while valuing Barracks, the prerequisite code sees Heroic Epic as a relevant future building,
passes the visible/player-level checks, and the local-prerequisite branch evaluates Heroic Epic
anyway. Heroic Epic has a plainly positive +50% military-production contribution in an appropriate
production city, and its military-production modifier also excludes it from the later generic
capital/low-impact national-wonder slot penalty that can zero some other national wonders at zero
slots.

The resulting positive Heroic-Epic value is scaled and ADDED to the Barracks value even though this
city can never use that Barracks to unlock Heroic Epic.

This is a deterministic score error, not merely a UI visibility discrepancy: the impossible future
building raises `AI_buildingValue(BUILDING_BARRACKS)` and can therefore change the building ranking
and production choice.

CURRENT SAS HAS ADDITIONAL SHIPPED WITNESSES
---------------------------------------------
A current XML scan shows national wonders with prerequisite-building relations still include:

- Forbidden Palace <- Courthouse (3 empire-wide; Courthouse also required locally);
- Heroic Epic <- Barracks (local);
- National Epic <- Library (local);
- Wall Street <- Bank (4 empire-wide; Bank also required locally);
- Iron Works <- Forge (3 empire-wide; Forge also required locally).

For the empire-wide cases, the later `iHighestValue` loop can likewise choose a city whose national-
wonder slots are already full as the supposedly best future location. Its impossible target value can
then inflate the prerequisite building's value empire-wide. The Heroic-Epic local fixture is cleaner
because no competing target-city selection is needed.

Oxford University is NOT used as the current SAS witness: SAS has changed Oxford from a national
wonder to a world wonder. This data cross-check avoids blindly carrying K-Mod's historical example
into the modified SAS ruleset.

ANCESTRY
--------
K-Mod practical 549 / `4defd11f3c...` introduced the local prerequisite-building value boost,
explicitly describing examples such as a prerequisite building gaining value because it enables a
later building. The code used permissive constructibility while assessing the future target.

By practical 556 / `0a31ba2ac1...`, K-Mod added the explicit comment beside this logic:

    "the above call to canConstruct will return true even if the city already has the
     maximum number of national wonders. This is a minor flaw in the AI."

AdvCiv retained the model and later added a second explanatory comment at the target-city
`bVisible=true` call. Base AdvCiv 1.14 and current SAS retain the behavior.

DEDUP / ROOT SEPARATION
-----------------------
F547 is distinct from:

- F498: stale construction-value CACHE reuse after mutable same-turn state changes. F547 produces the
  wrong fresh value even with every cache cleared.
- F533: a lost `bHaveEnough=false` assignment in Culture-victory prerequisite-count logic. F547 is a
  constructibility-domain omission in the general prerequisite-unlock valuation.
- ordinary national-wonder placement heuristics: F547 occurs while valuing the prerequisite building,
  before the future national wonder is actually selected for construction.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The prerequisite-building count itself must remain ignored while evaluating a future target; simply
changing every `bTestVisible=true` call to a full `canConstruct()` would incorrectly reject exactly the
future building whose missing prerequisite is being valued.

Instead, preserve the intentional prerequisite look-ahead while separately enforcing constraints that
cannot be solved by building `eBuilding`, especially the target city's exhausted national-wonder
slots. For example, exclude a national-wonder target city when `isNationalWondersMaxed()` is true,
while still using the visible-only construction test for prerequisite-building availability.

The local prerequisite branch needs the same city-level impossible-target filter before adding its
future-building value.

CURRENT STONE AFTER C031-WIP321
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F547 / KI#870 CONFIRMED.
F548 / provisional KI#871 UNUSED.
Latest physical checkpoint: C031-WIP321.
No repository source files modified.

NEXT
----
Continue under F548 unused:
1. finish the ranked historical lost-assignment / changed-target sieve after excluding F522/F533 and
   known repaired history;
2. continue current `AI_buildingValue` hard-gate/visible-only checks only where a different permanent
   constraint is skipped; fold same impossible-target prerequisite effects into F547;
3. resume post-completion and post-city-turn hard-consumer boundaries not already owned by
   F498/F502/F519/F520/F542-F547;
4. promote F548 only for a separate current root with a concrete live consequence.

END C031-WIP321


================================================================================
C031-WIP322 HANDOFF -- Queue 005 CvCityAI.cpp continued
F446-F548 confirmed; F549 / KI#872 UNUSED
Repository source untouched.
================================================================================

CONTINUATION FROM WIP321

This checkpoint preserves the post-WIP321 resource-area contract audit. The current
repository source has not been modified.

------------------------------------------------------------------------------------------------
F548 / KI#871 - AI_countOwnedBonuses completely misses owned city-radius land resources
                 on a different CvArea from their working city
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED -- inherited K-Mod/BBAI city-radius resource-count omission retained by
Base AdvCiv and current SAS. One physical owned land resource can disappear from the
AI's literal owned-resource count solely because the resource is in a different land
CvArea while still lying inside and belonging to a city's workable radius.

ROOT
----
Current `CvPlayerAI::AI_countOwnedBonuses` divides the map into two counting paths.

First it counts owned resources OUTSIDE every city radius:

    if (kPlot.getOwner() == getID() && !kPlot.isCityRadius() &&
        kPlot.getBonusType(getTeam()) == eBonus)
    {
        iCount++;
    }

Then resources inside city radii are obtained through each city's
`AI_countNumBonuses` (plus the current SAS plot-unique neutral anticipation added for
F343/KI#666).

For an owned city-radius resource, `AI_countNumBonuses` begins with this geographic
filter:

    if (!((bLand && isArea(kPlot.getArea())) ||
        (bWater && kPlot.isWater())))
    {
        continue;
    }

The default player-level call requests both land and water, but `bLand` means
"land in THIS CITY'S CvArea", not "any land plot in this city's workable radius".
Therefore an owned land plot in a separate land area is rejected before the normal:

    kPlot.getOwner() == eOwner && kPlot.getWorkingCity() == this

owned-resource branch can count it.

The plot then cannot be recovered by the player-level whole-map pass because the plot
is still `isCityRadius() == true`.

Thus the two halves form an actual gap:

    owned + city radius + land + different CvArea
        -> skipped by outside-radius map pass
        -> skipped by city helper's same-area filter
        -> counted nowhere

CITY / PLOT CONTRACT PROOF
--------------------------
This is not an impossible map state.

`CvCity::canWork(CvPlot const&)` has no same-land-area requirement. Once the plot's
working city is this city, it checks siege, water-working/blockade rules for water,
and whether the plot has yield; it does not reject a land plot merely because
`kPlot.getArea()` differs from `getArea()`.

Likewise, `CvPlot::defaultWorkingCity()` chooses among `NearbyCityIter` cities owned by
the plot owner according to city-plot priority; it contains no same-area filter.

Finally, `CvPlot::setPlotCity()` establishes the city-radius flag by iterating every
`CityPlotIter` plot and incrementing `changeCityRadiusCount(1)`, again without any
area restriction.

So a mainland city can legitimately own and work a land resource on a nearby island
inside its BFC while that island remains a distinct land CvArea.

CLEAN CURRENT-SAS SHIPPED-DATA FIXTURE - URANIUM / FISSION
----------------------------------------------------------
Current SAS supplies a particularly clean tech-value witness:

    BONUS_URANIUM
        TechReveal    = TECH_RADIO
        TechCityTrade = TECH_FISSION
        iMinAreaSize  = 3

and current `TECH_FISSION` has `TECH_RADIO` as a prerequisite.

Construct an ordinary geometry:

1. An AI owns a mainland coastal city.
2. A separate land island of at least three tiles lies partly inside that city's
   workable radius (for example across a one-tile water gap), so it is a distinct
   land CvArea while satisfying Uranium's normal minimum-area placement rule.
3. The city's culture owns the island Uranium tile and the plot's working city is the
   mainland city.
4. This is the AI's only Uranium.
5. The AI has Radio and is evaluating Fission.

The Uranium is already revealed and physically owned.

Correct physical count:

    AI_countOwnedBonuses(BONUS_URANIUM) = 1

Current count:

    whole-map path: skipped because kPlot.isCityRadius()
    city path:      skipped because !isArea(kPlot.getArea()) and !kPlot.isWater()
    result:         0

`CvPlayerAI::AI_techValue` explicitly treats the helper as the owned physical count.
For a revealed resource whose `TechCityTrade` is the tech under evaluation it does:

    int iOwned = AI_countOwnedBonuses(eLoopBonus, 2);
    if (iOwned > 0)
    {
        int iEnableValue = 4;
        iEnableValue += AI_bonusVal(eLoopBonus, 1, true) * iCityCount;
        iEnableValue *= (iOwned > 1) ? 120 : 100;
        iEnableValue /= 100;
        iValue += iEnableValue;
    }

With the off-area city-radius Uranium, current code takes `iOwned == 0` and awards no
Uranium-enabling value to Fission. A correct count of 1 enters the branch and adds the
full one-copy enabling value.

This can therefore change research ranking; it is not merely a reporting/count API
blemish.

ADDITIONAL LIVE CONSEQUENCES
----------------------------
The same helper is used as literal resource multiplicity by global corporation value.
Current corporations consume land resources including Iron, Copper, Stone, Marble,
Aluminum, Coal and Uranium. An owned off-area city-radius copy of one of those
resources is omitted from `AI_corporationValue(..., pCity == NULL)`, changing the
estimated corporation yield/commerce and maintenance.

The Fission fixture is sufficient by itself; the corporation path just confirms that
this helper's contract truly is physical owned-resource multiplicity.

ANCESTRY
--------
Vanilla BtS, as reconstructed by current Civ4CE, does NOT have this omission in
`CvPlayer::countOwnedBonuses`: it simply scans every map plot and counts every owned
plot with the requested bonus, regardless of city radius or land area.

The earliest reachable K-Mod source already contains the changed architecture. K-Mod
practical 3 / `2414225e64...` is the initial K-Mod commit; its
`CvPlayer::countOwnedBonuses` counts owned bonuses outside city radii and delegates
inside-radius/easily-claimed bonuses to `CvCityAI::AI_countNumBonuses`.
The same initial K-Mod `AI_countNumBonuses` admits only:

    pLoopPlot->area() == area()
        OR (bWater && pLoopPlot->isWater())

so different-land-area BFC resources are already omitted there. The exact pre-K-Mod
origin may lie in the Better BTS AI baseline imported by that initial tree; the
reachable history establishes the defect from K-Mod's first commit onward.

AdvCiv practical 1618 / `0de04b1e45...` later moved `countOwnedBonuses` into
`CvPlayerAI` under `advc.042` explicitly "w/o functional changes". Base AdvCiv retains
the same city helper filter (with later `bLand` cleanup), including the source TODO:

    advc.040 (tbd.): Param for including resources in different land areas?

Current SAS retains that filter. The later F343/KI#666 repair makes neutral
city-radius anticipation plot-unique but intentionally leaves the owned geographic
filter untouched, so F548 remains present in the supplied source.

DEDUP / ROOT SEPARATION
-----------------------
F548 is distinct from F343/KI#666 even though both reach
`AI_countOwnedBonuses`:

- F343 OVERCOUNTS one UNOWNED neutral physical resource once per overlapping city
  radius. Its repair deduplicates neutral plots at player scope.
- F548 UNDERCOUNTS one already-OWNED land resource to zero when it is inside a city
  radius but belongs to a different land CvArea.

They have opposite count errors, different eligibility predicates, different fixtures,
and different minimum repairs. F343's current visited-neutral-plot set does nothing
for F548's already-owned island tile.

F548 also does not depend on Worker pathing or `AI_countNumImprovableBonuses`; the
clean Fission consequence is a player-level resource-count / tech-value error even if
no Worker ever attempts to improve the island.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not blindly remove the same-area restriction from `AI_countNumBonuses` globally.
Some city-local callers use the helper for Worker/production heuristics where land-area
reachability can be intentional.

For `AI_countOwnedBonuses`, owned physical resources should instead be counted at
player scope independently of the city-local Worker geography. The robust structure is:

1. Count every REVEALED owned physical bonus plot exactly once map-wide, regardless of
   city-radius or land-area membership.
2. Separately add the existing "easily claimed" neutral city-radius anticipation,
   with the F343 plot-unique guard so one neutral physical plot is added at most once.
3. Preserve the current Advanced Start special handling / early cap (`iMaxCount`) as
   appropriate.

That simultaneously matches the helper's "actual count" consumers and avoids forcing
city-local Worker heuristics to treat transport-separated island land as locally
reachable.

POST-F548 CONTROLS / CONTINUATION
---------------------------------
The neighboring `AI_countNumImprovableBonuses` same-area filter has NOT been promoted
as a second defect. Its callers are Worker-production / local-improvement heuristics,
where excluding a land resource on another CvArea can be deliberate because an
ordinary local land Worker cannot walk there. F548 relies only on the physical owned-
resource contract, where the area exclusion is demonstrably wrong.

CURRENT STONE AFTER C031-WIP322
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F548 / KI#871 CONFIRMED.
F549 / provisional KI#872 UNUSED.
Latest physical checkpoint: C031-WIP322.
No repository source files modified.

NEXT
----
Continue under F549 unused:
1. finish the city bonus/resource helper family, but promote only if another caller has
   a hard contract different from F548's physical-count root;
2. resume current mixed-domain count/ratio and hard-reject checks;
3. continue ranked historical lost-assignment / changed-target candidates after
   excluding F522/F533 and repaired history;
4. continue post-city-turn / completion-boundary state consumers not already owned by
   F498/F502/F519/F520/F542-F548;
5. freeze a control checkpoint rather than force F549 if the remaining seams saturate.

END C031-WIP322


================================================================================
C031-WIP323 APPEND - F549 / KI#872 CONFIRMED
================================================================================

F549 / KI#872 - SAS AI_bestUnit war-mode boolean makes the defense branch unreachable
in every ordinary real war, so even when the enemy exceeds the configured strong-power
threshold the generic unit picker takes offense mode first and can zero dedicated city
defender roles.

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
SAS-specific; introduced by SAS practical 4932 / `6a226d0fba...`.
Current source untouched.

ROOT
----
Current `CvCityAI::AI_bestUnit()` computes:

    bool const bAnyRealWar = (kTeam.getNumWars(false, true) > 0);
    bool const bAnyPlannedWar = kTeam.AI_isAnyChosenWar();
    int const iEnemyPowerPercent = kTeam.AI_getEnemyPowerPercent(true);

    bool const bDefenseMode = (((iEnemyPowerPercent >= iDefenseModeThreshold) ||
            bAnyPlannedWar || bAnyRealWar || bDanger || bDefense) &&
            !bPeaceAloneLikely);

    bool const bOffenseMode = (((bAnyRealWar &&
            iEnemyPowerPercent <= iOffenseModeThreshold) ||
            bWarPlan || bAnyPlannedWar || bAnyRealWar || bAssault ||
            (!bDefense && bLandWar)) && !bPeaceAloneLikely);

and then chooses the strategy with:

    if (bOffenseMode)
    {
        ...
        aiUnitAIVal[UNITAI_CITY_DEFENSE] = 0;
        aiUnitAIVal[UNITAI_CITY_SPECIAL] = 0;
    }
    else if (bDefenseMode)
    {
        ... strongly increases CITY_DEFENSE / CITY_SPECIAL / RESERVE ...
    }

The `|| bAnyRealWar` near the end of `bOffenseMode` makes the earlier

    (bAnyRealWar && iEnemyPowerPercent <= iOffenseModeThreshold)

power comparison irrelevant in every real war. The same real-war term also makes
`bDefenseMode` true, but offense is tested first, so the defense branch cannot run.

CURRENT SHIPPED-DATA FIXTURE
----------------------------
Current XML defaults are:

    SAS_ENEMY_STRONG_POWER_THRESHOLD = 120
    SAS_ENEMY_WEAK_POWER_THRESHOLD   = 80

Take an ordinary land war in a non-isolated area (`bPeaceAloneLikely == false`) with
current enemy-power percentage 150.

The intended threshold classification is unambiguous:

    150 >= 120  -> defense threshold crossed
    150 <= 80   -> false; enemy is not weak

Current booleans nevertheless become:

    bDefenseMode = true
        because 150 >= 120 (and independently because bAnyRealWar == true)

    bOffenseMode = true
        because the later unconditional `|| bAnyRealWar` is true

Control flow therefore enters offense mode and never reaches defense mode.

That is not a soft score tie. The offense block explicitly removes the two most
city-anchored defensive roles from the generic choice:

    aiUnitAIVal[UNITAI_CITY_DEFENSE] = 0;
    aiUnitAIVal[UNITAI_CITY_SPECIAL] = 0;

whereas the defense block multiplies those roles heavily (with exact factors depending
on the current map branch) and also boosts reserve defense. Thus the losing/strong-
enemy state can select the exact opposite role policy from the one the two configured
thresholds and comments describe.

The generic picker is live AI production. `AI_chooseProduction()` reaches
`AI_chooseUnit()` with the default `NO_UNITAI` in its generic military-production gate
(and again in the final-war fallback); that path calls `AI_bestUnit()` and consumes
these role weights. This is therefore not dead diagnostic code.

WHY THIS IS A BOOLEAN BUG, NOT JUST A TUNING DISAGREEMENT
---------------------------------------------------------
The source itself establishes two distinct intended modes and exposes separate strong
and weak power thresholds. The current expression contains the algebraically redundant
shape:

    (realWar && enemyWeak) || ... || realWar

so `enemyWeak` cannot influence the result whenever `realWar` is true. The birth
comments explicitly notice the `(a && b) || a` redundancy, but retain it after trying
to avoid the different mistake of testing `enemyPower <= threshold` unguarded in
peacetime, where enemy power may read as zero.

The correct way to avoid the peacetime-zero problem is to keep the real-war/planned-
war guard around the weak-power comparison, not to re-add unconditional real war as a
second OR term that erases the comparison.

The branch ordering makes the contradiction observable: both modes being true would
still be ambiguous if they only contributed independent soft bonuses, but here
`if (bOffenseMode) ... else if (bDefenseMode)` gives offense absolute precedence.

ANCESTRY
--------
SAS practical 4932 / `6a226d0fba4af5688d6c9d69a5390e132c3aef8b`, title beginning
"UNITAI AI preference changes, settler slow growth AI policy...", introduced the
custom offense/defense mode block into `AI_bestUnit()` with hard-coded thresholds 130
and 70.

Its own source comment says, in substance, that `(a & b) || a` is redundant. The
commit changed the peacetime weak-power attempt from an unguarded check to:

    (bAnyRealWar && iEnemyPowerPercent <= 70)

but left another independent `|| bAnyRealWar` later in the same OR expression. Thus
the intended guard was added while the old unconditional real-war trigger remained and
made the new threshold test ineffective.

SAS practical 5072 / `6b42984519...` later parameterized the two values into
`SAS_ENEMY_STRONG_POWER_THRESHOLD` and `SAS_ENEMY_WEAK_POWER_THRESHOLD` (current
defaults 120/80), but retained the unconditional real-war term. The current source
still has that exact structure.

The neighboring settler gate also computes these booleans, but there both defense and
offense ultimately block Settlers, so the clean F549 consequence is the later
`AI_bestUnit()` role-selection branch where the two modes deliberately have opposite
unit-role effects.

DEDUP / CONTROLS
----------------
- Not a Base AdvCiv/K-Mod inherited role-weight issue: the entire offense/defense
  threshold layer is SAS-specific.
- Not a stale enemy-power/cache root: the fixture accepts the current returned value
  (150) as correct and shows that the boolean discards its classification.
- Not a UnitAI-domain mismatch. A separate XML/domain audit found the only mixed-domain
  allowed role is `UNIT_TACTICAL_NUKE` (DOMAIN_AIR with UNITAI_ICBM), and
  `AI_bestUnit()` explicitly special-cases UNITAI_ICBM into a global count, so that
  does not cause this defect.
- Not the deliberate generic-Settler suppression: current SAS intentionally routes
  Settler decisions through explicit production logic and zeroes the generic Settler
  role.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve the intended distinction explicitly rather than allowing both booleans to be
true and relying on branch order. For example, define the strategic context first and
then make weak/strong enemy power select between offense and defense only when the
power value is meaningful.

At minimum, the unconditional `|| bAnyRealWar` must not remain in `bOffenseMode` after
`(bAnyRealWar && iEnemyPowerPercent <= iOffenseModeThreshold)` if the weak threshold is
supposed to discriminate real-war offense from real-war defense.

Also review the independent `bWarPlan` / `bAnyPlannedWar` terms: `AI_isFocusWar()` can
be true for defensive-war contexts too, so a robust repair should state precedence
explicitly (e.g. strong/danger/defensive posture wins defense; weak/attack posture wins
offense) rather than merely deleting one redundant term and leaving another broad war
predicate to override the same distinction.

POST-F549 CONTROLS / CONTINUATION
---------------------------------
The first `AI_bestUnit()` contract pass also checked domain accounting. Current XML
has one legal mixed-domain UnitAI:

    UNIT_TACTICAL_NUKE: DOMAIN_AIR, UNITAI_ICBM

while `AI_unitAIDomainType(UNITAI_ICBM)` returns DOMAIN_IMMOBILE. In this particular
function that is safe because UNITAI_ICBM is explicitly grouped with DOMAIN_AIR for
existing-role subtraction and uses the global `AI_totalUnitAIs` count.

The large `AI_bestUnitAI()` SAS delta mainly disables the old `bGrowMore` food-
production veto because Settler policy was moved to parent production logic; no
separate hard contract failure has been promoted from that change in this tranche.

CURRENT STONE AFTER C031-WIP323
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F549 / KI#872 CONFIRMED.
F550 / provisional KI#873 UNUSED.
Latest physical checkpoint: C031-WIP323.
No repository source files modified.

NEXT
----
Continue under F550 unused:
1. finish `AI_bestUnit()` / `AI_bestUnitAI()` current-SAS hard-contract audit, with
   special attention to derived-state booleans, role/domain assumptions and generic
   production callers;
2. inspect `AI_chooseBuilding()`'s Barbarian world-wonder rejection only if it can be
   shown to violate the helper's fallback contract rather than deliberate Barbarian
   policy;
3. resume mixed-domain count/ratio and completion-boundary hard-state checks;
4. continue ranked historical lost-assignment / changed-target candidates after
   excluding known/repaired roots;
5. freeze a control checkpoint rather than force F550 if the remaining seams close.

END C031-WIP323


================================================================================
C031-WIP324 APPEND - F550 / KI#873 CONFIRMED
================================================================================

F550 / KI#873 - SAS AI_bestUnit treats a land area's local isolation as probable peace
even during a known real overseas war, disabling the entire custom offense/defense
war-mode layer on the island/naval maps whose branches explicitly try to tune naval
war production.

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
SAS-specific; same practical-4932 custom war-mode introduction as F549, but a separate
boolean gate with an independent repair and witness.
Current source untouched.

ROOT
----
Current `CvCityAI::AI_bestUnit()` computes:

    bool const bAreaAlone = kOwner.AI_isAreaAlone(getArea());
    int const iHasMetCount = kTeam.getHasMetCivCount(true);
    ...
    bool const bAnyRealWar = (kTeam.getNumWars(false, true) > 0);
    bool const bAnyPlannedWar = kTeam.AI_isAnyChosenWar();
    bool const bPeaceAloneLikely = (bAreaAlone || iHasMetCount <= 0);

and both strategy modes are finally gated by:

    ... && !bPeaceAloneLikely

`AI_isAreaAlone()` does NOT mean "no active enemy" or "peace likely". Outside its
Ancient/start-era visibility precaution, it returns whether the land CvArea's
non-Barbarian city count is entirely accounted for by our team:

    (kArea.getNumCities() - barbarianCitiesInArea) == ourTeamCitiesInArea

Thus an island containing only our cities is "alone" even when our team is currently
at war with a known civilization on another island.

CURRENT LIVE FIXTURE
--------------------
Use a normal post-start-era Archipelago/island state:

1. Player A owns one or more cities on island LandArea A.
2. No non-Barbarian foreign city exists on LandArea A.
3. Player B owns cities on a different land area across the sea.
4. A and B have met and are currently at real war.
5. A's city is coastal and the map is recognized by the current SAS naval-heavy map
   cache.

For A's city:

    bAreaAlone      = true
    iHasMetCount     > 0
    bAnyRealWar     = true
    bPeaceAloneLikely = true || false = true

Consequently:

    bDefenseMode = (... && !true) = false
    bOffenseMode = (... && !true) = false

The current war mode is therefore skipped entirely despite the known real war.

This is particularly contradictory to the code's own structure because immediately
inside those modes SAS has explicit `bNavalHeavyMapname` branches that adjust:

    UNITAI_ATTACK_SEA
    UNITAI_RESERVE_SEA
    UNITAI_ESCORT_SEA
    UNITAI_ASSAULT_SEA
    UNITAI_SETTLER_SEA
    UNITAI_MISSIONARY_SEA
    UNITAI_SPY_SEA
    UNITAI_MISSILE_CARRIER_SEA

and related land/offense/defense roles specifically for naval-heavy wartime strategy.
On the archetypal separate-island naval war, the local-area-alone gate prevents those
branches from running at all.

LIVE CONSUMER
-------------
As established for F549, `AI_chooseProduction()` has generic military-production paths
that call `AI_chooseUnit()` with default `NO_UNITAI`; this invokes `AI_bestUnit()` and
uses these role values to select the concrete unit role. The suppressed mode therefore
changes live AI production selection rather than merely a diagnostic label.

WHY THIS IS INDEPENDENT FROM F549
---------------------------------
F549 and F550 occur in the same SAS strategy block but are logically independent:

- F549: during an ordinary non-isolated real war, `bOffenseMode` contains an
  unconditional `|| bAnyRealWar`, so offense wins over defense regardless of the weak/
  strong power thresholds.

- F550: during an overseas war where the city's LAND AREA is locally alone,
  `bPeaceAloneLikely` disables BOTH modes before their offense-vs-defense precedence is
  reached.

Fixing F549's redundant real-war OR still leaves the overseas war with both modes
false. Fixing F550's peace/isolation gate still leaves F549's losing land war choosing
offense. The fixtures and minimum repairs therefore do not collapse into one root.

ANCESTRY
--------
SAS practical 4932 / `6a226d0fba4af5688d6c9d69a5390e132c3aef8b` introduced the
custom `AI_bestUnit()` war-mode layer with:

    bPeaceAloneLikely = (bAreaAlone || iHasMetCount <= 0)

and gated both offense and defense by `!bPeaceAloneLikely` from birth.

Later threshold/map-name refactors retained that gate. Current SAS still contains the
same logic.

`AI_isAreaAlone()` itself is inherited AdvCiv/K-Mod logic and its local-land-area
contract is coherent. The bug is SAS interpreting that local geography predicate as a
peace predicate after it already has authoritative real/planned-war state in hand.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not change `AI_isAreaAlone()` globally; its meaning is valid for its other callers.
Fix the SAS war-mode gate instead.

At minimum, local area isolation must not suppress the strategy when a real/planned war
already establishes that peace is not merely "likely". For example, derive the peace
heuristic only outside active/selected war context:

    bPeaceAloneLikely = !bAnyRealWar && !bAnyPlannedWar &&
                        (bAreaAlone || iHasMetCount <= 0);

Then reconcile this with F549's separate offense/defense precedence so strong enemy
power / defensive posture can select defense and weak enemy power / offensive posture
can select offense.

CONTROL NOTES AFTER F550
------------------------
- The Barbarian `AI_chooseBuilding()` world-wonder veto returns false instead of trying
  the second-best ordinary building. That can bias Barbarians away from infrastructure
  whenever a Wonder tops the list, but the caller deliberately falls through toward
  unit production and the source policy explicitly says Barbarians should fight rather
  than compete for Wonders. Retain as fallback-quality/policy debt; no F-number.
- `AI_bestUnitAI()`'s removed `bGrowMore` food-production veto remains deliberate SAS
  architecture around explicit parent Settler/Worker policy; no new hard root found.

CURRENT STONE AFTER C031-WIP324
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F550 / KI#873 CONFIRMED.
F551 / provisional KI#874 UNUSED.
Latest physical checkpoint: C031-WIP324.
No repository source files modified.

NEXT
----
Continue under F551 unused:
1. leave the now-bounded AI_bestUnit war-mode family unless a different contract is
   found; F549/F550 own its two demonstrated boolean failures;
2. inspect remaining low-coverage live `CvCityAI` hard gates and current SAS deltas,
   prioritizing object/enum identity, impossible fallback selection, and producer/
   consumer state boundaries rather than subjective role weights;
3. continue mixed-domain count/ratio and historical lost-assignment/change-target
   sieves after dedup against existing findings;
4. freeze a control checkpoint rather than force F551 if the remaining seams close.

END C031-WIP324


================================================================================
C031-WIP325 - QUEUE 005 CvCityAI.cpp DEEP RE-AUDIT CONTINUATION
================================================================================

START STATE
-----------
Resumed from C031-WIP324 with F551 / provisional KI#874 UNUSED.
Repository source remained untouched throughout this tranche.

POST-WIP324 CONTROL / DEDUP WORK
--------------------------------
1. SAS AI_buildUnitProb rival-vassal power-per-city asymmetry was completed and then
   deduplicated before spending F551. CvTeam::getPower(true) does include vassal power,
   while CvTeam::getNumCities() does not add vassal cities; however WIP286 already owns
   this exact distorted-input family under KI#197. No new F-number.

2. SAS_AI_findBestFallbackUnit() was audited end-to-end:
   - full canTrain legality precedes scoring;
   - shipped offense-only/defense-only defines are mutually sane (1/0), with an assert
     guarding the unsupported both-true configuration;
   - raw XML cost is intentionally the stable era-cap currency;
   - ordinary military lines use accepted fallback UnitAIs;
   - Paratrooper (UNITAI_PARADROP) is outside the deliberately narrow fallback role set,
     but no shipped state was proved where that omission alone makes the emergency
     fallback fail despite no other legal fallback military unit. Retain as conservative
     policy/robustness debt, not F551.

3. Several apparent low-coverage methods were controls or already-owned work:
   - AI_foodAvailable() is commented/dead inherited residue;
   - AI_healthHappyImprovementValue()'s wrong recursive receiver remains the real but
     CURRENT-SAS-masked WIP288 upstream note;
   - AI_juggleCitizens / AI_jobChangeValue citizen-conservation work is already deeply
     covered by WIP285-WIP286;
   - AI_estimateReligionBuildings, AI_nukeEplosionValue and
     AI_calculateMilitaryOutput were already explicitly bounded in earlier checkpoints;
   - SAS file-local production helpers were already covered by WIP310;
   - water-area production capacity rediscoveries fold into KI#193/KI#197.

4. A boolean-algebra scanner temporarily flagged a live-looking
       !bDanger && A || B
   expression. Exact source-context reconstruction showed it is inside a disabled K-Mod
   block comment. The original closure was correct; this was a scan/context false
   positive, not gameplay code.

5. Owner/team/city/area ID-domain and dense AI_buildingValue receiver scans did not
   expose another F521-style compile-valid identity mismatch. The remaining dense
   receiver cases are intentional or already owned (notably F547's visible-only future
   construction path).

F551 / KI#874 CONFIRMED
-----------------------
SAS temporary "force one Artist until BFC" logic never releases the forced Artist for
AI cities, so a temporary emergency assignment can become permanent.

CURRENT CODE / INTENT
---------------------
SAS practical 5067 / commit 8a12b125dd634ab11ab5535fccddb39c451ac9d2
introduced the feature under the explicit title:

    "Force one artist if we don't have BFC and city culture per turn is low, until we do"

Current GlobalDefines_advciv_sas.xml enables it by default:

    SAS_DO_TURN_FORCE_ARTIST_IF_NO_BFC_AND_LOW_CULTURE = 1
    SAS_DO_TURN_FORCE_ARTIST_FOR_BFC_MIN_NO_CULTURE_PER_TURN_THRESHOLD = 3
    SAS_DO_TURN_FORCE_ARTIST_MIN_NO_CULTURE_LEVEL_THRESHOLD = 2

In CvCity::doTurn(), the AI branch does roughly:

    if (cultureLevel < 2 && culturePerTurn <= 3)
    {
        if (forcedArtists < 1 && isSpecialistValid(Artist, 1))
            changeForceSpecialistCount(Artist, +1);
    }
    // else: comment says AI_jobChangeValue can handle unassignment

There is no corresponding AI-side decrement after the BFC is reached.

The whole current CvGameCoreDLL mutation inventory confirms that the only explicit
Artist-force decrement in this helper is in the HUMAN-convenience branch. Generic
setForceSpecialistCount calls can sanitize an invalid forced target or clear forced
specialists when citizen automation is manually disabled, but they do not release a
still-valid AI Artist merely because the BFC purpose has ended.

WHY AI_jobChangeValue CANNOT RELEASE IT
---------------------------------------
The source comment saying AI_jobChangeValue can handle unassignment is incorrect for a
forced specialist.

AI_assignWorkingPlots treats force counts as hard targets:

    if (getSpecialistCount(e) < getForceSpecialistCount(e))
        setSpecialistCount(e, getForceSpecialistCount(e));

AI_removeWorstCitizen normally skips a specialist when:

    getSpecialistCount(e) <= getForceSpecialistCount(e)

and AI_juggleCitizens only puts a worked specialist into the removable worked-job set
when:

    getSpecialistCount(e) > getForceSpecialistCount(e)

Thus exactly one forced Artist is deliberately protected from ordinary job-value
reassignment. AI_jobChangeValue may value another job more highly, but it does not own
or decrement m_aiForceSpecialistCount and cannot make that protected Artist removable.

CURRENT SHIPPED FIXTURE
-----------------------
Use an ordinary AI running CIVIC_CASTE_SYSTEM (TECH_CODE_OF_LAWS). Current civic XML
makes Artist, Scientist and Merchant specialists unlimited/valid under Caste System.

1. The AI founds or captures a young city with culture level below 2 and culture/turn
   <= 3.
2. Artist is legally assignable because Caste System makes SPECIALIST_ARTIST valid.
3. CvCity::doTurn raises the forced Artist target from 0 to 1.
4. AI citizen assignment subsequently ensures one Artist is actually assigned.
5. On Normal speed, CULTURELEVEL_FLEDGLING (the BFC expansion threshold) is 10 culture;
   the Artist supplies the intended fast culture and the city reaches the BFC.
6. Once culture level is >= 2, the force-creation branch simply stops executing. It
   does NOT decrement forced Artist count.
7. While Caste System remains active (or the city later has an Artist slot), Artist
   remains valid, so KI#70's invalid-target sanitizer correctly does nothing.
8. Normal AI assignment/juggling continues protecting the forced Artist indefinitely,
   even though the emergency BFC objective has already been satisfied.

The consequence is persistent citizen misallocation: one population remains locked to
Artist instead of returning to the normal plot/specialist optimizer. This can reduce
food, production or other commerce and can persist for many turns or effectively the
rest of the city's life while Artist validity remains.

ANCESTRY / POSITIVE CONTROL
---------------------------
This is SAS-specific; Base AdvCiv/K-Mod do not contain the SAS BFC-Artist helper.

The later human-convenience history is especially strong evidence that forced counts do
need explicit release. Practical 5128 / commit 9d5a1a113d22683ac03a34654a30262c6876b094
is explicitly titled:

    "auto unassign the forced artist after we have BFC for the convenience human player part of the code"

and adds changeForceSpecialistCount(Artist, -1) after the human BFC condition ends.
The AI branch is left with the comment that AI_jobChangeValue should handle its
unassignment; as proved above, forced-specialist protection prevents that.

DEDUP / ROOT BOUNDARY
---------------------
Keep F551 separate from:

- KI#70: invalid forced-specialist target/capacity sanitation. F551's Artist remains
  legally valid; the defect is failure to release a no-longer-needed valid target.
- KI#313: provenance/ownership of the HUMAN convenience helper's automatic forced
  Artist. F551 concerns AI cities, where there is no manual-human provenance conflict.
- WIP285-WIP286 citizen-juggle controls: those correctly preserve forced specialists;
  that correct behavior is what makes F551 persistent once the force target is stale.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The AI BFC helper needs explicit ownership/lifetime handling just like the corrected
human helper, but without confusing an unrelated pre-existing force with the helper's
own increment.

Safest direction:
1. record whether the AI BFC helper itself added the +1 Artist target (provenance), or
   otherwise use a narrowly scoped AI-only state that cannot steal another system's
   forced Artist;
2. once getCultureLevel() reaches the configured BFC threshold, decrement exactly the
   helper-owned force once;
3. keep KI#70's independent invalid-target sanitizer intact;
4. mark citizen assignment dirty through the existing force-count setter so the now
   unforced Artist can be re-evaluated normally.

F552 / provisional KI#875 remains UNUSED.

CURRENT STONE AFTER C031-WIP325
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F551 / KI#874 CONFIRMED.
F552 / provisional KI#875 UNUSED.
Latest physical checkpoint: C031-WIP325.
No repository source files modified.

NEXT
----
Continue under F552 unused:
1. finish the current AI_specialistValue / AI_jobChangeValue hard-invariant pass now
   that the BFC-force lifecycle root is owned by F551;
2. distinguish stale forced-target lifetime from specialist valuation/multiplicity
   bugs already owned by KI#70, KI#313, F524 and WIP285-WIP286;
3. resume current hard gate/domain/identity and producer-consumer boundary sieves;
4. freeze a control checkpoint instead of forcing F552 if the remaining specialist
   and production seams close.

END C031-WIP325


================================================================================
C031-WIP326 - QUEUE 005 CvCityAI.cpp DEEP RE-AUDIT CONTINUATION
================================================================================

START STATE
-----------
Resumed from C031-WIP325 with F552 / provisional KI#875 UNUSED.
Repository source remained untouched throughout this tranche.

POST-WIP325 CONTROLS
--------------------
1. `AI_minDefenders()` / `AI_defensiveBuildingValue()` were finished first as promised by
   the WIP325 cursor.
   - The SAS-only early anti-Barbarian third-defender window in `AI_minDefenders()` is the
     already documented KI#49 tuning/policy. Its current added conditions do not mix an
     incompatible unit/area domain or violate a legality/count invariant.
   - `AI_defensiveBuildingValue()` remains semantically Base AdvCiv 1.14 apart from cached
     `CvGame`/owner/team references and formatting. The genuine obsolete-safe-effect defect in
     this helper family remains F536/KI#859; no second current root was established here.

2. The concrete `AI_chooseUnit(UnitTypes, UnitAITypes)` substitution path was rechecked around
   its cautionary stale-derived-state comment. When a strict-defender candidate is replaced,
   `eChangedUnit`, `eChangedUnitAI` and `pUnitInfo` are updated together; all later naval,
   civilian and air category booleans are first computed from the changed UnitAI. The earlier
   siege/strict-defender booleans are consumed before replacement. The old WIP control remains
   valid; no F552 there.

3. The final pre-push EXPLORE/WORKER/SPY/Settler hard vetoes were reopened. Their strongest
   stale Worker-demand consequences fold into F543/KI#866; no new independent area/domain
   identity defect was promoted in this pass.

F552 / KI#875 CONFIRMED
-----------------------
`AI_chooseProduction()` treats any positive-combat unit currently in production as the city's
missing defender, so an undefended coastal city can keep building a combat ship instead of
reconsidering production for a real land garrison.

ROOT
----
At the top of current `CvCityAI::AI_chooseProduction()`, when a city already has production
invested, the AI normally gets an opportunity to abandon the current order and choose something
more appropriate. One emergency retention rule says:

    // if building a combat unit, and we have no defenders, keep building it
    UnitTypes eProductionUnit = getProductionUnit();
    if (eProductionUnit != NO_UNIT)
    {
        if (getPlot().getNumDefenders(getOwner()) == 0)
        {
            if (GC.getInfo(eProductionUnit).getCombat() > 0)
                return;
        }
    }

The comment and surrounding control flow clearly mean that, with zero city defenders, a combat
unit already being produced should be preserved because it can become the urgently needed
city defender.

`CvUnitInfo::getCombat() > 0`, however, does not establish that the unit can defend a LAND city.
It is true for ordinary naval combat units as well.

CURRENT SHIPPED WITNESS
-----------------------
Current SAS `CIV4UnitInfos.xml` defines the ordinary Galley as:

    <Type>UNIT_GALLEY</Type>
    <Combat>UNITCOMBAT_NAVAL</Combat>
    <Domain>DOMAIN_SEA</Domain>
    <DefaultUnitAI>UNITAI_ASSAULT_SEA</DefaultUnitAI>
    ...
    <iCombat>2</iCombat>

So a Galley satisfies the retention test despite being a sea-domain unit.

Use an ordinary coastal AI city with:

    current land/city defenders = 0
    current production          = Galley
    invested production         > 0
    production turns left       > 3

and invoke an ordinary production reconsideration (e.g. any of the supported
`setChooseProductionDirty`/AI production-refresh paths). The earlier food-production and <=3-turn
retention exits need not apply.

Current code then evaluates:

    getNumDefenders(owner) == 0     -> true
    Galley.getCombat() > 0          -> true
    return

and therefore keeps the Galley.

Completing that Galley does not create the missing land-city garrison. A coastal city can hold
sea units, but sea-domain naval combat is not a substitute for a land defender against capture.
The exact emergency fact that should make the AI reconsider production is therefore converted
into a reason to preserve an irrelevant order.

The same witness can be built with other positive-combat sea units (Frigate, Destroyer, etc.);
Galley is simply an early, unambiguous shipped example.

LIVE CONSEQUENCE
----------------
This is a hard production-control return, not a soft score. Once the condition fires,
`AI_chooseProduction()` does not clear the current queue and does not reach the normal defender /
military production logic in that reconsideration. The city can spend additional turns on a
ship while remaining without a land defender.

The defect is especially adverse in the state named by the source comment itself: an undefended
city. Danger is not required by this branch and does not repair it.

ANCESTRY
--------
This is inherited, not SAS-specific.

- Stock BtS/Civ4CE contains the same zero-defender + `getCombat() > 0` retention rule.
- The earliest reachable K-Mod snapshot (practical 3 / commit
  `2414225e6415911ac28483d2bdac67b4100d36ff`) already contains the same rule.
- K-Mod practical 489 later performs only declaration/style cleanup around it.
- Base AdvCiv 1.14 and current SAS retain the condition unchanged semantically.

Thus this is a BtS-era city-production contract defect surviving through K-Mod, AdvCiv and SAS.

DEDUP / ROOT BOUNDARY
---------------------
Keep F552 separate from F545/KI#868.

F545 also found a naval-vs-land mismatch involving a unit finishing production, but that root is
inside `AI_updateSafety()` and corrupts the cached `CITYSAFETY_*` forecast. F552 requires no
safety-cache involvement at all: it is a direct early return inside production selection that
keeps an unsuitable current order.

Fixing F545's safety predictor leaves this `AI_chooseProduction()` return unchanged. Fixing this
retention test leaves F545's safety classification unchanged. They are independent producers,
consumers and repair sites.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
The retention condition should ask whether the produced unit can actually serve as the missing
city defender, rather than using generic positive combat strength.

At minimum, require a land-domain combat defender (and preserve any relevant special-unit rules),
e.g. conceptually gate the emergency retention on `DOMAIN_LAND` plus positive combat/defender
eligibility. Prefer mirroring the same defender eligibility contract that `getNumDefenders` is
intended to represent rather than inventing a second approximation.

Do not globally reject naval production in undefended cities; the bug is only that the existing
"this current unit will solve zero defenders" early return treats naval combat as proof.

F553 / provisional KI#876 remains UNUSED.

CURRENT STONE AFTER C031-WIP326
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F552 / KI#875 CONFIRMED.
F553 / provisional KI#876 UNUSED.
Latest physical checkpoint: C031-WIP326.
No repository source files modified.

NEXT
----
Continue under F553 unused:
1. finish the remaining `AI_chooseProduction()` early-retention / hard-return contracts,
   especially tests that claim an in-progress order solves danger, defense or food-production
   state;
2. continue the authoritative final `AI_chooseUnit` pre-push veto audit, deduplicating stale
   Worker demand against F543 and naval/topology accounting against KI#193/KI#197;
3. then resume live current-SAS hard-gate/domain/identity and producer-consumer boundary sieves;
4. promote F553 only for a separate live root; otherwise freeze a control checkpoint.

END C031-WIP326


================================================================================
C031-WIP327 - QUEUE 005 CvCityAI.cpp DEEP RE-AUDIT CONTROL CHECKPOINT
================================================================================

START STATE
-----------
Resumed from C031-WIP326 with F553 / provisional KI#876 UNUSED.
Repository source remained untouched throughout this tranche.

POST-WIP326 CONTROLS
--------------------
1. The remaining `AI_chooseProduction()` food-production / near-completion retention control was
   reconstructed against K-Mod ancestry.
   - The `isFoodProduction()` retention branch deliberately does NOT keep a food-production order
     merely because it is within three turns when the area strategy is `AREAAI_DEFENSIVE`.
   - Stock K-Mod already has this `if (...) { if (!DEFENSIVE) return; } else if (turns <= 3)`
     structure; AdvCiv later tightened Settler eligibility while preserving it.
   - Current shipped food-production units are Settler/Worker-family noncombat units. The branch is
     therefore an intentional emergency-defense override, not an accidental `else if` attachment.

2. Carrier-air / missile-air shortage accounting was checked for mixed land/water-area scope.
   - Main carrier-air and missile-air comparisons are global-vs-global.
   - The extra `AI_totalAreaUnitAIs` missile comparison intentionally describes docked carriers and
     missiles sharing a coastal city's land `CvArea`; once a carrier sails to sea, it leaves that
     local pair together.
   - No independent KI#197-style numerator/denominator domain mismatch was established.

3. SAS comments saying several unit caps include both existing and queued/training units were
   verified against the accessors rather than trusted.
   - `AI_totalUnitAIs(eAI)` is `AI_getNumAIUnits(eAI) + AI_getNumTrainAIUnits(eAI)`.
   - The area form likewise includes existing + training units.
   - The simultaneous-overqueue prevention comments therefore match the current contract.

4. A familiar positive-combat-as-defender defect was rediscovered in `AI_doPanic()` but correctly
   deduplicated before numbering.
   - A currently produced combat ship can cause the panic routine to hurry/retain it and return,
     skipping a needed land defender.
   - This is already F534 / KI#857, including the source-documented "A ship can block a needed land
     defender" consequence. F553 was not spent on it.

5. The land `UNITAI_SPY` war-plan veto looks like a copy/paste inversion of the preceding
   `UNITAI_SPY_SEA` rule (`!bNavalHeavyMapname` with the same stale naval comment), but WIP278
   already audited this exact branch and classified it as authored KI#53 policy: SAS intentionally
   suppresses land-Spy production during ordinary land-war planning to avoid stalling military
   buildup. The contradictory/stale comment remains a documentation issue, not a new F553 root.

6. The structural "best concrete unit is vetoed, but `AI_chooseUnit` does not try the second-best
   concrete unit for the same UnitAI" behavior was also deduplicated.
   - The Trebuchet/Catapult-style witness and other post-selection hard vetoes were already broadened
     under KI#53.3 in WIP287.
   - Do not promote this architecture again as F553.

7. Recent SAS diagnostic/SASGameRecord instrumentation was audited for the invariant that logging
   must not alter gameplay or the synchronized RNG stream.
   - The air 1-in-3 diagnostic path performs the same single synchronized RNG draw as the normal
     path rather than drawing an extra logging-only number.
   - Project/building/process traces reuse already-consumed random values.
   - Logging-only helper calls found in this area are deterministic read/evaluation routines (for
     example `AI_bestCityUnitAIValue`) rather than mutating production helpers.
   - No log-level-dependent gameplay or RNG-stream change was established.

8. The AdvCiv "outdated pirates" gate was checked because it compares pirate prereq-tech era to
   `CvGame::getCurrentEra()` (world-average era), not the owner team's era. Its birth commit
   explicitly introduces a global-world obsolescence rule for privateer-style pirates. This is an
   intentional policy, not a current owner/team receiver bug.

9. The current-file `getCombat() > 0` city-defender proxy inventory is now bounded:
   - `AI_chooseProduction()` early retention -> F552 / KI#875;
   - `AI_updateSafety()` prospective defender -> F545 / KI#868;
   - `AI_doPanic()` production blocker -> F534 / KI#857;
   - later fallback / replacement paths use explicit land-domain or role checks.
   No unowned fourth live use was found in this tranche.

10. The first-capital economic-before-defender and initial Work Boat/Worker retention logic was
    checked for a persistent-state deadlock. The gates are driven by current Worker/Work Boat
    existence, traveling Work Boats and improved-sea-bonus state rather than by a one-shot
    persistent flag; no permanent "economic first forever" state was established.

11. `AI_chooseUnit(UnitTypes, UnitAITypes)` can theoretically be called with a concrete unit and
    `NO_UNITAI`, but no live internal current-SAS caller was found that does so. Normal call sites
    resolve a real UnitAI before the final push. Treat this as robustness only unless a real caller
    is established.

12. A line-oriented boolean-precedence sieve falsely re-flagged the inherited
    `!bDanger && A || B` expression near the old K-Mod building logic as live. Reopening exact
    block-comment context confirmed it remains inside disabled/commented code. Future automated
    source sieves must preserve block-comment state before ranking a candidate.

F553 / provisional KI#876 remains UNUSED.

CURRENT STONE AFTER C031-WIP327
-------------------------------
Queue 005 CvGameCoreDLL/CvCityAI.cpp - OPEN.
32/169 unique files complete.
F446 / KI#769 through F552 / KI#875 CONFIRMED.
F553 / provisional KI#876 UNUSED.
Latest physical checkpoint: C031-WIP327.
No repository source files modified.

NEXT
----
Continue under F553 unused with a genuinely different late-stage lens rather than repeatedly
reopening KI#53/KI#193/KI#197/KI#198 families:
1. inspect authoritative current-SAS hard gates that mix object/domain/count scopes and have not
   already been assigned to a known root;
2. inspect production-completion / event-boundary values whose producer and immediate consumer can
   disagree without going through already-owned cache families;
3. preserve exact block-comment/live-code context in all mechanical scans;
4. if those lenses also saturate, consider whether Queue 005 has reached a defensible closure point
   instead of forcing F553.

END C031-WIP327

================================================================================
C031-WIP328 APPEND - F553 / KI#876 CONFIRMED
================================================================================

START STATE
-----------
Resumed from C031-WIP327 with F553 / provisional KI#876 UNUSED.
Current authoritative source snapshot: AdvCiv-SAS_light_source_20260904T202954.zip.
Base AdvCiv 1.14, K-Mod and Civ4CE were used only as local ancestry/control material.
Repository source remained untouched.

POST-WIP327 CONTROL - MILITARY INSTRUCTOR / HEROIC EPIC ANTICIPATORY VALUE
-------------------------------------------------------------------------
The unfinished `AI_permanentSpecialistValue()` state check closes as intentional policy,
not F553.

SAS gives a Military Instructor extra value in a top production city when the city either
already has the configured Heroic-Epic-effect building or is currently constructing it:

    bHasHeroicEffectBuilding = (getNumBuilding(eHeroicEffectBuilding) > 0)
    bBuildingHeroicEffectBuilding = (getProductionBuilding() == eHeroicEffectBuilding)

This initially looked vulnerable to a cancelled Heroic Epic leaving a Great General headed
toward a no-longer-preferred city. The live Great-General producer does not freeze that score,
however. `CvUnitAI::AI_join()` scans candidate cities and calls
`AI_permanentSpecialistValue()` again on subsequent AI turns while the unit is moving. If the
Heroic Epic is cancelled, the anticipatory extra disappears at the next evaluation and the
best join city can change; if the building completes, `getNumBuilding()` preserves the intended
bonus. No hard stale-state lifetime was established.

F553 / KI#876 - SAS city-local focus-war callers were changed from the current city's land
area to the CAPITAL'S land area by replacing `AI_isFocusWar(area())` with the no-argument
`AI_isFocusWar()` under the mistaken assumption that the latter is a broader check
------------------------------------------------------------------------------------------------

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
SAS-specific caller-scope regression introduced in practical commit 4957 /
`fe58e134631681ba9e79e481dd29a761b8820696`.
Independent of F344/KI#667, F549/KI#872 and F550/KI#873.
Current source untouched.

ROOT / API CONTRACT
-------------------
`CvPlayerAI::AI_isFocusWar` has an optional area parameter:

    bool AI_isFocusWar(CvArea const* pArea = NULL) const;

The implementation does NOT interpret NULL as "all areas", "empire-wide", or a larger
war-focus scope. It explicitly substitutes the capital's land area:

    if (pArea == NULL)
        pArea = getCapital()->area();

The function then uses that selected area's AreaAI state. In particular, for a war that is not
one of the team's chosen wars:

    if (!kOurTeam.AI_isAnyChosenWar() &&
        pArea->getAreaAIType(getTeam()) != AREAAI_DEFENSIVE)
    {
        return false;
    }

Therefore these two calls are genuinely different for a multi-landmass empire:

    AI_isFocusWar(area())  // this city's land area
    AI_isFocusWar()        // capital's land area

The current SAS comments that motivated the change say the no-argument form is the "larger
one" and might "handle more cases". That premise is false under the actual API contract: it is
a default-to-capital-area overload, not a union/global query.

INTRODUCTION / ANCESTRY
-----------------------
Base AdvCiv 1.14 deliberately uses the current city's area in its city-local consumers,
including:

    CvCityAI::AI_bestUnit:      AI_isFocusWar(area())
    CvCityAI::AI_buildingValue: AI_isFocusWar(area())

SAS practical 4957 changed four existing city-local reads from `AI_isFocusWar(area())` to the
no-argument form, including the inherited `AI_bestUnit()` and `AI_buildingValue()` calls, and
also introduced the same no-argument interpretation in the centralized concrete
`AI_chooseUnit()` production gates.

The 4957 diff contains the mistaken rationale beside the changes:

    "sometimes AI_isFocusWar is used with, sometimes without in cvcityai.cpp,
     going for the larger one ... maybe we handle more cases this way"

Current SAS retains those calls and comments. The underlying `AI_isFocusWar` implementation
still defaults NULL to `getCapital()->area()`, just as Base AdvCiv does.

SUPPORTED LIVE DIVERGENCE
-------------------------
Use an AI with two disconnected land areas:

* LandArea A contains the capital and is not `AREAAI_DEFENSIVE`.
* LandArea B contains another AI city and is `AREAAI_DEFENSIVE`.
* The team is in a reactive/non-chosen real war (e.g. an attacked war), so
  `AI_isAnyChosenWar()` is false.
* The hostile war-plan team is not a pushover.
* The AI is not globally forced true by `AI_STRATEGY_ALERT2`.

For the city on LandArea B:

    AI_isFocusWar(area())

passes the defensive-area gate and reaches the live enemy-war-plan loop; the non-pushover
enemy makes it return true.

But current SAS calls:

    AI_isFocusWar()

which substitutes LandArea A. Since no chosen war exists and the capital area is not defensive,
the function returns false immediately before considering the enemy loop.

So the same city and same game state produce opposite focus-war classifications solely because
SAS silently changed the queried CvArea from the city to the capital.

LIVE CONSEQUENCE 1 - BUILDING VALUE
-----------------------------------
Current `AI_buildingValue()` now reads:

    bool const bWarPlan = kOwner.AI_isFocusWar();

Base AdvCiv reads the city's area. This value reaches inherited quantitative valuation. For
free-experience buildings, for example:

    int iWeight = 12;
    ...
    iWeight /= (bWarPlan || (bHighProductionCity && ... ) ? 1 : 4);

In a met-rival, non-top-production city satisfying the fixture above, the correct local focus
war keeps `iWeight` at 12, while current SAS's capital-area false result reduces it to 3. Thus a
frontline secondary-area city's free-XP building value can be quartered during exactly the
local defensive war that `AI_isFocusWar(area())` was designed to detect.

The same wrong `bWarPlan` also feeds SAS hard building pre-rules (Barracks/Stable urgency,
science/gold/culture delays, national-wonder pressure gates, etc.), so the scope change is not
merely diagnostic.

LIVE CONSEQUENCE 2 - CONCRETE SIEGE HARD REJECT
------------------------------------------------
Current centralized `AI_chooseUnit(UnitTypes, UnitAITypes)` also uses the capital-area form:

    bool const bWarPlan = kPlayer.AI_isFocusWar();

With the shipped anti-excess-siege and Trebuchet-like gates enabled, a Trebuchet-like unit starts
with a 20% share cap but is tightened to 15% when `!bWarPlan`:

    int iCapTrebs = 20;
    ...
    if (!bWarPlan)
        iCapTrebs = 15;
    ...
    if (iTrebsShareOff >= iCapTrebs + iTrebuchetsLikeMinExtraCap)
        return false;

Current shipped `SAS_AI_CHOOSE_UNIT_NO_EXCESS_SIEGES = 1`,
`SAS_NO_EXCESS_TREBUCHETS_LIKE = 1`, and
`SAS_AI_CHOOSE_UNIT_NO_EXCESS_SIEGES_TREBUCHETS_LIKE_MIN_EXTRA_CAP = 0`.

Continue the secondary defensive-area fixture with a safe local city, a non-strong enemy,
relevant strategic resources, and a Trebuchet-like share between 15% and 19%. The local
`AI_isFocusWar(area()) == true` reading would retain the 20% cap and can allow the unit; current
SAS obtains false from the peaceful capital area, reduces the cap to 15%, and hard-rejects that
concrete unit. This establishes a direct production branch difference, not only a soft score.

SCOPE / CONTROLS
----------------
Not every no-argument `AI_isFocusWar()` call is wrong. Many `CvPlayerAI` consumers deliberately
ask about the player's default/capital-area strategic focus. Existing Base AdvCiv `CvCityAI`
code also has some no-argument uses where the logic is intentionally player-global. F553 is
specifically the SAS city-local substitutions/new gates whose comments treat the no-argument
form as a broader replacement for `area()`.

One 4957 Settler block is additionally protected by broader `bAnyRealWar` /
`bAnyPlannedWar` terms, so its changed `bWarPlan` can often be redundant. F553 does not require
every edited call site to have an independent symptom; the building-value and concrete-unit
consumers above are sufficient live witnesses of the shared wrong-area root.

DEDUP / ROOT SEPARATION
-----------------------
* F344 / KI#667 fixes the INTERNAL `AI_isFocusWar` state flag for the intended
  multiple-pushover-war exception. F553 assumes the helper itself returns the correct result for
  the CvArea it is given; SAS is giving it the wrong area.
* F549 / KI#872 is the SAS `AI_bestUnit()` offense-mode Boolean where an unconditional real-war
  term defeats the enemy-power discriminator. Restoring the city-area focus-war input does not
  remove that Boolean defect.
* F550 / KI#873 is the later `bPeaceAloneLikely` gate that disables both custom war modes in an
  overseas war. F553 already changes inherited building valuation and the centralized concrete
  unit gate before/without relying on that mode layer.
* This is not an `AI_isAreaAlone` semantics issue and does not require changing
  `AI_isFocusWar` globally.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Restore the explicit current-city area for city-local strategic decisions:

    kOwner.AI_isFocusWar(area())

or equivalently pass `&getArea()` where the signature/context prefers it.

At minimum, restore the Base AdvCiv area-aware calls in `AI_bestUnit()` and
`AI_buildingValue()`, and use the current city area in the SAS concrete `AI_chooseUnit()` gates
whose thresholds are meant to describe that city's local military situation. Review the other
4957 substitutions individually; do not globally replace every no-argument caller because many
player-scope callers intentionally use the default capital-area semantics.

CURRENT STONE AFTER C031-WIP328
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F553 / KI#876 CONFIRMED.
F554 / provisional KI#877 UNUSED.
Latest physical checkpoint: C031-WIP328.
No repository source files modified.

NEXT
----
Continue under F554 unused:
1. finish the remaining `AI_bestUnit()` SAS-only scope/Boolean changes after excluding
   F549/F550/F553;
2. audit nearby current-SAS hard producer/consumer gates for area/player/domain mismatches;
3. re-check any `AI_isFocusWar()` city-local-looking caller only if its downstream value is not
   already subsumed by stronger real/planned-war state;
4. if the remaining Queue-005 surfaces close, preserve saturation rather than manufacture F554.

END C031-WIP328



================================================================================
C031-WIP329 - F554 / KI#877 CONFIRMED
================================================================================

START STATE
-----------
Resumed from C031-WIP328 with F554 / provisional KI#877 UNUSED.
Repository source remained untouched throughout this tranche.

POST-WIP328 CONTROLS BEFORE F554
--------------------------------
1. The remaining `AI_bestUnit()` removal of Base AdvCiv's `bWarPossible` guards was
   reconstructed against `CvTeamAI::AI_isWarPossible()`.
   - The helper is intentionally a coarse game-option / current-war gate: it returns true
     during any current war, under Always War, and otherwise unless Always Peace or No
     Changing War/Peace forbids starting a war.
   - In ordinary rulesets this is therefore expected to be true for long periods even
     without an immediate target, matching the SAS source comment that it was too broad for
     Settler suppression.
   - The states where it is false are special locked-peace rulesets, where land military can
     still have Barbarian/garrison value. Removing the old role-weight guards is aggressive
     policy, but no objective impossible-order / conservation defect was established.

2. The apparently dramatic unconditional `UNITAI_SETTLE = 0` / `UNITAI_SETTLER_SEA = 0`
   inside SAS `AI_bestUnit()` was checked through every live generic caller.
   - Normal AI Settler production is now owned by explicit `AI_chooseUnit(UNITAI_SETTLE, ...)`
     paths in `AI_chooseProduction()` plus the central site-value gates.
   - Remaining generic `AI_chooseUnit()` callers are generic military/default fallback,
     human governor, or Barbarian paths.
   - The inherited comment that generic `AI_chooseUnit()` "Can train Settlers, but that
     rarely happens" is stale in SAS, but no live Settler-production hole was proved from it.

F554 / KI#877 - THE SAS "CITY MAINTENANCE BUILDING" PREFILTER USES THE WRONG SIGN,
SO EVERY CURRENT MAINTENANCE-REDUCING BUILDING BYPASSES THE ENTIRE AUTHORED GATE
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
SAS-specific sign inversion introduced with the building-choice prefilter.
Current source untouched.

CURRENT SOURCE
--------------
Inside the regular-building optimization block of `CvCityAI::AI_buildingValue()` SAS says:

    // not 0 (but higher value instead) to exclude false positives like ikhanda ...
    const bool bCityMaintenanceBuilding =
        (kBuilding.getMaintenanceModifier() >= 25);

    if (bCityMaintenanceBuilding)
    {
        if (iMaintenanceTimes100 < iSAS_AI_BUILDING_VALUE_GATE_M100_REGULAR_BUILDINGS)
            return 0;
        else if (bAtWar)
        {
            if (bEnemyStrong)
                return 0;
        }
    }

The comments and downstream branches clearly intend this Boolean to recognize a meaningful
CITY-MAINTENANCE REDUCER, while excluding small incidental reducers such as the Ikhanda.

ENGINE / XML SIGN CONTRACT
--------------------------
Civ4 expresses maintenance reductions as NEGATIVE `iMaintenanceModifier` values. The inherited
K-Mod/AdvCiv valuation in this same function confirms the sign contract when it computes the new
maintenance using:

    100 + getMaintenanceModifier() + kBuilding.getMaintenanceModifier()

and rewards the resulting reduction in `iMaintenanceTimes100`.

Current supplied SAS `CIV4BuildingInfos.xml` contains exactly these nonzero building maintenance
modifiers:

    BUILDING_ZULU_IKHANDA                  -15
    BUILDING_COURTHOUSE                    -50
    BUILDING_AMERICAN_FEDERAL_COURTHOUSE   -60
    BUILDING_HOLY_ROMAN_RATHAUS            -70
    BUILDING_RUSSIAN_PRIKAZ                -50
    BUILDING_VERSAILLES                    -50

There is NO current building with a positive maintenance modifier, let alone `>= +25`.

Therefore:

    kBuilding.getMaintenanceModifier() >= 25

is false for every actual maintenance-reducing building in current SAS.

The comment about excluding Ikhanda makes the intended missing sign particularly clear: a
threshold by reduction magnitude would distinguish `-15` Ikhanda from the `-50/-60/-70`
Courthouse-class reducers, but `>= +25` distinguishes none of them because the sign is reversed.

LIVE CONSEQUENCE
----------------
The whole custom maintenance-building prefilter is dead for current maintenance reducers.
For example, a normal Courthouse has `iMaintenanceModifier = -50`, hence:

    -50 >= 25  -> false

so it never reaches either authored policy:

1. low-maintenance cities should hard-reject the Courthouse-class building until current city
   maintenance reaches `SAS_AI_BUILDING_VALUE_GATE_M100_REGULAR_BUILDINGS` (currently documented
   as the 6-gpt-style threshold); and
2. an AI already at war against a strong enemy should hard-reject that economic investment and
   preserve production for the emergency.

Instead the Courthouse falls through to inherited/general scoring as if this SAS prefilter did
not exist. The same is true for Federal Courthouse, Rathaus and Prikaz. This is not merely a
threshold-tuning disagreement: the condition's sign makes the entire intended category empty
under the shipped data.

A second downstream effect is that `bCityMaintenanceBuilding` participates in
`bPreviousBuildings`, which is intended to mark already-classified regular-building families
before later economic/culture catch-all rules. Because the Boolean is false, true maintenance
reducers also do not receive that category marker. The primary F554 root remains the wrong-sign
classification; any resulting fall-through classification is a sibling consequence.

ANCESTRY
--------
SAS practical 4950 / commit `e7181173b7498ce4f6ae09140991bdfb88c459eb`, titled
"(Seemingly enhanced) Add prefiltering building choices rules to help AI choose more reponsively
and efficiently buildings depending on war context and other...", introduced the condition from
birth as:

    const bool bCityMaintenanceBuilding =
        (kBuilding.getMaintenanceModifier() >= 25);

with the same explicit Ikhanda-exclusion comment and low-maintenance / wartime hard rejects.

Later practicals 4965, 5087, 5088 and 5148 adjusted nearby thresholds, resource checks and define
names but retained the sign. Current source still has the original `>= 25` test.

DEDUP / ROOT BOUNDARY
---------------------
- KI#306 concerns insufficient SAME-TURN INVALIDATION of a government-center/top-production
  ranking cache. F554 has no cache dependency; the relevant branch is never entered even with
  perfectly fresh maintenance state.
- F399 concerns an optional/Alt saved-maintenance hover omitting government-center effects. F554
  is AI production logic and a scalar modifier sign inversion.
- The inherited `AI_buildingValue()` maintenance arithmetic itself uses the negative modifier
  coherently. F554 is confined to SAS's prefilter classifier.
- Treat every maintenance-reducer bypass caused by this Boolean as the same F554 root rather than
  numbering Courthouse/Rathaus/Prikaz separately.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve the apparent authored intent: recognize meaningful maintenance REDUCTIONS while leaving
small incidental reductions such as Ikhanda outside this hard category.

With the current sign convention, the direct repair corresponding to the existing threshold is:

    const bool bCityMaintenanceBuilding =
        (kBuilding.getMaintenanceModifier() <= -25);

A magnitude form can also be used if desired, but it should explicitly require a negative
modifier so a hypothetical future maintenance-increasing building is never classified as a
reducer. After repair, retest whether the current 25-point category threshold and the 6-gpt
maintenance gate are still the desired policy; those numerical choices are tuning, separate from
the sign bug.

CURRENT STONE AFTER C031-WIP329
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F554 / KI#877 CONFIRMED.
F555 / provisional KI#878 UNUSED.
Latest physical checkpoint: C031-WIP329.
No repository source files modified.

NEXT
----
Continue under F555 unused:
1. stay in the regular-building prefilter briefly and verify adjacent category classifiers against
   current XML sign/domain contracts, because F554 shows that this authored classifier layer can
   contain compile-valid sign mistakes;
2. deduplicate any category fall-through consequences into F554 when they stem solely from
   `bCityMaintenanceBuilding` being false;
3. then resume hard producer/consumer and area/player/domain checks outside the now-saturated
   `AI_bestUnit()` war-mode family;
4. preserve another control checkpoint rather than force F555 if the sibling classifiers close.

END C031-WIP329


================================================================================
C031-WIP330 - F555 / KI#878 CONFIRMED
================================================================================

START STATE
-----------
Resumed from C031-WIP329 with F555 / provisional KI#878 UNUSED.
Repository source remained untouched throughout this tranche.

ADJACENT CLASSIFIER SIGN/DOMAIN CONTROL BEFORE F555
---------------------------------------------------
The regular-building classifier layer was checked against current shipped
`CIV4BuildingInfos.xml` after F554 exposed the maintenance-modifier sign inversion.
The neighboring raw XML domains inspected here are coherent with their comparisons:

- bombard defense: current nonzero values are +25/+50;
- RaiseDefense: +50/+100;
- all-city defense: +25;
- food kept: current nonzero values are +15 through +50;
- espionage defense: +25/+50;
- trade-route count additions: +1;
- trade-route modifiers: +25/+50;
- foreign-trade-route modifiers: +50/+100.

So there is no second immediate wrong-sign sibling analogous to F554 in those fields.
The `iFoodKept >= 25` classifier deliberately excludes the current +15 Chinese
Changpingcang effect in the same style that other category thresholds exclude small
incidental effects; that is a policy threshold, not an empty sign-inverted category.

F555 / KI#878 - THE FINAL "UNKNOWN / MISC BUILDING" WAR-TAIL IS ATTACHED TO
`!bCultureOnlyBuilding`, SO IT HARD-REJECTS ALMOST EVERY PREVIOUSLY CLASSIFIED
REGULAR BUILDING DURING WAR/DANGER/WAR-PLAN STATES
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
SAS-specific control-flow attachment error introduced with the building-choice prefilter.
Current source untouched.

CURRENT SOURCE / CONTROL FLOW
-----------------------------
Near the end of the regular-building prefilter, SAS deliberately accumulates the families
already handled above:

    const bool bPreviousBuildings4 = (
        bPreviousBuildings3 ||
        bEspionageBuilding
    );

It then identifies a PURE culture building only when the building has culture and was NOT
already classified:

    const bool bCultureOnlyBuilding = (
        bCultureBuilding &&
        !bPreviousBuildings4
    );

The culture-specific block is followed by a comment that explicitly describes the next branch
as an UNKNOWN / UNCOVERED BUILDING tail and says that it "doesn't handle previously covered
buildings which should have their own war rules":

    if (bCultureOnlyBuilding)
    {
        ... culture-only policy ...
    }
    // unknown or uncovered building ...
    // this doesn't handle previously covered buildings ...
    else
    {
        if (bAtWar || bEnemyStrong || bDanger)
            return 0;
        else if (bWarPlan)
            return 0;
    }

But that `else` means exactly:

    if (!bCultureOnlyBuilding)

It does NOT mean:

    if (building was not matched by any previous family)

Therefore every regular building that was previously classified but whose own category branch
falls through without an immediate return reaches this "unknown" tail and is hard-rejected
whenever `bAtWar`, `bDanger`, `bEnemyStrong`, or `bWarPlan` is true.

The only broad class that escapes the tail by construction is a PURE culture building, because
that is the one case where `bCultureOnlyBuilding` is true. This is the inverse of the intended
category partition: the supposedly conservative fallback for unknown buildings applies to most
known buildings, while pure culture avoids the fallback entirely.

CLEAN SHIPPED GRANARY FIXTURE
-----------------------------
A normal current Granary has `iFoodKept = 40`, so:

    bFoodKeptBuilding == true

Choose an ordinary AI city where:

- the regular-building optimization define is enabled (current shipped default is 1);
- the AI is at war;
- `bEnemyStrong == false` (enemy power is below the strong threshold);
- happiness is not below the food-kept minimum, so the low-happiness reject does not fire;
- the building is otherwise constructible;
- no earlier unrelated force/reject fires.

The authored food-kept branch explicitly preserves this case:

    else if (bAtWar)
    {
        if (bEnemyStrong)
            return 0;
        // else even if enemy is weak, food kept buildings could be useful to slave or grow ...
        // ... no reason to hard reject here ...
    }

So with `bAtWar == true` and `bEnemyStrong == false`, the Granary is intentionally allowed to
fall through to ordinary valuation.

Later:

    bPreviousBuildings  == true   // because bFoodKeptBuilding
    bPreviousBuildings2 == true
    bPreviousBuildings3 == true
    bPreviousBuildings4 == true
    bCultureOnlyBuilding == false // by definition, already-classified buildings cannot be pure culture

The final bare `else` therefore executes and immediately does:

    if (bAtWar || bEnemyStrong || bDanger)
        return 0;

because `bAtWar` is true.

Thus the exact state that the food-kept branch comments say should NOT be hard-rejected is
hard-rejected a few dozen lines later by a branch documented as applying only to UNKNOWN /
UNCOVERED buildings.

OTHER DIRECT CONSEQUENCES OF THE SAME ROOT
------------------------------------------
Do not number these separately; they are consequences of the same wrong `else` attachment.
Examples include:

- Health buildings: their category rejects at war only when `bEnemyStrong`; a non-strong wartime
  case is meant to fall through, but the final tail converts it to `return 0`.
- Production buildings: after the early-growth rule, wartime rejection is likewise explicitly
  limited to strong-enemy pressure; the tail broadens it to all war states that reach it.
- Happiness buildings: same broadening from "enemy strong" to any `bAtWar` that survives the
  category's own rules.
- Land-unit / naval-unit buildings and other already-classified families can similarly inherit
  the fallback veto whenever their own branch deliberately leaves the decision to ordinary
  valuation.

Science, pure economy, pure trade and pure espionage already contain broad war/danger/war-plan
rejects of their own, so F555 does not claim an additional behavioral difference for states
those branches already return from. The bug is the control-flow overreach into previously
classified categories that deliberately chose NOT to return.

DOCUMENTATION ORACLE
--------------------
Current `README_Known_Issues.md` KI#48 documents the intended prefilter as a partition. Its table
states:

    Unknown / misc | Anything not matched above | If war/danger/enemy strong or war plan -> skip

and separately documents Food-kept, Health, Production and Happiness as rejecting at war only
under their narrower category-specific conditions (notably `enemy strong`).

That documentation exactly matches the source comments and contradicts the live bare-`else`
control flow. F555 therefore does not depend on guessing which wartime policy is preferable: the
code fails to implement the project's own recorded category contract.

SHIPPED ENABLEMENT
------------------
Current `GlobalDefines_advciv_sas.xml` has:

    SAS_AI_BUILDING_VALUE_REGULAR_BUILDINGS_OPTIMIZE = 1

so the faulty prefilter is enabled by default in the supplied tree.

ANCESTRY
--------
SAS practical 4950 / commit `e7181173b7498ce4f6ae09140991bdfb88c459eb`, titled
"(Seemingly enhanced) Add prefiltering building choices rules to help AI choose more reponsively
and efficiently buildings depending on war context and other...", introduced the full category
prefilter.

The birth diff already contains:

    bPreviousBuildings4
    bCultureOnlyBuilding = bCultureBuilding && !bPreviousBuildings4
    if (bCultureOnlyBuilding) { ... }
    // unknown or/and uncovered building ...
    // this doesn't handle previously covered buildings ...
    else { wartime/danger/war-plan return 0; }

So the mismatch existed from the feature's birth; it is not a later brace/refactor regression.
Later cleanup commits retained the same attachment. Current source still has it.

DEDUP / ROOT BOUNDARY
---------------------
- KI#48 is the authored feature family and its intended behavior documentation; it does not record
  this control-flow implementation defect. F555 is a concrete bug inside that feature.
- KI#48.2 fixes the definition of immediate happiness buildings; F555 applies after category
  classification and is independent of the happiness helper's correctness.
- KI#48.3 fixes double-counted unhealthy food loss in the growth signal; F555 needs no food-health
  arithmetic error and survives with perfectly correct category inputs.
- F554 / KI#877 is the maintenance classifier wrong-sign root. Maintenance reducers can currently
  reach later categories partly because F554 makes their marker false, but F555 is independently
  witnessed by a correctly classified Granary (`bFoodKeptBuilding == true`). Do not merge them.
- The various war-mode findings F549/F550/F553 concern how military/focus-war state is computed.
  F555 survives even when `bAtWar`, `bDanger`, `bEnemyStrong` and `bWarPlan` are all perfectly
  accurate; the wrong Boolean is the category condition guarding the final tail.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Preserve every category's existing authored rules and restrict the final tail to buildings that
really were not matched above.

At this exact location, because the code is already in the `else` of `bCultureOnlyBuilding`, the
smallest structural correction is:

    else if (!bPreviousBuildings4)
    {
        if (bAtWar || bEnemyStrong || bDanger)
            return 0;
        else if (bWarPlan)
            return 0;
    }

When `bPreviousBuildings4` is false and `bCultureOnlyBuilding` is also false, the building has
neither a prior category nor culture, i.e. it is genuinely unknown/uncovered for this classifier
chain. Previously classified buildings instead fall through to the inherited/general
`AI_buildingValue()` logic exactly as their category comments intend.

An equivalent explicit `bUnknownRegularBuilding` Boolean would be even clearer if desired, but
avoid altering the individual category policy thresholds while fixing this control-flow root.

REGRESSION TESTS
----------------
1. Granary wartime non-strong fixture above:
   - `bFoodKeptBuilding == true`;
   - its own food-kept branch deliberately does not return 0;
   - assert the unknown-tail is NOT entered after the fix.

2. Health / production / happiness known-building controls:
   in wartime states that their own category rules deliberately allow to fall through, assert the
   final unknown-tail does not overwrite that decision.

3. Genuine unknown/misc regular building:
   with every category Boolean false, assert war/danger/war-plan still returns 0 exactly as KI#48
   documents.

4. Pure culture building:
   preserve the culture-specific logic and its inherited fall-through when the early culture
   reject does not fire; the unknown-tail remains skipped.

CURRENT STONE AFTER C031-WIP330
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F555 / KI#878 CONFIRMED.
F556 / provisional KI#879 UNUSED.
Latest physical checkpoint: C031-WIP330.
No repository source files modified.

NEXT
----
Continue under F556 unused:
1. finish the adjacent regular-building classifier/control-flow pass now that both F554 and F555
   have bounded the main failures in this layer;
2. verify whether any remaining category marker uses an accessor with a different unit/scope than
   its comment assumes, but treat intended magnitude thresholds as policy;
3. then return to other current-SAS hard producer/consumer and area/player/domain contracts;
4. if this layer closes, record that saturation and do not manufacture F556.

END C031-WIP330


================================================================================
C031-WIP331 - F556 / KI#879 CONFIRMED
================================================================================

START STATE
-----------
Resumed from C031-WIP330 with F556 / provisional KI#879 UNUSED.
Repository source remained untouched throughout this tranche.

PREFILTER-SPECIFIC FOREIGN-TRADE CANDIDATE BOUNDED FIRST
-------------------------------------------------------
The SAS regular-building trade prefilter reuses inherited `bForeignTrade` as if it meant
"this city currently has a genuinely foreign trade route". Its actual definition is broader:

    if (TEAMID(pTradeCity->getOwner()) != getTeam() ||
        !sameArea(*pTradeCity))
    {
        bForeignTrade = true;
    }

so a domestic/same-team OVERSEAS route also sets it.

That is a real semantic mismatch with the comment, but the new HARD trade-only prefilter does not
currently supply the cleanest shipped consequence: both current buildings with a nonzero foreign
trade-route modifier also classify earlier as economy buildings.

- Custom House: foreign trade modifier +50, plus one Merchant slot and one free Merchant;
- Netherlands Beurs: foreign trade modifier +100, trade-route modifier +50 and +50% Gold.

Therefore `bTradeOnlyBuilding` is false for those current buildings because `bPreviousBuildings2`
already includes `bEconomyBuilding`. Do not claim a current hard-veto failure from the SAS-only
trade-only branch without future XML that makes a foreign-modifier building truly trade-only.

The inherited GENERAL building scorer, however, consumes the same broad Boolean directly and is
fully live. That is the independent F556 root below.

F556 / KI#879 - `AI_buildingValue()` CONFLATES DOMESTIC OVERSEAS TRADE WITH FOREIGN TRADE,
AWARDING `ForeignTradeRouteModifier` VALUE WHEN THE RUNTIME MODIFIER CANNOT APPLY
--------------------------------------------------------------------------------

STATUS
------
CONFIRMED in current supplied AdvCiv-SAS source.
Inherited Civ4/K-Mod-family AI valuation defect retained by Base AdvCiv and SAS.
Current source untouched.

RUNTIME CONTRACT
----------------
Actual trade-route scoring in current `CvCity::totalTradeModifier(CvCity const* pOtherCity)` keeps
OVERSEAS and FOREIGN effects distinct:

    if (!sameArea(*pOtherCity))
        iModifier += OVERSEAS_TRADE_MODIFIER;

    if (getTeam() != pOtherCity->getTeam())
    {
        iModifier += getForeignTradeRouteModifier();
        iModifier += getPeaceTradeModifier(pOtherCity->getTeam());
    }

Thus an own-city / same-team trade partner on a different land area receives the normal overseas
trade bonus but does NOT receive the city's `ForeignTradeRouteModifier`.

AI PROXY CONTRACT
-----------------
`CvCityAI::AI_buildingValue()` constructs one Boolean near the top of the function:

    bool bForeignTrade = false;
    for (each current trade route)
    {
        CvCity* pTradeCity = getTradeCity(i);
        ...
        if (TEAMID(pTradeCity->getOwner()) != getTeam() ||
            !sameArea(*pTradeCity))
        {
            bForeignTrade = true;
            break;
        }
    }

So the Boolean is actually:

    has a different-team route OR has an overseas route

not:

    has a route to a different team.

The broad proxy can make sense for some of its old uses because foreign and overseas routes can
both be more lucrative than ordinary domestic same-area routes. It is not valid as the predicate
for a FOREIGN-ONLY building modifier.

LIVE WRONG CONSUMER
-------------------
In the inherited trade-route part of current `AI_buildingValue()`:

    iTempValue += 5 * kBuilding.getTradeRouteModifier() *
        getTradeYield(YIELD_COMMERCE) / max(1, iTotalTradeModifier);

    if (bForeignTrade)
    {
        iTempValue += 3 * kBuilding.getForeignTradeRouteModifier() *
            getTradeYield(YIELD_COMMERCE) /
            max(1, iTotalTradeModifier + getForeignTradeRouteModifier());
    }

The `if (bForeignTrade)` guard therefore awards positive value for a building's
`ForeignTradeRouteModifier` whenever ANY current route is merely overseas, even if every route is
to the same owner/team and the runtime foreign modifier consequently applies to NONE of them.

A second inherited raw-yield valuation later in the same function repeats the same predicate for
`kBuilding.getForeignTradeRouteModifier()`. Treat both as consumers of one F556 proxy/root, not as
separate findings.

CLEAN SHIPPED FIXTURE
---------------------
Use a coastal AI city whose current trade-route set contains an own-city route on another land
area, but no route to any different team. This is an ordinary domestic intercontinental trade
state.

Then:

    TEAMID(pTradeCity->getOwner()) == getTeam()
    !sameArea(*pTradeCity)          == true
    bForeignTrade                  == true

At runtime for that partner:

    !sameArea(*pOtherCity)         == true  -> overseas bonus applies
    getTeam() != pOtherCity->getTeam() == false
                                      -> ForeignTradeRouteModifier does NOT apply

Current shipped XML supplies live buildings whose valuation is affected:

    BUILDING_CUSTOM_HOUSE
        iCost = 160
        iForeignTradeRouteModifier = +50

    BUILDING_NETHERLANDS_BEURS
        iCost = 200
        iTradeRouteModifier = +50
        iForeignTradeRouteModifier = +100

For either building, if the city has positive current trade commerce, the AI adds a positive
foreign-modifier component even though the same-team overseas route receives zero such modifier
when the building is actually completed. The Beurs still has a real ordinary +50% trade-route
modifier and Gold effects; F556 does not claim the entire building is worthless. It claims the
FOREIGN component is phantom in this state. The Custom House also has current SAS Merchant effects;
again, only the foreign-route component is misvalued.

This is a value-ranking defect rather than construction-legality failure, but the false component
feeds the authoritative `AI_buildingValue()` used by generic AI building selection and can alter
which constructible building wins.

ANCESTRY / CONTROL
------------------
The broad `bForeignTrade` definition is not a recent SAS invention:

- the supplied Civ4CE reference has the same `different team OR different area` proxy and uses it
  to value `getForeignTradeRouteModifier()`;
- K-Mod 1.46 retains the same proxy and the same foreign-modifier consumer;
- Base AdvCiv 1.14 retains both;
- current SAS retains both, with local refactoring/caching around them.

K-Mod changed the detailed trade-value arithmetic relative to the older Civ4-style code but did
not separate the two concepts. The mismatch therefore survives across the supplied ancestry
controls.

The runtime contract in current `CvCity::totalTradeModifier()` remains explicit: overseas is
keyed by area; foreign modifier is keyed by team. The AI proxy merges those two independent axes.

DEDUP / ROOT BOUNDARY
---------------------
- F554 is the SAS maintenance-classifier sign inversion; unrelated field and branch.
- F555 is the final regular-building war-tail attachment; it survives with perfectly classified
  trade state and says nothing about trade-yield arithmetic.
- Existing KI#197 domain/denominator families concern military/naval production scopes, not trade
  route foreign-vs-overseas identity.
- No prior album root was found for `bForeignTrade`, `ForeignTradeRouteModifier`, Custom House or
  the `iGlobalTradeValue` consumer.
- The SAS-only trade prefilter's reuse of `bForeignTrade` is an adjacent expression of the same
  semantic naming/proxy hazard, but current XML classification prevents it from being the clean
  hard shipped witness. Keep it under F556 rather than manufacture another root.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not destroy the old broader information, because some inherited uses intentionally use
"foreign OR overseas" as a rough signal for more lucrative trade.

Instead split the predicates explicitly, for example:

    bool bHasForeignTrade = false;
    bool bHasOverseasTrade = false;

and while scanning current routes set them independently:

    bHasForeignTrade |= (pTradeCity->getTeam() != getTeam());
    bHasOverseasTrade |= !sameArea(*pTradeCity);

Then:

- guard `getForeignTradeRouteModifier()` valuation and the SAS foreign-only prefilter with
  `bHasForeignTrade`;
- preserve a combined `bHasForeignTrade || bHasOverseasTrade` signal only at old consumers where
  that broader rough profitability heuristic is genuinely intended.

This is safer than simply deleting `|| !sameArea(...)` from the existing Boolean globally.

REGRESSION TESTS
----------------
1. Domestic overseas only:
   own-city route on another land area, no different-team route.
   - `bHasOverseasTrade == true`;
   - `bHasForeignTrade == false`;
   - foreign-modifier AI component must be zero.

2. Foreign same-area route:
   - `bHasForeignTrade == true`;
   - foreign-modifier component remains valued.

3. Foreign overseas route:
   both signals true; foreign modifier remains valued and generic overseas-profit heuristics can
   still use the broader signal where intended.

4. No trade routes:
   both false; preserve zero foreign-modifier valuation.

CURRENT STONE AFTER C031-WIP331
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F556 / KI#879 CONFIRMED.
F557 / provisional KI#880 UNUSED.
Latest physical checkpoint: C031-WIP331.
No repository source files modified.

NEXT
----
Continue under F557 unused:
1. finish the remaining regular-building classifier/value boundary now that sign, tail-control and
   foreign-vs-overseas scopes have each been bounded;
2. inspect any other reused inherited proxy whose broad meaning is narrower at a new SAS consumer;
3. move outward from `AI_buildingValue()` once the live SAS-specific prefilter surface saturates;
4. preserve a control checkpoint rather than force F557.

END C031-WIP331


================================================================================
C031-WIP332 - POST-F556 CONTROL CHECKPOINT / F557 / KI#880 UNUSED
================================================================================

START STATE
-----------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN under deep-method re-audit.
32/169 unique files complete.
F446 / KI#769 through F556 / KI#879 remain CONFIRMED.
F557 / provisional KI#880 remains UNUSED.
Authoritative current source: user-supplied
`AdvCiv-SAS_light_source_20260904T202954.zip`.
Base/ancestry controls: supplied AdvCiv 1.14, K-Mod 1.46 and Civ4CE material.
Repository source remains untouched.

POST-WIP331 CONTROL TRANCHE
---------------------------

1. SMALL BUILDING SELECTOR LAYER - CLOSED
------------------------------------------
The immediate selectors around the newly-audited building prefilter were finished before
moving outward:

- `AI_bestBuildingThreshold`: the meaningful SAS semantic guard is the already-owned
  negative `WonderConstructRand` protection (KI#72); no second threshold/return root
  survived.
- `AI_bestProcess`: Base-identical selection semantics.
- `AI_chooseProcess`: current SAS delta is diagnostic logging only.
- `AI_chooseBuilding`: the gameplay delta is the already-bounded Barbarian world-wonder
  veto; a rejected wonder falls through to the caller's broader production policy rather
  than stranding the queue.

No F557.

2. `GAMEOPTION_NO_BARBARIANS` / BORDER-OBSTACLE WONDER - FALSE POSITIVE CLOSED
-------------------------------------------------------------------------------
The SAS anti-Barbarian-border-wonder gate initially looks backwards because the late-era
rejection is entered only when `!GAMEOPTION_NO_BARBARIANS`.

Runtime info mutation closes the concern: when No Barbarians is enabled, `CvGame` disables
the effective building `AreaBorderObstacle` flag through
`CvBuildingInfo::setAreaBorderObstacleEnabled(...)`. Consequently the inner
`kBuilding.isAreaBorderObstacle()` predicate cannot identify the Great-Wall-style effect in
that ruleset anyway. The outer option test is redundant-looking but does not create a live
wrong branch.

No F557.

3. SAS TOP-HAMMER-CITY CACHE STALENESS - REAL, BUT ALREADY F008 / KI#306
------------------------------------------------------------------------
The current building prefilter's function-static top-HPT cache is keyed by player, game turn
and number of cities. Base production can change later in the same game turn without changing
city count, so later city evaluations can consume an obsolete top-1/top-2/top-3 ranking.

Album dedup found the exact same root already preserved as F008 / reopened KI#306, including
the insufficient `(turn + numCities)` key and sequential-city-turn witness. Do not spend F557
on this rediscovery.

4. K-MOD -> ADVCIV LOST-STATE REFACTOR SIEVE - F533 REMAINS ISOLATED
---------------------------------------------------------------------
A targeted ancestry diff searched for the mechanical pattern that created F533: K-Mod state
assignment removed or bypassed while an AdvCiv refactor retains only `break`/`continue`.
Candidate hunks were reconstructed rather than judged textually.

The strong rediscovery was the culture-victory prerequisite loop where Base/SAS `break` on an
insufficient prerequisite building class without setting `bHaveEnough=false`. That is exactly
F533 / KI#856 (AdvCiv practical 1808), not F557.

Other candidate hunks checked in this pass preserve their state updates or are deliberate loop
rewrites (including the `bBelowMedian`, hurry-validity and `bChooseWorker` shapes). No sibling
lost-state regression survived.

5. REGULAR-BUILDING CLASSIFIER DATA/SIGN BOUNDARY - CLOSED BEYOND F553-F556
---------------------------------------------------------------------------
The neighboring current XML fields were checked against the units/signs assumed by the SAS
classifier instead of inferring them from names.

Current shipped examples confirm:
- bombard / city-defense effects are positive-domain;
- espionage-defense modifiers are positive-domain;
- food-kept values are positive-domain;
- trade-route and trade-route-modifier values are positive-domain;
- maintenance reducers are negative-domain, which is already the F554 sign inversion;
- foreign-vs-overseas route semantics remain the separate F556 root.

The remaining classifier thresholds can be debated as tuning, but no additional shipped sign,
receiver or unit contradiction was established in this tranche. F555 continues to own the
misattached final war-tail control-flow failure.

6. WONDER COST / GAME-SPEED / SENTINEL ARITHMETIC - CLOSED CONTROL
------------------------------------------------------------------
The wonder viability gates were checked against `CvPlayer::getProductionNeeded(BuildingTypes)`.
The required building cost already scales by the game speed's `ConstructPercent`; SAS scales its
Normal-speed cost and turn-window thresholds by the same construct factor, so the examined code
does not double-apply game-speed scaling.

The `AI_BUILDING_ALWAYS_PICK_FIRST = 100000` sentinel was also challenged through the nearby
`AI_bestBuildingThreshold` multiplication path. The live path contains the inherited
`FAssert(MAX_INT / 1000 > iValue)` before the final `*1000`, and current building cost/count
ranges did not produce a new demonstrated overflow root. Preserve the existing historical
999999 crash rationale; no F557 from the current 100000 value here.

7. LOW-COVERAGE HELPER MAP - COVERAGE FALSE POSITIVES CLOSED
-------------------------------------------------------------
Literal method-name frequency understated prior thematic coverage. Rechecking the album before
re-auditing prevented several duplicates:

- `AI_getYieldMultipliers`: WIP288 already proved the current shipped-data denominator chains;
  WIP314 explicitly reclosed it together with the plot/yield magic-value helpers. Current SAS is
  semantically Base AdvCiv in this helper apart from signature/style. The Barbarian early return
  is inherited AdvCiv policy, not a fresh SAS root shown here.
- `AI_cityThreat`: WIP292/WIP307 already bounded the helper; AdvCiv advc.022 fixed K-Mod's
  wrong-direction-attitude concern. Current SAS changes are cached references/formatting.
- `AI_doDraft`, `AI_citizenValue`, `AI_experienceWeight`,
  `AI_specialPlotImprovementValue`, `AI_getTargetPopulation`,
  `AI_timeWeightedImprovementYields` and floating-defender helpers all have deeper thematic
  stones than raw literal mention counts suggest.

8. `AI_calculateWaterWorldPercent` / ITERATOR SCOPE - CLOSED CONTROL
-------------------------------------------------------------------
The lightly-mentioned water-world helper was reconstructed with current `AgentIterator`
semantics. The AdvCiv iterator cleanup preserves the intended same-team/same-master-vassal
scope, and the count/range arithmetic did not expose a new denominator or owner/team mismatch.

CURRENT STONE AFTER C031-WIP332
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` - OPEN.
32/169 unique files complete.
F446 / KI#769 through F556 / KI#879 CONFIRMED.
F557 / provisional KI#880 UNUSED.
Latest physical checkpoint: C031-WIP332.
Repository source untouched.

EXACT NEXT CURSOR
-----------------
Continue under F557 unused.

1. Leave the now heavily saturated regular-building prefilter/selectors unless a new objective
   contract points back into them.
2. Continue the K-Mod -> AdvCiv semantic-loss sieve beyond `break`/`continue`, especially
   wrong receiver / lost assignment / changed enum-domain refactors.
3. Prefer substantial current-SAS producer/consumer helpers with hard state/count/domain
   contracts over policy-heavy valuation weights.
4. Dedup against the album before promoting every familiar-looking candidate.
5. If another broad control tranche closes without an independent live root, preserve another
   checkpoint and seriously consider Queue 005 saturation rather than manufacturing F557.

END C031-WIP332


===============================================================================
C031-WIP333 — QUEUE 005 CvCityAI.cpp — F557 / KI#880 CONFIRMED
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.

Unique files complete: 32/169.
F446-F557 confirmed across the current protected re-audit span (with the previously
recorded duplicate/retracted numbering exceptions retained in the album header).
F558 / provisional KI#881 UNUSED.
No repository source files were modified.

PRE-F557 CONTROL — `AI_chooseUnit(UnitTypes, UnitAITypes)` SUCCESS CONTRACT
-------------------------------------------------------------------------
The post-WIP332 concrete-unit return-contract suspicion was closed before promoting
F557.

The overload returns `true` once its input concrete unit is non-`NO_UNIT`, while the
actual `pushOrder` is guarded by `eChangedUnitAI != NO_UNITAI`. In isolation this looks
capable of reporting success without queuing an order.

Current shipped callers do not produce that state:

* direct concrete-unit call sites pass an explicit non-`NO_UNITAI` role;
* the generic `AI_chooseUnit(UnitAITypes, ...)` path asks `AI_bestUnit(..., &eUnitAI)`;
* `AI_bestUnit` writes the output UnitAI in the same winning branch that writes the
  winning concrete UnitTypes.

No live caller was found that supplies/receives a concrete unit while leaving the role
as `NO_UNITAI`. The guard is therefore awkward/future-fragile but not a current F557.

-------------------------------------------------------------------------------
F557 / provisional KI#880 — AdvCiv sign regression reverses the immediate-anger
change from building happiness in `AI_buildingValue`
-------------------------------------------------------------------------------

ROOT
----
Current SAS inherits this AdvCiv expression in the happiness valuation block:

    int iBuildingActualHappiness = getAdditionalHappinessByBuilding(
            eBuilding, iGood, iBad);

    int iAngerDelta = std::max(0,
            -iHappinessLevel + iBuildingActualHappiness)
            - std::max(0, -iHappinessLevel);

    // High value for any immediate change in anger.
    iValue -= iAngerDelta * 4 * iCitizenValue;

The building effect has the wrong sign inside the projected post-building anger term.

`CvCity::getAdditionalHappinessByBuilding` explicitly documents that it returns the
"total additional happiness" from adding the building, and its return value is:

    iGood - iBad - iStarting

Thus a building that adds one net happiness supplies
`iBuildingActualHappiness == +1`.

If `iHappinessLevel` is the current happiness surplus, then current angry population
from this simplified threshold is:

    max(0, -iHappinessLevel)

and projected angry population after gaining `B` happiness must be:

    max(0, -(iHappinessLevel + B))

or equivalently:

    max(0, -iHappinessLevel - B)

Current AdvCiv/SAS instead computes:

    max(0, -iHappinessLevel + B)

which treats positive happiness as if it were additional unhappiness for this term.

MINIMAL SHIPPED FIXTURE
-----------------------
Current SAS contains ordinary constructible +1-happiness buildings, e.g.
`BUILDING_COLOSSEUM` has:

    iHappiness = 1

Take a city whose evaluated `iHappinessLevel == -1` and a building whose net projected
additional happiness is +1.

Before the building:

    angry threshold = max(0, 1) = 1

Correct post-building threshold:

    max(0, -(-1 + 1)) = 0

Correct anger delta:

    0 - 1 = -1

The current code instead calculates:

    max(0, -(-1) + 1) - 1
  = max(0, 2) - 1
  = +1

So a building that removes one angry citizen is represented by the variable named
`iAngerDelta` as ADDING one angry citizen.

The next line then compounds the inversion:

    iValue -= iAngerDelta * 4 * iCitizenValue;

Correct K-Mod arithmetic gives `iAngerDelta == -1`, producing a positive immediate
value of `+4 * iCitizenValue`.

Current AdvCiv/SAS gives `iAngerDelta == +1`, producing the opposite
`-4 * iCitizenValue` penalty.

The following "positive change bias" also keys on `iAngerDelta < 0`; current code can
therefore suppress that bonus precisely when positive building happiness actually
reduces anger.

The separate later padding term for positive happiness does not repair this root. It
is an additional heuristic term; the explicitly immediate angry-citizen term still has
its sign reversed and can materially distort total `AI_buildingValue` and therefore
building ranking/construction decisions.

ANCESTRY / INTRODUCTION
-----------------------
K-Mod has the correct expression:

    int iAngerDelta = std::max(0,
            -(iHappinessLevel + iBuildingActualHappiness))
            - std::max(0, -iHappinessLevel);

The supplied reachable AdvCiv history identifies the regression exactly.

AdvCiv practical commit 2719,
commit `529ebb7b4f59681b609c10c4d3ad31838ba62d67`, titled:

    Improve AI support for mods with custom eras

refactored this block to use the newer `iCitizenValue`. In that same hunk it changed:

    - std::max(0, -(iHappinessLevel+iBuildingActualHappiness))
    + std::max(0, -iHappinessLevel+iBuildingActualHappiness)

The era-support objective gives no semantic reason to reverse building happiness; the
sign loss occurs incidentally alongside the citizen-value refactor.

Later reachable commits (including practical 2920/2954/3392) only reformat/preserve the
already-regressed expression. Base AdvCiv 1.14 and current SAS retain it.

Civ4CE does not contain this K-Mod happiness-valuation block in the supplied reference;
the relevant positive control is therefore K-Mod -> AdvCiv ancestry rather than an
original-BtS/Civ4CE comparison.

DEDUP / ROOT BOUNDARY
---------------------
No existing album finding owns this immediate-building-happiness sign inversion.

It is distinct from:

* F355 — event building happiness/health multiplicity;
* F367 — cross-city mutation of `iExtraPop` in player happiness/health weighting;
* F397 — LuxuryModifier omitted by the BUG additional-building-happiness preview;
* KI#48.2 — SAS regular-building prefilter classification for immediate happiness;
* F555 — the later SAS unknown/misc building-tail control-flow overreach.

F557 occurs inside inherited `AI_buildingValue` after a building has reached the normal
happiness valuation and survives even when `getAdditionalHappinessByBuilding` returns a
perfectly correct value.

REPAIR DIRECTION
----------------
Restore the post-building anger expression to the K-Mod/sign-correct form, e.g.:

    int iAngerDelta = std::max(0,
            -iHappinessLevel - iBuildingActualHappiness)
            - std::max(0, -iHappinessLevel);

or equivalently retain the original parenthesized K-Mod form:

    int iAngerDelta = std::max(0,
            -(iHappinessLevel + iBuildingActualHappiness))
            - std::max(0, -iHappinessLevel);

Do not change the later subtraction from `iValue`; with the correct delta sign, reduced
anger is negative and therefore correctly adds value.

REGRESSION TESTS
----------------
1. `iHappinessLevel=-1`, building happiness `+1`:
   assert `iAngerDelta == -1`, not `+1`.
2. `iHappinessLevel=-2`, building happiness `+1`:
   assert anger changes `2 -> 1`, delta `-1`.
3. `iHappinessLevel=+1`, building happiness `+1`:
   assert both before/after anger are zero and delta remains 0.
4. If a negative net-happiness building/mod fixture is supplied by a mod, verify the
   opposite direction as well: losing happiness must increase rather than reduce anger.
5. Compare the repaired arithmetic against K-Mod for a table of `(H,B)` values spanning
   negative, zero and positive happiness surplus.

CURRENT STONE AFTER C031-WIP333
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F557 / KI#880 CONFIRMED.
F558 / provisional KI#881 UNUSED.
Latest physical checkpoint: C031-WIP333.
No repository source files modified.

NEXT
----
Continue under F558 unused:
1. finish the K-Mod -> AdvCiv arithmetic/operator-change sieve around the remaining
   `AI_buildingValue` and citizen/improvement valuation hunks, validating full context
   rather than trusting adjacent diff pairs;
2. continue the current-SAS `AI_chooseUnit` final hard-gate producer/consumer/domain
   proof with F549/F550/F553 and KI#53/KI#193/KI#197 treated as owned boundaries;
3. promote only a separate live root; otherwise preserve saturation controls and move
   toward Queue 005 closure rather than manufacturing F558.

END C031-WIP333

===============================================================================
C031-WIP334 — QUEUE 005 CvCityAI.cpp — F558 / KI#881 CONFIRMED
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.

Unique files complete: 32/169.
F446-F558 confirmed across the current protected re-audit span (with the previously
recorded duplicate/retracted numbering exceptions retained in the album header).
F559 / provisional KI#882 UNUSED.
No repository source files were modified.

POST-WIP333 CONTROLS PRESERVED BEFORE F558
------------------------------------------
1. `AI_chooseUnit(UnitTypes, UnitAITypes)` false-success concern remains CLOSED.
   Every shipped direct concrete-unit caller supplies a real UnitAI role, while the
   generic overload obtains the concrete unit and output UnitAI from the same winning
   `AI_bestUnit` branch. No live path was found where the overload returns success while
   its `eChangedUnitAI == NO_UNITAI` guard prevents `pushOrder`.

2. The remaining final EXPLORE / WORKER / SPY / Settler hard gates did not expose an
   independent root after dedup against KI#53 / KI#192 / KI#193 / KI#197 and the already
   confirmed F549/F550/F553 families.

3. F557's immediate-happiness sign regression has a useful sibling positive control:
   the corresponding health expression still subtracts the building's positive health
   contribution with the expected sign. This supports F557 as an isolated one-line
   AdvCiv refactor regression rather than a changed sign convention.

4. F397 breadth extension, NOT A NEW ROOT:
   the already-confirmed LuxuryModifier omission in
   `getAdditionalHappinessByBuilding` is also consumed by `AI_buildingValue`, so F397
   can distort AI building valuation in addition to the previously documented preview /
   help-facing consequence. This is the same helper omission and therefore does not
   consume a new finding ID.

5. `AI_strictAdditionalHappy` No-Unhappiness arithmetic remains FUTURE-PROOFING ONLY.
   Its comment says a No-Unhappiness building should contribute zero when the city is
   already happy, but the helper adds raw `unhappyLevel()` rather than current net angry
   population. Current shipped data masks the mistake: the only `bNoUnhappiness=1`
   building is the Globe Theatre, a National Wonder, while all current consumers of
   `AI_strictAdditionalHappy` in this prefilter are inside the regular `!bWonder` path.
   Do not number from current data.

6. The live divisor/sentinel sweep performed around the neighboring prefilter and
   concrete-unit gates did not establish another zero-divisor or `-1` sentinel leak.

-------------------------------------------------------------------------------
F558 / provisional KI#881 — SAS regular-building difficulty skew reads the AI
PLAYER handicap instead of the GAME handicap that actually controls AI research cost
-------------------------------------------------------------------------------

ROOT
----
SAS practical 4950 introduced two handicap references near the start of the custom
`AI_buildingValue` prefilter:

    CvHandicapInfo const& hGame = GC.getInfo(kGame.getHandicapType());
    CvHandicapInfo const& hAI   = GC.getInfo(kOwner.getHandicapType());

The regular espionage-only building branch later tries to infer the AI's actual research
cost advantage/disadvantage:

    const int iHumanResearchPercent = hGame.getResearchPercent();
    const int iAIResearchPercent    = hAI.getAIResearchPercent();
    const int iResearchPercentGap =
            iHumanResearchPercent - iAIResearchPercent;

and interprets:

    gap <= -20  => AI pays much more for research; strongly prefer EP offense;
    gap >= +30  => AI pays much less for research; strongly prefer EP defense.

`hAI` is the wrong receiver for that question.

AUTHORITATIVE RUNTIME CONTRACT
------------------------------
Current AdvCiv/SAS research cost is computed in `CvTeam::getResearchCost`.
For a nonhuman non-Barbarian team it explicitly says:

    // Important to use game handicap here (not team handicap)

and applies:

    GC.getInfo(kGame.getHandicapType()).getAIResearchPercent()
        + kGame.AIHandicapAdjustment()

The acting AI player's own handicap entry is not the source of the
`AIResearchPercent` multiplier used by actual technology cost.

That distinction is deliberate throughout AdvCiv. `CvPlayer::init` contains another
historical correction whose comment states that using `this->getHandicapType()` meant
"Noble" and therefore made an AI difficulty block do nothing; that code was repaired by
reading `kGame.getHandicapType()` instead.

WHY ORDINARY SAS GAMES ARE ESPECIALLY CLEAR
-------------------------------------------
`CvInitCore::resetPlayer` initializes player handicaps from `STANDARD_HANDICAP`.
AdvCiv's own nearby historical comment identifies the ordinary AI player value as
Noble. Outside special Rise & Fall reassignment, an ordinary AI therefore reads the
Noble `CvHandicapInfo` through `kOwner.getHandicapType()` even when the human-selected
GAME handicap is Rookie, Monarch, Immortal, Deity, etc.

Current SAS also sets `iAIHandicapIncrementTurns = 0` on every one of its 11 handicap
entries, so:

    kGame.AIHandicapAdjustment() == 0

throughout current shipped ordinary games. The actual AI-research multiplier is
therefore exactly the GAME handicap's XML `iAIResearchPercent`, while the prefilter
reads the AI player's Noble value.

CURRENT SHIPPED HANDICAP DATA
-----------------------------
Current SAS uses `iResearchPercent = 100` for the human at every handicap and:

    Rookie       AIResearchPercent = 124
    Settler      AIResearchPercent = 118
    Chieftain    AIResearchPercent = 112
    Warlord      AIResearchPercent = 106
    Noble        AIResearchPercent = 100
    Prince       AIResearchPercent =  94
    Monarch      AIResearchPercent =  88
    Emperor      AIResearchPercent =  82
    Immortal     AIResearchPercent =  76
    Deity        AIResearchPercent =  70
    Deity Plus   AIResearchPercent =  64

Thus the intended real gap spans:

    Rookie:      100 - 124 = -24
    Deity:       100 -  70 = +30
    Deity Plus:  100 -  64 = +36

but an ordinary AI's `hAI` lookup uses Noble 100 and collapses the custom prefilter gap
to:

    100 - 100 = 0

on all those ordinary difficulty settings.

The feature is therefore not merely imprecise: its two shipped threshold cases can be
silently disabled by the wrong receiver.

LIVE SHIPPED BUILDING FIXTURES
------------------------------
The regular-building prefilter is enabled by default:

    SAS_AI_BUILDING_VALUE_REGULAR_BUILDINGS_OPTIMIZE = 1

Current XML provides clean espionage-only consumers that reach the difficulty block when
not at war, not in danger and not executing a war plan.

A. `BUILDING_INTELLIGENCE_AGENCY`

    CommerceModifier(COMMERCE_ESPIONAGE) = +50
    Spy specialist slots                  = 2
    no health / happiness / maintenance / production / science / gold / trade role
      that would classify it in an earlier regular-building category

It therefore qualifies as an espionage source building.

On Rookie the actual research-cost gap is -24, satisfying the authored
`iEPOffGap = -20`. The intended branch should strongly prefer the espionage source:

    AI_BUILDING_ALWAYS_PICK_FIRST - 1000

The wrong Noble `hAI` receiver yields gap 0 instead, so that priority never fires.

B. `BUILDING_SECURITY_BUREAU`

    EspionageDefenseModifier = +50
    flat espionage           = +8
    Spy specialist slots     = 2
    no earlier regular-building category under ordinary city state

It qualifies both as an espionage source and, importantly for the high-difficulty
branch, as high-enough espionage defense.

On Deity the actual gap is exactly +30; on Deity Plus it is +36. Both satisfy the
`iEPDefGap = +30` condition and should strongly prefer the defensive building. The
wrong Noble receiver again yields gap 0 and suppresses that intended priority.

This is a direct producer/consumer failure in live AI building selection, not merely a
comment mismatch or theoretical mod configuration.

SECOND CONSUMER OF THE SAME WRONG RECEIVER — CURRENTLY DATA-MASKED
------------------------------------------------------------------
The wonder timing gate later reuses the same `hAI` object:

    int const iAIWCP = hAI.getAIWorldConstructPercent();

and compares it against an assumed human 100% world-wonder construction cost.

Actual `CvPlayer::getProductionNeeded(BuildingTypes)` again obtains the AI world-wonder
cost modifier from:

    GC.getInfo(kGame.getHandicapType()).getAIWorldConstructPercent()

plus the game AI handicap adjustment. So the receiver is wrong there by the same
contract too.

Current SAS XML, however, has `iAIWorldConstructPercent = 100` at all 11 handicap
levels. The wrong and right lookups therefore coincide today. Preserve this as a
same-root future-proofing manifestation, not an additional current finding.

ANCESTRY / INTRODUCTION
-----------------------
This is SAS-authored rather than inherited from BtS/K-Mod/Base AdvCiv.

Reachable SAS practical commit 4950,
commit `e7181173b7498ce4f6ae09140991bdfb88c459eb`, titled:

    (Seemingly enhanced) Add prefiltering building choices rules to help AI choose
    more reponsively and efficiently buildings depending on war context and other...

introduced the regular-building prefilter and, in the same patch, introduced:

    CvHandicapInfo const& hAI = GC.getInfo(kOwner.getHandicapType());

plus both the espionage research-gap use and the wonder `AIWorldConstructPercent` use.
Current SAS retains them.

Base AdvCiv, K-Mod and Civ4CE do not contain this SAS prefilter, so they serve as
runtime-contract/ancestry controls rather than carriers of F558 itself.

DEDUP / ROOT BOUNDARY
---------------------
No previous album finding owns this wrong-handicap receiver.

It is distinct from:

* KI#48 — the authored SAS building-prefilter feature and its existing tuning/fixes;
  F558 is a mechanical failure of the difficulty signal that KI#48's espionage table
  assumes is real.
* F554 — maintenance-building sign inversion in the same prefilter.
* F555 — misattached unknown/misc wartime tail in the same prefilter.
* F556 — inherited foreign-vs-overseas trade-route valuation mismatch.
* F557 — inherited AdvCiv immediate-happiness sign regression later in
  `AI_buildingValue`.
* AdvCiv's older fixed Advanced-Start handicap receiver bug — that historical comment
  is a positive control demonstrating the player-handicap/Noble trap, not the same
  current call site.

REPAIR DIRECTION
----------------
For the espionage research-cost skew, derive the AI factor from the same source used by
actual research cost, e.g. conceptually:

    CvHandicapInfo const& hGame = GC.getInfo(kGame.getHandicapType());
    const int iAIResearchPercent =
            hGame.getAIResearchPercent() + kGame.AIHandicapAdjustment();

Current data has zero dynamic adjustment, but including it keeps the prefilter aligned
with the authoritative cost contract if the XML tuning changes later.

For the wonder cost-gap manifestation, likewise read `AIWorldConstructPercent` from the
GAME handicap (and account for the same AI handicap adjustment if the intended quantity
is actual construction cost), rather than from `kOwner.getHandicapType()`.

Do not simply change every use of player handicap globally; player handicap is
legitimate for many human/player-specific modifiers. F558 is specifically about code
claiming to model AI research/world-wonder COST modifiers whose runtime implementations
explicitly use game handicap.

REGRESSION TESTS
----------------
1. Ordinary Deity game, non-Rise-&-Fall AI, peaceful/safe city able to construct
   Security Bureau:

       human ResearchPercent = 100
       game AIResearchPercent = 70
       gap = +30

   Assert the EP-defense priority condition is true.

2. Ordinary Rookie game, peaceful/safe city able to construct Intelligence Agency:

       human ResearchPercent = 100
       game AIResearchPercent = 124
       gap = -24

   Assert the EP-offense priority condition is true.

3. Noble baseline:

       100 - 100 = 0

   Assert neither difficulty priority is forced solely by the handicap skew.

4. If nonzero `iAIHandicapIncrementTurns` is re-enabled in a test XML, assert the
   prefilter's computed AI research multiplier tracks the same
   `AIHandicapAdjustment()` used by `CvTeam::getResearchCost`.

5. For world wonders, temporarily give two handicap entries different
   `iAIWorldConstructPercent` values and assert the prefilter's gap matches
   `CvPlayer::getProductionNeeded(BuildingTypes)`, not the AI player's ordinary Noble
   handicap entry.

CURRENT STONE AFTER C031-WIP334
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains OPEN.
32/169 unique files complete.
F446 / KI#769 through F558 / KI#881 CONFIRMED.
F559 / provisional KI#882 UNUSED.
Latest physical checkpoint: C031-WIP334.
No repository source files modified.

NEXT
----
Continue under F559 unused:
1. finish the remaining wrong-handicap / wrong-scope receiver scan in live
   `CvCityAI.cpp`, now treating F558's player-vs-game handicap distinction as owned;
2. continue K-Mod -> AdvCiv arithmetic/operator/receiver semantic-loss hunks outside the
   already saturated regular-building prefilter;
3. finish the current-SAS concrete-unit final hard-gate controls only where an objective
   producer/consumer contract remains unowned;
4. if those close too, preserve the saturation evidence and evaluate Queue 005 closure
   instead of manufacturing F559.

END C031-WIP334


===============================================================================
C031-WIP335 — QUEUE 005 CLOSURE — FINAL LOW-COVERAGE / DIFFERENTIAL SATURATION
===============================================================================

STATUS
------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` is CLOSED for the planned deep-method re-audit.
32/169 unique files remain complete in the outer queue; the protected re-audit cursor advances to Queue 006.
F521 / KI#844 through F558 / KI#881 are the confirmed new-root span from this Queue-005 re-audit, with older roots also broadened/reopened where documented.
F559 / provisional KI#882 remains UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260904T202954.zip`.
No repository source files modified.

POST-WIP334 HANDICAP-RECEIVER FAMILY — EXHAUSTED
------------------------------------------------
F558's wrong-handicap receiver pattern was deliberately expanded to every remaining live
handicap lookup in `CvCityAI.cpp` before closure.

The only two consumers of the SAS-local `hAI = GC.getInfo(kOwner.getHandicapType())` are:
- the live espionage research-gap signal owned by F558;
- the world-wonder AI construction-percent signal, which has the same wrong receiver but is
  currently data-masked because shipped `iAIWorldConstructPercent` is 100 on every handicap.

Other live handicap references in the file use the game handicap for game-level rules or are
unrelated game-speed/Barbarian settings. No second independent receiver root survives.

K-MOD -> ADVCIV OPERATOR / LOST-STATE SIEVE — CLOSED
----------------------------------------------------
The post-F558 ancestry pass mechanically challenged operator/sign/condition changes where
AdvCiv refactored recognizable K-Mod logic.

The dramatic-looking positive-condition -> negated-condition cases examined are equivalent
loop flattenings (`continue`/early-return forms), not semantic inversions. The sieve correctly
rediscovers F557's happiness-sign regression and F533's missing prerequisite-state update,
which is useful validation that the method is sensitive to real refactor losses; no unowned
sibling survives.

The health analogue adjacent to F557 remains a positive control: its post-building health
calculation keeps the expected `-healthLevel - buildingHealth` sign structure.

F397 BREADTH EXTENSION — SAME ROOT, NOT F559
--------------------------------------------
The previously owned `getAdditionalHappinessByBuilding()` LuxuryModifier omission is not only
a preview/help discrepancy. `AI_buildingValue()` also consumes that projection, so the same
root can distort AI valuation of happiness buildings under a non-default LuxuryModifier.
This broadens F397 only; no new root number is consumed.

MASKED `AI_strictAdditionalHappy()` NO-UNHAPPINESS NOTE
-------------------------------------------------------
The helper's `isNoUnhappiness()` branch says the contribution should be zero if the city is
already happy, but adds raw `unhappyLevel()` rather than current net angry population. That
would overstate a No-Unhappiness building in a city whose happiness already offsets all raw
unhappiness.

Current shipped data masks the path in this prefilter: Globe Theatre is the only current
`bNoUnhappiness=1` building and it is a National Wonder, while all live
`AI_strictAdditionalHappy()` consumers here are inside the regular `!bWonder` prefilter.
Preserve as future-proofing debt, not F559.

FINAL `AI_chooseUnit` RETURN / HARD-GATE CONTRACT CONTROLS
---------------------------------------------------------
The concrete-unit overload's guarded `pushOrder` cannot report success without queueing for a
shipped caller:
- every direct concrete-unit caller passes an explicit non-`NO_UNITAI` role;
- the generic overload gets the winning concrete unit and output UnitAI from the same
  `AI_bestUnit` winning branch.

The remaining EXPLORE / WORKER / SPY / Settler final hard gates were rechecked from the final
resolved concrete unit and UnitAI. Once KI#53, KI#192, KI#193, KI#197 and the already-confirmed
F549/F550/F553 boundaries are excluded, no independent role/domain/availability failure
survives.

MUTABLE FUNCTION-STATIC / CACHE LIFETIME SWEEP — SATURATED
----------------------------------------------------------
Every mutable function-static/cache-like state in current `CvCityAI.cpp` was enumerated.

The meaningful live cases are already owned/bounded:
- top-hammer-city cache keyed by turn + city count -> F008 / reopened KI#306 same-turn
  staleness family;
- one-entry direct-Artist culture-value-percent cache -> previously recorded same-turn lag,
  repeatedly bounded as delayed dampening rather than a sufficiently strong independent root.

No third mutable cache with an incomplete key/lifetime contract was found.

LOW-LITERAL-COVERAGE HELPER CONTROLS
------------------------------------
`AI_defianceAngerCost()` was reopened from the final coverage census, then deduplicated to
WIP314. WIP314 already traced the actual Defy Resolution religion applicability and anger
mechanics and classified the active-timer `*2` as explicit extra discouragement of repeated
denial rather than an exact simulation contract.

The apparent zero-coverage `AI_buildingSpecialYieldChangeValue()` was a scanner false
positive: only a commented obsolete signature remains in AdvCiv/SAS. Its live K-Mod
replacement is `AI_buildingSeaYieldChangeWeight()`, which Queue 005 already audited,
including the `WorkablePlotIter(*this)` working-city scope.

`AI_commerceToProductionMultiplierShift()` was also checked as a remaining one-mention helper.
AdvCiv practical 4214 extracted the same shared production-vs-commerce shift from the two
formerly duplicated callers; practical 4215 then deliberately added the Barbarian New-World
branch to both consumers through that helper. Current SAS matches Base AdvCiv semantically.
No sign/rank/divisor or caller-asymmetry defect survives.

FINAL LIVE-METHOD DIFFERENTIAL CENSUS
-------------------------------------
A comment-aware parser identifies 111 live out-of-line `CvCityAI::AI_*` method bodies in the
current source. This deliberately excludes commented/dead BtS/K-Mod signatures that had
misled earlier line-oriented coverage counts.

Cross-checking literal album coverage against Base AdvCiv produces a strong final result:

* the only zero-mention live methods are tiny emphasis accessors, and they are semantically
  identical to Base AdvCiv;
* EVERY live method with only one or two album mentions is semantically identical to Base
  AdvCiv after comments/whitespace are stripped;
* among the few three-mention methods with current SAS textual deltas, the executable changes
  are only already-bounded nonsemantic/performance/diagnostic edits:
  - `AI_splitEmpireValue`: const/reference spelling only;
  - `AI_countGoodSpecialists`: static cache of `DEFAULT_SPECIALIST` info lookup;
  - `AI_specialPlotImprovementValue`: cached owner/team references;
  - `AI_calculateNeededFloatingDefenders`: cached `GC.getGame()` reference plus signature
    formatting;
  - `AI_chooseProcess`: diagnostic logging only.

The apparently low-mention large helpers (`AI_getYieldMultipliers`, `AI_projectValue`,
`AI_cityThreat`, Worker/improvement helpers, citizen/specialist helpers, water-worker helpers,
etc.) all resolve to explicit thematic checkpoints elsewhere in Queue 005 rather than genuine
coverage gaps.

FINAL ADMISSION-COMMENT / ARITHMETIC / SENTINEL CONTROL
-------------------------------------------------------
A final live scan of TODO/FIXME/"wrong"/"bug" comments was cross-ranked against the cumulative
album. The surviving admissions are either:
- subjective tuning notes (naval/map-size scaling, specialist opportunity allocation);
- already-owned historical/current roots (e.g. KI#205, F533 and related families);
- comments describing fixes already present in current code;
- dead/commented inherited blocks.

The accompanying unguarded-divisor / negative-sentinel pass found no independent current
zero-divisor, invalid-enum or hard return-contract failure beyond existing roots.

QUEUE 005 CLOSURE DECISION
--------------------------
The stronger Queue-005 re-audit has now exercised:

* current SAS vs Base AdvCiv semantic deltas;
* K-Mod and Civ4CE ancestry for suspected inherited/refactor regressions;
* production-retention, emergency selection and concrete-unit hard gates;
* building prefilter category/sign/control-flow/runtime-effect contracts;
* citizen/specialist conservation and temporary-force lifetimes;
* land/water/area/player/team/owner/object-ID domains;
* Work Boat, naval capacity and disconnected-water counting;
* same-turn caches and mutable static state;
* synchronized-RNG/logging observational noninterference;
* arithmetic/sign/divisor/sentinel/default-return contracts;
* explicit TODO/FIXME/bug-admission comments;
* low-literal-coverage and final comment-aware live-method differential inventory;
* mandatory collision against the cumulative album before every late candidate.

The late pass still found real bugs F551-F558, demonstrating that the sieve remained capable
of producing findings rather than merely closing candidates. After those roots, the final
coverage/differential controls leave no independent live root for F559.

Leaving F559 unused is therefore the correct saturation result.

CURRENT STONE AFTER C031-WIP335
-------------------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` — DEEP-METHOD RE-AUDIT CLOSED.
32/169 unique files complete in the outer queue.
F521 / KI#844 through F558 / KI#881 CONFIRMED across this Queue-005 re-audit tranche, alongside
the documented broadened/reopened older roots.
F559 / provisional KI#882 UNUSED.
Latest physical checkpoint: C031-WIP335.
No repository source modified.

NEXT
----
Advance to protected deep-method re-audit Queue 006 `CvGameCoreDLL/CvCity.cpp` using the same
stronger method used for Queues 001-005. Treat its earlier original review as baseline rather
than closure. Begin with current-SAS semantic deltas and hard city-state lifecycle contracts
(population/citizen assignment, production completion/queue transitions, building state,
religion/corporation state, ownership/working-city/plot effects, anger/timers and caches), then
use Base AdvCiv / K-Mod / Civ4CE ancestry and a low-coverage live-method census before closure.
F559 / provisional KI#882 remains the next unused root slot.

END C031-WIP335


================================================================================
C031-WIP336 - QUEUE 006 CVCITY.CPP DEEP-METHOD RE-AUDIT OPEN / F559 CONFIRMED
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 005 `CvGameCoreDLL/CvCityAI.cpp` remains CLOSED at C031-WIP335.
Queue 006 `CvGameCoreDLL/CvCity.cpp` is now OPEN under the stronger deep-method method.
F559 / provisional KI#882 CONFIRMED below.
F560 / provisional KI#883 UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260904T202954.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository source files modified.

QUEUE 006 RECONCILIATION / CURRENT-SOURCE BASELINE
--------------------------------------------------
The earlier original File-006 pass remains useful historical baseline but is not treated as
current closure. A fresh current-tree semantic map finds 542 live `CvCity` method bodies,
with 39 bodies semantically different from Base AdvCiv after comments/whitespace are stripped.
This makes the re-audit substantially more bounded than Queue 005.

The original File-006 F012 / KI#333 "Port can preempt emergency defense" finding is no longer
live in the current SAS source. Practical 6248 / `c366e5ea9d...`, titled
"Fix AdvCiv-SAS emergency-production priority and map-heaviness lifecycle bugs (KI#333, KI#336)",
reordered `CvCity::doTurn()` so the broader emergency-defense block runs before the Port /
water-hammer rule. Current Known Issues also marks KI#333 fixed. Preserve F012 as historical
provenance; do not count it as a current live defect.

The current `CvCity::doTurn()` fallback still calls no-argument `AI_isFocusWar()` while making
area-sensitive emergency-production decisions. Queue-005 F553 proved that this overload
defaults to the capital's land area, not a player-global war state. This is therefore another
live consumer/breadth extension of F553, not a new Queue-006 root.

RECENT CURRENT-SAS CVCITY FRONTIER
----------------------------------
After the KI#333 repair, only a small recent commit frontier materially touches `CvCity.cpp`:
- practical 6290 / `7302a07c38...`: future-BFC valuation + inherited liberation lifetime fix;
- practical 6373 / `af4775625b...`: inherited UWAI city-cache lifecycle repairs;
- practical 6391 / `00dcf84d8f...`: SASGameRecord production lifecycle history;
- practical 6394 / `1ef349e7cf...`: SASGameRecord consequential action history;
- practical 6398 / `1ffe3c616e...`: SASGameRecord city population/state history;
- practical 6399 / `11fb13b1d0...`: SASGameRecord overhead optimization/gating.

The low-coverage semantic-delta pass through these and neighboring helpers has so far closed
as intended fixes, static info/define caching, serialization of new SAS state, or observational
GameRecord logging. In particular, the logging added around `upgradeProduction()` does not
create F559; it exposes an inherited gameplay assignment that already existed.

F559 / provisional KI#882 - CONFIRMED
Inherited unit-production upgrade migration overwrites an independent destination hammer bank
---------------------------------------------------------------------------------------------

ROOT
----
`CvCity::upgradeProduction()` migrates stored production from a queued unit that has become
obsolete to an available upgrade with an absolute assignment:

    int iUpgradeProduction = getUnitProduction(eUnit);
    setUnitProduction(eUnit, 0);
    setUnitProduction(eUpgradeUnit, iUpgradeProduction);

`setUnitProduction()` is an absolute setter for `m_aiUnitProduction`; it does not accumulate.
If `eUpgradeUnit` already has separately earned stored production, the assignment destroys
that destination bank and replaces it with the source bank.

The new SASGameRecord instrumentation explicitly snapshots `getUnitProduction(eUpgradeUnit)`
before this inherited assignment for diagnostics, but does not alter gameplay. The bug is the
old assignment itself.

WHY TWO INDEPENDENT BANKS CAN LEGITIMATELY COEXIST
--------------------------------------------------
Invalidating an unfinished unit order does not erase its stored production. In
`CvCity::popOrder`, the `ORDER_TRAIN` branch removes the order and then:

    if (!bFinish)
        break;

The later `setUnitProduction(eTrainUnit, 0)` is reached only when the unit actually finishes.
`checkCanContinueProduction()` removes an invalid queued unit through `popOrder(..., false, ...)`,
so losing a prerequisite parks the unit's hammer bank rather than deleting it.

AI parked unit production is especially durable: inherited `CvCity::doDecay()` increments the
parked production timer for everyone but applies unit-production decay only inside `if (isHuman())`.
Thus an AI city's destination bank can persist indefinitely while that destination is temporarily
untrainable.

CURRENT SHIPPED-DATA FIXTURE - CATAPULT -> CANNON
-------------------------------------------------
Current SAS XML supplies an ordinary, no-UU-required resource toggle:

- `UNIT_CATAPULT`
  - upgrades to `UNITCLASS_CANNON`;
  - prerequisite tech `TECH_MATHEMATICS`;
  - `BonusType = NONE` and no required strategic bonus.
- `UNIT_CANNON`
  - prerequisite `TECH_CHEMISTRY` (plus the current OR-tech entry including Machinery);
  - `BonusType = BONUS_IRON`.

A legal AI-city lifecycle is therefore:

1. The city has the Cannon technologies and Iron and invests, e.g., 60 hammers into Cannon.
2. Iron is lost. Cannon can no longer continue. `checkCanContinueProduction()` pops its order
   with `bFinish=false`, preserving the 60-hammer Cannon bank.
3. With Iron absent, `allUpgradesAvailable(UNIT_CATAPULT)` returns `NO_UNIT` because Cannon is
   not trainable. Catapult is therefore not rejected as obsolete and can legally be queued.
4. The city invests, e.g., 20 independent hammers into Catapult.
5. Iron returns. Cannon becomes an available upgrade; `allUpgradesAvailable(UNIT_CATAPULT)`
   now returns Cannon and `upgradeProduction()` migrates the queued Catapult order.
6. The current code performs:

       Catapult bank: 20 -> 0
       Cannon bank:   60 -> 20

   Forty already-earned Cannon hammers simply vanish. More generally, the destination bank is
   lost regardless of whether it is larger or smaller than the source; the migrated result is
   always exactly the source value rather than the sum of the two independent banks.

This does not depend on a human decay timing window because AI parked unit production does not
decay. The same logical loss can also affect a human city if prerequisites return before the
parked destination bank decays away.

ANCESTRY
--------
The destructive assignment is inherited, not SAS-authored:

- Civ4CE / BtS lineage uses `setUnitProduction(eUpgradeUnit, iUpgradeProduction)`;
- K-Mod 1.46 retains the same assignment inside `doCheckProduction()`;
- Base AdvCiv 1.14 extracted that logic into `CvCity::upgradeProduction()` while preserving
  the assignment;
- current AdvCiv-SAS retains Base AdvCiv gameplay semantics and only adds diagnostics around it.

DEDUP
-----
The cumulative album has one earlier `CvCity::upgradeProduction` mention concerning explicit
unit-class-making counter transfer around queue mutation. It does not own stored-hammer
replacement. No existing KI/root found in the reconciled album or current Known Issues owns
this destination-production overwrite.

CLASSIFICATION
--------------
CONFIRMED inherited gameplay/accounting defect. This is not a preference or valuation issue:
two independently accumulated production banks are represented in persistent city state, and
one is destroyed solely as a side effect of changing the queued unit to its now-available
upgrade.

REPAIR DIRECTION
----------------
Preserve the destination bank while transferring the source bank, e.g. by accumulating the
source into the destination rather than replacing it. Any final patch should keep the existing
source clear and queue/class-making migration semantics unchanged, and should verify normal
completion/overflow behavior if the combined bank reaches or exceeds the upgraded unit's cost.

CURRENT STONE AFTER C031-WIP336
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the one protected open file.
F559 / provisional KI#882 CONFIRMED.
F560 / provisional KI#883 UNUSED.
Latest physical checkpoint: C031-WIP336.
No repository source modified.

NEXT
----
Continue Queue 006 from the current-SAS semantic-delta frontier and inherited hard city-state
contracts. Exhaust `upgradeProduction` siblings first (production-time transfer, queue role /
class counters, multiple queued source instances and chained upgrades), then continue
`doCheckProduction` / `popOrder` / decay / overflow lifecycle and the remaining low-coverage
current-vs-Base deltas. Deduplicate against historical File-006 roots and current fixed KI#333
before spending F560.

END C031-WIP336


================================================================================
C031-WIP337 - QUEUE 006 CVCITY.CPP / F559 BREADTH / F560 CONFIRMED
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 remains CONFIRMED and is broadened below.
F560 / provisional KI#883 is CONFIRMED below.
F561 / provisional KI#884 UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260904T202954.zip`.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives.
No repository source files modified.

POST-WIP336 - F559 PRODUCTION-UPGRADE BREADTH
---------------------------------------------
The inherited `upgradeProduction()` overwrite is broader than the first Catapult -> Cannon
resource-loss fixture.

1. A zero-production queued source can erase a nonzero destination bank.

`upgradeProduction()` is gated on `getFirstUnitOrder(eUnit) != -1`, not on positive source
production. Therefore a queued obsolete source order that has not received any hammers yet can
reach:

    int iUpgradeProduction = getUnitProduction(eUnit); // 0 is legal
    setUnitProduction(eUnit, 0);
    setUnitProduction(eUpgradeUnit, iUpgradeProduction); // destination -> 0

If the destination unit type has parked production from an earlier build, merely converting the
zero-hammer queued source can erase the entire destination bank. This is the same F559 absolute-
assignment root, not a new finding.

2. Multiple source types can converge on one destination during the same call.

Current ordinary unit lines include Axeman -> Maceman and Swordsman -> Maceman. If both source
types have separately stored production and queued orders when Maceman becomes available,
`upgradeProduction()` can migrate both. The first source writes the Maceman bank; the second
source then overwrites it. Thus F559 does not require a pre-existing destination build or a
resource-loss cycle: independent queued upgrade sources can destroy one another's production.

3. Production-time state remains a repair-design issue, not a separate numbered root.

The upgrade migration transfers/overwrites the hammer bank but does not transfer or reconcile
`m_aiUnitProductionTime`. Where source and destination banks coexist they may have different
inactive ages, and the current one-timer-per-unit-type representation cannot preserve both
histories exactly. The correct timer merge policy (reset/source/destination/min/max) is therefore
not mechanically determined by the current representation. Keep this under the F559 repair/test
envelope rather than manufacture F560 from an ambiguous merge rule.

POST-WIP336 DEDUP / BREADTH - WORKER COMPLETION HOOK ALSO FIRES ON CANCELLATION
-------------------------------------------------------------------------------
A superficially new `popOrder()` candidate is actually a breadth extension of Queue-005 F538.

The AdvCiv 113b hook:

    if (eTrainAIUnit == UNITAI_WORKER)
        AI().AI_changeWorkersHave(1);

runs before:

    if (!bFinish)
        break;

so the +1 is not completion-only. Cancelling/removing an unfinished UNITAI_WORKER order through
`popOrder(..., false, ...)` also adds a phantom Worker to `m_iWorkersHave`.

This can be visible within the same production-selection call. `CvCityAI::AI_chooseProduction()`
can clear the current queue and immediately continue choosing a replacement order; if the cleared
order is a Worker, later `AI_getWorkersHave()` gates see the false +1 until the next full
`AI_updateWorkersHaveAndNeeded()` refresh. The exact hook is already F538's root (F538 proved that
the same unconditional +1 double-counts a Worker that was already represented by the morning
near-completion cache), so do NOT spend a new Queue-006 number on cancellation. Broaden F538's
repair direction: the +1 must be conditioned on a real completed Worker and must avoid counting a
Worker already represented by the current cache snapshot.

F560 / provisional KI#883 - CONFIRMED
Production-decay turn preview is one turn late on fractional game-speed thresholds
------------------------------------------------------------------------------------

ROOT
----
The BULL production-decay getters imported by AdvCiv say they predict whether parked production
will decay this turn and how many turns remain:

    bool CvCity::is##Order##ProductionDecay(...) const
    {
        return (... &&
            100 * get##Order##ProductionTime(e##Order) >=
            DECAY_TIME * gameSpeedPercent);
    }

    int CvCity::get##Order##ProductionDecayTurns(...) const
    {
        return 1 + max(0,
            ceil(DECAY_TIME * gameSpeedPercent / 100) -
            get##Order##ProductionTime(e##Order));
    }

But authoritative runtime `CvCity::doDecay()` first increments the inactive timer and only then
tests a strict greater-than condition:

    change##Order##ProductionTime(e##Order, 1);
    if (100 * get##Order##ProductionTime(e##Order) >
        DECAY_TIME * gameSpeedPercent)
    {
        ... decay ...
    }

For the timer value T observed before this turn's `doDecay()`, the real question is therefore:

    100 * (T + 1) > DECAY_TIME * gameSpeedPercent

The BULL getters instead model the current T against the threshold. These happen to agree when the
scaled threshold is an exact integer number of turns, but disagree when it has a fractional-turn
remainder.

CURRENT SHIPPED-DATA WITNESSES
------------------------------
Current SAS inherits the BtS decay-time defines from the supplied Civ4CE/BtS GlobalDefines:

    BUILDING_PRODUCTION_DECAY_TIME = 50
    UNIT_PRODUCTION_DECAY_TIME     = 10

Current SAS adds/retains several non-100 game speeds. In particular:

    GAMESPEED_NITRO  Train/Construct = 33%
    GAMESPEED_QUICK  Train/Construct = 67%

No current SAS XML override of the two decay-time defines was found.

Quick unit fixture:

    threshold = 10 * 67 = 670
    current parked-unit timer T = 6

Runtime on the upcoming city turn:

    increment: 6 -> 7
    100 * 7 = 700 > 670
    => production decays THIS turn.

Preview getters before that call:

    isUnitProductionDecay:
        100 * 6 = 600 >= 670 -> false

    getUnitProductionDecayTurns:
        1 + (ceil(670 / 100) - 6)
        = 1 + (7 - 6)
        = 2

So the hover can say the unit will decay in 2 turns when runtime will decay it on the very next
`doDecay()` call.

Quick building fixture is analogous:

    threshold = 50 * 67 = 3350
    T = 33
    runtime increments to 34: 3400 > 3350 -> decay this turn
    getter returns 1 + (ceil(33.5) - 33) = 2 turns.

Nitro has the same fractional-threshold mismatch (e.g. unit threshold 330 at T=3).
Speeds whose scaled thresholds are exact multiples of 100 (Normal, Turbo for these defines,
Epic, Marathon, Slow, Very Slow) mask the timing defect.

LIVE USER-FACING CONSUMER
-------------------------
Current `CvGameTextMgr::setUnitHelp` and `setBuildingHelp` call
`getUnitProductionDecayTurns()` / `getBuildingProductionDecayTurns()` when the city-screen
production-decay queue option is enabled, then `setProductionDecayHelp()` renders either the
immediate-decay text or the "in N turns" warning. The current config enables the production-decay
queue warning by default. Thus the one-turn-late value is not merely an unused Python helper.

`isUnitProductionDecay()` / `isBuildingProductionDecay()` are also exported through CyCity and
encode the same late boundary.

ANCESTRY
--------
BtS/Civ4CE and K-Mod contain the authoritative runtime sequence: increment the production-time
counter, then decay when `100 * time > DECAY_TIME * gameSpeedPercent`.

The preview getters are AdvCiv/BULL additions. Pre-SAS AdvCiv practical 3156 /
`0c4bd84a716240c6c6889acaa15b51a1d9ce5105`, titled "Production decay warnings from BULL",
introduced both:

- current-timer `>=` in `is##Order##ProductionDecay`;
- `1 + ceil(threshold/100) - currentTime` in `get##Order##ProductionDecayTurns`;

while leaving runtime `doDecay()` with the increment-first, strict-`>` contract. Base AdvCiv 1.14
and current SAS retain the mismatch.

DEDUP
-----
F391 / KI#714 is the sibling production-decay hover defect, but it owns a DIFFERENT formula:
`getUnitProductionDecay()` / `getBuildingProductionDecay()` calculate the amount lost as if game
speed were always 100%. Fixing F391's loss amount does not alter the timer predicates above;
fixing F560's timing does not fix F391's amount. Keep them separate.

Whole-album search found no prior root for the one-turn-late `ProductionDecayTurns` / `is...Decay`
boundary.

CLASSIFICATION
--------------
CONFIRMED inherited AdvCiv/BULL UI/API timing defect against the authoritative BtS/K-Mod runtime
state transition. It is current-data live on Quick and Nitro.

REPAIR DIRECTION
----------------
Derive both preview APIs from the exact state transition performed by `doDecay()`, preferably
through one shared helper so amount/timing/runtime cannot drift independently again.

For current timer T and scaled threshold S = DECAY_TIME * gameSpeedPercent, immediate decay is:

    100 * (T + 1) > S

The turns-left helper should return 1 at the largest T for which the upcoming increment crosses
that strict boundary. Regression tests should include Quick and Nitro fractional thresholds as
well as Normal/Turbo exact-threshold controls and should be paired with F391's amount tests.

CURRENT STONE AFTER C031-WIP337
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 CONFIRMED and broadened.
F560 / provisional KI#883 CONFIRMED.
F561 / provisional KI#884 UNUSED.
Latest physical checkpoint: C031-WIP337.
No repository source modified.

NEXT
----
Continue Queue 006 under F561 unused. Finish the remaining production lifecycle around
`doCheckProduction`, `checkCanContinueProduction`, `popOrder`, fail-gold/overflow, queue counters
and production-time state; dedup Worker-cache manifestations into F538 and hammer-bank collisions
into F559. Then return to the remaining current-SAS-vs-Base semantic-delta map and hard city-state
lifecycle contracts.

END C031-WIP337


================================================================================
C031-WIP338 - QUEUE 006 CVCITY.CPP / F561 CONFIRMED
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 and F560 / KI#883 remain CONFIRMED.
F561 / provisional KI#884 is CONFIRMED below.
F562 / provisional KI#885 UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives.
No repository source files modified.

POST-WIP337 PRODUCTION-LIFECYCLE CLOSURES
-----------------------------------------
The remaining `popOrder` / fail-gold / cancellation-overflow pass did not produce F561.

- `popOrder(..., false, ...)` does call `handleOverflow` before its `!bFinish` break. A positive
  raw overflow on cancellation would therefore be dangerous, but the obvious dynamic unit-cost
  producer is current-data dead: all 93 shipped `CIV4UnitClassInfos.xml`
  `iInstanceCostModifier` values are 0. No ordinary shipped cancellation state with positive raw
  overflow was established, so preserve this as robustness debt rather than a numbered root.
- The great-wonder/world-project fail-gold loop's `pMinProductionCity` / `iMinProduction` naming
  is misleading, but once the world class/project is actually maxed all surviving competing
  queued copies are invalid and must be removed. No gameplay contradiction follows merely from
  the stale names/comments.
- The fail-gold cap is evaluated through the completing city's production-needed getter. This
  could matter in a mod with per-city Python building-cost callbacks, but the current
  `USE_GET_BUILDING_COST_MOD_CALLBACK` is 0 and current world-wonder/world-project AI cost
  modifiers are 100, so no shipped different-cost witness survives.

LOW-COVERAGE LIVE-METHOD CENSUS
-------------------------------
A fresh live-method census was generated rather than relying on literal source-diff size.
Several substantial inherited helpers have zero literal album mentions despite the original
File-006 changed-method pass. `getAdditionalBaseYieldRateByBuilding` (~94 live lines) was the
first hard-value helper taken end-to-end. That uncovered F561.

F561 / provisional KI#884 - CONFIRMED
Additional-building-yield preview truncates trade-yield modifiers before multiplication
-----------------------------------------------------------------------------------------

ROOT
----
`CvCity::getAdditionalBaseYieldRateByBuilding()` estimates how much yield a building's trade-
route modifiers would add. Current AdvCiv/SAS performs:

    int iTradeYield = ((iTradeProfit * iTradeModifier) / iTradeProfitDivisor) *
            (iPlayerTradeYieldModifier / 100);
    ...
    int iNewTradeYield = ((iTradeProfit * iTradeModifier) / iTradeProfitDivisor) *
            (iPlayerTradeYieldModifier / 100);

The integer division `iPlayerTradeYieldModifier / 100` occurs FIRST. Therefore:

- any positive trade-yield modifier below 100 becomes 0;
- 100 through 199 become 1, discarding their fractional bonus;
- etc.

Authoritative runtime trade yield does not do this. `CvCity::calculateTradeYield()` computes:

    (iTradeProfit * GET_PLAYER(getOwner()).getTradeYieldModifier(eYield)) / 100

so the trade-yield modifier is multiplied before division.

CURRENT SAS DATA MAKES THE REGRESSION SHARPLY LIVE
--------------------------------------------------
Current SAS `TRAIT_IMPERIALIST` deliberately has:

    TradeYieldModifiers:
        YIELD_FOOD       +10
        YIELD_PRODUCTION +20
        YIELD_COMMERCE   +30

Base yield trade modifiers are 0 Food, 0 Production, 100 Commerce, so an Imperialist owner has
trade-yield modifiers 10 / 20 / 130 respectively.

Twelve current leaders use Imperialist, including Augustus, Catherine, Charlemagne, Cyrus,
Genghis Khan, Isabella, Joao, Julius Caesar, Mehmed, Napoleon, Suleiman and Victoria. This is
therefore ordinary shipped leader data, not a hypothetical XML configuration.

CLEAN CURRENT NUMERIC FIXTURE - IMPERIALIST + LIGHTHOUSE
--------------------------------------------------------
Use an Imperialist city with a mature foreign trade route. Current inherited values already
used elsewhere in this audit include:

    THEIR_POPULATION_TRADE_PERCENT = 50
    TRADE_PROFIT_PERCENT           = 20
    CAPITAL_TRADE_MODIFIER         = 25
    FOREIGN_TRADE_MODIFIER         = 150

Take the local city at the population point where its own population-trade modifier contributes
0, connected to its capital, and a sufficiently distant foreign partner at population 17 so the
partner-population term is limiting. Then:

    base trade profit = 17 * 50 * 20 / 100 = 170 hundredths
    ordinary mature foreign modifier = 100 + 25 + 150 = 275%

Current `BUILDING_LIGHTHOUSE` adds `iTradeRouteModifier = +25`, so:

    before Lighthouse: floor(170 * 275 / 10000) = 4 trade-profit units
    after Lighthouse:  floor(170 * 300 / 10000) = 5 trade-profit units

For Imperialist Production trade yield (20%):

    runtime before = floor(4 * 20 / 100) = 0
    runtime after  = floor(5 * 20 / 100) = 1
    real building effect = +1 production

But the additional-building-yield helper evaluates:

    iPlayerTradeYieldModifier / 100 = 20 / 100 = 0

before multiplication, so both the before and after estimates are 0 and the Lighthouse is shown
as adding no production through that route.

The Food 10% path can be erased in the same way. Commerce is also rounded incorrectly because
130 / 100 becomes 1 rather than preserving the 30% extra, although a particular before/after
integer boundary can sometimes mask the final difference.

LIVE CONSUMERS
--------------
This helper is not dead. `CvGameTextMgr` uses `CvCity::getAdditionalYieldByBuilding()`, which
calls it, in multiple BULL/BUG hover surfaces:

- Building Actual Effects, available for the active-owned city when the option is enabled or
  while Alt is held;
- Building Additional Production hover;
- Building Additional Food hover.

Thus an Imperialist player can receive a directly wrong current-city building-effect preview.

ANCESTRY - ADV CIV REFACTOR REGRESSION
--------------------------------------
K-Mod's original expression preserved the correct multiplication order:

    iTradeProfit * iTradeModifier / iTradeProfitDivisor *
            iPlayerTradeYieldModifier / 100;

AdvCiv practical 1908 / `360e6ee7b0b923d2a98576498f328c8043bc6091`, titled
`Refactor CvCity using EnumMap, FOR_EACH_ENUM, AgentIterator`, mechanically flattened the
surrounding loop and changed the expression to:

    ((iTradeProfit * iTradeModifier) / iTradeProfitDivisor) *
            (iPlayerTradeYieldModifier / 100);

That parenthesization introduces the premature integer truncation. Base AdvCiv 1.14 retains it,
and current SAS inherits it unchanged. SAS's Imperialist trade-yield redesign makes the latent
regression especially visible because Food/Production modifiers are intentionally below 100.

DEDUP
-----
This is independent of the nearby established roots:

- F398 / KI#721: the same broad BUG additional-yield feature treats conditional military/domain/
  space production modifiers as generic production. F398 is in
  `getAdditionalYieldRateModifierByBuilding`; F561 is a separate arithmetic regression in the
  trade branch of `getAdditionalBaseYieldRateByBuilding` and survives even if F398 is fixed.
- F556 / KI#879: AI building valuation conflates foreign and merely overseas routes when valuing
  `ForeignTradeRouteModifier`. F561 is a player-facing additional-yield preview and does not
  depend on foreign-vs-overseas identity confusion.
- Existing trade-route cache findings concern stale route/profit state, not this deterministic
  integer-operation order on a fresh current route.

Whole-album search found no prior numbered root for `iPlayerTradeYieldModifier / 100` or this
additional-building-trade-yield truncation.

CLASSIFICATION
--------------
CONFIRMED inherited AdvCiv refactor regression, current-data live and directly user-visible.

REPAIR DIRECTION
----------------
At minimum, restore multiplication-before-division semantics instead of dividing
`iPlayerTradeYieldModifier` independently. Better, derive the preview from the same runtime
trade-yield arithmetic used by `calculateTradeYield` so operation order and rounding cannot drift.

Because runtime first sums per-route integer trade profits and then applies the player's trade-
yield modifier to the aggregate, a fully exact repair should also compare the old and projected
TOTAL trade profit before calling/applying `calculateTradeYield`, rather than independently
rounding yield route by route. Regression tests should include Imperialist Food/Production
modifiers below 100, Commerce 130, and a Lighthouse/Port trade-route-modifier boundary case.

CURRENT STONE AFTER C031-WIP338
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 CONFIRMED and broadened.
F560 / KI#883 CONFIRMED.
F561 / provisional KI#884 CONFIRMED.
F562 / provisional KI#885 UNUSED.
Latest physical checkpoint: C031-WIP338.
No repository source modified.

NEXT
----
Continue Queue 006 under F562 unused. Stay with the low-coverage inherited hard-value/state
helpers now that the production-lifecycle family is bounded: finish the remaining
`getAdditional*ByBuilding` arithmetic against runtime contracts, then `setHasReligion`,
`setCultureLevel`, `getReligionGrip`, health/happiness improvement-state helpers and other
substantial zero-/low-mention live methods. Dedup aggressively against Queue-003 BUG-hover roots
(F397-F400 family) and the later trade-cache roots before spending F562.

END C031-WIP338


================================================================================
C031-WIP339 - QUEUE 006 CVCITY.CPP / F562 CONFIRMED
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F561 / KI#884 remain CONFIRMED.
F562 / provisional KI#885 is CONFIRMED below.
F563 / provisional KI#886 UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison/ancestry material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS
archives plus the local reachable-history commit-diff/log material generated with the light source.
No repository source files modified.

POST-WIP338 GP-PROJECTION AUDIT
-------------------------------
The first low-coverage helper after F561 was `CvCity::GPProjection()`. Its own long-standing
comment says projected GP percentages have been observed above 100% with the Parthenon and
attributes that to unspecified rounding error. Reconstructing runtime GP accumulation shows that
this is not merely display rounding: the projection and the actual per-unit GP banks use different
rounding models and can therefore predict a different Great Person type distribution.

F562 / provisional KI#885 - CONFIRMED
GPProjection uses fractional per-type future GPP while runtime truncates each type every turn
--------------------------------------------------------------------------------------------

ROOT
----
Current runtime Great Person progress has two distinct update paths in `CvCity::doGreatPeople()`:

    changeGreatPeopleProgress(getGreatPeopleRate());

where:

    getGreatPeopleRate() =
        (getBaseGreatPeopleRate() * getTotalGreatPeopleRateModifier()) / 100;

and, separately for every Great Person unit type:

    changeGreatPeopleUnitProgress(eUnit,
        (getGreatPeopleUnitRate(eUnit) * iTotalGreatPeopleRateModifier) / 100);

The latter integer division happens ONCE PER TURN AND PER GP TYPE. Those accumulated integer
`getGreatPeopleUnitProgress` banks are the authoritative weights used when the GP is actually
selected.

`GPProjection()` does not project those same integer increments. For every type it uses:

    getGreatPeopleUnitProgress(eLoopUnit) +
        per100(iTurnsLeft * getGreatPeopleUnitRate(eLoopUnit) *
            getTotalGreatPeopleRateModifier())

`per100` keeps the future contribution fractional across all remaining turns. The denominator,
however, advances with:

    iTurnsLeft * getGreatPeopleRate()

which uses the aggregate integer rate after summing all base GP rates. Therefore numerator and
runtime-selection arithmetic diverge whenever a modifier produces different truncation after
per-type separation.

CLEAN CURRENT SAS FIXTURE - PARTHENON + HEROIC EPIC
----------------------------------------------------
Current shipped XML gives:

    BUILDING_PARTHENON
        GreatPeopleUnitClass = UNITCLASS_GREAT_SPY
        iGreatPeopleRateChange = 2
        iGlobalGreatPeopleRateModifier = +50

    BUILDING_HEROIC_EPIC
        GreatPeopleUnitClass = UNITCLASS_GREAT_ARTIST
        iGreatPeopleRateChange = 1

Put both in one city and consider the state immediately after any Great Person birth. Runtime
resets every per-unit GP progress bank to 0. Assume the rates/buildings then remain unchanged.
The city's base GP rate is 3 and its total modifier is 150%, so:

    aggregate getGreatPeopleRate() = floor(3 * 150 / 100) = 4 per turn

Authoritative per-unit runtime increments are:

    Great Spy:    floor(2 * 150 / 100) = 3 per turn
    Great Artist: floor(1 * 150 / 100) = 1 per turn

After any T future turns, the selection banks therefore remain exactly:

    Spy    = 3T
    Artist = 1T

and, when a GP is generated with those unchanged rates, the real type probabilities are 75% Spy
and 25% Artist.

But `GPProjection()` projects:

    Spy future contribution    = 3T
    Artist future contribution = 1.5T
    denominator                = 4T

so its shares are:

    Spy    = 75%
    Artist = 37.5%

`scaled::getPercent()` rounds to nearest integer, so the displayed vector is normally about:

    Great Spy    75%
    Great Artist 38%
    TOTAL       113%

This does not depend on assuming a particular first-GP threshold: starting just after a GP birth
makes the per-unit banks zero, and the ratio error persists for whatever positive `GPTurnsLeft()`
results from the current threshold/progress state.

WHY THE EXISTING 100% CAP DOES NOT REPAIR IT
--------------------------------------------
Current `GPProjection()` contains the source comment:

    "I've seen 103 here with the Parthenon (150% modifier)."

and caps EACH INDIVIDUAL entry with:

    std::min(100, rShare.getPercent())

That only prevents one type from exceeding 100%. It cannot normalize several positive entries
whose SUM exceeds 100%, as in the 75% + 38% fixture above.

The later error-correction block also only handles a positive deficit:

    int iTotalError = 100 - iRoundedPercentages;
    if (iTotalError >= 2) ...

For a 113% total, `iTotalError` is -13, so no correction runs.

AUTHORITATIVE CONSUMERS / CONSEQUENCE
-------------------------------------
This is a real player-facing probability predictor:

- `CvGameTextMgr::parseGreatPeopleHelp` calls `GPProjection()` and prints each returned percentage
  directly in the Great Person help text;
- BUG's `GPUtil.py::calcPercentages` calls the Python-exposed `city.GPProjection(iUnit)` for the
  GP progress bar/hover;
- current SAS diagnostic logging also calls `GPProjection`, but that path is observational only
  and is NOT used here to claim an AI-decision consequence.

At actual GP generation, `CvCity::doGreatPeople()` sums the integer per-unit progress banks and
chooses the GP using those integer weights. Thus the displayed 38% Artist in the fixture is not
just a differently rounded presentation of a 25% true weight; it is based on future progress that
runtime will never add to the Artist bank.

ANCESTRY - ADV CIV FIX LEFT THE PROJECTION ON THE OLD ARITHMETIC MODEL
---------------------------------------------------------------------
`GPProjection()` is an AdvCiv feature (`advc.001c`), not inherited BtS/K-Mod gameplay logic.
The important reachable-history sequence is:

1. AdvCiv introduced projected GP odds from current rates and modifiers.
2. Practical 2144 / `e487a760c727626ad97c850dafb56085614fdd85`, titled
   `Fix GP birth odds`, also changed true per-unit GP progress so modifiers would affect the
   actual GP-selection banks.
3. That first runtime change accidentally omitted `/ 100`.
4. Practical 2293 / `786861d30efedc81fb9f85fcf72648a367f8058b`, titled
   `Fix AdvCiv bug with GP birth probabilities`, corrected runtime to:

       (iUnitRate * iTotalGreatPeopleRateModifier) / 100

   i.e. integer truncation per unit type per turn.

The projection was not changed to mirror that corrected runtime arithmetic. It retained the
fractional `per100(iTurnsLeft * rate * modifier)` model. The current mismatch therefore survives
Base AdvCiv 1.14 and current SAS.

A later practical commit `0bbebd2ff9f3ec55b5fa21d580c82ee6358de542`, titled
`Smooth over an inaccuracy in the displayed GP birth odds`, explicitly notes Parthenon totals
above 100% and caps individual results, but does not repair the underlying per-type projection.

DEDUP
-----
Whole-album searches for `GPProjection`, `GPTurnsLeft`, Great Person birth probability/odds and
Parthenon projection found no prior numbered root for this arithmetic mismatch.

This is independent of:

- F449 / KI#772, which concerns GP birth MESSAGE information leaking between observers;
- Queue-003/UI display findings that alter text composition but not the probability arithmetic;
- old AdvCiv savegame GP-progress compatibility handling, which explains why total city GP
  progress is not simply substituted for the sum of per-unit banks.

The initial `GPTurnsLeft() == -1` suspicion is NOT part of F562. Under current data, the obvious
zero-total-GPP states also zero the live per-unit rates; no separate shipped consequence was
established from the raw -1 reuse.

CLASSIFICATION
--------------
CONFIRMED inherited/current Base AdvCiv GP-projection arithmetic defect, directly user-visible in
current SAS data.

REPAIR DIRECTION
----------------
Project the exact same integer per-turn contribution that runtime adds to each GP unit bank:

    int iPerTurnUnitProgress =
        (getGreatPeopleUnitRate(eUnit) * getTotalGreatPeopleRateModifier()) / 100;

then use:

    currentUnitProgress + iTurnsLeft * iPerTurnUnitProgress

for each numerator. The projected denominator for selection probabilities should be the sum of
those projected PER-UNIT banks, not independently projected aggregate city GPP, because the
actual GP type RNG is weighted by the per-unit banks.

After that, percentage rounding can be normalized to 100 as a separate presentation step.
Regression coverage should include the current Parthenon + Heroic Epic 2:1 base-rate / 150%
fixture and verify 75% / 25%, plus a case with pre-existing per-unit progress.

CURRENT STONE AFTER C031-WIP339
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F562 / provisional KI#885 CONFIRMED.
F563 / provisional KI#886 UNUSED.
Latest physical checkpoint: C031-WIP339.
No repository source modified.

NEXT
----
Continue Queue 006 under F563 unused. Finish the remaining neighboring low-coverage projection /
`getAdditional*ByBuilding` helpers against their authoritative runtime paths, then return to
religion/culture/revolt and city-state lifecycle helpers (`setHasReligion`, `setCultureLevel`,
`getReligionGrip`, improvement-state health/happiness, production/culture ownership transitions).
Dedup aggressively against Queue-003 hover roots and existing culture/religion/cache families.

END C031-WIP339


===============================================================================
C031-WIP340 — QUEUE 006 CVCITY.CPP CONTINUATION — F563 / KI#886 CONFIRMED
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
32/169 unique files complete.
F559 / KI#882 through F563 / provisional KI#886 CONFIRMED.
F564 / provisional KI#887 UNUSED.
Latest physical checkpoint: C031-WIP340.
No repository source files modified.

F563 / KI#886 — `changeNoBonusCount()` VERIFIES PRODUCTION BEFORE APPLYING
THE RESOURCE-SUPPRESSION STATE, SO A BUILDING SUCH AS NATIONAL PARK CAN LEAVE
RESOURCE-DEPENDENT ORDERS INVALIDLY QUEUED FOR THE REST OF THE TURN
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. `CvCity::changeNoBonusCount()` attempts to revalidate production when a
building disables access to a bonus, but it calls `verifyProduction()` while the old bonus
availability is still authoritative. The actual `m_aiNoBonus` mutation happens only afterward.
Consequently, an order that becomes illegal specifically because of the suppression survives the
only immediate verification pass. The reverse transition is also missed: removing suppression can
make an upgrade newly available, but the pre-change city reports zero of the bonus and skips
verification entirely.

ROOT — VERIFICATION OBSERVES THE OLD `hasBonus()` STATE
-------------------------------------------------------
Current SAS:

    void CvCity::changeNoBonusCount(BonusTypes eBonus, int iChange)
    {
        if (iChange == 0)
            return;

        if (getNumBonuses(eBonus) > 0)
        {
            processBonus(eBonus, -1);
            verifyProduction();
        }

        m_aiNoBonus.add(eBonus, iChange);
        FAssert(getNoBonusCount(eBonus) >= 0);

        if (getNumBonuses(eBonus) > 0)
            processBonus(eBonus, 1);

        updateCorporation();
        ...
    }

`getNumBonuses()` begins with:

    if (isNoBonus(eBonus))
        return 0;

so the suppression count itself is what changes city-level resource availability. While the
pre-mutation `verifyProduction()` runs, `isNoBonus(eBonus)` is still false when adding the first
suppression and `hasBonus(eBonus)` therefore still reports the old resource access.

`verifyProduction()` delegates to `checkCanContinueProduction(true, HUMAN_CHOOSE)`, which walks
the queue and ultimately calls `canTrain(..., bContinue=true)`, `canConstruct(..., true)`, etc.
Unit resource legality reaches `CvPlot::canTrain`; for city training it tests the current city
`hasBonus()` state for required AND/OR bonuses. Thus the verifier is capable of catching this
kind of invalidation — it simply runs one state transition too early here.

CLEAN CURRENT SAS FIXTURE — NATIONAL PARK -> COAL -> IRONCLAD
-------------------------------------------------------------
Current shipped XML supplies a direct ordinary-game witness:

- `BUILDING_NATIONAL_PARK` has:

      <NoBonus>BONUS_COAL</NoBonus>

- `UNIT_IRONCLAD` has Coal among its required resource prerequisites (in addition to Iron).

A coastal city can therefore have both technologies/resources needed to queue:

    1. National Park
    2. Ironclad

while Coal is still available.

When National Park completes, `popOrder()` deliberately removes the completed building order from
the queue BEFORE calling `setNumRealBuilding()`. Its source comment states why: `processBuilding`
may call `verifyProduction`, and the just-completed building should no longer be treated as a queue
entry at that point. The Ironclad therefore is exactly the kind of remaining queued order the
new verification was meant to reconsider.

`processBuilding(National Park,+1)` calls `changeNoBonusCount(BONUS_COAL,+1)`:

1. Coal is still available, so `getNumBonuses(COAL) > 0`.
2. Coal's processed city effects are removed.
3. `verifyProduction()` runs NOW.
4. At that instant `m_aiNoBonus[COAL]` is still 0, so `hasBonus(COAL)` remains true and the queued
   Ironclad passes its resource check.
5. Only afterward does `m_aiNoBonus.add(COAL,+1)` make `hasBonus(COAL)` false.
6. No second general `verifyProduction()` follows that state change.

The result is a current production queue whose head can be an Ironclad for which the same city's
`canTrain(Ironclad,true)` is now false. Normal start-of-turn production checking will eventually
clean the order on the following owner turn, but the city spends the remainder of the current
turn in a state the `advc.064d` immediate-invalidation machinery was specifically introduced to
prevent. Human UI/hurry queries and AI/player queue counters can observe that stale current order
in the meantime.

REVERSE TRANSITION IS MISSED TOO
--------------------------------
When the suppression count falls from 1 -> 0 while an underlying Coal source exists, the
pre-change `getNumBonuses(COAL)` is zero precisely because `isNoBonus(COAL)` is true. Therefore the
function skips the only explicit `verifyProduction()` call. After `m_aiNoBonus` is decremented,
Coal becomes available, which can make queued unit upgrades or other orders newly legal, but the
production/upgrade queue is not immediately reconsidered through this caller.

This is the same state-ordering root: production validation is not keyed to the FINAL
old-vs-new `hasBonus()` transition.

SIBLING CONTROL — `changeFreeBonus()` GETS THE ORDERING RIGHT
-------------------------------------------------------------
The neighboring free-bonus path does not justify the old-state ordering. `changeFreeBonus()` first
removes the old plot-group contribution with verification disabled, mutates the free-bonus count,
and then rebuilds the plot-group bonus with the default `bVerifyProduction=true`. The resulting
plot-group verification therefore sees the post-mutation resource state.

Likewise `updateCorporationBonus()` snapshots old `hasBonus()` values, recomputes produced bonuses,
and conditionally verifies after detecting an actual old-vs-new loss. `changeNoBonusCount()` is the
outlier.

ANCESTRY — THE IMMEDIATE-INVALIDATION FIX WAS INSERTED AT THE WRONG SIDE OF
THE SUPPRESSION MUTATION
----------------------------------------------------------------------------
BtS/Civ4CE and K-Mod contain the same basic `changeNoBonusCount()` mutation but no local
`verifyProduction()` call.

AdvCiv practical 1501 / commit
`72eb8cc66b8939503fd0205b9cbf0d72b6ea7b3e` (2019-05-15), titled:

    Invalid orders don't generate overflow; art style bugfix

introduced `advc.064d`. Its commit message explicitly says:

    When the production order of a city becomes invalid during the city owner's turn,
    the city immediately asks for a new order.

and explains that this addresses the hammer-stacking exploit. In the same change, AdvCiv inserted:

    processBonus(eIndex, -1);
    verifyProduction();

inside `changeNoBonusCount()` — but before the inherited line that actually increments
`m_paiNoBonus` / current `m_aiNoBonus`.

Base AdvCiv 1.14 and current SAS retain this ordering unchanged. The intent of the added verifier is
therefore unusually explicit; the defect is that it evaluates the old availability state and
cannot fulfil that intent for `NoBonus` transitions.

DEDUP
-----
Whole-album searches for `changeNoBonusCount`, `NoBonus`, National Park + Coal, and production
verification around bonus suppression found no earlier numbered root.

This is independent of:

- F559/KI#882, which loses stored hammers when `upgradeProduction()` overwrites a destination
  production bank;
- F560/KI#883, which concerns the BULL decay-turn predictor versus runtime timer ordering;
- prior corporation / resource-network findings, which concern bonus propagation, corporation
  inputs or plot-group state rather than verification occurring before `m_aiNoBonus` changes;
- the old hammer-stacking exploit that `advc.064d` was meant to repair generally. F563 is a
  surviving caller-specific hole in that repair.

CLASSIFICATION
--------------
CONFIRMED inherited Base-AdvCiv/current-SAS state-ordering defect, live in current XML through
National Park + Coal + Ironclad.

REPAIR DIRECTION
----------------
Validate against the final resource state, not the pre-mutation state. A robust local shape is:

    bool const bOldHasBonus = hasBonus(eBonus);
    if (bOldHasBonus)
        processBonus(eBonus, -1);

    m_aiNoBonus.add(eBonus, iChange);
    FAssert(getNoBonusCount(eBonus) >= 0);

    bool const bNewHasBonus = hasBonus(eBonus);
    if (bNewHasBonus)
        processBonus(eBonus, 1);

    updateCorporation(...);
    if (bOldHasBonus != finalHasBonus)
        verifyProduction();

The exact placement should account for corporation-produced bonuses so that the verification sees
the FINAL post-`updateCorporation` availability state and runs only once. Regression tests should
cover both:

- National Park gaining `NoBonus=Coal` while an Ironclad is queued behind it; and
- suppression being removed while a newly restored resource makes a queued unit upgrade available.

POST-F563 CONTROLS / CLOSED LEADS
---------------------------------
1. `setRevealed()` apparent dangling-if: FALSE POSITIVE caused by a concatenated source excerpt.
   Exact source has the active-team symbol-update block correctly braced through the end of the
   function, matching Base AdvCiv.

2. World-wonder notification `GET_TEAM(kObs.getID())`: FALSE POSITIVE. `CvCity.cpp` includes
   `CoreAI.h`, whose overloaded `CoreAI::getTeam(PlayerTypes)` explicitly converts through
   `TEAMID(ePlayer)`. The PlayerTypes argument is therefore type-safe here.

3. `getRevoltProtection()` accepts the strongest revolt-protection unit on the plot regardless of
   owner/team. Current Leadership gives +50 revolt protection, so an Open-Borders ally can in
   principle contribute. This behavior is inherited from BtS/K-Mod and can plausibly be read as
   any friendly military presence suppressing unrest; no hard contradiction sufficient for F564
   established.

4. `damageGarrison()` damages every defending unit on the city plot when a revolt fires, including
   possible third-party co-located defenders. BtS/K-Mod do the same. Because a local revolt can
   plausibly harm allied forces physically present in the city, this remains authored mechanics,
   not a numbered root.

CURRENT STONE AFTER C031-WIP340
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F563 / provisional KI#886 CONFIRMED.
F564 / provisional KI#887 UNUSED.
Latest physical checkpoint: C031-WIP340.
No repository source modified.

NEXT
----
Continue Queue 006 under F564 unused. Exhaust resource-mutation sibling contracts around
`changeNumBonuses`, corporation-produced bonuses and building processing; then continue the
low-coverage `CvCity` hard-state census (culture ownership, religion/corporation lifecycle,
hurry/conscript state, city reveal/visibility and rank/cache mutators). Dedup before numbering and
freeze the next physical stone regardless of whether F564 is used.

END C031-WIP340


===============================================================================
C031-WIP341 APPEND - F564 CONFIRMED / CORPORATION-BONUS-GAIN PRODUCTION INVALIDATION
===============================================================================

-------------------------------------------------------------------------------
F564 / provisional KI#887 - CORPORATION-PRODUCED RESOURCE GAINS DO NOT VERIFY
PRODUCTION, SO A NEWLY AVAILABLE UPGRADE CAN LEAVE AN OBSOLETE UNIT ORDER LIVE
AND HURRYABLE FOR THE REST OF THE TURN
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvCity.cpp + CvUnit.cpp + current Corporation/Unit XML
Primary functions: CvCity::updateCorporationBonus, CvCity::verifyProduction,
CvCity::canTrain, CvCity::canHurry, CvCity::setHasCorporation
Classification: incomplete AdvCiv `advc.064d` immediate-invalidation repair retained by
Base AdvCiv 1.14 and current AdvCiv-SAS; made directly reachable by current corporation/unit data.

ROOT - `updateCorporationBonus()` VERIFIES ONLY LOSSES
-----------------------------------------------------
Current SAS snapshots every old `hasBonus()` state, rebuilds corporation-produced bonuses, then
processes each changed resource:

    if (abHadBonuses.get(eLoopBonus) != hasBonus(eLoopBonus))
    {
        if (hasBonus(eLoopBonus))
            processBonus(eLoopBonus, 1);
        else
        {
            processBonus(eLoopBonus, -1);
            if (bVerifyProduction)
                verifyProduction();
        }
    }

A lost corporation-produced resource therefore triggers immediate production validation, but a
gained corporation-produced resource does not.

Resource gain can invalidate production too. `CvCity::canTrain(..., bIgnoreUpgrades=false)` rejects
an otherwise trainable source unit whenever:

    allUpgradesAvailable(eUnit) != NO_UNIT

and `verifyProduction()` deliberately calls `checkCanContinueProduction(true, HUMAN_CHOOSE)`, whose
`bCheckUpgrade=true` path invokes `upgradeProduction()` before the final legality pass. Thus the
immediate-verification machinery explicitly treats newly available upgrades as a production-state
change that can invalidate a queued order.

CURRENT SAS FIXTURE - ALUMINUM CO. MAKES TANK -> MODERN TANK AVAILABLE
---------------------------------------------------------------------
Current XML provides a normal live chain:

- `CORPORATION_6` (Aluminum Co.) consumes Coal and produces Aluminum.
- `UNIT_TANK` upgrades to `UNIT_MODERN_TANK`.
- Tank requires Oil but not Aluminum.
- Modern Tank requires Oil + Aluminum, plus its ordinary tech/building prerequisites.

Take a human city that already has the Factory, Oil and technologies needed for both units except
that it currently has no Aluminum. A Tank can legally remain queued because Modern Tank is not yet
trainable.

During the owner player's unit phase, an Executive can successfully spread Aluminum Co. into the
city. `CvUnit::spreadCorporation()` calls:

    pCity->setHasCorporation(eCorporation, true, ...);

immediately on success. `setHasCorporation()` mutates corporation membership and immediately calls
`updateCorporation()`, which calls `updateCorporationBonus()` with production verification enabled.
The newly active Aluminum Co. therefore produces Aluminum immediately.

After that state change:

    allUpgradesAvailable(UNIT_TANK) == UNIT_MODERN_TANK

so the same city's `canTrain(UNIT_TANK, true)` is now false. But the corporation resource changed
from absent -> present, so `updateCorporationBonus()` takes its gain branch and never calls
`verifyProduction()`. The queued Tank remains the current order for the rest of the turn.

CONSEQUENCE IS STRONGER THAN A DISPLAY LAG - THE STALE TANK CAN BE HURRIED
-------------------------------------------------------------------------
`CvCity::canHurry()` checks disorder, production progress/cost, whether the current order is a unit
or building, gold and population. It does NOT call `canContinueProduction()`, `canTrain()`, or
`allUpgradesAvailable()`.

Therefore, after the owner spreads Aluminum Co. and creates the stale Tank order above, the human
can still whip/buy that Tank production during the same turn. `hurry()` spends the population/gold
and adds production to the stale current target.

At the next city-turn `doCheckProduction()` will finally call `upgradeProduction()` and rewrite the
Tank order to Modern Tank. The human has consequently been allowed to pay a hurry cost against an
order that was already illegal under the city's final current resource state. This is precisely the
kind of intra-turn stale production state that `advc.064d` says it exists to eliminate.

ANCESTRY / INTENT
-----------------
BtS/Civ4CE and K-Mod rebuild corporation-produced bonuses and process resource gains/losses but have
no local immediate production verifier.

AdvCiv practical 1501 / commit `72eb8cc66b8939503fd0205b9cbf0d72b6ea7b3e`, titled:

    Invalid orders don't generate overflow; art style bugfix

introduces `verifyProduction()` and states that when an order becomes invalid during the owner's
turn the city should immediately ask for a new order. In `updateCorporationBonus()` the same change
adds `verifyProduction()` only inside the resource-LOSS branch.

That handles the obvious "required resource disappeared" case, but overlooks the equally real
upgrade-legality case where a newly GAINED resource makes an upgrade trainable and thereby makes the
old source unit invalid. Base AdvCiv 1.14 and current SAS retain this asymmetry unchanged.

DEDUP
-----
Whole-album/Known-Issues searches for corporation bonus gain, immediate verification, Aluminum/Tank
and newly available upgrades found no earlier numbered root owning this missing gain-side verifier.

Independent of:

- F559/KI#882: `upgradeProduction()` overwrites a destination hammer bank once an upgrade migration
  actually runs. F564 is the earlier missing trigger that lets a now-obsolete source order survive
  after corporation resource gain.
- F563/KI#886: `changeNoBonusCount()` does call `verifyProduction`, but calls it against the OLD
  suppression state. F564 is a separate caller where corporation BONUS GAINS never call the verifier
  at all.
- F429/KI#752 and other corporation-preview/resource-dependency findings: those concern hypothetical
  UI calculations or corporation-resource propagation, not production queue invalidation.

REPAIR DIRECTION
----------------
When corporation recomputation changes any city-level `hasBonus()` state, process all bonus effects
first and then, if `bVerifyProduction`, call `verifyProduction()` once against the FINAL corporation
resource state regardless of whether each changed bonus was gained or lost.

A single post-loop verification is preferable to repeated per-resource calls and naturally handles
both:

- lost required resources; and
- gained resources that make queued unit upgrades newly available.

Regression fixture: queue Tank in a Factory/Oil city lacking Aluminum; spread Aluminum Co. from the
owner's Executive; assert that the Tank is immediately upgraded/revalidated before the player can
hurry the stale order.

-------------------------------------------------------------------------------
POST-F564 RESOURCE-SIBLING CONTROL - OLD CORPORATION OUTPUT IS SEEDED BEFORE RESET
-------------------------------------------------------------------------------
K-Mod deliberately resets `m_paiNumCorpProducedBonuses[i]` before seeding
`aiLastCorpProducedBonuses` from `getNumBonuses(i)`. AdvCiv's EnumMap refactor instead snapshots
`getNumBonuses()` first and resets `m_aiNumCorpProducedBonuses` afterward. Mechanically, the current
seed therefore includes the previous corporation output.

This can feed stale corporation output into another produced-resource generation in a resource
production dependency chain. Current SAS data masks the gameplay consequence, however: the only
produced resources are Oil and Aluminum, and neither is currently consumed by another corporation
that itself produces a resource. Preserve as future-proofing/refactor debt, not F565.

CURRENT STONE AFTER C031-WIP341
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F564 / provisional KI#887 CONFIRMED.
F565 / provisional KI#888 UNUSED.
Latest physical checkpoint: C031-WIP341.
No repository source modified.

NEXT
----
Continue Queue 006 under F565 unused. Leave the currently data-masked corporation seed-order
regression unnumbered; continue the low-coverage city hard-state census with hurry/conscript,
religion/corporation activation, culture ownership/revolt, reveal/visibility and rank/cache
mutation contracts. Dedup before promotion and freeze the next physical stone regardless of
whether F565 is used.

END C031-WIP341


===============================================================================
C031-WIP342 APPEND - F565 CONFIRMED / TEAM-CULTURE PERCENT DOUBLE TRUNCATION
===============================================================================

-------------------------------------------------------------------------------
F565 / provisional KI#888 - TEAM CULTURE PERCENT IS FORMED BY SUMMING ALREADY-
TRUNCATED PLAYER PERCENTAGES, SO MULTI-PLAYER TEAMS CAN FALL ONE OR MORE POINTS
BELOW THEIR TRUE SHARE AND CROSS HARD RAZING/OCCUPATION/CONSCRIPT THRESHOLDS
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvCity.cpp + CvPlot.cpp + CvPlayer.cpp + stock GlobalDefines
Primary functions: CvCity::calculateTeamCulturePercent,
CvPlot::calculateTeamCulturePercent
Classification: inherited stock BtS arithmetic defect retained by K-Mod, Base AdvCiv and current
AdvCiv-SAS; current multi-player teams/permanent alliances make the threshold consequences live.

ROOT - DIVIDE EACH MEMBER FIRST, THEN SUM
-----------------------------------------
Current `CvCity::calculateTeamCulturePercent` does:

    int iR = 0;
    for (PlayerIter<EVER_ALIVE,MEMBER_OF> it(eTeam); it.hasNext(); ++it)
        iR += calculateCulturePercent(it->getID());
    return iR;

where each `calculateCulturePercent(player)` independently computes:

    (playerCultureTimes100 * 100) / totalCultureTimes100

with integer truncation.

`CvPlot::calculateTeamCulturePercent` has the same shape: it sums each team member's independently
truncated player percentage instead of summing the team's raw plot culture first.

For a multi-player team this is not equivalent to the team percentage:

    floor(A * 100 / T) + floor(B * 100 / T)

can be smaller than:

    floor((A + B) * 100 / T)

The returned value is therefore not the integer percentage of TEAM culture; it is a sum of rounded-
down member percentages.

CURRENT HARD-THRESHOLD FIXTURE 1 - RAZING 25%
---------------------------------------------
Current SAS does not override the stock BtS define
`RAZING_CULTURAL_PERCENT_THRESHOLD`; the inherited value is 25.
`CvPlayer::canRaze()` forbids razing when:

    kCity.calculateTeamCulturePercent(getTeam()) >= 25

Take a captured city with total city culture split:

    team member A = 1
    team member B = 1
    all rivals     = 6
    total          = 8

The team's true integer share is:

    floor((1 + 1) * 100 / 8) = 25

but current code returns:

    floor(1 * 100 / 8) + floor(1 * 100 / 8)
    = 12 + 12
    = 24

Thus `canRaze()` allows razing even though the team's actual city-culture share is exactly the
configured 25% protection threshold.

The acquisition path preserves each player's city culture when reconstructing the captured city,
so such a split is a valid conquest state for a team game/permanent-alliance team.

CURRENT HARD-THRESHOLD FIXTURE 2 - OCCUPATION 75%
-------------------------------------------------
The inherited `OCCUPATION_CULTURE_PERCENT_THRESHOLD` is 75. During conquest, `acquireCity()` uses the
same city-team percentage to decide whether occupation should be added.

With:

    team member A = 3
    team member B = 3
    rivals        = 2
    total         = 8

true team share is exactly 75%, but current code returns:

    floor(300/8) + floor(300/8) = 37 + 37 = 74

so the `< 75` branch can impose occupation even though the team meets the configured exemption.

SIBLING HARD THRESHOLD - PLOT CULTURE / CONSCRIPTION 10%
--------------------------------------------------------
`CvCity::canConscript()` asks:

    getPlot().calculateTeamCulturePercent(getTeam()) < CONSCRIPT_MIN_CULTURE_PERCENT

The inherited threshold is 10. `CvPlot::calculateTeamCulturePercent()` repeats the same member-wise
truncation root. With plot culture:

    member A = 1
    member B = 2
    rivals   = 27
    total    = 30

true team share is exactly 10%, while current code returns:

    floor(100/30) + floor(200/30) = 3 + 6 = 9

and can therefore reject conscription at the very threshold the rules intend to allow.

ANCESTRY
--------
Stock BtS/Civ4CE already implements both city and plot team percentages by looping team members and
summing `calculateCulturePercent(member)`. K-Mod retains that arithmetic. AdvCiv changes member
iteration semantics (e.g. EVER_ALIVE where appropriate) but preserves the divide-before-sum form.
Base AdvCiv 1.14 and current SAS retain it.

Current light-source XML does not override the three relevant stock GlobalDefines; Civ4CE's BtS
GlobalDefines provide the inherited values:

    CONSCRIPT_MIN_CULTURE_PERCENT       = 10
    RAZING_CULTURAL_PERCENT_THRESHOLD  = 25
    OCCUPATION_CULTURE_PERCENT_THRESHOLD = 75

DEDUP
-----
Whole-album and Known-Issues searches for `calculateTeamCulturePercent`, team-culture truncation and
all three hard-threshold define names found no prior numbered root.

This is distinct from revolt/culture-strength findings: F565 is a pure aggregation arithmetic error
that exists even when every individual player's culture bank is correct.

REPAIR DIRECTION
----------------
For both city and plot helpers, sum raw culture for the requested team first, then divide once by the
same total used by the player-percentage helper. Preserve the current member-iteration policy.

City shape:

    int iTeamCulture = 0;
    for (PlayerIter<EVER_ALIVE,MEMBER_OF> it(eTeam); it.hasNext(); ++it)
        iTeamCulture += getCultureTimes100(it->getID());
    int const iTotal = countTotalCultureTimes100();
    return (iTotal > 0 ? iTeamCulture * 100 / iTotal : 0);

Plot should do the corresponding sum in its native plot-culture units.

Regression tests should use the exact 25%, 75% and 10% split fixtures above so a future refactor
cannot reintroduce per-member truncation.

CURRENT STONE AFTER C031-WIP342
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F565 / provisional KI#888 CONFIRMED.
F566 / provisional KI#889 UNUSED.
Latest physical checkpoint: C031-WIP342.
No repository source modified.

NEXT
----
Continue Queue 006 under F566 unused. Resume low-coverage hard-state census after the team-culture
family: religion/corporation activation and vote-source effects, city reveal/visibility, rank/cache
mutators, conquest/culture state, hurry/conscript arithmetic and remaining K-Mod -> AdvCiv semantic
refactors. Dedup before promotion and freeze the next stone regardless of whether F566 is used.

END C031-WIP342

===============================================================================
C031-WIP343 APPEND - F566 CONFIRMED / BARBARIAN NONE-BUILDING EVENT-STATE SENTINEL
===============================================================================

-------------------------------------------------------------------------------
F566 / provisional KI#889 - BUILDING HAPPY/HEALTH CLASS-CHANGE SETTERS LOST THE
NO_BUILDING GUARD; A BARBARIAN CAPTURE OF AN ANGEL-OF-MERCY HOSPITAL CITY CAN
INDEX BUILDING STATE WITH NO_BUILDING
-------------------------------------------------------------------------------
Status: CONFIRMED
File family: CvGameCoreDLL/CvCity.cpp + CvPlayer.cpp + current Event/Civilization XML
Primary functions: CvCity::setBuildingHappyChange, CvCity::setBuildingHealthChange,
CvCity::getNumActiveBuilding, CvPlayer::acquireCity
Classification: AdvCiv enum-map refactor regression retained by Base AdvCiv 1.14/current SAS;
current-data reachable through EVENT_ANGEL_OF_MERCY_2 and the Barbarian building map.

ROOT - THE K-MOD NO_BUILDING GUARD WAS LOST
-------------------------------------------
K-Mod's patched setters resolve the civilization-specific building for a building class and guard
that result before querying active-building state. In representative form:

    BuildingTypes eBuilding = ...getCivilizationBuildings(eBuildingClass);
    if (NO_BUILDING != eBuilding)
    {
        if (getNumActiveBuilding(eBuilding) > 0)
            ...
    }

AdvCiv practical 3183 / 59a3e3190061b421b117b58d047aa1196259f5fa,
"Rewrite of enum map classes", replaced the vector-backed class-change state with EnumMaps and
simplified both functions. In that hunk it also removed the existing

    FAssert(eBuilding != NO_BUILDING);

and made the active-building query unconditional:

    BuildingTypes eBuilding = getCivilization().getBuilding(eBuildingClass);
    ...
    m_aeiBuildingHappyChange.set(eBuildingClass, iChange);
    if (getNumActiveBuilding(eBuilding) > 0)
        ...

and equivalently for health.

Current SAS still has exactly this shape in both setters.

WHY NO_BUILDING IS NOT A VALID INPUT THERE
------------------------------------------
`getNumActiveBuilding(BuildingTypes eBuilding)` immediately asks the team whether that concrete
building is obsolete:

    if (GET_TEAM(getTeam()).isObsoleteBuilding(eBuilding))
        return 0;

`CvTeam::isObsoleteBuilding` indexes its BuildingTypes EnumMap through
`getObsoleteBuildingCount(eIndex)`. `ArrayEnumMap::get` asserts enum bounds and then performs the
unsafe indexed lookup. `NO_BUILDING` is the negative sentinel, not a valid BuildingTypes key.
Therefore this is an out-of-bounds assertion in debug and invalid EnumMap indexing in release;
it is not merely a harmless "no building means zero" query.

CURRENT-SAS DATA WITNESS - ANGEL OF MERCY + BARBARIAN CAPTURE
-------------------------------------------------------------
The shipped data supplies the entire path without hypothetical XML:

1. `EVENTTRIGGER_ANGEL_OF_MERCY` explicitly requires `BUILDINGCLASS_HOSPITAL` in the picked city.
2. `EVENT_ANGEL_OF_MERCY_2` stores BOTH of these persistent city class changes:

       BUILDINGCLASS_HOSPITAL  +1 happiness
       BUILDINGCLASS_HOSPITAL  +1 health

   `CvCity::applyEvent` writes them through `setBuildingHappyChange` and
   `setBuildingHealthChange` while the original civilization has a real Hospital, so the event
   itself is valid and the maps become nonzero.
3. Current `CIV4CivilizationInfos.xml` deliberately maps the Barbarian civilization's
   `BUILDINGCLASS_HOSPITAL` to `NONE`.
4. Barbarians can conquer cities through the normal `CvPlayer::acquireCity` path. That function
   snapshots every non-default building-class yield/commerce/happy/health change from the old city,
   destroys/recreates the city for the new owner, and then reapplies the class-change maps to the
   new city.
5. For the captured Angel-of-Mercy Hospital city, the new Barbarian city's
   `getCivilization().getBuilding(BUILDINGCLASS_HOSPITAL)` is therefore `NO_BUILDING`.
6. Reapplying the copied happiness map calls `setBuildingHappyChange(HOSPITAL,+1)`, stores the map
   entry, then calls `getNumActiveBuilding(NO_BUILDING)`. The health reapplication has the same
   flaw if execution reaches it.

So the fixture is simply:

    ordinary civ owns Hospital city
    -> Angel of Mercy 2 chosen there
    -> city later captured by Barbarians
    -> acquireCity reapplies Hospital +happy/+health class state under Barbarian civ
    -> NO_BUILDING reaches BuildingTypes EnumMap lookup

THE LATER ADVCIV CRASH FIX IS A STRONG SIBLING ORACLE
-----------------------------------------------------
AdvCiv practical 3414 / 02a30151ec..., titled

    "Fix crash when AP religion present in Barbarian city"

later discovered the same missing-sentinel problem in the two neighboring enum-map-refactored
setters:

    CvCity::setBuildingYieldChange
    CvCity::setBuildingCommerceChange

and repaired each by adding immediately after civilization-class resolution:

    if (eBuilding == NO_BUILDING)
        return;

That commit did NOT add the corresponding guard to `setBuildingHappyChange` or
`setBuildingHealthChange`, although practical 3183 had simplified all four in the same family.
The current source therefore contains two repaired siblings and two surviving vulnerable siblings.
This makes the intended contract unusually unambiguous.

ANCESTRY
--------
- BtS/Civ4CE/K-Mod family: class-change storage is vector-backed; K-Mod's patched happy/health
  setters explicitly guard `NO_BUILDING` before active-building queries.
- AdvCiv practical 3183 (`59a3e319...`, "Rewrite of enum map classes"): rewrites these maps and
  removes/loses the building-sentinel protection in the simplified setters.
- AdvCiv practical 3414 (`02a30151...`, "Fix crash when AP religion present in Barbarian city"):
  restores the missing guard to yield/commerce sibling setters only.
- Base AdvCiv 1.14 and current SAS: happy/health siblings remain unguarded.

DEDUP
-----
No prior album root owns this setter/sentinel regression. It is distinct from:

- F563/F564 resource-state production verification: those concern stale order legality after bonus
  mutations, not invalid BuildingTypes indexing;
- earlier AP/Barbarian history itself: practical 3414 fixed yield/commerce callers, whereas F566 is
  the two sibling setters left behind;
- generic event-state/UI projection findings: this path occurs while reconstructing actual city
  state during conquest and reaches a hard enum-domain boundary.

REPAIR DIRECTION
----------------
Mirror the already-established practical-3414 sibling fix in BOTH functions before touching the
per-city map or active-building state:

    BuildingTypes const eBuilding = getCivilization().getBuilding(eBuildingClass);
    if (eBuilding == NO_BUILDING)
        return;

Whether the class-change map should be retained even when the current civilization has no concrete
building is a separate serialization/design question. The existing yield/commerce precedent says
"return and do not retain"; matching it is the smallest consistent repair. A regression fixture
should give a Hospital city Angel of Mercy 2, then transfer it to the Barbarian player and verify
that city acquisition completes without an enum-bounds assertion/crash.

POST-F565 / PRE-F566 CONTROLS
-----------------------------
1. `setHasReligion` / `processVoteSource`: religion removal can leave an order stale until generic
   production cleanup, but no caller-specific immediate-verification contract stronger than the
   already-recorded generic canHurry robustness family was established. No F566 spent there.
2. `processVoteSource` building iteration: current XML has no duplicate `(BuildingClass,Religion)`
   religious-building mappings that would multiply an Apostolic-Palace-style effect.
3. `getReligionGrip`: `calculateCulturalOwner()==NO_PLAYER` is not dereferenced through the revolt
   branch because `revoltProbability` returns zero for that state.
4. city rank caches: base-yield/commerce/population mutators reach the player-wide invalidators;
   no new local-only stale-rank root found.
5. `getCulturePercentAnger`: AdvCiv's closed-border + wartime stacking is explicitly documented by
   `advc.099` and matches the intended additive defines; not a refactor bug.
6. `getAddedFreeSpecialistCount`: narrowing the loop to the current civilization's concrete
   building set is consistent with conquest building-class conversion for ordinary cities; no
   current hard mismatch established.
7. `getAdditionalDefenseByBuilding`: `RaiseDefense` makes the generic additive formula
   future-proofing-sensitive, but current SAS building data gives nonzero RaiseDefense only to
   Walls/Castles and gives those buildings zero additive `iDefense`; the suspected floor+modifier
   overprojection is currently data-masked.

CURRENT STONE AFTER C031-WIP343
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F566 / provisional KI#889 CONFIRMED.
F567 / provisional KI#890 UNUSED.
Latest physical checkpoint: C031-WIP343.
No repository source modified.

NEXT
----
Continue Queue 006 under F567 unused. First exhaust the remaining building-class-change siblings
(`setBuildingYieldChange`, `setBuildingCommerceChange`, acquisition/event serialization) now that
practical 3414 gives a precise sentinel contract; then return to the low-coverage K-Mod->AdvCiv
arithmetic/state-method census. Dedup before numbering and freeze the next physical stone regardless
of whether F567 is used.

END C031-WIP343



===============================================================================
C031-WIP344 - QUEUE 006 CVCITY.CPP CONTINUATION - F567 CONFIRMED
===============================================================================

F567 / PROVISIONAL KI#890 - COLONY-MAINTENANCE CAP MULTIPLIES MAX_DISTANCE FACTOR TWICE
--------------------------------------------------------------------------------------

SUMMARY
-------
`CvCity::calculateColonyMaintenanceTimes100(...)` inflates K-Mod's intended colony-maintenance
cap because an AdvCiv refactor replaces duplicated cap arithmetic with
`calculateDistanceMaintenanceTimes100(..., true)` but leaves an outer multiplication by
`MAX_DISTANCE_CITY_MAINTENANCE` in place. The helper already contains that factor internally.
With the shipped BtS/SAS inherited define value 25, the cap is therefore 25 times too large.

CURRENT CODE CONTRACT
---------------------
The extracted distance-maintenance helper begins with:

    int iTempMaintenance = 100 *
            GC.getDefineINT(CvGlobals::MAX_DISTANCE_CITY_MAINTENANCE) *
            calculateMaintenanceDistance(&kCityPlot, eOwner);

and then applies population/world/distance scaling. Passing `bNoPlayerModifiers=true` only omits
PLAYER handicap/distance modifiers; it does NOT omit `MAX_DISTANCE_CITY_MAINTENANCE`.

The colony-maintenance function then computes:

    int iMaintenanceCap = GC.getDefineINT(CvGlobals::MAX_DISTANCE_CITY_MAINTENANCE) *
            calculateDistanceMaintenanceTimes100(kCityPlot, eOwner, iPopulation, true);

so the factor is applied twice.

K-Mod's pre-refactor cap applies it exactly once:

    int iMaintenanceCap = 100 * MAX_DISTANCE_CITY_MAINTENANCE * maintenanceDistance;
    ... population/world/distance scaling ...
    iMaintenanceCap *= MaxColonyMaintenance;
    iMaintenanceCap /= 100;

The helper call already reproduces the first line plus those common scaling steps. The additional
outer factor has no K-Mod counterpart.

CURRENT DATA / MAGNITUDE
------------------------
BtS/Civ4CE `GlobalDefines.xml` defines:

    MAX_DISTANCE_CITY_MAINTENANCE = 25

and `iMaxColonyMaintenance` is 200 on every stock handicap. Current SAS inherits the same
MAX_DISTANCE factor unless explicitly overridden (no override is present in the supplied light
source). Thus the cap produced by the AdvCiv/SAS formula is 25x K-Mod's intended cap before the
later colony-maintenance civic modifier is applied.

This does not multiply the ordinary colony-maintenance amount itself; it inflates the upper bound.
The practical consequence is that a cap intended to constrain high colony maintenance becomes far
looser and can effectively cease to bind in many states. That is a concrete rules regression, not
a tuning preference.

ANCESTRY / BIRTH
----------------
AdvCiv practical 3680 / commit `b2709079f382d51015aedffae338fba399644174`, titled:

    Vassalage civic sets colony maintenance to 0

adds a `bNoPlayerModifiers` parameter to `calculateDistanceMaintenanceTimes100` and replaces the
explicit K-Mod colony-cap calculation with the comment:

    // advc: Let's do that without duplicating code

followed by the current helper call. The same hunk retains/adds the extra outer
`MAX_DISTANCE_CITY_MAINTENANCE` multiplication even though the helper includes that factor.
Base AdvCiv 1.14 and current SAS retain the error unchanged.

DEDUP
-----
Whole-album searches for colony-maintenance cap, `MAX_DISTANCE_CITY_MAINTENANCE`, and
`calculateColonyMaintenanceTimes100` found no prior numbered root owning this double application.
Existing colony-maintenance findings concern valuation/projection/civic effects, not the runtime cap
formula itself.

REPAIR DIRECTION
----------------
Remove the redundant outer factor and use the helper directly:

    int iMaintenanceCap = calculateDistanceMaintenanceTimes100(
            kCityPlot, eOwner, iPopulation, true);

then apply `MaxColonyMaintenance` and the dedicated colony-maintenance modifier exactly as current
code does. A regression test should compare the refactored result against K-Mod's explicit formula
for several distances/populations and assert exact equality when the new colony modifier is 0.

POST-F567 / PRE-F568 CONTROLS
-----------------------------
1. `setBuildingYieldChange` and `setBuildingCommerceChange` already contain the practical-3414
   `NO_BUILDING` guard. `setBuildingHappyChange` and `setBuildingHealthChange` are the two surviving
   unguarded siblings and are already owned by F566; acquisition/serialization does not expose a
   third independent setter root.
2. `alterSpecialistCount` SAS deltas are KI#313 provenance handling plus cached default-specialist
   lookup; no independent current arithmetic/state defect established in this pass.
3. Simple wrapper maintenance functions only divide the Times100 result and add no new semantics.

CURRENT STONE AFTER C031-WIP344
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F567 / provisional KI#890 CONFIRMED.
F568 / provisional KI#891 UNUSED.
Latest physical checkpoint: C031-WIP344.
No repository source modified.

NEXT
----
Continue Queue 006 under F568 unused. Audit the remaining low-coverage K-Mod -> AdvCiv semantic
refactors, prioritizing hurry/conscript arithmetic, maintenance helper parameter scopes,
`cultureGarrison`/revolt state, meltdown/building state, and mutable city rank/cache mutators.
Dedup before promotion and freeze the next physical stone regardless of whether F568 is used.

END C031-WIP344


===============================================================================
C031-WIP345 - QUEUE 006 CONTINUATION - F567 BREADTH / F568 UNUSED
===============================================================================

F567 BREADTH - HELPER SUBSTITUTION ALSO DROPS K-MOD'S HANDICAP DISTANCE FACTOR
-----------------------------------------------------------------------------
The practical-3680 helper substitution has a second semantic mismatch from K-Mod's explicit colony
cap formula. K-Mod's cap includes the owner's handicap `DistanceMaintenancePercent` while excluding
the player's runtime distance-maintenance modifier (e.g. State Property). The new helper parameter
`bNoPlayerModifiers=true` wraps BOTH terms inside the same exclusion block:

    if (!bNoPlayerModifiers)
    {
        ... getDistanceMaintenanceModifier() ...
        ... getHandicapType().getDistanceMaintenancePercent() ...
    }

The colony cap therefore loses the handicap factor too.

Current SAS handicap data make this live and nonconstant:

    Rookie      55%
    Settler     60%
    Chieftain   65%
    Warlord     70%
    Noble       75%
    Prince      80%
    Monarch     85%
    Emperor     90%
    Immortal    95%
    Deity      100%
    Deity Plus 105%

Thus F567's current formula differs from K-Mod in TWO ways caused by the same refactor root:

1. `MAX_DISTANCE_CITY_MAINTENANCE` is applied twice (25x with shipped define);
2. `DistanceMaintenancePercent` is omitted from the cap.

Do not allocate F568 separately. The robust repair is to reconstruct the intended K-Mod cap
semantics explicitly or give the helper independent switches for player runtime modifiers and
handicap/world scaling; a single broad `bNoPlayerModifiers` flag cannot currently express the old
formula exactly.

POST-F567 CONTROLS / F568 NOT SPENT
-----------------------------------
1. Hurry helper zero-cost intermediate: `getHurryCost(true, ...)` can mathematically reach 0 and
   `getHurryPopulation` would then floor to one population, but the live top-level `canHurry()`
   rejects current production that will finish from this turn's production/overflow/features. No
   demonstrated actual population hurry reaches that helper-only state.
2. `cultureGarrison(ePlayer)`'s apparently inverted team comparison is coherent with
   `CvUnit::isGarrisonInTeamCity()`: it counts the CURRENT city's team/master/vassal garrison while
   excluding units belonging to the revolting cultural owner's team.
3. `doMeltdown()` game-speed divisor scaling is safe under current data (`NukeExplosionRand` is far
   above the Quick-speed truncation-to-zero boundary); the roll-before-superseded check changes RNG
   consumption deliberately but not the stated meltdown eligibility contract.
4. Rank-cache getters recompute all city ranks together and the previously checked yield/commerce
   mutators reach player-wide invalidation; no new local stale-rank root found in this slice.

CURRENT STONE AFTER C031-WIP345
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F567 / provisional KI#890 CONFIRMED.
F568 / provisional KI#891 UNUSED.
Latest physical checkpoint: C031-WIP345.
No repository source modified.

NEXT
----
Continue Queue 006 under F568 unused. Resume the low-coverage inherited state-method census at
religion spread / GP runtime state, event and name/acquisition mutators, production experience,
meltdown and remaining K-Mod->AdvCiv arithmetic rewrites. Preserve F562 as the GP projection root
and F566/F567 family boundaries; promote only an independent current contract failure.

END C031-WIP345


===============================================================================
C031-WIP346 - QUEUE 006 CONTINUATION - F568 STILL UNUSED
===============================================================================

POST-F567 LOW-COVERAGE CONTROLS
-------------------------------
1. `getAdditionalYieldRateModifierByBuilding` can theoretically miss a hypothetical building's
   own `PowerYieldModifier` when that same building is what first creates power, because the loop
   over active power-yield buildings does not yet include the hypothetical building. Current SAS
   XML masks this: no shipped power/clean-power/power-bonus building also carries a nonzero
   `PowerYieldModifier`. Preserve as future-proofing debt, not F568.
2. `doGreatPeople` remains the runtime oracle for F562. Its integer per-unit increment is live and
   intentionally distinct from total aggregate GPP; no second runtime-state root found adjacent to
   the already-confirmed projection mismatch.
3. `doReligion`'s decrement of `iChances` after a successful spread is coherent with the formula's
   `- getReligionCount()` term: a successful spread consumes both the attempted candidate and one
   unit of remaining multi-religion capacity. No off-by-one root established.
4. `setName(..., bInitial=true)` now bypasses the duplicate-name validity scan for initial/acquired
   names by explicit SAS KI#161.2 policy. This can preserve an acquired name even if the new owner
   already has a matching city name, but the behavior is authored by the current comment/repair;
   no independent hard identity failure was demonstrated, so do not number it.
5. `addProductionExperience`'s free-promotion optimization uses `isAnyFreePromotion()` backed by the
   current EnumMap state; the promotion loop is not incorrectly gated by `canAcquirePromotionAny`.
6. `doMeltdown` / superseded-plant ordering and game-speed scaling remain internally consistent
   under current NukeExplosionRand data; no zero-divisor or wrong-plant removal state found.

CURRENT STONE AFTER C031-WIP346
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F567 / provisional KI#890 CONFIRMED.
F568 / provisional KI#891 UNUSED.
Latest physical checkpoint: C031-WIP346.
No repository source modified.

NEXT
----
Continue Queue 006 under F568 unused. Take the next hard-state layer: `allUpgradesAvailable` and
queue obsoletion contracts, city-event mutation helpers, production/commerce rank invalidation,
revolt/free-unit state and remaining static maintenance/culture arithmetic. Dedup against F333,
F559-F567 and earlier Queue-004 roots before promotion.

END C031-WIP346


===============================================================================
C031-WIP347 - QUEUE 006 CONTINUATION - OPTIONAL/SENTINEL CONTROLS, F568 UNUSED
===============================================================================

STATUS ENTERING THIS TRANCHE
----------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files complete; Queue 006 remains the protected open file.
F559 / KI#882 through F567 / provisional KI#890 CONFIRMED.
F568 / provisional KI#891 UNUSED.
No repository source modified.

POST-WIP346 HARD-STATE / SENTINEL CONTROLS
-------------------------------------------
1. `allUpgradesAvailable` / ordinary queue-obsoletion recursion was rechecked against its live
   callers. No new independent current root survived beyond the already-owned production/resource
   families F559, F563 and F564.
2. `applyEvent`'s random pillage-count upper-bound problem is already F413 / KI#736; do not
   duplicate it in Queue 006. The remaining event city mutators examined here either use the
   established setters or are current-data/policy bounded.
3. Population/rank/trade-route invalidation rechecks fold into existing roots where appropriate:
   the cross-player trade-route population lifetime is already F505 / KI#828, and base
   yield/commerce rank invalidation reaches the player cache through the existing mutator chain.
4. `addRevoltFreeUnits` can fall back from a CITY_DEFENSE-selected unit to an ATTACK-selected unit
   while still initializing it with UNITAI_CITY_DEFENSE. This is inherited BtS/K-Mod behavior and
   no hard legality/state failure was established; retain as policy/role robustness rather than
   F568.
5. `getRevoltProtection` / revolt garrison handling can observe third-party units on the city plot.
   This is inherited and can plausibly represent allied physical presence; no independent hard
   owner-domain contradiction was promoted.

LATENT ENUM->BOOL DEFECT - CURRENT DATA MASKS IT
------------------------------------------------
`CvCity::defensiveGarrison()` contains a real compile-valid sentinel mistake in the optional
DEMAND_BETTER_PROTECTION path. The local-city branch correctly tests:

    allUpgradesAvailable(pUnit->getUnitType()) != NO_UNIT

but the capital fallback uses the returned `UnitTypes` enum directly as a Boolean:

    (pCapital != NULL && pCapital->allUpgradesAvailable(pUnit->getUnitType()))

`NO_UNIT == -1` is truthy in C++, valid enum value 0 is false, and other valid unit indices are
truthy. Thus the capital fallback does not represent "an upgrade exists".

The defect is born in AdvCiv practical 2390 / commit
`8458e4ad2477a0876d577e0af502931bc350a8f9` ("Tweaks to demand-better-protection option"), whose
comment says the obsolete/outdated multiplier should apply when the current city OR capital can no
longer train the old unit.

Do NOT spend F568 under the current shipped-data standard, however. Current SAS has:

    DEMAND_BETTER_PROTECTION = 0
    DEMAND_BETTER_PROTECTION_OBSOLETE_PERCENT = 100

so the whole alternative protection mechanic is disabled, and even independently the outdated
multiplier is neutral (`rOUTDATED_PERCENT == 1`). Preserve as a strong future-proofing repair note.

NO-ESPIONAGE / TRADE-CULTURE MISMATCH - ALSO CURRENTLY MASKED
--------------------------------------------------------------
K-Mod's `getTradeCultureRateTimes100()` reads raw:

    m_aiCommerceRate.get(COMMERCE_CULTURE)

while current `getCommerceRateTimes100(COMMERCE_CULTURE)` correctly adds raw espionage commerce
under `GAMEOPTION_NO_ESPIONAGE`. Thus if K-Mod trade culture is enabled together with No Espionage,
trade-culture spread ignores espionage output that the core city-culture mechanic converts into
culture.

This is a genuine optional-feature compatibility mismatch inherited from K-Mod, but current SAS
ships:

    USE_KMOD_TRADE_CULTURE = 0

in `GlobalDefines_advc.xml`. Therefore the trade-culture producer/consumer block is disabled by
current data and does not consume F568.

CURRENT STONE AFTER C031-WIP347
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F567 / provisional KI#890 CONFIRMED.
F568 / provisional KI#891 UNUSED.
Latest physical checkpoint: C031-WIP347.
No repository source modified.

NEXT
----
Continue Queue 006 under F568 unused using a different objective lens: enabled-by-default
K-Mod->AdvCiv arithmetic/state refactors and current-data-exposed inherited helpers. Prioritize
integer operation ordering, sentinel/domain contracts and state mutation ordering; exclude disabled
optional systems and dedup against F391/F399/F413/F505/F559-F567 before promotion.

END C031-WIP347


===============================================================================
C031-WIP348 - QUEUE 006 CONTINUATION - F568 CONFIRMED
===============================================================================

F568 / PROVISIONAL KI#891 - MAXTYPICALDISTANCE IS USED AS A HARD RELIGION-DISTANCE BOUND
-----------------------------------------------------------------------------------------

SUMMARY
-------
AdvCiv's `advc.140` distance normalization replaces K-Mod/BtS uses of the real
`maxPlotDistance()` bound with `CvMap::maxTypicalDistance()` in live religion formulas that still
assume the denominator is an upper bound on `plotDistance()`.

`maxTypicalDistance()` is deliberately only a map/civ-density heuristic: it is derived from the
WorldInfo cell dimensions, recommended-vs-ever-alive civilization ratio, sea level and wraps. Map
scripts can independently override the actual plot grid. Consequently a legal city-to-city
`plotDistance()` can exceed `maxTypicalDistance()`.

Two current `CvCity.cpp` consumers then leave their documented/old bounded domains:

1. `CvCity::getReligionGrip()` turns the holy-city proximity bonus NEGATIVE for sufficiently distant
   connected cities.
2. `CvCity::doReligion()` lets the K-Mod spread-distance multiplier exceed the stated
   `[1, 1+iDistanceFactor]` range.

CURRENT CODE CONTRACT
---------------------
`getReligionGrip()` currently applies:

    iScore += iRELIGION_INFLUENCE_DISTANCE_WEIGHT *
            (GC.getMap().maxTypicalDistance() - iDistance) /
            GC.getMap().maxTypicalDistance();

K-Mod/BtS used `maxPlotDistance()`, making this term range from the full positive weight down to 0.
With a merely typical denominator, `iDistance > maxTypicalDistance()` makes the term negative.
Current SAS ships:

    RELIGION_INFLUENCE_DISTANCE_WEIGHT = 50

so this is a real score reversal, not a zero-weight dormant path.

`doReligion()` likewise computes:

    iDivisor *= 100 + (100 * iDistanceFactor * plotDistance(...)) /
            GC.getMap().maxTypicalDistance();

and immediately documents:

    // now iDivisor is in the range [1, 1+iDistanceFactor] * iDivisorBase

That range requires the normalized distance to be <= 1. The `maxTypicalDistance()` substitution no
longer guarantees it. Current SAS has `RELIGION_SPREAD_DISTANCE_FACTOR = 9` and divisor base 5, so
the out-of-range denominator changes actual spontaneous-religion spread probability.

CURRENT SHIPPED REACHABILITY FIXTURE
------------------------------------
This is reachable with current SAS map scripts/settings, not just a hypothetical custom map.

Use:

    World size: Standard
    Civilization players ever alive: 48 (supported by the current 48-civ DLL)
    Map script: SAS_Longworld
    Longworld width-per-player option: 15
    Longworld wrap mode: WrapX (the recommended/default wrap mode)
    Sea level: Medium

For Standard, current `CIV4WorldInfo.xml` has cell dimensions 39 x 28 and 8 default players.
Current `maxTypicalDistance()` uses approximately:

    sqrt(39 * 28 * sqrt(8 / 48)) * 3.5 ~= 73.90

and therefore rounds to about 74.

`SAS_Longworld.py`, however, overrides Standard to base width 34 and height 6, then width option 15
scales width to about 43 cells. Because a map-script grid override is multiplied by 4 in
`CvMap::reset`, the actual map is about 172 x 24 plots. Longworld generates one continuous
horizontal land strip across every X coordinate. With WrapX, city sites can therefore be roughly
86 plots apart horizontally on the same connected landmass - already greater than the ~74
`maxTypicalDistance()` heuristic.

At distance 86, the religion-grip term is approximately:

    50 * (74 - 86) / 74 = -8   (integer arithmetic; exact truncation depends on order)

where the old hard-bound formulation could never fall below 0.

The spread formula likewise uses normalized distance 86/74 > 1, exceeding its own documented
maximum distance factor. Thus both manifestations are current-data/current-script reachable.

WHY THIS IS ONE ROOT
--------------------
Both failures come from the same semantic substitution: a heuristic "typical distance" is used in
formulas whose algebra and comments require an actual upper bound. Do not split the religion-grip
negative score and spread-divisor overflow into separate findings.

ANCESTRY
--------
The behavior is AdvCiv `advc.140` lineage. K-Mod/BtS religion distance normalization used
`maxPlotDistance()`. AdvCiv introduced the typical-distance heuristic to reduce wrap/world-size
impact and changed these consumers to use it. Later AdvCiv practical 1955 / commit
`206f9c6a96fdafc1985b34e304438327304082be` ("Merge FLTA class with its base; resolve type cast
problems.") explicitly restores `CvMap::maxPlotDistance()` as the real geometric maximum while
keeping a separate `maxTypicalDistance()` function, yet the religion consumers retain the
`maxTypicalDistance()` substitution. Base AdvCiv 1.14 and current SAS retain it.

DEDUP
-----
Whole-album and current Known-Issues searches found no existing numbered root for
`maxTypicalDistance`, the religion influence distance weight or the religion spread distance bound.
This is independent of F567's maintenance helper misuse: both involve `maxTypicalDistance`, but
F567 is a colony-maintenance helper-substitution arithmetic regression, whereas F568 is a bounded
religion-distance normalization contract.

REPAIR DIRECTION
----------------
The conservative correctness repair is to restore a guaranteed bound for these bounded formulas,
e.g. use `maxPlotDistance()` as K-Mod did, or explicitly clamp the normalized distance to
`maxTypicalDistance()` before applying formulas that promise a 0..1 distance fraction. If the
design goal is to retain AdvCiv's reduced wrap sensitivity, use `min(iDistance,
maxTypicalDistance())` for these bounded religion terms rather than allowing the heuristic to be
crossed. Regression tests should include a shipped Longworld/48-civ state with city distance above
`maxTypicalDistance()` and assert:

    religion proximity contribution >= 0
    normalized spread-distance factor <= 1

POST-F568 / F569 NOT YET SPENT
------------------------------
1. `defensiveGarrison()` has a real enum-as-bool mistake in the optional
   DEMAND_BETTER_PROTECTION capital-upgrade fallback (`NO_UNIT == -1` is truthy), but current SAS
   ships the feature disabled and the obsolete multiplier neutral at 100%. Preserve as latent
   future-proofing, not F569.
2. `getTradeCultureRateTimes100()` ignores No-Espionage's espionage-to-culture conversion, but
   current SAS ships `USE_KMOD_TRADE_CULTURE = 0`. Preserve as optional-feature incompatibility,
   not F569.

CURRENT STONE AFTER C031-WIP348
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F568 / provisional KI#891 CONFIRMED.
F569 / provisional KI#892 UNUSED.
Latest physical checkpoint: C031-WIP348.
No repository source modified.

NEXT
----
Continue Queue 006 under F569 unused. Resume enabled-by-default inherited arithmetic/state auditing,
prioritizing current `CvCity` methods whose AdvCiv refactors changed integer operation order,
normalization bounds, owner/team/state receivers, or mutation ordering. Keep optional/disabled
systems as controls and dedup against F559-F568 before promotion.

END C031-WIP348

===============================================================================
C031-WIP349 — QUEUE 006 CONTINUATION — POST-F568 CONTROL TRANCHE
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
32/169 unique outer-queue files complete.
F559 / KI#882 through F568 / KI#891 CONFIRMED.
F569 / provisional KI#892 UNUSED.
Latest physical checkpoint: C031-WIP349.
No repository source files modified.

POST-F568 CLEAN / DEDUP CONTROLS
--------------------------------
This checkpoint preserves the investigation after WIP348 without inventing a new
finding merely to advance the number.

1. DEAD-CIV CULTURE ANGER IS EXPLICIT ADVC.099 POLICY, NOT F569.
   `getCulturePercentAnger()` intentionally iterates EVER_ALIVE rivals. The shipped
   define documentation explicitly says closed-borders culture anger also applies
   when the foreign civilization has been eliminated. The related revolt system
   likewise deliberately allows posthumous foreign culture. This is authored policy,
   not a liveness-filter regression.

2. `handleOverflow()` EARLY RETURN CANNOT DROP LEGAL FAIL-GOLD THROUGH ZERO
   OVERFLOW CAPACITY.
   `computeOverflow()` can conceptually distinguish retained overflow from lost
   production, but `overflowCapacity()` has a positive city-growth-threshold bound.
   In a legal city the retained capacity therefore cannot be zero while positive lost
   production needs payment. The tempting `iOverflow <= 0` early return does not
   create the feared current completion path.

3. `getProductionExperience(NO_UNIT, true)`'s doubled generic-XP term is an authored
   comparison score rather than unit XP arithmetic. Practical 2757 introduced the
   helper for city/AI/UI comparison, and K-Mod's military-production scoring already
   gives generic XP twice the weight of conditional domain XP. No hard state/value
   contract is violated.

4. GENERIC POPULATION-UNHEALTH PREVIEW ROUNDING DRIFT IS CURRENT-DATA MASKED.
   `getAdditionalHealthByBuilding()` rounds `population * deltaModifier / 100`, while
   runtime `unhealthyPopulation()` rounds old and new total population unhealth
   separately. Those formulas differ for some generic stacked fractional modifiers.
   Current SAS, however, only ships building population-unhealth modifiers of -20
   (Public Transportation) and -100 (National Park), with no civic source. A city
   cannot stack a second -20 Public Transportation; the -100 National Park state
   saturates the effect. Current legal combinations do not expose the generic
   counterexamples. Keep as future-proofing, not F569.

5. ADVC.912C MILITARY HAPPINESS IS INTERNALLY CONSISTENT.
   Current Civic XML explicitly documents Hereditary Rule's unchanged
   `iHappyPerMilitaryUnit=1` as happiness per every two eligible military units.
   `CvCity::getMilitaryHappiness()` implements exactly
   `(getMilitaryHappinessUnits() * getHappyPerMilitaryUnit()) / 2`.
   This is not an accidental halving.

6. ADVC.912C LUXURY HAPPINESS RUNTIME IS ALSO COHERENT.
   `getBonusGoodHappiness()` applies LuxuryModifier to the aggregate raw positive
   bonus happiness. Hereditary Rule's current +25% therefore deliberately allows
   quarter-happiness contributions from multiple luxuries to accumulate before
   integer truncation. The hypothetical-building omission remains the already-owned
   F397 breadth extension; no independent runtime F569 surfaced here.

7. PRACTICAL 2840 FLOATING-POINT/INTEGER REPLACEMENTS IN `CvCity` CLOSE CLEANLY.
   `getHurryCost()`'s positive-domain round-up expressions are algebraically
   equivalent to `intdiv::uceil`; the old-save GP overflow conversion is a
   type-preserving `scaled(...,100)` to `per100(...)` rewrite. The adjacent
   ROUND_DIVIDE-to-intdiv conversions preserve the intended signed/unsigned rounding
   contracts in their legal domains; the generic health-preview caveat is item 4.

8. FREE-BUILDING MULTIPLICITY CONTRACT IS SAFE UNDER LIVE CALLERS.
   `getNumBuilding()` adds real + free counts, making the old
   `setNumFreeBuilding()` delta expression look suspicious if arbitrary free counts
   were supplied. Live callers do not do that. `CvPlayer::m_aiFreeBuildingCount`
   tracks multiple empire-level sources; each city is switched only between free
   value 0 and 1 when the player-side count crosses the 0/positive boundary. The
   inherited city setter therefore remains correct under its actual contract.

9. LOW-COVERAGE SAS-vs-Base METHOD CENSUS DID NOT EXPOSE A NEW SAS-ONLY ROOT.
   The zero-/low-mention semantic deltas are overwhelmingly static define/info
   caching, diagnostics, or already-owned current fixes (e.g. KI#162 UI guard,
   KI#161/KI#325 naming, KI#313 specialist provenance). Stateful candidates were
   inspected individually rather than promoted from low literal mention counts.

NEXT
----
Continue Queue 006 from WIP349 / F569 unused. Change lens away from the now-bounded
advc.912c / BUG-preview helpers. Prefer K-Mod->AdvCiv refactors that alter mutation
ordering, owner/team/area receivers, enum/sentinel handling, or integer operation
ordering in enabled-by-default runtime mechanics. Dedup before numbering and keep
source untouched.

===============================================================================
C031-WIP350 - QUEUE 006 CONTINUATION - F569 CONFIRMED / REFACTOR-CENSUS STONE
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
32/169 unique outer-queue files complete.
F559 / KI#882 through F569 / KI#892 CONFIRMED.
F570 / provisional KI#893 UNUSED.
Latest physical checkpoint: C031-WIP350.
No repository source files modified.

F569 / KI#892 - CPPCHECK CLEANUP MOVED REAL-POPULATION TRUNCATION AFTER `* 1000`
---------------------------------------------------------------------------------

SUMMARY
-------
AdvCiv practical 2571 / commit `db829e2c201207c94e70e4745b2f98462ede3004`
("Misc. cleanup following a Cppcheck analysis") changed `CvCity::getRealPopulation()`
while converting the public return type from `long` to `int`.

The BtS/K-Mod expression is:

    ((long)pow((float)getPopulation(), 2.8f)) * 1000

which first truncates the nonlinear population estimate to a whole unit and then scales
it by 1000. Real population is therefore expressed in whole thousands.

Practical 2571 rewrote it as:

    (long long)(std::pow((float)getPopulation(), 2.8f) * 1000)

followed by an integer-range conversion. Moving `* 1000` inside the cast is not a
return-type cleanup: it preserves the fractional part of `pow(...)` to the nearest
whole person instead of truncating to the inherited whole-thousand scale. The same
rewrite was later carried to master by the 0.99 merge (practical 2998), and current
AdvCiv 1.14 and current SAS still use it.

CURRENT SHIPPED EFFECT
----------------------
The drift is immediate for ordinary city sizes. For example:

    city size 1: BtS/K-Mod 1,000    current SAS 1,000
    city size 2: BtS/K-Mod 6,000    current SAS about 6,964
    city size 3: BtS/K-Mod 21,000   current SAS about 21,674
    city size 8: BtS/K-Mod 337,000  current SAS about 337,794
    city size 11: BtS/K-Mod 823,000 current SAS about 823,947

(The final few units depend only on the normal floating-point evaluation of `pow`; the
semantic difference is the cast position and does not depend on a particular rounding
fixture.)

This is player-visible in current SAS. `CvDLLWidgetData` displays
`CvCity::getRealPopulation()`, and the Domestic Advisor Overview 4 explicitly exposes a
`POP#` "real population" column through the Python API.

It also changes existing population-feat timing because `CvPlayer::getRealPopulation()`
adds the city values and `CvAdvisorUtils.endTurnFeats()` compares that aggregate against
fixed thresholds with strict `>` tests. A small ordinary fixture demonstrates a real
branch change:

    city sizes: 2, 2, 6, 8

    BtS/K-Mod:
        6,000 + 6,000 + 150,000 + 337,000 = 499,000
        499,000 > 500,000  -> false

    current AdvCiv/SAS (approximately):
        6,964 + 6,964 + 150,946 + 337,794 = 502,668
        502,668 > 500,000  -> true

So the cleanup can mark the half-million population feat accomplished in a state where
the inherited real-population contract is still below the threshold. Other threshold
crossings are similarly possible when several fractional remnants accumulate.

ANCESTRY / INTENT EVIDENCE
--------------------------
The supplied independent references agree on the inherited formula:

* supplied K-Mod 1.46 keeps the cast before `* 1000`;
* supplied Civ4CE BtS keeps the same BtS expression;
* the parent side of practical 2571 itself has `1000 * (int)pow(...)`.

The practical-2571 log describes the commit as miscellaneous cleanup following Cppcheck
and gives no gameplay/population-formula redesign rationale. Its surrounding change is
explicitly about the unhelpful `long` return type (`sizeof(int)==sizeof(long)`). The
precision/scale change therefore does not follow from the stated cleanup goal.

DEDUP
-----
The album previously inspected `getRealPopulation()` in Queue 004, but only for the
separate large-empire INT_MAX saturation/API-range limitation. No existing finding or
Known-Issues entry owns the cast-order/whole-thousand regression or its population-feat
consequence. F569 is independent of that earlier range control.

REPAIR DIRECTION
----------------
Preserve practical 2571's overflow-safe wide intermediate and public `int` API while
restoring the inherited operation order, e.g. conceptually:

    long long iPop = (long long)std::pow((float)getPopulation(), 2.8f) * 1000;
    return truncIntCast<int>(iPop);

The important part is that the `pow` result is converted to the wide integer BEFORE the
`* 1000`. Regression checks should include city size 2 => 6,000 and an aggregate fixture
such as sizes 2,2,6,8 remaining below the 500,000 feat threshold.

POST-F569 ZERO-COVERAGE REFACTOR CONTROLS
-----------------------------------------
The WIP349 cursor ranked CvCity practical commits with little or no explicit album
coverage and inspected their supposedly semantics-preserving rewrites hunk-by-hunk.
The following do not consume F570:

1. PRACTICAL 1395 (`cbb4852bb0`) - LARGE CPPCHECK REFACTOR MOSTLY CLOSES CLEANLY.
   The reviewed CvCity hunks move declarations into narrower scopes, replace wrapping
   `if (changed)` blocks with equivalent early returns, and dedent the same mutations.
   Production-turn calculations, power/occupation/WLTKD mutations, working-plot state,
   real-building processing, queue locals and production upgrade locals retain their
   original operation/mutation order. The sea-prerequisite Boolean simplification is
   algebraically equivalent. Its `setNumFreeBuilding` shape was already proven safe
   under the live 0/1 city-side caller contract in WIP349. No independent root survived.

2. PRACTICAL 1719 (`7bc68e5a42`) - LIBERATION CHANGE IS EXPLICIT POLICY/BUGFIX.
   The new `isEverOwned` requirement deliberately prevents K-Mod's base-culture term
   from allowing a culture-level-0 city to be liberated to a player who never owned it.
   The remaining receiver/variable changes are equivalent. Not F570.

3. PRACTICAL 2426 (`305e9a6750`) - `isNoMaintenance()` EXACTLY FACTORS THE OLD TEST.
   The helper is precisely `isDisorder() || isWeLoveTheKingDay() || population <= 0`,
   the negation of the old live-maintenance condition. No semantic drift.

4. PRACTICAL 2462 (`ed23feded9`) - HURRY-ANGER / MAINTENANCE REFORMATTING IS EXACT.
   The `iChange == 0` early return preserves the old guarded timer rescale, including
   computing the old ratio before mutating the modifier. Parenthesis/style changes do
   not alter arithmetic.

5. PRACTICAL 2571 - OTHER CVcity CLEANUP HUNKS CLOSE APART FROM F569.
   The forced-production variable rewrite still calls `doCheckProduction()` for side
   effects and always passes false to `doProduction`; hurry-overflow NULL guarding,
   trade local cleanup, iterator increments and scoped queue strings preserve their
   caller contracts. F569 specifically owns the non-equivalent real-population cast.

6. PRACTICAL 2590 (`d13882d579`) - EARLIER ANCESTRY FOR EXISTING F566, NOT F570.
   This practical explicitly removes the `NO_BUILDING` early returns from
   `setBuildingHappyChange` and `setBuildingHealthChange`, leaving only assertions.
   That is the same hard sentinel root already confirmed as F566 / KI#889. WIP343's
   current-data fixture remains decisive: an ordinary civilization can receive
   `EVENT_ANGEL_OF_MERCY_2` on a Hospital city, then Barbarian conquest copies the
   persistent class happy/health changes through `CvPlayer::acquireCity`; the new
   Barbarian city maps `BUILDINGCLASS_HOSPITAL` to `NO_BUILDING`, and restoration reaches
   the unguarded active-building lookup. Barbarians not running events themselves does
   NOT mask that capture/restore path.

   Ancestry correction/breadth for F566: practical 2590 is the earliest supplied AdvCiv
   hunk in this census that removes the K-Mod-style guards. Practical 3183 later rewrites
   the class-change maps and retains the vulnerable no-guard shape; practical 3414 repairs
   the yield/commerce sibling setters only. Do not allocate a second finding.

7. PRACTICAL 2617 (`3c7a6bbcf7`) - ADJACENT-PLOT ITERATOR REPLACEMENT IS EQUIVALENT
   ON SHIPPED MAP DIMENSIONS.
   `plotDirection`/cardinal loops are replaced by the corresponding adjacent/orthogonal
   iterators. The only generic distinction would require pathological tiny wrapped maps
   where geometric directions alias the same plot; current supported map grids are far
   outside that case.

8. PRACTICAL 2655 (`f25915b8ba`) - REVOLT-PROBABILITY MOVE IS ORDER-NEUTRAL.
   `occupationFactor = 0.5` moves below an early zero-return war check; paths that use
   the factor still assign the same value, while the skipped path returns zero directly.

9. PRACTICAL 2986 (`48710959f4`) - POINTER/STYLE AND `_MOD_FRACTRADE` CLEANUP CLOSE.
   `isConnectedTo` pointer-to-reference conversion is used only after non-null checks;
   anger arithmetic is parenthesis-only; religion `plotDistance` overload replacement
   is equivalent. The disabled `_MOD_FRACTRADE` branches are not a current SAS path.
   The retained `maxTypicalDistance` religion issue is already F568, not this practical.

10. PRACTICAL 3047 (`2b7d7298e0`) - LOCAL CACHING/REUSE ONLY.
    Process-building happiness/health values and specialist lookup results are cached
    without changing state order or source.

11. PRACTICAL 3052 (`0e04fd7f79`) - SPARSE ENUMMAP ITERATION PRESERVES ZERO-DEFAULT
    SEMANTICS.
    Full enum scans with zero/default checks become NON_DEFAULT key/pair scans for
    building prerequisites, production modifiers, religion influence, specialists,
    unit-combat XP, preview bonus health/happiness and related nested maps. Skipped
    entries were zero/false and had no side effect; explicit zero is likewise default.

12. PRACTICAL 3143 (`826a14644c`) - CULTURE-SPREAD `floor()` IS NOT AN INDEPENDENT
    POSITIVE-CULTURE REGRESSION; CITY-TRADE ROOT IS ALREADY F460.
    The rewrite changes the scaled final conversion from `round()` to `floor()`, but
    K-Mod's positive floating-point path truncates positive culture and therefore has
    floor semantics. The commit explicitly fixes the prior negative-culture sign error.
    Its city-trade conversion failure has already been deduplicated to F460. No F570.

13. PRACTICAL 3397 (`fe3e71e351`) - CULTURE SETTER EARLY RETURN IS EQUIVALENT.
    `setCultureTimes100` changes an outer inequality guard into an equality early return
    and performs the same mutation, level update and plot-culture delta afterward.

14. PRACTICAL 3592 (`3f6bf79aca`) - POPULATION-RANK REFACTOR IS NAMING/FORMATTING.
    The same `(negative population, city ID)` pairs are sorted and assigned ranks in the
    same order. Independent rank invalidation defects remain owned by earlier findings.

15. PRACTICAL 3887 (`6f8d41a1ac`) - HEALTH AGGREGATION MAX/MIN REWRITE IS ALGEBRAIC.
    Replacing repeated positive/negative `if` additions with `std::max(0,x)` and
    `std::min(0,x)` preserves every component including espionage health.

16. PRACTICAL 3964 (`eeed71fa81`) - OPTIONAL OVERFLOW OUTPUT `LOCAL_REF` REWRITE
    PRESERVES LIVE CALLERS.
    The macro initializes the same outputs and aliases non-null output pointers exactly
    as the prior pointer checks. Current callers pass distinct local output variables or
    NULL; no output-pointer alias case changes behavior. WIP349 separately proved that
    the later zero-retained-overflow early return cannot discard legal fail-gold because
    overflow capacity is positive in a legal city.

CURRENT STONE AFTER C031-WIP350
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F569 / KI#892 CONFIRMED.
F570 / provisional KI#893 UNUSED.
Latest physical checkpoint: C031-WIP350.
No repository source modified.

NEXT
----
Continue Queue 006 under F570 unused. The zero-covered small-refactor tranche is now
substantially bounded, so change lens again rather than repeatedly rechecking syntax-only
commits. Prefer enabled current-data `CvCity` contracts where inherited K-Mod/AdvCiv
changes can violate a hard invariant across callers: signed sentinel/range interpretation,
state mutation before/after callbacks, owner/team identity across acquisition, cached
cross-object values, or arithmetic that crosses a documented threshold. Dedup against
F559-F569 before promotion and freeze another stone even if F570 remains unused.

END C031-WIP350


================================================================================
C031-WIP351 - QUEUE 006 CVCITY.CPP / HARD-INVARIANT CONTROL STONE, F570 UNUSED
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F569 / KI#892 remain CONFIRMED.
F570 / provisional KI#893 remains UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot, with the
supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS material used locally for comparison.
No repository source files modified.

POST-WIP350 HARD-INVARIANT / LOW-COVERAGE CONTROLS
---------------------------------------------------
1. `GPProjection()` is not a fresh F570 seam. The apparent asymmetric percentage-normalization
   problem (positive deficits can be adjusted while totals above 100% are left alone) is already
   part of F562 / KI#885. F562 proves the deeper cause: projected per-unit GP progress keeps
   fractional future progress while runtime truncates each GP type every turn, so e.g. current
   Parthenon + Heroic Epic data can project about 75% + 38% although runtime selection banks are
   75% + 25%. Do not duplicate it.

2. `addRevoltFreeUnits()` fallback role mismatch is already an explicit WIP347 control. It may
   select an `UNITAI_ATTACK` candidate after `UNITAI_CITY_DEFENSE` has no candidate and then spawn
   that unit with `UNITAI_CITY_DEFENSE`, but this is inherited BtS/K-Mod behavior. No hard
   legality/state failure or current independent root was established; keep it as role-policy
   robustness, not F570.

3. SAS-specific `CvCity::doTurn` emergency/no-production production logic was reopened only far
   enough to map it against the earlier File-006 audit before spending time on already-owned code.
   The existing album already owns the live priority defect as provisional KI#333 and bounds the
   related water-building/fallback paths under KI#310, KI#313 and explicit construction/push-order
   controls. No new contradictory state surfaced in this pass.

4. STATIC-LOCAL CROSS-OBJECT CACHE SIEVE CLOSES CLEANLY.
   Current `CvCity.cpp` static locals introduced/retained for performance are fixed GlobalDefine
   values, InfoType IDs, colors or other XML constants. No static local in the reviewed city paths
   caches owner/team/city/game-turn-dependent computed state and then reuses it for a different
   city. This lens therefore produced no F570.

5. `revoltProbability()`, `probabilityOccupationDecrement()`, `doRevolt()` and nearby occupation
   ordering remain internally coherent under current contracts.
   - the city-level `calculateCulturalOwner()` helper honors REVOLTS_IGNORE_CULTURE_RANGE, so the
     probability path and the source-player path do not silently use different culture-range
     policies;
   - occupation decrement is rolled first and returns immediately on success, matching the
     already-audited help-text ordering;
   - if the speed-adjusted revolt probability reaches/exceeds one, leaving the decrement result at
     zero is mathematically consistent with `(1-p)^exponent` at p >= 1;
   - `damageGarrison` affecting third-party co-located defenders is inherited and was already
     retained as plausible physical-presence mechanics.
   No independent current root survived.

6. GENERIC PRODUCTION-RATE ZERO-DENOMINATOR IS REAL BUT CURRENT SAS DATA MASKS IT.
   In `getProductionDifference()`, the feature-production-needed branch computes:

       iModifier = getBaseYieldRateModifier(YIELD_PRODUCTION, iProductionModifier)
       ...
       iFeatureProdNeeded = iTmp / iModifier - iRate

   while `getBaseYieldRateModifier()` clamps to `std::max(0, ...)`, not 1. Thus a modded legal
   0%-production-rate state could divide by zero. Current shipped SAS does not reach that state:
   build-specific unit/building/project production modifiers are clamped to at least -50, and the
   current building/civic/player/area/global production-yield modifier sources inspected here do
   not supply a compensating negative production-rate modifier. Preserve as mod-mod robustness;
   do not spend F570 under the current-data standard.

7. `isPrereqBonusSea()` looked broad because it tests whether a passable adjacent water AREA has
   any bonus rather than checking one specific resource. Current unit XML resolves the contract:
   the only DOMAIN_SEA units with `bPrereqBonuses=1` are `UNIT_WORKBOAT` and
   `UNIT_BARBARIAN_WORKBOAT`. The helper is the AdvCiv lake-workboat exception and its area-level
   resource test matches that authored purpose. No current wrong-unit eligibility root established.

8. `getCityBillboardProductionIcon()` still assumes a valid typed queue entry after
   `canBeSelected() && isProduction()`. K-Mod had assertions rather than runtime guards for the same
   typed getters, and no ordinary current producer of a malformed queue entry was established.
   The separate production-bar callback has SAS hardening because of reproduced KI#162/KI#103
   crash evidence; absent comparable evidence/path here, do not manufacture F570 from corruption
   alone.

CURRENT STONE AFTER C031-WIP351
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F569 / KI#892 CONFIRMED.
F570 / provisional KI#893 UNUSED.
Latest physical checkpoint: C031-WIP351.
No repository source modified.

NEXT
----
Continue Queue 006 under F570 unused. Prefer lower-coverage AdvCiv-added city helpers whose values
feed ordinary gameplay rather than UI-only corruption hardening: reconstruction/state helpers,
mission/culture arithmetic, acquisition/contact identity and production-state mutators. For each
candidate require a current shipped-data witness, dedup against the whole album, and distinguish a
real hard contract failure from intentional AdvCiv policy or future mod-mod robustness. Freeze the
next physical stone whether or not F570 is consumed.

END C031-WIP351


================================================================================
C031-WIP352 - QUEUE 006 CVCITY.CPP / ACTIVE INVESTIGATE TEAM-OWNERSHIP BUG, F570
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F570 / KI#893 are now CONFIRMED.
F571 / provisional KI#894 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot, with the
supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS material used locally for comparison.
No repository source files modified.

POST-WIP351 LOWER-COVERAGE GAMEPLAY-HELPER CONTROLS
----------------------------------------------------
1. `calculateBaseYieldRate()` closes cleanly after tracing the actual stored-yield writers.
   This helper is gameplay-relevant rather than a debug-only calculator: AdvCiv's scenario
   initialization recomputes each city's stored base yield from it. The reconstruction covers the
   same live components that normally feed `m_aiBaseYieldRate`: worked-plot yield, specialist base
   yield plus player extra specialist yield, active-building yield plus city building-class yield
   overrides, trade yield and corporation yield. No omitted current source was established.

2. `updateBuildingDefense()` is consistent with AdvCiv's authored obsolescence contract.
   Castle-family buildings still carry `RaiseDefense` despite obsoleting at Military Science, which
   initially makes the raw `getNumBuilding()` scan look stale. The tech-obsolescence path calls
   `processBuilding(..., bObsolete=true)`, however, and that function deliberately skips the entire
   building-defense/bombard-defense mutation in obsolescence mode. Technological obsolescence is
   therefore intentionally not supposed to remove the physical defensive works; actual building
   removal does recompute them. The helper's "No obsoletion to worry about" comment is coherent
   with that rule. Not F570.

3. `failGoldPercent()` retains a hard-coded `(ProcessTypes)0` = Wealth assumption. This is fragile
   under process reordering, but current SAS XML still has `PROCESS_WEALTH` first and converts 50%
   to gold, so the live assumption holds. Future/mod-mod robustness only, not F570.

4. `cultureTimes100InsertedByMission()` preserves a deliberate AdvCiv change rather than a unit
   mismatch. Runtime and mission-cost arithmetic both work in hundredths of culture; AdvCiv raises
   the minimum insertion to one full culture (`max(100, ...)`) rather than BtS's effectively tiny
   minimum. Current XML and the cost path use the same units. Not F570.

5. `meetNewOwner()` / acquisition contact ordering does not leave a lasting missing-contact state in
   the reviewed current paths. The explicit city-reveal/contact helper is conservative, while plot
   ownership/reveal handling can establish the ordinary owner contact afterward. No current hard
   acquisition identity failure survived.

6. Production-state wrappers/setters, `isAnyProductionProgress()`, `doPopOrder()`,
   `sanitizeProductionTurns()`, `canUpgradeTo()` and the remaining thin low-coverage helpers did not
   expose an independent state contradiction in this tranche. Known production issues remain owned
   by prior findings (especially F559 for `upgradeProduction()`).

-------------------------------------------------------------------------------
F570 / KI#893 - CONFIRMED
ADVC ACTIVE INVESTIGATE-CITY PERMISSION IS STORED AS A GLOBAL CITY BOOL, SO A
LATER HOTSEAT/PBEM ACTIVE TEAM CAN INHERIT ANOTHER TEAM'S PAID CITY-INVESTIGATION
ENTITLEMENT BEFORE THE TARGET CITY'S NEXT TURN
-------------------------------------------------------------------------------

ROOT / ANCESTRY
---------------
AdvCiv practical 1412 (`ef40cbb8b47a2b11e4b213076d0f1cad62ad13b1`, advc.103,
"Misc. tweaks (mostly UI)") revised city investigation into an active spy mission. Its commit
message says the new mission has base cost 40 espionage, 100% success, and leaves the spy in the
foreign city rather than returning it to the capital. Current shipped SAS still contains:

    ESPIONAGEMISSION_INVESTIGATE_CITY_ACTIVE
    iCost = 40
    bIsPassive = 0
    bInvestigateCity = 1
    iDifficultyMod = -100
    bReturnToCapital = 0

The same practical introduced one city member:

    bool m_bInvestigate; // advc.103: Refers to the active team

and made selection permission begin with:

    if (m_bInvestigate || isActiveTeam() || kGame.isDebugMode())
        return true;

Execution of any Investigate-City mission sets only that global bit:

    if (kMission.isInvestigateCity() && pCity != NULL)
    {
        pCity->setInvestigate(true);
        gDLL->UI().selectCity(pCity);
        ...
    }

There is no stored team/player identity associated with the entitlement. The comment explicitly
states team-scoped semantics, but the representation cannot enforce them.

IMPORTANT CORRECTION TO THE INITIAL CANDIDATE
---------------------------------------------
The bit is NOT permanently one-way. Current `CvCity::doTurn()` has:

    m_bInvestigate = false; // advc.103

and initialization/reset also starts it false. The confirmed defect is therefore not "active
investigation lasts forever". It is a cross-active-team ownership/lifetime defect during the
window before the investigated TARGET city executes its next `doTurn()`.

CURRENT HOTSEAT REPRODUCTION / TURN-ORDER PROOF
-----------------------------------------------
Use three alive human civilizations on three different teams in sequential Hotseat; call them A,
B and C, with turn order A -> B -> C. Let C own a foreign city `c` that B has revealed but is not
otherwise entitled to investigate. During A's human turn:

1. A has a spy on `c`, enough directional EP, and performs the shipped active Investigate City
   mission. `CvPlayer::doEspionageMission` sets `c.m_bInvestigate = true` and opens `c` for A.
2. A ends its turn. `CvPlayer::setTurnActive(false)` calls A's `CvPlayer::doTurn()` before
   activating the next player. That `doTurn()` iterates only A's own cities and calls their
   `CvCity::doTurn()`; foreign city `c`, owned by C, is not touched, so its flag remains true.
3. Hotseat then activates B. `CvPlayer::setTurnActive(true)` calls `CvGame::setActivePlayer(B)`.
   `setActivePlayer/updateActiveVisibility` clears the previous UI city selection, but neither
   clears nor re-keys `c.m_bInvestigate`.
4. During B's turn, `c.canBeSelected()` reaches the unconditional `m_bInvestigate` test before the
   passive-espionage/current-active-team tests. It therefore returns true for B although B did not
   perform/pay for A's active mission and need not satisfy B's own passive Investigate threshold.
5. The permission persists for intermediate players until C's city turn eventually executes and
   clears the bit (or another lifecycle reset occurs).

This is not dependent on speculative malformed state. The active mission is shipped and normally
available through the espionage popup; `canDoEspionageMission` contains no Hotseat/PBEM exclusion.
Sequential Hotseat's ordinary player-turn handoff supplies the cross-team active-player transition.

PBEM BREADTH
------------
The same representation is also structurally unsafe for PBEM. `m_bInvestigate` is serialized in
`CvCity::read/write`, while the ending player's `doTurn()` again clears only that player's own
cities before control/save transfer. Thus an investigated city owned by a later player can carry
the unkeyed entitlement across the handoff/save. Hotseat is sufficient as the concrete confirmed
fixture; PBEM should be covered by the repair rather than treated as a separate finding.

WHY THIS IS A REAL INFORMATION/ENTITLEMENT LEAK
-----------------------------------------------
`canBeSelected()` is precisely the gate AdvCiv altered so the paid active mission can open and
re-open the foreign city screen. Returning true for a different active team grants that UI access
through the same entitlement path. The issue is therefore not merely a stale visual bit: one human
team can inherit another team's paid espionage city-investigation permission.

DEDUP
-----
Whole-album search found prior Investigate City work, but not this root. In particular F508/KI#831
owns stale `m_abEspionageVisibility` / plot-sight invalidation for passive espionage thresholds.
Passive research/demographics/Investigate checks were explicitly bounded there as dynamically
queried values. F570 is different: advc.103's ACTIVE mission creates a transient city-selection
entitlement whose storage loses the investigating team's identity across active-player handoff.
Do not merge it into F508.

REPAIR BOUNDARY
---------------
The entitlement needs to be keyed to the investigating team (the contract named by the existing
header comment), not represented by one city-global Boolean. A robust repair should record
per-team investigation permission, set only the acting player's team on successful active
Investigation, require the current active team to own that permission in `canBeSelected()`, and
clear the relevant temporary permissions at the intended city-turn lifetime boundary.

A single `TeamTypes` field would fix sequential Hotseat but can still overwrite another team's
permission under simultaneous multiplayer; a per-team bit/EnumMap is the safer contract if active
investigation is supported there. Any serialization change should deliberately handle current save
compatibility. Merely clearing the old bool in `setActivePlayer()` would address the sequential
handoff symptom but encodes UI-global lifetime rather than the stated team-owned semantics and is
not the strongest general repair.

CLASSIFICATION
--------------
AdvCiv-specific active-espionage transient-entitlement ownership bug; current shipped mission and
ordinary three-human Hotseat settings provide a direct live witness. Also structurally relevant to
PBEM because the unkeyed flag is serialized.

CURRENT STONE AFTER C031-WIP352
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F570 / KI#893 CONFIRMED.
F571 / provisional KI#894 UNUSED.
Latest physical checkpoint: C031-WIP352.
No repository source modified.

NEXT
----
Continue Queue 006 under F571 unused. The lower-coverage reconstruction/acquisition/active-mission
pass has now yielded F570 and bounded several tempting helpers. Continue the SAS-vs-AdvC semantic
function-body census, prioritizing current gameplay state rather than logging/UI-only changes:
`initialPopulation`, `cultureStrength`, `setCultureLevel`, `setHasReligion`, `liberate`,
`failProduction` and other custom/inherited mutations whose receiver, owner/team identity, callback
ordering, or signed arithmetic differs materially from Base AdvCiv/K-Mod. Require a shipped-data
reproducer and whole-album dedup before promotion. Freeze another physical stone whether or not
F571 is consumed.

END C031-WIP352


================================================================================
C031-WIP353 - QUEUE 006 CVCITY.CPP / TRAIT-CULTURE ABSOLUTE-SETTER BUG, F571
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F571 / KI#894 are now CONFIRMED.
F572 / provisional KI#895 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot, with the
supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS material used locally for comparison.
No repository source files modified.

POST-WIP352 SAS-vs-ADVC STATEFUL-HELPER CONTROLS
-------------------------------------------------
1. The SAS-vs-AdvC function-body census re-found a real upstream lifetime hazard in
   `CvCity::liberate()`: Base AdvCiv calls `GET_TEAM(getTeam())` / `GET_PLAYER(getOwner())` on the
   old city object after `CvPlayer::acquireCity()`, even though `acquireCity()` kills the old city
   and explicitly says that its old-city pointer "Mustn't be accessed past this point". Current SAS
   already repairs this by caching `eOldOwner` / `eOldTeam` before acquisition. This is existing
   KI#486.2 / practical 6290 ancestry, not F571.

2. `initialPopulation`, `cultureStrength`, `setCultureLevel`, `setHasReligion`, `failProduction`,
   `doPartisans`, `doRevolt`, hurry paths and the first remaining SAS body differences reduce to
   static-constant caching, diagnostics/logging, formatting/comment refactors, or previously owned
   fixes. No fresh semantic root was established from those differences.

3. The earlier album control around advc.908b was deliberately re-opened only at the arithmetic
   layer. That control proved the acquisition lifecycle is complete: `keepCity()` invokes
   `initTraitCulture()` immediately for a non-occupied acquired city, while `setOccupationTimer()`
   invokes it when occupation ends. It did NOT validate what `initTraitCulture()` writes. F571 is
   therefore independent rather than a duplicate of that lifecycle control.

-------------------------------------------------------------------------------
F571 / KI#894 - CONFIRMED
ADVC.908B REFACTORED CREATIVE FREE-CITY CULTURE INTO A DELTA, THEN PASSED THAT
DELTA TO THE ABSOLUTE `setCulture` SETTER; ACQUIRED CITIES BELOW THE TRAIT FLOOR
CAN RECEIVE TOO LITTLE CULTURE OR EVEN LOSE EXISTING CULTURE
-------------------------------------------------------------------------------

ROOT / CURRENT CODE
-------------------
Current `CvCity::initTraitCulture()` (advc.908b) does:

    int iFreeCityCulture = GC.getGame().freeCityCultureFromTrait(eLoopTrait)
            - getCulture(getOwner());
    if (iFreeCityCulture > 0)
        setCulture(getOwner(), iFreeCityCulture, true, false);

The local is therefore the MISSING / DELTA amount: target trait culture minus current city culture.
But `CvCity::setCulture` is an ABSOLUTE setter:

    void CvCity::setCulture(PlayerTypes ePlayer, int iNewValue, ...)
    {
        setCultureTimes100(ePlayer, 100 * iNewValue, ...);
    }

Thus for target F and existing owner culture C with 0 < C < F, the helper writes `F-C` as the new
absolute culture total instead of bringing the city to F.

CURRENT SHIPPED DATA
--------------------
Current SAS `TRAIT_CREATIVE` has `iFreeCityCulture = 10`; this is the only nonzero
`iFreeCityCulture` entry in the shipped trait file. `CvGame::freeCityCultureFromTrait()` scales it
by GameSpeed `iTrainPercent`. Current Normal speed has `iTrainPercent = 100`, so the intended
Creative free-city-culture target is exactly 10.

The error therefore spans every ordinary Normal-speed existing value 1..9:

    current C    code computes F-C    absolute result    intended floor
        1               9                   9                 10
        4               6                   6                 10
        5               5                   5                 10
        6               4                   4                 10
        9               1                   1                 10

For C > 5 the supposedly beneficial top-up actively REDUCES city culture.

CLEAN CURRENT-SAS CITY-TRADE WITNESS
------------------------------------
Use Normal speed. Let a non-Creative old owner peacefully trade/cede a city to a Creative player.
Arrange the old owner to have exactly 18 city culture there and the Creative recipient to have 0
pre-existing city culture. Eighteen is an ordinary legal accumulated city-culture value.

`CvPlayer::acquireCity()` first recreates the city, then restores the saved per-player city-culture
values. In the `bTrade` path it performs the shipped cession conversion:

    iOldOwnerCulture = 1800   // times100
    iNewOwnerCulture = 0
    iConvertedCulture = (1800 / 300) * 100 = 600

so the Creative new owner now has exactly 6 city culture. A city trade then unconditionally reaches
`keepCity(kNewCity)`. If the traded city is not under occupation, `keepCity()` immediately calls
`initTraitCulture()`; if an occupation timer exists, the already-verified occupation-end hook calls
the same helper later.

At 6 culture, current advc.908b arithmetic is:

    target = 10
    current = 6
    iFreeCityCulture = 10 - 6 = 4
    setCulture(owner, 4, true, false)

The acquired Creative city is therefore changed from 6 culture DOWN TO 4 rather than being raised
to the intended free-culture floor of 10. Because `bPlots=true`, the setter also propagates the
signed city-culture difference through plot-culture updating; the wrong absolute write is not
confined to a display value.

A second ordinary family exists for conquest/recapture: whenever the Creative new owner retains
or otherwise has 1..9 city culture when `keepCity()` or the occupation-end hook invokes
`initTraitCulture()`, the same wrong absolute assignment occurs. The city-trade fixture above is
sufficient as the deterministic current reproducer.

ANCESTRY / REGRESSION PROOF
---------------------------
AdvCiv practical 3263 (`dee7d269b1aad6598447a508ca9f5ed28408b6d7`, advc.908b), titled
"Creative trait free culture also for conquered cities", introduced this exact regression while
refactoring previously correct new-city code.

Before practical 3263, city initialization did:

    int iFreeCityCulture = GC.getGame().freeCityCultureFromTrait(eLoopTrait);
    if (iFreeCityCulture > 0)
        setCulture(getOwner(), iFreeCityCulture, true, false);

For a new city starting at zero, the full target was correctly passed to the absolute setter.
Practical 3263 extracted the code to `initTraitCulture()` so that acquired cities could receive the
same benefit, subtracted existing city culture to compute only the missing amount, but left the
callee as `setCulture` rather than using a delta mutator or passing the full target. The commit's
stated behavioral goal therefore directly exposes the setter-contract error.

DEDUP
-----
Whole-album search found advc.908b in two relevant prior places, neither owning this arithmetic:

* the earlier acquisition control proves that occupied conquered cities eventually call
  `initTraitCulture()` when occupation ends;
* a later Creative-city fixture relies on the new-city-at-zero case, where `target-current` equals
  the full target and the bug is naturally masked.

No prior finding or control owns the delta-vs-absolute setter mismatch for already-cultured
acquired cities. F571 is independent.

REPAIR BOUNDARY
---------------
Preserve the intended culture FLOOR, not the current delta-as-absolute behavior. The most direct
contract is:

    int iFreeCityCulture = GC.getGame().freeCityCultureFromTrait(eLoopTrait);
    if (getCulture(getOwner()) < iFreeCityCulture)
        setCulture(getOwner(), iFreeCityCulture, true, false);

Equivalently, a separately named missing amount could be passed to `changeCulture`, but writing the
full target through the existing absolute setter is clearer and remains correct if multiple traits
with different free-culture targets are ever introduced: each trait can only raise the city to its
own floor, never lower it.

CLASSIFICATION
--------------
AdvCiv-specific refactor regression / delta-vs-absolute-setter bug in live trait culture state.
Current shipped Creative data and ordinary city trading provide a deterministic witness; conquered
cities are breadth under the same root.

CURRENT STONE AFTER C031-WIP353
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F571 / KI#894 CONFIRMED.
F572 / provisional KI#895 UNUSED.
Latest physical checkpoint: C031-WIP353.
No repository source modified.

NEXT
----
Continue Queue 006 under F572 unused. The stateful SAS-vs-AdvC census has now re-found one already
repaired upstream lifetime hazard (KI#486.2) and one new live AdvC refactor regression (F571).
Continue through lower-coverage AdvC-added city arithmetic/state helpers, prioritizing functions
that translate fixed-point culture/religion values, reverse production modifiers, mutate
corporation/building state, or reconstruct state across acquisition. Require current shipped-data
reachability and whole-album dedup before promotion. Freeze another physical stone whether or not
F572 is consumed.

END C031-WIP353


================================================================================
C031-WIP354 - QUEUE 006 CVCITY.CPP / OLD BEHAVIOR-CHANGING PRACTICAL CENSUS
================================================================================

HANDOFF / SOURCE STONE
----------------------
Resumed directly from C031-WIP353 with F571 / KI#894 confirmed and
F572 / provisional KI#895 unused. Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Current authoritative source is still the user-supplied AdvCiv-SAS light-source snapshot;
Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS supplied trees were used locally for ancestry.
No repository source files modified.

POST-WIP353 F572 CENSUS / NEGATIVE CONTROLS
--------------------------------------------
1. `calculateTradeProfitTimes100()` precision change (practical 2187 / advc.004) closes as an
   intentional precision-preserving repair, not a new arithmetic loss.

   K-Mod's `calculateTradeProfit()` computes:

       baseProfit * totalTradeModifier / 10000

   and returns integer Commerce. AdvCiv renames the internal helper to
   `calculateTradeProfitTimes100()` and computes:

       baseProfit * totalTradeModifier / 100

   i.e. the same value retained in hundredths. The public integer helper then divides by 100.
   `updateTradeRoutes()` deliberately consumes the hundredths form so candidate routes have fewer
   arbitrary ties. Integer-yield consumers continue through `calculateTradeProfit()` and therefore
   preserve the old truncation boundary.

   The important live defects around this value are already-owned cache-lifetime roots:
   F505/KI#828 (partner-city population changes do not invalidate other players' cached route
   value/selection) and F506/KI#829 (peace-duration credit advances after the route cache was
   built). No independent precision defect was established here.

2. Practical 2187 ("Fix AdvCiv bug in city trade route assignment") closes as a genuine correction.
   It removes an earlier AdvC two-pass preference for open-borders/rival players that could make
   route assignment depend on pass order instead of value, then ranks all eligible partner cities
   in one pass using the higher-precision hundredths score. Current reservation checks and
   plot-group/reveal/disorder gates remain coherent with the final assignment contract. No F572.

3. Practical 1441 ("Changes to overflow and hurry AI", advc.064b) was re-opened at the
   operation-order layer. The new queued-production helper has real special semantics for `iNum`
   and overflow/feature-production prediction, but the album already owns the important mismatch:
   F443/KI#766 proves that existing queue-row hover help discards the real queue index and therefore
   asks this helper with the wrong `iNum`. The feature/resource immediate-validation descendants
   are likewise already F563/F564. No additional shipped arithmetic contradiction survived this
   pass.

4. Practical 2193 ("Upgrade -don't cancel- obsolete production orders") is a repair, not a new
   regression. It separates `upgradeProduction()` from `checkCanContinueProduction()` and lets the
   immediate-verification path request one upgrade-aware retry before removing an invalid order.
   Current gaps where the verifier sees the wrong resource state or is never called are already
   F563/KI#886 and F564/KI#887 respectively; the final helper contract itself did not yield a new
   F572 root.

5. Practical 1499's `CvCity.cpp` hunks are predominantly style/local-scope cleanup plus the known
   adjacent-city culture bump-order adjustment in `init`. This practical had already been broadly
   classified in the album's historical census as style/type/pathfinder-heavy; rechecking its
   `allUpgradesAvailable`, rank and production-legality hunks found no changed sentinel or mutation
   semantics that survive as a current independent defect.

6. Practical 1508 moves physical queue deletion into `doPopOrder()` so a completed order is gone
   before building processing can recursively call `verifyProduction()`. This ordering is required
   by the later `advc.064d` immediate-validation design and is consistent with the already-confirmed
   F563 fixture (National Park completes, then processing its NoBonus state verifies the remaining
   queue). The later fail-gold code correctly switched from dereferencing the deleted node to the
   saved `eOrderType`. No independent use-after-delete or queue-counter error was found here.

7. Practical 2339 ("Fix ship production in cities with limited sea access") removes a city-local
   blanket DOMAIN_SEA rejection from `CvCity::canTrain` and moves the limited-water/resource logic
   to the plot/trainability layer. The album already contains extensive primary/secondary
   `waterArea()` findings in the naval-AI files; this `CvCity` hunk itself does not create a distinct
   city-production legality contradiction under current source. Preserve those naval water-area
   findings under their existing KIs rather than splitting F572 here.

8. Fixed-point culture/religion transition recheck remains clean for F572: `setCultureTimes100`
   propagates the actual old->new delta to plot culture, and practical 3143's signed `floor()`
   behavior remains the WIP350 negative control rather than a new positive-culture regression.

CURRENT STONE AFTER C031-WIP354
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F571 / KI#894 CONFIRMED.
F572 / provisional KI#895 remains UNUSED.
Latest physical checkpoint: C031-WIP354.
No repository source modified.

NEXT
----
Continue Queue 006 under F572 unused from a fresh lens rather than re-probing the now-dense
production/trade history. Prioritize lower-coverage AdvC-added city helpers whose stored state is
keyed by player/team/building/religion/corporation identity, especially acquisition/reset paths and
runtime-to-cache transitions. Rank hard invariant risks first: wrong receiver/owner after transfer,
absolute-vs-delta writes, one global flag standing in for per-team state, enum sentinel reaching an
EnumMap, or post-mutation verification executed against pre-mutation state. Require a current
shipped-data witness and whole-album dedup before promotion. Freeze WIP355 regardless of whether
F572 is consumed.

END C031-WIP354


================================================================================
C031-WIP355 - QUEUE 006 CVCITY.CPP / CAPITAL-LOSS ANNOUNCEMENT COPY-CONTEXT UAF,
F572
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F572 / KI#895 are now CONFIRMED.
F573 / provisional KI#896 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot, with the
supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS material used locally for comparison.
No repository source files modified.

POST-WIP354 KEYED CITY-STATE / LIFETIME CONTROLS
-------------------------------------------------
1. The first keyed-state sweep rechecked religion/corporation/building/visibility and city-kill
   lifecycle state rather than reopening already-owned production/trade arithmetic. No independent
   root was manufactured from `setHasReligion`, `setHasCorporation`, `setRevealed`,
   `setEspionageVisibility`, `changeFreeBonus` or the already-owned `changeNoBonusCount` family in
   this tranche.

2. `CvCity::kill()` then exposed a harder lifetime boundary. This was challenged against the actual
   free-list implementation rather than assuming that `CvPlayer::deleteCity()` merely detaches an
   object. It does not: `CvPlayer::deleteCity(iID)` calls `m_cities.removeAt(iID)`, and
   `FFreeListTrashArray::removeAt()` executes `SAFE_DELETE(m_pArray[iIndex].pData)` immediately.
   Any later member access through the old `CvCity *this` is therefore a real use-after-free.

-------------------------------------------------------------------------------
F572 / KI#895 - CONFIRMED
ADVC PRACTICAL 4125 MOVED THE CAPITAL-CHANGE ANNOUNCEMENT FROM `CvPlayer` INTO
`CvCity::kill()` AFTER THE OLD CITY IS DELETED, BUT COPIED PLAYER-RELATIVE
`getID()` / `getTeam()` EXPRESSIONS UNCHANGED; THE BLOCK BOTH DEREFERENCES FREED
CITY STORAGE AND COMPARES OBSERVER PLAYER IDS AGAINST A CITY ID
-------------------------------------------------------------------------------

ROOT / CURRENT CODE
-------------------
Current `CvCity::kill()` correctly snapshots several old-capital values before deletion:

    PlayerTypes const eOwner = getOwner();
    CvPlayer& kOwner = GET_PLAYER(eOwner);
    ...
    bool const bCapital = isCapital();
    CvWString sNameKey;
    EagerEnumMap<PlayerTypes,bool> abRevealed;
    if (bCapital)
    {
        sNameKey = getNameKey();
        ... snapshot old reveal state ...
    }

It then destroys the city:

    kOwner.deleteCity(getID());

The deletion is immediate, not deferred:

    void CvPlayer::deleteCity(int iID)
    {
        m_cities.removeAt(iID);
    }

and `FFreeListTrashArray::removeAt` does:

    SAFE_DELETE(m_pArray[iIndex].pData);

Nevertheless, after `findNewCapital()` the moved AdvC announcement block still evaluates:

    if (kObs.getID() == getID())
        continue;
    if (GET_TEAM(getTeam()).isHasMet(kObs.getTeam()) || kObs.isSpectator())
        ...

Both `getID()` and `getTeam()` are member accesses on the already-deleted `CvCity` object.
`CvCity::getID()` is the inline `return m_iID`; `CvCity::getTeam()` derives from the city's owner.
Reading either through `this` after `SAFE_DELETE` is undefined behavior / use-after-free.

COPY-CONTEXT SEMANTIC ERROR EVEN WHEN FREED MEMORY HAPPENS TO LOOK INTACT
-------------------------------------------------------------------------
The `getID()` line is also wrong independently of whether the stale memory happens to retain its
old bytes.

The announcement block was copied from `CvPlayer::setCapital()`. In that original location:

    if (kObs.getID() == getID())
        continue;

compared the observer's PlayerTypes ID with the CAPITAL OWNER player's ID and therefore skipped the
owner/self observer.

After practical 4125 moved the same text into `CvCity::kill()`, `getID()` means the CITY's
per-player free-list ID instead. No receiver rebinding was made. Thus the new test is effectively:

    if (observerPlayerID == destroyedCapitalCityID)
        skip observer;

which has no valid player-identity meaning.

DETERMINISTIC ORDINARY CURRENT-SAS WITNESS
------------------------------------------
No unusual XML, multiplayer mode or API mutation is required.

Example:

1. Major Player 2's original/first capital is city ID 0, as is ordinary for the first city in that
   player's own city free list.
2. Player 2 later owns at least one other city, so losing the old capital leaves Player 2 alive and
   `findNewCapital()` returns a replacement capital.
3. Player 0 is a major observer that has met Player 2 and has the appropriate old/new capital
   visibility for a capital-move message.
4. The old capital is captured, razed, otherwise killed, or transferred through a path that invokes
   `CvCity::kill()` while it is still the capital.
5. After deleting that city, the announcement loop reaches Player 0. If the freed city's old ID is
   still read as 0 (the common benign-looking manifestation of the UAF), current code executes:

       kObs.getID() == getID()
       0            == 0

   and silently skips Player 0.
6. Player 2, the actual capital owner, is NOT skipped by that copied self-filter because
   `2 != cityID 0`.

Thus even in the non-crashing stale-memory case the moved block deterministically applies the wrong
observer identity whenever city ID differs from owner player ID. First capitals make this easy:
city ID 0 is common for every player, whereas only Player 0 has player ID 0.

The use-after-free broadens the consequence beyond wrong filtering: allocator reuse or other
post-delete activity before the loop can make the later `getID()` / `getTeam()` reads arbitrary and
can in principle misroute the met-team gate or crash. The deterministic player-vs-city-ID mismatch
is sufficient to prove current wrong behavior without relying on allocator reuse.

ANCESTRY / REGRESSION PROOF
---------------------------
AdvCiv practical 4125 (`174b7ed58a0d8a1734cc23192aefdbb2495d5102`), titled
`Fix timing of findNewCapital upon conquest of old capital`, introduced the current structure.

Before 4125, the `advc.106` capital-change announcement lived in `CvPlayer::setCapital()`. Its
receiver-relative expressions were correct there:

    kObs.getID() == getID()        // observer player vs this CvPlayer
    GET_TEAM(getTeam()).isHasMet   // this CvPlayer's team

Practical 4125 removed that block from `CvPlayer::setCapital()` and inserted it into
`CvCity::kill()` AFTER `kOwner.deleteCity(getID())`, copying those expressions unchanged.
The same practical explicitly says the old capital must be deleted before looking for the new
capital, so the post-delete placement is intentional; the missed part is preserving/rebinding all
old-owner identity needed by the moved code.

Practical 4135 (`99f31a43eff07936842b1eccb3237fa97d8a8161`), titled
`Amend findNewCapital bugfix; update DLL.`, is a revealing partial lifetime repair. It changes the
saved old-capital name from a raw `wchar const*` into an owning `CvWString` before deletion, avoiding
use of a dangling name pointer later. It does NOT replace the surviving post-delete `getID()` or
`getTeam()` member accesses. Base AdvCiv 1.14 and current SAS therefore retain the incomplete repair.

K-Mod 1.46 / stock BtS lineage does not contain this exact root. Its `CvCity::kill()` deletes the
city and later finds a new capital, but it has no copied AdvC `advc.106` announcement block that
accesses `this` after deletion. The defect belongs to AdvC's announcement/timing integration.

DUPLICATE CHECK
---------------
The reconciled album heavily audits capital-loss timing and practical 4125, including new-capital
selection, victory-progress reset, UWAI city-set invalidation and the earlier `liberate()`
post-acquisition lifetime repair (KI#486.2). It also contains several independent message/visibility
findings such as hidden-capital coordinate leaks.

Whole-album searches for practical 4125, `TXT_KEY_MISC_CAPITAL_MOVED`, capital announcement,
post-`deleteCity` city-member access, player-ID/city-ID mixing and the exact observer filter found no
numbered root owning this copied `CvPlayer`-receiver block. The prior WIP238 note that practical
4125's capital-loss lifecycle was "already heavily audited" did not inspect these two moved
receiver-relative expressions. F572 is therefore independent.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
Do not access `this` after `kOwner.deleteCity(...)`.

The conservative repair is to use the already-snapshotted owner identity throughout the
post-delete announcement:

    if (kObs.getID() == eOwner)
        continue;
    if (GET_TEAM(TEAMID(eOwner)).isHasMet(kObs.getTeam()) || kObs.isSpectator())
        ...

or equivalent `kOwner.getID()` / `kOwner.getTeam()` calls, since `kOwner` remains valid.

More generally, treat `kOwner.deleteCity(getID())` as the hard last-use boundary for every
`CvCity` member. Snapshot any city-owned value needed later before that call, just as practical
4135 already does for the old capital name and current code does for reveal state / capital status.

A regression test should use a surviving non-Player-0 civilization whose old capital is city ID 0,
plus a met/revealed Player 0 observer, then destroy the capital and verify:
* Player 0 is not spuriously skipped because its player ID matches the city ID;
* the actual old owner is treated according to the intended self-observer policy;
* the announcement path performs no `CvCity` member access after `deleteCity`.

CURRENT STONE AFTER C031-WIP355
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F572 / KI#895 CONFIRMED.
F573 / provisional KI#896 UNUSED.
Latest physical checkpoint: C031-WIP355.
No repository source modified.

NEXT
----
Continue Queue 006 under F573 unused. Stay on hard lifetime/keyed-state invariants around
`CvCity::kill`, acquisition, religion/corporation/building state and post-mutation callbacks, but
dedup aggressively against the already-dense acquisition/HQ/maintenance families. In particular,
scan for any other `this` / city-member access after the `deleteCity` boundary and for moved helpers
whose receiver changed from CvPlayer/CvTeam to CvCity without rebinding owner/team identity. Then
resume remaining low-coverage city-state helpers if that boundary saturates. Require current shipped
reachability, ancestry and whole-album dedup before promoting F573. Freeze WIP356 regardless.

END C031-WIP355


================================================================================
C031-WIP356 - QUEUE 006 CVCITY.CPP / POST-DELETE isActiveOwned UAF, F573
================================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F573 / KI#896 are now CONFIRMED.
F574 / provisional KI#897 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot, with the
supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS material used locally for comparison.
No repository source files modified.

POST-F572 HARD `deleteCity` LAST-USE INVENTORY
----------------------------------------------
F572 established `kOwner.deleteCity(getID())` as an immediate `CvCity` destruction boundary. A
line-by-line inventory of the remainder of `CvCity::kill()` found one additional city-member read
outside F572's practical-4125 capital-announcement block:

    if (isActiveOwned())
        gDLL->UI().setDirty(SelectionButtons_DIRTY_BIT, true);

This has separate ancestry from F572 and therefore was challenged as its own root rather than
folded into practical 4125.

-------------------------------------------------------------------------------
F573 / KI#896 - CONFIRMED
ADVC PRACTICAL 3218 REPLACED THE SAFE SAVED-OWNER TEST AT THE END OF
`CvCity::kill()` WITH `isActiveOwned()`, WHICH READS `getOwner()` FROM THE CITY
AFTER `deleteCity()` HAS ALREADY `SAFE_DELETE`D IT
-------------------------------------------------------------------------------

ROOT / CURRENT CODE
-------------------
Near the start of current `CvCity::kill()`, owner identity is safely snapshotted:

    PlayerTypes const eOwner = getOwner();
    CvPlayer& kOwner = GET_PLAYER(eOwner);

Later the city is immediately destroyed:

    kOwner.deleteCity(getID());

As proved for F572, `CvPlayer::deleteCity` calls `m_cities.removeAt(iID)`, whose
`FFreeListTrashArray::removeAt` executes:

    SAFE_DELETE(m_pArray[iIndex].pData);

`SAFE_DELETE` expands to `delete (p); (p)=NULL;`.

After substantial post-delete cleanup and any capital-replacement work, `CvCity::kill()` ends with:

    if (isActiveOwned())
        gDLL->UI().setDirty(SelectionButtons_DIRTY_BIT, true);

But `CvCity::isActiveOwned()` is inline:

    bool isActiveOwned() const
    {
        return (GC.getInitCore().getActivePlayer() == getOwner());
    }

and `getOwner()` returns `m_eOwner` from `this`. Thus the final UI gate dereferences the destroyed
CvCity object after the hard free-list deletion boundary.

CURRENT REACHABILITY / CONSEQUENCE
----------------------------------
Unlike F572, this line is not capital-specific. Every ordinary `CvCity::kill()` path reaches it after
city deletion unless execution exits abnormally first. That includes conquest/transfer/raze and
other normal city-removal paths.

If freed storage still contains the old `m_eOwner` bytes, the result merely happens to equal the
intended saved-owner condition and the bug can remain visually silent. But the program has already
executed `delete` on the object; C++ permits no subsequent member read. Reuse/corruption of that
storage can make the condition false/true arbitrarily or cause a crash. The direct user-facing side
effect controlled by the read is whether `SelectionButtons_DIRTY_BIT` is dirtied for the active
owner after its city disappears, so a benign misread can also leave stale selection-button UI.

The important audit classification is the unconditional lifetime violation: current compiled
source reads a member from an object that the same function has already explicitly deleted.

ANCESTRY / REGRESSION PROOF
---------------------------
Stock BtS/Civ4CE and K-Mod 1.46 are safe at this exact point. They save `eOwner` before deletion and
end `CvCity::kill()` with the saved-value comparison:

    if (eOwner == GC.getGameINLINE().getActivePlayer())
        ... dirty SelectionButtons ...

AdvCiv practical 3218 (`40060514c0e678c1d2bd252c961eef0e1333121b`), titled
`Simplify accesses to CvInitCore::m_eActivePlayer`, mechanically replaced many expressions such as
`getOwner() == activePlayer` with the new convenience helper `isActiveOwned()`.

In `CvCity::kill()` specifically, its diff changes:

    - if (eOwner == GC.getGame().getActivePlayer())
    + if (isActiveOwned())

without accounting for the fact that this location is AFTER `deleteCity(getID())`. The mechanical
simplification therefore changed a safe cached-owner read into a use-after-free.

The same change appears in later merge history (e.g. practical 3392), but practical 3218 is the
actual introducing semantic change. Base AdvCiv 1.14 and current SAS retain it.

DUPLICATE CHECK
---------------
F572 / KI#895 owns the separate practical-4125 capital-announcement block after deletion, including
its copied `getID()` / `getTeam()` receiver mistakes. F573 is not breadth of that commit: its unsafe
line predates 4125 and was introduced by practical 3218's active-player helper simplification.

The album contains extensive city-kill, capital-loss, acquisition and use-after-free work, but full
searches for `deleteCity` + `isActiveOwned`, the end-of-kill SelectionButtons gate, practical 3218
and this exact saved-`eOwner` replacement found no numbered root. Earlier `isActiveOwned` findings
concern live-city cache/UI targeting and not post-destruction lifetime.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
Restore the already-available saved-owner test and never touch `this` here:

    if (eOwner == GC.getInitCore().getActivePlayer())
        gDLL->UI().setDirty(SelectionButtons_DIRTY_BIT, true);

(or equivalent comparison against `kOwner.getID()`).

This is a one-line semantic repair and exactly restores the stock/K-Mod lifetime-safe shape while
retaining AdvCiv's active-player access style if desired.

More broadly, practical 3218-style helper substitutions should not replace cached identity values
across object-destruction boundaries. The helper is fine while a city is live; this one call site is
not.

CURRENT STONE AFTER C031-WIP356
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` - DEEP-METHOD RE-AUDIT OPEN.
32/169 unique outer-queue files remain complete; Queue 006 is the protected open file.
F559 / KI#882 through F573 / KI#896 CONFIRMED.
F574 / provisional KI#897 UNUSED.
Latest physical checkpoint: C031-WIP356.
No repository source modified.

NEXT
----
Continue Queue 006 under F574 unused. The direct post-`deleteCity` member-access inventory in
`CvCity::kill()` is now exhausted by F572/F573. Move one ring outward to other self-destructive city
transitions (`acquireCity(this)`, `setOwner`/culture flip, raze/disband/task dispatch) and verify that
all callers return or cache identity before the old city is destroyed. Dedup against KI#486.2 and
F460 first. Then resume keyed religion/corporation/building/reset transitions if lifecycle callers
close. Require current shipped reachability, ancestry and whole-album dedup before promotion.
Freeze WIP357 regardless.

END C031-WIP356

===============================================================================
C031-WIP357 — QUEUE 006 CONTINUATION FROM F574 UNUSED
===============================================================================

POST-WIP356 KEYED-STATE / ACQUISITION CONTROLS BEFORE F574
-----------------------------------------------------------
The immediate post-delete lifetime ring was completed before returning to city-state
transfer. No third use-after-free root was forced out of the already-dense destruction
paths:

* `CvCity::doRevolt()` returns immediately after `kill(true)` / culture-owner transfer.
* `CvCity::doTask()` performs no city-member work after raze, disband, gift/cede or
  liberation transfers.
* `CvCity::liberate()` is already repaired in current SAS under KI#486.2 by caching the
  old owner/team before `acquireCity()` invalidates the old city object.

The first acquisition-state candidates also closed under their real contracts:

* `m_aiNumRevolts` is not copied by ownership transfer, but that reset is inherited from
  BtS/K-Mod and matches the longstanding per-owner revolt-history model. It is not a new
  AdvC/SAS omission.
* `m_aiReligionInfluence` is not copied as a raw array, but the meaningful producers are
  rebuilt: surviving buildings are reprocessed in the replacement city and restoring a
  holy city re-applies `HOLY_CITY_INFLUENCE`. No independent lost-source state was proved.

F574 / KI#897 — CITY TRADE / LIBERATION CAN CLEANSE ACTIVE POISON-WATER OR FOMENT-UNHAPPINESS ESPIONAGE COUNTERS
----------------------------------------------------------------------------------------------------------------
STATUS: CONFIRMED — inherited BtS/K-Mod city-transfer state omission, live and
exploitable in current SAS; AdvC's own transfer hardening preserves analogous anger
penalties but still drops these two hostile city counters.

ROOT
----
`CvPlayer::acquireCity()` destroys the old `CvCity` and constructs a fresh replacement.
It snapshots and restores a substantial amount of physical/persistent city state,
including population, highest population, defense damage, bombarded state, ownership
history, per-player culture, religions, corporations/HQ identity, retained buildings,
free specialists, custom building yield/commerce/happy/health changes and reveal state.

For city trades it additionally snapshots and conditionally restores the three anger
penalties:

    int const iHurryAngerTimer = pOldCity->getHurryAngerTimer();
    int const iConscriptAngerTimer = pOldCity->getConscriptAngerTimer();
    int const iDefyResolutionAngerTimer = pOldCity->getDefyResolutionAngerTimer();
    ...
    // advc.ctr: Only copy the anger timers if a reset to 0 could be exploited
    if (isHuman() && (GET_TEAM(eOldOwner).isHuman() || TEAMID(eOldOwner) == getTeam()))
    {
        kNewCity.changeHurryAngerTimer(iHurryAngerTimer);
        kNewCity.changeConscriptAngerTimer(iConscriptAngerTimer);
        kNewCity.changeDefyResolutionAngerTimer(iDefyResolutionAngerTimer);
    }

However, the same transfer snapshots neither:

    m_iEspionageHealthCounter
    m_iEspionageHappinessCounter

Both fields initialize to 0 in a fresh `CvCity`. Consequently every `acquireCity(...,
bTrade=true,...)` transfer silently clears an active Poison Water or Foment Unhappiness
penalty even though the physical city is retained and immediately recreated on the same
plot.

CURRENT SAS DATA / LIVE EFFECT
------------------------------
Current SAS ships both active missions enabled:

    ESPIONAGEMISSION_CITY_POISON_WATER
        iCost = 120
        bTargetsCity = 1
        iCityPoisonWaterCounter = 8

    ESPIONAGEMISSION_CITY_UNHAPPINESS
        iCost = 120
        bTargetsCity = 1
        iCityUnhappinessCounter = 8

`CvPlayer::doEspionageMission()` applies those values directly:

    pCity->changeEspionageHealthCounter(8);
    pCity->changeEspionageHappinessCounter(8);

and `CvCity::doTurn()` normally decrements each positive counter only one point per city
turn. While active, the counters directly worsen city health / happiness. The XML/help
also describes the missions as temporarily increasing unhealthiness or unhappiness
*within an enemy city*; the runtime storage is city-local, not keyed to the victim owner.

Thus the ownership transfer changes an eight-turn city effect to zero immediately rather
than letting its normal countdown run.

WHY ADVC'S DIRECT HUMAN PING-PONG RULE DOES NOT CLOSE THE EXPLOIT
-----------------------------------------------------------------
AdvC intentionally added city-trade culture rules to prevent simple back-and-forth human
trades. After an ordinary city trade, tile culture is partly converted to the recipient,
so a direct immediate reverse trade is normally blocked by `canTradeCityTo()`.

But that function deliberately bypasses the ordinary culture restrictions when the
recipient is the city's liberation player:

    if (kCity.getLiberationPlayer(bConquest) == eRecipient)
        return true;

This provides an ordinary return path. Plot nationality (used to authorize the first
trade) and stored city culture (heavily used by `getLiberationPlayer`) are separate
quantities, and AdvC's trade conversion does not erase the former owner's city culture;
it only converts one third of the old owner's city-culture amount to the new owner.
Therefore a city can simultaneously:

* have enough recipient plot nationality for A -> B to be a legal initial trade, and
* retain enough A city culture / proximity that, after B receives it, A is B's best
  liberation player.

A cooperative human/team or vassal setup makes this especially easy because
`getLiberationPlayer()` gives non-rivals a culture-score bonus.

DETERMINISTIC EXPLOIT SHAPE
---------------------------
One ordinary current-SAS reproduction is:

1. Human A owns non-capital city C. Human B is a cooperative team/vassal partner (or
   simply another human satisfying the normal city-trade rules).
2. B has at least the shipped `CITY_TRADE_CULTURE_THRESH = 10%` nationality on C's tile
   and enough relative tile culture for A -> B to be legal. Separately, C retains much
   larger stored city culture for A and A has a favorable capital distance, so A will be
   C's best liberation player once B owns it.
3. An enemy spy successfully performs Poison Water or Foment Unhappiness in C. Assert
   the corresponding city counter is 8 (or any still-positive remainder).
4. A trades/gifts C to B. `CvPlayer::acquireCity()` kills C, creates the replacement and
   does not copy either espionage counter. The replacement counter is immediately 0.
5. Because A remains the replacement city's best liberation player, B can immediately
   liberate C back to A; `canTradeCityTo()` explicitly accepts the liberation path before
   the normal anti-ping-pong culture tests.
6. A again owns the same physical city/plot, but the hostile eight-turn city penalty has
   vanished without eight city turns passing and without any counterespionage or other
   cleansing mechanic.

The first transfer alone already demonstrates the state loss. The liberation return is
what turns it into a direct victim-side cleansing exploit rather than merely a question
of whether a new political owner should inherit a hostile effect.

ANCESTRY
--------
The omission is inherited from BtS and remains in K-Mod: their `acquireCity()` routines
also copy the three anger timers but do not preserve the two espionage city counters.
AdvC retains that base architecture while substantially rewriting city cession/trade
(`advc.ctr`) and explicitly documenting that anger timers are restored when resetting
them to zero could be exploited. Current SAS retains the same omission and ships both
8-turn active espionage missions.

So this is not a SAS-introduced regression. It is an inherited transfer-state hole that
remains live despite AdvC's surrounding anti-reset / anti-ping-pong hardening.

DUPLICATE / RECONCILIATION CHECK
--------------------------------
The reconciled album was searched for Poison Water, Foment Unhappiness, espionage health
and happiness counters, city-trade espionage state, `acquireCity` counter restoration and
counter resets.

No existing numbered root owns ownership transfer zeroing these two active hostile city
counters. Earlier espionage findings concern popup/target identity, visibility/caches or
other mission mechanics. F570/KI#893 concerns Investigate City's active-team permission;
it is a different state/keying root. The acquisition family around F460/F496/F566 and
HQ/corporation transfer likewise owns different state.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Treat the two active hostile city counters as part of the city state that must not be
silently reset by a voluntary ownership transfer. At minimum:

1. snapshot `getEspionageHealthCounter()` and `getEspionageHappinessCounter()` before
   `pOldCity->kill(...)`;
2. restore them into `kNewCity` for the `bTrade` paths where reset-to-zero can be
   exploited (analogous to the anger-timer policy).

A broader design can deliberately decide whether conquest should cleanse hostile city
conditions. That is separate from the confirmed voluntary-transfer exploit; the narrow
repair need not change conquest semantics.

REGRESSION TESTS
----------------
A. Poison Water trade/liberation
1. Create a legal A -> B city trade where A becomes the city's liberation player under B.
2. Set / create Poison Water counter 8 in A's city.
3. Trade city A -> B and assert the replacement city's counter is still 8 (or the exact
   pre-transfer value), not 0.
4. Liberate B -> A and assert the counter remains unchanged by transfer.
5. Advance normal city turns and assert it decays one point per turn.

B. Foment Unhappiness
Repeat A with `m_iEspionageHappinessCounter = 8`.

C. No-effect control
Transfer an otherwise identical city with both counters 0. The replacement should stay
at 0 and ordinary city-trade behavior should be unchanged.

D. Conquest policy control
If the chosen repair is intentionally limited to voluntary transfers, confirm conquest
retains the project's desired existing behavior and document that distinction explicitly.

NUMBERING
---------
F574 / KI#897 is CONFIRMED.
F575 / provisional KI#898 is the next UNUSED root.

C031-WIP357 DURABLE STATE
-------------------------
Queue 006: `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F574/KI#897 are CONFIRMED.
F575 / provisional KI#898 is UNUSED.
Actual AdvC-SAS source remains UNTOUCHED.

Exact continuation cursor:
WIP357 -> F575 unused -> continue keyed serialized/reconstructed city-state census across
reset / kill / acquireCity, then return to hard receiver/sentinel/arithmetic invariants
inside the remaining low-coverage CvCity methods -> current-data proof -> whole-album
dedup -> promote only an independent root -> freeze WIP358 regardless.

END C031-WIP357


================================================================================
C031-WIP358 - Queue 006 CvCity.cpp - F575 UNUSED - post-F574 low-coverage/runtime-invariant controls
================================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F574/KI#897 remain CONFIRMED.
F575 / provisional KI#898 remains UNUSED.
Actual AdvC-SAS source remains UNTOUCHED.

This checkpoint deliberately records a substantial clean tranche after F574 rather than
forcing a numbered root from generic robustness debt, disabled-option behavior or a
rediscovery already owned elsewhere in the album.

POST-F574 SERIALIZATION / TRANSFER-STATE CONTROLS
-------------------------------------------------
1. SAS BFC human-Artist provenance save field is not a cross-version defect under the
   project's explicit save policy.

   `m_iSASAutoBFCHumanArtistState` is serialized in current `CvCity::read/write` while
   `uiFlag` remains 17. In isolation that would make an older stream lacking the inserted
   integer incompatible with the current reader. However, the reconciled album already
   records the deliberate AdvC-SAS policy (including KI#46 and the KI#313 repair history):
   only current-version save compatibility is promised. The practical that added this
   state explicitly retained the current city stream marker; same-version read/write order
   is paired. Historical migration is therefore outside the supported contract and is not
   F575.

2. The same BFC Artist provenance state is coherent across ownership reconstruction.
   Forced-specialist assignments themselves are not transferred as persistent city state;
   the replacement city is reconstructed and begins AVAILABLE for the human helper. AI
   ownership also clears the helper state. No stale human provenance was established across
   `acquireCity`.

ZERO-COVERAGE FUNCTION CENSUS - CLOSED CONTROLS
------------------------------------------------
A fresh literal-name census was generated against the full WIP357 album so low-mention
helpers would not be mistaken for already-proven coverage. The first arithmetic/sentinel
bearing functions were taken end-to-end.

* `unmodifyOverflow` / `hurryOverflow`
  - The reversal helper consistently removes the build-specific modifier through
    `100 * raw / max(1, modifier + 100)` under the legal current production-modifier
    domain.
  - The surrounding overflow-capacity / retained-overflow contracts remain those already
    proved in WIP349/WIP351; no new current zero-capacity or lost-gold state emerged.
  - Historical practical 2180's MinCityYield hurry fixes remain present. No independent
    F575 was established from the hurry preview path.

* `getPopulationTradeModifier`
  - The implementation is inherited BtS/K-Mod arithmetic and current inherited defines
    preserve the expected `max(0, (population + offset) * modifier)` contract. No AdvC/SAS
    semantic drift was found.

* `isCorporationBonus`
  - Correctly gates on an active player corporation, the city's possession of that
    corporation, and matching prerequisite bonus. Its caller uses it only to decide
    whether bonus-count changes require corporation recomputation. No missing live key or
    sentinel path was found.

* Great-Person unit progress/rate setters
  - Progress is a direct nonnegative EnumMap setter. The No-Espionage guard on Spy-GPP
    rates is stable because game options do not toggle mid-game; no stale-rate transition
    was established under current supported play.

* `clearTradeRoutes`
  - Every checked live caller invokes it while the city's owner identity is still valid.
    For each claimed destination it clears the per-owner route reservation before resetting
    the IDInfo slot. The important `kill()` caller runs before owner deletion specifically
    to avoid dead-city claims. No wrong-owner acquisition boundary was found.

* `isMeltdownBuilding` / `isMeltdownBuildingSuperseded`
  - Comparator ordering is coherent: for otherwise comparable power sources, larger
    `iNukeExplosionRand` is safer (lower per-turn risk), and a clean source supersedes a
    dirty source. Current SAS has only one active nonzero-meltdown building producer, so
    the multi-danger ordering is additionally current-data trivial. No F575.

REDISCOVERY / DEDUP CONTROLS
----------------------------
The zero-name census deliberately re-hit one important existing root:

* `getAdditionalBaseYieldRateByBuilding()` performs

      ... * (iPlayerTradeYieldModifier / 100)

  and therefore prematurely truncates current Imperialist trade-yield modifiers below or
  between whole hundreds. This is exactly F561/KI#884, including its current SAS
  Imperialist + Lighthouse fixture. It is not a new F575.

The neighboring population-unhealth additional-building preview retains inherited K-Mod
arithmetic. Its generic rounding mismatch had already been proved mathematically but
current SAS data masks the demonstrated counterexamples (single -20 Public Transportation
source and saturating -100 National Park state). `changeDefenseModifier` belongs to the
already-dense advc.004c bombard family; no new contradiction emerged from its current
formula.

SAS-VS-BASE-ADVC STRUCTURAL DRIFT CONTROLS
------------------------------------------
Function-body comparison was normalized to avoid treating comments/signature layout as
semantics. The low-album-count SAS differences inspected here are semantic no-ops:

* `alterWorkingPlot()` only caches `GC.getDEFAULT_SPECIALIST()` in a static typed local;
  the same specialist is added under the same condition.
* `getVisibleBuildings()` only caches fixed city-graphic defines / precomputes the same
  linear coefficient.
* `getRevoltTestProbability()` and `addRevoltFreeUnits()` only cache fixed defines.
* `failGoldPercent()` only caches the three fixed MAXED_*_GOLD_PERCENT defines.

No owner/team/city/game-turn dependent value was accidentally hoisted into a process-wide
static in these functions.

HISTORICAL CORRECTIVE-PRACTICAL CENSUS
--------------------------------------
A second census ranked AdvC practicals touching `CvCity.cpp` whose titles explicitly say
fix/bug/correct/repair and which have little literal practical-number coverage in the
album. Small corrective hunks were checked for a later half-reversion.

The checked repairs remain present and coherent, including:

* practical 2125: food-production calculation consumes food with angry citizens handled
  through the corrected `foodConsumption(true)` contract;
* practical 2180: MinCityYield is included consistently in hurry legality/cost preview;
* practical 2292: zero-production competing wonder orders are representable by the
  corrected minimum-production initialization (the broader final fail-production loop was
  already audited separately);
* practical 2583 / later culture-range fixes: revolt/flip ownership uses plot cultural
  ownership rather than the range-ignoring city helper where appropriate;
* practical 2988: occupation decrement clamps/tests the computed decrement probability,
  not the unrelated revolt probability;
* practical 3419: civic `BuildingHealthChanges` are not double-counted through
  `getExtraBuildingBadHealth` in `badHealth`;
* practical 3666: a random-event state with food already beyond the growth threshold
  returns one turn instead of feeding a negative numerator into unsigned-ceiling division;
* practicals 4126 -> 4140 -> 4248: the city-kill civ-wide commerce cache cleanup retains
  the successive disorder and commerce-modifier corrections;
* practical 4220: WLTKD announcement returns on celebration end and only announces the
  transition into celebration;
* practical 4336: excess air-unit capacity handling uses the boolean result of
  `jumpToNearestValidPlot()` rather than dereferencing a potentially dead unit afterward.

No later regression of those repaired conditions was proved in current `CvCity.cpp`.

NO-ESPIONAGE DEBUG ASSERTION - REAL BUT NOT CURRENT-SUPPORTED F575
-----------------------------------------------------------------
While reconciling the commerce-cache fix chain, one debug-only mismatch was proved:
`CvPlayer::updateCommerceRates()` starts each commerce check from the raw player cache
`m_aiCommerceRateTimes100[eCommerce]`, but its FASSERT recomputes the sum by calling each
city's public `getCommerceRateTimes100(eCommerce)` accessor. Under GAMEOPTION_NO_ESPIONAGE,
that accessor redirects raw Espionage into Culture and zeroes Espionage.

Therefore, with any raw Espionage source (current Palace supplies +4), the debug assertion
for Culture compares raw player Culture against city Culture+Espionage and can fail even
when the caches are internally correct.

This is a genuine debug-contract incompatibility, but it is not promoted here: the album
already established that current SAS hides No Espionage, defaults it off and ships no
scenario/map enabling it under the ordinary-current-game threshold. Preserve as optional /
future-proofing debt unless that support policy changes.

C031-WIP358 DURABLE STATE
-------------------------
Queue 006: `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F574/KI#897 are CONFIRMED.
F575 / provisional KI#898 remains UNUSED.
Actual AdvC-SAS source remains UNTOUCHED.

Exact continuation cursor:
WIP358 -> F575 unused -> leave the saturated helper/overflow/transfer lenses -> inspect
low-coverage behavior-changing AdvC city subsystems and current SAS data interactions,
prioritizing state that is reconstructed by a different formula than runtime, observer /
active-team visibility permissions, and corrective practicals whose assumptions later
changed -> current-data proof -> whole-album dedup -> promote only an independent root ->
freeze WIP359 regardless.

END C031-WIP358


================================================================================
C031-WIP359 - QUEUE 006 CVCITY.CPP / EVENT EXTRA-HAPPINESS/HEALTH TRANSFER LOSS, F575
================================================================================

HANDOFF / SOURCE STONE
----------------------
Resumed directly from C031-WIP358 with F574 / KI#897 confirmed and
F575 / provisional KI#898 unused. Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Current authoritative source remains the user-supplied AdvCiv-SAS light-source snapshot;
Base AdvCiv 1.14, K-Mod 1.46 and supplied Civ4CE/BtS were used locally for ancestry and
contract checks. No repository source files modified.

POST-WIP358 CONTROL CENSUS BEFORE PROMOTION
-------------------------------------------
1. `CvCity::processVoteSource` globally iterates buildings and writes religious vote-source
   bonuses by BuildingClass. This looks capable of double-applying a class if multiple global
   BuildingTypes with the same `(ReligionType, BuildingClass)` exist. Current SAS XML masks that
   generic risk completely: all 28 buildings with non-NONE `ReligionType` have unique
   `(religion, class)` pairs. Preserve as mod-mod robustness only; no F575.

2. `CvCity::updateCorporationBonus` seeds its first propagation layer from current total bonuses,
   including the previous corporation-produced closure, then propagates later layers from newly
   produced bonuses only. A producer->producer dependency could therefore deserve special
   scrutiny. Current SAS does have produced Aluminum consumed by Creative Constructions / Mining
   Inc., but neither consuming corporation itself produces a bonus. The only bonus-producing
   corporations (Standard Ethanol -> Oil; Aluminum Co. -> Aluminum) consume ordinary map/trade
   resources, not another corporation-produced resource. No producer->producer chain exists in
   shipped XML, so the suspected recursive double-count does not become current F575.

3. `CvCity::m_aEventsOccured` is serialized but not copied by `CvPlayer::acquireCity`. The nearby
   `m_bPrereqEventCity` naming is misleading: stock BtS, K-Mod, AdvC and SAS all deliberately load
   that member from XML `bPrereqEventPlot`, and both the city and plot trigger checks use the same
   location-sensitive prerequisite flag. Current location-sensitive continuation triggers also
   require the new owner's player-level prerequisite event record; that record naturally does not
   transfer with the city. Thus losing only the city-local event-history vector on ownership
   change was not shown to alter current trigger eligibility independently. No F575 on that basis.

F575 / KI#898 - CONFIRMED
-------------------------
CITY OWNERSHIP TRANSFER RESETS PERMANENT RANDOM-EVENT EXTRA HAPPINESS/HEALTH WHILE PRESERVING
OTHER STATE FROM THE SAME EVENT; A LEGAL TRADE -> LIBERATION ROUND TRIP CAN CLEANSE A NEGATIVE
CITY EVENT PENALTY WITHOUT LOSING ITS BENEFIT.

ROOT
----
`CvCity::applyEvent` applies permanent city-local event happiness/health directly to serialized
city members:

    if (kEvent.getHappy() != 0)
        changeExtraHappiness(kEvent.getHappy());
    if (kEvent.getHealth() != 0)
        changeExtraHealth(kEvent.getHealth());

These mutate `m_iExtraHappiness` / `m_iExtraHealth`. Both members are written/read in the city
save stream and directly feed live `happyLevel` / `goodHealth` / `badHealth`; they are persistent
city state, not a one-turn UI value.

`CvPlayer::acquireCity`, however, destroys the old `CvCity` and constructs a replacement. Its
explicit transfer snapshot restores many persistent city-local states (culture, ever-owned flags,
real buildings and provenance, per-building yield/commerce/happy/health changes, religions,
corporations, free specialists, reveal state, etc.), but it never snapshots or restores either:

    pOldCity->getExtraHappiness()
    pOldCity->getExtraHealth()

The replacement city therefore starts both members from reset value 0.

CURRENT SHIPPED WITNESS - CONTROVERSIAL PHILOSOPHER
----------------------------------------------------
Current SAS ships ordinary random event outcome `EVENT_CONTROVERSIAL_PHILOSOPHER_2` with:

    <bPickCity>1</bPickCity>
    <iHappy>-1</iHappy>
    <FreeSpecialistCounts>
        ... SPECIALIST_SCIENTIST ... 1 ...
    </FreeSpecialistCounts>

and no EventTimes/ClearEvents that later reverses the city happiness change.

The corresponding trigger is normal live content:

    EVENTTRIGGER_CONTROVERSIAL_PHILOSOPHER
    iPercentGamesActive = 75
    iWeight = 1000
    Civic = CIVIC_THEOCRACY
    AND prerequisite TECH_PHILOSOPHY
    Obsolete at TECH_THEORY_OF_EVOLUTION
    bPickCity = 1

So an ordinary Theocracy/Philosophy-era city can choose outcome 2 and receive both:

    permanent city ExtraHappiness delta = -1
    added free Scientist count          = +1

The acquisition code treats the two halves asymmetrically. Before deleting the old city it
explicitly snapshots every added free specialist:

    aeFreeSpecialists.push_back(
        pOldCity->getAddedFreeSpecialistCount(eLoopSpecialist));

and after `initCity` restores them through:

    kNewCity.changeFreeSpecialistCount(eSpecialist, aeFreeSpecialists[eSpecialist]);

But there is no equivalent snapshot/restore for city ExtraHappiness. Therefore a transfer preserves
the free Scientist while silently erasing the event's -1 happiness cost.

DETERMINISTIC CURRENT-GAME EXPLOIT
----------------------------------
Reuse the ordinary voluntary-transfer / liberation route already proved for F574/KI#897:

1. Player A owns city C and receives `EVENT_CONTROVERSIAL_PHILOSOPHER_2` in C.
2. Confirm C has city-local ExtraHappiness = -1 and one added free Scientist from the same event.
3. A legally trades C to B in a configuration where A remains C's best liberation player under B.
4. `CvPlayer::acquireCity` kills A's old city object and creates B's replacement city.
5. The replacement restores the added free Scientist but initializes ExtraHappiness to 0.
6. Because A remains the liberation player, B can use the liberation path to return C to A; as
   proved in F574, that path is accepted before the normal anti-ping-pong culture tests.
7. A again owns the same physical city/plot with the free Scientist intact, but the permanent
   -1 happiness penalty has vanished without any event-clearing mechanic.

The defect is observable after the FIRST transfer already; the liberation return merely turns it
into a direct original-owner cleansing exploit.

The same root also erases positive permanent city event state. Current
`EVENT_HORTICULTURE_3` supplies +3 city-local health through `m_iExtraHealth`, and
`EVENT_FREEDOMCONCERT_1` supplies +1 city-local happiness. Thus ownership change can also destroy
legitimate persistent benefits. Negative current outcomes such as `EVENT_NUCLEAR_PROTEST_2`
(-2 happiness) broaden the cleansing side.

WHY THIS IS INDEPENDENT OF F574
-------------------------------
F574/KI#897 owns two active espionage-duration counters
`m_iEspionageHealthCounter` / `m_iEspionageHappinessCounter` being reset by voluntary transfer.
Those are temporary hostile mission timers with per-turn decay.

F575 owns different serialized members, `m_iExtraHappiness` / `m_iExtraHealth`, whose random-event
changes are persistent and can represent either benefits or penalties. The Controversial
Philosopher fixture is especially objective because the SAME event's free-Specialist benefit is
explicitly preserved by `acquireCity` while its happiness cost is silently discarded. This is not
merely broader wording for the espionage-counter root.

ANCESTRY
--------
The omission is inherited from the BtS/K-Mod city-replacement acquisition architecture: those
lineages likewise reconstruct a new `CvCity` and do not transfer city ExtraHappiness/ExtraHealth.
AdvC retains that architecture while adding substantial explicit city-trade anti-reset policy
(`advc.ctr`) and preserving other event-created city state such as added free specialists and
per-building class changes. Current SAS retains the omission and ships the live event fixtures
above.

DUPLICATE CHECK
---------------
The reconciled album already contains city-local ExtraHealth work, notably the health-hover mismatch
using persistent `EVENT_HORTICULTURE_3`, and F574's acquisition loss of espionage counters. Whole-
album searches for `m_iExtraHappiness`, `m_iExtraHealth`, Controversial Philosopher, Horticulture,
Freedom Concert, event happiness/health plus `acquireCity`, city trade and liberation found no
numbered root owning loss of these permanent event-created city members on ownership transfer.
F575 is independent.

CONSERVATIVE REPAIR DIRECTION - NOT IMPLEMENTED
------------------------------------------------
Treat city-local ExtraHappiness / ExtraHealth as persistent city state across ownership replacement,
just like added free specialists and per-building event changes:

1. snapshot `pOldCity->getExtraHappiness()` and `pOldCity->getExtraHealth()` before `kill`;
2. restore the exact values into `kNewCity` after initialization (via the existing change setters or
   an equivalent transfer-safe setter).

Because these members can contain Python/mod-event contributions in addition to XML random events,
copy the aggregate city-local state rather than trying to reconstruct only known event sources.

If a design intentionally wants conquest to cleanse some event effects, that policy should be
explicit and can be limited by `bTrade`/`bConquest`. The confirmed exploit requires only voluntary
transfer, where AdvC already preserves other anti-reset state. A narrow repair may therefore preserve
these values at least for `bTrade` first, with conquest policy documented separately.

REGRESSION TESTS
----------------
A. Controversial Philosopher atomic-state test
1. Set/trigger city ExtraHappiness=-1 and added free Scientist=1 as outcome 2 does.
2. Trade city A->B.
3. Assert replacement ExtraHappiness remains -1 and added free Scientist remains 1.
4. Liberate B->A and assert both remain unchanged.

B. Positive city-event state
Repeat with `EVENT_HORTICULTURE_3` (+3 ExtraHealth) and/or `EVENT_FREEDOMCONCERT_1`
(+1 ExtraHappiness); neither benefit should vanish merely because ownership is reconstructed.

C. Zero-state control
Transfer a city with both city-local extras at 0 and verify ordinary transfer behavior is unchanged.

D. Conquest policy control
If implementation deliberately distinguishes voluntary transfer from conquest, lock that distinction
with an explicit conquest regression test rather than allowing reset semantics to be accidental.

NUMBERING
---------
F575 / KI#898 is CONFIRMED.
F576 / provisional KI#899 is the next UNUSED root.

C031-WIP359 DURABLE STATE
-------------------------
Queue 006: `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F575/KI#898 are CONFIRMED.
F576 / provisional KI#899 is UNUSED.
Actual AdvC-SAS source remains UNTOUCHED.

Exact continuation cursor:
WIP359 -> F576 unused -> continue the serialized/reconstructed city-state inventory around
`acquireCity` and city-local random-event fields, but dedup against F574/F575 before splitting any
nearby omission; then return to remaining low-coverage behavior-changing CvCity helpers and
observer/active-team permissions -> current-data proof -> whole-album dedup -> promote only an
independent root -> freeze WIP360 regardless.

END C031-WIP359


===============================================================================
C031-WIP360 CONTINUATION - POST-F575 TRANSFER / OBSERVER NEGATIVE CONTROLS
===============================================================================

STATUS
------
F576 / provisional KI#899 remains UNUSED in this tranche.
Queue 006 (`CvGameCoreDLL/CvCity.cpp`) remains OPEN.
Actual AdvC-SAS source remains UNTOUCHED.

1. `acquireCity` omissions adjacent to F575 are breadth, not new roots
--------------------------------------------------------------------
After confirming F575, the serialized/reconstructed city-state inventory was continued instead of
immediately splitting every omitted member into a new finding. Several additional city-local random-
event effects are indeed stored in `CvCity` members that `CvPlayer::acquireCity` does not snapshot
and restore:

* `m_iHappinessTimer` can be changed by city events through `iHappyTurns`;
* `m_iCultureUpdateTimer` can be changed together with occupation by event `iRevoltTurns`;
* `m_iSpaceProductionModifier` can be permanently changed by event `iSpaceProductionMod`.

Current SAS has live XML witnesses. In particular `EVENT_COMET_FRAGMENT_1` has
`iSpaceProductionMod=5`, while Earth Day, Healing Plant, Controversial Philosopher option 1 and Spy
Discovered option 3 include nonzero city `iHappyTurns` outcomes.

These do not justify F576: they arise from the same acquisition-reconstruction omission already
owned by F575 -- persistent / event-created city-local scalar state is not copied while selected
other event-created state is. They should be treated as F575 breadth when repairing and testing the
transfer-state policy. The especially strong F575 witness remains Controversial Philosopher option 2,
because one atomic event's added free Scientist survives while its permanent ExtraHappiness penalty
is lost.

2. `m_aEventsOccured` transfer loss remains a negative control
-------------------------------------------------------------
The city-local event-history vector is serialized but not reconstructed by `acquireCity`. Current
continuation/prerequisite triggers that consult city/plot event history also require the receiving
player's player-level prerequisite event record. That player-level record naturally does not transfer
with the city. Thus the city-vector omission alone did not produce an independent current eligibility
change and remains below finding threshold.

3. `processVoteSource` duplicate class concern is current-data masked
--------------------------------------------------------------------
`CvCity::processVoteSource` iterates global buildings and writes religious vote-source effects by
building class. A mod could make duplicate `(ReligionType, BuildingClass)` pairs and process the same
class twice. Current SAS religious-building XML has unique religion/class pairs, so the suspected
double application is not reachable in shipped data.

4. `updateCorporationBonus` closure concern is current-data masked
-----------------------------------------------------------------
The corporation-bonus propagation algorithm superficially permits stale closure state to matter when
one bonus-producing corporation feeds another bonus-producing corporation. Current SAS has no such
producer->producer chain: Standard Ethanol produces Oil from food resources and Aluminum Co. produces
Aluminum from Coal, while the corporations that consume Aluminum do not themselves produce another
bonus. No current F576.

5. Observer/active-team probes - no promoted root
-------------------------------------------------
A fresh observer-permission pass inspected `isAllBuildingsVisible`, `getVisibleEffects`,
`getMusicScriptId`, reveal/espionage visibility mutation and related active-team branches.

* `getVisibleEffects` tests occupation visibility through `isVisible(getTeam())`, which is
  observer-agnostic and looks suspicious in isolation; however the DLL does not expose enough of the
  EXE's effect-callback visibility contract to prove that a hidden observer receives the effect. Do
  not promote from source shape alone.
* `isAllBuildingsVisible` retains the inherited expression
  `plotCheck(...) > 0 != NULL`. It is stylistically/portability-fragile compared with an explicit
  `plotCheck(...) != NULL`, but current Windows/VC behavior and lack of a demonstrated wrong result
  make this a robustness note rather than KI#899.
* `getMusicScriptId` assumes a valid active team in the foreign-city branch. KI#162 already proves
  that some EXE UI callbacks can occur with `NO_PLAYER` during transitions, but no equivalent caller
  proof was established for this music callback. Therefore no crash is inferred merely from the
  missing guard.

6. Nearby runtime arithmetic re-hits existing controls
------------------------------------------------------
`doGreatPeople` overflow compression and `doMeltdown` speed-scaled explosion odds were rechecked while
sieving low-coverage arithmetic. They are already represented by earlier Queue-006 controls/F562
analysis; current `NukeExplosionRand` magnitude prevents the obvious zero-divisor concern. No new
root was manufactured.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F575/KI#898 are CONFIRMED.
F576 / provisional KI#899 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS source remains UNTOUCHED.

Exact continuation cursor:
WIP360 -> F576 unused -> leave the now-dense acquireCity/random-event family unless a genuinely
independent state contract appears; continue low-coverage behavior-changing CvCity helpers with
priority on owner/team receiver identity, enum/sentinel use, mutation-before-validation and
multiply/divide/order changes -> prove against current SAS callers/XML -> whole-album dedup ->
promote only an independent live root -> freeze WIP361 regardless.

END C031-WIP360


===============================================================================
C031-WIP361 CONTINUATION - LOW-COVERAGE MUTATOR / SENTINEL SIEVE
===============================================================================

STATUS
------
F576 / provisional KI#899 remains UNUSED.
Queue 006 (`CvGameCoreDLL/CvCity.cpp`) remains OPEN.
Actual AdvC-SAS source remains UNTOUCHED.

1. Improvement health/happiness hypothetical arithmetic closes
-------------------------------------------------------------
`calculateHealthHappyChange` / `goodBadHealthHappyChange` initially look unusual because the
hypothetical new health is obtained by calling `calculateSurroundingHealth` with a percentage delta
while the plot still physically contains its old feature/improvement. This is intentional and
coherent: the helper adds the proposed good/bad percentage delta to the real current surrounding
state, then compares separately-rounded good and bad health against the old state. The separation
matches the documented BtS positive/negative rounding contract. No independent arithmetic loss was
found.

2. Conscription selection / Python override control
--------------------------------------------------
The ordinary `getConscriptUnit` path only considers civilization units for which `canTrain` succeeds,
then `canConscript` and `initConscriptedUnit` use that selected unit coherently. The inherited Python
`conscriptUnitOverride` can replace the result without repeating `canTrain`; that is a generic script
contract weakness, but current SAS supplies no live non-`NO_UNIT` override demonstrating an invalid
unit. Therefore it is not F576.

3. Hurry preview helpers remain caller-bounded
---------------------------------------------
`canHurryUnit` / `canHurryBuilding` do not independently call full `canTrain` / `canConstruct`.
`canHurryUnit` is effectively unused by current AI after the documented K-Mod rewrite. Live
`canHurryBuilding` callers invoke it while evaluating building candidates already generated through
their normal validity/can-construct pipeline. No current route was found where the helper alone makes
an illegal building eligible; do not infer a bug solely from the helper's narrower contract.

4. Culture-level / partisan conversion is coherent
--------------------------------------------------
`calculateCultureLevel` recomputes the highest current culture level directly from the game's scaled
thresholds. `getNumPartisanUnits` deliberately converts that enum to a count with `max(0, level-1)`,
and its source comment explicitly documents the subtraction as the intended design (e.g. zero
partisans at Poor culture). Current culture-level ordering preserves the assumption; no sentinel or
off-by-one defect was found.

5. Observer/active-team probes remain below proof threshold
----------------------------------------------------------
The WIP360 observer probes (`getVisibleEffects`, `isAllBuildingsVisible`, `getMusicScriptId`) were
rechecked while sieving active-team references. No DLL-side evidence established the necessary EXE
callback/visibility path for an independent current gameplay failure. They remain robustness / caller-
contract notes only, not KI#899.

6. Sentinel scan re-hits an already frozen latent defect
--------------------------------------------------------
A broad `NO_*` sentinel-expression scan re-hit the `defensiveGarrison` capital fallback:
`pCapital->allUpgradesAvailable(...)` is used directly as a Boolean even though the function returns
a `UnitTypes` sentinel and `NO_UNIT == -1` is truthy. This exact defect was already frozen in WIP347
as latent/current-data masked because Demand Better Protection is disabled and its obsolete multiplier
is neutral in shipped SAS. No renumbering.

7. Additional low-coverage controls
-----------------------------------
* `getNumActiveWorldWonders` uses a sensible MAX_INT default stop count; the early-stop comparison is
  correct for `hasActiveWorldWonder(1)` and full-count callers.
* `getAdditionalStarvation` correctly clamps both newly added spoiled food and newly removed spoiled
  food against the city's existing food surplus/deficit.
* `calculateCultureLevel` and surrounding simple change/set wrappers did not expose mutation-before-
  validation or enum/receiver mistakes beyond already-owned findings.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F575/KI#898 are CONFIRMED.
F576 / provisional KI#899 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS source remains UNTOUCHED.

Exact continuation cursor:
WIP361 -> F576 unused -> continue Queue 006 from less-covered behavior-changing AdvC helpers rather
than simple change/set wrappers; prioritize ownership/team assumptions, lifecycle callbacks, signed
fixed-point conversions and helper rewrites whose comments claim equivalence -> current SAS caller/XML
proof -> whole-album dedup -> promote only an independent root -> freeze WIP362 regardless.

END C031-WIP361


================================================================================
C031-WIP362 CONTINUATION - POST-WIP361 LOW-COVERAGE BEHAVIOR / CORRECTIVE-PRACTICAL CENSUS
================================================================================

Scope / provenance
------------------
Continued Queue 006 (`CvGameCoreDLL/CvCity.cpp`) from C031-WIP361 with F576 /
provisional KI#899 deliberately UNUSED. Current AdvCiv-SAS source, supplied base
AdvCiv 1.14, K-Mod 1.46, Civ4CE and locally cached reachable-history diffs were used.
No repository source file was modified.

The continuation deliberately prioritized behavior-changing AdvC helpers and late
corrective practicals rather than thin setters/accessors. Candidates were required to
survive current-SAS data/caller proof and whole-album dedup before numbering.

ADVANCED-START CULTURE PRICE - ODD BUT NOT PROMOTED
----------------------------------------------------
`CvCity::getCultureThreshold(eLevel)` deliberately returns the threshold for
`eLevel + 1`; the no-argument overload therefore means the next expansion threshold.
`CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_CULTURE)` raises city culture to
that next threshold, while `getAdvancedStartCultureCost(true, pCity)` prices
`CvCity::getCultureThreshold(currentLevel + 1)`, which superficially looks one level
farther ahead than the action.

This was not promoted. The same pricing shape is inherited through Firaxis/BtS,
K-Mod and supplied Civ4CE, and AdvC practical advc.250c explicitly tuned
`ADVANCED_START_CULTURE_COST` while retaining the contract. Current SAS ships
`ADVANCED_START_CULTURE_COST = 15` with documentation calibrating the cost per 10
culture, and the first-expansion price matches that calibration. The result may be
nonlinear/odd, but no objective regression or contradictory authored contract was
proved.

LATE CIV-WIDE COMMERCE-CACHE REPAIR CHAIN - CURRENT REPAIR RETAINED
-------------------------------------------------------------------
The late AdvC corrective chain was reconstructed directly:

* practical 4126 added explicit subtraction of player free-city commerce when a city
  is killed so the player's cached civ-wide commerce does not retain the dead city;
* practical 4140 amended that subtraction to skip disorder because disorder already
  suppresses the city's commerce (and a city razed before ever leaving disorder may
  never have added it);
* practical 4248 further amended the subtraction to include the owner's ordinary
  commerce-rate modifier and strengthened the final zero-rate assertions.

Current SAS retains all three corrections. `setOccupationTimer` calls
`updateCommerce()` when disorder changes, `CvPlayer::changeFreeCityCommerce` refreshes
city commerce through the player update path, and no later half-reversion of those
corrections was established.

A generic residual robustness seam exists: the kill-time explicit subtraction does
not include `getCapitalCommerceRateModifier`, although that modifier is part of
`CvCity::getTotalCommerceRateModifier`. Current SAS XML contains no nonempty
`CapitalCommerceModifiers`, so this cannot produce a shipped-game discrepancy and is
not F576. If such a civic/source is reintroduced, the cleanup formula should be
revisited rather than assumed fully generic.

MILITARY-HAPPINESS CACHE VS INVISIBILITY - GENERIC HAZARD, CURRENT XML MASKED
-----------------------------------------------------------------------------
Practical 4205 made military-happiness eligibility depend on
`CvUnit::isGarrisonInTeamCity()`, including visibility to the city team, and introduced
`CvCity::updateMilitaryHappinessUnits()` for initialization/vassal-state repairs.
Movement updates adjust the cached count for the moving unit, but a stationary
master/vassal garrison whose invisibility becomes detected/undetected because a
see-invisible unit moves could theoretically change eligibility without itself moving.

That generic stale-cache route is not current F576. Current SAS XML has exactly one
unit with a non-NONE always-invisible type (`UNIT_ATTACK_SUBMARINE`,
`INVISIBLE_SUBMARINE`), and it explicitly has `bMilitaryHappiness = 0`. No shipped
invisible military-happiness unit exists, so the required fixture is absent.

PRACTICAL 3970 PLAYER/TEAM SPELLING - IMPORTANT FALSE POSITIVE
---------------------------------------------------------------
The wartime culture-flip anti-war-success block contains:

    GET_TEAM(eCulturalOwner).isAtWar(getTeam())

while later expressions use `TEAMID(eCulturalOwner)`. Because `eCulturalOwner` is
`PlayerTypes`, the first spelling initially looked like a compile-valid raw
PlayerTypes -> TeamTypes index bug in team games.

It is NOT one in current AdvC/SAS. `GET_TEAM(x)` expands through
`CvGamePlay::getTeam(x)`, and the gameplay helper provides a `PlayerTypes` overload that
resolves the player's current TeamTypes (including team games / reassignment). This is
the same overload trap already documented elsewhere in the album. Do not reflag
`GET_TEAM(PlayerTypes)` merely from syntax; inspect the active helper overload first.

OTHER LOW-COVERAGE CONTROLS IN THIS SLICE
-----------------------------------------
* Optimized civilization-building loops such as `getBonusYieldRateModifier` and
  `updateExtraBuildingHealth` were checked against the acquisition/event building
  representation. Ordinary city building state is keyed/remapped through building
  class to the current civilization; no shipped path to a foreign/unmapped live
  BuildingTypes instance was proved.
* Large-map distance-maintenance arithmetic was checked against SAS's expanded map
  sizes. `maxMaintenanceDistance()` caps its effective distance to a fraction of
  `maxTypicalDistance`, preventing the initially suspected denominator/range escape.
* The 4124 No-Espionage debug assertion mismatch is already recorded in WIP358 and
  remains optional/current-hidden support debt, not a new finding.
* Practical 4105's founding-city exception in the culture-spread limiter remains
  present; no later range regression was established in this pass.
* The inherited `isAllBuildingsVisible` pointer-relational spelling remains odd, but
  no current observer/caller contradiction was established here; preserve as a probe,
  not a numbered root.

C031-WIP362 DURABLE STATE
-------------------------
Queue 006: `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F575/KI#898 are CONFIRMED.
F576 / provisional KI#899 remains UNUSED.
Actual AdvC-SAS repository source remains UNTOUCHED.

Exact continuation cursor:
WIP362 -> F576 unused -> continue zero/low-coverage behavior-changing AdvC city
practicals and hard invariants; prioritize lifecycle/ownership receivers, signed and
fixed-point arithmetic, culture/revolt state, production interruption, visibility and
city-trade transitions -> current SAS data/caller proof -> whole-album dedup -> promote
only an independent live root -> freeze WIP363 regardless.

END C031-WIP362


================================================================================
C031-WIP363 CONTINUATION - RANDOM-EVENT CITY-HISTORY / CLEAR-SCOPE CENSUS
================================================================================

Scope / provenance
------------------
Continued Queue 006 (`CvGameCoreDLL/CvCity.cpp`) from C031-WIP362 with F576 /
provisional KI#899 UNUSED. The current AdvCiv-SAS source, supplied Base AdvCiv 1.14,
K-Mod 1.46 and Civ4CE/BtS source plus current shipped EventInfo/EventTriggerInfo XML
were used locally. No repository source file was modified.

The first obligation was the unfinished `canApplyEvent` food-rounding candidate from
the WIP362 handoff; after closing that against shipped XML, the pass continued into
`CvCity::getTriggerValue`, location-sensitive prerequisite history and the player/city
ClearEvents handoff.

POST-WIP362 CONTROL - EVENT FOOD VALIDATOR/RUNTIME INTEGER ORDER IS GENERICALLY
FRAGILE BUT CURRENT SAS XML DOES NOT REACH AN ELIGIBILITY DISAGREEMENT
----------------------------------------------------------------------------
`CvCity::canApplyEvent` checks city food with:

    kEvent.getFood() +
        ((100 + kEvent.getFoodPercent()) * getFood()) / 100 >= 0

whereas `CvCity::applyEvent` mutates it as:

    changeFood(kEvent.getFood() +
        (kEvent.getFoodPercent() * getFood()) / 100);

Over real arithmetic these are equivalent; under C++ integer truncation toward zero
they can differ when the percentage is negative. A generic fixture such as current
food 1, flat food -1, percent -50 would make the validator compute -1 while runtime
would land exactly on 0.

A reliable per-EventInfo parse of CURRENT SAS XML closes the shipped route. There are
only seven food-changing EventInfos:

* EVENT_HAPPY_HUNTING_1: +8 flat, 0%
* EVENT_FAMINE_2: 0 flat, -50%
* EVENT_FAMINE_3: 0 flat, -100%
* EVENT_FARM_BANDITS_2: -5 flat, 0%
* EVENT_FARM_BANDITS_3: -5 flat, 0%
* EVENT_BABY_BOOM_1: +10 flat, 0%
* EVENT_SPOILED_GRAIN_1: 0 flat, -100%

No shipped event combines a negative flat amount with a negative percentage. For the
-50%-only case, odd food values can make the validator's intermediate result one lower
than runtime's retained food, but both remain nonnegative; -100% is exact. Therefore
the generic arithmetic mismatch cannot change current event legality and is not F576.

POST-WIP362 CONTROL - REQUIRED-BUILDING LIST WITH iNumBuildings=0 IS INTENTIONAL IN
THE TWO CURRENT FIXTURES
-------------------------------------------------------------------------------
A field census of `CvCity::getTriggerValue` found two shipped triggers with a nonempty
BuildingsRequired list but `iNumBuildings=0`:

* EVENTTRIGGER_MASTER_BLACKSMITH_DONE lists Forge;
* EVENTTRIGGER_DEFECTING_AGENT lists Intelligence Agency.

The city scorer only enforces BuildingsRequired when `iNumBuildings > 0`, which looked
like a possible skipped city prerequisite. Current caller/data contracts close both:
Defecting Agent is not a pick-city trigger and uses its building requirement through
the player/global building-count path. Master Blacksmith Done's PythonCanDo callback
explicitly checks the player's Forge target and rewrites the completion trigger's city
ID back to the original quest city. No invalid city selection was established.

POST-WIP362 CONTROL - `bPrereqEventCity` / XML `bPrereqEventPlot` NAME MISMATCH IS
INHERITED FIRAXIS LOCATION-SENSITIVE SEMANTICS
-------------------------------------------------------------------------------
`CvEventTriggerInfo::read` loads member `m_bPrereqEventCity` from XML tag
`bPrereqEventPlot`. The spelling initially looked like a parser typo. Supplied
Civ4CE/BtS, K-Mod, Base AdvC and current SAS all retain the same mapping, and consumers
use it coherently as a location-sensitive prerequisite:

* pick-city triggers require the prerequisite EventType in the city's
  `m_aEventsOccured` through `CvCity::getTriggerValue`;
* plot triggers require matching stored trigger coordinates through CvPlot logic.

The odd member/tag naming is therefore legacy contract, not independently F576.

F576 / KI#899 - CONFIRMED
-------------------------
CLEAREVENTS COLLAPSES A SPECIFIC SUCCESSFUL EVENTTYPE CLEAR INTO ONE BOOLEAN, AND
`CvCity::applyEvent(..., bClear=true)` THEN ERASES EVERY CITY-LOCAL EVENT-HISTORY FLAG;
AN UNRELATED CITY EVENT CAN SILENTLY BREAK ANOTHER SAME-CITY RECURRING EVENT CHAIN.

STATUS
------
CONFIRMED. This is an inherited Firaxis/BtS execution-scope defect retained by K-Mod,
Base AdvC 1.14 and current AdvCiv-SAS. Current shipped XML contains a direct ordinary
single-clear-target witness; it does NOT require the multi-ClearEvents configuration
that an earlier album control correctly classified as current-data masked.

ROOT - SPECIFIC PLAYER-SCOPE CLEAR BECOMES UNSCOPED CITY-SCOPE BOOLEAN
---------------------------------------------------------------------
`CvPlayer::applyEvent` evaluates every configured ClearEvents target separately. For a
successful target `eLoopEvent`, it performs the correctly scoped player/team/global
reset:

    if (kEvent.getClearEventChance(eLoopEvent) <= 0)
        continue;

    bClear = (SyncRandNum(100) < kEvent.getClearEventChance(eLoopEvent));
    if (!bClear)
        continue;

    ...
    resetEventOccured(eLoopEvent, false);

The identity of the cleared EventType is therefore known and respected at player
scope. But the function preserves only the single Boolean `bClear` for the target
city call:

    pCity->applyEvent(eEvent, *pTriggeredData, bClear);

`CvCity::applyEvent` first records the CURRENT city event:

    setEventOccured(eEvent, true);

then applies its city effects. At the end, a true Boolean does not clear the specific
successful `eLoopEvent`. It instead does:

    if (bClear)
    {
        FOR_EACH_ENUM(Event)
            setEventOccured(eLoopEvent, false);
    }

Thus one successful ClearEvents entry means "erase every EventTypes flag from this
city," even though the XML named one particular prior EventType and the player-level
code cleared exactly that one.

CITY-LOCAL EVENT HISTORY IS REAL PERSISTENT GAME STATE
------------------------------------------------------
`m_aEventsOccured` is serialized in `CvCity::read/write`; `isEventOccured` queries it,
and `CvCity::getTriggerValue` uses it whenever a trigger has the location-sensitive
prerequisite flag:

    if (kTrigger.isPrereqEventCity() && kTrigger.getNumPrereqEvents() > 0)
    {
        ...
        if (!isEventOccured((EventTypes)kTrigger.getPrereqEvent(i)))
            return MIN_INT;
    }

This is not dead/UI-only history. It is one half of live same-city event-chain
eligibility.

CURRENT SHIPPED WITNESS - SLAVE REVOLT STATE ERASED BY UNRELATED FARM BANDITS
----------------------------------------------------------------------------
Current SAS ships `EVENT_SLAVE_REVOLT_3` as a city event with exactly one ClearEvents
entry:

    EVENT_SLAVE_REVOLT_3 -> clear EVENT_SLAVE_REVOLT_3 with 40% chance

When option 3 occurs and that 40% roll FAILS (60% of executions), the normal ordering
leaves both:

1. the player's `EVENT_SLAVE_REVOLT_3` occurrence record; and
2. that city's local `EVENT_SLAVE_REVOLT_3` flag.

Those two records are exactly what `EVENTTRIGGER_SLAVE_REVOLT_AGAIN` expects. Current
trigger XML gives it:

    iPercentGamesActive = 100
    iWeight = -1
    PrereqEvents = EVENT_SLAVE_REVOLT_3
    bPrereqEventPlot = 1   // loaded as the city/location prerequisite flag
    bRecurring = 1
    bPickCity = 1

`CvPlayer::doEvents` directly calls `trigger(eLoopEventTrigger)` whenever
`getEventTriggerWeight` returns -1. `getEventTriggerWeight` first requires the player's
prerequisite EventInfo occurrence, while `pickTriggerCity -> CvCity::getTriggerValue`
requires the same city's local occurrence. Both layers are intentional and live.

Now let the SAME ordinary city later qualify for `EVENTTRIGGER_FARM_BANDITS`. The two
trigger families have no mutually exclusive requirements: e.g. a population-4+
Slavery city that has retained Slave Revolt 3 can also work/contain the ordinary
Farm-Bandits farm/resource/route/unit fixture.

Every current Farm Bandits outcome has one positive ClearEvents target, and option 1
provides the simplest deterministic path:

    EVENT_FARM_BANDITS_1 -> clear EVENT_FARM_BANDITS_3 with 100% chance

The player does NOT need to have previously experienced Farm Bandits 3;
`resetEventOccured(EVENT_FARM_BANDITS_3, false)` simply becomes a target-specific no-op
if that player record is absent. The 100% roll nevertheless leaves `bClear=true`.

`CvCity::applyEvent(EVENT_FARM_BANDITS_1, ..., true)` then:

1. records FARM_BANDITS_1 locally;
2. executes the chosen event;
3. loops over EVERY EventType and removes every local city occurrence.

That erases the unrelated `EVENT_SLAVE_REVOLT_3` city flag. Crucially,
CvPlayer cleared only `EVENT_FARM_BANDITS_3`; the player's
`EVENT_SLAVE_REVOLT_3` occurrence is untouched.

On subsequent event turns, `SLAVE_REVOLT_AGAIN` therefore passes its player-level
prerequisite but the formerly affected city fails the required same-city prerequisite.
An unrelated Farm Bandits resolution has silently broken the retained Slave Revolt
continuation state.

The inverse family can be used similarly: successful Slave Revolt clearing can wipe an
unrelated retained Farm-Bandits city flag. The root is not specific to either family;
these two shipped recurring chains merely provide the clearest current consequence.

WHY THE EARLIER CLEAREVENTS CONTROL DOES NOT DEDUP THIS FINDING
--------------------------------------------------------------
WIP196 preserved a separate generic concern: if ONE EventInfo had MULTIPLE positive
ClearEvents targets, repeatedly assigning one `bClear` would make the final Boolean
represent only the last roll. That control was correctly left unnumbered because
current SAS has exactly one positive ClearEvents target per EventInfo.

F576 does not require multiple targets at all. With exactly one current target, the
Boolean accurately says that THAT target cleared at player scope; the defect is the
NEXT layer throwing away its EventTypes identity and interpreting the bool as permission
to clear ALL city-local event histories. Current one-target XML therefore exposes F576
directly rather than masking it.

ANCESTRY
--------
Supplied Civ4CE/BtS contains the same pair of operations:

* `CvPlayer::applyEvent` reduces target-specific ClearEvents processing to `bool bClear`
  and passes it to the city;
* `CvCity::applyEvent(..., bClear)` loops over every EventInfo and clears every city
  occurrence when the bool is true.

K-Mod 1.46 retains the same logic. Base AdvC 1.14 translates the loops to enum helpers
but preserves the exact semantics, and current SAS retains them. This is therefore a
base Firaxis/BtS bug inherited through all supplied descendants, not a SAS-only change.

DUPLICATE CHECK
---------------
Whole-album searches for ClearEvents, `bClear`, `m_aEventsOccured`, city-local event
history, Slave Revolt Again and Farm Bandits found no numbered root owning this
specific player-target -> all-city-history scope widening. Existing event findings own
other lifecycle/propagation defects (e.g. team EventInfo propagation, stale delayed
choices, acquisition loss of city-event effects), and WIP196's multi-target Boolean
control is explicitly distinct as described above. F576 is independent.

CONSERVATIVE REPAIR DIRECTION - NOT IMPLEMENTED
------------------------------------------------
Do not collapse successful ClearEvents targets into a Boolean before the city-local
history update. Preserve the EventTypes identities that actually passed their rolls.
A future-proof repair should support multiple successful targets even though current
XML only uses one per EventInfo:

1. accumulate the successfully cleared EventTypes in `CvPlayer::applyEvent`;
2. apply the selected city event normally so its own local occurrence is first recorded;
3. clear from that target city ONLY the local flags whose EventTypes are in the
   successful ClearEvents set.

This ordering matters when an event explicitly clears itself: the freshly recorded
current event should be removed only if its own EventType is a successful XML clear
target. Player/team/global `resetEventOccured(eLoopEvent, ...)` logic can remain
specific as it already is.

A minimally invasive current-data patch could carry one `EventTypes` instead of a bool,
but a small vector/list is safer and preserves the XML contract if multiple targets are
added later.

REGRESSION TESTS
----------------
A. Unrelated-history preservation
1. Give player A / city C retained `EVENT_SLAVE_REVOLT_3` state at BOTH player and city
   scope (equivalent to option 3 with its 40% clear roll failing).
2. Apply `EVENT_FARM_BANDITS_1` to C; its Farm-Bandits-3 clear is 100%.
3. Assert the player-level Farm-Bandits-3 record is cleared if present.
4. Assert C's unrelated `EVENT_SLAVE_REVOLT_3` flag remains set.
5. Assert `EVENTTRIGGER_SLAVE_REVOLT_AGAIN` can still select C when its other conditions
   are satisfied.

B. Intended same-event clear
1. Apply `EVENT_SLAVE_REVOLT_3` with a forced successful 40% clear result.
2. Assert both the player occurrence and C's local `EVENT_SLAVE_REVOLT_3` flag are
   cleared, preserving intended self-clear semantics.

C. Failed-clear control
Force the Slave-Revolt-3 40% clear roll to fail and assert both player and city records
remain available to `SLAVE_REVOLT_AGAIN`.

D. Multiple-target future-proofing
Construct a test EventInfo with two ClearEvents entries and independently controlled
rolls; assert city-local history clears exactly the successful EventTypes, not all
history and not merely the final roll's state.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F576/KI#899 are CONFIRMED.
F577 / provisional KI#900 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository source remains UNTOUCHED.

Exact continuation cursor:
WIP363 -> F577 unused -> continue the random-event city selection / application census
far enough to close remaining validator/runtime sibling fields, then leave the event
family unless an independent hard contract survives; return to low-coverage
behavior-changing CvCity lifecycle/production/visibility helpers -> current SAS
caller/XML proof -> whole-album dedup -> promote only an independent live root -> freeze
WIP364 regardless.

END C031-WIP363


===============================================================================
C031-WIP364 - QUEUE 006 CVCITY.CPP / F577 CONFIRMED
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F576 / KI#899 remain CONFIRMED.
F577 / provisional KI#900 is CONFIRMED below.
F578 / provisional KI#901 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison/ancestry material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and
Civ4CE/BtS archives plus the local reachable-history material already reconciled into the
album. No repository source files modified.

CORRECTION TO WIP101 NEGATIVE CONTROL
-------------------------------------
WIP101 said that the remaining BUG additional-commerce building projection used the city's
runtime base-commerce and total-commerce-modifier structure consistently. That statement was
too broad. It correctly checked the city-local arithmetic once the hypothetical extra base
commerce/modifier values had been assembled, but it did NOT audit player-global state that the
candidate building itself mutates through `CvPlayer::processBuilding` before runtime commerce is
recomputed.

The old control is therefore amended, not silently erased: the final arithmetic shape is sound
for the inputs it models, but the hypothetical after-building state omits two live input families
owned by the candidate building itself.

F577 / provisional KI#900 - CONFIRMED
BUG BUILDING-COMMERCE "ACTUAL EFFECTS" PROJECTION EVALUATES THE CANDIDATE AGAINST THE OLD
PLAYER-GLOBAL SPECIALIST-EXTRA / STATE-RELIGION-BUILDING COMMERCE STATE
------------------------------------------------------------------------------------------------

ROOT
----
`CvCity::getAdditionalCommerceTimes100ByBuilding` advertises:

    Returns the total additional commerce times 100 that
    adding one of the given buildings will provide.

It constructs a hypothetical extra base-commerce value with
`getAdditionalBaseCommerceRateByBuilding` and a hypothetical modifier with
`getAdditionalCommerceRateModifierByBuilding`, then compares that to the city's current
`getBaseCommerceRateTimes100` / total modifier.

The helper does model many city-local consequences of the candidate building:

* ordinary / obsolete-safe CommerceChanges;
* city BuildingClass commerce changes;
* the CURRENT player's state-religion-building commerce if the candidate itself is a
  religious building;
* global religion/corporation commerce on the candidate;
* candidate free specialists (through the specialist helper);
* local + global commerce-rate modifiers.

But actual building completion also calls `CvPlayer::processBuilding`, which for every commerce
type performs:

    changeSpecialistExtraCommerce(c,
        kBuilding.getSpecialistExtraCommerce(c) * iChange);
    changeStateReligionBuildingCommerce(c,
        kBuilding.getStateReligionCommerce(c) * iChange);

Those are PLAYER-GLOBAL inputs to city commerce, and the hypothetical building helper never
constructs their post-building values.

That omission has two direct effects in the construction city itself.

1. SpecialistExtraCommerce granted by the candidate

Runtime base city commerce contains:

    (getSpecialistPopulation() + getNumGreatPeople()) *
        owner.getSpecialistExtraCommerce(eCommerce)

separately from the stored specialist-info commerce.

When the candidate building raises `owner.getSpecialistExtraCommerce`, every existing
specialist / settled Great Person in the construction city immediately gains that commerce.
The helper leaves the owner value at its pre-building amount.

Its handling of FREE specialists is also evaluated against the old player state:

    getAdditionalBaseCommerceRateBySpecialistImpl(...)
      -> owner.specialistCommerce(...)

`owner.specialistCommerce` includes the player's CURRENT SpecialistExtraCommerce, not the
candidate building's soon-to-be-added amount. Thus a building that both grants a free
specialist and raises SpecialistExtraCommerce undercounts the newly added specialist too.

2. StateReligionCommerce granted by the candidate

Runtime building commerce for each active religious building includes the player's current
`getStateReligionBuildingCommerce(eCommerce)` when that building's ReligionType matches the
player's state religion.

A candidate with nonzero `StateReligionCommerces` changes that player-global amount during
`CvPlayer::processBuilding`. At the end of `CvCity::processBuilding`, the construction city calls
`updateBuildingCommerce()`, so its existing matching religious buildings are recomputed using
the NEW value immediately.

The hypothetical helper never overlays the candidate's new StateReligionCommerce onto those
already-present religious buildings. It only asks whether the candidate building ITSELF has a
ReligionType and, if so, adds the player's OLD state-religion-building bonus to that candidate.

Therefore the helper is not evaluating the documented post-building city state.

PRIMARY CURRENT-SAS REPRO A - GREAT LIBRARY FALSE-ZERO RESEARCH
---------------------------------------------------------------
Current `BUILDING_GREAT_LIBRARY` has:

    SpecialistExtraCommerces = [0, 1, 0]
    CommerceChanges          = empty
    CommerceModifiers        = empty
    FreeSpecialistCounts     = empty
    ObsoleteTech             = NONE

Commerce order is Gold / Research / Culture / Espionage, so this is +1 Research per
specialist/settled-GP for the owner.

Use an ordinary constructible Great Library city with:

    one existing specialist (or settled Great Person)
    no pre-existing player SpecialistExtraCommerce(RESEARCH)
    no unrelated Research change from the Great Library itself
    ordinary 100% Research modifier for the minimal numeric fixture

PREVIEW:
`getAdditionalBaseCommerceRateByBuildingImpl(RESEARCH, GREAT_LIBRARY)` sees no direct Research
CommerceChange, no Research modifier and no free specialist, so `iExtraRate == 0` and
`iExtraModifier == 0`. `getAdditionalCommerceTimes100ByBuilding` returns 0 immediately.

RUNTIME:
completion reaches

    changeSpecialistExtraCommerce(RESEARCH, +1)

and the city already has one specialist. `getBaseCommerceRateTimes100(RESEARCH)` therefore gains
100 hundredths, i.e. +1 Research before any greater-than-100 commerce multiplier.

Thus the player-facing building projection can say 0 additional Research while the completed
building immediately raises that city's actual Research by +1. With N existing specialists /
settled Great People, the omitted base effect is N Research before modifiers.

PRIMARY CURRENT-SAS REPRO B - SANKORE FALSE-ZERO RESEARCH
---------------------------------------------------------
Current `BUILDING_SANKORE` has:

    StateReligionCommerces = [0, 3]
    CommerceChanges        = empty
    CommerceModifiers      = empty
    ReligionType           = NONE
    ObsoleteTech           = NONE

so it grants +3 Research to every active building of the owner's state religion.

Use a city that can construct Sankore and already contains one active building whose
`ReligionType` equals the owner's state religion.

PREVIEW:
Sankore itself has ReligionType=NONE and no direct Research CommerceChange/modifier, so the
building helper has no route by which the candidate's new +3 player-global state-religion bonus
can be applied to the existing religious building. The minimal projection is 0 Research.

RUNTIME:
`CvPlayer::processBuilding(SANKORE,+1)` calls

    changeStateReligionBuildingCommerce(RESEARCH, +3)

and `CvCity::processBuilding` subsequently calls `updateBuildingCommerce()` for the construction
city. The already-present state-religion building is therefore recounted at the new amount and
city Research immediately rises by +3 before modifiers.

This reproducer is independent of F476's stale-OTHER-city cache defect: the construction city
gets its explicit end-of-`processBuilding` recount. F577 is wrong even in a build where F476 is
fully repaired.

INTERACTION / REPAIR-SHAPE CONTROL - NATYA SHASTRA
--------------------------------------------------
Current `BUILDING_NATYA_SHASTRA` simultaneously has:

    FreeSpecialistCounts:       +1 Artist
    SpecialistExtraCommerces:   [0, 0, 1]  -> +1 Culture per specialist
    StateReligionCommerces:     [0, 0, 2]  -> +2 Culture per state-religion building

Current Artist intrinsic commerce is:

    [0 Gold, 1 Research, 4 Culture]

The old helper does count the free Artist, but evaluates that Artist through the owner's
PRE-BUILD `specialistCommerce`; it therefore omits Natya's own +1 Culture from the new Artist.
It also omits +1 Culture for every specialist/settled GP already in the city and +2 Culture for
every qualifying existing state-religion building.

This means a repair must model the post-building player-global state coherently rather than bolt
on only `existingSpecialists * delta` after the old helper has already valued free specialists.

LIVE USER-FACING CALLERS
------------------------
This helper is not dead and does not require enabling an obscure debug-only path.

`CvGameTextMgr::setBuildingNetEffectsHelp` describes itself as appending the:

    actual (i.e. absolute) effects of adding eBuilding to pCity

and feeds every commerce type from `getAdditionalCommerceTimes100ByBuilding`.
`setBuildingHelpActual` exposes that "Actual Effects" block for an active-owned city while Alt
is held (or through the corresponding BUG option; debug mode can broaden visibility).

The separate `setBuildingAdditionalCommerceHelp` also iterates every currently constructible
building and uses this exact helper to populate the "Building Additional Commerce" hover.
Thus false-zero / under-valued current-city projections are ordinary player-visible output.

ANCESTRY
--------
The BUG Building Additional Commerce helper is already present in supplied K-Mod 1.46 with the
same conceptual inputs and the same omission. Base AdvCiv 1.14 retains it, with mostly enum /
cleanup refactors, and current SAS retains the same semantic contract.

This is therefore an inherited BUG/K-Mod projection defect rather than a SAS-created code
regression.

The defect is live even in the supplied upstream data: K-Mod and Base AdvCiv 1.14 both have
building-level SpecialistExtraCommerce / StateReligionCommerce producers, including Sistine
Chapel plus Spiral Minaret / Sankore. SAS adds especially clean witnesses such as Great Library
SpecialistExtraCommerce and raises Sankore to +3 Research.

DEDUP / NEIGHBOR ROOTS
----------------------
Whole-album semantic search found no numbered root owning this hypothetical post-building
player-global-commerce omission.

* F438 / KI#761 is the Finance Advisor's CURRENT specialist-Gold breakdown using base-only
  `city.getSpecialistCommerce` while runtime / its own hover include player-wide
  SpecialistExtraCommerce. F577 is a different function and phase: a HYPOTHETICAL candidate
  building itself changes SpecialistExtraCommerce, but the before/after building projection
  evaluates against the old owner state.

* F476 / KI#799 is RUNTIME cache invalidation for StateReligionBuildingCommerce/state-religion
  changes across cities. F577 survives a complete F476 repair and has a clean same-city Sankore
  witness because the construction city explicitly recounts building commerce at the end of
  `processBuilding`.

* F397-F399 and F561 own other BUG building-projection domains (happiness/health/production/
  maintenance/trade-yield arithmetic). None owns the SpecialistExtraCommerce /
  StateReligionCommerce inputs to the commerce projection.

* WIP101's earlier "additional commerce structure matches runtime" statement is explicitly
  amended above. It checked the final city-local arithmetic but missed candidate-driven
  `CvPlayer::processBuilding` state mutations. It is not treated as a prior finding or a dedup.

CLASSIFICATION
--------------
CONFIRMED inherited BUG/K-Mod player-facing hypothetical-state defect; ordinary-live in current
SAS data.

CONSERVATIVE REPAIR DIRECTION - NOT IMPLEMENTED
------------------------------------------------
Treat the projection as a true old-city-state vs post-building-city-state comparison.
At minimum the hypothetical after-state must overlay the candidate building's:

1. `SpecialistExtraCommerce(eCommerce)` onto the owner's current value, and apply the DELTA to
   every specialist / settled Great Person that will exist after construction, including free
   specialists granted by the candidate itself;

2. `StateReligionCommerce(eCommerce)` onto the owner's current value, and apply that DELTA to
   every active building in this city whose ReligionType matches the effective state religion,
   including the candidate itself if it is such a religious building;

3. existing direct building commerce / global religion / corporation / modifier inputs already
   handled by the helper.

The safest long-term design is to share one explicit projected-base-commerce routine with runtime
input semantics instead of independently reconstructing pieces in the BUG helper. Do not mutate
real CvPlayer state merely to obtain the preview.

NO_ESPIONAGE culture/espionage remapping must remain applied after the full projected input is
assembled, just as the current helper already attempts for its narrower input set.

REGRESSION TESTS
----------------
A. Great Library / existing specialist
1. City has one specialist and owner has 0 pre-existing specialist-extra Research.
2. Query Great Library additional Research before construction.
3. Assert projected delta is +1 Research at 100% modifier, not 0.
4. Complete Great Library and assert actual city Research delta equals preview.
5. Repeat with multiple specialists/settled Great People and a non-100 Research modifier.

B. Candidate free-specialist interaction
1. Use Natya Shastra with no pre-existing specialist-extra Culture.
2. Assert its free Artist is valued with Natya's own new +1 specialist-extra Culture.
3. Add an existing specialist and verify that specialist gains +1 too.

C. State-religion-building interaction
1. City has one active building of owner's state religion.
2. Query Sankore additional Research.
3. Assert preview includes +3 base Research from that existing building.
4. Complete Sankore and compare the immediate same-city runtime delta.
5. Repeat with two qualifying religious buildings and a Research multiplier.

D. Mixed Natya interaction
City has one existing specialist and one qualifying state-religion building. Assert the Culture
preview includes the free Artist's intrinsic commerce PLUS Natya's own specialist-extra on the
free Artist PLUS +1 on the existing specialist PLUS +2 on the religious building, before the
appropriate final city modifier.

E. F476 independence
With player-wide runtime building-commerce invalidation repaired, repeat the same-city Sankore
test and assert F577 remains fixed only when the hypothetical helper models the candidate's new
StateReligionCommerce.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F577/KI#900 are CONFIRMED.
F578 / provisional KI#901 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository source remains UNTOUCHED.

Exact continuation cursor:
WIP364 -> F578 unused -> leave the now-dense BUG additional-building projection family unless a
mechanically independent contract appears; resume low-coverage lifecycle/production/visibility /
fixed-point helpers and old behavior-changing practicals -> current SAS caller/data proof ->
whole-album dedup -> promote only an independent live root -> freeze WIP365 regardless.

END C031-WIP364


===============================================================================
C031-WIP365 - QUEUE 006 CVCITY.CPP / F578 CONFIRMED
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F577 / KI#900 remain CONFIRMED.
F578 / provisional KI#901 is CONFIRMED below.
F579 / provisional KI#902 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives plus
locally reconciled reachable-history material. No repository source files modified.

POST-WIP364 CONTROLS BEFORE F578
--------------------------------
1. Event-trigger `BuildingsRequired` with `iNumBuildings == 0` does not produce a current city-
   selector bug. `EVENTTRIGGER_MASTER_BLACKSMITH_DONE` enforces the Forge/world-size requirement in
   `canTriggerMasterBlacksmithDone` and redirects the trigger to the original quest city;
   `EVENTTRIGGER_DEFECTING_AGENT` has `bPickCity=0`, so `CvCity::getTriggerValue` is not its city
   selector. Preserve the XML shape as metadata/outer-trigger contract, not F578.

2. The low-coverage city-rank setters remain coherent. `setBaseYieldRate` reaches the player-wide
   yield-rank invalidator through `updateCommerce`, while specialist/base-yield mutators likewise
   dirty the relevant rank state. `setFreeSpecialistCount` updates Great-Person count and delegates
   specialist yield/commerce/GPP processing through `processSpecialist`; no omitted live component
   was established there.

3. Commerce-happiness candidate-vs-aggregate integer rounding is GENERICALLY non-additive but
   CURRENT-DATA MASKED. Runtime aggregates `m_aiCommerceHappinessPer` before multiplying by the
   commerce slider, whereas `getAdditionalHappinessByBuilding` multiplies/divides the candidate
   building separately. Current SAS has only:
      Theatre / Greek Odeon = 5 Culture;
      Hall of Dancers / Colosseum / Ball Court / Indian Mausoleum = 10 Culture;
      Broadcast Tower = 3 Gold, 4 Research, 10 Culture, 10 Espionage.
   With the shipped 10-point commerce increments and actual replacement/coexistence structure, the
   current fractional pieces do not create a demonstrated marginal-delta mismatch. Keep as mod-mod
   robustness, not KI#901.

-------------------------------------------------------------------------------
F578 / provisional KI#901 - CONFIRMED
BUG BUILDING-ADDITIONAL-YIELD PROJECTION IGNORES THE CANDIDATE BUILDING'S
PLAYER-GLOBAL `SpecialistYieldChanges`, SO A WONDER CAN PREVIEW +0 PRODUCTION
WHILE IMMEDIATELY RAISING THE CONSTRUCTION CITY'S REAL BASE PRODUCTION
-------------------------------------------------------------------------------

ROOT
----
`CvCity::getAdditionalYieldByBuilding` claims to return the total additional yield from adding a
building. Its base-yield helper models direct building yield, worked sea/river changes, city
building-class yield, trade-route changes, vote-source religion yield and candidate free
specialists. But it never models `CvBuildingInfo::SpecialistYieldChanges` owned by the candidate.

Actual building completion does. `CvCity::processBuilding` calls `CvPlayer::processBuilding` for the
appropriate team member(s). `CvPlayer::processBuilding` iterates the candidate building's
`getSpecialistYieldChange()` map and executes:

    changeSpecialistExtraYield(eSpecialist, eYield,
        candidateSpecialistYieldChange * iChange);

`CvPlayer::changeSpecialistExtraYield` then immediately calls `updateExtraSpecialistYield()` for the
player's cities. In each city, `CvCity::updateExtraSpecialistYield` recomputes:

    (getSpecialistCount(eSpecialist) + getFreeSpecialistCount(eSpecialist)) *
        owner.getSpecialistExtraYield(eSpecialist, eYield)

and applies the resulting delta through `changeBaseYieldRate`.

Therefore the candidate can change the construction city's actual base yield through specialists
that ALREADY exist in that city, even when the candidate has no ordinary YieldChange or YieldModifier.
The BUG helper evaluates all of those specialists using the player's PRE-BUILD specialist-extra-yield
state and contains no overlay for the candidate's new global SpecialistYieldChanges.

CLEAN CURRENT-SAS FIXTURE - ANGKOR WAT
--------------------------------------
Current SAS `BUILDING_ANGKOR_WAT` has:

    SpecialistYieldChanges:
        SPECIALIST_PRIEST -> +1 Production, +1 raw Commerce

For the Production-yield projection it has no confounding direct producer:

    YieldChanges(PRODUCTION)                 = 0
    YieldModifiers(PRODUCTION)               = 0
    PowerYieldModifiers(PRODUCTION)          = 0
    Sea/River/GlobalSeaPlotYieldChanges      = 0
    Area/GlobalYieldModifiers                = 0
    TradeRouteModifier / ForeignTradeRoute   = 0
    FreeSpecialistCounts                     = none

Take an active-owned city with exactly one assigned Priest, no pre-existing Priest specialist-extra
Production, and an ordinary 100% base Production modifier. Let Angkor Wat be constructible there.

Before completion:
    Priest extra Production from Angkor Wat = 0

BUG hypothetical calculation:
    getAdditionalBaseYieldRateByBuilding(PRODUCTION, ANGKOR_WAT) = 0
    getAdditionalYieldRateModifierByBuilding(PRODUCTION, ANGKOR_WAT) = 0
    => getAdditionalYieldByBuilding(...) = 0

Actual completion:
    CvPlayer::processBuilding(ANGKOR_WAT, +1, ...)
      -> changeSpecialistExtraYield(PRIEST, PRODUCTION, +1)
      -> city updateExtraSpecialistYield(PRODUCTION)
      -> one existing Priest contributes +1 new base Production
      -> changeBaseYieldRate(PRODUCTION, +1)

So the same city immediately changes by +1 real Production while the helper can report +0.
The current SAS addition of +1 raw Commerce to the same Priests supplies a second yield-domain
manifestation of the same omitted SpecialistYieldChanges root.

CURRENT-DATA BREADTH
--------------------
Current SAS has three nonzero building SpecialistYieldChanges producers:

* `BUILDING_ANGKOR_WAT`: Priest +1 Production, +1 raw Commerce;
* `BUILDING_BROADWAY`: Artist +2 raw Commerce;
* `BUILDING_CRISTO_REDENTOR`: Priest +1 Production, +1 raw Commerce.

Cristo Redentor additionally grants one free Priest. That exposes an interaction the repair must
handle: the existing BUG helper values candidate free specialists through
`getAdditionalBaseYieldRateBySpecialist`, which uses the OLD player specialist-extra-yield state.
The newly granted free Priest should receive the candidate's own Priest yield bonus too. More
generally, if C matching specialists already exist, F matching free specialists are added by the
candidate, current extra yield is E and the candidate adds D, the omitted base-yield component is
not merely C*D; the new specialists also need D, i.e. `(C + F) * D` in addition to the helper's
already-modeled `F * (intrinsic + E)` term.

LIVE USER-FACING CALLERS
------------------------
The defect is not a dead helper.

1. Building Actual Effects:
   `CvGameTextMgr::setBuildingNetEffectsHelp` describes itself as appending the "actual (i.e.
   absolute) effects" of adding a building. It calls `getAdditionalYieldByBuilding` for every yield.
   For an active-owned city this path is available by holding Alt on the building hover (or enabling
   the corresponding BUG option).

2. Production hover:
   `CvGameTextMgr::setProductionHelp` enables `setBuildingAdditionalYieldHelp(...,
   YIELD_PRODUCTION, ...)` when Alt is held or `MiscHover__BuildingAdditionalProduction` is enabled.
   That helper enumerates currently constructible buildings and calls the same city projection.

Thus an ordinary Alt hover in the Angkor-Wat fixture can omit the +1 Production that completion will
immediately create in the city.

ANCESTRY / CLASSIFICATION
-------------------------
K-Mod 1.46 already contains the BUG Building Additional Yield helpers with the same omission: after
direct/plot/trade effects it accounts for candidate free specialists but does not overlay the
candidate's `SpecialistYieldChanges` player state.

K-Mod's shipped Angkor Wat already supplies a live positive-control producer (Priest +1 Production),
so the defect predates SAS data changes. Base AdvCiv 1.14 inherits the helper. Current SAS retains it
and broadens the visible current-data witnesses. The supplied Civ4CE/BtS core does not contain this
BUG helper layer.

Classification:
    inherited BUG/K-Mod hypothetical-building yield-projection defect retained by AdvCiv/SAS.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album searches found no numbered root owning candidate `SpecialistYieldChanges` omission in
`getAdditionalYieldByBuilding`.

It is distinct from:

* F398 / KI#721 - the SAME broad yield helper wrongly treats military/domain/space ORDER-SPECIFIC
  production modifiers as generic Production yield. F578 instead omits a REAL base-yield change
  produced by candidate-driven player specialist-yield state; fixing F398 does not fix F578.
* F561 / KI#884 - arithmetic/order loss in the trade-yield branch of
  `getAdditionalBaseYieldRateByBuilding`; F578 survives with no trade routes at all.
* F577 / KI#900 - the commerce projection omits candidate-driven
  SpecialistExtraCommerce/StateReligionCommerce. F578 is a different helper and authoritative state
  (`SpecialistExtraYield`) feeding base YIELDS rather than final CommerceTypes output.
* KI#306 broadened - the AI top-production-city cache can remain stale AFTER a SpecialistYieldChanges
  building mutates city production. F578 is wrong BEFORE-BUILD hypothetical calculation; even a
  perfectly invalidated KI#306 cache leaves this projection at +0.

The early album reference to Angkor Wat/Cristo Redentor SpecialistYieldChanges belongs to the KI#306
cache proof and is therefore a useful runtime oracle, not a duplicate of F578.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
Extend the hypothetical base-yield state to include the candidate's
`SpecialistYieldChanges` for the construction city.

For each `(SpecialistTypes s, YieldTypes y, delta D)` supplied by the candidate:

1. count the city's current assigned + explicitly free specialists of type s;
2. include candidate `FreeSpecialistCounts(s)` in the post-building count;
3. add the post-state delta caused by D for all those specialists;
4. ensure candidate free specialists are not valued twice - the existing free-specialist helper may
   continue to add their intrinsic/current-extra yield, while the new overlay adds only the
   candidate-driven D portion.

A robust implementation should construct one coherent hypothetical post-building specialist state
rather than bolt isolated terms together; that avoids the same interaction mistake F577 exposed in
commerce projection.

This helper's local-city contract does not require summing the candidate's SpecialistYieldChanges
across every other city in the empire. It does, however, have to include the construction city's own
immediate real yield change if it claims "total additional yield" for that city.

REGRESSION TESTS
----------------
A. Angkor Wat zero-vs-+1 witness
1. City has one Priest, no Priest specialist-extra Production, and no complicating Production
   modifiers.
2. Assert pre-build projection for Angkor Wat includes +1 Production.
3. Complete Angkor Wat and assert the city's base Production rises by exactly +1.
4. Assert projected and actual local delta agree.

B. Existing-specialist multiplicity
With three Priests, assert Angkor Wat projects +3 Production before ordinary yield modifiers.

C. Cristo interaction
1. City begins with one Priest.
2. Candidate Cristo Redentor grants one free Priest and Priest +1 Production.
3. Assert projection includes both the candidate free Priest's ordinary specialist yield and the
   candidate-driven +1 Production on BOTH the old and new Priest.

D. No-specialist control
With no matching Priest and candidate Angkor Wat (which grants no free Priest), assert the immediate
local Production delta from SpecialistYieldChanges is 0.

E. F398 separation
Use Heroic Epic or Drydock to verify conditional order-production modifiers remain excluded from
ordinary base Production after F398 repair while Angkor-Wat specialist Production remains included.

CURRENT STONE AFTER C031-WIP365
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 are CONFIRMED.
F579 / provisional KI#902 is UNUSED.
Latest physical checkpoint: C031-WIP365.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F579 unused. Leave the now-dense BUG additional-building projection family unless a
mechanically independent contract appears. Resume low-coverage behavior-changing `CvCity` lifecycle,
production, visibility and fixed-point/state helpers, with special priority on current-SAS / AdvC
semantic deltas and old bugfixes that may have left sibling validation/runtime paths behind. Apply
current caller/XML proof and whole-album dedup before promotion. Freeze WIP366 regardless of whether
F579 is consumed.

END C031-WIP365

===============================================================================
C031-WIP366 - POST-F578 LOW-COVERAGE HISTORICAL-REPAIR / HARD-STATE CONTROLS
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 remain CONFIRMED.
F579 / provisional KI#902 remains UNUSED.
Actual AdvCiv-SAS source remains UNTOUCHED.

PURPOSE OF THIS TRANCHE
-----------------------
After F578, deliberately leave the now-dense BUG additional-building projection family unless a
mechanically independent contract appears. Rank low-coverage `CvCity` code by semantic risk instead:
small AdvC history changes that altered loops/conditions/arithmetic, persistent-state lifecycle,
owner/team receivers, rank/cache invalidation and production/visibility helpers. Require current-data
or current-caller proof and whole-album dedup before consuming F579.

NEGATIVE CONTROL - PRACTICAL 2617 ADJACENT-PLOT ITERATOR REPLACEMENT
--------------------------------------------------------------------
Practical 2617 replaced repeated `FOR_EACH_ENUM(Direction)` + `plotDirection(...) != NULL` patterns
with `FOR_EACH_ADJ_PLOT[_VAR]` helpers in city founding/killing culture updates, capital water checks,
`cultureStrength`, `sharedWaterArea`, and Great Wall neighborhood scans.

The replacement helper iterates the same legal adjacent/cardinal plots while excluding the same
off-map/null positions that the explicit `plotDirection` guard skipped. The live operations and their
ordering are unchanged. No wrap/border witness was found in which the macro broadens or shrinks the
neighborhood. No F579.

NEGATIVE CONTROL - PRACTICAL 3052 SPARSE ENUMMAP ITERATION
----------------------------------------------------------
Practical 3052 converted several full enum scans plus `!= 0` / `isAny...` checks into sparse
`FOR_EACH_NON_DEFAULT_*` iteration. Audited the semantically interesting `CvCity` hunks:

* building-class prerequisites in `canConstruct`;
* bonus production modifiers for buildings/projects;
* religion influence, max/free specialists, improvement free specialists and unit-combat XP in
  `processBuilding`;
* bonus-yield modifiers;
* free-specialist contributions to additional GPP/yield/commerce helpers;
* bonus happiness/health hypothetical loops;
* religion additions in `setNumRealBuildingTimed`.

In each case the old body either explicitly skipped default/zero entries or adding a zero entry was a
no-op. The sparse maps therefore enumerate exactly the entries that could have changed state. The
split `getSpecialistCount` / `getFreeSpecialistCount` loops also preserve the old semantics: each map
is independently applied and neither depended on the other map having a non-default key. No current
state omitted; no F579.

NEGATIVE CONTROL - PRACTICAL 3887 HEALTH GETTER REWRITE
-------------------------------------------------------
Practical 3887 rewrote verbose sign tests into `std::max(0, x)` / `std::min(0, x)` accumulation for
city good/bad health. This is algebraically equivalent on every integer input:

    old: if (x > 0) total += x        <=>  total += max(0, x)
    old: if (x < 0) total += x        <=>  total += min(0, x)

The espionage-health counter remains subtracted as a non-negative bad-health quantity. Combined
owner+city ExtraHealth is still sign-classified only after aggregation, exactly as before. No changed
rounding/order or current-data edge survives. No F579.

NEGATIVE CONTROL - PRACTICALS 2655 / 2986 / 3047 / 3397 / 3592
---------------------------------------------------------------
The reviewed `CvCity` hunks in these small low-coverage practicals are either formatting/renaming or
semantics-preserving early-return/refactor changes:

* 2655: revolt/occupation arithmetic and comments only;
* 2986: formatting of production/anger arithmetic, pointer-to-reference `isConnectedTo`, removal of
  unsupported `_MOD_FRACTRADE` preprocessor branches, and equivalent culture/religion calls;
* 3047: local-variable simplification of building bonus health/happiness and free-specialist code;
* 3397: `setCulture` inverted guard (`if equal return`) preserves the same old->new mutation and plot
  culture delta;
* 3592: population-rank vector/name cleanup only.

No independently behavior-changing current root was found in this set.

RANK-CACHE / LOW-COVERAGE STATE CONTROL
---------------------------------------
A function-body coverage map highlighted the population/base-yield/yield/commerce rank finders as
low-literal-mention methods. Their local behavior is coherent:

* each finder recomputes and writes ranks for ALL of the owner's cities at once;
* population changes call owner-wide `invalidatePopulationRankCache()`;
* base-yield changes call owner-wide `invalidateYieldRankCache(eYield)` through the canonical
  `changeBaseYieldRate` / related mutation paths;
* commerce mutations reach owner-wide `invalidateCommerceRankCache` through the update paths already
  audited in earlier cache findings.

No new stale-rank route was established here. Existing population/trade-route rank-state findings
remain the relevant owned roots where applicable.

WLTKD / MAINTENANCE CONTROL
---------------------------
`CvCity::setWeLoveTheKingDay` has zero literal album mentions but is not a stale-maintenance bug:
changing either into or out of WLTKD first assigns `m_bWeLoveTheKingDay` and calls
`updateMaintenance()`. AdvC then returns early only on WLTKD END so that the celebration message is
shown on BEGIN only. This is explicit `advc.001` presentation policy; maintenance has already been
updated before the return. No F579.

LOW-COVERAGE PRODUCTION HELPERS - CURRENT STATUS
------------------------------------------------
Quick hard-invariant checks of `isProductionLimited`, `getFoodTurnsLeft` and `hurryProduction` did
not expose a current defect:

* `isProductionLimited` switches on the head order and queries the matching Unit/Building/Project
  `isLimited` flag; maintain/no-order returns false as intended;
* `getFoodTurnsLeft` explicitly uses negative return values for starvation and clamps a zero-food
  starvation state to -1 turn;
* `hurryProduction` retains the advc.064b population-rush compensation already audited with the
  overflow/hurry family; no new divide/order contradiction surfaced in the current legal domain.

CURRENT STONE AFTER C031-WIP366
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 are CONFIRMED.
F579 / provisional KI#902 is UNUSED.
Latest physical checkpoint: C031-WIP366.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F579 unused with a different low-coverage lens rather than squeezing the mostly-clean
small practical refactors. Prefer lifecycle/state transitions and current gameplay calculations with
nontrivial owner/team/global dependencies: production lifecycle callbacks, reveal/visibility state,
religion/corporation/vote-source transitions, meltdown/power selection, and signed fixed-point or
validation/runtime siblings. Current-data/caller proof and whole-album dedup remain mandatory.
Freeze WIP367 regardless of whether F579 is consumed.

END C031-WIP366


===============================================================================
C031-WIP367 - POST-WIP366 PRODUCTION / VISIBILITY / POWER NEGATIVE CONTROLS
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 remain CONFIRMED.
F579 / provisional KI#902 remains UNUSED.
Actual AdvCiv-SAS source remains UNTOUCHED.

PURPOSE OF THIS TRANCHE
-----------------------
Continue the low-coverage lifecycle/state pass promised by WIP366. Prioritize code whose correctness
can depend on queue-head mutation, active owner/team identity, reveal state, or power/meltdown state.
Do not spend F579 on disabled configuration, an exposed-API robustness concern without a current
producer, or an inherited oddity whose current contract remains coherent.

NEGATIVE CONTROL - `getArtStyleType` / advc.005f CURRENTLY DISABLED
-------------------------------------------------------------------
`CvCity::getArtStyleType()` contains an optional advc.005f branch that substitutes the city's
cultural owner as art-style source. Current SAS ships:

    ENABLE_005F = 0

and the define comment itself says 0 is recommended. Therefore the cultural-owner branch is not a
current gameplay path. Even if enabled, the local `eArtPlayer` begins as the real city owner and is
replaced only when `calculateCulturalOwner()` returns a concrete player. A `NO_PLAYER` result trips
an assertion in assert builds but leaves the real owner in `eArtPlayer`; it does not feed
`GET_PLAYER(NO_PLAYER)` in release semantics. No F579.

NEGATIVE CONTROL - ORDINARY CULTURE PROCESS DOES NOT HAVE A LEVEL-0 STOP-MARKER HOLE
------------------------------------------------------------------------------------
K-Mod's human Culture-process metadata stores the current culture level in `OrderData::iData2` and
later stops the process when:

    iData2 > 0 && getCultureLevel() > iData2

At first glance, a human process started at culture level 0 would never stop at its first expansion.
Current SAS culture-level data and `calculateCultureLevel()` close that ordinary case: both
`CULTURELEVEL_NONE` and `CULTURELEVEL_POOR` have threshold 0, and the calculation scans levels in
reverse. Therefore an ordinary non-occupation city with zero culture evaluates to
`CULTURELEVEL_POOR` (1), not NONE (0), and receives a positive marker. Level NONE is primarily the
forced occupation state; occupation ending is not a clean ordinary "culture process expanded from
level 0" witness. Existing F494 remains the real unhandled Culture-process metadata transition (AI
Auto Play hand-back). No F579.

PRODUCTION LIFECYCLE CONTROL - MAKING COUNTERS / PROCESS HEAD STATE
--------------------------------------------------------------------
Rechecked `pushOrder`, `popOrder`, `startHeadOrder`, `stopHeadOrder`, and `processProcess` against the
already-audited making-count family.

* Valid ORDER_TRAIN / ORDER_CONSTRUCT / ORDER_CREATE insertion increments exactly the matching
  player/team making counter before the node is installed.
* `popOrder` decrements the counter encoded by the concrete queue node before deletion/completion;
  saved-repeat insertion gives the intentional temporary +1 then -1 net state already frozen in
  the earlier counter control.
* `startHeadOrder` / `stopHeadOrder` intentionally mutate only ORDER_MAINTAIN's
  ProductionToCommerce modifiers. Unit/building/project making state belongs to queue membership,
  not head position, so reordering the head does not require making-counter changes.
* The duplicate `cityBuildingUnit` / `cityBuildingBuilding` reporter dispatch in `pushOrder` is the
  already-frozen logging/UI-quality control; current shipped handlers do not establish a new
  authoritative queue/state mutation.

No independent F579.

ROBUSTNESS ONLY - `bPop` REMOVES OLD HEAD BEFORE NEW ORDER VALIDATION
---------------------------------------------------------------------
`CvCity::pushOrder(..., bPop=true)` performs `popOrder(0)` before validating the replacement order.
Thus a stale/malformed caller could erase the existing head and then fail `canTrain` / `canConstruct`
/ `canCreate` / `canMaintain`, leaving no replacement. The normal city UI/network path supplies
`bPop=true` for direct replacement clicks, but this tranche did not establish a deterministic
ordinary current-SAS producer in which the displayed order is legal at click time and becomes
illegal before deterministic message execution. Preserve as multiplayer/API hardening unless a
concrete current transition is proved; no F579.

MELTDOWN / POWER CONTROL
------------------------
Current SAS has one nonzero meltdown-risk building, `BUILDING_NUCLEAR_PLANT`, with
`iNukeExplosionRand = 1429`. Even the fastest shipped core speed ratio used by the advc.652 scaling
is far above the point that could truncate this divisor to 0, so the integer speed adjustment does
not create a current zero-divisor/random-domain failure.

`isMeltdownBuildingSuperseded()` also orders same-dirtiness sources coherently: larger
NukeExplosionRand means lower one-in-N meltdown probability and therefore a safer source; clean
power supersedes dirty power. No current free/multiple-risk source combination was established that
breaks this ordering. No F579.

VISIBILITY CONTROL
------------------
`isAllBuildingsVisible()` contains the odd inherited expression:

    getPlot().plotCheck(...) > 0 != NULL

but operator precedence reduces this to a boolean comparison equivalent to `plotCheck(...) > 0`;
it does not dereference or compare a pointer here. `getVisibleEffects()` uses city-owner visibility
for the occupation smoke effect, but no authoritative hidden-state leak was established from the
DLL caller contract; the surrounding city itself must already be rendered/visible by the EXE.
Preserve as a readability/probe note only. No F579.

CURRENT STONE AFTER C031-WIP367
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 are CONFIRMED.
F579 / provisional KI#902 is UNUSED.
Latest physical checkpoint: C031-WIP367.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F579 unused with a fresh semantic-risk map. Prefer long live methods with little album
coverage that mutate persistent city/player/team state or calculate gameplay-facing quantities:
anger/happiness arithmetic, production-time/decay state, power/health transition siblings,
religion/corporation spread state, and old bugfixes whose runtime repair may have left a preview or
mutation sibling behind. Require current SAS XML/caller proof plus whole-album dedup before promotion.
Freeze WIP368 regardless of whether F579 is consumed.

END C031-WIP367


================================================================================
C031-WIP368 - POST-WIP367 SEMANTIC-RISK NEGATIVE CONTROL CHECKPOINT
================================================================================

AREA CLEAN-POWER / YIELD-RANK INVALIDATION CONTROL
--------------------------------------------------
A promising local inconsistency closes after tracing the whole caller chain. `CvCity::changePowerCount`
explicitly invalidates the owner's yield-rank cache when local `isPower()` changes, while
`CvArea::changeCleanPowerCount()` visibly calls only team `updateCommerce()` / `updatePowerHealth()`.
This initially looked able to leave Factory-style power Production changes out of cached
`findYieldRateRank()` results after area-wide clean power (e.g. Three Gorges) toggles.

The indirect chain is sufficient, however:

    CvArea::changeCleanPowerCount
      -> CvTeam::updateCommerce
      -> each member CvPlayer::updateCommerce()
      -> each city CvCity::updateCommerce()
      -> GET_PLAYER(getOwner()).invalidateYieldRankCache()

Thus area-wide clean-power transitions do invalidate yield ranks before the cache can remain stale.
No F579.

CORPORATION HEADQUARTERS / REPLACEMENT REACHABILITY CONTROL
------------------------------------------------------------
`CvCity::setHasCorporation()` contains an inherited branch that, if a newly-added corporation
competes with a corporation headquartered in the same city, calls
`CvGame::replaceCorporation(old,new)`. The replacement function converts every old-corporation city
and kills old executives but does not visibly retarget/clear the old global headquarters pointer.
This is structurally fragile and inherited from BtS/K-Mod.

Ordinary current-SAS reachability closes the candidate: `CvUnit::canSpreadCorporation()` explicitly
rejects spreading a corporation into a city that is headquarters of a competing corporation. Normal
city acquisition restores only the corporation set the city already possessed, and normal
corporation founding does not use this replacement route. Therefore the stale-HQ-pointer shape needs
a lower-level/script/injected state rather than a demonstrated shipped executive path. Preserve as
robustness/API hardening; no F579.

HEADQUARTERS FREE-UNIT CONTROL
------------------------------
`CvCity::setHeadquarters()` grants the corporation's configured `FreeUnitClass` after calling
`CvGame::setHeadquarters()`, so repeated wrapper calls would be dangerous. Current native callers are
bounded: `CvPlayer::foundCorporation()` returns if the corporation is already founded, and building
processing invokes the wrapper only while `!isCorporationFounded`. Acquisition and Python
WorldBuilder-style headquarters manipulation use the lower-level `CvGame::setHeadquarters()` and do
not award the wrapper free unit. No ordinary repeat-award route established; no F579.

PRODUCTION AUTOMATION CLEAR-QUEUE CONTROL
------------------------------------------
`CvCity::setProductionAutomated(bNewValue,bClear)` clears the order queue only inside
`isActiveOwned() && isCitySelected()`. This would be surprising for an arbitrary programmatic
`bNewValue=true,bClear=true` call on an unselected city, but current callers do not establish such a
case: the UI task is dispatched to selected cities, Python passes `bClear=false`, and RiseFall's
`bClear=true` call disables automation (`bNewValue=false`). Current behavior is therefore caller-
contract dependent but not a demonstrated gameplay defect. No F579.

CULTURE UPDATE TIMER ASSERTION - INHERITED DEBUG CONTRACT BUG ONLY
------------------------------------------------------------------
`CvCity::setCultureUpdateTimer(int iNewValue)` contains an inherited copy/paste assertion:

    m_iCultureUpdateTimer = iNewValue;
    FAssert(getOccupationTimer() >= 0);

The assertion plainly checks the wrong timer; K-Mod/AdvC carry the same defect. Current native callers
add positive mission/event revolt turns, decrement only while `getCultureUpdateTimer() > 0`, or reset
to 0, so no shipped native path was found that relies on the missing nonnegative assertion. This is
a real debug/API robustness defect but does not meet the current-live bar for F579.

ANGER / OCCUPATION INITIAL CONTROL
----------------------------------
The basic overcrowding/hurry/conscript/defy anger formulas and occupation transition setters were
re-read while looking for signed-divisor and mutation-order mistakes. `getOvercrowdingPercentAnger`
uses identical positive population terms in numerator/denominator by design to generate the base
one-citizen overcrowding anger in permillage units. Hurry/conscript/defy timers guard zero before
using their positive flat lengths; occupation transitions perform corporation/maintenance/trade/
commerce/culture updates on the actual boolean occupation-state edge. No independent current defect
was established in this slice.

CURRENT STONE AFTER C031-WIP368
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F578 / KI#901 are CONFIRMED.
F579 / provisional KI#902 is UNUSED.
Latest physical checkpoint: C031-WIP368.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F579 unused. Leave the now-controlled clean-power/HQ/automation/timer branches behind.
Prioritize persistent city-state transitions whose local helper differs from the runtime/global
owner-team state: religion/corporation update ordering, anger/happiness modifier rescaling,
production-time state, reveal/espionage visibility mutation, and historical AdvC bugfix siblings.
Require current SAS caller/XML proof and whole-album dedup before promotion. Freeze WIP369 regardless.

END C031-WIP368


================================================================================
C031-WIP369 - QUEUE 006 CVCITY.CPP / HURRY-ANGER MODIFIER RESCALE ROUNDING, F579
================================================================================

HANDOFF / SOURCE STONE
----------------------
Resumed from C031-WIP368 with F578 / KI#901 confirmed and F579 / provisional KI#902 unused.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN. Current SAS, Base AdvCiv 1.14, K-Mod 1.46 and
Civ4CE/BtS supplied trees were checked locally. Actual AdvCiv-SAS source remains UNTOUCHED.

POST-WIP368 CONTROLS BEFORE PROMOTION
-------------------------------------
1. `CvCity::getReligionGrip()` has an apparently unguarded `GET_PLAYER(cultOwner)` after
   `calculateCulturalOwner()`. The dereference is nevertheless protected by a stronger implication:
   the enclosing branch first requires `revoltProbability(true,false,true) > 0`, and
   `revoltProbability` itself calls the same `calculateCulturalOwner()` and returns exactly 0 for
   `NO_PLAYER`, same-team cultural owners and disabled Barbarian revolt owners. There is no
   intervening mutation. Thus a positive result guarantees a valid cultural-owner PlayerTypes.
   No F579 on that sentinel path.

F579 / KI#902 - CONFIRMED
-------------------------
CHANGING A CITY'S HURRY-ANGER MODIFIER RESCALES THE STORED TIMER USING THE RAW PERCENT MODIFIER
INSTEAD OF THE ACTUAL INTEGER `flatHurryAngerLength()`. ON SHIPPED SPEEDS WHERE THE FLAT LENGTH HAS
ALREADY ROUNDED, ADDING/REMOVING THE AZTEC SACRIFICIAL ALTAR CAN CREATE OR DELETE WHOLE WHIP-ANGER
STACKS. CURRENT SAS'S STONEHENGE -> FREE MONUMENT MAPPING PROVIDES AN ORDINARY EMPIRE-WIDE PRODUCER.

ROOT
----
The authoritative number of hurry-anger layers for a positive timer is:

    ((getHurryAngerTimer() - 1) / flatHurryAngerLength()) + 1

i.e. `ceil(timer / actualFlatLength)`.

But `changeHurryAngerModifier()` does not rescale against that actual old/new flat length. It uses:

    iRatio = (100 * (m_iHurryAngerTimer - 1)) /
             max(1, 100 + oldModifier);
    m_iHurryAngerModifier += iChange;
    m_iHurryAngerTimer = (iRatio *
             max(1, 100 + newModifier)) / 100 + 1;

That is equivalent only when the speed-scaled base hurry length and the modifier produce exact
integer lengths. `flatHurryAngerLength()` performs a separate integer truncation:

    base = CvGame::getHurryAngerLength();
    length = base * max(0,100+modifier) / 100;
    return max(1,length);

so the raw modifier ratio and the actual timer cycle can diverge.

CURRENT SAS SPEED WITNESSES
---------------------------
Stock/current `HURRY_ANGER_DIVISOR` is 10. Current SAS game speeds include:

    Turbo: HurryConscriptAngerPercent = 50  -> base length 5
    Nitro: HurryConscriptAngerPercent = 33  -> base length 3
    Epic:  HurryConscriptAngerPercent = 150 -> base length 15

The current Aztec Sacrificial Altar has `iHurryAngerModifier = -50`.
Therefore the actual modified flat lengths are:

    Turbo: 5 * 50 / 100 = 2   (not 2.5)
    Nitro: 3 * 50 / 100 = 1   (not 1.5)
    Epic: 15 * 50 / 100 = 7   (not 7.5)

Normal (10 -> 5), Quick (6 -> 3), Marathon/SAS slow speeds with even base lengths are controls where
this particular -50% transition is exact and the inherited formula happens to preserve the stack
cycle.

DETERMINISTIC CURRENT-SAS TURBO FIXTURE - REMOVAL DELETES AN ANGER STACK
------------------------------------------------------------------------
Current SAS deliberately maps:

    CIVILIZATION_AZTEC
      BUILDINGCLASS_MONUMENT -> BUILDING_AZTEC_SACRIFICIAL_ALTAR

and current Stonehenge has:

    FreeBuilding = BUILDINGCLASS_MONUMENT

`CvPlayer::processBuilding(STONEHENGE,+1)` therefore gives the Aztec player a free Sacrificial
Altar in every eligible city through `changeFreeBuildingCount`; losing Stonehenge removes those
free Altars through the symmetric `-1` path.

On Turbo while the free Altar is active:

    base hurry length = 5
    modifier = -50
    actual flat hurry length = 2

Repeated human whipping naturally creates timer 3:

1. First whip after the city turn: timer 2.
2. Next city turn decrements it to 1.
3. A second legal whip after that city turn adds another 2: timer 3.

Authoritative current anger count is then:

    ((3 - 1) / 2) + 1 = 2 stacks.

Now let an enemy capture the Aztec city containing Stonehenge. City destruction/acquisition removes
Stonehenge from the old owner; player building processing immediately drops the empire-wide free
Monument source, so every remaining Aztec free Sacrificial Altar is removed. In a target city with
`timer=3`, `changeHurryAngerModifier(+50)` runs:

    old modifier = -50
    iRatio = 100 * (3 - 1) / 50 = 4
    new modifier = 0
    new timer = 4 * 100 / 100 + 1 = 5

The new actual flat hurry length is 5, hence authoritative anger becomes:

    ((5 - 1) / 5) + 1 = 1 stack.

One entire whip-anger layer has disappeared because a duration-reduction source was REMOVED.
Nothing paid off or decremented that layer; the source transition itself destroyed it.

The inverse direction is also wrong on affected speeds. Turbo timer 5 with no modifier is one
anger stack; adding -50 through the current formula gives timer 3 while actual new length is 2,
which is interpreted as two stacks until a later decrement. The removal fixture is the cleaner
persistent gameplay proof because it deletes an existing stack and does not rely on observing a
short intra-city-turn state.

WHY THE STONEHENGE LOSS PATH IS ORDINARY AND PERSISTENT
-------------------------------------------------------
`BUILDING_STONEHENGE` is a live world wonder in current SAS and grants the Monument class for free.
`CvPlayer::changeFreeBuildingCount` loops every city and calls `setNumFreeBuilding`; a 1->0 building
count transition calls the city's normal `processBuilding`, including
`changeHurryAngerModifier(+50)` for the Sacrificial Altar.

When another player captures the Stonehenge city, the old city's real buildings are removed during
its kill/acquisition lifecycle, so the old owner loses the free-building source immediately. The
remaining Aztec city's transformed timer persists as ordinary serialized city state. On its next
turn it starts from the already-wrong one-stack representation; this is not merely a tooltip or a
single-expression preview.

ANCESTRY
--------
The timer-rescale implementation is inherited unchanged through:

- supplied Civ4CE/BtS;
- K-Mod 1.46;
- Base AdvCiv 1.14;
- current AdvCiv-SAS.

Thus the arithmetic defect is inherited Firaxis behavior. Current SAS gives it an especially clean
ordinary producer because the mod maps the Aztec Sacrificial Altar to Monument while Stonehenge
still grants `BUILDINGCLASS_MONUMENT` empire-wide, and SAS additionally ships very fast speeds such
as Turbo/Nitro whose integer half-lengths expose the mismatch strongly. Epic already demonstrates
the same rounding family at 15 -> 7.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album searches for `changeHurryAngerModifier`, timer rescaling, Sacrificial Altar timer
rounding and Stonehenge/Sacrificial interactions found no prior numbered owner.

Distinct from:

- F492 / KI#815: No-Slavery Sacrificial-Altar pop-rush entitlement is mis-cached when the building
  is completed while the human is disabled by AI Auto Play. F579 changes authoritative hurry-anger
  timer arithmetic regardless of No Slavery or human-disabled state.
- F530 / KI#853: `CvCityAI::AI_yieldValue` uses the wrong modulo to PREDICT the next anger-stack
  expiry. F579 mutates the authoritative `m_iHurryAngerTimer` itself during a modifier transition.
- F537 / KI#860: the same AI valuation counts at most one future recovery per anger source. Again,
  F579 is runtime city state, not AI prediction.
- F574/F575 transfer-state findings: no ownership-copy omission is required; Stonehenge capture is
  merely one normal producer of the free-building source transition.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Rescale against the actual old and new integer flat hurry lengths, not the raw percentage modifier.
Conceptually:

    oldLength = flatHurryAngerLength();
    oldTimer = m_iHurryAngerTimer;
    m_iHurryAngerModifier += iChange;
    newLength = flatHurryAngerLength();
    if (oldTimer > 0)
        m_iHurryAngerTimer = ((oldTimer - 1) * newLength) / oldLength + 1;

Using `(timer - 1)` preserves the existing cycle-phase convention; using actual lengths preserves
stack count/phase across the discrete speed rounding that the current raw-modifier formula ignores.
Use a widened intermediate if desired even though current timer magnitudes are small. Do not alter
normal decrement cadence or `getHurryPercentAnger()`.

For the Turbo removal fixture:

    old timer=3, old length=2, new length=5
    repaired timer = ((3-1)*5)/2 + 1 = 6
    repaired stacks = ceil(6/5) = 2
    next stack drop = ((6-1)%5)+1 = 1 turn

which preserves both the two-stack count and the one-turn phase to the next drop.

REGRESSION TESTS
----------------
A. Turbo remove -50 source
- old length 2, timer 3, assert 2 stacks;
- remove Sacrificial-Altar source;
- assert resulting state still has 2 stacks and same relative next-drop phase.

B. Turbo add -50 source
- old length 5, timer 5, assert 1 stack;
- add source;
- assert no transient second authoritative stack is created by the conversion.

C. Exact-length controls
Repeat 0 <-> -50 on Normal (10 <-> 5) and Quick (6 <-> 3); repaired behavior should agree with the
current implementation where integer scaling is already exact.

D. Current Stonehenge/Aztec integration
- Aztec owns Stonehenge and a second city with only its free Sacrificial Altar;
- on Turbo create timer 3 in the second city through two normally spaced whips;
- capture the Stonehenge city with another player;
- assert free Altar disappears but the second city's preexisting two whip-anger layers are not
  collapsed to one by the source transition.

E. Epic/Nitro rounding controls
Exercise the shipped 15->7 and 3->1 length transitions to prevent reintroducing raw-percentage
rescaling.

CURRENT STONE AFTER C031-WIP369
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F579 / KI#902 are CONFIRMED.
F580 / provisional KI#903 is UNUSED.
Latest physical checkpoint: C031-WIP369.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F580 unused. First audit sibling duration/modifier rescalers and other persistent
happiness/health timers to determine whether F579 is isolated; then leave anger if clean. Resume
reveal/espionage visibility, religion/corporation state ordering, production-time state and remaining
low-coverage lifecycle functions. Require current caller/XML proof plus whole-album dedup before
promotion. Freeze WIP370 regardless.

END C031-WIP369


================================================================================
C031-WIP370 - Queue 006 CvCity.cpp - F580 UNUSED - reveal/visibility + low-coverage receiver controls
================================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F579/KI#902 remain CONFIRMED.
F580 / provisional KI#903 remains UNUSED.
Actual AdvCiv-SAS source remains UNTOUCHED.

PURPOSE
-------
Preserve the first post-F579 tranche without forcing a finding. The pass finished the
`setRevealed(false)` / espionage-visibility lifetime probe, rechecked several low-coverage
persistent-state helpers, and deliberately deduplicated one strong wrong-receiver rediscovery to
F433 rather than spending F580 twice.

REVEAL / ESPIONAGE VISIBILITY CONTROL
--------------------------------------
`CvPlot::setRevealed(eTeam, false, ...)` clears the city's revealed bit through
`CvCity::setRevealed(eTeam, false)`, but that city setter does not itself clear
`m_abEspionageVisibility[eTeam]`. The API is therefore asymmetric in isolation.

Current ordinary gameplay does not establish a live stale-visibility path from that asymmetry:

* Team reset / team-merge cleanup paths that can unreveal cities after ordinary play has begun
  explicitly clear both city reveal and city espionage visibility.
* The only ordinary generic plot-unreveal producer found outside those cleanup paths is Advanced
  Start visibility removal (`ADVANCEDSTARTACTION_VISIBILITY`, bAdd=false).
* Advanced Start actions are gated by `getAdvancedStartPoints() >= 0` and take place before the
  player exits Advanced Start. There is no ordinary Advanced Start action that creates the
  espionage-point relationship needed to satisfy the passive city-visibility mission threshold.
  Thus the current supported phase cannot normally combine “passive espionage visibility true”
  with the generic Advanced Start unreveal operation.

Classification: real setter/API robustness asymmetry, but no current shipped F580 producer proved.

LOW-COVERAGE PRODUCTION / STATE CONTROLS
----------------------------------------
1. `chooseProduction()` is a thin popup constructor after the occupation/automation guards. Its
   order-type/data pairing remains coherent; no independent queue mutation occurs there.

2. BULL hurry-overflow helpers were rechecked against the current AdvC overflow rewrite. Their
   retained/lost-production contract remains the already-audited WIP358/WIP349 family; no new
   current-live root emerged.

3. `getProductionExperience(NO_UNIT, true)` remains the authored advc.002f city-comparison score
   (generic XP intentionally receives extra weight). This is the already-frozen WIP349 control.

4. `getCulturePercentAnger()` remains explicit advc.099 policy. Eliminated-civ culture and
   closed-borders/wartime relation modifiers are intentional; no new arithmetic contradiction was
   found.

5. `updatePowerHealth()` correctly derives good/bad power health from the current power/dirty-power
   predicates and dirties assignment state when the result changes. The earlier clean-power yield-
   rank suspicion remains closed through the indirect team->player->city `updateCommerce()` chain.

SAS UNCERTAIN-COMMENT CHECK
---------------------------
The explicit caution comments in the SAS emergency production fallback were tested rather than
trusted:

* current era enumeration begins with `ERA_ANCIENT` at ordinal 0 and proceeds contiguously in the
  shipped XML, so the fallback cost cap `iMaxHammerPerEra * (iCurrentEra + 1)` has the ordinal
  meaning assumed by its comment;
* `kOwner.AI()` is the normal AdvC typed access to `CvPlayerAI` and is not a dubious cast workaround;
* the cached `ERA_RENAISSANCE` lookup is valid in current XML.

These comments are stale caution, not current defects.

F433 / KI#756 REDISCOVERY - DO NOT RENUMBER
--------------------------------------------
The receiver/sentinel sieve independently re-found a very strong-looking block in
`CvCity::processBuilding`:

    PlayerTypes eDirtyPlayer = NO_PLAYER;
    if (kBuilding.isNationalWonder() && isActiveOwned())
        eDirtyPlayer = getOwner();
    else if (kBuilding.isWorldWonder())
        eDirtyPlayer = kGame.getActivePlayer();
    if (eDirtyPlayer != NO_PLAYER)
    {
        gDLL->UI().setDirty(GlobeLayer_DIRTY_BIT, true);
        GET_PLAYER(getOwner()).setBonusHelpDirty();
    }

The computed cross-player target is ignored and the city owner is dirtied instead. This is NOT
F580: the whole album already owns the exact root as F433/KI#756, with the Pyramids/Stone resource-
layer cache reproducer and the minimal `GET_PLAYER(eDirtyPlayer).setBonusHelpDirty()` repair.

The rediscovery confirms the receiver sieve is still finding real defects, but the number is
protected by dedup discipline.

RELIGION / CORPORATION CONTROLS
-------------------------------
* `setHasReligion()` removes vote-source effects under the old religion state, flips the religion
  bit, then reapplies under the new state; maintenance/religion happiness/commerce are refreshed
  after the owner count changes. No mutation-order hole was established.
* `setHasCorporation()` updates the global headquarters (when present) and then the changed city.
  The previously suspicious competing-HQ replacement path remains ordinary-gameplay unreachable
  through executive spreading because competing headquarters are rejected as targets.
* `updateCorporationBonus()` remains generically fragile for multi-step corporation-produced-bonus
  chains, but current SAS has no produced-bonus -> produced-bonus chain: Cereal Mills produces Oil;
  Aluminum Co. produces Aluminum from Coal; no corporation that itself produces a bonus consumes a
  corporation-produced bonus. The generic closure concern therefore remains current-data masked.

CURRENT STONE AFTER C031-WIP370
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F579/KI#902 are CONFIRMED.
F580 / provisional KI#903 is UNUSED.
Latest physical checkpoint: C031-WIP370.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F580 unused with a fresh hard-state lens rather than revisiting the saturated
reveal/resource-help/anger families. Prioritize current-live owner/team cache invalidation,
building-state transitions, production lifecycle state and AdvC-added calculations whose comments
promise a bound or semantic equivalence. Require current caller/XML proof plus whole-album dedup
before promotion. Freeze WIP371 regardless.

END C031-WIP370


================================================================================
C031-WIP371 - F580 / KI#903: resource health sign-flip UI invalidation uses bad-health with wrong sign
================================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F580/KI#903 are CONFIRMED.
F581 / provisional KI#904 is UNUSED.
Actual AdvCiv-SAS source remains UNTOUCHED.

F580 / KI#903 - CONFIRMED CURRENT ADVC RESOURCE-STATUS INVALIDATION DEFECT
-------------------------------------------------------------------------
Title:
`CvCity::processBonus()` subtracts an already-negative bad-health/bad-happiness component when
predicting whether a resource change crosses the healthy/happy boundary, so current negative
building-resource health can skip the `CityInfo_DIRTY_BIT` refresh that advc.001w was specifically
added to provide after resource trades.

ROOT
----
Current `CvCity::processBonus()` accumulates resource health in the normal signed representation:

    iGoodValue >= 0
    iBadValue  <= 0

and runtime applies those values directly:

    changeBonusGoodHealth(iGoodValue * iChange);
    changeBonusBadHealth(iBadValue * iChange);

Therefore the actual health-rate delta represented by this block is:

    (iGoodValue + iBadValue) * iChange

Before applying it, however, advc.001w predicts whether the active-owned city crosses the
healthy/unhealthy boundary with:

    iHealth + (iGoodValue - iBadValue) * iChange

Since `iBadValue` is already negative, subtracting it reverses the sign of every bad component.
A pure -2 health resource effect is therefore predicted as +2.

The happiness half contains the same arithmetic shape:

    iHappy + (iGoodValue - iBadValue) * iChange

but current SAS XML has no negative `BonusHappinessChanges`, so the health branch supplies the
current shipped-game proof; the happiness expression is the same generic/future-proofing breadth.

CURRENT SAS XML PROOF
---------------------
Current `CIV4BonusInfos.xml` gives both Coal and Oil:

    iHealth = 0
    iHappiness = 0

Current `CIV4BuildingInfos.xml` contains exactly four negative building-resource health entries:

    BUILDING_FACTORY:
        BONUS_OIL  -> -2 health
        BONUS_COAL -> -2 health

    BUILDING_INDUSTRIAL_PARK:
        BONUS_OIL  -> -1 health
        BONUS_COAL -> -1 health

Thus negative `iBadValue` is not hypothetical mod-mod data; ordinary Factory/Industrial-Park cities
produce it in current SAS.

CLEAN CURRENT RUNTIME FIXTURE - FACTORY + COAL
----------------------------------------------
Take an active-human city that:

1. has an active Factory;
2. does not yet have Coal;
3. currently has `healthRate() == +1`;
4. then receives one connected Coal through an ordinary resource trade or any ordinary plot-group
   connection change.

Immediately before `processBonus(BONUS_COAL,+1)`:

    iGoodValue = 0
    iBadValue  = -2
    iHealth    = +1

The real city-state transition is:

    +1 + (0 + -2) = -1

so the city crosses healthy -> unhealthy.

The advc.001w pre-check instead evaluates:

    +1 + (0 - -2) = +3

and concludes that the sign has not changed. It therefore skips:

    gDLL->UI().setDirty(CityInfo_DIRTY_BIT, true);

The reverse disconnection is symmetric. Starting from `healthRate() == -1` and removing Coal
(`iChange=-1`), real state becomes +1 while the predictor evaluates -3 and again misses the
unhealthy -> healthy boundary.

ORDINARY RESOURCE-TRADE PRODUCER / MASKING TRACE
------------------------------------------------
The current annual resource-deal path is direct:

    CvDeal::startTrade(TRADE_RESOURCES)
      -> CvPlayer::changeBonusImport / changeBonusExport
      -> capital plot `updatePlotGroupBonus`
      -> CvPlotGroup::changeNumBonuses
      -> CvCity::changeNumBonuses
      -> CvCity::processBonus

`CvDeal::startTrade` performs no blanket `CityInfo_DIRTY_BIT` invalidation after the resource case.
`CvPlot.cpp` and `CvPlotGroup.cpp` contain no `CityInfo_DIRTY_BIT` setter. The resource plot-group
path therefore does not supply a later independent global refresh that would make the advc.001w
pre-check irrelevant.

The `changeBonusGoodHealth` / `changeBonusBadHealth` mutators do call `setInfoDirty(true)` for the
active team, and that local city dirty state predates advc.001w. It does not invalidate this finding:
practical 1387 deliberately added the separate `CityInfo_DIRTY_BIT` sign-boundary check precisely
because the pre-existing local dirtying did not reliably refresh the healthy/happy city status after
resource trades. F580 is the arithmetic bug inside that dedicated reliability fix.

ANCESTRY / INTENT
-----------------
The signed pre-check was introduced by AdvC practical commit 1387,
`40f76ddd83320144401c2d172b8315da3f0b3ef4`, titled `Misc. tweaks and bugfixes`, under tag
`advc.001w`.

Its commit message explicitly says:

    Happy and healthy status on city billboards is (reliably) updated after the players signs a
    resource trade. [advc.001w]

Supplied K-Mod 1.46 and Civ4CE BtS have no corresponding pre-check in `processBonus`; they simply
apply the signed good and bad values. AdvC 1.14 and current SAS retain the exact
`iGoodValue - iBadValue` predictor. The defect is therefore an AdvC bug introduced inside a UI
reliability fix, not inherited Firaxis/K-Mod arithmetic.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album searches for `processBonus`, `advc.001w`, `CityInfo_DIRTY_BIT`, resource-health
boundary invalidation, Factory/Coal resource-health UI and the signed expression found no prior
numbered owner.

Distinct from:

- the numerous bonus-trade AI valuation findings: those affect deal legality/value, not city status
  invalidation after a resource actually changes;
- F433/KI#756: wrong-player `setBonusHelpDirty()` after world-wonder resource-help changes; that is
  the Resource globe/help cache and a wrong receiver, while F580 is the city healthy/unhealthy
  status dirty bit and wrong signed arithmetic;
- F397 and related building-preview findings: hypothetical building health/happiness valuation, not
  authoritative resource-state mutation/UI invalidation;
- F563/F564/F566/F567 and other Queue-006 production/state roots: no production validation or city
  acquisition is involved here.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Use the same signed net delta for the pre-check that runtime is about to apply:

    if ((iHealth < 0) !=
        (iHealth + (iGoodValue + iBadValue) * iChange < 0))
    {
        gDLL->UI().setDirty(CityInfo_DIRTY_BIT, true);
    }

and analogously for happiness:

    iHappy + (iGoodValue + iBadValue) * iChange

This is the smallest behavior-preserving repair and exactly matches the signed storage convention
already consumed by `changeBonusGood*` / `changeBonusBad*`. Do not take absolute values or change
the good/bad accumulators themselves.

REGRESSION TESTS
----------------
A. Factory + Coal connect
- active-human Factory city, no Coal, `healthRate()==+1`;
- connect/import Coal;
- assert final `healthRate()==-1` and `CityInfo_DIRTY_BIT` is requested.

B. Factory + Coal disconnect
- same city with Coal and `healthRate()==-1`;
- disconnect/cancel Coal;
- assert final `healthRate()==+1` and dirty bit is requested.

C. Industrial Park one-point control
- use `iBadValue=-1` and a city immediately on either side of zero;
- verify both add/remove sign crossings dirty correctly.

D. Ordinary positive health control
- resource/building combination with positive BonusHealthChange;
- fixed expression should agree with the old expression when `iBadValue==0`.

E. Mixed good + bad robustness
- synthetic or future XML with both signs for one bonus;
- assert the predictor equals the actual signed net delta rather than sum of magnitudes.

F. Happiness breadth
- synthetic negative `BonusHappinessChanges` fixture;
- assert the analogous sign crossing uses `good + bad`. Current SAS has no negative producer, so
  this is defense-in-depth rather than the current proof.

CURRENT STONE AFTER C031-WIP371
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F580/KI#903 are CONFIRMED.
F581 / provisional KI#904 is UNUSED.
Latest physical checkpoint: C031-WIP371.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F581 unused. Leave the resource-status/advc.001w branch unless a distinct contract
appears. Resume low-coverage persistent-state and lifecycle auditing, prioritizing production-time
state, religion/corporation/vote-source transitions, owner/team cache invalidation, and AdvC
historical bugfixes whose repair may have left a validator/helper sibling inconsistent. Require
current caller/XML proof plus whole-album dedup before promotion. Freeze WIP372 regardless.

END C031-WIP371


===============================================================================
C031-WIP372 - QUEUE 006 CVCITY.CPP CONTINUATION - F581 CONFIRMED
===============================================================================

F581 / KI#904 - PROSPECTIVE-OWNER CITY MAINTENANCE FORGETS TO COUNT THE CITY
ITSELF IN NUMBER-OF-CITIES / COLONY STATE
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvCiv prospective-maintenance / AI-valuation defect.
Current SAS data and ordinary city-trade / UWAI conquest callers are live.

ROOT
----
AdvCiv added an optional `PlayerTypes eOwner` to the member maintenance helpers and documents the
parameter in `CvCity.h` as:

    Added an optional parameter to allow the computation of
    projected maintenance for cities yet to be conquered.

The member wrappers then evaluate the foreign city under the requested prospective owner:

    int CvCity::calculateNumCitiesMaintenanceTimes100(PlayerTypes eOwner) const
    {
        return calculateNumCitiesMaintenanceTimes100(getPlot(),
                eOwner == NO_PLAYER ? getOwner() : eOwner,
                getPopulation());
    }

    int CvCity::calculateColonyMaintenanceTimes100(PlayerTypes eOwner) const
    {
        return calculateColonyMaintenanceTimes100(getPlot(),
                eOwner == NO_PLAYER ? getOwner() : eOwner,
                getPopulation());
    }

However the static helpers they delegate to explicitly have an `iExtraCities` parameter for
hypothetical additions, defaulting to 0:

    static int calculateNumCitiesMaintenanceTimes100(...,
            int iPopulation = -1, int iExtraCities = 0);
    static int calculateColonyMaintenanceTimes100(...,
            int iPopulation = -1, int iExtraCities = 0);

and use it in exactly the state that differs before vs. after ownership transfer:

    int iMaintenanceCities = kOwner.getNumCities() + iExtraCities + ...;

and:

    int iNumCities =
        (kCityArea.getCitiesPerPlayer(eOwner) - 1 + iExtraCities) *
        iNumCitiesPercent;

The member wrappers never pass `1` when `eOwner != getOwner()`. They therefore ask the prospective
owner's CURRENT city and area-city counts to stand in for the POST-ACQUISITION counts while the
foreign candidate city is not yet included in either count.

The correct ownership-state invariant is simple:

- current owner: the city is already included -> `iExtraCities = 0`;
- different prospective owner: the city is not yet included -> `iExtraCities = 1`.

NUMBER-OF-CITIES MAINTENANCE - CLEAN SAME-AREA FAILURE
------------------------------------------------------
Take an ordinary trade/acquisition of a foreign city located in the prospective recipient's capital
area. Colony maintenance is therefore 0 both before and after transfer, so no colony arithmetic can
mask the count error.

Let the prospective recipient currently own N cities.

Before transfer, the buggy prospective wrapper computes the candidate city's number-of-cities
maintenance from:

    N + 0

because `GET_PLAYER(eOwner).getNumCities()` does not include the still-foreign candidate.

Immediately after transfer, authoritative runtime maintenance on that same city computes from:

    N + 1

because the city now belongs to eOwner and is included in `getNumCities()`.

Thus the city's own post-acquisition number-of-cities maintenance is systematically understated by
one full city-count unit in the prospective evaluation whenever the cap has not saturated.

This is not supplied by the heuristic directly below the call in `AI_assetVal`:

    // Estimate incr. civic upkeep and incr. maint. in other cities
    rMaintCost += ... * getNumCities();

The comment explicitly identifies that term as the incremental cost in OTHER cities (and civic
upkeep). It scales with the recipient's existing city count; it does not make the candidate city's
own maintenance helper use the post-acquisition N+1 count.

COLONY MAINTENANCE - FIRST-OVERSEAS-CITY SIGNATURE
--------------------------------------------------
The colony formula makes the hidden state assumption even more obvious.

Suppose the prospective owner has its capital on another land area and currently has zero cities in
the candidate city's area.

Prospective wrapper before acquisition:

    kCityArea.getCitiesPerPlayer(eOwner) = 0
    iExtraCities = 0
    -> (0 - 1 + 0) * iNumCitiesPercent
    -> a negative value, then SQR(...)
    -> positive projected colony maintenance (subject to its positive cap)

Actual runtime immediately after acquiring that first city in the area:

    kCityArea.getCitiesPerPlayer(eOwner) = 1
    iExtraCities = 0
    -> (1 - 1 + 0) = 0
    -> exactly 0 colony maintenance

So the projection can invent positive colony maintenance for the recipient's FIRST city in an
overseas area, even though the post-transfer runtime formula correctly charges zero. If the
recipient already has one or more cities in that area, the same missing +1 instead evaluates one
fewer "other colony city" than the actual post-transfer state.

CURRENT ORDINARY AI CALLER - CITY TRADE
---------------------------------------
`CvPlayerAI::AI_cityTradeVal` explicitly documents its acquisition case:

    If eToPlayer is this player: How much this player values acquiring kCity
    from its current owner.

For that case, the prospective recipient evaluates the still-foreign city through:

    scaled r = AI_assetVal(kCity, false);

`AI_assetVal` determines:

    bool const bOwn = (c.getOwner() == getID());

so `bOwn` is false for the acquisition case, and later subtracts:

    scaled rMaintCost = per100(c.calculateBaseMaintenanceTimes100(getID()));

`calculateBaseMaintenanceTimes100(getID())` delegates to the three owner-parameter maintenance
members above. Distance maintenance can legitimately use the prospective owner's current
capital/government-center set. Number-of-cities and colony maintenance cannot: those two require the
candidate city to be added to the prospective owner's count state first.

The erroneous result therefore directly changes AI city-trade acquisition values and, consequently,
trade price / willingness calculations.

CURRENT ORDINARY AI CALLER - UWAI CONQUEST ASSET SCORE
-------------------------------------------------------
The same helper is not limited to peaceful city trades.

`UWAICache::rebuildCities()` constructs cache-city entries for known teams, and
`UWAICache::City` calls:

    updateAssetScore(eCacheOwner);

which stores:

    m_iAssetScore =
        (GET_PLAYER(eCacheOwner).AI_assetVal(city().AI(), true) / 2).round();

For a foreign target city, `eCacheOwner` is the prospective conqueror while the city is still owned
by its current player. The same `calculateBaseMaintenanceTimes100(getID())` call therefore uses the
missing-city count state in conquest asset valuation too. This can feed UWAI target/war valuation,
not merely diplomacy.

CURRENT DATA / NONZERO REACHABILITY
-----------------------------------
The missing number-of-cities term is live under ordinary SAS settings. For example Standard world
size currently has:

    iNumCitiesMaintenancePercent = 30

and Noble has:

    iNumCitiesMaintenancePercent = 75

with ordinary positive population, crowdedness and player modifiers. Before the maintenance cap
saturates, adding one city therefore contributes a positive amount to the candidate city's own
number-of-cities maintenance. No special XML or disabled option is needed.

Standard also has positive colony-maintenance scaling (`iColonyMaintenancePercent = 45`), and Noble
has `iColonyMaintenancePercent = 100`; a sufficiently nonzero maintenance-distance cap therefore
makes the first-overseas-city `(0-1)^2` witness positive before acquisition while authoritative
post-acquisition colony maintenance is 0.

ANCESTRY
--------
The mismatch is old AdvCiv behavior rather than SAS code.

In practical 1253 / commit:

    f6ff6a182f5187541fff6e5bbbe12f9573b3778c
    "Initial commit (cont.)"

AdvC already introduces the member `eOwner` parameters with the explicit header comment about
"projected maintenance for cities yet to be conquered." The new static number-of-cities helper
already has `extraCities`, but the member wrapper passes only owner + population, leaving the
default at 0. The colony projection in that snapshot directly uses:

    (cityArea->getCitiesPerPlayer(owner) - 1)

which has the same foreign-owner count error.

Practical 1386 / commit:

    dc9c167dfc9ea3567899c58e275a2d05484d25ab
    "Refactoring and a couple of minor corrections in AdvCiv code"

adds `iExtraCities` to the static colony helper and rewrites the formula to:

    getCitiesPerPlayer(eOwner) - 1 + iExtraCities

but the prospective-owner member wrapper still does not pass 1. The refactor therefore creates the
exact repair mechanism without connecting it to the older conquered-city API.

Practical 2053 / commit:

    fd5d755af022962c4e3e09495e052fbc80bf2457
    "AI code for city trade evaluation"

introduces the current `AI_assetVal` city-trade maintenance subtraction by explicitly summing the
three `...Times100(getID())` prospective-owner members.

Practical 2463 / commit:

    fd00cf5197afe3d1c519d62ffb288ea11955e968
    "Fix maintenance calc in raze-city AI code (AdvCiv bug)"

consolidates those calls into `calculateBaseMaintenanceTimes100(getID())` while preserving the
same prospective-owner count semantics. Base AdvCiv 1.14 and current SAS retain the omission.

DEDUP
-----
Whole-album searches for the member/static maintenance helpers, `iExtraCities`, projected/
prospective maintenance, `AI_assetVal`, city-trade maintenance and colony maintenance found no prior
numbered root owning this foreign-owner count mismatch.

Distinct from:

- F392: Settler founding-cost hover includes normal maintenance for WLTKD cities; that preview DOES
  use the static helpers with `iExtraCities=1` for founding and fails for a different no-maintenance
  state reason.
- F328: AI civic valuation cannot invert already-saturated -100% maintenance modifiers.
- F465 and nearby Queue-004 roots: vassal/team maintenance invalidation after player/team lifecycle
  changes, not prospective city acquisition arithmetic.
- F567: runtime colony-maintenance CAP double-applies MAX_DISTANCE_CITY_MAINTENANCE.
- F329: SAS prospective-city World-Wonder activity checks the current owner's obsolescence team;
  same broad city-acquisition domain, entirely different state/input.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Normalize the requested owner once and pass an extra-city count exactly when the member is being
used for a different prospective owner:

    PlayerTypes eCalcOwner = (eOwner == NO_PLAYER ? getOwner() : eOwner);
    int iExtraCities = (eCalcOwner == getOwner() ? 0 : 1);

    return calculateNumCitiesMaintenanceTimes100(
            getPlot(), eCalcOwner, getPopulation(), iExtraCities);

and analogously:

    return calculateColonyMaintenanceTimes100(
            getPlot(), eCalcOwner, getPopulation(), iExtraCities);

Distance maintenance needs no city-count adjustment. Current-owner calls remain byte-for-byte
semantically unchanged (`iExtraCities=0`). The existing `calculateBaseMaintenanceTimes100(eOwner)`
then inherits the corrected prospective behavior automatically.

REGRESSION TESTS
----------------
A. Same-area acquisition / number-of-cities
- recipient has N cities, candidate is foreign and in recipient capital area;
- compare pre-transfer `candidate.calculateNumCitiesMaintenanceTimes100(recipient)` with the same
  city's actual value immediately after transfer;
- corrected prospective result must equal post-transfer result (before unrelated state changes).

B. First overseas city / colony
- recipient capital on area A, no recipient city on candidate area B;
- before transfer prospective colony maintenance must be 0;
- after transfer actual colony maintenance must also be 0.

C. Existing overseas foothold
- recipient has M>=1 cities on candidate area B;
- corrected prospective result must use M existing OTHER cities, matching post-transfer area count
  M+1 minus the candidate itself.

D. Current-owner control
- call all three member helpers with NO_PLAYER and explicit current owner;
- results must be unchanged by the repair.

E. City-trade AI caller
- instrument `AI_assetVal(c,false)` for a foreign tradable city;
- verify its candidate-city maintenance component equals maintenance immediately after a simulated/
  sandboxed transfer state rather than the pre-transfer N-city state.

F. UWAI foreign-target caller
- build a foreign `UWAICache::City` for an observer/conqueror and verify its maintenance input uses
  the observer's N+1 / area+1 hypothetical counts.

CURRENT STONE AFTER C031-WIP372
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F581/KI#904 are CONFIRMED.
F582 / provisional KI#905 is UNUSED.
Latest physical checkpoint: C031-WIP372.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F582 unused. Leave the prospective-maintenance helper family unless a mechanically
distinct root appears. Resume low-coverage lifecycle/persistent-state auditing, prioritizing
production-time state, religion/corporation/vote-source transitions, owner/team invalidation and
bounded/signed arithmetic. Require current caller/XML proof plus whole-album dedup before promotion.
Freeze WIP373 regardless.

END C031-WIP372


===============================================================================
C031-WIP373 - QUEUE 006 CVCITY.CPP / F582 STILL UNUSED
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F581 / KI#904 remain CONFIRMED.
F582 / provisional KI#905 remains UNUSED.

Authoritative current source is the user-supplied
`AdvCiv-SAS_light_source_20260905T174936.zip`, whose reconciled album ends at WIP372.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS
archives plus the local reachable-history commit-diff cache. The user-supplied durable
full-thread carryover was used to preserve the post-WIP372 analysis that had not yet been
frozen into the album. No repository source files were modified.

POST-WIP372 HISTORICAL-REPAIR CONTROLS PRESERVED FROM THE THREAD CARRYOVER
--------------------------------------------------------------------------
1. Practical 1784 - spurious "can no longer work on" popups / production validation.
   The current narrow `verifyProduction` path is coherent with the repair's purpose.
   Plot-group/resource recalculation can occur in the middle of a human turn, when full
   production legality contains conditions that should not be treated as durable order
   invalidation. The stronger checks still occur at normal production-selection/turn
   boundaries. No surviving independent stale-order root was established here.

2. Practical 2071 - unit bumping after city acquisition.
   The acquisition code suppresses premature bumping during the temporary no-city interval,
   then the conquest path reaches the later valid-plot verification after the replacement
   city/occupation state exists. No current normal transfer/conquest path was found that
   escapes the intended later bump.

3. Practical 4091 - overflow limit and the completed order's production modifier.
   Current `popOrder`/`handleOverflow` ordering retains the repair: overflow is handled while
   the just-finished order and its production modifier are still authoritative, rather than
   after the next queued order becomes current. This family is already dense in prior album
   overflow findings and did not yield a distinct F582 root.

4. Practical 2794 - corporation-founded announcement.
   The current implementation correctly relies on `setListHelp` mutating its first-item flag
   by reference. No announcement ordering/state defect survived.

5. Practical 2988 - occupation decrement probability.
   The current code clamps/tests the computed decrement probability `r`, not the separate
   revolt probability. The old regression remains repaired.

6. Practical 2530 / old-save Great Person random-range recovery.
   If malformed old-save GP progress is large enough to exceed the CvRandom range, AdvC scales
   the total progress and each per-unit bucket separately with ceiling. The sum of separately
   rounded buckets can theoretically exceed the separately rounded total and perturb the final
   weighting slightly. The code explicitly treats this as recovery for very old malformed
   save state, and ordinary current GP thresholds do not approach that regime. Preserve as
   old-save robustness debt only; not F582.

HYPOTHETICAL-OWNER / PROSPECTIVE-STATE API CENSUS
--------------------------------------------------
F581 exposed a mechanical wrapper assumption: a still-foreign city was evaluated with a
prospective owner's statistics without adding the candidate city itself. The next pass therefore
checked the other explicit CvCity APIs that accept or infer a hypothetical owner rather than
assuming the same root repeats elsewhere.

1. `getNumActiveWorldWonders(iStopCountAt, eOwner)` - POSITIVE CONTROL.
   When `eOwner` is supplied, the function intentionally changes the observer/owner team used for
   wonder obsolescence while still iterating the actual buildings present in this city. This is the
   correct prospective-obsolescence helper. The album already contains F329, where a caller used
   the non-prospective `hasActiveWorldWonder()` instead of this available API. No new defect exists
   in the helper itself.

2. `isConnectedToCapital(ePlayer)` - CLOSED.
   CvCity delegates directly to `CvPlot::isConnectedToCapital(ePlayer)`. With an explicit player,
   the plot helper looks up that player's actual capital and tests connectivity in the requested
   player's network. Current foreign-player callers use this as a network/connectivity query (for
   example cross-player trade/resource logic), not as an assertion that the city has already been
   transferred. No missing prospective +1/ownership-state analogue to F581 was established.

3. `isAutoRaze(eConqueror)` - CLOSED.
   The conqueror-specific policy input is One-City-Challenge ownership, and the function resolves
   that from the explicitly assumed owner. The remaining conditions are game/city state (No City
   Razing option, elimination rules, highest population) rather than statistics that implicitly
   require inserting the candidate city into the conqueror's empire. Current prospective callers
   therefore match the helper contract.

LOW-COVERAGE STATE / MUTATOR CONTROLS IN THIS SLICE
---------------------------------------------------
1. The tempting building-event happiness/health setter asymmetry is a clean F566/KI#889 RE-HIT,
   not F582. `setBuildingYieldChange` / `setBuildingCommerceChange` guard an unmapped
   `NO_BUILDING`, while the corresponding happiness/health path can reach the missing mapping.
   F566 already owns this exact building-class/event mapping root with a current event/capture
   witness; do not duplicate it.

2. `updateExtraBuildingHappiness()` and `updateExtraBuildingHealth()` recompute their city caches
   from active civilization-mapped buildings and the owner's per-building modifiers. The ordinary
   acquisition/event building representation remains class-mapped to the current civilization, as
   already controlled in WIP362. No foreign/unmapped live BuildingTypes instance was proved here.

3. City rank recalculation (`findPopulationRank`, base/yield/commerce rank helpers) was rechecked
   only as a receiver/invalidation control. The actual mutation paths still reach the corresponding
   owner-wide invalidators; this remains the WIP365/WIP366 conclusion rather than a new root.

4. `cultureGarrison(ePlayer)` has a stale source comment saying the parameter is no longer used,
   but the implementation deliberately uses it to exclude the prospective revolt/cultural owner's
   own team from the suppressing garrison. Current `revoltProbability` supplies the calculated
   cultural owner. The comment is wrong; the behavior is coherent.

5. `failGoldPercent()` still encodes `(ProcessTypes)0` as Wealth. Current SAS ProcessInfo ordering is
   `PROCESS_WEALTH`, `PROCESS_RESEARCH`, `PROCESS_CULTURE`, so the assumption remains live-correct.
   This is the already-recorded mod-mod/process-reordering robustness seam, not a current defect.

6. Practical 4336's air-capacity crash repair remains present in current `popOrder`: the result of
   `jumpToNearestValidPlot()` is used directly to determine whether the over-capacity produced air
   unit died, and the dead-unit branch exits before later dereferences/event reporting. The newer
   SASGameRecord completion row is intentionally emitted before that relocation/destruction and
   does not undo the lifetime guard.

CLASSIFICATION
--------------
No independent current-live F582 root survived this tranche. The prospective-state sweep produced
three useful positive/negative controls, the most concrete building-state asymmetry was an exact
F566 re-hit, and the historical repairs sampled above remain present or are current-data masked.
F582 / KI#905 therefore remains deliberately UNUSED.

CURRENT STONE AFTER C031-WIP373
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F581/KI#904 are CONFIRMED.
F582 / provisional KI#905 is UNUSED.
Latest physical checkpoint: C031-WIP373.
Actual AdvCiv-SAS repository source remains UNTOUCHED.

NEXT
----
Continue under F582 unused. Leave the now-closed explicit prospective-owner trio and the saturated
building-event mapping family behind. Resume the low-coverage authoritative-state sieve with a
fresh emphasis on production failure/interruption state, religion/corporation/vote-source mutation
ordering, owner/team-wide invalidation, and setter/assert or signed/bounded-state mismatches. Use
reachable-history bugfix siblings and the supplied K-Mod/Civ4CE sources as oracles, require an
ordinary current caller/XML fixture where applicable, deduplicate whole-album before promotion, and
freeze WIP374 regardless.

END C031-WIP373


================================================================================
C031-WIP374 CONTINUATION - F582 UNUSED; RECEIVER/SETTER/PRODUCTION-HYPOTHESIS CONTROLS
================================================================================

CONTINUATION ANCHOR
-------------------
Continued from the exact C031-WIP373 stone with F582 / provisional KI#905 UNUSED and
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN. This slice deliberately used a mechanical
parameter-vs-current-owner/team sieve, setter/assert invariant scan, and low-coverage
production/building-preview pass. Source remains untouched.

PARAMETER-VS-CURRENT-OWNER / TEAM SIEVE
----------------------------------------
F581's prospective-owner maintenance bug justified checking every low-coverage CvCity helper
where an explicit PlayerTypes/TeamTypes parameter coexists with `getOwner()` / `getTeam()`.
The strongest-looking cases in this slice close as follows.

1. `getCultureLevel(ePlayer)` - CLOSED.
   The actual owner is used only to select the city's cached culture level. For another player,
   the function computes the level from that player's culture rather than reusing the owner cache.
   This is the intended fast-path split, not an F581-style prospective-owner leak.

2. `calculateTradeTotals(..., eWithPlayer)` - CLOSED.
   `eWithPlayer` is a route-partner filter. Whether a route is domestic or foreign is correctly
   classified relative to this city's actual owner. The two concepts are intentionally different.

3. `canCultureFlip(eToPlayer, ...)` and `isMartialLaw(eRevoltPlayer)` - CLOSED.
   The explicit player parameter identifies the cultural/revolt side while defending ownership,
   war and garrison state must remain relative to the city's actual current owner/team. No hidden
   prospective transfer is implied by these APIs.

4. `doPlotCultureTimes100(..., ePlayer, ...)` - CLOSED against advc.025 semantics.
   Its capitulated-vassal reduction deliberately compares the CITY OWNER with the tile owner;
   `ePlayer` can be a different culture recipient/source in trade-culture paths. The AdvC rule is
   explicitly about culture spread BY a city owned by a capitulated vassal onto its master's tile.
   Current SAS changes the percentage but not that ownership contract.

WORLD-WONDER OBSERVER `GET_TEAM(kObs.getID())` - EXACT FALSE-POSITIVE RE-HIT
----------------------------------------------------------------------------
`setNumRealBuildingTimed()` contains the superficially alarming sequence:

    CvPlayer& kObs = *it;
    ...
    GET_TEAM(kObs.getID()).isHasMet(getTeam())

This is NOT a player-ID-as-team-ID defect. It is the exact WIP340 control: because `kObs.getID()`
has type `PlayerTypes` and `CoreAI.h` supplies the overloaded `getTeam(PlayerTypes)`, overload
resolution converts through `TEAMID(ePlayer)`. Preserve as a useful type-system positive control;
do not assign F582.

`defensiveGarrison()` ENUM-AS-BOOL - LATENT RE-HIT ONLY
-------------------------------------------------------
The capital fallback still passes `pCapital->allUpgradesAvailable(...)` directly as a boolean,
where the local-city branch correctly compares the returned UnitTypes against `NO_UNIT`. Since
`NO_UNIT == -1`, that generic expression is semantically unsafe and unit type ID 0 is false.
However this is already preserved in WIP347/WIP360, and current SAS ships the associated
`DEMAND_BETTER_PROTECTION` feature disabled with its obsolete multiplier neutral. No current-live
F582 manifestation is available under shipped configuration.

`setCultureUpdateTimer()` WRONG ASSERTION - INHERITED DEBUG-CONTRACT RE-HIT
---------------------------------------------------------------------------
The setter writes `m_iCultureUpdateTimer` but asserts `getOccupationTimer() >= 0` instead of its
own timer. This is a real copy/paste assertion bug, but it is already an album control. Native
writers initialize/reset to 0, add positive espionage/event revolt durations, and decrement only
while the culture-update timer is positive. The Python/API changer could manufacture negative
state, but no ordinary shipped native path does. Keep as API/debug robustness debt, not F582.

MECHANICAL SETTER/ASSERT CENSUS
-------------------------------
A fresh scan of CvCity setters/changers containing `FAssert` found the culture-update timer as the
only obvious scalar-field/getter mismatch in this pass. Other superficially mixed results were
comments/disabled BtS code or deliberate aggregate checks (for example power count + dirty-power
count). No second current-live setter-contract root emerged.

`failProduction()` MULTI-INSTANCE WORLD-WONDER GENERALIZATION - LATENT ONLY
---------------------------------------------------------------------------
The advc.123f failure path contains a genuine generalized-loop inconsistency. Its comment says that
if a mod-mod permits (for example) two global instances of a world wonder and two competing
instances are being built in parallel, completing one rival instance should abort only ONE of the
competing instances (the lower-investment one). But `pMinProductionCity` is declared/reset inside
the per-city loop, so the implementation would select and fail one matching order in EACH city.

That is real generic/mod-mod robustness debt. It does not qualify as current SAS F582 because the
current `CIV4BuildingClassInfos.xml` world-wonder classes all use `iMaxGlobalInstances = 1`.
At the only shipped global limit, once a rival wonder is maxed every competing instance really does
need cancellation, so the broader loop defect has no current-data witness.

CORPORATION-HQ ADDITIONAL-COMMERCE PREVIEW - WRONG HYPOTHETICAL RESULT, UNREACHABLE HERE
------------------------------------------------------------------------------------------
All seven current corporation headquarters buildings simultaneously set `FoundsCorporation` and
`GlobalCorporationCommerce`. `getAdditionalBaseCommerceRateByBuildingImpl()` values the prospective
HQ global commerce using `countCorporationLevels(eCorp)` BEFORE construction. Before founding that
count is zero, while `processBuilding()` -> `setHeadquarters()` -> `CvGame::setHeadquarters()`
immediately calls `setHasCorporation(eCorp, true, ...)` on the founding city; the real post-build
HQ therefore has at least one corporation level. In isolation, the helper underestimates the
founding building's resulting headquarters commerce by one city's HQ-commerce contribution.

The reachability census prevents promotion: the additional-commerce-by-building helpers have no
current C++ gameplay/AI caller outside their own wrapper chain and are not exposed through CyCity /
Python in this source tree. Therefore this is an unused preview-helper correctness/API seam, not an
ordinary shipped gameplay F582.

LOW-COVERAGE SUPPORTING CONTROLS
--------------------------------
1. `findBaseYieldRateRank()` and `findCommerceRateRank()` themselves are coherent: when a rank is
   invalid they sort every owned city and repopulate every city's cache. Mutation-side invalidation
   remains the already-audited owner-wide cache question, not a defect in these rank builders.

2. `updateFreshWaterHealth()` recomputes signed good/bad fresh-water health, dirties AI citizen
   assignment, and refreshes active-team city info on actual change. Earlier river-state auditing
   already established the relevant map-state update hooks. No new stale cache emerged.

3. `initConscriptedUnit()` uses the already-selected current conscript unit, assigns a suitable
   UnitAI when available, applies production experience, exhausts moves, and reports the unit built.
   This remains consistent with the prior conscription-selection control.

4. The production-time setters keep the stored decay timers nonnegative; the ordinary turn logic
   increments parked positive production and resets/clears through the existing production
   lifecycle. No new decay-state root beyond the already-owned preview/timing family was found.

CLASSIFICATION
--------------
No independent ordinary current-SAS F582 root survives this tranche. It produced several valuable
controls with distinct failure reasons: type-safe overload conversion; disabled feature data;
native-writer masking; current XML max-instance masking; and unreachable/unexported preview code.
F582 / provisional KI#905 therefore remains deliberately UNUSED.

CURRENT STONE AFTER C031-WIP374
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F581/KI#904 are CONFIRMED.
F582 / provisional KI#905 is UNUSED.
Latest physical checkpoint: C031-WIP374.
Actual AdvCiv-SAS repository source remains UNTOUCHED.

NEXT
----
Continue under F582 unused. Leave the parameter-vs-owner sieve, observer overload false positive,
current-disabled protection branch, culture-timer assertion, multi-instance failProduction mod-mod
case and unreachable HQ preview behind. Resume with the remaining zero/one-mention authoritative
state functions and historically fragile city bugfix siblings, prioritizing ordinary current-data
callers, production state, corporation/religion/vote-source transitions, rank/cache invalidation,
and arithmetic where a stored signed value is interpreted as a magnitude. Compare supplied AdvC,
K-Mod and Civ4CE where diagnostic; whole-album deduplicate before promotion; freeze WIP375 regardless.

END C031-WIP374


===============================================================================
C031-WIP375 - F582 / KI#905: PEACEFUL GOVERNMENT-CENTER TRANSFER IS OMITTED
FROM PROSPECTIVE DISTANCE MAINTENANCE / AI CITY-TRADE ACQUISITION VALUE
===============================================================================

CONTINUATION ANCHOR
-------------------
Continued from the exact C031-WIP374 stone with F582 / provisional KI#905 UNUSED and
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN. This slice first used Civ4CE as a historical
bugfix oracle, then returned to F581's prospective-owner maintenance family only when a
mechanically distinct distance-maintenance state omission surfaced. Actual AdvCiv-SAS
repository source remains untouched.

CIV4CE EVENT-SETTER ORACLE CONTROL - DO NOT BLINDLY ASSUME CE IS THE FIXED SIDE
-------------------------------------------------------------------------------
Civ4CE's BtS changelog contains a line saying that city Healthy/Unhealthy bonus display from
EVENTS was fixed. Its actual BtS `CvCity::setBuildingHappyChange` / `setBuildingHealthChange`
bodies, however, still contain the old BtS sign/receiver mistakes that K-Mod explicitly replaces.
Examples in the supplied CE source include removing an already-negative bad-happiness value by
passing that negative value straight back into `changeBuildingBadHappiness`, and the first-insert
health branch calling happiness mutators at all. K-Mod labels the corresponding block as original
BtS code and replaces it with the Unofficial Patch sign-correct version.

Current AdvCiv 1.14 / SAS does NOT retain those CE setter mistakes. Its live event-building family
issue is the already-owned F566/KI#889 `NO_BUILDING` class-mapping guard mismatch. Therefore this
comparison is preserved as an oracle-quality control only: Civ4CE remains useful for historical
cross-checking, but a CE changelog claim cannot be treated as proof that every corresponding CE
body is the repaired implementation.

-------------------------------------------------------------------------------
F582 / KI#905 - CONFIRMED CURRENT ADVC CITY-TRADE VALUATION DEFECT
-------------------------------------------------------------------------------

TITLE
-----
`calculateDistanceMaintenanceTimes100(eOwner)` searches only the prospective owner's already-owned
cities for government centers, so AI valuation of a peaceful foreign-city acquisition cannot see a
government-center building that will survive the transfer. Current SAS's Forbidden Palace gives a
clean live case: pre-transfer AI valuation charges positive distance maintenance to the candidate,
while immediately after transfer the retained Forbidden Palace makes that city's distance
maintenance exactly zero.

ROOT
----
AdvC exposes prospective-owner maintenance through:

    int CvCity::calculateDistanceMaintenanceTimes100(PlayerTypes eOwner) const
    {
        return CvCity::calculateDistanceMaintenanceTimes100(getPlot(),
                eOwner == NO_PLAYER ? getOwner() : eOwner, getPopulation());
    }

The static helper ultimately calls:

    calculateMaintenanceDistance(&kCityPlot, eOwner)

which does:

    FOR_EACH_CITY(pLoopCity, GET_PLAYER(eOwner))
    {
        int iDist = plotDistance(pCityPlot, pLoopCity->plot());
        iLongestDist = std::max(iLongestDist, iDist);
        if (pLoopCity->isGovernmentCenter())
            iShortestGovernment = std::min(iShortestGovernment, iDist);
    }
    return min(min(iLongestDist, iShortestGovernment), cap);

For a foreign candidate city, `GET_PLAYER(eOwner)` necessarily excludes the candidate itself.
That is normally fine if the candidate will remain an ordinary city. It is not fine when peaceful
transfer preserves a building that makes the candidate a government center for eOwner.

The current city-trade acquisition path uses exactly this hypothetical state. In
`CvPlayerAI::AI_cityTradeVal`, case 1 is documented as:

    How much this player values acquiring kCity from its current owner.

and calls:

    scaled r = AI_assetVal(kCity, false);

For a foreign candidate, `AI_assetVal` later subtracts:

    scaled rMaintCost = per100(c.calculateBaseMaintenanceTimes100(getID()));

`calculateBaseMaintenanceTimes100` includes the prospective distance-maintenance wrapper above.
No parameter tells that wrapper that the foreign candidate itself may become a government center
as part of the same peaceful transfer.

CURRENT SAS DATA - FORBIDDEN PALACE IS A LIVE TRANSFERABLE GOVERNMENT CENTER
----------------------------------------------------------------------------
Current `CIV4BuildingInfos.xml` has only three `bGovernmentCenter=1` buildings:

    BUILDING_PALACE
        bCapital       = 1
        iConquestProb  = 0

    BUILDING_BARBARIAN_PALACE
        bCapital       = 0
        bNeverCapture  = 1
        iConquestProb  = 0

    BUILDING_FORBIDDEN_PALACE
        bCapital       = 0
        bGovernmentCenter = 1
        bNeverCapture  = 0
        iConquestProb  = 0
        PrereqTech     = TECH_CONSTITUTION
        iCitiesPrereq  = 6

`BUILDINGCLASS_FORBIDDEN_PALACE` has `iMaxPlayerInstances=1`.

The Palace cannot supply the peaceful-city-trade witness because `canTradeCityTo` explicitly
rejects capitals. The Barbarian Palace is special barbarian data. The Forbidden Palace is therefore
the clean ordinary current-SAS witness.

`canTradeCityTo` does not reject a city merely for containing a national wonder. Once the normal
city-trade conditions are met (recipient reveal, >= CITY_TRADE_CULTURE_THRESH culture, etc.), a
Forbidden-Palace city can be traded.

PEACEFUL ACQUISITION PRESERVES THE FORBIDDEN PALACE UNDER ORDINARY CONDITIONS
-----------------------------------------------------------------------------
`CvPlayer::acquireCity` snapshots every real building, kills/recreates the city for the new owner,
and restores each building that the recipient can legally possess/construct. The destruction block
contains:

    if (isProductionMaxedBuildingClass(eBuildingClass, true) ||
        !kCityPlot.canConstruct(eBuilding))
    {
        continue;
    }

    int iOdds = kBuilding.getConquestProbability();
    if (!bConquest || bRecapture || SyncRandSuccess100(iOdds))
        kNewCity.setNumRealBuildingTimed(...);

For a peaceful city trade, `bConquest == false`, so the conquest-probability roll is bypassed.
Therefore choose an entirely ordinary recipient that:

1. knows Constitution;
2. already has at least 6 cities;
3. does not already own its one allowed Forbidden Palace;
4. can legally receive the candidate city under the normal city-trade culture/reveal rules.

The transferred Forbidden Palace passes the recipient-side building checks and is restored in the
new city despite `iConquestProb=0` (that zero matters to conquest, not peaceful trade).

Restoring it runs normal building processing. `CvCity::processBuilding` calls:

    changeGovernmentCenterCount(kBuilding.isGovernmentCenter() ? iChange : 0);

and `changeGovernmentCenterCount` immediately calls:

    GET_PLAYER(getOwner()).updateMaintenance();

so the retained Forbidden Palace is authoritative runtime government-center state for the new
owner immediately after transfer.

CLEAN CURRENT RUNTIME FIXTURE - POSITIVE PREVIEW DISTANCE -> ZERO ACTUAL DISTANCE
--------------------------------------------------------------------------------
Take a tradable foreign city C that contains the Forbidden Palace. Let prospective recipient R meet
the four conditions above and have no other government center except an ordinary capital. Put C at
positive plot distance D from that capital (and, more generally, at positive distance from every
currently owned government center of R).

BEFORE transfer, while C is still foreign:

    C.calculateDistanceMaintenanceTimes100(R)
      -> calculateMaintenanceDistance(C.plot, R)
      -> iterates only R's currently owned cities
      -> C is absent
      -> nearest government center is at positive distance
      -> projected distance-maintenance component is positive
         under ordinary positive distance/world/handicap modifiers.

AFTER peaceful transfer and Forbidden-Palace restoration:

    C belongs to R
    C.isGovernmentCenter() == true

The normal maintenance search now iterates C itself. For that loop entry:

    plotDistance(C.plot, C.plot) = 0
    C.isGovernmentCenter()       = true
    iShortestGovernment          = 0

Therefore:

    calculateMaintenanceDistance(C.plot, R) = 0
    calculateDistanceMaintenanceTimes100(R) = 0

for C's distance component.

So the prospective acquisition calculation charges maintenance that the transferred city will not
actually pay. The result is deterministic once the ordinary transfer/building-retention conditions
above hold.

AI EFFECT
---------
`AI_assetVal(c, false)` is the peaceful-transfer branch. The same function explicitly treats
buildings as surviving when `!bConquest`:

    if (!bConquest || bOwn) // Most mundane buildings don't survive conquest
        ... value buildings ...

Thus the valuation already distinguishes peaceful transfer from conquest for building survival,
but its maintenance component does not carry the surviving government's-center effect into the
prospective owner state.

The false positive maintenance is subtracted from the foreign city's asset value and then feeds
`AI_cityTradeVal` acquisition pricing/willingness. The AI therefore undervalues acquisition of a
tradable Forbidden-Palace city relative to the post-transfer maintenance state.

There is additional same-root breadth: a newly acquired Forbidden Palace can also lower distance
maintenance in OTHER recipient cities by becoming their nearer government center. `AI_assetVal`'s
separate heuristic immediately below the candidate-city maintenance call is explicitly an estimate
of extra civic upkeep / extra number-of-cities maintenance in other cities; it does not simulate
owner-wide distance-maintenance SAVINGS from a newly retained government center. The candidate
city's own positive->zero mismatch is sufficient for confirmation without relying on this broader
effect.

WHY THIS IS NOT THE SAME ROOT AS F581
-------------------------------------
F581/KI#904 is a count-state bug in the prospective number-of-cities and colony helpers:

- the requested owner's `getNumCities()` / `getCitiesPerPlayer()` does not yet include the foreign
  candidate;
- the static helpers already expose `iExtraCities` but the member wrappers fail to pass +1.

That error applies generally to foreign candidate cities and has a mechanical +1 repair.

F582 is a different state dimension and implementation mechanism:

- `calculateMaintenanceDistance` searches a concrete LIST of the prospective owner's existing
  cities for government centers;
- the missing hypothetical state is not merely one city count but a BUILDING EFFECT on the
  candidate that can survive peaceful transfer;
- the repair therefore requires projecting retained government-center state, not passing
  `iExtraCities=1`.

The two findings share the broad prospective-owner-maintenance domain but have separate causes,
fixtures, and repairs.

CORRECTION / NARROWING TO THE F581 WRITEUP
------------------------------------------
WIP372's F581 caller section said:

    Distance maintenance can legitimately use the prospective owner's current
    capital/government-center set.

That sentence is too broad. It is correct for ordinary candidate cities whose transfer will not
add a government center, and it is normally appropriate for the original conquest-oriented use
when the relevant government-center building does not survive conquest. F582 establishes the
peaceful-transfer exception: a retained Forbidden Palace changes the prospective government-center
set as part of the acquisition itself.

Treat this WIP375 note as the authoritative narrowing of that F581 control sentence; F581's
number-of-cities/colony proof and repair remain unchanged.

ANCESTRY
--------
K-Mod's normal runtime distance-maintenance code is current-owner-only. Its
`calculateMaintenanceDistance()` walks the current owner's city list; when the current city itself
is a government center, that is correct because the city is already in the list and contributes
zero distance.

AdvC practical 1253 / initial-import commit
`f6ff6a182f5187541fff6e5bbbe12f9573b3778c` adds the optional owner parameter with the explicit
header contract:

    projected maintenance for cities yet to be conquered

and moves K-Mod's search into a static helper parameterized by owner. The helper loops only
`GET_PLAYER(owner)` cities. For the original conquest-oriented purpose, current SAS's ordinary
Palace/Forbidden-Palace government-center buildings have `iConquestProb=0`, so ignoring their
post-capture government-center role is not itself a current contradiction: they do not survive an
ordinary conquest transfer.

AdvC practical 2053 / commit
`fd5d755af022962c4e3e09495e052fbc80bf2457`, "AI code for city trade evaluation", introduces the
current `AI_assetVal` city-trade path. The diff adds:

    scaled r = AI_assetVal(kCity, false);

for acquisition valuation and, inside `AI_assetVal`, subtracts the sum of:

    c.calculateColonyMaintenanceTimes100(getID())
    c.calculateDistanceMaintenanceTimes100(getID())
    c.calculateNumCitiesMaintenanceTimes100(getID())

while the same function's `!bConquest` building branch assumes peaceful-transfer buildings survive.
That reuse extends the conquest-oriented prospective helper to a transfer mode where the candidate
can retain a government-center building, activating F582.

Practical 2463 / commit `fd00cf5197afe3d1c519d62ffb288ea11955e968` later consolidates the three
calls into `calculateBaseMaintenanceTimes100(getID())` but leaves the same distance-maintenance
prospective-state omission intact. Base AdvCiv 1.14 and current SAS retain it.

DEDUP
-----
Whole-album semantic searches for prospective/foreign distance maintenance, government centers,
Forbidden Palace, AI_assetVal and city-trade maintenance found no prior numbered root owning this
specific transfer-state omission.

Distinct from:

- F399/KI#722: the optional BUG saved-maintenance BUILDING HOVER ignores government-center savings
  when hypothetically constructing Forbidden Palace. F582 is AI city-acquisition valuation across
  ownership transfer, not UI help, and arises through the prospective-owner city list.
- F581/KI#904: prospective num-cities/colony count omission; different state and repair as above.
- KI#306 / associated cache findings: within-turn invalidation of government-center/top-production
  ranking state, not hypothetical transfer arithmetic.
- F567: runtime colony-maintenance cap arithmetic, unrelated to government-center projection.
- F329: prospective active-world-wonder owner/team obsolescence, same broad transfer domain but a
  different API and state dimension.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do not blindly make every foreign candidate a government center. The prospective state depends on
transfer mode and on whether a government-center building would survive / be legal for the
recipient.

A robust repair should make peaceful `AI_assetVal` project the building-retention state used by
`acquireCity` closely enough to determine whether the candidate will be a government center for the
recipient. If yes, its own prospective distance-maintenance component is exactly zero; ideally the
projection should also account for owner-wide distance-maintenance savings in other recipient
cities.

Possible implementation shapes include:

1. a transfer-aware maintenance projection helper that can treat the candidate plot as an added
   government center when acquisition rules say a relevant building survives; or
2. a narrower `AI_assetVal` adjustment for foreign peaceful acquisitions, sharing a helper with
   acquisition/building-retention legality rather than duplicating those rules.

Avoid using only `c.isGovernmentCenter()` without a recipient-side survival check: current state can
contain a government-center building that the new owner would fail to retain because of class
limits/prerequisites. The current Forbidden-Palace fixture deliberately chooses a recipient for
whom retention is legal.

REGRESSION TESTS
----------------
A. Clean peaceful Forbidden-Palace transfer
- recipient has Constitution, >=6 cities, no Forbidden Palace;
- candidate is non-capital, tradable, contains Forbidden Palace, and is at positive distance from
  every recipient government center;
- pre-transfer projected distance component for recipient should be 0 after the repair;
- immediately post-transfer runtime distance component should also be 0.

B. Recipient already has Forbidden Palace
- candidate's old Forbidden Palace cannot become a second retained national wonder;
- projection must NOT assume a new zero-distance government center merely from the candidate's
  current `isGovernmentCenter()` state.

C. Recipient fails a building prerequisite
- e.g. insufficient city count / tech in a controlled fixture;
- projection must match acquisition's failure to retain the government-center building.

D. Ordinary foreign city without government-center building
- prospective distance maintenance must remain the normal distance to recipient's existing
  government-center set.

E. Conquest control
- current Forbidden Palace has `iConquestProb=0`;
- a conquest projection must not assume that it survives and becomes a government center.

F. Other-city savings breadth
- recipient has another city whose nearest government center becomes the transferred Forbidden
  Palace;
- if repair claims full post-acquisition maintenance projection, verify that city's reduced distance
  maintenance is also represented; otherwise document candidate-city-only scope explicitly.

CURRENT STONE AFTER C031-WIP375
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F582/KI#905 are CONFIRMED.
F583 / provisional KI#906 is UNUSED.
Latest physical checkpoint: C031-WIP375.
Actual AdvCiv-SAS repository source remains UNTOUCHED.

NEXT
----
Continue under F583 unused. Leave the prospective maintenance family behind now that the count-state
(F581) and retained-government-center distance-state (F582) roots are separated and preserved.
Resume remaining low/zero-mention authoritative `CvCity` state with historical bugfix oracles,
prioritizing production lifecycle, religion/corporation/vote-source transitions, rank/cache
invalidation, and signed/bounded arithmetic under ordinary current SAS callers/data. Whole-album
deduplicate before promotion and freeze WIP376 regardless.

END C031-WIP375


===============================================================================
C031-WIP376 CONTINUATION — F583 / KI#906 CONFIRMED
===============================================================================

HANDOFF / STARTING STONE
------------------------
Continuation began from C031-WIP375 with:

- F559/KI#882 through F582/KI#905 CONFIRMED;
- F583 / provisional KI#906 UNUSED;
- Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN;
- actual AdvCiv-SAS repository source UNTOUCHED.

This tranche first completed the low-coverage production/rank and historical corrective-practical
sibling pass. It then promoted one independent inherited gameplay-state root as F583/KI#906.

POST-WIP375 CONTROLS BEFORE F583
--------------------------------

1. Production storage / rank helpers

The remaining low-mention production-storage helpers do not expose a new lifecycle defect. Project
production intentionally has no decay timer. Unit/building production storage and timers retain the
expected completion/cancellation lifecycle. The rank-building functions themselves are coherent;
the interesting failure mode is producer-side invalidation, already covered heavily in the earlier
WIP365/WIP366 rank/cache pass.

2. Practical 3666 — food already beyond growth threshold after random event

AdvC practical 3666 repairs `getFoodTurnsLeft()` for the transient state where a random event can
leave stored food above `growthThreshold()` until the next city turn. The repair returns one turn
instead of sending a negative numerator into unsigned-ceiling division. Current `doGrowth()` then
consumes that state normally. No independent sibling event/growth invariant break was proved.

3. Practical 4331 — `GPProjection()` negative-turn-looking arithmetic

`GPProjection()` clamps its aggregate projected contribution with `std::max(0, iTurnsLeft)` while a
per-unit numerator still multiplies by raw `iTurnsLeft`. This superficially looks wrong when
`GPTurnsLeft()==-1`.

Under current shipped SAS data, however, the negative-turn case is masked by the GP-rate invariant:
outside disorder there are no negative Great-People-rate modifiers capable of reducing a positive
base GP rate to zero. Therefore `GPTurnsLeft()==-1` implies the current base GP rate is zero, and all
per-unit current GP-rate buckets multiplied by -1 are likewise zero. Preserve as defensive/API
robustness breadth, not a current-live numbered root.


F583 / KI#906 — SETTLER/WORKER FOOD-DIFFERENCE STILL CHARGES ANGRY CITIZENS
----------------------------------------------------------------------------
Status: CONFIRMED CURRENT SAS GAMEPLAY ROOT; INHERITED FROM BTS / PRESENT IN K-MOD AND BASE ADVC.

ROOT
----
Current `CvCity` uses two different food-consumption contracts for the SAME Settler/Worker city
turn.

The production path correctly honors the special BtS rule restored by AdvC practical 2125:

    int iFoodProduction = (bFoodProduction ?
            std::max(0, getYieldRate(YIELD_FOOD) - foodConsumption(
            /* advc.001 (K-Mod bug): */ true)) : 0);

`foodConsumption(true)` excludes angry citizens from the population count used for this special
food-to-production calculation.

But `foodDifference()` still uses ordinary `foodConsumption()` when a food-production order is
active:

    if (!bIgnoreProduction && isFoodProduction())
        iDifference = std::min(0,
                getYieldRate(YIELD_FOOD) - foodConsumption());

The no-argument call is equivalent to `foodConsumption(false, 0)`, so angry citizens are charged
there.

`doGrowth()` consumes this mismatched result authoritatively every city turn:

    int iDiff = foodDifference();
    changeFood(iDiff);
    ...
    else if (getFood() < 0)
    {
        changeFood(-getFood());
        if (getPopulation() > 1)
            changePopulation(-1);
    }

Thus the special food-production order can simultaneously:

- treat an angry citizen as NOT consuming food when converting food into Settler/Worker
  production; and
- treat that same angry citizen as consuming food when subtracting from the city's stored-food
  bar.

This is not just a preview mismatch. `doGrowth()` mutates authoritative stored food and can
ultimately reduce population through starvation.

ORDINARY CURRENT-SAS TURN FLOW
------------------------------
Current `CvCity::doTurn()` orders the relevant operations as:

    doCheckProduction();
    doGrowth();
    doCulture();
    doPlotCultureTimes100(...);
    doProduction(...);

The ordering does not mask the bug. Both `doGrowth()` and `doProduction()` inspect the same active
Settler/Worker order and current food yield. In a non-starving one-turn witness, `doGrowth()` first
subtracts the false stored-food deficit; population and worked yields remain unchanged; later
`doProduction()` grants the special food-production surplus using `foodConsumption(true)`.

CURRENT SHIPPED REACHABILITY
----------------------------
Current SAS XML ships ordinary food-production units:

- `UNIT_SETTLER` has `<bFood>1</bFood>`;
- `UNIT_WORKER` has `<bFood>1</bFood>`;
- the Indian Fast Worker, Netherlands Swift Worker, Maya Quick Worker and Barbarian Worker also
  have `<bFood>1</bFood>`.

`CvCity::isFoodProduction(UnitTypes)` maps this unit flag to the special production path. No
optional mod-mod data or disabled define is required.

CLEAN NUMERIC REPRODUCTION
--------------------------
Use an ordinary healthy, non-disordered size-4 city with:

- exactly 1 angry citizen;
- no net unhealthiness or other food-consumption modifier;
- total food yield = 7;
- stored food safely above zero, e.g. 10;
- an active Settler or Worker order.

With the normal 2 food per citizen:

    ordinary foodConsumption(false) = 4 * 2 = 8
    special  foodConsumption(true)  = (4 - 1 angry) * 2 = 6

PRODUCTION ACCOUNTING:

    getYieldRate(FOOD) - foodConsumption(true)
    = 7 - 6
    = +1

So `getProductionDifference()` correctly converts +1 food into Settler/Worker production.

STORED-FOOD ACCOUNTING IN THE SAME TURN:

    foodDifference()
    = min(0, 7 - foodConsumption(false))
    = min(0, 7 - 8)
    = -1

Then `doGrowth()` applies:

    stored food: 10 -> 9

before `doProduction()` later awards the +1 special food-production contribution.

The same 7 food has therefore been classified as both:

    +1 surplus for Settler/Worker production

and

    -1 deficit for authoritative stored food

solely because one branch ignores the angry citizen while the other counts it.

A sufficiently low stored-food state broadens the same root into population loss: repeated false
negative `foodDifference()` can exhaust the bar, after which `doGrowth()` executes its normal
starvation path even though the special Settler/Worker accounting says the angry citizen is not
consuming food.

DOCUMENTED / HISTORICAL CONTRACT
--------------------------------
This behavior conflicts directly with the rule AdvC itself says practical 2125 is restoring.

AdvC practical 2125 / commit `968880bb420387f4d1022cb9296c8ada3dbfd8f9`, titled
"Fix K-Mod bug: food consumption by angry citizens", changes only the production calculation from:

    foodConsumption()

to:

    foodConsumption(true)

and the commit message states that angry citizens "indeed aren't supposed to consume food when
producing a Settler or Worker."

The AdvC manual repeats the same rule and records practical 2125 as repairing a K-Mod 1.45
regression. Current SAS also ships loading hint `TXT_KEY_ADVC_HINT_47`:

    Consider producing settlers and workers in cities with angry or unhealthy citizens.
    So long as food gets converted into production, angry citizens don't consume food, ...

The witness above is deliberately in the exact "food gets converted into production" state:
`foodConsumption(true)` produces a +1 convertible surplus. Yet `foodDifference()` still charges
that angry citizen against the stored-food bar.

The historical CivFanatics discussion linked by the AdvC commit also records Firaxis developer
alexman confirming that the angry-citizen exception was by design, with the stated purpose of
avoiding a production penalty / extra happiness-health micromanagement when using Settlers and
Workers. The local AdvC manual already records that source and rationale, so the finding does not
depend on an external page remaining available.

ANCESTRY
--------
The mismatch predates AdvC.

Civ4CE's reconstructed BtS `CvCity.cpp` has:

    getProductionDifference(...):
        ... getYieldRate(YIELD_FOOD) - foodConsumption(true) ...

while its BtS `foodDifference()` still uses:

    ... getYieldRate(YIELD_FOOD) - foodConsumption() ...

So the split accounting is present in the BtS lineage itself.

K-Mod inherits the same `foodDifference()` basis. K-Mod 1.45 later regressed the production side by
changing its production calculation to ordinary `foodConsumption()` too, making the two wrong-but-
consistent in the opposite direction. AdvC practical 2125 correctly restores the special
production-side `foodConsumption(true)`, but it leaves the inherited stored-food branch untouched,
revealing the original BtS split again.

K-Mod practical/history commit 957 (`430199d82a53d9701e0d9695dbcff4bf58e44bfb`) later adds
`bIgnoreProduction` to `foodDifference()` for AI callers that need the ordinary growth signal. It
does not alter the active-food-production branch's use of `foodConsumption()`.

Base AdvCiv 1.14 and current SAS therefore retain the same split.

DEDUP
-----
Whole-album searches for `foodDifference`, `foodConsumption(true)`, angry citizens,
Settler/Worker starvation and practical 2125 found no numbered root owning this mismatch.

The only direct practical-2125 album note was WIP375's historical-repair control saying that the
food-PRODUCTION calculation correctly uses `foodConsumption(true)`. F583 is the newly proved
sibling in the authoritative stored-food calculation.

Distinct from:

- KI#48.3 / F007: `AI_buildingValue` double-subtracts unhealthiness after receiving a
  health-adjusted `foodDifference(false, true)`; AI valuation only, different arithmetic and call
  mode.
- the earlier disorder/food-display findings around `foodDifference()`: UI/display contracts, not
  angry-citizen consumption during food-production orders.
- citizen/plot-value Worker-production findings: AI assignment/value logic rather than the
  authoritative city food bar.
- practical 2125 itself: that repairs a K-Mod regression in `getProductionDifference`; F583 is the
  still-unrepaired sibling in `foodDifference` inherited from BtS.

CONSERVATIVE REPAIR
-------------------
Make the active food-production branch use the same special consumption contract as production:

CURRENT:

    if (!bIgnoreProduction && isFoodProduction())
        iDifference = std::min(0,
                getYieldRate(YIELD_FOOD) - foodConsumption());

REPAIR:

    if (!bIgnoreProduction && isFoodProduction())
        iDifference = std::min(0,
                getYieldRate(YIELD_FOOD) - foodConsumption(true));

This is deliberately narrow.

- It changes only the branch where `foodDifference()` is honoring the active special
  food-production order.
- Calls with `bIgnoreProduction=true` continue to use normal `foodConsumption()` and therefore keep
  their intended ordinary growth/food signal.
- Non-food-production orders are unchanged.
- It aligns the authoritative stored-food accounting with the already-repaired
  `getProductionDifference()` contract rather than inventing a third rule.

REGRESSION TESTS
----------------
A. One angry citizen, positive special surplus
- size 4, 1 angry, healthy, food yield 7, stored food 10, Settler active;
- special food production must be +1;
- `foodDifference()` must be 0 after repair, not -1;
- stored food must remain 10 after `doGrowth()`.

B. Genuine deficit even after excluding the angry citizen
- same city, food yield 5;
- special consumption = 6;
- food-production contribution = 0;
- `foodDifference()` must be -1;
- true starvation remains possible. The repair must not make Settler/Worker production an
  unconditional starvation immunity.

C. No angry citizens
- food-production branch should match existing behavior; `foodConsumption(true)` equals the normal
  population-consumption basis apart from the already-intended associated health handling.

D. Non-food-production order
- switch to a building or ordinary unit;
- angry citizen must again count in normal food consumption / starvation.

E. Explicit ignore-production AI call
- `foodDifference(..., true)` must continue to use ordinary food consumption and expose the normal
  growth signal expected by K-Mod/AdvC AI callers.

F. Worker variants
- repeat A for ordinary Worker and at least one current food-production Worker UU; same contract.

G. Starvation boundary
- begin with 0 stored food, population >1, one angry citizen, and a yield that is nonnegative only
  under `foodConsumption(true)`;
- repaired food-production branch must not decrement population merely because the excluded angry
  citizen was charged by the old `foodDifference()` basis.

CLASSIFICATION
--------------
F583 / KI#906 is a current-live gameplay-state defect with base BtS ancestry. It is unusually clean
because the same city turn contains two authoritative calculations that are intended to implement
the same special Settler/Worker angry-citizen rule but use different values of the existing
`bNoAngry` parameter.

The actual AdvCiv-SAS source remains UNTOUCHED; this album only records the proof and repair/test
boundary.

CURRENT STONE AFTER C031-WIP376
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559/KI#882 through F583/KI#906 are CONFIRMED.
F584 / provisional KI#907 is UNUSED.
Latest physical checkpoint: C031-WIP376.
Actual AdvCiv-SAS repository source remains UNTOUCHED.

NEXT
----
Continue under F584 unused. Leave the practical-2125 / food-production consumption family behind
now that both the repaired production-side contract and the inherited stored-food sibling are
preserved. Resume remaining low/zero-mention authoritative `CvCity` state and historical corrective
siblings, prioritizing production completion/cancellation, building/religion/corporation/vote
source state, ownership/acquisition transitions, rank/cache invalidation, and signed/bounded
arithmetic under ordinary current SAS callers/data. Whole-album deduplicate before promotion and
freeze WIP377 regardless.

END C031-WIP376


================================================================================
C031-WIP377 - POST-F583 HISTORICAL-ORACLE / LOW-COVERAGE CONTROL CHECKPOINT
================================================================================

DURABLE CURSOR
--------------
F559 / KI#882 through F583 / KI#906 remain confirmed.
F584 / provisional KI#907 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Repository/gameplay source remains completely UNTOUCHED by this audit.

This checkpoint freezes the first F584 tranche after WIP376. No candidate in this
tranche met the current-live + independent-root promotion bar. Several apparently
promising seams were useful controls because they failed for distinct reasons.

1. CIV4CE "DISAPPEARING CITIZENS" CHANGELOG ITEM - NO DISTINCT CURRENT CVCITY ROOT
----------------------------------------------------------------------------------
Civ4CE's BtS changelog contains:

    Fixed disappearing citizens when you remove one in a large city

The obvious CvCity-side candidates were compared directly across the supplied
Civ4CE BtS source, K-Mod 1.46, Base AdvCiv 1.14 and current SAS:

    CvCity::alterSpecialistCount
    CvCity::alterWorkingPlot

Civ4CE retains essentially the same BtS lineage for these routines; there is no
Civ4CE-vs-current-SAS body difference that identifies the changelog item as a
surviving CvCity state defect. Current specialist-removal / force-target logic is
also already densely owned by earlier findings, especially F528 / KI#851 and
F551 plus the earlier forced-specialist sanitizer controls. Do not infer a new
CvCity root solely from the historical changelog wording.

2. CIV4CE THREE GORGES / POWER-UNHEALTHINESS FIXES - CURRENT LINEAGE ALREADY COHERENT
-------------------------------------------------------------------------------------
Civ4CE names three old city bugs:

* Three Gorges Dam giving power unhealthiness to all cities;
* new cities failing to receive the proper Three Gorges power-health state;
* cities with only hydro/nuclear power failing to receive the intended power-health
  handling.

Current AdvC/SAS no longer has the broken state model implicated by those old bugs.
The relevant current chain remains:

    processBuilding / processBonus
      -> changePowerCount(local count, dirty classification)
      -> isPower / isDirtyPower
      -> updatePowerHealth

and area clean power propagates through the area/team/player update chain already
traced in WIP368. Current `isDirtyPower()` correctly excludes area-clean-powered
cities, while clean local Hydro/Nuclear and resource-gated dirty Coal power are
counted separately. WIP370-era health-state controls likewise already verified
`updatePowerHealth()` assignment invalidation. No F584 sibling survived.

`getBonusPower()` was rechecked as part of this pass. It counts only active
civilization buildings whose `PowerBonus` matches the changing bonus and whose
`isDirtyPower()` matches the requested bucket. `processBonus()` applies the dirty
and clean buckets independently through `changePowerCount`; the final state is
coherent even if a hypothetical bonus powered both kinds. Current SAS has ordinary
resource-gated power through Coal Plant (dirty Coal) and Nuclear Plant (clean
Uranium), and the helper matches those XML contracts.

3. CIV4CE HANGING GARDENS / GLOBAL POPULATION ITEM - CURRENT SAS DATA MASKS THE GENERIC PATH
--------------------------------------------------------------------------------------------
The generic `setNumRealBuildingTimed()` world-wonder completion path still handles
`GlobalPopulationChange` by visiting the correct owner/team-share cities, changing
population and immediately reassigning work. The Civ4CE changelog's old Hanging
Gardens failure is useful ancestry evidence that this path was historically fragile.

However, current SAS `CIV4BuildingInfos.xml` has no nonzero
`iGlobalPopulationChange` building at all. Thus the generic code cannot supply a
current shipped F584 witness even if some older BTS-era implementation had a bug.
Preserve only as historical/mod-mod robustness context.

4. `doPartisans()` - HISTORICAL CAPTURE BUGS ALREADY REPAIRED / SEMANTICALLY COVERED
------------------------------------------------------------------------------------
Civ4CE also says it fixed "several city capturing bugs relating to partisans."
Current SAS `CvCity::doPartisans()` was rechecked against Base AdvCiv 1.14 and the
current event XML:

* events-off and low-population/barbarian guards are explicit;
* partisan ownership is based on plot highest culture, not the captured city's
  current owner culture;
* dead/cityless cultural players and non-war states are rejected;
* `getNumPartisanUnits()` uses the documented `max(0, cultureLevel - 1)` rule;
* the manual trigger is required to be the negative-weight `EVENTTRIGGER_PARTISANS`.

The album had already frozen the culture-level/count conversion as coherent and
had separately audited the raze ordering around `kCity.doPartisans()` before native
disband. The later Python Partisans invalid-conscript problem is already KI#524.2-
KI#524.6 and is not a new CvCity root. No F584.

5. CORRECTIVE-COMMIT / FUNCTION-NAME COVERAGE CAVEAT
-----------------------------------------------------
A mechanical pass mapped historical AdvC commits with bug/fix/correct/repair-like
titles to CvCity hunk function names, then compared those names against album
mentions. This exposed an important methodology caveat: a function name having zero
album mentions does NOT imply that a bugfix in the same broad commit concerned that
function.

For example practical 1499 (`3e233e6794`, "Last batch of style changes; some
bugfixes and UI tweaks") touches many CvCity helpers with zero/one literal album
mentions, but the commit message identifies its substantive bugfixes primarily in
AI code; most CvCity hunks are style/refactoring. Likewise practical 1381's CvCity
changes support an AI strategic-resource fix by adding an assumed bonus to
`allUpgradesAvailable` / `canTrain`, rather than repairing `isBuildingsMaxed` merely
because that function appears in an adjacent hunk.

Future historical-oracle passes should therefore use function-body/commit-message
proof, not practical-number or hunk co-occurrence alone.

6. `findHighestCulture()` - POSTHUMOUS OWNER IS AUTHORED POLICY; TIE SPECULATION BELOW BAR
------------------------------------------------------------------------------------------
Current AdvC/SAS intentionally changed K-Mod's ALIVE loop to:

    for (PlayerIter<EVER_ALIVE> it; it.hasNext(); ++it) // advc.099: was ALIVE

This is consistent with the already-frozen advc.099 policy allowing posthumous city
culture. Current callers also account for liveness when a live actor is required;
for example raze memory explicitly checks `kHighestCulturePlayer.isAlive()`.

The strict `>` comparison means exact culture ties are resolved by iteration/player
ID order. Such tie handling could in principle influence `acquireCity()`'s
`bRecapture` classification, but no documented contrary contract or ordinary wrong
state was established in this tranche. Do not promote tie-order speculation as F584.

7. LOW-COVERAGE AUTOMATION / HURRY / CONSCRIPTION HELPERS - NO NEW LIVE CONTRADICTION
--------------------------------------------------------------------------------------
`setProductionAutomated` remains the already-frozen WIP368-ish control: `bClear`
clears the queue only for the active selected city, but the shipped task/UI caller
supplies that state; the non-UI RiseFall caller turns automation OFF, where clear is
irrelevant. No current hidden programmatic clear request was found.

The hurry/conscript arithmetic was challenged at its main bounded points:

* `getHurryGold` returns zero for a non-gold hurry and otherwise nonnegative cost;
  `canHurry` rejects a gold hurry with zero/insufficient gold cost under the current
  order contract.
* `getHurryPopulation` has a minimum cost of one citizen for a population hurry;
  ordinary `canHurry` first rejects an order that current/overflow/feature/minimum
  production will already complete, preventing a zero-needed-production pop-rush
  witness through that path.
* `getHurryCostModifier` can mathematically reach zero under sufficiently negative
  XML modifiers, but the album already proved current SAS ships no negative hurry
  modifier data capable of reaching that state.
* `getConscriptUnit` chooses the highest positive conscription-value trainable unit.
  Its Python override is a generic modding hook, but no current callback supplies an
  illegal replacement; current gameplay therefore stays on the validated native
  selection.
* `conscriptMinCityPopulation` simply adds the configured minimum to the current
  conscript population cost; no independent rounding/sentinel mismatch established.

Known hurry/overflow/Slavery findings and controls remain the relevant owners; this
pass does not reopen them.

8. OTHER LOW-COVERAGE HELPERS CLOSED OR CURRENT-DATA LATENT
-----------------------------------------------------------
* `countNumImprovedPlots(..., bPotential=true)` feeds the building
  ImprovementFreeSpecialist valuation path, but current SAS has no building with a
  nonempty `ImprovementFreeSpecialists` table (WIP373 control), so no shipped F584
  witness.
* BUG building/specialist additional GP/commerce helpers with low album counts are
  preview/help-side and have no authoritative AI/gameplay caller in the current DLL
  where checked. Do not spend a gameplay root number on unreachable preview arithmetic.
* production-time setters / `isAnyProductionProgress` / rank builders remain the
  earlier WIP370/WIP365-WIP366 controls; no new lifecycle mismatch surfaced.
* `changeNoUnhappinessCount` correctly dirties citizen assignment. The deliberately
  thinner `changeUnhealthyPopulationModifier` remains the prior control: normal
  player-turn full assignment recomputation prevents persistent stale assignment.

NEXT EXACT CURSOR
-----------------
WIP377 -> F584 / KI#907 UNUSED -> continue Queue 006 from remaining low-coverage
AUTHORITATIVE state rather than help/UI projections. Prefer independent producer/
consumer contracts, acquisition/lifecycle transitions, bounded stored state and
historical repairs with an exact body-level mapping. Use current SAS XML/callers for
reachability, Base AdvC/K-Mod/Civ4CE only as verified comparison oracles, whole-album
semantic dedup before numbering, then freeze WIP378 regardless.

No repository source file was modified.


===============================================================================
C031-WIP378 - POST-WIP377 AUTHORITATIVE-STATE / CURRENT-DATA CONTROL TRANCHE
===============================================================================

DURABLE STATE
-------------
F559-F583 CONFIRMED.
F584 / provisional KI#907 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Repository/gameplay source UNTOUCHED.

This stone preserves the post-WIP377 tranche before continuing. No finding number
is consumed merely because a compact helper looks suspicious; current supported
reachability and whole-album semantic dedup remain mandatory.

ADV C 1.14 -> CURRENT SAS SEMANTIC BODY SIEVE
---------------------------------------------
A normalized function-body comparison was used to separate real `CvCity.cpp`
semantic divergence from comments/formatting. Only a small finite group of
current SAS bodies differs materially from Base AdvCiv 1.14; most differences
checked here are SAS logging, static-local caching, serialization of an already-
owned SAS state member, or already-owned fixes/controls.

Low-coverage changed bodies checked in this tranche include:
- `getProductionBarPercentages`: current typed-order / NO_PLAYER hardening is the
  already-owned KI#162 family; no new production-state root.
- `getVisibleBuildings`, `addRevoltFreeUnits`, `initialPopulation`,
  `failGoldPercent`: only cached constant/static-local changes where inspected.
- `initConscriptedUnit`, `hurry`, `handleOverflow`, `failProduction`,
  `upgradeProduction`, `popOrder`: SAS recorder/logging additions dominate the
  current divergence; no independent gameplay mutation was established here.
- `setName`: current behavior belongs to the already-preserved KI#161.2 naming
  policy/control family.
- current-format `read`/`write`: the SAS BFC-helper state remains paired; the
  cross-version question was already closed under the project's save-compatibility
  policy in WIP358.

BUILDING HEALTH/HAPPINESS -> CITY BILLBOARD GLOBAL DIRTY CONTROL
----------------------------------------------------------------
A seemingly strong sibling of F580 was checked and CLOSED under current-live
reachability.

`CvCity::processBuilding()` can alter direct building health/happiness and the
bonus/extra variants without itself setting global `CityInfo_DIRTY_BIT`. Local
mutators use assignment dirty and in several cases `setInfoDirty(true)`. Because
practical 1387 explicitly added `CityInfo_DIRTY_BIT` for resource-driven sign
crossings, this generic asymmetry was worth tracing rather than assuming local
city dirty is equivalent.

Current ordinary masking/reachability:
- normal production completion, city events and meltdown happen inside a player
  turn whose normal tail sets `CityInfo_DIRTY_BIT` globally;
- a successful espionage mission explicitly dirties `CityInfo_DIRTY_BIT` in the
  normal unit espionage path, while AI espionage also runs inside the AI player
  turn with the blanket tail refresh;
- current Great-Person `construct` buildings were enumerated from shipped XML;
  the constructible shrines, corporation HQs, Academy, Military Academy and
  Scotland Yard all have zero direct `iHappiness` and zero direct `iHealth`, so
  that mid-turn construct route gives no current health/happy boundary witness;
- Advanced Start is pregame; acquisition/other turn-boundary paths have their own
  broad refresh/lifecycle coverage.

Conclusion: generic robustness/UI-invalidating asymmetry, but no ordinary shipped
path was proved that leaves the active player's healthy/unhappy billboard stale.
Do NOT promote as F584 without a current-supported unmasked producer.

LOW-COVERAGE STORED-STATE HELPERS - CLOSED
------------------------------------------
The following compact authoritative helpers were traced through their meaningful
callers/state consequences and did not yield a new root:
- `setCitizensAutomated`: enabling immediately calls `AI_assignWorkingPlots`;
  disabling clears forced-specialist counts. Selection-button dirtying is present.
- `changeSpecialistCommerce`: mutates the specialist commerce cache and immediately
  calls `updateCommerce(eCommerce)`.
- project production storage: no project decay timer exists by design; project
  production setter/UI invalidation is paired with its lifecycle.
- `changeFreeSpecialist` and `changeMaxSpecialistCount`: both assignment-dirty the
  city; type-specific free-specialist changes directly process the specialist.
- `countUniqueBonuses`: its only important C++ consumer here is AdvC bonus-trade
  attitude normalization, where a distinct-resource count is the intended signal;
  it is not attempting to value resource quantities.
- `getBuildingHappiness`: current AI plot-build happiness adjustment that once used
  this helper is commented out; remaining direct surface is mainly Python/help.
  Its state-religion, player-extra, commerce-slider and event-class components
  match the runtime ingredients inspected here.

F583 BREADTH - FOOD BAR USES THE SAME FOOD-PRODUCTION MISMATCH
--------------------------------------------------------------
`CvCity::getFoodBarPercentages()` calls `foodDifference()` directly when building
its stored/rate food bars. Therefore, while a Settler/Worker food-production
order is active, the same F583 mismatch can be visible in the food bar: angry
citizens that runtime production correctly excludes via `foodConsumption(true)`
are still counted by the current `foodDifference()` path.

This is breadth/UI manifestation of F583 / KI#906, not a new root. The repair to
the active-food-production branch of `foodDifference()` fixes both authoritative
stored-food evolution and this food-bar projection together.

NO-ESPIONAGE / GREAT-SPY GPP DESYNCHRONIZATION - REAL BUT CURRENTLY UNSUPPORTED
-------------------------------------------------------------------------------
A stronger inherited contract mismatch was proved but deliberately does NOT
consume F584 under the album's current-supported-game threshold.

`CvCity::setGreatPeopleUnitRate(eUnit, iNewValue)` contains a stock/K-Mod guard:

    if (GAMEOPTION_NO_ESPIONAGE && GC.getInfo(eUnit).getEspionagePoints() > 0)
        return;

Current `UNIT_GREAT_SPY` has 3000 espionage points, so under No Espionage all
Great-Spy per-unit GPP rate changes are suppressed.

However both `processSpecialist()` and `processBuilding()` still add the same
source's GPP to `m_iBaseGreatPeopleRate` after calling the suppressed per-unit
setter. Thus the invariant explicitly asserted by `doGreatPeople()`:

    sum(getGreatPeopleUnitRate(...)) == getBaseGreatPeopleRate()

can be broken by ordinary content under that option. This is not merely a Spy-
specialist edge case. Current SAS XML contains shipped buildings with
`GreatPeopleUnitClass = UNITCLASS_GREAT_SPY` and positive GPP, including:
- Forbidden Palace (+1)
- Parthenon (+2)
- Versailles (+3)
- Mt. Rushmore (+3)
- Kremlin (+3)
- Military Academy (+1)
- one corporation headquarters (+1)

Consequences in a release DLL are stronger than the debug assertion: total city
GPP can advance toward the threshold while the corresponding Great-Spy unit bank
does not advance. With only suppressed sources, the threshold can be reached with
zero total per-unit selection weight; with mixed sources, threshold timing and GP
selection weights no longer describe the same contributions.

Ancestry comparison: Base AdvCiv 1.14, K-Mod and Civ4CE/BtS retain the same core
split (setter suppresses espionage-point GP units, while base GPP continues to be
updated). AdvC's advc.051 relocation narrows base-rate additions to valid mapped
GP units but does not reconnect the No-Espionage suppression result.

PROMOTION BOUNDARY:
The album has already established for F575 and related controls that current SAS
hides `GAMEOPTION_NO_ESPIONAGE`, defaults it off and ships no ordinary scenario/
map enabling it. Therefore this remains inherited optional/future-proofing debt,
not F584. If SAS restores supported No-Espionage play, this branch should be
promoted/repaired before release.

A robust repair should keep base and per-unit GPP changes atomic: when the mapped
GP unit is suppressed under No Espionage, do not add that source to base GPP
(or deliberately remap it to a supported GP type, if that is the desired policy).
Regression tests should cover a Great-Spy-point building alone and mixed with an
ordinary GP source, checking both total GPP and summed per-unit rates/progress.

CURRENT STONE
-------------
F559-F583 CONFIRMED.
F584 / provisional KI#907 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Source untouched.

EXACT CONTINUATION CURSOR
-------------------------
WIP378 -> F584 unused -> leave the closed billboard/No-Espionage/compact-helper
branches behind -> continue current-SAS XML-backed producer/consumer mismatches,
especially non-default signed fields and lifecycle/acquisition state -> verify
ordinary current-supported callers -> use Base AdvC/K-Mod/Civ4CE only as body-
verified comparison oracles -> whole-album semantic dedup before numbering ->
promote only an independent root -> freeze WIP379 regardless.
===============================================================================


===============================================================================
C031-WIP379 - POST-WIP378 XML-BACKED IMPROVEMENT-SPECIALIST / PROCESSBUILDING CONTROLS
===============================================================================

DURABLE STATE
-------------
F559-F583 CONFIRMED.
F584 / provisional KI#907 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Repository/gameplay source UNTOUCHED.

PURPOSE
-------
Continue from WIP378's current-SAS-data producer/consumer sieve. Recheck old
"current-data masked" conclusions against the actual September 5 light-source XML
rather than relying on prior prose, and promote only a current-supported hard
contradiction. This tranche corrected one stale factual control but did not force a
finding number.

CORRECTION - CURRENT SAS DOES SHIP IMPROVEMENTFREESPECIALISTS
--------------------------------------------------------------
WIP377 said current SAS had no building with a nonempty
`ImprovementFreeSpecialists` table. That statement is FALSE in the current snapshot.
A namespace-aware parse of shipped `CIV4BuildingInfos.xml` finds exactly two live
entries:

    BUILDING_GLOBE_THEATRE
      PrereqTech = TECH_AESTHETICS
      bNoUnhappiness = 1
      IMPROVEMENT_FOREST_PRESERVE -> +1 free specialist

    BUILDING_NATIONAL_PARK
      PrereqTech = TECH_BIOLOGY
      iUnhealthyPopulationModifier = -100
      IMPROVEMENT_FOREST_PRESERVE -> +1 free specialist

The Globe effect is a SAS addition; National Park is the inherited canonical user.
Do not reuse the old "no shipped producer" argument.

`changeImprovementFreeSpecialists` itself remains a thin mutator:

    m_aiImprovementFreeSpecialists.add(eImprov, iChange);

with no `AI_setAssignWorkDirty(true)` in that function.

CURRENT RUNTIME DIRTYING - NO F584
----------------------------------
The missing setter-local assignment dirty does not currently establish a persistent
normal-play stale citizen state.

1. Globe Theatre building completion/removal is independently masked.
   `processBuilding` applies `changeNoUnhappinessCount` before the
   ImprovementFreeSpecialists table. `changeNoUnhappinessCount` dirties citizen
   assignment. Therefore Globe's normal building-state transition already requests
   reassignment.

2. National Park does not have Globe's NoUnhappiness mask, and
   `changeUnhealthyPopulationModifier(-100)` is itself a thin setter. However the
   normal `CvCity::doTurn` path performs unconditional `AI_assignWorkingPlots()` later
   in the same city turn. A National Park completed during `doProduction()` therefore
   cannot carry stale improvement-free-specialist assignment into the next ordinary
   turn.

3. Improvement transitions are explicitly handled on the plot side. Current
   `CvPlot::setImprovementType` tests the working city's
   `getImprovementFreeSpecialists` for relevant old/new improvements and dirties
   assignment when an improvement-free-specialist effect can change.

4. `changeImprovementFreeSpecialists` is exposed through CyCity/Python, so the setter
   is not a perfect self-contained API. No current SAS Python caller was found that
   mutates this table dynamically. Preserve generic API/future-mod robustness debt,
   not a shipped F584.

SAME-TURN GP NUANCE - NOT CAUSED BY THE MISSING DIRTY FLAG
----------------------------------------------------------
When a building completes during `doProduction`, `doGreatPeople` occurs before the
later unconditional working-plot/specialist reassignment in the city turn. Thus a
newly available improvement-free specialist need not contribute a GP tick on the
completion turn.

Merely adding `AI_setAssignWorkDirty(true)` inside
`changeImprovementFreeSpecialists` would not move assignment before `doGreatPeople`;
it only marks work dirty. Therefore this timing nuance is not evidence that the thin
setter itself is the root. Do not number it without a separate intended-turn-order
contract.

GLOBE THEATRE AI VALUATION - REAL EARLY FUTURE VALUE, BUT POLICY/HEURISTIC
------------------------------------------------------------------------
`CvCityAI::AI_buildingValue` values each ImprovementFreeSpecialists entry as:

    countNumImprovedPlots(eImprovement, true) * 50 * iFreeSpecialists

`CvCity::countNumImprovedPlots(..., bPotential=true)` counts either an existing target
improvement OR a workable plot for which:

    kPlot.canHaveImprovement(eImprovement, getTeam())

The latter is plot-validity, not current worker-build availability. It does not require
that the owner currently knows the BuildInfo's prerequisite tech.

Current SAS creates a large timing gap:

    Globe Theatre             -> TECH_AESTHETICS
    BUILD_FOREST_PRESERVE     -> TECH_BIOLOGY

So at Aesthetics, before a player can order a Forest Preserve, each current city plot
that is physically capable of becoming a Forest Preserve can already contribute +50
to Globe Theatre's AI building value. With e.g. 8 such forests that term is +400.

This is a REAL SAS-specific heuristic interaction, but it is not promoted as F584.
Reasons:

* `bPotential=true` is deliberate K-Mod behavior, not an accidental SAS argument;
* the Globe effect itself was deliberately added by SAS as a gradual/permanent
  forest-linked national-wonder effect;
* selecting a forest-rich city for an irrevocable national wonder in anticipation of
  a later improvement can be a defensible long-term policy;
* no hard caller contract says AI_buildingValue must count only immediately realized
  effects, and AI building valuation elsewhere intentionally contains forward-looking
  terms.

Preserve as a balance/heuristic tuning candidate. If desired, a future tuning change
could discount potential-but-not-currently-buildable improvements by tech distance or
count only existing target improvements until the relevant BuildInfo becomes available.
That would be a design choice, not a correctness repair established here.

PROCESSBUILDING SHRINE KEY - EXACT OLD CONTROL RE-HIT
-----------------------------------------------------
While inspecting XML-backed processBuilding state, this suspicious pair resurfaced:

    if (kBuilding.getHolyCity() != NO_RELIGION)
        m_aiShrine.add(kBuilding.getReligionType(), iChange);

It tests `HolyCity` but indexes the cache by `ReligionType`. This is an exact prior
album control, not F584. All seven current shrine buildings set HolyCity and
ReligionType to the same religion, and `hasShrine` reads the same religion-keyed cache.
Generic XML robustness only.

CURRENT-XML DIFFERENCE SIEVE
----------------------------
A structured SAS-vs-Base-AdvC-1.14 building XML comparison was added to the search
method. Rather than treating all inherited building fields equally, the next pass is
prioritizing fields SAS changed from zero/default/empty into active values and tracing
them into authoritative city/player/team consumers. This already caught the stale
ImprovementFreeSpecialists control and is a better current-mod oracle than literal
function-name rarity alone.

CURRENT STONE
-------------
F559-F583 CONFIRMED.
F584 / provisional KI#907 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Source untouched.

EXACT CONTINUATION CURSOR
-------------------------
WIP379 -> F584 unused -> leave the ImprovementFreeSpecialists dirtying/Globe heuristic
and shrine-key controls behind -> continue structured current-SAS-vs-AdvC building XML
non-default effect sieve into authoritative `CvCity::processBuilding` and neighboring
stored-state consumers -> prefer sign/sentinel/receiver/lifecycle contradictions with
ordinary current callers -> whole-album semantic dedup -> promote only an independent
root -> freeze WIP380 regardless.
===============================================================================


===============================================================================
C031-WIP380 - F584 / KI#907 CONFIRMED: BUILDING AI OMITS GLOBAL GREAT-GENERAL RATE
===============================================================================

START STATE
-----------
Resumed from C031-WIP379 with F584 / provisional KI#907 UNUSED and Queue 006
`CvGameCoreDLL/CvCity.cpp` OPEN. Repository/gameplay source remained untouched.

TEAMSHARE XML CORRECTION / PERMANENT-ALLIANCE CONTROL
-----------------------------------------------------
The structured current-XML pass also corrected an over-broad shorthand used while
reopening the old TeamShare/Permanent-Alliance area-counter control.

Current globally-limited BuildingInfo entries are NOT literally all TeamShare. A direct
BuildingClass + BuildingInfo parse gives 53 globally-limited entries, of which 33 have
`bTeamShare=1` and 20 have `bTeamShare=0`. Thus "most current world-wonder entries are
TeamShare" is accurate; "all" is not. Mt. Rushmore itself is globally unique but has
`bTeamShare=0`.

This correction does not promote the old PA accounting wrinkle. Ordinary player-scoped
TeamShare effects are replayed once per member by `CvCity::processBuilding` ->
`CvPlayer::processBuilding` and later removed once per member, so their counts remain
balanced. The noncanonical post-merge aggregation remains specific to the team-keyed
`CvArea` effects:

    kArea.changeCleanPowerCount(getTeam(), ...);
    kArea.changeBorderObstacleCount(getTeam(), ...);

Current live producers remain Three Gorges Dam (AreaCleanPower) and Great Wall
(BorderObstacle). The previously established behavioral masking therefore survives; only
the old wording required correction. Preserve robustness/accounting debt, not F584.

F584 / KI#907 - CONFIRMED
AI_BUILDINGVALUE DOES NOT VALUE BUILDING GREAT-GENERAL-RATE MODIFIERS, NOW A LARGE
CURRENT SAS EFFECT
--------------------------------------------------------------------------------

ROOT
----
Building `iGreatGeneralRateModifier` is an authoritative PLAYER-GLOBAL effect.
`CvCity::processBuilding` forwards active building state to `CvPlayer::processBuilding`,
which performs:

    changeGreatGeneralRateModifier(
        kBuilding.getGreatGeneralRateModifier() * iChange);

The stored player modifier is then consumed directly when combat experience creates
Great-General progress. Current `CvUnit::changeExperience` computes:

    int iCombatExperienceMod = 100 + kPlayer.getGreatGeneralRateModifier();
    ...
    kPlayer.changeCombatExperience(
        (iChange * iCombatExperienceMod * iGlobalPercent) / SQR(100));

So a +200 building modifier really triples the normal Great-General progress generated
by qualifying combat while active; repeated +15 sources add cumulatively to the same
empire-wide player value.

`CvCityAI::AI_buildingValue`, however, never adds value for
`kBuilding.getGreatGeneralRateModifier()`.

A whole current C++ search finds that getter in `CvCityAI.cpp` only here:

    kBuilding.getGreatGeneralRateModifier() < 40

inside AdvC's national-wonder-slot placement heuristic. That comparison can exempt a
military national wonder from a capital-slot devaluation, but it does NOT price the
Great-General-rate effect into the candidate's construction value.

This is especially diagnostic because `CvPlayerAI::AI_civicValue` DOES explicitly value
the equivalent civic field during a war plan:

    iValue += (kCivic.getGreatGeneralRateModifier() *
        getNumMilitaryUnits()) / 50;

The AI therefore understands the strategic value of this exact player mechanic in the
civic domain while omitting it in the building domain.

CURRENT SAS ACTIVATION
----------------------
Base AdvCiv 1.14 and supplied K-Mod 1.46 BuildingInfos contain NO building with a nonzero
`iGreatGeneralRateModifier`. The inherited building-AI omission was consequently dormant
for their shipped data.

Current SAS deliberately activates the field on three buildings:

    BUILDING_JAPAN_DOUJOU       +15
    BUILDING_MT_RUSHMORE       +200
    BUILDING_MILITARY_ACADEMY   +50

The first two make the ordinary AI-construction consequence unambiguous.

A. JAPANESE DOUJOU

The Doujou is the Japanese Barracks replacement, cost 50. Its current XML comment
explicitly states that every early Doujou adds cumulatively to the empire-wide modifier.
Thus e.g. four active Doujous give Japan +60 Great-General rate.

The Doujou also has a fixed `iAIWeight=70`, so Japan is not necessarily prevented from
building it. But that fixed weight is not a semantic valuation of the +15 effect: it does
not scale with city count, accumulated Doujous, military size, war state, or the marginal
Great-General rate that the candidate actually adds. More importantly, the second witness
has no such fixed compensation.

B. MT. RUSHMORE - CLEANEST WITNESS

Current SAS reworked Mt. Rushmore into a globally unique military world wonder and gives
it, among other effects:

    iGreatGeneralRateModifier       = +200
    iGlobalWarWearinessModifier     = +50   (bad for the owner)
    iMilitaryProductionModifier     = +15
    iAIWeight                       = 0

The XML history/comment explicitly describes the new positive military effects as a
tradeoff/compensation for making its global war-weariness modifier positive (harmful).
Runtime implements all of those effects.

`AI_buildingValue` also explicitly prices the WAR-WEARINESS side with the correct sign:

    iValue += ... * -iGlobalWarWearinessModifier * ...;

so Mt. Rushmore's +50 global war weariness reduces its AI value. The building AI also
prices its +15 local military-production effect in the ordinary production block.

But it never reads the +200 Great-General-rate benefit at all. Hence the AI evaluates an
asymmetric subset of the intended tradeoff: it sees a designed drawback and a smaller
production upside while silently dropping the very large empire-wide Great-General-rate
upside.

This can materially lower Mt. Rushmore's rank against other build choices and can make
its intended military tradeoff appear substantially worse to AI than it really is.

The Great-General field is player-visible too: `CvGameTextMgr` emits
`TXT_KEY_BUILDING_GENERAL_RATE_MOD` whenever it is nonzero. This is not a hidden metadata
hint; it is a real advertised gameplay effect.

MILITARY ACADEMY BREADTH
------------------------
Current Military Academy also has +50 Great-General rate, but `iCost=-1` and its creation
path is Great-Person-driven rather than ordinary hammer construction. Preserve it as data
breadth of the same missing building-domain valuation, not as the primary
`AI_buildingValue` reproducer.

ANCESTRY
--------
K-Mod's building AI has no valuation for this field and K-Mod ships no building producer.
Base AdvCiv 1.14 likewise ships no nonzero building producer. AdvC practical 1290 added the
`getGreatGeneralRateModifier() < 40` national-wonder slot-classification check, showing
that the field was acknowledged for placement characteristics, but did not add positive
building value for it.

SAS later activated the dormant field in gameplay data. Commit-diff material records
Mt. Rushmore changing from 0 to +200 (`SASBranch_4855_a9d6994c3a`) and the Japanese
Doujou receiving the deliberately cumulative +15 effect (`SASBranch_6079_921cbbd9c7`,
later wording refined). No corresponding `AI_buildingValue` valuation was added.

Classification:
    inherited K-Mod/AdvC building-AI omission made current-live and strategically large
    by SAS building data.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album searches for `GreatGeneralRateModifier`, `getGreatGeneralRateModifier`, and
building/Great-General valuation found no numbered root owning this omission.

It is distinct from:

* F554 / KI#877 - SAS's regular-building maintenance classifier uses the wrong sign;
  F584 is an entirely omitted positive gameplay field after normal candidate reachability.
* F557 / KI#880 and neighboring `AI_buildingValue` arithmetic findings - those misvalue
  already-considered happiness/other terms; F584 has no Great-General-rate value term at all.
* F577/F578 - hypothetical player-facing building commerce/yield projection helpers omit
  candidate-created global state; F584 is authoritative AI construction ranking in
  `CvCityAI::AI_buildingValue`, not the BUG "actual effects" preview layer.
* Great-General unit-action findings elsewhere in the album - those concern UI/unit-ID
  targeting and do not affect building construction valuation.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
Add an explicit building-domain value for `kBuilding.getGreatGeneralRateModifier()`.
Do not simply hide it inside a fixed XML `iAIWeight`: the effect is empire-wide and its
marginal strategic value naturally depends on military size / likelihood of combat and,
ideally, current Great-General progress/rate context.

The existing civic valuation supplies a conservative design oracle:

    if (bWarPlan)
        value += modifier * militaryUnits / 50;

A building formula need not copy that coefficient literally, but should preserve the same
sign and scale positively with a positive modifier. Off-war value can be discounted rather
than forced to zero because permanent buildings/wonders outlive current war plans.

For TeamShare buildings in future data, evaluate recipient-team breadth consistently with
how `CvPlayer::processBuilding` actually distributes the player-global effect.

REGRESSION TESTS
----------------
1. Field isolation:
   Create two otherwise-identical candidate BuildingInfos differing only by
   `iGreatGeneralRateModifier=0` vs +100; in a militarily relevant city/player state,
   assert the +100 candidate receives strictly greater `AI_buildingValue`.

2. Mt. Rushmore current fixture:
   Compare current Mt. Rushmore against a temporary clone with its GG modifier zeroed;
   assert the live +200 version gains an explicit positive AI term while its +50 global
   war-weariness drawback remains negative.

3. Doujou scaling:
   For Japan, verify the marginal +15 effect remains positively valued and that any chosen
   formula responds sensibly to military size/war state rather than relying on `iAIWeight`.

4. Runtime oracle:
   With a +200 source active, confirm qualifying combat XP adds Great-General combat
   experience at 3x the zero-source rate before other domestic/global percentages.

5. Zero control:
   Buildings with `iGreatGeneralRateModifier=0` must retain their existing AI value exactly.

CURRENT STONE
-------------
F559-F584 CONFIRMED.
F585 / provisional KI#908 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Repository/gameplay source UNTOUCHED.

EXACT CONTINUATION CURSOR
-------------------------
WIP380 -> F585 unused -> leave the now-proved building Great-General-rate omission and
TeamShare-count correction behind -> continue the structured SAS-vs-AdvC non-default
building-effect sieve, prioritizing current fields whose runtime producer is authoritative
but whose AI/state consumer assumes the old zero/default domain -> verify ordinary current
callers/data -> whole-album semantic dedup -> promote only an independent root -> freeze
WIP381 regardless.
===============================================================================


===============================================================================
C031-WIP381 - QUEUE 006 CVCITY.CPP / F585 CONFIRMED
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN under the protected deep-method re-audit.
F559 / KI#882 through F584 / KI#907 remain CONFIRMED.
F585 / provisional KI#908 is CONFIRMED below.
F586 / provisional KI#909 is UNUSED.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot,
with supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE sources plus the locally generated
reachable-history commit diffs used as comparison/ancestry material. Repository/gameplay
source remains UNTOUCHED.

POST-WIP380 STRUCTURED BUILDING-EFFECT SIEVE
--------------------------------------------
After F584's fully omitted building Great-General-rate field, the current-SAS-vs-AdvC
non-default building-effect sieve continued through effects that `AI_buildingValue` DOES
recognize but may still evaluate under assumptions inherited from BtS/K-Mod data.

The next strong seam is `StateReligionCommerces`. Unlike F584, this field has had a
building-AI term since K-Mod. The defect is that the replacement formula treats the XML
amount only as a Boolean gate and then discards its magnitude.

F585 / KI#908 - CONFIRMED
K-MOD'S "MORE ACCURATE" STATE-RELIGION-COMMERCE BUILDING VALUE DROPS THE XML COMMERCE
MAGNITUDE, SO +1 / +2 / +3 EFFECTS RECEIVE THE SAME BASE TERM
--------------------------------------------------------------------------------

ROOT
----
Current SAS `CvCityAI::AI_buildingValue` loops through commerce types and contains:

    if (eStateReligion != NO_RELIGION &&
        kBuilding.getStateReligionCommerce(eLoopCommerce) != 0)
    {
        int iCount = 0;
        ... count active / making buildings of the state religion ...
        iCount = std::max(iCount, kOwner.getHasReligionCount(eStateReligion));
        iTempValue += iCount * 35 *
            kOwner.AI_averageCommerceMultiplier(eLoopCommerce) / 1000;
    }

`kBuilding.getStateReligionCommerce(eLoopCommerce)` is consulted only in the `!= 0`
condition. Its actual integer value never enters the arithmetic.

Consequently, holding commerce type, empire state, counted qualifying buildings and
commerce multiplier constant, candidate values of:

    +1 StateReligionCommerce
    +2 StateReligionCommerce
    +3 StateReligionCommerce
    +100 StateReligionCommerce

all receive exactly the same StateReligionCommerce contribution to `AI_buildingValue`.

This is not merely a debatable heuristic coefficient. The magnitude was present in the
formula that K-Mod replaced.

K-MOD REGRESSION ANCESTRY - PRACTICAL 962
-----------------------------------------
The supplied reachable-history diff
`KMod_962_d72a951d43.diff` is titled:

    Improved the stateReligionCommerce part in CvCityAI::AI_buildingValue.

It shows the original BtS code being commented out:

    if (eStateReligion != NO_RELIGION)
    {
        iTempValue += (kBuilding.getStateReligionCommerce(iI) *
            kOwner.getHasReligionCount(eStateReligion) * 3);
    }

and replaced by K-Mod's comment:

    A more accurate calculation of the value from increasing commerce on all
    state religion buildings. (eg. Sankore)

The replacement improves the *scope/count* model by counting actual state-religion
buildings / buildings in production and falling back to religion presence, but its final
term is only:

    iCount * 35 * AI_averageCommerceMultiplier(...) / 1000

The old `getStateReligionCommerce(iI)` multiplier was not carried into the replacement.

Later K-Mod practical 1030 reformatted / moved this block but retained the same missing
magnitude. Supplied K-Mod 1.46, Base AdvCiv 1.14 and current SAS all still contain the same
semantic formula.

That history is unusually strong evidence that this is an accidental regression inside
a replacement intended to make the calculation *more* accurate, not an intentional
policy that all nonzero XML amounts should be equivalent.

AUTHORITATIVE RUNTIME IS MAGNITUDE-LINEAR
-----------------------------------------
The gameplay effect does preserve the full XML integer.

`CvPlayer::processBuilding` performs for every commerce type:

    changeStateReligionBuildingCommerce(
        c, kBuilding.getStateReligionCommerce(c) * iChange);

`changeStateReligionBuildingCommerce` stores the full signed change in the player's
`m_aiStateReligionBuildingCommerce` and calls `updateCommerce`.

For each active building whose `ReligionType` matches the owner's current state religion,
`CvCity::getBuildingCommerceByBuilding` then adds:

    kOwner.getStateReligionBuildingCommerce(eCommerce) *
        getNumActiveBuilding(eBuilding)

Thus +3 Research is genuinely 50% more base Research per qualifying state-religion
building than +2 Research. Runtime is linear in the XML amount; the AI candidate value is
not.

CURRENT SHIPPED DATA
--------------------
Current SAS has three nonzero building producers:

    BUILDING_NATYA_SHASTRA      [0, 0, 2] -> +2 Culture
    BUILDING_SPIRAL_MINARET     [2]       -> +2 Gold
    BUILDING_SANKORE            [0, 3]    -> +3 Research

Supplied Base AdvCiv 1.14 has:

    BUILDING_SISTINE_CHAPEL     [0, 0, 3] -> +3 Culture
    BUILDING_SPIRAL_MINARET     [2]       -> +2 Gold
    BUILDING_SANKORE            [0, 2]    -> +2 Research

Supplied K-Mod 1.46 has the same +3 Sistine / +2 Minaret / +2 Sankore pattern.

Therefore the magnitude loss was already live in inherited K-Mod/AdvC data: e.g. a +3
Sistine-style effect is not 50% larger than a +2 effect in this part of building value.
Commerce-type weighting can of course make Gold/Research/Culture terms differ, but the
candidate XML magnitude itself has no influence once it is nonzero.

CURRENT SAS AMPLIFICATION - SANKORE +2 -> +3
---------------------------------------------
SAS history gives a particularly clean same-building regression fixture.

`SASBranch_4855_a9d6994c3a.diff` changes Sankore's inherited state-religion Research
bonus from +2 to +3:

    <!-- custom: was 0 / 2 -->
    <StateReligionCommerces>
        <iCommerce>0</iCommerce>
        <iCommerce>3</iCommerce>
    </StateReligionCommerces>

Runtime therefore makes the state-religion-building Research effect 50% stronger than the
inherited +2 version.

But the K-Mod AI formula only tests Research != 0. For the exact same player/city state,
changing Sankore temporarily between +2 and +3 leaves this entire StateReligionCommerce
AI term bit-for-bit identical.

So the deliberate SAS +2 -> +3 data buff has ZERO effect on this portion of AI construction
valuation.

CONCRETE VALUE WITNESS
----------------------
Take one city evaluating Sankore with a state religion, a fixed `iCount` of qualifying
state-religion buildings, and fixed average Research multiplier M.

Current +3 Sankore receives:

    iCount * 35 * M / 1000

A temporary otherwise-identical +2 Sankore receives:

    iCount * 35 * M / 1000

A +1 clone receives the same term again.

At runtime, however, each qualifying religious building gains respectively +3, +2, or +1
base Research. No other state difference is needed to expose the mismatch.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album semantic search found no numbered root owning the loss of
`StateReligionCommerce` MAGNITUDE inside `CvCityAI::AI_buildingValue` or K-Mod practical
962.

It is independent from:

* F476 / KI#799 - runtime cross-city cache invalidation when player-global
  StateReligionBuildingCommerce / state religion changes. F585 exists even if every city
  cache is perfectly updated; it misranks the candidate before construction.

* F577 / KI#900 - BUG/player-facing hypothetical "Actual Effects" building-commerce
  projection evaluates the candidate against old player-global StateReligionCommerce.
  F585 is authoritative AI construction ranking in `CvCityAI::AI_buildingValue`, not the
  preview helper, and survives a complete F577 repair.

* F584 / KI#907 - building `iGreatGeneralRateModifier` has NO positive AI value term at
  all. F585 concerns an existing StateReligionCommerce term whose count logic runs but
  accidentally collapses all nonzero magnitudes to the same value.

* F557 and neighboring `AI_buildingValue` arithmetic findings concern other fields / signs
  and do not own this K-Mod practical-962 magnitude regression.

CLASSIFICATION
--------------
CONFIRMED inherited K-Mod building-AI valuation regression; current-live in K-Mod/AdvC
and amplified by SAS's deliberate Sankore +2 -> +3 data change.

CONSERVATIVE REPAIR DIRECTION - NOT IMPLEMENTED
------------------------------------------------
Preserve K-Mod's improved count of actual / making state-religion buildings, but restore
an explicit dependence on:

    kBuilding.getStateReligionCommerce(eLoopCommerce)

in the final value term.

The exact coefficient is AI tuning rather than a correctness invariant. Blindly
multiplying the existing `35` by the XML amount may over-scale a coefficient that K-Mod
implicitly calibrated around the common +2 effect. A conservative compatibility shape is
to make magnitude linear while re-normalizing the coefficient so the inherited +2 case
stays near its old value, e.g. conceptually:

    legacyTerm * amount / 2

with integer ordering/rounding chosen deliberately. That would make +1 half the inherited
+2 baseline and +3 1.5x it while retaining K-Mod's improved `iCount` semantics.

Alternatively re-tune the coefficient from empirical AI tests, but keep these hard
properties:

1. zero amount -> zero term;
2. positive larger amount -> strictly larger positive term in the same state;
3. if negative StateReligionCommerce is ever supported by data, sign handling must not
   accidentally turn it into a positive benefit merely because the old condition was
   `!= 0`.

REGRESSION TESTS
----------------
1. Magnitude isolation:
   Evaluate otherwise-identical candidate buildings with +1 / +2 / +3 Research
   StateReligionCommerce in the same city/player state. Assert the respective field term is
   monotonic and proportional according to the chosen normalization rather than identical.

2. Current Sankore fixture:
   Compare current +3 Sankore with a temporary +2 clone. Assert the +3 version receives a
   larger AI value from this mechanic while all unrelated terms remain equal.

3. Runtime oracle:
   With N active state-religion buildings, confirm +3 produces exactly 3N additional base
   Research and +2 produces 2N before city commerce modifiers.

4. Zero control:
   A candidate with 0 StateReligionCommerce must receive no term.

5. Existing count semantics:
   Preserve K-Mod's improved counting of active/making state-religion buildings and its
   religion-presence fallback; the fix should restore magnitude, not regress to the coarse
   BtS count model.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F585/KI#908 are CONFIRMED.
F586 / provisional KI#909 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository/gameplay source remains UNTOUCHED.

EXACT CONTINUATION CURSOR
-------------------------
WIP381 -> F586 unused -> leave the now-proved StateReligionCommerce magnitude regression
and Great-General-rate omission behind -> continue the structured current-SAS non-default
building-effect sieve, prioritizing fields whose inherited AI term uses Boolean/sentinel
or hard-coded assumptions rather than the actual XML magnitude -> current caller/data
proof -> whole-album semantic dedup -> promote only an independent root -> freeze WIP382
regardless.

END C031-WIP381


================================================================================
C031-WIP382 — POST-F585 / F586 SEARCH CONTROL TRANCHE
================================================================================

STARTING DURABLE STATE
----------------------
Resumed from C031-WIP381:

* F559/KI#882 through F585/KI#908 CONFIRMED;
* F586 / provisional KI#909 UNUSED;
* Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN;
* actual AdvC-SAS repository/gameplay source UNTOUCHED.

This tranche deliberately does NOT force F586. It records the historical-repair,
semantic-difference, current-data/cardinality and low-coverage controls completed after
WIP381 so that the next search does not need to rediscover them.

PRACTICAL 2292 — EXACT PRIOR CONTROL, NOT A FRESH ROOT
------------------------------------------------------
AdvC practical 2292, the minor wonder-completion cancellation repair, is already owned as
an explicit WIP358 control.

Its relevant one-line correction changes the `failProduction` minimum from zero to -1 so
that a competing wonder order with ZERO stored production can still become the selected
order that is cancelled. Rechecking it against current `failProduction()` does not expose
a sibling current-live defect.

The generalized multi-global-instance branch of `failProduction()` was also audited later:
its loop structure does not match its own "abort only one instance" comment for a
hypothetical world-wonder class with `iMaxGlobalInstances > 1`, but current SAS world
wonders use one global instance. That remains generic/mod-mod robustness rather than F586.

Therefore practical 2292 is crossed off as prior semantic coverage, not counted merely
because the literal practical number occurs rarely in the album.

CORRECTIVE-PRACTICAL CENSUS — LITERAL MENTION COUNT IS NOT SEMANTIC COVERAGE
----------------------------------------------------------------------------
A fresh inventory of AdvC pre-SAS corrective commits touching `CvCity.cpp` initially made
several practical numbers look under-audited. Rechecking the actual roots shows that many
are already semantically covered elsewhere in the album even when their practical number
is sparse.

Examples crossed off here include:

* practical 2187 — trade-route assignment; already covered by the dense trade-route
  lifecycle / reservation findings and controls;
* practical 2193 / the neighboring Great-Person odds work — already represented by the
  Great-Person projection / weighting investigation;
* practical 2292 — exact prior wonder-cancellation control above;
* practical 2339 — lake/ship-production seam already preserved in the earlier production
  controls;
* practical 2144 / 2293 Great-Person odds corrections — already intersect the F562-era
  Great-Person probability/projection work;
* practical 1784 / 2619-era interrupted-production repairs — their mid-turn resource /
  plot-group verification contract was already traced while separating the existing
  resource-verification findings from deliberate `checkCanContinueProduction` narrowing.

Search policy correction: rank *repair assumptions whose environment changed in SAS*, not
just practical numbers that happen to have few literal album hits.

48-CIV / LARGER-DATA REOPENING — RANDOM-RANGE GP RECOVERY STILL OLD-SAVE-ONLY
-----------------------------------------------------------------------------
Because SAS supports substantially larger games, practical 2530 (CvRandom-range overflow)
was reopened as a cardinality/bounds candidate instead of trusting its old environment.

Current `CvCity::doGreatPeople()` still contains the AdvC recovery branch:

    bool const bOverflow =
        (iTotalGreatPeopleUnitProgress > CvRandom::getRange());

When true, both the total and each per-unit progress bucket are independently divided by
100 with ceiling before the random choice.

The earlier album control remains correct:

* separate ceiling of the buckets can make their scaled sum slightly exceed the separately
  ceiled total, so the recovery distribution is not mathematically exact;
* however the source explicitly identifies this path as recovery for malformed old AdvC
  0.97 saves;
* ordinary current-SAS GP thresholds / progress do not approach CvRandom's range merely
  because the game supports 48 civilizations or larger maps.

No current-live F586 is established by the expanded player cardinality.

SAS-vs-ADVC 1.14 SEMANTIC FUNCTION-DIFF PRUNING
-----------------------------------------------
A normalized function-body comparison between supplied Base AdvC 1.14 `CvCity.cpp` and
current SAS leaves only a small finite set of semantic body differences after stripping
comments/formatting. The majority are SAS instrumentation, logging, static-define caches or
already-owned behavior changes.

Several apparently interesting names close immediately against existing policy/repairs:

* `setName(..., bInitial=true)` — SAS's initial-name duplicate bypass is the already
  explicit KI#161.2 behavior;
* `liberate()` — SAS caches the old owner/team before transfer; this is the existing
  KI#486.2 old-owner repair, not a regression;
* production-bar / food-bar differences intersect the already-confirmed F583
  food-production accounting family;
* production-time / decay bodies retain the already-audited parked-production timer
  lifecycle;
* the remaining large SAS `doTurn` differences are dominated by known emergency-production,
  logging and automatic-specialist logic rather than a newly isolated inherited root.

Conclusion: semantic diff against AdvC 1.14 is now close to exhausted as a direct F586
source. Continue using it only when a concrete current-data invariant points back to a
changed body.

PARTHENON GLOBAL-GP MODIFIER — DYNAMIC OUTPUT IS CORRECT; REASSIGNMENT GRANULARITY ONLY
---------------------------------------------------------------------------------------
Current SAS Parthenon has:

    iGlobalGreatPeopleRateModifier = +50

`CvPlayer::processBuilding` applies that modifier through
`changeGreatPeopleRateModifier`. The player mutator itself is intentionally thin and does
not call `AI_makeAssignWorkDirty`.

This initially looked suspicious because `CvCityAI::AI_jobChangeValue` /
`AI_specialistValue` values specialist Great-Person points using the city's current
`getTotalGreatPeopleRateModifier()`.

The runtime/output part is nevertheless coherent:

* `CvCity::getGreatPeopleRate()` reads the total modifier dynamically;
* `CvCity::doGreatPeople()` also reads `getTotalGreatPeopleRateModifier()` dynamically and
  applies it to both total and per-unit progress;
* there is no stale cached GP rate that remains numerically wrong after Parthenon changes
  the player modifier.

What can lag is only citizen *re-optimization* for cities whose assignment was chosen
before the modifier changed during the same player's city-turn sequence. But this is the
same turn-order granularity used for other legitimate empire-wide specialist effects:
individual city turns end with an unconditional `AI_assignWorkingPlots()`, and the next
normal player turn performs the full assignment pass again.

Adding a dirty flag to the player modifier would not by itself cause cities later in the
same city-turn sequence to reassess before their own production / GP tick. Therefore this
is not an isolated broken setter contract and is not promoted as F586.

SHRINE CACHE INDEX ASYMMETRY — REAL API MISMATCH, CURRENT-DATA MASKED
--------------------------------------------------------------------
The structured `processBuilding()` pass preserves one suspicious copy/paste shape:

    if (kBuilding.getHolyCity() != NO_RELIGION)
        m_aiShrine.add(kBuilding.getReligionType(), iChange);

The gate uses `HolyCity`, while the index uses `ReligionType`.

A complete current-building XML check finds seven shipped shrine-like buildings with
`HolyCity != NONE`, and for all seven:

    HolyCity == ReligionType

So the mismatched getter is behaviorally masked by current data. A mod-mod building with a
HolyCity prerequisite different from its ReligionType could corrupt the shrine cache, but
that is not a current SAS F586.

LOW-COVERAGE FUNCTION CENSUS — LARGE FALSE LEAD CORRECTED
---------------------------------------------------------
A fresh function-length x album-mention census was used to identify genuinely neglected
`CvCity` surfaces rather than rereading saturated setters.

`cultureDistance()` initially appeared as roughly 220 lines with zero album mentions, but
that was a parser-boundary artifact: the actual function is simply:

    return std::max(1, plotDistance(0, 0, iDX, iDY));

The following long `cultureStrength()` body had been accidentally attributed to it by the
crude scanner. Therefore no large unaudited `cultureDistance` subsystem exists.

Other zero/one-mention helpers checked in this pass continue to divide mostly into:

* graphical/help-only helpers;
* thin production-storage setters whose lifecycle is already covered;
* projection helpers already bounded by the BUG-preview family;
* rank/invalidation helpers already covered through their producers;
* simple resource/power counters with direct current-state scans.

This is useful pruning even though it does not consume F586.

CURRENT CARDINALITY / PLAYER-TEAM SCAN
--------------------------------------
A targeted `CvCity.cpp` scan for fixed-width storage, `MAX_PLAYERS` /
`MAX_CIV_PLAYERS`, `CvRandom` bounds and obvious short/char truncation does not expose a
new city-state limit reached specifically by SAS's larger player counts.

The only nearby player-count loop in ordinary city logic that stood out is an already
commented-out blockade-defense experiment. Live player/team-indexed city state uses the
engine's EnumMap / typed iteration machinery rather than a new 18-player scalar ceiling.

No 48-civ F586 from this pass.

CURRENT STATUS / NEXT SEARCH DIRECTION
--------------------------------------
No independent current-live root survived this tranche strongly enough to consume F586.
The most useful result is saturation/pruning:

* historical corrective practicals must be ranked semantically, not by literal mentions;
* the direct SAS-vs-AdvC 1.14 semantic diff is nearly exhausted;
* 48-civ cardinality does not reactivate the GP random-range recovery path;
* global GP rate changes are dynamically correct and expose only ordinary intra-turn
  reassignment granularity;
* shrine HolyCity/ReligionType indexing remains current-data masked.

The highest-yield continuation is therefore the strategy that produced F580/F582/F584:
find inherited assumptions that are activated by *current SAS data*, especially
non-default effects crossing an authoritative producer/consumer boundary in
`CvCity::processBuilding`, acquisition, religion/corporation state, production lifecycle
or revolt/culture state.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F585/KI#908 remain CONFIRMED.
F586 / provisional KI#909 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository/gameplay source remains UNTOUCHED.

EXACT CONTINUATION CURSOR
-------------------------
WIP382 -> F586 unused -> current-SAS data-activated inherited-assumption sieve -> prioritize
rare/non-default `CvCity` lifecycle effects and producer/consumer pairs rather than thin
setters -> current caller/XML proof -> K-Mod/AdvC/CE ancestry only when diagnostic ->
whole-album semantic dedup -> promote only an independent root -> freeze WIP383 regardless.

END C031-WIP382


================================================================================
C031-WIP383 — F586 / KI#909 CONFIRMED; TEAMSHARE BUILDING-AI SCOPE MISMATCH
================================================================================

CONTINUATION BASIS
------------------
Continued from C031-WIP382 with F586 / provisional KI#909 unused and Queue 006
`CvGameCoreDLL/CvCity.cpp` open. The immediate lens was the mechanical census of current
non-default BuildingInfo effects that authoritative runtime code applies versus the scope
and magnitude actually represented by `CvCityAI::AI_buildingValue()`.

The audit repository/gameplay source remains completely untouched. This checkpoint changes
only the audit album.

F586 / KI#909 — CONFIRMED
-------------------------
`CvCityAI::AI_buildingValue()` does not account for `bTeamShare` when valuing building
effects that runtime distributes to every member of the builder's team. As a result,
TeamShare world wonders are systematically valued as though their player-global,
area-global and some one-shot global effects benefit only the constructing player.

This is a scope/cardinality mismatch between authoritative runtime application and AI
construction valuation. It is inherited from BtS through K-Mod and Base AdvCiv and is
current-live in SAS data.

AUTHORITATIVE RUNTIME CONTRACT
------------------------------
Current `CvCity::processBuilding()` applies ordinary player-level building effects as:

    for (MemberIter it(getTeam()); it.hasNext(); ++it)
    {
        if (kBuilding.isTeamShare() || it->getID() == getOwner())
            it->processBuilding(eBuilding, iChange, getArea());
    }

Therefore, for a TeamShare building, each current member of the builder's team receives
`CvPlayer::processBuilding(eBuilding, ...)`.

That player function applies, among other fields:

    GlobalGreatPeopleRateModifier
    GreatGeneralRateModifier
    DomesticGreatGeneralRateModifier
    AnarchyModifier
    GoldenAgeModifier
    GlobalHurryModifier
    GlobalFreeExperience
    GlobalWarWearinessModifier
    AreaFreeSpecialist
    GlobalFreeSpecialist
    CoastalTradeRoutes
    AreaTradeRoutes
    AreaHealth / GlobalHealth
    AreaHappiness / GlobalHappiness
    WorkerSpeedModifier
    GlobalSpaceProductionModifier
    AllCityDefenseModifier
    AreaCleanPower / AreaBorderObstacle
    AreaYieldModifier / GlobalYieldModifier
    GlobalCommerceModifier
    SpecialistExtraCommerce
    StateReligionCommerce
    CommerceFlexible
    BuildingHappinessChanges
    SpecialistYieldChange

The exact semantics differ by field, but the important invariant is straightforward:
for `bTeamShare=1`, these effects are not restricted to the constructing player.

The one-shot global-population path independently confirms the same contract. In
`CvCity::setNumRealBuildingTimed(..., bFirst=true)`, a nonzero
`GlobalPopulationChange` loops all team members and skips another member only when the
building is NOT TeamShare:

    for (MemberAIIter it(getTeam()); it.hasNext(); ++it)
    {
        CvPlayerAI const& kMember = *it;
        if (kMember.getID() != getOwner() && !kBuilding.isTeamShare())
            continue;
        FOR_EACH_CITYAI_VAR(pLoopCity, kMember)
            pLoopCity->setPopulation(... + kBuilding.getGlobalPopulationChange());
    }

Thus a TeamShare Hanging Gardens genuinely changes teammate cities as part of the
building's authoritative completion effect.

AI VALUATION CONTRACT VIOLATION
-------------------------------
At the top of current `AI_buildingValue()`:

    int const iNumCities = kOwner.getNumCities();

This is explicitly the constructing player's city count. The function contains no
`kBuilding.isTeamShare()` / `isTeamShare()` handling at all.

Yet several team-distributed effects are valued directly from that owner-only count. For
example:

    iValue += (kBuilding.getGlobalGreatPeopleRateModifier() * iNumCities) / 8;

    iValue += kBuilding.getGlobalHealth() * iNumCities * 4;

    int iFreeSpecialists = kBuilding.getFreeSpecialist() +
            kBuilding.getAreaFreeSpecialist() * iNumCitiesInArea +
            kBuilding.getGlobalFreeSpecialist() * iNumCities;

    iValue += kBuilding.getGlobalPopulationChange() * iNumCities * 4;

Global happiness, global commerce/yield modifiers, global free experience and multiple
other terms likewise scale from the builder's own cities/context. TeamShare breadth is
never added.

This is not merely a disagreement about heuristic coefficients. The formula's population /
city breadth is smaller than the actual set of cities and players that receive the
candidate building's effect.

CLEAN CURRENT SAS REPRODUCER — HANGING GARDENS
----------------------------------------------
Current SAS `BUILDING_HANGING_GARDEN` has:

    bTeamShare              = 1
    iGlobalHealth           = 1
    iGlobalPopulationChange = 1

Consider an ordinary fixed two-player team before construction:

    Player A (builder): 5 cities
    Player B (teammate): 5 cities

Runtime on completion:

* `GlobalPopulationChange=+1` visits both team members through the explicit TeamShare
  completion loop -> +1 population in all 10 cities;
* `GlobalHealth=+1` reaches `CvPlayer::processBuilding()` for both A and B -> +1 global
  building health to both players, hence across the same 10-city team breadth.

But `AI_buildingValue()` sets:

    iNumCities = Player A.getNumCities() = 5

and values the corresponding cardinality-dependent terms as if only those 5 cities receive
the effects.

Using the function's own existing coefficients, the global-health term is based on 5
cities rather than 10, and the global-population term is likewise based on 5 rather than
10. No later TeamShare adjustment restores Player B's benefit.

This creates a direct current-live under-valuation of Hanging Gardens in AI team games.

CURRENT SAS BREADTH
-------------------
A namespace-aware direct parse of the supplied current
`Assets/XML/Buildings/CIV4BuildingInfos.xml` finds 33 globally-limited entries with
`bTeamShare=1`. The current TeamShare buildings with especially clear nonlocal effects
include:

    Pyramids          iGoldenAgeModifier                +50
    Hanging Gardens   iGlobalPopulationChange            +1
                      iGlobalHealth                      +1
    Parthenon         iGlobalGreatPeopleRateModifier    +50
    Hagia Sophia      iGlobalHappiness                    +2
    Notre Dame        iGlobalFreeSpecialist               +1
    Taj Mahal         iGlobalWarWearinessModifier        -25
    Eiffel Tower      iGlobalHappiness                    +1
    Statue of Liberty iAreaFreeSpecialist                 +1

For the ongoing player/area effects, current `CvCity::processBuilding()` distributes the
source through each team member's `CvPlayer::processBuilding()`, but the corresponding AI
terms are evaluated from the builder's own player/area state only.

Some effects need more than a simple team city-count correction. For example, Pyramids'
CivicOption/GoldenAge-related value can depend on each teammate's civic and Golden Age
context. The finding is therefore broader than one missing multiplication: the valuation
layer lacks the TeamShare scope dimension entirely.

ANCESTRY — BASE BTS -> K-MOD -> ADVC -> SAS
-------------------------------------------
The supplied Civ4CE BtS source is a useful original-lineage oracle here. Its
`CvCity::processBuilding()` already distributes TeamShare buildings across team members,
while BtS `CvCityAI::AI_buildingValue()` contains the same owner-only formulas, including:

    GlobalGreatPeopleRateModifier * iNumCities
    GlobalPopulationChange * iNumCities

and no building TeamShare valuation branch.

Supplied K-Mod 1.46 preserves the same pattern. Supplied Base AdvCiv 1.14 likewise has:

    iValue += (kBuilding.getGlobalGreatPeopleRateModifier() * iNumCities) / 8;
    ...
    iValue += kBuilding.getGlobalPopulationChange() * iNumCities * 4;

with no `isTeamShare()` use in `CvCityAI.cpp` building valuation.

Hanging Gardens is TeamShare with +1 GlobalPopulationChange and +1 GlobalHealth in the
supplied K-Mod, Base AdvCiv 1.14 and current SAS XML, so this is not a SAS-created data
combination. Classification:

    inherited BtS/K-Mod/AdvC AI-building scope omission, current-live in ordinary team play.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album semantic searches for TeamShare + AI/building/value/team-member breadth found
no numbered finding owning this issue.

It is distinct from:

* the WIP181/WIP209/WIP380 TeamShare Permanent-Alliance area-counter controls, which concern
  replay/removal of authoritative stored counters after team reassignment; F586 concerns
  construction AI valuing a correctly-applied live TeamShare effect with owner-only scope;
* F584 / KI#907, where `GreatGeneralRateModifier` had no positive building valuation at all;
  F586 applies to fields that DO have AI value terms, but those terms use the wrong
  recipient breadth when the building is TeamShare;
* F585 / KI#908, where StateReligionCommerce loses its XML magnitude inside K-Mod's
  replacement formula; F586 preserves magnitude but omits teammate recipients;
* acquisition/prospective-owner findings F581/F582, which concern hypothetical ownership
  of one foreign city rather than team distribution of a constructed wonder.

CORRECTION TO WIP377 CONTROL
----------------------------
WIP377 contained the statement that current SAS had no building with nonzero
`iGlobalPopulationChange`, and therefore treated the Civ4CE Hanging Gardens historical
item as current-data masked.

That statement is factually wrong for the supplied current snapshot. A direct
namespace-aware parse and literal XML inspection both show:

    BUILDING_HANGING_GARDEN
    iGlobalPopulationChange = 1

The generic path is current-live, and it becomes the clean primary F586 fixture. Preserve
WIP377 as historical audit context, but this WIP supersedes that specific XML conclusion.

REPAIR DIRECTION — NOT IMPLEMENTED
----------------------------------
Do not simply multiply every building value by team size. Local city effects remain local,
and several TeamShare player effects are contextual per recipient.

A conservative repair should introduce TeamShare-aware recipient state only for effects
that runtime actually distributes through `CvPlayer::processBuilding()` or the explicit
TeamShare one-shot block. At minimum:

1. derive an effect-recipient city count:

       own cities
       + teammate cities when kBuilding.isTeamShare()

   and a matching same-area recipient count for area effects;

2. use those counts in cardinality-based global/area terms such as GlobalPopulationChange,
   GlobalHealth/Happiness, GlobalFreeSpecialist, GlobalGreatPeopleRateModifier, etc.;

3. for recipient-context-sensitive effects (CivicOption, GoldenAgeModifier,
   StateReligionCommerce, specialist modifiers, perhaps war-weariness), ideally evaluate
   the teammate's own relevant state rather than approximating everything from the builder;

4. keep one-player teams bit-for-bit unchanged.

The narrowest safe first patch could correct only the simple count-linear fields while
leaving more contextual TeamShare valuation as documented follow-up, but the root should
remain described as missing recipient-team scope rather than separate bugs per field.

REGRESSION TESTS
----------------
1. Hanging Gardens fixed-team cardinality:
   Two same-team players with 5 cities each. Compare current Hanging Gardens to an
   otherwise-identical non-TeamShare clone. Assert the TeamShare version receives extra AI
   value representing Player B's five GlobalPopulationChange and GlobalHealth recipients.

2. One-player-team neutrality:
   With no teammate, patched value must match the pre-fix owner-only calculation.

3. Parthenon global modifier breadth:
   Same-team players with different city counts; +50 GlobalGreatPeopleRateModifier should
   gain value as teammate recipient cities are added.

4. Area scope:
   Statue of Liberty's AreaFreeSpecialist should include teammate cities in the wonder's
   area but not teammate cities on another landmass.

5. Sign preservation:
   TeamShare positive benefits and negative modifiers must retain existing sign semantics;
   e.g. Taj Mahal's -25 GlobalWarWearinessModifier is a beneficial reduction and should
   gain additional positive value for eligible teammate breadth, not flip sign.

6. Runtime oracle:
   Independently confirm the underlying TeamShare wonder still applies its player/area
   effect once per current team member; the AI repair must follow that runtime scope rather
   than change runtime behavior.

POST-F586 CONTINUATION
----------------------
With the generic TeamShare scope omission now owned by F586, do not create sibling findings
for each TeamShare field unless a separate arithmetic/sign/lifecycle contradiction exists.

Continue Queue 006 by returning to the remaining runtime-effect vs `AI_buildingValue()`
coverage census, explicitly excluding:

* F584 missing GreatGeneralRateModifier value;
* F585 StateReligionCommerce magnitude loss;
* F586 TeamShare recipient breadth.

Next priority: current non-default fields where AI tests only a sentinel/boolean or applies
a magnitude with the wrong baseline/sign, then authoritative CvCity lifecycle/state paths
outside building valuation if this census saturates.

NUMBERING / DURABLE STATE
-------------------------
F559/KI#882 through F586/KI#909 are CONFIRMED.
F587 / provisional KI#910 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository/gameplay source remains completely UNTOUCHED.

EXACT CONTINUATION CURSOR
-------------------------
WIP383 -> F587 unused -> continue current non-default runtime-building-effect vs
`AI_buildingValue` magnitude/sentinel census, with TeamShare breadth now excluded as F586 ->
current XML/caller proof -> K-Mod/AdvC/CE ancestry when diagnostic -> whole-album semantic
dedup -> promote only an independent root -> freeze WIP384 regardless.

END C031-WIP383

===============================================================================
C031-WIP384 — QUEUE 006 CvCity.cpp / BUILDING-AI RUNTIME-EFFECT CENSUS — F587 CONFIRMED
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.

F559-F587 CONFIRMED.
F588 / KI#911 UNUSED.
No gameplay/repository source files were modified.

This stone continues from C031-WIP383's systematic comparison between current
non-default building runtime effects and `CvCityAI::AI_buildingValue`. It also
preserves two current-data-masked magnitude/sign controls so those do not need
rediscovery.

-------------------------------------------------------------------------------
POST-WIP383 CONTROL — FREE-TECH BUILDING VALUE DROPS THE XML COUNT, BUT CURRENT
SAS ONLY SHIPS A ONE-FREE-TECH PRODUCER
-------------------------------------------------------------------------------

The inherited free-tech valuation checks:

    if (kBuilding.getFreeTechs() > 0)
    {
        ... estimate an available technology value ...
        iValue += iTechValue * 8 / researchPercent;
    }

The replacement term does not multiply by `kBuilding.getFreeTechs()`. The old
BtS-style code did contain an explicit count multiplier (e.g.
`getFreeTechs() * 80`). Thus a hypothetical two- or three-free-tech building
would receive essentially the same free-tech term as a one-tech building,
apart from whatever state changed between evaluations.

Current SAS does not expose that generalized defect in ordinary data: Oracle is
the only current building with nonzero `iFreeTechs`, and its value is exactly 1.
Keep this as a future/mod-mod magnitude control rather than F587.

-------------------------------------------------------------------------------
POST-WIP383 CONTROL / CORRECTION — NEGATIVE MILITARY-PRODUCTION VALUATION IS
NOT GENERICALLY MASKED; BARBARIAN PALACE MAKES IT CURRENT-LIVE
-------------------------------------------------------------------------------

An initial pass almost closed the signed-domain omission because the Barbarian
civilization receives BUILDINGCLASS_PALACE as a free building class and the
first city therefore starts with `BUILDING_BARBARIAN_PALACE` for free.
That closure was too broad and is superseded by F587 below.

The important lifecycle distinction is:

* `BUILDING_BARBARIAN_PALACE` has `bCapital=0`; it is a government center, not
  the ordinary capital marker.
* `CvCity::init` grants civilization `FreeBuildingClasses` only when the player
  has exactly one city at initialization.
* `BUILDING_BARBARIAN_PALACE` has `bNeverCapture=1`, so ordinary conquest does
  not restore it to the conquering civ in `CvPlayer::acquireCity`.
* If Barbarians still own another city after the original Palace city is
  captured, no normal `findNewCapital()` relocation recreates this non-capital
  building there.
* Once the Palace-class count is back to zero, an ordinary remaining barbarian
  city that is not already a government center can legally construct the
  mapped Barbarian Palace: it has cost 5 and no technology prerequisite.
* `CvCityAI::AI_barbChooseProduction()` explicitly calls `AI_chooseBuilding()`;
  therefore `AI_bestBuildingThreshold` / `AI_buildingValue` is a live caller
  for the reconstructed building.

So the signed-domain omission below is not merely dormant XML robustness.

-------------------------------------------------------------------------------
F587 / KI#910 — AI_buildingValue ignores negative building military-production
modifiers; current Barbarian Palace can be rebuilt after conquest and its -25%
military-production drawback receives zero construction-value penalty
-------------------------------------------------------------------------------

ROOT
----
`CvCity::processBuilding()` applies a building's military-production modifier
with its full signed XML value:

    changeMilitaryProductionModifier(
        kBuilding.getMilitaryProductionModifier() * iChange);

`CvCity::getProductionModifier(UnitTypes)` then applies that authoritative city
state to every military-production unit:

    if (GC.getInfo(eUnit).isMilitaryProduction())
        iMultiplier += getMilitaryProductionModifier();

and clamps only the final total at -50. Thus a negative building value is a
real production penalty, not metadata.

`CvCityAI::AI_buildingValue()`, however, gates the entire building-side
military-production valuation on positivity:

    const int iMilitaryProductionModifier =
        kBuilding.getMilitaryProductionModifier();

    if (iHasMetCount > 0 && iMilitaryProductionModifier > 0)
    {
        ... add positive military-production value ...
    }

There is no corresponding `< 0` branch or later signed correction. A negative
modifier therefore contributes exactly zero through this mechanic even though
runtime immediately slows military-unit production.

CURRENT SAS ACTIVATION
----------------------
A namespace-aware/current-XML census gives these nonzero building values:

    BUILDING_BARBARIAN_PALACE   -25
    BUILDING_MONGOL_GER          +15
    BUILDING_HEROIC_EPIC         +50
    BUILDING_MT_RUSHMORE         +15
    BUILDING_PENTAGON            +15

Supplied AdvC 1.14 and K-Mod 1.46 do not ship any negative building value in
this field; their active producers are positive (Heroic Epic and Military
Academy). The inherited K-Mod/AdvC positive-only branch was therefore dormant
for negative building data until SAS activated the signed domain.

CLEAN CURRENT-LIVE BARBARIAN RECONSTRUCTION WITNESS
---------------------------------------------------
Construct the following normal gameplay state:

1. Barbarians own at least two cities, A and B.
2. A was their first city and received the civilization's free Palace class,
   mapped to `BUILDING_BARBARIAN_PALACE`.
3. A is conquered by a normal civilization while B survives.
4. Because Barbarian Palace has `bNeverCapture=1`, the conquest reconstruction
   skips it; the Barbarian Palace-class count becomes zero.
5. B is not already a government center. Barbarian Palace has no tech
   prerequisite, only 5 hammer cost, and is the Barbarian civilization's legal
   Palace-class building, so B can construct it once the class is absent.
6. `AI_barbChooseProduction()` reaches its ordinary `AI_chooseBuilding()` calls,
   which evaluate available buildings through `AI_buildingValue()`.

At that point the candidate Palace's `-25` military-production effect is a real
marginal drawback. If B constructs it, every military-production unit gets 25
percentage points less production modifier through
`CvCity::getProductionModifier(UnitTypes)`.

But the building AI's positive-only gate contributes no negative value at all.
The Palace is ranked from its other effects/cost while the military-production
penalty is silently omitted. That can raise it above competing infrastructure
or units relative to its actual post-construction value.

This is particularly relevant to Barbarian behavior because
`AI_barbChooseProduction()` deliberately alternates building choices with a
strong military-unit-production policy. A building that makes those subsequent
military units slower is exactly the kind of effect the construction ranking
needs to price.

ANCESTRY / CLASSIFICATION
-------------------------
The positive-only check is inherited through K-Mod and AdvC. Their supplied
building XML does not use a negative `iMilitaryProductionModifier`, so the
problem is best classified as an inherited signed-domain assumption activated
by current SAS data.

It is not a general claim that K-Mod's positive valuation formula can simply be
run unchanged with a negative number: parts of that formula select preferred
high-production wonder cities and reward synergy with existing free XP. The
hard correctness point is narrower: the current function reads the field,
accepts it as signed runtime data, and then drops the entire negative domain.
A repair should add a deliberate penalty model for `< 0` rather than blindly
remove the positivity gate.

DEDUP
-----
Whole-album semantic search found no existing root for a negative
`iMilitaryProductionModifier` being ignored by AI construction value.

Distinct nearby families include:

* F398 — BUG/player-facing additional-production hover incorrectly treats
  conditional military/domain/space modifiers as generic hammer yield;
* F584 — building AI entirely omits `iGreatGeneralRateModifier`;
* F585 — StateReligionCommerce AI term retains nonzero/type context but drops
  the XML amount;
* F586 — TeamShare recipient breadth is omitted from building valuation.

F587 is a separate signed-domain construction-ranking error with its own
current producer and ordinary barbarian caller.

REPAIR BOUNDARY
---------------
Preserve the current positive-value logic. Add an explicit negative-modifier
valuation path that penalizes a candidate according to expected military-unit
production lost in this city. Keep the sign handling separate from the
positive limited-wonder placement bonuses unless a broader formula is proved
safe for both domains.

At minimum, regression tests should establish:

1. positive Heroic-Epic/Ger/Pentagon-style values retain current behavior;
2. a constructible building with `iMilitaryProductionModifier=-25` receives a
   lower value than the otherwise-identical zero-modifier building;
3. Barbarian Palace is tested in the post-conquest reconstruction state above,
   not only in the first-city-free state;
4. runtime unit-production math remains unchanged; this is an AI valuation
   repair only;
5. `iHasMetCount==0` does not accidentally erase a real negative production
   penalty merely because the old positive-benefit heuristic waited until the
   AI had met another civilization.

-------------------------------------------------------------------------------
TEAM / GAME BUILDING-EFFECT CENSUS — NO EXTRA NUMBER IN THIS STONE
-------------------------------------------------------------------------------

After F586, the runtime-effect census was widened beyond `CvPlayer::processBuilding`.

`CvTeam::processBuilding()` currently applies, among other things:

* force-team-vote eligibility for a building's vote source;
* map centering;
* enemy war-weariness modifier.

`CvGame::processBuilding()` updates the building's diplomatic vote source.

Current data checks:

* Statue of Zeus' +50 enemy-war-weariness modifier is magnitude-aware in AI
  valuation;
* UN / Apostolic Palace vote-source effects receive the inherited vote-source
  valuation term; `bForceTeamVoteEligible` is not separately priced, but the
  current data ties it to those vote-source buildings and this pass did not
  prove an independent ranking contradiction from that bundling;
* Stonehenge map-centering remains a tiny structural effect, not a promoted
  F588 candidate here.

Do not split those into findings without a concrete independent current-state
score or lifecycle contradiction.

CURRENT STONE / CONTINUATION
----------------------------
F587 / KI#910 CONFIRMED.
F588 / KI#911 UNUSED.
Queue 006 `CvCity.cpp` remains OPEN.
Source/gameplay tree untouched.

Continue from the runtime-effect-vs-AI census with F587's signed
MilitaryProduction family excluded as owned breadth. Next priorities:

* remaining structural team/game building booleans and count-valued effects;
* other current XML fields whose AI path gates on `> 0`, `!= 0`, or a sentinel
  but may accept a wider runtime domain;
* one-shot / acquisition / religion / corporation state combinations activated
  by SAS data;
* current reachability and whole-album semantic dedup before consuming F588.

END C031-WIP384

===============================================================================
C031-WIP385 — F587 CAPTURE-PATH PRECISION + F495 RE-HIT — F588 UNUSED
===============================================================================

STATUS
------
F559-F587 CONFIRMED.
F588 / KI#911 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
No gameplay/repository source files were modified.

-------------------------------------------------------------------------------
F587 PRECISION CORRECTION — CURRENT UNPATCHED ACQUISITION DESTROYS THE BARBARIAN
PALACE THROUGH THE ALREADY-OWNED F495 SOURCE/DESTINATION BUG, NOT DIRECTLY FROM
THE SOURCE BUILDING'S bNeverCapture CHECK
-------------------------------------------------------------------------------

WIP384 correctly established the important gameplay outcome needed by F587:
a Barbarian Palace can disappear when its city is conquered while another
Barbarian city survives, leaving the surviving city able to evaluate/rebuild
the Palace through `AI_barbChooseProduction` -> `AI_chooseBuilding`.

One sentence in WIP384 described today's exact conquest mechanism too simply.
Current AdvC/SAS `acquireCity()` first remaps a non-world source building class
to the RECIPIENT civilization's concrete building type:

    BuildingTypes const eBuilding =
        (GC.getInfo(eLoopBuilding).isWorldWonder() ?
         eLoopBuilding : getCivilization().getBuilding(eBuildingClass));

For an ordinary major civilization capturing a Barbarian Palace:

    source      = BUILDING_BARBARIAN_PALACE
    class       = BUILDINGCLASS_PALACE
    destination = BUILDING_PALACE

The subsequent current-code `kBuilding.isNeverCapture()` check therefore reads
the mapped normal Palace, not the source Barbarian Palace. Likewise the current
conquest-probability lookup reads the destination Palace.

More importantly, the restoration count is the already-confirmed F495 bug:

    aiNumRealBuilding.get(eBuilding)

instead of the source snapshot:

    aiNumRealBuilding.get(eLoopBuilding)

The old barbarian city has a Barbarian-Palace count, not a normal-Palace count,
so the destination-key read is zero and no Palace is reconstructed in the
captured replacement city.

Thus the present shipped path still removes the Barbarian Palace exactly as
F587 needs; the immediate current mechanism is F495's wrong source/destination
snapshot key rather than a direct source `bNeverCapture` skip.

F587 ALSO SURVIVES THE PLANNED F495 REPAIR
------------------------------------------
This precision correction does not make F587 depend on leaving F495 unfixed.
F495's recorded repair direction restores explicit source/destination identity
and also calls out that K-Mod/BtS use SOURCE-building conquest policy.

Under that intended repair:

    source BUILDING_BARBARIAN_PALACE has bNeverCapture = 1

so ordinary conquest still removes the source building. Therefore a state with
another surviving Barbarian city still reaches the post-loss reconstruction
choice and still exercises F587's ignored `-25` military-production drawback.

Keep F587 / KI#910 CONFIRMED. Supersede only WIP384's simplified statement of
the exact current acquisition branch.

-------------------------------------------------------------------------------
F588 CANDIDATE — acquireCity UNIQUE-BUILDING CONVERSION SNAPSHOT MISINDEX
CLOSED AS EXACT F495 / KI#818 RE-HIT
-------------------------------------------------------------------------------

The reconstruction pass rediscovered this very strong-looking shape:

1. old-city building data are snapshotted by SOURCE `BuildingTypes`;
2. non-world buildings are mapped to the recipient civilization's DESTINATION
   `BuildingTypes`;
3. current AdvC/SAS restoration reads:

       aiNumRealBuilding.get(eBuilding)
       aeBuildingOriginalOwner.get(eBuilding)
       aiBuildingOriginalTime.get(eBuilding)

   where `eBuilding` is the mapped destination type.

For example, a Maya Ball Court transferred to a civilization using ordinary
Colosseum maps to `BUILDING_COLOSSEUM`, whose old-city snapshot slot is zero,
so the class building disappears rather than converting.

This is NOT F588. The reconciled album already owns the exact root as:

    F495 / KI#818 — AdvCiv acquireCity indexes the old-city building snapshot by
    the recipient's mapped BuildingType, so ordinary cross-civilization unique-
    building conversions disappear on city transfer.

C031-WIP235 contains the current Maya Ball Court <-> Colosseum reproducer,
source/destination count and provenance proof, broad current-UB mapping census,
K-Mod/Civ4CE ancestry, and repair/tests.

C031-WIP236 further identifies practical 2416's EnumMap refactor as the actual
regression and practical 2438 as a partial source-loop repair that left the
later destination-key reads wrong.

That earlier finding also already preserves the adjacent conquest-policy
change: current AdvC/SAS reads destination `isNeverCapture` and
`getConquestProbability`, whereas K-Mod/BtS read source policy. Current
Barbarian Granary/Harbor/Lighthouse 33% vs ordinary 66% values were explicitly
recorded there as an F495 sibling to resolve when the primary conversion bug is
fixed.

Do not consume F588 on any of these acquisition source/destination symptoms.

CURRENT STONE / CONTINUATION
----------------------------
F587 / KI#910 CONFIRMED.
F588 / KI#911 UNUSED.
Queue 006 remains OPEN.
Source/gameplay tree untouched.

Leave the saturated `acquireCity` source/destination-building family behind.
Continue F588 from a genuinely different authoritative lifecycle surface:
religion/corporation transitions, production/order state, or another current
SAS data combination that changes an inherited assumption. Keep current
reachability + ancestry + whole-album semantic dedup before numbering.

END C031-WIP385


===============================================================================
C031-WIP386 - POST-WIP385 STATE / RELIGION / CORPORATION / PROCESS CONTROLS
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F587 / KI#910 remain CONFIRMED.
F588 / KI#911 remains UNUSED.
Actual AdvCiv-SAS gameplay/source remains UNTOUCHED.

PURPOSE OF THIS TRANCHE
-----------------------
Continue beyond WIP385 without reopening the saturated `acquireCity` source/destination-building
family owned by F495. Re-enter genuinely authoritative low-coverage city state: base-yield
recomputation, WLTKD/vassal state, religion/corporation transitions, headquarters one-shot rewards,
prospective maintenance boundaries, and production/process queue state. Promote only a current-live
independent contradiction after whole-album semantic dedup.

BASE-YIELD / WLTKD / VASSAL CONTROLS
------------------------------------
`CvCity::calculateBaseYieldRate()` is a direct invariant recomputation of the already-applied plot,
specialist/free-specialist, active-building, trade and corporation yield sources. No producer was
found whose state is omitted from the recomputation or whose stored base yield diverges under
current SAS data.

WLTKD is likewise internally coherent: `setWeLoveTheKingDay` mutates the boolean and immediately
calls `updateMaintenance`; `isNoMaintenance()` reads WLTKD dynamically. No stale secondary cache or
turn-lagged maintenance state was found.

The AdvC vassal happiness/unhappiness formulas differ from BtS/K-Mod but are explicit AdvC policy,
not stale counting: one voluntary-vassal happiness bonus and the intended any-vassal penalty. Do not
promote this policy divergence as F588.

RELIGION / CORPORATION TRANSITION CONTROLS
------------------------------------------
`setHasReligion()` retains the already-audited vote-source ordering: remove currently active
vote-source effects before changing the religion bit, flip owner/city religion state, then reapply
vote-source effects under the new state. This is a prior control, not an F588 candidate.

`setHasCorporation()` updates corporation output from authoritative state rather than accumulating
incremental deltas. When the changed city is also headquarters, the surrounding headquarters update
can cause an extra recomputation, but the operation is idempotent and cannot double-add yield or
commerce. The generic produced-bonus dependency-chain concern remains the previously preserved
current-data-masked corporation control; no current SAS produced corporation bonus is consumed by
another producing corporation.

HEADQUARTERS ONE-SHOT FREE-UNIT CONTROL
---------------------------------------
`CvCity::setHeadquarters()` is the founding wrapper: it calls `CvGame::setHeadquarters` and then
creates the corporation's configured free unit. That initially looked dangerous for HQ relocation.
The ownership/reconstruction path does not call this wrapper, however. `acquireCity()` restores an
HQ through `CvGame::setHeadquarters` directly. The game-level setter updates old/new HQ corporation
state but does not mint the founding free unit. Therefore capture, peaceful transfer, or ordinary HQ
restoration does not grant a second founder/executive. No F588.

PROSPECTIVE CORPORATION-MAINTENANCE BOUNDARY
--------------------------------------------
F581/F582 make prospective maintenance a high-value audit seam, but corporation maintenance inside
`calculateBaseMaintenanceTimes100(eOwner)` is intentionally excluded for a foreign prospective
owner. The source states explicitly:

    // advc.ctr: Anticipating corporation maintenance gets too complicated
    if (eOwner == getOwner())
        iR += calculateCorporationMaintenanceTimes100();

Therefore this is an acknowledged approximation, unlike F581's missing hypothetical city/area +1
or F582's retained-government-center omission. Do not relabel it as a broken eOwner contract.

EXACT RE-HIT - CULTURE PROCESS `iData2 > 0` IS WIP367'S CLOSED LEVEL-0 CASE
--------------------------------------------------------------------------
The process-order lifecycle again surfaced the apparently suspicious K-Mod pair:

    // pushOrder, human Culture process
    iData2 = getCultureLevel();

and later:

    if (kOrder.iData2 > 0 && ... &&
        getCultureLevel() > kOrder.iData2)
    {
        popOrder(0, false, ALL_CHOOSE);
    }

At first glance `-1` is the unset sentinel while culture level 0 is a valid enum, suggesting a
classic sentinel-vs-zero bug. Whole-album dedup finds this exact candidate already closed in
C031-WIP367. Current `calculateCultureLevel()` scans levels in reverse and both NONE and POOR have a
0 threshold; an ordinary non-occupation city with 0 culture therefore resolves to POOR (1), so the
human process stores a positive marker. NONE (0) is primarily forced occupation state and does not
provide a clean ordinary first-border-expansion witness. F494 remains the real live Culture-process
metadata defect (AI Auto Play hand-back), not this level-0 branch.

PRODUCTION-QUEUE LIFECYCLE CONTROL
----------------------------------
`pushOrder` / `popOrder` / `startHeadOrder` / `stopHeadOrder` remain heavily covered. The duplicated
`cityBuildingUnit` / `cityBuildingBuilding` event dispatch is the existing album control; current
shipped handlers do not establish a new authoritative queue mutation. Removing or replacing an
ORDER_MAINTAIN head updates its ProductionToCommerce modifiers through `processProcess`; a new head
runs `startHeadOrder`, and normal AI/automated empty-queue flow chooses a replacement order. No new
stored making-counter or process-state contradiction was established here.

CURRENT STONE / CONTINUATION
----------------------------
Durable state after this tranche:

    F559-F587 CONFIRMED
    F588 / KI#911 UNUSED
    Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN
    gameplay/source UNTOUCHED

Continue from a different hard-state lens rather than squeezing these closed branches. Good next
surfaces are remaining runtime producer/consumer invariants whose assumptions SAS data can activate:
rare signed/non-default city fields, event/espionage state consumed before the unconditional
end-turn reassignment, religion/corporation one-shot state outside the now-closed HQ wrapper, or
another low-coverage historical repair sibling. Current-SAS reachability and whole-album semantic
dedup remain mandatory. Freeze WIP387 regardless of whether F588 is consumed.

END C031-WIP386


===============================================================================
C031-WIP387 - F588 / KI#911 CONFIRMED - TEAMSHARE GOLDEN AGE IS OWNER-ONLY
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F588 / KI#911 are now CONFIRMED.
F589 / KI#912 is UNUSED.
Actual AdvCiv-SAS gameplay/source remains UNTOUCHED.

-------------------------------------------------------------------------------
F588 / KI#911 - TEAMSHARE TAJ MAHAL / VERSAILLES GOLDEN AGE IS GRANTED ONLY TO
THE CONSTRUCTING PLAYER
-------------------------------------------------------------------------------

ROOT
----
`CvCity::setNumRealBuildingTimed()` handles one-shot building rewards after a
new real building is added with `bFirst=true`.

For a Golden-Age building, current AdvC/SAS mutates only the city owner:

    if (kBuilding.isGoldenAge())
    {
        if (bEndOfTurn)
        {
            for (int i = 0; i < iChange; i++)
                kOwner.startGoldenAgeDelayed();
        }
        else kOwner.changeGoldenAgeTurns(iChange * kOwner.getGoldenAgeLength());
    }

There is no `kBuilding.isTeamShare()` check and no team-member loop.
`startGoldenAgeDelayed()` and `changeGoldenAgeTurns()` are both player-local;
no later team-level mechanism propagates either state to teammates.

The immediately adjacent one-shot global-population effect has the missing
scope logic explicitly:

    if (kBuilding.getGlobalPopulationChange() != 0)
    {
        for (MemberAIIter it(getTeam()); it.hasNext(); ++it)
        {
            CvPlayerAI const& kMember = *it;
            if (kMember.getID() != getOwner() && !kBuilding.isTeamShare())
                continue;
            ... change every qualifying member city population ...
        }
    }

That contrast makes this a runtime TeamShare scope omission, not merely an AI
valuation preference.

CURRENT LIVE DATA
-----------------
Current SAS ships two buildings with BOTH:

    bTeamShare = 1
    bGoldenAge = 1

namely:

    BUILDING_TAJ_MAHAL
    BUILDING_VERSAILLES

Taj Mahal already has this combination in the supplied AdvC / BtS lineage.
SAS additionally changed Versailles from `bGoldenAge=0` to `bGoldenAge=1`, so
it supplies a second current witness.

The other TeamShare one-shot combinations support the same scope contract:

* Hanging Gardens: `iGlobalPopulationChange=+1`; the code explicitly loops all
  team members when `bTeamShare=1`.
* Oracle: `iFreeTechs=1`; the free technology is selected by one player but
  technology ownership is team-level, so the reward is intrinsically shared.

Golden Age state is different: it is stored on `CvPlayer`, not `CvTeam`, and
therefore requires explicit teammate application when a TeamShare building is
supposed to grant the reward team-wide. Current code supplies none.

CLEAN CURRENT-LIVE REPRODUCER
-----------------------------
Use a normal fixed team with two alive players A and B.

1. A and B are on the same team before the wonder is completed.
2. A completes Taj Mahal normally at end of turn.
3. `setNumRealBuildingTimed(..., bFirst=true, bEndOfTurn=true)` reaches the
   Golden-Age one-shot block.
4. Only A executes `startGoldenAgeDelayed()`; A's
   `m_iScheduledGoldenAges` increases.
5. B receives no scheduled Golden Age and no immediate Golden Age turns.
6. On player activation, A converts its scheduled Golden Age to turns; B has
   no corresponding state to convert.

The same owner-only result occurs through the immediate/non-end-turn path,
which calls only A's `changeGoldenAgeTurns()`.

Current SAS Versailles reproduces the same root because it is also TeamShare
and now has `bGoldenAge=1`.

ANCESTRY
--------
The supplied Civ4CE BtS body already has the mismatch:

* Golden-Age one-shot -> `GET_PLAYER(getOwnerINLINE()).changeGoldenAgeTurns(...)`
  only;
* adjacent global-population one-shot -> loops same-team players and checks
  `isTeamShare()`.

K-Mod preserves that structure. AdvC preserves it as well. Practical 2208
(advc.001x, "Fix issues with Taj Mahal Golden Age length") changes WHEN the
builder's wonder Golden Age begins by introducing delayed scheduling at normal
end-of-turn completion, but it does not change WHO receives the TeamShare
reward. Therefore F588 is inherited from BtS rather than introduced by
practical 2208.

SAS activates additional breadth by making Versailles a Golden-Age wonder.

TEAMSHARE CONTRACT BOUNDARY
---------------------------
This finding does NOT claim that every field in a TeamShare BuildingInfo is
blindly copied to every teammate. City-local effects such as the building
city's Great-Person points remain local by design.

The narrower contract is player/global reward scope:

* persistent player-level building effects are explicitly replayed to every
  team member by `CvCity::processBuilding()` when `bTeamShare=1`;
* the adjacent one-shot global-population reward explicitly applies to every
  team member when `bTeamShare=1`;
* a free technology is team-owned by the game's technology model;
* the one-shot Golden Age is likewise a player-global reward, but is the sole
  current TeamShare one-shot in this block that remains owner-only because its
  state lives on `CvPlayer` and the code omits the teammate loop.

That is sufficient to classify the owner-only Golden Age as a separate runtime
scope contradiction.

DEDUP
-----
Whole-album semantic searches for TeamShare + Golden Age / Taj / Versailles /
scheduled Golden Ages / teammates found no existing runtime root.

Distinct nearby families:

* F586 / KI#909: `AI_buildingValue()` values correctly-applied TeamShare
  effects using only the builder's own city/player scope. That is an AI score
  omission. F588 is the authoritative runtime reward itself being owner-only.
* WIP181/WIP209/WIP380: Permanent-Alliance TeamShare replay / area-counter
  robustness. Those concern persistent TeamShare replay across team merging,
  not a one-shot Golden Age at construction.
* practical 2208 control: delayed Taj Golden Age timing/length is correct for
  the builder; F588 concerns recipient breadth, not timing.

REPAIR BOUNDARY
---------------
Keep practical 2208's delayed-start semantics. Apply the Golden-Age one-shot to
all current team members when `kBuilding.isTeamShare()`, otherwise only to the
owner, mirroring the recipient selection used by the adjacent global-
population block.

For each qualifying recipient:

* normal end-of-turn completion: call that recipient's
  `startGoldenAgeDelayed()` once per `iChange`;
* immediate/non-end-turn completion: add
  `iChange * kRecipient.getGoldenAgeLength()` to that recipient.

Use each recipient's own Golden Age length state rather than copying the
builder's duration; Pyramids and future player-specific modifiers can differ.
Do not change `bFirst=false` acquisition/reconstruction behavior: captured or
transferred Taj/Versailles must not re-fire the one-shot reward.

REGRESSION TESTS
----------------
1. Two-player fixed team: A completes Taj -> both A and B schedule/receive one
   Golden Age.
2. Current SAS Versailles reproduces the same team-wide behavior.
3. Non-TeamShare synthetic Golden-Age building remains owner-only.
4. Single-player team remains behaviorally unchanged.
5. Give teammates different Golden Age length modifiers where constructible;
   each recipient uses its own length.
6. `bEndOfTurn=true`: preserve practical-2208 delayed scheduling for every
   recipient; no same-end-turn leakage.
7. `bEndOfTurn=false`: immediate path gives each recipient exactly its normal
   current Golden Age length, with no old BtS `+1` compensation.
8. `bFirst=false` acquisition / peaceful transfer / save reconstruction must
   not trigger a new Golden Age.
9. Re-test F586 building valuation after F588 repair: Taj/Versailles now have
   additional teammate Golden-Age runtime value that a TeamShare-aware AI
   valuation should eventually include.

CURRENT STONE / CONTINUATION
----------------------------
F588 / KI#911 CONFIRMED.
F589 / KI#912 UNUSED.
Queue 006 remains OPEN.
Source/gameplay tree untouched.

Continue from a different hard-state family with F586/F588 TeamShare breadth
excluded as owned. Prioritize current SAS-authored `CvCity::doTurn` forced
production/queue mutations, remaining one-shot building rewards whose runtime
scope differs from their data contract, and rare state consumed before normal
end-turn reassignment. Require current reachability + ancestry where useful +
whole-album semantic dedup before consuming F589. Freeze WIP388 regardless.

END C031-WIP387


================================================================================
C031-WIP388 — POST-F588 SATURATION / SEMANTIC-COVERAGE CORRECTION STONE
================================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F588 / KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Actual AdvCiv-SAS repository/gameplay source remains UNTOUCHED.

PURPOSE OF THIS STONE
---------------------
Continue directly from WIP387 after promoting F588, but preserve the next clean tranche
without forcing F589. The central result is methodological: a fresh literal function-name
coverage census made several functions look under-audited, but semantic dedup shows that
nearly all of the apparent frontier is already owned by earlier Queue-006 controls/findings.
This checkpoint records those collisions so later passes do not waste another cycle on them.

POST-F588 SAS `doTurn` FORCED-PRODUCTION CONTROL
-------------------------------------------------
The newer SAS-authored forced-production / emergency-building block near the top of
`CvCity::doTurn()` was re-entered because ancestry cannot validate mod-authored behavior.
No new independent current-live root survived:

* the strategic-defense-vs-Port priority issue is the already-repaired KI#333 family;
* the water-building fallback is the already-repaired KI#310 family;
* the forced human BFC Artist provenance is KI#313;
* the no-production fallback / `SASTryEmergencyBuilding` legality and push-order contracts
  were already proven in the original File-006 tranche under the current positive-cost
  configured classes;
* Process orders are now explicitly excluded from the empty/invalid-queue safety fallback,
  preserving the normal AI military-spending gate.

Do not allocate F589 from this branch unless a genuinely new producer/consumer invariant is
proved beyond those existing roots.

LITERAL LOW-COVERAGE CENSUS — SEMANTIC DEDUP
----------------------------------------------
A fresh function-length x literal-album-mention scan initially highlighted several
non-UI functions. End-to-end album lookup shows that the low literal counts are misleading:

1. `hurryOverflow()`
   Already closed in WIP358 together with `unmodifyOverflow`; the retained/lost-production
   and hurry preview contract is part of the WIP349/WIP351 overflow family. No F589.

2. `goodBadHealthHappyChange()`
   Already audited in WIP361. Its hypothetical improvement/feature calculation deliberately
   keeps positive and negative health percentages separate before BtS-style rounding. No
   independent arithmetic loss was found. No F589.

3. `isMeltdownBuilding()` / `isMeltdownBuildingSuperseded()`
   Already audited in WIP358 and later ordering checks. The comparator treats larger
   `iNukeExplosionRand` as safer and clean power as preferable; current SAS data makes the
   multi-danger comparison additionally trivial. No F589.

4. `chooseProduction()`
   Already closed in WIP370 as a thin human popup constructor after its occupation /
   automation guards. It does not itself mutate the production queue. No F589.

5. `updateExtraBuildingHappiness()` / `updateExtraBuildingHealth()`
   Already audited in WIP373 and related event/acquisition work. They recompute from active
   civilization-mapped buildings plus owner modifiers and dirty citizen assignment on real
   changes. No new foreign-building or stale-cache state was established. No F589.

6. `calculateBaseYieldRate()` / WLTKD helpers
   Already explicitly closed in WIP386: base yield is a recomputation of authoritative plot,
   specialist, building, trade and corporation yield state; WLTKD changes maintenance
   immediately before presentation-only early returns. No F589.

7. Apparent `cultureDistance()` ~220-line blind spot
   Already corrected in WIP382: the crude parser had attributed the following
   `cultureStrength()` body to `cultureDistance()`. The real helper is only the small
   `max(1, plotDistance(...))` wrapper. No large unaudited subsystem exists there.

`setNumFreeBuilding()` RE-HIT
------------------------------
The mechanically larger-looking free-building transition was also rechecked. It is already a
WIP349 control, later exercised by the Stonehenge/Sacrificial-Altar F579 producer:

    oldEffective = getNumBuilding(eBuilding)
    set free count
    if effective count changed:
        processBuilding(eBuilding, iNewValue - oldEffective)

Arbitrary free counts would make the delta expression look suspicious, but live callers do not
supply arbitrary counts. `CvPlayer::m_aiFreeBuildingCount` can aggregate multiple empire-level
sources, while each city is switched only between free count 0 and 1 when the player-side count
crosses zero. Current init/reset paths likewise use 0/1. Therefore the inherited city-side setter
remains correct under its actual supported contract. No F589.

ADDITIONAL MUTATOR SPOT CHECKS
-------------------------------
A mechanical scan of zero-literal `change*` / `set*` functions found no fresh authoritative
receiver/sign error in the sampled state:

* maintenance and production-to-commerce modifier changes immediately recompute their
  dependent maintenance/commerce state;
* river/sea yield mutations reach yield recomputation;
* building/bonus happiness and health counters dirty citizen assignment as expected, while
  the special billboard invalidation cases remain the previously-owned F580/practical-3949
  families;
* project production changes affect the authoritative production map directly and dirty the
  active city UI; projects intentionally have no parked-production decay timer;
* specialist/free-specialist changes delegate through the existing specialist yield/commerce/
  GPP processing paths already audited in earlier Queue-006 work.

CURRENT CLASSIFICATION
----------------------
No independent current-live F589 root was proved in this tranche. The important progress is
that the apparent remaining low-coverage surface shrank substantially once literal-name coverage
was replaced by semantic-family coverage. Continue from a DIFFERENT mechanical lens instead of
re-reading these functions.

DURABLE STATE
-------------
F559/KI#882 through F588/KI#911 are CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvCiv-SAS repository/gameplay source remains UNTOUCHED.

EXACT CONTINUATION CURSOR
-------------------------
WIP388 -> F589 unused -> leave literal low-coverage / SAS forced-production / free-building
families behind -> switch to a fresh hard-state sieve: paired producer/consumer arithmetic and
state-transition invariants across `CvCity` fields whose live values are changed by current SAS
XML, especially sign, sentinel, owner/team scope, and mutation-order assumptions -> current XML /
caller proof -> Base AdvC/K-Mod/Civ4CE ancestry only when diagnostic -> whole-album semantic dedup
-> promote only an independent current-live root -> freeze WIP389 regardless.

END C031-WIP388



================================================================================
C031-WIP389 - POST-WIP388 EVENT / CROSS-FIELD / SERIALIZATION CONTROL TRANCHE
================================================================================

DURABLE STATE
-------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Actual AdvC-SAS repository/gameplay source remains UNTOUCHED.

This stone deliberately preserves a late-stage saturation tranche rather than consuming a
finding number for a rediscovery, disabled path, generic robustness issue or intentional
approximation. The pass used current SAS-vs-AdvC event-data comparison, paired
`canApplyEvent`/`applyEvent` invariants, cross-field BuildingInfo assumptions,
obsolescence-family dedup, owner/team parameter semantics and a mechanical current-format
CvCity serialization census.

SAS EVENTINFO DIFFERENCES VS BASE ADVC 1.14 - CLOSED
----------------------------------------------------
A direct EventInfo comparison found only eight scalar/text differences across the current event
set. None silently removes a still-supported city effect:

* `EVENT_BLESSED_SEA_DONE_3`: `UNITCLASS_PROPHET` -> `UNITCLASS_GREAT_PROPHET`.
  This is a valid SAS unit-class rename/mapping update.
* `EVENT_BROTHERS_IN_NEED_5`: `BONUS_IVORY` -> `BONUS_ELEPHANTS`.
  This follows the current SAS bonus rename.
* `EVENT_HYMNS_AND_SCULPTURES_1`: `UNITCLASS_ARTIST` -> `UNITCLASS_GREAT_ARTIST`.
  This is the corresponding valid SAS GP-class rename.
* `EVENT_WININGMONKS_1` / `_2`: `BONUS_WINE` -> `BONUS_GRAPES`.
  These follow the current SAS resource rename.
* `EVENT_FARM_BANDITS_2`: inherited `PythonHelp=getHelpFarmBandit2` removed.
  The inherited XML referred to a Python help function that does not exist; this is already the
  KI#602 cleanup family, not F589.
* `EVENT_INDEPENDENTFILMS_1`: Python callback/help removed. SAS replaced the old
  `BONUS_MOVIES` domain with `BONUS_CAMEL` and disabled Independent Films coherently: trigger,
  callback, help and old Python implementation were removed together. No supported effect is
  partially left behind.

NATIVE `canApplyEvent` -> `applyEvent` PAIR - NO NEW ROOT
---------------------------------------------------------
The paired city-event executor was challenged for state/arithmetic mismatches distinct from the
already-owned F413/F566/F575 families.

* Current event data has no negative `FreeSpecialistCounts`; live entries are +1-scale.
* Population-loss events are rejected when their delta would reduce population to zero or below.
* Current percentage-food events are pure -50% / -100% cases and do not combine a simultaneous
  flat-food term, so the slightly different integer expression grouping in precheck vs mutation
  cannot change legality under shipped data.
* Current building changes are +/-1 and use the existing real/max-count guards.
* The tempting `iMinPillage + SyncRandNum(iMaxPillage-iMinPillage)` range issue is exactly
  F413/KI#736. Current SAS's RNG safety handling makes the `min==max` Looters-1 zero range safe;
  the inclusive-maximum loss for Looters 2/3 remains F413 breadth, not F589.

CURRENT BUILDING CROSS-FIELD ASSUMPTIONS - CLOSED / ROBUSTNESS ONLY
-------------------------------------------------------------------
A structured current-XML check challenged places where `processBuilding` guards one field but
indexes/applies another.

* Every shipped shrine-like building with `HolyCity != NO_RELIGION` currently has
  `HolyCity == ReligionType`. Therefore the suspicious shrine cache code that guards
  `getHolyCity()` but indexes `m_aiShrine` with `getReligionType()` is generic robustness only
  under current data.
* Every current nonzero `StateReligionHappiness` building has a concrete `ReligionType`.
* Current GP-rate buildings have a valid Great-Person unit class.
* Current power / dirty-power field combinations remain internally coherent.

ONE-SHOT / TEAMSHARE CROSS-FIELD BOUNDARY AFTER F588
----------------------------------------------------
Current TeamShare one-shot payloads were rechecked after F588:

* Hanging Gardens global population has explicit teammate handling.
* Oracle free technology is team-owned state and naturally propagates through the tech system.
* Taj Mahal and SAS Versailles Golden Ages are the now-owned F588/KI#911 runtime TeamShare gap.
* Apostolic Palace vote-source religion binding is not another teammate reward payload.

No second unowned TeamShare one-shot root was established.

F554 MAINTENANCE-PREFILTER SIGN - EXACT DEDUP RE-HIT
----------------------------------------------------
The current-data signed-field sieve mechanically rediscovered:

    const bool bCityMaintenanceBuilding =
        (kBuilding.getMaintenanceModifier() >= 25);

Current maintenance reducers are negative (Ikhanda -15%, Courthouse -50%, Federal Courthouse
-60%, Rathaus -70%, Prikaz -50%, Versailles -50%). This is NOT F589: the exact wrong-sign
prefilter, including its intended strong-maintenance threshold and current XML witnesses, is
already F554/KI#877.

`doPlotCultureTimes100(..., ePlayer, ...)` OWNER-vs-CULTURE-PLAYER - WIP374 RE-HIT
---------------------------------------------------------------------------------
The advc.025 capitulated-vassal multiplier initially looked like a prospective-player leak because
it uses the host city's `getTeam()` even though `ePlayer` can differ in K-Mod trade-culture calls.
This exact semantic question is already preserved in WIP374:

* advc.025 deliberately defines the rule by the CITY OWNER spreading culture toward its master;
  `ePlayer` is the culture identity being credited and need not define the host ownership rule;
* current inherited `USE_KMOD_TRADE_CULTURE` is 0, so the foreign-trade-culture producer is also
  disabled in shipped SAS;
* `CAPITULATED_TO_MASTER_CULTURE_PERCENT` remains 50 as intended.

No F589.

OBSOLESCENCE ACTIVE-vs-RETAINED SPLIT - F536 FAMILY RE-HIT
-----------------------------------------------------------
A proposed add -> obsolete -> unobsolete -> remove equivalence audit was checked against album
coverage before being repeated. The F536 tranche already traces the exact
`processBuilding(..., bObsolete=true)` split, including retained GPP/obsolete-safe commerce and
active-only defense/effect handling. This family is deliberately excluded from F589.

CURRENT-FORMAT `CvCity::read` / `write` MECHANICAL SYMMETRY - CLEAN
-------------------------------------------------------------------
A fresh machine comparison was performed over the current SAS city stream rather than relying on
spot checks.

* `CvCity::read` contains 117 stream operations.
* `CvCity::write` contains 117 corresponding stream operations.
* The persistent member references match in the same order. The sole syntactic mismatch is the
  expected event-vector size read (`iNumElts`) versus writing `m_aEventsOccured.size()`.
* The current BFC Artist provenance state remains explicitly paired, as already noted in WIP358.

A header-member census found only four `CvCity` members absent from BOTH current read and write:

    m_ePlot
    m_pPlot
    m_pArea
    m_kWallOverridePoints

These are derived/runtime/graphical state, not omitted authoritative save state:

* `m_ePlot` is derived from the serialized coordinates and reconstructed by `updatePlot()`
  immediately after `m_iX` / `m_iY` are read;
* `m_pPlot` / `m_pArea` are runtime references rebuilt from city/map state;
* `m_kWallOverridePoints` is graphical Great-Wall geometry, not gameplay persistence.

Thus no current-format CvCity save/load omission or stream-order mismatch was established. The
project's already-recorded current-version-only save compatibility policy remains unchanged.

STATUS / NEXT EXACT CURSOR
--------------------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Source remains UNTOUCHED.

Continue from WIP389 by leaving EventInfos, F554 maintenance prefilter, advc.025 trade-culture
ownership semantics, F536 obsolescence and current-format serialization behind. Switch to a fresh
mechanical hard-state lens: current SAS XML values that activate inherited negative/sentinel or
combined-field domains in native CvCity production/growth/culture/maintenance/revolt logic;
prefer authoritative mutation/caller contradictions over AI heuristic quality. Prove current
reachability, whole-album semantic dedup and ancestry only where diagnostic; promote F589 only for
an independent root; freeze WIP390 regardless.

END C031-WIP389


===============================================================================
C031-WIP390 - POST-WIP389 HARD-STATE / PRODUCER-CONSUMER CONTROL TRANCHE
===============================================================================

DURABLE STATE
-------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Repository/gameplay source remains UNTOUCHED.

PURPOSE
-------
Continue exactly from WIP389 while leaving the saturated EventInfo, maintenance-prefilter,
`advc.025` culture-owner, obsolescence and current-format serialization families behind. This
tranche replaces literal-function coverage with mechanically checkable authoritative state
invariants: fixed-capacity representation, current SAS signed/map-size activation, and producer
order / combined-field symmetry. Current SAS XML is treated as the reachability oracle; Base
AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS remain ancestry controls only.

FIXED TRADE-ROUTE CAPACITY - CLEAN
----------------------------------
`CvCity` allocates its trade-city cache to `MAX_TRADE_ROUTES`, but the legal city route count is
also explicitly clamped before consumers use it:

    return std::min(iTradeRoutes, GC.getDefineINT(CvGlobals::MAX_TRADE_ROUTES));

and `updateTradeRoutes()` asserts the resulting count does not exceed the cache size.

Current SAS route-count data does not bypass that boundary. Relevant direct building changes are
small (+1 on Castle / Barbarian Castle / Airport / Spanish Lonja), while current civic changes
include Protectionism -1 and Trade Bloc +1. Protectionism's negative route count is the exact old
WIP229 control: it offsets inherited `INITIAL_TRADE_ROUTES = 1`, so current play does not enter an
unsupported negative city-route domain.

No fixed-array overflow / negative-loop F589 was established.

POWERBONUS RESOURCE-vs-BUILDING ORDER - POSITIVE CONTROL
---------------------------------------------------------
The apparent producer-order asymmetry at the prior cutoff is fully closed by the later per-resource
part of `CvCity::processBuilding`.

When the resource already exists and the conditional-power building is added, the building path
executes:

    if (kBuilding.getPowerBonus() == eLoopBonus)
        changePowerCount(iChange, kBuilding.isDirtyPower());

When the building already exists and the resource appears, `processBonus()` computes the same
active conditional-power source through `getBonusPower(...)` and changes power there.

Thus current Coal Plant / Coal and Nuclear Plant / Uranium converge to the same city power state in
both producer orders. This is a useful positive invariant, not F589.

GENERIC SELF-PRODUCED / SELF-SUPPRESSED BONUS ORDER HAZARD - CURRENT-DATA MASKED
---------------------------------------------------------------------------------
The same mechanical producer-order pass exposes a real generic fragility in `processBuilding` if a
single building were configured BOTH to create/suppress a resource (`FreeBonus` / `NoBonus`) and to
condition its own health, happiness, power or yield effect on that same resource.

`setNumRealBuildingTimed` changes the building count before `processBuilding` runs. Therefore:

* a hypothetical self-`FreeBonus` building can make `changeFreeBonus -> processBonus(+1)` see the
  newly active building and apply its conditional effect, after which the later per-bonus loop can
  apply the same candidate effect again;
* a hypothetical self-`NoBonus` building can make `changeNoBonusCount -> processBonus(-1)` see the
  newly active building and remove an effect that had never existed before construction.

Current shipped SAS XML masks both shapes completely. It contains no building with a non-NONE
`FreeBonus`. The only current `NoBonus` producer is `BUILDING_NATIONAL_PARK -> BONUS_COAL`, and it
has no Coal-keyed BonusHealthChanges / BonusHappinessChanges / BonusYieldModifiers / PowerBonus of
its own. The already-confirmed F563 remains the separate current NoBonus-before-verification bug.

Keep this as generic XML/mod-mod robustness debt; do not spend F589 on it.

SAS EXPANDED WORLD-SIZE / DISTANCE ARITHMETIC - NO NEW ROOT
------------------------------------------------------------
Current SAS extends the world-size table through SAS24/SAS32/SAS40/SAS48. The native city
maintenance distance path was rechecked against those larger maps rather than assuming BtS-sized
geometry.

`calculateMaintenanceDistance` caps the selected distance with
`GC.getMap().maxMaintenanceDistance()`, and `calculateDistanceMaintenanceTimes100` normalizes by
`maxTypicalDistance()`. With the current SAS world dimensions and maintenance percentages, the
intermediate products remain comfortably inside signed 32-bit `int`; no zero denominator or map-
size-driven overflow was established.

The two actual nearby distance-domain defects remain the already-owned roots:

* F567: colony-maintenance cap multiplies the max-distance factor twice;
* F568: `maxTypicalDistance` is used as a hard religion-distance bound.

Do not duplicate either under the expanded-world-size lens.

CURRENT `FreeBuilding` CHAINS - THRESHOLD COUNTER IS REVERSIBLE
---------------------------------------------------------------
Current SAS has several unusual but live free-building chains, especially the Barbarian building
set (e.g. Walls/Castle/Lighthouse -> Palace, Palace -> Granary, Granary -> Barracks) plus
Stonehenge -> Monument.

`CvPlayer::changeFreeBuildingCount` stores source multiplicity but installs/removes the city free
building only on the 0 -> positive and positive -> 0 boundaries. Nested free-building effects can
therefore increment the downstream source count multiple times without installing duplicate city
copies; removal unwinds the same nested source counts and removes the downstream free building only
when the last source disappears.

The current Barbarian civilization mappings resolve Palace/Granary/Barracks to the intended
Barbarian concrete types, while unsupported classes such as Monument map to NONE; the existing
`CvPlayer::processBuilding` NO_BUILDING guard handles that mapping. No recursive duplicate-copy,
negative-count or missing-mapping F589 was established here.

LOW-LEVEL MUTATOR CENSUS - COVERAGE CONTROL
-------------------------------------------
A fresh census of compact `CvCity::change*` / `set*` mutators was used only as a mechanical lens,
not as evidence by name count. Several superficially thin setters immediately collapse into prior
semantic controls:

* `changeUnhealthyPopulationModifier` -> prior WIP188/WIP377/WIP379 assignment-recompute control;
* sea/river plot yield setters -> `updateYield` and prior player-wide rank/cache reconciliation;
* hurry-anger modifier -> already-owned F579 arithmetic family;
* improvement free specialists -> WIP379 current Globe/National Park control;
* base-yield changes -> `updateCommerce()` performs player-wide yield-rank invalidation, as preserved
  in WIP365.

This reinforces the earlier conclusion that zero/low literal album mention is no longer a useful
proxy for semantic audit coverage.

STATUS / NEXT EXACT CURSOR
--------------------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Source remains UNTOUCHED.

Continue from WIP390 by leaving fixed trade-route capacity, conditional PowerBonus order, expanded
world-size maintenance arithmetic, current free-building-chain recursion and the current-data-
masked self-bonus combined-field hazard behind. Use the compact-mutator census only to select truly
unreconciled authoritative producer/consumer state, especially transitions whose dependent state is
stored rather than dynamically queried. Require an ordinary current-SAS producer, concrete gameplay
consequence, whole-album semantic dedup and ancestry only where diagnostic. Promote F589 only for an
independent current-live root; physically freeze WIP391 regardless.

END C031-WIP390


===============================================================================
C031-WIP391 - STORED DERIVED-STATE INVALIDATION CENSUS / CACHE-FAMILY CLOSURE
===============================================================================

DURABLE STATE
-------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Repository/gameplay source remains UNTOUCHED.

PURPOSE
-------
Continue exactly from WIP390's compact-mutator cursor by prioritizing `CvCity` state that is
NOT derived on demand. The mechanical question for each current stored aggregate/cache is:

    which mutable inputs determine it,
    which ordinary current-SAS transitions can change those inputs,
    and does every such transition rebuild the stored value before authoritative gameplay
    consumes it?

This is deliberately dependency-based rather than function-name based. Existing numbered roots
are treated as owners of their whole invalidation family so that a rediscovered cache shape does
not consume F589 twice.

CACHE-FAMILY CENSUS - EXISTING ROOTS EXPLAIN THE HIGH-RISK SURFACE
------------------------------------------------------------------
The most important stored city aggregates already have explicit album ownership:

* `m_iMilitaryHappinessUnits`
  Relational team/master-vassal eligibility is the F455/KI#778 family. The current full recount
  helper and vassal refresh were rechecked; Permanent-Alliance team reassignment is the existing
  missing invalidator, not a new F589.

* `m_iMaintenance`
  Ordinary population, government-center, city maintenance-modifier, WLTKD, occupation,
  religion/corporation and player maintenance-modifier transitions all reach a maintenance
  rebuild. The exceptional time-varying inverse-inflation corporation term is already
  F421/KI#744. Vassal/team-member maintenance invalidations are also owned by the older
  Queue-004 maintenance roots. No independent CvCity-side input was found here.

* `m_aiBuildingCommerce`
  Building add/remove/obsoletion ends in `updateBuildingCommerce`; original owner/time are stored
  before `processBuilding`; game-turn advancement retains the global delayed-commerce safety
  rebuild. The nonlocal dynamic holes are already owned by F476/KI#799 (state-religion / empire
  state-religion-building-commerce input) and F496/KI#819 (corporation-suppression policy changing
  other players' HQ world-level commerce), with HQ-owner activity effects in the F491 family.
  No additional current input was left without an owner.

* `m_aiTradeYield` / `m_aTradeCities`
  City route-count/modifier setters rebuild routes locally, and the fixed route capacity was
  closed in WIP390. Cross-city partner-population invalidation is F505/KI#828 and route-topology
  invalidation is F507/KI#830. The current source's ordinary route mutators do not expose a third
  independent cache dependency in this pass.

* `m_abEspionageVisibility`
  The exceptional persisted passive-visibility entitlement and directional EP/cost transitions
  are already F508/KI#831. Other espionage mission/cost queries remain dynamic rather than a
  second city cache.

* surrounding health/happiness caches
  Access-dependent relation/ownership invalidation is already F509/KI#832. Local feature and
  improvement source changes still call the combined updater.

This map is important late-audit evidence: the strongest stored-state failure modes are not
unexplored; they are exactly where the earlier album findings cluster.

POWER HEALTH / POWERED-COMMERCE STATE - CLEAN CURRENT CONTROL
--------------------------------------------------------------
`m_iPowerGoodHealth` / `m_iPowerBadHealth` are rebuilt whenever local power/dirty-power truth
changes through `changePowerCount`.

The nonlocal clean-power input is also wired correctly. `CvArea::changeCleanPowerCount` compares
old/new area clean-power truth and on an edge calls both:

    GET_TEAM(eIndex).updateCommerce();
    GET_TEAM(eIndex).updatePowerHealth();

Thus Three Gorges / area-clean-power transitions refresh both powered yield/commerce consequences
and stored power health for the affected team. This remains the older positive control rather
than F589.

FRESH-WATER HEALTH - CURRENT RUNTIME RIVER-EDGE REPAIR CLOSES THE LIVE MUTATOR
-------------------------------------------------------------------------------
`m_iFreshWaterGoodHealth` / `m_iFreshWaterBadHealth` are stored rather than queried dynamically.
Historically that makes river-edge mutation a natural invalidation hazard.

The current SAS source now explicitly repairs this in the finalized-game river setters: both
`CvPlot::setNOfRiver` and `setWOfRiver` call the shared `updateRiverDerivedState` helper after a
real edge change. That helper updates irrigation around the edge, calls `updateFreshWaterHealth`
for nearby cities, and rebuilds plot groups. The source comments tie this to the already-repaired
KI#351 family.

Map-generation initialization still occurs before final initialization and city initialization
itself calls `updateFreshWaterHealth`, so no current ordinary runtime freshwater-cache F589 remains.

EXTRA BUILDING HAPPINESS / HEALTH - INPUTS ARE PAIRED WITH REBUILDS
-------------------------------------------------------------------
`updateExtraBuildingHappiness` and `updateExtraBuildingHealth` store owner-specific building
bonuses in four city counters. Their live mutable inputs are:

    active civilization-mapped building presence
    + player's per-BuildingType extra happiness/health maps.

Both sides are explicitly paired:

* `processBuilding` calls both city rebuilds after building state changes;
* `CvPlayer::changeExtraBuildingHappiness` mutates the player map and immediately calls the
  player-wide happiness rebuild;
* `CvPlayer::changeExtraBuildingHealth` does the analogous health rebuild.

Obsolescence/acquisition and civilization-mapping edge cases were already covered in the earlier
Queue-006 building-state work (including F566 for the separate event missing-mapping setter).
No new stored-cache root survives here.

RELIGION HAPPINESS / RELIGION COMMERCE - CLEAN DEPENDENCY PAIRING
-----------------------------------------------------------------
`m_iReligionGoodHappiness` / `m_iReligionBadHappiness` depend on city religion presence, effective
state religion, and the player's state/non-state religion happiness values.

Current transitions refresh those inputs:

* `setHasReligion` calls `updateReligionHappiness`;
* changing state/non-state religion happiness calls the player-wide updater;
* `setLastStateReligion` and state-religion-count transitions call the player-wide updater.

Religion commerce is a separate stored map. Under current `advc.172`, ordinary religion commerce
is no longer selected by state-religion identity; it depends on city religion presence and holy-
city state. `setHasReligion` rebuilds it, and `CvGame::setHolyCity` explicitly rebuilds both the old
and new holy cities. The state-religion building-commerce defect therefore remains F476's distinct
`m_aiBuildingCommerce` root, not a hidden `m_aiReligionCommerce` sibling.

SPECIALIST-DERIVED YIELD / COMMERCE - IMMEDIATE MUTATION CHAIN
--------------------------------------------------------------
Changing assigned or free specialist counts enters `processSpecialist`, which immediately:

* changes the specialist's base yields;
* changes specialist commerce and rebuilds the relevant commerce cache;
* calls `updateExtraSpecialistYield()` for the owner's player-wide specialist-extra-yield layer;
* updates GP rate / specialist free experience as applicable.

Player-side specialist-extra-yield mutation likewise calls the city updater. The thin
`changeImprovementFreeSpecialists` setter remains the WIP379 current-control/API-robustness case;
normal Globe/National-Park and improvement transitions do not establish a new persistent cache
root.

CORPORATION YIELD / COMMERCE / MAINTENANCE - EXISTING ROOTS + CURRENT LOCAL PAIRING
------------------------------------------------------------------------------------
`updateCorporation` remains the central local rebuild: produced bonuses, building commerce,
corporation yield, corporation commerce and maintenance are recalculated together.

`changeNumBonuses` calls it whenever the changed bonus is a prerequisite of an active corporation
present in the city; `changeNoBonusCount` calls it after resource suppression changes; occupation
and player anarchy edge changes also call corporation rebuilds. The major nonlocal/activity holes
are already represented by F491/F496 and the older corporation-maintenance cache root F421.
No separate current local resource -> corporation-cache omission was established.

CULTURE LEVEL / RANK CACHES - DIRECT INVALIDATION CONTROL
----------------------------------------------------------
`setCultureTimes100` immediately calls `updateCultureLevel`; population/base-yield/yield/commerce
rank caches continue to route through their owner-wide invalidators, including
`setBaseYieldRate -> updateCommerce -> invalidateYieldRankCache`. These remain the WIP365/WIP366
rank controls rather than F589.

CLASSIFICATION
--------------
No independent current-live F589 root survived the stored-derived-state census.

The useful result is a dependency closure: nearly every high-risk stored CvCity aggregate either
has a current explicit producer->rebuild chain or maps to a previously numbered invalidation root.
That means another pass over generic `update*`/`change*` naming would now have very low expected
value and a high duplicate rate.

STATUS / NEXT EXACT CURSOR
--------------------------
F559/KI#882 through F588/KI#911 remain CONFIRMED.
F589 / provisional KI#912 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Source remains UNTOUCHED.

Continue from WIP391 by leaving the generic stored-cache invalidation census behind. Switch to a
fresh authoritative transition lens that does NOT depend on cache invalidation: destructive or
replacement state changes where a legal current transition must preserve a hard count/queue/
ownership invariant. Prefer current city kill/acquire/raze, building replacement/one-shot and
production-order interruption edges only where they are not already F572-F575, F588, WIP367/
WIP386 or earlier overflow/decay controls. Use current SAS data for reachability and whole-album
semantic dedup before numbering. Promote F589 only for an independent current-live contradiction;
physically freeze WIP392 regardless.

END C031-WIP391


===============================================================================
C031-WIP392 - DESTRUCTIVE / REPLACEMENT TRANSITION CENSUS - F589 CONFIRMED
===============================================================================

DURABLE STATE
-------------
F559/KI#882 through F589/KI#912 are now CONFIRMED.
F590 / provisional KI#913 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Repository/gameplay source remains UNTOUCHED.

PURPOSE
-------
Continue exactly from WIP391's destructive/replacement-state cursor. The primary mechanical
question is whether legal current city destruction, acquisition, recreation and hard-count
transitions preserve the authoritative state/count that their later consumers assume.

This tranche first closes several attractive but already-owned or current-data-masked acquisition
seams, then promotes one independent inherited AdvC runtime root reached directly from the
`CvCity::init` / `CvCity::kill` area-count lifecycle.

`GET_TEAM(eOwner)` IN `CvCity::kill` - TYPE-OVERLOAD FALSE ALARM / WIP362 RE-HIT
--------------------------------------------------------------------------------
`CvCity::init` increments:

    GET_TEAM(getTeam()).changeNumCities(1);

while `CvCity::kill` decrements:

    PlayerTypes const eOwner = getOwner();
    ...
    GET_TEAM(eOwner).changeNumCities(-1);

In isolation this looks like a PlayerTypes value being used as a TeamTypes index. It is not.
AdvC deliberately overloads `GET_TEAM` by static enum type: `GET_TEAM(TeamTypes)` addresses that
team directly, while `GET_TEAM(PlayerTypes)` resolves `GET_PLAYER(ePlayer).getTeam()` first.
Therefore `GET_TEAM(eOwner)` is the old owner's actual current team, including pre-teaming /
Permanent-Alliance cases.

The album had already preserved this exact typed-overload trap in WIP362. Do not promote it.

`acquireCity()` PERSISTENT-STATE PRESERVATION CENSUS - SATURATED BY EXISTING ROOTS
---------------------------------------------------------------------------------
A member-by-member pass over serialized/current city state against `CvPlayer::acquireCity()` found
no independent F589 in the already-dense transfer family:

* F574 owns active espionage extra-health / extra-happiness duration state lost across the
  kill -> recreate transfer.
* F575 owns event-created persistent city-local scalar state that is not preserved; WIP360 already
  broadened that family to the relevant adjacent serialized/event state.
* WIP357 records the inherited per-owner reset semantics of revolt-history counters.
* religion influence is reconstructed from surviving religion/building/holy-city state rather than
  raw-copied.
* food, production and build-queue state follow the inherited new-owner-starts-fresh-production
  transfer policy.
* source/destination unique-building conversion, provenance and conquest-policy indexing are the
  already-confirmed F495/KI#818 family.
* current SAS BFC-artist acquisition provenance remains the WIP358 positive control.

CITY `m_szScriptData` - GENERIC TRANSFER ROBUSTNESS ONLY UNDER CURRENT SAS DATA
-------------------------------------------------------------------------------
City script data are serialized and explicitly exposed to Python:

    CvCity::getScriptData()
    CvCity::setScriptData(...)
    pStream->ReadString(m_szScriptData)
    pStream->WriteString(m_szScriptData)

but `CvPlayer::acquireCity()` does not snapshot or restore `m_szScriptData`; a city with custom
script data therefore loses it when ownership is implemented as kill -> recreate.

This is a real generic scenario/API preservation seam, but current SAS searches found no shipped
map, scenario or gameplay script that assigns city ScriptData. Under the album's current-live
threshold it remains robustness debt rather than F589.

REAL/FREE BUILDING OVERLAP ON ACQUISITION - CURRENT CONTROL
------------------------------------------------------------
Acquisition restores persistent real buildings with `bFirst=false`, so their ordinary persistent
effects return without replaying creation-only one-shot rewards. If the recipient already has the
same class/type effectively present through a free-building source, the effective building-count
delta can correctly remain zero while the real copy itself is retained; removing the free source
later then exposes the already-owned real copy. No duplicate persistent processing or lost real copy
was established from this overlap.

-------------------------------------------------------------------------------
F589 / KI#912 - `CvArea::changeCitiesPerPlayer` REPORTS EVERY POSITIVE CITY ADDITION
AS A BARBARIAN CITY CREATION, SO ORDINARY CIV CITIES AND OWNERSHIP TRANSFERS
FALSELY SUPPRESS FUTURE BARBARIAN CITY SPAWNING
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED. The defect is reached by ordinary current-SAS city founding and city ownership
reconstruction. It is inherited from AdvC's `advc.300` Barbarian-city-density feature; K-Mod and
the supplied Civ4CE/BtS body do not contain this counter.

ROOT CONTRACT
-------------
AdvC adds a per-area persistent counter whose names and only gameplay consumer establish a narrow,
unambiguous meaning:

    int CvArea::getBarbarianCitiesEverCreated() const
    void CvArea::reportBarbarianCityCreated()
    int m_iBarbarianCitiesEver;

The consumer in `CvGame::createBarbarianCity` computes:

    int iDestroyedCities = a.getBarbarianCitiesEverCreated() -
            a.getCitiesPerPlayer(BARBARIAN_PLAYER);
    FAssert(iDestroyedCities >= 0);
    iDestroyedCities = std::max(0, iDestroyedCities);
    iUnownedTilesThreshold += iDestroyedCities * 3;

Thus the intended invariant is:

    Barbarian cities ever created in this land area
      - Barbarian cities currently alive in this area
      = Barbarian cities destroyed / removed from this area.

That destroyed-city count deliberately raises the unowned-tiles denominator, making replacement
Barbarian cities progressively harder to spawn.

THE PRODUCER IGNORES WHICH PLAYER'S CITY CHANGED
-------------------------------------------------
Current SAS `CvArea::changeCitiesPerPlayer` is:

    void CvArea::changeCitiesPerPlayer(PlayerTypes eIndex, int iChange)
    {
        m_aiCitiesPerPlayer.add(eIndex, iChange);
        FAssert(getCitiesPerPlayer(eIndex, true) >= 0 || gDLL->GetWorldBuilderMode());
        m_iNumCities += iChange;
        FAssert(getNumCities() >= 0);
        // <advc.300>
        for (int i = 0; i < iChange; i++)
            reportBarbarianCityCreated();
        // </advc.300>
    }

There is no:

    eIndex == BARBARIAN_PLAYER

condition.

Therefore EVERY positive city-count change reports a Barbarian city creation, regardless of
whether the added city belongs to Rome, India, a minor civilization or the actual Barbarian player.
The positive loop prevents removals from incrementing the counter, but it does not repair the
player-domain mismatch.

NORMAL CURRENT `CvCity` LIFECYCLE REACHABILITY
-----------------------------------------------
Every ordinary city initialization performs:

    getArea().changeCitiesPerPlayer(getOwner(), 1);

and every city kill performs the inverse `-1` call.

Consequently:

1. A major civilization founds a normal city in land area A.
2. `CvCity::init` calls `A.changeCitiesPerPlayer(majorPlayer, +1)`.
3. The generic area mutator calls `reportBarbarianCityCreated()` once.
4. No Barbarian city has been created, but `m_iBarbarianCitiesEver` permanently increases by 1.
5. Later `createBarbarianCity` subtracts only CURRENT Barbarian cities, so the false major-civ
   addition survives as an extra `iDestroyedCities` penalty.

This needs no rare event, malformed save, WorldBuilder edit or unusual option. Normal expansion is
enough.

CITY TRANSFERS ADD ANOTHER FALSE CREATION
-----------------------------------------
`CvPlayer::acquireCity()` implements transfer by snapshotting the old city, calling:

    pOldCity->kill(...)

and then constructing the replacement with:

    initCity(...)

The kill's `-1` does not increment `m_iBarbarianCitiesEver`; the replacement `+1` does.
Therefore every conquest, peaceful city trade/liberation or other ownership reconstruction can add
another false "Barbarian city ever created" to the land area even though no new Barbarian city
exists.

A captured Barbarian city makes the arithmetic especially concrete:

* genuine Barbarian founding: ever += 1, currentBarb += 1;
* capture kills the Barbarian city: currentBarb -= 1, ever unchanged;
* recipient's major-civ replacement city initializes: currentBarb unchanged, EVER += 1 again.

After one genuinely destroyed Barbarian city, the current code can therefore produce:

    ever = 2
    currentBarb = 0
    iDestroyedCities = 2

instead of the intended value 1.

A major-civ-to-major-civ transfer is even more purely false: the replacement adds +1 to `ever`
without any Barbarian city having existed at all.

CURRENT GAMEPLAY CONSEQUENCE
----------------------------
The false count feeds an authoritative Barbarian-city spawn-density gate, not merely logging or UI.
For each candidate land area, current code starts from the handicap's
`iUnownedTilesPerBarbarianCity` and does:

    iUnownedTilesThreshold += iDestroyedCities * 3;
    iTargetCities /= std::max(1, iUnownedTilesThreshold);

Current SAS Monarch uses an unowned-tile threshold of 105. Ten false positive city additions in one
land area therefore make the denominator 135 before any genuinely destroyed Barbarian cities are
counted:

    intended: 105
    current:  105 + 10 * 3 = 135

For the same unowned-tile numerator, the continuous target ratio becomes 105 / 135 ~= 0.778 of the
intended value (about 22% lower), before integer truncation; floor boundaries can make the concrete
eligible-city-count effect larger and discontinuous.

Current SAS's handicap thresholds make the root live throughout normal difficulty play where
Barbarian city creation is enabled (e.g. 110 Prince, 105 Monarch, 100 Emperor, 95 Immortal,
90 Deity, 85 Deity+). The exact magnitude varies with area settlement/transfer history.

The top-level `createBarbarianCities()` path is likewise live: after its normal global city-count /
elapsed-turn gates it calls `createBarbarianCity(false)`, which DOES evaluate civ-settled areas.
Thus ordinary major-civ founding in an area can suppress later Barbarian settlement in that same
area through this false destroyed-city history.

COASTAL WATER-AREA BREADTH
--------------------------
`CvCity::init` also increments an adjacent water area's city-per-player bookkeeping for coastal
cities under `advc.030b`, so the same generic producer increments that water area's
`m_iBarbarianCitiesEver` too. `createBarbarianCity` evaluates non-water candidate plots and consumes
the candidate plot's land area, so this water-side contamination is not needed for F589's current
spawn consequence. It is additional evidence that the producer is attached at too generic a layer.

ANCESTRY / INTRODUCTION
-----------------------
The supplied K-Mod 1.46 and Civ4CE/BtS bodies have the ordinary
`CvArea::changeCitiesPerPlayer` bookkeeping but no `m_iBarbarianCitiesEver` /
`reportBarbarianCityCreated` feature.

The preserved AdvC history already introduces the mismatch in practical 1253
`f6ff6a182f...` (`Initial commit (cont.)`). In that imported `advc.300` code:

    int CvArea::numBarbCitiesEver() const { return nBarbCitiesEver; }
    void CvArea::barbCityCreated() { nBarbCitiesEver++; }

and the generic player-indexed mutator receives unconditionally:

    barbCityCreated(); // advc.300

while the same history adds the consumer:

    nDestroyedCities = a.numBarbCitiesEver() -
            a.getCitiesPerPlayer(BARBARIAN_PLAYER);

So the player-domain contradiction is present from the earliest preserved implementation of this
feature in the supplied reachable history.

Practical 1740 `ea0557d9c5...` (`EnumMap adapted to BtS`) later changes the unconditional single
call into:

    for (int i = 0; i < iChange; i++)
        reportBarbarianCityCreated();

This is a partial repair in one dimension: negative city-count changes no longer increase the
"ever" counter. It still does not test `eIndex`, so every positive non-Barbarian addition remains
misclassified.

Base AdvCiv 1.14 retains that exact positive-loop/no-player-guard form, and current SAS inherits it.

DEDUP
-----
Whole-album searches for `BarbarianCitiesEver`, `reportBarbarianCityCreated`,
`barbCityCreated`, the destroyed-Barbarian-city subtraction, `iUnownedTilesThreshold` and the
specific `advc.300` producer/consumer relation found no existing numbered runtime root.

The album does contain many other Barbarian-city findings, but they concern site valuation,
production, capture/reconstruction, research, naval demand or AI targeting rather than this
per-area historical spawn-density counter.

The generic `changeCitiesPerPlayer` mention in the InvasionGraph family is likewise unrelated: it
establishes that the real `CvArea` city count is not graph-simulated. F589 concerns an extra
side effect incorrectly attached to that real lifecycle mutator.

REPAIR BOUNDARY
---------------
The narrow repair is to report a Barbarian creation only when the player whose area city count is
being increased is actually the Barbarian player, while retaining the current positive-count
multiplicity:

    if (eIndex == BARBARIAN_PLAYER)
    {
        for (int i = 0; i < iChange; i++)
            reportBarbarianCityCreated();
    }

This preserves practical 1740's protection against decrements and makes ordinary major/minor-civ
founding or ownership reconstruction neutral to the Barbarian-history counter.

No source patch is made in this audit stone.

REGRESSION TESTS
----------------
1. Fresh land area, no Barbarians: found several normal major-civ cities. Assert
   `getBarbarianCitiesEverCreated() == 0`.
2. Found one actual Barbarian city: assert `ever == 1`, `currentBarb == 1`, destroyed == 0.
3. Destroy that Barbarian city without replacement by Barbarians: assert `ever == 1`,
   `currentBarb == 0`, destroyed == 1.
4. Capture that Barbarian city into a major civilization: the recipient's replacement-city init
   must NOT raise `ever`; destroyed remains exactly 1.
5. Transfer a major-civ city to another major civilization in the same area: `ever` must remain
   unchanged.
6. With a deterministic area/unowned-tile fixture, compare the spawn target before/after founding
   ordinary civ cities. The destroyed-city term and denominator must be unchanged unless actual
   Barbarian cities were destroyed.
7. Found a coastal major-civ city: its adjacent-water bookkeeping must not affect the LAND area's
   Barbarian-history count; if the narrow player guard is used, any remaining water-area Barbarian
   bookkeeping is behaviorally irrelevant to current city spawning.
8. Save/load after genuine Barbarian founding/destruction: the serialized ever-created count and
   corrected destroyed-city arithmetic must remain stable.

CLASSIFICATION
--------------
F589 / KI#912 CONFIRMED.

This is an inherited AdvC authoritative gameplay bug: a generic player-indexed area-city mutator
updates a Barbarian-only historical counter without checking the player index. Normal civ expansion
and ownership reconstruction therefore masquerade as destroyed Barbarian-city history and suppress
future Barbarian city spawning.

STATUS / NEXT EXACT CURSOR
--------------------------
F559/KI#882 through F589/KI#912 are CONFIRMED.
F590 / provisional KI#913 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Source remains UNTOUCHED.

Continue from WIP392 with the destructive/replacement count family narrowed by F589. Leave generic
`acquireCity` persistent-event state (F574/F575), unique-building source/destination conversion
(F495), the typed `GET_TEAM(PlayerTypes)` overload control, and the current-data-masked city
ScriptData transfer seam behind. Resume hard transition symmetry at production/order interruption,
raze/kill external counters, or another current-live state whose producer and authoritative
consumer disagree. Require current reachability + whole-album semantic dedup before consuming F590;
physically freeze WIP393 regardless.

END C031-WIP392

================================================================================
C031-WIP393 - POST-F589 DESTRUCTIVE / PRODUCTION TRANSITION SATURATION; F590 UNUSED
================================================================================

CONTINUATION STATE
------------------
Continued from physical C031-WIP392 after confirming F589 / KI#912. F590 / provisional KI#913
remained UNUSED throughout this stone. Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN. Gameplay
source remains UNTOUCHED.

The purpose of this tranche was to finish the destructive/replacement and production/order
transition family far enough to distinguish a genuinely independent F590 from attractive-looking
shapes already owned by earlier findings or closed by AdvC's deeper representation contracts.

POST-`deleteCity` CAPITAL-LOSS CANDIDATE - EXACT F572/F573 RE-HIT
-----------------------------------------------------------------
Re-entering `CvCity::kill()` after the F589 area-count work immediately exposes a visually severe
sequence: the city is deleted from the owner's FreeList and later code in the same function still
contains receiver-relative reads.

Current source reaches:

    kOwner.deleteCity(getID());

and the capital-loss announcement block later contains `getID()` / `getTeam()` reads; the function
also ends with a receiver-relative `isActiveOwned()` read.

The FreeList implementation makes this a real destruction boundary rather than a mere detach:
`CvPlayer::deleteCity(iID)` removes the city from `m_cities`, and the FreeList removal performs the
object deletion.

However, whole-album semantic dedup closes this entire candidate family before F590 can be spent:

* F572 / KI#895 already owns the post-delete CAPITAL announcement path, including the surviving
  `getID()` / `getTeam()` receiver reads and the copied player-ID/city-ID context mistake.
* F573 / KI#896 already owns the later non-capital-specific final `isActiveOwned()` access after the
  same hard deletion boundary.

The direct post-`deleteCity` `this`-access inventory was explicitly declared exhausted in WIP356.
This pass therefore adds no independent root and does not reopen F572/F573.

PLAIN INIT/KILL EXTERNAL COUNTS - CLEAN AFTER F589
--------------------------------------------------
After excluding F589's special-purpose side effect inside the otherwise generic
`CvArea::changeCitiesPerPlayer`, the ordinary city/team/game count pairs are simple balanced
arithmetic:

* city init increments the owning team/game city totals;
* city kill decrements the corresponding totals;
* the typed `GET_TEAM(PlayerTypes)` overload resolves a player's actual team and is already a
  WIP362 false-positive control;
* the remaining direct team/global count mutators do not attach another hidden subtype-specific
  historical consequence analogous to F589.

This matters because F589 was not a generic count-pair problem. It was caused specifically by an
AdvC-only Barbarian-history side effect being grafted onto a broad player-indexed area mutator
without checking which player caused the positive count change.

GLOBAL-WARMING ICE MELT -> LAKE/FRESH-WATER HEALTH CANDIDATE - CLOSED
---------------------------------------------------------------------
The current KI#346 repair makes a runtime Ice passability change rebuild water areas:

    GC.getMap().resetPathDistance();
    if (GC.getDefineBOOL(CvGlobals::PASSABLE_AREAS))
        GC.getMap().recalculateWaterAreas();
    updatePlotGroups();

`CvCity` fresh-water health is stored (`m_iFreshWaterGoodHealth` /
`m_iFreshWaterBadHealth`) rather than queried dynamically, while `CvPlot::isFreshWater()` can read
adjacent `CvPlot::isLake()`. At first glance this suggests a new stale-cache root: melting Ice can
merge passability-split water `CvArea`s, `calculateReprAreas()` calls `updateLakes()`, and the
water-area rebuild does not explicitly call `CvCity::updateFreshWaterHealth()`.

The candidate closes under AdvC's REPRESENTATIVE-AREA contract.

`CvMap::calculateReprAreas()` deliberately groups water `CvArea`s that belong to the same BtS-style
water body even when passability rules split them into separate movement areas. For water it uses
orthogonal same-water adjacency to converge their representative IDs. `CvArea::updateLake(true)`
then classifies lake status from the SUM of the tile counts of every water area sharing that
representative ID:

    int iTotalTiles = getNumTiles();
    ...
    if (pOther->m_iRepresentativeArea == m_iRepresentativeArea &&
        pOther->getID() != getID())
    {
        iTotalTiles += pOther->getNumTiles();
        ...
    }

Therefore Ice can split or merge the PASSABLE `CvArea` representation without changing the total
BtS-style representative water body used for `isLake()`. The KI#346 melt trigger does not change
water-vs-land topology; it changes passability. The summed representative-area tile count is
unchanged, so adjacent `isLake()` does not flip merely because the Ice barrier melts.

No current lake->ocean or ocean->lake transition is produced by this trigger, and therefore no
fresh-water health input changes that would require an additional city-cache refresh. F590 is not
consumed here.

`recalculateWaterAreas()` RESTORATION CONTROL
---------------------------------------------
The current water-only rebuild first detaches/deletes water areas, rebuilds them, recalculates
representative areas/lakes, and only after every water plot has its new area restores coastal-city
counts:

    for (...) {
        if (!kPlot.isCity())
            continue;
        CvArea* pWaterArea = kPlot.waterArea(true);
        if (pWaterArea != NULL)
            pWaterArea->changeCitiesPerPlayer(kPlot.getPlotCity()->getOwner(), 1);
    }

Water-plot area statistics and unit bookkeeping are rebuilt through plot-area processing as water
plots receive their new areas. Building/area effects tied to a city's ordinary area remain land-area
state; the special coastal-water city count is the city-side water-area state that needs explicit
restoration here. No second current-live missing restoration was established.

The malformed/dormant negative-side `CvPlot::processArea` shapes encountered nearby are already
preserved in earlier audit controls and remain outside the current-live threshold; they are not a
new F590.

PRODUCTION / ORDER INTERRUPTION RECHECK - SATURATED
---------------------------------------------------
The adjacent production/order surfaces were re-entered only far enough to challenge independent
hard-state asymmetries. They remain heavily covered by prior stones:

* ORDER_MAINTAIN head removal/start symmetry is already explicitly proved in WIP367/WIP386.
* The current completion -> `chooseProduction()` DLL path for the open KI#136 popup symptom was
  already traced through `popOrder(..., ALL_CHOOSE)`; no missing city-side call is exposed here.
* The duplicate city-building event/reporting surface in `pushOrder` has already been classified;
  current shipped consumers do not turn it into a new authoritative gameplay root.
* The `pushOrder(..., bPop=true)` replacement-before-full-validation shape is already a robustness
  control rather than an unowned ordinary-play root.
* `failProduction()`'s generalized multi-global-instance minimum-selection arithmetic was audited in
  WIP374. Current world wonders/world projects use the supported one-global-instance domain, so the
  hypothetical >1-global-instance defect remains mod-mod robustness only.

No production/order branch in this pass met the independent-current-live threshold for F590.

PROJECT `CreatedCount` NAMING CANDIDATE - CURRENT-COUNT SEMANTICS, NOT A BUG
---------------------------------------------------------------------------
`CvTeam::changeProjectCount` always calls:

    GC.getGame().incrementProjectCreatedCount(eProject, iChange);

including negative `iChange`, despite the game member's historical `CreatedCount` name. That can
look suspicious if the member is interpreted as a monotonic 'ever created' statistic.

Its authoritative consumer shows otherwise. `CvGame::isProjectMaxedOut` compares
`getProjectCreatedCount(eProject)` against a world project's CURRENT allowed global instance count,
and project transfer/reset paths deliberately compensate the game count when moving/removing team
projects. The game member is therefore functioning as the current global project-instance total for
legality, despite its name. Negative changes are required for those transitions.

This is not an F590 root.

WIP393 RESULT
-------------
No independent F590 was confirmed in this tranche.

The useful result is a strong saturation boundary around the post-F589 destructive/production
family:

* post-delete city-member access -> F572/F573 owned;
* ordinary init/kill count pairs -> balanced once F589's special side effect is separated;
* Ice passability rebuild -> representative-area lake classification proves no fresh-water input
  change;
* water-area rebuild -> current coastal city-count restoration accounted for;
* production/order interruption -> prior WIP367/WIP386/WIP374 and related controls;
* project `CreatedCount` -> current-instance semantics, not monotonic history.

STATUS / NEXT EXACT CURSOR
--------------------------
F559/KI#882 through F589/KI#912 remain CONFIRMED.
F590 / provisional KI#913 remains UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Source remains UNTOUCHED.

Leave the direct `deleteCity` lifetime family, Ice-melt lake/fresh-water hypothesis, generic queue
lifecycle and project-count naming behind. Continue from a mechanically different F589-shaped lens:
scan AdvC/SAS-added SIDE EFFECTS attached to broad player/team/type-indexed mutators across the
city's immediate dependency classes (`CvArea`, `CvPlayer`, `CvTeam`, `CvGame`, and only then
`CvPlot`). For each side effect, ask whether it semantically belongs to every index/value transition
accepted by the generic mutator or only to one subtype that needs an explicit gate. Require current
reachability, authoritative consequence and whole-album semantic dedup before promoting F590.
Physically freeze WIP394 regardless.

END C031-WIP393

===============================================================================
C031-WIP394 -- QUEUE 006 CvCity.cpp CONTINUATION AFTER WIP393
Date: 2026-09-06
STATUS: INCOMPLETE SAFETY CHECKPOINT -- Queue 006 remains OPEN
===============================================================================

This checkpoint continues directly from C031-WIP393 with F590 / provisional
KI#913 unused. It records the post-WIP393 low-coverage indexed-mutator/setter
sieve and deliberately does NOT promote any candidate that is only a generic
robustness concern, an already-owned cache family, or a misleadingly thin
setter. No repository/gameplay source file was modified.

CURRENT NUMBERING
-----------------
F559 / KI#882 through F589 / KI#912 CONFIRMED.
F590 / provisional KI#913 UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` OPEN.
Repository/gameplay source UNTOUCHED.

POST-WIP393 GENERIC-MUTATOR / SETTER CONTROLS
----------------------------------------------

1. CvGame::changeNoNukesCount -- ordinary vote path CLOSED

The authoritative global no-nukes counter is a thin mutator and does not itself
walk every city queue. That initially looked capable of allowing a queued nuclear
unit to finish after a successful No Nukes resolution.

The normal current vote-resolution timing closes the shipped path. Vote state is
resolved before player city production, and each city reaches `doCheckProduction`
before completing its current production. Continuing `ORDER_TRAIN` legality is
rechecked through `canTrain(..., true)`, which consumes the current global No Nukes
state. A nuke that has become illegal therefore cannot simply finish from the stale
queue after a normally resolved ban.

A hypothetical external API that mutates the counter in an arbitrary mid-city-turn
window is a broader robustness contract, not a demonstrated current F590 trigger.


2. SAS Water Wheel bIrrigation + bIgnoreIrrigation ordering -- CLOSED

Current SAS Water Wheel activates both technology flags in the same technology.
`CvTeam::processTech` applies the Irrigation count first; that transition rebuilds
map irrigation. It then applies IgnoreIrrigation without a second irrigation
rebuild.

The apparent order/cache contradiction is false after tracing the consumers:

* `bIrrigation` controls the stored irrigation-spreading network through
  `isPotentialIrrigation` / `m_bIrrigated` and therefore requires the rebuild;
* `bIgnoreIrrigation` is queried dynamically by improvement-placement legality
  when deciding whether a normally irrigation-requiring improvement may be built;
* `updateIrrigated` does not cache the IgnoreIrrigation rule.

Thus Water Wheel's current dual-flag configuration does not leave stored irrigation
state stale.


3. Construction BridgeBuilding / Engineering RouteChange pathfinder candidate --
   existing C027 control, NOT F590

Changing bridge ability or route movement can change path costs without an explicit
blanket invalidation of AdvCiv's reusable GroupPathFinder search tree. The cache's
reuse identity cannot generically encode every global movement-rule transition.

This exact Engineering-style concern was already challenged in the earlier C027
GroupPathFinder audit. The normal triggers established there did not preserve the
same group/start/moves search identity needed for harmful reuse, while final human
movement uses the cleared/final finder. No stronger ordinary current reuse sequence
was established here, so the abstract cache hole is retained as that existing
control rather than renumbered.


4. CvTeam::setForceRevealedBonus -- positive remove/flip/re-add control

The live random-event path can force-reveal a resource such as Oil before its normal
technology reveal. The setter correctly reconciles authoritative plot-group bonus
state rather than merely changing observer UI:

    for owned matching-bonus plots:
        updatePlotGroupBonus(false, false)
    change forced-reveal state
    for owned matching-bonus plots:
        updatePlotGroupBonus(true)
    update matching plot yields/layout

Therefore a newly forced-revealed connected resource is removed under the old
visibility contract and re-added under the new one, allowing resource counts and
city effects to converge immediately. Permanent-Alliance item sharing also copies
forced-reveal state. No F590.


5. Counterespionage / Disengage / StolenVisibility setter thinness -- semantic
   dedup controls

The counterespionage setters store the directional timer/modifier and dirty the
Espionage advisor, but the important passive-demographics invalidation failure is
already owned by F461. The stored passive city-visibility lifetime family is owned by
F508 and related prior work.

Disengage and stolen-visibility state likewise already have extensive Permanent-
Alliance / deal / team-lifecycle coverage. Thin local setter bodies are not evidence
of a new independent root.


6. CvArea PlayerTypes/TeamTypes mutator gate census -- F589 remains unusual

The post-F589 mechanical pass checked the other broad indexed area mutators:

* units/cities/population per player;
* building good health, bad health and happiness;
* area trade routes and free specialists;
* area power and best-found value;
* revealed tiles, clean power and border obstacles by team;
* area AI type;
* area yield modifiers and UnitAI counts.

Apart from F589's `changeCitiesPerPlayer`, the secondary effects are either pure
indexed accounting or explicitly update/dirty the same supplied player/team. No
second mutator attaches a semantically narrower Barbarian-/special-subtype effect
without first identifying that subtype.

F589 is therefore not evidence for a broad family of missing PlayerTypes/TeamTypes
gates inside CvArea.


7. Low-coverage authoritative CvPlot setter tranche -- no independent F590

`CvPlot::setBonusType` is producer-order symmetric for live resource state: old
area/map counts are removed, old plot-group bonus contribution is removed, the bonus
identity changes, the new contribution is added, new area/map counts are installed,
and yield/layout state is refreshed.

`setRevealedOwner`, `setRevealedImprovementType` and `setRevealedRouteType` maintain
team-relative fog-memory/intelligence state. Their apparently lightweight bodies do
not own the hidden authoritative economy for a plot.

`setTerrainType` updates impassability, yield and plot-group state and hands land /
water mismatches through `setPlotType`. Current terrain XML does not activate a new
post-start terrain-impassability domain requiring a separate F590 here.

`setFeatureType` remains bounded by the already-owned feature-passability/fresh-water
families; current SAS additionally contains the explicit post-finding repairs around
live Ice/global-warming and river-derived-state transitions. Do not duplicate them.

`CvPlot::changeBlockadedCount` changes the authoritative team-relative blockade state
and dirties the affected working city's assignment. Its caller `CvUnit::updatePlunder`
checks whether trade-network status changed and rebuilds affected players' plot groups.
That can still leave the city's cached trade-route selection/yield until the normal
late route refresh, but that is precisely F507's existing root: authoritative
player-specific trade-network topology changes without rebuilding cached trade routes
before economy. Record blockade as F507 breadth, not F590.

The remaining ownership-duration/city-radius/plot-group/recycled-slot state maps to
older ownership-score, hidden-city-radius and F477/F478 lifecycle work rather than a
fresh root in this tranche.


RESULT OF WIP394
----------------
No independent current-live contradiction survived as F590.

The useful result is a narrower search space:

* F589's subtype-gate mistake appears isolated among CvArea's indexed mutators;
* low-coverage literal setter counts are again a poor predictor of semantic coverage;
* several thin setters delegate the authoritative reconciliation to their callers or
  to explicit remove/mutate/re-add transitions;
* blockade supplies useful F507 breadth but not a distinct root.

F590 / provisional KI#913 therefore remains intentionally UNUSED.


NEXT CURSOR
-----------
Continue Queue 006 from this physical stone, but leave the broad indexed-mutator and
literal setter census behind.

Use a different mechanical lens: threshold/cross-zero transitions and paired stored
state whose behavior changes specifically at 0<->positive, NONE<->concrete or
inactive<->active boundaries. Prioritize current SAS XML/data combinations that can
exercise both sides during ordinary play. For each candidate:

1. establish the authoritative producer and consumer;
2. prove an ordinary current transition reaches the boundary;
3. compare K-Mod / Base AdvCiv / Civ4CE ancestry when useful;
4. semantic-dedup against the whole album before numbering;
5. promote F590 / KI#913 only for an independent current-live root.

Physically freeze C031-WIP395 at the next settled tranche whether F590 is confirmed
or remains unused.

===============================================================================
END C031-WIP394
===============================================================================

===============================================================================
C031-WIP395 -- QUEUE 006 CvCity.cpp / F590 CONFIRMED
Date: 2026-09-06
STATUS: INCOMPLETE SAFETY CHECKPOINT -- Queue 006 remains OPEN
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Continue directly from C031-WIP394.
F559 / KI#882 through F589 / KI#912 remain CONFIRMED.
F590 / provisional KI#913 is CONFIRMED below.
F591 / provisional KI#914 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives plus
locally reconciled album/history material. No repository/gameplay source file modified.

POST-WIP394 THRESHOLD / LIFECYCLE CONTROLS BEFORE F590
-------------------------------------------------------
1. Live global-warming Ice melt / water-area recreation does not establish a new stale fresh-water
   health root. AdvC's water `CvArea::updateLake` classification uses the representative BtS-style
   water body rather than one passability-split water-area fragment. Melting Ice can merge the live
   passability areas without changing the representative water body's total tile count, so the
   adjacent city's dynamic `CvPlot::isFreshWater()` lake input does not flip merely because those
   water `CvArea` objects are recreated.

2. Water-area object recreation under the recent KI#346 repair also does not leave an ordinary-live
   stale unit/city area pointer. Cities cache their land area; water-unit area pointers are replaced
   while `CvPlot::setArea` assigns the recreated water areas. Team `AreaAIType` is recomputed before
   the affected team's units act. The remaining non-plot-derived water-area fields are either
   land-only semantics or recomputed lifecycle state rather than a demonstrated current authority
   loss.

3. `CvPlayerAI::m_neededExplorersByArea` is keyed by area ID and is therefore abstractly invalidated
   by water-area reconstruction. The normal turn lifecycle closes the shipped window: player AI
   cache update clears/rebuilds that map at player activation before the later team/unit/city
   consumers use it. Preserve this as a positive cache-generation control, not F590.

4. The SAS-vs-Base-AdvC XML activation sieve is more productive than continuing generic cross-zero
   setters. It exposes inherited projection helpers that were written for a narrower live XML
   domain than current SAS now uses. The first current-live independent root is below.

-------------------------------------------------------------------------------
F590 / provisional KI#913 - CONFIRMED
BUG BUILDING-ADDITIONAL-YIELD PROJECTION IGNORES THE CANDIDATE BUILDING'S
PLAYER-GLOBAL `GlobalSeaPlotYieldChanges`, SO A GLOBAL WATER-PLOT WONDER CAN
PREVIEW +0 LOCAL YIELD WHILE IMMEDIATELY RAISING THE CONSTRUCTION CITY'S REAL YIELD
-------------------------------------------------------------------------------

ROOT
----
`CvCity::getAdditionalYieldByBuilding` claims to return the total additional yield from adding a
building. Its base-yield helper, `getAdditionalBaseYieldRateByBuilding`, explicitly models the
candidate building's CITY-LOCAL `SeaPlotYieldChanges` by counting currently worked water plots:

    if (kBuilding.getSeaPlotYieldChange(eYield) != 0)
    {
        int iChange = kBuilding.getSeaPlotYieldChange(eYield);
        for (WorkingPlotIter it(*this); it.hasNext(); ++it)
        {
            if (it->isWater())
                iExtraRate += iChange;
        }
    }

But the same helper never models the candidate's PLAYER-GLOBAL
`GlobalSeaPlotYieldChanges` field.

Actual building completion does. `CvCity::processBuilding` delegates the player-global building
payload to `CvPlayer::processBuilding`. For every yield, that routine executes:

    changeSeaPlotYield(y,
        kBuilding.getGlobalSeaPlotYieldChange(y) * iChange);

`CvPlayer::changeSeaPlotYield` mutates the player's authoritative `m_aiSeaPlotYield` and immediately
calls `updateYield()`.

`CvPlot::calculateYield` consumes that player-global state on every non-impassable water plot:

    if (isWater() && !isImpassable())
        iYield += GET_PLAYER(ePlayer).getSeaPlotYield(eYield);

Therefore a candidate with `GlobalSeaPlotYieldChanges` changes the real base yield of EVERY worked
water plot in the construction city as soon as it completes. The BUG hypothetical helper sees only
`getSeaPlotYieldChange()` and evaluates the candidate under the PRE-BUILD player sea-plot-yield
state. It can consequently report zero for a real immediate local yield increase.

CLEAN CURRENT-SAS FIXTURE - MOAI STATUES
----------------------------------------
Current SAS deliberately moved Moai's water-plot Production from a city-local effect to a
player-global one:

    BUILDING_MOAI_STATUES
        SeaPlotYieldChanges                  = empty
        GlobalSeaPlotYieldChanges(PRODUCTION)= +1
        YieldChanges(PRODUCTION)             = 0
        YieldModifiers(PRODUCTION)           = 0

The XML comment states that the old local/city-only SeaPlotYieldChanges effect was removed and the
replacement now applies to all cities.

Take a constructible Moai city that currently works exactly one ordinary water plot, with a 100%
base Production modifier and no other candidate-dependent Production effect.

Before completion:
    candidate global sea-plot Production in player state = 0

BUG hypothetical calculation:
    getAdditionalBaseYieldRateByBuilding(PRODUCTION, MOAI)
      -> local SeaPlotYieldChanges branch sees 0
      -> no GlobalSeaPlotYieldChanges branch exists
      -> +0 candidate base Production from that worked water plot
    getAdditionalYieldRateModifierByBuilding(PRODUCTION, MOAI) = 0
    => projected additional Production from this mechanic = 0

Actual completion:
    CvPlayer::processBuilding(MOAI, +1, ...)
      -> changeSeaPlotYield(PRODUCTION, +1)
      -> updateYield()
      -> the already-worked water plot now contributes +1 base Production
      -> the construction city's real Production rises by +1 at 100% modifier

With N currently worked water plots, the omitted immediate local base-yield component is N * +1.
The same global player state also changes other cities, but this city's own immediate delta is
sufficient to violate the helper's local "total additional yield" contract.

CURRENT-SAS BREADTH
-------------------
Current SAS has two live nonzero `GlobalSeaPlotYieldChanges` producers:

* `BUILDING_MOAI_STATUES`: +1 Production on water plots;
* `BUILDING_GREAT_LIGHTHOUSE`: +1 raw Commerce yield on water plots.

Both were changed from the inherited data domain. Current SAS's Colossus no longer carries the
inherited global sea-plot Commerce effect.

For a current Great Lighthouse candidate, each already-worked water plot likewise gains +1 base raw
Commerce yield on completion, while the BUG helper has no candidate-global sea overlay.

LIVE USER-FACING CALLERS
------------------------
This is the same live BUG Building Additional Yield surface audited around F578, not dead code.

1. `CvGameTextMgr::setBuildingNetEffectsHelp` requests `getAdditionalYieldByBuilding` for every yield
   while composing the "actual effects" of adding the hovered building.

2. `CvGameTextMgr::setBuildingAdditionalYieldHelp` enumerates currently constructible buildings and
   calls the same helper for Production/Food/etc. hover output.

An ordinary active-owned city hover can therefore omit the water-plot yield that completion will
immediately create.

ANCESTRY / CURRENT ACTIVATION
-----------------------------
The helper omission is inherited rather than introduced by SAS.

K-Mod 1.46 contains the BUG Building Additional Yield helper with the same local
`SeaPlotYieldChanges` branch and no `GlobalSeaPlotYieldChanges` branch. K-Mod also ships a live
witness: `BUILDING_COLOSSUS` has `GlobalSeaPlotYieldChanges(COMMERCE)=+1` and no local
SeaPlotYieldChanges.

Base AdvCiv 1.14 inherits both the helper omission and the Colossus global-water effect. Current SAS
retains the helper but reallocates the live global-water mechanic to Great Lighthouse and Moai,
including the especially clear Moai +1 Production fixture.

The supplied Civ4CE/BtS core does not contain this BUG hypothetical-building helper layer.

Classification:
    inherited BUG/K-Mod Building Additional Yield projection defect retained and newly re-fixtured
    by AdvCiv-SAS XML.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album search before this stone found no numbered root for `GlobalSeaPlotYieldChanges`,
`getGlobalSeaPlotYieldChange`, or candidate player-global sea-plot yield omission.

F590 is close to F578 / KI#901 because both live in `getAdditionalYieldByBuilding` and both omit a
candidate-created PLAYER-GLOBAL yield input. It is nevertheless mechanically independent under the
album's existing root boundaries:

* F578 is specifically `SpecialistYieldChanges` -> `changeSpecialistExtraYield` ->
  city specialist counts -> `changeBaseYieldRate`. Its repair must construct a hypothetical
  specialist state, including candidate free specialists and the new per-specialist yield.
* F590 is `GlobalSeaPlotYieldChanges` -> `changeSeaPlotYield` -> every currently worked water plot's
  `CvPlot::calculateYield`. It requires no specialist at all and its repair is a worked-water-plot
  term using the candidate's GLOBAL sea delta.
* A complete F578 repair that overlays candidate specialist-extra-yield state leaves F590 exactly
  unchanged.
* A complete F590 repair that adds global-water candidate yield leaves F578 exactly unchanged.

F561 / KI#884 remains arithmetic/order loss in the helper's TRADE branch; F590 survives with no
trade route. F398 / KI#721 remains misuse of order-specific Production modifiers as generic city
Production; F590 is a real base tile-yield delta and survives with no military/domain/space order.

The two roots share a broader architectural lesson (the BUG helper does not construct one coherent
hypothetical post-building player state), but they are separate omitted producer/consumer chains and
separate repairs.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
Extend `getAdditionalBaseYieldRateByBuilding` with the candidate's global sea-plot yield for THIS
city's already-worked water plots.

Conceptually, for candidate global sea delta D for yield Y:

    for each currently worked water plot:
        local omitted base-yield delta += D

This should be kept separate from `SeaPlotYieldChanges`: both fields can legally coexist and would
then stack in the post-building state. The helper's local-city contract does not require summing the
candidate's effect across other cities in the empire, but it must include the construction city's
own changed plots.

A more general future refactor could construct one hypothetical post-building player/city yield
state and thereby cover F578/F590 coherently, but the minimal repair for F590 is independent.

REGRESSION TESTS
----------------
A. Current SAS Moai clean witness
1. City works exactly one water plot.
2. Moai is constructible; no candidate-local Production yield/modifier confounders.
3. Assert pre-build projection includes +1 Production from Moai's global sea effect.
4. Complete Moai and assert the city's real base Production rises by +1.
5. Projection and actual local delta must agree.

B. Multiplicity
With four worked water plots, assert the Moai candidate contributes +4 base Production before
ordinary modifiers.

C. Unworked-water control
With coastal water plots present but none currently worked, assert the immediate current-city
Production delta from this mechanic is 0.

D. SAS Great Lighthouse breadth
With two worked water plots, assert the candidate projection includes +2 raw Commerce yield from
its `GlobalSeaPlotYieldChanges(COMMERCE)=+1`.

E. Inherited K-Mod/Base AdvC fixture
Under inherited Colossus data, one worked water plot must contribute +1 candidate raw Commerce in
the same helper.

F. F578 separation
Use a Moai city with zero specialists and a worked water plot to prove F590 independently; use an
Angkor-Wat Priest city with no worked water plots to prove F578 independently.

CURRENT STONE AFTER C031-WIP395
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F590 / KI#913 are CONFIRMED.
F591 / provisional KI#914 is UNUSED.
Latest physical checkpoint: C031-WIP395.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F591 unused. Stay with the SAS-vs-Base-AdvC XML activation sieve long enough to test
adjacent inherited hypothetical-state assumptions, but do not split F590 into cosmetic siblings.
Prioritize fields whose CURRENT SAS values newly activate a producer/consumer chain that the
corresponding city helper never simulates. Check current XML reachability, ancestry and whole-album
semantic dedup before promotion. Then leave the already-dense BUG projection family unless another
mechanically independent post-building state component survives. Physically freeze WIP396 at the
next settled tranche regardless of whether F591 is consumed.

===============================================================================
END C031-WIP395
===============================================================================

===============================================================================
C031-WIP396 -- QUEUE 006 CvCity.cpp / F591 CONFIRMED
Date: 2026-09-06
STATUS: INCOMPLETE SAFETY CHECKPOINT -- Queue 006 remains OPEN
===============================================================================

HANDOFF / SOURCE STONE
----------------------
Continue directly from C031-WIP395.
F559 / KI#882 through F590 / KI#913 remain CONFIRMED.
F591 / provisional KI#914 is CONFIRMED below.
F592 / provisional KI#915 is UNUSED.
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
Authoritative current source remains the user-supplied AdvCiv-SAS light-source snapshot.
Comparison material remains the supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives plus
locally reconciled album/history material. No repository/gameplay source file modified.

POST-F590 ADJACENT XML-DOMAIN CONTROL
-------------------------------------
Before leaving the BUG hypothetical-building family, compare the remaining player/area-global yield
modifier fields against current XML rather than assuming every omitted runtime field is live.

`CvPlayer::processBuilding` can apply both `AreaYieldModifiers` and `GlobalYieldModifiers`, while
`CvCity::getAdditionalYieldRateModifierByBuilding` does not explicitly overlay either candidate
field. However current SAS, Base AdvCiv 1.14 and K-Mod 1.46 all ship NO nonzero building
`AreaYieldModifiers` or `GlobalYieldModifiers` entries in their BuildingInfos. There is therefore no
current-data F591 sibling from those two fields. Preserve the generic helper limitation as mod-mod
robustness only.

The happiness/health sibling below is different: current shipped XML has multiple direct live
fixtures, including deliberately SAS-added values.

-------------------------------------------------------------------------------
F591 / provisional KI#914 - CONFIRMED
BUG BUILDING-ADDITIONAL HAPPINESS/HEALTH PROJECTIONS MODEL AREA EFFECTS BUT OMIT
THE CANDIDATE'S GLOBAL HAPPINESS/HEALTH, SO CURRENT WONDERS CAN PREVIEW +0 IN THE
CONSTRUCTION CITY WHILE COMPLETION IMMEDIATELY GIVES THAT CITY +2 HAPPINESS OR +3 HEALTH
-------------------------------------------------------------------------------

ROOT
----
The paired BUG helpers are city-local hypothetical post-building projections:

    CvCity::getAdditionalHappinessByBuilding(...)
    CvCity::getAdditionalHealthByBuilding(...)

Both explicitly include the candidate's AREA-scoped field once for the construction city:

    addGoodOrBad(kBuilding.getAreaHappiness(), iGood, iBad);
    addGoodOrBad(kBuilding.getAreaHealth(), iGood, iBad);

Neither helper includes the corresponding GLOBAL field:

    kBuilding.getGlobalHappiness()
    kBuilding.getGlobalHealth()

Actual completion does apply those fields to the constructing player's authoritative global
building-happiness/health state. `CvCity::processBuilding` delegates the player-level payload to
`CvPlayer::processBuilding`, which executes:

    if (kBuilding.getGlobalHealth() > 0)
        changeBuildingGoodHealth(kBuilding.getGlobalHealth() * iChange);
    else
        changeBuildingBadHealth(kBuilding.getGlobalHealth() * iChange);

    changeBuildingHappiness(kBuilding.getGlobalHappiness() * iChange);

The city runtime then consumes those player fields directly:

    happyLevel / unhappyLevel
        -> GET_PLAYER(getOwner()).getBuildingHappiness()

    totalGoodBuildingHealth
        -> GET_PLAYER(getOwner()).getBuildingGoodHealth()

    totalBadBuildingHealth
        -> GET_PLAYER(getOwner()).getBuildingBadHealth()

Thus `GlobalHappiness` and `GlobalHealth` change the construction city's real local state just as
surely as the Area variants do. Omitting them from a city-local "additional building" projection is
a direct scope mismatch.

CLEAN CURRENT-SAS HEALTH FIXTURE - RED CROSS
--------------------------------------------
Current SAS deliberately gives `BUILDING_RED_CROSS`:

    iHealth       = 0
    iAreaHealth   = 0
    iGlobalHealth = +3   // custom: was 0
    bTeamShare    = 0

Take an ordinary constructible Red Cross city with no event/class extra health and no other
candidate-dependent health term.

BUG hypothetical result:
    getAdditionalHealthByBuilding(RED_CROSS, ...)
      -> Basic Health       +0
      -> BuildingClass      +0 in clean fixture
      -> Player extra       +0 in clean fixture
      -> AreaHealth         +0
      -> no GlobalHealth branch exists
      -> +0 from Red Cross's defining current health effect

Actual completion:
    CvPlayer::processBuilding(RED_CROSS, +1, ...)
      -> changeBuildingGoodHealth(+3)
      -> every owner city, INCLUDING the construction city, sees +3 real good building health

So the construction city can be projected at +0 health by this helper while immediately gaining
+3 real health from the candidate.

This fixture is deliberately non-TeamShare, so no team-recipient semantics are needed to reproduce
F591.

CLEAN CURRENT-SAS HAPPINESS FIXTURE - HAGIA SOPHIA
--------------------------------------------------
Current SAS deliberately gives `BUILDING_HAGIA_SOPHIA`:

    iHappiness       = 0
    iAreaHappiness   = 0
    iGlobalHappiness = +2   // custom: was 0
    bTeamShare       = 1

In a one-player team, with no event/class extra happiness and no other candidate-dependent happiness
term:

BUG hypothetical result:
    getAdditionalHappinessByBuilding(HAGIA_SOPHIA, ...)
      -> Basic Happiness   +0
      -> AreaHappiness     +0
      -> no GlobalHappiness branch exists
      -> +0 from the wonder's current global happiness effect

Actual completion:
    CvPlayer::processBuilding(HAGIA_SOPHIA, +1, ...)
      -> changeBuildingHappiness(+2)
      -> construction city happyLevel gains +2

So the same city can be projected at +0 happiness while completion immediately gives it +2.
One-player-team setup deliberately removes F586/F588 TeamShare breadth from the proof.

CURRENT-SAS BREADTH
-------------------
Namespace-aware current BuildingInfo parsing finds:

GlobalHappiness:
* `BUILDING_HAGIA_SOPHIA`: +2, TeamShare;
* `BUILDING_EIFFEL_TOWER`: +1, TeamShare.

GlobalHealth:
* `BUILDING_RED_CROSS`: +3, non-TeamShare, custom SAS change from 0;
* `BUILDING_HANGING_GARDEN`: +1, TeamShare.

Current SAS therefore has both a clean non-TeamShare health witness and newly activated global-
happiness witnesses.

LIVE USER-FACING CONSEQUENCES
-----------------------------
The helpers are live in ordinary optional/Alt UI paths.

1. `CvGameTextMgr::setBuildingNetEffectsHelp` uses both helpers while composing the candidate
   building's "actual effects" line. A global-health/happiness candidate can therefore omit the
   local health/happiness that completion actually creates in this city.

2. `CvDLLWidgetData::parseHealthHelp` calls `setBuildingAdditionalHealthHelp` for the active-owned
   city when the BUG option is enabled OR Alt is held. That function only lists a constructible
   building when the helper returns nonzero good/bad health. In the clean Red Cross fixture the
   candidate's +3 defining health effect can therefore be absent from the additional-health list.

3. `parseHappinessHelp` analogously exposes `setBuildingAdditionalHappinessHelp`; the clean Hagia
   global +2 can be absent from the additional-happiness list and from its projected angry-population
   change.

AI CONSEQUENCE - BROAD FLAT VALUE DOES NOT REPAIR THE LOCAL THRESHOLD
---------------------------------------------------------------------
`CvCityAI::AI_buildingValue` also consumes these two helpers for the CONSTRUCTION CITY's nonlinear
immediate state:

* `iBuildingActualHappiness` feeds immediate anger reduction, growth bias and happiness-padding
  formulas;
* `iBuildingActualHealth` feeds immediate food-deficit/waste reduction, growth bias and health
  padding.

The same function later adds separate broad empire/area value terms from
`getGlobalHappiness()` / `getGlobalHealth()`. Those flat cardinality terms do not make the helper's
local omission harmless: they do not feed the current city's threshold/food-deficit arithmetic.
For example, a starving/unhealthy Red Cross construction city can have its immediate +3 health
excluded from `iWasteDelta` even though a later `GlobalHealth * iNumCities * 4` term adds generic
empire value.

This also means a repair must avoid accidental double counting in the later global/area AI terms;
see Repair Direction below.

ANCESTRY / CURRENT ACTIVATION
-----------------------------
K-Mod 1.46 already contains both BUG helper bodies with the same shape: AreaHappiness/AreaHealth are
included and GlobalHappiness/GlobalHealth are omitted.

K-Mod's shipped Hanging Gardens already has `iGlobalHealth=+1`, giving the health half a live
inherited witness. Base AdvCiv 1.14 preserves the same helper omission and Hanging Gardens fixture.

The supplied Base AdvCiv and K-Mod BuildingInfos have no nonzero `iGlobalHappiness` building in this
parse. Current SAS newly activates that half through Hagia Sophia +2 and Eiffel Tower +1, and adds
the especially strong non-TeamShare Red Cross +3 GlobalHealth fixture.

The supplied Civ4CE/BtS core does not contain this BUG hypothetical-building helper layer.

Classification:
    inherited BUG/K-Mod additional-building local-scope omission, retained and substantially
    broadened by current AdvCiv-SAS building data.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album semantic search before this stone found no numbered finding owning the omission of
candidate `GlobalHappiness` / `GlobalHealth` from these additional-building helpers.

Distinct from:

* F397 / KI#720: AdvC LuxuryModifier scaling/rounding is omitted from POSITIVE BONUS-HAPPINESS
  projection. F591 reproduces at LuxuryModifier=0 with no BonusHappinessChanges at all.
* F557 / KI#880: downstream `AI_buildingValue` has an AdvC sign regression in how it converts the
  helper's happiness result into anger change. F591 supplies the wrong/missing input even if F557's
  sign is fixed.
* F586 / KI#909: AI valuation of TeamShare buildings uses the builder's owner-only recipient breadth.
  F591 reproduces with non-TeamShare Red Cross and, for happiness, on a one-player team.
* F590 / KI#913: candidate player-global WATER-PLOT YIELD is omitted by the separate additional-yield
  helper. F591 is happiness/health state, different authoritative player counters, different
  consumers and different repair sites.
* WIP349's population-unhealth preview rounding note: generic modifier-rounding drift was current-
  data masked; F591 is an exact shipped nonzero field omission with zero-modifier clean fixtures.

Happiness and health are grouped into ONE finding here because they are the paired manifestations
of the same BUG helper scope error: each helper consciously adds the candidate's Area field once for
this city while forgetting the directly corresponding Global field that also affects this city.
Splitting one number per sibling function would over-fragment the root.

REPAIR DIRECTION - NOT IMPLEMENTED
----------------------------------
At the helper-contract level, include the candidate's global field in the construction city's local
before/after state just as the Area field is included:

    Happiness helper: addGoodOrBad(kBuilding.getGlobalHappiness(), iGood, iBad)
    Health helper:    addGoodOrBad(kBuilding.getGlobalHealth(), iGood, iBad)

subject to the same obsolete/current-building assumptions already used by the existing helpers.

However `AI_buildingValue` currently also adds separate area/global cardinality terms. If the helper
is repaired globally for every caller, review those AI terms to avoid counting the construction
city's same global effect twice. A clean design is:

1. make the helper genuinely represent this city's local post-building delta;
2. in AI empire-breadth terms, add only OTHER recipient cities beyond the construction city for the
   simple count-linear portion, while retaining any intentional strategic weighting separately;
3. preserve the helper result for immediate anger/health threshold arithmetic.

Alternatively, if compatibility requires keeping the legacy helper contract for AI, add the missing
global local effect explicitly in the UI and AI-immediate callers. Do not leave the current split
where UI and nonlinear local AI state silently omit it.

REGRESSION TESTS
----------------
A. Red Cross health zero-vs-+3
1. One-player game/team; clean city; Red Cross constructible.
2. Assert candidate local health projection includes +3.
3. Complete Red Cross and assert construction city's good building health rises by +3.
4. Projected and actual local health delta agree.

B. Hagia happiness zero-vs-+2
1. One-player team; clean city; Hagia constructible.
2. Assert candidate local happiness projection includes +2.
3. Complete Hagia and assert construction city happyLevel rises by +2.

C. Hanging Gardens inherited witness
Under Base AdvC/K-Mod-equivalent data, assert +1 GlobalHealth contributes +1 to the construction
city's projected health.

D. Eiffel breadth
Assert current Eiffel Tower +1 GlobalHappiness contributes +1 to its construction city's projected
happiness.

E. UI candidate-list visibility
With Red Cross's other health terms neutral, Alt-hover Health should still list Red Cross because its
+3 GlobalHealth changes the city. Analogous test for Hagia on Happiness.

F. AI immediate-threshold test
Create an unhealthy/starving city where +3 Red Cross health changes current waste/food deficit.
Assert the repaired local helper feeds that +3 into the immediate `iWasteDelta` branch while the
empire-global health term is not double counted for this same city.

G. F397 separation
Set LuxuryModifier=0 and no resource-conditioned happiness. Hagia's +2 must still reproduce F591.

H. F586 separation
Use non-TeamShare Red Cross in a one-player team. F591 must survive with no teammate recipient.

CURRENT STONE AFTER C031-WIP396
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F591 / KI#914 are CONFIRMED.
F592 / provisional KI#915 is UNUSED.
Latest physical checkpoint: C031-WIP396.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue under F592 unused. The BUG hypothetical-building projection family is now very dense
(F397/F398/F561/F577/F578/F590/F591 and related controls). Leave it unless a current field exposes a
mechanically different authoritative state transition, rather than simply another missing term.

Return to the SAS-vs-Base-AdvC XML activation sieve OUTSIDE these projection helpers: find current
SAS fields whose new sign/sentinel/cardinality combination reaches inherited city legality,
production, growth, culture, religion/corporation, ownership or plot-state code written for the old
domain. Require an ordinary current path, authoritative consequence and whole-album dedup before
promoting F592. Physically freeze WIP397 at the next settled tranche regardless.

===============================================================================
END C031-WIP396
===============================================================================

===============================================================================
C031-WIP397 — POST-F591 SAS-ACTIVATED XML DOMAIN SIEVE OUTSIDE BUG PROJECTIONS
===============================================================================

PURPOSE
-------
Continue Queue 006 under F592 unused after F590/F591, deliberately leaving the now-dense
BUG hypothetical-building projection family. Compare current SAS XML against Base AdvCiv
for newly activated sign/sentinel/nested-map domains that feed authoritative city state,
then require a live producer/consumer contradiction and whole-album semantic dedup before
numbering anything.

RESULT
------
No independent current-live F592 was established in this tranche. The newly activated
city-relevant domains checked here either have explicit signed handling / immediate
refreshes or map to already-owned roots. Preserve the controls below so later literal
coverage scans do not rediscover them.

CONTROL — PROTECTIONISM +25% CORPORATION MAINTENANCE IS SIGN-SAFE
-----------------------------------------------------------------
Current SAS activates a positive civic `iCorporationMaintenanceModifier` (+25 on
Protectionism), whereas Base AdvCiv's shipped civic values are nonpositive. The runtime
city calculation does not assume that the modifier itself is <= 0:

    iMaintenance *= std::max(0,
        GET_PLAYER(getOwner()).getCorporationMaintenanceModifier() + 100);
    iMaintenance /= 100;

and `CvPlayer::changeCorporationMaintenanceModifier` rebuilds maintenance. Thus +25
correctly means a 125% multiplier. The civic AI also uses the modifier with its sign,
so this SAS domain extension does not create a new runtime/state root.

CONTROL — SERFDOM'S NEW WATER-IMPROVEMENT COMMERCE REFRESHES LIVE YIELDS
------------------------------------------------------------------------
Current SAS removes Base AdvCiv's Town -1 commerce under Serfdom and adds +1 commerce to
Fishing Boats and Whaling Boats, in addition to the existing Farm/Plantation effects.
`CvPlayer::processCivics` applies every improvement/yield pair through
`changeImprovementYieldChange`, whose mutator stores the signed delta and immediately
calls `updateYield()`. The album had already established this producer/consumer contract
in WIP202. Water improvements do not enter a separate stale cache path here.

CONTROL — CIVIC SPECIALIST-VALID / SPECIALIST-COMMERCE ACTIVATIONS REFRESH ASSIGNMENT/YIELD
------------------------------------------------------------------------------------------
Current SAS newly activates civic SpecialistValids (e.g. Organized Religion Priest,
Police State Spy) and additional SpecialistExtraCommerces. `changeSpecialistValidCount`
marks city assignment AI dirty; specialist commerce/yield mutators run their existing
city/yield refresh paths. This remains the already-audited specialist/improvement mutator
family rather than F592.

CONTROL — SAILING EXTRA-WATER-SEE-FROM TECH CHANGE REBUILDS EXISTING SIGHT
-------------------------------------------------------------------------
Current SAS moves/activates `bExtraWaterSeeFrom=1` on Sailing. Although `CvPlot::seeFromLevel`
consults the team flag dynamically for water plots, `CvTeam::changeExtraWaterSeeFromCount`
explicitly performs the required old-state/new-state visibility transition:

    GC.getMap().updateSight(false);
    m_iExtraWaterSeeFromCount += iChange;
    GC.getMap().updateSight(true);

So already-stationary units/cities do not retain stale sight when the technology is
acquired or removed. No F592.

CONTROL — MOVED TERRAIN TRADE / COMMERCE-FLEXIBLE TECH FLAGS HAVE THEIR OWN REFRESHES
--------------------------------------------------------------------------------------
SAS moves coast terrain trade away from Sailing (to Seafaring) and activates a commerce
flexibility flag on Monarchy. `changeTerrainTradeCount` rebuilds every member's plot
groups, while `changeCommerceFlexibleCount` updates the team count and dirties the
relevant active-player UI state. No city copied-state omission was found.

CONTROL — OWNERSHIP-SENSITIVE COMMERCE FIELDS ARE ALREADY OWNED
---------------------------------------------------------------
The SAS-vs-AdvC building diff highlights more capturable buildings with
`CommerceChangeOriginalOwners`, but this is not a fresh ownership seam. F477 already
proves the authoritative historical-PlayerTypes alias problem for exactly this field,
and the later acquisition/provenance work traces preservation of building original
owner/time through `acquireCity`. Do not duplicate that family under F592.

CONTROL — NEW/LARGER FREE-SPECIALIST, GLOBAL-XP AND AREA EFFECTS USE BALANCED RUNTIME MUTATORS
---------------------------------------------------------------------------------------------
Current SAS activates/raises effects such as Notre Dame GlobalFreeSpecialist, Statue of
Liberty AreaFreeSpecialist, Pentagon GlobalFreeExperience and several TeamShare wonders.
`CvPlayer::processBuilding` applies these player/area counters, and TeamShare processing
replays player-level effects for each teammate as previously audited. The associated free
specialist mutators dirty assignment AI. F586 already owns AI recipient-cardinality breadth;
no distinct runtime loss/duplication was established here.

CONTROL — HOSPITAL HEAL RATE AND POSITIVE WAR-WEARINESS DOMAINS ARE DIRECT/SIGNED
--------------------------------------------------------------------------------
Hospital's larger +30 HealRate is stored in the city's nonnegative `m_iHealRate` and read
directly by `CvUnit::healRate`; no secondary cache boundary exists. SAS's positive
war-weariness modifiers (e.g. civic +25 or Mt. Rushmore +50 global) flow through the
existing `max(0, 100 + modifiers)` runtime formula; building/civic AI paths use the
opposite sign when valuing war-weariness penalties. Mt. Rushmore's separate omitted
Great-General-rate benefit remains F584, not a war-weariness sign bug.

CONTROL — STATE PROPERTY -20% CAPITAL PRODUCTION REMAINS F503 BREADTH
---------------------------------------------------------------------
The nested XML comparison again surfaces State Property's SAS-only negative capital
Production modifier. This is a useful current trigger for F503's stale process-commerce
cache, already recorded in WIP388; it must not consume F592.

XML-SIEVE CONCLUSION
--------------------
The SAS-vs-Base-AdvC activation scan was useful mainly as a domain proof map. Its strongest
new-looking city state changes either have explicit remove/change/readd or updateYield /
updateMaintenance / assignment-dirty transitions, or fall under already-numbered roots.
Continuing to enumerate XML fields is now more likely to re-hit coverage than expose a new
hard contradiction.

CURRENT STONE AFTER C031-WIP397
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F591 / KI#914 are CONFIRMED.
F592 / provisional KI#915 is UNUSED.
Latest physical checkpoint: C031-WIP397.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Change mechanical lens rather than squeeze the XML diff: audit authoritative CvCity and
city-adjacent counters/enum maps for integer-width, clamp and cross-source cardinality
assumptions under current SAS's expanded world sizes, effect magnitudes and combinations.
Prefer hard overflow/underflow/unsupported-domain evidence over AI-value heuristics. Dedup
semantically before numbering. Physically freeze WIP398 at the next settled tranche
regardless.

===============================================================================
END C031-WIP397
===============================================================================


===============================================================================
C031-WIP398 — QUEUE 006 CvCity.cpp — INTEGER-WIDTH PASS / F592 CONFIRMED
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
No repository source files modified.

F592 / KI#915 — CONFIRMED
-------------------------
TITLE:
ADVCIV'S `m_aiBuildingOriginalTime` COMPACTS CALENDAR YEARS INTO SIGNED 16-BIT
`short`; SAS STARTS AT -50,000, SO EARLY BUILDING DATES WRAP AND THEIR 1000-YEAR
COMMERCE DOUBLING CAN BE SUPPRESSED FOR THE ENTIRE NORMAL GAME

ROOT
----
`CvCity` exposes building construction time as an `int`, and ordinary construction
passes the current CALENDAR YEAR into it:

    void CvCity::setNumRealBuilding(...)
    {
        setNumRealBuildingTimed(...,
                GC.getGame().getGameTurnYear(), ...);
    }

But the authoritative storage is narrower:

    static int const iBuildingOriginalTimeUnknown = MIN_SHORT;
    EagerEnumMap<BuildingTypes,int,short,
            iBuildingOriginalTimeUnknown> m_aiBuildingOriginalTime;

`EnumMapBase::set` ultimately compacts the `int` through `_compact<short,int>`.
`ENUM_MAP_EXTRA_ASSERTS` is commented out in the shipped source, so the normal path is:

    return (ToType)fValue;

On the target two's-complement/MSVC integer representation this keeps the low 16 bits.
Therefore legal calendar years outside [-32768, 32767] do not survive the assignment.

CURRENT SAS ACTIVATES THE OUT-OF-RANGE DOMAIN ON TURN 0
-------------------------------------------------------
Current `Assets/XML/GlobalDefines_advc.xml` deliberately has:

    START_YEAR = -50000

All eight current shipped game speeds use that same Ancient-start date and end around AD 2105.
The first date representable by the signed-short field is reached only after several initial
turn states:

    Nitro       turn 2  (-32000)
    Turbo       turn 2  (-30000)
    Quick       turn 4  (-32000)
    Normal      turn 4  (-30000)
    Epic        turn 6  (-32000)
    Marathon    turn 9  (-32000)
    Slow        turn 14 (-32500)
    Very Slow   turn 16 (-32000)

Every building whose original time is written before those boundaries receives a corrupted
construction year.

EXACT ENCODING EXAMPLES
-----------------------
With signed 16-bit storage:

    -50000 ->  15536
    -45500 ->  20036
    -41000 ->  24536
    -36500 ->  29036

There is also an unavoidable sentinel collision at exactly -32768 because the map reserves:

    iBuildingOriginalTimeUnknown = MIN_SHORT = -32768

So the present encoding cannot represent every otherwise valid signed-short calendar year either.

GAMEPLAY CONSUMER — BUILDING CULTURE/COMMERCE AGE DOUBLING
----------------------------------------------------------
`CvCity::getBuildingCommerceByBuilding` applies Civ4's delayed building-commerce increase when:

    getGameTurnYear() - getBuildingOriginalTime(eBuilding) >=
            kBuilding.getCommerceChangeDoubleTime(eCommerce)

Current SAS has 72 concrete BuildingTypes with a 1000-year culture-commerce double time,
including Monument and its UBs, Library, Theatre, temples/monasteries, National Wonders and
many World Wonders.

For a clean example, current `BUILDING_MONUMENT` has:

    PrereqTech = TECH_MYSTICISM
    iCost = 30
    iAdvancedStartCost = 100
    ObsoleteSafe culture = +1
    CommerceChangeDoubleTime(culture) = 1000

If a Monument is created at -50000, its stored original time becomes +15536. The intended
age test after 1000 years would be approximately:

    -49000 - (-50000) = 1000    -> double culture

The actual test instead sees:

    -49000 - 15536 < 1000

Indeed, because the scheduled game ends around AD 2105, the current year never reaches
15536 + 1000. Such a Monument can therefore remain permanently at its undoubled age factor
for the entire ordinary game rather than doubling after 1000 calendar years.

DETERMINISTIC CURRENT SUPPORTED REPRO — ADVANCED START
------------------------------------------------------
This does not require malformed XML or a debugger.

1. Start an Ancient-era Advanced Start game. Current Ancient `iStartPercent=0`, so the game
   date is the configured -50000 start year.
2. Use a civilization/start state with Mysticism (many current civilizations have it) and
   enough Advanced Start points.
3. Found/buy a city as needed and purchase `BUILDING_MONUMENT`.
4. `CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_BUILDING, ...)` calls:

       pCity->setNumRealBuilding(eBuilding,
               pCity->getNumRealBuilding(eBuilding) + 1);

5. That stores `GC.getGame().getGameTurnYear()` = -50000 into the short-backed map,
   yielding +15536 on the target representation.
6. `CyCity::getBuildingOriginalTime` can expose the wrapped value directly; more importantly,
   the 1000-year commerce-age check uses the same corrupted value.

This is a deterministic supported turn-0 trigger. Ordinary hand-built early buildings may often
finish after the short-invalid window because SAS's first calendar increments are intentionally
large; that bounds prevalence but does not remove the live supported bug.

SAVE/LOAD AND CITY TRANSFER PRESERVE THE CORRUPTION
---------------------------------------------------
Current saves serialize the compact enum map directly:

    m_aiBuildingOriginalTime.write(pStream);
    m_aiBuildingOriginalTime.read(pStream);

So save/reload preserves the already-narrowed short value rather than reconstructing the real year.

`CvPlayer::acquireCity` likewise snapshots:

    aiBuildingOriginalTime.set(eBuilding,
            pOldCity->getBuildingOriginalTime(eBuilding));

and restores that value through `setNumRealBuildingTimed`. Since `getBuildingOriginalTime`
already returns the widened form of the compact `short`, acquisition carries the wrapped timestamp
forward; it does not repair the historical year.

ANCESTRY / ACTIVATION HISTORY
-----------------------------
The supplied Civ4CE/BtS source and K-Mod 1.46 both retain the original full-width representation:

    int* m_paiBuildingOriginalTime;

Base AdvCiv practical 3183 / commit
`59a3e3190061b421b117b58d047aa1196259f5fa` (2021-08-17), titled
"Rewrite of enum map classes", changed this field to the current short-backed enum map:

    static int const iBuildingOriginalTimeUnknown = MIN_SHORT;
    EagerEnumMap<BuildingTypes,int,short,...> m_aiBuildingOriginalTime;

Under Base AdvCiv's ordinary ~4000 BC timeline that compact range is sufficient.

SAS practical 4438 / commit `8b844543827034e697ca3a4815f4d6b4d5be7af0` later introduced a
-100000 start year. SAS practical 4789 / commit
`357017c68e6101cf8dfb0c55ea8de3e1dd7ff436` then changed it to the current -50000 while
retaining the compact AdvC field.

Classification:
    SAS-activated inherited AdvC storage-range assumption. BtS/K-Mod use full int; AdvC's compact
    representation is safe for its own default chronology, while SAS's expanded chronology makes
    the legal runtime value domain exceed it immediately.

DEDUP / ROOT BOUNDARY
---------------------
Whole-album search before promotion found no finding owning `m_aiBuildingOriginalTime`'s compact
storage width or `MIN_SHORT` sentinel.

Most importantly, this is distinct from F271 / KI#594:

* F271 is `getTurnYearForGame` truncating NEGATIVE FRACTIONAL BC YEARS toward zero under supported
  nondefault calendars/scenarios. Its building-age consequence is at most a one-calendar-year
  timestamp shift.
* F592 starts from an already-correct integral calendar year and then corrupts it when storing that
  year into a 16-bit city field. Current default SAS games activate it directly at -50000, and the
  resulting error can suppress the 1000-year doubling for the entire scheduled game.

It is also distinct from F495's cross-civilization building acquisition-index bug: F592 reproduces
in a single untouched city and acquisition merely preserves its already-corrupted timestamp.

REPAIR DIRECTION — NOT IMPLEMENTED
----------------------------------
Widen the authoritative building original-time representation so every supported calendar year can
be represented plus a distinct unknown sentinel. The simplest semantic type is full `int`, matching
BtS/K-Mod and the public getter/setter contract.

Because the compact enum map is serialized, changing its stored representation requires explicit
save-compatibility handling rather than blindly changing `short` to `int` in the declaration.
A versioned reader can widen legacy short values for ordinary old timelines, while current/future
saves store full-width years.

A compact offset encoding is possible in principle, but tying persistent city provenance to the
current global START_YEAR would complicate scenarios, nondefault start years and save compatibility.
Full-width calendar-year storage is the robust contract.

REGRESSION TESTS
----------------
A. Turn-0 Advanced Start Monument
1. Ancient start, START_YEAR=-50000, Mysticism, sufficient Advanced Start points.
2. Purchase Monument.
3. Assert getBuildingOriginalTime(Monument) == -50000 exactly.

B. 1000-year age threshold
Construct/create a +1-culture Monument at -50000. At -49000, assert its age-multiplied base culture
has doubled according to CommerceChangeDoubleTime=1000.

C. Save/reload
Save immediately after creating the -50000 Monument; reload and assert original time remains -50000
and the later doubling threshold is unchanged.

D. City transfer
Transfer a qualifying early building through a path that preserves it and assert its full original
year survives acquisition without wrapping.

E. Boundary years
Round-trip at least -50000, -32769, -32768, -32767, 0, 32767 and a supported large positive
scenario year. The unknown sentinel must remain distinct from every valid timestamp.

F. Base-timeline compatibility
A normal Base-AdvC-like -4000 building timestamp should remain unchanged; widening must not alter
ordinary age semantics.

CURRENT STONE AFTER C031-WIP398
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
Latest physical checkpoint: C031-WIP398.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Continue the integer-width / clamp / cardinality audit under F593 unused. The building-original-time
short is now owned by F592; do not renumber its save/acquisition/sentinel manifestations. Challenge
other compact authoritative city fields only against CURRENT reachable magnitudes/cardinalities,
then change lens if those remain comfortably bounded. Prefer a hard current-live overflow,
sentinel collision or state mismatch over speculative extended-game limits. Semantic whole-album
dedup before numbering. Physically freeze WIP399 at the next settled tranche regardless.

===============================================================================
END C031-WIP398
===============================================================================


===============================================================================
C031-WIP399 — QUEUE 006 CvCity.cpp — REMAINING COMPACT-FIELD BOUNDS CONTROL
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
No repository source files modified.

PURPOSE
-------
After F592 proved one real compact-storage domain failure, finish the same mechanical lens rather
than assuming every `char`/`short` city enum map is suspicious. The remaining compact fields were
checked against current reachable magnitudes/cardinalities and their actual mutators/consumers.
No second current-live width failure was established in this tranche.

CONTROL — PRODUCTION/DECAY AGE COUNTERS FIT CURRENT TURN HORIZON
---------------------------------------------------------------
`m_aiBuildingProductionTime` and `m_aiUnitProductionTime` are signed-short-backed elapsed-turn
counters. Current shipped game speeds run from 165 turns (Nitro) through 2000 turns (Very Slow).
The normal decay lifecycle increments these counters at most once per city turn and resets them when
production resumes/clears. Their current scheduled-game domain therefore remains far below 32767.

This is intentionally different from F592: those counters store TURN COUNTS, whereas
`m_aiBuildingOriginalTime` stores CALENDAR YEARS and SAS jumps immediately to -50000.

CONTROL — CITY RANK SHORTS REMAIN BELOW CURRENT CITY CARDINALITY
---------------------------------------------------------------
`m_aiBaseYieldRank`, `m_aiYieldRank` and `m_aiCommerceRank` use signed short with `-1` as the dirty
sentinel. Real ranks are positive city ordinal values.

Current SAS's largest XML world size is `WORLDSIZE_SAS48` (99 x 70 terrain cells). AdvC normally
converts XML cells to plots with the current 2-plots-per-cell scale, i.e. 198 x 140 = 27,720 plots,
already fewer than SHRT_MAX even before applying ordinary city-spacing constraints. Map scripts can
override dimensions and switch to the legacy 4-plots-per-cell scale, but ordinary city founding still
uses `MIN_CITY_RANGE=2`; no current shipped path capable of producing 32,768 legal cities was found.
No rank-sentinel collision is reachable in normal current data.

CONTROL — REAL BUILDING COUNT CHAR IS HARD-CAPPED AT ONE
---------------------------------------------------------
`m_aiNumRealBuilding` is char-backed, but current/base `CITY_MAX_NUM_BUILDINGS=1` and construction,
acquisition and AI legality all use that cap. World/National/ordinary buildings therefore do not
approach the compact count limit.

`m_aiNumFreeBuilding` is also char-backed. The earlier WIP349 free-building audit already proved that
live city-side callers use thresholded 0/1 state after empire-level source aggregation. Do not reopen
that family as an integer-width issue.

CONTROL — SPECIALIST / XP / RELIGION COMPACT SUMS ARE SMALL UNDER CURRENT XML
-----------------------------------------------------------------------------
Current source stacking stays comfortably inside signed-char/signed-short ranges for:

* DomainFreeExperience / UnitCombatFreeExperience;
* DomainProductionModifier;
* specialist count/max/forced/free-specialist maps;
* ImprovementFreeSpecialists;
* ReligionInfluence / StateReligionHappiness / Shrine count;
* FreePromotionCount;
* event-derived BuildingClass happiness/health deltas.

The earlier XML activation pass already measured representative worst-case stacks (e.g. land-domain
free XP in the low tens, not anywhere near 127). City population/BFC cardinality further bounds the
specialist/improvement-driven maps. No current combination approaches its encoded limit.

CONTROL — BONUS COUNTERS HAVE WIDE ENOUGH CURRENT GENERATED DOMAIN
------------------------------------------------------------------
`m_aiNumBonuses` and `m_aiNumCorpProducedBonuses` use short; `m_aiNoBonus` / `m_aiFreeBonus` use
char as SOURCE counters. Current NoBonus/FreeBonus building sources are sparse and stay in the
single digits. Normal map-resource generation likewise does not create tens of thousands of one
connected BonusType for one city. A deliberately pathological scenario could exceed such a bound,
but no ordinary current SAS generator/data path does; that does not meet the present current-live
finding threshold.

CONTROL — REVOLT-PER-PLAYER SHORT IS TURN-HORIZON BOUNDED
---------------------------------------------------------
`m_aiNumRevolts` is a short-backed count for a given PlayerTypes entry. Current scheduled games have
at most 2000 turns, and a city cannot accumulate 32768 distinct per-turn revolt increments inside
that horizon. No current width issue.

COMPACT-FIELD CONCLUSION
------------------------
F592 is the exceptional current width failure in this family because SAS changed the VALUE UNIT's
range (calendar year) without changing the compact representation. The other compact city maps are
counts/ranks/turn timers whose current reachable domains remain comfortably bounded.

Do not continue scanning field declarations merely because `char`/`short` appears; a new finding
requires a current producer capable of reaching the bound or colliding with a sentinel.

CURRENT STONE AFTER C031-WIP399
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
Latest physical checkpoint: C031-WIP399.
Actual AdvCiv-SAS source remains UNTOUCHED.

NEXT
----
Change mechanical lens. Prefer authoritative arithmetic/state transitions whose correctness depends
on UNITS or REPRESENTATION rather than literal function coverage: calendar/year-vs-turn confusion,
percent-vs-times100 confusion, plot-vs-city ownership identity, and cached values whose producer and
consumer use different scaling. Use current SAS data/normal callers first, ancestry only as an oracle,
and whole-album semantic dedup before numbering. Physically freeze WIP400 regardless.

===============================================================================
END C031-WIP399
===============================================================================


===============================================================================
C031-WIP400 — QUEUE 006 CvCity.cpp — UNIT/SCALE + CURRENT-vs-BASE BODY CLOSURE
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
No repository/gameplay source files modified.

PURPOSE
-------
After the compact-storage pass, change representation lens rather than continuing literal field
searches. First challenge obvious unit/scale seams (Times100 vs whole units, turn counts vs calendar
years, percent scaling, player/team identity). Then mechanically re-diff the current SAS
`CvCity.cpp` against supplied Base AdvCiv 1.14 by function body and inspect the remaining changed
bodies with low literal album coverage. This is a completeness/control stone, not a reason to
renumber already-owned current repairs or recorder instrumentation.

UNIT/SCALE PRE-SIEVE — NO NEW ROOT
----------------------------------
The first scale-unit scan mostly routes into already-owned semantic families. Current culture,
maintenance and trade Times100 helpers generally convert at their documented boundaries, while
previous stones already own the meaningful exceptions. F592 is intentionally not generalized:
it is a storage-domain mismatch (calendar YEAR into signed short), not evidence that every
unit-bearing city field is wrong.

No independent current-live percent-vs-Times100, turn-vs-year or player-vs-team contradiction was
established in this preliminary scale pass.

CURRENT SAS -> BASE ADVCIV BODY SIEVE
-------------------------------------
A current-body comparison isolates the finite set of `CvCity` functions that differ from supplied
Base AdvCiv 1.14. The low/moderate-coverage bodies were re-opened individually instead of treating
any textual delta as semantic gameplay divergence.

SAFE / OBSERVATIONAL CURRENT DELTAS
-----------------------------------
The following current SAS deltas preserve the inherited gameplay mutation and add only recorder
state capture, diagnostics, or cached constants/info IDs where checked:

* `initConscriptedUnit` — GameRecord/diagnostic context only around inherited unit creation.
* `changeSpecialistCount` — `_DEBUG` assertion/diagnostic only; release mutation remains the
  inherited `setSpecialistCount(old + delta)`.
* `isSpecialistValid` — caches `DEFAULT_SPECIALIST`; predicate is unchanged.
* `setCultureLevel` — caches UI highlight color for the existing message/replay path.
* `addRevoltFreeUnits` — caches revolt-unit defines; arithmetic unchanged.
* `alterWorkingPlot` — caches `DEFAULT_SPECIALIST`; assignment semantics unchanged.
* `setNumRealBuildingTimed` — caches `BUILDING_TEXT` color; building gameplay state unchanged by
  this current-vs-Base delta. Its deeper building-state contracts are already audited elsewhere.
* `doGrowth` — captures pre/post population/food/granary values and logs growth/starvation/avoid-
  growth events; the inherited `changeFood`, granary clamp, population and starvation order is
  preserved.
* `doPlotCultureTimes100` — caches the capitulation culture percentage; same scaled calculation.
* `doCheckProduction` — stores already-read production in locals and snapshots active/queued state
  only when GameRecord level requires it; the same maxed-class production banks are cleared.
* `doDecay` — names the inherited decay result `iNewProduction` so the actual mechanical loss can
  be logged; formula and setters are unchanged.
* `doReligion` — caches `RELIGION_SPREAD_RAND`; spread probability arithmetic unchanged.
* `doPartisans` — caches event-trigger ID; gameplay path unchanged.
* `initialPopulation` — caches initial-population define; result unchanged.
* `applyEvent` — caches RED UI color and records before/after plot state around the inherited
  event pillage mutation. The event arithmetic/state roots remain F413/F566/F575 controls.
* `failProduction` — GameRecord context plus cached UI color; fail-gold gameplay unchanged.
* `handleOverflow` — the current output parameters expose positive lost production vs negative
  unused capacity for GameRecord. `computeOverflow`, overflow storage and overflow-gold gameplay
  remain inherited; this was already explicitly controlled in earlier Queue-006 stones.
* `hurry` — caches the already-computed hurry production and pre-change values for GameRecord;
  the same production/population/gold/anger mutations execute in the same order.
* `upgradeProduction` — current extra reads/logging are observational. The inherited absolute
  destination-bank assignment itself is the already-confirmed F559 / KI#882 root; do not renumber
  it because current instrumentation makes the overwrite more visible.

CURRENT SEMANTIC DELTAS ALREADY OWNED
-------------------------------------
Several changed bodies really are semantic, but their behavior belongs to explicit existing SAS
repairs/policies rather than F593:

* `canBeSelected` — current `NO_PLAYER` guard belongs to KI#162 production-bar/autoplay hardening.
* `getProductionBarPercentages` — owner/registration, typed-order and stable production snapshot
  hardening is the existing KI#162/KI#103 family; this is UI/projection code, not a new
  authoritative production root.
* `alterSpecialistCount` — Artist provenance transition belongs to KI#313; the other delta is the
  cached default specialist.
* `setName` — null/empty and initial/acquisition naming behavior is the already-preserved
  KI#161/KI#161.2 family.
* `liberate` — caching old owner/team before acquisition is the already-recorded inherited
  liberation lifetime repair; its purpose is precisely to avoid reading the replaced city through
  `this` after ownership reconstruction.
* `SASTryEmergencyBuilding` / current `doTurn` forced-production changes remain under the
  already-audited KI#333/KI#310/KI#313 and emergency-production controls; the current KI#333
  priority bug is fixed.

OTHER CHANGED BODIES WITH NO NEW AUTHORITATIVE ROOT
---------------------------------------------------
`setLayoutDirty` and `addGreatWall` are graphical/layout surfaces. `read`/`write` were already
closed by WIP389's complete current-format 117-read <-> 117-write authoritative stream census plus
existing SAS-state persistence findings. `getProductionBarPercentages` is handled above as UI.
The heavily represented `init`, `kill`, `doTurn`, `hurry`, acquisition/liberation and production
lifecycle bodies retain their earlier semantic findings/controls; this body diff provides no basis
to duplicate them merely because the current source contains logging or later fixes.

CONCLUSION
----------
The current-vs-Base `CvCity.cpp` body frontier is substantially more saturated than literal function
mention counts imply. The low-coverage differences remaining after F592 are overwhelmingly
observational/performance deltas or explicitly owned repairs. No independent current-live F593 was
established in this tranche.

This is useful negative evidence: further Queue-006 work should not repeatedly re-diff current SAS
against Base AdvCiv looking for textual changes. The higher-value frontier is now inherited city
contracts activated by current data/state combinations, or cross-object lifecycle invariants not
visible as a current-vs-Base body delta.

CURRENT STONE AFTER C031-WIP400
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
Latest physical checkpoint: C031-WIP400.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Leave the current-vs-Base body-diff lens behind. Resume under F593 unused with a cross-object
contract sieve: authoritative `CvCity` mutations whose correctness depends on a matching update in
`CvPlot`, `CvArea`, `CvPlayer`, `CvTeam` or `CvGame`, especially transitions with rollback/removal,
ownership replacement or a subtype-specific side effect. Prefer a reproducible hard state/count
contradiction over UI valuation or generic robustness. Whole-album semantic dedup before numbering;
physically freeze WIP401 regardless.

===============================================================================
END C031-WIP400
===============================================================================


===============================================================================
C031-WIP401 — QUEUE 006 CvCity.cpp — CROSS-OBJECT BALANCE / INVERSE CONTRACTS
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
No repository/gameplay source files modified.

PURPOSE
-------
Follow every small set of authoritative `CvCity` mutations that maintain aggregates or state in
`CvPlot`, `CvArea`, `CvPlayer`, `CvTeam` or `CvGame`. Prefer scope/identity/inverse contradictions
of the F589 kind over helper projections. The direct external aggregate surface is finite: city
counts, population/power, building/project/making counts, religion/corporation counts, gold, sight,
and a few one-shot global building states.

DIRECT COUNT/AGGREGATE PAIRS — NO NEW ROOT
-------------------------------------------
The ordinary city lifecycle remains balanced for the inspected external aggregates:

* init/kill pair land-area, team and game city counts;
* setPopulation changes area/player/team/game population by the same delta, while player population
  power/assets/score are deliberately recomputed from total population inside
  `CvPlayer::changeTotalPopulation`;
* building persistent state changes owner/area power and player/team building-class counts on the
  same effective active-building transition;
* religion/corporation add/remove paths update the current owner count and trigger their required
  building-commerce / AI-work refreshes;
* production queue start/stop/upgrade “making” counters remain under the earlier production
  lifecycle findings/controls, especially F559 for the independent destination hammer-bank
  overwrite; no new aggregate attribution mismatch was established here.

WATER-AREA COASTAL CITY COUNTS — KI#346 REPAIR IS COMPLETE FOR LIVE ICE REBUILD
-------------------------------------------------------------------------------
A tempting sibling of the recent KI#346 fix was checked carefully. Cities are counted in both
land `CvArea` and their adjacent passable water `CvArea`; replacement water areas therefore cannot
recover coastal-city counts merely by processing water plots.

Current `CvMap::recalculateWaterAreas()` explicitly handles this correctly:

1. detach only water plots without processing/decrementing the old areas;
2. delete old water `CvArea` objects;
3. rebuild all water areas and representative areas;
4. only after every water plot has its replacement area, iterate all city plots and increment the
   new `waterArea(true)->CitiesPerPlayer` for the city owner.

Thus the live Ice-melt reconstruction does not lose coastal-city counts.

MALFORMED INHERITED `processArea` NEGATIVE GUARD — REAL CODE SMELL, NOT CURRENT F593
------------------------------------------------------------------------------------
`CvPlot::processArea()` contains this inherited AdvCiv condition around its old coastal-city
negative-count guard:

    if (iChange > 0 || (iChange < 0 &&
        pWaterArea->getCitiesPerPlayer(getOwner()) > 0, true))

Because of the comma operator, the parenthesized right side evaluates to `true`; the intended
`> 0` protection is ineffective whenever a water area exists. Base AdvCiv 1.14 contains the same
text; K-Mod/BtS do not have AdvCiv's adjacent-water city-count extension.

However, no ordinary current-live trigger was established for the dangerous branch:

* wholesale current `CvMap::recalculateAreas()` first detaches every plot with
  `setArea(NULL, false)`, so it does not process/decrement the old city plot areas;
* map-script and current `CvWBDesc.applyMap` recalculations occur during map construction before
  WB cities are applied;
* the new live Ice path uses `recalculateWaterAreas()` above and bypasses this old negative path;
* water<->land plot-type changes erase a city before the plot-area transition.

Keep the comma expression as generic robustness/dead-defensive-code debt unless a supported caller
is found that runs `processArea(city,-1)` while the city still exists. Do not spend F593 merely on
the malformed expression.

ONE-SHOT GLOBAL BUILDING STATE — INVERSE CONTRACTS CHECKED
----------------------------------------------------------
`setNumRealBuildingTimed` has several intentionally asymmetric global effects. The existing F588
Golden-Age TeamShare root remains separate. The neighboring state was checked for destruction:

* building-class “created” count is explicitly EVER-created history and does not decrement;
* `makeNukesValid(true)` is the intended permanent Manhattan-style unlock once enabled;
* Apostolic-Palace `VoteSourceReligion` persists as metadata after destruction, but the active
  institution is not keyed to that field. `CvGame::processBuilding()` changes the vote source's
  `DiploVoteCount` with building add/remove, and `isDiploVote` is exactly `count > 0`. Therefore
  razing/removing the AP disables the live vote source even though its old religion enum remains;
  rebuilding the vote-source building sets the religion again on first construction.

No independent stale active-vote-source root was established.

CROSS-OBJECT CONCLUSION
-----------------------
F589 remains unusual because a generic player-indexed city-count mutator carried a semantic side
effect specifically named/consumed as Barbarian history without checking the player subtype. The
other direct city external aggregates inspected here either update exactly the supplied owner/team,
are paired on removal, or have explicit one-way semantics.

No independent current-live F593 was established in this tranche.

CURRENT STONE AFTER C031-WIP401
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
Latest physical checkpoint: C031-WIP401.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Change lens again. Audit hard runtime PRECONDITIONS inside `CvCity.cpp`: random-number ranges,
divisors, array/index bounds and min/max expressions whose legality depends on current XML or live
state. F413 already owns event pillage's exclusive/zero random range; deduplicate it. Prefer a
normal current-data zero/negative range, divide-by-zero, invalid enum/index or inverted bound over
AI-quality heuristics. Physically freeze WIP402 regardless.

===============================================================================
END C031-WIP401
===============================================================================


===============================================================================
C031-WIP402 — QUEUE 006 CvCity.cpp — HARD PRECONDITION DEDUP / F593 UNUSED
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN at this checkpoint.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
No repository/gameplay source files modified.

PURPOSE
-------
Mechanically revisit hard runtime preconditions: synchronized-random ranges, arithmetic divisors and
index/range expressions whose validity depends on current data/state. This pass is deliberately
short once it intersects earlier semantic controls; do not inflate literal re-auditing into new
coverage.

SYNCHRONIZED RANDOM CALL CENSUS
-------------------------------
The small current `CvCity.cpp` synchronized-random surface was checked:

* WLTKD: current inherited `WE_LOVE_THE_KING_RAND = 1000`; positive range.
* religion grip randomization: current `RELIGION_INFLUENCE_RANDOM_WEIGHT = 40`; positive range.
* event improvement pillage: already F413 / KI#736, including the exclusive maximum and
  min==max zero-range case; current SAS RNG defensive behavior makes the fixed Looters-1 outcome
  safe, but the inclusive-range root remains owned by F413.
* event random city-plot offset: `NUM_CITY_PLOTS` is a positive compile-time city-radius
  cardinality.
* Great-Person unit selection: ordinary current `doGreatPeople()` advances generic GP progress and
  the per-unit progress buckets from the same current GP rates before the threshold selection.
  Current shipped Python gameplay contains no call that independently raises generic city GP
  progress through the exposed `CyCity.changeGreatPeopleProgress` API. A deliberately scripted
  mismatched state/old malformed save could still make the selector's total-unit-progress range
  pathological, but no new ordinary current producer was established.

DIVISOR/RANGE PRECONDITION DEDUP
--------------------------------
The apparently most dangerous arithmetic branch is not fresh:

    iModifier = getBaseYieldRateModifier(YIELD_PRODUCTION, iProductionModifier)
    ...
    iFeatureProdNeeded = iTmp / iModifier - iRate

`getBaseYieldRateModifier` can generically clamp to zero. WIP351 already audited exactly this
zero-denominator shape and proved current shipped SAS data do not reach the 0%-production-rate
state; preserve it as mod-mod robustness rather than F593.

Other denominator families inspected or mechanically re-entered also map to prior controls:

* `flatHurryAngerLength`, `flatConscriptAngerLength` and `flatDefyResolutionAngerLength` each clamp
  to at least 1 before downstream division; hurry-anger arithmetic has its own earlier numbered
  rounding/timer findings.
* religion distance normalization through `maxTypicalDistance()` is not a denominator-zero root;
  its real current defect is the already-confirmed F568/KI#891 hard-bound misuse on oversized /
  custom-dimension maps.
* religion spread uses a current positive base/divisor path; its out-of-intended-range distance
  factor is likewise F568.
* production-turn estimates explicitly return `MAX_INT` when production difference is zero.
* current game-speed percentages used by production decay are positive; their preview/runtime
  mismatches are already F391/F560 families.

CONCLUSION
----------
This precondition sieve does not establish F593. More importantly, it confirms that WIP351 and the
later event/religion/production findings already own the meaningful hard-range seams. Continuing to
scan `/` and random-call syntax inside `CvCity.cpp` would now mostly reproduce those stones.

CURRENT STONE AFTER C031-WIP402
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` remains OPEN at this physical checkpoint.
F559 / KI#882 through F592 / KI#915 are CONFIRMED.
F593 / provisional KI#916 is UNUSED.
Latest physical checkpoint: C031-WIP402.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Perform a Queue-006 closure decision rather than inventing another near-duplicate lens. Reconcile
all late controls (WIP388-WIP402), confirm there is no stranded unnumbered current-live candidate,
and if semantic saturation holds, close the protected deep-method `CvCity.cpp` queue with F593
unused. Then advance to the next deep-method queue/file instead of padding CvCity coverage.

===============================================================================
END C031-WIP402
===============================================================================


===============================================================================
C031-WIP403 — QUEUE 006 CLOSURE — FINAL RECONCILIATION / SEMANTIC SATURATION
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` is CLOSED for the planned deep-method re-audit.
The protected deep-method cursor advances to Queue 007 `CvGameCoreDLL/CvUnit.cpp`.
F559 / KI#882 through F592 / KI#915 are the confirmed new-root span from this Queue-006
re-audit, with older roots also broadened/reopened where documented.
F593 / provisional KI#916 remains UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T010258.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository source files modified.

POST-WIP402 RECONCILIATION — NO STRANDED CURRENT-LIVE CANDIDATE
---------------------------------------------------------------
The final closure pass reread the post-saturation stones WIP388-WIP402 specifically for any
candidate that had been left in chat/WIP prose without either promotion, semantic ownership,
or a current-live rejection.

No such candidate survives.

The important late controls are all classified:
- the generic processBuilding resource/effect ordering hazard is current-data masked;
- acquisition ScriptData loss is generic scenario/Python robustness under shipped SAS rather
  than a demonstrated current gameplay transfer contract;
- post-delete `this` reads are already owned by F572/F573;
- water-area/lake/fresh-water reconstruction concerns close under representative-area and
  current KI#346 lifecycle handling;
- No-Nukes queue invalidation closes through doCheckProduction before city production;
- Water Wheel irrigation/ignore-irrigation ordering is semantically split and correct;
- Engineering/Construction movement-rule path-cache concerns remain the earlier deep
  GroupPathFinder control without a supported current stale-reuse sequence;
- `setForceRevealedBonus` and the lower-level CvPlot mutators preserve authoritative
  remove/change/re-add or already-owned trade-cache contracts;
- current water-area reconstruction restores coastal city counts explicitly;
- the inherited `CvPlot::processArea` comma-operator guard is malformed but no current live
  path has been established that executes its dangerous negative branch while a city remains;
- Great-Person random selection has no shipped producer that can raise generic GP progress
  independently of per-unit progress;
- generic zero-production-modifier division remains current-data-masked and was already
  preserved in WIP351;
- the remaining compact city fields stay comfortably within current cardinality/magnitude
  bounds after F592 isolates the one actual calendar-year/short-domain failure.

The late pass therefore leaves no hidden provisional F593 behind the physical checkpoints.

QUEUE 006 METHOD COVERAGE AT CLOSURE
------------------------------------
The stronger Queue-006 re-audit has now exercised, at minimum:

* current SAS vs Base AdvCiv semantic body deltas and the final low-coverage changed-body
  census;
* K-Mod / Civ4CE / BtS ancestry where refactor or inherited behavior mattered;
* population, growth, citizen/specialist and Great-Person state;
* production completion, upgrade-bank transfer, overflow, hurry, decay, emergency fallback,
  legality revalidation and ORDER_MAINTAIN lifecycle;
* real/free building counts, replacement, obsolescence, one-shot effects, TeamShare and
  acquisition provenance;
* building-derived yield/commerce/happiness/health projections and AI_buildingValue runtime
  effect coverage;
* religion, corporation, holy-city/HQ and event application/transfer state;
* owner/team/player/area/plot-domain contracts, including hypothetical-owner maintenance and
  city acquisition/destruction transitions;
* city -> plot/area/player/team/game aggregate balance and coastal-water-area reconstruction;
* trade-route, commerce, maintenance, visibility, rank and surrounding-health/happiness cache
  invalidation families;
* read/write serialization symmetry and derived/runtime-only members;
* SAS-activated XML sign, sentinel, namespace and cross-field domains;
* fixed-capacity / compact-field / integer-width bounds under current SAS data and calendars;
* random-range, divisor, enum/sentinel and threshold preconditions;
* explicit uncertainty/bug comments and mechanical producer/consumer / add-remove / cross-zero
  invariant sieves;
* mandatory whole-album semantic dedup before every late candidate was numbered.

The late stages still produced F589-F592 after multiple saturation stones, demonstrating that
these sieves remained capable of finding independent current defects rather than merely
closing candidates. After F592, WIP399-WIP402 and the reconciliation above leave no
independent current-live root for F593.

Leaving F593 unused is therefore the correct Queue-006 saturation result.

QUEUE 006 RESULT
----------------
The protected `CvCity.cpp` deep-method re-audit is CLOSED with 34 confirmed new roots:
F559/KI#882 through F592/KI#915 inclusive.

The final late roots are useful examples of the breadth reached by the audit:
- F589 / KI#912: generic area city-count changes incorrectly report every positive city as a
  Barbarian city creation, corrupting the destroyed-Barbarian-city history signal;
- F590 / KI#913: candidate-building additional-yield projection omits player-global sea-plot
  yield changes;
- F591 / KI#914: candidate-building additional happiness/health helpers omit the corresponding
  GlobalHappiness/GlobalHealth fields while modeling AreaHappiness/AreaHealth;
- F592 / KI#915: AdvC's compact signed-short building-original-year storage cannot represent
  SAS's -50,000 start calendar, corrupting early building-age culture-doubling timestamps.

These are independent roots with distinct producers, consumers and repair surfaces; F593 is
not needed to describe any remaining current-live Queue-006 behavior.

CURRENT STONE AFTER C031-WIP403
-------------------------------
Queue 006 `CvGameCoreDLL/CvCity.cpp` — DEEP-METHOD RE-AUDIT CLOSED.
Protected deep-method cursor: Queue 007 `CvGameCoreDLL/CvUnit.cpp` NEXT.
F559 / KI#882 through F592 / KI#915 CONFIRMED across Queue 006.
F593 / provisional KI#916 UNUSED.
Latest physical checkpoint: C031-WIP403.
No repository source modified.

NEXT
----
Advance to protected deep-method re-audit Queue 007 `CvGameCoreDLL/CvUnit.cpp` using the same
stronger method used for Queues 001-006. Treat the earlier original File-007 review as useful
baseline rather than present-day closure.

Begin by reconciling the old File-007 results (Great-General visibility, Military Advisor
battle-location visibility, KI#331 scrap contract and the old false-positive controls) against
current SAS source and Known Issues. Then build a fresh current-SAS-vs-Base-AdvC semantic body
map, trace hard unit lifecycle/state contracts (init/kill/delayed death, group membership,
movement/setXY, combat damage/death/capture, promotions/experience, upgrade/scrap/gift,
transport/cargo, blockade/visibility, mission actions and serialization), use K-Mod/Civ4CE
ancestry selectively, and finish with low-coverage/invariant sieves before closure.

F593 / provisional KI#916 remains the next unused root slot.

END C031-WIP403


===============================================================================
C031-WIP404 — PROTECTED QUEUE 007 CvUnit.cpp — OPENING RECONCILIATION
===============================================================================

STATUS
------
Queue 006 `CvGameCoreDLL/CvCity.cpp` is CLOSED by C031-WIP403.
Queue 007 `CvGameCoreDLL/CvUnit.cpp` is now OPEN under the protected deep-method
re-audit.
F559 / KI#882 through F592 / KI#915 remain CONFIRMED from Queue 006.
F593 / provisional KI#916 is UNUSED.
No repository/gameplay source files modified.

SOURCE / COMPARATORS
--------------------
Current authority: attached AdvCiv-SAS light source snapshot 2026-09-06.
Current CvUnit.cpp: 13,085 lines.
Local ancestry comparators:
* Base AdvCiv 1.14 CvUnit.cpp: 12,179 lines.
* K-Mod 1.46 CvUnit.cpp: 13,823 lines.
* Civ4CE BtS CvUnit.cpp: 12,675 lines.

This queue treats the original File-007 review as a baseline/control, not present-day
closure. Fresh numbering begins only after current-source reconciliation and whole-album
semantic dedup.

OPENING RECONCILIATION — OLD F013 / KI#334 GREAT-GENERAL VISIBILITY
-------------------------------------------------------------------
The old inherited Great-General killer-identity leak is repaired in CURRENT source.

Current `CvUnit::kill` binds the validated killer player/team once. For ordinary third-
party observers, the exact message naming the killer is sent only when the observer has
met BOTH victim and killer teams (spectators remain unrestricted). An observer that knows
the victim but not the killer receives `TXT_KEY_MISC_GENERAL_KILLED_PUBLIC`, which omits
the killer identity.

The SAS Timeline/replay amplification is also repaired: the persistent coordinate-less
replay message now always uses the sanitized public text. A one-player replay visibility
record therefore no longer stores information that requires two-team contact knowledge.

Known Issues currently marks KI#334 fixed and documents the same repair. Do not rediscover
this old root as F593.

OPENING RECONCILIATION — OLD F014 / KI#335 BATTLES VASSAL PERSPECTIVE
---------------------------------------------------------------------
The old Military Advisor Battles subject-vs-observer map leak is repaired in CURRENT
Python. `CvMilitaryAdvisor.py` retains the selected vassal as the battle-history subject,
but centralizes the plot/context conversion through `getBattleObserverPlotContext` and,
outside debug mode, uses the real active human as observer. Hidden rows receive sanitized
coordinates/context, which is then shared by the table, LOG path and camera-action state.
Known Issues currently marks KI#335 fixed.

This remains relevant provenance for the CvUnit battle-history event producers, but the
privacy defect itself is no longer current and must not consume F593.

OPENING RECONCILIATION — OLD F002 / KI#331 SCRAP CONTRACT
---------------------------------------------------------
The old silent-veto scrap contract is repaired in CURRENT source.

`CvUnit::scrap()` now returns bool:

    if (!canScrap())
        return false;
    scrapInternal();
    return true;

Routine callers checked in the previous protected queues branch on that success. Mandatory
cleanup has a separate `scrapForced(bool bMandatoryGameRule)` path, while the actual
mutation is centralized in `scrapInternal()`. This prevents the former "caller reports
handled/killed although canScrap vetoed removal" mismatch without weakening the restrictive
SAS preservation policy.

Do not allocate F593 to old KI#331 caller sites unless a NEW active caller violates the
current bool/forced contract.

CURRENT-VS-BASE MEMBER BODY INVENTORY
-------------------------------------
A fresh brace-aware current-vs-Base comparison finds 456 current CvUnit member definitions
vs 454 in Base AdvCiv 1.14. The two current-only helpers are the KI#331 repair surface:
`scrapForced` and `scrapInternal`.

After stripping comments/whitespace, 56 shared member bodies differ from Base. The changed
surface includes the expected Great-General/Battle-history logging path, KI#331 scrapping,
upgrade routing, espionage provenance, combat/GameRecord hooks, air/bombard/pillage/nuke
logging, and the existing combat-strength/visibility fixes. This is close to the old
File-007 53-body frontier but not identical; every changed body will be reconciled against
current behavior rather than assumed safe from the earlier pass.

FIRST WHOLE-FILE CONTROL — STALE MISSION-AI UNIT ID WARNING IS ALREADY CLOSED
------------------------------------------------------------------------------
`CvUnit::kill` still carries AdvC's caution that SelectionGroups are not scanned for a
`m_missionAIUnit` reference to the dying unit. This looks dangerous if a unit slot is later
reused, but the album's Queue-041 `CvSelectionGroupAI.cpp` deep audit already traced the
FFreeList representation: the full unit ID contains a generation component and `getAt`
validates that complete ID. Ordinary slot reuse therefore resolves the stale IDInfo to NULL,
not to an unrelated replacement unit. Only extreme generation wrap remains theoretical.
Do not promote this known-issue comment under F593.

CURRENT STONE AFTER C031-WIP404
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / provisional KI#916 remains UNUSED.
Latest physical checkpoint: C031-WIP404.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Continue with hard unit lifecycle/state contracts: init/finalize/kill/delayed death and
setXY/group/transport membership first, then combat damage/death/capture, promotion/XP,
upgrade/gift/scrap, blockade/visibility and mission actions. Reconcile changed methods and
also audit inherited whole-file invariants. Use K-Mod/Civ4CE ancestry selectively and whole-
album semantic dedup before numbering. Physically freeze WIP405 at the next settled tranche
regardless.

===============================================================================
END C031-WIP404
===============================================================================


===============================================================================
C031-WIP405 — QUEUE 007 CvUnit.cpp — CONVERT/GIFT IMMOBILITY LIFETIME
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 CONFIRMED in this tranche.
F594 / provisional KI#917 is now UNUSED.
No repository/gameplay source files modified.

OPENING LIFECYCLE CONTROLS
--------------------------
The first hard-lifecycle pass paired unit initialization, finalization, movement/area
membership, delayed death and final destruction before looking for replacement-state
loss.

The ordinary structural counters are balanced:
* `CvUnitAI::finalizeInit` adds player/area UnitAI counts; `AI_setUnitAIType`, `setXY`
  and `kill(false)` maintain the corresponding role/area transitions.
* team/player unit-class counts, extra unit cost, nuke/military/assets/power and UWAI
  creation/destruction notifications pair between final initialization and final kill.
* `kill(true)` only marks delayed death after finishing moves; later `doDelayedDeath`
  reaches `kill(false)` when the unit is no longer fighting.
* the final `kill(false)` path snapshots the owner/type/capture information needed after
  removal and does not repeat CvCity's already-owned post-delete `this` hazards.
* transport/cargo replacement keeps carrier cargo counts and links balanced through
  `setTransportUnit`; cargo that no longer fits after conversion is detached and moved to
  a valid plot by the inherited Unofficial Patch handling.

The familiar create-new-before-kill-old window during upgrade/gift is likewise the
already-controlled delayed-death staging discussed earlier in the album; it does not by
itself consume a new finding.

TRAIT/PLAYER FREE PROMOTIONS DURING CONVERT — CONTROL / DESIGN-SEMANTICS BOUNDARY
---------------------------------------------------------------------------------
`CvUnit::convert` overwrites the freshly initialized target's promotions with:

    old unit has promotion || target UnitInfo XML-free promotion

and therefore does not preserve a promotion granted to the NEW unit by its owner's
trait/player free-promotion tables during the replacement initialization. Current SAS has
upgrade edges where this can matter by unit-combat class (for example, an upgrade can
enter one of Aggressive/Protective's qualifying combat classes).

This shape is inherited through BtS, K-Mod and Base AdvCiv, and gift semantics make the
intended contract ambiguous: owner/trait free promotions may deliberately be acquisition
or training-time benefits rather than a conversion entitlement. No explicit current
contract establishing that a replacement upgrade/gift must newly acquire those promotions
was found. Keep this as a design/semantics control rather than spending F593 on it.


F593 / KI#916 — GIFTING AN EVENT-IMMOBILIZED UNIT CLEARS ITS IMMOBILITY TIMER
-----------------------------------------------------------------------------

ROOT
----
`CvUnit::convert()` reconstructs the logical unit during gift/upgrade and transfers a
large amount of persistent identity/state, but it does NOT transfer the authoritative,
serialized `m_iImmobileTimer`.

Fresh unit initialization resets that member to zero. Therefore a replacement created by
`gift()` starts with no immobility unless `convert()` explicitly restores the old timer;
it currently does not.

CURRENT AUTHORITATIVE PRODUCER
------------------------------
Current shipped random events create this exact state through `CvUnit::applyEvent`:

    if (kEvent.getUnitImmobileTurns() > 0)
        changeImmobileTimer(kEvent.getUnitImmobileTurns());

Current EventInfos contain two live positive values:
* `EVENT_FARM_BANDITS_2`: `iUnitImmobileTurns = 2`;
* `EVENT_TOXCATL_2`: `iUnitImmobileTurns = 3`.

The player-facing generic event text is explicit: the chosen unit "cannot move" for the
specified number of turns. `m_iImmobileTimer` is not a derived presentation value; it is
serialized by `CvUnit::read/write`, decremented on unit turns, and `CvUnit::canMove()`
returns false whenever it is positive.

WHY ORDINARY UPGRADE DOES NOT REPRODUCE IT
------------------------------------------
The initially tempting upgrade path closes correctly at its legality gate:

    CvUnit::canUpgrade -> isReadyForUpgrade -> canMove

and `canMove()` rejects `getImmobileTimer() > 0`. An event-immobilized unit therefore
cannot normally upgrade until the timer has expired. Do not describe F593 as an upgrade
exploit.

LIVE GIFT PATH DOES REPRODUCE IT
--------------------------------
Gift uses a different legality contract.

`CvUnit::canDoCommand(COMMAND_GIFT)` calls `canGift()`. `canGift()` validates foreign plot
ownership, peaceful entry, recipient capacity/acceptance, missionary/corporation and
Rise/Fall restrictions, but it does NOT call `canMove()` and does not inspect the
immobility timer.

When legal, `CvUnit::gift()` performs:

    CvUnit* pGiftUnit = recipient.initUnit(...);
    pGiftUnit->convert(this);

The new unit was initialized with `m_iImmobileTimer = 0`. `convert()` transfers:
* promotions;
* game turn created;
* damage and moves;
* level and experience;
* custom name and attached leader type;
* transport/cargo relationships;

but not `m_iImmobileTimer`, then schedules the old unit for delayed death. There is no
later step that copies the timer. The recipient therefore owns the same logical gifted
unit with timer zero.

CURRENT-DATA REACHABILITY
-------------------------
This does not require malformed XML or Python.

The cleanest shipped fixture is `EVENT_TOXCATL_2`:
* `EVENTTRIGGER_TOXCATL` is active in 90% of games, has weight 100 and selects one unit;
* its Python trigger restricts it to the Aztecs, but otherwise the unit picker has no
  UnitClass requirement;
* `bUnitsOnPlot = 0` and the trigger's unit-distance weight is 0, so the picked eligible
  attacking unit need not stand in the triggering Courthouse city;
* `CvUnit::canApplyEvent` only requires an immobilization target to be able to attack and
  not be a mostly-defensive unit;
* the second outcome charges 65 gold and immobilizes the selected unit for three turns.

A comparable non-civ-specific fixture exists through Farm Bandits, whose second choice
immobilizes the chosen attacking unit for two turns.

DETERMINISTIC REPRODUCTION
--------------------------
A compact controlled reproduction can use two human players so recipient AI acceptance
cannot introduce a confounder:

1. Use an Aztec player A with a Courthouse and exactly one event-eligible attacking unit U.
2. Give A and human player B peaceful access/open borders.
3. Place U on a B-owned tile. (`canGift` is now structurally eligible.)
4. Force/trigger `EVENTTRIGGER_TOXCATL` for A and choose `EVENT_TOXCATL_2`.
5. Confirm U receives `m_iImmobileTimer = 3` and cannot move.
6. Without waiting for the timer to expire, issue COMMAND_GIFT for U to B.
7. `canGift()` accepts the command despite the positive immobility timer.
8. `gift()` initializes B's replacement unit and calls `convert(U)`.
9. The replacement's timer remains its initialization default 0 because `convert` does
   not copy U's timer.
10. The old U is removed through delayed death. B's replacement is no longer immobilized;
    on B's actionable turn it can move normally instead of serving the remaining event
    duration (and under simultaneous turns can become actionable immediately).

Positive control: do not gift U; `CvUnit::doTurn()` decrements the timer normally and
`canMove()` continues to reject movement while the remaining value is positive.

This establishes a normal command path from a shipped positive producer to a state-losing
replacement. The bug does not depend on raw `CyUnit::convert` or an external mod caller.

ANCESTRY
--------
The defect is inherited from Firaxis/BtS rather than introduced by SAS:
* Civ4CE/BtS `CvUnit::convert` omits the immobility timer;
* K-Mod 1.46 retains the omission;
* Base AdvCiv 1.14 retains the omission;
* current SAS retains it.

All supplied lineage controls also ship the same positive random-event producers
(`iUnitImmobileTurns = 2` and `3`). Their `canGift` implementations likewise do not use
`canMove()` / the immobility timer. AdvC/SAS refactors the ownership/peace checks but does
not close this state-transfer hole.

DUPLICATE / ROOT BOUNDARY
-------------------------
Whole-album searches for `m_iImmobileTimer`, `iUnitImmobileTurns`, `CvUnit::convert`,
immobile+gift and convert+timer found no existing root owning this lifetime mismatch.

Related controls/findings are distinct:
* prior unit-event roots concern event help, effect arithmetic or stale event-target
  validation, not ownership replacement after a valid unit effect has already applied;
* F575 concerns CvCity acquisition losing city event-derived persistent state, not a unit
  replacement helper omitting a serialized temporary status;
* the earlier create-new-before-kill-old conversion count control concerns a transient
  double count, not dropped state;
* the trait/player free-promotion question above has an ambiguous acquisition-policy
  contract, whereas the immobility timer has an explicit runtime/serialization contract
  and a direct player-facing "cannot move for N turns" promise.

CONSERVATIVE REPAIR
-------------------
Preserve the timer as part of `CvUnit::convert`'s transferred unit state, e.g.:

    setImmobileTimer(pUnit->getImmobileTimer());

This is preferable to globally forbidding gifts from immobile units unless such a new game
rule is independently desired. Gift already represents an ownership transfer of the same
logical unit and preserves damage, moves, XP, promotions, name and created-turn identity;
preserving the active movement prohibition is the narrow state-consistency repair.

Normal upgrade behavior is unaffected in practice because `canUpgrade` already rejects an
immobile source. The repair also makes direct conversion semantics safer should another
supported caller be introduced later.

REGRESSION TEST
---------------
1. Apply a 3-turn immobility event to an eligible combat unit on a peaceful foreign-owned
   plot and assert `getImmobileTimer() == 3` / `canMove() == false`.
2. Gift it successfully to the plot owner.
3. Resolve the replacement unit and assert its immobility timer is still 3 (or the exact
   remaining pre-gift value), not 0.
4. Assert it remains unable to move until ordinary turn decrements consume the timer.
5. Positive control: ordinary non-immobile gifts remain unchanged.
6. Upgrade control: an immobile unit remains unable to upgrade through
   `isReadyForUpgrade -> canMove`.

CURRENT STONE AFTER C031-WIP405
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 CONFIRMED.
F594 / provisional KI#917 UNUSED.
Latest physical checkpoint: C031-WIP405.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Continue the `CvUnit::convert` replacement-state census first so F593's exact boundary is
stable: compare serialized/authoritative fields omitted by convert and classify intentional
reset state (fortification, combat/made-attack state, facing/UI/runtime references) vs any
second current producer that should survive gift/upgrade. Then continue setXY/group/
transport ownership transitions, damage/capture and promotion/experience invariants, and
finally the current-vs-Base changed-body / low-coverage sieves. Whole-album semantic dedup
before F594.

===============================================================================
END C031-WIP405
===============================================================================


===============================================================================
C031-WIP406 — QUEUE 007 CvUnit.cpp — GIFT / SIMULTANEOUS-TURN MOVE TIMESTAMP
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 and F594 / KI#917 are CONFIRMED.
F595 / provisional KI#918 is UNUSED.
No repository/gameplay source files modified.

F594 / KI#917 — GIFT CONVERSION CLEARS THE UNIT'S ANTI-DOUBLE-MOVE TIMESTAMP
---------------------------------------------------------------------------

ROOT
----
`CvUnit::convert()` also fails to transfer `m_iLastMoveTurn` from the source unit to the
replacement unit created by `gift()`.

This member is persistent/serialized authoritative timing state, not merely an animation
stamp. Every ordinary physical `setXY` departure records:

    setLastMoveTurn(GC.getGame().getTurnSlice());

Fresh unit initialization starts `m_iLastMoveTurn` at 0. `gift()` creates a fresh unit and
then calls `convert(this)`, whose manual transfer list does not include the timestamp.
Therefore ownership replacement turns a recently moved unit into one whose recorded last
movement is effectively "long ago".

CURRENT CONSUMER / PURPOSE
--------------------------
In simultaneous-turn multiplayer, `CvSelectionGroup::doTurn()` uses every human unit's
last-move turn slice to enforce the anti-double-move delay:

    iWaitTurns = MIN_TIMER_UNIT_DOUBLE_MOVES -
        (GC.getGame().getTurnSlice() - pUnit->getLastMoveTurn());

and raises the group's mission timer when that result is positive.

Current SAS does not override the standard BtS `MIN_TIMER_UNIT_DOUBLE_MOVES` value; the
supplied BtS XML defines it as 32. Current AdvC/SAS code still actively executes this
SelectionGroup throttle for human groups in simultaneous turns once the team has met a
human opponent.

LIVE GIFT PATH
--------------
As established for F593, `CvUnit::canGift()` does not require `canMove()` or otherwise
preserve movement-timing state. A human unit can therefore:

1. move normally onto a peaceful foreign-human-owned plot, which records the current
   turn slice in `m_iLastMoveTurn`;
2. be gifted immediately to that plot owner;
3. be reconstructed through `recipient.initUnit(...)` + `pGiftUnit->convert(this)`;
4. emerge with `m_iLastMoveTurn == 0` instead of the source's recent turn slice.

`m_iMoves` is copied, so this is NOT a claim that gift immediately grants fresh movement
points in the same player turn. The defect is specifically the separate simultaneous-turn
real-time anti-double-move throttle. On the recipient's next human-group turn, the fresh
zero timestamp makes the elapsed-slice calculation appear far older than the actual move
(and, once the game turn-slice counter is beyond the normal 32-slice window, yields no
waiting time at all). With the real timestamp preserved, the group would be delayed until
the configured anti-double-move interval had elapsed.

DETERMINISTIC MULTIPLAYER REPRODUCTION
--------------------------------------
Use a simultaneous-turn game with two human players A and B that have met and can
peacefully enter each other's territory.

1. Wait until the global turn-slice counter is comfortably above 32.
2. On A's turn/activity window, move unit U onto a B-owned tile.
3. Record/assert that U's `getLastMoveTurn()` equals the recent/current turn slice.
4. Gift U to B before the anti-double-move interval has elapsed.
5. Resolve B's replacement unit U2.
6. Current code gives U2 `getLastMoveTurn() == 0` because initialization reset the member
   and `convert()` omitted it.
7. At B's relevant simultaneous-turn group activation / next turn boundary, compare the
   anti-double-move wait:
   * current U2: elapsed = currentSlice - 0, normally >= 32 -> no timer delay;
   * preserved source timestamp: elapsed < 32 -> positive mission-timer delay.
8. The recipient can therefore act without the timing protection that the same physical
   unit's immediately preceding move should have imposed.

Positive control: perform the same movement without gifting; the original unit retains its
last-move slice and the SelectionGroup throttle applies normally.

ANCESTRY
--------
This is inherited Firaxis behavior rather than a SAS-local regression.

Civ4CE/BtS already has all pieces:
* `m_iLastMoveTurn` initialized to 0 and serialized;
* ordinary movement updates it;
* simultaneous-turn logic consumes it together with `MIN_TIMER_UNIT_DOUBLE_MOVES`;
* `CvUnit::convert` omits it;
* gift reconstructs the unit through init + convert.

K-Mod keeps the SelectionGroup timing throttle while disabling some older duplicate
checks in CvGame/CvGameInterface, and retains the same convert omission. Base AdvCiv 1.14
and current SAS inherit the same behavior.

DUPLICATE / ROOT BOUNDARY
-------------------------
Whole-album search finds prior discussion of `MIN_TIMER_UNIT_DOUBLE_MOVES` only for a safe
static-local define cache in the SelectionGroup audit; no existing root owns loss of the
per-unit timestamp across gift/conversion.

F594 is distinct from F593 despite sharing the same manual `convert()` transfer helper:
* F593 producer: random-event `iUnitImmobileTurns`; consumer: `canMove()`; field:
  `m_iImmobileTimer`; ordinary single-player gameplay is sufficient.
* F594 producer: physical `setXY` movement; consumer: simultaneous-turn SelectionGroup
  timing throttle; field: `m_iLastMoveTurn`; multiplayer timing is required.
* repairing either omitted member leaves the other defect unchanged.

This is also distinct from generic ownership-turn semantics. `m_iMoves` remains copied and
ordinary per-turn move refresh is not being redefined; F594 concerns the explicit real-time
anti-double-move timestamp whose purpose is to survive exactly the boundary between one
move opportunity and the next.

CONSERVATIVE REPAIR
-------------------
Preserve the movement timestamp in `CvUnit::convert`, e.g.:

    setLastMoveTurn(pUnit->getLastMoveTurn());

alongside the other transferred identity/state. This retains normal gifting and does not
alter movement-point accounting. Combined with F593's independent timer transfer, the two
repairs make the replacement unit retain both its game-turn movement prohibition and its
simultaneous-turn real-time movement history.

REGRESSION TEST
---------------
1. In simultaneous multiplayer, move U at turn slice S onto a peaceful human recipient's
   tile and assert U.lastMoveTurn == S.
2. Gift U immediately and resolve replacement U2.
3. Assert U2.lastMoveTurn == S, not 0.
4. Activate B's group before S+MIN_TIMER_UNIT_DOUBLE_MOVES and assert the expected positive
   wait is applied.
5. Repeat after enough slices have elapsed and assert no excess wait is imposed.
6. Single-player/sequential-turn control: preserving the field does not change ordinary
   movement-point legality.

CONVERSION CENSUS CONTINUATION
------------------------------
The two confirmed omissions do not imply that every serialized field should be copied.
Several neighboring omissions have ownership/replacement semantics that plausibly or
explicitly reset:
* fortify turns are positional/control-state and are normally lost on replacement;
* combat/attack/interception flags describe the old action instance and are constrained by
  copied movement state / ordinary turn reset;
* blockade/recon state carries team-specific visibility/economic effects and should not be
  blindly transferred across ownership without explicit remove/re-add semantics;
* hotkeys and runtime combat pointers are UI/transient identity, not transfer state;
* `m_szScriptData` is persistent API/scenario state, but current SAS has no ordinary
  gameplay producer/caller establishing a shipped conversion requirement.

These remain controls unless a current producer + replacement consumer establishes a
stronger contract. Continue the census rather than blanket-copying the serialized object.

CURRENT STONE AFTER C031-WIP406
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F594 / KI#917 CONFIRMED.
F595 / provisional KI#918 UNUSED.
Latest physical checkpoint: C031-WIP406.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Finish the convert-omission census with special attention to team-scoped recon/blockade
state and current Python/script-data producers, but preserve intentional replacement resets.
Then resume hard `setXY`/group/transport membership, capture/ownership, damage/death,
promotion/experience and mission-action invariants. Semantic whole-album dedup before F595.

===============================================================================
END C031-WIP406
===============================================================================


===============================================================================
C031-WIP407 — QUEUE 007 CvUnit.cpp — DIRECT CARGO GIFT / FOREIGN TRANSPORT LINK
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F595 / KI#918 are CONFIRMED.
F596 / provisional KI#919 is UNUSED.
No repository/gameplay source files modified.

F595 / KI#918 — DIRECTLY GIFTING LOADED CARGO CAN LEAVE IT ON A FOREIGN-TEAM TRANSPORT
-------------------------------------------------------------------------------------

ROOT
----
Current AdvC/SAS `CvUnit::canGift(..., bTestTransport=true)` no longer checks that an
already-loaded unit's transport belongs to the recipient's team. `gift()` then reconstructs
the selected cargo unit under the plot owner, and `CvUnit::convert()` deliberately preserves
its old transport relation:

    CvUnit* pTransportUnit = pUnit->getTransportUnit();
    if (pTransportUnit != NULL)
        pUnit->setTransportUnit(NULL);
    setTransportUnit(pTransportUnit);

`setTransportUnit()` itself has no team/owner legality test. The result is therefore a
persistent unit state that normal loading explicitly forbids:

    cargo.getTeam() != cargo.getTransportUnit()->getTeam()

Normal `canLoadOnto()` begins with exactly the missing invariant:

    if (kUnit.getTeam() != getTeam())
        return false;

LIVE CURRENT SAS REPRODUCTION
-----------------------------
Current XML provides ordinary no-special-cargo fixtures:
* `UNIT_GALLEY`: DOMAIN_SEA, `iCargo=3`, `DomainCargo=DOMAIN_LAND`;
* `UNIT_GALLEON`: 4 land cargo;
* `UNIT_TRANSPORT`: 5 land cargo;
* ordinary land units such as Warrior/Axeman/Maceman have no cargo role of their own.

Use two peaceful players A and B on different teams with Open Borders:

1. A loads Warrior U onto A's Galley T.
2. Move T into a B-owned coastal-water tile (or another B-owned plot where the transport
   is valid under the current OB/domain rules).
3. Select U itself, not T, and issue `COMMAND_GIFT`.
4. The command path calls `canGift(bTestVisible, true)` / `gift(true)` by default.
5. U has a transport, so current `canGift` enters its `bTestTransport` branch. It checks
   only `pTransport->isRevealedValidDomain(kPlot)`; it no longer checks that T's team equals
   B's team. The gift therefore remains legal.
6. `gift()` creates B's replacement U2 on the same plot and calls `U2.convert(U)`.
7. `convert()` detaches old U from T and immediately attaches U2 to the same A-owned T.
8. Old U is killed. No parent-transport gift follows in this direct-cargo command, so
   nothing repairs U2's cross-team transport relation.

The stable resulting state is therefore:

    U2.owner/team = B / Team B
    T.owner/team  = A / Team A
    U2.getTransportUnit() == T

This is not reachable through normal loading because `canLoadOnto()` rejects the team
mismatch before changing the transport pointer.

WHY WHOLE-TRANSPORT GIFTING DOES NOT CLOSE THE BUG
--------------------------------------------------
`CvUnit::gift()` recursively gifts a transport's cargo with `gift(false)` before converting
the transport itself. During that transaction the newly recipient-owned cargo is indeed
briefly attached to the donor transport, but the later transport `convert()` enumerates the
old transport's cargo and relinks each compatible cargo unit to the recipient's replacement
transport. That staging therefore converges to a legal final state.

Directly gifting one loaded cargo unit has no enclosing transport conversion. The same
cross-owner staging becomes persistent instead of temporary. This distinction is why a
blanket ban inside `setTransportUnit()` would be unsafe: the recursive whole-transport gift
currently relies on the temporary relation while the transaction is in progress.

AUTHORITATIVE GAMEPLAY CONSEQUENCES
-----------------------------------
The foreign transport link is not cosmetic.

Transport movement (`CvUnit::setXY`) moves every unit on the old plot whose
`getTransportUnit() == this`, without an owner/team filter. Once T has updated its own
coordinates, the linked B-owned U2 is recursively moved to the new plot and retains T as
its transport. Player A therefore controls the physical location of Player B's unit simply
by moving A's transport.

Transport territory checks likewise inspect the linked cargo unit's own entry legality,
so the foreign unit can constrain where A's transport may go even though B does not own
the transport.

Transport death is stronger still: `CvUnit::kill()` enumerates all units on its plot whose
`getTransportUnit() == this` and kills those cargo units regardless of owner. A's transport
can therefore carry—and, if destroyed, destroy—B's gifted unit.

The B-owned cargo can detach later if it reaches a location where its own `canUnload()` is
legal, but until then the invalid foreign-transport relation is persistent and gameplay
relevant.

ANCESTRY / INTRODUCING REGRESSION
---------------------------------
BtS/Civ4CE and K-Mod already protected this exact direct-gift boundary. Their
`canGift(..., bTestTransport)` contains:

    if (bTestTransport)
    {
        if (pTransport && pTransport->getTeam() != pPlot->getTeam())
            return false;
    }

Recursive cargo gift during whole-transport gifting passes `false`, deliberately bypassing
the check until the parent transport is converted; ordinary direct gifting uses the default
`true` and is therefore rejected across teams.

Base AdvC practical commit 2612 / `fc7640933b544f0f943f527e2edc08999da1dc00`, titled:

    Let sea and air units enter rival forts through OB

changed `canGift` while refactoring domain-validity handling. The relevant diff removed the
old team guard and replaced it with:

    else if (bTestTransport)
    {
        //if (pTransport->getTeam() != kRecipient.getTeam())
        if (!pTransport->isRevealedValidDomain(kPlot))
            return false;
    }

The commented-out line is precisely the lost safety condition. Base AdvC 1.14 and current
SAS retain that state. This makes F595 an AdvC-introduced regression inherited by SAS,
not a Firaxis/K-Mod defect.

WHOLE-ALBUM DEDUP / ROOT BOUNDARY
---------------------------------
Whole-album searches for cargo+gift, foreign/cross-team transport, `bTestTransport`,
`canGift`, `setTransportUnit` and `canLoadOnto` found no existing root owning this transfer.

Earlier transport findings are distinct:
* F145 concerns group `COMMAND_LOAD` reusing one free cargo slot during precheck;
* the load-popup ordinal race can retarget onto another SAME-TEAM player's transport, a
  state that normal `canLoadOnto` explicitly permits;
* prior `setTransportUnit` controls concern grouping/capacity/non-colocation behavior, not
  ownership conversion creating a cross-team transport relation;
* F593/F594 are scalar state lost by `convert()`; F595 is the opposite class: `convert()`
  preserves a pointer relation whose legality changed because ownership/team changed.

CONSERVATIVE REPAIR
-------------------
Restore the lost team invariant in the direct-gift precheck while preserving AdvC's newer
fort/domain-validity rule and the existing recursive whole-transport transaction:

    else if (bTestTransport)
    {
        if (pTransport->getTeam() != kRecipient.getTeam() ||
            !pTransport->isRevealedValidDomain(kPlot))
        {
            return false;
        }
    }

Equivalent split checks are fine. The important contract is:
* direct loaded-cargo gift (`bTestTransport=true`) may preserve its current transport only
  when that transport is already on the recipient's team;
* recursive cargo transfer during gifting of the parent transport (`gift(false)`) remains
  allowed to use the temporary staging relation and is repaired by the subsequent parent
  `convert()`.

Do NOT simply add a universal team assertion/rejection to `setTransportUnit()` without
redesigning the whole-transport gift transaction; that helper is intentionally used during
the temporary cross-owner staging described above.

REGRESSION TEST
---------------
1. Two different teams A/B have Open Borders. Load A Warrior U onto A Galley T.
2. Move T onto a B-owned water plot.
3. Assert `U.canGift(false, true)` is false after the repair; the direct Gift command must
   not create B-owned cargo on A's transport.
4. Positive control: if the transport already belongs to B's team (e.g. cooperative
   same-team different-player transport), direct cargo gift remains allowed when all other
   conditions pass because the post-gift relation remains team-legal.
5. Whole-transport control: gift T itself. Recursive `gift(false)` still transfers its
   eligible cargo, and the replacement B transport ends with the transferred cargo attached
   to it rather than the donor transport.
6. Domain-validity control: retain AdvC's `isRevealedValidDomain` check so the practical-2612
   rival-fort/OB behavior is not regressed.
7. Defensive postcondition test: after every completed user-visible gift transaction,
   assert each cargo unit with a transport has `cargo.team == transport.team` unless a
   deliberately documented internal transaction is still in progress.

CURRENT STONE AFTER C031-WIP407
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F595 / KI#918 CONFIRMED.
F596 / provisional KI#919 UNUSED.
Latest physical checkpoint: C031-WIP407.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Continue cargo/ownership lifecycle immediately after F595, excluding the now-owned direct
foreign-transport gift root. Audit unload/jump/death/capture/transport-upgrade transitions,
then damage/death/capture and promotion/experience state. Preserve the distinction between
transient transaction states and persistent post-command states. Whole-album semantic dedup
before F596.

===============================================================================
END C031-WIP407
===============================================================================


===============================================================================
C031-WIP408 — QUEUE 007 CvUnit.cpp — GIFT / PER-TURN ATTACK COUNT
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F596 / KI#919 are CONFIRMED.
F597 / provisional KI#920 is UNUSED.
No repository/gameplay source files modified.

F596 / KI#919 — GIFT CONVERSION CLEARS THE UNIT'S PER-TURN ATTACK COUNT
----------------------------------------------------------------------

ROOT
----
`CvUnit::convert()` does not preserve current AdvC/SAS `m_iMadeAttacks` from the source
unit. A fresh replacement unit starts with:

    m_iMadeAttacks = 0;

and `gift()` reconstructs the logical unit through `recipient.initUnit(...) + convert(this)`
without finishing its remaining movement afterward. `convert()` copies `m_iMoves` but not
the already-spent attack count.

Therefore a unit that has already attacked/bombarded this turn can retain its unused
movement while ownership transfer silently restores its attack allowance.

CURRENT RUNTIME CONTRACT
------------------------
AdvC practical advc.164 generalized BtS's old boolean `m_bMadeAttack` into an integer count:

    void CvUnit::setMadeAttack(bool bNewValue)
    {
        if (bNewValue)
            m_iMadeAttacks++;
        else m_iMadeAttacks = 0;
    }

    bool CvUnit::isMadeAllAttacks() const
    {
        return (getBlitzCount() >= 0 && m_iMadeAttacks > getBlitzCount());
    }

The count is authoritative per-turn state: it is serialized, reset only in normal unit
`doTurn`, and gates attacks/range strikes/bombardment through `isMadeAllAttacks()`.

`convert()` preserving only movement points is insufficient. Movement expenditure and attack
allowance are separate state machines: a multi-move non-Blitz unit can have movement left
while `m_iMadeAttacks` correctly prevents another attack.

CLEAN CURRENT SAS REPRODUCTION — FRIGATE BOMBARD/GIFT
-----------------------------------------------------
Current SAS `UNIT_FRIGATE` is an especially simple fixture:

    Domain = DOMAIN_SEA
    iMoves = 5
    iBombardRate = 12
    no Blitz

BtS/K-Mod already ship the same qualitative fixture with 4 moves and Bombard 8.

Use a simultaneous-turn multiplayer game with human A and B, both at war with C and able to
peacefully share B's territory:

1. Put A's Frigate F on a B-owned water plot adjacent to a bombardable C city.
2. F bombards the C city.
3. `CvUnit::bombard()` executes:

       setMadeAttack(true);
       changeMoves(GC.getMOVE_DENOMINATOR());

   so F has one attack recorded but still has 4 of its 5 movement points available.
4. Because the Frigate is still on a B-owned valid sea plot, `canGift()` accepts gifting F
   to B; it does not reject an already-attacked unit.
5. `gift()` creates B's replacement F2 and calls `F2.convert(F)`.
6. `convert()` copies F's used-move state, so F2 still has only 4/5 movement remaining, but
   initialization left `m_iMadeAttacks=0` and conversion never restores the source count.
7. B is also at war with C. `canBombard()` now sees `isMadeAllAttacks()==false` and F2 still
   has movement, so B can bombard the same (or another eligible) city again in the same
   simultaneous turn.

Without the gift, the same non-Blitz Frigate's `m_iMadeAttacks=1` makes
`isMadeAllAttacks()` true and blocks that second bombard.

The same root can affect ordinary attacks for multi-move units whenever a legal same-turn
gift position is reached, but bombard is the clean fixture because it spends one move and
leaves the unit on the recipient-owned plot.

TURN-BOUNDARY SCOPE
-------------------
This is specifically a same-active-turn ownership-transfer defect. Normal `CvUnit::doTurn`
resets both attack count and movement for a new owner turn:

    setMadeAttack(false);
    ...
    setMoves(0);

so a sequential game where the recipient only acts after its normal next-turn refresh does
not demonstrate the stale-transfer difference. Simultaneous multiplayer provides the direct
supported path: both players' unit-turn state has already been initialized, then gift occurs
inside that shared action window and the recipient can immediately command the replacement.

ANCESTRY
--------
The root is inherited from Firaxis/BtS rather than introduced by SAS.

Civ4CE/BtS:
* initializes `m_bMadeAttack=false`;
* `bombard()` sets it true and spends one move;
* Frigate has 4 moves and Bombard 8;
* `gift()` reconstructs the unit through `initUnit + convert`;
* `convert()` omits `m_bMadeAttack` while copying movement.

K-Mod retains the same behavior. AdvC practical advc.164 replaces the boolean with
`m_iMadeAttacks` to support multiple Blitz attacks, but `convert()` still omits the state.
Base AdvC 1.14 and current SAS therefore preserve the inherited transfer hole, with SAS's
5-move Frigate making the remaining-movement fixture even wider.

WHOLE-ALBUM DEDUP / ROOT BOUNDARY
---------------------------------
Whole-album searches for `m_iMadeAttacks`, `isMadeAllAttacks`, gift+bombard and
made-attack conversion find only unrelated SelectionGroup Blitz-policy controls. No existing
root owns loss of per-unit attack exhaustion across ownership replacement.

F596 is distinct from the preceding conversion findings:
* F593 preserves a random-event movement prohibition (`m_iImmobileTimer`);
* F594 preserves the simultaneous-turn real-time movement timestamp (`m_iLastMoveTurn`);
* F596 preserves the exact per-turn count of already-consumed attacks (`m_iMadeAttacks`).

Fixing any one of those fields leaves the other two defects unchanged. F596 also cannot be
reduced to F594: a unit can have a perfectly recent last-move timestamp yet still regain an
extra bombard solely because its attack count reset.

CONSERVATIVE REPAIR
-------------------
Preserve the exact attack count during `CvUnit::convert`. In current AdvC/SAS a direct
same-class assignment is semantically appropriate, e.g.:

    m_iMadeAttacks = pUnit->m_iMadeAttacks;

or introduce an explicit exact-count setter and use that.

Do NOT restore only `isMadeAttack()` through `setMadeAttack(true)`: current advc.164 allows
more than one attack through Blitz-count effects, so a source unit may legitimately have
`m_iMadeAttacks > 1`. The transfer must preserve the integer count, not collapse it to a
boolean.

Upgrade remains safe with this repair: `upgrade()` calls `convert()` and then
`pUpgradeUnit->finishMoves()`, so preserving the count cannot grant extra same-turn movement
or attacks. Normal next-turn `doTurn` continues to reset the count as before.

REGRESSION TEST
---------------
1. Simultaneous MP: human A/B both at war with C; A Frigate on B-owned water adjacent to C
   city.
2. Bombard once. Assert source `m_iMadeAttacks == 1` / `isMadeAllAttacks()==true` and that
   movement remains.
3. Gift to B in the same active turn.
4. Assert replacement has exactly the source attack count and used-move count.
5. Assert second bombard/attack remains unavailable solely because the attack allowance was
   already consumed.
6. Positive control: gift a unit that has not attacked; its count remains 0.
7. Blitz control: after N attacks, gift a unit with positive Blitz count and assert the exact
   N is retained rather than being collapsed to 1.
8. Turn-boundary control: normal `doTurn` still resets the transferred count for a new turn.

CURRENT STONE AFTER C031-WIP408
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F596 / KI#919 CONFIRMED.
F597 / provisional KI#920 UNUSED.
Latest physical checkpoint: C031-WIP408.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Finish the remaining per-turn conversion-state census, especially `m_bMadeInterception`,
recon/blockade state and exact movement/action semantics, then leave `convert()` if no
additional current producer survives. Continue damage/death/capture and promotion/experience
invariants afterward. Whole-album semantic dedup before F597.

===============================================================================
END C031-WIP408
===============================================================================


===============================================================================
C031-WIP409 — QUEUE 007 CvUnit.cpp — GIFT / PER-TURN INTERCEPTION FLAG
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F597 / KI#920 are CONFIRMED.
F598 / provisional KI#921 is UNUSED.
No repository/gameplay source files modified.

F597 / KI#920 — GIFT CONVERSION CLEARS A NON-AIR UNIT'S USED-INTERCEPTION FLAG
----------------------------------------------------------------------------

ROOT
----
`CvUnit::convert()` does not preserve serialized `m_bMadeInterception` from the source unit.
A fresh replacement unit begins with:

    m_bMadeInterception = false;

and ownership transfer through `gift()` therefore makes a non-air interceptor that has
already intercepted this turn appear available to intercept again.

This is a real authoritative per-turn flag, not animation state. `bestInterceptor()` rejects:

    pLoopUnit->isMadeInterception()

for every candidate. After a resolved interception, current `updateAirCombat()` marks the
interceptor used when it is not itself an air-domain unit:

    if (pInterceptor->getDomainType() != DOMAIN_AIR)
        pInterceptor->setMadeInterception(true);

Normal `CvUnit::doTurn()` resets the flag for a new turn. Save/load serializes it so saving
mid-turn does not grant a second interception. Gift/conversion is the unsupported reset.

CURRENT SAS LIVE FIXTURE
------------------------
`UNIT_MOBILE_SAM` is a clean current non-air interceptor:

    Domain = DOMAIN_LAND
    iInterceptionProbability = 60
    iAirRange = 1
    iMoves = 2

`UNIT_SAM_INFANTRY` is another shipped fixture (15%, range 1). The basic
`PROMOTION_AIR_INTERCEPTION1` is available to `UNITCOMBAT_GUN` and adds +5 interception,
so ordinary promotion state can raise these values further. A deterministic code-level test
can call/force a successful interception; an ordinary game repro can simply use repeated air
missions until the first interception occurs.

SUPPORTED SAME-TURN REPRODUCTION
--------------------------------
Use simultaneous multiplayer with human A and B peacefully sharing territory and both at war
with C:

1. Station A's Mobile SAM S on a B-owned land tile. This is a valid non-cargo location and
   gift target under Open Borders.
2. C launches an air mission whose target lies within S's interception range.
3. Let S successfully intercept and survive.
4. `updateAirCombat()` sets `S.m_bMadeInterception=true`; a subsequent C air mission in the
   same state would skip S in `bestInterceptor()`.
5. A gifts S to B before the common simultaneous action window ends.
6. `gift()` reconstructs B's S2 through `initUnit + convert`. `convert()` omits
   `m_bMadeInterception`, so S2 has the default false flag.
7. C launches another eligible air mission in the same turn. B is also C's enemy, S2 can air
   defend from the same plot, and `bestInterceptor()` is now allowed to select S2 again.

Without the ownership conversion, S remains excluded after its first interception until
normal next-turn `doTurn()` clears the flag.

WHY AIR-DOMAIN INTERCEPTORS ARE NOT THE REQUIRED FIXTURE
--------------------------------------------------------
The current combat path sets `m_bMadeInterception` only for non-air interceptors. Air-domain
interceptors are constrained through their separate moved/activity rules in
`bestInterceptor()`. F597 therefore concerns precisely the land/sea interceptor state for
which this boolean is the one-interception-per-turn gate; Mobile SAM provides the strongest
current example.

ANCESTRY
--------
The omission is inherited from Firaxis/BtS:
* BtS initializes and serializes `m_bMadeInterception`;
* its interceptor-selection path consumes the flag and successful non-air interception sets
  it;
* normal unit turn resets it;
* `CvUnit::convert()` omits it;
* `gift()` uses init + convert without an interception-state repair.

K-Mod retains the same state machine and omission. Base AdvCiv 1.14 and current SAS retain it
as well. Unlike F596's advc.164 boolean-to-count refinement, this field remains a boolean in
current code, but the transfer hole is the same inherited lifetime mismatch.

WHOLE-ALBUM DEDUP / ROOT BOUNDARY
---------------------------------
Whole-album search found no existing `m_bMadeInterception` / gift-interception root.

F597 remains independent from the neighboring conversion findings:
* F593: temporary movement prohibition (`m_iImmobileTimer`);
* F594: simultaneous-turn physical-move timestamp (`m_iLastMoveTurn`);
* F596: active attack/bombard count (`m_iMadeAttacks`);
* F597: passive non-air interception availability (`m_bMadeInterception`).

Each has a different producer and runtime consumer. Copying the attack count does not make a
used SAM unavailable to `bestInterceptor`, and copying the interception flag does not restore
attack exhaustion.

CONSERVATIVE REPAIR
-------------------
Preserve the flag in `CvUnit::convert`, e.g.:

    setMadeInterception(pUnit->isMadeInterception());

or direct boolean assignment. Normal turn reset remains unchanged. Upgrade is not harmed by
preserving the current-turn state; if an upgrade replaces a unit mid-turn, silently restoring
a consumed passive interception would be no more justified than doing so on gift.

REGRESSION TEST
---------------
1. Make a non-air interceptor successfully intercept once and survive; assert
   `isMadeInterception()==true`.
2. Confirm the same unit is excluded from a second immediate `bestInterceptor()` selection.
3. Gift it during the same simultaneous turn to a legal recipient on whose owned plot it
   stands.
4. Assert the replacement still has `isMadeInterception()==true`.
5. Confirm it remains excluded from another interception until normal `doTurn()`.
6. Next-turn control: `doTurn()` clears the flag exactly once as before.
7. Fresh-unit control: gifting a unit that has not intercepted transfers false.

CURRENT STONE AFTER C031-WIP409
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F597 / KI#920 CONFIRMED.
F598 / provisional KI#921 UNUSED.
Latest physical checkpoint: C031-WIP409.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Finish convert-state closure now that the four hard current-live omissions are isolated.
Challenge recon/blockade/fortification/script state only with a real producer and ownership
semantics; avoid blanket-copying team-scoped side effects. Then move into damage/death/capture
and promotion/experience invariants. Whole-album semantic dedup before F598.

===============================================================================
END C031-WIP409
===============================================================================


===============================================================================
C031-WIP410 — QUEUE 007 CvUnit.cpp — CONVERT / REPLACEMENT-STATE CLOSURE
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F597 / KI#920 are CONFIRMED.
F598 / provisional KI#921 is UNUSED.
No repository/gameplay source files modified.

CONVERT-STATE CLOSURE AFTER F593-F597
-------------------------------------
A final serialized/authoritative-state pass over `CvUnit::convert()` did not justify a fifth
conversion finding. The four promoted omissions have current producers, clear same-turn
consumers and ownership-replacement semantics that require preservation:

* F593 `m_iImmobileTimer`;
* F594 `m_iLastMoveTurn`;
* F596 `m_iMadeAttacks`;
* F597 `m_bMadeInterception`.

The remaining omitted state falls into different semantic classes and should not be copied
blindly.

RECON STATE — TEAM-SCOPED EFFECT, NOT SIMPLE IDENTITY
-----------------------------------------------------
`setReconPlot()` mutates actual team sight around the recon target:

    oldPlot.changeAdjacentSight(getTeam(), ..., false, this, true);
    ...
    newPlot.changeAdjacentSight(getTeam(), ..., true, this, true);

and tracks `m_iLastReconTurn` so normal turn-end cleanup knows when to clear it.

On gift across teams, simply copying the recon coordinates into the replacement would
transfer the donor team's completed intelligence mission to the recipient. Conversely, the
source unit's normal kill path calls `setReconPlot(NULL)` and removes the donor team's sight.
That is a coherent ownership-transition policy. Preserving recon would require an explicit
game-rule decision and remove/re-add semantics under the new team, not the scalar-copy repair
used for F593-F597. No current contract establishes that such transfer is intended.

BLOCKADE — STRATEGIC TEAM/OWNER ACTION SHOULD RESET
---------------------------------------------------
`m_bBlockading` is not passive identity. `setBlockading()` immediately calls `updatePlunder`
and changes strategic blockade state for affected teams/cities. Source-unit death explicitly
clears it. Auto-copying a donor's blockade into a newly owned unit would silently start the
recipient blockading without that owner issuing the action and could change which teams are
legally affected. Reset-on-replacement is therefore the safe/current semantic control.

FORTIFICATION / COMBAT-ACTION / UI-RUNTIME STATE
------------------------------------------------
The neighboring omissions such as fortify turns, facing/action/combat-focus/runtime combat
pointers, hotkey/selection state and transient attack-plot/combat timers do not have the same
identity-preservation contract as damage, XP, promotions, name or the four promoted
same-turn legality fields. Replacement/upgrade has historically reset positional control
state, and several values are coupled to EXE animation/group lifecycle. No independent
current-live contradiction was established.

UNIT SCRIPTDATA
---------------
Unit ScriptData is serialized and exposed to Python/WorldBuilder, but current SAS source
search finds no ordinary gameplay producer/consumer beyond WorldBuilder scenario import/
export and the CyUnit API itself. Loss across gift/upgrade remains generic scenario/mod-API
robustness debt, not a current shipped F598 under this audit's current-live threshold.

CARGO / TRANSPORT FOLLOW-UP CONTROLS
------------------------------------
After excluding F595's direct cross-team cargo gift:
* normal transport movement recursively moves every linked cargo unit and preserves its
  valid transport pointer;
* `jumpToNearestValidPlot` ultimately uses `move`/`setXY`, so cargo follows transport moves
  through the same path;
* transport upgrade uses exact current cargo counts and reattaches compatible cargo one by
  one; cargo that no longer fits is detached before `jumpToNearestValidPlot`;
* unload decrements transport cargo count through `setTransportUnit(NULL)` and leaves normal
  group/activity cleanup;
* transport death intentionally kills linked cargo regardless of owner (same-team
  multi-player cargo is legal), while F595 is what could create the unsupported foreign-team
  ownership case.

No additional current-live cargo count/link asymmetry survived once F595 is treated as owned.

CURRENT STONE AFTER C031-WIP410
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593 / KI#916 through F597 / KI#920 CONFIRMED.
F598 / provisional KI#921 UNUSED.
Latest physical checkpoint: C031-WIP410.
Actual AdvCiv-SAS gameplay/repository source remains UNTOUCHED.

NEXT
----
Leave `convert()` and cargo replacement behind. Audit hard damage/death/capture state next:
`setDamage`, delayed death, capture ownership/type reconstruction, kill recursion, Great
General/leader attachment, nuke/suicide and combat-result transitions. Then promotion/XP and
remaining changed-body/low-coverage unit surfaces. Semantic whole-album dedup before F598.

===============================================================================
END C031-WIP410
===============================================================================


===============================================================================
C031-WIP411 -- QUEUE 007: CvUnit.cpp DEATH / CAPTURE RECONSTRUCTION CONTROL
Continuation after C031-WIP410
===============================================================================

F598 / provisional KI#921 -- UNUSED
------------------------------------
No independent current-live death/capture root was promoted in this tranche.

CAPTURE-CLASS CIVILIZATION-MAPPING CANDIDATE -- CURRENT-DATA CLOSED
------------------------------------------------------------------
A mechanically sharp-looking producer/consumer mismatch exists in inherited AdvCiv
capture logic:

- CvUnit::getCaptureOdds(kDefender) rejects capture when
  kDefender.getCaptureUnitType(DEFENDER civilization) == NO_UNIT;
- final CvUnit::kill capture reconstruction instead computes
  getCaptureUnitType(CAPTURING PLAYER civilization).

Those are not generically equivalent when a UnitClass can map to NONE for one
civilization and a concrete UnitType for another. If current data activated such a
capture class, the odds precheck and executor could disagree about whether capture is
representable.

Current SAS does NOT activate that mismatch:

- the only shipped UnitInfos with a non-NONE <Capture> class are UNIT_SETTLER,
  UNIT_WORKER, UNIT_INDIAN_FAST_WORKER, UNIT_NETHERLANDS_SWIFT_WORKER and
  UNIT_MAYA_QUICK_WORKER;
- every one maps on capture to UNITCLASS_WORKER;
- CvXMLLoadUtility::InitUnitDefaults initializes every civilization's omitted UnitClass
  mapping to the UnitClass default before civilization-specific XML overrides are read;
- ordinary civilizations therefore resolve UNITCLASS_WORKER to UNIT_WORKER;
- India, Maya and Netherlands resolve it to their concrete worker UUs;
- CIVILIZATION_BARBARIAN explicitly resolves it to UNIT_BARBARIAN_WORKER;
- no current civilization maps UNITCLASS_WORKER to NONE.

Therefore the only predicate getCaptureOdds uses from the defender-side mapping
("is this NO_UNIT?") has the same answer as the captor-side executor for every current
capturable unit/civilization combination. Different concrete worker types are intended:
a captured Worker is reconstructed as the capturing civilization's Worker/UU.

This remains a useful generic robustness contract: should a future capturable UnitClass
be disabled for some civilization, getCaptureOdds should test the prospective capturing
civilization rather than the defender civilization. It does not meet this album's
current-live threshold today.

DEATH / DELAYED-DEATH / CAPTURE LIFECYCLE CONTROL
--------------------------------------------------
- setDamage reaching max HP enters kill(true, ePlayer); delayed death retains the unit
  object until doDelayedDeath can safely complete kill(false), rather than deleting an
  object still involved in combat animation/state.
- final kill snapshots owner, unit type, capturing player and captor-specific capture
  UnitType before setXY(INVALID), unitLost and deleteUnit; reconstruction therefore does
  not dereference the deleted unit for those values.
- the plot is a persistent map object; the CvPlot reference captured at kill entry
  remains valid after the unit is removed and supplies the capture coordinates.
- capture reconstruction guards NO_PLAYER / NO_UNIT and deliberately excludes the
  Barbarian player, matching the BtS/K-Mod lineage policy that Barbarians do not retain
  captured units.
- AI capture refusal is intentionally handled before initUnit; no half-created captured
  unit or leaked unit-class count was established.

BARBARIAN RANDOM-CAPTURE MESSAGE QUIRK -- NOT PROMOTED
------------------------------------------------------
AdvCiv's advc.010 randomized capture layer can set bCapture after a successful capture
roll by a Barbarian attacker, while final kill still follows the inherited BtS/K-Mod
policy of not creating captured units for BARBARIAN_PLAYER. Because bCapture suppresses
the ordinary attack-success "destroyed" message in the advc.010 combat branch, a
successful Barbarian roll can therefore produce a message-level inconsistency even
though the target ultimately disappears. This is not an authoritative capture/accounting
failure and was not assigned F598 in this gameplay-focused pass.

ANCESTRY / DEDUP
----------------
- getCaptureOdds is inherited unchanged from Base AdvCiv in current SAS (advc.010).
- BtS/Civ4CE and K-Mod predate getCaptureOdds and use deterministic capture eligibility,
  while their final kill path already excludes Barbarian captors.
- whole-album semantic search found no prior independent root matching the prospective
  captor-vs-defender civilization mapping seam; the current XML proof above is why it
  remains a control rather than a new number.

DURABLE STATE
-------------
Queue 007 CvGameCoreDLL/CvUnit.cpp remains OPEN.
F593/KI#916 through F597/KI#920 remain confirmed.
F598 / provisional KI#921 remains UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- leave death/capture reconstruction after this current-data closure;
- audit promotion / experience / level state and promotion application/removal symmetry;
- emphasize current SAS promotion XML combinations, free-promotion producers,
  unit-combat/domain applicability, XP caps and replacement/ownership transitions;
- whole-album semantic dedup before numbering;
- promote F598 only for an independent current-live root;
- physically freeze the next WIP regardless.
===============================================================================


===============================================================================
C031-WIP412 -- QUEUE 007: CvUnit.cpp PROMOTION / XP / EVENT-GRANT CONTROLS
Continuation after C031-WIP411
===============================================================================

F598 / provisional KI#921 -- UNUSED
------------------------------------
No independent current-live promotion/XP root was promoted in this tranche.

GREAT-GENERAL XP-ONLY BRANCH -- CURRENT DATA MASKED
----------------------------------------------------
CvUnit::canPromote has a special early-true branch for a Warlord unit with positive
LeaderExperience but NO LeaderPromotion. That branch can therefore admit an
`ePromotion == NO_PROMOTION` command before the normal NO_PROMOTION rejection.
CvUnit::promote would be unsafe/semantically unclear for such a mod-mod unit because
later code expects a concrete promotion.

Current SAS does not ship that configuration. The only leader unit is
UNIT_GREAT_GENERAL, with iLeaderExperience=20 and concrete PROMOTION_LEADER. Normal
CvUnit::lead therefore supplies a real promotion id. Keep the XP-only branch as generic
mod-mod robustness only; it is not F598 in current SAS.

LEVEL / EXPERIENCE OWNER-MODIFIER CONTROL
-----------------------------------------
CvUnit::convert copies the old level and rescales stored XP by the old/new owners'
LevelExperienceModifier. CvUnit::experienceNeeded applies the receiving owner's same
modifier to the level threshold. This is the inherited K-Mod contract rather than an
unpaired unit-state transform.

Current SAS TraitInfos additionally have no nonzero iLevelExperienceModifier values, so
ordinary current gift/upgrade conversion does not even activate cross-owner XP scaling.
No current F598 here.

EVENT XP -> PROMOTION-READY TIMING -- CLOSED
--------------------------------------------
CvUnit::applyEvent can add direct unit XP (current fixtures include At the Sword +3 XP
and Friendly Locals +1 XP) without calling testPromotionReady immediately. In isolation
that looked unlike CvCity::addProductionExperience and Great-General XP distribution,
which explicitly refresh promotion readiness.

The full turn contract closes it:
* ordinary combat XP uses the same changeExperience helper and likewise does not refresh
  m_bPromotionReady at the XP write itself;
* CvUnit::doTurn begins by calling testPromotionReady for every surviving unit;
* random events are resolved at the player turn boundary, so the unit's next ordinary
  turn refresh establishes the correct promotion-ready state before normal action;
* Great-General/city-production callers need the explicit refresh because those grants
  can occur outside that ordinary unit-turn boundary.

This is inherited timing policy, not an event XP omission.

EVENT-WIDE FREE PROMOTIONS / PATH-STATE CONTROL
-----------------------------------------------
CvPlayer::applyEvent grants UnitCombatPromotions and UnitClassPromotions directly with
CvUnit::setHasPromotion, then persists the exact pair through CvPlayer::setFreePromotion
for future units. This bypasses CvUnit::promote's explicit CvSelectionGroup::resetPath,
so the current event promotion set was checked mechanically against path/visibility/range
side effects rather than assuming the bypass safe.

Current live movement-sensitive fixtures:
* Horse Whispering completion grants PROMOTION_SENTRY (+1 visibility) to current and
  future mounted-melee / mounted-ranged units;
* Harbormaster completion grants PROMOTION_NAVIGATOR (+1 move) to naval units;
* Long Range Fighters grants PROMOTION_AIR_RANGE1 (+1 air range) to Fighter-class units.

The concrete state transitions close:
* changeExtraVisibilityRange removes old adjacent sight, changes the range and reapplies
  sight immediately, so Sentry does not leave stale fog/visibility state;
* extra air range is consumed dynamically by air-mission legality/range checks and has no
  reusable GroupPathFinder cost state;
* Navigator changes maxMoves/movesLeft. GroupStepMetric::canReuseInitialPathData compares
  the live group's initial moves against the cached start-node moves and resets nodes when
  they differ, so the +move event cannot silently reuse old movement-point path data;
* current event-wide grants do not include move-discount, terrain/feature double-move,
  EnemyRoute, River or other path-cost-rule promotions that could change path cost while
  leaving initial moves equal.

A machine cross-check of all 37 current UnitCombat/UnitClass event promotion grants also
found no grant that violates the normal numeric withdrawal/interception/evasion maxima or
puts Blitz onto a <=1-move non-paradrop unit. Several events intentionally grant promotions
outside ordinary UnitCombat acquisition masks; that is established event design, not a
numeric-state contradiction.

PROMOTION APPLY / REMOVE SYMMETRY
---------------------------------
setHasPromotion updates the stored promotion bit and every inspected derived counter in
matched +/- form: Blitz, Amphibious/River/EnemyRoute/AlwaysHeal, double-move counts,
visibility, moves, movement discount, air range, interception/evasion, first strikes,
withdrawal, collateral/bombard/heal modifiers, combat/city/hills modifiers, revolt/collateral
protection, pillage/upgrade/XP modifiers, Kamikaze, Cargo, terrain/feature modifiers,
UnitCombat modifiers and Domain modifiers.

For ordinary learned/Great-General promotions, CvUnit::promote performs the additional
reusable group-path reset after setHasPromotion. Trait, UnitInfo, city and persistent-player
free promotions are applied while a new unit is being initialized or through already-audited
current-event paths above. No independent stale derived promotion state survived.

DURABLE STATE
-------------
Queue 007 CvGameCoreDLL/CvUnit.cpp remains OPEN.
F593/KI#916 through F597/KI#920 remain confirmed.
F598 / provisional KI#921 remains UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- leave the promotion / XP / Great-General family after this current-data closure;
- audit authoritative movement/action state outside promotion application: move spending,
  finishMoves, paradrop/rebase/air-range actions, withdrawal/retreat and action-count reset;
- then inspect unit heal/damage terrain/feature transitions and low-coverage changed bodies;
- whole-album semantic dedup before numbering;
- promote F598 only for an independent current-live root;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP412
===============================================================================


===============================================================================
C031-WIP413 -- QUEUE 007: CvUnit.cpp MOVEMENT / ACTION-COUNT AUDIT
Continuation after C031-WIP412
===============================================================================

F598 / KI#921 -- SEA-PILLAGE INTERCEPTION CAN ERASE SPENT BLITZ ATTACKS
-------------------------------------------------------------------------------
CONFIRMED -- inherited Base AdvCiv regression introduced by the advc.164
boolean-to-count Blitz redesign; current SAS remains live.

ROOT
----
CvUnit::pillage() still contains the K-Mod/BtS sea-patrol interception staging:

    if (pInterceptor != NULL)
    {
        setMadeAttack(false);
        int iWithdrawal = withdrawalProbability();
        changeExtraWithdrawal(-iWithdrawal);
        attack(pInterceptor->plot(), false, NULL, true);
        changeExtraWithdrawal(iWithdrawal);
        return false;
    }

That reset was harmless with the old Boolean made-attack state: clearing m_bMadeAttack
before the forced combat and setting it again when combat begins could only leave the
same one-bit state ("has attacked") that already existed.

AdvC practical advc.164 changed the authoritative state to an integer count:

    void CvUnit::setMadeAttack(bool bNewValue)
    {
        if (bNewValue)
            m_iMadeAttacks++;
        else m_iMadeAttacks = 0;
    }

and exhaustion is now:

    return (getBlitzCount() >= 0 && m_iMadeAttacks > getBlitzCount());

PROMOTION_BLITZ / current SAS PROMOTION_BLITZKRIEG uses iBlitz=1, i.e. one extra
attack and therefore two attacks total. The old `setMadeAttack(false)` now destroys
all prior attack-count history rather than temporarily clearing one Boolean.

CURRENT-LIVE SAS FIXTURE
------------------------
Current SAS explicitly keeps Blitzkrieg live for UNITCOMBAT_NAVAL:

- PROMOTION_BLITZKRIEG has <iBlitz>1</iBlitz>;
- its UnitCombats include UNITCOMBAT_NAVAL;
- current Frigate is UNITCOMBAT_NAVAL, has bPillage=1, 5 moves and BombardRate=12;
- Destroyer/Battleship and other current naval combat units likewise have enough
  movement/pillage capability for the same interaction;
- the inherited AdvC hint explicitly tells players that ships can reach Blitz and that
  it gives them one extra attack per turn.

MISSION_PILLAGE does not reject a unit merely because isMadeAllAttacks() is true; the
mission requires canPillage() plus remaining movement. Therefore a Blitz ship can
legally have exhausted both attacks and still attempt a sea pillage when it has moves
left.

CONCRETE STATE TRANSITION / REPRO
---------------------------------
A clean source-level fixture is a Blitz Frigate on a pillageable enemy water tile next
to a bombardable enemy city, with an enemy Sea Patrol unit in interception range:

1. Give/promote the Frigate with current PROMOTION_BLITZKRIEG.
2. Bombard twice while enough city defense remains.
   Each bombard calls setMadeAttack(true), so m_iMadeAttacks == 2.
   With iBlitz=1, isMadeAllAttacks() is true because 2 > 1.
3. The Frigate still has movement (5 base moves; the two bombardments cost one move
   each), and MISSION_PILLAGE remains legal on its current improved water tile.
4. Let bestSeaPillageInterceptor select an enemy Sea Patrol unit.
5. CvUnit::pillage calls setMadeAttack(false): 2 -> 0.
6. The forced `attack(..., bSeaPatrol=true)` begins combat and updateCombat calls
   setMadeAttack(true): 0 -> 1.
7. If the pillager survives, its previous two-spent-attack history has collapsed to one.
   `isMadeAllAttacks()` is now false because 1 > 1 is false, so the same physical ship
   is eligible for another normal attack/bombard despite already having exhausted its
   two-attack Blitz allowance before the interception.

The Sea Patrol combat may legitimately decide whether it should consume an additional
attack allowance; that design question does not affect the bug. Whatever the intended
answer, an interception cannot validly REDUCE the number of attacks already spent.

WHY THIS IS AN advc.164 MIGRATION REGRESSION
--------------------------------------------
BtS/Civ4CE and K-Mod use the original Boolean made-attack contract. K-Mod already has
this same sea-pillage staging and calls setMadeAttack(false) before the forced patrol
combat. In that representation the operation cannot turn "two attacks already spent"
into "one" because no such count exists.

The preserved AdvC practical diff shows advc.164 replacing m_bMadeAttack with
m_iMadeAttacks, making `setMadeAttack(false)` reset the full counter and changing Blitz
from an unlimited Boolean property into an integer number of extra attacks. The
pillage-interceptor block was carried through with its old `setMadeAttack(false)` call
unchanged. Base AdvC 1.14 therefore already contains the mismatch, and current SAS
inherits it.

Base AdvC also explicitly enables PROMOTION_BLITZ for UNITCOMBAT_NAVAL and ships an
advc.164 hint saying ships can reach Blitz, so the regression is live in Base AdvC
itself rather than requiring SAS's renamed/rebalanced promotion.

ROOT BOUNDARY / DEDUP
---------------------
Whole-album semantic search finds no prior sea-pillage-interception / Blitz-counter
root. F596/KI#919 is related only in the data member involved: F596 loses
m_iMadeAttacks when gift/convert reconstructs ownership. F598 instead corrupts the
same authoritative count inside an ordinary same-owner sea-pillage action because an
old Boolean-clearing idiom survived the advc.164 integer migration. Fixing either path
leaves the other defect unchanged.

F143/KI#466 and the neighboring Sea Patrol work concern mixed-group patrol admission /
persistence. They do not address the pillager's attack-count mutation during an actual
interception.

CONSERVATIVE REPAIR BOUNDARY
----------------------------
Preserve the attack count that existed before the temporary sea-patrol staging. A
minimal repair can snapshot m_iMadeAttacks before the legacy clear and, after starting
the patrol combat, ensure the resulting count is at least the snapshot, e.g. conceptually:

    int const iMadeAttacksBefore = m_iMadeAttacks;
    setMadeAttack(false);
    ...
    attack(..., true);
    m_iMadeAttacks = std::max(m_iMadeAttacks, iMadeAttacksBefore);

This preserves the old Boolean contract generalized to a count: a patrol interception
may establish that at least one attack has occurred, but it cannot erase already-spent
attacks. The exact implementation should keep the existing temporary-withdrawal logic
and sea-patrol combat staging intact.

REGRESSION TEST
---------------
1. Create a current naval Blitz unit with enough moves (Frigate is sufficient), on an
   improved enemy water plot next to a city with enough defense for two bombardments.
2. Put an enemy eligible Sea Patrol unit within patrol range.
3. Bombard twice; assert m_iMadeAttacks == 2 and isMadeAllAttacks() == true.
4. Trigger MISSION_PILLAGE and an interception; arrange/seed the combat so the pillager
   survives.
5. After combat, assert m_iMadeAttacks has NOT fallen below 2 and the ship cannot gain
   an extra offensive attack merely because the pillage was intercepted.
6. Also test 0- and 1-spent-attack cases so the repair does not break ordinary patrol
   interception behavior.

MOVEMENT / ACTION CONTROLS CLOSED IN THIS TRANCHE
--------------------------------------------------
- airlift and recon finish movement explicitly;
- air strike, air bomb, bombard and ordinary range strike pair their move expenditure
  with their made-attack state under current configuration;
- paradrop deliberately requires an unmoved unit, spends half a move and consumes an
  attack; the promotion-validity code explicitly contains a paradrop/Blitz exception,
  so this is inherited intended action economy rather than F598;
- current SAS movement/action bodies that differ from Base AdvC in this region are
  overwhelmingly GameRecord instrumentation, constant hoists, signature formatting or
  already-owned KI#466/KI#467 group-state repairs; no separate SAS regression survived.

DURABLE STATE
-------------
Queue 007 CvGameCoreDLL/CvUnit.cpp remains OPEN.
F593/KI#916 through F598/KI#921 are CONFIRMED.
F599 / provisional KI#922 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- continue movement/action state after owning the sea-pillage Blitz reset;
- inspect withdrawal/retreat, air/rebase/nuke/paradrop completion, move carry-over and
  per-turn action reset for independent current-live contradictions;
- then move into heal/damage / feature-turn-damage and unit plot-transition state;
- whole-album semantic dedup before numbering;
- promote F599 only for an independent current-live root;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP413
===============================================================================


===============================================================================
C031-WIP414 -- QUEUE 007: CvUnit.cpp SETXY / FORCED-RELOCATION LIFECYCLE CONTROL
Continuation after C031-WIP413
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F598/KI#921 remain CONFIRMED.
F599 / provisional KI#922 remains UNUSED.
Actual repository/gameplay source remains untouched.

FEATURE TURN-DAMAGE -> DELAYED-DEATH -> HEAL ORDERING
-----------------------------------------------------
`CvUnit::doTurn()` applies feature turn damage before its ordinary healing branch. Since
lethal `changeDamage` schedules `kill(true)` rather than immediately deleting the unit,
that source shape can generically leave a delayed-death unit executing later `doTurn`
logic; transport cargo is especially sensitive because delayed `kill` already performs
some destruction-side consequences before the unit object disappears.

Current SAS does not activate this path: every shipped `CIV4FeatureInfos.xml` entry has
`iTurnDamage = 0`. Therefore the ordering remains a real mod-mod robustness hazard but not
a current-live F599 under this audit's threshold. Preserve as a control rather than
numbering a data-masked defect.

SETXY REMOVE -> MOVE -> ADD PAIRING
-----------------------------------
The authoritative `CvUnit::setXY` transition was paired mechanically across the old and
new plots. After excluding already-owned roots, the remaining core state is balanced:

* old/new plot membership is removed then added;
* adjacent sight is removed under the old coordinates and re-added under the new ones;
* `CvArea` unit count, unit power and UnitAI count are paired -1/+1;
* outside-unit support count is paired against the same owner/team/vassal predicate;
* city military-happiness presence is paired (neighboring stale-city behavior belongs to
  F455 rather than a new movement root);
* working-plot siege dirtiness is refreshed on departure/arrival where the mechanics use it;
* the unit's cached plot/area pointers are rebuilt immediately by `updatePlot` / `updateArea`;
* blockade teardown on departure belongs to the already-audited blockade family
  (F479/F507 and KI#467 controls), not a new setXY imbalance.

Current SAS's own body differences from Base AdvC in this function are almost entirely
constant-hoist/performance changes around war-weariness/capture values; the underlying
movement lifecycle is inherited.

`unitSetXY` DESTRUCTION / NULL-PLOT CALLBACK CONTROL
----------------------------------------------------
`setXY(INVALID_PLOT_COORD, INVALID_PLOT_COORD, ...)` reports `unitSetXY(NULL, this)` near
the end of the transition, which initially looks like a possible Python null-plot hazard.
The shipped callback contract closes it:

* practical advc.003y guards this native event behind `ON_UNIT_SET_XY`;
* current SAS keeps the corresponding USE callback define disabled (`0`);
* the only shipped Python `onUnitSetXY` handler is the stock movement-log stub and has no
  gameplay listener layered on top of it;
* the broader callback-guard family was already audited in the album and remains a clean
  disabled-event control.

No current-live null-plot consumer exists, so no F599.

GOODY-BEFORE-COORDINATE-ASSIGNMENT CONTROL
-------------------------------------------
`setXY` resolves a goody hut on `pNewPlot` before removing the unit from its old plot and
before assigning the new coordinates. The full current `CvPlayer::doGoody/receiveGoody`
path was checked for a destructive/reentrant unit transition. Shipped outcomes can grant
XP/healing, map/tech/gold, spawn units and spawn hostile Barbarians, but the hut-location
operations receive `pPlot` explicitly and do not require the explorer's coordinates to
have already changed. No current outcome deletes/replaces the moving unit in-place before
`setXY` resumes. This inherited ordering therefore stays a control.

FORCED RELOCATION / jumpToNearestValidPlot CONTROL
--------------------------------------------------
`jumpToNearestValidPlot` clears the bumped group's mission/automation state, then either
uses normal `move` or the explicit advc.163 `bFreeMove` path (`setXY`). The current caller
census closes the apparent free-move / anti-double-move timestamp concern:

* the only native caller passing `bFreeMove=true` is `CvPlot::verifyUnitValidPlot`;
* that caller passes it only for `!kUnit.isHuman()` to prevent humans from trolling AI
  stacks by forcing a paid bump;
* `setXY` does record `m_iLastMoveTurn`, but the simultaneous-turn anti-double-move
  consumer is specifically a human-group throttle. Thus the free AI bump does not create
  a human F594-style timestamp exploit;
* ordinary human bumps go through `move` and spend movement as intended;
* `bForceMove` correctly excludes the current plot while retaining the same legality
  checks used by the normal relocation search;
* no-current-destination falls through to `kill(false)` and the inspected callers either
  use the returned Boolean or do not dereference the killed unit afterward.

K-MOD AIR-UPGRADE / FULL-CAPACITY ORACLE CONTROL
------------------------------------------------
a K-Mod changelog fix for air units upgrading in a city at full air capacity was used as
an ancestry oracle. Current AdvC/SAS retains the safety property: a freshly initialized
air upgrade may auto-load during placement, but `convert()` subsequently applies the old
unit's transport state; when the old air unit was not cargo, `setTransportUnit(NULL)`
removes that accidental fresh-unit auto-load. The old K-Mod bug is therefore not live in
current SAS.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F598/KI#921 remain CONFIRMED.
F599 / provisional KI#922 remains UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- leave the saturated `setXY` / forced-relocation lifecycle family;
- use known K-Mod/BtS unit bugfixes plus current SAS/Base-AdvC body diffs as oracles for
  authoritative movement/combat/state regressions that AdvC may have diverged from;
- prioritize hard current-live state contradictions over UI/animation-only differences;
- whole-album semantic dedup before numbering;
- promote F599 only for an independent current-live root;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP414
===============================================================================


===============================================================================
C031-WIP415 -- QUEUE 007: K-MOD UNIT-BUGFIX ORACLE / SEMANTIC FRONTIER RESET
Continuation after C031-WIP414
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F598/KI#921 remain CONFIRMED.
F599 / provisional KI#922 remains UNUSED.
Actual repository/gameplay source remains untouched.

K-MOD BUGFIX ORACLE: CURRENT CONTROLS
-------------------------------------
The K-Mod changelog/source was used only as an ancestry oracle: historical fixes were
re-proved against current AdvC-SAS rather than assumed to remain correct.

* Simultaneous-turn air interception retains K-Mod's availability guard: an interceptor
  that is itself fighting, or whose interception plot is already fighting, is not started
  into a conflicting air combat. This closes the old simultaneous-combat collision bug.
* The simultaneous attacker/defender cleanup safeguards around combat and unit death are
  still present; the previously-audited post-delete/unit-lifecycle roots remain the proper
  owners for the dangerous cases rather than a new F599.
* K-Mod's original BtS air-upgrade/full-city-capacity repair was already closed in WIP414:
  `convert` restores the old transport state after fresh-unit placement, undoing an
  accidental carrier auto-load when the old air unit was not cargo.
* The Mongoose/K-Mod transport/load safety patches remain present in current source:
  self-loading cargo is rejected, all-terrain land units are not auto-loaded merely for
  entering water, cargo cannot pillage, and cargo land interceptors are rejected.
* The K-Mod mission-queue lifecycle change is also retained: deleting the head mission no
  longer implicitly starts the next queued mission inside `deleteMissionQueueNode`; callers
  activate a surviving follow-up explicitly after the prior mission has completed. This
  preserves the safety property behind K-Mod's historical single-nuke/multiple-target queue
  fix instead of blindly starting a queued action during destructive/suicide mission
  teardown.
* `jumpToNearestValidPlot` still clears the bumped group's mission queue (WIP414 control),
  preserving K-Mod's historical worker-bump repair.

These controls make the old changelog increasingly low-yield as a fresh-finding source;
no current-live independent F599 emerged from this ancestry tranche.

LITERAL COVERAGE IS NOT SEMANTIC COVERAGE
-----------------------------------------
A fresh function-name census still reports many zero-literal-mention CvUnit methods, but
this is again a misleading late-audit metric: numerous zero-name helpers belong to deeply
audited families (combat arithmetic, promotion counters, UI/rendering, range-strike code,
transport helpers, or already-owned movement/cache roots). The next pass therefore uses
families/invariants rather than raw name counts.

The best remaining authoritative frontier from the census is territory/domain legality:
`canEnterArea`, `canEnterTerritory`, `canCargoEnterTerritory`, `canAdvance`, `isIntruding`,
and the corresponding move/capture/transport callers. These functions combine owner/team,
Open Borders/vassal/war state, hidden nationality, cargo, domains and observer/revealed
state and are comparatively underrepresented in the album while directly constraining
real movement.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F598/KI#921 remain CONFIRMED.
F599 / provisional KI#922 remains UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- leave the increasingly saturated K-Mod changelog oracle;
- audit territory/domain movement legality as a paired producer/consumer family:
  canEnterArea / canEnterTerritory / canCargoEnterTerritory / canAdvance / isIntruding;
- test current SAS Open Borders, vassal, hidden-nationality, cargo and all-terrain domains;
- compare with K-Mod/Base AdvC only where ancestry helps explain a suspicious branch;
- whole-album semantic dedup before numbering;
- promote F599 only for an independent current-live contradiction;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP415
===============================================================================


===============================================================================
C031-WIP416 -- QUEUE 007: ALL-TERRAIN AI AREA PREFILTER -- F599 CONFIRMED
Continuation after C031-WIP415
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F598/KI#921 remain CONFIRMED.
F599 / provisional KI#922 is CONFIRMED.
F600 / provisional KI#923 is UNUSED.
Actual repository/gameplay source remains untouched.

-------------------------------------------------------------------------------
F599 / provisional KI#922 -- AdvC `AI_canEnterByLand` intentionally omitted
`canMoveAllTerrain`; SAS later activates that ability on real land-domain AI
units, so many AI target/reinforcement prefilters reject legal cross-water
routes before the pathfinder can test them
-------------------------------------------------------------------------------

ROOT
----
Current AdvC/SAS defines the cheap land-area prefilter as:

    // advc.030:
    // Not checked (to save time): unused canMoveAllTerrain
    bool AI_canEnterByLand(CvArea const& kArea) const
    {
        return (isArea(kArea) ||
            (canMoveImpassable() && canEnterArea(kArea)));
    }

`CvUnit::canEnterArea` is only:

    return kArea.canBeEntered(getArea(), this);

and `CvArea::canBeEntered` explicitly documents that it does NOT support
`canMoveAllTerrain`:

    /* If I wanted to support canMoveAllTerrain here, then I couldn't do
       anything more when u==NULL. So that's not supported. */

For two different land areas / representative landmasses it therefore returns
false before the unit's all-terrain ability is considered.

The nearby authoritative `AI_plotValid` comment states the optimization's
original assumption directly:

    For land units not in cargo, a faster inline check can accomplish almost
    the same thing - except for the unused NoRevealMap and MoveAllTerrain tags.
    So I've replaced some AI_plotValid calls with same-area or
    AI_canEnterByLand checks.

And `AI_plotValid` itself preserves the missing ability correctly:

    case DOMAIN_LAND:
        return (pPlot->getArea().canBeEntered(getArea(), this) ||
                canMoveAllTerrain());

So the fast replacement is knowingly narrower than the function it replaced.
That was harmless only while `MoveAllTerrain` remained unused.

CURRENT SAS ACTIVATES THE OMITTED DOMAIN
----------------------------------------
Current `CIV4UnitInfos.xml` contains three real DOMAIN_LAND all-terrain units:

* `UNIT_AIRSHIP`
  - `DefaultUnitAI = UNITAI_EXPLORE`
  - `bCanMoveImpassable = 1`
  - `bCanMoveAllTerrain = 1`
* `UNIT_ROBOTIC_RECON_DRONE`
  - `DefaultUnitAI = UNITAI_EXPLORE`
  - `bCanMoveImpassable = 1`
  - `bCanMoveAllTerrain = 1`
* `UNIT_GUNSHIP`
  - `DefaultUnitAI = UNITAI_ATTACK`
  - `bCanMoveImpassable = 1`
  - `bCanMoveAllTerrain = 1`
  - 4 moves in current SAS

The Gunship is the cleanest combat/reinforcement fixture because its normal AI
role directly calls several helpers containing `AI_canEnterByLand` prefilters.

This ability is intentional current gameplay, not an accidental XML residue.
The Main Changes Guide says Airship and Gunship can move through all terrains,
including water tiles, using `bCanMoveImpassable` + `bCanMoveAllTerrain`.

REAL MOVEMENT / PATHFINDING ACCEPTS THE SAME ROUTE
--------------------------------------------------
The false negative belongs to the coarse AI prefilter, not to movement legality.

`CvUnit::canMoveInto` for `DOMAIN_LAND` uses:

    if (!isValidDomain(kPlot.isWater()))
        ...

and `CvUnit::isValidDomain` says:

    case DOMAIN_LAND:
        return (!bWater || canMoveAllTerrain());

The group pathfinder likewise preserves the ability. Its AI destination-area
fast rejection is guarded by:

    if (!kPlot.isArea(kGroupArea) &&
        !kGroup.canMoveAllTerrain() &&
        !kPlot.isAdjacentToArea(kGroupArea))
    {
        return false;
    }

Thus an all-terrain Gunship group can legally path from landmass A, across water,
to landmass B. The pathfinder is capable of proving the route -- but many AI
callers never invoke it because `AI_canEnterByLand(B)` has already returned
false.

DETERMINISTIC CURRENT-LIVE CONSEQUENCE: AI_guardCity
----------------------------------------------------
`UNIT_GUNSHIP`'s default role is `UNITAI_ATTACK`, and normal `AI_attackMove`
repeatedly calls `AI_guardCity(..., bSearch=true, ...)`, including the one-turn
and three-turn reinforcement searches.

`AI_guardCity` scans the owner's cities in this order:

    FOR_EACH_CITYAI(pLoopCity, GET_PLAYER(getOwner()))
    {
        if (!AI_canEnterByLand(pLoopCity->getArea()))
            continue;
        ...
        if (generatePath(pLoopCity->getPlot(), ..., &iPathTurns, 10))
            ...
    }

Reproduction geometry:

1. AI player owns city A on landmass A and under-defended/endangered city B on
   distinct landmass B.
2. A current SAS Gunship is on A and is available for the normal UNITAI_ATTACK
   movement logic.
3. Water separates A and B, but an all-terrain Gunship has a legal raw path
   across those water tiles.
4. `AI_guardCity` evaluates B.
5. `AI_canEnterByLand(B.area)`:
   - B is not in the Gunship's current area;
   - `canMoveImpassable` is true, so it calls `canEnterArea`;
   - both source and target are land areas with different representative areas;
   - `CvArea::canBeEntered` returns false without supporting MoveAllTerrain.
6. `AI_guardCity` executes `continue` and NEVER calls `generatePath` for B.
7. B therefore cannot be selected by this reinforcement routine even though the
   movement engine can route the Gunship there.

This is a hard reachability contradiction, not merely a different heuristic
score: the legal destination is removed before path cost or city need is ever
considered.

BREADTH
-------
The helper is used widely. Current `CvUnitAI.cpp` contains more than twenty
`AI_canEnterByLand` call sites. Only a small minority explicitly add their own
`|| canMoveAllTerrain()` escape (notably Settler/founding code). Many others do
not, including city guarding, healing/city selection, pillaging, city attacks,
goody targeting and several Great-Person/spy/upgrade-related destination scans.
Those callers can inherit the same all-terrain false-negative when their own
semantics otherwise permit a cross-area destination.

There is also broader same-landmass AI code outside this helper. In particular,
`AI_pickTargetCity` rejects cities outside `getArea()` and `AI_goToTargetCity`
asserts that its target is in the current area. Those restrictions can further
limit cross-water use of a SAS all-terrain UNITAI_ATTACK Gunship. They are useful
breadth / follow-up territory, but they are NOT required to prove F599 and are
not folded into the helper repair without a separate call-contract audit.

ANCESTRY / ACTIVATION
---------------------
The narrow helper is inherited from pre-SAS AdvC optimization work.

Reachable AdvC history:

    practical 1925
    e4ff624d23ec90298119911fa182fa0e4c870d5a
    "Remove or replace unnecessary AI_plotValid calls"

introduced the replacement pattern and documented that MoveAllTerrain was an
unused exception. Practical 1926 then moved the helper out of the header while
retaining:

    // Not checked (to save time): unused canMoveAllTerrain

Base AdvC 1.14 data kept that assumption dormant:

* `UNIT_GUNSHIP`: DOMAIN_LAND, MoveImpassable=0, MoveAllTerrain=0.
* `UNIT_AIRSHIP`: DOMAIN_AIR, MoveAllTerrain=0.
* Robotic Recon Drone did not exist.

SAS later intentionally activates the dormant domain. The clearest Gunship
activation is:

    practical 4985
    b9e253a3ce3f13933babe7714a22ddadd31789a4

whose title includes making Gunship move impassable and whose XML diff changes:

    bCanMoveImpassable: 0 -> 1
    bCanMoveAllTerrain: 0 -> 1

Later SAS unit reworks retain those flags and add/retain the all-terrain Airship
and Robotic Recon Drone.

Classification: SAS-activated inherited AdvC optimization assumption.

DEDUP BOUNDARY
--------------
Whole-album semantic search found no existing root for all-terrain AI units
being rejected by `AI_canEnterByLand` / area prefilters.

Nearby controls are distinct:

* F376/KI#699 is the malformed `AI_connectBonus` replacement
  `kPlot.isArea(kPlot.getArea())`, a boolean tautology that makes the entire
  helper return false. Its repair note merely observed that restoring
  `AI_plotValid` would preserve unusual all-terrain semantics; it does not own
  the broad `AI_canEnterByLand` helper omission.
* F152 concerns GroupPathFinder cache membership / movement-rule invalidation,
  not target elimination before pathfinding.
* earlier Gunship/no-city-capture work concerns combat/capture valuation once a
  city is already in the relevant area, not cross-landmass reachability.
* historical archaeology C139/C140 verified that the XML/Sevopedia/engine
  movement capability itself is intentional and legal; it did not establish
  that every AI fast prefilter honors that newly activated capability.

REPAIR DIRECTION
----------------
The smallest root-level repair is to make the helper preserve the same explicit
MoveAllTerrain escape as `AI_plotValid`:

    bool AI_canEnterByLand(CvArea const& kArea) const
    {
        return (isArea(kArea) || canMoveAllTerrain() ||
            (canMoveImpassable() && canEnterArea(kArea)));
    }

This remains only a cheap coarse reachability gate. Individual terrain/feature,
ownership, war, danger and path-cost legality are still decided by the real
plot/path checks afterward. Therefore an all-terrain unit being allowed past
this prefilter does not imply that every destination becomes legal.

Regression controls should include:

1. current SAS Gunship on landmass A + own endangered city B on landmass B:
   helper must not reject B before `generatePath`;
2. raw GroupPathFinder succeeds across a legal water corridor for the same
   all-terrain Gunship;
3. ordinary Infantry on A remains rejected for B by the coarse land-area gate;
4. current Settler behavior remains unchanged (its existing explicit
   MoveAllTerrain escape becomes redundant but equivalent);
5. review the separate direct `isArea(getArea())` attack-city target filters as
   follow-up breadth rather than assuming the helper fix automatically covers
   them.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F599/KI#922 are CONFIRMED.
F600 / provisional KI#923 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F599's now-owned AI_canEnterByLand / all-terrain prefilter root;
- inspect the adjacent direct same-area-only target selectors only where a
  current all-terrain unit's intended role can reach them, deduplicating as
  F599 breadth unless the repair/state machine is genuinely independent;
- continue territory/domain legality outside that AI optimization family;
- whole-album semantic dedup before numbering;
- promote F600 only for an independent current-live contradiction;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP416
===============================================================================

===============================================================================
C031-WIP417 -- QUEUE 007: TERRITORY / DOMAIN LEGALITY CONTROL -- F600 UNUSED
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F599/KI#922 remain CONFIRMED.
F600 / provisional KI#923 remains UNUSED.
Actual repository/gameplay source remains untouched.

POST-F599 DIRECT SAME-AREA TARGET-SELECTOR REVIEW
-------------------------------------------------
The adjacent `AI_pickTargetCity` / `AI_goToTargetCity` same-area restrictions
were reopened because current SAS makes Gunship a real DOMAIN_LAND
`canMoveAllTerrain` UNITAI_ATTACK unit.

Current `AI_pickTargetCity` still rejects every enemy city outside the unit's
current `CvArea` before path generation:

    if (!pLoopCity->isArea(getArea()))
        continue;

and `AI_goToTargetCity` asserts that its city target belongs to the current
area and filters adjacent attack plots by the same area.

These restrictions can limit how fully the AI exploits an all-terrain Gunship,
but the evidence is not clean enough for an independent F600:

* `AI_pickTargetCity` / `AI_goToTargetCity` is explicitly the ordinary
  land-assault-city target routine rather than a generic all-terrain
  reachability helper;
* current Gunship has `bNoCapture=1`;
* normal UNITAI_ATTACK entry reaches the target-city branch only under
  `bLandWar` / group conditions, and cross-water enemy-city strategy also
  depends on area-level war architecture beyond this one filter;
* therefore restoring `canMoveAllTerrain()` only at this one city filter would
  not by itself establish a complete or clearly intended overseas attack-city
  state machine.

This is useful F599 breadth / all-terrain-AI architectural follow-up, not a
second numbered root. F599 itself remains independently proven through generic
`AI_canEnterByLand()` callers such as own-city reinforcement where the legal
cross-water destination is unambiguously within the caller's contract.

TERRITORY / DOMAIN LEGALITY PAIR
--------------------------------
The authoritative `CvUnit` territory family was traced together:

* `canEnterTerritory`
* `getDeclareWarMove`
* `canMoveInto`
* `canEnterArea`
* `isValidDomain`
* `canCargoEnterTerritory`
* `isIntruding`
* `canAdvance`

The core current rules remain coherent:

* friendly / unowned / enemy territory, RivalTerritory, always-invisible,
  hidden-nationality, Open Borders and AdvC disengagement access are handled by
  `canEnterTerritory`;
* the AdvC barbarian border-obstacle check is deliberately applied before the
  broad RivalTerritory/hidden exceptions and remains inherited from Base 1.14;
* `canMoveInto` adds plot-specific terrain/feature/domain/loading/combat rules
  after the territory decision;
* all-terrain land units are accepted dynamically by `isValidDomain`;
* cargo territory checks enumerate the actual cargo and require each passenger
  to have the relevant territory access;
* `canAdvance` separately blocks no-city-capture units from advancing into an
  enemy city after combat.

No current-live owner/team/direction contradiction comparable to F595 or F599
was established in these functions.

GENERIC SPY-CARGO ROBUSTNESS CONTROL -- CURRENTLY MASKED
-------------------------------------------------------
One genuine generic mismatch was found but does not meet the current-live
threshold under shipped SAS settings.

`CvUnit::canMoveInto` contains a special Spy restriction:

    if (isSpy() && GC.getDefineBOOL(CvGlobals::USE_SPIES_NO_ENTER_BORDERS))
    {
        if (kPlot.getOwner() != NO_PLAYER &&
            !GET_PLAYER(getOwner()).canSpiesEnterBorders(kPlot.getOwner()))
        {
            return false;
        }
    }

But `canCargoEnterTerritory` asks each loaded unit only for
`canEnterTerritory(...)`. A Spy has RivalTerritory access, so with
`USE_SPIES_NO_ENTER_BORDERS=1` a transport could generically carry a Spy into
territory that the Spy could not enter under its own `canMoveInto` rule.

The inherited BtS default is:

    USE_SPIES_NO_ENTER_BORDERS = 0

and current SAS does not override that define in its shipped XML. Therefore the
extra Spy-border rule is disabled in current recommended play and the cargo
mismatch is dormant. Preserve as mod-mod / define-activation robustness debt;
do not assign F600.

ISINTRUDING CONTROL
-------------------
`isIntruding()` intentionally does not mean "unit lacks legal movement access."
Its live callers are Spy interception / Spy-AI timing. Open Borders therefore
still counts as foreign intrusion but receives the reduced
`ESPIONAGE_SPY_NO_INTRUDE_INTERCEPT_MOD`; vassal/master relationships and AdvC
Disengagement suppress intrusion explicitly. This is an intentional espionage
semantic distinction, not a mismatch with `canEnterTerritory`.

DURABLE STATE
-------------
Queue 007 remains OPEN.
F593-F599 confirmed.
F600 / KI#923 UNUSED.
Source untouched.

Exact continuation cursor:
- leave F599's all-terrain prefilter root and same-area attack-city breadth
  behind unless a new caller proves a separate complete contract;
- leave the USE_SPIES_NO_ENTER_BORDERS cargo seam as current-data-masked;
- rebuild the next authoritative CvUnit frontier outside territory legality;
- prefer deterministic state/action/counter contradictions over heuristic
  quality disagreements;
- whole-album semantic dedup before numbering;
- promote F600 only for an independent current-live root;
- physically freeze WIP418 regardless.
===============================================================================
END C031-WIP417
===============================================================================

===============================================================================
C031-WIP418 -- QUEUE 007: F600 CONFIRMED -- AIR-COMBAT DAMAGE USES ATTACKER PLOT
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F600/KI#923 are CONFIRMED.
F601 / provisional KI#924 is UNUSED.
Actual repository/gameplay source remains untouched.

F600 / KI#923 -- ADVC PRACTICAL-1925 PLOT-CLEANUP REGRESSION MAKES AIR-STRIKE
DAMAGE USE THE ATTACKER'S PLOT/CITY INSTEAD OF THE DEFENDER'S TARGET PLOT/CITY
-------------------------------------------------------------------------------
Status: CONFIRMED CURRENT-LIVE BASE-ADVC REGRESSION inherited by SAS.

Current SAS and Base AdvCiv 1.14 contain:

    int CvUnit::airCombatDamage(const CvUnit* pDefender) const
    {
        int iOurStrength = airCurrCombatStr(pDefender);
        FAssertMsg(iOurStrength > 0,
            "Air combat strength is expected to be greater than zero");
        int iTheirStrength = pDefender->maxCombatStr(plot(), this);

        int iStrengthFactor = (iOurStrength + iTheirStrength + 1) / 2;

        int iDamage = std::max(1,
            ((GC.getDefineINT(CvGlobals::AIR_COMBAT_DAMAGE) *
            (iOurStrength + iStrengthFactor)) /
            (iTheirStrength + iStrengthFactor)));

        CvCity const* pCity = getPlot().getPlotCity();
        if (pCity != NULL)
        {
            iDamage *= std::max(0, (pCity->getAirModifier() + 100));
            iDamage /= 100;
        }
        return iDamage;
    }

Both `plot()` / `getPlot()` calls above refer to the attacking aircraft's own
current base plot. `pDefender` can be many tiles away on the actual air-strike
target plot.

BtS/Civ4CE and K-Mod instead preserve the original target-plot contract:

    pPlot = pDefender->plot();
    ...
    iTheirStrength = pDefender->maxCombatStr(pPlot, this);
    ...
    pCity = pPlot->getPlotCity();

That distinction is authoritative gameplay state, not UI valuation.
`CvUnit::airStrike` chooses the real target defender and then directly applies:

    int iDamage = airCombatDamage(pDefender);
    ...
    pDefender->setDamage(iUnitDamage, getOwner());

CURRENT-LIVE CONSEQUENCE 1 -- TARGET BUNKER IS READ FROM THE WRONG CITY
-----------------------------------------------------------------------
Current SAS ships exactly one nonzero building `iAirModifier` source:

    BUILDING_BUNKER
    iAirModifier = -50

The stock/K-Mod target-plot logic therefore halves ordinary air-strike damage
against a defender in a Bunker city:

    targetCity.getAirModifier() + 100 = 50

Current AdvC/SAS instead asks `getPlot().getPlotCity()` on the aircraft itself.
Thus, for an aircraft based in an ordinary city with no Bunker and striking an
enemy Bunker city, the target's -50% air-defense modifier is not read at all;
the multiplier is taken from the attacker's base city instead. Conversely, if
the attacking aircraft is based in a Bunker city and attacks a remote city
without one, current code can wrongly halve the outgoing strike damage.

A deterministic two-city regression fixture is therefore enough:

1. Player A owns an air-strike-capable Fighter/Bomber in city A with no Bunker.
2. Player B owns a valid defender in city B and builds a Bunker there.
3. A air-strikes B.
4. Correct BtS/K-Mod logic applies B's -50 AirModifier.
5. Current AdvC/SAS reads A's city instead and therefore ignores B's Bunker.

Terrain/defender modifiers can be chosen neutral in the fixture so that the
Bunker term alone exposes the wrong city; the source contradiction does not
depend on any particular combat-roll outcome because `airCombatDamage` is
deterministic after the defender is selected.

CURRENT-LIVE CONSEQUENCE 2 -- DEFENDER STRENGTH USES THE WRONG PLOT
--------------------------------------------------------------------
The same wrong-plot replacement feeds:

    pDefender->maxCombatStr(plot(), this)

`maxCombatStr` uses its supplied `pPlot` for authoritative defensive context,
including plot defense, city-defense status, hills defense, feature defense and
terrain defense. Passing the aircraft's base plot therefore evaluates the
defender as though it were standing on the attacker's plot for those terms,
while the defender is actually standing on the remote target plot.

This can independently overstate or understate air-strike damage whenever the
attacker's base terrain/city context differs from the target defender's real
terrain/city context. The later attacker-modifier portion of `maxCombatStr`
recovers the defender's actual `plot()` internally for `pAttackedPlot`, but the
defender-side plot modifiers above still use the caller-supplied wrong plot.

ANCESTRY / INTRODUCTION IS EXACT
--------------------------------
The regression was introduced by Base AdvC practical commit 1925:

    e4ff624d23ec90298119911fa182fa0e4c870d5a
    2020-01-13
    "Remove or replace unnecessary AI_plotValid calls"

The commit broadly optimized plot-access / legality code, cached current plots,
and replaced many `plot()->` uses with `getPlot().`. In the exact CvUnit hunk it
removed the local variable whose semantic identity was the DEFENDER plot:

    - CvPlot* pPlot = pDefender->plot();

and mechanically changed:

    - pDefender->maxCombatStr(pPlot, this);
    + pDefender->maxCombatStr(plot(), this);

    - CvCity* pCity = pPlot->getPlotCity();
    + CvCity const* pCity = getPlot().getPlotCity();

The practical's documented purpose concerns AI_plotValid and plot-access
optimization; it gives no gameplay rationale for changing the target of air
combat from defender plot to attacker plot. K-Mod and Civ4CE retain the old
correct defender-plot code. Base AdvC 1.14 and current SAS retain the practical-
1925 regression unchanged.

SAME-ROOT DORMANT BREADTH -- rangeCombatDamage
----------------------------------------------
The very next hunk in practical 1925 performs the same substitution inside
`rangeCombatDamage`:

    - CvPlot* pPlot = pDefender->plot();
    - pDefender->maxCombatStr(pPlot, this);
    + pDefender->maxCombatStr(plot(), this);

This is the same mechanical root and should be repaired at the same time by
using the defender's plot. Current SAS, however, does not provide a clean
current-live range-strike fixture through this helper: the non-air units with
positive `iAirRange` (SAM Infantry / Mobile SAM) have `iAirCombat=0`, while
`canRangeStrike()` requires `airBaseCombatStr() > 0`. Preserve this as same-root
robustness breadth rather than consuming a second finding number.

WHOLE-ALBUM DEDUP
-----------------
Broad WIP417 searches for `airCombatDamage`, `AirModifier`, `iAirModifier`,
`Bunker`, air-strike damage, target-city air defense, and defender-plot air
combat found no existing finding root.

Earlier air-strike entries concern AI assertions/selection, interception,
logging, help, or generic action-state accounting. None owns the authoritative
executor using the attacking aircraft's plot in place of the defender's target
plot when computing strike damage.

F600 is therefore independent of:

* F598's naval Blitz / sea-pillage made-attacks counter regression;
* F599's all-terrain AI reachability prefilter;
* prior air-strike AI valuation/assertion/help findings, which do not repair
  `CvUnit::airCombatDamage`'s target-plot identity.

NARROW REPAIR BOUNDARY
----------------------
Restore one explicit defender-plot identity and use it consistently:

    CvPlot const* pPlot = pDefender->plot();
    int iTheirStrength = pDefender->maxCombatStr(pPlot, this);
    ...
    CvCity const* pCity = pPlot->getPlotCity();

or the equivalent direct reference-based form.

Apply the same target-plot repair to `rangeCombatDamage` because practical 1925
introduced the identical substitution there, even though current shipped SAS
does not presently activate that sibling helper with nonzero ranged air-combat
strength.

Regression controls:

1. Air-strike a defender in a Bunker city from a non-Bunker base: target -50%
   AirModifier must apply.
2. Reverse Bunker placement (attacker base has Bunker, target does not): the
   attacker's Bunker must not reduce outgoing damage.
3. Use target vs base plots with deliberately different defensive terrain/city
   modifiers and verify defender strength follows the target plot.
4. Ordinary identical-terrain/no-Bunker strike remains unchanged.
5. If non-air range-strike content is later activated, verify its defender
   strength also uses `pDefender->plot()`.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F600/KI#923 are CONFIRMED.
F601 / provisional KI#924 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F600's practical-1925 wrong target-plot root (including dormant
  rangeCombatDamage breadth);
- continue the rebuilt authoritative CvUnit frontier outside territory and
  air/range damage;
- prefer current-live producer/consumer/state contradictions;
- use K-Mod/BtS/Civ4CE differences as ancestry oracles, never as proof alone;
- whole-album semantic dedup before numbering;
- promote F601 only for an independent current-live root;
- physically freeze WIP419 regardless.
===============================================================================
END C031-WIP418
===============================================================================


===============================================================================
C031-WIP419 -- QUEUE 007: PRIVATEER DEAD LAND-CARGO CAPACITY -- F601 CONFIRMED
Continuation after C031-WIP418
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F600/KI#923 remain CONFIRMED.
F601 / provisional KI#924 is CONFIRMED.
F602 / provisional KI#925 is UNUSED.
Actual repository/gameplay source remains untouched.

-------------------------------------------------------------------------------
F601 / provisional KI#924 -- SAS deliberately gives Privateer two DOMAIN_LAND
cargo slots, but the inherited hidden-nationality loading rule rejects every
current land unit; the advertised capacity is unusable and downstream code can
mistake the Privateer for a real land transport
-------------------------------------------------------------------------------

ROOT
----
Current SAS `UNIT_PRIVATEER` declares:

    <Domain>DOMAIN_SEA</Domain>
    ...
    <SpecialCargo>NONE</SpecialCargo>
    <DomainCargo>DOMAIN_LAND</DomainCargo>
    <iCargo>2</iCargo>
    ...
    <bHiddenNationality>1</bHiddenNationality>
    <bAlwaysHostile>1</bAlwaysHostile>

The two slots are not inherited stale data. SAS practical 4676 / commit
`bf2f1f436787c7e34669e156d443297974c23250` (2025-04-12), whose full message
explicitly lists "rework slightly the privateer", changes this exact field:

    <DomainCargo>DOMAIN_LAND</DomainCargo>
  - <iCargo>0</iCargo>
  + <iCargo>2</iCargo>

Thus SAS intentionally activated cargo capacity on the Privateer.

The authoritative loader, however, retains the BtS/K-Mod/Base-AdvC hidden-
nationality restriction. `this` is the proposed cargo and `kUnit` the proposed
transport:

    if (!m_pUnitInfo->isHiddenNationality() &&
        kUnit.getUnitInfo().isHiddenNationality())
    {
        return false;
    }

An ordinary land unit therefore cannot load onto a hidden-nationality
Privateer even when domain, team, plot and free cargo-space tests all succeed.

CURRENT XML EXHAUSTION -- ZERO LEGAL LAND CARGO TYPES
------------------------------------------------------
A namespace-aware census of the supplied current `CIV4UnitInfos.xml` finds:

    hidden-nationality unit types: 1
        UNIT_PRIVATEER -- DOMAIN_SEA

    hidden-nationality DOMAIN_LAND unit types: 0

    total current DOMAIN_LAND unit types: 109

Privateer itself requires DOMAIN_LAND cargo. Because every current land unit is
non-hidden-nationality, every one reaches the restriction above and is rejected.
The only hidden-nationality unit is the sea-domain Privateer itself and therefore
does not satisfy the transport's `DomainCargo=DOMAIN_LAND` contract.

There is no promotion/state escape hatch here: the loading test queries the
unit's `CvUnitInfo::isHiddenNationality()` flag directly. Current promotions do
not turn an ordinary land unit into a hidden-nationality cargo type for this
check.

Hence the currently shipped Privateer has two nominal land-cargo slots but
**zero legal current cargo unit types**.

PLAYER-FACING CONTRACT IS ALSO WRONG
------------------------------------
This is not merely an unused internal XML number. Current `CvGameTextMgr`
advertises every positive `getCargoSpace()` in unit help / Sevopedia:

    if (u.getCargoSpace() > 0)
    {
        ...
        TXT_KEY_UNIT_CARGO_SPACE, u.getCargoSpace()
    }

and live-unit help similarly displays `cargoSpace()`.

The Privateer therefore presents itself as having cargo capacity 2 while the
actual load command rejects every current unit compatible with its declared
cargo domain.

ANCESTRY / ACTIVATION BOUNDARY
------------------------------
The hidden-nationality loading rule is old and deliberate-looking rather than a
SAS invention. Civ4CE/BtS and K-Mod contain the same logic:

    if (!cargo.isHiddenNationality() && transport.isHiddenNationality())
        return false;

Base AdvC 1.14 retains it in `CvUnit::canLoadOnto` after the API refactor.

The privateer's positive cargo capacity is the new activation. Practical 4676
changes 0 to 2 while retaining `DomainCargo=DOMAIN_LAND` and the Privateer's
hidden-nationality identity. Therefore this is a SAS XML <-> inherited-C++
contract regression, not a newly introduced loader bug.

DOWNSTREAM GAMEPLAY / AI BREADTH
--------------------------------
The dead capacity can affect more than the load button because several generic
systems trust `cargoSpace()` as evidence that a hull is a usable transport.

A concrete already-audited example is `PUF_isEnemyCityAttacker`:

    if (u.getCargoSpace() <= 0 || u.getSpecialCargo() != NO_SPECIALUNIT)
    {
        if (u.getDomainType() != DOMAIN_LAND)
            return false;
        ...
    }
    return PUF_isEnemy(...);

A sea unit with positive generic cargo space bypasses the non-land rejection.
Privateer therefore qualifies for the helper's conservative "land invasion
transport" proxy solely because `iCargo=2`, even though no current land attacker
can actually load onto it.

This corrects an older album CONTROL in the `PUF_isEnemyCityAttacker` audit that
listed:

    Galley, Galleon, Transport, Privateer

as generic positive-capacity DOMAIN_LAND transports and concluded that the
cargo XML supported the helper's intended proxy. That statement was structurally
true only at the UnitInfo field level. The deeper `canLoadOnto` audit proves that
Privateer's advertised capacity is not *usable* capacity. Preserve the older
control for Galley/Galleon/Transport, but exclude Privateer from the positive
"can actually carry land attackers" premise.

ROOT BOUNDARY / DEDUP
---------------------
Whole-album searches for Privateer + cargo/load/transport, hidden-nationality
cargo/loading, and `canLoadOnto` found no prior finding that owns this content
contract.

This is independent of:

* F595/KI#918, where directly gifting already-loaded cargo can preserve an
  invalid cross-team transport pointer. F601 occurs before loading and makes
  Privateer's declared current cargo capacity unusable at all.
* F599/KI#922, which concerns all-terrain AI area prefilters.
* earlier Privateer blockade/heal/visibility findings, which do not involve
  cargo eligibility.

The older PUF transport passage is a positive-control correction/breadth witness,
not an existing root that repairs or subsumes F601.

REPAIR / DESIGN BOUNDARY
------------------------
Do not blindly delete the inherited hidden-nationality restriction. It has a
plausible gameplay purpose: nationally identifiable troops should not
necessarily be allowed to hide aboard an anonymous Privateer without diplomatic
consequences.

The current contradiction should instead be resolved deliberately in one of two
ways:

1. If Privateer was not meant to transport land units, restore `iCargo=0`
   (and keep `DomainCargo` irrelevant as before). This removes the misleading
   help/AI transport signal.
2. If the two cargo slots are intended gameplay, define which cargo can legally
   use them and modify the nationality/cargo design coherently -- e.g. a
   purpose-specific allowed class/state rather than globally allowing all
   ordinary troops aboard hidden-nationality ships without considering identity
   and diplomacy.

Regression controls for either choice:

* Sevopedia/live help must match usable cargo behavior.
* A normal current land unit should either be deliberately loadable or the
  Privateer should no longer advertise generic capacity.
* Generic threat/transport helpers must not count dead capacity as land-invasion
  capacity.
* Galley/Galleon/Transport ordinary cargo behavior remains unchanged.

DORMANT ADJACENT CONTROL -- `bGarrisonStrength`
-----------------------------------------------
The post-WIP418 semantic-operand pass also found a real arithmetic defect in the
special `maxCombatStr(..., bGarrisonStrength=true)` path: `AI_plotDefense` is
stored in `iExtraModifier` and added, the garrison branch then skips replacing it
with `fortifyModifier()`, but the common tail adds `iExtraModifier` again. Plot
strength can therefore be counted twice (or a stale preceding modifier can be
re-added).

Current SAS ships `DEMAND_BETTER_PROTECTION=0`, and this is the gameplay family
that uses that special garrison-strength mode. Preserve it as dormant
AdvC/mod-mod correctness debt, not F602.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F601/KI#924 are CONFIRMED.
F602 / provisional KI#925 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F601's dead Privateer cargo-capacity root and its PUF threat-proxy
  breadth;
- continue the rare SAS UnitInfo-field activation sieve outside all-terrain and
  Privateer cargo;
- pair each newly activated field with its authoritative CvUnit consumer and
  current legal producer/caller;
- prefer state/legality contradictions over AI-style disagreements;
- whole-album semantic dedup before numbering;
- promote F602 only for an independent current-live root;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP419
===============================================================================


===============================================================================
C031-WIP420 -- QUEUE 007 CvUnit.cpp -- F602 COMBAT-LIMIT DOMAIN ACTIVATION
===============================================================================

F602 / KI#925 -- SAS Janissary `iCombatLimit=110` exceeds max hit points;
actual combat treats it as fully lethal while combat-odds code requires 110 damage
-------------------------------------------------------------------------------
STATUS: CONFIRMED CURRENT-LIVE SAS CONTENT <-> COMBAT-ODDS CONTRACT BUG
SEVERITY / CONSEQUENCE: player-visible displayed combat odds and AI combat valuation
can materially underestimate Janissary attacks. The actual combat resolver kills at
normal 100 damage, while the odds model requires damage up to the raw 110 combat
limit. The same raw-limit mistake also survives in the legacy/manual AI odds fallback.

ROOT
----
Current SAS deliberately ships one ordinary combat unit whose XML combat limit is
above the engine's normal full-health range:

    UNIT_OTTOMAN_JANISSARY
        iCombat      = 12
        iCombatLimit = 110

A current UnitInfo census finds no other shipped `iCombatLimit > 100` unit. Base
AdvCiv 1.14's Janissary instead has:

        iCombat      = 9
        iCombatLimit = 100

The core combat resolver does NOT give a 110 limit any meaning beyond ordinary lethal
combat. `CvUnit::resolveCombat` checks the special non-lethal combat-limit stop with:

    bool const bLimitReached =
        (std::min(GC.getMAX_HIT_POINTS() - 1,
            pDefender->getDamage() + iDamage) >= combatLimit());

For a limit of 110 and the normal 100-hit-point unit range, the left side is capped at
99 and can never reach 110. The combat-limit withdrawal/stop branch therefore never
fires. Damage continues normally until `pDefender->isDead()` at full lethal damage.
The combat simulator uses the same `min(MAX_HIT_POINTS - 1, ...) >= combatLimit()`
contract, so it likewise treats 110 as ordinary lethal combat.

`CombatOdds::setupCombatantsImpl`, however, uses the raw combat limit as a damage
requirement:

    int iDefHitLimit =
        kDefender.maxHitPoints() - kAttacker.combatLimit();

    att.setHitsToWin(intdiv::uceil(std::max(0,
        kDefender.currHitPoints() - iDefHitLimit),
        att.damagePerRound()));

At full health with `combatLimit() == 110`, `iDefHitLimit` becomes -10. The attacker
is therefore modeled as needing:

    100 - (-10) = 110 damage

rather than the 100 damage that actually kills the defender.

This is a hard executor-vs-predictor disagreement. Values at or above full hit points
are treated as fully lethal by the real combat lifecycle and by surrounding
`combatLimit() >= MAX_HIT_POINTS` policy checks, but CombatOdds extrapolates the raw
XML number beyond the target's actual health range.

CURRENT-LIVE PLAYER / AI CONSUMERS
----------------------------------
This is not dead diagnostic arithmetic.

`CvGameTextMgr` obtains normal land-combat odds through:

    int const iCombatOdds =
        calculateCombatOdds(*pAttacker, *pDefender);

and specifically treats attackers with
`combatLimit() >= GC.getMAX_HIT_POINTS()` as normal lethal attackers when deciding
whether to show victory odds. The Janissary therefore enters the normal displayed
victory-odds path, but that displayed value is calculated with the impossible 110-
damage requirement above.

The normal LFB-enabled AI path also calls `calculateCombatOdds`, and current SAS ships
`LFB_USECOMBATODDS=1`. Thus AI attack valuation can inherit the same underestimated
Janissary odds.

Even the old/manual `CvUnitAI::AI_attackOdds` fallback has the same raw-domain
assumption:

    int iHitLimitThem =
        pDefender->maxHitPoints() - combatLimit();

followed by a rounds-needed calculation. Disabling LFB therefore does not make the
above-100 data safe by itself.

CLEAN CURRENT FIXTURE
---------------------
A neutral, full-health Janissary versus a normal full-health Musketman is enough to
expose the round-count mismatch without terrain/city complications.

Current generic Musketman:

    iCombat = 12
    no XML free promotion

Current Janissary:

    iCombat = 12
    free PROMOTION_COMBAT3 (+10% combat)
    iCombatLimit = 110

On a neutral plot, the exact damage-per-round value depends on the normal integer
strength/firepower formula, but the invariant does not: actual combat needs only
enough successful hits to accumulate 100 damage; CombatOdds asks the same attacker to
accumulate 110. Whenever one extra hit is needed to bridge 100 -> 110 -- which occurs
for the ordinary roughly-20-damage rounds in this matchup -- the displayed/AI model
requires one more winning combat round than the executor.

For intuition only (not required for the proof), roughly 20 damage per Janissary hit
means:

    actual resolver: 5 successful hits can kill at 100
    CombatOdds:       6 successful hits are requested for 110

That is large enough to move the modeled victory probability materially, not merely a
one-permille presentation rounding difference.

SAS INTRODUCTION / PERSISTENCE
------------------------------
The >100 domain is SAS-authored and deliberate-looking rather than accidental XML
parser garbage.

Practical 4957 / commit
`fe58e134631681ba9e79e481dd29a761b8820696` (2025-08-24) changes the relevant
musket-era unique-unit block from:

    iCombat      12 -> 14
    iCombatLimit 100 -> 110

alongside broader military-balance changes.

Practical 5025 / commit `8138f8af...` later reorganizes the Musketman/unique-unit
blocks and explicitly carries the 110 limit with the Janissary identity while the
generic Musketman side is restored to 100. This is useful provenance: the unusual
value does not merely remain attached to an arbitrary XML position during a reorder.

Practical 5924 later nerfs current Janissary combat strength from 14 -> 12 while
leaving:

    iCombatLimit = 110

unchanged. The >100 value therefore survives multiple later balance passes and is
current data.

ANCESTRY / ACTIVATION BOUNDARY
------------------------------
Base AdvCiv 1.14's Janissary uses `iCombatLimit=100`, and normal BtS/K-Mod unit-data
conventions keep lethal units at the full-hit-point limit rather than above it. The
combat and odds code was consequently written around the conventional domain:

    partial/non-lethal: combatLimit < MAX_HIT_POINTS
    fully lethal:       combatLimit >= MAX_HIT_POINTS

The real combat resolver is robust enough that values above the maximum simply behave
like fully lethal combat. `CombatOdds`, however, assumes the raw value itself is a
reachable damage threshold. SAS newly activates the unsupported distinction between
`== MAX_HIT_POINTS` and `> MAX_HIT_POINTS`.

ROOT BOUNDARY / WHOLE-ALBUM DEDUP
---------------------------------
Whole-album searches for `combatLimit`, `hitsToWin`, `iDefHitLimit`, Janissary and
`calculateCombatOdds` found no existing finding owning an above-max-hit-point combat
limit.

The closest earlier work is independent:

* F200/KI#523: loss of raw ordinary odds through personality adjustment + 1..99 clamp
  before nonlinear AI weighting.
* F210/KI#533 and the later current-file air controls: DOMAIN_AIR is fed into ordinary
  combat-strength/odds machinery.
* prior siege/combat-limit investigations explicitly test the normal
  `0 < iCombatLimit < 100` partial-lethal domain.
* an old Battle-History control noted that ordinary lethal fighting units used 100 and
  therefore did not exercise a partial-limit classification branch. It did not cover
  or repair a `>100` predictor/executor mismatch.

F602 is therefore an independent current-live root: SAS expands the XML domain beyond
full hit points, and the combat-odds predictor fails to clamp that value to what the
actual combat state can represent.

REPAIR BOUNDARY
---------------
For current SAS content, the smallest and clearest repair is probably data-level:

    UNIT_OTTOMAN_JANISSARY iCombatLimit: 110 -> 100

A value of 110 supplies no extra lethal capability in the actual resolver; the unit
already kills normally at 100. Unless the 110 number was intended to encode some new
mechanic that the engine currently does not implement, restoring 100 removes only the
predictor inconsistency, not a real combat advantage.

For engine robustness, odds code should also normalize any combat limit against the
defender's representable hit-point range, e.g. conceptually:

    effectiveLimit = min(attacker.combatLimit(), defender.maxHitPoints())

before deriving `iDefHitLimit` / hits-to-win. The legacy AI fallback should use the
same normalized semantics. That would make future/mod-mod values above max HP behave
in the predictor exactly as they already behave in actual combat.

Do not clamp partial siege limits upward; values below max HP are real supported
non-lethal combat and must remain unchanged.

REGRESSION CONTROLS
-------------------
- Janissary versus a full-health ordinary defender: displayed/AI hits-to-win must
  match the real resolver's lethal 100-damage threshold.
- Any `combatLimit >= defender.maxHitPoints()` must produce the same combat odds as a
  limit exactly equal to defender.maxHitPoints().
- Current Catapult/Trebuchet/Cannon/etc. sub-100 non-lethal limits must retain their
  existing partial-damage semantics and odds.
- The normal combat executor itself should remain unchanged unless the mod explicitly
  decides to invent a semantic meaning for >100 combat limits.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F602/KI#925 are CONFIRMED.
F603 / provisional KI#926 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F602's above-max-hit-point Janissary CombatOdds root;
- continue the rare SAS UnitInfo-domain activation sieve from F603 unused;
- next high-value changed domains include Explorer `bNoCapture`, Scout
  `bIgnoreTerrainCost`, and the SAS Airship's AIR -> LAND capability redesign, with
  F599 all-terrain reachability already excluded;
- require an authoritative current producer/consumer contradiction, not merely a
  surprising balance choice;
- whole-album semantic dedup before numbering;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP420
===============================================================================


===============================================================================
C031-WIP421 -- QUEUE 007 CvUnit.cpp -- F603 GUNSHIP WATER-WITHDRAWAL ACTIVATION
===============================================================================

F603 / KI#926 -- SAS SELF-PROPELLED ALL-TERRAIN LAND UNIT LOSES WITHDRAWAL ON WATER
----------------------------------------------------------------------------------
Status:
- CONFIRMED current-live SAS activation bug in inherited CvUnit combat semantics.
- Current fixture: UNIT_GUNSHIP.
- Root is independent of F599/KI#922: movement/pathfinding is legal here; the stale
  assumption is in the combat withdrawal consumer.

INHERITED RULE / ORIGINAL DOMAIN ASSUMPTION
-------------------------------------------
Current SAS `CvUnit::withdrawalProbability()` is unchanged in meaning from Base AdvC,
K-Mod and BtS/Civ4CE:

    int CvUnit::withdrawalProbability() const
    {
        if (getDomainType() == DOMAIN_LAND && getPlot().isWater())
            return 0;
        return std::max(0,
            m_pUnitInfo->getWithdrawalProbability() + getExtraWithdrawal());
    }

Civ4CE's BtS source and K-Mod use the same DOMAIN_LAND + water hard zero. AdvC
practical 1925 only replaced `plot()->isWater()` with the equivalent
`getPlot().isWater()` during its broad plot-access cleanup; it did not change the
semantic rule.

Under ordinary BtS/K-Mod data this is a useful amphibious rule: a LAND unit whose
combat origin is water is normally cargo attacking from a sea transport, and such an
attacker must not retreat back into the sea.

SAS ACTIVATES A NEW LEGAL STATE
-------------------------------
Current SAS deliberately makes Gunship a self-propelled all-terrain LAND unit:

    Type                 UNIT_GUNSHIP
    Domain               DOMAIN_LAND
    DefaultUnitAI        UNITAI_ATTACK
    bOnlyDefensive       0
    bNoCapture           1
    bCanMoveImpassable   1
    bCanMoveAllTerrain   1
    iMoves               4
    iCombat              26
    iCombatLimit         100
    iWithdrawalProb      25

Base AdvCiv 1.14 already gives Gunship 25% withdrawal, but both movement flags are 0.
SAS practical 4985 / commit `b9e253a3ce3f13933babe7714a22ddadd31789a4`
(2025-09-04) deliberately changes both flags to 1. Current Main Changes documentation
also states that Airship and Gunship can move through all terrains, including water,
through `bCanMoveImpassable` + `bCanMoveAllTerrain`.

The authoritative movement helper honors that domain expansion:

    case DOMAIN_LAND:
    case DOMAIN_IMMOBILE:
        return (!bWater || canMoveAllTerrain());

and `canMoveInto()` uses that result. A current Gunship can therefore move onto water
under its own power; it is not merely occupying the transport's water plot as cargo.

COMBAT PATH IS ALSO LEGAL
-------------------------
Gunship is a normal attacker (`bOnlyDefensive=0`, combat limit 100). `canAttack()`
permits it, and `canAttack(const CvUnit&)` has no general LAND-vs-SEA prohibition.
The special cross-domain amphibious prohibition there only rejects LAND attackers
whose `combatLimit() < 100`; Gunship is 100.

Therefore a supported current state is:

    self-propelled Gunship moves onto water
        -> attacks an adjacent enemy naval defender on water
        -> normal CvUnit::updateCombat / resolveCombat
        -> otherwise-lethal attacker round
        -> withdrawalProbability()
        -> DOMAIN_LAND && origin water
        -> returns 0

The Gunship's XML 25% withdrawal is thus suppressed solely because the legal combat
origin happens to be water.

LIVE EXECUTOR / UI EFFECT
-------------------------
This is not presentation-only. `resolveCombat()` directly rolls
`withdrawalProbability()` when attacker damage would become lethal. The same value is
also folded into combat-message odds and other AI/odds consumers.

Live-unit help calls `pUnit->withdrawalProbability()` and therefore removes the
withdrawal line while a Gunship is on water. By contrast, UnitInfo/Sevopedia help uses
`u.getWithdrawalProbability()` and continues to advertise the Gunship's base 25%.
The UI disagreement is useful corroboration; the actual combat roll is the primary
bug.

WHY `isCargo()` IS THE PRECISE REPAIR DISCRIMINATOR
---------------------------------------------------
The inherited rule was trying to distinguish an amphibious cargo attacker, not water
as an inherently withdrawal-forbidden combat environment.

The current amphibious execution path proves that transport state survives until the
withdrawal roll:

1. `CvSelectionGroup::groupAmphibMove` gathers LAND cargo groups and pushes their
   MOVE_TO / attack mission. It does not unload them first.
2. `CvSelectionGroup::groupAttack` chooses the cargo attacker and directly calls
   `CvUnit::attack()`.
3. `CvUnit::updateCombat()` enters `resolveCombat()` before any winning advance.
4. Only after combat can `groupMove()` call `CvUnit::move()`/`setXY()` to advance the
   attacker onto the destination. `setXY()` clears its transport link when the new
   plot no longer matches its transport.

So an ordinary amphibious LAND attacker is still `isCargo()==true` when
`withdrawalProbability()` is consulted.

A semantically narrow repair is therefore conceptually:

    if (getDomainType() == DOMAIN_LAND &&
        getPlot().isWater() &&
        isCargo())
    {
        return 0;
    }

This preserves the inherited no-retreat rule for actual transported amphibious
attackers, including a Gunship that is genuinely loaded aboard a transport, while a
self-propelled Gunship on water keeps its ordinary 25% withdrawal.

Using `!canMoveAllTerrain()` as the exception would repair the current fixture, but it
is less exact: capability is not the real reason withdrawal should be blocked; actual
cargo/amphibious state is.

CURRENT-DATA PARTITION
----------------------
Current SAS has three DOMAIN_LAND units with `bCanMoveAllTerrain=1`:

    UNIT_AIRSHIP              bOnlyDefensive=1  withdrawal 20
    UNIT_ROBOTIC_RECON_DRONE  bOnlyDefensive=1  withdrawal 50
    UNIT_GUNSHIP              bOnlyDefensive=0  withdrawal 25

Airship and Robotic Recon Drone reinforce the expanded domain but cannot reproduce the
normal attack-side withdrawal loss because they are defensive-only. Gunship is the
clean current-live witness.

WHOLE-ALBUM DEDUP / ROOT BOUNDARY
---------------------------------
Whole-album searches for withdrawal, retreat, amphibious, Gunship, isCargo and
canMoveAllTerrain found no prior root owning this combat-state contradiction.

Closest earlier work is independent:

- F149/KI#472: false-success accounting in `groupAmphibMove` can consume a deferred
  landing when no cargo actually lands. That is mission/execution success state; it
  does not alter combat withdrawal semantics.
- F599/KI#922: `AI_canEnterByLand`/same-area prefilters can reject destinations that
  a MoveAllTerrain unit can physically reach. F603 begins only after legal movement
  has succeeded and the real combat resolver is running.
- Prior withdrawal references concern ordinary odds/value weighting or Battle History
  outcome classification, not suppression of a supported self-propelled water
  attacker's retreat chance.

Fixing any of those roots leaves F603 unchanged, and fixing F603 does not repair them.

DELIBERATE CONTROL: COLLATERAL LIMIT 200
----------------------------------------
The adjacent rare-domain pass also rechecked SAS's widespread
`iCollateralDamageLimit=200`. The executor can allow collateral damage to reach the
ordinary 100-HP death boundary even though the XML limit exceeds 100. However SAS
practical 4676 explicitly says to increase collateral damage limit to 200 for all
units as part of the collateral-system rework. Without contradictory design evidence,
that remains intentional SAS gameplay rather than F603.

REGRESSION CONTROLS
-------------------
- Self-propelled current Gunship on water: live-unit help and actual lethal-round
  withdrawal roll should retain its normal 25% (plus valid promotion modifiers).
- The same Gunship while genuinely loaded as LAND cargo and amphibiously attacking
  from a transport must retain the inherited 0% withdrawal behavior.
- Ordinary transported LAND attackers without MoveAllTerrain must remain at 0% when
  attacking from water.
- Land combat from land and naval combat from water must remain unchanged.
- Airship/Robotic Recon Drone remain defensive-only; no new attack permission should
  be introduced by the repair.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F603/KI#926 are CONFIRMED.
F604 / provisional KI#927 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F603's inherited water-withdrawal / SAS MoveAllTerrain activation root;
- continue the rare SAS UnitInfo-domain/action activation sieve under F604 unused;
- Explorer `bNoCapture`, Scout/recon `bIgnoreTerrainCost`, Airship AIR->LAND redesign
  and other unusual current UnitInfo domains remain candidate controls/frontiers;
- continue preferring producer/consumer semantic contradictions over balance opinions;
- whole-album semantic dedup before numbering;
- physically freeze the next WIP regardless.
===============================================================================
END C031-WIP421
===============================================================================


===============================================================================
CHECKPOINT C031-WIP422 -- QUEUE 007 CvUnit.cpp DEEP RE-AUDIT OPEN
===============================================================================
Date: 2026-09-06
Model: GPT-5.6-Sol
Authoritative current source snapshot:
    AdvCiv-SAS_light_source_20260906T174246.zip
Comparative local source snapshots:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(20260906-154339).zip
    Civ4CE-main(3).zip
Prior physical checkpoint:
    C031-WIP421
New finding in this append:
    F604 / provisional KI#927
Next finding number:
    F605 UNUSED / provisional KI#928
Queue state:
    007 CvGameCoreDLL/CvUnit.cpp DEEP RE-AUDIT OPEN
Source mutation state:
    NONE -- audit only

F604 / provisional KI#927
-------------------------
TITLE
    SAS Airship and Robotic Recon Drone advertise positive withdrawal chances that can
    never execute because both units are OnlyDefensive; upgraded recon units can carry
    still more unusable Retreat withdrawal into the same impossible attacker state.

STATUS
    CONFIRMED current-live SAS content <-> inherited combat-semantics contradiction.
    This is not a balance disagreement about whether the recon units ought to attack.
    The current XML simultaneously declares them strictly defensive and deliberately
    assigns attacker-only withdrawal capability, and current help exposes both claims.

CURRENT DATA / UNIQUE ACTIVATION
    Current SAS has exactly two UnitInfo entries with both:

        bOnlyDefensive = 1
        iWithdrawalProb > 0

    They are:

        UNIT_AIRSHIP
            Domain              = DOMAIN_LAND
            DefaultUnitAI       = UNITAI_EXPLORE
            Combat              = UNITCOMBAT_RECON
            bOnlyDefensive      = 1
            iCombat             = 6
            iWithdrawalProb     = 20

        UNIT_ROBOTIC_RECON_DRONE
            Domain              = DOMAIN_LAND
            DefaultUnitAI       = UNITAI_EXPLORE
            Combat              = UNITCOMBAT_RECON
            bOnlyDefensive      = 1
            iCombat             = 10
            iWithdrawalProb     = 50

    The same census over Base AdvCiv 1.14 and Civ4CE/BtS finds zero units combining
    OnlyDefensive with positive base withdrawal. The unsupported combination is
    therefore a SAS activation rather than normal inherited stock data.

ATTACK LEGALITY MAKES THE WITHDRAWAL UNREACHABLE
    CvUnit::canAttack() is explicit:

        if (!canFight())
            return false;
        if (isOnlyDefensive())
            return false;

    The defender-specific overload is equally explicit:

        if (!canCombat() || isOnlyDefensive())
            return false;

    Thus neither recon unit can legally become the attacker in ordinary unit combat.

    CvUnit::resolveCombat, however, rolls withdrawal only for `this`, the attacking
    unit, when that attacker would otherwise receive lethal damage:

        if (getDamage() + iAttackerDamage >= maxHitPoints() &&
            SyncRandSuccess100(withdrawalProbability()))
        {
            ...
            break;
        }

    There is no corresponding defender-withdrawal roll. The sea-pillage code even
    reinforces this contract by temporarily removing the pillager's withdrawal when
    the pillager is represented as the combat attacker but is conceptually the
    defender:

        int iWithdrawal = withdrawalProbability();
        changeExtraWithdrawal(-iWithdrawal); // no withdrawal since we are really the defender
        attack(...);
        changeExtraWithdrawal(iWithdrawal);

    Therefore positive withdrawal on an OnlyDefensive unit has no execution path.

CURRENT UI CONTRADICTION
    Actual-unit help reports OnlyDefensive explicitly:

        if (pUnit->isOnlyDefensive())
            ... TXT_KEY_UNIT_ONLY_DEFENSIVE

    but later, independently, reports any positive effective withdrawal:

        if (pUnit->withdrawalProbability() > 0)
            ... TXT_KEY_UNIT_WITHDRAWL_PROBABILITY

    UnitInfo / Sevopedia help does the same pair independently:

        if (u.isOnlyDefensive())
            ... TXT_KEY_UNIT_ONLY_DEFENSIVE

        if (u.getWithdrawalProbability() > 0)
            ... TXT_KEY_UNIT_WITHDRAWL_PROBABILITY

    A current newly built Airship can therefore be described as both "Can only
    defend" and "20% withdrawal", while the Robotic Recon Drone can show the same
    contradiction at 50%. The first statement makes the second capability impossible.

UPGRADE BREADTH -- THE FALSE EFFECTIVE VALUE CAN BECOME EVEN LARGER
    Current recon upgrade chain is:

        Scout -> Explorer -> Airship -> Robotic Recon Drone

    Current Explorer has free:

        PROMOTION_RETREAT1  (+10 withdrawal)
        PROMOTION_RETREAT2  (+20 withdrawal)

    CvUnit::convert does not discard the old unit's promotions while upgrading:

        setHasPromotion(eLoopPromotion,
            pUnit->isHasPromotion(eLoopPromotion) ||
            m_pUnitInfo->getFreePromotions(eLoopPromotion));

    setHasPromotion then applies the promotion's withdrawal change through:

        changeExtraWithdrawal(GC.getInfo(ePromotion).getWithdrawalChange() * iChange);

    Consequently an Explorer upgraded into Airship can retain +30 extra withdrawal.
    Its effective live-unit value can therefore become 20 + 30 = 50 despite remaining
    unable to attack. If that line upgrades again, Robotic Recon Drone can retain the
    same +30 on top of its own 50 base value, i.e. an effective 80, still with no legal
    attacker path. This is same-root consequence breadth, not a separate promotion bug.

DESIGN / HISTORY PROOF
    SAS practical 4911:

        92fc29e26e9bfe40311c02af385f58383e09c157
        2025-07-27
        "rework the airship now a recon unit ..."

    performs the Airship AIR -> LAND/recon redesign and deliberately sets:

        bOnlyDefensive: 0 -> 1
        iWithdrawalProb: 0 -> 20

    The same UnitInfo says the unit is to be "strictly observer/explorer/recon based"
    while the withdrawal value is explicitly added.

    SAS practical 4912:

        9ac030d829b21dd2e0bbb128bb4ebf8bb6c4a144
        2025-07-27
        "add the robotic_recon_drone ..."

    introduces the Drone with the explicit comment:

        "defensive unit, not an attack drone"

    and simultaneously gives it positive withdrawal (40 at introduction).

    SAS practical 4949:

        0910e8acee9589366d50e12ff5a860bdd8aa8a8e

    later changes Drone withdrawal 40 -> 50 and, for BOTH Airship and Drone, makes the
    design intent even more explicit by documenting the base withdrawal as a way for a
    newly produced unit to have "quite good" withdrawal while an upgraded Explorer
    would have still more. That same practical removes Retreat1/2 as direct free
    promotions from Airship/Drone but intentionally relies on the base withdrawal and
    the upgrade path. Their OnlyDefensive state remains unchanged.

    The contradiction is therefore not a stale field accidentally inherited from the
    old DOMAIN_AIR Airship: SAS deliberately authored both sides and later revisited the
    withdrawal values while keeping the units strictly defensive.

ANCESTRY / ENGINE CONTRACT
    The attacker-only nature of withdrawal and OnlyDefensive attack prohibition are
    inherited Civ4/K-Mod/AdvC semantics. Base AdvC's Airship is DOMAIN_AIR and has no
    positive withdrawal / OnlyDefensive combination, and Robotic Recon Drone does not
    exist there. Current SAS activates a data combination the inherited rules make
    meaningless.

DETERMINISTIC CONSEQUENCE
    1. Build either current recon unit.
    2. UnitInfo/Sevopedia exposes its positive withdrawal percentage and OnlyDefensive
       status together.
    3. Put the unit adjacent to any legal enemy combat unit.
    4. Attack initiation fails because isOnlyDefensive() is true.
    5. Since resolveCombat is never entered with the recon unit as attacker, its 20/50
       withdrawal can never be rolled.

    For an Explorer-upgraded Airship/Drone, the live-unit helper can advertise still
    higher effective withdrawal because the inherited Retreat promotions are retained,
    but the attack prohibition remains absolute.

DEDUP BOUNDARY
    Whole-album semantic search found no existing root for positive withdrawal on a
    unit that cannot legally attack.

    Nearby findings are independent:

    * F603/KI#926 concerns a real attacking Gunship whose legal 25% withdrawal is
      wrongly suppressed only when self-propelled on water. F604 is the opposite
      boundary: Airship/Drone cannot attack anywhere, so their positive withdrawal is
      unreachable on every plot.
    * F599/KI#922 concerns all-terrain AI reachability prefilters, not combat retreat.
    * earlier promotion-validity findings concern whether effects can be acquired;
      here the effective withdrawal number is genuinely present on the unit, but the
      only combat role that could consume it is prohibited.

REPAIR DIRECTION
    The repeated "strictly observer/explorer/recon" / "defensive unit, not an attack
    drone" comments make preserving OnlyDefensive the least disruptive current-design
    interpretation.

    Under that interpretation:

    1. set Airship and Robotic Recon Drone base iWithdrawalProb to 0 so UnitInfo help no
       longer advertises an impossible intrinsic capability;
    2. harden CvUnit::withdrawalProbability() for effective/live-unit state:

           if (isOnlyDefensive())
               return 0;

       before applying base + extra withdrawal. This also prevents retained Explorer
       Retreat promotions from creating a false effective percentage after upgrade;
    3. optional UI robustness: UnitInfo help can additionally avoid displaying
       withdrawal for isOnlyDefensive() types, protecting mod-mod XML even if such a
       combination is authored again.

    The alternative repair -- clearing bOnlyDefensive so withdrawal can execute -- is
    mechanically possible, but it contradicts the repeated current design comments and
    would turn both recon units into human-usable attackers. That is a gameplay redesign,
    not the conservative audit repair.

POSITIVE CONTROLS CLOSED IN THE SAME RARE-FIELD SIEVE
    * Explorer bNoCapture 1 -> 0 is intentional, not a bug. Practical 4377 is explicitly
      titled "reworked explorer units" and its inline XML comments say the Explorer can
      conquer cities, including opportunistic very weak / Barbarian cities. The same
      rework intentionally clears OnlyAttackBarbarians and blocks offensive AI roles
      through NotUnitAIs.
    * Airship becoming DOMAIN_LAND makes generic land-only actions such as airlift
      structurally available. That is an odd consequence worth remembering, but no
      current source/design invariant has yet shown it to be erroneous. Do not promote
      "Airship can airlift" merely because the old DOMAIN_AIR version could not.
    * iCollateralDamageLimit=200 remains deliberate SAS practical-4676 design; no
      contradictory evidence was found in this pass.

NEXT CURSOR
    WIP422 physical -> F604/KI#927 confirmed -> F605/KI#928 UNUSED -> continue the rare
    SAS UnitInfo/domain/action activation sieve outside F599/F601/F602/F603/F604,
    especially remaining bIgnoreTerrainCost / unusual land-domain action consumers;
    then rebuild the next under-covered authoritative CvUnit producer/consumer frontier
    if the rare-field sieve closes. Physically freeze WIP423 regardless before changing
    file/lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
CHECKPOINT C031-WIP423 -- QUEUE 007 CvUnit.cpp DEEP RE-AUDIT OPEN
===============================================================================
Date: 2026-09-06
Model: GPT-5.6-Sol
Authoritative current source snapshot:
    AdvCiv-SAS_light_source_20260906T174246.zip
Comparative local source snapshots:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(20260906-154339).zip
    Civ4CE-main(3).zip
Prior physical checkpoint:
    C031-WIP422
New finding in this append:
    F605 / provisional KI#928
Next finding number:
    F606 UNUSED / provisional KI#929
Queue state:
    007 CvGameCoreDLL/CvUnit.cpp DEEP RE-AUDIT OPEN
Source mutation state:
    NONE -- audit only

F605 / provisional KI#928
-------------------------
TITLE
    SAS Airship and Robotic Recon Drone are given Mobility Cost as a free promotion,
    but their explicit FlatMovementCost makes the promotion's sole movement-discount
    effect unreachable; live help can advertise both effects even though the flat-cost
    executor returns before the discount is ever consumed.

STATUS
    CONFIRMED current-live SAS content <-> inherited movement-semantics contradiction.
    This is not a tuning judgment about whether the units should be fast. The current
    XML deliberately installs an iMoveDiscountChange promotion on the only two current
    explicit flat-moving units, while the movement executor structurally bypasses that
    field for every flat-movement step.

CURRENT DATA / EXHAUSTIVE ACTIVATION
    Current SAS has exactly two UnitInfo entries with explicit:

        bFlatMovementCost = 1

    They are:

        UNIT_AIRSHIP
            Domain              = DOMAIN_LAND
            Combat              = UNITCOMBAT_RECON
            iMoves              = 2
            bFlatMovementCost   = 1
            bIgnoreTerrainCost  = 0
            free PROMOTION_MOBILITY_COST

        UNIT_ROBOTIC_RECON_DRONE
            Domain              = DOMAIN_LAND
            Combat              = UNITCOMBAT_RECON
            iMoves              = 2
            bFlatMovementCost   = 1
            bIgnoreTerrainCost  = 0
            free PROMOTION_MOBILITY_COST

    Current promotion data has exactly one positive raw move-discount promotion:

        PROMOTION_MOBILITY_COST
            iMoveDiscountChange = 1

    Thus the two explicit flat-moving types are also exactly the two SAS UnitInfo types
    that deliberately receive this otherwise meaningful move-discount effect for free.

EXECUTOR -- FLAT MOVEMENT RETURNS BEFORE MOVE DISCOUNT
    CvPlot::movementCost begins with the flat-movement special case:

        if (kUnit.flatMovementCost())
        {
            return GC.getMOVE_DENOMINATOR();
        }

    Only later, inside ordinary terrain-cost calculation, can ExtraMoveDiscount matter:

        if (iRegularCost > 0)
            iRegularCost = std::max(1,
                    iRegularCost - kUnit.getExtraMoveDiscount());

    Therefore a flat-moving unit always pays exactly one MOVE_DENOMINATOR for the step
    and exits before `getExtraMoveDiscount()` is read. Whether that unit has 0, +1, or
    any larger move discount cannot change its per-tile movement cost while flat
    movement remains active.

    This is stronger than ordinary redundancy on cheap terrain: the promotion has zero
    movement-cost execution states on these UnitInfo types under their defining flat
    movement rule.

PROMOTION STATE REALLY IS INSTALLED
    CvUnitInfo::isPromotionValid normally contains a related semantic guard:

        if (isIgnoreTerrainCost() &&
            kPromotion.getMoveDiscountChange() != 0)
        {
            return false;
        }

    i.e. the inherited validator already understands that a movement-cost discount is
    meaningless when another unit rule bypasses ordinary terrain cost.

    There is no equivalent FlatMovementCost check. More importantly for the two current
    units, free promotions bypass normal validation immediately:

        if (getFreePromotions(ePromotion))
            return true;

    Mobility Cost is normally restricted by UnitCombat eligibility to mounted/armor
    families, while Airship and Drone are UNITCOMBAT_RECON. SAS therefore deliberately
    injects the promotion through the free-promotion path; this is not a promotion the
    recon units naturally selected despite failing an eligibility check.

CURRENT UI CONTRADICTION
    Actual-unit help independently reports explicit FlatMovementCost:

        if (pUnit->getUnitInfo().isFlatMovementCost())
            ... TXT_KEY_UNIT_FLAT_MOVEMENT

    and then reports any nonzero effective move discount:

        if (pUnit->getExtraMoveDiscount() != 0)
            ... TXT_KEY_PROMOTION_MOVE_DISCOUNT_TEXT

    Promotion help likewise describes Mobility Cost's iMoveDiscountChange normally.
    A current Airship/Drone can therefore advertise both flat movement and a movement
    discount even though the executor never combines them: flat movement exits first.

DESIGN / HISTORY PROOF
    The chronology resolves the remaining intent question cleanly.

    SAS practical 4676:

        bf2f1f436787c7e34669e156d443297974c23250
        2025-04-12
        broad XML/unit/collateral rework

    is where Airship was deliberately converted from its inherited air-domain model to
    a self-propelled DOMAIN_LAND unit. In the same Airship hunk:

        Domain:              DOMAIN_AIR -> DOMAIN_LAND
        bCanMoveImpassable:  0 -> 1
        bCanMoveAllTerrain:  0 -> 1
        bFlatMovementCost:   0 -> 1
        iMoves:              1 -> 2

    with the explicit comment that the change should make the Airship "walk" on all
    terrains. At this point the Airship had no free Mobility Cost promotion. So flat
    movement was already an established, intentional movement mechanic months before
    the dead promotion was added.

    SAS practical 4911:

        92fc29e26e9bfe40311c02af385f58383e09c157
        2025-07-27
        "rework the airship now a recon unit ..."

    preserves `bFlatMovementCost=1` and newly gives Airship free:

        PROMOTION_SENTRY
        PROMOTION_MOBILITY_COST
        PROMOTION_RETREAT1
        PROMOTION_RETREAT2

    The contemporaneous comment explains Mobility Cost as simulating the levitating
    Airship being able to travel day and night "freely regardless of terrain" at low
    energy cost. That is precisely the mechanical effect already enforced by flat
    movement, and the executor makes the newly added discount unable to change it.

    SAS practical 4912:

        9ac030d829b21dd2e0bbb128bb4ebf8bb6c4a144
        2025-07-27
        "add the robotic_recon_drone ..."

    introduces Robotic Recon Drone with the same deliberate pair:

        bFlatMovementCost = 1
        free PROMOTION_MOBILITY_COST

    Later documentation cleanup continues to describe Mobility Cost as a low-energy /
    continuous-movement property; no later mechanical consumer was added that gives the
    promotion an independent effect on flat-moving units.

    The history therefore distinguishes this from harmless stale XML. Flat movement
    came first as the real movement rule; Mobility Cost was later authored to reinforce
    an effect the engine had already made complete, but cannot stack with it.

ANCESTRY / ACTIVATION BOUNDARY
    BtS/Civ4CE, K-Mod and Base AdvC retain the same movement ordering: flat movement
    returns a fixed denominator before terrain movement discounts are evaluated.

    Their ordinary data does not place the SAS pair onto these current LAND recon
    units. Base AdvC's Airship remains an air-domain unit and Robotic Recon Drone does
    not exist. The unsupported/redundant combination is therefore activated by SAS
    content, not by a new current executor regression.

DETERMINISTIC CONSEQUENCE
    For either current Airship or Drone:

        flatMovementCost() == true
            -> movementCost() returns MOVE_DENOMINATOR
            -> getExtraMoveDiscount() is never queried
            -> Mobility Cost cannot reduce the step

    Removing only PROMOTION_MOBILITY_COST from the unit leaves every movementCost()
    result unchanged while the current flat-movement rule remains enabled.

    This is mechanically testable on flatland, hills, forests/jungle and any other
    traversable terrain: the unit pays the same one-denominator flat step before and
    after the promotion because all those states exit at the same first branch.

DEDUP BOUNDARY
    Whole-album semantic search found existing Mobility Cost material only in contexts
    where the promotion really works -- notably mounted/Hussar path-cost findings and
    current movement-heuristic controls. Those roots depend on ordinary terrain-cost
    execution reaching `getExtraMoveDiscount()`.

    F605 is the inverse state: explicit flat movement prevents that consumer from being
    reached at all. No previous finding owns this flat-movement / move-discount
    contradiction.

    Nearby roots remain independent:

    * F599 concerns MoveAllTerrain omitted from coarse AI reachability prefilters.
    * F603 concerns Gunship withdrawal on legal self-propelled water combat.
    * F604 concerns impossible withdrawal on OnlyDefensive Airship/Drone.
    * F605 concerns only the movement-cost effect of Mobility Cost after the same recon
      redesign; fixing any of the above does not make this discount execute.

REPAIR DIRECTION
    The conservative content repair is to remove free PROMOTION_MOBILITY_COST from
    Airship and Robotic Recon Drone while keeping their explicit FlatMovementCost.
    That preserves every current movementCost result and removes a mechanically dead /
    misleading promotion from the units and help text.

    Engine robustness can additionally make the semantic contract explicit in
    CvUnitInfo::isPromotionValid:

        if ((isIgnoreTerrainCost() || isFlatMovementCost()) &&
            kPromotion.getMoveDiscountChange() != 0)
        {
            return false;
        }

    This would protect ordinary/non-free promotion acquisition in future mod content,
    parallel to the existing IgnoreTerrainCost guard. It would not by itself reject an
    explicitly configured free promotion because free promotions intentionally return
    true before the normal validation checks; XML cleanup remains necessary for current
    Airship/Drone.

    The alternative -- removing FlatMovementCost so Mobility Cost becomes meaningful --
    would materially redesign their movement and contradict the older, explicit all-
    terrain flat-travel design. It is not the conservative audit fix.

REGRESSION CONTROLS
    * Airship and Drone must retain exactly their current per-step flat movement cost on
      all legal terrains after removing the dead promotion.
    * Ordinary mounted/armor users of PROMOTION_MOBILITY_COST must continue to receive
      their current terrain-cost reduction.
    * IgnoreTerrainCost promotion filtering must remain unchanged.
    * Free-promotion semantics generally must remain unchanged; do not move or remove the
      global free-promotion early return merely to repair these two XML entries.
    * F599/F603/F604 behavior is independent and must not be altered by this repair.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F605/KI#928 are CONFIRMED.
F606 / provisional KI#929 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F605's FlatMovementCost + free Mobility Cost dead-effect root;
- finish the remaining rare SAS UnitInfo/domain/action activation sieve, including
  bIgnoreTerrainCost and newly-land Airship action surfaces, but retain positive
  controls unless a concrete current invariant is contradicted;
- if that sieve closes, mechanically rebuild the under-covered authoritative CvUnit.cpp
  producer/consumer frontier rather than scanning source order;
- prefer target/owner/team/plot/state-identity changes and executor/predictor mismatches;
- whole-album semantic dedup before numbering;
- physically freeze WIP424 regardless before changing file/lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP423
===============================================================================



===============================================================================
CHECKPOINT C031-WIP424 -- QUEUE 007 CvUnit.cpp DEEP RE-AUDIT OPEN
===============================================================================
Date: 2026-09-06
Model: GPT-5.6-Sol
Authoritative current source snapshot:
    AdvCiv-SAS_light_source_20260906T174246.zip
Comparative local source snapshots:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(20260906-154339).zip
    Civ4CE-main(3).zip
Prior physical checkpoint:
    C031-WIP423
New finding in this append:
    F606 / provisional KI#929
Next finding number:
    F607 UNUSED / provisional KI#930
Queue state:
    007 CvGameCoreDLL/CvUnit.cpp DEEP RE-AUDIT OPEN
Source mutation state:
    NONE -- audit only

F606 / provisional KI#929
-------------------------
TITLE
    AdvC air-bomb city-defense damage still unconditionally applies the
    IgnoreBuildingDefense compensation ratio even though SAS deliberately removed
    IgnoreBuildingDefense from every current unit; ordinary Fighter/Bomber city
    bombardment is therefore amplified by defensive buildings that SAS now intends to
    affect those aircraft, and a building-only-defense city also exposes the stale
    ratio's zero-denominator boundary.

STATUS
    CONFIRMED current-live SAS content <-> inherited AdvC bombard-semantics
    contradiction. This is not a balance opinion about aircraft bomb rates. The
    calculation documents exactly why its building/culture ratio exists, and the SAS
    content deliberately invalidates that prerequisite for all current air bombers.

AUTHORITATIVE EXECUTOR CONTRACT
    Current CvUnit::airBombDefenseDamage says:

        /* <advc.004c> Same as in damageToBombardTarget except that
           IgnoreBuildingDefense doesn't have to be checked here b/c all air units
           have that */
        int iDefWithBuildings = kCity.getDefenseModifier(false);
        int iDefSansBuildings = kCity.getDefenseModifier(true);
        FAssertMsg(iDefSansBuildings > 0 || isHuman(), ...);
        return (airBombCurrRate() *
                scaled(iDefWithBuildings, iDefSansBuildings)).round();

    `airBomb()` applies that result directly:

        pCity->changeDefenseModifier(
                -std::max(0, airBombDefenseDamage(*pCity)));

    The ratio is therefore gameplay execution, not only an estimate or UI value.

    The sibling ordinary Bombard helper makes the missing semantic gate explicit:

        bool const bIgnore = ignoreBuildingDefense();
        ...
        if (!bIgnore)
            iBombardModifier -= pBombardCity->getBuildingBombardDefense();
        ...
        if (bIgnore && iDefSansBuildings > 0)
            rDamage *= scaled(iDefWithBuildings, iDefSansBuildings);

    Its comment explains that the ratio is needed so a unit which ignores defensive
    buildings reduces the visible total defense by enough to produce the intended
    reduction in the non-building defense it actually fights through.

CURRENT SAS DATA -- THE ASSUMPTION IS NOW FALSE FOR EVERY AIR BOMB
    Namespace-aware exhaustive census of current CIV4UnitInfos.xml:

        total UnitInfo entries                  = 131
        bIgnoreBuildingDefense = 1 entries      = 0

    Every current unit with positive iBombRate is an air-domain unit and has the flag
    disabled:

        UNIT_FIGHTER          iBombRate  8   bIgnoreBuildingDefense 0
        UNIT_JET_FIGHTER      iBombRate 12   bIgnoreBuildingDefense 0
        UNIT_BOMBER           iBombRate 16   bIgnoreBuildingDefense 0
        UNIT_STEALTH_BOMBER   iBombRate 20   bIgnoreBuildingDefense 0
        UNIT_GUIDED_MISSILE   iBombRate 16   bIgnoreBuildingDefense 0

    Base AdvC 1.14 and the local BtS/Civ4CE source have the same five positive-rate
    air-bomb types with bIgnoreBuildingDefense=1. Their data therefore satisfies the
    assumption written into AdvC's helper; current SAS data does not.

CURRENT SAS DESIGN INTENT IS EXPLICIT
    The current Main Changes Guide states under Military - Common:

        City defenses affect all units, including gunpowder and later.
        See bIgnoreBuildingDefense in CIV4UnitInfos.xml.

    This agrees with the exhaustive current XML state and with combat execution:
    maxCombatStr uses the attacker's `ignoreBuildingDefense()` when asking the target
    plot for its defensive modifier. SAS's 0 values therefore have a real intended
    combat meaning; they are not unused documentation flags.

DETERMINISTIC LIVE CONSEQUENCE
    The stale ratio grows whenever building defense raises the city's displayed
    defense above its non-building/cultural defense.

    Simple current fixture using Walls:

        building / raised defense = 50
        cultural defense          = 20
        Bomber iBombRate          = 16
        Bomber IgnoreBuildingDefense = false

    Current air-bomb helper nevertheless calculates:

        16 * 50 / 20 = 40

    and reduces total city defense by 40 instead of the nominal 16 before clamping at
    the city's remaining defense. The extra 24 points arise solely from the
    compensation mechanism for ignoring a building effect that this SAS Bomber no
    longer ignores.

    Castle-style fixture:

        displayed defense with building = 100
        non-building defense             = 60
        Bomber iBombRate                 = 16

    gives approximately:

        16 * 100 / 60 = 26.67 -> 27

    rather than the unscaled 16 baseline.

    The important defect is the conditionality, not either chosen example: as the
    building/non-building defense ratio increases, current SAS air-bomb damage is
    increased because of a property every current air bomber explicitly lacks.

ZERO-NONBUILDING-DEFENSE BOUNDARY -- SAME ROOT
    Current CvCity::isBombardable can legally expose a human Air Bomb mission when a
    city has positive building defense but zero non-building defense. For example,
    raised defense from Walls can make `getDefenseModifier(false) > 0` while
    `getDefenseModifier(true) == 0`.

    `airBombDefenseDamage` then constructs `scaled(iDefWithBuildings, 0)`. The ordinary
    `damageToBombardTarget` sibling explicitly protects this ratio with
    `iDefSansBuildings > 0`; the air helper omitted that guard only under its historical
    "all air units ignore buildings" assumption. Do not promote this as another
    finding: it is an edge consequence of the same stale unconditional ratio.

HISTORY / INTRODUCTION BOUNDARY
    AdvC practical 1262:

        c01c07e94be3b86fa524a1cb14cff918d0996ae3

    replaced BtS's direct:

        changeDefenseModifier(-airBombCurrRate())

    with the building/non-building ratio. The introducing source comment already says
    that IgnoreBuildingDefense need not be checked because all air units have it.

    In the same practical, ordinary siege Bombard was changed to apply the ratio only
    when `bIgnore` is true. Thus the asymmetry was deliberate and data-dependent, not a
    generic rule that every air mission should scale by defensive buildings.

    Base AdvC still satisfies that data prerequisite. SAS practical 4676
    (bf2f1f436787c7e34669e156d443297974c23250, 2025-04-12) broadly changed inherited
    bIgnoreBuildingDefense=1 military entries to 0 as part of its unit/XML rework;
    later SAS edits completed/retained the all-zero current state. The current guide
    subsequently documents that city defenses affect all units.

    SAS therefore changed the producer/configuration contract while inheriting the
    old AdvC consumer unchanged.

UI / OBSERVABILITY BREADTH
    `CvGameTextMgr::getAirBombPlotHelp` calls each eligible selected aircraft's same
    `airBombDefenseDamage()` helper when forecasting city-defense reduction. Thus the
    UI and execution agree with each other numerically, but both agree on the stale
    over-scaled result. The earlier action-help reconciliation control does not make
    the calculation semantically correct; it only established executor/help symmetry.

    This remains independent of F071/KI#393, which fixed which selected aircraft are
    included in a multi-unit Air Bomb preview. F071 can choose the correct eligible
    set while every included unit still contributes the wrong F606 damage amount.

SASGAMERECORD BREADTH -- SAME STALE ASSUMPTION REPEATED
    The current AIR city-bombard logging call is:

        logSASGameRecordCityBombard(this, pCity, "AIR",
                airBombCurrRate(), true, ...);

    The `true` increments `iIgnoreBuildingDefenseActions` in the GameRecord sequence.
    It was added in practical 6092 and retains the historical AdvC assumption even
    though every current SAS air bomber has `ignoreBuildingDefense()==false`.

    This is logging consequence breadth of the same semantic contract, not a separate
    root. A repair to air-bomb gameplay should also stop hardcoding this metadata and
    record the acting unit's actual flag (or whatever deliberately chosen post-fix Air
    Bomb semantic is intended).

DEDUP BOUNDARY
    Whole-album semantic search found several Air Bomb roots/controls, but none owns
    this calculation contract:

    * F071/KI#393: multi-unit Bombard/Air Bomb preview counted target-ineligible units.
    * F072/KI#394: Air Bomb best-unit selection could choose a target-ineligible
      aircraft and abort a valid group mission.
    * later action-help controls established that the chosen helper and executor use
      the same `airBombDefenseDamage` calculation.
    * an earlier AI_getBombardTurns control discussed synthetic global air support and
      IgnoreBuildingDefense as an approximation, not actual Air Bomb city damage.
    * F536/KI#859 independently observed that current SAS has no IgnoreBuildingDefense
      units while auditing obsolete-building valuation.

    None of those findings tests whether AdvC's unconditional air-bomb ratio is still
    legal after SAS removes the flag from the actual bomber. Fixing any of them leaves
    F606 unchanged, and fixing F606 does not repair their mission-selection roots.

CONSERVATIVE REPAIR BOUNDARY
    The minimum semantics-preserving repair is to stop applying AdvC's compensation
    ratio unless the acting unit really ignores building defense, and to retain the
    same positive-denominator guard already used by `damageToBombardTarget`, e.g. in
    conceptual form:

        scaled rDamage = airBombCurrRate();
        if (ignoreBuildingDefense() && iDefSansBuildings > 0)
            rDamage *= scaled(iDefWithBuildings, iDefSansBuildings);
        return rDamage.round();

    Under current SAS XML (`ignoreBuildingDefense()==false` for all units), this makes
    normal Air Bomb city-defense damage use its configured bomb rate rather than an
    immunity-compensation multiplier.

    There is a separate design choice about BuildingBombardDefense. Ordinary
    `damageToBombardTarget` also applies a city's BombardDefense when the attacker does
    not ignore buildings. Stock/AdvC air bombers never exercised that branch because
    they all ignored buildings. If SAS intends the newly non-ignoring aircraft to obey
    Walls/Castle BombardDefense as well, `airBombDefenseDamage` can be generalized to
    mirror the full sibling formula. That would be a deliberate additional gameplay
    rule, not something this audit should silently bundle into the minimal F606 fix.

    GameRecord should likewise pass the real semantic value rather than literal
    `true` once the desired Air Bomb rule is settled.

REGRESSION CONTROLS
    * On a city with no building contribution (`defWithBuildings ==
      defSansBuildings`), current positive-rate aircraft must keep the same configured
      Air Bomb reduction after the fix.
    * Current SAS non-ignoring aircraft must no longer gain extra bombard damage merely
      because Walls/Castle raise `defWithBuildings / defSansBuildings`.
    * If a future/modded air bomber has IgnoreBuildingDefense=true, the historical AdvC
      compensation ratio should remain available with the denominator guard.
    * Human Air Bomb against positive building defense / zero non-building defense must
      not feed a zero denominator into the ratio.
    * F071 target-eligibility filtering and F072 mission selection must remain intact.
    * GameRecord AIR rows must not claim building-defense immunity inconsistent with
      the chosen gameplay semantics.

CONTROLS CLOSED IN THIS TRANCHE
    * Reused cargo-vector suspicion in `isBetterDefenderThan` is false: `getCargoUnits`
      begins by clearing the supplied vector, so the second defender's cargo valuation
      is not contaminated by the first defender's cargo.
    * Explorer city capture is deliberate SAS design: practical 4377's recon rework
      explicitly comments that Explorer can conquer opportunistic weak/barbarian
      cities; its bNoCapture relaxation is not an audit root.
    * Scout IgnoreTerrainCost remains an explicit recon redesign capability with no
      contradictory executor found in this activation sieve.
    * `iCollateralDamageLimit=200` remains explicit practical-4676 collateral design;
      its above-100 value was not promoted merely for differing from inherited BtS
      ranges.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F606/KI#929 are CONFIRMED.
F607 / provisional KI#930 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F606's stale unconditional IgnoreBuildingDefense Air Bomb scaling root,
  including its zero-denominator and GameRecord breadth;
- continue the mechanically rebuilt under-covered authoritative CvUnit.cpp frontier
  outside F593-F606 rather than lingering on already-classified recon XML states;
- prioritize executor/predictor, target/owner/team/plot identity, damage/cap state,
  mission legality, conversion, capture and rare action-state consumers;
- use Base AdvC/K-Mod/BtS ancestry only to establish activation/regression boundaries;
- whole-album semantic dedup before numbering;
- physically freeze WIP425 regardless before changing file/lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.


===============================================================================
CHECKPOINT C031-WIP425 -- QUEUE 007 CvUnit.cpp DEEP RE-AUDIT OPEN
===============================================================================
Date: 2026-09-06
Model: GPT-5.6-Sol
Authoritative current source snapshot:
    AdvCiv-SAS_light_source_20260906T174246.zip
Comparative local source snapshots:
    AdvCiv-1.14(5).zip
    Civ4-K-Mod-master-1.46(20260906-154339).zip
    Civ4CE-main(3).zip
Prior physical checkpoint:
    C031-WIP424
New finding in this append:
    F607 / provisional KI#930
Next finding number:
    F608 UNUSED / provisional KI#931
Queue state:
    007 CvGameCoreDLL/CvUnit.cpp DEEP RE-AUDIT OPEN
Source mutation state:
    NONE -- audit only

F607 / provisional KI#930
-------------------------
TITLE
    Interceptors receive no combat experience at all for intercepting a normal
    Paratrooper: resolveAirCombat gates both interceptor-XP paths on the paradropper's
    own interception-derived retaliation damage, which is zero for the shipped
    Paratrooper even when the interceptor damages or kills it.

STATUS
    CONFIRMED current-live inherited BtS/K-Mod/Base-AdvC combat-state bug. The
    paradrop interception path is shipped and reachable in current SAS, and the XP
    suppression follows deterministically from current unit data. This is not a
    tuning disagreement about how much XP interception should grant; the existing
    function already awards ordinary interception XP, but an unrelated damage-capacity
    variable suppresses it only for the non-AIR attacker reuse.

CURRENT PARATROOPER FIXTURE
    Current SAS CIV4UnitInfos.xml:

        UNIT_PARATROOPER
        Domain                    DOMAIN_LAND
        UnitCombat                UNITCOMBAT_GUN
        iDropRange                5
        iCombat                   22
        iInterceptionProbability  0
        iEvasionProbability       25
        iXPValueAttack            4
        iXPValueDefense           2
        FreePromotions            none

    Current positive-interception defenders include Machine Gun, Mechanized Infantry,
    SAM Infantry, Mobile SAM, Destroyer, Fighter and Jet Fighter. Thus ordinary
    Paratrooper interception is not a theoretical unsupported state.

AUTHORITATIVE EXECUTION CHAIN
    CvUnit::paradrop moves the unit to the target, then calls:

        if (interceptTest(kPlot, ...))
            return true;

    interceptTest selects bestInterceptor and, on a successful interception roll,
    calls:

        fightInterceptor(kPlot, false);

    fightInterceptor routes the combat through updateAirCombat. That function is
    explicitly shared between normal air missions and paradrops:

        FAssert(getDomainType() == DOMAIN_AIR || getDropRange() > 0);
        ...
        if (DOMAIN_AIR != getDomainType())
            kAirMission.setMissionType(MISSION_PARADROP);
        ...
        resolveAirCombat(pInterceptor, pPlot, kAirMission);

    The resulting XP behavior therefore belongs to real Paradrop execution, not a
    display-only or dead helper.

WRONG STATE VARIABLE IN resolveAirCombat
    In the non-air-v-air branch, current code computes:

        iOurRoundDamage =
            pInterceptor->currInterceptionProbability() * MAX_INTERCEPTION_DAMAGE / 100;

        iTheirRoundDamage =
            currInterceptionProbability() * MAX_INTERCEPTION_DAMAGE / 100;

        if (getDomainType() == DOMAIN_AIR)
            iTheirRoundDamage = max(MIN_INTERCEPTION_DAMAGE, iTheirRoundDamage);

    `iOurRoundDamage` is damage the interceptor can inflict on the mission unit.
    `iTheirRoundDamage` is the mission unit's potential retaliation damage.

    For the current Paratrooper:

        getDomainType()                  = DOMAIN_LAND
        currInterceptionProbability()    = 0
        therefore iTheirRoundDamage      = 0

    while a real interceptor generally has iOurRoundDamage > 0 and can damage or kill
    the Paratrooper.

    Yet both interceptor-XP branches are conditioned on the *Paratrooper's*
    retaliation variable:

        if (isDead())
        {
            if (iTheirRoundDamage > 0)
                pInterceptor->changeExperience(...);
        }
        ...
        else if (iOurDamage > 0)
        {
            if (iTheirRoundDamage > 0)
                pInterceptor->changeExperience(EXPERIENCE_FROM_WITHDRAWL, ...);
        }

    Therefore an unpromoted current Paratrooper makes both guards false.

DETERMINISTIC CONSEQUENCES
    Successful interception where the Paratrooper survives:

        interceptor inflicts iOurDamage > 0
        Paratrooper iTheirRoundDamage == 0
        -> surviving-interception XP branch reached
        -> inner guard false
        -> interceptor gains 0 XP

    Successful interception where the interceptor kills the Paratrooper:

        isDead() == true for Paratrooper
        Paratrooper iTheirRoundDamage == 0
        -> kill-XP branch reached
        -> inner guard false
        -> interceptor gains 0 XP

    Thus even a Mobile SAM or Fighter that outright destroys the paradropping unit is
    denied the experience that the existing interception-combat code is otherwise
    structured to award.

WHY THIS IS NOT A GENERIC "NO RETALIATION, NO XP" RULE
    The same function already disproves that interpretation for ordinary aircraft.
    When the mission unit is DOMAIN_AIR, iTheirRoundDamage is forced to at least
    MIN_INTERCEPTION_DAMAGE even when its own interception probability is 0.

    Moreover, land/sea interceptors cannot actually be damaged in the attacker's
    successful-round branch because defender damage is applied only when:

        DOMAIN_AIR == pInterceptor->getDomainType()

    Nevertheless such ground/naval interceptors still pass the XP guard against an
    ordinary AIR attacker because iTheirRoundDamage was forced positive. The guard
    therefore does not reliably encode "interceptor was at risk" or "both sides could
    deal damage". It is an AIR-attacker-era proxy that breaks for the explicitly
    supported non-AIR Paradrop path.

PROMOTION BOUNDARY
    PROMOTION_AIR_INTERCEPTION1 is currently legal for UNITCOMBAT_GUN and can add a
    small positive interception value to a promoted Paratrooper. Such a specially
    promoted unit can make iTheirRoundDamage positive and thereby restore interceptor
    XP accidentally.

    That does not close the finding; it sharpens it. Whether the interceptor gets XP
    for damaging/killing the same paradropping unit can depend on the *target's own air
    interception promotion*, even though that value is unrelated to whether the
    interceptor successfully dealt damage. The ordinary shipped Paratrooper has no
    such free promotion and reproduces the zero-XP state directly.

ANCESTRY / ACTIVATION BOUNDARY
    The same core structure exists in local BtS/Civ4CE, K-Mod 1.46 and Base AdvC
    1.14. BtS already routes paradrop interception through updateAirCombat and already
    permits that function for `DOMAIN_AIR || getDropRange() > 0`; this is therefore
    an inherited Civ4/BtS-era bug rather than a SAS-authored regression.

    AdvC practical 2784 / commit

        160fa499a896cf595f74bcbde68b7ce41bedc466

    substantially improved Paradrop/Air mission interception UI and group execution,
    but did not change this resolveAirCombat XP contract. Base AdvC and current SAS
    therefore retain the old mismatch.

    Current SAS keeps an ordinary active Paratrooper with DropRange 5 and positive
    evasion, so the inherited defect remains live.

DEDUP BOUNDARY
    Whole-album semantic search found prior Paradrop interception-help and mission-
    eligibility findings, plus generic interception/air-defense analysis. None owns
    combat experience inside resolveAirCombat, nor the use of iTheirRoundDamage as an
    interceptor-XP gate.

    Fixing the earlier Paradrop hover/selection roots does not change XP after a real
    interception. Fixing F607 does not change which interceptor is shown/selected or
    whether the mission is legal.

CONSERVATIVE REPAIR BOUNDARY
    The safe semantic objective is narrow: when an interceptor actually damages or
    kills a paradropping unit, do not condition the interceptor's already-existing XP
    award on the paradropper having positive interception-derived retaliation damage.

    For current ordinary AIR attackers the existing `iTheirRoundDamage > 0` condition
    is effectively always true because of MIN_INTERCEPTION_DAMAGE, so removing or
    replacing that stale proxy for the interceptor-XP branches can be made behavior-
    neutral for normal air interception while fixing Paradrop.

    The exact patch should be chosen against the desired XP rule rather than blindly
    swapping variables. Strong controls are:

    * killed mission unit: interceptor receives the existing kill XP calculation when
      it was the combatant that killed the unit;
    * surviving mission unit with iOurDamage > 0: interceptor receives the existing
      EXPERIENCE_FROM_WITHDRAWL-style award;
    * ordinary AIR-vs-AIR and AIR-vs-ground interception XP remains unchanged;
    * a non-intercepted Paradrop still gains no combat XP;
    * do not alter interception odds, damage, evasion or MadeInterception state.

CONTROLS CLOSED IN THIS TRANCHE
    * `flankingStrikeCombat` recomputes iFlankedDefenderOdds but uses the main
      defender's iDefenderOdds for the flanking-success roll in BtS, K-Mod, Base AdvC
      and SAS. The unused per-target odds value is suspicious-looking but inherited
      design, not enough by itself for a new root.
    * `rangeStrike` remains unavailable to current SAS non-air units with the required
      air-combat/range combination; F600 already records its wrong-plot sibling as
      dormant robustness breadth.
    * `attackForDamage` has no current gameplay caller outside its Python exposure in
      this source snapshot; no current-live defect was promoted from it.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F607/KI#930 are CONFIRMED.
F608 / provisional KI#931 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- exclude F607's Paradrop-interceptor XP gate root;
- continue the rebuilt authoritative CvUnit.cpp frontier under F608;
- prioritize under-covered executor/predictor pairs, especially damage caps,
  conversion/capture state, mission-state bookkeeping and target/plot identity;
- rangeStrike/attackForDamage are currently classified as dormant/non-live controls;
- whole-album semantic dedup before numbering;
- physically freeze WIP426 regardless before changing file/lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP425
===============================================================================

===============================================================================
C031-WIP426 CONTINUATION — F608 REMAINS UNUSED; ACTIVATION / ANIMATION / SEA-
PILLAGE LIFETIME CONTROLS CLOSED
===============================================================================

STATUS AT THIS STONE
--------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F607/KI#930 remain CONFIRMED.
F608 / provisional KI#931 remains UNUSED.
Actual AdvCiv-SAS/Base AdvCiv/K-Mod/Civ4CE gameplay source remains untouched.

PURPOSE OF THIS TRANCHE
-----------------------
Continue from WIP425 without forcing a finding. The pass first completed the rare
SAS UnitInfo activation sieve, then rebuilt low-album-coverage CvUnit.cpp bodies and
checked several source-level asymmetries that looked stronger than their eventual
runtime effect.

CONTROL 1 — EXPLORER IgnoreTerrainCost × free Woodsman II: PARTIALLY REDUNDANT,
NOT F608
-------------------------------------------------------------------------------
Current SAS Explorer has `bIgnoreTerrainCost=1` and free Woodsman I + Woodsman II.
`CvPlot::movementCost()` establishes the interaction mechanically:

    if (kUnit.flatMovementCost())
        return GC.getMOVE_DENOMINATOR();

    ...

    if (kUnit.ignoreTerrainCost())
        iRegularCost = 1;

    bool const bHasTerrainCost = (iRegularCost > 1);
    if (bHasTerrainCost && (... terrain/feature double-move ...))
        iRegularCost /= 2;

Thus Woodsman II's Forest/Jungle double-move component cannot further reduce an
Explorer's already-one-denominator terrain movement. The promotion is not wholly
dead, however: its Forest/Jungle attack/defense modifiers remain live.

History also argues against calling the overlap a bug. SAS practical 4374 added
Woodsman I as an Explorer identity/navigation bonus; practical 4377's broader
Explorer rework deliberately added Woodsman II because the Explorer should be more
in control of forest/jungle environments and because those combat bonuses do not
make city capture too strong. Later SAS recon documentation independently gives
ground exploration units IgnoreTerrainCost.

Classification: intentional overlapping mechanics with one redundant sub-effect,
not a hard content↔executor contradiction.

CONTROL 2 — flankingStrikeCombat dead per-target values: INHERITED DESIGN / DEAD
COMPUTATION, NOT F608
-------------------------------------------------------------------------------
`flankingStrikeCombat()` calls `getDefenderCombatValues(*pLoopUnit, ...)` for each
potential flanked unit and computes per-target strength/odds/damage values, but the
actual flanking hit roll and damage use the primary battle's caller-supplied
`iDefenderOdds`/`iDefenderDamage` instead.

The same behavior exists through BtS/Civ4CE -> K-Mod -> Base AdvC -> SAS. The
per-target locals are stale/unnecessary computation, but the primary-battle escape
odds and primary defender round damage are the historical flanking formula. No SAS
activation changes that contract. This is therefore code-cleanup debt, not F608.

CONTROL 3 — planBattle computeWaveSize attacker fallback typo: ALGEBRAICALLY DEAD,
NOT F608
-------------------------------------------------------------------------------
Current inherited helper contains:

    aiDesiredSize[BATTLE_UNIT_DEFENDER] =
        aiDesiredSize[BATTLE_UNIT_DEFENDER] <= 0 ? iDefenderMax : ...;
    aiDesiredSize[BATTLE_UNIT_ATTACKER] =
        aiDesiredSize[BATTLE_UNIT_ATTACKER] <= 0 ? iDefenderMax : ...;

The attacker branch superficially appears to want `iAttackerMax`. However the
returned value is:

    min(min(attackerDesired, iAttackerMax),
        min(defenderDesired, iDefenderMax))

When attackerDesired uses the wrong `iDefenderMax`, the final expression is still
capped by both `iAttackerMax` and `iDefenderMax`; replacing that intermediate value
with `iAttackerMax` cannot change the result. This is a typo-looking but
behavior-neutral line.

CONTROL 4 — planBattle BATTLE_TIME_RANGED wrong-side accumulation: REAL BOOKKEEPING
ASYMMETRY, BUT NO PROVEN LIVE CONSEQUENCE
-------------------------------------------------------------------------------
K-Mod's planBattle rewrite records positive combat-log damage during a ranged phase
into the defender's `BATTLE_TIME_RANGED` slot. In the symmetric branch where the
defender damages the attacker it correctly increments `iAttackerDamage`, but then
again executes:

    kBattle.addDamage(BATTLE_UNIT_DEFENDER,
            BATTLE_TIME_RANGED, combat_log[i]);

rather than updating the attacker slot with the corresponding positive damage.
`CvBattleDefinition::addDamage()` is a plain signed integer addition, so this is a
real wrong-side bookkeeping asymmetry inherited by Base AdvC and SAS.

However, the same K-Mod code explicitly comments that this ranged-time damage is
probably not used, and current `resolveCombat()` repeats that `BATTLE_TIME_RANGED`
damage is set inside planBattle "not that it actually does anything". The actual
battle-round choreography is driven by the separately built `CvBattleRound` list,
and final gameplay damage was already resolved before animation planning.

Without a demonstrated EXE/animation consumer that makes the wrong slot observable,
this remains dormant animation-metadata debt rather than a current-live F608.

CONTROL 5 — sea-pillage withdrawal suppression lifetime: SAFE / SYNCHRONOUS OUTCOME,
NOT F608
-------------------------------------------------------------------------------
When a sea pillager is intercepted, `pillage()` says the pillager is "really the
defender" and temporarily removes all withdrawal:

    int iWithdrawal = withdrawalProbability();
    changeExtraWithdrawal(-iWithdrawal);
    attack(pInterceptor->plot(), false, NULL, true);
    changeExtraWithdrawal(iWithdrawal);

This initially looked unsafe because visible combat animation is asynchronous.
Tracing the lifecycle closes it: `attack()` immediately calls `updateCombat()`, and
for the real defender `updateCombat()` immediately enters `resolveCombat()` on the
initial call. All combat RNG, damage, death and withdrawal decisions are therefore
resolved before `attack()` returns. What remains deferred is the animation timer and
cleanup, not the withdrawal outcome.

The modifier restoration is therefore safe for its intended purpose.

RARE SAS UNITINFO RANGE/COMBINATION SIEVE — CURRENT CLOSED SET
--------------------------------------------------------------
A namespace-correct current UnitInfo census was compared against Base AdvC scalar
ranges and rare boolean combinations. Previously-owned activations remained the
meaningful outliers:

* `bCanMoveAllTerrain=1` -> Airship, Robotic Recon Drone, Gunship (F599/F603 family);
* `bFlatMovementCost=1` -> Airship, Robotic Recon Drone (F605);
* `iCombatLimit=110` -> Ottoman Janissary (F602);
* `iCollateralDamageLimit=200` -> deliberate SAS collateral redesign; iterator/death
  mutation control already closed;
* negative Warrior city modifiers -> current signed combat consumers examined so far
  handle them;
* new animal/hill combat values and high late-game combat/power values are ordinary
  signed/scalar balance data in their inspected consumers.

No new hard executor-domain violation was promoted from the remaining range census.

LOW-COVERAGE SEMANTIC-DIFF FRONTIER
-----------------------------------
A mechanical CvUnit.cpp function-body pass, weighted by whole-album literal coverage
and SAS-vs-Base-AdvC semantic difference, was rebuilt after excluding already-owned
movement/conversion/combat roots. Several low-coverage changed bodies (`stealPlans`,
`spreadCorporation`, `testSpyIntercepted`, `greatWork`, `showSiegeTower`) differ only
through current SAS GameRecord logging/performance caching or already-understood
behavior. `nukeInterceptionChance` is semantically Base AdvC-identical apart from
signature formatting.

This pass therefore did not justify spending F608 merely because a function was
under-covered.

DURABLE STATE
-------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` remains OPEN.
F593/KI#916 through F607/KI#930 are CONFIRMED.
F608 / provisional KI#931 is UNUSED.
Actual repository/gameplay source remains untouched.

Exact continuation cursor:
- start from this WIP426 stone with F608 unused;
- exclude Explorer/Woodsman overlap, inherited flanking formula, computeWaveSize
  algebraic typo, unproven BATTLE_TIME_RANGED animation metadata, and sea-pillage
  temporary-withdrawal lifetime as closed controls;
- continue authoritative CvUnit.cpp state/identity audit rather than source order;
- prioritize state that crosses synchronous/deferred boundaries, unit/owner/team
  identity, delayed death/capture/convert bookkeeping, and predictor/executor pairs;
- whole-album semantic dedup before numbering;
- promote F608 only for a distinct current-live contradiction;
- physically freeze WIP427 regardless before changing queue/file lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP426
===============================================================================

===============================================================================
C031-WIP427 — QUEUE 007 CLOSURE — FINAL RECONCILIATION / SEMANTIC SATURATION
===============================================================================

STATUS
------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` is CLOSED for the planned deep-method re-audit.
The protected deep-method cursor advances to Queue 008 `CvGameCoreDLL/CvGame.cpp`.
F593 / KI#916 through F607 / KI#930 are the confirmed new-root span from this
Queue-007 re-audit.
F608 / provisional KI#931 remains UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository/gameplay source files modified.

POST-WIP426 RECONCILIATION — NO STRANDED CURRENT-LIVE CANDIDATE
---------------------------------------------------------------
The final closure pass reconciled both the current-SAS-vs-Base changed-body frontier and
the late inherited/activation controls specifically to ensure that no candidate was being
left in chat or WIP prose merely because F608 had remained unused.

No independent current-live F608 survives.

Late controls are classified as follows:

* Explorer `IgnoreTerrainCost` + free Woodsman II has a redundant double-move sub-effect,
  but Woodsman II's forest/jungle combat modifiers remain live and the SAS history supports
  the overlap as deliberate recon identity rather than a promised extra movement reduction.
* `flankingStrikeCombat` computes stale per-flanked-unit combat values but intentionally uses
  the primary battle's escape odds / round damage in the inherited flanking formula. The
  dead locals are cleanup debt, not a new gameplay root.
* `computeWaveSize` uses `iDefenderMax` as the attacker's <=0 fallback, but the final nested
  min is already bounded by both attacker and defender maxima; the typo-looking substitution
  is algebraically unable to change the returned wave size.
* K-Mod `planBattle` writes defender-caused ranged-phase damage into the defender's
  `BATTLE_TIME_RANGED` slot. This is a genuine wrong-side animation-metadata asymmetry, but
  gameplay damage is already resolved and both K-Mod/current code explicitly describe the
  ranged-time damage field as effectively unused. No observable current EXE consequence was
  established, so it remains dormant bookkeeping debt.
* Sea-pillage interception temporarily removes withdrawal and restores it directly after
  `attack()`. The combat outcome itself resolves synchronously inside the initial
  `attack -> updateCombat -> resolveCombat` call; only animation cleanup is deferred, so the
  temporary-state lifetime is correct.
* The lethal-collateral iterator concern closes because lethal `setDamage` uses delayed death
  during traversal; the mutable unit-list traversal itself is deletion-safe where immediate
  removal is required.
* Robotic Recon Drone fortification remained design-ambiguous (the contrary wording belongs
  to an art-definition rationale while gameplay data deliberately permits defensive bonuses),
  and was correctly not promoted.

FINAL CURRENT-SAS -> BASE-ADVC CHANGED-BODY RECONCILIATION
----------------------------------------------------------
A fresh brace-aware function inventory found the finite current `CvUnit` body delta surface.
The low-coverage functions were reopened directly rather than inferred safe from name counts.

The changed members with zero earlier literal album-name coverage close as follows:

* `addAttackSuccessMessages`, `addDefenseSuccessMessages`, `addWithdrawalMessages` — cached
  GREEN/RED color IDs only; message semantics unchanged.
* `getHealthBarModifier` — cached HEALTH_BAR_WIDTH define only.
* `getNameKeyNoGG` — the mechanical SAS enum rename `UNITAI_GENERAL -> UNITAI_GREAT_GENERAL`;
  the current Great General XML/AI enum was renamed consistently throughout the tree.
* `maxXPValue` — cached animal/barbarian XP-limit defines only.
* `pillageImprovement` — cached message colors only in the current-vs-Base delta.
* `shouldShowEnemyGlow` — current early null-plot validation is the already-owned KI#524
  failed-unit-init/EXE-glow crash repair, not a new Queue-007 root.

The next-lowest-coverage changed bodies likewise reconcile cleanly:

* `airStrike` adds current GameRecord capture and color caching around inherited gameplay;
  the substantive wrong-target-plot damage root is already F600.
* `collectBlockadeGold`, `greatWork`, `spreadCorporation`, `stealPlans` and
  `testSpyIntercepted` add observational GameRecord state and/or cached constants without
  changing the authoritative gameplay mutation in the inspected delta.
* `showSiegeTower` caches its define.
* `rangeStrike` / `attackForDamage` remain the late dormant/non-live controls documented in
  WIP425-WIP426.
* `flankingStrikeCombat` is classified above.

The more heavily represented changed bodies (`kill`, `setXY`, `resolveCombat`, `updateAirCombat`,
upgrade/scrap/gift, espionage, nuke/bombard/pillage, read/write and mission actions) were already
exercised repeatedly by the WIP404-WIP426 lifecycle, combat, movement, territory, serialization,
rare-field and producer/consumer passes. Their current instrumentation/fixes do not leave an
unowned low-coverage semantic delta.

QUEUE 007 METHOD COVERAGE AT CLOSURE
------------------------------------
The stronger Queue-007 re-audit has now exercised, at minimum:

* opening reconciliation of original File-007 findings and currently fixed SAS roots;
* current SAS vs Base AdvC semantic member-body inventory and final low-coverage reconciliation;
* K-Mod / Civ4CE / BtS ancestry for inherited combat/movement/mission assumptions;
* init/finalize/kill/delayed-death, replacement/convert/gift/upgrade and FFreeList identity;
* group/plot/area membership, setXY, cargo/transport and territory/domain legality;
* normal combat resolution, defender selection, strength/damage/odds, withdrawal, first strikes,
  collateral/flanking, paradrop and air interception;
* air strike, air bombard, range-strike and current SAS air->land/all-terrain redesign contracts;
* capture/advance/no-capture, hidden-nationality cargo and invasion-transport semantics;
* promotion validity/effects, XP and Great-General state;
* pillage/blockade/sea-patrol, movement/action economy and mission-state lifetime;
* scrap preservation, action execution and no-production/unsupported-caller controls;
* read/write serialization and derived/runtime state inherited from the earlier whole-file pass;
* SAS-activated UnitInfo boolean combinations and scalar ranges outside Base AdvC shipped domains;
* rare content->executor pairings, including all-terrain, flat movement, >100 combat limit,
  defensive-only withdrawal, building-defense air bombard and lethal collateral;
* under-covered inherited animation/executor bodies (`planBattle`, `computeWaveSize`, flanking,
  range/attack-for-damage) and explicit false-positive closure;
* mandatory whole-album semantic dedup before numbering late candidates.

The late stage continued to find strong independent defects through F603-F607, showing that the
sieves still had discovery power after the initial body/lifecycle passes. WIP426 plus the final
changed-body reconciliation above leave no independent current-live root requiring F608.

Leaving F608 unused is therefore the correct Queue-007 saturation result.

QUEUE 007 RESULT
----------------
The protected `CvUnit.cpp` deep-method re-audit is CLOSED with 15 confirmed new roots:
F593/KI#916 through F607/KI#930 inclusive.

The final roots demonstrate the activation breadth reached by the pass:

* F599: all-terrain legal destinations rejected by inherited coarse AI area reachability;
* F600: air/range combat damage target context accidentally changed from defender plot to
  attacker plot by an AdvC plot-access cleanup;
* F601: SAS-added Privateer DOMAIN_LAND cargo slots have zero legal current cargo because the
  inherited hidden-nationality transport rule rejects every ordinary land unit;
* F602: Janissary `iCombatLimit=110` is lethal at 100 in the executor but modeled as 110 HP by
  combat-odds predictors;
* F603: self-propelled Gunship water combat loses withdrawal through the inherited
  "land-on-water == transported amphibious attacker" assumption;
* F604: Airship/Robotic Recon Drone advertise withdrawal while `bOnlyDefensive` prevents them
  from ever attacking and therefore ever executing withdrawal;
* F605: free Mobility Cost is mechanically bypassed on the same units by flat movement;
* F606: air-bomb defense scaling still compensates for IgnoreBuildingDefense although SAS
  deliberately removed that flag from all current air bombers;
* F607: Paradrop interception can damage or kill the Paratrooper yet award the interceptor 0 XP
  because XP is gated on the paradropper's own retaliation-damage variable.

These late roots are distinct from one another and from the earlier Queue-007 findings. F608 is
not needed to describe any remaining current-live `CvUnit.cpp` behavior established by this audit.

CURRENT STONE AFTER C031-WIP427
-------------------------------
Queue 007 `CvGameCoreDLL/CvUnit.cpp` — DEEP-METHOD RE-AUDIT CLOSED.
Protected deep-method cursor: Queue 008 `CvGameCoreDLL/CvGame.cpp` NEXT.
F593 / KI#916 through F607 / KI#930 CONFIRMED across Queue 007.
F608 / provisional KI#931 UNUSED.
Latest physical checkpoint: C031-WIP427.
No repository/gameplay source modified.

NEXT
----
Advance to protected deep-method re-audit Queue 008 `CvGameCoreDLL/CvGame.cpp` using the same
stronger checkpoint/WIP method. Treat the earlier original Queue-008 review (ten findings) as a
baseline requiring reconciliation against current SAS rather than as present-day closure.

Begin with a fresh current-SAS-vs-Base-AdvC semantic body map and reconcile the old Queue-008
findings/controls. Then prioritize game-global state transitions and owner/team/player identity,
turn/year/calendar boundaries, random/sync state, victory/end-game state, deal/vote/religion/global
counters, map/player iteration cardinalities, init/reset/read/write symmetry, and cross-object
producer/consumer contracts. Use K-Mod/Civ4CE ancestry selectively and whole-album semantic dedup
before numbering.

F608 / provisional KI#931 remains the next unused root slot.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP427
===============================================================================


===============================================================================
C031-WIP428 — PROTECTED QUEUE 008 CvGame.cpp — OPENING RECONCILIATION
===============================================================================

STATUS
------
Queue 008 `CvGameCoreDLL/CvGame.cpp` protected deep-method re-audit is OPEN.
Queue 007 `CvUnit.cpp` remains CLOSED at C031-WIP427.
F608 / provisional KI#931 remains UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository/gameplay source files modified.

ORIGINAL QUEUE-008 FINDINGS RECONCILED AGAINST CURRENT SAS
---------------------------------------------------------
The earlier original CvGame.cpp review produced ten roots, KI#336 through KI#345.
All ten are explicitly repaired in the current tree and are therefore regression
controls for this protected re-audit, not candidates for F608:

* KI#336 — dynamic map-heaviness recomputation timing/lifecycle;
* KI#337 — AP/UN replay-message historical audience;
* KI#338 — starting-location volatility safeguard checked the old human plot;
* KI#339 — starting-handicap random offset was drawn but not applied;
* KI#340 — compact alive-team offset was mixed with absolute team-ID space;
* KI#341 — `initScoreCalculation` used a civilization enum as a tech enum;
* KI#342 — exhausted global-warming retries could accept an unsuitable plot;
* KI#343 — staggered AP/UN missing-voter grace;
* KI#344 — Lock Mods random password could begin with NUL;
* KI#345 — End Score cumulative flag was reset during the victory scan.

Current Known Issues/history material marks all ten Fixed. The current source also
contains their corresponding repair comments/logic. Queue 008 therefore starts from
those repaired behaviors rather than re-reporting their historical defects.

CURRENT-vs-BASE OPENING FRONTIER
--------------------------------
A fresh current-SAS-vs-Base-AdvC member-body/delta pass was rebuilt before source-order
reading. The first low-coverage changes are mostly non-semantic performance/logging or
already-owned repairs:

* `isPowerfulStartingBonus`, `normalizeRemovePeaks`,
  `calculateGwSustainabilityThreshold`, `calculateGwSeverityRating`, `doFPCheck`,
  `setStartingPlotNormalizationLevel`, `getGlobalWarmingChances`,
  `updateGwPercentAnger`, `normalizeRemoveBadTerrain`,
  `normalizeRemoveBadFeatures` — cached defines/enum IDs only in the inspected delta;
* `clearReplayMessageMap` also clears the parallel replay-audience vector introduced by
  repaired KI#337;
* `isPastCityName` accepts both canonical text key and rendered localized city name,
  which is the already-owned KI#325 locale-identity repair;
* `toggleDebugMode`, `createAnimals` and several other current deltas add diagnostic /
  GameRecord state without changing the inspected gameplay decision;
* `updateWar` passes the explicit `WAR_DECLARATION_ALWAYS_WAR` provenance to the
  expanded current `declareWar` signature while preserving the inherited primary-DoW
  behavior.

SAVE-LOAD OPTION REMAP CONTROL
------------------------------
Current `CvGame::read` deliberately removed Base AdvC's historical `uiFlag` compatibility
branches and keeps only the current-layout option remap. Its raw indices initially looked
alarming, especially because an inline comment says `or 19 - you'd need to check which is
correct`, so they were verified mechanically rather than trusted.

For the current GameOption enum/XML order they are exact:

* 17 = `GAMEOPTION_NEW_RANDOM_SEED`
* 18 = `GAMEOPTION_LOCK_MODS`
* 19 = `GAMEOPTION_RISE_FALL`
* 20 = `GAMEOPTION_TRUE_STARTS`
* 23 = `GAMEOPTION_NO_VASSAL_STATES`
* 27 = `GAMEOPTION_NO_ESPIONAGE`

Current `CvGame::write` writes `uiFlag = 27`, matching Base AdvC's latest layout marker.
Thus the hard-coded current-version mapping does not itself corrupt current-format saves.
Removal of older-layout compatibility remains an explicit SAS compatibility policy and is
not promoted as a current-live F608.

`onAllGameDataRead` was also compared to Base AdvC. The removed save-flag-dependent work is
legacy migration work for earlier layouts/rules (old handicap storage, pre-river-rule plot
groups, legacy organization seats, pre-maintenance/attitude versions). The current-layout
AI-autoplay reset remains preserved independently. No same-version missing-finalization root
has yet been established.

STARTING-PLOT / HANDICAP REGRESSION CONTROLS
--------------------------------------------
The current starting-position delta was reopened because SAS now supports additional /
non-linearly spaced handicaps and larger player/team sets.

* `initGameHandicap` and `updateAIHandicap` now average XML `iDifficulty` and map through
  `handicapFromDifficulty` rather than averaging raw enum IDs. Current handicap difficulty
  values are monotone 18,26,...,98 across Rookie through Deity+, so the nearest-entry mapping
  matches the intended non-linear-enum-safe contract.
* `assignStartingPlots` rotates the actual alive-team vector; this is the repaired KI#340.
* `applyStartingLocHandicaps` evaluates the sorted site about to be assigned; repaired KI#338.
* `sortByStartingLocHandicap` actually applies its chosen random eligible-agent offset;
  repaired KI#339.
* Remaining normalization deltas inspected so far are define caching only.

GLOBAL-WARMING REGRESSION CONTROLS
----------------------------------
Current `doGlobalWarming` / `getRandGWPlot` retain the repaired Queue-008 behavior:
unsuitable exhausted draws are rejected (KI#342), and passability changes trigger path-distance /
water-area / plot-group topology refresh. No new global-warming root established in this opening
pass.

INITIAL CURRENT QUEUE-008 PRIORITIES
------------------------------------
With the old ten findings reconciled and the shallow delta surface filtered, the next pass will
prioritize actual stateful/global contracts rather than source order:

1. current save/read/write/onAllGameDataRead same-version symmetry and newly persisted SAS state;
2. winner/victory/game-state and optional Fast Save transition ordering/reentrancy;
3. barbarian creation/unit selection and current SAS content-domain assumptions;
4. turn/year/calendar and expanded-world/player cardinality boundaries;
5. RNG/sync/checksum state and 48-civ-safe indexing/bit arithmetic;
6. vote/deal/religion/corporation/global counters and owner/team/player identity transitions;
7. final low-coverage current-vs-Base semantic body reconciliation before Queue-008 closure.

DURABLE STATE
-------------
Files complete: 33 / 169.
Queue 008 `CvGameCoreDLL/CvGame.cpp` is OPEN.
F608 / provisional KI#931 is UNUSED.
Latest physical checkpoint: C031-WIP428.
No gameplay/source files modified.

Exact continuation cursor:
- WIP428 physical -> F608 unused;
- treat KI#336-KI#345 and current-version GameOption index mapping as regression/positive controls;
- continue same-version serialization/new-persisted-state symmetry first;
- then victory/end-state, barbarian/global state, calendar/cardinality and sync/RNG producer-consumer pairs;
- whole-album semantic dedup before numbering;
- promote F608 only for a distinct current-live contradiction;
- physically freeze WIP429 regardless before changing major lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP428
===============================================================================


===============================================================================
C031-WIP429 — QUEUE 008 CvGame.cpp — F608 / KI#931 CONFIRMED
===============================================================================

STATUS
------
Queue 008 `CvGameCoreDLL/CvGame.cpp` protected deep-method re-audit remains OPEN.
F608 / KI#931 CONFIRMED.
F609 / provisional KI#932 is UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository/gameplay source files modified.

F608 / KI#931 — ADV CIV-SAS VICTORY FAST SAVE SERIALIZES PRE-END GAMESTATE_ON
--------------------------------------------------------------------------------
Classification: current-live AdvCiv-SAS lifecycle/save-state defect introduced by the optional
Fast Save feature (practical 6374 / `3d67a8f613...`), enabled by default in current SAS.

Current `CvGame::setWinner` first assigns:

    m_eWinner = eNewWinner;
    m_eVictory = eNewVictory;

For ordinary games it then fires `CvEventReporter::victory`. BUG registers
`SASFastSave.saveGameEnd` as the first victory handler specifically so the victory save occurs
before AI Auto Play's victory handler. Rise & Fall suppresses the normal victory event, so SAS
calls the same helper directly at the equivalent point in `setWinner`.

Both routes therefore execute the Fast Save BEFORE the later block that transitions the game to
`GAMESTATE_OVER` or `GAMESTATE_EXTENDED`.

This timing is explicit in current source/docs, not inferred:

* `BugEventManager.py` says the save runs after winner/victory assignment but before the later
  OVER/EXTENDED transition;
* `SASFastSave.py` repeats the same contract;
* `README_Main_Changes_Guide.md` documents that Victory Fast Saves use that pre-transition event;
* the practical-6374 commit message says victories are saved before autoplay shutdown.

`SAS_FAST_SAVE_GAME_END_ENABLE` is 1 by default, so ordinary current single-player victories can
exercise this path without special unsupported settings.

The serialization contract makes the resulting snapshot internally inconsistent as an end save.
`CyGame::saveGame` directly invokes:

    gDLL->getEngineIFace()->SaveGame(szTmp, SAVEGAME_NORMAL);

from the victory callback. `CvGame::write` serializes, in order:

    m_eWinner
    m_eVictory
    m_eGameState

At this point winner/victory are already final but `m_eGameState` is still `GAMESTATE_ON`.
`CvGame::read` restores those same three members directly, and `onAllGameDataRead` contains no
winner/victory/game-state reconciliation.

The normal victory machine also cannot complete the missing transition after that save is loaded.
`CvGame::testVictory()` begins with:

    if (getVictory() != NO_VICTORY)
        return;

So a loaded Victory Fast Save has the persistent combination:

    winner    != NO_TEAM
    victory   != NO_VICTORY
    gameState == GAMESTATE_ON

and future victory tests return before `setWinner` can ever run again. Consequently the later
`setGameState` transition that was absent from the snapshot is not reconstructed by ordinary game
progression.

That matters because `setGameState(GAMESTATE_OVER)` is itself the producer for end-of-game side
effects: `gameEnd` reporting, BUG's ordinary end save, Rise & Fall end preparation,
`showEndGameSequence`, and extended-game popup setup. A snapshot taken before that transition is
therefore not semantically equivalent to the final victory state it is named/documented to save.

AUTOPLAY / RISE-FALL BOUNDARY
-----------------------------
The exact final state must not be guessed as always `GAMESTATE_OVER`.

For ordinary AI Auto Play, the Fast Save handler is deliberately registered before
`AIAutoPlay.onVictory`; that later handler calls `endAIAutoPlay`, changing autoplay state before
`CvGame::setWinner` resumes and chooses the final game state. Engine autorun can independently
lead to the `GAMESTATE_EXTENDED` branch. Rise & Fall uses its own direct pre-transition save and
has separate end-state preparation.

Thus a narrow repair should make the permanent victory Fast Save capture the ACTUAL post-victory
state/lifecycle, rather than simply mutating the pre-event snapshot to `GAMESTATE_OVER`.
The practical repair placement may require preserving the runtime-tested reason the previous late
hooks failed to create files, but the serialized state itself needs to reflect the completed
winner/victory transition.

The older BUG AutoSave end callback is a useful positive control: `setGameState` first assigns
`m_eGameState = GAMESTATE_OVER`, then emits `gameEnd` / `gameEndSave`; a save taken there naturally
contains the completed state. SAS moved victory saving earlier for runtime reliability, but that
also moved it across this state boundary.

HISTORY / DEDUP
---------------
Practical 6374 (`3d67a8f613...`, 2026-09-01) introduced SASFastSave, the early victory event
registration, and the Rise & Fall direct call. Its runtime validation established that turn-0 and
terminal victory files were physically created, but the supplied history does not show a load-and-
resume validation of the terminal Fast Save.

No later supplied commit through the current light-source snapshot changes this victory Fast Save
placement. Whole-album search finds no earlier root owning a pre-end `GAMESTATE_ON` victory save or
winner/victory/game-state serialization mismatch. The older Queue-008 KI#336-KI#345 set is
independent.

F608 is therefore one distinct root. The ordinary-game and Rise & Fall hooks, pre-autoplay ordering,
and skipped end side effects are consequence/activation breadth of the same lifecycle snapshot
error, not separate findings.

POSITIVE / NEGATIVE CONTROLS FROM THIS TRANCHE
----------------------------------------------
* The scary-looking current-version raw GameOption remap in `CvGame::read` remains a positive
  control: indices 17/18/19/20/23/27 exactly match current enum/XML ordering.
* Newly persisted map-heaviness flags, replay-audience masks and SAS source/version history retain
  matched current read/write order in the inspected stream.
* `onAllGameDataRead` does not contain a hidden reconciliation that would rescue F608.

CURRENT STONE AFTER C031-WIP429
-------------------------------
Queue 008 `CvGameCoreDLL/CvGame.cpp` — DEEP-METHOD RE-AUDIT OPEN.
F608 / KI#931 CONFIRMED.
F609 / provisional KI#932 UNUSED.
Latest physical checkpoint: C031-WIP429.
No repository/gameplay source modified.

NEXT
----
Continue Queue 008 with current same-version serialization/global-lifecycle state outside F608.
Prioritize newly persisted SAS state and read/write/reset invariants first, then winner/end-state
consumers not already owned by F608, barbarian/global counters, calendar/turn boundaries,
48-civ/team cardinality and sync/RNG state. Whole-album semantic dedup before numbering.

Exact continuation cursor:
WIP429 physical -> F609 unused -> exclude F608 Fast Save lifecycle root -> continue same-version
serialization/new-state symmetry -> victory/end-state consumers outside F608 -> barbarian/global
state -> calendar/cardinality/sync-RNG -> freeze WIP430 regardless before changing major lens.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP429
===============================================================================


===============================================================================
C031-WIP430 — QUEUE 008 CvGame.cpp — POST-REVIEW STATE/LIFECYCLE CONTROLS
===============================================================================

STATUS
------
Queue 008 `CvGameCoreDLL/CvGame.cpp` protected deep-method re-audit remains OPEN.
F608 / KI#931 remains the latest confirmed root.
F609 / provisional KI#932 remains UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository/gameplay source files modified.

PURPOSE OF THIS STONE
---------------------
After F608, the audit narrowed the remaining Queue-008 search to `CvGame.cpp` changes made after
its original whole-file review, rather than re-running already-closed inherited branches.  The
post-review commit map is concentrated around autoplay lifecycle, GameRecord instrumentation,
replay/vote history, source-version persistence, Fast Save, and the KI#336-KI#345 repairs.
This tranche closes several high-risk-looking save/lifecycle candidates without spending F609.

CONTROL A — SAVES CREATED DURING AI AUTOPLAY RESTORE HUMAN STATUS COHERENTLY
----------------------------------------------------------------------------
`CvGame::read` reads `m_iAIAutoPlay` and, when the stored value is nonzero, temporarily changes it
to -1 until all game data has been loaded. `CvGame::onAllGameDataRead` later changes that sentinel
to 0 and refreshes AI attitudes.

A possible cross-object concern was that the active civilization might remain permanently marked
as AI-controlled because normal autoplay uses `CvPlayer::setHumanDisabled(true)`. The player
serialization/lifecycle disproves that concern:

* `CvPlayer::read` begins with `reset()`;
* `CvPlayer::reset` explicitly sets `m_bDisableHuman = false`;
* it then runs `updateHuman()`, which derives the human slot from `CvInitCore` when human-disable is
  false;
* `m_bDisableHuman` itself is transient and is not restored from the save stream.

Thus the game-level -1 sentinel survives only long enough for AdvC's load-finalization logic; it
does not leave the player's human-disable bit persisted across the load. This inherited advc.127
mechanism is internally coherent under current SAS.

CONTROL B — PRACTICAL 6358 SOURCE-HISTORY SAVE-LAYOUT CHANGE IS INTENTIONALLY INCOMPATIBLE
-------------------------------------------------------------------------------------------
Practical 6358 appends a variable-length `m_aSASVersionHistory` block to the end of the current
`CvGame` serialized payload. `CvGame::read` unconditionally reads that block, while the numeric
`uiFlag` remains 27. Superficially this means a pre-6358 save with the same flag cannot be parsed by
the new layout.

That is real format incompatibility, but it is explicitly documented policy rather than a current
bug. The same practical's root README states:

    As with other AdvCiv-SAS save-layout changes, older save formats are intentionally
    unsupported rather than migrated.

The Main Changes Guide repeats that older save layouts are intentionally unsupported. Therefore
F609 must not be spent on the missing version gate: current same-version streams remain paired, and
backward migration is outside the mod's declared contract.

CONTROL C — START FAST SAVE DOES NOT PRECEDE SOURCE-HISTORY CREATION
--------------------------------------------------------------------
A second possible interaction between practical 6358 and practical 6374 also closes cleanly.
`CvEventReporter::gameStart()` performs:

    initializeSASVersionHistoryForNewGame();
    ...
    m_kPythonEventMgr.reportGameStart();

BUG registers `SASFastSave.saveGameStart` on that later `GameStart` event. The START Fast Save is
therefore taken only after the creation-source entry exists. This is unlike F608's victory save,
where the callback is intentionally before the later game-state transition.

CONTROL D — AUTOPLAY STOP SAVE OCCURS AFTER NORMAL HUMAN RE-ENABLE
------------------------------------------------------------------
`AIAutoPlay.checkPlayer` first calls `game.endAIAutoPlay(iEndCause)` and only then invokes
`SASFastSave.saveAutoPlayStop()` for scheduled/user-interrupted completion. In single player,
`CvGame::setAIAutoPlay(0, true, ...)` re-enables the current active player through
`setHumanDisabled(false)` before returning. The ordinary STOP snapshot therefore does not share
F608's pre-transition defect.

POST-ORIGINAL-REVIEW CvGame CHANGE MAP
--------------------------------------
The current light-source history was mechanically enumerated for practical commits >=6000 that
modify `CvGame.cpp`. The meaningful post-original-review tail is especially small:

* 6229-6248 — the independently reviewed KI#338-KI#345 / KI#336 repairs and related controls;
* 6250, 6345 — GameRecord/debug lifecycle instrumentation;
* 6358 — persisted source/version history (controls above);
* 6374 — SASFastSave (F608 owns the victory snapshot root);
* 6393 — diplomatic vote history logging;
* 6399 — recorder-gate/performance cleanup plus equivalent debug toggle rewrite.

Earlier 6000-series changes are primarily GameRecord provenance/logging, explicit autoplay end
causes, barbarian-spawn recording and war/tech provenance. Their `CvGame` hunks are being treated as
cross-check surfaces, not assumed defects.

CURRENT STONE AFTER C031-WIP430
-------------------------------
Queue 008 `CvGameCoreDLL/CvGame.cpp` — DEEP-METHOD RE-AUDIT OPEN.
F608 / KI#931 CONFIRMED.
F609 / provisional KI#932 UNUSED.
Latest physical checkpoint: C031-WIP430.
No repository/gameplay source modified.

NEXT
----
Finish the post-6221 semantic reconciliation, especially practical 6250/6345/6393/6399 and their
stateful call boundaries, then return to current XML/config activation against inherited
`CvGame` global mechanics only if that tail closes cleanly. Keep KI#336-KI#345 and F608 excluded.
Whole-album semantic dedup before numbering; freeze WIP431 regardless before closing Queue 008 or
changing major lens.

Exact continuation cursor:
WIP430 physical -> F609 unused -> inspect post-review GameRecord/debug/vote/autoplay call boundaries
for gameplay-state side effects -> classify/close -> current-content activation sweep for CvGame
only if needed -> whole-album dedup -> promote F609 only for a distinct current-live root -> freeze
WIP431 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP430
===============================================================================


===============================================================================
C031-WIP431 — QUEUE 008 CvGame.cpp — DEEP-METHOD RE-AUDIT CLOSED
===============================================================================

STATUS
------
Queue 008 `CvGameCoreDLL/CvGame.cpp` protected deep-method re-audit is CLOSED.
F608 / KI#931 is the only new root added by the protected Queue-008 re-loop.
F609 / provisional KI#932 remains UNUSED and transfers to Queue 009.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE archives.
No repository/gameplay source files modified.

FINAL POST-ORIGINAL-REVIEW RECONCILIATION
-----------------------------------------
The earlier File-008 audit had already completed both a current-vs-Base semantic-diff pass and an
independent inherited whole-file invariant pass. Its ten roots KI#336-KI#345 were rechecked first;
all ten are explicitly repaired in the current SAS tree and serve as regression controls.

A mechanical history map then enumerated every practical >=6000 that changes `CvGame.cpp` and,
more importantly, every post-original-review change. The late tail is finite:

* practical 6229-6248: the reviewed KI#336-KI#345 fixes and closely related topology/replay fixes;
* 6250: GameRecord session finalization before game reset;
* 6345: successful Debug-mode transition logging;
* 6358: persistent source/version history;
* 6374: SASFastSave;
* 6393: diplomatic-vote history logging;
* 6399: recorder-gate/performance cleanup and an algebraically equivalent Debug toggle rewrite.

The final added-line risk scan covered indexing, division/modulo, random draws, bit shifts,
serialization, size-driven allocation, pointer/sentinel checks and state-changing call ordering in
that late tail. No unowned current-live root survives outside practical 6374's F608.

LATE-TAIL CONTROLS
------------------
1. `CvGame` save/load during AI Auto Play is coherent across `CvPlayer`: player read begins with
   `reset()`, which clears transient `m_bDisableHuman` and reconstructs human status from
   `CvInitCore`; the game-level -1 autoplay sentinel is later cleared in `onAllGameDataRead`.

2. Practical 6358's variable-length source-history payload deliberately breaks older save layouts.
   Its README and Main Changes Guide explicitly state that older AdvCiv-SAS save schemas are
   intentionally unsupported rather than migrated. The unchanged `uiFlag=27` is therefore not an
   accidental current compatibility promise. Current same-version read/write order is symmetric.

3. START Fast Saves occur only after `CvEventReporter::gameStart` calls
   `initializeSASVersionHistoryForNewGame`, so they contain the creation provenance entry.

4. Scheduled/user-interrupted STOP Fast Saves occur after `endAIAutoPlay` re-enables normal human
   control, so they do not share F608's pre-transition victory snapshot defect.

5. Practical 6393 vote-result logging calls deterministic vote counting / `findHighestVoteTeam`.
   Those functions do not consume RNG. The logger writes files and recorder-owned globals only;
   it does not mutate vote outcomes or game state.

6. Practical 6250 session finalization flushes recorder-owned pending observations before reset.
   Its inspected call chain reads the old game/map state but does not mutate authoritative gameplay
   state.

7. Current 48-civilization-sensitive masks in `CvGame.cpp` use `qword`; valid Player IDs including
   Barbarians fit within bits 0..48. The relevant start/shuffle arrays use the corresponding
   `MAX_CIV_PLAYERS`, `MAX_PLAYERS`, `MAX_CIV_TEAMS` and `MAX_TEAMS` domains consistently.

8. A post-original-review XML history sweep found no new unit/building/victory/world/cardinality
   content that activates a fresh inherited `CvGame` assumption. The late GlobalDefines are
   primarily logging/AI knobs; the one directly relevant new game-global feature is Fast Save,
   already owned by F608.

QUEUE 008 RESULT
----------------
The stronger protected Queue-008 re-audit therefore adds one distinct current-live root:

* F608 / KI#931 — victory Fast Save serializes winner/victory while `m_eGameState` is still
  `GAMESTATE_ON`; loading restores that impossible pre-end snapshot, and `testVictory` immediately
  returns because victory is already set, so the missing OVER/EXTENDED lifecycle transition cannot
  reconstruct itself.

The original ten Queue-008 findings remain fixed controls. No F609 is required to describe any
remaining current-live `CvGame.cpp` behavior established by this re-audit.

QUEUE 008 METHOD COVERAGE AT CLOSURE
------------------------------------
The protected re-loop covered:

* reconciliation of KI#336-KI#345 against current source;
* fresh current-vs-Base changed-body mapping;
* current-format read/write/reset symmetry and newly persisted state;
* winner/victory/game-state lifecycle and Fast Save execution timing;
* autoplay load/stop and active-human restoration across `CvGame`/`CvPlayer`;
* source-version-history creation/load/save lifecycle and declared compatibility policy;
* replay-audience qword masks and current 48-civ/team cardinality;
* current barbarian city/unit spawn additions and their logging call boundaries;
* vote-resolution state and late diplomatic-vote instrumentation;
* GameRecord session rollover/debug/active-player hooks;
* global-warming, starting-position, Lock Mods, Time-victory and replay fixes as regression controls;
* post-original-review commit/addition risk scan for arithmetic, random, pointer, serialization and
  ordering hazards;
* whole-album semantic dedup before final numbering.

CURRENT STONE AFTER C031-WIP431
-------------------------------
Queue 008 `CvGameCoreDLL/CvGame.cpp` — DEEP-METHOD RE-AUDIT CLOSED.
Protected deep-method cursor: Queue 009 `CvGameCoreDLL/CvPlot.cpp` NEXT.
F608 / KI#931 CONFIRMED.
F609 / provisional KI#932 UNUSED.
Latest physical checkpoint: C031-WIP431.
No repository/gameplay source modified.

NEXT
----
Advance to protected Queue 009 `CvGameCoreDLL/CvPlot.cpp`. Reconcile its original seven findings
(KI#346-KI#352) against current SAS before new discovery. Then rebuild a fresh current-SAS-vs-Base
semantic body map and prioritize plot topology/passability, owner/team/reveal state, culture and
plot-group connectivity, improvement/bonus/feature transitions, movement/pathing legality,
serialization and SAS-activated terrain/unit-domain assumptions. Use K-Mod/Civ4CE ancestry and
whole-album semantic dedup before numbering.

F609 / provisional KI#932 remains the next unused root slot.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP431
===============================================================================


===============================================================================
C031-WIP432 — QUEUE 009 CvPlot.cpp — CLEAN FRONTIER + F609 ADVANCED-START REVEAL
===============================================================================

STATUS
------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` protected deep-method re-audit remains OPEN.
Original Queue-009 roots KI#346-KI#352 are reconciled as fixed current-tree controls.
F609 / KI#932 is CONFIRMED in the protected Queue-009 re-loop.
F610 / provisional KI#933 is UNUSED.
Authoritative current source: user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material: supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS archives.
No repository/gameplay source files modified.

QUEUE-009 OPENING RECONCILIATION
--------------------------------
The original seven `CvPlot.cpp` roots remain repaired in current SAS and are being used as
regression controls:

* KI#346 — live Ice/global-warming passability changes now defer one water-area/path-distance/
  plot-group refresh to the end of the mutation batch;
* KI#347 — Python found-value overrides are cached into the widened current integer storage;
* KI#348 — foreign-team air-capacity checks use the observing/requesting team;
* KI#349 — `debugStr` returns owning string storage rather than a temporary `c_str`;
* KI#350 — nuke reporting uses observer-relative visual-owner snapshots rather than exposing a
  hidden-nationality unit's real owner;
* KI#351 — runtime N/W river-edge mutations refresh river-derived irrigation/fresh-water-health/
  plot-group state;
* KI#352 — culture decay iterates the correct player domain and cannot index one past the
  civilization-player array for Barbarian culture.

The fresh current-SAS-vs-Base body map was rebuilt with comment-aware function extraction rather
than raw diff-hunk proximity. Only about twenty common `CvPlot` member bodies differ semantically
in the current tree. Those differences reduce to the seven repaired roots above, KI#171/F180-family
repairs, recorder-only instrumentation, safe constant caching, or declared save-layout policy.

The eight SAS-only `CvPlot` helpers are the first-city/BFC quality family. Literal `CvPlot` method
names made them look under-covered, but their actual mechanics were already deeply audited through
Queue/File 016 `CitySiteEvaluator.cpp`; that pass produced and repaired F180/KI#503 (missing active
irrigation food) and explicitly checked the remaining bonus-improvement/build-selection helpers.
They are therefore cross-file coverage, not fresh Queue-009 roots.

INHERITED-BODY / CACHE CONTROLS
-------------------------------
Several attractive inherited candidates were checked and deliberately not promoted:

1. `isRiverConnection` / `isRiverToRiverConnection`
   AdvC's rewritten river-connection logic was compared against K-Mod/BtS. The ordinary cardinal
   mapping is equivalent, and the river-to-river helper was mechanically checked for directional
   symmetry under its shared crossing constraints. No current contradiction established.

2. `canSeeDisplacementPlot`
   The apparently unused original displacement parameters are inherited from the BtS/K-Mod line-
   of-sight formulation; the alternate exact-line formulation is itself preserved as commented-out
   experimental code. No fresh current defect established.

3. `m_bAnyIsthmus` invalidation
   `CvPlot::setPlotType` can change land/water topology without locally recomputing this cache when
   the operation stays on the local-area-update branch. This is a real supported-API robustness
   seam for post-start `CyPlot.setPlotType`/modmod topology mutation. It does not presently satisfy
   the current-live SAS threshold: normal map-generation lake batches force the KI#572 full area
   recalc, scenario import recalculates areas after plot-type loading, and `normalizeAddLakes`
   rejects coastal land so it cannot normally alter a navigable-sea isthmus. Keep as robustness
   debt, not F609.

4. `m_bPotentialCityWork` invalidation
   The cache ultimately depends on `canEverFound`. `setTerrainType` and `setFeatureType` do not
   independently refresh it for every theoretical foundability change. Current SAS content keeps
   this dormant: all ordinary land terrains Grass/Plains/Desert/Tundra/Snow have `bFound=1`, while
   the sole impassable feature is Ice on water, and water never passes `canEverFound`. Global
   Warming terrain swaps therefore do not change the cached predicate. Keep as general-mod
   robustness only.

F609 / KI#932 — AdvC terrain-only revelation skips its requested plot-group refresh
-------------------------------------------------------------------------------
CLASSIFICATION: inherited AdvC regression, current-live through shipped Advanced Start.

ROOT
----
BtS/K-Mod `CvPlot::setRevealed` updates every alive member player's plot group immediately when the
plot's revealed bit changes and `bUpdatePlotGroup` is requested. Only after that does it branch on
`bTerrainOnly`.

AdvC practical 1253 / commit `f6ff6a182f5187541fff6e5bbbe12f9573b3778c` reworked revealed
trade-network information (advc.124). To ensure owner/improvement/route/city revealed state was
settled before recomputing connectivity, it moved the plot-group update to the bottom of
`CvPlot::setRevealed`, with the explicit comment:

    all revealed status changes need to be applied first

But the inherited early exit remained above the relocated refresh:

    if (bTerrainOnly)
        return;

Consequently `setRevealed(..., bTerrainOnly=true, ..., bUpdatePlotGroup=true)` can change the
permanent revealed bit and then return without performing the plot-group refresh that the caller
explicitly requested.

CURRENT-LIVE ACTIVATION
-----------------------
Current `CvPlayer::doAdvancedStartAction(ADVANCEDSTARTACTION_VISIBILITY, ...)` uses exactly:

    pPlot->setRevealed(getTeam(), true, true, NO_TEAM, true);

for purchased visibility (and the symmetric false call for the internal removal path).

This is not an unavailable configuration:

* current `ADVANCED_START_VISIBILITY_COST = 1`, so visibility purchasing is enabled;
* `CvGameTextMgr::getVisibilityDataForAS` exposes the visibility item in the player-facing Advanced
  Start palette whenever the positive cost is available;
* `CvPlayerAI::AI_advancedStartRevealRadius` and the broader Advanced Start AI also purchase
  `ADVANCEDSTARTACTION_VISIBILITY` directly.

WHY REVELATION CAN CHANGE REAL CONNECTIVITY
-------------------------------------------
AdvC trade-network construction intentionally uses team revelation as topology state:

* `CvPlot::isTradeNetwork` returns false for an unrevealed plot;
* `CvPlot::isTradeNetworkConnected` returns false when either endpoint is unrevealed;
* `CvPlot::updatePlotGroup` consumes those predicates to attach/combine player plot groups.

Current SAS makes this especially easy to reproduce without any hidden route or improvement:
`TECH_SEAFARING` gives `TERRAIN_COAST` terrain trade. A revealed Coast plot can therefore be network
terrain purely through its terrain and the team's technology. Revealing the last fogged Coast tile
in a chain after Seafaring can change the correct network connectivity immediately.

Because terrain-only revelation intentionally does not reveal owner/improvement/route/city state,
there are no later revealed-status mutations that need to precede a refresh in this branch. The
only relevant state change is the revealed bit itself — precisely the state BtS/K-Mod refreshed
before entering the terrain-only branch.

PERSISTENCE / CONSUMER PROOF
----------------------------
`ADVANCEDSTARTACTION_EXIT` refunds leftover points, leaves Advanced Start mode and performs the
normal production/tech/civic prompts; it does not rebuild plot groups globally. There is no general
per-turn wholesale plot-group rebuild that guarantees repair immediately after Advanced Start.
Thus a purchased visibility tile can leave the player's cached plot groups reflecting the
pre-reveal topology even though `isRevealed` and the direct trade-network predicates now say the
connection is legal.

Subsequent unrelated topology/technology/city updates may eventually rebuild or locally touch the
network, but that does not make the state correct at the mutation boundary. The defect is the
omitted requested refresh and can affect connected-resource/trade-network consumers until such an
unrelated update occurs.

HISTORY / ACTIVATION BOUNDARY
-----------------------------
BtS/Civ4CE and K-Mod perform the plot-group update before the terrain-only gate. AdvC's advc.124
refactor deliberately moved it later for the full-revelation case, thereby creating the early-
return hole. Current SAS inherits that ordering unchanged while keeping Advanced Start visibility
live and adding an early Coast-trade technology (`TECH_SEAFARING`) that provides a very simple
current fixture.

WHOLE-ALBUM DEDUP
-----------------
The album contains extensive Advanced Start findings (technology refund/irreversible-state,
purchase-result control flow, role assignment, city-created callbacks, etc.) and several plot-group
roots (route topology, city caches, Ice passability), but none owns `CvPlot::setRevealed`'s
`bTerrainOnly` early return skipping the explicitly requested plot-group refresh. This is a distinct
mutation/invalidation root.

REPAIR BOUNDARY
---------------
Do not simply move the full bottom refresh back above all revealed owner/improvement/route handling;
that would undo advc.124's valid reason for delaying full-revelation recomputation.

A narrow repair is to preserve the delayed bottom refresh for `bTerrainOnly == false`, but before
returning from the terrain-only branch, update team members' plot groups when:

* the caller requested `bUpdatePlotGroup`, and
* the revealed bit actually changed (`bOldValue != bNewValue`).

Using the revealed-bit change specifically is preferable in the terrain-only branch: owner,
improvement, route and city revelation are intentionally not modified there, so mismatches between
those stored values and current actual values must not masquerade as terrain-only revelation
changes.

CURRENT STONE AFTER C031-WIP432
-------------------------------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` — DEEP-METHOD RE-AUDIT OPEN.
F609 / KI#932 CONFIRMED.
F610 / provisional KI#933 UNUSED.
Latest physical checkpoint: C031-WIP432.
No repository/gameplay source modified.

NEXT
----
Continue the inherited authoritative-body pass outside F609, with emphasis on revelation/
visibility transfer, plot-group/resource topology, culture/ownership transitions and dynamic
feature/terrain/improvement state. Keep KI#346-KI#352, F180, F507/F542-family route-cache roots and
F609 excluded. Check current content/callers before promoting dormant API robustness.

Exact continuation cursor:
WIP432 physical -> F610 unused -> continue visibility/revealed-state bookkeeping outside the
terrain-only setRevealed root -> plot-group/resource mutation invariants -> culture/owner transfer
state -> inherited-body activation against current SAS XML/domains -> whole-album semantic dedup ->
promote only a distinct current-live root -> freeze WIP433 regardless before major lens change or
Queue-009 closure.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP432
===============================================================================


===============================================================================
C031-WIP433 — QUEUE 009 CvPlot.cpp — INHERITED FRESH-WATER MUTATION + F610
===============================================================================

STATUS
------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` protected deep-method re-audit remains OPEN.
F609 / KI#932 remains CONFIRMED.
F610 / KI#933 is CONFIRMED.
F611 / provisional KI#934 is UNUSED.
Authoritative current source remains the user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material remains supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS.
No repository/gameplay source files modified.

POST-WIP432 INHERITED-BODY CONTROLS
-----------------------------------
1. `CvPlot::countFriendlyCulture` / war-weariness third-party culture
   This looked surprising because the helper includes culture belonging to every living team that
   the queried team can peacefully enter. A combatant can therefore count culture belonging to a
   vassal/Open-Borders third party as "friendly" in `CvTeam::changeWarWeariness`.

   This is inherited intended semantics rather than a fresh Queue-009 defect. BtS/Civ4CE and K-Mod
   already implement the same set directly as own-team OR vassal OR Open Borders culture. AdvC's
   `canPeacefullyEnter` formulation is the optimized equivalent. AdvC's advc.099 addition only
   preserves defeated culture from players that belonged to the queried team itself. Do not spend
   F610 on this inherited policy.

2. KI#346 Ice-melt -> lake/fresh-water second-order hypothesis
   `recalculateWaterAreas()` calls `calculateAreas_dfs()` and `calculateReprAreas()`, and the latter
   ends in `updateLakes()`. More importantly, `CvArea::updateLake(true)` classifies a water area by
   summing every passability-split area sharing its representative BtS-style water body. Melting
   Ice changes passable-area divisions but does not change water-vs-land topology or the summed
   representative water-body size. Therefore the KI#346 Ice repair does not by itself flip lake
   status and does not create a new city-fresh-water invalidation root. This corroborates the prior
   CvCity deep-loop control.

3. `isConnectSea`
   The helper's circular scan does not explicitly compare the last valid neighbor with the first,
   but its threshold is `iChanges > 2`; one connected sea arc cannot exceed two transitions, while
   two separated arcs necessarily create enough transitions in the linearized scan. More
   importantly, all current C++ callers are commented out. Keep as dormant helper/design code, not
   F610.

F610 / KI#933 — Global Warming can remove Oasis fresh water without refreshing derived state
-------------------------------------------------------------------------------------------
CLASSIFICATION: current-live inherited BtS mutation/invalidation defect.

ROOT
----
`CvPlot::isFreshWater()` explicitly depends on adjacent features whose XML has
`bAddsFreshWater=1`. The source immediately above it even carries the inherited warning:

    if this changes need to call updateIrrigated and CvCity::updateFreshWaterHealth

But `CvPlot::setFeatureType()` changes the feature and refreshes sight, direct plot yield,
graphics and nearby feature/improvement health/happiness only. It does NOT call either
`updateIrrigated()` or `CvCity::updateFreshWaterHealth()` when the old/new feature changes the
AddsFreshWater predicate.

CURRENT-LIVE SAS ACTIVATION
---------------------------
Current shipped `CIV4FeatureInfos.xml` has exactly the relevant ordinary feature fixture:

    FEATURE_OASIS
    iAppearance       = 500
    bAddsFreshWater   = 1
    bNoImprovement    = 1
    terrain           = TERRAIN_DESERT

Current Global Warming remains enabled (`GLOBAL_WARMING_PROB = 20`, out of 1000) and its target
selector can choose any ordinary non-city land plot, including a Desert/Oasis plot. Desert gets a
low target score, but remains eligible and can be returned when the sampled pool contains no
higher-scoring candidate.

The current player-facing Global Warming concept text independently makes the intended live path
explicit: if the selected plot has a feature including "Forest, Jungle, Floodplain, Oasis", that
feature is removed.

`CvGame::doGlobalWarming()` implements exactly that. For an Oasis, the "remove other features"
branch calls:

    pPlot->setFeatureType(NO_FEATURE);

There is no later global fresh-water-health or irrigation rebuild in `doGlobalWarming()`.

AUTHORITATIVE CITY-HEALTH CONSEQUENCE
-------------------------------------
A city adjacent to the Oasis satisfies `getPlot().isFreshWater()` because the adjacent feature
adds fresh water. `CvCity::updateFreshWaterHealth()` stores that result in:

    m_iFreshWaterGoodHealth / m_iFreshWaterBadHealth

rather than querying `isFreshWater()` dynamically whenever city health is consumed. Under the
normal inherited BtS define, fresh water contributes +2 health.

The complete current caller set for `updateFreshWaterHealth()` is essentially city initialization
plus the explicit KI#351 runtime-river repair helper. It is not a per-turn recomputation. Thus when
Global Warming removes the only adjacent Oasis, `isFreshWater()` immediately becomes false while
the city's stored fresh-water health can remain at +2 indefinitely until some unrelated operation
happens to refresh it.

IRRIGATION / YIELD CONSEQUENCE
------------------------------
The same Oasis can be the fresh-water source for adjacent Farm irrigation. `isIrrigationAvailable`
checks `isFreshWater()` and the stored irrigation network; improvement yield then grants the
Farm's `IrrigatedYieldChange` when irrigation is available.

`setFeatureType()` does not touch that irrigation network or neighboring Farm yields. Therefore
removing the Oasis can leave adjacent Farm plots and downstream irrigation chains carrying the
pre-removal `m_bIrrigated` state and cached irrigated yield after their actual fresh-water source
has disappeared.

A repair must be more careful than merely calling the current incremental `updateIrrigated()` on
an already-irrigated Farm: that routine's teardown path is entered when an irrigated plot ceases to
be *potential irrigation*, whereas loss of an external fresh-water source can leave every Farm in
the component still potential. A source-loss-aware component invalidation/rebuild (or equivalent
fresh-water-derived-state helper) is needed.

ANCESTRY
--------
This root predates SAS and AdvC:

* BtS/Civ4CE `CvPlot::setFeatureType` refreshes feature health/happiness but not fresh-water health
  or irrigation;
* BtS Global Warming can remove any non-Fallout feature on a selected land plot, including Oasis;
* K-Mod's Global Warming rewrite retains an explicit "remove other features" branch and still does
  not add the missing fresh-water-derived updates;
* Base AdvC inherits both the feature mutation omission and the K-Mod/AdvC warming path;
* current SAS keeps the same executor contract and explicitly documents Oasis removal.

WHOLE-ALBUM DEDUP / CORRECTION
------------------------------
This is distinct from:

* KI#346/F025 — impassable-feature changes leaving PASSABLE_AREAS/path/plot-group topology stale;
* KI#351/F030 — runtime N/W river-edge mutation missing fresh-water/irrigation/plot-group refreshes.

Fixing either existing root does not repair `setFeatureType(NO_FEATURE)` when an AddsFreshWater
feature disappears.

The original Queue-009 closure contained a negative control saying that `setFeatureType`'s
fresh-water facet was incomplete only in general because current data had no supported post-start
AddsFreshWater creation/removal path. That control is now CORRECTED: current Global Warming is
exactly such a shipped path, and current documentation explicitly names Oasis as removable.

The later generic statement that `setFeatureType` was "bounded by already-owned
feature-passability/fresh-water families" must likewise be read with this correction: feature
passability remains owned by KI#346, but AddsFreshWater feature removal is the distinct F610 root.

REPAIR BOUNDARY
---------------
When `setFeatureType` changes whether the plot's feature adds fresh water after final
initialization:

1. refresh `updateFreshWaterHealth()` for cities whose city plot can gain/lose fresh water from
   this feature (adjacent city plots are the direct authoritative set);
2. invalidate/rebuild irrigation connectivity for the local potential-irrigation component in a
   way that handles source loss, then refresh affected yields/layout;
3. keep this separate from KI#346 passability topology and from the ordinary
   `updateSurroundingHealthHappiness()` feature-health/happiness calculation.

Map-generation mutations should avoid unnecessary repeated live-game rebuild cost; the existing
`isFinalInitialized()` boundary used by irrigation handling is an appropriate lifecycle guard to
respect.

CURRENT STONE AFTER C031-WIP433
-------------------------------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` — DEEP-METHOD RE-AUDIT OPEN.
F609 / KI#932 CONFIRMED.
F610 / KI#933 CONFIRMED.
F611 / provisional KI#934 UNUSED.
Latest physical checkpoint: C031-WIP433.
No repository/gameplay source modified.

NEXT
----
Continue the inherited authoritative-body pass outside F609/F610. First follow the irrigation
teardown implication far enough to determine whether the current KI#351 river repair is complete
for *source removal* or whether that is repair breadth of F610/a distinct remaining root. Then
continue plot-group/resource/visibility/ownership and dynamic terrain/improvement state, with
current callers/XML activation and whole-album semantic dedup before any promotion.

Exact continuation cursor:
WIP433 physical -> F611 unused -> verify current `CvMap::updateIrrigated` source-loss teardown
semantics against KI#351 river removal and Oasis F610 repair boundary -> classify without duplicate
inflation -> continue remaining inherited low-coverage `CvPlot` state producers -> whole-album
dedup -> promote only distinct current-live root -> freeze WIP434 regardless before Queue-009
closure or major lens change.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP433
===============================================================================


===============================================================================
C031-WIP434 — QUEUE 009 CvPlot.cpp — KI#351 REOPENED: IRRIGATION SOURCE REMOVAL
===============================================================================

STATUS
------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` protected deep-method re-audit remains OPEN.
F609 / KI#932 remains CONFIRMED.
F610 / KI#933 remains CONFIRMED.
F611 is CONFIRMED as KI#351 REOPENED/BROADENED after the partial SAS repair; no new KI number is
consumed by this same-root follow-up.
F612 / provisional KI#934 is UNUSED.
Authoritative current source remains the user-supplied `AdvCiv-SAS_light_source_20260906T174246.zip`.
Local comparison material remains supplied Base AdvCiv 1.14, K-Mod 1.46 and Civ4CE/BtS.
No repository/gameplay source files modified.

F611 / KI#351 REOPENED — current river repair adds irrigation but cannot remove it when fresh water disappears
-------------------------------------------------------------------------------------------------------------
CLASSIFICATION: current supported-API AdvCiv-SAS follow-up defect in the KI#351 repair, on top of
an inherited BtS irrigation-updater limitation. This is deliberately linked to/reopens KI#351
rather than consuming provisional KI#934 because the user-visible root is the same runtime river-
edge derived-state contract.

WHY THE CURRENT KI#351 REPAIR IS ONLY HALF-CORRECT
--------------------------------------------------
Practical 6234 / commit `d29d65355088fba9563f2a0b68ebe2cd145a75ec` added the shared
`updateRiverDerivedState` helper. After a finalized-game N/W river-edge presence change, it calls:

    kPlot.updateIrrigated();
    every adjacent plot -> updateIrrigated();
    nearby cities -> updateFreshWaterHealth();
    GC.getGame().updatePlotGroups();

The corresponding KI#351 documentation says this local irrigation refresh lets the inherited
pathfinder propagate both additions and removals. The addition half is correct; the removal half
is not.

`CvMap::updateIrrigated(CvPlot& kPlot)` has only two state-changing branches:

1. ADDITION

       !kPlot.isIrrigated()
       && kPlot.isPotentialIrrigation()
       && kPlot.isIrrigationAvailable(true)

   -> flood the connected potential-irrigation component with `setIrrigated(true)`.

2. REMOVAL

       kPlot.isIrrigated()
       && !kPlot.isPotentialIrrigation()

   -> clear the triggering plot, inspect adjacent potential-irrigation components for any remaining
      fresh-water source, and clear a source-less component with `setIrrigated(false)`.

Losing a river fresh-water source does NOT make an existing Farm cease to be potential irrigation.
Once the team has the irrigation-spreading technology, an owned improved Farm carrying irrigation
remains `isPotentialIrrigation()==true` whether or not it is still beside a river. A city plot has
the same structural property while owned/flat.

Therefore an already-irrigated Farm affected by river REMOVAL enters neither state-changing branch:

    isIrrigated()            == true
    isPotentialIrrigation()  == true

The local calls added by KI#351 return without searching for a surviving fresh-water source and
without clearing `m_bIrrigated`.

ASYMMETRIC SOURCE-LEVEL REPRODUCTION
------------------------------------
The asymmetry makes the proof especially clean.

RIVER ADDITION:
* Start with a non-irrigated potential Farm/component.
* Add the river edge.
* The affected Farm now has fresh water, so `isIrrigationAvailable(true)` is true.
* KI#351 calls `updateIrrigated()`; its addition branch floods the connected component true.
* The repair works.

RIVER REMOVAL:
* Start after irrigation propagation has been enabled and a river-fed Farm/component is already
  `m_bIrrigated=true`.
* Remove its last fresh-water river source through the supported runtime river-edge setter.
* The Farm remains a valid irrigation carrier (`isPotentialIrrigation()==true`).
* KI#351 calls `updateIrrigated()` on the changed plot and every adjacent plot, but every already-
  irrigated potential Farm takes no teardown branch.
* `m_bIrrigated` remains true even though the component has no fresh-water source.

CURRENT SAS GAMEPLAY DATA MAKES THE STALE BIT MATTER
----------------------------------------------------
Current `IMPROVEMENT_FARM` has:

    IrrigatedYieldChange = +1 food
    bCarriesIrrigation   = 1

Current `TECH_WATER_WHEEL` grants both `bIrrigation=1` and `bIgnoreIrrigation=1`.
`bIgnoreIrrigation` only permits irrigation-requiring improvements to be built without an available
irrigation source; it does NOT grant the irrigated yield. `calculateImprovementYieldChange` still
uses `isIrrigationAvailable()` for the +1 food.

That makes the stale state authoritative. `isIrrigationAvailable()` first returns true when the
plot's stored `isIrrigated()` flag is true. Therefore after the last river source is removed, the
stale self flag can preserve the Farm's +1 irrigated food and can keep downstream irrigation chains
alive indefinitely.

The tech-acquisition path also removes any uncertainty about whether a river-fed Farm ever gets the
stored flag: `CvTeam::changeIrrigationCount` calls the map-wide irrigation updater, and a fresh-water
Farm that is potential irrigation enters the addition branch and propagates `m_bIrrigated=true`.

ANCESTRY / WHY THIS WAS EASY TO MISS
------------------------------------
The narrow per-plot updater is inherited in behavior from BtS/Civ4CE and K-Mod. BtS/K-Mod keep the
same teardown condition: an irrigated plot must cease to be potential irrigation before the source-
search/removal path runs. Base AdvC moved the pathfinding implementation into `CvMap` but retained
the same semantics.

That inherited algorithm is well suited to its historical common trigger — removing/changing an
irrigation-carrying improvement or otherwise making a carrier cease to be potential irrigation.
It is not a general "fresh-water source changed" recomputation API.

Original BtS river setters did not call it at all. SAS practical 6234 correctly identified runtime
river edits as a missing invalidation source, but reused this narrower updater under the assumption
that it could also propagate source removal.

WHOLE-ALBUM DEDUP / RELATION TO F610
------------------------------------
Do not assign provisional KI#934 to this.

* KI#351/F030 already owns the runtime river-edge derived-state contract. Its city fresh-water-
  health and plot-group repair portions remain valid; only irrigation teardown is incomplete.
  Reopen/broaden KI#351 for the removal half rather than pretending the original root is fully fixed.
* F610/KI#933 is still independently valid: `setFeatureType(NO_FEATURE)` for an Oasis calls no
  fresh-water-health or irrigation invalidation at all. The city-health omission alone is a hard
  F610 consequence.
* F610's irrigation consequence exposes the same deeper source-loss limitation: merely wiring the
  existing per-plot `updateIrrigated()` into `setFeatureType` would not clear an already-irrigated
  source-less Farm component. A correct source-loss helper can therefore be shared by the KI#351
  reopen and F610 without merging their distinct mutator roots.

REPAIR BOUNDARY
---------------
Preserve the existing efficient incremental updater for ordinary carrier additions/removals, but add
an explicit fresh-water-source-change operation. For the locally affected potential-irrigation
component(s), it must be able to:

1. traverse the component after the source mutation;
2. test whether any current member still satisfies `isFreshWater()`;
3. if no source remains, clear `m_bIrrigated` across the component through `setIrrigated(false)` so
   adjacent yields/layout are refreshed;
4. if a new source appeared, retain/reuse the existing addition flood.

The source-change operation can be called from the finalized-game river setters and from the F610
AddsFreshWater feature-change repair. It should not force a full-map reset for ordinary map
construction; the rare runtime-mutation lifecycle guard used by KI#351 is appropriate.

DOCUMENTATION CORRECTION
------------------------
Current KI#351 documentation says the local calls allow the inherited pathfinder to propagate
"additions or removals." Correct that statement when fixing this follow-up: additions work; source
removals require the new source-aware component validation described above.

CURRENT STONE AFTER C031-WIP434
-------------------------------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` — DEEP-METHOD RE-AUDIT OPEN.
F609 / KI#932 CONFIRMED.
F610 / KI#933 CONFIRMED.
F611 / KI#351 REOPENED/BROADENED CONFIRMED.
F612 / provisional KI#934 UNUSED.
Latest physical checkpoint: C031-WIP434.
No repository/gameplay source modified.

NEXT
----
Continue Queue 009 outside F609/F610 and the reopened KI#351 irrigation-source-loss family. Resume
the low-coverage inherited authoritative-body pass with plot-group/resource topology and dynamic
improvement/ownership state first. Check current callers/XML activation and whole-album semantic
dedup before promotion. If no distinct current-live root survives, keep F612/KI#934 unused and use
the next durable stone to decide Queue-009 closure rather than forcing another finding.

Exact continuation cursor:
WIP434 physical -> F612 / provisional KI#934 unused -> exclude F609, F610 and F611/KI#351 reopen ->
plot-group/resource topology -> dynamic improvement/ownership state -> remaining inherited low-
coverage authoritative CvPlot bodies -> current-content activation -> whole-album dedup -> promote
only a distinct current-live root -> freeze WIP435 regardless before Queue-009 closure/lens change.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP434
===============================================================================


===============================================================================
C031-WIP435 — QUEUE 009 CvPlot.cpp — FINAL CLOSURE / F612 UNUSED
===============================================================================

STATUS
------
Queue 009 `CvGameCoreDLL/CvPlot.cpp` protected deep-method re-audit is CLOSED.
F609 / KI#932 remains CONFIRMED.
F610 / KI#933 remains CONFIRMED.
F611 / KI#351 REOPENED/BROADENED remains CONFIRMED.
F612 / provisional KI#934 remains UNUSED and transfers to the next pending queue.
No repository/gameplay source files modified.

FINAL SIGNED-DEFENSE ACTIVATION CONTROL — SAS JUNGLE -25 IS SUPPORTED
--------------------------------------------------------------------
Current SAS practical 5242 deliberately changes `FEATURE_JUNGLE` defense from 0 to -25, with an
inline design rationale that Jungle should be less defensible/open to attack. This is therefore an
intentional signed-domain activation, not accidental XML drift.

The remaining executor audit did not find an inherited nonnegative-only contract:

* `CvPlot::defenseModifier` adds terrain/feature/improvement/city values as signed integers.
* `CvTeamAI::AI_plotDefense` forwards that signed value unchanged.
* pathfinding treats a negative destination defense as worse defensive terrain rather than wrapping
  or reversing the preference;
* attack opportunism becomes more eager against a defender on negative-defense terrain, which is
  consistent with the feature's stated design;
* guard/healing/airbase and plot-defender heuristics value such terrain less;
* the core combat-strength modifier machinery already accepts signed combat modifiers;
* current shipped terrain/improvement data do not stack an additional negative plot-defense value
  on top of Jungle, so the current minimum native plot contribution remains modest (-25).

One UI line-budget precheck reserves a help line only for `defenseModifier(...) > 0` although the
later help body prints any nonzero defense value. Negative Jungle defense can therefore make that
heuristic under-reserve one line, but this is a tooltip/layout-budget imperfection rather than a
hard gameplay-state contradiction and does not consume F612.

FRESH-WATER DEPENDENCY FAMILY — EXHAUSTED AFTER F610 / F611
------------------------------------------------------------
`CvPlot::isFreshWater()` depends on four relevant state classes:

1. whether the plot is water/impassable;
2. direct river presence;
3. adjacent water-body lake status;
4. adjacent feature `bAddsFreshWater`.

For current shipped runtime mutations:

* river-edge changes are the KI#351/F611 family;
* `FEATURE_OASIS` is the sole current AddsFreshWater feature and Global Warming removal is F610;
* current Global-Warming terrain substitutions remain ordinary passable land and do not create a
  new water/impassability fresh-water transition;
* the KI#346 Ice-area rebuild does not change representative water-body tile count and therefore
  does not turn a lake into sea merely by opening an Ice-blocked passability channel.

No separate fresh-water invalidation root remains outside F610 and the KI#351 reopen.

UNIT-VALIDITY / SAS ALL-TERRAIN MUTATION CONTROL — CLEAN
---------------------------------------------------------
A final plot-mutation check tested whether SAS's all-terrain LAND units could be incorrectly bumped
when a plot changes type/feature and `CvPlot::verifyUnitValidPlot()` runs.

The authoritative call is:

    kUnit.isValidDomain(*this)

and `CvUnit::isValidDomain(bool bWater)` explicitly returns valid for LAND on water when
`canMoveAllTerrain()` is true (and likewise for SEA on land). Thus this plot-side legality consumer
already honors the capability activated by Gunship/Airship/Recon Drone. The earlier all-terrain
roots F599 and F603 remain correctly bounded to their own AI reachability and withdrawal consumers.
No F612.

CURRENT-FORMAT CvPlot SAVE/LOAD SYMMETRY — CLEAN
-------------------------------------------------
A mechanical same-version stream comparison was performed rather than relying on visual inspection.
Current `CvPlot::read` and `CvPlot::write` each contain 61 corresponding serialization operations,
in the same semantic order.

The only syntactic representation differences are expected:

* write `areaID()` -> read temporary union member `m_iArea`, later resolved to `m_pArea`;
* write `(int)m_iRiverID` -> read int with explicit short-range validation before assigning the
  compact `short` member;
* boolean members are read through the shared temporary `bVal` and assigned in the same order.

Persisted enum maps, culture/found values, plot-group IDs, visibility/stolen visibility/blockade
counts, revealed owner/improvement/route state, build progress, culture-range counts, invisible-
visibility counts, script data, ruins name and the unit ID list are paired.

Header members absent from the stream are runtime/derived/graphical caches or pointers: border-
danger/safe-range caches, adjacency pointer/count construction state, graphical symbols/entities,
center unit/plot builder, layout-dirty/display flags and static fog/visibility caches. No omitted
current authoritative gameplay state or stream-order mismatch was established.

FINAL QUEUE-009 RECONCILIATION
------------------------------
The deep re-audit has now covered all three remaining discovery surfaces rather than merely stopping
at the current-vs-Base diff:

1. CURRENT SAS semantic deltas — fully reconciled against the original KI#346-KI#352 repairs,
   GameRecord/caching-only changes and already-owned cross-file roots;
2. INHERITED authoritative bodies — low-literal-coverage topology, culture/ownership, visibility,
   resource/plot-group, irrigation/fresh-water, area and unit-validity paths were checked against
   current callers and current data;
3. CURRENT-CONTENT activation — unusual SAS states such as all-terrain LAND units, Oasis runtime
   removal and negative Jungle defense were explicitly tested against inherited consumers.

Three live roots survive this protected re-audit:

* F609 / KI#932 — Advanced Start terrain-only revelation skips the requested plot-group refresh;
* F610 / KI#933 — Global Warming Oasis removal leaves fresh-water health/irrigation derived state
  stale;
* F611 / KI#351 REOPENED/BROADENED — the current river repair propagates irrigation additions but
  cannot tear down an already-irrigated component when its last fresh-water source disappears.

No independent fourth root survived. F612 / provisional KI#934 remains UNUSED.

CURRENT STONE AFTER C031-WIP435
-------------------------------
Protected deep-method re-audit queue 001 through 009 is COMPLETE.
Queue 009 `CvGameCoreDLL/CvPlot.cpp` is CLOSED.
F609 / KI#932 CONFIRMED.
F610 / KI#933 CONFIRMED.
F611 / KI#351 REOPENED/BROADENED CONFIRMED.
F612 / provisional KI#934 UNUSED.
Latest physical checkpoint: C031-WIP435.
No repository/gameplay source modified.

NEXT
----
Resume the ordinary pending queue after the protected 001-009 reloop. The next longest pending file
is Queue 020 `CvGameCoreDLL/CvInfo_Unit.cpp` (2,599 queued lines). Open it under F612 / provisional
KI#934 unused. First reconcile XML-info loading/serialization/accessor invariants against current
UnitInfos and the many SAS-expanded unit capability domains already discovered during Queue 007;
then audit current-vs-Base semantic changes and inherited field cross-contracts. Keep associated XML
and CvUnit consumers as supporting material, dedup against F599-F607 before promotion, and freeze the
next durable WIP regardless.

Exact continuation cursor:
WIP435 physical -> protected deep reloop 001-009 COMPLETE -> Queue 020 `CvInfo_Unit.cpp` NEXT ->
F612 / provisional KI#934 unused -> current-vs-Base CvInfo_Unit semantic inventory -> current UnitInfo
field-domain / loader / array-cardinality contracts -> consumer cross-checks against Queue-007 roots
-> whole-album semantic dedup -> promote only an independent current-live root -> freeze WIP436.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP435
===============================================================================


===============================================================================
C031-WIP436 — QUEUE 020 CvInfo_Unit.cpp — OPENING CONTRACT / DATA-CARDINALITY PASS
===============================================================================

STATUS
------
Queue 020 `CvGameCoreDLL/CvInfo_Unit.cpp` is OPEN.
F612 / provisional KI#934 remains UNUSED.
No repository/gameplay source files modified.

CURRENT-vs-BASE SEMANTIC SURFACE — SMALL AND ACCOUNTED FOR
-----------------------------------------------------------
A direct current-SAS vs Base AdvC 1.14 comparison shows that the executable semantic delta in this
2,599-line file is unusually small:

1. SAS adds `CvUnitInfo::m_eObsoleteTech`, initializes it to `NO_TECH`, loads the XML
   `<ObsoleteTech>` tag, exposes `getObsoleteTech()`, and includes it in the optional binary XML-info
   cache read/write code.
2. The four UnitInfo terrain/feature attack/defense modifier getters gain SAS define gates that
   return 0 when the corresponding optional mechanic is disabled.
3. `CvUnitInfo::isPromotionValid()` contains the already-fixed KI#324.2 integration repair: the
   inherited `getCollateralDamage()==0` veto was removed while the zero limit/max safety gates were
   retained, allowing SAS's deliberately broadened collateral promotions to work on units whose
   base collateral strength is zero.
4. The remaining Info subclasses in this file are executable-semantically inherited from Base AdvC.

BINARY XML-INFO CACHE CONTROL — DORMANT
----------------------------------------
The new ObsoleteTech member was inserted into `CvUnitInfo::read/write` without bumping that optional
cache format's local `uiFlag`. That initially looks like a cache compatibility hazard, but current
SAS ships:

    #define ENABLE_XML_FILE_CACHE 0

in `CvInfo_Base.h`. These binary Info-cache methods are compiled out. Current UnitInfos are parsed
from XML, so this does not provide a current-live F612.

OBSOLETE-TECH CROSS-FIELD AUDIT — INTENTIONAL HARD TECH CUTOFF
---------------------------------------------------------------
Current UnitInfos contain 82 non-NONE ObsoleteTech assignments. A tech-graph audit found 18 units
whose ObsoleteTech does not descend from every one of that unit's own prerequisite techs (e.g.
Bronze/Iron-Working unit branches cut off at Civil Service/Feudalism/Firearms).

That is not a contradiction with the documented feature. KI#93/Main Changes documentation defines
ObsoleteTech as a hard production cutoff once the listed technology has been discovered, intended
to remove strategically obsolete unit clutter; it is not defined as "replacement is currently
trainable." Additional controls are clean:

* no current unit has ObsoleteTech equal to its own required tech;
* no current ObsoleteTech is in an earlier/equal current tech-era/cost position than the unit's own
  required-tech set in the tested ordering;
* gameplay/AI/help consumers use `getObsoleteTech()` rather than bypassing the optional mechanic by
  reading the raw member;
* therefore the global SAS toggle coherently returns `NO_TECH` to those consumers when disabled.

The non-ancestral cases are intentional skipped-branch behavior, not F612.

FREE-PROMOTION BYPASS — DEDUP / POSITIVE CONTROL
-------------------------------------------------
`CvUnitInfo::isPromotionValid()` deliberately returns true immediately for a unit's XML free
promotion. A current-data census found 13 free UnitInfo/promotion pairs that would fail normal
player-selectable promotion eligibility (UnitCombat/effect restrictions). Most are deliberate
special-unit or UU injections whose effects remain executable.

The only mechanically inert pair family uncovered by this bypass is Airship/Robotic Recon Drone +
`PROMOTION_MOBILITY_COST`; that is already the exact root of F605/KI#928 (flat movement returns
before move-discount consumption). Do not duplicate it as F612.

PROMOTION DEPENDENCY GRAPH — CLEAN
----------------------------------
Current `CIV4PromotionInfos.xml` contains 57 promotions and 66 prerequisite edges across mandatory
and OR prerequisite fields. Mechanical graph validation found:

* zero prerequisite cycles;
* zero dangling prerequisite IDs.

Thus the recursive prerequisite calls in `CvUnitInfo::isPromotionValid()` cannot enter a current
XML recursion cycle.

UNIT MESH / BATTLE-ART CARDINALITY — CLEAN
------------------------------------------
All 131 current UnitInfos have nonempty UnitMeshGroups with positive required counts. A few values
look suspicious in isolation (`iRequired` greater than `iGroupSize`, or melee/ranged wave size
above group size), but every flagged shared unit carries the same tuple in Base AdvC; these are
Civ4 visual/battle-group semantics rather than array-cardinality mistakes.

Only one shared unit's mesh tuple differs at all between Base and SAS: the Celtic Gallic Warrior's
art-definition tag name. Counts are unchanged.

Current UnitArtStyle validation also found:

* 36 style-unit overrides;
* zero unknown UnitType references;
* zero overrides with more/fewer mesh entries than the target UnitInfo's group definitions;
* zero missing/empty Early/Middle/Late style tags;
* 286 UnitInfo/style Early/Middle/Late art references, 187 unique art tags, and zero references to
  missing current Unit Art definitions.

No art-style loader overrun, stale-value fallback or dangling-art current fixture was established.

ERA-BASED ART-SELECTOR ACTIVATION — CLEAN
-----------------------------------------
`CvUnitInfo::getArtInfo()` derives early/middle/late thresholds from `GC.getNumEraInfos()`. A
possible SAS-era-count activation was checked directly: Base AdvC and current SAS both have exactly
seven eras in the same order (Ancient, Classical, Medieval, Renaissance, Industrial, Modern,
Future). SAS has not shifted the inherited thresholds.

UNIT-CLASS INTEGRITY — CLEAN
-----------------------------
Current XML has 93 UnitClasses and 131 UnitInfos. Mechanical consistency checks found:

* every UnitInfo's Class exists;
* every UnitClass DefaultUnit exists;
* every DefaultUnit belongs to that exact class;
* no UnitClass is orphaned from all UnitInfos.

The SAS player-instance caps on Settler/recon/Airship/Drone classes are intentional data-policy
changes, not loader/reference inconsistencies.

OTHER INFO SUBCLASSES — STRONG CONTROLS
---------------------------------------
`CvSpecialUnitInfo` uses the stock three SpecialUnit definitions; current SAS differs from BtS data
only by explanatory comments. `CIV4EspionageMissionInfo.xml` is byte-identical to Base AdvC 1.14,
and the corresponding `CvEspionageMissionInfo` implementation is inherited. No SAS data activation
therefore exists for a new Queue-020 root in that subclass.

CURRENT STONE AFTER C031-WIP436
-------------------------------
Queue 020 `CvGameCoreDLL/CvInfo_Unit.cpp` — OPEN.
F612 / provisional KI#934 UNUSED.
Latest physical checkpoint: C031-WIP436.
No repository/gameplay source modified.

NEXT
----
Finish the inherited current-data cross-contract sweep for the file-owned Info classes rather than
forcing a root from data that merely differs from Base. In particular:

* bound the few PromotionInfo numeric domains expanded beyond Base (bombard/collateral/interception)
  against their current executor ceilings and dedup against KI#324.2/F604-F607;
* inspect `CvUnitInfo` static/global capability summaries and remaining array/vector accessors for a
  current data state whose parser/validator contract disagrees with a consumer;
* reconcile low-literal-coverage inherited helpers against the album and current callers;
* if no independent live contradiction survives, close Queue 020 with F612 unused and advance to
  Queue 021 `CyPlayer.cpp` rather than manufacturing KI#934.

Exact continuation cursor:
WIP436 physical -> Queue 020 OPEN -> F612 / provisional KI#934 unused -> PromotionInfo expanded
numeric-domain executor ceilings -> static/global UnitInfo capability summaries -> remaining
inherited parser/accessor cross-contracts -> whole-album dedup -> promote only independent
current-live root -> close/freeze WIP437 if frontier exhausts; otherwise preserve exact candidate
state in WIP437.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP436
===============================================================================


===============================================================================
C031-WIP437 — QUEUE 020 CvInfo_Unit.cpp — F612 / KI#934 CONFIRMED; QUEUE CLOSED
===============================================================================

STATUS
------
Queue 020 `CvGameCoreDLL/CvInfo_Unit.cpp` is CLOSED.
F612 / KI#934 CONFIRMED.
Queue 021 `CvGameCoreDLL/CyPlayer.cpp` is OPEN.
F613 / provisional KI#935 is UNUSED.
No repository/gameplay source files modified.

F612 / KI#934 — CITY BOMBARD DAMAGE IS WIRED TO THE WRONG BOMBARD MECHANIC FOR AIR BOMBERS
------------------------------------------------------------------------------------------------
Current SAS deliberately makes `PROMOTION_CITY_BOMBARD_DAMAGE` selectable by both
`UNITCOMBAT_SIEGE` and `UNITCOMBAT_AIR_BOMBER` and gives it:

    iBombardRateChange = 10

This field has one runtime storage path on a promoted unit:

    CvUnit::setHasPromotion(...)
        -> changeExtraBombardRate(kPromo.getBombardRateChange() * iChange)

and `CvUnit::bombardRate()` consumes it only for the ordinary adjacent Civ4 BOMBARD mechanic:

    return m_pUnitInfo->getBombardRate() + getExtraBombardRate();

Aircraft city bombing is a distinct executor. `CvUnit::airBombBaseRate()` returns only the UnitInfo
`iBombRate`, and `airBombCurrRate()` merely scales that base by current hit points. Neither function
reads `getExtraBombardRate()` or the promotion's `iBombardRateChange`.

Current shipped fixtures make the mismatch direct:

    Bomber:         DOMAIN_AIR, UNITCOMBAT_AIR_BOMBER, iBombRate=16, iBombardRate=0
    Stealth Bomber: DOMAIN_AIR, UNITCOMBAT_AIR_BOMBER, iBombRate=20, iBombardRate=0

Therefore City Bombard Damage leaves their normal AIRBOMB city-defense damage at 16/20 (before the
separate health/defense scaling already audited under F606). The +10 promotion does not increase the
mission that its SAS name and eligibility say it should improve.

WRONG-MISSION ACTIVATION — NOT MERELY AN INERT BONUS
------------------------------------------------------
The same mismatch also activates the opposite mechanic. `CvUnit::canBombard(CvPlot const&)` has no
AIR-domain exclusion; it requires positive `bombardRate()`, attack availability, non-cargo status
and an adjacent bombardable city. `CvSelectionGroup::canDoMission(MISSION_BOMBARD)` delegates to
that method and likewise has no AIR-domain exclusion.

An unpromoted current Bomber has ordinary `bombardRate()==0` and cannot use adjacent BOMBARD. Once
City Bombard Damage is acquired:

    base iBombardRate = 0
    promotion change  = +10
    bombardRate()     = 10

so a DOMAIN_AIR Bomber can become eligible for the ordinary adjacent siege/naval city-bombard
mission while its normal ranged AIRBOMB damage remains unchanged.

HISTORY / INTENT — SAS DOMAIN EXPANSION WITHOUT EXECUTOR ADAPTATION
-------------------------------------------------------------------
Base AdvC's upstream Accuracy promotion used `iBombardRateChange` for Siege, where that field is the
correct ordinary-BOMBARD executor contract.

SAS practical 4569 (`f774063b13ef8b6addc8d7f195b08643d7c646d2`) deliberately:

* renamed Accuracy to "City Bombard Damage";
* raised/kept the promotion as a city-bombard effect;
* added an XML comment `give city bombard damage to more unit types`;
* added the then-generic air combat class while explicitly noting the intent to later distinguish
  bombers from fighters.

SAS practical 4616 (`9b2a569e43...`) then introduced `UNITCOMBAT_AIR_BOMBER` and moved this
promotion's air eligibility specifically onto that new bomber class. The content intent is therefore
not inferred from a stray XML value: SAS deliberately extended an ordinary BombardRate promotion to
a unit domain whose city-bombing executor uses the separate BombRate field.

LIVE ACQUISITION / UI / AI PROOF
--------------------------------
The promotion has no tech prerequisite and no promotion prerequisite. A sufficiently experienced
current AIR_BOMBER can therefore select it normally.

`CvGameTextMgr` displays nonzero `getBombardRateChange()` through the ordinary bombard promotion
help text, so the player is shown the +10 bombard benefit.

`CvUnitAI::AI_promotionValue` also assigns positive value to `getBombardRateChange()` (especially for
`UNITAI_ATTACK_CITY`) without checking that AIR_BOMBER's actual AIRBOMB executor ignores the field.
Thus eligibility, UI and AI valuation all expose the stale field-domain contract.

DEDUP BOUNDARY
--------------
This is independent of the prior aircraft roots:

* F606/KI#929: stale IgnoreBuildingDefense compensation inside `airBombDefenseDamage()` after SAS
  removed that aircraft immunity;
* F612/KI#934: City Bombard Damage modifies ordinary BombardRate while current aircraft city bombing
  consumes BombRate.

Fixing F606 does not make the promotion improve AIRBOMB. Fixing F612 does not remove F606's defense
scaling error. The album contained no earlier `PROMOTION_CITY_BOMBARD_DAMAGE` / BombardRate-vs-
BombRate root.

REPAIR BOUNDARY
---------------
Do not blindly add `getExtraBombardRate()` to `airBombCurrRate()`: that accessor also participates in
non-city aircraft bombing, so doing so could broaden the explicitly city-focused promotion into an
improvement/route-bombing bonus.

A conservative repair should instead preserve the ordinary BombardRate effect for Siege while:

1. deliberately applying the City Bombard Damage promotion to AIR-unit *city-defense* AIRBOMB
   damage (or introducing a dedicated air-city-bombard modifier); and
2. preventing DOMAIN_AIR units from acquiring the ordinary adjacent `MISSION_BOMBARD` merely
   because promotion-derived `bombardRate()` becomes positive.

FINAL SPECIALIZED-EFFECT RECONCILIATION
---------------------------------------
After F612, the remaining current selectable specialized PromotionInfo effects were cross-checked
against current eligible unit domains and their executors. Air Range, Evasion, Interception,
Collateral, Cargo, movement, healing, withdrawal and related effects all have live current executor
paths. Interception acquisition obeys the current 100% cap; collateral protection is bounded at
zero damage; the broad collateral promotions are already covered by the repaired KI#324.2 path.
The only mechanically inert specialized free-promotion combination remains the previously confirmed
F605/KI#928 Airship/Recon-Drone Mobility Cost + FlatMovementCost root.

QUEUE-020 CLOSURE
-----------------
Queue 020 has now covered:

* the complete current-vs-Base executable semantic delta;
* ObsoleteTech load/accessor/toggle and current tech-graph contracts;
* optional binary XML-cache activation (compiled out in shipped SAS);
* free-promotion bypasses and dedup against Queue-007 roots;
* promotion prerequisite graph/cardinality;
* UnitClass integrity;
* Unit mesh, battle-art and UnitArtStyle cardinality/reference integrity;
* era-based art selection against current era count/order;
* SpecialUnit and EspionageMission subclasses;
* PromotionInfo numeric ceilings and specialized-effect domain/executor contracts.

No second independent current-live Queue-020 root survives the final frontier.

CURRENT STONE AFTER C031-WIP437
-------------------------------
Queue 020 `CvGameCoreDLL/CvInfo_Unit.cpp` — CLOSED.
F612 / KI#934 CONFIRMED.
Queue 021 `CvGameCoreDLL/CyPlayer.cpp` — OPEN.
F613 / provisional KI#935 UNUSED.
Latest physical checkpoint: C031-WIP437.
No repository/gameplay source modified.

NEXT
----
Audit Queue 021 `CvGameCoreDLL/CyPlayer.cpp` as a Python-wrapper boundary rather than treating its
2,503 lines as ordinary gameplay logic. Mechanically compare wrappers to the underlying CvPlayer
signatures and current Python call sites, prioritizing:

* argument-order/default/sentinel mismatches;
* narrowing or bool/int conversions that lose current 48-player / expanded-enum state;
* wrappers whose const/read-only appearance hides state mutation or whose ownership/lifetime return
  differs from the native contract;
* current SAS-added/changed wrappers and Python-exposed functions used by the redesigned UI;
* album dedup against earlier Python/signature findings before any promotion.

Exact continuation cursor:
WIP437 physical -> Queue 020 CLOSED / F612 KI#934 confirmed -> Queue 021 CyPlayer.cpp OPEN ->
F613 / provisional KI#935 unused -> current-vs-Base wrapper inventory -> wrapper/native signature
pairing -> current Python-call-site activation -> whole-album dedup -> promote only a current-live
independent wrapper root -> freeze WIP438 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP437
===============================================================================


===============================================================================
C031-WIP438 — QUEUE 021 CyPlayer.cpp — CLOSED WITH F613 UNUSED
===============================================================================

STATUS
------
Queue 021 `CvGameCoreDLL/CyPlayer.cpp` is CLOSED.
F613 / provisional KI#935 remains UNUSED.
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` is OPEN.
No repository/gameplay source files modified.

CURRENT SAS DELTA — TWO WRAPPERS, BOTH ACCOUNTED FOR
----------------------------------------------------
Current SAS differs from Base AdvC 1.14 in only two executable CyPlayer bodies:

* `calculateUnitCostBreakdown()`;
* `calculateUnitSupplyBreakdown()`.

Both were introduced for the Military Advisor Summary and expose exact intermediate native support-
cost counters/subtotals. Their tuple order matches the sole current Python consumer exactly.

Their direct use of the internal reference-output CvPlayer overloads intentionally returns raw
formula subtotals even during anarchy. That was the already-known KI#219 UI misuse, not a native
wrapper arithmetic defect. Current practical 6171 fixes the consumer: `CvMilitaryAdvisor.py`
retains the useful free/paid counters but explicitly zeroes displayed charged Unit Cost/Supply and
totals while `pPlayer.isAnarchy()` is true. Do not reopen KI#219 from the unchanged raw bindings.

BOOST-PYTHON BINDING / IMPLEMENTATION RECONCILIATION — CLEAN
-------------------------------------------------------------
Both `CyPlayerInterface1.cpp` and `CyPlayerInterface2.cpp` were mechanically reconciled against the
current wrapper implementation:

* 436 Python bindings, with zero duplicate public Python method names;
* every bound non-inline CyPlayer method has an implementation;
* methods reported as bound-without-`.cpp`-definition (`isNone`, several identity/getter wrappers,
  etc.) are intentional inline methods in `CyPlayer.h`;
* the few non-helper `.cpp` definitions not published (`countReligionSpreadUnits`,
  `countNumCitiesConnectedToCapital`, `getImprovementUpgradeRate`) have no current SAS Python call
  sites and do not create a shipped current-live mismatch.

ADV C++ WRAPPER SEMANTIC-CHANGE PASS — CLEAN
--------------------------------------------
The inherited AdvC-vs-K-Mod wrapper changes were checked against native signatures and current
callers, especially the changes with real semantic adaptation rather than formatting:

* `findStartingPlot(bool)` maps the removed native randomize argument into AdvC's
  `setRandomWBStart` state; current direct CyPlayer use is the WB scenario path intended by the
  surrounding comment;
* `canFound` and `canBuild` add safer map/native-plot validation;
* civic APIs translate Python sequences into the enum-map native representation;
* `calculatePollution` casts Python's retained PollutionTypes values to the native AdvC flags;
* `canRevolution(0)` intentionally remains the legacy Python “can do any revolution” query while
  `canAdopt(list)` handles a proposed civic map explicitly;
* `createGreatPeople` and `canSplitArea` validate map objects/area IDs before forwarding;
* history wrappers use `getHistorySafe` rather than unchecked history-array access;
* AI-area wrappers receive actual city/plot areas at all current SAS call sites;
* favorite civic/religion, scoreboard and random-civ/leader wrappers preserve their documented
  AdvC information/state contracts.

EMPTY CIVIC-LIST CONTROL — SAFE
--------------------------------
Current Domestic Advisor intentionally calls:

    player.getCivicUpkeep([], False)

AdvC's wrapper first copies the player's current CivicMap and then calls `pyListToCivicMap`.
`CvDLLPythonIFaceBase::putSeqInArray` explicitly initializes the destination pointer to NULL and
returns 0 when the Python sequence length is less than 1. `pyListToCivicMap` therefore performs no
overwrite for `[]`; the copied current civics remain intact. There is no zero-length allocation or
out-of-bounds CivicOption read.

POINTER / NULL CONTRACT PASS — NO CURRENT-LIVE VIOLATION
---------------------------------------------------------
A mechanical scan identified the legacy wrappers that dereference `CyCity*`, `CyPlot*`, `CyArea*`
or `CyUnit*` parameters without a local wrapper-side null test. Current SAS Python call-site review
found concrete engine objects at those live call sites; several are callback/API contracts where
null is not a supported argument. AdvC additionally hardened selected wrappers such as `canBuild`
with native-object null checks. No current shipped caller supplies Python `None` into an unsafe
legacy dereference.

INTEGER / ENUM / RETURN CONTRACTS — CLEAN
-----------------------------------------
The AdvC return-type repairs (`getExpInBorderModifier` int, current native `getRealPopulation` int,
bool false fallbacks for boolean APIs) are aligned across native declaration, CyPlayer declaration
and Boost binding. Current 48-player IDs travel through `int`/PlayerTypes casts; no CyPlayer wrapper
uses a 32-bit bitmask to encode the expanded player set.

QUEUE-021 CLOSURE
-----------------
The audit covered the complete SAS delta, Boost binding/body symmetry, AdvC-vs-K-Mod semantic
wrapper changes, live Python call sites, Python sequence conversion, pointer/null contracts and
integer/enum/lifetime surfaces. No independent current-live Queue-021 root survives.

CURRENT STONE AFTER C031-WIP438
-------------------------------
Queue 021 `CvGameCoreDLL/CyPlayer.cpp` — CLOSED.
F613 / provisional KI#935 UNUSED.
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
Latest physical checkpoint: C031-WIP438.
No repository/gameplay source modified.

NEXT
----
Audit Queue 022 `CvGameCoreDLL/TrueStarts.cpp` under F613/KI#935 unused. Start by reconstructing the
current True Starts activation/configuration/data pipeline (scenario/map-script/start-position
inputs -> candidate scoring/assignment -> player starting plots), then compare current SAS against
Base AdvC where applicable. Prioritize:

* player/team cardinality and 48-civ indexing;
* coordinate wrapping/distance and map-size assumptions;
* invalid/missing civ/leader/start-data fallbacks;
* assignment uniqueness and team-clustering constraints;
* current scenario/map-script activation rather than unsupported malformed input;
* whole-album dedup with StartingPositionIteration / original map-start findings before promotion.

Exact continuation cursor:
WIP438 physical -> Queue 021 CLOSED / F613 unused -> Queue 022 TrueStarts.cpp OPEN -> activation and
data-pipeline map -> player/team/cardinality/index contracts -> coordinate/distance/assignment
invariants -> current map/scenario fixtures -> whole-album dedup -> promote only independent live
root -> freeze WIP439 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP438
===============================================================================


===============================================================================
C031-WIP439 — QUEUE 022 TrueStarts.cpp — F613 / KI#935 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / provisional KI#936 is UNUSED.
No repository/gameplay source files modified.

CURRENT-vs-BASE CODE BOUNDARY
-----------------------------
Current SAS `TrueStarts.cpp` is executable-semantically identical to Base AdvC 1.14. The direct
file diff contains signature/comment-formatting changes only. Queue-022 findings therefore require
an inherited AdvC defect activated by SAS configuration/content or a SAS True-Starts data-integration
defect; there is no unexplained SAS C++ rewrite to blame.

F613 / KI#935 — TRUE STARTS USES CIV/LEADER-PAIR COUNT TWICE, SO 35-48 RANDOM PLAYERS CAN EXHAUST DISTINCT CIVS WHILE UNIQUENESS STAYS FORCED
------------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::changeCivs()` correctly builds two different AI candidate cardinalities:

    aeValidAICivs   = distinct valid True-Starts civilization IDs
    m_validAICivs   = valid (civilization, leader) pairs

The pair list is the correct object for the first hard guard: there must be enough actual selectable
civ/leader combinations to assign every True-Starts player. Immediately afterward, however, the
code decides whether duplicate civilizations must be permitted:

    if (m_validAICivs.size() < m_truPlayers.size() ||
        aeValidHumanCivs.size() < m_truHumans.size())
    {
        bUniqueCivs = false;
    }

The AI term repeats the **pair-list** test from the hard guard instead of testing the already-built
`aeValidAICivs` distinct-civilization list. Because the previous hard guard has already returned if
`m_validAICivs.size() < m_truPlayers.size()`, that first term is unreachable as true here. AI-player
shortage can therefore never disable `bUniqueCivs`.

CURRENT SAS CARDINALITIES MAKE THE DEFECT LIVE
-----------------------------------------------
Current data was mechanically enumerated:

    valid AI-playable True-Starts civilizations: 34
    valid True-Starts AI civilization/leader pairs: 52
    current playable/AI-playable major civilizations: 36
    MAX_CIV_PLAYERS: 48
    WORLDSIZE_SAS40 default players: 40
    WORLDSIZE_SAS48 default players: 48

Thus a 48-random-player True Starts game passes the hard leader-pair guard because 52 >= 48. The
broken uniqueness check then also sees 52 >= 48 and keeps `bUniqueCivs=true`, despite having only 34
distinct candidate civilizations.

The assignment loop explicitly skips a candidate when:

    bUniqueCivs && m_civTaken.get(candidateCiv)

After 34 successful assignments every distinct True-Starts civilization is taken. Players 35-48
can still have many unused leaders, but every pair belongs to an already-taken civ; no candidate can
become `eeBestFit`. The later finalization consequently reaches the existing `No civ found for
player` error path for those unassigned True-Starts players instead of the intended duplicate-civ
fallback.

This is not merely a theoretical 48-slot compile configuration. SAS ships its explicit 40- and
48-player world sizes and a 48-major-player DLL, so the supported normal setup can exceed the
34-distinct-civ boundary directly.

BASE ADVC ACTIVATION BOUNDARY / HISTORY
---------------------------------------
The duplicated test is inherited unchanged from AdvC's original True Starts merge, practical 3579
(`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`). The same hunk introduced both candidate vectors and
the two checks.

Base AdvC 1.14 has:

    MAX_CIV_PLAYERS = 18

while its True Starts data already provides 34 distinct major civilizations. The typo was therefore
normally dormant upstream: an ordinary Base AdvC game cannot have enough major players to exhaust
the distinct True-Starts civ pool.

SAS raises `MAX_CIV_PLAYERS` to 48 and adds 24/32/40/48-player scales while retaining the inherited
check. F613 is therefore best classified as an inherited AdvC cardinality defect activated by SAS's
expanded supported player domain.

REPAIR BOUNDARY
---------------
The local variable/data model already expresses the intended distinction. The conservative repair is:

    if (aeValidAICivs.size() < m_truPlayers.size() ||
        aeValidHumanCivs.size() < m_truHumans.size())
    {
        bUniqueCivs = false;
    }

That changes only the duplicate-civilization policy when there are fewer distinct valid civs than
players, while preserving the earlier hard guard that requires enough civ/leader combinations.

DEDUP
-----
Whole-album/Known-Issues searches found no prior root owning this `bUniqueCivs` / distinct-civ-vs-
leader-pair cardinality defect. Prior True Starts findings concern tech-removal caches, latitude
contracts, changeLeader/UWAI cache invalidation, start normalization and other lifecycle/state
interactions; none repairs this assignment-pool exhaustion.

SECOND CURRENT DATA LEAD — NOT YET F614
---------------------------------------
The same current-data census found a separate SAS integration gap:

    current major playable civs = 36
    current TruCiv records       = 34

The missing major civilizations are exactly:

    CIVILIZATION_IRELAND
    CIVILIZATION_KINGDOM_OF_BENIN

Both are `bPlayable=1` and `bAIPlayable=1`. Their current leaders (Grace O'Malley, Michael Collins,
Ewuare) likewise have no TruLeader records.

This matters more than missing geographic flavor: `changeCivs()` builds the random True-Starts civ
pool solely by iterating `TruCiv`, so Ireland and Benin can never be selected for a player whose civ
is Random under True Starts. The option help states that players set to a Random civilization
receive a civilization matching their starting location; it does not state that mod-added playable
civilizations are excluded.

This is a strong possible F614 / KI#936 SAS content-integration root, but it is deliberately not
promoted in this checkpoint. Finish the civ-introduction / True-Starts-data history and determine
whether the omission was an explicit unsupported-content choice or simply missed integration.

CURRENT STONE AFTER C031-WIP439
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / provisional KI#936 UNUSED.
Latest physical checkpoint: C031-WIP439.
No repository/gameplay source modified.

NEXT
----
Finish the Ireland/Kingdom-of-Benin TruCiv/TruLeader integration proof, then continue the remaining
True Starts data/assignment invariants: duplicate Tru mappings, target latitude/longitude ranges,
bonus civ/region references, assignment uniqueness, contemporaries and bonus swap destination
contracts. Dedup against StartingPositionIteration and prior map-start findings before promotion.

Exact continuation cursor:
WIP439 physical -> Queue 022 OPEN -> F613/KI#935 confirmed -> F614/KI#936 unused -> missing Ireland +
Kingdom of Benin TruCiv/leader integration history/intent -> remaining XML reference/range/assignment
invariants -> whole-album dedup -> promote only independent current-live root -> freeze WIP440
regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP439
===============================================================================


===============================================================================
C031-WIP440 — QUEUE 022 TrueStarts.cpp — F614 / KI#936 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / provisional KI#937 is UNUSED.
No repository/gameplay source files modified.

F614 / KI#936 — SAS'S IRELAND AND KINGDOM OF BENIN ARE ABSENT FROM TRUE-STARTS CIV DATA, SO RANDOM + TRUE STARTS CAN NEVER SELECT THEM
-------------------------------------------------------------------------------------------------------------------------------------
Current SAS contains 36 major civilizations that are both playable and AI-playable, but
`Assets/XML/TrueStarts/CIV4TruCivInfos.xml` still contains only 34 major-civilization records.
The exhaustive set difference is exactly:

    CIVILIZATION_IRELAND
    CIVILIZATION_KINGDOM_OF_BENIN

Both are ordinary current playable/AI-playable civilizations. Their current leaders are likewise
absent from `CIV4TruLeaderInfos.xml`:

    LEADER_GRACE_OMALLEY
    LEADER_MICHAEL_COLLINS
    LEADER_EWUARE

The missing TruLeader records are useful breadth, but the missing TruCiv records are the decisive
selection defect.

CURRENT-LIVE RANDOM + TRUE STARTS EXECUTOR
------------------------------------------
The `TrueStarts` constructor records a normal player in `m_truPlayers` when
`CvInitCore::wasCivRandomlyChosen(player)` is true (plus the separate documented network/scenario
cases). A manually selected civilization in an ordinary single-player game is therefore left alone;
this root is specifically about players whose civilization is Random while True Starts is active.

`TrueStarts::changeCivs()` does not construct its candidate civilization pool from
`CIV4CivilizationInfos.xml`. It constructs it exclusively by iterating the TruCiv info class:

    FOR_EACH_ENUM_RAND(TruCiv, mapRand())
    {
        CvTruCivInfo const& kTruCiv = GC.getInfo(eLoopTruCiv);
        CivilizationTypes const eCiv = kTruCiv.getCiv();
        ...
        if (kLoopCiv.isPlayable())
            aeValidHumanCivs.push_back(eCiv);
        if (kLoopCiv.isAIPlayable())
            aeValidAICivs.push_back(eCiv);
    }

The later leader-pair lists are then built only by pairing LeaderHead entries with those already
accepted civilization IDs. A playable civilization with no `CvTruCivInfo` therefore has no route
into `m_validHumanCivs` or `m_validAICivs` at all.

Consequently, although ordinary Random civilization selection can use Ireland and Kingdom of Benin,
a Random player processed by True Starts can never finish as either civilization. The option help
states that players set to a Random civilization receive a civilization matching their starting
location; it does not document a smaller 34-civ subset or exclusion of SAS-added major civs.

CUSTOM-CIV INTEGRATION IS AN EXPLICIT TRUE-STARTS DATA CONTRACT
---------------------------------------------------------------
The original AdvC True Starts schema comments deliberately make mod integration lightweight. For a
new civilization, latitude and longitude are mandatory; the remaining TruCiv preference fields are
optional. The original schema explains why:

    All other elements are optional (minOccurs 0). So that mods with
    additional civs need to define only the geo coordinates,
    which are easy to look up ...

That establishes that missing custom civilizations are not an inherent limitation of the True
Starts implementation. The data format was explicitly designed so mods adding civilizations could
supply only a minimal TruCiv entry rather than reproduce all of AdvC's climate/geography research.

SAS HISTORY / OMISSION BOUNDARY
-------------------------------
Kingdom of Benin was introduced in SAS practical 4448
(`d2eae21e1ef676a01c36e1f79817e0b38b5362b3`, 2025-04-01). That large civilization/leader/content
commit does not touch any TrueStarts XML file. Later Benin work replaces/refines its leader and
continues to treat the civilization as ordinary playable content, but no later SAS commit adds a
TruCiv record.

Ireland was introduced in practical 5462
(`d191fe898b3ac9b2dd8830ff55a029294b404f3b`, 2026-02-16) with Grace O'Malley and Michael Collins.
That commit likewise does not touch the TrueStarts XML files.

A history-wide scan of SAS TrueStarts-data changes found only maintenance of already-existing
upstream records in the relevant period:

* practical 4743 — Montezuma -> Moctezuma TruLeader rename;
* practical 4749 — CHINESE_LEADER -> MAO_ZEDONG TruLeader rename;
* practical 4878 — Viking -> Scandinavia TruCiv/TruBonus references.

No SAS commit adds Ireland or Kingdom of Benin to `CIV4TruCivInfos.xml`. No current documentation or
commit message located by the audit declares either playable civilization intentionally unsupported
by True Starts.

TRULEADER BREADTH
-----------------
The three SAS leaders also lack TruLeader records. This is not what excludes their civilizations:
`changeCivs()` iterates ordinary LeaderHeadInfos once a civilization is present in the TruCiv pool.
Missing TruLeader data is handled by the contemporaries/leader-scoring code as absent optional
historical information. It does, however, mean that after adding the missing TruCiv records the new
leaders would lack the historical-reign contribution used for contemporaneity/leader fitness.
Adding Ewuare, Grace O'Malley and Michael Collins TruLeader records is therefore the natural complete
content integration, while only the two TruCiv records are required to repair the hard exclusion.

REPAIR BOUNDARY
---------------
The conservative content repair is to add at least one `TruCivInfo` for each missing major civ:

    CIVILIZATION_KINGDOM_OF_BENIN
    CIVILIZATION_IRELAND

with valid geographic target coordinates. The schema intentionally permits all other TruCiv fields
to be omitted. For full-quality True Starts scoring, also add TruLeader records for Ewuare, Grace
O'Malley and Michael Collins and, where historically justified, bonus encouragement/discouragement
entries. No C++ change is required for the core F614 selection repair.

DEDUP
-----
Whole-album/Known-Issues searches found no existing root owning missing SAS civilization records in
True Starts. F613 is independent: F613 repairs distinct-civilization cardinality when many random
players need assignments; even with that typo fixed, Ireland and Benin remain absent from the
candidate pool. Adding Ireland/Benin does not repair F613's 35-48-player uniqueness bug.

CURRENT STONE AFTER C031-WIP440
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / provisional KI#937 UNUSED.
Latest physical checkpoint: C031-WIP440.
No repository/gameplay source modified.

NEXT
----
Continue the remaining True Starts data/algorithm audit under F615 unused. Mechanically validate all
TruCiv/TruLeader/TruBonus references and coordinate/range contracts, then audit
`initContemporaries`, `calcFitness`, wrapped latitude/longitude distance, assignment uniqueness and
bonus-swap destination invariants. Distinguish malformed unsupported XML from shipped-data states
and dedup against prior StartingPositionIteration/latitude/True-Starts findings before promotion.

Exact continuation cursor:
WIP440 physical -> Queue 022 OPEN -> F613/KI#935 + F614/KI#936 confirmed -> F615/KI#937 unused ->
complete TrueStarts XML referential/range census -> wrapped geo-distance/current map activation ->
contemporaries/fitness -> bonus swapping -> assignment/finalization -> whole-album dedup -> promote
only independent current-live root -> freeze WIP441 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP440
===============================================================================


===============================================================================
C031-WIP441 — QUEUE 022 TrueStarts.cpp — F615 / KI#937 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED (with additional fixed-civ NULL-dereference breadth below).
F615 / KI#937 CONFIRMED.
F616 / provisional KI#938 UNUSED.
No repository/gameplay source files modified.

F614 BREADTH UPDATE — A FIXED IRELAND/BENIN CAN ALSO NULL-DEREFERENCE TRUE-STARTS FITNESS
---------------------------------------------------------------------------------------
The post-WIP440 geo-distance audit found a stronger consequence of F614's missing TruCiv records.
In an ordinary single-player True Starts game, a manually selected civilization is not placed in
`m_truPlayers`; `changeCivs()` records it as a fixed assignment in `m_civs` instead. If another
player is Random, True Starts evaluates candidates for that player through `calcFitness()`.

The geographic-distance portion deliberately includes already-fixed civilizations:

    FOR_EACH_NON_DEFAULT_PAIR(m_civs, Player, CivilizationTypes)
    {
        ...
        CvTruCivInfo const& kLoopTruCiv =
                *m_truCivs.get(perPlayerVal.second);
        ...
    }

There is no NULL test. Thus a normal mixed setup such as:

    human = manually selected CIVILIZATION_IRELAND
    AI    = Random
    True Starts = enabled

puts Ireland into `m_civs`; evaluating any random candidate then dereferences the missing Ireland
TruCiv pointer. Kingdom of Benin has the same defect. This is same-root severity breadth of F614,
not a new finding: adding the missing TruCiv records repairs both Random-pool exclusion and this
fixed-civ dereference.

F615 / KI#937 — EMPTY-NEW-WORLD TRUE STARTS USES +30 DEGREES INSTEAD OF -30 DEGREES AS THE AMERICAS LONGITUDE CUTOFF
--------------------------------------------------------------------------------------------------------------------
For map scripts whose setup deliberately leaves the New World empty (`Terra`, `Earth2`, and several
supported custom-option combinations), `TrueStarts::changeCivs()` uses a first pass intended to
exclude civilizations from the Americas so that Old-World starting slots are filled by Old-World
civilizations when enough are available.

The inherited condition is:

    int iLongitudeTimes10 = kTruCiv.get(CvTruCivInfo::LongitudeTimes10);
    int iLatitudeTimes10 = kTruCiv.get(CvTruCivInfo::LatitudeTimes10);
    if (iLongitudeTimes10 < 300 || // Americas
        // Future-proof for Australia
        (iLongitudeTimes10 > 1100 && iLatitudeTimes10 < -100))
    {
        continue;
    }

The XML/schema uses ordinary signed geographic longitude in tenths of degrees and explicitly states
that a negative sign means the western hemisphere. Current examples include:

    America      -770   (-77.0 degrees)
    Aztec        -991   (-99.1 degrees)
    Native Am.   -900   (-90.0 degrees)
    England         1   (+0.1 degrees)
    France         24   (+2.4 degrees)
    Germany       130   (+13.0 degrees)
    Rome          125   (+12.5 degrees)
    Spain         -40   (-4.0 degrees)
    Portugal      -92   (-9.2 degrees)

Therefore `< 300` means **west of +30 degrees east**, not “Americas.” It excludes most of Europe and
western Africa along with the Americas. The source comment and data convention make the likely typo
particularly clear: the intended Americas cutoff is west of roughly 30 degrees west, i.e. `< -300`.

CURRENT DATA / ORDINARY GAMEPLAY CONSEQUENCE
--------------------------------------------
An exhaustive current TruCiv census gives:

    total current TruCiv records: 34
    surviving first-pass records with the shipped `< 300` test: 14
    excluded records: 20

The 14 surviving civilizations are Arabia, Babylon, China, Egypt, Ethiopia, India, Japan, Khmer,
Korea, Mongol, Persia, Russia, Sumeria and Zulu. The excluded set contains all American civs as
intended, but also Byzantium, Carthage, Celt, England, France, Germany, Greece, Holy Rome, Mali,
Netherlands, Ottoman, Portugal, Rome, Spain and Scandinavia.

For an Empty-New-World map with at most 14 True-Starts players, those 14 surviving entries satisfy
the required candidate count, so `changeCivs()` breaks after pass 0. The wrongly excluded European
and western-African civilizations never reach the valid-civ pool at all. This affects ordinary
supported Terra/True-Starts games, not malformed XML or an exotic external API state.

Even when there are more than 14 players and the fallback second pass runs, the bad boundary still
changes the intended first-pass population and composes with a separate pass-accumulation issue
currently being audited under F616.

HISTORY / ACTIVATION BOUNDARY
-----------------------------
The condition was introduced unchanged in AdvC's original True Starts merge, practical 3579
(`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`, 2022-01-30), alongside the signed-longitude schema and
comments. It remains identical in Base AdvC 1.14 and current SAS.

This is therefore an inherited AdvC True Starts bug, already live upstream on Empty-New-World games,
not a SAS-authored C++ regression. SAS retains and prominently supports the relevant map scripts and
True Starts option, so it remains current-live in AdvC-SAS.

REPAIR BOUNDARY
---------------
The narrow semantic repair is to correct the Americas cutoff to the signed-western value intended by
the comment/data convention, most plausibly:

    if (iLongitudeTimes10 < -300 ||
        (iLongitudeTimes10 > 1100 && iLatitudeTimes10 < -100))

The exact historical cutoff could be retuned if desired, but it must not classify London, Paris,
Rome, Madrid and the rest of western Europe as American merely because their longitudes are below
+30 degrees east.

DEDUP
-----
Whole-album/Known-Issues search found no prior root for the Empty-New-World longitude sign/cutoff.
It is independent of F613 (large-player distinct-civ cardinality) and F614 (missing SAS TruCiv
records). Adding Ireland/Benin does not repair the bad longitude comparison; correcting the
comparison does not add the missing civ data or repair F613.

CURRENT STONE AFTER C031-WIP441
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / provisional KI#938 UNUSED.
Latest physical checkpoint: C031-WIP441.
No repository/gameplay source modified.

NEXT
----
Finish the immediately adjacent two-pass candidate accumulation audit. The first-pass vectors are
not cleared before pass 2, so all first-pass civilizations appear to be appended again when the
Empty-New-World fallback needs the full civ pool. Quantify current Base/SAS activation and downstream
bias/cardinality consequences, classify independently from F615 if warranted, then continue
geo-distance/contemporaries/bonus-swap/finalization invariants and freeze WIP442 regardless.

Exact continuation cursor:
WIP441 physical -> Queue 022 OPEN -> F613/F614/F615 confirmed -> F616/KI#938 unused -> Empty-New-
World pass-2 duplicate accumulation proof -> geo-distance/contemporaries -> bonus swapping -> final
assignment/sanitize invariants -> whole-album dedup -> promote only independent current-live root ->
freeze WIP442 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP441
===============================================================================


===============================================================================
C031-WIP442 — QUEUE 022 TrueStarts.cpp — F616 / KI#938 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / provisional KI#939 UNUSED.
No repository/gameplay source files modified.

F616 / KI#938 — EMPTY-NEW-WORLD FALLBACK PASS APPENDS TO FIRST-PASS CIV VECTORS, SO THE SUPPOSED DISTINCT-CIV POOL CONTAINS DUPLICATES
---------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::changeCivs()` performs up to two passes when `m_bEmptyNewWorld` is true. Pass 0 tries to
restrict the candidate pool geographically. If those candidates are insufficient, pass 1 is intended
to broaden the pool to all available True-Starts civilizations:

    std::vector<CivilizationTypes> aeValidHumanCivs;
    std::vector<CivilizationTypes> aeValidAICivs;
    ...
    for (int iPass = 0; iPass < 2; iPass++)
    {
        FOR_EACH_ENUM_RAND(TruCiv, mapRand())
        {
            ...
            if (m_bEmptyNewWorld && iPass == 0)
            {
                ... geographical exclusion ...
            }
            if (kLoopCiv.isPlayable())
                aeValidHumanCivs.push_back(eCiv);
            if (kLoopCiv.isAIPlayable())
                aeValidAICivs.push_back(eCiv);
        }
        if (aeValidHumanCivs.size() >= m_truHumans.size() &&
            aeValidAICivs.size() >= m_truPlayers.size())
        {
            break;
        }
    }

Neither vector is cleared before pass 1, and there is no `already present` test. Every civilization
accepted in pass 0 is therefore appended a second time when pass 1 broadens to all civs.

CURRENT RAW CARDINALITIES
-------------------------
With the current shipped F615 `< +300` cutoff:

    pass-0 distinct TruCivs: 14
    full TruCiv pool:        34
    pass-1 accumulated entries: 14 + 34 = 48

Those civ entries later expand into civ/leader pairs. Current leader associations give:

    correct full-pool civ/leader pairs: 52
    pairs belonging to the 14 pass-0 civs: 21
    pass-1 m_validAICivs entries after duplication: 52 + 21 = 73

This is not merely wasted iteration. Multiple downstream consumers assume that these candidate
vectors represent the candidate population rather than a first-pass sample plus the same sample
again.

CURRENT-LIVE FITNESS DISTORTION BELOW F613'S PLAYER THRESHOLD
-------------------------------------------------------------
`changeCivs()` computes the median target oceanity by iterating every entry in `m_validAICivs`:

    for (size_t i = 0; i < m_validAICivs.size(); i++)
    {
        CvTruCivInfo const& kTruCiv = *m_truCivs.get(m_validAICivs[i].first);
        scaled rSample = per100(kTruCiv.get(CvTruCivInfo::Oceanity));
        if (rSample >= 0)
            arOceanityTargets.push_back(rSample);
    }
    m_rMedianOceanityTarget = stats::median(arOceanityTargets);

That median is then used to normalize the oceanity weight in every candidate's `calcFitness()`.
Mechanical current-data evaluation gives:

    correct 52-pair median oceanity: 30%
    duplicated 73-entry pass-2 median: 25%

Thus the duplicate accumulation changes actual civ fitness values. This does not depend on F613's
35+-distinct-civ exhaustion. Current SAS `WORLDSIZE_HUGE` itself defaults to 16 players; on Terra +
True Starts the current F615 cutoff leaves only 14 pass-0 candidates, so an ordinary 16-random-player
setup reaches pass 1 and receives this duplicate-weighted 25% baseline.

OTHER LIVE BREADTH
------------------
When Unrestricted Leaders is enabled, True Starts chooses a random available leader after selecting
the best-fit civilization by iterating `validCivs` and pushing every not-yet-taken leader. Duplicate
civ/leader entries therefore give leaders attached to first-pass civilizations extra copies in the
random draw, biasing what is intended to be the unrestricted random leader choice.

The duplicate vectors also undermine the cardinality contract behind F613. Suppose F615's longitude
cutoff is repaired first. Current data then leaves about 29 of the existing 34 TruCivs in the
Old-World first pass (the five American civs are excluded). SAS32/SAS40/SAS48 can still require the
fallback pass. Pass 1 would append those 29 again, so `aeValidAICivs.size()` would exceed the number
of **distinct** civilizations even after F613's one-line repair. A corrected F613 check could then
again keep `bUniqueCivs=true` for 40/48 players despite having only 34 distinct candidate civs.

This makes F616 independent at the repair level as well as current-live below F613's failure
threshold.

CURRENT SAS DEFINE DOES NOT ERASE THE ROOT
------------------------------------------
The duplicate civ/leader list also feeds `m_biasFromLeaderCount`. Current SAS deliberately sets
`PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT=0`, which makes that one bias contribution zero. This is a
useful control, not a fix: the median-oceanity and Unrestricted-Leaders consumers above do not depend
on that define, and the distinct-civ cardinality corruption remains.

HISTORY / BASE BOUNDARY
-----------------------
The accumulating two-pass loop was introduced together with the rest of True Starts in AdvC
practical 3579 (`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`) and remains unchanged in Base AdvC 1.14
and current SAS.

Base AdvC's lower supported player ceiling made the high-cardinality consequences less prominent,
although with the inherited F615 cutoff a manually configured 15-18-player Empty-New-World game can
already enter the fallback. SAS makes the path ordinary through its expanded world sizes: Huge now
defaults to 16 players, and SAS24/SAS32/SAS40/SAS48 default to 24/32/40/48 respectively.

REPAIR BOUNDARY
---------------
Pass 1 is a fallback to the full candidate set, not a weighted union with pass 0. The conservative
repair is therefore to ensure that pass-0 entries are not retained as duplicates when pass 1 begins,
e.g. clear/rebuild `aeValidHumanCivs` and `aeValidAICivs` before the broad pass, or append only civs
not already present. Clearing before pass 1 most directly expresses the existing comment and avoids
letting pass-0 order/weight leak into the fallback population.

F613's distinct-civ check should still be repaired independently; F616 ensures that the vector it
uses is genuinely distinct.

DEDUP
-----
Whole-album/Known-Issues searches found no prior True Starts root owning this two-pass accumulation.
F615 fixes which civs belong to pass 0 but does not clear them before pass 1. F613 fixes the wrong
AI cardinality variable but cannot make a duplicated `aeValidAICivs` vector distinct. F614 supplies
missing SAS civ records but does not change pass accumulation.

CURRENT STONE AFTER C031-WIP442
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / provisional KI#939 UNUSED.
Latest physical checkpoint: C031-WIP442.
No repository/gameplay source modified.

NEXT
----
Continue outside the now-dense candidate-construction family. Audit geographic distance/wrapping and
`initContemporaries`/leader-time state, then the True Bonus relocation/swap executor and final
assignment/sanitize state. The full current XML reference/range census is already clean apart from
F614. Whole-album dedup before every promotion; freeze WIP443 regardless at the next durable tranche.

Exact continuation cursor:
WIP442 physical -> Queue 022 OPEN -> F613-F616 confirmed -> F617/KI#939 unused -> geo-distance/wrap
invariants -> contemporaries/leader-time fitness -> bonus relocation/swap -> assignment/sanitize ->
whole-album dedup -> promote only independent current-live root -> freeze WIP443 regardless.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP442
===============================================================================


===============================================================================
C031-WIP443 — QUEUE 022 TrueStarts.cpp — F617 / KI#939 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / provisional KI#940 UNUSED.
No repository/gameplay source files modified.

F617 / KI#939 — POLAR PRECIPITATION TEST TREATS THE FEATURE ENUM AS A BOOLEAN, SO EVERY SNOW PLOT IS CLASSIFIED AS NORTHERNMOST TAIGA
------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::precipitation()` separates warm forest, temperate woodland, tundra/steppe, and finally
polar-desert precipitation. Immediately before the polar-desert fallback it contains:

    // I interpret this as the northernmost taiga
    if (eTerrain == m_ePolarDesert && m_eCoolForest)
        return 250;

`m_eCoolForest` is a `FeatureTypes` enum loaded from `TEMPERATE_FEATURE`; it is not a predicate about
`kPlot`. The function had already cached the actual feature in `eFeature`, and the neighboring
branches correctly use comparisons such as:

    if (eFeature == m_eCoolForest)

Current SAS defines are:

    FROZEN_TERRAIN    = TERRAIN_SNOW
    TEMPERATE_FEATURE = FEATURE_FOREST

and current Feature XML order makes:

    FEATURE_FOREST = 4

so `m_eCoolForest` converts to true in the `&&` expression. The malformed condition is therefore
semantically:

    if (eTerrain == TERRAIN_SNOW && true)
        return 250;

Every flat/hill Snow plot reaches the hard-coded 250 mm return, regardless of whether it has a
forest, another feature, or no feature at all.

CURRENT DATA MAKES THE INTENDED FEATURE TEST EVEN CLEARER
---------------------------------------------------------
Current `FEATURE_FOREST` is valid only on Grass, Plains and Tundra; it is not valid on Snow. Thus
under shipped SAS data a literal "Snow + Forest" northernmost-taiga fixture cannot naturally occur.
The malformed branch nevertheless captures all Snow and prevents the following polar-desert code
from ever evaluating any of it.

The intended polar-desert fallback is:

    int iExtra = 0;
    FOR_EACH_ADJ_PLOT(kPlot)
    {
        if (pAdj->getTerrainType() == kPlot.getTerrainType() &&
            pAdj->getFeatureType() == kPlot.getFeatureType())
        {
            iExtra -= 30;
        }
    }
    return std::max(1, 200 + iExtra);

For a bare Snow plot, that means the intended precipitation depends on how much same-terrain,
same-feature polar desert surrounds it:

    0 matching adjacent Snow plots -> 200 mm
    1                            -> 170 mm
    2                            -> 140 mm
    4                            ->  80 mm
    6                            ->  20 mm
    7 or 8                       ->   1 mm (clamped)

Current code returns 250 mm in every one of those states. The error is therefore at least +50 mm and
can reach +249 mm for an interior polar-desert Snow tile.

CURRENT-LIVE ACTIVATION
-----------------------
This is not a theoretical terrain state. Current map-generation code in the shared map generator
and many shipped scripts explicitly creates `TERRAIN_SNOW` (including Boreal, Highlands, Earth2,
Tectonics, PerfectMongoose, RandomScriptMap and others). True Starts climate scoring is used during
normal civilization/start matching whenever the True Starts option is enabled.

`calcClimateFitness()` calls `precipitation()` across weighted city-radius subregions, computes
regional and overall precipitation, exaggerates unusually dry/wet values, and then subtracts fitness
according to mismatch with each civilization's XML `iPrecipitation` target. The current TruCiv data
contains targets spanning extremely dry values (including 1, 106, 111, 156, 183, 208, 306 mm) through
very wet values. Replacing genuinely polar-desert values such as 1-200 with a hard 250 therefore
changes both regional precipitation and civilization fitness; it is not presentation-only data.

The same values also feed climate-variation scoring, so flattening all Snow plots to exactly 250 mm
can alter both the mean/dryness fit and the variation fit of polar starting surroundings.

HISTORY / ROOT CAUSE
--------------------
The exact malformed condition was introduced with the original AdvC True Starts merge, practical
3579 (`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`), alongside the comment:

    // I interpret this as the northernmost taiga

The same newly-added function contains the correct form `eFeature == m_eCoolForest` in two nearby
branches. No later semantic True Starts change repaired or justified the polar expression; the only
later source-history hit on `TrueStarts::precipitation()` is formatting-only.

This is therefore an inherited AdvC source typo, not a SAS-authored regression. It remains live in
current SAS because Snow terrain and True Starts climate fitness are both shipped/current features.

REPAIR BOUNDARY
---------------
The syntactically/directly intended repair is:

    if (eTerrain == m_ePolarDesert && eFeature == m_eCoolForest)
        return 250;

Under current SAS Feature XML that special branch will normally be unreachable because Forest is not
valid on Snow; bare Snow will then correctly reach the polar-desert fallback. If the design instead
intends a special northern-taiga feature/terrain combination that can actually occur, that content
policy can be reconsidered separately. The executor bug is the missing comparison against the plot's
actual feature.

DEDUP / CONTROLS
----------------
Whole-album/Known-Issues search found no prior root for `TrueStarts::precipitation`, polar desert,
taiga, or the `m_eCoolForest` boolean misuse. It is independent of F613-F616, which all concern
candidate civilization construction/cardinality/data integration. Correcting those candidate pools
does not change climate scoring, and correcting precipitation does not repair their assignment
errors.

During the same tranche two attractive but separate concerns were rejected or kept dormant:

* The first all-Random contemporaries normalization can form a fixed-point 0/0, but Win32 `MulDiv`
  produces an error sentinel whose final fixed-point `round()` collapses to the intended 0 contribution;
  no current fitness difference was demonstrated.
* `changeVegetation()` contains a real winner-update typo (`iScore = iBestScore` instead of
  `iBestScore = iScore`) that discards its documented best-adjacent-plot score. SAS ships both True
  Starts sanitation toggles disabled, and this function is only reached through that sanitization
  path, so it is retained as dormant Base-AdvC source debt rather than F617.

CURRENT STONE AFTER C031-WIP443
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / provisional KI#940 UNUSED.
Latest physical checkpoint: C031-WIP443.
No repository/gameplay source modified.

NEXT
----
Continue outside the now-covered candidate-pool and precipitation roots. Audit the remaining live
climate/latitude/oceanity fitness calculations, final civ/leader assignment uniqueness, fixed-player
interaction, and bonus-swap executor. Preserve the sanitation-only `changeVegetation()` typo as a
control unless a current enabled caller is found. Whole-album dedup before every promotion; freeze
WIP444 at the next durable tranche regardless of whether F618 is used.

Exact continuation cursor:
WIP443 physical -> Queue 022 OPEN -> F613-F617 confirmed -> F618/KI#940 unused -> remaining live
climate/latitude/oceanity invariants -> final assignment/fixed-player interaction -> bonus swap live
executor -> whole-album dedup -> promote only independent current-live root -> freeze WIP444.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP443
===============================================================================


===============================================================================
C031-WIP444 — QUEUE 022 TrueStarts.cpp — F618 / KI#940 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / provisional KI#941 UNUSED.
No repository/gameplay source files modified.

F618 / KI#940 — TRUE STARTS KEEPS CIV/LEADER AVAILABILITY COUPLED UNDER UNRESTRICTED LEADERS, SO FIXED LEADERS ERASE VALID CIVS AND FIXED CIVS ERASE VALID LEADERS
----------------------------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::TrueStarts()` explicitly disables leader-specific True Starts data when Unrestricted
Leaders is enabled:

    // Ignore the truLeader info when leaders are unrestricted
    if (!GC.getGame().isOption(GAMEOPTION_LEAD_ANY_CIV))
    {
        FOR_EACH_ENUM(TruLeader)
        {
            m_truLeaders.set(...);
        }
    }

and `changeCivs()` later states the intended contract equally clearly:

    /*  When leaders are unrestricted, then the best-fit has been
        selected only based on a civ. Stick to that civ, and
        combine it with a random leader. */

The candidate construction, however, still represents every candidate as a **native
(civilization, leader) pair** before that late decoupling step.

For AI candidates it builds:

    FOR_EACH_ENUM_RAND(LeaderHead, mapRand())
    {
        for (size_t i = 0; i < aeValidAICivs.size(); i++)
        {
            if (GC.getInfo(aeValidAICivs[i]).isLeaders(eLoopLeaderHead) &&
                !m_leaderTaken.get(eLoopLeaderHead))
            {
                m_validAICivs.push_back(std::make_pair(
                        aeValidAICivs[i], eLoopLeaderHead));
            }
        }
    }

and for human candidates it stores the same native pairs, merely deferring the taken-leader test
until assignment:

    if (m_leaderTaken.get(validCivs[j].second) ||
        (bUniqueCivs && m_civTaken.get(validCivs[j].first)))
    {
        continue;
    }

Only **after** choosing `eeBestFit` from those native pairs does the Unrestricted Leaders branch
replace the leader with a random available leader.

CURRENT-LIVE CIV EXCLUSION
--------------------------
This makes a taken native leader erase its native civilization even though the game option says that
civilization may use any other leader.

Concrete current setup:

    GAMEOPTION_TRUE_STARTS = on
    GAMEOPTION_LEAD_ANY_CIV = on

    fixed player:
        civilization = CIVILIZATION_JAPAN
        leader       = LEADER_SALADIN

    another player:
        civilization = Random

The fixed player is not in `m_truPlayers`, so `changeCivs()` records:

    m_civTaken[JAPAN]    = true
    m_leaderTaken[SALADIN] = true

Arabia itself is **not** taken. Under Unrestricted Leaders it should therefore remain a valid
civilization for the Random player and could simply receive any free leader.

But current Arabia has one native leader:

    CIVILIZATION_ARABIA -> LEADER_SALADIN

When `m_validAICivs` is built, the only Arabia pair fails `!m_leaderTaken[SALADIN]`; no Arabia entry
is created at all. For a Random human, the Arabia/Saladin pair exists initially but the later
`m_leaderTaken` test skips it. Either way, Arabia disappears from civilization fitness/selection
solely because its native leader was used elsewhere.

Current shipped data makes this broad rather than exotic. Among the 34 civilizations currently
represented by `CvTruCivInfo`, **20 have exactly one native leader**. Any one of those civs can be
removed from an otherwise valid Random True-Starts pool by fixing its native leader to some other
civilization under Unrestricted Leaders.

INVERSE ERROR — FIXED CIVILIZATIONS ERASE FREE LEADERS
------------------------------------------------------
The coupling also runs in the other direction. Candidate civilizations are filtered first by:

    if (m_civTaken.get(eCiv))
        continue;

so no native `(civ, leader)` pairs are created for a fixed civilization. Later, the supposedly
unrestricted random-leader draw is built only from the leaders present in `validCivs`:

    std::vector<LeaderHeadTypes> aeAvailableLeaders;
    for (size_t j = 0; j < validCivs.size(); j++)
    {
        if (!m_leaderTaken.get(validCivs[j].second))
            aeAvailableLeaders.push_back(validCivs[j].second);
    }

Thus, in the example above, fixing Japan removes its native leader Tokugawa from the Random player's
leader draw even though Tokugawa is not taken and Unrestricted Leaders should permit him to lead any
other civilization.

This is independent of the fixed leader chosen for Japan: merely taking a civilization can suppress
all of that civilization's otherwise-free native leaders from later unrestricted assignment.

F614 SAME-ROOT DATA BREADTH
---------------------------
F614's missing `TruCiv` records for Ireland and Kingdom of Benin add another manifestation: because
no candidate pairs are ever created for those civs, their native leaders Ewuare, Grace O'Malley and
Michael Collins also never enter this `validCivs`-derived unrestricted leader pool. That breadth is
still caused by F614's missing data. F618 remains independently reproducible with any of the 34
properly integrated Base-AdvC civs, e.g. Arabia/Japan above.

WHY FITNESS REALLY IS SUPPOSED TO BE CIV-ONLY HERE
--------------------------------------------------
The late comment is not merely aspirational. At construction time True Starts deliberately leaves
`m_truLeaders` empty under `GAMEOPTION_LEAD_ANY_CIV`; therefore leader historical bias and
contemporaneity data are disabled. `calcFitness()` consequently has no leader-specific True Starts
signal in this mode. The chosen native leader is effectively just a temporary key used to carry a
civilization through data structures until the final random-leader replacement.

That makes filtering a civilization according to that temporary native leader's availability a
structural contradiction: the leader is explicitly not part of the intended fitness identity under
this option.

HISTORY / BASE BOUNDARY
-----------------------
The coupling was introduced with AdvC's original True Starts implementation, practical 3579
(`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`). The original commit already contains both:

    // Ignore the truLeader info when leaders are unrestricted

and the later:

    // ... best-fit has been selected only based on a civ ...

while constructing `m_validHumanCivs` / `m_validAICivs` from native civ/leader pairs exactly as
current SAS does.

Base AdvC 1.14 retains the same logic. This is therefore an inherited AdvC True-Starts /
Unrestricted-Leaders integration defect, not a SAS-authored regression. SAS current data and game
options reproduce it directly.

REPAIR BOUNDARY
---------------
When `GAMEOPTION_LEAD_ANY_CIV` is enabled, civilization availability and leader availability need to
be built independently:

* civilization fitness candidates should be the untaken playable/AI-playable `TruCiv` civilizations,
  without requiring any of their native leaders to be free;
* the final unrestricted leader draw should come from globally valid untaken playable leaders,
  without requiring their native civilization to remain available;
* leader-specific True Starts fitness should remain disabled as current code already intends.

The current pair structure could still be retained internally with a harmless representative leader
for indexing if desired, but that representative must not gate civilization eligibility, and the
final leader pool must not be reconstructed from civ-native candidate pairs.

DEDUP
-----
Whole-album/Known-Issues searches found no prior root for this True Starts / Unrestricted Leaders
availability coupling.

It is independent of the adjacent Queue-022 findings:

* F613 fixes the wrong pair-vs-distinct-civ cardinality check; it does not decouple civ and leader
  eligibility.
* F614 adds missing Ireland/Benin True-Starts records; F618 reproduces using fully integrated Arabia
  and Japan.
* F615 fixes Empty-New-World longitude filtering.
* F616 removes fallback-pass duplicates; a perfectly deduplicated pair list still filters Arabia
  because Saladin is taken and still removes Tokugawa because Japan is taken.
* F617 fixes polar precipitation scoring and does not affect candidate identity.

CURRENT STONE AFTER C031-WIP444
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / provisional KI#941 UNUSED.
Latest physical checkpoint: C031-WIP444.
No repository/gameplay source modified.

NEXT
----
Continue outside the now-dense candidate-construction/unrestricted-leader family. Finish remaining
live climate/latitude/oceanity scoring and final-assignment state, then audit bonus relocation/swap
and sanitize-independent executors. Preserve the sanitation-only `changeVegetation()` winner-update
typo as dormant under shipped SAS defines. Whole-album dedup before every promotion; freeze WIP445
at the next durable tranche regardless of whether F619 is used.

Exact continuation cursor:
WIP444 physical -> Queue 022 OPEN -> F613-F618 confirmed -> F619/KI#941 unused -> remaining live
climate/latitude/oceanity invariants -> final assignment state -> bonus relocation/swap live executor
-> whole-album dedup -> promote only independent current-live root -> freeze WIP445.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP444
===============================================================================


===============================================================================
C031-WIP445 — QUEUE 022 TrueStarts.cpp — F619 / KI#941 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / provisional KI#942 UNUSED.
No repository/gameplay source files modified.

F619 / KI#941 — TRUE STARTS READS AN UNINITIALIZED GLOBAL-MAX-ELEVATION MEMBER, SO ELEVATION FITNESS CAN DEPEND ON STALE STACK DATA
-------------------------------------------------------------------------------------------------------------------------------
`TrueStarts` contains the plain integral member:

    int m_iMaxMaxElevationTarget; // global max

There is no in-class initializer (the project is C++03-era code), no constructor initializer list,
and no assignment to this member in `TrueStarts::TrueStarts()`.

`CvGame` constructs True Starts as an ordinary automatic local object:

    TrueStarts ts;
    ts.changeCivs();

so the member has an indeterminate value when `changeCivs()` first reads it.

The first and only initialization-like operation is actually a read-modify-write:

    for (size_t i = 0; i < m_validAICivs.size(); i++)
    {
        CvTruCivInfo const& kTruCiv = *m_truCivs.get(m_validAICivs[i].first);
        ...
        m_iMaxMaxElevationTarget = std::max(m_iMaxMaxElevationTarget,
                kTruCiv.get(CvTruCivInfo::MaxElevation));
    }

Reading the indeterminate `int` is undefined behavior. If its stale stack value happens to be below
one of the real positive targets, later iterations can accidentally repair it. If it is a positive
value above the largest candidate target, that garbage value survives every `std::max` and becomes
the session's supposed historical maximum elevation.

CURRENT DATA / INTENDED VALUE
-----------------------------
All 34 current `CvTruCivInfo` records provide positive `MaxElevation` data. Across the full current
pool the range is:

    Netherlands:  322 m
    ...
    Inca:        6893 m
    India:       8586 m   <- current global maximum

Thus, when India is among the valid candidate civilizations, the intended value is 8586. If some
high-elevation civs are already fixed/taken, the intended maximum can be lower; either way it should
be derived solely from the actual candidate data, never from previous stack contents.

LIVE CONSUMER
-------------
`calcFitness()` uses this member in the MaxElevation component:

    scaled rMaxElev = (rAreaPeakScore.getPercent() + 8) * 200;
    // Scenarios may have enormous clumps of peaks
    rMaxElev.decreaseTo(m_iMaxMaxElevationTarget);

    scaled rFromMaxElev =
        (rMaxElev.sqrt() - scaled(iTargetMaxElev).sqrt()).abs() * -fixp(2.4);
    iFitness += rFromMaxElev.round();

The comment explains the purpose of the global maximum: cap the map-derived elevation estimate so
peak-heavy maps/scenarios do not produce values beyond the historical target domain.

A stale positive `m_iMaxMaxElevationTarget` above the true maximum weakens or removes that cap. This
changes `rMaxElev` and therefore civilization fitness on sufficiently peak-heavy starts. A stale
negative value can create still stranger behavior at the clamp, although common debug stack-fill
patterns being negative may also cause the later positive `std::max` iterations to mask the defect.
Release builds have no such correctness guarantee.

This is therefore not merely an analyzer warning about an unused or presentation-only field: the
indeterminate value feeds a normal True Starts civ-fitness term.

HISTORY / BASE BOUNDARY
-----------------------
The defect was introduced with AdvC's original True Starts implementation, practical 3579
(`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`). That single merge added all three relevant pieces:

    int m_iMaxMaxElevationTarget;

    m_iMaxMaxElevationTarget = std::max(m_iMaxMaxElevationTarget, ...);

    rMaxElev.decreaseTo(m_iMaxMaxElevationTarget);

No constructor initialization was supplied then or in later Base AdvC changes. Base AdvC 1.14 and
current SAS retain the same member and use. The bug is therefore inherited AdvC source debt and is
live whenever True Starts evaluates MaxElevation fitness.

REPAIR BOUNDARY
---------------
Initialize the member to a value below every valid target before the accumulation, preferably in the
constructor initializer list/body or immediately before the candidate loop, e.g.:

    m_iMaxMaxElevationTarget = MIN_INT;

or, because current MaxElevation values are nonnegative:

    m_iMaxMaxElevationTarget = 0;

Then the existing `std::max` accumulation deterministically derives the maximum from the candidate
set. Initializing immediately before the loop additionally makes repeated `changeCivs()` calls on a
single object safe if that were ever introduced later.

A separate optional robustness choice is to avoid applying the clamp when no valid MaxElevation
sample exists, but current shipped TruCiv data always supplies those values; that is not needed to
repair F619.

DEDUP / CONTROLS
----------------
Whole-album/Known-Issues searches found no prior root for `m_iMaxMaxElevationTarget`, uninitialized
True Starts elevation state, or this MaxElevation clamp.

It is independent of F613-F618: those alter candidate cardinality/data/fitness inputs, but even a
perfect candidate list still seeds the elevation maximum from an indeterminate integer. Conversely,
initializing this member does not repair any candidate construction, longitude, precipitation or
Unrestricted-Leaders defect.

CURRENT STONE AFTER C031-WIP445
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / provisional KI#942 UNUSED.
Latest physical checkpoint: C031-WIP445.
No repository/gameplay source modified.

NEXT
----
Continue the remaining live fitness/final-assignment state after excluding F619. Inspect scalar
initialization and zero-sample/division boundaries systematically, then finish assignment/color/player
identity mutation. The bonus relocation and vegetation executor remains sanitation-only under current
SAS defines unless another live caller is found. Whole-album dedup before promotion; freeze WIP446 at
the next durable tranche regardless of whether F620 is used.

Exact continuation cursor:
WIP445 physical -> Queue 022 OPEN -> F613-F619 confirmed -> F620/KI#942 unused -> scalar/default and
zero-sample fitness invariants -> final assignment/player identity/color state -> current-live bonus
paths -> whole-album dedup -> promote only independent current-live root -> freeze WIP446.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP445
===============================================================================


===============================================================================
C031-WIP446 — QUEUE 022 TrueStarts.cpp — F620 / KI#942 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / provisional KI#943 UNUSED.
No repository/gameplay source files modified.

F620 / KI#942 — TRUE STARTS OVERWRITES A MANUALLY CHOSEN LEADER WHEN ONLY THE CIVILIZATION WAS RANDOM UNDER UNRESTRICTED LEADERS
-------------------------------------------------------------------------------------------------------------------------------
AdvC deliberately tracks civilization-randomness and leader-randomness separately. Current
`CvInitCore` exposes both flags:

    bool CvInitCore::wasCivRandomlyChosen(PlayerTypes eID) const
    {
        return m_abCivChosenRandomly.get(eID);
    }

    bool CvInitCore::wasLeaderRandomlyChosen(PlayerTypes eID) const
    {
        if (wasCivRandomlyChosen(eID) && !getOption(GAMEOPTION_LEAD_ANY_CIV))
            return true;
        return m_abLeaderChosenRandomly.get(eID);
    }

The special case is important: when Unrestricted Leaders (`GAMEOPTION_LEAD_ANY_CIV`) is enabled,
`wasCivRandomlyChosen()` being true does **not** imply that the leader was random. The two setup
choices are intentionally independent.

`TrueStarts::TrueStarts()`, however, decides which normal single-player slots it owns solely from the
civilization flag:

    if (GC.getInitCore().wasCivRandomlyChosen(itPlayer->getID()) ||
        GC.getGame().isNetworkMultiPlayer() ||
        (GC.getGame().isScenario() && !GC.getInitCore().getWBMapNoPlayers()))
    {
        m_truPlayers.push_back(&*itPlayer);
        ...
    }

It never asks `wasLeaderRandomlyChosen()`.

Therefore the fully supported setup state:

    GAMEOPTION_LEAD_ANY_CIV = true
    civilization = Random
    leader       = manually chosen / fixed

places that player in `m_truPlayers` even though only the civilization is supposed to be rerolled.

LIVE ASSIGNMENT EXECUTOR
------------------------
`changeCivs()` excludes `m_truPlayers` from the fixed-player bookkeeping, evaluates a replacement
civilization for each True-Starts player, and then has this Unrestricted-Leaders block:

    if (GC.getGame().isOption(GAMEOPTION_LEAD_ANY_CIV))
    {
        /*  When leaders are unrestricted, then the best-fit has been
            selected only based on a civ. Stick to that civ, and
            combine it with a random leader. */
        std::vector<LeaderHeadTypes> aeAvailableLeaders;
        ...
        eeBestFit.second = aeAvailableLeaders[MapRandNum(...)];
    }

At finalization it unconditionally applies both selected identities:

    itPlayer->changeCiv(eCiv, ...);
    itPlayer->changeLeader(eLeader, ...);

Consequently, a player who asked for **Random civilization + a specific leader** under Unrestricted
Leaders does not merely receive a geographically fitted civilization. True Starts silently discards
the fixed leader and assigns another leader randomly.

This is a direct setup-contract violation, not a heuristic disagreement: current `CvInitCore` has an
explicit, separately persisted answer to whether the leader was randomly chosen, and True Starts
never consults it before replacing the leader.

CONCRETE REPRODUCER
-------------------
For example:

    True Starts:          enabled
    Unrestricted Leaders: enabled
    civilization:         Random
    leader:               Saladin (manually selected)

The setup provenance is:

    wasCivRandomlyChosen(player)    == true
    wasLeaderRandomlyChosen(player) == false

True Starts nevertheless includes that player in `m_truPlayers`; after choosing a fitted civilization,
it enters the Unrestricted-Leaders branch and randomly chooses an available leader. The later
`changeLeader()` therefore need not preserve Saladin.

KI#209 POSITIVE CONTROL / DEDUP
-------------------------------
This root is especially well bounded by current KI#209.

KI#209 fixed an inherited Base AdvC copy/paste bug in `CvInitCore::reassignPlayer` that saved
`bRandomLeader` from the civilization-randomness map. Its documentation explicitly uses the same
supported setup states as examples:

    manually chosen civ + random leader
    random civ + manually chosen leader

and states that the distinction matters notably under `GAMEOPTION_LEAD_ANY_CIV`.

The current KI#209 repair correctly preserves those two provenance flags independently. F620 begins
**after that repair has succeeded**: the flags are correct, but True Starts reads only the civ flag and
then overwrites the fixed leader itself. Thus this is not a recurrence or breadth of KI#209; fixing
KI#209 is what makes the remaining True Starts contract violation especially explicit.

Whole-album searches found no prior root for True Starts ignoring `wasLeaderRandomlyChosen()` or
replacing a manually selected leader solely because the civilization was random. F618 is also
independent: F618 concerns candidate-pool civ/leader coupling under Unrestricted Leaders; F620
concerns whether an individual player's leader should be reassigned at all.

HISTORY / BASE BOUNDARY
-----------------------
The behavior is inherited from AdvC's original True Starts merge, practical 3579
(`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`). That commit introduced both:

    if (wasCivRandomlyChosen(...))
        m_truPlayers.push_back(...);

and the later Unrestricted-Leaders rule that combines the fitted civ with a random leader.

Base AdvC 1.14 retains the same structure. Current SAS retains it too. The separate
`wasLeaderRandomlyChosen()` contract also exists in Base AdvC and explicitly treats Unrestricted
Leaders as the mode where random-civ and random-leader provenance can differ.

F620 is therefore inherited AdvC True Starts source debt, current-live under the supported
Unrestricted-Leaders setup domain.

REPAIR BOUNDARY
---------------
The repair should preserve True Starts' right to replace a random civilization without granting it
permission to replace a fixed leader.

A conservative design is to retain each True-Starts player's original leader whenever:

    GAMEOPTION_LEAD_ANY_CIV &&
    !GC.getInitCore().wasLeaderRandomlyChosen(ePlayer)

and only draw from the unrestricted available-leader pool when the leader was actually random.

The fixed leader also needs to participate in `m_leaderTaken` / availability bookkeeping so another
True-Starts player cannot be assigned the same leader unless the normal game rules explicitly allow
leader duplication. This should be integrated with the broader F618 decoupling repair rather than
adding another native-pair dependency.

Do not solve F620 by excluding every random-civ/fixed-leader player from `m_truPlayers`: that would
preserve the leader but also prevent True Starts from geographically reassigning the civilization,
which was the part the player did ask to randomize.

CURRENT STONE AFTER C031-WIP446
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / provisional KI#943 UNUSED.
Latest physical checkpoint: C031-WIP446.
No repository/gameplay source modified.

NEXT
----
Exclude F620 and finish True Starts' remaining scalar/default/zero-sample climate invariants, then
final assignment/color/team identity state and any sanitize-independent bonus/resource mutation.
Keep the already-proven `changeVegetation()` reversed winner assignment as dormant source debt under
shipped `TRUE_STARTS_SANITIZE=0` / `TRUE_STARTS_SANITIZE_SCENARIOS=0` unless a live caller emerges.
Whole-album semantic dedup before promotion; freeze WIP447 at the next durable boundary regardless
of whether F621 is used.

Exact continuation cursor:
WIP446 physical -> Queue 022 OPEN -> F613-F620 confirmed -> F621/KI#943 unused -> remaining
zero-sample/climate invariants -> final assignment identity/color state -> sanitize-independent
resource/bonus mutation -> whole-album dedup -> promote only independent current-live root -> freeze
WIP447.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP446
===============================================================================


===============================================================================
C031-WIP447 — QUEUE 022 TrueStarts.cpp — F621 / KI#943 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / provisional KI#944 UNUSED.
No repository/gameplay source files modified.

F621 / KI#943 — TRUE STARTS' OCEANITY BASELINE COUNTS CIVS ONCE PER NATIVE LEADER, SO SAS'S ZERO LEADER-COUNT-BIAS SETTING STILL CHANGES FITNESS BY LEADER COUNT
----------------------------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::changeCivs()` stores valid AI choices as native `(civilization, leader)` pairs in
`m_validAICivs`. That representation is appropriate where leader-specific fitness or availability is
needed, but the civ-only Oceanity normalization later loops the pair list directly:

    std::vector<scaled> arOceanityTargets;
    for (size_t i = 0; i < m_validAICivs.size(); i++)
    {
        CvTruCivInfo const& kTruCiv = *m_truCivs.get(m_validAICivs[i].first);
        scaled rSample = per100(kTruCiv.get(CvTruCivInfo::Oceanity));
        if (rSample >= 0)
            arOceanityTargets.push_back(rSample);
    }
    m_rMedianOceanityTarget = stats::median(arOceanityTargets);

Oceanity belongs only to `CvTruCivInfo`; the leader component is irrelevant. Consequently a civ with
three native leaders contributes its identical Oceanity target three times, a two-leader civ twice,
and a one-leader civ once.

CURRENT SAS DATA MAKES THE DIFFERENCE MEASURABLE
------------------------------------------------
For an ordinary all-Random current True Starts setup before any fixed-civ filtering:

    distinct current TruCivs:            34
    native valid civ/leader pairs:       52

The current Oceanity targets give:

    median over 34 distinct civs:        27.5%
    median over 52 native pairs:         30.0%

Fourteen of the 34 current True-Starts civilizations have more than one native leader; America,
England, France and Russia have three in the current data, while ten others have two. The difference
therefore comes from real shipped leader cardinalities, not malformed XML.

`calcFitness()` uses the pair-weighted median to normalize every candidate's Oceanity sensitivity:

    scaled rOceanityWeight = rTargetOceanity - m_rMedianOceanityTarget;
    if (rOceanityWeight > 0 && m_rMedianOceanityTarget < 1)
        rOceanityWeight /= 1 - m_rMedianOceanityTarget;
    else if (m_rMedianOceanityTarget > 0)
        rOceanityWeight /= m_rMedianOceanityTarget;
    rOceanityWeight = rOceanityWeight.abs();
    rOceanityWeight += fixp(0.5);
    ...
    rFromOceanity = ... * -175 * rOceanityWeight;

Across current Oceanity targets, using 30% instead of the distinct-civ 27.5% baseline changes this
weight by up to roughly 7.6 percentage points (around the current 25% target). It therefore changes
actual True-Starts civilization fitness and can change which civilization wins an assignment.

DIRECT CONFIG CONTRACT
----------------------
This is especially concrete in current SAS because leader-count bias is explicitly configurable and
SAS deliberately disables it:

    PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT = 0

The current SAS XML comment explains the design intent: each civilization should have an equal
leader-count chance rather than France/Rome-style multi-leader civs appearing more often than
one-leader civs such as Kingdom of Benin.

The True Starts implementation itself states the same zero-setting contract immediately before its
explicit leader-count bias term:

    /*  A weight of 0 is supposed to result in no bias from the leader count.
        We can easily do that. */

At 0, `m_biasFromLeaderCount` correctly becomes zero. The Oceanity median, however, still depends on
the number of native leaders because it is built from `m_validAICivs`. Thus the file does **not** in
fact remove all leader-count influence when the configured weight is zero.

HISTORY / INTRODUCTION BOUNDARY
-------------------------------
AdvC's original True Starts merge, practical 3579 (`4977b513da...`), introduced the pair-weighted
Oceanity median together with the rest of the fitness model.

AdvC practical 3591 (`52444489ce...`, "True Starts bias for civs w/ multiple leaders based on XML")
then connected `PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT` to True Starts and added the explicit promise
that 0 should mean no leader-count bias. That commit changed `m_biasFromLeaderCount` but left the
Oceanity-median loop over `m_validAICivs` untouched. Base AdvC's recommended value is 20; current SAS
sets the define to 0 deliberately, making the stale pair weighting a particularly clear current
contract contradiction.

REPAIR BOUNDARY
---------------
Compute the civ-only Oceanity median from **distinct valid civilization IDs**, not native
civilization/leader pairs. The candidate-construction block already has the conceptual object needed
(`aeValidAICivs`); the implementation can either preserve a deduplicated civ vector for this later
step or use a local seen-civ map while building `arOceanityTargets`.

The same distinct-civ principle is appropriate for any other civ-only aggregate introduced later.
Do not remove the pair representation itself: leader-specific fitness/availability still needs it
when leaders are restricted.

DEDUP / F616 ALBUM CORRECTION
-----------------------------
F621 is independent of F616.

F616 concerns **second-pass duplicate accumulation** on Empty-New-World maps. It showed that the
current pass-2 duplication changes the Oceanity median from 30% to 25%. That remains valid evidence
that F616 affects gameplay, but WIP442 called 30% the "correct 52-pair median." This wording is now
refined:

    30% = unduplicated native-pair baseline used by current code
    27.5% = semantically distinct-civ baseline when leader-count bias is disabled
    25% = additionally duplicated Empty-New-World pass-2 value under F616's current reproduction

Fixing F616 removes accidental duplicate *passes* but still leaves each civ represented once per
native leader, so F621 survives. Fixing F621 by deduplicating civ-only targets does not repair F616's
other effects (candidate cardinality, unrestricted-leader draw duplication, uniqueness counts).

F618 and F620 are also separate Unrestricted-Leaders roots. F621 reproduces with ordinary restricted
leaders and an all-Random roster; it needs neither of those option-specific states.

CURRENT STONE AFTER C031-WIP447
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / provisional KI#944 UNUSED.
Latest physical checkpoint: C031-WIP447.
No repository/gameplay source modified.

NEXT
----
Exclude F621. Finish the remaining current-live fitness/assignment invariants and then perform a
full function-by-function Queue-022 closure reconciliation. Preserve the sanitation-only
`changeVegetation()` reversed score assignment and the sanitize inner-loop bound hazard as dormant
under shipped SAS defines unless an enabled caller is found. If the live frontier closes, do not
force F622; close Queue 022 and advance to Queue 023 at the next durable stone.

Exact continuation cursor:
WIP447 physical -> Queue 022 OPEN -> F613-F621 confirmed -> F622/KI#944 unused -> remaining live
fitness/finalization checks -> disabled-sanitization controls -> whole-file semantic reconciliation ->
close Queue 022 if clean -> freeze WIP448 regardless -> advance Queue 023 under next unused F-id.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP447
===============================================================================


===============================================================================
C031-WIP448 — QUEUE 022 TrueStarts.cpp — F622 / KI#944 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / KI#944 CONFIRMED.
F623 / provisional KI#945 UNUSED.
No repository/gameplay source files modified.

F622 / KI#944 — TRUE STARTS' CLIMATE-REGION DIRECTION FILTER USES RAW COORDINATE DELTAS, SO WRAPPED-SEAM STARTS SILENTLY LOSE DIAGONAL REGIONS
--------------------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::calcClimateFitness()` deliberately samples the region around the start plus eight
orthogonal/diagonal region centers on a circle around it. The outer center loop first uses the
iterator's wrap-aware plot distance:

    int const iDistBetweenRegions = (GC.getMap().getWorldSize() < 2 ? 3 : 4);
    for (PlotCircleIter itCenter(kStart, iDistBetweenRegions);
        itCenter.hasNext(); ++itCenter)
    {
        bool const bStart = (&*itCenter == &kStart);
        if (itCenter.currPlotDist() != iDistBetweenRegions && !bStart)
            continue;

It then tries to retain only the eight intended directions, but switches coordinate systems:

        /*  Only consider the 8 plots on a straight orthogonal or
            diagonal line from kStart */
        int iDX = abs(itCenter->getX() - kStart.getX());
        int iDY = abs(itCenter->getY() - kStart.getY());
        if (iDX != 0 && iDY != 0 && iDX != iDY)
            continue;

`itCenter->getX()` / `getY()` are the wrapped, on-map coordinates of the returned `CvPlot`. Raw
subtraction of those coordinates does not represent local displacement when the iterator crossed a
wrapped map seam.

ITERATOR CONTRACT MAKES THE MISMATCH EXPLICIT
---------------------------------------------
`PlotCircleIter` derives from `SpiralPlotIterator<true>`. The iterator intentionally keeps its own
unwrapped traversal coordinates even when they temporarily lie outside the map, then resolves the
actual plot through `plotValidXY`:

    m_iCurrX += ...;
    m_iCurrY += ...;
    m_pNext = GC.getMap().plotValidXY(m_iCurrX, m_iCurrY);

The iterator comments explicitly document this distinction and provide the exact helpers needed by
this directional test:

    /* World-wrap isn't applied to m_iCurrX (i.e. it can be off the map),
       hence no need to check world wrap (CvMap::xDistance) here. */
    int currXDist() const { return abs(m_pCenter->getX() - m_iCurrX); }
    int currYDist() const { return abs(m_pCenter->getY() - m_iCurrY); }

At the same time, `currPlotDist()` calls the global plot-distance helper on the resolved plots.
Current `CvMap::plotDistance` derives its components through `xDistance` / `yDistance`, whose
`coordDistance` explicitly reduces wrapped deltas to the shorter distance across the seam.

Thus the same `itCenter` can correctly pass the rim test as a nearby wrapped plot and then fail the
very next straight-line test because that second test reinterprets its wrapped storage coordinate as
an unwrapped displacement.

CONCRETE X-WRAP REPRODUCER
--------------------------
For ordinary world sizes, `iDistBetweenRegions` is 4. With the current Civ4 plot-distance metric,
the eight intended outer directions are:

    (-4, 0), (+4, 0), (0, -4), (0, +4),
    (-3,-3), (-3,+3), (+3,-3), (+3,+3)

because `plotDistance(3,3) == 4`.

Take any sufficiently large X-wrapping map of width `W`, put the starting plot at X=1, and consider
the intended northwest center at unwrapped displacement (-3,+3). The spiral iterator visits
unwrapped X=-2 and resolves that through `plotValidXY` to stored map X=`W-2`.

The wrap-aware rim test sees the intended local distances 3 and 3 and therefore keeps the point on
the radius-4 circle. The raw directional test instead computes:

    iDX = abs((W - 2) - 1) = W - 3
    iDY = 3

For every normal map width, both are nonzero and unequal, so that diagonal center is rejected. The
southwest center is rejected the same way. The west cardinal center still survives only because
`iDY == 0` bypasses the equality requirement.

Therefore an X-wrapping radius-4 start in either of the first three or last three map columns can
lose two of its eight intended outer climate-region centers solely because of where the arbitrary
coordinate seam lies. On the radius-3 path used by the two smallest world sizes, the analogous
intended diagonal displacement is (2,2), so the first/last two columns are affected. Y-wrap has the
same defect in the other axis; toroidal maps can combine both effects.

This is not merely a cosmetic region list difference. The omitted regions feed all of the following
live climate calculations:

* the weighted median precipitation of the outer regions;
* the search for a region whose precipitation reinforces the start region;
* the resulting overall precipitation target mismatch;
* the count of close precipitation matches;
* the climate-variation sample/error calculation.

A sufficiently distinct climate region on the omitted side can therefore change the civilization's
True Starts fitness. Translating the same topological terrain across the artificial map seam can
change the score even though a wrapping map has no gameplay boundary there.

CURRENT-LIVE ACTIVATION
-----------------------
Wrapping is a supported and common current map topology. `CvMap` initializes wrapping to:

    m_bWrapX = true;
    m_bWrapY = false;

before allowing map-script/init data to override it. Multiple shipped SAS scripts explicitly enable
X-wrap; for example Tectonics returns true except for its nonwrapping option, while
`SAS_Spiky_Avenues.py` and `Water.py` return true unconditionally. The ordinary Fractal,
Continents, Pangaea and Terra scripts also expose cylindrical/toroidal wrap options.

A focused pass through current `StartingPositionIteration.cpp` found no seam exclusion that prevents
valid starting plots from occupying those X columns. Such an exclusion would in any case be
geometrically unnatural on a wrapping map, where X=0 is adjacent to X=width-1.

The defect is therefore current-live whenever True Starts evaluates a sufficiently seam-adjacent
start on a wrapping map; whether a particular omitted region has enough valid land to affect the
score depends on the generated terrain, but the region-selection asymmetry itself is deterministic.

HISTORY / BASE BOUNDARY
-----------------------
AdvC practical 3579 (`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`), the original True Starts merge,
introduced the exact two-stage filter already in this form: wrap-aware `currPlotDist()` followed by
raw `getX()` / `getY()` subtraction for the eight-direction test.

Across the 6,403 locally mirrored commit diffs, only one later SAS diff also mentions
`calcClimateFitness`: SASBranch practical 5961, which merely reformats the function declaration onto
one line and does not touch this region filter. Base AdvC 1.14 still contains the same body verbatim.
K-Mod 1.46 and Civ4CE do not contain AdvC's `TrueStarts` implementation. F622 is therefore inherited
AdvC True Starts source debt, not a SAS regression or a K-Mod/Firaxis carryover.

DEDUP / CONTROLS
----------------
Focused whole-album and Known-Issues searches found no prior root for `calcClimateFitness` selecting
its region centers with raw wrapped coordinates, `currXDist` / `currYDist`, or wrapped-seam climate
region loss.

KI#257 is a useful thematic control, not a duplicate: it fixed raw seam deltas in the independently
inherited Planet/C2C Python feature-spread code. F622 is a separate C++ True Starts root in a
different algorithm and was introduced independently by AdvC practical 3579.

KI#259 is also separate: that issue concerned Planet climate callbacks returning inverted latitude
bounds and thereby disabling latitude-sensitive True Starts support. F617 concerns precipitation
classification after a climate region has already been selected. Neither owns the region-center
selection asymmetry here.

The earlier climate arithmetic controls remain closed: the apparently malformed Temperate-relative
latitude scaling is numerically harmless under current XML because the relevant Temperate changes
are zero, and the singleton-region variation path has not demonstrated a current gameplay mismatch.
Those should not be folded into F622.

REPAIR BOUNDARY
---------------
The narrow repair is to keep the directional filter in the iterator's unwrapped local coordinate
system:

    int iDX = itCenter.currXDist();
    int iDY = itCenter.currYDist();

and retain the existing orthogonal/diagonal predicate. These helpers were designed precisely to
measure iterator displacement before world-wrap converts the position back into an on-map plot.

Using `GC.getMap().xDistance(...)` / `yDistance(...)` would also repair ordinary wrapped distances,
but `currXDist()` / `currYDist()` more directly expresses the intended "which spoke of this spiral
are we on?" question and matches the iterator's own documented contract.

A focused regression test should place an otherwise-valid True Starts start within three columns of
an X seam on a normal world size and verify that the start plus all eight intended region directions
remain eligible. Translating equivalent terrain across the seam should no longer change the region
set or climate fitness merely because stored X coordinates wrap.

CURRENT STONE AFTER C031-WIP448
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / KI#944 CONFIRMED.
F623 / provisional KI#945 UNUSED.
Latest physical checkpoint: C031-WIP448.
No repository/gameplay source modified.

NEXT
----
Exclude F622 and finish Queue 022 in closure mode. Reconcile the remaining live
`calcFitness`/`calculatePlotWeights`/`getSurroundings` invariants and final assignment state against
F613-F622 and prior album coverage. Keep the sanitation-only `changeVegetation()` winner-assignment
typo and `sanitize()` inner-loop bound-order hazard as dormant inherited source defects while the
shipped normal/scenario True Starts sanitation defines remain disabled, unless another enabled caller
is found. Do not force F623 if no distinct current-live contradiction survives.

The queue table in the inherited handoff text said to advance to Queue 023 after closure, but the
current reconciled album already marks Queue 023 `InvasionGraph.cpp` COMPLETE from the earlier C022
tranche. Therefore the ordinary next pending queue after Queue 022 is Queue 024 `CyCity.cpp` (with
Queue 025 `StartingPositionIteration.cpp` still separately marked as the priority-next-WIP target in
the queue notes; follow the latest album scheduling rule when Queue 022 actually closes rather than
re-auditing completed Queue 023).

Exact continuation cursor:
WIP448 physical -> Queue 022 OPEN -> F613-F622 confirmed -> F623/KI#945 unused -> remaining live
fitness/overlap/finalization reconciliation -> dormant sanitation controls -> whole-file/whole-album
closure -> if clean close Queue 022 without inventing F623 -> resolve Queue 024-vs-priority-025 from
the current queue scheduling notes -> freeze the next physical checkpoint.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP448
===============================================================================


===============================================================================
C031-WIP449 — QUEUE 022 TrueStarts.cpp — F623 / KI#945 CONFIRMED
===============================================================================

STATUS
------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` remains OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / KI#944 CONFIRMED.
F623 / KI#945 CONFIRMED.
F624 / provisional KI#946 UNUSED.
No repository/gameplay source files modified.

F623 / KI#945 — TRUE STARTS' EMPTY-NEW-WORLD DETECTION COMPARES LOCALIZED MAP-OPTION DISPLAY TEXT TO ENGLISH LITERALS, SO SUPPORTED NON-ENGLISH GAMES CAN LOSE THE OLD-WORLD CIV FILTER
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
`TrueStarts::changeCivs()` decides whether the generated map intentionally leaves a New World empty
before it builds the candidate civilization pool. For several scripts it does so by asking
`CvMap::isCustomMapOption` to search the *display description* returned by the map script:

    m_bEmptyNewWorld =
    (
            szMapName == CvWString("Terra") ||
            szMapName == CvWString("Earth2") ||
            ((szMapName == CvWString("PerfectMongoose") ||
            szMapName == CvWString("Tectonics") ||
            szMapName == CvWString("RandomScriptMap")) &&
            // This will only work when playing in English
            GC.getMap().isCustomMapOption("Old World", true)) ||
            ...
    );

The comment is literally correct: this branch does not inspect an option ID/value or an invariant
script token. It searches a user-visible string after the Python map script has localized it.

`CvMap::isCustomMapOption` documents the contract explicitly:

    /*  ...
        Translations will have to be handled by the caller (by generating szOptionsValue
        through gDLL->getText). */

and then obtains each current option description by calling the Python map script's
`customMapOptionDescription`. Therefore the description being compared is in the active UI
language whenever the script returns a translated key.

CLEAN CURRENT REPRODUCER: TECTONICS
-----------------------------------
Current shipped `PrivateMaps/Tectonics.py` defines landmass option 0 with nine values. Selection 8
is the dedicated Terra mode whose starting positions are forced onto the best/Old-World landmass:

    selection_names = [
        ...,
        "TXT_KEY_MAP_SCRIPT_TERRA",
        "TXT_KEY_MAP_SCRIPT_TERRA_OLD_WORLD_START"]

    if (iOption == 0):
        return unicode(CyTranslator().getText(selection_names[iSelection], ()))

and later:

    userInputLandmass = CyMap().getCustomMapOption(0)
    ...
    if (userInputLandmass == 8):     # "Terra"
        allOnBest = true

The local AdvC 1.14 text data supplied with the audit gives the relevant translation key as:

    <Tag>TXT_KEY_MAP_SCRIPT_TERRA_OLD_WORLD_START</Tag>
    <English>Randomized Earth (Old World Start)</English>
    <French>Randomisation de la Terre (départ : Antiquité)</French>
    <German>Zufällige Erde (Start Alte Welt)</German>
    <Italian>Terra casuale (partenza Vecchio Mondo)</Italian>
    <Spanish>Tierra al azar (inicio en Viejo Mundo)</Spanish>

In English, the selection-8 description contains `"Old World"`, so the True Starts test succeeds.
In French it contains neither `"Old"` nor `"World"`; the same selected map option therefore fails
the English substring search and leaves `m_bEmptyNewWorld` false. German, Italian and Spanish also
do not contain the English substring.

There is no later Tectonics-specific OR term that repairs the result. Thus the same Tectonics map
configuration is classified as an empty-New-World game in English and as an ordinary unrestricted
world in another fully supported Civ4 language.

SECOND INDEPENDENT CURRENT REPRODUCER: RANDOMSCRIPTMAP
-----------------------------------------------------
This is not peculiar to one AdvC-added Tectonics label. Current `PrivateMaps/RandomScriptMap.py`
uses the standard translated key for its AI-start option 2:

    startAi_names = [
        "TXT_KEY_MAP_SCRIPT_NO_CONDITIONS",
        "TXT_KEY_MAP_SCRIPT_DEFAULT",
        "TXT_KEY_MAP_SCRIPT_ALL_IN_OLD_WORLD"]
    ...
    return unicode(CyTranslator().getText(szName, ()))

and its actual setup logic makes selection 2 unconditionally force the Old-World start rule:

    elif (aiStartMethod == 2):
        gl_aiStartType = STA_OLDWORLD

The Civ4/BtS text data supplied through the local Civ4CE reference archive defines that key as:

    <English>All In Old World</English>
    <French>Tous dans l'Antiquité</French>
    <German>Alle in Alter Welt</German>
    <Italian>Tutto nel Vecchio Mondo</Italian>
    <Spanish>Todos en el Viejo Mundo</Spanish>

Again, the English `"Old World"` substring exists only in English. A RandomScriptMap setup using a
non-Terra generation method plus `All In Old World` is therefore detected by True Starts in English
but missed in the other supported languages. (The separate `isCustomMapOption("Terra")` OR only
helps when the chosen generation method itself is Terra; it does not repair the general forced
Old-World-start option.)

WHY THIS CHANGES LIVE TRUE-STARTS BEHAVIOR
------------------------------------------
`m_bEmptyNewWorld` directly controls the first candidate-building pass. When true, True Starts
rejects civilizations whose historical coordinates lie in the New World before performing its
fallback pass:

    if (m_bEmptyNewWorld && iPass == 0)
    {
        int iLongitudeTimes10 = kTruCiv.get(...LongitudeTimes10);
        int iLatitudeTimes10 = kTruCiv.get(...LatitudeTimes10);
        if (iLongitudeTimes10 < 300 ||
            (iLongitudeTimes10 > 1100 && iLatitudeTimes10 < -100))
        {
            continue;
        }
    }

So the localization failure is not a menu-label cosmetic issue. It changes which civilizations are
eligible in the preferred True Starts pool on exactly the map modes designed to reserve an empty New
World. In a non-English game, New-World civilizations can enter the normal first-pass candidate
pool even though the map script is simultaneously forcing starting plots into its Old World.

That can alter civilization assignment for random players and defeats the intended True Starts
coordination with the map script. F615 and F616 concern what happens *after* an empty-New-World map
has been recognized; F623 prevents that state from being recognized in the first place.

CURRENT-LIVE / LANGUAGE SUPPORT BOUNDARY
----------------------------------------
French, German, Italian and Spanish are not hypothetical third-party localizations here: both the
AdvC Tectonics key and the standard BtS RandomScriptMap key contain explicit translations for those
languages in the locally supplied game/reference data. Tectonics and RandomScriptMap are shipped
current SAS map scripts, and their current option-description functions actively call
`CyTranslator().getText` for the affected selections.

By contrast, some other branches in the same True Starts expression happen to use scripts whose
current option text is literal English. For example, current PerfectMongoose selection 1 is the
literal string `"Old World (unless Pangaea); -30% players recommended"`, and current Totestra's
relevant description is literal `"Keep New World empty"`; those particular calls are not rescued by
good design, they merely remain language-invariant because those scripts themselves do not localize
that display text. The finding does not require every branch to fail: Tectonics and RandomScriptMap
already provide supported, current-live reproductions.

A useful local contrast exists in the same `TrueStarts.cpp`: the balanced-resource check correctly
passes translated text into this helper:

    m_bBalancedResources =
            kMap.isCustomMapOption(gDLL->getText("TXT_KEY_MAP_BALANCED"));

That demonstrates both the intended API contract and that True Starts already knows how to avoid
English-only comparison where a stable translated key exists.

HISTORY / BASE BOUNDARY
-----------------------
AdvC practical 3579 (`4977b513daa6bc436f6d0ea9e8ee011b131b8d36`), the original True Starts merge,
introduced the `m_bEmptyNewWorld` map-option detection with the same literal
`isCustomMapOption("Old World", true)` test. The original practical already therefore carried the
locale-dependent root.

Current Base AdvC 1.14 still has the same test and now contains the candid source comment:

    // This will only work when playing in English

No locally mirrored later commit changes the exact `Old World` call; the 6,403-diff corpus points
back to the original practical for this root. Current SAS retains the Base AdvC 1.14 behavior
unchanged. This is therefore inherited AdvC True Starts source debt, not a SAS regression and not a
K-Mod/Civ4CE True Starts carryover (those references do not contain this AdvC implementation).

The fact that the source comments on the limitation does not make the gameplay behavior correct; it
is useful provenance showing that the locale dependency was known in code but never made robust.
The current Known-Issues document and audit album do not otherwise own it.

DEDUP / CONTROLS
----------------
Focused whole-album and Known-Issues searches found no existing True Starts finding for localized
map-option descriptions, the literal `"Old World"` test, or non-English empty-New-World detection.

F615 / KI#937 is separate: once `m_bEmptyNewWorld` is true, its Americas longitude cutoff has the
wrong sign/threshold behavior. F616 / KI#938 is separate: the fallback candidate pass appends to the
first-pass vectors without clearing them. F623 occurs earlier and can leave `m_bEmptyNewWorld`
false altogether.

The source's own English-only comment is also not a duplicate issue owner; it is an acknowledgment
inside the affected code, with no implemented correction or player-facing KI entry found locally.

REPAIR BOUNDARY
---------------
The robust repair should stop inferring semantic map-option state from localized display text where
True Starts already knows the map script. Script-specific numeric option IDs/selection values are
language-independent and directly encode the same conditions used by the scripts themselves.

For the clean Tectonics reproducer, the invariant test is simply its landmass option 0 being
selection 8. For RandomScriptMap, AI-start option 1 selection 2 directly means forced Old-World
starts; generation-method option 0 selection 2 is its Terra mode. PerfectMongoose likewise already
stores Old-World starts as option 1 selection 1. Using those invariant values avoids both translation
and wording drift.

A translation-based repair is possible for individual standard-key choices, but it is less general:
the scripts use different labels/keys, and literal-English scripts coexist with translated ones.
Moreover `CvMap` itself documents that display-string matching requires the caller to manage
translations. The clean architectural boundary is therefore to query known semantic option values
for these known scripts rather than parse what the UI happens to display.

Regression coverage should run at least Tectonics option 0=8 and RandomScriptMap AI-start option
1=2 under English and French and assert that `m_bEmptyNewWorld` is identical. True Starts' preferred
candidate pool should not depend on UI language.

CURRENT STONE AFTER C031-WIP449
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — OPEN.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / KI#944 CONFIRMED.
F623 / KI#945 CONFIRMED.
F624 / provisional KI#946 UNUSED.
Latest physical checkpoint: C031-WIP449.
No repository/gameplay source modified.

NEXT
----
Exclude F623 and return to closure mode. Finish the remaining current-live final-assignment and
fitness invariants, then perform the full function-by-function Queue-022 reconciliation against
F613-F623 and all prior album coverage. Keep the sanitation-only `changeVegetation()` reversed
winner assignment and `sanitize()` bound-order hazard explicitly dormant while the shipped
sanitation defines remain disabled. Do not force F624 if nothing distinct and current-live survives.

Also preserve one adjacent `CvMap` API observation for that file's own audit rather than conflating it
with F623: the `CvWString` convenience overload of `isCustomMapOption` forwards only the converted
string and drops its own `bCheckContains`/`bIgnoreCase` arguments. Existing exact-match translated
calls with default flags can still work; this is a separate helper-contract candidate to dedup when
`CvMap.cpp` is audited, not part of the True Starts localization root proven here.

The reconciled queue state remains: Queue 023 `InvasionGraph.cpp` is already COMPLETE; the ordinary
next pending queue is Queue 024 `CyCity.cpp`, while Queue 025 `StartingPositionIteration.cpp` has a
separate priority-next-WIP note. Resolve that scheduling note only after Queue 022 actually closes.

Exact continuation cursor:
WIP449 physical -> Queue 022 OPEN -> F613-F623 confirmed -> F624/KI#946 unused -> remaining live
fitness/finalization closure -> dormant sanitation controls -> whole-file/whole-album reconciliation
-> close Queue 022 if clean -> resolve Queue024-vs-priority025 scheduling -> freeze next physical
checkpoint.

No gameplay/source files were modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP449
===============================================================================


===============================================================================
C031-WIP450 — QUEUE 022 TRUESTARTS.CPP CLOSURE / F624 UNUSED
===============================================================================

PHYSICAL CONTINUATION BASIS
---------------------------
Reconciled current repository album from `AdvCiv-SAS_light_source_20260907T070730.zip`, which
already contains the C031-WIP449 material through F623 / KI#945. The fresh local source was checked
against that stone before continuing. No gameplay/source file was modified during this pass.

STARTING STONE
--------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` OPEN.
F613 / KI#935 through F623 / KI#945 CONFIRMED.
F624 / provisional KI#946 UNUSED.
Latest prior physical checkpoint: C031-WIP449.

RECONCILIATION OF THE FRESH CODEX ALBUM
---------------------------------------
The fresh reconciled album contains the full F622 and F623 proofs and still records WIP449 as the
latest authoritative physical checkpoint. It also carries the queue scheduling rule at the album
front: after Queue 022 closes, every still-PENDING entry marked `priority next wip` must be processed
before ordinary queue order. Queue 025 `StartingPositionIteration.cpp` is therefore next ahead of
ordinary Queue 024 `CyCity.cpp`.

No post-WIP449 gameplay finding had already been promoted by the reconciliation. F624 remained free.

F624 CANDIDATE CLOSED — m_rMedianSpace CANNOT BE ZERO ON THE LIVE PATH
---------------------------------------------------------------------
The first continuation cursor from WIP449 was this division in `calcFitness()`:

    scaled rSpace = kStats.areaSpaceWeights() / m_rMedianSpace;

The denominator is built in the constructor as the median of every True-Starts player's
`SurroundingsStats::areaSpaceWeights()`.

A direct iterator/weight proof shows that each individual sample is at least 1:

1. `getSurroundings()` creates a `PlotCircleIter` with the default `bIncludeCenter=true`.
2. `SpiralPlotIterator::init` therefore exposes the player's own starting plot as the first element.
3. At that center, `distWeight(kStart, kStart, radius)` is exactly 1 because plot distance is 0.
4. The rival-overlap reduction in `calculatePlotWeights()` cannot reduce the center below 1:
   `rOtherDistWeight <= 1`, while `rDistWeight == 1`, so
   `max(0, rOtherDistWeight - rDistWeight) == 0` for every rival.
5. The start necessarily belongs to its own area and is not a lake starting tile, so the
   `SurroundingsStats` same-area branch executes and adds `sqrt(1) == 1` to
   `m_rAreaSpaceWeights`.

Thus every `areaSpaceWeights()` sample is >=1, and every nonempty median is >=1. The constructor only
leaves `m_rMedianSpace` at its default when there are no True-Starts players, in which case
`calcFitness()` is never invoked for a True-Starts player. The denominator is therefore safe on the
current live path. No F624.

F624 CANDIDATE DEDUP — DISTINCT-CIV SHORTAGE IS ALREADY F613 / KI#935
---------------------------------------------------------------------
A fresh mechanical pass over `changeCivs()` independently rediscovered this suspicious test:

    if (m_validAICivs.size() < m_truPlayers.size() ||
        aeValidHumanCivs.size() < m_truHumans.size())
    {
        bUniqueCivs = false;
    }

Current shipped True-Starts data has 34 distinct supported civilizations but 52 native
civ/leader pairs (20 one-leader civs, 10 two-leader civs, 4 three-leader civs). With 35-48 all-random
players, the 52 pair count can satisfy the first comparison although 34 distinct civilizations
cannot satisfy uniqueness. The later assignment filter can then exhaust all distinct civs and leave
late players unassigned.

This is a genuine live defect, but the reconciled album confirms that this exact root and current
34-vs-52 reproducer are already F613 / KI#935, the first finding of Queue 022. It is therefore an
important dedup positive control, not F624.

REMAINING LIVE DIVISION / INDEX / STATE SWEEP
---------------------------------------------
The remainder of the current-live arithmetic/index/state pass did not produce an independent F624.
The main denominator checks close as follows:

- `calculateRadius`: current shipped world sizes all have a positive `iDefaultPlayers`
  (Arena 2 through SAS48 48), while at least one alive player exists when this code is useful;
  `rCrowdedness` is therefore positive.
- geographic fitness: `CvTruCivInfo` longitude/latitude domains are fixed at -1800..1800 and
  -900..900, producing positive `m_iMaxGeoDist`; valid maps have positive `maxPlotDistance()`.
- average distance error divides only when `iOtherPlayers > 0`.
- river mean divides only when `areaRiverWeights() > 0`.
- oceanity normalization guards the zero/one median endpoints before each division.
- peak-map normalization divides by `m_rMedianPeakScore` only after proving it greater than a
  positive threshold.
- horizontal stretch uses `max(areaYSpaceWeights(), scaled::epsilon())`.
- climate-region mean divides only after rejecting a zero region weight / invalid start region.
- close-match precipitation ratios are entered only with at least one possible region match.
- the already-reconciled singleton climate-variation fixed-point 0/0 remains an ugly robustness
  path but still does not demonstrate a wrong current gameplay result under the established finding
  threshold.
- the integer `iMaxTimeDiff==0` contemporaries candidate from the prior tranche remains closed for
  current shipped data: the only equal-year pair (Qin/Hannibal at -221) cannot form a current
  candidate set whose maximum contemporary difference stays zero; when China is eligible Mao's
  1949 record makes the maximum positive, and removing China removes Qin from the candidate matrix.

The first all-random contemporaries fitness pass can also evaluate `0 / sqrt(0)` in AdvC's fixed-point
`scaled` arithmetic because no leader has yet been locked. The numerator is necessarily zero in that
state (there are no `m_leaders` entries to add contemporary value), so this follows the same
fixed-point zero/zero robustness family and does not supply a nonzero gameplay error. It is not
promoted.

REMAINING LIVE FUNCTION RECONCILIATION
--------------------------------------
Every current-live TrueStarts function has now been reconciled against F613-F623, prior album
coverage, or a clean invariant:

- constructor / player-provenance collection: F620 owns the random-civ/manual-leader overwrite;
  documented network/scenario broad reassignment behavior remains an explicit design limitation.
- `overrideScenarioOptions`: latitude-limit changes are propagated correctly because
  `CvMap::setLatitudeLimits` immediately calls `updateLatitude()` for every plot; no stale cached
  latitude bug survives.
- `initTargetLatitudes`: the suspicious scaling expressions remain numerically harmless with current
  inherited Temperate baseline changes of zero; no current result mismatch proven.
- `changeCivs`: F613-F616, F618-F621 and F623 own the confirmed candidate-pool, missing-data,
  empty-New-World, unrestricted-leader, uninitialized-elevation, provenance and oceanity-baseline
  defects. Remaining assignment/color/team/handicap state checks hold.
- `initContemporaries`: current candidate data prevents a live zero integer divisor; asymmetry and
  randomized neighbor count are explicit design choices.
- `calculateRadius`, `updateFitnessValues`, `calculatePlotWeights`, `getSurroundings`, and
  `SurroundingsStats`: no independent TrueStarts contradiction survives the denominator/index/state
  sweep.
- `calcFitness`: all uncovered live denominators and candidate-state accesses either have a positive
  invariant/guard or belong to an already-owned finding.
- `calcClimateFitness`: F622 owns the wrapped-seam directional-filter defect; the remaining
  singleton-variation arithmetic stays below threshold as above.
- `precipitation`: F617 owns the live polar-feature enum/boolean bug; no second live enum mix-up was
  found.

SANITATION-ONLY SOURCE DEBT REMAINS DORMANT
-------------------------------------------
The previously observed resource/vegetation mutation defects remain preserved but are not promoted
under Queue 022's current-live threshold because shipped SAS has normal and scenario True-Starts
sanitation disabled. In particular, keep the reversed `changeVegetation()` best-destination winner
assignment and the `sanitize()` bound/order hazard as explicit dormant controls for a future pass if
those defines are enabled or another shipped caller emerges.

ADJACENT FUTURE ROOT — PLOTRADIUSITERATOR WRAP DUPLICATION
----------------------------------------------------------
The closure pass exposed one useful cross-file observation that should be carried to the pending
`PlotRadiusIterator` audit rather than misassigned to TrueStarts.

`SpiralPlotIterator` keeps unwrapped iterator coordinates but resolves each coordinate through
`CvMap::plotValidXY`. It has no visited-plot suppression. On a wrapped dimension, once its radius
reaches at least half that dimension, opposite unwrapped coordinates can therefore resolve to the
same physical `CvPlot` and be yielded more than once.

A current shipped reproducer exists without exotic mod data:

- `WORLDSIZE_ARENA` grid width is 14;
- `TrueStarts::calculateRadius()` gives a base surroundings radius of 7 at ordinary 2/2 crowdedness;
- current `PrivateMaps/Fractal.py` defaults its world-wrap option to cylindrical (`getWrapX()==true`).

At X offsets -7 and +7 from the same Arena start, the two unwrapped coordinates identify the same
wrapped map column. `PlotCircleIter` can therefore revisit the same physical opposite-seam plots.
TrueStarts' broad `SurroundingsStats` and bonus loops can then count those physical plots more than
once.

This looks worth a dedicated root audit because the iterator is general and its own queue is still
PENDING (Queue 167 in the current album). Do not spend F624 on it here without first determining the
intended general iterator contract, breadth across callers, and prior dedup when that root file is
audited. F622 is separate: it is a TrueStarts-specific misuse of raw resolved coordinates even at
small climate-region radius and does not require duplicate iterator output.

ADJACENT CvMap OBSERVATION PRESERVED
------------------------------------
Continue to preserve the WIP449 CvMap helper candidate separately: the `CvWString` overload of
`isCustomMapOption` drops its own `bCheckContains` / `bIgnoreCase` parameters when forwarding. It is
not part of F623 and should be deduped/proven in the CvMap audit.

QUEUE 022 FINAL STATE
---------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — COMPLETE.
F613 / KI#935 CONFIRMED.
F614 / KI#936 CONFIRMED.
F615 / KI#937 CONFIRMED.
F616 / KI#938 CONFIRMED.
F617 / KI#939 CONFIRMED.
F618 / KI#940 CONFIRMED.
F619 / KI#941 CONFIRMED.
F620 / KI#942 CONFIRMED.
F621 / KI#943 CONFIRMED.
F622 / KI#944 CONFIRMED.
F623 / KI#945 CONFIRMED.
F624 / provisional KI#946 UNUSED.
No gameplay/source file modified.

NEXT QUEUE RESOLUTION
---------------------
The album-front scheduling rule resolves the earlier Queue024-vs-Queue025 ambiguity. Queue 025
`CvGameCoreDLL/StartingPositionIteration.cpp` is explicitly marked `priority next wip`, so it is the
next file after closing Queue 022. Ordinary Queue 024 `CyCity.cpp` remains PENDING and follows after
priority-next-WIP entries are handled according to the queue rule.

Exact continuation cursor:
C031-WIP450 physical -> Queue 022 COMPLETE -> F624/KI#946 still unused -> Queue 025
`StartingPositionIteration.cpp` OPEN (priority-next-WIP) -> establish file lineage/current SAS deltas
and prior album ownership -> audit live initialization/iteration/scoring/state invariants -> freeze
next physical checkpoint regardless of whether a finding is promoted.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP450
===============================================================================


===============================================================================
C031-WIP451 — QUEUE 025 STARTINGPOSITIONITERATION: FIRST CONFIRMED ROOT
===============================================================================

PHYSICAL CONTINUATION / RECONCILIATION
--------------------------------------
This checkpoint continues directly from C031-WIP450. The freshly supplied reconciled
`AdvCiv-SAS_light_source_20260907T070730.zip` was checked first: its repository album contains the
WIP449 True-Starts findings F622/F623 and still reserves F624 / provisional KI#946. WIP450 remains
the later ChatGPT physical closure stone: Queue 022 `TrueStarts.cpp` is COMPLETE, F624 was unused at
that closure, and Queue 025 `StartingPositionIteration.cpp` is the priority-next-WIP file.

No repository/gameplay source was modified. All investigation below used the local SAS snapshot,
its locally mirrored reachable-history diffs, Base AdvC 1.14, K-Mod and Civ4CE reference archives.

QUEUE 025 LINEAGE / SAS DELTA BOUNDARY
--------------------------------------
`StartingPositionIteration.cpp` is an AdvC-added subsystem (`advc.027`), not inherited BtS/K-Mod
code. The local history mirror contains 48 commits touching the file. Only two later SAS-branch
commits touch it materially in the available history:

- SAS practical 5314 changes SPI found-value storage from `short` to `int`, explicitly preventing
  found-value overflow/underflow; current SAS carries the corresponding custom source comment.
- SAS practical 5961 is broad signature-formatting/collapse work and does not establish a new
  gameplay-semantic root in the portion audited so far.

Accordingly, current semantic defects that survive unchanged from the AdvC lineage should be
classified as inherited AdvC unless a later SAS change is proven causal.

F624 / PROVISIONAL KI#946 — CONFIRMED INHERITED ADVC NORMALIZATION-LEVEL FALLTHROUGH
------------------------------------------------------------------------------------
SEVERITY / CLASS: current-live gameplay normalization defect; inherited AdvC control-flow bug.
ROOT FILE: `CvGameCoreDLL/StartingPositionIteration.cpp`,
`NormalizationTarget::isReached(..., bNearlyReached=true, ...)`.

CURRENT CODE
------------
The current function starts with a base tolerance of 2.5%, then tries to vary the extra tolerance
by the game's configured starting-plot normalization level:

    scaled rTolerance = fixp(2.5);
    if (bNearlyReached)
    {
        switch(GC.getGame().getStartingPlotNormalizationLevel())
        {
        case CvGame::NORMALIZE_HIGH: rTolerance += 2;
        case CvGame::NORMALIZE_MEDIUM: rTolerance += 4;
        default: rTolerance += 6;
        }
    }
    rTolerance /= 100;
    ...
    return (rIncreaseNeeded <= rTolerance * iCurrFoundVal);

There are no `break` statements and no intentional-fallthrough annotation.

The actual tolerances before the later handicap adjustment are therefore:

- LOW: 2.5 + 6 = 8.5%
- MEDIUM: 2.5 + 4 + 6 = 12.5%
- HIGH: 2.5 + 2 + 4 + 6 = 14.5%

This reverses the semantic direction of the normalization levels. The stronger the requested
normalization, the more tolerant the target becomes and the earlier it can report that a weak start
is already "nearly reached."

CONTRACT / LIVE CALLER PROOF
----------------------------
`CvGame.h` explicitly documents the three levels as:

    1: low    (weak starting plots on average, high variance); for single-player
    2: medium (strong starting plots, low variance); for multi-player
    3: high   (very strong starting plots, low variance); BtS/K-Mod behavior

`NormalizationTarget::isNearlyReached` simply forwards to `isReached(..., true)`. The live caller in
`CvGame::normalizeAddExtras` tests it while traversing candidate resource plots:

    if (pTarget != NULL ? pTarget->isNearlyReached(*pStartingPlot) :
        citySiteEval.evaluate(*pStartingPlot) >= rTargetValue)
    {
        ...
        goto done_placing_resources;
    }

So a *larger* tolerance means "nearly reached" succeeds sooner and resource normalization stops
sooner. HIGH and MEDIUM should not be more permissive than LOW here; that directly contradicts the
level contract.

A simple boundary illustrates the live effect. Suppose a site's computed `rIncreaseNeeded` is 10%
of its current found value before the common handicap adjustment:

- the structurally intended HIGH tolerance (4.5%; see history below) would return false and continue
  trying to normalize the site;
- current HIGH fallthrough gives 14.5%, returns true, and stops adding normalization bonuses;
- current MEDIUM likewise stops at 12.5%;
- LOW at 8.5% would continue.

The result can therefore be the opposite of the configured normalization strength.

CURRENT-LIVE ACTIVATION
-----------------------
Current SAS has:

    ENABLE_STARTING_POSITION_ITERATION = 1
    NORMALIZE_STARTPLOTS_AGGRESSIVELY = 0

So SPI and its `NormalizationTarget` are live on supported map scripts that do not override the
relevant starting-position path. With the default normalization define, ordinary single-player is
normally LOW and therefore hits only the `default` arm; that common case masks the bug. The other
supported current paths do not:

- `CvGame::setStartingPlotNormalizationLevel` promotes multiplayer games with more than one human
  from LOW to MEDIUM;
- the "Balanced" map option explicitly sets normalization to HIGH;
- HIGH is also the documented aggressive/BtS/K-Mod level and can be selected through the existing
  normalization setting path.

Thus this is not an unreachable configuration or mod-data hypothetical. MEDIUM multiplayer and HIGH
Balanced/aggressive normalization are current supported paths, and the only `isNearlyReached`
caller is specifically part of start normalization.

HISTORY — MISSING BREAKS ARE PROVABLE, NOT A STYLE GUESS
---------------------------------------------------------
The local reachable-history mirror gives unusually strong intent evidence.

AdvC practical 2302 (`d6ea7fc8701bfe2d4353437da01e5d5b392d8b7e`, title
"Starting positions (cont.), normalization.") introduced this code as:

    scaled rTolerance = 2;
    if (bNearlyReached)
    {
        switch(GC.getGame().getStartingPlotNormalizationLevel())
        {
        case CvGame::NORMALIZE_HIGH: rTolerance = 4;
        case CvGame::NORMALIZE_MEDIUM: rTolerance = 6;
        default: rTolerance = 8;
        }
    }

Without breaks, HIGH sets 4, then 6, then 8; MEDIUM sets 6, then 8. All three levels therefore
collapse to 8%. That already defeats the purpose of switching on the normalization level.

Just three practical commits later, AdvC practical 2305
(`82c08218c66b1a01049f4bf81647a77ab3949573`, title
"More tweaks, fixed miscalculations for starting positions") changed the base from 2 to 2.5 and
changed those assignments to additions:

    HIGH:   rTolerance += 2;
    MEDIUM: rTolerance += 4;
    default:rTolerance += 6;

but retained the missing breaks. With normal non-fallthrough switch semantics, this is an extremely
coherent adjustment of the earlier intended 4/6/8 values to 4.5/6.5/8.5 — each target rises by
exactly 0.5 while preserving HIGH < MEDIUM < LOW. With the inherited fallthrough, the same tweak
instead produces 14.5/12.5/8.5 and reverses that ordering.

That two-commit evolution makes the omitted breaks a concrete control-flow error rather than an
argument about preferred tuning. Current Base AdvC 1.14 retains the same fallthrough unchanged.
K-Mod and Civ4CE do not contain this AdvC-created SPI implementation, so the root is inherited AdvC,
not BtS/K-Mod/Civ4CE and not introduced by SAS.

DEDUP
-----
Focused searches of the reconciled audit album and Known Issues found no existing owner for the
`NormalizationTarget` normalization-level tolerance fallthrough or the HIGH/MEDIUM/LOW reversal.

The only prior StartingPositionIteration-specific KI found in the current Known-Issues corpus is
KI#492 / album F169, which is a separate performance defect: workable-plot precomputation performs
and discards full city-site evaluations. It does not overlap this normalization stop condition.

The repository Known-Issues file currently carries KI#946 only as the WIP449 reservation saying
F624 remained unassigned while Queue 022 was open. WIP450 subsequently closed Queue 022 without
using it. F624 is now legitimately consumed here by the first independent Queue-025 root.

REPAIR BOUNDARY
---------------
The narrow semantic repair is to terminate each explicit case, preserving the apparent intended
values from practical 2305:

    case CvGame::NORMALIZE_HIGH:
        rTolerance += 2;
        break;
    case CvGame::NORMALIZE_MEDIUM:
        rTolerance += 4;
        break;
    default:
        rTolerance += 6;
        break;

That yields 4.5% HIGH, 6.5% MEDIUM and 8.5% LOW before the existing common handicap adjustment. It
restores the documented direction: stronger normalization requires a site to get closer to target
before `isNearlyReached` stops resource placement.

Regression coverage should evaluate the same SPI-produced `NormalizationTarget` / start state at
LOW, MEDIUM and HIGH and assert monotonicity:

    HIGH tolerance < MEDIUM tolerance < LOW tolerance

and, for a boundary state such as a required increase around 10%, verify that LOW may call the site
nearly reached while MEDIUM/HIGH continue normalization — never the reverse solely because the
normalization level is stronger.

OTHER QUEUE-025 CONTROLS FROM THIS TRANCHE
------------------------------------------
No other root is promoted in this checkpoint.

1. `DistanceTable` stores Dijkstra distances and queue-node distances as signed `short`. In theory a
   sufficiently labyrinthine <=12,000-plot supported custom map could make a simple shortest path
   exceed 32,767 weighted distance units and narrow on queue insertion. Ordinary current map-script
   geometry is far below that threshold, and no shipped reproducer has been established yet. Keep
   this as robustness debt/candidate only; do not spend F625 without a supported current path.

2. The cross-file `PlotCircleIter` wrap-duplication observation carried from WIP450 is directly
   relevant here: `PotentialSites::recordSite` uses `PlotCircleIter(kPlot, 8, false)`, enough to
   exceed half-width on 14-wide wrapped Arena maps. This reinforces Queue 167
   `PlotRadiusIterator.cpp` as the likely root owner. Do not duplicate it into Queue 025 before the
   iterator contract/root is audited.

CURRENT STONE AFTER C031-WIP451
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — COMPLETE.
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN (priority-next-WIP).
F613-F623 / KI#935-KI#945 — confirmed prior TrueStarts roots.
F624 / provisional KI#946 — CONFIRMED here: inherited AdvC normalization-level tolerance switch
fallthrough makes MEDIUM/HIGH stop "nearly reached" normalization too early and reverses the
intended strength ordering.
F625 / provisional KI#947 — UNUSED next root ID.
No repository/gameplay source modified.

Exact continuation cursor:
C031-WIP451 physical -> Queue 025 OPEN -> exclude F624 -> continue PotentialSites/DistanceTable /
SpaceEvaluator / iteration-step / team-assignment / NormalizationTarget state and arithmetic audit ->
dedup against F169/KI#492 and all prior album findings -> preserve general PlotRadiusIterator wrap
candidate for Queue 167 -> freeze next physical checkpoint regardless of whether F625 is promoted.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP451
===============================================================================


===============================================================================
C031-WIP452 — QUEUE 025 DISTANCETABLE WATER-FALLBACK DISTANCE UNIT MISMATCH
===============================================================================

PHYSICAL CONTINUATION
---------------------
This checkpoint continues directly from C031-WIP451 / Queue 025
`CvGameCoreDLL/StartingPositionIteration.cpp`. F624 / provisional KI#946 remains the confirmed
normalization-level fallthrough from WIP451. F625 / provisional KI#947 was unused at that stone.

No repository/gameplay source was modified. Investigation used the current reconciled SAS snapshot,
its local reachable-history mirror, Base AdvC 1.14, and the current XML data.

F625 / PROVISIONAL KI#947 — CONFIRMED INHERITED ADVC DISTANCETABLE UNIT MISMATCH
---------------------------------------------------------------------------------
SEVERITY / CLASS: current-live starting-position space-evaluation defect; inherited AdvC arithmetic /
metric mismatch.
ROOT FILE: `CvGameCoreDLL/StartingPositionIteration.cpp`,
`StartingPositionIteration::DistanceTable::DistanceTable` water-destination fallback.

CURRENT CODE / CONTRACT
-----------------------
`DistanceTable` is explicitly a weighted path-distance system. `computeDistances` runs Dijkstra and
`stepDist` assigns ordinary land movement costs of 9 for cardinal steps and 12 for diagonal steps:

    if (!kTo.isWater())
        return (bDiagonal ? 12 : 9);

The header documents the same scale through:

    /* Typical distance according to DistanceTable::stepDist
       between two friendly adjacent cities. */
    short getAvgCityDist() const { return 40; }

and the source immediately above `stepDist` warns:

    /* If the scale of the distances is changed, the value returned by
       getAvgCityDist needs to be adjusted. */

Water work tiles need special handling because a city can work them without a settler/ship path to
the water tile. The constructor therefore replaces a water destination's ordinary Dijkstra value
with the distance to a nearby legal land city site, plus working/improvement adjustments.

For land sites that are themselves destinations this is scale-consistent:

    short iDist = d(kSource, *it);
    ...
    iShortestDist = iDist;

But the explicitly documented fallback for a legal low-value city site that was omitted from the
DestinationTable does this instead:

    else if (!bDest && it->sameArea(kSource))
    {
        short iDist = static_cast<short>(kMap.plotDistance(&kSource, &*it));
        ...
        iShortestDist = iDist;
    }

It then immediately adds values expressed in the weighted DistanceTable scale:

    iShortestDist += getAvgCityDist() / 5;          // +8
    if (!bInnerRing)
        iShortestDist += getAvgCityDist() / 4;      // +10
    if (kWaterDest.getBonusType(NO_TEAM) != NO_BONUS)
        iShortestDist += (5 * getAvgCityDist()) / 8;// +25

`CvMap::plotDistance`, however, is the normal octile geometric metric in tile-distance units. A
10-tile cardinal separation returns 10, while `DistanceTable::stepDist` assigns 90 to the same ten
ordinary cardinal land steps. The fallback therefore mixes two incompatible units in one stored
`DistanceTable` value.

CONCRETE CURRENT-LIVE OCEAN-FISH / SNOW PATH
---------------------------------------------
The source itself names the intended fallback case:

    "Bad land tiles aren't stored in the DistanceTable, so this will fail for e.g.
     an Ocean Fish workable only from a Snow tile. Fall back on CvMap::plotDist ..."

Current SAS XML makes this a concrete supported map state rather than a historical comment:

- `TERRAIN_SNOW` has no base yields (`<Yields/>`), is land (`bWater=0`), passable
  (`bImpassable=0`) and city-foundable (`bFound=1`).
- `BONUS_FISH` has no reveal-tech gate, adds +1 food, and is allowed on both Coast and Ocean.

The SPI precompute stores a plot in `relevantPlots` only when its SPI workable-plot value is positive
or it is already a candidate/current city site:

    if (yieldValues.get(eLoopPlotNum) > 0 ||
        sitePlotNums.count(eLoopPlotNum) > 0)
        relevantPlots.push_back(...);

For an ordinary featureless/resource-less Snow tile, `CitySiteEvaluator::evaluateWorkablePlot`
starts from zero natural yields. The future-improvement multiplier cannot make zero positive; the
population-sustenance subtraction leaves no positive yield contribution, and the later marginal
space term also adds nothing because its `iVal` is zero. The function returns `max(r, 0)`, so such a
Snow tile can legitimately have value 0 and be omitted from `relevantPlots` despite being a legal
city site.

An Ocean Fish, by contrast, has positive food/resource value and can be retained as a water
destination. If its only legal working city site is one of those omitted Snow tiles, `bDest` remains
false and the raw-`plotDistance` fallback is taken exactly as the source comment anticipates.

NUMERIC COUNTEREXAMPLE
----------------------
Use an ordinary passable same-landmass source S and a legal zero-value Snow city site L ten cardinal
land steps away. Let an inner-ring Ocean Fish W be workable from L and from no retained land
destination.

The weighted land-path part of the intended `DistanceTable` metric is:

    10 cardinal land steps * 9 = 90

The current fallback instead stores:

    plotDistance(S,L) = 10

For an inner-ring Fish, both branches then add the same water-working/resource adjustments:

    + getAvgCityDist()/5       = +8
    + 5*getAvgCityDist()/8     = +25

So current fallback result is:

    10 + 8 + 25 = 43

where the same geometry expressed in the surrounding `DistanceTable` scale is approximately:

    90 + 8 + 25 = 123

For an outer-ring working site both receive the same additional +10, giving 53 versus roughly 133.
The error grows with land separation because every additional ordinary cardinal land tile adds only
1 in the fallback instead of 9 in the path metric.

This is not merely a display value. `SpaceEvaluator` consumes the stored distance directly:

    short iDist = kDists.d(kStartPlot, kLoopPlot) - iDistSubtr;
    ...
    rSum += claim_t(1, max(1, iDist));

and later uses the same distance for claim competition and cached access/delay factors before:

    rSpaceVal += rYieldVal * rAccessFactor;

The under-scaled fallback therefore makes affected remote water yields/resources look much closer
and more readily claimable than equivalent land/path geometry warrants, feeding the actual SPI
start-space score and consequently start-position iteration decisions.

The geometric fallback also does not preserve the obstacle/path semantics of Dijkstra's `stepDist`;
`sameArea` is only a coarse landmass check. The unit mismatch alone is sufficient to establish the
current defect, so no stronger path-topology assumption is needed for F625.

HISTORY / INHERITANCE
---------------------
AdvC practical 2302 (`d6ea7fc8701bfe2d4353437da01e5d5b392d8b7e`, "Starting positions (cont.),
normalization.") introduced the water-destination fallback and the current weighted-distance scale
in the same change.

That introduction already contains, together:

- the raw `kMap.plotDistance(&kSource, &*it)` fallback;
- ordinary land `stepDist` costs of 9/12;
- `getAvgCityDist() == 40` with the comment that it is the typical `stepDist` distance between
  adjacent cities; and
- the warning that changing the distance scale requires changing `getAvgCityDist`.

So the mismatch did not arise because a later commit rescaled `DistanceTable` while forgetting an
old fallback. It entered in practical 2302 already mixing raw tile distance with the new weighted
path metric. Focused local-history search finds the exact Ocean-Fish comment and raw fallback only in
that introduction; no later corrective change is present in the reachable mirror.

Current Base AdvC 1.14 retains the same raw fallback and the same 9/12 path costs unchanged. K-Mod
and Civ4CE do not contain this AdvC-created SPI subsystem. Classification is therefore inherited
AdvC, not SAS/BtS/K-Mod/Civ4CE.

DEDUP
-----
Focused searches of the reconciled album, WIP451 and current Known Issues found no existing owner
for this water-fallback metric/unit mismatch, Ocean-Fish/Snow fallback, or raw `plotDistance` inside
SPI `DistanceTable`.

F169 / KI#492 remains a separate SPI performance defect (discarded full city-site evaluations).
F624 / KI#946 is the separate normalization-level switch fallthrough. The general
`PlotRadiusIterator` wrapped-map duplicate-yield candidate remains reserved for Queue 167 and does
not overlap this distance root.

REPAIR BOUNDARY
---------------
The semantic requirement is narrow: any fallback value written into `DistanceTable` must use the
same weighted path-distance units and reachability semantics as `d()` / `computeDistances` before
the existing +8/+10/+25 water-work adjustments are applied.

A robust repair should therefore obtain the Dijkstra/`stepDist` distance to the otherwise-omitted
legal land city site (for example by retaining/querying full per-source Dijkstra distances for this
fallback, or by explicitly making the necessary fallback land sites distance destinations). Merely
copying raw `CvMap::plotDistance` into the table is not scale-compatible. Multiplying geometric
distance by an arbitrary constant would improve units but would still lose `stepDist`'s diagonal,
impassability/isthmus and frontier/path semantics, so it is a weaker approximation rather than the
clean contract repair.

Regression coverage should construct a positive water destination whose only legal working city
site is a zero-valued omitted land tile and assert that:

1. increasing ordinary cardinal source-to-city-site separation by one tile changes the fallback by
   the ordinary land `stepDist` scale (9), not by raw geometric 1;
2. the fallback is comparable to a retained-land-destination case with identical geometry; and
3. unreachable/blocked path geometry is not converted into a deceptively tiny finite access value
   merely because the plots share a `CvArea`.

OTHER QUEUE-025 CONTROL — REMOTE DOUBLE-MOVE SEARCH PRUNING
-----------------------------------------------------------
A separate code-shape candidate remains below promotion threshold for now. In `currAltSites`, remote
alternatives are iterated as:

    eRemoteSite = m_pPotentialSites->getRemoteSite(i);
    if (eRemoteSite == NO_PLOT_NUM || eRemoteSite == eTakenSite)
        break;

For the second half of a double move, `eTakenSite` is the first player's chosen site. If that site is
remote candidate 0 (or 1), `break` discards all later remote candidates even though only the taken
site is forbidden; `continue` would match the local comment "Don't move the second player to the
same site as the first." This can prune valid heuristic alternatives, but it is still being held as
a lower-severity search-quality candidate rather than consumed as F626 without a stronger outcome
proof.

CURRENT STONE AFTER C031-WIP452
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — COMPLETE.
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN (priority-next-WIP).
F624 / provisional KI#946 — confirmed inherited AdvC normalization-level tolerance fallthrough.
F625 / provisional KI#947 — CONFIRMED here: documented low-value-land fallback stores raw
`CvMap::plotDistance` tile units in the weighted SPI `DistanceTable` (9/12 land-step units), making
Ocean-Fish/Snow and analogous water access dramatically too cheap in space evaluation.
F626 / provisional KI#948 — UNUSED next root ID.
No repository/gameplay source modified.

Exact continuation cursor:
C031-WIP452 physical -> Queue 025 OPEN -> exclude F624/F625 -> continue DistanceTable overflow and
metric invariants / SpaceEvaluator / PotentialSites / currAltSites search completeness / iteration
step state / team assignment / NormalizationTarget closure -> dedup all survivors -> preserve general
PlotRadiusIterator wrapped-map duplicate root for Queue 167 -> freeze next physical checkpoint
regardless of whether F626 is promoted.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP452
===============================================================================

===============================================================================
C031-WIP453 — QUEUE 025 STARTINGPOSITIONITERATION.CPP / F626 CONFIRMED
===============================================================================

CONTINUATION / RECONCILIATION
-----------------------------
This checkpoint continues directly from C031-WIP452 / Queue 025
`CvGameCoreDLL/StartingPositionIteration.cpp`. F624 / provisional KI#946 remains the
confirmed inherited normalization-level fallthrough. F625 / provisional KI#947 remains the
confirmed inherited `DistanceTable` water-fallback metric mismatch. F626 / provisional KI#948
was unused at WIP452.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this tranche.

The first post-WIP452 suspicion about `startingPositionValue()` dividing by
`PlayerIter<CIV_ALIVE>::nextIndex()` after the iterator finishes was closed as a negative control:
`AgentIterator::nextIndex()` at end is the number of agents visited in the filtered sequence, not
an absolute player-slot ID. Sparse player IDs therefore do not skew that denominator.

F626 / PROVISIONAL KI#948 — CONFIRMED INHERITED ADVC SPI UNEQUAL-TEAM ROUND-ROBIN OVERFILLS COMPLETED TEAMS
------------------------------------------------------------------------------------------------------------

ROOT FILES:
- `CvGameCoreDLL/StartingPositionIteration.cpp`,
  `StartingPositionIteration::assignSitesToTeams` / `getTeamSite`;
- downstream contract in `CvGameCoreDLL/CvGame.cpp`, `CvGame::assignStartingPlots`;
- `CvGameCoreDLL/CvPlayer.cpp`, `CvPlayer::setStartingPlot`, for duplicate-start persistence.

ROOT DEFECT
-----------
`assignSitesToTeams()` distributes the already-generated SPI sites among teams by rotating through
all alive teams, normally placing two sites for one team before advancing:

    m_sitesPerTeam[eCurrTeam].push_back(*itCurrSite);
    aeAvailableSites.erase(itCurrSite);
    int const iLoopTeamSites = m_sitesPerTeam[eCurrTeam].size();
    if (GET_TEAM(eCurrTeam).getNumMembers() <= iLoopTeamSites ||
        iLoopTeamSites % 2 == 0)
    {
        iLoopTeamIndex++;
        iLoopTeamIndex %= aieTeamsBySize.size();
    }

The first condition advances away from a team once it has received all of its members' sites, but
the completed team remains in `aieTeamsBySize`. Nothing skips it when the round-robin later wraps
back around. If unassigned sites still remain, the completed team receives another site before the
same condition advances away again.

The assignment-count error depends only on team sizes, not on `teamValue` or map geometry.

MINIMAL CURRENT SUPPORTED COUNTEREXAMPLE
----------------------------------------
Two alive teams of sizes 5 and 1 (six civilization players total) are sufficient.

The current rotation is:

    Team 5: +2  -> 2/5
    Team 1: +1  -> 1/1 complete
    Team 5: +2  -> 4/5
    Team 1: +1  -> 2/1 overfilled; no sites remain

Final `m_sitesPerTeam` cardinalities are therefore 4 and 2 rather than the required 5 and 1.

This setup is not excluded by SPI's large-team safety gate. For six players and two alive teams:

    players^2 = 36
    36 * aliveTeams = 72

and the constructor bails only when `players^2 > 36 * aliveTeams`. Thus 5+1 is on the ordinary
supported SPI team-game path. Many other supported unequal arrangements fail similarly (for
example 5+2, 5+1+1, 5+2+1); this is not a single exotic partition.

CALLER CONTRACT / DEBUG FAILURE
-------------------------------
`CvGame::assignStartingPlots()` consumes the SPI result by asking for exactly one stored site for
each actual member of each team:

    apStartsPerPlayer[itMember->getID()] = spi.getTeamSite(
            itTeam->getID(), itMember.nextIndex() - 1);

`getTeamSite()` returns NULL when the requested member index is outside that team's stored vector.
For the 5+1 example, the fifth member of the size-5 team therefore receives NULL.

The caller explicitly states the intended invariant:

    // SPI should do all the work or none
    FAssert(itPlayer.nextIndex() == 1 && pNormalizationTarget == NULL);

But a completed SPI run has already executed `doIterations()`, which sets
`m_bNormalizationTargetReady = true`; `createNormalizationTarget()` therefore returns a non-NULL
target. The missing fifth site consequently violates this assertion in an FAssert-enabled build.
This is not merely a preference disagreement inside the greedy scorer; the producer returns a
shape the consumer explicitly declares invalid.

RELEASE-PATH PARTIAL-APPLICATION / DUPLICATE-START HAZARD
----------------------------------------------------------
The Release path is also behaviorally unsafe. The caller applies the SPI site permutation in alive
player order and breaks at the first NULL:

    if (apStartsPerPlayer[itPlayer->getID()] != NULL)
        itPlayer->setStartingPlot(apStartsPerPlayer[itPlayer->getID()]);
    else
    {
        bTeamAssignmentDone = false;
        ...
        break;
    }

The values stored in `m_sitesPerTeam` are player IDs only as *placeholders for their current start
sites*; `teamValue()` is free to assign any available placeholder site to any team. Consequently an
earlier player can be reassigned to the original start of a later player. If the underfilled team's
NULL member is then encountered before that later player is processed, the loop breaks and the
later player keeps the same original start.

A concrete legal placeholder shape for 5+1 illustrates the leak:

    large-team stored placeholders: [5, 0, 1, 2]   (only four entries)
    small-team stored placeholders: [3, 4]          (two entries)

If players 0..4 are the large team and player 5 is the solo team, the caller can perform:

    player 0 -> original site of player 5
    player 1 -> original site of player 0
    player 2 -> original site of player 1
    player 3 -> original site of player 2
    player 4 -> NULL => break

Player 5 is never reached and still points at its original site. Players 0 and 5 now both point at
that same plot.

`CvPlayer::setStartingPlot()` only decrements/increments area starting-plot counters and assigns the
player's `m_pStartingPlot`; it does not evict or rewrite another player whose starting-plot pointer
already names `pNewValue`. Duplicate starts are therefore allowed to persist through this partial
application.

The subsequent old BtS fallback does not restore the omitted unique-site permutation:

- its preliminary loop only calls `findStartingPlot()` for players whose start pointer is NULL;
  the partially applied players all still have non-NULL pointers;
- `rearrangeTeamStarts(false, 0)` only swaps the existing player->start assignments, so it preserves
  the multiset of currently referenced plots and cannot recreate a unique start that was lost when
  the partial permutation broke;
- its distance precomputation / team-closeness logic assumes ordinary distinct starts and can itself
  encounter zero distances if duplicate starts survived;
- `applyStartingLocHandicaps()` explicitly returns early for unequal team sizes, so that later phase
  does not rebuild the assignment either.

Thus the defect ranges from a guaranteed Debug assertion / loss of the SPI team solution to a
Release-path duplicate-start state for reachable site-placeholder permutations. The duplicate
manifestation does not require any illegal team configuration or water start; only ordinary unequal
team sizes plus a site ordering that assigns a later player's placeholder to an earlier player.

HISTORY / INHERITANCE
---------------------
AdvC practical 2708 (`9a623ad895c73c2ce0fe4d5f5cf668ab04848307`, 2020-12-12), titled
"Let SPI handle team games, plus other changes to starting sites", introduced together:

- `m_sitesPerTeam`;
- `assignSitesToTeams()` with the current round-robin;
- `getTeamSite()`;
- the `CvGame::assignStartingPlots()` consumer and its explicit
  "SPI should do all the work or none" assertion.

The full commit message describes the new code as a greedy algorithm for assigning starting sites in
team games and says SPI is deliberately skipped only for team games with sufficiently high player
count / average team size because the greedy algorithm handles those poorly. The 5+1 counterexample
passes that very gate, so it is within the feature's intended active domain rather than a bypassed
large-team case.

Focused reachable-history search finds `assignSitesToTeams` only in the original team-support work,
a later merge carrying the same implementation, a broad refactor/header touch and SAS formatting
churn. No semantic completed-team skip was subsequently added.

Current Base AdvC 1.14 retains the same rotation and the same `CvGame` all-or-none consumer
unchanged. K-Mod and Civ4CE do not contain this AdvC-created SPI team assignment subsystem.
Classification: inherited AdvC defect, not a SAS regression.

DEDUP
-----
Focused album / Known-Issues searches found no prior owner for SPI overfilling a completed team,
`m_sitesPerTeam` cardinality mismatch, or the resulting partial-permutation/duplicate-start hazard.

Keep separate from earlier starting-position findings:

- F017 / KI#338: sorted-site volatility safeguard evaluates the human's old site;
- F018 / KI#339: StartingLoc handicap tie random offset is computed but unused;
- F019 / KI#340: the *fallback* BtS team-start rotation mixes compact alive-team count with absolute
  team IDs (now repaired in current SAS);
- F624 / KI#946: SPI normalization-level tolerance switch fallthrough;
- F625 / KI#947: SPI `DistanceTable` fallback metric/unit mismatch.

F019 concerns random priority *inside the legacy fallback*. F626 is an earlier producer/consumer
cardinality failure that can incorrectly force that fallback and can partially corrupt the start
permutation before fallback begins. They are independent roots.

REPAIR BOUNDARY
---------------
Preserve the intended "two members at a time" greedy rotation, but never select a team that has
already received `getNumMembers()` sites.

A narrow repair can advance past completed teams before assigning the next site (or remove completed
teams from the active rotation). Because the total number of available placeholder sites equals the
total number of alive civilization players/team members, successful completion should establish:

    m_sitesPerTeam[eTeam].size() == GET_TEAM(eTeam).getNumMembers()

for every alive civilization team.

Add a post-assignment assertion for that exact invariant before exposing `m_sitesPerTeam` to
`CvGame`. The consumer's existing all-or-none assertion is too late because it detects the mismatch
only while already applying the permutation.

Regression coverage should include at least:

1. 5+1 teams: expected stored counts 5+1, never 4+2;
2. 5+2 teams: expected 5+2, never 4+3;
3. 5+1+1 and 5+2+1 mixed teams;
4. a placeholder ordering where the large team selects the original site of a later player, proving
   that the fixed producer lets the caller apply a complete permutation with no duplicate starts;
5. existing equal-size 2+2 / 3+3 cases to ensure the two-at-a-time clustering behavior is retained;
6. the existing large-team SPI bail condition, which should remain unchanged unless deliberately
   redesigned separately.

OTHER QUEUE-025 CONTROLS CARRIED FORWARD
----------------------------------------
- `currAltSites` still uses `break` when a strict remote candidate equals `eTakenSite`, pruning later
  remote alternatives for the second half of some double moves. This remains below finding threshold
  pending a stronger gameplay/search-completeness proof; do not conflate it with F626.
- General wrapped-map plot duplication from `PlotRadiusIterator` remains reserved for Queue 167 as
  the likely iterator-root owner rather than duplicated into SPI.
- The `startingPositionValue()` `nextIndex()` denominator suspicion is closed: end-of-iterator
  `nextIndex()` is the filtered visited count.

CURRENT STONE AFTER C031-WIP453
-------------------------------
Queue 022 `CvGameCoreDLL/TrueStarts.cpp` — COMPLETE.
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN (priority-next-WIP).
F624 / provisional KI#946 — confirmed inherited AdvC normalization-level tolerance fallthrough.
F625 / provisional KI#947 — confirmed inherited AdvC `DistanceTable` fallback metric mismatch.
F626 / provisional KI#948 — CONFIRMED here: SPI's round-robin revisits already-completed teams in
unequal team games, producing wrong `m_sitesPerTeam` cardinalities, violating the all-or-none caller
contract and permitting partial-permutation duplicate starting plots.
F627 / provisional KI#949 — UNUSED next root ID.
No repository/gameplay source modified.

Exact continuation cursor:
C031-WIP453 physical -> Queue 025 OPEN -> exclude F624/F625/F626 -> continue teamValue / team
assignment invariants, SpaceEvaluator / PotentialSites / currAltSites search completeness,
DistanceTable overflow/metric edge cases, iteration Step state and NormalizationTarget closure ->
dedup all survivors -> preserve general PlotRadiusIterator wrapped-map duplicate root for Queue 167
-> freeze next physical checkpoint regardless of whether F627 is promoted.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP453
===============================================================================


===============================================================================
C031-WIP454 — QUEUE 025 DISTANCETABLE STALE DIJKSTRA ENTRIES OVERWRITE SHORTEST PATHS
===============================================================================

PHYSICAL CONTINUATION
---------------------
This checkpoint continues directly from C031-WIP453 / Queue 025
`CvGameCoreDLL/StartingPositionIteration.cpp`. F624 / provisional KI#946 remains the confirmed
normalization-level fallthrough, F625 / provisional KI#947 remains the confirmed water-fallback
metric mismatch, and F626 / provisional KI#948 remains the confirmed unequal-team round-robin /
partial-permutation defect. F627 / provisional KI#949 was unused at that stone.

No repository/gameplay source was modified. Investigation used the current reconciled SAS snapshot,
its local reachable-history mirror, Base AdvC 1.14 and focused whole-album / current-Known-Issues
dedup searches.

F627 / PROVISIONAL KI#949 — CONFIRMED INHERITED ADVC STALE DIJKSTRA QUEUE ENTRIES OVERWRITE SETTLED SHORTEST DISTANCES
----------------------------------------------------------------------------------------------------------------------
SEVERITY / CLASS: current-live central starting-position metric defect; inherited AdvC shortest-path
bookkeeping/order bug.
ROOT FILE: `CvGameCoreDLL/StartingPositionIteration.cpp`,
`StartingPositionIteration::DistanceTable::computeDistances`.

CURRENT CODE / ALGORITHM CONTRACT
---------------------------------
`DistanceTable::computeDistances` explicitly describes itself as:

    // Plain old Dijkstra's algorithm

It uses an inverse-ordered `std::priority_queue<Node>` so that the smallest queued distance is popped
first:

    bool operator<(Node const& kOther) const
    {
        return m_iDistance > kOther.m_iDistance;
    }

and keeps a reached/settled map because the same physical plot can occur in the queue more than once.
The source comment says this directly:

    /* Note: even if not yet reached, it may already be in q;
       that's what the abReached[...] check above is for. */

The current loop, however, writes the popped distance into the public `DistanceTable` BEFORE testing
whether that queue entry is stale:

    Node v = q.top();
    q.pop();
    CvPlot const& kAt = v.get();
    setDistance(kSource, kAt, v.getDistance());
    {
        PlotNumTypes eAt = kAt.plotNum();
        if (abReached.get(eAt))
            continue;
        abReached.set(eAt, true);
    }

The first pop of a reachable plot is the shortest path under Dijkstra's positive edge weights and is
correctly marked reached. But any longer copy that had already been queued later reaches the top,
executes `setDistance` FIRST, overwrites the correct settled value, and only then notices
`abReached[eAt]` and discards itself.

`setDistance` is a plain assignment for destinations:

    m_distances[eSource][eDestination] = iDistance;

There is no minimum check that could preserve the earlier shortest value.

MINIMAL CURRENT-LIVE GRID COUNTEREXAMPLE
----------------------------------------
Take a normal passable land corner/patch with source S, two cardinal neighbors A and B, and diagonal
destination D:

    B -- D
    |  / |
    S -- A

For ordinary land, `stepDist` is 9 cardinal and 12 diagonal:

    if (!kTo.isWater())
        return (bDiagonal ? 12 : 9);

Queue evolution is therefore:

1. Start with `S(0)`.
2. Pop S. Queue direct `A(9)`, `B(9)` and diagonal `D(12)`.
3. Pop A(9) before D(12). Because D is not reached yet, A queues an alternate `D(18)`.
4. Pop B(9) before D(12). B can likewise queue another `D(18)`.
5. Pop the direct D(12). Current code stores 12 and marks D reached. This is the correct shortest
   path.
6. Later pop stale D(18). Current code stores 18 first, THEN sees that D is reached and continues.

The final table therefore contains 18, not 12. This does not require a rare obstacle, water frontier,
wrapping map or special game option. It arises from an ordinary diagonal destination with a second
cardinal route. Larger open neighborhoods can have additional stale alternatives; the exact final
overwrite can depend on which longer copies had entered the queue, but it cannot be assumed to remain
the first settled shortest value.

The reproducer is current-live in SPI. During construction, all candidate/current starting sites are
explicitly forced into `relevantPlots` via `sitePlotNums`, and any other workable plot with positive
SPI yield value is also a destination:

    if (yieldValues.get(eLoopPlotNum) > 0 ||
        sitePlotNums.count(eLoopPlotNum) > 0)
    {
        relevantPlots.push_back(...);
    }

So D can simply be another starting/candidate site; no yield/XML edge case is needed.

DOWNSTREAM GAMEPLAY CONSEQUENCE
-------------------------------
The corrupted table is a central SPI input rather than temporary debug state.

`SpaceEvaluator` reads `DistanceTable::d` for every retained yield destination when building claim
competition:

    short iDist = kDists.d(kStartPlot, kLoopPlot) - iDistSubtr;
    ...
    rSum += claim_t(1, std::max<short>(1, iDist));

and later directly reads the stored source/destination entry again to derive each player's claim and
access/delay factor before adding yield to expansion-space value.

More importantly, original/current starting sites are destinations too, and
`computeRivalDistFactors` uses these same corrupted values as the path distances between starting
sites:

    aiRivalDists.push_back(m_pPathDists->d(
        kStartPlot, *itRival->getStartingPlot()));

Those factors feed ordinary per-player start-value scoring. Consequently F627 can alter both
expansion-space ownership/access estimates and the rival-spacing component used to decide which
starting-position solution SPI prefers.

The bug generally biases a destination toward whichever stale longer queue copy is written last,
not toward its shortest path. It is therefore not a simple uniform rescale that would cancel in
relative comparisons.

HISTORY / INHERITANCE
---------------------
AdvC practical 2280 (`a70e4ce3a1db01c51da7fa370a706d8784191b38`,
"Algorithm for fairer starting positions (wip)", 2020-05-28) CREATED
`StartingPositionIteration.cpp` and introduced `DistanceTable::computeDistances` with this exact
ordering:

    setDistance(kSource, kAt, v.getDistance());
    if (abReached[iAt])
        continue;
    abReached[iAt] = true;

The introduction already contained the explicit duplicate-queue comment, so duplicate entries were
part of the intended algorithm from the start; only the placement of the table write is wrong.

AdvC practical 2302 changed distance/frontier semantics but retained the ordering. Practical 3134
refactored the reached state to `EnumMap`, again preserving `setDistance` before the guard. The later
3392 merge likewise preserves it. SAS practical 5961 is signature/formatting cleanup only and does
not semantically touch this loop.

Current Base AdvC 1.14 still has the same `setDistance`-before-`abReached` sequence. K-Mod and Civ4CE
do not contain the AdvC-created StartingPositionIteration subsystem. Classification is therefore an
inherited AdvC defect, not a SAS/BtS/K-Mod/Civ4CE regression.

DEDUP
-----
Focused whole-album and current `README_Known_Issues.md` searches for `computeDistances`, Dijkstra,
priority-queue, reached/settled and stale-distance wording found no prior owner for this SPI shortest-
path overwrite.

The nearby Queue-025 findings are independent:

- F624 / KI#946: `NormalizationTarget::isReached` switch fallthrough changes normalization-level
  tolerance ordering.
- F625 / KI#947: the special low-value-land water fallback writes raw `CvMap::plotDistance` units
  into the weighted `DistanceTable` metric. F625 remains real after F627 is repaired because its
  fallback explicitly replaces the ordinary Dijkstra result with a separate raw geometric value.
- F626 / KI#948: unequal-team round-robin overfills completed teams and can cause partial starting-
  plot permutation/duplicate starts.
- F169 / KI#492: discarded full city-site evaluations are a performance issue, not path correctness.

No semantic duplicate was found.

REPAIR BOUNDARY
---------------
The narrow semantic repair is to discard already-settled queue entries BEFORE publishing their
queue distance:

    CvPlot const& kAt = v.get();
    PlotNumTypes eAt = kAt.plotNum();
    if (abReached.get(eAt))
        continue;
    abReached.set(eAt, true);
    setDistance(kSource, kAt, v.getDistance());

With the current nonnegative/positive `stepDist` costs, the first popped entry for a plot is the
Dijkstra shortest path. Writing only that settled entry restores the intended contract without
changing path weights, frontier heuristics, destination selection or `SpaceEvaluator` formulas.

An alternative defensive `min(old,new)` write could mask the symptom for destinations, but moving
the write behind the reached guard is the clearer algorithmic fix and avoids unnecessary stale
writes entirely.

Regression coverage should include at least:

1. ordinary land S -> diagonal D returns 12 even after cardinal alternate D(18) entries are queued;
2. several small obstacle/open-grid cases compared against a reference shortest-path calculation;
3. inter-start distances used by `computeRivalDistFactors`, not just yield destinations;
4. water/frontier routes to ensure only stale-entry publication changes, not the intended 4/5/9/12
   and frontier edge costs; and
5. F625's Ocean-Fish/omitted-land fallback separately, because fixing F627 must not be mistaken for
   fixing that independent metric mismatch.

POST-F627 CONTROLS / CONTINUATION
--------------------------------
The following candidates remain intentionally unpromoted:

- `DistanceTable` signed-short overflow on pathological long/thin custom maps remains generic
  mod-data robustness until a supported shipped-map path is established.
- `currAltSites` uses `break` when a remote alternative equals the first move's taken site; this can
  prune later remote alternatives but remains a lower-severity search-completeness concern pending a
  stronger outcome proof.
- `SpaceEvaluator`'s static delay-factor cache is safe under the current contract because
  `DistanceTable::getLongDist()` is constant (`8 * 40`), making the cache threshold invariant.
- `startingPositionValue`'s end-of-iterator `nextIndex()` divisor is safe because `AgentIterator`
  defines end `nextIndex()` as the filtered iteration count, not the absolute player ID.
- General wrapped-map `PlotRadiusIterator` duplicate visits remain reserved for Queue 167 rather than
  being consumed as a Queue-025 root.

CURRENT LEDGER / RESUME STONE
-----------------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN (priority-next-WIP).
F624 / provisional KI#946 — confirmed inherited AdvC normalization-level tolerance fallthrough.
F625 / provisional KI#947 — confirmed inherited AdvC water-fallback distance metric mismatch.
F626 / provisional KI#948 — confirmed inherited AdvC unequal-team round-robin/cardinality defect.
F627 / provisional KI#949 — CONFIRMED here: stale duplicate Dijkstra queue entries overwrite already-
settled shortest distances because `setDistance` precedes the reached guard.
F628 / provisional KI#950 — UNUSED next root ID.

Exact continuation:
C031-WIP454 physical -> Queue 025 OPEN -> exclude F624/F625/F626/F627 -> continue `SpaceEvaluator`,
solution/double-move and normalization-target producer/consumer invariants -> revisit remote-site
`break` only if a concrete gameplay/search failure survives -> history/dedup every survivor -> freeze
next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP454
===============================================================================


===============================================================================
C031-WIP455 — QUEUE 025 SPI SCENARIO OWNERSHIP + POST-F627 CLOSURE PASS
===============================================================================

PHYSICAL CONTINUATION
---------------------
This checkpoint continues directly from C031-WIP454. Queue 025
`CvGameCoreDLL/StartingPositionIteration.cpp` remains OPEN. F624-F627 remain confirmed and
excluded from rediscovery. F628 / provisional KI#950 was unused at the start of this tranche.

The current SAS snapshot, its local reachable-history mirror, Base AdvC 1.14 and the local Civ4CE
reference scenario files were used. No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.

POST-F627 NEGATIVE CONTROLS / CLOSED LEADS
------------------------------------------
Several remaining SPI producer/consumer and lifecycle suspicions were reduced before spending a new
finding number:

1. `CvPlot::isPotentialCityWork` does not admit geometrically unreachable water yield into SPI's
   expansion-space table. `updatePotentialCityWork()` only marks a tile if at least one plot in its
   city radius can `canEverFound()`, and `CitySiteEvaluator::evaluateWorkablePlot` again searches
   city-radius plots that the representative player can found. The isolated-water-resource lead is
   therefore closed.

2. The narrower WorldBuilder `RandomStartLocation` lead is also below finding threshold. Practical
   2708's contract is to skip the direct assignment of a pre-marked `StartingPlot` for such a player
   and run the randomized `findStartingPlot` path instead; it does not promise that the randomized
   search can never coincidentally choose a marked tile. Current SPI preserves the player's random-WB
   flag when it calls `findStartingPlot`.

3. `CvArea::getNumStartingPlots()` stays synchronized with SPI's speculative and committed moves.
   `CvPlayer::setStartingPlot` decrements the old area's starting-plot count and increments the new
   area's count, so `teamValue` is not reading a stale area cardinality merely because SPI has moved
   sites.

4. The broader scenario/fixed-start team-permutation suspicion remains closed. The AdvC practical
   2708 contract is preservation of the physical set of scenario `StartingPlot` tiles, not permanent
   ownership of each marked tile by the player temporarily assigned there. Team permutation keeps the
   physical set.

5. `m_rMedianLandYieldVal` is an unguarded divisor downstream, but no bundled map-script path was
   established where at least half of potential-work land evaluates to exactly zero. Keep as
   custom-map/mod-data robustness only.

6. `currAltSites` still has a real search-pruning oddity: when adding the top strict-remote sites it
   does `break` rather than `continue` if one entry equals the first move's `eTakenSite`, so later
   strict-remote candidates are discarded for a second move. History shows this shape is inherited.
   It remains below numbered-finding threshold because the demonstrated consequence is heuristic
   search incompleteness, not a hard state/correctness failure.

7. `teamValue`'s nested closeness loop repeats a team's full assigned-site vector once per member of
   that team already in the candidate area. Uniform repetition does not change friendly min/mean; with
   several rival teams it implicitly weights rivals by same-area presence. The shape is original to
   AdvC team support, and no contrary contract was found. Preserve as opaque heuristic, not a bug.

8. The exact `>= getLongDist()` versus `> getLongDist()` remote-step boundary remains a tiny search
   inconsistency only.

9. `SpaceEvaluator`'s process-static delay cache is safe under the current contract because
   `DistanceTable::getLongDist()` is invariant at `8 * 40`, hence the cached threshold is always 400.

10. `startingPositionValue()` dividing by end-of-iteration `it.nextIndex()` is safe: the iterator
    contract makes that value the filtered sequence length at end, not the absolute player slot ID.

11. Several pointer members (`m_pYieldValues`, `m_pYieldsPerArea`, `m_pPotentialSites`) become dangling
    when constructor-local precomputation objects leave scope, but all public post-construction paths
    avoid them. This is latent source debt rather than a live use-after-scope.

12. The constructor's early "Revert any changes and bail" branch has a genuine scenario-robustness
    flaw: on a partially fixed scenario, if `findStartingPlot` fails for a missing player, it clears
    *all* alive starting plots, including pre-existing marked scenario starts, instead of only sites
    generated by SPI. The caller does not reload those authored assignments. A concrete supported
    custom-WB failure can therefore destroy fixed state. However, no bundled scenario was established
    to make `findStartingPlot` return NULL in this branch, so preserve this as scenario robustness/source
    debt rather than consuming F628.

F628 / PROVISIONAL KI#950 — CONFIRMED INHERITED ADVC SCENARIO NORMALIZATION-TARGET LEAK
---------------------------------------------------------------------------------------
SEVERITY / CLASS: current-live one-time scenario memory/resource leak; inherited AdvC ownership bug.
ROOT FILES:
- `CvGameCoreDLL/StartingPositionIteration.cpp` (`doIterations`, `createNormalizationTarget`)
- `CvGameCoreDLL/CvGame.cpp` (`assignStartingPlots`, `setInitialItems`)

ROOT / OWNERSHIP CONTRACT
-------------------------
`StartingPositionIteration::createNormalizationTarget()` explicitly allocates a target for the caller:

    // To be (safe-)deleted by caller!
    NormalizationTarget* createNormalizationTarget() const;

and the implementation performs a heap allocation:

    if (!m_bNormalizationTargetReady)
        return NULL;
    NormalizationTarget& kResult = *new NormalizationTarget(
            *createSiteEvaluator(true), m_currSolutionAttribs);
    return &kResult;

The target itself owns another heap allocation: `NormalizationTarget::~NormalizationTarget()` deletes
its `m_pEval` `CitySiteEvaluator`.

The ordinary top-level owner is `CvGame::setInitialItems()`:

    NormalizationTarget* pNormalizationTarget = assignStartingPlots();
    normalizeStartingPlots(pNormalizationTarget);
    SAFE_DELETE(pNormalizationTarget);

So `assignStartingPlots()` must either return the allocated target to this owner or delete it itself.

SCENARIO PATH LOSES THE ONLY POINTER
------------------------------------
Current `CvGame::assignStartingPlots()` constructs SPI and obtains the target:

    StartingPositionIteration spi;
    pNormalizationTarget = spi.createNormalizationTarget();

It deliberately uses that pointer before normalization when sorting/reassigning starts by handicap:

    applyStartingLocHandicaps(pNormalizationTarget);

but then returns:

    return (bScenario ? NULL : pNormalizationTarget);

Therefore, whenever a scenario SPI run has produced a non-NULL target:

1. `new NormalizationTarget(...)` allocates the target;
2. its constructor owns a newly allocated normalization `CitySiteEvaluator`;
3. `applyStartingLocHandicaps` reads the target but does not take ownership or delete it;
4. the scenario return expression discards the sole `pNormalizationTarget` pointer and returns NULL;
5. `setInitialItems()` receives NULL, so its `SAFE_DELETE` cannot free either allocation.

This is a true leak, not delayed ownership or an object retained for later scenario normalization.
The scenario path intentionally returns NULL because scenarios are not supposed to receive the later
normalization-target phase; the allocation nevertheless already happened and has no remaining owner.

WHY A SCENARIO TARGET CAN NOW EXIST
-----------------------------------
The history removes any ambiguity about whether scenario targets are supposed to remain NULL.

Before AdvC team/scenario starting-site work, practical 2685 explicitly made readiness conditional:

    m_bNormalizationTargetReady = !m_bScenario;

so `createNormalizationTarget()` returned NULL in scenarios and there was no target to leak.

AdvC practical 2708 (`9a623ad895`, "Let SPI handle team games, plus other changes to starting sites")
changed that exact line to:

    m_bNormalizationTargetReady = true;

while the same practical moved SPI/target creation into the shared scenario-capable
`CvGame::assignStartingPlots()` flow and added the final:

    return (bScenario ? NULL : pNormalizationTarget);

The reason for making the target available in scenarios is visible in the new caller ordering:
`applyStartingLocHandicaps(pNormalizationTarget)` can use SPI's richer physical-site values before the
function suppresses the target from the later normalization phase. What practical 2708 omitted is the
corresponding ownership cleanup after that final scenario use.

CURRENT-LIVE / BUNDLED-CONTENT ACTIVATION
-----------------------------------------
This is not limited to malformed XML or an external mod-mod. AdvC/SAS explicitly support legacy WB
scenario maps and even special-case them in current `TrueStarts.cpp`; `Africa.Civ4WorldBuilderSave`
is named there directly for latitude handling.

The local Civ4CE/base-game reference copy of that supported map has:

- grid 54 x 66 = 3,564 plots, well below current `SPI_PLOT_LIMIT = 12,000`;
- 18 player slots with `LeaderType=NONE` / `CivType=NONE` and distinct one-player teams;
- 9 physical `StartingPlot` markers.

That is exactly the partial-start scenario class practical 2708 describes in the SPI constructor:

    "Respect the per-plot StartingPlot flags in scenarios.
     But there can be fewer such plots than players. We'll
     deal with any players that don't have a starting site yet."

When such a scenario is started with more marked-site-eligible players than marked plots and SPI
successfully completes the missing starts, `doIterations()` marks the normalization target ready and
`createNormalizationTarget()` allocates it. The leak then follows deterministically from the scenario
return expression above. The allocation is only once per game/scenario initialization, so this is not
a runaway per-turn leak, but it is current live and unnecessary.

The supported-content link is especially strong because current Base AdvC 1.14 and current SAS both
explicitly recognize the filename `Africa.Civ4WorldBuilderSave` in `TrueStarts.cpp`; this is not an
unknown third-party scenario format inferred only from Civ4CE data.

HISTORY / INHERITANCE
---------------------
The ownership mismatch is introduced by the AdvC practical-2708 scenario/team expansion:

- practical 2685: scenario runs explicitly leave `m_bNormalizationTargetReady` false;
- practical 2708: readiness becomes unconditional true, the target is consumed by the new shared
  handicap-reassignment flow, and `assignStartingPlots` simultaneously suppresses the pointer on its
  scenario return without deleting it.

Current Base AdvC 1.14 retains both:

    m_bNormalizationTargetReady = true;

and:

    return (bScenario ? NULL : pNormalizationTarget);

unchanged. Current SAS inherits the same ownership behavior. K-Mod/Civ4CE do not contain the AdvC-created
SPI `NormalizationTarget` subsystem in this form. Classification is therefore inherited AdvC, not a SAS
regression.

DEDUP
-----
Focused whole-album and current Known-Issues searches for scenario `NormalizationTarget` ownership,
scenario target leaks, `createNormalizationTarget`, and the conditional scenario return found no prior
owner. F624 concerns the target's normalization-level tolerance fallthrough, not target lifetime.
F626 concerns team-site cardinality/partial permutation. The constructor rollback robustness note above
is also independent: F628 requires a *successful* scenario SPI run that creates a target, not the early
failure path.

REPAIR BOUNDARY
---------------
The narrow repair is to preserve the scenario target through its last intended consumer, then delete it
instead of discarding the pointer. For example, after `applyStartingLocHandicaps`:

    if (bScenario)
    {
        SAFE_DELETE(pNormalizationTarget);
        return NULL;
    }
    return pNormalizationTarget;

or equivalent ownership-safe restructuring.

Do not simply restore `m_bNormalizationTargetReady = !m_bScenario`: practical 2708 deliberately made
scenario targets available so `applyStartingLocHandicaps` can use SPI's start values. The repair should
retain that behavior and fix only ownership after the last scenario use.

Regression coverage should include:

1. ordinary non-scenario game: target still returned to `setInitialItems` and deleted after normalizing;
2. all-starts-fixed scenario: target stays NULL as before;
3. partial-start scenario where SPI completes: target is usable by `applyStartingLocHandicaps`, then
   destroyed exactly once before returning NULL;
4. debug/SPI-log branch: preserve its explicit early return semantics if that mode intentionally hands
   ownership to the outer caller.

POST-F628 CURSOR
----------------
Queue 025 remains OPEN. F629 / provisional KI#951 is UNUSED.

The next pass should continue the remaining remote-site ranking/search semantics, then do a final
function-by-function reconciliation against F624-F628 and prior album owners. Do not promote the
`m_remoteSitesByAreaSize` weighting merely from its name: current ranking uses
`areaTiles * 100 + foundValue`, and history must prove whether area size was intended as a strict
primary key or merely one weighted component. Likewise keep the partial-scenario rollback as robustness
unless bundled/current activation is established.

CURRENT LEDGER / RESUME STONE
-----------------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN.
F624 / provisional KI#946 — confirmed inherited AdvC normalization-level tolerance fallthrough.
F625 / provisional KI#947 — confirmed inherited AdvC water-fallback distance metric mismatch.
F626 / provisional KI#948 — confirmed inherited AdvC unequal-team round-robin/cardinality defect.
F627 / provisional KI#949 — confirmed inherited AdvC stale-Dijkstra overwrite defect.
F628 / provisional KI#950 — CONFIRMED here: successful scenario SPI target is consumed for handicap
reassignment, then the only pointer is discarded by `return (bScenario ? NULL : pNormalizationTarget)`,
leaking the `NormalizationTarget` and its owned `CitySiteEvaluator`.
F629 / provisional KI#951 — UNUSED next root ID.

Exact continuation:
C031-WIP455 physical -> Queue 025 OPEN -> exclude F624-F628 -> finish remote-site ranking/search and
remaining lifecycle/normalization invariants -> function-by-function reconciliation -> close Queue 025
if no independent current-live root survives -> freeze next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP455
===============================================================================

===============================================================================
C031-WIP456 — QUEUE 025 CONTINUATION / F629 CONFIRMED
===============================================================================
Physical parent: C031-WIP455
Audit target: `CvGameCoreDLL/StartingPositionIteration.cpp`
Gameplay source status: UNTOUCHED.

POST-WIP455 NEGATIVE CONTROLS / CLOSURE
----------------------------------------
1. The `SpaceEvaluator` near-start exclusion

       if (kMap.plotDistance(&kStartPlot, &kLoopPlot) <= 2)
           // Accounted for by found values

   does NOT accidentally remove the four 5x5 outer corners that are outside a Civ4 21-tile city
   radius. Current `CvMap::plotDistance` uses the AdvC octile approximation; at delta (2,2) it returns
   3, not 2. Thus the four corners survive this exclusion. This candidate is CLOSED.

2. The `isPotentialCityWork` water-reachability concern remains CLOSED: the cached predicate requires
   a geometrically legal city site in the tile's workable radius, so isolated water yield with no
   possible land city does not enter the SPI expansion-space destination set through that predicate.

3. The narrower WorldBuilder `RandomStartLocation` concern remains below finding threshold. The
   practical-2708 contract is to avoid directly assigning a pre-marked scenario site to that player
   and instead run the randomized starting-plot finder; it does not promise that the randomized path
   can never coincidentally select a marked plot.

F629 / PROVISIONAL KI#951 — CONFIRMED INHERITED ADVC TEAM-SITE FALLBACK MISCLASSIFIES COMPLETED RIVAL SITES AS "UNASSIGNED"
------------------------------------------------------------------------------------------------------------------------
SEVERITY / CLASS: current-live team-start placement defect; deterministic greedy-ranking corruption;
inherited AdvC practical-2708 implementation/contract mismatch.
ROOT FILE: `CvGameCoreDLL/StartingPositionIteration.cpp`, `StartingPositionIteration::teamValue`.

LOCAL CONTRACT AND CONTRADICTION
--------------------------------
When `teamValue` evaluates the first site of a team in an area, it first builds rival-closeness data.
The code explicitly excludes rival teams that have already received all their members:

    if (!bFriendly &&
        /* Ignore closeness to sites of rivals that have already
           placed all there members */
        m_sitesPerTeam[i].size() == GET_TEAM(eLoopTeam).getNumMembers())
    {
        continue;
    }

That exclusion is semantically important: a completed rival cannot later add another teammate near the
candidate, so it should not influence the "leave room for possible future rival members" term.

If there are no friendly sites and no non-completed rival samples, the immediately following fallback
states a second, equally explicit contract:

    /* When placing the first site of a team in an area,
       try to keep a distance from rival sites in case that
       those rivals will add more members to the area.
       When there are no rival sites either, try to maximize
       the distance from the unassigned sites, if any. */

But the implementation does not iterate the unassigned-site set used by `assignSitesToTeams`. It loops
through every alive player's physical starting site:

    for (PlayerIter<CIV_ALIVE> itOther; itOther.hasNext(); ++itOther)
    {
        if (itOther->getID() != eSitePlayer)
        {
            short iCloseness = m_pPathDists->getLongDist() -
                    m_pPathDists->d(kSite, *itOther->getStartingPlot());
            if (iCloseness > 0)
                arUnassignedCloseness.push_back(iCloseness);
        }
    }

Player IDs are merely placeholders for physical sites in this phase. Players/sites already present in
`m_sitesPerTeam` are ASSIGNED, yet this fallback adds them to `arUnassignedCloseness` anyway. In the
specific state that activates the fallback, this means sites of completed rivals that the preceding
loop deliberately excluded can be immediately reintroduced under the opposite label "unassigned."

CURRENT-LIVE SUPPORTED 2+1+1+1 REPRODUCER
-----------------------------------------
A 5-player game with team sizes 2+1+1+1 stays inside SPI's team-game support gate:

    SQR(5) = 25 <= 36 * 4 = 144.

It also avoids F626's unequal-team round-robin overfill root. `assignSitesToTeams` gives the size-2 team
its first two sites, advances after that team is complete, then each singleton receives one site. The
loop never wraps back to the completed size-2 team before the five sites are exhausted.

Now put all five candidate starts on one ordinary, sufficiently large, non-wrapping-equivalent landmass
so the normal SPI weighted land metric is 9 per cardinal step and 12 per diagonal step. One concrete
layout is:

    completed rival A1 = (0, 0)
    completed rival A2 = (0,10)
    remaining candidate X = (0,20)
    remaining candidate Y = (0,30)
    remaining candidate Z = (5,45)

When the next singleton team is choosing its first site, A1/A2 belong to a rival team that has already
placed all two members. The explicit completed-rival branch therefore excludes A1/A2, leaving
`arRivalCloseness` empty and activating the alleged "unassigned sites" fallback.

With `DistanceTable::getLongDist() = 320`, relevant shortest weighted land distances are:

    A1-A2 =  90
    A1-X  = 180        A2-X =  90
    A1-Y  = 270        A2-Y = 180
    A1-Z  = 420        A2-Z = 330
    X-Y   =  90
    X-Z   = 240
    Y-Z   = 150

and closeness is `max(0, 320 - distance)`.

INTENDED FALLBACK — ONLY THE OTHER UNASSIGNED SITES
----------------------------------------------------
For X, compare only Y/Z: closeness {230,80}; vicinity penalty about -118.
For Y, compare only X/Z: closeness {230,170}; vicinity penalty -185.
For Z, compare only X/Y: closeness {80,170}; vicinity penalty about -103.

So the intended "maximize distance from the unassigned sites" rule chooses Z.

CURRENT FALLBACK — COMPLETED A1/A2 ARE ADDED BACK
--------------------------------------------------
For X, the fallback samples A1/A2/Y/Z: {140,230,230,80}; penalty about -125.
For Y, it samples A1/A2/X/Z: {50,140,230,170}; penalty about -99.
For Z, A1/A2 are beyond `getLongDist` and contribute no positive closeness, leaving {80,170}; penalty
about -103.

The current code therefore chooses Y instead of Z. This is a deterministic greedy ranking inversion
caused solely by sites that the function has already classified as completed-rival/assigned being fed
back into the vector documented as "unassigned."

All sites are in the same area, so the dominant `iRemainingSiteWeight * iRemainingAreaSites` and the
`iAreaSites` tiebreaker are identical for X/Y/Z. At this point five area sites exist, two have been
assigned and three remain, so the `iRemainingAreaSites == 1` final-site balancing branch is inactive.
The vicinity term above therefore decides this comparison directly.

The exact half-value rounding convention for the approximate -118/-103 displays is irrelevant to the
inversion: intended Z remains better than X/Y by a wide margin, while current Y is better than Z.

WHY THIS IS DISTINCT FROM HEURISTIC DISAGREEMENT
------------------------------------------------
This finding does not claim that AdvC should use a different notion of team clustering. Both intended
rules are already stated by the source itself:

1. completed rival teams are to be ignored by this closeness term;
2. when no relevant rival sample remains, only unassigned sites are to be considered.

The bug is that the implementation of rule 2 does not know which placeholders remain unassigned and
therefore violates both local contracts at once. Moving already-completed rival sites while holding
unassigned-site geometry fixed can change the fallback result, even though the source says those rivals
are to be ignored.

HISTORY / INHERITANCE
---------------------
AdvC practical 2708 (`9a623ad895c73c2ce0fe4d5f5cf668ab04848307`, "Let SPI handle team games, plus
other changes to starting sites") introduced `assignSitesToTeams`, `teamValue`, the completed-rival
exclusion, the "unassigned sites" comment, and the broad `PlayerIter<CIV_ALIVE>` fallback together.
Thus the contradiction was present from the first team-support implementation rather than being a SAS
regression.

Later history contains no semantic correction making the fallback truly unassigned-only. Practical
2723 only fixes an assert typo around this area; later appearances are refactors/merges rather than a
contract rewrite. Current Base AdvC 1.14 retains the same completed-rival exclusion and the same broad
alive-player fallback unchanged. Current SAS likewise inherits it; the recent SAS semantic change in
this file is the separate found-value `short` -> `int` widening, plus broad formatting/refactor churn.

DEDUP
-----
Focused current Known-Issues and whole-album searches found no existing owner for this exact
completed-rival/unassigned fallback root.

- F626 is the unequal-team round-robin/cardinality defect. The 2+1+1+1 reproducer above is chosen
  specifically so F626 does not activate.
- F627 corrupts `DistanceTable` shortest distances through stale Dijkstra queue overwrites. F629
  survives with ideal/correct weighted distances as shown above.
- F624, F625 and F628 concern normalization tolerances, water fallback units, and scenario target
  ownership respectively.
- Existing older team-start findings such as sparse team-ID/BtS fallback issues do not own this
  AdvC-created `teamValue` fallback misclassification.

REPAIR BOUNDARY
---------------
The narrow repair is to make the fallback iterate only the same currently available placeholder sites
that `assignSitesToTeams` calls unassigned, excluding `eSitePlayer` itself.

A clean structural repair is to pass `aeAvailableSites` (or an equivalent const view/set of currently
unassigned placeholders) into `teamValue`, then build `arUnassignedCloseness` only from that set.
Alternatively, `teamValue` could derive an assigned-placeholder set from `m_sitesPerTeam` and skip those
IDs, but passing the authoritative available set avoids reconstructing state and makes the local comment
contract explicit.

Do NOT remove the completed-rival exclusion, change the broader team-clustering weights, or retune the
vicinity formula as part of this root fix. The defect is membership of the fallback sample, not the
chosen min/mean heuristic.

Regression coverage:
1. supported 2+1+1+1 team game after the size-2 team completes;
2. fallback sample contains exactly the other still-available placeholder sites;
3. moving completed-rival physical sites while holding available-site geometry constant cannot alter
   this fallback's ranking;
4. ordinary cases with an unfinished rival keep using `arRivalCloseness` exactly as before;
5. separately retain F626 coverage for 5+1 / unequal-team completed-team skipping.

POST-F629 CURSOR
----------------
Queue 025 remains OPEN. F630 / provisional KI#952 is UNUSED.

Continue a closure-oriented function-by-function pass over `PotentialSites`, `SpaceEvaluator`, `Step`,
normalization and remaining history strata. Preserve the low-grade `currAltSites` taken-site `break`,
the exact remote-threshold `>=`/`>` mismatch, `teamValue`'s same-area rival repetition, short-distance
pathological overflow, and wrapped-radius duplicate visitation as controls unless a stronger current-live
contract emerges. The wrapped-radius issue remains best rooted in the later PlotRadiusIterator queue.

CURRENT LEDGER / RESUME STONE
-----------------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — OPEN.
F624 / provisional KI#946 — confirmed inherited AdvC normalization-level tolerance fallthrough.
F625 / provisional KI#947 — confirmed inherited AdvC water-fallback distance metric mismatch.
F626 / provisional KI#948 — confirmed inherited AdvC unequal-team round-robin/cardinality defect.
F627 / provisional KI#949 — confirmed inherited AdvC stale-Dijkstra overwrite defect.
F628 / provisional KI#950 — confirmed inherited AdvC scenario normalization-target ownership leak.
F629 / provisional KI#951 — CONFIRMED here: `teamValue`'s fallback documented as "unassigned sites"
iterates all alive player placeholders and therefore reintroduces assigned/completed rival sites that
its immediately preceding branch explicitly excluded, producing live team-site ranking inversions.
F630 / provisional KI#952 — UNUSED next root ID.

Exact continuation:
C031-WIP456 physical -> Queue 025 OPEN -> exclude F624-F629 -> finish remaining SPI state/producer-
consumer/history reconciliation -> close Queue 025 if no independent current-live root survives ->
freeze next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP456
===============================================================================

===============================================================================
C031-WIP457 — QUEUE 025 CLOSURE / FINAL RECONCILIATION
===============================================================================
Physical parent: C031-WIP456
Audit target: `CvGameCoreDLL/StartingPositionIteration.cpp`
Gameplay source status: UNTOUCHED.

FINAL NEGATIVE-CONTROL RECONCILIATION
-------------------------------------
This closure pass completed the remaining constructor / PotentialSites / DistanceTable /
SpaceEvaluator / Step / normalization / team-assignment producer-consumer checks and the
full semantic-history sweep of every commit touching `StartingPositionIteration.cpp`.

The following late candidates were explicitly investigated and CLOSED below finding threshold:

1. MAP-SCRIPT RESTRICTED-AREA UNION — INTENTIONAL SHIPPED CONTRACT
   `m_bRestrictedAreas` causes `PotentialSites` to retain the union of areas occupied by the
   initial map-script-selected sites rather than preserving a separate per-player area mask.
   This initially looked capable of moving a player into another player's script-selected area.
   Bundled script evidence closes the concern:
   - `Water.py`, `Fantasy_Realm.py`, and `Lakes.py` simply force every player to the same biggest
     land area.
   - `PerfectMongoose.py` explicitly documents that it supplies one area per player but accepts
     that SPI may move players between those starting areas; only the broader Old-World/New-World
     split is intended to survive SPI. The shared union therefore matches the shipped contract.

2. TEAM REASSIGNMENT VS. NORMALIZATION-TARGET ATTRIBUTES — PHYSICAL-PLOT KEYING IS CORRECT
   `assignSitesToTeams()` computes a later permutation of the physical sites, while
   `m_currSolutionAttribs` was evaluated before that permutation. This looked at first like the
   subsequent `NormalizationTarget` might key a newly assigned plot with another plot's old
   attributes. The exact caller ordering disproves that: `CvGame::assignStartingPlots()` calls
   `spi.createNormalizationTarget()` before it applies `spi.getTeamSite()` to actual players.
   `NormalizationTarget` therefore records the pre-team physical plot -> breakdown mapping while
   it is still valid. Later player permutations look up by physical plot, which deliberately
   preserves the correct site data through team and handicap reassignment.

3. STEP ACCEPT / ROLLBACK AND SOLUTION-ATTRIBUTE SYNCHRONIZATION — CURRENT CODE SYMMETRIC
   `Step::take()` applies every requested move; rejected steps restore the complete original
   player-position snapshot via `takeBack()`. Accepted steps retain the physical moves and copy the
   full `SolutionAttributes` object. The old pre-practical-3183 manual-copy path that could omit
   members is no longer current.

4. `SpaceEvaluator` DELAY-CACHE INDEXING — BOUNDED
   The cache is sized to `m_iDistThresh`; each consumer first subtracts `m_iDistSubtr` from a raw
   distance already constrained to the threshold. Thus the effective `iDist` used as the cache
   index remains below the vector size. No current out-of-bounds path was established.

5. `SpaceEvaluator` EXCLUSION OF PLOTS WITHIN ANY STARTING BFC — DELIBERATE ACCOUNTING SPLIT
   Those tiles are intentionally omitted from expansion-space claims because their value is already
   represented by the city-site found-value component. The earlier geometric concern about the four
   5x5 outer corners is also closed: AdvC `CvMap::plotDistance(2,2)` is 3, not 2, so those non-BFC
   corners are not accidentally suppressed by the `<= 2` check.

6. OCC / REPRESENTATIVE-PLAYER CITY-SITE EVALUATOR CONCERN — NO CURRENT SETUP CONTRADICTION
   Before initial cities exist, the one-city-challenge limit does not invalidate first-city founding
   evaluation. No supported setup was found where choosing the least-tech representative player makes
   all viable starting sites disappear or creates a current-live invalid state.

7. WORLD-BUILDER `RandomStartLocation` — CONTRACT PRESERVED
   Practical 2708's contract is to avoid directly assigning one of the pre-marked scenario starts and
   instead invoke the randomized `findStartingPlot` path. It does not guarantee that randomized search
   can never coincidentally select a tile that was also marked. Current code preserves the flag/path.

8. LOW-GRADE HEURISTIC / PATHOLOGICAL CONTROLS — NOT PROMOTED
   The previously recorded `currAltSites` taken-site `break`, exact remote-threshold `>=`/`>` mismatch,
   same-area teamValue repetition, remote-site area-size/found-value weighting, and pathological short
   distance-overflow possibilities remain below the album's independent current-live finding threshold.
   General wrapped-map duplicate visitation remains reserved for Queue 167 `PlotRadiusIterator.cpp`,
   which owns the iterator-level root rather than SPI.

FULL HISTORY SWEEP
------------------
Every recorded semantic stratum touching `StartingPositionIteration.cpp` was reconciled, including the
initial WIP series and later corrections/tuning/refactors: practical 2280, 2284, 2302, 2305, 2322,
2337, 2354, 2355, 2382, 2394, 2402, 2404, 2431, 2441, 2446, 2503, 2535, 2562, 2567, 2571, 2595,
2617, 2685, 2698, 2708, 2723, 2998, 3029, 3109, 3134, 3158, 3183, 3208, 3209, 3234, 3392,
3605, 3880 and 3898, plus current SAS semantic work (notably the found-value short->int pipeline)
and later formatting/refactor churn.

No additional one-line regression or producer/consumer mismatch survives independently of F624-F629.
In particular, practical 2322 repairs an earlier distance-variable mistake; 2394's rival-volatility
change is immediately amended by 2402; 2567 establishes safe pointer initialization for early-return
construction; 2617 is an adjacency-iteration refactor; 3183 replaces the older manual
`SolutionAttributes` copy with whole-object assignment; and 3605's water-yield destination filtering is
coherent with the later water-distance path (apart from the separately confirmed F625 fallback-unit root).

QUEUE 025 — CLOSED
------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` is COMPLETE.

Confirmed independent roots owned by this queue:

F624 / provisional KI#946 — inherited AdvC normalization-level tolerance switch fallthrough.
F625 / provisional KI#947 — inherited AdvC water fallback mixes raw plot-distance units with SPI
                                weighted path-distance units.
F626 / provisional KI#948 — inherited AdvC unequal-team round-robin can revisit/overfill a completed
                                team and violate team/site cardinality.
F627 / provisional KI#949 — inherited AdvC Dijkstra destination distances can be overwritten by stale
                                later priority-queue entries because `setDistance` precedes the reached check.
F628 / provisional KI#950 — inherited AdvC partial-scenario normalization-target allocation is leaked
                                after its scenario-only handicap consumer because the caller returns NULL.
F629 / provisional KI#951 — inherited AdvC `teamValue` fallback documented as using unassigned sites
                                iterates all alive placeholders, reintroducing already-assigned/completed
                                rival sites and causing deterministic team-site ranking inversions.

F630 / provisional KI#952 remains UNUSED.

No gameplay source file was modified during Queue 025. The only physical change is this durable audit
checkpoint.

NEXT QUEUE
----------
The priority-next-WIP override that selected Queue 025 has now been consumed. Resume ordinary queue
order at the still-PENDING Queue 024 `CvGameCoreDLL/CyCity.cpp`.

Preserve Queue 167 `PlotRadiusIterator.cpp` as the owner/candidate location for the wrapped-map radius
iterator duplicate-visitation root; do not duplicate it into Queue 024 or retroactively into Queue 025.

Exact continuation stone:
C031-WIP457 physical -> Queue 025 COMPLETE -> F624-F629 confirmed -> F630/KI#952 UNUSED ->
Queue 024 `CvGameCoreDLL/CyCity.cpp` OPEN next -> establish current source / lineage / SAS deltas ->
perform deep method-by-method audit under existing dedup and source-ownership rules -> freeze next
physical checkpoint regardless of finding count.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP457
===============================================================================

===============================================================================
C031-WIP458 — QUEUE 024 CyCity.cpp — F630 / KI#952 CONFIRMED
===============================================================================
Physical parent: C031-WIP457
Audit target: `CvGameCoreDLL/CyCity.cpp`
Gameplay source status: UNTOUCHED.

F630 / provisional KI#952 — CONFIRMED — INHERITED ADVCIV PYTHON-WRAPPER SEMANTIC REGRESSION
------------------------------------------------------------------------------------------------

TITLE
-----
CyCity.AI_cityValue forwards AdvCiv's sign-flipped AI_splitEmpireValue, while the shipped colony
advisor still uses K-Mod's old `< 0` city-value test, reversing colony-advice polarity.

CURRENT LIVE CHAIN
------------------
The current native function is explicitly sign-flipped:

    // CvCityAI.cpp
    int CvCityAI::AI_splitEmpireValue() const
    {
        ...
        int iValue = ...; // city output/value minus colony/maintenance costs
        ...
        iValue -= iCosts;
        return -iValue; // advc.ctr: Sign flipped to match the new function name
    }

Current native empire-splitting code consumes that new polarity consistently:

    // CvPlayerAI.cpp
    mapAreaValues[pLoopCity->getArea().getID()] += pLoopCity->AI_splitEmpireValue();
    ...
    // advc.ctr: First condition inverted to match new semantics of AI_splitEmpireValue
    if (it->second <= 0 && !bForce)
        continue;

So under current semantics:
- positive `AI_splitEmpireValue` means splitting that area is attractive;
- negative means the city/area is valuable enough that splitting is unattractive.

But the public Python wrapper retained its old K-Mod name while forwarding the new, opposite-sign
native function:

    // CyCity.cpp
    int CyCity::AI_cityValue()
    {
        return m_pCity ? m_pCity->AI_splitEmpireValue() : -1;
    }

And current shipped `Assets/Python/CvAdvisorUtils.py` still uses the original K-Mod predicate:

    elif (gc.getPlayer(iPlayer).canSplitEmpire() and
          gc.getPlayer(iPlayer).canSplitArea(pCity.area().getID()) and
          pCity.AI_cityValue() < 0):
        ... TXT_KEY_POPUP_COLONY_DEMAND ...

That callback is live: `CvEventManager.onCityDoTurn` calls `CvAdvisorUtils.cityAdvise(pCity,
iPlayer)` for ordinary city turns. The surrounding gates merely require the human advisor-popup
option, a non-network game, the periodic city-advice turn, a city old enough to advise, no current
liberation target, and a splittable empire/area.

POLARITY PROOF
--------------
K-Mod's original `CvCityAI::AI_cityValue()` computed:

    iValue = useful city output/value - colony/maintenance costs;
    return iValue;

and K-Mod's unchanged advisor reasonably prompted a colony when:

    pCity.AI_cityValue() < 0

i.e. the city was a net burden under that heuristic.

AdvCiv practical 2176 changed the native semantic to:

    return -iValue;

so the same bad/burdensome city now yields a positive split value, while a strong/profitable city
yields a negative split value. The unchanged Python `< 0` predicate therefore does the opposite of
its original meaning:

- strong/profitable city: old `iValue > 0` -> new wrapper value `< 0` -> colony nag CAN fire;
- costly/split-worthy city: old `iValue < 0` -> new wrapper value `> 0` -> colony nag is SUPPRESSED.

This is not merely a stale method name. The one current shipped consumer branches on the old sign and
therefore receives behavior opposite to the current native split heuristic.

HISTORY / INTRODUCTION
----------------------
AdvCiv practical 2176, commit
`a5a55a4760176372f73087ece2fcbf373f0c2a3c`, title:

    Use AI_assetVal in a couple of additional places

contains the complete introduction fingerprint in four C++ files only:

1. `CvCityAI::AI_cityValue` -> `AI_splitEmpireValue`;
2. `return iValue` -> `return -iValue`, with the explicit comment
   "Sign flipped to match the new function name";
3. `CvPlayerAI::AI_doSplit` changes its area acceptance condition to match the new polarity;
4. `CyCity::AI_cityValue` is changed only from forwarding `AI_cityValue()` to forwarding
   `AI_splitEmpireValue()`.

The commit does NOT touch `Assets/Python/CvAdvisorUtils.py`.

Current Base AdvCiv 1.14 retains the same wrapper forwarding and the same shipped Python `< 0`
predicate. Current SAS differs from Base AdvCiv in `CyCity.cpp` only through the already-fixed
KI#480 `getOrderFromQueue` ownership copy; it inherits this AI-city-value wrapper semantic unchanged.
K-Mod 1.46 retains the original internally consistent pair: native `AI_cityValue()` returns `iValue`,
and `CvAdvisorUtils.py` tests `< 0`.

DEDUP
-----
Whole-album and current Known-Issues searches found no existing owner for this exact CyCity/Python
colony-advice polarity mismatch.

- Earlier audits of native `AI_splitEmpireValue` correctly found no current SAS-vs-Base semantic
  regression in the native function itself; F630 is the cross-boundary stale Cy/Python consumer
  contract introduced upstream in AdvCiv.
- KI#213's `AI_countBestBuilds` / good-plot-policy family is unrelated.
- KI#480 concerns `OrderData` Python ownership in this same wrapper file, not AI-value semantics.
- Ordinary native `AI_doSplit` uses the sign-flipped value correctly and is not the defect.

REPAIR BOUNDARY
---------------
The smallest current-behavior repair is to restore the old Python-facing `AI_cityValue` polarity
rather than silently changing every potential Python caller:

    int CyCity::AI_cityValue()
    {
        return m_pCity ? -m_pCity->AI_splitEmpireValue() : -1;
    }

That preserves the historical/public method name and its K-Mod/BtS value semantics, making the
existing shipped `< 0` advisor test correct again.

An alternative is to rename/expose a new Python `AI_splitEmpireValue` method and change the shipped
advisor to test `> 0`, but that is a broader public-API change and should not masquerade as the
minimal bug repair.

Regression coverage:
1. construct a splittable human overseas/colony city with old-style net city value < 0; the Python
   wrapper's legacy `AI_cityValue()` must remain < 0 and the periodic colony advice may trigger;
2. a clearly profitable/high-output city with old-style value > 0 must return > 0 through the legacy
   wrapper and must not trigger the `< 0` colony nag;
3. native `CvPlayerAI::AI_doSplit` must continue using `AI_splitEmpireValue()` with its current
   positive-means-split semantics;
4. Base/K-Mod comparison should retain K-Mod's original wrapper/consumer polarity as the semantic
   oracle.

POST-F630 CURSOR
----------------
Queue 024 `CvGameCoreDLL/CyCity.cpp` remains OPEN.
F631 / provisional KI#953 is UNUSED.

Current SAS delta is otherwise only the already-fixed KI#480 `getOrderFromQueue` value-copy repair.
Continue the AdvC-vs-K-Mod semantic wrapper pass, then Boost binding/return-policy, enum/sentinel,
argument-null/live-call-site and Python-consumer reconciliation. Do not rediscover KI#480 or spend a
finding on unsupported Python-None arguments without a stronger public/live contract.

CURRENT LEDGER / RESUME STONE
-----------------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — COMPLETE.
Queue 024 `CvGameCoreDLL/CyCity.cpp` — OPEN.
F630 / provisional KI#952 — CONFIRMED here: inherited AdvCiv sign-polarity regression across
`CvCityAI::AI_splitEmpireValue` -> `CyCity::AI_cityValue` -> shipped `CvAdvisorUtils.cityAdvise`.
F631 / provisional KI#953 — UNUSED next root ID.

Exact continuation:
C031-WIP458 physical -> Queue 024 OPEN -> exclude F630 and KI#480 -> finish current/Base/K-Mod/Civ4CE
wrapper/native signature and public Python consumer reconciliation -> promote only an independent
mechanical/current-live or strong public-API root -> freeze next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP458
===============================================================================

===============================================================================
C031-WIP459 — QUEUE 024 CyCity.cpp — POST-F630 WRAPPER-CONTRACT RECONCILIATION
===============================================================================
Physical parent: C031-WIP458
Audit target: `CvGameCoreDLL/CyCity.cpp`
Gameplay source status: UNTOUCHED.

QUEUE STATE
-----------
Queue 024 remains OPEN.
F630 / provisional KI#952 remains CONFIRMED exactly as frozen in WIP458.
F631 / provisional KI#953 remains UNUSED.

CURRENT SAS VS BASE ADVC 1.14
----------------------------
After normalizing line endings/format-only noise, current SAS `CyCity.cpp` differs executably from
Base AdvC 1.14 only in the already-owned KI#480 `getOrderFromQueue` repair: SAS returns an independent
`OrderData` copy to make the existing Boost `manage_new_object` policy truthful. Do not rediscover
that ownership root in this queue.

BOOST BINDING / IMPLEMENTATION SYMMETRY
----------------------------------------
Reconciled both `CyCityInterface1.cpp` and `CyCityInterface2.cpp` against current `CyCity.cpp/.h`:
- 435 unique bound Python method names;
- all 435 resolve to a current body or inline implementation;
- no duplicate Python method names across the two interface files;
- only two current implementation methods are unbound: `getBonusBadHappiness` and
  `getNumTrainUnitAI`.
Both omissions already exist in K-Mod/Civ4CE and neither has a shipped current Python caller. Treat
as dormant legacy API omissions, not current F631 evidence.

MANAGED POINTER RETURNS
-----------------------
Every currently bound `manage_new_object` Cy object return was checked:
- `getCityIndexPlot` -> allocates `new CyPlot(...)`;
- `plot` -> allocates `new CyPlot(...)`;
- `area` -> allocates `new CyArea(...)`;
- `waterArea` -> allocates `new CyArea(...)`;
- `getRallyPlot` -> allocates `new CyPlot(...)`;
- `getTradeCity` -> allocates `new CyCity(...)`;
- `getOrderFromQueue` -> SAS now allocates `new OrderData(copy)` (KI#480).
No second current owning-policy mismatch was found.

POINTER-ARGUMENT / NULL CONTRACT PASS
-------------------------------------
Potentially unsafe top-level Python pointer arguments include `getCityPlotIndex`, `canWork`,
`addProductionExperience`, `atPlot`, `isBombardable`, `calculateTradeProfit`, `isWorkingPlot`, and
`AI_countBestBuilds` (while `isConnectedTo` already checks both wrapper and native pointer).

Shipped current Python call sites were reconciled:
- `canWork` / `isWorkingPlot`: called only with concrete map/city-radius plots after caller checks;
- `calculateTradeProfit`: current trade UI/BUG paths obtain a trade-city wrapper and validate owner /
  iterate only active trade routes before use;
- `AI_countBestBuilds`: called with the concrete `pCity.area()`;
- `addProductionExperience`, `atPlot`, `isBombardable`, `getCityPlotIndex`: no shipped current Python
  callers.
Thus missing Python-`None` hardening by itself remains below the current-live finding threshold.

ADVC.901 LEGACY FEATURE-NAME WRAPPERS — CLOSED CONTROL
------------------------------------------------------
Practical 1732 deliberately broadened the native feature-only health/happiness concept to
"surroundings" so accessible improvement effects can participate. It intentionally changed the four
legacy Cy bodies:

    getFeatureGoodHealth      -> getSurroundingGoodHealth
    getFeatureBadHealth       -> getSurroundingBadHealth
    getFeatureGoodHappiness   -> getSurroundingGoodHappiness
    getFeatureBadHappiness    -> getSurroundingBadHappiness

without renaming the Python-facing methods. No shipped current Python code calls those four names.
This is stale public naming / compatibility shape, not a demonstrated current semantic regression.

ADVC-ADDED LIVE WRAPPER CONSUMERS — RECONCILED
-----------------------------------------------
`GPProjection`:
- wrapper returns the requested unit's projected percentage or -1;
- current BUG `GPUtil.py` accepts only values >0;
- the native disorder guard was deliberately added, briefly reverted, then restored in adjacent
  AdvC history; no wrapper drift found.

Production turns / food turns / decay:
- production-turn wrappers map native `MAX_INT` to -1, and shipped UI displays only values >0;
- `getFoodTurnsLeft` maps native stagnation `MAX_INT` to 0; current city-screen/domestic-advisor
  callers gate on food difference and explicitly treat zero-food growth as no growth;
- BULL production-decay wrappers and current queue UI use the same countdown semantics introduced
  together.
No independent root found.

Revolt/culture-flip wrappers:
- `revoltProbability()` exports the native [0,1]-style probability as float;
- Domestic Advisor multiplies it by 100 for display;
- `canCultureFlip()` is used only to color that display according to whether a flip is possible.
The pair was introduced with its consumer and remains coherent.

Partisan wrapper:
- `getNumPartisanUnits(ePlayer)` was introduced together with the Python Partisans event rewrite and
  forwards the native culture-level-minus-one helper exactly; current Python uses it consistently.

`AI_neededFloatingDefenders`:
- exposed primarily for OOS logging with the const-cache-safe arguments (`false, true`); no gameplay
  decision consumes the wrapper result.

FRACTIONAL-TRADE PROBE — CLOSED FALSE LEAD
------------------------------------------
Current BUG `TradeUtil.initFractionalTrade()` probes for `CyCity.calculateTradeProfitTimes100`; that
method is not exposed, while native AdvC does have `CvCity::calculateTradeProfitTimes100`.
This initially looks like a missing binding, but history proves it is intentional:
- practical 2187 renamed the native helper and increased its precision specifically so trade-route
  candidate assignment has fewer ties;
- the public/actual trade-profit function remains `calculateTradeProfitTimes100(...) / 100`;
- practical 2986 explicitly removed `_MOD_FRACTRADE` support from AdvC as untested.
Therefore BUG is correct to detect fractional trade as inactive. Exposing the internal times-100
ranking helper as though it were BULL fractional-trade support would make the Python UI use the wrong
units. No F631.

TRADE-CITY NULL CHAIN — CLOSED CONTROL
--------------------------------------
`CvCity` keeps its trade-city vector sized to `MAX_TRADE_ROUTES`. `CyCity::getTradeCity(i)` can return
a valid Cy wrapper whose native pointer is null for an unused slot; current UI paths either:
- iterate only `range(city.getTradeRoutes())`, or
- scan MAX routes and require `getOwner() >= 0` before using `calculateTradeProfit`.
No live null-native dereference was established.

DESTRUCTIVE-WRAPPER LIFETIME CURSOR
-----------------------------------
`CyCity::kill()` is live through the Tsunami random event and calls native `CvCity::kill(true)`.
That native call removes/destroys the city while the local Cy wrapper still physically stores the old
pointer. The current Tsunami handler does not access that wrapper after `kill()`, so no live
use-after-free has yet been demonstrated. Continue checking destructive wrappers (`kill`, `doTask`,
`liberate`) and callbacks before treating this generic Civ4 wrapper lifetime shape as a finding.

OTHER PRESERVED LOW-GRADE CONTROLS
----------------------------------
- `setWallOverridePoints(tuple)` reads coordinates in pairs and would read past an odd-length tuple;
  no shipped current Python caller exists. Keep as public-input hardening debt unless a live source
  contract appears.
- legacy interface documentation has stale signatures in places (e.g. liberation docs), while Boost
  binds the actual C++ function pointer and current callers use the true arity. Documentation-only.
- `getTradeCultureRateTimes100(iLevel)` retains an unused Python parameter, but K-Mod already ignored
  that parameter natively; current AdvC merely removed it from the native signature. No behavior
  change.
- enum/index wrappers generally assume valid Python enum values, as in Firaxis/K-Mod. Do not promote
  invalid-input crashes without a shipped/current producer that can supply the invalid value.

CURRENT LEDGER / EXACT RESUME
-----------------------------
Queue 025 `StartingPositionIteration.cpp` — COMPLETE.
Queue 024 `CvGameCoreDLL/CyCity.cpp` — OPEN.
F630 / provisional KI#952 — CONFIRMED (AI_cityValue / AI_splitEmpireValue polarity regression).
F631 / provisional KI#953 — UNUSED.

Exact continuation:
C031-WIP459 physical -> Queue 024 OPEN -> exclude F630 + KI#480 -> audit inherited Firaxis/K-Mod
wrapper contracts beyond the small AdvC delta: destructive-call lifetime/callbacks, enum/index
producers, object-return nullable chains, old Python API semantics and any current shipped callers ->
trace any surviving candidate into native source + K-Mod/Civ4CE + commit history -> freeze WIP460 at
the next clean checkpoint regardless of finding count.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP459
===============================================================================

===============================================================================
C031-WIP460 — QUEUE 024 CyCity.cpp — FINAL CLOSURE / F631 UNUSED
===============================================================================
Physical parent: C031-WIP459
Audit target: `CvGameCoreDLL/CyCity.cpp`
Gameplay source status: UNTOUCHED.

QUEUE STATE
-----------
Queue 024 `CvGameCoreDLL/CyCity.cpp` is now COMPLETE.
F630 / provisional KI#952 remains CONFIRMED exactly as frozen in WIP458.
F631 / provisional KI#953 remains UNUSED.

FINAL INHERITED WRAPPER RECONCILIATION
--------------------------------------
The remaining Firaxis/K-Mod/AdvC wrapper surface was completed method-by-method after WIP459.
No second independent current-live or strong public-API root survived.

1. DESTRUCTIVE CyCity WRAPPERS — NO CURRENT PYTHON UAF ROOT
   - The only shipped direct Python `CyCity.kill()` call is the Tsunami random-event handler.
     That handler does not read the same CyCity wrapper after `kill()` destroys the native city.
   - No shipped direct Python callers of `CyCity::liberate(bool)` or `CyCity::doTask(...)` were found.
   - Native liberation's acquire/destroy boundary already has the separate SAS KI#486.2 repair in
     `CvCity::liberate`; do not duplicate that root into this wrapper queue.
   Thus the generic stale-wrapper lifetime shape remains API-hardening debt rather than a demonstrated
   current CyCity defect.

2. POINTER-ARGUMENT DEREFERENCES — SHIPPED PRODUCERS SATISFY THE CONTRACT
   The only direct wrapper-argument dereferences in current `CyCity.cpp` are concentrated in
   `getCityPlotIndex`, `canWork`, `addProductionExperience`, `atPlot`, `isBombardable`,
   `calculateTradeProfit`, `isWorkingPlot`, and `AI_countBestBuilds`.
   Current shipped Python either has no caller for the unsafe legacy methods or supplies a concrete
   wrapper/native object after its own validity checks. AdvC's explicit guards in
   `getCityPlotIndex`, `canWork`, `isWorkingPlot`, and `isConnectedTo` further reduce the surface.
   No current Python-None/native-null producer was established.

3. ENUM / INDEX / MUTATING CALLS — LIVE INPUTS VALID
   - shipped city-index consumers use `0 .. NUM_CITY_PLOTS-1`;
   - current production/build/hurry/event consumers pass valid enum IDs with the bound arity;
   - the Influenza random event's `changePopulation(-2)` path first restricts targets to population
     greater than 2, so it cannot create a negative city population through that call;
   - odd-length `setWallOverridePoints` input remains an uncalled public-input hardening issue.

4. LEGACY PRODUCTION-TURNS API — PROCESS 0 -> -1 CHANGE IS UI-NEUTRAL
   Practical 1687 removed native `getGeneralProductionTurnsLeft()` and retained the old Python name as
   an alias to `getProductionTurnsLeft()`. AdvC's native comment notes the one semantic difference:
   processes formerly returned 0 while the replacement produces MAX_INT, converted by CyCity to -1.
   The shipped Domestic Advisor displays this value only when `> 0`; both 0 and -1 therefore render
   blank for a process. No current user-visible regression survives.

5. CIV4CE / K-MOD ORACLE CROSS-CHECK
   The comparison trees do not reveal an overlooked current CyCity semantic repair. Relevant
   differences are already owned/explained: AdvC's stronger pointer/sentinel guards, F630's colony
   value sign drift, and SAS's KI#480 `OrderData` ownership repair (which Firaxis/K-Mod/Civ4CE still
   lack). No independent F631 emerged.

6. BINDING / OWNERSHIP / ADVC-DELTA CONTROLS FROM WIP459 RETAINED
   - 435 unique bound method names resolve to current bodies/inline implementations;
   - no duplicate Python method names;
   - the only unbound current bodies are legacy `getBonusBadHappiness` and `getNumTrainUnitAI`, both
     already unbound in comparison trees and without shipped callers;
   - every current `manage_new_object` Cy return is an actual allocation, including the SAS KI#480
     `OrderData` copy;
   - advc.901 surrounding-health/happiness legacy names, GPProjection, production/food/decay
     sentinels, revolt/culture-flip, partisan, floating-defender logging, trade-city nullable chains,
     and the fractional-trade probe all remain reconciled as documented in WIP459.

FULL QUEUE RESULT
-----------------
Queue 024 owns one confirmed independent root:

F630 / provisional KI#952 — inherited AdvC practical-2176 Python/native sign-polarity regression:
`CyCity::AI_cityValue()` forwards sign-flipped native `AI_splitEmpireValue()` while shipped
`CvAdvisorUtils.cityAdvise` still uses K-Mod's old `< 0` colony-advice predicate.

F631 / provisional KI#953 remains UNUSED. Do not manufacture a second finding from generic wrapper
hardening, stale names, unsupported Python inputs, or already-owned native lifetime defects.

SCHEDULING RECONCILIATION
-------------------------
The album-front method rule is explicit: after closing a file, every still-PENDING entry marked
`priority next wip after the first reloop` is processed before ordinary queue order, in queue order.
WIP457's sentence that Queue 025 had consumed the whole priority override was too narrow; it consumed
only Queue 025 itself. Queue 024 was audited next and is now closed, but the scheduling rule must be
restored from this point onward rather than continuing directly to ordinary Queue 026.

The next still-PENDING priority-marked entry by queue order is therefore:

    Queue 036 `CvGameCoreDLL/CvMap.cpp` (1,596 queued lines)

After Queue 036, later priority-marked pending entries remain ahead of ordinary queue order unless a
newer durable checkpoint explicitly changes the scheduling rule.

CURRENT LEDGER / EXACT RESUME
-----------------------------
Queue 025 `CvGameCoreDLL/StartingPositionIteration.cpp` — COMPLETE.
Queue 024 `CvGameCoreDLL/CyCity.cpp` — COMPLETE.
F630 / provisional KI#952 — CONFIRMED.
F631 / provisional KI#953 — UNUSED next root ID.
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN NEXT (priority-next-WIP rule).

Preserve the already-noted adjacent `CvMap::isCustomMapOption(CvWString, bool, bool)` forwarding
candidate from the True Starts audit as a Queue-036 cursor: the overload appears to drop the two
Boolean arguments when forwarding. Re-prove it locally and deduplicate before promotion; do not
pre-assign F631 merely from the old cursor note.

Exact continuation:
C031-WIP460 physical -> Queue 024 COMPLETE / F630 confirmed / F631 unused -> Queue 036 CvMap.cpp OPEN
(priority) -> establish current/Base/K-Mod/Civ4CE lineage and SAS delta -> begin with the preserved
`isCustomMapOption` overload cursor, then audit wrap/distance/area/reset/plot-index and Python map
contracts -> promote only an independent current-live or strong public-API root -> freeze next WIP.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP460
===============================================================================

===============================================================================
C031-WIP461 — QUEUE 036 CvMap.cpp — F631 / KI#953 CONFIRMED
===============================================================================
Physical parent: C031-WIP460
Audit target: `CvGameCoreDLL/CvMap.cpp`
Gameplay source status: UNTOUCHED.

QUEUE STATE
-----------
Queue 036 `CvGameCoreDLL/CvMap.cpp` is OPEN under the restored priority-next-WIP rule.
F631 / provisional KI#953 is CONFIRMED here.
F632 / provisional KI#954 is UNUSED next root ID.

F631 / PROVISIONAL KI#953
-------------------------
TITLE:
Inherited AdvC `CvMap::isCustomMapOption(CvWString, ...)` defeats its translated-string contract by
narrowing through a known non-ASCII-unsafe conversion and also drops both matching-mode arguments.

ROOT
----
Current `CvMap.cpp` provides two overloads:

    bool CvMap::isCustomMapOption(char const* szOptionsValue,
        bool bCheckContains, bool bIgnoreCase) const

and, explicitly "for convenience, especially when working with translated strings":

    bool CvMap::isCustomMapOption(CvWString szOptionsValue,
        bool bCheckContains, bool bIgnoreCase) const
    {
        CvString szNarrow(szOptionsValue);
        return isCustomMapOption(szNarrow.c_str());
    }

The wide overload is broken in two coupled ways at the same forwarding boundary:

1. it narrows the translated `CvWString` through `CvString` before comparing it;
2. it fails to forward `bCheckContains` and `bIgnoreCase`, so the narrow overload silently receives
   its defaults (`false`, `true`) no matter what the wide caller requested.

The first issue is current-live; the second is an additional explicit public-overload contract defect.
They share one faulty forwarding implementation and therefore remain one root/finding.

NON-ASCII CONVERSION IS EXPLICITLY KNOWN UNSAFE IN THE CURRENT SOURCE
---------------------------------------------------------------------
Current `CvString.h` documents its explicit std::wstring constructor:

    /* This conversion breaks when there are any non-ASCII chars in the wstring. ... */
    CvString(const std::wstring& s) { Copy(s.c_str()); }

`CvString::Copy(const wchar*)` performs the conversion through:

    sprintf(s, "%S", w);

and `CvWString::Copy(const char*)` converts back through `swprintf(..., L"%S", s)`.
Thus the `CvMap` overload specifically advertised for translated wide strings routes those strings
through a conversion that the same current codebase says breaks on any non-ASCII character.

CURRENT SHIPPED FRENCH REPRODUCER — BALANCED RESOURCES
------------------------------------------------------
The standard game text defines `TXT_KEY_MAP_BALANCED` as:

    English  = Balanced
    French   = Equilibré
    German   = Ausgewogen
    Italian  = Bilanciato
    Spanish  = Equilibrado

French therefore contains a non-ASCII `é`.

Bundled standard scripts such as `PrivateMaps/Fractal.py` return the selected map-option description
through:

    translated_text = unicode(CyTranslator().getText(...))

and use `TXT_KEY_MAP_BALANCED` for the Balanced selection.

Current `CvMapGenerator::generateRandomMap()` correctly attempts a language-independent check:

    if (kMap.isCustomMapOption(gDLL->getText("TXT_KEY_MAP_BALANCED")))
        GC.getGame().setStartingPlotNormalizationLevel(CvGame::NORMALIZE_HIGH);

`gDLL->getText` supplies the translated wide `Equilibré`, which selects the `CvWString` overload.
That overload narrows the non-ASCII text through the conversion documented as broken, so it can fail
to match the map script's still-wide translated option description.

This is gameplay-significant in current SAS. `Assets/XML/GlobalDefines_advc.xml` keeps:

    NORMALIZE_STARTPLOTS_AGGRESSIVELY = 0

and documents that Balanced should override that setting and normalize as in BtS. If the French
Balanced check is lost, `CvGame::initDiplomacy()` sees that normalization is not HIGH and calls the
default-level setter, which yields LOW in ordinary single-player. A French Balanced game can therefore
receive LOW instead of the explicitly intended HIGH start normalization.

`TrueStarts` also calls the same wide overload with translated `TXT_KEY_MAP_BALANCED` when populating
`m_bBalancedResources`; that is a second consumer of the same broken helper. Its sanitation effect is
configuration-dependent, so the map-generator normalization path above is the clean current-live
activation proof.

HISTORY — REGRESSION INTRODUCED BY THE LANGUAGE-INDEPENDENCE CHANGE
-------------------------------------------------------------------
AdvC practical 3712 (`756f4f2c2e4edb5224ddd2324e1cf11b7f5853b2`), titled
"Language-independent check for Balanced Resources option", introduced the wide overload and replaced
the previous `CvPythonCaller::isAnyCustomMapOptionSetTo(CvWString)` helper.

The removed helper compared two wide strings directly:

    CvWString szOptionDescr(...);
    if (szOptionDescr == szTranslatedDesc)
        return true;

so it did not narrow translated text.

The replacement wide `CvMap` overload was introduced in that same practical with the comment about
translated wide strings, but immediately performed `CvString szNarrow(szOptionsValue)` and then called
the narrow overload without either Boolean argument. The non-ASCII regression and Boolean-forwarding
defect therefore existed from the overload's first revision.

Current Base AdvC 1.14 retains the exact same body. Current SAS only reformats the signature and
inherits the semantics unchanged. K-Mod and Civ4CE predate/do not contain this AdvC-created helper,
so classification is an inherited AdvC defect rather than a SAS regression.

SECONDARY PUBLIC-API REPRODUCER — DROPPED BOOLEAN FLAGS
-------------------------------------------------------
The header publicly declares:

    bool isCustomMapOption(CvWString szOptionsValue,
        bool bCheckContains = false, bool bIgnoreCase = true) const;

but the implementation ignores both supplied values. For example:

    isCustomMapOption(CvWString(L"Old World"), true, false)

actually runs the narrow overload as though called with `(false, true)`: exact-match rather than
contains-match, and case-insensitive rather than case-sensitive. Current shipped non-default calls use
narrow literals, so this secondary symptom is principally a strong public-API defect; it is grouped
with the current-live translated-string failure because both arise from the same incorrect wide-overload
forwarding implementation.

DEDUP
-----
Focused whole-album/current-KI searches found no prior owner for this helper-level root.

F623 / KI#945 is separate. F623 occurs because `TrueStarts::changeCivs` passes English literal
`"Old World"` to a helper that compares against localized option descriptions; a correct helper still
cannot make an English literal equal arbitrary translated text. F631 occurs when a caller does the
right thing and supplies a translated wide value, but the wide overload itself damages that value and
ignores its own mode arguments. The French Balanced reproducer activates F631 without relying on F623.

Other prior locale/wide-string findings do not own this map-option matching helper or its explicit
parameter-forwarding contract.

REPAIR BOUNDARY
---------------
Do not repair this by merely adding the two Boolean arguments to the existing narrow forwarding call;
that would fix only the secondary flag defect while preserving the current-live non-ASCII regression.

A clean repair is to make the wide overload the actual comparison implementation and let the narrow
ASCII overload convert its input *up* to `CvWString` and forward all arguments, e.g. structurally:

    bool CvMap::isCustomMapOption(char const* s, bool contains, bool ignoreCase) const
    {
        return isCustomMapOption(CvWString(s), contains, ignoreCase);
    }

    bool CvMap::isCustomMapOption(CvWString value, bool contains, bool ignoreCase) const
    {
        // perform lowercasing / description iteration directly in wide strings
    }

That preserves translated Unicode throughout and makes both overloads honor the same matching controls.

Regression coverage:
1. French + Fractal (or another standard script) + Balanced selected: translated `Equilibré` must be
   recognized and starting-plot normalization must remain HIGH despite
   `NORMALIZE_STARTPLOTS_AGGRESSIVELY=0`;
2. English/German/Italian/Spanish Balanced detection remains unchanged;
3. a wide-string call with `bCheckContains=true` must perform containment rather than exact matching;
4. a wide-string call with `bIgnoreCase=false` must preserve case sensitivity;
5. narrow callers such as True Starts' English-literal contains checks retain their existing behavior;
6. F623 remains separately covered because English literals cannot safely identify localized option
   descriptions regardless of this helper repair.

POST-F631 CURSOR
----------------
Queue 036 remains OPEN. F632 / provisional KI#954 is UNUSED.

Continue with current-SAS-vs-Base semantic deltas first because this file has several SAS-specific map
lifecycle repairs/changes (`resetRiverIDs`, `recalculateWaterAreas`, `read` modernization and
SASGameRecord finalization). Then reconcile inherited wrap/distance/index/area/plot-group/random-plot,
serialization and Python-exposed map contracts. Deduplicate against prior map/area/river/load findings
before promotion.

CURRENT LEDGER / EXACT RESUME
-----------------------------
Queue 024 `CvGameCoreDLL/CyCity.cpp` — COMPLETE.
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F630 / provisional KI#952 — confirmed inherited AdvC CyCity colony-advice sign regression.
F631 / provisional KI#953 — CONFIRMED here: inherited AdvC wide custom-map-option forwarding root,
with current-live French Balanced normalization failure plus dropped wide-overload mode arguments.
F632 / provisional KI#954 — UNUSED next root ID.

Exact continuation:
C031-WIP461 physical -> Queue 036 OPEN -> exclude F631 and prior map/load/river roots -> audit SAS-only
CvMap deltas, especially serialization/read compatibility and water-area/river-ID lifecycle -> then
inherited wrap/distance/index/area/random-plot/public-Python contracts -> history/dedup any survivor ->
freeze next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP461
===============================================================================

===============================================================================
C031-WIP463 — QUEUE 036 CvMap.cpp — F633 / KI#955 CONFIRMED
===============================================================================
Physical parent: C031-WIP462.
Audit target: `CvGameCoreDLL/CvMap.cpp`
Gameplay source status: UNTOUCHED.

F633 / provisional KI#955 — CONFIRMED — INHERITED BTS/BBAI/K-MOD DUAL-COAST AREA OMISSION
------------------------------------------------------------------------------------------

TITLE
-----
`CvMap::findCity(..., bSameArea=true, bCoastalOnly=true)` recognizes only a coastal city's primary
water area, so a naval unit in that city's second sea can be told that no valid upgrade city exists.

CURRENT ROOT
------------
Current `CvMap::findCity` accepts a city under the same-area filter when either its land area equals
the target plot's area or, for coastal searches, its single primary `waterArea()` equals the target:

    if (!bSameArea || pLoopCity->isArea(getPlot(iX, iY).getArea()) ||
        (bCoastalOnly && pLoopCity->waterArea() == getPlot(iX, iY).area()))

The DLL nevertheless has an explicit `CvPlot::secondWaterArea()` / `CvCity::secondWaterArea()`
contract for a land plot/city adjacent to two different accessible water areas. `waterArea()` chooses
the largest adjacent water area; `secondWaterArea()` chooses the largest other accessible water area.
Numerous current AI paths correctly test `waterArea(...) == target || secondWaterArea() == target` for
such dual-coast/isthmus cities. `findCity` does not.

HARD PRODUCER / CONSUMER CONTRADICTION
--------------------------------------
`CvUnit::getUpgradeCity(UnitTypes, bool bSearch, ...)` documents the distinction as exhaustive search
versus checking the closest valid city; it is not supposed to change which coastal areas make a city
valid.

For a naval unit, the exhaustive (`bSearch=true`) branch loops every team city, requires a coastal
city that `canUpgradeTo(eUnit)`, scores distance, and checks `generatePath`. It does not reject a city
merely because the unit is in that city's second water area.

The non-search (`bSearch=false`) branch instead calls:

    GC.getMap().findCity(getX(), getY(), NO_PLAYER, getTeam(), true, bCoastalOnly)

and then asks whether the returned city `canUpgradeTo(eUnit)`. Because `findCity` recognizes only the
primary `waterArea()`, the same physical city can therefore satisfy `getUpgradeCity(eUnit, true)` but
be invisible to `getUpgradeCity(eUnit, false)`.

MINIMAL CURRENT-LIVE REPRODUCER
-------------------------------
Take a normal dual-coast/isthmus city C adjacent to two separate non-lake water areas:
- area A is larger, so `C.waterArea() == A`;
- area B is the city's accessible second sea, so `C.secondWaterArea() == B`;
- a naval unit U belonging to C's team is on an owned water plot in B;
- C can upgrade U to target unit type T;
- no other eligible coastal city has B as its primary water area.

Then:
1. `U.getUpgradeCity(T, true)` can evaluate C, path to C and return it;
2. `U.getUpgradeCity(T, false)` calls `findCity` on U's B-area plot;
3. C's land area is not B and `C.waterArea()` is A, so C is discarded before `canUpgradeTo(T)`;
4. the non-search result is NULL despite C being a valid, reachable upgrade city.

This topology is not a custom-data fiction. The current DLL explicitly models dual-coast cities
through `secondWaterArea()`, and existing SAS Known Issues/history already exercise isthmus/dual-coast
capital behavior (e.g. KI#194). Current AI code also repeatedly checks both primary and second water
areas for sea-worker/resource and assault-transport logic.

PLAYER-FACING / AI CONSEQUENCE
------------------------------
`CvUnit::hasUpgrade(eUnit, false)` is exactly `getUpgradeCity(eUnit, false) != NULL`, and ordinary
`canUpgrade()` falls through to that test. `CvUnit::canDoCommand(COMMAND_UPGRADE, ...)` calls
`canUpgrade`, while UI upgrade help also calls `canUpgrade(eTo, true)`. Thus a naval unit standing in
its city's second sea can have the ordinary upgrade command/help suppressed until its position changes,
even though the exhaustive upgrade-city search recognizes a usable city.

AI code can also underestimate local upgrade availability where it calls the same primary-only
`findCity`/non-search path. The exact broader policy that `getUpgradeCity(false)` checks only one
nearest city is separate; this finding is the narrower map-area omission that makes an otherwise
qualifying dual-coast city absent from the nearest-city candidate set at all.

LINEAGE / BIRTH
---------------
The mismatch is inherited rather than SAS-local.
- At the earliest available K-Mod boundary (`KMod_3_2414225e64`), the imported Better BTS AI
  `CvPlot::secondWaterArea()` helper and `CvCity::secondWaterArea()` wrapper are already present.
- In that same boundary, `CvMap::findCity` already contains the primary-only coastal condition:
  `pLoopCity->waterArea() == target->area()`.
- Later AdvC changes add observer/no-cheat handling, constness, optimizations and formatting but retain
  the primary-only area test through current SAS.
- The architectural shape is therefore best described as an inherited BtS/BBAI integration omission
  retained by K-Mod and AdvC: the general nearest-city helper never learned the multi-water-area
  model that BBAI itself added and other callers use.

DEDUP / OWNERSHIP BOUNDARY
--------------------------
- KI#194 concerns Settler-site reachability through a capital's second water area; it does not use or
  repair `CvMap::findCity` or upgrade eligibility.
- KI#216 concerns duplicate Work Boat demand when primary and second water pointers collapse to the
  same accessible sea; this finding is the opposite omission (a genuinely distinct second sea is
  ignored).
- KI#188.3.3 fixes AI upgrade-city routing that had already found a city with `bSearch=true`: its
  `bUpgradeCityKnown` flag avoids incorrectly re-running the nearest-city eligibility test. It does
  not repair ordinary `findCity`/`hasUpgrade(false)` or the missing second-water-area candidate.
- F632/KI#954 is the unrelated `Shelf*` ownership leak.

REPAIR BOUNDARY
---------------
Do not merely call `secondWaterArea()` unconditionally inside the existing first condition: that
helper assumes a land plot with an adjacent primary water area. Prefer expressing the real coastal
contract directly and safely.

A robust shape is to cache the valid target plot/area once and, when `bCoastalOnly` and the target is
water, accept any coastal city whose city plot is adjacent to that target water area (or explicitly
check primary plus guarded second water area). `CvPlot::isAdjacentToArea` naturally handles the full
geometric contract, including more than two adjacent water partitions, while a target-water guard
prevents accidentally treating adjacency to a different land area as same-area eligibility.

Regression coverage:
1. construct a dual-coast city with primary water area A and second water area B;
2. place a naval unit in owned B water and make the city capable of its upgrade;
3. verify `getUpgradeCity(T,false)` and `getUpgradeCity(T,true)` agree that the city is eligible;
4. verify the normal upgrade command is available under the same gold/movement requirements;
5. retain ordinary single-coast, inland-land-unit and primary-sea behavior;
6. include an Ice/passable-area case so the fix does not regress the existing primary/second-water
   semantics around impassable water.

POST-F632 CONTROLS CLOSED IN THIS TRANCHE
-----------------------------------------
- `syncRandPlot` weighted selection: current live weights are nonnegative; `BarbarianWeightMap`
  deliberately permits zero probability, and `weightedChoice` intentionally returns NULL when all
  valid candidates have zero total weight. `piValidCount` still reports geometric validity. No F633
  there.
- `findCity` ordinary same-area/coastal semantics are sound for primary seas and valid-coordinate
  current callers; the confirmed defect is specifically omission of secondary adjacent water areas.
- replay texture: empty `&m_replayTexture[0]` remains formal C++ UB with a zero-byte stream count but
  still lacks a demonstrated MSVC/current wrong-game consequence, so it remains below finding
  threshold rather than being folded into F633.
- point-to-plot exact landscape-edge conversion remains under investigation; no live engine producer
  has yet been established at the exact positive boundary.

CURRENT LEDGER / EXACT RESUME
-----------------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F631 / provisional KI#953 — CONFIRMED (wide custom-map-option locale/forwarding root).
F632 / provisional KI#954 — CONFIRMED (final `Shelf*` ownership leak).
F633 / provisional KI#955 — CONFIRMED here (dual-coast `findCity` area omission).
F634 / provisional KI#956 — UNUSED next root ID.

Exact continuation:
C031-WIP463 physical -> Queue 036 OPEN -> exclude F631/F632/F633 and prior map/KI roots -> continue
point-to-plot edge conversion, replay serialization/lifetime, read/rebuild/area ownership, coordinate
and public-Python contracts, then history-only semantic strata -> promote only a distinct hard root ->
freeze WIP464 at the next settled tranche regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP463
===============================================================================

===============================================================================
C031-WIP464 — QUEUE 036 CvMap.cpp — POST-F633 CLOSURE TRANCHE / F634 UNUSED
===============================================================================
Physical parent: C031-WIP463.
Audit target: `CvGameCoreDLL/CvMap.cpp`
Working source base: reconciled `AdvCiv-SAS_light_source_20260907T121108.zip`.
Gameplay source status: UNTOUCHED.

RECONCILED CURRENT STATE
------------------------
Queue 036 remains OPEN. F631/F632/F633 remain confirmed. F634 / provisional KI#956 remains UNUSED.
This tranche deliberately closes several plausible-looking map-lifecycle/topology/API candidates below
finding threshold rather than consuming a new root without a current supported consequence.

1. `CvMap::setup()` / GroupPathFinder heuristic — CLOSED
--------------------------------------------------------
`CvMap::setup()` derives minimum route movement and flat-movement costs for the reusable group
pathfinder. The theoretical bad state would leave those minima at `MAX_INT` if no real route supplied
a cost, but current shipped route XML rules that out:
- Road: movement cost 30 and flat movement cost 30;
- Engineering reduces Road movement by 10, leaving 20;
- Railroad: movement cost 20 and flat movement cost 6.

`GroupPathFinder`/`KmodPathFinder` then clamp the heuristic inputs and `minimumStepCost()` to positive
values. Under current XML this produces a finite admissible lower bound; no overflow/sentinel misuse or
path rejection was demonstrated. No F634.

2. StepFinder topology invalidation — CLOSED for shipped/live behavior
---------------------------------------------------------------------
`CvMap::calculatePathDistance()` invokes the EXE StepFinder with reuse enabled, so a topology mutation
that changes passability must reset it. Current live topology paths reconcile correctly:
- `CvPlot::setPlotType()` resets path-distance state;
- the only current impassable feature is `FEATURE_ICE`;
- ordinary feature growth/events/builds do not create/remove Ice;
- the SAS KI#346 global-warming Ice-melt repair explicitly calls `resetPathDistance()` before rebuilding
  water areas/plot groups;
- bundled map scripts either calculate path distance after feature placement, change topology through
  `setPlotType()`, or (PerfectMongoose) explicitly reset the pathfinder before distance use.

A custom Python/mod-data path that directly toggles Ice through `setFeatureType()` without the SAS
refresh remains generic API-hardening territory, not a demonstrated current-live F634.

3. `calculateReprAreas()` hard 10-pass guard — CLOSED
-----------------------------------------------------
The representative-area loop's `iLoop > 10` emergency stop looked suspicious in isolation. Small
exhaustive legal passability patterns and larger randomized water/Ice partitions converged on the first
changing scan, with the second scan merely observing zero changes. This agrees with the dataflow:
initial area IDs are allocated in first-encounter plot order and representative propagation scans plots
in that same order, allowing the lowest representative encountered in a component to propagate forward
within the pass. No legal current topology requiring >10 iterations was established. Treat the guard as
defensive legacy code, not F634.

4. Coordinate helpers / point conversion — CLOSED below live threshold
---------------------------------------------------------------------
Two mathematical/public-API edges remain real but not current-live findings:
- `coordRange(-range, range, true)` returns `range` rather than 0 because C++ negative modulo yields 0
  and the function adds `range`. Normal DLL offset callers stay within the local one-map span; no shipped
  producer requiring exact negative-multiple wrapping was established. General iterator/wrap behavior
  remains reserved for Queue 167 `PlotRadiusIterator` where the root contract belongs.
- `pointXToPlotX(+width/2)` / Y analogue can produce `gridWidth`/`gridHeight`; `pointToPlot` then returns
  NULL on a flat map or wraps to column/row 0 on a wrapped map. There are no shipped Python callers and
  no current DLL caller outside the wrapper, while normal `plotXToPointX`/`plotYToPointY` center values
  never generate that exact positive landscape edge. Public API edge only.

Current `xDistance`/`yDistance` consumers inspected pass actual in-map plot coordinates, satisfying the
helpers' narrower assumptions.

5. Rebuild/read/ordinary ownership lifecycle — CLOSED beyond F632
-----------------------------------------------------------------
Current-format `CvMap::read()`/`write()` serialize matching field order. The removed historical
`uiFlag` compatibility branches are the intentional KI#46 current-version save policy. `reset()` calls
`uninit()`, removes the old plot array/area container/replay texture and reconstructs nonserialized A*
state through `setup()`; plot-extra reset behavior matches its current callers. BUG minimap settings are
intentionally persistent UI-option cache rather than per-map serialized game state.

The one independent ownership omission remains F632: `uninit()` does not delete the heap-owned final
`Shelf` objects. There is no second generic map-state leak established here.

6. Shelf pointer lifetime across live recomputation — CLOSED beyond F632
------------------------------------------------------------------------
Current `getShelves()` consumers in `CvGame` and `CvTeam` copy raw `Shelf*` values into stack-local
vectors and consume them synchronously inside barbarian/research calculations. `recalculateWaterAreas()`
/`computeShelves()` is not re-entered from those consumers. Therefore the KI#346 live Ice rebuild does
not leave a demonstrated cached local `Shelf*` dereference after deletion. F632 remains precisely a
final-cache ownership leak (and transient stale objects between `uninit()` and a later recomputation),
not a proven use-after-free root.

7. Coastal-city area-counter check — DEDUP / CLOSED, NOT F634
-------------------------------------------------------------
While tracing KI#346, current inherited `CvPlot::processArea` exposes this suspicious expression:

    if (iChange > 0 || (iChange < 0 &&
        pWaterArea->getCitiesPerPlayer(getOwner()) > 0, true))

The comma operator makes the parenthesized condition true whenever the outer negative-change branch is
entered, strongly suggesting the intended call had been
`getCitiesPerPlayer(getOwner(), true) > 0`. However Queue 009 `CvPlot.cpp` already audited this exact
root and classified it as dormant under supported live topology mutation: current wholesale area
rebuilds detach plots with `setArea(NULL, false)`, live water/land plot-type changes erase the city
first, and the SAS Ice repair rebuilds only water topology without invoking the dangerous decrement
shape. Do not duplicate it into Queue 036.

Likewise, KI#346 restoring only `cityPlot.waterArea(true)` is consistent with normal city-init area
bookkeeping: the city counter belongs to the canonical primary adjacent water area; `secondWaterArea()`
is a connectivity/query concept, not a second city-count ownership record. This is separate from F633,
which concerns city eligibility from a genuinely second sea.

8. Replay texture — CURRENT CONTROL, NOT YET A FINDING
------------------------------------------------------
`getReplayTexture()` safely returns NULL whenever the stored size differs from the expected minimap
pixel count, and `CvReplayInfo` falls back to the live minimap texture. Later-era setup schedules a
delayed `updateReplayTexture()`, while entering the configured era also captures it if absent.

`CvMap::write()` still forms `&m_replayTexture[0]` even when the vector is empty and then asks the stream
to write zero bytes. Forming that pointer is formally undefined in standard C++, and an empty cache is
a normal state before the replay snapshot exists / after `erasePlots()`. However no concrete MSVC 2003
release/debug runtime read, assertion, crash or serialization corruption has yet been demonstrated from
the zero-count write. Keep this as a precise robustness/UB control unless runtime/library evidence raises
it to the audit's current-live threshold.

9. `syncRandPlot`, `findCity`, `findWater`, Python-null boundaries — CLOSED/OWNED
-------------------------------------------------------------------------------
- Weighted `syncRandPlot` intentionally permits zero weights; `BarbarianWeightMap` uses zero as
  suppression and clamps live weights nonnegative. Exhaustive all-zero selection returning NULL is a
  defined outcome, not an arithmetic bug.
- `findCity` direction filtering uses `dxWrap`/`dyWrap`; ordinary callers supply valid coordinates.
  The distinct second-water omission is already F633.
- `CyMap::findCity` / `CyMap::findWater` dereference object arguments without top-level Python-`None`
  guards, but no shipped Python caller of these APIs was found; current native callers pass concrete
  plots/cities. Treat as API hardening, not F634.
- `findWater`'s current map-generator callers always pass a concrete plot.

10. Selected history fixes — RECONCILED
----------------------------------------
History-assisted checks did not uncover a surviving second root in the inspected semantic strata:
- practical 1965: safe `getPlot` substitution in bounded `syncRandPlot` coordinates;
- practical 2172: river counter converted to `short`;
- practical 2571: repaired the missing `else` in river-ID load overflow handling; current code is fixed;
- practical 2598: plot-count/isthmus caching changes paired with current refresh paths;
- practical 4007: shelf performance changes preserve recomputation ownership behavior (F632 teardown
  omission remains separately owned);
- practical 4030: area rebuild uses `setArea(NULL,false)` to avoid updating objects immediately being
  deleted and fixes separate lake-generation overflow.

CURRENT LEDGER / EXACT RESUME
-----------------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F631 / provisional KI#953 — CONFIRMED (wide custom-map-option locale/forwarding root).
F632 / provisional KI#954 — CONFIRMED (`Shelf*` final-cache ownership leak).
F633 / provisional KI#955 — CONFIRMED (dual-coast `findCity` / naval-upgrade area omission).
F634 / provisional KI#956 — UNUSED.

Exact continuation:
C031-WIP464 physical -> Queue 036 OPEN -> exclude F631/F632/F633 and all controls above -> continue
remaining `CvMap` public/index/area/replay/river/distance contracts and commit-history strata -> inspect
CyMap wrapper behavior only where shipped callers activate it -> promote only a distinct supported root
-> otherwise close Queue 036 conservatively -> freeze next physical WIP.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP464
===============================================================================

===============================================================================
C031-WIP465 — QUEUE 036 CvMap.cpp — F634 CACHED-LAKE NORMALIZATION REGRESSION
===============================================================================
Physical parent: C031-WIP464.
Audit target: `CvGameCoreDLL/CvMap.cpp` with the owning `CvArea`/`CvPlot`/normalization lifecycle.
Working source base: reconciled `AdvCiv-SAS_light_source_20260907T121108.zip`.
Gameplay source status: UNTOUCHED.

RECONCILED CURRENT STATE
------------------------
Queue 036 remains OPEN. F631/F632/F633 remain confirmed.
F634 / provisional KI#956 is CONFIRMED here.
F635 / provisional KI#957 is UNUSED.

F634 / PROVISIONAL KI#956 — INCREMENTALLY CREATED NORMALIZATION LAKES RETAIN
`m_bLake=false` UNTIL THE POST-NORMALIZATION WHOLE-MAP AREA REBUILD
--------------------------------------------------------------------------
CLASSIFICATION: inherited AdvC regression introduced by advc.030 cached lake state; current-live in
ordinary shipped map generation, with SAS's current normalization evaluator making the scoring
consequence especially explicit.

ROOT / CACHE CONTRACT
---------------------
K-Mod/BtS derive lake state directly from current area state:

    bool CvArea::isLake() const
    {
        return (isWater() && (getNumTiles() <= GC.getLAKE_MAX_AREA_SIZE()));
    }

AdvC practical 1274 / commit `b50e4a9a5a243f4f582213df1b07bae12dfb142a` (work in progress for
advc.030, "Ice can separate water areas") replaced that dynamic predicate with cached state:

    m_bLake = false;
    ...
    bool isLake() const { return m_bLake; }
    void updateLake(...);

Practical 1275 / commit `71fa4eef1a22df6b910bb41a5dc68d3ca9c3586d` completed advc.030 and
centralized whole-map lake refresh through `CvMap::updateLakes()`, but the inherited incremental
`CvArea::changeNumTiles()` logic was left in its old dynamic-predicate form:

    bool bOldLake = isLake();
    m_iNumTiles += iChange;
    if (bOldLake != isLake())
        ...

Once `isLake()` is cached, nothing between those two calls changes `m_bLake`; the old transition test
can no longer discover a size/membership-induced lake transition by itself.

CURRENT NORMALIZATION REPRODUCER
--------------------------------
`CvGame::normalizeAddLakes()` is a normal native normalization phase for standard shipped map scripts
such as Fractal, Continents and Pangaea (they do not override `normalizeAddLakes`). For a starting plot
that lacks fresh water it selects a legal land tile and executes:

    pLakePlot->setPlotType(PLOT_OCEAN);

`normalizeCanAddLakeTo()` explicitly rejects `kPlot.isCoastalLand()`. Therefore the selected land tile
is not adjacent to pre-existing water. In the normal `CvPlot::setPlotType(..., bRecalculate=true)`
local-update branch:

1. the land tile becomes water;
2. no adjacent water `CvArea` exists, so `pNewArea` remains NULL;
3. `CvMap::addArea()` creates a fresh `CvArea`;
4. `CvArea::init(true)` resets that area with `m_bLake=false` and zero tiles;
5. `setArea(pNewArea)` / `processArea(+1)` increments its tile count to 1 through
   `CvArea::changeNumTiles()`;
6. `changeNumTiles()` compares cached `isLake()` before/after the count mutation, sees false both
   times and never calls `updateLake()`;
7. neither `setArea()` nor the remainder of this incremental `setPlotType()` path calls
   `CvMap::updateLakes()` or `CvArea::updateLake()`.

The resulting physical topology is an isolated one-tile water area that should unquestionably be a
lake, but its live state remains:

    isWater() == true
    getArea().isWater() == true
    getArea().getNumTiles() == 1
    getArea().isLake() == false   // stale cached classification

The cache is repaired only later, after *all* start normalization is finished, when
`CvGame::setInitialItems()` executes the advc.030 whole-map `recalculateAreas(false)`, whose
`calculateReprAreas()` path ends in `updateLakes()`.

LIVE CONSUMERS RUN BEFORE THAT REPAIR
-------------------------------------
`CvGame::normalizeStartingPlots()` orders the phases as:

    normalizeAddRiver
    normalizeRemovePeaks
    normalizeAddLakes          <-- stale one-tile lake can be created here
    normalizeRemoveBadFeatures
    normalizeRemoveBadTerrain
    normalizeAddFoodBonuses
    normalizeAddGoodTerrain
    normalizeAddExtras

No `updateLakes()`/`recalculateAreas()` occurs between `normalizeAddLakes()` and those later scoring
and bonus/terrain phases.

Two current values are immediately wrong during that interval:

1. LAKE YIELD
   Current `CIV4YieldInfos.xml` gives `YIELD_FOOD` an `iLakeChange` of +1. `CvPlot::calculateNatureYield`
   adds that +1 only when `isLake()` is true. The newly created normalization water tile therefore
   evaluates one food too low until the post-normalization area rebuild.

2. FRESH WATER / FOUND VALUE
   `CvPlot::isFreshWater()` treats an adjacent `pAdj->isLake()` as fresh water. Because the new water
   tile says `isLake()==false`, the starting land plot continues to report no fresh water even though
   normalization has just deliberately created its fresh-water lake.

   `NormalizationTarget::isReached()` recomputes the current found value through its normalization
   `CitySiteEvaluator`. That evaluator is constructed with `bStartingLoc=false, bNormalize=true`, so
   current SAS's generic home river/fresh-water term applies while the separate starting-location-only
   bonus does not. Current SAS XML sets:

       SAS_EVALUATE_EXTRA_VALUE_HOME_FRESH_WATER_RIVER = 150

   Thus the stale classification suppresses a direct +150 home fresh-water term during normalization,
   in addition to the lake plot's missing +1 food contribution. (Earlier rough investigation mentioned
   +250; that was corrected before confirmation after tracing the evaluator flags. The +100
   `SAS_EVALUATE_STARTING_HOME_FRESH_WATER_VALUE` term is not used by the normalization evaluator.)

NORMALIZATION RESULT CAN DIVERGE
--------------------------------
The stale value is consumed as control flow, not merely logging:

- `normalizeAddFoodBonuses()` computes `bTargetReached = pTarget->isReached(*pStartingPlot)` and
  reduces/continues food-resource placement based on it; after each placed resource it recomputes the
  same target condition.
- `normalizeAddExtras()` checks `pTarget->isReached(*pStartingPlot)` before adding food/production
  features and again in later extra-placement loops.

Therefore the starting site can be treated as still below target after it has already received the
fresh-water lake that should raise its current score. A borderline site can receive an additional food
resource/feature/other normalization improvement that it would not have received if the just-created
lake had its correct classification. The later whole-map rebuild then flips the lake cache to true,
restoring the +1 lake food and fresh-water value *on top of* any extra normalization granted while the
cache was stale.

This is current shipped behavior under the native normalization path; no custom map data, runtime
Python mutation or unsupported topology is required.

LINEAGE / ORACLES
-----------------
- K-Mod 1.46 still computes `CvArea::isLake()` dynamically from `isWater()` and `getNumTiles()`, so the
  one-tile area is a lake immediately after its tile count becomes 1.
- AdvC practical 1274 introduced `m_bLake=false` + cached `isLake()` as part of the in-progress
  passable-area/ice work.
- AdvC practical 1275 retained the cache and centralized `updateLakes()` after whole area
  calculations, but did not add an incremental refresh for this `setPlotType()` path.
- Base AdvC 1.14 retains the same cached `isLake`, unchanged `changeNumTiles`, native
  `normalizeAddLakes`, and incremental `setPlotType` behavior.
- Current SAS inherits that root. SAS's added found-value home fresh-water term makes the temporary
  under-evaluation larger and especially easy to trace, but is not the root cause.

DEDUP
-----
This is NOT F249 / KI#572.

F249 concerns practical 4030's *batched map-generator* lake creation: multiple accepted lake plots were
converted without recalculating areas, leaving an earlier new water plot still attached to its stale
LAND `CvArea` while later generation consumers ran.

F634 begins with a different state and survives an F249 repair:
- `normalizeAddLakes()` uses ordinary per-plot recalculation;
- the new plot receives the correct fresh WATER `CvArea` immediately;
- the defect is specifically that this newly created water area's cached `m_bLake` remains false until
  the later whole-map update.

The earlier Queue-009 `m_bAnyIsthmus` and `m_bPotentialCityWork` cache controls are likewise separate:
those were held below current-live threshold and do not own lake classification or normalization
scoring.

REPAIR BOUNDARY
---------------
The required invariant is simple: a topology-changing incremental area update must not leave
`CvArea::isLake()` stale for live consumers.

For the narrow normalization repair, refresh lake classification before any phase after
`normalizeAddLakes()` consumes lake/fresh-water state; e.g. one `CvMap::updateLakes()` after the native
lake-creation loop would make all newly created normalization lakes coherent before food/terrain/extras
scoring.

A broader repair can instead restore incremental `m_bLake` maintenance when water-area membership/tile
count changes, but it must be designed around advc.030 representative-area construction so that full
area recalculation does not repeatedly classify partially built components. Do not simply call
`updateLake()` blindly from every `changeNumTiles()` during `calculateAreas()` without accounting for
that lifecycle.

Regression coverage:
1. standard Fractal/Continents/Pangaea-style native normalization;
2. start with no river/fresh water and at least one legal isolated lake candidate;
3. immediately after `normalizeAddLakes`, verify the created water area is `isLake()==true` and the
   start is `isFreshWater()==true`;
4. verify `calculateNatureYield(YIELD_FOOD)` on the lake includes `iLakeChange=+1` before later
   normalization phases;
5. compare `NormalizationTarget::isReached`/resulting food/extras placement with and without the
   refresh and ensure no extra compensation is granted solely because of stale lake state;
6. retain advc.030 Ice-separated representative-water behavior and F249/KI#572 batched generation
   behavior.

POST-F634 LEDGER / EXACT RESUME
-------------------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F631 / provisional KI#953 — CONFIRMED (wide custom-map-option locale/forwarding root).
F632 / provisional KI#954 — CONFIRMED (`Shelf*` final-cache ownership leak).
F633 / provisional KI#955 — CONFIRMED (dual-coast `findCity` / naval-upgrade area omission).
F634 / provisional KI#956 — CONFIRMED here (incremental normalization lake leaves cached
`m_bLake=false` through later normalization scoring).
F635 / provisional KI#957 — UNUSED.

Exact continuation:
C031-WIP465 physical -> Queue 036 OPEN -> exclude F631-F634 and all WIP464 controls -> continue the
remaining `CvMap` river/coordinate/index/replay/public/history strata, with Base AdvC/K-Mod/Civ4CE and
whole-album dedup before consuming F635 -> close Queue 036 conservatively if no distinct supported root
survives -> freeze the next physical WIP at the next settled tranche regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP465
===============================================================================

===============================================================================
C031-WIP466 — QUEUE 036 CvGameCoreDLL/CvMap.cpp — F635 / KI#957 CONFIRMED
===============================================================================

BASE / SCHEDULING STATE
-----------------------
The reconciled light-source ZIP `AdvCiv-SAS_light_source_20260907T121108.zip` remains the source base.
Queue 036 `CvGameCoreDLL/CvMap.cpp` remains OPEN and priority-next-WIP.
F631-F634 remain confirmed and are excluded from reuse. F635 / provisional KI#957 is consumed here.
No gameplay source was modified.

F635 / PROVISIONAL KI#957 — ADVc.153 HUMAN UNIT-CYCLE STARTUP PENALIZES FULLY-READY
GROUPS INSTEAD OF THE PARTIALLY-READY GROUPS ITS OWN COMMENT SAYS SHOULD BE LAST
--------------------------------------------------------------------------------

CURRENT CODE
------------
`CvMap::findSelectionGroup` currently contains:

    bool const bAIControl = pLoopSelectionGroup->isAIControlled(); // advc.153
    if (bReadyToSelect && !pLoopSelectionGroup->readyToSelect(
        !bAIControl)) // advc.153: was false
    {
        continue;
    }
    ...
    int iValue = plotDistance(iX, iY,
            pLoopSelectionGroup->getX(), pLoopSelectionGroup->getY());
    /*  <advc.153> Select groups where only some units are ready last
        (unless plotDistance is 0) */
    if (!bAIControl && pLoopSelectionGroup->readyToSelect())
        iValue *= 100; // </advc.153>

`CvSelectionGroup::readyToSelect(bool bAny=false)` forwards to `readyToMove(bAny)`:

    return (readyToMove(bAny) && !isAutomated());

and `readyToMove` uses:

    (bAny ? canAnyMove() : canAllMove())

Therefore for a normal human group:

* `readyToSelect(true)` means at least one member can move;
* `readyToSelect()` / `readyToSelect(false)` means all members can move.

The first advc.153 filter intentionally broadens human cycling so partially-ready groups remain
eligible. But the immediately following block says that groups where only SOME units are ready should
be selected LAST, while testing the opposite state: `readyToSelect()` is true for the FULLY-ready
group. The 100x distance penalty is consequently applied to fully-ready human groups instead.

DETERMINISTIC LIVE RANKING INVERSION
------------------------------------
The live caller `CvGame::getNextGroupInCycle` uses this function when no unit is currently selected:

    pCycleUnit = gDLL->UI().getHeadSelectedUnit();
    ...
    if (pCycleUnit == NULL)
    {
        CvPlot* pPlot = gDLL->UI().getLookAtPlot();
        pGroup = GC.getMap().findSelectionGroup(
                pPlot != NULL ? pPlot->getX() : 0,
                pPlot != NULL ? pPlot->getY() : 0,
                getActivePlayer(), true, bWorkers);
    }

That path feeds both `cycleSelectionGroups` and the advc.154 cycle-button resolver. It is ordinary
human UI behavior, not a Python-only or debug path.

Take two eligible non-automated human groups while no unit is selected:

A. Fully-ready group at plotDistance 1 from the current look-at plot.
   - `readyToSelect(true)` = true
   - `readyToSelect(false)` = true
   - current `iValue = 1 * 100 = 100`

B. Partially-ready group at plotDistance 2, with at least one member able to move and at least one
   member out of moves.
   - `readyToSelect(true)` = true, so advc.153 deliberately keeps it in the cycle
   - `readyToSelect(false)` = false
   - current `iValue = 2`

Current code therefore selects B (2 < 100), although the local contract explicitly says that B's
class — “groups where only some units are ready” — should be selected last. With the intended polarity,
A remains value 1 and B receives the penalty (2 * 100), so A is selected first.

This is not a tie/order artifact. A two-tile partially-ready group beats a one-tile fully-ready group
deterministically. More generally, a partially-ready group wins over a fully-ready group unless it is
roughly 100 times farther away. The source's parenthetical “unless plotDistance is 0” is also consistent
with a multiplicative penalty intended for the partially-ready group: a partial group on the exact
look-at tile remains value 0 and can sensibly win despite the preference.

PLAYER-FACING CONSEQUENCE
-------------------------
AdvC manual change 153 explicitly adds partially-ready human groups to unit cycling so that the player
is made aware of unspent movement points while avoiding unnecessary group splitting. The current
ranking inversion does not merely include those groups; when cycling starts with no current selection,
it strongly prioritizes them over nearby fully-ready groups. The cycle button/hotkey can therefore
jump first to a mixed spent/unspent stack even when a closer group has all movement available — the
opposite of the ordering stated in the implementation itself.

LINEAGE
-------
K-Mod's inherited `CvMap::findSelectionGroup` required `readyToSelect()` (all units ready) and had no
partial-group preference because partially-ready groups were not eligible at all.

AdvC practical 2799 refactored the old condition to an equivalent explicit
`readyToSelect(false)`.

AdvC practical 2810 / commit `9ffb0d869861f50784f03f968fc8acd6a471460f`, titled
“Merge branch split-groups-after-missions” and identified as change `advc.153`, made both relevant
changes in the same hunk:

    bool const bAIControl = pLoopSelectionGroup->AI_isControlled();
    if (bReadyToSelect && !pLoopSelectionGroup->readyToSelect(!bAIControl))
        continue;
    ...
    /* Select groups where only some units are ready last
       (unless plotDistance is 0) */
    if (!bAIControl && pLoopSelectionGroup->readyToSelect())
        iValue *= 100;

For human groups `!bAIControl` is true, so the eligibility test became `readyToSelect(true)` as
intended. The newly added ranking test, however, retained the default all-ready meaning instead of
negating it. This is therefore an AdvC practical-2810 polarity bug introduced with the feature itself,
not a later SAS regression.

Practical 2821 later changed only the worker filter to `hasWorkerWithMoves()`. Practical 4198 renamed
`AI_isControlled` to `isAIControlled`. Neither changes the inverted readiness predicate.

Supplied Base AdvC 1.14 retains the same executable body. Current SAS is executable-semantically
identical in this function apart from formatting/source-comment changes.

DEDUP
-----
No reconciled album or current Known Issues entry owns this `CvMap::findSelectionGroup` readiness-
ranking polarity root. Existing unit-cycle findings concern distinct lifetime, stale group-cycle ID,
selection-popup rebinding, cargo cycling, and `advc.153` movement/regrouping behavior. In particular:

* F155/KI#478 and F156/KI#479 are live-list lifetime hazards during `advc.153` movement/combat;
* the previously audited group-cycle stale-ID controls establish container lifetime, not ranking;
* Rapid Unit Cycling popup findings concern actor/selection rebinding after cycling, not which ready
  group `findSelectionGroup` chooses when cycling begins without a current selection.

F635 is thus a separate ordering/polarity defect in the map-level nearest-group selector.

REPAIR BOUNDARY
---------------
The intended invariant is directly stated by the source comment: when `bReadyToSelect` is requested for
human cycling, groups where only some units can move remain eligible but should rank after fully-ready
groups unless they are on the exact look-at plot.

A narrow repair is to apply the penalty to the partially-ready state rather than the fully-ready state,
and to scope the special ranking to the ready-to-select search:

    if (bReadyToSelect && !bAIControl &&
        !pLoopSelectionGroup->readyToSelect())
    {
        iValue *= 100;
    }

The existing earlier filter has already guaranteed `readyToSelect(true)` for human candidates, so
`!readyToSelect(false)` at this point means exactly “some can move, but not all.” Keeping the
`bReadyToSelect` guard also avoids changing the generic public/Python behavior when callers explicitly
ask `findSelectionGroup` to consider groups regardless of readiness.

Regression coverage:
1. human player, no current selected unit, current look-at plot fixed;
2. group A fully ready at distance 1;
3. group B partially ready at distance 2;
4. cycle button / ordinary cycle command should choose A first;
5. after A is cycled/handled, B remains eligible because `readyToSelect(true)` is true;
6. partial group exactly at distance 0 should retain the source's documented exception;
7. worker-cycle mode should still require `hasWorkerWithMoves()`;
8. AI-controlled groups retain the all-ready eligibility/ranking behavior.

POST-F635 CONTROLS / QUEUE STATUS
---------------------------------
1. Current SAS vs supplied Base AdvC 1.14 function-body comparison found no missing upstream `CvMap`
   semantic fix: all 85 common bodies are semantically identical after comment/format normalization
   except deliberate SAS `read()` and `setupGraphical()` changes; SAS-only `recalculateWaterAreas`
   and `resetRiverIDs` are already-owned repairs. F635 is therefore an inherited root still present in
   Base 1.14, as the practical-2810 lineage independently shows.
2. `setLatitudeLimits` has only current TrueStarts callers; all pass ordered, in-range limits already
   audited with Queue 022.
3. WB `CyMap.rebuild` receives explicit WB dimensions/latitudes/wraps and the WB loader explicitly
   recalculates areas after applying plot types. No independent rebuild-state defect found here.
4. `plotByIndexExternal` / shipped Python index loops remain bounded. Generic invalid-index behavior is
   API hardening, not a current finding.
5. Practical 2172 originally had an out-of-range river-read guard accidentally overwritten by an
   unconditional cast, but practical 2571 explicitly repaired it with the current `else`; this is a
   historical fixed control, not a live finding.

POST-F635 LEDGER / EXACT RESUME
-------------------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F631 / provisional KI#953 — CONFIRMED.
F632 / provisional KI#954 — CONFIRMED.
F633 / provisional KI#955 — CONFIRMED.
F634 / provisional KI#956 — CONFIRMED.
F635 / provisional KI#957 — CONFIRMED here (advc.153 human unit-cycle readiness ranking polarity).
F636 / provisional KI#958 — UNUSED.

Exact continuation:
C031-WIP466 physical -> Queue 036 OPEN -> exclude F631-F635 and all WIP464/WIP465 controls -> continue
remaining inherited `CvMap` semantic-history/public/index/replay/area/path contracts, using K-Mod,
Civ4CE, Base AdvC 1.14 and whole-album dedup before consuming F636 -> freeze the next physical WIP at
the next settled tranche regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP466
===============================================================================

===============================================================================
C031-WIP467 -- QUEUE 036 CvMap.cpp: STOCK CITY-SCENARIO ICE TOPOLOGY / F636
Date: 2026-09-07
STATUS: INCOMPLETE SAFETY CHECKPOINT -- CvMap.cpp remains OPEN
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Parent physical stone: C031-WIP466.
- Working source base: reconciled `AdvCiv-SAS_light_source_20260907T121108.zip`.
- Queue 036 `CvGameCoreDLL/CvMap.cpp` remains OPEN (priority-next-WIP).
- F631/KI#953 through F635/KI#957 remain CONFIRMED from prior physical stones.
- F636 / provisional KI#958 is CONFIRMED here.
- F637 / provisional KI#959 is UNUSED.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.

-------------------------------------------------------------------------------
F636 / provisional KI#958 -- city-bearing WorldBuilder scenarios retain the
pre-feature water-area partition, so stock Earth1000AD ignores Ice barriers in
PASSABLE_AREAS topology for gameplay
-------------------------------------------------------------------------------

STATUS
------
NEW CURRENT DEFECT ROOT, inherited AdvC scenario/PASSABLE_AREAS initialization
interaction; stock BtS Earth1000AD reproducer.

ROOT / LOAD ORDER
-----------------
Current WorldBuilder map application deliberately performs area calculation before
features are applied:

    CvWBDesc.applyMap():
      ... rebuild map ...
      pDesc.preApply() for every plot    // plot type + terrain
      CyMap().recalculateAreas()
      pDesc.apply() for every plot       // rivers/start/bonus/improvement/FEATURE/route

`CvPlotDesc.preApply()` sets PlotType/TerrainType only. `CvPlotDesc.apply()` later
calls `plot.setFeatureType(...)`, so `FEATURE_ICE` is absent when that preliminary
area partition is built.

That ordering matters because current/default `PASSABLE_AREAS=1` deliberately makes
Ice part of water-area topology. `CvMap::calculateAreas_dfs()` skips impassable roots
on its first pass and does not traverse from an impassable plot back into passable
water, allowing Ice packs to separate passable seas.

Ordinary newly generated games repair the preliminary partition after Ice/normalization
through `CvGame::setInitialItems()` -> `GC.getMap().recalculateAreas()`.
Scenarios do not call `setInitialItems`; they enter `CvGame::initScenario()` instead.
Current `initScenario()` explicitly refuses the post-feature rebuild whenever any major
player already has a city:

    if (GC.getDefineBOOL(CvGlobals::PASSABLE_AREAS))
    {
        bool bRecalc = true;
        for (...)
        {
            if (GET_PLAYER(...).getNumCities() > 0)
                bRecalc = false;
        }
        if (bRecalc)
            GC.getMap().recalculateAreas();
    }

The source comment explains the safety concern: wholesale `recalculateAreas()` was not
trusted around preplaced scenario cities/free units. The consequence is nevertheless
that city-bearing scenarios keep the earlier pre-Ice area identities for gameplay.

STOCK CURRENT-LIVE REPRODUCER
-----------------------------
The supplied Civ4CE BtS archive contains the stock public scenario
`BtS/PublicMaps/Earth1000AD.CivBeyondSwordWBSave`, a scenario usable by BtS mods.
Its map data provides a concrete supported fixture:

- grid: 124 x 68, wrap X = 1, wrap Y = 0;
- 69 preplaced cities;
- 537 `FEATURE_ICE` plot entries.

The 69 cities deterministically disable `initScenario()`'s post-feature
`recalculateAreas()` branch.

Reconstructing Earth1000AD's water graph from the WB plot data with current
`CvMap::isSeparatedByIsthmus` semantics shows that Ice is not topologically inert.
Without Ice, several high-Arctic non-Ice waters belong to the giant ocean component.
When Ice is treated as impassable, that same old giant component is split; concrete
passable pockets split out include:

- 16 tiles around `(12,64)` ... `(21,62)`;
- 9 tiles around `(95,65)` ... `(101,66)`;
- 5 tiles around `(5,66)` ... `(8,66)`;
- further 2-tile pockets in the same old ocean component.

For reference, the largest non-Ice water connectivity component is 5262 tiles when
features are ignored and 4775 passable tiles when Ice barriers are honored. The exact
CvArea tile totals also include the barrier Ice assigned to one side, but the decisive
fact is the changed connectivity of ordinary non-Ice water plots: plots in the pockets
and plots in the main sea are in one preliminary area and in different correct
PASSABLE_AREAS after features.

HARD NATIVE CONTRACT / CONSEQUENCE
----------------------------------
This is not merely stale diagnostic metadata. `CvArea::canBeEntered` uses area identity
as its strongest reachability shortcut:

    if (getID() == kFrom.getID())
        return true;

For two distinct water areas separated only by Ice, the correct post-Ice topology has
different area IDs but the same representative BtS water body. The subsequent test then
returns false for an ordinary sea unit that cannot move through impassable tiles:

    if (isWater() == kFrom.isWater() &&
        (m_iRepresentativeArea != kFrom.m_iRepresentativeArea ||
         (u != NULL && !u->canMoveImpassable())))
        return false;

Thus Earth1000AD's stale pre-feature partition changes this coarse naval reachability
answer from the intended FALSE to TRUE for ordinary ships across those Ice barriers.
Current AI/unit code deliberately consumes `CvArea::canBeEntered` and water-area identity
as coarse reachability/accounting filters before or alongside pathfinding. The scenario
therefore starts with a topology contract that contradicts the actual feature-level
passability until some later unrelated full area rebuild happens (if one ever does).

LINEAGE
-------
AdvC practical 1274 / commit `b50e4a9a5a...` introduced Ice-sensitive PASSABLE_AREAS
and explicitly noted that at least the Earth_IceAge scenario places Ice after the first
`CvMap::calculateAreas` call. It initially attempted a scenario recalculation from
`setInitialItems`.

Practical 1275 / `71fa4eef1a...` moved the ordinary post-Ice recalculation to after
starting-position normalization and documented that some scenarios do not call
`setInitialItems`.

Practical 1347 / `1cfdc903de...` then added the current preplaced-city bailout in
`initScenario`, explicitly because full `recalculateAreas()` was considered unsafe with
preplaced cities/other scenario state. Later refactors preserve this behavior. Supplied
Base AdvC 1.14 retains the same logic; current SAS inherits it.

So this is a deliberate safety workaround with an unintended correctness cost, not a
SAS formatting regression. The stock Earth1000AD data proves the excluded domain is real.

DEDUP
-----
This is independent of the earlier area/Ice findings:

- F025 / KI#346: topology becomes stale when Ice passability changes *during gameplay*
  (notably global-warming melt) after areas were initially correct. Current SAS repairs
  that mutation path with deferred `resetPathDistance`, `recalculateWaterAreas`, and
  plot-group refresh.
- F249 / KI#572: batched map generation can leave stale area pointers while map plots are
  being constructed; different lifecycle/root.
- F634 / KI#956: newly added normalization lakes temporarily retain stale cached `m_bLake`;
  lake classification/value cache, not scenario Ice-area partition.

No reconciled album/Known-Issues owner was found for the load-time city-bearing scenario
case proved here. F636 therefore owns only the missing safe post-feature water-area rebuild
for scenarios that contain cities.

REPAIR BOUNDARY
---------------
Do not simply remove the historical city guard and call wholesale `recalculateAreas()`;
the source explicitly records why that was avoided.

Current SAS now has a much narrower helper introduced for KI#346:
`CvMap::recalculateWaterAreas()`. It:

1. detaches/deletes only water CvAreas;
2. leaves land/city area state intact;
3. rebuilds water areas through `calculateAreas_dfs()`;
4. runs `calculateReprAreas()` -> `updateLakes()` -> `computeShelves()`;
5. restores coastal-city counts on the newly built primary water areas.

That helper is therefore the natural repair candidate for the city-bearing scenario branch
after WB features have been applied. A scenario-start repair should also review whether any
path-distance / plot-group cache can already exist at that exact initialization point; if so,
mirror KI#346's invalidation as needed rather than broadening the rebuild indiscriminately.

Regression coverage:
1. load stock Earth1000AD with `PASSABLE_AREAS=1`;
2. preserve every preplaced city/unit and its land-area bookkeeping;
3. after scenario initialization, non-Ice Arctic pocket `(12,64)` and the main ocean should
   have different water-area IDs where Ice blocks passage;
4. their representative water-area IDs should still match as the same BtS ocean body;
5. `CvArea::canBeEntered` for an ordinary non-Ice-capable ship across those areas should be
   false, while an Ice-capable/submarine-style permitted unit retains the intended exception;
6. stock Earth_IceAge (no preplaced cities) should remain correct through its existing full
   scenario recalculation path;
7. scenarios without Ice should remain behaviorally unchanged.

POST-F635 / PRE-F636 CONTROLS PRESERVED
---------------------------------------
1. Older `CvMap` semantic-history strata checked after WIP466 mostly validate current code:
   practical 2718's `findCity` precedence fix and practical 4030's full-rebuild safety fix are
   present; old synchronized-random and out-parameter refactors remain paired.
2. `CvMap::init` still contains an inherited malformed diagnostic format fragment ending in
   `num custom options=%6`. This is a real malformed printf-style logging expression and may
   cause formatting retries/empty output, but no gameplay-state corruption was established;
   retained below finding threshold rather than consuming a root number.
3. The scenario investigation began from `calculateAreas()`'s own candid comment that some
   scenarios receive only the preliminary land/sea/peak partition. F636 is promoted only
   because stock Earth1000AD proves city+Ice activation and because the stale partition flips
   a concrete `CvArea::canBeEntered` reachability result.

POST-F636 LEDGER / EXACT RESUME
-------------------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — OPEN (priority-next-WIP).
F631 / provisional KI#953 — CONFIRMED.
F632 / provisional KI#954 — CONFIRMED.
F633 / provisional KI#955 — CONFIRMED.
F634 / provisional KI#956 — CONFIRMED.
F635 / provisional KI#957 — CONFIRMED.
F636 / provisional KI#958 — CONFIRMED here (city-bearing WB scenarios skip the post-feature
Ice-aware water-area rebuild; stock Earth1000AD reproducer).
F637 / provisional KI#959 — UNUSED.

Exact continuation:
C031-WIP467 physical -> Queue 036 OPEN -> exclude F631-F636 plus all recorded negative controls ->
continue the remaining retained `CvMap` whole-body/history pass (area/path/replay/map option/random
plot/index/serialization/public API) and whole-album dedup before F637 -> if no new hard root survives,
move toward formal Queue036 closure -> freeze the next physical WIP regardless at the next settled
boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP467
===============================================================================

===============================================================================
C031-WIP468 -- QUEUE 036 CvMap.cpp DURABLE CLOSURE / NEXT PRIORITY QUEUE 057
Date: 2026-09-07
STATUS: QUEUE 036 COMPLETE; Queue 057 CombatOdds.cpp next priority-next-WIP file
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Parent physical stone: C031-WIP467.
- Working source base: reconciled `AdvCiv-SAS_light_source_20260907T121108.zip`.
- Queue 036 `CvGameCoreDLL/CvMap.cpp` is now COMPLETE.
- F631/KI#953 through F636/KI#958 remain CONFIRMED.
- F637 / provisional KI#959 remains UNUSED; no seventh Queue-036 root was forced.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.

QUEUE 036 FINAL RESULT
----------------------
Six independent current defect roots survive the full current-body, companion-header,
SAS-delta, Base-AdvC-1.14, K-Mod/Civ4CE lineage, WorldBuilder/scenario and history-assisted
reconciliation:

1. F631 / KI#953 -- the translated-string `isCustomMapOption(CvWString,...)` overload
   narrows non-ASCII text and drops both Boolean forwarding controls; current French Balanced
   detection is a shipped live reproducer.
2. F632 / KI#954 -- final `Shelf*` objects owned by `m_shelves` leak at CvMap teardown because
   recomputation deletes old shelves but `uninit()`/destruction does not delete the final cache.
3. F633 / KI#955 -- `findCity(...sameArea,coastalOnly...)` ignores a city's second water area,
   making ordinary naval `canUpgrade` disagree with exhaustive upgrade-city search on dual coasts.
4. F634 / KI#956 -- normalization-created lakes keep cached `m_bLake=false` until the later full
   area rebuild, so later normalization misses lake food/fresh-water value and can over-normalize.
5. F635 / KI#957 -- advc.153 human group cycling penalizes fully-ready groups instead of the
   intended partially-ready groups because `readyToSelect()` polarity is inverted in ranking.
6. F636 / KI#958 -- city-bearing WorldBuilder scenarios skip the post-feature Ice-aware water-area
   rebuild; stock Earth1000AD starts gameplay with pre-Ice PASSABLE_AREAS topology.

FINAL NEGATIVE / DEDUP CONTROLS
-------------------------------
The following attractive shapes were explicitly reconciled and are NOT additional Queue-036 roots:

- GroupPathFinder heuristic setup is safe under current route XML; movement-cost minima are real and
  the pathfinder clamps remain admissible.
- StepFinder reuse is correctly invalidated on shipped topology mutations: `setPlotType` resets it;
  current runtime Ice melt is already KI#346 and explicitly resets path distance.
- `calculateReprAreas()`'s defensive 10-pass cap did not reproduce: exhaustive small and larger
  randomized legal passability patterns converge in the first changing scan, with the next pass only
  confirming no changes.
- `coordRange` has the inherited exact-negative-multiple modulo oddity, but current local-offset
  callers stay within the supported span; reserve generic wrapped-iterator consequences for Queue167.
- `pointToPlot`'s exact positive landscape-edge mapping is a public API boundary with no shipped
  engine/DLL producer reaching the edge coordinate.
- current-format read/write ordering is paired; historical `uiFlag` branches removed by SAS belong to
  deliberate KI#46 current-version-only save compatibility policy.
- SAS `recalculateWaterAreas`, GameRecord quickload hook and `resetRiverIDs` reconcile to KI#346,
  KI#382 and KI#567 respectively; no duplicate roots.
- inherited `CvPlot::processArea` comma-expression bug was already audited as a dormant Queue-009
  control and is not re-owned here.
- replay serialization's `&m_replayTexture[0]` on a zero-length vector remains formal C++ UB, but the
  stream count is zero and no MSVC-2003 crash/read/corruption was established; held below threshold.
- `syncRandPlot` weighted zero weights are intentional suppression; its limited-trial `piValidCount=1`
  is explicitly only a success indicator and no live consumer treats it as exhaustive cardinality.
- `findWater`, `xDistance`, `yDistance`, `plotNum`, `directionXY`, `plotCity` and other header helpers
  have current shipped callers satisfying their documented coordinate/adjacency/enum preconditions;
  malformed external/Python arguments remain hardening territory.
- short river ID capacity is safe on the largest shipped SAS world (198x140 = 27,720 plots, below
  MAX_SHORT); custom super-map overflow remains robustness only.
- the Ice/lake-yield transition hypothesis was already closed in earlier CvPlot/CvCity work through
  representative-water-body lake classification; no duplicate.
- `setPlotExtraYield(PlotNumTypes,...)` does not refresh plot yield, but no current live caller uses that
  compatibility overload; current Python/event paths use the CvPlot overload that calls updateYield.
- the inherited `CvMap::init` diagnostic format ending in `num custom options=%6` is malformed and can
  damage that memory-log line / cause format retries, but no gameplay-state consequence was proven;
  retained as low-grade diagnostic/performance debt rather than consuming F637.
- SAS vs supplied Base AdvC 1.14 function-body normalization found no missed later upstream CvMap fix:
  common executable bodies match except deliberate SAS read/setupGraphical differences and owned SAS
  repair helpers.
- final whole-body divisor/pointer/index/enum/cache scan found no additional supported current root.

WHY QUEUE 036 IS CLOSED
-----------------------
The audit covered all 89 retained CvMap.cpp functions plus the behaviorally relevant CvMap.h inline
surface, current SAS-only semantic deltas, the full path-specific history inventory, supplied Base
AdvC 1.14 comparison, K-Mod/Civ4CE oracle checks, all current custom-map-option/WorldBuilder scenario
load paths needed for the surviving findings, and a final mechanical pointer/division/index/cache
pass. Every surviving hard current defect has an owner F631-F636; remaining suspicious shapes are
explicitly recorded above as fixed, dormant, unsupported-input hardening or below-threshold debt.

PRIORITY SCHEDULING RESOLUTION
------------------------------
The reconciled album-front rule says to process every still-PENDING entry explicitly marked
`priority next wip` before returning to ordinary queue order, following queue order among equally
marked files. With Queue036 complete, the next still-pending priority marker is:

    Queue 057 -- CvGameCoreDLL/CombatOdds.cpp -- 713 lines

Queue058 ArmamentForecast.cpp and the later priority-marked files follow only after Queue057 closes.
Ordinary Queue026 and other entries explicitly marked “skip until the priority next wip ones have been
done first” remain intentionally deferred.

LEDGER / EXACT RESUME
---------------------
Queue 036 `CvGameCoreDLL/CvMap.cpp` — COMPLETE.
F631 / provisional KI#953 — CONFIRMED.
F632 / provisional KI#954 — CONFIRMED.
F633 / provisional KI#955 — CONFIRMED.
F634 / provisional KI#956 — CONFIRMED.
F635 / provisional KI#957 — CONFIRMED.
F636 / provisional KI#958 — CONFIRMED.
F637 / provisional KI#959 — UNUSED.

Exact continuation:
C031-WIP468 physical -> Queue036 COMPLETE -> open priority Queue057 `CvGameCoreDLL/CombatOdds.cpp` ->
use F637/KI#959 as the next unused root -> first reconcile current SAS deltas + K-Mod/AdvC lineage +
album/KI ownership, then perform whole-file arithmetic/probability/index/edge-case audit -> freeze a
new physical WIP at the next settled tranche regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
END C031-WIP468
===============================================================================


===============================================================================
C031-WIP469 -- QUEUE 057 CombatOdds.cpp / F637 CONFIRMED SAFETY CHECKPOINT
Date: 2026-09-07
STATUS: Queue 057 OPEN; F637/KI#959 CONFIRMED; F638/KI#960 UNUSED
===============================================================================

PHYSICAL CONTINUATION
---------------------
- Parent physical stone: C031-WIP468.
- Working source base: reconciled `AdvCiv-SAS_light_source_20260907T150520.zip`.
- Supplied local comparison trees: Base AdvC 1.14, K-Mod 1.46, Civ4CE.
- Queue 057 `CvGameCoreDLL/CombatOdds.cpp` is OPEN.
- F637 / provisional KI#959 is CONFIRMED here.
- F638 / provisional KI#960 is UNUSED.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.

-------------------------------------------------------------------------------
F637 / provisional KI#959 -- `calculateCombatOdds(..., bHideFreeWins)` ignores
its Boolean argument, so AdvC's combat-result odds can hide Barbarian free wins
even when the caller explicitly requests the real executor odds
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED current inherited AdvC defect; live and player-visible with the normal
`SHOW_ODDS_IN_COMBAT_MESSAGES` path. Base AdvC 1.14 retains it unchanged.

CURRENT ROOT
------------
`CombatOdds.h` exposes:

    int calculateCombatOdds(
        CvUnit const& kAttacker,
        CvUnit const& kDefender,
        bool bHideFreeWins = true);

`CombatOdds.cpp` receives the parameter but currently initializes through:

    int iOdds = setupCombatants(kAttacker, kDefender, att, def);

The nested helper itself has the intended forwarding-capable signature:

    int setupCombatants(..., bool bHideFreeWins = true)

and forwards that value correctly to `setupCombatantsImpl`. The public wrapper simply
omits the argument, so C++ default-argument substitution forces `true` at that call
site regardless of the caller's requested value.

LIVE ACTIVATION
---------------
`CvUnit::resolveCombat()` deliberately requests the opposite behavior:

    m_iAttackOdds = calculateCombatOdds(*this, *pDefender,
            false); // Do reveal free wins vs. Barbarians

That value is cached in `m_iAttackOdds`, adjusted for withdrawal chance, and later used
by the combat-result message when `SHOW_ODDS_IN_COMBAT_MESSAGES` is enabled. This is
therefore not a dormant public-API parameter.

EXECUTOR ORACLE
---------------
Actual combat obtains its per-round roll through `CvUnit::getDefenderCombatValues()`.
When a non-Barbarian player with free wins remaining attacks a Barbarian, that function
caps the Barbarian's per-round hit chance at 10% (and therefore gives the attacker at
least 90%). `setupCombatantsImpl` contains the matching free-win adjustment, but only
inside `if (!bHideFreeWins)`.

The intended public call with `false` should therefore match the executor. Because the
argument is dropped one layer above, it instead calculates from the unmodified unit
strengths.

DETERMINISTIC STOCK FIXTURE
---------------------------
Use otherwise equal-strength, full-health, no-first-strike units, with a normal player
still below `getFreeWinsVsBarbs()` attacking a Barbarian.

Supplied stock BtS data has:
- `COMBAT_DAMAGE = 20`;
- `MAX_HIT_POINTS = 100`;
- `COMBAT_DIE_SIDES = 1000`;
and the current SAS tree contains no override of those inherited defines.

Equal strength therefore means five successful hits are required on each side.

Without the free-win rule:
- attacker per-round hit chance = 0.5;
- probability of scoring five hits before the defender scores five = exactly 0.5;
- current LFB calculation returns 500 permille.

With the executor's free-win rule:
- attacker per-round hit chance = 0.9;
- exact probability of scoring five hits before five defender hits is
  sum(C(4+k,k) * 0.9^5 * 0.1^k, k=0..4) = 0.99910908;
- current LFB interval/rounding implementation returns 999 permille.

Thus the live `calculateCombatOdds(..., false)` path can retain/display 500 permille
for a battle that the actual executor resolves at about 999.1 permille (999 in the
current integer predictor). The discrepancy is large and deterministic.

BIRTH / LINEAGE PROOF
---------------------
AdvC practical 4003 / commit-diff mirror `AdvCivPreSAS_4003_8128c2bc0a.diff`, titled
"Further amend attacker odds messages," introduced all relevant pieces together:
1. `setupCombatants` gained `bHideFreeWins` and correctly forwarded it to the lower
   implementation;
2. `calculateCombatOdds` gained the same new Boolean parameter;
3. `CvUnit::resolveCombat` began calling `calculateCombatOdds(..., false)` with the
   explicit comment "Do reveal free wins vs. Barbarians";
4. but the body of `calculateCombatOdds` was not changed to pass that new parameter to
   `setupCombatants`.

The defect therefore exists from the feature's introduction rather than from later SAS
drift. Supplied Base AdvC 1.14 still has the same omitted argument. Current SAS
`CombatOdds.cpp` is executable-semantically identical to Base AdvC 1.14 after
comment/whitespace normalization, so SAS inherited the defect.

INTERNAL CROSS-CHECK
--------------------
`combat_odds::initCombatants(...)` does pass its `bHideFreeWins` argument through to
`setupCombatantsImpl<true>`. ACO's `getCombatOddsSpecific` reaches that path and supplies
its `ACO__IgnoreBarbFreeWins` option. The free-win implementation itself is therefore
not missing; the fault is localized to the public `calculateCombatOdds` forwarding
layer.

WHOLE-ALBUM DEDUP
-----------------
A whole-album search finds no prior finding mentioning `bHideFreeWins`, free-win
visibility, `IgnoreBarbFreeWins`, `SHOW_ODDS_IN_COMBAT_MESSAGES`, or `m_iAttackOdds` as
this root.

The closest existing CombatOdds-related owner is F602/KI#925, but F602 concerns SAS's
Janissary `iCombatLimit=110` exceeding max hit points and creating executor/predictor
combat-limit disagreement. F637 is independent: ordinary lethal combat, normal hit
limits, and a dropped free-win-visibility argument.

REPAIR BOUNDARY
---------------
The narrow repair is simply to forward the public parameter at the first setup call:

    int iOdds = setupCombatants(
            kAttacker, kDefender, att, def, bHideFreeWins);

For diagnostic `MONTE_CARLO_ODDS_TEST` consistency, the later repeated setup call in that
conditional block should use the same explicit `bHideFreeWins` value as well if the test
is ever enabled. No combat-resolution rule needs redesigning.

VALIDATION TARGETS
------------------
1. Equal-strength normal attacker vs. Barbarian with free wins remaining:
   `calculateCombatOdds(..., false)` should return about 999 permille, not 500.
2. The same call with `true` should continue to hide the free-win adjustment and return
   the underlying 500 permille in the equal-strength fixture.
3. Exhausted free wins should make true/false identical.
4. Reverse Barbarian-attacker / protected normal-defender case should honor the mirrored
   10/90 cap already present in `setupCombatantsImpl`.
5. ACO behavior should remain unchanged because its `initCombatants` path was already
   forwarding correctly.
6. Ordinary non-Barbarian combats should remain unchanged.
7. Combat-result messages with `SHOW_ODDS_IN_COMBAT_MESSAGES` enabled should now match
   the executor's Barbarian free-win rule before withdrawal adjustment.

QUEUE-057 BASELINE PRESERVED
----------------------------
- Current SAS `CombatOdds.cpp` is executable-semantically identical to supplied Base
  AdvC 1.14 after comments/whitespace normalization.
- The path-specific history inventory contains nine substantive AdvC commits touching
  `CombatOdds.cpp` (2843, 2851, 2852, 2854, 2855, 2998, 3864, 3900, 4003) plus SAS's
  later formatting/history mirror 5961.
- F637 is the first surviving current Queue-057 root.

POST-F637 LEDGER / EXACT RESUME
-------------------------------
Queue 057 `CvGameCoreDLL/CombatOdds.cpp` -- OPEN.
F637 / provisional KI#959 -- CONFIRMED here (ignored `bHideFreeWins` forwarding).
F638 / provisional KI#960 -- UNUSED.

Exact continuation:
C031-WIP469 physical -> Queue057 OPEN -> exclude F637 plus all prior album owners ->
inspect the LFB interpolation/cache boundary near attacker odds 1000 first (the prior
handoff specifically flagged non-interval values such as 999), including whether the
rounded-up cache endpoint may exceed `COMBAT_DIE_SIDES` and whether that can corrupt the
returned probability -> then continue first-strike/binomial bounds, cache dimensions,
combat-limit setup, trivial-odds exits, and all remaining nine history strata -> freeze
another physical WIP at the next settled boundary whether F638 is consumed or remains
unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
===============================================================================
C031-WIP470 -- QUEUE 057 CombatOdds.cpp -- F638 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP469.
- Working source base: reconciled `AdvCiv-SAS_light_source_20260907T150520.zip`.
- Supplied local comparison trees: Base AdvC 1.14, K-Mod 1.46, Civ4CE.
- Queue 057 `CvGameCoreDLL/CombatOdds.cpp` remains OPEN.
- F637 / provisional KI#959 remains CONFIRMED.
- F638 / provisional KI#960 is CONFIRMED here.
- F639 / provisional KI#961 is UNUSED.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.

-------------------------------------------------------------------------------
POST-F637 NEGATIVE CONTROL -- LFB interpolation cannot query odds near 1000
-------------------------------------------------------------------------------

The preceding handoff specifically flagged a possible cache/interpolation problem for
non-interval attacker per-round odds such as 999. That candidate does NOT survive the
live wrapper contract.

`LFBcalculateCombatOdds(Combatant const& att, Combatant const& def)` first decides
whether the attacker is the stronger per-round side:

    bool bFlip = (att.odds() > def.odds());

It then calls `LFBlookupCombatOdds` with `def.odds()` when flipped and `att.odds()`
otherwise. Since the two per-round odds sum to `COMBAT_DIE_SIDES`, the cache/interpolator
always receives the weaker side's odds and therefore at most half of the die range
(normally <= 500 for a 1000-sided combat die).

Consequently a live 999 attacker-odds case is queried as the complementary 1, not 999,
and the upper interpolation endpoint cannot run above 1000 through this wrapper.
Preserve this as a Queue-057 negative control, not F638.

-------------------------------------------------------------------------------
F638 / provisional KI#960 -- ACO specific-outcome math misweights bilateral
chance-first-strike rolls once either side has more than one chance first strike
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited Advanced Combat Odds / K-Mod defect; current-live and
player-visible in ACO health-outcome / expected-HP help. Supplied Base AdvC 1.14
retains the same mathematics exactly.

ROOT
----
`combat_odds::getCombatOddsSpecific` models a requested terminal combat outcome after
building:

    int const AttFSnet = att.lowFS() - def.lowFS();
    int const AttFSC = att.FSChances();
    int const DefFSC = def.FSChances();

Its three outcome branches then sum over a range of effective first-strike counts and
finish with:

    answer /= (AttFSC + DefFSC + 1);

This inherited closed form is exact when only one side has chance first strikes, and
also happens to handle the 1-chance-vs-1-chance case. It ceases to match the executor
when both units have chance-first-strike ranges and at least one range contains more
than one possible extra strike.

The underlying reason is that the real game does not make one uniform draw over the
combined net-first-strike range. Each unit independently rolls its own chance-first-
strike count. The distribution of their difference therefore comes from the Cartesian
product of the two uniform draws and has multiplicities that change across the net
first-strike values. The ACO closed form does not preserve those multiplicities for the
larger bilateral ranges.

AUTHORITATIVE EXECUTOR ORACLE
-----------------------------
`CvUnit::setCombatUnit()` initializes each combatant separately:

    setCombatFirstStrikes(pCombatUnit->immuneToFirstStrikes() ? 0 :
            (firstStrikes() + SyncRandNum(chanceFirstStrikes() + 1)));

Normal combat calls `setCombatUnit` for both attacker and defender, so absent immunity:

    attacker realized FS = attacker guaranteed FS
                           + uniform integer [0, attacker chance FS]
    defender realized FS = defender guaranteed FS
                           + uniform integer [0, defender chance FS]

These are two independent synchronized RNG draws. During `resolveCombat`, simultaneous
remaining first-strike counters cancel naturally because a successful combat roll only
inflicts damage when the opposing side has no first strike left.

Thus the exact ACO oracle for chance-first-strike state is to average the fixed-realized-
first-strike combat probability over all
`(AttFSC + 1) * (DefFSC + 1)` draw pairs (or equivalently use the correctly weighted
distribution of their net difference).

CURRENT-DATA ACTIVATION
-----------------------
This is not merely a mod-mod XML possibility.

Current SAS promotion data includes:
- `PROMOTION_FIRST_STRIKE1`: +1 chance first strike;
- `PROMOTION_FIRST_STRIKE2`: +1 guaranteed first strike;
- `PROMOTION_FIRST_STRIKE3`: +2 chance first strikes.

`FIRST_STRIKE3` is valid for many current combat classes (including multiple archer,
melee, gun, armor, naval and siege classes) and can be acquired through the current
promotion tree. A unit carrying the First Strike chain through FS3 therefore reaches:

    guaranteed first strikes = 1
    chance first strikes     = 3

An opposing eligible unit with only FS1 reaches:

    guaranteed first strikes = 0
    chance first strikes     = 1

Current unit XML also contains innate chance-first-strike units, so bilateral chance
ranges are ordinary supported combat state rather than a synthetic constructor-only
case.

SMALL EXACT FIXTURE
-------------------
For a clean arithmetic proof, take equal per-round combat odds (P_A = P_D = 0.5), one
hit required to defeat either combatant, no withdrawal, no immunity, and the current-
reachable first-strike states above:

    attacker: 1 guaranteed + 3 chance
    defender: 0 guaranteed + 1 chance

The executor has eight equally likely independent draw pairs. Their net attacker first
strikes are:

    1, 0, 2, 1, 3, 2, 4, 3

or by multiplicity:

    net 0: 1 pair
    net 1: 2 pairs
    net 2: 2 pairs
    net 3: 2 pairs
    net 4: 1 pair

With one hit needed and 50/50 normal rounds, attacker win probability for n nonnegative
net first strikes is `1 - 0.5^(n+1)`. Therefore exact engine probability is:

    (0.5
     + 2*0.75
     + 2*0.875
     + 2*0.9375
     + 0.96875) / 8
    = 0.82421875
    = 82.421875%

A literal evaluation of the current `getCombatOddsSpecific` victory branch for the same
inputs yields:

    sum1 = 0
    sum2 = 4.03125
    divisor = AttFSC + DefFSC + 1 = 5
    answer = 4.03125 / 5
           = 0.80625
           = 80.625%

The error is therefore 1.796875 percentage points in this compact exact fixture.

NORMAL MULTI-HIT CONTROL
------------------------
The root is not dependent on the one-hit simplification.

With the same first-strike state, equal 50/50 per-round odds, and five successful hits
required by each side, exact Cartesian-product enumeration gives these probabilities
for attacker victory after respectively 0..4 defender hits:

    exact:
        0 hits: 0.079345703125
        1 hit : 0.1300048828125
        2 hits: 0.15057373046875
        3 hits: 0.146697998046875
        4 hits: 0.128265380859375

    current ACO:
        0 hits: 0.082421875
        1 hit : 0.1302734375
        2 hits: 0.14931640625
        3 hits: 0.145166015625
        4 hits: 0.127099609375

The largest single displayed outcome error in that ordinary five-hit fixture is about
0.3076 percentage points. Because ACO repeatedly consumes these specific-outcome
probabilities for expected hit points, unharmed probabilities, bottom-HP probabilities,
kill/pullout/retreat totals and the HP-bar distribution, the wrong weighting propagates
through several player-facing ACO summaries.

LIVE CONSUMERS
--------------
`CvGameCoreDLL/ACOText.cpp` calls `getCombatOddsSpecific` throughout the Advanced Combat
Odds help path to compute, among other values:
- attacker and defender unharmed probabilities;
- expected attacker/defender HP;
- lowest-surviving-HP probabilities;
- attacker kill / combat-limit pullout / retreat / defender kill totals;
- each printed HP-outcome probability.

No gameplay combat resolution or AI attack decision uses this helper directly; F638 is
a UI-prediction correctness defect, not a combat-executor mutation.

LINEAGE / COMPARISON
--------------------
The function is explicitly marked as code from `ADVANCED COMBAT ODDS v1.1, 11/7/09,
PieceOfMind`. Supplied K-Mod 1.46 retains the same `AttFSC` / `DefFSC` loop structure
and final `(AttFSC + DefFSC + 1)` divisor in `CvGameTextMgr.cpp`.

AdvC later refactored the combatant setup and moved the helper into `CombatOdds.cpp`;
those changes did not replace the inherited bilateral chance-first-strike mathematics.
Current SAS and supplied Base AdvC 1.14 have byte-for-byte identical normalized
`getCombatOddsSpecific` function bodies after whitespace removal (same SHA-256 in the
local audit comparison), so the defect is inherited rather than SAS-created.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `getCombatOddsSpecific`, `AttFSC`, `DefFSC`,
`chanceFirstStrikes`, Advanced Combat Odds and bilateral chance-first-strike wording
find no existing finding that owns this probability-distribution root.

Nearby combat findings are independent:
- F200/KI#523 concerns loss of `AttackOddsChange` information after a 1..99 weighted-
  odds clamp;
- F204/KI#527 and F210/KI#533 concern air-domain misuse in debug/LFB strength paths;
- F602/KI#925 concerns SAS `iCombatLimit=110` executor/predictor disagreement;
- F637/KI#959 concerns a dropped `bHideFreeWins` argument in the separate public
  `calculateCombatOdds` wrapper.

F638 reproduces with ordinary lethal combat, no barbarian free wins, no air unit,
no attack-odds personality modifier and combat limits <= max HP.

REPAIR BOUNDARY
---------------
Do not try to patch only one algebraic endpoint or change the final divisor in
isolation. The robust repair boundary is the chance-first-strike distribution itself.

Two equivalent safe approaches are:
1. enumerate attacker extra-FS draw `0..AttFSC` and defender extra-FS draw `0..DefFSC`,
   evaluate the fixed realized/net-first-strike specific-outcome probability for each
   pair, then divide by `(AttFSC + 1) * (DefFSC + 1)`; or
2. precompute the exact multiplicity/weight of each possible net-first-strike value
   from those two independent uniform draws, then apply the fixed-net formula using
   those weights.

Chance-first-strike counts are small in current data, so explicit pair averaging is a
particularly clear correctness oracle. Preserve first-strike immunity by continuing to
use the already-normalized `Combatant.lowFS()` / `FSChances()` values.

VALIDATION TARGETS
------------------
1. Zero chance first strikes on both sides: unchanged.
2. Chance FS only on attacker: unchanged and matches current formula.
3. Chance FS only on defender: unchanged and matches current formula.
4. One chance FS vs. one chance FS: preserve the currently correct result.
5. Current chain fixture 1 guaranteed + 3 chance vs. 0 guaranteed + 1 chance:
   exact one-hit equal-odds result = 82.421875%, not 80.625%.
6. Swap attacker/defender and verify complementary terminal probabilities.
7. Five-hit equal-odds fixture: every specific terminal probability must match direct
   independent-draw enumeration, not only the summed victory probability.
8. First-strike immunity on either side must collapse that opponent's normalized FS
   count exactly as today.
9. ACO total terminal probabilities should sum to 1 (modulo float precision and
   withdrawal/combat-limit semantics) after the replacement.
10. `calculateCombatOdds`/LFB ordinary battle odds remain behaviorally separate; F638
    repairs only the ACO specific-outcome helper.

POST-F638 LEDGER / EXACT RESUME
-------------------------------
Queue 057 `CvGameCoreDLL/CombatOdds.cpp` -- OPEN.
F637 / provisional KI#959 -- CONFIRMED (ignored `bHideFreeWins` forwarding).
F638 / provisional KI#960 -- CONFIRMED (bilateral chance-first-strike distribution).
F639 / provisional KI#961 -- UNUSED.

Exact continuation:
C031-WIP470 physical -> Queue057 OPEN -> exclude F637/F638 plus all prior album owners
-> continue mechanical arithmetic audit from `getBinomialCoefficient` and the
`setupCombatantsImpl` hit/damage bounds through LFB cache dimensions, interval rounding,
integer width, trivial odds, first-strike summations, combat-limit handling and the
remaining nine path-history strata -> cross-check all survivors against Base AdvC 1.14,
K-Mod/Civ4CE and whole-album ownership -> promote F639 only for a distinct current-live
root -> freeze another physical WIP at the next settled boundary whether F639 is used
or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
===============================================================================
C031-WIP471 -- QUEUE 057 CombatOdds.cpp -- CLOSED / F639 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP470.
- Queue 057 `CvGameCoreDLL/CombatOdds.cpp` is CLOSED.
- Queue-057 confirmed roots: F637/KI#959 and F638/KI#960.
- F639 / provisional KI#961 remains intentionally UNUSED.
- Files complete: 37 / 169.
- No gameplay source was modified.
- Next priority file under the reconciled scheduling rule: Queue 058
  `CvGameCoreDLL/ArmamentForecast.cpp` (693 queued lines).

FINAL POST-F638 SATURATION PASS
-------------------------------
The remainder of Queue 057 was re-read mechanically after F638 rather than closed by
history exhaustion alone. The following surfaces were challenged:

1. `getBinomialCoefficient`
   - all live uses are internal to CombatOdds;
   - recurrence arithmetic is exact at each combinatorial step;
   - ordinary current combat with `combatLimit <= MAX_HIT_POINTS` remains within the
     historical round-count/coefficient domain because positive combat firepower keeps
     per-hit damage bounded away from zero;
   - SAS's one current `combatLimit > 100` data point can push the modeled rounds beyond
     those ordinary assumptions, but that is downstream breadth of already-confirmed
     F602/KI#925 (Janissary raw 110 damage requirement), not an independent F639.

2. `setupCombatantsImpl`
   - attacker/defender current strength, defender-context modifiers, current firepower,
     mean-firepower rounding and per-hit damage match `CvUnit::getDefenderCombatValues`;
   - attacker hit probability is deliberately reconstructed as the complement of the
     executor's integer defender threshold, which is more faithful to the real
     `SyncRandNum(...) < iDefenderOdds` branch than independently truncating both sides;
   - zero per-round odds and zero attacker hits-to-win exits are coherent with current
     callers;
   - first-strike immunity and normalized guaranteed/chance counts match executor setup;
   - above-max combat-limit disagreement remains F602;
   - dropped free-win visibility remains F637.

3. LFB fixed calculation / cache
   - the four cache dimensions cover every input consumed by the cached fixed result:
     net first strikes, attacker hits-to-win, defender hits-to-win and the rounded
     per-round odds index;
   - nested-vector growth obtains child pointers only after the corresponding parent
     dimension is grown, so reallocation does not leave a surviving pointer into an
     invalidated element range;
   - `-1` is a safe "not computed" sentinel because combat probabilities are
     nonnegative;
   - the weaker-side wrapper bounds the live odds query to <= half the combat die;
     interpolation may evaluate the adjacent 512 endpoint around a 500 query, but this
     is a valid mathematical endpoint for the same fixed state and does not violate the
     cache contract;
   - direct physical-state sweeps over correlated strength/HP/damage states found the
     16-point interpolation approximation within about one permille of the exact
     fixed-net probability in the tested current combat domain. The much larger errors
     obtainable by independently choosing impossible odds/hit-count tuples do not map
     to the engine's coupled strength/firepower equations and are not promoted.

4. LFB chance-first-strike distribution
   - unlike F638's ACO helper, LFB explicitly loops every attacker realized FS value
     and every defender realized FS value and divides by
     `(att.FSChances()+1)*(def.FSChances()+1)`;
   - that is the correct executor Cartesian-product distribution and is retained as the
     positive oracle for F638.

5. Floating-point synchronized-AI debt
   - AdvC itself leaves a `Fixme` noting that LFB's float/pow/binomial path is called
     from synchronized AI and could in principle be less deterministic than integer or
     scaled arithmetic;
   - practical 2843 also documents this concern in the LFB option comments;
   - however, the supplied/current evidence does not demonstrate an actual current
     cross-client divergence or a specific input whose final integer result differs by
     platform. The album requires a demonstrated current-live contradiction, so this
     remains explicit technical debt / OOS-risk commentary rather than F639.

6. Monte Carlo diagnostics
   - `MONTE_CARLO_ODDS_TEST` is hard-disabled in both assert and non-assert builds;
   - its helper therefore has no gameplay effect;
   - when F637 is repaired, its repeated setup call should forward the same
     `bHideFreeWins` value for diagnostic consistency, already recorded under F637.

7. History / comparison closure
   - current SAS and supplied Base AdvC 1.14 remain executable-semantically identical
     in CombatOdds, so no later 1.14 repair is missing;
   - practical 2843 created the refactored CombatOdds file and LFB path;
   - 2851 centralized free-win handling; 2852 changed rounding helpers; 2854 moved the
     ACO specific-outcome helper; 2855 deleted redundant legacy odds code;
   - later 3864/3900 are comment/const cleanup; 4003 introduced the F637 forwarding
     omission;
   - Civ4CE's stock `getCombatOdds` provides the original combinatorial/executor oracle;
     K-Mod provides the inherited LFB/ACO ancestry. No additional distinct surviving
     semantic regression emerged.

QUEUE-057 ROOT LEDGER
---------------------
F637 / KI#959
    `calculateCombatOdds(..., bHideFreeWins)` drops the Boolean when calling
    `setupCombatants`, so the explicit combat-result-message request to reveal
    Barbarian free wins still calculates hidden-free-win odds.

F638 / KI#960
    inherited ACO `getCombatOddsSpecific` misweights bilateral chance-first-strike
    distributions when both sides have chance ranges and at least one side has more
    than one chance; current SAS promotion/unit data reaches that state.

F639 / KI#961
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 057 is closed for this audit pass. Remaining concerns are approximation policy,
documented floating-point determinism debt, robustness outside current combat-data
domains, disabled diagnostic code, or breadth of already-owned roots. None clears the
album's independent current-live threshold for F639.

EXACT CONTINUATION
------------------
C031-WIP471 physical -> Queue057 CLOSED -> F637-F638 confirmed -> F639/KI#961 UNUSED ->
open next selected priority Queue058 `CvGameCoreDLL/ArmamentForecast.cpp` (693 lines) ->
establish SAS vs Base AdvC/K-Mod/Civ4CE provenance and caller map -> audit arithmetic,
cache/state lifetime, null/enum/range contracts and all file-history semantic strata ->
whole-album dedup before numbering -> freeze the next physical WIP at the next settled
boundary whether F639 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================
===============================================================================
C031-WIP472 -- QUEUE 058 ArmamentForecast.cpp -- F639 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP471.
- Working source base: reconciled `AdvCiv-SAS_light_source_20260907T150520.zip`.
- Supplied local comparison trees: Base AdvC 1.14, K-Mod 1.46, Civ4CE.
- Queue 057 `CombatOdds.cpp` remains CLOSED with F637-F638.
- Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` is OPEN.
- F639 / provisional KI#961 is CONFIRMED here.
- F640 / provisional KI#962 is UNUSED.
- No gameplay source was modified.

QUEUE-058 PROVENANCE BASELINE
-----------------------------
Current SAS `ArmamentForecast.cpp` has 694 physical lines (queue snapshot counted 693).
After comments/whitespace are stripped, its executable token stream differs from
supplied Base AdvC 1.14 at exactly ONE expression:

    Base AdvC 1.14:
        pTargetCity->getDistance() > getUWAI().maxLandDist()

    current SAS:
        pTargetCity->getDistanceByLand() > getUWAI().maxLandDist()

That one deliberate SAS delta is the already-owned KI#591 repair from practical 6315:
the target-specific naval/land buildup classification now uses the qualifying land
route duration rather than a potentially faster mixed/sea route. It is not a Queue-058
candidate.

K-Mod 1.46 and Civ4CE do not contain `ArmamentForecast`; this class is AdvC/UWAI code.
The supplied commit-diff history starts the file in practical 1253 and contains a long
AdvC refinement lineage plus later merge/formatting mirrors.

-------------------------------------------------------------------------------
F639 / provisional KI#961 -- accumulated peace flag makes ArmamentForecast's
hypothetical-vs-AreaAI branch depend on known-team iteration / TeamTypes order
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC/UWAI order-dependent state leak. Current-live in WarEvaluator
peace-scenario military analyses. Supplied Base AdvC 1.14 retains the same logic.

ROOT
----
Inside the per-known-team loop, ArmamentForecast keeps a global accumulated flag:

    bool bPeaceAssumed = false;

For each current `eOther`, it computes the pair-local condition:

    bool bPeaceAssumedLoop = (... current eTeam/eOther relation ...);

and, only when that current pair has a real war plan, latches the global flag:

    if (bPeaceAssumedLoop && kTeam.AI_getWarPlan(eOther) != NO_WARPLAN)
        bPeaceAssumed = true;

The next decision combines the CURRENT team's reachability with the ACCUMULATED
all-prior-teams peace flag:

    bool const bReachEither = canReachEither(eOther, eTeam);
    if (bReachEither && (bWarAssumed || bPeaceAssumed))
        bFictionalScenario = true;

`bWarAssumed` is pair-local. `bPeaceAssumed` is not.

Consequently, once an earlier iteration sees an assumed-peace war plan, ANY later
reachable known team can satisfy the expression even when that later team has no
fictional war or peace relation at all. Conversely, a reachable unrelated team that
appears earlier in the loop cannot see a peace flag that is latched later.

WHY THIS IS DETERMINISTIC ORDER DEPENDENCE
------------------------------------------
`TeamAIIter<MAJOR_CIV,KNOWN_TO>` obtains its sequence from `CvAgents::MAJOR_ALIVE`.
`CvAgents` constructs the base team sequence by increasing integer TeamTypes:

    for (int i = 0; i < iMaxTeams; i++)
        teamSeqCache(ALL).push_back(&GET_TEAM((TeamTypes)i));

`updateAllCachedSequences` walks that sequence in order when filling
`MAJOR_ALIVE`. Later insertion helpers explicitly insert by increasing agent ID.
The KNOWN_TO iterator filters that ordered major-team cache without reordering it.

Thus the ArmamentForecast loop order is stable TeamTypes order, not an unordered set.

MINIMAL STATE PROOF
-------------------
Let forecast player/team P be on the "their side" of the military analysis, and let:

    U = a known opponent whose current P-U war/war plan is assumed to end in the
        peace scenario, but neither U nor P can reach the other;

    R = another known team that P or R can reach, while no fictional P-R war or peace
        is assumed in this scenario.

For U:
    bPeaceAssumedLoop = true
    P has a war plan vs U -> bPeaceAssumed becomes true
    bReachEither = false
    -> bFictionalScenario remains false at U

For R:
    bPeaceAssumedLoop = false
    bWarAssumed = false
    bReachEither = true

If TeamTypes(U) < TeamTypes(R):
    U executes first and latches bPeaceAssumed=true.
    R then evaluates:
        true && (false || true)
    -> bFictionalScenario = true.

If TeamTypes(R) < TeamTypes(U):
    R executes before the latch:
        true && (false || false)
    -> no change.
    U later has bReachEither=false
    -> bFictionalScenario remains false.

The diplomatic, reachability and war-plan facts are otherwise the same. Only which
known team occupies the lower TeamTypes slot changes the branch result.

LIVE CONSEQUENCE
----------------
`bFictionalScenario` is not merely logging state.

When true:
    `predictArmament(...)` is called directly with the scenario-derived intensity,
    defensive and naval flags accumulated above.

When false:
    ArmamentForecast enters the "Checking AreaAI" branch and can replace or strengthen
    those inputs from the real current `AreaAI`, `AI_isFocusWar`, ALERT/CRUSH/TURTLE/
    LAST_STAND/FINAL_WAR state and the power-curve sanity check; it also derives naval
    and defensive classification from the current home-area AreaAI.

Therefore identical military-analysis facts can produce different projected military
power / branch production solely because the unrelated known teams occupy different
TeamTypes slots.

The situation is naturally reachable in the supported model: UWAI explicitly treats
two known teams as unreachable when neither side's cache has a reachable city, while a
third known team can be reachable. WarEvaluator evaluates a recursive peace scenario
for every ordinary war-vs-peace utility calculation, constructing MilitaryAnalyst and
InvasionGraph, whose nodes instantiate ArmamentForecast for the participating known
players. No custom API or invalid enum is required.

IMPORTANT SEMANTIC NUANCE
-------------------------
The global `bPeaceAssumed` itself has legitimate later uses:
- report whether ANY war-plan peace is assumed;
- suppress ALERT2 interpretation when the scenario assumes peace.

F639 does NOT claim that the global flag should cease to exist.

The defect is specifically coupling that accumulated flag to the CURRENT loop team's
`bReachEither` before all pairs have been considered.

There are two plausible intended policies:

A. Pair-local policy:
   A peace assumption makes the scenario fictional only when THAT same war pair can
   reach each other.

B. Global policy:
   Any assumed peace can make the scenario fictional whenever the forecast player has
   some meaningful reachable war theatre.

The present code implements neither policy consistently; it implements "global peace
flag AND a reachable team encountered at or after the first peace pair."

REPAIR BOUNDARY
---------------
Choose the intended policy explicitly and make it order-independent.

For pair-local semantics, compute a local qualified flag, e.g.:

    bool const bPeaceAssumedForThisWar =
        (bPeaceAssumedLoop &&
         kTeam.AI_getWarPlan(eOther) != NO_WARPLAN);

    if (bPeaceAssumedForThisWar)
        bPeaceAssumed = true; // retain global aggregate for later uses

    if (bReachEither && (bWarAssumed || bPeaceAssumedForThisWar))
        bFictionalScenario = true;

For deliberately global semantics, accumulate reachability separately through the
whole loop and combine the two aggregates only after the loop, rather than combining
a prefix-state flag with the current element:

    bAnyReachable |= bReachEither;
    ...
    after loop:
        if (bPeaceAssumed && bAnyReachable)
            bFictionalScenario = true;

The pair-local interpretation appears structurally more likely because the adjacent
`bWarAssumed` operand is pair-local and the comments around `bReachEither` discuss the
specific war's ability to cause buildup. But the audit finding does not require
guessing that policy: either repair removes the current TeamTypes-order dependence.

LINEAGE
-------
Practical 1253, the initial ArmamentForecast/UWAI file commit, already contains:

    if(reachEither && (warAssumed || peaceAssumed))
        fictionalScenario = true;

with `peaceAssumed` latched earlier in the same ordered loop.

The later comprehensive UWAI rewrites / merge snapshots (including practicals 2920,
2954 and 3392) preserve the same local/global asymmetry. Current Base AdvC 1.14 still
has it unchanged. Therefore this is an inherited AdvC root, not SAS-created.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for ArmamentForecast, peaceAssumed, fictional scenario,
order-dependent, TeamTypes iteration order and related wording find no prior finding
that owns this root.

Prior order-dependent findings concern independent state in voting, city/resource
scans, deal valuation, etc. KI#591 is the unrelated ArmamentForecast mixed-route
distance mismatch. No existing finding covers peace-scenario branch selection based
on the prefix position of an unrelated team.

VALIDATION TARGETS
------------------
1. Construct a three-team logical fixture P/U/R as above.
2. Run once with TeamTypes(U) < TeamTypes(R), once with only U/R IDs exchanged.
3. Hold diplomacy, war plans, reachability, AreaAI and military data constant.
4. Current code must demonstrate differing `bFictionalScenario` branch selection.
5. Repaired code must make the branch invariant under U/R ID exchange.
6. Verify an actually reachable assumed-peace pair still marks the scenario fictional.
7. Verify a reachable assumed-war pair still marks it fictional through bWarAssumed.
8. Verify global bPeaceAssumed still controls report text and ALERT2 suppression.
9. Verify a peace assumption involving only unreachable pairs follows the explicitly
   chosen pair-local/global policy rather than incidental iterator position.
10. War and peace utility results should remain invariant under player/team slot
    permutation when all game-state relationships are otherwise isomorphic.

POST-F639 LEDGER / EXACT RESUME
-------------------------------
Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` -- OPEN.
F639 / provisional KI#961 -- CONFIRMED.
F640 / provisional KI#962 -- UNUSED.

Exact continuation:
C031-WIP472 physical -> Queue058 OPEN -> exclude F639 plus KI#591 and all prior album
owners -> continue constructor state-machine audit (war counters, vassal/total-war
classification, fictional-scenario transitions, AreaAI fallback) -> then
`predictArmament` branch-weight arithmetic, unavailable-branch redistribution,
nuclear/cavalry overlap, production/power scaling and `productionFromUpgrades` ->
cross-check every surviving candidate against Base AdvC 1.14 and the full path-history
strata -> promote F640 only for a distinct current-live root -> freeze the next physical
WIP at the next settled boundary whether F640 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP473 -- QUEUE 058 ArmamentForecast.cpp -- F640 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP472.
- Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` remains OPEN.
- F639 / provisional KI#961 remains CONFIRMED.
- F640 / provisional KI#962 is CONFIRMED here.
- F641 / provisional KI#963 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F640 / provisional KI#962 -- SAS's global 75% unit-upgrade discount is not
removed by ArmamentForecast's inverse upgrade-cost model, so UWAI systematically
understates the military production/power gained from ordinary upgrades
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED SAS-specific cross-file regression. The ArmamentForecast inverse remains the
Base AdvC 1.14 formula while SAS practical 5157 changed the price producer.

PRODUCER CHANGE
---------------
Current `CvUnit::upgradePrice` first computes the inherited raw price:

    iPrice = BASE_UNIT_UPGRADE_COST
           + production-cost difference * UNIT_UPGRADE_COST_PER_PRODUCTION;

then applies the AI handicap multiplier where relevant. SAS subsequently adds:

    static const int iSAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT =
        std::max(0, GC.getDefineINT(
            "SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT"));

    iPrice = (iPrice * iSAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT) / 100;

and only afterward applies the individual unit's upgrade discount.

Current SAS XML sets:

    SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT = 75

and every current handicap sets:

    iAIUnitUpgradePercent = 100

The Main Changes Guide explicitly describes the resulting policy: both humans and AI
pay 75% of the former/base upgrade price.

STALE CONSUMER
--------------
`ArmamentForecast::productionFromUpgrades()` starts from:

    scaled r = kPlayer.AI_getGoldToUpgradeAllUnits();

That cached total is produced by `AI_updateGoldToUpgradeAllUnits`, which calls each
candidate unit's REAL `upgradePrice`. It therefore already contains the SAS 75%
multiplier.

ArmamentForecast then says that it performs:

    // An approximate inversion of CvUnit::upgradePrice

but its inversion still knows only about the inherited terms:

    rUpgrCostPerProd = UNIT_UPGRADE_COST_PER_PRODUCTION;
    r /= rUpgrCostPerProd;

    rTypicalGoldForProdDiff = rUpgrCostPerProd * 30;
    rBaseCostModifier =
        rTypicalGoldForProdDiff /
        (rTypicalGoldForProdDiff + BASE_UNIT_UPGRADE_COST);
    r *= rBaseCostModifier;

and, for AI players only:

    rAIUpgradeFactor = iAIUnitUpgradePercent / 100;
    if (rAIUpgradeFactor > 0.1)
        r /= rAIUpgradeFactor;

There is no compensation for
`SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT`.

Thus the forecast interprets already-discounted gold as though it had been generated by
the old undiscounted price formula.

EXACT CALIBRATION PROOF
-----------------------
The inherited inverse is explicitly calibrated around a "mean training cost difference
of 30 hammers."

Let:
    B = BASE_UNIT_UPGRADE_COST
    C = UNIT_UPGRADE_COST_PER_PRODUCTION

Take an ordinary unit with:
- a +30-hammer upgrade target;
- no per-unit upgrade discount;
- enough income/gold that the income cap does not bind;
- current SAS AI handicap factor = 100%.

Before the SAS global discount, the raw price is:

    R = B + 30*C

The inherited inverse computes:

    (R / C) * (30*C / (30*C + B))
    = 30

which is exactly the intended 30-hammer calibration (apart from fixed/integer rounding).

Current SAS first changes the real price to approximately:

    G = 0.75 * R

but ArmamentForecast runs the same inverse:

    (0.75*R / C) * (30*C / (30*C + B))
    = 22.5

Therefore the same ordinary +30-hammer upgrade is represented as only about
22.5 hammers of military production: a systematic 25% under-credit caused solely by
the forgotten new global multiplier.

Integer truncation in `CvUnit::upgradePrice` can move an individual fixture slightly
around 22.5, but it cannot remove the root.

INCOME-BOUND CASE ALSO REMAINS WRONG
------------------------------------
The repair cannot be dismissed on the ground that ArmamentForecast caps upgrades by
available income before converting them.

If the full discounted upgrade set is affordable, the whole discounted total must be
converted back to the same production differences the units actually gain.

If the income cap binds, each unit of actual upgrade gold now buys `1 / 0.75` as much
pre-discount upgrade value as under the old pricing. Therefore the bounded gold amount
also needs the global discount removed before translating it into production
equivalent.

The existing AI-handicap correction is already performed after the income cap for
exactly the same conceptual reason.

LIVE WAR-EVALUATION CONSEQUENCE
-------------------------------
`productionFromUpgrades()` is not a diagnostic-only estimate.

On the first InvasionGraph simulation phase (the only phase where upgrading is allowed),
`Node::predictArmament` constructs ArmamentForecast with `bNoUpgrading=false`.
The returned upgrade-equivalent production is passed to:

    predictArmament(..., rAdditionalProduction, ...)

and then enters:

    rTotalProductionForBuildUp = rAdditionalProduction;
    rTotalProductionForBuildUp +=
        iTurnsBuildUp * rArmamentPortion * rPerTurnProduction;

That total is converted into predicted power for every available MilitaryBranch and is
written into the graph-local military state consumed by MilitaryAnalyst/WarEvaluator.

So current SAS's cheaper upgrade policy improves the power a player can obtain for a
given amount of gold, while UWAI's armament projection credits only about 75% of the
production-equivalent upgrade gain that its own inherited inverse intended. This can
bias projected war outcomes and war/peace utility.

LINEAGE
-------
The ArmamentForecast inverse dates to the initial AdvC UWAI implementation and current
Base AdvC 1.14 retains it.

SAS practical 5157 / ae78db3d20 introduced
`SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT` into `CvUnit::upgradePrice` with current
value 75, while also setting the AI upgrade handicap percentage to 100. That practical
did not update ArmamentForecast.

The earlier practical-5157 archaeology correctly established that the new 75% price is
actually paid consistently by humans and AI. F640 is a different, downstream result:
the price producer is coherent, but one consumer that explicitly tries to invert that
producer was not updated.

WHOLE-ALBUM DEDUP / RELATED ROOTS
---------------------------------
No existing album finding owns the missing SAS-global-multiplier inversion.

F365/KI#688 is independent. It proves that `AI_updateGoldToUpgradeAllUnits` caches a
per-unit `upgradePrice` by UnitType even though Great-General `PROMOTION_LEADER` can
give an individual unit a 100% upgrade discount. F640 reproduces with an ordinary unit
having no individual discount and with only one unit of its type, so F365 is absent.

The inherited per-unit `getUpgradeDiscount()` and optional Python upgrade-price override
also make any aggregate inverse inherently approximate. They do not excuse the SAS
regression: the new global 75% multiplier is uniform, deterministic, explicitly known
to the DLL and affects essentially every ordinary upgrade.

REPAIR BOUNDARY
---------------
Preserve the income affordability cap in ACTUAL discounted gold, then remove every
known global multiplicative pricing factor before applying the inherited raw-price
inverse.

For the SAS factor specifically, after the income-bound step and before the
`UNIT_UPGRADE_COST_PER_PRODUCTION` inversion, conceptually:

    scaled rSASUpgradeFactor =
        per100(SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT);

    if (rSASUpgradeFactor > small_safe_threshold)
        r /= rSASUpgradeFactor;

This mirrors the existing AI-handicap correction. The exact placement relative to the
AI factor can be chosen for clarity because the two are multiplicative, but both should
be applied after the actual-gold affordability cap.

If the SAS factor is configured as zero or nearly zero, do not divide by it. As with the
existing "AI upgrades free or almost free" case, aggregate gold alone is then
insufficient to reconstruct the production difference; a stronger future solution
would estimate upgrade targets directly rather than infer them from gold.

VALIDATION TARGETS
------------------
1. Set SAS upgrade percent to 100: behavior must match the inherited forecast.
2. Current 75%, AI handicap 100%, ordinary +30-hammer upgrade with no unit discount:
   forecast should return approximately the same 30-hammer production equivalent as
   the old 100% pricing, not ~22.5.
3. Repeat with the income bound below the full upgrade bill; conversion must reflect
   the greater upgrade value bought by each discounted gold unit.
4. Human and AI at current SAS settings should receive the same global-factor
   compensation; AI then additionally uses any configured AI handicap factor.
5. Preserve F365 as a separate test: mixed ordinary/Great-General units still require
   the aggregate price-cache fix and must not be considered repaired by F640.
6. Changing `SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT` should not silently change
   the inferred production difference of a fixed set of fully affordable ordinary
   upgrades; it should primarily change affordability.
7. InvasionGraph first-phase predicted military power should reflect the corrected
   upgrade-equivalent production; later no-upgrade phases remain unchanged.

POST-F640 LEDGER / EXACT RESUME
-------------------------------
Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` -- OPEN.
F639 / provisional KI#961 -- CONFIRMED.
F640 / provisional KI#962 -- CONFIRMED.
F641 / provisional KI#963 -- UNUSED.

Exact continuation:
C031-WIP473 physical -> Queue058 OPEN -> exclude F639/F640, F365 and all prior owners ->
finish the simulated-city-loss/coastal-branch-state trace (decide same-root addendum to
F269 vs distinct root only after caller/state proof) -> continue constructor/AreaAI
state-machine negative controls -> finish `predictArmament` arithmetic and
`productionFromUpgrades` producer/consumer contracts -> exhaust ArmamentForecast
history and whole-body mechanical hazards -> either promote a genuinely independent
F641 or close Queue058 cleanly -> freeze the next physical WIP at the next settled
boundary regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP474 -- QUEUE 058 ArmamentForecast.cpp -- F641 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP473.
- Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` remains OPEN.
- F639 / provisional KI#961 remains CONFIRMED.
- F640 / provisional KI#962 remains CONFIRMED.
- F641 / provisional KI#963 is CONFIRMED here.
- F642 / provisional KI#964 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F641 / provisional KI#963 -- post-conquest concurrent ArmamentForecast still
scans the real pre-loss city topology, so losing the last simulated coastal city
can leave Fleet/Logistics production in a graph state with only inland cities
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited UWAI simulation-state/interface defect. Current-live in the normal
second-half concurrent build-up that intentionally runs AFTER InvasionGraph has applied
hypothetical city conquests. Base AdvC 1.14 retains the same behavior.

SIMULATION ORDER / PRODUCER
---------------------------
`InvasionGraph::simulate` deliberately executes:

    1. initial armament build-up;
    2. `simulateLosses()` over the war horizon;
    3. concurrent armament build-up for the second half.

The initial tracked UWAI import (practical 1253) already contains this ordering and
comments that the concurrent phase assumes reduced production output if cities have
been lost.

Successful city conquest does not mutate the real CvPlayer/CvCity map. In
`Node::applyStep`, the defender records graph-local loss state:

    addCityLoss(kStepCity);

and the attacker records the corresponding conquest. This is intentional: InvasionGraph
is a hypothetical war simulation.

`Node::productionPortion()` translates those graph-local losses only into a scalar:

    lost population / real current total population

and `Node::predictArmament` passes that scalar to ArmamentForecast:

    ArmamentForecast(..., pTargetCity, productionPortion());

No lost-city set, remaining-city count, or graph-local city predicate is passed.

STALE CONSUMER
--------------
During naval branch allocation, `ArmamentForecast::predictArmament` ignores the
graph-local conquest state and scans the real player's unchanged cities:

    int iRevealed = 0;
    int iRevealedCoast = 0;
    FOR_EACH_CITY(pCity, kPlayer)
    {
        if (pCity->isRevealed(TEAMID(m_eAnalyst)))
        {
            iRevealed++;
            if (pCity->isCoastal())
                iRevealedCoast++;
        }
    }

It then compares that real-city scan against the real current city count:

    if (iRevealed >= kPlayer.getNumCities() && iRevealedCoast <= 0)
        rBranchPortions[FLEET] = 0;
    else
        rBranchPortions[FLEET] =
            min(0.35, 0.08 + revealedCoastPortion / 2.8);

Therefore cities already conquered in the graph continue to determine both:
- whether the surviving simulated empire is known to have zero coastal cities;
- the fraction of revealed cities counted as coastal.

A scalar production reduction cannot preserve this topology.

CLEAN CURRENT-LIVE FIXTURE
--------------------------
Let forecast player P currently own four real cities:

    C = its only coastal city
    I1, I2, I3 = inland cities

Let the analyst be P itself (or otherwise have all four cities revealed), eliminating
fog-of-war ambiguity.

Let P and Q be in a mutual war/invasion-graph cycle. P's own primary target in Q remains
naval: unreachable by land from P's capital, or beyond the qualifying land-distance
threshold. P has valid pre-war Fleet/Logistics typical units.

During `simulateLosses`, Q successfully conquers C.

The graph now represents P as having exactly three surviving cities, all inland:

    m_cityLosses = { C }
    productionPortion() > 0
    real CvPlayer ownership remains unchanged

Because Q is P's own primary target, the current KI#590 repair does NOT clear P's
outgoing edge merely because Q took C:

    if (m_pPrimaryTarget != NULL && m_pPrimaryTarget != &kAttacker)
        changePrimaryTarget(NULL);

Here `m_pPrimaryTarget == &kAttacker`, so the naval target can remain live into the
immediately following concurrent ArmamentForecast.

CURRENT BRANCH RESULT
---------------------
Current ArmamentForecast still sees all four REAL cities:

    iRevealed = 4
    iRevealedCoast = 1
    kPlayer.getNumCities() = 4

The "all known and no coast" gate is false. The raw Fleet allocation becomes:

    coast portion = 1 / 4 = 0.25

    Fleet = 0.08 + 0.25 / 2.8
          ~= 0.1692857

before later defensive/intensity/nuclear transformations.

If a valid Logistics representative exists, its allocation is also derived from this
positive Fleet share:

    Logistics = min(Fleet, 1 / typical cargo)

so the same stale coastal city can support positive transport buildup as well.

GRAPH-CONSISTENT RESULT
-----------------------
The simulated surviving-city view is:

    remaining cities = 3
    revealed remaining cities = 3
    revealed remaining coastal cities = 0

The existing own code's policy for that state is unambiguous:

    all remaining cities revealed AND zero coast
    -> Fleet portion = 0
    -> Logistics portion = 0

Thus this is not an argument about the chosen 0.08/2.8 heuristic. The graph satisfies
the exact condition for which ArmamentForecast itself intentionally disables naval
production, but the condition is evaluated against a city that the graph has already
removed.

IMPORTANT REPAIR TRAP
---------------------
Simply filtering C out of the `FOR_EACH_CITY` loop is insufficient if the comparison
continues to use the real `kPlayer.getNumCities()`.

That partial patch would produce:

    iRevealed = 3
    iRevealedCoast = 0
    kPlayer.getNumCities() = 4   [still real/pre-loss]

so:

    iRevealed >= kPlayer.getNumCities()

is false and ArmamentForecast falls into the heuristic branch:

    Fleet = 0.08 + 0 / 2.8 = 0.08

It would still forecast Fleet production for an all-revealed simulated empire with no
coastal city.

The repair therefore needs a coherent graph-local remaining-city view: both the city
iteration/filter and the denominator/count must describe the same hypothetical state.

PRE-LOSS MILITARY-BRANCH COPY MAKES THE INTERFACE GAP BROADER
--------------------------------------------------------------
`Node::initMilitary` copies the real UWAICache branches once at node construction:

    new MilitaryBranch::Fleet(*kCacheMilitary[FLEET])
    new MilitaryBranch::Logistics(*kCacheMilitary[LOGISTICS])

and the graph explicitly treats typical units as stable for the evaluation ("they don't
change").

In real-cache refreshes, sea typical-unit selection calls
`AI_isExpectingToTrain`, which scans real cities and rejects a sea unit unless enough
qualifying coastal cities can actually train it. Thus loss of the last real coast can
invalidate Fleet/Logistics trainability.

A hypothetical graph loss does not trigger that real-cache refresh and cannot do so,
because ownership was intentionally never changed. This means the branch copies can
also retain pre-loss naval candidates after the graph has removed the enabling coast.

The all-revealed fixture above does not NEED this second manifestation: stale city
topology alone is sufficient to prove F641 because it creates a positive Fleet share.
But it confirms that the scalar-only ArmamentForecast interface is missing production-
capability state as well as population quantity.

LIVE CONSEQUENCE
----------------
The concurrent ArmamentForecast converts the resulting branch portions into new branch
power and permanently adds it to the graph-local `m_military` state. That military state
is subsequently consumed by MilitaryAnalyst/WarEvaluator.

So after the simulator itself has concluded that P lost its only coastal city, P can be
credited with second-half Fleet and Logistics build-up as if that city still existed.
This can strengthen later naval/offensive projections and change war-vs-peace utility.

The production scalar does reduce P's TOTAL output after C is lost. It does not repair
the allocation error: some of the reduced output is still assigned to naval branches
that the surviving simulated city topology cannot support.

LINEAGE
-------
Practical 1253 / f6ff6a182f introduced the core model together:
- graph-local city losses;
- population-based `productionPortion`;
- initial build-up -> loss simulation -> concurrent build-up.

Practical 1264 / 0884de493c then added ArmamentForecast's revealed-city/coastal-city
branch allocation, using the real Civ player city iterator and real city count. From
that point onward, the post-loss concurrent phase could consume pre-loss coastal
topology.

Current Base AdvC 1.14 retains:
- the same simulation ordering;
- the scalar-only `productionPortion` interface;
- copied branch state;
- the real `FOR_EACH_CITY` coastal scan and real `getNumCities()` denominator.

Current SAS repairs several nearby InvasionGraph state leaks but still retains this one.

WHOLE-ALBUM DEDUP / WHY THIS IS NOT F269 OR F214
-------------------------------------------------
Whole-album semantic searches found no prior owner for post-loss ArmamentForecast
coastal/trainability state.

F269/KI#592 is nearby but distinct. It proved that later BATTLE RESOLUTION read real
`CvArea::getCitiesPerPlayer` after graph-local city losses, overstating area Army share
and garrison rally sources. Current SAS repaired that with
`Node::remainingCitiesInArea`, subtracting graph-local losses at those consumers.

F641 survives that repair unchanged:
- it occurs later in the concurrent PRODUCTION FORECAST;
- ArmamentForecast does not receive Node's city-loss set;
- it reads a different real-map inventory (`FOR_EACH_CITY` plus `getNumCities`);
- repairing `remainingCitiesInArea` cannot alter it.

The two findings share the general fact that hypothetical ownership is graph-local, but
have independent consumers, independent effects and independent repair surfaces.
F269's area-count helper cannot fix F641 without extending the simulation-state
interface into ArmamentForecast.

F214/KI#537 is also distinct. F214 fixes MilitaryBranch typical-unit refresh after a
REAL game-state capability loss by resetting cached candidates before rescanning.
F641 requires no real capability loss at all: the real UWAICache is correct for the
unchanged real map, while the hypothetical InvasionGraph state intentionally differs
from it. Calling the ordinary cache refresh would simply rediscover the still-real
coastal city.

F641 also does not depend on KI#591's mixed-route distance root; use a target that is
unambiguously unreachable by land and naval classification remains true.

REPAIR BOUNDARY
---------------
Do not mutate real CvCity ownership or the real UWAICache during war evaluation.

Instead, give ArmamentForecast enough graph-local city state to reason about the
surviving simulated empire. A narrow repair could pass:
- the lost-city ID set (or a predicate/helper owned by Node), and
- the simulated remaining-city count;

then the coastal scan must:
- skip every graph-lost city;
- compare revealed remaining cities with the simulated remaining count.

A stronger reusable design would expose a small simulation-aware city-view abstraction
from InvasionGraph::Node and use it for any ArmamentForecast question whose answer
depends on which cities remain, including branch trainability.

For Fleet/Logistics specifically, loss of every simulated qualifying coastal city must
make their new-production share zero even if the unchanged real UWAICache still has a
pre-loss typical sea unit.

Do not conflate this with the intentionally approximate population scaling of total
production; F641 is the discrete topology/capability contradiction after that scaling.

VALIDATION TARGETS
------------------
1. P has four fully revealed cities, exactly one coastal C, naval target Q.
2. Before simulated loss of C, preserve ordinary positive Fleet/Logistics allocation.
3. Q captures C in `simulateLosses`; P retains three inland cities and its target Q.
4. Concurrent forecast must see:
       remaining=3, revealed remaining=3, coastal remaining=0
   and allocate Fleet=0, Logistics=0.
5. Swap which of P's four cities is lost:
   - lose inland city -> coast remains -> naval allocation remains possible;
   - lose sole coastal city -> naval allocation becomes zero.
6. A patch that merely skips lost cities but still compares against real
   `getNumCities()` must FAIL the regression test (it leaves the 0.08 fallback);
   both iterator and denominator must be simulation-aware.
7. Repeat with more than one coast: Fleet ratio must use surviving coast / surviving
   revealed cities, not pre-loss topology.
8. Preserve fog-of-war policy: unknown surviving cities may still justify uncertainty,
   but a city already known by the GRAPH to be lost must never count as an unknown or
   surviving coast.
9. Verify F269's remainingCitiesInArea battle calculations remain unchanged.
10. Verify real-game UWAICache/MilitaryBranch state is not mutated by hypothetical
    evaluation.

POST-F641 LEDGER / EXACT RESUME
-------------------------------
Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` -- OPEN.
F639 / provisional KI#961 -- CONFIRMED.
F640 / provisional KI#962 -- CONFIRMED.
F641 / provisional KI#963 -- CONFIRMED.
F642 / provisional KI#964 -- UNUSED.

Exact continuation:
C031-WIP474 physical -> Queue058 OPEN -> exclude F639-F641, F214/F269/F365, KI#591
and all prior album owners -> finish the remaining constructor state-machine and
`predictArmament` controls -> inspect all `productionFromUpgrades` approximation edges
not already F365/F640 -> exhaust the full ArmamentForecast practical-history list and
mechanical hazards (division/clamps/overflow/null/state/report-mute contracts) ->
compare surviving questions against Base AdvC 1.14 and whole-album ownership -> either
promote a genuinely independent F642 or close Queue058 cleanly -> freeze the next
physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP475 -- QUEUE 058 ArmamentForecast.cpp -- F642 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP474.
- Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` remains OPEN.
- F639-F641 / provisional KI#961-KI#963 remain CONFIRMED.
- F642 / provisional KI#964 is CONFIRMED here.
- F643 / provisional KI#965 is UNUSED.
- No gameplay source was modified.

NEGATIVE CONTROLS SINCE WIP474
------------------------------
Two sharp-looking current bodies were challenged and rejected before numbering F642.

1. Shared UWAIReport mute state is NOT leaked by ArmamentForecast.
   `UWAIReport::setMute(bool)` is deliberately a nesting counter:
       true  -> m_iMuted++
       false -> m_iMuted--
   so ArmamentForecast's own AI-only true/false pair correctly restores a caller that
   was already muted. InvasionGraph's concurrent-build-up outer mute therefore survives
   the inner forecast calls.

2. Fleet + Logistics multipliers do not make current branch portions negative in the
   reachable maximum.
   In offensive naval buildup with revealed coast, Home Guard is first reduced to 0.12.
   Fleet and Logistics are each at most 0.35 before the low-navy multiplier; that
   multiplier is clamped to 1.5 and then diluted to `(1.5+1)/2 = 1.25`.
   Hence:
       0.12 + 0.35*1.25 + 0.35*1.25 = 0.995
   leaving Army >= 0.005. The assert is therefore consistent with current bounds.

-------------------------------------------------------------------------------
F642 / provisional KI#964 -- WarEvaluator's agent-target "considering peace /
total war" parameters alter ArmamentForecast for unrelated current wars
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC/UWAI scenario-parameter leak. Current-live in ordinary peace
evaluation during an ongoing war. Supplied Base AdvC 1.14 retains the same logic.

ROOT
----
`ArmamentForecast::predictArmament` is instantiated for each InvasionGraph node/player,
but its total-vs-limited-war adjustment reads the GLOBAL evaluation parameters for the
analyst's agent-target pair:

    WarEvalParameters const& kParams = m_kMA.evaluationParams();
    if (kParams.isConsideringPeace())
    {
        if (kParams.isTotal())
        {
            rAtWarAdjustment = 0.05;
            if (GET_TEAM(kParams.getAgent()).AI_getWarPlan(
                    kParams.getTarget()) != WARPLAN_TOTAL)
            {
                eIntensity = FULL;
            }
        }
        else rAtWarAdjustment -= 0.05;
    }

The comments describe the underlying K-Mod behavior being modeled as:
- a civ focuses somewhat more production on military when IT is in total war;
- when switching from limited to total, total war should imply full buildup.

But none of the conditions above asks whether the FORECASTED player `m_ePlayer` is the
agent, is fighting the target, or is itself in a total/limited war. The state belongs
to `kParams.getAgent()` versus `kParams.getTarget()` and is applied to every node.

WAR-EVALUATOR PARAMETER ORACLE
------------------------------
For an agent A already at war with main target B:

    WarEvalParameters constructor:
        m_bConsideringPeace = A.isAtWar(B) = true

During the ordinary war-scenario evaluation:
    WarEvaluator::evaluate(eWarPlan, ...)
        sets m_kParams.isTotal according to eWarPlan.

After computing that scenario, WarEvaluator recursively computes the peace baseline:

    evaluate(NO_WARPLAN, false, iPreparationTime)

and the recursive call executes:

    m_bPeaceScenario = true;
    m_kParams.setTotal(false);

Afterward the original parameters are restored.

Therefore for an ongoing TOTAL A-B war, the two scenarios intentionally differ as:

    A-B war scenario:
        isConsideringPeace = true
        isTotal            = true

    A-B peace scenario:
        isConsideringPeace = true
        isTotal            = false

That is appropriate for A-B. F642 is that ArmamentForecast applies the same switch to
players whose own war state has not changed between those scenarios.

UNRELATED-COMPONENT ACTIVATION
------------------------------
MilitaryAnalyst intentionally includes more than the agent-target component.

While constructing the analysis it gathers EVERY major known player currently at war:

    if (isKnownToBeAtWar(TEAMID(ePlayer), kAgent.getID()))
        currentlyAtWar.insert(ePlayer);

and passes that whole set into InvasionGraph. The graph constructor makes a Node for
each such player and links their current targets.

The source explicitly explains why the extra components are simulated:

    "Start with the agent's component, but simulate the other ones as well.
     While they can't affect the outcome of the agent's wars, anticipating
     conquests of other civs is going to be helpful for war evaluation."

The peace scenario removes the A-B side war through `removeWar`; unrelated current wars
continue normally. Nodes are deliberately not deleted merely because a simulated peace
removes another war.

MINIMAL CURRENT-LIVE FIXTURE
----------------------------
Let:
    A = analyst/agent
    B = A's main target
    C and D = two other major civs known to A

Actual state:
    A and B are already at war under WARPLAN_TOTAL.
    C and D are also already at war with each other.
    C/D have no war, vassal, ally or reachability relationship to A/B that changes in
    the evaluated A-B peace scenario.

MilitaryAnalyst includes A, B, C and D because all are known current war parties.
The recursive A-B peace scenario removes A-B but leaves C-D unchanged.

Now inspect C's forecast.

In the A-B WAR scenario:
    kParams.isConsideringPeace() == true
    kParams.isTotal()            == true

so C receives:
    rAtWarAdjustment = +0.05

In the A-B PEACE scenario:
    kParams.isConsideringPeace() == true
    kParams.isTotal()            == false

so the SAME C-D war receives:
    rAtWarAdjustment = -0.05

Thus C's armament share differs by an absolute 0.10 solely because A is comparing
continued total war with peace against B.

For a simple unclamped example with C's unchanged local forecast otherwise at NORMAL
intensity and base armament portion 0.30:

    C in A-B war scenario:
        0.30 + 0.05 = 0.35

    C in A-B peace scenario:
        0.30 - 0.05 = 0.25

That is 35% versus 25% of C's per-turn production assigned to armament even though
nothing about C-D changed.

The exact base buildUnitProb is not important to the proof: away from the clamp, the
foreign parameter leak is always a 10-percentage-point war-minus-peace difference.

FULL-INTENSITY FORM IS EVEN STRONGER WHEN REACHED
-------------------------------------------------
The same block contains:

    if (GET_TEAM(kParams.getAgent()).AI_getWarPlan(kParams.getTarget()) !=
        WARPLAN_TOTAL)
        eIntensity = FULL;

If a call evaluates total war while the agent's actual A-B plan is not yet TOTAL, then
EVERY forecasted node -- including unrelated C -- can be forced to FULL intensity based
on A's real war-plan type.

This is the same root and should be fixed with it. The ordinary ongoing-total-war
fixture above is sufficient by itself and does not depend on reaching this stronger
form.

PLAYER-VISIBLE / AI CONSEQUENCE
-------------------------------
The unrelated forecast is not discarded diagnostics.

`InvasionGraph::simulateArmament` calls `Node::predictArmament` for every non-NULL node.
ArmamentForecast converts the contaminated portion into additional branch power:

    iTurnsBuildUp * rArmamentPortion * rPerTurnProduction

and writes that into the graph-local MilitaryBranch state used by the simulated C-D
combat.

`simulateComponent` explicitly simulates the disconnected components. MilitaryAnalyst
then exports their conquests, losses, gained/lost power and predicted scores.

These results can enter WarUtility aspects. For example, `GreedForAssets::threatToCities`
uses MilitaryAnalyst gained Army power for rival players that are part of the analysis,
and `KingMaking` deliberately consults analyzed rivals' simulated relative losses.
So changing C-D buildup can alter A's evaluated war-vs-peace utility even though the
A-B choice did not change C-D's war state.

This violates a useful invariance contract:
    an unrelated continued C-D war should not receive different armament policy merely
    because A's evaluated A-B branch is "total war" versus "peace."

LINEAGE
-------
The root is present in practical 1253 / f6ff6a182f, the initial ArmamentForecast/UWAI
import. The original code already read `params.isConsideringPeace()` and
`params.isTotal()` inside `predictArmament` without qualifying the forecast player.

The major UWAI revision/refactor (practicals 2920/2954) preserved the policy while
renaming it to current structures. Current Base AdvC 1.14 retains it unchanged.
SAS's only executable divergence in ArmamentForecast itself is the already-owned KI#591
land-distance repair, so F642 is inherited rather than SAS-created.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `rAtWarAdjustment`, "switch from limited to total",
ArmamentForecast total-war/considering-peace parameter leakage, and unrelated forecast
components find no existing root owner.

F639 is distinct:
    F639 is an iteration-order leak inside the constructor's per-known-team fictional-
    scenario flag (`bPeaceAssumed` prefix state combined with current reachability).

F642 is order-independent:
    even with one unrelated C-D component and no F639 activation, the later
    `predictArmament` arithmetic directly reads A-B scenario parameters while forecasting
    C.

F641 is also distinct:
    F641 concerns graph-local POST-CONQUEST city topology missing from the concurrent
    forecast interface.

REPAIR BOUNDARY
---------------
The total/limited adjustment must be derived from the FORECAST PLAYER'S scenario war
state, not copied wholesale from the analyst's A-B parameter object.

At minimum:
- special handling of `kParams.isTotal()` as the proposed A-B plan should be gated to
  the agent (and perhaps its intended team/vassal participants if that is deliberately
  modeled);
- unrelated current-war nodes must derive any total-vs-limited production adjustment
  from their own real/simulated war plans;
- the recursive A-B peace scenario must not subtract 0.05 from an unrelated C whose
  C-D war continues unchanged.

A stronger design would compute an explicit per-node/per-war scenario war-plan
classification in MilitaryAnalyst/InvasionGraph and pass that local classification to
ArmamentForecast, avoiding future uses of agent-target parameters as though they were
properties of every player.

Do not simply remove the adjustment globally: the A-B agent forecast still needs a way
to model the intended difference between limited and total war.

VALIDATION TARGETS
------------------
1. Fixture A-B ongoing TOTAL, unrelated C-D ongoing war.
2. Compare A-B war scenario and recursive A-B peace scenario.
3. Hold every C-D local input fixed (AreaAI, intensity inputs, production, city state,
   branch power, war plan).
4. Current code must show C's raw armament portion differs by +0.05 versus -0.05.
5. Repaired code must make C's forecast invariant under the A-B scenario switch.
6. A's own forecast should still model the intended total-vs-limited adjustment.
7. B/participating allies or vassals should follow one explicit documented response
   policy rather than inheriting A's parameter accidentally.
8. Repeat with a disconnected C-D component that yields different conquest outcomes
   under 25% vs 35% buildup; those outcomes must no longer depend on A-B's total flag.
9. Repeat an ordinary ongoing LIMITED A-B war: unrelated C-D must not receive a global
   -0.05 merely because A is "considering peace."
10. If the stronger limited->total transition path is retained, ensure it can force FULL
    only for the intended A-B participants, never arbitrary unrelated nodes.

POST-F642 LEDGER / EXACT RESUME
-------------------------------
Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` -- OPEN.
F639 / provisional KI#961 -- CONFIRMED.
F640 / provisional KI#962 -- CONFIRMED.
F641 / provisional KI#963 -- CONFIRMED.
F642 / provisional KI#964 -- CONFIRMED.
F643 / provisional KI#965 -- UNUSED.

Exact continuation:
C031-WIP475 physical -> Queue058 OPEN -> exclude F639-F642 plus all prior album owners
-> finish remaining constructor/predictArmament/productionFromUpgrades saturation ->
explicitly revisit simulated-capital/AreaAI and target-city topology as likely F641
breadth rather than new roots -> exhaust all 70-ish ArmamentForecast path-history
entries with special attention to practical 1395 minor bugfixes, 1499 bugfix batch,
2281 instance-cost work, 2605/2699/2609 buildup changes, 2920 revision, 3945 overflow
repair and 4368 naval fixes -> mechanical divisors/clamps/nulls/report-state/overflow
closure -> either promote a genuinely independent F643 or close Queue058 cleanly ->
freeze another physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP476 -- QUEUE 058 ArmamentForecast.cpp -- CLOSED / F643 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP475.
- Queue 058 `CvGameCoreDLL/ArmamentForecast.cpp` is CLOSED.
- Queue-058 confirmed roots:
    F639 / provisional KI#961
    F640 / provisional KI#962
    F641 / provisional KI#963
    F642 / provisional KI#964
- F643 / provisional KI#965 remains intentionally UNUSED.
- Files complete: 38 / 169.
- Files open: 0 / 169.
- Next selected priority file: Queue 060 `CvGameCoreDLL/CvArea.cpp` (686 queued lines).
- No gameplay source was modified.

FINAL SATURATION PASS
---------------------
The post-F642 pass challenged every remaining current body and all 71 path-history
entries rather than closing the file merely because four findings had already emerged.

1. Constructor state machine
   - war/war-plan counting, vassal exclusion, single-pushover handling, recent-attack
     handling, total-war classification, peace-scenario preparation abandonment,
     ALERT/DAGGER/AreaAI interactions and human defensive handling were re-read against
     their later consumers;
   - the historical vassal-war-count correction from practical 1677 is present;
   - practical 1299's old power-curve typo (`bur < 18`) is repaired to `0.18`;
   - no distinct surviving state leak beyond F639 and F642 was established.

2. Report mute lifetime
   - the apparent nested-mute problem is a false positive:
     `UWAIReport::setMute(true)` increments a nesting counter and `setMute(false)`
     decrements it;
   - ArmamentForecast's inner AI-only mute pair therefore restores, rather than clears,
     an outer InvasionGraph mute.

3. Per-turn production / hurry estimate
   - `AI_estimateYieldRate` intentionally uses demographic/history information and the
     source explicitly documents that UWAI routinely cheats in this respect;
   - current hurry XML has Population hurry at 27 production/population and Gold hurry
     at 3 gold/production;
   - the `(GoldPerProduction*2)/3 + ProductionPerPopulation/angerLength` expression is
     a coarse heuristic and has counterintuitive mod-robustness semantics for arbitrary
     gold-hurry XML, but current data does not produce an independent contradiction.
     Preserve as tuning/robustness debt, not F643.

4. Intensity and armament bounds
   - peaceful-victory downgrading is intentionally one intensity step;
   - era adjustment is bounded and current `AI_getCurrEraFactor` contracts make the
     later naval era denominators positive whenever the code reaches them;
   - armament portion is capped/clamped to [0, 0.75];
   - F642 remains the only confirmed foreign scenario-parameter contamination in the
     total/limited adjustment.

5. Naval branch arithmetic
   - practical 4368's two fixes are current:
       * reveal status is checked from the analyst team, not forecast player's team;
       * the all-revealed/no-coast Fleet=0 result is no longer immediately overwritten.
   - with current reachable maxima, Home Guard + Fleet + Logistics cannot make Army
     negative:
       offensive naval Home Guard = 0.12;
       Fleet <= 0.35;
       Logistics <= 0.35;
       each low-navy multiplier <= 1.25 after dilution;
       total <= 0.995.
   - cargo and fleet divisors are guarded by positive typical-power checks, and the
     current-era gate plus strictly increasing/default AIEraFactor contract protects
     the fleet era-factor denominator.

6. Branch redistribution and overlap
   - unavailable branch weight is redistributed proportionally only after the branch
     portions sum to one; the near-one early return prevents division by zero;
   - Cavalry's later copy of Army share is intentional overlap: the class contract says
     all Cavalry is also Army, so it is not an exclusive production budget;
   - Nuclear likewise represents strategic nuke power separately from Army's ordinary
     combat usefulness;
   - no double-budget root was established.

7. Production-to-power scaling
   - practical 3945's overflow repair is present:
         branchPortion * totalProduction * (typicalPower / typicalCost)
     rather than multiplying all scaled operands before division;
   - typical-cost <=0 is skipped;
   - current branch objects are fully created by InvasionGraph and their pointer/index
     contracts hold for all callers.

8. Upgrade inversion
   - ordinary inherited approximation limitations (per-instance costs, unknown exact
     upgrade mix, income bounding) remain heuristic by design;
   - F365 owns the separate per-unit General/upgrade-discount cache collapse;
   - F640 owns the concrete SAS 75%-global-price-factor omission;
   - no third independent upgrade-conversion root survived.

F641 SAME-ROOT BREADTH -- SIMULATED CAPITAL / ROUTE STATE
---------------------------------------------------------
The final state-lifetime pass found two additional manifestations of F641's already
confirmed root. They are recorded as breadth, not new F643 findings.

A. `getAreaAI(ePlayer)` always reads the REAL current capital:

       CvCity const* pCapital = GET_PLAYER(ePlayer).getCapital();
       return pCapital->getArea().getAreaAIType(...);

   If InvasionGraph has hypothetically lost that capital before the concurrent forecast,
   real ownership is intentionally unchanged, so AreaAI can still be selected from a
   home area that the graph no longer regards as containing the player's surviving
   capital/cities.

B. the target-city naval classifier consumes UWAICache fields computed from the REAL
   pre-simulation source-city/capital network:

       pTargetCity->canReachByLandFromCapital()
       pTargetCity->getDistanceByLand()

   `UWAICache::City::updateDistance` explicitly weights the cache owner's real capital
   and real qualifying source cities. Graph-local loss of that capital, the sole coastal
   launch city, or other qualifying production cities does not rebuild this hypothetical
   route state.

These are the same interface defect already proved by F641:
ArmamentForecast receives only a scalar `productionPortion()` after graph-local city
losses, not a simulation-aware remaining-city/capital/capability view. A repair should
therefore avoid three separate ad-hoc fixes and expose one coherent graph-local city
view to:
- revealed/coastal counts and remaining-city denominator;
- home-area/capital selection where relevant;
- target-route / naval-production capability where simulated city loss changes them.

The deliberately approximate total-production scaling can remain scalar; discrete
topology/capability questions cannot be reconstructed from that scalar.

HISTORY CLOSURE
---------------
All 71 indexed ArmamentForecast history entries were classified.

High-value semantic strata:
- practical 1253: initial ArmamentForecast/UWAI implementation; source of F639/F642
  and initial graph/forecast interface later implicated by F641;
- practical 1264: added revealed-city/coastal branch allocation; historical birth of
  F641's clean coastal manifestation;
- practical 1275: observer-aware typical cost/power;
- practicals 1287/1289: naval low-fleet/cargo buildup and Ancient-era zero protection;
- practical 1299: corrected 18 -> 0.18 power-curve threshold;
- practical 1335: no-war-vs-extra peace handling;
- practical 1344: small-enemy/pushover forecast refinement;
- practical 1363/1341: upgrade affordability/current-gold horizon refinements;
- practical 1677: vassal war count and cached war-plan count corrections;
- practical 2281: per-instance typical unit cost support;
- practicals 2609/2699: current intensity/branch tuning;
- practicals 2621/2734/2719: era-factor buildup refinements and custom-era support;
- practical 2767: capital-specific land-reach target semantics;
- practical 2920: comprehensive UWAI scaled-number rewrite, including current
  per-hurry-type production estimate; no unported later correction found;
- practical 2923/2957: follow-up correctness/compile adjustments;
- practical 3945: predicted-power overflow repair;
- practical 4368: analyst reveal-team and no-coast Fleet=0 repairs;
- SAS practical 6315: KI#591 land-only route-duration repair.

Merge/format/refactor-only mirrors and the remaining small historical changes reconcile
with the current body. Current SAS remains executable-semantically identical to supplied
Base AdvC 1.14 except the deliberate KI#591 `getDistanceByLand()` divergence, so there is
no missed Base-1.14 ArmamentForecast repair.

QUEUE-058 ROOT LEDGER
---------------------
F639 / KI#961
    accumulated `bPeaceAssumed` prefix state is combined with the current team's
    reachability, making fictional-scenario/AreaAI selection depend on TeamTypes order.

F640 / KI#962
    SAS global 75% upgrade-price multiplier reaches the price producer but is missing
    from ArmamentForecast's inverse, under-crediting fully affordable ordinary upgrades
    by about 25%.

F641 / KI#963
    post-conquest concurrent forecasting sees real pre-loss city topology/capability;
    clean sole-coast fixture forecasts Fleet/Logistics where simulated remaining empire
    has no coast. Same-root breadth also covers stale real-capital AreaAI and cached
    pre-loss route/capital capability.

F642 / KI#964
    analyst agent-target `isConsideringPeace` / `isTotal` parameters alter every graph
    node, so an unchanged unrelated C-D war can receive +0.05 vs -0.05 armament merely
    because A compares total war with peace against B.

F643 / KI#965
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 058 is closed for this pass. Remaining concerns are explicit approximation/tuning,
mod-robustness outside current data, same-root breadth of F641, or historical defects
already repaired. None clears the independent current-live threshold for F643.

EXACT CONTINUATION
------------------
C031-WIP476 physical -> Queue058 CLOSED -> F639-F642 confirmed ->
F643/KI#965 UNUSED -> open next selected priority Queue060
`CvGameCoreDLL/CvArea.cpp` (686 queued lines) -> establish current SAS vs Base AdvC 1.14,
K-Mod and Civ4CE provenance plus path-history/caller map -> audit area counters,
serialization, ownership/units/cities/yields, AreaAI transitions, target-city and
water/land topology interactions, enum/index/division/null contracts -> whole-album
dedup before numbering -> promote F643 only for a distinct current-live root -> freeze
the next physical WIP at the next settled boundary whether F643 is consumed or remains
unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP477 -- QUEUE 060 CvArea.cpp -- F643 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: reconciled C031-WIP476 from
  `AdvCiv-SAS_light_source_20260907T164725.zip`.
- Queue 058 remains CLOSED with F639-F642 / KI#961-KI#964.
- Queue 060 `CvGameCoreDLL/CvArea.cpp` is OPEN.
- F643 / provisional KI#965 is CONFIRMED here.
- F644 / provisional KI#966 is UNUSED.
- No gameplay source was modified.

QUEUE-060 BASELINE / EARLY DEDUP
--------------------------------
The refreshed light-source ZIP reconciles with C031-WIP476.

Current SAS `CvArea.cpp` is executable-semantically the same as supplied Base AdvC 1.14
apart from removal of old save-version migration branches under SAS's intentional
current-version-only KI#46 policy. No newer Base-1.14 CvArea repair is missing.

Early sharp-looking CvArea surfaces were deduplicated before F643:
- unconditional `reportBarbarianCityCreated()` in `changeCitiesPerPlayer` is exactly
  already-confirmed F589/KI#912;
- `changeNumTiles` trying to detect a lake-state transition through two reads of cached
  `isLake()` is same-root territory of F634/KI#956, not a new CvArea root;
- the malformed negative `CvPlot::processArea` water-city/building branches remain the
  previously recorded dormant/robustness controls absent a supported live mutation path.

-------------------------------------------------------------------------------
F643 / provisional KI#965 -- practical 3898 changed Barbarian fog-unit selection
from parent land-area context to spawn-plot context, so shelf water calls suppress
resource units and transport cargo is evaluated as though the land unit lived at sea
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC regression, current-live in the normal Barbarian shelf/fog
spawn system. Supplied Base AdvC 1.14 retains it. The root is a caller/context contract
regression introduced when `randomBarbarianUnit` was changed from `CvArea` to `CvPlot`.

ORIGINAL CONTRACT
-----------------
AdvC practical 1253 introduced both `CvArea::hasAnyAreaPlayerBonus` and the refactored
Barbarian unit selector:

    UnitTypes CvGame::randomBarbUnit(UnitAITypes ai, CvArea const& a)

The resource rule explicitly used the supplied LAND area:

    a.hasAnyAreaPlayerBonus(orReq)

with the source comment:

    "Also require the resource to be connected by someone on this continent;
     in particular, don't spawn Horse Archers on a horseless continent."

The same practical's cargo-spawn path selected its land passenger with:

    randomBarbUnit(loadAI, area)

even though the passenger was physically created on the transport's water coordinates.
That distinction was intentional: `area` described the continent whose shelf was being
processed, while the water plot was only the temporary cargo location.

The outer modern routine still preserves that architecture. It skips water CvAreas and
iterates:

    for each LAND area a:
        getShelves(a, shelves)
        process each attached shelf
        then process land fog spawning in a

Its own comment remains explicit:

    "For each land area, first spawn sea Barbarians for each shelf attached
     to that land area. Skip water areas entirely."

CONTRACT CHANGE / BIRTH OF ROOT
-------------------------------
Practical 3898 / commit `40bd5f76d9dc1dca3984bbd75f96c0835a4fb3bd`
changed the selector signature:

    randomBarbarianUnit(UnitAITypes, CvArea const&)
        ->
    randomBarbarianUnit(UnitAITypes, CvPlot const&)

and changed BOTH shelf call sites:

    cargo:
        randomBarbarianUnit(eLoadAI, kArea)
            ->
        randomBarbarianUnit(eLoadAI, pTransport->getPlot())

    ordinary shelf sea fog spawn:
        randomBarbarianUnit(eUnitAI, kArea)
            ->
        randomBarbarianUnit(eUnitAI, *pPlot)

The resource checks were simultaneously changed from:

    kArea.hasAnyAreaPlayerBonus(eBonus)

to:

    kPlot.getArea().hasAnyAreaPlayerBonus(eBonus)

The plot parameter was introduced to support new local terrain suitability logic,
notably "Mounted units only in open terrain." But a shelf plot and a transport plot are
WATER plots, whereas the old area argument was the parent LAND continent.

WATER-AREA CvArea CONTRACT
--------------------------
`CvArea::getCitiesPerPlayer` deliberately hides adjacent coastal-city bookkeeping for
generic water-area callers:

    int CvArea::getCitiesPerPlayer(
        PlayerTypes eIndex, bool bCheckAdjacentCoast) const
    {
        if (!bCheckAdjacentCoast && isWater())
            return 0;
        return m_aiCitiesPerPlayer.get(eIndex);
    }

The default argument is false.

`CvArea::hasAnyAreaPlayerBonus` calls that method without the adjacent-coast override:

    for (PlayerIter<ALIVE> itPlayer; ...)
    {
        if (getCitiesPerPlayer(itPlayer->getID()) > 0 &&
            itPlayer->hasBonus(eBonus))
        {
            return true;
        }
    }

Therefore:

    any WATER CvArea
        -> getCitiesPerPlayer(player) == 0 for every player
        -> hasAnyAreaPlayerBonus(any bonus) == false

This is intentional for the generic CvArea city-count interface, but it means the
post-3898 plot-local resource test can never succeed at either shelf water call.

CURRENT SEA-FOG CONSEQUENCE
---------------------------
For shelf fog spawning the chosen `pPlot` is a water shelf plot and the requested AI is
`UNITAI_ATTACK_SEA`.

Current supported XML includes resource-requiring naval candidates. A clean example is:

    UNIT_FRIGATE
        DOMAIN_SEA
        iCombat = 8
        PrereqTech = TECH_CHEMISTRY
        additional TECH_ASTRONOMY
        BonusType = BONUS_IRON
        UNITAI_ATTACK_SEA = allowed

`CvPlayer::canTrain(UnitTypes)` checks civilization mapping, techs, obsolescence and
global/unit-class restrictions but does NOT enforce a city-local resource network;
that is exactly why `randomBarbarianUnit` performs its explicit area-resource test.

Once Barbarians have the relevant techs and a player on the shelf's parent continent
has connected Iron, the pre-3898 selector could admit Frigate through
`kArea.hasAnyAreaPlayerBonus(BONUS_IRON)`.

Current code instead asks the shelf WATER CvArea and therefore rejects it
categorically.

The same applies to every resource-requiring sea fog candidate. Current SAS XML has
multiple such naval units, including Frigate, Ship of the Line, Ironclad and later
Oil/Uranium-gated ships. Their exact era/obsolescence gates differ, but whenever such a
unit otherwise qualifies, the water-area resource predicate forces its eligibility
probability from positive to zero.

CURRENT TRANSPORT-CARGO CONSEQUENCE
-----------------------------------
The cargo path is an even cleaner domain mismatch:

    CvUnit* pTransport = pShelf->randomBarbarianTransport();
    ...
    UnitTypes eLoadUnit =
        randomBarbarianUnit(UNITAI_ATTACK, pTransport->getPlot());

The selected unit is DOMAIN_LAND and is immediately created at the ship's coordinates
and loaded into the transport. The water tile is therefore not the passenger's intended
habitat; it is only its temporary cargo location.

Current ordinary resource-gated land candidates include:

    UNIT_AXEMAN
        DOMAIN_LAND
        DefaultUnitAI = UNITAI_ATTACK
        UNITAI_ATTACK allowed
        PrereqTech = TECH_BRONZE_WORKING
        OR bonuses = BONUS_COPPER / BONUS_IRON

On a continent with connected Copper/Iron and appropriate Barbarian tech, the old
land-area selector could choose the Axeman. The current cargo selector asks the
transport's WATER CvArea and rejects every OR resource through
`hasAnyAreaPlayerBonus == false`.

Thus resource-requiring land units are categorically excluded from Barbarian transport
cargo even when the parent continent satisfies the exact resource policy that the
helper was designed to enforce.

MOUNTED-CARGO MANIFESTATION
---------------------------
Practical 3898 also introduced plot-local mounted suitability:

    if (bMounted)
    {
        if (kPlot.isWater() ||
            kPlot.defenseModifier(NO_TEAM, true) > 0)
        {
            continue;
        }
        ...
    }

Current SAS preserves the rule with its split
`UNITCOMBAT_MOUNTED_MELEE` / `UNITCOMBAT_MOUNTED_RANGED` classes.

For ordinary LAND fog spawning this makes sense: the unit really will stand on the
candidate land plot.

For transport cargo, `kPlot` is the transport's water tile. Therefore EVERY mounted
land passenger is rejected before creation solely because cargo is being selected while
the transport is at sea.

Current mounted candidates include Chariots, Horse Archers and the SAS Camel/mounted
chains. They also carry resource requirements, so the water-area resource bug already
rejects them; the mounted-water check is an independent manifestation of the SAME
context loss rather than a separate F644.

WHY THIS IS ONE ROOT
--------------------
Both symptoms begin in the same practical-3898 interface change:

    selector context = parent land area
        ->
    selector context = physical spawn plot

That new context is correct for ordinary land fog spawning, where local terrain matters,
but it is insufficient for shelf/cargo selection:
- resource ecology belongs to the parent land continent;
- cargo land-terrain suitability cannot be inferred from the transport's water tile;
- sea-unit resource eligibility likewise needs the parent continent/resource context
  rather than a water CvArea whose generic city API intentionally reports zero.

Do not create separate fixes for Frigate resources, Axeman cargo and mounted cargo.
They are caller-specific consequences of one lost-context interface.

BASE ADVC / HISTORY
-------------------
Supplied Base AdvC 1.14 has the same current signature, water-plot cargo caller,
water-plot shelf caller, `kPlot.getArea().hasAnyAreaPlayerBonus` resource checks and
mounted-water rejection. This is inherited by SAS rather than created by SAS.

The pre-3898 history is a particularly strong internal oracle:
- practical 1253 introduced the continent-resource rule with explicit "horseless
  continent" wording;
- cargo from its introduction selected through that same parent land `area`;
- practical 3898 alone replaced both shelf uses with physical water plots while adding
  plot-local terrain logic.

K-Mod/Civ4CE do not contain this full AdvC shelf/cargo architecture, so the decisive
lineage oracle is AdvC's own before/after contract.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `randomBarbarianUnit` / `randomBarbUnit`;
- Barbarian cargo and mounted cargo;
- `hasAnyAreaPlayerBonus`;
- "horseless continent";
- shelf/sea fog resource gating;
- practical 3898 around this selector

find no existing owner.

The album's earlier `randomBarbarianUnit` note is explicitly different: it records a
future/mod-mod-only concern about optional OR resources with `NO_TECH`, for which
current XML had no reachable configuration. F643 uses ordinary current resource-gated
units and a normal supported shelf/cargo caller.

F589/KI#912 is unrelated Barbarian-city-history bookkeeping inside
`changeCitiesPerPlayer`.

REPAIR BOUNDARY
---------------
The selector now needs TWO kinds of context rather than pretending one CvPlot can
represent both:

1. parent land-area / continental resource context;
2. optional actual spawn-terrain context.

A narrow safe interface would pass both, e.g. conceptually:

    randomBarbarianUnit(
        eUnitAI,
        CvArea const& kParentLandArea,
        CvPlot const* pSpawnPlot)

Then:
- use `kParentLandArea.hasAnyAreaPlayerBonus` for resource gating;
- apply mounted/open-terrain/disease checks only when the selected LAND unit will
  actually occupy `pSpawnPlot` as a free unit;
- for transport cargo, do not reject a land passenger because the transport plot is
  water. If cargo terrain preference is desired, derive it from a prospective landing
  context instead of the current ship tile;
- for shelf sea units, use the water plot for water-domain/local placement checks but
  retain the attached land area for continental resource ecology.

Since `createBarbarianUnits` already receives `CvArea& kArea` and `Shelf* pShelf`, all
necessary parent-area information is available at both current call sites.

Do NOT "fix" this by changing generic `CvArea::getCitiesPerPlayer` to expose water-area
adjacent coast by default; its false-default is deliberate and has many unrelated
callers.

VALIDATION TARGETS
------------------
1. Land fog Axeman on a Copper/Iron continent: behavior unchanged.
2. Barbarian transport on shelf attached to that continent:
   Axeman cargo eligibility becomes positive again when tech/era gates allow it.
3. Same transport on a continent with no Copper/Iron:
   Axeman remains excluded.
4. Horse Archer/Chariot-style cargo:
   no longer rejected merely because the transport tile is water; parent-continent
   resource rule still applies.
5. Ordinary mounted LAND fog spawn:
   retain current open-terrain and disease restrictions on the actual land spawn plot.
6. Frigate shelf fog spawn beside a parent continent with connected Iron and valid tech:
   eligibility becomes positive.
7. Equivalent shelf beside parent continent without Iron:
   Frigate remains excluded.
8. Resource-free sea units continue to spawn under existing era/AI weighting.
9. Do not change generic water `getCitiesPerPlayer()` default semantics.
10. Confirm Base-1.14 current behavior reproduces the same zero-eligibility result and
    that restoring explicit parent-land context removes it.

POST-F643 LEDGER / EXACT RESUME
-------------------------------
Queue 060 `CvGameCoreDLL/CvArea.cpp` -- OPEN.
F643 / provisional KI#965 -- CONFIRMED.
F644 / provisional KI#966 -- UNUSED.

Exact continuation:
C031-WIP477 physical -> Queue060 OPEN -> exclude F643 plus F589/F634/F636/F269/F025
and all prior area/topology owners -> continue full CvArea body/history audit ->
target-city cache/recycled-player-slot state, AreaAI updates, water/coastal city-count
callers, representative-area/lake state, enum-map widths, serialization pairing,
counter mutators, reveal/power/yield/building/trade/free-specialist state,
division/null/index contracts -> cross-check survivors against Base AdvC 1.14 and
whole-album ownership -> promote F644 only for a distinct current-live root -> freeze
the next physical WIP at the next settled boundary whether F644 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP478 -- QUEUE 060 CvArea.cpp -- RECYCLED TARGET-CITY LEAD CLOSED BELOW F644
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP477.
- Queue 060 `CvGameCoreDLL/CvArea.cpp` remains OPEN.
- F643 / provisional KI#965 remains CONFIRMED.
- F644 / provisional KI#966 remains UNUSED.
- No gameplay source was modified.

PARKED WIP203 LEAD REVISITED -- `CvArea::m_aTargetCities[player]`
-----------------------------------------------------------------
The old parked robustness lead was re-opened because the direct timing proof is stronger
than WIP203 had established.

Persistent shape:
- every CvArea owns `m_aTargetCities[MAX_PLAYERS]`;
- the array is serialized;
- `CvPlayer::resetPlotAndCityData` does not scrub it;
- `CvPlayerAI::AI_init/reset` resets the new player's city-target TIMER but does not
  iterate CvAreas to reset the target pointers;
- `CvPlayer::initInGame` therefore can recycle PlayerTypes slot X while surviving
  foreign target-city IDInfo remains stored in `m_aTargetCities[X]`.

The target refresh is not unconditional in the ordinary player-units pre-turn path:

    if (AI_getCityTargetTimer() == 0 && SyncRandOneChanceIn(8))
        AI_updateAreaTargets();

And team turn ordering creates a superficially dangerous consumer path:

    CvTeamAI::AI_doTurnPre()
        -> AI_updateAreaStrategies(false)   // explicitly does NOT refresh targets
        -> AI_doWar()
        -> UWAI::Team::doWar/review
        -> alignAreaAI(...)
        -> capitalArea.AI_getTargetCity(memberPlayer)

So WIP203 was correct that stale target storage can survive player initialization and
that an ordinary team-war consumer exists before the probabilistic player-unit refresh.

WHY IT STILL DOES NOT CLEAR THE FINDING THRESHOLD
--------------------------------------------------
The missing activation is the supported colony/vassal war lifecycle.

`CvPlayer::splitEmpire` creates the recycled player through `CvGame::addPlayer`, then:
1. assigns its parent;
2. gives it inherited technologies;
3. makes the new team a voluntary vassal of the parent through `assignVassal`;
4. transfers the colony cities;
5. initializes/processes UWAI for the new player.

`CvTeam::setVassal` then aligns the new vassal's war/peace state with its master.
If the master is at war with a third party, the new vassal queues/declares the same war.

The declaration/war-plan path calls `CvTeamAI::AI_setWarPlan`. Current AdvC/SAS
`AI_setWarPlan` deliberately dirties every member's city-target timer and then calls:

    AI_updateAreaStrategies();

with the default `bTargets=true`.

`AI_updateAreaStrategies(true)` executes:
    - recompute AreaAI;
    - `AI_updateAreaTargets()`;

and the latter synchronously calls each zero-timer member's `CvPlayerAI::AI_updateAreaTargets`.
By this stage the colony cities have been transferred, so the refresh computes the new
civilization's own target state and overwrites/clears the recycled slot's stale
`m_aTargetCities[X]` values before the later team-turn `AI_doWar()->alignAreaAI` path.

Conversely, when the new colony has no active/preparing war plan, UWAI has no plan whose
land/naval classification would call `alignAreaAI` in a way that can turn the stale city
into a demonstrated strategic branch consequence.

This closes the precise gap that kept the lead parked in WIP203:
- stale storage exists;
- a pre-random-refresh consumer exists in isolation;
- but the supported event that gives the fresh colony the necessary war-plan state also
  forces a synchronous target refresh first.

DEDUP / SCOPE
-------------
Do not promote this as F644 on present evidence.

It remains useful robustness debt adjacent to:
- F477/KI#800: durable bare PlayerTypes identity aliases across recycled slots;
- F479/KI#802: independently orphaned TeamTypes-indexed blockade counters.

`m_aTargetCities` is derived target state rather than historical identity or blockade
counter state. A future custom spawn path that creates/reuses a player and installs a
war plan without the current `AI_setWarPlan -> AI_updateAreaStrategies(true)` sequence
could activate the stale target. Current supported colony creation does not prove that
path.

A preventive cleanup remains reasonable engineering:
    when recycling a PlayerTypes slot, clear that player's `m_aTargetCities` entry in
    every live CvArea;
but the album threshold requires a demonstrated current-live consequence, not merely
stale persistent data.

OTHER QUEUE-060 CONTROLS RECONFIRMED
------------------------------------
- `CvPlot::processArea` coastal-city decrement's comma-operator typo is the exact
  already-audited dormant Queue-009 root:
      pWaterArea->getCitiesPerPlayer(getOwner()) > 0, true
  and is not F644 under current supported topology mutation.
- `changeNumTiles` lake-transition logic is F634/KI#956 breadth because cached `isLake`
  cannot observe the transition until explicit lake refresh.
- generic non-Barbarian calls incrementing `m_iBarbarianCitiesEver` are F589/KI#912.

EXACT CONTINUATION
------------------
C031-WIP478 physical -> Queue060 OPEN -> F643 confirmed -> F644/KI#966 UNUSED ->
recycled m_aTargetCities lead CLOSED below threshold for supported colony lifecycle ->
continue remaining CvArea mutators and callers:
clean power / border obstacle / trade routes / yield modifiers / train-AI and AI-unit
counters / bonus counts / AreaAI state -> serialization pairing/current-version policy ->
exhaust all 62-ish CvArea history strata, especially semantic changes around 1274/1275
Ice-area partition, 1253 area bookkeeping, 1420 water-city API, 1740 EnumMap conversion,
1900 area identity refactor, 1953 IDInfo validation, and later area partition cleanup ->
cross-file ownership/topology checks -> whole-album dedup -> promote F644 only for a
distinct current-live root -> freeze the next physical WIP at the next settled boundary
whether F644 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP479 -- QUEUE 060 CvArea.cpp -- CLOSED / F644 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP478.
- Queue 060 `CvGameCoreDLL/CvArea.cpp` is CLOSED.
- Queue-060 confirmed root:
    F643 / provisional KI#965
- F644 / provisional KI#966 remains intentionally UNUSED.
- Files complete: 39 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` (673 queued lines).
- No gameplay source was modified.

FINAL CVAREA SATURATION PASS
----------------------------
The remaining bodies and all indexed CvArea history strata were checked after the
recycled-target lead was closed below threshold.

1. Core scalar counters
   `m_iNumTiles`, owned tiles, river edges, units, cities, population and starting plots
   remain paired with their current mutation sources. Negative temporary area-rebuild
   states are either explicitly tolerated under WorldBuilder/recalculation or belong to
   the already-audited dormant `CvPlot::processArea(-1)` shape.

2. Water-area city semantics
   `getCitiesPerPlayer(player, false)` returning zero on water is deliberate and widely
   relied upon to distinguish land city ownership from adjacent-coast bookkeeping.
   Callers that need adjacent coast explicitly pass `true`.
   F643 is the distinct regression where practical 3898 changed the Barbarian selector
   to a WATER spawn-plot context but continued asking this generic land-oriented API.

3. Historical Barbarian city count
   generic positive `changeCitiesPerPlayer` increments remain exact F589/KI#912
   ownership; no second root was split here.

4. Lake / representative-area state
   cached lake transition and normalization problems are owned by F634/KI#956;
   scenario pre-feature area topology is owned by F636/KI#958;
   dynamic Ice invalidation remains F025/KI#346.
   `canBeEntered`'s representative-area fast rejection is consistent once those caches
   are current.

5. Player/team-indexed derived state
   - `m_aiBestFoundValue` has the previously proven mandatory new-player refresh.
   - `m_aTargetCities` was re-investigated in WIP478 and remains stale storage but not a
     demonstrated supported colony-lifecycle defect because the war-plan event that
     makes UWAI alignment relevant synchronously refreshes area targets first.
   - AreaAI itself is recomputed in team AI turn setup.
   No additional F477/F479-style recycled-slot defect survived.

6. Building-derived area effects
   good/bad health, happiness, free specialists, trade routes, clean power, border
   obstacles and yield modifiers pair with current city/building process paths.
   Clean-power state changes update team commerce/power-health; first border-obstacle
   activation verifies unit placement. No unmatched live decrement/add path was found.

7. Unit-AI counters
   trainable UnitAI and actual UnitAI counters preserve nonnegative invariants in current
   city/unit process paths; `NO_UNITAI` aggregation intentionally sums concrete UnitAI
   buckets rather than indexing the map with a negative enum.

8. Bonus counts
   current `m_aiBonuses` bookkeeping and `getNumTotalBonuses` EnumMap total agree with
   plot processing. No distinct resource-count root beyond F643's wrong AREA CONTEXT was
   established.

9. Serialization
   current SAS read/write order is internally paired.
   The only executable difference from supplied Base AdvC 1.14 is SAS's intentional
   KI#46/current-version-only removal of historical `uiFlag < ...` compatibility
   branches. Current writer still emits uiFlag=4 and the current reader consumes that
   current layout directly. No same-version mismatch exists.

10. Base/K-Mod/Civ4CE comparison
   current non-serialization CvArea executable behavior tracks supplied Base AdvC 1.14.
   K-Mod provides the pre-AdvC ancestry for ordinary area counters/AI fields;
   Civ4CE provides the Firaxis/BtS baseline. No later Base-1.14 repair was found missing
   from SAS.

HISTORY CLOSURE
---------------
The CvArea path-history index contains 62-ish touches. High-value semantic strata:
- practical 1253: initial AdvC area bookkeeping, Barbarian area state, trade routes;
- practicals 1274/1275: cached lake / representative-area partition;
- practical 1420: water-area city API correction (`false` -> `0`);
- practical 1637: canBeEntered performance/partition fast path;
- practical 1740: EnumMap conversion;
- practical 1794/1795: EnumMap total / obsolete improvement-state removal;
- practical 1900: area identity/ref getter cleanup;
- practical 1953: serialized target-city owner validation;
- practical 2033: reproducibility write markers;
- practical 2186: inlining/refactor only;
- practical 2361/3131/3579/3760: representation/refactor cleanup;
- practical 3183: enum-map rewrite;
- later SAS touches are save-policy/comments/formatting rather than new CvArea gameplay.

No unported semantic repair survives as F644.

QUEUE-060 ROOT LEDGER
---------------------
F643 / KI#965
    practical 3898 changed Barbarian selector context from parent land area to physical
    spawn plot. Shelf and transport calls therefore use water-area resource state and
    cargo terrain, suppressing current resource-gated sea/cargo units and all mounted
    cargo on the ship's water tile.

F644 / KI#966
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 060 is closed for this pass. Remaining sharp edges are:
- already-owned findings;
- stale-but-self-healed or lifecycle-blocked robustness state;
- intentional water-area abstraction;
- old-save compatibility intentionally removed by KI#46;
- dormant unsupported negative area mutation.

None clears the independent current-live threshold for F644.

EXACT CONTINUATION
------------------
C031-WIP479 physical -> Queue060 CLOSED -> F643 confirmed ->
F644/KI#966 UNUSED -> open priority Queue062
`CvGameCoreDLL/MilitaryAnalyst.cpp` (673 lines) -> establish SAS/Base AdvC/K-Mod/Civ4CE
provenance and caller/history map -> audit constructor scenario sets, war/peace
component membership, city conquest/loss aggregation, gained/lost power, score and
utility-export state, map/team/player identity, branch/state lifetime, arithmetic and
null/index contracts -> dedup especially against F269/F641/F642 and prior UWAI roots ->
promote F644 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F644 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP480 -- QUEUE 062 MilitaryAnalyst.cpp -- F644 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP479.
- Queue 060 `CvArea.cpp` remains CLOSED with F643/KI#965.
- Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` is OPEN.
- F644 / provisional KI#966 is CONFIRMED here.
- F645 / provisional KI#967 is UNUSED.
- No gameplay source was modified.

QUEUE-062 PROVENANCE BASELINE
-----------------------------
Current SAS `MilitaryAnalyst.cpp` has 670 physical lines (queue snapshot counted 673).
Compared with supplied Base AdvC 1.14, executable differences are limited to already
reconciled SAS fixes / refactors, notably:
- KI#442: `m_capitulationsAcceptedPerTeam` is indexed by `TEAMID(ePlayer)` rather than
  raw PlayerTypes;
- KI#433: member predicted game score no longer repeatedly adds team-shared accepted-
  vassal score; the consumer aggregates that once at team scope;
- formatting/name cleanup.

The nuclear simulation examined for F644 is otherwise inherited from Base AdvC 1.14.
K-Mod/Civ4CE do not contain this full MilitaryAnalyst/UWAI layer; AdvC's own history is
the primary lineage oracle.

-------------------------------------------------------------------------------
F644 / provisional KI#966 -- MilitaryAnalyst chooses the assumed Tactical-Nuke
mix from the DEFENDER team's human/AI status, although tactical evasion belongs
to the incoming ATTACKER's nuke arsenal
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC/UWAI side-identity bug, current-live whenever simulated nuclear
war meets nonzero interception and the two sides differ in human/AI status. It changes
predicted nukes hitting cities and therefore nuclear-war utility.

CURRENT HELPER
--------------
The file-local helper takes only one TeamTypes parameter:

    scaled nukeChanceToHit(TeamTypes eTeam)

It computes two conceptually different pieces of state from that SAME team:

1. target-side interception:
       max(GET_TEAM(eTeam).getNukeInterception(),
           master.getNukeInterception())

2. assumed incoming weapon mix:
       rTactRatio =
           GET_TEAM(eTeam).isHuman() ? 0.5 : 1/3

It then treats Tactical Nukes as having hardcoded 50% evasion and calculates:

    hit probability =
        1 - interceptionProb *
            ((1 - tacticalRatio) +
             tacticalRatio * (1 - evasionProb))

Interception belongs to the DEFENDER. The tactical-vs-ICBM mix belongs to the ATTACKER.
One team argument cannot correctly supply both whenever those teams differ.

CALL-SITE PROOF
---------------
`simulateNuclearWar` confirms that `eTeam` is always the side being HIT:

    // enemy nukes fired on us:
    rHittingUs =
        rFiredOnUs * nukeChanceToHit(kAgent.getID());

    // our nukes fired on enemy:
    rHittingEnemy =
        rFiredByUs * nukeChanceToHit(kEnemy.getTeam());

Thus:
- when ENEMY fires at US, US's human/AI status determines the assumed percentage of
  Tactical Nukes in ENEMY's incoming arsenal;
- when WE fire at ENEMY, ENEMY's human/AI status determines the assumed percentage of
  Tactical Nukes in OUR arsenal.

This reverses the ownership of exactly the state that affects evasion.

HISTORY / INTENT ORACLE
-----------------------
Practical 1253, the initial MilitaryAnalyst/UWAI implementation, introduced:

    double MilitaryAnalyst::interceptionMultiplier(TeamTypes tId)

with calls already passing the defender:
    hitUs  = firedOnUs * interceptionMultiplier(agent.getID());
    hitCiv = firedByUs * interceptionMultiplier(civ.getTeam());

The original helper comment said:

    "Assume 1/3 of their nukes are Tactical, which can't be intercepted"

That sentence identifies the tactical ratio as a property of the INCOMING nukes, while
`tId` is demonstrably the defending side. The side mismatch therefore dates to the
initial implementation.

Practical 1280 revised tactical evasion to 50% and introduced different assumed weapon
mixes:

    tactRatio = (GET_TEAM(tId).isHuman() ? 0.5 : 0.33)

The intended behavioral distinction is reasonable by itself -- humans may build/use a
different Tactical-Nuke share than AI -- but it was keyed to the already-wrong defender
parameter.

The later scaled-number refactors (2920/2923/2954/2957 and merge snapshots) preserve
the same call orientation. Current Base AdvC 1.14 retains it unchanged.

CLEAN NUMERIC FIXTURE
---------------------
Take:
    attacker = AI team
    defender = human team
    defender interception = 50%
    defender master interception <= 50%
    hardcoded Tactical evasion = 50%

Current code sees the HUMAN DEFENDER and assumes the ATTACKER's incoming arsenal is
50% Tactical:

    intercepted exposure =
        (1 - 0.5) + 0.5 * (1 - 0.5)
        = 0.5 + 0.25
        = 0.75

    current hit chance =
        1 - 0.50 * 0.75
        = 0.625
        = 62.5%

Under the helper's own policy, an AI ATTACKER should use the AI tactical ratio 1/3:

    exposure =
        2/3 + (1/3)*0.5
        = 5/6

    correct-policy hit chance =
        1 - 0.50 * 5/6
        = 7/12
        ~= 0.583333
        = 58.33%

Error:
    +4.1667 percentage points in hit probability.

Reverse the identities:
    human attacker -> AI defender

Current code uses defender AI ratio 1/3 and predicts ~58.33%, while the intended human-
attacker mix gives 62.5%. The same physical interception level and nuke counts therefore
change in the WRONG direction according to who is being attacked.

At 100% interception the difference doubles to:
    25% hit chance for 50%-Tactical incoming arsenal
    vs
    16.67% for 1/3-Tactical incoming arsenal,
an 8.33-point absolute error.

CURRENT-LIVE ACTIVATION
-----------------------
The nuclear simulation runs after every conventional MilitaryAnalyst simulation unless
the agent is eliminated / cityless. It uses current UWAICache NUCLEAR unit counts for
both sides.

No exotic custom data is needed:
- current SAS/BtS has ICBMs and Tactical Nukes;
- SDI/building/team effects provide nonzero nuke interception;
- ordinary single-player war evaluation naturally has an AI team and a human team.

The helper intentionally models a MIX of Tactical and non-Tactical nukes rather than
reading exact unit composition, so the finding is not that the ratio is approximate.
The defect is that the chosen approximation is keyed to the wrong side.

LIVE CONSEQUENCE
----------------
The contaminated hit chance scales:

    rHittingUs
    rHittingEnemy

Those values are then stored as:
- PlayerResult nukes suffered;
- `m_nukedCities[attacker][victim]`.

WarUtilityAspect consumers translate predicted nuclear hits into city/asset losses,
fallout effects and nuclear utility. Therefore defender human/AI status can change the
forecasted effectiveness of an unchanged ATTACKER arsenal even when interception and
all military facts are held constant.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `nukeChanceToHit`;
- `interceptionMultiplier`;
- Tactical-Nuke ratio / Tactical percentage;
- defender-vs-attacker nuke mix;
- MilitaryAnalyst nuclear interception

find no prior owner for this side-identity root.

Existing nuke findings elsewhere concern target legality, counting/building nukes,
interception display/executor behavior, AI nuke targeting or other consumers. F644 is
specifically the UWAI MilitaryAnalyst forecast choosing the INCOMING weapon mix from
the VICTIM identity.

REPAIR BOUNDARY
---------------
The helper needs both sides, or the tactical ratio needs to be passed separately.

Conceptually:

    nukeChanceToHit(
        TeamTypes eAttacker,
        TeamTypes eDefender)

Then:
- defender/master determines `rInterceptionProb`;
- attacker determines the assumed Tactical-Nuke share;
- evasion remains the chosen policy/constant unless separately modernized.

Calls become conceptually:

    rHittingUs =
        rFiredOnUs *
        nukeChanceToHit(kEnemy.getTeam(), kAgent.getID());

    rHittingEnemy =
        rFiredByUs *
        nukeChanceToHit(kAgent.getID(), kEnemy.getTeam());

A stronger future model could derive Tactical-vs-ICBM share from actual cached nuclear
unit classes instead of human/AI heuristic. That is optional tuning; merely assigning
the existing heuristic to the firing side repairs F644.

VALIDATION TARGETS
------------------
1. AI attacker -> human defender, 50% interception:
   use AI 1/3 Tactical ratio; expected current-policy hit ~=58.33%.
2. Human attacker -> AI defender, same 50% interception:
   use human 1/2 Tactical ratio; expected hit =62.5%.
3. Swap only defender human/AI while keeping attacker identity and interception equal:
   hit chance must remain unchanged.
4. Swap only attacker human/AI:
   current heuristic intentionally changes assumed Tactical share and therefore hit
   chance.
5. 0% interception:
   hit chance remains 100% regardless tactical ratio.
6. 100% interception:
   verify 1/3-Tactical -> 16.67% and 1/2-Tactical ->25% with 50% evasion.
7. Master/vassal interception maximum remains defender-side logic.
8. Both `rHittingUs` and `rHittingEnemy` call with correct attacker/defender orientation.
9. Nuclear WarUtility changes only through corrected hit forecasts; nuke-count and
   portion-fired heuristics remain otherwise unchanged.

POST-F644 LEDGER / EXACT RESUME
-------------------------------
Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` -- OPEN.
F644 / provisional KI#966 -- CONFIRMED.
F645 / provisional KI#967 -- UNUSED.

Exact continuation:
C031-WIP480 physical -> Queue062 OPEN -> exclude F644 plus KI#433/KI#442 and all prior
UWAI/nuclear owners -> audit constructor war-set construction, defensive-pact expansion,
peace removal, uninvolved/future parties, phase timing and comparability -> finish
simulateNuclearWar target weighting/denominators/total-war classification ->
prepareResults city-cache lookup, population/score aggregation, war table,
capitulation/team membership and elimination semantics -> lost/gained power and utility
export contracts -> exhaust MilitaryAnalyst path history and whole-album dedup ->
promote F645 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F645 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP481 -- QUEUE 062 MilitaryAnalyst.cpp -- F645 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP480.
- Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` remains OPEN.
- F644 / provisional KI#966 remains CONFIRMED.
- F645 / provisional KI#967 is CONFIRMED here.
- F646 / provisional KI#968 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F645 / provisional KI#967 -- post-conventional nuclear simulation still uses
real pre-simulation enemy existence/cities/nukes, so a civ eliminated by the
InvasionGraph can continue firing and receiving nukes
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited UWAI simulation-state leak. Current-live whenever the conventional
MilitaryAnalyst predicts complete conquest/elimination of an enemy that has nuclear
weapons. Base AdvC 1.14 retains it.

SIMULATION ORDER IS EXPLICIT
----------------------------
The MilitaryAnalyst constructor executes:

    phase 1 conventional simulation (optional)
    phase 2 conventional simulation
    prepareResults();       // conventional-war result materialization
    simulateNuclearWar();   // only afterward

`prepareResults` has already populated each node's city losses/conquests and enables
`isEliminated(PlayerTypes)`:

    lost = node city losses
    eliminated =
        lost.size() == GET_PLAYER(ePlayer).getNumCities()

Thus, when nuclear simulation begins, the graph already knows which players the
conventional forecast has removed.

ASYMMETRIC ELIMINATION CHECK
----------------------------
`simulateNuclearWar` starts with:

    if (isEliminated(eWe) || kWe.getNumCities() <= 0)
        return;

So the analyst civ correctly stops all nuclear modeling if IT was conventionally
eliminated.

But inside the enemy loop there is no matching:

    if (isEliminated(kEnemy.getID()))
        continue;

Instead, every enemy that remains in the precomputed war table can still supply:

    int iEnemyNukes =
        kEnemy.uwai().getCache().getPowerValues()[NUCLEAR]->numUnits();

and participates in all later nuclear arithmetic using the real current CvPlayer.

WHY THE ELIMINATED ENEMY REMAINS IN THE LOOP
--------------------------------------------
`prepareResults` builds `m_warTable` from:
- wars continued;
- wars declared by;
- wars declared on.

Those sets describe the scenario diplomacy/war relation, not whether a participant has
subsequently lost all cities in the graph. Conquest does not mutate real CvTeam war
state and `m_warTable` is not cleared on elimination.

Therefore a conventionally eliminated enemy can still satisfy:

    isWar(eWe, kEnemy.getID())

and enter nuclear simulation normally.

DIRECT CONSEQUENCE 1 -- DEAD ENEMY STILL FIRES REAL CACHED NUKES
-----------------------------------------------------------------
For that eliminated enemy current code computes:

    iEnemyNukes = REAL UWAICache nuclear unit count

then:

    rFiredOnUs =
        portionFired * ... *
        iEnemyNukes / sqrt(rEnemyTargets)

and can add:

    playerResult(eWe).addNukesSuffered(...)
    m_nukedCities[kEnemy][eWe] += ...

So a civilization that the immediately preceding simulation says has zero surviving
cities can still launch its unchanged pre-simulation nuclear arsenal and inflict
additional future nuclear losses on the winner.

This does not require assuming that every unit should disappear exactly with the last
city in live Civ4. The MilitaryAnalyst's own `isEliminated` semantic treats loss of all
cities as elimination for score/utility purposes; it already returns 0 predicted score
for that player. Continuing to grant that same modeled-eliminated player an ordinary
future strategic nuclear phase is internally inconsistent.

DIRECT CONSEQUENCE 2 -- WE FIRE AT NONEXISTENT ENEMY CITIES
------------------------------------------------------------
The reciprocal side also remains active:

    rFiredByUs.decreaseTo(
        1.5 * kEnemy.getNumCities());

`kEnemy.getNumCities()` is the REAL current pre-simulation city count, not the graph's
remaining-city count.

For an enemy with:
    real cities before simulation = N
    simulated city losses          = N

MilitaryAnalyst says:
    isEliminated(enemy) = true
    predicted member score = 0

yet nuclear simulation still permits up to:
    1.5 * N

expected nuke launches/hits toward that eliminated civ and records nukes suffered by it.

DIRECT CONSEQUENCE 3 -- DEAD ENEMIES DILUTE OUR TARGET SHARE
-------------------------------------------------------------
Before iterating enemies, `rOurTargets` counts every player for which:

    isWar(eWe, player)

with no elimination filter.

Our finite `iOurNukes` are later divided by `rOurTargets`.

Therefore an enemy already eliminated by the conventional forecast can continue to
consume target weight and REDUCE the modeled number of our nukes fired at surviving
enemies.

This means F645 is not only "extra bookkeeping on a dead player"; it can change nuclear
losses assigned to still-live rivals and therefore alter the analyst's own utility.

PARTIAL-LOSS BREADTH
--------------------
The same stale-state seam exists before complete elimination.

If an enemy begins with 10 real cities and conventional simulation predicts loss of 6,
the post-conventional state has 4 surviving cities. Nuclear cap still uses:

    1.5 * 10

instead of a graph-consistent remaining-city count based on 4.

Similarly, our own cap uses `kWe.getNumCities()` after only checking whether ALL our
cities were lost. If we lost some but not all, the model still caps enemy nuclear fire
against our full real pre-simulation city count.

The complete-elimination fixture is the strongest proof; partial-loss caps are same-root
breadth.

CLEAN CURRENT-LIVE FIXTURE
--------------------------
Let:
    A = analyst civ
    B = enemy civ
    A and B are at war in the MilitaryAnalyst scenario
    B currently owns 1 city and at least 1 cached nuclear unit

Conventional InvasionGraph predicts A conquers B's only city during phase 1/2.

After `prepareResults`:
    isEliminated(B) == true
    predictedMemberGameScore(B) == 0

Current nuclear phase nevertheless:
    - `isWar(A,B)` remains true through m_warTable;
    - reads B's real cached nuclear unit count;
    - computes B nuclear fire on A;
    - permits A nuclear fire at up to 1.5 * B's REAL one-city count.

If another surviving enemy C is also at war with A, B additionally remains in
`rOurTargets`, reducing A's nuke share allocated toward C.

No exotic API or invalid state is needed: the entire contradiction is produced by one
ordinary MilitaryAnalyst run.

LINEAGE
-------
Practical 1253 introduced the architecture in essentially the same order:
- simulate conventional invasion;
- prepare results;
- simulate nuclear war;
- early-return only if "we" are gone/cityless;
- iterate enemies using real CvPlayer nuclear counts and city counts.

Later nuclear refinements adjust total-war portion, interception/evasion, vassal
multipliers and scaled arithmetic but do not make enemy existence/city caps
simulation-aware.

Current Base AdvC 1.14 retains this asymmetry unchanged.

WHOLE-ALBUM DEDUP
-----------------
No prior album finding owns post-conventional MilitaryAnalyst nuclear fire by a
graph-eliminated enemy.

Related findings are distinct:
- F641/KI#963: ArmamentForecast concurrent production reads real pre-loss city topology
  after graph-local conquests. That defect occurs DURING conventional buildup and has a
  different consumer/interface/repair.
- F269/KI#592: InvasionGraph battle-area city counts after graph-local loss.
- F642/KI#964: unrelated-war ArmamentForecast receives agent-target scenario parameters.
- F644/KI#966: Tactical-Nuke mix is keyed to defender rather than attacker.

F645 survives all of those fixes. Even with perfectly simulated conventional city
topology and correct nuke hit chance, `simulateNuclearWar` still needs to skip dead
players and use remaining simulated city state.

REPAIR BOUNDARY
---------------
Before nuclear arithmetic, derive graph-consistent surviving-player/city state from the
already-prepared conventional results.

At minimum:
1. skip any enemy for which `isEliminated(enemy)` is true;
2. exclude eliminated enemies from `rOurTargets` and `rEnemyTargets`;
3. cap nuclear fire using REMAINING simulated city counts:
       current real city count - graph city losses
   for both attacker/defender sides.

Prefer a small helper such as:
    simulatedRemainingCities(PlayerTypes)

rather than repeating:
    real city count - lostCities(player).size()

and use that helper consistently in:
- initial "we are gone" check;
- enemy skip;
- `rFiredOnUs` city cap;
- `rFiredByUs` enemy city cap;
- target-weight inclusion where elimination matters.

Do not mutate real CvPlayer/CvCity state. This is an analysis-local forecast.

A stronger future model may also remove nuclear units lost in conventional battle/city
capture. F645 does not require modeling exact nuke-unit casualties; the hard minimum is
that a player the model itself declares eliminated cannot continue an ordinary future
nuclear exchange.

VALIDATION TARGETS
------------------
1. Enemy B loses all cities conventionally:
   B contributes no subsequent nuclear launches, target weight or suffered-city cap.
2. B loses some but not all cities:
   nuclear cap uses simulated remaining city count.
3. Analyst A loses some cities:
   enemy fire cap uses A's simulated remaining count.
4. Analyst A loses all cities:
   preserve current early return.
5. Two enemies B/C; B eliminated, C survives:
   B no longer dilutes `rOurTargets`; A's finite nuke allocation toward C increases
   accordingly.
6. Eliminated B with zero nukes should still be excluded from target weights/caps.
7. Surviving B with nukes follows existing total-war/vassal/interception policy.
8. F644 attacker/defender orientation fix composes independently.
9. Predicted eliminated player's member score remains zero and is now consistent with
   nuclear behavior.

POST-F645 LEDGER / EXACT RESUME
-------------------------------
Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` -- OPEN.
F644 / provisional KI#966 -- CONFIRMED.
F645 / provisional KI#967 -- CONFIRMED.
F646 / provisional KI#968 -- UNUSED.

Exact continuation:
C031-WIP481 physical -> Queue062 OPEN -> exclude F644/F645 plus F269/F641/F642,
KI#433/KI#442 and all prior UWAI owners -> finish nuclear target-denominator and
total-war/vassal controls -> audit constructor side/DP/future-war set algebra,
peace-removal symmetry and phase comparability -> prepareResults cache/null contracts,
population score, capitulation unions and war table -> result/query helpers and
elimination/team semantics -> exhaust MilitaryAnalyst history -> whole-album dedup ->
either promote independent F646 or close Queue062 cleanly -> freeze the next physical
WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP482 -- QUEUE 062 MilitaryAnalyst.cpp -- CLOSED / F646 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP481.
- Queue 062 `CvGameCoreDLL/MilitaryAnalyst.cpp` is CLOSED.
- Queue-062 confirmed roots:
    F644 / provisional KI#966
    F645 / provisional KI#967
- F646 / provisional KI#968 remains intentionally UNUSED.
- Files complete: 40 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 064 `CvGameCoreDLL/MilitaryBranch.cpp` (623 queued lines).
- No gameplay source was modified.

FINAL POST-F645 SATURATION PASS
-------------------------------
The remainder of MilitaryAnalyst was re-read mechanically and against every indexed
history stratum before closure.

1. `prepareResults` city-cache dereference
   ---------------------------------------
   The apparently unsafe:

       kOurCache.lookupCity(*it)->city()

   for conquest/loss sets is backed by an InvasionGraph invariant.

   Every city that can become a real simulation city-attack target passes through
   `Node::targetCity` / `isValidTarget`, which requires:

       m_kAgentCache.lookupCity(kCacheCity.getID()) != NULL

   `m_kAgentCache` is precisely the analyst/agent UWAICache later referenced by
   MilitaryAnalyst::prepareResults.

   Successful `SimulationStep` city attacks add only that already-sanitized
   `UWAICache::City` to:
       defender m_cityLosses
       attacker m_conquests

   No independent write path inserts arbitrary city IDs into those result sets.

   This explains why older revisions (e.g. around practical 2605) carried defensive
   NULL checks while the current code can dereference directly. Under the current
   graph-target contract, the lookup is not a demonstrated F646.

2. War-table symmetry
   ------------------
   `m_warTable[A][B]` is true when A's result records B as:
       war continued,
       war declared by A,
       or war declared on A.

   Every modeled declaration is stored reciprocally:
       attacker gets DoWBy(target)
       target gets DoWOn(attacker)

   and existing wars are recorded for every player/enemy pair.

   Thus player-level table orientation is symmetric for the modeled relation; the
   TeamTypes overload's use of team leader IDs is adequate because war relation is
   team-wide and every team-member pair receives the same relation.

3. Defensive-pact / future-party construction
   -------------------------------------------
   A potential target's defacto defensive-pact allies are not merely added to report
   bookkeeping. During the first known-player pass they are also inserted into
   `ourFutureOpponents`, which is later:
       - included in `uninvolved`,
       - added to the InvasionGraph,
       - and introduced through `addFutureWarParties`.

   The later `theirDPAllies` pass supplies the corresponding DoW result sets.
   No current divergence between graph participation and result bookkeeping was found.

4. Peace-removal / no-war-vs-extra handling
   ----------------------------------------
   Practical 1335's special `isNoWarVsExtra` handling is present:
       - remove only the extra-target difference in the peace scenario;
       - keep future-war-party insertion for comparability;
       - force target refresh after peace-side removals.

   Practical 2770's capitulation-specific removal remains explicitly incomplete in
   source comments ("misses the wars that we need to declare on the enemies of theyId").
   This is a known model limitation rather than a newly demonstrated implementation
   contradiction from Queue 062; no independent current fixture beyond that declared
   incompleteness was promoted.

5. Nuclear denominator arithmetic
   ------------------------------
   `rEnemyTargets` cannot be zero for an enemy that enters the loop: the enemy is in a
   modeled war with `eWe`, and the major-player target pass therefore includes at least
   the analyst player (subject to ordinary vassal weighting).

   The vassal multipliers and target weights are heuristic policy. Their remaining
   simulated-elimination/city-count inconsistency is already F645 breadth.

   F644 owns attacker-vs-defender identity for tactical-nuke mix.

6. Later nuclear bugfix lineage
   ----------------------------
   Important upstream fixes are all present:
   - practical 2928 changes the per-enemy zero-nuke branch from `return` to `continue`,
     so one nuclear-empty opponent no longer aborts analysis of all later opponents;
   - upstream 1.12 / SAS merge carries the corrected enemy-vassal multiplier:
         if (kEnemy.isAVassal())
     rather than mistakenly testing kAgent twice;
   - the corrected enemy-target relation is:
         isWar(kEnemyOfEnemy.getID(), kEnemy.getID())
     rather than self-war.
   No later Base-1.14 nuclear correction is absent from current SAS.

7. Score projection / capitulation
   -------------------------------
   Current SAS already contains the recent dedicated repairs:
   - KI#442 maps each member node's accepted-capitulation set into
     `m_capitulationsAcceptedPerTeam[TEAMID(ePlayer)]`;
   - KI#433 keeps `predictedMemberGameScore` member-local and leaves the team-shared
     accepted-vassal score addition to the team-level KingMaking consumer.

   `isEliminated` and `hasCapitulated` are graph-local queries and line up with current
   consumers. The post-elimination nuclear contradiction remains F645 rather than a
   score-helper defect.

8. Result/query helper contracts
   -----------------------------
   Lazy `PlayerResult` allocation is consistently guarded by the public accessors.
   lost/gained power and military-production queries return zero for players without a
   graph node.
   City logging uses graph-result IDs that come from the same sanitized current-city
   cache path.
   No independent null/index/lifetime defect survived.

HISTORY CLOSURE
---------------
All MilitaryAnalyst history entries in the supplied path index were classified.

High-value semantic strata:
- practical 1253: initial UWAI MilitaryAnalyst/InvasionGraph architecture; birth of the
  side-mixed nuclear helper later confirmed as F644 and the stale post-conventional
  nuclear-state architecture later confirmed as F645;
- practical 1280: tactical-nuke evasion / human-vs-AI ratio refinement, preserving
  F644's wrong-side identity;
- practical 1335: no-war-vs-extra peace-scenario handling;
- practical 1386: vassal target-weight corrections;
- practical 1395: naming/refactor only in this file;
- practical 2770: capitulation peace-removal special case, explicitly noting its
  remaining model incompleteness;
- practical 2920: comprehensive scaled-number UWAI rewrite;
- practical 2928: `return` -> `continue` nuclear multi-opponent correctness fix;
- practical 3514: improved score projection;
- upstream 1.12 fixes merged around SAS 4370/4403/5851:
    enemy-vassal multiplier and enemy-target relation in nuclear analysis;
- SAS practical 6272: KI#433 team-vassal score de-duplication;
- SAS practical 6274: KI#442 TeamTypes indexing repair.

Merge/format/include/iterator changes and the remaining small strata do not introduce a
distinct current-live root.

QUEUE-062 ROOT LEDGER
---------------------
F644 / KI#966
    Tactical-Nuke ratio is chosen from the defending team's human/AI status although
    the weapon mix belongs to the firing side; at 50% interception this can shift the
    modeled hit probability from ~58.33% to 62.5% purely through victim identity.

F645 / KI#967
    nuclear simulation runs after conventional results but ignores enemy graph
    elimination/remaining-city state: a conventionally eliminated enemy can still fire
    real cached nukes, receive nukes against real pre-simulation city count, and dilute
    target allocation toward surviving enemies.

F646 / KI#968
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 062 is closed for this pass. Remaining concerns are:
- same-root breadth of F645 in partial city losses / target denominators;
- explicitly documented model approximations;
- already-repaired historical bugs;
- already-owned KI#433/KI#442;
- graph contracts that close apparent null/index hazards.

None clears the independent current-live threshold for F646.

EXACT CONTINUATION
------------------
C031-WIP482 physical -> Queue062 CLOSED -> F644-F645 confirmed ->
F646/KI#968 UNUSED -> open next selected priority Queue064
`CvGameCoreDLL/MilitaryBranch.cpp` (623 queued lines) -> establish SAS/Base AdvC 1.14,
K-Mod and Civ4CE provenance plus full caller/history map -> audit branch classification,
representative unit selection, unit counts/power/cost, HomeGuard/Army/Nuclear overlap,
trainability and observer-context state, branch update/cache lifetime, enum/index/null/
division/overflow contracts -> dedup especially against F214/F602/F641 and existing
MilitaryBranch/UWAICache roots -> promote F646 only for a distinct current-live root ->
freeze the next physical WIP at the next settled boundary whether F646 is consumed or
remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP483 -- QUEUE 064 MilitaryBranch.cpp -- F646 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP482.
- Queue 062 `MilitaryAnalyst.cpp` remains CLOSED with F644-F645.
- Queue 064 `CvGameCoreDLL/MilitaryBranch.cpp` is OPEN.
- F646 / provisional KI#968 is CONFIRMED here.
- F647 / provisional KI#969 is UNUSED.
- No gameplay source was modified.

QUEUE-064 PROVENANCE BASELINE
-----------------------------
Current SAS `MilitaryBranch.cpp` has 633 physical lines (queue snapshot counted 623).

Its executable divergences from supplied Base AdvC 1.14 are the recent, already-owned
SAS repairs:
- KI#537: invalidate typical-unit/power state before rescanning, including Nuclear and
  Fleet bombard capability;
- KI#560: include ordinary aircraft in actual branch census;
- KI#561 is consumed in the related InvasionGraph/HomeGuard split rather than a distinct
  new Queue-064 candidate.

The `canKnowTypicalUnit` logic proved below is unchanged from Base AdvC 1.14.
K-Mod/Civ4CE do not contain this full AdvC UWAI MilitaryBranch abstraction; AdvC's own
pre/post practical-1275 history is the decisive oracle.

-------------------------------------------------------------------------------
F646 / provisional KI#968 -- `canKnowTypicalUnit` reverses its prerequisite-tech
test, making tech-gated typical units automatically known while applying
observer uncertainty only to no-tech units such as Warrior
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC regression, current-live in UWAI observer estimates for rival
military branches. This candidate was deliberately parked from Queue 031/UWAICache and
is now promoted in its owning Queue 064.

CURRENT CODE / INTERNAL CONTRADICTION
-------------------------------------
`MilitaryBranch::canKnowTypicalUnit(TeamTypes eObserver)` is the gate for whether a
foreign observer receives exact typical-unit power/cost or deliberately underestimated
values.

After the self/no-observer/no-unit fast path, current code says:

    CvUnitInfo const& kTypicalUnit = GC.getInfo(getTypicalUnit());

    if (kTypicalUnit.getPrereqAndTech() != NO_TECH) // Warrior
        return true;

    if (GET_PLAYER(m_eOwner).getUnitClassCount(
            kTypicalUnit.getUnitClassType()) > 0)
        return true; // The unit's in the wild

    for (MemberIter itObs(eObserver); itObs.hasNext(); ++itObs)
    {
        if (itObs->canSeeTech(m_eOwner))
            return true; // Tech visible on Foreign Advisor
    }

    return false;

The condition and comment have opposite polarity:
- a Warrior-style no-tech unit has `getPrereqAndTech() == NO_TECH`;
- a tech-gated advanced unit has `getPrereqAndTech() != NO_TECH`.

As written, EVERY ordinary tech-gated typical unit returns true immediately. The checks
for whether the owner has actually fielded the unit or whether the observer can see the
owner's technologies are reached only for no-tech units.

That makes the intended information-hiding machinery almost exactly backwards.

CURRENT XML CONTROL
-------------------
Current SAS XML supplies a simple direct polarity check:

    UNIT_WARRIOR
        PrereqTech = NONE
        iCost      = 18
        iCombat    = 3
        iPower     = 3

    UNIT_ARCHER
        PrereqTech = TECH_HUNTING
        iCost      = 25
        iCombat    = 2
        iPower     = 4

Thus the source comment `// Warrior` belongs to the `== NO_TECH` case, not the current
`!= NO_TECH` case.

The exact advanced unit chosen as a branch's typical unit varies with civilization,
era, trainability and branch utility. F646 does not depend on Archer specifically:
once any selected typical unit has an ordinary prerequisite technology, current
`canKnowTypicalUnit` bypasses all observer-knowledge tests.

LIVE OBSERVER POLICY
--------------------
The two public consumers make the intended hidden-vs-known distinction explicit:

    getTypicalPower(eObserver):
        known   -> m_rTypicalPower
        unknown -> 0.80 * m_rTypicalPower

    getTypicalCost(eObserver):
        known   -> actual estimated cost
        unknown -> 0.85 * actual estimated cost

`CvTeam::canSeeTech` is not trivially true for every known rival. Outside self/vassal
visibility, it requires both teams alive/major and:
    met AND (observer can tech-trade OR rival can tech-trade).

Therefore ordinary early-game rivals can be known/met while their technology is not yet
visible. If the rival has researched a military technology and can train its unit but
has not yet fielded that unit class, all three intended "unknown" conditions exist:
- observer != owner;
- owner has zero units of the hidden typical class;
- observer members cannot yet see the owner's tech.

Current code still returns true solely because the unit HAS a prerequisite technology.

HISTORY -- EXACT BIRTH IN PRACTICAL 1275
----------------------------------------
The pre-practical-1275 implementation contained the anti-cheating rule directly inside
`updateTypicalUnit`.

For a candidate land unit it said, in substance:

    // Don't want the AI to magically know which units a civ can train
    // in the very early game, when techs aren't public knowledge,
    // and want humans to actually train an Archer to deter an AI rush.

    if (NO_TECH != unit.getPrereqAndTech() && // exclude Warrior
        (techs aren't publicly visible) &&
        owner has no unit of this class)
    {
        continue;
    }

This old condition is semantically coherent:
    prerequisite != NO_TECH
        -> this is NOT the always-known Warrior/no-tech case
        -> hide the unfielded unit while technology is private.

Practical 1275 deliberately removed that knowledge restriction from typical-unit
SELECTION and introduced observer-specific `getTypicalUnitPower(pov)`,
`getTypicalUnitCost(pov)` and `canKnowTypicalUnit(pov)` so the same cached branch could
be viewed differently by different observers.

But the newly introduced helper used:

    if (NO_TECH != getTypicalUnit()->getPrereqAndTech())
        return true; // Warrior

The old `NO_TECH !=` expression was carried over while its action changed from
"exclude this hidden tech unit" to "declare this unit known." The `// Warrior` comment
was retained, exposing the polarity inversion in the very commit that introduced the
new helper.

Later refactors:
- normalized comparison syntax;
- changed observer from PlayerTypes to TeamTypes;
- iterated all observer team members;
- expanded comments about power-curve inference;

but preserved the same `!= NO_TECH` return-true test. Base AdvC 1.14 still has it.

CURRENT-LIVE FIXTURE
--------------------
Take two met major civs:
    O = observer/analyst
    R = rival/branch owner

State:
- R has researched a technology enabling the branch's current best typical military
  unit U;
- R's capital/cities can train U, so `updateTypicalUnit` may legitimately select it;
- U has `PrereqAndTech != NO_TECH`;
- R has not yet built any unit of U's class;
- O cannot yet see R's technologies (`canSeeTech(R) == false`);
- O and R are on different teams.

This is precisely the information state the practical-1275 policy was designed for.

Expected under that policy:
    canKnowTypicalUnit(O) = false

so O sees:
    typical power = 80% of exact
    typical cost  = 85% of exact.

Current:
    U has a prerequisite tech
    -> first post-fast-path condition returns true
    -> exact power and exact cost are exposed immediately.

No RNG, custom map state, invalid enum or save migration is needed.

LIVE CALLERS / CONSEQUENCE
--------------------------
The observer-aware values are used throughout UWAI, not merely in debug text.

InvasionGraph calls, among others:
    getTypicalCost(TEAMID(m_eAgent))
    getTypicalPower(TEAMID(m_eAgent))

when translating rival production, army size, battle losses and garrison/left-behind
power into the analyst's forecast.

ArmamentForecast converts projected military production into branch power using:

    typicalPower(observer) / typicalCost(observer)

For a hidden typical unit, the intended ratio is:

    (0.80 * P) / (0.85 * C)
        = 0.941176... * (P/C)

Current F646 returns:
    P / C

so, relative to the intended hidden estimate, forecast power per production is:

    1 / 0.941176...
        = 1.0625

or 6.25% higher.

Other callers use typical power alone, so they can differ by the full intended 20%
uncertainty adjustment.

`WarUtilityAspect` also uses observer-aware typical power/cost to convert simulated
branch losses back into production-equivalent unit losses. Thus F646 can feed both
InvasionGraph tactical projections and final war-utility economics.

The design choice to underestimate both power and cost is heuristic; F646 is not a
critique of those percentages. The defect is that their knowledge gate is inverted and
therefore bypassed for the units it was introduced to hide.

WHOLE-ALBUM DEDUP / PARKED OWNERSHIP
------------------------------------
This exact candidate appears in earlier durable album checkpoints as a PARKED Queue-064
lead discovered while auditing Queue 031 UWAICache:

    "canKnowTypicalUnit prerequisite check appears inverted:
         if (kTypicalUnit.getPrereqAndTech() != NO_TECH) // Warrior
             return true;
     ...
     Do not consume a queue-031 root for that implementation-level queue-064 candidate."

It was intentionally NOT assigned an F/KI number there.

Whole-album search finds no confirmed finding that owns the prerequisite-polarity root.
It is therefore promoted now, in its owning file, as F646 rather than duplicated.

Nearby Queue-064 findings are distinct:
- KI#537: stale typical-unit candidate survives loss of all trainability;
- KI#560: ordinary aircraft omitted from actual branch unit census;
- KI#561: HomeGuard partition counted nuclear overlap incorrectly.

F646 reproduces after all of those are fixed.

REPAIR BOUNDARY
---------------
The narrow repair is the polarity indicated by both the comment and pre-1275 contract:

    if (kTypicalUnit.getPrereqAndTech() == NO_TECH) // Warrior / no-tech unit
        return true;

Then preserve the subsequent evidence checks:
- if owner already has a unit of that class -> known;
- if any observer-team member can see owner's tech -> known;
- otherwise -> unknown and apply the 80%/85% estimates.

A future refinement could account for units with unusual alternative tech structures,
free/civilization-specific availability or observed unit-class history more precisely.
That is optional. The current regression is the direct inversion of the primary-tech
test.

VALIDATION TARGETS
------------------
1. Observer == owner team -> exact values unchanged.
2. NO_TEAM observer -> exact values unchanged.
3. NO_UNIT typical -> existing safe behavior unchanged.
4. No-tech Warrior-style typical unit -> known immediately.
5. Tech-gated U, no fielded class, tech invisible -> canKnow=false.
6. Same U after first unit of its class is built -> canKnow=true.
7. Same U once observer can see owner's tech -> canKnow=true.
8. Multi-member observer team: visibility by any member should make U known.
9. Hidden branch:
       getTypicalPower = 0.80 * exact
       getTypicalCost  = 0.85 * exact.
10. ArmamentForecast hidden power/cost conversion should use the intended
    0.80/0.85 ratio rather than exact P/C.
11. Re-run early-game human-deterrence fixture from practical-1275 intent:
    an unfielded newly unlocked defender should not be magically known merely because
    the rival can train it.

POST-F646 LEDGER / EXACT RESUME
-------------------------------
Queue 064 `CvGameCoreDLL/MilitaryBranch.cpp` -- OPEN.
F646 / provisional KI#968 -- CONFIRMED.
F647 / provisional KI#969 -- UNUSED.

Exact continuation:
C031-WIP483 physical -> Queue064 OPEN -> exclude F646 plus KI#537/KI#560/KI#561 and
all prior MilitaryBranch/UWAICache owners -> continue every branch subclass:
typical-unit selection, sea expect-to-train, capital trainability, national/instance
limits, production-cost projection, unitPower exponentiation, actual reportUnit census,
HomeGuard partition, Army/Fleet/Logistics/Cavalry/Nuclear filters, bombard/soften flags,
serialization and factory/string enum contracts -> exhaust all MilitaryBranch path
history -> cross-check survivors against Base AdvC 1.14 and whole-album ownership ->
promote F647 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F647 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP484 -- QUEUE 064 MilitaryBranch.cpp -- CLOSED / F647 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP483.
- Queue 064 `CvGameCoreDLL/MilitaryBranch.cpp` is CLOSED.
- Queue-064 confirmed root:
    F646 / provisional KI#968
- F647 / provisional KI#969 remains intentionally UNUSED.
- Files complete: 41 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 068 `CvGameCoreDLL/KmodPathFinderLegacy.cpp` (551 queued lines).
- No gameplay source was modified.

POST-F646 SATURATION PASS
-------------------------
The remainder of Queue 064 was checked against live callers, current XML and the full
53-diff MilitaryBranch history set.

1. Observer-qualified `getTypicalUnit(eObserver)` ignores its parameter
   -------------------------------------------------------------------
   This is a real API/history smell but does not clear the current-live threshold.

   Practical 2920 added the observer argument, and the blockade consumer later says:
       "Let's cheat a little less (use eObserver arg)"
   before calling:
       kEnemyFleet.getTypicalUnit(eOurTeam)

   The inline getter still returns `m_eTypicalUnit` unconditionally.

   However the only live observer-qualified type caller is blockade evaluation around
   bombard-capable naval warfare. Current SAS unlocks tech visibility through Writing,
   and any current bombard-capable warship technology chain is far beyond Writing.
   `CvTeam::canSeeTech` therefore makes the enemy's technologies visible between met
   major teams in that supported state. No current hidden-warship type fixture survives.

   Keep this as interface/mod-robustness debt. F646 independently repairs the observer
   gate for power/cost, where hidden early land-unit states ARE current-live.

2. Fleet `canBombard` derives from only the typical ship
   -----------------------------------------------------
   The header says canBombard means whether ANY trainable unit in the branch can
   bombard, whereas Fleet::updateTypicalUnit sets it from only the chosen typical ship.

   A generic mod can therefore have:
       best typical fleet ship cannot bombard
       another trainable fleet ship can bombard
       -> canBombard false.

   Current SAS progression does not supply a clean supported fixture. The tempting
   late Carrier-vs-old-bombarder shape collapses because the Carrier technology path
   already makes a substantially stronger bombarding contemporary warship trainable
   on the same strategic-resource alternatives; that ship wins the Fleet utility/cost
   comparison instead. No current-data branch inversion is demonstrated.

3. `canEmploy` combat-only gate after KI#560
   -----------------------------------------
   KI#560 expanded actual Army census to ordinary aircraft (`getAirCombat()>0`), while
   `canEmploy` still requires `getCombat()>0`.

   This does not undo KI#560 because the only live `canEmploy` caller asks whether the
   *typical Army unit* is also Cavalry. Base `updateTypicalUnit` deliberately excludes
   DOMAIN_AIR from typical-unit selection, so that argument is a conventional land
   combat unit.

4. Production-cost approximation
   ------------------------------
   `estimateProductionCost` omits the full `CvPlayer::getProductionNeeded` modifier
   stack when choosing a typical unit, but current omitted terms do not yield a distinct
   supported branch-choice contradiction:
   - game-speed and start-era train percentages are common across candidate units;
   - player `UnitExtraCost` is currently used for founding units, which are excluded by
     the combat-strength gate;
   - world-unit handicap differences are excluded by the small national-limit gate;
   - current Python unit-cost override supplies no ordinary combat-unit differential.

   Per-instance unit costs, the non-common term that historically mattered, are already
   modeled by practical 2281.

5. Trainability
   ------------
   - sea typical units use `CvTeamAI::AI_isExpectingToTrain`, which scans all cities,
     checks actual city trainability/coast and requires enough production;
   - land typical units intentionally use the capital as a faster representative;
   - current ordinary military land units do not have a supported local-only building
     prerequisite that produces a capital-false/other-city-true contradiction;
   - NuclearArsenal likewise uses capital trainability; current ICBM/Tactical-Nuke
     enabling conditions are empire/team-resource/global-project conditions rather than
     a unique noncapital-city capability.

   These remain documented approximation surfaces, not F647.

6. Branch power / count contracts
   ------------------------------
   - Army actual census now includes air and nukes as intended;
   - HomeGuard receives conventional nonnaval power/count after KI#561's nuclear
     subtraction and splits it with a ratio clamped by the current unit-count inputs;
   - Army re-adds the separately counted nuclear headcount and capped nuclear power;
   - Fleet/Logistics/Cavalry/Nuclear overlap is intentional branch semantics;
   - Nuclear Army power and NuclearArsenal power use the same unmodified base-power
     transform when counted, so nuclear subtraction is coherent;
   - unit create/destroy callbacks pair through UWAICache, with the exceptional revival
     inventory already repaired by KI#548.

7. Arithmetic / enum / string / persistence
   -----------------------------------------
   - `estimateProductionCost` guards nonpositive output before division;
   - typical cost returns -1 for NO_UNIT and callers guard availability;
   - HomeGuard divides only when nonnaval unit count >0;
   - POWER_CORRECTION is positive current XML (160%) and receives nonnegative branch
     power;
   - debug-string array deliberately contains NUM_BRANCHES+1 entries, and `str(enum)`
     maps invalid or NUM_BRANCHES to the final "(unknown branch)" slot;
   - branch factory covers every real branch enum and returns NULL only for invalid IDs;
   - serialization's embedded save-version encoding correctly preserves NO_UNIT and
     current scaled values; owner identity is supplied by UWAICache when branch objects
     are constructed before `read`.

HISTORY CLOSURE
---------------
All 53 supplied diffs touching MilitaryBranch.cpp were classified.

High-value semantic strata include:
- practical 1253: initial branch architecture;
- practical 1275: observer-specific typical-unit knowledge refactor and exact birth of
  F646's prerequisite-polarity inversion;
- practical 1299/1304/1398: defensive-unit and limited/instance candidate filtering;
- practical 2113/2118: HomeGuard count/power split;
- practical 2281: per-instance unit-cost correction;
- practical 2719: custom-era factor support;
- practical 2920 plus 2921/2923/2954: comprehensive UWAI scaled rewrite and follow-up
  corrections;
- practical 2944: delay collateral-availability forecast until units exist/in production;
- practical 3571: nuke classification refactor;
- SAS practical 6295: KI#536-537 cache invalidation repairs;
- SAS practical 6306: KI#560-561 military census/HomeGuard nuclear partition repairs.

No unported Base AdvC 1.14 repair or independent current-live F647 remained.

QUEUE-064 ROOT LEDGER
---------------------
F646 / KI#968
    `canKnowTypicalUnit` reverses the primary-tech test, so tech-gated typical units are
    immediately "known" while the uncertainty checks are reached only for no-tech units.
    Practical 1275's own pre-refactor policy/comment is the direct oracle.

F647 / KI#969
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 064 is closed for this pass. The remaining sharp edges are either observer API
debt without current activation, mod-robustness gaps, explicit approximation policy or
already-owned military-cache fixes.

EXACT CONTINUATION
------------------
C031-WIP484 physical -> Queue064 CLOSED -> F646 confirmed ->
F647/KI#969 UNUSED -> open next selected priority Queue068
`CvGameCoreDLL/KmodPathFinderLegacy.cpp` (551 queued lines) -> establish current SAS
versus Base AdvC 1.14/K-Mod/Civ4CE provenance and call graph -> audit legacy A* node
initialization/reuse, parent/child pointers, open/closed-list ordering, path costs,
movement flags, validity callbacks, destination/source transitions, reset lifetime,
coordinate/index bounds and comparator arithmetic -> reconcile against current
KmodPathFinder replacement/legacy callers and full history -> whole-album dedup before
numbering -> promote F647 only for a distinct current-live root -> freeze the next
physical WIP at the next settled boundary whether F647 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP485 -- QUEUE 068 KmodPathFinderLegacy.cpp -- CLOSED / F647 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP484.
- Queue 068 `CvGameCoreDLL/KmodPathFinderLegacy.cpp` is CLOSED.
- F647 / provisional KI#969 remains intentionally UNUSED.
- Files complete: 42 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 072 `CvGameCoreDLL/CvStatistics.cpp` (482 queued lines).
- No gameplay source was modified.

WHY THIS QUEUE CLOSES WITHOUT A LIVE FINDING
--------------------------------------------
The file is a preserved verification oracle, not a live gameplay pathfinder.

`KmodPathFinderLegacy.h` hard-defines:

    #ifdef FASSERT_ENABLE
        #define VERIFY_PATHF 0
    #else
        #define VERIFY_PATHF 0
    #endif

and both the header class declaration and the entire `.cpp` implementation are wrapped
in:

    #if VERIFY_PATHF

Therefore current assert, debug and release builds all compile this implementation out.

`GroupPathFinder.cpp` likewise wraps all legacy cross-check calls in `#if VERIFY_PATHF`;
with the current header value they do not execute.

HISTORY ORACLE
--------------
The supplied history makes this intentional:
- practical 2587 preserved the old AdvC 0.98 K-Mod pathfinder as a legacy verifier;
- practical 2588 restructured the live pathfinder while retaining this oracle;
- practical 2805 fixed a bug in the legacy verifier's `bIsStart` argument
  (`knownCost != 0` -> `knownCost == 0`);
- practical 2861 introduced the `VERIFY_PATHF` test define;
- practical 3212 changed `#ifdef VERIFY_PATHF` to `#if VERIFY_PATHF`, specifically
  "Deactivate legacy pathfinding test properly";
- practical 4196 later mirrored the live danger-avoidance split into the verifier so it
  would remain a useful oracle if manually re-enabled.

The current source therefore contains known comments about generic A* limitations and
path-history cost propagation, but none are active in shipped/current gameplay.

MECHANICAL CONTROLS
-------------------
If manually enabled for developer testing:
- node-map allocation matches current map dimensions and is reset on source/group/flag
  changes;
- start/destination coordinates are asserted before node access;
- open-list iteration and parent/child rewiring are bounded by `NUM_DIRECTION_TYPES`;
- practical 2805's start-cost flag correction is present;
- practical 4196's two-stage danger/step-through validity logic matches the current
  July-2024 GroupStepMetric behavior;
- `iMaxPath` is normalized to MAX_INT in settings;
- heuristic weights are recalculated when destination changes;
- the legacy verifier intentionally documents that cost propagation can be imperfect
  in path-history-dependent cases. That is test-harness limitation, not live gameplay.

No current build path activates the verifier, so none of those surfaces clears the
album's current-live threshold for F647.

QUEUE-068 ROOT LEDGER
---------------------
F647 / KI#969
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 068 is closed. This file can still be useful as a developer oracle if
`VERIFY_PATHF` is intentionally re-enabled during assert testing, but it is dead code in
the current shipped configuration.

EXACT CONTINUATION
------------------
C031-WIP485 physical -> Queue068 CLOSED -> F647/KI#969 UNUSED ->
open priority Queue072 `CvGameCoreDLL/CvStatistics.cpp` (482 lines) ->
establish SAS/Base AdvC/K-Mod/Civ4CE provenance and caller/history map ->
audit event hooks, per-player/per-unit/per-building counters, timeline/year/turn
aggregation, serialization/reset/init lifetime, enum/index ownership, Python/UI
consumers and integer/overflow/null contracts -> whole-album dedup before numbering ->
promote F647 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F647 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP486 -- QUEUE 072 CvStatistics.cpp -- F647 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP485.
- Queue 068 remains CLOSED with no new root.
- Queue 072 `CvGameCoreDLL/CvStatistics.cpp` is OPEN.
- F647 / provisional KI#969 is CONFIRMED here.
- F648 / provisional KI#970 is UNUSED.
- No gameplay source was modified.

QUEUE-072 PROVENANCE BASELINE
-----------------------------
Current SAS `CvStatistics.cpp` is 482 physical lines and its normalized executable token
stream is byte-for-byte identical to supplied Base AdvC 1.14.

K-Mod 1.46 and Civ4CE carry the same underlying CvStatistics lifetime model; their
minor normalized differences from current AdvC are type/include/style evolution rather
than a player-reincarnation reset.

The path history is very small compared with adjacent queues:
- K-Mod inherited/introduced the statistics record and later made `getNumCitiesBuilt`
  a gameplay consumer through its city-name logic;
- AdvC touches are overwhelmingly include cleanup, enum casts and iterator/style
  refactors;
- no later Base AdvC repair resets a single player record on `CvGame::addPlayer`.

-------------------------------------------------------------------------------
F647 / provisional KI#969 -- a recycled dead PlayerTypes slot inherits the
extinct civilization's entire CvPlayerRecord because mid-game `addPlayer` resets
CvPlayer but never resets CvStatistics
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited BtS/K-Mod/AdvC lifetime bug. Current-live on the explicitly
supported human-colony fallback that reuses a defeated PlayerTypes slot after all
never-alive slots are exhausted.

This is distinct from F477/KI#800:
- F477 owns historical PlayerTypes VALUES stored in other map/city objects, which alias
  the new identity after slot reuse;
- F647 owns per-slot STATISTICS STATE that remains attached to the numeric slot because
  its sidecar record is never reinitialized.

PLAYER-REUSE PRODUCER
---------------------
`CvPlayer::getSplitEmpirePlayer` first tries a never-alive slot.

If no such slot exists, current SAS deliberately retains the old fallback for a HUMAN
splitter:

    /* Reusing a defeated player might not work correctly. advc:
       Allow human players to try it ... */
    if (!isHuman())
        return NO_PLAYER;

    for (PlayerIter<EVER_ALIVE> itPlayer; ...)
    {
        if (!itPlayer->isAlive())
        {
            eNewPlayer = itPlayer->getID();
            break;
        }
    }

`splitEmpire` then calls:

    CvGame::addPlayer(eNewPlayer, eBestLeader, eBestCiv);

and `CvGame::addPlayer` ultimately calls:

    GET_PLAYER(eNewPlayer).initInGame(eNewPlayer);

`initInGame` begins with:

    reset(eID);

and performs extensive player/team/map/city cleanup. It does NOT touch
`CvEventReporter`'s `CvStatistics` object.

STATISTICS LIFETIME
-------------------
`CvStatistics` owns:

    std::vector<CvPlayerRecord*> m_PlayerRecords;

indexed directly by PlayerTypes.

`getPlayerRecord(i)` lazily constructs a record only when the vector entry is missing.
If the extinct player X already had a record, reusing numeric slot X for new colony Z
therefore returns the SAME object.

`CvPlayerRecord::reset()` would correctly zero:
- victory;
- time;
- leader;
- every unit built/killed/lost counter;
- every building-built counter;
- every founded-religion flag;
- cities built;
- cities razed;
- golden ages.

But no per-player reset is called by `CvGame::addPlayer` or `CvPlayer::initInGame`.

The only exposed statistics reset currently does:

    CvEventReporter::resetStatistics()
        -> m_kStatistics.reset()
        -> destroy ALL player records

and is used for whole-game reset/load setup, not for adding one mid-game identity.

`CvEventReporter::newPlayer(ePlayer)` does not repair this even if the EXE invokes it:

    m_kStatistics.setLeader(ePlayer, GET_PLAYER(ePlayer).getLeaderType());

It overwrites only `m_eLeader`. Every accumulated counter remains from X.

EXTERNAL STATISTICS CONSEQUENCE
-------------------------------
The statistics interface exports the inherited record directly:

    victorytype
    timeplayed
    leader
    citiesbuilt
    citiesrazed
    goldenages
    unit_N_built
    unit_N_killed
    unit_N_lost
    building_N_built
    religion_N_founded

Therefore a newly created colony Z in recycled slot X can be reported as having:
- founded X's cities;
- built/lost/killed X's units;
- built X's buildings;
- razed X's cities;
- founded X's religions;
- entered X's Golden Ages.

New events for Z simply increment the already-nonzero arrays, permanently merging two
unrelated civilizations into one statistical history. Serialization preserves the
merged record because `CvStatistics::write/read` stores one record per ever-alive
numeric slot and has no identity-generation tag.

CURRENT GAMEPLAY CONSEQUENCE -- K-MOD CITY-NAME OFFSET
-------------------------------------------------------
This is not only server/report metadata.

K-Mod added direct read-only access from `CvPlayer`:

    CvPlayer::getPlayerRecord()

and current `CvPlayer::getCivilizationCityName` uses:

    iRandOffset = std::max(
        0,
        getPlayerRecord()->getNumCitiesBuilt() - getNumCityNames());

for the player's own civilization after the explicit city-name list has been checked.

Current SAS civilization XML has finite city-name lists (for playable examples:
Korea/Mali 30, Ireland 36, England 38, America 60, etc.).

Clean supported fixture:
1. Old civilization X occupies PlayerTypes slot P and over its lifetime founds N cities.
2. X is eliminated.
3. All never-alive player slots are exhausted.
4. Human player H creates a colony; the supported fallback reuses dead slot P for
   unrelated derivative civilization Z.
5. `CvPlayer::initInGame(P)` resets Z's `CvPlayer`, but `CvStatistics[P]` still says:
       citiesBuilt = N
6. Z's transferred cities are acquisitions, not newly founded Z cities, so the stale
   value is not a legitimate Z history.
7. Once Z exhausts/needs the fallback civilization-name lookup, its starting offset is
   calculated from X's N rather than Z's own founded-city count.

For example, with a 30-name derivative civ and old X having founded 40 cities:

    current inherited offset = 40 - 30 = 10

where a fresh Z record would begin with:

    0 - 30 -> clamped to 0.

Thus the new civilization's fallback city-name rotation is shifted by ten places solely
because an extinct unrelated civilization once occupied the same numeric slot.

This is a modest gameplay-visible manifestation, but it proves that the record is
authoritative DLL state rather than dead telemetry.

POTENTIAL STRONGER FAIRPLAY BREADTH
-----------------------------------
`WarUtilityAspect::FairPlay` also reads:

    kThey.getPlayerRecord()->getNumCitiesBuilt()

to infer whether a HUMAN rival has lost cities and should receive anti-dogpile
protection.

Current assets include the ChangePlayer system and `CvGame::changeHumanPlayer`, so a
human can in developer/auto-play supported workflows take control of another living
civ, including a colony. If such a recycled colony becomes human, stale X city counts
can exaggerate Z's apparent city losses and increase FairPlay cost.

This is real downstream breadth but is NOT required to confirm F647; the ordinary
human colony creation + cumulative statistics + city-name consumer already establishes
the root without requiring a human-switch workflow.

ANCESTRY
--------
The lifetime architecture is inherited from stock BtS/Civ4CE:
- statistics are indexed by PlayerTypes;
- the record persists for ever-alive players;
- `newPlayer` sets only leader;
- dead-slot colony reuse exists when no never-alive slot is available.

K-Mod preserves it and additionally makes `getNumCitiesBuilt` a gameplay input for city
naming.

AdvC's BBAI-derived `initInGame/resetPlotAndCityData` improves recycled-player cleanup
elsewhere but never adds a statistics reset. Current Base AdvC 1.14 is executable-
identical to SAS in `CvStatistics.cpp`.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- CvStatistics / CvPlayerRecord recycling;
- recycled `citiesBuilt`;
- statistics sidecar reset on addPlayer;
- colony statistics inheritance

find no prior confirmed owner.

F477/KI#800 shares the upstream trigger (reuse of a dead PlayerTypes slot) but owns a
different state class and repair surface:
    external serialized fields whose STORED VALUE equals old PlayerTypes.

Clearing/remapping every F477 historical reference would leave F647 untouched because
`m_PlayerRecords[P]` is keyed by P and the entire record object itself is stale.

F479/KI#802 similarly demonstrates why separately stored TeamTypes-indexed sidecar state
can deserve its own root despite the same reincarnation trigger.

REPAIR BOUNDARY
---------------
Add an explicit single-player statistics reset for identity creation.

Conceptually:
    CvStatistics::resetPlayerRecord(PlayerTypes ePlayer)

should:
1. destroy/reinitialize only that slot's `CvPlayerRecord`;
2. restore `m_iID = ePlayer`;
3. set the new leader after the new identity is known.

Call it from the true mid-game NEW-IDENTITY path (`CvGame::addPlayer` /
`CvPlayer::initInGame`), not from generic save-load `newPlayer` callbacks whose comment
says they are also invoked when launching a loaded game.

Do not call whole-game `resetStatistics`, which would erase every existing player's
history.

Preferred architectural F477 repair -- refusing EVER_ALIVE slot reuse entirely -- would
also prevent F647 activation. But if the supported human fallback remains, the
per-player record still requires explicit lifecycle handling.

VALIDATION TARGETS
------------------
1. Normal never-alive colony slot:
   fresh record remains all-zero except new identity fields.
2. Recycled defeated slot X -> new Z:
   immediately after addPlayer:
       citiesBuilt=0, citiesRazed=0, goldenAges=0;
       all unit/building counters=0;
       all religion-founded flags=false.
3. Old X counters must remain absent from Z's exported statistics.
4. Z events increment from zero.
5. Save/load after reincarnation preserves only Z's new record.
6. Ordinary save/load of a non-recycled existing player must NOT reset its record.
7. City-name fallback after explicit-list exhaustion must use Z's own founded-city count.
8. Whole-game reset still clears every record as today.
9. If preferred F477 fix disables dead-slot reuse, verify the per-player reset remains
   harmless for never-alive dynamic colony creation.

POST-F647 LEDGER / EXACT RESUME
-------------------------------
Queue 072 `CvGameCoreDLL/CvStatistics.cpp` -- OPEN.
F647 / provisional KI#969 -- CONFIRMED.
F648 / provisional KI#970 -- UNUSED.

Exact continuation:
C031-WIP486 physical -> Queue072 OPEN -> exclude F647 plus F477/F479 and all prior
recycled-identity owners -> resolve old `setVictory` eliminated-human warning through
actual statistics/victory semantics and dedup -> audit event counter ownership, kill
attacker sentinel contracts, city/acquisition/founding semantics, array enum bounds,
read/write XML-count coupling, lazy record initialization and EXE reporting callbacks ->
exhaust the small CvStatistics history set and Base/K-Mod/Civ4CE comparison -> promote
F648 only for a distinct current-live root -> close Queue072 or freeze another WIP at
the next settled boundary regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP487 -- QUEUE 072 CvStatistics.cpp -- F648 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP486.
- Queue 072 `CvGameCoreDLL/CvStatistics.cpp` remains OPEN.
- F647 / provisional KI#969 remains CONFIRMED.
- F648 / provisional KI#970 is CONFIRMED here.
- F649 / provisional KI#971 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F648 / provisional KI#970 -- `CvStatistics::setVictory` records an eliminated
human teammate as a winner because its "still alive" branch never checks `isAlive`
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited Firaxis/BtS statistics-reporting bug. Current-live in an ordinary
team game where a human teammate is eliminated before the surviving teammate/team later
wins. Base AdvC 1.14, K-Mod and Civ4CE retain the same source warning/condition.

SOURCE CONTRADICTION
--------------------
`CvStatistics::setVictory` iterates every ever-alive civilization player:

    if (GET_PLAYER((PlayerTypes)i).isEverAlive())
    {
        // DAN: They could be eliminated and still watching the game and get a win!
        // How to prevent this?
        if (GET_PLAYER((PlayerTypes)i).isHuman() &&
            GET_PLAYER((PlayerTypes)i).getTeam() == eWinner)
        {
            // If this guy is still alive and on the winning team, record the victory
            getPlayerRecord(i)->setVictory(eVictory);
        }
        else
            getPlayerRecord(i)->setVictory(NO_VICTORY);
    }

The code immediately states both the bug and the intended missing predicate:
- the DAN comment warns that an eliminated human can receive a win;
- the branch comment describes “still alive”;
- the branch itself never calls `isAlive()`.

LIFECYCLE PROOF
---------------
Ordinary `CvPlayer::setAlive(false)` does NOT clear human control.

On elimination it:
- sets `m_bAlive = false`;
- decrements team alive count;
- kills units/cities/deals;
- deactivates turn;
- leaves the network slot open specifically when `isHuman()` is true:

      if (!isHuman())
          gDLL->closeSlot(getID());

It does not change `m_bHuman` and does not reassign `getTeam()`.

The one exceptional path that calls `setIsHuman(false)` is explicitly:
    defeat of the active player DURING AI Auto Play while human control is disabled.

That special case does not close the ordinary team-game fixture.

Thus after normal elimination:
    isEverAlive() == true
    isAlive()     == false
    isHuman()     == true
    getTeam()     == original team

all persist simultaneously.

CURRENT-LIVE FIXTURE
--------------------
Let humans H1 and H2 be members of team T.

1. H1 and H2 begin alive on T.
2. H1 is eliminated during ordinary play.
   H1 remains:
       isEverAlive=true
       isAlive=false
       isHuman=true
       getTeam=T
3. H2 survives.
4. Team T later achieves victory V.
5. `CvEventReporter::victory(T,V)` calls:
       m_kStatistics.setVictory(T,V)
6. H1 passes the current branch:
       isEverAlive
       && isHuman
       && getTeam()==T
   despite `isAlive=false`.
7. H1's `CvPlayerRecord::m_eVictory` becomes V rather than NO_VICTORY.

This requires no recycled slot, custom Python or debug mode.

DIRECT EXPORTED CONSEQUENCE
---------------------------
`CvEventReporter::victory` performs:

    m_kPythonEventMgr.reportVictory(...)
    m_kStatistics.setVictory(...)
    ... set time played for every ever-alive player ...
    gDLL->reportStatistics();

The comment on the last call says:
    "automatically report MP stats on victory"

`getPlayerStatistics` exports:

    "victorytype" = pRecord->getVictory()

along with the rest of that player's record.

Therefore the defeated H1 is externally reported as having won victory V even though
the source's own intended condition says the player should be “still alive.”

SCOPE
-----
F648 does NOT claim that `CvGame`'s actual winning team is wrong, nor that a team should
stop being allowed to win after losing one member. Team victory resolution remains
unchanged.

The defect is specifically the per-HUMAN statistics outcome assigned to a player whom
the game has already eliminated.

If designers deliberately wanted team historical membership to count as a personal
statistics win, the two comments would need to be changed and the “eliminated human”
warning resolved as intentional. The surviving source text provides the opposite
oracle.

ANCESTRY
--------
The exact DAN warning and missing `isAlive` test exist in Civ4CE's stock-BtS
`CvStatistics.cpp`.

K-Mod retains the same function. AdvC's subsequent CvStatistics touches are includes,
casts and cleanup; supplied Base AdvC 1.14 and current SAS remain executable-identical
here.

This is therefore inherited rather than SAS-created.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- eliminated human statistics victory;
- `CvStatistics::setVictory`;
- false victory/win record after elimination;
- the “still alive and on the winning team” comment

find no prior confirmed owner.

F647 is independent:
    recycled PlayerTypes slots inherit prior CvPlayerRecord counters.

F648 reproduces with ordinary initial human players, no slot reuse and a fresh record.

REPAIR BOUNDARY
---------------
The narrow repair is exactly the missing condition expressed by the existing comment:

    if (GET_PLAYER(ePlayer).isAlive() &&
        GET_PLAYER(ePlayer).isHuman() &&
        GET_PLAYER(ePlayer).getTeam() == eWinner)
    {
        setVictory(eVictory);
    }
    else
        setVictory(NO_VICTORY);

Preserve `isEverAlive()` as the outer gate if statistics still need a final loss record
for defeated participants.

Do not change team-level victory resolution or revive/eliminate any player.

VALIDATION TARGETS
------------------
1. Two-human winning team, both alive at victory:
   both receive V.
2. H1 eliminated, H2 survives and team wins:
   H1 -> NO_VICTORY, H2 -> V.
3. Eliminated human on losing team:
   remains NO_VICTORY.
4. Alive AI on winning team:
   preserve current policy (NO_VICTORY if statistics intentionally reports human wins
   only).
5. Eliminated AI:
   NO_VICTORY.
6. Human temporarily disabled by AI Auto Play:
   preserve existing control semantics; do not infer alive state from human flag.
7. `gDLL->reportStatistics()` must export H1 `victorytype=NO_VICTORY`.
8. Actual `CvGame::getWinner()` / winning team remains unchanged.

POST-F648 LEDGER / EXACT RESUME
-------------------------------
Queue 072 `CvGameCoreDLL/CvStatistics.cpp` -- OPEN.
F647 / provisional KI#969 -- CONFIRMED.
F648 / provisional KI#970 -- CONFIRMED.
F649 / provisional KI#971 -- UNUSED.

Exact continuation:
C031-WIP487 physical -> Queue072 OPEN -> exclude F647/F648 and all prior statistics/
recycled identity owners -> finish event-counter producer contracts:
`unitKilled` attacker sentinel, city built vs acquisition, raze owner, religion founder,
golden-age reporting -> array enum bounds and current XML counts -> statistics read/
write/reset/newPlayer/load/report lifecycle -> classify every small path-history diff ->
compare Base AdvC/K-Mod/Civ4CE -> either promote a genuinely independent F649 or close
Queue072 cleanly -> freeze the next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP488 -- QUEUE 072 CvStatistics.cpp -- F649 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP487.
- Queue 072 `CvGameCoreDLL/CvStatistics.cpp` remains OPEN.
- F647-F648 / provisional KI#969-KI#970 remain CONFIRMED.
- F649 / provisional KI#971 is CONFIRMED here.
- F650 / provisional KI#972 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F649 / provisional KI#971 -- Rise & Fall's all-fog popup suppression gates the
entire `cityBuilt` EventReporter call, so interlude-founded cities disappear from
CvStatistics/Python and can later corrupt FairPlay protection for a human chapter
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED AdvC-specific regression introduced by advc.706 / practical 1268 and retained
by Base AdvC 1.14/current SAS. Current-live in the supported Rise & Fall interlude.

ROOT
----
Stock/K-Mod `CvPlayer::found` ends by reporting every successfully founded city:

    CvEventReporter::getInstance().cityBuilt(pCity);

Practical 1268 / advc.706 changed that to:

    if (CvPlot::activeVisibility)
        CvEventReporter::getInstance().cityBuilt(pCity);

with the comment:

    // advc.706: Suppress name-city popup

Later practical 1386 renamed/replaced the visibility mechanism with the current global
all-fog flag:

    if (!CvPlot::isAllFog()) // advc.706: Suppress name-city popup
        CvEventReporter::getInstance().cityBuilt(pCity);

The intended reason is UI-only: do not expose/pop up city founding while the human is
between Rise & Fall chapters.

But `CvEventReporter::cityBuilt` is not a popup helper. It performs:

    m_kPythonEventMgr.reportCityBuilt(pCity);
    m_kStatistics.cityBuilt(pCity);

and `CvStatistics::cityBuilt` increments:

    CvPlayerRecord::m_iNumCitiesBuilt.

Thus one UI/fog guard suppresses:
- every Python cityBuilt event;
- the authoritative per-player founded-city statistics counter;
- every other current/future EventReporter side effect added to this event.

SUPPORTED ALL-FOG ACTIVATION
----------------------------
Rise & Fall intentionally runs AI turns while the global flag is true.

At the end of a chapter:

    RiseFall::atTurnEnd
        interludeCountdown = interludeLength;
        setPlayerControl(currentChapterCiv, false);

`setPlayerControl(..., false)` performs:

    CvPlot::setAllFog(true);
    setUIHidden(true);

and turns the old human civilization back over to AI.

`atGameTurnStart` then spends the configured interlude turns decrementing the countdown.
Normal civilizations continue taking AI turns during this interval. The flag is global,
not scoped to the former human civ, so it affects `CvPlayer::found` for EVERY player.

Only when a later chapter takes control again does:

    setPlayerControl(newCiv, true)
        -> CvPlot::setAllFog(false)

restore ordinary event reporting.

Therefore any AI civilization that founds a city during the interlude reaches:

    initCity(...)
    real city ownership/population/map state created
    BUT
    CvEventReporter::cityBuilt skipped.

This is an ordinary supported Rise & Fall timeline, not a transient map-loading edge.

DIRECT STATISTICS CONTRADICTION
-------------------------------
Take civ C with:
    2 correctly recorded cities founded outside interludes.

During a Rise & Fall all-fog interlude C founds three additional cities.

Real state/history:
    C has founded 5 cities.

Current `CvPlayerRecord`:
    getNumCitiesBuilt() == 2

because all three interlude events were gated off.

Later C can be selected as the human civilization for a new chapter; the old missing
events are never replayed or reconstructed. Save/load preserves the undercount exactly.

LIVE AI CONSEQUENCE THROUGH FAIRPLAY
------------------------------------
`WarUtilityAspect::FairPlay` uses the statistics counter as gameplay state:

    int const iCitiesTheyFounded =
        kThey.getPlayerRecord()->getNumCitiesBuilt();

    int const iCitiesTheyHave = kThey.getNumCities();

    if (iCitiesTheyFounded > 0 &&
        iCitiesTheyHave < iCitiesTheyFounded)
    {
        rFromCityLoss =
            100 * (1 - iCitiesTheyHave / iCitiesTheyFounded)^0.85;
    }
    else if (iCitiesTheyHave == iCitiesTheyFounded)
    {
        // only recent-DoW protection
    }

This block is explicitly meant to stop AI dogpiling a HUMAN who has already lost cities
early in the game.

Continue the clean fixture:
- C enters a later human chapter with five real cities but only two recorded founded;
- during human play C loses three cities and now has two.

Current F649 state:
    iCitiesTheyFounded = 2
    iCitiesTheyHave    = 2
    -> no lost-city penalty at all;
       code falls into the separate equal-count recent-DoW branch.

Correct event history:
    iCitiesTheyFounded = 5
    iCitiesTheyHave    = 2

so:

    rFromCityLoss =
        100 * (1 - 2/5)^0.85
        = 100 * 0.6^0.85
        ~= 64.8

before the later attitude divisor.

Thus a UI-popup suppression during a previous AI-only interlude can remove roughly
65 raw FairPlay utility points from a later AI decision against the human.

Even less extreme undercounts distort the denominator continuously; the 5->2 example is
chosen only because it makes the branch flip especially clear.

OTHER CURRENT CONSEQUENCES
--------------------------
1. External/report statistics undercount every all-fog-founded city.
2. K-Mod city-name fallback uses `getNumCitiesBuilt`, so interlude omissions can affect
   the fallback rotation after explicit names are exhausted.
3. Every Python listener to the generic cityBuilt event misses those real city
   foundations. Some UI listeners may intentionally want silence during the interlude,
   but event suppression is broader than UI silence and prevents non-UI bookkeeping.

LINEAGE
-------
Civ4CE / stock BtS and K-Mod call `cityBuilt` unconditionally after a successful found.

Practical 1268 introduced the advc.706 visibility guard with the explicit
"Suppress name-city popup" rationale.

Practical 1386 changed the implementation from `CvPlot::activeVisibility` to
`!CvPlot::isAllFog()` while preserving the same whole-event guard and comment.

Later refactors/formatting retain it. Supplied Base AdvC 1.14 has exactly:

    if (!CvPlot::isAllFog()) // advc.706: Suppress name-city popup
        CvEventReporter::getInstance().cityBuilt(pCity);

Current SAS inherits it unchanged.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `CvPlot::isAllFog` + cityBuilt;
- Rise & Fall interlude founded-city statistics;
- advc.706 name-city popup suppression;
- missing cityBuilt Python/statistics events

find no existing owner.

F647 is independent:
    recycled player slot inherits an old record.

F648 is independent:
    eliminated human gets a false statistics victory.

F649 reproduces with a fresh ordinary non-recycled CvPlayerRecord and no victory.

REPAIR BOUNDARY
---------------
Do not suppress the authoritative city-built EVENT in order to suppress a popup.

Preferred split:
1. always call:
       CvEventReporter::getInstance().cityBuilt(pCity);
2. suppress only the UI/Python behavior that actually needs concealment during
   Rise & Fall all-fog state.

Possible approaches:
- make the specific name-city popup listener check `CvPlot::isAllFog()` /
  `RiseFall::isBlockPopups`;
- or add an explicit "silent UI" context to the Python event path while still executing
  C++ statistics bookkeeping.

At minimum, if preserving Python-event suppression is considered intentional, move
`m_kStatistics.cityBuilt(pCity)` outside the visibility gate so authoritative C++
statistics are unconditional. But the cleaner event architecture is to report the real
event and let presentation listeners decide whether to display anything.

VALIDATION TARGETS
------------------
1. Ordinary non-R&F city founding:
   Python event + statistics remain unchanged.
2. Enter R&F interlude (`isAllFog=true`), let AI C found one city:
   real city exists AND `getNumCitiesBuilt` increments.
3. No name-city / forbidden UI popup appears during interlude.
4. Multiple civs founding while global all-fog is active all receive correct counters.
5. Save/load after interlude preserves the corrected counts.
6. Later human takeover of C sees complete founded-city history.
7. FairPlay 5-founded -> 2-current fixture computes ~64.8 raw city-loss cost rather
   than entering the equality branch at 2/2.
8. If Python cityBuilt remains enabled under all-fog, verify individual UI listeners
   respect the R&F popup suppression policy without discarding non-UI listeners.

POST-F649 LEDGER / EXACT RESUME
-------------------------------
Queue 072 `CvGameCoreDLL/CvStatistics.cpp` -- OPEN.
F647 / provisional KI#969 -- CONFIRMED.
F648 / provisional KI#970 -- CONFIRMED.
F649 / provisional KI#971 -- CONFIRMED.
F650 / provisional KI#972 -- UNUSED.

Exact continuation:
C031-WIP488 physical -> Queue072 OPEN -> exclude F647-F649 and all prior owners ->
finish remaining event producer/counter contracts, especially cityBuilt suppression
interactions, unit/great-person double-count controls, raze/religion/golden-age
ownership -> array bounds and serialization/read/load contracts -> classify the entire
small CvStatistics history set -> Base/K-Mod/Civ4CE semantic comparison -> either
promote a genuinely independent F650 or close Queue072 cleanly -> freeze the next
physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP489 -- QUEUE 072 CvStatistics.cpp -- CLOSED / F650 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP488.
- Queue 072 `CvGameCoreDLL/CvStatistics.cpp` is CLOSED.
- Queue-072 confirmed roots:
    F647 / provisional KI#969
    F648 / provisional KI#970
    F649 / provisional KI#971
- F650 / provisional KI#972 remains intentionally UNUSED.
- Files complete: 43 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 074 `CvGameCoreDLL/WarEvaluator.cpp` (460 queued lines).
- No gameplay source was modified.

FINAL QUEUE-072 SATURATION PASS
-------------------------------
1. Unit-kill ownership
   -------------------
   `CvStatistics::unitKilled` indexes the attacker directly, which would be unsafe for
   NO_PLAYER. The live producer contract closes that edge:
       CvUnit::kill(..., ePlayer)
   calls the EventReporter kill event only inside:
       if (ePlayer != NO_PLAYER)
   so current statistics never receives the sentinel through the ordinary kill path.

2. Unit-built counting
   -------------------
   - ordinary trained units are reported once after successful completion;
   - K-Mod intentionally reports conscription as unit-built;
   - Great People are spawned outside ordinary city production and
     `greatPersonBorn` intentionally adds them to the unit-built statistic;
   - current Great-Person units are not ordinary production candidates, so no supported
     double-count fixture survives.
   An air unit that is physically created then immediately scrapped for impossible air
   placement can miss the ordinary unitBuilt event because the production switch breaks
   before that call; this is a narrow telemetry-definition edge and current SAS has
   already added separate SASGameRecord completion logging. It is not promoted as F650.

3. City-built semantics
   --------------------
   F649 owns the one demonstrated current corruption: Rise & Fall all-fog suppresses the
   whole event for real founded cities.

   WorldBuilder scenario preplaced cities use Python `player.initCity` and therefore do
   not increment "cities built." That is defensible record semantics (the player did not
   build them during the session). The only stock BtS public map with preplaced cities in
   the supplied Civ4CE set is Earth1000AD (69 cities, GameTurn 160), whose later-era
   context is outside FairPlay's early city-loss branch. No independent F650 is assigned.

4. City razing / religion / Golden Age
   ------------------------------------
   - cityRazed is called with the razing player, matching the exported statistic;
   - religion founding receives a concrete founding player from the city/religion
     assignment path;
   - Golden Age increments only on false->true Golden-Age transition, not when duration
     is merely extended;
   - enum values reaching the fixed arrays are current validated game-info enums.

5. Building-built
   --------------
   Ordinary construction and Great-Person forced construction both represent a building
   actually created by that player and report exactly once on their respective paths.
   Free/inherited/setup buildings that are merely applied do not masquerade as built
   production statistics.

6. Arrays / serialization
   ----------------------
   `CvPlayerRecord::reset` allocates all arrays to current GC info counts and read/write
   traverse those same counts in identical order.

   As in stock Civ4, statistics streams do not persist historical XML array lengths;
   loading a save against a materially different unit/building/religion info set is a
   broad mod-save-compatibility domain, not a current same-build defect.

   `CvEventReporter::readStatistics` resets the entire statistics object before reading,
   so lazy records from a previous in-memory state do not contaminate a normal save
   load. F647 is specifically the missing SINGLE-slot reset on mid-game identity reuse.

7. Lazy player records / reporting
   --------------------------------
   `getPlayerRecord` bounds-asserts the internal PlayerTypes index and initializes ID
   consistently. All current C++ callers supply real PlayerTypes.
   `getPlayerStatistics` exports exactly the stored values; F647/F648/F649 own the three
   demonstrated ways those stored values become wrong.

HISTORY / COMPARISON CLOSURE
----------------------------
Current SAS and supplied Base AdvC 1.14 have identical normalized executable
`CvStatistics.cpp` bodies.

The small AdvC history consists almost entirely of:
- include cleanup (1388, 1469/1470, 1679);
- type/cast cleanup and enum indexing (2918/2936/2998 mirrors);
- project/build-system changes.

The underlying record/update/report semantics come from stock BtS/Civ4CE and K-Mod.
No later Base-1.14 repair is missing.

The major Queue-072 defects therefore come from CROSS-FILE lifecycle/caller contracts:
- F647: dynamic player reincarnation never resets this sidecar record;
- F648: stock victory recorder omits its own "still alive" predicate;
- F649: AdvC Rise & Fall UI concealment suppresses the authoritative city event.

QUEUE-072 ROOT LEDGER
---------------------
F647 / KI#969
    recycled dead PlayerTypes slot inherits extinct civilization's entire CvPlayerRecord.

F648 / KI#970
    eliminated human teammate is exported as a statistics winner because setVictory
    checks everAlive/human/team but not alive.

F649 / KI#971
    Rise & Fall all-fog suppresses the whole cityBuilt event, undercounting interlude
    founding and later corrupting statistics/FairPlay for a human-selected civ.

F650 / KI#972
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 072 is closed. Remaining edges are telemetry-definition choices, broad cross-mod
save compatibility or unsupported/low-consequence API hardening; none clears the
independent current-live threshold for F650.

EXACT CONTINUATION
------------------
C031-WIP489 physical -> Queue072 CLOSED -> F647-F649 confirmed ->
F650/KI#972 UNUSED -> open priority Queue074
`CvGameCoreDLL/WarEvaluator.cpp` (460 lines) -> establish current SAS/Base AdvC 1.14,
K-Mod/Civ4CE provenance and path-history/caller map -> audit recursive war/peace
parameter mutation and restoration, preparation-time handling, total/limited-war
classification, extra-target/no-war-vs-extra modes, cached utility/report lifecycle,
team/player orientation, vassal/DP/war-plan assumptions, arithmetic/clamps/sentinels and
simulation reuse -> dedup especially against F639/F642 and prior WarUtility roots ->
promote F650 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F650 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP490 -- QUEUE 074 WarEvaluator.cpp -- F650 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP489.
- Queue 072 `CvStatistics.cpp` remains CLOSED with F647-F649 / KI#969-KI#971.
- Queue 074 `CvGameCoreDLL/WarEvaluator.cpp` is OPEN.
- F650 / provisional KI#972 is CONFIRMED here.
- F651 / provisional KI#973 is UNUSED.
- No gameplay source was modified.

EARLY QUEUE-074 NEGATIVE / PARKED CONTROLS
------------------------------------------
1. 48-civ cache-ID width is NOT broken in current SAS.
   `WarEvalParamID` switches from `int` to `__int64` when the player/team maxima reach
   the large-DLL range. Practical 2948 is explicitly the 48-civ UI-cache width repair.
   The old "fits into 32 bit" comment in getID is stale documentation, not current
   overflow.

2. Explicit preparation-time postcondition is genuinely inconsistent but remains below
   the independent gameplay threshold for now.
   Top-level `evaluate(eWarPlan, iPreparationTime)` finishes by replacing
   `m_kParams.getPreparationTime()` with `defaultPreparationTime(eWarPlan)` even when
   the caller passed an explicit value such as 0. `UWAIAgent::doScheme` has a live
   `evaluate(WARPLAN_PREPARING_LIMITED, bShortWork ? 0 : -1)` call and immediately reads
   the overwritten preparation time. However that stored local value is currently used
   for diagnostics/comparison logging rather than installed plan timing; actual plan
   review recomputes preparation from plan age. Preserve as API/postcondition debt unless
   a stronger consumer is found.

3. `WarEvalParameters::getID()` is not actually a perfect mixed-base encoding:
   after the naval bit it multiplies by 2, then adds `preparationTime+1` before
   multiplying by 50. Therefore, with all other fields equal, e.g.:
       nonnaval prep=2
   collides with:
       naval prep=0.
   Total/non-total can collide at larger prep offsets too.
   This is a real mathematical defect, but the current cache-using caller shapes examined
   so far mostly force preparation 0/direct-war states. Do not assign a second root
   without proving two supported colliding cache states are actually queried.

-------------------------------------------------------------------------------
F650 / provisional KI#972 -- the `bUseCache=true` WarEvaluator path writes live
cache entries without marking the cache dirty, so scheduled `clearCache` invalidation
can return early and leave stale war-utility results active
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC UI/AI cache-lifetime regression, current-live in ordinary
war-trade evaluation. Supplied Base AdvC 1.14 retains the same logic.

CACHE STATE MACHINE
-------------------
WarEvaluator has two orthogonal ways to use its primitive 16-entry cache:

A. static wrapper mode:
       WarEvaluator::enableCache()
           m_bCheckCache = true;
           m_bCacheCleared = false;

B. per-instance mode:
       WarEvaluator(params, true)
           m_bUseCache = true;

The cache check accepts either mode (plus the EXE diplomacy read context):

    if (!m_bPeaceScenario &&
        (m_bCheckCache || m_bUseCache || gDLL->isDiplomacy()))
    {
        look for matching cached result;
    }

The cache WRITE accepts the two explicit modes:

    if (m_bCheckCache || m_bUseCache)
    {
        aiLastCallParams[iLastIndex] = id;
        aiLastCallResult[iLastIndex] = utility;
        iLastIndex = ...;
    }

But only `enableCache()` changes the dirty/cleared bookkeeping:

    void enableCache()
    {
        m_bCheckCache = true;
        m_bCacheCleared = false;
    }

The per-instance `bUseCache=true` constructor and the write block do NOT set
`m_bCacheCleared=false`.

INVALIDATION CONTRACT
---------------------
`UWAI::invalidateUICache()` is simply:

    WarEvaluator::clearCache();

and `CvGame::update` deliberately calls it every four turn slices:

    if (getTurnSlice() % 4 == 0)
        AI().uwai().invalidateUICache();

The source comment says this is to prevent UI state -- specifically the willing-to-talk
indicator -- from becoming outdated during a turn.

`clearCache`, however, begins:

    if (m_bCacheCleared)
        return;

    m_bCacheCleared = true;
    for (...)
        aiLastCallResult[i] = MIN_INT;

Therefore a cache entry written exclusively through `bUseCache=true` while the flag is
still true is invisible to the invalidator.

MINIMAL DETERMINISTIC STATE-MACHINE PROOF
-----------------------------------------
Start from normal initialized state:

    m_bCacheCleared = true
    all results      = MIN_INT

1. Construct:
       WarEvaluator eval(params, true);

2. Evaluate an uncached war utility U.
   The final non-peace branch writes:
       aiLastCallParams[k] = params.getID();
       aiLastCallResult[k] = U;
   but leaves:
       m_bCacheCleared == true.

3. Game state changes in any war-utility-relevant way:
       city ownership,
       military power,
       attitude/memory,
       war plan,
       economic state,
       diplomacy,
       etc.

4. Scheduled invalidation executes:
       invalidateUICache()
           -> clearCache()

5. `clearCache` sees:
       m_bCacheCleared == true
   and RETURNS without clearing the live U entry.

6. Another `WarEvaluator(params, true)` with the same parameter ID checks the cache
   before reevaluating the changed world and returns U.

No hash collision, overflow, RNG or custom state is needed. The entry is stale solely
because the dirty flag fails to track a real write.

ORDINARY HUMAN WAR-TRADE-ALERT ACTIVATION
-----------------------------------------
`UWAI::Team::declareWarTrade` is the only explicit constructor caller with
`bUseCache=true`:

    WarEvalParameters params(... sponsor ...);
    WarEvaluator eval(params, true);
    int iU = min(-1, eval.evaluate(WARPLAN_LIMITED));

The AdvCiv4lerts WarTrade alert invokes this path directly:

    WarTradeAlert::check()
        -> kHireling.AI_declareWarTrade(target, ownerTeam)
        -> CvTeamAI::AI_declareWarTrade
        -> uwai().declareWarTrade(...)
        -> WarEvaluator(params, true)

Crucially, `WarTradeAlert::check` does NOT surround the call with
`WarEvaluator::enableCache()`.

Python schedules this alert on every:

    BeginActivePlayerTurn

through:
    activePlayer.checkAlert(...)

So a simple current game with:
    H = human owner/sponsor
    A = AI hireling
    B = possible war target

can produce one A-vs-B/sponsor-H cache entry at the start of H's turn while
`m_bCacheCleared` is still true. If the human never invokes a separate trade/willing-
to-talk widget path that calls `enableCache`, the every-four-slice invalidation is a
no-op. After AI turns change the world, the next active-player-turn alert can hit the
old A/B/H utility instead of recomputing it.

This can make "will now trade war / no longer trade war" alerts lag arbitrarily until:
- the 16-entry ring happens to evict the key;
- some unrelated wrapper calls `enableCache`, causing a later invalidation to work;
- or a whole-game/new-player cache reset occurs.

GAMEPLAY AI CALLERS USE THE SAME STALE PATH
-------------------------------------------
This is not isolated to informational alerts.

`CvTeamAI::AI_declareWarTrade` routes all UWAI-enabled nonhuman teams through the same
`UWAI::Team::declareWarTrade` function and therefore the same `bUseCache=true`
WarEvaluator.

Current synchronized/gameplay callers include, among others:
- force-war vote/resolution evaluation in `CvPlayerAI`;
- vassal-deal acceptance checks, where accepting the vassal may require declaring war
  on the incoming vassal's enemies;
- ordinary trade/denial code paths.

A stale cached war utility can therefore preserve an old `NO_DENIAL`/denial outcome
across real state changes, not merely an old alert message.

The cache parameter ID intentionally encodes scenario parameters, not mutable game
state. Periodic `clearCache` is the mechanism intended to make that safe. F650 is the
failure of that mechanism for one supported write mode.

LINEAGE -- EXACT BIRTH IN PRACTICAL 1443
----------------------------------------
Practical 1422 ("Cache of the WarEvaluator class improved") introduced/expanded:
- per-instance constructor `useCache`;
- global/static cache usage;
- EXE diplomacy cache reads.

At that point there was no `cacheCleared` early-return optimization.

Practical 1443 then introduced:
    cacheCleared = true;
    enableCache() -> cacheCleared = false;
    clearCache() -> if(cacheCleared) return;

The change converted the old public `checkCache=true/false` UI wrappers into
`enableCache()/disableCache()` and correctly marks wrapper-mode caching dirty.

But the already-existing per-instance `useCache=true` mode remained a writer and was
not taught to mark the new flag dirty.

Thus practical 1443 creates the exact state:
    live cache entry + cacheCleared==true.

Later refactors rename members but preserve the asymmetry. Current Base AdvC 1.14 has
identical:
- `enableCache` dirty transition;
- early-returning `clearCache`;
- `m_bUseCache` read/write conditions;
- no dirty transition on per-instance write.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `m_bCacheCleared` / `cacheCleared`;
- WarEvaluator clear/invalidate cache;
- bUseCache war evaluator;
- stale war-utility cache;
- `invalidateUICache`

find no prior confirmed owner.

Nearby queue findings are distinct:
- F639/F642 are ArmamentForecast scenario-state leaks;
- F644/F645 are MilitaryAnalyst nuclear simulation defects;
- prior cached-UWAI findings concern other cache fields/lifetimes, not WarEvaluator's
  dirty-bit state machine.

REPAIR BOUNDARY
---------------
Make dirty state track ACTUAL cache contents, not only one activation API.

Narrow robust fix:
    whenever a result is written into `aiLastCallResult`, set:
        m_bCacheCleared = false;

Conceptually:

    if (m_bCheckCache || m_bUseCache)
    {
        ... write entry ...
        m_bCacheCleared = false;
    }

This directly establishes the invariant:
    any valid result entry => m_bCacheCleared == false.

Alternative simpler fix:
    remove the early-return optimization from `clearCache` and always clear 16 ints.
The cache is only 16 entries; correctness may be worth eliminating the secondary state
entirely.

Do not rely on every caller of `bUseCache=true` remembering to call `enableCache`:
the constructor mode exists specifically as an independent API and the current
declare-war-trade caller intentionally uses it that way.

VALIDATION TARGETS
------------------
1. Initial state -> bUseCache=true write:
       cache dirty flag becomes false.
2. `clearCache` immediately afterward:
       all result slots become MIN_INT.
3. Same parameter query after changed world:
       evaluator recomputes, does not return old U.
4. WarTradeAlert across two active-player turns with a state change between:
       second check reflects changed war-trade utility.
5. Synchronized `AI_declareWarTrade` after invalidation:
       no stale denial/acceptance from prior state.
6. Wrapper mode `enableCache/disableCache` remains behaviorally unchanged.
7. Repeated `clearCache` when truly empty may still short-circuit if the optimization is
   retained.
8. New-player/revived-player `clearCache` calls must clear entries written by either
   cache mode.
9. Cache ring replacement semantics remain unchanged.
10. Separate getID collision/preparation-time questions remain independent; F650 fixes
    invalidation only.

POST-F650 LEDGER / EXACT RESUME
-------------------------------
Queue 074 `CvGameCoreDLL/WarEvaluator.cpp` -- OPEN.
F650 / provisional KI#972 -- CONFIRMED.
F651 / provisional KI#973 -- UNUSED.

Exact continuation:
C031-WIP490 physical -> Queue074 OPEN -> exclude F650 plus all prior WarEvaluator/UWAI
owners -> continue malformed mixed-base `WarEvalParameters::getID` collision activation
proof -> cache modes with `gDLL->isDiplomacy`, warAllies/extraTargets excluded-key state,
cache hit/write symmetry and invalidation callers -> finish recursive parameter restore,
explicit preparation postcondition, total/limited/naval comparison and diagnostic rerun
contracts -> exhaust all 39-ish WarEvaluator history strata -> whole-album dedup ->
promote F651 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F651 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP491 -- QUEUE 074 WarEvaluator.cpp -- CLOSED / F651 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP490.
- Queue 074 `CvGameCoreDLL/WarEvaluator.cpp` is CLOSED.
- Queue-074 confirmed root:
    F650 / provisional KI#972
- F651 / provisional KI#973 remains intentionally UNUSED.
- Files complete: 44 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 082 `CvGameCoreDLL/CvFractal.cpp` (407 queued lines).
- No gameplay source was modified.

FINAL POST-F650 SATURATION PASS
-------------------------------
1. `WarEvalParameters::getID` is mathematically non-injective
   ----------------------------------------------------------
   This remains a real implementation flaw but not a second current-live finding on the
   caller set proved in Queue 074.

   The mixed-base sequence uses:
       ... total; *2;
       naval; *2;
       preparationTime+1; *50;

   so the preparation digit is allowed to exceed the immediately preceding base.
   Examples with all other fields equal:
       nonnaval prep 2 == naval prep 0
       limited prep 4 == total prep 0 (same naval state).

   Current cache-producing/reading caller shapes examined do not produce a supported
   colliding pair under the same remaining parameters:
   - sponsored declare-war-trade sets `immediateDoW`, forcing prep 0;
   - at-war willing-to-talk evaluation uses prep 0;
   - normal default limited/total land/naval combinations are distinct;
   - explicit prep 3/5 callers use different sponsor/target/considering-peace contexts
     and do not match the live cached keys.

   Keep as cache-key robustness debt and a strong unit-test target. Do not consume F651
   without a concrete colliding live pair.

2. Long game-speed preparation bound
   ----------------------------------
   `getID` asserts `preparationTime+1 < 50`, while custom Slow/Very Slow train percents
   can make default preparation exceed that nominal bound.

   The same activation problem applies: cached contexts currently force/consume short
   preparation states. Noncached WarEvaluator calls never call getID at all. Therefore
   the assert/key-domain mismatch remains mod-speed/cache robustness rather than F651.

3. `warAllies` / `extraTargets` excluded from cache key
   ----------------------------------------------------
   `getID` explicitly asserts both sets are empty and documents that those scenarios
   should have caching disabled.

   The supported extra-target and war-ally evaluators use ordinary WarEvaluator
   instances (`m_bUseCache=false`) in synchronized AI/vote computation; no surrounding
   global cache-enable scope was found for those current calls.

   `gDLL->isDiplomacy()` can trigger cache reads even without explicit cache enable,
   which is a sharp API seam, but the current joint-war valuation path that constructs a
   WarAlly set is used by synchronized AI trade planning rather than the human
   diplomacy-screen value path. No concrete stale-key read was established.

4. Cache read/write asymmetry
   --------------------------
   `gDLL->isDiplomacy()` enables cache READ but not WRITE. This is intentional:
   trade-screen computations can reuse results written by explicit denial/UI cache paths
   without pushing additional entries.

   F650 is the independent dirty-state bug for the actual per-instance writer.

5. Recursive war/peace parameter restoration
   ------------------------------------------
   The recursive peace evaluation mutates:
       naval=false
       total=false
       preparationTime
   but the parent war branch restores all three before:
       - writing its cache key/result;
       - returning to the outer land/naval comparison.

   Sponsor, capitulation, ignore-distraction, considering-peace, allies and extra targets
   are not changed by the recursive call. No additional restoration leak survives.

6. Top-level land/naval comparison
   -------------------------------
   - naval is skipped when the agent has no assault/settler-sea transport;
   - both runs receive the same requested preparation input;
   - the +3 anti-naval preference is deliberate policy;
   - when logging is enabled, the extra third run is executed only for the selected
     orientation and does not alter the previously computed utility except by
     recomputation of deterministic evaluator state;
   - report muting is nesting-safe through `UWAIReport`'s counter semantics.

7. Explicit preparation-time output postcondition
   ----------------------------------------------
   As recorded in WIP490, top-level evaluate overwrites the exposed parameter with
   `defaultPreparationTime(eWarPlan)` even after an explicit preparation input.
   `doScheme` reads this after its short-work `prep=0` call, but the value is currently
   used for logging/comparison diagnostics; actual war-plan state/timing is not installed
   from that local variable. No gameplay F651 is assigned.

8. Cache invalidation
   ------------------
   F650 owns the only demonstrated current state-machine contradiction:
       per-instance `bUseCache=true` write
       + dirty flag left true-as-cleared
       + scheduled clear early-return
       -> stale result survives.

   Wrapper-mode `enableCache/disableCache` correctly marks cache dirty and scheduled
   clearing then works.

HISTORY CLOSURE
---------------
All indexed WarEvaluator / WarEvalParameters semantic strata were reviewed.

High-value changes include:
- practical 1253: initial UWAI evaluator;
- practical 1259: explicit cache clearing support;
- practical 1422: expanded cache and per-instance `useCache`;
- practical 1443: dirty/cleared optimization; exact birth of F650;
- practical 1446: willing-to-talk UI cache wrapper cleanup;
- practical 1478: `long` -> `int` cache IDs with documented ally/extra-target exclusion;
- practical 2608/2617: cache size 16 and follow-up syntax repair;
- practical 2920/2923: comprehensive UWAI rewrite;
- practical 2948: 48-civ `WarEvalParamID` width repair;
- practical 2963: style-only in evaluator;
- SAS 6036/6054: logging and already-owned war/peace utility fixes, not cache semantics.

Current SAS core evaluator/cache behavior remains Base AdvC 1.14-compatible aside from
SAS diagnostic logging and previously reconciled SAS fixes. No later Base-1.14 repair is
missing.

QUEUE-074 ROOT LEDGER
---------------------
F650 / KI#972
    per-instance `bUseCache=true` writes live WarEvaluator cache entries without marking
    the cache dirty; periodic `clearCache` can therefore return early and preserve stale
    war-trade utility.

F651 / KI#973
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 074 is closed. The malformed cache-key positional encoding and explicit-prep
postcondition remain documented robustness/API debt, but neither has a distinct
supported current gameplay activation after the full caller sweep.

EXACT CONTINUATION
------------------
C031-WIP491 physical -> Queue074 CLOSED -> F650 confirmed ->
F651/KI#973 UNUSED -> open priority Queue082
`CvGameCoreDLL/CvFractal.cpp` (407 queued lines) -> establish SAS/Base AdvC 1.14,
K-Mod/Civ4CE provenance and map-script caller/history map -> audit fractal initialization,
dimensions/wrapping, exponent/grain/flags, RNG seeding, ridge/hint arrays, height
percentile lookup, coordinate conversions, polar/invert flags, memory ownership and
Python map-script interfaces -> dedup against existing map-generation findings
F249/F634/F636 and related map roots -> promote F651 only for a distinct current-live
root -> freeze the next physical WIP at the next settled boundary whether F651 is
consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP492 -- QUEUE 082 CvFractal.cpp -- F651 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP491.
- Queue 074 `WarEvaluator.cpp` remains CLOSED with F650/KI#972.
- Queue 082 `CvGameCoreDLL/CvFractal.cpp` is OPEN.
- F651 / provisional KI#973 is CONFIRMED here.
- F652 / provisional KI#974 is UNUSED.
- No gameplay source was modified.

QUEUE-082 PROVENANCE BASELINE
-----------------------------
Current SAS `CvFractal.cpp` is 407 physical lines.

Its normalized executable token stream is identical to supplied Base AdvC 1.14.
K-Mod 1.46 and Civ4CE retain the same inherited height-inversion boundary loop; AdvC's
later changes concern types, polar behavior, dimension selection and refactoring rather
than this post-transform boundary contract.

Only about twenty historical commits touch the file. The most relevant is practical
3716 ("Fractal maps: choose smarter internal dimensions"), which can enlarge DEFAULT
internal dimensions on tall/large maps. That code deliberately applies only when the
script uses default exponents. Scripts that explicitly request custom exponents retain
those requested dimensions.

-------------------------------------------------------------------------------
F651 / provisional KI#973 -- `FRAC_INVERT_HEIGHTS` transforms only the interior
fractal cells but `getHeight` can interpolate against the uninverted +1 boundary row/
column; SAS's enlarged Team Battleground now samples those stale cells in normal maps
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited Civ4/K-Mod/AdvC fractal transform bug with a current SAS activation.
It changes actual generated land/ocean plot types on the shipped Team Battleground map
from SAS32 upward.

ROOT
----
`CvFractal` allocates:

    m_aaiFrac[m_iFracX + 1][m_iFracY + 1]

because `getHeight` performs bilinear interpolation and may need `iLowX+1` /
`iLowY+1`.

For NON-wrapped dimensions, the fractal generation loops explicitly generate the +1
boundary coordinates:

    iX < (m_iFracX >> iPass) + 1
    iY < (m_iFracY >> iPass) + 1

For WRAPPED dimensions, each pass explicitly fills the +1 boundary from the opposite
edge:

    m_aaiFrac[m_iFracX][iY] = m_aaiFrac[0][iY]
    m_aaiFrac[iX][m_iFracY] = m_aaiFrac[iX][0]

So index `m_iFracX` / `m_iFracY` is legitimate interpolation data, not unused padding.

After the whole fractal is generated, however, inversion does only:

    if (m_eFlags & FRAC_INVERT_HEIGHTS)
    {
        for (int iX = 0; iX < m_iFracX; iX++)
        {
            for (int iY = 0; iY < m_iFracY; iY++)
                m_aaiFrac[iX][iY] = 255 - m_aaiFrac[iX][iY];
        }
    }

The entire +1 row and +1 column remain in the PRE-inversion height domain.

`getHeight`, by contrast, clamps the low coordinate to `m_iFracX-1` /
`m_iFracY-1` specifically so it can still read:

    m_aaiFrac[iLowX + 1][...]
    m_aaiFrac[...][iLowY + 1]

When map dimensions are larger than the internal fractal dimensions, ordinary final
map coordinates acquire a nonzero interpolation weight on those +1 boundary cells.
The returned height therefore mixes:
    inverted interior data
with:
    uninverted boundary data.

WRAPPED AND NON-WRAPPED FORMS
-----------------------------
The root affects both kinds of boundaries once they are sampled.

Non-wrapped:
    the +1 boundary is a genuine generated edge value and simply never gets inverted.

Wrapped:
    the +1 boundary was copied from row/column zero BEFORE inversion. Row/column zero is
    then inverted, while its duplicate remains the original value, so periodic seam
    equality is broken:
        boundary == old(edge0)
        edge0    == 255 - old(edge0)

For a wrapped map the duplicate should be refreshed from the post-transform edge before
interpolation can consume it.

WHY STOCK TEAM BATTLEGROUND DID NOT EXPOSE IT
---------------------------------------------
The shipped BtS Team_Battleground.py already contains:

    elif userInputPlots == 1: # Top vs Bottom
        fractal_world = FractalWorld(fracXExp=6, fracYExp=6)
        fractal_world.initFractal(
            continent_grain=4,
            rift_grain=-1,
            has_center_rift=False,
            invert_heights=True)
        generatePlotTypes(water_percent=8)

So its continent fractal has:

    m_iFracX = 2^6 = 64
    m_iFracY = 2^6 = 64.

But the stock square Top-v-Bottom Huge grid is only:

    13 x 13 terrain cells

and a Python grid-size override is multiplied by 4 in `CvMap::reset`, yielding:

    52 x 52 map plots.

With output dimensions below 64, `getHeight` never needs interpolation cell 64.
The inherited defect therefore remained dormant in this stock fixture.

SAS ACTIVATION
--------------
SAS practical 5423 added/fixed Team_Battleground for AdvCiv-SAS and deliberately
calibrated the square modes beyond Huge using the Huge 13x13 / 16-player anchor.

Current world-size calibration gives:

    Huge / 16 players:
        13x13 cells -> 52x52 plots

    SAS24:
        16x16 cells -> 64x64 plots

    SAS32:
        18x18 cells -> 72x72 plots

    SAS40:
        21x21 cells -> 84x84 plots

    SAS48:
        23x23 cells -> 92x92 plots

The current XML default player counts are 16/24/32/40/48 respectively, and
`sas_calibrate_grid_from_anchor` computes these values from the 13x13 Huge anchor.

At SAS24 the last plot maps exactly to internal coordinate 63 with zero fractional
weight, so boundary cell 64 still is not sampled.

At SAS32 and above it is.

EXACT INTERPOLATION WEIGHT
--------------------------
Current `FLOAT_PRECISION` is 1024.

For Team Battleground SAS32:
    output width = 72
    internal width = 64

so:

    m_iXInc = floor(64 * 1024 / 72)
            = 910

At final map column x=71:

    iLowX = floor(910 * 71 / 1024)
          = 63

    iErrX = 910*71 - 63*1024
          = 98

Therefore `getHeight(71,y)` assigns boundary column 64:

    98 / 1024
    ~= 9.57%

of the horizontal interpolation weight.

The fraction grows with SAS world size:

    SAS32:  9.57%
    SAS40: 22.27%
    SAS48: 27.34%

On SAS48 a stale boundary value can therefore shift a one-dimensional edge
interpolation by up to roughly:

    0.2734 * 255
    ~= 69.7 height points

before the second bilinear dimension is considered.

EXACT CURRENT-DATA LAND/OCEAN REPRODUCTION
------------------------------------------
A literal integer translation of current `CvFractal::fracInitInternal`,
`CvRandom::get`, `getHeightFromPercent` and `getHeight` was used as an arithmetic
oracle for the cleanest supported case:

    map script: Team_Battleground
    layout: Top vs Bottom
    world size: SAS32
    wrap: Flat
    sea level: Medium (SeaLevelChange=0)
    continent fractal: explicit 6x6 exponent, grain 4, no rift
    invert heights: true
    requested water percent: 8
    fractal-entry CvRandom state: 0

The RNG transition is the current 32-bit LCG:

    state = 1103515245*state + 12345 (mod 2^32)

whose odd multiplier makes it bijective modulo 2^32. Therefore a chosen fractal-entry
state is reachable from a game/map seed after any fixed preceding sequence of RNG calls;
state 0 is a valid deterministic reproduction state rather than an impossible stream.

The interior-only current inversion produces:

    getHeightFromPercent(8) = 51

Because `getHeightFromPercent` deliberately scans only:
    x < m_iFracX
    y < m_iFracY

the threshold itself is unchanged by repairing only the +1 boundary.

On the 72x72 output, current code versus a coherent full-boundary inversion gives these
actual bottom-row continent heights:

    plot (33,71):
        current = 68  -> LAND (>51)
        corrected = 45 -> OCEAN (<=51)

    plot (35,71):
        current = 57  -> LAND
        corrected = 34 -> OCEAN

    plot (40,71):
        current = 54  -> LAND
        corrected = 32 -> OCEAN

    plot (41,71):
        current = 60  -> LAND
        corrected = 42 -> OCEAN

    plot (64,71):
        current = 58  -> LAND
        corrected = 37 -> OCEAN

Thus this is not merely a hidden fractal-field discontinuity. In one exact current
supported fixture, FIVE map plots change `PLOT_OCEAN`/land classification because the
edge interpolation mixes pre- and post-inversion domains.

The ordinary `FractalWorld.generatePlotTypes` consumer is direct:

    iWaterThreshold =
        self.continentsFrac.getHeightFromPercent(water_percent)

    val = self.continentsFrac.getHeight(x,y)

    if val <= iWaterThreshold:
        PLOT_OCEAN
    else:
        land/hills/peaks

No later normalization reconstructs the intended continent height field.

WRAP OPTIONS CAN STRENGTHEN THE SAME ROOT
-----------------------------------------
Team Battleground also exposes:
    Flat
    Cylinder
    Toroid

through `getWrapX/getWrapY`.

Top-v-Bottom with Cylinder/Toroid still uses the same explicit 64x64 inverted fractal.
For a wrapped dimension, boundary cell 64 is a pre-inversion copy of edge cell 0, so
SAS32+ interpolation additionally creates a literal periodic seam mismatch.

This is same-root breadth; the flat SAS32 fixture already proves F651.

PRACTICAL 3716 DOES NOT REPAIR THIS SCRIPT
------------------------------------------
AdvC practical 3716 ("Fractal maps: choose smarter internal dimensions") can enlarge
default internal dimensions for large/tall maps.

But its code tracks whether the script supplied default exponents. Team Battleground
explicitly constructs:

    FractalWorld(fracXExp=6, fracYExp=6)

and current SAS intentionally preserves explicit map-script fractal exponents.

So the automatic large-map dimension adjustment does not fire here. This is not a
missed invocation of an existing repair; it is a post-transform boundary bug exposed
by a legitimate explicit-resolution script on larger SAS output grids.

ANCESTRY
--------
Civ4CE BtS, K-Mod 1.46, Base AdvC 1.14 and current SAS all retain the same inversion:

    for x < m_iFracX
        for y < m_iFracY
            invert

while allocating/using +1 boundary cells.

Therefore the C++ root is inherited from Firaxis/BtS.

The current normal activation is SAS-specific in the Team Battleground fixture because
SAS extends the map beyond the original 64x64 internal fractal resolution:
- stock script had explicit 6x6 inversion;
- stock supported square sizes remained <=52 plots;
- SAS practical 5423 introduced calibrated larger square grids;
- SAS32+ crosses the 64-plot interpolation boundary.

Other current invert-height scripts can share the same root whenever their output
dimension exceeds their chosen internal fractal dimension.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `CvFractal`;
- `FRAC_INVERT_HEIGHTS`;
- inverted-height boundary / seam;
- Team Battleground fractal edges;
- +1 fractal interpolation boundary

find no prior confirmed owner.

Nearby map findings are distinct:
- F249/KI#572: map-generator stale area pointer after batched plot mutation;
- F634/KI#956: cached `m_bLake` not refreshed during normalization;
- F636/KI#958: WorldBuilder city-bearing scenarios keep pre-Ice water-area topology.

F651 occurs before CvPlot/CvArea topology exists and changes the raw generated plot type
array.

REPAIR BOUNDARY
---------------
The invariant should be:

    every fractal cell that `getHeight` can interpolate from must represent the SAME
    post-transform height domain.

Do not blindly reason only about the percentile interior.

For nonwrapped, nonpolar dimensions:
    transform the generated +1 boundary row/column too.

For wrapped dimensions:
    after inversion, refresh duplicate boundary row/column from the already-inverted
    row/column zero (or equivalently transform the duplicates consistently).

A robust implementation can separate:
1. transform all genuine fractal samples;
2. re-establish boundary conditions AFTER the transform:
       wrap -> copy transformed opposite edge;
       polar -> reapply explicitly intended polar boundary semantics.

This avoids accidentally changing the special FRAC_POLAR policy merely by extending a
loop bound. No current F651 fixture requires polar+invert.

Also keep `getHeightFromPercent` scanning only the unique/interior data as today;
wrapped duplicate boundaries should not be double-counted in percentile statistics.

VALIDATION TARGETS
------------------
1. Team Battleground Top-v-Bottom Huge 52x52:
   preserve ordinary output (boundary not sampled).
2. SAS24 64x64:
   preserve output; final plot has zero +1 interpolation weight.
3. SAS32 72x72 flat:
   every interpolated boundary sample is in inverted height domain.
4. Exact RNG-state-0 fixture:
   threshold remains 51 and the five listed bottom-row plots match the coherent
   corrected heights/classifications.
5. SAS40/SAS48:
   no mixed pre/post-inversion edge values.
6. Cylinder:
   transformed `fracX` duplicate equals transformed column 0 after inversion.
7. Toroid:
   both transformed +1 duplicates equal their wrapped source edges, including corner.
8. Nonwrapped:
   genuine generated +1 edge values are transformed whenever interpolation can use them.
9. `getHeightFromPercent` distribution remains based on unique cells and does not count
   wrap duplicates.
10. FRAC_POLAR:
    explicitly preserve/document intended polar boundary value after transform rather
    than accidentally inverting or duplicating it.
11. Non-inverted fractals:
    bit-for-bit unchanged.
12. Scripts whose output dimensions remain below internal resolution:
    bit-for-bit unchanged.

POST-F651 LEDGER / EXACT RESUME
-------------------------------
Queue 082 `CvGameCoreDLL/CvFractal.cpp` -- OPEN.
F651 / provisional KI#973 -- CONFIRMED.
F652 / provisional KI#974 -- UNUSED.

Exact continuation:
C031-WIP492 physical -> Queue082 OPEN -> exclude F651 plus all prior map owners ->
finish the reinitialization ownership leak by proving/closing current caller lifetime ->
audit `getHeightFromPercent` integer termination/percent bounds, exponent/grain shift
bounds, hinted-fractal dimensions and lengths, center-rift assumptions, `tectonicAction`
index/wrap contracts, `yieldX`, large-map internal-dimension selection, polar behavior,
allocation/deallocation pairing and Python/EXE API invalid-input surfaces -> exhaust the
~20 CvFractal history strata -> compare Base AdvC/K-Mod/Civ4CE -> promote F652 only for
a distinct current-live root -> close Queue082 or freeze another WIP at the next settled
boundary regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP493 -- QUEUE 082 CvFractal.cpp -- CLOSED / F652 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP492.
- Queue 082 `CvGameCoreDLL/CvFractal.cpp` is CLOSED.
- Queue-082 confirmed root:
    F651 / provisional KI#973
- F652 / provisional KI#974 remains intentionally UNUSED.
- Files complete: 45 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 089 `CvGameCoreDLL/CvGameAI.cpp` (338 queued lines).
- No gameplay source was modified.

FINAL POST-F651 SATURATION PASS
-------------------------------
1. `tectonicAction` / rift wrapping
   --------------------------------
   The C++ function and `yieldX` both carry the inherited comment:
       "Assumes FRAC_WRAP_X is on."

   Shipped scripts do violate that literal precondition. The strongest case is
   `Tilted_Axis.py`:
       getWrapX() -> false
       getWrapY() -> true
   while its Massive/Normal/Small Continents modes use positive rift grains and pass
   a rift fractal to `fracInitRifts`.

   The implementation then:
   - wraps negative/overflowing rift-strip X coordinates through `yieldX`;
   - carves its edge rift across the left/right map edges as though they were adjacent;
   - finally copies fractal column 0 into duplicate column `m_iFracX`.

   This is unusual on a non-wrapped X map, but it is not promoted as F652:
   - stock Firaxis/BtS itself ships Tilted Axis with this exact call shape;
   - the rift documentation describes a forced "edge" ocean in addition to optional
     center rifts, so modular edge carving may be deliberate implementation policy even
     on a flat horizontal dimension;
   - no authoritative alternative algorithm/output exists locally from which to prove
     a particular generated plot wrong.

   Preserve as documented design/precondition debt, not a finding.

2. `reset()` without `uninit()`
   ----------------------------
   `fracInitInternal` begins with:
       reset();
   and `reset` simply nulls `m_aaiFrac`; it does not delete a prior allocation.
   Reinitializing one already-initialized CvFractal object would therefore leak its
   complete old `(fracX+1)*(fracY+1)` allocation.

   A mechanical shipped-caller sweep found no normal second initialization of the same
   object:
   - generic `FractalWorld.continentsFrac` chooses exactly one of fracInit/fracInitRifts;
   - hinted worlds use their own one-time init path;
   - regional fractals are newly constructed locals per region call;
   - Terrain/Feature generators construct new member fractals and initialize each once;
   - duplicate textual init call sites in Tilted Axis/Fantasy are mutually exclusive
     rift/no-rift branches.

   Thus the ownership bug is real for Python/mod API reuse but no current shipped map
   activates it. Do not spend F652 on a leak with no current lifetime fixture.

3. `getHeightFromPercent`
   ----------------------
   - input is clamped to 0..100;
   - current internal dimensions keep `100*iSum` well within signed int;
   - binary search terminates at the lower bound for every clamped percent;
   - strict `< threshold` counting versus map consumers' `<= threshold` produces the
     inherited discrete-height approximation, not an unbounded or invalid result;
   - excluding duplicate +1 wrap boundaries from percentile statistics is intentional.
   F651 remains the separate transform-domain mismatch when getHeight interpolates an
   uninverted +1 boundary.

4. Interpolation arithmetic
   ------------------------
   With FLOAT_PRECISION=1024:
   - each bilinear weight product is <= 1024^2;
   - four weighted height terms sum below signed 32-bit range;
   - current map coordinates guarantee raw low indices remain within the final
     interpolation cell; the safety clamps do not produce negative interpolation
     weights in supported dimensions;
   - dimensions from every shipped world/map script are positive, so X/Y increments do
     not divide by zero.

5. Exponent / grain / shift contracts
   -----------------------------------
   Current shipped exponents stay within the range where:
   - `1 << exponent` is defined and small;
   - random displacement shifts `1 << (8-iSmooth+iPass)` remain nonnegative;
   - generated arrays are practical in size.

   `iSmooth = range(minExp-grain, 0, minExp)` intentionally clamps extreme grain values.
   Invalid externally supplied huge/negative exponents remain Python/mod API hardening,
   not current gameplay.

6. Hinted fractals
   ---------------
   `fracInitHinted` strips FRAC_POLAR deliberately and computes hint width/height from
   the actual fractal flags. Shipped hinted generators provide arrays matching those
   dimensions. The release-build FAssert-only length check is sharp for custom callers
   but no supplied current mismatch was found.

7. Rift-fractal dimension coupling
   -------------------------------
   `tectonicAction` indexes `pRifts` using the main fractal's quarter-X and full Y
   coordinates, so mismatched internal dimensions could be unsafe. All shipped
   `fracInitRifts` producers construct the rift and continent fractals with the same
   output dimensions and the same X/Y exponents before passing the pointer. No current
   mismatch survives.

8. Polar behavior
   --------------
   AdvC's Fractal-map-specific `polarHeight()` override is intentional and localized.
   Wrapped dimensions take precedence over FRAC_POLAR in boundary setup. Current
   scripts use those combinations consistently. F651's recommended repair must
   explicitly re-establish polar/wrap boundary semantics after inversion rather than
   blindly inverting every +1 cell.

HISTORY CLOSURE
---------------
All 19 indexed CvFractal.cpp history entries were classified.

High-value strata:
- K-Mod initial import: inherited Firaxis fractal implementation and the inversion
  boundary root later confirmed as F651;
- practical 1395: cppcheck/refactor plus initialization of X/Y increments;
- practical 1456: Fractal-map polar-height refinement;
- practical 2549/2554: enum refactors only;
- practical 2842: broad refactor;
- practical 2982: Civ4Col-style `while` wrapping in `yieldX`, defensive for large
  excursions;
- practical 3716: FLOAT_PRECISION 1000->1024 and smarter default internal dimensions;
- SAS 5961: signature formatting only.

Current SAS and supplied Base AdvC 1.14 are executable-semantically identical in this
file. K-Mod/Civ4CE retain F651's inherited inversion bounds and the same rift assumptions.
No later Base-1.14 repair is missing.

QUEUE-082 ROOT LEDGER
---------------------
F651 / KI#973
    FRAC_INVERT_HEIGHTS transforms only unique/interior cells while getHeight can
    interpolate +1 boundary samples; SAS32+ Team Battleground Top-v-Bottom now samples
    those stale pre-inversion cells and can flip actual land/ocean plot types.

F652 / KI#974
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 082 is closed. Remaining sharp surfaces are unsupported reinitialization/API
lifetime, inherited rift-design assumptions without a correctness oracle, or invalid
external parameter hardening. None clears the independent current-live threshold for
F652.

EXACT CONTINUATION
------------------
C031-WIP493 physical -> Queue082 CLOSED -> F651 confirmed ->
F652/KI#974 UNUSED -> open next selected priority Queue089
`CvGameCoreDLL/CvGameAI.cpp` (338 lines) -> establish SAS/Base AdvC 1.14/K-Mod/Civ4CE
provenance and history/caller map -> audit UWAI option initialization/load semantics,
scenario AI initialization, combat-value/turn-scaling arithmetic, current-era factor,
vote-source-era derivation, max-culture scaling, exclusive-radius weights,
serialization/reset lifetime and dynamic XML/state assumptions -> whole-album dedup
before numbering -> promote F652 only for a distinct current-live root -> freeze the
next physical WIP at the next settled boundary whether F652 is consumed or remains
unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP494 -- QUEUE 089 CvGameAI.cpp -- CLOSED / F652 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP493.
- Queue 089 `CvGameCoreDLL/CvGameAI.cpp` is CLOSED.
- No new Queue-089 root was promoted.
- F652 / provisional KI#974 remains intentionally UNUSED.
- Files complete: 46 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 096 `CvGameCoreDLL/CvMemoryManager.cpp` (286 queued lines).
- No gameplay source was modified.

PROVENANCE
----------
Current SAS `CvGameAI.cpp` is executable-semantically the same as supplied Base AdvC
1.14 except for two deliberate SAS changes:
1. practical 5052 caches three GlobalDefine values in local static const variables in
   `AI_sortOutUWAIOptions`;
2. practical 4946 removes historical save-version padding under SAS's current-version-
   only KI#46 policy.

K-Mod/Civ4CE provide the smaller inherited CvGameAI baseline; the era/vote-source,
scenario and exclusive-radius helpers are AdvC additions.

SATURATION PASS
---------------
1. Derived-cache reset/load lifetime
   ---------------------------------
   `CvGame::read` begins from the normal `CvGame::reset` lifecycle, and nonconstructor
   game reset calls `AI().AI_reset()`.

   `AI_reset(false)` recomputes:
       AI_updateExclusiveRadiusWeight()
       AI_updateVoteSourceEras()
       AI_updateMaxCultureLevelPercent()

   so these nonserialized XML-derived fields do not retain stale values across ordinary
   load/new-game reset. No F652 here.

2. `AI_getCurrEraFactor`
   ---------------------
   The helper is explicitly based on `CvGame::getCurrentEra` and uses the same
   `PlayerIter<CIV_ALIVE>` population (alive non-Barbarian civilizations, including
   minor civs). It averages the player-level continuous era factor instead of rounding
   each civ to an integer era.

   Current callers use it as a game-progress heuristic for city sites, military
   production, war utility, branch availability and timing. No caller requires a
   major-civ-only or highest-era contract, and the history/intended comment matches the
   implemented population.

   The iterator is nonempty in every live game; the -1 fallback is assertion-only
   robustness.

3. Vote-source era cache
   ---------------------
   `AI_updateVoteSourceEras` derives each building's latest prerequisite-tech era using
   `CvBuildingInfo::isTechRequired`, which includes the main prerequisite, additional
   prerequisites and special-building tech requirements.

   Current SAS has exactly one vote-source building for each source:
       Apostolic Palace -> Philosophy -> Classical
       United Nations   -> Mass Media  -> Modern

   Therefore the per-building `set(eVS, eMaxEra)` cannot currently overwrite a competing
   building attached to the same vote source. The generic multi-building-source case is
   mod-robustness debt only.

   Live consumers (`AI_votesToGoForVictory`, diplomacy-victory strategy) use the latest
   source era exactly as intended.

4. Maximum-culture-level speed scaling
   -----------------------------------
   `AI_updateMaxCultureLevelPercent` identifies the Normal speed by unique
   `iResearchPercent==100` and scales against the actual final-culture-level thresholds.

   Current SAS values:
       Nitro      research 33, Legendary 15,000
       Turbo      50,              20,000
       Quick      67,              30,000
       Normal    100,              45,000
       Epic      150,              67,500
       Marathon  200,              90,000
       Slow      300,             135,000
       Very Slow 400,             180,000

   Normal is unique and the thresholds are positive. Consumers use the resulting percent
   only for culture-victory/high-culture timing thresholds. No current ambiguity or zero
   denominator exists.

5. Exclusive-radius weights
   -------------------------
   The three stored positions correspond to:
       index 0 -> unknown distance (-1)
       index 1 -> distance 0/1 mapping as documented
       index 2 -> distance 2

   All current callers pass either the default -1 or nonnegative plot/city distances.
   Inputs >= array size intentionally return zero. No live caller supplies less than -1.

   Current CITY_RADIUS_DECAY and game-speed GoldenAgePercent keep the pow base positive;
   reset recomputes the values after speed/XML state is available.

6. `AI_turnsPercent`
   -----------------
   Only two live callers scale bounded building-construction turn horizons by the
   current positive game-speed ConstructPercent. The MAX_INT sentinel is explicitly
   preserved before multiplication. No current overflow/zero-percent path survives.

7. `AI_combatValue`
   ----------------
   The normalization denominator `getBestLandUnitCombat()` is guaranteed at least 1,
   including the no-best-unit fallback. Current unit combat/first-strike data keep the
   integer arithmetic well within range. Air units use air combat as intended.

8. Scenario initialization
   -----------------------
   `AI_initScenario`:
   - recalculates city base yields and working plots;
   - forces a capital only for a major civ with >1 cities and no capital;
   - runs UWAI `turnPre` only when UWAI or background-UWAI is enabled.

   Stock supplied scenario controls:
   - Earth1000AD has preplaced cities and its one-city civ has an explicit Palace;
   - Europe / Earth_IceAge supplied fixtures do not produce a current one-city/no-capital
     contradiction.

   The `>1` condition leaves a possible custom one-city/no-capital scenario edge, but no
   supported supplied activation was found.

9. SAS practical-5052 static GlobalDefines
   ---------------------------------------
   Current SAS changed:
       GC.getDefineINT("USE_KMOD_AI_NONAGGRESSIVE")
       GC.getDefineINT("DISABLE_UWAI")
       GC.getDefineINT("UWAI_IN_BACKGROUND")
   into local `static const` reads.

   `CyGlobalContext.setDefineINT` does make GlobalDefines mutable in principle, so a
   script/mod that changes these exact settings after the first
   `AI_sortOutUWAIOptions()` call would no longer be observed.

   Current Python searches show no runtime setter for any of these three defines; only
   unrelated camera/minimap settings are mutated. Normal initial/new/load game calls
   occur after XML defines have loaded. Therefore this remains SAS dynamic-define/API
   robustness debt rather than a demonstrated F652.

10. Serialization
    -------------
    Current `read/write` pairing is coherent:
       CvGame base state
       current uiFlag
       UWAI state

    SAS intentionally removed old uiFlag padding branches under KI#46. No current-writer
    / current-reader mismatch exists.

HISTORY CLOSURE
---------------
All indexed CvGameAI history strata were classified.

High-value semantic changes:
- K-Mod base: inherited combat/turn helpers;
- practical 1253: initial AdvC/UWAI integration;
- practical 1331: scenario AI initialization;
- practical 1443: small AdvC AI/state tweaks;
- practical 1499: broad cleanup/bugfix batch, no surviving Queue-089 root;
- practical 1842: WPAI -> UWAI rename;
- practical 2719: custom-era support; introduces continuous game-era factor and
  vote-source-era cache;
- practical 2920: comprehensive UWAI/scaled-number revision;
- practical 3223: fixes Legacy/Aggressive AI option handling in scenarios;
- practical 3509: victory-strategy weight redistribution (cross-file focus);
- practical 3617: force multi-city scenario major civs to obtain a capital;
- practical 4203: remove unused CvGameAI member;
- SAS 4946: old-save uiFlag removal;
- SAS 5052: static-const define micro-optimization.

No later Base AdvC 1.14 semantic repair is missing from current SAS.

QUEUE-089 ROOT LEDGER
---------------------
F652 / KI#974
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 089 is closed. Remaining sharp surfaces are custom-scenario/mod-API robustness,
dynamic define mutability that current SAS does not exercise, or intentionally
approximate AI heuristics. None clears the independent current-live threshold for F652.

EXACT CONTINUATION
------------------
C031-WIP494 physical -> Queue089 CLOSED -> F652/KI#974 UNUSED ->
open next selected priority Queue096 `CvGameCoreDLL/CvMemoryManager.cpp` (286 lines) ->
establish whether the custom allocator is compiled/active in the current DLL and map
all operator-new/delete callers -> compare SAS/Base AdvC/K-Mod/Civ4CE lineage ->
audit block headers, alignment, realloc, coalescing/free-list ownership, zero-size and
array/scalar delete paths, initialization/shutdown lifetime, integer overflow and
threading assumptions -> whole-album dedup before numbering -> promote F652 only for a
distinct current-live root -> freeze the next physical WIP at the next settled boundary
whether F652 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP495 -- QUEUE 096 CvMemoryManager.cpp -- CLOSED / F652 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP494.
- Queue 096 `CvGameCoreDLL/CvMemoryManager.cpp` is CLOSED.
- No new root was promoted.
- F652 / provisional KI#974 remains UNUSED.
- Files complete: 47 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 108 `CvGameCoreDLL/CvAgents.cpp` (212 queued lines).
- No gameplay source was modified.

ACTIVATION BASELINE
-------------------
This file is not a general custom heap implementation. It mainly installs global
new/delete shims that forward allocation to the Civ4 DLL interface.

The shims are compiled only in DEBUG builds:

    CvGameCoreDLL.h:
        #ifdef _DEBUG
            #define USE_MEMMANAGER

    CvMemoryManager.cpp:
        #ifdef USE_MEMMANAGER
            operator new/delete...
        #endif

Release builds therefore compile the allocator overrides out.

`MEMORY_TRACKING` is an additional optional debug layer but its define is commented out
even in the debug header; none of its fixed-size leak-tracking arrays run in current
normal debug builds.

CURRENT DEBUG PATH
------------------
When `gDLL` is installed:
    scalar new      -> gDLL->newMem
    scalar delete   -> gDLL->delMem
    array new       -> gDLL->newMemArray
    array delete    -> gDLL->delMemArray

Before `gDLL` installation:
    new/new[]       -> CRT malloc
    delete/delete[] -> CRT free

The file also fills newly allocated debug memory with 0xDA to expose uninitialized use.

HISTORY / REPAIRS
-----------------
The queue has only five cpp history touches.

- practical 1791 moved the existing memory/profiling implementation out of
  CvGameCoreDLL.cpp into this file without changing its core semantics;
- practical 1926/2127 are memory/debug-print infrastructure cleanup;
- practical 2130 explicitly fixed the likely inherited BtS copy/paste bug in the
  file/line array overloads:
      new[]  -> `newMemArray`, not `newMem`
      delete[] -> `delMemArray`, not `delMem`
- practical 2503 changes diagnostic assert spelling only.

No later Base AdvC 1.14 repair is missing.

CROSS-ALLOCATOR LIFETIME CHECK
------------------------------
The allocator choice is based on the CURRENT `gDLL` pointer rather than metadata stored
with the allocation. In principle this creates a dangerous transition:
    allocation before gDLL -> CRT malloc
    deallocation after gDLL -> engine delMem
or the reverse during shutdown.

A current global/static-object sweep did not produce a supported failure:
- most STL global containers are initially empty and allocate only later, after global
  initialization and the DLL interface setup;
- SAS static maps/vectors used for gameplay/logging grow during game execution, not DLL
  static initialization;
- the notable static nonempty CvString XML assertion prefix is small and not mutated
  through a demonstrated cross-interface deallocation lifecycle;
- ordinary DLL global cleanup follows Civ4's explicit CvGlobals shutdown/interface
  lifecycle.

This remains a genuine allocator-design hazard for future global/static allocations but
not a demonstrated current debug-build defect.

MECHANICAL CONTROLS
-------------------
- practical 2130's scalar/array allocator pairing fix is present.
- the file/line allocation overloads are effectively dormant because K-Mod disabled the
  old macro-replacement layer in `CvGameCoreDLLDefNew.h`; their diagnostic FErrorMsg
  remains a developer assertion if manually restored.
- `reallocMem`/`memSize` wrappers belong to that same disabled macro API.
- zero-size / allocation-failure standards behavior is less robust than modern C++
  operator-new requirements, but current engine allocation callers do not supply a
  demonstrated failure fixture.
- optional MEMORY_TRACKING bookkeeping is inactive and therefore not F652 material.
- release gameplay is entirely unaffected by the file's operator overrides.

QUEUE-096 ROOT LEDGER
---------------------
F652 / KI#974
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 096 is closed. Remaining issues are debug-only allocator architecture hazards,
manually enabled diagnostics, or dormant API standards-hardening rather than a
demonstrated current-live bug.

EXACT CONTINUATION
------------------
C031-WIP495 physical -> Queue096 CLOSED -> F652/KI#974 UNUSED ->
open priority Queue108 `CvGameCoreDLL/CvAgents.cpp` (212 lines) ->
establish SAS/Base AdvC 1.14 provenance and AgentIterator/caller map -> audit cache
construction/order, alive/ever-alive/major/member/vassal sequence membership, player and
team death/revival/new-player updates, recycled slot insertion, master/vassal
reclassification, vector ordering/duplicate prevention and pointer lifetime -> dedup
especially against F477/F479/F647 and all iterator findings -> promote F652 only for a
distinct current-live root -> freeze the next physical WIP at the next settled boundary
whether F652 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP496 -- QUEUE 108 CvAgents.cpp -- F652 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP495.
- Queue 096 `CvMemoryManager.cpp` remains CLOSED.
- Queue 108 `CvGameCoreDLL/CvAgents.cpp` is OPEN.
- F652 / provisional KI#974 is CONFIRMED here.
- F653 / provisional KI#975 is UNUSED.
- No gameplay source was modified.

QUEUE-108 PROVENANCE BASELINE
-----------------------------
`CvAgents` is an AdvC AgentIterator cache layer; stock BtS/Civ4CE and K-Mod do not have
this persistent cached-sequence architecture and therefore provide direct-scan oracles
rather than the same helper.

Current SAS and supplied Base AdvC 1.14 retain the same `playerSetAliveInGame` /
`playerRevived` implementation.

Existing album ownership was checked before numbering:
- F470/KI#793: human-involved Permanent Alliance omits the full CvAgents rebuild;
- F474/KI#797: recycled colony duplicates historical CIV_EVER_ALIVE entries;
- F469/KI#792: alive-member-count dependent vassal maintenance invalidation;
- F477/F479/F647: other recycled player/team/sidecar identity roots.

None owns the generic revival insertion contract below.

-------------------------------------------------------------------------------
F652 / provisional KI#974 -- practical 2010 generalized a colony-specific
CvAgents cache insertion helper to arbitrary player revival without removing its
singleton/new-major-team assumptions; shared-team revival corrupts both team and
member caches
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED AdvC regression in the explicit player-revival lifecycle. The cleanest
supported reproducer is WorldBuilder revival, which current SAS itself recently treats
as a real reproducible lifecycle (KI#475.3).

The root is broader than one bad PlayerTypes->TeamTypes expression: practical 2010
reused the old "new colony" cache insertion sequence for an already-ever-alive player
whose team/status may already exist in the caches.

CURRENT HELPER
--------------
`CvAgents::playerSetAliveInGame` currently does:

    CvPlayerAI* pPlayer = &GET_PLAYER(ePlayer);
    CvTeamAI* pTeam = &GET_TEAM(ePlayer);

    if (!bRevive)
        insert team into CIV_EVER_ALIVE;

    insert pTeam into CIV_ALIVE;
    insert pTeam into MAJOR_ALIVE;

    if (!bRevive)
        insert player into CIV_EVER_ALIVE;

    insert pPlayer into CIV_ALIVE;
    insert pPlayer into MAJOR_ALIVE;

    members = MEMBER[pTeam->getID()];
    assert pPlayer is in members;
    insert pPlayer into MEMBER_ALIVE[pTeam->getID()];

`playerRevived(ePlayer)` simply calls:

    playerSetAliveInGame(ePlayer, true);

Three assumptions embedded in the old colony path are invalid for generic revival:

1. TEAM ID ASSUMPTION
   ------------------
   `GET_TEAM(ePlayer)` indexes TeamTypes using the raw PlayerTypes numeric value.
   The player's actual team is:

       GET_PLAYER(ePlayer).getTeam()
       / TEAMID(ePlayer)

   Shared-team games do not require PlayerID == TeamID.

2. TEAM-LIFETIME ASSUMPTION
   ------------------------
   The helper always inserts the team into CIV_ALIVE/MAJOR_ALIVE.
   When one member of a multi-player team dies while another survives, the team was
   never removed from those caches. Reviving the dead member must NOT insert the team
   again.

3. STATUS/RELATION ASSUMPTION
   --------------------------
   The helper unconditionally inserts into MAJOR_ALIVE and does not restore
   `VASSAL_ALIVE` membership. A generic revived player can be minor, or can rejoin a
   still-alive vassal team whose vassal relation persisted through the individual
   member's death.

All three are natural for a newly created derivative colony in the original helper:
normally a new major player receives its own newly alive singleton team whose numeric
slot matches the player slot. They are not generic revival invariants.

EXACT BIRTH -- PRACTICAL 2010
-----------------------------
Before practical 2010, the function was specifically:

    CvAgents::colonyCreated(PlayerTypes eNewPlayer)

and began:

    CvPlayerAI* pPlayer = &GET_PLAYER(eNewPlayer);
    CvTeamAI* pTeam = &GET_TEAM(eNewPlayer);

followed by insertion of the NEW team/player into ever-alive, alive, major and member
caches.

Practical 2010 / commit
`56af3a8dfa7f97c1fa229e1b9499a91428db0ed1`,
"Fix issues with AI Auto Play ending upon player defeat", refactored that exact body
into:

    playerSetAliveInGame(PlayerTypes ePlayer, bool bRevive)

and added:

    playerRevived(ePlayer)
        -> playerSetAliveInGame(ePlayer, true)

The commit merely suppresses the EVER_ALIVE insertions for `bRevive`. It leaves:
- raw `GET_TEAM(ePlayer)`;
- unconditional team alive/major insertion;
- unconditional player major insertion;
- no vassal-cache restoration.

That is the exact regression point: colony-new-identity assumptions were carried into a
generic revival API.

Practical 2547 later makes the debug-only `members` local conditional on
FASSERT_ENABLE but does not change the runtime insertion semantics.

SUPPORTED REVIVAL PRODUCER
---------------------------
`CvPlayer::verifyAlive` explicitly revives a dead player that again owns a city or unit:

    if (!isAlive())
    {
        if (getNumCities() > 0 || getNumUnits() > 0)
            setAlive(true);
        return;
    }

`CvPlayer::setAlive(true)` detects `bEverAlive` and, for a non-colony parentless player,
calls:

    GC.getAgents().playerRevived(getID());

Current SAS contains a very recent source comment documenting a reproducible WorldBuilder
revival workflow:

    "Dead-at-load players have no deserialized UWAI owner/cache.
     Initializing the revived player before updating that cache fixed the reproducible
     crash when exiting WorldBuilder after giving that player a unit.
     See KI#475.3."

So "give a dead player a unit in WorldBuilder -> verifyAlive -> setAlive(true)" is not
an invented API-only path; current project history has already treated it as a concrete
reproduction lifecycle.

CLEAN SHARED-TEAM FIXTURE
-------------------------
Let:
    P0 = Player 0
    P1 = Player 1
    T0 = Team 0

and set both players on shared Team T0:

    P0.getTeam() == T0
    P1.getTeam() == T0

while Team slot T1 is unused/dead.

1. Both P0 and P1 begin alive.

2. P1 is eliminated while P0 survives.
   `CvAgents::playerDefeated(P1)` correctly:
   - removes P1 from player CIV_ALIVE / MAJOR_ALIVE;
   - removes P1 from MEMBER_ALIVE[T0];
   - does NOT remove T0 from team CIV_ALIVE / MAJOR_ALIVE because T0 still has P0.

3. In WorldBuilder, give P1 a unit or city and leave WB.
   `verifyAlive()` calls `setAlive(true)`.
   `setAlive` increments T0's alive count and calls `playerRevived(P1)`.

4. Current `playerSetAliveInGame(P1,true)` computes:

       pTeam = &GET_TEAM((TeamTypes)P1.getID())
             = &GET_TEAM(T1)

   rather than T0.

5. It inserts dead/unrelated T1 into:
       team CIV_ALIVE
       team MAJOR_ALIVE

6. It correctly inserts P1 into the global player CIV_ALIVE/MAJOR_ALIVE caches, then
   looks for P1 in:

       MEMBER[T1]

   even though the permanent MEMBER cache correctly records P1 under T0.

ASSERT-BUILD CONSEQUENCE
------------------------
The source immediately asserts:

    std::find(MEMBER[T1], P1) != end

which is false in the fixture.

Thus an assert/debug build has an immediate deterministic failure at the cache-revival
boundary.

RELEASE-BUILD CONSEQUENCE
-------------------------
Without asserts, the function inserts P1 into:

    MEMBER_ALIVE[T1]

and never restores:

    MEMBER_ALIVE[T0].

The cache layer is then internally contradictory:

    authoritative P1.getTeam() == T0

    MemberIter(T0)       -> omits revived P1
    MemberIter(T1)       -> contains P1 despite P1.getTeam()!=T1
    TeamIter<CIV_ALIVE>  -> contains dead/unrelated T1
    TeamIter<MAJOR_CIV>  -> contains T1 as a major alive team

These exact-cache iterators deliberately skip rechecking predicates when the selected
cache is supposed to be exact. `AgentIterator.cpp`'s optimized `isAlive`/`isMinorCiv`
helpers explicitly trust CIV_ALIVE/MAJOR_ALIVE membership in those cases. The wrong
entries are therefore not filtered back out.

IMMEDIATE TEAM-STATE CONSEQUENCE
--------------------------------
If the revived P1 is given a CITY in land area A, the true team aggregate:

    CvTeam(T0)::countNumCitiesByArea(A)

uses:

    for (MemberIter it(T0); ...)
        count += area.getCitiesPerPlayer(it->getID());

Because MEMBER_ALIVE[T0] still omits P1, T0 reports a city count that excludes P1's
revived city even though:
    P1.getTeam()==T0
and the city belongs to P1.

The same wrong member cache feeds many authoritative team aggregates:
- unit count by area;
- population by area;
- power by area;
- unit/building classes currently being produced;
- religion/corporation counts;
- total culture;
- current team era;
- typical unit values;
- research/team modifier paths;
- diplomacy/war loops that enumerate actual team members.

The global team-cache corruption can separately change logic based on the number of
alive teams. For example:

    CvPlayerAI::AI_calculateDiplomacyVictoryStage()
        if (TeamIter<CIV_ALIVE>::count() <= 2)
            return 0;

In a true two-team game, insertion of unused T1 makes the cache count three and can
bypass that early two-team guard.

EVEN A ONE-LINE TEAM LOOKUP FIX IS INCOMPLETE
----------------------------------------------
Suppose only:

    GET_TEAM(ePlayer)

were changed to the correct T0.

In the shared-team fixture, T0 never died and is ALREADY present in team CIV_ALIVE /
MAJOR_ALIVE.

The helper would still execute:

    insertIntoVector(teamSeqCache(CIV_ALIVE), T0);
    insertIntoVector(teamSeqCache(MAJOR_ALIVE), T0);

`insertIntoVector` asserts on an existing ID in debug builds but, in release, it still
inserts the pointer after the existing one. That would create literal duplicate alive
team entries.

So F652 must not be "fix the cast." The generic revival lifecycle needs status-aware,
idempotent cache restoration.

VASSAL SAME-ROOT BREADTH
------------------------
`playerDefeated` explicitly removes an individually defeated member from:

    VASSAL_ALIVE[master]

when their team is a vassal.

If another teammate keeps that vassal team alive, the vassal agreement remains.

On member revival, `playerSetAliveInGame(...,true)` does not restore the revived player
to the master's VASSAL_ALIVE player cache at all. Even after fixing the team ID and
duplicate-team insertion, `PlayerIter<ALIVE,VASSAL_OF>` can therefore continue omitting
the revived vassal member.

This is the same generic-revival helper incompleteness, not a separate F653.

MINOR-CIV SAME-ROOT BREADTH
---------------------------
The helper unconditionally inserts both player and team into MAJOR_ALIVE.

`setAlive(true)` / `playerRevived` does not have a major-civ-only precondition.
A revived minor civilization can therefore be misclassified as major as well.

Again, this is the same preserved "new colony is major" assumption from the helper's
original purpose.

ANCESTRY / UPSTREAM COMPARISON
------------------------------
Supplied Base AdvC 1.14 retains the same practical-2010 implementation unchanged.

K-Mod/Civ4CE do not have persistent `CvAgents` cached sequences; their loops query
current player/team slots directly and therefore cannot acquire this stale/wrong
membership state from `playerRevived`.

This is an AdvC AgentIterator integration regression.

WHOLE-ALBUM DEDUP
-----------------
F470/KI#793:
    Permanent Alliance changes authoritative team assignment without rebuilding
    CvAgents caches. No revival is involved.

F474/KI#797:
    recycled colony re-inserts already-present EVER_ALIVE historical pointers.
    F652 uses bRevive=true, so those EVER_ALIVE insertions are skipped entirely.

F469/KI#792:
    vassal-city maintenance cache invalidation when alive member count changes.
    Its WIP191 revival control assumed `playerRevived` correctly reinserts the member.
    Queue 108 now shows that assumption only holds in the singleton/ID-aligned case.
    F652 owns the underlying revival cache membership corruption; F469 remains its
    independent downstream cached-maintenance invalidation.

F477/F479/F647:
    recycled identity / sidecar state; no slot recycling is required for F652.

No earlier album root owns:
- raw PlayerID->TeamID lookup in playerRevived;
- duplicate alive-team reinsertion on shared-team revival;
- missing vassal membership restoration on revival.

REPAIR BOUNDARY
---------------
Treat revival as restoring CURRENT authoritative state, not as creating a new colony.

A robust narrow design:

1. Resolve the actual team:
       TeamTypes eTeam = pPlayer->getTeam();

2. Always restore player caches according to CURRENT status:
       CIV_ALIVE if non-Barbarian alive civ
       MAJOR_ALIVE only if !minor
       MEMBER_ALIVE[eTeam]
       VASSAL_ALIVE[master] when appropriate

   Every insertion must be uniqueness-preserving/idempotent.

3. Restore team alive/major/vassal caches only when the TEAM actually transitioned from
   dead to alive.
   `CvPlayer::setAlive(true)` increments the team's alive count before calling
   playerRevived, so:
       getAliveCount()==1
   is a natural indication that this revived member changed the team 0->1 alive.
   If alive count >1, the team was already alive and must not be inserted again.

4. Preserve EVER_ALIVE sequences on revival; they already contain the historical
   player/team.

A simpler and safer low-frequency option is:
    `playerRevived` -> `updateAllCachedSequences()`
because revival is rare and the full rebuild derives every sequence from current
authoritative player/team/vassal state. That also avoids future omission of a newly
added cache category.

Do not simply suppress duplicate assertions: release insertion must itself be unique.

VALIDATION TARGETS
------------------
1. Singleton player/team revival where PlayerID==TeamID:
   restore exactly one player/team alive entry.
2. Shared team P0/P1 on T0; P1 dies, P0 survives; revive P1:
   - no assert;
   - T0 appears exactly once in CIV_ALIVE/MAJOR_ALIVE;
   - no unrelated Team(P1-ID) entry is inserted;
   - P1 appears exactly once in MEMBER_ALIVE[T0].
3. Give revived P1 a city in area A:
   T0.countNumCitiesByArea(A) includes it.
4. Team current era / population / power / production aggregates include P1.
5. Shared vassal team: revive one member while teammate survives:
   revived member returns to VASSAL_ALIVE[master].
6. Minor-civ revival:
   CIV_ALIVE yes; MAJOR_ALIVE no.
7. Fully dead team revived by first member:
   team alive caches are inserted once.
8. Revive another member of that now-alive team:
   team caches remain unique.
9. `TeamIter<CIV_ALIVE>::count()` remains equal to authoritative live-team count.
10. Save/load after revival reconstructs identical sequences through
    `updateAllCachedSequences`.
11. Permanent Alliance F470 and recycled-colony F474 regression tests remain separate.

POST-F652 LEDGER / EXACT RESUME
-------------------------------
Queue 108 `CvGameCoreDLL/CvAgents.cpp` -- OPEN.
F652 / provisional KI#974 -- CONFIRMED.
F653 / provisional KI#975 -- UNUSED.

Exact continuation:
C031-WIP496 physical -> Queue108 OPEN -> exclude F652 plus F470/F474/F469 and all
prior AgentIterator/recycled-state owners -> finish updateAllCachedSequences invariants,
playerDefeated status symmetry, colonyCreated behavior, updateVassal direct/master
semantics, allianceFormed rebuild, sorted/duplicate insert helpers, cache-id routing and
all CvAgents history strata -> check the additional colony-parent revival skip
(`setAlive` calls playerRevived only when parent==NO_PLAYER) as possible F652 breadth
or independent F653 only after lifecycle proof -> whole-album dedup -> either promote a
distinct current-live F653 or close Queue108 cleanly -> freeze the next physical WIP
regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP497 -- QUEUE 108 CvAgents.cpp -- F653 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP496.
- Queue 108 `CvGameCoreDLL/CvAgents.cpp` remains OPEN.
- F652 / provisional KI#974 remains CONFIRMED.
- F653 / provisional KI#975 is CONFIRMED here.
- F654 / provisional KI#976 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F653 / provisional KI#975 -- defeated colonies are excluded from the generic
`playerRevived` cache-restoration path solely because `getParent()!=NO_PLAYER`, so
WorldBuilder revival can make the CvPlayer/CvTeam alive again while all CvAgents
alive caches still omit them
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED AdvC regression in the explicit revival lifecycle. Base AdvC 1.14 retains the
same guard. This is independent of F652:
- F652 corrupts cache restoration when `playerRevived` IS called with a shared-team /
  minor / vassal player;
- F653 skips `playerRevived` entirely for a previously created colony.

ROOT
----
Current `CvPlayer::setAlive` changes authoritative player/team life state first:

    bool const bEverAlive = isEverAlive();
    m_bAlive = bNewValue;
    GET_TEAM(getTeam()).changeAliveCount(isAlive() ? 1 : -1);

then updates CvAgents:

    if (!isAlive())
        GC.getAgents().playerDefeated(getID());
    else if (bEverAlive && getParent() == NO_PLAYER) // Colonies are handled by initInGame
        GC.getAgents().playerRevived(getID());

The comment is only valid for a colony's FIRST creation:
    `CvPlayer::initInGame`
        -> `GC.getAgents().colonyCreated(getID())`.

It is not valid for a colony that was already created, later defeated, and is now being
revived.

PARENT PERSISTS ACROSS DEFEAT
-----------------------------
A colony receives:

    GET_PLAYER(eNewPlayer).setParent(getID());

during the ordinary split-empire creation path.

`m_eParent` is initialized to NO_PLAYER only during player reset/new identity and is
serialized. Ordinary `setAlive(false)` / defeat does not clear it. Searches of current
CvPlayer mutation paths show no defeat-time `setParent(NO_PLAYER)`.

Therefore a correctly created colony C has:

    C.getParent() != NO_PLAYER

both before and after elimination.

DEFEAT REMOVES CACHES
---------------------
On C's death, `setAlive(false)` always calls:

    CvAgents::playerDefeated(C)

which removes C from:
- player CIV_ALIVE;
- player MAJOR_ALIVE when major;
- MEMBER_ALIVE[team];
- VASSAL_ALIVE[master] when applicable;

and, if C was the last alive team member, removes its team from:
- team CIV_ALIVE;
- team MAJOR_ALIVE;
- team VASSAL_ALIVE[master].

The persistent EVER_ALIVE/MEMBER historical caches intentionally remain.

SUPPORTED REVIVAL PRODUCER
---------------------------
`CvPlayer::verifyAlive` is generic:

    if (!isAlive())
    {
        if (getNumCities() > 0 || getNumUnits() > 0)
            setAlive(true);
        return;
    }

It has no parent/colony exclusion.

Current SAS recently documented a reproducible WorldBuilder revival lifecycle in this
same `setAlive(true)` branch (KI#475.3):
    leave WorldBuilder after giving a dead player a unit
    -> verifyAlive
    -> setAlive(true)
    -> revived-player UWAI initialization.

That source fix was written for the generic dead-player workflow, and the producer does
not prohibit a previously created colony. WorldBuilder can give a dead colony a unit or
city just as it can any other dead PlayerTypes slot.

CLEAN FIXTURE
-------------
1. Human/AI parent P creates ordinary colony C through `splitEmpire`.
2. C is initialized normally:
       C.parent = P
       C is a vassal on its own team
       CvAgents colonyCreated inserts its live player/team/member/vassal entries.
3. C is later eliminated.
       playerDefeated removes all C live-cache entries.
       C.parent remains P.
4. In WorldBuilder, give C one unit or city and exit.
5. `verifyAlive(C)` calls `setAlive(true)`.

Current authoritative state becomes:
    C.isAlive() == true
    C's team alive count increments, potentially 0 -> 1.

But the CvAgents branch evaluates:
    bEverAlive == true
    getParent() != NO_PLAYER

and therefore SKIPS `playerRevived(C)`.

No `initInGame` runs here; this is revival of an existing identity, not colony creation.
No `updateAllCachedSequences()` follows.

RESULTING CACHE CONTRADICTION
-----------------------------
For an ordinary singleton colony team that had fully died:

    authoritative:
        C.isAlive() == true
        GET_TEAM(C.team).isAlive() == true

    CvAgents:
        PlayerIter<CIV_ALIVE>      -> omits C
        PlayerIter<MAJOR_CIV>      -> omits C
        MemberIter<MEMBER_ALIVE>   -> omits C
        PlayerIter<VASSAL_OF>      -> omits C
        TeamIter<CIV_ALIVE>        -> omits C's revived team
        TeamIter<MAJOR_CIV>        -> omits C's revived team
        TeamIter<VASSAL_OF>        -> omits C's revived team

The exact AgentIterator cache specializations deliberately trust these cached sequences
instead of rechecking `isAlive`, so the revived civilization is invisible to every
consumer using those iterators.

CURRENT-LIVE CONSEQUENCES
-------------------------
This omission is immediately semantic, not merely diagnostic.

Examples include:
- MilitaryAnalyst / InvasionGraph:
      `PlayerIter<MAJOR_CIV>` omits the revived colony entirely from war simulation.
- diplomacy/war/team update loops:
      many `PlayerIter<MAJOR_CIV>` / `TeamIter<CIV_ALIVE>` loops never see it.
- city/site and plot logic:
      generic CIV_ALIVE / MAJOR_CIV scans exclude a player that owns real units/cities.
- vassal iteration:
      the revived colony remains a real vassal by authoritative team state but is absent
      from `PlayerIter<ALIVE,VASSAL_OF>` and team-vassal caches.
- team-count dependent strategy:
      if its team had gone 0->1 alive, `TeamIter<CIV_ALIVE>::count()` undercounts the
      number of actual live teams.

A particularly clean observable invariant is simply:

    CvGame authoritative alive players
        includes C

while:

    PlayerIter<CIV_ALIVE>::count()
        does not increase on C revival.

No later per-turn repair exists; `updateAllCachedSequences` is normally reserved for
game start/load and special explicit topology changes such as Permanent Alliance.

EXACT BIRTH IN PRACTICAL 2010
-----------------------------
Practical 2010 / commit
`56af3a8dfa7f97c1fa229e1b9499a91428db0ed1`
introduced generic revival handling while fixing AI Auto Play defeat/revival.

Before that commit, CvAgents only had the new-colony insertion helper.

The commit added in `CvPlayer::setAlive`:

    else if (bEverAlive && getParent() == NO_PLAYER) // Colonies are handled by initInGame
        GC.getAgents().playerRevived(getID());

and simultaneously generalized the old `colonyCreated` insertion body into
`playerSetAliveInGame(..., bRevive)`.

The parent guard correctly avoids double-inserting a colony during its FIRST
`initInGame` creation. But it also suppresses every LATER resurrection of that same
colony identity.

Current Base AdvC 1.14 retains the same condition unchanged.

K-Mod/Civ4CE do not have persistent CvAgents cached alive sequences, so their direct
player/team scans cannot become stale through this omitted callback.

WHOLE-ALBUM DEDUP
-----------------
F652/KI#974:
    `playerRevived` is called, but its implementation assumes PlayerID==TeamID/new team/
    major status and corrupts shared-team/minor/vassal revival.

F653:
    `playerRevived` is never called because an old colony still has a parent.

Fixing F652 only inside `CvAgents::playerRevived` or `playerSetAliveInGame` leaves F653
unchanged.

F447/KI#770:
    malformed recycled colony creation chooses a defeated player whose OLD TEAM is
    still alive, skips initInGame, transfers cities and later revives the old identity.
    F653 requires a correctly created ordinary colony first, then a later ordinary
    defeat and explicit revival. No player-slot recycling or malformed split is needed.

F474/KI#797 and F647/KI#969:
    recycled identity/history state; again no recycling is needed for F653.

No existing confirmed finding owns the parent-gated omission of generic colony revival.

REPAIR BOUNDARY
---------------
The lifecycle distinction should be:
    FIRST colony creation
versus:
    revival of an already-ever-alive player,
not:
    parentless
versus:
    colony.

A safe design is:

1. During `initInGame` / initial colony creation:
       continue using `colonyCreated` exactly once.

2. During any `setAlive(false -> true)` where `bEverAlive==true`:
       restore CvAgents revival state regardless of parent.

If F652 is repaired by making:
    `playerRevived -> updateAllCachedSequences()`,
then F653's caller repair becomes especially simple:
    call `playerRevived` for every ever-alive revival.

If a narrow incremental cache update is retained, it must also restore the colony's
current vassal membership and team state.

Do not call `initInGame` on revival: that would incorrectly reset an existing colony
identity. The missing action is only cache/UWAI revival state.

UWAI NOTE
---------
The same parent guard also skips the adjacent:

    initRevivedPlayerInGame / processNewPlayerInGame

for revived colonies. Current live consequence proof for F653 requires only the
CvAgents omission, but the UWAI skip should be audited/fixed in the same lifecycle
change; a revived colony should not retain deleted/dead UWAI state merely because it has
a parent.

VALIDATION TARGETS
------------------
1. Create ordinary colony C; verify live caches contain it exactly once.
2. Eliminate C; verify all live caches remove it.
3. Revive C through WorldBuilder unit/city:
   - C.isAlive true;
   - player CIV_ALIVE / MAJOR_ALIVE contains C once;
   - MEMBER_ALIVE contains C once;
   - vassal-player cache contains C when relation persists;
   - if team went 0->1, team CIV_ALIVE / MAJOR_ALIVE / vassal cache contains team once.
4. `PlayerIter<CIV_ALIVE>::count()` increments exactly once on revival.
5. MilitaryAnalyst/normal diplomacy iterators see C after revival.
6. Parent value remains unchanged; this is revival, not new colony creation.
7. First colony creation still uses `colonyCreated` only once and does not double insert.
8. Parentless revival continues working after F652 repair.
9. Shared-team colony/mod scenario combines correctly with F652's idempotent restoration.
10. UWAI revived-player state is initialized for revived colonies too.
11. Save/load after revival reconstructs identical caches through full rebuild.

POST-F653 LEDGER / EXACT RESUME
-------------------------------
Queue 108 `CvGameCoreDLL/CvAgents.cpp` -- OPEN.
F652 / provisional KI#974 -- CONFIRMED.
F653 / provisional KI#975 -- CONFIRMED.
F654 / provisional KI#976 -- UNUSED.

Exact continuation:
C031-WIP497 physical -> Queue108 OPEN -> exclude F652/F653 plus F470/F474/F469 and
other AgentIterator/recycled-state owners -> finish `updateVassal` symmetry and team/
player death ordering, colonyCreated reuse behavior, updateAllCachedSequences exact
predicate coverage, insert/erase uniqueness, allianceFormed rebuild, status cache
routing and all CvAgents history strata -> check whether any remaining update path can
leave VASSAL_ALIVE/MAJOR_ALIVE inconsistent independently of F652/F653 -> whole-album
dedup -> either promote a distinct current-live F654 or close Queue108 cleanly -> freeze
the next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP498 -- QUEUE 108 CvAgents.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP497.
- Queue 108 `CvGameCoreDLL/CvAgents.cpp` is CLOSED.
- Queue-108 confirmed roots:
    F652 / provisional KI#974
    F653 / provisional KI#975
- F654 / provisional KI#976 remains intentionally UNUSED.
- Files complete: 48 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 110 `CvGameCoreDLL/AgentIteratorTest.cpp` (240 queued lines).
- No gameplay source was modified.

FINAL QUEUE-108 SATURATION PASS
-------------------------------
1. Full rebuild predicates
   -----------------------
   `updateAllCachedSequences()` reconstructs every derived sequence from authoritative
   player/team state:
   - CIV_EVER_ALIVE: non-Barbarian ever-alive civs;
   - CIV_ALIVE: non-Barbarian alive civs;
   - MAJOR_ALIVE: alive nonminor civs;
   - MEMBER: all player slots by current team;
   - MEMBER_ALIVE: alive players by current team;
   - VASSAL_ALIVE: alive major vassal teams/players by current master.

   Ordering follows the ALL vectors' increasing IDs and is therefore deterministic.
   This full rebuild is the positive oracle for both F652 and F653.

2. `playerDefeated`
   ----------------
   Ordinary death removes the player from all live player/member caches and removes the
   team only when the authoritative team becomes dead. Vassal membership is removed
   from the current master before a dead team later cancels its vassal agreement.

   This ordering matches `CvPlayer::setAlive(false)`, which decrements team alive count
   before calling `playerDefeated`.

3. `updateVassal`
   --------------
   Practical 2324 fixed the inherited crash when a team with already-dead members
   becomes/frees a vassal. Current code:
   - inserts/removes only alive member players;
   - handles the dead-vassal-team cancellation ordering explicitly;
   - updates both player VASSAL_ALIVE and team VASSAL_ALIVE caches.

   Normal setVassal caller ordering updates authoritative `m_eMaster` before calling the
   CvAgents hook. No independent stale/master-orientation root survived.

   F652 remains the revival-specific omission of vassal membership because
   `playerSetAliveInGame` never restores it.

4. `colonyCreated`
   ---------------
   For a genuinely never-alive colony slot, insertion into ever-alive/alive/major/member
   caches is coherent.

   Its `insertIntoVector(..., false)` behavior on reused EVER_ALIVE slots can create
   duplicate historical entries; that exact recycled-colony root is already
   F474/KI#797 and is not renumbered here.

5. `allianceFormed`
   ----------------
   Permanent Alliance takes the conservative full `updateAllCachedSequences()` route
   when the hook is called. F470/KI#793 owns the separate human-involved PA path that
   can omit this hook altogether.

6. Insert / erase helpers
   ----------------------
   `insertIntoVector` preserves increasing ID order. Its debug duplicate assertion does
   not prevent release duplication, which is why lifecycle callers must themselves be
   idempotent:
   - F474 owns reused EVER_ALIVE insertion;
   - F652 owns team duplicate insertion during shared-team revival.

   Ordinary non-revival live hooks meet uniqueness preconditions.

   `eraseFromSeqCache` removes exactly one expected entry. Under coherent caches that is
   sufficient; multi-duplicate states are downstream of already-owned insertion roots.

7. Cache routing / iterator trust
   ------------------------------
   AgentIterator's exact-cache specializations intentionally avoid rechecking predicates
   when a cache is supposed to be exact. This design makes CvAgents lifecycle
   correctness critical but is not independently defective when the caches are built
   through `updateAllCachedSequences`.

HISTORY CLOSURE
---------------
All CvAgents path-history strata were classified.

High-value changes:
- practical 1816: initial AgentIterator/CvAgents cache architecture and lifecycle hooks;
- practical 1817: compile-time cache/predicate routing refactor;
- practical 1848: scenario initialization timing fix;
- practical 2010: generic revival support; exact birth of both F652 and F653:
    * old colony insertion helper generalized to `playerRevived`;
    * parentless-only revival guard added in CvPlayer;
- practical 2324: dead-member vassal-cache crash repair;
- practical 2547: assert-local compile-warning cleanup;
- later practicals are include/refactor/iterator modernization;
- SAS 6022/6083 are formatting/instrumentation-adjacent and do not alter cache semantics.

Supplied Base AdvC 1.14 retains F652/F653. K-Mod/Civ4CE do not have this persistent
CvAgents exact-cache layer.

QUEUE-108 ROOT LEDGER
---------------------
F652 / KI#974
    generic revival reuses colony-new-team assumptions: raw PlayerID as TeamID,
    unconditional team reinsertion, no vassal restoration, unconditional major status.

F653 / KI#975
    defeated colony revival never calls `playerRevived` because persistent parent
    excludes it; authoritative alive state returns while all live AgentIterator caches
    can remain missing the colony/team.

F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 108 is closed. Remaining problems are exact breadth of F652/F653 or already-owned
recycled/alliance insertion roots. No independent F654 survives.

EXACT CONTINUATION
------------------
C031-WIP498 physical -> Queue108 CLOSED -> F652-F653 confirmed ->
F654/KI#976 UNUSED -> open priority Queue110
`CvGameCoreDLL/AgentIteratorTest.cpp` (240 lines) -> establish compile/runtime activation
and whether failures can affect current game startup/load -> compare every tested
predicate/cache relation against current AgentIterator/CvAgents contracts -> audit test
side effects, RNG/state mutation, assert/release differences and historical purpose ->
whole-album dedup -> promote F654 only for a distinct current-live root -> freeze the
next physical WIP at the next settled boundary whether F654 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP499 -- QUEUE 110 AgentIteratorTest.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP498.
- Queue 110 `CvGameCoreDLL/AgentIteratorTest.cpp` is CLOSED.
- No new root was promoted.
- F654 / provisional KI#976 remains UNUSED.
- Files complete: 49 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 112 `CvGameCoreDLL/UWAI.cpp` (237 queued lines).
- No gameplay source was modified.

ACTIVATION
----------
The actual profiling/test workload is entirely wrapped in:

    //#define AGENT_ITERATOR_TEST

    #ifdef AGENT_ITERATOR_TEST
        ...
    #endif

The define is commented out in current source.

`CvAgents::gameStart` does call:

    int TestAgentIterator(int);
    int iNonsense = TestAgentIterator(100);

after loading a save, but with the macro disabled the function body is simply:

    int r = 0;
    return r;

The `if (iNonsense == -1)` fallback can never execute.

Therefore:
- no iterator loop is run;
- no profiling code is instantiated;
- no game state is mutated;
- no cache validation/assertion happens;
- the file adds only a tiny constant-return call on save load.

The commented test loops themselves are historical microbenchmarks comparing
AgentIterator, raw MAX_* loops and direct cached-vector traversal. They are not a
current correctness oracle because they merely accumulate IDs and do not compare
results or assert equality.

HISTORY / SCOPE
---------------
The file was introduced alongside AgentIterator/CvAgents as a profiling aid and remains
developer-only scaffolding. No shipped code enables `AGENT_ITERATOR_TEST`.

Any discrepancy in the commented/raw benchmark predicates would affect only a manually
enabled profiling experiment, not current game logic.

QUEUE-110 ROOT LEDGER
---------------------
F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 110 is closed. It is dormant test/profiling code under current build settings.

EXACT CONTINUATION
------------------
C031-WIP499 physical -> Queue110 CLOSED -> F654/KI#976 UNUSED ->
open priority Queue112 `CvGameCoreDLL/UWAI.cpp` (237 lines) -> establish current
SAS/Base AdvC 1.14 provenance and all wrapper/caller state transitions -> audit
initialization/reset/read/write enablement, background-UWAI mode, cache invalidation,
new/revived-player processing, team wrapper access, report lifetime and option toggles ->
dedup especially against F650/F652/F653 and earlier UWAI owner/cache findings ->
promote F654 only for a distinct current-live root -> freeze the next physical WIP at
the next settled boundary whether F654 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP500 -- QUEUE 112 UWAI.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP499.
- Queue 112 `CvGameCoreDLL/UWAI.cpp` is CLOSED.
- No new root was promoted.
- F654 / provisional KI#976 remains UNUSED.
- Files complete: 50 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 120 `CvGameCoreDLL/AgentIterator.cpp` (186 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
1. Global enable/background state
   ------------------------------
   `m_bEnabled` is serialized. `m_bInBackground` is intentionally not: immediately after
   loading, `CvGameAI::AI_sortOutUWAIOptions(true)` reconstructs background mode from
   the current GlobalDefine before UWAI turn logic resumes.

   `isEnabled(bool)` correctly distinguishes foreground versus background execution.

2. UI cache invalidation
   ---------------------
   UWAI simply forwards:
       invalidateUICache -> WarEvaluator::clearCache.
   F650 owns the demonstrated dirty-bit bug inside WarEvaluator; UWAI adds no separate
   stale state.

3. New-player initialization
   -------------------------
   `initNewPlayerInGame`:
       clears WarEvaluator cache;
       initializes the new team UWAI;
       initializes the new player UWAI.

   The supported `CvPlayer::initInGame` creation path uses a fresh player/team slot
   whose team is the normal same-ID new team, so the `GET_TEAM(eNewPlayer)` assumption
   is valid there.

   `processNewPlayerInGame` then updates that player's UWAICache and runs team `turnPre`
   for current major teams after CvAgents colony/new-player insertion has occurred.

   Malformed recycled-colony creation is already F447; generic revival cache corruption
   is F652/F653.

4. Revived-player initialization
   -----------------------------
   Current SAS's KI#475.3/KI#548 repair:
       initializes player UWAI skipped by dead-at-load deserialization;
       rebuilds incremental military unit counts for units created while the player was
       dead;
       intentionally does not reinitialize a surviving shared team's UWAI.

   CvTeam::changeAliveCount handles a team returning from zero members. This division of
   ownership is coherent.

   Defeated colonies skip this helper only because the adjacent CvPlayer parent guard
   skips the whole revival block; that is same-root breadth of F653, not an independent
   UWAI.cpp defect.

5. `processNewPlayerInGame`
   ------------------------
   The full cache update before team `turnPre` is necessary because several WarUtility
   aspects can run immediately for the new player. Current ordering supplies initialized
   owner/cache state before those uses.

   Any player absent from TeamAIIter due F652/F653 is a CvAgents root already owned.

6. `maxSeaDist` / `isReady`
   ------------------------
   - map-size and wrap scaling are bounded positive heuristics;
   - scenario readiness deliberately delays war planning for the first two elapsed
     turns because team AI initialization is incomplete at scenario start.
   No arithmetic/lifecycle contradiction survives.

7. Personality weighting
   ---------------------
   Current SAS sets `UWAI_PERSONALITY_PERCENT=120`, so the transformation is active.

   The shared `aiDevNull` fallback would be suspicious because different absent arrays
   have different logical defaults (e.g. MemoryDecay getter defaults to -1), and dummy
   values can participate in medians. However current SAS's 57 LeaderHeadInfos allocate
   ALL eight array groups traversed by this function:
       Flavors
       ContactRands
       ContactDelays
       MemoryDecays
       MemoryAttitudePercents
       NoWarAttitudeProbs
       UnitAIWeightModifiers
       ImprovementWeightModifiers

   Thus no current leader uses `aiDevNull`; the fallback remains mod/XML robustness only.

   The SAS `static const` read of UWAI_PERSONALITY_PERCENT would ignore a runtime
   `setDefineINT` change after first execution, but current Python does not mutate this
   define. Same dynamic-define debt class as Queue 089.

8. XML vector lifetime
   -------------------
   `doXML` is invoked from the normal XML loading pipeline once per DLL XML-load
   lifecycle. No current supported second call on the same UWAI instance without game/
   DLL reconstruction was found, so the push_back vectors do not accumulate duplicate
   aspect entries in current use.

HISTORY CLOSURE
---------------
High-value strata:
- practical 1841/1842: initial UWAI global wrapper and naming;
- practical 1846/2019: scenario/new-player integration;
- practical 2920/2954: comprehensive UWAI/scaled rewrite;
- practical 3999: fix broken UWAI_PERSONALITY_PERCENT handling and add null-array
  compatibility;
- later 4023/4065: refinements;
- SAS 5044: static-const personality define optimization;
- SAS 6283/6300: revived-player/UWAICache lifecycle repairs KI#475.3/KI#548.

No Base AdvC 1.14 repair is missing from the inherited core wrapper.

QUEUE-112 ROOT LEDGER
---------------------
F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 112 is closed. Remaining sharp edges are current-unactivated XML/dynamic-define
robustness or breadth of already-owned player-revival/cache roots.

EXACT CONTINUATION
------------------
C031-WIP500 physical -> Queue112 CLOSED -> F654/KI#976 UNUSED ->
open priority Queue120 `CvGameCoreDLL/AgentIterator.cpp` (186 lines) -> audit the
compile-time cache-selection matrix, exact-vs-filtered predicate trust, barbarian
insertion, relation parameter validation, end/next pointer lifetime and all status/
relation combinations -> reconcile especially against F652/F653 and existing
AgentIterator cache roots -> compare history and test scaffolding -> promote F654 only
for a distinct current-live root -> freeze next physical WIP at the next settled
boundary whether F654 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP501 -- QUEUE 120 AgentIterator.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP500.
- Queue 120 `CvGameCoreDLL/AgentIterator.cpp` is CLOSED.
- No new root was promoted.
- F654 / provisional KI#976 remains UNUSED.
- Files complete: 51 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 123 `CvGameCoreDLL/TeamPathFinder.cpp` (175 queued lines).
- No gameplay source was modified.

IMPORTANT PARKED ITERATOR DEFECT
--------------------------------
`PlayerIter<EVER_ALIVE,MEMBER_OF>` has a genuine cache/filter mismatch.

For that template combination:
    eCACHE_SUBSETEQ = NO_CACHE
    eCACHE_SUPER    = MEMBER

The MEMBER cache intentionally contains EVERY player slot currently assigned to the
team, regardless of ever-alive state.

But the optimized helper says:

    isEverAlive(kAgent):
        return (eCACHE != CvAgents::ALL || kAgent.isEverAlive());

Since eCACHE_SUPER is MEMBER rather than ALL, this returns true unconditionally and
does not test `kAgent.isEverAlive()`.

Five current call sites use the exact combination:
- CvTeam tech-era propagation;
- CvTeam persistent player-level tech processing;
- CvCity team-culture percent;
- CvPlot team-culture percent;
- CvTeamAI vassal/nuke-memory logic.

If a team's MEMBER cache contains a never-alive slot, those calls will incorrectly
process it as ever-alive.

WHY IT REMAINS BELOW F654
-------------------------
A current supported activation was not established.

At normal initialization, unused player slots retain their own TeamID. Shared-team
participants configured for a game become actual/ever-alive players. Current colony,
revival and recycling paths deal with players that are already ever-alive.

No shipped WB scenario or ordinary current lifecycle was found that leaves:
    player.isEverAlive()==false
while:
    player.getTeam()==an active existing team's TeamID.

Thus the code-level predicate defect is real and should be unit-tested/fixed, but the
album's current-live threshold is not met.

A narrow future fix would make `isEverAlive` recognize only caches that actually imply
ever-alive:
    CIV_EVER_ALIVE / CIV_ALIVE / MAJOR_ALIVE / MEMBER_ALIVE / VASSAL_ALIVE,
not generic MEMBER.

OTHER MATRIX CONTROLS
---------------------
- ALIVE+MEMBER_OF uses exact MEMBER_ALIVE and is coherent.
- ANY_STATUS+MEMBER_OF intentionally uses MEMBER without filtering.
- VASSAL_OF cache routing correctly falls back to current relation checks when a cached
  exact set is unavailable.
- POTENTIAL_ENEMY / KNOWN / ENEMY combinations select supersets that include minor/
  Barbarian agents as required by AgentPredicates comments.
- HUMAN intentionally routes through MAJOR_ALIVE because enum/documentation classify it
  among major non-Barbarian statuses.
- barbarian synthetic insertion is excluded for relations/statuses whose underlying
  exact cache already contains or intentionally excludes it.
- relation constructors assert a valid TeamTypes argument whenever relation != ANY.
- `short` cache position/size fields are safe for current 48-player/team maxima.
- random iterator shuffled-index ownership pairs new/delete[] correctly.
- recursion in `generateNext` is bounded by the cache length (<50 current agents).

HISTORY / SCOPE
---------------
The implementation was introduced with the CvAgents architecture and then refactored
into compile-time cache/predicate selection. Later changes are predicate additions,
template/iterator modernization and performance cleanup.

F652/F653 are lifecycle corruption of exact caches. Queue 120 itself behaves correctly
when those caches satisfy their declared invariants, apart from the parked
EVER_ALIVE+MEMBER_OF optimization above.

QUEUE-120 ROOT LEDGER
---------------------
F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 120 is closed. One exact template-level robustness defect is preserved in this
checkpoint but lacks a supported current state that activates it.

EXACT CONTINUATION
------------------
C031-WIP501 physical -> Queue120 CLOSED -> F654/KI#976 UNUSED ->
open priority Queue123 `CvGameCoreDLL/TeamPathFinder.cpp` (175 lines) -> establish all
callers and cache/state lifetime -> audit reachable-team graph construction, city/plot
ownership transitions, vassal/master/open-border/war traversal assumptions, path
distance and invalidation, null/index/recursion contracts -> compare with Base AdvC
history and existing path/topology findings -> promote F654 only for a distinct
current-live root -> freeze next physical WIP at the next settled boundary whether
F654 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP502 -- QUEUE 123 TeamPathFinder.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP501.
- Queue 123 `CvGameCoreDLL/TeamPathFinder.cpp` is CLOSED.
- No new root was promoted.
- F654 / provisional KI#976 remains UNUSED.
- Files complete: 52 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 125 `CvGameCoreDLL/UWAIReport.cpp` (166 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
1. SHALLOW_WATER deep-water ownership
   ----------------------------------
   The pathfinder blocks deep water unless:
       plot team == moving team.

   This initially looks inconsistent with the "mustn't enter deep water" summary, but
   the real `CvUnit::canMoveInto` has the same inherited AdvC/Firaxis exception for
   terrain/feature impassability inside the unit team's own cultural borders.

   Therefore UWAI's shallow transport finder is modeling a real movement rule rather
   than granting hypothetical extra access.

2. Hypothetical-war territory
   --------------------------
   A plot is treated as hostile/passable-under-war when:
       moving team already at war with plot team
   OR:
       plot team's master == war target's master.

   This correctly folds the target's master/vassal bloc into a hypothetical declaration:
   war on a vassal implicates its master; war on a master implicates its vassals.

   Other unrelated war plans and defensive pacts are intentionally not assumed because
   they may be abandoned/canceled.

3. Canal semantics
   ---------------
   Water modes can step through land only when a city or ActsAsCity improvement supplies
   a canal. Expensive full team-base checking was intentionally reduced to this physical
   canal predicate because border legality is separately checked.

   Current callers use the finder for strategic reachability, not exact unit loading
   legality.

4. Destination shortcuts
   ----------------------
   - LAND destination requires same area and nonwater.
   - water modes allow water destinations without an initial same-water-area precheck so
     canals can connect otherwise separate water areas;
   - coastal land destinations are allowed as unload destinations but not traversal.

   The finder still has to generate an actual path through valid steps, so accepting a
   broad destination shape does not itself create reachability.

5. Route cost
   ----------
   LAND mode intentionally ignores hills/features/rivers and assumes the from-plot route
   type for both routed endpoints. Enemy/hypothetical-enemy routes are ignored. These
   are documented strategic-distance approximations, not executor-equivalence claims.

6. Reset / reuse
   -------------
   UWAICache allocates one set of node maps and resets nodes/metrics for each known team,
   matching KmodPathFinder's reuse contract. Static one-off callers call `init` before
   each new team context.

   War target is never the moving team on current callers; NULL is safely replaced by
   Barbarian team solely to avoid downstream null checks when no hypothetical enemy is
   needed.

7. Limits / arithmetic
   -------------------
   current max path limits, movement denominator and route changes remain positive;
   heuristic weight is initialized before any generated path; `getPathCost` is read only
   after successful generation.

HISTORY / PROVENANCE
--------------------
TeamPathFinder is an AdvC strategic finder layered on KmodPathFinder. Its major history
replaced older team reachability helpers and then received performance/refactor changes.
Current Base AdvC 1.14 retains the same semantics; no later repair is missing from SAS.

Related map/path findings concern different layers:
- F636 stale scenario area topology;
- KmodPathFinder live path-state findings elsewhere;
- Queue 068 legacy verifier is compiled out.

QUEUE-123 ROOT LEDGER
---------------------
F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 123 is closed. Remaining differences from exact unit movement are explicitly
documented strategic approximations and match current caller expectations.

EXACT CONTINUATION
------------------
C031-WIP502 physical -> Queue123 CLOSED -> F654/KI#976 UNUSED ->
open priority Queue125 `CvGameCoreDLL/UWAIReport.cpp` (166 lines) -> audit report
ownership, mute nesting, stream/file lifetime, encoding/format calls, player/team name
lookup, synchronized-state isolation and diagnostic side effects -> dedup especially
against prior report/background findings -> promote F654 only for a distinct
current-live root -> freeze next physical WIP at the next settled boundary whether
F654 is consumed or unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP503 -- QUEUE 125 UWAIReport.cpp -- CLOSED / F654 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP502.
- Queue 125 `CvGameCoreDLL/UWAIReport.cpp` is CLOSED.
- No new root was promoted.
- F654 / provisional KI#976 remains UNUSED.
- Files complete: 53 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 129 `CvGameCoreDLL/AIStrengthMemoryMap.cpp` (145 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
1. Mute nesting is deliberately reference-counted:
       setMute(true)  -> ++m_iMuted
       setMute(false) -> --m_iMuted.
   This preserves an outer mute through nested ArmamentForecast/InvasionGraph/
   WarEvaluator diagnostics. Live pairs examined do not return between mute/unmute.

2. Silent reports set m_iMuted=1 and ignore setMute calls entirely, preserving stable
   silence without counter drift.

3. Report buffering/file writes:
   every nonmuted `log` appends then flushes/clears the report; destructor only appends
   the Textile footer when residual text exists, then frees every allocated narrow-string
   buffer. No persistent gameplay state is touched.

4. Network multiplayer suppresses the report unless general logging is enabled, matching
   the anti-cheat comment. Reporting remains diagnostic only.

5. `narrow` has a generic empty-string hazard:
       while (s[s.length()-1] == ' ')
   would index before the string if the converted name were empty or the caller supplied
   `iCharLimit=0`.
   Current callers use positive defaults (8/12) and current leader/unit/tech/city names
   are nonempty. No supported activation was found; preserve as API hardening only.

6. enum/name accessors receive validated current Player/Team/Unit/Tech/WarPlan values
   from diagnostic callers. SAS's shared WarPlan token helper removes the old duplicate
   label map rather than adding state.

HISTORY CLOSURE
---------------
The file's history is report-formatting, naming, silent/mute behavior, scaled-UWAI
refactors and SAS diagnostic-label cleanup. No gameplay mutation or unported Base AdvC
repair survives.

QUEUE-125 ROOT LEDGER
---------------------
F654 / KI#976
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 125 is closed. Remaining edges are diagnostic/API robustness only.

EXACT CONTINUATION
------------------
C031-WIP503 physical -> Queue125 CLOSED -> F654/KI#976 UNUSED ->
open priority Queue129 `CvGameCoreDLL/AIStrengthMemoryMap.cpp` (145 lines) ->
audit initialization/reset/read/write size/team identity, visibility/decay update
semantics, plot-index bounds, team death/revival/alliance lifecycle and consumers ->
dedup against existing AI-memory/team-revival findings -> promote F654 only for a
distinct current-live root -> freeze next physical WIP at the next settled boundary
whether F654 is consumed or unused.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP504 -- QUEUE 129 AIStrengthMemoryMap.cpp -- F654 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP503.
- Queue 129 `CvGameCoreDLL/AIStrengthMemoryMap.cpp` is OPEN.
- F654 / provisional KI#976 is CONFIRMED here.
- F655 / provisional KI#977 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F654 / provisional KI#976 -- a fully defeated team's enemy-strength memory stops
decaying while the team is dead and is not reset/re-aged on revival, so revived AI
can immediately pathfind and choose attacks using enemy stacks remembered from many
turns before its elimination
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited K-Mod/AdvC strength-memory lifecycle defect with a current supported
revival producer (WorldBuilder / verifyAlive). It is independent of F652/F653:
those findings concern CvAgents/UWAI revival cache membership; F654 survives even when
the player/team are restored to all correct iterator caches.

PURPOSE / NORMAL LIFETIME
-------------------------
AIStrengthMemoryMap is explicitly a rough memory of enemy land strength on plots.

It is updated when AI_localDefenceStrength actually observes a plot:

    GET_TEAM(getTeam()).AI_strengthMemory().set(p, iPlotTotal);

and, once per alive team turn, decayed in:

    CvTeamAI::AI_doTurnPost()
        -> m_strengthMemory.decay();

`decay()` applies:
    4% reduction every turn, rounding down,

unless the plot is currently visible and no enemy unit is there, in which case the
entry is erased immediately.

This is plainly time-sensitive intelligence rather than permanent history.

DEFEAT DOES NOT RESET THE MAP
-----------------------------
On player/team elimination:
- `CvPlayer::setAlive(false)` removes units/cities/deals and updates CvAgents;
- `CvTeam::changeAliveCount(0)` clears wars/vassal relations/war plans as appropriate.

Neither path calls:
    CvTeamAI::AI_reset()
or:
    m_strengthMemory.reset().

The strength-memory map therefore remains allocated with every pre-defeat entry.

`CvTeamAI::AI_reset` WOULD clear it:

    m_strengthMemory.reset();

but AI_reset is used for true team reset/reinitialization, not ordinary death.

DEAD INTERVAL ALSO STOPS DECAY
------------------------------
The only normal decay call is `CvTeamAI::AI_doTurnPost`.

A dead team does not take AI turns, so for every turn spent dead:
    no visible-empty erasure;
    no 4% decay.

The map is therefore frozen exactly at its elimination-time values.

REVIVAL DOES NOT RE-AGE OR CLEAR IT
-----------------------------------
When a previously dead team's first member is revived:

    CvPlayer::verifyAlive
        -> setAlive(true)
        -> CvTeam::changeAliveCount(+1)

`CvTeam::changeAliveCount` does initialize UWAI when the team transitions 0 -> 1:

    if (m_iAliveCount == 1 && previous <= 0 && isMajorCiv() && UWAI enabled)
        AI().uwai().init(getID());

but it does NOT reset `CvTeamAI` and does not touch `m_strengthMemory`.

Current SAS's KI#475.3 revived-player helper likewise initializes player UWAI and rebuilds
that player's military unit census only. It intentionally avoids reinitializing the
whole team and does not touch strength memory.

Therefore a correctly restored revived team inherits the exact strength-memory map from
the moment it died.

SUPPORTED REPRODUCTION
----------------------
Use the same current WorldBuilder revival lifecycle already documented by KI#475.3:

1. Team T is alive under AI control.
2. While enemy stack E is visible at fog-capable plot P, normal AI strength evaluation
   stores:
       strengthMemory[P] = 1000
   (any positive representative value works).
3. T is eliminated.
4. Leave T dead for N turns while the world continues.
5. Enemy E moves away / dies; P is not visible to T because T is dead.
6. In WorldBuilder, give T's dead player a unit or city and leave WB.
7. `verifyAlive` revives T.

Current:
    strengthMemory[P] is still exactly 1000.

A continuously alive team's corresponding memory would have decayed every turn.

INTEGER DECAY DELTA
-------------------
The recurrence is:

    value_next = floor(96 * value / 100).

Starting from 1000:

    after 10 turns  -> 662
    after 25 turns  -> 353
    after 50 turns  -> 120
    after 75 turns  -> 36
    after 100 turns -> 5
    after 105 turns -> 0

So a 100-turn dead interval can revive with:
    current stale value = 1000
versus:
    normal time-aged value = 5.

After 105 turns, the correct normal-memory analogue would be zero while current revival
still preserves the full original 1000.

FIRST REVIVED TURN CAN CONSUME STALE VALUE BEFORE DECAY
-------------------------------------------------------
The first post-revival decay is not immediate. It remains scheduled for:

    CvTeamAI::AI_doTurnPost()

at the end of the team's turn.

AI/pathfinding activity earlier in that revived turn can therefore consume the full
stale value before even one 4% decay has occurred.

DIRECT PATHFINDER CONSEQUENCE
-----------------------------
`GroupPathFinder` uses strength memory whenever MOVE_ATTACK_STACK evaluates a fogged
destination:

    if (kTo.isVisible(eTeam))
        compute actual local defence;
    else
        iEnemyDefence =
            GET_TEAM(eTeam).AI_strengthMemory().get(kTo.plotNum());

If positive, the remembered defence:
- adds PATH_COMBAT_WEIGHT;
- computes attack ratio against the remembered strength;
- adds potentially large movement cost when the ratio is below 400%.

Thus a revived land stack can avoid/reroute around P because of an enemy army that
existed before the civilization died, even if that army disappeared a century earlier.

OTHER LIVE CONSUMERS
--------------------
The same map is consulted by:
- CvSelectionGroupAI path/stack decisions;
- CvUnitAI city assault and target-safety logic;
- additional AI danger/attack computations.

F654 therefore changes actual AI movement/targeting, not only diagnostics.

SAVE/LOAD DOES NOT SELF-HEAL
----------------------------
AIStrengthMemoryMap is serialized for each team. Normal CvTeamAI read begins from reset,
then reads the stored sparse map back.

If the game is saved while the team is dead and later loaded, its frozen pre-defeat
memory is preserved exactly; no elapsed-dead-time metadata exists from which to apply
missed decay on revival.

LINEAGE
-------
K-Mod introduced the original strength-memory concept and per-turn decay.

AdvC practical 2676 refactored it into `AIStrengthMemoryMap`, preserving:
- reset only through CvTeamAI reset;
- per-turn decay in team AI turn-post;
- direct serialization.

Subsequent practicals optimize sparse storage, reproducibility and plot indexing but do
not add defeat/revival lifetime handling.

Current Base AdvC 1.14 retains the same lifecycle. SAS's recent revival fixes initialize
UWAI/player caches but do not reset/re-age strength memory.

WHOLE-ALBUM DEDUP
-----------------
No existing finding owns strength-memory persistence through a fully dead team interval.

Earlier Permanent Alliance audits explicitly classified unmerged StrengthMemory as
tactical/fog-of-war state rather than durable relationship state. That supports F654's
lifetime interpretation: it is transient intelligence that should not remain frozen
indefinitely.

F652/F653:
    revival AgentIterator/cache restoration.

KI#475.3/KI#548:
    revived player UWAI initialization and missed military-unit inventory.

Neither changes the team strength-memory map.

REPAIR BOUNDARY
---------------
The safest revival policy is to clear strength memory when a team returns from zero
alive members:

    m_strengthMemory.reset();
    m_strengthMemory.init(map.numPlots(), teamID);

(or a dedicated clear-for-revival helper).

Why reset rather than replaying N missed decays:
- exact observations may be arbitrarily old;
- during death the team has no meaningful visibility/intelligence network;
- no death-turn timestamp is currently stored with each entry;
- a fresh zero map is conservative and deterministic.

If designers prefer remembered intelligence after resurrection, store team death turn
and apply the missed number of integer decay steps before the first consumer. Entries
whose plots are now visible and empty should also be cleared before use.

Do not reset a shared team when only one member dies/revives and another member kept the
team alive; in that case AI_doTurnPost continued decaying the TEAM's memory normally.
The trigger should be the authoritative team alive-count transition 0 -> 1.

VALIDATION TARGETS
------------------
1. Alive team memory 1000, 100 normal turns -> 5.
2. Fully dead team for 100 turns, then revival:
   current fails at 1000; repaired policy should be fresh/appropriately aged.
3. 105-turn dead interval:
   no full-strength phantom remains.
4. Shared team where one member dies but team stays alive:
   do not clear memory on member revival; normal team decay continued.
5. Revived team first AI turn:
   GroupPathFinder must not consume pre-death phantom defence.
6. Visible empty remembered plot after revival:
   memory is zero before strategic movement.
7. Save while dead -> load -> revive:
   same repaired result as uninterrupted dead interval.
8. Current ordinary alive-team decay remains unchanged.
9. F652/F653 revival-cache tests compose independently.

POST-F654 LEDGER / EXACT RESUME
-------------------------------
Queue 129 `CvGameCoreDLL/AIStrengthMemoryMap.cpp` -- OPEN.
F654 / provisional KI#976 -- CONFIRMED.
F655 / provisional KI#977 -- UNUSED.

Exact continuation:
C031-WIP504 physical -> Queue129 OPEN -> exclude F654 plus F652/F653 and prior tactical-
memory owners -> finish sparse-map read/write/reset/decay bounds and team-leader
visibility semantics -> inspect zero-entry retention, malformed plot indices and
reproducibility ordering -> classify all AIStrengthMemoryMap history -> either promote
a genuinely independent F655 or close Queue129 cleanly -> freeze next physical WIP
regardless.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP505 -- QUEUE 129 AIStrengthMemoryMap.cpp -- CLOSED / F655 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP504.
- Queue 129 `CvGameCoreDLL/AIStrengthMemoryMap.cpp` is CLOSED.
- Queue-129 confirmed root:
    F654 / provisional KI#976
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 54 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 130 `CvGameCoreDLL/CvRandom.cpp` (137 queued lines).
- No gameplay source was modified.

FINAL CONTROLS
--------------
- Sparse zero entries are semantically zero and bounded by map plots; save output drops
  them. No unbounded current growth.
- read/write pair current sparse entries coherently. Invalid serialized plot indices are
  malformed-save robustness; current writer only emits CvPlot::plotNum values.
- ENABLE_REPRO_TEST sorts hash-map output explicitly; ordinary save order need not be
  deterministic because loading reconstructs the same key/value map.
- team leader used for visible-enemy-unit clearing is maintained on alive transitions;
  detection/visibility semantics are team-based for current use.
- reset/init clear the map for genuine new-team/player initialization.
- only the fully-dead-team revival lifetime omits reset/aging; that is F654.

HISTORY CLOSURE
---------------
Practical 2676 refactored K-Mod strength memory into this sparse class. Later changes
adjusted decay iteration, plot-index caching, reproducibility ordering and Barbarian
support without altering the defeat/revival lifetime. No second current root survived.

QUEUE-129 ROOT LEDGER
---------------------
F654 / KI#976
    fully dead team freezes tactical enemy-strength memory and resumes with stale
    pre-defeat values on revival.

F655 / KI#977
    UNUSED.

EXACT CONTINUATION
------------------
C031-WIP505 physical -> Queue129 CLOSED -> F654 confirmed ->
F655/KI#977 UNUSED -> open priority Queue130 `CvGameCoreDLL/CvRandom.cpp` (137 lines) ->
audit RNG state transition, range mapping/bias, zero/negative bounds, synchronized vs
async callers, seed/reset/read/write lifecycle, shuffle ownership and reproducibility
contracts -> compare K-Mod/Civ4CE/Base AdvC lineage and whole-album RNG findings ->
promote F655 only for a distinct current-live root -> freeze next WIP regardless.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP506 -- QUEUE 130 CvRandom.cpp -- CLOSED / F655 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP505.
- Queue 130 `CvGameCoreDLL/CvRandom.cpp` is CLOSED.
- No new root was promoted.
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 55 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 138 `CvGameCoreDLL/CvDLLInterfaceIFaceBase.cpp` (91 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
1. Core RNG transition
   -------------------
   The inherited 32-bit LCG:
       seed = 1103515245 * seed + 12345
   uses unsigned arithmetic and therefore has defined modulo-2^32 wraparound.

   Output range mapping uses the upper 16 seed bits:
       floor(high16 * range / 65536)
   and returns exactly [0,range-1] for positive 16-bit ranges. The small unavoidable
   modulo bias when 65536 is not divisible by the range is inherited RNG policy, not a
   contract error.

2. SAS KI#72 int-range safety guard
   --------------------------------
   Current SAS adds:
       if (iNum <= 0) {
           assert only when negative;
           return 0;
       }

   Base AdvC 1.14 asserted on a negative int before casting to unsigned short; a real
   `AI_chooseProduction` caller produced -4 and SAS fixed both the observed callers and
   this central safety net.

   The guard also means `get(0)` no longer advances the RNG seed. This is deliberate
   current SAS semantics and was already treated as such in the album's earlier
   zero-agent starting-location control:
       MapRandNum(0) -> 0 without advancing RNG.

   No supported current caller was found that relies on a dummy zero-range call
   consuming one synchronized random state transition. Do not create a new root for a
   deliberate safety-policy stream change.

3. Shuffle
   -------
   Both array and vector overloads implement Fisher-Yates:
       j = i + get(size-i)
   over positive current sizes.

   Empty vector asserts only as an intent warning; loop body is safe. The raw array
   overload's negative size would be API misuse, and all current callers derive
   nonnegative element counts.

4. `weightedChoice`
   ----------------
   Current live callers supply:
   - valid-plot probability weights from nonnegative maps;
   - Barbarian shelf weights after a total-weight >0 check;
   - ordinary uniform candidate vectors.

   If summed weights exceed 65535, proportional downscaling floors each entry and
   recomputes total. With current candidate vector sizes, a positive distribution cannot
   have every entry round to zero under this scaling; at least one significant current
   weight survives. Negative individual weights are unsupported and absent in callers.

   A zero total intentionally returns NULL without an RNG draw; callers already treat
   "no weighted candidate" as no selection.

5. Float helper
   ------------
   `getFloat` divides `get(65535)` by 65535. Since get(65535) returns at most 65534,
   result lies in [0,1), not 1.0.

6. External/setup RNG
   ------------------
   `getExternal` first reports the call to `CvInitCore::externalRNGCall`, then executes
   the same generator. This is the intended setup-random-option tracking path.

7. State lifetime / serialization
   ------------------------------
   `reset`, `init`, `reseed`, `read` and `write` operate on the one serialized
   `m_uiRandomSeed` consistently.

   `CvRandomExtended` serializes its diagnostic file name immediately after base seed;
   read clears then restores it symmetrically. Logging observes the PRE-draw seed as
   intended for reproducibility traces and does not mutate RNG state.

HISTORY / PROVENANCE
--------------------
Current `CvRandom.cpp` is executable-semantically identical to Base AdvC 1.14; SAS's
meaningful local change is the KI#72 int-overload safety guard in `CvRandom.h`.

K-Mod/AdvC history in this small file covers:
- initial high-bit LCG wrapper;
- external RNG setup tracking;
- logging/reproducibility support;
- shuffle relocation/refactor;
- weighted-choice helper and overflow downscaling;
- enum/style/include cleanup.

No later Base 1.14 repair is missing and no independent current-live F655 survived.

QUEUE-130 ROOT LEDGER
---------------------
F655 / KI#977
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 130 is closed. Remaining concerns are unsupported invalid-range/negative-weight
API hardening or intentional RNG-policy choices, not a distinct current gameplay bug.

EXACT CONTINUATION
------------------
C031-WIP506 physical -> Queue130 CLOSED -> F655/KI#977 UNUSED ->
open priority Queue138 `CvGameCoreDLL/CvDLLInterfaceIFaceBase.cpp` (91 lines) ->
establish whether this tiny interface file contains live executable wrappers or only
ABI scaffolding -> audit virtual/interface forwarding, pointer/reference lifetimes,
export/EXE contracts and any SAS divergence -> compare Base/K-Mod/Civ4CE and album
ownership -> promote F655 only for a distinct current-live root -> freeze next WIP at
the next settled boundary whether F655 is consumed or unused.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP507 -- QUEUE 138 CvDLLInterfaceIFaceBase.cpp -- CLOSED / F655 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP506.
- Queue 138 `CvGameCoreDLL/CvDLLInterfaceIFaceBase.cpp` is CLOSED.
- No new root was promoted.
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 56 / 169.
- Files open: 0 / 169.
- Next selected priority file:
    Queue 151 `CvGameCoreDLL/WarEvalParameters.cpp` (74 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
This tiny cpp file contains only the AdvC/K-Mod `addMessage` wrapper plus the SAS label-
font wrapper.

1. AdvC message filtering / force behavior
   - R&F silent AI Auto Play intentionally drops messages while the human is disabled.
   - ordinary AI messages are suppressed unless R&F is preserving them for a later
     human chapter.
   - major/log/chat messages during ordinary AI Auto Play are forced immediately and
     stripped of sound/icon/arrows as intended.
   - globe view correctly clears `bForce`; practical 4038 fixed the older reversed
     condition that had forced essentially every normal on-screen message.
   - default duration and default WHITE flash color are bounded/current.

2. Plot overload
   The CvPlot-reference overload forwards only the plot coordinates and otherwise
   preserves the same message arguments. All live callers pass a valid plot reference.

3. SAS label-font wrapper
   SAS practical 5668 added one centralized wrapper around the outgoing message text:
       `<font=SAS_UI_FONT_LABEL> ... </font>`
   unless the string is empty, already contains `<font=`, or the configured label size
   is outside 1..4.

   Current `SAS_UI_FONT_LABEL` is XML configuration loaded before normal message traffic;
   no runtime setter for this define exists in current Python/C++ call sites. Therefore
   the local static-const cache is consistent with current configuration lifetime.

   Existing message markup (color/link/icon tags) is intentionally nested inside the
   outer font tag and the EXE already parses such tagged message payloads. Strings that
   already carry their own font markup are left untouched. No current malformed-markup,
   gameplay or logging contradiction was demonstrated.

4. ABI/interface boundary
   `addMessageExternal` remains the protected pure-virtual EXE-facing slot with the same
   signature/order as Base AdvC. The nonvirtual wrapper does not alter object ownership
   or retain any caller pointer/string reference after return.

PROVENANCE / HISTORY
--------------------
- practical 3177 moved the growing inline wrapper from the interface header into a cpp;
- practical 3179 renamed the file to the current InterfaceIFaceBase spelling;
- practical 4038 fixed the reversed globe-view force test;
- SAS 5000 caches the WHITE color lookup;
- SAS 5668 adds centralized event-message label-font wrapping;
- later SAS changes are formatting/comment cleanup.

Current Base AdvC 1.14 matches all non-SAS wrapper behavior. K-Mod/Civ4CE carry the
underlying interface/message semantics in their older header/source layout.

QUEUE-138 ROOT LEDGER
---------------------
F655 / KI#977
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 138 is closed. Remaining concerns are presentation/configuration choices or
unsupported runtime mutation of a static UI define, not a distinct current-live bug.

EXACT CONTINUATION
------------------
C031-WIP507 physical -> Queue138 CLOSED -> F655/KI#977 UNUSED ->
open priority Queue151 `CvGameCoreDLL/WarEvalParameters.cpp` (74 lines) ->
audit constructor state, ally/extra-target closure over masters/vassals, sponsor/
immediate-DoW coupling, no-war-vs-extra semantics and the cache perfect-hash encoding ->
carry forward Queue074's proven mathematical getID collisions but promote only if two
supported cache states actually collide -> compare history/Base AdvC and whole-album
ownership -> freeze next WIP whether F655 is consumed or unused.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP508 -- QUEUE 151 WarEvalParameters.cpp -- CLOSED / F655 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP507.
- Queue 151 `CvGameCoreDLL/WarEvalParameters.cpp` is CLOSED.
- No new root was promoted.
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 57 / 169.
- Files open: 0 / 169.
- All explicitly marked `priority next wip` queues are now complete.
- Ordinary queue order resumes with:
    Queue 026 `CvGameCoreDLL/CvInitCore.cpp` (2132 queued lines).
- No gameplay source was modified.

SATURATION PASS
---------------
1. Constructor state
   -----------------
   Agent/target/report/sponsor/capitulation are stored directly.
   `consideringPeace` is derived from actual current agent-target war state.
   Capitulation is asserted to be an at-war evaluation.
   Sponsor implies immediate DoW as intended.

2. War ally / extra target closure
   --------------------------------
   `addWarAlly` and `addExtraTarget` add:
       supplied team
       its master
       its direct major vassals.

   A generic call with a dependent vassal would not automatically add all sibling
   vassals of that vassal's master. This is a real abstract closure limitation.

   Current callers do not provide a clean activation:
   - diplo-vote ally lists are built from FREE_MAJOR_CIV candidates;
   - end-all-war extra targets are FREE_MAJOR_CIV enemies;
   - imminent-two-front extra target comes from FREE_MAJOR_CIV potential enemies;
   - direct joint-war handling normalizes a vassal main target to its master before
     parameter construction;
   - no supported caller was found that passes a dependent vassal as ally/extra target
     and then requires sibling-vassal closure.

   Keep as parameter-API robustness, not F655.

3. `isNoWarVsExtra`
   ----------------
   The helper requires:
       extra targets present
       not considering peace
       not immediate DoW
       agent not actually at war with main target.
   That matches its documented use: compare war against the main target while dropping
   the extra wars in the recursive peace scenario. Current callers intentionally call
   `setNotConsideringPeace` where this special comparison is wanted.

4. Sponsor / immediate-DoW coupling
   --------------------------------
   Constructor sponsor and later `setSponsor` both force immediateDoW=true.
   Clearing/replacing sponsor with NO_PLAYER does not automatically clear immediateDoW,
   but no current caller uses setSponsor as a reversible toggle; the flag can also be
   deliberately set independently for vote/joint-war scenarios.

5. `getID` perfect-hash claim is false
   -----------------------------------
   Queue 074 already established the arithmetic defect.

   Current positional sequence does:
       ... total; *2;
       naval; *2;
       prep+1; *50;

   so preparation time is added after only a base-2 shift and can overlap the naval/
   total positions. Algebraic examples:
       nonnaval prep=2  == naval prep=0
       limited prep=4   == total prep=0
   with all other encoded fields equal.

   The declared "perfect hash" therefore is not injective in the full documented
   parameter domain.

   However the full cache-caller sweep still does not yield two supported colliding
   states with the same remaining fields under an enabled cache:
   - sponsored declare-war-trade forces immediate DoW and prep=0;
   - willing-to-talk/at-war UI cache uses prep=0;
   - current explicit 3/5/default-preparation callers do not share sponsor,
     considering-peace, immediate-DoW and cache-use state in a colliding pair;
   - warAllies/extraTargets scenarios are intended to run uncached.

   Keep this as a strong unit-test/robustness defect but not F655 without a current
   gameplay collision.

6. Range / width
   -------------
   - large-player DLL uses `__int64` WarEvalParamID; practical 2948 fixes the old
     48-civ width risk.
   - prep+1<50 remains an assert-domain requirement. Very slow custom game speeds can
     produce larger theoretical defaults, but current cache-active callers force small
     preparation values.
   - ally/extra-target sets are intentionally excluded from the ID and assert empty
     whenever getID is called; current supported cached callers obey that contract.

HISTORY / PROVENANCE
--------------------
High-value history:
- practical 1253: initial WarEvalParameters/UWAI parameter object;
- practical 1335: extra-target/no-war-vs-extra semantics;
- practical 1478: int mixed-base cache ID;
- practical 2770: capitulation parameter assertion/context;
- practical 2920/2954: scaled/UWAI refactor;
- practical 2948: 48-civ ID width fix;
- later changes are type/style/include cleanup.

Current SAS and Base AdvC 1.14 retain the same semantic parameter implementation.
K-Mod/Civ4CE do not contain this exact AdvC UWAI parameter class.

QUEUE-151 ROOT LEDGER
---------------------
F655 / KI#977
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 151 is closed. The malformed mixed-base hash is a real latent robustness defect,
but no distinct current-live colliding cache state was proved after the complete caller
sweep. Other sharp surfaces are caller-constrained parameter API limitations.

SCHEDULING TRANSITION
---------------------
The reconciled album rule was to process every still-pending entry explicitly marked
`priority next wip` before returning to ordinary queue order.

Queue 168 `CvGameCoreDLL/ArithmeticTraits.cpp` remains the final such marked entry.

Complete Queue 168 before ordinary queue order resumes at Queue 026 `CvGameCoreDLL/CvInitCore.cpp`.

EXACT CONTINUATION
------------------
C031-WIP508 physical -> Queue151 CLOSED -> F655/KI#977 UNUSED ->
open final priority Queue168 `CvGameCoreDLL/ArithmeticTraits.cpp` -> inspect the tiny
compile-time trait definitions and history -> close or promote only a distinct current-live
root -> then resume ordinary Queue026.

No gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP509 -- QUEUE 168 ArithmeticTraits.cpp -- CLOSED / PRIORITY RELOOP COMPLETE
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP508.
- Queue 168 `CvGameCoreDLL/ArithmeticTraits.cpp` is CLOSED.
- No new root was promoted.
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 58 / 169.
- Files open: 0 / 169.
- ALL explicitly marked priority-next-WIP entries are now complete.
- Ordinary queue order resumes at Queue 026 `CvGameCoreDLL/CvInitCore.cpp`.
- Queue 108 table bookkeeping was reconciled to its already-physical WIP498 closure.
- No gameplay source was modified.

QUEUE-168 CLOSURE
-----------------
The implementation file defines only:
    arithm_traits<float>::min = -FLT_MAX
    arithm_traits<float>::max =  FLT_MAX
    arithm_traits<double>::min = -DBL_MAX
    arithm_traits<double>::max =  DBL_MAX

The negative minima are intentionally correct; C/C++ `FLT_MIN` and `DBL_MIN` are the
smallest positive normalized values, not the lowest representable values.

The header's integer, bool, enum and ScaledNum specializations are compile-time trait
metadata. The two cpp history touches are introduction and small cleanup. There is no
runtime state/lifetime/caller surface from which a current bug could emerge.

QUEUE-168 ROOT LEDGER
---------------------
F655 / KI#977
    UNUSED.

PRIORITY-RELOOP COMPLETION
--------------------------
The special stronger-WIP scheduling tranche that began after the earlier reconciled
album notes is now fully exhausted. Do not continue using the stale "skip until priority
WIPs are done" annotations as active scheduling barriers; their condition is satisfied.

Resume the normal numeric queue at the first still-PENDING entry:
    Queue 026 `CvGameCoreDLL/CvInitCore.cpp` (2,132 queued lines).

EXACT CONTINUATION
------------------
C031-WIP509 physical -> all priority queues complete -> F655/KI#977 UNUSED ->
open ordinary Queue026 CvInitCore.cpp -> establish SAS/Base AdvC/K-Mod/Civ4CE semantic
diff and full history/caller map -> audit game/setup slot lifecycle, options/custom map
options, player/team/civ/leader selections, random setup, WB/load copy/reset, multiplayer
sync/serialization, string/password/map-script state, external RNG option tracking,
enum/index/range/ownership contracts -> whole-album dedup before numbering -> promote
F655 only for a distinct current-live root -> freeze the next physical WIP at the next
settled boundary whether F655 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified.
===============================================================================

===============================================================================
C031-WIP510 -- QUEUE 026 CvInitCore.cpp -- OPEN BASELINE / F655 UNUSED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP509.
- All explicit priority-next queues are complete.
- Ordinary queue order has resumed.
- Queue 026 `CvGameCoreDLL/CvInitCore.cpp` is OPEN.
- F655 / provisional KI#977 remains UNUSED.
- Files complete: 58 / 169.
- Files open: 1 / 169.
- No gameplay source was modified.

INITIAL PROVENANCE / SEMANTIC DIFF
----------------------------------
Current SAS `CvInitCore.cpp` has exactly 2132 physical lines, matching the queue count.

A function-body normalization against supplied Base AdvC 1.14 found only two semantic
function differences:
    `resetGame`
    `read`

Everything else is executable-semantically Base-1.14-compatible once comments/formatting
are removed.

`read`
------
The difference is the already-reconciled SAS KI#46 policy:
- historical `uiFlag < N` save-migration branches were removed;
- current reader consumes the current writer layout directly;
- current same-version pairing remains the relevant audit contract.

Do not treat loss of old-save compatibility as a Queue-026 finding unless a current
writer/current reader mismatch is established.

`resetGame` / `resetPlayer`
---------------------------
SAS practical 5083 caches XML standard defaults as local static constants, but only
inside:

    if (GC.isCachingDone())

This preserves the Base AdvC protection against querying defines before XML is loaded.

Cached values:
    STANDARD_CLIMATE
    STANDARD_SEALEVEL
    STANDARD_ERA
    STANDARD_GAMESPEED
    STANDARD_TURNTIMER
    STANDARD_CALENDAR
    STANDARD_HANDICAP

Current Python/C++ search finds no runtime setter for any of these exact defines.
Normal setup/reset calls therefore see immutable post-XML configuration and the static
cache does not presently create a live stale-value defect.

Keep dynamic-define mutation as mod/API robustness only.

QUEUE-026 AUDIT PLAN / EXACT RESUME
-----------------------------------
C031-WIP510 physical -> Queue026 OPEN -> F655/KI#977 UNUSED ->
continue from the current semantic-diff baseline into:

1. reset/uninit/default lifecycle:
   - game fields;
   - player fields;
   - dynamic custom-map-option/victory arrays;
   - graphics-dependent civ cleanup.

2. player slot/state matrix:
   - SlotStatus / SlotClaim;
   - human/open/computer/takeover-AI semantics;
   - reassignPlayer;
   - team/civ/leader/color/art/handicap/random-choice flags;
   - active-player and network slot interactions;
   - recycled/new-player cross-checks against F477/F479/F647/F652.

3. map/game setup:
   - map script and Python existence;
   - hidden/custom map options;
   - world/climate/sea/era/speed/calendar;
   - turn timer, victories and options;
   - scenario/WB no-player and Pangaea state.

4. network/admin/PBEM/hotseat:
   - passwords/email/SMTP;
   - ready/slot claim;
   - multiplayer checksum fields;
   - active-player and game-type transitions.

5. serialization:
   - current write/read order;
   - array lengths and allocation ownership;
   - reset-before-read;
   - strings/enum maps;
   - current-version-only KI#46 boundaries.

6. full path-history classification and whole-album dedup.

Promote F655 only for a distinct current-live root; freeze another physical WIP at the
next settled boundary whether F655 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP511 -- QUEUE 026 CvInitCore.cpp -- F655 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP510.
- Ordinary Queue 026 `CvGameCoreDLL/CvInitCore.cpp` remains OPEN.
- F655 / provisional KI#977 is CONFIRMED here.
- F656 / provisional KI#978 is UNUSED.
- No gameplay source was modified.

EARLY QUEUE-026 CONTROLS CLOSED BELOW F655
------------------------------------------
Several initially sharp reset/load/setup surfaces were traced through their complete
current lifecycles before numbering anything:

1. `clearCustomMapOptions` does not zero `m_iNumHiddenCustomMapOptions`, so a Python-map
   -> WorldBuilder-map refresh can leave a stale hidden count. AdvC practical 1390
   deliberately removed the option-count getters from the EXE export surface, and the
   one current SAS C++ consumer clamps hidden options to the actual option count. WB
   maps have zero custom options there, so no current gameplay/UI consequence was
   demonstrated.

2. `CvInitCore::read` loads `m_aeCiv` directly and can temporarily coexist with an old
   `CvPlayer::m_pCivilization` wrapper. The actual loaded-core -> live-core handoff goes
   through `resetPlayer(..., pSource)`, which calls `setCiv` before the player's second
   reset. A changed civilization therefore rebuilds the wrapper; an unchanged one
   already matches. The temporary mismatch does not survive normal load.

3. Practical 2323's EnumMap read-size memory-corruption repair, practical 4151's
   active-team network-coop synchronization repair and practical 4288's QuickStart
   `setType(string)` fall-through repair are all present.

4. The SAS practical-5083 static caches for standard Climate/SeaLevel/Era/GameSpeed/
   TurnTimer/Calendar/Handicap are created only after XML caching is complete. Current
   Python/C++ does not mutate these exact defines at runtime.

RETRY-BRANCH EVIDENCE RETAINED FOR CONTINUATION
-----------------------------------------------
The earlier competing C031-WIP510 download treated Queue026 as closed before the later,
stronger branch proved F655. Its closure judgment and F655-unused ledger are superseded,
but these useful controls were not present verbatim in that later branch and are retained
as evidence for its continuing saturation pass:

- `setActivePlayer` updates the prior player-history snapshot before switching, refreshes
  the cached active team and claims the new slot; identity setters refresh live derived
  state once player statics exist.
- `getAvailableSlot` prefers OPEN slots, then eligible takeover-AI computer slots;
  `closeInactiveSlots` / `reopenInactiveSlots` follow their Pitboss, Hotseat, PBEM and
  scenario mode restrictions.
- Name, password and checksum-string arrays pair their bounds and ownership. The total-name-
  length counter is only an `FASSERT_ENABLE` scenario diagnostic, not release gameplay.
- The current writer/read stream was mechanically paired across setup strings and enums,
  custom/victory/game options, turn and Advanced Start state, per-player strings and maps,
  random-choice flags, slot state and playable/minor flags. Read clears dynamic storage
  first, intentionally changes assigned claims to reserved, and preserves AdvC's within-
  game-load player-reset ordering. KI#46 removed old-format migration, not current-format
  symmetry.

These controls remain inputs rather than a replacement for the open stronger audit. The
later branch's supported-config interpretation of F655 is authoritative.

-------------------------------------------------------------------------------
F655 / provisional KI#977 -- `reRandomizeCivsAndLeaders` never marks a newly
assigned random leader as taken, so its "unique leaders" algorithm can assign the
same leader to multiple players even when enough unique leaders exist
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC setup-algorithm defect with a supported but currently
configuration-gated SAS activation.

IMPORTANT SCOPE:
Current AdvC-SAS deliberately sets:

    PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT = 0

which returns from this AdvC re-randomizer immediately and retains BtS behavior.
Therefore F655 does NOT affect default current SAS setup.

The XML define is, however, an intentional supported AdvC configuration feature. Base
AdvC 1.14 recommends and ships value 20. When enabled in current SAS, the 48-player DLL
and 36-civilization roster make the uniqueness failure directly reachable.

ROOT
----
`CvInitCore::reRandomizeCivsAndLeaders` first builds a persistent map of leaders already
claimed by NON-random setup slots:

    EagerEnumMap<LeaderHeadTypes,bool> abLeaderTaken;

    for each setup player:
        if (!abRandomize[player])
            abLeaderTaken.set(getLeader(player), true);
        else
            iRandomLeadersNeeded++;

It then counts every playable leader not in that map:

    if (bPlayable && !abLeaderTaken.get(eLeader))
        iLeadersAvailable++;

and explicitly refuses the enhanced algorithm when uniqueness is impossible:

    if (iRandomLeadersNeeded > iLeadersAvailable)
    {
        FAssertMsg(...,
            "Can't assign unique leaders; falling back on BtS algorithm.");
        return;
    }

For each random player, civilization weights and the candidate leader list are again
filtered through the SAME `abLeaderTaken` map:

    if (!abLeaderTaken.get(leadersPerCiv[eCiv][j]))
        iWeight += iPER_EXTRA_LEADER_CIV_SELECTION_WEIGHT;

    ...

    if (!abLeaderTaken.get(leadersPerCiv[eNewCiv][j]))
        aeAvailableLeaders.push_back(...);

After selecting the random leader, however, current code only does:

    setCiv(ePlayer, eNewCiv);
    setLeader(ePlayer, eNewLeader);

It NEVER executes:

    abLeaderTaken.set(eNewLeader, true);

Thus the map describes only fixed/non-random setup leaders for the whole function.
Every leader assigned to an earlier random player remains "available" to every later
random player.

CIV-USE STATE DOES UPDATE
-------------------------
This omission is especially clear because the adjacent civilization state IS updated:

    aiTakersPerCiv.add(eNewCiv, 1);

The algorithm therefore remembers that a civilization was selected but forgets that its
leader was selected.

For the first pass it spreads random players over least-used civilizations. Once the
number of random players exceeds the number of playable civilizations, civilization
reuse is intentional; at that point one-leader civilizations can deterministically
reuse their only leader because the leader-taken map was never changed.

CURRENT SAS DATA
----------------
Mechanical current-XML enumeration gives:

    MAX_CIV_PLAYERS:                       48
    playable civilizations:                36
    unique playable mapped leaders:        55

So a 48-player all-random setup passes the function's own uniqueness feasibility test:

    iRandomLeadersNeeded = 48
    iLeadersAvailable    = 55

and the enhanced algorithm promises to assign unique leaders.

The 48 slots necessarily exceed the 36 civilization identities, so at least 12
civilization assignments must be repeats after the least-used-civ pass has exhausted
all 36 once.

EXACT SYNCHRONIZED-RNG REPRODUCTION
-----------------------------------
A literal arithmetic translation of the current routine was run against the current
SAS civilization/leader XML order and current `CvRandom` implementation:

    seed = 1103515245 * seed + 12345 (mod 2^32)

    result =
        (((seed >> 16) & 65535) * upper) / 65536

Fixture:
    48 SS_TAKEN/SS_COMPUTER major slots
    all 48 had civilization + leader chosen randomly
    unrestricted leaders OFF
    PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT = 20
    synchronization seed = 0
    current SAS 36-civ / 55-leader XML

Current algorithm:
    assignments completed: 48
    distinct leaders:      38

A particularly simple duplicate is:

    Player 2:
        CIVILIZATION_AZTEC
        LEADER_MOCTEZUMA

    Player 36:
        CIVILIZATION_AZTEC
        LEADER_MOCTEZUMA

The later Aztec assignment sees Montezuma as available because `abLeaderTaken` still
contains only leaders that were fixed before re-randomization.

The same seed produces ten duplicated leader identities overall. Other sampled valid
seeds (1, 42, 12345, 0xDEADBEEF) likewise completed all 48 assignments with only
38-41 distinct leaders, confirming that the seed-0 example is not a numerical corner.

ONE-LINE ORACLE
---------------
Using the same exact XML/RNG fixture but updating the local map immediately after each
random assignment:

    abLeaderTaken.set(eNewLeader, true);

produces:

    assignments completed: 48
    distinct leaders:      48

with the same seed 0.

The corrected algorithm therefore satisfies the current precheck (`55 >= 48`) without
needing to fall back to BtS behavior.

HISTORY -- EXACT BIRTH IN PRACTICAL 2532
----------------------------------------
Practical 2532 / commit `60a15e6434` ("Handle assignment of random civs and leaders in
the DLL") introduced the entire `reRandomizeCivsAndLeaders` routine.

That original patch already contains:
- `abLeaderTaken`;
- the unique-leader feasibility assertion;
- civilization weighting by not-yet-taken leaders;
- available-leader filtering;
- the final `setCiv` / `setLeader`;
- NO update of `abLeaderTaken` after assigning the random leader.

The omission therefore exists from the feature's introduction.

Practical 2539 later amended random-civ setup bookkeeping but did not add the missing
leader update. Current Base AdvC 1.14 retains the same function.

Current SAS has already repaired a separate practical-2532 copy/paste bug in
`reassignPlayer` (KI#209). F655 is independent and remains in the still-live
re-randomization body.

BASE ADVC VS CURRENT SAS ACTIVATION
-----------------------------------
Base AdvC 1.14 ships:

    PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT = 20

and documents 20 as the recommended setting. Its standard smaller player ceiling makes
the >civilization-count duplicate-civ activation much less ordinary because Base has
more playable civs than normal simultaneous players.

AdvC-SAS expands the DLL to 48 civilization players but deliberately sets the define to
0 because the mod prefers equal civilization probability over multi-leader civ bias.
That default masks F655.

If the documented enhanced selection feature is re-enabled in SAS -- e.g. value 20 as
upstream recommends -- a normal 48-random-player setup activates the bug immediately.
This is therefore configuration-gated current functionality, not an unreachable
future-data-only issue.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for:
- `reRandomizeCivsAndLeaders`;
- `abLeaderTaken`;
- duplicate/unique randomized leader;
- `PER_EXTRA_LEADER_CIV_SELECTION_WEIGHT`

find no confirmed owner for this missing post-assignment update.

Related roots are independent:
- KI#209: practical-2532 `reassignPlayer` copied the civ-random flag into the saved
  leader-random flag.
- F616/F621: True Starts uses the same XML define in a different civilization-fitness
  pipeline and has separate duplicate/pair-weighting defects.

F655 reproduces with True Starts off.

REPAIR BOUNDARY
---------------
The narrow repair is exactly what the algorithm's data structure requires:

    LeaderHeadTypes eNewLeader = ...
    setCiv(ePlayer, eNewCiv);
    setLeader(ePlayer, eNewLeader);
    abLeaderTaken.set(eNewLeader, true);

Updating the map before the next player makes:
- later civilization weights reflect remaining unique leaders;
- civilizations with no unused leader become ineligible;
- later `aeAvailableLeaders` lists exclude prior random assignments;
- the initial `iRandomLeadersNeeded <= iLeadersAvailable` contract meaningful.

Do not mark the leader only inside CvInitCore's persistent setup flags; `abLeaderTaken`
is deliberately a local temporary selection-state map and must be updated locally.

VALIDATION TARGETS
------------------
1. Current default SAS define 0:
   preserve exact BtS/current behavior; function continues to return early.
2. Define 20, 48 all-random SAS players, sync seed 0:
   all 48 assignments complete with 48 distinct leaders.
3. Repeat seeds 1, 42, 12345 and 0xDEADBEEF:
   no duplicate leader identities.
4. Fixed/non-random leaders remain excluded exactly as today.
5. Random civilization with fixed/non-random leader-only cases continue to use the BtS
   path as intended.
6. Unrestricted Leaders remains an immediate return.
7. If requested random leaders exceed all available unique playable leaders, preserve
   the existing fallback assertion/return.
8. Civ-use balancing (`aiTakersPerCiv`) remains unchanged.
9. A one-leader civilization selected a second time can only be chosen when its sole
   leader is still genuinely unused.
10. Network host-only re-randomization remains deterministic from sync seed and does not
    introduce new net messages.

POST-F655 LEDGER / EXACT RESUME
-------------------------------
Queue 026 `CvGameCoreDLL/CvInitCore.cpp` -- OPEN.
F655 / provisional KI#977 -- CONFIRMED.
F656 / provisional KI#978 -- UNUSED.

Exact continuation:
C031-WIP511 physical -> Queue026 OPEN -> exclude F655 plus KI#209 and all prior setup/
slot/random-civ owners -> continue remaining reset/setup/network/serialization audit:
Advanced Start and start-point scaling, transferred-map name/path semantics, slot/netID/
claim/ready state, force-control and unsaved read-preservation, game-type visibility,
Pangaea/WB state, source-copy/reset symmetry, random-civ bookkeeping around
`m_bCivLeaderSetupKnown`, full current read/write pairing and all remaining CvInitCore
history strata -> whole-album dedup -> either promote a distinct current-live F656 or
close Queue026 cleanly -> freeze the next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP512 -- QUEUE 026 CvInitCore.cpp -- F656 CONFIRMED
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP511.
- Ordinary Queue 026 `CvGameCoreDLL/CvInitCore.cpp` remains OPEN.
- F655 / provisional KI#977 remains CONFIRMED.
- F656 / provisional KI#978 is CONFIRMED here.
- F657 / provisional KI#979 is UNUSED.
- No gameplay source was modified.

-------------------------------------------------------------------------------
F656 / provisional KI#978 -- transferred Python map filenames are execution aliases,
but AdvC's cached Pangaea classifier treats the `_t` alias as the logical map-script
identity; transferred Pangaea therefore caches `m_bPangaea = false`
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC map-setup classification defect on the native transferred-
Python-map path. Current Base AdvC 1.14 retains the same root.

ROOT -- TWO DIFFERENT MAP-NAME CONTRACTS ARE MIXED
--------------------------------------------------
`CvInitCore` deliberately has two representations of a Python map-script name.

Persistent/logical identity is the raw member:

    m_szMapScriptName

The current writer serializes that raw member directly:

    pStream->WriteString(m_szMapScriptName);

and `getWBMapScript()` likewise tests the raw member directly.

By contrast, public `getMapScriptName()` is also an execution-filename adapter. For a
non-WB Python map transferred by the EXE it returns:

    m_szMapScriptName + MAP_TRANSFER_EXT

where:

    MAP_TRANSFER_EXT = "_t"

The inherited comment is explicit:

    "If it's a transferred Python file, we have to hack in the transferred extension"

That decorated value is correct when code needs to call the physically transferred
Python module.

`setMapScriptName` performs both kinds of work in sequence:

    m_szMapScriptName = szMapScriptName;
    refreshCustomMapOptions();
    updatePangaea();

`refreshCustomMapOptions()` intentionally consumes `getMapScriptName()` and sends that
name to `pythonMapExists` and the Python custom-map-option calls. On a transferred map,
this is exactly where the `_t` execution alias belongs.

`updatePangaea()`, however, also consumes the same decorated getter:

    m_bPangaea = (getMapScriptName().compare(L"Pangaea") == 0);

When the raw logical script is `Pangaea` and `gDLL->getTransferredMap()` is true,
`getMapScriptName()` returns `Pangaea_t`. The comparison is therefore deterministically:

    L"Pangaea_t" == L"Pangaea"  ->  false

and the cached logical classifier is wrong.

TRANSFER-PATH ACTIVATION IS ESTABLISHED INTERNALLY
--------------------------------------------------
This is not based only on guessing when the opaque EXE toggles `getTransferredMap()`.

Whole-current-source search finds `refreshCustomMapOptions()` private and called only by
`setMapScriptName()`. Its inherited implementation specifically routes through the
transfer-aware getter when checking and invoking the Python map module. Thus the native
purpose of the `_t` branch is to make this setter/refresh lifecycle work for transferred
Python maps.

Current SAS `PrivateMaps/Pangaea.py` is a concrete transferred-map candidate and exposes:

    getNumCustomMapOptions()       -> 3
    getNumHiddenCustomMapOptions() -> 2

so the transfer-aware custom-option refresh is meaningful for the exact script whose
logical classifier is broken by the following line.

CURRENT CONSUMERS -- THE FALSE CACHE IS GAMEPLAY/SETUP STATE
------------------------------------------------------------
`m_bPangaea` is not decorative metadata. Current SAS has three independent consumers.

1. `StartingPositionIteration.cpp`

    bool const bIgnoreScript =
        (GC.getInitCore().isPangaea() && !GC.getGame().isTeamGame());

The comment explains the contract: Pangaea overrides `assignStartingPlots`, but its
special behavior is relevant only for team games. In an ordinary non-team Pangaea game,
AdvC's fair-start iteration is therefore allowed to ignore the script's claimed start.

If the transferred Pangaea cache is false, a start reported as script-selected is no
longer ignored; the iterator can bail/revert instead of following the intended Pangaea
special case.

2. `CitySiteEvaluator.cpp`

    if (GC.getInitCore().isPangaea())
        rMult /= 2;

Pangaea deliberately halves the production multiplier applied to the coastal/naval
component of city-site value. A false cache omits that halving and changes AI city-site
scoring.

3. `CvPlayerAI.cpp::AI_neededExplorers_bulk`

    if (GC.getInitCore().isPangaea())
        iNeeded = (iNeeded + 1) / 2;

A false cache omits Pangaea's reduction in needed water explorers and can change AI
naval-exploration demand.

The setup and gameplay consumers therefore make the wrong cache materially observable;
this is not merely a displayed map name ending in `_t`.

HISTORY / LINEAGE
-----------------
The transfer-aware `getMapScriptName()` behavior is inherited from the Firaxis/K-Mod
line. Supplied K-Mod and Civ4CE both retain the same `_t` execution-name adapter and use
it from custom-map-option refresh.

AdvC practical 1276 already introduced a logical Pangaea check directly against
`getMapScriptName()`, so the logical-name / transferred-filename mismatch predates the
cache.

Practical 2280 added the Pangaea special case to Starting Position Iteration.

Practical 2284 then centralized Pangaea identity into `CvInitCore::m_bPangaea` and
introduced the current `updatePangaea()` implementation. The same practical:
- resets the cache to false;
- updates it after `setMapScriptName`;
- serializes it in `CvInitCore`;
- changes consumers from direct map-name comparison to `isPangaea()`.

Thus practical 2284 preserves and centralizes the older bad comparison instead of
separating logical identity from the transferred execution alias. Current Base AdvC
1.14 still has the same `updatePangaea()` body.

SOURCE-COPY SUPPORTING HAZARD -- DO NOT OVERCLAIM ACTIVATION YET
----------------------------------------------------------------
`resetGame(CvInitCore* pSource, ...)` also copies the persisted map name through the
execution getter:

    setMapScriptName(pSource->getMapScriptName());

If this copy occurs while `getTransferredMap()` is true, a source raw name `Pangaea`
can be stored in the destination as `Pangaea_t`; the destination getter can then append
another `_t` and expose `Pangaea_t_t` during its immediate refresh.

The local source proves that transformation algebraically, but the exact opaque-EXE
ordering of `resetGame(pSource)` versus the transfer flag has not been established.
Keep this as supporting evidence for the same raw-name/execution-alias design hazard,
not as a second independently claimed live consequence of F656.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `Pangaea`, `transferred map`, `MAP_TRANSFER_EXT`, map-script
name/path, `isPangaea` and `updatePangaea` find no confirmed owner for this transferred-
Pangaea classification root.

Related findings are independent:
- F634 concerns stale cached lake state during native normalization.
- F636 concerns Ice-insensitive area topology in city-bearing WB scenarios.
- KI#46 is SAS's deliberate current-format-only save-reader policy.
- KI#209 and F655 concern random civ/leader setup bookkeeping.

REPAIR BOUNDARY
---------------
The narrow semantic repair is to classify Pangaea from the raw logical identity rather
than from the transfer-aware execution filename, e.g. inside `updatePangaea()`:

    m_bPangaea = (m_szMapScriptName.compare(L"Pangaea") == 0);

That preserves the `_t` getter exactly where it is needed for Python execution while
keeping logical map identity independent of file transfer.

A broader cleanup may also make `resetGame(pSource,...)` copy the raw member rather than
`pSource->getMapScriptName()`, because the current writer already establishes raw
`m_szMapScriptName` as the persisted representation. Do not make that broader change
without validating the EXE copy lifecycle first.

VALIDATION TARGETS
------------------
1. Local/non-transferred Pangaea: `isPangaea()` remains true.
2. Transferred Pangaea: Python lookup still targets `Pangaea_t`, while `isPangaea()` is
   true.
3. Transferred non-Pangaea Python map: classifier remains false and `_t` lookup remains
   intact.
4. WB maps: raw WB filename detection remains unchanged; no transfer suffix is added.
5. Pangaea's 3 custom options / 2 hidden options remain available on the transfer path.
6. Non-team Pangaea continues through the intended SPI ignore-script special case.
7. Pangaea coastal city-site production weighting still receives its /2 multiplier.
8. Pangaea water-explorer need still receives `(iNeeded + 1) / 2`.
9. Save/write/load preserves the logical Pangaea flag and raw map-script identity.
10. If the source-copy cleanup is attempted separately, explicitly test for one and only
    one `_t` suffix through loaded-core / live-core transfer copies.

POST-F656 LEDGER / EXACT RESUME
-------------------------------
Queue 026 `CvGameCoreDLL/CvInitCore.cpp` -- OPEN.
F655 / provisional KI#977 -- CONFIRMED.
F656 / provisional KI#978 -- CONFIRMED.
F657 / provisional KI#979 -- UNUSED.

Exact continuation:
C031-WIP512 physical -> Queue026 OPEN -> exclude F655/F656 plus KI#209 and all prior
setup/map owners -> continue Advanced Start/start-point scaling, slot/netID/claim/ready,
force-control and unsaved read-preservation, game-type visibility/load-state transitions,
WB state/source-copy symmetry beyond the F656 raw-name hazard, `m_bCivLeaderSetupKnown`
bookkeeping, full current read/write pairing and remaining CvInitCore history strata ->
whole-album dedup -> either promote distinct F657 or close Queue026 cleanly -> freeze the
next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP513 -- QUEUE 026 CvInitCore.cpp -- CLOSED / F657 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP512.
- Queue 026 `CvGameCoreDLL/CvInitCore.cpp` is CLOSED.
- Queue-026 confirmed roots: F655/KI#977 and F656/KI#978.
- F657 / provisional KI#979 remains intentionally UNUSED.
- Files complete: 59 / 169.
- No gameplay source was modified.
- The stronger-WIP priority reloop is already complete; ordinary numeric queue order
  therefore resumes with Queue 027 `CvGameCoreDLL/CvInfo_RandomEvent.cpp`
  (2,099 queued lines).

FINAL POST-F656 SATURATION PASS
-------------------------------
The remaining CvInitCore surface was challenged mechanically and historically after
F656 rather than closed merely because the obvious setup paths had been exhausted.
The following candidate families were resolved without consuming F657.

1. Advanced Start point initialization
   - `resetAdvancedStartPoints()` starts from the selected era's configured points and
     applies `WorldInfo::getAdvancedStartPointsMod()` before storing the result;
   - every current SAS world size, including the added SAS24/SAS32/SAS40/SAS48 sizes,
     configures that modifier as 100, matching the source comment that the world-size
     effect has been removed through XML;
   - the old game-speed adjustment is deliberately disabled and start-cost scaling is
     handled elsewhere under advc.250c;
   - `getAdvancedStartMinPoints()` correctly skips unit classes with `NO_UNIT` before
     testing the default settler/founder unit, carrying practical 4333's mod-support
     fix. No current mismatch survives here.

2. Game-type visibility and save/load transitions
   - `read()` deliberately reads the serialized `..._LOAD` game type directly rather
     than routing it through `setType()`; practical 1944 introduced that representation
     so within-game loading can reset players safely;
   - the apparently skipped `setType()` side effects do not yield a distinct current
     defect on the demonstrated copy paths: `resetGame(pSource,bSaveGameType)` compares
     the broader `getGameMultiplayer()` family (network MP + Pitboss + PBEM + Hotseat),
     and when the family changes it calls `setType()` before copying game options;
   - same-family loads already have the corresponding option-visibility family, while
     `read()` explicitly refreshes every initialized player's human/team state at the
     end;
   - scenario identity after game start is intentionally no longer represented by
     `CvInitCore::getScenario()`; the source comment directs runtime consumers to
     `CvGame::isScenario()` instead. No independent F657 was established.

3. Force controls and the `bBeforeRead` preservation boundary
   - normal `resetGame()` does reset `m_abForceControls`; only `resetGame(true)` returns
     before the unsaved-session fields are cleared;
   - ForceControls are intentionally marked unsaved in CvInitCore and are setup-screen
     controls (speed, handicap, options, victories, max turns, city elimination and
     Advanced Start), not persistent game rules;
   - the Python WB descriptor can write/read textual `ForceControl=` tokens, but its
     current `CvGameDesc.apply()` does not itself apply that tuple; native setup/EXE
     handling therefore cannot be reconstructed farther from the supplied source;
   - losing or retaining ForceControl UI constraints after a save has already become a
     running game is not a demonstrated gameplay-state contradiction. Keep this as an
     opaque setup-lifecycle note, not F657.

4. Unsaved state preserved across `read()`
   - sync/map seed members, active player/team, mode, network IDs, ready flags and
     Python/XML checks are intentionally outside CvInitCore's save stream;
   - actual game RNG state is serialized by `CvGame` itself, so a preserved InitCore
     seed during a load is not evidence that the loaded game resumes from the wrong RNG
     state;
   - `resetPlayer(true)` preserves network/session data while clearing saved player
     initialization fields and resetting previously-live `CvPlayer` objects as required
     by advc.001p/advc.706. No distinct stale gameplay member was found.

5. Network ID compression
   - Firaxis/K-Mod/Civ4CE store `m_aiNetID` as `int`; AdvC's enum-map rewrite eventually
     changed it to `ArrayEnumMap<PlayerTypes,PlayerTypes>` and `setNetID` explicitly casts
     the EXE's `int getAssignedNetworkID(...)` result to `PlayerTypes`;
   - this means values outside the single-byte PlayerTypes domain would be truncated,
     and the current 48-civ DLL constrains player IDs themselves to that domain;
   - local history shows AdvC deliberately relies on `-1`/`0` in SP, Hotseat and R&F,
     while network games ask the EXE for the assigned ID. The supplied sources do not
     establish an EXE network-ID value outside the PlayerTypes range, nor a current
     failing connection fixture;
   - because the album requires a demonstrated live contract rather than an interface-
     width suspicion, this remains a targeted robustness/network test candidate and
     does not consume F657.

6. Slot / claim / ready lifecycle
   - `getAvailableSlot()` first claims unassigned OPEN slots and only then unassigned
     COMPUTER slots when Take Over AI is enabled;
   - `closeInactiveSlots()` and `reopenInactiveSlots()` preserve the documented
     Pitboss/Hotseat/PBEM distinctions, and the host-only synchronization caveat is
     explicit;
   - save read converts serialized `SLOTCLAIM_ASSIGNED` to `SLOTCLAIM_RESERVED`, avoiding
     treating the prior process's live connection claim as a current assignment;
   - slot-status changes refresh `CvPlayer::updateHuman()` when player statics exist.
     No independent ownership/claim inversion survived.

7. Player reassignment and active-team state
   - current SAS already fixes KI#209, the practical-2532 copy/paste error that saved the
     target's random-leader state from its random-civilization flag;
   - all saved and unsaved slot data are swapped in `reassignPlayer`, including the
     compressed net ID, ready flag and compatibility checks;
   - practical 4151's network-coop fix is present: `setTeam()` refreshes both the player
     team type and cached active team, while `setActivePlayer()` routes through
     `updateActiveTeam()`;
   - no additional reassignment root distinct from KI#209 emerged.

8. Random civ/leader setup bookkeeping beyond F655
   - `externalRNGCall` marks setup-known state, detects the EXE's 10000-sided civ/leader
     selection calls only before any player has ever been alive, and records random-civ
     / random-leader provenance;
   - those provenance maps and `m_bCivLeaderSetupKnown` are copied by InitCore-to-InitCore
     resets and paired in the current save stream;
   - `wasLeaderRandomlyChosen()` intentionally treats a random civilization as implying
     a random leader when Unrestricted Leaders is off, which is necessary for the joint
     civ/leader re-randomizer rather than contradictory bookkeeping;
   - the only concrete surviving algorithmic defect found in that routine is F655's
     missing post-assignment `abLeaderTaken` update. No second randomization root was
     forced into F657.

9. WB/map source-copy symmetry after F656
   - raw `m_szMapScriptName` is the serialized logical name; `getMapScriptName()` may add
     `_t` only as the transferred-Python execution alias;
   - F656 owns the demonstrated logical-Pangaea misclassification caused by using that
     decorated getter in `updatePangaea()`;
   - `resetGame(pSource)` also copies through the decorated getter, so a double-suffix
     algebra remains possible if that copy occurs while the EXE transfer flag is set,
     but the supplied source does not establish that opaque ordering. It remains
     supporting breadth of F656, not an independently promoted F657.

10. Dynamic arrays, enum/index access and string setters
    - custom map options and victories are freed/reset before replacement; live internal
      callers pass distinct source storage or local temporary arrays;
    - getters guard externally indexable custom-option/victory/string arrays or use
      EnumMap range machinery; remaining malformed-save counts are untrusted-file
      hardening rather than normal gameplay bugs;
    - string-to-enum setters leave state unchanged on unknown XML type strings, an
      inherited setup/API behavior with no current shipped invalid-string fixture;
    - the long-name scenario crash guard is diagnostic-only and intentionally does not
      alter release behavior.

11. Current read/write pairing
    - the current-format writer and reader pair the same saved fields in the same order:
      load game type, descriptive strings/raw map name/Pangaea/WB flag, standard map
      parameters, custom options, victories, game and MP options, stat reporting, turn
      controls, elimination/Advanced Start, player strings/flags/civ/leader/team/
      handicap/color/art style, random-choice provenance, setup-known flag, slot status/
      claims, playable and minor flags;
    - intentionally unsaved session members remain on both sides of the stream boundary;
    - SAS's current-format-only reader policy and removed historical `uiFlag` branches
      are already a deliberate project compatibility decision, not a new Queue-026
      finding.

12. History / comparison closure
    - current SAS and supplied Base AdvC 1.14 remain executable-semantically aligned for
      common CvInitCore behavior except deliberate SAS reset/read/current-format changes
      already reconciled in this audit;
    - the important surviving history strata were checked: practical 1325/1944
      within-game-load fixes, 1832 48-civ WB compatibility, 2284 Pangaea cache, 2300+
      enum-map conversion, 2531/2532/2539 reset/random-selection changes, 3183/3392 enum
      rewrites, 3556 long-name warning, 3579 True Starts integration, 4151 network-coop
      fix, 4288 QuickStart fix and 4333 unique-unit Advanced Start support;
    - K-Mod and Civ4CE are identical to each other in CvInitCore and preserve the older
      int-netID / transferred-map ancestry. No later external oracle exposed another
      current-live root beyond F655/F656.

QUEUE-026 ROOT LEDGER
---------------------
F655 / KI#977
    `reRandomizeCivsAndLeaders()` filters against `abLeaderTaken` but never marks a
    newly assigned random leader as taken, allowing duplicate leaders when AdvC's
    enhanced civ-selection weighting is enabled and enough unique leaders exist.

F656 / KI#978
    transferred Python map filenames use `_t` as an execution alias, but AdvC's
    `updatePangaea()` classifies through that decorated getter; transferred Pangaea
    therefore caches false and changes SPI/AI Pangaea-specific behavior.

F657 / KI#979
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 026 is closed for this audit pass. Remaining concerns are opaque EXE lifecycle
questions, API/input hardening, configuration/state that is intentionally unsaved, or
supporting breadth of F656. None clears the album's independent current-live threshold
for F657.

EXACT CONTINUATION
------------------
C031-WIP513 physical -> Queue026 CLOSED -> F655-F656 confirmed -> F657/KI#979 UNUSED ->
open ordinary Queue027 `CvGameCoreDLL/CvInfo_RandomEvent.cpp` (2,099 lines) -> establish
current SAS vs Base AdvC 1.14 / K-Mod / Civ4CE provenance and caller/data map -> audit
random-event XML loading, event/trigger field defaults, array ownership, enum/index
contracts, probability/value semantics and all file-history semantic strata -> whole-
album dedup before numbering -> freeze the next physical WIP at the next settled
boundary whether F657 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP514 -- QUEUE 027 CvInfo_RandomEvent.cpp -- CLOSED / F657 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP513.
- Queue 027 `CvGameCoreDLL/CvInfo_RandomEvent.cpp` is CLOSED.
- Queue-027 confirmed roots: none.
- F657 / provisional KI#979 remains intentionally UNUSED.
- Files complete: 60 / 169.
- No gameplay source was modified.
- Ordinary numeric queue order advances to Queue 028
  `CvGameCoreDLL/CvXMLLoadUtilitySet.cpp` (2,095 queued lines).

BASELINE / PROVENANCE
---------------------
1. Current SAS vs supplied Base AdvC 1.14
   - Current `CvInfo_RandomEvent.cpp` is executable-semantically identical to supplied
     Base AdvC 1.14 after comments/formatting are removed. The current SAS additions in
     the associated header are Sevopedia/Python text-return wrappers, not event-state
     semantics.
   - Therefore there is no missed later Base-AdvC-1.14 random-event-info repair hiding
     ahead of SAS in this file.

2. K-Mod / Civ4CE ancestry
   - K-Mod 1.46 and supplied Civ4CE/BtS are executable-identical to each other for the
     inherited CvEventInfo/CvEventTriggerInfo implementations inspected here.
   - The common current-vs-K-Mod behavioral deltas are confined to AdvC XML-default
     loading helpers, defensive null-aware accessors, the advc.003t enum-map replacement
     of the four BuildingExtra* tuple vectors, the moved/refactored
     `isPlotEventTrigger()` helper, disabled XML-cache implementation details and later
     assertion/type hygiene.
   - No current semantic difference outside those known strata survived the pass.

CURRENT-DATA FIXTURE
--------------------
The supplied current SAS XML was parsed directly rather than reasoning from schema
intent alone:

- 339 EventInfo entries in `Assets/XML/Events/CIV4EventInfos.xml`;
- 197 EventTriggerInfo entries in `Assets/XML/Events/CIV4EventTriggerInfos.xml`;
- all 339 EventInfos contain the standard array/list container tags used by this loader,
  including UnitCombatPromotions, UnitClassPromotions, TechFlavors, PlotExtraYields,
  FreeSpecialistCounts, AdditionalEvents, EventTimes and ClearEvents;
- 59 triggers have `iNumPlotsRequired > 0`.

The current XML and live consumers were used below as the acceptance oracle for parser
and default-value concerns.

FINAL QUEUE-027 SATURATION PASS
-------------------------------

1. Promotion-array allocation / absent-list concern -- CLOSED
   Initial inspection of `CvEventInfo::read()` looked hazardous because it allocates
   `m_piUnitCombatPromotions` / `m_piUnitClassPromotions`, then only populates them under
   `if (pszPromotions != NULL)`.

   The exact overload resolves the concern. `CvXMLLoadUtility::SetVariableListTagPair`
   for `CvString**` calls `InitStringList` first and fills the entire list with the
   supplied default. These two calls explicitly pass `"NONE"`; only the empty-string
   default is eligible for the advc.003t all-default deallocation optimization.
   Therefore `pszPromotions` remains allocated and every slot is converted through
   `FindInInfoClass("NONE")` to `NO_PROMOTION` when no non-default entry exists.

   The primitive array getters likewise return 0/NO_PROMOTION when their compact
   all-default arrays are NULL. No uninitialized live read survives.

2. Plot-trigger classification -- CLOSED against all current producers
   The moved `CvEventTriggerInfo::isPlotEventTrigger()` initially looked as though it
   could reject Python-selected plot triggers with `iNumPlotsRequired=1`. That suspicion
   came from probing generic field names rather than the inherited XML contract.

   The actual fields/defaults reconcile all current data:
   - `m_iPlotType` is loaded from XML `iPlotType`, not `PlotType`;
   - constructor default 0 is `PLOT_PEAK`, while `NO_PLOT` is -1;
   - Hiyo Silver explicitly uses plot type 1 (`PLOT_HILLS`);
   - Dustbowl continuation explicitly uses -1 but also sets XML
     `bPrereqEventPlot=1`, which is the inherited location-prerequisite flag stored in
     member `m_bPrereqEventCity` and accepted by the predicate when `!isPickCity()`;
   - Holy Mountain and Volcano inherit the peak default, matching their intended
     location shape.

   A mechanical evaluation of the current 59 triggers with `iNumPlotsRequired > 0`
   finds zero for which `isPlotEventTrigger()` returns false. The helper itself is the
   same inherited predicate that practical 1948 moved from CvGameCoreUtils onto the
   info class. No location-selection regression was established.

3. `bPrereqEventCity` vs XML `bPrereqEventPlot` naming -- EXISTING CONTROL
   The member-name/tag-name mismatch is inherited Firaxis semantics, not a new parser
   typo. Existing album work already challenged it while auditing city/plot random-event
   history: pick-city consumers use the location-sensitive prerequisite as city-local
   event history, while plot consumers compare stored prerequisite coordinates.
   Do not duplicate that control as F657.

4. XML cache read/write surface -- COMPILED OUT
   The large `#if ENABLE_XML_FILE_CACHE` read/write blocks are not current runtime
   behavior: `CvInfo_Base.h` defines `ENABLE_XML_FILE_CACHE 0` under advc.003i.
   Their legacy serialization symmetry was still spot-checked, but cache-only defects
   cannot clear the album's current-live threshold from this configuration.

5. advc.xmldefault ID loading -- SAFE in current file
   Practical 2222 replaced repeated temporary-string + `FindInInfoClass` sequences with
   `SetInfoIDFromChildXmlVal` for EventInfo and EventTriggerInfo identity fields. The
   destination members are initialized to their correct NO_* defaults where absence
   needs a sentinel, and current XML uses the expected tags. No omitted-tag path was
   found that converts a meaningful NO_* default into a valid enum accidentally.

6. advc.003t BuildingExtra* enum-map conversion -- SAFE for current data
   Practical 3183 replaced the inherited tuple/vector representation with compact
   enum maps:
   - BuildingExtraYield: BuildingClass x Yield -> short change;
   - BuildingExtraCommerce: BuildingClass x Commerce -> short change;
   - BuildingExtraHappy / BuildingExtraHealth: BuildingClass -> char change.

   Two superficially suspicious details were resolved:
   - the loader calls `SetVariableListTagRate(..., "BuildingExtraYield")` and
     `...Commerce` in singular form, but `XMLTagPairRateIterator` deliberately appends
     `s` for the outer XML container and then seeks singular child entries. This exactly
     matches unchanged Firaxis `<BuildingExtraYields><BuildingExtraYield>...` and
     `<BuildingExtraCommerces><BuildingExtraCommerce>...` structure;
   - current values fit comfortably within the compact representations. Current ranges
     are +1..+5 for extra yields, +1..+25 for extra commerce, +1..+3 happiness and +1
     health.

   A per-EventInfo duplicate-key census found no duplicate BuildingClass/Yield,
   BuildingClass/Commerce or BuildingClass Happy/Health keys whose old first-match vector
   semantics could differ from compact map insertion. No current representation-loss
   root survives.

7. Compact all-default array ownership -- SAFE
   AdvC's generic primitive `SetVariableListTagPair` may delete all-zero arrays as an
   optimization. Every corresponding current getter in CvEventInfo is null-aware and
   maps NULL back to the semantic default (0 or NO_PROMOTION). Destruction uses
   SAFE_DELETE_ARRAY throughout. The 339-entry current data census did not expose an
   accessor that assumes a compacted all-default array must remain allocated.

8. Trigger text / era vector pairing -- SAFE on loader and current XML
   `getTextEra(i)` asserts against `m_aszText.size()` rather than independently against
   `m_aiTextEra.size()`, but the XML loader pushes one era for every accepted TriggerText
   and the disabled cache reader likewise reads exactly the text count into the era
   vector. `getNumTexts()` explicitly asserts equality. Current XML did not produce an
   unpaired text/era sequence. This remains internal-invariant style, not F657.

9. Constructor defaults and plot type 0 -- INHERITED DATA CONTRACT
   `CvEventTriggerInfo` initializes `m_iPlotType(0)`, which means PLOT_PEAK rather than
   NO_PLOT. This is inherited unchanged from Firaxis/K-Mod/Civ4CE and is used by shipped
   triggers that omit iPlotType (e.g. peak-shaped Holy Mountain/Volcano cases). Changing
   it to NO_PLOT as generic enum style would silently alter existing event behavior.
   No bug is assigned merely from the unusual default.

10. Existing random-event roots are in consumers, not this parser
    Whole-album dedup is especially important here. Prior confirmed findings already own
    multiple live random-event defects downstream of these correctly loaded fields,
    including:
    - F353: AI_eventValue values BonusRevealed through unrelated BonusType;
    - F354: non-city BuildingExtraYield/Commerce changes are collected but never valued;
    - F355: BuildingExtraHappy/Health valuation loses BuildingClass identity;
    - later player/city findings cover event trigger revalidation, team/global scope and
      ClearEvents city-history behavior.

    Those findings are evidence that the EventInfo fields are live; they do not imply a
    second loading root here. No parser/default/representation defect was found that
    explains or duplicates them.

11. History closure
    The small semantic history touching this split file was exhausted:
    - practical 1644 split CvInfos;
    - practical 1948 moved/refactored the plot-trigger predicate without changing its
      inherited logic;
    - practical 2222/2236 introduced XML-default-safe ID readers;
    - practical 3183/3392 converted tuple/vector data to enum maps;
    - later edits are merge/type/organization/formatting strata.
    No later Base AdvC 1.14 repair is missing from SAS, and no K-Mod/Civ4CE oracle
    contradicts current behavior on the audited live fields.

QUEUE-027 ROOT LEDGER
---------------------
F657 / KI#979
    UNUSED.

CLOSURE JUDGMENT
----------------
Queue 027 is closed for this audit pass. The live parser/default/ownership and compact
representation contracts reconcile with current XML and current consumers. Remaining
oddities are inherited naming/default conventions, disabled XML-cache code, defensive
assertion opportunities, or defects already owned by downstream player/city/AI findings.
None clears the threshold for a distinct F657.

EXACT CONTINUATION
------------------
C031-WIP514 physical -> Queue027 CLOSED -> F657/KI#979 UNUSED -> open ordinary Queue028
`CvGameCoreDLL/CvXMLLoadUtilitySet.cpp` (2,095 lines) -> establish current SAS vs Base
AdvC 1.14 / K-Mod / Civ4CE provenance -> prioritize shared XML loader ownership,
parser-level restoration, sibling iteration, list allocation/default compaction,
enum/index validation, modular/default inheritance, progress/cache gates and current
XML producers -> dedup against all prior parser/info findings before numbering ->
freeze the next physical WIP at the next settled boundary whether F657 is consumed or
remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP515 -- QUEUE 028 CvXMLLoadUtilitySet.cpp -- F657 CONFIRMED / OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP514.
- Queue 028 `CvGameCoreDLL/CvXMLLoadUtilitySet.cpp` remains OPEN.
- New confirmed root: F657 / provisional KI#979.
- Next unused root: F658 / provisional KI#980.
- Files complete remain: 60 / 169.
- No gameplay source was modified.

F657 / provisional KI#979
-------------------------
TITLE
    AdvC's enum-map XML helper allocates temporary commerce arrays with `new[]`
    but destroys them with scalar `delete`; current BuildingInfo loading executes
    this mismatched deallocation 51 times on shipped CommerceChangeOriginalOwners.

STATUS
    CONFIRMED current-live inherited AdvC C++ lifetime / allocator-pairing defect.
    The persistent building data are copied into the enum map before the temporary is
    freed, so the deterministic semantic payload is normally already captured; the
    defect is the invalid deallocation itself. In standard C++ the new[]/delete mismatch
    is undefined behavior. In AdvC DEBUG builds, the project's own global allocation
    shims additionally route the two forms through different Civ4 DLL allocation APIs,
    making the pairing error concrete rather than stylistic.

ROOT CONTRACT -- ARRAY ALLOCATION, SCALAR DELETION
---------------------------------------------------
Current `CvXMLLoadUtility.h` implements the temporary-array wrapper:

    template<class InfoMap>
    void SetVariableListPerCommerce(InfoMap& kMap, TCHAR const* szRootTagName)
    {
        ...
        bool* pbTmp = NULL;
        int iSet = SetCommerceArray(&pbTmp);
        if (pbTmp != NULL)
        {
            ...
            kMap.insert(eLoopCommerce, pbTmp[eLoopCommerce]);
            ...
            delete pbTmp;
        }
    }

But `SetCommerceArray` -> `SetCommerce` allocates through `InitList`:

    InitList(ppbCommerce, NUM_COMMERCE_TYPES);

and `InitList` is:

    *ppList = new T[iListLen];

For this instantiation T is bool. Therefore the allocation expression is `new bool[...]`
and the only valid matching delete-expression is `delete[] pbTmp`. `delete pbTmp` is a
new[]/delete mismatch and has undefined behavior under the C++ object-allocation rules.

The sibling `SetVariableListPerYield` helper contains the same typo:

    int* piTmp = NULL;
    ...
    delete piTmp;

but current source has no caller of `SetVariableListPerYield`. Do not split the dormant
int variant into a second finding; it is the same helper-family typo and should be fixed
at the same time with `delete[]`.

CURRENT SHIPPED ACTIVATION -- 51 ORDINARY BUILDING READS
--------------------------------------------------------
Current `CvBuildingInfo::read()` invokes the commerce helper twice for every building:

    pXML->SetVariableListPerCommerce(
            CommerceFlexible(), "CommerceFlexibles");
    pXML->SetVariableListPerCommerce(
            CommerceChangeOriginalOwner(), "CommerceChangeOriginalOwners");

The first container is empty in current CIV4BuildingInfos.xml and therefore normally does
not allocate a temporary. The second is live and common.

Namespace-aware census of current `Assets/XML/Buildings/CIV4BuildingInfos.xml`:

    BuildingInfo entries                         = 166
    CommerceFlexibles containers present         = 166
    non-empty CommerceFlexibles                   =   0
    CommerceChangeOriginalOwners present          = 166
    non-empty CommerceChangeOriginalOwners        =  51
    non-empty entries containing a true value     =  51

Representative current data:

    BUILDING_ACADEMY
        CommerceChangeOriginalOwners = [0, 0, 1]

    BUILDING_GREAT_LIBRARY
        CommerceChangeOriginalOwners = [0, 0, 1]

and the same pattern is used by shrines, wonders and other buildings. This is the same
51-entry current producer population already independently established in the album for
F477's historical-PlayerTypes alias root; F477 concerns the stored owner value after
capture/recycled-player reuse, not XML allocation lifetime.

`LoadPreMenuGlobals()` normally loads all BuildingInfos on application startup:

    LoadGlobalClassInfo(GC.m_paBuildingInfo, "CIV4BuildingInfos", ...);

so these 51 mismatched delete-expressions are reached during ordinary current SAS XML
initialization, before a game needs to be played. This is not modular-XML or malformed-
XML robustness only.

DEBUG-BUILD ALLOCATOR ORACLE -- THE FORMS REALLY ROUTE DIFFERENTLY
------------------------------------------------------------------
Queue 096 already established the current AdvC/SAS debug allocator contract, and direct
source inspection reconfirms it in `CvMemoryManager.cpp`:

    operator new[](size_t size)
        -> gDLL->newMemArray(...)

    operator delete(void* p)
        -> gDLL->delMem(...)

    operator delete[](void* p)
        -> gDLL->delMemArray(...)

Therefore a DEBUG build taking the current helper path allocates through the array API
`newMemArray` and then sends that pointer to the scalar deallocator `delMem`. The correct
pair is `newMemArray` / `delMemArray`.

Release builds compile these custom global allocator shims out, so the exact practical
manifestation depends on the Visual C++ CRT implementation; for trivial bool arrays it
may appear benign on a particular allocator. That does not make the source valid: the
C++ delete-expression is mismatched and the program has undefined behavior at each live
deallocation. No stronger claim about a deterministic Release-build crash is required.

ANCESTRY / INTRODUCTION BOUNDARY
--------------------------------
Base AdvC 1.14 retains the exact same scalar deletes in both helpers.

K-Mod 1.46 and supplied Civ4CE/BtS do not have these temporary enum-map wrappers. Their
inherited `CvBuildingInfo::read` loads `CommerceChangeOriginalOwners` directly into the
persistent bool array:

    pXML->SetCommerce(&m_pbCommerceChangeOriginalOwner);

and later destroys that persistent array through `SAFE_DELETE_ARRAY`. There is no
corresponding temporary new[]/scalar-delete pair.

The current helpers are marked `advc.003t` and arise from AdvC practical 3183,
`59a3e3190061b421b117b58d047aa1196259f5fa` (2021-08-17), "Rewrite of enum map classes",
which replaced many persistent XML arrays with compact enum maps and introduced the
new temporary-array conversion helpers. Current SAS inherited the typo unchanged.

WHOLE-ALBUM DEDUP
-----------------
- F477 owns `CommerceChangeOriginalOwners` historical PlayerTypes aliasing after a dead
  player slot is recycled into a colony. It does not concern XML parsing or temporary
  storage lifetime.
- Queue 096 `CvMemoryManager.cpp` closed without a root and documented the distinct
  newMem/newMemArray and delMem/delMemArray debug routes. F657 uses that as an allocator
  oracle but is a separate live caller bug.
- Prior parser/info findings concern wrong fields, compact-map semantics, default/null
  handling or downstream random-event consumers. No prior root owns this mismatched
  delete-expression.

REPAIR BOUNDARY
---------------
Minimal C++03-safe repair in `CvXMLLoadUtility.h`:

    delete[] pbTmp;

and, for the same latent typo in the unused sibling helper:

    delete[] piTmp;

No XML schema, map encoding, BuildingInfo semantics or serialized data need change.

QUEUE-028 CONTINUATION
----------------------
F657 is frozen. Continue Queue 028 under F658 / provisional KI#980 unused.

Highest-value remaining surfaces:
- `SetGlobalClassInfo` default-object lifetime across monolithic + modular loads;
- pass-2 type-index lookup and replacement bounds;
- `LoadDiplomacyInfo` modular ordering / override identity;
- parser restoration in SetYields / SetCommerce / FeatureStruct / ImprovementBonuses;
- XMLTagPairIterator and XMLTagPairRateIterator sibling/depth contracts;
- invalid enum/index error paths, but promote only if current shipped data activates them;
- SAS-specific early Vote/Event loading and static MAX_NUM_LANGUAGES change (currently
  reconciled as safe, retain as negative controls unless later evidence contradicts it);
- whole-album dedup before consuming F658.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================

===============================================================================
C031-WIP516 -- QUEUE 028 CvXMLLoadUtilitySet.cpp -- CLOSED / QUEUE 029 OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP515.
- Queue 028 `CvGameCoreDLL/CvXMLLoadUtilitySet.cpp`: CLOSED.
- Confirmed Queue-028 root: F657 / provisional KI#979.
- F658 / provisional KI#980 remains UNUSED.
- Files complete: 61 / 169.
- Ordinary Queue 029 `CvGameCoreDLL/CyEnumsInterface.cpp` is now OPEN.
- The stale top-of-album progress/queue lines inherited through the recent append-only WIPs
  have been reconciled in this physical copy through Queue 029, so the header again agrees
  with the durable tail state.
- No gameplay source was modified.

QUEUE-028 FINAL SATURATION / NEGATIVE CONTROLS
----------------------------------------------
1. Current SAS-specific Vote/Event preload moves -- RECONCILED
   - VoteInfo is now loaded in PreMenu immediately after CivicInfo. `CvVoteInfo::read()`
     resolves ForceCivics and VoteSource data whose producers are already loaded there.
   - EventInfo/EventTriggerInfo are now loaded at the end of the PreMenu info block. Their
     current readers resolve yields, commerce, terrain/features, units/classes, buildings,
     civs/leaders, religions/corporations and other info families that are already loaded by
     that point. EventInfo pass 2 still runs only after the full EventInfo pass-1 population.
   - The move is therefore not itself a demonstrated dependency-order regression.

2. `LoadOptionalGlobals()` unconditional `DestroyFXml()` after the eager-event move --
   NOT a new SAS root
   - `CvGame::reset()` calls `CvGlobals::loadOptionalXMLInfo()` for every non-constructor
     reset, so the no-True-Starts branch is genuinely reachable after the event preload move.
   - However, supplied Base AdvC 1.14 already has a supported path with no parser created in
     this call: when GAMEOPTION_NO_EVENTS is enabled, the game is not network multiplayer,
     and True Starts is disabled, the inherited event block is skipped and the function still
     ends in `DestroyFXml()`.
   - The XML interface signature is `DestroyFXml(FXml*& xml)`, i.e. ownership is passed by
     reference. The longstanding Base path plus that interface contract are strong evidence
     that destroying a null/already-cleared parser handle is intentional/safe. The SAS move
     merely makes that existing branch common; it does not establish a new double-destroy.

3. GlobalDefine load-order regression -- CLOSED against current data
   - SAS adds `GlobalDefines_advciv_sas.xml` before the BBAI/AI/TechDiffusion/LFB define
     files. A name census found 743 SAS defines and ZERO duplicate names in those four files
     loaded afterward (9 + 38 + 8 + 10 definitions respectively).
   - Therefore none of the current SAS-specific define overrides is silently undone by those
     later inherited files.

4. `MAX_NUM_LANGUAGES` local -> static-const optimization -- SAFE under current contract
   - The value is read only after SetGlobalDefines and represents the fixed XML-language
     ceiling for the process. No current runtime setter/reload path mutates that define before
     a later LoadGlobalText call. Caching the lookup does not change current language selection.

5. FunctionTypes six-enum/five-XML asymmetry -- ALREADY OWNED AS NEGATIVE CONTROL
   - Queue 040 already proved C++ has six FunctionTypes including FUNC_STEPKEY while stock and
     current GlobalTypes XML supplies five strings. `SetGlobalStringArray(..., bUseEnum=true)`
     allocates six safely and leaves the sixth string empty; no current SAS/CE asset or caller
     consumes FUNC_STEPKEY. Do not rediscover this as F658.

6. `SetGlobalClassInfo` default/pass-2 lifecycle -- CURRENT MONOLITHIC PATH RECONCILES
   - Current AdvC-SAS has ModularLoading disabled. In the ordinary path the static XML-default
     object is released on the final call for each class load.
   - The only current `*_DEFAULTS` info element is LEADER_DEFAULTS, and LeaderHeadInfo is a
     one-pass class. No current two-pass class combines XML-default inheritance with a live
     module boundary.
   - Current two-pass classes (Tech, Promotion, Improvement, Project, Civilization, Event)
     perform pass 2 immediately after their complete pass-1 population when modular loading is
     off. Current Type strings are unique across the non-text XML census, so GetChildTypeIndex
     maps each pass-2 record back to the intended same-vector index.
   - Module-failure/static-default cleanup edges remain mod robustness debt only.

7. Diplomacy modular ordering -- BELOW CURRENT-LIVE THRESHOLD
   - `LoadGlobalClassInfo` sorts module files; `LoadDiplomacyInfo` does not. That can make
     repeated diplomacy-type merges dependent on EXE enumeration order under modular XML.
   - Current `AdvCiv-SAS.ini` has ModularLoading=0, so no current shipped activation was
     established. Keep as a modular-support note, not F658.

8. Yield/commerce fixed-array loops and comments -- CURRENT DATA SAFE
   - `SetYields` and templated `SetCommerce` diagnose an excessive sibling count but would
     still index by that count, making malformed/oversized XML a genuine hardening concern.
   - AdvC's own XML helper comments record that XML comment nodes do not appear to inflate the
     relevant child counts. Current XML census finds no yield list above NUM_YIELD_TYPES=3 and
     no commerce list above NUM_COMMERCE_TYPES=4; inline K-Mod/SAS comments inside such lists do
     not create a demonstrated current overflow.

9. `SetFeatureStruct` / `SetImprovementBonuses` current producer validation -- CLEAN
   - Current BuildInfos feature structures reference only known current FeatureTypes; the
     current comment before a Fallout struct is safely skipped by the helper's value-navigation
     contract.
   - Current ImprovementInfos bonus structures reference only known BonusTypes, contain no
     overlong yield arrays, and have no duplicate live bonus-key condition requiring a separate
     parser root.
   - Their error branches can still index a negative/out-of-range key after logging if fed bad
     XML, but current shipped data does not activate those branches.

10. Generic primitive tag-pair out-of-range branch -- REAL HARDENING ISSUE, NOT CURRENT ROOT
    - The templated `SetVariableListTagPair(T**...)` logs/asserts when `pair.first >=
      iInfoBaseLength` but then still writes `(*pptList)[pair.first]`. The string/audio siblings
      have analogous trust-after-assert patterns.
    - This should ideally `continue`/reject malformed keys, but current XML/type census and the
      already-audited info producers provide no current out-of-category key reaching the branch.
      Per the album threshold, keep it as malformed/mod XML robustness debt rather than F658.

11. `XMLTagPairIterator` / `XMLTagPairRateIterator` parser-depth and temporary ownership -- CLEAN
    - Base-destructor parser-level restoration balances every local descent used by the current
      iterator consumers; rate iterator temporary arrays are destroyed with `delete[]`.
    - Singular code tag names intentionally pluralize to the Firaxis outer XML container
      (`BuildingExtraYield` -> `BuildingExtraYields`, etc.), already confirmed against current
      random-event data in Queue 027.
    - No current duplicate-key or compact-rate representation loss survived the producer census.

12. Global actions / fixed mission-command lookups -- NO CURRENT ACTIVATION DEFECT
    - The AdvC rewrite preserves the inherited ActionInfo population and subtype index updates.
      `SetGlobalActionInfo` is called once in the normal bootstrap with an initially empty
      ActionInfo vector, making the inherited mission-offset compatibility term zero.
    - Required MISSION_/COMMAND_ identities are present in current info data. No second-call or
      action-vector reload path was established.

13. `ReadGlobalDefines`/post-global error paths -- CURRENT FILE SET VALID
    - `ReadGlobalDefines` can return true after a LoadCivXml failure and several post-global
      conversion branches rely on diagnostics rather than aborting. Those are legitimate loader
      robustness weaknesses.
    - All required current shipped GlobalDefines files are present and the current post-global
      type strings resolve in the loaded info universe. No normal startup activation was found.

CONFIRMED ROOT RETAINED
-----------------------
F657 / provisional KI#979 remains the sole Queue-028 finding:
- `SetVariableListPerCommerce` allocates a temporary with `new bool[...]` and destroys it
  with scalar `delete`;
- current `CommerceChangeOriginalOwners` data executes the mismatched deallocation for 51
  BuildingInfos during ordinary startup;
- the dormant Yield sibling has the same typo and belongs to the same repair root;
- repair both with `delete[]`.

CLOSURE JUDGMENT
----------------
Queue 028 is closed for this audit pass. After F657, the remaining shared-loader surfaces
reduce to current-safe contracts, already-owned controls, disabled modular/cache paths, or
malformed-XML hardening where no shipped producer activates the unsafe error branch. No evidence
supports consuming F658 merely to increase the finding count.

EXACT CONTINUATION
------------------
C031-WIP516 physical -> Queue028 CLOSED -> F657/KI#979 confirmed -> F658/KI#980 UNUSED ->
files complete 61/169 -> Queue029 `CvGameCoreDLL/CyEnumsInterface.cpp` (2,052 queued lines)
OPEN -> establish current SAS vs Base AdvC 1.14 / K-Mod / Civ4CE provenance -> audit every
Python-exposed enum binding against the actual C++ enum values and EXE/Python consumers,
with special attention to omitted/renamed enum members, duplicate Python names, wrong enum
class registrations, value shifts, sentinel exposure, stale commented bindings, and any SAS
C++ enum additions not exported to Python -> whole-album dedup before numbering -> freeze the
next physical WIP at the next settled boundary whether F658 is consumed or remains unused.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================



===============================================================================
C031-WIP517 -- QUEUE 029 CyEnumsInterface.cpp -- F658 FROZEN / CONTINUE OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP516.
- Queue 029 `CvGameCoreDLL/CyEnumsInterface.cpp`: OPEN.
- Confirmed Queue-029 root: F658 / provisional KI#980.
- F659 / provisional KI#981 is UNUSED.
- Files complete remain 61 / 169; Queue 029 is not closed yet.
- No gameplay source was modified.

F658 / provisional KI#980 -- AdvC Python `NO_GLOBE_LAYER` binding exports the enum COUNT sentinel instead of `NO_GLOBE_LAYER`, so ordinary no-layer reports store and serialize the wrong GlobeLayerTypes value
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ROOT / CONTRACT
---------------
AdvC added two DLL-only state values after the real globe-layer range:

    enum GlobeLayerTypes
    {
        GLOBE_LAYER_STRATEGY = -1,
        GLOBE_LAYER_TRADE,       // 0
        GLOBE_LAYER_UNIT,        // 1
        GLOBE_LAYER_RESOURCE,    // 2
        GLOBE_LAYER_RELIGION,    // 3
        GLOBE_LAYER_CULTURE,     // 4
    #ifdef _USRDLL
        NUM_GLOBE_LAYER_TYPES,   // 5 -- count / one-past-last real layer
        NO_GLOBE_LAYER,          // 6 -- actual no-layer state
        GLOBE_LAYER_UNKNOWN      // 7 -- initialization/unknown state
    #endif
    };

The matching Python interface should therefore expose the symbolic no-layer state as value 6.
Current SAS instead inherits this binding:

    python::enum_<GlobeLayerTypes>("GlobeLayerTypes")
        ...
        .value("NUM_GLOBE_LAYER_TYPES", NUM_GLOBE_LAYER_TYPES)
        .value("NO_GLOBE_LAYER", NUM_GLOBE_LAYER_TYPES)
        ;

Thus two different Python names collapse to value 5, while the actual C++ `NO_GLOBE_LAYER`
value 6 is not exposed at all.

LIVE CURRENT ACTIVATION
-----------------------
This is not a dormant public-API typo. Current `Assets/Python/Screens/CvMainInterface.py`
uses the bad name in the normal globe-view update path:

    eCurrentLayerType = GlobeLayerTypes.NO_GLOBE_LAYER
    if iCurrentLayerID >= 0:
        ... translate a real layer ...
    gc.getGame().reportCurrentLayer(eCurrentLayerType)

Whenever the engine reports no current globe layer (`iCurrentLayerID < 0`), Python therefore
passes 5 rather than the intended 6.

The exposed `CyGame::reportCurrentLayer(int)` performs only a cast and forwards the value:

    m_kGame.reportCurrentLayer((GlobeLayerTypes)iLayer);

and, outside the save-restore branch, `CvGame::reportCurrentLayer` stores it directly:

    else m_eCurrentLayer = eLayer;

`CvGame::write` then serializes `m_eCurrentLayer`. Therefore ordinary current play can populate
and save the internal current-layer state with `NUM_GLOBE_LAYER_TYPES` -- a count sentinel --
when the semantic state is `NO_GLOBE_LAYER`.

CURRENT CONSEQUENCE / SEVERITY BOUNDARY
---------------------------------------
The immediate visible gameplay effect is muted by the current consumers: the important hot
checks mostly ask only whether `getCurrentLayer()` equals `GLOBE_LAYER_RESOURCE` or
`GLOBE_LAYER_UNIT`, and both the wrong count value 5 and correct no-layer value 6 compare false.
The Python UI likewise only branches specially on Resource/Unit after reporting the layer.

That does not make the binding correct. The active Python->DLL contract transmits the wrong enum
identity, `CvGame` stores a value that means "number of real layer types" rather than "no layer",
and savegames preserve that wrong identity. Future/current code that distinguishes count from
no-layer would inherit corrupted state. This is a low-current-impact but fully live value-binding
defect, not merely theoretical external API misuse.

LINEAGE / ORACLE
----------------
AdvC practical 1465 / commit `1e107b2c01f79eca77c363287362969920c635e6`
("Unit layer updated upon visible moves") introduced the complete current-layer reporting system
and the typo in the same change. Its CyEnumsInterface hunk added:

    .value("NUM_GLOBE_LAYER_TYPES", NUM_GLOBE_LAYER_TYPES)
    .value("NO_GLOBE_LAYER", NUM_GLOBE_LAYER_TYPES)

while the same practical added distinct `NO_GLOBE_LAYER` and `GLOBE_LAYER_UNKNOWN` values to
`CvEnums.h` and made `CvMainInterface.py` initialize the reported state from
`GlobeLayerTypes.NO_GLOBE_LAYER`.

Supplied Base AdvC 1.14 retains the same bad binding unchanged. K-Mod 1.46 and Civ4CE do not have
AdvC's added NO/UNKNOWN globe-layer reporting states, so they are ancestry controls rather than a
corrected downstream oracle.

WHOLE-ALBUM DEDUP
-----------------
Whole-album grep/review found no prior `NO_GLOBE_LAYER`, `reportCurrentLayer`,
`GLOBE_LAYER_UNKNOWN` or `NUM_GLOBE_LAYER_TYPES` owner. Queue 008 audited downstream `CvGame.cpp`
without owning this producer: its report/store code behaves according to the enum value Python
supplies. F658 therefore belongs to Queue 029's binding layer.

NARROW REPAIR BOUNDARY
----------------------
The direct repair is one token in `CyEnumsInterface.cpp`:

    .value("NO_GLOBE_LAYER", NO_GLOBE_LAYER)

No enum ordering, save layout, Python caller, globe-layer engine ID or UI translation needs to
change. Existing saves that happened to store the old count sentinel remain a compatibility
consideration, but the producer should stop generating that invalid semantic state.

QUEUE-029 CONTROLS / SIDE DISCOVERY SO FAR
------------------------------------------
1. `TaskTypes.TASK_CONSCRIPT` is inherited as `TASK_HURRY` in SAS, Base AdvC, K-Mod and Civ4CE.
   C++ dispatch distinguishes the tasks, so the binding is plainly wrong, but no current shipped
   Python caller of `TaskTypes.TASK_CONSCRIPT` was found. Keep as latent public-API cleanup unless
   a supported current caller appears; do not merge it into F658.

2. `MemoryTypes.MEMORY_DECLARED_WAR_RECENT` is bound to `MEMORY_INDEPENDENCE`. The copy/paste
   error was introduced by AdvC together with the new memory enum and survives Base AdvC 1.14.
   Current SAS Sevopedia memory metadata uses string/index data rather than the Python enum
   attribute, and no shipped `MemoryTypes.MEMORY_DECLARED_WAR_RECENT` access was found. Keep as a
   latent binding typo unless a current consumer appears; do not merge it into F658.

3. `BUTTONPOPUP_KICKED` -> `BUTTONPOPUP_PITBOSS_DISCONNECT` is inherited through Civ4CE/K-Mod and
   appears to be a compatibility alias, not a newly established root.

4. Active-Python missing-member census initially produced many false alarms. BUG intentionally
   creates custom `WidgetTypes` at runtime through `WidgetUtil.createWidget`; FontUtil similarly
   augments `FontSymbols`; BULL-only sentry constants sit in guarded compatibility code. Those
   dynamically-created names are not omissions from CyEnumsInterface.

5. Cross-file SAS KI#108 bug discovered during the binding census -- REAL but NOT Queue-029-owned:
   current `CvDiplomacy.isSimpleVassalSelfOffer()` (introduced in SAS practical/commit 5480,
   `51122e118b...`) compares against `TradeableItems.NO_TRADE_ITEM`, but Python historically
   exposes the -1 trade sentinel as `TradeableItems.NO_TRADEABLE_ITEMS`. The requested attribute
   therefore raises AttributeError. A broad `except: pass` swallows that error. If a second trade
   item exists, retrieval can succeed, the missing-name comparison then throws, and the helper can
   fall through to `True` instead of rejecting the mixed offer. If that mixed demand is accepted,
   the caller can wrongly suppress `DIPLOEVENT_MADE_DEMAND`, contrary to KI#108's explicit
   "pure one-item Vassal/Surrender only" contract. The narrow repair belongs in the new Python
   helper (use the actually exported sentinel or avoid probing an unavailable list length), not
   in the inherited enum binding. Preserve this for the SAS issue/fix workflow rather than
   misnumbering it as F658.

EXACT CONTINUATION
------------------
C031-WIP517 physical -> Queue029 OPEN -> F658/KI#980 confirmed -> F659/KI#981 UNUSED -> continue
full binding census, especially C++ enum member vs Python name/value identity, SAS-specific
WorldSize/Control/Feat/UnitAI/SASAutoPlay additions, hardcoded sentinels/counts, duplicate-value
aliases, and every remaining genuinely active Python enum reference after dynamic BUG/Font
extensions are removed -> compare Base AdvC/K-Mod/Civ4CE/history -> preserve the KI#108 Python
sentinel bug as a separate cross-file issue -> whole-album dedup -> either promote a genuinely
live independent F659 or close Queue029 cleanly -> freeze the next physical WIP regardless.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP518 -- QUEUE 029 CyEnumsInterface.cpp -- CLOSED / QUEUE 030 OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP517.
- Queue 029 `CvGameCoreDLL/CyEnumsInterface.cpp`: CLOSED.
- Confirmed Queue-029 root: F658 / provisional KI#980.
- F659 / provisional KI#981 remains UNUSED.
- Files complete: 62 / 169.
- Ordinary Queue 030 `CvGameCoreDLL/CvInfo_Misc.cpp` (1,919 queued lines) is OPEN.
- No gameplay source was modified.

QUEUE-029 FINAL SATURATION / NEGATIVE CONTROLS
-----------------------------------------------
1. Binding inventory and duplicate-value sweep
   - After removing comments, current SAS registers 154 Boost.Python enum classes and 1,413
     active `.value(...)` calls in this file.
   - No duplicate Python member name occurs within a registered class.
   - Only four registered C++ symbols are deliberately/accidentally exposed under two Python
     names: BUTTONPOPUP_PITBOSS_DISCONNECT, TASK_HURRY, MEMORY_INDEPENDENCE and
     NUM_GLOBE_LAYER_TYPES. The last is F658; the Task/Memory pairs are latent typos discussed
     below; the ButtonPopup pair is an inherited compatibility alias.

2. All ten Python-name/C++-symbol spelling mismatches were classified
   - `PLOT_STYLE_NONE -> NO_PLOT_STYLE`: explicit AdvC naming compatibility.
   - `BUTTONPOPUP_KICKED -> BUTTONPOPUP_PITBOSS_DISCONNECT`: inherited through Civ4CE/K-Mod;
     no independent current defect established.
   - `FORCECONTROL_MAX_CITY_ELIMINATIONS -> FORCECONTROL_MAX_CITY_ELIMINATION`: Python plural
     compatibility spelling for the same intended control.
   - `NUM_VOICETARGETS -> NUM_VOICETARGET_TYPES`: count-name spelling compatibility.
   - `TASK_CONSCRIPT -> TASK_HURRY`: real inherited copy/paste typo, but no shipped Python
     consumer of the bad member was found; latent public-API debt, not F659.
   - `NO_ENTITY_EVENT_NONE -> NO_ENTITYEVENT` and `ANIMCAT_NONE -> NO_ANIMCAT`: sentinel naming
     compatibility for dynamically-populated info enums.
   - `NO_TRADEABLE_ITEMS -> TRADE_ITEM_NONE`: intentional old Python spelling for the -1 trade
     sentinel after AdvC regularized the C++ enum aliases.
   - `MEMORY_DECLARED_WAR_RECENT -> MEMORY_INDEPENDENCE`: real AdvC copy/paste typo, but current
     shipped memory UI/provider code uses index/string metadata and never accesses this Python
     enum attribute; latent API debt, not F659.
   - `NO_GLOBE_LAYER -> NUM_GLOBE_LAYER_TYPES`: F658, current-live and serialized.

3. Current shipped Python access census
   - Direct `EnumClass.MEMBER` references were compared against the current hardcoded bindings.
   - The apparent BUG widget omissions are intentional: `WidgetUtil.createWidget` allocates
     values starting at `WidgetTypes.NUM_WIDGET_TYPES`, and current Assets/Config XML explicitly
     registers the CDA, GP bar, BUG option, Info-screen and Tech-chooser widget names seen in
     Python.
   - FontUtil intentionally adds dynamic FontSymbols used by BUG/UI screens.
   - BULL-only sentry Activity/Mission members in UnitUtil are compatibility probes inside a
     guarded try/except and are absent from current SAS C++ by design.
   - `TradeableItems.TRADE_DISENGAGE` appears only inside unused BUG DealUtil helper definitions;
     no current caller of those helpers was found. `TRADE_CITY` appears only in TradeUtil's test
     routine while the actual current binding is the inherited plural `TRADE_CITIES`.
   - After those runtime extensions/guarded/test-only cases are removed, no second current
     hardcoded Python enum access points to an unbound Queue-029 member. The one real active
     missing name is SAS KI#108's `NO_TRADE_ITEM`, which is the separate caller bug below.

4. SAS-specific enum-interface changes versus supplied Base AdvC 1.14 -- RECONCILED
   Current semantic differences reduce to:
   - new `SASAutoPlayEndCause` class;
   - ControlTypes replacement/removal matching the current advisor redesign;
   - FeatTypes names matching SAS's split archer/mounted/melee combat families;
   - UnitAITypes great-person renames matching current CvEnums;
   - two current WidgetTypes additions already introduced for fixed advisor hovers;
   - WorldSizeTypes Arena + SAS24/SAS32/SAS40/SAS48 additions.

   Each current addition maps to its same-named C++ constant. The current WorldInfo XML order is
   exactly Arena, Duel, Tiny, Small, Standard, Large, Huge, SAS24, SAS32, SAS40, SAS48, matching
   the C++ WorldSizeTypes order fixed in SAS practical 5971. All nine SASAutoPlayEndCause values
   match CvEnums.h and their live AIAutoPlay.py consumers.

5. Inherited Task typo -- retained below current finding threshold
   `TaskTypes.TASK_CONSCRIPT` is bound to `TASK_HURRY` in current SAS, Base AdvC 1.14, K-Mod and
   Civ4CE. Current C++ `CvCity::doTask` distinguishes hurry from conscription, so an external
   Python caller would execute the wrong city task. However, the current shipped Python tree has
   no `TaskTypes.TASK_CONSCRIPT` access. Per the album's supported-current-caller rule, preserve
   this as a real latent API bug without consuming F659.

6. AdvC Memory typo -- retained below current finding threshold
   AdvC introduced `MEMORY_DECLARED_WAR_RECENT` and simultaneously exported that Python name as
   `MEMORY_INDEPENDENCE`; Base AdvC 1.14 still carries it. Current Python does not access the bad
   enum attribute, so no current manifestation independent of the binding itself was established.

7. Cross-file SAS KI#108 regression exposed by the census -- ACTIONABLE, NOT Queue-029 ROOT
   SAS practical 5480 added `CvDiplomacy.isSimpleVassalSelfOffer()` with
   `TradeableItems.NO_TRADE_ITEM`. The inherited Python API actually exposes the -1 sentinel as
   `NO_TRADEABLE_ITEMS`. The helper's broad exception swallows the resulting AttributeError; when
   a second item can be retrieved, that can let a mixed Vassal/Surrender demand pass the helper's
   intended "no second item" test and suppress `DIPLOEVENT_MADE_DEMAND` after acceptance.
   This directly undermines KI#108's documented narrow pure-one-item guard. Repair the Python
   caller (e.g. use the exported sentinel) in the ordinary issue/fix workflow; do not alter the
   historical enum alias merely to make the erroneous new caller name work.

CONFIRMED ROOT RETAINED
-----------------------
F658 / provisional KI#980 remains the sole Queue-029 finding:
- Python `GlobeLayerTypes.NO_GLOBE_LAYER` exports `NUM_GLOBE_LAYER_TYPES` instead;
- current CvMainInterface uses that name on every ordinary no-layer report;
- CyGame casts/forwards it and CvGame stores/serializes the count sentinel as current-layer state;
- immediate behavior is mostly masked by current Resource/Unit equality-only consumers;
- repair the binding to `.value("NO_GLOBE_LAYER", NO_GLOBE_LAYER)`.

CLOSURE JUDGMENT
-----------------
Queue 029 is saturated for this audit pass. The remaining anomalies are intentional compatibility
names, dynamic BUG/Font extensions, guarded/test-only references, or real but currently dormant
public API typos lacking a supported shipped caller. No evidence supports spending F659 merely on
an omission that current SAS never consumes.

EXACT CONTINUATION
------------------
C031-WIP518 physical -> Queue029 CLOSED -> F658/KI#980 confirmed -> F659/KI#981 UNUSED ->
files complete 62/169 -> Queue030 `CvGameCoreDLL/CvInfo_Misc.cpp` OPEN -> establish current SAS vs
Base AdvC 1.14 / K-Mod / Civ4CE semantic provenance -> audit its mixed info readers/getters,
array ownership/default initialization, enum/index resolution, XML read/write symmetry,
leader/action/diplomacy/misc data contracts and any SAS changes -> current XML producer census +
whole-album dedup before numbering -> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP519 -- QUEUE 030 CvInfo_Misc.cpp -- CLOSED / QUEUE 032 OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP518.
- Queue 030 `CvGameCoreDLL/CvInfo_Misc.cpp`: CLOSED.
- No new Queue-030 finding was promoted.
- F659 / provisional KI#981 remains UNUSED.
- Files complete: 63 / 169.
- Queue 031 `UWAICache.cpp` was already complete from the earlier priority/deep pass, so ordinary
  order advances to Queue 032 `CvGameCoreDLL/CyUnit.cpp` (1,704 queued lines), now OPEN.
- No gameplay source was modified.

QUEUE-030 PROVENANCE BASELINE
-----------------------------
A normalized current-SAS versus supplied Base AdvC 1.14 comparison reduced the entire 1,919-line
file to one executable SAS divergence plus one comment-only language note:

1. `CvMainMenuInfo::getSoundtrack()` contains the SAS opening-menu shuffle-bag feature.
2. The `CvGameText::read` language-count area has a SAS comment only; executable behavior remains
   Base AdvC 1.14.

Every other executable body in current `CvInfo_Misc.cpp` is Base-AdvC-equivalent. K-Mod 1.46 and
Civ4CE provide the older monolithic `CvInfos.cpp` ancestry for the inherited classes. This made
Queue 030 principally a current-data/lifetime audit of the one SAS divergence plus a targeted
inherited reader/getter saturation pass rather than a broad SAS rewrite review.

SAS MAIN-MENU SHUFFLE -- CURRENTLY CLEAN / TARGETED RUNTIME OBSERVATION ONLY
----------------------------------------------------------------------------
The custom getter keeps a function-static shuffle bag and consumes one entry whenever the EXE
asks `CvMainMenuInfo::getSoundtrack()` for the configured trigger profile. This initially looked
risky because an exported getter with destructive static state would skip/consume tracks if the
closed-source EXE queried it more than once for one logical menu opening.

The local evidence does not justify promoting that concern:
- there is no local DLL/Python caller; `getSoundtrack()` is a DllExport consumed by the EXE, so
  exact call multiplicity cannot be proved from the supplied source set;
- SAS practical 5485 / `34d4d7d39242f5824ea69f7524c6995b0901bc76` introduced the feature with
  the explicit contract "no repeats until all configured scripts are played once";
- the later build check validates the trigger and every active shuffle entry against
  `Audio2DScripts.xml`;
- the current light-source configuration has shuffle enabled, trigger `AS2D_OPENING_MENU`, exactly
  two active entries (`AS2D_OPENING_MENU_01`, `_02`), seven `NONE` slots, no duplicate active
  script, and both active scripts plus the trigger exist in current `Audio2DScripts.xml`;
- KI#110's retained runtime notes explicitly separate the old Sevopedia/BUG path-startup failures
  from the shuffle itself and state that the music shuffle "seems to work fine."

The implementation also guarantees a nonempty bag through its trigger fallback before the RNG
range is formed and correctly uses `getASyncRand()` for UI/non-synchronized selection. Without a
reproduction showing multiple `getSoundtrack()` consumptions per logical opening, the stateful
getter remains a worthwhile targeted runtime observation, not F659.

INHERITED DIPLOMACY NULL-POINTER TYPO -- REAL DEAD CODE, NOT F659
-----------------------------------------------------------------
AdvC practical 1610 / `409be17bbd437acf2670994d2bb5d1516ada865e`, "Store empty tag pair lists
as NULL pointers," compacted all-default `SetVariableListTagPair` arrays to NULL. In the obsolete
`CvDiplomacyTextInfo` class, four getters were changed to shapes such as:

    return (m_pResponses[i].m_pbCivilizationTypes[j] == NULL ? false :
            m_pResponses[i].m_pbCivilizationTypes[j]);

That is not a pointer check: `[j]` is evaluated before the comparison, so a compacted NULL list
would be dereferenced. The comment clearly intended null-safe behavior, and the K-Mod predecessor
had simply indexed an always-allocated array.

This does not become a current root because `CvDiplomacyTextInfo` has no current `CvGlobals` info
array/getter and no shipped producer/consumer path beyond its standalone Boost.Python class
binding. The live diplomacy system is `CvDiplomacyInfo` / `CvDiplomacyResponse` in
`CvInfo_Civilization.cpp`; its corresponding AdvC-compaction-aware getters correctly use:

    m_pbCivilizationTypes != NULL ? m_pbCivilizationTypes[i] : false

(and equivalent pointer-first tests for leader, attitude and diplomacy-power arrays). The bad
legacy getters are therefore useful dead-code archaeology and a possible cleanup if the class is
ever resurrected, but not a supported-current F659.

CURRENT XML / VECTOR SHAPE CONTROLS
-----------------------------------
1. `CvWorldPickerInfo`
   Current `CIV4WorldPickerInfos.xml` contains 32 WorldPickerInfo entries. Every one has exactly
   12 `<Size>` values: Random plus the current 11 world sizes (Arena, Duel, Tiny, Small, Standard,
   Large, Huge, SAS24, SAS32, SAS40, SAS48). Twenty-five entries intentionally have only the
   generic Random climate/water decal/gloss; seven have 6 climate and 4/4 water paths. No current
   size-vector underflow relative to the SAS world-size extension was found.

2. Allocation / destruction symmetry
   Live dynamic allocations in this file pair `new[]` with `SAFE_DELETE_ARRAY`: diplomacy
   response arrays, tutorial-message arrays and tutorial script lists. The Queue-028 scalar
   delete bug does not repeat here. `CvQuestInfo` is commented out as `advc.003j: unused`.

3. `CvEffectInfo` / `CvAttachableInfo` default-copy stubs
   Their copy constructors deliberately error instead of copying, but both advertise
   `isDefaultsType() == false`; the XML-default template therefore does not use these as defaults
   types. No current default-inheritance activation was established.

4. Animation/entity vectors
   `CvAnimationPathInfo` and `CvEntityEventInfo` retain inherited EXE-facing index APIs with
   upper-bound or caller-count contracts. Negative indices could harden poorly in some getters,
   but no current DLL/Python producer supplies a negative index; this remains external-API
   robustness rather than a supported-current root.

5. Tutorial / legacy UI structures
   Tutorial arrays are initialized from counted `TutorialMessage` children and destroyed
   symmetrically. The old Quest implementation is compiled out. Slide-show, throne-room,
   route/river model, landscape and formation readers remain the Base/K-Mod inherited contracts;
   no current source/data mismatch survived the pass.

6. `CvGameText::read`
   Current SAS executable behavior is Base AdvC/K-Mod behavior. The pre-existing language-count
   fallback and sibling traversal were reviewed but no current language fixture or supported
   caller demonstrated a new wrong-language/out-of-range root. The existing SAS source comment
   correctly leaves this external/localization contract untouched.

HISTORY / DEDUP CONTROLS
------------------------
- AdvC changes touching the split file after Queue-030's ancestry point are predominantly the
  CvInfos split, assertion/style cleanup, XML default plumbing, setter cleanup and enum-map
  refactoring. Supplied Base AdvC 1.14 carries the same inherited implementations.
- Practical 1783's `GetXmlVal(fVal)` WorldPicker adjustment matches the current XML helper API.
- Practical 2222's RouteType `SetInfoIDFromChildXmlVal` conversion and the explicit non-default
  Effect/Attachable copy stubs survive consistently.
- Practical 3183's broad enum-map rewrite does not create a second current allocation or compact-
  pointer defect in this file.
- Whole-album review found existing main-menu/profile and KI#110 music-path investigations but no
  owner that would turn the unproved stateful getter concern into a duplicate finding.
- No existing finding is undermined or reopened by Queue 030.

CLOSURE JUDGMENT
-----------------
Queue 030 is saturated for this audit pass. The sole current SAS semantic divergence is the
opening-music shuffle, whose current pool/references are valid and whose recorded runtime behavior
is healthy; the only sharp inherited pointer bug found is in an obsolete unregistered diplomacy
class while the live sibling uses the correct pointer-first test. The remaining concerns are
external-EXE/API robustness or invalid-data/module-reload cases without a current producer.
Do not spend F659 merely to turn those into findings.

EXACT CONTINUATION
------------------
C031-WIP519 physical -> Queue030 CLOSED -> F659/KI#981 UNUSED -> files complete 63/169 -> Queue031
already complete from prior pass -> Queue032 `CvGameCoreDLL/CyUnit.cpp` OPEN -> establish current
SAS vs Base AdvC 1.14 / K-Mod / Civ4CE provenance -> reconcile every Python wrapper argument,
return sentinel, ownership/lifetime and enum cast against `CvUnit` plus current shipped Python
callers -> prioritize wrappers that silently drop/replace parameters, stale-unit/null handling,
wrong enum domains and Python-visible behavior divergence -> whole-album dedup before numbering ->
either promote a demonstrated F659 or close Queue032 cleanly -> freeze the next physical WIP at
the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP520 -- QUEUE 032 CyUnit.cpp -- CLOSED / QUEUE 033 OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP519.
- Queue 032 `CvGameCoreDLL/CyUnit.cpp`: CLOSED.
- No new Queue-032 finding was promoted.
- F659 / provisional KI#981 remains UNUSED.
- Files complete: 64 / 169.
- Queue 033 `CvGameCoreDLL/CvInfo_Terrain.cpp` (1,672 queued lines): OPEN.
- No gameplay source was modified.

QUEUE-032 PROVENANCE BASELINE
-----------------------------
Current SAS `CyUnit.cpp` is executable-token-identical to supplied Base AdvC 1.14 after comments
and whitespace are removed. Therefore there is no SAS-specific Queue-032 regression and no newer
Base-AdvC CyUnit-wrapper repair that SAS failed to port.

K-Mod 1.46 and Civ4CE provide the older wrapper baseline. K-Mod and Civ4CE are themselves
executable-equivalent here apart from K-Mod commenting out the obsolete `canCargoAllMove` and
`canMoveThrough` wrappers. Relative to K-Mod, the current AdvC/SAS differences are a compact set
of signature/rename adaptations plus the `isWorker` helper and const-wrapper constructor.

ADVC SIGNATURE-ADAPTATION AUDIT -- RECONCILED
---------------------------------------------
The material K-Mod -> current wrapper changes were traced against current `CvUnit` declarations,
their introducing history and current shipped Python callers:

1. Path movement
   `generatePath` now passes a plot by reference plus `MovementFlags`; `canMoveInto` and
   `canMoveOrAttackInto` likewise pass plot references. Current shipped Python has no
   `CyUnit.generatePath` caller. `AStarTools.py` does call `canMoveOrAttackInto(plot, true)`;
   the wrapper preserves K-Mod's two-argument Python contract and lets AdvC's later
   `bDangerCheck=false` default remain false. The AStar caller supplies valid CyPlots.

2. Territory / area
   AdvC merged `CvUnit::canEnterArea` into `canEnterTerritory(..., pArea)`. The CyUnit wrapper's
   old `canEnterArea` public name delegates to that merged function with the same team,
   right-of-passage and area inputs. There is no shipped current Python call.

3. Cargo loading
   `canLoadUnit` -> `canLoadOnto` and `canLoad` -> `canLoadOntoAnyUnit` preserve the old public
   wrapper meanings after AdvC's load-function rename/reference conversion. No shipped current
   Python caller exercises either wrapper.

4. Sea Patrol
   After AdvC added `bCheckActivity`, the wrapper deliberately passes `true`; practical 3866
   states this is "For the same behavior as in K-Mod." No contradictory current caller was found.

5. Air Bomb / nuke / interceptor
   The route-capable air-bomb refactor changes `canAirBombAt` from `(fromPlot,x,y)` internally to
   `(targetPlot,fromPlot)`. The wrapper reconstructs target `(x,y)`, validates it and forwards
   the optional source plot. `canNukeAt` and `bestInterceptor` received matching reference/null
   adaptations. None has a shipped current Python caller.

6. Experience
   AdvC changed the final `CvUnit::changeExperience` argument from boolean update-global to a
   global-percent integer. The legacy Python boolean is intentionally translated to 100 or 0,
   preserving its original all-or-none meaning. No shipped current Python caller uses it.

7. Renamed/internal methods
   `isNoCapture -> isNoCityCapture`, `isCombat -> isInCombat`, owner/X/Y accessors and AI
   accessors follow current C++ renames/refactors. `isWorker` is the deliberate AdvC addition.
   Current Python's sole visible `isCombat()` occurrence is a comment documenting why the
   Military Advisor correctly uses `canFight()` instead; `isWorker()` has one normal live caller.

INHERITED WRONG-METHOD / ENUM-CAST SWEEP -- CLEAN
-------------------------------------------------
A mechanical wrapper-name -> underlying-method census found only nine non-identical method names:
`canEnterArea/canEnterTerritory`, the two load-renames, `isNoCapture/isNoCityCapture`,
`isCombat/isInCombat`, `isInGroup/getGroupID`, `getNameForm/getName(form)`, and the two
UnitAI accessors. Each is an intentional current contract; there is no surviving sibling of the
old K-Mod `CyUnit::canJoin -> CvUnit::canFound` typo (fixed by AdvC practical 2536).

Every wrapper parameter annotated with an enum domain was also compared with the cast used in the
body. No wrong enum domain survived. The active `CyUnitInterface1.cpp` Boost.Python table binds
each exported Python name to the same-named `CyUnit` method, with no active duplicate or
misdirected function pointer.

NULL / WRAPPER-LIFETIME HARDENING -- BELOW FINDING THRESHOLD
-------------------------------------------------------------
A number of inherited wrappers dereference auxiliary `CyPlot*`, `CyUnit*`, `CyArea*` or `CyCity*`
arguments after checking only `m_pUnit`; examples include `canMoveInto`, `canLoadUnit`,
`setTransportUnit`, several combat-strength helpers and `attackForDamage`. Some later AdvC
wrappers are more defensive (`canNukeAt`, `bestInterceptor`, `canAirBombAt`, `canBuild`).

These are real public-Python API hardening inconsistencies, but the current shipped Python census
did not find a supported call that passes Python `None`, a NULL-internal wrapper, or an invalid
plot into one of the sharp functions. Calls to the only active changed movement wrapper use plots
constructed directly from `CyMap`. Per the album rule, do not spend F659 on hypothetical
third-party Python misuse or generic stale-wrapper lifetime behavior inherited across the Cy API.

OTHER CONTROLS
--------------
- Pointer-return wrappers continue the inherited Civ4 convention of allocating a Cy wrapper even
  when the wrapped game object can be NULL; Python is expected to use `isNone()`. Return-value
  policies in the binding file match that convention.
- `getBuildType` contains AdvC/K-Mod's corrected ternary cast; the old pointer-cast precedence oddity
  does not survive current code.
- Practical 1372's temporary `isFound -> canFound` wrapper change was explicitly reverted by
  practical 3056 when `CvUnit::canFound()` was removed; current behavior matches K-Mod again.
- Current SAS/Base AdvC equivalence means no unported upstream fix remains to search for in this file.
- Whole-album checks found numerous deeper `CvUnit` movement/loading roots, but none owns or is
  contradicted by a distinct current CyUnit-wrapper defect from this pass.

CLOSURE JUDGMENT
-----------------
Queue 032 is saturated for this audit pass. The historical signature changes reconcile, the
current shipped Python callers do not activate the remaining NULL-hardening edges, and the
mechanical wrong-method/enum-cast/binding sweeps found no independent current root. F659 remains
unused rather than being consumed by public-API robustness alone.

EXACT CONTINUATION
------------------
C031-WIP520 physical -> Queue032 CLOSED -> F659/KI#981 UNUSED -> files complete 64/169 ->
Queue033 `CvGameCoreDLL/CvInfo_Terrain.cpp` OPEN -> establish current SAS vs Base AdvC 1.14 /
K-Mod / Civ4CE provenance -> audit terrain/feature/improvement/bonus/route/world info readers,
array/map ownership and defaults, enum/index resolution, XML shape assumptions and any SAS
extensions -> current XML producer census + whole-album dedup before numbering -> either promote
a demonstrated F659 or close Queue033 cleanly -> freeze the next physical WIP at the next settled
boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP521 -- QUEUE 033 CvInfo_Terrain.cpp -- F659 CONFIRMED / QUEUE STILL OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP520.
- Queue 033 `CvGameCoreDLL/CvInfo_Terrain.cpp`: OPEN.
- F659 / provisional KI#981: CONFIRMED.
- F660 / provisional KI#982: UNUSED.
- Files complete remains 64 / 169 because Queue 033 is not yet closed.
- No gameplay/source file was modified.

QUEUE-033 BASELINE / CURRENT-DATA CONTROLS SO FAR
--------------------------------------------------
Current SAS `CvInfo_Terrain.cpp` is line-for-line content-identical to supplied Base AdvC 1.14
apart from file representation/line endings. Any Queue-033 root is therefore inherited AdvC
behavior, not a SAS-only regression or a missed newer Base-AdvC repair.

Current shipped XML census:
- 9 TerrainInfos
- 6 FeatureInfos
- 35 BonusInfos
- 2 RouteInfos
- 24 ImprovementInfos
- 12 GoodyInfos
- 92 TechInfos (for the technology-cache/dependency checks)

The first data-contract sweep is clean:
- all 6 FeatureInfo ArtDefineTags resolve;
- all 35 BonusInfo ArtDefineTags resolve;
- all 24 ImprovementInfo ArtDefineTags resolve;
- every ImprovementInfo explicitly supplies `bActsAsCity`;
- current ImprovementPillage / ImprovementUpgrade references that are present resolve;
- current TechYieldChanges / RouteYieldChanges keys are valid and non-duplicated;
- current BonusTypeStruct keys are valid and non-duplicated;
- all shipped terrain and feature FootstepSounds roots are present, so the questionable inherited
  NULL-array fallback of 0 does not activate;
- `CvFeatureInfo::getNumVarieties()` would be unsafe for a missing feature ArtInfo, but no current
  feature has a missing ArtDefine reference;
- the empty/missing RoutePillage value safely resolves to NO_ROUTE.

F659 / provisional KI#981 -- AdvC enum-macro global cleanup reordered dependent info destruction and leaks nested yield rows
----------------------------------------------------------------------------------------------------------------------------

ROOT
----
Two live info destructors require another still-loaded info-family count in order to free their
nested 2D arrays:

1. `CvImprovementInfo::~CvImprovementInfo()` frees:
       m_ppiTechYieldChanges[0 .. GC.getNumTechInfos()-1]
       m_ppiRouteYieldChanges[0 .. GC.getNumRouteInfos()-1]

2. `CvCivicInfo::~CvCivicInfo()` frees:
       m_ppiImprovementYieldChanges[0 .. GC.getNumImprovementInfos()-1]

K-Mod's inherited explicit `CvGlobals::deleteInfoArrays()` ordering satisfies those lifetime
dependencies:

    CivicInfo
      before ImprovementInfo
        before RouteInfo
        before TechInfo

In particular, the supplied K-Mod source deletes:
    m_paCivicInfo
    m_paImprovementInfo
    m_paRouteInfo
    ...
    m_paTechInfo

AdvC practical 1752 / ce3dc24000fc371696417c14c8306ff9d04707fb
("CvGlobals::getInfo functions added") replaced the hand-written cleanup order with:

    #define DELETE_INFO_ARRAY(Name, Dummy) deleteInfoArray(m_pa##Name##Info);
    DO_FOR_EACH_INFO_TYPE(DELETE_INFO_ARRAY);

That macro iterates the general info declaration order, not a destruction-dependency order.
Current order places Route before Improvement, then later Tech and Civic:

    Route ... Improvement ... Tech, Civic

`deleteInfoArray` deletes all objects and immediately calls `array.clear()`. Therefore:

- by the time `CvImprovementInfo::~CvImprovementInfo()` executes,
  `GC.getNumRouteInfos()` is already 0;
- by the time `CvCivicInfo::~CvCivicInfo()` executes,
  `GC.getNumImprovementInfos()` is already 0.

Both destructors still `SAFE_DELETE_ARRAY` their outer pointer array, but their row-deletion loops
execute zero times. The nested `new int[NUM_YIELD_TYPES]` rows are consequently leaked.

CURRENT SHIPPED ACTIVATION
--------------------------
This does not depend on optional XML content. Both readers allocate their complete 2D matrices
unconditionally through `CvXMLLoadUtility::Init2DIntList`:

- every one of the 24 current ImprovementInfos allocates 2 RouteInfo rows;
- every one of the 25 current CivicInfos allocates 24 ImprovementInfo rows;
- NUM_YIELD_TYPES is 3.

Thus one normal global cleanup skips:

    24 * 2  = 48 Improvement route-yield row allocations
    25 * 24 = 600 Civic improvement-yield row allocations

for 648 leaked `new int[3]` allocations in the current data set: 7,776 bytes of raw integer
payload, plus allocator bookkeeping/alignment overhead.

The Improvement tech-yield rows do NOT share this failure because current global cleanup still
destroys ImprovementInfo before TechInfo; `GC.getNumTechInfos()` therefore remains valid while
that first row loop executes.

ANCESTRY / ORACLE
-----------------
- Supplied K-Mod retains the explicit dependency-safe cleanup order.
- AdvC practical 1752 is the exact regression point: the same diff removes the explicit
  Civic -> Improvement -> Route -> Tech order and substitutes `DO_FOR_EACH_INFO_TYPE`.
- Current Base AdvC 1.14 retains the macro cleanup and the same dependent destructors.
- Current SAS inherits it unchanged.
- A mechanical scan of current destructors for `GC.getNum*Infos()` lifetime dependencies found
  exactly these two destructors, so the known nested-info cleanup impact is bounded rather than
  an open-ended guess.

CONSEQUENCE / SEVERITY BOUNDARY
-------------------------------
This is a real normal-cleanup memory-management defect, not gameplay-state corruption. With the
current data the raw leak is small (about 7.6 KiB plus allocator overhead), and DLL-local source
does not prove repeated same-process global cleanup/reload cycles. If cleanup occurs only at final
process/DLL teardown, the operating system will reclaim the memory shortly afterward.

The finding is therefore low gameplay severity but high source certainty: the intended
destructors are demonstrably prevented from freeing allocations that were made during ordinary
XML loading.

REPAIR DIRECTION
----------------
Do not use the general enum/info declaration order as a destruction order where destructors query
other info-family sizes.

At minimum preserve the dependency chain:
    CivicInfo before ImprovementInfo before RouteInfo,
while keeping ImprovementInfo before TechInfo.

A broader robust repair is to make nested arrays self-describing / store their allocation
dimensions so destructors do not depend on mutable global info-vector counts during teardown.
If retaining centralized deletion order, use an explicit cleanup-specific order (or explicit
special cases) rather than silently reusing `DO_FOR_EACH_INFO_TYPE`.

DEDUP / OWNERSHIP
-----------------
The earlier Queue-040 `CvGlobals.cpp` audit recorded unrelated cleanup/lifetime controls
(`CvAgents` ownership, XML-loader lifetime, info-map reset) but did not own this nested-array
destruction-order failure. Whole-album search found no existing finding for the
Civic/Improvement/Route teardown dependency.

Queue 033 exposed the defect through `CvImprovementInfo::~CvImprovementInfo`; the common root is
the AdvC practical-1752 global deletion-order refactor, with the pending Queue-067
`CvInfo_Civics.cpp` destructor as a same-root sibling. Keep the two leaks under one finding.

QUEUE-033 REMAINING CURSOR
--------------------------
Continue from F660 UNUSED:
- finish `CvBonusInfo::updateCache` against the actual current tech dependency graph and its two
  live consumers;
- finish route-prerequisite vectorization / Python fixed-count compatibility;
- finish feature river-side invariants and current placement-data constraints;
- finish improvement matrix allocation/default/pass-2 and bonus-struct ownership controls;
- finish GoodyInfo and remaining inherited K-Mod/Civ4CE semantic deltas;
- whole-album dedup and final mechanical pointer/index/divisor/history saturation;
- either promote a genuinely independent F660 or close Queue 033 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol during this audit.
===============================================================================


===============================================================================
C031-WIP522 -- QUEUE 033 CvInfo_Terrain.cpp CLOSED / QUEUE 034 OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP521.
- Queue 033 `CvGameCoreDLL/CvInfo_Terrain.cpp` (1,672 queued lines): CLOSED.
- Confirmed Queue-033 root: F659 / provisional KI#981.
- F660 / provisional KI#982: UNUSED.
- Files complete: 65 / 169.
- Queue 034 `CvGameCoreDLL/CvInfo_Civilization.cpp` (1,637 queued lines): OPEN.
- Gameplay/source files remain untouched.

QUEUE-033 FINAL RESULT
----------------------
Current SAS `CvInfo_Terrain.cpp` is line-for-line content-identical to supplied Base AdvC 1.14
apart from file representation/line endings. The queue therefore contained no SAS-only regression
and no missed Base-AdvC-1.14 repair.

F659 remains the sole confirmed root:
AdvC practical 1752 replaced K-Mod's explicit dependency-safe info destruction order with the
generic `DO_FOR_EACH_INFO_TYPE` order. Current cleanup deletes RouteInfo before ImprovementInfo
and ImprovementInfo before CivicInfo. `CvImprovementInfo::~CvImprovementInfo()` and
`CvCivicInfo::~CvCivicInfo()` obtain nested-array row counts from those already-cleared global
info families, so their row-deletion loops execute zero times. Current data skips 48
Improvement/Route rows plus 600 Civic/Improvement rows = 648 `new int[3]` allocations,
7,776 bytes raw integer payload plus allocator overhead per global cleanup. Keep both
manifestations under F659; do not create a later duplicate from Queue 067.

FINAL CURRENT-DATA / NEGATIVE CONTROLS
--------------------------------------
Info/data census used throughout the saturation:
- 9 TerrainInfos
- 6 FeatureInfos
- 35 BonusInfos
- 2 RouteInfos
- 24 ImprovementInfos
- 12 GoodyInfos
- 92 TechInfos
- 22 BuildInfos
- 93 UnitClassInfos

Cross-reference validation against the current light-source XML found no invalid current refs:
- Bonus TechReveal / TechCityTrade / TechObsolete, terrain and feature references;
- Feature terrain references;
- Route prereq-bonus, OR-bonus, movement-tech and RoutePillage references;
- Improvement ImprovementPillage / ImprovementUpgrade, terrain, feature, bonus, tech and route
  keys;
- Build tech/improvement links used by the bonus-improvement cache;
- Goody UnitClass / BarbarianClass references.
No duplicate current TechYieldChanges, RouteYieldChanges or BonusTypeStruct keys were found.

Art/audio/data controls:
- all 6 FeatureInfo ArtDefineTags resolve;
- all 35 BonusInfo ArtDefineTags resolve;
- all 24 ImprovementInfo ArtDefineTags resolve;
- every current TerrainInfo and FeatureInfo has a FootstepSounds root, so the inherited
  questionable NULL-array fallback of 0 is not current-live;
- `CvFeatureInfo::getNumVarieties()` would be unsafe if a Feature ArtInfo were absent, but no
  current feature activates that state;
- all 11 non-empty current GoodyInfo Sound values resolve in the current Audio2DScripts.xml;
  GOODY_TECH intentionally has an empty Sound.

FEATURE / RIVER-SIDE HISTORY CONTROL
------------------------------------
The apparent Oasis XML mismatch is intentional history, not a surviving bug:
- practical 2321 ("Allow Oases at river corners") introduced `bNoRiverSide` and temporarily
  changed Oasis from `bNoRiver=1` to `bNoRiver=0, bNoRiverSide=1`;
- practical 2335 explicitly says "Don't allow Oases on river corners after all" and reverted
  Oasis to `bNoRiver=1`;
- current SAS and Base AdvC retain that deliberate revert.
The reader's consistency rules remain coherent:
- `bNoRiver` implies `bNoRiverSide`;
- `bRequiresRiverSide` implies `bRequiresRiver`.
Current FLOOD_PLAINS supplies both requires-river flags; current Oasis intentionally forbids any
river contact.

FEATURE MOVE/EFFECT CONTROL
---------------------------
Current `OnUnitChangeTo` fields are empty. Current positive effect-probability feature entries are
Forest/Jungle and use the normal bird-scatter effect path. No current feature supplies a
non-empty invalid transition target.

ROUTE CONTROL
-------------
Current route topology:
- Road: RoutePillage empty -> NO_ROUTE.
- Railroad: RoutePillage -> ROAD.
- Railroad has two valid OR bonus prerequisites (Coal/Oil).
AdvC practical 2796's prereq-vector conversion is compatible with current data:
- native C++ can represent arbitrary vector length;
- the Python accessor explicitly bounds-checks and returns NO_BONUS outside the vector;
- current maximum is two, within the inherited Python fixed-count expectation.
RoutePillage's Air-Bomb downgrade behavior and sabotage's intentional full-route removal are
separate documented contracts, not a mismatch.

IMPROVEMENT CONTROL
-------------------
Every current improvement explicitly supplies `bActsAsCity`.
Current upgrade chain is acyclic:
    Cottage -> Hamlet -> Village -> Town
with the expected reverse pillage chain:
    Hamlet -> Cottage, Village -> Hamlet, Town -> Village.
`finalUpgrade()` also has an explicit loop detector for malformed/cyclic data.
The improvement bonus array is allocated on both reader branches, and current BonusTypeStructs
are valid/nonduplicate. Every current resource-improvement pairing treated as improveable by the
bonus-tech cache has at least one positive bonus yield, so the cache's `bAnyImprovYield` filter
does not omit a current trade-only/yield-zero resource path.

BONUS IMPROVEMENT-TECH CACHE CONTROL
------------------------------------
`CvBonusInfo::updateCache()` is invoked after BuildInfos and ImprovementInfos have loaded.
A dependency-graph check across the current 92 TechInfos found no current resource for which the
research-cost heuristic chooses a technologically ancestral/earlier prerequisite in place of a
later required build/reveal technology. The important water-Oil case resolves to Marine
Technology rather than stopping incorrectly at Combustion.
Its two live consumers (`CitySiteEvaluator` and the game bonus valuation path) therefore receive
coherent current cache values.

BONUS GENERATION NUMERIC CONTROL
--------------------------------
Current map-placed bonuses (placement order >= 0) have valid nonnegative placement parameters.
The two deliberately disabled pseudo-bonuses, BONUS_DRAMA and BONUS_MUSIC, use
`iPlacementOrder=-1`, so their all-zero random-appearance/tile/player settings never enter normal
bonus placement.

GOODY CONTROL
-------------
All current Goody numeric/reference combinations fit the live `CvPlayer::canReceiveGoody` /
`receiveGoody` contracts. In particular:
- hostile current goodies have `iMinBarbarians` > 0 before the probability-rescaling division;
- the good Scout goody's `BarbarianClass=UNITCLASS_WORKER` is AdvC's upgrade-class mechanism,
  not a hostile-spawn configuration;
- GOODY_MAP's `iBarbarianUnitProb=100` with `bBad=0` is explicitly documented in the current XML
  as AdvC's upgrade/additional-outcome probability;
- current Goody unit classes and audio scripts resolve.

LOWER-THRESHOLD ROBUSTNESS EDGES PRESERVED
------------------------------------------
Do not silently forget these, but they do not consume F660 under the current-live threshold:
- a malformed bad GoodyInfo with a non-NONE BarbarianClass but `iMinBarbarians=0` could reach a
  divide-by-zero in the hostile probability rescaling; no current bad goody has that shape;
- missing Feature ArtInfo would make `getNumVarieties()` unsafe; no current feature has one;
- the terrain/feature NULL footstep fallback of 0 is semantically questionable relative to the
  usual -1 "none" convention, but all current entries allocate the list;
- XML-file-cache serialization surfaces remain compiled out under the current
  `ENABLE_XML_FILE_CACHE=0` configuration.

HISTORY SATURATION
------------------
Functional history strata checked/reconciled include:
- 4037 route air-bomb/RoutePillage support;
- 3183 enum-map conversion;
- 3147 XML-info refactor;
- 2796 route prereq vectorization;
- 2335 / 2321 Oasis river-corner revert/introduction;
- 2307 bonus improvement-tech cache;
- 2236 / 2222 XML defaults;
- 2060 optional XML tags;
- 1932 global-warming preservation;
- 1732 XML/enum cleanup;
- 1644 split from CvInfos.cpp.
No independent current-live F660 survived the history or final mechanical pass.

NEXT EXACT CURSOR
-----------------
C031-WIP522 physical
-> 65 / 169 files complete
-> Queue 033 CLOSED
-> F659 / KI#981 confirmed
-> F660 / KI#982 UNUSED
-> Queue 034 `CvInfo_Civilization.cpp` OPEN
-> establish current SAS vs Base AdvC 1.14 semantic baseline
-> compare K-Mod/Civ4CE ancestry
-> audit CvCivilizationInfo / CvLeaderHeadInfo / CvTraitInfo / diplomacy-response ownership and
   XML default/pass-2 contracts
-> trace current XML activation and live consumers
-> whole-album dedup
-> promote F660 only for an independent supported root
-> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP523 -- QUEUE 034 CvInfo_Civilization.cpp CLOSED / QUEUE 035 OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP522.
- Queue 034 `CvGameCoreDLL/CvInfo_Civilization.cpp` (1,637 queued lines): CLOSED.
- No independent current-live Queue-034 C++ root promoted.
- F660 / provisional KI#982: UNUSED.
- Files complete: 66 / 169.
- Queue 035 `CvGameCoreDLL/CvInfo_GameOption.cpp` (1,619 queued lines): OPEN.
- No gameplay/source/XML file was modified.

QUEUE-034 BASELINE
------------------
Current SAS `CvInfo_Civilization.cpp` is executable-semantically identical to supplied Base
AdvC 1.14 except for SAS's optional per-era leader-art feature (plus comments/includes/file
representation). Therefore no missed Base-AdvC-1.14 repair exists in this queue; any other root
would have to be inherited from AdvC/K-Mod/Firaxis.

The per-era leader-art divergence itself is currently dormant:
- `SAS_CV_LEADER_HEAD_INFO_ENABLE_XML_ERA_ART_DEFS` is 0 in current configuration;
- current LeaderHead XML contains no configured `EraArtDefineTags`;
- the pointer array is allocated only when the feature is enabled;
- the SAS copy constructor correctly deep-copies its `CvString` elements rather than memcpying
  their internal representation;
- the known duplicate-leader/first-alive-player ambiguity remains the already-owned KI#120.
No new root was created from this feature.

CIVILIZATION DATA / MAPPING CENSUS
----------------------------------
Current `CIV4CivilizationInfos.xml` contains 38 civilizations.

The current mapping/reference census reconciles:
- all 38 civilizations provide exactly five `InitialCivics`;
- positional InitialCivics order matches current CivicOption order for every civilization:
      Government -> Legal -> Labor -> Economy -> Religion;
- CivilizationBuildings has no duplicate BuildingClass key within a civilization;
- CivilizationUnits has no duplicate UnitClass key within a civilization;
- deliberate Barbarian `NONE` targets are class-disabling mappings, not unresolved references;
- current FreeUnitClasses, FreeBuildingClasses, FreeTechs, DisableTechs and Leaders references
  resolve;
- current derivative-civilization, player-color, art-style and civilization-art references resolve;
- Barbarian/Minor civilization lack of UnitArtStyleType is an explicitly supported reader case;
- derivative-civilization cycles are used only as equality/preference data by the live split-empire
  path and are not recursively traversed.

BARBARIAN XML-COMMENT ITERATOR CONTROL
--------------------------------------
A late suspicion concerned current direct XML comments in the Barbarian `<Buildings>` and
`<Units>` blocks because `CvCivilizationInfo::read` uses the inherited:
    GetNumChildren -> SetToChild -> loop -> NextSibling
shape without a per-sibling `SkipToNextVal()`.

This is not a current skipped-mapping bug. Firaxis/Civ4CE/K-Mod use the same loop. The iteration
contract is internally paired: `GetNumChildren` and `NextSibling` traverse the sibling nodes,
while `GetChildXmlVal` descends into a mapping and skips to its first value. A comment sibling can
therefore consume a loop iteration without consuming a real mapping; it does not force the final
real mapping outside the counted loop. The old nearby wording implying that `SetToChild` itself
always selects a non-comment child is stale/misleading, but current Barbarian comments do not
demonstrate lost building/unit mappings.

LEADER DEFAULTS / OWNERSHIP CONTROL
-----------------------------------
AdvC's `CvLeaderHeadInfo(CvLeaderHeadInfo const&)` default-copy constructor remains structurally
brittle because it memcpy-copies the derived scalar/object region and then deep-copies pointer
arrays. That copied region includes the embedded `CvString m_szArtDefineTag`.

For current data this does not activate a heap-owning string alias:
- the only defaults object copied by the XML-default mechanism is `LEADER_DEFAULTS`;
- current `LEADER_DEFAULTS` explicitly has an empty ArtDefineTag;
- all pointer arrays are separately deep-copied;
- SAS's later per-era `CvString` array is separately copied element-by-element.
A mod-mod that gave the default object a non-empty heap-owning string would deserve renewed
lifetime testing, but current SAS does not supply that state. Keep as robustness debt, not F660.

LEADER ARRAY / REFERENCE CENSUS
-------------------------------
Current LeaderHead keyed-array data reconciles:
- ContactRands covers all 14 ContactTypes exactly once;
- ContactDelays covers all 14 ContactTypes exactly once;
- MemoryDecays covers all 37 current MemoryTypes exactly once, including AdvC-added
  MEMORY_INDEPENDENCE and MEMORY_DECLARED_WAR_RECENT;
- NoWarAttitudeProbs covers all five AttitudeTypes;
- omitted MemoryAttitudePercent entries are bookkeeping/recent-memory types whose intended
  direct attitude contribution is zero, matching the null/default getter;
- empty UnitAIWeightModifiers represents the intentional all-zero default;
- no duplicate keys were found in Traits, Flavors, ContactRands, ContactDelays, MemoryDecays,
  MemoryAttitudePercents, NoWarAttitudeProbs, UnitAIWeightModifiers,
  ImprovementWeightModifiers, or the four era-music maps;
- favorite civic/religion, traits, improvement weights, era keys and leader art definitions
  resolve in current data.

Zero ContactRand values are intentional "never contact" values and are handled by
`CvPlayerAI::AI_contactRoll`; they are not divisors on the ordinary call path.

The only nearby assertion typo:
    FAssert(m_piContactDelay == NULL ||
            m_piContactRand[CONTACT_PEACE_TREATY] >= 0);
appears to test ContactRand while discussing ContactDelay. It is debug-only and current values are
nonnegative; no runtime behavior root was promoted.

DORMANT PUBLIC-API DEFECT -- `CvCivilizationInfo::getNumLeaders()`
------------------------------------------------------------------
`m_iNumLeaders` is initialized to 0 and is not populated by the current XML reader. Its only
read/write handling sits in the compiled-out XML-file-cache serialization path. Therefore
`CvCivilizationInfo::getNumLeaders()` reports 0 for every current civilization.

Mechanical current-tree search found no C++ or shipped Python consumer of this accessor. The real
leader membership APIs use `isLeaders(...)` / the leader boolean map instead. This is a genuine
inherited public-API defect but dormant in the current shipped game, so it does not consume F660
under the album's current-live threshold.

TRAITINFO CONTROL
-----------------
Current TraitInfo census contains 10 traits. It reconciles:
- all free-promotion and UnitCombat keys resolve;
- no duplicate keyed entries were found;
- fixed-order yield/commerce arrays fit current XML lengths;
- Creative's shorter CommerceChanges intentionally leaves the later commerce slot(s) at zero;
- Financial is the only current nonzero natural-yield-threshold case and satisfies the reader's
  nonnegative invariant;
- current scalar/default values match the loader's zero/default semantics.
No TraitInfo root survived.

DIPLOMACY RESPONSE CONTROL
--------------------------
Current `CIV4DiplomacyInfos.xml` contains:
- 110 DiplomacyInfos;
- 560 responses;
- every response has at least one diplomacy text.

All current response Civilization/Leader/Attitude/Power restriction references resolve.
The active `CvDiplomacyResponse` getters correctly test the pointer before indexing, unlike the
real-but-obsolete reversed NULL check discovered in old `CvDiplomacyTextInfo` during Queue 030.
Current `<Responses>` blocks also do not contain direct sibling comments that would stress the
response-count traversal. No live diplomacy-response root survived.

SEPARATE ACTIONABLE SAS DATA REGRESSION -- STALIN RENAISSANCE PEACE INTRO
-------------------------------------------------------------------------
The Queue-034 reference census exposed one current invalid leader audio reference, but its root is
XML/data rather than `CvInfo_Civilization.cpp`, so it is deliberately NOT numbered F660 here.

Current `Assets/XML/Civilizations/CIV4LeaderHeadInfos.xml` has, for LEADER_STALIN's Renaissance
`DiplomacyIntroMusicPeace`:
    AS2D_DIPLO_STALIN_EARLY_INTROO

Current `Audio2DScripts.xml` defines:
    AS2D_DIPLO_STALIN_EARLY_INTRO

All other Stalin era entries use the correct spelling.

History localizes the typo to SAS practical/commit-count 4880:
    aa5518de413426df04a854c2e9f6436328958d79
which introduced the individual Stalin music mappings and the correctly-spelled Audio2DScript in
the same change, but gave the Renaissance peace-intro mapping the extra trailing `O`.

Live consequence:
- `CvLeaderHeadInfo` resolves non-empty diplomacy-audio tags through the audio-tag lookup;
- `CvPlayer::getIntroMusicScriptId` selects the leader's era-indexed peace-intro script for
  peaceful diplomacy;
- when the viewing player is in the Renaissance, Stalin therefore selects the invalid/missing
  `...INTROO` mapping instead of his intended early intro.
The narrow repair is data-only: remove the extra trailing `O`.

Whole-album search found no previous owner for this exact Stalin intro typo. Preserve it as a
separate actionable SAS data regression for the appropriate XML/data fix or documentation pass;
do not misattribute it to Queue 034 C++.

FINAL OWNERSHIP / ARITHMETIC / INDEX SATURATION
-----------------------------------------------
The final whole-file pass found:
- no live arithmetic/divisor hazard in this file;
- no destructor that uses another info-family's mutable global count during global teardown
  (unlike F659);
- flat/pointer arrays are freed by their owning classes;
- the live diplomacy response vector deletes its owned response objects;
- current indices passed to exposed getters are bounded/asserted according to their family;
- XML-file-cache read/write-only concerns remain disabled under current
  `ENABLE_XML_FILE_CACHE=0`.

HISTORY SATURATION
------------------
Functional strata checked/reconciled include at least:
- practical 4176 misc cleanup/comments;
- 3579/3544/3538 later LeaderHead/victory-weight/serialization work;
- 3432 info-base typedef/refactor;
- 3392/3220/3183 XML/enum-map reader evolution;
- 3148 Creative commerce data;
- 3060 Financial natural-yield threshold;
- 3050 cache compile fixes;
- 2998/2885/2868 leader/favorite/global-enum refactors;
- 2545 XML-file-cache macro rename;
- 2287/2286 recent-war-memory data;
- 2246 LoveOfPeace;
- 2237/2236/2223/2222 LEADER_DEFAULTS/default inheritance;
- 2066/2047/2011 and earlier XML/refactor strata;
- 1918/1842/1720/1644 inherited civilization/leader split and AI-threshold changes;
- SAS 5683-5687 per-era leader-art feature and 5856 follow-up comment.
No independent current-live F660 survived.

NEXT EXACT CURSOR
-----------------
C031-WIP523 physical
-> 66 / 169 files complete
-> Queue 034 CLOSED
-> F660 / KI#982 UNUSED
-> separate unnumbered actionable SAS data regression preserved:
   Stalin Renaissance `AS2D_DIPLO_STALIN_EARLY_INTROO`
-> Queue 035 `CvInfo_GameOption.cpp` OPEN
-> establish SAS vs Base AdvC 1.14 executable-semantic baseline
-> compare K-Mod/Civ4CE ancestry
-> inventory all info classes/readers and current XML cardinalities
-> audit fixed-order arrays, numeric/divisor contracts, ownership and default/pass-2 behavior
-> trace current gameplay/Python consumers
-> whole-album dedup
-> promote F660 only for an independent supported root
-> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP524 -- QUEUE 035 CvInfo_GameOption.cpp CLOSED / QUEUE 038 OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP523.
- Queue 035 `CvGameCoreDLL/CvInfo_GameOption.cpp` (1,619 queued lines): CLOSED.
- No independent current-live Queue-035 root promoted.
- F660 / provisional KI#982: UNUSED.
- Files complete: 67 / 169.
- Queue 036 `CvMap.cpp` and Queue 037 `CvPythonCaller.cpp` were already completed earlier.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- No gameplay/source/XML file was modified.

QUEUE-035 BASELINE
------------------
Current SAS `CvInfo_GameOption.cpp` is executable-semantically identical to supplied Base
AdvC 1.14 except for two SAS additions:
1. the `CvEraInfo` soundtrack script-name mirror used by the Sevopedia Music era chart;
2. the `CvGameSpeedInfo::m_iCulturePercent` reader/getter exposed for the Sevopedia GameSpeed
   chart.
The later Play Now -> Simple Game edit is comment/terminology-only in this file.
Therefore there is no missed newer Base-AdvC repair in Queue 035.

SAS ERA SOUNDTRACK-NAME MIRROR
------------------------------
The SAS music addition has paired ownership:
- `m_paszSoundtracks` is initialized NULL;
- allocated as `new CvString[m_iNumSoundtracks]` whenever the inherited soundtrack ID array is
  allocated;
- populated from the same `pszSoundTrackNames` source list as the inherited audio-tag lookup;
- released with `SAFE_DELETE_ARRAY` in `CvEraInfo::~CvEraInfo`;
- `getSoundtrackScriptName(i)` asserts the same `getNumSoundtracks()` bound and returns an empty
  string if the optional mirror pointer is absent.

Current seven-era soundtrack counts are:
    Ancient 4
    Classical 19
    Medieval 20
    Renaissance 19
    Industrial 16
    Modern 14
    Future 11

Every current era soundtrack `AS2D_*` script name resolves in the supplied
`Audio2DScripts.xml`. The addition is observational/Sevopedia-facing and does not change the
engine-selected integer audio IDs.

The inherited local guard `if (m_iNumSoundtracks > 0)` assumes the string-list pointer was
successfully produced when the count is positive. Current valid XML satisfies that contract, so
no current failure is activated.

GAMESPEED CULTURE-PERCENT MIRROR
--------------------------------
The SAS GameSpeed addition reads the existing `iCulturePercent` XML field into a dedicated member
and exposes it to the Sevopedia chart. Current values are:
    33, 50, 67, 100, 150, 200, 300, 400
across the eight current game speeds.
The current XML itself documents this field as ordinarily unused for gameplay because culture
thresholds are handled by `CvCultureLevelInfo`; this addition therefore exposes data rather than
introducing a new gameplay scaling path.

GAMESPEED TIMELINE / ARRAY CONTROL
----------------------------------
Current `CIV4GameSpeedInfo.xml` contains eight GameSpeedInfos. Every GameTurnInfo row has:
- positive `iMonthIncrement`;
- positive `iTurnsPerIncrement`.

The eight timelines total respectively:
    165, 250, 330, 500, 750, 1000, 1500, 2000 turns
and each spans the same 625,260 months from the shared start to the current designed end date.

Direct XML comments inside the GameTurnInfos blocks do not create a current count/index defect.
The inherited Firaxis XML iterator contract used here pairs `GetNumChildren`, `SetToChild` and
`NextSibling`; the shipped timelines load and are consumed through their real GameTurnInfo count.
This follows the same comment/sibling control reconciled in Queue 034.

`m_pGameTurnInfo` has straightforward allocation/destruction ownership and indexed access is
bounded by `getNumTurnIncrements()`.

ERA / AI-AGE CONTROL
--------------------
Current `CIV4EraInfos.xml` contains seven eras.
No explicit `AIEraFactor` is supplied, so `CvEraInfo::allInfosRead()` deliberately assigns:
    0, 100, 200, 300, 400, 500, 600
through its `100 * eraIndex` default path. This is strictly increasing and satisfies the current
custom-era AI contract.

Each AI-age marker has a single current owner:
- AIAgeOfPestilence: Medieval
- AIAgeOfExploration: Renaissance
- AIAgeOfPollution: Industrial
- AIAgeOfFertility: Industrial
- AIAgeOfGuns: Industrial
- AIAgeOfProduction: Industrial
- AIAtomicAge: Modern

Current Unit Victory/Defeat `AS2D_*` era references resolve in Audio2DScripts.
The corresponding `ASSS_*` city-soundscape definitions are not present in the supplied light
source material available to this queue, so those string-to-soundscape targets were deliberately
left as an explicit evidence limitation rather than falsely claimed validated. Current source
shows no symptom making that limitation a finding candidate.

GAMEOPTION CONTROL
------------------
Current `CIV4GameOptionInfos.xml` contains 28 GameOptionInfos and matches current
`GameOptionTypes` order, including EXE-sensitive positions.
Current default/visibility values reconcile. Examples already important elsewhere in the album:
- `GAMEOPTION_NO_EVENTS` defaults enabled;
- `GAMEOPTION_NO_CHANGING_WAR_PEACE` and `GAMEOPTION_NO_ESPIONAGE` are hidden.
Dynamic visibility mutation/reset behavior was audited in Queue 026; no duplicate root exists here.

HANDICAP CONTROL
----------------
Current `CIV4HandicapInfo.xml` contains 11 HandicapInfos.
Current `Goodies` lists:
- contain only valid current GoodyInfo types;
- intentionally repeat Goody types to weight random outcome selection; repeated list elements are
  not keyed-map duplicates.

Current `FreeTechs` and `AIFreeTechs` roots are empty for all 11 handicaps. The corresponding
92-TechInfo bool arrays therefore remain the intended all-false state and no unresolved tech key
exists in the current handicap data.

The high-risk positive/divisor-like current fields are valid:
- human `iResearchPercent`, build-time, base-growth-threshold, GP-threshold, culture-level,
  train and construct percentages are positive;
- AI research/growth/train/construct percentages are positive at every handicap;
- current research-related values satisfy the source assertions.

Current `iDifficulty` values are the deliberately non-enum-index scores already audited by the
later SAS difficulty fixes; do not rediscover those consumer-side roots here.

VICTORY / WORLD / SEA-LEVEL / OTHER INFO CONTROLS
-------------------------------------------------
Current data reconciles:
- seven VictoryInfos, including culture/land/religion/delay fields and current CityCulture refs;
- eleven WorldInfos with positive research/trade/maintenance scaling and current extended
  SAS world sizes;
- SeaLevel `iResearchPercent` values satisfy the positive contract;
- the Custom Game sea-level recommendation behavior is inherited AdvC UI policy already
  reconciled against Simple Game/slot-state handling;
- Climate/TurnTimer/PlayerOption/GraphicOption readers have no independent current-live
  ownership/index/divisor root.

Current shipped VictoryInfo `iTotalCultureRatio` behavior was already examined under the
CvPlayer queue and does not create a new Queue-035 root.

OWNERSHIP / SERIALIZATION
-------------------------
`CvEraInfo`, `CvGameSpeedInfo` and `CvHandicapInfo` release the arrays they allocate.
No destructor in this file relies on an already-cleared external info-family count, so the F659
global-destruction-order root has no sibling here.

The old `CvHandicapInfo` XML file-cache stream version machinery is compiled out under current
`ENABLE_XML_FILE_CACHE=0`. Historical stream compatibility concerns therefore remain inactive in
the current build and do not consume F660.

HISTORY SATURATION
------------------
History strata touching this file were reviewed, including:
- 1644 file split;
- 1752 global-info accessor refactor;
- 1967 CvWorldInfo getter inlining;
- 2026 ground-breaking-research era XML option;
- 2098 positive research/AI percentage assertions following city-trade work;
- 2520/2561 sea-level recommendation display;
- 2525 Golden-Age scaling assertion;
- 2571 Cppcheck cleanup;
- 2719 custom-era AI support;
- 2998 merge replay of that custom-era work;
- 3147 CvXMLInfo simple-element refactor;
- 3183 enum-map/XML-cache rewrite;
- 3299 difficulty-based foreign-culture work;
- 4308 AI Age of Production;
- SAS 5300 Sevopedia GameSpeed chart exposure;
- SAS 5359 Sevopedia Music soundtrack-name mirror;
- SAS 5439 Simple Game terminology.
No independent current-live F660 survived.

WHOLE-ALBUM DEDUP / CLOSURE
---------------------------
Existing album owners already cover nearby consumer-side issues involving:
- handicap difficulty mapping;
- GameSpeed event/trade/revolt/religion scaling;
- `ExtraFreeOutsideUnits`;
- world-size enum/order and 48-slot compatibility;
- victory/team culture arithmetic;
- sea-level/setup state where applicable.

Queue 035 is therefore CLOSED with F660 unused rather than manufacturing a parser/data root.

NEXT EXACT CURSOR
-----------------
C031-WIP524 physical
-> 67 / 169 files complete
-> Queue 035 CLOSED
-> F660 / KI#982 UNUSED
-> Queue 036 and Queue 037 already complete
-> Queue 038 `RiseFall.cpp` OPEN
-> establish current SAS vs Base AdvC 1.14 semantic baseline
-> compare K-Mod/Civ4CE ancestry where the Rise/Fall subsystem has an oracle
-> inventory chapter/state transitions, retired/civ-selection logic, score and defeat/revival
   ownership, serialization, era/turn arithmetic, player/team identity and map visibility
-> trace current options/config activation
-> whole-album dedup
-> promote F660 only for an independent supported root
-> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP525 -- QUEUE 038 RiseFall.cpp -- F660 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP524.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / provisional KI#982: CONFIRMED.
- F661 / provisional KI#983: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source file was modified.

QUEUE-038 PROVENANCE BASELINE
-----------------------------
Current SAS `RiseFall.cpp` is executable-semantically identical to supplied Base AdvC 1.14
except for already-reconciled SAS fixes:
- `setPlayerHandicap` refreshes cached founding-unit extra costs (the fix for F448 / KI#771);
- two autoplay-stop sites now pass explicit `SAS_AUTOPLAY_END_RISE_FALL` (KI#203-era
  autoplay-end-cause plumbing);
- `abandonPlans` guards a selection group's NULL plot before terrain inspection (SAS practical
  5383 crash fix).
Other observed differences are formatting/comments.

Therefore any additional Queue-038 root must be inherited AdvC behavior rather than a missed
Base-AdvC-1.14 repair or a fresh SAS regression.

F660 / provisional KI#982 -- Rise/Fall save load frees ReadWideString array with scalar delete
-----------------------------------------------------------------------------------------------

ROOT
----
`RiseFall::read(FDataStreamBase* pStream)` restores the original human leader name like this:

    wchar* tmp = pStream->ReadWideString();
    originalName = new CvWString(tmp);
    delete tmp;

`FDataStreamBase.h` explicitly documents `ReadWideString()` as:

    virtual wchar* ReadWideString() = 0; // allocates memory

The returned value is an allocated character ARRAY, not a scalar `wchar`. The matching
deallocation expression must therefore be `delete[] tmp`, not scalar `delete tmp`.

INTERNAL FIRAXIS OWNERSHIP ORACLE
---------------------------------
This does not rely on guessing an EXE implementation detail. Firaxis's own
`FVariableSystem.inl` uses the same API:

    m_wszValue = pStream->ReadWideString();

and `FVariable::~FVariable()` releases that storage with:

    delete [] m_wszValue;

The inherited stream contract therefore directly establishes array ownership for the
`ReadWideString()` return.

CURRENT NORMAL ACTIVATION
-------------------------
The bad branch is ordinary Rise/Fall save-load behavior, not optional malformed data.

During `RiseFall::init()`:
- `originalName` is always initialized from the first human-controlled chapter's leader name:
      originalName = new CvWString(
          GC.getInitCore().getLeaderName(chapters[0]->getCiv()));
- Rise/Fall then immediately performs an initial autosave via `g.autoSave(true)`.

`RiseFall::write()` writes:
    bool origNameGiven = (originalName != NULL);
and serializes the wide string whenever that bool is true.

Because initialized Rise/Fall normally has a non-NULL `originalName`, an ordinary saved
Rise/Fall game contains this field. Loading such a save reaches the
`ReadWideString()` -> scalar `delete` mismatch.

The copy into the separately allocated `CvWString` occurs before the wrong delete, so this finding
is specifically invalid deallocation / allocator corruption risk rather than a dangling
`originalName` pointer.

ANCESTRY / INTRODUCTION
-----------------------
AdvC practical 1268 / e11525d303... ("Rise & Fall" introduction) added the subsystem and this
exact read/write code in the same initial diff:

    wchar* tmp = pStream->ReadWideString();
    originalName = new CvWString(tmp);
    delete tmp;

Current Base AdvC 1.14 still retains the scalar delete unchanged.
K-Mod and Civ4CE do not contain RiseFall.cpp, so their useful oracle here is the inherited
FDataStream/FVariable ownership contract rather than a Rise/Fall implementation.

SEVERITY BOUNDARY
-----------------
This is a C++ mismatched delete-expression and therefore undefined behavior. The exact visible
manifestation depends on the allocator/build. It may appear harmless for a small wide string on a
particular release CRT, but debug/custom allocators can distinguish array/scalar deallocation and
allocator metadata can be corrupted.

Do NOT overclaim a deterministic crash: the source certainty is the incorrect deallocation form
on a normal current save-load path.

DEDUP
-----
- F657 / KI#979 is another new[]/scalar-delete defect, but it is independently rooted in
  `CvXMLLoadUtility::SetVariableListPerCommerce` and executes during XML startup.
- F660 is the separate Rise/Fall save-stream ownership error introduced in practical 1268.
- Whole-album search found no prior owner for `ReadWideString`, `originalName`, or this
  RiseFall::read deallocation.

NARROW REPAIR
-------------
Replace:
    delete tmp;
with:
    delete[] tmp;

An even cleaner modernization would read directly into a `std::wstring` / `CvWString` through a
non-owning-buffer stream overload if the surrounding API allows it, but the surgical inherited
repair is simply the correct array delete.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F661 UNUSED:
- finish `reportElimination` / `nextCivAlive` no-player and game-over ordering proof;
- audit `mostRecentlyFinished` and `prepareForExtendedGame` unsigned reverse loops, separating
  current preconditions from dormant API robustness;
- audit chapter/interlude turn arithmetic, score/retirement state and save compatibility;
- audit active-player/net-ID/human-control transitions, civ-selection popup paths and extended
  game transfer;
- audit off-limits/vassal state, diplomacy cooperation restrictions and message/popup gates;
- audit trade helpers against already-owned Rise/Fall diplomacy findings;
- compare practicals 1268-1430, 2239, 2253, 3027, 3280, 3281, 3297, 4186 and SAS fixes;
- whole-album dedup and final mechanical pointer/index/divisor/state saturation;
- either promote a genuinely independent F661 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP526 -- QUEUE 038 RiseFall.cpp -- F661 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP525.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / provisional KI#982: CONFIRMED (Rise/Fall ReadWideString scalar delete).
- F661 / provisional KI#983: CONFIRMED.
- F662 / provisional KI#984: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source file was modified.

F661 / provisional KI#983 -- Rise/Fall defeat path dereferences NO_PLAYER when no major civilization survives
--------------------------------------------------------------------------------------------------------------

ROOT
----
`RiseFall::reportElimination(PlayerTypes civId)` handles defeat of the civilization currently
assigned to the active Rise/Fall chapter. After scoring and relinquishing player control it does:

    CvPlayer& nextAlive = GET_PLAYER(nextCivAlive(BARBARIAN_PLAYER));

`nextCivAlive(startExcluded)` searches cyclically for an alive player but deliberately excludes
`startExcluded`. When called with `BARBARIAN_PLAYER`, it therefore searches only for another
non-Barbarian alive player. If no such player exists, it returns `NO_PLAYER`.

`reportElimination` does not check that result before passing it to `GET_PLAYER`.

Current `GET_PLAYER` resolves to an inline `CvPlayer`/`CvPlayerAI` accessor that:
- asserts the enum is in `[0, MAX_PLAYERS)`;
- then indexes the player array directly with that value.

`NO_PLAYER == -1` therefore violates the accessor contract and becomes an out-of-bounds player
array access in release semantics.

MINIMAL SUPPORTED CURRENT FIXTURE
---------------------------------
This does not require malformed setup or an unsupported zero-civilization game.

`RiseFall::init()` explicitly supports exactly one living major civilization:

    int civs = g.countCivPlayersAlive();
    ...
    maxChapters = std::min(civs, maxChapters);
    if(maxChapters < 1) {
        shutOff(...);
        return;
    }

For `civs == 1`, `maxChapters` becomes 1 and initialization proceeds normally. The sole
civilization owns the sole/final chapter.

If that civilization is eliminated during play (for example its last city/unit is lost to
Barbarians), `CvPlayer::setAlive(false)`:
1. sets `m_bAlive = false`;
2. performs the normal elimination cleanup;
3. calls `RiseFall::reportElimination(getID())` immediately when the Rise/Fall option is enabled.

At that instant there is no other major civilization alive. The Barbarian player is intentionally
excluded by the `nextCivAlive(BARBARIAN_PLAYER)` search, so the search reaches its sentinel case
and returns `NO_PLAYER`. `GET_PLAYER(NO_PLAYER)` follows immediately.

The ordinary later "no human alive -> GAMESTATE_OVER" fallback is specifically disabled for
Rise/Fall and, in any event, occurs in the game update loop after this synchronous setAlive /
reportElimination call. It cannot protect this dereference.

The same failure generalizes to any multi-civ Rise/Fall game in which the current chapter's
civilization is the last surviving major civilization.

ANCESTRY / EXACT REGRESSION POINT
---------------------------------
The original Rise/Fall implementation in practical 1268 used:

    CvPlayer& nextAlive = GET_PLAYER(nextCivAlive(civId));

When a defeated civ was the last major civilization, this search could reach the still-alive
Barbarian player, which was itself undesirable because the code could then attempt to make the
Barbarian slot the temporary human/defeat-popup recipient.

AdvC practical 2239 / 23c40cee78... ("R&F: Fix player defeat popup") changed that line to:

    CvPlayer& nextAlive = GET_PLAYER(nextCivAlive(BARBARIAN_PLAYER));

The intent is sound: never choose the Barbarian player as the successor. But this introduced a
new required outcome -- `NO_PLAYER` when no major successor exists -- without adding a guard
before `GET_PLAYER`.

Current Base AdvC 1.14 retains the practical-2239 form unchanged. Current SAS inherits it.

CONSEQUENCE / BRANCHES
----------------------
The invalid reference is constructed before either subsequent branch:
- non-final chapter: setActivePlayer / setIsHuman / popup delivery;
- final chapter: setPlayerControl(nextAlive.getID(), true) then GAMESTATE_OVER.

Therefore the final-chapter "end game directly when defeated" intent does not rescue the minimal
one-civ fixture; the code must first survive construction of `nextAlive`, which it cannot do
validly when the search returns `NO_PLAYER`.

DEBUG builds should trip the bounds assertion. Release builds can index the player pointer array
with -1 and then dereference the resulting invalid pointer; exact symptoms depend on adjacent
memory and allocator/layout, so do not overstate a deterministic crash shape beyond invalid/OOB
player access.

NARROW REPAIR DIRECTION
-----------------------
Resolve the successor ID first:

    PlayerTypes eNextAlive = nextCivAlive(BARBARIAN_PLAYER);

and handle `eNextAlive == NO_PLAYER` before any `GET_PLAYER` call.

For a final/no-successor defeat, the natural behavior is to restore any needed original setup
state without requiring another player object and end the game cleanly.

For a non-final chapter with no surviving eligible major civilization, the subsystem needs an
explicit policy rather than fabricating a successor: most naturally end the Rise/Fall run/game
because no civilization remains to take over, or otherwise route through a deliberately supported
revival/extended-game mechanism if one exists. Do not fall back to the Barbarian player.

DEDUP
-----
Whole-album search found no prior owner for:
- `RiseFall::reportElimination` returning/using NO_PLAYER;
- `nextCivAlive(BARBARIAN_PLAYER)` with no surviving major civ;
- the practical-2239 defeat-popup regression.

The album's earlier popup-dispatch audit only validated RF_DEFEAT/RF_CHOOSECIV/RF_RETIRE callback
routing. It does not own this producer-side successor-selection failure.

F660 is independent save-stream deallocation UB. F448 is the previously fixed cached-settler-cost
handoff defect. F649 concerns popup suppression corrupting city-built statistics. None duplicate
F661.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F662 UNUSED:
- audit the other `nextCivAlive` callers for NO_PLAYER assumptions, especially civ-selection and
  extended-game paths;
- settle unsigned reverse loops in `mostRecentlyFinished` and `prepareForExtendedGame`;
- audit chapter/interlude arithmetic, retirement and score state;
- audit active-player/net-ID/human-control transitions and popup selection paths;
- audit serialization version/state invariants beyond F660;
- audit off-limits/vassal state and diplomacy restriction recursion;
- dedup trade helpers against already-owned Rise/Fall trade findings;
- finish practical-history and mechanical pointer/index/divisor/state saturation;
- either promote an independent F662 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP527 -- QUEUE 038 RiseFall.cpp -- F662 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP526.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / KI#982: CONFIRMED.
- F661 / KI#983: CONFIRMED.
- F662 / provisional KI#984: CONFIRMED.
- F663 / provisional KI#985: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source file was modified.

F662 / provisional KI#984 -- Rise/Fall civ recommendation comparator discards victory-stage ordering
-----------------------------------------------------------------------------------------------------

ROOT
----
`RiseFall::eligibleByRecommendation()` sorts eligible civilizations through the static comparator
`RiseFall::byRecommendation(PlayerTypes one, PlayerTypes two)`.

After capitulation/vassal criteria, current code compares the candidates' victory stages:

    int vs1 = victoryStage(one);
    int vs2 = victoryStage(two);
    if(vs1 > vs2)
        return false;
    if(vs2 > vs1)
        return false;

The second result has the wrong polarity. When `two` is closer to victory than `one`, `one` should
sort before `two`, i.e. the comparator should return `true`.

The intended direction is internally obvious from both the criterion meaning and the exact
surrounding comparator pattern. Immediately below:

    int sc1 = g.getPlayerScore(one);
    int sc2 = g.getPlayerScore(two);
    if(sc1 > sc2)
        return false;
    if(sc2 > sc1)
        return true;

Likewise the vassal/capitulation pairs return false when `one` is less desirable and true when
`two` is less desirable.

Therefore the narrow correction is:
    if(vs2 > vs1)
        return true;

LIVE CONSEQUENCE
----------------
This is not a dormant helper:
- `eligibleByRecommendation()` is used to build the actual Rise/Fall civilization-selection list;
- it separately sorts never-played and repeat candidates with `std::sort(..., byRecommendation)`;
- `launchCivSelectionPopup()` presents that resulting order to the player, numbering candidates
  from most to least recommended;
- `knownName(..., nameNumber=true)` independently reconstructs the same list so the unknown-civ
  recommendation numbers remain consistent;
- the popup/help text explicitly displays each candidate's victory stage.

When two candidates have different victory stages:
- `byRecommendation(one,two)` returns false for one direction;
- `byRecommendation(two,one)` also returns false for the reverse direction.
They are therefore treated as equivalent for sorting despite the explicitly added victory-stage
criterion. The comparator never reaches score or player-rank tie-breaking for that pair.

The practical order then depends on the incoming player-slot order / sort implementation rather
than the intended preference for the civilization farther from victory. This can put a civ close
to winning ahead of a safer takeover candidate and undermines the recommendation numbering.

ANCESTRY / EXACT INTRODUCTION
-----------------------------
The original practical-1268 Rise/Fall comparator did not use victory stage; it ranked by:
- capitulation;
- vassal status;
- score;
- player rank.

AdvC practical 1270 / beb230a3bafeecd05c7efba87b45b6bd1615e121 added:
- `victoryStage(...)`;
- the victory-stage line in civ-selection help;
- the two new comparator branches.

The typo (`return false` in both unequal directions) is present in the introduction diff itself.
Current Base AdvC 1.14 retains it unchanged. Current SAS inherits it unchanged.

WHY THIS IS NOT A DELIBERATE EQUIVALENCE CLASS
----------------------------------------------
The new criterion was inserted *before* score/rank and practical 1270 simultaneously made
victory stage visible in the civ-selection information. A deliberate "different stages are
equivalent" rule would make the stage comparator redundant and would also suppress the lower
score/rank criteria whenever stages differ.

The mirrored score/vassal branches immediately around it establish the intended strict direction:
the second unequal case must return true.

SEVERITY BOUNDARY
-----------------
This is a player-facing recommendation/order bug, not game-state corruption. The player can still
choose any offered civilization. Its importance is strategic/UI: Rise/Fall deliberately tries to
steer the player away from already-advantaged/victory-threatening civs, and this typo drops one of
the explicit ranking dimensions whenever it matters.

DEDUP
-----
Whole-album search found no prior owner for:
- `byRecommendation`;
- the victory-stage comparator polarity;
- Rise/Fall civilization recommendation ordering by victory stage.

Existing Rise/Fall trade/victory-stage findings concern other consumers/rules and do not own this
sort comparator.

NARROW REPAIR
-------------
Change only:
    if(vs2 > vs1)
        return false;
to:
    if(vs2 > vs1)
        return true;

No comparator redesign is needed.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F663 UNUSED:
- finish all other `nextCivAlive` callers and broaden F661 only where they share the exact
  no-successor root;
- settle `mostRecentlyFinished` and `prepareForExtendedGame` reverse-loop preconditions;
- audit civ-selection empty-list/retire-all handling and selected-civ state;
- audit chapter/interlude/retire/scoring arithmetic and serialization state;
- audit active-player/net-ID/human-control transitions;
- audit victory/extended-game transfer and off-limits/vassal restrictions;
- dedup trade helper behavior against existing findings;
- finish practical-history and mechanical pointer/index/divisor/state saturation;
- either promote an independent F663 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP528 -- QUEUE 038 RiseFall.cpp -- F663 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP527.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / KI#982: CONFIRMED.
- F661 / KI#983: CONFIRMED.
- F662 / KI#984: CONFIRMED.
- F663 / provisional KI#985: CONFIRMED.
- F664 / provisional KI#986: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source file was modified.

F663 / provisional KI#985 -- selected civilization eliminated before takeover is scored as a player defeat instead of retrying selection
-------------------------------------------------------------------------------------------------------------------------------

INTENDED CONTRACT VISIBLE IN CURRENT SOURCE
-------------------------------------------
`RiseFall::atTurnEnd()` contains an explicit branch and comment for the case where the civilization
selected for the next chapter dies before the player actually takes control:

    else if(!currentCh.isScored()) { /* If scored, then defeat happened
        after starting the chapter, and normal civ selection is coming up.
        Otherwise, nextCiv has died in between selection and chapter start. */
        retryingCivSelection = true;
        haltForCivSelection(nextCivAlive(nextCivId));
    }

The intended distinction is therefore source-explicit:
- elimination after chapter start -> score/defeat flow;
- elimination after selection but before chapter start -> retry civilization selection.

Current control flow does not preserve that distinction.

LIVE SELECTION / TAKEOVER SEQUENCE
----------------------------------
At the scheduled start of a later chapter:
1. `RiseFall::atGameTurnStart()` sees `currentCh.getCiv() == NO_PLAYER`.
2. It calls `haltForCivSelection(...)` and temporarily makes an alive civilization human solely
   so the RF_CHOOSECIV popup can be delivered.
3. Clicking a civilization calls `handleCivSelection(selectedCiv)`, which immediately stores:
       chapters[pos]->setCiv(selectedCiv);
4. If the selected civilization is not the temporary popup-host civilization, `afterCivSelection`
   returns that temporary host to AI control. The selected civilization remains AI-controlled
   until normal turn order reaches it.
5. `atTurnEnd()` is supposed to hand human control to the selected civilization immediately before
   its turn and call `RFChapter::start()`.

There is therefore a real interval after chapter-civ assignment but before `RFChapter::start()`.

A selected civilization in a later player slot can be eliminated during that interval by an
earlier-turn civilization. This requires no malformed state: the selected AI is alive when offered
and selected, then loses its final city/unit during ordinary AI turn processing before its turn.

ACTUAL ELIMINATION PATH
-----------------------
`CvPlayer::setAlive(false)` synchronously calls:
    RiseFall::reportElimination(getID())
whenever GAMEOPTION_RISE_FALL is enabled.

`reportElimination()` determines the current chapter through `getCurrentChapter()`.
But `RFChapter::isOngoing()` is only:
    hasStarted() && !hasEnded()
and `hasStarted()` means:
    current game turn >= scheduled startTurn

It does NOT mean that `RFChapter::start()` has actually executed.

Therefore, during the pre-takeover interval on the scheduled start turn:
- `getCurrentChapter()` returns this chapter;
- `chapters[pos]->getCiv()` already equals the selected but still-AI civilization;
- `reportElimination()` treats the elimination as the player's chapter defeat.

It immediately calls:
    chapters[pos]->score();

Yet `RFChapter::start()` has not run. `start()` is what calls:
    breakdown.atChapterStart(*this);

Before that call, `RFChapterScore::chapter` is still NULL and its initial-rank state is zero.
`RFChapterScore::update()` therefore returns immediately through:
    if(chapter == NULL || initialRank <= 0)
        return;

`RFChapter::score()` nevertheless then sets:
    scoreTimestamp = current game turn;

Thus the not-yet-started chapter becomes permanently `isScored()==true` with no valid chapter-start
score baseline. `reportElimination()` then follows the normal defeat/control-transfer popup path.

Instead of being offered another civilization, the player is told that the chapter they never
actually began was defeated, and that chapter is consumed/scored.

WHY THE EXISTING RETRY BRANCH CANNOT REPAIR IT
-----------------------------------------------
There are two independent reasons the comment's intended branch fails.

1. The initial Rise/Fall implementation already loses the `!currentCh.isScored()` condition.

Practical 1268 introduced both `reportElimination()` and the retry branch in the same first
Rise/Fall implementation. Because `reportElimination()` scores the selected chapter synchronously
when the selected civ dies, the later turn-end retry test:
    else if(!currentCh.isScored())
is false in precisely the pre-takeover elimination state.

So the root conceptual bug begins with practical 1268: "scheduled/current chapter" is used as a
proxy for "chapter actually started under player control."

2. Practical 1430 made the dead-civ branch structurally unreachable as well.

The original practical-1268 `atTurnEnd()` computed:
    PlayerTypes nextCivId = (PlayerTypes)((civId + 1) % MAX_PLAYERS);

Practical 1430 changed this to:
    PlayerTypes nextCivId = nextCivAlive(civId);

The retry branch still sits inside:
    if(nextCivId == currentCh.getCiv()) {
        ...
        if(GET_PLAYER(nextCivId).isAlive()) { ... }
        else if(!currentCh.isScored()) { retry ... }
    }

`nextCivAlive()` only returns an alive player. If the selected current chapter civ is dead, it is
skipped and the outer equality is false. Hence the inner `isAlive()==false` branch can no longer
be reached for the state its comment describes.

Practical 1430 is therefore an additional later control-flow regression layered on the original
practical-1268 lifecycle mistake; the user-visible root is kept as one finding.

SOURCE-PROVABLE SCORE CORRUPTION BOUNDARY
-----------------------------------------
Do not overstate arbitrary score mathematics. What source proves is:
- the selected chapter's `RFChapterScore::atChapterStart` never ran;
- `RFChapterScore::update` no-ops because `chapter==NULL` / initialRank<=0;
- `RFChapter::score` still stamps `scoreTimestamp`;
- the chapter is thereafter classified as scored/ended;
- the normal defeat path is used rather than the explicit retry-selection intent.

The score object's reset values are zero, so this can also leave a zero/default score breakdown
for a chapter that the human never controlled.

ANCESTRY
--------
- AdvC practical 1268 / e11525d303... introduced the original Rise/Fall subsystem, including:
  * immediate `setCiv` before takeover;
  * `reportElimination` scoring the current scheduled chapter;
  * the explicit "died in between selection and chapter start" retry comment/branch.
  The lifecycle contradiction is therefore present from the initial implementation.
- AdvC practical 1430 changes the next-turn test from raw next player slot to
  `nextCivAlive(civId)`, which makes the dead-selected-civ inner branch structurally unreachable.
- Current Base AdvC 1.14 retains both behaviors.
- Current SAS inherits them unchanged.

DEDUP
-----
Whole-album search found no previous owner for:
- selected chapter civilization dying between RF_CHOOSECIV selection and actual takeover;
- `retryingCivSelection` being bypassed by `reportElimination`;
- scoring an RFChapter before `RFChapter::start()` / `RFChapterScore::atChapterStart()`;
- the practical-1430 dead-selected-civ retry branch becoming unreachable.

F661 is distinct: it is the no-major-successor `NO_PLAYER` dereference after a genuine
`reportElimination` path. F663 concerns entering that defeat path at all for a chapter the player
has not yet started. A repair for F663 can prevent some pre-takeover cases from ever reaching
F661, but genuine last-major defeat after actual chapter start still independently activates F661.

REPAIR DIRECTION
----------------
The robust repair is to distinguish "scheduled/current by turn number" from "actually started."

For elimination of `chapters[pos]->getCiv()` before that chapter's real `start()`:
- do not call `score()` as a player defeat;
- clear/reassign the dead selected civilization as appropriate;
- set `retryingCivSelection = true`;
- halt on an eligible alive civilization and relaunch civ selection.

Avoid inferring actual chapter start solely from current game turn >= startTurn.
A dedicated started/initialized flag, or an explicit RFChapter/RFChapterScore accessor reflecting
that `atChapterStart` has run, is safer than reconstructing the state from player human flags.

The `atTurnEnd()` logic should also not use `nextCivAlive()` in a way that makes its intended
dead-selected-civ detection unreachable. Detect the selected chapter civ's current liveness
directly before searching for the next alive turn participant.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F664 UNUSED:
- finish `prepareForExtendedGame` winner/no-winner and no-alive-player state;
- finish empty eligible / "Retire All" civ-selection paths and broaden F661/F663 only if they
  share the same established roots;
- finish trade/cooperation/offLimits helper contracts and existing-finding dedup;
- finish chapter/interlude/retirement/scoring arithmetic and save-state invariants;
- finish net-ID/human-control transition history (especially practical 3297);
- settle diplomacy text substitution/current-language evidence;
- finish practical 1350/1351/1354/1363/1371/1386/1388/1407/1430/2253/3027/3280/3281/3297/4186
  history plus final pointer/index/divisor/state saturation;
- either promote an independent F664 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

NEGATIVE CONTROLS PRESERVED SINCE WIP527
----------------------------------------
- `GET_TEAM(PlayerTypes)` syntax in RiseFall is SAFE in current AdvC/SAS because CoreAI.h
  deliberately overloads GET_TEAM for PlayerTypes and performs TEAMID(ePlayer). Do not rediscover.
- `mostRecentlyFinished()` has a real unsigned reverse-loop underflow pattern but is currently
  unused and has no shipped caller.
- the equivalent reverse loops in `prepareForExtendedGame()` are protected on supported
  initialized Rise/Fall paths because chapter 0 is always created and started, guaranteeing a
  matching `hasStarted()` element before underflow.
- `AI_DIPLOCOMMENT_THANKS` currently has one Response containing all thank-you text variants;
  `findThanks()` returning the first response therefore covers current data.
- `substituteDiploText()` replaces every text in that response and `restoreDiploText()` restores
  them on the game-update path.
- duplicated `if(aiVictStage < 3)` in `isCooperationRestricted()` is idempotent and has no
  behavioral consequence.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP529 -- QUEUE 038 RiseFall.cpp -- F664 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP528.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / KI#982: CONFIRMED.
- F661 / KI#983: CONFIRMED.
- F662 / KI#984: CONFIRMED.
- F663 / KI#985: CONFIRMED.
- F664 / provisional KI#986: CONFIRMED.
- F665 / provisional KI#987: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source/XML file was modified.

F664 / provisional KI#986 -- interlude victory can transfer human control to the Barbarian player
--------------------------------------------------------------------------------------------------

ROOT
----
When the game ends between Rise/Fall chapters, `RiseFall::prepareForExtendedGame()` takes its
`pos < 0` branch. To avoid handing the player the civilization that just won, it does:

    TeamTypes winnerId = g.getWinner();
    if(winnerId == NO_TEAM)
        winnerId = (TeamTypes)0;

    // Try to avoid giving the player control of the winner b/c it's confusing
    PlayerTypes haltId = nextCivAlive(GET_TEAM(winnerId).getLeaderID());
    if(haltId == NO_PLAYER)
        haltId = GET_TEAM(winnerId).getLeaderID();

    setPlayerControl(haltId, true);

The search helper iterates all `MAX_PLAYERS`, not merely the major-civilization player domain:

    int r = (startExcluded + 1) % MAX_PLAYERS;
    while(!GET_PLAYER((PlayerTypes)r).isAlive() && r != startExcluded)
        r = (r + 1) % MAX_PLAYERS;

`BARBARIAN_PLAYER == MAX_CIV_PLAYERS` is therefore a normal candidate unless it is specifically
used as `startExcluded`. The `pos < 0` winner branch does not exclude it.

SUPPORTED NATIVE ACTIVATION
---------------------------
The most direct fixture is a victory during a Rise/Fall interlude in which the winning
civilization is the only surviving major civilization.

This state is supported by the normal victory rules:
- Conquest victory checks opponents through `TeamIter<CIV_ALIVE, NOT_SAME_TEAM_AS>`.
- The `CIV_ALIVE` domain excludes the Barbarian team.
- Thus Barbarian survival does not prevent the sole remaining major civilization from satisfying
  conquest victory.

The special Barbarian player is not an eligible human civilization:
- `CvInitCore::getPlayableCiv` explicitly says:
      // Don't allow people to play the barb civ
  and returns false for the Barbarian slot in the ordinary fallback.
- `CvPlayer::isMajorCiv()` is explicitly `!isBarbarian() && !isMinorCiv()`.
- Rise/Fall initialization and ordinary civ selection operate on major-civilization domains.
- `CvPlayer::verifyAlive()` deliberately excludes Barbarians from the normal no-city/no-unit
  elimination logic, consistent with the persistent special-player role.
- `nextCivAlive` itself contains an assertion that explicitly permits its result to be
  `BARBARIAN_PLAYER`, proving that this helper is not a major-civ-only search.

When the winner is the only living major civ and the Barbarian player is alive, scanning after the
winner eventually reaches `BARBARIAN_PLAYER`. The helper returns that player rather than
`NO_PLAYER`. Therefore the intended fallback:
    if(haltId == NO_PLAYER)
        haltId = winnerLeader;
never runs.

LIVE CONSEQUENCE
----------------
`prepareForExtendedGame` immediately calls:
    setPlayerControl(BARBARIAN_PLAYER, true);

`RiseFall::setPlayerControl` has no major-civ or playable-civ guard. For `b == true`, it:
- applies the Rise/Fall human-handicap transition to the chosen player;
- calls `CvGame::changeHumanPlayer(civId)`;
- unfogs the map / changes UI visibility;
- updates human modifiers and city maintenance.

`CvGame::changeHumanPlayer` likewise has no Barbarian guard. It:
- swaps player options;
- marks the former human nonhuman;
- calls `GET_PLAYER(eNewHuman).setIsHuman(true, ...)`;
- can activate the new player's turn;
- ultimately sets the active player to the chosen slot.

The extended-game/end-game path therefore temporarily converts the special Barbarian slot into the
human/active player even though the setup layer explicitly declares that slot non-playable.

`CvGame::setGameState(GAMESTATE_OVER)` calls `RiseFall::prepareForExtendedGame()` synchronously
*before* `showEndGameSequence()` and before the Extended Game popup is queued. Thus the wrong active
player affects the native victory/end-game sequence rather than being harmless state written after
the UI has finished.

Do not overclaim the exact EXE-side symptom. Source proves the wrong player identity and violation
of the non-playable-Barbarian contract; the visual/UI consequences can depend on the closed-source
end-game code.

INTERLUDE REQUIREMENT
---------------------
This root concerns `pos < 0`, i.e. a victory while no chapter is currently ongoing. Rise/Fall
deliberately spends time in interludes under AI Auto Play between chapters, so victory can be
reached there by the AI-controlled civilizations. The code itself has a dedicated `pos < 0`
extended-game branch precisely to support game end in such an interval.

ANCESTRY / INTRODUCTION
-----------------------
AdvC practical 1268 / e11525d303... introduced:
- `nextCivAlive`;
- `prepareForExtendedGame`;
- the exact "avoid giving the player control of the winner" search through
  `nextCivAlive(winnerLeader)`;
- the fallback to the winner only when that search returns `NO_PLAYER`.

The special-player-domain mismatch is therefore present in the original Rise/Fall implementation.
Current Base AdvC 1.14 retains it. Current SAS inherits it unchanged.

Later practicals touched scoring/serialization or nearby extended-game behavior but did not make
this search major-civ-only.

WHY THIS IS DISTINCT FROM F661
------------------------------
F661 is a *no eligible successor* problem:
    reportElimination -> nextCivAlive(BARBARIAN_PLAYER) -> NO_PLAYER -> GET_PLAYER(-1).

F664 is the opposite failure mode:
- `nextCivAlive(winnerLeader)` successfully finds a player;
- the found player is the *wrong domain* (the alive Barbarian special slot);
- because it is not NO_PLAYER, the code does not take its fallback.

The repair therefore cannot be reduced to the F661 sentinel guard.

NARROW REPAIR DIRECTION
-----------------------
Search for another *living major civilization*, not another arbitrary living player.

For example:
- iterate `PlayerIter<MAJOR_CIV>` / the `MAX_CIV_PLAYERS` domain;
- choose an alive major other than the winner when possible;
- if no such major exists, deliberately keep/give control to the winner as the existing fallback
  already intends.

Do not let `BARBARIAN_PLAYER` or a minor-civ slot become the extended-game human simply because it
is alive.

WHOLE-ALBUM DEDUP
-----------------
Whole-album search found no previous owner for:
- `prepareForExtendedGame` selecting the Barbarian player;
- interlude victory transferring human/active control to BARBARIAN_PLAYER;
- the practical-1268 winner-avoidance search using the all-player rather than major-civ domain.

Existing extended-game findings elsewhere concern popup gating or dead-human UI behavior and do
not own this Rise/Fall player-selection root.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F665 UNUSED:
- inspect the `afterCivSelection` "Retire All" path, especially a dead previous chapter civ and
  empty eligible lists;
- finish `prepareForExtendedGame` NO_TEAM / NO_PLAYER and retirement branches without duplicating
  F661/F664;
- finish active-player / net-ID / human-control transitions, especially practical 3297;
- finish chapter/interlude/retirement score arithmetic and save-state invariants;
- finish cooperation/offLimits/trade helper behavior and dedup against existing findings;
- finish remaining Rise/Fall history (1350/1351/1354/1363/1371/1386/1388/1407/1430/2253/3027/
  3280/3281/3297/4186 and later refactors);
- final pointer/index/divisor/state-machine saturation and whole-album dedup;
- either promote an independent F665 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP530 -- QUEUE 038 RiseFall.cpp -- F665 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP529.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): OPEN.
- F660 / KI#982: CONFIRMED.
- F661 / KI#983: CONFIRMED.
- F662 / KI#984: CONFIRMED.
- F663 / KI#985: CONFIRMED.
- F664 / KI#986: CONFIRMED.
- F665 / provisional KI#987: CONFIRMED.
- F666 / provisional KI#988: UNUSED.
- Files complete remains 67 / 169 because Queue 038 is not closed yet.
- No gameplay/source/XML file was modified.

F665 / provisional KI#987 -- Rise/Fall chapter-selection handoff replaces the user's PlayerOptions with temporary AI-slot options
-------------------------------------------------------------------------------------------------------------------------------

ROOT
----
Rise/Fall needs a human player temporarily while the next-chapter civilization-choice popup is
open. `haltForCivSelection(PlayerTypes haltCiv)` implements that by directly doing:

    CvPlayer& h = GET_PLAYER(haltCiv);
    GET_PLAYER(getActivePlayer()).setNetID(-1);
    g.setActivePlayer(haltCiv);
    GET_PLAYER(haltCiv).setNetID(0);
    h.setIsHuman(true);
    ...
    h.addPopup(...);

This deliberately does NOT use `CvGame::changeHumanPlayer()`.

That distinction matters because PlayerOptions are per-player state. The normal human-control
transfer routine contains explicit PlayerOption preservation:

    EagerEnumMap<PlayerOptionTypes,bool> aeNewHumanOldOptions;
    ...
    GET_PLAYER(eNewHuman).setOption(
        eLoopPlayerOption,
        GET_PLAYER(eCurHuman).isOption(eLoopPlayerOption));
    ...
    GET_PLAYER(eCurHuman).setOption(
        eLoopPlayerOption,
        aeNewHumanOldOptions.get(eLoopPlayerOption));

and then activates the new human through `setActivePlayer(..., true)`, which sends the active
player options through the normal handoff path.

The temporary Rise/Fall popup-host transition skips this preservation step entirely.

SOURCE-PROVABLE STATE LOSS
--------------------------
The relevant lower-level routines confirm that no hidden option copy repairs the gap:

1. `CvGame::setActivePlayer(haltCiv)` in ordinary single-player:
   - changes the active-player ID;
   - updates visibility/UI;
   - only enters the hotseat/PBEM option-send branch when the new player is already human and the
     game is hotseat/PBEM or `bForceHotSeat` is true;
   - does not copy PlayerOptions from the former active player.

2. `CvPlayer::setIsHuman(true)`:
   - updates slot status;
   - updates team leader / alerts / AI-human state;
   - does not copy or initialize PlayerOptions.

3. `CvPlayer::reset()` initializes each player's option map independently with:
       m_abOptions.reset();

Thus an AI civilization that has never been human retains its own per-player option values rather
than automatically inheriting the user's active-player preferences.

The handoff therefore loses the provenance of the actual user's PlayerOptions as soon as the
temporary AI popup host becomes active.

HOW THE WRONG VALUES REACH THE NEXT PLAYED CIV
----------------------------------------------
After the popup:

Case A -- the player chooses a DIFFERENT civilization from the temporary popup host.

The host is returned to AI control, but remains the active player until turn order reaches the
selected civilization. Then:

    setPlayerControl(selectedCiv, true)
      -> CvGame::changeHumanPlayer(selectedCiv)

`changeHumanPlayer` treats `getActivePlayer()` -- the temporary host -- as `eCurHuman` and copies
that host's PlayerOptions to `selectedCiv`.

The actual previous chapter civilization, where the user's real option state still resides, is no
longer consulted.

Case B -- the player chooses the temporary popup host itself.

`afterCivSelection` immediately calls `setPlayerControl(h.getID(), true)`. The active player and
new human are the same temporary host. No old chapter option state is reintroduced; the host simply
continues with its pre-existing option map.

Therefore the defect is not limited to choosing a different recommendation. The first normal
between-chapter civ-selection handoff can lose the user's option state regardless of which offered
civilization is selected, unless that temporary slot happens already to contain the desired values
(e.g. from having been controlled earlier).

CURRENT PLAYER-FACING OPTIONS AFFECTED
--------------------------------------
Current `PlayerOptionTypes` includes, among others:
- PLAYEROPTION_ADVISOR_POPUPS
- PLAYEROPTION_ADVISOR_HELP
- PLAYEROPTION_WAIT_END_TURN
- PLAYEROPTION_MINIMIZE_POP_UPS
- PLAYEROPTION_SHOW_FRIENDLY_MOVES
- PLAYEROPTION_SHOW_ENEMY_MOVES
- PLAYEROPTION_QUICK_MOVES
- PLAYEROPTION_QUICK_ATTACK
- PLAYEROPTION_QUICK_DEFENSE
- PLAYEROPTION_STACK_ATTACK
- PLAYEROPTION_AUTO_PROMOTION
- PLAYEROPTION_START_AUTOMATED
- PLAYEROPTION_SAFE_AUTOMATION
- PLAYEROPTION_NUMPAD_HELP
- PLAYEROPTION_NO_UNIT_CYCLING
- PLAYEROPTION_NO_UNIT_RECOMMENDATIONS
- PLAYEROPTION_RIGHT_CLICK_MENU
- PLAYEROPTION_LEAVE_FORESTS
- PLAYEROPTION_MISSIONARIES_AUTOMATED

Many of these have direct live DLL/Python/UI consumers, so this is not merely hidden metadata.

MINIMAL SUPPORTED FIXTURE
-------------------------
A simple deterministic fixture is:
1. Start a normal single-player Rise/Fall game.
2. During the first chapter, enable any PlayerOption whose value differs from the unplayed AI
   popup-host slot, e.g. Wait at End of Turn, Quick Moves, Safe Automation or Leave Forests.
3. Reach the next chapter's civ-selection popup.
4. Complete the normal selection/takeover transition.

The new chapter's controlled player receives/retains the temporary host's option map rather than
the previous controlled civilization's map. When `changeHumanPlayer` is used, its normal option
send then propagates those wrong values to the active-player/EXE side as well.

The exact visible set of toggled options depends on the user's configuration and whether the host
slot was controlled previously. Do not overclaim that every option always becomes false; source
proves that the wrong player's option map is used.

INTERNAL ORACLE / DESIGN INTENT
-------------------------------
`CvGame::changeHumanPlayer` is the strongest internal oracle. Even before AdvC's later option-swap
refinement, the inherited control-change routine copied PlayerOptions from the old human to the new
human. AdvC practical 3284 ("Small tweaks to code that switches control of human civ") made this
contract even more explicit by preserving the new AI slot's old values and swapping the maps
rather than simply overwriting them.

Rise/Fall's temporary-host path bypasses that exact control-transfer contract.

ANCESTRY
--------
AdvC practical 1268 / e11525d303... introduced the first Rise/Fall implementation with both:
- `setPlayerControl(..., true)` using the proper `CvGame::changeHumanPlayer`;
- `haltForCivSelection(...)` instead directly calling:
      setActivePlayer(haltCiv);
      h.setIsHuman(true);
      h.updateHuman();

The option-provenance break therefore exists in the initial Rise/Fall implementation.

Practical 3297 later improved net-ID correctness in the temporary-host path but retained the direct
`setActivePlayer + setIsHuman` approach and did not transfer PlayerOptions.

Current Base AdvC 1.14 retains the defect. Current SAS inherits it unchanged.

WHY THIS IS DISTINCT FROM F664
------------------------------
F664 is player-domain selection during an interlude victory: the code can choose BARBARIAN_PLAYER
as the human.

F665 occurs on the ordinary supported major-civ selection path even when every involved
civilization is valid. The chosen player identity can be correct while the human's PlayerOptions
come from the wrong slot.

REPAIR DIRECTION
----------------
Preserve the real human option map across the temporary popup-host phase.

Possible narrow designs:
- before changing active player in `haltForCivSelection`, copy the current/previous human's
  PlayerOptions to the temporary host so any later `changeHumanPlayer` forwards the correct map;
- or store the user's option map explicitly in RiseFall across the selection phase and apply it to
  the actual selected civ immediately before/after human takeover;
- or redesign the popup handoff so it can use the normal `changeHumanPlayer` machinery without
  unwanted hotseat/password UI, while retaining the net-ID behavior practical 3297 requires.

Do not merely copy options from the temporary host at final takeover; that is exactly the faulty
provenance.

WHOLE-ALBUM DEDUP
-----------------
Whole-album search found no prior owner for:
- Rise/Fall chapter switching losing PlayerOptions;
- `haltForCivSelection` bypassing the `changeHumanPlayer` option transfer;
- a temporary AI popup host becoming the source of the next human's option map.

Existing hotseat/control/net-ID findings do not own this per-player option-state root.

QUEUE-038 CONTINUATION CURSOR
-----------------------------
Continue from F666 UNUSED:
- finish the remaining net-ID/control history with F665 now accounted for;
- finish empty eligible / Retire All and dead-previous-civ behavior without overclaiming supported
  dead-human end-game handling;
- finish `prepareForExtendedGame` NO_TEAM/NO_PLAYER branches and broaden F661/F664 only when they
  share those exact roots;
- finish trade/cooperation/offLimits helpers;
- finish serialization/transient-state and chapter/interlude score arithmetic;
- finish remaining practical history and final pointer/index/divisor/state-machine saturation;
- whole-album dedup;
- either promote a genuinely independent F666 or close Queue 038 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP531 -- QUEUE 038 RiseFall.cpp CLOSED / QUEUE 043 ACOText.cpp OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP530.
- Queue 038 `CvGameCoreDLL/RiseFall.cpp` (1,555 queued lines): CLOSED.
- Confirmed Queue-038 roots: F660-F665 / provisional KI#982-KI#987.
- F666 / provisional KI#988: UNUSED.
- Files complete: 68 / 169.
- Queues 039-042 were already completed in earlier durable checkpoints.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- No gameplay/source/XML file was modified.

QUEUE-038 PROVENANCE CLOSURE
----------------------------
Current SAS RiseFall.cpp is executable-semantically identical to supplied Base AdvC 1.14 except
for already-owned/reconciled SAS changes:
- F448/KI#771 founding-unit-extra-cost cache refresh after direct Rise/Fall handicap changes;
- explicit SAS autoplay-end cause plumbing;
- the SAS practical-5383 NULL group-plot guard in `abandonPlans`.
No missed Base-AdvC-1.14 repair was found.

QUEUE-038 CONFIRMED ROOT SET
----------------------------
F660 / KI#982
    RiseFall::read obtains a wchar array from FDataStreamBase::ReadWideString but releases it
    with scalar `delete` instead of `delete[]`. Normal initialized Rise/Fall saves contain the
    original leader-name field, so ordinary save loading reaches the mismatched deallocation.
    Introduced practical 1268.

F661 / KI#983
    `reportElimination` calls `GET_PLAYER(nextCivAlive(BARBARIAN_PLAYER))` without handling
    `NO_PLAYER`. If the current chapter civilization is the final surviving major civilization,
    the major-only search has no successor and GET_PLAYER(-1) is reached. Practical 2239 created
    the sentinel possibility by excluding Barbarians without guarding it.
    Same-root note: other major-only successor-selection sites must likewise never feed a
    `NO_PLAYER` return directly into `haltForCivSelection`/GET_PLAYER. The demonstrated ordinary
    last-major defeat path remains the primary activation.

F662 / KI#984
    `byRecommendation` returns false in both unequal victory-stage directions:
        if(vs1 > vs2) return false;
        if(vs2 > vs1) return false;
    so the explicit victory-stage civ-selection ranking criterion is discarded and score/rank
    tie-breaking is skipped for differing stages. Practical 1270 introduction typo.

F663 / KI#985
    A civilization selected for the next chapter can be eliminated after selection but before
    actual human takeover / RFChapter::start(). Scheduled-turn `isOngoing` makes
    `reportElimination` treat that not-yet-started chapter as the player's defeat, stamp it
    scored without a valid chapter-start score baseline, and bypass the source-explicit
    retry-selection intent. Practical 1268 contains the lifecycle contradiction; practical 1430
    additionally made the dead-selected-civ retry branch unreachable by first filtering through
    `nextCivAlive`.

F664 / KI#986
    On victory during an interlude (`prepareForExtendedGame`, pos<0), the attempt to choose an
    alive player other than the winner calls the all-player `nextCivAlive(winnerLeader)`.
    Conquest victory ignores Barbarian teams, so the winner can be the sole surviving major while
    the special Barbarian player remains alive. The helper then returns BARBARIAN_PLAYER rather
    than NO_PLAYER, bypassing the fallback-to-winner and feeding the explicitly non-playable
    Barbarian slot into `setPlayerControl(...,true)` / `changeHumanPlayer` before the native
    end-game sequence. Practical 1268 root.

F665 / KI#987
    The normal between-chapter civilization-selection handoff loses PlayerOption provenance.
    `haltForCivSelection` directly changes active player and calls `setIsHuman(true)` on a
    temporary AI popup host, bypassing `CvGame::changeHumanPlayer`'s explicit per-player option
    transfer. Later takeover therefore uses/retains the temporary host's option map rather than
    the previous controlled civilization's human preferences. Live options include Wait End
    Turn, Quick Moves/Combat, Safe Automation, Leave Forests, No Unit Cycling, advisor options,
    etc. Practical 1268 root; practical 3297 fixed net IDs but retained this direct handoff.

FINAL NEGATIVE / BELOW-THRESHOLD CONTROLS
-----------------------------------------
- `mostRecentlyFinished()` has a textbook unsigned reverse-loop underflow pattern but is private,
  marked currently unused, and has no shipped caller.
- The similar reverse loops in `prepareForExtendedGame()` are protected on supported initialized
  Rise/Fall paths because chapter 0 necessarily exists and has started before the subsystem can
  reach game end.
- `GET_TEAM(PlayerTypes)` calls in this subsystem are safe in current AdvC/SAS because CoreAI.h
  deliberately overloads the PlayerTypes form and maps it through TEAMID.
- `afterCivSelection` "Retire All" can restore a previous/dead chapter identity when no eligible
  civ exists, but the surrounding end-game pipeline explicitly supports dead-human identities;
  no independent source-proven corruption/crash survived beyond F661/F664.
- `prepareForExtendedGame`'s `winnerId == NO_TEAM -> team 0` fallback is suspicious for hypothetical
  game-over-in-interlude without a winner, but the ordinary supported interlude victory path has
  a winner and no independent current activation was established.
- `pessimisticDealVal` computes TEAMID(activePlayer) before its NO_PLAYER check. This would be
  unsafe with NO_PLAYER, but both shipped callers are active-human AI-diplomacy evaluation paths,
  so the precondition currently holds.
- `isCooperationRestricted` master-team recursion follows valid vassal-master leadership on its
  shipped callers; no cycle/NO_PLAYER root survived.
- `allSquare` returning true for an empty list is consistent with the intended "human receives a
  gift/non-dual concession" permissiveness and current callers.
- current `AI_DIPLOCOMMENT_THANKS` has one response containing all relevant text alternatives;
  `findThanks()` choosing the first response is sufficient for current data.
- `substituteDiploText`'s wide-to-narrow conversion remains a real localization robustness concern:
  CvString explicitly warns its std::wstring conversion breaks for non-ASCII, and current German
  `TXT_KEY_RF_NO_THANKS` contains umlaut characters. Exact MSVC `%S` behavior is locale/code-page
  dependent, so this is preserved as a targeted German/non-ASCII runtime test rather than promoted
  as deterministic F666 without runtime evidence.
- `restoreDiploText` restores all currently substituted texts on the normal game-update path.
- `abandonPlans` research-percent stepping assumes a positive
  COMMERCE_PERCENT_CHANGE_INCREMENTS; current game configuration satisfies the ordinary contract.
- production-decay threshold arithmetic, research clearing, espionage weights, city production
  automation and the current SAS NULL group-plot guard reconciled.
- `setPlayerName`, Dawn-of-Man, war/quest replay/UI surfaces did not yield independent roots.
- save read/write field ordering beyond F660 reconciles for current version 1; transient selection
  flags are deliberately not persisted and saves are not expected during interludes
  (`interludeCountdown < 0` load assertion).

HISTORY SATURATION
------------------
The retained Rise/Fall semantic history was checked through the major practical strata, including:
- 1268 initial subsystem;
- 1269/1270/1271 initial scoring/recommendation refinements;
- 1292/1307/1318;
- 1350/1351/1354 cooperation/trade/name fixes;
- 1363, 1368, 1371;
- 1386/1388;
- 1394/1395;
- 1407;
- 1430 turn-order / successor / retirement fixes;
- 1493/1497 and later pointer/storage refactors;
- 1548/1552/1564 and related mechanical refactors;
- 1644 and later file/global refactors;
- 1912 autosave timing;
- 2239 defeat-popup successor change (F661);
- 2253 city-dot-map active-owner refresh;
- 2545/2555/2571 and serialization/refactor strata;
- 2719/2998 custom-era merge strata;
- 3027 No-Slavery compatibility;
- 3218/3392 refactors;
- 3280 production-decay reset;
- 3281 handicap increase;
- 3284 generic changeHumanPlayer option-map preservation oracle for F665;
- 3297 Rise/Fall net-ID maintenance;
- 3746/3761/3877/4104;
- 4186 Marathon chapter-count adjustment;
- current SAS fixes listed above.
No independent F666 survived final history/mechanical saturation.

WHOLE-ALBUM DEDUP
-----------------
Whole-album dedup was repeated for:
- extended-game / dead-human popup behavior;
- nextCivAlive and no-successor handling;
- Rise/Fall civ recommendation sorting;
- pre-takeover elimination;
- Barbarian human-control transfer;
- changeHumanPlayer / PlayerOption transfer;
- Rise/Fall trade/cooperation helpers;
- diplomacy-text substitution.
F660-F665 remain distinct roots; no older finding owns them.

NEXT EXACT CURSOR
-----------------
C031-WIP531 physical
-> 68 / 169 files complete
-> Queue 038 CLOSED
-> F660-F665 / KI#982-KI#987 confirmed
-> F666 / KI#988 UNUSED
-> Queues 039-042 already complete
-> Queue 043 `CvGameCoreDLL/ACOText.cpp` OPEN
-> establish current SAS vs Base AdvC 1.14 semantic baseline
-> compare ACO ancestry / K-Mod/Civ4CE availability
-> inventory text-builder call graph, combat-odds inputs, first-strike/combat-limit/free-win
   assumptions, UI option gating and string/index ownership
-> reconcile against already-completed Queue 057 CombatOdds.cpp and existing combat findings
-> trace current Python/UI activation
-> whole-album dedup
-> promote F666 only for an independent supported root
-> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP532 -- QUEUE 043 ACOText.cpp -- F666 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP531.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / provisional KI#988: CONFIRMED.
- F667 / provisional KI#989: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

QUEUE-043 BASELINE
------------------
Current SAS `ACOText.cpp` is executable-semantically identical to supplied Base AdvC 1.14 except
for two SAS micro-performance edits that cache `ANIMAL_MAX_XP_VALUE` and
`BARBARIAN_MAX_XP_VALUE` in local static const ints. Those edits do not change the values or
control flow.

Therefore F666 is an inherited AdvC regression, not a SAS-only defect or a missed newer Base-AdvC
repair.

F666 / provisional KI#988 -- ACO overstates Leadership XP near animal/barbarian XP caps
---------------------------------------------------------------------------------------

ROOT
----
`CvGameTextMgr::setACOPlotHelp` separately computes:
- base combat XP (`iExperience` / `iWithdrawXP`);
- promotion-derived extra XP (`iBonusAttackerXP` / `iBonusWithdrawXP`).

For a human attacker fighting an Animal/Barbarian defender, current ACO first computes the
remaining total XP room:

    iMaxExtraXP = max(0, XP_CAP - kAttacker.getExperience());

and clamps the base award:

    iExperience = range(iExperience, 0, iMaxExtraXP);

But it then clamps the bonus with:

    iBonusAttackerXP =
        range(iBonusAttackerXP, 0, iMaxExtraXP + iExperience);

and likewise:

    iBonusWithdrawXP =
        range(iBonusWithdrawXP, 0, iMaxExtraXP + iWithdrawXP);

The `+ iExperience` / `+ iWithdrawXP` is the wrong direction. The bonus's available room is the
remaining cap *after* base XP, not the cap room plus base XP.

LIVE EXECUTOR CONTRACT
----------------------
Actual combat uses `CvUnit::changeExperience`.

On a normal attacker victory:

    changeExperience(iExperience, pDefender->maxXPValue(), true, ...);

For a Barbarian/Animal defender, `pDefender->maxXPValue()` supplies the ordinary XP cap.
Inside `changeExperience`:
1. `iUnitExperience` begins as the base combat award;
2. `getExperiencePercent()` (e.g. Leadership) multiplies that total combat award;
3. `setExperience(current + iUnitExperience, iMax)` applies the final total-XP cap.

Thus promotion bonus XP does NOT sit outside the Animal/Barbarian cap. The final amount gained is:

    min(currentXP + promotedCombatAward, cap) - currentXP

ACO's separate display must obey the same remaining total room.

CURRENT DATA ACTIVATION
-----------------------
The current mod contains a normal reachable promotion:

    PROMOTION_LEADERSHIP
        PromotionPrereq = PROMOTION_LEADER
        iExperiencePercent = 100

so a Leadership unit receives +100% combat XP.

The supplied SAS light-source mod XML does not override the inherited BtS global defines for:
- ANIMAL_MAX_XP_VALUE
- BARBARIAN_MAX_XP_VALUE
- MIN_EXPERIENCE_PER_COMBAT
- MAX_EXPERIENCE_PER_COMBAT
- EXPERIENCE_FROM_WITHDRAWL

The supplied Civ4CE/BtS `GlobalDefines.xml` therefore provides the inherited values:
- ANIMAL_MAX_XP_VALUE = 5
- BARBARIAN_MAX_XP_VALUE = 10
- MIN_EXPERIENCE_PER_COMBAT = 1
- MAX_EXPERIENCE_PER_COMBAT = 10
- EXPERIENCE_FROM_WITHDRAWL = 1

ACO is a supported BUG option (`ACO__Enabled`). Once enabled, Victory/Withdraw/Defeat XP is part of
the ordinary ACO combat tooltip; the base + promotion-bonus display is not hidden behind the
optional experience-range setting.

DETERMINISTIC EXAMPLE
---------------------
Take a Leadership (+100% XP) attacker at 9 XP fighting a normal Barbarian.

Suppose the nominal pre-promotion base award is only 1 XP.

Actual executor:
    base award = 1
    Leadership turns combat award into 2
    final setExperience is capped at 10
    actual gain = 10 - 9 = +1 total XP

Current ACO:
    iMaxExtraXP = 10 - 9 = 1
    iExperience = min(1, 1) = 1
    iBonusAttackerXP = 1
    upper bonus clamp = iMaxExtraXP + iExperience = 2
    displayed gain = 1 + 1 = +2 XP

For a nominal base award of 2 XP, current ACO can display:
    base 1 + bonus 2 = +3 XP
while actual total gain is still only +1.

The same root exists for:
- Animal cap 5;
- combat-limit withdrawal XP (`iBonusWithdrawXP`) near either cap.

ANCESTRY / EXACT REGRESSION POINT
---------------------------------
This is especially well localized.

K-Mod practical 101 / d5c68c5392 ("Added the Advanced Combat Odds mod") imported the original
ACO cap logic with the correct expression:

    iBonusAttackerXP =
        range(iBonusAttackerXP, 0,
              XP_CAP - (currentXP + iExperience));

and the equivalent withdrawal expression.

That is exactly the proper "room remaining after base XP" direction.

AdvC practical 2204 / d28df2fae6785654721a1fd0f84db16b09bd76de
("Misc. refactoring, comments") simplified the duplicated Animal/Barbarian block. During that
refactor it changed the correct subtraction into:

    iBonusAttackerXP =
        range(iBonusAttackerXP, 0, iMaxExtraXP + iExperience);

    iBonusWithdrawXP =
        range(iBonusWithdrawXP, 0, iMaxExtraXP + iWithdrawXP);

The practical title describes this as refactoring, but the sign/relationship changed semantics.
Current Base AdvC 1.14 retains the practical-2204 expression. Current SAS inherits it.

INTERNAL ORACLE
---------------
There are two independent internal oracles:
1. the current `CvUnit::changeExperience` + `setExperience(..., iMax)` executor;
2. K-Mod's original ACO implementation, which already used
       cap - (currentXP + baseXP)
   before AdvC practical 2204 changed it.

This removes ambiguity about whether ACO intentionally displays "uncapped theoretical bonus XP":
its original code explicitly tried to display the capped actual award.

CONSEQUENCE / SEVERITY
----------------------
This is player-facing combat-tooltip misinformation. It does not change actual combat XP.
A Leadership unit close to the Animal/Barbarian XP limit can be told it will gain more XP than the
executor can award.

The discrepancy is strategically relevant because the entire purpose of ACO's XP display is to
forecast the combat result before committing to the attack.

NARROW REPAIR DIRECTION
-----------------------
The simplest display repair is to cap the displayed bonus by the room that remains after the
already-capped displayed base award:

    int iBonusRoom = max(0, iMaxExtraXP - iExperience);
    iBonusAttackerXP = range(iBonusAttackerXP, 0, iBonusRoom);

and equivalently:

    int iBonusWithdrawRoom = max(0, iMaxExtraXP - iWithdrawXP);
    iBonusWithdrawXP = range(iBonusWithdrawXP, 0, iBonusWithdrawRoom);

More robustly, derive the displayed *total actual XP gain* through the same ordering as
`CvUnit::changeExperience` and split base/bonus only for presentation afterward. That avoids future
drift if experience-modifier/cap ordering changes again.

DEDUP
-----
Whole-album search found no prior owner for:
- ACO Leadership XP exceeding Animal/Barbarian caps;
- `iBonusAttackerXP` / `iBonusWithdrawXP` cap arithmetic;
- AdvC practical 2204's ACO refactor sign error.

This is independent from:
- F602/KI#925: executor/predictor disagreement from SAS Janissary combatLimit 110;
- F637/KI#959: calculateCombatOdds failing to forward bHideFreeWins;
- F638/KI#960: `getCombatOddsSpecific` bilateral chance-first-strike probability bug.

ACO correctly calls `initCombatants(..., bHideFreeWins)` and therefore does not duplicate F637.
ACO displays driven by `getCombatOddsSpecific` can inherit F638's wrong probabilities, but those
symptoms remain owned by F638 unless ACO adds an independent summation/display error.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F667 UNUSED:
- finish exact expected-HP normalization and underflow/zero-probability boundaries;
- finish survival bar arithmetic and 0/100/extreme probability pixel bounds;
- finish XP-range thresholds and compare all XP displays with current executor ordering/caps;
- finish attacker/defender detailed-health condensation and minimum-HP outcome accounting;
- finish average-health/unharmed/basic-info outputs;
- audit `setACOModifiersPlotHelp`, including total-defense modifier zero/rounding boundaries;
- compare all probability paths against already-owned F602/F637/F638;
- inspect ACO BUG-option activation/defaults and current XML/data bounds;
- finish ACO/K-Mod/AdvC history (including 2963/2971/2985/3392/3903 and SAS formatting/perf edits);
- whole-album dedup and final pointer/index/divisor/numeric saturation;
- either promote an independent F667 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP533 -- QUEUE 043 ACOText.cpp -- F667 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP532.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / KI#988: CONFIRMED.
- F667 / provisional KI#989: CONFIRMED.
- F668 / provisional KI#990: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F667 / provisional KI#989 -- ACO uses the wrong per-combat XP ceiling when a non-Barbarian attacker defeats a normal Barbarian
-------------------------------------------------------------------------------------------------------------------------------

ROOT
----
At the start of `CvGameTextMgr::setACOPlotHelp`, current ACO derives separate per-combat XP caps:

    int iMaxXPAtt = MAX_EXPERIENCE_PER_COMBAT;
    int iMaxXPDef = iMaxXPAtt;
    if (kAttacker.isBarbarian())
        iMaxXPDef -= 4;
    if (kDefender.isBarbarian())
        iMaxXPAtt -= 4;

For a normal non-Animal Barbarian defender and current MAX_EXPERIENCE_PER_COMBAT=10, ACO therefore
uses an attacker victory cap of:

    iMaxXPAtt = 10 - 4 = 6

and calculates:

    iExperience =
        (kDefender.attackXPValue() * def.strength()) / att.strength();
    iExperience = range(iExperience, MIN_EXPERIENCE_PER_COMBAT, iMaxXPAtt);

The real combat executor uses a different rule for exactly this direction:

    int iExperience = pDefender->attackXPValue();
    iExperience = ((iExperience * iDefenderStrength) / iAttackerStrength);
    iExperience = range(iExperience,
        MIN_EXPERIENCE_PER_COMBAT,
        MAX_EXPERIENCE_PER_COMBAT /
            (pDefender->isBarbarian() && !pDefender->isAnimal() ? 2 : 1));

For an ordinary Barbarian defender the actual ceiling is therefore:

    10 / 2 = 5

not 6.

This is direction-specific. When the attacker is a normal Barbarian and the defender wins, the
executor uses:
    MAX_EXPERIENCE_PER_COMBAT - 4
and ACO's `iMaxXPDef -= 4` matches it.

CURRENT SHIPPED DATA / DETERMINISTIC FIXTURE
--------------------------------------------
Current `CIV4UnitInfos.xml` gives:
- UNIT_WARRIOR: iCombat=3, iXPValueAttack=4;
- UNIT_SWORDSMAN: iCombat=6, iXPValueAttack=4.

`CvUnit::attackXPValue()` applies advc.312's normal-Barbarian reduction:

    UnitInfo.iXPValueAttack
        - (isBarbarian() && !isAnimal() ? 1 : 0)

so a Barbarian Swordsman has:
    attackXPValue() = 4 - 1 = 3.

The Barbarian civilization has no special replacement/disable mapping for UNITCLASS_SWORDSMAN,
so the ordinary Swordsman class remains available to that civilization under the normal Barbarian
unit system.

Take a full-health, unmodified human Warrior (strength 3) defeating a full-health, unmodified
Barbarian Swordsman (strength 6) on a context with no additional strength modifiers.

Both paths begin with:
    raw XP = (3 * 6) / 3 = 6.

Actual executor:
    range(6, 1, 10/2) = 5
    -> awards at most 5 base combat XP, subject also to the separate total Barbarian-XP cap.

Current ACO:
    iMaxXPAtt = 10 - 4 = 6
    range(6, 1, 6) = 6
    -> displays 6 XP.

Choose an attacker at 0 XP and without an ExperiencePercent promotion to isolate this root:
BARBARIAN_MAX_XP_VALUE=10 does not further reduce the executor's +5 award, and F666's
Leadership/remaining-total-cap bug is not involved.

The matchup is low-odds but wholly legal; ACO exists precisely to preview such attacks before the
player commits.

XP-RANGE DISPLAY IS WRONG UNDER THE SAME ROOT
----------------------------------------------
The optional ACO experience-range display also uses `iMaxXPAtt` as its upper bound and builds its
CombatRatioThresholds from that value.

Against a normal Barbarian defender it therefore constructs a 1..6 attacker-XP range when the
executor's advc.312 attacker-victory ceiling is 1..5. The root is not only the headline Victory XP
number; the detailed ratio-to-XP thresholds are shifted at the top end as well.

ANCESTRY / EXACT INTRODUCTION
-----------------------------
AdvC practical 1253 / f6ff6a182f... introduced advc.312 ("barbarian experience" changes).

The SAME practical changes the real executor asymmetrically:

Attacker is killed; defender earns XP from a normal Barbarian attacker:
    MAX_EXPERIENCE_PER_COMBAT - 4

Defender is killed; attacker earns XP from a normal Barbarian defender:
    MAX_EXPERIENCE_PER_COMBAT / 2

It also changes `CvUnit::attackXPValue()` for a normal Barbarian:
    base attack XP - 1.

In the ACO adaptation in that same practical, however, both directions were represented by
subtracting 4:

    if(pAttacker->isBarbarian()) maxXPDef -= 4;
    if(pDefender->isBarbarian()) maxXPAtt -= 4;

Thus practical 1253 itself introduces the display/executor disagreement. The ACO code appears to
have mirrored the defender-wins `-4` rule onto the attacker-wins direction instead of mirroring
the executor's `/2` rule.

Before advc.312, both K-Mod's ACO and combat executor used the common
MAX_EXPERIENCE_PER_COMBAT ceiling, so there was no such asymmetry to model.

Current Base AdvC 1.14 retains the mismatch. Current SAS inherits it unchanged.

INTERNAL ORACLE
---------------
The live `CvUnit::resolveCombat` XP formula is the primary oracle: it is the code that actually
awards the combat XP.

The practical-1253 diff is an unusually strong history oracle because it introduces both:
- the actual `/2` attacker-win rule;
- the ACO `-4` attacker display rule;
in the same semantic feature change.

This eliminates the possibility that ACO merely failed to follow some much later executor
redesign.

CONSEQUENCE / SEVERITY
----------------------
This is player-facing ACO misinformation. A non-Barbarian attacker can be told it will receive
6 base XP for defeating a sufficiently stronger normal Barbarian when the executor caps that
single combat at 5.

It does not change combat or awarded XP.

The optional XP-range/ratio guidance inherits the same one-XP upper-bound error, potentially
telling the player that a combat-ratio region yields 6 XP when 5 is the true maximum.

NARROW REPAIR
-------------
Mirror the executor rule exactly when deriving ACO's attacker per-combat maximum.

Conceptually:

    int iMaxXPAtt = MAX_EXPERIENCE_PER_COMBAT;
    if (kDefender.isBarbarian() && !kDefender.isAnimal())
        iMaxXPAtt /= 2;

while retaining the defender-side:
    if (kAttacker.isBarbarian() && !kAttacker.isAnimal())
        iMaxXPDef -= 4;

The `!isAnimal()` condition should match the current executor too; current ACO's broad
`isBarbarian()` test also applies its `-4` pre-cap to Animals before the later total-XP adjustment,
whereas the executor's advc.312 per-combat reductions explicitly exclude Animals.

A stronger future-proof repair would centralize the per-combat XP formula/cap so ACO and
`resolveCombat` cannot drift independently.

RELATION TO F666
----------------
F666 and F667 both affect ACO XP display but have different roots and activation.

F667:
- introduced practical 1253;
- wrong *per-combat base XP ceiling* for attacker defeating a normal Barbarian;
- activates at low/current total XP without Leadership;
- deterministic 6-displayed vs 5-awarded example.

F666:
- introduced practical 2204;
- wrong *remaining total cap for promotion-derived bonus XP* through `+ baseXP` instead of
  remaining-room subtraction;
- most visible with Leadership/ExperiencePercent near Animal/Barbarian lifetime XP caps;
- also affects withdrawal bonus XP.

Fixing either one does not fix the other.

DEDUP
-----
Whole-album search through WIP532 found no prior owner for:
- ACO `iMaxXPAtt` using `MAX_XP-4` against Barbarian defenders;
- the executor's corresponding `MAX_XP/2` rule;
- ACO's 6-vs-5 Barbarian per-combat XP forecast;
- the XP-range thresholds inheriting that wrong top cap.

Nearby owners remain distinct:
- F666: promotion bonus / remaining lifetime XP cap;
- F602/KI#925: SAS combatLimit=110 executor/predictor discrepancy;
- F637/KI#959: ignored bHideFreeWins argument in calculateCombatOdds;
- F638/KI#960: bilateral chance-first-strike probability error in getCombatOddsSpecific.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F668 UNUSED:
- finish expected-HP normalization and exact zero/extreme-probability divisions;
- finish survival-bar pixel math and 0/100% boundaries;
- finish all remaining XP display paths after F666/F667, including defender-side promotion XP
  and Animal-specific per-combat handling;
- finish attacker/defender detailed-health condensation, duplicate/unharmed outcome checks and
  minimum-HP combat-limit accounting;
- finish average-health/basic-info outputs;
- audit `setACOModifiersPlotHelp` total-defense/rounding/index assumptions;
- reconcile all probabilities against F602/F637/F638;
- finish current BUG-option activation and ACO history (2963/2971/2985/3392/3903 plus SAS edits);
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F668 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP534 -- QUEUE 043 ACOText.cpp -- F668 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP533.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / KI#988: CONFIRMED.
- F667 / KI#989: CONFIRMED.
- F668 / provisional KI#990: CONFIRMED.
- F669 / provisional KI#991: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F668 / provisional KI#990 -- ACO prints the defender's unhurt detailed-HP outcome twice when one attacker hit is lethal
------------------------------------------------------------------------------------------------------------------------

ROOT
----
The defender detailed-health section iterates attacker-hit counts backwards:

    for (int iHitsByAtt = att.hitsToWin(); iHitsByAtt >= 1; iHitsByAtt--)

For a kill-capable attacker:

    if (kAttacker.combatLimit() >= kDefender.maxHitPoints())
    {
        if (iHitsByAtt == att.hitsToWin())
            iHitsByAtt--; // we don't need to do HP for when the unit is dead
    }

When `att.hitsToWin() == 1`, the first iteration therefore changes `iHitsByAtt` from 1 to 0.

The loop then computes the probability that the defender exits combat having suffered zero
attacker hits:

    prob += 100 * (
        getCombatOddsSpecific(..., def.hitsToWin(),     0) +
        getCombatOddsSpecific(..., def.hitsToWin() - 1, 0));

and sets:
    def_HP = kDefender.currHitPoints().

If that probability exceeds the detailed-health cutoff (normally 0.5%), the ordinary/direct branch
immediately prints an unhurt-defender bar.

After the loop, current ACO then unconditionally does:

    // print the unhurt value...always
    prob = 100 * (
        getCombatOddsSpecific(..., def.hitsToWin(),     0) +
        getCombatOddsSpecific(..., def.hitsToWin() - 1, 0));

and prints the same current-HP outcome again.

Thus a one-hit-to-kill defender whose unhurt-survival probability is above the detail cutoff gets
two identical detailed-health rows.

THE EXISTING "DON'T PRINT TWICE" GUARD DOES NOT COVER THIS
----------------------------------------------------------
Immediately before the unconditional final unhurt row, ACO contains:

    if (bCondensed && att.hitsToWin() > 1)

with the comment:

    "the reason we need att.hitsToWin() to be greater than 1 is
     that if it's equal to 1 then we end up with the
     defender detailed HP bar show up twice
     because it will also get printed below"

This guard prevents only a *condensed low-probability batch* from being flushed when
`hitsToWin()==1`.

It does not suppress the direct row already emitted inside the loop when `prob >
HP_percent_cutoff`. The nearby comment therefore identifies the exact duplication state but
applies its prevention only to one of the two loop-output modes.

CURRENT SHIPPED DETERMINISTIC FIXTURE
-------------------------------------
Current unit data provides a simple no-first-strike/no-withdrawal fixture:
- UNIT_SWORDSMAN:
    iCombat = 6
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iWithdrawalProb = 0
- UNIT_TANK:
    iCombat = 30
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iWithdrawalProb = 0

Take a full-health Swordsman attacking a Tank currently at 10 HP, with no external strength
modifiers.

Current combat-strength/firepower math gives:
- Swordsman current strength = 6;
- injured Tank current strength = 30 * 10/100 = 3;
- defender per-round win probability = 3 / (6 + 3) = 1/3;
- Swordsman damage to Tank = 12 HP per winning round;
- Tank damage to Swordsman = 32 HP per winning round.

Therefore:
- attacker needs 1 hit to kill the 10-HP Tank:
      att.hitsToWin() = 1;
- defender needs 4 hits to kill the 100-HP Swordsman:
      def.hitsToWin() = 4.

With no first strikes or withdrawal, the probability that the Tank survives unhurt is the
probability it wins four rounds before the Swordsman wins even one:

    (1/3)^4 = 1/81 ~= 1.2345679%

This exceeds the default merged-bar cutoff:
    HP_percent_cutoff = 0.5%.

The first loop therefore directly prints approximately:
    Tank 10 HP -- 1.23%

Then the unconditional "print the unhurt value...always" block computes and prints the exact same
10-HP / ~1.23% outcome again.

A Tank reaching 10 HP is ordinary valid unit state; the finding does not rely on malformed XML,
a special promotion or undefined combat parameters.

CURRENT UI ACTIVATION
---------------------
The detailed defender-health section is a supported ACO display controlled by
`ACO__ShowDefenderHealthBars`. Current BUG configuration contains that option (default list value
2). `ACO__MergeShortBars` defaults true, which gives the 0.5% cutoff used in the fixture above.

The duplication is therefore user-facing whenever the corresponding ACO view is displayed for a
one-hit lethal matchup whose unhurt-defender outcome exceeds the cutoff.

ANCESTRY
--------
The bug is inherited from the original ACO integration rather than introduced by AdvC.

K-Mod practical 101 / d5c68c5392 ("Added the Advanced Combat Odds mod") introduces:
- the backward defender-HP loop;
- the `n_D--` dead-state skip;
- the direct output branch;
- the later unconditional unhurt output;
- and the comment explicitly discussing the one-hit "show up twice" case.

The original code already restricts only the condensed flush with:
    if (bIsCondensed && iNeededRoundsAttacker > 1)

while leaving the direct above-cutoff output unsuppressed.

Later K-Mod/AdvC refactors preserve the same control flow. Current Base AdvC 1.14 retains it.
Current SAS inherits it unchanged.

CONSEQUENCE / SEVERITY
----------------------
This is a low-severity but source-certain player-facing ACO presentation bug:
- the same surviving-defender HP state and probability are listed twice;
- users can interpret the duplicated rows as two separate outcomes or simply see needless clutter;
- probabilities are not doubled in actual combat and game state is unaffected.

The finding is numbered because it has:
- a current supported UI path;
- a deterministic shipped-unit fixture;
- explicit source evidence;
- and an independent root from the probability/XP bugs already owned elsewhere.

NARROW REPAIR DIRECTION
-----------------------
Ensure the unhurt-defender state is emitted exactly once.

A simple structural repair is to exclude `iHitsByAtt==0` from the loop for kill-capable
`att.hitsToWin()==1` and leave the unconditional final unhurt block as the single owner.

Alternatively, track whether the loop has already emitted/condensed the zero-hit state and skip the
final unhurt output when it has. The former is cleaner because the final block is already designed
as the canonical "unhurt value...always" output.

Do not merely retain the existing `bCondensed && hitsToWin()>1` condition; that is the incomplete
guard that leaves the direct-output duplication live.

DEDUP
-----
Whole-album search through WIP533 found no prior owner for:
- ACO defender detailed-health bar duplication;
- `att.hitsToWin()==1` / one-hit defender detailed HP;
- the acknowledged "show up twice" comment.

F638 can make the probabilities themselves wrong for bilateral chance-first-strike matchups, but
F668 is independent: the simple Swordsman/Tank fixture uses no first strikes and duplicates a
correctly computed probability.

F666/F667 concern XP display and do not overlap.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F669 UNUSED:
- finish expected-HP conditional divisions at exact/extreme odds and combat limits;
- finish survival overview bar pixel arithmetic and negative/overflow boundaries;
- finish Animal-specific per-combat XP path and defender-side ExperiencePercent cap behavior;
- finish attacker detailed-health lowest-state accounting and condensed-range ordering;
- finish defender detail beyond F668, including combat-limit PullOut aggregation;
- finish average-health/unharmed/basic-info outputs;
- audit `setACOModifiersPlotHelp`, especially total-defense modifier arithmetic;
- reconcile all probability symptoms against F602/F637/F638;
- finish ACO BUG-option/current-data activation and later history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F669 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP535 -- QUEUE 043 ACOText.cpp -- F669 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP534.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / KI#988: CONFIRMED.
- F667 / KI#989: CONFIRMED.
- F668 / KI#990: CONFIRMED.
- F669 / provisional KI#991: CONFIRMED.
- F670 / provisional KI#992: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F669 / provisional KI#991 -- ACO float underflow can turn legal extreme-combat conditional expected HP into 0/0
---------------------------------------------------------------------------------------------------------------

ROOT
----
ACO computes outcome probabilities through `combat_odds::getCombatOddsSpecific`, whose return type
and internal accumulators are `float`.

It then displays conditional expected hit points by dividing accumulated weighted HP by the
corresponding outcome-probability sum.

For an attacker victory:
    E_HP_Att_Victory / AttackerKillOdds

For a combat-limit pullout:
    E_HP_Att_Withdraw / PullOutOdds

For defender victory/attacker retreat:
    E_HP_Def_Defeat / (RetreatOdds + DefenderKillOdds)

The first two have no zero-denominator guard at all. The defender line uses:
    def.odds() != 0 ? E_HP_Def_Defeat /
        (RetreatOdds + DefenderKillOdds) : 0.0

but `def.odds()` is the integer *per-round* chance, not a guarantee that the accumulated
single-precision whole-combat outcome probability remains representable.

A legal combat can therefore have nonzero mathematical victory odds while
`getCombatOddsSpecific` underflows every relevant outcome to 0.0f. The weighted expected-HP
numerator underflows to zero along with it. ACO then evaluates 0.0f / 0.0f.

CURRENT SHIPPED DETERMINISTIC FIXTURE -- ATTACKER VICTORY
---------------------------------------------------------
Current `CIV4UnitInfos.xml`:
- UNIT_SCOUT:
    iCombat = 1
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iWithdrawalProb = 0
    bOnlyDefensive = 0
    bOnlyAttackAnimals = 0
- UNIT_MODERN_TANK:
    iCombat = 40
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iWithdrawalProb = 0

A 1-HP Scout is a legal living land unit and can attack the Modern Tank.

With no external modifiers:
- Scout max combat strength is 100 internal, but at 1/100 HP its current combat strength is 1;
- full Modern Tank current combat strength is 4000.

`CvUnit::getDefenderCombatValues` therefore derives defender round odds:

    floor(1000 * 4000 / (4000 + 1)) = 999

so attacker round odds are exactly:
    1 / 1000 = 0.001.

Current firepower/damage arithmetic gives:
- Scout firepower = (100 + 1 + 1)/2 = 51;
- Tank firepower = 4000;
- mean/strength factor = (51 + 4000 + 1)/2 = 2026;
- Scout damage to Tank:
      floor(20 * (51 + 2026) / (4000 + 2026)) = 6 HP.

The full-health Tank therefore requires:
    ceil(100 / 6) = 17
Scout hits to defeat.

Any Tank hit kills the 1-HP Scout, so the only attacker-victory sequence is 17 consecutive Scout
round wins. With no first strikes or withdrawal:

    P(victory) = (0.001)^17 = 10^-51.

Single-precision float's smallest positive subnormal is about 1.4 * 10^-45. `10^-51` therefore
rounds/underflows to exactly `0.0f`.

For this no-first-strike state, `getCombatOddsSpecific(kScout,kTank,0,17)` is precisely that
17-round probability and all other attacker-victory hit-count states are impossible because the
Scout has only 1 HP.

Thus:
    AttackerKillOdds = 0.0f
and:
    E_HP_Att_Victory = 1 HP * 0.0f = 0.0f.

Current ACO unconditionally formats:
    E_HP_Att_Victory / AttackerKillOdds
i.e.:
    0.0f / 0.0f.

The mathematical victory odds are nonzero; only the single-precision representation has collapsed.

REVERSE FIXTURE / DEFENDER LINE
-------------------------------
The same representational problem exists in the opposite direction.

Take a full-health Modern Tank attacking a 1-HP Scout, again with no external modifiers.
The Scout's per-round chance is nonzero (the integer Combatant odds remain 1), but defeating the
Tank before one Tank hit removes the Scout requires an extremely long sequence of the minimum
per-round success probability. The accumulated `DefenderKillOdds` can underflow to 0.0f while:

    def.odds() != 0

remains true.

The defender expected-HP expression then reaches:
    0.0f / (0.0f + 0.0f).

Hence its current `def.odds()!=0` guard is not a sufficient underflow guard.

The combat-limit pullout expression has the same root if all pullout-state probabilities
underflow.

TARGET-BUILD CONSEQUENCE BOUNDARY
---------------------------------
0.0f/0.0f is a non-finite floating-point operation. On the normal IEEE-style target runtime it
produces a NaN value, which is then passed to `CvWString::Format("%.1f", ...)`.

Do not overclaim an exact rendered token such as "nan" or "1.#QNAN"; formatting of a non-finite
float is CRT-specific. Source proves that ACO supplies a non-finite conditional-HP value instead
of a meaningful finite HP forecast on the supported matchup.

The headline combat probability may simultaneously format as 0.00% because the single-precision
outcome sum has underflowed, even though the true probability is merely fantastically small.

ANCESTRY
--------
K-Mod practical 101 / d5c68c5392 ("Added the Advanced Combat Odds mod") introduced the original
ACO implementation with the same unguarded expressions:

    E_HP_Att_Victory / AttackerKillOdds
    E_HP_Att_Withdraw / PullOutOdds

and the corresponding defender expected-HP normalization.

AdvC later moved/refactored the code but did not add representational zero guards. Current Base
AdvC 1.14 retains the behavior. Current SAS inherits it unchanged.

This is therefore an original ACO numeric-stability defect, not an AdvC-only regression.

WHY POSITIVE PER-ROUND ODDS DO NOT SAVE IT
------------------------------------------
Current combat initialization ensures ordinary positive-strength opponents have nonzero integer
per-round odds (at least one side out of COMBAT_DIE_SIDES). That protects against a logically
impossible outcome caused by a literal 0% round probability.

It does not protect a product/sum of many nonzero round probabilities stored in `float`.
The Scout/Tank fixture turns seventeen `0.001` factors into an unrepresentable whole-combat
probability while every individual round chance remains valid and nonzero.

REPAIR DIRECTION
----------------
At minimum, never normalize conditional expected HP by a non-positive/non-finite accumulated
probability.

For each conditional output:
- if the accumulated outcome probability is representably > 0, divide normally;
- otherwise omit the conditional HP, display an explicit "effectively 0%"/unavailable state, or
  compute it through a numerically stable method.

A better repair is to calculate ACO probability distributions in `double` (or a stable scaled/log
representation) rather than float. Double can represent `10^-51` easily and would also reduce
rounding loss throughout the detailed health distributions.

Even with double, defensive denominator checks remain appropriate.

DEDUP
-----
Whole-album search through WIP534 found no prior owner for:
- ACO conditional expected-HP 0/0;
- float underflow in extreme whole-combat probabilities;
- AttackerKillOdds/PullOutOdds zero normalization;
- nonzero `def.odds()` paired with zero representable DefenderKillOdds.

This is distinct from:
- F638/KI#960: algebraically wrong bilateral chance-first-strike probabilities;
- F668/KI#990: duplicate defender-health row;
- F666/F667: XP display drift;
- F602/KI#925: combatLimit executor/predictor mismatch.

The F669 Scout/Tank fixture uses no first strikes, so F638 does not contribute.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F670 UNUSED:
- finish remaining survival-overview bar arithmetic after treating underflow as F669;
- finish Animal lifetime/per-combat XP-range behavior and defender-side XP display;
- finish attacker lowest-HP / retreat bar accounting;
- finish defender combat-limit PullOut aggregation beyond F668/F669;
- finish average/unharmed/basic-info outputs;
- audit `setACOModifiersPlotHelp` total-defense modifier arithmetic and zero-strength invariants;
- reconcile all remaining probability symptoms against F602/F637/F638;
- finish ACO BUG-option/current-data activation and later history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F670 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP536 -- QUEUE 043 ACOText.cpp -- F670 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP535.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / KI#988: CONFIRMED.
- F667 / KI#989: CONFIRMED.
- F668 / KI#990: CONFIRMED.
- F669 / KI#991: CONFIRMED.
- F670 / provisional KI#992: CONFIRMED.
- F671 / provisional KI#993: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F670 / provisional KI#992 -- ACO Experience Range ignores remaining Animal/Barbarian lifetime XP room
------------------------------------------------------------------------------------------------------

ROOT
----
ACO correctly performs a current-experience-aware lifetime-cap adjustment for the main Victory /
Withdraw XP fields when the defender is an Animal or Barbarian:

    iMaxExtraXP = max(0, XP_CAP - kAttacker.getExperience());
    iExperience = range(iExperience, 0, iMaxExtraXP);
    iWithdrawXP = range(iWithdrawXP, 0, iMaxExtraXP);

(The promotion-bonus part of this same block has the separate F666 sign bug.)

Later, however, the optional `ACO__ShowExperienceRange` section independently reconstructs the XP
ratio thresholds from only:

    iMaxXPAtt
    MIN_EXPERIENCE_PER_COMBAT
    kDefender.attackXPValue()
    CombatRatio

It does not consult:
- `kAttacker.getExperience()`;
- `ANIMAL_MAX_XP_VALUE`;
- `BARBARIAN_MAX_XP_VALUE`;
- `iMaxExtraXP`;
- or, except for one special lowest-XP branch, the already lifetime-capped `iExperience`.

In the ordinary middle-range branch it explicitly prints:

    current-ratio XP = iMaxXPAtt - (i + 1)

instead of `iExperience`.

Thus the headline Victory line can correctly say that an at-cap unit will gain 0 XP while the
immediately following Experience Range says that the same current combat ratio yields several XP.

CURRENT DETERMINISTIC BARBARIAN FIXTURE
---------------------------------------
Use:
- a full-health human UNIT_SWORDSMAN;
- current experience exactly 10 XP;
- a full-health Barbarian UNIT_SWORDSMAN;
- no external strength modifiers and no ExperiencePercent promotion.

Current data:
- Swordsman base strength = 6;
- Swordsman base `iXPValueAttack = 4`;
- a normal Barbarian's `CvUnit::attackXPValue()` is 4 - 1 = 3 (advc.312);
- BARBARIAN_MAX_XP_VALUE = 10.

Actual executor:
- the attacker is already at the Barbarian lifetime XP cap;
- `changeExperience(..., iMax=10, ...)` cannot raise its total XP;
- actual gain = 0.

Current ACO main Victory XP:
    iMaxExtraXP = max(0, 10 - 10) = 0;
    iExperience = range(rawXP, 0, 0) = 0;
so the main line also correctly displays 0 XP.

Current ACO Experience Range:
- equal strengths give `CombatRatio = 1.00`;
- current code has `iMaxXPAtt = 6` because of F667's separate per-combat-cap bug;
- threshold array from attackXPValue 3 is:
      3/6 = 0.50
      3/5 = 0.60
      3/4 = 0.75
      3/3 = 1.00
      3/2 = 1.50
- strict `CombatRatio > threshold` selects the 0.75..1.00 interval branch;
- that branch prints the current ratio from:
      iMaxXPAtt - (i+1) = 6 - 3 = 3 XP.

So the tooltip can show, for the same attack:
    Victory: ... 0 XP
followed by an XP-range line whose `R=1.00` entry says:
    3 XP.

INDEPENDENT OF F667
-------------------
Fixing F667 does not repair this fixture.

If `iMaxXPAtt` is corrected to the real normal-Barbarian per-combat maximum of 5:
- thresholds become 3/5, 3/4, 3/3, 3/2;
- at CombatRatio=1.00 the strict comparison again selects the interval below the 1.00 threshold;
- the reconstructed current-ratio value is:
      5 - (1+1) = 3 XP.

The detailed range still says 3 XP while actual/main-line gain remains 0.

Therefore F670 is not merely an additional symptom of F667's 6-vs-5 ceiling.

AT 9 XP THE SAME ROOT MISSTATES 1 AS 3
--------------------------------------
The discrepancy is not limited to an exactly capped unit.

At 9 current XP against the same Barbarian:
- lifetime room is 1;
- executor gain is at most 1;
- main ACO `iExperience` becomes 1;
- Experience Range still reconstructs 3 XP at R=1.00.

ANIMALS SHARE THE ROOT
----------------------
The original/current range code likewise ignores:
    ANIMAL_MAX_XP_VALUE - currentXP.

The Animal lifetime cap is 5, so a unit at/near 5 XP can receive 0/1 XP from an Animal while the
ratio-range display continues to advertise theoretical per-combat values from `iMaxXPAtt`.

Current ACO's broad `if(kDefender.isBarbarian()) iMaxXPAtt -= 4` also gives Animals a nominal
pre-range ceiling that does not mirror the executor; however the lifetime-room omission survives
regardless of how that separate per-combat ceiling is repaired.

UI ACTIVATION
-------------
`ACO__ShowExperienceRange` is a supported BUG ACO option in current `ACO_config.xml`.
Its default list value is 0, so this detailed line is opt-in rather than always shown.

Once enabled for the corresponding ACO view, it is ordinary combat-tooltip output; no debug mode
or malformed data is required.

ANCESTRY
--------
This root predates AdvC.

K-Mod practical 101 / d5c68c5392, the original ACO import, already contains both:
1. the Animal/Barbarian current-XP lifetime-cap block for the main displayed award, including the
   originally correct bonus remaining-room subtraction;
2. a later independent Experience Range block whose threshold and labels use only
   MAX_EXPERIENCE_PER_COMBAT / MIN_EXPERIENCE_PER_COMBAT and attackXPValue.

Thus the initial ACO implementation already lets the detailed range ignore the lifetime cap that
the main XP line honors.

AdvC practical 1253 later changes the theoretical `iMaxXPAtt` used by the range for advc.312,
creating F667's separate normal-Barbarian per-combat discrepancy, but it does not add the missing
current-XP/lifetime-room dimension.

Current Base AdvC 1.14 and SAS retain the root.

CONSEQUENCE / SEVERITY
----------------------
This is opt-in but direct player-facing misinformation:
- the headline XP field and detailed Experience Range can contradict one another;
- the range can advertise XP outcomes that are impossible because the unit has already exhausted
  its Animal/Barbarian lifetime XP allowance;
- the error is strategically relevant precisely when a player uses the optional ratio guidance to
  choose an XP-farming combat.

No actual XP award or game state is changed.

REPAIR DIRECTION
----------------
The range display needs to describe *actual awardable XP*, not only the theoretical raw
ratio-to-base-XP function.

At minimum:
- derive each ratio bucket's raw base XP;
- pass it through the same per-combat rule;
- then clamp it by:
      max(0, defender.maxXPValue() - attacker.currentXP)
  for the current defender/attacker context;
- if ExperiencePercent bonuses are intended to be shown in the range, apply the same ordering as
  `CvUnit::changeExperience` and then the final total cap.

When remaining lifetime room is zero, the entire displayed XP range should collapse to 0 rather
than retaining impossible positive neighboring labels.

Best long-term repair: share one executor-aligned XP-award helper between combat resolution and
ACO rather than reconstructing independent formulas in multiple display sections.

RELATION TO F666/F667
---------------------
F666:
- promotion-derived bonus XP remaining-room sign error;
- main Victory/Withdraw XP display;
- practical 2204 regression.

F667:
- wrong normal-Barbarian *per-combat base ceiling* (6 vs real 5);
- main XP and detailed range;
- practical 1253 regression.

F670:
- detailed Experience Range ignores the attacker's *remaining lifetime Animal/Barbarian XP room*;
- persists even when F667's per-combat maximum is corrected;
- present since original K-Mod ACO import.

The three need separate repairs.

DEDUP
-----
Whole-album search through WIP535 found no prior owner for:
- ACO ShowExperienceRange ignoring Animal/Barbarian current-XP caps;
- a main Victory 0/1 XP line contradicted by a 3-XP current-ratio range;
- range thresholds not using remaining lifetime XP room.

F667's WIP note records only the separate 1..6 vs executor 1..5 upper-bound error and does not own
this current-experience dimension.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F671 UNUSED:
- finish survival-overview bar 0/100% visual boundaries;
- finish defender-side XP/ExperiencePercent behavior under normal player-facing ACO preconditions;
- finish attacker low-HP/retreat detail accounting;
- finish defender combat-limit PullOut aggregation beyond F668/F669;
- finish average/unharmed/basic-info outputs;
- audit `setACOModifiersPlotHelp`, especially total-defense modifier arithmetic;
- reconcile all remaining probability symptoms against F602/F637/F638;
- finish current BUG-option activation and later ACO history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F671 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP537 -- QUEUE 043 ACOText.cpp -- F671 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP536.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666 / KI#988: CONFIRMED.
- F667 / KI#989: CONFIRMED.
- F668 / KI#990: CONFIRMED.
- F669 / KI#991: CONFIRMED.
- F670 / KI#992: CONFIRMED.
- F671 / provisional KI#993: CONFIRMED.
- F672 / provisional KI#994: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F671 / provisional KI#993 -- ACO computes capped Animal/Barbarian withdrawal XP but displays the uncapped raw define
----------------------------------------------------------------------------------------------------------------------

ROOT
----
ACO creates a dedicated base withdrawal-XP value:

    int iWithdrawXP = GC.getDefineINT(CvGlobals::EXPERIENCE_FROM_WITHDRAWL);

and, for an Animal or Barbarian defender, correctly clamps that value to the attacker's remaining
lifetime XP room:

    iMaxExtraXP = max(0, XP_CAP - kAttacker.getExperience());
    iWithdrawXP = range(iWithdrawXP, 0, iMaxExtraXP);

Thus a human attacker already at the Animal/Barbarian lifetime XP cap obtains:

    iWithdrawXP = 0

inside ACO itself.

However, neither visible base withdrawal-XP field uses `iWithdrawXP`.

Combat-limit pullout line:
    szTempBuffer.Format(...,
        100.0f * PullOutOdds,
        GC.getDefineINT(CvGlobals::EXPERIENCE_FROM_WITHDRAWL));

Normal retreat line:
    szTempBuffer.Format(...,
        100.0f * RetreatOdds,
        ...,
        GC.getDefineINT(CvGlobals::EXPERIENCE_FROM_WITHDRAWL));

The correctly lifetime-capped `iWithdrawXP` is therefore calculated and then ignored at display
time. It is used only indirectly when ACO computes the promotion-derived bonus cap.

LIVE EXECUTOR CONTRACT
----------------------
Both actual withdrawal forms award experience through the same capped executor.

Normal combat retreat:
    changeExperience(
        EXPERIENCE_FROM_WITHDRAWL,
        pDefender->maxXPValue(),
        true, ...);

Combat-limit pullout:
    changeExperience(
        EXPERIENCE_FROM_WITHDRAWL,
        pDefender->maxXPValue(),
        true, ...);

`CvUnit::maxXPValue()` returns:
- ANIMAL_MAX_XP_VALUE for Animals;
- BARBARIAN_MAX_XP_VALUE for Barbarians.

`changeExperience` ultimately calls `setExperience(current + award, iMax)`, so when the attacker
already has XP equal to that defender-derived maximum, actual gain is zero.

Current inherited values are:
- EXPERIENCE_FROM_WITHDRAWL = 1
- ANIMAL_MAX_XP_VALUE = 5
- BARBARIAN_MAX_XP_VALUE = 10

CURRENT SHIPPED ACTIVATION
--------------------------
The current unit roster supplies many ordinary human-controllable retreat cases. Examples:
- UNIT_WARRIOR: iWithdrawalProb = 5
- UNIT_HORSE_ARCHER: iWithdrawalProb = 20
- UNIT_ARCHER / Longbow family: nonzero withdrawal in current SAS
- numerous mounted/ranged later units also have withdrawal probabilities.

The combat-limit branch is likewise ordinary:
- UNIT_CATAPULT: iCombatLimit = 75
- UNIT_TREBUCHET: iCombatLimit = 75
- UNIT_CANNON: iCombatLimit = 80
- later artillery families use sub-100 combat limits.

ACO itself is a supported BUG option (`ACO__Enabled`); once enabled, the headline
Victory/Withdraw/Retreat/Defeat section is ordinary ACO output and is not dependent on
`ACO__ShowExperienceRange`.

DETERMINISTIC NORMAL-RETREAT FIXTURE
------------------------------------
Take a human Horse Archer at exactly 10 XP attacking an ordinary Barbarian combat unit.

Current Horse Archer:
    iWithdrawalProb = 20.

Because the defender is a normal Barbarian:
    pDefender->maxXPValue() = 10.

Actual retreat award:
    changeExperience(1, 10, ...)
with current attacker XP already 10
    -> actual XP gain = 0.

ACO's own cap block:
    iMaxExtraXP = max(0, 10 - 10) = 0;
    iWithdrawXP = range(1, 0, 0) = 0.

But the visible Retreat line formats:
    EXPERIENCE_FROM_WITHDRAWL = 1

and therefore tells the player a successful retreat earns 1 XP.

A current Warrior can provide the same root with its 5% withdrawal probability.

DETERMINISTIC COMBAT-LIMIT FIXTURE
----------------------------------
Take a human Catapult at exactly 10 XP attacking an ordinary Barbarian defender.

Current Catapult:
    iCombatLimit = 75.

If it reaches the combat-limit pullout:
- executor uses `changeExperience(1, defender.maxXPValue()==10, ...)`;
- actual gain is 0;
- ACO computes `iWithdrawXP=0`;
- but the visible Withdraw line still prints raw `1 XP`.

Thus both withdrawal mechanisms independently expose the same formatting root.

ANIMALS SHARE THE ROOT
----------------------
At 5 XP against an Animal:
- actual retreat/pullout XP gain is capped to 0;
- ACO computes `iWithdrawXP=0`;
- the visible field still prints 1 XP.

The discrepancy therefore applies to both lifetime-cap families the original cap block was
specifically written to support.

ANCESTRY
--------
This is inherited from the original K-Mod ACO import.

K-Mod practical 101 / d5c68c5392 contains:
- explicit current-XP caps for `iWithdrawXP` against Animals and Barbarians;
- the correct original remaining-room treatment for bonus withdrawal XP;
- but later formats both PullOut and Retreat base XP directly from
  `GC.getDefineINT("EXPERIENCE_FROM_WITHDRAWL")`.

So the original implementation already computes the corrected variable and then fails to use it
for presentation.

AdvC refactors preserved that disconnect. Current Base AdvC 1.14 retains it. Current SAS inherits
it unchanged.

RELATION TO F666 / F670
-----------------------
F671 is independent.

F666:
- promotion-derived *bonus* withdrawal/victory XP uses the wrong remaining-room expression;
- practical 2204 AdvC regression.
Even with no ExperiencePercent promotion at all, F671 still displays 1 base XP when actual gain
is 0.

F670:
- optional ShowExperienceRange ignores lifetime Animal/Barbarian XP room for kill-capable combat;
- original ACO root.
F671 affects the always-visible headline withdrawal/retreat XP field and does not depend on the
optional range.

Fixing F666 and F670 therefore leaves F671 fully active.

CONSEQUENCE / SEVERITY
----------------------
This is direct player-facing combat-tooltip misinformation:
- A unit that cannot legally gain any more XP from an Animal/Barbarian can still be told that
  retreating or pulling out will grant 1 XP.
- The source has already computed the correct zero value, making the display contradiction
  especially clear.

Actual combat XP is unaffected.

NARROW REPAIR
-------------
Use the already-computed `iWithdrawXP` in both visible fields instead of rereading the raw define:

Combat-limit PullOut:
    ... PullOutOdds, iWithdrawXP

Normal Retreat:
    ... RetreatOdds, ..., iWithdrawXP

Then repair F666 separately for the promotion-derived `iBonusWithdrawXP` remaining-room
calculation.

A stronger future-proof cleanup would calculate a single executor-aligned withdrawal award
structure and reuse it for both base and bonus display.

DEDUP
-----
Whole-album search through WIP536 found no prior owner for:
- ACO computing `iWithdrawXP` and then formatting the raw withdrawal define;
- at-cap Barbarian/Animal retreat displaying 1 XP but awarding 0;
- combat-limit PullOut sharing that same base-XP display mismatch.

F666/F667/F670 are adjacent ACO XP findings but have different formulas and activation, as
documented above.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F672 UNUSED:
- finish survival overview and individual bar visual arithmetic;
- finish attacker lowest-HP / retreat probability grouping after F668;
- finish defender combat-limit PullOut grouping and one-hit/multi-hit edge cases;
- finish Average Health / Unharmed and basic info probability accounting;
- finish `setACOModifiersPlotHelp` and total-defense modifier inversions/rounding;
- reconcile all remaining probability symptoms with F602/F637/F638/F669;
- finish current BUG-option activation and later history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F672 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP538 -- QUEUE 043 ACOText.cpp -- F672 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP537.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666-F672 / provisional KI#988-KI#994: CONFIRMED.
- F673 / provisional KI#995: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F672 / provisional KI#994 -- ACO Attacker Unharmed odds omit retreat outcomes when the next defender hit would be lethal
--------------------------------------------------------------------------------------------------------------------------

ROOT
----
ACO initializes the two "Unharmed" probabilities asymmetrically:

    float AttackerUnharmed = getCombatOddsSpecific(
        kAttacker, kDefender, 0, att.hitsToWin());

    float DefenderUnharmed = getCombatOddsSpecific(
        kAttacker, kDefender, def.hitsToWin(), 0);

    DefenderUnharmed += getCombatOddsSpecific(
        kAttacker, kDefender, def.hitsToWin() - 1, 0);
        // attacker withdraws or retreats

The defender statistic explicitly includes a retreat state that leaves the defender at its
current HP.

The attacker statistic includes only attacker victory/combat-limit-pullout terminal states with
zero defender hits. It never includes a normal retreat state.

Ordinarily that omission is harmless because normal retreat occurs after the attacker has already
survived `def.hitsToWin()-1` applied defender hits and is therefore damaged.

But when:
    def.hitsToWin() == 1

then:
    def.hitsToWin() - 1 == 0.

Every successful normal retreat therefore occurs before *any* defender damage has been applied.
The attacker exits combat at exactly its entering HP and is genuinely unharmed by the combat.

LIVE EXECUTOR ORDER
-------------------
`CvUnit::resolveCombat()` handles a potentially lethal defender hit in this order:

    if (getDamage() + iAttackerDamage >= maxHitPoints() &&
        SyncRandSuccess100(withdrawalProbability()))
    {
        ...
        changeExperience(...);
        break;
    }
    changeDamage(iAttackerDamage, ...);

So on successful withdrawal the lethal hit is NOT applied. The unit leaves combat with whatever
damage it already had before that round.

If `def.hitsToWin()==1`, there were zero prior successful defender hits. Therefore its final HP is
exactly its initial HP.

ACO itself agrees about the HP state. It defines:

    E_HP_Att_Retreat =
        kAttacker.currHitPoints()
        - (def.hitsToWin() - 1) * def.damagePerRound();

For `def.hitsToWin()==1` this becomes:

    E_HP_Att_Retreat = kAttacker.currHitPoints();

Thus the same function explicitly knows that one-hit-to-die retreat is an unchanged-HP outcome,
but does not add its probability to `AttackerUnharmed`.

PROBABILITY IDENTITY
--------------------
Normal retreat outcomes are represented by:

    getCombatOddsSpecific(
        kAttacker, kDefender,
        def.hitsToWin() - 1,
        iHitsByAtt)

for all:
    0 <= iHitsByAtt < att.hitsToWin().

ACO later sums exactly those states into:
    RetreatOdds.

When `def.hitsToWin()==1`, every one of those states has:
    iHitsByDef = 0

and therefore leaves the attacker at its starting HP.

The correct attacker-unharmed probability in this case is consequently:

    AttackerUnharmed + RetreatOdds

not merely the current `AttackerUnharmed`.

CURRENT SHIPPED DETERMINISTIC FIXTURE
-------------------------------------
Current UNIT_HORSE_ARCHER:
- iCombat = 6
- iWithdrawalProb = 20
- iFirstStrikes = 0
- iChanceFirstStrikes = 0
- iCombatLimit = 100

Take a human Horse Archer at 1 HP attacking any legal defender that can damage it, e.g. a
full-health ordinary Swordsman.

`Combatant::damagePerRound` is clamped to at least 1 HP, so with current attacker HP=1:
    def.hitsToWin() = ceil(1 / damagePerRound) = 1.

The Horse Archer has a live 20% withdrawal chance.

Any defender-success round that would kill it therefore:
- triggers the withdrawal roll;
- on successful withdrawal, applies no damage;
- leaves the Horse Archer at the same 1 HP.

Those retreat outcomes have positive probability and belong in the attacker "Unharmed" total.
Current ACO omits all of them.

Current UNIT_WARRIOR also supplies the root with its SAS 5% withdrawal probability; Horse Archer
is simply a larger/easier visible fixture.

UI ACTIVATION
-------------
`ACO__ShowUnharmedOdds` is a supported ACO BUG option. Its current list default is 0 (off), so
the statistic is opt-in rather than part of every ACO view.

Once enabled, it is ordinary player-facing combat-tooltip output. No debug mode, malformed XML,
Barbarian control or unsupported unit ownership is required.

ANCESTRY
--------
This defect is present in the original K-Mod ACO import.

K-Mod practical 101 / d5c68c5392 introduces:

    AttackerUnharmed =
        getCombatOddsSpecific(attacker, defender, 0, neededAttackerHits);

    DefenderUnharmed =
        getCombatOddsSpecific(attacker, defender, neededDefenderHits, 0);

    DefenderUnharmed +=
        getCombatOddsSpecific(attacker, defender, neededDefenderHits - 1, 0);
        // attacker withdraws or retreats

The attacker-side retreat counterpart is absent from the initial implementation and remains
absent through later AdvC refactors.

Current Base AdvC 1.14 retains it. Current SAS inherits it unchanged.

WHY THIS IS NOT A SEMANTIC "WAS HIT" INTERPRETATION
----------------------------------------------------
The ACO statistic is implemented from *final HP states*, not from animation/event semantics:
- the defender is counted unharmed when the attacker retreats before landing a damaging hit;
- attacker/defender detailed bars and expected HP treat successful withdrawal as avoiding the
  would-be lethal damage;
- `E_HP_Att_Retreat` explicitly gives the attacker's unchanged current HP when one defender hit
  would kill it.

So omitting a retreat that leaves final HP unchanged is inconsistent with ACO's own surrounding
definition of the metric.

CONSEQUENCE / SEVERITY
----------------------
This is opt-in, low-severity but direct player-facing probability misinformation.

For sufficiently injured withdrawal-capable attackers, the tooltip understates the chance of
leaving combat at exactly the same HP because it reports only no-hit victory/pullout paths and
drops every successful normal retreat.

The game executor and actual combat are unaffected.

NARROW REPAIR
-------------
After `RetreatOdds` has been calculated, add it to AttackerUnharmed only when a normal retreat
leaves the unit at its entering HP:

    if (def.hitsToWin() == 1)
        AttackerUnharmed += RetreatOdds;

A more structural implementation could derive "Unharmed" from the same final-HP distribution used
by the detailed bars, eliminating special-case drift.

DEDUP
-----
Whole-album search through WIP537 found no prior owner for:
- attacker unharmed odds omitting retreat;
- the `def.hitsToWin()==1` unchanged-HP withdrawal state;
- asymmetry between `AttackerUnharmed` and `DefenderUnharmed`.

F668 concerns duplicate defender detailed-health output and uses a no-withdrawal fixture.
F669 concerns floating-point underflow.
F666/F667/F670/F671 concern XP display.
F672 is independent.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F673 UNUSED:
- finish attacker lowest-HP and survival-bar visual accounting after F672;
- finish defender combat-limit PullOut distribution and one-hit/multi-hit edge cases;
- finish unconditional Average Health and confirm all terminal probability mass is represented;
- finish remaining Unharmed/BasicInfo semantics;
- audit `setACOModifiersPlotHelp` total-defense inverse formula, strength-zero preconditions and
  formatting/rounding;
- reconcile remaining probability symptoms against F602/F637/F638/F669;
- finish current BUG-option activation and later ACO history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F673 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP539 -- QUEUE 043 ACOText.cpp -- F673 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP538.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): OPEN.
- F666-F673 / provisional KI#988-KI#995: CONFIRMED.
- F674 / provisional KI#996: UNUSED.
- Files complete remains 68 / 169 because Queue 043 is not closed yet.
- No gameplay/source/XML file was modified.

F673 / provisional KI#995 -- condensed defender-health flush hardcodes "HP" and then appends the localized HP token again
---------------------------------------------------------------------------------------------------------------------------

ROOT
----
Inside the defender detailed-health loop, low-probability adjacent HP outcomes can be merged into a
condensed range. When that accumulated range is flushed *before* a later individually displayed
outcome, current code formats the first endpoint as:

    szTempBuffer.Format(L"%dHP", first_combined_HP_Def);
    szString.append(szTempBuffer.GetCString());
    szString.append(gDLL->getText("TXT_ACO_HP"));

The literal format already contains `HP`, then the localized HP token is appended again.

If the range has a second endpoint, that endpoint is correctly formatted as only the number before
appending `TXT_ACO_HP`:

    szTempBuffer.Format(L"%d", last_combined_HP);
    ...
    szString.append(gDLL->getText("TXT_ACO_HP"));

The later/end-of-loop condensed flush also correctly formats the first endpoint as `%d` without
the hardcoded suffix.

So this is an isolated typo in the mid-loop condensed defender-health flush.

LOCALIZATION ORACLE
-------------------
The original/current ACO text key was introduced as:

    TXT_ACO_HP
        English: HP
        French:  PV
        German:  TP
        Italian: PV
        Spanish: PG

No later supplied history changes that key.

Consequently the defect is not merely an English duplicate:
- English first endpoint becomes e.g. `10HPHP`;
- French becomes `10HPPV`;
- German becomes `10HPTP`;
while the range's second endpoint uses the proper localized unit alone.

CURRENT SHIPPED DETERMINISTIC FIXTURE
-------------------------------------
Use a full-health current UNIT_SCOUT attacking a full-health current UNIT_EXPLORER on a neutral
context without external strength modifiers.

Current XML:
Scout:
    iCombat = 1
    iWithdrawalProb = 0
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iCombatLimit = 100
    bOnlyDefensive = 0
    bOnlyAttackAnimals = 0

Explorer:
    iCombat = 4
    iWithdrawalProb = 0
    iFirstStrikes = 0
    iChanceFirstStrikes = 0
    iCombatLimit = 100
    bOnlyDefensive = 0
    bOnlyAttackAnimals = 0

With no external modifiers:
- attacker strength = 100 internal;
- defender strength = 400 internal;
- defender per-round odds =
      floor(1000 * 400 / 500) / 1000 = 0.8;
- attacker per-round odds = 0.2;
- mean firepower = 250;
- Scout damage to Explorer =
      floor(20 * (100 + 250) / (400 + 250)) = 10 HP;
- Explorer damage to Scout =
      floor(20 * (400 + 250) / (100 + 250)) = 37 HP.

Therefore:
- Scout needs 10 successful hits to win;
- Explorer needs 3 successful hits to win.

With no first strikes/withdrawal, the probability that the Explorer wins after exactly k Scout
hits is the ordinary negative-binomial terminal probability:
    C(3+k-1,k) * 0.8^3 * 0.2^k.

The defender-health loop walks k downward from 9. The current probabilities are approximately:

    k=9 -> 0.00144%
    k=8 -> 0.00590%
    k=7 -> 0.02359%
    k=6 -> 0.09175%
    k=5 -> 0.34406%
    k=4 -> 1.22880%

Current `ACO__MergeShortBars` defaults true, giving:
    HP_percent_cutoff = 0.5%.

Thus:
- k=9..5 are accumulated into `bCondensed`;
- their defender HP values are 10,20,30,40,50;
- k=4 exceeds the cutoff and forces the exact mid-loop `if(bCondensed)` flush.

At that moment:
    first_combined_HP_Def = 10
    last_combined_HP = 50.

English output for the range is therefore effectively:
    `10HPHP-50HP`

before the probability percentage.

French is effectively:
    `10HPPV-50PV`.

This uses ordinary current units and the default merge-short-bars behavior once the defender
detailed-health ACO view is enabled.

UI ACTIVATION
-------------
`ACO__ShowDefenderHealthBars` is a supported ACO list option (current default list value 2).
`ACO__MergeShortBars` defaults true.

So the malformed label is reachable through ordinary ACO detailed-health output; no debug mode or
malformed XML is required.

ANCESTRY
--------
The typo is present in the original K-Mod ACO import.

K-Mod practical 101 / d5c68c5392 contains the same sequence in the mid-loop condensed defender
flush:

    Format(L"%dHP", first_combined_HP_Def);
    append(...);
    append(getText("TXT_ACO_HP"));

The sibling/end-of-loop implementations already use the correct `%d` form, so the original source
contains its own direct oracle.

AdvC later moved/refactored the ACO code but retained the typo. Current Base AdvC 1.14 retains it.
Current SAS inherits it unchanged.

CONSEQUENCE / SEVERITY
----------------------
This is low-severity but deterministic player-facing presentation corruption:
- duplicated HP suffix in English;
- mixed hardcoded-English + localized unit in non-English languages;
- only the first endpoint of a mid-loop condensed defender range is malformed.

Combat probabilities and gameplay state are unaffected.

NARROW REPAIR
-------------
Replace:

    szTempBuffer.Format(L"%dHP", first_combined_HP_Def);

with:

    szTempBuffer.Format(L"%d", first_combined_HP_Def);

and continue using the existing localized:
    TXT_ACO_HP

exactly as the sibling branches already do.

DEDUP
-----
Whole-album search through WIP538 found no prior owner for:
- `%dHP` followed by `TXT_ACO_HP`;
- `HPHP`, `HPPV`, `HPTP` condensed ACO labels;
- the mid-loop defender condensed-health first-endpoint typo.

F668 concerns duplicate *rows* for a one-hit-to-kill defender; F673 concerns a malformed HP suffix
inside a different condensed-range branch. Their fixtures and repairs are independent.

QUEUE-043 CONTINUATION CURSOR
-----------------------------
Continue from F674 UNUSED:
- finish survival-overview bar visual arithmetic and exact endpoint lengths;
- finish attacker lowest-HP and defender PullOut grouping after F668/F672/F673;
- finish unconditional Average Health and terminal-mass accounting;
- finish remaining BasicInfo/UnroundedExperience semantics;
- finish `setACOModifiersPlotHelp` inverse total-defense modifier and strength preconditions;
- reconcile remaining probability symptoms against F602/F637/F638/F669;
- finish current ACO option activation and later history 2963/2971/2985/3392/3903;
- whole-album dedup and final numeric/pointer/index saturation;
- either promote an independent F674 or close Queue 043 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP540 -- QUEUE 043 ACOText.cpp CLOSED / QUEUE 044 CvMessageData.cpp OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP539.
- Queue 043 `CvGameCoreDLL/ACOText.cpp` (1,335 queued lines): CLOSED.
- Confirmed Queue-043 roots: F666-F673 / provisional KI#988-KI#995.
- F674 / provisional KI#996: UNUSED.
- Files complete: 69 / 169.
- Queue 044 `CvGameCoreDLL/CvMessageData.cpp` (1,306 queued lines): OPEN.
- Queue 045 was already completed in an earlier durable checkpoint.
- No gameplay/source/XML file was modified.

QUEUE-043 PROVENANCE CLOSURE
----------------------------
Current SAS `ACOText.cpp` is executable-semantically identical to supplied Base AdvC 1.14 except
for two SAS micro-performance edits caching the Animal and Barbarian maximum-XP global defines in
local `static const int`s. The values and control flow are unchanged. Consequently all Queue-043
roots are inherited ACO/AdvC behavior rather than SAS-only regressions or missed Base-AdvC-1.14
repairs.

ACO correctly calls `combat_odds::initCombatants(..., bHideFreeWins)`, so it does not reproduce
F637's ignored-parameter root. Any ACO probability display fed by the bilateral
chance-first-strike error in `getCombatOddsSpecific` remains owned by F638 unless ACO adds an
independent display/math defect.

QUEUE-043 CONFIRMED ROOT SET
----------------------------
F666 / KI#988 -- promotion-derived XP exceeds remaining Animal/Barbarian lifetime room in display
    AdvC practical 2204 refactored K-Mod's correct:
        cap - (currentXP + baseXP)
    into the wrong:
        remainingRoom + baseXP
    for ACO's promotion bonus XP. Leadership (+100% XP) near the 5/10 lifetime cap can therefore
    be shown gaining more total XP than `CvUnit::changeExperience` can award. The withdrawal bonus
    sibling shares the same root.

F667 / KI#989 -- wrong normal-Barbarian attacker-win per-combat XP ceiling
    ACO uses `MAX_EXPERIENCE_PER_COMBAT - 4` (6 with current MAX=10) when the defender is a normal
    Barbarian, while `CvUnit::resolveCombat` uses `MAX/2` (5). A current Warrior vs Barbarian
    Swordsman can therefore display 6 base XP where the executor awards at most 5. AdvC practical
    1253 introduced the executor's asymmetric advc.312 rule and incorrectly mirrored the
    defender-win `-4` form onto ACO's attacker-win direction.

F668 / KI#990 -- duplicate defender-unhurt detailed-health row for one-hit lethal attacks
    When `att.hitsToWin()==1`, the defender detail loop decrements the dead-state count to zero and
    can directly print the unhurt defender outcome. The post-loop block then unconditionally
    prints that same unhurt state again. Full Swordsman vs 10-HP Tank gives a deterministic
    ~1.23% duplicated row. The original K-Mod ACO already contains an incomplete guard and a
    comment acknowledging the "show up twice" case.

F669 / KI#991 -- single-precision underflow causes conditional expected-HP 0/0
    ACO's whole-combat outcome probabilities are floats, then conditional HP lines divide weighted
    HP by those sums without representational-zero protection. A current 1-HP Scout vs full
    Modern Tank has attacker round chance 0.001 and requires 17 consecutive hits:
        0.001^17 = 1e-51
    which underflows below float's smallest positive subnormal. Both victory probability and its
    weighted HP numerator become 0.0f, so ACO evaluates 0/0 and formats a non-finite conditional
    HP value. Reverse/withdrawal conditional lines share the numeric-stability root.

F670 / KI#992 -- optional Experience Range ignores remaining Animal/Barbarian lifetime XP room
    The headline XP value clamps by currentXP against the 5/10 lifetime cap, but the independent
    ShowExperienceRange reconstruction uses only theoretical combat ratio/per-combat bounds.
    A 10-XP Swordsman vs an equal Barbarian Swordsman can therefore show headline 0 XP while the
    same tooltip's R=1.00 range entry advertises 3 XP. The mismatch remains after F667 is repaired.

F671 / KI#993 -- capped withdrawal XP is computed but the raw define is displayed
    ACO correctly computes and lifetime-clamps `iWithdrawXP`, yet both normal Retreat and
    combat-limit Withdraw formatting reread raw `EXPERIENCE_FROM_WITHDRAWL` (1). A 10-XP Horse
    Archer retreating from a normal Barbarian, or a 10-XP Catapult pulling out at combat limit,
    gains 0 actual XP but is told it will gain 1. Animals at the 5-XP cap share the root.

F672 / KI#994 -- Attacker Unharmed omits normal retreat when one defender hit is lethal
    The defender statistic explicitly includes the zero-damage retreat state, but the attacker
    statistic never adds normal retreat outcomes. When `def.hitsToWin()==1`, successful withdrawal
    happens before the would-be lethal hit is applied, so every retreat leaves the attacker at its
    entering HP. A current 1-HP Horse Archer provides a deterministic supported fixture. ACO's own
    `E_HP_Att_Retreat` formula confirms that the final HP equals current HP in this state.

F673 / KI#995 -- condensed defender-health first endpoint becomes HPHP / HPPV / HPTP
    One mid-loop condensed-range flush does:
        Format(L"%dHP", first_combined_HP_Def);
        append(TXT_ACO_HP);
    while all sibling branches correctly format only `%d` before appending the localized unit.
    Current Scout vs Explorer yields a deterministic default-merge range of 10-50 HP and reaches
    this branch, producing English `10HPHP-50HP`, French `10HPPV-50PV`, etc. Original K-Mod ACO
    contains the typo.

FINAL PROBABILITY-MASS / HEALTH CONTROLS
----------------------------------------
The remaining expected-health accounting was reconciled after the numbered roots:
- unconditional attacker average HP includes all attacker-victory/combat-limit-pullout states plus
  every normal-retreat terminal state; attacker death contributes zero as intended;
- unconditional defender average HP includes defender-kill/attacker-retreat states and
  combat-limit pullout states; defender death contributes zero;
- for a fixed final attacker HP, the bottom detailed bar correctly groups the final
  victory/pullout state with all normal-retreat outcomes occurring after
  `def.hitsToWin()-1` applied defender hits;
- combat-limit defender HP is clamped/aggregated at `iDefenderHitLimit`, matching the executor's
  pullout health floor;
- the compiled-out "abnormal" zero-damage-last-hit branch is unreachable under the current
  `Combatant::hitsToWin` arithmetic and remains guarded by an assertion in the live branch.

F672 remains the sole independent Unharmed-mass omission found beyond the already-owned F638
probability source error.

SURVIVAL OVERVIEW BAR CONTROL
-----------------------------
The inherited overview-bar `pixels` reuse is unusual but algebraically bounded.

Let:
    r1 = round(prob1)
    r2 = round(prob2)
with:
    prob2 >= prob1.

The first green interior count is:
    2*r1 - 1
(the left/right cap convention accounts for the offset).

The yellow count becomes:
    2*r2 - ((2*r1 - 1) + 1)
  = 2*(r2-r1)
which is never negative.

The first section decrements the fixed 199-pixel interior by exactly the emitted green interior;
the yellow section can only consume the nonnegative probability increment, and the remaining red
section fills the balance. Endpoint cases at 0% and 100% retain nonnegative interior counts. The
code is awkward and its old comment expresses understandable doubt, but no current F674 visual
length corruption survives the arithmetic proof.

The lowest-attacker-HP green/yellow bar likewise reserves left/right end pixels explicitly; the
zero-probability and no-retreat branches do not generate a negative drawable interior.

MODIFIER-HELP CONTROL
---------------------
`setACOModifiersPlotHelp` is called only after `setCombatPlotHelp` has resolved a defender and
required:
    pDefender->canDefend(pPlot)
for the actual combat-help path. Thus the defender's base combat strength is positive.

Current `CvUnit::maxCombatStr`:
- returns 0 only when baseCombatStr==0;
- otherwise computes positive/negative modifiers and returns at least 1.

Therefore the total-defense calculations:
positive:
    maxCombatStr / baseCombatStr - 100
negative:
    100 - baseCombatStr*10000 / maxCombatStr
have nonzero denominators on the supported call path.

The negative expression is not a sign bug: it is the algebraic inverse of AdvC/BtS's negative
strength formula:
    maxCombatStr = baseCombatStr*10000 / (100 - modifier).
The display therefore reconstructs the intended negative modifier.

Air attackers can reach the modifier-help routine, but the total-defense calculation is still
about the positive-strength defender. The attacker strength display explicitly uses
`airCurrCombatStrFloat` for DOMAIN_AIR.

CURRENT OPTION / DATA CONTROLS
------------------------------
Current `ACO_config.xml` confirms ACO remains a supported opt-in BUG feature:
- `ACO__Enabled` default false;
- view selection controls remain active once ACO is enabled;
- `ACO__MergeShortBars` defaults true;
- detailed Health/XP/Unharmed options have their current list defaults documented in the finding
  fixtures.
An option being disabled by default was not treated as "dead" when it is a supported player
setting with ordinary native UI activation.

Current promotion data has one nonzero `iExperiencePercent` case:
    PROMOTION_LEADERSHIP = +100%.
No negative ExperiencePercent promotion exists in current data, so no separate negative-XP display
root was manufactured.

`ACO__ShowUnroundedExperience` deliberately presents the raw continuous combat-ratio quantity;
unlike the labelled Victory/Withdraw XP forecast and Experience Range, it is not claimed to be the
post-cap awarded integer XP and was not treated as another lifetime-cap defect.

LATER HISTORY SATURATION
------------------------
Later ACOText history was rechecked after the original/practical roots:
- practical 2854 split the ACO code into ACOText.cpp without changing the audited behavior;
- practical 2963 adds scopes around the bar-pixel locals (no semantic arithmetic change);
- practical 2971 consolidates ACO and BtS combat-modifier text through shared helper functions;
- practical 2985 adjusts unit-help arguments and modifier/first-strike presentation order;
- practical 2998 / later merge/replay strata preserve those changes;
- practical 3392 is the corresponding broad info/refactor history without a new ACO arithmetic
  root;
- practical 3903 correctly changes the normal withdrawal label from Withdraw to Retreat; it does
  not repair or introduce the XP/probability arithmetic findings;
- current SAS edits in this file only cache the two XP-cap global defines.

No independent F674 survived these later semantic strata.

WHOLE-ALBUM DEDUP / CLOSURE
---------------------------
Final dedup was repeated against:
- F602/KI#925 combatLimit executor/predictor mismatch;
- F637/KI#959 ignored bHideFreeWins forwarding;
- F638/KI#960 bilateral chance-first-strike probability bug;
- previous combat, XP, tooltip, localization and bar-display findings.

F666-F673 remain distinct roots with distinct activation/repair boundaries. Queue 043 is therefore
CLOSED with F674 unused rather than forcing another finding.

NEXT EXACT CURSOR
-----------------
C031-WIP540 physical
-> 69 / 169 files complete
-> Queue 043 CLOSED
-> F666-F673 / KI#988-KI#995 confirmed
-> F674 / KI#996 UNUSED
-> Queue 044 `CvGameCoreDLL/CvMessageData.cpp` OPEN
-> establish current SAS vs Base AdvC 1.14 executable-semantic baseline
-> compare K-Mod/Civ4CE message/net-command ancestry
-> inventory all message classes, read/write symmetry, enum/player/unit/city identifiers and
   `Execute` validation
-> focus on network/replay desync risk, stale IDs, authorization/ownership, null lookup handling,
   signed/unsigned serialization, and command-state preconditions
-> trace current EXE/Python/native producers where local source permits
-> whole-album dedup against earlier network/game-action findings
-> promote F674 only for an independent supported root
-> freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP541 -- QUEUE 044 CvMessageData.cpp -- F674 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP540.
- Queue 044 `CvGameCoreDLL/CvMessageData.cpp` (1,306 queued lines): OPEN.
- F674 / provisional KI#996: CONFIRMED.
- F675 / provisional KI#997: UNUSED.
- Files complete remains 69 / 169 because Queue 044 is not closed yet.
- No gameplay/source/XML file was modified.

QUEUE-044 BASELINE SO FAR
-------------------------
Current SAS `CvMessageData.cpp` is executable-semantically identical to supplied Base AdvC 1.14
except for SAS research-observer/acquisition metadata:
- inclusion of SASGameRecordLog;
- player-research-command logging before clear/push;
- explicit TECH_ACQUISITION_FREE_CHOICE metadata on the free-choice setHasTech path.
No SAS serialization-order drift has been found.

Mechanical PutInBuffer / SetFromBuffer reconciliation across the current DLL message classes found
matching active field order and representation, including:
- K-Mod PushOrder fields;
- AdvC CivicMap serialization;
- MovementFlags and `m_bModified` in CvNetPushMission;
- FP test result/player order;
- the two global random-civ/random-leader enum maps.
The old `GAMEMESSAGE_PUSH_MODIFIED_MISSION` enumerator remains vestigial after practical 1448 folded
the modifier bit into GAMEMESSAGE_PUSH_MISSION; current source has no producer/user for the old
message type, so its absent `createMessage` case is retained as dead compatibility residue rather
than a current finding.

F674 / provisional KI#996 -- one-shot FP compatibility flag can let the sender/first matching peer mask incompatible multiplayer machines
----------------------------------------------------------------------------------------------------------------------------------------

ROOT
----
AdvC's multiplayer floating-point compatibility test sends:

    CvMessageControl::getInstance().sendFPTest(FPChecksum());

while:
    !m_bFPTestDone

Each received `CvNetFPTest` calls:

    GC.getGame().doFPCheck(m_iResult, m_ePlayer);

Current `CvGame::doFPCheck` is:

    if (m_bFPTestDone)
        return;
    m_bFPTestDone = true;
    if (iChecksum == FPChecksum())
        return;

    // warn about mismatch ...

Thus the local game examines exactly ONE delivered FP-test message. A matching first checksum
permanently marks the entire compatibility test complete and every later sender is ignored.

SELF-MESSAGE ACTIVATION
-----------------------
The generic `sendMessageData` transport is also the sole mutation path for ordinary local gameplay
commands. For example, changing a commerce percentage sends CvNetPercentChange; the local
CvPlayer state is changed only when CvNetPercentChange::Execute runs. The sender's machine must
therefore execute its own normal game-net messages as part of this transport.

AdvC's own practical-1399 source history states the network contract explicitly while implementing
the then-two-stage modified mission command:

    "All machines receive the push-mission message.
     Everyone except the group owner discards the message.
     The group owner checks the CTRL key and sends a new message around ..."

The group owner is precisely the originating player's machine. This is direct inherited-source
evidence that DLL game messages sent through this channel return to/execute on the sender as well
as the other machines.

`sendFPTest` uses that same `gDLL->sendMessageData` channel and does not exclude the sender.

Therefore a client's own FP-test message has:
    iChecksum == that client's FPChecksum()

and, whenever it is the first FP message processed locally:
1. `m_bFPTestDone` becomes true;
2. the checksum matches;
3. doFPCheck returns silently;
4. every later peer checksum is discarded before comparison.

The exact network ordering of self versus remote messages is EXE-controlled, so do not claim that
self always arrives first. The defect is that a normal supported self-first order causes a false
negative.

MULTI-PEER FALSE NEGATIVE EXISTS EVEN WITHOUT RELYING ON SELF-FIRST ORDER
-------------------------------------------------------------------------
The single bool is insufficient for games with three or more machines even if one chooses not to
assume any particular self-delivery ordering.

Let local machine A compute checksum X.
Let another compatible machine B also compute X.
Let machine C compute incompatible checksum Y.

If A processes B's message first:
    X == X
so `m_bFPTestDone` becomes true and A ignores C's later Y checksum.

The implementation has no per-sender bitset/count and no "keep listening until all other active
network humans have been checked" state. Hence it cannot guarantee that every local client
compares itself with every participating FP behavior.

The displayed warning text is explicitly local:
    "Your machine's FP test computation has yielded a different result than that of %s."
That reinforces that each receiver is intended to detect a mismatch against the sender it is
checking, not merely establish that some arbitrary first pair somewhere matched.

CURRENT ACTIVATION / LIFECYCLE
------------------------------
`m_bFPTestDone` is set to false for network multiplayer:
- at game initialization (`initDiplomacy`);
- after loading a network save.

`CvGame::updateTestEndTurn()` runs during the active player's normal UI-turn update and, while the
flag is false, sends `FPChecksum()` through CvMessageControl.

`CvNetFPTest` serializes both:
- checksum;
- originating PlayerTypes;
and `doFPCheck` uses the sender identity in its mismatch message.

Thus this is ordinary network-multiplayer diagnostic behavior, not a malformed packet or debug-only
entry point.

ANCESTRY / EXACT INTRODUCTION
-----------------------------
AdvC practical 1500 /
    4196f0aed1dbb1fdd93a8165c9d1a6be7f6e5d46
("Floating point compatibility test for multiplayer")
introduced in one change:
- `m_bFPTestDone`;
- `FPChecksum`;
- `doFPCheck`;
- `updateTestEndTurn` sending the checksum while the bool is false;
- CvNetFPTest and its serialization.

The initial implementation already:
    if (b_mFPTestDone) return;
    b_mFPTestDone = true;
    if (iChecksum == FPChecksum()) return;

so the first-message masking is present from introduction rather than caused by a later refactor.

Current Base AdvC 1.14 retains the same one-shot logic.
Current SAS inherits it unchanged apart from unrelated surrounding safety/formatting work.
K-Mod and Civ4CE do not contain AdvC's FP-test addition and therefore serve only as pre-feature
ancestry, not as an implementation oracle.

CONSEQUENCE / SEVERITY
----------------------
This does not cause an out-of-sync by itself. It weakens the diagnostic intended to warn users
before incompatible floating-point behavior produces later desynchronization.

Supported false-negative outcomes include:
- self message is processed first -> the local client only verifies itself;
- 3+ player game -> a compatible first peer masks a later incompatible peer.

A different client may still happen to detect the mismatch depending on message order. Therefore
do not overclaim that every mixed-platform game suppresses every warning. Source proves that the
algorithm is not exhaustive and can silently declare the local test done before an incompatible
participant is examined.

REPAIR DIRECTION
----------------
Track which participating network players/checksums have actually been examined instead of using
one global "first message arrived" bool.

Possible narrow design:
- ignore the active player's own FP message for compatibility completion;
- maintain an EnumMap/bitset of peer PlayerTypes already checked;
- compare every distinct relevant network human's checksum with local FPChecksum;
- keep the send/receive phase active until all required peers are accounted for (or until a
  mismatch has already been reported, if one warning is intentionally sufficient).

If the intent is only to detect whether ANY platform class differs, an alternative is to
synchronize/collect all checksum values centrally and warn when more than one distinct value
exists. Either design must not let a first matching/self checksum terminate the test globally.

DEDUP
-----
Whole-album search through WIP540 found no previous owner for:
- `m_bFPTestDone`;
- `CvNetFPTest`;
- `doFPCheck`;
- first-message/self-message masking of the floating-point compatibility test.

This is independent from ordinary game-state OOS/checksum findings and from the current Queue-044
stale-popup receiver findings already owned elsewhere.

QUEUE-044 NEGATIVE / DEDUP CONTROLS ESTABLISHED BEFORE F674
------------------------------------------------------------
- `CvPlayer::splitEmpire(int)` resolves the current area and re-runs both `canSplitEmpire()` and
  `canSplitArea()`; stale Free Colony area state is not blindly applied by CvNetEmpireSplit.
- `CvPlayer::revolution` rechecks `canRevolution`; stale civic commands are rejected.
- `CvPlayer::convert` rechecks `canConvert`.
- `CvPlayer::launch` rechecks team `canLaunch`.
- `CvPlayer::setCommercePercent` rechecks flexible-commerce permission and clamps 0..100.
- `CvCity::hurry` and `CvCity::conscript` re-run `canHurry` / `canConscript`.
- `CvPlayer::raze` re-runs `canRaze`; the earlier popup audit already owns the distinct
  Return/Liberate stale-target problems.
- CvNetResearch free-choice staleness is already owned by the earlier free-tech entitlement
  finding.
- CvNetChooseElection, CvNetDiploVote, CvNetChangeWar, CvNetEventTriggered and
  CvNetExtendedGame have known stale-popup/consumer roots elsewhere in the album; do not renumber
  those receiver symptoms in Queue 044.
- `CvNetCivLeaderSetup::m_ePlayer` is currently unused/not serialized, but the payload itself is
  deliberately global (two full per-player random-choice maps). Host/non-host emission semantics
  remain under review; no defect is claimed from the unused field alone.
- Missing enum/bounds authentication for forged or corrupt packets is generic network hardening,
  below the current-supported-input finding threshold unless a normal producer can generate the
  bad state.

QUEUE-044 CONTINUATION CURSOR
-----------------------------
Continue from F675 UNUSED:
- finish CvNetCivLeaderSetup host/non-host lifecycle and practical-2532/3579 history;
- finish current `foundReligion` receiver dedup against religion-slot findings;
- finish city DoTask/Gift/Disband and production Pop/Push current-state semantics;
- finish unit JoinGroup/PushMission/AutoMission/DoCommand identity/group-rebinding semantics;
- finish EspionageChange, ChangeVassal and player/team-transition behavior;
- finish Advanced Start and ModNetMessage ownership/contracts;
- audit all Execute methods for supported stale object-generation IDs rather than forged packets;
- complete practical history 1399/1448/1461/1499/1500/2416/2551/2779/3183/3218/3579;
- whole-album dedup and final serialization/pointer/index/state saturation;
- either promote an independent F675 or close Queue 044 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP542 -- QUEUE 044 CvMessageData.cpp -- F675 CONFIRMED / QUEUE STILL OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP541.
- Queue 044 `CvGameCoreDLL/CvMessageData.cpp` (1,306 queued lines): OPEN.
- F674 / KI#996: CONFIRMED.
- F675 / provisional KI#997: CONFIRMED.
- F676 / provisional KI#998: UNUSED.
- Files complete remains 69 / 169 because Queue 044 is not closed yet.
- No gameplay/source/XML file was modified.

F675 / provisional KI#997 -- delayed JoinGroup conflates a dead/missing target head with the explicit "ungroup" sentinel
------------------------------------------------------------------------------------------------------------------------

ROOT
----
`CvNetJoinGroup` serializes:
- the acting PlayerTypes;
- the unit to regroup;
- the numeric player-local unit ID of the intended target group's head.

The ordinary grouping producer deliberately uses:
    FFreeList::INVALID_INDEX
as a semantic command value meaning "remove this unit from its current group / make it separate."

Examples in current `CvGame::selectionListGameNetMessage` and `CvGame::selectUnit` call:

    sendJoinGroup(pUnit->getID(), FFreeList::INVALID_INDEX);

for explicit degrouping.

For an actual join request they instead send:
    sendJoinGroup(pUnit->getID(), pHeadSelectedUnit->getID());

`CvNetJoinGroup::Execute` does not preserve that distinction:

    CvUnit* pUnit = GET_PLAYER(m_ePlayer).getUnit(m_iUnitID);
    if (pUnit != NULL)
    {
        CvUnit* pHeadUnit = GET_PLAYER(m_ePlayer).getUnit(m_iHeadID);
        if (pHeadUnit != NULL)
            pUnit->joinGroup(pHeadUnit->getGroup());
        else
            pUnit->joinGroup(NULL);
    }

Therefore two semantically different receive states collapse to the same action:

A. `m_iHeadID == FFreeList::INVALID_INDEX`
   -> intentional "ungroup" request
   -> `getUnit(INVALID_INDEX)==NULL`
   -> `joinGroup(NULL)` as intended.

B. `m_iHeadID` was a real target unit ID when sent, but that unit no longer exists when executed
   -> stale failed JOIN request
   -> generation-safe `getUnit(oldRealID)==NULL`
   -> the exact same `joinGroup(NULL)` is executed.

The receiver never asks whether the serialized ID itself was the intentional sentinel.

CURRENT STABLE-ID BEHAVIOR MAKES THE BUG PRECISE
-------------------------------------------------
Current player unit IDs are generation-bearing FFreeListTrashArray IDs. If the original target
head dies and its physical slot is later reused, the old full ID does not resolve to the new unit.
`CvPlayer::getUnit/getAt` returns NULL for the stale generation.

Thus this is not a "stale ID may accidentally hit another unit" claim. The normal generation
protection works correctly. The defect is what CvNetJoinGroup decides NULL means afterward:
"the JOIN target disappeared" is interpreted as "the user requested UNGROUP."

SUPPORTED SIMULTANEOUS-TURN ACTIVATION
--------------------------------------
No malformed packet or private API is required.

Use human player A with:
- a target selection group containing head H plus surviving member T;
- another unit U, optionally already grouped with some other friendly unit V.

A's UI issues an ordinary command to add U to H's selected group. Current grouping code sends:

    CvNetJoinGroup(A, U.id, H.id)

Before that message executes in synchronized game order, an enemy player in simultaneous
multiplayer kills H. T survives.

This kind of in-flight unit invalidation is explicitly part of the current engine model.
`CvUnit::kill()` contains K-Mod's source comment:

    "With simultaneous turns, a unit can be captured while
     trying to execute an attack order. (eg. a bomber)"

and the surrounding code is written to tolerate the unit disappearing while a command/state
transition is underway. Queue 044's generic message transport is also the normal synchronized
path for local and remote gameplay commands; F674's transport proof independently establishes
that these game-net messages are delivered/executed through that shared channel rather than
mutating only at the producer.

When H dies:
- H's `kill()` calls `joinGroup(NULL, false, false)`;
- that removes H from its old selection group;
- T remains in the group and becomes its surviving head/member;
- H's generation-bearing unit ID no longer resolves.

When A's pending CvNetJoinGroup then executes:
- U still resolves normally;
- H does not resolve;
- receiver executes `U.joinGroup(NULL)`.

Instead of joining the still-existing target group represented by T -- or at minimum abandoning
the failed join without changing U -- the command DETACHES U from its current group.

If U was grouped with V, the stale failed join therefore actively mutates an unrelated existing
group relationship: U leaves V even though the user never asked to ungroup it.

If U was already alone, the bug can be less visible, but the command still changes meaning from
"join target group" to "remain/create ungrouped state."

TARGET GROUP SURVIVAL
---------------------
`CvSelectionGroup::removeUnit` removes only the dying H node. With T still present the target
selection group remains valid; there is no need for the entire intended destination group to have
disappeared.

The receiver nevertheless stores only H's unit ID, not a stable group identity or fallback member.
Once H is gone it cannot recover T's group.

The finding therefore has two layers:
1. representation weakness: a group is named solely by one member/head whose lifetime can end;
2. definite receiver bug: failure to resolve a real non-sentinel target is treated as an explicit
   user degroup command rather than a failed/stale join.

The second layer has a clear narrow repair even without redesigning group identity.

CURRENT PRODUCERS
-----------------
Current supported grouping paths send real head IDs, including:
- `CvGame::selectionListGameNetMessage(GAMEMESSAGE_JOIN_GROUP)`;
- `CvGame::selectUnit` when explicitly adding/toggling a unit into the currently selected group;
- healthy-unit selection/grouping;
- other K-Mod selection-list grouping helpers.

The explicit degroup paths in the same code use `FFreeList::INVALID_INDEX`, proving that the
serialized integer already contains enough information to distinguish "intentional no target"
from "real target was lost."

ANCESTRY
--------
The same `CvNetJoinGroup::Execute` shape is present in:
- supplied Civ4CE/BtS;
- supplied K-Mod 1.46;
- supplied Base AdvC 1.14;
- current SAS.

All four resolve `m_iHeadID` and call `joinGroup(NULL)` whenever that resolution returns NULL,
without checking whether `m_iHeadID` itself was the explicit invalid-index sentinel.

This is therefore inherited Firaxis/BtS-era message semantics, not a SAS regression and not a
missed Base-AdvC-1.14 repair.

CONSEQUENCE / SEVERITY BOUNDARY
-------------------------------
This is a synchronized gameplay-command semantic error, not memory corruption:
- a supported JOIN command can become an UNGROUP command because its target head died before
  execution;
- the joining unit can be detached from a valid previous group;
- the intended surviving target group is not joined.

Exact frequency depends on simultaneous-move/network ordering and combat timing. Do not claim that
every head death races every join request. Source establishes an ordinary supported ordering in
which the stale real target is possible and that the receiver gives that state the wrong semantic
meaning.

NARROW REPAIR
-------------
Preserve the existing explicit-sentinel contract:

    if (m_iHeadID == FFreeList::INVALID_INDEX)
        pUnit->joinGroup(NULL);       // intentional degroup
    else
    {
        CvUnit* pHeadUnit = GET_PLAYER(m_ePlayer).getUnit(m_iHeadID);
        if (pHeadUnit != NULL)
            pUnit->joinGroup(pHeadUnit->getGroup());
        // else stale real JOIN target: no-op rather than degroup
    }

That surgical fix prevents a failed join from becoming an unrelated destructive operation.

A stronger redesign could serialize a stable selection-group identity in addition to/instead of
the head unit, allowing the join to find the surviving group after head death. Such a group-ID
solution would need the same generation/lifetime scrutiny used elsewhere in the album. The safe
minimal behavior when the real target cannot be recovered is simply no-op.

DEDUP
-----
Whole-album search through WIP541 found no prior owner for:
- CvNetJoinGroup treating a missing real target head as the ungroup sentinel;
- a head dying between grouping command send and Execute;
- a failed JOIN detaching the source unit from another group.

Nearby findings are distinct:
- F154 concerns automation cancellation leaking across cargo carried by separate transport groups;
- F315 concerns a Delete confirmation later operating on a replacement current UI selection;
- existing generational-ID controls establish that stale unit IDs resolve NULL rather than aliasing
  new objects, which is a premise of F675 rather than its root.

QUEUE-044 ADDITIONAL CLOSURE CONTROLS SINCE WIP541
---------------------------------------------------
CvNetCivLeaderSetup:
- `m_ePlayer` is indeed unused and not serialized;
- practical 3579 proves the actual message is intentionally GLOBAL and HOST-ONLY:
  `CvInitCore::reRandomizeCivsAndLeaders` is documented "Only executed by the host";
  `CvGame::setInitialItems` sends one full random-civ/random-leader bitmap only when local InitCore
  contains the host-known randomized-choice markers;
- the EXE syncs most setup data but not the was-randomly-chosen metadata, which is exactly why the
  later host message exists.
No competing non-host empty-map producer is established; unused `m_ePlayer` stays dead payload
metadata, not a finding.

CvNetChangeVassal:
- current tree contains the send wrapper but no current in-tree C++/Python caller;
- Execute does not perform a full current legality test, but without a normal current producer this
  remains legacy/API hardening below threshold.

CvNetEspionageChange:
- current Python producer sends a current target TeamTypes from the Espionage Advisor;
- setter clamps weight to [0,99];
- Permanent Alliance can leave stale per-TeamTypes weight slots, but the previously-audited live
  espionage allocator iterates only alive OTHER teams, excluding both absorbed dead team slots and
  the player's own surviving team;
- no independent gameplay-allocation root was established from a delayed weight message.

City/unit object identity:
- current city/unit IDs are generation-safe FFreeListTrashArray IDs; destruction/reuse does not
  silently rebind a pending message to a newly allocated object in the same physical slot.
- Queue 044 therefore focuses on receiver semantics after NULL/current-object resolution, not a
  nonexistent raw-slot alias.

QUEUE-044 CONTINUATION CURSOR
-----------------------------
Continue from F676 UNUSED:
- finish DoTask Gift/Cede/Disband/working-state classification and dedup against popup/deal roots;
- finish PushOrder/PopOrder stale ordinal/current-queue semantics;
- finish PushMission/AutoMission/DoCommand group-state rebinding after F675;
- finish FoundReligion receiver only for independent roots beyond existing religion-slot owners;
- finish AdvancedStart / ModNetMessage / Ping special-domain paths;
- finish all 28 Execute methods mechanically for supported stale state;
- finish practical-history saturation (1399/1448/1461/1499/1500/2416/2551/2779/3183/3218/3579);
- whole-album dedup and final serialization/pointer/index/state saturation;
- either promote an independent F676 or close Queue 044 cleanly;
- freeze the next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP543 -- QUEUE 044 CvMessageData.cpp CLOSED / QUEUE 046 CyGame.cpp OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP542.
- Queue 044 `CvGameCoreDLL/CvMessageData.cpp` (1,306 queued lines): CLOSED.
- F674 / KI#996: CONFIRMED.
- F675 / KI#997: CONFIRMED.
- F676 / provisional KI#998: UNUSED.
- Files complete: 70 / 169.
- Queue 046 `CvGameCoreDLL/CyGame.cpp` (1,268 queued lines): OPEN NEXT in resumed ordinary queue order.
- No gameplay/source/XML file was modified.

QUEUE-044 FINAL PROVENANCE / SERIALIZATION SATURATION
------------------------------------------------------
The supplied current SAS and Base AdvC 1.14 `CvMessageData.h` become byte-for-byte identical after
comments and whitespace are removed (same normalized length and SHA-256). A balanced-function
comparison of `CvMessageData.cpp` likewise finds the current message implementation executable-
semantic-identical to Base AdvC 1.14 except for SAS's current research-observer/GameRecord hook in
`CvNetResearch::Execute`. No missed Base-1.14 message fix or current SAS protocol regression remains
hidden behind formatting churn.

All 28 current serialized CvNet message classes were mechanically reconciled again at closure:
- every ordinary `PutInBuffer` has the same active field count and field order as its
  `SetFromBuffer` partner after accounting for enum pointer casts;
- `CvNetCivLeaderSetup`'s two custom enum-map writes pair with the same two enum-map reads in the
  same order;
- no current message has an asymmetric extra/missing field;
- the vestigial old `GAMEMESSAGE_PUSH_MODIFIED_MISSION` enum remains dead compatibility residue from
  the practical-1399 -> practical-1448 transition, not an unserialized live message.

FINAL 28-EXECUTE CURRENT-STATE MATRIX
-------------------------------------
Every current `Execute` method was revisited for supported stale-state behavior rather than forged
or corrupt packets. The closure classification is:

1. `CvNetExtendedGame` -- stale popup/session semantics already owned by the earlier popup audit.
2. `CvNetAutoMoves` -- idempotent current-player state setter; no independently retargetable object.
3. `CvNetTurnComplete` -- idempotent player end-turn state setter; no independent stale target root.
4. `CvNetPushOrder` -- generation-safe city lookup plus current `pushOrder` legality checks for
   Train/Construct/Create/Maintain.
5. `CvNetPopOrder` -- generation-safe city lookup; `popOrder(iNum)` walks the CURRENT queue and
   returns when the ordinal is no longer present. The ordinary K-Mod move-order bundle sends Pop
   then Push in synchronized order and deliberately relies on that current queue transition.
6. `CvNetDoTask` -- generation-safe city lookup. Consequential Raze/Hurry/Conscript revalidate in
   their authoritative routines; Liberation stale-recipient/payload roots are already F316/F317;
   `TASK_GIFT/TASK_CEDE` have no current game-net producer (deal execution calls `doTask` directly);
   working-plot helpers re-evaluate current workability or no-op on a missing plot.
7. `CvNetUpdateCivics` -- `revolution` rechecks current revolution legality.
8. `CvNetResearch` -- ordinary research rechecks `canEverResearch`; free-choice staleness is already
   F313/KI#636 and is not renumbered here.
9. `CvNetEspionageChange` -- current TeamTypes weight slot plus bounded setter; absorbed/dead-team
   weights are excluded by the live allocator as already controlled in WIP542.
10. `CvNetAdvancedStartAction` -- authoritative Advanced Start routines re-evaluate current points,
    ownership/cost/action state throughout. The genuinely unsafe Unit/Route destructive fallback is
    already F485/KI#808 from the CvPlayer audit, including its normal stock-screen producer; Queue
    044 therefore does not duplicate that receiver symptom.
11. `CvNetModNetMessage` -- intentionally generic five-int mod callback. Current shipped SAS Python
    use is Strategy Overlay add/remove-city overlay synchronization, not authoritative gameplay
    object mutation by this C++ receiver. Generic extension misuse remains API policy, not F676.
12. `CvNetConvert` -- `CvPlayer::convert` rechecks `canConvert`.
13. `CvNetEmpireSplit` -- `splitEmpire` re-resolves current area and rechecks split legality.
14. `CvNetFoundReligion` -- current religion choice collision/retry behavior does not create a new
    message root; orphaned/reserved-slot failures are already F320/KI#643 and F480/KI#803.
15. `CvNetLaunchSpaceship` -- `CvPlayer::launch` rechecks current launch legality.
16. `CvNetEventTriggered` -- delayed event choice/application roots already have earlier owners;
    no independent message-layer identity root survives.
17. `CvNetJoinGroup` -- F675/KI#997 is the independent receiver root: missing real target head is
    conflated with the explicit INVALID_INDEX ungroup sentinel.
18. `CvNetPushMission` -- late resolution from the serialized head unit to its CURRENT group is
    required by the current UI protocol: when the UI selection does not mirror one selection group,
    JoinGroup messages are deliberately sent first and the mission is then sent only for the head.
    Executing against the synchronized post-join group is therefore intentional, not a stale-group
    bug by itself.
19. `CvNetAutoMission` -- same intentional post-JoinGroup current-group contract as PushMission;
    dead anchor IDs no-op through generation-safe lookup.
20. `CvNetDoCommand` -- command execution re-runs `canDoCommand`; K-Mod/Unofficial-Patch's saved
    `eUpgradeType` protects the Alt/GetAll loop against the anchor pointer changing while units are
    upgraded. No separate supported wrong-group/current-type root cleared the finding threshold.
21. `CvNetPercentChange` -- current commerce-flexibility validation and 0..100 clamping remain in
    the player setter.
22. `CvNetChangeVassal` -- no current in-tree C++/Python producer; missing broad receiver legality
    remains legacy/API hardening rather than a supported F676 path.
23. `CvNetChooseElection` -- stale-popup/vote lifecycle roots already owned elsewhere.
24. `CvNetDiploVote` -- stale-popup/vote lifecycle roots already owned elsewhere.
25. `CvNetChangeWar` -- direct `declareWar` receiver is the consumer already involved in the
    F311/KI#634 stale vassal-tribute popup root. Current in-tree producers are those popup paths;
    no independent ordinary producer was found, so defense-in-depth receiver hardening is not
    renumbered.
26. `CvNetPing` -- relation is evaluated at execution time and the consequence is presentation-only
    ping visibility; no gameplay mutation root.
27. `CvNetFPTest` -- F674/KI#996 owns the one-shot first-message/self-message compatibility masking.
28. `CvNetCivLeaderSetup` -- practical 3579 establishes the payload as intentionally global and
    host-originated; `m_ePlayer` remains unused metadata, not a missing per-player execution gate.

CITY / ORDER / GROUP CLOSURE DETAILS
------------------------------------
`CvNetDoTask` did not yield F676:
- `TASK_RAZE` reaches `CvPlayer::raze`, which begins by rechecking `canRaze`.
- `TASK_HURRY` and `TASK_CONSCRIPT` re-run `canHurry` / `canConscript` immediately before mutation.
- `TASK_DISBAND` is the inherited decline-an-involuntarily-acquired-city path already examined in
  the CvPlayer audit. If the old city is captured/destroyed before delivery, its generation-bearing
  owner-local city ID no longer resolves. No normal remote transition was found that keeps the old
  object live while changing this into a different destructive entitlement.
- `TASK_GIFT` / `TASK_CEDE` are used directly by deal execution and have no current `sendDoTask`
  gameplay producer; exposing their raw task enum through generic Python is not enough to promote a
  normal-input network finding.
- `TASK_CHANGE_WORKING_PLOT` calls `alterWorkingPlot`, which re-resolves the current city plot and
  current `canWork` state; `TASK_CLEAR_WORKING_OVERRIDE` resolves the current plot and no-ops if it
  is absent.

`CvNetPushOrder` / `CvNetPopOrder` did not yield F676:
- production additions are revalidated against current city legality in `pushOrder`;
- invalid/missing pop ordinals return without mutation;
- the K-Mod queue-move producer snapshots the intended order, sends Pop, then sends Push with the
  new position. Those messages are a deliberate same-command synchronized bundle; treating the
  later Push against the current post-Pop queue is required behavior.
- city capture/destruction invalidates the old owner-local generation ID rather than rebinding the
  command to a replacement city.

`CvNetPushMission` / `CvNetAutoMission` current-group rebinding was explicitly challenged after
F675. It closes as intended protocol behavior. `CvGame::selectionListGameNetMessage` first emits
JoinGroup messages whenever the UI selection is not already one real group, then emits a single
mission for the selected head. The receiver must therefore resolve that unit's group at execution
time to see the grouping changes that were just synchronized. A blanket "serialize old group"
repair would break this supported multi-selection command path.

`CvNetDoCommand` also closes. Individual unit IDs are generation-safe and every actual command
passes through `CvUnit::doCommand`, whose first gate is current `canDoCommand`. For Alt + commands
whose CommandInfo has `bAll`, the inherited Unofficial Patch deliberately saves the anchor's unit
TYPE before iterating because upgrade can replace/change the anchor pointer during the loop. The
receiver's current-type choice can become stale only through an owner-side command transition; no
independent supported transition was found that makes it apply a valid command to an unintended
cohort rather than simply becoming invalid/no-op. Do not promote that theoretical scheduling
shape without a concrete producer/repro.

ADVANCED START / MODNET / RELIGION / PING CLOSURE
--------------------------------------------------
Advanced Start was deliberately not declared clean merely because `CvNetAdvancedStartAction` is a
thin wrapper. Its destructive Unit/Route remove fallback is real, current and supported -- but it
is already F485/KI#808, with the stock Advanced Start screen, Hotseat/sequential activation,
foreign revealed plots, validation failure and destructive fallback fully proven there. Reusing
that symptom as F676 would violate one-root/one-finding dedup.

Current shipped `sendModNetMessage` Python usage is the Strategy Overlay's add/remove dot-map city
markers. The C++ message is explicitly a modder extension payload and its receiver merely forwards
five ints to the Python event reporter. There is no C++ object identity to stale-rebind in this
class itself.

`BUTTONPOPUP_FOUND_RELIGION` sends the chosen ReligionTypes plus the reserved slot ReligionTypes.
`CvPlayer::foundReligion` handles a religion that has become founded by reopening/rechoosing for a
human, while the serious slot-ownership failures are already the earlier religion findings. No
new independent Queue-044 receiver root survives.

`CvNetPing` evaluates same-team/vassal visibility against current teams when executed. Even if the
relationship changes while a ping is in flight, the result is a transient UI ping visibility
choice rather than synchronized gameplay state corruption; keep below the finding threshold.

PRACTICAL-HISTORY SATURATION
----------------------------
The remaining CvMessageData history checklist was read from the supplied local commit-diff mirror:
- practical 1399 (`bb97a41a85`): introduced the temporary two-stage modified-mission network
  protocol and explicitly documented sender/group-owner receive behavior; useful transport oracle
  for F674, but no separate surviving current message class.
- practical 1448 (`a8812e96d7`, "Fixed pre-build command in multiplayer"): folded the modifier bit
  into `CvNetPushMission` and removed the temporary `CvNetPushModifiedMission`; current active
  serialization contains `m_bModified` symmetrically.
- practical 1461 (`810c41e85f`, "Bugfix: Orders cancel automation"): changed the received manual
  mission call's `bManual` argument from false to true so player-issued orders cancel automation;
  current behavior matches that explicit fix.
- practical 1499 (`3e233e6794`): retained the Unofficial-Patch `eUpgradeType` snapshot in
  `CvNetDoCommand` so the Alt/GetAll iteration does not reread a pointer/type that an upgrade can
  change. The apparent oddity is the fix, not a regression.
- practical 1500 (`4196f0aed1`): introduced the FP compatibility message/test and its one-shot
  completion flag; this is the exact F674 lineage.
- practical 2416 (`b9b15b001b`): converted civic message representation from a vector/raw loop to
  `CivicMap`; current write/read and `revolution` consumers agree.
- practical 2551 (`50ce0a8733`): broad refactor/formatting plus typed `MovementFlags` propagation;
  current serialization and mission call use the same field and representation.
- practical 2779 (`cd433d2680`): constructor formatting/refactoring only in CvMessageData.
- practical 3183 (`59a3e31900`): enum-map rewrite changed CivicMap `Write/Read` API spelling to
  `write/read`; paired serialization remains symmetric.
- practical 3218 (`40060514c0`): active-player/helper simplification and local cleanup in this file;
  no retained semantic Queue-044 defect beyond already-owned consumers.
- practical 3579 (`4977b513da`): introduced the global civ/leader-random-choice metadata message for
  True Starts integration; current host-only producer/lifecycle was resolved in WIP542.

FINAL DEDUP / NEGATIVE BOUNDARY
-------------------------------
Whole-album searches were repeated around:
- `sendDoTask`, Raze/Disband/Liberate/Gift/Cede;
- `CvNetFoundReligion` and religion-slot ownership;
- `CvNetChangeWar` and stale declare-war popup consumers;
- `CvNetResearch` free-tech entitlement;
- Advanced Start removal fallbacks;
- JoinGroup/group identity;
- vote/event/extended-game delayed replies.

All substantial surviving overlaps have prior owners where noted above. Missing bounds/authentication
for maliciously forged enum/player/team IDs remains generic network hardening, not a normal-producer
finding. No seventh independent Queue-044 root cleared the album threshold.

QUEUE-044 FINAL RESULT
----------------------
Queue 044 closes with exactly two new roots:
- F674 / KI#996 -- one-shot FP compatibility flag can let a first matching/self checksum mask a
  later incompatible multiplayer machine;
- F675 / KI#997 -- stale real JoinGroup target-head failure is treated as the explicit ungroup
  sentinel and can detach the source unit from a valid group.

F676 / provisional KI#998 remains UNUSED.

QUEUE-046 OPENING STONE
-----------------------
Ordinary queue order resumes at Queue 046 `CvGameCoreDLL/CyGame.cpp` because Queue 045 is already
complete and all explicit priority-next-WIP overrides were consumed earlier.

Local comparison material has already been staged from the supplied archives:
- current SAS `CvGameCoreDLL/CyGame.cpp`;
- Base AdvC 1.14 `CvGameCoreDLL/CyGame.cpp`;
- K-Mod 1.46 `CvGameCoreDLL/CyGame.cpp`;
- Civ4CE/BtS `CvGameCoreDLL/CyGame.cpp`.

The current file contains additional SAS-facing wrapper surface beyond Base/K-Mod/BtS, so do not
infer semantic divergence from raw line/hash differences. The next pass should establish a
function-level current-vs-Base delta map first, then audit Python-exposed lifetime/null/enum/index
contracts and any SAS-only wrappers before history archaeology.

EXACT CONTINUATION
------------------
C031-WIP543 physical -> Queue 044 CLOSED -> F674-F675 confirmed -> F676/KI#998 UNUSED ->
Queue 046 `CyGame.cpp` OPEN -> build function-level SAS-vs-Base/K-Mod/Civ4CE map -> classify SAS-only
wrappers and Python-exposed pointer/index/lifetime surfaces -> path-history archaeology and whole-
album dedup -> promote only a concrete supported F676, otherwise continue cleanly -> freeze next
physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP544 -- QUEUE 046 CyGame.cpp -- FUNCTION-DELTA / PYTHON-CONTRACT BASELINE
===============================================================================

CONTINUATION STATE
------------------
- Parent physical stone: C031-WIP543.
- Queue 044 remains CLOSED with F674-F675 confirmed.
- Queue 046 `CvGameCoreDLL/CyGame.cpp` remains OPEN.
- F676 / provisional KI#998 remains UNUSED.
- No gameplay/source/XML file was modified.

FUNCTION-LEVEL PROVENANCE BOUNDARY
----------------------------------
A balanced-function parser was run over the supplied current SAS, Base AdvC 1.14, K-Mod 1.46 and
Civ4CE copies of `CyGame.cpp` rather than comparing raw file hashes/line counts.

Current SAS contains 248 `CyGame` function bodies. Base AdvC 1.14 contains 236 matching current
signatures. After removing comments and whitespace, ALL 236 common bodies are executable-semantic
identical between current SAS and Base AdvC 1.14.

Thus current Queue-046 divergence is unusually small and explicit: exactly 12 current SAS-only
wrappers, with no Base-only counterpart:

1. `isLandHeavyMapnameCached()`
2. `isNavalHeavyMapnameCached()`
3. `getInitialActivePlayer()`
4. `getNumSASVersionHistoryEntries()`
5. `getSASVersionHistoryDirtyState(int)`
6. `getSASVersionHistoryTurn(int)`
7. `getSASVersionHistoryCommitHash(int)`
8. `getSASVersionHistoryVersion(int)`
9. `getInitialMapRandSeed()`
10. `getInitialSyncRandSeed()`
11. `endAIAutoPlay(int)`
12. `logSASGameRecordRejectedAIOffer(...)`

This means a new F676 is overwhelmingly likely to be either:
- an inherited Python-wrapper contract defect already present in Base AdvC 1.14; or
- a bug inside one of those 12 explicit SAS additions.
There is no hidden current-SAS rewrite of an ordinary Base wrapper to blame.

SAS-ONLY WRAPPER PASS -- NO F676
--------------------------------
The 12 SAS-only functions were traced through their current callees and shipped Python producers.
No supported defect has survived so far.

Map classification:
- `isLandHeavyMapnameCached` and `isNavalHeavyMapnameCached` are read-only forwarding wrappers to
  the same cached classification consumed by the DLL. No recomputation or alternate string path is
  introduced by the Python bridge.

Fast-save identity/seeds:
- `getInitialActivePlayer` forwards the serialized initial player identity rather than current
  active player by design.
- `getInitialMapRandSeed` / `getInitialSyncRandSeed` expose the two members of the serialized
  initial RNG-seed pair without mutating current RNG state.
- The already-confirmed Victory Fast Save state-timing bug remains F608/KI#931 in `CvGame`; these
  thin identity/seed getters do not create a second root.

Save-version history:
- all five indexed wrappers forward to `CvGame` accessors that explicitly bounds-check the index;
  invalid indices return `-1` or an empty string rather than indexing the history vector.
- current `SevoPediaHistory.py` and `CvVictoryScreen.py` iterate only
  `range(getNumSASVersionHistoryEntries())`.
No SAS-local out-of-range bridge exists here.

AI Auto Play:
- `endAIAutoPlay(int)` validates the Python integer against the contiguous explicit end-cause range
  `SAS_AUTOPLAY_END_SCHEDULED ... SAS_AUTOPLAY_END_OTHER`, maps anything outside it to OTHER, and
  forwards the validated enum to the explicit-stop `CvGame::setAIAutoPlay` path.
- current `AIAutoPlay.py` is the shipped caller.
No unchecked enum escape or inverted end-cause mapping was found.

Rejected AI-offer GameRecord bridge:
- proposer/responder are bounded to `[0, MAX_PLAYERS)`;
- each Python flat trade list must have even length;
- trade-item enum values are bounded to `[0, NUM_TRADEABLE_ITEMS)`;
- the temporary integer array is deleted on both success and invalid-item exit;
- canonical `TradeData` is rebuilt before entering the C++ GameRecord serializer.
The current Python caller pre-gates the recorder level as documented. No gameplay mutation occurs.

POINTER / NULL WRAPPER SWEEP
----------------------------
The true object-pointer arguments in current `CyGame.cpp` were separated from comment-annotated
enum integers. The sharp inherited wrappers are:

- `cyclePlotUnits(CyPlot*)`
- `selectionListMove(CyPlot*)`
- `cityPushOrder(CyCity*)`
- `setHolyCity(..., CyCity*)`
- `setHeadquarters(..., CyCity*)`

The first three dereference the wrapper pointer without a local NULL guard. This is inherited, not
SAS-local. Current shipped Python has no caller of `cyclePlotUnits`, `selectionListMove`, or
`cityPushOrder`; the already-completed `CvGame` selection audit also established non-NULL plots for
its normal DLL producers. This remains Python/mod API hardening, not a supported F676 chain.

WorldBuilder does call `setHolyCity` and `setHeadquarters`, but its current `CvWBDesc` path passes the
resolved `self.city` object. Clearing has separate `clearHolyCity` / `clearHeadquarters` wrappers.
No shipped `None` producer was found.

Conversely, `getHolyCity` and `getHeadquarters` safely return `new CyCity(NULL)` when no city exists:
`CyCity` explicitly accepts a null `CvCity*` and exposes it through the normal `isNone` wrapper
contract. `getDeal` similarly wraps a nullable deal in a `CyDeal` whose methods test `m_pDeal`.

REPLAY INDEX / LIFETIME PASS
----------------------------
The direct replay field wrappers initially look like unchecked `int i` forwarding. The authoritative
`CvGame` side, however, funnels every field through `isValidReplayIndex(uint)` before indexing:
- bad turn -> -1;
- bad type -> NO_REPLAY_MESSAGE;
- bad plot coordinates -> INVALID_PLOT_COORD;
- bad player -> NO_PLAYER;
- bad color -> NO_COLOR.

`CvInfoScreen.getLastTurn` deliberately asks for index `getNumReplayMessages()-1`; when there are zero
messages this becomes -1 and converts to an out-of-range uint, which the above validity check safely
rejects and returns -1. No underflow crash exists there.

One low-grade API edge remains deliberately below threshold: `CvGame::getReplayMessageText` returns
NULL for an invalid index, while `CyGame::getReplayMessageText` returns `std::wstring` directly from
that pointer. A malicious/unsupported direct Python request for invalid replay text could therefore
construct a string from NULL. Current shipped Python has no direct `CyGame.getReplayMessageText`
caller; normal replay screens use `CyReplayInfo` with bounded message loops. Without a supported bad
index producer this is defensive API debt, not F676.

RISE & FALL CHAPTER INDEX PASS
------------------------------
The five chapter wrappers call `RiseFall::getChapter(chapter)`, whose release build relies on the
caller respecting `0 <= chapter < chapters.size()` (the source has an FAssert, not a release guard).
Shipped callers satisfy that contract:
- `CvVictoryScreen.showRiseFall` loops `range(gc.getGame().getMaxChapters())`;
- `CvDawnOfMan` uses `getCurrentChapter()` only on its Rise & Fall chapter-start presentation path.
- score-breakdown wrappers independently return an empty string when no current chapter exists.

No current ordinary UI route supplying an invalid chapter index was established. Generic console/
mod misuse therefore remains below finding threshold.

NONTRIVIAL INHERITED WRAPPER CONTROLS
-------------------------------------
Several other apparently special bodies were reconciled rather than inferred from their shape:

- `setPlotExtraYield` and `changePlotExtraCost` now resolve coordinates through `CvMap`, return on a
  missing plot, and are superseded in shipped Python by the corresponding `CyMap` methods. No stale
  plot pointer is retained.
- `getNextUnitInCycle` explicitly returns NULL if `CvGame::getCycleButtonUnit` finds no unit before
  allocating a `CyUnit` wrapper.
- `saveGame` is a thin BULL/AdvC engine SaveGame bridge. F608 owns the current Fast Save timing/state
  bug; the wrapper itself does not choose the bad victory timing.
- `addPlayer` remains legacy/mod-mod exposure and has no current shipped Python caller. The deeper
  supported player-slot recycling roots have already been extensively owned by the earlier
  `CvPlayer` / `CvStatistics` findings; do not renumber their consequences in this wrapper queue.
- `reportCurrentLayer` merely casts and forwards. The live wrong `NO_GLOBE_LAYER` Python enum binding
  is already owned by the earlier `CyEnumsInterface` finding; no duplicate F676 here.

HISTORY CHECKLIST OPENED
------------------------
The supplied local `PATH_HISTORY_INDEX` gives the complete reachable `CyGame.cpp` lineage. The
current-SAS additions above map cleanly to practicals 6019, 6119, 6358, 6374, 6381 and the comment-
only 6399 follow-up.

Important older AdvC strata already sampled:
- practical 2429 removes `m_pGame` NULL checks by changing the wrapper singleton to guaranteed
  `CvGame& m_kGame`; this is broad representation cleanup, not evidence that every pointed-to
  CyPlot/CyCity parameter became nullable;
- practical 3746 performs enum-map / plot-extra refactoring, including typed rank casts and moving
  deprecated game-level plot-extra helpers onto `CvMap`;
- practical 1542 adds the BULL save-game bridge;
- practical 1465 adds `reportCurrentLayer` (whose separate Python enum-binding root is already owned);
- practical 1268 adds the Rise & Fall chapter exposure.

The remaining task is to saturate the older semantic history and shipped Python call graph for
argument-domain mismatches (especially rank/vote/options/random/save/replay/R&F), then perform the
whole-album duplicate pass before either closing Queue 046 or promoting a genuinely supported root.

EXACT CONTINUATION
------------------
C031-WIP544 physical -> Queue 046 OPEN -> 248 current wrappers mapped -> 236/236 common Base bodies
semantically identical -> all 12 SAS-only wrappers classified with no F676 -> pointer/null + replay
index + R&F chapter surfaces controlled -> F676/KI#998 UNUSED -> continue older practical-history
strata + shipped Python argument-domain sweep + whole-album dedup -> promote only a concrete ordinary
producer-to-consequence root, otherwise close Queue 046 cleanly -> freeze next physical WIP.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP545 -- QUEUE 046 CyGame.cpp CLOSED / QUEUE 048 CvInfo_Building.cpp OPEN NEXT
===============================================================================

STATUS
------
- Queue 046 `CvGameCoreDLL/CyGame.cpp` is CLOSED for this pass.
- No new finding was promoted.
- F676 / provisional KI#998 remains UNUSED.
- Queue 047 `CvDllPythonEvents.cpp` was already complete from C027, so ordinary queue order resumes at
  Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp`.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified.

QUEUE-046 FINAL SATURATION
--------------------------
The WIP544 baseline established 248 current CyGame wrappers, with all 236 wrappers shared with Base
AdvC 1.14 executable-semantically identical after stripping comments/whitespace. The 12 SAS-only
wrappers were all classified without a supported defect. This continuation completed the remaining
shipped-Python argument-domain and historical strata rather than stopping at that structural result.

RANK / SCORE DOMAIN
-------------------
`getRankPlayer` and `getRankTeam` use AdvC's typed enum-map keys (`PlayerTypes` / `TeamTypes`) even
though their semantic parameter is a rank. Current shipped callers use only bounded rank loops:
- player ranks: `0 .. MAX_CIV_PLAYERS-1`;
- team ranks: `0 .. MAX_CIV_TEAMS-1`.
Those are exactly the map domains. `getPlayerScore` / `getTeamScore` callers likewise pass live or
explicitly bounded player/team IDs. Practical 3746's casts are representation cleanup, not a rank/
identity mix-up on the current call graph.

VOTE / OPTION / ENUM DOMAIN
---------------------------
The Python vote screens derive VoteTypes from `getNumVoteInfos()` loops and VoteSourceTypes from
`getNumVoteSourceInfos()` loops before calling `countPossibleVote`, `getVoteRequired`, secretary-
general, vote-source-religion and vote-timer wrappers. Team eligibility passes bounded team loops.
`isVotePassed` is likewise called only with a vote-info loop index.

Game/MP/forced-control options are consumed through their corresponding enum counts or named enum
constants. The Python-console repair helper `setVictoryValid` has no ordinary shipped caller. The
remaining creation-count / force-civic / event-active style wrappers either have bounded XML-info
callers or no shipped caller. No ordinary cross-enum producer was found.

RANDOM NUMBER BRIDGES
---------------------
`getMapRandNum` / `getSorenRandNum` remain thin wrappers over `CvRandom`. Current Python random-event,
Top Civs and ChangePlayer uses pass positive constants or list lengths whose use sites require the
same list to be nonempty before indexing. The current SAS `CvRandom::getInt(int,...)` additionally
returns 0 for nonpositive input while asserting only on negative ranges, so there is no wrapper-level
zero-range crash to assign here. Any empty-list Python indexing problem would be a Python producer
bug, not a hidden CyGame bridge defect; none was established in the shipped call graph.

REPLAY / SAVE DOMAIN
--------------------
`CvScreensInterface.showReplay` explicitly calls `saveReplay(argsList[0])` only when the supplied
player is > -1. Native end-game replay popups store a real human player ID. Thus the unguarded
`GET_PLAYER(ePlayer)` inside `CvGame::saveReplay` has a valid supported producer.

The WIP544 invalid `getReplayMessageText(i)` API edge remains below threshold: all numeric replay
field accessors reject invalid indexes in `CvGame`; the text accessor can return NULL for an invalid
index, but current shipped Python has no direct caller of `CyGame.getReplayMessageText`. Normal replay
screens use `CyReplayInfo` and bounded loops. No ordinary invalid-index producer exists.

RISE & FALL RECHECK
-------------------
The chapter-index surface was re-opened to verify an important naming ambiguity rather than merely
trusting the earlier caller scan. `RiseFall::getMaxChapters()` returns `chapters.size()`, not a larger
configuration ceiling. New-game initialization constructs exactly `maxChapters` RFChapter objects,
and save loading reconstructs the serialized vector before exposure. Therefore
`CvVictoryScreen`'s `for i in range(gc.getGame().getMaxChapters())` is exactly bounded for
`getChapterStart/End/Score/ScoreTurn/Civ`.

`CvDawnOfMan` obtains its chapter from the current R&F path. `getCurrentChapter()` itself scans the
same vector and returns either a valid index or -1. No supported release-build out-of-bounds route
into `RiseFall::getChapter` was established.

GLOBAL-WARMING NO_PLAYER RECHECK
--------------------------------
`CvWorldAdvisorScreen` deliberately passes `PlayerTypes.NO_PLAYER` to
`calculateGwLandDefence` and `calculateGwSustainabilityThreshold` for global totals. This is an
explicit underlying C++ contract, not an invalid enum escape:
- `calculateGwLandDefence(NO_PLAYER)` counts features for all owners;
- `calculateGwSustainabilityThreshold(NO_PLAYER)` returns the global threshold before any
  `GET_PLAYER` access.
The same sentinel is used natively by `CvGame::updateGwPercentAnger`.

OTHER SHIPPED ARGUMENT CONTROLS
-------------------------------
- `setScreenDimensions` receives the current interface resolution.
- `setHelpTextAreaWidth` receives the computed positive help-area rectangle width.
- `doControl` receives named `ControlTypes` constants from the UI.
- `setActivePlayer`'s live AI-Autoplay use passes an existing player's ID.
- `changeHumanPlayer`'s ChangePlayer path passes a selected player index after the utility's player
  selection logic.
- Holy-city/headquarters WorldBuilder calls pass a resolved CyCity; clear operations have dedicated
  null-setting wrappers.
- `reportCurrentLayer`'s separate `NO_GLOBE_LAYER` binding defect remains owned by the earlier
  `CyEnumsInterface` finding and is not duplicated here.

HISTORY SATURATION
------------------
The full reachable `PATH_HISTORY_INDEX` for `CyGame.cpp` was extracted from the supplied light-source
archive and its CyGame hunks were reviewed. The semantic strata reconcile as follows:
- practical 4251: help-text-area width exposure;
- 3794: argument naming only;
- 3746: enum-map typing + deprecated plot-extra migration with plot-null guards;
- 3546: Python-console `setVictoryValid` exposure;
- 3392/3293 and merge mirrors: cast/return-type cleanup and UWAI accessor naming;
- 3281: R&F AI handicap exposure;
- 2820: null-safe next-unit-in-cycle wrapper;
- 2749/2998 merge lineage: city-bar/UI control exposure;
- 2532/1465: current-globe-layer reporting (separate earlier owner already recorded);
- 2429: guaranteed `CvGame& m_kGame` representation replacing impossible wrapper-singleton NULL
  fallback branches;
- 1542: BULL SaveGame bridge;
- 1494: mod-mod `addPlayer` UWAI bookkeeping;
- 1395: old NULL-wrapper fallback booleans corrected from -1 to false (made moot by practical 2429);
- 1386/1357/1268 and related R&F strata: Python exposure and presentation helpers;
- remaining entries are include/style/merge/rename or equivalent no-semantic-change history for this
  façade.
No later Base AdvC 1.14 CyGame repair is missing from current SAS; WIP544's 236/236 common-body
semantic identity remains the strongest upstream control.

WHOLE-ALBUM DEDUP / NEGATIVE CONTROL
------------------------------------
Whole-album searches for the surviving sharp surfaces (`setHolyCity`, `setHeadquarters`,
`reportCurrentLayer`, replay text, player votes, RNG wrappers, screen/help width, addPlayer) found no
new independent root. Known deeper state defects remain owned in their native files, especially the
headquarters cache findings and the CyEnums globe-layer finding; this façade adds no separate
producer or consequence. Generic Python-console/mod misuse is outside the ordinary supported-path
threshold unless a concrete shipped producer is found.

QUEUE-046 CLOSE
---------------
The file is a thin façade. After function-level SAS/Base comparison, SAS-only classification,
shipped-Python call-graph validation, sentinel/index rechecks, full reachable history and album dedup,
there is no remaining supported producer-to-consequence chain strong enough for F676. Queue 046 is
therefore closed cleanly rather than forcing an ID.

EXACT CONTINUATION
------------------
C031-WIP545 physical -> Queue 046 CLOSED with no new finding -> 71 / 169 complete -> F676/KI#998
UNUSED -> Queue 047 already complete -> open ordinary Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp`
(1,128 queued lines) -> build current/Base/K-Mod/Civ4CE semantic delta + XML/info-consumer contract
map -> inspect serialization/list fields + array/index ownership + historical strata -> whole-album
dedup -> freeze next physical WIP at the next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP546 -- QUEUE 048 CvInfo_Building.cpp BASELINE / OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP545.
- Queue 046 `CvGameCoreDLL/CyGame.cpp` remains CLOSED with no new finding.
- Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp` is OPEN.
- F676 / provisional KI#998 remains UNUSED.
- Files complete remain 71 / 169; one file is open.
- No gameplay/XML source was modified.

CURRENT / BASE ADVC 1.14 PROVENANCE
-----------------------------------
The supplied current SAS and Base AdvC 1.14 `CvInfo_Building.cpp` are executable/XML-loader
identical. After normalizing CRLF/LF, their entire 1,127/1,128-line files differ by only one current
SAS explanatory comment above the already-fixed VoteSource ReligionCommerces loader. There is no
current source-code delta in constructors, getters, cache serialization, XML loading, pass2/pass3,
BuildingClass, SpecialBuilding, VoteSource, Vote or Project behavior.

The header comparison is likewise semantic-clean. Current SAS mainly collapses inherited multiline
inline getters and adds a Python-exposure comment for the existing `SpecialistExtraCommerce` enum-map
wrapper (KI#303). No storage/getter behavior differs from Base AdvC 1.14.

This is a strong upstream control: any F676 found in Queue 048 must be an inherited AdvC/K-Mod/Firaxis
root, a current XML-data interaction, or a cross-file consumer contract. It cannot be a missed newer
Base-1.14 repair in this file.

CLASS / LOADER SURFACE
----------------------
The 1,128 queued lines contain six related info classes:
- `CvBuildingInfo`;
- `CvBuildingClassInfo`;
- `CvSpecialBuildingInfo`;
- `CvVoteSourceInfo`;
- `CvVoteInfo`;
- `CvProjectInfo`.

A tag-inventory comparison against supplied K-Mod 1.46 and Civ4CE BtS shows the current loaders
retain the inherited XML field surface. The meaningful AdvC differences are known deliberate ones:
`bConditional` + `iAreaTradeRoutes` replace/qualify K-Mod's old global-trade-route behavior, and the
nested Specialist/Bonus yield loaders were refactored into `SetVariableListTagRate` while preserving
the plural XML containers. BuildingClass, SpecialBuilding, Vote, and Project tag sets otherwise
match their K-Mod/Civ4CE ancestors.

BUILDING ENUM-MAP COVERAGE
--------------------------
Mechanical reconciliation of every `CvBuildingInfo` enum-map declaration against both loading paths
found 36 / 36 maps present in all required places:
- each has an XML loader in `CvBuildingInfo::read(CvXMLLoadUtility*)`;
- each has a matching XML-cache `.read(stream)`;
- each has a matching XML-cache `.write(stream)`.

This includes the two 2D maps (`SpecialistYieldChange`, `BonusYieldModifier`), the two commerce-bool
maps, all yield/commerce maps, prerequisite maps, trait maps, domain/unit-combat maps and building-
class maps. No forgotten map member or cache-only/XML-only member surfaced.

The seemingly singular calls
    SetVariableListTagRate(..., "SpecialistYieldChange", ...)
    SetVariableListTagRate(..., "BonusYieldModifier", ...)
are correct: `XMLTagPairRateIterator` explicitly appends `s` to find the outer
`SpecialistYieldChanges` / `BonusYieldModifiers` container, then descends into the singular child.
Current XML has live nonempty examples of both shapes.

XML-CACHE STREAM SYMMETRY
-------------------------
Mechanical extraction of `CvBuildingInfo::read(FDataStreamBase*)` and `write(...)` yields 156 ordered
scalar/vector/map stream items on each side. All 156 match in order, including both variable-length
prerequisite vectors and all 36 enum maps. No current read/write layout shift was found.

All 112 constructor-initialized scalar/enum/bool fields are also present in cache read. XML loading
covers 111 / 112; the sole exception is `m_eMissionType`, which is intentionally assigned later by
`CvXMLLoadUtility::SetGlobalActionInfo` to `MISSION_CONSTRUCT` rather than sourced from Building XML.
That is the inherited action-registration lifecycle, not an omitted XML tag.

EARLY DEDUP / KNOWN ROOTS
-------------------------
The Queue-028 audit already owns the live `SetVariableListPerCommerce` new[]/scalar-delete defect as
F657/KI#979. `CvBuildingInfo::read` executes that helper for `CommerceChangeOriginalOwner`; do not
re-report the allocator root from Queue 048.

The old VoteSource ReligionCommerces parse regression is already the project's KI#121 and current
SAS has the corrected `SetCommerceList(ReligionCommerce(), "ReligionCommerces")`. Current Base AdvC
1.14 contains the same repair. Queue 048 must not duplicate that historical fixed bug.

HISTORY CURSOR
--------------
The supplied path-history list contains 29 `CvInfo_Building.cpp` strata. High-value known semantic
layers already identified include:
- practical 2796: fixed-size prerequisite arrays -> typed vectors + Python-safe accessors;
- practical 2320: conditional Great-Wall-style building effects;
- practical 2341: spaceship minimum-threshold repair;
- practical 3116: Internet `iTechShare` clarified/validated as a player count;
- practical 3183+: enum-map rewrites;
- SAS 4371/5692 lineage: ReligionCommerces regression and repair/KI#121.
The remaining history and current consumers still need full saturation before this queue can close.

EXACT CONTINUATION
------------------
C031-WIP546 physical -> Queue 048 OPEN -> F676/KI#998 UNUSED -> current/Base executable loader
identity established -> 36/36 BuildingInfo enum maps reconcile across XML + cache read/write ->
156/156 cache stream items order-identical -> mission-type lifecycle controlled -> continue full
history strata + BuildingClass/SpecialBuilding/VoteSource/Vote/Project consumer contracts + current
XML value census + pointer/index/art-info/cache edge sweep -> whole-album dedup -> either promote a
supported independent F676 or close Queue 048 cleanly -> freeze next physical WIP at next settled
boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP547 -- F676 / KI#998 CONFIRMED: GREAT WALL CONDITIONAL XML TAG NEVER LOADS
===============================================================================

START STATE
-----------
- Parent physical stone: C031-WIP546.
- Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp` remains OPEN.
- F676 / provisional KI#998 has been CONFIRMED.
- F677 / provisional KI#999 is now UNUSED.
- Files complete remain 71 / 169; one file remains open.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified.

F676 / KI#998 -- CONFIRMED
-------------------------
`CvBuildingInfo` reads the wrong XML tag for AdvC's option-dependent Great Wall abilities, so the
Great Wall is never marked conditional. This defeats the intended Barbarian-option switching in
Base AdvC and leaves a live wrong AI valuation path in current SAS.

ROOT -- SAME-COMMIT XML / C++ NAME MISMATCH
-------------------------------------------
AdvC practical 2320 / commit `7d224ca79b4f579c2ef4fe9acd4038b2213d97c4`, titled
"XML tag for disabling dynamic Great Wall abilities", introduced the entire mechanism in one commit.
The XML and schema added:

    <bConditionalAbilities>1</bConditionalAbilities>

and explicitly documented that this tag lets the DLL disable `bBorderObstacle`,
`iAreaTradeRoutes`, or `iDomesticGreatGeneralRateModifier` depending on Barbarian game options.

The same commit added the C++ member `m_bConditional`, but its XML loader asks for a different name:

    pXML->GetChildXmlValByName(&m_bConditional, "bConditional", false);

The supplied current SAS and Base AdvC 1.14 schemas contain only `bConditionalAbilities`; neither
schema defines `bConditional`. Current SAS and Base AdvC 1.14 BuildingInfos likewise put
`bConditionalAbilities=1` on the Great Wall and contain no `bConditional` tag.

`GetChildXmlValByName(bool*, ...)` assigns its default first, then performs an exact
`SetToChildByTagName(..., szName)` lookup. The third argument `false` used above only makes the tag
non-mandatory; the default remains false. There is no alias/remapping layer on this call. Therefore
Great Wall's `m_bConditional` deterministically stays false on normal XML loading.

OPTION SWITCHING IS THEN BYPASSED
---------------------------------
The three affected getters all have the same structure:

    if (!m_bEnabled... && m_bConditional)
        return disabled_value;
    return XML_value;

`CvGame::applyOptionEffects()` updates the three static enable flags from the Barbarian options:

- Domestic Great-General modifier enabled for Raging Barbarians or No Barbarians;
- Area trade routes enabled for non-Raging games or No Barbarians;
- area border obstacle enabled unless No Barbarians is active.

Because `m_bConditional` is always false, none of those static switches can suppress the Great
Wall's configured values. `applyOptionEffects()` runs at ordinary game initialization and after save
loading, so this is not a dormant lifecycle branch; the gating infrastructure executes but its sole
current XML marker was never loaded.

BASE ADVC 1.14 CONSEQUENCE -- LARGE AND DIRECT
----------------------------------------------
The supplied Base AdvC 1.14 Great Wall still has all three dynamic values active in XML:

    bBorderObstacle = 1
    iAreaTradeRoutes = 1
    iDomesticGreatGeneralRateModifier = 100
    bConditionalAbilities = 1

The intended getter matrix is therefore:

- ordinary Barbarians: border obstacle + area trade route, but no +100% domestic GG modifier;
- Raging Barbarians: border obstacle + +100% domestic GG modifier, but no area trade route;
- No Barbarians: area trade route + +100% domestic GG modifier, but no border obstacle.

With the loader mismatch, Base AdvC instead exposes all three configured effects in all three
rulesets. In particular, an ordinary non-Raging game receives the +100% domestic Great-General rate
that `applyOptionEffects()` explicitly tried to turn off, while a Raging game retains the area trade
route that the same function explicitly tried to turn off. Base AdvC 1.14 preserves the defect
unchanged.

CURRENT SAS CONSEQUENCE -- AI VALUES A USELESS NO-BARBARIAN EFFECT
------------------------------------------------------------------
Current SAS later rebalanced the Great Wall's two numerical dynamic values to zero:

    iAreaTradeRoutes = 0
    iDomesticGreatGeneralRateModifier = 0

so the large Base-AdvC balance error is partly masked. `bBorderObstacle=1` and
`bConditionalAbilities=1` remain, however. Under `GAMEOPTION_NO_BARBARIANS`, the intended effective
`isAreaBorderObstacle()` is false, but the tag mismatch leaves it true.

That is not merely stale metadata. Current `CvCityAI::AI_buildingValue()` contains this explicit
advc.310 assumption:

    if (kBuilding.isAreaBorderObstacle() &&
        !getArea().isBorderObstacle(getTeam()) &&
        !bConstCache)
    {
        /* A check for GAMEOPTION_NO_BARBARIANS is unnecessary
           b/c the Great Wall ability is then disabled via CvInfos. */
        ...
        iValue += 6 * iAreaCitySites;
    }

The omitted option check is safe only if the info-layer suppression works. It does not. In an
ordinary No-Barbarians game, while the Great Wall is still unbuilt and the city/area has available
city sites, normal non-const-cache building evaluation can therefore add positive value for the
anti-Barbarian border effect even though no Barbarian units can exist. Ordinary callers reach
`AI_buildingValue(..., bAsync)` with the default/false cache mode, so this is a supported AI choice
surface rather than a console-only API edge.

The stale true flag also causes the no-Barbarian Great Wall to increment the area's border-obstacle
count and exposes the border-obstacle help predicate. Barbarian movement itself is vacuous in that
ruleset, but the AI valuation above provides the concrete current-SAS consequence needed for the
finding threshold.

EARLIER ALBUM NEGATIVE CONTROL CORRECTED
----------------------------------------
C031-WIP332 previously closed a suspected No-Barbarians / Great-Wall AI issue on the premise that
`CvGame::setAreaBorderObstacleEnabled(false)` made `kBuilding.isAreaBorderObstacle()` false through
CvInfos. That premise is now disproven by the exact XML-loader mismatch. Preserve WIP332 as history,
but its Section 2 negative-control conclusion is superseded by F676.

ANCESTRY / ORACLE
-----------------
This is an AdvC regression introduced at practical 2320 itself. The introducing diff is unusually
strong evidence because it adds, in the same commit:

1. schema tag `bConditionalAbilities`;
2. Great Wall XML tag `bConditionalAbilities`;
3. C++ member `m_bConditional`;
4. C++ XML lookup for the non-existent tag name `bConditional`.

K-Mod/Civ4CE predate this AdvC dynamic-ability mechanism and therefore do not provide a direct
working implementation oracle. The intended contract is instead stated by practical 2320's own
schema/XML comments and by `CvGame::applyOptionEffects()` plus the conditional getter logic. Current
Base AdvC 1.14 still has the exact mismatch, and current SAS inherits it.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `bConditionalAbilities`, `m_bConditional`, dynamic Great-Wall abilities,
Great-Wall area trade routes and domestic Great-General modifiers found no prior finding that owns
this parser root. Earlier Great-Wall findings/controls concern TeamShare area accounting, AI wonder
valuation, Great-General valuation, or other lifecycle behavior. F676 is independent.

NARROW REPAIR BOUNDARY
----------------------
The smallest contract-preserving repair is to make the loader read the XML/schema name that was
introduced and remains shipped:

    pXML->GetChildXmlValByName(&m_bConditional,
        "bConditionalAbilities", false);

Renaming the schema/XML tag to `bConditional` would also reconcile the contract but is a broader
asset-format change for no benefit. With XML cache currently compile-time disabled, there is no
live old-cache migration requirement; if cache support is ever re-enabled, a regenerated info cache
would naturally serialize the now-correct true member through the already-symmetric stream layout.

QUEUE-048 CONTINUATION
----------------------
F676 is frozen before continuing the remainder of Queue 048. The mechanical map/cache baseline from
WIP546 remains valid; this finding is the deliberately-excluded "XML-data interaction / cross-file
consumer" class that the current-vs-Base executable identity could not rule out.

Exact cursor:
C031-WIP547 physical -> Queue 048 OPEN -> F676/KI#998 CONFIRMED -> F677/KI#999 UNUSED -> practical
2320 tag mismatch frozen + WIP332 negative control corrected -> continue VoteInfo/ProjectInfo/current
BuildingInfo data-reference census + remaining history saturation + pointer/index/art-info/cache
edge sweep -> whole-album dedup -> either promote an independent F677 or close Queue 048 cleanly ->
freeze next physical WIP at next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP548 -- QUEUE 048 CvInfo_Building.cpp CLOSED / F677 UNUSED
===============================================================================

START STATE
-----------
- Parent physical stone: C031-WIP547.
- Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp` was OPEN.
- F676 / KI#998 was already CONFIRMED and frozen.
- F677 / provisional KI#999 was UNUSED.
- Files complete were 71 / 169.
- No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified.

F676 REPAIR COUPLING -- GREAT WALL 3D MODEL MUST SURVIVE THE PARSER FIX
------------------------------------------------------------------------
The WIP547 one-line parser repair is sufficient for Base AdvC 1.14 gameplay semantics, but current
SAS has one additional repair constraint because SAS intentionally removed the Great Wall's old area
trade-route bonus.

`CvCity::processBuilding` contains an advc.310 Great-Wall graphics workaround. Its comment explicitly
says the 3D model should remain visible when playing without Barbarians even though the effective
BorderObstacle ability is then disabled through `CvBuildingInfo`. The guard is:

    if (kBuilding.isAreaBorderObstacle() ||
        kBuilding.getAreaTradeRoutes() > 0)
    {
        ... addGreatWall()/RemoveGreatWall ...
    }

History confirms that this second term is deliberate identity plumbing rather than a gameplay test:
practical 1253 added the no-Barbarian visual workaround using the then-current global-trade-route
value; practical 1492 renamed that effect to area trade routes; later refactors preserved the same
shape. Base AdvC 1.14 still gives Great Wall `iAreaTradeRoutes=1`, so after correcting
`bConditionalAbilities`, a No-Barbarians game has effective `isAreaBorderObstacle()==false` but the
route term remains positive and continues to identify the wall for rendering.

Current SAS deliberately changed Great Wall `iAreaTradeRoutes` from 1 to 0 (and its domestic Great-
General modifier from 100 to 0). Therefore a bare F676 loader fix in current SAS would make both
terms false under No Barbarians and prevent the Great-Wall model path from recognizing construction.
The present parser bug accidentally masks that latent repair regression by leaving the effective
border-obstacle getter true.

This is NOT an independent F677: it does not occur in the current unfixed tree and is caused only by
repairing F676 while retaining SAS's intentional balance change. Record it as part of F676's safe
repair boundary.

For SAS, do not restore `iAreaTradeRoutes=1` merely as a graphics signal; that would reintroduce a
removed gameplay bonus. Instead preserve visual identity independently of option-filtered gameplay
getters, e.g. with a raw configured-border-obstacle/Great-Wall visual predicate (or equivalent
explicit building identity). Current XML contains exactly one `bBorderObstacle=1` building --
`BUILDING_GREAT_WALL` -- so a raw configured-border-obstacle identity is unambiguous in the shipped
asset set. The same raw/static identity should preferably be used consistently by the nearby
existing-wall count if that block is refactored.

BUILDINGCLASS getLimit() CONSUMER SATURATION
--------------------------------------------
All current DLL callers of `CvBuildingClassInfo::getLimit()` were inspected:

1. `CvCityAI::AI_bestBuilding` uses it as a finite build-count/placement throttle, falling back to a
   prerequisite-derived cap only when `getLimit()==-1`.
2. `CvCityAI::AI_buildingValue` uses `getLimit()>=0` only to classify the building as limited.
3. The recursive prerequisite-building valuation uses a positive limit to stop valuing requirements
   for a class whose allowed copies are already built/making.

The current BuildingClass XML census contains no mixed global/team/player limit triplets. Every class
uses one of the ordinary shapes: unlimited `(-1,-1,-1)`, global/world `(1,-1,-1)`, or player/national
`(-1,-1,1)`. (Palace also has the player-one shape with `bNoLimit=1`; that flag exempts it from the
per-city wonder-count rules, not from the one-Palace-per-player class instance contract.) Thus the
method's additive implementation is exact for every shipped class and no mixed-cap F677 survives.

FINAL XML / ART / NUMERIC-DOMAIN CENSUS
---------------------------------------
A reverse direct-tag sweep was used specifically to catch a second F676-style XML/loader typo.
Among live BuildingInfo tags, the only superficially unowned direct names were:

- `bConditionalAbilities` -- the confirmed F676 mismatch;
- `iRaiseDefense` -- intentional: `CvXMLInfo` registers `RaiseDefense` and prefixes `i` when loading.

The nested Specialist/Bonus yield container names are likewise deliberately synthesized by their
specialized iterator/load helpers. BuildingClass, SpecialBuilding, VoteSource, Vote and Project
inputs exposed no second live direct-tag mismatch.

The full light-source archive was also used for the Art XML that was not part of the earlier selected
source extraction. Cross-referencing all 166 current BuildingInfo entries found:

- 0 unresolved non-NONE `ArtDefineTag` references;
- 0 unresolved non-NONE building `MovieDefineTag` references;
- 0 unresolved non-NONE ProjectInfo movie references.

The C++ art getters themselves already null-check failed building/movie art lookup results before
returning button/path pointers, so no independent art-pointer root was found.

Numeric-domain checks likewise closed rather than producing F677:
- vote population/state-religion percentages are within 0..100 and voter minima are nonnegative;
- project interception/success percentages and TechShare values are within their current contracts;
- building latitude ranges are ordered and within 0..90;
- the three `iNumFreeBonuses=-1` entries (Broadway, Rock'n'Roll, Hollywood) use the inherited sentinel
  handled explicitly by `CvGame::getNumFreeBonuses`; current SAS has also removed their FreeBonus
  resources, so those sentinel values are inert unless a bonus is configured again.

VOTE / SPECIALBUILDING / PROJECT CLOSURE CONTROLS
--------------------------------------------------
The current-data and consumer checks from WIP546-WIP547 remain clean and are now closure controls:

- all 22 VoteInfo entries resolve their configured vote-source/civic references;
- all 11 ProjectInfo entries resolve their project/victory references;
- no project self-prerequisite exists and no configured VictoryMinThreshold exceeds its threshold;
- Internet is the sole nonzero `iTechShare` entry, at the intended value 2;
- all current SpecialBuilding `TechPrereqAnyone` values are NONE, so `CvBuildingInfo::isTechRequired`
  not considering that separate team-global activation concept creates no current building-prereq
  omission;
- `CvVoteInfo`'s Boolean members are all explicitly initialized in the current constructor and all
  corresponding shipped XML tags are populated;
- VoteSource pass-3 deliberately accepts NONE for its optional Civic reference;
- Project practical 2341's minimum-threshold fallback survives in the current typed-map loader;
- practical 2796's vector prerequisite conversion retains Python-safe bounds accessors;
- practical 3116's Internet player-count interpretation is reflected by current consumers/data;
- practical 3052/3053/3147/3183 are representation/loader-map refactors whose current maps and, when
  enabled, stream ordering were already mechanically reconciled in WIP546.

HISTORY SATURATION / NO INDEPENDENT F677
----------------------------------------
All substantive `CvInfo_Building.cpp` history strata supplied in the local commit-diff mirror were
classified. The two additional SAS diffs that mention the file beyond the earlier 29-item semantic
list (5697 and 6316) touch documentation/KI references rather than executable BuildingInfo code.
No later Base AdvC 1.14 semantic repair is absent from current SAS; current SAS and supplied Base
1.14 remain executable-loader-identical apart from the known current comment-only difference and
SAS data/configuration outside this `.cpp` file.

No second supported root survives the combined loader/member, cache-stream, reverse-tag,
reference/data, history, consumer, pointer/index and art-reference sweeps. F677 is deliberately left
UNUSED rather than assigning an ID to robustness-only XML misuse or to the F676-induced rendering
repair constraint.

QUEUE 048 CLOSED
----------------
Queue 048 `CvGameCoreDLL/CvInfo_Building.cpp` is CLOSED with one new root:

- F676 / KI#998 -- `bConditionalAbilities` is loaded under the nonexistent name `bConditional`, so
  Great Wall option-dependent abilities never become conditional; Base AdvC gets the wrong dynamic
  ability matrix and current SAS can value a useless border obstacle under No Barbarians. Current
  SAS repair must additionally preserve the no-Barbarians Great-Wall 3D model independently of the
  removed area-trade-route gameplay bonus.

F677 / provisional KI#999 remains UNUSED.
Files complete: 72 / 169.
Files open: 0 / 169 at this physical stone.
Next ordinary unfinished queue: 049 `CvGameCoreDLL/CvReplayInfo.cpp` (1,081 queued lines).

EXACT CONTINUATION
------------------
C031-WIP548 physical -> Queue 048 CLOSED -> F676/KI#998 CONFIRMED -> F677/KI#999 UNUSED ->
72/169 files complete -> open ordinary Queue 049 `CvGameCoreDLL/CvReplayInfo.cpp` -> establish
current/Base/K-Mod/Civ4CE provenance + replay serialization/read/write contracts + Python/UI/load
consumers + practical history -> whole-album dedup -> freeze next physical WIP at next settled
boundary whether F677 is consumed or not.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP549 -- QUEUE 049 CvReplayInfo.cpp / F677 KI#999 CONFIRMED
===============================================================================

START STATE
-----------
- Parent physical stone: C031-WIP548.
- Queue 049 `CvGameCoreDLL/CvReplayInfo.cpp` was OPEN.
- F677 / provisional KI#999 was UNUSED.
- Files complete: 72 / 169.
- Supplied local comparison material now includes Base AdvC 1.14, K-Mod 1.46,
  Civ4CE and Taurus 1.04 source.
- No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified.

F677 / KI#999 -- ADVC'S TAURUS REPLAY EXCEPTION ACCEPTS BIT-7 VERSION 132
WITHOUT NORMALIZING IT, SO BTS-V4 SEA LEVEL BECOMES FINAL SCORE; THE
NO-MOD-NAME TAURUS CASE IS ALSO FILTERED BY THE WRONG HOF SWITCH
--------------------------------------------------------------------------------
Status: CONFIRMED; inherited Base AdvC compatibility-feature defect (advc.106i,
practical 3844), retained in Base AdvC 1.14 and current SAS. Optional/config-gated
in AdvC; current SAS has separately declared broad BtS/mod replay compatibility
unsupported through KI#166, so the primary supported impact belongs to Base AdvC.
Primary file: CvGameCoreDLL/CvReplayInfo.cpp
Oracle/source fixture: supplied Taurus 1.04 SDK `CvReplayInfo.cpp` / `BugMod.h`.

TAURUS 1.04 WRITER ORACLE -- THE STORED VERSION IS EXACTLY 132, DATA FORMAT 4
-----------------------------------------------------------------------------
The supplied Taurus 1.04 source removes the remaining uncertainty about the
marker and stream layout.

`BugMod.h` defines:

    #define BUG_DLL_SAVE_FORMAT  (1 << 6)
    #define TAURUS_SAVE_FORMAT   (BUG_DLL_SAVE_FORMAT << 1)

Therefore `TAURUS_SAVE_FORMAT == 1 << 7 == 128`.

Taurus `CvReplayInfo.cpp` then defines:

    int CvReplayInfo::REPLAY_VERSION = 4
        | TAURUS_SAVE_FORMAT;

and comments that the marker exists so Taurus replays can always be identified
"even if the mod name hasn't been saved." The serialized version integer is
therefore exactly:

    4 | 128 = 132

Crucially, Taurus's `write()` still writes the ordinary BtS replay-version-4
layout. Immediately after world size and climate it writes:

    stream.Write((int)m_eSeaLevel);
    stream.Write((int)m_eEra);
    stream.Write((int)m_eGameSpeed);
    ...

There is no AdvC version-6 final-score substitution in Taurus. Taurus also
always serializes the trailing mod-name string, but `createInfo()` leaves it
empty when its configurable `Taurus__ModNameInReplays` option is disabled.
That option defaults true in Taurus 1.04 but is intentionally user-configurable.

ADVC PRACTICAL 3844 INTENT
--------------------------
The exact introducing commit is practical 3844 / 5355820f3ac6530a945b4267323cfcdb532b0d0e,
"Accept replays created by the Taurus mod". Its message says Taurus replays
should be accepted when `HOF_DISPLAY_OTHER_MOD_REPLAYS` is set and explicitly
calls out the bit marker as a way to identify Taurus "even when the mod name
isn't stored in the replay file". It also states that Taurus is essentially
BULL and uses a compatible replay format.

The entire code change was only this acceptance exception:

    if(iVersion > REPLAY_VERSION &&
        // Taurus replays set a single bit in the version number
        (iVersion & ~(1 << 7)) > REPLAY_VERSION)
    {
        return false;
    }

The masked value is used only in that comparison. `iVersion` itself is never
normalized and no persistent `bTaurusReplay` identity is retained.

BUG 1 -- ACCEPTED TAURUS VERSION 132 TAKES ADVC'S VERSION >= 6 FIELD MEANING
---------------------------------------------------------------------------
For a Taurus 1.04 replay, AdvC reads `iVersion = 132`.

It correctly passes the new compatibility check because:

    132 & ~(1 << 7) = 4

and 4 is an accepted replay-data version. But the live variable remains 132.
Later parsing therefore executes:

    if(iVersion >= 6)
    {
        m->iFinalScore = iType;
        m_eSeaLevel = NO_SEALEVEL;
    }
    else
    {
        m->iFinalScore = MIN_INT;
        m_eSeaLevel = (SeaLevelTypes)iType;
        ...
    }

The `iType` just read is Taurus's BtS-v4 sea-level field. Thus an accepted
Taurus replay stores its sea-level enum (normally a tiny value such as 0/1/2)
as `m->iFinalScore` and suppresses the later fallback reconstruction:

    if(m->iFinalScore == MIN_INT)
        m->iFinalScore = getFinalPlayerScore();

because the bogus sea-level value is not `MIN_INT`.

This is directly player-visible. Current/Base Hall-of-Fame Python uses
`replayInfo.getFinalScore()` both as the displayed Game Score and as the
`SORT_BY_GAME_SCORE` ordering key. So a Taurus game can appear with a score
of roughly its sea-level enum instead of its actual final player score.

The remainder of the Taurus stream stays aligned: version 132 is still >3 so
AdvC correctly reads Taurus's map-script string, and the rest of the Taurus
writer follows the BtS-v4 field order through the trailing mod-name string.
This is therefore a semantic reinterpretation, not merely a speculative
out-of-step parser concern.

BUG 2 -- THE EXACT "NO MOD NAME" TAURUS CASE IS STILL GATED AS AN UNMODDED BTS REPLAY
-----------------------------------------------------------------------------------
At the end of AdvC's reader:

    if(m_szModName.empty())
    {
        if(iModID < 0 && GC.getDefineINT("HOF_DISPLAY_BTS_REPLAYS") <= 0)
            return false;
    }

A Taurus replay version 132 is far below AdvC's packed `100 * SAVE_VERSION`
range, so `iModID` remains -1. If Taurus's `Mod Name In Replays` option is
disabled, the trailing string is intentionally empty. Consequently
`HOF_DISPLAY_OTHER_MOD_REPLAYS=1` -- the very switch practical 3844 says is
sufficient -- does not permit the file. AdvC additionally requires
`HOF_DISPLAY_BTS_REPLAYS=1` merely because the mod-name string is empty.

This directly contradicts practical 3844's explicit reason for recognizing
Taurus's bit marker: identification even when no mod name is stored.

ACTIVATION MATRIX
-----------------
The two symptoms belong to one incomplete Taurus-recognition root:

1. Taurus default (`ModNameInReplays=true`) + AdvC `HOF_DISPLAY_OTHER_MOD_REPLAYS=1`:
   - replay can pass the mod-name compatibility gate;
   - version remains 132;
   - sea-level slot is misread as final score.

2. Taurus `ModNameInReplays=false` + AdvC `HOF_DISPLAY_OTHER_MOD_REPLAYS=1`,
   `HOF_DISPLAY_BTS_REPLAYS=0`:
   - bit-7 compatibility check passes;
   - replay is nevertheless rejected at the empty-mod-name gate.

3. Same no-mod-name Taurus replay with both AdvC switches enabled:
   - replay can pass the empty-name gate;
   - version remains 132;
   - sea-level slot is still misread as final score.

So practical 3844 does not correctly implement either half of its stated
Taurus-marker contract.

ANCESTRY / BASE / SAS SCOPE
---------------------------
K-Mod and Civ4CE do not contain this AdvC Taurus exception. The regression is
introduced by AdvC practical 3844 itself; the local diff is a five-line change
that adds only the masked acceptance comparison and leaves all downstream
version and mod-name logic untouched.

Supplied Base AdvC 1.14 retains the exact code unchanged, so this remains a
real Base-AdvC defect in the documented/configurable `HOF_DISPLAY_OTHER_MOD_REPLAYS`
feature.

Current SAS also retains the reader behavior. However, SAS KI#166 intentionally
disables pretending SAS's own shifted raw XML IDs are BtS-compatible and states
that broad cross-BtS replay/Hall-of-Fame compatibility is not supported.
Therefore record the root as inherited and present in SAS source, but do not
overstate ordinary SAS product impact; Base AdvC's supported Taurus-compatibility
feature is the clean ownership/activation case.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for Taurus, replay version/bit-7 handling, final-score/
sea-level reinterpretation, and the HOF display switches found no prior owner.
F272/KI#595 is nearby but independent: it concerns high foreign replay color
IDs wrapping into reserved system colors after a foreign replay has been
accepted. F677 concerns Taurus identity/version normalization and filtering
before/while the replay itself is parsed.

NARROW REPAIR DIRECTION
-----------------------
Treat Taurus recognition as state, not a one-expression exception. Conceptually:

    bool bTaurusReplay = ((iVersion & (1 << 7)) != 0);
    if(bTaurusReplay)
        iVersion &= ~(1 << 7);

Then use the normalized replay-data version for every subsequent format branch
and for `m->iVersionRead`. Preserve `bTaurusReplay` for the trailing empty-name
filter so a recognized Taurus replay is governed by
`HOF_DISPLAY_OTHER_MOD_REPLAYS`, not misclassified as an unmodded BtS replay.

The exact implementation should still reject unsupported future Taurus data
versions after masking, and should not weaken the ordinary foreign-mod sanity
checks. The intent is not to accept arbitrary high-bit versions; it is to make
the already-special-cased Taurus marker coherent throughout the parser.

QUEUE-049 CONTINUATION
----------------------
F677 is frozen before continuing the rest of Queue 049. Two remaining cursors
from the pre-freeze pass are deliberately NOT classified yet:

- `addSettingsMsg()` writes the initial-active-player value directly while
  ordinary replay messages are compacted through the ever-alive player map;
  a supported gap-producing setup still needs proof before this can become a
  finding.
- `createInfo` first-call/SaveMap lifecycle and repeated-read/container-reset
  behavior still need exact producer/lifecycle tracing.

Exact cursor:
C031-WIP549 physical -> Queue 049 OPEN -> F677/KI#999 CONFIRMED -> Taurus 1.04
oracle pins version 132 and BtS-v4 layout -> practical-3844 acceptance/gating
root frozen -> F678/KI#1000 UNUSED -> continue settings-message compact-player
mapping + createInfo/SaveMap lifecycle + self-generated replay/read bounds +
remaining practical-history saturation + whole-album dedup -> either promote an
independent F678 or close Queue 049 cleanly -> freeze next physical WIP at next
settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP550 — QUEUE 049 CONTINUATION / F678-KI#1000 CONFIRMED
===============================================================================

PHYSICAL HANDOFF
----------------
This checkpoint continues directly from C031-WIP549. Queue 049
`CvGameCoreDLL/CvReplayInfo.cpp` remains OPEN. F677/KI#999 (Taurus bit-7 replay
identity/version normalization) remains confirmed. The next independent root,
F678/KI#1000, is now confirmed and frozen below before any further Queue-049
candidates are pursued.

F678 / KI#1000 — ADVC PRACTICAL 3883'S PER-REPLAY BTS-COMPATIBILITY CHECK RUNS
BEFORE THE REPLAY FIELDS EXIST, SO AN INCOMPATIBLE REPLAY CAN KEEP AN EMPTY MOD
NAME EVEN AFTER THE WRITER FALLS BACK TO THE ADVC FORMAT
-------------------------------------------------------------------------------

CLASSIFICATION
--------------
Confirmed Base AdvC 1.14 defect in the supported/configurable
"Mod Name In Replays" / BtS-compatible replay-export feature.

Current AdvC-SAS is not activated by this root because SAS KI#166 deliberately
hard-disables `isStoringReplaysAsBtS()` and always stores the normal SAS replay
format with its mod identity. Keep the inherited source/history finding because
it is a real Base-AdvC feature defect, but do not claim ordinary current-SAS
runtime impact.

THE TWO DECISIONS ARE MADE AT DIFFERENT OBJECT-LIFECYCLE STATES
--------------------------------------------------------------
Base AdvC 1.14 starts `CvReplayInfo::createInfo` with:

    if (!isStoringReplaysAsBtS())
        m_szModName = GC.getModName().getFullPath();

Only after that decision does `createInfo` populate the fields used by
`isStoringReplaysAsBtS`, including:

    m_eDifficulty
    m_eWorldSize
    m_eGameSpeed
    m_listVictoryTypes
    m_listPlayerScoreHistory / player colors

The constructor initializes the relevant scalar fields to their NO_* values and
leaves the vectors empty:

    m_eDifficulty(NO_HANDICAP)
    m_eWorldSize(NO_WORLDSIZE)
    m_eGameSpeed(NO_GAMESPEED)
    ...

`getColor` also safely returns `NO_COLOR` while the replay-local player vector
is still empty. Consequently, with the BUG option requesting BtS-compatible
storage, the early call can return true simply because none of the completed
replay's incompatibilities have been copied into the object yet.

At write time, AdvC calls the same predicate again:

    if (!isStoringReplaysAsBtS())
    {
        setMinimapSizeFromXML();
        stream.Write(m_iMinimapSize * MAX_CHAR + m_eActivePlayer);
    }
    else
        stream.Write(m_eActivePlayer);

By then the object is complete, so the second call can correctly reject BtS
compatibility and switch to AdvC's own serialized representation. But there is
no corresponding late assignment to `m_szModName`; the only normal write-side
assignment is the premature one at the start of `createInfo`. The writer
therefore serializes the stale empty string at its end:

    stream.WriteString(m_szModName);

This produces a split-brain replay: its payload is explicitly *not* being
stored as BtS-compatible, while its persisted mod-name discriminator still says
"no mod".

CONCRETE SHIPPED BASE-ADVC ACTIVATION: WORLDSIZE_XXL
----------------------------------------------------
This does not require a mod-mod or malformed replay.

Supplied Base AdvC 1.14 ships seven world sizes in
`Assets/XML/GameInfo/CIV4WorldInfo.xml`:

    0 WORLDSIZE_DUEL
    1 WORLDSIZE_TINY
    2 WORLDSIZE_SMALL
    3 WORLDSIZE_STANDARD
    4 WORLDSIZE_LARGE
    5 WORLDSIZE_HUGE
    6 WORLDSIZE_XXL

The live per-replay safety predicate explicitly treats world-size IDs above 5
as unsafe for BtS:

    if (getWorldSize() > 5)
    {
        FErrorMsg("World size incompatible with BtS replay format");
        return false;
    }

Therefore a Base-AdvC XXL replay is an exact native fixture.

Activation sequence with the BUG option "Mod Name In Replays" unchecked:

1. A fresh `CvReplayInfo` has `m_eWorldSize = NO_WORLDSIZE` (-1), no victory
   vector and no replay-local players yet.
2. `createInfo` calls `isStoringReplaysAsBtS()` before populating them.
3. The world-size check sees -1, not the actual XXL ID 6; the other per-replay
   checks likewise see constructor/empty state. With the normal 512 minimap,
   the predicate returns true.
4. Because the predicate returned true, `createInfo` deliberately leaves
   `m_szModName` empty.
5. `createInfo` then stores the real map state, including `m_eWorldSize = 6`.
6. `write()` calls `isStoringReplaysAsBtS()` again. It now sees 6, emits
   "World size incompatible with BtS replay format", returns false, and writes
   AdvC's packed active-player/minimap representation.
7. Nothing repairs `m_szModName`, so the writer still appends an empty mod name.

The option is user-facing and intentional. In Base 1.14
`Assets/Config/BUG Main Interface.xml`, `ModNameInReplays` defaults TRUE; users
must explicitly uncheck it to request the cross-BtS Hall-of-Fame behavior.
Thus F678 is not a default-settings defect, but it is a real supported option
failure on a shipped AdvC game configuration.

WHY THE EMPTY NAME MATTERS
--------------------------
The BUG-option help states that checking "Mod Name In Replays" writes the mod
folder name and thereby excludes AdvC replays from the Hall of Fame when BtS is
run without mods. Conversely, unchecking it is the supported way to let safe
AdvC replays appear there.

The compatibility guard's own comment says that, when a replay is unsafe for
BtS, the requested BtS storage is ignored specifically because running it in
BtS might crash. Its error messages likewise say the option is being ignored.
That safety fallback therefore requires the replay to regain its AdvC mod
identity when the late compatibility check fails.

F678 fails exactly that half of the fallback: the payload switches back to the
AdvC form but the serialized mod name remains empty.

The supplied K-Mod/BtS-lineage oracle demonstrates the downstream danger:

- its replay reader accepts any replay version >= 2; there is no upper-version
  rejection, so AdvC's packed/high version is not by itself a protective gate;
- it reads the active-player field as a plain player ID, whereas AdvC's
  non-BtS writer has packed `m_iMinimapSize * MAX_CHAR + m_eActivePlayer`
  (`MAX_CHAR = 0x7f`);
- it stores world-size ID 6 without reader-side bounds validation;
- its Hall-of-Fame Python directly evaluates
  `gc.getWorldInfo(replayInfo.getWorldSize()).getDescription()`.

K-Mod's `CyGlobalContext::getWorldInfo(int)` returns NULL outside its loaded
world-info range. AdvC's own safety threshold (>5) identifies 6 as beyond the
BtS world-size domain. Thus the exact XXL replay the late guard rejected can
nevertheless retain the empty mod-name discriminator and reach an unmodded
Hall-of-Fame path containing an unsupported world-info lookup. The packed
active-player field also becomes a nonsensical raw player ID to the BtS reader,
which can break leader-filter semantics even where the default Hall-of-Fame row
uses the stored leader-name string directly.

Do not overstate the exact UI failure mode as guaranteed process termination:
the Python/Boost layer may turn the invalid world-info reference into a Python
exception rather than a native crash. The proved defect is stronger and does
not depend on that distinction: AdvC's own "unsafe for BtS" fallback correctly
rejects the completed replay, yet the file retains the empty identity that the
feature uses to expose it to BtS.

PRACTICAL-LINEAGE PROOF
-----------------------
The root begins with AdvC practical 3883 / 09dfa610790db75e6ea9c50c8757b74b6154c8de,
"Move Store-Replays-as-BtS XML option to BUG menu" (2022-07-11).
Its commit message says the feature was moved to the BUG menu and disabled by
default.

Before practical 3883, AdvC used a static/global `STORE_REPLAYS_AS_BTS` decision.
The immediately preceding code deliberately disabled BtS export globally when
any extra world-size/victory/handicap/game-speed info existed:

    GC.getNumWorldInfos() <= 6 &&
    GC.getNumVictoryInfos() <= 7 &&
    GC.getNumHandicapInfos() <= 9 &&
    GC.getNumGameSpeedInfos() <= 4

The old comment explicitly acknowledged that a particular replay might not use
an added type but intentionally kept one format for all replays. With AdvC's
seventh XXL world size, this conservative gate disables the feature and avoids
the F678 state split.

Practical 3883 intentionally replaced that global policy with per-replay checks
so compatible individual games could still be exported. In the same diff it:

- removes the static global compatibility computation;
- replaces the `createInfo` mod-name decision with
  `if (!isStoringReplaysAsBtS())` at the top of `createInfo`;
- replaces the writer's global flag with another live
  `isStoringReplaysAsBtS()` call; and
- introduces `isStoringReplaysAsBtS()` based on replay-instance fields.

That creates the temporal inconsistency in one coherent change: the first
instance-field check runs before those instance fields are filled, while the
second runs after they are filled.

Practical 3843 had already corrected the older global count comparisons; it is
not the F678 root. Practical 3883 is where the formerly conservative static
check becomes a state-dependent predicate evaluated at two different lifecycle
points.

BASE / SAS / K-MOD / CIV4CE SCOPE
---------------------------------
Supplied Base AdvC 1.14 retains practical-3883 behavior unchanged, including the
shipped XXL trigger and user-facing BUG option. F678 is therefore a current
Base-AdvC defect in that optional compatibility feature.

Current AdvC-SAS intentionally avoids the activation. KI#166 makes
`CvReplayInfo::isStoringReplaysAsBtS()` immediately return false because SAS has
shifted raw XML IDs and does not claim BtS replay interoperability. Therefore
current SAS's `createInfo` always assigns its mod name and the writer always
uses the SAS/AdvC representation. No SAS repair is required for F678 unless the
BtS-export feature is deliberately re-enabled in the future.

K-Mod/Civ4CE are useful reader/origin oracles but do not own the AdvC
practical-3883 per-replay export policy.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `ModNameInReplays`, store-as-BtS behavior, empty mod
names, XXL replay compatibility, HOF display switches and KI#166 found no prior
owner for this lifecycle split.

Nearby replay findings remain independent:

- F272/KI#595: foreign replay color IDs can wrap into reserved local colors.
- F677/KI#999: Taurus bit-7 version 132 is accepted without normalization and
  the no-mod-name Taurus case is filtered by the wrong HOF switch.
- KI#166: SAS intentionally disables its own BtS-compatible replay export;
  that is a product policy/mitigation, not the Base-AdvC practical-3883 root.

NARROW REPAIR DIRECTION FOR BASE ADVC
-------------------------------------
Make the persisted mod-name decision only after the fields used by
`isStoringReplaysAsBtS()` have been populated, or re-evaluate and repair
`m_szModName` once the completed replay is known to be incompatible.

The safest conceptual invariant is:

    final isStoringReplaysAsBtS() == true  -> serialized mod name may be empty
    final isStoringReplaysAsBtS() == false -> serialized mod name must identify AdvC

Do not merely remove the late writer safety check; its explicit purpose is to
avoid feeding BtS unsupported enum domains. Also do not regress the first-call
BUG-path optimization: that path asks `getModName()` for the current mod folder,
and Base AdvC's public getter deliberately reports the AdvC mod path to the
running EXE regardless of the persisted replay string.

A compact repair is therefore to finalize/repair `m_szModName` after the real
replay fields are copied (before serialization), while retaining the writer's
late safety assertion/check as a consistency guard.

POST-F678 QUEUE STATUS
----------------------
F678/KI#1000 is now confirmed and frozen. Queue 049 remains OPEN.
F679/KI#1001 is UNUSED.

Already closed negative/latent controls in the post-WIP549 tranche:

- `createInfo`'s first-call early return is an intentional BUG/Python-reload
  optimization; `BugPath.py::initModFolder` only needs the mod path.
- `addSettingsMsg` stores a raw initial-active player ID instead of a compact
  replay-local player ID, but its major-event/no-plot pseudo-message has no
  shipped consumer that interprets that ID as replay-local ownership.
- `createInfo(ePlayer)` records the current active player's compact ID even when
  metadata is built for another `ePlayer`; supported replay-popup handoff paths
  activate the relevant human before popup retrieval, and the debug Info-Screen
  temporary replay does not consume that active-player metadata.
- repeated `read/createInfo` reuse remains API-hardening debt; shipped callers
  construct fresh replay objects.
- the packed/high AdvC replay version is not itself a BtS rejection bug: the
  BtS/K-Mod reader has only a lower version check (`iVersion < 2`) and therefore
  accepts high version numbers. This negative control is what exposed the more
  consequential mod-name lifecycle split above.

Exact continuation stone:
C031-WIP550 physical -> Queue 049 OPEN -> F677/KI#999 + F678/KI#1000 CONFIRMED
-> F679/KI#1001 UNUSED -> finish self-generated replay/read bound census +
remaining practical-history saturation + player-history/message/minimap
consumer contracts + whole-album dedup -> either promote an independent F679
or close Queue 049 cleanly -> freeze next physical WIP at next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP551 DURABLE CHECKPOINT - QUEUE 049 CvReplayInfo.cpp CLOSED
2026-09-09 - ChatGPT-5.6-Sol local audit continuation
===============================================================================

PHYSICAL CONTINUATION BASE
--------------------------
Continued from C031-WIP550 only. Current SAS/Base AdvC 1.14/K-Mod/Civ4CE and the
user-supplied Taurus 1.04 source trees were inspected locally. No web/GitHub
lookup was used and no gameplay/XML source was modified.

QUEUE RESULT
------------
Queue 049 `CvGameCoreDLL/CvReplayInfo.cpp` is now COMPLETE.

Confirmed roots retained from this queue:
- F677 / KI#999: AdvC practical 3844 accepts Taurus bit-7 replay version 132
  without normalizing the underlying BtS-v4 format version, and the no-mod-name
  Taurus exception is filtered through the separate BtS-replay switch. This can
  reinterpret the BtS sea-level slot as AdvC's v6 final-score slot. Supplied
  Taurus 1.04 directly confirms `REPLAY_VERSION = 4 | (1 << 7)` and the BtS-v4
  field layout.
- F678 / KI#1000: Base AdvC practical 3883 evaluates per-replay BtS-export
  compatibility before the replay-instance fields have been populated, then
  re-evaluates at write time. A shipped XXL replay with "Mod Name In Replays"
  unchecked can therefore suppress the mod name while later being written in
  the non-BtS AdvC representation. Current SAS KI#166 disables this optional
  cross-BtS export path and is not activated by F678.

No independent F679 was found. F679/KI#1001 remains UNUSED.

FINAL POST-F678 CONTROLS
------------------------
1. Replay minimap texture / no-graphics lifecycle - CLOSED BELOW THRESHOLD

`CvReplayInfo::createInfo` first tries the map's stored replay texture and then
falls back to `gDLL->UI().getMinimapBaseTexture()`. If both are NULL, it does
allocate the minimap byte array without filling it. This is a real low-level
robustness smell, but the shipped replay-save producer is graphical:

    CvScreensInterface.showReplay(argsList)
        -> CyGame().saveReplay(argsList[0])
        -> CvGame::saveReplay
        -> EXE SaveReplay
        -> CvGame::writeReplay
        -> CvReplayInfo::createInfo

No second DLL/Python `SaveReplay` producer exists in the supplied current tree.
PitBoss hosts deliberately avoid graphical screens/BUG UI initialization and do
not provide a separate replay-save call here. In an ordinary client game the
live minimap texture exists; if `REPLAY_TEXTURE_ERA` captured an earlier texture,
`CvMap::getReplayTexture()` supplies that instead.

The nullable-copy behavior also predates AdvC's cached replay-texture feature:
K-Mod/BtS-lineage `CvReplayInfo::createInfo` likewise allocated the buffer and
only copied when `getMinimapBaseTexture()` returned non-NULL. Therefore no
supported current-SAS/Base activation was established. Keep this as a
no-graphics/direct-API robustness control, not F679.

The separate formal C++ UB in `CvMap::write()` from forming
`&m_replayTexture[0]` for a zero-length vector was already recorded during
Queue 036 as below threshold; do not duplicate it here.

2. Per-player replay history vector length - CLOSED / HISTORY ORACLE

The reader permits each replay-local player's serialized history vector to have
an independent count. `isValidTurn()` checks only the replay-global
`m_iInitialTurn..m_iFinalTurn`, while `getTurnData()` directly indexes a given
player vector. A malformed/foreign replay can therefore describe a shorter
per-player history than the global turn range.

AdvC's own writer cannot produce that shape. Practical 2635
`34026a349317749c4bf9d2a9b9d88516acecfbe6`, "Fix out-of-bounds accesses to
PlayerHistory", changed replay construction to iterate every turn from initial
through final for every ever-alive player and read each live statistic through
`getHistorySafe()`, padding unavailable history with 0. Thus every self-written
player vector has the same full initial-through-final length even when the live
history contained a gap.

BtS/K-Mod writers likewise emit the full inclusive turn interval. No supported
short-vector producer was found. Keep the permissive reader shape as
foreign/malformed replay hardening only.

3. Reader sanity-check census - NO SUPPORTED F679

The full current `CvReplayInfo::read` bound surface was rechecked. The one call
whose Boolean result is not acted on is:

    stream.Read(&m_iFinalTurn);
    checkBounds(m_iFinalTurn, 0, 10000);

That weakens malformed/foreign-replay validation and can combine with an
inconsistent history count, but self-written replays serialize a matching full
history interval. A game extending beyond the arbitrary diagnostic range is
not corrupted merely because this return value is ignored; the later history
count is the operative self-format constraint. No ordinary shipped replay was
found that turns the missing `if (!...) return false` into gameplay corruption.

Other permissive/off-by-one reader edges remain robustness-only in current
contracts:
- active-player upper bound 64 is one wider than compact 0..63;
- handicap upper bound accepts `getNumHandicapInfos()` itself;
- victory-list count has no separate aggregate bound, though each element does;
- packed minimap resolution lacks a dedicated positive/reasonable read bound;
- per-player history count is capped independently at 10000.

These can matter for corrupted or differently formatted foreign files, but no
self-writer or supported Taurus/BtS fixture established an independent root
beyond F677-F678.

4. Replay-message NULL/count asymmetry - ALLOCATION-FAILURE ONLY

`write()` stores `m_listReplayMessages.size()` and then skips a NULL pointer
instead of emitting a placeholder. A NULL list entry would desynchronize the
stream. Current replay construction appends only successfully allocated
messages; `read()` can append NULL only if `new CvReplayMessage` itself fails,
and the public `CvReplayInfo::addReplayMessage` has no shipped caller supplying
NULL. This is allocation-failure/API hardening, not an ordinary finding.

5. Repeated read/createInfo object reuse - UNSUPPORTED REUSE CONTROL

Some list fields are not globally cleared at the beginning of every `read`, and
`createInfo` explicitly asserts that its replay-message list is empty rather
than deleting prior messages. Shipped Hall-of-Fame/replay paths create fresh
`CvReplayInfo` objects for independent files/snapshots; the first-call BUG path
was already proved intentional and only obtains the current mod folder before
returning. No ordinary object-reuse producer survives.

6. Settings pseudo-message raw player ID - NO CONSUMER CONSEQUENCE

`addSettingsMsg()` intentionally tags its turn-0 major-event pseudo-message with
`getInitialActivePlayer()` (original game player ID), unlike ordinary copied
replay messages which use compact replay-local IDs. Practical 1524 explicitly
changed replay settings to name the initially active player.

Shipped consumers inspect replay-message player IDs for plot-owner/founding
logic. The settings message has no plot and is not a plot-owner/city-founded
message, so its raw-vs-compact asymmetry is not interpreted as ownership. Keep
as latent metadata debt, not F679.

7. `createInfo(ePlayer)` versus current active-player compact ID - SUPPORTED PATH CLOSED

Replay metadata such as difficulty/name is taken from the requested `ePlayer`,
while `m_eActivePlayer` is compacted from `CvGame::getActivePlayer()`. End-game
replay popups for hotseat/PBEM are retrieved only when the relevant human is
active; the engine explicitly performs active-player handoff. The other
non-active temporary replay identified in the supplied tree is the Info-Screen
perspective path, which consumes replay messages rather than the temporary
replay's active-player final-score identity. No wrong supported output was
established.

8. Taurus/K-Mod/Civ4CE final cross-check

Taurus 1.04 was retained as a direct writer oracle for F677. K-Mod and Civ4CE
keep the ordinary BtS-v4 replay history/minimap/message layout relevant to the
negative controls above. Civ4CE's later `CvReplayInfo` differences do not supply
an independent repair for AdvC's practical-3844/3883 policies.

REMAINING HISTORY SATURATION
----------------------------
The semantic history entries most likely to hide a surviving replay defect were
reconciled against current code:
- K-Mod 801: replay player-color identity fix; current AdvC's later identity
  handling is intentional and does not reintroduce the concealed-color bug.
- K-Mod 1232: replay cleanup/memory-management fixes retained where applicable.
- practical 1430: initial BtS-compatible replay export policy; later policy
  changes are owned by F678.
- practical 1511: additional replay-format safety checks retained.
- practical 1524: initial-active-player settings identity retained intentionally.
- practical 1703: custom-map-option settings text retained.
- practical 1844: configurable minimap/replay-texture lifecycle; no supported
  no-graphics replay writer found beyond the robustness control above.
- practical 2126 and 4085: CvReplayInfo storage/delete fixes retained.
- practical 2517: NO_PLAYER replay-message enum-map fix retained.
- practical 2635: safe/full PlayerHistory replay construction retained and used
  as the decisive history-length oracle above.
- practical 3843: flipped BtS-compatibility comparisons corrected before the
  later Taurus/per-replay roots.
- practical 3844: F677 root.
- practical 3845: string/read sanity tests retained; remaining malformed-file
  gaps do not create a supported root.
- practical 3883: F678 root.

QUEUE-049 CLOSURE
-----------------
The file has now been audited through:
- current SAS versus supplied Base AdvC 1.14 semantic deltas;
- BtS/K-Mod/Civ4CE/Taurus writer/reader format oracles;
- all public getters and replay-local compact-player mapping;
- replay settings and historical-audience consumers;
- self-written game-option/victory/player/history/message/minimap domains;
- reader bounds and version branches;
- optional cross-BtS/other-mod/Taurus compatibility paths;
- replay texture capture/save lifecycle;
- relevant practical-history strata and whole-album dedup.

No third independent supported root survives. Queue 049 closes with F677-F678
confirmed and F679 unused.

AUDIT PROGRESS AFTER CLOSURE
----------------------------
Files complete: 73 / 169.
Next ordinary unfinished file: Queue 050 `CvGameCoreDLL/CyPlot.cpp`
(1,067 queued lines).
Next new root ID remains F679 / provisional KI#1001.

Exact continuation stone:
C031-WIP551 physical -> Queue 049 CLOSED -> F677/KI#999 + F678/KI#1000
CONFIRMED -> F679/KI#1001 UNUSED -> ordinary Queue 050 CyPlot.cpp next -> begin
current/Base semantic-delta map + shipped Python caller/None/index contracts +
history/consumer/dedup pass -> freeze next physical WIP at next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP552 — QUEUE 050 CyPlot.cpp — INHERITED WRAPPER / PYTHON-CONSUMER PASS;
F679 REOPENS KI#300
===============================================================================

STATUS
------
Queue 050 `CvGameCoreDLL/CyPlot.cpp` is OPEN.
F679 is CONFIRMED as a reopening of existing/fixed KI#300.
F680 / provisional KI#1001 is UNUSED.
No repository/gameplay source files modified.

CURRENT-vs-BASE PROVENANCE BASELINE — ZERO EXECUTABLE DELTA
-----------------------------------------------------------
Current SAS `CyPlot.cpp` and supplied Base AdvC 1.14 `CyPlot.cpp` were compared
with line-ending/whitespace normalization. The only textual difference is a
line-wrap in `calculateImprovementYieldChange`; there is no executable wrapper
change. Thus a new C++ root in this queue must be inherited from Base AdvC/K-Mod
or arise from a current consumer violating the long-standing wrapper contract,
not from a missed Base-1.14 repair or a recent SAS edit to `CyPlot.cpp` itself.

The supplied K-Mod/Civ4CE comparison and 29 retained history diffs identify the
small AdvC semantic wrapper delta. The important changed-signature/refactor
surfaces checked so far are:

* advc.251 / practical 1407 (also present in the merged 1548 history):
  `getBuildTime` and `getBuildTurnsLeft` gained a PlayerTypes argument and
  `changeBuildProgress` changed its last argument from TeamTypes to PlayerTypes.
  The only shipped Python caller of these changed timing wrappers is
  `CvMainInterface.py`, which passes `pSelectedGroup.getOwner()` as the player.
  No shipped Python caller of `getBuildTime` or `changeBuildProgress` survives.
* practical 2612/2998 moved fort/domain logic toward `CvUnit` and added explicit
  NULL guards to `movementCost`, `isEnemyCity`, `isValidDomainForLocation` and
  `isValidDomainForAction`; no live caller regression was established.
* practical 3192 made `calculateImprovementYieldChange` ignore the retained
  Python `bOptimal` argument because the underlying CvPlot argument became
  obsolete. There is no shipped Python caller.
* practical 1900 adapted `getNearestLandArea` from an area ID return to a
  nullable CvArea pointer and explicitly restores the ID/invalid sentinel at the
  Python facade.
* practical 3912 removed an explicit `true` from `setRouteType`; the current
  CvPlot method defaults `bUpdatePlotGroup=true`, so WorldBuilder's one shipped
  `plot.setRouteType(...)` call retains the former semantics.
* the enum-map extra-move-cost migration, total-culture accessor change,
  defensive safeIntCast for river IDs and the K-Mod Boolean-sentinel cleanups
  reconcile with current contracts.

POINTER-ARGUMENT SURFACE — SHIPPED CALLERS DO NOT ACTIVATE THE SHARP EDGES
---------------------------------------------------------------------------
Several inherited wrappers dereference a Cy* argument without checking the
outer wrapper pointer (or its inner pointer), including `nukeExplosion`,
`isAdjacentToArea`, `shareAdjacentArea`, `getFeatureProduction`,
`getBestDefender`, `isVisibleEnemyDefender`, `getNumVisibleEnemyDefenders`,
`isTradeNetworkConnected` and `isPotentialCityWorkForArea`.

A shipped-Python grep found no current caller of those CyPlot signatures.
`getFeatureProduction` text hits are BuildInfo/City methods rather than this
three-argument CyPlot facade. Other pointer-taking wrappers such as
`isConnectedTo`, `movementCost`, `isEnemyCity` and the domain checks explicitly
validate their pointer arguments. Keep the unguarded set as mod/Python API
hardening unless a supported producer emerges; it does not justify F680.

NULLABLE RETURN-WRAPPER CONTRACT — POSITIVE CONTROL
----------------------------------------------------
The nullable object-return wrappers use the standard Civ4 Cy* convention:

    CyCity* CyPlot::getPlotCity()
    {
        return m_pPlot ? new CyCity(m_pPlot->getPlotCity()) : NULL;
    }

For a VALID plot with NO city, `m_pPlot->getPlotCity()` is NULL but the outer
return is still a non-NULL newly allocated `CyCity*`. `CyPlotInterface1.cpp`
exports this with `python::return_value_policy<python::manage_new_object>()`.
`CyCity.h` explicitly implements:

    bool isNone() { return (m_pCity == NULL); }

Therefore Python must test the returned city wrapper with `.isNone()`; Python
identity `is None` tests only whether Boost returned no CyCity wrapper at all.
The same managed-wrapper shape applies to `getWorkingCity`,
`getWorkingCityOverride`, nullable CyArea/CyPlot/CyUnit returns, etc. This
contract is also already recorded elsewhere in the album (e.g. Queue 046 notes
that `new CyCity(NULL)` is the normal nullable-city wrapper).

F679 — KI#300 REOPENED — TIMELINE FIX TESTS A NULL CyCity WRAPPER WITH PYTHON
`is None`, SO THE RAZED-HOLY-CITY NAME LEAK SURVIVES AFTER THE EMPTY PLOT IS
REVEALED
-----------------------------------------------------------------------------
Root owner is the current SAS Python consumer discovered while auditing the
CyPlot nullable-return contract; `CyPlot.cpp` itself is correct/inherited.

Practical 6217 / commit 8f6e4adb214d... was titled:

    Fix KI#300 and KI#322 AdvCiv-SAS Timeline and AIP regressions

Its KI#300 repair correctly stopped relying only on the current city name to
CLASSIFY religion-founding replay text, but its final no-city test was added as:

    if not self.bRevealAll and szHiddenReligionText is not None and \
            (pPlot is None or bPlotHidden or pPlot.getPlotCity() is None):
        szText = szHiddenReligionText
    elif not self.bRevealAll and pPlot is not None:
        pCity = pPlot.getPlotCity()
        if pCity is not None:
            ...

The two `getPlotCity()` Python identity tests contradict the wrapper contract
above. For a valid empty plot:

1. `CyPlot::getPlotCity()` returns `new CyCity(NULL)`, not a NULL `CyCity*`;
2. Boost therefore returns a Python CyCity object;
3. `pPlot.getPlotCity() is None` is FALSE;
4. `pCity is not None` is TRUE;
5. the null CyCity wrapper's guarded `getOwner()` returns NO_PLAYER and
   `getName()` returns an empty string;
6. the fallback replacement has no current city name to replace, leaving the
   historical replay text intact.

SUPPORTED KI#300 ACTIVATION
---------------------------
The branch practical 6217 was specifically intended to cover a religion whose
holy city is razed before an observer learns its historical name. Its own new
comment states that classification must remain independent of current plot
visibility because "a razed hidden holy-city plot can be revealed later."

That later-revealed state is exactly where the wrong identity test matters:

* the replay event has valid map coordinates, so `pPlot` is a valid CyPlot;
* the plot has subsequently become revealed, so `bPlotHidden == False`;
* the city was razed, so the underlying `CvPlot::getPlotCity() == NULL`;
* `szHiddenReligionText` is non-NULL because practical 6217 correctly recognizes
  the localized religion-founding template;
* the intended no-city branch nevertheless fails because the returned CyCity
  WRAPPER is not Python `None`;
* the fallback sees no surviving city name and cannot sanitize the historical
  city name embedded in the replay text.

If the plot is still hidden, `bPlotHidden` happens to make the new repair work.
Thus practical 6217 partially fixes KI#300 but misses its own explicitly noted
"razed plot can be revealed later" state.

The historical KI#300 documentation is consistent with why this escaped: the
fix was validated for ordinary visible founding-city text while the exact
razed-before-reveal branch was source-reviewed rather than directly reproduced.
Current HEAD still contains the erroneous identity checks.

DEDUP / STATUS
--------------
Whole-album search found no prior audit finding owning this wrapper-contract
mistake. KI#300 itself is an existing SAS known issue marked fixed; F679 does
not consume provisional KI#1001. It REOPENS KI#300 because the same promised
knowledge/spoiler contract remains violated in a surviving branch of the repair.

NARROW REPAIR BOUNDARY
----------------------
Do not change `CyPlot::getPlotCity()`; its managed-null-wrapper behavior is the
normal Civ4 Python API and changing it would break many callers.

Instead obtain the wrapper once and test its inner state, e.g. conceptually:

    pCity = pPlot.getPlotCity() if pPlot is not None else None
    bNoCity = (pCity is None or pCity.isNone())

then use `bNoCity` for the hidden-religion reconstruction and only query owner/
name when `not bNoCity`. For this replay path the plot coordinates are ordinary
valid replay coordinates; a broader cleanup could also use `pPlot.isNone()` for
CyMap's own managed-null-plot convention, but that is not required to establish
or repair F679's supported fixture.

CURRENT QUEUE-050 CURSOR AFTER F679
-----------------------------------
Queue 050 remains OPEN. F680 / provisional KI#1001 remains UNUSED.

Continue the remaining CyPlot facade audit rather than stopping at the reopened
Python consumer:
- complete wrapper-by-wrapper shipped Python enum/index argument domains;
- finish nullable return-wrapper consumers and lifetime/ownership controls;
- reconcile the remaining retained practical/K-Mod history strata;
- compare inherited AdvC deviations from K-Mod/Civ4CE for caller-semantic drift;
- whole-album dedup all surviving sharp edges;
- either promote an independent supported F680 or close Queue 050 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP552 physical -> Queue 050 CyPlot.cpp OPEN -> current SAS vs Base 1.14
executable-identical -> changed-signature/history first tranche clean -> F679
REOPENS KI#300 because `getPlotCity() is None` cannot detect `new CyCity(NULL)`
for a valid razed-city plot -> F680/provisional KI#1001 UNUSED -> continue enum/
index + nullable-return consumers + remaining history/dedup -> close or promote
only an independent F680 -> freeze next physical WIP.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP553 — QUEUE 050 CyPlot.cpp — CLOSURE AFTER F679 / KI#300 REOPEN
===============================================================================

STATUS
------
Queue 050 `CvGameCoreDLL/CyPlot.cpp` is CLOSED.
F679 / KI#300 REOPENED is CONFIRMED.
No independent F680 survives; F680 / provisional KI#1001 remains UNUSED.
No repository/gameplay source files modified.

POST-F679 WRAPPER CLOSURE PASS
------------------------------
The remainder of the 1,067-line facade was completed instead of stopping at the
reopened Timeline consumer.

1. C++ wrapper self-NULL safety

A mechanical body pass found only two current methods that dereference
`m_pPlot` itself without a local NULL check:

    getRiverWEDirection()
    getRiverNSDirection()

Both are inherited through K-Mod/Civ4CE/Base AdvC. Their shipped Python callers
are `CvWBDesc` and `savemap`, which invoke them while iterating valid map plots.
No supported null-CyPlot producer reaches those calls. Keep as generic Python
API hardening, not F680.

The remaining sharp dereferences are Cy* ARGUMENTS rather than `m_pPlot`:
`nukeExplosion`, `isAdjacentToArea`, `shareAdjacentArea`,
`getFeatureProduction`, `getBestDefender`, `isVisibleEnemyDefender`,
`getNumVisibleEnemyDefenders`, `isTradeNetworkConnected` and
`isPotentialCityWorkForArea`. No current shipped Python caller reaches these
CyPlot signatures. The guarded pointer-taking methods remain safe.

2. Managed-object return ownership/lifetime surface

All nine `new Cy*` return sites pair with Boost `manage_new_object` registration:
`getNearestLandPlot`, `getBestDefender`, `getSelectedUnit`, `area`, `waterArea`,
`getPlotCity`, `getWorkingCity`, `getWorkingCityOverride` and `getUnit`.

The underlying nullable pointer is intentionally represented by a live Cy*
wrapper whose `.isNone()` tests the inner game pointer. This is the positive
control that exposed F679; no second shipped caller was found using Python
`is None` incorrectly on the other nullable CyPlot returns.

3. Index/enum/sentinel domains used by shipped Python

The live callers were checked around the exposed mutators and state queries:

* WorldBuilder plot/terrain/feature/bonus/improvement/route IDs are resolved
  through their matching XML-info tables before forwarding. NO_* sentinel use
  (`setImprovementType(-1)`, feature variety `-1`) matches underlying contracts.
* `getBonusType(-1)` intentionally maps -1 to `NO_TEAM`; `CvPlot::getBonusType`
  explicitly treats `NO_TEAM` as “do not apply team revelation filtering.”
* `setRevealed(..., eFromTeam=-1)` intentionally supplies `NO_TEAM` and is
  supported by the underlying reveal path.
* the Customizable Domestic Advisor's
  `city.plot().defenseModifier(-1, True, False)` supplies `NO_TEAM` as defender;
  `CvPlot::defenseModifier` explicitly returns terrain/feature/hill defense
  before city-defense/team logic when defender is `NO_TEAM`. The AdvC wrapper's
  extra attacker-team argument is deliberately `NO_TEAM`, preserving the
  documented feature-defense behavior.
* `getUnit(i)` shipped plot callers either iterate `range(getNumUnits())` or use
  the debug index-0 helper; `CvPlot::getUnitByIndex` itself returns NULL when the
  linked-list index does not exist, which becomes the normal nullable CyUnit
  wrapper rather than an out-of-bounds access.

No supported invalid enum/index feed was established.

4. K-Mod -> AdvC default-argument drift check

The most promising inherited seam was `isCoastalLand`:

K-Mod header:

    bool isCoastalLand(int iMinWaterSize = -1) const;

current AdvC/SAS header:

    bool isCoastalLand(int iMinWaterSize = 0) const;

The unchanged Python facade calls `m_pPlot->isCoastalLand()` with no explicit
argument, and several shipped Python modules use it. Practical 1631 proves this
is NOT a semantic regression. Before that commit, a default `-1` reached the
ordinary comparison `waterAreaSize >= -1`, which trivially meant “any adjacent
passable water.” Practical 1631 newly assigned a special meaning to NEGATIVE
input (`MIN_WATER_SIZE_FOR_OCEAN`) and changed the default to 0 in the same
hunk specifically to preserve the prior no-argument “any water” result.

The other newer underlying optional arguments likewise preserve the old facade
semantics through defaults:
- `getFoundValue(player)` leaves `bRandomize=false`; scenario AI code requests
  randomization explicitly;
- `canHaveBonus` / `canHaveFeature` leave the newer ignore-current/feature flags
  false;
- added improvement/build/reveal flags default to the inherited behavior;
- `getFoundValue`'s shipped map-generator caller therefore remains deterministic
  as before.

No default-argument semantic drift survives.

5. Historical wrapper strata

All 29 retained `CyPlot.cpp` history diffs were reconciled. The semantic strata
are exhausted by:
- K-Mod's original wrapper plus Boolean-sentinel cleanup;
- AdvC player-aware build-time/progress signatures;
- area object/ID adaptation;
- fort/domain/reference refactors and added pointer guards;
- total-culture accessor optimization;
- NO_TEAM attacker support in `defenseModifier`;
- enum-map extra move-path-cost storage;
- typed CultureLevel range parameters;
- obsolete `bOptimal` removal under the retained Python ABI;
- short-safe river ID cast;
- route setter default-argument cleanup;
- constructor/include/accessor refactors;
- SAS's sole line-wrap-only mirror.

No unported Base AdvC 1.14 fix exists because current SAS and Base 1.14 are
executable-identical in this file.

6. Whole-album dedup / cross-file consumer control

F679 is intentionally attached to the existing fixed KI#300 rather than
consuming new KI#1001. The album already records the `new CyCity(NULL)` nullable
wrapper convention during Queue 046, independently confirming the API contract.
No earlier audit root owns practical 6217's Python `is None` mistake.

Other CyPlot-adjacent sharp states (coast/lake behavior, area/reveal contracts,
plot-group connectivity, found-value caching, irrigation etc.) are already
owned by their corresponding CvPlot/CvMap/player findings where applicable and
were not duplicated here.

QUEUE-050 CLOSURE
-----------------
Queue 050 has now been audited through:
- current SAS vs Base AdvC 1.14 executable comparison;
- K-Mod/Civ4CE facade and underlying-signature oracles;
- all retained CyPlot-specific history strata;
- self-NULL and pointer-argument dereference census;
- managed-new-object return/lifetime conventions;
- shipped Python caller domains for changed/sentinel-sensitive wrappers;
- underlying default-argument drift from K-Mod to AdvC;
- whole-album dedup and current SAS consumer inspection.

One supported root survives: F679 reopening KI#300. No independent F680 does.

AUDIT PROGRESS AFTER CLOSURE
----------------------------
Files complete: 74 / 169.
Next ordinary unfinished file: Queue 051 `CvGameCoreDLL/CyTeam.cpp`
(1,042 queued lines).
Next new root ID remains F680 / provisional KI#1001.

Exact continuation stone:
C031-WIP553 physical -> Queue 050 CLOSED -> F679 REOPENS KI#300 -> F680 /
provisional KI#1001 UNUSED -> ordinary Queue 051 CyTeam.cpp next -> current/Base
semantic delta + nullable pointer/index/enum wrapper contracts + shipped Python
caller domains + history/dedup -> freeze next physical WIP at next settled
boundary.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP554 -- QUEUE 051 CyTeam.cpp -- CLOSED / F680 UNUSED
===============================================================================

CLOSURE STATE
-------------
- Parent physical stone: C031-WIP553.
- Queue 051 `CvGameCoreDLL/CyTeam.cpp` is CLOSED.
- No new root was promoted.
- F680 / provisional KI#1001 remains UNUSED.
- Files complete: 75 / 169.
- Next ordinary unfinished file: Queue 052 `CvGameCoreDLL/CvXMLLoadUtilityGet.cpp`
  (973 queued lines).
- No gameplay source was modified.

CURRENT / BASE / LINEAGE BASELINE
---------------------------------
Current SAS and supplied Base AdvC 1.14 `CyTeam.cpp` are executable-identical after
comments/whitespace are removed. The only textual differences are SAS line wrapping.
Therefore there is no unported Base-1.14 wrapper fix and no current SAS-only
executable change to attribute a new defect to.

The supplied K-Mod and Civ4CE facades were used as older semantic oracles. The ten
common K-Mod->AdvC body changes all reconcile with explicit later engine changes:
- `getAtWarCount`, `getWarPlanCount`, `getAnyWarPlanCount` and
  `getChosenWarCount` route to the later cached/AI-owned war-plan APIs introduced
  by practical 1677;
- `countEnemyPowerByArea` / `countEnemyDangerByArea` route to their later AI-owned
  helpers;
- TechShare wrapper parameters changed from TeamTypes/int to PlayerTypes because
  practical 3116 fixed Internet/TechShare to count PLAYERS rather than teams;
- `AI_getWarSuccess` rounds the later ScaledNum storage from practical 3662.
No stale facade semantics survive those migrations.

POINTER / LIFETIME SURFACE
--------------------------
`CyTeam` owns no returned heap wrapper objects and has no new/delete ownership surface.
The constructor deliberately stores the AI view of the same team object after
practical 1676 made the AI functions non-virtual.

The only unguarded external wrapper pointer is `CyArea* pArea` in seven inherited
count helpers:
- countNumUnitsByArea
- countNumCitiesByArea
- countTotalPopulationByArea
- countPowerByArea
- countEnemyPowerByArea
- countNumAIUnitsByArea
- countEnemyDangerByArea

Each dereferences `pArea->getArea()` after only testing `m_pTeam`. A forged Python
`None` could therefore crash, but the complete shipped-Python search found ZERO
callers of all seven wrappers. Preserve as generic extension/API hardening, not F680.

SHIPPED PYTHON ARGUMENT-DOMAIN SWEEP
------------------------------------
The live callers of changed or index-sensitive team wrappers stay inside the
underlying C++ contracts:
- diplomacy/random-event team IDs come from live `CvPlayer::getTeam()` or bounded
  team loops;
- `declareWarEvent` / `makePeaceEvent` use real counterpart teams and the intended
  event flags introduced by advc.106g;
- WorldBuilder `setHasTech(..., NO_PLAYER, ...)` is explicitly supported by
  `CvTeam::setHasTech`, which replaces NO_PLAYER with the team leader;
- WorldBuilder project art writes call `changeProjectCount(...,+1)` first, then use
  `projectCount - 1`, so the art-instance ordinal is a newly-created valid slot;
- SpaceShipScreen project/default-art getters iterate real ProjectTypes;
- `AI_estimateYieldRate` is called by TopCivs with a real player and
  `YIELD_COMMERCE`;
- `AI_getWarSuccess` event checks use real opponent teams; rounding the ScaledNum
  preserves the public integer facade intended by practical 3662;
- `getAnyWarPlanCount(true)` uses the documented `NUM_WARPLAN_TYPES` sentinel in
  `AI_countWarPlans`;
- espionage, vassal, open-borders, victory and research getters use bounded IDs
  derived from game/player/XML loops.
No shipped caller was found passing a NO_* sentinel into an underlying map/array
access that does not support it.

DEDUP / EXISTING-OWNER CONTROLS
-------------------------------
Two apparently interesting wrapper consumers are already controlled elsewhere:
- `getHasMetTurn` accurately exposes the timestamp added by practical 2468; the
  Permanent-Alliance failure to preserve that timestamp is the already-owned
  F079 root in `CvTeam.cpp`, not a CyTeam facade defect.
- `AI_estimateYieldRate` deliberately uses demographic/history information in UWAI;
  that information-use question was explicitly closed as tuning/design rather than
  a new defect during the Queue-058 ArmamentForecast saturation pass.

Other team-state sharp edges found by the earlier `CvTeam.cpp` audit remain owned by
that file (PA migration, tech bookkeeping, victory state, espionage state, etc.).
CyTeam merely exposes the resulting state and does not create those roots.

HISTORY SATURATION
------------------
All 25 `CyTeam.cpp` path-history entries were reconciled. The substantive strata are:
- K-Mod initial facade and canEventuallyDeclareWar exposure;
- advc.130v capitulation exposure;
- advc.155 alive/master-team exposure;
- advc.106g event-aware war/peace wrappers;
- advc.152 denial helper;
- AI-class/non-virtual refactor;
- cached war-plan API migration;
- EnumMap / player-count TechShare correction;
- AI area-helper migration;
- advc.091 first-contact turn exposure;
- UWAI yield-estimate exposure;
- ScaledNum war-success rounding;
- later style/line-wrap-only SAS changes.
No remaining history hunk creates a supported wrapper-contract mismatch.

QUEUE-051 CLOSURE
-----------------
Queue 051 has now been audited through:
- current SAS vs Base AdvC 1.14 executable comparison;
- K-Mod/Civ4CE facade comparison;
- all direct external-pointer dereferences;
- changed/default/sentinel-sensitive wrapper semantics;
- shipped Python caller argument domains;
- project-art / research / espionage / diplomacy / vassal consumers;
- all retained path-history strata;
- whole-album dedup against existing team-state findings.

No independent supported root survives. F680 remains unused.

AUDIT PROGRESS AFTER CLOSURE
----------------------------
Files complete: 75 / 169.
Next ordinary unfinished file: Queue 052 `CvGameCoreDLL/CvXMLLoadUtilityGet.cpp`
(973 queued lines).
Next new root ID remains F680 / provisional KI#1001.

Exact continuation stone:
C031-WIP554 physical -> Queue 051 CLOSED -> F680/KI#1001 UNUSED -> ordinary
Queue 052 CvXMLLoadUtilityGet.cpp next -> current/Base semantic delta + XML getter
pointer/index/buffer contracts + call-site/schema/data census + history/dedup -> freeze
next physical WIP at next settled boundary.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP555 -- QUEUE 052 CvXMLLoadUtilityGet.cpp -- F680 CONFIRMED / OPEN
===============================================================================

HANDOFF / AUTHORITATIVE CURSOR
------------------------------
- Working source base: reconciled `AdvCiv-SAS_light_source_20260909T081735.zip`.
- Queue 052 `CvGameCoreDLL/CvXMLLoadUtilityGet.cpp` remains OPEN.
- F680 / provisional KI#1001 is CONFIRMED and frozen below.
- F681 / provisional KI#1002 is UNUSED.
- Queue 028 remains closed; F680 is a cross-file activation discovered during the
  Queue-052 raw-string getter audit and refines/supersedes one earlier Queue-028
  negative control rather than reopening Queue 028 as a full file audit.
- No gameplay/XML source was modified.

QUEUE-052 PROVENANCE BASELINE
-----------------------------
Current SAS `CvXMLLoadUtilityGet.cpp` is executable-semantically identical to the
supplied Base AdvC 1.14 file after comment/whitespace normalization:

    normalized bytes = 18,153
    SHA-256 = d3643f2b5c92a6030c4f6afb9f67a45d423e5eaafc1dbc9c5e05460b7960423b

Supplied K-Mod 1.46 and Civ4CE retain the older equivalent raw-string XML interface
and are executable-identical to one another for this file. There is therefore no
missed Base-1.14 repair to attribute here; the weakness is inherited, while current
SAS data creates the first demonstrated shipped activation found in this audit.

F680 / provisional KI#1001
--------------------------
TITLE
-----
Current SAS overlong GlobalDefine violates the inherited 256-byte raw XML string buffer

ROOT
----
`CvXMLLoadUtility::GetXmlVal(char*)` forwards an unbounded raw pointer to the closed
FXML interface:

    gDLL->getXMLIFace()->GetLastNodeValue(m_pFXml, r);

The interface itself exposes only:

    virtual bool GetLastNodeValue(FXml* xml, char* pszText) = 0;

There is no destination-capacity parameter. The source even notes that a safe raw
`{w}char*` helper is missing; the size-safe overloads are the std::string/std::wstring
paths.

The inherited GlobalDefines loader in `CvXMLLoadUtilitySet.cpp` uses that raw API in
both text/string branches through a fixed stack buffer:

    char szVal[256];
    GetXmlVal(szVal);
    GC.getDefinesVarSystem()->SetValue(szName, szVal);

Thus a DefineTextVal longer than 255 bytes cannot be represented in the destination
including its terminating NUL.

CURRENT SHIPPED SAS ACTIVATION
------------------------------
`Assets/XML/GlobalDefines_advciv_sas.xml` currently contains:

    <DefineName>SAS_MAP_SCRIPT_NAMES_LAND_HEAVY</DefineName>

with this 297-byte ASCII DefineTextVal:

    aDebugMap,Arboria,Balanced,BTG_Cross,BTG_Grid,BTG_Lagoon,Continents,Custom_Continents,Donut,Earth2,Fractal,Global_Highlands,Hemispheres,Ice_Age,Inland_Sea,Maze,Mixed_Continents,Pangaea,Peirce,Planet_Generator_0_68,Ring,SAS_Longworld,SAS_Parallel_Lines,SAS_Spiky_Avenues,Tectonics,Terra,Water,Wheel

The value therefore needs 298 bytes including NUL but is read into `char[256]`.
This is not a custom-file or malformed-input edge: `SetGlobalDefines()` normally loads
`xml\\GlobalDefines_advciv_sas.xml` during startup, and `DefineTextVal` takes the
string/text branch above.

CAPACITY / CONSEQUENCE PROOF
----------------------------
Because the closed FXML call receives no buffer capacity, the full 297-byte value
cannot safely fit in the supplied 256-byte destination. The exact closed-EXE behavior
for an overlong value is not locally visible, so do not overclaim one implementation:

1. If the EXE copies the full value plus NUL, the call writes 42 bytes beyond the
   256-byte destination.
2. If it imposes an undocumented legacy 255-character cap/truncation internally,
   the live define is truncated and its tail is lost/corrupted.

Either behavior violates the current SAS data contract. A 256-byte destination cannot
contain the intended string losslessly and safely.

The truncation boundary is gameplay-relevant. Current positions near/after byte 255 are:

    SAS_Parallel_Lines  233..250
    SAS_Spiky_Avenues   252..268
    Tectonics           270..278
    Terra               280..284
    Water               286..290
    Wheel               292..296

At a 255-character cap, `SAS_Spiky_Avenues` is already cut mid-token and the later map
scripts disappear entirely.

LIVE CONSUMER
-------------
`CvGame::SAS_isMapScriptListedInDefine()` reads this define through
`GC.getDefineSTRING`, tokenizes it on commas and compares the current map-script name.
`CvGame::recomputeMapnameHeaviness()` uses the result to populate
`m_bLandHeavyMapname`, which feeds SAS map-heaviness AI/gameplay behavior. Therefore a
truncated/corrupted tail is not diagnostic-only configuration text.

DATA CENSUS
-----------
A current SAS GlobalDefines census found 994 Define entries in the relevant files.
The largest DefineName is 126 bytes. `SAS_MAP_SCRIPT_NAMES_LAND_HEAVY` is the sole
current DefineTextVal at or above 256 bytes and is the maximum at 297.

Supplied Base AdvC 1.14 has 250 defines, maximum DefineName length 42 and maximum
DefineTextVal length 14; none reaches 256. Thus the inherited raw-buffer weakness is
dormant in the supplied Base data even though Base/K-Mod/Civ4CE retain the same
fixed-buffer loader pattern.

SAS REGRESSION LINEAGE
----------------------
The map-script list was initially introduced below the dangerous size. Relevant
historical lengths reconstructed from the supplied SAS diffs include:

    practical 5415: 229
    ...
    practical 5455: 237
    practical 5456: 255  (last value that exactly fits char[256] including NUL)
    practical 5457: 272  (first definite capacity violation)
    practical 5458: 278
    practical 5486/current lineage: 297

Practical 5457 / `3fffd5ea98c6ba9287173e65d7733feb32ee5098`, titled
"minor - also make Mixed_Continents.py land_heavy, and remove it from Simple Game",
changed the value from exactly 255 characters to 272. That is the first demonstrated
current-SAS activation of the inherited capacity defect.

WHOLE-ALBUM DEDUP / PRIOR-CONTROL CORRECTION
--------------------------------------------
No earlier album finding owns this raw-string GlobalDefines capacity root. Searches
for the define name, `szVal[256]`, and the raw `GetLastNodeValue(char*)` defect found
no prior owner.

Queue 028 `CvXMLLoadUtilitySet.cpp` was closed at C031-WIP516 with F657. Its saturation
control #13 stated that the current shipped GlobalDefines set was valid for normal
startup because all required files resolved and loaded. That control checked
presence/path resolution, not string-value capacity. F680 therefore explicitly
REFINES/SUPERSEDES that narrow negative control: the files are present, but one
current shipped value is too long for the inherited raw loader buffer.

REPAIR BOUNDARY
---------------
The narrow robust repair is to stop using a fixed raw buffer for text defines. In both
string/text branches of `ReadGlobalDefines`, use the existing size-safe string overload,
for example conceptually:

    CvString szVal;
    GetXmlVal(szVal);
    GC.getDefinesVarSystem()->SetValue(szName, szVal.c_str());

`FVariableSystem::SetValue(char const*, T)` accepts the resulting C string through its
existing templated setter. This preserves the full current value and removes the
future length ceiling instead of merely moving it from 256 to 512.

The same file also has `char szName[256]` and `char szNodeType[256]`. Current SAS data
keeps DefineName safely below 256 and node-type text is tiny, so those are preventive
hardening surfaces, not part of current F680. A systematic follow-up could migrate
internal raw char/wchar XML reads to the existing std::string/std::wstring overloads,
but F680 does not require claiming every raw overload as independently defective.

QUEUE-052 NEGATIVE CONTROLS ALREADY CLOSED
-------------------------------------------
- The 108-entry hotkey table covers every nonempty current shipped `<HotKey>` value
  found in the supplied tree (46 distinct values), with no duplicate mapping labels.
- The bool named-child overload's unusual `(mandatory, default)` argument order has no
  shipped dangerous call-site inversion: no three-argument `true` call is using the
  mandatory slot as though it were a true default, and the explicit `false` calls are
  consistent with optional false-valued tags.
- `SetGlobalTypeFromChildXmlVal` / `SetInfoIDFromChildXmlVal` default/sentinel behavior
  remains intentional under the XML-default inheritance design; no supported mismatch
  has yet survived.

QUEUE-052 CONTINUATION
----------------------
F680 is frozen. Continue Queue 052 under F681 / provisional KI#1002 UNUSED:
- finish the remaining raw-buffer caller census (`szName`, info-class temp buffers,
  wchar paths) against current shipped maximum lengths;
- finish named-child cursor/default/sentinel contracts and enum `GetNextXmlVal` uses;
- reconcile practicals 3566/3183/3052/3047/2868/2236/2223/2126/1783/1490 and the
  remaining history entries against current consumers;
- perform whole-album dedup for any surviving independent root;
- either promote a concrete F681 or close Queue 052 cleanly;
- freeze the next physical WIP at the next settled boundary.

AUDIT PROGRESS AT THIS STONE
----------------------------
Files complete: 75 / 169.
Files open: 1 / 169.
Current open: Queue 052 `CvGameCoreDLL/CvXMLLoadUtilityGet.cpp`.
Confirmed in this queue: F680 / provisional KI#1001.
Next new root: F681 / provisional KI#1002 UNUSED.

Exact continuation stone:
C031-WIP555 physical -> Queue052 OPEN -> F680/KI#1001 CONFIRMED -> F681/KI#1002
UNUSED -> finish raw-buffer/data-domain + named-child/default/enum/history/dedup ->
either promote F681 or close Queue052 -> freeze next physical WIP.

No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified by
ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP556 -- QUEUE 052 CvXMLLoadUtilityGet.cpp -- CLOSED / QUEUE 053 OPEN
===============================================================================

HANDOFF STATE
-------------
- Parent physical stone: C031-WIP555.
- Working source base remains the reconciled `AdvCiv-SAS_light_source_20260909T081735.zip`.
- Queue 052 `CvGameCoreDLL/CvXMLLoadUtilityGet.cpp`: CLOSED.
- Confirmed Queue-052 root: F680 / provisional KI#1001.
- No independent F681 was promoted from the remaining Queue-052 surface.
- F681 / provisional KI#1002 remains UNUSED and becomes the cursor for Queue 053
  `CvGameCoreDLL/EnumMapTest.cpp`.
- Files complete: 76 / 169; one file open (Queue 053).
- No SAS/Base AdvC/K-Mod/Civ4CE/Taurus gameplay/XML source was modified.

QUEUE-052 FINAL RAW-BUFFER CENSUS
---------------------------------
The WIP555 follow-up raw-pointer census has now been completed against the current supplied
source/data rather than extrapolating from F680.

1. Current SAS non-text XML leaf values vs. the common 256-byte temporaries -- F680 IS THE
   ONLY SHIPPED CAPACITY VIOLATION FOUND

   A mechanical scan of all 186 XML files in the supplied light-source archive found the
   following maxima among representative raw-string domains used by the DLL readers:

       DefineName              126 bytes
       Type                     53 bytes
       BuildingClassType        35 bytes
       UnitClassType            30 bytes
       PrereqTech               24 bytes
       FeatureType              20 bytes
       BonusType                15 bytes
       Path                    123 bytes
       NIF / SHADERNIF          77 bytes
       KFM                      71 bytes

   Outside localized Text XML and BUG Config XML (which are not these CvInfo raw-buffer
   consumers), the only current XML leaf at or above 256 bytes is the already-confirmed
   297-byte `SAS_MAP_SCRIPT_NAMES_LAND_HEAVY` DefineTextVal. Thus the broad 256-byte
   `szTextVal`/`szName`/iterator temporary family does not supply a second present-data root.

2. The inherited 32-byte river/route-model buffers -- CURRENT/STOCK DATA FIT

   `CvRiverModelInfo` and `CvRouteModelInfo` retain raw `TCHAR[32]` members for DeltaType,
   Connections, ModelConnections and Rotations. The unchanged base XML is not duplicated in
   every SAS file, so the supplied Civ4CE base/BtS data and supplied Taurus route-model data
   were used as concrete stock-lineage fixtures. Their maxima are:

       DeltaType          23 bytes  `DELTA_COUNTER_CLOCKWISE`
       Connections        19 bytes  `N NE E SE S SW W NW`
       ModelConnections   11 bytes  `NE SE SW NW`
       Rotations          12 bytes  `0 90 180 270`

   These all fit including NUL. Current SAS-owned Feature art `Connections` reaches only
   11 bytes. The 32-byte declarations remain brittle API design, but no shipped overflow is
   demonstrated and they do not consume F681.

3. Wide-character raw overloads -- NO CURRENT FIXED-ARRAY XML ACTIVATION FOUND

   The DLL still exposes the inherited `wchar*` GetXmlVal/GetNextXmlVal/GetChildXmlVal and
   named-child overloads without a capacity parameter. Current fixed wchar arrays found in
   the tree are UI/text formatting buffers, not XML getter destinations. This remains generic
   hardening debt rather than a second Queue-052 finding.

4. Raw defaults -- NO CURRENT DEFAULT-COPY OVERFLOW

   `assignDefault(char*)` / `assignDefault(wchar*)` also use unbounded strcpy/wcscpy. Current
   fixed-array XML call sites that explicitly supply string defaults use the empty string;
   no overlong shipped default was found. This shares the same size-less raw API weakness as
   F680 but has no independent activation.

NAMED-CHILD / DEFAULT / CURSOR CONTRACTS
----------------------------------------
- The AdvC named-child overloads restore the parser to the parent on every successful read
  and on the in-child error branch. Missing optional children leave the parser at the caller's
  level. The pre-lookup `SkipToNextVal()` added by practical 3566 is the deliberate fix for
  XML comments and matches current comment-bearing data.
- The first-child `GetChildXmlVal` family intentionally leaves the parser at the child on
  success so callers can iterate siblings with `GetNextXmlVal`; treating that as a missing
  SetToParent would invert its API contract. The only unbalanced failure is malformed/comment-
  only child data after an actual descent, which has no current shipped producer.
- Integer named-child defaults use MIN_INT as the mandatory sentinel; optional callers pass a
  real default. Float uses `arithm_traits<float>::min`, which current ArithmeticTraits defines
  as `-FLT_MAX`. Practical 4241 specifically repaired the former misguided FLT_MIN sentinel.
  Current callers therefore distinguish a missing mandatory node from valid zero/negative
  values as intended.
- The bool overload's unusual argument order `(bMandatory, bDefault)` was rechecked against
  current calls. No three-argument true value is being passed as though it were a default;
  explicit optional-false calls remain consistent with their XML schemas.
- `SetGlobalTypeFromChildXmlVal` retains the XML-default inheritance contract already closed
  at WIP555: inherited nonzero values use an empty-string fallback to mean "leave inherited
  value unchanged", while zero/uninitialized values require a node when mandatory checking is
  enabled. No current sentinel/type-index mismatch survived.

ENUM / NARROW-VALUE FOLLOW-UP
-----------------------------
- `GetNextXmlVal(short&)` and `GetNextXmlVal(char&)` first parse through int and then use
  `safeIntCast`, so their explicit narrow scalar path is range-checked rather than silently
  truncating.
- The enum `GetNextXmlVal(T&)` template parses through int and casts to the target enum. Its
  current generic enum-map consumers do not use it for enum-to-enum XML pairs: those pairs are
  read as strings and resolved through `getGlobalEnumFromString`. Compact EnumMap storage also
  exposes its logical `ValueType` to the XML iterator, so an `int` value stored compactly in
  char/short form is parsed as int before the map's own checked encoding boundary.
- No current shipped numeric enum input outside its declared domain was found. A malformed XML
  integer could still create an invalid enum through the public template; that is robustness
  debt, not a current F681 activation.

HISTORY RECONCILIATION
----------------------
The full retained path history is now reconciled against the current bodies:
- practical 1398/1490 introduced and tightened mandatory-node diagnostics;
- 1548 is merge/refactor history around the same parser code;
- 1783 imported the MNAI empty-element checks;
- 2126 moved mandatory-assert state into the utility implementation while addressing Wine
  memory corruption elsewhere;
- 2222/2223/2236 introduced and merged XML-default inheritance helpers;
- 2287 is setter cleanup/formatting in this file;
- 2503 converts unconditional false asserts to FErrorMsg diagnostics;
- 2868 cleans up global-enum string lookup;
- 2998/3392/3579 are merge strata carrying those changes;
- 3047 is minor refactoring;
- 3052/3183 introduce and then harden nested EnumMap narrow-value parsing with safeIntCast;
- 3566 fixes comment parsing before named-child lookup;
- 4241 changes the float mandatory sentinel from FLT_MIN to `arithm_traits<float>::min`;
- SAS practicals 5372/5961 contribute no executable semantic regression here (later media/
  formatting/signature-collapse maintenance only).

Current SAS remains executable-semantically identical to supplied Base AdvC 1.14 for this file,
so no later upstream repair was missed. K-Mod/Civ4CE/Taurus retain the same broad size-less raw
string interface; none supplied a current oracle that invalidates the closure above.

WHOLE-ALBUM DEDUP / CLOSURE JUDGMENT
------------------------------------
Queue 028 already audited the iterator depth/error paths and malformed key trust-after-assert
surfaces. Queue 030 audited the CvInfo_Misc producers containing the 32-byte route/river fields.
The Queue-052 pass does not reopen those files merely because their callers use this getter API.
F680 remains the one current shipped root exposed by the file: a current SAS 297-byte value
crosses a real inherited 256-byte destination boundary. Everything else in the remaining file
reduced to current-safe data, intentional cursor/default semantics, already-owned cross-file
controls, or malformed/mod XML hardening without a shipped activation.

QUEUE 052 IS CLOSED. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Open ordinary Queue 053 `CvGameCoreDLL/EnumMapTest.cpp` (970 queued lines) under
F681 / provisional KI#1002 UNUSED. The priority reloop is already complete; the old per-row
"skip until priority next WIPs" annotation is no longer operative.

Exact continuation stone:
C031-WIP556 physical -> Queue052 CLOSED -> F680/KI#1001 retained -> no independent F681 ->
Queue053 EnumMapTest.cpp OPEN -> F681/KI#1002 UNUSED -> establish build/runtime reachability +
current-vs-Base/K-Mod/Civ4CE/Taurus provenance -> audit assertions, boundary cases, serialization/
compact-map tests and any accidental runtime hooks -> either promote F681 or close Queue053 ->
freeze next physical WIP.

No gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP557 - ORDINARY QUEUE 053 CLOSURE / QUEUE 055 OPEN
===============================================================================

QUEUE 053
---------
File: CvGameCoreDLL/EnumMapTest.cpp
Queued size: 970 lines
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

RUNTIME / BUILD REACHABILITY
----------------------------
The file initially looks like pure dormant test code, but the exact runtime hook matters:
`CvXMLLoadUtilitySet.cpp::LoadPreMenuGlobals` declares and calls `TestEnumMap()` immediately after
`DestroyFXml()`. Therefore it would be wrong to describe the function as uncalled.

However, the executable body of `TestEnumMap()` is entirely wrapped in:

    #ifdef _TEST_ENUM_MAP
        ... all test bodies ...
    #endif

and the same guard encloses `TestStream` plus the helper test routines. The only definition in
this file is deliberately commented out:

    //#define _TEST_ENUM_MAP // advc: Uncomment to enable the tests

No supplied Visual Studio configuration or make/build definition defines `_TEST_ENUM_MAP`.
Consequently the shipped XML-bootstrap call reaches an empty function. No test object, EnumMap
mutation, serialization operation or assertion from this translation unit exists in the shipped
preprocessed program.

An early narrow reference search during this tranche briefly missed the `LoadPreMenuGlobals`
caller. The source-level result above is authoritative: one live caller, but an empty shipped
callee. This correction is recorded so the stale "uncalled" interpretation is not propagated.

CURRENT / ANCESTRY BASELINE
---------------------------
Current SAS and supplied Base AdvC 1.14 are executable-semantically identical for this file after
comments/whitespace/newline normalization (normalized SHA-256
`bb3bdfa8c640ce1952d28da5464e9c932929b8eea63b5a44c8d3e841a30bd007`). The raw files differ only
in formatting/newline representation. The supplied K-Mod, Civ4CE and Taurus trees do not carry
this AdvC EnumMap test translation unit, so there is no downstream oracle or SAS-only divergence
to reconcile here.

The retained path history is compact and coherent:
- practical 1774 merged the EnumMap tests and added the XML-bootstrap `TestEnumMap()` hook;
- 1794 merged `CvEnumMap::getTotal` test coverage and removed the old `_DEBUG` conjunction while
  retaining the explicit `_TEST_ENUM_MAP` opt-in;
- 2545 is surrounding enum/defines cleanup;
- 3108 added SparseEnumMap-era coverage;
- 3183 rewrote the enum-map classes and correspondingly rebuilt this harness around `TestStream`;
- 3186 added regression coverage for premature ArrayEnumMap allocation;
- 3392 is merge history;
- 3825 added the ListEnumMap `nextNonDefault` regression cases and related monotone/end-marker
  coverage.

The history therefore reads as a developer regression harness tracking EnumMap implementation
repairs, not as gameplay logic resident in this file.

DISABLED-HARNESS REVIEW
-----------------------
The guarded test suite was still read rather than dismissed solely because the macro is off. It
covers ordinary/list/array/compact EnumMaps, bool and pointer values, nonzero/default sentinels,
2D maps, serialization round trips, monotone/non-default iteration, compact encoded maps and the
specific historical regressions above. Nothing in the current test bodies escapes the guard.

Several old `TestStream` interface stubs are imperfect in isolation. In particular the raw
`ReadString(char*)` overload merely reassigns its local pointer, and the wchar string writer/reader
use inconsistent element widths. Those helpers are not called by the test bodies (the only
`ReadString`/`WriteString` references in this file are their own method definitions), and the
whole `TestStream` class is itself compiled out in the shipped configuration. Other deliberately
unimplemented FDataStream methods likewise remain test-harness scaffolding. These are not current
runtime defects and do not consume F681.

CLOSURE JUDGMENT
----------------
Queue 053 has no shipped executable semantic surface beyond one empty function call during XML
bootstrap. Current SAS matches Base AdvC 1.14, the guarded harness does not leak side effects into
production, and the imperfections found inside the opt-in test stream are neither compiled nor
exercised by the test cases that remain. There is therefore no supportable current gameplay,
save/load, UI or AI root to assign here.

QUEUE 053 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Queue 054 GroupPathFinder.cpp is already COMPLETE at its earlier priority checkpoint. Resume the
first unfinished ordinary entry: Queue 055 `CvGameCoreDLL/CyInfoInterface3.cpp` (837 queued lines)
under F681 / provisional KI#1002 UNUSED. The old row-local "skip until priority next WIPs" marker
is obsolete because the priority reloop is complete.

Exact continuation stone:
C031-WIP557 physical -> Queue053 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue054 already
COMPLETE -> Queue055 CyInfoInterface3.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus +
path-history provenance -> audit Python exposure signatures, enum conversions, lifetime/reference
policies, writable setters and any SAS-only bindings -> dedup against prior Cy*/CvInfo findings ->
either promote a genuinely live F681 or close Queue055 cleanly -> freeze next physical WIP.

No gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP558 - ORDINARY QUEUE 055 CLOSURE / QUEUE 056 OPEN
===============================================================================

SOURCE / CURSOR RECONCILIATION
------------------------------
This checkpoint is based on the newly supplied reconciled light-source snapshot
`AdvCiv-SAS_light_source_20260909T154245.zip`, not on an unreconciled earlier WIP copy. Its album
already contains the authoritative WIP557 Queue-053 closure and includes the user's intervening
header/reconciliation edits. Those edits are preserved here.

QUEUE 055
---------
File: CvGameCoreDLL/CyInfoInterface3.cpp
Queued size: 837 lines
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

CURRENT / ANCESTRY BASELINE
---------------------------
The current binding table was compared mechanically with supplied Base AdvC 1.14. Every Base
binding survives in current SAS with the same Python name and native target; current SAS adds 50
binding entries for its later Sevopedia/data-inspection needs. There is therefore no Base-1.14
binding repair missing from the current file.

K-Mod/Civ4CE/Taurus provide useful ancestry controls. Civ4CE and Taurus carry the same inherited
Interface3 body for this surface, while AdvC later reorganized some info bindings (notably moving
Yield/Terrain exposure to Interface2) and added Python-safe wrappers for selected indexed getters.
The current file retains those AdvC protections plus the SAS additions.

SAS-ONLY / CURRENT-LIVE EXPOSURES
---------------------------------
The SAS additions are not treated as harmless merely because this is a Boost.Python registration
unit. Their shipped consumers were traced:
- CvLeaderHeadInfo additions feed the Sevopedia AI-personality and per-era leader-art views;
- CvWorldInfo / CvEraInfo additions feed the World Size, Era and Music/track views;
- CvCorporationInfo::getBonusProduced feeds current corporation/policy UI;
- the expanded CvEventTriggerInfo / CvEventInfo surface feeds the current Sevopedia event/trigger
  inspection page.

The custom indexed getter pairs reconcile with those callers. Soundtrack names are requested only
inside `getNumSoundtracks()` bounds; Event/EventTrigger text, news and requirement arrays are
iterated through their paired `getNum*` counts; per-era leader art is queried only with
`range(gc.getNumEraInfos())`. CvCorporationInfo::py_getBonusProduced is scalar. No shipped producer
was found passing an invalid index to these new surfaces.

BINDING-ID / TARGET PARITY
--------------------------
A mechanical per-class scan found no duplicate Python method names in the current Interface3
registration table. The apparent name/target differences were checked rather than assumed wrong:
- `CvEspionageMissionInfo.getStealTreasuryTypes` intentionally maps to the renamed native
  `getStolenGoldPercent`; AdvC kept the older Python API spelling for compatibility, and the current
  Foreign Advisor uses that alias;
- `CvEventTriggerInfo.getPlotsType` maps to the singular native `getPlotType` as an inherited API
  naming choice;
- `pyGet*` / `py_*` targets are deliberate Python-safe or string-conversion wrappers.
No remaining wrong-class or wrong-getter binding survived this pass.

KI#122 CONTROL / DEDUP
----------------------
The three especially suspicious historical binding bugs are already fixed in the reconciled
source and explicitly owned by KI#122:
- EventTrigger `getReligionRequired` now targets `getReligionRequired` rather than
  `getBonusRequired`;
- EventTrigger `isPickOtherPlayerCity` now targets `isPickOtherPlayerCity` rather than
  `isPickCity`;
- EventInfo `getNumUnits` now targets `CvEventInfo::getNumUnits` rather than the EventTrigger
  class method.
K-Mod/Civ4CE/Taurus preserve those older bad bindings, which makes them a useful historical oracle
but not a current F681 root. The album's KI#122 remains their owner.

REFERENCE / OWNERSHIP REVIEW
----------------------------
The current file has only the inherited `reference_existing_object` cases for Color, diplomacy
responses and tutorial messages. Their native objects are members or allocated children of the
corresponding global info records; the bindings do not manufacture references to stack temporaries
or short-lived wrapper objects. Current diplomacy Python code also bounds response/text accesses
through `getNumResponses()` and `getNumDiplomacyText()` and passes enum/global-info indices from
native domains.

WRITABLE INFO SURFACE
---------------------
The few inherited mutators exposed here (`setTag`, `setPath`, `setNIF`, `CvGameText::setText`) were
checked against the shipped Python tree. No shipped call to the asset tag/path/NIF mutators was
found. The broad `.setText()` traffic belongs to UI screens, popup objects and controls; no current
shipped mutation of a CvGameText info record was identified. Thus this file does not currently
open a live Python route that mutates global info behind native caches.

NEGATIVE / ROBUSTNESS CONTROLS
------------------------------
Some public indexed getters ultimately rely on assertion-checked native preconditions and could be
misused by arbitrary mod Python in a release build. That is an external API-hardening surface, not
a demonstrated current SAS activation: shipped callers use paired counts, valid info indices or
fixed enum domains. Likewise, small doc-string signature mistakes in registration text do not alter
the Boost.Python callable signature and remain documentation defects only.

This closure is consistent with the earlier CyPlayer/CyGame/CyPlot audit standard: promote a Python
wrapper defect only when the shipped bridge violates a live native contract, not merely because a
public modding API can be called incorrectly by third-party code.

CLOSURE JUDGMENT
----------------
Queue 055 contains real live Python exposure, including substantial SAS additions, but the current
registration targets, indexed-call domains, lifetime policies and shipped mutation surface all
reconcile. The known three wrong bindings are already fixed/owned by KI#122, and Base AdvC 1.14
contains no newer Interface3 correction absent from SAS.

QUEUE 055 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Open ordinary Queue 056 `CvGameCoreDLL/CvInfo_Asset.cpp` (768 queued lines) under
F681 / provisional KI#1002 UNUSED. The priority reloop is complete; the old per-row skip annotation
is obsolete.

Exact continuation stone:
C031-WIP558 physical -> Queue055 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue056
CvInfo_Asset.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus provenance -> audit asset/NIF/
KFM/path XML loading, ownership/destruction, copy/read/cache behavior, Python-visible mutators and
null/empty-art contracts -> dedup against prior art/UI/CvInfo findings -> either promote a genuinely
live F681 or close Queue056 cleanly -> freeze next physical WIP.

No gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP559 - ORDINARY QUEUE 056 CLOSURE / QUEUE 059 OPEN
===============================================================================

QUEUE 056
---------
File: CvGameCoreDLL/CvInfo_Asset.cpp
Queued size: 768 lines
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

CURRENT / BASE / ANCESTRY BASELINE
----------------------------------
Current SAS and supplied Base AdvC 1.14 are executable-semantically identical across the entire
file after comment/whitespace normalization (normalized SHA-256
`d2b73530f8d97f80f5674f0085d0392031970871e89cd716a49dd07202661d97`). Thus there is no newer
Base art-info repair missing from the current branch.

K-Mod, Civ4CE and Taurus retain these art-info implementations inside the old monolithic
`CvInfos.cpp`; Civ4CE and Taurus are themselves executable-identical across the reconstructed art
block. The AdvC differences are narrow and history-backed rather than an unexplained semantic fork:
explicit empty XML defaults for optional paths, diagnostic cleanup, singleton access cleanup, the
file/header split, and XML-default template accommodation.

XML-DEFAULT / COPY-CONSTRUCTOR CONTROL
--------------------------------------
`CvArtInfoScalableAsset` has an intentionally nonfunctional copy constructor that emits
`FErrorMsg("No copy-ctor implemented")`. In isolation that looks dangerous because AdvC's generic
`SetGlobalClassInfo` can create records by copying a `_DEFAULTS` record. The associated header is
the key contract: `CvArtInfoScalableAsset::isDefaultsType()` is hardwired to false specifically for
that loader template. Art-info vectors store pointers rather than value-copying these records, and
no current art XML type ends in `_DEFAULTS`. The unimplemented copy constructor is therefore a
compile-time/template accommodation, not a shipped copy path.

TERRAIN BLEND PARSER
--------------------
`BuildSlotList` is old Firaxis/K-Mod code and is not robust against an odd number of tokens: after
reading a slot token it unconditionally converts the following rotation token. This was checked
against the actual inherited stock BtS terrain-art fixture rather than promoted from syntax alone.
All 135 `TextureBlend01`..`TextureBlend15` entries in the supplied BtS/Civ4CE art definition have
an even number of slot/rotation tokens. SAS does not override `CIV4ArtDefines_Terrain.xml`, so the
current shipped path inherits that validated data. The parser remains malformed-mod-XML hardening,
not F681.

The `xmlName` buffer is also bounded for its actual loop: the code rewrites the final two digits of
`TextureBlend00` only for indices 1..15. `getBlendList` has an assert-only 1..15 precondition, but
its runtime consumer is in the closed game EXE rather than a current DLL/Python producer; no shipped
invalid blend mask was demonstrated.

The apparent double `CvArtInfoAsset::read(pXML)` in `CvArtInfoTerrain::read` is inherited and
idempotent for the current loader. The base read remaps/re-reads the same scalar/string fields and
`Path`; it does not append blend slots or allocate secondary state. It is redundant startup work,
not a current state-corruption root.

CURRENT ART-DATA ACTIVATION CHECKS
----------------------------------
The parser-sensitive current art data was checked directly from the reconciled light-source ZIP:
- all 231 current UnitArtInfo records contain both `AudioRunTypeLoop` and `AudioRunTypeEnd` under
  `AudioRunSounds`; optional empty action/selection/patrol audio tags resolve through the existing
  -1 fallback contract;
- all six current FeatureArtInfo records use recognized TileArtType and LightType values;
- every current feature `Connections` token is one of NW/N/NE/E/SE/S/SW/W;
- every current FeatureArtPiece contains at least one nonempty ModelFile;
- every current FeatureVariety contains art pieces; the release fallback that indexes piece zero
  after a failed mask lookup therefore does not encounter an empty vector in the shipped data;
- current art-definition Type values are unique within each supplied ArtDefines file, so the art
  manager's string-key map is not silently overwriting a duplicate current tag at build time.

UNIT / FEATURE STATE REVIEW
---------------------------
Unit scalar members are constructor-initialized before XML population; absent optional action,
selection and patrol sounds are converted to -1, absent trails retain the explicit -1 width
sentinel, and the current XML supplies the mandatory unit scale/land/air/ranged/train fields. The
current caller paths for selection/action sounds already use -1 as the no-unit-art-sound fallback
into civilization audio.

Feature enum parsing leaves the constructor's NONE value if malformed input reaches an unknown
string while emitting an error; current data contains no such string. Rotation-mask arithmetic is
consistent with the eight directional bit positions and masks back to eight bits after 90/180/270
degree shifts. Feature variety/model accesses have inherited assertion-based index contracts, but
no current shipped producer outside the engine was found violating the paired variety/model data.

RETAINED PATH HISTORY
---------------------
The full current-file path list is compact and reconciled:
- practical 1644 split these classes out of CvInfos.cpp;
- 1661 is comment/tag cleanup;
- 1842 replaces an older bounds-assert helper with FAssertBounds;
- 2237 adds the XML-default-required scalable-art copy-ctor stub while disabling defaults through
  the associated header contract;
- 2503 changes unconditional false asserts to FErrorMsg diagnostics;
- 2571 removes a no-op ROTATE_NONE self-assignment after Cppcheck;
- 2998/3392 are merge strata carrying those changes;
- 3183 removes an obsolete include during the enum-map rewrite;
- 4099 adds only an optional commented diagnostic around NIF lookup;
- 4262 temporarily applies a special unit-scale adjustment and 4263 immediately reverts it.
No retained history stratum supplies a surviving current semantic repair or regression beyond the
negative controls above.

CLOSURE JUDGMENT
----------------
Queue 056's sharpest code is old art-loader robustness: malformed terrain blend pairs, invalid
feature enum/direction strings, and assertion-only engine-facing indices. The shipped current data
satisfies those contracts, current SAS matches Base AdvC 1.14, and the later history contains no
missed repair. No current gameplay/UI/loader root meets the album threshold.

QUEUE 056 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Queues 057 CombatOdds.cpp and 058 ArmamentForecast.cpp are already COMPLETE at their earlier
priority checkpoints. Resume the first unfinished ordinary entry: Queue 059
`CvGameCoreDLL/CyGlobalContext.cpp` (687 queued lines) under F681 / provisional KI#1002 UNUSED.

Exact continuation stone:
C031-WIP559 physical -> Queue056 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue057-058 already
COMPLETE -> Queue059 CyGlobalContext.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus +
path-history provenance -> audit singleton/info access, enum/index conversion, null-wrapper and
reference lifetime contracts, Python-facing mutation/helpers and SAS additions -> dedup against
prior Cy*/CvGlobals/CyInfo findings -> either promote a genuinely live F681 or close Queue059 ->
freeze next physical WIP.

No gameplay/XML source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP560 - ORDINARY QUEUE 059 CLOSURE / QUEUE 061 OPEN
===============================================================================

QUEUE 059
---------
File: CvGameCoreDLL/CyGlobalContext.cpp
Queued size: 687 lines (713 physical lines in the reconciled 2026-09-09 source)
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

CURRENT / BASE / ANCESTRY BASELINE
----------------------------------
The reconciled source has grown modestly beyond the original queue snapshot. Current SAS contains
713 physical lines; the queue's historical 687-line count is retained for stable scheduling.

The SAS-vs-Base AdvC 1.14 semantic delta is tightly bounded. Current SAS adds eleven thin, read-only
ModName/source-metadata accessors plus the KI#141 `getAudio3DScriptName` helper. The remaining info,
player/team and enum wrappers are inherited. Base AdvC 1.14, K-Mod, Civ4CE and Taurus were used as
ancestry controls for the inherited sharp edges below; there is no newer Base repair absent from SAS.

SAS MODNAME ACCESSORS
---------------------
`getModDisplayName`, `getModFolderName`, `getModPathInRoot`, version/hash/branch/date and dirty-state
accessors are direct wrappers over the existing persistent `GC.getModName()` resolver. They return
cached scalar/string metadata, introduce no secondary ownership, indexing or mutation path, and do
not duplicate filesystem interpretation in Python. No defect survives this surface.

KI#141 AUDIO REVERSE LOOKUP
---------------------------
`getAudio3DScriptName(int)` lazily parses `Audio3DScripts.xml` once, stores each ScriptID in vector
order and returns the indexed AS3D_* script name to Python. The live Sevopedia media player receives
3D script IDs from `CvCivilizationInfo::getSelectionSoundScriptId` / `getActionSoundScriptId`, which
are created by the engine's `getAudioTagIndex(..., AUDIOTAG_3DSCRIPT)` call during XML loading.

The current audio fixture is structurally clean: every enumerated 3D script entry has a ScriptID,
and no duplicate ScriptID was found. KI#141's own recorded runtime testing is also a useful internal
activation oracle: the reverse mapping played the correct civilization Select/Order script at normal
volume, while using the integer through the wrong audio namespace played a different sound. The
follow-up switched to handle-returning `Play3DSound(szScript, -1, -1, -1)` and verified stoppable,
normal-volume playback. Thus the helper's vector-order premise is not merely theoretical; it has
already been exercised against actual engine IDs in this shipped feature.

The helper returns a stable pointer into its process-lifetime static vector after the one-time load;
there is no later vector mutation/reallocation. Load failure or an out-of-range ID returns the static
empty string and the Python caller deliberately falls back to the old ID-based 3D path. No F681 root
survives here.

BONUS-CLASS INDEX-0 CONTROL
---------------------------
The inherited `getBonusClassInfo` test is genuinely off by one as an API contract:

    return (i > 0 && i < GC.getNumBonusClassInfos() ? ... : NULL);

so valid bonus-class slot 0 is rejected. This is present in Base AdvC 1.14, K-Mod, Civ4CE and Taurus.
The first shipped Python sweep initially looked dormant, but the fuller current-tree search found two
live PerfectMongoose callers. That activation was therefore traced through the actual current XML
rather than dismissed from reachability alone.

Slot 0 is `BONUSCLASS_GENERAL`, and its current `iUnique` is exactly 0. PerfectMongoose uses the
returned class only to obtain that unique range for same-class spacing / area-suitability weighting;
when the wrapper returns None it skips or returns zero from that contribution. A correct slot-0
object would also supply range 0, producing the same current spacing and weighted-count result. Thus
this is a real public API off-by-one but not a consequential shipped map-generation defect with the
present data. It remains a useful negative control, not F681.

TEAM LOWER-BOUND CONTROL
------------------------
The inherited `getCyTeam(int i)` returns `&cyTeams[i]` whenever `i < MAX_TEAMS` and does not check
`i >= 0`; a direct `gc.getTeam(-1)` could therefore index before the static wrapper array. The same
shape survives in Base AdvC 1.14, K-Mod, Civ4CE and Taurus.

The shipped Python call tree was checked specifically for NO_TEAM/-1 producers and variable team
IDs. Current live calls derive teams from valid players/active-game state or bounded loops over team
slots; no shipped route feeding NO_TEAM into `gc.getTeam` was established. Main-menu / no-active-team
states do not execute the relevant in-game consumers. This remains third-party Python API hardening,
not a current SAS root.

OTHER INDEX / SENTINEL WRAPPERS
-------------------------------
The remaining ordinary global-info getters overwhelmingly use explicit `i >= 0 && i < count/domain`
guards. A few old direct wrappers (`getCivicOptionInfo`, `getCivicInfo`, `getDiplomacyInfo`, player/
graphic option accessors) rely on the native caller contract instead, but current shipped Python
producers use paired counts, fixed enum loops or validated info types. No current invalid sentinel or
out-of-range producer was found for them.

`getCyPlayer` has both lower and upper validation and `getCyActivePlayer` explicitly handles
NO_PLAYER. `getInfoTypeForString` / `getTypesEnum` retain the AdvC logging/sentinel behavior already
audited through their native/global consumers; no independent queue-local defect was established.

DEDUP / CLOSURE JUDGMENT
------------------------
The file's strongest anomalies are inherited public-wrapper hardening issues rather than current
shipped failures. The one off-by-one with a live PerfectMongoose caller is semantically neutral under
the current slot-0 `iUnique=0` data, the negative-team array hazard has no shipped negative producer,
and the SAS additions are thin metadata wrappers plus the empirically exercised KI#141 audio helper.
No prior Cy*/CvGlobals/CyInfo finding is reopened by this pass and no independent root reaches the
album threshold.

QUEUE 059 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Queue 060 `CvArea.cpp` is already COMPLETE at its earlier priority checkpoint. Resume ordinary
Queue 061 `CvGameCoreDLL/CvEventReporter.cpp` (679 queued lines) under F681 / provisional KI#1002
UNUSED. The old per-row priority-skip annotation is obsolete because the priority reloop is complete.

Exact continuation stone:
C031-WIP560 physical -> Queue059 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue060 already
COMPLETE -> Queue061 CvEventReporter.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus + retained
history -> audit event hook argument order/types, Python callback naming/dispatch, lifetime/reference
crossings, gameplay-event vs logging/UI-only reachability, multiplayer/replay/turn-state surfaces and
SAS additions -> dedup against prior CvDllPythonEvents/CvEventManager/CvGame/Cy* findings -> either
promote a genuinely live F681 or close Queue061 cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP561 - ORDINARY QUEUE 061 CLOSURE / QUEUE 063 OPEN
===============================================================================

QUEUE 061
---------
File: CvGameCoreDLL/CvEventReporter.cpp
Queued size: 679 lines (703 physical lines in the reconciled 2026-09-09 source)
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

CURRENT / BASE DELTA
--------------------
Current SAS has grown from Base AdvC 1.14's 571-line reporter to 703 physical lines, but the
executable delta is concentrated rather than architectural. The additions are SASGameRecord/BBAI
diagnostic hooks plus three deliberately widened native reporter contracts:
- `combatResult` receives the actual battle plot for SASGameRecord while Python still receives the
  inherited winner/loser pair;
- `unitCaptured` forwards the already-created captured unit to the SAS Python battle-history event;
- `techAcquired` receives `TechAcquisitionCause` for GameRecord provenance while the Python event
  deliberately retains its original four arguments.
The rest of the reporter remains the inherited thin bridge into `CvDllPythonEvents` and
`CvStatistics`.

PYTHON ABI / OBJECT-LIFETIME CLOSURE
------------------------------------
The reporter's forwarding names, argument order and arity were reconciled against the current
`CvDllPythonEvents` declarations and live Python consumers. The widened C++-only provenance above
does not leak into established Python event signatures.

The strongest lifetime-looking edge was `combatLogHit`: native `CombatDetails` values can be stack
locals in combat resolution, and BUG Autolog retains the received Python values until a later
mission event. Whole-album dedup found this exact hypothesis already resolved in the completed
CvDllPythonEvents audit. `makePythonObject(T*)` reaches Boost.Python's raw-pointer deep-conversion
path; `CombatDetails` is registered as an ordinary copyable value class, so Python owns a copied
record rather than borrowing the native stack address. Current Queue-061 call ordering has not
changed that conversion contract. No dangling wrapper survives.

The other current object crossings agree with their native timing controls already established in
that audit: lethal combat winner/loser objects still exist during the synchronous callback;
`unitCaptured` fires only after successful replacement-unit initialization; city acquire/raze
callbacks run while the passed city is still live; nullable goody/nuke/Great-Person fields are
handled by the established consumer contracts.

SAS DIAGNOSTIC / GAMERECORD HOOKS
---------------------------------
Every current reporter-local SAS hook was checked at its call boundary rather than assuming logging
is harmless:
- `gameStart` initializes new-game source/version history before the Python GameStart event and
  therefore before START Fast Save; this is the already-established CvGame lifecycle control.
- `endGameTurn` intentionally flushes buffered plot/revelation changes after Python's turn-end event
  so Python changes are included. Its periodic modulo cannot divide by zero because
  `getSASGameRecordTurnInterval()` clamps the XML setting to at least 1.
- combat-result logging receives the explicit attacked plot fixed by KI#377; both attacker-loss and
  defender-loss native producers pass the same battle target.
- Worker-death diagnostics run only behind Worker log level 2+, only for worker/workboat losers,
  and are invoked while winner/loser, plot, group and city context remain live. The helper guards
  nullable group/mission/city state; ordinary combat plots have valid terrain/info enums.
- city build/acquire/culture, capture/promotion/gift, religion/corporation, Golden Age, state
  religion, gold trade, victory and vassal logging consume already-realized state at reporter
  boundaries and do not mutate gameplay state.
- `techAcquired` logging tolerates the event's PlayerTypes provenance directly and does not
  dereference the player; the inherited Python event still receives its original `bAnnounce`
  packet.
- war-completion GameRecord work intentionally lives in `CvTeam::makePeace`, because this later
  reporter boundary occurs after final war-success state has been erased.
- project-production GameRecord work intentionally lives at `CvCity::popOrder` to preserve exact
  overflow state; this reporter remains the Python-only project event.

STATISTICS / CROSS-FILE DEDUP
-----------------------------
The reporter's `CvStatistics` calls were reconciled against the completed Queue-072 audit rather
than re-numbered here:
- F647/KI#969 owns stale statistics when a dead PlayerTypes slot is recycled;
- F648/KI#970 owns the missing alive test in per-human victory statistics;
- F649/KI#971 owns Rise & Fall all-fog suppressing the entire `cityBuilt` reporter and therefore
  the statistics/Python event;
- the apparent `unitKilled` NO_PLAYER indexing hazard is neutralized by its native caller guard;
- ordinary city raze, religion founding, Golden-Age transition and enum-array producers satisfy the
  statistics contracts.
No independent reporter-layer root remains after those owners are excluded.

SAVE / MULTIPLAYER / HISTORY CONTROLS
-------------------------------------
The inherited save-message flags and statistics serialization were checked with their retained
history. The pre-save assertion includes the later PBEM/network exception; autosave/quicksave flag
assertions are intentionally disabled where the EXE cannot expose sufficient scheduling state.
`readStatistics` resets statistics before reading and then calls `onAllGameDataRead`; write uses the
paired reproducibility markers. These lifecycle paths are already covered by the CvGame and
CvStatistics audits and introduce no reporter-specific state mismatch.

The meaningful retained history is correspondingly small:
- practical 1407 adds the post-statistics-read all-data-loaded bridge;
- 1436 introduces replacement save messages, later corrected for PBEM and the autosave text key;
- 1452/1449 are multiplayer/OOS synchronization fixes retained in current behavior;
- 1648 splits DLL-to-Python event reporting into `CvDllPythonEvents`;
- 2033 adds statistics reproducibility-write instrumentation;
- 2843 moves `combatLogHit` into the reporter without changing the generic packet;
- 3655 adds the PBEM pre-save assertion exception;
- 3830 corrects the autosaving text tag;
- SAS 5765 adds the coherent unitCaptured event;
- SAS 6011-6399 add/refine diagnostic/GameRecord hooks, with KI#377-KI#382 repairs and subsequent
  authoritative-boundary refinements preserved in the reconciled source.
No later Base-1.14 repair is missing from SAS and no retained history stratum exposes a new root.

CLOSURE JUDGMENT
----------------
Queue 061's sharpest-looking contracts either belong to previously confirmed cross-file findings or
close under the current Boost.Python/caller/logging lifecycle. The SAS additions preserve Python ABI,
operate on live authoritative objects/state, and respect their own logger bounds. No distinct
current-live gameplay/UI/save/log-corruption root reaches the album threshold.

QUEUE 061 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Queue 062 `MilitaryAnalyst.cpp` is already COMPLETE at its earlier priority checkpoint. Resume the
first unfinished ordinary entry: Queue 063 `CvGameCoreDLL/CyInfoInterface1.cpp` (643 lines) under
F681 / provisional KI#1002 UNUSED. The old per-row "skip until priority WIPs" annotations are
obsolete now that the priority reloop is complete.

Exact continuation stone:
C031-WIP561 physical -> Queue061 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue062 already
COMPLETE -> Queue063 CyInfoInterface1.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus + path
history -> audit every Boost.Python binding for wrong native target, duplicate Python name,
argument/default mismatch, reference_existing_object ownership, enum/index/sentinel contracts and
SAS-added Sevopedia/UI getters -> trace live Python consumers for any suspicious wrapper -> dedup
against the completed CyInfoInterface3/CyGlobalContext/CvDllPythonEvents and prior KI#122 binding
repairs -> either promote a genuinely consequential F681 or close Queue063 cleanly -> freeze next
physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP562 - ORDINARY QUEUE 063 CLOSURE / QUEUE 065 OPEN
===============================================================================

QUEUE 063
---------
File: CvGameCoreDLL/CyInfoInterface1.cpp
Queued/current size: 643 lines
Status: COMPLETE - NO NEW FINDING
Finding cursor: F681 / provisional KI#1002 remains UNUSED

CURRENT / BASE-1.14 DELTA
-------------------------
The current reconciled SAS file is 643 physical lines versus supplied Base AdvC 1.14's 638. After
removing comments/whitespace, the entire executable delta is exactly two added read-only bindings:

- `CvUnitInfo.getObsoleteTech -> CvUnitInfo::getObsoleteTech`, added for the SAS Sevopedia
  unit/technology obsolete-tech display;
- `CvBuildingInfo.getSpecialistExtraCommerce ->
  CvBuildingInfo::py_getSpecialistExtraCommerce`, added with the KI#303 Specialist Extra Yields
  repair.

Every inherited Base binding otherwise retains the same public Python name and native target. Thus
there is no newer Base-1.14 Interface1 repair missing from the reconciled SAS source.

SAS-ADDED BINDINGS - LIVE CALLER CHECKS
---------------------------------------
Both additions are live but satisfy their native contracts:

- Unit `getObsoleteTech()` is a scalar getter. Its native implementation returns the configured
  obsolete tech only when `SAS_CV_UNIT_INFO_ENABLE_XML_UNIT_OBSOLETE_TECH` is enabled and otherwise
  returns `NO_TECH`. Current Sevopedia callers treat that returned enum/sentinel as data and do not
  pass an index into the getter.
- Building `getSpecialistExtraCommerce(k)` is the Python adapter generated by
  `DEF_SHORT_INFO_ENUM_MAP`. Current UI/Sevopedia callers iterate `k` over
  `CommerceTypes.NUM_COMMERCE_TYPES`, exactly the native CommerceTypes domain. The paired KI#303
  display therefore does not create an invalid-index route.

BINDING-TARGET / DUPLICATE CENSUS
---------------------------------
The complete `.def` table contains 522 registrations. A mechanical per-class target/name census
found no wrong-class target. The only duplicate public method name within one registered class is
`CvActionInfo.getAutomateType`, which is registered twice to the exact same
`CvActionInfo::getAutomateType` target and the exact same zero-argument signature. K-Mod/Civ4CE have
one registration; Base AdvC and current SAS retain the duplicate. Since either overload candidate
executes the identical getter with identical arguments/result, this is redundant registration, not
a semantic binding defect.

Several public/native name differences were challenged explicitly:

- `CvUnitClassInfo.getDefaultUnitIndex -> CvUnitClassInfo::getDefaultUnit` preserves the legacy
  Python name after AdvC deliberately renamed the native getter (the current header documents the
  rename).
- `CvUnitInfo.isCanMoveImpassable -> CvUnitInfo::canMoveImpassable` likewise preserves the legacy
  Python spelling after the native AdvC rename; current Sevopedia callers use that compatibility
  name successfully.
- `CvBuildingInfo.getCivic -> CvBuildingInfo::getCivicOption` is inherited unchanged through
  Civ4CE, K-Mod and Base AdvC. No shipped Python call on a CvBuildingInfo object was found for this
  method; the textual `.getCivic()` hits belong to VoteSource/EventTrigger info objects. It therefore
  supplies no current-live F681 activation.
- `pyGet*` and `py_*` targets are the expected string/enum adapters rather than mismatched fields.

ADVC ENUM-WRAPPER ANCESTRY
--------------------------
A current-vs-K-Mod/Civ4CE target comparison reports 43 common bindings whose native target name
changed. The pattern is coherent rather than suspicious: 40 are CvInfo enum-indexed getters moved
from direct methods to `py_` adapters, and the remaining three are the documented native renames
above plus the corresponding prereq adapters. AdvC practicals 1922/3052 introduced this scheme.
`CyInfoWrapper.h` states the contract directly: enum-typed native getters should not be exposed
straight to Python, so the adapter receives Python `int` and casts to the corresponding enum type.
`CvInfo_EnumMap.h` generates the same wrappers for enum-map accessors. The current binding targets
match those generated adapters.

INDEX / SENTINEL / SHIPPED-CALLER SWEEP
---------------------------------------
117 Interface1 registrations advertise one or more integer parameters; 73 method names have shipped
Python call sites. The live low-frequency/high-value cases were inspected rather than treating the
public API as automatically safe. Representative controls include:

- `getUnitNames(i)` is bounded by `getNumUnitNames()`;
- `getSeeInvisibleType(i)` is bounded by `getNumSeeInvisibleTypes()`;
- promotion feature/unit-combat accessors use `getNumFeatureInfos()` / `getNumUnitCombatInfos()`;
- building/civic/unit yield and commerce accessors iterate exact YieldTypes/CommerceTypes domains;
- unit `getNotUnitAIType(i)` iterates `NUM_UNITAI_TYPES`;
- unit speed-bonus slots use `GC.getNUM_UNIT_PREREQ_OR_BONUSES()`;
- specialist/building two-dimensional maps use a valid outer info ID plus an exact yield/commerce
  enum loop.

A literal-negative-index scan found no shipped Python call that passes `-1` directly to an
Interface1 indexed getter. As with the completed Interface3/CyGlobalContext audits, arbitrary
third-party Python can still violate assertion-backed native preconditions in a release build; that
is API hardening, not a demonstrated current SAS defect.

REFERENCE / MUTATION / DOC-STRING CONTROLS
------------------------------------------
The only `reference_existing_object` registrations in this file are Unit/Building `getArtInfo`.
They return global ArtFileMgr-owned info records rather than stack/transient objects, and no shipped
Python `.getArtInfo(...)` call was found in the current tree. Their lifetime therefore supplies no
current dangling-reference route.

The inherited writable `CvUnitInfo.setCombat` and `setInvisible` bindings also have no shipped
Python callers. They remain external modding API surface rather than current global-info mutation
behind native caches.

Some registration documentation strings are stale -- notably the Unit art-info text no longer
matches the full current native signature -- but Boost.Python binds the actual member-function
pointer, not the human-readable doc string. With no shipped caller on that route, this is at most a
documentation defect.

DEDUP / CLOSURE JUDGMENT
------------------------
The known historical wrong-info bindings fixed under KI#122 are in CyInfoInterface3 and remain
separately owned there. The prior CyPlayer/CyGame/CyPlot/CyGlobalContext audits likewise establish
the threshold used here: a wrapper earns a finding only when a shipped producer violates a live
native contract, not merely because malformed third-party Python could call it incorrectly.

Queue 063's complete current-vs-Base delta is safe, its AdvC ancestry differences are deliberate
Python enum adapters/native-name compatibility, the duplicate Action binding is semantically
idempotent, live indexed consumers respect their paired domains, and no transient-reference or
current info-mutation route survives. No independent current-live root reaches the album threshold.

QUEUE 063 IS CLOSED WITHOUT A FINDING. F681 REMAINS UNUSED.

NEXT CURSOR
-----------
Queue 064 `MilitaryBranch.cpp` is already COMPLETE at its earlier priority checkpoint. Resume the
first unfinished ordinary entry: Queue 065 `CvGameCoreDLL/CvStructs.cpp` (603 queued/current lines)
under F681 / provisional KI#1002 UNUSED. The old per-row priority-skip annotation is obsolete now
that the priority reloop is complete.

Exact continuation stone:
C031-WIP562 physical -> Queue063 CLOSED / no finding -> F681/KI#1002 UNUSED -> Queue064 already
COMPLETE -> Queue065 CvStructs.cpp OPEN -> establish current/Base/K-Mod/Civ4CE/Taurus + retained
history -> audit constructor/reset initialization, enum/sentinel defaults, ownership/copy/assignment,
serialization/replay/network crossing structures, array/index sizes and SAS-added fields -> trace
live producers/consumers for any suspicious struct state -> dedup against prior save/message/replay/
CvInitCore/CvMessageData findings -> either promote a genuinely consequential F681 or close Queue065
cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP563 - QUEUE 065 F681 CONFIRMED / QUEUE 065 CONTINUES
===============================================================================

QUEUE 065
---------
File: CvGameCoreDLL/CvStructs.cpp
Queued/current size: 603 lines
Status: OPEN - FIRST ROOT CONFIRMED
Confirmed here: F681 / provisional KI#1002
Next cursor: F682 / provisional KI#1003 UNUSED

F681 / provisional KI#1002 -- `FirstContactData` PARAMETER CONSTRUCTOR LEAVES
ABSENT-PLOT COORDINATES UNINITIALIZED, AND LIVE CONTACT PROCESSING READS/LOGS THEM
--------------------------------------------------------------------------------

ROOT
----
`FirstContactData` has a safe no-argument constructor in `CvStructs.h`:

    FirstContactData() : u1(), u2(), x1(-1), x2(-1), y1(-1), y2(-1) {}

but its parameterized constructor in `CvStructs.cpp` has no initializer list. It only assigns a
coordinate pair when the corresponding plot pointer is non-null:

    if (pAt1 != NULL) { x1 = ...; y1 = ...; }
    if (pAt2 != NULL) { x2 = ...; y2 = ...; }

The IDInfo members still receive their own default construction, but the four built-in `int`
members do not. Therefore a legitimate one-plot construction leaves the absent plot's coordinate
pair indeterminate. Reading those integers is undefined behavior; in the actual 32-bit MSVC runtime
they manifest as whatever values remain in the object/stack storage.

NATIVE SHIPPED ACTIVATION
-------------------------
This is not malformed external input. Ordinary current DLL paths intentionally use the
parameterized constructor with only one plot, for example:

- `CvGame::doVoteSelection`: `FirstContactData fcData(pSrcCity->plot());`
- `CvPlot::setOwner`: `FirstContactData fcData(this);`
- `CvUnit::setXY`: `FirstContactData fcData(pNewPlot, NULL, this);`
- `CvPlot::changeVisibilityCount` can also pass a null second plot when its optional unit is null.

The pointer defaults are part of the declared constructor contract, so a null `pAt2` is expected
state, not an exceptional caller violation.

`CvTeam::meet` passes the record into both directional `makeHasMet` calls. Current
`CvTeam::makeHasMet` then reads both pairs unconditionally whenever `pData != NULL`:

    pAt1 = GC.getMap().plot(kData.x1, kData.y1);
    pAt2 = GC.getMap().plot(kData.x2, kData.y2);

Thus a one-plot contact necessarily reads the uninitialized `x2/y2` pair. `CvMap::plot` range-wraps
or rejects the resulting integers; most wild values become NULL, but accidentally in-range values
can be interpreted as an unrelated real plot. That fake `pAt2` participates in choosing the met
player/contact plot and in the first-contact/espionage reminder message location. The UI consequence
therefore depends on the stale integer values, but the invalid read itself is unconditional on the
one-plot path.

EMPIRICAL CURRENT-RUNTIME ORACLE
--------------------------------
The reconciled light source includes the current SASGameRecord example generated by an actual run,
and its `TEAM_MET` rows expose the defect directly. `CvTeam::meet` logs the raw FirstContactData
coordinate pairs at the default SAS game-record level 3. Across 119 recorded TEAM_MET actions, nine
rows mark a contact-data pair invalid. The malformed second pairs include repeated values such as:

    meetDataPlot1=61,26 ... meetDataPlot2=0,1114128 meetDataPlot2Valid=0
    meetDataPlot1=33,10 ... meetDataPlot2=1701972,100295445 meetDataPlot2Valid=0

The same garbage patterns recur in later contacts (`0,1114128` and
`1701972,100295445`), while the first plot remains a normal map coordinate. This is exactly the
signature expected from one-plot FirstContactData construction: `x1/y1` are assigned, `x2/y2` are
not. The source therefore has both a static proof and an observed current-runtime manifestation;
this is not a merely theoretical uninitialized-member warning.

The recorder's `meetDataPlot2Valid` field guards its *interpretation* of the coordinate but cannot
repair the already-read indeterminate integers. It also means current default diagnostics can
contain meaningless first-contact source coordinates even when no accidental in-range plot affects
the player-facing message.

LINEAGE / REGRESSION BOUNDARY
-----------------------------
This is an AdvC regression rather than Firaxis/K-Mod ancestry.

Practical 1407 / `bf48af8b35...` introduced advc.071 FirstContactData with the safe default
constructor above. Its call sites followed this pattern:

    FirstContactData fcData;
    setFirstContactData(fcData, pPlot, ...);

so every absent plot retained the initialized `-1,-1` sentinel.

Practical 1499 / `3e233e6794...` ("Last batch of style changes; some bugfixes and UI tweaks") moved
`setFirstContactData` into a new convenience constructor and simultaneously converted live callers
to `FirstContactData(pPlot, ...)`. The new constructor copied the conditional assignment body but
omitted the old default initialization. That single refactor is the regression point.

Later AdvC refactoring (including practical 3823's broader contact-location processing) retained the
constructor unchanged. Supplied Base AdvC 1.14 is executable-semantically identical to current SAS
for `CvStructs.cpp` and still contains the same omission. K-Mod and Civ4CE do not yet contain
FirstContactData at all, providing the pre-AdvC ancestry control.

DEDUP
-----
Whole-album and current Known-Issues/source-analysis searches found no existing owner for
FirstContactData's uninitialized coordinate pair or the malformed `TEAM_MET` fields. This is
independent of earlier first-contact/meet fixes such as team-contact symmetry/Tech Share and the
various event/binding findings: those operate after or around contact state, while F681 is the data
record's missing initialization at construction.

REPAIR BOUNDARY
---------------
Keep the repair local to the parameterized `FirstContactData` constructor. Before conditionally
copying supplied plot/unit data, initialize absent-state members to the same sentinels as the safe
default constructor. In this VC2003-era codebase that means an ordinary initializer list rather
than C++11 delegating construction, e.g. initialize `u1`, `u2` and all four coordinates (`-1`) and
then overwrite only the supplied plot pairs. This restores the original practical-1407 contract
without changing contact selection logic or callers.

F681 / provisional KI#1002 IS CONFIRMED.
F682 / provisional KI#1003 is now the unused Queue-065 cursor.

PHYSICAL SAFETY / CONTINUATION
------------------------------
Freeze this root before continuing the rest of CvStructs. Queue 065 remains OPEN. Continue under
F682 with the remaining struct families: serialized Event/Vote data, PB setup arrays/vectors,
battle/air mission constructors and copy semantics, `setBattleRound`, EXE-facing ABI/layout structs,
IDInfo legacy-owner normalization, and FirstContact/Deal data after excluding F681.

Exact continuation stone:
C031-WIP563 physical -> Queue065 OPEN -> F681/KI#1002 CONFIRMED from uninitialized one-plot
FirstContactData coordinates with current runtime TEAM_MET oracle -> F682/KI#1003 UNUSED -> finish
CvStructs whole-body/history/ABI/serialization audit -> dedup -> either promote a distinct F682 or
close Queue065 cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP564 - QUEUE 065 CvStructs.cpp CLOSED / F681 RETAINED / F682 UNUSED / QUEUE 066 OPEN
===============================================================================
SOURCE IDENTITY
---------------
Continued from the physically frozen C031-WIP563 checkpoint against the reconciled
`AdvCiv-SAS_light_source_20260909T154245.zip` source base. This continuation changed only this audit
album copy. Gameplay/XML/Python source remains untouched.

QUEUE 065 FINAL CLOSURE
-----------------------
Queue 065 `CvGameCoreDLL/CvStructs.cpp` is now COMPLETE.

The one consequential live root in the file remains the already-frozen F681 / provisional KI#1002:
the parameterized `FirstContactData` constructor fails to initialize the absent plot's coordinates,
and ordinary one-plot contact construction therefore feeds indeterminate coordinates into
`CvTeam::makeHasMet`. WIP563 contains the full source proof, practical-1499 regression lineage,
whole-album dedup, repair boundary and the supplied current SASGameRecord runtime oracle.

The remaining whole-body/history/ABI/serialization pass did NOT establish a distinct F682. F682 /
provisional KI#1003 remains UNUSED for Queue 066.

FINAL NEGATIVE / DEDUP CONTROLS
-------------------------------
1. `CvBattleDefinition::setBattleRound` is genuinely incorrect but dead in current SAS

The retained function says:

    void CvBattleDefinition::setBattleRound(int index, const CvBattleRound& round)
    {
        m_aBattleRounds.assign(index, round);
    }

`std::vector::assign(index, round)` replaces the entire vector with `index` copies; it does not set
one indexed element. The function is therefore objectively wrong as a setter. However, current SAS
has no caller. K-Mod's rewritten `CvUnit::planBattle` clears the round vector and uses
`addBattleRound` for every generated round; current source references to `setBattleRound` are only
its declaration and definition. Civ4CE/Taurus retain the older Firaxis-style live caller, which is a
useful ancestry/control result, but that path does not survive into current SAS. Classify this as a
real inherited dead-path defect, not a current gameplay F682.

2. Battle/air mission construction and copy semantics do not expose another live root

`CvBattleRound` initializes all per-side killed/alive arrays plus wave/ranged state, and current
`planBattle` populates each round before `addBattleRound`. The current battle plan has a small bounded
round count, so the vector-size to `int` getter conversion is not a practical overflow surface.

`CvBattleDefinition`'s copy constructor does not copy base `m_pPlot`, but the current ordinary
battle-definition construction path does not set a battle plot through that base member; its live
engine mission setup uses the explicitly populated units/damage/timing/round state. No current
consumer was found that depends on a copied non-null battle `m_pPlot`. `CvAirMissionDefinition`,
where the plot is part of the live mission state, explicitly copies it. No consequence established.

3. `OrderData` / `MissionData` live creation is fully initialized before list insertion

`CvCity::pushOrder` fills all semantic `OrderData` members (`eOrderType`, `iData1`, `iData2`,
`bSave`) after validation and before any `m_orderQueue.insert...` operation. The out-of-range
`getOrderData` fallback also explicitly fills all four fields with sentinel/default values.

`CvSelectionGroup::pushMission` fills all current `MissionData` fields before insertion:
`eMissionType`, `iData1`, `iData2`, `eFlags`, `iPushTurn`, and advc.011b `bModified`. Copies returned
to Python are made from already-populated queue records. No uninitialized live queue datum survives.

4. Current mission-list serialization is symmetric under the album's established save policy

Current `CvSelectionGroup::write` writes the current-format mission list and current `read` consumes
the same sequence. The surviving `MissionDataLegacy` declaration is now unused because the project
has deliberately removed historical uiFlag migration branches; that policy was already audited in
the completed `CvSelectionGroup.cpp` pass as same-version-current-format authoritative rather than
cross-version save migration. Queue 065 adds no independent mismatch.

5. Event/vote data serializers remain field-order symmetric

The retained `EventTriggeredData`, `VoteSelectionSubData`, `VoteSelectionData` and
`VoteTriggeredData` read/write paths cover their current members in matching order. AdvC practical
4023 supplied explicit initialization for the vote-selection/trigger data families. Ordinary
`EventTriggeredData` creation through `CvPlayer::initTriggeredData` fills the IDs/enums/plot/unit/
building fields before consumers use the record, while save loading supplies them through read.
No second uninitialized-record root comparable to F681 was established.

6. `CvPlotIndicatorData`'s mode-specific fields are caller-complete

The two live construction families are unit-layer and resource-layer indicators. Unit-layer records
set `m_pUnit`, label/icon/color/help and visibility behavior; their target point is irrelevant because
the unit identifies the target. Resource-layer records leave the constructor-default unit pointer
null and explicitly set the target point, label/icon/color/help and visibility. The EXE-facing copy
constructor requirement noted by AdvC does not convert the irrelevant per-mode field into a live
uninitialized read. No current consequence proven.

7. PitBoss/FOW structs remain engine/API surfaces without a demonstrated shipped default-state use

`PBGameSetupData` only initializes its option vectors in its DLL-side constructor, and the PB player
records/FOW record are plain engine-facing data structures. Their primitive/default pointer fields
look sharp if arbitrarily default-constructed from extension code, but no current shipped DLL/Python
producer was found that constructs one and consumes those uninitialized values as gameplay state.
The Python bindings expose engine-supplied records/accessors; generic malformed-index/default-object
hardening does not meet the album threshold.

8. `IDInfo`, layout-sensitive trade data and remaining POD structures stay within their contracts

`IDInfo::validateOwner` retains the legacy owner normalization expected by old serialized object IDs;
no legitimate current state was found where a valid object ID plus `NO_PLAYER` should be resolved
through the unchecked release-only branch. `TradeData` retains its explicit constructor and
`BOOST_STATIC_ASSERT(sizeof(TradeData) == 12)` because CLinkList/external ABI layout must remain
stable. `GameTurnInfo` is populated by the game-speed XML reader before use. No independent root.

HISTORY / BASE CONTROL
----------------------
Current SAS and supplied Base AdvC 1.14 remain executable-semantically identical across
`CvStructs.cpp`; F681 is therefore inherited from Base rather than a SAS-only regression. The
remaining retained semantic history (battle/mission construction, IDInfo legacy normalization,
Vote/Event initialization, ABI padding/static asserts, and mission-time defaults) reconciles with
current callers. No later Base 1.14 repair is missing from SAS in this file.

QUEUE 065 VERDICT
-----------------
CONFIRMED: F681 / provisional KI#1002 (see WIP563 for full proof).
NO INDEPENDENT F682.
Queue 065 is COMPLETE.

QUEUE 066 OPEN
--------------
Open ordinary Queue 066 `CvGameCoreDLL/CvInfo_Command.cpp` (580 queued lines) under F682 /
provisional KI#1003 UNUSED. Start with current-vs-Base/K-Mod/Civ4CE provenance, XML read/default
semantics and command/mission/control/action graph consistency; then trace any raw enum/index arrays,
Python/UI consumers and executable-facing contracts before promoting anything.

Exact continuation stone:
C031-WIP564 physical -> Queue065 CLOSED -> F681/KI#1002 confirmed and retained -> dead inherited
setBattleRound recorded below threshold -> F682/KI#1003 UNUSED -> Queue066 CvInfo_Command.cpp OPEN ->
provenance/XML/default/action graph/index/caller audit -> dedup -> promote only a consequential live
root or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP565 - QUEUE 066 CvInfo_Command.cpp CLOSED CLEAN / F682 UNUSED / QUEUE 067 OPEN
===============================================================================
SOURCE IDENTITY
---------------
Continued from C031-WIP564 against the reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`
source base, with supplied Base AdvC 1.14 plus K-Mod/Civ4CE/Taurus ancestry used as local oracles.
Only this audit-album copy was changed.

QUEUE 066 BASELINE
------------------
Current SAS `CvInfo_Command.cpp` is 580 physical lines, exactly matching the queued count. After
normalizing CRLF/LF line endings, the entire current file is byte-for-byte identical to supplied
Base AdvC 1.14. The matching header is likewise semantically aligned. Therefore a Queue-066 root
would have to be inherited behavior still present in Base 1.14, not a SAS-only change or missed
later-Base repair.

ACTION SUBTYPE / SUB-ID GRAPH
-----------------------------
`CvXMLLoadUtility::SetGlobalActionInfo` is the authoritative producer. It constructs `ActionData`
from bounded enum loops for InterfaceMode, Mission, Build, Religion, Corporation, Specialist,
Building, Control, Automate, Command, Promotion and Unit. Each record carries the matching
`ACTIONSUBTYPE_*` plus that same family's enum ID. After stable priority sorting, the pair is copied
unchanged into the newly allocated `CvActionInfo`.

The Queue-066 dispatches then map back to exactly those source families:
- mission data: build/religion/corporation/specialist/building use the subtype's own sub-ID;
- mission type: those families resolve their configured/stamped mission type, while MISSION returns
  its own sub-ID;
- command type: promotion/unit resolve their stamped commands, AUTOMATE resolves its command,
  COMMAND returns its own ID;
- command data: promotion/unit carry the target subtype ID; AUTOMATE carries its automate ID;
  COMMAND reads the command's optional Automate datum (normally NO_AUTOMATE for ordinary commands);
- interface/control/automate getters only expose their corresponding subtype.

Current live callers first test the returned mission/command/interface/control sentinel before
executing the matching action path. Widget help branches that cast mission/command data are reached
through the corresponding action type. No wrong-info-array reinterpretation or cross-family ID use
was found.

The superficially odd `getCommandData` behavior is inherited Civ4 protocol, not a polarity bug.
K-Mod, Civ4CE and Taurus contain the same semantics in their pre-split `CvInfos.cpp`: promotion/unit
actions use the subtype ID as command data; command actions expose their optional automate datum;
automation actions combine `COMMAND_AUTOMATE` with the specific automate ID.

XML / DEFAULT CONTRACTS
-----------------------
The current mod fixtures relevant to this file validate structurally:
- CIV4CommandInfos.xml: 14 Type entries, all unique;
- CIV4MissionInfos.xml: 48 Type entries, all unique;
- CIV4InterfaceModeInfos.xml: 19 Type entries, all unique;
- every current InterfaceMode entry contains the required CursorType and Mission fields; `NONE`
  mission values are the deliberate sentinel form.

Constructors initialize command/automate/mission/interface scalar state to their NO_* or false/zero
baseline. XML readers either overwrite those fields through the enum-aware helpers or preserve the
constructor default when an optional field is absent. In particular practical 2868 deliberately
uses `SetGlobalTypeFromChildXmlVal` for `CvAutomateInfo::Automate` because that info class is not yet
loaded as an ordinary info array at this stage. No load-order regression was found.

HISTORY
-------
Fifteen reachable AdvC history commits touch this split file. The meaningful retained strata are:
- practical 1644: split from CvInfos;
- 1922: enum/signature wrapper cleanup;
- 2222/2236: XML-default work;
- 2287: remove unnecessary setters / use XML helper functions;
- 2868: correct global-type lookup for Automate during load order;
- 3432: base-class typedef refactor;
- 3667: enum-return cleanup;
- 3859: rewrite/rename action `OriginalIndex` to the clearer subtype `SubID` while preserving the
  established action semantics.
The remaining touches are merges/style/refactoring. Supplied Base 1.14 contains no subsequent repair
that SAS lacks.

NEGATIVE CONTROLS
-----------------
- `getHotkeyInfo` returns only info objects belonging to the action's stamped subtype. All action
  records are produced from bounded loaded-info loops; no shipped path constructs a public action
  with an arbitrary invalid sub-ID.
- Repeated forwarding getters (`getDescription`, button, hotkey priorities/modifiers, etc.) simply
  delegate to that resolved persistent info object; there is no transient ownership/lifetime issue.
- `CvMissionInfo` defaults EntityEvent to NO_ENTITYEVENT and only replaces it when XML supplies a
  value; optional waypoint/visibility/time state is constructor-safe.
- `CvInterfaceModeInfo` defaults cursor/mission to NO_* and booleans false, with current XML supplying
  the expected fields.
- `CvActionInfo::getControlType` returns -1 for non-controls, equivalent to the NO_CONTROL sentinel
  used by its callers.
- The nonzero `iMissionOffset` accommodation is retained modular-loading infrastructure; normal
  startup begins with zero existing action infos. No current native fixture/caller demonstrated a
  bad offset state.

QUEUE 066 VERDICT
-----------------
No new consequential live defect established.
F682 / provisional KI#1003 remains UNUSED.
Queue 066 `CvInfo_Command.cpp` is COMPLETE.

QUEUE 067 OPEN
--------------
Open ordinary Queue 067 `CvGameCoreDLL/CvInfo_Civics.cpp` (576 queued lines) under F682 /
provisional KI#1003 UNUSED. Begin with current-vs-Base provenance and XML/default inheritance, then
trace every enum-indexed civic/upkeep/vote/diplomacy field into current consumers and current XML,
with special attention to sentinel defaults, array dimensions, option-category assumptions and any
SAS-specific civic data additions.

Exact continuation stone:
C031-WIP565 physical -> Queue066 CLOSED CLEAN -> F682/KI#1003 UNUSED -> Queue067 CvInfo_Civics.cpp
OPEN -> provenance/XML/defaults/enum-array/current-consumer audit -> whole-album dedup -> either
promote a consequential live F682 or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP566 - QUEUE 067 CvInfo_Civics.cpp CLOSED / F659 DEDUP / F682 UNUSED / QUEUE 069 OPEN
===============================================================================
SOURCE / PROVENANCE
-------------------
Current SAS `CvInfo_Civics.cpp` is exactly 576 physical lines and, after line-ending normalization,
is byte-for-byte identical to supplied Base AdvC 1.14. The header differs only by formatting of two
inline `CvUpkeepInfo` getters. No executable SAS-vs-Base delta exists in this queue.

QUEUE 067 CURRENT-DATA / CONTRACT RESULT
----------------------------------------
Current CIV4CivicInfos.xml contains 25 unique civic Type entries: five civics in each of the five
current civic-option categories. All 25 explicitly supply CivicOptionType, TechPrereq, Upkeep and
AnarchyLength. Current improvement-yield entries reference known current Improvement types, and the
variable-list readers dimension hurry/special-building/specialist/building-class/feature arrays from
the corresponding loaded info counts.

The six fixed Yield/Commerce arrays are always initialized to their full enum dimensions. If the XML
container is absent, `InitList` supplies zeros; if present, `SetYieldArray`/`SetCommerceArray` fills
the same fixed-size domain. `m_ppiImprovementYieldChanges` is likewise unconditionally allocated as
`numImprovements * NUM_YIELD_TYPES` before optional per-improvement overrides are parsed. Therefore
the non-null-checking `getImprovementYieldChanges(i,j)` has a valid backing matrix after either XML
or XML-cache loading on ordinary current startup.

Current native consumers use matching enum domains: BuildingClass loops for building happiness/
health, Feature loops for feature happiness, Hurry/SpecialBuilding/Specialist loops for their bool
arrays, and Improvement x Yield loops for improvement changes. The public assert-only getters remain
ordinary extension-API hardening if passed arbitrary invalid integers; no shipped producer found
crosses those domains.

`CvPlayer::processCivics` was already reconciled field-by-field in the earlier CvPlayer deep audit:
all persistent current CivicInfo modifiers are processed, while eligibility/upkeep/anger and
runtime/city-dependent quantities are read through their authoritative paths. Queue 067 found no
missing scalar-processing sibling.

F659 DEDUP -- DO NOT RENUMBER
----------------------------
`CvCivicInfo::~CvCivicInfo()` contains a real current defect, but it is already explicitly owned by
F659 / KI#981 from Queue 033.

The destructor frees each `m_ppiImprovementYieldChanges[i]` row using
`GC.getNumImprovementInfos()` as the loop bound. AdvC practical 1752 replaced K-Mod's explicit
info-destruction order with generic `DO_FOR_EACH_INFO_TYPE`; current teardown clears ImprovementInfo
before CivicInfo. Consequently the CivicInfo destructor sees zero improvements and leaks its nested
rows. F659 already proved both the Improvement/Route and Civic/Improvement manifestations, current
allocation counts, K-Mod oracle, practical-1752 lineage and repair boundary. Queue 067 is the
anticipated same-root sibling; do not consume F682 for it.

XML CACHE / HISTORY CONTROLS
----------------------------
The XML-cache read/write field order is symmetric for the current layout, including AdvC's
`bCanAlwaysForce`, colony/luxury modifiers and all fixed/dynamic arrays. Historical cache uiFlag
migration is not a new live current-format mismatch. Significant file history is explainable:
practical 2317 made selected AdvC/K-Mod tags optional with zero defaults; practical 2418 added
`bCanAlwaysForce`; practical 3050 repaired enum reads for XML-cache compilation; practical 3220
restored the yield/commerce array helpers; practical 3680 added the Vassalage colony-maintenance
modifier consistently to constructor/read/write/XML. Base 1.14 contains no newer repair absent from
SAS.

Other controls:
- `m_iUpkeep` can legitimately be NO_UPKEEP when XML says NONE; current callers check this before
  indexing UpkeepInfo.
- every current civic has a non-NO CivicOptionType, matching the reader assertion and all option-map
  consumers;
- `WeLoveTheKing` is optional for many civics and its string storage is safe when absent;
- `CvCivicOptionInfo::m_pabTraitNoUpkeep` uses a count-sized optional list and null means all false;
- `CvUpkeepInfo` scalar percentages default to zero and are direct values, not indexes.

QUEUE 067 VERDICT
-----------------
No independent current root.
F659 remains the owner of the CivicInfo teardown leak.
F682 / provisional KI#1003 remains UNUSED.
Queue 067 `CvInfo_Civics.cpp` is COMPLETE.

QUEUE 069 OPEN
--------------
Queue 068 `KmodPathFinderLegacy.cpp` was already complete at C031-WIP485, so ordinary order advances
to Queue 069 `CvGameCoreDLL/AdvCiv4lerts.cpp` (543 queued lines), under F682 / provisional KI#1003
UNUSED. Audit the C++ alert calculations against the Python/BUG alert surface, visibility/intel
contracts, city/player iteration and turn-state timing, with current-vs-Base/K-Mod provenance and
whole-album dedup before numbering anything.

Exact continuation stone:
C031-WIP566 physical -> Queue067 CLOSED -> F659 dedup retained -> F682/KI#1003 UNUSED -> Queue068
already COMPLETE -> Queue069 AdvCiv4lerts.cpp OPEN -> provenance + alert timing/visibility/value/
iterator/current-caller audit -> dedup -> promote only demonstrated live root or close cleanly ->
freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP567 - QUEUE 069 AdvCiv4lerts.cpp CLOSED CLEAN / F682 UNUSED / QUEUE 070 OPEN
===============================================================================
SOURCE / PROVENANCE
-------------------
Current SAS `CvGameCoreDLL/AdvCiv4lerts.cpp` is 543 physical lines, matching the queue snapshot.
Supplied Base AdvC 1.14 has the same live alert algorithms. The current executable delta is limited
to recent SAS performance-only cache hoists: `ALERT_ON_NO_LONGER_WAR_TRADE`, `WAR_TRADE_ALERT`
color and the two local `CITY_BLUE` lookups are cached in function-local `static const` values instead
of being re-fetched on every alert. The corresponding values are stable global configuration/info
state during ordinary gameplay; no semantic SAS-vs-Base regression survives.

LIVE BRIDGE / LIFECYCLE
-----------------------
`CvPlayer::initAlerts` constructs exactly four C++ alert objects in this order:

    0 WarTradeAlert
    1 RevoltAlert
    2 BonusThirdPartiesAlert
    3 CityTradeAlert

Current `Assets/Python/Contrib/Civ4lerts.py` returns exactly IDs 0/1/2/3 for those four classes and
calls `activePlayer.checkAlert(id, silent)` at BeginActivePlayerTurn. Save/load and controller-change
paths rebuild the C++ objects; Python `_reset` performs the intended silent baseline check when the
alert is enabled. `AdvCiv4lert::check` preserves a pre-existing silent state across nested/silent
calls (the practical-2991 repair), and the current Auto Play suppression path retains practical
2123's corrected message-type behavior.

`CvPlayer::checkAlert` itself still has an inherited `iAlertID > size()` rather than `>= size()`
bounds typo. All shipped Python IDs are the exact fixed 0..3 values above, so ID==4 is not produced
by current code. Keep that as extension-API hardening, not F682.

WAR-TRADE ALERT
---------------
The current iterator domains and state transitions reconcile with the intended alert contract:
- `FREE_MAJOR_CIV,OTHER_KNOWN_TO` supplies eligible non-vassal major teams known to the owner;
- contact/war/open-borders and `AI_declareWarTrade` checks gate current bribability;
- when UWAI is disabled, `DENIAL_TOO_MANY_WARS` is separately tracked through `m_tooManyWars`;
- default-off `ALERT_ON_NO_LONGER_WAR_TRADE` suppresses the historically noisy negative transition;
- the hireling-capital coordinates are visibility-aware through the team-capital helpers.

The suspicious-looking `GET_TEAM(m_eOwner)` usage elsewhere in this file is NOT a PlayerTypes/
TeamTypes confusion. `CoreAI.h` deliberately overloads `GET_TEAM(PlayerTypes)` through `TEAMID`.
The album already contains/retracts this exact false-positive family from the UWAI audits; do not
renumber it here.

REVOLT ALERT
------------
The plot-number state sets are rebuilt from the owner's current city list on each check. Current
revolt probability and occupation transitions are evaluated while each city pointer is live, and
messages are emitted before the sets are replaced. The deliberate post-occupation erase causes a
fresh revolt warning on the following turn if revolt risk remains after pacification. No stale-city
pointer or current plot-number collision path survives under the shipped turn lifecycle.

BONUS-THIRD-PARTIES ALERT
-------------------------
The deal census is structurally correct for ordinary state changes:
- each resource item is encoded as `numBonusInfos * recipient + bonus`, which is injective over the
  current player/bonus domains;
- `std::multiset` preserves multiple same-resource exports between the same pair;
- ended trades are suppressed when war, death or recent stop-trading memory makes the cause obvious;
- non-spectator visibility requires the alert owner to have met both parties;
- strategic resources are announced per resource, while non-strategic resources are intentionally
  reduced to first/last resource-trade transitions between the two players via
  `getNumTradeBonusImports(eFrom)`.

LOW-GRADE MULTISET UX EDGE -- BELOW FINDING THRESHOLD
-----------------------------------------------------
`set_difference` on the multisets preserves multiplicity, but the resulting `aiNewDeals` and
`aiMissingDeals` vectors are then iterated element-by-element while `showMessage` recomputes the same
new/old TOTAL count for every duplicate encoded value. Therefore a legal 0->2 or 2->0 transition of
the same bonus between the same third-party pair inside one observer interval can call the same
message twice.

Multiple copies are not impossible: `CvPlayer::canTradeItem(TRADE_RESOURCES)` permits another copy
while the exporter still has a tradeable copy; human-human trades return NO_DENIAL, and AI duplicate
imports can also remain legal where corporation value makes extra copies useful. Thus the data
structure edge is real. However it requires at least two identical deal transitions between two
checks and only duplicates an informational alert; it does not alter deal state, valuation,
visibility or resource counts. The implementation has had this shape since the advc.210d introduction
in practical 1287. Preserve as a low-grade alert-presentation defect / future cleanup candidate, not
as F682 under the current consequential-root threshold.

CITY-TRADE ALERT
----------------
The city-trade state vectors store integer city IDs only; temporary `CvCity const*` vectors are used
solely within one non-mutating check and consumed immediately by the message helpers. At-war cession
checks pair `canTradeItem(..., true)` with `AI_considerHypotheticalOffer` so a merely legally
tradeable city is not announced unless the AI would actually accept the peace bundle. At peace,
liberation is split from ordinary willingness to buy, and practical 2230's previous-owner test still
suppresses the noisy immediate post-conquest liberation case.

The earlier concern that `canTradeItem(..., true)` might test only the wrong side's willingness was
rejected after tracing `CvPlayer::canTradeItem` / `AI_cityTrade`: a human giver is not subjected to a
fictional AI refusal, while the AI recipient's acquisition/acceptance constraints are still applied.
No current semantic inversion survives.

HISTORY / DEDUP CONTROLS
------------------------
Relevant retained history reconciles with the current body:
- practical 1287 introduced `BonusThirdPartiesAlert` and its multiset design;
- practical 1343 reduced war-trade alert flicker; practical 1349 made negative war-trade alerts
  configurable/default-off;
- practical 1491 refactored the base alert class without changing alert state semantics;
- practicals 1718/1719 introduced and adjusted CityTradeAlert;
- practical 2123 fixed post-Auto-Play message typing;
- practical 2230 suppressed liberation-to-previous-owner noise;
- practical 2349 added too-much-on-our-hands transitions;
- practical 2991 repaired nested/silent-state restoration;
- practical 3614 only updated an already-commented peace-counter reference after the exact-counter
  refactor.

Whole-album searches found no prior owner that this queue should reopen. Existing third-party intel,
trade, Python-ID and UWAI PlayerTypes/TeamTypes findings are independent roots, and the rejected
`GET_TEAM(PlayerTypes)` family remains explicitly deduped.

QUEUE 069 VERDICT
-----------------
No independent consequential current root.
F682 / provisional KI#1003 remains UNUSED.
Queue 069 `CvGameCoreDLL/AdvCiv4lerts.cpp` is COMPLETE.

QUEUE 070 OPEN
--------------
Ordinary queue order advances to Queue 070 `CvGameCoreDLL/CvInfo_Base.cpp` (494 queued lines) under
F682 / provisional KI#1003 UNUSED. Begin with current-vs-Base/K-Mod provenance, then audit XML/default
inheritance, base-info string/key/hotkey/button/civilopedia fields, cache/read ordering, text-manager
lifetimes, copy/default semantics and any public index/sentinel surfaces against current consumers and
current shipped XML before whole-album dedup.

Exact continuation stone:
C031-WIP567 physical -> Queue069 CLOSED CLEAN -> F682/KI#1003 UNUSED -> Queue070 CvInfo_Base.cpp OPEN
-> provenance + base-info XML/default/cache/string/lifetime/current-consumer audit -> whole-album
history/dedup -> promote only a demonstrated consequential live root or close cleanly -> freeze next
physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP568 - QUEUE 070 CvInfo_Base.cpp CLOSED CLEAN / F682 UNUSED / QUEUE 071 OPEN
===============================================================================
SOURCE / PROVENANCE
-------------------
Current SAS `CvGameCoreDLL/CvInfo_Base.cpp` is exactly 494 physical lines, matching the queued
snapshot. Supplied Base AdvC 1.14 is 495 lines; after comments/whitespace are normalized the two
files are executable-semantically identical. The only current SAS/Base source difference is the
5961 signature-collapse formatting of `CvHotkeyInfo::setHotKeyDescription`.

The retained path history was reconciled from the 1644 `CvInfos` split through the XML-tag-as-enum
work, XML-default inheritance, sea-level description override, compact `CvXMLInfo` refactors,
checked integer-cast rewrite, True Starts merge, duplicate-element diagnostics and the later SAS
formatting mirror. Base 1.14 supplies no newer repair absent from SAS.

BASE STRING / LOCALIZATION CACHE CONTRACT
-----------------------------------------
`CvInfoBase` stores persistent XML keys/Type/Button data and lazily caches localized descriptions,
text/help/strategy/civilopedia strings. `CvGlobals::infosReset()` is an EXE-facing callback used when
the display language changes and explicitly calls `reset()` for every info vector plus ArtFileMgr;
`reset()` clears all five localized caches. Therefore runtime language changes do not leave stale
localized text in current infos.

`getDescription(uiForm)` grows the cache one grammatical form at a time and obtains each form from
`getDescriptionInternal()` using the current cache size. The sole current override,
`CvSeaLevelInfo::getDescriptionInternal`, deliberately substitutes Custom-Game recommendation text
and otherwise delegates to the base form lookup; it respects that cache protocol.

`getType()` and `getButton()` intentionally return NULL for an empty stored string. The inherited
`isMatchForLink` expression `CvWString(getType())` is NULL-safe in current `CvWString::Copy`, which
explicitly clears the destination for NULL/empty input before attempting `strlen`. The subsequent
form loop uses the persistent text key. No current null-dereference root survives.

Python exposes `getDescriptionForm(uint)`, so an arbitrary extension could request a huge form index
and force large cache growth (and a Python -1 converted to unsigned would be especially hostile).
Mechanical current-tree search found no shipped Python call to `getDescriptionForm`; shipped UI uses
ordinary `getDescription()`. Keep this as public-API hardening only, not F682.

XML BASE READ / DEFAULT INHERITANCE
-----------------------------------
`CvInfoBase::read` restores the ordinary inherited contract: skip to the next value, map children,
then read GraphicalOnly/Type/Description/Civilopedia/Help/Strategy/Button with safe empty/false
defaults. A failed `SkipToNextVal` returns false to normal concrete readers. `CvXMLInfo::read`
does not itself propagate the base return value, which is brittle for malformed/empty records, but
current `SetGlobalClassInfo` invokes it only on actual value nodes; no shipped malformed activation
was found.

The XML-default loader deserves special control because `CvInfoBase(CvInfoBase const&)` intentionally
contains only `FErrorMsg("Copy-ctor not implemented")` and copies no base state. Current data has
exactly one `*_DEFAULTS` record: `LEADER_DEFAULTS`. `CvLeaderHeadInfo` supplies its own dedicated
copy constructor for the derived defaultable state; its base subobject is default-constructed and
then overwritten by the concrete leader's ordinary `CvInfoBase::read`. This is the intended AdvC
pattern already reconciled in the CivilizationInfo/default audits. No other current info class is
silently copied through the non-implemented base copy constructor.

SCALABLE / HOTKEY BASE CONTRACTS
--------------------------------
`CvScalableInfo` initializes both scales to 1.0. Its XML reader requires `fScale` and gives
`fInterfaceScale` a 1.0 default. The art-info producer side was already audited in Queue 056; current
scalable art records satisfy the required scale data. There is no independent Queue-070 art root.

`CvHotkeyInfo` initializes all action/hotkey priorities to -1 and modifier flags false, then reads
optional hotkey data with explicit defaults and order priority 5. A missing alternate hotkey leaves
`m_iHotKeyValAlt` at its constructor sentinel. `setHotKeyDescription` is safe with the intentionally
NULL alternate-description key because `CvWString::operator=(wchar const*)` clears on NULL. The
post-load `SetGlobalActionInfo` path deliberately replaces the alternate description with the
matching mission/command description where required; Queue 066 already reconciled the action
subtype protocol.

The `CvHotkeyInfo::IntElementTypes` declaration still says
`NUM_INT_ELEMENT_TYPES = base_t::NUM_BOOL_ELEMENT_TYPES`, an obvious-looking inherited typo. It is
currently inert: `CvXMLInfo` has zero base integer AND zero base boolean elements, `CvHotkeyInfo`
adds none of either, so both counts are exactly zero before every concrete Hotkey-derived class
begins its own contiguous element enum. It would become dangerous only if common base element counts
diverged in a future extension; current vector indexing is unchanged.

CvXMLInfo COMPACT ELEMENT STORAGE
---------------------------------
The generic simple-element layer builds separate integer/bool element lists, sizes its backing
vectors to those exact counts, reads each registered `i<Name>` / `b<Name>` tag, and stores integer
values as `short` through `safeIntCast<short>`.

A complete current-source census found 38 integer registrations across Building, Trait/GameOption,
Tech, Terrain/Improvement and TrueStarts info classes. A complete current XML numeric census found
NO stored value outside signed-short range. Representative extremes include:
- TrueStarts LongitudeTimes10: -991..1358;
- TrueStarts Precipitation: 1..2270;
- TrueStarts StartOfReign: -2650..1949;
- TrueStarts MaxElevation: 322..8586;
- TrueStarts TotalArea: up to 22800;
- ordinary game/handicap/terrain/tag percentages remain far smaller.

Two TrueStarts optional registrations use `MIN_INT` as their nominal absent sentinel:
`MaxElevation` and `StartOfReign`. A signed short cannot represent `MIN_INT`; in a release build an
actually omitted field would narrow that value (typically to 0), so the later `== MIN_INT` tests
could not recognize absence. This is a real design/robustness defect in the compact generic storage.
It is NOT currently activated: all 34/34 shipped `CvTruCivInfo` records explicitly provide
MaxElevation and all 52/52 shipped `CvTruLeaderInfo` records explicitly provide StartOfReign, with
all values inside short range. The existing F619 TrueStarts MaxElevation root is the separate
uninitialized aggregate member and does not turn this dormant sentinel mismatch into a second live
root. Preserve this as future-data hardening, not F682.

The element-ID/count invariant also reconciles for current derived classes: registrations are
appended in the same contiguous order as their corresponding enum values beginning at each
`base_t::NUM_*_ELEMENT_TYPES`. Debug duplicate-ID/name checking was added in practical 3579; no
current registration collision or mismatched live getter domain was found.

XML FILE CACHE CONTROL
----------------------
`CvInfo_Base.h` defines `ENABLE_XML_FILE_CACHE 0`. The `CvInfoBase`, `CvXMLInfo` and `CvHotkeyInfo`
binary cache methods are therefore compiled out in current SAS. Their incomplete generic-element
size/version protocol and old migration history are not current runtime surfaces; prior XML-loader
queues already established the same compile-time boundary. Do not promote dormant cache-format
concerns here.

HISTORY / DEDUP CONTROLS
------------------------
Meaningful retained strata reconcile as follows:
- practical 1644 split these bodies out of `CvInfos.cpp` without semantic change;
- practical 1732 introduced the generic simple-element mechanism;
- 1777 folded it into `CvHotkeyInfo`; later compact/refactor work retained contiguous enum storage;
- practicals 2222/2223/2236/2237 introduced/refined `LEADER_DEFAULTS`; current one-default-object
  ownership/copy behavior matches the dedicated LeaderHead implementation;
- practical 2561 split `getDescriptionInternal` to support sea-level recommendation text without
  duplicating cache logic; current override remains the sole consumer;
- practical 3050 repaired dormant XML-cache compilation;
- 3147 refactored `CvXMLInfo` element descriptors/lists; 3183 replaced narrow casts with the current
  checked `safeIntCast` family;
- practical 3579 added True Starts data plus debug duplicate-element checks;
- SAS 5961 is formatting only in this `.cpp`.

Whole-album dedup found no independent current root belonging to this file. Existing localization,
XML-loader/default, TrueStarts and action/hotkey findings remain separate and already-owned where
applicable.

QUEUE 070 VERDICT
-----------------
No independent consequential current root.
F682 / provisional KI#1003 remains UNUSED.
Queue 070 `CvGameCoreDLL/CvInfo_Base.cpp` is COMPLETE.

QUEUE 071 OPEN
--------------
With the previously marked priority-WIP files already completed/reconciled, ordinary queue order now
advances to Queue 071 `CvGameCoreDLL/CyInfoInterface2.cpp` (491 queued lines), under F682 /
provisional KI#1003 UNUSED. Audit binding-name/native-target parity, enum/index adapters, ownership
policies/default arguments, SAS-vs-Base additions and every suspicious wrapper against shipped
Python callers before whole-album dedup.

Exact continuation stone:
C031-WIP568 physical -> Queue070 CLOSED CLEAN -> F682/KI#1003 UNUSED -> Queue071
CyInfoInterface2.cpp OPEN -> provenance + mechanical binding table + ownership/index/default/live-
Python reachability -> whole-album history/dedup -> promote only a demonstrated consequential live
root or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP569 - QUEUE 071 CYINFOINTERFACE2.CPP CLOSED CLEAN; QUEUE 073 OPEN
===============================================================================
Authoritative input: reconciled local `AdvCiv-SAS_light_source_20260909T154245.zip` plus the supplied
Base AdvC 1.14, K-Mod, Civ4CE and Taurus source archives. No web/GitHub lookup was used.
Gameplay/XML/Python source remains untouched.

QUEUE 071 PROVENANCE / SAS DELTA
--------------------------------
Current SAS `CvGameCoreDLL/CyInfoInterface2.cpp` is 491 physical lines versus 452 in supplied Base
AdvC 1.14. Mechanical active-binding comparison (comments stripped before parsing) localizes the
entire executable SAS-vs-Base delta in this file to 27 new read-only getter registrations:
- 16 `CvHandicapInfo` getters used by the SAS Sevopedia Handicap Chart;
- 11 `CvGameSpeedInfo` getters used by the SAS Sevopedia Game Speed Chart.

The 16 Handicap additions are:
`getAIResearchPercent`, `getBuildTimePercent`, `getBaseGrowthThresholdPercent`,
`getGPThresholdPercent`, `getAIGPThresholdPercent`, `getCultureLevelPercent`, `getTrainPercent`,
`getConstructPercent`, `getCreatePercent`, `getAdvancedStartPointsMod`,
`getAIAttitudeChangePercent`, `getAIHandicapIncrementTurns`, `getForeignCultureStrength`,
`getBarbarianCityAttackBonus`, `getSeaBarbarianBonus`, and `getSeaBarbarianExtraMoves`.

The 11 GameSpeed additions are:
`getCulturePercent`, `getAIMemoryRandPercent`, `getAIContactRandPercent`,
`getAIContactDelayPercent`, `getFullTradeCreditPercent`, `getRevoltDivPercent`,
`getReligionSpreadDivPercent`, `getEventRollSidesPercent`, `getVoteIntervalPercent`,
`getUnitCostPercent`, and `getExtraFreeOutsideUnits`.

All 27 additions are live in shipped SAS Python. Their chart specifications name the same XML/native
field represented by each getter, and the C++ implementations return the corresponding member or
registered generic element one-for-one. No wrong getter, swapped field, hidden mutation or lifetime
change was found in the SAS delta.

MECHANICAL BINDING PARITY
-------------------------
After stripping comments, the file contains 339 active `.def` registrations across 19 info classes.
The audit found:
- zero duplicate active Python method names within a class;
- zero Python-class/native-target-class mismatches;
- no suspicious name/target mismatch after accounting for deliberate `py_` integer adapters and
  stable Python compatibility names.

A temporary apparent duplicate/misbinding was explicitly rejected: the source still contains the
commented historical line
    //.def("getHappiness", &CvImprovementInfo::getDefenseModifier, ...)
followed by the compiled correct binding
    .def("getHappiness", &CvImprovementInfo::getHappiness, ...)
This is documentation of the old Unofficial Patch repair. Civ4CE retains the old wrong active
binding, while current SAS/Base/K-Mod/Taurus use the repaired getter. It is not a current root.

INDEX / ENUM ADAPTER CONTRACTS
------------------------------
The inherited array/index bindings were cross-checked against current shipped Python producers.
Live callers derive indexes from the matching info counts or exact enum domains: techs, features,
terrains, bonuses, yields, routes, building/unit classes, leader IDs and civilization city-name
counts. The one map-script feature path worth checking (`PerfectMongoose`) guards
`FeatureTypes.NO_FEATURE` before calling the build-feature arrays. No shipped negative sentinel or
out-of-range producer was found for these Queue-071 bindings.

The major AdvC-vs-K-Mod/Civ4CE/Taurus target differences are intentional type-safety refactors:
- practical 1922 routes BuildInfo feature access through `py_getFeatureTech`,
  `py_getFeatureTime`, `py_getFeatureProduction` and `py_isFeatureRemove`;
- practical 2796 routes RouteInfo prerequisite-list access through `py_getPrereqOrBonus` after the
  native list became typed/vector-backed;
- practicals 3052/3392 route BuildingClass victory-threshold access through
  `py_getVictoryThreshold` after the nested XML data refactor;
- practical 1918 renamed the native BuildingClass default-building getter while preserving the
  stable Python name `getDefaultBuildingIndex`.
These changes preserve the Python API while adapting it to typed native signatures; no semantic
regression was found.

REFERENCE / OWNERSHIP CONTROL
-----------------------------
Only two active bindings in this file use `reference_existing_object`:
- `CvGameSpeedInfo.getGameTurnInfo`, returning an element of the globally owned GameSpeedInfo turn
  increment array;
- `CvBonusInfo.getArtInfo`, returning the globally owned bonus art-info object.
Both owners persist for the relevant Python lifetime, and shipped callers do not retain them across
info teardown/reload. `GameTurnInfo` is technically exposed as writable by the separate struct
binding, but current shipped Python only reads its two fields; no mutation of global game-speed data
was found. Keep hypothetical external-Python mutation as API hardening, not F682.

KNOWN DORMANT API DEFECT DEDUP
------------------------------
`CvCivilizationInfo.getNumLeaders` is bound here, but the earlier Queue-034 audit already proved that
its backing `m_iNumLeaders` is never populated by the current XML reader and therefore always
reports 0. Current shipped Python has no caller; actual leader membership uses `isLeaders(...)`.
That inherited dormant public-API defect is already recorded in the album and remains below the
current-live finding threshold. Do not duplicate it as F682.

HISTORY CONTROL
---------------
The retained path history reconciles without exposing another root:
- practical 1412 made the info bindings `boost::noncopyable`;
- practical 1499 preserved the Unofficial Patch `getHappiness` correction while simplifying its
  comments;
- practical 1610 corrected only the Python documentation return type of the free-tech booleans;
- practical 1644 followed the CvInfo header split;
- practical 1725 moved YieldInfo/TerrainInfo bindings here because Interface3 was too large;
- practicals 1918/1922/2796/3052 supplied the typed/native adapter changes above;
- practical 2287 removed unused RouteModel mutators from Python;
- practical 1398 retired the obsolete `getAIPerEraModifier` exposure;
- SAS 5292 and 5300 added the 27 chart getters audited above.
No later Base AdvC 1.14 repair is missing from SAS in this file.

QUEUE 071 VERDICT
-----------------
No independent consequential current root.
F682 / provisional KI#1003 remains UNUSED.
Queue 071 `CvGameCoreDLL/CyInfoInterface2.cpp` is COMPLETE.

QUEUE 073 OPEN
--------------
Queue 072 `CvStatistics.cpp` was already completed at the earlier WIP489 priority pass, so ordinary
queue order advances directly to Queue 073 `CvGameCoreDLL/CyPlayerInterface1.cpp` (476 queued lines)
under F682 / provisional KI#1003 UNUSED. Audit Python-name/native-target parity, overload/default
argument bindings, player/sentinel/index domains, ownership policies and current SAS additions
against shipped Python callers and the supplied ancestry before whole-album dedup.

Exact continuation stone:
C031-WIP569 physical -> Queue071 CLOSED CLEAN -> F682/KI#1003 UNUSED -> Queue072 already COMPLETE ->
Queue073 CyPlayerInterface1.cpp OPEN -> provenance + binding parity + live Python reachability +
player/sentinel/ownership/default contracts -> history/dedup -> promote only a demonstrated
consequential current root or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP570 - QUEUE 073 CYPLAYERINTERFACE1.CPP CLOSED CLEAN; QUEUE 075 OPEN
===============================================================================
Authoritative input: reconciled local `AdvCiv-SAS_light_source_20260909T154245.zip` plus supplied
Base AdvC 1.14, K-Mod, Civ4CE and Taurus archives. No web/GitHub lookup used.
Gameplay/XML/Python source remains untouched.

QUEUE 073 PROVENANCE / SAS DELTA
--------------------------------
Current SAS `CvGameCoreDLL/CyPlayerInterface1.cpp` is 476 physical lines. Active-binding comparison
against supplied Base AdvC 1.14 finds exactly two executable additions and no removals:

    calculateUnitCostBreakdown
    calculateUnitSupplyBreakdown

These are the same two SAS wrapper additions already exhaustively reconciled during Queue 021
`CyPlayer.cpp` at C031-WIP438. They return the exact raw native support-cost subtotals consumed by
the SAS Military/Financial UI. Their former anarchy-display misuse is the already-owned KI#219
Python-consumer issue and is fixed at current HEAD; the bindings themselves are not a new root.

MECHANICAL REGISTRATION PARITY
------------------------------
After stripping comments, Queue 073 contains 384 active `.def` registrations and 384 unique Python
method names. Every Python name targets the same-named `CyPlayer` wrapper method; there is no active
duplicate registration, wrong-class target, or name/target swap.

The earlier Queue-021 audit is a particularly strong dedup oracle here because it reconciled BOTH
`CyPlayerInterface1.cpp` and `CyPlayerInterface2.cpp` together against current `CyPlayer.cpp/.h`:
436 unique public bindings, zero duplicate public names, every bound non-inline method implemented,
and all apparent bound-without-body cases accounted for by intentional inline wrappers. Queue 073
does not reopen that already-complete facade/body proof.

OWNERSHIP POLICY CHECK
----------------------
Queue 073 has eight active `manage_new_object` registrations:

    findStartingPlot
    initCity
    initUnit
    getStartingPlot
    getCapitalCity
    getCity
    getUnit
    getSelectionGroup

Current wrapper bodies were rechecked. Each successful object return is an independent `new Cy*`
wrapper (or returns NULL before allocation), so Python owns exactly the wrapper object declared by
the Boost policy; the underlying Cv* game object remains engine-owned. No KI#480-style
`manage_new_object` mismatch survives here.

The standard Civ4 iterator tuple wrappers (`firstCity`, `nextCity`, `firstUnit`, etc.) are wrapper
implementation behavior rather than Queue-073 return-policy registrations and were already included
in the Queue-021 wrapper audit. No new registration-layer ownership change was introduced here.

PLAYER / ENUM / POINTER CONTRACT DEDUP
--------------------------------------
Queue 021 already mechanically scanned the legacy CyCity/CyPlot/CyArea/CyUnit pointer arguments and
current shipped Python callers. Live callers supply concrete engine wrappers; no supported Python
`None` feed into an inherited unsafe dereference was established. Its 48-player/enums pass likewise
found no narrowing/bitmask loss in these CyPlayer wrappers.

The current Interface1 registration layer adds no default argument that changes those contracts and
no overload selector capable of binding a different native signature. AdvC changes versus K-Mod/
Civ4CE (favorite civic/religion knowledge, visibility/espionage helpers, WB-start controls,
calculateCurrentTotalYield, etc.) all target wrapper bodies already reconciled against their native
contracts at Queue 021.

DOCUMENTATION-ONLY NOISE
------------------------
Several inherited `.def` docstrings are stale even though the actual member pointer is correct, e.g.
`getProjectProductionNeeded` is documented with an obsolete bool/three-argument signature,
`getCapitalCity` mentions an ID argument, and `changeAnarchyTurns` omits its integer argument. Boost
binds the C++ member-function type rather than parsing these documentation strings, so this can
mislead external API readers but does not alter Python call arity, return type or gameplay. Preserve
as low-grade documentation debt, not F682.

ANCESTRY / BASE CONTROL
-----------------------
Current-vs-Base active registration parity is complete apart from the two safe SAS breakdown
bindings. K-Mod/Civ4CE/Taurus differences are the known AdvC wrapper/API evolution already covered
by Queue 021; Base AdvC 1.14 contains no later Queue-073 repair missing from current SAS. No distinct
current-live inherited registration root survives whole-album dedup.

QUEUE 073 VERDICT
-----------------
No independent consequential current root.
F682 / provisional KI#1003 remains UNUSED.
Queue 073 `CvGameCoreDLL/CyPlayerInterface1.cpp` is COMPLETE.

QUEUE 075 OPEN
--------------
Queue 074 `CvGameCoreDLL/WarEvaluator.cpp` was already completed at C031-WIP491 / F650, so ordinary
queue order advances directly to Queue 075 `CvGameCoreDLL/SelfMod.cpp` (472 queued lines) under
F682 / provisional KI#1003 UNUSED. Audit current-vs-Base/ancestry provenance first, then self-modifier
state initialization/update/cache contracts, numeric domains and all live consumers; dedup before
promotion.

Exact continuation stone:
C031-WIP570 physical -> Queue073 CLOSED CLEAN -> F682/KI#1003 UNUSED -> Queue074 already COMPLETE ->
Queue075 SelfMod.cpp OPEN -> provenance + state lifecycle + numeric/cache contracts + live consumers
+ history/dedup -> promote only a demonstrated consequential current root or close cleanly -> freeze
next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP571 - QUEUE 075 SELFMOD.CPP - F682 / KI#1003 CONFIRMED; F683 UNUSED
===============================================================================
Physical parent: C031-WIP570.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14 and Taurus 1.04, plus local reachable-history diffs. Gameplay/XML/Python source untouched.

PROVENANCE CONTROL
------------------
After comments and whitespace/signature-layout changes are removed, current SAS `SelfMod.cpp` and
supplied Base AdvC 1.14 have identical executable token streams. K-Mod/Civ4CE predate this AdvC/Taurus
runtime-patch subsystem. Taurus 1.04 retains the same fallback-search implementation. Therefore the
root below is inherited rather than a SAS-only regression or a missed later Base repair.

F682 / PROVISIONAL KI#1003 - FALLBACK EXE ADDRESS SEARCH LEAKS 512 KiB PER SEARCH
--------------------------------------------------------------------------------
`SelfMod::updateAddressOffset` falls back from a quick expected-address test to a bounded memory
search. It allocates the copied search window as:

    int const iHaystackBytes = iMaxSubtrahend + iMaxAddend;
    byte* pHaystackBytes = new byte[iHaystackBytes];

then fills it and calls `std::search`. There is no `delete[] pHaystackBytes`, smart pointer, vector,
or other owner anywhere in the function.

The allocation is lost on BOTH outcomes:
- no match: the function sets `m_iAddressOffset = MIN_INT` and returns immediately;
- match: it computes `std::distance(pHaystackBytes, pos)`, exits the block, stores the offset and
  returns without freeing the array.

With the default `iMaxAbsOffset = 256 * 1024`, both current search origins are farther than 256 KiB
from the hard low/high bounds, so each fallback allocates exactly:

    262,144 bytes below + 262,144 bytes above = 524,288 bytes = 512 KiB

and permanently loses that allocation for the lifetime of the DLL process.

SUPPORTED CURRENT ACTIVATION
----------------------------
This is not limited to an unrecognized executable.

1. Help-text-area patch, ordinary default Steam offset

`HelpTextAreaWidthMod::apply` calls `findAddressOffset` when the quick bytes are not at their original
address. The current source explicitly documents that in the default Steam version its needle is at
`0x0055AD2F`, corresponding to offset +4800. That is a recognized successful fallback-search path.
Shipped `CvMainInterface.setDefaultHelpTextArea` calls `gc.getGame().setHelpTextAreaWidth(...)` before
creating the help area. The stored-width guard prevents identical repeated work, but the first shifted-
EXE patch still loses 512 KiB; a later different target width can invoke it again.

2. Plot-indicator patch, recognized Steamless offset

`PlotIndicatorSizeMod::apply` documents and accepts the Steamless/unpacked Steam BtS sequence at
+1616. Both BUG settings `MainInterface__PlotIndicatorSize` and `MainInterface__OffScreenUnitSizeMult`
have shipped `<change module="InputUtil" function="updatePlotIndicatorSize"/>` callbacks, and that
callback calls `cyUpdatePlotIndicatorSize()` -> `smc::BtS_EXE.patchPlotIndicatorSize()`. Whenever a
changed setting produces a new base size on the shifted layout, the fallback search allocates and
loses another 512 KiB. Screen-dimension changes can also call the patch entry point.

The no-match/error path leaks the same buffer, so unsupported layout retries are no safer.

CONSEQUENCE / CLASSIFICATION
----------------------------
This is persistent native heap growth rather than gameplay-state corruption. A single activation is
modest, but the buffer is unusually large for a leaked temporary (512 KiB) and shipped UI option
callbacks can repeat it during one process. The leak is deterministic whenever the quick-address test
fails and fallback search is entered. It therefore clears the current-live finding threshold rather
than being generic API robustness.

LINEAGE
-------
Reachable-history practical 3847 / commit `a06dd7516458677ebf90bb980223ad7198fe9ee0`, titled
"Refactor SelfMod class (based on Taurus 1.00)", introduced `updateAddressOffset` and the
`new byte[iHaystackBytes]` fallback-search buffer in the same patch. No deletion was introduced.
Later practicals added/adjusted supported Steam/Steamless offsets but preserved the leak. Supplied
Base AdvC 1.14 and Taurus 1.04 still contain it unchanged.

DEDUP
-----
Whole-album search found no prior finding owning this `SelfMod::updateAddressOffset` search-buffer
lifetime. Earlier ownership/leak findings (e.g. map Shelf ownership, info teardown, popup records)
are unrelated allocation roots. F682 is independent.

NARROW REPAIR BOUNDARY
----------------------
Keep the search algorithm and address-offset semantics unchanged; only give the temporary search
window automatic lifetime. Preferred repair is a local RAII container such as
`std::vector<byte> aHaystack(iHaystackBytes)` and use its contiguous storage for fill/search/distance.
A manual `delete[]` would also work only if every success/failure exit is covered; RAII is safer and
prevents future early-return leaks.

QUEUE 075 CONTINUES
-------------------
F682 / KI#1003 is CONFIRMED and frozen.
F683 / provisional KI#1004 is UNUSED.
Queue 075 remains OPEN. Continue the remaining runtime-patch logic: VirtualProtect restore ranges,
address arithmetic/offset validation, repeated-patch caches, option/FoV/resolution numeric domains,
help-width byte narrowing, supported executable-layout handling and whole-album/history dedup.

Exact continuation stone:
C031-WIP571 physical -> Queue075 SelfMod.cpp OPEN -> F682/KI#1003 CONFIRMED search-buffer leak ->
F683/KI#1004 UNUSED -> finish remaining SelfMod page-protection/address/numeric/cache/live-caller
contracts -> either promote an independent F683 or close Queue075 cleanly -> freeze next WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP572 - QUEUE 075 SELFMOD.CPP CLOSED - F682 ONLY; F683 UNUSED; QUEUE 076 OPEN
===============================================================================
Physical parent: C031-WIP571.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14 and Taurus 1.04, plus local reachable-history diffs. Gameplay/XML/Python source untouched.

QUEUE 075 CLOSURE
-----------------
Queue 075 `CvGameCoreDLL/SelfMod.cpp` is COMPLETE with exactly one demonstrated current-live root:
F682 / provisional KI#1003, the 512-KiB fallback-search temporary allocation leak recorded and frozen
in C031-WIP571. No independent F683 survives the remaining whole-body/history pass.

POST-F682 NEGATIVE CONTROLS
---------------------------
1. Plot-indicator page-protection restoration

`PlotIndicatorSizeMod::apply` temporarily changes page protections around four float operands and then
restores them. A generic helper that restored overlapping page-protection operations in the wrong order
could be dangerous, but the four current patch addresses occupy distinct memory pages; the help-area
patch has only one site. The current shipped patch set therefore does not create an overlapping-page
restore conflict.

2. Retry/cache state after patch failure

`patchPlotIndicatorSize` marks its attempt state before applying the patch, and both the plot-size and
help-width paths cache the requested value before/around application. That can suppress an identical
retry after a failed patch. Current supported failures, however, are deterministic executable-layout
mismatches rather than demonstrated transient conditions; `DISABLE_EXE_RUNTIME_MODS` intentionally
permits ordinary unpatched BtS behavior. Without a supported transient activation this remains
robustness/failure-policy behavior, not a separate gameplay defect.

3. Help-text-area byte narrowing

The EXE operand for help-area width is a byte and the code deliberately narrows the calculated
multiplicand through `safeIntCast<byte>`. The shipped HUD geometry, help-font range and current SAS
width-adjustment/scaling values keep the resulting multiplicand comfortably inside 0..255 at normal
supported resolutions. Extreme custom theme/define configurations can exceed that representation, but
no current shipped activation was established.

4. FoV/resolution numeric domains

Screen-height dependent plot scaling is entered only after a positive-height guard. Current FoV UI and
defaults keep the square-root/division domain positive and sensible; the resulting factor is also
clamped. Plot-indicator/off-screen option ranges are bounded by their shipped option definitions. No
live divide-by-zero, invalid square root or overflow root survives.

5. Address arithmetic and supported offsets

The fallback search bounds are small relative to the current patch addresses, so current signed-offset
arithmetic does not underflow/overflow the recognized address domain. Plot-indicator code explicitly
accepts the known 0/+1616 layouts; help-width source documents 0/+4800. Search-end inclusivity and
other hypothetical farther layouts are unsupported-EXE robustness rather than current defects.

6. Instruction-cache flush concern

The patcher does not explicitly call `FlushInstructionCache` after writing the operand bytes. On the
actual x86 BtS target there is no demonstrated stale-instruction failure in the shipped patch paths,
and no current fixture establishes a consequential runtime defect. Preserve as a portability/
hardening consideration only, not F683.

HISTORY / PROVENANCE CLOSURE
----------------------------
Current SAS and supplied Base AdvC 1.14 remain executable-semantically identical in this file once
comments/formatting are normalized. Reachable history from the original plot-indicator patch through
the help-area/Steam-support changes contains no later independent repair omitted by current SAS.
Taurus 1.04 confirms the inherited SelfMod family and independently retains F682's allocation root.
No second current-live root emerged from the retained history.

QUEUE RESULT / NEXT CURSOR
--------------------------
Queue 075 is CLOSED at C031-WIP572.
F682 / KI#1003 remains CONFIRMED.
F683 / provisional KI#1004 remains UNUSED.
Files complete: 89 / 169.

Ordinary queue order now advances to Queue 076 `CvGameCoreDLL/CyCityInterface1.cpp` (461 queued lines)
under F683 / provisional KI#1004 UNUSED. Start with current-vs-Base/ancestry registration provenance,
then binding-name/target parity, ownership return policies, default/sentinel/index contracts and live
Python reachability; dedup against the already-completed Queue 024 `CyCity.cpp` wrapper-body audit
before promoting anything.

Exact continuation stone:
C031-WIP572 physical -> Queue075 CLOSED -> F682/KI#1003 CONFIRMED; no independent F683 -> Queue076
CyCityInterface1.cpp OPEN -> F683/KI#1004 UNUSED -> provenance + binding parity + ownership/default/
index/live-call contracts + Queue024/whole-album dedup -> promote only a distinct consequential live
root or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP573 - QUEUE 076 CYCITYINTERFACE1.CPP CLOSED CLEAN; F683 UNUSED; QUEUE 077 OPEN
===============================================================================
Physical parent: C031-WIP572.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, K-Mod and Civ4CE comparison trees, plus the already-completed Queue 024 `CyCity.cpp` audit.
Gameplay/XML/Python source untouched.

PROVENANCE CONTROL
------------------
Current SAS and supplied Base AdvC 1.14 `CyCityInterface1.cpp` have identical executable token streams
across the full current 461-line file. Therefore there is no SAS-only registration delta and no missed
later Base repair to explain a new F683 here.

ACTIVE BINDING TABLE
--------------------
After stripping comments before parsing registrations:
- 404 active `.def(...)` registrations;
- 404 unique Python method names;
- zero active duplicate Python names;
- exactly one Python-name/native-target difference:

    getGeneralProductionTurnsLeft -> CyCity::getProductionTurnsLeft

This is the deliberate AdvC compatibility alias already audited in Queue 024. Native practical 1687
removed `getGeneralProductionTurnsLeft`; the replacement differs for processes by returning MAX_INT,
which CyCity maps to -1 instead of the legacy 0. Shipped Domestic Advisor display tests only `> 0`, so
both sentinel values render identically. It is not a current defect.

OWNERSHIP POLICIES - QUEUE 024 DEDUP ORACLE
--------------------------------------------
All seven `python::manage_new_object` registrations were rechecked against the Queue-024 wrapper-body
proof:
- `getCityIndexPlot` -> independent `new CyPlot`;
- `plot` -> independent `new CyPlot`;
- `area` -> independent `new CyArea`;
- `waterArea` -> independent `new CyArea`;
- `getRallyPlot` -> independent `new CyPlot`;
- `getTradeCity` -> independent `new CyCity`;
- `getOrderFromQueue` -> independent `new OrderData(copy)` after the already-owned SAS KI#480 repair.

No second owning-policy mismatch survives. In particular, do not reopen KI#480 merely because its
registration lives in this interface file.

ANCESTRY / TARGET-PARITY CONTROLS
---------------------------------
Relative to K-Mod/Civ4CE, current AdvC has expected additions/removals from later city APIs. Only two
shared-name target changes appear:
- `getGeneralProductionTurnsLeft`: intentional compatibility alias described above;
- `getProjectProductionModifier`: K-Mod/Civ4CE bind this name to generic `getProductionModifier`, while
  current AdvC correctly binds the project-specific getter (`advc.001`). This is an inherited fix
  already present in Base 1.14, not a current regression.

The other AdvC-only registrations (hurry-overflow, production-decay/time helpers, revolt/culture-flip,
partisans and AI floating-defender diagnostics) were already traced to their native bodies and shipped
Python consumers during Queue 024. No registration/body semantic drift was found here.

INDEX / POINTER / DESTRUCTIVE-CALL DEDUP
-----------------------------------------
Queue 024 explicitly reconciled the bound pointer arguments, enum/index producers, nullable trade-city
chains, destructive `kill`/liberation lifetime shape, production/food/decay sentinels and current
Python call sites across BOTH CyCity interface files. Repeating those native-body roots under the
registration queue would double-count the same contracts. No new registration-layer default argument,
return policy, overload or target changes those prior conclusions.

LOW-GRADE DOCUMENTATION ONLY
----------------------------
Legacy docstrings retain stale wording/signatures in places, but Boost.Python binds the actual member
function pointer rather than parsing those strings. No runtime arity/type defect follows from the
text alone.

QUEUE RESULT / NEXT CURSOR
--------------------------
Queue 076 `CvGameCoreDLL/CyCityInterface1.cpp` is COMPLETE with NO NEW FINDING.
F683 / provisional KI#1004 remains UNUSED.
Files complete: 90 / 169.

Ordinary queue order advances to Queue 077 `CvGameCoreDLL/StartPointsAsHandicap.cpp` (451 queued
lines) under F683 UNUSED. Establish current-vs-Base/ancestry provenance first, then audit rank/start-
plot collection, normalization/order assumptions, handicap reassignment arithmetic, player/team
sentinels, game-option activation and downstream consumers. Dedup against prior starting-position and
TrueStarts findings before promotion.

Exact continuation stone:
C031-WIP573 physical -> Queue076 CLOSED CLEAN -> F683/KI#1004 UNUSED -> Queue077
StartPointsAsHandicap.cpp OPEN -> provenance + activation + ranking/order/arithmetic + player/team/
plot sentinels + downstream effects + history/whole-album dedup -> promote only a distinct current-live
root or close cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP574 - QUEUE 077 STARTPOINTSASHANDICAP.CPP - F683 / KI#1004 CONFIRMED; F684 UNUSED
===============================================================================
Physical parent: C031-WIP573.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, reachable pre-SAS AdvC history and current shipped Python/UI consumers. Gameplay/XML/Python
source untouched.

PROVENANCE CONTROL
------------------
Current SAS and supplied Base AdvC 1.14 `StartPointsAsHandicap.cpp` have identical executable token
streams across the full 451-line file. K-Mod/Civ4CE predate this AdvC-specific subsystem. Therefore
F683 is inherited AdvC behavior rather than a SAS-only regression or missed Base repair.

F683 / PROVISIONAL KI#1004 - REBUILDING THE SPAH SETTINGS STRING LEAKS THE PREVIOUS WSTRING
------------------------------------------------------------------------------------------
`StartPointsAsHandicap` owns its display string through the raw member pointer
`pointsDisplayString`. Constructor initializes it to NULL, and destructor/reset correctly delete the
CURRENT pointer. `updatePointsDisplayString`, however, replaces that member with a fresh allocation
without releasing the previous value:

    if(civs[nCivs - 1]->startPoints_configured() <= 0) {
        pointsDisplayString = new wstring();
        return;
    }
    ...
    pointsDisplayString = new wstring(s.str());

There is no `SAFE_DELETE(pointsDisplayString)` before either assignment. Once overwritten, the old
`std::wstring` object and its owned character buffer are unreachable; reset/destructor can free only
the newest pointer.

ORDINARY SHIPPED ACTIVATION
---------------------------
This is not a dormant helper.

1. Initial game setup
   `StartPointsAsHandicap::setInitialItems()` calls `updatePointsDisplayString(true)`, creating the
   first owned string for every active SPaH game.

2. Victory-screen Settings tab
   Shipped `CvVictoryScreen.py` calls `g.SPaHPointsForSettingsScreen()` when SPaH is active.
   `CyGame::SPaHPointsForSettingsScreen()` calls `startPointsAsHandicap().forSettingsScreen()`.
   Since practical 1416, `forSettingsScreen` itself calls `updatePointsDisplayString(bTab)` before
   returning the pointer. Therefore the FIRST ordinary Settings query already overwrites and leaks
   the setup allocation; every subsequent Settings-screen rebuild/query can leak another allocation.

3. Replay/settings capture
   `CvReplayInfo` calls `forSettingsScreen(false)` when building SPaH settings text, providing an
   independent later live query that can overwrite/leak whichever display allocation is current.

The per-call leak is small compared with F682, but it is deterministic, repeatable through normal UI
use and unbounded over repeated queries. The lost object includes both the heap-allocated `wstring`
object and its dynamically allocated distribution-text buffer.

LINEAGE
-------
The original SPaH implementation (practical 1253) kept `forSettingsScreen()` as a simple return of the
cached pointer and built the string during setup. Practical 1416 / commit
`72f41f88dca77624eaab3422f829b38b8d997b55`, titled "Changes to Worker AI and DoW confirmation
popup", changed the method to:

    updatePointsDisplayString(bTab);
    return pointsDisplayString;

so the same cached member could be formatted with tab/non-tab indentation. That patch also improved
reset/destructor cleanup of the CURRENT pointer, but it did not add deletion before the newly repeated
allocations in `updatePointsDisplayString`. The leak starts at this recompute-on-query change and
survives Base AdvC 1.14/current SAS.

DEDUP
-----
Whole-album search found no prior owner for the SPaH display-string lifetime. It is independent of
F682's 512-KiB SelfMod search-buffer leak, F628's scenario normalization-target leak, F632's Shelf
ownership leak and F659's info-teardown row leaks.

NARROW REPAIR BOUNDARY
----------------------
Preserve all formatting and call semantics. Before assigning either new `wstring`, release the prior
owned value, e.g. `SAFE_DELETE(pointsDisplayString)`. Cleaner long-term ownership would store the
text by value (`mutable std::wstring`) and return/reference/copy that value, eliminating manual heap
ownership entirely. Either repair must preserve the two indentation modes used by Victory/Replay.

QUEUE 077 CONTINUES
-------------------
F683 / KI#1004 is CONFIRMED and frozen.
F684 / provisional KI#1005 is UNUSED.
Queue 077 remains OPEN. Continue the independent gameplay/state audit: SPaH save/read identity and
unused-array serialization, `randPoints` state, scenario activation without `setInitialItems`,
start-plot reassignment/order invariants, randomization bounds, Advanced-Start point arithmetic and
final-score/display consumers. Dedup before any F684 promotion.

Exact continuation stone:
C031-WIP574 physical -> Queue077 OPEN -> F683/KI#1004 CONFIRMED repeatable settings-string leak ->
F684/KI#1005 UNUSED -> finish save/read + scenario + randPoints + ranking/reassignment + arithmetic/
sentinel + downstream-consumer contracts -> promote only an independent consequential root or close
Queue077 cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP575 - QUEUE 077 STARTPOINTSASHANDICAP.CPP - F684 / KI#1005 CONFIRMED; F685 UNUSED
===============================================================================
Physical parent: C031-WIP574.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, reachable pre-SAS AdvC history, shipped main-menu regeneration path and current XML/manual
fixtures. Gameplay/XML/Python source untouched.

F684 / PROVISIONAL KI#1005 - REGENERATE MAP CONSUMES SPAH'S ONE-SHOT SETUP VALUE, THEN REBUILDS
WITH -1 AND COLLAPSES THE HANDICAP DISTRIBUTION
---------------------------------------------------------------------------------------------
`StartPointsAsHandicap::assignPoints()` treats `CvInitCore::NumAdvancedStartPoints` as the encoded
SPaH setup value. Since practical 4067 it deliberately consumes that value immediately:

    int pointsEntered = GC.getInitCore().getNumAdvancedStartPoints();
    // (This value should not be used in a SPaH game)
    GC.getInitCore().setNumAdvancedStartPoints(-1);

That is safe only if SPaH initialization runs once. `CvGame::regenerateMap()` performs another full
`setInitialItems()` on the same `CvInitCore` without restoring the encoded value:

    m_eInitialActivePlayer = NO_PLAYER;
    setInitialItems();

The second SPaH `setInitialItems()` therefore calls `assignPoints()` with `pointsEntered == -1`.
Release builds do not stop at `FAssert(pointsEntered > 0)`. Because `-1 < 10000`, the code takes the
flat-distribution branch and sends `-1` to every AI's `setStartPoints_configured`. `MajorCiv::adjust`
then clamps it to `CvInitCore::getAdvancedStartMinPoints()`.

CURRENT-DATA FIXTURE
--------------------
`CvInitCore::getAdvancedStartMinPoints()` returns the Advanced-Start cost of the first default
founding unit. In current SAS XML the default Settler has:

    <bFound>1</bFound>
    <iAdvancedStartCost>100</iAdvancedStartCost>

so the regenerated setup deterministically assigns every AI 100 points.

The shipped AdvC SPaH manual documents five/six-digit unequal distributions; e.g. `80050` means a
strongest AI target of 800 points and a weakest target of 50 percent = 400 points, with intermediate
AIs distributed between them. On the first setup that produces the intended unequal roughly
400..800-point range (subject to the optional SPaH randomization preserving extrema/order). After
Regenerate Map the same game instead rebuilds every AI at 100 points.

SECOND GAMEPLAY CONSEQUENCE - STARTING-PLOT HANDICAP ALSO DISAPPEARS
--------------------------------------------------------------------
`setInitialItems()` stores the boolean returned by `assignPoints()`:

    unequalDistrib = assignPoints();

The valid `80050` first pass returns true. The corrupted `-1` second pass takes the flat branch and
returns false. Later `rearrangeStartingPlots()` immediately does:

    if(!unequalDistrib)
        return;

Thus Regenerate Map removes not only the configured Advanced-Start point distribution but also the
SPaH spatial rule that assigns weaker AIs nearer human starts and stronger AIs farther away. This is
a direct gameplay change to both halves of the option's handicap model.

ORDINARY SHIPPED ACTIVATION
---------------------------
The map-regeneration path is not a debug-only helper. `CvDLLButtonPopup::launchMainMenuPopup` exposes
"Regenerate Map" during ordinary single-player turn 0 when the game is not multiplayer, is not a WB
map script and no teams have met. The confirmation popup's `case 3` directly calls:

    kGame.regenerateMap();

`CvGame::regenerateMap` rebuilds the map, resets turn state and invokes `setInitialItems()` again.
Therefore a user can reproduce F684 through the normal main menu before playing the first turn in an
ordinary random-map SPaH game.

There is also an automated regeneration hook (`AUTO_REGEN_MAP`) for map-generation statistics, but
its current define defaults to 0; the normal menu path is sufficient native activation.

LINEAGE
-------
Practical 4067 / commit `3aa769419d5efe301d8cf16c05c55fd7c287f522`, titled
"Fix unequal start point assignment (SPaH option)", added exactly the two one-shot-consumption lines
shown above. The companion `CvPlayer::initFreeUnits` change was meant to preserve points already
assigned by SPaH. No regeneration restoration/re-encoding was added. The defect therefore begins
with practical 4067's consumption change. Supplied Base AdvC 1.14 and current SAS retain the same
`StartPointsAsHandicap.cpp` behavior; K-Mod/Civ4CE predate this AdvC-specific subsystem.

DEDUP
-----
Whole-album search found no prior owner for the consumed-SPaH-value/regeneration interaction. It is
independent of F083/KI#405, which concerns True-Starts team-tech cache transitions during
`CvGame::regenerateMap`, and independent of F683/KI#1004's SPaH display-string memory leak.

NARROW REPAIR BOUNDARY
----------------------
Preserve practical 4067's rule that the encoded setup value must not be reused as ordinary global
Advanced-Start points during normal initialization, but preserve enough canonical SPaH setup state
to rebuild the option when `setInitialItems()` legitimately runs again. Suitable narrow designs
include storing the decoded/configured SPaH distribution (or original encoded setup value) in the
SPaH object and reusing that on regeneration, or explicitly restoring/reseeding the SPaH setup input
before the regeneration `setInitialItems()` pass. Do not simply leave the global value live if doing
so reopens practical 4067's original double-assignment issue.

QUEUE 077 CONTINUES
-------------------
F683 / KI#1004 is CONFIRMED and frozen.
F684 / KI#1005 is CONFIRMED and frozen.
F685 / provisional KI#1006 is UNUSED.
Queue 077 remains OPEN. Continue the remaining independent contracts: save/read player identity,
unused `allPts` serialization suffix, `randPoints` state/display, reset/report state, ranking and
randomization arithmetic, end-score/distribution consumers and final history/dedup controls. The
previous scenario-empty-vector hypothesis is explicitly REJECTED because `CvGame::initScenario()`
calls `initFreeState()`, which initializes SPaH when the option remains active.

Exact continuation stone:
C031-WIP575 physical -> Queue077 OPEN -> F683/KI#1004 display-string leak + F684/KI#1005
Regenerate-Map one-shot SPaH-value loss CONFIRMED -> F685/KI#1006 UNUSED -> finish save/read identity
+ allPts/randPoints/reset + ranking/randomization + downstream score/display contracts -> promote
only a distinct consequential live root or close Queue077 cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP576 - QUEUE 077 STARTPOINTSASHANDICAP.CPP - F685 / KI#1006 CONFIRMED; F686 UNUSED
===============================================================================
Physical parent: C031-WIP575.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14 and reachable pre-SAS AdvC history. Gameplay/XML/Python source untouched.

F685 / PROVISIONAL KI#1006 - SPAH SAVES SERIALIZE THE UNINITIALIZED UNUSED TAIL OF `allPts`
------------------------------------------------------------------------------------------
Current `StartPointsAsHandicap` contains:

    int allPts[2 * MAX_CIV_PLAYERS];

The constructor initializes the pointer/string/count/flags but never initializes this array. In the
current 48-player DLL, `MAX_CIV_PLAYERS == 48`, so `allPts` contains 96 ints = 384 bytes.

On a new SPaH game, `setInitialItems()` builds the `civs` objects and assigns their point values but
does not populate `allPts`. The first save reaches:

    for(int i = 0; i < nCivs; i++) {
        allPts[2 * i] = civs[i]->startPoints_configured();
        allPts[2 * i + 1] = civs[i]->startPoints_actual();
    }
    pStream->Write(2 * MAX_CIV_PLAYERS, allPts);

Only the first `2*nCivs` ints are initialized; the stream nevertheless writes all 96 ints. For an
8-major-civ game, 16 ints are valid and the remaining 80 ints = 320 bytes are stale/uninitialized
object-storage contents. Any game with fewer than all 48 major-civ slots used has an unused tail.

CONSEQUENCE / CLASSIFICATION
----------------------------
`read()` reads the same full 96-int block but consumes only the first `2*nCivs` entries when
reconstructing SPaH, so the tail does not corrupt normal gameplay state. The defect is instead a
real serialization/information and reproducibility problem:

* bytes that are not part of SPaH state are copied from previously uninitialized process storage into
  the save stream;
* a save file can therefore contain stale heap/object bytes unrelated to the game state represented
  by SPaH;
* AdvC's `FDataStreamBase::Write(int,const int[])` also passes the exact byte range through
  `REPRO_TEST_REPORT` when reproducibility testing is enabled. `ReproTest.h` explicitly warns that
  uninitialized bytes written to saves defeat deterministic save-data comparison; this full unused
  array tail is the same class of problem, not merely struct padding.

Loading a save initializes the entire array from that save, so the strongest native fixture is the
first save of a newly initialized SPaH game. It requires no malformed data or unsupported DLL size.

LINEAGE
-------
Practical 1253 / commit `f6ff6a182f5187541fff6e5bbbe12f9573b3778c` introduced SPaH and this
serialization scheme. The original constructor allocated:

    allPts = new int [2 * MAX_CIV_PLAYERS];

without initializing the elements, and the original `write()` already filled only `2*nCivs` values
before writing the full `2*MAX_CIV_PLAYERS` block. Later refactoring changed `allPts` to fixed member
storage but retained the same incomplete initialization/full-block write. Supplied Base AdvC 1.14
and current SAS retain the defect; K-Mod/Civ4CE predate SPaH.

DEDUP
-----
Whole-album search found no prior owner for the SPaH `allPts` unused-tail serialization. It is
independent of F683's settings-string heap leak and F684's Regenerate-Map loss of the encoded SPaH
setup value.

NARROW REPAIR BOUNDARY
----------------------
Preserve the existing save layout unless a format migration is intentionally introduced. The
minimal compatibility-safe repair is to zero-initialize the whole `allPts` array (constructor/member
initialization, and preferably clear the unused tail before every write) before filling the live
prefix. Writing only `2*nCivs` entries would remove the tail too but changes the serialized layout
and therefore needs explicit save-format compatibility handling.

QUEUE 077 CONTINUES
-------------------
F683 / KI#1004, F684 / KI#1005 and F685 / KI#1006 are CONFIRMED and frozen.
F686 / provisional KI#1007 is UNUSED.
Queue 077 remains OPEN. Finish save/read identity, `randPoints` display state, reset/report state,
ranking/randomization bounds, end-score/distribution consumers and final history/dedup. Keep the
save/read player-ID reconstruction issue unpromoted unless a supported non-prefix player fixture and
consequential consumer are pinned; current evidence shows live Advanced-Start points themselves are
serialized separately by `CvPlayer`.

Exact continuation stone:
C031-WIP576 physical -> Queue077 OPEN -> F683-F685/KI#1004-KI#1006 CONFIRMED -> F686/KI#1007
UNUSED -> finish player-identity/randPoints/reset/ranking/randomization/score-display closure ->
promote only a distinct consequential root or close Queue077 cleanly -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP577 - QUEUE 077 STARTPOINTSASHANDICAP.CPP - F686 / KI#1007 CONFIRMED; F687 UNUSED
===============================================================================
Physical parent: C031-WIP576.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, reachable pre-SAS AdvC history, current `CvInitCore` slot contracts and shipped SPaH manual.
Gameplay/XML/Python source untouched.

F686 / PROVISIONAL KI#1007 - SPAH SAVE DATA OMITS PLAYER IDS AND REATTACHES POINT ROWS TO THE WRONG
PLAYERS AFTER LOAD WHEN HUMAN/AI SLOTS ARE NOT A CONTIGUOUS HUMAN-FIRST PLAYER-ID PREFIX
-------------------------------------------------------------------------------------------------
The live setup path explicitly records real player identity. `gatherCivs()` scans all player IDs,
skips nonparticipants/minors and constructs:

    MajorCiv* mciv = new MajorCiv(p.getID());

It then sorts `civs` with all humans first and, within equal point classes, by player ID. SPaH's
serialized state does NOT store those player IDs. `write()` emits only `nCivs` plus configured/actual
point pairs in current `civs` vector order.

`read()` reconstructs each row with a newly invented identity:

    for(int i = 0; i < nCivs; i++) {
        MajorCiv* mciv = new MajorCiv((PlayerTypes)i);
        mciv->setStartPoints_configured(allPts[2 * i]);
        mciv->setStartPoints_actual(allPts[2 * i + 1]);
        ...
    }

This assumes that participating major-player IDs are exactly `0..nCivs-1` AND that this numeric ID
order is the same as the human-first serialized vector order. Neither condition is a general
`CvInitCore` contract.

SUPPORTED MULTIPLAYER/HOTSEAT SLOT CONTRACT
-------------------------------------------
SPaH is disabled when Advanced Start is off or when all living civs are human; it is not disabled
merely because the game is multiplayer. The AdvC manual explicitly discusses team-game behavior for
SPaH. `CvInitCore` independently supports normal `SS_OPEN`, `SS_TAKEN`, `SS_COMPUTER` and
`SS_CLOSED` states per player slot. `getAvailableSlot()` scans for `SS_OPEN` slots while leaving
`SS_COMPUTER` slots in place, so the engine contract does not require all humans to occupy the
lowest IDs before every AI.

A simple supported slot arrangement is therefore:

    Player 0 = human
    Player 1 = AI / SS_COMPUTER
    Player 2 = human
    Player 3 = AI / SS_COMPUTER

At initial SPaH setup, `gatherCivs()` sorts those wrappers human-first:

    [P0 human, P2 human, P1 AI, P3 AI]

and `write()` serializes point rows in that order. On load, `read()` instead binds those four rows to:

    [P0, P1, P2, P3]

so rows 1 and 2 attach to the wrong controller identities.

DETERMINISTIC FLAT-400 FIXTURE
------------------------------
The shipped manual documents <=9999 Base Points as the ordinary flat-distribution mode. With 400
points and the four-player arrangement above, the saved rows are conceptually:

    P0 human: -1
    P2 human: -1
    P1 AI:    400
    P3 AI:    400

On read:

* row 0 -> P0 human remains -1;
* row 1 -> P1 AI receives the former P2-human value -1; `MajorCiv::adjust` clamps it to the current
  founding-unit minimum, 100;
* row 2 -> P2 human receives the former P1-AI value 400; `adjust` forces all human SPaH values to -1;
* row 3 -> P3 AI remains 400.

The reconstructed SPaH distribution is therefore 100,400 instead of the actual 400,400.

DOWNSTREAM CONSEQUENCE
----------------------
This does NOT retroactively change each `CvPlayer`'s live Advanced-Start point state: those player
objects serialize their gameplay state independently, and SPaH `read()` does not call
`assignStartPoints()`. The defect corrupts SPaH's own persistent metadata, which has two native
post-load consumers:

1. Victory/Settings and replay settings call `forSettingsScreen()` and display the reconstructed
   distribution, so the user sees 100,400 instead of 400,400.
2. `CvGame::getDifficultyForEndScore()` calls `m_pSpah->distribution()` and uses the max plus mean of
   those AI points in the difficulty factor. A save/load therefore changes the SPaH end-score
   handicap input even though the actual starting handicap did not change.

The same root also covers gaps caused by closed/minor/nonparticipating IDs: storing only row order
cannot reconstruct the real `PlayerTypes` relation after load.

LINEAGE
-------
Practical 1253 / commit `f6ff6a182f5187541fff6e5bbbe12f9573b3778c` introduced both sides of the
contract in one patch: `gatherCivs()` created wrappers from real `p.getID()` and sorted humans first,
while `write()` stored no IDs and `read()` recreated every row as `(PlayerTypes)i`. No later SPaH
history repair adds identity to the stream. Supplied Base AdvC 1.14 and current SAS retain the same
behavior; K-Mod/Civ4CE predate SPaH.

DEDUP
-----
Whole-album search found no prior owner for SPaH save/load player-identity reconstruction. F685 is a
separate bug in the same serialization function: it concerns uninitialized UNUSED tail bytes,
whereas F686 concerns the semantic identity missing from the USED rows. F684 concerns regeneration,
not save/load.

NARROW REPAIR BOUNDARY
----------------------
Persist stable player identity for each serialized SPaH row and reconstruct `MajorCiv` with that
stored `PlayerTypes`, rather than deriving identity from row number. Because this changes persistent
layout, add an explicit save-format/version migration or a compatibility representation (for
example, a fixed per-player table keyed by PlayerTypes) rather than silently inserting fields into
legacy data. Preserve `CvPlayer`'s independent Advanced-Start serialization.

QUEUE 077 CONTINUES
-------------------
F683-F686 / KI#1004-KI#1007 are CONFIRMED and frozen.
F687 / provisional KI#1008 is UNUSED.
Queue 077 remains OPEN only for final closure controls: `randPoints` is never set true and therefore
fails to emit the intended "Not shown: randomization" note when randomized values are hidden, but
this is currently a low-grade presentation/config edge; `report` accumulation is debug-only;
randomization/reassignment arithmetic and score consumers otherwise remain to be closed against
history and album controls.

Exact continuation stone:
C031-WIP577 physical -> Queue077 OPEN -> F683-F686/KI#1004-KI#1007 CONFIRMED -> F687/KI#1008
UNUSED -> finish randPoints/reset/randomization/reassignment/score-history closure -> either promote
one genuinely distinct consequential root or close Queue077 -> resolve next unskipped priority file
-> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP578 - QUEUE 077 STARTPOINTSASHANDICAP.CPP - F687 / KI#1008 CONFIRMED; F688 UNUSED
===============================================================================
Physical parent: C031-WIP577.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, current multiplayer slot/takeover contracts and reachable pre-SAS AdvC history. Gameplay/XML/
Python source untouched.

F687 / PROVISIONAL KI#1008 - TAKING OVER THE LAST SPAH AI LEAVES AN EMPTY SCORE DISTRIBUTION AND
FINAL-SCORE `stats::max` INDEXES ELEMENT ZERO
-----------------------------------------------------------------------------------------------
SPaH is disabled during initial setup when Advanced Start is absent or when every living civ is
human:

    if (!isOption(GAMEOPTION_ADVANCED_START) ||
        PlayerIter<HUMAN>::count() == PlayerIter<CIV_ALIVE>::count())
        setOption(GAMEOPTION_SPAH, false);

This is a one-time initialization gate. The option is not cleared later when controller status
changes.

`StartPointsAsHandicap::distribution()` determines AI samples dynamically from CURRENT human state:

    for(int i = 0; i < nCivs; i++)
        if(!civs[i]->isHuman())
            r.push_back(civs[i]->startPoints_actual());

That is normally nonempty because a valid SPaH game starts with at least one AI. Multiplayer,
however, explicitly supports taking over AI slots (`MPOPTION_TAKEOVER_AI`); `CvInitCore` treats
`SS_COMPUTER` as a vacant takeover candidate when that option is enabled, and slot-status changes
refresh `CvPlayer::isHuman()`.

MINIMAL SUPPORTED ACTIVATION
----------------------------
1. Start a multiplayer game with Advanced Start + SPaH, one human civ and one AI civ.
2. Because an AI exists during `initFreeState`, SPaH remains enabled and initializes normally.
3. With the standard Take Over AI MP option enabled, a second human assumes the AI slot. The former
   AI now reports `isHuman()==true`; no code re-runs the initial all-human SPaH disable gate.
4. At the end of a victorious game, final score calculation calls `getDifficultyForEndScore()`.

The SPaH branch performs:

    std::vector<int> aiStartPointDistrib;
    m_pSpah->distribution(aiStartPointDistrib); // now empty
    std::vector<scaled> arDistr;
    ...
    stats::max(arDistr)
    stats::mean(arDistr)

`stats::max` in `ArithmeticUtils.h` contains only a debug assertion before indexing:

    FAssert(!kSamples.empty());
    T r = kSamples[0];

In a release build the assertion does not provide a guard, so an empty SPaH AI distribution causes
an invalid element-0 access. `stats::mean` independently assumes nonempty input and would divide by
zero if reached.

PLAYER-VISIBLE CONSEQUENCE
--------------------------
`CvGame::getScoreComponent` calls `getDifficultyForEndScore()` whenever a score component is both
final and victory-scored. Thus this is not merely an exposed helper/API edge: the native end-game
score path itself reaches the empty-vector contract after a supported multiplayer controller
transition. Depending on build/runtime details, the invalid access can crash or produce undefined
final-score behavior.

LINEAGE
-------
Practical 1253 / commit `f6ff6a182f5187541fff6e5bbbe12f9573b3778c` introduced SPaH's
`distribution()` and the SPaH branch of `CvGame::getDifficultyForEndScore()` together. The original
code already assumed at least one AI sample and applied max/mean without an empty guard. Later
refactors moved from old double helpers to `stats::max`/`stats::mean` but retained the same
precondition. Supplied Base AdvC 1.14/current SAS still carry it; K-Mod/Civ4CE predate SPaH.

The AdvC manual explicitly says multiplayer scoring was not designed in detail; that may explain the
missing dynamic-controller case, but it does not make an invalid-access end-score path intentional.

DEDUP
-----
Whole-album search found no prior owner for the SPaH empty-distribution/takeover score path. F686
concerns save/load identity; F687 occurs without saving or loading and requires only a live controller
transition after valid SPaH initialization.

NARROW REPAIR BOUNDARY
----------------------
Make the scoring consumer robust to an empty current-AI distribution. If no AI samples remain,
return/use the ordinary non-SPaH difficulty contribution (there is no remaining AI start-point
handicap to average), or preserve an immutable initial-SPaH distribution specifically for scoring.
The former is the narrow crash-safety repair; the latter may better reflect historical handicap if
multiplayer score semantics are intentionally defined. In either case, do not call max/mean on an
empty vector.

QUEUE 077 CONTINUES FOR FINAL CLOSURE
-------------------------------------
F683-F687 / KI#1004-KI#1008 are CONFIRMED and frozen.
F688 / provisional KI#1009 is UNUSED.
Remaining checked edges are currently below threshold: `randPoints` is never set true, so hiding
randomized values omits its intended explanatory note; `report` can accumulate across resets but its
log call is disabled; randomization index/bound arithmetic and starting-plot reassignment remain
valid on shipped initialized paths. Finish history/dedup and close Queue 077 if no independent F688
survives.

Exact continuation stone:
C031-WIP578 physical -> Queue077 OPEN -> F683-F687/KI#1004-KI#1008 CONFIRMED -> F688/KI#1009
UNUSED -> final randPoints/report/randomization/reassignment/history closure -> close Queue077 if no
new consequential root -> resolve next unskipped priority file -> freeze next physical WIP.

No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP579 - QUEUE 077 CLOSED; PRIORITY QUEUE 124 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP578.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14 and complete reachable history for `StartPointsAsHandicap.cpp`. Gameplay/XML/Python source
untouched.

QUEUE 077 FINAL CLOSURE
-----------------------
`CvGameCoreDLL/StartPointsAsHandicap.cpp` is COMPLETE with exactly five current-live roots:

* F683 / KI#1004 - repeated Settings/replay string reconstruction leaks the previous owned wstring;
* F684 / KI#1005 - Regenerate Map re-enters SPaH after practical 4067 consumed the encoded setup
  value, collapsing AI points to the founding-unit minimum and disabling unequal plot reassignment;
* F685 / KI#1006 - saves serialize the uninitialized unused tail of the 96-int `allPts` block;
* F686 / KI#1007 - save rows omit PlayerTypes and are reconstructed as `0..nCivs-1`, corrupting
  SPaH metadata after load when human/AI/participating player IDs are not a human-first prefix;
* F687 / KI#1008 - multiplayer takeover of the last AI leaves the SPaH score distribution empty and
  final victorious score calculation calls `stats::max`/`stats::mean` without an empty guard.

F688 / provisional KI#1009 is NOT consumed by Queue 077.

FINAL NEGATIVE / BELOW-THRESHOLD CONTROLS
-----------------------------------------
1. `randPoints` never becomes true
   History confirms that practical 1253 introduced the serialized/display flag, practical 1395
   merely initialized it false, and no later StartPointsAsHandicap history sets it true. Therefore
   when `SPAH_RANDOMIZE>0` and `SPAH_SHOW_RANDOMIZED=0`, the Settings screen shows the configured
   pre-randomization values but omits its intended `Not shown: randomization` explanatory line.
   This is a real current optional-config presentation defect, but current defaults set
   `SPAH_SHOW_RANDOMIZED=1` and the defect neither changes points nor reveals the hidden randomized
   values. Preserve as a low-grade UI edge, not F688.

2. `report` is not cleared by `reset()`
   Regeneration can accumulate old diagnostic text, but the only report logger remains commented
   out. No live gameplay/UI consumer reads `report`; below threshold.

3. Scenario-empty-vector hypothesis REJECTED
   Although the EXE scenario sequence differs from ordinary setup, `CvGame::initScenario()` calls
   `initFreeState()`, which initializes SPaH when it remains enabled. Scenarios therefore do not
   leave `civs` empty merely because the normal outer `setInitialItems` path is skipped.

4. Randomization bounds and extrema
   The `randomizePoints`/`bounce` index pattern was checked across supported AI counts through the
   48-player limit. Chosen `i` entries always have a strictly lower configured-point pursuer and
   chosen `j` entries a strictly higher pursued neighbor under the pair construction. Zero
   `deltaMax` is accepted by the current synchronized RNG contract. No OOB/zero-range root survives.
   The algorithm preserves ordering, extrema and total points as documented.

5. Starting-plot reassignment
   Practical 3528 correctly moved `rearrangeStartingPlots()` until after starting sites exist.
   `minDist` therefore receives live starting plots on the supported setup path. Sorting weak AIs by
   points and candidate plots by distance assigns weakest nearest human starts and strongest farthest,
   matching the shipped manual. Equal-distance instability is not a synchronized gameplay defect on
   identical builds.

6. Score-vector initialization
   Normal valid SPaH initialization guarantees at least one AI because the option is disabled when
   all living civs are human. F687 separately owns the later dynamic-controller transition that can
   invalidate that precondition; do not duplicate it as a generic initialization issue.

7. Save/read `unequalDistrib`
   The flag is not serialized/reconstructed, but starting-plot reassignment is an initialization-time
   operation and is not rerun after ordinary save loading. No independent gameplay effect was found.

HISTORY COMPLETION
------------------
All 25 reachable commits touching this file were reconciled. Apart from the roots above, semantic
changes are initialization/refactoring, warning/RNG modernization, or the practical-3528 ordering fix.
Current SAS and supplied Base AdvC 1.14 remain executable-semantically identical in this file, so no
missed post-branch Base repair exists.

SCHEDULING RESOLUTION
---------------------
Per the reconciled album rule, ordinary Queue 078 onward remains marked "skip until the priority next
wip ones have been done first". The next still-pending unskipped priority entry is Queue 124:

    CvGameCoreDLL/CyGlobalContextInterface4.cpp (171 queued lines)

Queue 124 is therefore OPEN under F688 / provisional KI#1009. Start with binding-name/native-target
parity, current-vs-Base provenance, duplicate registrations, ownership/reference policies and actual
Python reachability; use the completed CyGlobalContext/CyInfo registration audits as dedup controls.

Exact continuation stone:
C031-WIP579 physical -> Queue077 CLOSED / F683-F687 confirmed / F688 unused -> Queue124
CyGlobalContextInterface4.cpp OPEN -> F688/KI#1009 UNUSED -> provenance + active binding table +
ownership/default/index/live-Python contracts -> freeze next settled WIP.

Files complete: 91 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP580 - PRIORITY QUEUE 124 CLOSED; ORDINARY QUEUE 078 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP579.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, current Python consumers and the already-completed Queue 059 CyGlobalContext implementation
audit. Gameplay/XML/Python source untouched.

QUEUE 124 CLOSURE
-----------------
`CvGameCoreDLL/CyGlobalContextInterface4.cpp` is COMPLETE with no new finding. F688 / provisional
KI#1009 remains UNUSED.

The full active Boost.Python registration table was reconstructed from the 171-line current file:
98 active `.def` registrations expose 96 unique Python names. The only duplicates are
`getNumVoteSourceInfos` and `getVoteSourceInfo`, each registered twice in immediate succession to the
same native method, same signature and same return policy. This is inherited redundant registration,
not a semantic ambiguity; current Victory/Sevopedia consumers receive the same result whichever
method object Boost resolves.

Current SAS and supplied Base AdvC 1.14 are executable-semantically identical after stripping
comments/whitespace. No SAS-only binding delta or missed post-branch Base repair exists here.

The only active Python/native spelling difference is the legacy public name
`getEntityEventType` -> native `getEntityEventTypes`. This is a singular/plural API alias, not a
wrong-family target. The other enum-string helpers bind directly to their matching native getters.

All info/art getters returning `reference_existing_object` point at globally owned GC/ArtFileMgr
records whose lifetime matches Python use. Their native implementations and index guards were
already audited in Queue 059; this registration file introduces no independent ownership or bounds
root. Shipped Python consumers of live indexed getters use paired `getNum*` loops or valid enum IDs;
VoteSource, ArtStyle, Flavor and related callers were checked explicitly.

No duplicate/misbinding/lifetime/index defect survived the finding threshold. Preserve the duplicate
VoteSource registrations only as harmless inherited cleanup debt.

SCHEDULING RESOLUTION
---------------------
Queue 124 was the final still-pending priority override left by the reconciled row annotations. It is
now closed. Return to ordinary numeric queue order at the first unfinished entry:

    Queue 078 `CvGameCoreDLL/CvVirtualWrappers.cpp`
    album queued size: 435 lines; current reconciled source: 450 physical lines.

Queue 078 is OPEN under F688 / provisional KI#1009. The file is an EXE-ABI adapter layer, so audit
priority is: current-vs-Base delta; vtable/signature preservation; pointer/null contracts at EXE
entry points; casts from base Cv* to AI subclasses; trade-list ownership and side effects; stream
read/write adapters; and whether SASGameRecord additions alter authoritative trade behavior.

Initial Queue-078 provenance already shows a small current SAS delta versus Base 1.14: the reconciled
file adds SASGameRecord logging at the EXE `AI_considerOfferExternal` and
`AI_counterProposeExternal` boundaries plus the logger include/comments. Treat those additions as
suspect only if they change state/order/lifetime; logging itself is not a finding.

Exact continuation stone:
C031-WIP580 physical -> Queue124 CLOSED / no finding / F688 unused -> Queue078
CvVirtualWrappers.cpp OPEN -> F688/KI#1009 UNUSED -> bound SAS-vs-Base delta -> verify EXE ABI,
null/list/cast/stream contracts and trade logging ordering -> whole-album/history dedup -> promote
only a distinct current-live consequential root or close Queue078 cleanly -> freeze next settled WIP.

Files complete: 92 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP581 - QUEUE 078 CLOSED; QUEUE 079 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP580.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, full reachable history for `CvVirtualWrappers.cpp`, and earlier dedicated AI-wrapper audit
controls preserved in this album. Gameplay/XML/Python source untouched.

QUEUE 078 CLOSURE
-----------------
`CvGameCoreDLL/CvVirtualWrappers.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

Current source is 450 physical lines versus the album's 435-line queue snapshot. Current SAS differs
from supplied Base AdvC 1.14 in only one semantic cluster: SASGameRecord instrumentation added by
SAS practical 6380 around `AI_considerOfferExternal` and `AI_counterProposeExternal`, plus the logger
include/comments. The underlying AI call arguments, return values, counter-list clearing order and
EXE-facing virtual signatures are unchanged.

The logging path is observational:
- level gating occurs before capture/log work;
- relation capture reads attitude/memory/war-plan/war-state only;
- no synchronized RNG call or trade-list mutation is introduced;
- `AI_considerOffer` still executes once and its original boolean is returned unchanged;
- `AI_counterPropose` still executes once after the inherited counter-list clear and its original
  boolean is returned unchanged;
- serialization of the resolved lists occurs only after the authoritative call.
No gameplay/trade-state divergence survives from the SAS delta.

All 15 reachable history commits touching this file were reconciled. Practical 1676 introduced the
adapter file when AdvC made the internal AI functions non-virtual while preserving the EXE vtable.
Later changes deliberately track internal signature evolution: null-safe area/mission adapters,
reference conversions, removed legacy arguments, ScaledNum war-success rounding, and the
AI_isControlled move. The preserved EXE signatures remain the compatibility boundary.

The main pointer/cast seams were cross-checked against both history and prior album audits:
- `AI_isDeclareWarExternal(NULL)` explicitly returns false and was already proven safe;
- mission target wrappers return 0 for NULL after their reference conversion;
- area wrappers return 0 for NULL;
- `AI_dealValExternal` substitutes an empty list for NULL;
- trade-table offer/counter list pointers are required non-null by the EXE call contract;
- city/base-to-AI casts target objects allocated as the corresponding AI subclass;
- CvPlayer/CvTeam stream wrappers simply preserve the EXE read/write vtable slots with live streams;
- selection-group compatibility shims retain intentionally ignored K-Mod-removed arguments.
No independent ABI, null-deref, cast, stream or semantic-forwarding defect survived.

Whole-album dedup is especially strong here: earlier deep CvSelectionGroupAI/CvPlayerAI audits
already used these exact external wrappers as their supported EXE producers and separately rejected
virtual-slot/signature drift. F336 owns the independent lower-level `AI_balanceDeal` leniency bug;
it must not be duplicated against the correct `AI_counterProposeExternal` producer.

QUEUE 079 OPEN
--------------
Advance in ordinary order to Queue 079 `CvGameCoreDLL/ScaledNumTest.cpp` (432 lines) under F688.
Initial provenance: current SAS and Base AdvC 1.14 are executable-semantically identical. The only
live call is from `CvXMLLoadUtilitySet.cpp` after XML loading, but `SCALED_NUM_TEST` is commented out
in the shipped source, so the compiled function immediately returns under `#ifndef
SCALED_NUM_TEST`; the large test/benchmark body is compile-disabled. Audit should still reconcile
history and ensure no macro/build configuration enables it in shipped projects before clean closure.

Exact continuation stone:
C031-WIP581 physical -> Queue078 CLOSED / no finding / F688 unused -> Queue079 ScaledNumTest.cpp
OPEN -> verify shipped macro/build state + history/test-only reachability -> close without promoting
compile-disabled diagnostics unless a supported build activates them -> freeze next settled WIP.

Files complete: 93 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP582 - QUEUE 079 CLOSED; QUEUE 080 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP581.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14 and full reachable ScaledNumTest history. Gameplay/XML/Python source untouched.

QUEUE 079 CLOSURE
-----------------
`CvGameCoreDLL/ScaledNumTest.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

Current SAS and Base AdvC 1.14 are executable-semantically identical across all 432 lines. The file's
substantial assertion/benchmark body is compile-time test scaffolding only:

    //#define SCALED_NUM_TEST
    #ifdef SCALED_NUM_TEST
        ... test-only typedefs/includes ...
    #endif

    void TestScaledNum()
    {
    #ifndef SCALED_NUM_TEST
        return;
    #else
        ... ~400 lines of tests/benchmarks ...
    #endif
    }

A whole-light-source search finds no other `SCALED_NUM_TEST` definition in C++, headers, project
files, scripts or build configuration. The one shipped call in `CvXMLLoadUtilitySet.cpp` therefore
executes only the immediate return after XML loading. RNG sampling, asserts, speed profiling and
intentional edge-value experiments in the `#else` body do not exist in the shipped executable.

All 18 reachable history commits touching the file were reconciled. They track the ScaledNum test
suite itself (rename/polishing, compile fixes, RNG API updates, fixed-point exponentiation tests,
small-int mulDiv tests and merges). No commit enables `SCALED_NUM_TEST` in a supported shipped build.
Do not promote failures or roughness inside explicitly opt-in developer test scaffolding as current
gameplay defects.

QUEUE 080 OPEN
--------------
Advance to Queue 080 `CvGameCoreDLL/CyReplayInfo.cpp` under F688. Current file is 428 physical lines
and is executable-semantically identical to Base AdvC 1.14. Audit priorities: ownership of the
embedded/default replay versus borrowed `CvReplayInfo*`; Python-created wrapper lifetime; integer to
enum/uint index conversions; replay-message/player-history bounds; map wrapper ownership; read/write
stream semantics; and whether any negative/sentinel value from shipped Python can become a huge
unsigned replay index.

Exact continuation stone:
C031-WIP582 physical -> Queue079 CLOSED / no finding / F688 unused -> Queue080 CyReplayInfo.cpp OPEN
-> constructor/borrowed ownership + index/sentinel + stream/map wrapper + Python caller/history
contracts -> dedup against CvReplayInfo/replay findings -> promote only a distinct current-live root
or close cleanly -> freeze next settled WIP.

Files complete: 94 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP583 - QUEUE 080 CLOSED; QUEUE 081 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP582.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, underlying CvReplayInfo implementation and shipped replay/Hall-of-Fame Python consumers.
Gameplay/XML/Python source untouched.

QUEUE 080 CLOSURE
-----------------
`CvGameCoreDLL/CyReplayInfo.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009 remains
UNUSED.

Current SAS and supplied Base AdvC 1.14 are executable-semantically identical across the 428-line
file. The wrapper has two ownership modes: the default constructor points `m_pHoF` at its embedded
`m_replay`, while pointer construction borrows a `CvReplayInfo` owned by `CvGame` or
`CvHallOfFameInfo`. Shipped Python obtains borrowed wrappers through `CyGame().getReplayInfo()` or
`CyHallOfFameInfo.getReplayInfo()` and consumes them while the owning game/Hall-of-Fame container is
alive; no reload/mutation path invalidates the pointer during those screens.

Replay-message index conversion is safe despite the visually sharp `(uint)i` casts. Every native
message getter calls `CvReplayInfo::isReplayMsgValid(uint)`, which first checks `i <
m_listReplayMessages.size()` and returns a sentinel for an invalid index. A negative Python integer
therefore becomes a large unsigned value that fails the range check rather than indexing memory.
Shipped Replay/Info screens additionally iterate `0..getNumReplayMessages()-1`.

Player/turn history getters stay in signed space and call `isValidPlayer` plus `isValidTurn` before
indexing. `CvReplayInfo::createInfo` creates one TurnData entry for every turn from the global replay
initial through final turn for every ever-alive player, so the shared turn-range check matches each
stored row length.

The one narrower invalid-index caveat is `getReplayMessageText`: native invalid input returns NULL,
which would be unsuitable for arbitrary std::wstring assignment. No shipped caller supplies an
invalid message index, while all normal message getters are count-bounded. Preserve this only as
public-extension hardening, not F688.

The implicit-copy concern is likewise non-live: Python assignment retains the same Boost object;
shipped bindings return newly allocated pointer wrappers rather than CyReplayInfo by value. The
underlying CvReplayInfo copy constructor intentionally reports unsupported copying and no shipped
producer invokes it.

All six reachable history strata for this wrapper were reconciled (initial implementation, build
project changes, replay-minimap addition, enum refactor and ancestry merge). No missed semantic
repair or SAS regression exists.

QUEUE 081 OPEN
--------------
Advance to Queue 081 `CvGameCoreDLL/CvInfo_Tech.cpp` under F688. Start with current-vs-Base
provenance and XML/default inheritance, then audit prereq-array construction, era/commerce/domain
modifier element storage, sound/button/text fields, TechInfo destruction/copy contracts and all SAS
tech-tree additions against current XML counts.

Exact continuation stone:
C031-WIP583 physical -> Queue080 CLOSED / no finding / F688 unused -> Queue081 CvInfo_Tech.cpp OPEN
-> provenance + XML/default/copy/destructor + prereq-array/count + generic-element/domain contracts
-> caller/history/dedup -> promote only a distinct current-live root or close cleanly -> next WIP.

Files complete: 95 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP584 - QUEUE 081 CLOSED; QUEUE 083 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP583.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, K-Mod 1.46 oracle and current TechInfos XML. Gameplay/XML/Python source untouched.

QUEUE 081 CLOSURE
-----------------
`CvGameCoreDLL/CvInfo_Tech.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009 remains
UNUSED.

Current SAS and supplied Base AdvC 1.14 are executable-semantically identical across the full
425-line file. All 19 reachable history commits were reconciled: file split, XML-default framework,
prerequisite-vector conversion, XML-cache compile fixes, enum-map rewrite, restored commerce loaders,
K-Mod tech-commerce import and later minor refactors. No post-branch Base repair is missing.

Current XML contains 92 TechInfo records. The largest OrPreReqs list has 2 entries and the largest
AndPreReqs list has 3; these remain within the configured legacy Python display limits while the
native vectors themselves can safely store the actual sibling count. Current `CommerceFlexible`,
`DomainExtraMoves`, `TerrainTrades`, `Flavors` and `SpecialistExtraCommerces` child counts stay within
the corresponding fixed enum/list domains. `SetCommerceArray` and `SetYieldArray` restore the XML
cursor internally as designed, so the sequential child loads do not nest accidentally.

One real but below-threshold inherited AdvC defect was preserved explicitly:

    int CvTechInfo::getSpecialistExtraCommerce(int i) const
    {
        FAssertBounds(0, GC.getNumFlavorTypes(), i); // wrong domain
        return m_piSpecialistExtraCommerce ? m_piSpecialistExtraCommerce[i] : 0;
    }

K-Mod correctly checks `NUM_COMMERCE_TYPES`. The backing array and cache read/write paths are also
correctly sized to `NUM_COMMERCE_TYPES`, and every shipped native/Python caller supplies commerce
indices. Current flavor count is not the storage domain, but the wrong assertion therefore neither
rejects a valid shipped index nor changes release behavior. An out-of-domain extension caller could
still escape the intended debug guard and index past the commerce array; preserve as API/debug
hardening, not F688.

Destructor ownership is symmetric for all six allocated arrays. XML-cache write/read uses matching
sizes and freshly allocated arrays; prereq vectors are size-prefixed and freshly constructed on
normal cache load. No live leak/OOB/stale-default/cache root survives.

QUEUE 082 `CvFractal.cpp` was already completed at C031-WIP493 / F651, so advance directly to the
next unfinished ordinary entry, Queue 083 `CvGameCoreDLL/FProfiler.cpp` (403 lines), under F688.
Audit runtime enable/disable macros, singleton/profile-node ownership, stack nesting/reentrancy,
counter/time arithmetic, DLL shutdown/destruction order and any file/log buffer behavior before
classifying profiler-only defects.

Exact continuation stone:
C031-WIP584 physical -> Queue081 CLOSED / no finding / F688 unused -> Queue082 already complete ->
Queue083 FProfiler.cpp OPEN -> provenance + compile/runtime activation + ownership/nesting/time/log
contracts -> history/dedup -> promote only a current-live consequential root or close cleanly.

Files complete: 96 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP585 - QUEUE 083 CLOSED; QUEUE 084 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP584.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260909T154245.zip`, supplied Base AdvC
1.14, current Makefile.project/code-editor project configuration and profiler call sites.
Gameplay/XML/Python source untouched.

QUEUE 083 CLOSURE
-----------------
`CvGameCoreDLL/FProfiler.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009 remains
UNUSED.

Current SAS and supplied Base AdvC 1.14 are executable-semantically identical across all 403 lines.
The internal-profiler body contains several genuine development-path rough edges, but the normal DLL
build configuration does not activate them:

- `CvGameCoreDLL/Project/Makefile.project` documents `/DUSE_INTERNAL_PROFILER` and
  `/DUSE_TSC_PROFILER` as optional flags and leaves them disabled. The default Profile flags add only
  `/DPROFILE_AI_AUTO_PLAY`.
- Without `USE_INTERNAL_PROFILER`, no `FP_PROFILE_ENABLE` is implied; `GC.isDLLProfilerEnabled()`
  therefore returns false and the external Firaxis profiler begin/end calls are skipped.
- The Visual Studio `.vcxproj` code-editor configuration does define both profiler macros, explaining
  why an alternate development build can activate the internal implementation. That is not the
  ordinary Makefile DLL configuration used for shipped SAS builds.

Within the opt-in internal path, preserve these negative/development controls rather than findings:
1. every `IFPPythonCall` decrements `pythonDepth` while the matching increments are commented out;
   the variable is only referenced by those same commented trace strings, so this has no live
   consequence even in the opt-in profiler;
2. the 900-sample hard cap has a weak begin/end failure path, but the current source contains fewer
   than 900 distinct PROFILE/PROFILE_FUNC sites and ordinary call depth is far below 900;
3. `DETAILED_TRACE` is compiled in under the internal profiler but runtime-disabled through
   `detailedTraceEnabled=false` unless explicitly enabled;
4. timing/log arithmetic is profiler output only and does not feed synchronized game state.

No current gameplay, save, UI, network or normal-release performance root survives. Do not promote
opt-in profiler diagnostics merely because the alternate code-editor project can compile them.

QUEUE 084 OPEN
--------------
Advance to Queue 084 `CvGameCoreDLL/CyUnitInterface1.cpp` (389 lines) under F688. Use the earlier
CyUnit implementation audits as dedup controls, then reconstruct the active registration table,
current-vs-Base delta, duplicate names, native target parity, ownership policies and live Python
callers. Pay special attention to unit/plot pointers, combat/movement enum adapters and any
manage_new_object returns.

Exact continuation stone:
C031-WIP585 physical -> Queue083 CLOSED / no finding / F688 unused -> Queue084 CyUnitInterface1.cpp
OPEN -> provenance + registration/native-target + ownership + live Python/index contracts -> dedup
against CyUnit/CvUnit findings -> promote only a distinct consequential registration-layer root or
close cleanly.

Files complete: 97 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP586 - QUEUE 084 CLOSED; QUEUE 085 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP585.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, K-Mod 1.46, Civ4CE and the already-completed Queue-032 CyUnit wrapper audit. Gameplay/XML/
Python source untouched.

QUEUE 084 CLOSURE
-----------------
`CvGameCoreDLL/CyUnitInterface1.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

Current SAS and supplied Base AdvC 1.14 are line-for-line semantically identical across the active
389-line Boost.Python registration table. There is therefore no SAS-only registration regression and
no newer Base-AdvC repair missing from SAS.

The registration layer was reconciled mechanically against current `CyUnit.h` / `CyUnit.cpp` and the
prior Queue-032 audit:

- all 321 active `.def` public names are unique;
- every active public name points to the same-named `CyUnit` method; there is no surviving copy/paste
  misbinding like the old K-Mod/Civ4 `canRecon -> canParadrop` registration, which AdvC practical
  advc.001 corrected to the distinct `canParadrop` / `canParadropAt` names;
- all eight Cy-wrapper pointer returns (`CyPlot`, `CyCity`, `CyUnit`, `CySelectionGroup`, `CyArea`)
  use `manage_new_object`, matching the wrapper implementations that allocate a fresh Cy object;
- `getArtInfo` is the sole direct engine-info pointer return and correctly uses
  `reference_existing_object`, matching its non-owning `CvArtInfoUnit` lifetime;
- the active table contains no duplicate Python names that silently overwrite or shadow another
  binding;
- the Queue-032 wrong-method, enum-cast and shipped-caller census remains authoritative for the
  underlying wrapper behavior, so the registration pass did not reopen any of those surfaces.

Several inherited help/doc strings are stale, but the C++ function pointers expose the real Boost
signature and shipped Python uses those real signatures. Examples include `canLoad`, `canNuke`,
`canRecon`, `canParadrop` and `canAirBomb` being documented as zero-argument calls despite taking a
`CyPlot*`; `promote` is documented as a one-argument bool-returning call although the wrapper is a
void two-argument call; `cargoSpaceAvailable`, `setXY`, `setExperience` and
`getExtraDomainModifier` likewise omit required parameters in the help text. Current shipped callers
that exercise these surfaces pass the required arguments (e.g. PLE calls `promote(iPromo, -1)` and
`setXY(x,y,false,true,false)`, while WorldBuilder load calls `setExperience(value,-1)`). These are
real API-documentation defects, but not gameplay/runtime roots and therefore stay below the finding
threshold rather than consuming F688.

The inherited auxiliary-pointer NULL-hardening inconsistencies also remain owned by the Queue-032
closure: the binding layer does not itself manufacture NULL arguments, and no supported shipped
caller was found to activate those sharp wrappers with `None` or an invalid Cy object. No separate
registration-layer root survives.

QUEUE 085 OPEN
--------------
Advance to Queue 085 `CvGameCoreDLL/FFreeListTrashArray.cpp` (375 lines) under F688. Current SAS and
Base AdvC 1.14 differ in this file, so start by classifying that delta before relying on Base as an
oracle. Audit slot/free-list invariants, ID/index packing and wraparound, add/remove/load/grow behavior,
iterator contracts, stream read/write reconstruction, ownership/destruction and the explicit
`FLTA_MAX_BUCKETS` ceiling. Then reconcile all concrete template instantiations and savegame callers
before promoting any container-level defect.

Exact continuation stone:
C031-WIP586 physical -> Queue084 CLOSED / no finding / F688 unused -> Queue085
FFreeListTrashArray.cpp OPEN -> current-vs-Base delta first -> slot/free-list + ID packing +
add/remove/load/grow + stream reconstruction + concrete instantiations/save callers -> whole-album
dedup -> promote only a demonstrated consequential root or close cleanly -> next WIP.

Files complete: 98 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP587 - QUEUE 085 CLOSED; QUEUE 086 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP586.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, K-Mod 1.46, Civ4CE, Taurus and all concrete current FLTA instantiations/save callers.
Gameplay/XML/Python source untouched.

QUEUE 085 CLOSURE
-----------------
`CvGameCoreDLL/FFreeListTrashArray.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

After normalizing representation, current SAS and supplied Base AdvC 1.14 are executable-semantically
identical; SAS only reformats the two stream-helper signatures. K-Mod 1.46, Civ4CE and Taurus retain
the same underlying Firaxis free-list design, including the sharp inherited controls below.

Container invariants reconcile on valid runtime/save paths:

- slot counts are rounded to powers of two, normal containers start at 8 slots, and internal growth
  doubles only while the result remains within `FLTA_MAX_BUCKETS = 8192`;
- low 13 ID bits encode the physical slot while the upper bits carry a generation; normal full-ID
  lookup therefore rejects stale objects after a freed slot is reused;
- `remove(T*)` deliberately calls `removeAt(i)` with a generationless physical index, and
  `removeAt` deliberately accepts such index-only IDs; external object-ID deletion paths pass full
  generation-bearing IDs;
- removal links the physical slot into the free list without reducing `m_iLastIndex`, while
  `getCount = lastIndex - freeCount + 1` and the forward/reverse iterators skip holes correctly;
- `removeAll` deletes every owned object and clears physical/free-list state while preserving the
  generation counter; a fresh `init` intentionally resets the generation sequence, a behavior already
  accounted for by prior album findings involving player-slot/container reinitialization;
- `growArray` copies occupied pointers and free-list links verbatim, initializes only the new tail,
  and deletes only the old node array, not the pointed-to objects;
- all current game/player/map persistence uses `ReadStreamableFFreeListTrashArray` /
  `WriteStreamableFFreeListTrashArray`; the raw non-streamable `Read`/`Write` variant has no current
  game-state caller;
- the streamable writer persists slot count, last index, free-list head/count, current generation,
  every next-free link and every live object's own serialized ID; the reader restores that metadata
  before placing freshly read objects back into the slot selected by their ID.

One inherited coding defect is real but not activated by supported current state:

    FAssertMsg(m_iNumSlots <= FLTA_MAX_BUCKETS,
               "FFreeListTrashArray::init() size too large");

inside `init(int iNumSlots)` checks the OLD member `m_iNumSlots` rather than the rounded requested
`iNumSlots`. K-Mod, Civ4CE and Taurus contain the same wrong-variable assertion. A direct API call or
corrupt/malicious save requesting more than 8192 slots could therefore bypass the intended debug
guard and allocate a container incompatible with the 13-bit slot encoding. Normal current callers
invoke default `init()` and valid saves can only contain slot counts previously produced by the same
bounded growth logic, so no shipped supported path reaches that state. Preserve as malformed-save /
extension hardening, not F688.

Other sharp edges remain below threshold for the same reason: `getNextFreeIndex` /
`setNextFreeIndex` do not reject negative indices but current callers use `0..numSlots-1`; stream
metadata is trusted rather than hardened against arbitrary corruption; and `m_iCurrentID` would
ultimately overflow a signed int after roughly 262,143 successful additions to one container without
reinitialization, far beyond demonstrated current-game object churn. The intentional 8192
simultaneous-object ceiling can return NULL; city/unit creation already checks that result, while
other container callers assume the designed ceiling is not reached. No shipped map/game fixture was
found that approaches the per-container cap.

Whole-album dedup also matters here: prior findings already rely on FLTA generation semantics,
immediate deletion and generation reset/reuse across player-container reinitialization. This queue
does not create a new independent root behind those findings.

QUEUE 086 OPEN
--------------
Advance to Queue 086 `CvGameCoreDLL/CvXMLLoadUtility.cpp` (374 lines) under F688. Current SAS and Base
AdvC 1.14 are exactly identical, so there is no missed-upstream delta to classify first. Audit FXml
creation/destruction and error ownership, constructor/reset state, mask parsing, sibling/child cursor
movement, unknown-type handling, file-load failure paths, string-list allocation, progress callback
and `MapChildren`; reconcile these helpers against the already-completed XML Set/Get/Info queues so
that XML-cursor or allocation roots are not duplicated.

Exact continuation stone:
C031-WIP587 physical -> Queue085 CLOSED / no finding / F688 unused -> Queue086
CvXMLLoadUtility.cpp OPEN -> FXml lifetime + reset/mask + cursor traversal + unknown-type/error +
LoadCivXml + SetStringList + MapChildren -> cross-check concrete XML callers and prior XML findings ->
promote only a distinct current-live root or close cleanly -> next WIP.

Files complete: 99 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP588 - QUEUE 086 CLOSED; QUEUE 087 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP587.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, K-Mod 1.46, Civ4CE, Taurus, current shipped XML and the completed Queue-028/052 XML Set/Get
passes. Gameplay/XML/Python source untouched.

QUEUE 086 CLOSURE
-----------------
`CvGameCoreDLL/CvXMLLoadUtility.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

Current SAS and supplied Base AdvC 1.14 are exactly identical in this 374-line utility, so there is
no SAS-only regression and no newer Base-AdvC repair missing from the file. The retained Firaxis/
AdvC helper contracts were reconciled against their current callers and the previously audited XML
Set/Get code.

The FXml lifetime is balanced on supported loader paths. `CreateFXml` stores one parser object tied to
the long-lived schema cache; the concrete setup/read routines either destroy it through
`DestroyFXml` or call the same XML-interface destroy operation directly after the load attempt.
`DestroyFXml(FXml*&)` is an engine-interface by-reference contract. The constructor owns one schema
cache and the destructor releases it once. The two in-game reset helpers delete and clear the old
Landscape/Effect info vectors before repopulating them; no current C++ caller of those reset helpers
exists outside the utility itself, so they do not expose a present reload-time iterator/lifetime
root.

Cursor helpers reconcile with the Queue-052 Get pass. `SkipToNextVal` advances only while the current
node is a comment and reports failure when no non-comment sibling remains. `FindInInfoClass` /
`getGlobalEnumFromString` preserve the underlying `-1` result and add diagnostics rather than turning
an unknown XML type into a valid enum. `LoadCivXml` updates `GC.currentXMLFile` only after a successful
load; on failure the diagnostic can therefore mention the previously successful file, but this is
error-reporting quality rather than game-state corruption.

`SetStringList` received an explicit current-data comment stress test because it sizes with
`GetNumChildren` but reads values with `GetChildXmlVal` / `GetNextXmlVal`, which skip comments.
Current shipped data contains direct comments inside live string-list parents: the Handicap `Goodies`
lists alone contain 34 direct comments across 11 lists, several civilization `Cities` lists contain
comments, and one diplomacy-text list does too. Supplied Base AdvC 1.14 also ships direct comments in
its Handicap `Goodies` lists while retaining this identical helper. Thus comment-bearing lists are a
normal engine-supported fixture, not malformed-data theory. The proprietary XML-interface
implementation is not present in the supplied SDK sources, so its exact `GetNumChildren` internals
cannot be proved locally; however, no supported runtime symptom establishes that it counts comment
nodes as list entries, and the long-standing identical Firaxis/AdvC helper plus comment-bearing
shipped data is a strong negative control. Do not promote a hypothetical comment-count mismatch
without engine/runtime evidence.

The remaining sharp surfaces stay below threshold and are largely diagnostic/hardening:

- `logMsg` uses a fixed 2048-byte `_vsnprintf` buffer without an explicit terminator on truncation;
  current loader log strings and paths are far shorter than the cutoff;
- `handleUnknownTypeString` and `LoadCivXml` use fixed `sprintf` buffers (1024 and 256 bytes
  respectively), but current XML type names and shipped relative paths do not approach them;
- `CreateFXml` returns true after a non-throwing NULL engine allocation, but no normal supported
  engine path producing such a parser object was found; this is allocation/engine-failure hardening;
- `SetStringList` returns true even when value traversal fails and trusts the engine child count, so a
  malformed/custom list can leave default strings in the allocated tail; current shipped lists parse
  successfully and malformed XML handling is not a new gameplay root;
- the old hex-mask parser is compiled out under `#if 0`, and `UpdateProgressCB` is intentionally inert
  because its registration path was removed.

Whole-album dedup leaves the already-confirmed XML roots with their existing owners. In particular,
Queue-028 owns the Set-side allocator defect (F657), while Queue-052/F680 owns the live oversized XML
string read through a common 256-byte temporary. Nothing in this central helper creates a distinct
second root behind either issue.

QUEUE 087 OPEN
--------------
Advance to Queue 087 `CvGameCoreDLL/CyMap.cpp` (348 lines) under F688. Reuse the completed Queue-036
`CvMap.cpp` audit as the native contract oracle. Start with current-vs-Base provenance, then reconcile
every Python wrapper with `CvMap` signatures, ownership of returned `CyPlot` / `CyCity` / area-like
objects, coordinate/index conversion, random-plot and path-distance adapters, map-script custom
options, world-size/latitude access and any NULL sentinel translation. Cross-check shipped Python
callers before treating weak validation as live.

Exact continuation stone:
C031-WIP588 physical -> Queue086 CLOSED / no finding / F688 unused -> Queue087 CyMap.cpp OPEN ->
current-vs-Base + wrapper/native parity + pointer ownership + coordinate/index/path/random/custom-map
contracts -> shipped Python callers + Queue036 dedup -> promote only a distinct consequential wrapper
root or close cleanly -> next WIP.

Files complete: 100 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP589 - QUEUE 087 CLOSED; QUEUE 088 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP588.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14/Civ4CE/Taurus, completed Queue-036 CvMap audit, current CyMap Boost.Python registration and all
shipped Python/map-script callers. Gameplay/XML/Python source untouched.

QUEUE 087 CLOSURE
-----------------
`CvGameCoreDLL/CyMap.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009 remains UNUSED.

Current SAS and supplied Base AdvC 1.14 have exactly the same executable token stream; their
three-line physical-length difference is representation/comment formatting only. There is no
SAS-only wrapper regression or missed newer Base-AdvC repair.

Every native adapter was reconciled against the completed Queue-036 `CvMap` contracts and the active
`CyMapInterface.cpp` ownership policy:

- managed `plot`, `plotByIndex`, `pointToPlot`, `getArea`, `findCity`, `findSelectionGroup`,
  `findBiggestArea` and `syncRandPlot` results allocate a fresh Cy wrapper and the binding uses
  `manage_new_object`; a NULL native result therefore becomes an ordinary `Cy*.isNone()` wrapper;
- `sPlot` and `sPlotByIndex` intentionally reuse one process-static `CyPlot` and are bound with
  `reference_existing_object`; this is a sharp aliasing contract, not an ownership mismatch;
- `plotByIndex` inherits CvMap's explicit bounds check and safely wraps NULL for an invalid index;
  `plot` inherits the native coordinate/wrapping contract; `getArea` inherits FLTA's NULL-on-missing
  behavior;
- `calculatePathDistance` ultimately accepts NULL native plots and returns -1, although the Cy layer
  itself dereferences its two wrappers first; all shipped map-script callers feed valid map plots;
- `rebuild`'s raw custom-option pointer is exercised by WorldBuilder with the coherent `(0, None)`
  pair, and the following explicit `recalculateAreas()` matches the already-audited WB/CvMap load
  sequence;
- current custom-map-option consumers use script-defined option indices, and the Victory screen
  iterates `0..getNumCustomMapOptions()-1` before requesting non-default descriptions;
- plot-extra-yield/cost wrappers first validate coordinates through `CvMap::plot` and turn invalid
  coordinates into a zero/no-op result rather than dereferencing NULL;
- `getSettingsString` returns by value and delegates replay/settings construction to code already
  covered by the ReplayInfo audits.

The strongest apparent hazard is already-owned KI#257. `sPlot()` really does retarget one static
wrapper on every call, so two simultaneous Python references alias. KI#257 established an actual
Planet/C2C feature-spread failure from retaining a source `sPlot` while fetching a neighbor with a
second `sPlot`. Current Planet Generator now retains that source through independent managed
`map.plot(...)`, and a shipped-caller census shows every other active Python/map-script file contains
only one `sPlot`/`sPlotByIndex` call site. No second current alias activation survives, so do not
renumber the static-wrapper mechanism as F688.

The remaining Cy-pointer validation gaps are extension/API hardening rather than current gameplay
roots. `findCity` unconditionally dereferences `pSkipCity` even though its help string describes a
NULL default; `findWater` and `updateMinOriginalStartDist` similarly assume non-NULL wrappers, and
`calculatePathDistance` assumes non-NULL Cy wrapper objects before reaching the native NULL guard.
No shipped Python caller invokes the first three helpers, and all shipped path-distance calls supply
valid plots. The Boost binding also does not actually declare the textual `findCity` defaults, so its
help string is misleading documentation rather than a live default-argument path.

`plotX` / `plotY` intentionally perform raw index arithmetic and assume an initialized nonzero map
width; current shipped savemap callers obtain their indices from valid map/starting plots. Invalid
or pre-map Python indices remain API hardening. Enum casts and map-mutating helpers likewise add no
new validation regression beyond their already-audited native contracts.

QUEUE 088 OPEN
--------------
Advance to Queue 088 `CvGameCoreDLL/CvInfo_Organization.cpp` (342 lines) under F688. Establish
current-vs-Base/Civ4CE provenance first, then audit Religion/Corporation info construction,
destruction/read allocation, prerequisite arrays, spread factors, sound/button/text fields and any
SAS organization changes against XML schema/current data. Reconcile inherited fixed-size enum loops
and allocation ownership with the completed `CvInfo_Misc`, `CvInfo_Civics`, `CvInfo_Base` and XML
Set/Get queues before promoting anything.

Exact continuation stone:
C031-WIP589 physical -> Queue087 CLOSED / no finding / F688 unused -> Queue088
CvInfo_Organization.cpp OPEN -> provenance + Religion/Corporation allocation/read/destruction + XML
array sizes/schema/current data + getter/index contracts -> prior CvInfo/XML dedup -> promote only a
distinct consequential root or close cleanly -> next WIP.

Files complete: 101 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP590 - QUEUE 088 CLOSED; QUEUE 090 OPEN - F688 / KI#1009 UNUSED
===============================================================================
Physical parent: C031-WIP589.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, current Religion/Corporation XML + schema, completed Queue-028 XML Set pass and Queue-055
CyInfoInterface3 binding audit. Gameplay/XML/Python source untouched.

QUEUE 088 CLOSURE
-----------------
`CvGameCoreDLL/CvInfo_Organization.cpp` is COMPLETE with no new finding. F688 / provisional KI#1009
remains UNUSED.

After stripping comments/formatting, current SAS differs from supplied Base AdvC 1.14 by exactly one
small executable addition: `CvCorporationInfo::py_getBonusProduced()`, the SAS Python bridge that
returns the already-existing `m_eBonusProduced` enum as an int. Queue-055 already audited its
Boost.Python registration and shipped Corporation/Policy UI consumers; it is scalar, correctly bound,
and introduces no indexed-memory or ownership change. No Base-1.14 repair is missing.

Religion/Corporation object lifetime and current XML dimensions reconcile:

- constructors initialize every enum/scalar sentinel and every owned array pointer;
- destructors use `SAFE_DELETE_ARRAY` for the three Religion and three Corporation fixed enum arrays;
- each present religion commerce block is read through the NUM_COMMERCE_TYPES helper and each absent
  block is explicitly initialized to the same size; Corporation headquarters/commerce/yield arrays
  follow the analogous fixed enum dimensions;
- Queue-028 already owns the generic `SetCommerce`/`SetYields` comment/count and oversized-list
  hardening analysis. Current Organization XML does contain inline comments in some corporation
  commerce/yield lists, but its actual value counts fit the fixed enum sizes and that queue's engine
  comment-count negative control remains authoritative;
- current `PrereqBonuses` lists contain 1..5 valid BonusType values and no direct comment children;
  the dynamic `m_aePrereqBonuses` vector therefore removes the old fixed-array padding problem and all
  native C++ consumers iterate `getNumPrereqBonuses()` rather than a stale global maximum;
- Python compatibility callers that still iterate `NUM_CORPORATION_PREREQ_BONUSES` use
  `py_getPrereqBonus`, which returns `NO_BONUS` for negative or out-of-vector indices rather than
  indexing the vector;
- current Religion/Corporation XML contains no `*_DEFAULTS` record, and shipped
  `AdvCiv-SAS.ini` has `ModularLoading = 0`. Thus the generic XML-default copy-constructor concerns
  already catalogued in Queue-028 do not create a current Organization activation.

The shared Organization base reads TechPrereq, FreeUnitClass, spread factor and movie/sound strings
through already-audited XML helpers. Current schema requires the relevant Religion/Corporation tags,
and current references resolve to live types. Mission and symbol characters are populated later by
the established global setup path rather than left as gameplay XML omissions.

`CvReligionInfo::getButtonDisabled()` retains the inherited process-static 512-byte scratch buffer and
constructs a `_D.dds` sibling by replacing the source `.dds` suffix. Current Religion `<Button>` paths
are direct file paths only 41..45 characters long, so neither the unbounded copy nor atlas-string
misinterpretation activates. Its shipped Policy Advisor caller crosses the Python binding, where the
returned C string is converted for the immediate screen call before the next religion lookup. Keep
long custom paths / simultaneous native retention as API robustness, not F688.

The fixed commerce/yield getter assertions likewise sit behind enum-bounded native callers. The
Python-facing prerequisite getter has its explicit runtime guard. No current double-free, shallow
copy, out-of-bounds enum read, bad produced-bonus bridge or unresolved XML reference survives the
pass.

QUEUE 090 OPEN
--------------
Queue 089 `CvGameAI.cpp` was already completed at C031-WIP494, so ordinary progression advances to
Queue 090 `CvGameCoreDLL/CyGameInterface.cpp` (330 lines) under F688. Reuse the completed Queue-046
`CyGame.cpp` implementation audit as the native-wrapper oracle. Reconstruct all Boost.Python names,
targets, return policies and help signatures; compare SAS/Base; then cross-check SAS-added game
methods, pointer ownership, enum adapters and shipped Python callers. Dedup any stale help-only issue
from the analogous CyUnit/CyMap interface closures.

Exact continuation stone:
C031-WIP590 physical -> Queue088 CLOSED / no finding / F688 unused -> Queue089 already COMPLETE ->
Queue090 CyGameInterface.cpp OPEN -> SAS/Base registration delta + duplicate/name-target parity +
return ownership + CyGame native contract + shipped callers -> whole-album dedup -> promote only a
distinct consequential binding root or close cleanly -> next WIP.

Files complete: 102 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP591 - QUEUE 090 OPEN - F688 / KI#1009 CONFIRMED; F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP590.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, current shipped Python/UI/config, AdvC reachable-history diff cache and prior album ownership
findings. Gameplay/XML/Python source untouched.

QUEUE 090 INTERIM FINDING
-------------------------
Queue 090 `CvGameCoreDLL/CyGameInterface.cpp` remains OPEN. F688 / provisional KI#1009 is now
CONFIRMED. F689 / provisional KI#1010 is UNUSED.

F688 / provisional KI#1009 - CONFIRMED - ADVC PYTHON WRAPPER OWNERSHIP LEAK
----------------------------------------------------------------------------
Summary:
AdvC's unit-cycling Python API allocates a fresh `CyUnit` wrapper on every successful
`CyGame::getNextUnitInCycle` call, but `CyGameInterface.cpp` registers that pointer with Boost.Python
as `reference_existing_object`. The policy therefore treats the returned `CyUnit*` as borrowed and
does not delete the independently allocated wrapper when its Python proxy dies. Each successful call
leaks one heap-allocated CyUnit wrapper.

Current implementation:

    CyUnit* CyGame::getNextUnitInCycle(bool bForward, bool bWorkers)
    {
        CvUnit* pUnit = GC.getGame().getCycleButtonUnit(bForward, bWorkers);
        if (pUnit == NULL)
            return NULL;
        return new CyUnit(pUnit);
    }

Current binding:

    .def("getNextUnitInCycle", &CyGame::getNextUnitInCycle,
         python::return_value_policy<python::reference_existing_object>(),
         "CyUnit* (bool bForward, bool bWorkers)")

This is the inverse ownership mistake from the already-owned KI#480 queue-data defect. KI#480 used
`manage_new_object` on borrowed interior `MissionData`/`OrderData` storage and could therefore delete
memory it did not own. F688 instead uses a non-owning policy on storage that was explicitly allocated
with `new`, so the allocation loses its owner.

LIVE SHIPPED ACTIVATION
-----------------------
This is not a dormant console/mod API edge. Current shipped `CvMainInterface.py` calls the wrapper
from `updateUnitCyclingButtons` whenever the SelectionButtons dirty path redraws the ordinary unit
cycling controls:

    pNextUnit = gc.getGame().getNextUnitInCycle(True, False)

and, when both buttons are enabled and the next unit is not itself a worker:

    pNextWorker = gc.getGame().getNextUnitInCycle(True, True)

`updateSelectionButtons()` calls `updateUnitCyclingButtons`, and the main-interface update path invokes
`updateSelectionButtons()` whenever `SelectionButtons_DIRTY_BIT` is set.

The shipped BUG option does not hide the problem behind an unusual configuration. `Unit Cycling
Buttons` defaults to value 1 (`Units`), not `None`; therefore the ordinary default UI requests at
least the general next-unit wrapper whenever a suitable next unit exists. The `Workers` choice can
request a second wrapper on the same refresh. Local Python variables release their proxies after use,
but `reference_existing_object` deliberately does not take ownership of the pointed-to C++ object, so
that proxy lifetime does not free the fresh `CyUnit`.

DETERMINISTIC OWNERSHIP PROOF / INTERNAL ORACLE
-----------------------------------------------
The mismatch is unusually local:
- `CyUnit` is a wrapper object around a `CvUnit*`; `getNextUnitInCycle` creates the wrapper itself with
  `new CyUnit(pUnit)`.
- Every other current `CyGame.cpp` `return new Cy...` site is paired with
  `python::return_value_policy<python::manage_new_object>()`:
  `getHolyCity`, `getHeadquarters`, `getDeal`, `addDeal` and `getReplayInfo`.
- Other interfaces likewise use `manage_new_object` for fresh `CyUnit` wrappers, e.g.
  `CySelectionGroup::getHeadUnit/getUnitAt`, `CyPlot::getBestDefender/getSelectedUnit/getUnit`,
  `CyPlayer::initUnit/getUnit`, and the global `getUnit` helper.
Thus the unit-cycling registration is the isolated ownership-policy outlier, not a project-wide
convention that deliberately keeps Cy wrappers process-lifetime.

ANCESTRY
--------
AdvC reachable-history practical 2820 / commit `f03c305991...` ("Unit cycling buttons") introduced
the wrapper and its Python registration in the same change. The implementation already returned
`new CyUnit(pUnit)`, while the newly added binding already used `reference_existing_object`. The
ownership defect therefore begins with the feature's AdvC introduction rather than with later SAS
work.

Supplied Base AdvC 1.14 retains the same implementation and the same non-owning binding unchanged.
K-Mod/Civ4CE/Taurus do not contain this AdvC unit-cycling wrapper, so they do not provide an earlier
root for this particular API.

WHOLE-ALBUM DEDUP
-----------------
No existing finding owns this leak:
- KI#480/F157 is the opposite policy-direction error on queue-owned POD pointers;
- the previous Queue-046 `CyGame.cpp` audit correctly established the native wrapper's NULL behavior
  and live callers, but did not audit the separate Boost.Python ownership annotation and therefore
  did not own this cross-file binding root;
- F683/KI#1004 is an unrelated Settings/SPaH display-string allocation leak;
- no earlier finding records `getNextUnitInCycle`, `reference_existing_object` on a freshly allocated
  CyUnit, or this default SelectionButtons redraw chain.

REPAIR BOUNDARY
---------------
The narrow repair is to make the existing allocation contract truthful:

    .def("getNextUnitInCycle", &CyGame::getNextUnitInCycle,
         python::return_value_policy<python::manage_new_object>(),
         "CyUnit* (bool bForward, bool bWorkers)")

The native body should continue returning an independent CyUnit wrapper. Switching the body to a
static/borrowed wrapper would import the aliasing/lifetime problems already documented for static
CyPlot wrappers and is unnecessary. The neighboring fresh-object bindings provide the direct local
pattern to follow.

No gameplay behavior, unit ownership or underlying CvUnit lifetime is changed by this repair; only
the temporary Python wrapper object is correctly reclaimed.

QUEUE 090 CONTINUATION
----------------------
Do not close Queue 090 merely because F688 is confirmed. Continue the registration-layer audit under
F689: finish per-class duplicate/name-target checks, every pointer/reference return policy, CyDeal
trade-item lifetime, SAS-added binding arity/return types, stale help-string separation, and
shipped-caller parity against the already-completed Queue-046 CyGame implementation audit. Promote
only a second independent consequential binding root.

Exact continuation stone:
C031-WIP591 physical -> Queue090 OPEN -> F688/KI#1009 CONFIRMED (AdvC practical-2820
getNextUnitInCycle fresh-CyUnit / reference_existing_object leak; default shipped SelectionButtons
activation) -> F689/KI#1010 UNUSED -> finish remaining CyGame + CyDeal registration ownership,
name-target/arity/help and shipped-caller sweep -> whole-album dedup -> either promote a distinct
F689 or close Queue090 cleanly -> freeze next WIP.

Files complete: 102 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP592 - QUEUE 090 CLOSED; QUEUE 091 OPEN - F688 / KI#1009 CONFIRMED; F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP591.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, current shipped Python/UI/config, Queue-046 `CyGame.cpp` native-wrapper audit, current
`CyDeal.cpp/.h`, AdvC/SAS reachable commit-diff mirror and prior album ownership findings.
Gameplay/XML/Python source untouched.

QUEUE 090 CLOSURE
-----------------
`CvGameCoreDLL/CyGameInterface.cpp` is COMPLETE for this pass. F688 / KI#1009 remains CONFIRMED.
No independent F689 / provisional KI#1010 was promoted.

REGISTRATION TABLE / SAS-BASE PARITY
------------------------------------
A mechanical registration census finds 262 current `.def` name-target pairs and 261 distinct names.
The only repeated Python name is the expected `isNone`, once in the `CyGame` class and once in the
separate `CyDeal` class; there is no duplicate within either class that shadows an earlier binding.

Supplied Base AdvC 1.14 has 250 name-target pairs / 249 distinct names. Every Base pair survives with
the SAME Python name and SAME C++ target in current SAS. Current SAS adds exactly the 12 bindings
already mapped in the completed Queue-046 native `CyGame.cpp` audit:
- `logSASGameRecordRejectedAIOffer`;
- five persistent SAS-version-history accessors;
- `endAIAutoPlay`;
- cached land/naval-heavy-map getters;
- initial active-player, map-RNG-seed and sync-RNG-seed getters.

Those additions bind directly to the correspondingly named current `CyGame` methods. Queue 046
already validated their argument bounds, return domains and shipped callers; this registration pass
finds no arity swap, wrong target, overloaded-address ambiguity or return-policy requirement on any of
them. Later SAS history entries touching this file after those additions are comment/format/context
changes, not a hidden rebinding.

POINTER / REFERENCE OWNERSHIP SATURATION
----------------------------------------
The full object-return surface was classified against the native bodies:
- F688 is the one confirmed mismatch: `getNextUnitInCycle` returns fresh `new CyUnit(...)` storage but
  is registered as `reference_existing_object`.
- `getHolyCity`, `getHeadquarters`, `getDeal`, `addDeal` and `getReplayInfo` return independently
  allocated Cy wrappers and correctly use `manage_new_object`.
- `getMapRand` and `getSorenRand` return references to engine-owned `CvRandom` members and correctly
  use `reference_existing_object`.
- `CyDeal::getFirstTrade/getSecondTrade` return pointers to existing `TradeData` nodes owned by the
  live `CvDeal` trade lists; their non-owning policy is therefore correct in the ownership direction.

The last pair does have the usual borrowed-interior lifetime limitation: a Python mod could retain a
`TradeData` proxy and then mutate/kill the deal, leaving the proxy dangling. Current shipped callers
do not do that. `CvForeignAdvisor.py` reads ItemType/iData immediately inside the deal traversal;
`BUG/DealUtil.py` yields trade objects only while operating on the enclosing Deal and its stock
helpers consume them immediately. There is no ordinary producer that stores one across `deal.kill()`.
Keep that as API-lifetime hardening for the later native Queue-145 `CyDeal.cpp` audit, not F689.

`CyDeal::getFirstTrade/getSecondTrade` also omit an explicit `i < 0` guard, but a negative index
cannot match the loop's nonnegative `iCount` and therefore falls through to NULL rather than indexing
storage. Oversized indices are rejected before traversal.

HELP-STRING / DOCUMENTATION CONTROL
-----------------------------------
Like the already-closed CyUnit binding queue, this file contains inherited help strings whose prose
or nominal signature is stale or incomplete (for example old enum spelling/parameter annotations).
Those strings are Boost.Python documentation only; the callable target is determined by the member
function pointer. No shipped runtime path parses them as ABI metadata. They remain documentation debt
rather than an F689 root.

ANCESTRY / DEDUP
----------------
AdvC practical 2820 introduced F688's fresh `CyUnit` implementation and its non-owning registration
in the same feature patch. No later reachable AdvC practical repaired the policy, and supplied Base
AdvC 1.14 retains it unchanged. The other current registration changes map cleanly to the SAS
additions already audited in Queue 046.

Whole-album dedup remains unchanged:
- KI#480/F157 is the opposite ownership-direction bug: an OWNING policy on borrowed queue-node
  storage. Current SAS repaired that path by returning independent copies.
- F683/KI#1004 is an unrelated Settings/SPaH allocation leak.
- the Queue-046 native-wrapper pass does not own F688 because the native allocation is internally
  valid; the defect exists only when the separate Boost.Python policy lies about ownership.
No second supported binding root remains after name-target, arity, ownership, SAS-only and shipped
caller saturation.

QUEUE 091 OPEN
--------------
Ordinary order advances to Queue 091 `CvGameCoreDLL/CySelectionGroup.cpp` (328 queued / 331 current
physical lines) under F689 / provisional KI#1010. Supplied Base AdvC 1.14 has 329 physical lines.
The first direct diff is deliberately small: formatting plus SAS's already-known KI#480 repair in
`getMissionFromQueue`, which now returns an independent `new MissionData(*pMission)` copy so the
existing owning Python binding is truthful. Audit the remaining wrapper bodies independently rather
than assuming the earlier binding-file work covers their native pointer/null/index/path contracts.

Exact continuation stone:
C031-WIP592 physical -> Queue090 CLOSED / F688 CONFIRMED / no independent F689 -> 103 / 169 complete
-> Queue091 CySelectionGroup.cpp OPEN -> F689/KI#1010 UNUSED -> SAS/Base/K-Mod/Civ4CE body parity +
KI#480 repaired-copy control -> pointer/null/index + path-generation + mission-queue + returned
CyPlot/CyUnit ownership contracts -> shipped Python caller census + reachable history + whole-album
dedup -> promote only a distinct consequential root or close cleanly -> next physical WIP.

Files complete: 103 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP593 - QUEUE 091 CLOSED; QUEUE 093 OPEN - F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP592.
Authoritative source: reconciled `AdvCiv-SAS_light_source_20260910T085029.zip`, supplied Base AdvC
1.14, K-Mod 1.46, Civ4CE/Taurus comparison trees, current shipped Python, current native
`CvSelectionGroup.cpp/.h`, `LinkedList.h`, Queue-037 `CvPythonCaller.cpp`, reachable AdvC/SAS
history and prior album ownership findings. Gameplay/XML/Python source untouched.

QUEUE 091 CLOSURE
-----------------
`CvGameCoreDLL/CySelectionGroup.cpp` is COMPLETE for this pass. No independent F689 / provisional
KI#1010 was promoted.

SAS / BASE / HISTORY SATURATION
-------------------------------
Current SAS has 60 CySelectionGroup wrapper bodies. A body-normalized comparison against supplied
Base AdvC 1.14 finds 59 / 60 executable bodies equivalent. The sole deliberate semantic difference
is the current SAS KI#480 repair in `getMissionFromQueue`; the other visible current diff around
`generatePath` is formatting/comment structure rather than changed behavior. This gives a strong
negative control against an accidentally missed later Base-AdvC repair.

The reachable history behind the wrappers was sampled across every semantic stratum that still
survives in current code. Relevant AdvC changes include owner accessor cleanup, the const-reference
constructor, pointer-to-reference movement/path conversions, MovementFlags/MissionTypes casts,
`generatePath` NULL-underlying-plot hardening, explicit `getUnitAt` bounds foolproofing and the AI
stranding target. Current SAS additionally contains the later KI#480 queue-data ownership repair.
No historical transition leaves a current SAS-only regression or an unported Base fix.

KI#480 / F157 DEDUP - CURRENT REPAIR IS TRUTHFUL
------------------------------------------------
The most dangerous-looking ownership surface is already owned and repaired. Firaxis exposed an
interior `MissionData*` from `CvSelectionGroup::m_missionQueue` through a Boost.Python
`manage_new_object` policy, allowing Python to delete queue-owned storage or retain it after queue
mutation. Current SAS instead obtains the native pointer and returns an independent copy:

    MissionData const* pMission = m_pSelectionGroup ?
        m_pSelectionGroup->getMissionFromQueue(iIndex) : NULL;
    return pMission == NULL ? NULL : new MissionData(*pMission);

The existing `manage_new_object` registration is therefore now correct. Do not renumber this as
F689. The same ownership-direction issue and repair boundary are already recorded under F157 /
KI#480.

MISSION-QUEUE INDEX CONTRACT
----------------------------
The native queue lookup is also safe for invalid indices rather than performing unchecked array
indexing. `CvSelectionGroup::getMissionFromQueue` calls `m_missionQueue.nodeNum(iIndex)`, and
`CLinkList::nodeNum` walks from nonnegative counter zero until a counter equals the requested index.
A negative index can never match and an oversized index reaches the end; both return NULL. The SAS
copy wrapper then returns NULL without allocation.

The scalar queue getters used by shipped Python (`getMissionType`, `getMissionData1`,
`getMissionData2`) are likewise called under explicit queue-length bounds. Current UI/BUG callers
first require `getLengthMissionQueue() > 0` or loop `range(iNodeCount)`. Native scalar getters also
return sentinels when no node exists. There is therefore no hidden queue-index F689 behind the
already-owned pointer-lifetime defect.

FRESH CY-WRAPPER OWNERSHIP
--------------------------
The remaining object-return wrappers reconcile with their registrations:
- `lastMissionPlot`, `plot`, `area`, `getPathFirstPlot`, `getPathEndTurnPlot`, `getHeadUnit` and
  `getUnitAt` allocate independent Cy wrapper objects and are registered with `manage_new_object`;
- `getUnitAt` explicitly rejects negative and >= count indices before asking the native group for a
  unit;
- a valid group with no head can produce a CyUnit wrapper around NULL, which remains a valid
  Python-side `isNone` object and is independently reclaimed.

This is importantly unlike Queue-090 F688: no fresh wrapper in this class is mislabeled as borrowed.

POINTER-ARGUMENT / NULL SURFACE
-------------------------------
Several inherited public wrapper methods dereference the Cy wrapper *argument* without first testing
that the wrapper pointer itself is NULL: examples include `canStartMission`, `canDoInterfaceModeAt`,
`canEnterArea`, the move/at helpers, `isAmphibPlot`, `getBestBuildRoute`, `generatePath`, and the
optional mission-AI plot/unit arguments to `pushMission`. A third-party Python caller that explicitly
passes `None` where one of these methods expects a CyPlot/CyArea/CyUnit can therefore crash before
native validation.

That is real API sharpness but does not meet this album's finding threshold on current evidence.
Repository-wide shipped-caller census finds no ordinary caller for most of these group-level methods;
the definite shipped `pushMission` helper in `MonkeyTools.py` obtains both the plot and unit from live
objects immediately before the call. `generatePath` has no shipped CySelectionGroup Python consumer.
The AStarTools `canMoveOrAttackInto` hits are CyUnit calls, not CySelectionGroup. Keep these as generic
mod-API hardening unless a supported caller/fixture supplies NULL; do not spend F689 merely because
Boost.Python can theoretically receive `None`.

PATH / OUT-PARAMETER SURFACE
----------------------------
AdvC's current `generatePath` wrapper verifies that the underlying CvPlot objects exist before
passing them by reference to the native pathfinder. Its wrapper-pointer NULL omission belongs to the
API-hardening class above; there is no shipped caller. The path-result plot getters allocate fresh
CyPlot wrappers and match their owning policies.

`getBestBuildRoute(CyPlot*, BuildTypes*)` exposes a C++-style optional output pointer awkwardly to
Python, but the native method permits a NULL output pointer and the wrapper guards a NULL underlying
CvPlot. No shipped Python caller was found. This is not demonstrated corruption.

ENUM / LIVE CALLER CHECKS
-------------------------
The live shipped `setActivityType` calls use named valid values (SLEEP, INTERCEPT, PATROL, PLUNDER)
in autoplay/change-player/WorldBuilder code. No shipped `setAutomateType` Python consumer was found.
The unit-head callers in autoplay/change-player code test `getNumUnits() > 0` before requesting the
head unit. Mission scalar callers use bounded queue indices as described above. No enum/index crossing
with a supported invalid value survived the caller pass.

CONST-REFERENCE CALLBACK CONSTRUCTOR
------------------------------------
The AdvC const-reference constructor stores a `const_cast<CvSelectionGroup*>`. Repository-wide native
census finds the constructor used by `CvPythonCaller::doCombat`, which creates a temporary
CySelectionGroup wrapper for the Python combat callback and deletes that wrapper after the call.
The stock BUG game-utils callback is disabled by default unless a mod override enables it. The cast
technically lets an enabled callback invoke mutators on a group supplied through a const native
reference, and retaining the temporary Python wrapper beyond the callback would be lifetime misuse.
Those are callback-extension contract issues rather than a demonstrated stock activation; Queue-037
already audited the callback bridge. No independent F689 is assigned here.

WHOLE-ALBUM DEDUP / NEGATIVE CONTROLS
-------------------------------------
Whole-album lookup confirms the only existing CySelectionGroup ownership root is KI#480/F157, now
repaired. Prior pathfinder/CyPlot findings own their own native or static-wrapper roots and do not
supply a new current CySelectionGroup defect. No prior finding is being silently duplicated and no
current evidence justifies reopening one.

QUEUE 091 RESULT
----------------
Close Queue 091 with F689 unused. The file contains some inherited Python-API hardening opportunities,
but current shipped callers satisfy their preconditions; all fresh return-wrapper policies match;
mission queue invalid indices fail to NULL/sentinels; and the one consequential queue ownership defect
is already repaired and owned by KI#480/F157.

QUEUE ADVANCE
-------------
Queue 092 `CvGameCoreDLL/BBAILog.cpp` was already completed at the C029 durable checkpoint, so do not
re-audit it merely because ordinary numeric order passes through it.

Ordinary order therefore advances directly to Queue 093 `CvGameCoreDLL/CvPopupReturn.cpp`
(323 queued / 323 current physical lines), OPEN under F689 / provisional KI#1010 unused. Start with
SAS/Base/K-Mod/Civ4CE provenance, then audit popup-return map/list ownership, index/key semantics,
copy/read-write behavior and all Python/UI consumers before assigning a new root.

Exact continuation stone:
C031-WIP593 physical -> Queue091 CLOSED cleanly -> F689/KI#1010 UNUSED -> Queue092 already COMPLETE ->
Queue093 CvPopupReturn.cpp OPEN -> compare SAS/Base/K-Mod/Civ4CE + reachable history -> inspect
popup-return maps/lists, index/key/copy/serialization/UI/Python contracts -> whole-album dedup ->
either promote a consequential F689 or close Queue093 cleanly -> freeze next WIP.

Files complete: 104 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP594 - QUEUE 093 CLOSED; QUEUE 094 OPEN - F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP593.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, K-Mod 1.46,
Civ4CE BtS/Taurus SDK baselines, current shipped Python popup consumers, Queue-019
`CvDLLButtonPopup.cpp`, retained DllExport/history archaeology and prior album ownership findings.
Gameplay/XML/Python source untouched.

QUEUE 093 CLOSURE
-----------------
`CvGameCoreDLL/CvPopupReturn.cpp` is COMPLETE for this pass. No independent F689 / provisional
KI#1010 was promoted.

PROVENANCE
----------
Current SAS `CvPopupReturn.cpp` is textually identical to supplied Base AdvC 1.14 after line-ending
normalization: 323 / 323 lines and identical content. K-Mod 1.46, Civ4CE BtS and Taurus retain the
older Firaxis/K-Mod inline-header organization and the same core PopupReturn storage semantics.
Only a very small AdvC history touches the file: formatting, the advc.003k ABI/memory-corruption pass
that moved implementations from the header and made the unused copy constructor private, and the
later FAssert->FError conversion. No newer Base repair is missing from SAS.

The advc.003i export-removal history is a useful ABI control. AdvC explicitly removed DLLExports it
believed unused by the executable, made the PopupReturn copy constructor non-exported, but retained
`DllExport PopupReturn::operator=` plus the exported read/write surface. Therefore assignment and/or
serialization must continue to be treated as executable-facing compatibility contracts even though
no normal DLL source call site names them.

STORAGE / SERIALIZATION SATURATION
----------------------------------
PopupReturn carries eight variable-length result families: radio selection, checkbox bitfield,
edit-box strings, the legacy `m_aiSpinnerWidgetValues` vector, pulldown selection, list-box selection,
button selection and `m_aiSpinBoxID`. Current read() and write() serialize the same eight families in
the same order. The current class-size static assertion preserves the binary layout expected by the
executable; no SAS member addition or order drift exists.

Malformed-stream robustness remains intentionally outside the finding bar here. `read()` trusts the
serialized family counts, uses the inherited 1024-wchar ReadString buffer and does not clear existing
vectors before filling indices 0..size-1. Re-reading a shorter malformed/reused object could retain a
stale tail; an attacker-controlled huge count could also drive allocation. No supported save/network
fixture or current source caller demonstrates such reuse/hostile counts, and these patterns are
inherited ABI/stream-hardening rather than a current F689 root.

THE SPINNER / SPINBOX ASYMMETRY - REAL SOURCE ODDITY, NOT PROMOTED
-------------------------------------------------------------------
The strongest-looking line in the file is genuinely asymmetric:

    void setSpinnerWidgetValue(int value, int group)
        -> writes m_aiSpinnerWidgetValues[group]

    int getSpinnerWidgetValue(int group) const
        -> reads m_aiSpinBoxID[group]

so the nominal setter/getter pair does not round-trip. The original Firaxis/K-Mod/Civ4CE/Taurus code
has the same mismatch. The assignment/copy loops also use `getSpinnerWidgetValue` while filling the
legacy spinner-widget vector, meaning a source state containing only `m_aiSpinnerWidgetValues` would
not be value-preserved by operator=.

This initially looked like a strong F689 candidate, but the shipped activation census changes the
classification. The live Python `createSpinBox` consumers are the BUG Reminder popup and the
WorldBuilder start-year popup. Both read the submitted value through `getSpinnerWidgetValue`, and the
Python API exposes that historical name for a SpinBox result. `getSpinnerWidgetValue` and
`getCurrentSpinBoxValue` both read `m_aiSpinBoxID`, i.e. the live Python SpinBox value family.
Repository-wide current source contains no shipped producer/consumer of the separate legacy
`m_aiSpinnerWidgetValues` family outside PopupReturn's own setter/copy/read/write machinery.

Thus changing `getSpinnerWidgetValue` naively to read `m_aiSpinnerWidgetValues` would be dangerous: it
would likely break the known live Python SpinBox API in order to make an apparently paired C++ setter
look prettier. Preserve the asymmetry as an inherited ABI/legacy-field warning until an executable
fixture proves that `m_aiSpinnerWidgetValues` itself is populated and consumed. Do not spend F689 on
naming alone.

ASSIGNMENT OPERATOR CONTROL
---------------------------
The exported assignment operator grows destination vectors and overwrites all indices present in the
source, but never shrinks/clears a destination that was previously larger. It also cannot faithfully
copy a state where only the legacy spinner-widget vector differs, because that loop reads the live
SpinBox getter discussed above. Both are real generic assignment-semantics weaknesses.

They remain below the current-game finding threshold because no supported executable sequence was
established that assigns a smaller/different PopupReturn into a reused larger destination and then
queries a stale tail, and no shipped control uses the legacy spinner-widget family. The advc.003i
DllExport evidence is enough to keep these in the album as ABI hazards, but not enough to invent a
player-visible consequence. If a future runtime trace exposes repeated assignment or a native legacy
spinner control, reopen this exact candidate rather than rediscovering it.

GETTER / INDEX SURFACE
----------------------
Several legacy getters use vector::at without their own bounds guard (`getSelectedRadioButton`,
`getCheckboxBitfield`, `getSelectedPullDownValue`, `getButtonClicked`), while the partially guarded
edit/list/spin getters check only `i < size`; a negative Python/mod index can therefore pass the signed
upper-bound test and reach `at()` after conversion to an unsigned index. The setter macro is likewise
unsafe for a negative group/index. These are genuine public-API precondition edges but current shipped
Python uses fixed nonnegative group IDs produced by the matching popup definitions.

Current shipped Python PopupReturn consumer census is small and concrete: 19 edit-box reads, 14
button reads, 10 pulldown reads, 3 radio reads, 2 list-box reads and 2 SpinBox reads. Their group IDs
are fixed or derived from controls created in the same popup builder. Native Queue-019 PopupReturn
consumers likewise read control groups created by their corresponding launch routine (e.g. Admin
checkbox group 2 is explicitly created as group 2). No supported negative/missing-group activation
was found. Keep the unchecked-index behavior as mod-API hardening, not F689.

COPY CONSTRUCTOR CONTROL
------------------------
The historical copy constructor is intentionally unimplemented, private in current AdvC/SAS, and
emits FErrorMsg if somehow entered. AdvC practical 2126 explicitly marked it unused and removed its
old body while preserving the ABI-sensitive class shape. Repository-wide current source has no copy-
construction site. It is therefore a deliberate tripwire, not a current defect.

QUEUE 093 RESULT
----------------
Close Queue 093 with F689 unused. The file contains unusually confusing inherited naming and several
real robustness/assignment weaknesses, but the live Python SpinBox contract reconciles with
`m_aiSpinBoxID`, current popup indices satisfy their control definitions, serialization order is
symmetric, current SAS matches Base AdvC 1.14, and no supported fixture activates the legacy-spinner
or stale-assignment hazards.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 094 `CvGameCoreDLL/CvPlotGroup.cpp` (304 queued / 304 current
physical lines), OPEN under F689 / provisional KI#1010 unused. Audit plot-group membership,
recalculation/invalidation, bonus-count bookkeeping, add/remove sequencing, owner/plot identity,
serialization and any Python/UI consumers against Base/K-Mod/Civ4CE and reachable history.

Exact continuation stone:
C031-WIP594 physical -> Queue093 CLOSED cleanly -> F689/KI#1010 UNUSED -> legacy
PopupReturn spinner-widget asymmetry + nonshrinking assignment preserved below threshold -> Queue094
CvPlotGroup.cpp OPEN -> provenance + whole-body bookkeeping/invalidation audit -> callers/history +
whole-album dedup -> either promote a consequential F689 or close Queue094 cleanly -> freeze next WIP.

Files complete: 105 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP595 - QUEUE 094 CLOSED; QUEUE 095 OPEN - F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP594.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, K-Mod 1.46,
Civ4CE BtS SDK baseline, current CvPlot/CvPlayer/CvMap plot-group callers, LinkedList/FLTA ownership
machinery, prior album plot-group/resource/production findings. Gameplay/XML/Python source untouched.

QUEUE 094 CLOSURE
-----------------
`CvGameCoreDLL/CvPlotGroup.cpp` is COMPLETE for this pass. No independent F689 / provisional
KI#1010 was promoted.

PROVENANCE
----------
Current SAS and supplied Base AdvC 1.14 are executable-semantically identical in CvPlotGroup except
for SAS's deliberate modern-only enum-map save compatibility comments/read boundary. The live
membership/recalculation machinery is AdvC's K-Mod-derived implementation plus `advc.064d`
production-validation handling and the later DFS replacement for the old FAStar size check. K-Mod
and Civ4CE supply the expected ancestral controls; no newer Base 1.14 repair is missing from SAS.

MEMBERSHIP / ADD-REMOVE INVARIANTS
----------------------------------
`addPlot` appends the plot coordinates and then delegates the actual player->group ID transition to
`CvPlot::setPlotGroup`. All current callers invoke it only for a plot that is NULL-grouped or belongs
to another group: `CvPlayer::initPlotGroup`, `CvPlot::updatePlotGroup` and `CvMap::combinePlotGroups`.
The latter deliberately leaves the old group's linked-list node in place until after `setPlotGroup`
finishes; that is required because `setPlotGroup` removes the plot's resource contribution and city
bonus state from the old group before installing the new ID.

`removePlot` performs the reverse transition first and deletes the linked-list node second. If the
node was the final member, `deletePlotsNode` removes this CvPlotGroup from the owner's FLTA and thus
deletes the object. The function then breaks/returns without further member access. The same
self-destruction pattern occurs while dismantling a group in `recalculatePlots`; after the deletion,
that routine continues only through locals (`eOwner`, `oldPlotGroup`, `apOldCities`) and the static
recalculation counter. No current use-after-free was established.

BONUS-COUNT ACCOUNTING
----------------------
`CvPlot::setPlotGroup` deliberately performs bonus bookkeeping in four stages: remove the moving
plot's contribution from the old group; remove the old group's remaining aggregate from a city on
the moving plot; install the new group ID and add its pre-existing aggregate to that city; then add
the moving plot's own contribution to the new group. This apparently redundant sequence reconciles
symbolically and is necessary for city/free-bonus/capital import-export contributions.

`CvPlotGroup::changeNumBonuses` propagates each aggregate delta to every owner city currently in the
group. Temporarily negative group counts during topology surgery are inherited and explicitly
acknowledged by K-Mod; production validation is suppressed while a group recalculation is active so
those transient states are not treated as final legality. The final stable paths restore aggregate
counts through the same add/remove mechanics. No independent count leak or double application was
found.

NESTED `recalculatePlots` / PRODUCTION-VERIFICATION CANDIDATE - CLOSED
---------------------------------------------------------------------
The source itself warns that collecting only cities present in the group before the root
recalculation "might be inadequate" if nested recalculations occur. This was the strongest Queue-094
candidate and was traced through all concrete callers rather than dismissed from the comment.

For a SPLIT, the only cities that can lose access to bonuses are cities in the old group. Those are
exactly the city pointers collected in `apOldCities` before any membership is removed. Root
recalculation suppresses intermediate verification while `m_iRecalculating > 0`, rebuilds all old
plots through `CvPlot::updatePlotGroup`, then verifies those old-group cities once the final topology
is stable. A component can encounter/merge with a pre-existing external group while rebuilding; an
external city omitted from `apOldCities` gains the union network rather than suffering an unverified
final loss from this split.

For the broad `CvPlayer::updatePlotGroups` path, AdvC additionally performs `verifyCityProduction()`
for every player city after all existing groups have been recalculated and every map plot has been
reattached. The ordinary `CvPlot::updatePlotGroup()` facade likewise performs a player-wide human
verification after its inner per-player rebuild with intermediate verification disabled.

For MERGES outside an active recalc, `combinePlotGroups` moves the smaller group plot by plot.
Temporary resource states can occur while the old group's nodes are being transferred, but each city
plot transferred to the surviving group passes through the normal `setPlotGroup` path and the final
resource-bearing plot transfers invoke surviving-group verification. No concrete ordering was found
that leaves a city with a final resource LOSS and no later verifier. The evidence therefore does not
support promoting the author's cautionary comment into F689.

DFS SIZE-CHECK CONTROL
----------------------
The AdvC DFS precheck uses `isSamePlotGroup`, `isTradeNetwork` and `isTradeNetworkConnected`, matching
the predicates consumed by `CvPlot::updatePlotGroup`. The visitor count is capped at old length + 1;
for a consistent group its reachable set cannot exceed the linked-list membership count. The
`DepthFirstPlotSearch` start-node return value is irrelevant to this visitor because the first visit
cannot hit that cap for a nonempty group. No early-return false negative was established.

SERIALIZATION / API BOUNDARY
----------------------------
Read/write serialize ID, owner, bonus enum map and coordinate list symmetrically. SAS intentionally
removed the pre-modern enum-map branch and therefore no longer accepts the legacy Base save shape;
that project compatibility boundary is already documented elsewhere and is not a new Queue-094 bug.
Malformed streams or third-party direct misuse of public node helpers remain robustness territory.

WHOLE-ALBUM DEDUP / NEGATIVE CONTROLS
-------------------------------------
Existing plot-group/resource findings already own independent roots involving resource propagation,
route/reveal topology, old player-group IDs, corporation state and `advc.064d` production holes.
Queue094 produced no additional root after checking those neighboring mechanisms. In particular, do
not duplicate F563/F564-style production invalidation merely because CvPlotGroup is one propagation
layer: no final supported stale-production state specific to this file was demonstrated.

QUEUE 094 RESULT
----------------
Close Queue 094 with F689 unused. Membership and aggregate-bonus bookkeeping reconcile across current
supported callers; the self-deleting routines do not touch members afterward; nested recalculation
suppresses transient validation and performs stable-state verification on the cities capable of
losing resources; broad rebuilds have an all-city safety pass.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 095 `CvGameCoreDLL/CyGameTextMgr.cpp` (291 queued / current physical
line count to be checked), OPEN under F689 / provisional KI#1010 unused. Compare current SAS against
Base/K-Mod/Civ4CE, then audit every wrapper target, argument conversion, NULL/lifetime boundary,
out-parameter emulation and shipped Python consumer against the already-audited native
CvGameTextMgr contracts.

Exact continuation stone:
C031-WIP595 physical -> Queue094 CLOSED cleanly -> F689/KI#1010 UNUSED -> Queue095
CyGameTextMgr.cpp OPEN -> provenance + binding/wrapper/native-parity audit -> shipped Python callers +
whole-album dedup -> either promote a consequential F689 or close Queue095 cleanly -> freeze next WIP.

Files complete: 106 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP596 - QUEUE 095 CLOSED; QUEUE 097 OPEN - F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP595.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, K-Mod 1.46,
Civ4CE BtS baseline, current CyGameTextMgrInterface registration and shipped Python consumers, native
CvGameTextMgr contracts and prior album native-text findings. Gameplay/XML/Python source untouched.

QUEUE 095 CLOSURE
-----------------
`CvGameCoreDLL/CyGameTextMgr.cpp` is COMPLETE for this pass. No independent F689 / provisional
KI#1010 was promoted.

PROVENANCE / SAS DELTA
----------------------
After line-ending normalization, current SAS is identical to supplied Base AdvC 1.14 except for the
intentional `getSpecificUnitHelpOmitOwner` wrapper added for the Military Advisor. The method forwards
the existing native `setUnitHelp` omit-owner and promotion-icon-size parameters and its sole shipped
caller supplies a live CyUnit plus the advisor's configured positive inline icon size.

K-Mod provides a useful ancestry control: its pointer constructor accidentally initializes
`m_pGameTextMgr` from itself (`m_pGameTextMgr(m_pGameTextMgr)`). AdvC corrected that to
`m_pGameTextMgr(pGameTextMgr)`, and current SAS carries the correction. Thus this queue contains a
historical upstream wrapper bug already fixed before SAS, not a current finding.

WRAPPER / NATIVE PARITY
-----------------------
Every wrapper target was reconciled against the current native signature. Enum-like integer
arguments are cast to the expected native enum type; the shipped Python callers supply valid IDs or
the documented NO_* sentinel. The important sentinel cases are live and supported: `getTechHelp`
uses `iFromTech=-1` as NO_TECH, and Sevopedia trait help passes civilization -1, which native
`parseLeaderTraits`/trait rendering explicitly supports as NO_CIVILIZATION.

CyCity arguments to unit/building/project/religion/corporation help are NULL-safe at the wrapper
boundary. Specific-unit helpers check both the CyUnit pointer and its underlying CvUnit before
calling native text generation. `getDealString` similarly validates CyDeal and its underlying
CvDeal. `getTradeString` accepts a nullable TradeData pointer and the shipped Foreign Advisor passes
an immediately obtained live TradeData object; no retained-pointer lifetime is created by this
wrapper because native formatting consumes the structure synchronously and returns a copied string.

RETURN-LIFETIME CONTROL
-----------------------
All methods build a local CvWString/CvWStringBuffer and return `std::wstring`. Returning
`getCString()`/`c_str()` is safe here because construction of the std::wstring copies the characters
before the local buffer is destroyed. No borrowed string storage escapes the wrapper.

SHIPPED PYTHON CENSUS
---------------------
Current shipped callers cover essentially the whole useful wrapper surface: time/date/interface,
research/OOS/network, tech/unit/building/project/promotion/bonus/religion/corporation/terrain/feature,
civic/religion/corporation/civilization/leader text, trade and attitude helpers, and the custom
Military Advisor omit-owner helper. The few methods without current Python consumers are legacy
wrappers (`Reset`, vassal-revolt/deal-list formatting etc.); no invalid current caller was found.

The related `CyGameTextMgrInterface.cpp` contains stale HELP TEXT (including swapped descriptive
signatures for `getSpecialistHelp` and `getTradeString`, and an incomplete description of the religion
helper's parameters). Those strings do not determine Boost.Python argument binding; the actual member
function pointers are correct. Queue 157 will own the registration-file audit later. Do not spend
F689 on documentation text.

POINTER-CONSTRUCTOR / MEMBER CONTROL
------------------------------------
The class stores `m_pGameTextMgr`, but ordinary wrapper methods use the singleton `GAMETEXT` macro.
The default Python constructor initializes the stored pointer to that same singleton; the alternate
pointer constructor is C++-side and no current caller supplies a distinct/null manager expecting
polymorphic behavior. This is inherited design oddity, not a supported current defect.

QUEUE 095 RESULT
----------------
Close Queue 095 with F689 unused. Current SAS differs from Base only by a deliberate and correctly
wired UI bridge; the old K-Mod constructor bug is already repaired; string return lifetimes are
copied; pointer wrappers are guarded where live optionality exists; and shipped enum/sentinel callers
match native contracts.

QUEUE ADVANCE
-------------
Queue 096 `CvGameCoreDLL/CvMemoryManager.cpp` was already completed at C031-WIP495, so ordinary order
does not re-audit it. Advance directly to Queue 097 `CvGameCoreDLL/CvTalkingHeadMessage.cpp` (282
queued / current physical lines to verify), OPEN under F689 / provisional KI#1010 unused. Audit
copy/assignment ownership, string/sound/icon lifetime, shown/expiration timing, player/plot metadata,
serialization and executable/UI callers against Base/K-Mod/Civ4CE.

Exact continuation stone:
C031-WIP596 physical -> Queue095 CLOSED cleanly -> F689/KI#1010 UNUSED -> Queue096 already COMPLETE ->
Queue097 CvTalkingHeadMessage.cpp OPEN -> provenance + copy/assignment/lifetime/timing/serialization
contracts -> UI/executable callers + whole-album dedup -> promote only a consequential independent
F689 or close cleanly -> freeze next WIP.

Files complete: 107 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP597 - QUEUE 097 CLOSED; QUEUE 098 OPEN - F689 / KI#1010 UNUSED
===============================================================================
Physical parent: C031-WIP596.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, K-Mod 1.46,
Civ4CE BtS baseline, current CvPlayer retained-message/read-write/expiry callers, RiseFall message
archive consumer and prior F490/KI#813 ownership proof. Gameplay/XML/Python source untouched.

QUEUE 097 CLOSURE
-----------------
`CvGameCoreDLL/CvTalkingHeadMessage.cpp` is COMPLETE for this pass. No independent F689 /
provisional KI#1010 was promoted.

PROVENANCE
----------
After line-ending/signature-format normalization, current SAS is executable-identical to supplied
Base AdvC 1.14. Relative to K-Mod/Civ4CE, AdvC adds the `advc.106b` sound-played state/message-type
handling and `advc.700` human-vs-AI expiry policy. No newer Base fix is missing from SAS.

VALUE / STRING OWNERSHIP
------------------------
The object owns CvWString/CvString values for description, sound and icon. The compiler-generated
copy/assignment semantics therefore duplicate string storage rather than retaining caller character
pointers. `CvPlayer::m_listGameMessages.push_back(kMessage)` is value ownership and does not create a
borrowed-string lifetime problem. Scalar/message metadata likewise copies by value.

The default constructor arguments permit NULL description/sound/icon and are used by the normal
save-load path (`CvTalkingHeadMessage message; message.read(...)`). Current string wrappers support
that inherited empty/default construction; this is exercised by every message-queue load and is not
a theoretical null edge.

SOUND-PLAYED HACK / SAVE CONTRACT
---------------------------------
`getSound()` marks mutable `m_bSoundPlayed=true` before returning the owned sound string. AdvC's
comment explicitly documents the executable contract assumption: the EXE requests the sound through
this getter when it is going to trigger it. Retained message copies consult `getSoundPlayed()` to
avoid replaying sound.

`m_bSoundPlayed` is intentionally omitted from write(). On read(), it is forced true so loading a
save cannot replay historical message sounds. This asymmetry is deliberate and coherent rather than
serialization loss. No current source caller changes a message's sound after it has been fetched;
`setSound` has no DLL-side caller, so the theoretical stale-flag setter sequence is not a supported
activation.

EXPIRY / MESSAGE-TYPE CONTROL
-----------------------------
`CvPlayer::expireMessages` calls `getExpireTurn(isHuman() || isHumanDisabled())`. The deep CvPlayer
audit already reconciled the `isHumanDisabled()` inclusion as intended Rise & Fall behavior so a
human temporarily under AI control keeps human-style history. MESSAGE_TYPE_DISPLAY_ONLY is made
immediately expirable because it is not meant to persist; EOT gets one turn; human major/quest
messages receive the long retention window; AI Rise&Fall delivery intentionally uses shorter
windows. No enum branch or expiration arithmetic contradiction was established.

SERIALIZATION
-------------
Read/write order is symmetric for description, sound, icon, length, color, coordinates, arrow flags,
turn, message type, source player, chat target and shown state. Enum scalars are written as their
current integral representation and read through temporary ints consistently with the executable
ABI. The repro-test markers are instrumentation only. `m_bSoundPlayed` is the intentional exception
covered above.

F490/KI#813 WHOLE-ALBUM DEDUP
-----------------------------
Following this class into `CvPlayer::addMessage` re-exposes a genuine severe ownership defect:
HotSeat major events can place ONE freshly allocated CvTalkingHeadMessage* into both `m_aMajorMsgs`
and `m_aHotSeatMsgs`, after which common cleanup deletes the same address twice. That exact root,
ordinary HotSeat activation, ancestry and repair direction were already confirmed as F490/KI#813 at
C031-WIP223 during Queue004.

Queue097 therefore must not spend F689 on the same bug merely because this is the allocated object's
class. The class's own value/copy/destruction behavior is not what creates the alias; the producer is
CvPlayer's retained raw-pointer ownership logic.

OTHER UI / LIFETIME CONTROLS
----------------------------
`showMissedMessages` iterates value-owned queue elements and passes a live lvalue to the executable
for immediate display; it does not retain a second DLL owner. RiseFall's quest scan is const and only
reads message type. Event expiry erases the value from the list after matching the overloaded quest
trigger ID in length. No current iterator-after-erase or dangling message reference survived.

QUEUE 097 RESULT
----------------
Close Queue 097 with F689 unused. The class itself has coherent owned strings/value copying,
symmetric persisted state, intentional post-load sound suppression and authored expiry policy. The
only severe adjacent retained-message lifetime failure is already owned by F490/KI#813.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 098 `CvGameCoreDLL/RFChapterScore.cpp` (281 queued / current physical
line count to verify), OPEN under F689 / provisional KI#1010 unused. Audit chapter-score arithmetic,
normalization/divisors, rank/tie handling, era/turn boundaries, cached state, serialization and
RiseFall callers against Base AdvC history and current XML/defines.

Exact continuation stone:
C031-WIP597 physical -> Queue097 CLOSED cleanly; F490/KI#813 dedup retained -> F689/KI#1010 UNUSED ->
Queue098 RFChapterScore.cpp OPEN -> provenance + arithmetic/rank/timing/state/serialization audit ->
RiseFall callers + whole-album dedup -> promote only an independent consequential F689 or close
cleanly -> freeze next WIP.

Files complete: 108 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP598 - QUEUE 098 RFChapterScore.cpp - F689 / KI#1010 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP597.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, current
RiseFall/RFChapter lifecycle and Victory Screen callers, AdvC reachable-history diffs and current RF
localization. Gameplay/XML/Python source untouched.

QUEUE-098 PROVENANCE BASELINE
-----------------------------
Current SAS `RFChapterScore.cpp` is executable-semantically identical to supplied Base AdvC 1.14;
the only source difference is the SAS formatting collapse of the `computeRank` signature. The class
was introduced with Rise & Fall in AdvC practical 1268. The repeat-penalty state machine below was
already present in that introduction and no later RFChapterScore history commit repairs it.
K-Mod/Civ4CE predate the AdvC-only Rise & Fall subsystem and therefore provide no implementation
oracle for this class.

F689 / provisional KI#1010 -- repeat chapter can display a stale penalty that is no longer included in the score
---------------------------------------------------------------------------------------------------------------

ROOT
----
`RFChapterScore::reset()` initializes `fromRepeat` to zero, but ordinary `update()` only assigns it
inside the positive-score branch:

    total = fromRank + fromInitialRank + fromCivScore + fromInitialCivScore;
    fromRemainingTime = std::max(0, ::round(
            total * (remainingTimePercent / 200.0)));
    total += fromRemainingTime;
    if(chapter->isRepeat() && total > 0) {
        fromRepeat = ::round(total / -2.0);
        total += fromRepeat;
    }

If a repeat chapter was positive on one update, `fromRepeat` therefore becomes a negative penalty.
If a later update makes the pre-repeat `total` zero or negative, the branch is skipped and the old
negative `fromRepeat` remains cached even though it is no longer added to `total`.

`updateString()` does not gate the displayed repeat line on `total > 0` or clear the cached field:

    if(chapter->isRepeat() && chapter->getCiv() != NO_PLAYER)
        (*breakdownString) += L"\n" + gDLL->getText(
            "TXT_KEY_RF_CHAPTER_SCORE_REPEAT", -fromRepeat, ...);

Current English localization describes that value as:

    -%d1 points    because %s2 civ played repeatedly

The UI can consequently show an old repeat deduction next to a bottom-line total that does not
contain that deduction.

LIVE RECOMPUTATION / ACTIVATION
-------------------------------
This is not frozen-score-only state. The normal Rise/Fall Victory Screen repeatedly asks the DLL for:
- `getChapterScore(i)`, which calls `RFChapter::computeScore()` -> `RFChapterScore::update()` for an
  unscored chapter;
- `chapterScoreBreakdown()`, which calls `computeScoreBreakdown()` -> `update()` again for the
  current chapter.

A repeat chapter is ordinary supported gameplay: `RiseFall::handleCivSelection` calls
`setRepeat(true)` whenever the newly selected civilization has already appeared in an earlier
chapter.

The pre-repeat score is not monotonic. `computeRank(false)` uses the civilization's current rank;
`fromCivScore` uses current player scores refreshed by `CvGame::updateScore`; those scores are built
from current population, land, technologies and wonders. Rank can worsen and current population or
land score can fall through normal city/population loss. Nothing in RFChapterScore constrains a
positive preview to remain positive for the rest of the chapter.

DETERMINISTIC ARITHMETIC WITNESS
--------------------------------
With three rivals (`initialRivals=3`) and an initial rank of 2, the rank terms alone are allowed to
move from:

    current rank 1: fromRank = 100, fromInitialRank = -67

to:

    current rank 4: fromRank = 0, fromInitialRank = -67.

A legal score-ratio state such as `fromCivScore=120`, `fromInitialCivScore=-100` gives +53 before
the retirement/time term in the first state. With 100% remaining time, that becomes about +80 before
the repeat penalty and caches roughly `fromRepeat=-40`.

After later losses, rank 4 with a 100/100 current/initial score-ratio contribution gives -67 before
repeat handling. `fromRemainingTime` becomes zero and the `total > 0` repeat branch is skipped, so
awarded/displayed total arithmetic remains -67 (before the negative-score scaling) while
`fromRepeat` is still -40. The breakdown can therefore continue to print a 40-point repeat penalty
that was not deducted from that -67 total.

The exact numbers in a real game vary; the source-level invariant needed for the bug is simply a
supported repeat chapter whose live preview crosses from positive to nonpositive. Current scoring
contains no monotonicity guarantee preventing that transition.

WHY THE AWARDED SCORE AND THE BREAKDOWN DIVERGE
----------------------------------------------
The stale field does NOT get added to `total` when `total <= 0`; thus this root does not incorrectly
halve an already nonpositive chapter score. The defect is the player-facing explanation: the repeat
line retains a deduction from an earlier preview even though the current total excludes it.

This distinction matters for repair. Removing the `total > 0` condition would change scoring
semantics and penalize negative chapters. The narrow state repair is to clear `fromRepeat` on every
update before conditionally assigning it, e.g.:

    fromRepeat = 0;
    if(chapter->isRepeat() && total > 0) {
        ...
    }

or gate the text on a current nonzero/positive repeat deduction as well. Clearing before recompute is
the safer invariant because `fromRepeat` is serialized and should describe the current computed
breakdown state.

ANCESTRY
--------
AdvC practical 1268 / e11525d303... introduced in the same initial RFChapterScore implementation:
- `fromRepeat` as persistent member state;
- assignment only under `chapter->isRepeat() && total > 0`;
- unconditional repeat-line rendering for any repeat chapter.

No later reachable-history RFChapterScore diff touching `fromRepeat` changes those conditions.
Current Base AdvC 1.14 retains the same behavior. SAS inherits it unchanged.

WHOLE-ALBUM DEDUP
-----------------
Whole-album search found no prior root for stale `fromRepeat`, a phantom repeat-chapter deduction, or
a Rise/Fall score-breakdown line surviving after its contribution stops applying.

F660-F665 / KI#982-KI#987 own independent RiseFall.cpp roots (stream deallocation, last-major
sentinel handling, recommendation ordering, chapter-start lifecycle, interlude Barbarian takeover,
and PlayerOption provenance). F689 is local score-breakdown state retained across successive
`RFChapterScore::update()` calls.

QUEUE-098 CONTINUATION CURSOR
-----------------------------
Continue from F690 / provisional KI#1011 UNUSED:
- finish the raw `breakdownString` ownership/destruction lifecycle; RFChapterScore owns a heap
  std::wstring and has no explicit destructor, so prove whether chapter deletion leaks live strings
  or whether every supported delete path first calls `reset()`;
- finish the `initialCivScoresSorted[referenceRank2-1]` nonempty/index proof, especially first
  chapter start score and later chapter/vassal/elimination transitions;
- finish player-score/rank sentinel and zero-score arithmetic, victory-stage and winner overrides;
- finish remaining-time/divisor, negative-score square-root and integer-range boundaries;
- finish read/write/frozen-score/chapter-pointer lifetime contracts and RFChapter/RFTotalScore/UI
  consumers;
- finish all RFChapterScore history strata and whole-album dedup;
- either promote a genuinely independent consequential F690 or close Queue098 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP598 physical -> Queue098 OPEN -> F689/KI#1010 CONFIRMED -> F690/KI#1011 UNUSED ->
ownership/destructor + reference-score/index + arithmetic/serialization/history closure -> promote
only an independent consequential F690 or close cleanly -> freeze next WIP.

Files complete: 108 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP599 - QUEUE 098 RFChapterScore.cpp - F690 / KI#1011 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP598.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, current
RFChapter/RiseFall/CvGame lifetime callers and AdvC practical-1268 introduction diff. Gameplay/XML/
Python source untouched.

F690 / provisional KI#1011 -- RFChapterScore leaks its owned breakdown string when a chapter object is destroyed
---------------------------------------------------------------------------------------------------------------

ROOT / OWNERSHIP CONTRACT
-------------------------
`RFChapterScore` explicitly owns a heap string:

    std::wstring* breakdownString;

Its constructor establishes NULL ownership and `updateString()` replaces the owned object with:

    SAFE_DELETE(breakdownString);
    breakdownString = new std::wstring(...);

`reset()` also correctly deletes the current object. The class, however, declares and defines NO
destructor. Its compiler-generated destructor therefore destroys only the pointer value; it does not
execute the ownership cleanup performed by `reset()`.

`RFChapter` embeds the score object by value:

    RFChapterScore breakdown;

and likewise has no custom destructor that calls `reset()`. `RiseFall` owns chapters as raw pointers
and its ordinary reset/destruction path does:

    for(size_t i = 0; i < chapters.size(); i++)
        SAFE_DELETE(chapters[i]);
    chapters.clear();

Deleting an RFChapter invokes the implicit RFChapterScore destructor, which leaves any currently
owned `breakdownString` allocation unreachable.

NORMAL CURRENT ACTIVATION
-------------------------
A non-NULL breakdown string does not require opening an optional advisor screen. At the ordinary end
of every finite active chapter, `RiseFall::atTurnEnd()` executes:

    currentCh.setScoreAtEnd(currentCh.computeScore());

`RFChapter::computeScore()` calls `breakdown.update()`, and an initialized/unscored chapter's update
finishes with `updateString()`. Thus a completed chapter normally owns one current heap string even if
the player never opened the Rise/Fall score tab.

Later, normal game teardown reaches:

    CvGame::uninit()
        -> m_pRiseFall->reset()
        -> SAFE_DELETE(chapters[i])

without first calling `RFChapter::reset()` / `RFChapterScore::reset()`. The latest breakdown string
for each such chapter is therefore leaked. `RiseFall::~RiseFall()` also delegates to `reset()`, and
Rise/Fall reinitialization/map-regeneration paths can likewise delete accumulated chapter objects.

BOUND / SEVERITY
----------------
`updateString()` deletes the previous string before allocating its replacement, so repeated Victory
Screen previews do NOT leak one string per refresh. The leak is the single currently owned string per
RFChapter when that chapter object is destroyed.

In an ordinary multi-chapter game this therefore accumulates roughly one leaked breakdown string for
each chapter whose score has been computed, plus potentially the current chapter if its breakdown was
previewed. Repeated games within the same Civ4 process repeat the loss. This is a small bounded
resource leak rather than a high-frequency allocator flood, but the ownership violation is fully live
on normal Rise/Fall teardown and does not depend on malformed data.

ANCESTRY
--------
AdvC practical 1268 / e11525d303... introduced RFChapterScore with all three relevant pieces in the
same initial implementation:
- raw owning `std::wstring* breakdownString`;
- `SAFE_DELETE(breakdownString)` in `reset()` / `updateString()`;
- no `~RFChapterScore()` declaration or definition.

The simultaneously introduced RFChapter stores RFChapterScore by value and also has no destructor
cleanup wrapper. Reachable history contains no later destructor addition/removal. Current Base AdvC
1.14 retains the same raw pointer and missing destructor; SAS inherits it unchanged.

COPY / RULE-OF-THREE CONTROL
----------------------------
Current source does not copy RFChapterScore or RFChapter by value: RiseFall stores
`std::vector<RFChapter*>`, and score consumers bind `RFChapterScore const&`. Therefore the present
runtime leak is not accompanied by a demonstrated current shallow-copy double-free path.

That said, simply adding a destructor would formally make the class a Rule-of-Three type. A robust
repair should either prohibit/implement copying as well or remove raw ownership entirely.

REPAIR DIRECTION
----------------
Minimal current repair:

    RFChapterScore::~RFChapterScore() {
        SAFE_DELETE(breakdownString);
    }

with copy construction/assignment disabled or made deep if the class may ever become copyable.

Cleaner repair: store the breakdown as a `std::wstring` value plus a validity/empty state and return a
pointer/reference only at the API boundary as needed. That removes manual ownership and makes the
compiler-generated destructor safe automatically.

WHOLE-ALBUM DEDUP
-----------------
No earlier album finding owns RFChapterScore's missing destruction cleanup. F628/KI#950 is a separate
one-time scenario NormalizationTarget ownership leak; F688/KI#1009 is a high-frequency Python CyUnit
wrapper leak caused by the wrong Boost.Python return policy; F690 is the AdvC-only Rise/Fall
chapter-score member-lifetime leak.

QUEUE-098 CONTINUATION CURSOR
-----------------------------
Continue from F691 / provisional KI#1012 UNUSED:
- prove `initialCivScoresSorted` cannot be empty on supported started chapters, or establish a real
  `referenceRank2-1` underflow fixture;
- finish zero/dead/vassal chapter-civ, winner override and player-rank sentinel handling;
- finish remaining-time percentage/divisor, negative-score sqrt and integer-range boundaries;
- finish save/read frozen-score and `chapter` pointer lifetime, including started-vs-unstarted save
  behavior and F663 lifecycle dedup;
- finish the semantic RFChapterScore history strata (1269/1270/1271/1386/1388 etc.) rather than only
  the introduction;
- whole-album dedup and final mechanical pass;
- either promote an independent consequential F691 or close Queue098 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP599 physical -> Queue098 OPEN -> F689/KI#1010 + F690/KI#1011 CONFIRMED ->
F691/KI#1012 UNUSED -> reference-score/index + arithmetic/serialization/history closure -> promote
only independent consequential root or close Queue098 -> freeze next WIP.

Files complete: 108 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP600 - QUEUE 098 RFChapterScore.cpp - F691 / KI#1012 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP599.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, current
RFChapter/RiseFall selection + scoring contracts, AdvC practical-1268 introduction diff and manual.
Gameplay/XML/Python source untouched.

F691 / provisional KI#1012 -- selecting a vassal chapter civ erases its initial CIV-score baseline
--------------------------------------------------------------------------------------------------

ROOT / CURRENT CODE
-------------------
At chapter start, RFChapterScore::atChapterStart calls:

    computeRank(true)

which is responsible both for the initial rank and for capturing every civilization's initial CIV
score. The capture loop, however, rejects vassals before it checks whether the loop civilization is
the civilization being scored:

    CvPlayer const& they = GET_PLAYER((PlayerTypes)i);
    if(!they.isAlive() || they.isMinorCiv() || they.isAVassal()) {
        if(storeCivScores)
            initialCivScores[i] = 0;
        continue;
    }
    if(storeCivScores)
        initialCivScores[i] = modifiedCivScore(they.getID());

Therefore, if the human legitimately starts a chapter as a vassal, the chapter civilization's own
entry is forcibly stored as zero:

    initialCivScores[chapter->getCiv()] == 0

The later score update nevertheless uses that entry as the chapter civilization's historical
baseline:

    int initialCivScore = initialCivScores[chCiv];
    ...
    initialCivScorePercent = round(100.0 * initialCivScore / referenceScore);
    fromInitialCivScore = -initialCivScorePercent;

so a selected vassal receives no subtraction at all for the CIV score it already possessed when the
chapter began.

SUPPORTED VASSAL-SELECTION CONTRACT
-----------------------------------
This is not an unsupported team state. Rise/Fall deliberately allows vassals as chapter choices.
Current `RiseFall::isEligible` rejects dead/minor/off-limits civilizations and prior unscored/repeat
cases; it does NOT reject `isAVassal()` generally. `updateOffLimits` only blocks teams that are
vassals of the most recently controlled active team, matching the rule that the immediately previous
chapter's own vassals are temporarily off limits.

The recommendation ordering makes the policy explicit:

    if(p1.isAVassal() && !p2.isAVassal()) return false;
    if(p2.isAVassal() && !p1.isAVassal()) return true;

which sorts vassals later rather than removing them. The selection hover additionally identifies a
candidate as voluntary/capitulated and names its master. The AdvC manual says the list contains
"vassals not previously played" after non-vassals and explicitly explains that a selected voluntary
vassal may cancel the agreement while a capitulated vassal must grow enough to break free.

`RiseFall::setPlayerControl` changes human control/handicap/UI state but does not dissolve the vassal
agreement before `RFChapter::start()` calls `breakdown.atChapterStart(*this)`. Thus a selected vassal
reaches the faulty capture exactly in its supported vassal state.

DOCUMENTED SCORING CONTRACT
---------------------------
The manual defines chapter score as measuring how much the civilization improved since the start of
the chapter. For the direct CIV-score component it explicitly says that the chapter-start CIV score,
expressed as a percentage of the reference rank's score, is SUBTRACTED from the current CIV-score
percentage.

There is no documented exception for vassal chapters. Vassal status affects candidate ordering and
rank population, but not the stated before-vs-after CIV-score baseline. Zeroing the selected vassal's
own initial score therefore violates the scoring contract rather than implementing an alternative
vassal-scoring rule.

DETERMINISTIC SCORE WITNESS
---------------------------
Consider a later chapter whose selected vassal begins with CIV score 400 while four eligible
non-vassal major civilizations have initial scores 800, 700, 600 and 500.

For rank purposes it is coherent to exclude vassal rivals. The selected vassal compares below those
four non-vassals, so its initial rank is 5 with four counted rivals. The sorted initial reference
scores contain 800,700,600,500 and `referenceRank2` clamps rank 5 to rank 4, yielding
`referenceScore=500`.

The correct initial CIV-score baseline is therefore:

    round(100 * 400 / 500) = 80 points
    fromInitialCivScore = -80

Current code instead stored the selected vassal's own score as zero, producing:

    round(100 * 0 / 500) = 0
    fromInitialCivScore = 0

If the current vassal score remains 400 and no lower non-vassal score exists, the existing fallback
uses its own score as `nextBestScore`, so `fromCivScore=100`. With unchanged rank and no retirement
bonus, the direct current code can therefore award +100 where the same documented before/after
arithmetic would yield only +20. The exact inflation varies with scores/rank, but the missing initial
baseline is deterministic whenever a later chapter starts with a vassal civilization.

ANCESTRY / UNUSED INTENT CLUE
-----------------------------
AdvC practical 1268 / e11525d303... introduced Rise/Fall, vassal selection and RFChapterScore in the
same initial feature commit. Its first `computeRank` implementation already:
- declared `bool weVassal = TEAMREF(we).isAVassal();` for the selected chapter civilization;
- skipped all vassals before storing `initialCivScores`;
- never used `weVassal`.

Later refactors changed the expression to `we.isAVassal()` but still did not use it. Practical 2535 /
d6f5189db4... finally removed `weVassal` specifically while fixing compiler warnings about unused
locals, leaving the self-zeroing behavior untouched. This is not proof by itself, but it is consistent
with a vassal-specific case having been anticipated at feature birth and never completed.

Current Base AdvC 1.14 retains the same ordering and zero-baseline defect. Current SAS differs from
Base only in formatting on this file's relevant executable body and inherits the behavior.

REFERENCE-VECTOR / INDEX CONTROL
--------------------------------
This finding does NOT require `initialCivScoresSorted` to contain the selected vassal. The intended
ranking reference can remain restricted to non-vassals; only the selected civilization's own
historical numerator needs preservation.

A supported live vassal necessarily has a living master team, so ordinary vassal selection supplies
at least one non-vassal major civilization to the stored reference-score population. The independent
`referenceRank2 - 1` underflow question remains under Queue-098 closure, but it is not needed for
F691.

WHOLE-ALBUM DEDUP
-----------------
Whole-album search found no prior owner for a selected Rise/Fall vassal losing its own initial
chapter CIV-score baseline. Existing vassal-score findings concern different systems: e.g.
F129/KI#452 is UWAI vassal technology-score cache domain and F483/KI#806 is BtS team-score allocation
when a master teammate dies. F691 is local to Rise/Fall chapter-start score capture.

REPAIR DIRECTION
----------------
Preserve the deliberate exclusion of vassals from the rival/reference population, but capture the
chapter civilization's own starting score before that exclusion. For example, conceptually:

    if(storeCivScores) {
        if(they.getID() == we.getID())
            initialCivScores[i] = modifiedCivScore(they.getID());
        else if(!they.isAlive() || they.isMinorCiv() || they.isAVassal())
            initialCivScores[i] = 0;
        else
            initialCivScores[i] = modifiedCivScore(they.getID());
    }

with the existing rank-loop skip policy left separate. A cleaner structure would distinguish
"capture our baseline" from "eligible rival/reference-score population" explicitly so a ranking
filter cannot silently erase state later consumed as the chapter civilization's own history.

QUEUE-098 CONTINUATION CURSOR
-----------------------------
Continue from F692 / provisional KI#1013 UNUSED:
- finish `initialCivScoresSorted[referenceRank2 - 1]` nonempty/index proof independently of F691;
- investigate `RFChapter::getRemainingTimePercent`, especially defeated chapter civs versus voluntary
  retirement and `retireTurn == -1`, before classifying any timing issue;
- finish dead/current winner override and player-rank sentinel handling;
- finish negative-score sqrt, percentage/divisor and integer-range boundaries;
- finish read/write/frozen-score/chapter-pointer lifetime contracts and F663 lifecycle dedup;
- finish all RFChapterScore history strata and final whole-album/mechanical pass;
- either promote an independent consequential F692 or close Queue098 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP600 physical -> Queue098 OPEN -> F689/KI#1010 + F690/KI#1011 + F691/KI#1012 CONFIRMED ->
F692/KI#1013 UNUSED -> reference-index + remaining-time/death + arithmetic/serialization/history
closure -> promote only independent consequential root or close Queue098 -> freeze next WIP.

Files complete: 108 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP601 - QUEUE 098 RFChapterScore.cpp - F692 / KI#1013 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP600.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, current
RFChapter/RiseFall elimination + retirement lifecycle, CvPlayer/CvGame score refresh, AdvC
practical-1268 introduction diff and current Rise/Fall localization. Gameplay/XML/Python source
untouched.

F692 / provisional KI#1013 -- defeat feeds retireTurn=-1 into retirement math, awarding and displaying a false retirement bonus
-------------------------------------------------------------------------------------------------------------------------------

ROOT / CURRENT CODE
-------------------
`RFChapter::getRemainingTimePercent()` deliberately distinguishes a dead chapter civilization from
an ordinary live preview:

    int t = getRetireTurn();
    if(t < 0 && (getCiv() == NO_PLAYER ||
            GET_PLAYER(getCiv()).isAlive())) // Defeat is not retirement
        t = GC.getGame().getGameTurn();
    return (length <= 0 ? 100 :
            fmath::round((length - t + getStartTurn() - 1.0) /
                    (0.01 * length)));

For a live, not-retired chapter, `retireTurn==-1` is replaced by the current turn so the Victory
Screen can preview the bonus for retiring now. For an actually defeated chapter civilization,
however, the source comment explicitly says "Defeat is not retirement" and therefore skips that
assignment -- but then leaves the sentinel `t==-1` in the arithmetic instead of returning zero or
otherwise disabling the retirement bonus.

For every finite chapter this transforms the sentinel into:

    remainingTimePercent = round((length + startTurn) / (0.01 * length))

because `-(-1) - 1` cancels. Chapter 1 therefore reports 100% unspent on defeat regardless of when
defeat occurred, and every later chapter can report substantially MORE than 100% because its
absolute `startTurn` is added to the numerator. An endless/nonpositive-length chapter returns 100%
unconditionally, so the dead-civ case is wrong there as well.

LIVE DEFEAT ACTIVATION / RETIREMENT CONTRAST
--------------------------------------------
This is the ordinary Rise/Fall defeat path, not a hypothetical direct call:

1. `CvPlayer::setAlive(false)` sets `m_bAlive=false`, performs elimination cleanup, marks score dirty,
   then calls `RiseFall::reportElimination(getID())` when Rise/Fall is enabled.
2. If the eliminated civilization is the current chapter civilization,
   `reportElimination()` immediately calls `chapters[pos]->score()` BEFORE transferring control.
3. `RFChapter::score()` calls `breakdown.update()` and then freezes `scoreTimestamp`.
4. `RFChapterScore::update()` force-refreshes game scores, obtains
   `chapter->getRemainingTimePercent()`, and computes:

       fromRemainingTime = max(0, round(total * remainingTimePercent / 200.0));
       total += fromRemainingTime;

Ordinary retirement is separate and explicit. `RiseFall::retire()` is the sole current setter of a
nonnegative retirement timestamp:

    if(chapters[pos]->getRetireTurn() < 0)
        chapters[pos]->setRetireTurn(g.getGameTurn());

A civilization defeated without having retired therefore reaches the score path with
`retireTurn==-1` exactly as described above. There is no elimination-side setter that converts the
sentinel to a real retirement turn.

DEAD-PLAYER SCORE REFRESH DOES NOT MASK THE ROOT
-----------------------------------------------
The elimination score call correctly refreshes scores after the civilization is dead.
`CvPlayer::calculateScore(false)` returns zero for a dead player, so the defeated chapter
civilization's current direct CIV-score term normally becomes zero. That initially looks capable of
making the pre-time total nonpositive and masking the arithmetic bug, but rank scoring remains an
independent positive source.

`computeRank(false)` skips dead rivals while `initialRivals` remains the fixed count recorded at
chapter start. If several original rivals have also been eliminated, a civilization that started
near the bottom can therefore still have positive rank-improvement points at the instant it is
itself defeated. A low initial CIV-score subtraction can leave a positive pre-time total even with
`fromCivScore==0`.

DETERMINISTIC DEFAULT-NORMAL WITNESS
------------------------------------
Current default Rise/Fall settings use 7 base chapters, 8-turn interludes, zero chapter-length shift,
and Normal speed has 500 scheduled turns. With an Ancient turn-0 start and enough civilizations:

- `playLength = 500 - 6*8 = 452`;
- the first six chapters round to 65 turns each;
- Chapter 2 therefore starts on turn 73 and has length 65.

Suppose the selected non-vassal civilization began that chapter ranked 8th among 8 majors
(`initialRivals=7`, `initialRank=8`), with its initial CIV score equal to 20% of the later
rank-reference score. By the time it is defeated, four of its seven original rivals have also been
eliminated, leaving three live non-vassal rivals. The forced score refresh gives the dead chapter civ
CIV score 0, while `computeRank(false)` gives rank 4. The ordinary pre-time components can then be:

    fromRank            = round(100 * (7 - 4 + 1) / 7) = +57
    fromInitialRank     = round(-100 * (7 - 8 + 1) / 7) =   0
    fromCivScore        = 0
    fromInitialCivScore = -20
    ----------------------------------------------------------
    pre-time total                                      = +37

Because defeat leaves `t=-1`, Chapter 2 computes:

    remainingTimePercent = round((65 + 73) / 0.65) = 212%
    fromRemainingTime     = round(37 * 212 / 200)   = +39
    current total before any repeat penalty          = 76

A defeat with no prior retirement should contribute no retirement bonus, leaving 37 at this stage.
The sentinel bug instead more than doubles this valid witness. Later chapters can inflate the
percentage much further because the absolute chapter start turn grows while the denominator remains
only the chapter length.

PLAYER-FACING CONTRADICTION EVEN WHEN THE BASE TOTAL IS NONPOSITIVE
------------------------------------------------------------------
This root is observable even in defeat states where `max(0, ...)` prevents extra points.
`RFChapterScore::updateString()` says:

    if(!isScored || remainingTimePercent > 0) {
        ...
        else getText("TXT_KEY_RF_CHAPTER_SCORE_RETIRED", ...);
    }

and current English localization is:

    +%d1 points    from retiring (%d2%% time unspent)

A defeated chapter is scored immediately, and the sentinel calculation yields a positive
`remainingTimePercent` (at least 100% in the cases above). The frozen breakdown therefore describes
an ordinary defeat as "from retiring" even when the player never retired and even when the bonus
happens to clamp to +0. That directly contradicts the source's own "Defeat is not retirement"
comment as well as the separate retirement timestamp lifecycle.

ANCESTRY / BASE ORACLE
----------------------
AdvC practical 1268 / e11525d303... introduced all relevant pieces together:
- `RFChapter::getRemainingTimePercent()` with the exact dead-civ condition and the comment
  "Defeat is not retirement";
- `RFChapterScore::update()` applying half the remaining-time percentage to positive chapter score;
- `RiseFall::reportElimination()` scoring the current chapter immediately on defeat;
- `RiseFall::retire()` as the explicit writer of `retireTurn`.

No later semantic history repair changes this interaction. Supplied Base AdvC 1.14 retains the same
`getRemainingTimePercent()` implementation and therefore the same defect. Current SAS inherits it
unchanged.

WHOLE-ALBUM DEDUP
-----------------
No prior album root owns defeat being interpreted through the retirement-time sentinel. The nearest
Rise/Fall lifecycle finding, F663/KI#985, concerns a DIFFERENT stage: a civilization selected for a
future chapter can die before takeover and is incorrectly routed into defeat/scoring instead of the
explicit retry-selection branch. F692 starts only after a chapter civilization was legitimately
under player control and is actually defeated; its root is the `retireTurn==-1` sentinel leaking
into retirement score arithmetic.

F689 concerns stale repeat-breakdown state, F690 the raw breakdown-string lifetime and F691 the
vassal initial CIV-score baseline. None owns this timing/retirement root.

REPAIR DIRECTION
----------------
Keep live retirement previews and actual retirement timestamps distinct from defeat. Conceptually,
`getRemainingTimePercent()` should handle the dead, never-retired chapter before using the retirement
formula, e.g. return 0 when:

    getRetireTurn() < 0 && getCiv() != NO_PLAYER && !GET_PLAYER(getCiv()).isAlive()

then use the current turn only for a live no-retirement preview and the stored timestamp for an
actual retirement. This ordering also prevents an endless final chapter from taking the current
`length<=0 ? 100` shortcut on defeat.

Do not simply stamp the defeat turn into `retireTurn`: retirement is separately queried by
`RiseFall::hasRetired()` and controls autoplay/UI semantics, so conflating a defeat with an actual
retirement would spread the bug into lifecycle state. A score-calculation return of zero for
"defeated and never retired" preserves the existing semantic distinction.

QUEUE-098 CONTINUATION CURSOR
-----------------------------
Continue from F693 / provisional KI#1014 UNUSED:
- finish `initialCivScoresSorted[referenceRank2 - 1]` nonempty/index proof independently of F691;
- finish dead/current winner override and player-rank sentinel handling after the F692 death proof;
- finish negative-score sqrt, percentage/divisor and integer-range boundaries;
- finish read/write/frozen-score/chapter-pointer lifetime contracts and F663 lifecycle dedup;
- finish the later RFChapterScore semantic history strata after practical 1268;
- whole-album dedup and final mechanical pass;
- either promote an independent consequential F693 or close Queue098 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP601 physical -> Queue098 OPEN -> F689/KI#1010 + F690/KI#1011 + F691/KI#1012 +
F692/KI#1013 CONFIRMED -> F693/KI#1014 UNUSED -> reference-index + dead/winner/rank +
arithmetic/serialization/history closure -> promote only independent consequential root or close
Queue098 -> freeze next WIP.

Files complete: 108 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP602 - QUEUE 098 RFChapterScore.cpp CLOSED; QUEUE 099 CvDLLLogger.cpp OPEN
===============================================================================
Physical parent: C031-WIP601.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, RFChapter/
RiseFall/CvGame/CvPlayer contracts, current civilization + technology XML, BtS score constants from
local Civ4CE source, all reachable RFChapterScore history strata and whole-album dedup.
Gameplay/XML/Python source untouched.

QUEUE 098 FINAL RESULT
----------------------
Close Queue 098 `CvGameCoreDLL/RFChapterScore.cpp` with four independent inherited Rise/Fall roots:

- F689/KI#1010: repeat chapter can display a stale cached repeat deduction after a positive ->
  nonpositive live score transition;
- F690/KI#1011: final owned `breakdownString` leaks when RFChapterScore is destroyed because the class
  owns a raw pointer but has no destructor;
- F691/KI#1012: selecting a supported vassal chapter civilization zeros its own initial CIV-score
  baseline before later subtracting that baseline;
- F692/KI#1013: defeat leaves `retireTurn==-1` in retirement arithmetic, producing false/high
  "time unspent" and potentially awarding retirement points to a defeat.

No independent F693 survives the remaining index/rank/arithmetic/serialization/history pass.

REFERENCE-SCORE VECTOR / INDEX PROOF
------------------------------------
The sharp remaining expression is:

    referenceRank2 = std::min(referenceRank,
            (int)initialCivScoresSorted.size());
    int referenceScore = initialCivScoresSorted[referenceRank2 - 1];

A zero-size vector would make the subscript `-1`, so this was checked against supported current
chapter starts rather than dismissed from the nearby assertion.

`referenceRank` cannot be zero: `computeRank` begins at rank 1, only increases it by comparisons/ties,
adds 0.01 and rounds; `nextRankFound` can only choose `rank+1` rather than decrease it.

The initial-score vector also has at least one positive member on supported current data:
- If the selected chapter civilization is non-vassal, `computeRank(true)` stores its own current score.
  All 36 current playable civilizations have exactly two starting technologies. Current technology
  data has max-tech score 347 and the inherited normal tech-score factor is 2000, so two Ancient
  starting technologies alone round to about 12 score points even before population/land/wonders.
  Later legitimate chapter starts have at least the same accumulated-tech floor unless unusual
  unsupported data removes those technologies.
- If the selected civilization is a vassal, F691 incorrectly zeros the selected civ's own entry, but
  a live vassal has a live non-vassal master. That master remains in the initial reference population
  and likewise has a positive current score. F691 therefore corrupts the numerator while not making
  this vector empty.

Thus `initialCivScoresSorted.size() >= 1` and `referenceRank >= 1` imply `referenceRank2 >= 1` for the
supported current tree. Malformed/custom score configurations could violate that implicit contract,
but no current shipped activation justifies F693 for the raw subscript.

DEAD / WINNER / RANK SENTINEL CONTROLS
--------------------------------------
`modifiedCivScore`'s winning-AI override is deliberate Rise/Fall scoring policy: if an AI-controlled
civilization has actually won, it is treated as one point above the raw rank-0 score. `getRankPlayer`
is populated for every player by `CvGame::updateScore`; winner teams are live when this policy is
used. The `(PlayerTypes)0` cast is a historical typed-enum adaptation of rank index zero, not a player
ID lookup at that call site.

On elimination, `updateScore()` gives the dead chapter civilization score zero because
`CvPlayer::calculateScore` rejects dead players. Its player-rank slot is still populated in the full
rank table, so the later `getPlayerRank(chCiv)` read is defined. The resulting rank/current-score
behavior was part of the F692 proof and exposes no second invalid-index root.

`nextBestScore` is protected independently: if no positive lower score is found, it falls back to
`max(civScore,1)`. Current player scores are nonnegative, so the CIV-score percentage divisor cannot
be zero on supported state.

ARITHMETIC / TIE / NEGATIVE-SCORE CONTROLS
------------------------------------------
Rank percentage denominators use `max(1, initialRivals)`, so a one-civilization/no-rival case does not
divide by zero. Tie ranking uses half-rank increments and the explicit +0.01 before nearest-integer
rounding; this implements an average-rank-style tie treatment rather than exposing an integer
truncation defect.

The large remaining percentage terms are bounded comfortably within 32-bit range under current Civ4
score/turn scales. F692 can make `remainingTimePercent` nonsensically greater than 100, but its source
is already owned by that finding rather than a second overflow root.

Negative score softening:

    if(total < -9)
        totalScaled = -round(sqrt(-1.0 * total) * 3);

promotes through `-1.0` before negation, so even the theoretical INT_MIN sign edge is converted to
double instead of overflowing an integer unary minus. Current score components are far below that
boundary in any case. The transformation is explicit authored scoring policy.

SERIALIZATION / FROZEN-SCORE / POINTER-LIFETIME CONTROLS
--------------------------------------------------------
RFChapterScore write/read order is symmetric for every persisted scalar and all
`MAX_CIV_PLAYERS` initial scores. `breakdownString` is intentionally regenerated rather than
serialized. Its destruction leak is already F690; this pass found no second stream-layout root.

`RFChapter::read` passes `this` back into `breakdown.read` when the chapter has started. Future
chapters retain a NULL score-owner pointer until `RFChapter::start()` calls `atChapterStart`, which
sets the pointer and captures the baseline. Scored chapters have necessarily started, so their frozen
breakdowns regain the owner pointer after load and `update()` regenerates only the display string
without recomputing frozen score fields.

`RFChapterScore` is embedded by value inside `RFChapter`; the live chapter vector owns pointers to
RFChapter objects and does not copy/move RFChapterScore instances. Therefore F690's missing Rule of
Three is important when repairing ownership, but no current copy-induced double-free exists today.

The pre-takeover dead-civilization lifecycle remains F663/KI#985, not a new RFChapterScore root. That
path can mark a not-yet-started chapter scored with no captured baseline because RiseFall routes it
through defeat instead of retrying selection. Queue098 does not renumber that lifecycle bug merely
because its zero/early-return consequence lands in this class.

HISTORY CLOSURE
---------------
All reachable RFChapterScore history after practical 1268 was classified:
- 1269 deliberately normalizes first-chapter initial CIV score to 100%; authored special case;
- 1270 adds victory-stage ranking and carries the unused `weVassal` clue later relevant to F691;
- 1271 adds the optional victory-stage-ignore parameter but leaves current-score ranking using
  victory stages by design; the comment explicitly records the alternative rather than promising it;
- 1388 forces the chapter-start score refresh, fixing a stale-initial-score possibility;
- 1368/1386/1469/1493/1497/1548/1552/1564/1590/1679 and later 2920/2954/3218/3392/3746 changes are
  constness, includes, inline-accessor/refactor, warning cleanup or enum typing on the surviving body;
- 2535 removes the long-unused `weVassal` local but does not repair F691;
- SAS practical 5961 only collapses the `computeRank` signature formatting.

No later Base AdvC 1.14 semantic fix exists for F689-F692, and no unported Base repair remains in this
file.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 099 `CvGameCoreDLL/CvDLLLogger.cpp` (279 queued / 279 current
physical lines), OPEN under F693 / provisional KI#1014 unused. Audit logger initialization,
FILE*/varargs/string-format contracts, runtime log-level gates, path/file ownership, repeated
open/close behavior, null/error handling, threading/reentrancy assumptions and Base/K-Mod/Civ4CE
lineage; dedup any logging-only observations against existing GameRecord/logger findings before
promotion.

Exact continuation stone:
C031-WIP602 physical -> Queue098 CLOSED with F689-F692; F693/KI#1014 UNUSED -> Queue099
CvDLLLogger.cpp OPEN -> provenance + file/format/varargs/lifetime/error/caller/history sweep ->
promote only an independent consequential F693 or close cleanly -> freeze next WIP.

Files complete: 109 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP603 - QUEUE 099 CvDLLLogger.cpp - F693 / KI#1014 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP602.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, current
CvMapGenerator/CvGame map-stat callers, GlobalDefines_devel.xml, AdvC manual and practical-1702
introduction history. Gameplay/XML/Python source untouched.

F693 / provisional KI#1014 -- LOG_MAP_STATS writes through vector[-1] on ordinary featureless plots
--------------------------------------------------------------------------------------------------

ROOT
----
`CvDLLLogger::logMapStats()` allocates feature-count vectors with exactly one element per real XML
feature:

    std::vector<int> landFeatureCounts;
    landFeatureCounts.resize(GC.getNumFeatureInfos());
    std::vector<int> waterFeatureCounts;
    waterFeatureCounts.resize(GC.getNumFeatureInfos());

It then indexes those vectors unconditionally with the plot's `FeatureTypes` value:

    if (kPlot.isWater()) {
        ...
        waterFeatureCounts[kPlot.getFeatureType()]++;
    }
    else {
        ...
        landFeatureCounts[kPlot.getFeatureType()]++;
    }

Ordinary plots without a forest/jungle/oasis/ice/etc. return `NO_FEATURE`, the negative sentinel used
throughout CvPlot and the rest of the DLL. Passing that negative enum to `std::vector<int>::operator[]`
converts it to a huge unsigned `size_type`, so the increment writes outside the vector. There is no
bounds check and the operation has undefined behavior; a debug allocator may expose it quickly,
while a release build can corrupt adjacent heap memory or crash later.

LIVE SUPPORTED ACTIVATION
-------------------------
The map-stat facility is disabled by default but is an explicitly shipped AdvC diagnostic, not a dead
private helper. `Assets/XML/GlobalDefines_devel.xml` documents:

    LOG_MAP_STATS = 1 + MessageLog=1
        -> write terrain statistics to MPLog each time a map is generated

and the AdvC manual separately documents `advc.mapstat` and how to enable it.

There are two current native calls:
- `CvMapGenerator::afterGeneration()` -> `GC.getLogger().logMapStats()`;
- `CvGame::normalizeStartingPlots()` -> `GC.getLogger().logMapStats(true)`.

Once both MessageLog and LOG_MAP_STATS are enabled, either call enters the full plot loop. Standard
shipped maps necessarily contain ordinary featureless terrain: CvPlot reset starts with
`m_eFeatureType=NO_FEATURE`, and feature generation only places features on qualifying plots rather
than filling every tile. Thus the first featureless land or water tile triggers the invalid write.

The early logger gate does not protect the loop once the documented feature is enabled:

    if (!isEnabled() || !GC.getDefineBOOL("LOG_MAP_STATS"))
        return;

`isEnabled()` is initialized from the MessageLog setting, precisely the other documented prerequisite.

ANCESTRY / BASE PERSISTENCE
---------------------------
AdvC practical 1702 / 4b162eddaa... introduced `advc.mapstat`, its call from
`CvMapGenerator::afterGeneration`, the feature vectors and both unconditional feature-index writes in
one commit. The defect is therefore present from feature birth.

Later practicals add river statistics, after-normalization output, typed getters and formatting but
do not guard NO_FEATURE. Current supplied Base AdvC 1.14 is source-identical to SAS for this file and
retains the same writes. K-Mod/Civ4CE do not provide this AdvC-only map-stat implementation as a
repair oracle.

WHOLE-ALBUM DEDUP / SCOPE
-------------------------
Whole-album search finds no previous owner for `LOG_MAP_STATS` feature counting or an out-of-bounds
NO_FEATURE write in CvDLLLogger. Existing map-generation and feature-sentinel findings concern their
own gameplay/evaluator/XML consumers; this root is local to the optional map-stat diagnostic loop.

The finding is intentionally scoped as a diagnostic-feature crash/memory-corruption bug rather than
normal default gameplay: `LOG_MAP_STATS` ships at 0. That lower default activation does not make the
code safe when the documented switch is used.

REPAIR DIRECTION
----------------
Guard the feature sentinel before indexing, just as the same loop already does for NO_BONUS:

    FeatureTypes eFeature = kPlot.getFeatureType();
    if (eFeature != NO_FEATURE) {
        if (kPlot.isWater())
            waterFeatureCounts[eFeature]++;
        else
            landFeatureCounts[eFeature]++;
    }

Terrain types remain valid per initialized map-plot contract; resource counting already has its
NO_BONUS guard. If featureless tiles are desirable in the report, count them in a separate scalar or
an explicitly offset bucket rather than using the negative sentinel as a vector index.

QUEUE-099 CONTINUATION CURSOR
-----------------------------
Continue from F694 / provisional KI#1015 UNUSED:
- finish map-stat terrain/resource/area/divisor and temporary-string lifetime checks after F693;
- audit fixed 1024-byte sprintf buffers in random/combat/stuck logging against bounded/unbounded text;
- audit `out.str().c_str()` and temporary `CvString(...).c_str()` argument lifetimes through the
  synchronous `messageControlLog` contract;
- audit random-log filename/custom-data formatting and per-player logging paths;
- classify every later CvDLLLogger history stratum and Base/K-Mod/Civ4CE divergence;
- whole-album dedup and final mechanical pass;
- either promote an independent consequential F694 or close Queue099 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP603 physical -> Queue099 OPEN -> F693/KI#1014 CONFIRMED -> F694/KI#1015 UNUSED ->
remaining map-stat + sprintf/string lifetime + random/logging/history/dedup sweep -> promote only an
independent consequential F694 or close cleanly -> freeze next WIP.

Files complete: 109 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP604 - QUEUE 099 CvDLLLogger.cpp - F694 / KI#1015 CONFIRMED; QUEUE OPEN
===============================================================================
Physical parent: C031-WIP603.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14,
Civ4CE BtS source, CvPlayer::setTurnActive current caller structure and AdvC practical-1648
extraction history. Gameplay/XML/Python source untouched.

F694 / provisional KI#1015 -- turn-deactivation MessageLog entries are mislabeled "Turn ON"
--------------------------------------------------------------------------------------------

ROOT
----
`CvDLLLogger::logTurnActive(PlayerTypes ePlayer)` has no active-state parameter and always emits:

    std::sprintf(szOut, "Player %d Turn ON\\n", ePlayer);

`CvPlayer::setTurnActive(bool bNewValue, bool bDoTurn)` invokes that same helper in both state
branches:

    if (isTurnActive())
        ... GC.getLogger().logTurnActive(getID());
    else
        ... GC.getLogger().logTurnActive(getID());

The second call executes only after `m_bTurnActive = bNewValue` has made the player inactive, yet the
helper still writes `Turn ON`. Every logged OFF transition is therefore represented as an ON
transition.

LIVE SUPPORTED ACTIVATION / CONSEQUENCE
---------------------------------------
This is the ordinary turn lifecycle, not a dead diagnostic hook. `setTurnActive` calls the logger on
both activation and deactivation whenever the state actually changes. `CvDLLLogger` is enabled by
the shipped `MessageLog` CivilizationIV.ini setting; the class header explicitly exists to
encapsulate `messageControlLog` output under that setting.

With MessageLog enabled, a normal sequential turn can therefore contain two indistinguishable
`Player N Turn ON` records: one at true activation and one at false deactivation. In multiplayer,
HotSeat/PBEM, autoplay and OOS investigation, the log can no longer be read as an accurate turn-state
transition trace and can falsely suggest a second activation where the player actually ended its
turn.

This is diagnostic/logging correctness rather than gameplay-state corruption, but the album already
tracks source-proven diagnostic roots when the shipped diagnostic contract itself is wrong. Unlike a
cosmetic comment typo, this changes the factual event represented in an executable log.

ANCESTRY / ORACLE
-----------------
The regression is pinned to AdvC practical 1648 / 553b1a0f8d, which extracted several direct
`messageControlLog` blocks from their original classes into the new `CvDLLLogger` wrapper.

Immediately before the extraction, the two `CvPlayer::setTurnActive` branches were distinct:

    true branch  -> sprintf(..., "Player %d Turn ON\\n", getID());
    false branch -> sprintf(..., "Player %d Turn OFF\\n", getID());

Practical 1648 replaced both blocks with:

    GC.getLogger().logTurnActive(getID());

while creating the helper with a hardcoded `Turn ON` body. Thus the extraction preserved the call
sites but dropped the state bit needed to preserve the original semantics.

Civ4CE's BtS source provides an independent Firaxis-lineage oracle and still has the explicit ON and
OFF strings in the corresponding branches. Current supplied Base AdvC 1.14 retains the extracted
hardcoded-ON implementation, and current SAS is identical on this behavior. No later AdvC history
stratum repairs it.

WHOLE-ALBUM DEDUP
-----------------
Whole-album searches for `logTurnActive`, `Turn ON`, turn-transition logging and equivalent
mislabeling find no existing owner. Existing SASGameRecord/GameRecord logging findings concern
separate structured telemetry fields, argument/context errors, truncation or event provenance; none
owns this MessageLog state-polarity regression.

REPAIR DIRECTION
----------------
Preserve the original two-state contract explicitly. Narrow options include:

    void logTurnActive(PlayerTypes ePlayer, bool bActive)

and formatting `bActive ? "ON" : "OFF"`, with callers forwarding the state, or two clearly named
`logTurnOn` / `logTurnOff` helpers. Deriving state inside the helper from `GET_PLAYER(ePlayer)` would
also work at the current call positions, but an explicit parameter better preserves the event being
logged and avoids coupling the logger to caller timing.

QUEUE-099 CONTINUATION CURSOR
-----------------------------
Continue from F695 / provisional KI#1016 UNUSED:
- finish `logRandomNumber` fixed-buffer and filename/message contracts, including caller-origin
  bounds and dynamic message possibilities;
- finish `logCombat` / `logUnitStuck` fixed 1024-byte formatting safety against player/unit names;
- close remaining map-stat denominator/index/terrain/resource/area contracts after F693;
- retain the full-expression temporary-string lifetime proof for `out.str().c_str()` and
  `CvString(szCivNames).c_str()`;
- classify practicals 1649/1662/1678/1679/1753/1776/1798/1887/1949/1955/2000/2141/2194/2291/
  2545/2549/2582/2585/2684/2957/2963/2980/2998/3052/3131/3179/3183/3392/3579 and SAS-only comment/
  formatting strata, distinguishing temporary historical regressions that later repairs removed;
- final whole-file pointer/divisor/format/index/reentrancy/dedup sweep;
- either promote an independent consequential F695 or close Queue099 cleanly;
- freeze the next physical WIP at the next settled boundary.

Exact continuation stone:
C031-WIP604 physical -> Queue099 OPEN -> F693-F694/KI#1014-KI#1015 CONFIRMED ->
F695/KI#1016 UNUSED -> remaining fixed-buffer + map-stat + temp-lifetime + history/dedup sweep ->
promote only independent surviving root or close Queue099 -> freeze next WIP.

Files complete: 109 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP605 - QUEUE 099 CvDLLLogger.cpp CLOSED; QUEUE 100 CvInfo_Water.cpp OPEN
===============================================================================
Physical parent: C031-WIP604.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, Civ4CE BtS,
current CvRandom/CvPlayer/CvUnit/CvSelectionGroupAI/CvMapGenerator callers, shipped Python map/event
scripts and full reachable CvDLLLogger history. Gameplay/XML/Python source untouched.

QUEUE 099 FINAL RESULT
----------------------
Close Queue 099 `CvGameCoreDLL/CvDLLLogger.cpp` with two independent inherited AdvC roots:

- F693/KI#1014: enabling the documented LOG_MAP_STATS diagnostic indexes the feature-count vector
  with NO_FEATURE=-1 on ordinary featureless plots, causing an out-of-bounds write;
- F694/KI#1015: practical-1648 logger extraction dropped the OFF-state bit, so ordinary player-turn
  deactivations are written to MessageLog as `Turn ON`.

No independent F695 survives the fixed-buffer, map-stat, temporary-lifetime, random-log or history
closure pass.

FIXED 1024-BYTE FORMAT BUFFER CONTROLS
--------------------------------------
`logRandomNumber`, `logCombat` and `logUnitStuck` retain inherited unbounded `std::sprintf` calls into
1024-byte stack buffers. This is an undesirable robustness surface, but a current supported overflow
was not established strongly enough for F695.

For RNG logging, the current dominant native macros use compact `CALL_LOC_STR` strings consisting of
function name plus line number. A mechanical shipped-Python caller census found 55 direct
`getSorenRandNum` / `getMapRandNum` calls and every one supplies a string literal; the longest literal
is 44 characters (`Hurricane event expensive building destroyed`). Integer decorations add only a
small fixed suffix. The CyGame API can be called by third-party Python with an arbitrarily long
message, but that is extension hardening rather than a defect reached by current shipped callers.

For combat/stuck logging, manual unit rename is truncated to 25 characters by CvEventManager. BUG's
optional Unit Naming system and WorldBuilder/scenario APIs can supply names outside that manual cap,
so the sink should ideally be replaced with bounded formatting or a dynamic CvString; however, the
current shipped defaults/data do not provide a natural near-1024-byte name. Deliberately extreme
custom naming/scenario data is kept as robustness debt rather than promoted as a normal supported
crash root.

MAP-STAT REMAINING ARITHMETIC / INDEX CONTROLS
-----------------------------------------------
After separating F693's NO_FEATURE write:
- plot type indexes are valid for initialized map plots;
- terrain indexes are initialized by the time the after-generation/after-normalization logger runs;
- bonus/resource indexes are explicitly guarded against NO_BONUS before vector indexing;
- the land-count assertion documents the expected normal-map contract, and zero-count percentage
  categories return before division;
- a zero-water map therefore does not divide by zero because every water absolute count is zero and
  `appendPercentage` returns before computing the ratio;
- `getCivPlayersEverAlive()` is nonzero in the supported game/map-generation lifecycle; map stats are
  not called as a standalone no-player utility;
- area iteration and landmass classification use current valid area objects after map generation.

`appendPercentage` streams the literal text `"%%"`, so once F693 is repaired the report displays two
percent characters rather than one. This is a genuine tiny diagnostic-formatting typo inherited from
practical 1702, but it does not alter any quantity or control flow and remains below the album's
finding threshold.

TEMPORARY STRING / CONST_CAST CONTROLS
--------------------------------------

    gDLL->messageControlLog(const_cast<char*>(out.str().c_str()));
    gDLL->messageControlLog(const_cast<char*>(CvString(szCivNames).c_str()));

look alarming because the character pointer comes from a temporary. Under C++ full-expression
lifetime rules the temporary `std::string`/`CvString` survives until the synchronous function call
returns, so the pointer is valid for the duration of `messageControlLog`. No evidence was found that
the EXE retains or mutates those buffers after the call; the const_cast is an inherited interface-
constness workaround, not a demonstrated use-after-free.

RANDOM-LOGGER HISTORY CLOSURE
-----------------------------
The full reachable history distinguishes current defects from already-repaired historical ones:
- practical 1648 creates CvDLLLogger and F694 while moving direct logger blocks out of CvPlayer/
  CvUnit/CvRandom/CvSelectionGroupAI;
- 1649 only adds the new source/header to the project;
- 1662 simplifies logger conditions/comments;
- 1678/1679 change includes after AI/base-class separation;
- 1702 creates map-stat logging and F693 (plus the harmless doubled-percent text);
- 1753/1776/1798 are typed getter, plot-access and namespace/style refactors;
- 1887 temporarily changes `iData1 > MIN_INT` to impossible `iData1 < MIN_INT`, suppressing optional
  RNG data decorations; practical 2684 later restores the correct current comparison, so no current
  root remains from that temporary regression;
- 1949/1955/2000 are include/type/refactor strata;
- 2141 moves the RandLog enable check to the hot caller and leaves the logger assertion as intended;
- 2194 moves sstream to the PCH; 2291 changes wide/narrow conversion helper only;
- 2582 adds the optional separate file for CvRandomExtended/AsyncRand; 2585 repairs the associated
  caller/enable contract; 2684 makes that logging path actually work and restores the data sentinel;
- 2957 is compilation repair; 2963 fixes `%ul` to the current `%u` seed format and unsigned-int type;
- 2980 adds the second post-normalization map-stat call and river count without creating a new root;
- 2998/3392 are merge-carried equivalents; 3052/3131/3179/3183 are enum/include/inline/project
  refactors; 3579 adds the True Starts civ-leader list logger;
- SAS 5043/5304 are comment-only and SAS 5961 formatting-only in this file.

No supplied Base AdvC 1.14 repair exists beyond those current bodies; SAS and Base are behaviorally
identical here apart from SAS comments/formatting.

FINAL MECHANICAL / DEDUP CONTROLS
---------------------------------
The logger owns no FILE*, heap buffer or other resource itself; output ownership stays with the EXE
logging interface. Constructor state is immutable enable-state captured by CvGlobals and the class is
noncopyable. No second stale pointer, invalid enum index, signed divisor, mismatched format argument,
or current-history regression survived the end-to-end pass.

Whole-album dedup keeps F693/F694 independent of the structured SASGameRecord diagnostics (including
F053-F060 and later GameRecord roots): those use different sinks, formats and state provenance. No
existing owner covers map-stat NO_FEATURE memory corruption or turn-state polarity in MessageLog.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 100 `CvGameCoreDLL/CvInfo_Water.cpp` (277 queued / 277 current
physical lines), OPEN under F695 / provisional KI#1016 unused.

Audit all three small XML info classes (`CvWaterPlaneInfo`, `CvTerrainPlaneInfo`,
`CvCameraOverlayInfo`): constructor initialization, XML cursor parent/child balance, missing-tag
behavior, color/string lifetime, Fog/Overlay enum parsing, current XML data reality, K-Mod/Civ4CE/
Base provenance, and any engine-facing assumption about these graphics infos. Keep cosmetic spelling
(`Emmisive`) separate from actual XML contract unless source and data disagree.

Exact continuation stone:
C031-WIP605 physical -> Queue099 CLOSED with F693-F694; F695/KI#1016 UNUSED -> Queue100
CvInfo_Water.cpp OPEN -> XML cursor/init/enum/data/provenance sweep -> promote only an independent
current root or close cleanly -> freeze next WIP.

Files complete: 110 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP606 - QUEUE 100 CvInfo_Water.cpp CLOSED; QUEUE 101 RFChapter.cpp OPEN
===============================================================================
Physical parent: C031-WIP605.
Authoritative source: reconciled current SAS light source, supplied Base AdvC 1.14, K-Mod 1.46,
Civ4CE BtS source plus stock BtS WaterPlane/TerrainPlane/CameraOverlay XML and current misc schema.
Gameplay/XML/Python source untouched.

QUEUE 100 FINAL RESULT
----------------------
Close Queue 100 `CvGameCoreDLL/CvInfo_Water.cpp` without a new current finding. F695 remains unused.

All three graphical info classes were read end-to-end and cross-checked against their actual loader,
XML schema, inherited BtS data and K-Mod constructor hardening.

WATER-TRANSITION OPTIONAL-TAG CURSOR TRAP -- REAL, BUT CURRENT DATA MASKS IT
----------------------------------------------------------------------------
`CvWaterPlaneInfo::read` has a fragile parent-pop structure around `WaterTextures`:

    if (SetToChildByTagName(..., "WaterBaseTexture")) {
        ...
        SetToParent(...);
    }
    if (SetToChildByTagName(..., "WaterTransitionTexture")) {
        ...
        // no SetToParent here
    }
    SetToParent(...);
    ...
    SetToParent(...);

When the transition child exists, the first trailing parent pop returns from
`WaterTransitionTexture` to `WaterTextures`, and the final pop returns to the enclosing
`WaterPlaneInfo`. That is the stock-data path and is balanced.

However, `CIV4MiscSchema.xml` explicitly declares:

    <element type="WaterTransitionTexture" minOccurs="0" maxOccurs="1"/>

If a schema-valid water plane omits that optional child, the failed `SetToChildByTagName` leaves the
cursor at `WaterTextures`; the first trailing parent pop returns to `WaterPlaneInfo`, and the final
unconditional pop then over-pops to `WaterPlaneInfos`. `LoadGlobalClassInfo` expects `read()` to
return with the cursor at the current info node before it calls `NextSibling`; a missing transition
on a non-final record can therefore terminate/misdirect iteration and skip later water-plane infos.

This is inherited from Firaxis BtS and persists unchanged through K-Mod and Base AdvC. It is a real
schema/API robustness defect, but current SAS does not override the vanilla water-plane file and the
stock BtS file contains `WaterTransitionTexture` in both shipped WaterPlaneInfo records. Following the
album's established current-data rule (e.g. other mismatched getter/parser contracts that only fail
for mod-mod data), this does NOT consume F695. Preserve it as a high-quality future/mod-mod repair
note rather than relabeling masked data as a current SAS bug.

A robust repair would give each successful child descent its own matching `SetToParent`, then pop
`WaterTextures` exactly once independently of whether the optional transition exists.

CONSTRUCTOR / MISSING-TAG CONTROLS
----------------------------------
K-Mod practical/commit 1232 initialized the scalar members that Firaxis constructors had left
uninitialized:
- Water plane material alpha, base texture scale, U/V rates;
- Terrain plane visibility flags, material/close alpha, U/V scales/rates, Z height and fog enum;
- Camera overlay visibility and overlay enum.

Current SAS inherits those initializers. Water material color structs are populated from schema-
required MaterialColors children on the supported XML path; the required stock tags are present.
Missing required tags would be malformed data rather than a current supported configuration.

`CvString` texture members own their storage; getters return stable object-owned character buffers.
AdvC practical 2287's removal of trivial string setters replaced setter-via-temporary with direct
`GetChildXmlValByName(m_sz..., ...)` and did not change lifetime semantics.

FOG / CAMERA OVERLAY ENUM PARSING
---------------------------------
Current stock XML values are exactly among the parser's accepted strings:
- terrain plane: `FOG_TYPE_NONE` (the parser also accepts PARALLEL and PROJECTED);
- camera overlays: one DECAL and one ADDITIVE entry.

Unknown strings emit `FErrorMsg` and fall back to initialized/default NONE or DECAL rather than
leaving an invalid enum. No current bad token exists.

SPELLING / XML CONTRACT
-----------------------
The historical `EmmisiveMaterialColor` / `getMaterialEmmisive` spelling is linguistically wrong but
internally consistent across C++ code, schema and stock XML. Renaming only one side would break the
contract; it is not a functional defect.

PROVENANCE / CURRENT-VS-BASE CLOSURE
------------------------------------
Current SAS and supplied Base AdvC 1.14 are executable-identical in this file; differences are only
formatting/comments. The meaningful ancestry is Firaxis BtS -> K-Mod constructor initialization ->
AdvC include/style/DLLExport cleanup and direct-string-reader simplification. No later Base repair is
missing from SAS.

The remaining history entries (initial import, DLLExport cleanup/merge, header splitting, const
CvGlobals work, lazy XML infrastructure, getInfo migration, FErrorMsg cleanup, compiler warnings,
enum-map/header refactors and merges) do not create an additional current semantic root in these
three classes.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 101 `CvGameCoreDLL/RFChapter.cpp` (277 queued / 277 current physical
lines), OPEN under F695 / provisional KI#1016 unused.

Audit chapter lifecycle and ownership as a companion to the just-completed RFChapterScore queue:
constructor/reset/start/end/read/write state, civ selection/retirement/defeat transitions, pointer
ownership and vector integration in RiseFall, timing/sentinel contracts, score-freeze interaction,
and dedup especially against F663/KI#985 and F689-F692/KI#1010-KI#1013. Use Base AdvC history to
separate deliberate Rise/Fall rules from surviving lifecycle bugs.

Exact continuation stone:
C031-WIP606 physical -> Queue100 CLOSED / F695 UNUSED -> Queue101 RFChapter.cpp OPEN -> lifecycle +
serialization + ownership + RiseFall/RFChapterScore cross-contract/history sweep -> promote only an
independent current root or close cleanly -> freeze next WIP.

Files complete: 111 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP607 -- QUEUE 101 RFChapter.cpp -- F695 / KI#1016 CONFIRMED; QUEUE OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP606.
- Queue 101 `CvGameCoreDLL/RFChapter.cpp` (277 queued / 277 current physical lines): OPEN.
- F695 / provisional KI#1016: CONFIRMED.
- F696 / provisional KI#1017: UNUSED.
- Files complete remains 111 / 169 because Queue 101 is not closed yet.
- No gameplay/source/XML/Python file was modified.

F695 / provisional KI#1016 -- no-turn-limit final Rise/Fall chapter is misclassified as already-scored/non-ongoing by the Score tab
-----------------------------------------------------------------------------------------------------------------------------

ROOT / CROSS-FILE CONTRACT
--------------------------
`RFChapter` deliberately represents an endless chapter with a negative end turn:

    int RFChapter::getEndTurn() const {
        if(isEndless())
            return -1;
        return getStartTurn() + getLength() - 1;
    }

and its native lifecycle correctly keeps such a chapter ongoing:

    bool RFChapter::hasEnded() const {
        if(isScored())
            return true;
        if(isEndless())
            return false;
        ...
    }

`CvVictoryScreen.showRiseFall`, however, reconstructs the same state from the exported
`getChapterEnd()` sentinel with:

    extChapter = (endTurn < 0 and gc.getGame().getMaxTurns() > 0)
    ongoing = (currentTurn >= startTurn and
               (currentTurn <= endTurn or extChapter))

The `maxTurns > 0` qualification means that the Python screen recognizes the `-1` sentinel only for
a chapter made endless after a finite-turn game has entered extended play. It does not recognize the
other native source of an endless chapter: a normal final Rise/Fall chapter in a game with no turn
limit.

SUPPORTED CURRENT FIXTURE
-------------------------
This needs no mod-mod data or unsupported configuration.

Current `CIV4VictoryInfo.xml` has exactly one end-score victory:

    VICTORY_TIME: bEndScore = 1

All other shipped victory types have `bEndScore = 0`.

`CvGame::setStartTurnYear()` sets `MaxTurns` from the estimated end turn only if a currently valid
victory has `isEndScore()`. Therefore, with ordinary Time Victory disabled and no custom turn-limit
override, `getMaxTurns()` remains 0.

Rise/Fall explicitly supports that state. `RiseFall::init()` does:

    bool timeLimit = (g.getMaxTurns() > 0);
    ...
    int finalChLen = playLength - partialLength;
    if(!timeLimit)
        finalChLen = -1;
    ...
    if(!chapters.empty() && !timeLimit)
        chapters.back()->setEndless(true);

Rise/Fall's compatibility gates reject multiplayer, multi-member teams, Spah, Permanent Alliances
and One City Challenge; they do not require Time Victory. Disabling Time Victory is therefore a
normal supported Rise/Fall setup.

DETERMINISTIC UI CONSEQUENCES
-----------------------------
During that final no-turn-limit chapter:

- native `RFChapter::hasStarted()` is true;
- native `RFChapter::hasEnded()` is false because `isEndless()` is true;
- native `RiseFall::getCurrentChapter()` therefore returns the final chapter;
- exported `endTurn` is -1 and exported `scoreTurn` is -1.

But in the Score tab:

    extChapter = (-1 < 0 and 0 > 0) = false
    ongoing = (currentTurn >= startTurn and
               (currentTurn <= -1 or false)) = false

for every ordinary nonnegative final-chapter game turn.

That wrong local state has several visible effects in the same loop:

1. The chapter number, civilization and turn cell are not highlighted as the ongoing chapter.
2. The points branch sees `currentTurn >= scoreTurn` because `scoreTurn == -1`, and highlights the
   chapter's *dynamic* current score as though the scoring turn had already been reached.
3. Because `ongoing && !extChapter` never clears `allScored`, `allScored` can remain true while the
   game is still running.
4. The chapter is nevertheless added to `partialScore` because `extChapter` is false.
5. The bottom row therefore labels/highlights that changing partial score with `TXT_KEY_RF_TOTAL`
   rather than `TXT_KEY_RF_PARTIAL_SUM`; the source comment itself says `allScored` is "Only
   relevant when the game is over."
6. The scoring-turn column separately says that the final chapter is scored "at game end," making
   the already-scored visual treatment internally contradictory.

This is presentation/state truth rather than score mutation: the DLL continues to keep the chapter
ongoing and `getChapterScore()` continues to compute the current preview. The Score tab renders that
valid native state incorrectly.

ANCESTRY
--------
AdvC practical 1268 / e11525d303... introduced the complete Rise/Fall feature. The same birth
commit introduced both:

- the C++ negative-end-turn/endless final-chapter representation and no-time-limit setup; and
- the Python `extChapter = (endTurn < 0 and getMaxTurns() > 0)` reconstruction.

Thus this is an initial cross-layer contract mismatch rather than drift from a later refactor.
Later Score-screen edits preserve the predicate. Supplied Base AdvC 1.14 retains it, and current SAS
inherits the same semantic logic (its surrounding table/UI helpers are later presentation changes).

DEDUP / NEGATIVE CONTROLS
-------------------------
Whole-album search found no existing owner for the no-Time-Victory final-chapter Score-tab state.
This is independent from:

- F663/KI#985, where `hasStarted()` uses scheduled time as a proxy for actual player takeover and a
  selected civilization can be eliminated before `RFChapter::start()`;
- F689-F692/KI#1010-KI#1013, which are chapter-score breakdown, ownership, vassal-baseline and
  defeat/retirement arithmetic roots;
- extended-play handling after a *finite* game. When `maxTurns > 0` and a chapter is made endless
  for postgame extended play, the existing `extChapter` test is true and its special `"-"` display
  path is the behavior the code explicitly intended.

The initially suspicious `RFChapter::computeScore()` expression `GET_TEAM(activeId)` is also NOT a
new player/team-ID bug. This translation unit includes `CoreAI.h`; `GET_TEAM(PlayerTypes)` resolves
to `CoreAI::getTeam(PlayerTypes)`, which explicitly calls `TEAMID(ePlayer)`. This reuses the album's
standing rejected-KI#311/KI#387 negative control and must not be rediscovered.

REPAIR DIRECTION
----------------
Do not change the native `RFChapter::getEndTurn() == -1` sentinel merely to accommodate the screen;
that representation is used consistently by the C++ lifecycle.

The Python screen needs to distinguish:

- an *endless* chapter (`endTurn < 0`), which can be the live no-time-limit final chapter; from
- an *extended-game* endless chapter, whose rows intentionally use the special postgame `"-"`
  treatment.

A narrow shape is to derive these separately, e.g. conceptually:

    endlessChapter = (endTurn < 0)
    extChapter = (endlessChapter and extGame)
    ongoing = (currentTurn >= startTurn and
               (endlessChapter or currentTurn <= endTurn))

and retain the existing `not extChapter` guards where postgame extended-play rows should not count
as a live/scorable chapter. Exact postgame UI policy should be preserved when implementing the fix.

QUEUE-101 CONTINUATION CURSOR
-----------------------------
Continue from F696 / provisional KI#1017 UNUSED:
- finish RFChapter constructor/schedule numeric boundaries and current define ranges;
- finish reset/default/scheduled-vs-actual-start semantics with F663 dedup;
- finish start/end/score/frozen-score and hidden-information paths;
- finish retirement/defeat/endless interactions with F692 and F695 dedup;
- finish read/write reconstruction, including RFChapterScore pointer restoration and the save-on-
  chapter-boundary lifecycle;
- finish ownership/copy/destruction integration, deduping the missing RFChapterScore destructor to
  F690;
- finish all practical-1268 -> current RFChapter history and whole-file pointer/index/divisor/
  sentinel sweep;
- either promote an independent consequential F696 or close Queue 101 cleanly;
- freeze the next physical WIP immediately at the next settled boundary.

Exact continuation stone:
C031-WIP607 physical -> Queue101 OPEN -> F695/KI#1016 CONFIRMED -> F696/KI#1017 UNUSED -> finish
RFChapter lifecycle/serialization/ownership/history + F663/F690/F692/F695 dedup -> promote only an
independent current root or close cleanly -> freeze next WIP.

Files complete: 111 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP608 -- QUEUE 101 RFChapter.cpp -- F696 / KI#1017 CONFIRMED; QUEUE OPEN
===============================================================================

AUTHORITATIVE HANDOFF
---------------------
- Parent physical stone: C031-WIP607.
- Queue 101 `CvGameCoreDLL/RFChapter.cpp` remains OPEN.
- F695 / KI#1016: CONFIRMED.
- F696 / provisional KI#1017: CONFIRMED.
- F697 / provisional KI#1018: UNUSED.
- Files complete remains 111 / 169.
- No gameplay/source/XML/Python file was modified.

F696 / provisional KI#1017 -- penultimate chapter's explicit negative no-score-turn sentinel is rendered as a real ancient turn and as already scored
------------------------------------------------------------------------------------------------------------------------------------------

ROOT / NATIVE SENTINEL
----------------------
`RFChapter::getScoreTurn()` uses a negative return value to mean that no ordinary scheduled scoring
turn exists:

    if(scoreTimestamp >= 0)
        return scoreTimestamp;
    if(isEndless())
        return -1;
    return getDelay() + getEndTurn();

During Rise/Fall schedule construction, the chapter immediately before an endless final chapter has
no finite next-chapter length in which to place its normal delayed scoring point. Current
`RiseFall::init()` therefore does:

    int nextChapterLength = ... chapters[i + 1]->getLength();
    if(nextChapterLength <= 0)
        chapters[i]->setDelay(-10000); // For a negative scoreTurn

The comment makes the sentinel intent explicit. On normal Civ4 turn ranges this gives a large
negative `getScoreTurn()` for the penultimate chapter, e.g. an end turn of 250 yields -9750.

SUPPORTED ACTIVATION
--------------------
The same ordinary setup as F695 activates this independently:

- enable Rise/Fall;
- disable Time Victory (the only current `bEndScore=1` victory);
- `getMaxTurns()` remains 0;
- `RiseFall::init()` gives the final chapter length -1 and marks it endless;
- when scheduling the penultimate chapter, `nextChapterLength <= 0` and its delay becomes -10000.

No malformed save, custom XML or team configuration is needed.

SHIPPED SCORE-TAB MISINTERPRETATION
-----------------------------------
`CvVictoryScreen.showRiseFall()` obtains the exported `scoreTurn` and then does:

    s = localText.getText("TXT_KEY_RF_CHAPTER_SCORE_TURN", (scoreTurn,))
    if i == maxChapters - 1 and scoreTurn < 0:
        s = localText.getText("TXT_KEY_RF_CHAPTER_SCORE_END", ())
    elif currentTurn >= scoreTurn:
        s = self.highlight(s)

The localized strings are:

    TXT_KEY_RF_CHAPTER_SCORE_TURN: "Scored on Turn %d1_Trn"
    TXT_KEY_RF_CHAPTER_SCORE_END:  "Scored at game end"

Only the *final* chapter receives the negative-sentinel translation. The penultimate chapter is not
`i == maxChapters - 1`, so its deliberately negative score turn is rendered numerically:

    "Scored on Turn -9750"   (illustrative end turn 250)

or another similarly impossible negative turn.

The Points column has the same semantic mistake:

    elif currentTurn >= scoreTurn:
        s = self.highlight(s)

Since every real game turn is >= the deliberately huge negative sentinel, the penultimate chapter's
points are highlighted as though its scoring turn had already arrived even though
`RFChapter::isScored()` remains false and no scheduled score operation will occur.

The screen's own nearby comment acknowledges that `currentTurn >= scoreTurn` is only an
approximation for ordinary same-turn player-order timing, but it does not account for the explicit
negative "no score turn" state at all.

GAMEPLAY / SCORE SCOPE
----------------------
This finding does not claim that the penultimate chapter is accidentally frozen or scored by the
DLL. The negative sentinel does its native job: the ordinary `atTurnEnd` equality test will not fire
on a real turn. At game end, `RFTotalScore` can account for that chapter through its separate
"finished but unscored" path.

The defect is player-facing schedule/status truth in the shipped Rise/Fall Score tab:
- an impossible negative historical turn is displayed as a real scoring turn;
- the still-unscored points are visually marked as though scoring occurred.

ANCESTRY / STRONG INTERNAL ORACLE
---------------------------------
AdvC practical 1268 initially used a nonnegative delay when the next chapter had no finite length.
AdvC practical 1271 / cf4fe6e63d... deliberately changed this to:

    chapters[i]->setDelay(-10000); // For a negative scoreTurn

The same practical also changed the Score tab to special-case a negative scoreTurn as
"Scored at game end" for the final chapter:

    if i == maxChapters - 1 and scoreTurn < 0:
        ... TXT_KEY_RF_CHAPTER_SCORE_END ...

but did not cover the penultimate chapter that the same practical had just given a negative
scoreTurn. The regression/omission is therefore visible within one semantic commit rather than
inferred from later design drift.

Supplied Base AdvC 1.14 retains both sides unchanged; current SAS inherits them.

DEDUP / RELATION TO F695
------------------------
Whole-album search found no previous owner for the penultimate negative score-turn rendering.
F695 and F696 share a supported no-Time-Victory fixture but have independent state fields and repair
sites:

- F695: `endTurn < 0` / endless-final lifecycle is reconstructed with the wrong `maxTurns > 0`
  predicate, so the *current final chapter* is misclassified as non-ongoing and the partial total is
  mislabeled.
- F696: `scoreTurn < 0` / no-scheduled-score sentinel for the *previous penultimate chapter* is
  treated as an ordinary numeric turn and as already reached.

Fixing F695's endless/ongoing predicate does not fix the negative score-turn column or its
`currentTurn >= scoreTurn` highlight, so F696 remains independent.

F692 is also separate: it concerns `retireTurn == -1` being fed into retirement arithmetic after a
chapter civilization is defeated. F696 is a deliberate negative `scoreTurn` sentinel and does not
change score arithmetic.

REPAIR DIRECTION
----------------
Treat negative `scoreTurn` as a semantic state before numeric comparison, not as a real turn.
Conceptually:

    if scoreTurn < 0:
        s = TXT_KEY_RF_CHAPTER_SCORE_END   # or another explicit "not scheduled / at game end" text
    elif currentTurn >= scoreTurn:
        s = highlight(s)

The Points-column highlight likewise should require a nonnegative scheduled score turn (or, better,
use an exported native `isScored()` state if that API is added) rather than comparing the current
turn to a sentinel.

If design wants a different phrase for a finished-but-unscored penultimate chapter than the final
chapter's "Scored at game end," add that text explicitly; do not expose -10000-derived arithmetic to
the player.

QUEUE-101 CONTINUATION CURSOR
-----------------------------
Continue from F697 / provisional KI#1018 UNUSED:
- finish native constructor/schedule arithmetic and integer/sentinel bounds;
- finish scoreAtEnd hidden-information freeze path and all caller timing;
- finish read/write reconstruction, including save boundaries before/after actual `start()`;
- finish ownership/copy/destruction, deduping the RFChapterScore raw-string leak to F690;
- finish `hasStarted` scheduled-vs-actual lifecycle dedup to F663 and defeat retirement math to F692;
- finish endless/extended-game semantics after F695/F696 without broadening them beyond their proven
  UI consumers;
- finish practical 1268/1271/1297/1679/2920/3183 and infrastructure history plus whole-file
  pointer/index/divisor/state sweep;
- either promote a genuinely independent F697 or close Queue 101 cleanly;
- freeze next WIP at the next settled boundary.

Exact continuation stone:
C031-WIP608 physical -> Queue101 OPEN -> F695-F696 CONFIRMED -> F697/KI#1018 UNUSED -> finish native
RFChapter lifecycle/serialization/ownership/history + F663/F690/F692/F695/F696 dedup -> promote only
an independent consequential root or close cleanly -> freeze next WIP.

Files complete: 111 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP609 -- QUEUE 101 CLOSED; QUEUE 102 OPEN -- F695-F696 CONFIRMED; F697 UNUSED
===============================================================================

AUTHORITATIVE CLOSURE
---------------------
- Parent physical stone: C031-WIP608.
- Queue 101 `CvGameCoreDLL/RFChapter.cpp` (277 / 277 current physical lines): COMPLETE.
- Confirmed roots from this queue/cross-contract pass:
  - F695 / KI#1016: no-turn-limit endless final chapter is misclassified by the shipped
    Rise/Fall Score tab as non-ongoing/already-totaled.
  - F696 / KI#1017: penultimate chapter's deliberate negative no-score-turn sentinel is rendered
    as a real negative turn and as though scoring already happened.
- No independent F697 / provisional KI#1018 was promoted.
- Files complete advances to 112 / 169.
- No gameplay/source/XML/Python file was modified.

NATIVE RFCHAPTER LIFECYCLE CLOSURE
----------------------------------
The remaining native `RFChapter` implementation was reviewed end-to-end after the two Python
consumer mismatches were separated out.

Construction / schedule arithmetic:
- `RiseFall::init()` clamps/validates `maxChapters` before constructing any chapter and shuts the
  option off when the result is < 1.
- The apparent `(maxChapters - 1)` divisor in the self-length constructor is not reached for the
  one-chapter case: the `for(i < maxChapters - 1)` loop creates no such object and the sole final
  chapter uses the explicit-length constructor instead.
- With two chapters the divisor is 1, and `RiseFall::init()` separately avoids the
  `(maxChapters - 2)` scoring-delay interpolation divisor through its `maxChapters != 2` guard.
- Current `RF_CHAPTER_LENGTH_SHIFT_PERCENT=0`; the documented supported range is +/-75 and the
  computed factor is additionally bounded by `dRange(0.25,1.75)`. No current zero/negative finite
  length survives the subsequent `<10` validation.
- Finite `startTurn + length - 1` and `delay + endTurn` arithmetic is comfortably bounded by
  supported Civ4 turn counts. The deliberately negative no-score sentinel is F696's UI contract,
  not integer overflow.

Start / end state:
- `start()` asserts the scheduled turn and calls `breakdown.atChapterStart(*this)`, which installs
  the RFChapterScore back-pointer and captures initial scoring state.
- `hasEnded()` treats an explicitly scored chapter as ended, an endless chapter as not ended, and
  otherwise switches only after the finite end turn. This matches `RiseFall::atTurnEnd`, where the
  end turn itself is still a player-control turn.
- `hasStarted()` is only a scheduled-turn predicate. Its inability to distinguish a civ that was
  selected from one that the human actually took over is the already-confirmed F663/KI#985 root.
  Do not create F697 for the same semantic gap. The `startTurn=-1` reset value likewise does not
  activate independently: normal initialization assigns every chapter's start turn before play,
  and `read()` restores `startTurn` before calling `hasStarted()`.

Score / hidden-information freeze:
- At the natural chapter end, `RiseFall::atTurnEnd()` calls `computeScore()` while the chapter is
  still not `hasEnded()`, then stores the result in `scoreAtEnd`. This is the intended last-known
  fallback for later players that have not met the former chapter civilization.
- Once a finite chapter is ended but still awaiting delayed scoring, `computeScore()` returns
  `scoreAtEnd` only when the active player's actual team has not met the chapter civ's team;
  otherwise it may recompute the still-live score preview.
- The initially suspicious `GET_TEAM(activeId)` is SAFE: `CoreAI.h` overloads
  `getTeam(PlayerTypes)` and converts through `TEAMID(ePlayer)`. This is the standing rejected
  KI#311/KI#387 false-positive family already recorded during Queue038.
- After `scoreTimestamp` is set, `RFChapterScore::update()` freezes numeric components and only
  rebuilds presentation text. No post-score native score drift was found.

Retirement / defeat / endless controls:
- Ordinary retirement stamps `retireTurn` only once; `RiseFall::hasRetired()` and AutoPlay then use
  that timestamp coherently.
- Defeat leaving `retireTurn==-1` and feeding it into retirement math is already F692/KI#1013;
  no second RFChapter root is assigned here.
- `length<=0 -> 100` in `getRemainingTimePercent()` is an explicit behavior present from the
  original Rise/Fall implementation. For an endless chapter the special retire popup does not
  offer the ordinary chapter-retirement button. Without a stronger design contradiction, this
  remains authored scoring policy rather than a new F697.
- F695/F696 own the two proven shipped Score-tab failures to interpret the negative endless/score
  sentinels. The native sentinel producers themselves remain coherent.

SERIALIZATION / BACK-POINTER RECONSTRUCTION
-------------------------------------------
`RFChapter::write` and `read` have a mechanically paired 14-field scalar sequence in the same
order:

    savegameVersion, length, endless, delay, startTurn, scoreTimestamp,
    scoreShown, retireRecommended, position, scoreAtEnd, retireTurn,
    repeat, civ, chapters

followed by the paired `RFChapterScore` payload.

`PlayerTypes civ` is written through the enum-to-integral stream overload/conversion and read back
through an `int tmp`; `PlayerTypes` uses the same integer representation in this DLL contract.
AdvC practical 3183 merely removed the explicit `(int)` cast from the write call and did not change
stream width/layout.

`read()` performs `reset()`, reads all chapter fields, then invokes:

    breakdown.read(pStream, hasStarted() ? this : NULL);

For ordinary past/current chapters this restores the score object's non-owning back-pointer to the
newly materialized `RFChapter`; for future chapters it remains NULL until `start()` calls
`atChapterStart`. A save on the scheduled-start/pre-takeover boundary inherits F663's conceptual
scheduled-vs-actual-start ambiguity, but `initialRank` remains zero and `RFChapterScore::update()`
therefore still refuses to compute before the real start. No separate serialization corruption was
established.

OWNERSHIP / COPY / DESTRUCTION
------------------------------
`RiseFall` stores `RFChapter*` objects, allocates them with `new`, deletes each one in `reset()`, and
never value-copies/moves an `RFChapter` or `RFChapterScore` in the current tree. The score object's
`chapter` pointer points back into its owning chapter and therefore remains valid for that object's
lifetime.

The final `RFChapterScore::breakdownString` raw allocation not being freed on object destruction is
already F690/KI#1011. Queue101 confirms the owner-side destruction path but does not duplicate that
root. Any repair to F690 still needs Rule-of-Three-safe ownership because these classes currently
have compiler-generated copy operations even though the current tree does not exercise them.

CURRENT VS BASE / HISTORY CLOSURE
---------------------------------
After line-ending normalization, current SAS `RFChapter.cpp` and `.h` are text-identical to supplied
Base AdvC 1.14 (277 and 75 lines respectively). There is therefore no unported later Base repair in
this queue.

Semantic history is compact:
- practical 1268 creates RFChapter and its original lifecycle/sentinels;
- 1271 adds `setScored` and the explicit negative no-score-turn scheduling behavior implicated in
  F696;
- 1297 adds the scheduled-start assertion;
- 1368/1493/1548/1552/1564 are game-accessor const/inline refactors that settle back to current
  semantics;
- 1678/1679 remove old TEAMREF spelling, but the typed GET_TEAM overload preserves the conversion;
- 1947 switches to `CoreAI.h`, retaining the same typed team accessor;
- 2920/2954/3392 are namespace/comment/merge normalization around `fmath`;
- 3183 removes the redundant enum-to-int cast in serialization.

No later retained history establishes another semantic mutation.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 102 `CvGameCoreDLL/CyPlotInterface1.cpp` (273 queued lines), OPEN
under F697 / provisional KI#1018 unused.

Audit the Boost.Python registration layer against the already-completed native `CyPlot.cpp` queue:
method-name/function-target/arity/default symmetry, pointer/reference ownership policies, enum/index
bridges, static-vs-managed plot wrappers, shipped Python caller expectations and prior KI#257/F679
alias/null controls. Compare Base AdvC 1.14 and Civ4CE/K-Mod registration lineage where useful.

Exact continuation stone:
C031-WIP609 physical -> Queue101 CLOSED with F695-F696 / no independent F697 -> 112 / 169 complete
-> Queue102 CyPlotInterface1.cpp OPEN -> F697/KI#1018 UNUSED -> binding/ownership/name-target/arity +
shipped-caller + history/dedup sweep -> promote only an independent consequential root or close
cleanly -> freeze next WIP.

Files complete: 112 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP610 -- QUEUE 102 CyPlotInterface1.cpp CLOSED; QUEUE 103 CyGameCoreUtils.cpp OPEN
===============================================================================
Physical parent: C031-WIP609.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
K-Mod 1.46, Civ4CE, current CyPlot.cpp/.h, shipped Python/map scripts and local reachable commit-diff
history. Gameplay/XML/Python source untouched.

RESULT
------
Queue 102 closes without spending F697. The Boost.Python registration layer is mechanically aligned
with the already-audited CyPlot wrapper surface: all active method names bind to the same-named
CyPlot target, every wrapper that returns a freshly allocated CyPlot/CyUnit/CyCity/CyArea uses
`manage_new_object`, and no current shipped caller establishes a new NULL/index/ownership/arity
failure independent of the previously completed CyPlot/CyMap work.

CURRENT / BASE PROVENANCE
-------------------------
Current SAS `CyPlotInterface1.cpp` is text-identical to supplied Base AdvC 1.14 after newline
normalization (273 physical lines in both trees). Thus there is no SAS-only executable registration
change to explain or a later Base repair missing from SAS.

The registration history is compact and was checked in full:
- K-Mod initial interface supplies the inherited bulk of the table;
- K-Mod practical 647 corrects only the help-string return type for `isCityRadius` from int to bool;
- AdvC practical 1407 updates `getBuildTime`, `getBuildTurnsLeft` and `changeBuildProgress` for the
  player-aware native signatures;
- 1456 exposes `setLatitude` for True Starts;
- 1615/2127/2270 are include/console/PCH cleanup;
- 1926 removes the no-longer-supported `getNumVisiblePotentialEnemyDefenders` exposure;
- practical 2545 moves the random-plot flags into the typed enum and exposes them to Python;
- 2612 removes `isFriendlyCity` after the native helper is removed;
- 2998 is the corresponding merge replay.
No later semantic registration mutation survives outside those changes.

METHOD-NAME / TARGET / DUPLICATE CHECK
--------------------------------------
A mechanical parse found 196 active `.def("name", &CyPlot::target, ...)` bindings. Every active
binding has `name == target`; there are no duplicate active method names and no wrong-target binding.
This rules out the same class of registration polarity/target errors that produced findings in other
Cy*Interface queues.

OWNERSHIP POLICY CHECK
----------------------
The native CyPlot wrapper was scanned for fresh wrapper allocation. The current methods that return
fresh Python-visible wrapper objects are:
- `getNearestLandPlot` -> `new CyPlot`;
- `getBestDefender`, `getSelectedUnit`, `getUnit` -> `new CyUnit`;
- `area`, `waterArea` -> `new CyArea`;
- `getPlotCity`, `getWorkingCity`, `getWorkingCityOverride` -> `new CyCity`.

Every one of those registrations uses `python::return_value_policy<python::manage_new_object>()`.
There is therefore no inverse-ownership leak like Queue 090 F688/KI#1009. The CyMap static-wrapper
aliasing problem remains the already-owned KI#257 surface and is not recreated by this instance
registration table.

RANDPLOT ENUM EXPOSURE -- REAL API TYPO, BELOW FINDING THRESHOLD
---------------------------------------------------------------
The tail registration contains:

    .value("RANDPLOT_ADJACENT_LAND", RANDPLOT_ADJACENT_LAND)
    .value("RANDPLOT_PASSABLE", RANDPLOT_PASSABLE)
    .value("RANDPLOT_ADJACENT_LAND", RANDPLOT_ADJACENT_LAND)
    .value("RANDPLOT_NOT_VISIBLE_TO_CIV", RANDPLOT_NOT_VISIBLE_TO_CIV)

while the native `RandPlotFlags` enum has, between NOT_VISIBLE and the later HABITABLE flags,
`RANDPLOT_NOT_CITY = (1 << 6)`. Therefore `RANDPLOT_ADJACENT_LAND` is exported twice and
`RANDPLOT_NOT_CITY` is not exported symbolically at all.

The typo is ancestral, not a later drift: practical 2545 / 8d5b38f104 introduces the native enum and
the Python exposure in the same commit, and its birth diff already contains the duplicated
ADJACENT_LAND line and missing NOT_CITY line. Base AdvC 1.14 retains it unchanged.

This is a genuine exposed-API defect, but no current shipped Python or map script calls
`CyMap.syncRandPlot`, references `RandPlotTypes`, or names any `RANDPLOT_*` constant. A modder could
still pass the numeric bit 64 directly. Under the album's standing rule not to promote unused API
hardening/dead shipped paths, it is recorded here rather than consuming F697. If SAS later adds a
shipped Python caller needing NOT_CITY, promote/fix it then (replace the second ADJACENT_LAND export
with `RANDPLOT_NOT_CITY`).

HELP-STRING / DOCUMENTATION MISMATCHES -- NON-EXECUTABLE
--------------------------------------------------------
Several inherited `.def` documentation strings have stale return-type prose, including
`changeExtraMovePathCost` (says int; native void), `getNumCultureRangeCities` (says bool; native int),
`set/changeOwnershipDuration` and `set/changeImprovementDuration` (say int; native void),
`isForceUnowned` / `isInvisibleVisible` (say int; native bool), and
`changeInvisibleVisibilityCount` (says int; native void). `getFeatureProduction` also describes its
third Python wrapper argument as `CvCity**` although the actual binding type is `CyCity*`.
Boost.Python overload construction uses the C++ member pointer, not these help strings, so these do
not change call arity or runtime return values. Keep as documentation cleanup only.

SHIPPED-CALLER / NULL-CONTRACT CONTROLS
---------------------------------------
The few native wrappers with inherited direct dereferences of wrapper arguments were cross-checked
against shipped callers rather than promoted merely because Python could pass `None`:
- shipped Archipelago/Islands/Equal_Islands calls to `isAdjacentToArea` build `oceans` only from
  `map.getArea(i)` values that first pass `not area.isNone()` and are water/non-lake areas;
- the live Main Interface call to `getBuildTurnsLeft` supplies mission BuildType, selected-group
  owner and both extra-work arguments, matching the current player-aware binding;
- no shipped calls were found for the legacy `CyPlot.getFeatureProduction`, `shareAdjacentArea`,
  `isTradeNetworkConnected` or other sharper wrapper-argument contracts.
Those remain extension-API robustness concerns already belonging to the native CyPlot surface, not a
new binding-layer root.

FINAL DEDUP / CLOSURE
---------------------
Whole-album controls keep this queue separate from KI#257 static CyPlot wrapper aliasing and the
Queue 050 CyPlot findings, while refusing to renumber their already-audited native consequences.
No bad return policy, wrong bound method, duplicate active method registration, shipped-call arity
mismatch or current enum/index crossing survives the final pass.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 103 `CvGameCoreDLL/CyGameCoreUtils.cpp` (272 queued lines), OPEN
under F697 / provisional KI#1018 unused.

Audit this free-function Python bridge against `CvGameCoreUtils.cpp/.h` and the already-audited native
utility contracts: exact exported name/target/arity/default mapping, wrapper allocation ownership,
plot/direction/distance coordinate semantics, `getCity`/`getUnit` ID lookup lifetime, combat/odds and
promotion helpers, enum conversions, NULL handling, shipped Python callers, Base/K-Mod/Civ4CE
lineage, and whole-album dedup. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP610 physical -> Queue102 CLOSED / NO NEW FINDING; F697/KI#1018 UNUSED -> 113 / 169 complete
-> Queue103 CyGameCoreUtils.cpp OPEN -> binding/name-target/arity/ownership + coordinate/ID/null +
shipped-caller/history/dedup sweep -> promote only an independent consequential root or close cleanly
-> freeze next WIP.

Files complete: 113 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP611 -- QUEUE 103 CyGameCoreUtils.cpp CLOSED CLEAN; QUEUE 104 CvString.cpp OPEN
===============================================================================
Physical parent: C031-WIP610.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
K-Mod 1.46, Civ4CE, current CyGameCoreUtilsInterface.cpp, shipped Python/map scripts and local
reachable commit-diff history. Gameplay/XML/Python source untouched.

RESULT
------
Queue 103 closes without spending F697. Current SAS `CyGameCoreUtils.cpp` and its header are
text-identical to supplied Base AdvC 1.14 after newline normalization. The free-function bridge's
live shipped callers satisfy its coordinate/enum/object preconditions, all freshly allocated Python
wrappers have the correct owning registration in `CyGameCoreUtilsInterface.cpp`, and the sharper raw
wrapper/static-wrapper surfaces are either unused by shipped Python or already-owned API themes.
No independent current-live root survives.

CURRENT / BASE / ANCESTRY CONTROL
---------------------------------
Current SAS `CyGameCoreUtils.cpp` (272 lines) and `CyGameCoreUtils.h` are identical to Base AdvC
1.14, so there is no SAS drift or missed later Base repair in this queue. K-Mod/Civ4CE comparison
shows that AdvC's differences are principally native-helper relocation/refactoring rather than new
Python semantics:
- `dxWrap` / `dyWrap` and city-plot helpers were redirected through `CvMap` as global helpers moved;
- practical 1918 moved religion/tech/class/project predicates onto the corresponding info objects;
- practical 2081 updated `directionXY` to the native reference-taking signature;
- practical 2843 redirected `getCombatOdds` into the dedicated `calculateCombatOdds` implementation;
- practical 3601 strengthened `plotCityXYFromCity` so null *underlying* city/plot objects return
  `NO_CITYPLOT` rather than being dereferenced;
- practical 3792 added the plot-indicator runtime-patch bridge.
The remaining history strata are includes/PCH/removal of obsolete exposures or merge replay and do
not create an independent surviving behavior change.

REGISTRATION / OWNERSHIP CROSS-CHECK
------------------------------------
The companion `CyGameCoreUtilsInterface.cpp` was checked despite its later Queue-153 position because
ownership is part of the current wrapper contract.

Fresh wrapper returns are correctly owning:
- `plotDirection`, `plotCardinalDirection`, `plotXY`, `plotCity` -> fresh `new CyPlot` and
  `manage_new_object`;
- `getCity` -> fresh `new CyCity` and `manage_new_object`;
- `getUnit` -> fresh `new CyUnit` and `manage_new_object`.

The two `splotCardinalDirection` / `splotXY` variants deliberately retarget process-static `CyPlot`
objects and are registered as borrowed `reference_existing_object`. That means simultaneous retained
references from repeated calls to one static helper would alias. No shipped Python/map-script caller
uses either `splot*` function. Keep this as extension-API sharpness / thematic KI#257 control rather
than a new F697.

RAW WRAPPER / NULL CONTRACTS
----------------------------
`directionXYFromPlot` and `getCombatOdds` directly dereference their `CyPlot*` / `CyUnit*` wrapper
arguments, while `plotCityXYFromCity` checks the underlying native pointers only after dereferencing
its wrapper objects. Passing Python `None` or a destroyed/invalid wrapper can therefore be unsafe.
A shipped-caller census found no active calls to any of those three helpers. The only textual
`isPromotionValid` occurrence is disabled/commentary in the custom Sevopedia unit page. Thus these
remain generic extension-API hardening rather than supported current defects.

ENUM / INFO LOOKUP CONTRACTS
----------------------------
The `isPromotionValid`, tech-required, unit/building/project-class, wonder-class and project helpers
cast Python integers straight to native enum types and several immediately index `GC.getInfo`.
Shipped active callers were checked individually:
- tech-window, tech-splash and Sevopedia tech loops pass Unit/Building/Project indices obtained from
  `range(gc.getNum*Infos())` / known page IDs;
- advisor/event wonder checks pass `getBuildingClassType()` or bounded class-loop values;
- project checks pass existing Project IDs;
- Foreign Advisor `getEspionageModifier` passes team IDs from two valid player objects.
No shipped call supplies NO_* or an out-of-range info index. Unchecked arbitrary-Python integers are
therefore API robustness only.

PLOT / COORDINATE CALLER CONTROLS
---------------------------------
The active `plotDirection` callers iterate valid `DirectionTypes`; `CvMapGeneratorUtil` explicitly
checks the returned CyPlot for `isNone`, while BUG PlayerUtil and PerfectMongoose call CyPlot methods
whose wrappers return safe false/-1 sentinels when the underlying edge plot is NULL. Active `plotXY`
callers likewise consume a managed CyPlot wrapper and use null-safe CyPlot methods.

PerfectMongoose is the only shipped caller of free `plotCity`. Its first two loops use
`numCityPlots = gc.getNUM_CITY_PLOTS()` and the third explicitly loops over
`gc.getNUM_CITY_PLOTS()`, so the raw `int -> CityPlotTypes` cast is bounded by the native city-radius
index domain. No current bad city-plot subscript is established.

ID LOOKUP / COMBAT-ODDS CONTROLS
--------------------------------
No shipped Python call to the free `getCity(IDInfo)` or `getUnit(IDInfo)` helper was found; same-named
method definitions/usages belong to Python utility classes or CyPlayer/CyPlot methods. The wrappers
nevertheless return managed independent CyCity/CyUnit wrappers even when the native lookup yields
NULL, matching normal Cy* sentinel behavior.

No shipped caller of the free two-unit `getCombatOdds` bridge was found. Its practical-2843 target,
`calculateCombatOdds`, is already fully owned by Queue 057; do not duplicate its free-win/odds roots
here merely because this bridge forwards to it.

PLOT-INDICATOR DEDUP
--------------------
`cyUpdatePlotIndicatorSize()` has one shipped callback in BUG `InputUtil.updatePlotIndicatorSize`,
introduced with AdvC practical 3792, and simply calls `smc::BtS_EXE.patchPlotIndicatorSize()`.
Queue 075 already audited that native runtime-patch subsystem and confirmed F682/KI#1003, the
512-KiB fallback-address-search temporary leak. Its remaining patch/cache/address contracts were
closed there. This one-line wrapper introduces no separate ownership, parameter or state mutation;
keep any underlying behavior under F682/SelfMod rather than spending F697.

MINOR DOCUMENTATION DEBT
------------------------
The companion interface help string for free `getCity` says `CyPlot* (IDInfo city)` even though the
bound function returns `CyCity*`. This string is descriptive metadata only; the C++ function pointer
controls the actual Boost.Python return type and ownership. Record as help-text cleanup, not a
runtime finding.

FINAL DEDUP / CLOSURE
---------------------
Whole-album checks found no existing root that needs reopening from this bridge and no independent
new root. The static-wrapper alias shape is already represented by KI#257 as a design warning; the
combat calculator is Queue057-owned; the runtime patch is Queue075/F682-owned. No shipped caller
turns the remaining null/enum assumptions into a supported crash or wrong-result path.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 104 `CvGameCoreDLL/CvString.cpp` (269 queued lines), OPEN under
F697 / provisional KI#1018 unused.

Audit the narrow/wide string implementation end-to-end: constructors/assignment/self-assignment,
`format`/`formatv` buffer growth and `_vsnprintf` semantics, va_list lifetime/reuse, null input,
encoding conversion, replace/token helpers, bounds/overflow, Base/K-Mod/Civ4CE ancestry, live callers
(including the malformed `%6` CvMap logging note already held below threshold), and whole-album
ownership/dedup. Promote only a demonstrably consequential current root.

Exact continuation stone:
C031-WIP611 physical -> Queue103 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 114 / 169 complete ->
Queue104 CvString.cpp OPEN -> format/va_list/buffer/conversion/null/overflow + caller/history/dedup
sweep -> either promote a consequential F697 or close cleanly -> freeze next WIP.

Files complete: 114 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP612 -- QUEUE 104 CvString.cpp CLOSED CLEAN; QUEUE 105 CvInfo_City.cpp OPEN
===============================================================================
Physical parent: C031-WIP611.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
K-Mod 1.46, Civ4CE BtS, Taurus 1.04, shipped current callers and local reachable commit-diff history.
Gameplay/XML/Python source untouched.

RESULT
------
Queue 104 closes without spending F697. Current SAS `CvString.cpp` is text-identical to supplied
Base AdvC 1.14 after newline normalization. The header's only SAS-vs-Base differences are later
signature/return-statement whitespace collapses; executable behavior is unchanged. No supported
current crash, corruption, leak or wrong gameplay state was established in the string implementation.
Several real portability/API sharp edges are recorded below so they are not rediscovered as apparent
new findings later.

FORMAT / BUFFER-GROWTH CONTRACT
-------------------------------
Both narrow and wide `formatv` start with a 2048-element stack buffer, pass one element less as the
legacy MSVC formatting limit, and grow in 2048-element increments up to 40 attempts. Every dynamic
retry buffer is deleted before replacement and the final buffer is deleted on exit; no allocation
leak survives either success or failure. On failure the output is cleared (K-Mod practical 1232's
minor cleanup from assignment-to-empty to `clear()`).

The implementation retries `_vsnprintf` / `_vsnwprintf` with the same incoming `va_list`. In
portable modern C/C++ this is not a sound cross-platform assumption after a consuming `v*printf`
call; a fresh copied/restarted argument list is the robust design. The actual supported build,
however, is the historical 32-bit VC7.1/MSVCR71 ABI for which this exact Firaxis/K-Mod routine has
long been used, and no target-runtime failure was established from local evidence. Do not promote a
modern-portability concern as F697 without a target-ABI reproducer. If this code is ever ported to a
new compiler/runtime, rebuild each retry from `va_copy` (where available) or redesign around a
single sizing/copy-safe formatter.

MALFORMED FORMAT CONTROL -- CvMap `%6`
--------------------------------------
The already-recorded `CvMap::init` diagnostic still calls:

    CvString::format("... num custom options=%6", ...)

with an incomplete final conversion. That format string is unquestionably malformed and can drive
`formatv` through repeated failure/reallocation before returning an empty string under the legacy
runtime. The retry buffers are freed, however, and the call feeds `gDLL->logMemState`; no gameplay
state or persistent allocation corruption was demonstrated. Keep this as the Queue036 diagnostic /
performance note rather than manufacturing F697 in the generic formatter. The repair belongs at the
bad call site (`%d`), not by teaching `formatv` to reinterpret malformed format strings.

WIDE / NARROW CONVERSION -- REAL FRAGILITY, BELOW THRESHOLD
-----------------------------------------------------------
`CvString::Copy(wchar const*)` allocates `wcslen(w)+1` narrow bytes and calls unbounded
`sprintf(s, "%S", w)`. The header itself warns that this conversion breaks when the wide input
contains non-ASCII characters. On code pages where one wide character can expand to multiple
multibyte bytes, the allocation size is not generally a portable upper bound; on the historical
runtime/default locale unsupported characters can instead make conversion fail. This is therefore a
real internationalization/portability weakness.

Current active constructions from wide text were traced. They are dominated by diagnostics
(`CvDLLLogger`, UWAI reports, starting-position logs) and by strings that are semantically ASCII
identifiers/paths/function names (map-script names, popup Python-screen function identifiers,
password hashes/config fields). The current mod/source metadata consumed by the narrow-string helper
is ASCII. No supported present path plus target-locale behavior was established that turns this into
a deterministic memory overwrite/gameplay failure. Retain the source's own warning and prefer a
proper bounded locale/encoding conversion in future modernization; do not spend F697 on an
unproved locale-dependent consequence.

The reverse `CvWString::Copy(char const*)` allocates `strlen(s)+1` wide elements. A multibyte byte
sequence cannot require more wide code units than input bytes for the supported conversion shape, so
the same expansion-size concern does not apply in that direction. Practical 3839 (from C2C) also
fixed the concrete old stale-value behavior by clearing the destination for NULL/empty input.

CvWStringBuffer OWNERSHIP / ALIAS CONTROLS
------------------------------------------
`CvWStringBuffer` owns a raw `wchar*` and defines a destructor but no copy constructor/assignment;
a value copy would therefore shallow-copy ownership and risk double deletion. `append(const wchar*)`
would also be unsafe if given a pointer into its own storage and growth reallocated before `memcpy`;
`assign` of its own `getCString()` would clear the source before reading it.

A whole-current-tree usage census found only local construction plus pass-by-reference APIs. No
`CvWStringBuffer` value copy/return/assignment and no direct self-append/self-assign exists in shipped
C++. Thus these are latent Rule-of-Three/self-alias API hazards, not activated current roots. A future
cleanup could explicitly disable copying (appropriate for VC7.1) or implement deep-copy semantics.

OTHER BODY / HEADER CONTROLS
----------------------------
- `Replace` has bounded string indices and behaves consistently even when old==new.
- `getTokens` terminates for empty strings, delimiter-only strings and empty delimiter sets; its
  `npos` arithmetic is used only to feed `substr` in the standard whole-tail form.
- `ensureCapacity` maintains the terminating NUL and frees the previous allocation. Integer overflow
  would require unsupported near-GB string/buffer sizes and is not a shipped domain.
- `cstring::tolower` passes plain `char` to C `tolower`, which is formally unsafe for negative signed
  bytes; its only current caller lowercases the mod/path name comparison, and current AdvCiv-SAS
  names are ASCII. Keep non-ASCII path handling as portability/i18n debt.
- `cstring::empty` assumes non-NULL input but has no current direct caller in the shipped C++ tree.
- Derived `CvString`/`CvWString` sizes remain statically asserted equal to their STL bases for the
  EXE/DLL ABI; no data member/virtual-function drift exists.

HISTORY / PROVENANCE
--------------------
The relevant history was checked rather than inferred from the final file:
- the formatter/body originates in the Firaxis/K-Mod header implementation;
- practical 2222 moved the definitions into `CvString.cpp` without changing the formatter contract;
- K-Mod practical 1232 only changes failure output from assignment-to-empty to `clear()`;
- practical 3839 imports the C2C NULL/empty wide-narrow stale-value fix;
- practical 4084 imports the small Taurus `cstring` helpers;
- later changes are comments/whitespace/signature collapsing, including SAS practical 5961.
Supplied Base AdvC 1.14 retains the same implementation, so there is no missed upstream repair.

FINAL DEDUP / CLOSURE
---------------------
Varargs *caller* format/argument mismatches remain owned by their call-site queues (for example
F108/KI#431 in WarUtilityAspect). Fixed-size logger truncation/termination roots likewise belong to
their logger owners, not this resizing formatter. The CvMap malformed `%6` note remains a caller bug
below threshold. No generic CvString root independently explains those issues.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 105 `CvGameCoreDLL/CvInfo_City.cpp` (265 queued lines), OPEN under
F697 / provisional KI#1018 unused.

Audit city-info XML ownership/defaults and read lifecycle end-to-end: world-size/automate info arrays,
city-size art/style fields, effect/visibility/scaling values, enum/index getters, allocation teardown,
XML defaults and current-data dimensions, Base/K-Mod/Civ4CE lineage, Python/UI consumers and
whole-album dedup. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP612 physical -> Queue104 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 115 / 169 complete ->
Queue105 CvInfo_City.cpp OPEN -> XML/default/allocation/index/current-data/caller/history/dedup sweep
-> either promote a consequential F697 or close cleanly -> freeze next WIP.

Files complete: 115 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP613 -- QUEUE 105 CvInfo_City.cpp CLOSED CLEAN; QUEUE 106 CvDiploParameters.cpp OPEN
===============================================================================
Physical parent: C031-WIP612.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
Civ4CE BtS stock XML where the mod intentionally inherits an unmodified base file, shipped current
callers and local reachable commit-diff history. Gameplay/XML/Python source untouched.

RESULT
------
Queue 105 closes without spending F697. Current SAS `CvInfo_City.cpp` is text-identical to supplied
Base AdvC 1.14 after newline normalization; the header's only current-vs-Base differences are
signature/inline formatting. The five actual classes in this file -- Process, Specialist,
CultureLevel, Emphasize and Hurry -- have paired ownership/default initialization and no supported
current bad index, missing post-load state, stale XML cursor or wrong-data consequence was proved.

SPECIALIST MISSION-TYPE FALSE POSITIVE RESOLVED
-----------------------------------------------
`CvSpecialistInfo::read()` initializes `m_iMissionType` to NO_MISSION and does not read a mission
field from Specialist XML. This initially looks like lost configuration because the class exposes
`getMissionType` / `setMissionType` and `CvActionInfo::getMissionType` consumes it.

The omission is deliberate. `CvXMLLoadUtility::SetGlobalActionInfo()` synthesizes action metadata
after the underlying infos have loaded. Its ACTIONSUBTYPE_SPECIALIST branch assigns every
Specialist `MISSION_JOIN`, then feeds that mission to the action record. The same stage similarly
assigns MISSION_BUILD to Builds, MISSION_SPREAD to Religions, MISSION_SPREAD_CORPORATION to
Corporations and MISSION_CONSTRUCT to Buildings. No Specialist XML MissionType is expected.

ARRAY OWNERSHIP / XML-CURSOR CONTRACT
-------------------------------------
- Process owns `m_aiProductionToCommerceModifier`; Specialist owns Yield, Commerce and Flavor
  arrays; CultureLevel owns its speed-threshold array; Emphasize owns Yield/Commerce arrays. Every
  owned raw array starts NULL and is deleted by the corresponding destructor.
- Missing positional Yield/Commerce tags are initialized to the fixed enum-domain length and zero.
  Present tags go through `SetYieldArray` / `SetCommerceArray`, which encapsulate the XML-parent
  restoration introduced by practical 3220. The current readers therefore resume at the info parent
  before reading subsequent fields.
- `SetVariableListTagPair` initializes by the supplied info-domain length, resolves keyed XML pairs,
  bounds-checks keys, and may collapse an all-zero default array back to NULL. All getters in this
  file explicitly interpret NULL as zero, so that memory optimization preserves semantics.
- A theoretical second read into the same already-populated positional array would not reset values
  because `InitList` deliberately preserves an existing allocation. Global class loading creates and
  reads these info objects once in the supported startup lifecycle; no current same-object reread or
  modular overlay path was established. Keep rereadability as loader/API robustness, not F697.

CURRENT-DATA DIMENSION / REFERENCE PROOF
----------------------------------------
Current XML was parsed mechanically rather than inferred from the schema:
- 3 Process infos (Wealth, Research, Culture), each with a valid TechPrereq and three explicit
  Commerce values. BtS has four Commerce types; the fourth (Espionage) remains the initialized zero,
  matching these processes' intended data shape.
- 14 Specialist infos. Every GreatPeopleUnitClass is either NONE or resolves to a current UnitClass.
  Yield arrays contain at most the three Yield entries; Commerce arrays contain at most four entries
  (Spy/Great Spy explicitly use the Espionage slot). Missing/empty arrays safely read as zero.
  Current Specialist Flavors are empty, so the NULL-as-zero optimization is exact for current data.
- All 7 current CultureLevel infos contain keyed thresholds for all 8 current game speeds:
  NITRO, TURBO, QUICK, NORMAL, EPIC, MARATHON, SLOW and VERY_SLOW. There is no new-speed omission or
  positional mismatch in `getSpeedThreshold`.
- The mod does not override stock `CIV4EmphasizeInfo.xml`; the supplied Civ4CE BtS tree provides the
  inherited six standard Emphasize infos. Their three Yield slots and three explicit legacy
  Commerce slots fit the same fixed arrays; the BtS Espionage slot is zero-filled.
- Current Hurry XML contains the two expected entries (population and gold); their scalar fields need
  no dynamic allocation or cross-domain indexing.

CALLER / PYTHON INDEX CONTROLS
------------------------------
Native callers use typed enums or loops bounded by the corresponding GC info counts. Shipped Python
UI callers likewise iterate `getNum*Infos()` or use current engine-returned enum values. The
CyGlobalContext getters for Process, Specialist, Emphasize, CultureLevel and Hurry all explicitly
return NULL unless `0 <= i < getNum*Infos()`, preventing a raw invalid info pointer at that bridge.
Individual array getters assert their Yield/Commerce/GameSpeed/Flavor domains; no shipped Python
caller with an out-of-range sub-index was found.

The Boost.Python help string for `CvCultureLevelInfo::getSpeedThreshold` says `int ()` despite the
real one-int parameter. This is stale descriptive metadata only; the bound C++ function pointer has
the correct signature and live callers supply the speed index. Do not spend F697 on help text.

HISTORY / PROVENANCE
--------------------
The retained semantic history was checked end-to-end:
- practical 1644 split these classes out of monolithic CvInfos without inventing new behavior;
- practicals 2222/2236 replace the manual Process TechPrereq lookup with the default-aware helper;
- practical 2287 removes unnecessary setters and writes the Specialist texture directly;
- practical 2826 types Process's TechPrereq/Commerce access and is an enum-refactor, not a data-rule
  change;
- practical 3183 rewrites XML/enum-map plumbing while preserving the class contracts;
- practical 3220 restores wrapper helpers that centralize the Yield/Commerce parent reset;
- practical 3432 only switches explicit base-class names to the `base_t` aliases.
Merge/format strata do not add another semantic path. Supplied Base AdvC 1.14 retains the final
implementation, so there is no missed upstream repair.

FINAL DEDUP / CLOSURE
---------------------
The generic XML sibling-count/comment and keyed-array machinery was already audited in Queues
028/052/086; no independent activation originates in these five info classes. The action-info
mission synthesis belongs to the established SetGlobalActionInfo lifecycle and works as designed.
No allocation, default, index, type-reference or current-data defect survives the file-level pass.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 106 `CvGameCoreDLL/CvDiploParameters.cpp` (264 queued lines), OPEN
under F697 / provisional KI#1018 unused.

Audit its EXE-facing diplomacy-parameter ownership and ABI surface end-to-end: constructor defaults,
copy/assignment if any, wide-string and variable-text lifetime, argument-list ownership, leader/
player/comment enum domains, data setters/getters, Python/EXE consumers, Base/K-Mod/Civ4CE ancestry,
serialization or interface-transfer assumptions, and whole-album dedup. Promote only an independent
consequential current root.

Exact continuation stone:
C031-WIP613 physical -> Queue105 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 116 / 169 complete ->
Queue106 CvDiploParameters.cpp OPEN -> ownership/string/arg-list/enum/ABI/caller/history/dedup sweep ->
either promote a consequential F697 or close cleanly -> freeze next WIP.

Files complete: 116 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP614 -- QUEUE 106 CvDiploParameters.cpp CLOSED CLEAN; QUEUE 107 FAStarFunc.cpp OPEN
===============================================================================
Physical parent: C031-WIP613.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
shipped current callers, FVariable/CvTeam/CvPlayer contracts and local reachable commit-diff history.
Gameplay/XML/Python source untouched.

RESULT
------
Queue 106 closes without spending F697. Current SAS `CvDiploParameters.cpp` is executable-semantically
identical to supplied Base AdvC 1.14; current-vs-Base textual differences are signature collapsing /
line formatting. Its unusual EXE-facing ABI and diplomacy-object ownership survive the current
constructor, argument-vector, trade-list, chat/data flag, save/load and peace-offer-stage review.
No supported current leak, stale argument, invalid enum, use-after-free or handshake failure was
proved.

advc.134a PEACE-OFFER STAGING -- FRAGILE BUT INTENTIONAL / PRIOR NOTE CLOSED
---------------------------------------------------------------------------
`getWhoTalkingTo()` is deliberately not a pure getter. Practical 1406 / b9397691e4 introduced the
working workaround for the BtS EXE bug that discards AI-to-human peace offers:

1. At the start of the active human turn, `CvPlayer::validateDiplomacy()` validates queued
   OFFER_PEACE entries. For the first valid offer it calls the active human team's
   `advancePeaceOfferStage(AI_team)`, establishing stage 1 + the offering team.
2. The EXE later queries `CvDiploParameters::getWhoTalkingTo()` shortly before its erroneous war
   test. Only when the active team is exactly at stage 1 for this object's talking player's team does
   the getter advance the handshake to stage 2.
3. The EXE-facing `CvTeam::isAtWarExternal(AI_team)` recognizes exactly stage 2 + that same team,
   clears the transient pair back to stage 0/NO_TEAM and returns false once, thereby preventing the
   EXE from discarding the otherwise-valid peace popup.

The practical-1406 birth diff is strong intent evidence rather than retroactive speculation: its
commit message says the workaround finally worked by making `CvTeam::isAtWar` feign peace "at just
the right moment." The same change deliberately redirected DLL-internal diplomacy legality checks
(`CvPlayer::canContact`, `canTradeItem` and related paths) to the internal `::atWar`/real-state path
so those calls cannot consume the staged one-shot external false result.

The older full `CvTeam.cpp` audit had already recorded the two stage members as intentionally
transient/nonserialized and had retained the source-authored "Known issue in networked multiplayer"
warning below finding threshold pending a concrete failure. Queue 106 supplies the getter half of
that mechanism but does not establish a new supported failure: no shipped DLL call was found that
consumes stage 2 between the intended getter and EXE check. `validateDiplomacy` itself explicitly
supports one valid peace offer per turn and discards later valid peace offers; that is documented
implementation scope, not an accidental hidden second-offer state.

Do not "clean up" the side effect from `getWhoTalkingTo` or route internal war checks back through
`isAtWarExternal`; either would break the practical-1406 workaround. The network-MP warning remains
a known authored limitation unless runtime evidence gives a reproducible ordering failure and a safe
replacement protocol.

FVariable / DIPLO-COMMENT ARGUMENT OWNERSHIP
--------------------------------------------
The temporary-string path is safe in its supported use:
- `addVar(wchar const*)` allocates a string in a temporary `FVariable`.
- `std::vector::push_back` invokes `FVariable`'s copy constructor; `CopyFrom` deep-copies STRING /
  WSTRING data.
- assignment of the temporary vector into `m_diploCommentArgs` therefore produces independent owned
  string allocations, and destruction of the temporary vector cannot invalidate the stored args.
- the stored vector then releases its strings through `FVariable::~FVariable` at object destruction.

`FVariable::operator=` / `CopyFrom` and `Read` do have a latent overwrite weakness: they do not first
free an already-owned STRING/WSTRING before replacing it, and self-assignment is not guarded. This
could leak or self-corrupt if an existing string-bearing `FVariable` were overwritten. The supported
`CvDiploParameters` paths do not activate it: DLL producers construct a fresh parameters object and
set its diplo comment once; `CvPlayer::read` first clears the old diplomacy queue, allocates a fresh
`CvDiploParameters(NO_PLAYER)`, resizes a fresh default-int vector, then reads each FVariable once.
The EXE-facing external hotseat read contract gives no source-supported repeated-read witness. Keep
this as FVariable/API robustness debt rather than F697.

`setDiploComment(eCommentType, pArgs=NULL)` updates the comment type but deliberately leaves an
existing argument vector unchanged. Internal producers do not reuse a parameter object for a second
argument-less comment, and the overload macro calls this shape only on freshly constructed objects;
no stale-argument current path was established.

TRADE LIST / STRING / FLAG / SAVE CONTRACTS
-------------------------------------------
- `setOurOfferList` and `setTheirOfferList` clear the destination and value-copy each `TradeData`
  node; returned const references remain valid for the lifetime of the parameters object. No borrowed
  caller-owned list node is stored.
- `m_szChatText` is value-owned `CvWString`. The exported setter retains the inherited non-NULL input
  assumption; no shipped DLL call passes NULL, and no EXE-side NULL activation can be proved from
  local source.
- `m_bPendingDelete` is constructor-reset and intentionally absent from the save stream. There is no
  internal getter/setter consumer in the DLL; it is EXE-transient state, not durable game state.
- `m_bRenegotiate` is likewise documented as never set internally and behaviorally irrelevant in
  observed EXE use. The remaining AIContact/HumanDiplo/OurOffering/TheirOffering/Data fields are
  scalar state with paired current read/write handling where intended.
- `m_eWhoTalkingTo` and `m_eCommentType` are serialized as ints and restored before any current DLL
  consumer. The `NO_PLAYER` construction used by loading is therefore only a pre-read placeholder.
- diplo-comment vector size is serialized symmetrically. Negative/absurd sizes or invalid serialized
  enum/type values belong to malformed/corrupt-save hardening; same-version valid saves originate
  from the paired writer and do not produce those values.

ABI / LIFETIME CONTROLS
-----------------------
The header intentionally fixes EXE-facing layout and prevents accidental value copying:
`boost::noncopyable` blocks `CvDiploParameters` copy/assignment and the static assertion requires
`sizeof(CvDiploParameters)==100`. No current SAS data member, vtable or ordering drift was found.
The DLL's queued diplomacy objects are deleted by `CvPlayer::clearDiplomacy` / validation when it
owns them; direct `beginDiplomacy` objects follow the longstanding EXE ownership contract documented
at their callers. No new Queue106 double-owner or orphan path was established.

HISTORY / PROVENANCE
--------------------
The retained file history was checked rather than inferred from the final body:
- K-Mod baseline supplies the core object, argument-vector and serialization implementation;
- practical 1395 only documents the unused renegotiate flag;
- practical 1406 introduces the complete advc.134a getter-side peace-offer handshake;
- practicals 1469/1497 adjust include dependencies;
- merge practical 1548 carries the same 1406 behavior into the alternate branch;
- practical 1564 only renames `getGameINLINE` to `getGame`;
- practical 1744 const-corrects CLinkList traversal;
- practical 2043 removes redundant explicit CLinkList clears and cleans serialization loops;
- practical 2483 moves the FVariable include out of the header;
- practical 2623 changes the trade-list loops to the common traversal macro;
- practical 2859 refactors FVariable-facing names/inline getter placement without changing ownership;
- practical 2998 is the corresponding merge reconciliation;
- SAS practical 5961 only collapses a function signature.
Supplied Base AdvC 1.14 retains the same executable behavior, so there is no missed upstream repair.

FINAL DEDUP / CLOSURE
---------------------
The peace-stage network-MP warning remains the already-recorded CvTeam audit note; no independent
Queue106 activation was proved. Generic FVariable overwrite semantics are cross-file robustness and
not activated by current diplomacy-object construction/load. Hotseat direct-read behavior cannot be
inferred beyond the exported contract from local source, so it is not promoted on speculation.
No independent Queue106 root survives.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 107 `CvGameCoreDLL/FAStarFunc.cpp` (262 queued lines), OPEN under
F697 / provisional KI#1018 unused.

Audit the legacy engine-callback A* glue end-to-end: callback data casting, parent/node plot lookup,
coordinate/index bounds, movement-cost and heuristic domains, team/unit/player enum contracts,
route/area/irrigation/path destination state, callback return conventions, EXE lifetime assumptions,
Base/K-Mod/Civ4CE lineage and whole-album dedup against GroupPathFinder/TeamPathFinder/CvMap/CvPlot.
Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP614 physical -> Queue106 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 117 / 169 complete ->
Queue107 FAStarFunc.cpp OPEN -> callback/data/index/path/heuristic/lifetime/history/dedup sweep ->
either promote a consequential F697 or close cleanly -> freeze next WIP.

Files complete: 117 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP615 -- QUEUE 107 FAStarFunc.cpp CLOSED CLEAN; QUEUE 109 CvPopupInfo.cpp OPEN
===============================================================================
Physical parent: C031-WIP614.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
local K-Mod/Civ4CE/Taurus source, current finder setup/callers, reachable commit-diff history and
whole-album pathfinding/CvGlobals controls. Gameplay/XML/Python source untouched.

RESULT
------
Queue 107 closes without spending F697. Current SAS `FAStarFunc.cpp` has 262 physical lines and is
executable-semantically identical to supplied Base AdvC 1.14 after comments/whitespace are removed;
the Base file has only formatting/signature-layout differences. The live EXE callback glue preserves
the callback ABI and all current DLL call sites provide the expected group/player/area/bool context.
No independent null-data, bad enum, stale callback-data, coordinate, finder-lifetime or callback-
return defect survived the file-level pass.

PATH / INTERFACE-PATH CALLBACK CONTRACTS
----------------------------------------
`pathDestValid`, `pathCost`, `pathValid` and `pathAdd` are now thin wrappers around the already-
audited `GroupStepMetric` implementation. They reinterpret the callback data as a
`CvSelectionGroup const*` and derive `MovementFlags` from FAStar `GetInfo`.

The only current DLL use of the legacy EXE `InterfacePathFinder` explicitly calls
`SetData(&GC.getInterfacePathFinder(), gDLL->UI().getSelectionList())` immediately before
`GeneratePath`. `CvDLLInterfaceIFaceBase::getSelectionList()` returns `CvSelectionGroup*`, so the
callback cast is type-correct and the selection group remains an engine-owned live object for the
synchronous query. The ordinary global `PathFinder` is no longer used for normal DLL unit
pathfinding: `CvSelectionGroup::generatePath` uses the replacement `GroupPathFinder`, with the old
FAStar comparison code retained only as commented verification scaffolding.

The path callbacks intentionally do not repeat every `GroupStepMetric` proof here. Queue 054's deep
`GroupPathFinder.cpp` audit already owns the substantive generic movement/path-state roots F300-F305
(path-dependent danger revalidation, max-path commitment, mixed-unit movement-state accounting,
pre-DOW route simulation and sea-exploration heuristic admissibility). Queue 107 adds no separate
wrapper-layer activation for those roots. Practical 4196's later fix is present: `pathValid` first
runs the path-independent `canStepThrough(kFrom, group, flags)` and then the path-state danger
variant, matching the post-K-Mod repair intended to avoid closing a plot solely from one
path-dependent arrival state.

STEP FINDER -- LIVE CALLERS BOUNDED
----------------------------------
`stepDestValid` requires source and destination to share the current map area. `stepValid` then
rejects impassable destination plots, cross-area steps and `CvMap::isSeparatedByIsthmus` crossings;
`stepAdd` stores the ordinary nonnegative edge count in `m_iData1`; heuristic and cost are the
matching step-distance / unit-edge-cost pair.

Current DLL `StepFinder` producers are bounded:
- `CvMap::calculatePathDistance` rejects NULL source/destination pointers and passes their real map
  coordinates;
- the CvPlayerAI and CvUnitAI callers likewise pass existing plot/unit coordinates and reset/reuse
  the same initialized finder according to its documented synchronous contract;
- `CvMap::setup` initializes the finder to the current grid width/height and wrap flags after map
  setup/load.
The EXE therefore supplies bounded node coordinates to the callbacks on supported queries. The
start==destination special case can return distance zero even if that plot itself is impassable;
that is ordinary graph-distance-to-self semantics, not evidence of a traversable impassable edge.
No current overflow is possible for the stored step count on supported Civ4 map dimensions.

ROUTE / BORDER FINDERS -- INFO DOMAIN AND SEMANTICS
---------------------------------------------------
`routeValid` casts FAStar info to `PlayerTypes` and tests whether each traversed unowned/team plot
carries that player's `getBestRoute`. Its sole current DLL producer is
`CvCityAI::AI_updateRouteToCity`, which passes `getOwner()` from a live city. Thus `TEAMID(ePlayer)`
and `GET_PLAYER(ePlayer)` are reached only with a real player in the supported path.

The separate stale `m_routeToCity` lifecycle problem is already F542/KI#865: route topology can
change after the city caches this finder's result. That is a caller/cache invalidation root, not a
defect in `routeValid`; a fresh call correctly observes the current RouteType and best-route rule.

`borderValid` similarly expects a player ID and admits only plots on that player's team after the
initial node. Current CvCityAI/CvPlayerAI callers pass `kPlayer.getID()` / `getID()` and real plot
coordinates. Their caller-controlled cardinal-only choice is deliberate: land searches can forbid
diagonal-only corner connection while water searches may allow ship-diagonal reachability. No
NO_PLAYER info producer was found.

AREA FINDER -- LEGACY OPT-OUT PATH, CURRENT DEFAULT USES DFS
------------------------------------------------------------
`areaValid` retains the old BtS land-vs-water partition and `joinArea` assigns each closed node to
the area ID carried in FAStar info. In the legacy branch of `CvMap::calculateAreas`, the area is
created and initialized immediately before the synchronous `GeneratePath`, and that same live
`pArea->getID()` is passed as info. There is no deletion/reassignment interval capable of making the
callback ID stale during the traversal.

Current `PASSABLE_AREAS=1` is explicitly recommended in `GlobalDefines_advc.xml`; with that setting
`CvMap::calculateAreas` uses `calculateAreas_dfs` / representative-area rebuilding instead of this
FAStar callback and incorporates peak/shelf-ice/isthmus passability. Setting PASSABLE_AREAS<=0 is a
supported request for the documented BtS behavior where only land/water partition areas. Therefore
the simpler `areaValid` is not a missed current passability test. The header comment saying AreaFinder
is "Unused unless PASSABLE_AREAS ... is set" has the polarity phrased backwards relative to the
current code; documentation-only, not F697.

The stock/scenario Ice-area lifecycle defects discovered in the CvMap audit are already owned by
that queue (including F636's pre-feature scenario topology root); Queue 107 does not duplicate them.

IRRIGATION CALLBACK DATA / MUTATION LIFETIME -- CLEAN
-----------------------------------------------------
`CvMap::updateIrrigated(CvPlot&)` creates a dedicated FAStar object for the operation and initializes
it immediately before each traversal. `checkFreshWater` receives `&bFoundFreshWater` and
`changeIrrigated` receives `&bIrrigated`; both are stack locals whose lifetime extends through their
synchronous `GeneratePath` calls. The finder is destroyed only after all such traversals complete.
`potentialIrrigation` does not consume callback data.

`changeIrrigated` mutates only `CvPlot::setIrrigated`; that setter updates yield/layout state and does
not recursively invoke `updateIrrigated`, so traversing and applying the component does not recurse
into or destroy the active finder. No callback pointer escapes the query.

PLOT-GROUP FINDER -- PRIOR DORMANT CvGlobals CONTROL, NOT F697
--------------------------------------------------------------
The most syntactically alarming nearby path is not a new Queue107 finding. `plotGroupValid` and
`countPlotGroup` remain as callbacks for the obsolete global PlotGroupFinder, while current
`CvPlotGroup::recalculatePlots` uses its custom DFS and the old finder calls are commented out.

The earlier deep `CvGlobals.cpp` audit already recorded the corresponding dormant issue:
`CvGlobals::getPlotGroupFinder()` carries `FErrorMsg("Does this actually get called?")`, and if the
EXE had not supplied the finder through the exported setter, its attempted lazy
`Initialize(m_plotGroupFinder, ...)` cannot by the visible interface itself replace a NULL pointer
because the parameter is passed by value; the function would then dereference NULL. No live DLL
caller exists and the author explicitly doubts an EXE caller. Keep this as the already-recorded
dormant/API control rather than allocating F697 without activation. The same prior audit also owns
the possible stale-geometry concern if that obsolete finder were resurrected across map geometry
changes.

CALLBACK ABI / COORDINATE / RETURN CONTROLS
--------------------------------------------
`FAStarFunc.h` preserves the engine callback signatures from `CvDLLFAStarIFaceBase`: point callbacks
and node callbacks use the expected int/BOOL-compatible return domain and raw data/finder pointer
positions. The functions are invoked by the EXE through registered callbacks despite not being DLL
exports; changing their calling convention or parameter layout would therefore be unsafe, but no
current mismatch exists.

All active finders are initialized by `CvMap::setup` with the current grid dimensions and wrap flags,
so node/start/destination coordinates returned by the FAStar engine are map-domain coordinates.
The callback code's reference-returning `CvMap::getPlot` therefore relies on the same engine contract
as Base AdvCiv and current shipped callers satisfy it. Arbitrary external invocation with fabricated
FAStar nodes, NULL callback data or invalid `GetInfo` enums is extension/API hardening rather than a
current shipped defect.

HISTORY / PROVENANCE
--------------------
Reachable history was checked rather than treating the compact final wrappers as self-evident:
- practical 2551 / 50ce0a8733 moved the BtS/K-Mod callbacks into the new `FAStarFunc` unit while
  refactoring pathfinding;
- 2558 is tiny refactoring; 2571 applies Cppcheck/const cleanup; 2586 is minor cleanup;
- 2588 restructures KmodPathFinder for extension; 2607 replaces the old FAStar team-step finder with
  TeamPathFinder; 2639 is comments/minor refactoring;
- practical 2646 / da5f282da2 fixes the newly introduced UI path-cost regression by changing the
  wrapper's final `GroupStepMetric::cost` argument from `parent->m_iKnownCost != 0` to
  `parent->m_iKnownCost == 0`; the repaired polarity is present today;
- 2655/2842 are misc. refactoring;
- merge practical 2998 centralizes the substantive path rules in `GroupStepMetric` and leaves these
  functions as the current engine adapters;
- practical 4196 / b3de867844 adds the path-independent danger/step-through check described above;
- SAS practical 5961 only collapses function signatures.
Supplied Base AdvC 1.14 contains the same executable callback behavior. K-Mod/Civ4CE/Taurus retain
the ancestral callback families and provide no newer local oracle showing a repair absent from SAS.

FINAL DEDUP / CLOSURE
---------------------
The Queue054 generic GroupPathFinder roots F300-F305, F542/KI#865 route-target cache invalidation,
CvMap area/topology findings, TeamPathFinder work and the prior CvGlobals dormant PlotGroupFinder
note cover the neighboring failure families. No independent FAStar adapter defect survives current
activation/caller/history checks.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Queue 108 `CvGameCoreDLL/CvAgents.cpp` was already completed at C031-WIP498 / F652-F653, so ordinary
order advances directly to Queue 109 `CvGameCoreDLL/CvPopupInfo.cpp` (246 queued lines), OPEN under
F697 / provisional KI#1018 unused.

Audit popup-info ownership and EXE/Python transfer end-to-end: constructor/copy/destructor and raw
string/vector ownership, popup type/data/option enum domains, text/on-focus callback strings,
button/Python-module plumbing, serialization/read-write symmetry, EXE queue lifetime, Base/K-Mod/
Civ4CE ancestry, current callers and whole-album dedup against CvDLLButtonPopup/CvPopupReturn/
CvDiploParameters. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP615 physical -> Queue107 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 118 / 169 complete ->
Queue108 already complete -> Queue109 CvPopupInfo.cpp OPEN -> ownership/copy/string/vector/enum/
serialization/EXE-Python-lifetime/history/dedup sweep -> either promote a consequential F697 or close
cleanly -> freeze next WIP.

Files complete: 118 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP616 -- QUEUE 109 CvPopupInfo.cpp CLOSED CLEAN; QUEUE 111 CvArtFileMgr.cpp OPEN
===============================================================================
Physical parent: C031-WIP615.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
local K-Mod/Civ4CE source, current CvPlayer/CvDLLButtonPopup popup lifecycle and whole-album popup
findings. Gameplay/XML/Python source untouched.

RESULT
------
Queue 109 closes without spending F697. Current SAS `CvPopupInfo.cpp` has 246 physical lines and is
executable-semantically identical to supplied Base AdvC 1.14 after comments/whitespace are removed.
The class remains an EXE-facing value/transfer object with value-owned strings and Python-button
vector; valid current queueing, save/load and popup-launch paths do not activate its two obvious
reuse hazards. No independent ownership, serialization, index, enum, callback-string or EXE-lifetime
defect survives the supported-current caller/history pass.

ASSIGNMENT -- REAL SELF-ASSIGNMENT HAZARD, NO CURRENT ACTIVATION
---------------------------------------------------------------
`CvPopupInfo::operator=` copies scalar/string fields, clears `m_aPythonButtons`, then repopulates it
from `other`. If `this == &other`, the clear also clears the source and self-assignment silently loses
all Python buttons. This is a genuine generic value-object weakness.

The current DLL has only one assignment call: `CvDLLButtonPopup::OnAltExecute` allocates a fresh
`new CvPopupInfo`, checks it, then executes `*pInfo = info` before handing that distinct new object to
the UI. The target cannot alias the already-existing `info` reference. Queue 019's full
CvDLLButtonPopup audit had already classified this exact OnAlt copy as a deep, ownership-safe copy;
Queue 109 therefore does not invent a new root from unsupported external self-assignment.

The strings and `CvPopupButtonPython` elements are ordinary value-owned CvString/CvWString members;
copying between distinct objects duplicates value state rather than borrowing raw caller storage.
No shallow-string/vector ownership root exists.

READ / WRITE -- APPEND-ON-REUSE IS LATENT, VALID SAVE LOAD IS FRESH
-------------------------------------------------------------------
`CvPopupInfo::read` does not clear `m_aPythonButtons` before pushing the serialized buttons. Reusing
an already-populated object for a second read would therefore append stale buttons. That is a real
exported-API robustness problem but not the current save lifecycle:

- `CvPlayer::read` calls `clearPopups()` first;
- for every serialized popup it allocates a fresh default `CvPopupInfo`;
- it invokes `pInfo->read(*pStream)` exactly once on that fresh object;
- it then stores that pointer in `m_listPopups`.

For valid current saves the button vector is consequently empty at read entry. The paired writer
serializes the same scalar/text/callback/module fields, button count and each button's text/art in the
same order. `m_bPendingDelete` is intentionally absent from the stream and constructor-reset false;
there is no current DLL consumer of that flag outside its own accessors, so it is EXE-transient UI
state rather than durable game state.

Malformed/corrupt streams can still supply an absurd button count or invalid popup enum. Those are
save-hardening concerns; same-version valid streams originate from the paired writer and do not
create those values.

PYTHON BUTTON INDEX / CALLBACK CONTRACTS
----------------------------------------
`getPythonButtonText(i)` and `getPythonButtonArt(i)` assert only `i < size` and therefore do not
reject a negative index before indexing the vector. The only current DLL consumer is
`CvDLLButtonPopup::launchPythonPopup`, whose loop is exactly `for (int i = 0;
i < info.getNumPythonButtons(); i++)`; its index is therefore always nonnegative and strictly
bounded. No shipped Python code calls the C++ getters directly; Python producers only append buttons
through the popup API. An EXE/mod extension could misuse the exported getters with a negative index,
but local shipped activation is absent and this remains API hardening, not F697.

`launchPythonPopup` reads each button text/art from the same live `CvPopupInfo` object and uses the
same loop ordinal as the button ID. Queue 019 already audited that launch/reply relationship and the
OnAlt deep copy. Queue 109 adds no callback/button identity root.

QUEUE / EXE OWNERSHIP LIFECYCLE
-------------------------------
Current ownership transitions remain coherent on the visible DLL side:
- DLL popup producers allocate `CvPopupInfo` and hand the pointer to `CvPlayer::addPopup` or the UI;
- nonhuman `CvPlayer::addPopup` deletes the object immediately;
- queued human popups are owned by `m_listPopups` until deletion/transfer;
- `clearPopups` deletes all pointers still owned by that queue;
- `popFrontPopup` removes and returns the pointer for EXE transfer;
- displayed popups can be recovered by the EXE/UI during save/relaunch, according to the existing
  ownership contract documented in `CvPlayer::relaunchPopups`.

`CvPopupInfo` itself owns no raw heap pointer; its virtual destructor can remain empty because the
member strings/vector destroy their own contents. The longstanding `m_bPendingDelete` flag is not
used by current DLL logic and cannot create a DLL-side double-delete merely because it is not saved.

ABI / LAYOUT CONTROL
--------------------
This class is part of the DLL/EXE interface. AdvC practical 2126 explicitly added the warning that it
is unsafe to add data members and `BOOST_STATIC_ASSERT(sizeof(CvPopupInfo) == 160)`. The assertion is
still present and the current header/member order matches supplied Base AdvC 1.14. No SAS ABI drift
was found.

The convention of checking `new CvPopupInfo` for NULL is historically odd and explicitly questioned
by AdvC's source comment. A hypothetical nonthrowing/custom allocation failure during `CvPlayer::read`
could skip consuming the serialized object body and desynchronize the stream, but no current custom
`operator new` or supported allocation-failure mode establishing that path was found. Do not promote
that speculative failure merely from the defensive NULL checks.

HISTORY / PROVENANCE
--------------------
The retained history is consistent with the current contracts:
- the K-Mod baseline already contains the core scalar/string/button assignment and serialization;
- practical 2126's only `.cpp` semantic-neutral edit is `return (*this)` -> `return *this`, while the
  important change is the 160-byte ABI assertion/warning in the header;
- practical 3102 adds NULL checks at popup allocation callers, moves the empty destructor inline and
  documents uncertainty about the allocation convention; it does not change the value semantics;
- practical 4342 only extends that source comment while moving tech-splash production elsewhere;
- later merge/SAS history retains the same behavior; SAS practical 5961 is signature formatting.
Supplied Base AdvC 1.14 therefore provides no later repair missing from current SAS.

FINAL DEDUP / CLOSURE
---------------------
Queue 019 `CvDLLButtonPopup.cpp` already owns the substantive popup launch/reply/staleness families
and explicitly marked OnAlt's CvPopupInfo copy as deep and safe. Queue 093 `CvPopupReturn.cpp` owns
return-control map/list behavior, and Queue 106 separately covers `CvDiploParameters` EXE state.
Neither CvPopupInfo's self-assignment nor read-reuse weakness is activated by current shipped paths,
and the negative button-index weakness has only bounded current consumers. No independent Queue109
root survives.

F697 / provisional KI#1018 remains UNUSED.

QUEUE ADVANCE
-------------
Queue 110 `CvGameCoreDLL/AgentIteratorTest.cpp` was already completed at C031-WIP499 with no new
root, so ordinary order advances directly to Queue 111 `CvGameCoreDLL/CvArtFileMgr.cpp` (240 queued
lines), OPEN under F697 / provisional KI#1018 unused.

Audit the art-info singleton/cache and XML-type lookup boundary end-to-end: member initialization and
singleton lifetime, category InfoType strings, lazy cache/fallback behavior, NULL art records,
reload/reset semantics, EXE-facing DllExports, current XML identifiers, Base/K-Mod/Civ4CE ancestry
and whole-album dedup against CvGlobals/CvInfo_Asset/CyArtFileMgr. Promote only an independent
consequential current root.

Exact continuation stone:
C031-WIP616 physical -> Queue109 CLOSED CLEAN -> F697/KI#1018 UNUSED -> 119 / 169 complete ->
Queue110 already complete -> Queue111 CvArtFileMgr.cpp OPEN -> singleton/cache/type/XML/null/reload/
ABI/history/dedup sweep -> either promote a consequential F697 or close cleanly -> freeze next WIP.

Files complete: 119 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP617 -- QUEUE 111 CvArtFileMgr.cpp / F697 CONFIRMED; F698 UNUSED
===============================================================================
Physical parent: C031-WIP616.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current Art Defines / ModName rename documentation, practical-4087/4090 history and whole-album
format/logging controls. Gameplay/XML/Python source untouched.

F697 / provisional KI#1018 -- THEME-PATH WARNING USES USER-DERIVED MOD NAME AS printf FORMAT STRING
--------------------------------------------------------------------------------------------------
Status: CONFIRMED CURRENT INHERITED ADVCIV DIAGNOSTIC / STARTUP ROBUSTNESS DEFECT ROOT.

ROOT
----
`CvArtFileMgr::testThemePath()` constructs a `CvString sMsg` that embeds the actual loaded mod-folder
name twice. It then performs:

    char szMessage[1024];
    sprintf(szMessage, sMsg);

No formatting arguments follow `sMsg`. A percent conversion contained in the folder name is therefore
interpreted by `sprintf` as a real printf conversion and makes the function read (or, for `%n`, write
through) nonexistent variadic arguments.

SUPPORTED ACTIVATION
--------------------
This is not arbitrary hostile string injection into an internal helper. The warning exists
specifically for a normal user-configuration error: `DEFAULT_THEME_NAME` contains a `Mods/<name>/`
path that no longer matches the actual loaded mod folder, and its own text says "If you've renamed
the mod folder...".

Current SAS reinforces that supported workflow in `GlobalDefines_advciv_sas.xml`: the actual mod
folder may be renamed, while `DEFAULT_THEME_NAME` and the `.thm` resource path must be updated
manually. `ModName::update` extracts the literal folder component from the EXE-provided Mods path; it
does not escape or remove `%` characters.

A concrete trigger is therefore:
1. install/rename the mod to a valid folder name containing a percent conversion, e.g.
   `AdvCiv-SAS-%s`;
2. leave `Assets/XML/Art/CIV4ArtDefines_Misc.xml` at the old `DEFAULT_THEME_NAME`, exactly the
   mismatch this warning is designed to diagnose;
3. art maps finish building and `buildArtFileInfoMaps()` calls `testThemePath()`;
4. the extracted theme-folder name differs from the actual folder, so the warning branch builds
   `sMsg` containing `AdvCiv-SAS-%s`;
5. `sprintf(szMessage, sMsg)` interprets `%s` and fetches a nonexistent pointer argument. The warning
   can print unrelated memory, fault while reading that bogus pointer, or otherwise execute undefined
   varargs behavior instead of safely explaining the configuration error.

A `%n` sequence is even more structurally dangerous because the formatter would expect a pointer to
an integer and write through a nonexistent argument. The finding does not depend on selecting that
worst case; any conversion already proves that literal user path text is not preserved.

LINEAGE
-------
AdvC practical 4087 / `c0ed55ab48ec30fc14f1959f3c69646565a529c7`, "Better warnings about
failure to load UI theme", introduced `testThemePath`, the renamed-folder message and
`sprintf(szMessage, sMsg)` in the same change. Practical 4090 fixes only case-insensitive comparison
of the extracted folder names and leaves the formatting call untouched. Supplied Base AdvC 1.14 and
current SAS retain the defect executable-identically.

SCOPE / DEDUP
-------------
`CvGlobals::testInstallLocation()` contains the superficially identical `sprintf(szMessage, sMsg)`
idiom from practical 4087, but that function's `sMsg` is a fixed literal with no appended filesystem
text and no `%` conversion. It therefore does NOT share F697's user-derived format-string activation
and should not be broadened merely because the syntax looks the same.

The earlier Queue036 CvMap note about a static malformed `%6` logging format is a different root: an
intrinsically invalid fixed format string, not user-derived data being interpreted as a format.
Queue099's logger findings concern map-stat indexing and turn-state polarity, not this warning path.
No prior album owner for the theme-warning format-string root was found.

REPAIR BOUNDARY
---------------
No formatting is needed here at all. Pass the already-built string directly to the message box, e.g.
conceptually `gDLL->MessageBox(sMsg.c_str(), sHeading.c_str())`, or if the intermediate buffer must be
retained, use a fixed format such as `sprintf(szMessage, "%s", sMsg.c_str())` with appropriate
bounded copying. Do not escape individual percent signs in the mod name; path text is data, not a
format language.

F697 / provisional KI#1018 is CONFIRMED.
F698 / provisional KI#1019 is UNUSED.

QUEUE 111 REMAINS OPEN
----------------------
Continue `CvArtFileMgr.cpp` under F698: finish map-key lifetime, fallback/NULL behavior, category XML
coverage, Init/DeInit/Reset/resetInfo lifecycle, singleton static-construction/destruction, wide-city-
bar swap state, theme-path parsing, current hardcoded/dynamic art-tag validation, Python/EXE ABI,
remaining practical history and whole-album dedup. Freeze the next WIP at the next settled boundary,
whether Queue111 yields F698 or closes cleanly.

Exact continuation stone:
C031-WIP617 physical -> Queue111 OPEN -> F697/KI#1018 CONFIRMED -> F698/KI#1019 UNUSED -> finish
art-map/tag/NULL/reset/singleton/city-bar/theme parsing/XML/history/dedup sweep -> either promote an
independent consequential F698 or close Queue111 -> freeze next WIP.

Files complete: 119 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP618 -- QUEUE 111 CvArtFileMgr.cpp CLOSED / F697 CONFIRMED; F698 UNUSED
===============================================================================
Physical parent: C031-WIP617.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
local Civ4CE BtS art XML, practical-3801/3803 and later reachable history, current XML/callers and
whole-album dedup. Gameplay/XML/Python source untouched.

QUEUE 111 FINAL RESULT
----------------------
Queue 111 closes with F697 / provisional KI#1018 as its sole new root. F698 remains unused.
The remaining art-map, singleton, reset, city-bar, tag-coverage and NULL/fallback surfaces were
traced after F697 rather than left implicit.

ART-MAP KEY OWNERSHIP / LIFETIME -- CLEAN ON CURRENT LIFECYCLE
--------------------------------------------------------------
Each category map stores `const char*` keys obtained from the owned art-info object's `getTag()` /
`CvInfoBase::m_szType`; the map comparator compares string contents through strcmp rather than pointer
identity. The key storage therefore remains valid for exactly the lifetime of the corresponding info
objects.

`deInit()` deletes the category map BEFORE deleting the info objects and clearing their vector, so no
map operation can compare a key after its backing info has been destroyed on that path. `Reset()`
performs DeInit -> SetGlobalArtDefines -> Init -> buildArtFileInfoMaps, creating a new map before
loading/building new keys. `CvGlobals::infosReset()` (display-language change) calls art-info
`reset()`, but `CvInfoBase::reset()` only clears translated-text caches; it does not alter `m_szType`,
so existing map keys are not invalidated by language switching.

Calling exported `buildArtFileInfoMaps()` repeatedly without an XML reload merely reinserts the same
live info/tag objects into the existing maps. No current path deletes/replaces the info vectors while
retaining the old map.

CURRENT ART-TAG COVERAGE -- CLEAN
--------------------------------
A mechanical literal-caller census across current C++/Python found 328 literal calls to
`get<Category>ArtInfo("...")` / `get<Category>ArtPath("...")`. Their tags were checked against the
union of current SAS Art Defines plus the shipped Civ4/Warlords/BtS fallback Art Defines supplied by
Civ4CE. Zero literal lookups are unresolved.

Current mod Art-Define files also contain no duplicate `<Type>` within any supplied category, so
BUILD_INFO_MAP does not silently collapse two current same-name entries.

The generic missing-tag fallback remains intentionally sharp: a missing tag emits an error and asks
for category tag `ERROR`; if that is absent the lookup returns NULL, and `get*ArtPath` callers would
then dereference NULL. Current shipped literal callers do not activate that path; dynamic bad art tags
are XML/mod-extension robustness and were not promoted as F698.

CITY-BAR SWAP STATE -- SUPPORTED PATH CLEAN
-------------------------------------------
`swapCityBarPaths()` toggles `m_bCityBarPathsSwapped` before validating six art records, which would
leave the Boolean wrong if a record were missing. That is a real malformed/not-ready robustness edge,
but all six current tags exist:
- INTERFACE_CITY_BAR_MODEL / INTERFACE_WIDE_CITY_BAR_MODEL;
- INTERFACE_CITY_BAR_REGULAR_GLOW / INTERFACE_WIDE_CITY_BAR_REGULAR_GLOW;
- INTERFACE_CITY_BAR_CAPITAL_GLOW / INTERFACE_WIDE_CITY_BAR_CAPITAL_GLOW.

The routine gathers all six pointers before changing any path, so there is no partial path swap.
Normal activation is `CvGame::onGraphicsInitialized -> setCityBarWidth`; the art manager is already
built there. Conversely `CvArtFileMgr::Reset()` refuses once graphics are initialized. Thus the
supported lifecycle cannot reset art vectors behind an already-swapped Boolean, nor call the normal
city-bar path before maps are ready. The practical-3803 refactor therefore remains coherent on
current data.

RESET / SINGLETON / CATEGORY COVERAGE
-------------------------------------
`Init`/`DeInit` iterate the same self-registered ArtInfoItem set. The local-static manager is
constructed before the local-static category registrars; registrar construction calls back into the
already-created manager to register itself. Current AdvC has run with this practical-3801 lifetime
model for years, and no concrete target-runtime re-entrancy/destruction failure was established.
Static category objects are destroyed before the manager at process teardown, but the manager's
empty destructor does not dereference their now-stale pointers; ordinary runtime cleanup is handled
through DeInit.

`resetInfo()` covers every loaded concrete art category that participates in CvGlobals info reset.
The abstract/base Asset category has a map/vector but no SetGlobalArtDefines load and no current
runtime lookup owner; this is inherited structure, not missing current data.

THEME PARSING -- F697 REMAINS THE ONLY CURRENT ROOT
--------------------------------------------------
The substring length in `sThmPath.substr(posMods + 5, posRes - 5)` is only exact when `Mods/` begins
at offset zero. Current DEFAULT_THEME_NAME is exactly `Mods/AdvCiv-SAS/Resource/Civ4.thm`, so the
supported current path starts at zero and extracts the expected folder name. Prefixed absolute/custom
forms can false-warn and backslash forms bypass the check; those are diagnostic robustness edges, not
a second current defect under the shipped configuration.

F697's untrusted-format-string use remains independently confirmed and is not weakened by this
closure.

FINAL DEDUP / CLOSURE
---------------------
Queue 056 already audited the art-info value objects; Queue 075 owns the SelfMod allocation root;
Queue 142 will later inspect the Python CyArtFileMgr wrapper itself. Existing XML/UI findings do not
own F697, and no independent current art-map/reset/city-bar/tag root survives after the checks above.

F697 / provisional KI#1018 remains CONFIRMED.
F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Queue 112 `CvGameCoreDLL/UWAI.cpp` was already completed at C031-WIP500 with no new root. Ordinary
order therefore advances directly to Queue 113 `CvGameCoreDLL/CyTeamInterface.cpp` (235 queued
lines), OPEN under F698 / provisional KI#1019 unused.

Audit the Boost.Python Team registration layer end-to-end: exported names vs native CyTeam targets,
overload/arity/defaults, return ownership, enum/index exposure, null wrappers, current shipped Python
callers, Base/K-Mod/Civ4CE lineage and whole-album dedup against Queue 051 CyTeam.cpp and the deep
CvTeam/CvTeamAI audits. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP618 physical -> Queue111 CLOSED -> F697/KI#1018 CONFIRMED -> F698/KI#1019 UNUSED ->
120 / 169 complete -> Queue112 already complete -> Queue113 CyTeamInterface.cpp OPEN -> binding/
name-target/arity/ownership/caller/history/dedup sweep -> either promote consequential F698 or close
cleanly -> freeze next WIP.

Files complete: 120 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP619 -- QUEUE 113 CyTeamInterface.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP618.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current CyTeam.cpp/.h, shipped Python callers and whole-album Queue051/CvTeam/CvTeamAI controls.
Gameplay/XML/Python source untouched.

QUEUE 113 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
The file is newline-normalized text-identical to supplied Base AdvC 1.14, so there is no SAS binding
drift to explain independently.

BINDING TABLE -- CLEAN
----------------------
A mechanical parse finds 188 active `.def` CyTeam registrations. Every exported Python name equals
the referenced `CyTeam::method` target; there are no duplicate exported names and no accidental
name-to-neighbor target mismatch. All return values in this file are scalar/Boolean/enum-as-int or
`std::wstring`; no fresh/borrowed pointer return exists, so there is no Boost.Python ownership-policy
surface analogous to F688/KI#1009.

The actual C++ function pointers determine arity correctly. Two help strings are stale:
- `setVassal` documents only `(TeamTypes, bool)` although CyTeam::setVassal also requires the
  `bCapitulated` Boolean;
- `canLaunch` documents `bool ()` although the method requires a VictoryTypes argument.
These are Python API documentation defects only; they do not alter the bound signatures or shipped
behavior and are not promoted.

NULL / AREA WRAPPER EDGE -- UNSHIPPED API ROBUSTNESS
----------------------------------------------------
The seven `count*ByArea(CyArea*)` wrappers dereference `pArea->getArea()` after checking only the
owning CyTeam pointer. Passing Python None or a null CyArea wrapper would therefore crash. A current
shipped-Python census finds no caller of these Team area-count methods at all. This is inherited
extension-API hardening in CyTeam.cpp, not an activated Queue113 registration root; Queue051 already
audited the wrapper body and no new number is spent here.

ENUM / MUTATOR CALLERS
----------------------
The live shipped Team mutators in WorldBuilder/random events pass concrete team/tech/war-plan values
from bounded engine or XML state. `assignVassal`, rather than the lower-level three-argument
`setVassal`, is the shipped WorldBuilder restoration path. Random-event war/peace bridges use the
purpose-built event wrappers added by AdvC. No registration truncates or reorders their parameters.

Current `getPower()` calls found in shipped advisor/diplomacy Python resolve to CyPlayer objects, not
CyTeam; the CyTeam registration correctly requires its explicit `bIncludeVassals` Boolean. No hidden
zero-argument Team call was established.

HISTORY / DEDUP / CLOSURE
-------------------------
Supplied Base AdvC 1.14 has the same registration table. The deep CvTeam/CvTeamAI audits own native
war/vassal/research/state semantics, while Queue051 owns CyTeam wrapper-body null/enum contracts.
Queue113 therefore adds no independent root merely from stale help prose or unsupported Python API
misuse.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 114 `CvGameCoreDLL/CvInfo_Symbol.cpp` (211 queued lines), OPEN
under F698 / provisional KI#1019 unused.

Audit symbol/hotkey/action-info construction and XML ownership end-to-end: defaults, enum/index
arrays, mission/action mapping, Python exposure, current XML dimensions/data, Base ancestry and
whole-album dedup. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP619 physical -> Queue113 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 121 / 169 complete ->
Queue114 CvInfo_Symbol.cpp OPEN -> defaults/XML/action-index/hotkey/Python/history/dedup sweep ->
either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 121 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP620 -- QUEUE 114 CvInfo_Symbol.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP619.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current Yield/Commerce XML, supplied Civ4CE color/player-color fallback XML, symbol initialization
callers and whole-album controls. Gameplay/XML/Python source untouched.

QUEUE 114 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current CvInfo_Symbol.cpp and .h are newline-normalized text-identical to Base AdvC 1.14.

YIELD / COMMERCE DEFAULTS AND RUNTIME SYMBOLS
---------------------------------------------
All 3 current Yield rows supply the scalar fields consumed by CvYieldInfo, including valid
COLOR_YIELD_* references; all resolve in the shipped color universe. All 4 Commerce rows supply the
active initial-percent/happiness/AI-weight/flexible fields.

`m_iPopulationChangeDivisor` defaults to zero and current Yield XML also uses zero. The live CvPlot
consumer explicitly checks `getPopulationChangeDivisor() != 0` before dividing, so the zero default
is a disabled effect rather than a divide-by-zero root.

`m_wcSymbol` begins at zero because the symbol codepoints are not XML-owned here. CvGameTextMgr later
assigns Yield and Commerce chars through `setChar()` from the GameFont symbol IDs; this is the
intended post-load initialization lifecycle and not missing XML state.

DISABLED SYMBOL PATH STORAGE -- DEAD BY DESIGN
----------------------------------------------
AdvC intentionally stopped loading Yield `SymbolPaths`; `m_paszSymbolPath` stays NULL and the
accessor emits FErrorMsg then returns NULL. A whole-current-tree call search finds no caller of
`getSymbolPath()`. Current Yield XML retains the old path rows for data compatibility/documentation,
but the game renders yield symbols from the shared vertical-symbol atlas as the source comment says.
No live dangling/uninitialized array exists.

COLOR / PLAYER COLOR
--------------------
CvColorInfo reads all four RGBA scalars from the standard color XML. CvPlayerColorInfo initializes
all three ColorTypes to NO_COLOR before reading its primary/secondary/text references. The supplied
Civ4/Warlords/BtS fallback color universe contains the current Yield colors and ordinary player-color
data; no unresolved current reference was established.

The `getColorTypePrimaryExternal` / `SecondaryExternal` helpers deliberately expose typed ColorTypes
as ints for Boost.Python. No pointer ownership or index array is present in this file.

HISTORY / DEDUP / CLOSURE
-------------------------
The main AdvC changes here are extraction/refactoring, wchar typing and deliberately disabling the
unused Yield symbol-path array. Base 1.14 retains exactly the current behavior. Queue 070 already
covers CvInfoBase reset/text-cache behavior and the earlier symbol initialization code was audited in
CvGameTextMgr; no independent Queue114 root survives.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 115 `CvGameCoreDLL/CvMessageControl.cpp` (208 queued lines), OPEN
under F698 / provisional KI#1019 unused.

Audit every network-message sender against the corresponding CvMessageData constructor/execute
contract: field order/types, player/team/city/unit IDs, enum casts, Python/UI callers, ownership and
multiplayer determinism, then Base/K-Mod/Civ4CE history and dedup against Queue044 CvMessageData.

Exact continuation stone:
C031-WIP620 physical -> Queue114 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 122 / 169 complete ->
Queue115 CvMessageControl.cpp OPEN -> sender/receiver field-order/ID/enum/network/history/dedup sweep
-> either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 122 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP621 -- QUEUE 115 CvMessageControl.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP620.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current CvMessageData.cpp/.h, current C++/Python sender call sites, AdvC/K-Mod local commit-diff
history and whole-album Queue044 controls. Gameplay/XML/Python source untouched.

QUEUE 115 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current CvMessageControl.cpp and .h are executable-semantically identical to supplied Base AdvC
1.14 after comments/whitespace normalization; there is no missed newer Base fix or SAS protocol
drift in this sender layer.

ALL SENDER -> MESSAGE-CONSTRUCTOR CONTRACTS -- CLEAN
----------------------------------------------------
The 28 current send methods were reconciled against the corresponding CvNet* constructor signatures,
stored fields and the already-audited Queue044 Execute side. Argument order and types agree throughout,
including same-underlying-type positions that compilation alone would not reliably diagnose:
- PushOrder: active player, city ID, OrderTypes, data, save, pop, position;
- DoTask: city ID, TaskTypes, data1/data2, option/Alt/Shift/Ctrl;
- AdvancedStartAction: action, explicit player, X/Y, data, add/remove;
- FoundReligion: explicit player, religion, slot religion;
- EventTriggered: explicit player, EventTypes, triggered-instance ID;
- JoinGroup: source unit ID, target-head ID / INVALID_INDEX sentinel;
- PushMission: source unit ID, mission, data1/data2, MovementFlags, shift, modified;
- ChooseElection: selection then vote ID;
- DiploVote: vote ID then PlayerVoteTypes choice;
- ChangeVassal: master team, vassal Boolean, capitulated Boolean.
No sender swaps, truncates or silently casts a currently distinct serialized field.

ACTIVE-PLAYER / EXPLICIT-PLAYER CONTRACT
----------------------------------------
The ordinary local-action senders first require `isActive()`, whose sole contract is
`getActivePlayer() != NO_PLAYER`, before serializing the active player. This covers ExtendedGame,
AutoMoves, TurnComplete, Push/PopOrder, DoTask, UpdateCivics, Research, EspionageChange, Convert,
JoinGroup, Push/AutoMission, DoCommand, PercentChange, ChangeVassal, election/vote, war and ping.

The unguarded senders fall into intentional separate domains:
- AdvancedStartAction, EmpireSplit, FoundReligion, Launch and EventTriggered serialize the explicit
  PlayerTypes supplied by their caller rather than deriving an implicit active player;
- ModNetMessage is deliberately playerless generic five-int mod traffic;
- FPTest and CivLeaderSetup are AdvC-specific lifecycle messages discussed separately below.
Current in-tree call-site census does not expose an accidental implicit-NO_PLAYER path in the
explicit-player family. `sendChangeVassal` and `sendPing` currently have no in-tree producer, but
that does not alter the correctness of their guarded sender implementation.

FP TEST -- DEDUP TO F674 / KI#996, NO NEW SENDER ROOT
------------------------------------------------------
`sendFPTest` itself lacks the generic `isActive()` wrapper and serializes `getActivePlayer()`.
Its one current producer is `CvGame::updateTestEndTurn`. Before reaching the send, that routine has
already passed final-initialization protection and dereferenced the active player to require that
player's turn to be active. The current supported UI-turn path therefore already presupposes a real
active player; Queue115 does not establish a new NO_PLAYER sender activation.

The actual multiplayer FP-test defect is already F674/KI#996 from Queue044: `m_bFPTestDone` accepts
only the first received checksum, so a self/compatible first message can mask a later incompatible
machine. Queue044 traced CvNetFPTest serialization, execution and network lifecycle end-to-end.
Do not renumber that receiver/protocol root merely because this file contains the sending wrapper.

CIV/LEADER SETUP -- GLOBAL HOST PAYLOAD, NOT PLAYER-TARGETED
------------------------------------------------------------
`sendCivLeaderSetup` likewise has no generic active guard, but Queue044 and practical 3579 establish
that its payload is intentionally global: the host copies the complete per-player
`wasCivRandomlyChosen` and `wasLeaderRandomlyChosen` maps and broadcasts them during network game
initialization. CvNetCivLeaderSetup::Execute applies those full maps to InitCore on each machine.
Its stored `m_ePlayer` is unused/not serialized metadata; the message is not supposed to mutate only
that player. No missing player execution gate or constructor mismatch exists here.

CALL-SITE / RECEIVER DEDUP SATURATION
-------------------------------------
A whole-current-tree sender census was reconciled with Queue044's 28-Execute closure. Consequential
stale-state receiver behavior is already owned where appropriate:
- JoinGroup stale real target vs explicit ungroup sentinel -> F675/KI#997;
- FP test first/self-message masking -> F674/KI#996;
- Advanced Start destructive fallback -> earlier F485/KI#808;
- stale free-tech, popup/vote/war/event and liberation paths -> their earlier dedicated roots.
The sender wrappers do not introduce a second object identity, queue ordinal or permission failure
on top of those receiver contracts. Generation-safe city/unit IDs and authoritative receiver-side
legality checks remain the relevant protections established in Queue044.

HISTORY / NEGATIVE CONTROLS
---------------------------
Local history saturation accounts for the semantic sender changes that survive today:
- K-Mod changed PushOrder to the save/pop/position queue protocol, paired with its CvNetPushOrder;
- practical 1399/1448 introduced then folded the modified-mission bit into CvNetPushMission;
- practical 1500 introduced FPTest sender/message together (its independent protocol flaw is F674);
- practical 2416 converted civic traffic to CivicMap on both sides;
- practical 2551 typed mission flags as MovementFlags on both sides;
- practical 3218 centralized repeated active-player checks into getActivePlayer()/isActive() without
  changing which ordinary action senders are guarded;
- practical 3579 introduced the global CivLeaderSetup sender/message together.
Later SAS work in this file is formatting/signature collapsing only. Supplied Base 1.14 retains the
same executable sender layer.

Generic malformed/forged network values remain protocol/API hardening rather than a normal producer
finding. No raw heap ownership is retained by CvMessageControl: each call allocates one CvNet*
message and immediately transfers it to the engine's `sendMessageData` channel under the same
ownership model used throughout BtS/K-Mod/AdvC.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 116 `CvGameCoreDLL/CyArea.cpp` (205 queued lines), OPEN under
F698 / provisional KI#1019 unused.

Audit the Python Area wrapper end-to-end against CvArea and the later Boost.Python registration:
underlying pointer/null lifetime, city/unit/plot iteration helpers, enum/index arguments, object
ownership on returned wrappers, shipped Python callers, Base/K-Mod/Civ4CE lineage and whole-album
dedup against the completed CvArea audit. Promote only an independent consequential current root.

Exact continuation stone:
C031-WIP621 physical -> Queue115 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 123 / 169 complete ->
Queue116 CyArea.cpp OPEN -> pointer/lifetime/iteration/enum/ownership/caller/history/dedup sweep ->
either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 123 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP622 -- QUEUE 116 CyArea.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP621.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current CyAreaInterface.cpp, native CvArea.cpp/.h, shipped Python/map scripts, local AdvC/K-Mod
history and whole-album Queue060/Queue102/Queue113 controls. Gameplay/XML/Python source untouched.

QUEUE 116 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current CyArea.cpp, CyArea.h and CyAreaInterface.cpp are text/semantic-equivalent to supplied Base
AdvC 1.14 after newline normalization; no newer Base repair or SAS wrapper drift is missing here.

NULL / WRAPPER BODY CONTRACTS
-----------------------------
Every Python-facing scalar CyArea method checks `m_pArea` before dereferencing it and returns the
inherited sentinel (`-1` or false) for a null wrapper. `getNumHabitableTiles` likewise explicitly
returns -1 for NULL before calling the native count helper.

The C++-only `getArea()` accessor returns `CvArea&` and therefore assumes a non-null wrapper. That is
an inherited internal precondition rather than a Python method. Its cross-wrapper consumers were
already inspected in the CyTeam/CyPlot/CyGameCoreUtils passes; current shipped callers do not pass a
null CyArea wrapper into those dereferencing bridges. Unsupported None/null-wrapper use remains API
hardening, not F698.

ENUM / INDEX ARGUMENTS
----------------------
The PlayerTypes/TeamTypes/YieldTypes/UnitAITypes/BonusTypes integers are thin casts into the already-
audited native CvArea getters. Native arrays use their documented enum domains, and current shipped
Python callers obtain those values from bounded player/team/info loops or concrete engine objects.
No normal script path was found passing NO_PLAYER/NO_TEAM where the native getter requires a real
index. `countHasReligion` / `countHasCorporation` intentionally accept NO_PLAYER as their native
"all owners" sentinel.

TARGET-CITY RETURN OWNERSHIP -- REAL API LEAK SHAPE, UNSHIPPED
--------------------------------------------------------------
`CyArea::getTargetCity(player)` allocates a fresh wrapper:

    return m_pArea ? new CyCity(m_pArea->AI_getTargetCity(...)) : NULL;

while `CyAreaInterface.cpp` registers that return with `reference_existing_object` rather than
`manage_new_object`. Structurally this is the same inverse wrapper-ownership pattern as the live
F688/KI#1009 unit-cycling leak.

However, a whole current shipped-Python/map-script census finds ZERO calls to CyArea.getTargetCity.
All current target-city consumers are native C++ calls to `CvArea::AI_getTargetCity`. Therefore no
shipped allocation/leak activation exists today. Preserve this as a concrete Python API defect/debt
for Queue155 `CyAreaInterface.cpp`; do not spend F698 under the album's supported-current threshold
unless a shipped caller is later introduced/discovered.

AREA OBJECT LIFETIME / RECALCULATION
------------------------------------
CyArea intentionally wraps a raw CvArea pointer, so retaining a Python Area object across a full
`CyMap().recalculateAreas()` would be unsafe once the native area objects are rebuilt. Shipped map
scripts with explicit recalculateAreas calls were inspected around their Area retrievals. Their live
patterns recalculate first and then obtain/use Area wrappers, or obtain short-lived water/land Area
wrappers after the last relevant rebuild; no retained wrapper was demonstrated crossing a rebuild.
Ordinary gameplay uses immediate expressions such as city/plot `.area().get...` and random-event
predicates rather than long-lived stored area proxies.

This is therefore an inherent wrapper-lifetime API rule rather than a current UAF finding. Native
area-rebuild/topology defects are already owned by their Queue036/060 findings and are not duplicated
here.

ADVC-SPECIFIC HABITABLE-TILE BRIDGE
-----------------------------------
Practical 1290 introduced `CyArea::getNumHabitableTiles` together with its Python registration. It
null-checks the area, calls `countOwnedUnownedHabitableTiles()`, and returns the sum of owned and
unowned habitable tiles. Current shipped use in PerfectMongoose obtains the Area list after an
explicit area recalculation and uses the value for minimum-island-size filtering. The native helper
was already audited in Queue060; no wrapper arithmetic or ownership mismatch exists.

OTHER CURRENT SHIPPED USE
-------------------------
Current Python uses CyArea mainly for:
- map-generator bonus/starting-area tests (`getNumBonuses`, `getNumTiles`, water/lake state);
- random-event checks (`isWater`, city counts, border obstacle);
- BUG/advisor unit-value helpers with a city's current area;
- PerfectMongoose starting-area filtering via getNumHabitableTiles.
These uses are immediate and satisfy the wrapper/native preconditions. No Python iteration helper or
heap-owned collection is implemented in CyArea itself.

HISTORY / DEDUP / CLOSURE
-------------------------
K-Mod supplies the original thin wrapper surface; AdvC mainly tracks native naming/type refactors,
removes obsolete animal/improvement exposure and adds the practical-1290 habitable bridge. Base
AdvC 1.14 retains the same current implementation and registration policy.

Queue060 already owns native CvArea target-city, topology, water-city, resource and derived-state
findings/controls; Queue102 audited fresh Area wrappers from CyPlot registration; Queue113 covered
CyTeam's external CyArea pointer arguments. No independent CyArea-body root remains after those
dedups.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 117 `CvGameCoreDLL/FAssert.cpp` (204 queued lines), OPEN under
F698 / provisional KI#1019 unused.

Audit assertion/logging/debug infrastructure end-to-end: macro/callback contracts, formatting and
fixed buffers, stack-trace/file/line lifetime, release-vs-debug behavior, reentrancy/thread state,
Base/K-Mod/Civ4CE ancestry and whole-album dedup. Promote only a supported consequential root rather
than debug-only portability or deliberately fatal assertion behavior.

Exact continuation stone:
C031-WIP622 physical -> Queue116 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 124 / 169 complete ->
Queue117 FAssert.cpp OPEN -> assertion/format/buffer/reentrancy/history/dedup sweep -> either promote
consequential F698 or close cleanly -> freeze next WIP.

Files complete: 124 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP623 -- QUEUE 117 FAssert.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP622.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14
and Civ4CE/BtS FAssert sources, current FAssert.h, CvGlobals/CvGame lifecycle, all current assertion
call sites, local AdvC/K-Mod history and whole-album controls. Gameplay/XML/Python source untouched.

QUEUE 117 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current FAssert.h is executable-semantically identical to Base AdvC 1.14; FAssert.cpp differs only
in the later SAS autoplay-end-cause plumbing when the explicit Stop Auto Play button is pressed.

ASSERT DIALOG FORMAT / STORAGE
------------------------------
The fixed 65,536-byte assertion-text buffer is written with inherited `sprintf`, so this is not a
modern bounded-format implementation. Current inputs, however, do not establish a supported
65-KiB overflow:
- file/function/expression strings are compile-time source metadata;
- the title buffer is MAX_PATH+20 and only appends the executable basename to a short fixed prefix;
- 372 current FAssertMsg sites were classified; the great majority use short literals, while the 33
  dynamic-message sites use bounded diagnostic buffers, XML/type names or CvString-formatted
  diagnostic text far below the fixed 65-KiB buffer under current supported data.
Malformed/extreme external strings remain debug-hardening debt, not F698.

DEBUG SECOND-EVALUATION MACRO -- CURRENT CALLERS SAFE
-----------------------------------------------------
In debug builds, `BREAK_FOR_DEBUGGER(expr)` intentionally executes an `int 3` and then evaluates
`expr` once more so it can be inspected after breaking. That would be dangerous for an assertion
expression with side effects.

A balanced/comment-stripped mechanical scan of roughly 2,485 current FAssert/FAssertMsg invocations
found only two superficially mutating expressions:
1. CombatOdds' Monte-Carlo verification assigns debugger scratch values and can call setupCombatants,
   but the entire block is compiled out by current `MONTE_CARLO_ODDS_TEST=0`;
2. CvGame starting-position code calls `itPlayer.nextIndex()`, but AgentIterator declares
   `nextIndex() const` and merely returns the current iterator position; it does not advance state.
No active current assertion expression was proved to mutate gameplay when Debug is chosen.

FASSERTBOUNDS MULTIPLE EVALUATION / MACRO SHAPE
-----------------------------------------------
`FAssertBounds(lower,upper,index)` evaluates its arguments repeatedly and is a bare if/else-if macro
rather than a `do { ... } while (0)` wrapper. All 323 current C++ invocations were mechanically
screened for side-effecting index expressions and unsafe immediate unbraced-if embedding; none was
found. Current usage therefore satisfies this inherited macro contract.

STOP-AUTOPLAY GAME POINTER LIFECYCLE
------------------------------------
Practical 3779 added a Stop Auto Play button by evaluating
`GC.getGame().getAIAutoPlay()` whenever an assertion dialog is built. CvGlobals::m_game starts NULL,
so this is theoretically unsafe for an assertion before game allocation or after teardown.

Current lifecycle narrows that to robustness only:
- CvGlobals::init allocates m_game before normal XML/data loading and ordinary runtime assertions;
- before that allocation, the only two assertion sites in init are a fixed city-plot-priority
  invariant that is true for the compiled table and the fatal bootstrap requirement that gDLL was
  supplied by the executable;
- no supported normal-data early assertion was established before m_game exists.
Likewise no current normal teardown assertion was shown to fire after m_game deletion. A safer
assert subsystem could query `getGamePointer()` before showing the optional button, but present
evidence does not make this a supported-current root.

DIALOG RESULT / BUTTON CONTROL
------------------------------
Civ4CE/BtS used explicit result values 1..4. Practical 3779 changed the enum to implicit values
0..4 when adding ASSERT_DLG_STOP. Every current custom button passes those same current constants to
EndDialog and FAssertDlg switches on the same constants, so Debug/Ignore/IgnoreAlways/Stop/Abort
remain internally paired. Dialog creation failure falls through to the existing debugger behavior;
no supported wrong action was established from the numeric change.

WM_INITDIALOG explicitly sets focus to Debug and returns FALSE through the dialog procedure, which
is the Win32 convention when the application has set focus itself. The unused CONTEXT and clipboard
resource ID are dead/incomplete diagnostic scaffolding, not gameplay state.

AUTOPLAY STOP ACTION
--------------------
The sole executable SAS delta from Base 1.14 is that ASSERT_DLG_STOP now calls the extended
`setAIAutoPlay(0, true, SAS_AUTOPLAY_END_ASSERTION)` overload so current SASGameRecord/autoplay end
cause is preserved. This matches the newer autoplay API and does not change ordinary assert choices.

HISTORY / DEDUP / CLOSURE
-------------------------
Civ4CE supplies the Firaxis assert-dialog baseline. K-Mod moved/strengthened the bounds macro;
AdvC added function-name display, the debugger re-evaluation convenience, safer snprintf use in
FAssertBounds, Wine notes and practical-3779/3807 Stop Auto Play UI. Base AdvC 1.14 already contains
those semantics. Later SAS source here is formatting plus the intentional end-cause parameter.

No current assertion-infrastructure root duplicates the many gameplay findings whose debug asserts
help expose them; an assertion firing because another subsystem is invalid remains evidence for that
subsystem unless the assertion handler independently misbehaves on a supported path.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 118 `CvGameCoreDLL/FDialogTemplate.cpp` (204 queued lines), OPEN
under F698 / provisional KI#1019 unused.

Audit the runtime Win32 dialog-template builder used by FAssert: allocation/growth, DWORD/WORD
alignment, string/resource encoding, item-count/header-size arithmetic, ownership/destruction,
button/edit-box construction, Base/Civ4CE/K-Mod ancestry and its concrete assert-dialog dimensions.
Promote only an independent supported consequence.

Exact continuation stone:
C031-WIP623 physical -> Queue117 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 125 / 169 complete ->
Queue118 FDialogTemplate.cpp OPEN -> allocation/alignment/encoding/size/ownership/history/dedup sweep
-> either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 125 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP624 -- QUEUE 118 FDialogTemplate.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP623.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
Civ4CE/BtS and K-Mod-origin FDialogTemplate sources, current FAssert concrete caller, local path
history and whole-album controls. Gameplay/XML/Python source untouched.

QUEUE 118 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current SAS FDialogTemplate is executable-semantically identical to supplied Base AdvC 1.14; the
only SAS-line change is practical 5961 formatting. The implementation was introduced wholesale by
K-Mod practical 3 and inherited by AdvC. AdvC practical 2535 only added a local int-to-short warning
suppression; practical 1591 only reformatted the disabled copy operations.

CONCRETE ASSERT-DIALOG TEMPLATE LAYOUT
--------------------------------------
The only current in-tree constructor user is FAssert::DisplayAssertDialog. It creates the fixed
"Assert Failed!" dialog and adds four ordinary buttons plus an edit box, with an optional fifth
Stop Auto Play button. No current caller uses AddComponent, AddStatic, AddListBox, AddScrollBar or
AddComboBox.

The superficially suspicious AlignData implementation computes

    paddingSize = usedBufferLength % size;

instead of the generic `(size - remainder) % size`. This would be wrong for arbitrary alignment
sizes/remainders. It does NOT misalign the current implementation:
- AlignData is private and is called only as AlignData(sizeof(DWORD)), i.e. size 4;
- every template field/string contribution is an even number of bytes, so usedBufferLength can have
  only remainder 0 or 2 modulo 4;
- for the only nonzero case, remainder 2 equals the correct padding 4-2.
A mechanical reconstruction of the exact FAssert stream proves every DLGITEMTEMPLATE begins on a
DWORD boundary. Without Auto Play the component starts are bytes 80, 132, 184, 224 and 264; with
Stop Auto Play they are 80, 132, 184, 240, 280 and 320. All are divisible by four.

STANDARD VS GENERIC COMPONENT PATH
----------------------------------
AddStandardComponent performs the required DWORD alignment, writes a standard-control ordinal
(0xFFFF + 0x0080..0x0085), writes the UTF-16 caption and leaves creation-data length to the public
AddButton/AddEditBox/etc wrapper. The current FAssert caller therefore gets exactly one zero WORD
of creation data per control.

Generic AddComponent does not call AlignData before writing DLGITEMTEMPLATE. That is a real API
robustness defect if an external caller appends arbitrary custom-class controls after an odd layout,
but there is no current in-tree AddComponent caller. Do not spend F698 on an unused helper.

HEADER / FONT / STRING CONTRACTS
--------------------------------
The current assert constructor passes DS_SETFONT together with non-null "MS Shell Dlg", so the
fontSize/font tail is present as required. The generic API could be handed DS_SETFONT with font=NULL
and create a malformed template, but no current caller does so.

AppendString uses MultiByteToWideChar(CP_ACP) twice and assumes a valid non-null source string.
Every current FAssert caption/class/font string is a short ASCII literal, so conversion succeeds and
the allocation length is bounded. Invalid external byte strings or NULL captions are API hardening,
not current-data defects.

BUFFER GROWTH / OWNERSHIP / COUNTS
----------------------------------
EnsureSpace grows by twice the requested append length whenever the requested range would exceed the
current allocation. Because the shortfall is necessarily <= requested length, the new allocation is
always large enough for that append. It copies only used bytes, frees the old buffer, and updates the
single owning pointer; CDialogTemplate's copy constructor and assignment are deliberately private and
undefined, preventing accidental double ownership. Destructor frees exactly that final buffer.

malloc failure is unchecked, but ordinary assert-dialog data is only a few hundred bytes and the
project does not attempt OOM recovery elsewhere. Treat as fatal-runtime robustness, not F698.
`cdit` is a WORD and geometry fields narrow to signed shorts; the concrete FAssert dialog has only
5 or 6 controls and dimensions <=379x166, so no supported truncation/overflow is reachable.

PADDING CONTENT
---------------
AlignData advances over padding without zeroing it. Windows dialog-template parsing requires the
next DLGITEMTEMPLATE address to be DWORD-aligned; it does not consume semantic payload from those
padding bytes. The concrete template's alignment requirement is therefore met despite indeterminate
padding contents.

HISTORY / DEDUP / CLOSURE
-------------------------
K-Mod practical 3 introduced FDialogTemplate together with the custom assert infrastructure; its
allocation, alignment and control-building logic is already the same as current. Civ4CE/BtS carries
the same implementation. AdvC/Base 1.14 retains it unchanged apart from warning/formatting edits.
Queue117 already audited the assertion dialog's caller-side buffer, lifecycle and button semantics;
Queue118 adds the concrete runtime-template proof and finds no independent supported defect.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 119 `CvGameCoreDLL/CyStructsInterface1.cpp` (191 queued lines), OPEN
under F698 / provisional KI#1019 unused.

Audit the Boost.Python registrations for CyPlotPoint/CyOrderData/CyBattleDefinition and other exposed
struct wrappers: exported-name -> target/signature alignment, constructor/copy policy, property
lifetime/ownership, enum/int field contracts, Base/AdvC parity, shipped Python callers and
whole-album dedup. Promote only an independently activated supported root.

Exact continuation stone:
C031-WIP624 physical -> Queue118 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 126 / 169 complete ->
Queue119 CyStructsInterface1.cpp OPEN -> registration/name/signature/ownership/caller/history/dedup
sweep -> either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 126 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP625 -- QUEUE 119 CyStructsInterface1.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP624.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
Civ4CE/BtS, current CvStructs/CvUnit definitions, shipped Python corpus, prior Queue065 CvStructs and
Queue047 Python-event lifetime proofs, local path history and whole-album controls. Gameplay/XML/
Python source untouched.

QUEUE 119 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current CyStructsInterface1.cpp is text-identical to supplied Base AdvC 1.14 after newline
normalization. The active semantic deltas from BtS/Civ4CE are known AdvC refactors/additions and
reconcile with the current native structs.

DIRECT REGISTRATION MAP
-----------------------
The file exposes 15 value/data classes: NiPoint3, NiPoint2, NiColorA, POINT, XYCoords, IDInfo,
GameTurnInfo, OrderData, MissionData, TradeData, EventTriggeredData, EventMessage, FOWVis,
PBGameSetupData, PBPlayerSetupData, PBPlayerAdminData and CombatDetails (17 class registrations total
including the three Ni/POINT families counted separately). Every def_readwrite target was checked
against its current native field; no exported-name -> wrong-member mismatch or duplicate property
binding survives.

The apparently mismatched MissionData Python property `iFlags` intentionally targets the renamed
native `MissionData::eFlags`. AdvC practical 2551 changed the C++ field from an untyped int to
MovementFlags while preserving the old Python-facing property name for compatibility. `bModified`
is intentionally not exposed; native pushMission fills it and the repaired getMissionFromQueue path
returns an already-populated copy, as established in Queues065/091.

PYTHON-CONSTRUCTED VALUE TYPES
------------------------------
Current shipped Python directly constructs NiPoint3, NiColorA and TradeData. The numeric Ni value
classes have explicit matching Boost.Python constructors. TradeData has AdvC's explicit default
constructor with NO_TRADE_ITEM/-1/false/false defaults and initializes ABI padding; every shipped
trade helper then fills the needed fields before sending/querying. No uninitialized TradeData object
is created by the registration layer.

Other POD/engine-facing classes are primarily returned from the EXE/DLL. Arbitrary extension-code
default construction of GameTurnInfo/EventTriggeredData/PitBoss/FOW records can expose default-state
sharpness, but Queue065 already traced normal producers and found no shipped consumer of an
uninitialized record. Do not duplicate that robustness domain as F698.

RETURN / LIFETIME CONTRACTS
---------------------------
EventTriggeredData instances returned from CyPlayer are borrowed live-player records; their
reference_existing_object policy is registered in CyPlayerInterface2, not here, and was already
audited with their lifecycle. OrderData and MissionData queue accessors now return independently
allocated copies with manage_new_object, established in Queues024/091.

CombatDetails contains std::wstring but is registered as an ordinary copyable value class. The
Python event converter therefore materializes a Python-owned CombatDetails copy rather than retaining
the native stack address. Queue047 already proved the shipped combat log/Battle History consumers
are safe across the callback lifetime. All current CombatDetails native members are exposed except
the removed/replaced city-Barbarian field; practical 3938 correctly added the three advc.313 fields.

INDEXED ENGINE/API HELPERS
--------------------------
FOWVis::getOffsets and PBGameSetupData's getCustomMapOption/getVictory/getOptionAt/getMPOptionAt are
raw indexed accessors without local bounds checks. Current shipped Python contains no FOWVis or
PitBoss setup/admin access through these methods; arbitrary invalid extension indices remain API
hardening. Queue065 separately recorded the primitive/default pointer sharpness of these engine-facing
records and found no supported current activation.

HISTORY / DEDUP / CLOSURE
-------------------------
AdvC path history here is narrow: include cleanup, console-output redirection, MovementFlags typing,
CombatDetails string exposure toggling/reinstatement and advc.313 combat-detail expansion. Current
Base AdvC 1.14 retains the exact same registration. Queue065 owns the actual CvStructs storage/body
contracts and F681; Queue047 owns CombatDetails callback-copy lifetime; Queue091 owns MissionData copy
ownership. No independent registration-layer root remains.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Queue120 AgentIterator.cpp was already completed at C031-WIP501, so ordinary order advances to Queue
121 `CvGameCoreDLL/CvXMLLoadUtilityInit.cpp` (177 queued lines), OPEN under F698 / provisional
KI#1019 unused.

Audit XML-loader initialization/uninitialization and progress state: XML interface pointer lifecycle,
GlobalDefines/cache initialization order, load-stage counters/path construction/error behavior,
Base/Civ4CE/K-Mod ancestry and cross-contract dedup against Queues028/052/086. Promote only a current
supported root not already owned by the broader XML loader findings.

Exact continuation stone:
C031-WIP625 physical -> Queue119 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 127 / 169 complete ->
Queue120 ALREADY COMPLETE -> Queue121 CvXMLLoadUtilityInit.cpp OPEN -> init/uninit/XML-interface/
load-stage/path/error/history/dedup sweep -> either promote consequential F698 or close cleanly ->
freeze next WIP.

Files complete: 127 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP626 -- QUEUE 121 CvXMLLoadUtilityInit.cpp CLOSED CLEAN / F698 UNUSED
===============================================================================
Physical parent: C031-WIP625.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current callers/CvInfo allocators, XML utility sibling queues and local path history. Gameplay/XML/
Python source untouched.

QUEUE 121 RESULT
----------------
No independent current defect root was established. F698 / provisional KI#1019 remains unused.
Current SAS CvXMLLoadUtilityInit.cpp is text-identical to supplied Base AdvC 1.14 after newline
normalization. Contrary to the broad filename, the current file only contains low-level list/default
allocators and CleanUpGlobalVariables; FXml creation/load-stage lifecycle lives in the already-audited
CvXMLLoadUtility.cpp/Set/Get files.

LIVE ALLOCATOR CALLERS
----------------------
InitStringList is called only by SetGlobalStringArray with the already-discovered info-base count.
Init2DIntList is used by Civics improvement-yield tables and Terrain/Improvement yield-array loading;
its dimensions are current positive info counts plus fixed NUM_YIELD_TYPES. InitImprovementBonusList
is called with GC.getNumBonusInfos(), positive in current data. InitBuildingDefaults and
InitUnitDefaults are called only during Civilization-info loading after BuildingClass/UnitClass infos
exist and allocate exactly those enum counts.

Init2DFloatList, InitPointerFloatList, InitPointerIntList and Init2DDirectionTypesList have no current
in-tree callers. Their assert-only positive-size preconditions therefore do not create a supported
release-build negative-size allocation path.

INITIALIZATION / OWNERSHIP
--------------------------
Every live allocator checks the destination pointer is NULL in assert builds, allocates once and
fully initializes each semantic element before returning. The improvement-bonus path allocates each
NUM_YIELD_TYPES subarray and zeroes it after the CvImprovementBonusInfo constructor has supplied a
NULL pointer; the corresponding info destruction was already audited in Queue033. Default
building/unit arrays copy the already-loaded class default IDs over their complete enum domains.

Calling a helper on a non-null destination would leak the old allocation in a release build, but all
current loaders construct/reset their owning info arrays before these one-shot initialization calls.
No repeated live call on an owned pointer was found.

CLEANUP
-------
CleanUpGlobalVariables delegates to CvGlobals::deleteInfoArrays. CvGlobals' init/uninit ownership and
XML reload boundaries were already audited; Queue121 adds no second delete/order mismatch.

HISTORY / DEDUP
---------------
The retained AdvC history is mostly include cleanup, enum-loop modernization and renamed default
building/unit getters. The current implementation remains Base 1.14-identical. Queue028/052/086 own
the substantive XML parsing/count/default/cursor findings and controls; Queue033/034 own Terrain/
Civilization info array consumers. No independent allocator root remains.

F698 / provisional KI#1019 remains UNUSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 122 `CvGameCoreDLL/RFTotalScore.cpp` (175 queued lines), OPEN under
F698 / provisional KI#1019 unused.

Audit Rise/Fall total-score accumulation, chapter carry-forward, display caching/string ownership,
retirement/defeat/victory boundaries, serialization and no-turn-limit interactions against the
fresh Queue098/101 findings. Dedup aggressively before assigning another Rise/Fall root.

Exact continuation stone:
C031-WIP626 physical -> Queue121 CLOSED CLEAN -> F698/KI#1019 UNUSED -> 128 / 169 complete ->
Queue122 RFTotalScore.cpp OPEN -> score arithmetic/lifecycle/string/serialization/history/dedup sweep
-> either promote consequential F698 or close cleanly -> freeze next WIP.

Files complete: 128 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP627 -- QUEUE 122 RFTotalScore.cpp / F698 KI#1019 CONFIRMED / F699 UNUSED
===============================================================================
Physical parent: C031-WIP626.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current RFChapter/RFChapterScore/RiseFall/CvPlayer/CvReplayInfo/Victory/Dan-Quayle consumers, current
RF XML/defines, local AdvC history and whole-album controls. Gameplay/XML/Python source untouched.

F698 / provisional KI#1019 -- Rise/Fall normalized final score takes a fractional power of a
negative value when total Rise score falls below -250
-------------------------------------------------------------------------------
Status: CONFIRMED CURRENT INHERITED RISE/FALL NUMERIC-DOMAIN DEFECT ROOT; supported current scoring
can cross the domain boundary without malformed data or integer overflow.

ROOT
----
`RFTotalScore::getNormalizedScore()` currently returns:

    ::round(std::pow((getScore() + 250) / 12.5, 2.5));

The exponent 2.5 is fractional. The real-valued `pow` domain therefore requires the base to be
nonnegative, i.e. total Rise score >= -250. No clamp/assert/invariant in RFTotalScore or the chapter
scoring layer enforces that lower bound.

CURRENT SUPPORTED ARITHMETIC WITNESS
------------------------------------
The threshold is reachable with ordinary integer Civ scores and the default seven-chapter setup;
it does not require a wildly overflowing score.

For a non-first, non-repeat chapter, take a selected civilization that:
- starts the chapter ranked first;
- has initial Civ score 289 while the then-lowest reference civilization has score 100;
- by scoring time has fallen to the bottom rank but remains otherwise normally scoreable;
- has not retired early, so there is no positive remaining-time bonus at delayed chapter scoring.

At the later bottom-rank score:
- `fromRank = 0`;
- starting from rank 1 gives `fromInitialRank = -100`;
- because the selected civilization is now lowest, there is no lower positive `nextBestScore`, so
  the fallback uses its own current score and `fromCivScore = +100`;
- `referenceRank` is the bottom rank, whose saved initial reference score is 100, so
  `initialCivScorePercent = round(100 * 289 / 100) = 289` and
  `fromInitialCivScore = -289`.

Thus the raw chapter total is exactly:

    0 - 100 + 100 - 289 = -289.

RFChapterScore deliberately compresses a negative total below -9 to three times its square root:

    -round(3 * sqrt(289)) = -51 Rise points.

The first chapter has its special forced 100% initial-Civ baseline; the same first-to-last collapse
therefore yields raw -100 and a scaled -30. Current `RF_CHAPTERS_BASE` is 7. Consequently only six
poor scored chapters of this supported shape already give:

    -30 + 5 * -51 = -285.

A loss does not rescue the value: when the final total is nonpositive, RFTotalScore applies no
handicap percentage adjustment, and once the game is known lost its victory/unstarted compensation
is cleared. Further negative chapters can of course lower the sum more.

DOMAIN FAILURE
--------------
For total = -285, the normalizer evaluates:

    ((-285 + 250) / 12.5) ^ 2.5
    = (-2.8) ^ 2.5.

A negative real base with exponent 2.5 is outside `std::pow`'s real domain and yields a non-finite
domain-error result on the target floating-point/CRT model. `fmath::round(double)` then attempts an
integer conversion of that non-finite value:

    inline int round(double d) { return (int)((d >= 0 ? 0.5 : -0.5) + d); }

There is therefore no meaningful normalized score. Do not overclaim the exact integer bit pattern:
conversion/CRT behavior for the non-finite value is implementation-specific. The source-level
contract failure is already complete before that conversion.

LIVE CONSEQUENCES / CALL CHAIN
------------------------------
This is not an unused helper:
- `CvPlayer::calculateScore(true,...)` redirects Rise/Fall final scoring to
  `RiseFall::getNormalizedFinalScore()` -> `RFTotalScore::getNormalizedScore()`;
- `CvReplayInfo::createInfo` calls that final-score path and stores the result as the replay's
  normalized score, which is consumed by the Hall of Fame and `CvDanQuayle.py`;
- after choosing to continue beyond victory, `RFTotalScore::updateString()` also calls the same
  normalizer in `GAMESTATE_EXTENDED` and appends the result to the Score-tab breakdown.

`CvReplayInfo::read` later bounds-checks serialized normalized scores to [-10000,1000000]. Depending
on the target conversion result, the bad value can additionally fall outside replay validation;
that downstream exact representation is not needed for F698 and is not asserted as universal.

ANCESTRY / BASELINE
-------------------
Current SAS `RFTotalScore.cpp` is newline-normalized byte-identical to supplied Base AdvC 1.14.
Practical 1268 / e11525d303 introduced Rise/Fall and the same unguarded fractional-power normalizer,
originally using `(score + 200) / 14.0`. Practical 1271 / cf4fe6e63d adjusted the scoring scale and
moved the domain boundary to the current -250 via `(score + 250) / 12.5`; it still supplied no lower
clamp. Later retained history does not repair that domain assumption.

DEDUP
-----
This is independent of F689-F692/F695-F696. Those roots can alter particular chapter/timing/display
states; F698 exists even when every chapter is scored by the intended formulas and all those defects
are absent. It is also independent of the earlier ACO non-finite finding: that root is float
underflow followed by 0/0 in combat-help arithmetic, whereas F698 is a Rise/Fall final-score
fractional-power domain error.

REPAIR BOUNDARY
---------------
Preserve the authored nonlinear normalization for ordinary scores, but define behavior at and below
the lower endpoint before calling `pow`. At minimum, clamp the pre-power base (or the Rise total) to
a nonnegative domain, e.g. treating scores <= -250 as normalized score 0. If a negative normalized
score is desired instead, use an explicitly signed transformation rather than relying on fractional
`pow` of a negative base. Add a regression case for total < -250 and exactly -250.

F698 is now CONFIRMED. F699 / provisional KI#1020 is UNUSED.

QUEUE 122 REMAINS OPEN
----------------------
Continue the rest of RFTotalScore independently: victory/interlude/frozen-state transitions,
finished-scored versus finished-unscored carry-forward, raw-string ownership/reset/read lifecycle,
serialization pairing, negative/positive handicap arithmetic, no-turn-limit interactions and every
retained history stratum. Dedup against F689-F692/F695-F696 before assigning F699.

Exact continuation stone:
C031-WIP627 physical -> Queue122 OPEN -> F698/KI#1019 CONFIRMED -> F699/KI#1020 UNUSED -> finish
RFTotalScore lifecycle/arithmetic/string/serialization/history/dedup sweep -> either promote an
independent F699 or close Queue122 cleanly -> freeze next WIP.

Files complete: 128 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP628 -- QUEUE 122 RFTotalScore.cpp / F699 KI#1020 CONFIRMED / F700 UNUSED
===============================================================================
Physical parent: C031-WIP627.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current Handicap XML, Rise/Fall localization/manual, RiseFall::fillWS, current Victory Score-tab
consumer, practical-1268 ancestry and whole-album controls. Gameplay/XML/Python source untouched.

F699 / provisional KI#1020 -- Rise/Fall handicap breakdown hardcodes plus signs for negative
low-handicap modifiers
-------------------------------------------------------------------------------
Status: CONFIRMED CURRENT INHERITED RISE/FALL DISPLAY DEFECT ROOT; ordinary supported Rookie/
Settler games render a mathematically negative handicap adjustment with contradictory positive-sign
punctuation.

ROOT
----
`RFTotalScore::update()` intentionally permits a negative difficulty/handicap modifier:

    difficultyPercent = round(100 * ((handicap.getDifficulty() + 10) / 40.0) - 100);
    fromDifficulty = (total <= 0 ? 0 : round((difficultyPercent / 100.0) * total));

The breakdown then passes those signed integers directly to `TXT_KEY_RF_RISE_BREAKDOWN`:

    ..., fromDifficulty, difficultyPercent, ...

but the current English text hardcodes positive punctuation around both fields:

    %s19+%d10_Pts    (+%d11%%) from %s12_Hnd handicap

The `%d` conversion already renders the integer's own minus sign. The literal `+` therefore remains
in front of a negative value instead of adapting to its sign.

CURRENT DATA / DETERMINISTIC WITNESS
------------------------------------
Current `CIV4HandicapInfo.xml` contains:

    HANDICAP_ROOKIE   iDifficulty = 18
    HANDICAP_SETTLER  iDifficulty = 26
    HANDICAP_CHIEFTAIN iDifficulty = 34

Under the native formula these become respectively:

    Rookie:   100 * ((18 + 10) / 40) - 100 = -30%
    Settler:  100 * ((26 + 10) / 40) - 100 = -10%
    Chieftain:                                      +10%

For an ordinary positive pre-handicap Rise subtotal of 100 points, Rookie therefore computes:

    fromDifficulty = -30
    final total     = 70

while the localization receives `-30` and `-30` behind literal plus signs and renders the semantic
shape:

    +-30 Pts    (+-30%) from Rookie handicap

(Settler analogously renders `+-10` / `+-10%`.) The exact spacing/font tags are presentation details;
the contradictory signs are determined by the source text plus signed integer substitutions.

Even when the pre-handicap total is nonpositive, `fromDifficulty` is clamped to 0 but
`difficultyPercent` remains negative, so the percentage half of the same sign defect can still be
shown. The positive-subtotal witness above establishes both malformed fields at once.

WHY `fillWS` DOES NOT MASK IT
-----------------------------
`RiseFall::fillWS(int n, bool addPlus)` is only an alignment-prefix helper. The handicap call uses
its default `addPlus=false`; it takes `abs(n)` merely to choose indentation width and returns spaces,
not a sign. Thus `fillWS(fromDifficulty)` cannot consume the negative sign or neutralize the hardcoded
`+` in XML.

ANCESTRY / SAS WORDING CONTROL
------------------------------
Practical 1268 / e11525d303 introduced `RFTotalScore`, the negative-capable difficulty formula and
`TXT_KEY_RF_RISE_BREAKDOWN` together. Its original English text already used:

    +%d10 ... (+%d11%%) ... difficulty

so the sign mismatch exists at the Rise/Fall feature's birth. Supplied Base AdvC 1.14 retains the
same behavior. SAS's later localization override only renames the noun from "difficulty" to
"handicap" and preserves the same placeholders and hardcoded signs; SAS therefore inherits rather
than creates the defect.

The current manual explicitly says the final Rise score is multiplied by a factor based on the
difficulty setting. The negative values on easier handicaps are thus intentional score penalties,
not invalid configuration values that the UI may safely assume are positive.

DEDUP
-----
Whole-album search found no prior owner for the low-handicap Rise-breakdown sign mismatch. F689 is
a stale repeat-penalty line, F692 is false retirement scoring/text on defeat, F695-F696 are
no-turn-limit Score-tab state/sentinel defects, and F698 is the normalized-score numeric-domain
failure. F699 is independent: fixing any of those leaves the signed handicap delta behind a literal
plus sign.

REPAIR BOUNDARY
---------------
Make the sign dynamic rather than hardcoded. The existing `fillWS(..., true)` pattern used for signed
chapter sums is an internal formatting oracle: either let a sign-aware prefix helper supply `+` only
for nonnegative values and remove the XML literal plus, or split positive/negative localization
variants. The percentage sign must be made dynamic too. Preserve the existing handicap arithmetic;
this is a display repair, not a balance change.

Add regression checks for a positive Rise subtotal on Rookie/Settler and for a positive handicap,
ensuring the output contains one mathematically correct sign rather than `+-`.

F698-F699 are now CONFIRMED. F700 / provisional KI#1021 is UNUSED.

QUEUE 122 REMAINS OPEN
----------------------
Finish RFTotalScore independently: game-end/interlude/current-civ victory classification,
finished-scored versus finished-unscored carry-forward, frozen/extended-game transition, raw-string
reset/read/destruction ownership, current-format serialization pairing, handicap and integer bounds,
no-turn-limit interactions and all retained practical-1268 -> current history. Dedup the early
Rise/Fall Fast Save state against F608 rather than renumbering it.

Exact continuation stone:
C031-WIP628 physical -> Queue122 OPEN -> F698-F699/KI#1019-KI#1020 CONFIRMED -> F700/KI#1021
UNUSED -> finish RFTotalScore lifecycle/arithmetic/string/serialization/history/dedup sweep -> either
promote an independent F700 or close Queue122 cleanly -> freeze next WIP.

Files complete: 128 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP629 -- QUEUE 122 RFTotalScore.cpp / F700 KI#1021 CONFIRMED / F701 UNUSED
===============================================================================
Physical parent: C031-WIP628.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current Handicap XML/Main Changes Guide, practical-4853/practical-5970 reachable history,
RFTotalScore current/Base source and whole-album/Known-Issues dedup. Gameplay/XML/Python source
untouched.

F700 / provisional KI#1021 -- SAS Rise/Fall handicap multiplier still assumes the old AdvC
`iDifficulty` scale, so current Noble receives +50% instead of the documented neutral baseline
-------------------------------------------------------------------------------
Status: CONFIRMED CURRENT SAS CROSS-FEATURE SCORING DEFECT ROOT; a later handicap-rating redesign
changed the meaning and zero point of `iDifficulty` without updating Rise/Fall's inherited affine
conversion.

ROOT / CURRENT FORMULA
----------------------
`RFTotalScore::update()` still uses the original AdvC Rise/Fall conversion:

    difficultyPercent = round(100 *
            ((handicap.getDifficulty() + 10) / 40.0) - 100);

Algebraically this is:

    difficultyPercent = 2.5 * iDifficulty - 75

so its neutral point is permanently hard-coded at:

    iDifficulty = 30  ->  0%

That matched the original/base handicap rating scale's Noble row. It no longer matches SAS's
current definition of the field.

BASE ADVC CONTRACT / INTERNAL ORACLE
------------------------------------
Supplied Base AdvC 1.14 `CIV4HandicapInfo.xml` uses:

    Settler   0
    Chieftain 10
    Warlord   20
    Noble     30
    Prince    40
    Monarch   50
    Emperor   60
    Immortal  70
    Deity     85

Under the inherited formula, Noble is therefore exactly neutral and the ordinary rows step:

    Settler -75%, Chieftain -50%, Warlord -25%, Noble 0%,
    Prince +25%, Monarch +50%, Emperor +75%, Immortal +100%

(with Deity about +138%). This alignment is not accidental: practical 1268 introduced Rise/Fall
while Noble's `iDifficulty` was 30, and later retained RFTotalScore history never changed the
formula's zero point.

SAS BIRTH / CROSS-COMMIT REGRESSION
-----------------------------------
Practical 4853 / b9a7c4149ba5207a9a1430f6d72d8b520fc0bec8 (`completed most of the gradual handicap
settings`) changed the Handicap XML rating values without touching `RFTotalScore.cpp`:

    Settler 0->10, Chieftain 10->20, Warlord 20->30, Noble 30->40,
    Prince 40->50, Monarch 50->60, Emperor 60->70, Immortal 70->80, Deity 85->90.

That is the first current-ancestry point where the inherited Rise/Fall conversion stopped being
centered on Noble: Noble immediately changed from 0% to +25% even though no Rise/Fall scoring policy
change accompanied the XML retuning.

Practical 5970 / 5628eecbef59005325454373c9490773c850a5e8 then added Rookie and Deity+ and deliberately
redefined the rating scale to the current:

    Rookie 18, Settler 26, Chieftain 34, Warlord 42, Noble 50,
    Prince 58, Monarch 66, Emperor 74, Immortal 82, Deity 90, Deity+ 98.

Again, `RFTotalScore.cpp` was not changed. The practical's own commit message says the new values
"better reflect that Noble is an even game," and the current XML comment says Noble stays 50 as the
"even/baseline difficulty."

CURRENT DOCUMENTED CONTRACT
---------------------------
The current Main Changes Guide is explicit that:

* Noble `iDifficulty=50` is the even/baseline rating;
* `iDifficulty` is a general difficulty rating used by scoring, comparisons and code that averages
  or estimates handicap strength;
* direct AI/player balance is supplied by the many other handicap fields rather than by this rating.

Thus this is not merely a disagreement about how hard Noble feels in practice. The field's current
published semantic baseline is 50, while this score consumer still treats 30 as neutral.

DETERMINISTIC CURRENT CONSEQUENCE
---------------------------------
With today's SAS values the unchanged formula yields:

    Rookie    18 -> -30%
    Settler   26 -> -10%
    Chieftain 34 -> +10%
    Warlord   42 -> +30%
    Noble     50 -> +50%
    Prince    58 -> +70%
    Monarch   66 -> +90%
    Emperor   74 -> +110%
    Immortal  82 -> +130%
    Deity     90 -> +150%
    Deity+    98 -> +170%

For a completely ordinary positive pre-handicap Rise total of 200 on Noble:

    fromDifficulty = round(0.50 * 200) = +100
    final Rise total = 300

Yet Noble is the declared even/baseline handicap, so this consumer adds a 50% score bonus at the
very row whose rating is explicitly meant to carry no difficulty displacement. Practical 4853's
intermediate Noble=40 already produced +25%; practical 5970 amplifies the mismatch to +50% while
making the intended 50-point neutral center explicit.

This arithmetic affects gameplay scoring, not only presentation: `fromDifficulty` is added to
`total`, and that total feeds Rise/Fall final score/normalization and the Score-tab breakdown.
F699's `+-` localization problem is therefore only a sibling display symptom on the low-handicap
side; fixing F699 leaves F700's multiplier arithmetic unchanged.

DEDUP
-----
Whole-album and current Known-Issues searches found no prior owner for this RFTotalScore consumer
of the redefined `iDifficulty` scale. The earlier Queue-008 handicap checks validated
`handicapFromDifficulty` against the new XML ratings and explicitly treated consumer-side roots
separately; they do not repair this stale affine mapping. F448/KI#771 concerns Rise/Fall changing
human/AI control/handicap without rebuilding the founding-unit surcharge cache. F698 is the
normalized-score fractional-power domain bug and F699 is the signed-breakdown punctuation bug.
F700 is independent of all of them.

REPAIR BOUNDARY
---------------
Update Rise/Fall's handicap conversion to the current `iDifficulty` semantic scale instead of
retaining the old magic zero point of 30. At minimum, the mapping must make current Noble=50 neutral
and remain monotone on both sides. The exact slope/endpoint multipliers are a balance decision and
should not be guessed by the bug fix solely from the old formula; make that policy explicit (or use
an explicit per-handicap/Rise-score modifier) and regression-test at least:

    Noble (50) -> 0%
    a lower handicap -> negative modifier
    a higher handicap -> positive modifier

If preserving the old approximate +/-25 percentage points per handicap step is desired, derive that
from the current +8 rating spacing explicitly rather than accidentally from the obsolete +10 scale.
Whatever slope is chosen, F699's text should independently display its resulting sign correctly.

F698-F700 are now CONFIRMED. F701 / provisional KI#1022 is UNUSED.

QUEUE 122 REMAINS OPEN
----------------------
Finish the remaining RFTotalScore game-end/interlude/frozen-state, finished-scored vs
finished-unscored, raw-string/reset/read/destruction, current-format serialization, integer/bounds,
no-turn-limit and retained-history closure. Record the Victory Fast Save stale/unfrozen Rise state
as consequence breadth of F608/KI#931 rather than creating another root. Dedup against
F689-F692/F695-F696 and then either promote an independent F701 or close Queue122 cleanly.

Exact continuation stone:
C031-WIP629 physical -> Queue122 OPEN -> F698-F700/KI#1019-KI#1021 CONFIRMED -> F701/KI#1022
UNUSED -> finish RFTotalScore lifecycle/string/serialization/bounds/history/dedup closure -> either
promote an independent F701 or close Queue122 cleanly -> freeze next WIP.

Files complete: 128 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP630 -- QUEUE 122 RFTotalScore.cpp CLOSED / QUEUE 126 CvDllTranslator.cpp OPEN / F701 UNUSED
===============================================================================
Physical parent: C031-WIP629.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
RiseFall/RFChapter/RFChapterScore game-end callers, current Victory Screen/manual/localization,
practical-1268->current RFTotalScore history, current serialization and whole-album dedup. Gameplay/
XML/Python source untouched.

QUEUE 122 FINAL RESULT
----------------------
`CvGameCoreDLL/RFTotalScore.cpp` is CLOSED after full current-body, caller/lifecycle, arithmetic,
string ownership, serialization, current-data, ancestry and whole-album reconciliation.

Confirmed independent roots:

* F698 / KI#1019 -- `getNormalizedScore()` can feed a negative base to fractional `pow` when a
  supported total Rise score falls below -250, producing a non-finite value before integer rounding.
* F699 / KI#1020 -- the Rise breakdown hardcodes `+` before signed low-handicap adjustment values,
  producing `+-...` text on current Rookie/Settler states.
* F700 / KI#1021 -- SAS redefined `iDifficulty` so Noble=50 is explicitly the even/baseline rating,
  but RFTotalScore still uses the old AdvC mapping whose zero point is 30; current Noble therefore
  receives +50% Rise points instead of a neutral handicap adjustment.

No independent F701 survives Queue122.

GAME-END / FREEZE CONTRACTS -- CLEAN
------------------------------------
The remaining update/freeze branches were traced against `RiseFall::prepareForExtendedGame()` and
the manual rather than inferred from the local arithmetic alone.

* While a chapter is ongoing and GAMESTATE_ON, the right-hand Score-tab value is intentionally a
  projection "by winning this turn." The current chapter supplies `finalCiv`, initial rank/rivals
  and remaining-time percentage, while already-ended chapters contribute their current/scored
  values.
* If the human-controlled current civilization wins, `prepareForExtendedGame` deliberately does not
  score that current chapter before `freezeTotal`; the victory premium / missed-chapter
  compensation model is therefore used, matching the manual.
* If another civilization wins during an ongoing chapter, the current chapter is scored before the
  total is frozen. Once GAMESTATE is no longer ON and the winning team is not the current human
  civilization, `victoryBase` and `unstarted` are zeroed, so the final total is the accumulated
  chapter result rather than a false human victory premium.
* If victory occurs while no chapter is ongoing (interlude), the latest started chapter is scored
  first and `finalCiv` remains NO_PLAYER, so the final frozen total likewise suppresses the human
  victory premium. This matches the absence of a current human chapter to which such a premium
  could belong.
* The retirement/defeat timing problems found in Queue098 belong to F692; the no-turn-limit chapter
  and score-turn sentinel UI problems belong to F695/F696. RFTotalScore does not introduce a new
  independent state transition on top of those roots.

`freezeTotal()` calls `update()` exactly once before setting `frozen=true`. Later `update()` calls on
a frozen score deliberately call only `updateString()` and preserve the frozen arithmetic. This is
needed when GAMESTATE subsequently becomes EXTENDED: the stored final total stays fixed while the
breakdown gains the normalized-score line. No stale arithmetic mutation was found in that path.

FINISHED-SCORED / FINISHED-UNSCORED CONTROL
-------------------------------------------
`fromFinishedUnscored` is a deliberate live-preview category, not a forgotten finalization state.
For an ended chapter whose delayed scoring time has not yet arrived, `computeScore()` supplies the
score that would be awarded now while `isScored()` distinguishes it from already-frozen chapter
scores. The current Score tab explicitly documents non-bold chapter values as such previews.
At game end, `prepareForExtendedGame()` scores the latest relevant chapter where required before
freezing the total, preventing an accidentally mutable final chapter estimate from surviving as the
ordinary final-score contract.

RAW STRING OWNERSHIP / RULE-OF-THREE CONTROL
--------------------------------------------
The `std::wstring* breakdownString` initially resembles F690's `RFChapterScore` leak, but ownership
is different here:

* `RFTotalScore::reset()` deletes the current string;
* the sole live owner is the by-value `RiseFall::riseScore` member;
* `RiseFall::~RiseFall()` calls `reset()`, and `RiseFall::reset()` calls `riseScore.reset()`;
* `RiseFall::read()` resets the whole RiseFall object before `riseScore.read()`, and
  `RFTotalScore::read()` resets itself again before loading fields;
* `updateString()` deletes the previous string before allocating its replacement;
* the Python bridge immediately copies the returned wide string into a by-value `std::wstring`.

Therefore normal destruction, save loading and repeated Score-tab previews do not leak the final
RFTotalScore string. The class remains Rule-of-Three-fragile if it were ever value-copied, but no
current copy/assignment of `RFTotalScore` or `RiseFall` exists. Keep that as structural robustness,
not F701.

SERIALIZATION / BOUNDS CONTROL
------------------------------
Current `write` and `read` are exactly paired: version marker followed by 12 integer fields and the
`frozen` Boolean in the same order. `read` calls `reset()` first. The currently-unused version value
is legacy/future-format scaffolding; same-version persistence is coherent and cross-version save
compatibility is not being promoted as a current defect.

The ordinary totals are bounded by the small chapter count (configured base 7 and additionally
bounded by alive civilizations/game-speed construction), rank/current-score compression and the
finite victory premium. No independent supported integer-overflow/divide-by-zero root survives the
remaining arithmetic. `initialRivals` is explicitly protected by `max(1, initialRivals)` in the
victory-rank division.

VICTORY FAST SAVE -- F608 DEDUP / BREADTH
-----------------------------------------
The SAS optional Victory Fast Save is written while the winner/victory fields have already changed
but before the normal GAMESTATE_OVER transition invokes Rise/Fall's final preparation/freeze. Such a
snapshot can therefore also contain an unfrozen/pre-final `riseScore` state. This is not a second
RFTotalScore defect: F608/KI#931 already owns the exact lifecycle root that the Victory Fast Save
serializes the game before the end-state transition and cannot reconstruct that missing transition
on load. Record stale/unfrozen Rise score as additional consequence breadth of F608 if that issue is
repaired/documented; do not create F701 for the same premature snapshot.

NO-TURN-LIMIT CONTROL
---------------------
The final endless chapter's special remaining-time handling and the penultimate negative scoring
sentinel were already fully audited in Queue101. RFTotalScore merely consumes the chapter values it
is given. No additional independent no-turn-limit arithmetic root was established here beyond
F695/F696 and the Queue098 timing roots already deduped above.

HISTORY SATURATION
------------------
All retained semantic RFTotalScore history was reconciled:

* practical 1268 introduced the class and core Rise-score model;
* practical 1271 adjusted victory/remaining-time tuning and normalized-score parameters;
* practical 1331 added only the Dan-Quayle comment;
* 1469/1493/1497/1548/1564/1644/1753 are include/accessor/info-helper refactors with the score
  formulas preserved;
* practical 2920 is namespace/comment cleanup; duplicate later path-history mirrors do not add a
  distinct semantic RFTotalScore change;
* current SAS `RFTotalScore.cpp` remains semantically identical to supplied Base AdvC 1.14; the
  important SAS-specific interaction is therefore the later Handicap XML meaning change isolated
  as F700, not a hidden direct source drift.

QUEUE 122 is CLOSED with F698-F700 confirmed and no independent F701.

NEXT ORDINARY CURSOR
--------------------
Queues 123 `TeamPathFinder.cpp`, 124 `CyGlobalContextInterface4.cpp` and 125 `UWAIReport.cpp` were
already completed by the earlier priority/reloop work, so ordinary order advances directly to the
next still-pending file:

    Queue 126  CvGameCoreDLL/CvDllTranslator.cpp  (150 lines)

Open it under F701 / provisional KI#1022 unused. Audit translator ownership/buffer/varargs/text-key/
language/fallback contracts, current callers, Base/Civ4CE ancestry and whole-album dedup before
promoting anything.

Exact continuation stone:
C031-WIP630 physical -> Queue122 CLOSED with F698-F700 / no independent F701 -> 129 / 169 complete
-> skip already-complete Queues123-125 -> Queue126 CvDllTranslator.cpp OPEN -> F701/KI#1022 UNUSED
-> translator current/Base/caller/lifetime/format/history/dedup audit -> freeze next settled WIP.

Files complete: 129 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP631 -- QUEUE 126 CvDllTranslator.cpp OPEN / F701 CONFIRMED / F702 UNUSED
===============================================================================
Physical parent: C031-WIP630.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
K-Mod/Civ4CE translator lineage, current English diplomacy text + CIV4DiplomacyInfos reachability,
local path history and whole-album dedup. Gameplay/XML/Python source untouched.

F701 / provisional KI#1022 -- shipped English diplomacy contains two CT replacement tags that the
exported translator does not implement
-----------------------------------------------------------------------------------------------
Status: CONFIRMED CURRENT INHERITED DIPLOMACY-TEXT DEFECT ROOT; player-visible English paths.

TRANSLATOR CONTRACT
-------------------
`initializeTags` tells the EXE that counterparty substitutions begin with `[CT_`. `replaceCt` then
accepts the token stem without the closing bracket/form suffix; every working case compares exact
stems such as:

    [CT_NAME
    [CT_EMPIRE
    [CT_CIV_SHORT
    [CT_CIV_ADJ
    [CT_STATE_RELIGION
    [CT_BEST_UNIT
    [CT_WORST_ENEMY

This establishes the closed-EXE handoff strongly enough without guessing its parser internals: the
large current corpus of `[CT_NAME]`, `[CT_CIV_SHORT]` and `[CT_CIV_ADJ]` strings can only reach these
exact comparisons if the EXE supplies the corresponding stem. `replaceCt` uses equality, not prefix
matching, and its unknown branch calls `FErrorMsg("Unknown Diplomacy String")` then returns false.

CURRENT SHIPPED MISMATCHES
--------------------------
A mechanical scan of current XML finds six distinct CT stems in actual text. Four are implemented;
two are not:

1. `AI_DIPLO_NO_PEACE_LEADER_FRANKLIN_ROOSEVELT_1` contains:

       ... I do not negotiate with my enemies, [CT_CIV]!

   `CIV4DiplomacyInfos.xml` wires that key into `AI_DIPLOCOMMENT_NO_PEACE` specifically for
   `LEADER_FRANKLIN_ROOSEVELT`. `[CT_CIV` has no `replaceCt` branch.

2. `AI_DIPLO_DEMAND_TRIBUTE_POWER_STRONGER_RAGNAR_LODBROK_1` contains:

       ... The [CT_NAME_ADJ] are at our mercy. Give us tribute or die.

   `CIV4DiplomacyInfos.xml` wires that key to Ragnar's `DIPLOMACYPOWER_STRONGER` tribute-demand
   response. `[CT_NAME_ADJ` has no branch either.

These are English-only bad tokens in the current localization; the other localized variants use
valid tags or literal wording. English is a normal shipped language, so this is current supported
activation rather than mod-extension robustness.

CONSEQUENCE / SCOPE
-------------------
Whenever either specific line is selected, the CT replacement callback cannot produce a replacement
for that token and returns false through the translator's explicit unknown-string path. The exact
release-EXE presentation after a failed callback is closed-source, so do not overclaim whether the
literal token is preserved, omitted or otherwise handled; the proven defect is that the shipped
line requests a substitution the registered DLL translator cannot resolve. Debug builds additionally
hit the unknown-diplomacy-string error path.

ANCESTRY
--------
K-Mod practical 3 introduced this exported translator with the same seven CT branches. The supplied
K-Mod assets already contain both bad English tags in their diplomacy text; supplied Base AdvC 1.14
retains the same translator and both strings. Current SAS inherits them unchanged. This is therefore
an inherited K-Mod/BtS-text interaction, not an SAS-created regression.

The whole album contains no prior owner for `[CT_CIV]`, `[CT_NAME_ADJ]` or this unsupported-tag
interaction. F369/KI#692 also touches `CvDllTranslator`, but that root is the randomized
`[OUR_BEST_UNIT]` double-selection problem and is independent.

REPAIR BOUNDARY
---------------
Prefer correcting the two localization tags to the intended existing vocabulary rather than adding
ambiguous new semantic aliases to the DLL:
- Ragnar's noun phrase clearly wants the civilization adjective, i.e. `[CT_CIV_ADJ]`;
- Roosevelt's address should use whichever existing civilization form is intended by the text
  (`[CT_CIV_SHORT]` is the narrow likely choice, or `[CT_EMPIRE]` if full description is desired).
If compatibility with external text packs matters, `replaceCt` could additionally accept aliases,
but their semantics should be defined explicitly rather than guessing that NAME_ADJ means player
name adjective.

F701 / provisional KI#1022 is CONFIRMED. F702 / provisional KI#1023 is now unused.

QUEUE 126 CONTINUATION
----------------------
Finish `CvDllTranslator.cpp` rather than closing at F701: audit `replaceOur` diplomacy-player
lifetime/NO_PLAYER assumptions, current OUR/CT tag census including forms, color-map construction,
icon-map keys, initialization/language-reset lifetime, the KI#102/KI#692 SAS deltas, complete
K-Mod/AdvC path history and whole-album dedup. Promote only an independent supported F702.

Exact continuation stone:
C031-WIP631 physical -> Queue126 OPEN -> F701/KI#1022 CONFIRMED -> F702/KI#1023 UNUSED -> finish
OUR/CT/player-lifetime/tag-map/color/icon/language/history/dedup sweep -> either promote independent
F702 or close Queue126 cleanly -> freeze next WIP.

Files complete: 129 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP632 -- QUEUE 126 CvDllTranslator.cpp OPEN / F702 CONFIRMED / F703 UNUSED
===============================================================================
Physical parent: C031-WIP631.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
Civ4CE/K-Mod icon-translator baseline, current XML icon-token census, Advanced Scoreboard config/text,
practical-1489 ancestry and whole-album dedup. Gameplay/XML/Python source untouched.

F702 / provisional KI#1023 -- Advanced Scoreboard hover uses `[ICON_GOLDEN_AGE]`, but the global icon
translator registers `[ICON_GOLDENAGE]`
-------------------------------------------------------------------------------------------------
Status: CONFIRMED CURRENT ADVCIV UI-TEXT DEFECT ROOT; shipped option-help activation.

EXACT TOKEN CONTRACT
--------------------
`CvDllTranslator::initializeTags()` builds the icon replacement map used by the text system. It
registers:

    aIconMap[L"[ICON_GOLDENAGE]"] = ... GOLDEN_AGE_CHAR

with no underscore between GOLDEN and AGE. This spelling is inherited from the BtS/Civ4CE/K-Mod
translator baseline; Civ4CE's older Python translator uses the same `ICON_GOLDENAGE` spelling.

A mechanical current-XML census found 31 distinct `[ICON_*]` tokens. The C++ translator registers 33
icon keys. There is exactly one used-but-unregistered token:

    ICON_GOLDEN_AGE

and the corresponding registered `ICON_GOLDENAGE` key has zero current XML uses. (`ICON_MAP` and
`ICON_MOVES` are the other currently-unused registered keys.) Thus this is not a broad parser
ambiguity or an incomplete scan; it is a one-token spelling mismatch.

LIVE CURRENT ACTIVATION
-----------------------
`BUG_Advanced_Scoreboard_Options.xml`'s
`TXT_KEY_BUG_OPT_SCORES__DISPLAYORDER_HOVER` explains the Advanced Scoreboard Display Order. Its
English text says:

    J - [ICON_GOLDEN_AGE]/[ICON_BAD_GOLD] icon when in a Golden Age/ Anarchy

and the German variant uses the same bad Golden-Age token. `Assets/Config/Advanced Scoreboard.xml`
exposes the DisplayOrder option, and `BugScoreOptionsTab.py` places that text-edit/help control on the
shipped Scoreboard options tab. This is therefore ordinary option-help UI, not an unused localization
key.

CONSEQUENCE / SCOPE
-------------------
The registered icon map has no entry for `[ICON_GOLDEN_AGE]`, so the Golden-Age half of this legend
cannot be replaced through the normal icon-token map while the neighboring `[ICON_BAD_GOLD]` and
other icon tokens can. The exact visible fallback for an unknown icon key is EXE-side; keep the
finding scoped to failed icon substitution rather than asserting a specific literal/blank rendering.
This affects help text only, not the scoreboard's actual J-column Golden-Age state/icon logic.

ANCESTRY
--------
The translator's supported spelling predates AdvC and is present in Civ4CE/K-Mod. AdvC practical
1489 / ce9b54a237 (`More scoreboard changes; expanding scoreboard enabled by default.`) added the J
Golden Age/Anarchy legend and introduced `[ICON_GOLDEN_AGE]` into both English and German hover text.
Supplied Base AdvC 1.14 retains that mismatch; current SAS inherits it unchanged.

DEDUP / REPAIR
--------------
No prior album root owns this icon-token mismatch. F701 is the separate CT dynamic-diplomacy tag
family and cannot absorb a static icon-map typo.

The narrow repair is textual: replace `[ICON_GOLDEN_AGE]` with the already-established
`[ICON_GOLDENAGE]` in the affected hover translations. Renaming the global registered token would be
riskier for external/localization compatibility and is unnecessary.

F702 / provisional KI#1023 is CONFIRMED. F703 / provisional KI#1024 is now unused.

QUEUE 126 CONTINUATION
----------------------
Finish the remaining translator surface: all OUR/CT forms and player-lifetime assumptions, full
COLOR-token -> ColorInfo coverage, icon-map value sources, initialization/language-reset lifetime,
SAS KI#102 and KI#692 deltas, complete history/dedup saturation. Promote only an independent F703.

Exact continuation stone:
C031-WIP632 physical -> Queue126 OPEN -> F701-F702/KI#1022-KI#1023 CONFIRMED -> F703/KI#1024
UNUSED -> finish player lifetime/forms/color/icon/init/history/dedup closure -> either promote an
independent F703 or close Queue126 cleanly -> freeze next WIP.

Files complete: 129 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP633 -- QUEUE 126 CvDllTranslator.cpp CLOSED / QUEUE 127 CyMapGenerator.cpp OPEN / F703 UNUSED
===============================================================================
Physical parent: C031-WIP632.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
Civ4CE/K-Mod translator baseline, complete current XML token census, current diplomacy/BUG consumers,
all retained path history and whole-album dedup. Gameplay/XML/Python source untouched.

QUEUE 126 FINAL RESULT
----------------------
`CvGameCoreDLL/CvDllTranslator.cpp` is CLOSED after full tag-map, current-data, player-lifetime,
form, color/icon, Base/K-Mod/Civ4CE history and whole-album reconciliation.

Confirmed independent roots:
* F701 / KI#1022 -- two shipped English diplomacy lines use unsupported CT stems: Roosevelt
  `[CT_CIV]` and Ragnar `[CT_NAME_ADJ]`.
* F702 / KI#1023 -- Advanced Scoreboard Display Order hover uses `[ICON_GOLDEN_AGE]`, while the
  registered global icon key is `[ICON_GOLDENAGE]`.

No independent F703 survives Queue126.

COMPLETE DYNAMIC-TAG CENSUS
---------------------------
Current XML uses six distinct OUR stems, all implemented by `replaceOur`:

    OUR_NAME, OUR_EMPIRE, OUR_CIV_SHORT, OUR_CIV_ADJ, OUR_BEST_UNIT, OUR_WORST_ENEMY

`OUR_STATE_RELIGION` remains a supported translator branch but has no current XML use.
Current XML uses six distinct CT stems. Four are implemented (`CT_NAME`, `CT_EMPIRE`,
`CT_CIV_SHORT`, `CT_CIV_ADJ`); the only two unsupported current stems are exactly F701's
`CT_CIV` and `CT_NAME_ADJ`. `CT_STATE_RELIGION`, `CT_BEST_UNIT` and `CT_WORST_ENEMY` are supported
but currently unused.

The current form suffixes on these tags are only the ordinary empty/default form or numeric forms
1 through 4. They feed the existing player name/civilization-description/adjective APIs; no invalid
form index or parser mismatch was established.

OUR / CT PLAYER-LIFETIME CONTROL
--------------------------------
`replaceOur` obtains `gDLL->getDiplomacyPlayer()` without a NO_PLAYER guard. This looks asymmetric
next to KI#102's active-player guard in `replaceCt`, but current semantics explain it:
- OUR substitutions are speaker-side diplomacy text, processed while the EXE has a concrete
  diplomacy player;
- CT substitutions identify the active human counterpart, and autoplay can deliberately have
  `getActivePlayer()==NO_PLAYER`, which is the exact KI#102 crash state SAS repaired;
- current OUR tags occur in diplomacy quote/text keys, not a shipped non-diplomacy text path that
  independently invokes `replaceOur` with no speaker.
No supported current NO_PLAYER dereference survives on the OUR side.

ICON MAP SATURATION
-------------------
The C++ translator registers 33 icon keys; current XML uses 31 distinct icon keys. A mechanical set
comparison leaves exactly one used-but-unregistered key, `ICON_GOLDEN_AGE`, already F702. The
registered-but-unused keys are `ICON_GOLDENAGE`, `ICON_MAP` and `ICON_MOVES`; the first is F702's
correct established spelling, while the latter two are harmless retained vocabulary.
All other currently-used icon tags resolve through the registered map.

COLOR MAP SATURATION
--------------------
Current XML uses 16 distinct `[COLOR_*]` tokens. `COLOR_REVERT` is registered explicitly, and every
other used token resolves to a current `ColorInfo` type. The current 128 ColorInfo RGBA definitions
were mechanically checked: all 512 channels lie in [0,1], so multiplication by 255 and conversion
to integer produces valid 0..255 components. No missing color key or current out-of-range color
conversion exists.

The maps own their std::wstring/CvWString keys and values. `initializeTags` runs after the relevant
info tables/symbols exist; current language changes/reset behavior does not mutate ColorInfo type
identity or symbol IDs. No stale pointer/lifetime dependence was found in the map construction.

SAS DELTAS / EXISTING ROOTS
---------------------------
Compared with supplied Base AdvC 1.14, the only meaningful current SAS behavior changes are already
owned:
- KI#102: `replaceCt` now returns false when there is no active player during autoplay instead of
  indexing `GET_PLAYER(NO_PLAYER)`;
- KI#692/F369: the `[OUR_BEST_UNIT]` branch consumes the exact remembered randomized UNIT_BRAG unit
  rather than rerunning the asynchronous selector.
The include change from CvPlayer.h to CvPlayerAI.h exists solely to support that KI#692 handoff.
No new behavior regression was introduced by those repairs.

HISTORY / DEDUP CLOSURE
-----------------------
K-Mod practical 3 introduced the exported translator and its core icon/OUR/CT/color maps. AdvC's
retained changes are mostly info-access/include/assert-style refactors plus added icon vocabulary;
Base 1.14 preserves the same core behavior. Queue126's two surviving roots are both text-vocabulary
interactions: F701's bad CT stems were already present in K-Mod/AdvC text, while F702 was introduced
by AdvC practical 1489 when adding the scoreboard J legend. Whole-album search found no prior owner
for either root. F369/KI#692 and KI#102 are explicitly deduped above.

QUEUE 126 is CLOSED with F701-F702 confirmed and no independent F703.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue 127 `CvGameCoreDLL/CyMapGenerator.cpp` (149 queued lines), OPEN
under F703 / provisional KI#1024 unused.

Audit the Python map-generator wrapper against native CvMapGenerator and Queue045: pointer/ownership
contracts, enum/index bounds, erase/add rivers/features/bonuses/goodies hooks, regeneration
lifecycle, current shipped Python/map-script callers, Base/K-Mod/Civ4CE parity and whole-album dedup.
Promote only an independent supported root.

Exact continuation stone:
C031-WIP633 physical -> Queue126 CLOSED with F701-F702 / NO independent F703 -> 130 / 169 complete
-> Queue127 CyMapGenerator.cpp OPEN -> F703/KI#1024 UNUSED -> wrapper/native/caller/ownership/index/
map-lifecycle/history/dedup sweep -> either promote consequential F703 or close cleanly -> freeze
next WIP.

Files complete: 130 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP634 -- QUEUE 127 CyMapGenerator.cpp CLOSED / QUEUE 128 ReproTest.cpp OPEN / F703 UNUSED
===============================================================================
Physical parent: C031-WIP633.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current shipped Pangaea/WorldBuilder Python callers, native Queue045 CvMapGenerator audit, current
CyMapGeneratorInterface registration and retained local path history. Gameplay/XML/Python source
untouched.

QUEUE 127 FINAL RESULT
----------------------
`CvGameCoreDLL/CyMapGenerator.cpp` is CLOSED after wrapper/native, shipped-caller, ownership,
list-length, enum/input, map-lifecycle, Base/history and whole-album reconciliation. No independent
F703 survives.

BASE / OWNERSHIP CONTRACT
-------------------------
Current SAS `CyMapGenerator.cpp` is executable-semantically identical to supplied Base AdvC 1.14.
The default Python-visible constructor stores the address of `CvMapGenerator::GetInstance()`, a
process-lifetime singleton owned outside the wrapper. `CyMapGenerator` therefore correctly owns and
deletes nothing. The alternate C++ constructor can hold NULL and the ordinary forwarding methods
guard `m_pMapGenerator`; no Python return in this file creates a wrapper requiring a Boost.Python
ownership policy.

SHIPPED PYTHON CALLER CENSUS
----------------------------
A current Assets/PrivateMaps caller census found only three actual method invocations through this
wrapper:
- `PrivateMaps/Pangaea.py` calls `mapgen.setPlotTypes(plotTypes)` at two Solid-Pangaea cohesion
  points;
- `Assets/Python/pyWB/CvWBDesc.py` calls `CyMapGenerator().addBonuses()` when a WorldBuilder save
  requests randomized resources.
`PerfectMongoose.py` constructs a `CyMapGenerator`, but the current retained path does not invoke a
method through that local object. No shipped Python caller invokes `doRiver`, `canPlaceBonusAt`,
`canPlaceGoodyAt`, addUnique/NonUniqueBonusType, or the erase/add-generation methods directly.

`Pangaea.py` obtains each direct `setPlotTypes` list from its HintedWorld generators. Those return
`HintedWorld.generatePlotTypes`, which ultimately fills/returns the FractalWorld plot-type array
indexed across `iNumPlotsX * iNumPlotsY`. The list is therefore current-map-sized by construction
before either wrapper call. The WorldBuilder `addBonuses` path runs after map application/area
recalculation and simply delegates to the already-audited native resource-generation path.

DIRECT `setPlotTypes` LENGTH HAZARD -- EXTENSION API ONLY
---------------------------------------------------------
`CyMapGenerator::setPlotTypes` converts the supplied Python sequence with `putSeqInArray` and does
not pass or validate its length. Native `CvMapGenerator::setPlotTypes(const int*)` then reads
`GC.getMap().numPlots()` entries unconditionally. A third-party Python caller can therefore supply a
short list and induce an out-of-bounds native read.

This is a real exposed-API robustness weakness, but no shipped current caller supplies a malformed
length: the only two direct calls are the map-sized Pangaea lists proved above. Keep the edge
recorded rather than spending F703 without supported-current activation. This is also distinct from
native Queue045's Python-result validation: the ordinary map-function result path is checked by
`CvPythonCaller`; this direct wrapper is an optional lower-level bridge.

`doRiver` / ENUM / POINTER ROBUSTNESS
------------------------------------
`CyMapGenerator::doRiver` checks `m_pMapGenerator` but dereferences `pStartPlot->getPlot()` without
checking the wrapper or its underlying plot. Invalid/out-of-range enum integers are likewise cast
directly in several bridge methods. These are classic Python extension-boundary sharp edges, but
there are no shipped Python calls to `doRiver` or the enum-taking bonus/goody helpers. No supported
NULL-wrapper or invalid-enum activation was established.

The native `canPlaceBonusAt`/`canPlaceGoodyAt` paths already apply their own plot/data legality once
called with valid enum inputs; no wrapper-specific gameplay mismatch survives.

NATIVE QUEUE045 DEDUP / MAP-LIFECYCLE CONTROL
---------------------------------------------
Queue045 already audited `CvMapGenerator.cpp` deeply and owns F241-F249. Queue127 does not duplicate
those roots. In particular:
- native `setPlotTypes` writes every plot with deferred updates and then explicitly calls
  `CvMap::recalculateAreas()` before assigning water terrain, providing the strong negative control
  against stale-area variants such as F249;
- river, lake, feature, bonus, goody and Python-override generation semantics were already traced at
  the native layer;
- malformed third-party cardinal directions were already outside the supported shipped caller set.
This wrapper neither changes the native ordering nor adds another cache/state layer.

REGISTRATION / HISTORY
----------------------
A quick cross-file control against `CyMapGeneratorInterface.cpp` finds the wrapper methods exported
under their matching names/signatures. There are no pointer-return policies or fresh-object returns
to mis-own. Full registration-file coverage remains reserved for Queue159.

The retained K-Mod/AdvC history touching this wrapper is dominated by include/project/refactor work;
the meaningful AdvC include restoration for `CvDLLPythonIFaceBase` supports `putSeqInArray` rather
than changing the direct-list contract. Current SAS and Base AdvC 1.14 retain the same executable
wrapper behavior. No historical semantic regression surviving current shipped use was found.

QUEUE 127 is CLOSED with no new finding; F703 / provisional KI#1024 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue128 `CvGameCoreDLL/ReproTest.cpp` (149 queued/current physical
lines), OPEN under F703 / provisional KI#1024 unused.

Audit this compile-time-disabled reproducibility harness as test/debug infrastructure: constructor/
quick-save/quick-load cancellation lifecycle, singleton ownership, save-byte/object-ID vector
alignment, bounds during the second comparison pass, assertion/log formatting, autoplay/debug-state
restoration, runtime BBAI-gate SAS delta, Base/history parity and whole-album dedup. Promote only a
supported enabled-harness defect rather than ordinary disabled test-code sharpness.

Exact continuation stone:
C031-WIP634 physical -> Queue127 CLOSED / NO new F703 -> 131 / 169 complete -> Queue128 ReproTest.cpp
OPEN -> F703/KI#1024 UNUSED -> singleton/quick-save-load/vector-index/autoplay/log/history/dedup sweep
-> either promote consequential enabled-harness F703 or close cleanly -> freeze next WIP.

Files complete: 131 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================



===============================================================================
C031-WIP635 -- QUEUE 128 ReproTest.cpp OPEN / F703 CONFIRMED / F704 UNUSED
===============================================================================
Physical parent: C031-WIP634.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
current CvGame/CvGameInterface/AIAutoPlay save-control lifecycle, practical-2033 birth diff and
whole-album dedup. Gameplay/XML/Python source untouched.

F703 / provisional KI#1024 -- CANCELED REPRO TEST REMAINS ARMED AND CAN QUICK-LOAD AN UNRELATED SAVE
---------------------------------------------------------------------------------------------------
Status: CONFIRMED inherited AdvC enabled-debug-harness lifecycle defect. The implementation is
compile-gated out of ordinary SAS builds by default; consequence applies when ENABLE_REPRO_TEST is
deliberately enabled.

ROOT CONTRACT
-------------
`ReproTest::startTest(int iTurns)` deletes any previous test and then performs:

    ReproTest::m_pReproTest = new ReproTest(iTurns);

The constructor initializes `m_iAutoPlayTurns`, `m_bQuickLoadDone=false` and `m_iPos=0`, then checks
whether Quick Save is currently permitted. If a focused/open widget blocks it, the constructor says:

    FErrorMsg("Open widget blocks quick-save. Repro test canceled.");
    return;

Returning normally from a C++ constructor does not make `new` fail and does not return NULL. The
new object therefore reaches the assignment in `startTest()` and becomes the live singleton despite
the explicit "canceled" message. The constructor returns before Quick Save and before
`setAIAutoPlay`, so autoplay remains zero and the object's first-pass state stays armed.

CONCRETE ENABLED-HARNESS CONSEQUENCE
------------------------------------
Every synchronized write site uses the REPRO_TEST_* macros whenever `getInstance()!=NULL`.
Because the canceled object's autoplay count in the game is still zero, the next unrelated save
after the blocking UI closes is treated as ReproTest's first comparison pass:
- `beginWrite` clears the byte buffer and appends object IDs;
- `recordData` captures serialized bytes because game autoplay is <=0;
- `endWrite` appends each object's captured vector to `m_aaSaveData`;
- the final Statistics write reaches `endWrite(true)`.

If Quick Load is still blocked at that later point, the code finally deletes the singleton, but only
after the unrelated save has been consumed as a test pass. More seriously, if Quick Load is now
allowed, the code sets `m_bQuickLoadDone=true` and executes CONTROL_QUICK_LOAD.

The canceled test never executed its constructor's CONTROL_QUICK_SAVE. The QuickSave loaded here is
therefore whatever pre-existing user QuickSave happens to exist (or a failing/missing QuickSave),
not a baseline created by this ReproTest. An enabled debug session can thus unexpectedly roll back
to an unrelated older QuickSave after the user's next save, despite having been told that the test
was canceled.

CONTROL / REACHABILITY
----------------------
This is not malformed input. The constructor itself explicitly anticipates `canDoControl(
CONTROL_QUICK_SAVE)==false`; `CvGame::canDoControl` rejects Quick Save while the UI is focused.
ReproTest is started by right-clicking the leader widget when ENABLE_REPRO_TEST is enabled. Once the
blocking focus is gone, an ordinary manual/automatic save can invoke the synchronized writers while
the stale singleton remains live.

`CvGame::doControl(CONTROL_QUICK_LOAD)` does not verify that the current ReproTest created the file.
Its normal SP path simply invokes the current QuickSave (with AdvC's fullscreen existence check where
applicable). Therefore there is no hidden token or generation guard that reconnects this later load
to the failed constructor save.

ANCESTRY / SAS SCOPE
--------------------
AdvC practical 2033 / fe4f9ef0fb (`Code for testing whether AI Auto Play sequence is reproducible`)
introduced `startTest`, the allocating constructor, and the early "canceled" return in the same
patch. Practical 2503 changed the report from FAssertMsg to FErrorMsg but retained the lifecycle.
Supplied Base AdvC 1.14 still has the same root.

SAS practical 5972 merely compile-gates the disabled implementation out of normal builds unless
ENABLE_REPRO_TEST is explicitly enabled. The runtime-BBAI logging change is observational and does
not create or repair this root. Classification therefore stays enabled-debug-harness defect rather
than ordinary-gameplay defect.

DEDUP / REPAIR
--------------
No prior album root owns this canceled-constructor/singleton lifecycle. F685/KI#1006 concerns
uninitialized bytes that ReproTest can observe; it is not the harness-control failure here.

The narrow repair is to make cancellation happen before a live singleton is installed, or otherwise
return an explicit success state. For example, `startTest` can precheck Quick Save before allocating,
or construction can expose validity and `startTest` can delete/refuse the object on failure. Do not
leave a non-NULL singleton after printing "Repro test canceled."

F703 / provisional KI#1024 is CONFIRMED. F704 / provisional KI#1025 is now unused.

QUEUE 128 CONTINUATION
----------------------
Finish the independent zero-turn/player-ID-0 path, then the first/second save-pass object-ID/vector
alignment, second-pass bounds/termination, quick-load/debug-state/autoplay restoration, singleton
self-deletion, logging/runtime-gate SAS delta, full practical history and whole-album dedup. Promote
only a separate supported enabled-harness root.

Exact continuation stone:
C031-WIP635 physical -> Queue128 OPEN -> F703/KI#1024 CONFIRMED -> F704/KI#1025 UNUSED -> prove or
reject player-0/zero-autoplay behavior -> finish save-vector/lifetime/history/dedup closure -> either
promote independent F704 or close Queue128 cleanly -> freeze next WIP.

Files complete: 131 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP636 -- QUEUE 128 ReproTest.cpp OPEN / F703-F704 CONFIRMED / F705 UNUSED
===============================================================================
Physical parent: C031-WIP635.
Authoritative source: reconciled current SAS light source 20260910T125939, current Foreign Advisor
leader-widget producers, CvGame autoplay lifecycle, AIAutoPlay scheduled-stop/save path, Rise/Fall
active-player switching, practical-2033 birth diff, supplied Base AdvC 1.14 and whole-album dedup.
Gameplay/XML/Python source untouched.

F704 / provisional KI#1025 -- PLAYER ID 0 STARTS A ZERO-TURN REPRO TEST THAT NEVER AUTO-COMPLETES
------------------------------------------------------------------------------------------------
Status: CONFIRMED inherited AdvC enabled-debug-harness trigger/lifecycle defect. Compile-time disabled
in ordinary SAS builds; applies when ENABLE_REPRO_TEST is deliberately enabled.

ROOT CONTRACT
-------------
The harness intentionally uses the contacted player ID as its requested autoplay duration. The
header documents this explicitly, and `CvDLLWidgetData::executeAltAction` passes the leader widget's
`m_iData1` directly to:

    ReproTest::startTest(widgetDataStruct.m_iData1);

Foreign Advisor leader widgets store the concrete player ID in data1. Player IDs are zero-based, so
contacting player 0 requests `iTurns == 0`.

Unlike F703, Quick Save succeeds in this fixture. The constructor creates the intended baseline
QuickSave and then executes:

    kGame.setAIAutoPlay(m_iAutoPlayTurns, true); // m_iAutoPlayTurns == 0

ZERO-AUTOPLAY LIFECYCLE
-----------------------
`CvGame::setAIAutoPlay` clamps the requested value to at least zero. With zero it never disables the
human player and merely emits the AutoPlayComplete event. No AI turn runs.

The normal ReproTest lifecycle depends on reaching the end of the requested autoplay interval and
then writing a save whose bytes become the first comparison pass. In current SAS, scheduled autoplay
completion is finalized by AIAutoPlay.py's `onEndPlayerTurn`/`checkPlayer`, which also creates the
ordinary autoplay-stop Fast Save. That path requires an actual active autoplay interval and an end
player-turn event. With a zero-turn request it is never entered.

The header prerequisite `AutoSaveInterval = 1` cannot rescue this path either: zero autoplay turns
advance no game turn, so there is no post-autoplay autosave to begin the first pass.

CONCRETE CONSEQUENCE
--------------------
After the constructor returns, `startTest` installs the singleton. It is therefore armed with
`m_bQuickLoadDone=false`, but there is no automatic save to progress the test.

The user's next unrelated save becomes the first pass. At its final Statistics write, ReproTest
QuickLoads the baseline QuickSave that this test did create, marks `m_bQuickLoadDone=true`, and then
again calls `setAIAutoPlay(0,true)`. Once again no turn runs and no automatic completion save occurs.
The harness therefore waits for yet another unrelated save to perform its second-pass comparison.

If the user changes state before that later save, the harness can report a false
"Non reproducible state" difference caused by user actions rather than replaying a deterministic AI
sequence. Even without such a false report, a player-0 test does not execute the documented automatic
quicksave -> N AI turns -> save -> quickload -> N AI turns -> compare lifecycle.

SUPPORTED ENABLED-HARNESS REACHABILITY
--------------------------------------
Player 0 is not inherently the active human. Current Rise/Fall explicitly changes the active player
between civilizations, including arbitrary nonzero player IDs; Foreign Advisor enumerates every
other alive met player starting at ID 0. Debug/player-switch workflows used alongside this developer
harness likewise permit a nonzero active player. Thus player 0 can be a displayed rival and can be
right-clicked through the exact ReproTest trigger.

The zero case is not rejected anywhere in `startTest`, `ReproTest` construction or `setAIAutoPlay`.

ANCESTRY / DEDUP / REPAIR
-------------------------
AdvC practical 2033 introduced the player-ID-as-turn-count trigger, documented it in ReproTest.h and
used the zero-based ID directly from the leader widget. Supplied Base AdvC 1.14 retains it. SAS's
5972 compile gate and later logging changes neither create nor repair the zero-duration behavior.

No prior album root owns this trigger. F703 is separate: F703 begins when Quick Save is rejected and
a supposedly canceled constructor still becomes live; F704 begins after a successful Quick Save and
is caused by a valid zero-duration request that cannot drive the save/replay state machine.

The narrow repair is to guarantee a positive test duration at the trigger boundary (for example,
map player IDs to at least one turn, naturally `playerID + 1` if IDs are intentionally used to vary
the duration), or explicitly reject zero with a real cancellation that leaves no singleton. Do not
start the existing state machine with zero turns.

F704 / provisional KI#1025 is CONFIRMED. F705 / provisional KI#1026 is now unused.

QUEUE 128 CONTINUATION
----------------------
Finish first-pass/second-pass object-ID and byte-vector alignment, m_iPos bounds/termination,
QuickLoad/debug-state restoration, singleton delete-this safety, save/autoplay ordering, current SAS
Fast Save interaction, runtime BBAI-gate delta, all practical history and whole-album dedup. Promote
only an independent consequential enabled-harness root.

Exact continuation stone:
C031-WIP636 physical -> Queue128 OPEN -> F703-F704/KI#1024-KI#1025 CONFIRMED -> F705/KI#1026 UNUSED
-> finish vector/index/save/reload/debug/log/history/dedup closure -> either promote independent F705
or close Queue128 cleanly -> freeze next WIP.

Files complete: 131 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP637 -- QUEUE 128 ReproTest.cpp CLOSED / QUEUE 131 Shelf.cpp OPEN / F705 UNUSED
===============================================================================
Physical parent: C031-WIP636.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
all ENABLE_REPRO_TEST serialization macro sites, CvGame/CvPlayer/AIAutoPlay/SASFastSave lifecycle,
full ReproTest path history and whole-album dedup. Gameplay/XML/Python source untouched.

QUEUE 128 FINAL RESULT
----------------------
`CvGameCoreDLL/ReproTest.cpp` is CLOSED after enabled-harness constructor/singleton, QuickSave/
QuickLoad, zero-turn trigger, first/second serialization pass, object-ID/byte-vector, m_iPos,
autoplay/debug-state, SAS Fast Save, logging, Base/history and whole-album reconciliation.

Confirmed roots:
* F703 / KI#1024 -- a constructor that says Quick Save failure canceled the test returns normally;
  `startTest` still installs the object as the singleton, so a later unrelated save can become the
  first pass and can QuickLoad a pre-existing unrelated QuickSave.
* F704 / KI#1025 -- the contacted zero-based player ID is used directly as autoplay duration;
  player 0 therefore starts a zero-turn test that never reaches automatic completion and instead
  consumes later unrelated saves as its first/second passes.

No independent F705 survives Queue128.

FIRST/SECOND PASS VECTOR AND INDEX CONTRACT
-------------------------------------------
A complete current macro census finds 29 `REPRO_TEST_BEGIN_WRITE` sites, paired with 28 ordinary
`REPRO_TEST_END_WRITE` sites plus one `REPRO_TEST_FINAL_WRITE` in Statistics. The apparently nested
CvPlayer first-part END is inside a profiling scope but unconditional; its BEGIN/END pair is not
control-flow asymmetric.

`beginWrite` appends an object ID only when game autoplay is <=0 and the first pass is active.
`endWrite` returns immediately while autoplay is >0 and appends `m_aBytes` only on that same first
post-autoplay save. During a normal first comparison save, autoplay therefore remains stable at zero
through the synchronous write sequence and object IDs/save-data vectors grow one-for-one.

On the second pass no new object IDs or baseline vectors are appended. `m_iPos` starts at zero and is
advanced exactly once after each compared object. When the last expected object is processed,
`m_iPos == m_aObjectIDs.size()` immediately deletes/nulls the singleton; any later serialization
hook therefore sees no ReproTest and cannot make a one-past-end array access. If the second pass
contains fewer trailing sections the test can remain armed rather than proving that omission, but
no supported memory-safety/index fault follows; ordinary parent serialization also records the
counts/order whose divergence is usually detected earlier.

Two separate Game chunks both use the diagnostic label `"Game pt3"`. That can make an assertion
message less precise when identifying which chunk differs, but indexing is positional and does not
use the label as a key. Keep as diagnostic naming debt only.

QUICKLOAD / SELF-DELETION / DEBUG STATE
---------------------------------------
The singleton is always heap-allocated by `startTest`; its `SAFE_DELETE(m_pReproTest)` paths set the
static pointer to NULL. The in-method self-deletion paths either return immediately or touch only
global logging state afterward, not members of the deleted object. No supported post-delete member
access was found.

The first-pass final write sets `m_bQuickLoadDone=true` before invoking QuickLoad, preserving the
comparison state across the reload as intended. The saved debug-mode bit is then used to restore the
known AdvC behavior that loading clears debug mode. `m_iPos` intentionally remains zero across that
reload. No current separate reset path unexpectedly clears the static harness state.

CURRENT SAS AUTOPLAY-STOP FAST SAVE CONTROL
-------------------------------------------
Current SAS enables `SAS_FAST_SAVE_AUTOPLAY_STOP_ENABLE=1`. Scheduled AIAutoPlay completion calls
`endAIAutoPlay` and then `SASFastSave.saveAutoPlayStop()`. With ReproTest enabled, this normal save
can serve as the first comparison pass immediately at autoplay completion, instead of waiting for
the older autosave-interval path documented by Base AdvC.

ReproTest's original design already performs QuickLoad from inside the final serialization callback;
the SAS Fast Save therefore does not introduce the reentrant-load concept. After reload ReproTest
starts the same autoplay interval again. AIAutoPlay.py's still-live `checkPlayer` frame can observe
the re-disabled human and run its ordinary restoration tail, but the inspected calls either no-op on
same active player or refresh deterministic human/AI caches; a concrete persistent first-vs-second
state mismatch was not established. Do not manufacture F705 from this unusual but currently
unproven interaction.

HISTORY / BASE / SAS DELTA
--------------------------
ReproTest.cpp has a very small semantic history:
- practical 2033 creates the full harness and both F703/F704 roots;
- 2037 is comment wording only;
- 2142 adds optional BBAI/MessageLog markers;
- 2503 replaces diagnostic FAssertMsg calls with FErrorMsg;
- SAS 5903 switches BBAI logging from compile-time LOG_AI to runtime gLogBBAI;
- SAS 5972 compile-gates the implementation itself behind ENABLE_REPRO_TEST.
Supplied Base AdvC 1.14 retains the F703/F704 behaviors. No later change repairs them, and the two
current SAS changes are logging/build-scope changes rather than harness state-machine mutations.

QUEUE 128 is CLOSED with F703-F704 confirmed and no independent F705.

QUEUE ADVANCE
-------------
Queues129 `AIStrengthMemoryMap.cpp` and 130 `CvRandom.cpp` were already completed during the priority
reloop, so ordinary order advances directly to Queue131 `CvGameCoreDLL/Shelf.cpp` (134 queued/current
physical lines), OPEN under F705 / provisional KI#1026 unused.

Audit Shelf's map-edge/coastal shelf construction, plot/area/index assumptions, retained storage and
rebuild lifecycle, callers in map generation/normalization, Base/K-Mod history and whole-album dedup.
Pay special attention to diagonal/cardinal water topology and whether Ice/area-cache findings already
own any apparent mismatch. Promote only an independent supported root.

Exact continuation stone:
C031-WIP637 physical -> Queue128 CLOSED with F703-F704 / NO independent F705 -> 132 / 169 complete
-> Queue131 Shelf.cpp OPEN -> F705/KI#1026 UNUSED -> topology/index/storage/caller/history/dedup sweep
-> either promote consequential F705 or close cleanly -> freeze next WIP.

Files complete: 132 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP638 -- QUEUE 131 Shelf.cpp -- F705 CONFIRMED / F706 UNUSED
===============================================================================
Physical parent: C031-WIP637.
Authoritative source: reconciled current SAS light source 20260910T125939, supplied Base AdvC 1.14,
CvMap shelf construction, CvGame Barbarian spawn/cull callers, CvTeam Barbarian-contact caller,
CvPlot unit-list ordering, CvUnit cargo/visibility contracts, Shelf path history and whole-album/KI
dedup. Gameplay/XML/Python source untouched.

QUEUE 131 CONTINUATION STATE
----------------------------
Queue131 `CvGameCoreDLL/Shelf.cpp` remains OPEN.
F705 / provisional KI#1026 is CONFIRMED here.
F706 / provisional KI#1027 is UNUSED.

-------------------------------------------------------------------------------
F705 / provisional KI#1026 -- Shelf::countBarbarians counts loaded land cargo as
sea ships, double-accounting Barbarian passengers and distorting ship spawn/culling
-------------------------------------------------------------------------------

STATUS
------
CONFIRMED inherited AdvC gameplay defect in the normal Barbarian shelf/cargo system. Supplied Base
AdvC 1.14 retains the same executable behavior. The root is not F643/KI#965: F643 controls which
units may be selected for shelf/cargo spawning; F705 begins after valid cargo already exists and
misclassifies those land passengers as sea ships for population control.

CALLER CONTRACT: THE VALUE IS A SHIP COUNT
------------------------------------------
The main live caller in `CvGame::createBarbarianUnits()` processes each shelf attached to a LAND
area and names the result explicitly:

    int iShips = shelves[i]->countBarbarians();

That value is then used as the existing SEA-unit population in three independent controls:

1. `killBarbarian(iShips, shelfSize, ...)` -- shelf crowding/culling;
2. `numBarbariansToCreate(..., iShips)` -- number of new sea units needed;
3. `if (iShips > iBarbarianCities + 2) iNeededSea = 0` -- the explicit ship sanity cap.

`Shelf.h` likewise describes `killBarbarian()` as killing "Just any one Barbarian ship". The shelf
system is therefore not asking `countBarbarians()` for an arbitrary number of Barbarian objects on
water; this caller needs the number of ships.

CURRENT COUNTING IMPLEMENTATION
-------------------------------
For a shelf plot whose head unit is Barbarian, `Shelf::countBarbarians()` does:

    iR += p.plotCount(PUF_isVisible, BARBARIAN_PLAYER);

There is no owner, domain, cargo or sea-unit filter on `plotCount`.

`PUF_isVisible(unit, BARBARIAN_PLAYER)` calls `CvUnit::isInvisible(BARBARIAN_TEAM,false)`. For every
Barbarian-owned unit, `isInvisible` returns false immediately because `getTeam() == eTeam`; the later
`isCargo()` invisibility clause is never reached. Thus loaded Barbarian cargo is visible to its own
player and is counted.

Loaded passengers really are members of the transport plot's unit list. `CvUnit::setTransportUnit`
keeps the passenger on the same coordinates, and `CvUnit::getCargoUnits` finds cargo by iterating all
units on that plot and checking `getTransportUnit()==this`.

The head-unit shortcut does not mask the ordinary transport+cargo case. `CvPlot::addUnit` orders the
plot list through `CvUnit::isBeforeUnitCycle`; for one owner it orders DOMAIN_SEA before DOMAIN_LAND.
Current DomainTypes are SEA=0, AIR=1, LAND=2. A Barbarian Galley/Galleon therefore precedes its land
passengers, passes the `pAnyUnit->isBarbarian()` head check, and the following `plotCount` counts the
ship plus every Barbarian passenger.

CURRENT DATA / NORMAL REACHABILITY
----------------------------------
This is an intended normal AdvC state, not a hypothetical mod-mod configuration:

- Current Galley is DOMAIN_SEA, can carry DOMAIN_LAND and has iCargo=3.
- Current Galleon is DOMAIN_SEA, can carry DOMAIN_LAND and has iCargo=4.
- AdvC's `advc.306` shelf path deliberately calls `randomBarbarianTransport()` and can create/load up
  to two Barbarian land units onto an existing shelf transport.
- The code comment explicitly says to "Keep spawning units on ships" even when no new free units are
  needed.

A single ship with two such passengers therefore makes `countBarbarians()` return 3 rather than the
actual ship count 1.

DOUBLE ACCOUNTING IS SOURCE-PROVEN
----------------------------------
The same cargo-creation path explicitly treats those passengers as LAND units for the land target:

    iNeededLand -= createBarbarianUnits(..., pShelf, iNeededLand > 0);

and `createBarbarianUnits` documents its return value as:

    "Returns the number of land units spawned (possibly in cargo)."

Every loaded passenger increments `iCreated`, and that return value is subtracted from
`iNeededLand`. Thus a passenger is intentionally charged against the land-unit budget when created.
On subsequent shelf passes, F705 charges the very same passenger again by including it in `iShips`,
where it suppresses/culls SEA units. This is not merely a naming disagreement; the unit participates
in both population controls.

CONCRETE GAMEPLAY CONSEQUENCES
------------------------------
A deterministic hard-cap witness needs only a shelf attached to a civ-settled continent with zero
Barbarian cities:

    actual shelf fleet: one Galley + two loaded Barbarian land units
    correct ship count: 1
    current countBarbarians(): 3
    iBarbarianCities: 0

Current code executes:

    if (iShips > iBarbarianCities + 2)
        iNeededSea = 0;

The wrong count gives `3 > 2`, suppressing new sea-unit creation; the correct one-ship count gives
`1 > 2` false. Even below that hard boundary, `numBarbariansToCreate` subtracts `iUnitsPresent`
linearly from its target before applying the creation rate, so every passenger lowers sea creation
pressure as though it were another ship.

Culling can be distorted more severely. `CvGame::killBarbarian` explicitly returns false whenever
`iUnitsPresent <= 5` ("5 is never a crowd"). Two shelf transports carrying two passengers each are
only two ships but are counted as six units. They therefore cross from the guaranteed no-cull case
into the probabilistic crowd-culling path. `Shelf::killBarbarian` selects the head Barbarian combat
unit; because SEA sorts ahead of the same-owner LAND cargo, the ordinary loaded-fleet case selects a
ship. `CvUnit::kill` recursively kills its loaded cargo first, so a cull made eligible only by the
inflated count can remove an entire loaded transport stack rather than merely correcting a real ship
surplus.

SECOND LIVE CALLER
------------------
`CvTeam::isInContactWithBarbarians()` also adds `Shelf::countBarbarians()` after a land-area
preliminary count, with the comment "Actual check incl. ships." Cargo may or may not be desirable in
that *contact* heuristic, but this does not justify using one ambiguous helper for the sea-fleet
population caller. A repair should preserve the intended semantics of each caller explicitly rather
than silently choosing one global interpretation.

ANCESTRY / BASE CONTROL
-----------------------
Supplied Base AdvC 1.14 has the same `countBarbarians` body, cargo visibility behavior and ship-count
callers; SAS inherited the root unchanged (current Shelf changes versus Base are formatting only).

The supplied reachable Git history starts with practical 1253 "Initial commit (cont.)", where the
Shelf implementation already contains both layers:
- the `advc.300` shelf population code uses `countBarbarians()` as `nShips`;
- the later-tagged `advc.306` code places land cargo on shelf transports;
- the initial `countBarbarians()` used `plot->getNumUnits()`, which necessarily counted cargo too.

Practical 3180 "Refactoring in Shelf class" changed that broad `getNumUnits()` to
`plotCount(PUF_isVisible, BARBARIAN_PLAYER)` and added the visible-unit coexistence comment. This
narrows foreign/invisible-unit handling but does not repair F705: own Barbarian cargo is visible to
BARBARIAN_PLAYER by definition and remains counted. No later Shelf practical changes the domain/
cargo semantics.

Because practical 1253 is the first supplied public-history snapshot for Shelf, the exact pre-Git
birth of the advc.306 interaction cannot be dated more narrowly from the provided archaeology; do
not invent an earlier commit number.

WHOLE-ALBUM / KNOWN-ISSUES DEDUP
--------------------------------
Whole-album and current Known-Issues searches for Shelf/countBarbarians, Barbarian transport cargo,
ship-count cargo, and sea-spawn population find no prior owner for this double accounting.

Important neighboring roots are independent:
- F643/KI#965: practical 3898 loses the parent-land context when *selecting* shelf/cargo unit types,
  suppressing resource-gated and mounted cargo. F705 applies after cargo exists and corrupts fleet
  population accounting.
- KI#331: restrictive `canScrap` can veto inherited Barbarian-ship cleanup. F705 concerns the
  separate Shelf crowd-culling system and an incorrect `iUnitsPresent` input.
- F632/KI#954: final `Shelf*` ownership leak at CvMap teardown. No counting relationship.

REPAIR BOUNDARY
---------------
Do not merely special-case one transport class. The sea-density caller needs an explicit count of
Barbarian SEA units/ships, excluding loaded LAND cargo. A narrow helper such as
`countBarbarianShips()` can iterate shelf units and count Barbarian units with DOMAIN_SEA (and any
additional exact ship eligibility the caller requires).

Because `CvTeam::isInContactWithBarbarians` may conceptually want all relevant shelf Barbarians,
including or excluding loaded passengers by policy, separating the helpers/caller semantics is safer
than silently changing a generically named `countBarbarians()` and assuming both consumers want the
same quantity.

VALIDATION TARGETS
------------------
1. Empty shelf: ship count 0.
2. One unloaded Galley: ship count 1.
3. One Galley + one/two loaded land passengers: ship count remains 1.
4. Two loaded transports with two passengers each: ship count 2 and the `<=5` no-cull guard remains
   active because there are only two ships.
5. Parent continent with zero Barbarian cities, one Galley + two passengers: sea hard cap evaluates
   from 1 ship, not 3 pseudo-ships.
6. Loaded passengers continue to reduce `iNeededLand` through the existing `iCreated` return path.
7. Decide/test CvTeam Barbarian-contact semantics separately rather than inheriting sea-density
   semantics accidentally.
8. Preserve ordinary invisible/non-Barbarian coexistence behavior for the next Shelf audit tranche;
   that head-unit shortcut is a separate candidate and is not folded into F705 without proof.

F705 / provisional KI#1026 is CONFIRMED. F706 / provisional KI#1027 is UNUSED.

QUEUE 131 CONTINUATION
----------------------
Continue the remaining independent Shelf surfaces: invisible foreign-unit coexistence vs head-unit
shortcuts in `countBarbarians`, `killBarbarian` and `randomBarbarianTransport`; raw CvPlot pointer
lifetime/recompute and F632 dedup; computeShelves PASSABLE/HABITABLE/adjacent-land assumptions versus
Ice/area findings; weighted-random bounds; empty/ownership/city/unit gates; all path-history strata
and whole-album dedup. Promote F706 only for a distinct supported root.

Exact continuation stone:
C031-WIP638 physical -> Queue131 OPEN -> F705/KI#1026 CONFIRMED -> F706/KI#1027 UNUSED -> finish
invisible-coexistence/head-order + topology/storage/random/history/dedup sweep -> either promote an
independent consequential F706 or close Queue131 cleanly -> freeze next WIP.

Files complete: 132 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP639 -- QUEUE 131 Shelf.cpp -- F706 CONFIRMED / F707 UNUSED
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP638; gameplay/XML/Python
source remains untouched. Queue131 remains OPEN while the independent topology/storage/random tail is
completed.

F706 / provisional KI#1027 -- SHELF HEAD-UNIT SHORTCUTS MISS BARBARIAN SHIPS WHEN AN INVISIBLE
FOREIGN UNIT COEXISTS ON THE SAME WATER PLOT
-------------------------------------------------------------------------------
Status: CONFIRMED CURRENT INHERITED ADVCIV SHELF-ACCOUNTING/SELECTION ROOT. Supported current-unit
fixture and deterministic plot ordering prove activation; independent from F705's loaded-cargo
ship-count inflation.

ROOT CONTRACT
-------------
Three Shelf helpers assume that, if a shelf plot contains Barbarian units, a Barbarian unit will
occur at the head/front of the plot's unit list:

1. `Shelf::countBarbarians()` reads only `p.headUnitNode()`. If that one unit is not Barbarian, the
   function contributes zero for the entire plot; only when the head is Barbarian does it call
   `plotCount(PUF_isVisible, BARBARIAN_PLAYER)`.
2. `Shelf::killBarbarian()` likewise examines only the head unit. If the head is not a Barbarian
   combat unit, it skips the plot even when a Barbarian ship is present later in the list.
3. `Shelf::randomBarbarianTransport()` iterates the plot list but executes `break` on the first
   non-Barbarian unit. A non-Barbarian prefix therefore prevents later Barbarian transports from
   being considered for fog cargo.

The source comment justifying the shortcut says Barbarians are unable to coexist with visible units.
That is true only for units visible to the Barbarian team; it does not imply that every unit sharing
such a plot is visible to Barbarians.

SUPPORTED CURRENT COEXISTENCE FIXTURE
-------------------------------------
Current unit XML contains `UNIT_ATTACK_SUBMARINE`, a normal `DOMAIN_SEA` unit with
`INVISIBLE_SUBMARINE` invisibility. On a coastal/shelf water tile where the Barbarian team has no
Submarine detector, an Attack Submarine can therefore be invisible to Barbarians.

`CvUnit::canCoexistWithEnemyUnit(eTeam)` explicitly returns true when the unit is invisible to the
specified team. `CvUnit::setXY` uses that predicate for units already on the destination plot: when a
Barbarian fighting ship moves onto a tile occupied by an undetected foreign Attack Submarine, the
old submarine is allowed to remain because it can coexist with the Barbarian team. Thus a foreign
invisible sea unit and an ordinary visible Barbarian ship can legally share a shelf plot in supported
current gameplay.

DETERMINISTIC HEAD-ORDER PROOF
------------------------------
`CvPlot::addUnit` keeps the plot unit list sorted by `CvUnit::isBeforeUnitCycle`. That comparator
sorts by owner before domain/type/level/experience/ID. `BARBARIAN_PLAYER == MAX_CIV_PLAYERS`, while
every normal civilization player ID is strictly below `MAX_CIV_PLAYERS`.

Therefore, in the supported coexistence fixture above, the foreign civilization's Attack Submarine
is deterministically ordered before every Barbarian unit on the same plot. This is not a rare
linked-list insertion accident: the exact owner-order contract makes the non-Barbarian invisible unit
the head.

CONCRETE CONSEQUENCES
---------------------
With one invisible foreign Attack Submarine plus one or more visible Barbarian ships on the same
shelf plot:

- `countBarbarians()` sees the submarine at `headUnitNode`, fails `isBarbarian()`, and contributes 0
  for that plot. The sea-density code can therefore undercount the Barbarian fleet, spawn additional
  ships and avoid crowd culling that should have been considered. `CvTeam::isInContactWithBarbarians`
  can likewise miss those shelf Barbarians in its ship-inclusive contact check.
- `killBarbarian()` sees the same foreign head and skips the plot. A crowded shelf can therefore fail
  to remove a Barbarian ship merely because an undetected foreign submarine coexists there.
- `randomBarbarianTransport()` encounters the foreign unit first and immediately `break`s, so a
  Barbarian transport later on the same plot is excluded from the cargo-loading candidate set.

These are one root: all three helpers rely on a false "Barbarian units form the visible/head prefix"
assumption for a unit list that can legally contain an invisible lower-owner foreign prefix.

F705 SEPARATION
---------------
F705/KI#1026 is the opposite-direction population error: once `countBarbarians()` enters a
Barbarian-headed plot, `plotCount(PUF_isVisible, BARBARIAN_PLAYER)` counts loaded Barbarian LAND cargo
as though it were SEA fleet population. F706 applies when an invisible foreign unit sorts ahead of
the Barbarian units and causes the plot to be skipped/iteration to stop altogether. One inflates a
count after accepting a plot; the other hides valid Barbarian ships before the count/selection step.
They require different fixes and remain independent roots.

ANCESTRY / BASE CONTROL
-----------------------
Supplied Base AdvC 1.14 retains the same current helpers and behavior; current SAS Shelf executable
logic differs only cosmetically from Base, so F706 is inherited.

The first supplied public Shelf snapshot, practical 1253 ("Initial commit (cont.)"), already creates
all three faulty assumptions:
- `countBarbarians()` gates the plot on its head unit being Barbarian;
- `killBarb()` inspects only that head unit;
- `randomBarbCargoUnit()` scans plot units in order and breaks on the first non-Barbarian owner.
Practical 3180's "Refactoring in Shelf class" preserves the head gates and changes the accepted
`countBarbarians` plot from `getNumUnits()` to `plotCount(PUF_isVisible, BARBARIAN_PLAYER)`, but does
not repair the invisible-foreign-prefix problem. Because practical 1253 is the first supplied Shelf
history snapshot, do not invent a more precise pre-history birth date.

WHOLE-ALBUM / KNOWN-ISSUES DEDUP
--------------------------------
Whole-album searches for Shelf/headUnit/invisible-Barbarian coexistence found no prior owner for this
root. Neighboring findings remain separate:
- F705/KI#1026: loaded Barbarian cargo counted as ships.
- F643/KI#965: wrong parent-area context while selecting Shelf/cargo unit types.
- F632/KI#954: final Shelf-object ownership leak at CvMap teardown.
- KI#331: generic `canScrap` caller/callee contract; unrelated to Shelf's own crowd-culling helper.

REPAIR BOUNDARY
---------------
Do not use `headUnitNode()` or a `break` on the first foreign unit as a proof about the rest of a
plot's contents. Iterate the plot's units and explicitly select the property each caller needs:
- for sea-density counting, count Barbarian SEA units/ships only (also satisfying F705's domain fix);
- for culling, select a Barbarian combat SEA unit anywhere on the plot;
- for cargo loading, continue past non-Barbarian/invisible units and collect eligible Barbarian
  transports explicitly.

A shared explicit Barbarian-ship predicate/helper can repair F705 and F706 cleanly while preserving
the separate policy question of whether `CvTeam::isInContactWithBarbarians` wants ships only or all
relevant shelf Barbarians.

VALIDATION TARGETS
------------------
1. One Barbarian Galley alone: count 1; culling can select it; cargo loader can find it.
2. One foreign undetected Attack Submarine + one Barbarian Galley on the same shelf plot: Barbarian
   ship count remains 1; culling can still select the Galley; cargo loader can still find it.
3. Same fixture with two loaded Barbarian land passengers: sea count remains 1 (F705 + F706 joint
   regression target), while cargo units remain present and do not block selection.
4. A foreign *visible* hostile unit still cannot coexist through ordinary movement; no repair should
   weaken normal combat/coexistence rules.
5. Multiple shelf plots with and without invisible foreign units produce the same Barbarian ship
   accounting as an explicit full unit scan.

F706 / provisional KI#1027 is CONFIRMED. F707 / provisional KI#1028 is UNUSED.

QUEUE 131 CONTINUATION
----------------------
Finish the independent Shelf tail: raw `CvPlot*` storage lifetime/recompute versus F632; shelf
construction topology and PASSABLE/HABITABLE/adjacent-land assumptions versus F634/F636 and the
Ice/area findings; weighted-random nonnegative/zero-sum behavior and weight-map bounds; empty/
ownership/city/unit gates; remaining Shelf history strata; and whole-album dedup. Promote F707 only
for a distinct supported root rather than broadening F705/F706.

Exact continuation stone:
C031-WIP639 physical -> Queue131 OPEN -> F705-F706/KI#1026-KI#1027 CONFIRMED -> F707/KI#1028 UNUSED
-> finish topology/storage/random/history/dedup sweep -> either promote independent F707 or close
Queue131 cleanly -> freeze next WIP.

Files complete: 132 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP640 -- QUEUE 131 Shelf.cpp CLOSED / QUEUE 132 CvReplayMessage.cpp OPEN / F707 UNUSED
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP639. Gameplay/XML/Python
source remains untouched.

QUEUE 131 FINAL RESULT
----------------------
Queue131 `CvGameCoreDLL/Shelf.cpp` is CLOSED with two confirmed independent roots:
- F705/KI#1026: shelf fleet population counts loaded Barbarian LAND cargo as SEA ships;
- F706/KI#1027: head/prefix shortcuts miss Barbarian ships/transports when a lower-owner invisible
  foreign unit legally coexists on the shelf plot.
No independent F707 survives the remaining topology/storage/random/history saturation.

TOPOLOGY / CONSTRUCTION CONTROL
-------------------------------
`CvMap::computeShelves()` deletes/rebuilds the shelf map and then adds only plots that satisfy the
current intended shelf predicate:
- current shallow-water terrain (`GC.getWATER_TERRAIN(true)`);
- not plot-impassable;
- not a lake;
- habitable;
- adjacent to at least one land area.
Each shelf ID pairs one adjacent land-area ID with the water-area ID, so a shallow-water plot adjacent
to multiple continents intentionally belongs to multiple land shelves while retaining the same water
component identity. `getShelves(kArea, ...)` filters exactly by the land-area half of that ID.

The comment in `Shelf::randomPlot` that PASSABLE/ADJACENT_LAND/HABITABLE are pre-established by
`computeShelves` is therefore correct for a freshly built shelf. `randomBarbarianPlot` adds the
remaining dynamic gates (not visible to civs, unowned, civ-unit distance) before use.

REBUILD / RAW-POINTER LIFETIME
------------------------------
Shelf objects own only raw `CvPlot*` references into the map's stable plot array. Normal shelf
recomputation deletes/recreates Shelf objects, not plots. Current `getShelves()` consumers in CvGame
and CvTeam copy `Shelf*` values into function-local vectors and consume them synchronously; no current
consumer caches a Shelf pointer across a `computeShelves()` call. This agrees with the earlier Queue036
lifetime proof.

The final owned-Shelf deletion omission at CvMap teardown remains exactly F632/KI#954. Queue131 does
not add a second lifetime root.

FEATURE/PASSABILITY INVALIDATION DEDUP
--------------------------------------
A shelf can in principle become stale if a runtime feature mutation changes `p.isImpassable()` after
the last shelf build (Ice is the important current feature). That is not an independent Shelf root:
- F025/KI#346 already owns the supported dynamic `setFeatureType` passability/topology invalidation
  from global-warming Ice removal; Shelf membership is another derived topology consumer of the same
  missing refresh.
- F636/KI#958 owns the city-bearing WorldBuilder-scenario path that intentionally skips the safe
  post-feature Ice-aware water-area rebuild. Any shelf state derived before that feature topology is
  likewise downstream of the same scenario-load ordering root.
Do not spend F707 by renaming either already-owned invalidation family as a Shelf bug.

RANDOM / WEIGHT ARITHMETIC
--------------------------
Both weighted Shelf selections use current `BarbarianWeightMap` weights. Its probability weight is
built from the base positive weight and multiplied by `max(0, 100 - activity)`, so current weights are
never negative. `CvRandom::weightedChoice` explicitly returns NULL on total weight zero and safely
downscales totals above the RNG range. Shelf vectors and weight vectors are appended in lockstep.

`randomBarbarianTransport` separately computes `iTotalWeight` and returns NULL for nonpositive total
before its fractional probability math. Current per-plot weight bounds and shelf sizes keep this sum
far below signed-int overflow. No negative-weight, zero-range or vector-length root survives.

EMPTY / OWNERSHIP / CITY GATES
------------------------------
- Empty shelves return NULL/zero naturally.
- `randomPlot` rejects any occupied plot with `p.isUnit()` and honors the unowned/adjacent-unowned,
  not-visible-to-civ, city and civ-unit-distance restrictions requested by its caller.
- Sea cities are defensively rejected when RANDPLOT_NOT_CITY is requested even though current data
  has none.
- `countUnownedPlots` intentionally treats Barbarian-owned plots as owned; the surrounding spawn code
  comments on this land/shelf density policy explicitly.
No unsupported future sea-city or custom-weight behavior is promoted.

HISTORY SATURATION
------------------
The supplied Shelf history was read through every retained semantic stratum. The important changes
are:
- practical 1253: first supplied Shelf implementation; already contains the roots later normalized as
  F705/F706;
- 3180: refactors Shelf storage/traversal and changes the accepted-plot count from `getNumUnits()` to
  Barbarian-visible `plotCount`, preserving the faulty head/prefix assumption;
- 3204: introduces per-plot Barbarian weights for shelf placement;
- 3206: replaces the optional out-param local with `LOCAL_REF` only;
- 3208-3210: move/simplify synchronized weighted/random calls without changing eligibility;
- 3252: miscellaneous refactoring/comment changes;
- 3898 lineage: later Barbarian unit-selection/resource-context changes whose surviving wrong-area
  root is already F643/KI#965.
Header/include/enum-name/style/merge commits do not create another current Shelf semantic root.
Supplied Base AdvC 1.14 retains the same F705/F706 behavior.

QUEUE 131 is CLOSED with F705-F706 confirmed and no independent F707.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue132 `CvGameCoreDLL/CvReplayMessage.cpp` (127 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit replay-message value ownership/copying, plot/player/color/type sentinel semantics, time/turn
storage, serialization pairing and compatibility flags, construction/destruction/copy assignment,
all producers/consumers including SAS Timeline and already-owned replay visibility roots, Base/BtS
history and whole-album dedup. Promote only an independent current root rather than duplicating the
many message-audience/Timeline findings already established.

Exact continuation stone:
C031-WIP640 physical -> Queue131 CLOSED with F705-F706 / NO independent F707 -> 133 / 169 complete
-> Queue132 CvReplayMessage.cpp OPEN -> F707/KI#1028 UNUSED -> ownership/sentinel/serialization/
producer-consumer/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze
next WIP.

Files complete: 133 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP641 -- QUEUE 132 CvReplayMessage.cpp CLOSED / QUEUE 133 CyGlobalContextInterface1.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP640. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 132 RESULT
----------------
Queue132 `CvGameCoreDLL/CvReplayMessage.cpp` is CLOSED with no new finding.

VALUE OWNERSHIP / COPY CONTRACT
-------------------------------
`CvReplayMessage` owns only scalar enum/coordinate/turn fields plus a `CvWString` value. Construction
initializes type/player and the two invalid plot coordinates, defaults color to NO_COLOR, and the text
setter copies by value. The compiler copy constructor and the explicit assignment operator therefore
perform value-safe copies; self-assignment is harmless for these fields. No raw pointer, heap array or
borrowed external object is stored by the message.

The explicit assignment operator copies all seven message payload fields. No current source assigns one
`CvReplayMessage` object to another, so there is no hidden call site depending on additional SAS-only
state.

SAS HISTORICAL-AUDIENCE PARALLEL STATE -- CLEAN
-----------------------------------------------
Current SAS intentionally preserves the original `CvReplayMessage` payload/layout and stores KI#337's
historical audience in `CvGame::m_auiReplayMessageVisibility`, parallel to `m_listReplayMessages`.
The relevant lifecycle is lockstep:
- `CvGame::addReplayMessage` pushes one new message and one visibility mask together;
- `clearReplayMessageMap` deletes/clears messages and clears the mask vector;
- current-format CvGame write asserts equal sizes, writes every message, then every mask;
- current-format read reconstructs the message count and then resizes/reads exactly that many masks;
- `CvReplayInfo::createInfo` consults `isReplayMessageVisibleTo` before constructing a fresh replay
  message for the observer.

Replay/Hall-of-Fame export does not use `CvReplayMessage::operator=` and therefore cannot accidentally
lose an audience field that does not belong to this compatibility payload. It deliberately creates a
new message from turn/type/player and then copies color/text/plot only after the historical-audience
filter has passed.

SERIALIZATION PAIRING
---------------------
`CvReplayMessage::write` emits, in order:
1. turn,
2. replay-message type,
3. plot X,
4. plot Y,
5. player,
6. text,
7. color.
`read` consumes the same sequence, using temporary ints for enum values. On the supported VC7.1/Civ4
target these enums use the expected int representation. Current CvGame and CvReplayInfo writers/readers
therefore pair correctly.

The message-level reader does not independently bounds-check a corrupted replay type/player/color or
plot coordinate. That is malformed save/replay robustness, not a normal current-data activation, and
Queue049 already audited the outer replay-file validation/compatibility surface (F677/F678). Do not
manufacture F707 from arbitrary corrupted external bytes.

SENTINEL / CONSUMER CONTROL
---------------------------
NO_PLAYER, NO_COLOR and invalid plot coordinates are intentional message states. CvGame/CvReplayInfo
accessors return the same sentinels for invalid indexes, and higher-level Timeline/replay consumers use
message type/player/coordinates according to their visibility rules. The already-confirmed replay
privacy roots concern *which audience metadata was recorded* by particular producers, not storage
corruption in `CvReplayMessage` itself:
- F013/KI#334 Great-General killer identity visibility;
- F016/KI#337 vote-result replay audience (now repaired by the parallel visibility mechanism in the
  current source being audited);
- other Timeline subject/observer findings remain feature/caller roots.
No message-class defect duplicates them.

BASE / HISTORY CONTROL
----------------------
Current SAS `CvReplayMessage.cpp` is executable-identical to supplied Base AdvC 1.14; the apparent diff
is newline formatting only. Retained history is essentially inherited K-Mod/BtS code plus AdvC
include/traversal/style refactors; no semantic message-payload change survives here. The newer SAS
visibility work was intentionally implemented in CvGame/CvReplayInfo rather than changing this replay
payload so Hall-of-Fame/replay compatibility remains isolated.

QUEUE 132 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue133 `CvGameCoreDLL/CyGlobalContextInterface1.cpp` (124 queued/current
physical lines), OPEN under F707 / provisional KI#1028 unused.

Audit every Boost.Python global-context registration for exported-name->target/signature correctness,
return-value ownership/lifetime, default/help mismatch, enum/info-index assumptions and shipped Python
callers; cross-check the already-audited `CyGlobalContext.cpp` body and the other split interface files
(Queues059/124/154/156). Promote only an independent binding defect with supported activation.

Exact continuation stone:
C031-WIP641 physical -> Queue132 CLOSED / NO new F707 -> 134 / 169 complete -> Queue133
CyGlobalContextInterface1.cpp OPEN -> F707/KI#1028 UNUSED -> registration/ownership/signature/caller/
Base/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 134 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP642 -- QUEUE 133 CyGlobalContextInterface1.cpp CLOSED / QUEUE 134 CvInfo_Build.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP641. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 133 RESULT
----------------
Queue133 `CvGameCoreDLL/CyGlobalContextInterface1.cpp` is CLOSED with no new finding.

REGISTRATION CENSUS / NAME-TARGET CONTRACT
------------------------------------------
A mechanical census finds 69 active `.def` registrations in this split interface file. All 69 Python
export names are unique. Every export points to the intended `CyGlobalContext` member; the only
name->target spelling differences are deliberate established adapters:
- `getGame` -> `getCyGame`, `getMap` -> `getCyMap`, `getPlayer` -> `getCyPlayer`,
  `getActivePlayer` -> `getCyActivePlayer`, `getASyncRand` -> `getCyASyncRand`, and `getTeam` ->
  `getCyTeam`;
- public `getNumCivilizationInfos` binds the inherited C++ spelling `getNumCivilizatonInfos`.
No wrong target, swapped same-signature target, accidental duplicate or overload ambiguity survives.

RETURN OWNERSHIP / LIFETIME
---------------------------
Every pointer/reference-returning wrapper in this file uses `reference_existing_object` where the
underlying object is owned elsewhere:
- game/map/player/team wrappers are process-static `Cy*` wrappers owned by `CyGlobalContext` helpers;
- `CvRandom&` is the global asynchronous RNG;
- all `Cv*Info` pointers refer to GC-owned XML info storage.
Thus this file does not repeat the inverse-ownership bugs found in other interface queues (e.g. F688).

The SAS-only `getAudio3DScriptName` returns `const char*` through Boost.Python's ordinary string
converter, not as a borrowed pointer object. Queue059 already proved that its source pointer belongs to
a process-lifetime static vector that stops mutating after its one-time load, and the shipped Sevopedia
caller copies the result into Python text. No interface-level lifetime defect is added here.

CURRENT VS BASE
---------------
After newline normalization, removing only the KI#141 audio reverse-lookup registration and its custom
comments makes current Queue133 byte-for-byte identical to supplied Base AdvC 1.14. The other 68
registrations are therefore inherited. The SAS addition binds the already-audited Queue059 helper with
its exact one-int signature.

QUEUE059 IMPLEMENTATION DEDUP / REFINEMENT
------------------------------------------
The two strongest inherited implementation anomalies were already audited in Queue059 and this binding
file merely exposes them; neither becomes an independent Queue133 root.

1. `getBonusClassInfo(0)` off-by-one:
   the native wrapper uses `i > 0` and therefore rejects valid slot 0 (`BONUSCLASS_GENERAL`). Current
   shipped PerfectMongoose really does call this API, but Queue059 traced current slot-0 data:
   `BONUSCLASS_GENERAL` has `iUnique=0`. The two PerfectMongoose uses either skip the class-info range or
   return zero when the wrapper gives None; a correct slot-0 object would supply the same zero range.
   The public API is wrong, but current shipped map-generation behavior is unchanged. Do not renumber it
   as F707 merely because Queue133 registers the method.

2. negative player/team wrapper indexes:
   `getCyTeam` lacks a lower-bound check, so direct third-party `gc.getTeam(-1)`/other negative values can
   index before its static array. One precision correction to the older Queue059 prose: `getCyPlayer`
   also lacks a general `idx >= 0` check in release behavior; it explicitly excludes NO_PLAYER (-1) but
   an exotic negative such as -2 would still index before `cyPlayers`. Its debug `FAssert(idx>=0)` is
   not a release guard. The shipped Python census nevertheless supplies valid player/team IDs or bounded
   loops and establishes no negative-below-sentinel producer. Both remain extension-API hardening, not a
   supported current defect.

INFO-INDEX / ENUM CONTRACT
--------------------------
The remaining registered info getters were reconciled with their Queue059 implementations. Ordinary
info accessors guard their valid domains or are fed from paired count loops/current validated type IDs;
no new sentinel producer appears merely through this binding layer. The `reference_existing_object`
policy is appropriate because Python does not own GC info records and normal gameplay does not destroy
and recreate those records while retained Python objects remain live.

SPLIT-INTERFACE CONTROL
-----------------------
The four `CyGlobalContextInterface*` files were mechanically enumerated to ensure Queue133 did not
silently duplicate one of its 69 export names in another split file. No Queue133 name is duplicated in
Interfaces 2-4. Interface4 has its own duplicate VoteSource registrations, but Queue124 already completed
that file; this Queue133 closure does not reopen or renumber another queue's surface.

QUEUE 133 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue134 `CvGameCoreDLL/CvInfo_Build.cpp` (101 queued lines), OPEN under
F707 / provisional KI#1028 unused.

Audit the compact build-info family end-to-end: constructor defaults, owned feature-tech array,
XML read cursor/count contracts, mission/action linkage, kill/build-time/cost semantics, Python/info
consumers, current data, current-vs-Base/history and whole-album dedup. Promote only a supported
independent root.

Exact continuation stone:
C031-WIP642 physical -> Queue133 CLOSED / NO new F707 -> 135 / 169 complete -> Queue134 CvInfo_Build.cpp
OPEN -> F707/KI#1028 UNUSED -> ownership/default/XML/data/caller/Base/history/dedup sweep -> either
promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 135 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP643 -- QUEUE 134 CvInfo_Build.cpp CLOSED / QUEUE 135 TSCProfiler.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP642. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 134 RESULT
----------------
Queue134 `CvGameCoreDLL/CvInfo_Build.cpp` is CLOSED with no new finding.

CURRENT / BASE DELTA
--------------------
After newline/comment normalization, the only executable SAS-vs-Base AdvC 1.14 delta in this compact
info class is the existing KI#71 repair in `isFeatureRemove`: return false for `NO_FEATURE` before the
typed enum-bound assertion/index. The constructor, destructor, scalar getters/setter and XML reader are
otherwise inherited. This queue therefore tests whether that repair is incomplete or whether an
independent inherited info/data defect survives; neither was established.

OWNERSHIP / INITIALIZATION
--------------------------
Construction initializes every scalar to a valid neutral/sentinel value and all four per-feature raw
arrays to NULL. Destruction releases each array exactly once with `SAFE_DELETE_ARRAY`. Current code does
not copy/assign `CvBuildInfo` values; GC owns the allocated info objects for their process/load lifetime,
so the raw-array class does not enter a live Rule-of-Three path.

`SetFeatureStruct` allocates all four arrays at `GC.getNumFeatureInfos()` with neutral defaults:
NO_TECH/-1 for feature tech, zero for time/production, false for removal. Thus omitted feature rows are
well-defined. Queue028 already audited this parser's current-data key validation and the malformed-XML
trust-after-error branch; no separate Queue134 parser root is created here.

CURRENT BUILD / FEATURE DATA
----------------------------
The current XML fixture contains 22 BuildInfo records and 35 FeatureStruct records across 15 builds.
Mechanical current-data validation found:
- every FeatureStruct FeatureType resolves to one of the six current FeatureInfos;
- every FeatureStruct PrereqTech resolves or uses NONE;
- every BuildInfo PrereqTech, ImprovementType and RouteType resolves or uses NONE;
- no build contains a duplicate FeatureType row that would silently overwrite an earlier row.
The XML therefore never activates SetFeatureStruct's invalid-index error branch.

FEATURE SENTINEL CONTRACT / KI#71 CONTROL
-----------------------------------------
The existing SAS `isFeatureRemove(NO_FEATURE) -> false` guard is appropriate because this predicate is
intentionally called from several broad plot/UI/AI paths where a plot may have no feature. The other
three array getters (`getFeatureTech`, `getFeatureTime`, `getFeatureProduction`) retain the stronger
real-FeatureTypes precondition.

A complete current native caller sweep shows those three getters are called only when `isFeature()` is
already true, with an explicit `eFeature != NO_FEATURE`/ternary guard, or inside bounded FeatureTypes
enum loops. The shipped Python tech/pedia callers likewise loop `0 .. getNumFeatureInfos()-1`; the
already-audited PerfectMongoose path guards NO_FEATURE. Therefore KI#71 does not reveal a sibling
current OOB/assert root in the other getters.

BUILD MISSION / ACTION LIFECYCLE
--------------------------------
`m_iMissionType` intentionally starts at NO_MISSION and is not an XML field. During global ActionInfo
construction, every ACTIONSUBTYPE_BUILD receives `MISSION_BUILD` through `setMissionType` together with
its ActionInfo index. This is the same post-load synthesis model already seen for specialists,
religions, corporations and buildings; there is no missing Build XML value.

SCALAR / GAMEPLAY CONSUMERS
---------------------------
Current `iTime`, `iCost`, `bKill`, prerequisite, improvement, route and entity-event values are ordinary
BuildInfo data consumed through already-audited plot/player/unit/action paths. Current BuildInfos use
nonnegative time/cost values and valid referenced types. Build-time calculations apply game speed,
handicap, terrain and feature modifiers outside this info class; no divisor or overflow contract is
introduced by these plain stored scalars.

PYTHON / INFO BINDING CONTROL
-----------------------------
Queue071 already audited the actual BuildInfo Boost.Python bindings, including the typed `py_` feature
adapters introduced by AdvC practical 1922. Queue133 independently rechecked the global
`getBuildInfo` owner policy. Current Python consumers obtain Build IDs from `getNumBuildInfos`, action
mission data, or validated pedia records. No new lifetime/index defect originates in this data class.

HISTORY / DEDUP
---------------
Relevant history reconciles as type-safety/organization refactors rather than surviving bugs:
- practical 1918 typed the feature getters;
- practical 2425 moved their assertions to enum-aware bounds;
- practical 1752 normalized the entity-event sentinel;
- later XML helper refactors replaced repeated string lookup boilerplate without changing the data;
- SAS practical 5091 introduced the live KI#71 NO_FEATURE guard.
Queue028 already owns the generic SetFeatureStruct malformed-key hardening note, and KI#71 owns the
specific historical no-feature assertion. No independent F707 is justified.

QUEUE 134 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue135 `CvGameCoreDLL/TSCProfiler.cpp` (96 queued lines), OPEN under F707 /
provisional KI#1028 unused.

Audit profiler initialization/calibration, RDTSC/CPU-frequency arithmetic, nesting/sample ownership,
thread assumptions, zero/overflow/division behavior, output formatting and compile/runtime activation;
compare Base/K-Mod/Civ4CE/Taurus/history and dedup against Queue083 FProfiler before promoting any
profiling-only root.

Exact continuation stone:
C031-WIP643 physical -> Queue134 CLOSED / NO new F707 -> 136 / 169 complete -> Queue135 TSCProfiler.cpp
OPEN -> F707/KI#1028 UNUSED -> timing/arithmetic/lifecycle/thread/output/Base/history/dedup sweep ->
either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 136 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP644 -- QUEUE 135 TSCProfiler.cpp CLOSED / QUEUE 136 CyArgsList.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP643. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 135 RESULT
----------------
Queue135 `CvGameCoreDLL/TSCProfiler.cpp` is CLOSED with no new finding.

PROVENANCE / ACTIVATION
-----------------------
Current SAS and supplied Base AdvC 1.14 are executable-identical after newline normalization. This is
the AdvC `advc.003o` Time Stamp Counter profiler adopted from We the People, not an SAS gameplay
addition.

The code can be compiled through `USE_TSC_PROFILER` (the alternate VS/code-editor project currently
contains that define; the normal Makefile.project line is commented), but compilation alone does not
collect samples. Every active `TSC_PROFILE` call in the current tree is inside `AgentIteratorTest.cpp`
under the separately disabled `#define AGENT_ITERATOR_TEST`; the ScaledNumTest profile calls are
commented out. Therefore the current ordinary game has an empty sample map and `writeFile()` returns
without output even in a build that happens to compile the profiler.

SAMPLE OWNERSHIP / NAME LIFETIME
--------------------------------
When the opt-in AgentIterator speed test is explicitly enabled, every current `TSC_PROFILE` argument is
a string literal. `TSCSample` stores that stable pointer only for lexical-scope duration, records its
start counter, and the destructor immediately copies the name into the profiler's `std::string` map
key. No current temporary/dangling name producer exists.

Each newly inserted sample starts as `(count=1, total=iTime)`. Existing entries only increment count,
so `writeFile`'s `iTime / iCount` cannot divide by zero for a map entry produced through `addSample`.
64-bit call/time counters require unrealistic profiling durations to wrap and do not feed gameplay.

RDTSC / THREADING LIMITATIONS
-----------------------------
The naked VC7.1/x86 helper returns EDX:EAX from `rdtsc` as intended for the project's 32-bit target. It
does not serialize surrounding instructions, normalize for core migration, or compensate for
non-invariant timestamp counters; comments in the speed-test caller already acknowledge measurement
inaccuracy. Those conditions can make *profiling numbers* noisy or, on old unsynchronized CPUs,
apparently huge after unsigned subtraction. No synchronized/game state depends on those measurements.

`addSample` also explicitly documents that its `stdext::hash_map` is not thread-safe. No current active
TSC sample site runs on a worker thread (indeed no current sample site is enabled at all). Treat future
multithread profiling as instrumentation hardening rather than a present defect.

OUTPUT / SHUTDOWN ORDER
-----------------------
`CvGlobals::uninit()` calls `TSCProfiler::getInstance().writeFile()` before deleting the game/map/random/
logger state and before `gDLL->uninitGlobals`. Thus an enabled nonempty profiler still has the DLL
logging interface when it writes `TSCProfile.log`. The temporary `out.str().c_str()` remains alive for
the full `logMsg` call expression, matching the temporary-string lifetime control already audited in
other logger queues.

No sample reset is performed after writing; current lifecycle invokes the global shutdown write once.
Repeated manual writes would repeat accumulated diagnostics, not mutate gameplay.

DEDUP / FPROFILER CONTROL
-------------------------
Queue083 `FProfiler.cpp` already established the same threshold for opt-in profiling infrastructure:
profiler timing/log artifacts that do not feed synchronized gameplay are not promoted merely because a
debug/code-editor configuration can compile them. Queue135 adds no memory-safety or supported lifecycle
failure that would justify an independent F707.

QUEUE 135 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue136 `CvGameCoreDLL/CyArgsList.cpp` (95 queued lines), OPEN under F707 /
provisional KI#1028 unused.

Audit Python-argument packing ownership/refcounts, Unicode/narrow string conversions, `FVariable`
lifetime semantics, copy/destructor behavior, tuple creation/error paths, scalar-width conversions,
all shipped C++ producers and current-vs-Base/Civ4CE/Taurus history. Cross-check Queue106's FVariable
ownership analysis and Queue047 Python-event call paths before promoting any root.

Exact continuation stone:
C031-WIP644 physical -> Queue135 CLOSED / NO new F707 -> 137 / 169 complete -> Queue136 CyArgsList.cpp
OPEN -> F707/KI#1028 UNUSED -> Python-arg ownership/conversion/FVariable/producer/Base/history/dedup sweep
-> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 137 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP645 -- QUEUE 136 CyArgsList.cpp CLOSED / QUEUE 137 CyMapInterface.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP644. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 136 RESULT
----------------
Queue136 `CvGameCoreDLL/CyArgsList.cpp` is CLOSED with no new finding.

CURRENT / BASE / CONTRACT BASELINE
----------------------------------
Current SAS `CyArgsList.cpp` is executable-identical to supplied Base AdvC 1.14 after newline
normalization. It is the inherited Firaxis/K-Mod/AdvC argument-packaging bridge. Queue047's complete
`CvDllPythonEvents` pass and Queue037's `CvPythonCaller` pass already exercised the current producer
surface; Queue136 rechecks the container/ownership side rather than inventing a new Python ABI model.

REFERENCE-OWNERSHIP MODEL
-------------------------
Scalar/string/list overloads create new Python references (`PyInt_FromLong`, `PyFloat_FromDouble`,
`PyString_*`, `PyUnicode_*`, `PyList_New`). List elements are new refs and `PyList_SetItem` explicitly
steals each element reference. The completed list object is then one argument reference.

`CyArgsList` itself has no destructor/DECREF loop because `makeFunctionArgs` hands the accumulated
objects to the engine-side `MakeFunctionArgs` tuple builder. The exact steal/incref details of that
closed-source interface are not present in the SDK, but this is the inherited engine contract used by
all native event/Python-call paths. The current tree has no demonstrated leak/double-free/dangling tuple
symptom attributable to that contract. Missing opaque implementation detail is not evidence for F707.

`add(void*)` deliberately accepts an already-created Python object. Current uses are `makePythonObject`
results and the nested generic-event tuple produced by another `makeFunctionArgs`; Queue047 already
traced those lifetimes, including CombatDetails copying and nested generic events. No Queue136-local
ownership contradiction was established.

FIXED CAPACITY
--------------
`push_back` has a debug assertion but no release guard for the fixed `MAX_CY_ARGS=20`; an external/future
caller could overflow the raw array after 20 entries. Current producers were mechanically recounted:
`CvDllPythonEvents::reportMouseEvent` is the largest packet at 8 event entries plus the six context
flags appended by `postEvent` = 14 total. `reportKbdEvent` is 13 and all others are lower. Direct
`CvPythonCaller`/generic-event packets likewise remain below 20. Queue047 independently reached the same
maximum. This is future-API hardening, not a current overflow.

CLEAR / REUSE CONTROL
---------------------
`clear()` only resets `m_iCnt`; if called after new Python references had been added but before the
MakeFunctionArgs handoff, those refs would be abandoned. A whole current-tree search finds no call to
`CyArgsList::clear()` or an instance `.clear()` on any argument list. All current objects are local,
filled once, handed off once and destroyed as empty C++ shells. No current leak path results.

LIST LENGTH / TEMPORARY C++ BUFFERS
-----------------------------------
The live list overloads are bounded by concrete producers:
- mouse-event screen IDs come from a `std::vector<int>`; empty vectors pass NULL with length 0, which
  never dereferences the pointer, and ordinary vector size is far below Python/int limits;
- selection-group mission IDs allocate exactly `getNumUnits()` ints, traverse the same group
  synchronously with no intervening callback, convert all IDs into a fresh Python list, and then
  `delete[]` the C++ buffer (the old scalar-delete problem is already fixed in current K-Mod/AdvC).
No negative current length producer exists for byte/int/float list overloads.

STRING / WIDTH CONTROLS
-----------------------
The wchar overload explicitly maps NULL to an empty Unicode string; current wide-string producers are
valid null-terminated `CvWString`/literal data. Narrow string producers likewise provide real
null-terminated strings. `add(uint)` intentionally funnels through the historical signed-int Python
representation; no current caller supplies a uint value beyond signed-int range. Current build is
32-bit and ordinary enum/ID/turn values fit PyInt's long representation.

ERROR-PATH HARDENING
--------------------
Python allocation failure is guarded only by debug assertions in the list overloads; a genuine
out-of-memory failure could then pass NULL into `PyList_SetItem`/the argument array. This is inherited
catastrophic-OOM robustness, not a supported deterministic game-state defect. No F707 is promoted.

DEDUP / CLOSURE
---------------
Queue047 already established the current packet arities, temporary-array lifecycle and nested generic
ownership; Queue037 established the principal `CvPythonCaller` argument paths. Queue136 adds no
independent current root to those completed audits.

QUEUE 136 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue137 `CvGameCoreDLL/CyMapInterface.cpp` (94 queued lines), OPEN under
F707 / provisional KI#1028 unused.

Audit all Boost.Python map registrations against the completed Queue087 `CyMap.cpp` and Queue036
`CvMap.cpp` contracts: name->target/signature correctness, ownership policies, static-wrapper aliasing,
plot/index/sentinel assumptions, rebuild/mutation exposure, help-string drift, current-vs-Base/history
and shipped Python callers. Promote only an independent registration-layer root.

Exact continuation stone:
C031-WIP645 physical -> Queue136 CLOSED / NO new F707 -> 138 / 169 complete -> Queue137 CyMapInterface.cpp
OPEN -> F707/KI#1028 UNUSED -> registration/ownership/index/rebuild/caller/Base/history/dedup sweep ->
either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 138 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP646 -- QUEUE 137 CyMapInterface.cpp CLOSED / QUEUE 139 CyPlayerInterface2.cpp OPEN
===============================================================================
Source identity: same reconciled AdvCiv-SAS light-source snapshot as C031-WIP645. Gameplay/XML/Python
source remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 137 RESULT
----------------
Queue137 `CvGameCoreDLL/CyMapInterface.cpp` is CLOSED with no new finding.

CURRENT / BASE / REGISTRATION CENSUS
------------------------------------
Current SAS is executable-identical to supplied Base AdvC 1.14 after newline normalization. A
mechanical census finds 62 active `.def` registrations, all 62 with unique Python names and every name
pointing to the same-named `CyMap` target. No copy/paste misbinding, duplicate export or SAS-only
registration drift survives.

OWNERSHIP POLICY PARITY
-----------------------
The binding policies exactly match the already-completed Queue087 wrapper implementation:
- `syncRandPlot`, `findCity`, `findSelectionGroup`, `findBiggestArea`, `plotByIndex`, `plot`,
  `pointToPlot` and `getArea` allocate fresh Cy wrapper objects and use `manage_new_object`;
- `sPlotByIndex` and `sPlot` reuse one process-static `CyPlot` each and correctly use
  `reference_existing_object` rather than asking Python to delete static storage.
No inverse ownership root like F688 is present.

STATIC-PLOT ALIAS DEDUP
-----------------------
The static wrappers remain intentionally sharp: a subsequent `sPlot` call retargets the same Python-
visible object. Queue087 and the older KI#257 investigation already established this mechanism and the
former Planet Generator activation; current Planet uses an independently owned `map.plot` wrapper and
the remaining shipped static-plot caller census does not retain two simultaneous aliases. Queue137
only registers the same mechanism and does not renumber it as F707.

LIVE SIGNATURE / HELP CONTROLS
------------------------------
Several help strings are stale or incomplete, but Boost.Python binds the actual C++ signatures:
- `findBiggestArea` help claims `()` although the wrapper requires `bool bWater`; all shipped callers
  explicitly pass True/False;
- `getNumBonuses` / `getNumBonusesOnLand` help omits the required BonusTypes argument; shipped map
  generators pass a concrete bonus ID;
- `findCity` help describes C++-style default arguments including NULL `pSkipCity`, but no such Python
  defaults are declared and the wrapper itself assumes a real `CyCity*`; there is no shipped caller;
- `recalculateAreas` mentions `CyMap.setPlotTypes`, although plot-type bulk setting belongs to the map-
  generator interface. This is documentation drift only.
No current caller arity failure results from those strings.

POINTER / INDEX CONTRACT DEDUP
------------------------------
Queue087 already challenged the NULL-wrapper and index surfaces. `findWater`, `findCity`,
`updateMinOriginalStartDist` and the Cy-level `calculatePathDistance` expect non-NULL wrappers before
reaching native code; shipped path-distance map scripts supply valid map plots and the other sharp
helpers have no invalid shipped producer. `plotByIndex` safely wraps a NULL native plot for bad index;
ordinary coordinate/index helpers retain the native initialized-map preconditions audited in Queue036.

AREA / MAP MUTATION CONTROL
---------------------------
Shipped `recalculateAreas()` callers are map-generation/WorldBuilder code and reacquire area wrappers
after topology changes where needed. Queue087/Queue036 already audited those rebuild lifecycles. Plot-
extra-yield/cost and minimap setters similarly delegate to already-audited native methods; the binding
layer adds no altered ordering or lifetime.

HISTORY / DEDUP
---------------
The retained K-Mod/AdvC interface history consists of API movement/type refactors and the static-wrapper
optimization already owned by KI#257. Current source contains no newer Base repair absent from SAS and
no independent Queue137 registration root reaches the finding threshold.

QUEUE 137 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Queue138 `CvDLLInterfaceIFaceBase.cpp` was already completed at C031-WIP507. Ordinary order therefore
advances to Queue139 `CvGameCoreDLL/CyPlayerInterface2.cpp` (90 queued lines), OPEN under F707 /
provisional KI#1028 unused.

Audit this second CyPlayer binding tranche against completed Queue021 `CyPlayer.cpp` and the other
CyPlayer interface split: exported-name->target/signature parity, ownership, unit/city/plot return
policies, enum/sentinel adapters, stale help, SAS additions, shipped Python callers, Base/history and
whole-album dedup. Promote only an independent supported binding defect.

Exact continuation stone:
C031-WIP646 physical -> Queue137 CLOSED / NO new F707 -> Queue138 already COMPLETE -> 139 / 169 complete
-> Queue139 CyPlayerInterface2.cpp OPEN -> F707/KI#1028 UNUSED -> registration/ownership/signature/
caller/Base/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 139 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP647 -- QUEUE 139 CyPlayerInterface2.cpp CLOSED / QUEUE 140 CvCivilization.cpp OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP646, with supplied
Base AdvC 1.14 as the direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 139 RESULT
----------------
Queue139 `CvGameCoreDLL/CyPlayerInterface2.cpp` is CLOSED with no new finding.

CURRENT / BASE / CROSS-SPLIT REGISTRATION CENSUS
-------------------------------------------------
Current Queue139 is executable-identical to supplied Base AdvC 1.14 after newline normalization.
The file contains 48 active `.def` registrations, all with unique Python names and every name
pointing to the same-named `CyPlayer` wrapper target. Mechanical reconciliation across both current
CyPlayer interface files finds 436 active bindings total (388 + 48) and no cross-file duplicate
Python method name. Queue021 already reconciled those complete bindings against `CyPlayer.cpp/.h`;
Queue139 adds no registration-only contradiction.

EVENTTRIGGEREDDATA OWNERSHIP / INVALIDATION
-------------------------------------------
`getEventOccured`, `getEventTriggered` and `initTriggeredData` expose borrowed `EventTriggeredData`
records and therefore use `reference_existing_object`. That matches native ownership:
- `getEventOccured` returns the address of a value owned by `CvPlayer::m_mapEventsOccured`;
- `getEventTriggered` returns an object stored in the player's `m_eventsTriggered` free-list;
- `initTriggeredData` creates/populates a record in that same player-owned store.
Python must not retain these proxies across native erase/removal, but the shipped Random Event caller
census uses them synchronously inside the callback. The one representative branch that may call
`resetEventOccured` reads the prerequisite object's coordinates first and only resets after it no
longer accesses that borrowed proxy. Other callers immediately read fields or mutate the currently
triggered record and return. No shipped dangling-proxy continuation was established.

This is consistent with Queue119's earlier registration/lifetime note: EventTriggeredData records are
borrowed live-player storage, not fresh heap wrappers requiring `manage_new_object`. Do not invert the
policy merely because reset/delete can later invalidate an old external reference.

AREA-POINTER AI WRAPPERS
------------------------
`AI_unitValue`, `AI_totalAreaUnitAIs` and `AI_totalWaterAreaUnitAIs` inherit CyPlayer's raw `CyArea*`
precondition rather than locally accepting Python None. Current shipped Python callers in
CvRandomEventInterface/CvAdvisorUtils pass `plot.area()` or `pCity.area()` from live concrete map
objects. Queue021 already audited this family and found no null shipped caller; Queue139's binding
layer adds no changed ownership or nullable default.

ENUM / ID / HISTORY / MISC CONTRACTS
------------------------------------
The event/AI PlayerTypes, UnitAI, Civic, Memory and Corporation arguments are ordinary integer/enum
bridges matching the underlying wrapper signatures. Score/economy/industry/agriculture/power/culture/
espionage history methods delegate to the safe-history wrapper paths already audited in Queue021.
`canSplitArea`, empire splitting, forcePeace, script data, chooseTech, scoreboard-expanded state and
random civ/leader provenance likewise expose the already-audited CyPlayer methods without overload or
return-policy changes.

The `PollutionTypes` Python enum deliberately maps AdvC's nested `CvPlayer::POLLUTION_*` flags under
the retained old Python name; this is documented compatibility behavior and was already reconciled in
Queue021. No duplicate global PollutionTypes registration remains in the current enum interface.

HELP / ROBUSTNESS NOTES
-----------------------
Some help strings use old int-style enum notation and are less precise than the modern native types;
Boost.Python binds the actual member-function pointer and does not derive arity/type behavior from
those strings. Arbitrary extension code can also retain borrowed EventTriggeredData beyond native
removal or pass None to raw area-pointer wrappers. Those are API-lifetime/input hardening surfaces,
not supported-current Queue139 defects.

HISTORY / DEDUP / VERDICT
-------------------------
Current-vs-Base equality plus the completed Queue021 implementation audit and Queue073 first-interface
audit account for the relevant K-Mod/AdvC evolution. No independent current-live registration root
survives whole-album dedup.

Queue139 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue140 `CvGameCoreDLL/CvCivilization.cpp` (89 queued lines), OPEN under
F707 / provisional KI#1028 unused.

Audit the lightweight civilization helper as a native ownership/default-map/type-resolution boundary:
constructor/destructor state, civilization-info lookup, unit/building class remaps, fallback/sentinel
behavior, current callers, Base/history and whole-album dedup. Promote only an independent supported
current root.

Exact continuation stone:
C031-WIP647 physical -> Queue139 CLOSED / NO new F707 -> 140 / 169 complete -> Queue140
CvCivilization.cpp OPEN -> F707/KI#1028 UNUSED -> native helper ownership/class-map/sentinel/caller/
Base/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 140 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP648 -- QUEUE 140 CvCivilization.cpp CLOSED / QUEUE 141 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP647, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 140 RESULT
----------------
Queue140 `CvGameCoreDLL/CvCivilization.cpp` is CLOSED with no new finding.

CURRENT / BASE / OWNERSHIP BASELINE
------------------------------------
Current SAS `CvCivilization.cpp` and its header are executable-semantically identical to supplied
Base AdvC 1.14; the visible diff is newline/file-format representation only. The class is AdvC's
`advc.003w` value/cache wrapper around one `CvCivilizationInfo const&`. `CvPlayer::setCivilization`
owns the wrapper pointer, deletes the old wrapper before replacement and constructs the new one from
`GC.getInfo(eCivilization)` only for a real civilization. `CvPlayer` destruction/reset releases the
wrapper. The referenced CivilizationInfo objects are global XML infos whose lifetime encompasses the
player wrapper. `boost::noncopyable` prevents accidental copying of the reference-bearing cache.

CLASS-MAP CACHE CONSTRUCTION
----------------------------
Construction iterates every current BuildingClass and UnitClass exactly once, resolves the
civilization-specific mapping through `CvCivilizationInfo`, omits deliberate `NO_BUILDING`/`NO_UNIT`
class-disabling mappings, and stores only real concrete types. Unique-unit/building vectors compare
the resolved type against the corresponding class default and therefore correctly include a civ's
replacement while excluding ordinary defaults.

Queue034's completed `CvInfo_Civilization.cpp` audit already established the underlying current-data
contracts used here: all current class keys resolve, there are no duplicate class keys within a
civilization, deliberate Barbarian NONE targets are intentional disabling mappings, FreeUnitClasses /
FreeBuildingClasses / InitialCivics references resolve, and all 38 current civilization records have
five InitialCivics in CivicOption order. Queue140 adds no contradictory cache-layer behavior.

SENTINEL / CONVENIENCE-OVERLOAD CONTROL
---------------------------------------
The static `buildingClass(NO_BUILDING)` and `unitClass(NO_UNIT)` helpers deliberately return
`NO_BUILDINGCLASS` / `NO_UNITCLASS`. The convenience overloads `isFreeBuilding(BuildingTypes)` and
`getNumFreeUnits(UnitTypes)` are not themselves sentinel-safe if a caller passes NO_BUILDING/NO_UNIT:
they would forward a -1 class index to `CvCivilizationInfo`'s raw array getter after only a debug
`FAssertBounds`. This is real API robustness debt, but it has no current activation:
- the sole `isFreeBuilding(BuildingTypes)` caller iterates `kCiv.buildingAt(i)` over the wrapper's
  concrete-building vector, which by construction contains no NO_BUILDING;
- the sole `getNumFreeUnits(UnitTypes)` caller likewise iterates `kCiv.unitAt(i)`, whose vector contains
  no NO_UNIT;
- direct class-based uses originate from bounded enum loops or known concrete type mappings.
Do not spend F707 on an unsupported sentinel call.

INDEX / VECTOR CONTRACTS
------------------------
`buildingAt`, `unitAt`, `uniqueBuildingAt` and `uniqueUnitAt` use unchecked `std::vector::operator[]`,
but every current caller bounds iteration with the matching `getNum*` accessor. The wrapper is not
exposed directly to Python. No mismatched count/vector or stale-index path was found.

CIVILIZATION REPLACEMENT / CACHE FRESHNESS
------------------------------------------
`CvInitCore::setCiv` calls `CvPlayer::setCivilization` only when the civilization enum actually
changes, deliberately avoiding needless cache reconstruction during reload. When it changes, the old
wrapper is deleted and the entire building/unit mapping cache is rebuilt from the new CivilizationInfo.
Current XML infos and class counts are static after ordinary game initialization; no supported runtime
XML/class mutation can make the cached vectors stale.

FREE-BUILDING / FREE-UNIT SEMANTICS
-----------------------------------
The free-building and free-unit helpers delegate directly to the civilization info's class-indexed
arrays. Current live callers use them only with the wrapper's own concrete class mappings. The info
loader's absent-array fallback is not reached in current civilization data; Queue034 already checked
the present FreeUnitClasses/FreeBuildingClasses data. No count-vs-enum semantic mismatch survives.

HISTORY / DEDUP / VERDICT
-------------------------
The class is an AdvC `advc.003w` refactor/cache abstraction rather than an SAS-specific subsystem.
Current SAS carries no semantic delta from Base AdvC 1.14 in this file. Neighboring civilization XML,
leader/default, class-map and API issues were already saturated by Queue034; no independent current
root appears here.

Queue140 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue141 `CvGameCoreDLL/CvDLLEntity.cpp` (84 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit the small EXE-facing entity wrapper for native pointer ownership/lifetime, ID/entity identity,
setup/destroy ordering, graphical-reset and NULL contracts, all callers, Base/BtS history and whole-
album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP648 physical -> Queue140 CLOSED / NO new F707 -> 140 / 169 complete -> Queue141
CvDLLEntity.cpp OPEN -> F707/KI#1028 UNUSED -> ownership/entity-ID/setup-destroy/graphics/caller/
Base/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 140 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP649 -- QUEUE 141 CvDLLEntity.cpp CLOSED / QUEUE 142 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP648, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 141 RESULT
----------------
Queue141 `CvGameCoreDLL/CvDLLEntity.cpp` is CLOSED with no new finding.

BASE / ABI / OWNERSHIP CONTRACT
-------------------------------
Current SAS is executable-identical to supplied Base AdvC 1.14 after line-format normalization.
`CvDLLEntity` is a noncopyable thin DLL-side handle around an EXE-owned `CvEntity*`. It does not own
an independently allocated C++ entity object itself; creation/destruction are routed through
`CvDLLEntityIFaceBase`.

The important teardown contract is stronger than the source line can first appear:
`CvDLLEntityIFaceBase::destroyEntity(CvEntity*& ...)` takes the pointer by reference. Therefore
`CvDLLEntity::destroyEntity()` passes its actual `m_pEntity` slot to the EXE so the engine can destroy
and detach/null that handle atomically. The wrapper does not retain an intentionally dangling entity
pointer after a normal engine destruction.

CITY / UNIT LIFECYCLE RECONCILIATION
------------------------------------
`CvCity` and `CvUnit` use the wrapper consistently:
- graphical setup returns before touching the EXE unless graphics are initialized;
- normal init/finalize creates/attaches the engine entity and then invokes common `setup()`;
- reload removes/destroys the old entity, recreates it and re-runs graphical setup;
- city destruction removes then destroys its entity;
- unit teardown removes the unit from battle and removes its entity while the app/graphics are live,
  but intentionally skips engine removal during final app shutdown before still calling the EXE's
  safe destroy/detach path.
This is the already-established Civ4 lifecycle pattern, not an independent Queue141 ownership root.

NULL / PRE-SETUP CALL SURFACE
-----------------------------
The forwarding helpers (`setup`, visibility, selection, animation, movement, siege tower, mission
notification) do not add NULL guards around the engine handle. A custom caller invoking them before
entity creation could therefore rely on undocumented EXE NULL tolerance. Current City/Unit callers,
however, reach these operations only on graphics-initialized objects whose entity has been created;
existing direct `getEntity()` callers likewise sit in graphical/UI paths. No supported pre-entity
movement/animation call was established.

TYPE / REINTERPRET CONTROL
--------------------------
`CvDLLCityEntity` and `CvDLLUnitEntity` reinterpret the opaque generic entity pointer because the
derived EXE classes are intentionally incomplete to the DLL. Each typed pointer is created through
the matching `createCityEntity(CvCity*)` / `createUnitEntity(CvUnit*)` engine entry point; no current
cross-type assignment path exists. `setEntity` is the EXE attachment hook and current callers do not
retarget a City wrapper with a Unit entity or vice versa.

HISTORY / DEDUP / VERDICT
-------------------------
The source is inherited Base/AdvC glue and has no SAS semantic delta. Queue007/Queue006 already
saturated Unit/City reload/destruction behavior and did not expose an independent entity-wrapper
lifetime failure. No new ownership, stale-pointer, graphical-reset or ordering root survives here.

Queue141 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue142 `CvGameCoreDLL/CyArtFileMgr.cpp` (84 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit its singleton/wrapper returns against Queue111 `CvArtFileMgr.cpp`: Python ownership policy,
art-tag lookup/fallback semantics, NULL returns, wrapper allocation lifetime, current bindings/callers,
Base/history and whole-album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP649 physical -> Queue141 CLOSED / NO new F707 -> 141 / 169 complete -> Queue142
CyArtFileMgr.cpp OPEN -> F707/KI#1028 UNUSED -> wrapper/ownership/tag/NULL/binding/caller/Base/history/
dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 141 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP650 -- QUEUE 142 CyArtFileMgr.cpp CLOSED / QUEUE 143 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP649, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 142 RESULT
----------------
Queue142 `CvGameCoreDLL/CyArtFileMgr.cpp` is CLOSED with no new finding.

CURRENT / BASE / WRAPPER OWNERSHIP
----------------------------------
Current SAS wrapper logic is executable-identical to Base AdvC 1.14. Default `CyArtFileMgr()` points
at the global `ARTFILEMGR`; the alternate constructor merely borrows an explicitly supplied manager.
The wrapper itself allocates no ArtInfo objects. Every getter returns a pointer owned by the global
art-info storage and Queue162's binding file correctly uses `reference_existing_object` for those
borrowed records rather than transferring ownership to Python.

Queue111 already audited the native `CvArtFileMgr` map construction/reset/swap lifecycle and verified
all current literal art-tag lookups against the merged current+BtS tag universe. Queue142 delegates
directly to those native getters and introduces no changed lookup, fallback or path semantics.

SHIPPED PYTHON CALLER CONTROL
-----------------------------
Shipped Python creates `CyArtFileMgr()` as a lightweight global/local wrapper and overwhelmingly
consumes returned ArtInfo immediately for `.getPath()`, `.getButton()` or related immutable art data.
Representative Dawn-of-Man, Wonder Movie, World Advisor and PLE calls all use valid current tags.
The exhaustive native literal-tag census from Queue111 found no missing current literal tag; dynamic
callers derive tags from validated XML info fields.

NULL / RESET LIFETIME
---------------------
The getters return NULL when the wrapper manager pointer is NULL or a native tag lookup fails. Shipped
callers generally dereference immediately and therefore depend on valid shipped tags rather than a
Python-side NULL check; current data satisfies that contract. A third-party script can still request
an invalid tag and dereference None, but that is extension-input robustness, not a current root.

`Reset()` and `buildArtFileInfoMaps()` are publicly wrapped and can invalidate/rebuild manager maps if
misused from Python. The current game does not retain borrowed ArtInfo proxies across a deliberate art
manager reset; Queue111 established the supported graphics/XML reset ordering. No shipped dangling
ArtInfo proxy was found.

HEADER-ONLY LEGACY DECLARATIONS
-------------------------------
`CyArtFileMgr.h` still declares `getGenericBuildingArtInfo` and `getGenericCityArtInfo`, but this `.cpp`
has no definitions and the Python interface does not register either method. No current C++/Python
caller references them, so they are dead API declarations rather than a linked/runtime defect.

HISTORY / DEDUP / VERDICT
-------------------------
Current Base equality and Queue111's completed native-map/tag audit account for the meaningful
art-manager contracts. No inverse-ownership, missing shipped tag, reset-lifetime or wrapper-specific
root survives here.

Queue142 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue143 `CvGameCoreDLL/CySelectionGroupInterface.cpp` (81 queued/current
physical lines), OPEN under F707 / provisional KI#1028 unused.

Audit the Boost.Python registration against completed Queue091 `CySelectionGroup.cpp`: name->target
and signature parity, mission-queue return ownership (KI#480 repair), unit/plot wrapper ownership,
help/default drift, shipped callers, Base/history and whole-album dedup. Promote only an independent
supported current binding root.

Exact continuation stone:
C031-WIP650 physical -> Queue142 CLOSED / NO new F707 -> 142 / 169 complete -> Queue143
CySelectionGroupInterface.cpp OPEN -> F707/KI#1028 UNUSED -> registration/ownership/signature/caller/
Base/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 142 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP651 -- QUEUE 143 CySelectionGroupInterface.cpp CLOSED / QUEUE 144 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP650, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 143 RESULT
----------------
Queue143 `CvGameCoreDLL/CySelectionGroupInterface.cpp` is CLOSED with no new finding.

REGISTRATION CENSUS / BASE CONTROL
----------------------------------
Current SAS registration logic is executable-identical to supplied Base AdvC 1.14. Mechanical census
finds 58 active `.def` bindings, all with unique exported Python names and every exported name pointing
to the same-named `CySelectionGroup` wrapper method. No duplicate registration, swapped target or
accidental overload selection survives.

RETURN OWNERSHIP
----------------
All wrapper methods that allocate a fresh Python-side wrapper use `manage_new_object`:
`lastMissionPlot`, `plot`, `area`, `getPathFirstPlot`, `getPathEndTurnPlot`, `getHeadUnit` and
`getUnitAt`. Current SAS's KI#480 repair also makes `getMissionFromQueue` return an independent
`new MissionData(*pMission)` copy, truthfully matching the existing owning return policy. Queue091
already proved negative/oversized mission indexes resolve safely to NULL through the native list
lookup. No ownership inversion like F688 remains here.

POINTER / SIGNATURE / CALLER CONTROL
------------------------------------
The raw `CyPlot*`/`CyArea*` arguments inherit the wrapper's non-NULL preconditions already audited in
Queue091. Shipped Python does not pass None into these methods on established call paths. The binding
help strings contain minor old-style enum notation but do not determine runtime arity.

`getBestBuildRoute` exposes the inherited `BuildTypes* peBestBuild` out-pointer through Boost.Python,
which is awkward for ordinary Python and may be unusable without an appropriate pointer converter.
There is no shipped Python caller, and the wrapper itself safely rejects a NULL/invalid plot before
reaching the native function. Keep this as dormant extension-API design debt, not F707.

PATH / GROUP LIFETIME / KI DEDUP
--------------------------------
Path-return wrappers allocate independent CyPlot wrappers around the native path nodes. Queue091
already audited path generation, group lifetime and mission-queue access, while Queue015 audited the
native selection-group implementation. Queue143 introduces no new mutation or lifetime layer.
KI#480 remains the sole mission-queue ownership root; the current binding now matches its repair.

HISTORY / VERDICT
-----------------
The interface is inherited Base/AdvC registration glue and has no current SAS semantic delta beyond
binding to the already-repaired Queue091 implementation. No independent current-live root survives.

Queue143 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue144 `CvGameCoreDLL/CvDLLPython.cpp` (80 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit Python-system bootstrap and module initialization ordering, interface-registration sequence,
exception/error handling, Python extension singleton lifetime, exported entry points, Base/Civ4CE/
K-Mod history and whole-album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP651 physical -> Queue143 CLOSED / NO new F707 -> 143 / 169 complete -> Queue144
CvDLLPython.cpp OPEN -> F707/KI#1028 UNUSED -> bootstrap/registration/order/error/lifetime/Base/history/
dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 143 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP652 -- QUEUE 144 CvDLLPython.cpp CLOSED / QUEUE 145 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP651, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 144 RESULT
----------------
Queue144 `CvGameCoreDLL/CvDLLPython.cpp` is CLOSED with no new finding.

CURRENT / BASE / BOOTSTRAP STRUCTURE
------------------------------------
Current SAS is executable-identical to supplied Base AdvC 1.14 after newline normalization. The one
exported `DLLPublishToPython()` routine performs registration only; it owns no long-lived native game
state and adds no custom SAS initialization branch.

REGISTRATION ORDER
------------------
The sequence is internally coherent:
- enum and standalone wrapper/interface registrations run first;
- split large wrappers are each constructed exactly once (`CyCity`, `CyPlayer`, `CyUnit`, `CyPlot`,
  `CyGlobalContext`);
- each corresponding interface tranche then extends that same `python::class_` object.
No duplicate class construction or omitted interface initializer is present.

Some standalone registrations expose functions whose return/argument wrapper class is constructed
later in this routine. Boost.Python can register those function signatures before the class converter
is published; the completed extension module is not available to shipped Python until
`DLLPublishToPython()` returns, by which point the large classes are registered. No supported script
can call the half-built module in the middle of publication.

CROSS-QUEUE CONTROL
-------------------
All called interface functions have been or will be audited individually in their queue files. The
completed interface queues through Queue143 have not exposed a duplicate global registration that
would make this bootstrap order ambiguous. CyEnums is deliberately first so enum objects/names exist
before ordinary script use. The remaining class/interface split follows the inherited Civ4 pattern.

ERROR / LIFETIME / HISTORY
--------------------------
This thin routine contains no manual allocation, exception swallowing, pointer dereference or partial
rollback code. Registration failures are handled by the surrounding Python/EXE initialization layer;
there is no Queue144-specific state left partially initialized by local code. Current Base equality
and the long-standing shipped initialization path provide no independent current-live defect root.

Queue144 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue145 `CvGameCoreDLL/CyDeal.cpp` (80 queued/current physical lines),
OPEN under F707 / provisional KI#1028 unused.

Audit its borrowed `CvDeal*` wrapper, TradeData list-node exposure, kill/lifetime invalidation,
player/team/type accessors, Python binding ownership and shipped callers against completed Queue042
`CvDeal.cpp` and Queue090 `CyGameInterface.cpp`; then Base/history/dedup. Promote only an independent
supported current root.

Exact continuation stone:
C031-WIP652 physical -> Queue144 CLOSED / NO new F707 -> 144 / 169 complete -> Queue145 CyDeal.cpp
OPEN -> F707/KI#1028 UNUSED -> deal/TradeData ownership/lifetime/binding/caller/Base/history/dedup
sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 144 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP653 -- QUEUE 145 CyDeal.cpp CLOSED / QUEUE 146 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP652, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 145 RESULT
----------------
Queue145 `CvGameCoreDLL/CyDeal.cpp` is CLOSED with no new finding.

BASE / WRAPPER OWNERSHIP
------------------------
Current SAS is executable-identical to Base AdvC 1.14. `CyDeal` borrows a `CvDeal*`; the wrapper
destructor intentionally does not delete the native deal because ownership remains with
`CvGame::m_deals`. Scalar accessors guard a NULL wrapper and return ordinary sentinels/zero.

TRADEDATA INTERIOR POINTERS
---------------------------
`getFirstTrade` / `getSecondTrade` return pointers to `TradeData` values stored inside the native
deal's linked lists. Queue090 already reconciled the corresponding Boost.Python policy:
`reference_existing_object` is the correct ownership direction for that live interior storage.
Negative indexes naturally fall through the traversal and return NULL; oversized indexes are
rejected before traversal. Shipped Foreign Advisor / DealUtil callers iterate from 0 to the current
reported list length and inspect returned data synchronously.

KILL / STALE-WRAPPER API NOTE
-----------------------------
`CyDeal::kill()` calls `CvDeal::kill()`, whose `killSilent()` ends by
`CvGame::deleteDeal(getID())`; the game free-list therefore removes the underlying native object.
`CyDeal` does not clear `m_pDeal` afterward. A third-party Python script that retains and reuses the
same `CyDeal` after calling `.kill()` could therefore observe `isNone()==false` on an invalidated
native pointer and potentially access recycled/stale storage.

This is a genuine API-lifetime sharp edge but no shipped activation was established. The only shipped
Python kill helper (`DealUtil.Deal.kill`) forwards the call and does not itself access the wrapper
again; no in-tree caller of that helper was found. Foreign Advisor TradeData proxies are consumed
before deletion and not retained across `kill`. Keep this as extension-API robustness debt rather
than F707.

HISTORY / DEDUP / VERDICT
-------------------------
Queue042 already audited native deal creation/destruction/trade-list semantics and Queue090 audited
this wrapper's binding ownership. Current Base equality adds no newer SAS drift. No independent
current-live Queue145 root survives.

Queue145 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue146 `CvGameCoreDLL/CyMessageControl.cpp` (80 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit the Python message-control singleton wrapper against Queue115 sender contracts and Queue044
receivers: active-player assumptions, argument order/types, null/sentinel handling, wrapper lifetime,
Python bindings/callers, Base/history and dedup. Promote only an independent supported root.

Exact continuation stone:
C031-WIP653 physical -> Queue145 CLOSED / NO new F707 -> 145 / 169 complete -> Queue146
CyMessageControl.cpp OPEN -> F707/KI#1028 UNUSED -> wrapper/sender/active-player/argument/binding/caller/
Base/history/dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 145 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP654 -- QUEUE 146 CyMessageControl.cpp CLOSED / QUEUE 147 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP653, with supplied
Base AdvC 1.14 as direct comparison oracle. No web/GitHub lookup used. Gameplay/XML/Python source
remains untouched. F707 / provisional KI#1028 remains UNUSED.

QUEUE 146 RESULT
----------------
Queue146 `CvGameCoreDLL/CyMessageControl.cpp` is CLOSED with no new finding.

BASE / WRAPPER STRUCTURE
------------------------
Current SAS is executable-identical to supplied Base AdvC 1.14. This class owns no native state; each
method immediately forwards to the `CvMessageControl` singleton or a gDLL network helper. Queue115
already reconciled all native message constructors against Queue044 receivers, so Queue146's task is
limited to Python argument adaptation and temporary ownership.

CIVIC-LIST CONVERSION
---------------------
`sendUpdateCivics` converts a Python list to a temporary `int[]`, copies one value per CivicOption into
an enum `CivicMap`, sends the native message and `delete[]`s the temporary. The `delete[]` is the
existing C2C/AdvC allocation-form repair and matches `CvDLLPythonIFaceBase::putSeqInArray`, which
allocates the sequence buffer with `new[]` when nonempty.

The wrapper does not explicitly compare returned Python sequence length against the CivicOption count,
so a third-party short list could make the subsequent fixed CivicOption loop read beyond the temporary
array. Current shipped Policy Advisor constructs `m_paeDisplayPolicies` by iterating exactly
`gc.getNumCivicOptionInfos()` and is the sole current caller; ordinary UI therefore satisfies the
contract. Keep short-list handling as Python API robustness debt rather than F707.

PUSH-ORDER / MESSAGE ARGUMENT ADAPTERS
--------------------------------------
`sendPushOrder` intentionally retains Firaxis' old Python `(bAlt,bShift,bCtrl)` surface while adapting
it to K-Mod's native `(bSave,bPop,iPosition)` queue API. The header explicitly documents that the
wrapper arguments no longer match the native helper. Shipped city-screen callbacks pass the expected
modifier combinations, and Queue115/Queue044 prove the resulting native message field order.

Other wrappers are direct enum/int casts into already-audited senders. Active-player guarding is
performed by the native `CvMessageControl` methods. `sendAdvancedStartAction` and `sendEmpireSplit`
intentionally carry an explicit player selected by their UI producer; current callers pass validated
active/WorldBuilder player IDs and real area IDs. `GetConnState`/`GetFirstBadConnection` are thin
network-query helpers with no gameplay ownership.

BINDING / CALLER / HISTORY DEDUP
--------------------------------
Queue163 will audit the tiny binding table separately; Queue146 itself introduces no allocation,
retained pointer or sender/receiver reinterpretation beyond the civic temporary. Current Base equality
plus Queue115's completed sender audit leaves no independent current-live root.

Queue146 is CLOSED with no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue147 `CvGameCoreDLL/CvInfo_TrueStarts.cpp` (79 queued/current physical
lines), OPEN under F707 / provisional KI#1028 unused.

Audit True Starts XML info defaults/read semantics, coordinate/scalar ranges, civilization key
resolution, per-map/start data consumers in completed Queue022 `TrueStarts.cpp`, current XML fixtures,
Base/history and whole-album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP654 physical -> Queue146 CLOSED / NO new F707 -> 146 / 169 complete -> Queue147
CvInfo_TrueStarts.cpp OPEN -> F707/KI#1028 UNUSED -> XML/default/range/key/consumer/data/Base/history/
dedup sweep -> either promote consequential F707 or close cleanly -> freeze next WIP.

Files complete: 146 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP655 -- QUEUE 147 CvInfo_TrueStarts.cpp CLOSED / QUEUE 148 OPEN
===============================================================================
Source identity: reconciled AdvCiv-SAS light-source snapshot used by C031-WIP654, with supplied
Base AdvC 1.14 as direct comparison oracle and the local commit-diff mirror for history. No web/
GitHub lookup used. Gameplay/XML/Python source remains untouched. F707 / provisional KI#1028 remains
UNUSED.

QUEUE 147 RESULT
----------------
Queue147 `CvGameCoreDLL/CvInfo_TrueStarts.cpp` is CLOSED with no new finding.

BASE / HISTORY BOUNDARY
-----------------------
Current SAS `CvInfo_TrueStarts.cpp` is executable-identical to supplied Base AdvC 1.14. Local path
history has only practical 3579, which introduced the complete True Starts info layer, and SAS
practical 5372, which changes whitespace only. The header's later SAS practical 5961 touch is likewise
formatting/signature layout rather than a semantic loader change. Any defect here would therefore be
an inherited practical-3579 data/default/read contract, not an SAS code drift.

QUEUE-022 DEDUP CONTROL
-----------------------
The earlier deep Queue022 `TrueStarts.cpp` audit already established F613-F623 / KI#935-KI#945.
Those roots own, among other things, the 35-48-player civ-pool exhaustion, missing Ireland/Kingdom of
Benin TruCiv integration (including the fixed-civ NULL-dereference breadth), the Empty-New-World
longitude and duplicate-pass defects, Snow precipitation classification, unrestricted-leader
civ/leader coupling, uninitialized maximum-elevation aggregate, manually chosen leader overwrite,
leader-count-biased Oceanity baseline, wrapped-seam climate-region filtering and localized map-option
detection. Queue147 was therefore required to establish a distinct XML-info loader/default/type root;
none survived.

CURRENT TRUCIV DATA / DEFAULTS
------------------------------
A mechanical census of `Assets/XML/TrueStarts/CIV4TruCivInfos.xml` finds 34 records. Every record has:

* a unique TruCiv `<Type>`;
* a unique, current `CIVILIZATION_*` target;
* both mandatory latitude/longitude fields;
* coordinates inside the loader's inclusive -900..900 latitude and -1800..1800 longitude bounds;
* every supplied optional scalar inside the corresponding asserted range;
* a valid supplied `ARTSTYLE_*` region whenever GeoRegion is present.

All 34 current records supply precipitation, climate variation, Oceanity, maximum elevation and
mountainous-area data. Optional omissions match the documented defaults: MajorRiverWeight=-25,
TotalArea=-1, HStretch=-1, Bias=0 and GeoRegion=NO_ARTSTYLE. Queue022 already audited the consumers of
those sentinels/defaults; no new default/consumer contradiction appears here. The schema explicitly
states that only coordinates are mandatory so mod-added civilizations can provide a minimal record.

CURRENT TRULEADER DATA
----------------------
`CIV4TruLeaderInfos.xml` contains 52 records. TruLeader `<Type>` values and target `LEADER_*` values
are unique and all targets resolve in current LeaderHead data. Missing StartOfReign deliberately maps
to MIN_INT (unknown), and Bias defaults to zero. Queue022's contemporaries/leader-fitness pass already
proved the MIN_INT path is treated as absent information rather than indexed/arithmetic data. The
missing SAS leaders associated with Ireland/Benin remain F614 content breadth rather than a new loader
root.

CURRENT TRUBONUS MAPS
---------------------
`CIV4TruBonusInfos.xml` contains 28 unique TruBonus records with 28 unique valid current `BONUS_*`
targets. A mechanical traversal of all DiscouragedRegions, DiscouragedCivs and EncouragedCivs finds
195 key/value pairs. Every civilization key, region key and era value resolves; there are no duplicate
keys within any per-bonus map. `UntilEra=NONE` intentionally becomes NO_ERA and therefore means
"regardless of start era" in the Queue022 consumers. The enum-map default `(EraTypes)0` is equally
intentional: an absent key returns the first era and therefore fails the `> getStartEra()` test even
on an Ancient start, representing no bias.

The only current `bLandOnly=1` record is Oil. Current BONUS_OIL is valid on ordinary land terrain and
on TERRAIN_OCEAN, so the debug-only `bWaterFound && bLandFound` invariant is satisfied. The map loader
uses the generic XML tag-pair iterator audited in Queue086/Queue052; mixed global ArtStyle keys and
info-backed Civilization/Era values are intentionally resolved by `getGlobalEnumFromString`.

SAS CAMEL CONTENT NOTE -- NOT A FINDING
---------------------------------------
Current BONUS_CAMEL has no TruBonus record. That means True Starts applies no explicit per-civ or
per-region Camel encouragement/discouragement. This is worth remembering when True Starts content is
expanded, especially alongside the already-owned missing Ireland/Benin integration, but absence of a
TruBonus record is not itself a correctness invariant: the current system intentionally has several
other bonuses without TruBonus data (e.g. Aluminum, Uranium, Deer and Whale, plus the non-map Drama/
Music pseudo-bonuses). `getTruBonus` explicitly treats missing records as "no True Starts bias." Do
not spend F707 on this tuning/content omission without a stronger promised-data contract.

TYPE / ERROR / RELEASE-BUILD CONTROLS
-------------------------------------
`CivilizationType`, `LeaderType` and `BonusType` are schema-required and all current values resolve.
GeoRegion is optional; an omitted empty string is deliberately accepted by `CvGlobals` type lookup as
NO_ARTSTYLE without an error. Invalid third-party type strings would produce the existing XML loader
error path and sentinel values, but current shipped XML contains no such input.

The numeric range checks are assertions rather than release-mode sanitizers. That is consistent with
this info layer's data-validation role; current shipped values satisfy every check, so hypothetical
malformed mod XML is robustness rather than a current SAS root.

QUEUE 147 CLOSURE
-----------------
No independent current-live XML/default/range/key/ownership defect survives. Queue147 is CLOSED with
no new finding; F707 remains unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue148 `CvGameCoreDLL/CyHallOfFameInterface.cpp` (79 queued/current
physical lines), OPEN under F707 / provisional KI#1028 unused.

Audit Boost.Python Hall-of-Fame registration against `CyHallOfFameInfo`/`CvHallOfFameInfo`: exported
names/targets/signatures, ownership/return policies, lifetime/reset contracts, shipped Python callers,
Base/history and whole-album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP655 physical -> Queue147 CLOSED / NO new F707 -> 147 / 169 complete -> Queue148
CyHallOfFameInterface.cpp OPEN -> F707/KI#1028 UNUSED -> binding/name/target/signature/ownership/
lifetime/caller/Base/history/dedup sweep -> either promote consequential F707 or close cleanly ->
freeze next WIP.

Files complete: 147 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP656 -- QUEUE 148 CyHallOfFameInterface.cpp -- F707 / KI#1028 CONFIRMED
===============================================================================
Physical parent: C031-WIP655.
Source identity: reconciled AdvCiv-SAS light-source snapshot, supplied Base AdvC 1.14, local
commit-diff mirror, `CyHallOfFameInfo`/`CvHallOfFameInfo` ownership layer and shipped Hall-of-Fame
Python screen. No web/GitHub lookup used. Gameplay/XML/Python source remains untouched.

STATUS
------
Queue148 `CvGameCoreDLL/CyHallOfFameInterface.cpp` remains OPEN.
F707 / provisional KI#1028 is CONFIRMED.
F708 / provisional KI#1029 is UNUSED.

F707 / KI#1028 -- REOPENING HALL OF FAME LETS THE OLD WRAPPER DESTRUCTOR ERASE THE NEW NATIVE REGISTRATION, SO GAME RESET NO LONGER RELEASES THE CURRENT REPLAY SET
----------------------------------------------------------------------------------------------------------------------------------------------------------------------
Status: NEW CURRENT INHERITED ADVCIV LIFECYCLE / MEMORY-RETENTION ROOT; ordinary shipped UI
activation and practical-1430 birth interaction proved.

NATIVE REGISTRATION CONTRACT
----------------------------
AdvC's `CvHallOfFameInfo` constructor registers every live Hall-of-Fame container with `CvGame`:

    CvHallOfFameInfo::CvHallOfFameInfo()
    {
        GC.getGame().setHallOfFame(this);
    }

Its destructor calls `uninit()`, and `uninit()` unconditionally clears the game pointer, clears the
Hall-of-Fame compatibility flag and deletes every owned replay:

    CvHallOfFameInfo::~CvHallOfFameInfo()
    {
        uninit();
    }

    void CvHallOfFameInfo::uninit()
    {
        GC.getGame().setHallOfFame(NULL);
        CvGlobals::getInstance().setHoFScreenUp(false);
        ... SAFE_DELETE(m_aReplays[i]);
        m_aReplays.clear();
    }

`CvGame::reset()` consumes precisely that registration:

    if (m_pHallOfFame != NULL)
        m_pHallOfFame->uninit(); // Don't keep HoF in memory indefinitely

The pointer is therefore a current lifecycle hook, not dead bookkeeping.

SHIPPED PYTHON REPLACEMENT ORDER
--------------------------------
`CvScreensInterface.py` constructs one process-long `CvHallOfFameScreen` object. Every time its
`interfaceScreen()` is entered while the screen is not already active, the screen executes:

    self.hallOfFame = CyHallOfFameInfo()
    self.hallOfFame.loadReplays()

It does not reuse the previous wrapper or explicitly destroy it before constructing the replacement.
On the second and subsequent Hall-of-Fame openings there is therefore already an old
`self.hallOfFame` object.

Normal CPython assignment semantics evaluate the right-hand side before replacing the old attribute.
Consequently the native order is:

1. construct new `CyHallOfFameInfo`;
2. its by-value `CvHallOfFameInfo` constructor calls `setHallOfFame(new)`;
3. store the new Python attribute and release the old Python wrapper;
4. the old wrapper's `CvHallOfFameInfo` destructor calls `uninit()`;
5. that old object unconditionally calls `setHallOfFame(NULL)`;
6. the next Python line calls `new.loadReplays()`.

The old destructor has no identity check. It therefore erases the registration that the new object
just installed. `loadReplays()` sets the global `m_bHoFScreenUp` compatibility flag but does not
register the object again.

DETERMINISTIC CURRENT CONSEQUENCE
---------------------------------
After the second Hall-of-Fame opening, the current Python object is alive and owns the newly loaded
`CvReplayInfo*` vector, but `CvGame::m_pHallOfFame == NULL`.

On the next normal game reset/new-game/load reset, this intended cleanup is skipped:

    if (m_pHallOfFame != NULL) // false after the replacement race
        m_pHallOfFame->uninit();

Therefore the current Hall-of-Fame replay set remains allocated in the long-lived Python screen
object across the reset instead of being released at the explicit AdvC lifecycle boundary. The
`m_bHoFScreenUp` flag likewise remains true because its clearing lives in the skipped `uninit()`.
The replay memory is eventually released if the screen is opened yet again (when that wrapper is
replaced) or during Python shutdown, so classify this as retained/late-freed Hall-of-Fame memory,
not an unbounded per-frame leak. It can nevertheless retain the entire loaded replay collection
across games, exactly what practical 1430's reset hook says it was introduced to prevent.

The same registration loss reproduces after a first game reset too: reset calls `uninit()` on the
first Hall-of-Fame object but leaves the Python wrapper alive; the next Hall-of-Fame opening first
registers its replacement, then destruction of that old empty wrapper clears the replacement's new
registration.

BINDING / OWNERSHIP CONTROL
---------------------------
This is not a `manage_new_object` mistake in Queue148. `CyHallOfFameInfo::getReplayInfo(i)` correctly
allocates a fresh `CyReplayInfo` wrapper and the binding correctly transfers ownership of that wrapper
to Python. Queue080 already established that the wrapper then borrows the underlying replay from the
Hall-of-Fame container and shipped screens keep the container alive while consuming it.

F707 instead concerns ownership/registration of the Hall-of-Fame *container itself*: two sequential
containers exist briefly during ordinary Python attribute replacement, while every destructor acts
as though it must still be the globally registered container.

ANCESTRY
--------
The Python Hall-of-Fame screen already recreated `self.hallOfFame = CyHallOfFameInfo()` on screen
construction/opening before AdvC's lifecycle hook.

AdvC practical 1430 (`efd11afbeb`, "Save replays in the BtS format, bugfixes for R&F option")
introduced the interacting native pieces together:

* `CvGame::m_pHallOfFame` and `setHallOfFame`;
* constructor registration `setHallOfFame(this)`;
* destructor -> `uninit()`;
* unconditional `setHallOfFame(NULL)` inside `uninit()`;
* `CvGame::reset()` calling the registered object's `uninit()` specifically to avoid keeping HoF
  data in memory indefinitely.

Practical 1431 simplified temporary recursive-cleanup machinery but retained the unconditional clear.
Later history changes replay parsing/compatibility and formatting, not registration identity. Supplied
Base AdvC 1.14 and current SAS retain the same lifecycle. F707 is therefore inherited AdvC feature
interaction, not an SAS regression.

DEDUP
-----
Whole-album search finds no existing owner for one Hall-of-Fame object's destructor clearing a newer
container registration. Queue080 audited `CyReplayInfo` borrowed replay lifetime and found no
in-screen invalidation. F272/KI#595 concerns foreign-replay ColorTypes remapping while Hall of Fame is
active. Neither owns the container-registration replacement race.

REPAIR BOUNDARY
---------------
The robust native fix is identity-aware deregistration: only clear `CvGame::m_pHallOfFame` when the
object being uninitialized is still the registered object. For example, expose a clear-if-current
operation (or getter plus guarded clear) and have `CvHallOfFameInfo::uninit()` use it. This preserves
cleanup when the current container is reset/destroyed while preventing an old container from
unregistering its successor.

A Python-side workaround could explicitly release/reuse the old Hall-of-Fame object before
constructing a replacement, but the native global-registration invariant should ideally be robust to
overlapping old/new lifetimes because construction and destruction order is not otherwise encoded in
`setHallOfFame(NULL)`.

Regression test: open Hall of Fame, close it, open it again, then start/load/reset a game. Verify that
the second container remains registered until reset and that reset clears its replay vector and
`m_bHoFScreenUp` rather than skipping Hall-of-Fame cleanup.

CURRENT STONE
-------------
Queue148 remains OPEN. F707/KI#1028 CONFIRMED. F708/KI#1029 UNUSED.
Continue the remaining binding table, replay-index, help-signature, duplicate-registration,
loadReplays/replay-screen lifetime, Base/history and whole-album closure. Do not duplicate F707 from
Queue160/164 later; those files should broaden/dedup this root unless a separate cause is proved.

Exact continuation stone:
C031-WIP656 physical -> Queue148 OPEN -> F707/KI#1028 CONFIRMED -> F708/KI#1029 UNUSED -> finish
CyReplayInfo/CyHallOfFameInfo binding name-target/arity/ownership + load/index/caller/lifetime +
Base/history/dedup closure -> either promote independent F708 or close Queue148 cleanly -> freeze next
WIP.

Files complete: 147 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP657 -- QUEUE 148 CyHallOfFameInterface.cpp CLOSED / QUEUE 149 OPEN
===============================================================================
Physical parent: C031-WIP656.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, local history mirror,
Queue080 `CyReplayInfo.cpp`, pending Queue160/164 native Hall-of-Fame helpers and shipped Hall-of-Fame/
Replay Python screens. No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 148 RESULT
----------------
Queue148 is CLOSED with F707/KI#1028 as its sole new root. F708 / provisional KI#1029 remains UNUSED.

BINDING TABLE SATURATION
------------------------
A mechanical parse finds 50 active `.def` exports across `CyReplayInfo` and `CyHallOfFameInfo`.
Every exported Python name matches its C++ method target, and there are no duplicate exported names.
`CyReplayInfo` and `CyHallOfFameInfo` are each registered exactly once by `DLLPublishToPython`; there is
no duplicate class creation or split-interface ordering issue.

`CyHallOfFameInfo::getReplayInfo(int)` allocates a fresh `CyReplayInfo` wrapper around a replay pointer
owned by the by-value `CvHallOfFameInfo` container. Queue148 correctly declares
`manage_new_object` for that newly allocated wrapper. This is deliberately different from ownership
of the underlying `CvReplayInfo`, which remains with the Hall-of-Fame container. Queue080 already
proved that shipped Hall-of-Fame/Replay screens retain the owning Hall-of-Fame object while consuming
those borrowed replays and do not invalidate the underlying pointer mid-screen.

INDEX / CALLER CONTROLS
-----------------------
Native `CvHallOfFameInfo::getReplayInfo(i)` does not bounds-check `i`, but every shipped Hall-of-Fame
caller either loops `0 <= i < getNumGames()` or uses an index previously stored from exactly such a
loop in `infoList`. The replay-button path therefore feeds a valid index. Arbitrary third-party Python
can still request a bad index; retain that as public-extension hardening rather than F708.

`CyReplayInfo` message/history index behavior, signed-to-uint message casts and borrowed/default
ownership modes were already closed in Queue080. Queue148 introduces no new conversion around them.

LOAD / SCREEN LIFETIME CONTROLS
-------------------------------
`CvHallOfFameInfo::loadReplays()` does not explicitly clear an already populated replay vector before
calling the EXE loader. A third-party caller could invoke `loadReplays()` repeatedly on one wrapper;
the exact EXE replace/append behavior is not established here. Shipped `CvHallOfFameScreen` instead
constructs a fresh `CyHallOfFameInfo` before each load. The real shipped defect in that recreation
pattern is F707's old-destructor/new-registration race, already frozen at WIP656; do not split repeated
load behavior into F708 without a distinct current caller/consequence.

HELP-TEXT ONLY
--------------
Two binding help strings are stale:

* `createInfo` says `void ()`, but the actual bound method is `createInfo(int iPlayer)` and all shipped
  callers pass a player argument;
* `getModName` says `std::wstring ()`, while the wrapper returns `const char*`.

Boost.Python binds the actual C++ function types rather than parsing these help strings, so runtime
arity/conversion is correct. Documentation cleanup only.

HISTORY / BASE
--------------
Current Queue148 is executable-identical to supplied Base AdvC 1.14. K-Mod supplied the base binding
surface; practical 1844 added only `getMinimapSize`, and practical 2127 changed debug-console output
from `OutputDebugString` to `printToConsole`. Neither creates another semantic registration defect.
F707's lifecycle root lives in practical 1430's native Hall-of-Fame registration interaction with the
pre-existing Python screen recreation pattern and is intentionally carried forward for Queue160/164
dedup.

QUEUE 148 CLOSURE
-----------------
No independent F708 survives. Queue148 is CLOSED with F707/KI#1028 as its sole finding.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue149 `CvGameCoreDLL/CvBugOptions.cpp` (78 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit BUG option lookup/module/cache semantics against `CvBugOptions.h`, Python BUG configuration and
all native callers: missing-option/default behavior, bool/int/string conversion, cache lifetime and
reload, Python callback ownership/error state, initialization ordering, Base/K-Mod history and album
dedup. Promote only a supported independent root.

Exact continuation stone:
C031-WIP657 physical -> Queue148 CLOSED / F707 confirmed / no F708 -> 148 / 169 complete -> Queue149
CvBugOptions.cpp OPEN -> F708/KI#1029 UNUSED -> BUG option lookup/default/type/cache/init/Python-callback/
caller/Base/history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 148 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP658 -- QUEUE 149 CvBugOptions.cpp CLOSED / QUEUE 150 OPEN
===============================================================================
Physical parent: C031-WIP657.
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied by the user, supplied Base
AdvCiv 1.14, local BUG Python/config/text sources and local AdvC/K-Mod history mirror. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

QUEUE 149 RESULT
----------------
Queue149 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED.

BASE / IMPLEMENTATION CONTRACT
------------------------------
Current `CvBugOptions.cpp` and `.h` are executable-identical to supplied Base AdvC 1.14; the apparent
file diff is newline convention only. The C++ bridge has three small operations:

* `BUGOption::isEnabled(id, default, warn)` calls Python `getOptionBOOL`;
* `BUGOption::getValue(id, default, warn)` calls Python `getOptionINT`;
* `BUGOption::userDirPath()` calls Python `getUserDirStr`.

The Python bridge in `BUG/BugDll.py` resolves the option through `BugOptions.getOption(id)` and wraps
both lookup and type conversion in `castOptionValue`; missing options or conversion exceptions return
the C++-supplied default. Thus an absent configured option is an explicitly supported state rather
than an automatic false/zero contract violation once BUG is initialized.

OPTION-ID / TYPE CENSUS
-----------------------
A mechanical current-tree census of active literal native BUG calls finds no enabled/value type
crossing: Boolean C++ calls target Boolean options and integer C++ calls target list/integer options.
The only active literal IDs without an active option declaration are the retired per-city-icon
switches `MainInterface__TopProductionIcon`, `MainInterface__TopXPIcon`,
`MainInterface__NoUnhappyIcon` and `MainInterface__NoBadHealthIcon` (plus the sibling top-yield IDs
generated in the same city-billboard family). Their old option declarations are intentionally
commented out after `MainInterface__TopCityIcons` became the aggregate user control. The surviving
C++ compatibility probes all supply `false`, so Python's missing-option fallback cannot accidentally
enable a retired icon. This is deliberate backward-compatible structure, not F708.

List semantics were checked against BUG's actual `BaseListOption` implementation rather than inferred
from raw `<choice>` counts. String lists store/return the displayed zero-based index; numeric lists
store their numeric value and derive a nearest index only for UI selection. ACO's linked list options
(e.g. `ShowAttackerHealthBars`) deliberately reuse `ShowBasicInfo`'s four-value translated list, so
the native mode comparisons remain in the same 0..3 domain.

OFFSCREEN UNIT-SIZE FALSE POSITIVE
----------------------------------
`MainInterface__OffScreenUnitSizeMult` initially looked like a live off-by-one:

* Config XML contains nine historical `<choice>` nodes: 0.8 through 2.2 plus Disable.
* `SelfMod.cpp` treats index 7 as Disable.

However BUG does NOT build this string-list dropdown from those raw `<choice>` nodes. Its translated
`TXT_KEY_BUG_OPT_MAININTERFACE__OFFSCREENUNITSIZEMULT_LIST` contains exactly eight displayed entries:
`0.8|1.0|1.2|1.4|1.6|1.8|2.0|Disable`. `BaseListOption.getDisplayValues()` uses that translated list,
`setIndex()` stores the visible index, and the options UI bounds selection by the eight display
values. Therefore normal visible Disable is exactly index 7, matching `SelfMod`; 2.2 is not a visible
choice. The stray ninth config choice is historical/internal debris, not evidence that 2.2 disables
indicators or that Disable becomes 2.4x.

History strengthens the negative result: practical 3792 introduced this option with the same
nine-choice config / eight-entry translated-list split while native code already used index 7 for
Disable. The current behavior is therefore not a later reorder regression. Preserve this proof so the
same attractive false positive is not rediscovered in Queue075/SelfMod follow-up work.

DEFAULT / INITIALIZATION CONTROLS
---------------------------------
Most native fallback defaults match the corresponding configured defaults. Three list fallbacks can
differ from the live XML default (`ACO__ShowAttackerHealthBars`, `ACO__ShowBasicInfo`, and the
implicit-zero OffScreen size fallback), but those values are consumed after normal BUG initialization
on their shipped UI/self-mod paths. No supported path was established where the pre-BUG fallback is
used as persistent gameplay/UI state. A runtime Python-module failure after `checkBUGStatus` would
also leave the C++ result variable at zero rather than the requested default because `callFunction`'s
Boolean success return is ignored; no current shipped module/call failure was established. Keep both
as defensive/error-path hardening, not F708.

`checkBUGStatus` itself is intentionally lifecycle-aware. Practical 1407 introduced the graphics /
active-player guard, practical 1414 corrected its assertion polarity, and practical 3799 relaxed it
once screen dimensions have been supplied because BUG is then expected to be ready. `CvGame::reset`
returns screen dimensions to zero, while `CvMainInterface` supplies them from Python only after the
BUG/UI stack exists. No current caller was shown to cross that contract and persist a wrong fallback.

`userDirPath()` intentionally bypasses `checkBUGStatus`. Its sole native use is the fullscreen Quick
Load path. `BugPath.getUserDir()` initializes its own path state lazily; an empty Python result simply
falls back to ordinary `gDLL->QuickLoad()`. No independent initialization defect survives there.

HISTORY / DEDUP / CLOSURE
-------------------------
The substantive K-Mod/AdvC history reconciles with the current bridge: K-Mod supplied the BUG-Python
lookup; practicals 1407/1414 added/fixed initialization diagnostics; 1631/1637 removed obsolete
GlobalDefine/profiling wrappers; 1648 added the user-directory bridge; 1779 renamed that API; later
changes are includes, comments or formatting apart from practical 3799's screen-height readiness
rule. Supplied Base AdvC 1.14 retains the same executable behavior, so SAS has not omitted a newer
upstream Queue149 repair.

Whole-album dedup finds no existing BUG-option bridge root that this file independently reopens. The
current option consumers' already-owned UI/ACO/SelfMod defects remain rooted in their own files; this
bridge preserves the values those consumers request.

No consequential supported F708 survives. Queue149 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue150 `CvGameCoreDLL/PlayerHistory.cpp` (76 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit turn-index/history-vector initialization, signed turn offset and resize contracts, serialization
shape/version assumptions, scoreboard/graph consumers, reset/read lifetime, Base/K-Mod/AdvC history
and whole-album dedup. Promote only an independent supported current root.

Exact continuation stone:
C031-WIP658 physical -> Queue149 CLOSED / no F708 -> 149 / 169 complete -> Queue150
PlayerHistory.cpp OPEN -> F708/KI#1029 UNUSED -> turn-index/vector/init/read-write/reset/caller/history/
dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 149 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP659 -- QUEUE 150 PlayerHistory.cpp CLOSED / QUEUE 152 OPEN
===============================================================================
Physical parent: C031-WIP658.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, local AdvC history,
CvPlayer/CvInitCore/CvTeamAI/UWAI/CyPlayer and shipped Info/Main-interface callers. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

QUEUE 150 RESULT
----------------
Queue150 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED.
Current `PlayerHistory.cpp` is executable-identical to supplied Base AdvC 1.14.

STORAGE / INDEX LIFECYCLE
-------------------------
`grow(iTurn)` deliberately guarantees storage through the inclusive requested turn. Normal `set`
callers supply the nonnegative current game turn. For players initialized after turn 0,
`CvPlayer::initContainers()` pre-grows every history through the current turn with `-1` sentinels;
this is practical 2635's explicit repair for late-start/scenario/colony PlayerHistory OOB access.

The active-player-switch hook in `CvInitCore::setActivePlayer` updates the departing living player's
current history entry before the switch. This is the companion practical-2635 repair for the old
Alt+Z/player-switch graph discontinuity and prevents later fixed-index consumers from encountering a
missing current slot.

`getSafe(iTurn)` guards future/oversized indices but not negative ones. Arbitrary third-party Python
can therefore pass a negative history turn and, outside assertions, reach `vector[-1]`; shipped
Python score-delta callers test `>= 0`, while Info-Screen graph callers derive `turn-startTurn` from a
loop whose first turn is never before the game start. Internal `getHistory` callers likewise derive
bounded past turns. Keep the negative-index gap as public-API hardening rather than F708.

MOVING-AVERAGE LATE-CIV CONTROL
-------------------------------
The three-sample yield histories intentionally smooth Economy/Industry/Agriculture. A late-created
player's pre-grown `-1` entries are converted to zero by `std::max(history,0)` while still contributing
to the old-sample count, so its first observed value can be ramped (e.g. 90 -> 30) rather than shown
as an unsmoothed 90.

This is not promoted. The missing slots represent whole game turns before the civilization existed,
so treating their output as zero is a coherent three-game-turn average. The manual explicitly says
the yield curves contain three-sample moving averages, and the graph itself continues to use `-1` as
the no-history sentinel before birth. AI demographic estimators also omit nonpositive history values
when selecting observation samples, so the sentinel itself is never treated as a real negative yield.
No source/history contract requires "average only over post-birth observations," and changing the
divisor would alter intentional smoothing policy.

MOVING-AVERAGE / ANARCHY ARITHMETIC
-----------------------------------
At turn 0, `iOldSamples` becomes -1, but the special `iTurn==0` new-sample path raises `iSamples` back
to 0, the backwards loop executes zero iterations, and the denominator is clamped to at least one;
thus the first value is stored correctly. On revolution turns the new sample is intentionally omitted
and the prior moving average is carried through. Current demographic inputs are nonnegative and the
three-sample integer sum is safely bounded under supported game values.

SERIALIZATION / OWNER STATE
---------------------------
Current write stores a signed count followed by the contiguous int vector; current read consumes the
same shape and is entered on reset/cleared histories. `m_iMovingAvgSamples` and `m_eOwner` are
configuration/runtime metadata rather than serialized observations: reset establishes the intended
0 samples for SCORE and 3 for other histories, and read receives the owning player explicitly.

The legacy map-format branch temporarily disables smoothing while reconstructing turn/value pairs.
Practical 2681 originally introduced that migration and practical 2724 immediately fixed its local-
variable typo so `m_iMovingAvgSamples` is actually restored. Current SAS deliberately calls the
modern branch only because cross-version saves are unsupported; no same-version mismatch exists.
Malformed-save negative/huge counts remain generic stream hardening rather than a self-produced state.

CALLER / HISTORY / DEDUP CLOSURE
--------------------------------
The full current caller family was checked: CvGame score updates, player-end demographic updates,
CvTeamAI/UWAI estimates, replay construction, BBAI diagnostic averages, CyPlayer graph getters and
shipped Info/Main-interface score-delta/graph paths. The earlier deep CvPlayer audit already rechecked
and closed practical 2635's OOB family under current SAS lifecycle/logging; Queue150 confirms the
container side rather than reopening it.

History is compact and consistent: practical 2626 introduced the vector/moving-average class;
2630 changed only rounding implementation; 2635 added inclusive grow plus player-switch/late-player
repairs; 2681/2724 added then fixed legacy-read smoothing suppression; 2852 replaced the rounding
macro with `intdiv::round`; 2998 is the repository/layout mirror. Supplied Base AdvC 1.14 has no later
repair missing from SAS.

No independent supported F708 survives. Queue150 is CLOSED.

QUEUE ADVANCE
-------------
Queue151 `WarEvalParameters.cpp` was already completed during the priority reloop. Ordinary order
therefore advances to Queue152 `CvGameCoreDLL/BarbarianWeightMap.cpp` (73 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit map-size/allocation/indexing, barbarian-weight normalization and update contracts, coordinate /
plot-index assumptions, lifecycle/reset/rebuild, native consumers, Base/AdvC history and album dedup.

Exact continuation stone:
C031-WIP659 physical -> Queue150 CLOSED / no F708 -> 150 / 169 complete -> Queue151 already complete
-> Queue152 BarbarianWeightMap.cpp OPEN -> F708/KI#1029 UNUSED -> allocation/index/weight/update/caller/
history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 150 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP660 -- QUEUE 152 BarbarianWeightMap.cpp CLOSED / QUEUE 153 OPEN
===============================================================================
Physical parent: C031-WIP659.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, local AdvC history,
CvGame/CvMap/Shelf/CvUnit/CvUnitAI/CvPlayerAI and EnumMap contracts. No web/GitHub lookup. Gameplay/
XML/Python source untouched.

QUEUE 152 RESULT
----------------
Queue152 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current executable
behavior is Base AdvC 1.14; SAS practical 5961 only collapsed the `change` signature formatting.

ACTIVITY-MAP VALUE / INDEX CONTRACT
-----------------------------------
`BarbarianActivityMap` stores one signed-short value per PlotNum in a dense/lazy `ArrayEnumMap`.
Every live producer supplies a valid current `CvPlot`, positive bounded `iChange` and a small
nonnegative radius: combat deaths use the default 120/range 5; fog-spawn activity uses 60/range 2.
`change` caps the resulting value at `iMAX_STR=120`, so compact-short storage cannot overflow and
current code never drives a value negative.

`PlotCircleIter` supplies only current-map plots and `plotNum()` is therefore within the dynamic
PlotNum enum-map domain. Land/water equality deliberately prevents activity crossing coastlines.
`currPlotDist()` is protected by `max(1,...)`, so the center and nearby plots have no zero divisor.

The most attractive history bug is already fixed: practical 3204's birth implementation mistakenly
executed `m_map.set(kPlot.plotNum(), ...)` for every radius element, repeatedly overwriting only the
center. Practical 3230 changed that key to `itPlot->plotNum()`, which current Base/SAS retain. Do not
reopen that fixed ancestor.

DECAY / ITERATION
-----------------
During the barbarian-creation era, current game-speed `iBarbPercent` values are 33, 50, 67, 100,
150, 200, 300 and 400. Practical 3898's speed-scaled decay formula therefore has a positive
133..500 denominator and produces a bounded positive decay below 100%; there is no divide-by-zero or
sign inversion in supported data. Once the creation era ends, reset intentionally frees the lazy
map entirely.

`FOR_EACH_NON_DEFAULT_PAIR` over this `ArrayEnumMap` uses an integer dense-map scan rather than an STL
container-node iterator; writing the updated value back to the same key does not structurally
invalidate iteration. Decayed values reaching the default simply become skippable on later scans.

WEIGHT ARITHMETIC / CONSUMERS
-----------------------------
The inherited `RandPlotWeightMap` base weight is 100. Poor land nature yield can divide that by 5;
activity then multiplies it by `max(0,100-activity)/100`. With current activity in 0..120, the final
weight remains in 0..100 and cannot overflow. Zero is intentional: weighted `syncRandPlot` and Shelf
selection permit zero-weight valid plots while retaining the valid-plot count for population/skip
logic.

Nature-yield scoring deliberately takes the minimum of revealed-resource and no-resource forms so
Barbarian placement does not gain hidden-resource information. Current consumers in `CvGame`,
`Shelf`, `CvUnitAI` and `CvPlayerAI` use the same nonnegative weight contract; no signed/probability
mismatch survived.

LIFECYCLE / SERIALIZATION / DEDUP
---------------------------------
`CvGame` owns one `BarbarianWeightMap` for its lifetime, resets activity with game reset, decays it
before barbarian placement each game turn, and `CvMap::read/write` serialize exactly the activity
EnumMap. Queue036 already audited that map serialization/lifecycle surface; no new ordering or
map-resize mismatch appears here. The full album likewise has no unresolved activity-map root that
Queue152 reopens.

History is compact: practical 3204 introduced the feature; 3207 clarified comments; 3230 fixed the
center-key bug; 3898 game-speed-scaled decay; later changes are source organization/formatting. No
newer Base repair is missing from SAS.

No independent supported F708 survives. Queue152 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue153 `CvGameCoreDLL/CyGameCoreUtilsInterface.cpp` (73 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit Boost.Python export name->target/arity/ownership/default contracts against the already-completed
Queue103 `CyGameCoreUtils.cpp`, shipped Python callers, Base history and whole-album dedup.

Exact continuation stone:
C031-WIP660 physical -> Queue152 CLOSED / no F708 -> 151 / 169 complete -> Queue153
CyGameCoreUtilsInterface.cpp OPEN -> F708/KI#1029 UNUSED -> binding/target/arity/ownership/caller/Base/
history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 151 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP661 -- QUEUE 153 CyGameCoreUtilsInterface.cpp CLOSED / QUEUE 154 OPEN
===============================================================================
Physical parent: C031-WIP660.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue103
`CyGameCoreUtils.cpp` closure and shipped Python caller census. No web/GitHub lookup. Gameplay/XML/
Python source untouched.

QUEUE 153 RESULT
----------------
Queue153 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current interface
source is newline-normalized identical to supplied Base AdvC 1.14.

BINDING / OWNERSHIP SATURATION
------------------------------
Every active Python export was reconciled against the already-audited Queue103 wrapper declaration
and implementation. Exported names point to the intended `cy*` functions and arities match their C++
signatures. Fresh wrappers returned by `plotDirection`, `plotCardinalDirection`, `plotXY`, `plotCity`,
`getCity` and `getUnit` use `manage_new_object`, so Python owns exactly the heap wrapper that the
native bridge allocates.

The two `splot*` helpers deliberately return process-static `CyPlot` wrappers through
`reference_existing_object`. Queue103 already established that they have no shipped Python callers;
their alias/retention hazard remains extension-API-only and overlaps the static-wrapper KI#257 family
rather than becoming F708.

Queue103 likewise already closed the raw `CyPlot*`/`CyCity*` dereference assumptions, enum casts,
combat-odds bridge, and `cyUpdatePlotIndicatorSize` delegation (whose real allocation issue is F682).
This registration layer introduces no new guard removal or ownership inversion around those targets.

HELP-TEXT ONLY
--------------
Two visible help strings are stale but non-semantic:

* `getCity` says `CyPlot* (IDInfo city)` although the bound function returns `CyCity*`;
* `isReligionTech` says `int` although the bound function returns bool.

Boost.Python binds the actual C++ function pointer types, not these descriptive strings. Runtime
conversion and ownership are therefore unaffected.

HISTORY / DEDUP / CLOSURE
-------------------------
K-Mod supplied the core interface; later AdvC commits remove deprecated exports, follow native
signature/refactor changes, and practical 3792 adds the plot-indicator bridge. Supplied Base AdvC 1.14
retains the same registration table. Queue103's whole-native-wrapper audit is the decisive cross-file
oracle and no separate registration root survives.

No independent supported F708 survives. Queue153 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue154 `CvGameCoreDLL/CyGlobalContextInterface2.cpp` (73 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit the second GlobalContext binding tranche against Queue059 `CyGlobalContext.cpp`: exported
name->target/return-policy/index contracts, singleton-info ownership, current info-count dimensions,
shipped Python callers, Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP661 physical -> Queue153 CLOSED / no F708 -> 152 / 169 complete -> Queue154
CyGlobalContextInterface2.cpp OPEN -> F708/KI#1029 UNUSED -> binding/target/ownership/index/caller/Base/
history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 152 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP662 -- QUEUE 154 CyGlobalContextInterface2.cpp CLOSED / QUEUE 155 OPEN
===============================================================================
Physical parent: C031-WIP661.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue059
`CyGlobalContext.cpp`, current Python define writers and ModName/provenance consumers. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

QUEUE 154 RESULT
----------------
Queue154 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED.

BINDING TABLE SATURATION
------------------------
A mechanical parse finds 59 active `.def` exports. Every exported Python name matches the exact
`CyGlobalContext` member target; there are no duplicate names in this tranche. All return values are
scalars or copied narrow strings, so no `manage_new_object`/borrowed-object ownership policy is
needed or missing here.

The SAS-only delta versus Base AdvC 1.14 is the centrally backed mod/source metadata family:
`getModDisplayName`, folder/path, version/hash/branch/date, metadata type and dirty-state/file-count/
file-list getters. Their native implementations and ModName backing were already audited in
Queue059/Queue166-related provenance work; this interface binds each one by the same name and return
type. No split getter or stale independently computed source identity is introduced here.

DEFINE GETTER / SETTER CONTROL
------------------------------
`getDefine*` and `setDefine*` bind directly to the already-audited CyGlobalContext methods. Current
runtime Python writers modify camera/FOV and replay-minimap values. Earlier CvGlobals/current-file
audits mechanically reconciled those mutable define names with the targeted runtime cache refresh
logic and with per-file static-local caches; Queue154 adds no second caching layer.

The interface intentionally permits arbitrary third-party Python to mutate other defines at runtime.
A mod-mod can therefore violate a subsystem that has cached a define under the documented assumption
that it is stable, but no additional shipped writer/cache mismatch was found here and such generic
extension mutation is not F708.

CONSTANT / CAMERA CONTRACTS
---------------------------
The fixed gameplay constants, prerequisite-count getters, camera floats and player/team/map constants
all map one-to-one to native getters. `updateCameraStartDistance(bool)` matches its native Boolean
signature and is used in the same graphics/UI lifecycle already audited around camera define updates.
No enum/index or object lifetime crosses this interface tranche.

BASE / HISTORY / DEDUP
----------------------
The inherited part of current Queue154 is executable-identical to supplied Base AdvC 1.14. K-Mod
supplied the original interface; AdvC rearranged/removed unused constant getters and added camera
update plumbing. SAS practicals only add the ModName/source-provenance getter family. Whole-album
search finds no registration/name/return mismatch for this tranche and Queue059 supplies the native
semantic oracle.

No independent supported F708 survives. Queue154 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue155 `CvGameCoreDLL/CyAreaInterface.cpp` (62 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Queue116 already recorded one real but unshipped ownership mismatch to revisit here:
`CyArea::getTargetCity()` allocates a fresh `CyCity`, while the pending interface appeared to use a
borrowed-object policy. Reconcile that exact binding, all remaining name/arity policies and the full
shipped Python caller census before deciding whether anything crosses the current finding threshold.

Exact continuation stone:
C031-WIP662 physical -> Queue154 CLOSED / no F708 -> 153 / 169 complete -> Queue155
CyAreaInterface.cpp OPEN -> F708/KI#1029 UNUSED -> revisit getTargetCity ownership + full binding/
caller/Base/history/dedup sweep -> promote only shipped/consequential root or close cleanly -> freeze
next WIP.

Files complete: 153 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP663 -- QUEUE 155 CyAreaInterface.cpp CLOSED / QUEUE 156 OPEN
===============================================================================
Physical parent: C031-WIP662.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue116 `CyArea.cpp`,
current Python assets and local history. No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 155 RESULT
----------------
Queue155 is CLOSED with NO new shipped finding. F708 / provisional KI#1029 remains UNUSED. Current
interface is executable-identical to supplied Base AdvC 1.14.

CONFIRMED API-ONLY OWNERSHIP MISMATCH -- NOT F708
-------------------------------------------------
Queue116 parked one exact registration issue for this pass and Queue155 confirms it:

    CyArea::getTargetCity(...) -> new CyCity(...)

but the binding declares `reference_existing_object` instead of `manage_new_object`. A Python caller
therefore receives a newly allocated wrapper whose ownership is not transferred and can leak that
wrapper per successful call. This is structurally the same inverse-ownership direction as F688, but
activation is different: a complete shipped `Assets/Python` census finds zero calls to
`CyArea.getTargetCity`.

Do not promote F708 without a shipped/current producer. Preserve this as public extension-API debt;
if a future Python screen/script starts using the method, change the policy to `manage_new_object` or
return an independently managed wrapper by another explicit convention.

BINDING / INDEX CONTROLS
------------------------
The remaining active exports return scalar values only and map directly to the Queue116 wrapper
members. Player/team/bonus/AI-role arguments are cast inside the native wrapper under the same
preconditions already audited there; no interface-specific ownership or swapped target is added.
Current shipped area callers use bounded live player/team/info IDs.

Area-object lifetime across `recalculateAreas()` was already checked in Queue116: shipped map scripts
that rebuild areas reacquire wrappers afterward rather than retaining a stale `CyArea`. This interface
adds no caching layer.

`getNumHabitableTiles` is the AdvC-added wrapper and returns a scalar from the native area; no special
policy is required. Removed animal/improvement counters remain deliberately unexposed because the
native area no longer maintains those old caches.

HISTORY / DEDUP / CLOSURE
-------------------------
K-Mod supplied the broad interface. AdvC history tracks native area refactors/removal of obsolete
counters plus `getNumHabitableTiles`; supplied Base 1.14 retains the same `getTargetCity` policy.
Queue116 is the native semantic oracle and already classified this exact ownership mismatch as
unshipped API debt. No independent current F708 survives.

Queue155 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue156 `CvGameCoreDLL/CyGlobalContextInterface3.cpp` (60 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit GlobalContext info-count/info-object registrations against Queue059, especially return policies,
count/getter pairing, current enum dimensions, name->target and shipped Python caller contracts.

Exact continuation stone:
C031-WIP663 physical -> Queue155 CLOSED / no shipped F708 -> 154 / 169 complete -> Queue156
CyGlobalContextInterface3.cpp OPEN -> F708/KI#1029 UNUSED -> info binding/count/ownership/index/caller/
Base/history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 154 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP664 -- QUEUE 156 CyGlobalContextInterface3.cpp CLOSED / QUEUE 157 OPEN
===============================================================================
Physical parent: C031-WIP663.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue059
`CyGlobalContext.cpp/.h`, current Python callers and local AdvC/K-Mod history. No web/GitHub lookup.
Gameplay/XML/Python source untouched.

QUEUE 156 RESULT
----------------
Queue156 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current
`CyGlobalContextInterface3.cpp` is executable-identical to supplied Base AdvC 1.14.

INFO BINDING / OWNERSHIP SATURATION
-----------------------------------
The active table exposes the Attitude/Memory info getters, Player/Graphic option info, Hurry count,
and Concept/NewConcept/CityTab/Calendar/GameOption/MPOption/ForceControl/Season/Month/Denial info
families. Every object-returning binding uses `reference_existing_object`, which is the correct
ownership direction: these getters return pointers into GC/global info storage rather than freshly
allocated wrappers.

The apparently mismatched exported names are deliberate compatibility aliases, not wrong targets:
`getPlayerOptionsInfo` and `getPlayerOptionsInfoByIndex` both bind
`CyGlobalContext::getPlayerOptionsInfoByIndex`; `getGraphicOptionsInfo` and
`getGraphicOptionsInfoByIndex` do the same for Graphic options. Both aliases have existed together
through the inherited interface history. Current shipped Python uses the explicit `ByIndex` forms;
there are no shipped calls to the shorter aliases.

COUNT / INDEX CONTRACTS
-----------------------
Most native info getters independently reject negative/out-of-range indices and return NULL. The
PlayerOption and GraphicOption ByIndex helpers do not duplicate that bounds check, but all shipped
callers supply enum-valid indices: the Options screen iterates the corresponding
`NUM_*OPTION_TYPES` domain and its option callback receives one of those UI-generated indices.
Arbitrary third-party Python can still pass an invalid integer and reach the underlying enum-map
contract, but that is extension-API robustness rather than a shipped current defect.

The fixed-enum count/getter pairs use matching domains (`NUM_CITYTAB_TYPES`, `NUM_CALENDAR_TYPES`,
`NUM_GAMEOPTION_TYPES`, `NUM_MPOPTION_TYPES`, `NUM_FORCECONTROL_TYPES`, `NUM_DENIAL_TYPES`); the
XML-backed Concept/NewConcept/Season/Month counts use the corresponding GC counts. No count/getter
cross-enum or off-by-one pairing was found.

SHIPPED CALLER CONTROL
----------------------
Current Python caller census found live uses of Attitude/Memory, Player/Graphic option ByIndex,
Hurry, Concept/NewConcept, Calendar, GameOption, MPOption and ForceControl. Their indices originate
from matching enum loops, info counts or engine-owned enum values. CityTab/Season/Month/Denial and
the short Player/Graphic aliases have no shipped Python caller in the current tree. No live call
retains an info pointer across a GC info reload; these are process-global XML info objects during
normal gameplay.

BASE / HISTORY / DEDUP
----------------------
K-Mod supplied the broad registration table. AdvC history mainly follows enum/info refactors and
adds `IsGraphicsInitialized`; practical 1402/1469-era changes preserve both option aliases, and
later practicals only normalize comments/refactors. Supplied Base AdvC 1.14 retains the exact current
binding table. Queue059 is the native semantic oracle and supplies no contradictory ownership or
index contract. Whole-album search finds no independent registration root to reopen here.

No supported consequential F708 survives. Queue156 is CLOSED.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue157 `CvGameCoreDLL/CyGameTextMgrInterface.cpp` (57 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit the final GameTextMgr registration layer against Queue095 `CyGameTextMgr.cpp` and Queue003
`CvGameTextMgr.cpp`: exported names/targets, wrapper-pointer ownership, enum/argument arity, current
shipped Python callers, Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP664 physical -> Queue156 CLOSED / no F708 -> 155 / 169 complete -> Queue157
CyGameTextMgrInterface.cpp OPEN -> F708/KI#1029 UNUSED -> binding/ownership/arity/caller/Base/history/
dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 155 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP665 -- QUEUE 157 CyGameTextMgrInterface.cpp CLOSED / QUEUE 158 OPEN
===============================================================================
Physical parent: C031-WIP664.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue095
`CyGameTextMgr.cpp/.h`, Queue003 native GameTextMgr audit, current Python callers and local history.
No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 157 RESULT
----------------
Queue157 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED.

REGISTRATION / NATIVE PARITY
----------------------------
Every active Python export binds the intended `CyGameTextMgr` member. All returned help/text values
are `std::wstring` by value, so this interface needs no object-return ownership policy. CyCity,
CyUnit, TradeData and CyDeal pointer arguments are consumed synchronously by the native wrappers;
Queue095 already established their NULL/lifetime handling and the copied-string return boundary.

Current SAS differs from supplied Base AdvC 1.14 only by the deliberate
`getSpecificUnitHelpOmitOwner` export. Its Python name, four-argument signature and native target
match exactly, and its sole shipped Military Advisor caller supplies a live unit plus the configured
promotion icon size. This is the interface half of the already-audited KI#130 display repair, not a
new ownership or arity surface.

STALE HELP STRINGS -- DOCUMENTATION ONLY
----------------------------------------
The registration contains several inaccurate descriptive strings that do NOT control Boost.Python
argument binding:

- `getSpecialistHelp` is correctly bound to `(int iSpecialist, bool bCivilopediaText)`, but its help
  string describes the TradeData/player arguments belonging to `getTradeString`.
- `getTradeString` is correctly bound to `(TradeData*, int iPlayer1, int iPlayer2)`, but its help
  string describes the Specialist helper.
- `getReligionHelpCity`'s help string lists only five Python arguments although the actual member has
  six, including `bNoStateReligion`. The shipped Policy Advisor calls the real six-argument binding
  successfully.

Queue095 had already identified these exact strings and explicitly left them for Queue157. They can
be corrected opportunistically for Python API documentation, but no runtime F708 follows from them.

CALLER / ENUM / LIFETIME CONTROL
--------------------------------
Current Python callers exercise the major help families with the same valid IDs/sentinels audited in
Queue095. Optional CyCity pointers are commonly passed as `None` and are explicitly NULL-safe in the
wrapper. Specific-unit helpers validate the wrapper and underlying CvUnit. Trade/deal formatting
consumes borrowed structures only during the call. No interface registration changes an enum domain,
adds a retained pointer or returns a newly allocated wrapper.

BASE / HISTORY / DEDUP
----------------------
The inherited binding table is supplied unchanged by Base AdvC 1.14; K-Mod/AdvC history tracks the
same wrapper additions/refactors already classified in Queue095. The only SAS interface delta is the
intentional omit-owner helper above. Whole-album dedup points the native semantics to Queue095 and
finds no independent registration root.

Queue157 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue158 `CvGameCoreDLL/CyCityInterface2.cpp` (44 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit the second City registration tranche against completed Queue024 `CyCity.cpp`: exact exported
name->target/arity, object-return ownership, city lifetime, current Python callers, Base/history and
whole-album dedup.

Exact continuation stone:
C031-WIP665 physical -> Queue157 CLOSED / no F708 -> 156 / 169 complete -> Queue158
CyCityInterface2.cpp OPEN -> F708/KI#1029 UNUSED -> binding/ownership/arity/caller/Base/history/dedup
sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 156 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP666 -- QUEUE 158 CyCityInterface2.cpp CLOSED / QUEUE 159 OPEN
===============================================================================
Physical parent: C031-WIP665.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, completed Queue024
`CyCity.cpp`, current Python callers and local history. No web/GitHub lookup. Gameplay/XML/Python
source untouched.

QUEUE 158 RESULT
----------------
Queue158 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current
`CyCityInterface2.cpp` is executable-identical to supplied Base AdvC 1.14.

BINDING / TARGET SATURATION
---------------------------
All 30 active exports map name-for-name to the intended `CyCity` member. This second interface tranche
contains only scalar/void city-state operations: Great Person progress/projection, specialist counts,
free specialists, per-building yield/commerce/happy/health overrides and liberation. It returns no
fresh wrapper or borrowed aggregate object, so there is no Boost.Python ownership-policy surface.

Queue024 already audited the corresponding native wrapper targets and their current city-pointer /
enum assumptions. Queue158 adds no swapped BuildingClass/Yield/Commerce/Specialist target and no
argument reorder.

LIBERATION SIGNATURE CONTROL
----------------------------
The native methods are `getLiberationPlayer(bool bConquest)` and `liberate(bool bConquest)`. Their
registration help strings misleadingly say `int ()` / `void ()`, but the actual member-function
pointers retain the Boolean argument. Current shipped callers of `getLiberationPlayer` pass
`false`/`False` explicitly. The old commented no-argument MainInterface call is not active. Thus this
is stale API documentation, not a runtime arity defect.

OTHER STALE HELP STRINGS
------------------------
Several mutating specialist methods are described as returning `int` although their native return is
`void`, and a few descriptive strings omit a closing parenthesis. Again these strings are metadata
only; Boost.Python derives the callable type from `&CyCity::...`. No current Python behavior depends
on the prose signature.

BASE / HISTORY / DEDUP
----------------------
This file was split out of the first City interface purely to keep the original registration unit from
becoming too large. The binding table is unchanged in supplied Base AdvC 1.14 and the relevant native
semantics are owned by Queue024. Whole-album search finds no independent registration root here.

Queue158 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue159 `CvGameCoreDLL/CyMapGeneratorInterface.cpp` (43 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit the map-generator singleton registration against Queue127 `CyMapGenerator.cpp` and Queue045
`CvMapGenerator.cpp`: constructor/singleton lifetime, exported name->target/arity, plot/list pointer
contracts, shipped map/Python callers, Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP666 physical -> Queue158 CLOSED / no F708 -> 157 / 169 complete -> Queue159
CyMapGeneratorInterface.cpp OPEN -> F708/KI#1029 UNUSED -> singleton/binding/arity/caller/Base/history/
dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 157 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP667 -- QUEUE 159 CyMapGeneratorInterface.cpp CLOSED / QUEUE 160 OPEN
===============================================================================
Physical parent: C031-WIP666.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, completed Queue127
`CyMapGenerator.cpp`, Queue045 `CvMapGenerator.cpp`, shipped map/Python callers and local history.
No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 159 RESULT
----------------
Queue159 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current
`CyMapGeneratorInterface.cpp` is executable-identical to supplied Base AdvC 1.14.

SINGLETON / REGISTRATION CONTROL
--------------------------------
`python::class_<CyMapGenerator>("CyMapGenerator")` uses the wrapper's ordinary default constructor,
which points `m_pMapGenerator` at `CvMapGenerator::GetInstance()`. The interface returns no object
pointers and therefore needs no Boost.Python ownership policy. All 20 active exports map name-for-name
to the intended wrapper methods with matching callable arity.

Queue127 already established that every wrapper except `doRiver` guards a missing native generator,
and ordinary Python construction supplies the singleton. Queue159 does not add a second singleton,
borrowed lifetime or stale cached generator pointer.

PARKED WRAPPER SHARPNESS -- NO NEW ACTIVATION
---------------------------------------------
Queue127 recorded two real public-API robustness edges:
- `setPlotTypes` trusts that the supplied Python sequence contains one entry per map plot; native
  `CvMapGenerator::setPlotTypes` then indexes the resulting raw array by every plot index.
- `doRiver` dereferences the supplied `CyPlot*` without a NULL guard.

This interface exposes those methods exactly as audited; it does not itself make either condition
reachable from shipped code. Current Pangaea/PerfectMongoose list construction is map-sized, and no
shipped caller passes `None` to `doRiver`. Keep both as extension-API hardening notes rather than F708.

SHIPPED CALLER / NATIVE CONTROL
-------------------------------
Current shipped calls are the WorldBuilder `addBonuses` bridge and the Pangaea/PerfectMongoose map
objects already traced in Queue127. Enum/coordinate/list inputs obey their native contracts. The
WorldBuilder call simply enters the already-audited Queue045 bonus placement path; no interface-level
state or ownership changes its semantics.

BASE / HISTORY / DEDUP
----------------------
The registration table is inherited intact from Base AdvC 1.14/Firaxis-K-Mod lineage. Queue127 and
Queue045 are the wrapper/native semantic oracles and already own the only sharp input contracts.
Whole-album search finds no independent binding or singleton root here.

Queue159 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue160 `CvGameCoreDLL/CvHallOfFameInfo.cpp` (39 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit Hall-of-Fame info-object construction/reset/read/write ownership and global-registration
interaction against the recently confirmed F707/KI#1028 interface/lifecycle root, Base/Civ4CE history
and current callers. Dedup any manifestation of the same replacement/destructor race rather than
renumbering it.

Exact continuation stone:
C031-WIP667 physical -> Queue159 CLOSED / no F708 -> 158 / 169 complete -> Queue160
CvHallOfFameInfo.cpp OPEN -> F708/KI#1029 UNUSED -> ctor/reset/read-write/global-registration/
lifetime/Base/history/dedup sweep -> either promote consequential independent F708 or close cleanly
-> freeze next WIP.

Files complete: 158 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP668 -- QUEUE 160 CvHallOfFameInfo.cpp CLOSED / F707 DEDUP / QUEUE 161 OPEN
===============================================================================
Physical parent: C031-WIP667.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, F707/KI#1028 proof
from Queue148, current CvGame reset/registration and Hall-of-Fame Python-screen callers, local
practical-1430 history. No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 160 RESULT
----------------
Queue160 is CLOSED with NO independent new finding. F708 / provisional KI#1029 remains UNUSED.
The substantive Hall-of-Fame lifecycle defect in this native container is exactly F707/KI#1028 and
is deduplicated/broadened here rather than renumbered.

F707 NATIVE-HALF CONFIRMATION
-----------------------------
`CvHallOfFameInfo` constructor registers `this` through `CvGame::setHallOfFame`. Its destructor calls
`uninit()`, and `uninit()` unconditionally performs `setHallOfFame(NULL)`, clears the global HoF-screen
flag and deletes the owned replay vector. This is the exact native invariant failure already frozen
under F707: an old container that is no longer the registered instance can still clear a newer
container's registration when the Python Hall-of-Fame screen replaces wrappers.

Queue160 also confirms F707's reset variant. `CvGame::reset()` may call `uninit()` while the owning
Python wrapper itself remains alive. If a replacement Hall-of-Fame container is later constructed
before that old wrapper is destroyed, the delayed old destructor calls `uninit()` again and can clear
the replacement's registration. `uninit()` is memory-idempotent for its own cleared vector, but its
global deregistration is not identity-aware. This is one F707 root, not F708.

REPLAY OWNERSHIP / INDEX CONTROLS
---------------------------------
The container owns the `CvReplayInfo*` entries supplied by `gDLL->loadReplays` and deletes them during
`uninit`. `getReplayInfo(i)` returns a borrowed pointer and does not range-check the index. Queue148
already proved every shipped Hall-of-Fame consumer uses an index from `0 <= i < getNumGames()` or an
index previously captured from such a loop. Arbitrary third-party invalid indices remain extension
hardening only.

`loadReplays()` does not explicitly clear an already-populated vector. Shipped screen code creates a
fresh `CyHallOfFameInfo` for each load rather than invoking `loadReplays()` twice on one live native
container; the shipped recreation defect is F707's overlapping-container registration race. Without
establishing the closed-source EXE loader's append/replace behavior or a shipped repeated-load caller,
this is not an independent F708.

BASE / HISTORY / DEDUP
----------------------
Current Queue160 differs from supplied Base AdvC 1.14 only by newline convention. Practical 1430
introduced registration, destructor/uninit cleanup and the CvGame reset hook together; later history
retains the unconditional deregistration. Queue148's F707 proof already owns the interaction with the
pre-existing Python wrapper recreation order. No second native ownership/root survives.

Queue160 is CLOSED with F707/KI#1028 deduplicated and F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue161 `CvGameCoreDLL/CvGameCoreDLL.cpp` (38 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit DLL entry/bootstrap/shutdown contracts, global interface allocation/deallocation, module-handle
and thread/library lifetime, export boundary, Base/Civ4CE/K-Mod history and whole-album dedup. Because
this is process bootstrap code, distinguish a real supported startup/shutdown failure from generic
Windows-loader robustness.

Exact continuation stone:
C031-WIP668 physical -> Queue160 CLOSED / F707 DEDUP / no F708 -> 159 / 169 complete -> Queue161
CvGameCoreDLL.cpp OPEN -> F708/KI#1029 UNUSED -> DLL bootstrap/shutdown/global-interface/export/
lifetime/Base/history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze
next WIP.

Files complete: 159 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP669 -- QUEUE 161 CvGameCoreDLL.cpp CLOSED / QUEUE 162 OPEN
===============================================================================
Physical parent: C031-WIP668.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, local Civ4CE/K-Mod
baselines and AdvC/SAS history. No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 161 RESULT
----------------
Queue161 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED.

DLL ENTRY / TIMER LIFECYCLE
---------------------------
The file contains only `DllMain`. `DLL_PROCESS_ATTACH` requests 1 ms multimedia-timer resolution via
`timeBeginPeriod(1)`; `DLL_PROCESS_DETACH` pairs it with `timeEndPeriod(1)`. Thread attach/detach are
intentionally inert. The module handle and reserved pointer are unused and no pointer/state from the
loader callback is retained after return.

In assert-enabled builds the `MMRESULT` is captured and checked; in non-assert builds its declaration
is intentionally omitted while the `timeBeginPeriod` call remains. The `FAssertMsg` macro compiles
away consistently, so there is no release-build use of an undeclared value. Failure of the timer
request is diagnostic in debug and does not create a second cleanup/lifetime state in current code.

BASE / CIV4CE / K-MOD CONTROL
-----------------------------
Civ4CE/Firaxis used the same timer pair and additionally cleared `GC.setDLLIFace(NULL)` on process
detach. K-Mod's later baseline already omits that detach clear, and current AdvC global/interface
shutdown architecture retains the K-Mod direction. No current supported DLL-side consumer after
process detach was established, so the historical omission is not evidence of a surviving use-after-
shutdown defect.

AdvC moved profiler/memory-manager code out of this file and practical 2547 merely compile-gated the
assert-only result variable. Current SAS practical 5961 only reformatted the `DllMain` signature;
executable behavior is otherwise supplied Base AdvC 1.14.

ROBUSTNESS NOTES / DEDUP
------------------------
Calling console/debug helpers from `DllMain` and Windows loader-lock restrictions are general platform
robustness concerns, but this inherited path is exercised on every successful BtS DLL load and no
current deadlock/failure chain was established. Timer begin/end pairing is process-scoped rather than
per-thread. No previously owned startup/shutdown root is independently reopened here.

Queue161 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue162 `CvGameCoreDLL/CyArtFileMgrInterface.cpp` (31 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit ArtFileMgr Python registration against Queue142 `CyArtFileMgr.cpp` and Queue111
`CvArtFileMgr.cpp`: exported names/targets, borrowed global art-info ownership, current info-index
bounds/callers, Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP669 physical -> Queue161 CLOSED / no F708 -> 160 / 169 complete -> Queue162
CyArtFileMgrInterface.cpp OPEN -> F708/KI#1029 UNUSED -> binding/ownership/index/caller/Base/history/
dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 160 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP670 -- QUEUE 162 CyArtFileMgrInterface.cpp CLOSED / QUEUE 163 OPEN
===============================================================================
Physical parent: C031-WIP669.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, completed Queue142
`CyArtFileMgr.cpp`, Queue111 `CvArtFileMgr.cpp`, current Python art consumers. No web/GitHub lookup.
Gameplay/XML/Python source untouched.

QUEUE 162 RESULT
----------------
Queue162 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current interface
is executable-identical to supplied Base AdvC 1.14.

OWNERSHIP / REGISTRATION CONTROL
--------------------------------
Every ArtInfo-returning method (`Interface`, `Movie`, `Misc`, `Unit`, `Building`, `Civilization`,
`Leaderhead`, `Bonus`, `Improvement`, `Terrain`, `Feature`) returns a pointer owned by global
`ARTFILEMGR` storage. Every binding correctly uses `reference_existing_object`; none allocates a new
ArtInfo wrapper requiring `manage_new_object`. The default `CyArtFileMgr` wrapper itself points at the
global manager as already established in Queue142.

TAG / NULL / CALLER CONTROL
---------------------------
Queue111 mechanically checked the current literal art-tag universe (328 literal native lookups with
zero missing tags), and Queue142 verified current dynamic Python consumers derive their tags from
current XML or known interface constants. Shipped callers immediately consume the borrowed info for
paths/buttons and do not call `Reset`/`buildArtFileInfoMaps` while retaining those proxies.

An invalid third-party tag can return NULL and an incautious Python caller can dereference it; a
third-party explicit manager reset could likewise invalidate assumptions. Those are public-extension
robustness surfaces already parked by Queue142, not current F708 activation.

HELP / HISTORY / DEDUP
----------------------
The file header's comment says "published python interface for CyArea" despite registering
`CyArtFileMgr`; this is a copy/paste comment only. Runtime names and targets are correct. Current Base
equality plus Queue142/111 provides the complete native/wrapper oracle; no independent registration
root survives.

Queue162 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue163 `CvGameCoreDLL/CyMessageControlInterface.cpp` (25 queued/current
physical lines), OPEN under F708 / provisional KI#1029 unused.

Audit the message-control singleton registration against Queue115 `CvMessageControl.cpp` and the
CyMessageControl wrapper: exported name->target/arity, explicit-player/active-player sender contracts,
Python caller inputs, Base/history and Queue044 sender/receiver dedup.

Exact continuation stone:
C031-WIP670 physical -> Queue162 CLOSED / no F708 -> 161 / 169 complete -> Queue163
CyMessageControlInterface.cpp OPEN -> F708/KI#1029 UNUSED -> singleton/binding/arity/caller/Base/history/
dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 161 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP671 -- QUEUE 163 CyMessageControlInterface.cpp CLOSED / QUEUE 164 OPEN
===============================================================================
Physical parent: C031-WIP670.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, `CyMessageControl.cpp`,
completed Queue115 `CvMessageControl.cpp`, Queue044 `CvMessageData.cpp` and current Python producers.
No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 163 RESULT
----------------
Queue163 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current interface
and wrapper are executable-identical to supplied Base AdvC 1.14 apart from newline convention.

REGISTRATION / MESSAGE CONTRACT
-------------------------------
All 13 active Python exports map to the intended `CyMessageControl` wrapper with matching callable
arity: PushOrder, DoTask, TurnComplete, UpdateCivics, Research, PlayerOption, espionage weighting,
AdvancedStart, ModNetMessage, Convert, EmpireSplit and the two connection-query helpers. No object
return requires an ownership policy.

Queue115 already reconciled the corresponding native senders against all Queue044 `CvNet*` receiver
constructors and serialized field order. Queue163 introduces no same-typed ID swap, explicit-player /
active-player substitution or network-message ownership layer above those contracts.

PYTHON LIST SHARPNESS -- API ONLY
---------------------------------
`CyMessageControl::sendUpdateCivics` converts the supplied Python list to a raw array and then reads
one entry for every `CivicOptionTypes` value without separately checking the sequence length. The
shipped Policy Advisor passes `self.m_paeDisplayPolicies`, maintained as the complete one-civic-per-
option selection map, so current UI activation satisfies the contract. A deliberately short
third-party list can make the wrapper read beyond the conversion buffer; retain this as public API
hardening rather than F708 absent a shipped producer.

The wrapper's `delete[] PYiCivics` is the existing C2C-derived fix for the converted sequence buffer;
there is no current leak/double-delete on the supported path.

CALLER / BASE / DEDUP
---------------------
Current shipped callers supply UI/engine-derived valid player/team/city/tech/task/action values. The
modified PushOrder bShift/bCtrl mapping is inherited Base behavior and pairs with Queue115's native
save/pop/position protocol; no registration change alters it. Existing network roots F674/F675 and
other Queue044 receiver findings remain owned by their receiver/protocol causes rather than this thin
Python export layer.

Queue163 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue164 `CvGameCoreDLL/CyHallOfFameInfo.cpp` (24 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit this by-value Hall-of-Fame wrapper against Queue160 and F707/KI#1028: constructor/destructor
ordering, fresh `CyReplayInfo` wrapper ownership, replay index and Python replacement lifecycle. Dedup
F707 unless a genuinely separate wrapper-body cause survives.

Exact continuation stone:
C031-WIP671 physical -> Queue163 CLOSED / no F708 -> 162 / 169 complete -> Queue164
CyHallOfFameInfo.cpp OPEN -> F708/KI#1029 UNUSED -> wrapper/by-value-HoF/replay-return/index/F707/Base/
history/dedup sweep -> either promote independent F708 or close cleanly -> freeze next WIP.

Files complete: 162 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP672 -- QUEUE 164 CyHallOfFameInfo.cpp CLOSED / F707 DEDUP / QUEUE 165 OPEN
===============================================================================
Physical parent: C031-WIP671.
Source identity: same reconciled AdvCiv-SAS snapshot, supplied Base AdvC 1.14, Queue160 native
Hall-of-Fame container, Queue148 interface and F707/KI#1028 lifecycle proof. No web/GitHub lookup.
Gameplay/XML/Python source untouched.

QUEUE 164 RESULT
----------------
Queue164 is CLOSED with NO independent new finding. F708 / provisional KI#1029 remains UNUSED.
The wrapper body confirms and deduplicates F707/KI#1028.

BY-VALUE CONTAINER / F707 CONFIRMATION
--------------------------------------
`CyHallOfFameInfo` contains `CvHallOfFameInfo m_hallOfFame` by value. Its explicit constructor body is
empty, so the embedded native container's constructor runs first and immediately registers itself
with `CvGame`. The compiler-generated `CyHallOfFameInfo` destructor later destroys that embedded
container, whose destructor calls `uninit()` and unconditionally clears the game's Hall-of-Fame
registration.

This makes the Python replacement order proved in F707 structural, not speculative: constructing the
new Python wrapper necessarily constructs/registers its new embedded container before assignment can
release the old wrapper; destroying the old wrapper then destroys its embedded container and can
clear the newer registration. Queue164 therefore strengthens F707's exact cause but does not create a
second root.

REPLAY WRAPPER OWNERSHIP
------------------------
`getReplayInfo(i)` allocates `new CyReplayInfo(m_hallOfFame.getReplayInfo(i))`. Queue148 correctly
binds that fresh wrapper with `manage_new_object`. The new `CyReplayInfo` wrapper borrows the
underlying `CvReplayInfo` owned by `m_hallOfFame`; Queue080/148 established that shipped consumers keep
the Hall-of-Fame container alive while using those replay wrappers.

The native replay index remains unchecked, but shipped Hall-of-Fame callers bound it through
`getNumGames()` as already proved in Queue148/160. No Queue164-specific invalid index producer exists.

BASE / DEDUP / CLOSURE
----------------------
Current Queue164 is executable-identical to supplied Base AdvC 1.14. Its only meaningful lifecycle
interaction is practical-1430's native `CvHallOfFameInfo` registration/uninit behavior already owned
by F707. No separate allocation, stale replay pointer or wrapper-body cause survives.

Queue164 is CLOSED with F707/KI#1028 deduplicated and F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue165 `CvGameCoreDLL/CyRandomInterface.cpp` (20 queued/current physical
lines), OPEN under F708 / provisional KI#1029 unused.

Audit RNG Python registration against completed Queue130 `CvRandom.cpp`/CyRandom wrapper: object
ownership, seed/log-string/upper-bound semantics, synchronized-vs-map RNG exposure, shipped callers,
Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP672 physical -> Queue164 CLOSED / F707 DEDUP / no F708 -> 163 / 169 complete -> Queue165
CyRandomInterface.cpp OPEN -> F708/KI#1029 UNUSED -> RNG binding/ownership/seed/range/caller/Base/
history/dedup sweep -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 163 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP673 -- QUEUE 165 CyRandomInterface.cpp CLOSED / NO F708 / QUEUE 166 OPEN
===============================================================================
Physical parent: C031-WIP672.
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, local supplied Base
AdvC 1.14 and current shipped Python/map scripts. No web/GitHub lookup. Gameplay/XML/Python source
untouched.

QUEUE 165 RESULT
----------------
Queue165 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. Current
`CyRandomInterface.cpp` is byte/executable-identical to supplied Base AdvC 1.14 apart from newline
convention.

BINDING / RANGE CONTRACT
------------------------
The interface exposes the native `CvRandom` class directly with only three methods: `get(int,
TCHAR const*)`, `getSeed()` and `init(unsigned long)`. There is no fresh-wrapper return and therefore
no Boost.Python ownership policy to misdeclare.

Current `CvRandom::get(int,...)` documents and enforces the inherited half-open [0,iNum) contract with
an upper range of 65535. The SAS negative/zero-range safety net returns zero without advancing for
`iNum <= 0`, while `FAssertBounds` retains the >65535 diagnostic before the unsigned-short conversion.
A shipped Python/map-script census found no concrete literal range above this limit; the largest
literal calls are exactly 65535 (PerfectMongoose and savemap.py), with common map-generator calls at
30000/10000 and smaller. Dynamic ranges derive from map dimensions, vector/list lengths, player
counts, or bounded local geometry; no current shipped path was established above 65535.

SEED / RNG-DOMAIN CONTRACT
--------------------------
`getSeed()` has shipped observational use in OOSLogger for both map and Soren RNG state. The sole
shipped Python call to `CvRandom.init(seed)` is the explicit AIAutoPlay benchmark seed input on
`getSorenRand()`. The source comment deliberately exposes `init` because `reseed` is not bound and the
benchmark needs deterministic reruns. It is therefore an intentional user-requested synchronized-RNG
mutation, not an accidental map/game RNG crossover.

Map scripts obtain `getMapRand()` and consume it for deterministic map generation. No shipped map
script calls `init` on the engine map RNG except PerfectMongoose's own local `self.mapRand` setup path,
which resolves to the same intended map-generation seed machinery and does not establish a
registration defect in this interface.

BASE / DEDUP / CLOSURE
----------------------
Queue130 already audited native `CvRandom` arithmetic, serialization and SAS GameRecord RNG tracking;
Queue165 adds no wrapper mismatch above those contracts. The direct class binding has no lifetime
wrapper layer, all three exported member pointers match their Python names/arity, and no separate
seed/range defect survives the shipped-caller census.

Queue165 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue166 `CvGameCoreDLL/ModName.cpp`, OPEN under F708 / provisional
KI#1029 unused. The original queue snapshot counted only the old small AdvC implementation; current
SAS has expanded this file substantially with source-version/dirty-tree resolution. Audit the current
body as authoritative: path parsing, marker/Git fallback ordering, CreateProcess/pipe ownership,
command construction/quoting, cache/reset behavior, version-anchor arithmetic, dirty-file parsing,
archive/runtime callers, current metadata file reality, Base/Taurus ancestry and whole-album dedup.

Exact continuation stone:
C031-WIP673 physical -> Queue165 CLOSED / no F708 -> 164 / 169 complete -> Queue166 ModName.cpp OPEN ->
F708/KI#1029 UNUSED -> full current source-version resolver/path/process/cache/metadata/caller/Base/
history/dedup audit -> either promote consequential F708 or close cleanly -> freeze next WIP.

Files complete: 164 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP674 -- QUEUE 166 ModName.cpp CLOSED / NO F708 / QUEUE 167 OPEN
===============================================================================
Physical parent: C031-WIP673.
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, supplied Base AdvC
1.14, local commit-diff corpus for practical 6357+ source-version work, current docs/.gitattributes
and runtime callers. No web/GitHub lookup. Gameplay/XML/Python source untouched.

QUEUE 166 RESULT
----------------
Queue166 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. The original queue
snapshot counted the old 18-line AdvC implementation; current SAS has expanded `ModName.cpp` to 472
physical lines with the centralized source/version resolver, so this pass audited the full current
body rather than the stale queue size.

PATH / CACHE LIFECYCLE
----------------------
The inherited `update()` path cache remains compatible with the EXE-provided `Mods\<folder>\` shape.
SAS source details are reset together whenever `CvGlobals::setDLLIFace()` supplies a new DLL interface
and refreshes the loaded mod path. `resolveSourceDetails()` marks a resolution attempt before doing
filesystem work, but an early pathless result cannot become permanently stale because the later
`update()` invalidates the cache. Actual BBAI/SASGameRecord/Victory/Sevopedia/Python consumers run
after DLL/mod initialization.

`getAbsoluteModPath()` accepts an already-absolute drive/UNC path or reconstructs a relative mod path
from the running EXE directory. It rejects truncated/failed `GetModuleFileNameA`, strips trailing
separators, and therefore also avoids a trailing backslash escaping the closing quote in the later
`git.exe -C "..."` command line.

PROCESS / HANDLE CONTRACT
-------------------------
`runHiddenSourceCommand()` balances its pipe/null/process/thread handles on every normal success and
early-failure branch inspected. The read end is explicitly made non-inheritable, the parent closes
its write handle after `CreateProcessA`, stdout is drained before waiting for process completion, and
stderr/stdin are redirected to NUL. Thus the ordinary fixed Git commands do not create the common
pipe-buffer deadlock or persistent-handle leak pattern.

The command arguments themselves are fixed source strings; only the loaded mod path is interpolated.
Windows filenames cannot contain a literal double quote, and no shell is invoked, so shell
metacharacters in a folder name are not command injection. Git top-level output is normalized for
slash/case/trailing-separator comparison before the checkout is trusted.

ARCHIVE / LIVE-GIT SOURCE CONTRACT
----------------------------------
The archive marker parser accepts version-describe only when it is no longer an unsubstituted
`$Format:` placeholder and requires a syntactically exact 40-hex commit before treating archive
metadata as authoritative. The tracked `.gitattributes` marks `/Assets/SASModVersion.txt`
`export-subst`; practical-6357 history shows the expected marker placeholders. The light-source ZIP
itself intentionally does not carry the runtime marker, so absence from this analysis package is not
evidence about the player/source archive path.

`resolveVersionFromAnchorDescribe()` correctly handles both an exact
`SAS_VERSION_ANCHOR_<base>` tag and the `<base>-<distance>-g<sha>` form used by Git describe; the
base+distance scheme matches the documented practical commit-count convention provided the immutable
anchor is maintained as documented. Malicious/hand-edited gigantic numeric marker values could
overflow the int parser, but that is outside current exported-marker data and not promoted.

For a real worktree, the resolver requires a local `.git`, verifies `rev-parse --show-toplevel`
against the exact loaded mod path, validates the full HEAD SHA, distinguishes shallow/full/unknown
history before exposing the count-derived version, and deliberately ignores untracked files when
computing dirty state. Dirty-status parsing preserves Git's two status columns in display text while
keeping the uncapped changed-file count separately. The display-char limit can be exceeded slightly
by the final `... (+N more)` suffix, but that is bounded presentation debt, not a source-proven buffer
or gameplay failure.

CALLERS / BASE / HISTORY / FALSE POSITIVES
-------------------------------------------
The old AdvC folder-name/path behavior remains the Base ancestor; the large SAS delta is practical
6357+ provenance infrastructure. Later practicals remove redundant short-SHA storage and correct
document/sample anchor identity without introducing a second cache or conflicting source truth.
Current Python `CvModName`, Sevopedia, replay/settings and logging surfaces all read the same cached
primary values.

Potential issues deliberately not promoted: source metadata is descriptive rather than a security
attestation (a hand-edited archive marker can lie if it supplies a 40-hex string); unusual third-party
paths without the inherited trailing separator can make the old folder-name extraction imperfect;
non-ASCII Windows case-folding is only ASCII-normalized; old Git versions can yield
`gitHistoryUnknown`; and child-handle inheritance follows the Win32 process-wide inheritable-handle
model. No current shipped path turns these into a consequential SAS defect.

Queue166 is CLOSED with F708 unused.

QUEUE ADVANCE
-------------
Ordinary order advances to Queue167 `CvGameCoreDLL/PlotRadiusIterator.cpp`, OPEN under F708 /
provisional KI#1029 unused. The .cpp contains only the CvUnit bridge and explicit template
instantiations; audit the associated header as the actual implementation: spiral/scan traversal,
center/radius bounds, wrap/edge skipping, recursion depth, coordinate constructors, distance helpers,
all current callers, Base/history and whole-album dedup.

Exact continuation stone:
C031-WIP674 physical -> Queue166 CLOSED / no F708 -> 165 / 169 complete -> Queue167
PlotRadiusIterator.cpp OPEN -> F708/KI#1029 UNUSED -> implementation-header/traversal/radius/wrap/
recursion/caller/Base/history/dedup sweep -> either promote consequential F708 or close cleanly ->
freeze next WIP.

Files complete: 165 / 169. Files open: 1 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP675 -- QUEUE 167 PlotRadiusIterator.cpp CLOSED / QUEUE 168 ALREADY COMPLETE / QUEUE 169 OPEN
===============================================================================
Physical parent: C031-WIP674.
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, supplied Base AdvC
1.14, local AdvC practical-history diff corpus and all current iterator callers. No web/GitHub lookup.
Gameplay/XML/Python source untouched.

QUEUE 167 RESULT
----------------
Queue167 is CLOSED with NO new finding. F708 / provisional KI#1029 remains UNUSED. The 16-line .cpp
is only the CvUnit bridge plus explicit template instantiations; the associated
`PlotRadiusIterator.h` contains the actual traversal implementation and was audited as part of this
queue. After comments/whitespace are stripped, current SAS and supplied Base AdvC 1.14 are
executable-semantic identical.

SPIRAL POSITION / CENTER CONTRACT
---------------------------------
`SpiralPlotIterator` uses `m_iMaxPos = (2*r+1)^2 - 1`, starts `m_iPos=-1`, exposes the center directly
when requested and then advances through exactly the remaining square offsets. A mechanical mimic of
the current direction/change-count algorithm enumerated every square coordinate exactly once for
radii 0 through 19, including radius 0. Excluding the center now calls `computeNext()` without
shrinking `m_iMaxPos`.

That detail has direct history: practical 3910 is titled `Fix SquareIter omitting one tile` and
removed the old `m_iMaxPos--` from the `!bIncludeCenter` branch. Current code therefore carries the
known correction rather than the old omission root.

Circle mode builds the same complete square and recursively skips positions whose `plotDistance`
exceeds the radius. Wrapped maps deliberately map off-domain logical offsets through
`CvMap::plotValidXY`; current shipped radii remain far below the world dimensions at which a wrapped
physical plot could be revisited through multiple logical offsets.

EDGE / RECURSION / SCAN-LINE CONTRACT
--------------------------------------
Non-wrapped off-map offsets are recursively skipped. Current call radii are small gameplay/map
ranges (culture, visibility, air/nuke/patrol, Worker/AI search, normalization/TrueStarts and bonus
spacing); no supported radius approaches a recursion depth capable of exhausting the stack. Negative
radii would violate the iterator's implicit contract and can especially break ScanLine termination,
but no current caller supplies one.

The only two `ScanLinePlotIterator` callers are `CvCity::setCultureLevel`; the old-level loop is
explicitly guarded by `eOldValue != NO_CULTURELEVEL`, and the new-level loop by
`getCultureLevel() != NO_CULTURELEVEL`, so its radius is a real nonnegative CultureLevel enum.

The coordinate constructor dereferences `GC.getMap().plot(x,y)` and therefore assumes valid center
coordinates. Current concrete use is the nuke-target square in `CvUnit::canNukeAt`, whose target
coordinates come through the ordinary valid map/UI target contract; other current call sites use a
CvPlot/CvUnit center directly.

DISTANCE / CALLER / HISTORY CONTROLS
------------------------------------
`currXDist/currYDist` intentionally use the logical unwrapped spiral coordinates rather than wrapped
map distance, matching their use as offset/culture geometry. `currStepDist/currPlotDist` operate on
the resolved current plot and center when a current plot exists. Callers only query these while
`hasNext()` is true.

History from practical 1916 introduction through the unit-plot bridge, naming/inline refactors,
ScanLine additions and practical 3910 omission repair reconciles with current behavior. Major current
users were already independently audited in CvUnit/CvPlot/CvCity/CvMap/CvMapGenerator/TrueStarts/
StartingPositionIteration/AI files; no new iterator-layer cause emerges above their contracts.

Queue167 is CLOSED with F708 unused.

QUEUE 168 CONTROL
-----------------
Queue168 `ArithmeticTraits.cpp` was already fully completed at C031-WIP509 in the priority reloop and
remains closed. It is not reopened or double-counted here.

QUEUE ADVANCE
-------------
Ordinary order advances directly to the final pending Queue169 `CvGameCoreDLL/_precompile.cpp` (5
queued/current physical lines), OPEN under F708 / provisional KI#1029 unused. Audit the translation-
unit/precompiled-header role, project references, side effects/static initialization, Base/current
identity and final whole-album closure accounting.

Exact continuation stone:
C031-WIP675 physical -> Queue167 CLOSED / no F708 -> Queue168 already CLOSED at WIP509 -> 167 / 169
complete -> Queue169 _precompile.cpp OPEN -> F708/KI#1029 UNUSED -> precompile/project/static-init/
Base/dedup sweep -> either promote a real final root or close Queue169 -> freeze final album WIP.

Files complete: 167 / 169. Files open: 1 / 169. (Queue168 is already counted complete; Queue169 is the
only pending/open file.)
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP676 -- QUEUE 169 _precompile.cpp CLOSED / PRIMARY 169-FILE QUEUE COMPLETE
===============================================================================
Physical parent: C031-WIP675.
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, supplied Base AdvC
1.14, local K-Mod/AdvC practical-history diff corpus and current project build files. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

QUEUE 169 RESULT
----------------
Queue169 `CvGameCoreDLL/_precompile.cpp` is CLOSED with NO new finding. F708 / provisional KI#1029
remains UNUSED. The current translation unit contains only `#include "CvGameCoreDLL.h"` plus the
comment identifying it as the file responsible for building the project precompiled header. It has
no function body, file-scope data definition, static object or other runtime executable state of its
own.

PROJECT / PRECOMPILED-HEADER CONTRACT
-------------------------------------
The current Makefile explicitly compiles `_precompile.cpp` with `/YcCvGameCoreDLL.h` and emits
`_precompile.obj`; `PRECOMPILE_CFLAGS2` supplies the `CvGameCoreDLL.h` PCH name/path parameters. The
Visual Studio project likewise includes `..\_precompile.cpp` as a compile item. The translation unit
therefore has exactly the build-system role its source comment claims: force creation of the shared
CvGameCoreDLL precompiled header before ordinary source units consume it.

Current SAS and supplied Base AdvC 1.14 differ only in newline/format representation for this five-
line source. Local history traces the file to K-Mod's project/PCH setup and later AdvC project/
Makefile refactors; the retained changes concern build plumbing rather than an independent gameplay,
serialization, ownership or initialization contract. No current mismatch exists between the source,
Makefile and project references.

FINAL QUEUE ACCOUNTING
----------------------
A mechanical reconciliation of the authoritative queue table now finds exactly 169 distinct queue
rows and every one is marked COMPLETE. There are no remaining `[PENDING]` or genuine `[OPEN - ...]`
queue statuses. Queue168 `ArithmeticTraits.cpp` remains the single earlier-priority completion at
C031-WIP509 and is counted exactly once; Queue169 is the final ordinary-order closure here.

This final table reconciliation also corrects a bookkeeping-only tail drift in WIP675: that header
said `167 / 169` even though the table already contained 168 COMPLETE rows because Queue168 had been
closed earlier during the priority reloop. No source file or audit tranche was missing; the final
mechanical count is 169 / 169 complete.

PRIMARY FILE-ORIENTED PASS STATUS
---------------------------------
The primary 169-current-`.cpp` file-oriented audit queue is COMPLETE. This means every queued current
C++ translation unit has received its scheduled pass, including the deeper priority/re-audit work
and associated headers/XML/history/callers where needed. It does NOT assert that AdvCiv-SAS, Base
AdvC or Civ4 contain no further bugs: cross-file behavior, future source changes, unsupported mod-mod
inputs and empirical/runtime observations can still expose additional roots. The album's confirmed,
deduplicated, retracted/reopened and unused finding identities remain governed by the durable
checkpoints rather than by a claim of global bug-freedom.

F708 / provisional KI#1029 remains UNUSED at primary-queue completion; no final root was manufactured
merely to consume the next number.

Exact completion stone:
C031-WIP676 physical -> Queue169 CLOSED / no F708 -> all 169 queue rows COMPLETE -> 169 / 169 files
complete -> 0 / 169 files open -> F708/KI#1029 UNUSED -> primary current-.cpp file-oriented audit pass
COMPLETE -> preserve album for reconciliation / later issue-review, repair or new-source follow-up.

Files complete: 169 / 169. Files open: 0 / 169.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP677 -- POST-PRIMARY CROSS-FILE SEAM PASS / F708 CONFIRMED / F709 UNUSED
===============================================================================
Physical parent: C031-WIP676 (primary 169-file queue complete).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, supplied Base AdvC
1.14, current Fractal map script/world-size XML, local AdvC/K-Mod/BtS ancestry mirror and full audit
album. No web/GitHub lookup. Gameplay/XML/Python source untouched.

POST-PASS PURPOSE
-----------------
The 169-file primary queue is complete. This pass does not reopen every file; it reconciles explicit
cross-file observations and deliberately unpromoted notes whose final owner lived later in the queue.
The first carried seam was recorded by Queue022 TrueStarts and Queue025 StartingPositionIteration as
`PlotRadiusIterator` wrapped-map duplicate visitation, reserved for Queue167. Queue167's local closure
proved the unwrapped spiral enumeration but did not actually discharge that carried wrapped-map
fixture. Replaying the cross-file handoff now confirms the root.

F708 / PROVISIONAL KI#1029 -- WRAPPED RADIUS ITERATORS CAN YIELD THE SAME PHYSICAL PLOT MULTIPLE TIMES
----------------------------------------------------------------------------------------------------
STATUS: CONFIRMED current supported cross-file gameplay/start-generation defect root.
ROOT: `CvGameCoreDLL/PlotRadiusIterator.h`, `SpiralPlotIterator::computeNext` / `PlotCircleIter` and
`SquareIter` physical-plot iteration contract.

ROOT CONTRACT / MECHANISM
-------------------------
The iterator is documented as iterating over `CvPlot` instances within a radius. Internally it keeps
unwrapped logical coordinates (`m_iCurrX`, `m_iCurrY`) and resolves each offset independently through
`CvMap::plotValidXY`. On a wrapped dimension, distinct logical offsets that are congruent modulo the
map width/height therefore resolve to the same `CvPlot*`. There is no physical-plot visited/canonical-
offset suppression.

The primary Queue167 pass mechanically proved that every logical offset in the `(2r+1)^2` square is
generated exactly once. That property does NOT imply physical uniqueness after world wrap. Once a
radius reaches half of an even wrapped dimension, the two opposite half-map offsets already collide;
for larger radii, progressively more offsets alias the same physical columns/rows.

CURRENT SHIPPED FIXTURE A -- TRUE STARTS ON ARENA / FRACTAL
-----------------------------------------------------------
Current SAS adds visible `WORLDSIZE_ARENA` at 14x10 with two recommended players. Current Fractal's
hidden/default world-wrap option is Cylinder (`getCustomMapOptionDefault(0)==1`), so X wraps by
default. `GAMEOPTION_TRUE_STARTS` remains a visible supported game option.

`TrueStarts::calculateRadius` begins Arena at `6 + worldSize(0) = 6`; with the normal 2 players / 2
recommended crowdedness it adds one, making the minimum surroundings radius 7 before any ocean-based
expansion. On a 14-wide cylinder, logical X offsets -7 and +7 from a start resolve to the same physical
opposite map column.

A mechanical model of the current iterator + current `CvMap::plotDistance` on a representative
14x10 Cylinder center `(7,5)` gives, at radius 7 with center excluded:

    129 iterator visits
    126 distinct physical plots
    3 physical plots visited twice

The exact count varies near a non-wrapped Y edge, but the opposite-X alias itself remains. If the
non-ocean loop expands the surroundings radius to 8, the same representative center gives:

    163 iterator visits
    136 distinct physical plots
    27 physical plots visited twice

`TrueStarts::SurroundingsStats` consumes these visits additively: area/different-area plot weights,
space weights, X/Y space measures, river weights/scores and hill/peak scores are accumulated per
iterator visit. Those values feed `calcFitness` and therefore the civilization/leader fit used by
`TrueStarts::changeCivs`. The duplicate is consequently not an inert traversal artifact: seam-side
physical terrain can receive extra statistical weight and change a supported True Starts assignment.

CURRENT SHIPPED FIXTURE B -- DEFAULT STARTING POSITION ITERATION
---------------------------------------------------------------
The issue is not limited to the optional True Starts mode. Current
`ENABLE_STARTING_POSITION_ITERATION=1` and `SPI_PLOT_LIMIT=12000`; a 14x10 Arena is therefore well
inside the normal SPI path for a non-team Fractal game.

`StartingPositionIteration::PotentialSites::recordSite` unconditionally uses:

    PlotCircleIter(kPlot, 8, false)

and additively adjusts `kVicinityPenaltiesPerPlot` for lower-valued candidate sites. On the same
14x10 default Cylinder geometry, radius 8 can yield 163 visits for only 136 distinct physical plots.
Any candidate lying on one of the 27 aliased plots receives that stronger site's vicinity penalty
more than once. Although later removal of the same stronger site subtracts through the same iterator,
the doubled contribution exists while the site is active and participates in the pruning/ranking
loop, so the neighborhood score is not the intended one-contribution-per-nearby-site measure.

LINEAGE / CLASSIFICATION
------------------------
AdvC practical 1916 (`SquareIterator for loops over tiles within movement range`) introduced
`PlotRadiusIterator.h` and replaced many inherited nested `for (dx) for (dy) plotXY(...)` loops with
`SquareIter`/`PlotCircleIter`. The new iterator intentionally preserved the offset-loop behavior,
including world wrapping through `plotValidXY`; it did not introduce a physical-plot uniqueness guard.
The older BtS/K-Mod-style offset loops could already revisit a wrapped physical plot on sufficiently
small maps/ranges, so classify F708 as a centralized inherited neighborhood-iteration flaw rather
than claiming practical 1916 newly invented the mathematical alias.

SAS's 14x10 Arena world size provides an especially small, fully supported current activation. Base
AdvC 1.14 retains the same iterator implementation but does not supply this SAS Arena XML fixture.
The defect nevertheless belongs at the iterator contract/root layer because many current callers rely
on a radius meaning a set of nearby physical plots, not a multiset of logical wrapped offsets.

DEDUP
-----
Whole-album searches for wrapped duplicate visitation, opposite-seam plots, `PlotCircleIter` and
half-width aliases find only the explicit Queue022/Queue025 carry-forward notes reserving this exact
root for Queue167. No earlier F/KI owns it. F622 is separate: a TrueStarts climate directional-filter
bug involving resolved coordinates, not duplicate iterator output. F627 is also separate: duplicate
Dijkstra priority-queue entries overwrite settled path distances inside SPI; it does not concern map
plot iteration or world-wrap aliasing.

REPAIR BOUNDARY
---------------
Restore the iterator's physical-plot-set contract whenever a wrapped dimension is exhausted. A
central repair is preferable to patching each additive caller independently, but it should avoid an
unconditional heavyweight visited set in this very hot iterator.

A low-overhead approach can reject noncanonical logical offsets on wrapped dimensions before exposing
the resolved plot: retain exactly one representative of each offset modulo width/height, with an
explicit tie rule for the opposite cell on even dimensions. Equivalently, add duplicate suppression
only when `2 * radius >= wrappedDimension`, preserving the current fast path on ordinary maps/ranges.
Whichever implementation is chosen, regression coverage should verify physical uniqueness on flat,
cylindrical and toroidal maps and preserve `currPlotDist/currStepDist` semantics for the retained
representative.

Required regression fixtures include at least:
1. 14x10 Cylinder, radius 7: opposite half-width plots yielded exactly once;
2. 14x10 Cylinder, radius 8: every physical plot yielded at most once;
3. 14x10 Torus with radius 8: no X/Y combination is repeated even when both dimensions wrap;
4. ordinary larger maps/radii below half dimensions: unchanged iteration set/order where possible;
5. TrueStarts surroundings and SPI `recordSite` additive totals computed once per physical plot.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED here by cross-file seam reconciliation.
F709 / provisional KI#1030: UNUSED next root ID.

Continue the post-primary pass by mechanically revisiting the strongest explicit `not promoted`,
`below threshold`, `audit note`, carried-future-root and cross-file-owner notes. Promote only when new
cross-file evidence establishes a current supported consequence; keep API-only, malformed-data,
compile-disabled, tuning-only and already-owned observations below threshold.

Exact continuation stone:
C031-WIP677 physical -> primary 169-file queue remains COMPLETE -> post-pass F708 confirmed ->
F709/KI#1030 UNUSED -> reconcile strongest carried/unpromoted cross-file notes -> dedup against all
F001-F708 and current Known Issues -> freeze next physical post-pass handoff at settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP678 -- POST-PRIMARY DEFERRED-NOTE PASS / F709 CONFIRMED / F710 UNUSED
===============================================================================
Physical parent: C031-WIP677 (primary 169-file queue complete; F708 confirmed post-pass).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, supplied Base AdvC
1.14, current BUG Main Interface configuration / GlobalDefines, local AdvC ancestry mirror and full
audit album. No web/GitHub lookup. Gameplay/XML/Python source untouched.

POST-PASS PURPOSE
-----------------
The primary queue remains 169/169 complete. WIP210 and later CvPlayer ownership checkpoints explicitly
parked one deterministic low-severity `postProcessMessages` issue until substantive gameplay/state
territory had been exhausted. That condition is now satisfied, so the parked note was revalidated
against the current reconciled source rather than promoted from historical wording alone.

F709 / PROVISIONAL KI#1030 -- PERSISTENT NO_TECH AFTER RESEARCH EXHAUSTION MAKES THE TURN-LOG
AUTO-OPEN HEURISTIC SUBTRACT A NONEXISTENT TECH-COMPLETION MESSAGE
------------------------------------------------------------------------------------------------
STATUS: CONFIRMED current supported-config UI defect root; inherited from AdvC practical 1253 /
`advc.106b`. Low severity: messages are preserved; only automatic opening of the Turn Log is
suppressed by one count in the affected state.

CURRENT COUNTER CONTRACT
------------------------
`CvPlayer::doTurn` sets the game to the between-turn phase for sequential single-player play and
resets `m_iNewMessages = 0` for a human player at the end of that player's turn. While other players
act, `CvPlayer::addMessage` increments that counter only for message types that actually belong on
the Event tab of the Turn Log (`INFO`, `MINOR_EVENT`, `MAJOR_EVENT`, `MAJOR_EVENT_LOG_ONLY`). At the
next activation of the human turn, after warnings/diplomacy and before ordinary play resumes,
`CvPlayer::postProcessMessages()` decides whether the Turn Log should auto-open.

The intended advc.106b special case is:

    if (getCurrentResearch() == NO_TECH)
        m_iNewMessages--;

The adjacent comment explains why: immediately after finishing a technology, the tech-completion
message is already represented prominently by the technology splash screen, so that one message
should remain in the log but should not count toward the auto-open threshold.

The predicate, however, does not test whether a technology actually completed during the intervening
turns or whether such a message was counted. `getCurrentResearch()` merely returns the head of the
research queue and returns `NO_TECH` whenever that queue is empty.

SUPPORTED PERSISTENT NO_TECH STATE
----------------------------------
Current research code explicitly recognizes that `NO_TECH` can persist when all technologies have
been learned. `AI_chooseResearch()` clears the queue, tries available team/Python/AI choices and
leaves it empty when no researchable technology exists. `doResearch()` then has an explicit
`eCurrentTech == NO_TECH` branch whose current diagnostic comment names "all technologies were
learned" as a legitimate cause.

Therefore, once a human civilization has exhausted the technology tree, `NO_TECH` is not a one-turn
proxy for "a technology just completed". It can remain true on every subsequent turn even though no
new technology-completion message exists to discount.

DETERMINISTIC AUTO-OPEN CONSEQUENCE
-----------------------------------
The BUG option `MainInterface__MessageLimit` is a supported integer choice with values
`0,1,2,3,4,5,10`; current C++ uses default/fallback 3. `AutoOpenEventLog` controls the feature, and
the existing `MESSAGE_LIMIT_WITHOUT_MPU` policy can disable the limit when Minimize Pop-Ups is not
used. Thus the defect concerns supported configurations in which automatic Event/Turn-Log opening
and a finite message limit are enabled; it is not claimed to affect the current default UI setting.

The auto-open comparison is strict:

    m_iNewMessages > iLimit

Take a finite limit of 3 after the technology tree has been exhausted. If four genuine qualifying
interturn messages arrive, the correct count is 4 and should cross the configured threshold. At the
next turn start, persistent `NO_TECH` subtracts one despite there being no just-finished-tech message,
reducing the decision count to 3. `3 > 3` is false, so the Turn Log does not auto-open.

The same one-count suppression occurs for every finite configured limit: exactly `MessageLimit + 1`
genuine qualifying messages are treated as only `MessageLimit` whenever persistent `NO_TECH` is
present and no real technology completion occurred.

SCOPE / NON-CONSEQUENCES
------------------------
This does NOT delete or corrupt messages. `addMessage` has already appended the messages to
`m_listGameMessages`, and the problematic operation adjusts only the manual start-of-turn count used
to decide whether to call `showTurnLog()`. The user can still open the log manually. Classify F709 as
a deterministic low-severity UI/attention defect, not gameplay-state loss.

The relevant-diplomacy override can independently open the log when `m_iNewMessages > 0` and a
qualifying diplomacy entry is queued. F709's exact threshold witness therefore uses ordinary
interturn messages without such a relevant diplomacy override; no special malformed state is needed.

ANCESTRY
--------
AdvC practical 1253 / commit `f6ff6a182f5187541fff6e5bbbe12f9573b3778c` introduced advc.106b's
manual interturn-message counter and the `NO_TECH` subtraction in the same patch. Its original
comment already states that the subtraction is meant to discount the technology-finished message,
not all empty-research-queue states. Later refactors renamed `iNewMessages` to `m_iNewMessages` and
`postProcessBeginTurnEvents` to `postProcessMessages`, broadened retained-message handling and moved
call timing, but preserved this predicate. Supplied Base AdvC 1.14 retains the same current defect.

DEDUP / WHY IT IS NUMBERED NOW
------------------------------
The full album already contains this exact root several times as an intentionally unnumbered control:
WIP210 called it a deterministic, reachable, UI-only Turn-Log edge and explicitly said to reconsider
it only after substantive gameplay/state territory closed; later CvPlayer ownership passes re-hit it
and explicitly said not to consume the then-current finding number. No existing F/KI owns the
`NO_TECH` message-discount predicate.

The primary 169-file pass is now complete and the post-pass is specifically reconciling those carried
and deliberately deferred notes. Promoting F709 here therefore follows the album's prior scheduling
decision; it does not lower the technical proof threshold or retroactively claim gameplay severity.

REPAIR BOUNDARY
---------------
Do not infer a technology-completion message from the current research queue sentinel. Discount one
message only when the player actually completed a technology during the relevant between-turn window
and the corresponding qualifying completion message was counted.

A robust repair can track an explicit per-window technology-completion-message count/flag alongside
`m_iNewMessages`, or classify the actual message/event at production time and exclude that one from
the auto-open count while still retaining it in the Turn Log. Preserve the original advc.106b intent:
a real just-finished technology should remain logged without needlessly opening the log solely because
of that splash-covered notice.

Regression cases:
1. normal technology completion + otherwise threshold-count messages: exactly one real tech message
   is discounted;
2. all technologies already known + `MessageLimit + 1` genuine interturn messages: no phantom
   subtraction, Turn Log opens;
3. empty research queue for another legitimate non-completion reason: no phantom subtraction;
4. disabled Auto Open Event Log / infinite effective limit: behavior unchanged;
5. relevant diplomacy and HotSeat message-display paths retain their independent behavior.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED here.
F710 / provisional KI#1031: UNUSED next root ID.

Continue the post-primary pass by reconciling the strongest remaining explicit `not promoted`,
`below threshold`, `audit note`, carried-future-root and cross-file-owner notes. Keep supported-current
activation and whole-album dedup mandatory; do not convert API-only, malformed-data, compile-disabled,
tuning-only or already-owned observations into findings merely because the primary queue is complete.

Exact continuation stone:
C031-WIP678 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F709 confirmed ->
F710/KI#1031 UNUSED -> mine strongest remaining explicitly deferred/cross-file notes -> revalidate
against current reconciled source + supplied ancestors -> whole-album dedup -> freeze next physical
post-pass handoff at the next settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred-note pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================

===============================================================================
C031-WIP679 -- POST-PRIMARY DATA/CROSS-FILE PASS / F710 CONFIRMED / F711 UNUSED
===============================================================================
Physical parent: C031-WIP678 (primary 169-file queue complete; post-pass F708-F709 confirmed).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, current
CIV4LeaderHeadInfos.xml + Audio2DScripts.xml/AudioDefines.xml, CvInfo_Civilization XML reader,
CvPlayer diplomacy-music selector, SAS practical-4880 history and full audit album. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

POST-PASS PURPOSE / PRIOR CONTROLS
----------------------------------
The first two candidates after WIP678 were deliberately rechecked before consuming another root.

1. Global + PickPlayer World News hidden-information question -- CLOSED SAFE.
   The current event-announcement loop already filters each observer against the picked player's
   team contact, while the outer recipient iterator requires knowledge of the event owner and plot
   coordinates are reveal-gated separately. The old Queue004 uncertainty therefore does not survive
   the completed current-source/cross-file proof.

2. `CvPlayer::getWorkRate(BuildTypes)` duplicated `+100` -- still real source debt, NOT F710.
   Current code still computes the AI branch with `200 + AIWorkRateModifier` while real
   `CvUnit::workRate` uses `100 + AIWorkRateModifier`. A refreshed current caller census corrected
   one stale old note: SAS Phase-0 productive-feature code does call the two-argument
   `CvPlot::getBuildTurnsLeft`, which uses this player estimator. However current BuildInfo data has
   exactly one productive pure-removal build per relevant feature (`BUILD_REMOVE_FOREST` for Forest,
   `BUILD_REMOVE_JUNGLE` for Jungle); that call only tie-breaks otherwise equal productive-removal
   candidates, so no current AI choice can be flipped there. Other live ordinary gameplay Worker
   progress paths use actual unit work rates or the explicit multi-argument plot estimator. The bad
   helper can still distort active-player partial-build help during AI Auto Play, but that remains a
   low-value estimator/UI edge rather than the stronger current-live root selected below.

F710 / PROVISIONAL KI#1031 -- STALIN'S RENAISSANCE PEACE-INTRO XML REFERENCES A NONEXISTENT
`AS2D_DIPLO_STALIN_EARLY_INTROO`, SO THE INTENDED DIPLOMACY INTRO SCRIPT CANNOT RESOLVE
------------------------------------------------------------------------------------------------
STATUS: CONFIRMED current SAS-specific data regression; deterministic user-facing diplomacy-audio
root. The narrow repair is XML-only, but the activation/consumer contract crosses the audited C++
LeaderHead XML loader and CvPlayer diplomacy-music selector, so it is promoted here during the
post-primary deferred/data reconciliation rather than left permanently unnumbered.

CURRENT DATA CONTRADICTION
--------------------------
Current `Assets/XML/Civilizations/CIV4LeaderHeadInfos.xml` contains, for `LEADER_STALIN`:

    <DiplomacyIntroMusicPeace>
        ...
        <EraType>ERA_RENAISSANCE</EraType>
        <DiploScriptId>AS2D_DIPLO_STALIN_EARLY_INTROO</DiploScriptId>
        ...
    </DiplomacyIntroMusicPeace>

The Ancient, Classical, Medieval, Industrial, Modern and Future peace-intro slots all use:

    AS2D_DIPLO_STALIN_EARLY_INTRO

Current `Assets/XML/Audio/Audio2DScripts.xml` defines exactly that correctly spelled script:

    <ScriptID>AS2D_DIPLO_STALIN_EARLY_INTRO</ScriptID>
    <SoundID>DIPLO_STALIN_EARLY_INTRO</SoundID>

and `AudioDefines.xml` defines the corresponding `DIPLO_STALIN_EARLY_INTRO` SoundID. A whole-current-
XML search finds no `AS2D_DIPLO_STALIN_EARLY_INTROO` ScriptID. The extra trailing `O` is therefore
not an alternate alias; it is an unresolved current reference.

C++ LOAD / LIVE SELECTION CONTRACT
----------------------------------
`CvLeaderHeadInfo::read` loads `DiplomacyIntroMusicPeace` with
`SetVariableListTagPairForAudioScripts(..., GC.getNumEraInfos())`. For each non-empty era mapping,
that helper passes the configured script text to `gDLL->getAudioTagIndex` and stores the returned
integer in the era-indexed LeaderHead array.

`CvPlayer::getIntroMusicScriptId(PlayerTypes eForPlayer)` then selects the entry using the VIEWING
player's current era:

    EraTypes eEra = GET_PLAYER(eForPlayer).getCurrentEra();
    if (GET_TEAM(eForPlayer).isAtWar(getTeam()))
        return kLeader.getDiploWarIntroMusicScriptIds(eEra);
    return kLeader.getDiploPeaceIntroMusicScriptIds(eEra);

Therefore a player whose current era is Renaissance and who opens peaceful diplomacy with Stalin
selects precisely the malformed Renaissance peace-intro slot. The intended `...EARLY_INTRO` script
exists, but the configured `...EARLY_INTROO` reference does not.

The EXE-side audio lookup/playback implementation is closed source, so F710 does not invent an exact
fallback symptom such as “always silence” versus another invalid-script behavior. The source-proven
contract failure is sufficient and narrower: the current Renaissance slot cannot resolve the intended
Stalin intro script that every neighboring era maps to correctly.

ANCESTRY / SAS-SPECIFIC BIRTH
-----------------------------
SAS practical 4880 / commit `aa5518de413426df04a854c2e9f6436328958d79`, titled around assigning
individual copyright-safe leader music, introduced the Stalin-specific mappings and the correctly
spelled `AS2D_DIPLO_STALIN_EARLY_INTRO` Audio2DScript in the same change. In that same patch, only the
Renaissance `DiplomacyIntroMusicPeace` entry was written with the extra trailing `O`.

Supplied Base AdvC 1.14 does not contain this SAS per-era Stalin mapping/typo. Classify F710 as a
SAS data regression, not an inherited AdvC/K-Mod audio-system bug.

DEDUP
-----
Queue034 already discovered this exact unresolved reference and explicitly preserved it as a
“SEPARATE ACTIONABLE SAS DATA REGRESSION” without spending F660 because that queue's owner was the C++
info class. Whole-album/Known-Issues searches find no numbered F/KI for the Stalin `INTROO` typo.
F701 concerns unsupported `[CT_*]` diplomacy text substitution tags; F702 concerns the
`ICON_GOLDEN_AGE` scoreboard token. Neither owns audio-script reference resolution.

WHY IT IS NUMBERED NOW
----------------------
The primary 169-.cpp audit is complete, and the active post-pass explicitly reconciles parked
cross-file/data issues discovered while proving C++ contracts. F710 has all evidence the earlier
Queue034 note lacked only by scheduling convention, not by technical uncertainty: current data,
consumer path, live supported era/diplomacy state, exact birth commit and zero prior owner are all
established. Promoting it here does not lower the finding threshold.

REPAIR BOUNDARY
---------------
The repair is surgical and data-only:

    AS2D_DIPLO_STALIN_EARLY_INTROO
    ->
    AS2D_DIPLO_STALIN_EARLY_INTRO

Do not add a fake audio alias merely to preserve the typo. Regression coverage should mechanically
validate every non-empty LeaderHead diplomacy music/intro `AS2D_*` reference against the current
Audio2DScript ID set, including every era slot. That broader data-reference check would prevent the
same one-character regression elsewhere.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED at C031-WIP678.
F710 / provisional KI#1031: CONFIRMED here.
F711 / provisional KI#1032: UNUSED next root ID.

Continue the post-primary pass by mining the strongest remaining explicit deferred/current-live,
cross-file and actionable-data notes. Revalidate against the current reconciled source and dedup
against all F001-F710 before promotion. Keep extension-API-only, malformed-save/input, compile-
disabled, tuning-only and data-masked observations below threshold.

Exact continuation stone:
C031-WIP679 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F710 confirmed ->
F711/KI#1032 UNUSED -> mine strongest unresolved deferred/cross-file/data notes -> current-source +
history + whole-album proof -> freeze next physical post-pass handoff at the next settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred/data pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP680 -- POST-PRIMARY DEFERRED UI/CONFIG PASS / F711 CONFIRMED / F712 UNUSED
===============================================================================
Physical parent: C031-WIP679 (primary 169-file queue complete; post-pass F708-F710 confirmed).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10,
StartPointsAsHandicap.cpp/.h, GlobalDefines_advc.xml, CyGame/CvReplayInfo Settings/replay consumers,
AdvC practical-1253/1395/1416/1548/1994/3528 local history and full audit album. No web/GitHub
lookup. Gameplay/XML/Python source untouched.

POST-PASS CONTROL BEFORE PROMOTION
----------------------------------
The previously parked `CvPlayer::getWorkRate(BuildTypes)` duplicated-base estimate was rechecked
against current callers before selecting another root. One current SAS Phase-0 productive-feature
caller does reach the flawed two-argument plot-turn estimator, correcting an older "UI only" note,
but current BuildInfo data gives each productive removable feature only one corresponding pure
removal build. The bad estimate therefore cannot flip that tie-break under current data. Other
ordinary Worker progress paths use actual unit work rates or the explicit multi-argument estimator.
Retain the source typo/AI-Auto-Play hover distortion as low-value debt, not F711.

F711 / PROVISIONAL KI#1032 -- SPAH RANDOMIZATION-STATE FLAG IS NEVER SET, SO HIDING THE RANDOMIZED
DISTRIBUTION ALSO HIDES THE INTENDED "NOT SHOWN: RANDOMIZATION" DISCLOSURE
--------------------------------------------------------------------------------------------------
STATUS: CONFIRMED inherited AdvC optional-configuration presentation defect, current supported
configuration. Start-point assignment itself remains correct; the defect is Settings/replay text
failing to disclose that the displayed distribution is deliberately the pre-randomization one.

INTENDED CONFIG / DISPLAY CONTRACT
----------------------------------
Current `Assets/XML/GlobalDefines_advc.xml` explicitly documents two independent SPaH controls when
unequal AI start points are assigned:

    SPAH_RANDOMIZE > 0
        randomize the start-point distribution (recommended);

    SPAH_SHOW_RANDOMIZED > 0
        show the randomized distribution (recommended), otherwise show the distribution BEFORE
        randomization, hiding the exact randomized values.

`StartPointsAsHandicap::updatePointsDisplayString` contains an explicit disclosure branch for the
hidden-randomized-values mode:

    bool showActual = (GC.getDefineINT("SPAH_SHOW_RANDOMIZED") > 0);
    ...
    int pts = showActual ? civs[i]->startPoints_actual() :
                           civs[i]->startPoints_configured();
    ...
    if(randPoints && !showActual)
        s << "Not shown: randomization" << std::endl;

That final line makes the intended UI contract unambiguous: when randomization really occurred but
actual randomized values are intentionally hidden, Settings/replay should tell the reader that the
shown numbers are not the final randomized allocation.

CURRENT STATE BUG
-----------------
`randPoints` never becomes true in current source.

A whole-tree assignment search finds only:

    StartPointsAsHandicap::StartPointsAsHandicap()
        randPoints = false;

plus serialization reads/writes. `setInitialItems()` performs the real randomization through a
separate local Boolean:

    unequalDistrib = assignPoints();
    bool randomize = unequalDistrib;
    if(randomize)
        randomize = GC.getDefineINT("SPAH_RANDOMIZE") > 0;
    if(randomize) {
        randomizePoints();
        ...
    }

but never copies that successful state to `randPoints`.

Therefore, in every newly initialized game, `randPoints` remains false even when `randomizePoints()`
actually ran. `write()` serializes the false flag and `read()` restores it, so save/load does not
repair the state.

DETERMINISTIC CURRENT CONFIGURATION WITNESS
-------------------------------------------
Use a supported SPaH setup with:

1. SPaH enabled and unequal AI start points assigned;
2. enough AI civilizations for the randomization routine to operate (the shipped configuration
   explicitly notes that fewer than four AIs are not randomized);
3. `SPAH_RANDOMIZE=1` (current recommended/default value);
4. `SPAH_SHOW_RANDOMIZED=0` (explicitly supported hidden-exact-values mode).

`setInitialItems()` randomizes `startPoints_actual()`. The Settings/replay display deliberately
chooses `startPoints_configured()` because showActual is false. But `randPoints` is still false, so
it omits the only explanatory line:

    Not shown: randomization

The user therefore sees a valid-looking pre-randomization distribution without the disclosure that
actual start points differ. This does not reveal the hidden randomized values and does not change
SPaH gameplay; it misdescribes the displayed setup state.

LIVE CONSUMERS
--------------
`StartPointsAsHandicap::forSettingsScreen()` is exposed through `CyGame` for the Victory-screen
Settings presentation. `CvReplayInfo` independently asks for the same distribution text while
building replay/settings metadata. The stale false flag can therefore affect both the live Settings
view and persisted/displayed replay setup description when actual randomized values are hidden.

ANCESTRY
--------
AdvC practical 1253 / `f6ff6a182f...` introduced SPaH randomization, the two GlobalDefines, the
serialized `randPoints` field and the `if(randPoints && !showActual)` disclosure in the same initial
implementation. That birth implementation performs `randomizePoints()` through a local `randomize`
Boolean but contains no assignment to `randPoints`.

Practical 1395 / `cbb4852bb0...` later added explicit constructor initialization:

    randPoints = false;

fixing an uninitialized-state problem but cementing deterministic false state because no true
assignment was added. Later practicals 1416/1548/1994/3528 preserve the field/display/serialization
without supplying the missing state transition. Base AdvC 1.14 and current SAS therefore retain the
same defect.

DEDUP / WHY NUMBERED IN POST-PASS
---------------------------------
Queue077's final closure explicitly preserved this exact root as a "real current optional-config
presentation defect" below its then-current consequential finding threshold after five much stronger
SPaH roots (F683-F687). No existing F/KI owns the dead `randPoints` disclosure flag. F684 concerns
Regenerate Map re-entry, F685/F686 serialization content/identity, and F687 the empty SPaH score
distribution after multiplayer takeover; none repair or explain this presentation state.

The primary 169-file pass is complete, and the post-pass is now deliberately reconciling deferred,
proved low-severity issues once stronger gameplay/state territory is exhausted. F711 meets the same
standard as F709: deterministic supported activation, exact intended behavior in code/docs, current
consumer, ancestry and zero prior owner, while severity is kept narrow.

REPAIR BOUNDARY
---------------
Record the actual initialization-time randomization state. The narrow shape is to reset the flag at
the start of SPaH initialization and set it only when the randomization branch actually executes,
e.g. semantically:

    randPoints = false;
    ...
    if(randomize) {
        randomizePoints();
        randPoints = true;
        ...
    }

Preserve serialization so loaded games retain whether their distribution was randomized. Do not
infer randomization later merely from `SPAH_RANDOMIZE`, because fewer-than-four-AI and equal-point
setups can legitimately leave points unrandomized despite the define being enabled.

Regression cases:
1. unequal points + >=4 AIs + randomize=1 + showRandomized=0 -> configured values plus disclosure;
2. same setup + showRandomized=1 -> actual values, no disclosure needed;
3. equal distribution / randomization not applicable -> no false disclosure;
4. too few AI civs to randomize -> no false disclosure;
5. save/load after a randomized hidden-values setup -> disclosure state survives.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED at C031-WIP678.
F710 / provisional KI#1031: CONFIRMED at C031-WIP679.
F711 / provisional KI#1032: CONFIRMED here.
F712 / provisional KI#1033: UNUSED next root ID.

Continue the post-primary pass by mining the strongest remaining explicit deferred/current-live,
cross-file, API-activated and actionable-data notes. Revalidate against current reconciled source
and dedup against all F001-F711 before promotion. Keep compile-disabled, malformed-only,
extension-only, tuning-only and current-data-masked observations below threshold.

Exact continuation stone:
C031-WIP680 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F711 confirmed ->
F712/KI#1033 UNUSED -> mine strongest unresolved deferred/cross-file/data notes -> current-source +
history + whole-album proof -> freeze next physical post-pass handoff at next settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred/data pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP681 -- POST-PRIMARY OPTIONAL-ALERT PASS / F712 CONFIRMED / F713 UNUSED
===============================================================================
Physical parent: C031-WIP680 (primary 169-file queue complete; post-pass F708-F711 confirmed).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10,
AdvCiv4lerts.cpp/.h, Civ4lerts Python/config activation, CvPlayer resource-trade legality/denial,
AdvC practical-1287 history and full audit album. No web/GitHub lookup. Gameplay/XML/Python source
untouched.

F712 / PROVISIONAL KI#1033 -- THIRD-PARTY RESOURCE ALERT ITERATES MULTISET DIFFERENCE COPIES
INDIVIDUALLY, SO A 0->2 OR 2->0 SAME-BONUS CHANGE EMITS THE SAME ALERT TWICE
------------------------------------------------------------------------------------------------
STATUS: CONFIRMED inherited AdvC optional-alert presentation defect; deterministic supported
multiplayer/trade state. The underlying deals/resource counts remain correct. Scope is duplicate
informational notifications when the same encoded resource export changes multiplicity by more than
one between alert snapshots.

ROOT
----
`BonusThirdPartiesAlert::check()` stores every resource-export relation in per-exporter
`std::multiset<int>` collections. The encoded integer is:

    GC.getNumBonusInfos() * eTo + eBonus

so multiple copies of the same bonus exported from the same civilization to the same recipient are
represented by duplicate identical multiset elements.

The snapshot delta is also computed as a multiset difference:

    set_difference(updatedDeals[eFrom].begin(), updatedDeals[eFrom].end(),
                   m_exportDeals[eFrom].begin(), m_exportDeals[eFrom].end(),
                   inserter(aiNewDeals, aiNewDeals.begin()));

and analogously for missing deals. `std::set_difference` on multisets preserves multiplicity. If a
relation changes 0->2, `aiNewDeals` therefore contains the SAME encoded value twice.

The consumer then iterates those copies separately:

    for(size_t i = 0; i < aiNewDeals.size(); i++) {
        int iNewCount = updatedDeals[eFrom].count(aiNewDeals[i]);
        int iOldCount = m_exportDeals[eFrom].count(aiNewDeals[i]);
        showMessage(eFrom, aiNewDeals[i], iNewCount, iOldCount);
    }

Both iterations recompute the SAME totals (`iNewCount=2`, `iOldCount=0`) and call `showMessage` with
the same exporter, recipient, bonus and quantity transition. For a strategic resource, normal
non-debug mode emits the same `TXT_KEY_CIV4LERTS_EXPORTING_STRATEGIC` notification both times.
A 2->0 removal produces the symmetric duplicate "not exporting" notifications unless another
suppression cause (war/death/recent stop-trading memory) intentionally hides the transition.

SUPPORTED MULTIPLICITY / TIMING WITNESS
---------------------------------------
This is not malformed deal data. Current `CvPlayer::canTradeItem(TRADE_RESOURCES)` permits a resource
whenever the exporter still has at least one tradeable copy; it does not forbid an additional copy
merely because the recipient already receives that bonus. Human-human `AI_bonusTrade` returns
`NO_DENIAL`, so ordinary human multiplayer can legally create another same-resource export while the
exporter has sufficient copies.

The alert is sampled once at `BeginActivePlayerTurn` for its human owner. A supported three-human
fixture therefore suffices:

1. observer C begins a turn with no A->B Copper export; C's alert snapshot stores count 0;
2. before C's next active turn, A and B establish one Copper export and then a second separate Copper
   export while A still has another tradeable copy (e.g. deals initiated on their respective turns,
   or equivalent supported multiplayer diplomacy timing);
3. C's next `BeginActivePlayerTurn` runs `BonusThirdPartiesAlert::check()`;
4. `updatedDeals[A]` contains two identical `(B,Copper)` elements while the old multiset contains 0;
5. `set_difference` emits two identical delta elements;
6. the loop calls `showMessage` twice with the same 2-vs-0 totals and C receives two identical
   strategic-resource alerts.

The same root can occur for any multiplicity jump/drop greater than one; 0->2 and 2->0 are the clean
minimal message-producing cases in normal non-debug mode.

OPTION / SEVERITY BOUNDARY
--------------------------
`Assets/Config/Civ4lerts.xml` exposes the shipped `Bonus Third Parties` alert and defaults it to
False. The Python `BonusThirdParties` wrapper maps it to native alert ID 2 and checks it at
`BeginActivePlayerTurn` when enabled.

Therefore F712 is supported but optional/default-off. It does not alter diplomacy, resource
availability, deal state, visibility or counts. It is a low-severity notification multiplicity bug:
the observer gets redundant identical alerts for one aggregate relation transition.

ANCESTRY
--------
AdvC practical 1287 / `ad5faf8aa2...` introduced `BonusThirdPartiesAlert` with the multiset snapshot,
`set_difference`, and element-by-element delta loops in essentially this same form. The birth code
already recomputed total old/new counts for each duplicate difference element rather than coalescing
identical keys. Later alert refactors preserve the structure. Current SAS retains the inherited root.

DEDUP / WHY NUMBERED NOW
------------------------
Queue069's completed AdvC4lerts audit explicitly proved this edge and preserved it as a "LOW-GRADE
MULTISET UX EDGE -- BELOW FINDING THRESHOLD" because stronger gameplay/state roots were still the
priority. Whole-album search finds no numbered owner for duplicate identical third-party bonus-alert
messages. It is separate from visibility/war-memory suppression and from trade-count/accounting
findings: those states are correct; only the delta notification loop fails to coalesce multiplicity.

The primary 169-file pass is complete, and this post-pass deliberately resolves such already-proved
low-severity supported defects instead of leaving them permanently unnumbered. F712 is promoted with
its optional/default-off and presentation-only scope explicit.

REPAIR BOUNDARY
---------------
Emit at most one notification per unique encoded `(exporter, recipient, bonus)` relation per check.
The simplest local repair is to deduplicate the difference keys before iterating, while continuing
to compute the aggregate `iNewCount`/`iOldCount` from the multisets. Equivalently, iterate unique
keys whose count changed instead of every multiset-difference copy.

Do not replace the underlying multisets with plain sets unless quantity information is intentionally
removed: `showMessage` and nonstrategic transition logic use the old/new multiplicities.

Regression cases:
1. 0->1 strategic resource: one alert;
2. 0->2 same strategic resource between checks: still exactly one alert;
3. 2->0: exactly one ended-export alert when not otherwise suppressed;
4. 1->2 and 2->1 in normal non-debug mode: preserve current no-message behavior when the exporter-
   recipient relationship remains nonzero;
5. two different resources change simultaneously: one alert per distinct relevant resource;
6. hidden/unmet, war/death/recent-stop-trading suppression remains unchanged.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED at C031-WIP678.
F710 / provisional KI#1031: CONFIRMED at C031-WIP679.
F711 / provisional KI#1032: CONFIRMED at C031-WIP680.
F712 / provisional KI#1033: CONFIRMED here.
F713 / provisional KI#1034: UNUSED next root ID.

Continue the post-primary pass by mining the strongest remaining proved/deferred current-live notes
and cross-file/data/API seams. Revalidate current activation and whole-album ownership before
promotion. Keep compile-disabled, malformed-only, extension-only, tuning-only and current-data-masked
observations below threshold.

Exact continuation stone:
C031-WIP681 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F712 confirmed ->
F713/KI#1034 UNUSED -> mine strongest unresolved deferred/current-live note -> current-source +
history + whole-album proof -> freeze next physical post-pass handoff at next settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred/data pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP682 -- POST-PRIMARY DIAGNOSTIC/FORMAT PASS / F713 CONFIRMED / F714 UNUSED
===============================================================================
Physical parent: C031-WIP681 (primary 169-file queue complete; post-pass F708-F712 confirmed).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10, CvMap.cpp,
CvString.cpp/.h, K-Mod/AdvC local history and full audit album. No web/GitHub lookup.
Gameplay/XML/Python source untouched.

POST-F712 INTEGRITY SWEEP CONTROL
---------------------------------
The F710 audio-reference lesson was generalized before choosing F713: all current XML `AS2D_*` /
`AS3D_*` reference tokens were compared against the current Audio2DScript ID set. There are 679
distinct non-definition audio-script references and exactly one unmatched reference:
`AS2D_DIPLO_STALIN_EARLY_INTROO`, already owned by F710. No second audio-reference root was found.

F713 / PROVISIONAL KI#1034 -- `CvMap::init` PASSES AN INCOMPLETE `%6` PRINTF CONVERSION TO
`CvString::format`, SO EVERY MAP INITIALIZATION INVOKES THE CRT WITH A MALFORMED FORMAT STRING
------------------------------------------------------------------------------------------------
STATUS: CONFIRMED inherited K-Mod diagnostic/format-contract defect, current default lifecycle.
The precise MSVC 7.1 CRT symptom is deliberately not asserted; the source-proven defect is that the
format string is invalid and the intended custom-map-option count is not supplied with a valid
conversion specifier.

CURRENT LIVE CALL
-----------------
Every `CvMap::init` begins with:

    gDLL->logMemState(CvString::format(
        "CvMap::init begin - world size=%s, climate=%s, sealevel=%s, num custom options=%6",
        ...,
        GC.getInitCore().getNumCustomMapOptions()).c_str());

The final `%6` is not a complete printf conversion. A width (`6`) may appear inside a conversion,
but it must be followed by a conversion specifier such as `d`; `%6` at end of string has no type to
consume/format the supplied integer argument.

This path is not optional gameplay configuration or extension API: ordinary map initialization
executes it before `beforeInit`, reset/setup and plot allocation.

LOW-LEVEL FORMATTER CONSEQUENCE
-------------------------------
`CvString::format` forwards the literal and varargs to `CvString::formatv`, whose narrow formatter
calls target-CRT `_vsnprintf` directly. `formatv` assumes a negative/oversized return means only that
the destination buffer was insufficient, allocates a larger buffer and retries up to 40 attempts.

Because `%6` violates the formatter's input contract rather than merely exceeding the buffer, exact
runtime behavior is CRT-specific. F713 therefore does NOT claim a deterministic crash or a specific
rendered string. The bounded source-level consequences are:

* the intended custom-map-option integer has no valid printf conversion and the memory-state line
  cannot rely on correctly representing it;
* the map-init diagnostic invokes `_vsnprintf` with a malformed format on every map initialization;
* if that CRT reports the malformed format through the same negative-result channel used for
  truncation, `CvString::formatv` can perform its repeated buffer-growth retries before clearing or
  returning the diagnostic.

The game continuing to run does not make the format valid; it only constrains how severe the target
CRT happens to make the undefined/error formatting case.

MECHANICAL WHOLE-CURRENT FORMAT CHECK
-------------------------------------
A targeted current C++ scan of literal printf-style strings used by `CvString::format`, sprintf-like
calls and neighboring formatting sites found this `%6` as the only dangling/incomplete conversion
pattern of its kind. This reduces the risk that F713 is merely one instance of an already-numbered
generic malformed-format family; it is an isolated retained literal.

ANCESTRY
--------
Supplied K-Mod history already contains the map-init memory-state line with the exact malformed
`num custom options=%6` literal. AdvC formatting/refactor practicals repeatedly preserve it; supplied
Base AdvC 1.14 and current SAS still contain the same string. Classify F713 as inherited from K-Mod,
not an SAS-created logging regression.

DEDUP / PRIOR PARKING
---------------------
Queue036's final `CvMap.cpp` closure explicitly recorded this exact malformed `%6` as low-grade
current diagnostic/performance debt and declined to consume the then-current gameplay-oriented root
number because no gameplay-state consequence was involved. Queue104 later audited `CvString::formatv`
and likewise found no generic current gameplay root that subsumes this malformed caller.

Whole-album search finds no numbered owner for the `%6` call. The primary 169-file gameplay/state
pass is complete, and the post-pass now deliberately resolves proved low-severity supported defects;
F713 is promoted with its diagnostic-only scope and CRT-dependent symptom boundary explicit.

REPAIR BOUNDARY
---------------
Replace the incomplete conversion with a valid integer conversion. The natural diagnostic form is:

    num custom options=%d

If six-character field width was genuinely intended, use `%6d`; no surrounding formatting suggests
that fixed width is necessary. Do not alter `CvString::formatv` merely to special-case this caller.

A small static regression check for dangling `%`, width/flag-only terminal conversions in literal
printf strings would prevent recurrence.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED at C031-WIP678.
F710 / provisional KI#1031: CONFIRMED at C031-WIP679.
F711 / provisional KI#1032: CONFIRMED at C031-WIP680.
F712 / provisional KI#1033: CONFIRMED at C031-WIP681.
F713 / provisional KI#1034: CONFIRMED here.
F714 / provisional KI#1035: UNUSED next root ID.

Continue the post-primary pass by mining remaining proved/deferred current-live notes and by running
mechanical cross-file/data integrity checks suggested by prior findings. Revalidate current
activation and whole-album ownership before promotion. Keep compile-disabled, malformed-external-
input-only, extension-only, tuning-only and current-data-masked observations below threshold.

Exact continuation stone:
C031-WIP682 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F713 confirmed ->
F714/KI#1035 UNUSED -> mine strongest unresolved deferred/current-live/cross-file integrity seam ->
current-source + history + whole-album proof -> freeze next physical post-pass handoff at next
settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred/data pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


===============================================================================
C031-WIP683 -- POST-PRIMARY LOCALIZATION-MARKUP CONTROL PASS / F714 UNUSED
===============================================================================
Physical parent: C031-WIP682 (primary 169-file queue complete; post-pass F708-F713 confirmed).
Source identity: reconciled AdvCiv-SAS light-source snapshot supplied 2026-09-10,
Assets/XML/Text/AdvCiv-SAS_main.xml, SevoPediaMain.py, current localization corpus, reachable SAS
commit-diff mirror and full audit album. No web/GitHub lookup. Gameplay/XML/Python source untouched.

POST-WIP682 PENDING TOKEN CONTROL -- `ERA_RENAISSCANCE` HITS ARE HISTORY COMMENTS ONLY
--------------------------------------------------------------------------------------
The eight current `CIV4BuildingInfos.xml` matches for the misspelled token `ERA_RENAISSCANCE` were
rechecked individually. All eight occur only inside comments of the form:

    <!-- advc.126: was ERA_RENAISSCANCE -->

The active era values beside those comments use the corrected `ERA_RENAISSANCE` token. Therefore
there is no unresolved XML enum lookup, loader fallback or current building-data consequence. Close
this mechanical integrity lead as a false-positive/history-text control; do not consume F714.

POST-WIP682 PENDING MARKUP CONTROL -- BARBARIAN SEVOPEDIA `[TAB]` / `[LINK]` STRING IS VALID
--------------------------------------------------------------------------------------------
The generic nested-bracket detector had flagged the current string:

    [TAB]See [LINK=CIVILIZATION_BARBARIAN]Barbarians (Civilization)[\LINK] for details.

`[TAB]` is a standalone Civ4 text-control token, not an opening tag requiring a closer. The
`[LINK=...] ... [\LINK]` pair is the ordinary Civ4 hyperlink markup used throughout the shipped
localization corpus (including many Base/AdvC Sevopedia strings). The same text key is actively
referenced by current Barbarian unit/building Civilopedia fields, but its markup is syntactically
consistent with the engine's established text language. Close safe; no F714.

POST-WIP682 PENDING MARKUP CONTROL -- `[%d1_Player` IS INTENTIONAL SPLIT FOOTER LABEL, NOT AN
UNCLOSED CIV4 TOKEN
------------------------------------------------------------------------------------------------
`AdvCiv-SAS_main.xml` contains the adjacent current keys:

    TXT_KEY_PEDIA_SAS_GAME_PLAYER_ID_PREV  ->  `[%d1_Player`
    TXT_KEY_PEDIA_SAS_GAME_PLAYER_ID_NEXT  ->  `%d1_Player]`

They are live: `SevoPediaMain.SAS_setGamePlayerIdNavigationTexts` formats them with the neighboring
current-game player IDs and renders them as the previous/next footer controls.

Reachable SAS practical 5732 / commit `79509304cf774cfa976132fe2ba40c508939605e`, titled
"add sevopedia game player id prev/next links", introduced both keys together with that UI. The same
commit explicitly documents the design in `SevoPediaMain.py`: bracket-direction labels were chosen
instead of literal `<-` / `->` because angle brackets on this footer text path empirically render as
broken markup such as `/color` and can corrupt the control text.

The two XML strings are therefore deliberately asymmetric visual fragments: the left control shows
`[<player id>` and the right control `<player id>]`. A generic markup checker sees the unmatched
square bracket but there is no named Civ4 control token being opened. This is intentional current
UI text, not an unclosed formatter/markup defect. Close safe; no F714.

FALSE-POSITIVE VALUE
--------------------
These checks sharpen the next integrity sweep: generic square-bracket balancing is not a valid Civ4
text invariant because the language contains standalone tokens (`[TAB]`, icons, paragraphs), paired
control markup (`[LINK] ... [\LINK]`) and intentional literal bracket fragments. Future scans should
parse known token forms or trace suspicious strings into their rendering context before promotion.

POST-PASS LEDGER / CONTINUATION
-------------------------------
Primary current-.cpp queue: 169 / 169 COMPLETE, 0 open.
F708 / provisional KI#1029: CONFIRMED at C031-WIP677.
F709 / provisional KI#1030: CONFIRMED at C031-WIP678.
F710 / provisional KI#1031: CONFIRMED at C031-WIP679.
F711 / provisional KI#1032: CONFIRMED at C031-WIP680.
F712 / provisional KI#1033: CONFIRMED at C031-WIP681.
F713 / provisional KI#1034: CONFIRMED at C031-WIP682.
F714 / provisional KI#1035: UNUSED next root ID.

Continue the post-primary pass with stronger producer-consumer/reference integrity seams and the
remaining explicitly parked current-live notes. Revalidate activation, current data and whole-album
ownership before promotion. Prefer gameplay/state/decision consequences over another presentation
nit when available; keep compile-disabled, malformed-external-input-only, extension-only, tuning-only
and current-data-masked observations below threshold.

Exact continuation stone:
C031-WIP683 physical -> primary 169-file queue remains COMPLETE -> post-pass F708-F713 confirmed ->
F714/KI#1035 UNUSED -> localization-markup pending cursors CLOSED SAFE -> continue mechanical
cross-file producer<->consumer/data integrity mining + strongest deferred notes -> current-source +
history + whole-album proof -> freeze next physical post-pass handoff at next settled boundary.

Files complete: 169 / 169. Files open: 0 / 169. Post-primary cross-file/deferred/data pass ACTIVE.
No gameplay/XML/Python source was modified by ChatGPT-5.6-Sol.
===============================================================================


================================================================================
C031-WIP684 -- POST-PRIMARY EFFECTIVE-ASSET REFERENCE INTEGRITY PASS / F714 UNUSED
================================================================================

Physical continuation from C031-WIP683. No gameplay/XML/Python source was modified.
The primary 169/169 file audit remains complete; post-primary F708-F713 remain the
last confirmed roots; F714 / provisional KI#1035 remains UNUSED.

Purpose of this checkpoint
--------------------------
WIP683 closed the two generic localization-markup anomalies without findings. This
pass then changed the integrity model from "does the current mod snapshot define the
literal?" to the actual Civ4 layered asset contract: vanilla Civ4 + Warlords + BtS +
current AdvCiv-SAS overrides/inherited assets. That distinction matters because a
mod intentionally references many base-game types and text keys that are not copied
into its own XML tree.

Settled controls
----------------
1. C++ literal InfoType / GlobalDefine lookups
   - The first raw-regex candidate, CLIMATE_TEMPERATE in TrueStarts.cpp, is valid:
     supplied Civ4CE vanilla CIV4ClimateInfo.xml defines CLIMATE_TEMPERATE.
   - The apparent ALLOW_UNUSUAL_STARTS GlobalDefine candidate in
     StartingPositionIteration.cpp is inside a block comment and is not executable.
   - A comment-aware scan of the current C++ tree therefore leaves 93 distinct active
     literal getInfoTypeForString(...) names and 919 distinct active literal
     GlobalDefine lookup names; every one resolves in the effective Civ4/Warlords/
     BtS/current-SAS XML stack.
   - Other raw-regex misses such as LEADER_ debugging and PROMOTION_DISORGANIZED were
     likewise in disabled/commented code and were excluded by the executable scan.

2. Python literal InfoType / GlobalDefine lookups
   - A lexical/comment-aware Python scan found 1 distinct literal info-type lookup and
     170 distinct literal GlobalDefine lookups in current shipped Python.
   - Every literal resolves in the same effective asset stack.

3. Current XML TXT_KEY references
   - Current non-Text SAS XML contains 5,162 distinct TXT_KEY_* references.
   - 54 are not defined by the supplied XML Text files, but all 54 are inherited
     references already present in vanilla/Warlords/BtS data (e.g. world help,
     era strategy and action help keys). SAS introduces zero new unresolved text-key
     references in this comparison.

4. Current XML ART_DEF references
   - Current SAS XML contains 560 distinct active ART_DEF_* references.
   - All 560 resolve against 741 ART_DEF definitions present in the effective supplied
     asset stack. No new broken art-definition reference surfaced.

5. CHANGED_TXT_KEY_CORPORATION_2 control
   - A broader symbolic-leaf scan made this live value look suspicious because it does
     not begin with TXT_KEY_. It is not a SAS placeholder regression: the same value is
     present in Base AdvC 1.14 and BtS, and BtS's CIV4GameText_BTS_Fixed.xml explicitly
     defines <Tag>CHANGED_TXT_KEY_CORPORATION_2</Tag>.
   - CLOSED SAFE / inherited valid localization key.

Interpretation
--------------
These are useful negative controls rather than reasons to spend a finding number.
They also validate a stricter rule for subsequent cross-file sweeps: references must
be checked against the effective layered Civ4 asset stack and comments/disabled code
must be excluded before treating a missing literal as evidence.

Ledger after WIP684
-------------------
Primary queue: 169/169 COMPLETE.
Post-primary confirmed: F708, F709, F710, F711, F712, F713.
Next root ID: F714.
Provisional Known Issues slot: KI#1035.
F714 / KI#1035: UNUSED.

Exact continuation
------------------
WIP684 physical -> F714/KI#1035 UNUSED -> continue semantic producer<->consumer
integrity mining beyond literal references -> prefer current-live gameplay/state/UI
consequences over generic robustness -> current-source revalidation + ancestry +
whole-album dedup mandatory -> freeze the next physical checkpoint at the next
settled boundary even if F714 remains unused.


================================================================================
C031-WIP685 -- POST-PRIMARY DATA-MASK REVALIDATION + GRAPH INTEGRITY / F714 UNUSED
================================================================================

Physical continuation from C031-WIP684. No gameplay/XML/Python source was modified.
Primary 169/169 remains complete. Post-primary F708-F713 remain confirmed. F714 /
provisional KI#1035 remains UNUSED.

Why this pass
-------------
The completed album deliberately preserved several real source defects below threshold only because
then-current XML made their dangerous branch impossible or semantically neutral. Because SAS data
continued evolving during the long audit, those masking assumptions were revalidated against the
2026-09-10 reconciled source instead of being trusted historically.

Changed mask: Hanging Gardens global population path -- REOPENED, THEN CLOSED SAFE
----------------------------------------------------------------------------------
An older Queue006 control recorded that current SAS had no building with a nonzero
`iGlobalPopulationChange`, so Civ4CE's historical Hanging Gardens bug family had no shipped witness
at that checkpoint. Current XML has since changed: `BUILDING_HANGING_GARDEN` now has
`iGlobalPopulationChange = 1`.

That legitimately reactivates the generic path, but current C++ already contains the important
Civ4CE repair shape. `CvCity::setNumRealBuildingTimed` loops the appropriate owner/team-share cities,
changes each population, and immediately calls `AI_updateAssignWork()` after the increment. Civ4CE's
BtS changelog describes the historical symptom as Hanging Gardens sometimes failing to give the
extra population in all cities; its patched CvCity.cpp likewise performs the population change and
immediate work reassignment. Current SAS therefore exercises repaired/coherent code. No F714.

Other previously masked controls rechecked -- masks still intact
----------------------------------------------------------------
* `CvPlayer::updateExtraYieldThresholds` natural-threshold comparator typo: current Trait XML still
  has only one positive natural threshold source (Financial commerce = 3), so no competing source
  exists to expose the iteration-order bug.
* Golden-Age readiness UnitClass vs consumption UnitType mismatch: current Golden-Age-capable units
  remain exactly Prophet/Artist/Scientist/Merchant/Engineer/Spy, one UnitType per UnitClass; no
  duplicate Golden-Age class exists.
* Event team-propagation refactor bug: all 339 current EventInfo records still have `bTeam = 0`.
* Quest-expiration holder/subject asymmetry: current data still has no EventInfo that is both Quest
  and Global.
* Hostile-goody MinBarbarians division hole: the two current bad barbarian goodies still have
  `iMinBarbarians = 1` and `2`, respectively.
* `AI_strictAdditionalHappy` No-Unhappiness arithmetic: Globe Theatre remains the only current
  `bNoUnhappiness = 1` building and remains outside the regular-building prefilter path in question.
* `CvCity::defensiveGarrison` enum-as-bool error remains disabled/neutral because
  `DEMAND_BETTER_PROTECTION = 0` and its obsolete percent remains 100.
* No-Espionage/trade-culture mismatch remains disabled because `USE_KMOD_TRADE_CULTURE = 0`.
* Corporation old-output seed-order defect remains dependency-masked: current produced corporation
  bonuses are Oil and Aluminum; no corporation that consumes one of those also produces a resource
  that can continue a stale-output production chain.
* Water-transition optional-child cursor trap remains extension/data robustness: current SAS does not
  override the water-plane file and both effective BtS WaterPlaneInfo records contain the optional
  WaterTransitionTexture child required by the current read cursor choreography.

Additional structural data controls
-----------------------------------
The current authored graphs were checked for cycles/self-links because several C++ walkers assume
progress through these relations:
* technology prerequisite graph: no cycle, no self prerequisite;
* UnitClass upgrade graph: no cycle, no self upgrade;
* Improvement upgrade graph: no cycle, no self upgrade;
* Promotion prerequisite graph: no cycle, no self prerequisite.

A rough whole-DLL enum-truthiness scan was also attempted. Two apparent direct-Boolean uses of
`getUnit()` in CyGameTextMgr were scanner collisions: `CyUnit::getUnit()` returns `CvUnit*`, not a
UnitTypes enum. No finding is inferred from name-only overload matching; future variants of this
check must retain class-qualified return identity.

Ledger after WIP685
-------------------
Primary queue: 169/169 COMPLETE.
Post-primary confirmed: F708, F709, F710, F711, F712, F713.
F714 / provisional KI#1035: UNUSED.

Exact continuation
------------------
WIP685 physical -> F714/KI#1035 UNUSED -> continue high-signal cross-file semantic checks and
Civ4CE/base-oracle comparisons -> prioritize source defects whose old masking/caller assumptions
have changed or whose producer/consumer identity can now be proved -> whole-album dedup + ancestry
before promotion -> freeze next physical checkpoint at the next settled result.


================================================================================
C031-WIP686 -- AUDIT-THE-AUDIT-FIXES PASS: KI#744 + KI#809/KI#810 CLEAN / F714 UNUSED
================================================================================

Physical continuation from C031-WIP685. No gameplay/XML/Python source was modified.
Primary 169/169 remains complete. Post-primary F708-F713 remain confirmed. F714 /
provisional KI#1035 remains UNUSED.

Why this pass
-------------
The C031 audit itself generated repair commits while older files were already marked COMPLETE.
Those repair deltas therefore deserve a separate regression pass: verify that a narrowly correct
finding repair did not omit a sibling mutator, update a cache at the wrong transaction boundary,
or change another shipped caller's semantics after the original per-file audit had closed.

Repair re-audit: practical 6436 / KI#744 inflation-neutral corporation maintenance -- CLEAN
-----------------------------------------------------------------------------------------------
The repair snapshots every living player's effective `calculateInflationRate()` immediately before
`incrementGameTurn()` + `incrementElapsedGameTurns()`, then calls `updateMaintenance()` only when
the rounded rate changes. This matches the exact integer rate used by
`CvCity::calculateCorporationMaintenanceTimes100`, which divides corporation maintenance by
`100 + calculateInflationRate()` before the player's ordinary expenses are inflated again.

The dependency/mutator check closes the obvious omission risks:
* native `calculateInflationRate()` depends on the two game clocks, game speed, handicap and the
  player's `m_iInflationModifier`;
* the two clocks are exactly the values straddled by the new before/after comparison;
* current native source has no second gameplay mutator of `m_iInflationModifier`: initialization/
  serialization plus `CvPlayer::applyEvent` are the only writes, and the event write now refreshes
  maintenance immediately;
* event effects that can subsequently change city population, buildings, resource/corporation
  state or maintenance modifiers have their own maintenance/corporation invalidation paths, so the
  inflation refresh is not the final/only refresh when the same EventInfo later changes such state;
* Python-exposed game-length setters can theoretically alter the inflation cap externally, but
  current shipped Python has no caller. That remains API hardening, not a supported repair defect.

No repair-side regression or uncovered current-live native mutator survived.

Repair re-audit: practical 6435 / KI#809 + KI#810 stale event replies -- CLEAN
--------------------------------------------------------------------------------
KI#809's new `bRequiresUnit` predicate was compared directly with `CvUnit::applyEvent`. It covers
all five concrete unit-local payload families consumed there: disbanding, experience/healing,
immobility, promotion and renaming. A contextual unit ID remains optional for outcomes with none of
those effects, while an existing unit still goes through `CvUnit::canApplyEvent`.

KI#810 moved `setTriggerFired` from before the authoritative reply-time `canDoEvent` check to after
a successful validation. The ordering-sensitive shipped surfaces were checked explicitly:
* current EventTrigger XML has no active trigger-level `PythonCallback`, so delaying
  `afterEventTriggered` cannot change a shipped trigger callback's validation inputs;
* current EventInfo PythonCanDo callbacks do not inspect trigger-fired state;
* native `canDoEvent` only has a potentially relevant trigger-fired dependency in its Quest
  completion check. Three current triggers list one of their own possible Events in PrereqEvents
  (`SLAVE_REVOLT_AGAIN`, `FARM_BANDITS_AGAIN`, `DUSTBOWL_CONT`), but every overlapping EventInfo is
  non-Quest. Thus the old pre-validation firing of the current trigger is not required by that
  shipped branch.

Rejected stale choices still delete their pending EventTriggeredData when `bUpdateTrigger` is true,
matching the pre-existing cancellation path, but they no longer durably consume/propagate the
trigger first. No current shipped semantic dependency on the old erroneous ordering survived.

Ledger after WIP686
-------------------
Primary queue: 169/169 COMPLETE.
Post-primary confirmed: F708, F709, F710, F711, F712, F713.
F714 / provisional KI#1035: UNUSED.

Exact continuation
------------------
WIP686 physical -> dedicated audit-the-audit-fixes pass OPEN -> F714/KI#1035 UNUSED -> continue
backward through substantive repair commits (prefer stateful/cross-file repairs first) -> check
sibling mutators/callers + transaction ordering + current data activation -> whole-album dedup and
ancestry before any new promotion -> freeze another physical checkpoint at the next settled tranche.


================================================================================
C031-WIP687 -- F714 / KI#1035 CONFIRMED: KI#789 LOSS-SIDE REPAIR RUNS TOO EARLY
================================================================================

Physical continuation from C031-WIP686. No gameplay/XML/Python source was modified.
Primary 169/169 remains complete. Post-primary F708-F713 remain confirmed.

F714 / provisional KI#1035 -- SAS KI#789 maintenance repair misses vassal city loss
----------------------------------------------------------------------------------

STATUS
------
CONFIRMED current SAS repair-side defect. Practical 6419 / b832508039cbe1e7a7b5226bfe9dcb6f39c00778
(2026-09-08) correctly added a master-maintenance invalidation to
`CvTeam::changeNumCities`, but the city-destruction transaction reaches that hook before the
losing player's authoritative `m_cities` container has removed the doomed city. Founding/capture
through ordinary `initCity` ordering is safe; loss/raze can remain stale.

ORIGINAL KI#789 DEPENDENCY
--------------------------
`CvCity::calculateNumCitiesMaintenanceTimes100` computes the master burden by reading the live city
count of every living vassal player:

    int iNumVassalCities = 0;
    for (PlayerIter<ALIVE,VASSAL_OF> it(kOwner.getTeam()); it.hasNext(); ++it)
        iNumVassalCities += it->getNumCities();

    iNumVassalCities /= std::max(1, MemberIter::count(kOwner.getTeam()));
    int iMaintenanceCities = kOwner.getNumCities() + iExtraCities +
            iNumVassalCities / 2;

`CvPlayer::getNumCities()` is the count of the player's `m_cities` free-list, not the team's
`m_iNumCities` counter:

    DllExport int getNumCities() const { return m_cities.getCount(); }

Thus the repair must run only after the relevant player's city container represents the new state.

WHY POSITIVE CITY CHANGES ARE SAFE
----------------------------------
`CvPlayer::initCity` inserts the new city object first:

    CvCityAI* pCity = m_cities.AI_add();
    ...
    pCity->init(...);

Inside `CvCity::init`, only later does the transaction call:

    GET_TEAM(getTeam()).changeNumCities(1);

Practical 6419's new `CvTeam::changeNumCities(+1)` master refresh therefore sees
`vassalPlayer.getNumCities()` with the newly added city already counted. Ordinary vassal founding
and the new-owner half of acquisition are correctly refreshed.

WHY NEGATIVE CITY CHANGES ARE NOT SAFE
--------------------------------------
`CvCity::kill` has the opposite ordering:

    GET_TEAM(eOwner).changeNumCities(-1);
    GC.getGame().changeNumCities(-1);
    ...
    kOwner.deleteCity(getID());
    ...
    kOwner.updateMaintenance();

Practical 6419 changed `CvTeam::changeNumCities` to:

    m_iNumCities += iChange;
    ...
    if (iChange != 0 && isAVassal() && GC.getGame().isFinalInitialized())
    {
        for (MemberIter it(getMasterTeam()); it.hasNext(); ++it)
            it->updateMaintenance();
    }

But `kOwner.deleteCity(getID())` has not happened yet when the master members are refreshed.
`CvPlayer::deleteCity` is the operation that removes the city from `m_cities`:

    void CvPlayer::deleteCity(int iID)
    {
        m_cities.removeAt(iID);
    }

Therefore every master `updateMaintenance()` triggered by the new negative `changeNumCities`
recomputes through the old vassal `getNumCities()` value and stores the stale result again.

NO LATER MASTER REFRESH REPAIRS IT
----------------------------------
After deletion, `CvCity::kill` calls only `kOwner.updateMaintenance()`, where `kOwner` is the vassal
that lost the city. `CvGame::changeNumCities(-1)` merely adjusts the global city counter. A
whole-current-source search finds no second master-maintenance refresh later in the ordinary kill
path.

Consequently ordinary razing, city destruction, or loss to a player outside the same master-vassal
family can leave the master's cached number-of-cities maintenance at the pre-loss value until an
unrelated invalidator runs.

During acquisition there is one important incidental control: if the replacement city is inserted
into another vassal of the SAME master, that later +1 path refreshes the same master after both city
containers have reached their final counts and can mask the defect. This does not rescue ordinary
vassal loss to a third party, razing, disbanding or destruction.

DETERMINISTIC INTEGER BOUNDARY
------------------------------
A minimal count proof uses a singleton master and singleton vassal.

Before loss:

    vassal cities = 4
    master members = 1
    per-member vassal cities = 4 / 1 = 4
    half-count contribution = 4 / 2 = 2

After one vassal city is actually removed, the correct contribution is:

    3 / 1 = 3
    3 / 2 = 1

So every affected master city should immediately recompute with one fewer maintenance city.
Instead the negative `changeNumCities` callback runs while `getNumCities()` still returns 4,
recomputes the contribution as 2, and caches the old charge. The later `deleteCity` changes the
source count to 3 but performs no master refresh.

This is the reverse-side case already anticipated by the original F466/KI#789 proof (10 -> 9 in its
numeric example), but it survives specifically because the SAS repair chose the team-count mutation
as the callback boundary even though the maintenance formula reads the player city container.

ANCESTRY / REPAIR INTRODUCTION
------------------------------
The original missing-invalidation root is inherited BtS/K-Mod/AdvCiv and was confirmed as
F466/KI#789. Current Base AdvCiv 1.14 still lacks the repair entirely.

The CURRENT asymmetric defect is introduced by SAS practical 6419, commit
`b832508039cbe1e7a7b5226bfe9dcb6f39c00778`, titled:

    Fix BtS/AdvCiv maintenance and Permanent-Alliance caches
    (KI#788, KI#789, KI#792, KI#793) ...

No later SAS commit in the supplied reachable-history cache modifies
`CvTeam::changeNumCities`; current source still has the practical-6419 hook unchanged.

DEDUP / RELATION TO KI#789
--------------------------
This is deliberately recorded as a post-repair finding rather than pretending the old F466 proof
was wrong:
* F466/KI#789 proved the inherited dependency and that BOTH positive and negative vassal city-count
  changes lacked any master invalidation before the fix.
* Practical 6419 repairs positive changes because player `m_cities` is already updated before +1.
* The same hook does NOT repair negative city changes because player `m_cities` is updated after -1.

The whole reconciled album was searched for this practical-6419 ordering defect, `changeNumCities(-1)`
vs `deleteCity`, and post-delete master maintenance. No existing numbered finding owns this new
current-source repair-side ordering root.

CONSERVATIVE REPAIR BOUNDARY
----------------------------
Do not merely assume the team's `m_iNumCities` counter is the authoritative dependency: the formula
reads `CvPlayer::getNumCities()`.

The least risky correction is to ensure the master is refreshed once AFTER `m_cities.removeAt(iID)`
has completed on a vassal city deletion. Keeping the existing earlier refresh is redundant but
correctness-safe and avoids reordering old `CvCity::kill` transaction semantics such as `cityLost`
callbacks around team/global city counters.

A cleaner helper could centralize "refresh current master maintenance if this team is a vassal" and
be called both from the existing positive/team-count path and immediately after player-city removal.
Reordering `GET_TEAM(eOwner).changeNumCities(-1)` past `deleteCity()` is a broader lifecycle change
and should not be preferred without separately auditing all callbacks/observers between those two
boundaries.

REGRESSION TEST
---------------
1. Establish singleton master A and singleton vassal V.
2. Give V an even vassal-city count such as 4 so losing one crosses the K-Mod half-count integer
   boundary.
3. Record A's cached total maintenance.
4. Raze/destroy/lose one V city to a party that does not cause a later same-master vassal +1.
5. Immediately compare A's cached total against the value after an explicit A.updateMaintenance().
6. The repaired path must show no difference; the current practical-6419 path should differ.
7. Repeat a +1 founding control and verify it is already correct.

NUMBERING
---------
F714 / provisional KI#1035 is CONFIRMED.
F715 / provisional KI#1036 is the next unused root.

Ledger after WIP687
-------------------
Primary queue: 169/169 COMPLETE.
Post-primary confirmed: F708, F709, F710, F711, F712, F713, F714.
F715 / provisional KI#1036: UNUSED.

Exact continuation
------------------
WIP687 physical -> audit-the-audit-fixes pass remains OPEN -> F715/KI#1036 UNUSED -> continue
backward through substantive repair commits, prioritizing stateful/cross-file fixes and sibling
mutation ordering -> current-source proof + activation + whole-album dedup mandatory -> freeze the
next physical checkpoint at the next settled tranche.


================================================================================
C031-WIP688 -- REPAIR RE-AUDIT 6420-6423 CLEAN / F715 UNUSED
================================================================================

Physical continuation from C031-WIP687. No gameplay/XML/Python source was modified.
Primary 169/169 remains complete. Post-primary F708-F714 remain confirmed. F715 /
provisional KI#1036 remains UNUSED.

Practical 6420 / KI#790 -- -100% anarchy-timer repair -- CLEAN
----------------------------------------------------------------
The singularity repair was rechecked across all three active timers and current native activation.
For an old positive scale, the code proportionally changes revolution/conversion/anarchy timers and
clamps a crossing to scale zero. For an already-singular old scale, the modifier is changed first
and any still-active revolution/conversion cooldown is rebuilt from the new clamped minimum rather
than dividing by an invented denominator.

One sibling concern was checked explicitly: the repair does not rebuild positive `m_iAnarchyTurns`
when the OLD scale is already <=0, relying on the normal voluntary-reform state being zero there.
Current shipped native data preserves that assumption. The only non-voluntary C++ path capable of
adding player-wide anarchy independently is the espionage `iPlayerAnarchyCounter` effect, and all
current `CIV4EspionageMissionInfo.xml` records have `iPlayerAnarchyCounter = 0`. A positive-scale
voluntary anarchy that receives a modifier crossing to <=0 is cleared by the repaired proportional
branch at that crossing. Python can mutate anarchy directly, but that is extension/API state rather
than a current shipped repair regression.

Practical 6421 / KI#794 -- elimination war-weariness repair -- CLEAN
-------------------------------------------------------------------
The new pre-teardown snapshot is gated to final-team death (`AliveCount == 1` before the player is
removed) and uses `PlayerIter<MAJOR_CIV,ENEMY_OF>` while the direct war relation still exists. That
matches the set of major living players whose cached `updateWarWearinessPercentAnger()` can change
when `CvTeam::changeAliveCount` clears every war of the now-dead team. A non-final member death does
not clear the team war and therefore correctly does not force the refresh.

The post-teardown refresh occurs before the surviving players' next city/economy processing and no
current native intermediate action invalidates the saved player identities. Minor/Barbarian players
are correctly outside the same war-weariness anger contract. No sibling current-live omission
survived.

Practical 6422 / KI#795 -- Advanced Start Railroad legality repair -- CLEAN for current data
--------------------------------------------------------------------------------------------
Concrete route additions now accept any matching Build for which ordinary `CvPlayer::canBuild`
passes; generic price queries and removals retain the lighter inherited technology test. This
correctly reaches the route resource requirement through ordinary plot/build legality and preserves
alternative matching BuildInfos.

A repair-side overconstraint was checked: default `canBuild` also enforces ordinary Worker build
gold cost, whereas Advanced Start spends points. Current SAS `BUILD_ROAD` and `BUILD_RAILROAD` both
have `iCost = 0`, so this extra check is neutral for every current route Build. It would matter to a
future/modded positive-cost route Build and is worth remembering as extension robustness, but it is
not a current F715.

Practical 6423 / KI#799 -- religious-building-commerce cache repair -- CLEAN
----------------------------------------------------------------------------
The external cache inputs and native mutators were inventoried directly:
* `m_eLastStateReligion` is changed only through `setLastStateReligion` after initialization/read;
* `m_iStateReligionCount` changes only through `changeStateReligionCount`, whose sole native live
  caller is civic processing;
* `m_aiStateReligionBuildingCommerce` changes only through
  `changeStateReligionBuildingCommerce`, reached from building processing.

The repair covers each authoritative transition at the correct boundary:
* `setLastStateReligion` snapshots effective `getStateReligion()`, changes the remembered religion,
  and rebuilds building commerce only when effective identity actually changes;
* `setCivics` preserves the old effective religion across removal+addition of civic effects and
  rebuilds once after both sides if the effective state-religion identity changed;
* empire-wide state-religion building-commerce changes rebuild the affected commerce cache in every
  owned city immediately.

Initialization/serialization direct field writes remain outside the runtime invalidation contract,
and the existing turn-boundary safety rebuild continues to cover time-based building-commerce
changes. No current repair-side gap survived.

Ledger after WIP688
-------------------
Primary queue: 169/169 COMPLETE.
Post-primary confirmed: F708, F709, F710, F711, F712, F713, F714.
F715 / provisional KI#1036: UNUSED.

Exact continuation
------------------
WIP688 physical -> audit-the-audit-fixes pass OPEN -> F715/KI#1036 UNUSED -> continue with
practical 6424 city-site knowledge repairs, then stateful city-AI/citizen repair commits -> test
whether a repair substitutes a remembered/known value consistently across every downstream helper,
or fixes one cache/heuristic while leaving a sibling consumer stale -> whole-album dedup + ancestry
before promotion -> freeze next physical checkpoint at the next settled tranche.

================================================================================
C031-WIP689 - post-primary audit-the-audit-fixes continuation
Practical 6424/6425 repair revalidation CLEAN; F715 / provisional KI#1036 CONFIRMED
CvCityAI::AI_yieldValue - recovered temporary-anger layers are added as one happy citizen each,
although Conscript and Defy-Resolution layers represent 3 and 5 angry citizens respectively
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary cross-file / repair-side pass OPEN.
F714 / provisional KI#1035 remains CONFIRMED from WIP687 (KI#789 vassal-master maintenance
repair refreshes too early on city deletion).
F715 / provisional KI#1036 is now CONFIRMED.
F716 / provisional KI#1037 is the next UNUSED root.
Repository gameplay/XML/Python source remains untouched.

INTERMEDIATE REPAIR REVALIDATION - PRACTICAL 6424 CLEAN
------------------------------------------------------
Practical 6424 / 17dc160c6aec616ec50fce203f93f4ddc4285710 fixed KI#493, KI#495 and
KI#496 in CitySiteEvaluator.cpp. The post-repair audit traced the changed values through the
rest of the evaluator rather than checking only the edited lines.

1. `isUsablePlot` establishes foreign ownership from the same knowledge helper later used by the
   repaired paths:

       PlayerTypes const eOwner = getRevealedOwner(*p);
       bForeignOwned = (eOwner != NO_PLAYER && eOwner != ePlayer);

   Thus the KI#495 culture path does not classify a plot from live ownership and then count cities
   from remembered ownership. Both sides use the evaluator's same revealed-owner state.

2. `countKnownCities` preserves the evaluator's two intended modes:
   - all-seeing evaluation bypasses contact/deduction gates and therefore retains exact setup/
     normalization behavior;
   - ordinary evaluation skips foreign civilizations not met by the evaluating team and then
     requires `kTeam.AI_deduceCitySite` for foreign city sites.
   Own/team cities do not require city-site deduction.

3. KI#493's replacement of `CvArea::getNumCivCities()` therefore no longer lets an unknown rival
   settlement remove the first-colony bonus merely through a complete area city total.

4. KI#495's owner-filtered call uses the same revealed owner that made the tile foreign-owned.
   It does not fall back to an unfiltered area total.

5. KI#496's `getRevealedOwner` is a genuine remembered snapshot rather than an alias for live
   ownership. `CvPlot::setOwner` calls `updateRevealedOwner` for every alive team, and that helper
   only updates a team's remembered owner when `plotThatRevealsOwner` finds current visibility on
   the tile or an adjacent witness. A hidden border change can therefore leave the old remembered
   owner exactly as the KI#496 repair intends.

6. An owning team normally gains adjacent sight when ownership is established, so the repaired
   future-BFC helper does not create a routine self-ownership blindness on the ordinary own-border
   path. Former/foreign ownership can remain stale under fog by design.

The still-pending broader knowledge-boundary issues KI#497-KI#502 remain independent; no repair-
introduced sibling leak or polarity mismatch was established in practical 6424.

INTERMEDIATE REPAIR REVALIDATION - PRACTICAL 6425 CLEAN
------------------------------------------------------
Practical 6425 / 8d365b55f31620cd1fbe697db6948b1d56019ca8 fixed KI#844, KI#852 and
KI#856 in CvCityAI.cpp. All three repairs survive their downstream-use check.

KI#844 - AI_countOvergrownBonuses
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The corrected comparison

    p.getOwner() == getOwner()

matches domains (`PlayerTypes` vs `PlayerTypes`) and the function's purpose: count blocked bonuses
inside this city's plots that belong to this city's owner. The caller consumes only the resulting
count as a technology-value bonus. No city-ID semantic remained after the correction.

KI#852 - project production emphasis
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The repair computes

    isProductionProject() ||
    (eProductionBuilding != NO_BUILDING &&
     GC.getInfo(eProductionBuilding).isWorldWonder())

before entering building-only logic. A production order cannot simultaneously be a Project and a
Building, so the common +50 production / -25 commerce shift is applied exactly once. Building-
specific `getFoodKept` handling remains building-only. The pre-existing Barbarian early return is
unchanged in semantic position, so the fix does not newly alter that separate policy.

KI#856 - cultural-building prerequisite reserve
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The repaired loop sets `bHaveEnough=false` immediately before the retained early `break` when a
prerequisite building class is short. The Boolean's later consumer is exactly the intended branch
that decides whether a near-top cultural city may receive the less severe extra-building penalty.
No stale cross-iteration state or opposite-polarity consumer was found.

F715 / provisional KI#1036 CONFIRMED
====================================
ROOT
----
Practical 6426 / 4d919801201d26a1679385b0c9d634584a0483d7 fixed four inherited
`CvCityAI::AI_yieldValue` temporary-anger forecast defects (KI#853, KI#854, KI#858 and KI#860) by
introducing:

    static int SAS_angerLayersRecoveredBeforeGrowth(
        int iTimer, int iAngerLength, int iTurnsToGrow)

The helper correctly computes the NUMBER OF TIMER LAYERS that disappear before projected growth.
The caller then adds that number directly to `iFutureHappy` for all three anger sources:

    iFutureHappy += SAS_angerLayersRecoveredBeforeGrowth(
        getHurryAngerTimer(), flatHurryAngerLength(), iApproxTurnsToGrow);
    iFutureHappy += SAS_angerLayersRecoveredBeforeGrowth(
        getConscriptAngerTimer(), flatConscriptAngerLength(), iApproxTurnsToGrow);
    iFutureHappy += SAS_angerLayersRecoveredBeforeGrowth(
        getDefyResolutionAngerTimer(), flatDefyResolutionAngerLength(), iApproxTurnsToGrow);

That is dimensionally correct only for Hurry anger. `iFutureHappy` is later added directly to

    iHappinessLevel = happyLevel() - unhappyLevel(0)

and then used as a number of citizens of future growth headroom:

    iPopToGrow = max(0, iHappinessLevel + iFutureHappy)

But one timer layer does NOT mean one angry citizen for every source.

EFFECTIVE CURRENT DATA
----------------------
Current SAS does not override the relevant base BtS values. The effective shipped BtS
GlobalDefines are:

    HURRY_POP_ANGER             = 1
    CONSCRIPT_POP_ANGER         = 3
    DEFY_RESOLUTION_POP_ANGER   = 5
    PERCENT_ANGER_DIVISOR       = 1000

Therefore:
- one Hurry layer represents 1 angry citizen;
- one Conscript layer represents 3 angry citizens;
- one Defy-Resolution layer represents 5 angry citizens.

AUTHORITATIVE ENGINE ARITHMETIC
-------------------------------
`CvCity::getConscriptPercentAnger` computes, for a positive timer:

    1 + (((((timer - 1) / flatConscriptAngerLength()) + 1) *
        CONSCRIPT_POP_ANGER * PERCENT_ANGER_DIVISOR) /
        max(1, population + iExtra))

Defy Resolution uses the same shape with `DEFY_RESOLUTION_POP_ANGER`.

`CvCity::unhappyLevel` then ADDS those percentage contributions with all other anger and performs
the authoritative conversion back to unhappy citizens:

    iUnhappiness = (iAngerPercent * (population + iExtra)) /
        PERCENT_ANGER_DIVISOR;

So the layer count and the recovered unhappy-citizen count are different quantities.

CLEAN SINGLE-LAYER FIXTURE
--------------------------
Use a population-10 city with:

    Conscript anger length L = 10
    Conscript timer T = 9
    no Hurry anger
    no Defy-Resolution anger
    projected target growth G = 10 doGrowth executions away

This deliberately avoids every adjacent old finding:

- not F530 / KI#853: T=9 is not an exact multiple of L;
- not F531 / KI#854: nine decrements occur STRICTLY before the 10th projected growth;
- not F535 / KI#858's old +2 gate: practical 6426 removed that gate;
- not F537 / KI#860: there is exactly ONE Conscript timer layer, not a stack.

The practical-6426 helper correctly says:

    layersNow = 1
    decrementsBeforeGrowth = G - 1 = 9
    timerAtGrowth = 0
    layersAtGrowth = 0
    recoveredLayers = 1

and therefore adds only:

    iFutureHappy += 1

But at population 10 the current Conscript source itself is:

    1 + (1 * 3 * 1000 / 10)
    = 301 per-mille anger

and its isolated unhappy-population contribution is:

    301 * 10 / 1000
    = 3 unhappy citizens

When the timer reaches zero before target growth, that source disappears. Its ordinary magnitude
is therefore a recovery of 3 unhappy citizens, not 1. Because the total `unhappyLevel` rounds only
after adding all percentage sources, other simultaneous percentage anger can move a boundary by a
small rounding amount; it cannot make `+1 per Conscript layer` an exact representation of the
shipped 3-citizen source. A simple state with compatible remaining anger/happiness gives the exact
3-point delta above.

The same mismatch is larger for a single Defy-Resolution layer. At population 10:

    1 + (1 * 5 * 1000 / 10) = 501 per-mille
    501 * 10 / 1000 = 5 unhappy citizens

while the repaired forecast again adds only +1.

LIVE AI CONSEQUENCE
-------------------
This feeds the normal citizen/yield valuation decision rather than diagnostics.

For a clean Conscript example, choose other current happiness/anger so that the city's net
`iHappinessLevel` is -2 while the single 3-citizen Conscript layer above is active, and ensure
there are sufficient good future jobs.

Current repaired forecast:

    -2 + 1 = -1
    => iPopToGrow = 0

Magnitude-correct forecast after that Conscript layer has actually expired:

    -2 + 3 = +1
    => iPopToGrow = 1

Thus food growth value can remain disabled even though the temporary anger that will disappear
before growth creates real population headroom. Defy Resolution can understate the same headroom
by four additional citizens per recovered layer.

LINEAGE
-------
This magnitude mistake is older than SAS. The reconstructed BtS `CvCityAI::AI_yieldValue` already
increments happiness by exactly one after each source's next timer expiry, including Conscript and
Defy Resolution. K-Mod converted those increments into `iFutureHappy++`; Base AdvCiv 1.14 retained
the same one-point source magnitude.

Practical 6426 fixed several orthogonal timing/count defects but preserved that inherited
assumption by replacing each source with a generic `recovered layer count` and adding it directly
to `iFutureHappy`.

So F715 is best described as an INHERITED BtS/K-Mod/AdvCiv magnitude defect left incomplete by the
SAS practical-6426 anger repair, not as a behavior newly invented by practical 6426.

DEDUP / INDEPENDENCE
--------------------
Whole-album checks found no existing owner for this per-layer population magnitude.

Adjacent confirmed roots are independent:

- F530 / KI#853: exact-cycle modulo predicts the next drop at zero turns.
- F531 / KI#854: equality counts a timer decrement that occurs after the target growth.
- F535 / KI#858: an old hardcoded +2 pre-gate could suppress three independent recovery channels.
- F537 / KI#860: only one recovery was counted per source even when MULTIPLE timer layers expired.

F715 survives after all four are repaired. Its fixture has one non-exact-cycle layer whose expiry
is strictly before growth; the remaining error is that one recovered Conscript/Defy layer is not
one recovered unhappy citizen.

The old F537 repair direction itself explicitly proposed adding `layersNow - layersAtGrowth` to
`iFutureHappy`; practical 6426 implemented that direction faithfully. F715 is the newly isolated
missing multiplicative/source-magnitude dimension in that earlier reasoning.

CONSERVATIVE REPAIR DIRECTION
-----------------------------
Do NOT change the timer-layer helper's phase arithmetic; it correctly consolidates KI#853,
KI#854 and KI#860.

At minimum, translate recovered layers into the source's actual angry-population magnitude before
adding to `iFutureHappy`, e.g. conceptually:

    hurryRecoveredLayers      * HURRY_POP_ANGER
    conscriptRecoveredLayers  * CONSCRIPT_POP_ANGER
    defyRecoveredLayers       * DEFY_RESOLUTION_POP_ANGER

A still more exact implementation could project the temporary anger percentages at current and
pre-growth timers and convert their aggregate difference through the same population/divisor
rounding contract as `unhappyLevel`, especially if arbitrary modded define values or rounding at
mixed anger sources matter. The important repair invariant is that `iFutureHappy` must remain in
UNHAPPY-CITIZEN units, not raw timer-layer units.

REGRESSION CONTROLS
-------------------
A. Single Conscript layer:
   P=10, L=10, T=9, G=10 -> recovered timer layers 1; expected ordinary recovered unhappy
   magnitude 3, not 1.

B. Single Defy layer:
   same geometry -> expected ordinary recovered unhappy magnitude 5, not 1.

C. Hurry control:
   one Hurry layer retains magnitude 1.

D. Multi-layer control:
   two Conscript layers expiring before growth must preserve KI#860's full layer count AND apply
   the per-layer Conscript magnitude rather than collapsing back to one source recovery.

E. Phase/exact-cycle controls:
   preserve practical 6426's G-1 decrement rule and exact-cycle behavior from KI#853/KI#854.

PHYSICAL CHECKPOINT
-------------------
C031-WIP689 is the authoritative physical stone.

Exact continuation:

WIP689 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> audit-the-audit-fixes pass OPEN -> continue with practical
6427 and later stateful CvCityAI/citizen repair commits -> for every repair, trace repaired state
through sibling consumers and transaction ordering -> whole-album dedup + ancestry before any
promotion -> freeze the next physical WIP at the next settled tranche/finding.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP689
================================================================================

================================================================================
C031-WIP690 - audit-the-audit-fixes continuation
Practicals 6427-6429 repair revalidation CLEAN; F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

PRACTICAL 6427 / a5dca200642c9bdef7c9bcd16aea9ea799755987 - CLEAN
-------------------------------------------------------------------------------
KI#857 (`CvCityAI::AI_doPanic`): the repaired existing-production shortcut now requires the queued
combat unit to be DOMAIN_LAND. The surrounding panic calculation compares local land attack and
defense. The fallback itself may choose UNITAI_CITY_COUNTER, then UNITAI_CITY_DEFENSE, then
UNITAI_ATTACK, so the repaired shortcut was not intended to require only a formal city-defense AI
role. A positive-combat land unit is inside the same broad domain of emergency response; the old
false satisfaction by a combat ship is removed without creating an opposite-domain regression.

KI#848 (`AI_buildGovernorChooseProduction`): `eExperienceBuilding` is obtained from
`AI_bestBuildingThreshold(BUILDINGFOCUS_EXPERIENCE)`, validated against the already-selected unit's
free-XP channels, and the repair now queues that exact candidate. No later branch assumes the old
general `eBestBuilding` was queued. Production orders are mutually exclusive, so no duplicate
building target is introduced.

PRACTICAL 6428 / bc6376aeff484f797186dd73abecb08a50e6f25b - CLEAN
-------------------------------------------------------------------------------
KI#845 (`AI_doEmphasize`): the function explicitly asserts `!isHuman()`. Therefore clearing each
stale AI emphasis whenever special Production reaches the takeover threshold cannot erase a human
player's manual emphasis state. The loop clears every old Emphasize type before continuing, which
matches the pre-regression state transition rather than leaving a mixed stale/new emphasis set.

KI#851 (`AI_removeWorstCitizen`): the ordinary specialist-removal branch already requires assigned
count > forced count and therefore cannot produce assigned < forced. The final fallback intentionally
relaxes the ignore/force restriction when no other citizen can be removed; that is precisely where
the repair lowers the removed specialist type's force target after decrement. The setter uses the
post-removal assigned count, so it removes only the now-impossible excess target and does not clear
additional still-satisfiable forced specialists.

PRACTICAL 6429 / c5db3498a6d127e69f777e286a7dcf69a255b7b8 - CLEAN
-------------------------------------------------------------------------------
KI#847 (`AI_citizenSacrificeCost`): when specialist jobs are needed, the repaired code caches
`iSpecialistCount` and multiplies that type's aggregate yield and commerce contributions by the same
count already used for `iTotalScore` and `job_scores`. Each vector job still retains one specialist's
score. This restores matching numerator/denominator population multiplicity; no extra multiplication
was found downstream.

KI#850 (`AI_culturePressureFactor`): the potentially overflowing product is widened before
multiplication. Before this point `iAnswer` is a nonnegative sum of per-plot/per-rival squared
percent-scale terms. The subsequent factor

    estimateEndTurn / (gameTurn + estimateEndTurn)

is nonnegative and <=1 under the ordinary game-turn contract, so the 64-bit quotient cannot exceed
the original `iAnswer`. The cast back to int therefore does not undo the overflow repair; the final
500 cap continues to bound the public pressure factor.

No independent repair-side root survived practicals 6427-6429.

PHYSICAL CHECKPOINT
-------------------
C031-WIP690 is authoritative.

Exact continuation:
WIP690 physical -> F716/KI#1037 UNUSED -> audit practical 6430 event pillage maximum repair, then
6431-6434 logging/lifecycle additions where their new observational hooks could perturb state or
read objects after mutation -> continue backward/forward through any remaining substantive repair
commits -> whole-album dedup + ancestry before promotion -> freeze next physical WIP at next settled
finding/tranche.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP690
================================================================================

================================================================================
C031-WIP691 - audit-the-audit-fixes continuation
Practicals 6430-6434 repair/instrumentation revalidation CLEAN; F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

PRACTICAL 6430 / a634f58b00e3ac8ef41bf3fabd1df7c89e86b1f7 - CLEAN
-------------------------------------------------------------------------------
KI#736 repairs the inherited BtS random-event pillage upper bound in both city- and empire-scoped
execution. Each path still performs exactly one synchronized RNG draw; only the bound changes from
`Max-Min` to `Max-Min+1`, matching the XML/help contract that both endpoints are inclusive.
`Max==Min>0` naturally becomes `SyncRandNum(1)` and returns the sole configured value. The existing
`MaxPillage>0` gate and `Max>=Min` assertion remain. No new RNG call, selection, or state ordering is
introduced.

PRACTICAL 6431 / 22aec5ffbb588a2d280e8a68480d1195642c3c87 - CLEAN
-------------------------------------------------------------------------------
The new SASGameRecord random-event lifecycle instrumentation was re-audited as gameplay code rather
than presumed harmless because it "only logs."

- All expensive target/result work is gated by `gGameRecordLogLevel >= 2`; disabled and level-1
  logging retain the old hot paths apart from cheap branch checks at the explicit hook boundaries.
- `CvPlayer::applyEvent` stores the already-existing single `canDoEvent` result instead of invoking
  event validation twice. Gold/technology rows reuse already-resolved endpoints, selected tech and
  realized results; they add no RNG or event-tech selection.
- `CvEventInfo::hasUnitLocalEffect()` exactly matches the five unit-local EventInfo families consumed
  by `CvUnit::applyEvent`; this was independently revalidated again under practical 6435 / KI#810.
- `SASGameRecordRandomEventTargets` may call `CvUnit::canApplyEvent` for a surviving concrete unit.
  That predicate is const and contains no Python callback, RNG, write or event transaction; it checks
  promotion eligibility / attackability only. Accepted replies have just passed the same eligibility
  through `CvPlayer::canDoEvent`, so the recorder cannot alter the event by asking the question.
- Expiry logging copies `EventTriggeredData` before the durable occurrence is erased, and its optional
  reason output points only to static string literals. The Boolean branch order and Python expiry
  callback count remain unchanged.
- Free-unit logging stores the return from the existing `initUnit` call rather than issuing another
  creation call; clear-event and pillage rows count already-realized operations.

No stale/dangling access, extra synchronized randomness or changed random-event decision survived the
current-source audit.

PRACTICAL 6432 / 6efad6885ae140d115578b91291e2a2539fdef9d - CLEAN
-------------------------------------------------------------------------------
Financial-strike logging remains observational. `CvPlayer::doGold` reconstructs pre-change treasury
only after the authoritative ordinary gold change has already produced a negative balance, then logs
units before/after the existing strike-disband loop. No economic calculation or disband decision is
repeated.

The specialist-label cleanup is semantically correct despite Civ4's inherited names:
`CvCity::setFreeSpecialistCount` changes `m_iNumGreatPeople`, which is what `getNumGreatPeople()`
returns here. It is therefore a cache of actual typed free-specialist instances, not born Great
People. `totalFreeSpecialists()` remains the separate generic free-specialist allowance. The new
record labels distinguish those concepts correctly.

PRACTICAL 6433 / 7842e2a7e2... - NO RUNTIME C++ CHANGE
-------------------------------------------------------------------------------
This checkpoint is documentation/workflow context only for the repair-side audit; no new DLL runtime
surface required classification.

PRACTICAL 6434 / 04ed39adfe... - CLEAN
-------------------------------------------------------------------------------
The expanded realized random-event and nuclear-effect instrumentation was checked for lifetime,
ordering, RNG and disabled-logging regressions.

Random-event snapshots:
- City/player/unit before-state is captured only after the existing event-validity boundary and only
  when level-2 logging plus relevant EventInfo effects require it.
- A unit-local EventInfo may disband its concrete unit. The patch stores its ID/state before
  `CvUnit::applyEvent`, resolves that ID again through the owning player's unit table afterward, and
  uses a sentinel after-state when the object no longer exists; it never dereferences the killed
  pointer afterward.
- Empire-city snapshots store city IDs and resolve each city again after effects, skipping any city
  no longer present. Building-modifier rows inspect already-applied state and do not replay modifiers.
- Promotion-result counting wraps the existing `setHasPromotion` / `setFreePromotion` operations and
  only observes before/after state; no second promotion application is introduced.

Nuclear-effect snapshots:
- Exact affected-unit rows are emitted before the existing damage/kill call; cargo identity is
  captured while the transport/cargo objects still exist.
- The launching `pNukeUnit` cannot be invalidated by its own blast loop: `CvPlot::nukeExplosion`
  explicitly skips `pLoopUnit == pNukeUnit`, and `CvUnit::canNukeAt` additionally requires the launch
  plot to be farther from the target than `nukeRange()`.
- Successful suicide-unit deletion occurs only after the delayed/normal explosion transaction; the
  effect loggers therefore still have a live launcher while formatting every explosion row.
- City rows are emitted after the existing building/population mutations while the city remains live;
  Civ4 nukes reduce population to at least one rather than destroying the city object here.
- All nuke damage, fallout, building and population randomness remains the original single pass. The
  recorder consumes already-realized counters/vectors and adds no second scan that could alter RNG.

No independent current defect survived practicals 6430-6434.

PHYSICAL CHECKPOINT
-------------------
C031-WIP691 is authoritative.

Exact continuation:
WIP691 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> recent practical-6419..6436 repair-side audit now reconciled
(except purely later commits beyond that range, which must be classified separately rather than
assumed covered) -> continue with later 6437+ substantive C++ deltas first, checking whether they are
new repairs/instrumentation or ordinary development -> for each live delta trace object lifetime,
state ordering, cache invalidation, RNG count and whole-album dedup -> freeze the next physical WIP
at the next settled tranche/finding.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP691
================================================================================

================================================================================
C031-WIP692 - later SASGameRecord tail revalidation
Practicals 6437-6443 CLEAN; F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

PRACTICALS 6437-6443 - CLEAN
----------------------------
The later SASGameRecord geography/RNG/provenance/transaction/state-fingerprint/plot-ownership tail was
re-audited as runtime code rather than presumed harmless instrumentation.

- 6437 geography/native-preview dimensions reject nonpositive source/preview dimensions, clamp
  overview percentages, and force generated dimensions to at least one. No zero-divisor or gameplay
  mutation survived.
- 6438 authoritative RNG tracking observes only the two CvGame RNG objects by cached identity, updates
  recorder-local counters/fingerprints before the ordinary seed advance, adds no map/sync RNG draw,
  and is reset around authoritative new/load seed replacement. Recorder helpers do not recurse into
  synchronized RNG.
- 6439-6441 provenance/revision/transaction work remains diagnostic. Nested transaction RAII preserves
  an already-active root transaction instead of terminating it from an inner scope.
- 6442 semantic state fingerprints were checked across their getter surface. Strategy/victory hashes,
  diplomacy counters/timers, mission/group state and the ordinary team/player/city/unit fields are
  stored/cached getters. The calculation-looking `calculateInflationRate()` and plot-extra-yield
  lookup are read-only and consume no RNG. The checkpoint code additionally snapshots map/sync seeds
  and asserts they are unchanged across fingerprint construction. No hidden state/RNG transaction was
  found.
- 6443 plot ownership remains centralized. Current live CvPlot::setOwner entry paths are ordinary
  culture updates, city initialization/transfer, contested war/peace borders and Python/WorldBuilder
  wrappers; each finalized-game path supplies an appropriate root transaction/immediate cause scope.
  City acquisition deletes/recreates under CITY_ACQUISITION rather than retaining a dead old city
  pointer. The plot-state fingerprint guards the rollover case where an old plot still reports city
  identity but the city object no longer resolves before asking for second-owner state.

No independent current defect survived this tranche.

PHYSICAL CHECKPOINT
-------------------
C031-WIP692 is authoritative.

Exact continuation:
WIP692 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> practicals 6437-6443 CLEAN -> begin mechanical C/C++
printf/varargs format-contract sweep, first literal conversion/argument-count anomalies and malformed
conversions, then inspect surviving type/specifier mismatches and whole-album dedup -> freeze next WIP
at next settled finding/tranche.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP692
================================================================================

================================================================================
C031-WIP693 - literal printf/varargs format-contract sweep
F713 enriched; ACO debug-only omission parked; F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

FORMAT-CONTRACT SWEEP
---------------------
A comment-aware/balanced scan of literal CvString::format / CString::Format / sprintf-family calls was
used first to compare printf conversions with supplied varargs, then the surviving string-family
cases were checked manually for narrow/wide contracts.

F713 / KI#1034 ENRICHMENT - SAME ROOT, NO NEW NUMBER
----------------------------------------------------
The already-owned CvMap::init logging call is more malformed than the original `%6` proof alone:

  CvString::format("... world size=%s, climate=%s, sealevel=%s, num custom options=%6",
      ...getDescription(), ...getDescription(), ...getDescription(), getNumCustomMapOptions())

`CvString::format` is the narrow printf family, while CvInfoBase::getDescription() returns
`wchar const*`. On the MSVC narrow printf contract used by Civ4, those three wide strings require `%S`
(or explicit narrow conversion), not `%s`. The same call therefore contains three narrow/wide vararg
mismatches in addition to the already-confirmed incomplete `%6` conversion. This strengthens F713's
format-contract proof but is the same single live diagnostic call/root and does not consume F716.

ACOText.cpp DEBUG-ONLY ANOMALY - BELOW NUMBERED THRESHOLD
---------------------------------------------------------
A second real literal-count anomaly is inherited from K-Mod:

  szTempBuffer.Format(L"Odds that attacker withdraws at combatLimit (normal)",
      kAttacker.combatLimit());

The wide literal has no conversion although a combat-limit integer is supplied. The extra vararg is
harmless to the printf ABI; the visible consequence is only that ACO's optional/default-off Debug
line omits the numeric combatLimit that the source evidently meant to print. `ACO__Debug` remains a
real BUG option with default false. This is sensible cleanup (likely append `%d`) but remains below the
post-pass finding threshold unless stronger ordinary-user/runtime consequence appears.

NEGATIVE CONTROLS
-----------------
- After correcting the scanner not to treat ordinary C++ `<`/`>` comparisons as nested template
  delimiters, no other literal conversion-count mismatch survived the broad formatting-family pass.
- Narrow CvString::format/sprintf calls that visibly consume wide descriptions/names otherwise use
  the MSVC `%S` convention; wide CvWString/CString Format calls use wide `%s` operands consistently.
- No second active narrow/wide string-family vararg mismatch survived.

PHYSICAL CHECKPOINT
-------------------
C031-WIP693 is authoritative.

Exact continuation:
WIP693 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> F713 enriched with three additional `%s`/wide-description
contract mismatches in its already-owned CvMap::init call -> ACO debug-only missing conversion parked
below threshold -> continue mechanical format/varargs sweep into integer width/sign, pointer and
64-bit specifier contracts, then whole-album dedup -> freeze next WIP at next settled finding/tranche.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP693
================================================================================

================================================================================
C031-WIP694 - printf/varargs width/type-contract sweep closure
F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

FORMAT WIDTH/TYPE CLOSURE
-------------------------
The second formatting tranche checked surviving literal varargs for integer width/sign, pointer and
explicit 64-bit conversion contracts after WIP693's count/string-family pass.

- Active explicit 64-bit diagnostics use the MSVC/Civ4 `%I64d`, `%I64u` or `%I64X` forms. The DLL
  diagnostic file size is `__int64`; SASGameRecord RNG counters/fingerprints and semantic sequence /
  transaction identities are `unsigned __int64`; matching profiler counters use `%I64u` as well.
- No active `%p` call site survived that could consume a non-pointer or vice versa.
- No live call was found where a 64-bit argument is consumed by a 32-bit `%d/%u/%x` conversion or
  where a pointer-sized value changes the number of stack bytes consumed on Civ4's supported 32-bit
  target.
- Remaining signed/unsigned same-width formatting differences are diagnostic representation concerns,
  not demonstrated ABI/stack corruption; they stay below the numbered threshold.

Combined with WIP693, the literal printf-family sweep therefore has exactly one substantive already-
owned live call family: F713's CvMap::init diagnostic, now known to contain three narrow `%s` / wide
`getDescription()` mismatches plus the incomplete `%6` conversion. The inherited ACO Debug line with
an unused combatLimit vararg remains optional/debug-only cleanup.

PHYSICAL CHECKPOINT
-------------------
C031-WIP694 is authoritative.

Exact continuation:
WIP694 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> printf/varargs mechanical sweep CLOSED (F713 enriched; no new
number) -> change bug-finding lens to cross-file enum/sentinel and object-ID/domain consistency,
prioritizing direct truthiness/comparison/index crossings and producer/consumer domain mismatches ->
current-source proof + ancestry + whole-album dedup before any promotion -> freeze next WIP at next
settled finding/tranche.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP694
================================================================================

================================================================================
C031-WIP695 - enum/sentinel/object-domain mechanical pass tranche 1
False-positive controls reconciled; F716 / provisional KI#1037 UNUSED
================================================================================

STATUS
------
Primary queue: 169/169 COMPLETE.
Post-primary F708-F715 CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
Repository gameplay/XML/Python source untouched.

ENUM / OBJECT-DOMAIN PASS
-------------------------
A mechanical sweep targeted direct enum truthiness, raw PlayerTypes/TeamTypes crossings and object-ID
lookups whose integer representation can hide semantic-domain mistakes.

Important false-positive control rediscovered and deduplicated:
- `CvPlayer.cpp` starting-sight calls spell `GET_TEAM(getID()).revealSurroundingPlots(...)` in two
  places. `CvPlayer::getID()` is PlayerTypes, but this is NOT a raw team-table index in AdvCiv:
  CoreAI supplies a PlayerTypes overload of GET_TEAM that resolves the player's actual team. The
  completed album had already proved this exact control. Team games with differing player/team IDs
  therefore do not establish a wrong-team starting reveal from this spelling.

Other inspected survivors:
- `CyPlot::isAdjacentTeam(int /*TeamTypes*/ ePlayer, ...)` has a misleading local parameter name only;
  the wrapper contract/comment and cast are TeamTypes.
- Team-election resolution casts positive `PlayerVoteTypes` payloads to TeamTypes intentionally; team
  votes encode the selected team in that overloaded vote value domain.
- Direct truthiness candidates such as UnitInfo `getUnitAIType(...)`, target/defender-unit-class tests
  and related Info getters are Boolean-array predicates despite enum-looking names; no NO_* sentinel
  inversion survived.

The first broad raw-cast census is large because Civ4's Python/XML/loop interfaces intentionally carry
enums through ints. No new unambiguous PlayerTypes/TeamTypes/object-table mismatch has yet survived
manual contract checking. Continue only with narrowed candidates whose callee parameter/index domain
is explicit, and dedup against the album's extensive earlier enum-domain controls before promotion.

PHYSICAL CHECKPOINT
-------------------
C031-WIP695 is authoritative.

Exact continuation:
WIP695 physical -> primary 169/169 COMPLETE -> post-primary F708-F715 confirmed ->
F716 / provisional KI#1037 UNUSED -> printf/varargs pass closed -> enum/object-domain tranche 1 closed
with starting-sight GET_TEAM(PlayerTypes) rediscovered as an existing false-positive control -> narrow
future sweep to explicit raw indexes/casts and producer-consumer ID domains; prioritize substantive
current-live gameplay/state consequences over wrapper naming or debug-only issues -> ancestry + full
album dedup before numbering -> freeze next WIP at next settled boundary.

No gameplay/XML/Python source modified.
================================================================================
END C031-WIP695
================================================================================


===============================================================================
C031-WIP696 — POST-PRIMARY REPAIR-SIDE AUDIT CONTINUATION
PRACTICALS 6411–6418 REVALIDATED — F716 / KI#1037 REMAINS UNUSED
===============================================================================

STATUS
------
Primary queue remains 169/169 COMPLETE.
Post-primary F708/KI#1029 through F715/KI#1036 remain CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
No gameplay/XML/Python source files modified.

PURPOSE OF THIS TRANCHE
-----------------------
Continue the dedicated "audit the audit fixes" pass backwards from the already-audited
6419–6443 repair series. Rather than assuming a fix is safe because its motivating root
was valid, recheck transaction ordering, sibling callers, cache-input coverage and
repair-side regressions against the current reconciled source.

PRACTICAL 6418 — KI#784 / KI#785 / KI#786 — CLEAN
--------------------------------------------------
The demographics-latch repair remains consistent with its actual state inputs. Population
changes dirty score state and the normal game update reaches CvGame::updateScore; the
new observer/target espionage refreshes use the correct direction for counterespionage.
The force-civic repair reads the target player's revolution minimum, and sabotage
production now tests the target city owner rather than the spy owner. No sibling-domain
or ordering regression survived.

PRACTICAL 6417 — KI#783 — CLEAN
--------------------------------
The city-trade culture repair precomputes conversion values while the old city and its
working-city deductions still exist, then applies the saved values after the destructive
transfer. Both CityPlotIter passes enumerate the same fixed city-radius geometry. The
shared helper's getID() contribution is coherent at both live callers because each call
is made on the acquiring/evaluating player and passes that same player as eNewOwner.

PRACTICAL 6416 — KI#782 — CLEAN UNDER CURRENT SHIPPED CALLBACKS
----------------------------------------------------------------
Removing the final building-granted CivicOption can invoke verifyCivics() during city
teardown. The apparent half-dismantled-city concern does not produce a persistent
current-state defect: processCivics changes player aggregates and symmetric city/cache
state, and the doomed city's own contributions are subsequently removed by the same
CvCity::kill transaction. The shipped Python canDoCivic path remains the default false
hook, so no current callback makes legality depend on the transient half-torn-down city.
Keep arbitrary third-party callback behavior as extension robustness, not a finding.

PRACTICAL 6415 — KI#779 — CLEAN
--------------------------------
The hidden-personality strategy fix substitutes getFavoriteCivic() only for the public
favorite-civic input while leaving personality-derived flavors personality-derived.
No mixed identity or cache issue found.

PRACTICAL 6414 — KI#778 / KI#781 — CLEAN
-----------------------------------------
The final Permanent-Alliance recount only visits players whose own team becomes the
surviving merged team. This initially looked too narrow because addTeam also rewrites
vassal/master relations and absorbed-team plots. Full ordering resolves the concern:
outsider vassals/masters are migrated before plot-team rewrite, and setVassal already
refreshes the relevant outsider outside-unit/military-happiness state as each relation
changes. Their friendly/vassal-border predicate therefore remains coherent. The final
recount is needed precisely for the merged constituent players whose own team identity
and plot-team comparison changed.

PRACTICAL 6413 — KI#771 — CLEAN FOR CURRENT SAS DATA
-----------------------------------------------------
The centralized founding-unit surcharge rebuild now covers civilization/trait reset,
controller changes and Rise & Fall's direct player-handicap mutation. A broader input
was re-opened: getNewCityProductionValue() calls CvGame::AIHandicapAdjustment(), whose
formula depends on game turn. If iAIHandicapIncrementTurns were nonzero, the cached
founding-unit surcharge would acquire a per-turn input without a per-turn rebuild.
However every current shipped SAS HandicapInfo has iAIHandicapIncrementTurns=0, making
AIHandicapAdjustment() identically zero. Game speed/start era are setup constants and
player-level free-start-building production modifiers are trait-derived and already
covered. Preserve the clock dependency as extension-safety debt if the disabled handicap
increment feature is ever restored; it is not a current F716 root.

PRACTICAL 6412 — KI#769 — CLEAN
--------------------------------
The new iExtraMilitaryUnits parameter does not strand an old military projection caller.
A complete current caller census finds only two nonzero prospective-delta uses:

* selected-unit/cargo deletion hover — passes real total and military-support deltas;
* Settler founding-cost help — passes total -1 and military 0.

All other detailed/convenience overload callers request current-state values with zero
prospective deltas. The later hypothetical-extra-deletions loop intentionally preserves
AdvCiv's best-case assumption that each unknown further deletion can remove military
support as documented by the repair.

PRACTICAL 6411 — KI#767 / KI#774 — CLEAN
-----------------------------------------
Commerce residual repair is dimensionally correct for both signs. After awarding +1
whole commerce, subtracting 100 hundredths from the selected positive residual prevents
stale reselection; for negative total error, iSign=-1 and adding 100 to the selected
negative residual is the symmetric operation. The missionary training-area guard also
introduces no new null-pArea contract: the pre-existing completed-unit half already
unconditionally dereferences the requested CvArea, while live area-specific callers
supply it.

CURRENT STONE AFTER C031-WIP696
--------------------------------
WIP696 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F715 CONFIRMED.
F716 / provisional KI#1037 UNUSED.
No repository gameplay source modified.

NEXT
----
Continue the repair-side pass backwards from practical 6410. Prioritize repair deltas
with destructive transactions, ownership/team identity, cache invalidation or widened
caller contracts. Re-prove each candidate against current source and whole-album dedup;
consume F716 only for a distinct current-live root.


===============================================================================
C031-WIP697 — POST-PRIMARY REPAIR-SIDE AUDIT CONTINUATION
PRACTICALS 6407–6410 REVALIDATED — F716 / KI#1037 REMAINS UNUSED
===============================================================================

STATUS
------
Primary queue remains 169/169 COMPLETE.
Post-primary F708/KI#1029 through F715/KI#1036 remain CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
No gameplay/XML/Python source files modified.

PRACTICAL 6410 — KI#652 / KI#675 — CLEAN
-----------------------------------------
KI#675's liberation-first city-request ordering is genuinely lexicographic when the
whole loop is read. The new `(bLiberate && !bBestLiberate)` replacement arm lets the
first valid liberation city displace a non-liberation best regardless of the secondary
value, while the retained early `if (bBestLiberate && !bLiberate) continue` prevents a
later non-liberation city from displacing it. Candidates within the same liberation
class still compare by the intended numeric value. The initially suspicious second arm
therefore does not reopen F352.

KI#652's prospective World-Wonder test correctly varies active/obsolete status through
the prospective recipient team's obsolescence state while retaining the city's actual
building inventory. `getNumActiveWorldWonders(..., eOwner)` has an existing hypothetical-
owner contract and does not introduce a second ownership-domain substitution.

PRACTICAL 6409 — KI#947 / KI#948 / KI#951 — CLEAN
-------------------------------------------------
The expanded Dijkstra destination table preserves every original destination ID and
appends only previously absent legal city plots. Completed-team removal from the round-
robin vector keeps the current index on its successor (or wraps after erasing the final
slot), so unequal team sizes no longer overfill a completed team without skipping the
next one. The fallback team-value calculation now receives the producer's authoritative
remaining unassigned-site set; it does not recycle already assigned rival placeholders.
The older same-area weighting repetition remains the previously audited opaque AdvC
heuristic rather than a repair-side regression.

PRACTICAL 6408 — KI#492 / KI#946 / KI#949 / KI#950 — CLEAN
----------------------------------------------------------
Skipping full `AIFoundValue::evaluate()` for the standalone workable-plot evaluation is
safe: all state consumed by `evaluateWorkablePlot()` and its called helpers is initialized
by the constructor before the optional full evaluation, while overlap vectors populated
by `evaluate()` are not read on that path. The Dijkstra repair discards stale queue pops
before assigning the destination distance; there is no destination-count early exit that
could strand an unsettled target. The scenario normalization target is deleted after its
last scenario consumer, and the normalization switch breaks restore mutually exclusive
modes rather than changing their intended ordering.

PRACTICAL 6407 — KI#661 — CLEAN
--------------------------------
Ending already-rejected AI-human deals before the GPT-cap calculation does not create a
new dangling-pointer path. During the first deal scan, a deal is stored in
`aapDealsPerPlayer[eOther]` only when `AI_checkCancel` returns NO_CANCEL; cancellation
candidates take a different branch and therefore cannot also be in that survivor vector.
The first cancellation helper runs only after the global deal iterator has ended, copies
its popup/resource-cancellation payload before `CvDeal::kill()` removes the object, and
then clears the pointer vector.

The later GPT-cap pass converts each surviving GPT deal to one deal ID (breaking after
its first GPT item) before any cap-driven cancellation. The inner `eOther` declaration is
scoped to that per-player loop; the final helper call uses the original AI_doDeals target,
which is also the only index populated by the upfront `isBetween(getID(), eOther)` filter.
Team-agreement teardown can remove sibling proxy deals carrying that same team item, but
the newly early-killed cancellation set does not alias the same-target NO_CANCEL survivor
set whose GPT IDs are processed afterward. The repair therefore measures post-cancellation
economic state without introducing a new stale-pointer/use-after-free transaction.

CURRENT STONE AFTER C031-WIP697
--------------------------------
WIP697 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F715 CONFIRMED.
F716 / provisional KI#1037 UNUSED.
No repository gameplay source modified.

NEXT
----
Continue the repair-side pass backwards through practical 6406 and 6405, with special
attention to whole-deal validation/UI symmetry, team-state transaction ordering, Worker
cache authoritative boundaries and culture-governor arithmetic. Whole-album dedup remains
mandatory before promoting F716.


===============================================================================
C031-WIP698 — POST-PRIMARY REPAIR-SIDE AUDIT CONTINUATION
PRACTICALS 6405–6406 REVALIDATED — F716 / KI#1037 REMAINS UNUSED
===============================================================================

STATUS
------
Primary queue remains 169/169 COMPLETE.
Post-primary F708/KI#1029 through F715/KI#1036 remain CONFIRMED.
F716 / provisional KI#1037 remains UNUSED.
No gameplay/XML/Python source files modified.

PRACTICAL 6406 — KI#611 / KI#621 — CLEAN
-----------------------------------------
The backend bundle guard is transactionally early enough. `CvDeal::addTradeItems` tests
both proposed lists for vassal/surrender state combined with Permanent Alliance or
Defensive Pact before item-by-item validation or any `startTrade` mutation. Rejection
therefore leaves the newly allocated deal empty; `CvGame::implementAndReturnDeal` sees
both saved lists at length zero, kills that empty object and returns NULL. There is no
partial alliance/pact/vassal state to unwind.

The negotiation-side repair mirrors the same whole-offer contract. `updateTradeList`
derives vassal, PA and DP presence from both offer directions, hides vassal/surrender
inventory once PA/DP is already offered, and hides PA/DP once either side has offered
vassalage. Backend validation remains authoritative for forced/non-UI callers. No
one-direction or execute-before-reject sibling survived.

PRACTICAL 6405 — KI#861 / KI#863 / KI#864 — CLEAN
-------------------------------------------------
KI#861's Worker-cache rebuild occurs at the correct authoritative boundary for the
immediate next-production decision. `popOrder` removes the completed Worker order first,
then creates the actual unit, then calls `AI_updateWorkersHaveAndNeeded()` before
`AI_chooseProduction`. `PUF_isMissionPlotWorkingCity` explicitly falls back to the
unit's current plot when it has no mission, so the freshly created Worker on the city
plot is counted. The completed order can no longer also contribute the old <=3-turn
prospective Worker count; a distinct queued near-complete Worker remains intentionally
prospective. No double-count or missing-new-Worker repair regression remains.

KI#863 computes the culture ETA once and accepts only a positive 1..5-turn result.
`getCultureTurnsLeft()` returns -1 both for nonpositive culture rate and for no remaining
threshold, so excluding nonpositive values is the intended sentinel contract rather than
losing a real zero-turn expansion case.

KI#864 restores upward rounding at the historical intermediate base-requirement stage,
before occupation/war modifiers and the retained final nearest-integer conversion. The
repair therefore restores the original semantic operation rather than simply increasing
the final result by one.

CURRENT STONE AFTER C031-WIP698
--------------------------------
WIP698 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F715 CONFIRMED.
F716 / provisional KI#1037 UNUSED.
No repository gameplay source modified.

NEXT
----
Practicals 6404 and 6403 are documentation/history-maintenance only. Move to the recent
substantive gameplay/AI commits 6402 and 6401, which are present in current source but do
not yet have explicit coverage in this post-primary album pass. Audit them as fresh
cross-file changes rather than assuming successful autoplay implies semantic safety.


===============================================================================
C031-WIP699 — POST-PRIMARY CURRENT-TAIL AUDIT — F716 / KI#1037 CONFIRMED
PRACTICAL 6402 PHASE-0 HEALTH/HAPPINESS SAFETY OMITS OTHER AFFECTED CITIES
===============================================================================

STATUS
------
Primary queue remains 169/169 COMPLETE.
Post-primary F708/KI#1029 through F716/KI#1037 are CONFIRMED.
F717 / provisional KI#1038 is UNUSED.
No gameplay/XML/Python source files modified.

-------------------------------------------------------------------------------
F716 / KI#1037 — CONFIRMED
SAS PRODUCTIVE-FEATURE PHASE 0 CHECKS CHOP HEALTH/HAPPINESS ONLY FOR THE
PRODUCTION CITY, SO A SHARED-RADIUS FOREST CAN MAKE ANOTHER CITY UNHEALTHY
-------------------------------------------------------------------------------

STATUS / SCOPE
--------------
Confirmed current-default AdvCiv-SAS AI defect introduced by practical 6402
(`492ad93e03...`, productive-feature Phase 0 / tech-value work).

This is not a claim that every Forest chop is bad. The narrow contradiction is that the
new Phase-0 eligibility gate explicitly tries to prevent a productive-feature removal
that makes or worsens a current health/happiness deficit, but evaluates that transition
for only the chop-production city even though feature health/happiness is a multi-city
radius effect.

CURRENT ROOT
------------
`SAS_getWorkerPhase0ProductiveFeatureInfo(kUnit, kCity, kPlot, kInfo)` computes:

    kCity.calculateHealthHappyChange(kPlot, NO_IMPROVEMENT, NO_IMPROVEMENT,
        true, iHappyChange, iHealthChange, iHealthPercentChange);

and then rejects only when that one `kCity` would cross/worsen a rounded deficit.

The same helper separately requires `CvPlot::getFeatureProduction(...)` to resolve the
production recipient back to this exact `kCity`. That is correct for deciding which city
receives chop hammers, but it is not the ownership domain of feature health/happiness.
A feature in overlapping city radii contributes to every qualifying city radius.

The engine confirms the wider effect at mutation time. `CvPlot::setFeatureType` iterates
all `NearbyCityIter` cities around the plot and calls
`updateSurroundingHealthHappiness()` after changing the feature. `CvCity::calculateSurroundingHealth`
likewise iterates the full city radius and sums FeatureInfo health percentages regardless
of which overlapping city currently works the plot.

CURRENT-DATA ACTIVATION / DETERMINISTIC FIXTURE
-----------------------------------------------
The feature is enabled by default:

    SAS_WORKER_AI_PHASE0_PRODUCTIVE_FEATURE_CHOP_ENABLE = 1
    SAS_WORKER_AI_PHASE0_PRODUCTIVE_FEATURE_CHOP_CORE_FOOD_UNIT_MIN_ELIGIBLE_PLOTS = 3

Current `FEATURE_FOREST` has `iHealthPercent = 50`, and current
`BUILD_REMOVE_FOREST` is a legal pure removal once Masonry is available, with positive
feature production.

Construct two same-owner cities A and B with one overlapping Forest assigned/worked by A.
Give A three Phase-0-eligible Forests total and B two Forests total, with the shared plot
counting for both city radii. Put the Worker on the shared Forest and have A produce a
food-production Worker or Settler.

Before removal:

    A Forest health: 3 * 50% = 150% -> +1 rounded good health
    B Forest health: 2 * 50% = 100% -> +1 rounded good health

After removing the shared Forest:

    A: 100% -> +1  (rounded change 0)
    B:  50% ->  0  (rounded change -1)

Because the shared plot's working city is A, `getFeatureProduction()` returns A. Phase 0
therefore tests A only, sees `iHealthChange == 0`, and accepts the Forest as expendable.
With three uncommitted eligible removals, the shipped CORE threshold is met. If the Worker
is already on the shared tile, equal-production alternatives lose the shorter-path tie,
so the hard override can deterministically choose that Forest.

When the build completes, the real `setFeatureType` transaction refreshes both cities and
B actually loses its rounded +1 health. If B began exactly health-balanced, it becomes
unhealthy despite the Phase-0 safety rule that is intended to exclude deficit-worsening
removals.

WHY THIS IS NOT JUST A TUNING PREFERENCE
----------------------------------------
AdvCiv's older `CvCityAI::AI_healthHappyImprovementValue` explicitly documents the
underlying semantic requirement — "Other affected team cities matter just as much" — and
recurses across city radii for an improvement/feature transition. Current SAS largely
bypasses that old land-improvement evaluator, but practical 6402 introduced a new
*authoritative hard Worker override* with its own health/happiness safety test. The new
gate therefore needs to respect the same multi-city effect if it is to uphold its stated
safety condition.

The KI#33.2 documentation is equally explicit that a feature-removal build which would
"make or worsen a current health/happiness deficit" is not ordinary expendable reserve.
The current implementation satisfies that only for the production city.

ANCESTRY
--------
The Phase-0 helper, its local health/happiness guard, the CORE/NORMAL hard overrides and
the shipped enable/threshold configuration were introduced together by AdvCiv-SAS
practical 6402 (`492ad93e03...`). Base AdvCiv 1.14 has no SAS Phase-0 producer. This is
therefore a SAS current-tail regression/omission, not an inherited Firaxis/K-Mod root.

DEDUP
-----
Whole-album reconciliation found no prior owner for this new Phase-0 multi-city safety
boundary.

* WIP288's `AI_healthHappyImprovementValue` note is a different inherited AdvC receiver
  bug: it attempts to include neighbor cities but recursively calls the helper on `this`.
  That producer was explicitly classified as CURRENT-SAS masked for authoritative land
  Worker choices.
* F509 is the AdvC surrounding-health/happiness *cache invalidation* root when diplomatic
  access/ownership changes. F716 starts from a valid cache and chooses a real feature
  mutation while evaluating only one of the affected cities.
* Existing KI#33.2 is the parent enhancement/policy record, not a numbered proof of this
  safety omission.

REPAIR BOUNDARY
---------------
Keep Phase-0 hammer recipient selection city-specific, but validate the health/happiness
transition against every nearby same-team/currently affected city before classifying the
plot as safely expendable. A narrow implementation can iterate the plot's nearby city
radii and call each city's `calculateHealthHappyChange` with the same pure-removal
transition, rejecting the candidate if any such city would make/worsen a current rounded
health or happiness deficit. Preserve the existing local pressure-relief priority; if
multi-city relief is to influence priority too, treat that as a separate policy choice
rather than silently coupling it to the safety repair.

CURRENT STONE AFTER C031-WIP699
--------------------------------
WIP699 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F716 CONFIRMED.
F717 / provisional KI#1038 UNUSED.
No repository gameplay source modified.

NEXT
----
Continue practical 6402 rather than treating one finding as commit closure. Recheck the
remaining Phase-0 reservation/ordering paths and productive-feature technology valuation,
then audit practical 6401 first-Settler/escort coordination. Whole-album dedup remains a
hard gate before F717.

======================================================================
POST-PRIMARY TAIL / REPAIR-SIDE AUDIT — C031-WIP700
Practical 6402 remainder + practical 6401
======================================================================

STATUS
------
F717 / provisional KI#1038 remains UNUSED.
No repository gameplay/XML/Python source modified.

PRACTICAL 6402 — REMAINING SIBLINGS CLOSED
-------------------------------------------
The Phase-0 reservation cursor is real but does not clear the finding threshold. The
productive-feature counter treats any other group's `MISSIONAI_BUILD` target on the plot
as reserved. Current route/connect code (`AI_connectPlot`, `AI_routeCity`,
`AI_routeTerritory`) also uses `MISSIONAI_BUILD`; `AI_routeTerritory(false)` can therefore
reserve an owned unimproved Forest for a Road/Railroad without removing the feature.
However, the practical-6402 policy explicitly defines existing Worker targets as reducing
the *uncommitted* feature total. This is the same broad one-Worker-per-target reservation
channel used elsewhere, and its consequence is finite scheduling delay while that Worker
owns the tile, not a contradiction in the current safety/capability contract. Preserve as
a policy-quality note; do not consume F717.

The productive-feature technology-value sibling also closes under current XML. Every
productive removal route for a feature uses the same feature-removal technology:

    Forest  -> TECH_MASONRY       -> 30 production
    Jungle  -> TECH_METAL_CASTING -> 20 production
    Fallout -> TECH_ENVIRONMENTALISM -> 0 production

The representative pure removal Builds have no separate Build-level technology
prerequisite. Therefore a candidate tech is not credited for a productive removal
capability already available through another current Build/tech path. Overlapping city
radii are not double-credited because `getFeatureProduction()` must resolve the actual
production recipient to the exact city being valued.

Practical 6402 therefore closes with F716 as its one confirmed new root; no F717 sibling
survives this pass.

PRACTICAL 6401 — FIRST-SETTLER / ESCORT COORDINATION CLOSED CLEAN
-----------------------------------------------------------------
The new first-trained-Settler context intentionally counts completed `UNITAI_SETTLE`
units rather than `AI_totalUnitAIs`, which includes units in production. This keeps the
soft growth/escort context stable while the first Settler is being trained instead of
turning the rule off after one production turn. Normal AI production selection is not
rerun every turn while ordinary production remains valid, so this does not by itself
create Settler/defender churn.

The quick-defender precheck and actual queued unit remain consistent. The preparation
branch first resolves:

    AI_bestUnitAI(UNITAI_CITY_DEFENSE)

and measures that concrete unit's production turns. The subsequent
`AI_chooseUnit(UNITAI_CITY_DEFENSE)` resolves the same role through the same deterministic
best-unit path and forwards the concrete unit to the `(UnitTypes, UnitAITypes)` overload.
The only relevant early-land concrete replacement occurs later inside the strict-defender
anti-excess block. KI#179.3 recomputes the exact requested defender's build turns there
and treats a qualifying first-Settler preparation request as a real minimum-defense need,
so the offensive fallback/re-role path is skipped. The earlier hard rejection branches
are siege-specific and do not reject an ordinary CITY_DEFENSE choice. The max-turn gate
is therefore not proved on one unit and spent on a slower replacement.

The production-side and movement-side safety thresholds line up exactly with shipped
defaults:

    SAS_AI_SETTLER_ATTACH_CITY_ESCORT_MIN_HEALTHY_CITY_DEFENDERS_LEFT = 2
    SAS_AI_CHOOSE_PRODUCTION_FIRST_SETTLER_ESCORT_PREPARATION_MAX_TURNS_NORMAL_GAMESPEED = 5

Preparation triggers while healthy defenders <=2. One newly trained undamaged defender
raises the healthy pool by one; movement-side attachment requires healthy defenders >2,
then can detach one escort and leave exactly two. The escort pool may prefer a defender
with damage 26–50, but the safety count itself remains the stricter <=25-health pool, so
choosing a wounded candidate can only leave at least as many healthy defenders behind.
Current UnitInfos further show that every unit permitted for `UNITAI_CITY_DEFENSE` is a
land unit with at least one move, so the prepared concrete defender cannot satisfy the
production gate while being an immobile/non-land escort dead end.

The first-expansion found-value/growth changes also preserve their stated boundaries:
the optional second-city floor defaults to 0 and can only raise, not lower, normal AdvC
minimum found value; the full early filler-city floor begins from the configured later
city-count threshold; and the preferred-population wait compares actual growth turns to
a game-speed GrowthPercent-scaled Normal-speed window. Slow/stagnant first capitals are
therefore intentionally allowed to expand sooner rather than being caught by the old
binary "any positive growth below pop 5" gate.

The KI#181.2 documentation still records a separate historical observation about a
partially built Settler sometimes remaining parked in the production queue after a
military interruption. That is explicitly documented as a possible independent future
follow-up, not a defect introduced or silently solved by practical 6401; this audit does
not relabel it as F717 without a current deterministic reproduction/root.

CURRENT STONE AFTER C031-WIP700
--------------------------------
WIP700 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F716 CONFIRMED.
F717 / provisional KI#1038 UNUSED.
No repository gameplay/XML/Python source modified.

NEXT
----
Continue backward through the current-tail commit-diff mirror from practical 6400 and
older substantive commits. Separate documentation/generated-only commits immediately;
for live C++ changes, audit the exact introduced state/ordering/cache/AI-policy boundary
against current source/XML and whole-album dedup. Freeze the next physical WIP at the
next settled boundary whether or not F717 is consumed.

======================================================================
POST-PRIMARY CURRENT-TAIL AUDIT — C031-WIP701
Practicals 6400–6396
======================================================================

STATUS
------
F717 / provisional KI#1038 remains UNUSED.
No repository gameplay/XML/Python source modified.

PRACTICAL 6400
--------------
Generated/example SASGameRecord log refresh only; no gameplay source. Skipped cleanly.

PRACTICAL 6399 — SASGAMERECORD OVERHEAD OPTIMIZATION CLOSED CLEAN
-----------------------------------------------------------------
Reviewed every live C++/Python gameplay-adjacent hunk. The changes cache immutable
recorder-level tests or already-required values and leave gameplay calculations/mutations
outside log-only gates. Notable controls:

* `CvPlot::setBonusType` caches the old bonus and uses explicit old/new values for the
  same area/map/yield updates; this is algebraically equivalent to rereading the field.
* `CvGame::toggleDebugMode` changes `x = x ? false : true` to `x = !old`, equivalent.
* vote-result recorder booleans are cached only after pending votes have short-circuited;
  vote/default/defiance/outcome state remains unconditional.
* civic processing, nuke effects, blockade gold, production invalidation/upgrade and
  air/bombard gameplay calculations remain outside recorder gates.
* Golden-Age old state is derived from the already-read old turn count; `isGoldenAge()`
  is exactly `getGoldenAgeTurns() > 0`.
* the Python rejected-offer path merely moves responsibility for the level-2 gate to the
  caller before expensive list construction; payload validation remains in the bridge.

No recorder optimization accidentally gates gameplay or changes RNG/order.

PRACTICAL 6398 — CITY POPULATION/STATE HISTORY CLOSED CLEAN
-----------------------------------------------------------
`CvCity::doGrowth` captures food/granary/population before-state only at recorder level
2+, then performs the inherited growth/starvation/Avoid-Growth mutations in the original
order. Logging occurs after the realized population mutation; the successful-growth
Python event remains afterward. `cultureExpansion` adds one observation before the
existing Python report and performs no additional gameplay calculation/RNG. The new
SASGameRecord aggregation functions mutate recorder-local flow counters only.

PRACTICAL 6397 — TRADE MARKET SNAPSHOT CLOSED CLEAN
---------------------------------------------------
The optional level-3 Foreign-Advisor market reconstruction is observational. The
potentially expensive/suspicious valuation functions used by the snapshot were traced:
`AI_maxGoldTrade`, `AI_maxGoldPerTurnTrade`, `AI_goldForBonus`/`AI_bonusTradeVal` and
`AI_techTradeVal` do not consume synchronized RNG or submit/mutate deals in these query
paths. The helper loops reconstruct GPT quotes from deterministic public valuation
primitives and the snapshot uses `canTradeItem`/denial/research checks without executing
trade items. Independent XML gates keep the scan absent at lower log levels/default-off
subfeatures.

PRACTICAL 6396 — SPECIAL-UNIT ATTEMPT PROVENANCE CLOSED CLEAN
-------------------------------------------------------------
The Missionary refactor preserves K-Mod semantics exactly: one direct spread roll is made;
on failure the same ranked religion is selected; success occurs iff the direct roll
succeeds or the failed-ranked religion differs from the incoming one; then the original
add-new-religion-before-remove-displaced-religion order is applied. Failure messaging and
Python attempt reporting retain the realized Boolean.

Corporation spread caches the one already-required spread cost, performs the same one
success roll, pays gold at the same point, and only logs the stored outcome.

Espionage captures destructible target identity before the pre-mission interception can
kill the Spy/target and extends `testSpyIntercepted` with provenance arguments. Those
arguments are forwarded only inside its existing recorder block; interception chance,
RNG, diplomacy memory and kill behavior do not consume them. Ordinary travel callers use
header defaults and remain unchanged.

CURRENT STONE AFTER C031-WIP701
--------------------------------
WIP701 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F716 CONFIRMED.
F717 / provisional KI#1038 UNUSED.
No repository gameplay/XML/Python source modified.

NEXT
----
Practical 6395 is documentation/audit reconciliation only. Continue to the next
substantive current-tail commit (6394 and older), using exact local commit diffs and
current source/XML. Prioritize behavior changes over recorder-only churn; whole-album
dedup remains mandatory before promoting F717.

======================================================================
POST-PRIMARY CURRENT-TAIL AUDIT — C031-WIP702
Practicals 6395–6390
======================================================================

STATUS
------
F717 / provisional KI#1038 remains UNUSED.
No repository gameplay/XML/Python source modified.

PRACTICAL 6395
--------------
Documentation/audit reconciliation only; no live gameplay source. Skipped cleanly.

PRACTICAL 6394 — CONSEQUENTIAL ACTION HISTORY CLOSED CLEAN
----------------------------------------------------------
Reviewed the city-hurry, unit-gift, religion/corporation membership, circumnavigation,
pillage and naval-blockade hooks on both the gameplay-call and recorder-function sides.
The added work is observational and its object lifetimes remain valid:

* hurry captures cheap scalar before-state and logs while the city still exists, after the
  inherited production/gold/population/anger mutations;
* a gifted unit has already been recreated for the recipient and remains live at the
  reporter boundary; the old source unit is only delayed-death state after convert;
* religion/corporation reporter hooks observe the already-realized city membership before
  the existing Python report and add no gameplay mutation;
* pillage snapshots old plot state by value and logs while the acting unit/plot remain live;
* circumnavigation reuses the same bonus predicate and observes before/after sea moves;
* blockade recorder context persists only owner/unit ID plus copied strings/scalars, never
  raw unit/city pointers. Movement and final kill call setBlockading(false) while the unit
  is still valid. Loaded mid-blockades deliberately use END_FALLBACK with unknown start.
  blockadeRange uses a local pathfinder and does not consume synchronized RNG/game state.

No logger hook changes authoritative action order or retains a dangling object.

PRACTICAL 6393 — DIPLOMATIC VOTE HISTORY CLOSED CLEAN
-----------------------------------------------------
The proposal hook runs after VoteTriggeredData is fully initialized and before ballots.
Result logging happens before setVoteOutcome clears per-player vote storage, so the
weighted ballot/defaulted abstentions/defiers/endorsers are still authoritative. The
cancellation path records before the trigger is discarded.

Recorder helpers are observation-only: countVote/findHighestVoteTeam are deterministic
scans with no RNG, and target/source city pointers are resolved only for immediate use.
Player masks cast 1 to qword before shifting; the current 48-civ DLL therefore remains
within the 64-bit mask. No partial vote execution or outcome ordering is changed.

PRACTICAL 6392 — MAP DENSITY / RIVAL CONTEXT CLOSED CLEAN
---------------------------------------------------------
Recorder-only geography/expansion additions use stored/revealed/visible map state and
wrap-aware plotDistance. They do not run pathfinding, synchronized randomness, diplomacy
mutators or AI decision routines. Foreign-city information is reveal/deduction gated in
the existing factual snapshot contract. No gameplay side effect found.

PRACTICAL 6391 — PRODUCTION LIFECYCLE HISTORY CLOSED CLEAN
----------------------------------------------------------
Maxed-class/project production loss captures stored amount, active-head status and queue
presence before zeroing the production bank, then logs afterward; no pointer into an
erased queue node is retained. Production decay algebra is unchanged after naming the new
value. Upgrade logging observes the inherited assignment/overwrite behavior without
changing it.

SASGameRecordAIProductionChoiceScope is safe across empty queues: CvCity::getOrderData(0)
returns an explicit NO_ORDER sentinel when the queue is empty. The scope stores only the
city pointer for the duration of AI_chooseProduction; that routine chooses/rewrites
orders but does not destroy its own city. captureSASGameRecordProductionTarget uses
query-style production getters and no RNG. Disabled levels leave m_pCity NULL and do no
cold recorder work.

PRACTICAL 6390 — MILITARY QUALITY / COMBAT HISTORY CLOSED CLEAN
---------------------------------------------------------------
Combat pending context contains only copied owner IDs, unit IDs, battle coordinates,
odds and a Boolean. resolveCombat creates it only at actual resolution. Nonlethal combat
pops it before returning; lethal combat reaches combatResult while both winner and loser
objects are still live, and loser kill/capture occurs later. Thus delayed visible battle
animation does not create a dangling-unit dependency.

At level 2, only true binary civilization-vs-civilization luck samples allocate pending
context; withdrawal/combat-limit/Barbarian cases do not. At level 3 every exact battle can
carry identity context and the corresponding nonlethal/lethal path consumes it.
calculateCombatOdds is deterministic diagnostic work here; Barbarian free-win fights are
excluded from the luck sample, so the separately owned F637 forwarding defect does not
contaminate this recorder statistic.

Experience/promotion hooks observe already-computed actual deltas/applications and mutate
recorder-local counters only. No additional synchronized RNG, combat mutation or unit
lifetime extension found.

CURRENT STONE AFTER C031-WIP702
--------------------------------
WIP702 physical.
Primary 169/169 COMPLETE.
Post-primary F708–F716 CONFIRMED.
F717 / provisional KI#1038 UNUSED.
No repository gameplay/XML/Python source modified.

NEXT
----
Continue backward through practical 6389 and older substantive current-tail diffs using
the local commit-diff mirror. Skip documentation/generated-only commits explicitly;
for live changes, audit object lifetime, mutation/RNG ordering and producer-consumer
contracts against current source and whole-album dedup. Freeze the next WIP at the next
settled boundary whether or not F717 is consumed.

================================================================================
POST-PRIMARY CROSS-FILE / REPAIR-SIDE PASS - C031-WIP703
Continuation from WIP702; no gameplay/XML/Python source modified.
Primary queue remains 169/169 COMPLETE. F717 / provisional KI#1038 remains UNUSED.

Practical 6389 - offensive production floor secured landmass/naval profile - DEDUP / no new finding
- Revalidated the current helper family and practical-6389 birth diff.
- The strongest surviving defect is not new: per-water naval production is divided by empire-wide productive-city production even though the configured target is an empire production share. The completed album already reopened/broadened this exact multi-water denominator root as KI#197.11 (WIP281), including the disconnected-seas 20% -> 30/40% witness. Do not renumber as F717.
- Era arrays match the seven shipped eras; independent-rival master-team dedup remains type-correct; current bounds make stock multiplications safe. Detailed logging can compute a secured-profile diagnostic outside the base context, but actual production eligibility separately requires the base-context predicate, so logging cannot force production.

Practical 6388 - KI#186.3 captured-city Domination keep repair - CLEAN
- The repair hoists Base AdvCiv's explicit DOMINATION3 + primary-area keep reason before SAS force-raze gates while preserving both inherited exclusions: !bCultureVictoryEmergency and !bRaze.
- Final polarity/time-slice comparison against Base AdvCiv confirms it cannot override a culture-victory emergency or an already-forced raze; it merely prevents SAS's newer force-raze safeguards from pre-empting the inherited Domination keep reason.

Practical 6385 - expanded SASGameRecord city-raze context - CLEAN
- CvPlayer::raze has no return after beginSASGameRecordCityRaze() and before disband()+endSASGameRecordCityRaze(); the only early return is canRaze() before the recorder context exists.
- The pending context contains copied scalar/string identity/state, not a CvCity* retained across destruction. end() re-reads player/team/map state only after disband.
- The recorder's vector stack correctly handles theoretical synchronous nested razes: each nested raze pushes/pops before the outer Python callback returns. A top-context razer match prevents consuming a different pending context.
- Current shipped cityRazed Python handlers only read/log the still-live city and do not initiate another raze/session replacement. Session recorder reset clears the context stack.
- Team/player objects remain valid indexed game objects after a city raze; using the captured original razer-team identity for before/after victory/land/population comparison is intentional.

Practical 6384 - expanded goody / partial-research history - CLEAN
- receiveGoody stores newly created free-unit and hostile-Barbarian CvUnit pointers only until the same function's level-2 GOODY_RECEIVED row.
- Between storing those pointers and logging them there is no kill(), convert(), jump, combat resolution or recursive goody call. Free units are created/promoted in place; hostile units are spawned on adjacent plots and do not immediately attack.
- The trigger unit is only modified for XP/healing/moves in this transaction and remains live through the row.
- AdvCiv follow-up doGoody() recursion occurs only after logSASGameRecordGoodyReceived(), so follow-up effects cannot invalidate the stored unit pointers before they are consumed.
- No recorder-induced RNG/state mutation was found beyond measuring already-realized outcomes.

Authoritative continuation:
WIP703 physical -> primary 169/169 COMPLETE -> F708-F716 confirmed -> F717/KI#1038 UNUSED -> practical 6389 DEDUP KI#197.11 -> practicals 6388/6385/6384 CLEAN -> continue practical 6382 research-lifecycle history, then 6381/6380 diplomacy history; whole-album dedup remains mandatory before promotion.
================================================================================

================================================================================
POST-PRIMARY CURRENT-TAIL AUDIT - C031-WIP704
Continuation from WIP703; no gameplay/XML/Python source modified.
F717 / provisional KI#1038 remains UNUSED.

Practical 6382 - expanded research lifecycle history - CLEAN
- CvPlayer::doResearch records the exact fresh modified research rate plus incoming stored overflow immediately before clearing overflow and applying their combined modified progress.
- TECH_ACQUISITION_RESEARCH currently has exactly the ordinary doResearch progress caller. Therefore the recorder-local application context cannot be consumed by an unrelated progress source.
- CvTeam::setResearchProgress logs completion after the crossing has granted outgoing unmodified overflow and KI#404 has clamped stored team progress to exactly the research cost, but before setHasTech can retarget/clear queues and before the generic TECH_ACQUIRED row. That matches the claimed RESEARCH_COMPLETED chronology.
- In team research, iOldProgress is the shared team progress immediately before this player's crossing contribution; iProgressBeforeClamp includes exactly that contribution, and the crossing player correctly receives only its resulting excess.
- The target-change observer runs after AI_doTurnPre has finalized the turn's target and before this turn's science. It detects only switches away from a still-incomplete invested technology, handles repeat-tech completion through tech-count change, records remaining teammate researchers, and resets any pending factual cause at every observation when no qualifying redirect materializes. No stale cause drift found.

Practical 6381 - rejected AI->human offer bridge - CLEAN
- USER_DIPLOCOMMENT_REJECT_OFFER dispatch has eFrom=active human and eTo=AI, while getProposedTrade() is rooted at the active human: trades() are the human/responder side and otherTrades() are the AI/proposer side. The bridge intentionally passes otherTrades as proposerGives and trades as responderGives, so orientation is correct.
- Python flattens only TradeData item type/data pairs from the already-resolved BUG trade object. C++ validates player IDs, list parity and TradeableItems bounds and reconstructs local CLinkList<TradeData> copies; no pointer/lifetime dependency on the Python Trade object remains.
- The bridge performs only canonical recorder formatting/attitude-state reads. It does not invoke AI trade valuation, submit a deal, or mutate diplomacy.
- Unvalidated per-item iData originates from the engine-built diplomacy table under the shipped caller rather than arbitrary user text; current event construction caps/terminates the trade table normally.

Practical 6380 - resolved diplomacy / submitted-offer history - CLEAN
- handleDiploEvent captures relationship state before its mutation switch and captures after-state only once the resolved event has applied its memory, conversion/revolution, join-war, embargo or demand/help bookkeeping.
- Requester/responder orientation is correct. AI-originated GIVE_HELP/DEMAND/CONVERT/REVOLUTION/JOIN_WAR/STOP_TRADING naturally use this player as requester; ASK_HELP/MADE_DEMAND are the converse path where this player is the AI responder and ePlayer is the human requester, so the explicit flip is required.
- ASK_HELP/MADE_DEMAND outcome uses the same iData1>0 predicate that gameplay itself uses for the granted-request memory path. Religion/civic and team subjects are read from stable requester state or explicit iData1.
- AI_considerOfferExternal and AI_counterProposeExternal still invoke their gameplay AI method exactly once. Level-2 logging captures/query-formats the submitted lists and relation state; it neither repeats valuation nor executes a deal. Original pointer preconditions are unchanged.

Authoritative continuation:
WIP704 physical -> primary 169/169 COMPLETE -> F708-F716 confirmed -> F717/KI#1038 UNUSED -> practicals 6382/6381/6380 CLEAN -> continue first substantive pre-6380 diffs with current-source revalidation and whole-album dedup.
================================================================================

================================================================================
POST-PRIMARY CURRENT-TAIL / REPAIR-SIDE PASS - C031-WIP705
Continuation from WIP704; no gameplay/XML/Python source modified.
Primary queue remains 169/169 COMPLETE. F717 / provisional KI#1038 remains UNUSED.

Practical 6379 - factual SASGameRecord random-choice / team-merge / nuclear context - CLEAN
- CvUnit::nuke computes the authoritative interception chance and makes exactly one SyncRandSuccess100 roll before the new launch row. The logger receives a copied MAX_TEAMS affected-team map and runs before either intercepted-unit kill or successful delayed-death handling; it performs formatting only and adds no RNG/state mutation.
- The later 6434 nuke-recorder audit independently revalidated the same launcher lifetime: the launcher is excluded from nukeExplosion's victim loop and canNukeAt keeps the launch plot outside its own blast. Reuse that completed proof rather than inventing a duplicate finding here.
- CvPlot::nukeExplosion gathers fallout/population totals inside the existing single explosion pass and uses the already-realized AdvC effect vectors for improvements/features/units/buildings/cities. The compact effects row is emitted only for bBomb + non-NULL unit-launched detonations, after changeNukesExploded(1) and after all blast RNG. Intercepted nukes never reach this row.
- Fallout creation is counted only when the old feature was not already the configured fallout feature. Population killed uses the exact realized clamped city population delta. The city-count field intentionally mirrors the existing one-entry-per-city aCitizensKilled vector, including zero-pop-loss cities that were still inside the actual blast.
- CvTeam::addTeam logs the merge after UWAI's pre-reassignment history merge but before any absorbed player changes team. logSASGameRecordTeamMerged scans assigned player slots and formats strings only, so both pre-merge lists remain exact and logging cannot affect the transaction.
- PLAYER_SETUP merely exposes AdvC's existing civ/leader random-choice provenance. CvInitCore initializes unknown=false, marks new-game setup known through its external-RNG/setup synchronization, serializes the flag and per-player arrays, preserves them across slot swaps, and leaves old/imported unavailable provenance explicitly unknown. No new assignment inference or RNG is introduced by 6379.

Practical 6378 - source version / save-history UI and replay exposure - CLEAN / non-simulation
- C++ changes are presentation/provenance plumbing: centralized compact source formatting, replay filename/display text, replay Settings history, removal of the redundant short-SHA cache/accessor, and Python/UI accessors.
- getSASCompactSourceVersion derives only display text from already-cached version/SHA/dirty facts. Removing ModName's short-SHA cache does not alter source discovery; every surviving consumer derives the same first 10 characters from the full hash.
- CvPlayer's replay-saving name prefix executes only under m_bSavingReplay and affects the replay/Hall-of-Fame filename identity, not player identity during gameplay.
- CvReplayInfo::appendSettingsMsg reads persisted version-history fields and formats them into replay settings. No game-state mutation, RNG, save-rule change or gameplay cache dependency is introduced by this commit.

Authoritative continuation:
WIP705 physical -> primary 169/169 COMPLETE -> F708-F716 confirmed -> F717/KI#1038 UNUSED -> practicals 6379/6378 CLEAN -> continue repair-side audit with practical 6377 (KI#617/#618 embargo valuation), then 6376/#619-620 and older fixes; current-source proof + whole-album dedup mandatory before any promotion.
================================================================================

================================================================================
POST-PRIMARY REPAIR-SIDE PASS - C031-WIP706
Continuation from WIP705; no gameplay/XML/Python source modified.
F717 / provisional KI#1038 remains UNUSED.

Practical 6377 - KI#617/#618 embargo valuation scope repairs - CLEAN
- AI_stopTradingTradeVal's persistent-deal scan now uses the exact enacting player, matching CvPlayer::stopTradingWithTeam and guaranteeing every subsequent exact-player CvDeal accessor receives a real endpoint. Teammate-owned resource/GPT deals that survive the embargo cannot inflate price or reparations denial.
- The reparations safeguard is likewise exact-player scoped; a teammate's peace-reparations obligation survives this player's embargo and therefore must not block it.
- Multi-member war-bribe valuation still sums genuinely distinct player-owned losses, while bIncludeTeamRelations lets only the first qualifying member include the one shared Open Borders / Defensive Pact state. The flag is flipped only after a qualifying member is actually valued.
- Residual heuristic note: because the shared term is embedded in one member's valuation rather than algebraically factored into a separate team helper, member-specific sponsor/focus-war modifiers can make the representative contribution depend on the first qualifying MemberIter entry. The repair explicitly chooses that representative and no cancellation/state invariant requires another one; retain as possible valuation polish, not a current correctness finding.

Practical 6376 - KI#619/#620 attitude timing / legacy multi-war bundle repairs - CLEAN
- Both annual-deal directions now call AI_processPeacetimeTradeValue/GrantValue with bUpdateAttitude=false. CvGame::doDeals processes every deal and then calls the single global CvPlayerAI::AI_updateAttitudes(), so deal orientation/iteration no longer exposes a mid-batch refreshed attitude cache.
- The default immediate-update behavior remains available to non-recurring callers; the repair is confined to CvDeal::doTurn's batch context.
- AI_dealVal now increments its TRADE_WAR bundle count regardless of UWAI/legacy mode and makes every second hired-war item unaffordable before execution. It still computes the selected individual war value through the configured UWAI or legacy war AI, so one-war valuation semantics are unchanged.
- The guard is valuation-only exactly as the original AdvC policy was; it introduces no extra RNG/state mutation and does not execute or sponsor a second war while evaluating the list.

Practical 6375 - KI#577 original-UWAI invasion horizon scheduler repair - CLEAN
- Zero-defense priority probes are no longer treated as lower bounds for actual defended-step duration. An actual over-horizon step is deleted and only that invader is marked in a defender-local abHorizonExcluded set; unrelated concurrent invaders continue to be scheduled.
- The local exclusion is checked both when constructing priority probes and when selecting/summing candidate threats, so the same over-horizon invader cannot be selected repeatedly and no longer consumes the defender split on later iterations.
- The inherited comment saying a continued else branch would need to unset targeting described the old no-local-exclusion implementation: removing the graph edge was one way to avoid reselecting that invader. KI#577's explicit local exclusion supplies the missing loop-state mechanism without falsifying the persistent war target.
- Keeping m_pPrimaryTarget/m_targetedBy is coherent after this function: the invader can remain genuinely at war/targeting that defender even when no actual invasion step fits the current finite analysis horizon, and the later concurrent ArmamentForecast may still use that real target to choose naval/land build-up. No repeated step, stale pointer, or threat carry-over defect found.
- rPastThreat remains unchanged when an unexecuted over-horizon step is discarded, correctly preserving only defender commitment from the last actually failed attack; the excluded probe itself consumed no simulated defenders/time.

Authoritative continuation:
WIP706 physical -> primary 169/169 COMPLETE -> F708-F716 confirmed -> F717/KI#1038 UNUSED -> practicals 6377/6376/6375 CLEAN -> continue practical 6374 SASFastSave lifecycle/autoplay feature audit, then 6373 UWAI cache-lifecycle repair; whole-album dedup mandatory before promotion.
================================================================================

================================================================================
POST-PRIMARY CURRENT-TAIL / REPAIR-SIDE PASS - C031-WIP707
Continuation from WIP706; no gameplay/XML/Python source modified.
Primary queue remains 169/169 COMPLETE.

Practical 6374 - SASFastSave lifecycle / autoplay saves - CLEAN
---------------------------------------------------------------
Reviewed the new-game, victory, no-victory game-over and autoplay-stop save paths as one
state machine rather than isolated Python callbacks.

- Normal victory emits the existing victory event while the winner/victory state is
  authoritative. BugEventManager registers SASFastSave's victory handler before
  AIAutoPlay's victory handler, so the victory save is made before autoplay teardown.
- Rise & Fall suppresses that ordinary event and therefore uses CvGame::setWinner's
  direct SASFastSave fallback. The later GAMESTATE_OVER hook explicitly runs only when
  no winner/victory exists, preventing a duplicate end save.
- AIAutoPlay calls endAIAutoPlay before saveAutoPlayStop. In single-player the C++
  teardown restores human-disabled state as part of that transition. STOP saves are
  emitted only for scheduled/user interruption, not for victory, so the victory path
  cannot also create a STOP row.
- SASFastSave._save centrally rejects multiplayer (network, PBEM and hotseat through
  CvGame::isGameMultiPlayer), creates the normal single-player save directory when
  needed and uses the engine SaveGame API through CyGame::saveGame.
- Same-second basename collisions receive _2/_3/... suffixes rather than overwriting the
  first save. Filename provenance uses stored game/setup facts and UTC time only; it
  consumes no synchronized RNG.
- The autoplay popup's new default is maxTurns(game speed)+1. Current shipped totals are
  166/251/331/501/751/1001/1501/2001 including the +1, all within the existing four-
  character edit field.
- Later current history contains no SASFastSave lifecycle repair superseding these paths.

No duplicate save, autoplay-state, RNG or current shipped field-width defect survives.

F717 / KI#1038
---------------
TITLE
    KI#554's SAS UWAI city-cache repair rebuilds observer target values only for capital
    loss; routine foreign city creation/destruction can still leave surviving target
    values and attack ordering stale.

STATUS
    CONFIRMED repair-side AdvCiv-SAS regression / incomplete implementation of the
    already-confirmed inherited F231/KI#554 invalidation root.

BIRTH / REPAIR CONTEXT
    Practical 6373 (`af4775625b506c76af4a5fa409c59f61b1907d29`) fixed inherited
    KI#554/KI#556/KI#557.

    It correctly added UWAICache::rebuildCities(), fully rebuilding the cache when the
    CACHE OWNER's own city set changes. For foreign destruction it added a post-mutation
    callback:

        reportCitySetChanged(eChangedOwner, bCapitalChanged)

    but deliberately rebuilds an observer only when:

        m_eOwner == eChangedOwner || bCapitalChanged

    Its implementation comment says ordinary non-capital loss avoids rebuilding
    "unaffected observer caches", and its Known-Issues repair text says capital
    replacement additionally rebuilds observers because target values can change.

    Foreign city creation likewise remains an incremental add/remove/sort operation for
    observers; existing foreign City wrappers are not reconstructed.

WHY NON-CAPITAL FOREIGN CITY-SET CHANGES ALSO INVALIDATE TARGET VALUE
    UWAICache::City construction stores m_iTargetValue once through:

        GET_PLAYER(eCacheOwner).AI_targetCityValue(kCity, false, true, this)

    Current CvPlayerAI::AI_targetCityValue contains a non-capital Space3 branch whose
    result depends directly on the TARGET OWNER's complete current city set:

        kCity.findYieldRateRank(YIELD_PRODUCTION) <
            std::min(5, kOwner.getNumCities() / 4)

    If true it adds +3 and sets bThwartVictory.

    CvCity::findYieldRateRank is explicitly 1-based: it sorts all current owner cities
    and writes rank i+1. Thus neither dependency is capital-specific.

DETERMINISTIC CURRENT FIXTURE
    Observer C has B's cities in its UWAI cache. B is at Space3 but not Space4; C is not
    at victory stage 4. B has 8 cities. Let a surviving non-capital city S be B's best
    production city (rank 1), and let an unrelated lower-production non-capital city D
    be destroyed.

    Before D is lost:

        B cities = 8
        threshold = min(5, 8 / 4) = 2
        rank(S) = 1
        1 < 2  -> true

    so S receives the +3 anti-Space3 target-value bonus.

    After D is deleted:

        B cities = 7
        threshold = min(5, 7 / 4) = 1
        rank(S) remains 1
        1 < 1  -> false

    so a fresh City wrapper must lose that +3 bonus.

CURRENT CALLBACK RESULT
    Before CvCity storage is freed, reportCityDestroyed removes D from C's cache and
    immediately calls sortCitiesByAttackPriority(). S survives with its old
    m_iTargetValue, so this sort already uses the stale +3 value.

    After deletion, CvCity::kill broadcasts AI_citySetChanged(B, false). For observer C:

        m_eOwner != B
        bCapitalChanged == false

    therefore practical 6373's reportCitySetChanged declines to rebuild C. The stale
    value and the ordering produced from it persist until a later full cache refresh.

    The foreign-creation side has the same invalidation family: adding a foreign city can
    change its owner's getNumCities()/4 threshold and all production ranks, but observers
    only add the new wrapper and sort; surviving wrappers retain their previous values.

LIVE CONSUMER
    UWAICache::City::attackPriority() directly adds getTargetValue() to the cached
    priority score. sortCitiesByAttackPriority() orders the vector from those scores.

    InvasionGraph::Node binds m_kCache to the node player's own UWAICache and
    findBestTarget() relies on that cached vector ordering: the first reachable valid
    entry is treated as the best target. Thus a stale +/-3 target value can change city
    ordering and, near a boundary, which rival is modeled as the node's target. Military
    analysis can instantiate nodes for multiple current/future war parties, so caches
    are not merely private UI data that can safely wait for their owner's next turn.

WHOLE-ALBUM DEDUP / WHY F717 IS REPAIR-SIDE RATHER THAN A NEW INHERITED ROOT
    F231/KI#554 already proved the inherited underlying invalidation family and later
    refined it to City::m_iTargetValue. That refinement explicitly recorded dynamic
    target-value dependencies and said the post-city-set reconstruction must reconsider
    target value as well as geometry/reachability; capital replacement was described as
    an especially clear witness, not as the only invalidator.

    Therefore do NOT reinterpret this as a second inherited UWAI root. F717 records the
    current SAS repair-side regression: practical 6373 implemented the broader F231
    repair with an observer optimization that is too narrow for target-value inputs.
    This is analogous to F714's separate tracking of an incomplete later repair while
    retaining the original inherited root's ownership.

REPAIR BOUNDARY
    Do not blindly rebuild every observer for every city mutation without considering
    cost. The correctness requirement is that every observer City whose cached target
    value depends on the changed owner's city set be refreshed before the next live use,
    followed by attack-priority resorting.

    A simple safe repair is to rebuild eligible observer caches on foreign major-civ
    city creation/destruction as well as capital loss. A narrower optimization would
    need to recompute target values for surviving cities of eChangedOwner (and re-sort)
    whenever that owner's city set changes, while retaining the full rebuild only where
    observer deployment geometry also genuinely depends on the cache owner's own city
    set. Any optimized repair must cover both creation and destruction and must not
    assume capital status is the only foreign target-value city-set dependency.

CURRENT STONE AFTER C031-WIP707
--------------------------------
WIP707 physical.
Primary 169/169 COMPLETE.
Post-primary F708-F717 CONFIRMED.
F718 / provisional KI#1039 UNUSED.
No repository gameplay/XML/Python source modified.

NEXT
----
Continue practical 6373 after F717 rather than assuming one repair-side finding exhausts
it: recheck full rebuild membership/pathfinder/aggregate reset behavior and the remaining
KI#556/KI#557 repair interactions against current source. Then continue practical 6372
and older substantive diffs from the local commit-diff mirror. Whole-album dedup remains
mandatory before promoting F718.
================================================================================

================================================================================
POST-PRIMARY REPAIR-SIDE PASS - C031-WIP708
Continuation from WIP707; no gameplay/XML/Python source modified.

Practical 6373 remainder - KI#554/KI#556/KI#557 - CLEAN APART FROM F717
----------------------------------------------------------------------------
After freezing F717, rechecked the repair's own reconstruction and the other two city
lifecycle fixes rather than assuming the one observer invalidation exhausted the commit.

- UWAICache::rebuildCities reproduces the city-derived portion of the ordinary full
  update: delete wrappers, clear the map, reset owner/reachable counts from current
  state, reuse the same KNOWN_TO team iteration and TeamPathFinders, reconstruct every
  eligible City, sort by attack priority, then recompute total assets. It deliberately
  leaves unrelated persistent/military/threat/history cache families untouched.
- KI#556's incremental foreign-city membership now matches updateCities: same-team is
  allowed directly; a foreign team must be met; then human caches may include all known
  civ cities while AI caches still require AI_deduceCitySite.
- KI#557's add helper establishes unique map ownership before exposing the allocation in
  m_cityList. An unexpected duplicate asserts in Debug, deletes the new wrapper and
  returns without corrupting vector/map ownership. reportCityCreated removes an existing
  final-plot wrapper before its normal incremental add.
- First-city/own-city callbacks may perform an extra reconstruction after earlier UWAI
  initialization work, but no duplicate or stale city object survives under current
  current-source ordering.

No second independent 6373 repair-side root beyond F717 was found.

F718 / KI#1039
---------------
TITLE
    KI#529's assault-cargo repair substitutes canAttack for invasion capability; a
    Gunship-only assault load can still authorize a war it cannot execute against an
    undefended city.

STATUS
    CONFIRMED repair-side AdvCiv-SAS regression / incomplete implementation of the
    already-confirmed F206/KI#529 assault-cargo capability root.

BIRTH / REPAIR CONTEXT
    Practical 6372 (`a9d70f507ad2eb7a5d11c75ab528dceb1174b5fc`) fixed KI#529 by
    introducing CvSelectionGroup::getCargoThatCanAttack(). It counts loaded units whose
    transport belongs to the assault group and whose CvUnit::canAttack() is true.

    The new helper replaced total cargo at three relevant boundaries:
      * CvSelectionGroupAI::AI_isDeclareWar for UNITAI_ASSAULT_SEA;
      * SAS KI#193's opportunistic below-threshold AI_assaultSeaMove launch gate;
      * AI_enemyTargetMissions strategic-pressure counting for assault groups.

    The intent comments say actual attack-capable cargo should make declaration intent
    match an invasion the group can execute.

CURRENT SHIPPED COUNTEREXAMPLE
    A mechanical current CIV4UnitInfos.xml check finds exactly one fighting unit with
    bNoCapture=1 and bOnlyDefensive=0:

        UNIT_GUNSHIP
        DOMAIN_LAND
        iCombat 26
        bOnlyDefensive 0
        bNoCapture 1
        DefaultUnitAI UNITAI_ATTACK
        Special NONE

    Standard assault hulls such as Galley/Galleon/Transport accept DOMAIN_LAND cargo
    with no required SpecialCargo, so a Gunship is a legal assault-transport cargo type.

WHY THE REPAIRED CAPABILITY TEST STILL PASSES
    A Gunship can attack, so:

        getCargoThatCanAttack() > 0

    is true. Therefore an assault group carrying only a Gunship can still:
      * make AI_isDeclareWar return true; and
      * enter KI#193's opportunistic below-threshold AI_assaultSeaTransport path.

    AI_assaultSeaTransport itself also treats the Gunship as military strength. Its cargo
    scan adds every live canAttack cargo unit to iLandedAttackStrength; against an
    undefended target (iEnemyDefenders == 0), the defender/strength rejection block is
    skipped. A revealed/deduced foreign coastal city can therefore remain a valid assault
    target for a Gunship-only load.

DETERMINISTIC DECLARATION-THEN-FAILURE CHAIN
    Use the same legal diplomacy/path setup as F206: an imminent sneak-war target whose
    coastal city is reachable pre-war through Open Borders, and let that city be
    undefended.

    1. Pre-war, bNoCapture does not forbid ordinary entry into a non-enemy city, so the
       target can participate in the prospective MOVE_DECLARE_WAR path.
    2. AI_transportGoTo calls AI_considerPathDOW for that amphibious destination.
       F206 already proved the current declaration path removes Open Borders, changes
       war state, resets path state and then explicitly regenerates the target path.
    3. Post-DoW, CvUnit::canMoveInto rejects the Gunship's non-attack entry because
       isNoCityCapture() && isEnemyCity(kPlot).
    4. CvUnit::canMoveInto's attack path also rejects the empty city because K-Mod's
       current attack contract requires kPlot.hasDefender(...). With zero defenders,
       there is nothing for the Gunship to attack.
    5. canMoveOrAttackInto therefore has no legal Gunship mode into the target city.
       The regenerated amphibious assault can fail after the irreversible declaration.

    Thus practical 6372 removes the Missionary/Great-Person witness but does not satisfy
    the deeper F206 capability contract for a current shipped military cargo unit.

WHOLE-ALBUM DEDUP
    F206/KI#529 owns the inherited/SAS-amplified root: "any cargo" was used as invasion
    capability, letting a transport declare war and then lose its amphibious path.

    The original F206 repair analysis explicitly recommended a military/CAPTURE-capable
    measure and, more robustly, target-local post-DoW landing legality. The album also
    separately audited all-Gunship/no-capture city behavior and established that a
    Gunship cannot enter an undefended enemy city; that earlier check was not itself a
    root because the then-current callers had additional capture/path safeguards.

    Practical 6372 implemented only canAttack(), thereby reactivating that known
    no-city-capture boundary specifically inside KI#529's repaired assault-war gate.
    F718 therefore tracks the incomplete SAS repair; it does not renumber F206 or claim a
    second inherited K-Mod root.

REPAIR BOUNDARY
    Do not revert to total cargo and do not disable useful civilian rescue.

    The minimum current-data correction must prevent cargo that can fight but cannot
    execute the intended city landing from authorizing the declaration. A generic helper
    could at least distinguish city-capture-capable combat cargo (e.g. exclude
    isNoCityCapture for city-taking intent), but F206's stronger repair principle remains
    preferable: validate that some ready cargo member can legally move or attack into the
    actual amphibious target under the post-DoW state/contract before treating the
    transport as sufficient reason to declare war.

    AI_enemyTargetMissions is a somewhat different strategic-pressure use: a Gunship is
    genuine military pressure even though it cannot capture a city, so do not blindly
    change every getCargoThatCanAttack consumer to a capture-only census. Repair the
    declaration/opportunistic invasion capability boundary according to what that caller
    actually promises.

CURRENT STONE AFTER C031-WIP708
--------------------------------
WIP708 physical.
Primary 169/169 COMPLETE.
Post-primary F708-F718 CONFIRMED.
F719 / provisional KI#1040 UNUSED.
No repository gameplay/XML/Python source modified.

NEXT
----
Continue practical 6372 after F718: verify getCargoThatCanAttack lifetime/group ownership,
mixed-cargo and merge/split behavior, and the AI_enemyTargetMissions use independently so
F718's repair does not overreach into legitimate military-pressure counting. Then audit
practical 6371 (KI#515 victory-denial force-peace state) and 6370 (KI#509/#510 peace-asset
valuation) from the local commit-diff mirror. Whole-album dedup remains mandatory before
promoting F719.
================================================================================

======================================================================
POST-WIP708 CONTINUATION CHECKPOINT -- C031-WIP709
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F718 / KI#1039
Next finding number: F719 / provisional KI#1040 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6372 remainder -- CLOSED CLEAN APART FROM F718
--------------------------------------------------------
- getCargoThatCanAttack() derives assignment from each cargo unit's live transport and
  that transport's current group on every call; merge/split does not leave a cached
  group-membership assumption behind.
- Mixed cargo remains appropriate for the strategic-pressure caller: civilians no
  longer inflate military assault pressure; Gunships remain genuine military pressure.
- F718 remains intentionally caller-specific: canAttack() is insufficient proof for
  the declaration/opportunistic CITY-TAKING assault gates, but that does not imply
  that all strategic-pressure uses should exclude no-city-capture attackers.
- No second independent practical-6372 root survived.

Practical 6371 / KI#515 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit: bb7737726cc19bc64658b3a2379b4de9108c3496
Title: fix AdvCiv-SAS victory-denial force-peace state in KI#515

- scheme() already requires canEventuallyDeclareWar(eTarget); the added
  kAgent.canDeclareWar(eTarget) narrows only the DIRECT state.
- In current CvTeam logic, the relevant additional blocker represented by
  canDeclareWar beyond eventual legality is force peace.
- The repair therefore preserves legal long-horizon preparation while preventing a
  force-peace target from receiving a direct LIMITED/TOTAL plan or entering the direct
  declareWar branch before declaration is legal.
- The separate accepted-demand treaty exclusion remains intact.
- No repair-side regression found.

Practical 6370 / KI#509-KI#510 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: 045ed0caa45c474fa2ebccd9d568e8d470d21ee9
Title: fix inherited AdvCiv peace-asset valuation KI#509/KI#510

KI#509:
- endWarVal's early 'no payment possible' shortcut now includes TRADE_CITIES alongside
  Gold and Technologies.
- canPossiblyTradeItem(TRADE_CITIES) is deliberately only a cheap capability gate;
  it does not itself construct or approve a city payment. Existing downstream legality,
  valuation and city selection remain authoritative.
- A false-positive capability result can only cause the full valuation path to run;
  it cannot manufacture a treaty item.

KI#510:
- canTradeAssets retains AdvCiv's documented quantity policy:
      ceil(human city count / 6)
  cities may contribute to its AFFORDABILITY/UPPER-BOUND estimate.
- The repair collects all currently legal city values and sums only the best values
  allowed by that unchanged quantity cap instead of consuming an arbitrary city-ID
  prefix.
- This exactly implements F187's repair boundary. The original album explicitly noted
  that canTradeAssets only needs an upper-bound/existence answer and that actual peace
  construction can retain AI_negotiatePeace's own city-selection policy.
- CvPlayerAI::AI_negotiatePeace currently inserts at most one directly selected city,
  but that distinction predates practical 6370 and is not worsened by selecting the
  best values within canTradeAssets' pre-existing multi-city cap.
- Principal callers use canTradeAssets as willingness/affordability information rather
  than as a promise that a particular list of TradeData has already been constructible.
- No incomplete KI#509/KI#510 repair or new current defect proved.

WHOLE-ALBUM DEDUP / NEGATIVE CONTROL
------------------------------------
- F187/KI#510 already recorded both the one-sixth cap and the difference between
  canTradeAssets' estimator role and AI_negotiatePeace's city constructor. Do not
  reopen that intentional estimator/constructor distinction without an independent
  false-positive transaction proof.

PHYSICAL CONTINUATION STONE
---------------------------
WIP709 physical -> primary 169/169 COMPLETE -> F708-F718 CONFIRMED ->
F719/KI#1040 UNUSED -> practical 6372 remainder CLEAN -> practical 6371 CLEAN ->
practical 6370 CLEAN -> classify 6369 downward from local commit-diff mirror ->
open first substantive gameplay/state change -> current-source proof + whole-album
DEDUP mandatory before promotion -> freeze WIP710 at next settled boundary.

======================================================================
POST-WIP709 CONTINUATION CHECKPOINT -- C031-WIP710
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
New finding in this append: F719 / provisional KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practicals 6369-6360 -- REPAIR-SIDE REVALIDATION
------------------------------------------------
6369 / KI#671-KI#672 -- CLOSED CLEAN
- AI_espionageVal now receives the acting Spy, so structural/unit-sensitive legality and
  stationary mission-cost discount use the same physical unit as the sole selector.
- bCheckPoints=false removes only current-point affordability; the caller still owns
  immediate-vs-save-for-later handling and revalidates execution normally.
- KI#672's stealable-tech count is now team-scoped and the approximate tech cost is
  carried with the same rival team whose city modifier wins. No cross-rival accumulator
  survives.

6368 / KI#517 -- CLOSED CLEAN
- doWarReport synchronizes the global UWAI background state and this Team agent's cached
  background state for the whole diagnostic doWar call, restoring each independently.
- Team-local mutation guards reached by doWar therefore see background mode; prior
  background state is preserved.

6367 / KI#511 -- CLOSED CLEAN
- Temporary imminent-plan conversion now preserves TOTAL vs LIMITED intent before
  considerSwitchTarget; no-switch/background restores the original imminent type.

6365 / KI#512 -- CLOSED CLEAN
- Including the exact iLimitedU==0/iTotalU>0 boundary selects TOTAL when TOTAL is the
  only positively evaluated mode while retaining personality weighting when both are
  positive.

6364 / KI#513 -- CLOSED CLEAN
- Deep-sea-cargo capability and its land-distance exemption are derived per team member;
  an earlier member can no longer widen a later member's limit.

6363 / KI#519 -- CLOSED CLEAN
- Human-capitulation readiness is cleared at the war-ending sponsorship lifecycle
  boundary, after the live-war authorization has served its purpose and before any
  later war against the same human can reuse it.

6362 / KI#535 -- CLOSED CLEAN
- setForceUpdate(true) has one live producer in CvSelectionGroup::doTurn; that producer
  already clears every old queued mission/group attack.
- Thus force-update + group-attack coexisting later means the attack is newer state;
  preserving it in the separation pre-pass and AI_update entry is correct.

6361 / KI#523 -- CLOSED CLEAN
- Weighted odds request full-range pre-personality odds only at the lossy public 1/99
  boundaries; ordinary interior subtraction remains exact.
- The selected attacker remains the same actor the group attack path would use; the
  repair fixes F200's lossy reconstruction without changing ordinary public odds.

6360 / KI#532-KI#533 -- CLOSED CLEAN
- Air groups bypass the SAS land/sea low-power effective-strength ranking and inherited
  LFB ordinary-combat construction, reaching the existing iAirCombat-aware odds path.
- Land/sea behavior and default public contracts remain unchanged.

6359 / KI#525-KI#526 -- CLOSED CLEAN
- The enriched KI#319 no-progress signature observes queued-group-attack consumption
  and total current member moves in addition to head identity/role/geometry/moves,
  mission count, membership and cargo.
- Current synchronous group-AI action paths do not provide a live same-membership
  movement transfer that can spend one member while restoring another to preserve the
  same aggregate; role/group/cargo changes are separately observed.
- No surviving same-length mission replacement was proved that remains nonbusy/ready,
  leaves every other signature component unchanged and requires another same-slice
  evaluation.

F719 / provisional KI#1040
--------------------------
TITLE
    KI#530's repaired human smart-Bombard attacker census uses attack-only movement for
    undefended cities, so every legal immediate city-capturer is excluded and the
    easy-capture ordering state becomes unreachable exactly in the zero-defender case.

STATUS
    CONFIRMED. AdvCiv-SAS repair-side regression introduced by practical 6356
    (5234f5012d74..., "fix inherited AdvCiv KI#528/KI#530/KI#531 human Bombard ordering")
    while repairing inherited F207/KI#530. Current reconciled source retains it.

ROOT
    CvSelectionGroupAI::AI_bestUnitForMission(MISSION_BOMBARD) computes the human
    easy-city-capture attacker count with:

        if (pUnit->getOwner() == getOwner() && pUnit->canMove() &&
            !pUnit->canBombard(kAt) &&
            pUnit->canMoveInto(*pMissionPlot, true))
        {
            iAttackers++;
            ...
        }

    The owner and canMove additions correctly remove F207's foreign-unit and exhausted-
    unit false positives. The final predicate is too narrow for an undefended city.

VIOLATED CONTRACT / CROSS-FILE PROOF
    K-Mod's CvUnit::canMoveInto contract explicitly defines bAttack=true as requiring
    an actual fight. In the ordinary-combat branch it rejects the attack form when:

        !kPlot.hasDefender(...)

    and its own comment says:

        "bAttack means we have to actually fight an enemy unit.
         Capturing an undefended city doesn't count."

    Therefore, when iDefenders==0, every ordinary unit that can legally move into and
    capture the enemy city fails canMoveInto(target,true).

    That is the exact supported state F205/KI#528 already proved live: human Bombard is
    legal against an undefended city and the smart-ordering code intentionally has a
    zero-defender path. The original F207/KI#530 strengthening also scoped its proposed
    attack-specific predicate to the DEFENDED-target branch and explicitly kept the
    zero-defender smart-Bombard case separate. Practical 6356 applied it unconditionally.

DETERMINISTIC CONSEQUENCE
    For an undefended bombardable enemy city:

        iDefenders = 0
        iAttackers = 0   // regardless of a same-owner movable capture-capable unit

    The later easy-capture test first accepts 0 >= 0, but then multiplies the stack ratio
    by iAttackers/max(iDefenders,1) = 0/1, so bEasyCityCapture cannot become true.

    This is behaviorally visible in the Bombard actor ordering. When bEasyCityCapture is
    true, each Bombard candidate's priority is multiplied by its current hit points.
    Use two otherwise identical bombarders in one human group, a wounded one earlier in
    group iteration and a healthy one later, plus one same-owner healthy non-bombarder
    that can immediately enter/capture the adjacent undefended city. With no defenders,
    AI_compareStacks is overwhelmingly favorable.

    Correct immediate-capture census:
        attacker count >= 1 -> easy capture true -> healthy bombarder's hit-point factor
        beats the wounded one.

    Current repaired census:
        every capture-capable unit fails attack-only canMoveInto -> attacker count 0 ->
        easy capture false -> the otherwise identical bombarders tie and the first
        (wounded) unit remains selected.

    Thus practical 6356 fixed defended-city false positives but introduced a deterministic
    zero-defender false negative that changes which human siege unit spends Bombard.

ANCESTRY / DEDUP
    - Original inherited root F207/KI#530: generic destination-entry eligibility counted
      foreign peaceful units, same-owner Spies and units unable to act as immediate
      attackers in the defended-city easy-capture census.
    - F205/KI#528 separately owns the inherited zero-defender odds multiplier that erased
      every Bombard priority; its repair makes the present zero-defender ordering live.
    - F719 is not either inherited root. It records practical 6356's incomplete F207
      repair: replacing the broad predicate with an attack-only predicate across BOTH
      defended and undefended targets.
    - Whole-album search found no later owner or repair for this post-KI#530 regression.

NARROW REPAIR BOUNDARY
    Preserve KI#530's same-owner + canMove requirements, but represent "can participate
    in the immediate city take" rather than "must fight a defender".

    One coherent form is to require a real fighting/capture-capable unit and use
    canMoveOrAttackInto for the destination after the owner/current-move filters. Against
    a defended enemy city its ordinary-movement half is naturally blocked and the attack
    half proves combat legality; against an undefended enemy city its ordinary-movement
    half correctly represents city capture. isNoCityCapture units remain rejected by the
    underlying enemy-city movement rule.

    Alternatively branch explicitly on iDefenders, keeping attack-only canMoveInto for
    defended targets and using immediate legal capture-entry for the zero-defender case.
    Do not revert to the original unfiltered canMoveOrAttackInto census: F207's owner,
    combat-capability and current-action corrections remain required.

PHYSICAL CONTINUATION STONE
---------------------------
WIP710 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practical 6356 contains F719 but KI#528/KI#531 otherwise remain
open for repair-side sibling checks -> continue practical 6356 remainder -> then 6355
and older substantive repair commits -> current-source proof + whole-album dedup mandatory
before promotion -> freeze WIP711 at next settled boundary.

======================================================================
POST-WIP710 CONTINUATION CHECKPOINT -- C031-WIP711
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F719 / KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6356 remainder -- CLOSED CLEAN APART FROM F719
--------------------------------------------------------
- KI#528's zero-defender odds repair remains coherent with the now-live smart-Bombard
  zero-defender branch owned separately from F719.
- KI#531's manual defender census retains a theoretical invisible-defender boundary, but
  current shipped SAS data has no invisible combat-capable land defender that activates
  it. Keep as dormant extension/data robustness, not a current finding.

Practical 6355 / KI#708-KI#710 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: f00cdd56d428a07d313d20853e65d9d77822486a
Title: fix inherited AdvCiv KI#708-KI#710 guard scope and Privateer healing regressions

KI#708:
- SquareIter(radius=1) restores the planned city-site center to both coordination and
  scoring while retaining AdvC's adjacent guard coordination.
- The center tie-break is reachable again and a mission targeted at the exact future
  city tile can no longer be invisible to a later guard group.
- No repair-side duplicate-assignment or center-displacement regression found.

KI#710:
- Candidate enumeration is now anchored to the recovered guarded city rather than to
  the moving guard, preventing repeated one-hop drift outside that city's radius.
- The added city-to-candidate one-turn path check does not create a directional return
  trap in current city-radius geometry: candidate distance is at most two plots; route
  use requires routed endpoints; river crossing cost is edge-symmetric; a final
  destination step is legal when movement remains.
- Using current residual movement can conservatively reject a candidate during a
  same-turn reevaluation but cannot strand the guard; the next normal turn restores
  movement. This is scheduling conservatism, not a violated safety invariant.

KI#709:
- BBAI/K-Mod originally allowed the early Pirate heal only in a defended city-like plot
  (city/fort) that was locally safe. AdvC later replaced the whole location predicate
  with AI_isThreatenedFromLand() <= PROBABILITY_LOW; that helper deliberately reports
  NO_PROBABILITY on water, admitting arbitrary open-sea plots.
- The repair restores the inherited location class while retaining AdvC's newer threat
  helper:
    * actual city + CvCityAI::AI_isSafe(), OR
    * team city-defense plot (including city-like fort semantics) + positive local
      friendly defense,
    then AI_isThreatenedFromLand() <= PROBABILITY_LOW.
- This does not re-admit open sea or undefended forts. It also does not over-tighten the
  inherited actual-city exception: the bSafeCity arm corresponds to the earlier AdvC
  safe-city branch, while the defended city/fort arm corresponds to BBAI's city-like
  defended-location rule.
- K-Mod/BBAI ancestry comparison therefore supports the current repair; no independent
  KI#709 repair-side regression survives.

PHYSICAL CONTINUATION STONE
---------------------------
WIP711 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practical 6355 CLOSED CLEAN -> open practical 6354 / KI#704-KI#707
-> prioritize Worker-ferry KI#706 cargo/delivery accounting plus naval safety KI#704/#705
and carrier KI#707 -> current-source proof + whole-album dedup mandatory before promotion
-> freeze WIP712 at next settled boundary.

======================================================================
POST-WIP711 CONTINUATION CHECKPOINT -- C031-WIP712
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F719 / KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6354 / KI#704-KI#707 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: 7465af69801a9970783ca0ef0344625213da17ec
Title: fix inherited KI#704/KI#705/KI#707 naval AI and SAS KI#706 Worker ferry

KI#704:
- The finite iMaxTransportPath shortcut already chooses a candidate enemy coastal land
  plot p and uses a separate pathfinder rooted at the selected transport group.
- Replacing FOR_EACH_ADJ_PLOT(getPlot()) with FOR_EACH_ADJ_PLOT(p) changes only the
  destination end of that proof: reachable water beside the scanned enemy coast is now
  tested instead of water beside the passenger's old source plot.
- Existing movement flags, turn-budget adjustment, enemy-team/city-area filters and
  transport selection remain unchanged. No repair-side false-positive/negative sibling
  survived.

KI#705:
- The remote pickup branch now skips pLoopCity when AI_isDanger() is true, restoring
  K-Mod/BBAI's original safe-port predicate after AdvC's lost-negation flattening.
- The acquired-city age/enemy-power exception and AI_considerPickup role-specific
  eligibility still run before this gate; current-city waiting behavior is untouched.
- No new dangerous/safe polarity mismatch found.

KI#706:
- Once a Settler transport stands on a land/city plot in the destination area, each
  same-owner UNITAI_WORKER cargo unit has moved with the transport and is included in
  that owner's area UnitAI counter even while still loaded.
- For the HARD area-capacity test, subtracting all iWorkers is therefore the exact
  correction required by F383 and matches the sibling destination-unload helper's
  already-repaired semantics. Queued Workers remain represented separately by the train
  count and are not over-subtracted.
- Narrow team/manual-cargo robustness note: canLoadOnto permits same-team cargo while
  getUnitAICargo is owner-agnostic. A manually loaded teammate Worker could therefore
  be treated as this AI transport's Worker payload even though it is absent from the
  transport owner's area count. AI_findTransport's normal AI path scans owner units and
  does not create this cross-owner state; the broader assumption predates KI#706 and is
  not promoted as a current repair regression.

KI#707:
- >= PROBABILITY_REAL exactly restores the ordered strong-threat meaning used by every
  sibling naval role and by the pre-refactor boolean helper: REAL/HIGH enter emergency
  retreat, LOW/NO do not.
- No further Carrier-specific state depends on the old inverted != test.

PHYSICAL CONTINUATION STONE
---------------------------
WIP712 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practical 6354 CLOSED CLEAN -> practical 6353 recorder-only
initialization/timing compaction next -> then practical 6352 / KI#701/KI#703 repair-side
validation -> current-source proof + whole-album dedup mandatory before promotion ->
freeze WIP713 at next settled boundary.

======================================================================
POST-WIP712 CONTINUATION CHECKPOINT -- C031-WIP713
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F719 / KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6353 -- SASGameRecord initialization/timing compaction CLOSED CLEAN
----------------------------------------------------------------------------
Commit: d8358a19dc29cdb44f55a05d298e2428c3d30eeb
Title: Compact SASGameRecord initialization and timing output

- The compact initial-technology grouping key is the complete finalized technology-level
  payload returned by getSASInitialTeamTechLevelFields; only the team identifier is
  intentionally omitted. Each isEverAlive team is still visited exactly once, appended
  to exactly one group and counted in iTeamStateRows/techTeamsCovered.
- Group storage is lifetime-safe: when a new vector element is pushed the pointer is
  reacquired from back() and used before any later push; an existing-group pointer is
  used without growing the vector.
- Exact team membership remains explicit through teams= and teamCount=, so identical
  technology payloads are compressed without assuming scenario/team symmetry.
- Replacing successful setup-time TECH_ACQUIRED chatter does not erase the ruleset cause
  of trade capabilities: GAME_RECORD_TECH_CAPABILITY_SOURCES enumerates the loaded XML
  technologies that grant every capability previously repeated in acquisition rows.
- Initial player identities are emitted before the finalized team rows for both new and
  loaded sessions; numeric team/player IDs remain authoritative where a readable player
  identity is inapplicable (e.g. Barbarian/setup teams).
- Removing sessionWallMilliseconds from every ordinary row changes recorder granularity,
  not gameplay or ordering. Aborted-initialization buffered action rows retain their
  event-time stamps; session start/completion, snapshots, autoplay and map-ASCII blocks
  retain explicit monotonic timing boundaries. No gameplay mutation or RNG was moved
  behind a timing/logging gate.
- No unique finalized setup state or timing boundary was found lost by practical 6353.

Practical 6352 / KI#701 + KI#703 -- REPAIR-SIDE AUDIT CLOSED CLEAN
------------------------------------------------------------------
Commit: b5ca2102f9e246d8d9dc5eec10f2766f4a4ea4be
Title: fix inherited BtS/BBAI KI#701 and AdvCiv/BBAI KI#703 AI defects; defer SAS KI#702

KI#701:
- AI_guardCity proves pBestGuardPlot non-null before the defender-detachment loop and map
  plots remain stable while the donor group is split.
- AI_ejectBestDefender only reads the supplied plot as combat/city-defense ranking
  context, then detaches the selected unit. Passing the destination therefore fixes the
  original source-plot context defect without adding a lifetime/mutation dependency.
- Multiple-defender shortage filling naturally re-ranks the shrinking donor group for
  the same destination. No stale source/destination pointer or wrong-unit sibling remains.

KI#703:
- Exact K-Mod ancestry uses:
    otherTeam != ours && !GET_TEAM(otherTeam).isVassal(ours)
  and scans two plot steps so a distance-2 enemy route can add danger.
- Current SAS now matches that directional predicate literally. It intentionally ignores
  territory of OUR vassals while treating our master like any other non-vassal nearby
  team for this offensive-airbase caution heuristic. That asymmetry is inherited BBAI/
  K-Mod policy, not a repair regression.
- Restoring SquareIter radius 2 is required by the retained distance-2 routed-enemy arm;
  the center cannot count because p.getTeam()==getTeam there.
- getRevealedRouteType(getTeam()) != NO_ROUTE is the correct explicit typed test and
  preserves fog knowledge. It repairs the old raw-enum inversion (NO_ROUTE=-1, Road=0)
  without revealing the live route.
- No independent KI#703 repair-side defect survives.

PHYSICAL CONTINUATION STONE
---------------------------
WIP713 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practicals 6353 and 6352 CLOSED CLEAN -> classify/open practical
6351 downward from the local commit-diff archive -> skip docs/generated-only changes but
preserve chronology -> audit first substantive gameplay/AI repair with current source +
ancestry + whole-album dedup -> freeze WIP714 at next settled boundary.

======================================================================
POST-WIP713 CONTINUATION CHECKPOINT -- C031-WIP714
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F719 / KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6351 -- finalized-initialization diagnostic normalization CLOSED CLEAN
-------------------------------------------------------------------------------
Commit: 9068ad28635d8be44965efadf9bce1738e1b412d
Title: Normalize finalized initialization logging and refactor remove duplicated paths

- Shared CvGameCoreUtils serializers replace duplicated BBAI/SASGameRecord quoting,
  team/trade-item/deal and finalized-initial-state formatting. The moved helpers are
  read-only diagnostic transformations; no deal mutation moved into them.
- BBAI setup-time gameplay-trade chatter is suppressed only while initialization is being
  represented by authoritative finalized state. CvDeal::startTrade execution remains
  outside those log gates.
- Finalized BBAI team/deal rows run from gameStart after setup state exists; team and war
  log levels still govern detail. No successful setup state is used as a gameplay input.
- New NULL quoting semantics (`-`) and shared filename formatting affect only diagnostics.
- No recorder state/lifetime/RNG/gameplay regression survived.

Practical 6350 / KI#698-KI#700 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: b9586eef875d0dc353d8c7955d604c115656e60b
Title: fix SAS KI#698/KI#700 and inherited AdvCiv KI#699 Worker regressions

KI#698:
- countImprovedTiles now counts an improved BFC plot only when
  pPlot->getWorkingCity()==pCity.
- That is exactly WorkablePlotIter<WORKABLE_PLOT>'s eligibility test, which is the
  iterator used by AI_bestCityBuild. The small-city capacity threshold therefore no
  longer credits foreign/overlap plots that the same city Worker evaluator cannot select.

KI#700:
- Restoring `eFeature != NO_FEATURE && kOriginalBuildInfo.isFeatureRemove(eFeature)` fixes
  the reversed prerequisite polarity without touching legal feature-preserving builds.
- The retained pure-removal preference now applies only when the originally selected Build
  itself removes that feature, matching the intended chop-before-followup policy rather
  than turning Camps on Forest Deer/Fur into an unrelated pure chop.

KI#699:
- K-Mod's original AI_connectBonus used AI_plotValid. AdvC replaced many land-unit calls
  by cheaper area tests and accidentally wrote the self-true `kPlot.isArea(kPlot.getArea())`.
- Current repair restores the intended negative unit-area test: `!isArea(kPlot.getArea())`.
- Generic AI_plotValid additionally carries NoReveal/MoveAllTerrain/canBeEntered edge
  semantics. Current AI_connectBonus callers are ordinary land Worker/network-automation
  paths, and shipped Worker units do not have those exceptional movement/no-reveal tags;
  no current supported plot rejected solely by the cheaper repair was found.
- No independent repair-side defect survives.

Practical 6349 / KI#696 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit: 150b5d0b7ad5071c80640a9727cd26af49953708
Title: fix SAS victory-stage ordering regression (KI#696)

- AI_updateVictoryStageHash clears and rebuilds the stage hash once per pass.
- SAS Culture explicitly reads current Space/Conquest/Domination/Diplomacy stage bits as
  competing-victory context.
- AI_calculateDiplomacyVictoryStage does not read Culture stage; the dependency is one-way.
- Computing/storing Diplomacy before Culture therefore supplies the missing current-pass
  input without making Diplomacy depend on a not-yet-built Culture state or duplicating a
  strategy calculation. Remaining military/space ordering is unchanged.
- No reciprocal dependency or repair-side ordering regression survives.

PHYSICAL CONTINUATION STONE
---------------------------
WIP714 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practicals 6351/6350/6349 CLOSED CLEAN -> practical 6348 next,
then 6347 recorder/provenance if substantive and 6346 AI valuation repairs -> current-source
proof + ancestry + whole-album dedup mandatory before promotion -> freeze WIP715 at next
settled boundary.

======================================================================
POST-WIP714 CONTINUATION CHECKPOINT -- C031-WIP715
======================================================================
Authoritative continuation source: reconciled AdvCiv-SAS light source 2026-09-10
Primary queue: 169/169 COMPLETE
Confirmed post-primary findings through: F719 / KI#1040
Next finding number: F720 / provisional KI#1041 UNUSED
Gameplay/XML/Python source modified by this audit: NO

Practical 6348 / KI#692 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit: ab31e855ad60316ca16a1bdc269e12a1b27a3c20
Title: fix inherited AdvCiv brag identity regression (KI#692)

- AI_getGreeting clears the transient brag-unit handoff before each new greeting and writes
  it only when the selected greeting is UNIT_BRAG.
- The generic [OUR_BEST_UNIT] replacement consumes that exact identity once and clears it;
  otherwise it falls back to the ordinary best-unit selector used by non-brag text.
- Current source has no second C++ caller that precomputes/stores AI_getGreeting separately
  from the EXE diplomacy flow, and every shipped UNIT_BRAG text variant contains exactly
  one [OUR_BEST_UNIT] token.
- An opaque external caller could theoretically violate that one-shot rendering contract,
  but no current source producer is available; that remains API robustness, not a finding.

Practicals 6347 / 6345 / 6343 -- DIAGNOSTIC INFRASTRUCTURE DEDUP CLOSED
------------------------------------------------------------------------
- 6347 adds shared DLL provenance/timing helpers; 6345 refines SASGameRecord timing/debug
  diagnostics; 6343 extends setup provenance/initialization ordering.
- Their live source surfaces are superseded by and already covered in the later dedicated
  recorder/provenance audits (including ModName lifecycle, session timing, RNG purity,
  state-fingerprint purity and finalized setup ordering through practical 6443).
- No distinct simulation mutation in these older birth commits survives outside those
  already re-audited current implementations. Practical 6344 is documentation-only.

Practical 6346 / KI#690-KI#691 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: 8c083b1c53f6d0946ce994aca9983346c49e1a45
Title: fix inherited AI valuation defects (KI#690-KI#691)

KI#690:
- Each city now derives its own local iCityExtraPop from the unchanged caller-level input
  before applying that city's espionage visibility adjustment.
- The old intended one-city valuation is preserved; only the accidental mutation that
  leaked one city's adjustment into every later city is removed.

KI#691:
- AI_getPlotAirbaseValue still uses nearest friendly/foreign airbase distances for the
  distance terms, but the separate `iOtherCityCount * 50` term is genuinely a multiplicity
  value.
- Moving iOtherCityCount++ outside the nearest-distance update therefore counts every
  qualifying revealed foreign airbase exactly once rather than traversal-order records.
- No second state/visibility regression survives.

Practical 6342 / KI#689 + KI#693-KI#695 -- REPAIR-SIDE AUDIT CLOSED CLEAN
------------------------------------------------------------------------
Commit: 2f5263ebc081cd2f6d833ce375947aa8907ae4d0
Title: fix inherited BtS/AdvCiv Advanced Start defects (KI#689, KI#693-KI#695)

KI#689:
- AI_advancedStartPlaceExploreUnits selects and counts by eUnitAI = UNITAI_EXPLORE or
  UNITAI_EXPLORE_SEA and AI_bestAdvancedStartUnitAI filters concrete units through the
  same plot-specific Advanced Start legality/cost path.
- Passing that same eUnitAI into doAdvancedStartAction/initUnit makes the bought naval
  explorer satisfy the sea-explorer census that requested it; no role/legality mismatch
  remains.

KI#693:
- The new candidate precheck is algebraically identical to the authoritative purchase:
    newCost - oldRefund <= points  <=>  oldRefund + points >= newCost.
- Selection and doAdvancedStartAction are synchronous; no RNG or intervening game-state
  mutation can stale that exact cost/refund state between the two calls.
- The post-action improvement identity check prevents phantom improved-plot/population
  credit if the void action nevertheless rejects the request. Breaking then avoids an
  infinite retry of the same failed candidate.

KI#694:
- The new building budget guard
    iLastPointsTotal - currentPoints + iCost <= iCityPoints
  is the original projected-spending expression rearranged, with explicit invalid/current-
  point affordability checks.
- The change is control-flow only: one over-budget/invalid city-building candidate is
  skipped locally while the original ten bounded focus passes and later cheaper candidates
  remain eligible. doAdvancedStartAction immediately recomputes the same building cost and
  canConstruct state before purchase.

KI#695:
- AI_bestAdvancedStartUnitAI already rejects units whose concrete plot-specific Advanced
  Start cost/legality is invalid. The repaired outer loop recomputes that same cost and
  locally skips an invalid/unaffordable city-role candidate.
- Restoring all ten bounded BtS/K-Mod role passes therefore cannot create an unbounded buy
  loop; it only prevents one expensive CITY_DEFENSE/other candidate from suppressing later
  Worker/Reserve/Counter opportunities.
- No independent Advanced Start repair-side defect survives.

Practical 6341 / KI#687 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit: af7b2517f8285b3838067dcfad8ddf3e453a11ae
Title: fix inherited BtS deferred financial refresh; defer upgrade-cache repair (KI#687-KI#688)

- AI_doCheckFinancialTrouble runs every AI turn. While trouble persists and its false->true
  transition is still unserved, the repaired code deliberately leaves
  m_bWasFinancialTrouble=false when the ten-turn production-dirty cooldown blocks work,
  so the refresh is retried on later turns.
- The flag becomes true only after AI_makeProductionDirty actually runs; leaving financial
  trouble resets it to false normally.
- The only current writers of m_iTurnLastProductionDirty are this refresh and AI_nowHasTech.
  If a tech acquisition moves the cooldown window forward, it has just called the same
  empire-wide AI_makeProductionDirty operation, so the needed production reevaluation was
  not skipped merely because the financial-specific trigger is delayed.
- Both fields are initialized and serialized. No stale transition/save-load sibling survives.
- KI#688 remains the separately deferred performance-sensitive upgrade-cache issue; practical
  6341 intentionally does not claim to repair it.

PHYSICAL CONTINUATION STONE
---------------------------
WIP715 physical -> primary 169/169 COMPLETE -> F708-F719 CONFIRMED ->
F720/KI#1041 UNUSED -> practicals 6348/6346/6342/6341 CLOSED CLEAN ->
6347/6345/6343 diagnostic infrastructure DEDUP to later recorder/provenance audits ->
6344 docs-only -> open practical 6340/6339 event-valuation repairs -> trace exact event
producer/state timing, parent/additional-event valuation, random/conditional branches and
whole-album dedup -> promote F720 only with current producer + violated contract + concrete
consequence -> freeze WIP716 at next settled boundary.


===============================================================================
C031-WIP716 — F720 CONFIRMED
===============================================================================

F720 / provisional KI#1041 — practical 6340's KI#685 bounded trade-rounding
repair still misses the adjacent legal multiple when the requested amount is itself
an exact DIPLOMACY_VALUE_REMAINDER multiple but lies outside the requested interval

STATUS
------
CONFIRMED — current SAS repair-side regression / incomplete implementation of the
already-confirmed inherited F362 / KI#685 root. The repair fixed AdvCiv's destructive
fallback state mutation, but its exact-multiple special case can collapse the two
candidate multiples into the same out-of-bounds original value and therefore fail to
consider a legal adjacent multiple.

ROOT
----
The repaired helper still documents this contract:

    Set iTradeVal to the nearest multiple of DIPLOMACY_VALUE_REMAINDER
    in [iLower,iUpper]. No change if no such multiple exists.

Current practical-6340 code first rounds down and then defines:

    iRoundedUp = (iRoundedDown == iOriginal ?
        iOriginal : iRoundedDown + remainder);

Therefore, whenever iOriginal is already an exact multiple, both candidate identities
collapse to iOriginal:

    iRoundedDown == iOriginal
    iRoundedUp   == iOriginal

That is correct only while the exact original lies inside the requested interval. If the
bounds exclude the original but contain the neighboring multiple in the direction of the
interval, the helper never tests that legal multiple despite its documented contract.

CURRENT LIVE CALLER FIXTURE
---------------------------
Current AI_balanceDeal has two live human-facing gold-balancing callers. In the first
non-generous pass it computes:

    iGoldData = (iTheyReceive - iWeReceive) * 100 / iGoldValuePercent;

Current AI_goldTradeValuePercent() is exactly 200, and current
DIPLOMACY_VALUE_REMAINDER is 5.

Take an ordinary non-generous counterproposal state where:

    trade-value gap       = 200
    requested iGoldData   = 100
    human giver treasury  = 97

AI_maxGoldTrade() returns a human player's raw treasury, so:

    iMaxGold = 97

Because a human participates, the caller invokes:

    AI_roundTradeValBounds(iGoldData, false, MIN_INT, iMaxGold);

The requested interval is therefore [MIN_INT,97]. The nearest legal multiple of 5 below
100 is 95.

Current repaired helper instead computes:

    original    = 100
    roundedDown = 100
    roundedUp   = 100    // exact-multiple special case

Both candidates violate upper=97, so neither assignment fires and iGoldData remains 100.

CONCRETE CONSEQUENCE
--------------------
Immediately afterward the first gold pass requires:

    iMaxGold >= iGoldData

Current state:

    97 >= 100  -> false

Thus the balancing pass skips the perfectly legal 95-gold item and falls through to
non-gold item selection. This is a real current control-flow difference, not merely a
helper-comment violation or synthetic API input.

The final gold pass contains the same helper call. Its later min(iGoldData,iMaxGold)
can mask the failure in some paths, but for a human raw maximum it can also insert a
non-display-multiple amount such as 97 instead of selecting the intended bounded multiple
95. F720 does not depend on that later symptom; the first-pass missed legal item is already
sufficient and deterministic.

PRODUCER / CURRENT CONTRACT
---------------------------
AI_counterPropose routes the side that needs to give more value through the non-generous
AI_balanceDeal branch. Current K-Mod/AdvC comments explicitly support AI-AI deals and human
auto-counterproposals, so the human-giver/raw-treasury boundary is an ordinary supported
negotiation state rather than an external-only API construction.

For AI givers, AI_maxGoldTrade rounds the maximum to the diplomacy remainder before return.
The same exact-multiple omission can still make the first pass skip a legal lower maximum
(e.g. requested 100, max 95), although later deal composition may mask it. The human 97
fixture is the clearest direct witness.

INTRODUCTION / REPAIR HISTORY
-----------------------------
F362 / KI#685 proved the inherited AdvCiv defect: the old helper mutated iTradeVal to the
lower multiple before validating the preferred upper candidate, so a generous value 12
with bounds [12,14] could return 10 below its lower bound.

Practical 6340 / cc6bdfad387f5596e46be12af251f96d4cf9df04 repaired that exact root by
computing separate lower/upper candidate variables and only assigning an in-bounds one.
Its exact-multiple special case intentionally made the upper candidate equal the original
when roundedDown==original. That implementation fixes F362's non-multiple failure, but
creates/leaves the new neighboring-multiple omission when the exact original itself is
outside the interval.

Base AdvCiv 1.14 retains the original F362 implementation; this exact F720 behavior belongs
to the SAS repair, not to upstream AdvCiv.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album search finds no prior owner for the exact-multiple/out-of-bounds neighboring
candidate case.

- F362 / KI#685 remains the inherited AdvCiv root and its 12 -> bounds [12,14] destructive
  fallback failure.
- F720 / KI#1041 is the current SAS repair-side regression/incompleteness at the separate
  exact-multiple boundary after practical 6340.

REPAIR BOUNDARY
---------------
Keep practical 6340's key invariant: never mutate the caller until a valid bounded
candidate is chosen.

Additionally, treat an exact original as a candidate only when it is actually inside the
requested interval. If an exact multiple lies above iUpper or below iLower, advance to the
adjacent multiple toward the interval (or derive the nearest lower/upper multiple from the
bounds) before applying the requested preference order.

For example, with original=100, remainder=5 and upper=97, the lower candidate must become
95 rather than remaining 100. If original=100 already lies inside the interval, retaining
100 remains correct and no unnecessary adjacent rounding should occur.

No gameplay/XML/Python source has been modified by this audit.


PHYSICAL CONTINUATION STONE
---------------------------
WIP716 physical -> primary 169/169 COMPLETE -> F708-F720 CONFIRMED ->
F721 / provisional KI#1042 UNUSED -> practical 6340 KI#685 repair-side regression
CONFIRMED as F720 -> finish practical 6340 sibling KI#686 Conquest-precedence repair ->
continue practical 6339 inherited event-valuation repairs KI#681-KI#683 (positive/negative
tech cap, plot-extra yield timing, overlapping promotion valuation) -> current-source proof
+ whole-album dedup mandatory -> freeze WIP717 at next settled boundary.


===============================================================================
C031-WIP717 — F721 CONFIRMED
===============================================================================

F721 / provisional KI#1042 — practical 6339's KI#681 PlotExtraYield valuation repair
values the configured absolute map-extra yield as though it were an additive delta, so a
later random event can receive positive AI value for a yield increase that runtime does
not actually produce

STATUS
------
CONFIRMED — current SAS repair-side regression / incomplete implementation of the
already-confirmed inherited F358 / KI#681 event-valuation root. Practical 6339 fixes the
old worked-plot ordering omission by moving PlotExtraYield into aiYields before its
consumers, but still assumes the EventInfo number is an additive gain. Runtime stores it
as an absolute per-plot extra-yield value.

ROOT
----
Current practical-6339 AI_eventValue now does, before city/global yield valuation:

    aiYields.add(eLoopYield, kEvent.getPlotExtraYield(eLoopYield));

for a selected plot with a working city (and uses a direct positive estimate for an
unworked plot). Thus the configured EventInfo value is always treated as a fresh delta.

Runtime CvPlot::applyEvent instead calls:

    GC.getMap().setPlotExtraYield(*this, eLoopYield, iChange);

and CvMap::setPlotExtraYield is a true setter:

    m_aeiPlotExtraYield.set(kPlot.plotNum(), eYield, iChange);

It does not add the new value to the old one. The actual gameplay delta is therefore:

    configured absolute value - current stored PlotExtraYield

not simply the configured value.

Neither CvPlot::canTrigger nor CvPlot::canApplyEvent tests current PlotExtraYield, so a
later distinct trigger may legally select a plot already modified by an earlier event.
Non-recurring status only blocks that same EventTrigger from firing again; it does not
make all PlotExtraYield events mutually exclusive.

CURRENT SHIPPED THREE-TRIGGER FIXTURE
-------------------------------------
A completely current XML/Python sequence can create the mismatch on an ordinary owned
plot without WorldBuilder or unsupported API input:

1. EVENTTRIGGER_TRUFFLES can select an owned TERRAIN_GRASS plot with no bonus. Its
   EVENT_TRUFFLES_1 stores:

       YIELD_FOOD     = +1
       YIELD_COMMERCE = +1

   The trigger has no feature or improvement requirement, so an owned grassland Forest
   with no bonus is valid. The event does not remove that Forest.

2. EVENTTRIGGER_ANTELOPE can later select that same Forest once Hunting is known. The
   trigger requires FEATURE_FOREST, no improvement and no route; its Python can-trigger
   additionally requires that Deer can be placed there. EVENT_ANTELOPE_2 adds Deer and a
   Road, and its shipped doAntelope2 Python callback directly creates IMPROVEMENT_CAMP.

3. EVENTTRIGGER_GREAT_BEAST is then valid on that same Camp once Hunting + Polytheism are
   known (and before its Broader Education obsolescence). It offers three AI choices.
   EVENT_GREAT_BEAST_1 configures:

       PlotExtraYield YIELD_FOOD = +1

At this point the stored food extra from Truffles is already +1. Applying Great Beast 1
therefore performs:

    old stored food extra = +1
    set absolute value     = +1
    actual food delta      =  0

Practical 6339 nevertheless feeds +1 food into the event-value accumulator. On a worked
Camp this reaches the repaired aiYields city valuation; on an unworked Camp the direct
fallback estimate is likewise positive despite zero runtime delta.

CONCRETE CONSEQUENCE
--------------------
AI_chooseEvent evaluates every legal Great Beast option with AI_eventValue and picks the
strict maximum. Great Beast 1 can therefore receive positive permanent-food value for a
change that will produce no food at all, changing its ranking against Great Beast 2
(-15 gold, +1 population) and Great Beast 3 (its separate Python happiness effect).

This is not merely an inaccurate tooltip or dead helper. It feeds the current AI's random-
event decision directly.

KI#682 / KI#683 SIBLING RECHECK
-------------------------------
The other two practical-6339 repairs remain clean under the current-source re-audit:

- KI#683's executable-tech-progress cap matches CvTeam::changeResearchProgressPercent
  algebra for both positive and negative progress. Current EVENT_SETBACK_1 supplies a
  shipped negative-TechPercent control.
- KI#682 mirrors runtime UnitClassPromotion application to existing matching units plus
  the persistent future-unit class reward, while suppressing the same promotion when the
  same unit already receives it through UnitCombatPromotion. Current shipped EventInfos
  contain no simultaneous UnitClass+UnitCombat promotion overlap, so the duplicate path
  is defensive rather than a current disagreement.

INTRODUCTION / ANCESTRY
-----------------------
F358 / KI#681 proved the inherited BtS/K-Mod/AdvC ordering defect: worked PlotExtraYield
was appended to aiYields only after aiYields had already been consumed, so it received
zero value.

Practical 6339 / c269198c01d0980512f7f990c35f1931e6f52b78 fixes that ordering by moving
the configured PlotExtraYield earlier. Runtime set semantics are older/inherited and
unchanged. The repair therefore exposes a separate existing-state boundary that the old
worked-plot bug had accidentally masked in this fixture: the repaired evaluator now values
the absolute XML number as a new gain even when runtime overwrites the same stored value.

Base AdvCiv 1.14 retains F358's old late-add ordering; this exact worked-plot F721 behavior
belongs to the SAS repair.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album searches for PlotExtraYield, setPlotExtraYield, Horticulture, Parrots,
Truffles and Great Beast find no prior numbered owner for absolute-set versus additive-
delta event valuation.

- F358 / KI#681 remains the inherited ordering root (worked PlotExtraYield valued at zero).
- F721 / KI#1042 is the practical-6339 repair-side incompleteness: the moved value is now
  consumed, but it is not converted from configured absolute state to actual runtime delta.

REPAIR BOUNDARY
---------------
Keep practical 6339's corrected ordering, but value the actual change that CvMap will
produce. For each affected yield on pPlot, derive for example:

    iOldExtra = GC.getMap().getPlotExtraYield(*pPlot, eYield)
    iNewExtra = kEvent.getPlotExtraYield(eYield)
    iDelta    = iNewExtra - iOldExtra

and feed iDelta, not iNewExtra, into the worked/unworked event valuation. This preserves
normal first-time +1/+2/-1 events while making a repeated absolute +1 over existing +1
worth zero and correctly valuing replacements of a different magnitude/sign.

No gameplay/XML/Python source has been modified by this audit.


PHYSICAL CONTINUATION STONE
---------------------------
WIP717 physical -> primary 169/169 COMPLETE -> F708-F721 CONFIRMED ->
F722 / provisional KI#1043 UNUSED -> practical 6339 KI#681 repair-side regression
CONFIRMED as F721 -> KI#682/KI#683 sibling repairs CLOSED CLEAN -> classify practical
6338 downward from local commit-diff archive -> open first substantive gameplay/AI repair ->
current-source proof + whole-album dedup mandatory -> freeze WIP718 at next settled boundary.


===============================================================================
C031-WIP718 — F722 CONFIRMED
===============================================================================

F722 / provisional KI#1043 — practical 6338's KI#676 BonusRevealed valuation repair
prices the full resource reveal even when the team already reveals that bonus normally,
so Man Named Jed can pay gold for a force-reveal with no current information benefit

STATUS
------
CONFIRMED — current SAS repair-side regression / incomplete implementation of inherited
F353 / KI#676. Practical 6338 correctly switches valuation from the unrelated BonusType
field to BonusRevealed, but it values that field unconditionally rather than the reveal
state change that runtime will actually produce.

ROOT
----
Current AI_eventValue contains the practical-6338 repair:

    if (kEvent.getBonusRevealed() != NO_BONUS)
    {
        int const iRevealedBonusValue = AI_bonusVal(
                (BonusTypes)kEvent.getBonusRevealed(), 0, true);
        iValue += (iRevealedBonusValue * 10 * iGameSpeedPercent) / 100;
    }

Runtime CvPlayer::applyEvent instead performs only:

    GET_TEAM(getTeam()).setForceRevealedBonus(eBonus, true);

The practical gameplay benefit of that mutation depends on whether the team can already
reveal the bonus through its normal TechReveal. If the bonus is already naturally visible,
setting the force-revealed bit does not newly reveal any current map information.

CURRENT SHIPPED FIXTURE — A MAN NAMED JED AFTER COMBUSTION
----------------------------------------------------------
Current BONUS_OIL has:

    TechReveal    = TECH_COMBUSTION
    TechCityTrade = TECH_COMBUSTION

Current EVENTTRIGGER_MAN_NAMED_JED:

- has no prerequisite-tech gate;
- has no obsolete-tech gate;
- is non-recurring only with respect to its own trigger;
- locates its required Oil plot through CvPlot::canTrigger's getBonusType(NO_TEAM) path,
  i.e. the actual resource identity rather than the acting team's current reveal state;
- remains a supported trigger after the acting team has already learned Combustion.

Its current two choices are especially clean:

    EVENT_MAN_NAMED_JED_1:
        no material EventInfo payload

    EVENT_MAN_NAMED_JED_2:
        iGold = -10
        BonusRevealed = BONUS_OIL

Take a team that already knows Combustion and later receives this trigger (for example an
Explorer/Scout on a qualifying Oil plot, as the shipped trigger requires). Oil is already
naturally revealed. Applying option 2 changes the force-reveal bookkeeping bit but reveals
no new current resource information; technologies are not normally lost, so there is no
ordinary future-reveal benefit either.

The repaired AI valuation nevertheless grants the full Oil AI_bonusVal reveal term.
AI_bonusVal(..., bAssumeEnabled=true) intentionally suppresses tech-readiness discounts; it
does not test whether Oil is already revealed. In ordinary states where Oil has positive
strategic value, this positive term can offset the -10 gold and change the choice relative
to the free no-effect option.

CONCRETE CONSEQUENCE
--------------------
AI_chooseEvent calls AI_eventValue for both Jed choices and selects the strict maximum.
The repair therefore changes the probability/ranking of choosing EVENT_MAN_NAMED_JED_2
in a state where its advertised reveal is already operationally redundant. With equal or
sufficiently close event-AI random components, the bogus Oil reveal value can make the AI
pay 10 gold for no current reveal effect instead of choosing option 1.

This is a live AI decision input, not merely stale help or logging.

KI#677-KI#680 SIBLING RECHECK
-----------------------------
The remaining practical-6338 repairs do not yield another current repair-side root:

- KI#677/KI#678 now retain BuildingClass identity and count active affected instances.
  Runtime stores building-specific modifiers per existing city/player and can also create
  latent value for buildings constructed later; omitting speculative future construction
  is a heuristic-quality limitation rather than a violated exact current-effect contract.
- KI#679 removes only the duplicated early game-speed scaling and preserves the shared
  final scaling exactly once.
- KI#680's eligible own/foreign religion-city counts match applyEvent's religion-absence
  and MaxNumReligions predicates before runtime randomly chooses at most the configured
  number. Valuing the executable count rather than the random identities is consistent.

INTRODUCTION / ANCESTRY
-----------------------
F353 / KI#676 proved the inherited BtS bug: AI_eventValue tested BonusRevealed but valued
the unrelated BonusType field, so the shipped Oil-reveal option could receive zero reveal
value.

Practical 6338 / 8027e4713968747d7cf0ae9cf121ac4d4a3a3722 correctly changes the valued
resource identity to BonusRevealed. The runtime force-reveal semantics and the Jed trigger's
post-Combustion eligibility are older/inherited. Thus F722 is the current SAS repair-side
state omission, not a second inherited field-identity bug.

DEDUP / ROOT SEPARATION
-----------------------
Whole-album search finds no prior numbered owner for BonusRevealed being already naturally
revealed at AI-event-choice time.

- F353 / KI#676 remains the inherited wrong-field root.
- F722 / KI#1043 is the practical-6338 repair-side incompleteness: it values the correct
  resource but not the actual reveal-state delta.

REPAIR BOUNDARY
---------------
Retain practical 6338's correct BonusRevealed identity, but add reveal-state gating. A
BonusRevealed reward should receive reveal value only when it changes what the acting team
can currently reveal (or, if intentionally valuing a durable force-reveal independently,
only for a concrete future state the game can actually reach).

For current immutable technology ownership, a straightforward guard against an already
normally revealed bonus is sufficient to prevent the Jed-after-Combustion overvaluation.

No gameplay/XML/Python source has been modified by this audit.


PHYSICAL CONTINUATION STONE
---------------------------
WIP718 physical -> primary 169/169 COMPLETE -> F708-F722 CONFIRMED ->
F723 / provisional KI#1044 UNUSED -> practical 6338 KI#676 repair-side regression
CONFIRMED as F722 -> KI#677-KI#680 sibling repairs CLOSED CLEAN -> open practical 6337
KI#673-KI#674 gold-memory / rejected-war-trade repairs -> current-source proof + whole-album
dedup mandatory -> freeze WIP719 at next settled boundary.


===============================================================================
C031-WIP719 — PRACTICAL 6337 REPAIR-SIDE AUDIT CLOSED CLEAN
===============================================================================

F723 / provisional KI#1044 remains UNUSED.

Practical 6337 / KI#673-KI#674 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit: 325884264cc80ed986a99f00cb6fbc23eef0b591
Title: fix inherited AdvCiv KI#673-KI#674 gold memory and rejected war trades

KI#673:
- AI_doCounter already owns one per-known-rival counter/decay pass. Moving the cash-trade
  exponential decay before the unrelated Bonus-Trade attitude-divisor/value exits makes it
  run once for that rival independently of whether resource appreciation is enabled or
  receives a positive increment.
- The repair uses the same rDecayFactor as the surrounding peacetime memory decay and
  applies only old->new delta through AI_changeGoldTradedTo. No second writer/extra decay
  loop is introduced by the move.
- No repair-side state or orientation mismatch survives.

KI#674:
- The repaired city-for-war fallback correctly implements the package only when
  AI_counterPropose returns true, and otherwise falls through to the ordinary tech/gold
  proposal path.
- Important repair-side lifetime check: the candidate `weGive` and `hirelingGives` lists
  are declared inside the pBestCity block. AI_counterPropose may mutate those lists while
  trying to balance the rejected package, but on false the block exits and both temporary
  lists are destroyed.
- The later ordinary proposal path constructs fresh CLinkList<TradeData> objects from the
  selected technologies/gold/war target, so no rejected city or partial balancing item can
  leak into the fallback deal.
- No independent KI#674 repair-side root survives.

No gameplay/XML/Python source has been modified by this audit.


PHYSICAL CONTINUATION STONE
---------------------------
WIP719 physical -> primary 169/169 COMPLETE -> F708-F722 CONFIRMED ->
F723 / provisional KI#1044 UNUSED -> practical 6337 KI#673-KI#674 CLOSED CLEAN -> open
practical 6336 KI#669-KI#670 attacker demand / bounded-path repairs -> current-source proof
+ whole-album dedup mandatory -> freeze WIP720 at next settled boundary.

===============================================================================
C031-WIP720 - POST-PRIMARY REPAIR-SIDE AUDIT HANDOFF
===============================================================================
Physical continuation from C031-WIP719.

Authoritative state at this stone:
- Primary queue: 169/169 COMPLETE.
- Post-primary confirmed findings: F708-F722.
- Next finding root: F723 / provisional KI#1044 UNUSED.
- Practical 6336 (KI#669-KI#670): REPAIR-SIDE AUDIT CLOSED CLEAN.
- Gameplay/XML/Python source modified by this audit: NO.

Practical 6336 - KI#669 attacker-demand target-era sentinel
-------------------------------------------------------------
Current CvPlayerAI::AI_neededCityAttackers accumulates rMeanTargetEra and increments
iEnemies in the same qualifying branch: a known potential enemy contributes only when
our team has a non-NO_WARPLAN war plan against that enemy's team. iEnemies is therefore
the exact contributor count for the mean, not a broader presence census.

The repaired fallback now tests iEnemies == 0 instead of rMeanTargetEra == 0. This is
important because ERA_ANCIENT is legitimately numeric zero. One or more Ancient targets
can consequently yield a valid mean of zero without being mistaken for no targets, while
the Barbarian-era fallback is still used exactly when no civilization target contributed.
The fallback sets iEnemies=1 before division, so no zero divisor is introduced. The later
modulus retains a positive base term even at mean era zero.

No repair-side sibling defect survives current-source review.

Practical 6336 - KI#670 bounded joiner path failure
---------------------------------------------------
CvSelectionGroup::generatePath explicitly initializes *piPathTurns to MAX_INT and replaces
it with getPathTurns() only when path generation succeeds. The inherited caller then may
add one turn when the whole group cannot move. Before KI#670, a failed bounded path could
therefore increment MAX_INT and overflow before the <= iMaxPathTurns proximity test.

The repair checks generatePath's Boolean result and continues immediately on failure. This
removes exactly the invalid-output case while preserving successful-path semantics. When
a successful path already equals the requested maximum and !canAllMove() adds one turn,
the existing <= iMaxPathTurns condition correctly excludes the group as no longer imminent
enough. There is no new valid-path loss and no stale-output dependence.

Disposition:
- KI#669 repair CLEAN.
- KI#670 repair CLEAN.
- F723 / provisional KI#1044 remains UNUSED.

Exact continuation:
C031-WIP720 physical -> F723/KI#1044 UNUSED -> open practical 6335
(KI#666-KI#668) -> audit shared-neutral-resource counting, first-pushover-war
state capture and teammate missionary-area valuation against current consumers ->
whole-album dedup mandatory -> freeze WIP721 at next settled boundary.
===============================================================================

===============================================================================
C031-WIP721 - POST-PRIMARY REPAIR-SIDE AUDIT HANDOFF
===============================================================================
Physical continuation from C031-WIP720.

Authoritative state at this stone:
- Primary queue: 169/169 COMPLETE.
- Post-primary confirmed findings: F708-F722.
- Next finding root: F723 / provisional KI#1044 UNUSED.
- Practical 6335 (KI#666-KI#668): REPAIR-SIDE AUDIT CLOSED CLEAN.
- Gameplay/XML/Python source modified by this audit: NO.

Practical 6335 - KI#666 shared-neutral-resource multiplicity
----------------------------------------------------------
The repaired AI_countOwnedBonuses keeps owned city-radius resources on the inherited
unique working-city rule (plot owner == player and getWorkingCity() == that city), while
neutral candidates are counted through a player-scope plot bitmap. A physical neutral
plot in overlapping city radii can therefore contribute at most once. The later whole-map
pass counts only player-owned plots outside every city radius, so a neutral plot counted
through the bitmap cannot be counted again there. getBonusType(team) retains the existing
resource-reveal gate.

No new duplicate or omission is introduced by the repair.

Practical 6335 - KI#667 first pushover-war state
------------------------------------------------
The loop is team-based. bFoundPushoverWarInArea is set only after a qualifying war-plan
target is confirmed pushover and that target team's selected capital lies in the queried
area. A second such team returns true as the explicit multiple-weak-wars exception intended.
One qualifying pushover war still returns false. The older multi-member-team capital
representative simplification is inherited policy and not introduced by this state repair.

Practical 6335 - KI#668 area missionary team scope
--------------------------------------------------
The repaired area branch sums disjoint per-player city counts, religion membership and
spread-unit/in-production state across living members of our team. The production caller is
CvCityAI's missionary choice for the same area. CvUnit::canSpread genuinely permits a
Missionary to spread into a teammate city: same-team cities bypass the foreign-state-
religion restriction and ordinary team territory entry is legal. Teammate cities are thus
real consumers of the produced Missionary, validating the repaired team scope rather than
merely matching the global branch cosmetically.

Disposition:
- KI#666 repair CLEAN.
- KI#667 repair CLEAN.
- KI#668 repair CLEAN.
- F723 / provisional KI#1044 remains UNUSED.

Exact continuation:
C031-WIP721 physical -> F723/KI#1044 UNUSED -> open practical 6334
(KI#662-KI#665) -> audit naval safety/role/value and exploring-Work-Boat transform
repairs against exact current role, cargo, area, reachability and target contracts ->
whole-album dedup mandatory -> freeze WIP722 at next settled boundary.
===============================================================================

===============================================================================
C031-WIP722 - POST-PRIMARY REPAIR-SIDE AUDIT HANDOFF
===============================================================================
Physical continuation from C031-WIP721.

Authoritative state at this stone:
- Primary queue: 169/169 COMPLETE.
- Post-primary confirmed findings: F708-F722.
- Next finding root: F723 / provisional KI#1044 UNUSED.
- Practical 6334 (KI#662-KI#665): REPAIR-SIDE AUDIT CLOSED CLEAN.
- Gameplay/XML/Python source modified by this audit: NO.

Practical 6334 - KI#662 outgoing-nuke interception valuation
------------------------------------------------------------
The repair excludes exactly the owner's own team from K-Mod's KNOWN_TO interception
iteration. In the generic/non-war valuation, known foreign teams remain part of the
long-run defensive environment; in the war-context branch, the existing war-plan test
continues to limit the foreign teams used. The owner's defensive interception can no
longer devalue its own outgoing weapon, and no later scoring contract is changed.

Practical 6334 - KI#663 special-cargo carrier role
--------------------------------------------------
The repaired AI_unitValue tests the requested eUnitAI directly against the SpecialUnit's
carrier-role map instead of looping over every UnitAI and accepting any match. Cargo
capacity, special-cargo identity and later role-specific scoring remain unchanged. The
repair therefore restores the BtS/K-Mod requested-role contract without broadening or
narrowing unrelated cargo legality.

Practical 6334 - KI#664 sea-explorer area-local training subtraction
--------------------------------------------------------------------
AI_totalWaterAreaUnitAIs adds this player's queued sea explorers only from cities whose
waterArea() equals the queried water area. KI#664 now subtracts queued explorers using
that exact same city->waterArea()==area rule. Thus the repair removes the old global-vs-
local mismatch and does not introduce a second area-assignment convention, including for
cities adjacent to more than one water body.

Practical 6334 - KI#665 exploring-Work-Boat transform legality
--------------------------------------------------------------
Changing AI_countUnimprovedBonuses look-ahead from 5 to 0 makes the final immediate
UNITAI_WORKER_SEA role transition require a currently owned target. The helper's normal
AI_isUnimprovedBonus path check and canBuild checks then require that target to be reachable
from the current unit and executable with current build/technology state. A conservative
BorderFinder/team-shortcut limitation can make the explorer keep exploring, but it cannot
recreate the broken convert-for-future/unreachable-resource state that KI#665 repairs.

Disposition:
- KI#662-KI#665 repairs CLEAN.
- F723 / provisional KI#1044 remains UNUSED.

Exact continuation:
C031-WIP722 physical -> F723/KI#1044 UNUSED -> open practical 6333 deal-
balancing repairs -> audit list mutation, valuation bounds, offer orientation and
fallback ordering against current AI_counterPropose/AI_balanceDeal consumers ->
whole-album dedup mandatory -> freeze WIP723 at next settled boundary.
===============================================================================

===============================================================================
C031-WIP723 - POST-PRIMARY REPAIR-SIDE AUDIT HANDOFF
===============================================================================
Physical continuation from C031-WIP722.

Authoritative state at this stone:
- Primary queue: 169/169 COMPLETE.
- Post-primary confirmed findings: F708-F722.
- Next finding root: F723 / provisional KI#1044 UNUSED.
- Practical 6333 (KI#658-KI#660): REPAIR-SIDE AUDIT CLOSED CLEAN.
- Gameplay/XML/Python source modified by this audit: NO.

Practical 6333 - KI#658 rejected non-surplus candidate
------------------------------------------------------
Initializing best_it to end() gives an explicit no-valid-candidate state. The short-circuit
selection condition cannot dereference end() before assigning the first candidate that
survives the current no-overshoot filter. If no candidate survives, the loop breaks before
append/erase. The inherited rejected-first-resource leak is therefore removed without a
new iterator lifetime or mutation defect.

Practical 6333 - KI#659 Rise & Fall leniency on non-surplus resources
--------------------------------------------------------------------
The repair applies rLeniency to the value added to iWeReceive, restoring the discounted
accumulator units required by advc.705 and matching the ordinary-item/gold/GPT additions.
A possible further change -- testing item admissibility against discounted rather than raw
item value -- was challenged and rejected as a repair-side finding: the ordinary item path
uses the same raw-value no-overshoot/admission policy and only discounts accepted items in
the accumulator. Altering that policy would broaden trade selection rather than narrowly
complete F336/KI#659's missing accumulator conversion.

Practical 6333 - KI#660 early gold/GPT leniency
-----------------------------------------------
The repaired initial gold and negative-income early-GPT additions now use the same
rLeniency expressions already used by the corresponding later monetary additions. Actual
trade-list amounts remain unchanged; only the discounted-side internal accumulator is
converted into the same value units as the rest of the Rise & Fall retry.

Disposition:
- KI#658-KI#660 repairs CLEAN.
- F723 / provisional KI#1044 remains UNUSED.

Exact continuation:
C031-WIP723 physical -> F723/KI#1044 UNUSED -> open practical 6332
(KI#655-KI#657) -> audit folded-deal age preservation, obsolete-unit strategic-resource
classification and same-team bounded-help cancellation exemption against current deal
creation/cancellation consumers -> whole-album dedup mandatory -> freeze WIP724 at next
settled boundary.
===============================================================================

================================================================================
C031-WIP724 POST-PRIMARY REPAIR-SIDE CHECKPOINT
================================================================================

Physical continuation from C031-WIP723.

Authoritative state:
- Primary queue: 169/169 COMPLETE.
- Confirmed post-primary findings through F722/KI#1043.
- F723/KI#1044 remains UNUSED.
- No gameplay/XML/Python source modified.

PRACTICAL 6332 / KI#655-KI#657: REPAIR-CLEAN

KI#655 folded-deal age preservation:
- The two mature/cancelable deals are killed and replaced by one combined deal.
- The repair preserves max(d1.initialTurn, d2.initialTurn), i.e. the younger constituent age, so the folded deal cannot become older than one of its obligations and remains cancelable because both inputs were already cancelable.
- Cross-era SASGameRecord seam checked explicitly: CvDeal::addTradeItems logs a DIPLO_DEAL action before AI_foldDeals restores the historical initialGameTurn, but that action row records the action turn, deal ID, players and lists, not initialGameTurn/age.
- The canonical deal state fingerprint does include initialGameTurn, but AI_foldDeals restores it immediately after implementAndReturnDeal returns and before any later checkpoint. No persistent recorder mismatch survives.

KI#656 obsolete-unit strategic-resource classification:
- allUpgradesAvailable obsolete-unit filtering now precedes every unit resource-requirement flag mutation.
- Buildings remain an independent valid strategic-use source.
- No second unit-path mutation occurs before the skip.

KI#657 same-team bounded-help cancellation threshold:
- Threshold is now computed before both one-sided acceptance and bilateral cancellation paths.
- scaled::hash variation is deterministic and side-effect-free; moving the calculation earlier adds no RNG/state mutation.
- Same-team bilateral cancellation consumes the same threshold units as the one-sided bounded-help branch.

Result:
- Practical 6332 closes cleanly.
- F723/KI#1044 remains UNUSED.

NEXT EXACT CURSOR
C031-WIP724 physical -> F723/KI#1044 UNUSED -> practical 6331 (KI#653/KI#654 tech logic) -> inspect exact repair diff + current producer/consumer lifecycle -> whole-album dedup mandatory -> freeze WIP725 at next settled boundary.

================================================================================
C031-WIP725 POST-PRIMARY REPAIR-SIDE CHECKPOINT
================================================================================

Physical continuation from C031-WIP724.

Authoritative state:
- Primary queue: 169/169 COMPLETE.
- Confirmed post-primary findings through F722/KI#1043.
- F723/KI#1044 remains UNUSED.
- No gameplay/XML/Python source modified.

PRACTICAL 6331 / KI#653-KI#654: REPAIR-CLEAN

KI#653 Barbarian no-innovation repair:
- AI_bestTech now applies CvPlayer::canResearch to depth-0 Barbarian candidates, the exact candidate layer that can become the next ordinary research target.
- Deeper hypothetical candidates may influence path valuation, but cannot themselves enter research until they later reach depth 0 and pass the same anti-innovation rule.
- Ordinary Python-selected Barbarian research is separately revalidated before pushResearch.
- Current shipped Python has neutral defaults (canResearch override false; AI_chooseTech NO_TECH) and no registered AI_chooseTech override elsewhere.
- AI_chooseFreeTech does not separately validate a hypothetical Python-selected Barbarian free tech, but current Python always returns NO_TECH, so the live path falls through to repaired AI_bestTech(1,true). Extension-hardening only.

KI#654 live-market population repair:
- TeamIter<CIV_ALIVE> explicitly excludes Barbarians and dead teams; it is the intended living-civilization cache population.
- Seller team counts once and any second alive holder is therefore a live non-Barbarian civilization team, fixing F331's eliminated-historical-team trigger.
- CIV_ALIVE includes living minor civs, so a stronger "actual plausible competing seller" predicate could be narrower. No current shipped minor-tech-holder producer was found in the light-source data, and F331's proven root/repair direction required at minimum excluding dead teams. Do not promote this extension question.

Result:
- Practical 6331 closes cleanly.
- F723/KI#1044 remains UNUSED.

NEXT EXACT CURSOR
C031-WIP725 physical -> F723/KI#1044 UNUSED -> classify/open practical 6330 -> current-source producer/consumer repair audit -> whole-album dedup mandatory -> freeze WIP726 at next settled boundary.

================================================================================
C031-WIP726 POST-PRIMARY REPAIR-SIDE CHECKPOINT
================================================================================

Physical continuation from C031-WIP725.

Authoritative state:
- Primary queue: 169/169 COMPLETE.
- Confirmed post-primary findings through F722/KI#1043.
- F723/KI#1044 remains UNUSED.
- No gameplay/XML/Python source modified.

PRACTICAL 6330 / KI#646, KI#648-KI#650: REPAIR-CLEAN

KI#646 civic-switch paid-anarchy safeguards:
- iAnarchyLength starts at 0 and is written only for an accepted hypothetical bundle.
- Accepted later changes recompute it from the retained bundle; it is not merely a stale historical maximum.
- Post-loop research-wait and treasury guards keyed on iAnarchyLength>0 therefore describe the accumulated paid-anarchy revolution even when the final recheck pass accepts nothing.

KI#648 CivicPercentAnger active-state sign:
- iAngryCitizenChange is a directional hypothetical welfare change (adopt/drop).
- Multiplying by iS converts it back to absolute candidate value once, preserving positive value for anger prevented by an already-active civic.

KI#649 ordinary vs specialist commerce:
- Ordinary percentage/capital commerce again uses AI_averageCommerceMultiplier structural normalization and AI_commerceWeight.
- Specialist-extra-commerce alone retains the SAS toned specialist multiplier/weight.
- Current shipped civic XML has no negative ordinary CommerceModifier/CapitalCommerceModifier entry that would turn the inherited positive-only consumer into a new repair-side current root.

KI#650 war-weariness candidate modifier:
- Current code follows F327's explicit repair direction: candidate kCivic modifier, iS adopt/drop direction, and a nonzero iHappy guard before AI_getHappinessWeight.
- No second current getter reintroduces the active civic's aggregate modifier in this block.

Result:
- Practical 6330 closes cleanly.
- F723/KI#1044 remains UNUSED.

NEXT EXACT CURSOR
C031-WIP726 physical -> F723/KI#1044 UNUSED -> classify/open practical 6329 -> current producer/consumer repair audit -> whole-album dedup mandatory -> freeze WIP727 at next settled boundary.

================================================================================
C031-WIP727 POST-PRIMARY REPAIR-SIDE CHECKPOINT
================================================================================

Physical continuation from C031-WIP726.

Authoritative state:
- Primary queue: 169/169 COMPLETE.
- Confirmed post-primary findings through F722/KI#1043.
- F723/KI#1044 remains UNUSED.
- No gameplay/XML/Python source modified.

PRACTICAL 6329 / KI#644-KI#645: REPAIR-CLEAN

KI#644 prospective strategic-resource holdings:
- Current AI_bonusTradeVal callers use iChange as the acquisition/cancellation delta for the evaluated bonus.
- Applying iChange only when eBonus matches each strategic availability boolean yields the correct prospective first-copy/last-copy state while leaving substitute-resource holdings untouched.
- No caller supplies a different semantic quantity that would make the added delta dimensionality wrong.

KI#645 complete building technology gate:
- The synergy pass selects the buyer civilization's building for each class and tests the buyer team.
- Primary prerequisite plus every additional AND technology now match the complete current building-tech contract used by construction/older valuation logic.
- SpecialBuilding technology remains a broader robustness omission; the original F322 audit found no current shipped bonus-health/happiness building that reproduces it, and practical 6329 deliberately did not broaden the city-independent heuristic beyond the proven root.

Result:
- Practical 6329 closes cleanly.
- F723/KI#1044 remains UNUSED.

NEXT EXACT CURSOR
C031-WIP727 physical -> F723/KI#1044 UNUSED -> classify/open practical 6328 -> current producer/consumer repair audit -> whole-album dedup mandatory -> freeze WIP728 at next settled boundary.

===============================================================================
C031-WIP728 ADDENDUM — post-primary repair-side audit
Practical 6328 KI#622/KI#629 repair validation complete; F723 remains unused
===============================================================================

AUTHORITATIVE CONTINUATION STATE
--------------------------------
Primary queue remains 169 / 169 COMPLETE.
Post-primary confirmed findings remain F708-F722.
F723 / provisional KI#1044 remains UNUSED / next.
No gameplay, XML or Python source files modified.

PRACTICAL 6328 — 98ee0c269abf6dc0459e4d8c5062ab0a39d38445
Title:
  fix inherited KI#622 input handling and SAS KI#629 log identities; advance the
  C++ File Audit Album through C031-WIP10

Disposition: REPAIR-CLEAN.

KI#622 — consumable BUG keyboard/mouse return propagation
----------------------------------------------------------
The repair exactly restores the cross-language contract originally proven by
F299/KI#622.

Current live Ctrl+Z chain was revalidated end-to-end on the supplied source:

  * Assets/Config/BUG Core.xml registers Ctrl Z as a BUG shortcut calling
    InputUtil.toggleDebugMode.
  * BugEventManager.onKbdEvent handles shortcuts only for EventKeyDown; after
    invoking the registered handler it returns integer 1 itself. The shortcut
    function therefore does not need to return a value.
  * _handleConsumableEvent propagates the first positive handler result.
  * _dispatchEvent returns the selected dispatcher result.
  * practical 6328's BugEventManager.handleEvent now RETURNS that _dispatchEvent
    result instead of discarding it.
  * CvEventInterface.onEvent returns handleEvent's result to the DLL.
  * CvDllPythonEvents::postEvent recognizes exactly Python integer 1 as consumed,
    and reportKbdEvent/reportMouseEvent propagate that Boolean through the
    DllExport CvEventReporter input contract.

The companion InputUtil change is coherent with that repaired ownership:
InputUtil.toggleDebugMode now always invokes CyGame/CvGame::toggleDebugMode.
CvGame::toggleDebugMode itself retains AdvCiv's authoritative permission gate
(!m_bDebugMode && !isDebugToolsAllowed(false) => return), so Python does not
bypass a second legality policy. Once onKbdEvent handles Ctrl+Z, it returns 1,
therefore native/interface fallthrough is suppressed rather than performing a
second toggle.

This matches the original F299/KI#622 repair boundary exactly. The proprietary
EXE-side caller remains outside the SDK just as the original finding recorded;
no more-specific native follow-on behavior is invented here. The exported
consumed/not-consumed ABI is now restored.

KI#629 — BBAI/SASGameRecord process and session log identities
--------------------------------------------------------------
The repair also survives a lifecycle/rollover audit.

Shared process identity:
  * CvGameCoreUtils owns one static const g_szSASProcessUtcTimestamp initialized
    once per DLL load through createSASUtcTimestamp().
  * Both BBAI and SASGameRecord read that same value through
    getSASProcessUtcTimestamp(), so two logger-local lazy samples cannot straddle
    a UTC-second boundary and falsely describe one process with two identities.

BBAI session identity:
  * rollSASBBAILog ALWAYS refreshes g_szSASBBAILogTimestamp, including fixed-name
    mode.
  * timestamped mode additionally increments a chronological sequence and uses
    newN/loadN context, keeping same-second rollover filenames distinct.
  * fixed-name mode intentionally retains one filename, but each new/load header
    now carries the newly sampled session UTC rather than an old lazy timestamp.

SASGameRecord session identity:
  * rollSASGameRecordLog samples one time_t for the session UTC and separately
    resets its monotonic session/snapshot clocks at the same rollover boundary.
  * it always overwrites g_szSASGameRecordLogTimestamp, also in fixed-name mode.
  * timestamped mode increments the newN/loadN sequence, preventing same-second
    filename collisions.
  * session-local semantic sequence, transaction and immediate owner-cause state
    are reset at the same boundary.

Filename construction is shared through getSASDiagnosticLogName; when
bTimestamped=false it deliberately ignores timestamp/context and emits the
stable base filename, while headers still expose the refreshed UTC. No stale
context or filename/session identity contradiction survives.

No F723 promoted from practical 6328.

NEXT
----
C031-WIP728 physical -> F723/KI#1044 UNUSED -> classify/open practical 6327 ->
current producer/consumer repair audit -> whole-album dedup mandatory -> freeze
WIP729 at the next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP729 ADDENDUM — post-primary repair-side audit
Practical 6327 skipped docs-only; practical 6326 identity centralization clean
===============================================================================

AUTHORITATIVE STATE
-------------------
Primary 169 / 169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 UNUSED / next.
No gameplay/XML/Python source modified.

PRACTICAL 6327 — b6e40a8a0d...
Disposition: DOCS-ONLY; no live source audit required.

PRACTICAL 6326 — 66b46c22d2...
Title: Centralize mod display name and loaded-folder handling; Update DLL
Disposition: CLEAN.

The repair introduces a deliberate two-identity model and current consumers honor it:

  DISPLAY / PRODUCT IDENTITY
    ModName::getDisplayName() reads SAS_MOD_DISPLAY_NAME after GlobalDefines are
    available, with actual detected folder name as fallback.

    Current presentation consumers use it:
      * BUG CvModName.modName/displayName;
      * trade-route help text;
      * replay filename prefix;
      * replay settings text;
      * BBAI/SASGameRecord displayName diagnostics.

  FILESYSTEM / LOADED-INSTALL IDENTITY
    ModName::getName()/getPathInRoot remain the EXE-derived actual folder/path.
    Current filesystem consumers use those values rather than the branded name:
      * BUG Help documentation lookup uses BugPath.getModDir();
      * Sevopedia asset fallback uses CvModName.getFolderName();
      * Python exposes folder/path separately through CyGlobalContext;
      * diagnostic rows record folderName/modPath independently of displayName.

Queue166's completed current ModName lifecycle audit already proved the underlying
path/update/cache implementation clean, including refresh when the DLL interface
provides the real path. This repair-side pass therefore focused on the new
producer/consumer joins rather than reopening that implementation wholesale.

No current consumer calls getDisplayName before the GlobalDefines lifecycle it
requires: the live uses are BUG/Python setup after XML availability, gameplay UI
or replay generation, and new/load diagnostic logging. No gameplay/AI rule now
uses the branded display string.

Renaming the installed folder therefore changes filesystem identity without
changing the project display name, while changing the configured display name
cannot redirect filesystem lookup. Current configured names are safe for replay
filename/text use. More hostile arbitrary display strings remain configuration
robustness rather than a shipped defect.

No F723 from practical 6326.

NEXT
----
C031-WIP729 physical -> F723/KI#1044 UNUSED -> practical 6325 / KI#613
Permanent-Alliance self-pact teardown repair-side audit across CvDeal + CvTeam ->
whole-album dedup mandatory -> freeze WIP730 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP730 ADDENDUM — practical 6325 KI#613 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6325 — edb19dba0f...
Title: Fix inherited K-Mod/AdvCiv Permanent Alliance self-pact teardown (KI#613)
Disposition: REPAIR-CLEAN.

The original F290/KI#613 root was post-reassignment deal teardown reconstructing
old team agreements through the players' NEW common team identity. A valid old
Defensive Pact could therefore reach setDefensivePact(self,false), violating the
self-team contract and polluting teardown state.

Current repair was revalidated across both authoritative layers:

CvTeam::addTeam:
  * preserves K-Mod's post-player-reassignment deal-cancellation timing, which is
    needed to avoid bumping the new allies out of borders;
  * before generic deal teardown, directly clears the obsolete bilateral state
    between the preserved survivor/absorbed team IDs for Open Borders,
    Defensive Pact, Force Peace and Disengage;
  * both directional bits are cleared explicitly while the old absorbed team ID
    still names the actual obsolete relation.

CvDeal::endTrade:
  * after reassignment, if both participant players now resolve to the same team,
    it returns early only for obsolete team-level relation items:
      VASSAL, SURRENDER, OPEN_BORDERS, DEFENSIVE_PACT, PEACE_TREATY, DISENGAGE;
  * thus those saved/proxy items cannot reinterpret the old relation as a
    self-team cancellation or add false cancellation memory;
  * player-level RESOURCE and GOLD_PER_TURN components deliberately continue
    through ordinary counter teardown. A later album control already proved this
    is correct: such explicit same-team annual transfers can survive the PA and
    CvDeal::verify still owns their later validity/cancellation.

The PA item itself does not need an endTrade relation mutation: startTrade's
TRADE_PERMANENT_ALLIANCE case is non-saved and addTradeItems performs addTeam
from the already captured old eFirstTeam/eSecondTeam IDs.

Potential vassal/self-vassal sibling is neutralized in current source by the
later whole-deal guard that rejects vassal/surrender together with a PA or DP
before any item mutates state. No current supported transaction therefore relies
on same-team endTrade to dismantle a newly-created PA/vassal hybrid.

No asymmetric old pact bit, false cancellation memory, self-team mutator call or
player-level annual counter loss survives. No F723 from practical 6325.

NEXT
----
C031-WIP730 physical -> F723/KI#1044 UNUSED -> practical 6324 / KI#612 brokered-war
master-coalition repair audit -> current producer + current coalition legality +
whole-album dedup -> freeze WIP731 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP731 ADDENDUM — practical 6324 KI#612 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6324 — dbd181b1e8...
Title: Fix inherited brokered-war master-coalition validation exposed by SAS (KI#612)
Disposition: REPAIR-CLEAN.

The repaired structural rule was revalidated against the actual CvTeam war
cascade rather than only the two changed predicates.

Brokered-war orientation:
  * CvPlayer::canTradeItem(TRADE_WAR) executes on the hireling; eWhoTo/kToTeam
    is the sponsor and item.m_iData/kTargetTeam is the requested target.
  * canPossiblyTradeItem already requires the hireling team to be non-vassal and
    the sponsor not capitulated; KI#373 separately permits a foreign voluntary
    vassal as target under full UWAI.

Canonical coalition identity:
  * practical 6324 resolves sponsor and target through getMasterTeam(); equality
    therefore rejects sponsor-master -> own vassal, sponsor-vassal -> own master,
    and sibling-vassal requests under one master without banning foreign
    voluntary-vassal targets.
  * CvTeamAI::AI_declareWarTrade mirrors the exact same canonicalization for
    direct AI callers, so player-side trade-table legality and AI denial do not
    use different coalition identities.

Defensive Pact coverage:
  * TRADE_DEFENSIVE_PACT legality requires BOTH teams to be non-vassals.
  * CvTeam::setVassal kills existing Defensive Pact deals involving the new
    vassal. Thus no supported live DP can remain on a vassal member whose master
    relation would be hidden from the canonical-master test.
  * any DP capable of dragging the sponsor coalition into the hired war is
    therefore represented at sponsorMaster <-> targetMaster, exactly the pair
    practical 6324 checks.

Actual declaration cascade:
  * hireling.declareWar(target) first establishes the direct war;
  * if target is a vassal, CvTeam::declareWar queues alignment war against its
    master;
  * declaring on that master triggers the master's Defensive Pacts;
  * if sponsor is a vassal of one of those DP allies, vassal alignment then
    makes the sponsor itself an enemy of the hireling.
  * the repaired master/master equality + DP guard rejects precisely those
    transactions before this cascade can make the hireling attack its sponsor.

No coalition member relation capable of producing the forbidden cascade remains
outside the repaired canonical test under current supported vassal/DP rules.
No F723 from practical 6324.

NEXT
----
C031-WIP731 physical -> F723/KI#1044 UNUSED -> practical 6323 / KI#605
cancelled-deal memory accumulation repair-side audit -> trace deal-kill item
multiplicity, direction/orientation and one-per-deal vs one-per-item semantics ->
whole-album dedup mandatory -> freeze WIP732 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP732 ADDENDUM — practical 6323 KI#605 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6323 — f0764625c2...
Title: Fix inherited AdvCiv deal-cancellation memory accumulation (KI#605)
Disposition: REPAIR-CLEAN.

The repair was revalidated against the actual team-agreement proxy lifecycle.

Additive helper:
  * addEndTradeMemory again calls AI_changeMemoryCount rather than absolute
    AI_setMemoryCount, so a new cancellation cannot erase/reduce memory retained
    from an earlier independent cancellation.
  * the existing AdvC scaled-decay convention remains unchanged: one event adds
    +2, or +1 when that event is intentionally halved.

Vassal/surrender logical-event gating:
  * startTrade with a real vassal/surrender transaction calls startTeamTrade,
    which creates saved one-item player-pair proxy deals for the team agreement;
    the initiating item itself is not the persistent team relation.
  * cancellation of whichever proxy owns the logical teardown arrives with
    bKillTeam/bTeam=true.
  * endTeamTrade then kills all sibling proxies through kill(false), so their
    endTrade calls receive bTeam=false.
  * practical 6323 places ALL vassal-break memory work inside if(bTeam). Thus the
    one logical cancellation records once; sibling proxies cannot multiply it.

The retained repeated calls now have their documented meaning again:
  * ordinary master-side vassal cancellation receives two event additions on the
    doubled-decay scale;
  * denied-help handling gives the vassal three event additions while retaining
    the one master-side addition;
  * because each call is additive, prior unrelated cancellation memory is
    preserved rather than reset to the current event's amount.

Open Borders and Defensive Pact memory were already inside their corresponding
bTeam teardown blocks, so changing the shared helper back to additive does not
reintroduce sibling-proxy multiplication there either.

No supported saved deal shape was found that can carry multiple logical
vassal/surrender teardowns through one bTeam=true proxy. No F723 from practical
6323.

NEXT
----
C031-WIP732 physical -> F723/KI#1044 UNUSED -> practical 6322 / KI#604,#606,#608
whole-deal treaty/list-orientation repair audit -> compare whole-deal queries,
renewal proxy creation and embargo-side annual-item direction -> whole-album
dedup mandatory -> freeze WIP733 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP733 ADDENDUM — practical 6322 KI#604/#606/#608 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6322 — f5d92131c0...
Title: Fix inherited AdvCiv whole-deal treaty handling (KI#604, KI#606, KI#608)
Disposition: REPAIR-CLEAN.

KI#604 — whole-deal peace classification
-----------------------------------------
CvDeal::hasTradeItem now scans both saved lists and isPeaceDeal delegates to it.
This restores the logical peace marker regardless of which side owns the one-sided
saved treaty. The helper is read-only and does not alter item direction or hidden
state; all existing isPeaceDeal consumers inherit the corrected classification.

KI#606 — team-scope renewal
----------------------------
CvPlayer::resetDualDeal now operates on the team-level identity actually owned by
Open Borders, Defensive Pact and force peace:
  * deals are matched by the two teams, not just the invoking player pair;
  * the requested marker is found through the both-list helper;
  * every matching proxy gets the same current initial turn.

This exactly closes F283's expanded repair boundary: a second-list-only or another-
teammate peace owner is found, and all OB/DP proxies are renewed coherently rather
than leaving one older proxy able to tear down the shared team state early.

A broader architectural coupling remains: when a peace marker shares one CvDeal
with annual reparations, resetting that deal's initial turn also extends the
cancelability age of those annual items. That co-owned lifetime predates this
repair and belongs to the same whole-deal transaction-granularity family already
represented by F287/KI#610; no independent KI#606 repair-side root is assigned.

KI#608 — embargo/reparations orientation
----------------------------------------
Current source additionally contains later KI#617's exact-player refinement.
AI_stopTradingTrade therefore:
  * considers only deals between THIS enacting player and the target team, matching
    what this player's stopTradingWithTeam can actually cancel;
  * classifies peace across the whole deal;
  * scans only this player's gives-list for RESOURCE/GPT obligations.

Thus annual reparations OWED by the AI block an unfulfillable embargo promise,
while annual payments received by it or a teammate's independent reparations do
not. The repair and later scope refinement are mutually consistent.

No F723 from practical 6322.

NEXT
----
C031-WIP733 physical -> F723/KI#1044 UNUSED -> practical 6321 / KI#600-603
Python callback + event-help contract repair-side audit -> verify return-type
sentinels, percent-vs-absolute units, help-callback ownership and live Python
producers -> whole-album dedup mandatory -> freeze WIP734 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP734 ADDENDUM — practical 6321 KI#600-603 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6321 — a90878c416...
Title: Fix inherited Python callback and event-help contracts (KI#600-603)
Disposition: REPAIR-CLEAN.

KI#600 — optional map callback fallbacks
----------------------------------------
Current CvMapGenerator owns concrete native fallbacks at each repaired boundary:
  * generatePlotTypes false -> fills every plot with PLOT_LAND before setPlotTypes;
  * generateTerrainTypes false -> does not overwrite the terrain state already
    established by the native/map-generation path;
  * addFeatures false -> runs the native C++ FeatureInfo appearance loop.

Practical 6321 therefore removes only assertion pressure for a valid absent/default
Python hook. Actual Python overrides still require exact output size; malformed
returned vectors remain errors. addFeatures passes the call's no-assert flag but
still returns isOverride(), so a real Python implementation continues to suppress
the C++ fallback.

KI#601 — colored-plot failure sentinel
--------------------------------------
updateColoredPlots now initializes the callback result to 0. A failed Python call
therefore returns false and lets CvGame::updateColoredPlots run its native path.
This is the local inherited fallback contract; no other callback's distinct
sentinel/default is changed.

KI#602 — Farm Bandits help token
--------------------------------
EVENT_FARM_BANDITS_2's dangling getHelpFarmBandit2 symbol is simply removed from
PythonHelp. The event's actual gameplay/help effects are XML-backed, so no live
behavior is removed and strict diagnostics remain active for genuine named Python
help callbacks.

KI#603 — unit-cost override threshold
-------------------------------------
The repaired >1 threshold matches both:
  * BUG GameUtils' current getUnitCostMod default of -1 (with the DLL wrapper's
    disabled fast path returning 0); and
  * CvCity's existing getBuildingCostMod sibling, which applies only values >1.

Thus <=1 retains native cost while real percentage modifiers >=2 continue to be
applied. No current producer relies on 1 as a literal 1-percent request.

No F723 from practical 6321.

NEXT
----
C031-WIP734 physical -> F723/KI#1044 UNUSED -> practical 6320 / KI#597-599
Python callback repair-side audit -> nullable doGoody unit ownership, positive
override range and pillage-gold fallback/sentinel semantics -> whole-album dedup
mandatory -> freeze WIP735 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP735 ADDENDUM — practical 6320 KI#597-599 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6320 — 07bae58ec8...
Title: Fix inherited AdvCiv Python callback contracts (KI#597-599)
Disposition: REPAIR-CLEAN.

KI#597 — nullable doGoody unit wrapper
--------------------------------------
The live ownership-change goody path calls CvPlayer::doGoody(plot,NULL). CyUnit
has a pointer constructor that accepts CvUnit* and initializes m_pUnit directly;
its exposed methods guard a NULL m_pUnit. Practical 6320 therefore restores the
intended Python wrapper object without dereferencing a nullable unit.

KI#598 — positive numeric override truth
----------------------------------------
canBuild retains the documented tri-state contract:
  -1 -> no Python override (bOverride remains false),
   0 -> explicit forbid,
  >=1 -> explicit permit.
A failed call keeps the -1 initializer and exits before the nonnegative assertion.
The map-placement helper similarly resolves missing/default and negative fallback
first, then treats any positive actual override as true. The shared toBool exact-
one semantics are deliberately unchanged for unrelated callbacks.

KI#599 — pillage-gold fallback
------------------------------
CvUnit::pillageImprovement runs native synchronized-gold calculation exactly when
CvPythonCaller::doPillageGold returns false. Practical 6320 returns false for all
negative callback results, matching K-Mod's sentinel contract; zero remains a
valid explicit zero-gold override and positive values remain explicit Python gold.
Pillage gold has no supported negative gameplay meaning, so the broadened negative
fallback test cannot hide a legitimate award.

No F723 from practical 6320.

NEXT
----
C031-WIP735 physical -> F723/KI#1044 UNUSED -> practical 6319 / KI#593-596
repair-side audit -> collateral team/attacker identity, BC calendar floor/truncation,
replay color mapping and Assert-build diagnostic gates -> current producers +
whole-album dedup -> freeze WIP736 at next settled boundary.

No repository source files modified.
===============================================================================

===============================================================================
C031-WIP736 ADDENDUM — practical 6319 KI#593-594 repair-side validation
===============================================================================

STATE
-----
Primary 169/169 COMPLETE.
Post-primary F708-F722 confirmed.
F723 / provisional KI#1044 remains UNUSED / next.
No repository gameplay/XML/Python source modified.

PRACTICAL 6319 — dc19e5ba06...
Title: Fix inherited collateral-team and BC calendar defects (KI#593-594)
Disposition: REPAIR-CLEAN.

KI#593 — local collateral combat roles
--------------------------------------
AI_localAttackStrength's outer unit census and the repaired collateral estimator
now use compatible roles:
  * explicit enemy eAttackTeam -> attacker is that team, defender is this
    evaluating player's team;
  * eAttackTeam==NO_TEAM -> outer loop counts all teams currently at war with us;
    estimator keeps the generic attacker but restricts defender sampling to us;
  * own-offense eAttackTeam==getTeam() -> attacker is us and defender is the
    target plot's opposing team when that team is known.

If the own-offense target plot is neutral or belongs to our own team, the repair
passes NO_TEAM defender; estimateCollateralWeight then samples all non-attacker
defenders on the plot. That is the truthful generic fallback for enemy units on
neutral/our territory rather than reintroducing the old evaluator-team polarity.
No caller role mismatch survived.

KI#594 — negative fractional calendar years
--------------------------------------------
intdiv::umodulo(dividend,positiveDivisor) explicitly returns a remainder in
[0,divisor). Therefore:
  (month - umodulo(month,monthsPerYear)) / monthsPerYear
is the mathematical floor-year conversion for negative month counts and ordinary
integer division for exact/nonnegative years. The existing max(1,numMonthInfos)
keeps the divisor positive. No positive/default-calendar behavior changes.

No F723 from practical 6319.

NEXT
----
C031-WIP736 physical -> F723/KI#1044 UNUSED -> practical 6318 / KI#595-596
replay foreign-color mapping + Assert-build diagnostic gate repair-side audit ->
verify enum offset/range and build-macro ownership/lazy diagnostic side effects ->
whole-album dedup mandatory -> freeze WIP737 at next settled boundary.

No repository source files modified.
===============================================================================

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6318 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F722.
F723 / provisional KI#1044 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6318 -- 55e439ef7f2e11fa7b6e36524b8568ff7f50bac0
"Fix inherited AdvCiv replay colors (KI#595) and SAS Assert guards (KI#596)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#595 -- FOREIGN REPLAY COLOR REMAPPING
----------------------------------------
The repaired CvGlobals::getColorInfo branch is internally range-safe and preserves the
original AdvC intent to skip system colors 0 through 6 when remapping out-of-range
foreign replay color IDs.

The remap branch executes only when eColor >= getNumColorInfos(). For an ordinary color
table with more than seven entries, this also guarantees eColor >= 8, so
(eColor - 7) is nonnegative. The repaired expression

    7 + ((eColor - 7) % (iNumColors - 7))

therefore maps only into the usable suffix [7, iNumColors-1] and cannot wrap into the
system-color prefix. The explicit iNumColors > 7 assertion documents that intended
layout. The guarded small-table fallback uses std::max(1, iNumColors), avoiding a zero
divisor even under malformed/future data that does not provide the expected suffix.
No second range/polarity error survives.

KI#596 -- ASSERT-ENABLED DIAGNOSTIC GUARDS
------------------------------------------
The repair also matches the actual build-configuration contract.

CvGlobals::getDefineINT/FLOAT/STRING already capture lookup success under
FASSERT_ENABLE. Moving only the richer diagnostic/assert blocks from _DEBUG to
FASSERT_ENABLE therefore restores them in the optimized Assert target without adding
work to a normal Release build. Message construction remains lazy inside the failed
condition.

CvSelectionGroupAI::AI_update keeps the two concerns correctly separated:

* _DEBUG alone adds four extra loop attempts, then transiently subtracts those four so
  the diagnostic assert can trigger at the ordinary threshold, and restores them before
  the final stuck-group fallback;
* FASSERT_ENABLE alone controls the rich diagnostic/assert block itself.

Thus an optimized Assert build asserts at its normal termination threshold and then
executes the same fallback, while a Debug build still gets the intended four extra
post-assert diagnostic iterations. There is no missing restore or Release-only state
mutation introduced by the guard change.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
No independent post-primary owner exists for either repaired boundary. These checks
validate the practical-6318 implementations of the already-recorded KI#595/KI#596
repairs; they do not consume F723.

NEXT
----
WIP737 physical -> F723/KI#1044 UNUSED -> classify practical 6317 downward from the
local commit-diff archive -> skip only docs/generated-only commits -> open the first
live gameplay/AI repair -> require current producer + concrete violated contract +
whole-album dedup before promoting F723.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6317 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F722.
F723 / provisional KI#1044 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6317 -- 2d3cf8a9781cdaa642e039b1db4c76742c609ac5
"fix 4 inherited AdvCiv UWAI defects and 1 SAS regression (KI#507, KI#508,
KI#514, KI#516, KI#518); reconcile the Audit Album through C024"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#507 -- BROKERED-PEACE LOW-SCORE RELAXATION
--------------------------------------------
The repair changes only the branch that made AdvC's explicit low-score exception dead.
When the low-score/reluctance exception succeeds, bNoDenial now reaches the existing
bEnemyWillTalk test and then the NO_DENIAL gate. If the score exception does not apply,
DENIAL_VICTORY is still returned; if reluctance is too high, DENIAL_TOO_MUCH remains.
Thus the repaired branch does not bypass contact willingness or broaden the exception
beyond the state F184/KI#507 proved.

KI#508 -- RECIPROCAL WAR-SUCCESS INDEX
--------------------------------------
The target-side term now reads kTarget.AI_getWarSuccess(kAgent.getID()), matching the
reciprocal opponent relationship represented by the preceding agent-side term. No
second target/team identity conversion occurs downstream; the combined scalar is used
only in this peace-threshold expression.

KI#514 -- SIGNED EMERGENCY-PEACE ROUNDING
-----------------------------------------
The emergency path now uses ScaledNum::round() instead of uround() for the signed peace
threshold. peaceThreshold itself returns an integer before conversion to scaled in the
caller, so signed round preserves the release-policy integer while removing the invalid
nonnegative-only API contract/assertion. The subsequent '- 100' and min(iU,...) logic is
unchanged.

KI#516 -- HUMAN-MASTER VASSAL PREPARATION TIMEOUT
-------------------------------------------------
The repaired human-master polarity matches the surrounding lifecycle. For an AI vassal
of a human master, a >20-turn preparing plan can now be abandoned as documented. The
immediately following vassal synchronization block copies a master's preparatory plan
only when !kMaster.isHuman(), so a human-instructed plan that was just abandoned is not
reinstalled in the same pass. Conversely, AI-master vassals retain synchronization to
their AI master's live plan rather than timing it out and re-adding it every turn. The
separate kAgent.isHuman() short-circuit preserves the AI-AutoPlay human-team case.

KI#518 -- RANDOM NONLEADER DIPLOMAT ASSERTION
---------------------------------------------
Replacing the stale team-leader assertion with an alive-player assertion preserves the
actual caller contract. The war-scheme path deliberately chooses an alive random team
member. UWAI::Team::doWar returns through its human/vassal branch before scheme() for a
human team; CvTeam::isHuman() in current AdvC prefers a human leader, so the live scheme
caller cannot accidentally select a human member of a team treated as AI. The selected
nonleader player is therefore an alive AI diplomat, exactly the state AdvC's random-
member change intended to support.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
These are implementation validations of the existing F184/KI#507, F185/KI#508,
F191/KI#514, F193/KI#516 and F195/KI#518 roots. No independent repair-side defect was
found and F723 remains unused.

NEXT
----
WIP738 physical -> practical 6316 docs-only -> F723/KI#1044 UNUSED -> open practical
6315/KI#591 mixed-route land-distance repair -> compare graph edge weights, route mixed-
state semantics and every current distance consumer -> whole-album dedup before any new
number.

================================================================================
POST-PRIMARY REPAIR-SIDE FINDING -- F723 / KI#1044
PRACTICAL 6315 / KI#591 INCOMPLETE CLASH-DISTANCE REPAIR
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed finding range is now F708 through F723.
Next root: F724 / provisional KI#1045 UNUSED.
No gameplay/XML/Python source was modified.

F723 / provisional KI#1044
--------------------------
TITLE
    Practical 6315's KI#591 clash repair averages land-only distances for both sides
    whenever a clash is not fleet-only, but the inherited mode predicate requires only
    ONE side to have a qualifying land route. The other side can therefore contribute
    the new unavailable sentinel -1 as a physical deployment distance.

STATUS
    CONFIRMED current AdvCiv-SAS repair-side regression / incomplete implementation of
    inherited F268/KI#591. Introduced by practical 6315
    (f2c966c30a3d5023d186fbde78093213fe1dc79c).

ORIGINAL ROOT VS REPAIR-SIDE ROOT
---------------------------------
F268/KI#591 proved that UWAI had only one mixed fastest-route numeric distance. A land
reachability Boolean could therefore select a land invasion while getDistance() had
already been shortened by a faster sea route. Its repair correctly introduced a
separate getDistanceByLand() metric and converted land-specific consumers.

The original F268 clash addendum also observed that an overland clash could inherit a
sea-shortened getDistance() when both sides had land reachability. That justified a
route-specific clash metric.

Practical 6315, however, implemented the clash conversion as though every non-fleet
clash guaranteed a valid land-only distance for BOTH sides. The inherited clash-mode
predicate does not establish that stronger invariant.

CURRENT MODE PREDICATE
----------------------
For bClashOnly, Node::step says a clash is fleet-only only when NEITHER side can reach
its own target city by land from its capital:

    bNaval = (!canReachByLand(pAttTargetCity->getID(), true) &&
              !kDefender.canReachByLand(pDefTargetCity->getID(), true));

Therefore bNaval == false whenever EITHER side has capital-qualified land reachability.
The source comment explicitly describes that state as an Army clash rather than a Fleet
clash.

CURRENT REPAIRED CONSUMER
-------------------------
Practical 6315 changed clashDistance to:

    return scaled((bNaval ? pTargetCity->getDistance() :
            pTargetCity->getDistanceByLand()) +
            (bNaval ? pOtherTargetCity->getDistance() :
            pOtherTargetCity->getDistanceByLand()), 2);

For bNaval == false, BOTH terms are therefore forced to the new land-only metric.

But UWAICache::City initializes m_iDistanceByLand = -1 and sets it only from qualifying
land routes whose SOURCE city lies in the cache owner's primary area. Generic
getDistance() can remain valid through a nonqualifying land route, a sea route or an
assumed mixed route.

SUPPORTED ONE-SIDED-LAND TOPOLOGY
---------------------------------
A normal empire geography can satisfy the mode predicate asymmetrically:

* Player A's capital/primary-area city shares land with one of B's remote secondary-
  area cities. A therefore has a qualifying capital land route to B.
* Player B's capital/core is overseas. B's remote city on A's landmass is not in B's
  primary area. A geometric land path from that remote city to A may contribute to B's
  GENERIC mixed distance, but it does not set B's qualifying land-only vector; B can
  also generically reach A by sea.
* The two Nodes can therefore mutually select one another because targetCity() requires
  only City::canReach(), which is getDistance() >= 0.

This yields, for the mutual clash:

    A canReachByLandFromCapital == true
    A getDistanceByLand         == positive

    B canReach / getDistance    == valid
    B getDistanceByLand         == -1

The fleet-only predicate is false because A has the land route. The repaired
clashDistance nevertheless averages A's real land duration with B's -1 sentinel.

CONCRETE CONSEQUENCE
--------------------
If A's qualifying deployment duration is 5, the repaired clash distance is based on
(5 + -1) / 2 rather than two valid physical deployment estimates, materially
shortening the modeled meeting time.

The strongest boundary is a one-turn A land deployment: (1 + -1) / 2 becomes zero,
which violates the immediately following invariant:

    FAssert(rClashDist.isPositive());

Such a topology is possible when A's core is adjacent to B's remote colony while B's
own core/capital is overseas. Thus the repair can both corrupt simulation timing in
Release and trip the existing Debug/Assert invariant.

WHY SYMMETRIC PATHFINDING DOES NOT SAVE IT
------------------------------------------
TeamStepMetric's geometric land path is symmetric, but qualifying strategic land
reachability is not purely geometric: updateDistance sets bQualifyingLandRoute from
CACHE OWNER AI_isPrimaryArea(sourceCity.area). A shared landmass can be primary for A
and secondary for B. This is exactly why one cache can have a valid land-only metric
while the reciprocal cache has only generic/mixed reachability.

DEDUP / OWNERSHIP
-----------------
Keep F268/KI#591 as the inherited representation/consumer root. F723 is specifically
about the SAS repair's new unavailable-sentinel use at the asymmetric clash boundary.
The original F268 clash addendum covered sea-shortened mixed distance in an overland
clash; it did not and could not contain this -1 land-distance failure because
m_iDistanceByLand did not exist before practical 6315.

No later album finding owns this repair-side state.

NARROW REPAIR BOUNDARY
----------------------
Do not assume bNaval == false means both target-city cache records have valid land-only
distances. The clash needs a per-side deployment metric consistent with each side's
actual available route under the chosen clash model, or the mode predicate itself must
be strengthened if an Army clash truly requires qualifying land deployment from both
sides. At minimum, never feed getDistanceByLand() into arithmetic when it is -1.

NEXT
----
WIP739 physical -> F723/KI#1044 CONFIRMED -> F724/KI#1045 UNUSED -> finish remaining
practical-6315 consumers/save-format controls for siblings -> then practical 6314
KI#590/KI#592 repair-side audit -> current producer + violated contract + whole-album
dedup before any F724 promotion.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6314 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6314 -- 269a539ee43d8358608c6fe9475da681d9a80a6a
"fix inherited UWAI/AdvCiv city-loss state (KI#590, KI#592)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#592 -- SIMULATION-ADJUSTED PER-AREA CITY COUNTS
--------------------------------------------------
remainingCitiesInArea() starts from the real CvArea owner count and subtracts each
city in this Node's graph-local m_cityLosses exactly once. m_cityLosses is a set, and
MilitaryAnalysis models hypothetical conquest without deleting/transferring the real
CvCity objects. UWAICache::cvCityById can therefore still classify every simulated lost
city by its real area during the same analysis transaction.

The helper is wired into the three stale spatial consumers identified by F269/KI#592:
attacker area weighting, defender area weighting, and the defender's same-area extra-
garrison rally bound. Global remaining-city denominators already subtract m_cityLosses,
so numerator and denominator now describe the same simulation time slice. The rally
path's later max(0,...) safely bounds the existing '-1 for attacked city's local
garrison' adjustment when no other simulated city remains in the area.

No duplicate-loss subtraction or real-map/cache mutation is introduced.

KI#590 -- THIRD-PARTY CITY LOSS STOPS DEFENDER'S OUTGOING EDGE
--------------------------------------------------------------
On a successful city loss, the repair clears the defender's outgoing target only when
that target is not the attacker who just captured the city. changePrimaryTarget(NULL)
is the graph's authoritative edge mutator: it clears m_pPrimaryTarget and removes this
player from the old target's m_targetedBy set, so no reverse adjacency is orphaned.

The attacker exception preserves the ordinary two-party mutual-war case. Terminal
elimination/capitulation continue through their existing broader cleanup. Thus the
repair realizes the inherited comment's "third-party loss stops own offensive" policy
without accidentally erasing the direct defender-vs-attacker war relationship.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
These validate the existing F267/KI#590 and F269/KI#592 repairs. No independent
repair-side defect survives; F724 remains unused.

NEXT
----
WIP740 physical -> F724/KI#1045 UNUSED -> open practical 6313/KI#580/#582/#584/#585
InvasionGraph arithmetic repairs -> verify geometry split, branch-loss dimensions,
city/capital state and each caller -> whole-album dedup before any new number.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6313 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6313 -- b34e95e9aecbebb7c1e0f47f62839fe80688cdc3
"fix inherited UWAI and AdvCiv InvasionGraph arithmetic (KI#580, KI#582,
KI#584, KI#585)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#582 -- CLASH GEOMETRY VS PERSONALITY EFFORT
----------------------------------------------
The repaired order first partitions the fixed clash distance from the two physical Army
powers, subtracts the attacker's bounded physical share from the defender, and only
then applies MaxWarMinAdjacentLand personality scaling to attacker deployment effort.
The defender share is therefore geometrically nonnegative. The personality factor still
changes attacker duration/reinforcement effort without moving the physical meeting point
past the defender.

KI#580 -- CONFIDENCE/MODIFIER CASUALTY UNITS
--------------------------------------------
Army-clash powers are multiplied by combat correction and confidence before
clashLossesWinnerLoser/clashLossesTemporary. Practical 6313 now removes both factors
before persistent Army/Cavalry casualties or temporary winner unavailability are stored.
The confidence values are clamped earlier in Node::step to [0.4, 1.8], so the new
conversion cannot divide by zero. Cavalry loss remains a ratio of the already-converted
physical Army loss and is not converted twice. The attacker-win and defender-win clash
branches use the appropriate side's modifier/confidence for winner, loser and temporary
losses.

KI#584 -- ATTACKER LOGISTICS CONFIDENCE DIMENSION
-------------------------------------------------
Naval Fleet combat power includes attacker confidence, whereas rCargoCap is physical
Logistics capacity. The repaired attacker ratio multiplies cargo capacity by rConfAtt
before dividing by confidence-scaled Fleet power, mirroring the defender ratio. The
later Fleet casualty division by rConfAtt then returns the loss to physical units. No
extra inverse-confidence term remains in attacker transport losses.

KI#585 -- REPEAT TRIPS AFTER TRANSPORT LOSSES
---------------------------------------------
The repeat-trip factor is retained separately. Physical one-trip Logistics casualties
are subtracted from rCargoCap first, surviving cargo is clamped nonnegative, and only
that surviving capacity is multiplied by the repeat-trip factor. This implements the
causal order identified by F262/KI#585 without multiplying the physical loss itself or
letting sunk capacity make later trips.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
These validate F257/KI#580, F259/KI#582, F261/KI#584 and F262/KI#585. No independent
repair-side defect survives; F724 remains unused.

NEXT
----
WIP741 physical -> F724/KI#1045 UNUSED -> practical 6312/KI#589 emergency Home Guard
accounting -> verify baseline-vs-virtual defender loss partition, reporting units and
successive-step persistence -> whole-album dedup before any new number.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6312 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6312 -- 3f1f2c614b5b6f470352c5e93dfc2fca0e9f6241
"fix inherited UWAI emergency-defender accounting (KI#589)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#589 -- BASELINE VS VIRTUAL HOME-GUARD CASUALTIES
---------------------------------------------------
City-defense power is modeled as original HOME_GUARD power plus
m_rEmergencyDefPow minus recorded original Home Guard losses. The new applyPowerLoss
helper preserves those two ownership domains explicitly:

1. consume at most the remaining original Home Guard and add only that part to
   m_arLostPower[HOME_GUARD];
2. apply any excess casualty to m_rEmergencyDefPow, capped by the virtual pool;
3. leave all non-HOME_GUARD branches on the inherited ordinary physical-loss cap.

This prevents casualties from disappearing once original Home Guard reaches its cap
without reporting simulation-created emergency defenders as losses of the real
civilization military.

The repeated-city-loss lifecycle is coherent too. Battle casualties are applied first.
On a successful conquest, only the SURVIVING emergency pool is then halved, and new
emergency power generated during the following heal/approach duration is added after
that. A virtual defender consumed by a prior battle therefore cannot reappear at full
strength in a later one.

The helper is invoked for both attacker and defender branch losses, but only actual
HOME_GUARD loss entries can touch the emergency pool; ordinary branches retain their
old behavior. No negative or double-accounted pool survives.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
This validates the existing F266/KI#589 repair. No independent repair-side defect
survives; F724 remains unused.

NEXT
----
WIP742 physical -> F724/KI#1045 UNUSED -> practical 6311/KI#587/#588 -> verify
coastal-Fleet transport overlap persistence and Army/Cavalry deployment projection ->
whole-album dedup before any new number.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6311 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6311 -- 9afb3705fcb87fbb350e292a1e62320721d28d93
"fix inherited UWAI branch-state issues (KI#587, KI#588)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#587 -- COASTAL FLEET / LOGISTICS OVERLAP
------------------------------------------
The repaired defender Logistics conversion uses the same dimensional structure as the
full naval-battle path. rDefFleetPow already includes confidence and the deployed
Army-portion factor. The ratio

    remainingLogistics * rConfDef / rDefFleetPow

therefore represents total physical cargo capacity per unit of confidence-scaled,
portion-limited Fleet combat power. Multiplying the physical defender Fleet casualty by
that ratio transfers the same casualty fraction into the overlapping Logistics branch.
The result is capped at remaining Logistics power.

Thus combat-capable transports destroyed in an earlier coastal battle remain unavailable
for a later naval invasion without consuming more cargo capacity than exists. The
pre-existing approximate Fleet-power/cargo-slot conversion is retained rather than
silently redefined.

KI#588 -- ARMY/CAVALRY DEPLOYMENT PROJECTION
--------------------------------------------
Cavalry is an overlapping subset of Army. The repaired attacker deployment factor is
computed once and applied to both rArmyPow and rCavPow, preserving Cavalry <= Army
through the distance projection. In the cavalry-attack branch rArmyPow is deliberately
replaced with rCavPow before this point, so both values are equal and receive the same
attenuation. Later battle-area projection has the separate KI#583 correction and does
not reintroduce whole-force Cavalry.

WHOLE-ALBUM / OWNERSHIP CONTROL
-------------------------------
These validate F264/KI#587 and F265/KI#588. No independent repair-side defect survives;
F724 remains unused.

NEXT
----
WIP743 physical -> F724/KI#1045 UNUSED -> practical 6310/KI#575/#576/#578/#579/#581/#583
-> audit loss ownership, discarded-probe mutation, clash-portion identity, ally
confidence scaling, defender trait identity and Army/Cavalry area projection as one
simulation-state tranche -> whole-album dedup before any new number.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6310 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6310 -- e168cd4ca3eafda8bd4c86d7403ba9fbce5d7d65
"fix inherited InvasionGraph issues (KI#575, #576, #578, #579, #581, #583)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#579 restores the intended assignment-based ally-confidence transformation; neutral
confidence now leaves a supplied partial Army portion unchanged instead of squaring it.

KI#583 applies the same area availability factor to overlapping attacker Army and
Cavalry, matching the defender-side invariant and preventing the subset from retaining
whole-force scale.

KI#581 queries the actual defender player's cached Defensive-trait state rather than the
graph-wide evaluator identity; the rest of the garrison calculation is already defender-
owned.

KI#576 moves emergency-defense halving from const/speculative Node::step into applyStep
after an actually applied successful conquest. Discarded priority/cycle/horizon probes
therefore no longer mutate persistent graph state.

KI#575 derives conquest-garrison unit count and representative power from kAttacker, the
same node whose Army lost/shifted state receives the result. The tempting sibling of
also shifting CAVALRY is not a current correctness omission: the abstract garrison does
not model concrete branch composition, and every subsequent attacker step clamps
available Cavalry to remaining Army before combat. A proportional Cavalry shift would
be a new heuristic policy rather than restoring a violated branch bound.

KI#578 keeps each node's independently computed Army portion attached to that same node
for conquest/defense distraction regardless of clash winner, eliminating the inherited
cross-assignment without changing winner selection.

No repair-side producer/consumer contradiction survives. F724 remains unused.

NEXT
----
WIP744 physical -> F724/KI#1045 UNUSED -> practical 6309/KI#573/#574 -> validate highest
victory-stage interpretation and eliminated/capitulated Node edge cleanup through all
terminal callers -> whole-album dedup before any new number.

================================================================================
POST-PRIMARY REPAIR-SIDE CONTINUATION -- PRACTICAL 6309 SETTLED
================================================================================

STATE
-----
Primary per-file queue remains 169/169 COMPLETE.
Post-primary confirmed findings remain F708 through F723.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6309 -- f7d92669e9c368b5633f9bcedd3d14dc4d4deb72
"Fix inherited original UWAI capitulation and terminal-edge defects (KI#573-574)"
DISPOSITION: REPAIR-CLEAN; NO NEW FINDING.

KI#573 removes stage-1-first local helpers and uses the shared highest-stage functions
on one snapshot of the attacker's current cumulative victory hash. The resulting
Conquest-vs-Domination comparison now reflects the actual highest active levels without
changing the surrounding capitulation policy.

KI#574 centralizes terminal graph-edge cleanup in setEliminated(true). Current source has
only terminal true callers (complete simulated city elimination and setCapitulated);
there is no setEliminated(false) lifecycle that would require restoring a target. The
setter calls changePrimaryTarget(NULL), which removes both m_pPrimaryTarget and the
reverse m_targetedBy entry immediately, before recursive simulation can return to that
target. Ordinary nonterminal city loss remains outside this terminal cleanup.

No repair-side state leak survives. F724 remains unused.

NEXT
----
WIP745 physical -> F724/KI#1045 UNUSED -> practical 6308/KI#202 follow-up +
KI#569-572 map-generation repairs -> audit Python override ownership, river altitude,
OneArea bonus rank arithmetic and batched lake/area lifecycle through the next generation
phase -> whole-album dedup before any new number.

===============================================================================
C031-WIP746 — PRACTICAL 6308 MAP-GENERATION REPAIR-SIDE AUDIT CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6308 / KI#202 FOLLOW-UP + KI#569-KI#572 — REPAIR-CLEAN
-----------------------------------------------------------------
Practical 6308 (`17a619c1aa35060c3a0e9060499b98c274b05041`) repairs four
map-generation roots around AdvCiv's OneArea-resource and native river/lake phases.
The current reconciled implementation was audited as one phase-order transaction rather
than four isolated hunks.

KI#569 / original F246 — KI#202 revisit denominator
---------------------------------------------------
The repaired denominator now counts `eBonus` exactly once:

    int iNumUniqueBonusesOnArea = kLoopArea.countNumUniqueBonusTypes();
    if (kLoopArea.getNumBonuses(eBonus) <= 0)
        iNumUniqueBonusesOnArea++;

This preserves the inherited "plus this one" rule for a fresh area but does not add a
phantom second copy when KI#202 deliberately lets pass two revisit an area that already
contains `eBonus`. `CvPlot::setBonusType` maintains the area bonus counters immediately,
and `addUniqueBonusType` asserts that the resource is OneArea, so an existing `eBonus`
is necessarily represented in `countNumUniqueBonusTypes`. The lower-bound assertion is
therefore coherent rather than masking a reachable zero divisor.

KI#571 / original F248 — zero preliminary rank
----------------------------------------------
The repaired early optimization is now:

    if (bSASAdvCivOneAreaBonusDistribution && iBestValue > 0 &&
        iValue <= iBestValue)
        continue;

When no candidate exists yet, a zero preliminary score is allowed through the actual
eligible-plot scan. If at least one legal plot exists, the final score is clamped to at
least one. This does not invalidate the optimization for an already-positive best:
`rSuitability = (eligible/areaTiles + 2) / 3` is at most one, so suitability can only
reduce a positive preliminary rank; it cannot make a candidate with
`iValue <= iBestValue` overtake an existing positive best. The newly scanned zero case
also consumes no extra MapRand draw; the random preliminary term was already evaluated
before the old/new early check.

KI#570 / original F247 — hybrid Python river altitude
-----------------------------------------------------
`CvPythonCaller::riverValue` now initializes its callback result to zero and preserves
the numeric result even when Python calls `allowDefaultImpl()`:

    ARGSLIST(0);
    ... call("getRiverAltitude", ...);
    bOverride = isOverride();
    if (bOverride && lResult < 0) ...
    return toInt(lResult);

The consumer in `CvMapGenerator::getRiverValueAtPlot` exactly matches the old BtS/K-Mod
three-state contract:

    if (bOverride)
        return pyValue;       // complete Python override
    int iSum = pyValue;       // hybrid additive modifier
    ... native C++ score ...

Thus a default+numeric map script such as the inherited Inland Sea / Great Plains form
adds the returned modifier exactly once. Missing/default-stub paths start at zero rather
than leaking the old sentinel. Negative validation remains limited to a full override,
which preserves the old additive-hybrid freedom.

KI#572 / original F249 — batched random-lake area topology
----------------------------------------------------------
The repaired native lake phase converts the full accepted batch without attempting
incremental area ownership:

    for each accepted lake:
        setPlotType(PLOT_OCEAN, false, false);
    if (!apLakes.empty())
        GC.getMap().recalculateAreas();

and this happens before:

    addFeatures();
    addBonuses();
    addGoodies();
    afterGeneration();

`CvMap::recalculateAreas()` clears every old area pointer/object and runs the normal
area construction. Under current `PASSABLE_AREAS`, `calculateAreas_dfs()` is followed
by `calculateReprAreas()`, whose tail calls `updateLakes()`; under the ordinary BtS-style
branch, `calculateAreas()` itself calls `updateLakes()`. Therefore the later generators
see coherent water/land area ownership, representative areas, lake classification and
shelf state rather than the stale earlier-lake state proved by F249.

The per-lake `bRebuildGraphics=false` does not create a repair-side omission in this
lifecycle. Native `addLakes` runs during map construction before `RebuildAllPlots` and
later `CvMap::setupGraphical`; the immediate consumers need topology, not already-built
plot graphics. Each new lake still receives the correct water terrain through
`setPlotType`. Moreover native lake candidates are noncoastal at snapshot time and
KI#566 excludes adjacency to an earlier selected lake, so suppressing the incremental
adjacent-water terrain refresh cannot leave an already-adjacent preexisting water tile
with the wrong coast/ocean terrain in this path.

WHOLE-TRANSACTION CONTROLS
--------------------------
* KI#569 and KI#571 compose correctly on pass-two revisits: the denominator fix prevents
  a phantom unique resource while the zero-rank fix prevents a legally usable area from
  being confused with no candidate.
* The suitability scan still uses the same map-script latitude policy as placement
  (the separate KI#565 repair); practical 6308 does not reopen that mismatch.
* A suitability scan with range checks disabled can still nominate an area whose actual
  placement pass later finds no ranged slot, but the existing `areas_tried` loop then
  continues to other areas. That is inherited ranking/optimization behavior, not a
  practical-6308 regression.
* Full Python `addLakes` overrides remain owned by the map script and do not traverse the
  native KI#572 batch; no new ownership ambiguity is introduced.
* F634/KI#956 remains distinct: normalization adds individual lakes later and can leave
  their cached `m_bLake` stale despite correct area membership. KI#572 concerns the
  earlier native map-generator batch and now performs an explicit whole-map rebuild.

DEDUP / CLASSIFICATION
----------------------
No independent current defect was found in practical 6308. The repairs close their
original roots F246-F249 (and the KI#202 follow-up relationship) at the source boundaries
they claimed. F724 / provisional KI#1045 remains unused.

NEXT
----
WIP746 physical -> F724/KI#1045 UNUSED -> classify practical 6307 downward from the
local commit-diff archive -> skip only docs/generated-only work -> open the first live
simulation/AI repair -> current-source producer/consumer proof + whole-album dedup before
any new finding number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP746
===============================================================================

===============================================================================
C031-WIP747 — PRACTICAL 6307 UWAI LIFECYCLE REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6307 / KI#558 + KI#562 — REPAIR-CLEAN
------------------------------------------------
Practical 6307 (`a81b2d6806dbfed084f7bd0d301cd03ab53e965c`) repairs two
persistent-UWAI lifecycle roots. Both were re-audited against the current player/team
death, revival, leader-change and cache-init/uninit ordering.

KI#558 / original F235 — leader-derived UWAI trait flags
--------------------------------------------------------
`CvPlayer::changeLeader` performs the real player transition in the safe order:
old traits are removed, the new leader is installed, new traits are processed, ordinary
AI personality is refreshed, and only then the new targeted
`UWAICache::reportLeaderChanged()` calls `updateTraits()`.

The current cache has exactly two leader/trait-derived stored fields:
`m_bOffensiveTrait` and `m_bDefensiveTrait`; `updateTraits()` recomputes both from the
player's now-current traits/free promotions. Other personality behavior is read from
normal player/personality state rather than a second stale UWAICache identity copy.

All supported source-level leader replacement paths (True Starts and the exposed
ChangePlayer/CyPlayer route) funnel through this central `changeLeader` function. Dead
players skip the callback and receive a fresh UWAI initialization if later revived.

The older F235 note that a save created under one XML interpretation could retain
serialized derived flags under a later compatible XML reinterpretation is still a
cross-version save-hardening consideration; it is not a same-version current-live
regression left by practical 6307.

KI#562 / original F239 — war sponsorship at elimination/early scenario peace
----------------------------------------------------------------------------
The normal full `reportWarEnding` now starts by clearing bounty/sponsor obligation.
When UWAI is enabled but not ready during the early scenario window,
`AI_preMakePeace` instead calls the new lightweight `clearWarSponsorship` for both
living sides and deliberately defers only the history/reparations evaluation that
requires ready caches.

For final-team elimination, `CvTeam::changeAliveCount` reports/clears the war into each
SURVIVING opponent team's UWAI before raw `setAtWar(false)` erases war state. That is
the persistent side that needs the lifecycle callback. The eliminated player's own
cache is later explicitly `uninit()`ed in `CvPlayer::setAlive(false)` and all transient
bounty/sponsor maps are reset by `UWAICache::clear()`. On revival, UWAI initializes a
fresh cache and reconstructs new-player state. Therefore practical 6307 does not leave
a second stale obligation on the dead side merely because it reports the survivor only.

When full history is available at elimination, the survivor-side `reportWarEnding`
also records the past-war result before war-success state is erased, preserving the
useful history if the defeated team later revives. During the intentionally not-ready
scenario window only the transient contract is guaranteed, exactly as F239's repair
boundary requested.

No duplicate full report occurs: the elimination path still ends war through raw
`setAtWar(false)` rather than `makePeace`, so `AI_preMakePeace` is not also invoked.

DEDUP / CLASSIFICATION
----------------------
No independent current defect was found in practical 6307. KI#558 closes the supported
runtime leader-change invalidation; KI#562 closes the persistent survivor obligation
and early-scenario cleanup without applying history evaluation where its cache
preconditions are unavailable. F724 / provisional KI#1045 remains unused.

NEXT
----
WIP747 physical -> F724/KI#1045 UNUSED -> practical 6306/KI#560-561 military-census
repairs -> audit Army air-unit admission and HomeGuard denominator/power population as
one overlapping state -> verify rebuild/save migration paths and current UnitInfo role
coverage -> whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP747
===============================================================================

===============================================================================
C031-WIP748 — PRACTICAL 6306 UWAI MILITARY-CENSUS REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6306 / KI#560 + KI#561 — REPAIR-CLEAN
------------------------------------------------
Practical 6306 (`a841a39131b9ce77b6a2ff3c889b22a3b41e80b1`) repairs the
actual Army admission census and the overlapping conventional HomeGuard population.
The two changes were audited together because both define the same current-force state
consumed by `InvasionGraph::Node::initMilitary`.

KI#560 / original F237 — ordinary aircraft in actual Army power
---------------------------------------------------------------
`MilitaryBranch::reportUnit` now admits a unit when it has positive ground combat,
positive air combat, or is nuclear, while each branch retains its own domain and
positive-power filters. `Army::isValidDomain` includes LAND/AIR/IMMOBILE and its
`unitPower` explicitly documents that nukes are part of Army. Thus Fighters, Bombers,
Stealth Bombers and Guided Missiles now enter the actual Army inventory instead of
remaining invisible merely because their ground `iCombat` is zero.

Typical-unit selection remains intentionally land-combat oriented; KI#560 changes only
the present-force census. No sibling branch is accidentally broadened: Fleet/Cavalry/
Logistics/Nuclear still apply their own domain/unit-power rules.

KI#561 / original F238 — conventional HomeGuard ratio vs nuclear subset
------------------------------------------------------------------------
`UWAICache::m_iNonNavalUnits` now excludes nukes, matching the conventional Army power
pool supplied to `HomeGuard::initTotals`. `InvasionGraph` then derives:

    nuclearUnits = Nuclear.numUnits();
    conventionalArmyUnits = Army.numUnits() - nuclearUnits;

applies the guard ratio only to `conventionalArmyUnits`, and adds the nuclear unit count
back unchanged. Nuclear POWER continues through UWAI's preexisting invasion cap rather
than through the HomeGuard split.

Current shipped nuclear units are Tactical Nuke (AIR) and ICBM (IMMOBILE). Both satisfy
Army's domain/admission rule and NuclearArsenal's nuke rule exactly once, so the subset
subtraction is valid for current data. No current non-sea/non-Recon/non-nuclear
`bMilitaryProduction` unit with both ground and air combat zero remains in the
HomeGuard denominator without corresponding Army admission.

Aircraft added by KI#560 remain conventional Army units; defensive-air roles can also
contribute to the HomeGuard numerator. This restores the population/power consistency
rather than moving the old nuclear mismatch onto aircraft.

SAVE / MIGRATION CONTROL
------------------------
The original F237 noted that old serialized Army totals would need migration if saves
from the buggy producer were supported. Current AdvCiv-SAS explicitly supports only
saves from the current version, as documented with KI#560; current-version games create
and serialize the corrected inventories consistently. Revival already has the separate
military-inventory rebuild repaired under KI#548. No same-version repair-side stale
producer survives.

DEDUP / CLASSIFICATION
----------------------
No independent current defect was found in practical 6306. KI#560 and KI#561 now form a
coherent present-force census under current UnitInfos. F724 / provisional KI#1045
remains unused.

NEXT
----
WIP748 physical -> F724/KI#1045 UNUSED -> practical 6305/KI#566-568 native map-generation
repairs -> audit frozen lake-candidate sequencing, river-source/lake topology state and
OneArea small-island fallback in combination with the later 6308 KI#569-572 repairs ->
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP748
===============================================================================

===============================================================================
C031-WIP749 — PRACTICAL 6305 MAP-GENERATION REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6305 / KI#566-KI#568 — REPAIR-CLEAN
----------------------------------------------
Practical 6305 (`76ac9bf1b390445a54efe043990e886d8a438ece`) fixes three
independent map-generation lifecycle/selection roots. They were rechecked in the
current source together with practical 6308's later KI#569-KI#572 follow-ups.

KI#566 / original F243 — frozen native lake candidates
------------------------------------------------------
AdvCiv still snapshots the initially eligible inland candidates for its desert-lake
probability adjustment, but before each delayed random roll the repair rejects a
candidate adjacent to any earlier SELECTED lake. Initial candidates were already
required to be noncoastal against all preexisting water, so newly selected lakes are
the only water state capable of invalidating a later frozen candidate in this phase.
The full adjacency iterator includes wrapped neighbors, and the rejection occurs before
`MapRandOneChanceIn`, matching BtS/K-Mod's important "newly coastal -> no roll" order.

Current practical 6308 then converts the accepted batch and performs the explicit full
area rebuild. Thus KI#566 restores selection/RNG sequencing while KI#572 independently
restores final topology; neither repair substitutes for the other.

KI#567 / original F244 — repeated whole-river regeneration IDs
--------------------------------------------------------------
`eraseRivers()` removes all north/west river edges first, while their inherited IDs are
still intact for the normal edge-removal updates. Only after the visible network has
been erased does `CvMap::resetRiverIDs()` clear every plot's hidden river ID and restore
`m_iNextRiverID=0`.

The helper is used by whole-network element regeneration, not by ordinary partial river
edge edits, so local river identity semantics are unchanged. A subsequent generation
therefore starts from the same ID state as initial generation instead of encountering
invisible old barriers or monotonically consuming the compact short ID space.

KI#568 / original F245 — OneArea small-island bootstrap
-------------------------------------------------------
The explicit small-area exclusion now includes `!bIgnoreAreaLimit`, retaining AdvCiv's
preference only during the capped first pass. The existing uncapped second pass can
therefore consider valid small areas even while fewer than two thirds of the target
have been placed.

Combined with KI#202's correct pass-two reset of `areas_tried`, this closes both halves
of the old recovery deadlock: capped first-pass areas are revisitable, and small areas
are no longer permanently excluded from the fallback. Practical 6308's KI#569 then
counts a revisited `eBonus` once, and KI#571 prevents a legally usable fallback area
from being rejected solely because its preliminary rank truncates to zero.

No first-pass policy is accidentally broadened. The per-area cap, latitude policy,
actual placement checks and grouped-copy caps remain in force.

NEGATIVE CONTROLS / DEDUP
-------------------------
* The frozen lake candidate probability normalization still uses AdvCiv's initial
  candidate census. KI#566 intentionally restores sequential eligibility, so a later
  candidate invalidated by an earlier lake no longer consumes a roll; this is the
  semantic behavior being repaired rather than an independent RNG defect.
* The old public `regenerateGameElements` question that plot-type lakes themselves are
  not erased remains the earlier documented API/topology ambiguity; KI#567 owns only
  river identity and does not claim to rebuild terrain/height topology.
* KI#568 can let a small area compete with a large area during pass two, but pass two is
  precisely the uncapped recovery phase entered after the capped distribution failed
  to meet the target. That is intended recovery policy, not an over-broad first-pass
  relaxation.

No new current defect was found. F724 / provisional KI#1045 remains unused.

NEXT
----
WIP749 physical -> F724/KI#1045 UNUSED -> practical 6304/KI#564-565 -> audit grouped
OneArea copies against selected-area identity, map-wide target and per-area cap; verify
area-suitability latitude policy exactly matches actual placement and later KI#568-571
fallback behavior -> whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP749
===============================================================================

===============================================================================
C031-WIP750 — PRACTICAL 6304 ONEAREA PLACEMENT REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6304 / KI#564 + KI#565 — REPAIR-CLEAN
------------------------------------------------
Practical 6304 (`2db0794b155e2623a2418bc397bb51ac9d2d656b`) repairs grouped
OneArea placement bounds and area-ranking latitude policy. Both remain coherent after
the later KI#566-KI#572 map-generation repairs.

KI#564 / original F241 — grouped OneArea copies
------------------------------------------------
After a legal cluster center is placed, the repair derives the helper allowance from
the already-updated live state:

    iTargetRemaining = iTarget - map.getNumBonuses(eBonus);
    iGroupLimit = bIgnoreAreaLimit ? iTargetRemaining :
        min(iTargetRemaining, iAreaLimit - iAdded);

The center has already incremented both map count and `iAdded`, so the helper receives
exactly the remaining map-wide allowance and, during the capped pass, the remaining
per-area allowance. The enclosing loop only admits a center while the corresponding
limits still permit it, so these quantities are nonnegative; zero simply suppresses
extra grouped copies.

`placeGroup` additionally rejects a grouped plot outside the center's exact CvArea for
OneArea resources. This restores the BtS/K-Mod selected-landmass invariant while leaving
non-OneArea clustering unchanged. Current map topology is coherent before bonus
placement through KI#572, so the same-area test is not consuming the stale random-lake
area state formerly owned by F249.

KI#565 / original F242 — map-script latitude policy
---------------------------------------------------
The cheap per-area suitability scan now passes the same `bIgnoreLatitude` value obtained
from the map script that the subsequent real `canPlaceBonusAt` call uses. Thus scripts
such as Hub/Wheel/BTG variants no longer rank polar plots under one latitude contract
and place them under another.

The scan still deliberately disables the expensive range check. That can make ranking
optimistic when same-class/range spacing later rejects all actual slots, but the real
placement remains authoritative and the `areas_tried` loop can move on to another area.
This is inherited performance/ranking design, not a repair-side legality mismatch.

COMPOSITION WITH LATER REPAIRS
------------------------------
KI#568 only relaxes the small-island hard exclusion in pass two; KI#569 corrects the
revisited-area denominator; KI#571 rescues zero-ranked but actually eligible areas.
None bypass KI#564's map-wide/group/per-area allowance, and all continue to use the
KI#565 latitude-consistent suitability/placement contract.

No independent current defect was found in practical 6304. F724 / provisional KI#1045
remains unused.

NEXT
----
WIP750 physical -> F724/KI#1045 UNUSED -> practical 6303/KI#538-539 -> audit UWAI
leader reassignment and Permanent-Alliance cache migration across absorbed/surviving
players, ownership maps and serialized/persistent state -> whole-album dedup before any
new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP750
===============================================================================

WIP756/WIP771 FORK RECONCILIATION
---------------------------------
The two supplied chains share authoritative history through WIP750, then diverge.
The resumed WIP771 chain is authoritative: it retains F724/KI#1045 from the alternate
WIP756 tail, replaces that tail's premature F725-F727 promotions with deeper
current-source and whole-album-dedup closures that keep F725 unused, and continues
the chronological repair-history pass through physical checkpoint C031-WIP771.
Do not append or resume from the alternate WIP756 tail.

===============================================================================
C031-WIP751 — PRACTICAL 6303 UWAI LEADER/PA CACHE MIGRATION REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F723 remain confirmed.
F724 / provisional KI#1045 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6303 / KI#538 + KI#539 — REPAIR-CLEAN
------------------------------------------------
Practical 6303 repairs two related but distinct UWAI ownership boundaries during live
team-leader replacement and Permanent-Alliance absorption. The repair-side audit
included the intermediate leader changes caused by each absorbed player's `setTeam()`
and composition with later KI#551 target-key migration.

KI#538 / original F215 — newly promoted team leader state
----------------------------------------------------------
`CvTeam::updateLeaderID()` invokes `onTeamLeaderChanged(eFormerLeader)` immediately on
each newly selected living leader. The repaired helper explicitly fills the new
leader's private `m_abCanBeHiredAgainst` map FALSE, rather than using its TRUE default,
and copies the other fields that are actually accessed through `leaderCache()`:
`m_aiWarUtilityIgnoringDistraction` and human-target capitulation readiness.

This remains correct even when Permanent-Alliance player reassignment changes the
surviving team's leader more than once. Each `setTeam()` updates the surviving team's
leader immediately; if a later absorbed player becomes leader, migration chains from
the just-current leader into that new leader. Thus no intermediate transition exposes
a subordinate player's stale/default team-leader state.

The repair also intentionally does NOT blindly copy CanBeHired from the former leader:
the original F215 contract requires the promoted leader to start FALSE until normal
UWAI planning recomputes willingness.

KI#539 / original F216 — PA merge recipient boundary and target domain
----------------------------------------------------------------------
Before any absorbed player is reassigned, `CvTeam::addTeam()` saves the absorbed team
leader and calls `AI().uwai().addTeam(eTeamLeader)`. At that moment `MemberAIIter` over
the surviving team contains only its original members, so the absorbed leader cannot
add its history into itself and other absorbed members are not accidentally included.

`UWAICache::addTeam()` now merges across the full persistent `CivTeam` target-key domain,
not only currently alive major teams. This preserves histories/obligations keyed to a
dead-but-revivable civilization team, exactly matching the later refinement recorded
under F216.

INTERMEDIATE LEADER-CHANGE COMPOSITION
--------------------------------------
The persistent values merged by KI#539 (`m_aiPastWarScore`, sponsorship and bounty)
are ordinary per-player cache state: their public accessors read the owning cache
directly rather than `leaderCache()`. Therefore the repeated `updateLeaderID()` calls
during absorbed-player reassignment do not overwrite or re-copy the KI#539 history
merge. Only the genuinely leader-owned fields listed above are migrated by KI#538.

If the absorbed team leader or another absorbed player becomes the final surviving-team
leader, that does not make the history merge reciprocal by accident; original surviving
members retain the imported absorbed-leader history, while absorbed players retain their
own player histories. This is the deliberate one-way PA merge model recorded by F216,
not a missing leader-state copy.

COMPOSITION WITH LATER KI#551 / ORIGINAL F228
---------------------------------------------
After all absorbed players have joined the surviving team, current `CvTeam::addTeam()`
performs outsider target-key migration through `onTargetTeamAbsorbed`, but explicitly
skips every player whose current team is the surviving alliance. Consequently this
later absorbed-team -> surviving-team key rewrite cannot re-merge KI#539 history into
alliance members or double a bounty/past-war score already handled at the pre-merge
recipient boundary.

For outsiders, `onTargetTeamAbsorbed` adds absorbed past-war history once to the
surviving key, keeps the stronger existing/absorbed sponsored-war obligation, clears
the dead absorbed key, and migrates human-capitulation readiness where applicable.
This complements rather than duplicates KI#539.

No independent repair-side defect was found in practical 6303. F724 / provisional
KI#1045 remains unused.

NEXT
----
WIP751 physical -> F724/KI#1045 UNUSED -> practical 6302/KI#551 -> audit outsider
persistent target-key migration from its own repair-side perspective: conflict policy,
dead-key clearing, sponsor identity/lifetime, capitulation-readiness migration, and PA
ordering versus war transfer/peace reporting -> whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP751
===============================================================================

===============================================================================
C031-WIP752 — F724 / KI#1045 CONFIRMED: KI#551 PA MIGRATION RUNS AFTER TRANSIENT UWAI STATE IS CLEARED
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 are now confirmed.
F725 / provisional KI#1046 is UNUSED.
No gameplay/XML/Python source was modified.

F724 / provisional KI#1045
--------------------------
TITLE
    Practical 6302's KI#551 outsider Permanent-Alliance migration runs after the
    absorbed team's zero-alive war teardown has already erased sponsored-war and
    human-capitulation state, so two of the repair's advertised persistent/transient
    migrations still fail in the exact supported PA transfer that motivated KI#551.

STATUS
    CONFIRMED CURRENT ADVCIV-SAS REPAIR-SIDE REGRESSION / INCOMPLETE KI#551 REPAIR.
    F228/KI#551 remains the inherited AdvCiv root. F724 owns the ordering defect in the
    SAS repair introduced by practical 6302 and retained by current source; later KI#562
    makes the pre-migration cleanup explicit/unconditional rather than repairing it.

ROOT / EXACT ORDERING
---------------------
Practical 6302 added `UWAICache::onTargetTeamAbsorbed(survivor, absorbed)` near the end
of `CvTeam::addTeam`, after all absorbed players have been reassigned to the surviving
team.

But each absorbed `CvPlayer::setTeam()` first decrements the OLD team's alive count.
When the last living absorbed player leaves, `CvTeam::changeAliveCount(-1)` reaches zero
and, for every live major enemy still at war with the absorbed team, executes the normal
war-ending lifecycle while the old war flags still exist:

    if (kLoopTeam.isAtWar(getID()) && getUWAI().isEnabled())
        kLoopTeam.uwai().reportWarEnding(getID());

Only afterward does it clear the old team-war state.

`UWAI::Team::reportWarEnding(absorbed)` immediately clears leader-owned human
capitulation readiness for that target, and then every member cache calls
`UWAICache::reportWarEnding(absorbed)`. Current `UWAICache::reportWarEnding` begins by
calling `clearWarSponsorship(absorbed)`, which sets:

    bounty[absorbed] = 0
    sponsorPerTarget[absorbed] = NO_PLAYER

Thus by the time practical 6302's later `onTargetTeamAbsorbed` loop examines the outside
AI cache, the very sponsorship/bounty and readiness entries it intends to migrate have
already been erased.

CONCRETE KI#551 REPRODUCTION SURVIVES
-------------------------------------
Let A be the surviving PA team, B the absorbed team, C an outside AI hireling and D a
sponsor. D has paid C to declare war on B, so C stores a positive bounty/sponsor under
team B. A and B later form a Permanent Alliance while C's B war is still active.

`CvTeam::addTeam` first transfers B's differing war to A. During the subsequent absorbed
player reassignment, B's final alive-count drop invokes C's normal
`reportWarEnding(B)`, which clears `sponsorAgainst(B)` and `bountyAgainst(B)`. The later
KI#551 helper therefore sees no obligation to move to A. C remains physically at war
with A but loses HiredHand's intended paid-war persistence exactly as in original F228.

The same timing defeats KI#551's human-capitulation migration. If C had
`readyToCapitulateTo(B)` for a human B, `UWAI::Team::reportWarEnding(B)` clears that bit
before `onTargetTeamAbsorbed` can erase/move it to the final human surviving team.

PAST-WAR HISTORY IS DIFFERENT
-----------------------------
This ordering does NOT erase every KI#551 field. `reportWarEnding(B)` computes/updates
past-war history before the later migration, so `onTargetTeamAbsorbed` can still add the
final `m_aiPastWarScore[B]` into the surviving A key and clear B. The repair is therefore
partially effective: final historical score migrates, while the transient sponsored-war
obligation and capitulation handshake do not.

SPONSOR-BECOMES-TARGET NEGATIVE CONTROL
---------------------------------------
A separate edge was tested and is not the root. If the sponsor itself belongs to
surviving A and had hired C against B before A+B merge, a mechanically migrated sponsor
would now belong to C's war target team. `HiredHand::eval()` explicitly returns zero
when the hireling is at war with the sponsor's team, so such an obligation cannot force
C to keep fighting the sponsor's own merged alliance. This does not rescue the ordinary
external-sponsor reproduction above.

ANCESTRY / LATER-REPAIR COMPOSITION
-----------------------------------
Practical 6302 (`646137c86584e933b2306c9ff818d8cb5ad4659e`) introduced the outsider
migration after player reassignment. Practical 6303 later moved only the separate
survivor-member KI#539 merge before reassignment; it did not move KI#551.

Later KI#562 repaired elimination war-ending cleanup by making sponsorship clearing
explicit even when full UWAI war-history evaluation is unavailable. Current
`changeAliveCount` therefore still performs the same zero-alive cleanup before KI#551,
and current `reportWarEnding` unconditionally starts with `clearWarSponsorship`.

WHOLE-ALBUM DEDUP
-----------------
F228/KI#551 is the inherited one-way PA target-key migration omission and explicitly
proved sponsorship/past-war/readiness consequences. No later numbered owner covers the
new SAS ordering defect where the repair's source data is destroyed before its helper
runs. F724 is therefore a repair-side child of F228 rather than a renumbering of the
inherited root.

REPAIR BOUNDARY
---------------
Preserve the transient absorbed-target state before the last absorbed player's
zero-alive teardown can clear it, while still allowing the FINAL war-ending past-war
score to be migrated after teardown.

A safe implementation should not merely move the current helper wholesale before
player reassignment: doing so would migrate pre-war-ending history and can mis-detect
the final surviving team's human status before absorbed humans join. Prefer either:

  * snapshot outsiders' absorbed-target sponsor/bounty and capitulation-readiness before
    reassignment, then perform the existing final-key migration after zero-alive teardown
    using those snapshots while reading the final past-war score from the cache; or
  * explicitly suppress/transfer the transient cleanup for PA-transferred wars and then
    run one final migration after reassignment.

Keep the outsider direction separate from KI#539's survivor-member merge and preserve
KI#551's existing higher-bounty conflict policy when both target keys already contain
sponsorship.

NEXT
----
WIP752 physical -> F724/KI#1045 CONFIRMED -> F725/KI#1046 UNUSED -> finish any remaining
practical-6302 sibling/negative controls -> classify/open practical 6301 from local
diffs -> continue repair-side audit in descending chronology with whole-album dedup.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP752
===============================================================================

===============================================================================
C031-WIP753 — PRACTICAL 6301 KI#549 SECONDARY-COLONY REACHABILITY REPAIR CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6301 / KI#549 — REPAIR-CLEAN
--------------------------------------
Practical 6301 (`5daa6b4076c0904abb5b106a783e6e35e8cf4c0a`) separates strategic
land/sea reachability from selection of the fastest pairwise deployment duration in
`UWAICache::City::updateDistance`.

The repair-side audit confirms that the two decisions now remain independent:

* a land path from a secondary/non-primary owner area can still contribute its numeric
  duration, but `bQualifyingLandRoute` is false and therefore it does not establish
  `m_bReachByLand`;
* because that route does not qualify strategically, the independent sea-route search
  is still executed even when the secondary land duration is already short;
* finding a positive valid water path sets `bReachBySea=true` immediately, regardless
  of whether the sea duration beats the already-known land duration;
* the mixed pairwise duration is replaced only when the sea route is actually faster.

This is exactly original F226/KI#549's contract: route existence answers strategic
reachability, while route speed chooses the numeric deployment estimate.

The helper's documented inherited limitations for canal-only access and inland cities
more than one tile from coast remain unchanged. The repair does not reinterpret a route
that the existing water pathfinder rejects; it merely stops a valid found route from
being suppressed by a faster nonqualifying land route.

COMPOSITION WITH LATER KI#591
-----------------------------
Current source additionally caches a qualifying land-only deployment metric for
KI#591. It records each qualifying land duration before any faster sea route can replace
the mixed pairwise value. Thus the later repair composes with KI#549: secondary-area
land still cannot prove land reachability, a valid slower sea route can prove strategic
reachability, and consumers that explicitly require land-only distance no longer read
the mixed sea-favored metric.

No independent current repair-side defect was found in practical 6301. F725 /
provisional KI#1046 remains unused.

NEXT
----
WIP753 physical -> F725/KI#1046 UNUSED -> classify/open practical 6300 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP753
===============================================================================

===============================================================================
C031-WIP754 — PRACTICAL 6300 KI#548 REVIVAL INVENTORY + KI#552 BOUNTY SATURATION CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6300 / KI#548 + KI#552 — REPAIR-CLEAN
------------------------------------------------
Practical 6300 (`1f3e899b9d53f406e03e97d77e85ab3e571c0118`) repairs the exceptional
revival military-inventory lifecycle and sponsored-war bounty narrowing overflow.

KI#548 / original F225 — revived-player military inventory
----------------------------------------------------------
`CvPlayer::setAlive(true)` reaches `initRevivedPlayerInGame` only for an ever-alive,
non-colony player after the player/team alive state and AgentIterator revival boundary
have been established. The helper initializes the player's previously absent/deleted
UWAI owner/cache and immediately calls `rebuildMilitaryUnitCounts()` before
`processNewPlayerInGame()` exposes the cache to later UWAI consumers.

The rebuild asserts that the newly initialized incremental counts are empty and scans
all units currently owned by the revived player once, replaying the ordinary
`reportUnitCreated(UnitTypes)` classification. There is no intervening unit-creation
callback between cache init and this scan, so the unit that caused revival is recovered
without being counted twice; any other existing units are reconstructed through the
same Army/Fleet/Nuclear/Logistics/non-naval rules as ordinary incremental creation.

Team revival remains separately owned by `CvTeam::changeAliveCount`; the player helper
does not reinitialize a surviving teammate's UWAI state.

KI#552 / original F229 — compact bounty overflow
-------------------------------------------------
The full-width sponsored-war utility is now computed in `int`, saturated to
`[0, MAX_SHORT]`, and only then assigned to the signed-short `m_aiBounty` map. Supported
in-range values are unchanged. Zero/negative values still clear the sponsor as before;
out-of-range positive overpayments become the maximum representable obligation instead
of wrapping negative and erasing themselves.

This composes with later PA sponsorship conflict handling: comparisons see a stable
nonnegative value. Two payments both above the compact range may saturate equally, but
that loses only ordering beyond the chosen storage precision; it cannot invert a larger
payment into a smaller/negative obligation or clear the sponsor.

No independent current repair-side defect was found in practical 6300. F725 /
provisional KI#1046 remains unused.

NEXT
----
WIP754 physical -> F725/KI#1046 UNUSED -> classify/open practical 6299 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP754
===============================================================================

===============================================================================
C031-WIP755 — PRACTICAL 6299 KI#543-544 WAR-SIDE / MISSION-CENSUS REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6299 / KI#543 + KI#544 — REPAIR-CLEAN
------------------------------------------------
Practical 6299 (`d91753ea58f0fa15a154ea12a36ca423043943d0`) repairs UWAI nuclear
deterrence scope and the BBAI/K-Mod boarded-cargo mission census.

KI#543 / original F220 — owner war-side nuclear deterrence
-----------------------------------------------------------
`UWAICache::teamThreat` already computes owner defensive long-term power on the cache
owner's master side. The repair caches that same master-team identity and uses
`CvTeam::getNumNukeUnits()` for the existing nuclear-deterrence term.

Current `CvTeam::getNumNukeUnits()` sums all direct team members and all living vassal
players of that master. Thus the deterrence and defensive-power terms now use the same
war-side coalition: a teammate/vassal nuclear arsenal cannot disappear merely because
the particular cache-owning player has no nuke units.

KI#544 / original F221 — physical target-mission census
--------------------------------------------------------
K-Mod keeps loaded cargo in its own boarded groups while each transport also exposes
that cargo through `getCargo()`. The repaired census therefore counts:

    transport/group cargo count
    + each group member only when !unit.isCargo()

Every ordinary owner-controlled physical unit contributes once. Loaded cargo is counted
through its transport and skipped in its own boarded group; unloaded/noncargo members
remain counted through their own group. Both `AI_enemyTargetMissions` and
`UWAICache::updateTargetMissionCount` use the same rule.

At team-level consumers, summing per-member `AI_enemyTargetMissions` remains physically
correct even for manually cross-owner teammate cargo: such a unit is counted once via
the transport owner's cargo and skipped in its own boarded group. The per-player cache
could attribute a manually teammate-loaded unit to the transport owner, but the AI's
normal transport selection does not create cross-owner cargo; this is the same broader
team/manual-cargo robustness edge already parked during the KI#706 review, not an
independent current KI#544 repair regression.

No independent current defect was found in practical 6299. F725 / provisional KI#1046
remains unused.

NEXT
----
WIP755 physical -> F725/KI#1046 UNUSED -> classify/open practical 6298 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP755
===============================================================================

===============================================================================
C031-WIP756 — PRACTICAL 6298 KI#547 + KI#550 CITY-CACHE LIFECYCLE REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6298 / KI#547 + KI#550 — REPAIR-CLEAN
------------------------------------------------
Practical 6298 (`c909ca55d8f78e9915baad03f289c3bfcba691d1`) restores two immediate
UWAI city-cache invariants during mid-turn creation/destruction.

KI#547 / original F224 — cache-owner total assets
--------------------------------------------------
City destruction reports into every major AI cache while the old `CvCity` object remains
valid. `remove()` first erases the city wrapper from the detailed cache; only then does
an owner cache call `updateTotalAssetScore()`. That aggregate iterates the remaining
cached own cities, so it does not depend on the owner's still-not-yet-deleted city
container or accidentally retain the dismantled city.

Creation mirrors the transaction: the new wrapper is added, the required attack-priority
order is restored, and only the cache whose `m_eOwner` owns the new city recomputes its
own total assets. Observers' holdings did not change and therefore need no aggregate
refresh.

KI#550 / original F227 — destruction ordering
----------------------------------------------
`remove()` now returns whether this cache actually contained and erased the city.
`reportCityDestroyed` re-sorts only those affected caches, restoring the greedy
area-dependent attack-priority order even when a raze has no later creation callback.

The helper validates both `m_cityMap` and `m_cityList` before mutating either. Ordinary
removal then adjusts reachable-city count, erases the map entry, deletes the wrapper and
erases the vector entry as one coherent transaction; a debug-only impossible index
mismatch is left intact rather than partially removed.

LATER FINDING DEDUP
-------------------
A foreign city-set change can also alter surviving cities' target values through owner
city count/production rank. Current practical 6373's later narrowed KI#554 observer
rebuild leaves that state stale after noncapital loss; this is already confirmed as
F717/KI#1038. It is not a failure of KI#550's narrower ordering repair and is not
renumbered here.

No independent current defect was found in practical 6298. F725 / provisional KI#1046
remains unused.

NEXT
----
WIP756 physical -> F725/KI#1046 UNUSED -> classify/open practical 6297 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP756
===============================================================================

===============================================================================
C031-WIP757 — PRACTICAL 6297 KI#545-546 SCALEDNUM REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6297 / KI#545 + KI#546 — REPAIR-CLEAN
------------------------------------------------
Practical 6297 (`7574ccf3a13543a7f2f6d87e124f2fa17db3fd5c`) restores two algebraic
contracts lost in AdvCiv's ScaledNum rewrite.

KI#545 / original F222 — expansion-site opportunity value
----------------------------------------------------------
The repaired expression restores the original `siteWeight + settlers/10` model:

    aiFoundVals[i] * (arSiteWeights[i] + scaled(iSettlers, 10)) / 2500

The loop's separate `i = iSettlers` starting index remains the inherited rule that
current Settlers already cover the best candidate sites; it is not a duplicate numeric
Settler adjustment. Site weights remain 1.0/0.8/0.6/0.4/0.2, Barbarian-city additions,
Raging-Barbarian scaling, city-count normalization and the final production-value cap
remain unchanged.

KI#546 / original F223 — war-trade hireability persistence
-----------------------------------------------------------
For an already-hireable target the repair restores:

    1 - (threshold - utility) / 100

through `1 - scaled(iUtilityThresh - iWarUtility, 100)`. At the threshold this is 1.0;
ten utility below it is 0.9; values above threshold are >=1 and therefore guaranteed to
persist; sufficiently low values are <=0 and clear. This matches the original double
probability curve and leaves the distinct FALSE-to-TRUE probability/cap unchanged.

No second current repair-side defect was found in practical 6297. F725 / provisional
KI#1046 remains unused.

NEXT
----
WIP757 physical -> F725/KI#1046 UNUSED -> classify/open practical 6296 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP757
===============================================================================

===============================================================================
C031-WIP758 — PRACTICAL 6296 KI#540-542 UWAI CROSS-LAYER / VICTORY REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6296 / KI#540 + KI#541 + KI#542 — REPAIR-CLEAN
---------------------------------------------------------
Practical 6296 (`3e8eced21ad3f3f7db9eccf2ae46a233779b6de2`) repairs exact Fallout
identity, transport trainability and cumulative victory-stage ordering.

KI#540 / original F217 — exact Fallout identity
------------------------------------------------
`updateCanScrub` now resolves literal `FEATURE_FALLOUT` instead of choosing the first
feature at or below a mutable health threshold. Earlier effective-asset validation in
this post-pass proved all active literal C++ `getInfoTypeForString` lookups resolve in
the shipped stack, so the repair no longer depends on SAS Jungle health or XML order.
The broader existing question—whether any known Build can remove Fallout—remains
unchanged.

KI#541 / original F218 — actual city-trainable assault transport
----------------------------------------------------------------
The repair keeps the cheap `CvPlayer::canTrain` prefilter and then requires one actual
owned `CvCity::canTrain(eUnit)` before setting trainable transport capability. This
captures connected resources and sea-unit placement/coastal requirements while
remaining explicitly a *future trainability* cache rather than an inventory of already
possessed ships.

`UWAICache::update()` refreshes transport capability every normal cache update and does
so before rebuilding city distance/reachability state. Resource/city changes therefore
feed the stricter result on the same cadence as the previous player-level test; no new
repair-specific stale window is introduced.

KI#542 / original F219 — cumulative victory bits
-------------------------------------------------
Stage 4 is now tested before stage 3 for both owner and rivals, restoring the intended
highest-stage classification under cumulative victory hashes. The owner side still
intentionally values peaceful Culture/Space production opportunity; rival helper scope
remains the broader inherited victory-threat policy. Only unreachable ordering was
changed.

No independent current defect was found in practical 6296. F725 / provisional KI#1046
remains unused.

NEXT
----
WIP758 physical -> F725/KI#1046 UNUSED -> classify/open practical 6295 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP758
===============================================================================

===============================================================================
C031-WIP759 — PRACTICAL 6295 KI#536-537 UWAI RESET / TYPICAL-UNIT REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6295 / KI#536 + KI#537 — REPAIR-CLEAN
------------------------------------------------
Practical 6295 (`f21aa3ba4a1f813fdf8898b278040ba6fa18a5d9`) repairs full-reset
capitulation readiness and candidate-to-none MilitaryBranch refreshes.

KI#536 / original F213 — full cache reset
-----------------------------------------
`m_readyToCapitulateTo` is cleared only inside `clear(false)`, alongside the other
state that is event-maintained rather than recomputed by ordinary per-turn refresh.
`UWAICache::read()` first restores `m_eOwner`, then calls the default full `clear()` and
only afterward deserializes the saved readiness set; stale process/session contents
therefore cannot union with the save. Init/uninit receive the same full reset, while
ordinary `update()` uses `clear(true)` and correctly preserves the current-war handshake.

KI#537 / original F214 — typical trainable military candidate
--------------------------------------------------------------
The base `MilitaryBranch::updateTypicalUnit()` and the independent NuclearArsenal
override now invalidate `m_eTypicalUnit` and `m_rTypicalPower` before any early return or
candidate scan. Losing every valid trainable candidate therefore produces the valid
NO_UNIT/zero-power state instead of retaining the prior turn's capacity.

Fleet runs the base refresh first and then recomputes `m_bCanBombard` directly from the
new typical unit, so candidate-to-none also clears the former ship's bombard capability.
These fields describe forecast trainability only; existing-unit branch inventories and
actual military power are not erased by the refresh.

No independent current repair-side defect was found in practical 6295. F725 /
provisional KI#1046 remains unused.

NEXT
----
WIP759 physical -> F725/KI#1046 UNUSED -> classify/open practical 6294 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP759
===============================================================================

===============================================================================
C031-WIP760 — PRACTICAL 6294 KI#527 + KI#534 DEBUG-ASSERTION REPAIRS CLOSED CLEAN
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6294 / KI#527 + KI#534 — REPAIR-CLEAN
------------------------------------------------
Practical 6294 (`5e5f9ee12e9c993e2d754ba871f9df7bfd3d4773`) repairs two inherited
assertion/diagnostic paths without changing ordinary simulation.

KI#527 / original F204 — Shift-hover air stack comparison
---------------------------------------------------------
The repaired Debug-mode Shift row omits `AI_compareStacks` only for an air-domain
selection. This is sufficient despite `AI_sumStrength` itself rejecting both AIR and
IMMOBILE: `AI_compareStacks` preserves its group's own domain only when it is AIR; every
non-air group is remapped to the hovered plot's LAND or SEA domain before the strength
helper is called. Thus no supported immobile sibling still reaches the forbidden domain
through this caller.

Ordinary air combat help and ACO remain untouched; only the unsupported diagnostic
Stack Compare row is suppressed.

KI#534 / original F211 — transient empty selection groups
---------------------------------------------------------
`AI_enemyTargetMissions` now skips a registered group whose unit count is zero before
reading its retained MissionAI plot or querying `AI_isDeclareWar`. Such a group has no
physical units or cargo to contribute, so the skip cannot alter a valid census; it only
avoids the inherited null-head assertion during the supported separation-before-delayed-
death lifecycle.

No independent current repair-side defect was found in practical 6294. F725 /
provisional KI#1046 remains unused.

NEXT
----
WIP760 physical -> F725/KI#1046 UNUSED -> classify/open practical 6293 from local diff
archive -> continue descending repair-side audit with current-source proof and
whole-album dedup before any new number.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP760
===============================================================================

===============================================================================
C031-WIP761 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6293 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP760.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6293 / KI#521-KI#524.6 -- REPAIR-SIDE AUDIT CLOSED CLEAN
------------------------------------------------------------------
Commit 617843aa2bc9c108530719f7783c6467b1c85ea7 was re-audited as one
crash-hardening/state-progress transaction rather than as isolated guards.

1. KI#524.6 root ownership is repaired at the correct layer.
   CvPlayer::initUnit now rejects NO_UNIT before m_units.AI_add(). The inherited
   Partisans failure therefore cannot allocate a reset/unplaced container entry and
   then fault while initializing it. A current-tree search found no supported literal
   initUnit(NO_UNIT) caller. Native dynamic callers either prove a valid UnitType
   before creation or already handle NULL from allocation/creation failure, so the
   new early return tightens an invalid contract rather than removing a supported
   sentinel-object behavior.

2. The Partisans Python choices now use one coherent valid-conscript contract.
   Help avoids UnitInfo(NO_UNIT); canApplyPartisans1/2 reject absent capitals or
   NO_UNIT conscripts; applyPartisans1/2 revalidate at application time; and each
   application caches the validated UnitType locally for all spawns. If the required
   capital/conscript disappears between popup eligibility and application, the event
   has no other XML effect that needs partial application, so returning without a
   spawn is the safe state-change outcome rather than another corrupt creation.

3. KI#524.2-KI#524.5/.6 downstream guards match their real domains.
   - verifySpyUnitsValidPlot ignores unplaced objects before isSpy/plot ownership and
     then preserves the placed-Spy border test.
   - AI_doTurnUnitsPost excludes unplaced objects before the cached upgrade vector;
     every legitimate upgrade candidate is placed and later passes assume plot/city/
     group state.
   - shouldShowEnemyGlow returns false before UnitInfo-backed domain/combat access for
     an object with no plot; no plot means no glow location.
   - SASGameRecord and UWAI military-posture helpers similarly reject unplaced objects
     before combat classification because unplaced units are not deployable posture.
   These remain useful defense-in-depth for corrupt/legacy state even though the
   central NO_UNIT creation root is now prevented.

4. KI#522 and KI#524 repair distinct proven dimensions of the SAS KI#319 no-progress
   tripwire and compose with the later KI#525/KI#526 repairs.
   CvSelectionGroup::getCargo is exactly the sum of live transport-member cargo
   counts, so inherited missionless unload changes it while membership can remain
   constant. getNumUnits independently observes AdvCiv's deliberate non-head bombard
   split. Current source additionally observes queued-group-attack state and the sum
   of all members' moves. The fallback MISSION_SKIP now fires only when all proven
   progress dimensions remain unchanged. Legitimate unload, split, non-head movement
   or attack consumption re-enters inherited evaluation; genuine no-progress remains
   bounded by the existing attempt limit.

5. KI#521 remains the narrow correct debug fix. Human Bombard can legally target an
   undefended city; removing only the stale iDefenders>0 assertion preserves that
   gameplay, while the existing std::max(iDefenders,1) keeps the later ratio safe.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP761
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6292 from the local commit-diff archive, audit only its
live simulation/AI changes against current source and whole-album owners, and freeze
WIP762 at the next genuinely settled boundary.
===============================================================================

===============================================================================
C031-WIP762 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6292 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP761.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6292 / KI#503-KI#505 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit 61e14a5a947574a86bab81ad012addfa7097cc8d was challenged against current
CitySiteEvaluator/CvPlot behavior and the original F180-F182 repair boundaries.

1. KI#503 now matches ordinary current irrigated-improvement yield at the narrow
   contract it promised. SAS_getBonusImprovementFoodChange adds the improvement's
   irrigated food iff isIrrigationAvailable() is currently true, exactly like
   CvPlot::calculateImprovementYieldChange. It does not invent future irrigation-chain
   availability, preserving F180's deliberately narrow repair boundary.

2. KI#504 replaces the false empire-state capability proxy with the correct medium-
   term site-evaluation capability test. estimateImprovementProduction scans Builds
   that actually produce an improvement, requires each Build tech and any applicable
   feature-removal tech, then supplies that exact Build to canHaveImprovement for the
   candidate plot's terrain/feature/irrigation legality. This intentionally remains a
   future-site capability heuristic rather than present-worker CvPlot::canBuild; the
   old function already used canHaveImprovement and did not promise present ownership,
   Worker or Python-callback legality.

   Multiple Builds mapping to one improvement do not double-count because the result
   uses scaled::increaseTo rather than addition. A locked feature-removal variant is
   skipped without suppressing another currently valid Build variant. Current shipped
   Python does not provide a player-specific canBuild override that this site heuristic
   would need to model.

3. KI#505 is diagnostic-only and now logs countBadTiles' freshly computed reference
   value iUnrevealed before the caller later copies it into member iUnrevealedTiles.
   Gameplay count/scoring remains unchanged.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP762
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6291 from the local commit-diff archive and audit its
live simulation/AI changes against current source and whole-album owners.
===============================================================================

===============================================================================
C031-WIP763 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6291 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP762.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6291 / KI#487-KI#490 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit 4641cf5d384f948c8f02fa28ad69b75c37050ec9 was re-audited against the
current AIFoundValue ordering and the actual CvCity culture-spread engine.

1. KI#487 correctly removes only the false different-area=zero-pressure assumption.
   CvCity::doPlotCultureTimes100 uses culture level + CvCity::plotCultureExtraRange()
   (currently +3) as the broad same-area range and an explicit culture level + 1
   ceiling across a different CvArea. The found-value logic is deliberately a rival
   pressure/proximity heuristic rather than an exact next-turn culture depositor:
   inherited same-area scoring already uses the broad +3 scale even where current
   ownership can suppress outer-ring deposition. Therefore retaining +3 same-area and
   using the engine's narrower +1 geographic ceiling cross-area matches F164's repair
   contract without overclaiming exact culture simulation. Existing knowledge gates
   remain before scoring.

2. KI#488 correctly makes cautious Forest/Jungle health independent of whether a
   visible bonus shares the feature. Bonus presence does not remove the current
   feature or its health modifier. Flood Plains remain deliberately excluded from
   this simplified cautious-health accumulator exactly as documented.

3. KI#489 now derives SAS first-city gates from AIFoundValue's already-normalized
   iCities. The constructor sets iCities=5 for Barbarians, so bStartPhase cannot
   reintroduce a raw zero-city Barbarian-capital distinction; ordinary players still
   use their real city count.

4. KI#490 now runs the SAS preliminary BFC quality counters only after the established
   isUsablePlot contract admits the tile. Reserved planned-site overlap and rejected
   city-radius sharing therefore cannot influence hard first-city or later quality
   counters while contributing no ordinary site value. Fog semantics are preserved:
   isUsablePlot rejects unrevealed tiles and iUnrevealedTiles remains the dedicated
   separate fog channel, just as the pre-repair counters had already required revealed
   plots before classification. Usable/allowed sharing can still be classified, which
   is consistent with the main evaluator treating such plots as usable rather than
   unavailable.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP763
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6290 from the local commit-diff archive, trace its live
simulation/AI changes against current source and whole-album owners, and freeze WIP764
at the next settled boundary.
===============================================================================

===============================================================================
C031-WIP764 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6290 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP763.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6290 / KI#484-KI#486.2 -- REPAIR-SIDE AUDIT CLOSED CLEAN
------------------------------------------------------------------
Commit 7302a07c38c6b0c510d2f0561aaf23401463e254 was re-audited across the
future-BFC resource helpers and CvCity::liberate lifetime boundary.

1. KI#484 removes the unrelated candidate-city-area gate from rival future-BFC
   competition. plotDistance to the bonus plot itself remains the geometric selector;
   Civ4's plotDistance/CITY_PLOTS_RADIUS combination excludes the unusable 5x5 corners
   and admits the ordinary 21-tile BFC reach, including coastal-city competition for
   water bonuses across a CvArea boundary. Existing known-city/no-cheat gates remain.

2. KI#485 correctly treats one contested copy as insufficient evidence and continues
   searching other matching occurrences. It returns true only when at least one copy
   is safely claimable (or already teammate-owned), making the empire-wide duplicate
   suppression independent of city/plot iteration order without changing local tile
   value for contested resources.

3. KI#486's connected-permanent-resource algebra matches the engine's plot-group
   ledger exactly. Capital plot-group bonus accounting physically adds imports and
   subtracts exports, so:

       available + exports - imports

   reconstructs connected non-trade supply. Temporary imports can no longer erase the
   value of acquiring a first permanent copy, while an exported connected owned/free
   copy still counts as already supplying the empire. The subsequent future-BFC scan
   remains responsible for unconnected but safely claimable copies.

4. KI#486.2 repairs the CvCity lifetime at the true ownership boundary. liberate()
   caches eOldOwner/eOldTeam before acquireCity(this,...), and every city-dependent
   operation (name/message/visibility/liberation memory) occurs before that call.
   After acquireCity destroys/replaces the old CvCity, there is no remaining `this`,
   plot(), name or member dereference: only cached player/team IDs, stable team/player
   objects and scalar pre-transfer land values are used for the vassal-power and
   attitude updates. The repair therefore covers the reproduced crash and the later
   stale attitude dereference without moving pre-transfer work across the destruction
   boundary.

5. Current later repairs compose cleanly with this commit: remembered ownership
   (KI#496) feeds the future-BFC scan, while the KI#485 search semantics continue to
   exhaust contested copies before returning false.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP764
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6289 from the local commit-diff archive and audit its
live simulation/AI changes against current source and whole-album owners.
===============================================================================

===============================================================================
C031-WIP765 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6289 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP764.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6289 / KI#481-KI#483 -- REPAIR-SIDE AUDIT CLOSED CLEAN
----------------------------------------------------------------
Commit fa6e8970f647ab9bad15f7634aa1569f7eab8b89 was re-audited against current
AIFoundValue and CvTeam sentinel/domain semantics.

1. KI#481 now treats the Build prerequisite and feature-removal prerequisite as two
   independent near-future requirements. CvTeam::isHasTech(NO_TECH) explicitly returns
   true, so a missing prerequisite is satisfied rather than becoming an invalid lookup.
   A known prerequisite plus a member researching the other works; two different team
   members researching the two distinct prerequisites also works, which is coherent
   with the evaluator's intentionally near-future horizon. The old impossible
   same-player currentResearch==tech1&&tech2 conjunction is gone.

2. KI#482 restores the retained K-Mod control flow rather than inventing a new
   starting-surroundings model. Every distance-qualified plot reaches the classifier;
   water and same-area land contribute their usable yield, while sufficiently valuable
   land on another island contributes the intended badness point for nearby but
   unworkable geographic space. The old AdvC outer area gate had merely made this
   existing inner branch unreachable.

3. KI#483 applies the domain test to the actual resource plot supplied to
   getBonusImprovement. Land improvements cannot stand in for water resources (and
   vice versa), while the existing near-tech Build/feature-tech forecasting and best-
   yield comparison remain unchanged. This fixes the Well/Offshore-Platform timing
   family without collapsing the helper into current-worker buildability.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP765
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6288 from the local commit-diff archive and audit its
live simulation/AI changes against current source and whole-album owners.
===============================================================================

===============================================================================
C031-WIP766 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6288 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP765.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6288 / KI#520 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit b3989d03ac47d5e4cc8fdf0bd70139a36bd8f0aa changes only the false debug
assertion in UWAI::Team::makePeaceTradeVal from iWarDuration>0 to >=0. The existing
formula already consumes sqrt(iWarDuration+1), so declaration-turn brokerage with an
at-war counter of 0 was always a valid release-build numerical state. Current counter
lifecycle is nonnegative; the repair therefore does not conceal a supported negative
case or alter pricing/rounding/caller eligibility. It merely aligns the assertion with
the arithmetic and caller contract recorded by F197/KI#520.

No independent repair-side regression survived. F725/KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP766
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6287 from the local commit-diff archive.
===============================================================================

===============================================================================
C031-WIP767 -- POST-PRIMARY REPAIR-SIDE PASS -- PRACTICAL 6287 CLOSED CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Continues byte-for-byte from C031-WIP766.
Primary audit remains 169/169 COMPLETE.
F708-F724 remain confirmed.
F725 / provisional KI#1046 remains UNUSED.
No gameplay/XML/Python source was modified by ChatGPT during this audit.

PRACTICAL 6287 / KI#449 -- REPAIR-SIDE AUDIT CLOSED CLEAN
---------------------------------------------------------
Commit f23646b66d9644edbb8182ac046ff91fc823f2bc was re-audited through the generic
WarUtilityAspect/WarEvaluator driver rather than only Distraction::evaluate.

1. Distraction::preEvaluate runs exactly once per evaluating agent-team member, before
   that member's generic rival-player loop. Clearing m_evaluatedTeams there therefore
   deduplicates target teammates within one member's distinct MilitaryAnalysis while
   preserving the separate weighted contribution of every other agent teammate.
   WarEvaluator explicitly iterates all agent members and accumulates the aspect's
   member deltas into the final team utility.

2. The first relevant target teammate no longer supplies private state to the shared
   cost. The initial player-pair isWar check can defer evaluation to a later teammate
   if necessary; once a team is consumed, reachability explicitly scans every living
   target member. War duration, cached current-war utility, alternate-war targets,
   power, war success, city count and capitulation are all team-scoped. Simulated
   elimination requires every living target member to be eliminated.

3. m_evaluatedTeams is inserted before the reachability return, but this is safe
   because the reachability test already covers every living member of that target
   team for the fixed agent member. A later teammate cannot reveal reachability that
   the first scan omitted.

4. The almost-finished reduction now applies to the one team-war cost using combined
   current team cities, matching the repair's intentionally approximate time-to-finish
   model rather than multiplying a player-specific reduction per teammate.

No iteration-order or cross-member double-charge regression survived. F725/KI#1046
remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP767
F725 / provisional KI#1046: UNUSED
NEXT: classify/open practical 6286 from the local commit-diff archive.
===============================================================================

===============================================================================
C031-WIP768 CONTINUATION -- PRACTICAL 6286 KI#460-KI#461 REPAIR-SIDE AUDIT
===============================================================================
STATUS: SETTLED CLEAN BOUNDARY. Primary 169/169 remains COMPLETE.
POST-PRIMARY: F708-F724 confirmed. F725 / provisional KI#1046 UNUSED.
NO GAMEPLAY/XML/PYTHON SOURCE FILES MODIFIED.

PRACTICAL 6286 / KI#460 -- PREEMPTIVEWAR TEAM-THREAT DEDUP -- CLEAN
- WarUtilityAspect::preEvaluate runs once per current agent-member MilitaryAnalyst,
  so m_evaluatedThreatTeams persists across the generic rival-player loop at the
  exact scope where F137/KI#460 duplicated one target-team forecast.
- UWAICache::calculateThreatRating(PlayerTypes) is player-keyed only for its
  reachability gate; every nonzero survivor returns teamThreat(TEAMID(eRival)).
- The repair therefore correctly takes max threat across living target members:
  one reachable member exposes the team's shared forecast; multiple reachable
  teammates no longer charge it repeatedly.
- After consuming the team key, the calculation is team/coalition scoped. The
  apparent GET_PLAYER(eThey).getMasterTeam() use is invariant across teammates,
  who necessarily share one team/master coalition. Remaining eThey use is log
  presentation, not utility state.
- This matches the original F137/KI#460 repair boundary; no repair-side sibling.

PRACTICAL 6286 / KI#461 -- TACTICAL OPERATIONAL READINESS DEDUP -- CLEAN
- preEvaluate resets m_bOperationalEvaluated once per agent member, which is the
  correct scope because each agent member has its own physical force inventory.
- The configured target team is consumed once, then ALL reachable living members
  are scanned before target-side era state is used.
- rTheyEra and eTheyEra are aggregated maxima over those reachable members; all
  subsequent readiness arithmetic uses only those aggregates plus this agent
  member's attacker/cargo/escort inventory and war-plan preparation state.
- No later player-private kThey/eThey state leaks back into the one charged cost.
- This implements the original F138/KI#461 instruction to evaluate readiness once
  per agent member per target TEAM while aggregating player-keyed reachability/
  era inputs at team scope.

DEDU P / NEGATIVE CONTROL
- No new finding allocated. F725 / provisional KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP768
NEXT: practical 6285 from local commit-diff archive -> current-source proof ->
whole-album dedup -> consume F725 only for a concrete current repair-side defect.
===============================================================================

===============================================================================
C031-WIP769 CONTINUATION -- PRACTICAL 6285 KI#455-KI#456 REPAIR-SIDE AUDIT
===============================================================================
STATUS: SETTLED CLEAN BOUNDARY. Primary 169/169 remains COMPLETE.
POST-PRIMARY: F708-F724 confirmed. F725 / provisional KI#1046 UNUSED.
NO GAMEPLAY/XML/PYTHON SOURCE FILES MODIFIED.

PRACTICAL 6285 / KI#455 -- REVOLTS SCENARIO-WIDE UNION -- CLEAN
- WarUtilityAspect::evaluate(MilitaryAnalyst) calls preEvaluate() once per current
  agent member, adds its return exactly once, then executes the now-empty Revolts
  per-rival hook. The local countedCities set therefore has exactly the lifetime
  requested by F132/KI#455.
- The new preEvaluate reconstructs the union using every inherited admission rule:
  analysis participant, simulated war with this agent, non-pushover target team,
  rival primary area, and the actual-war AreaAI defensive/neutral exclusion.
- A city admitted through any relevant rival area contributes once to BOTH the
  nonnegative expected-revolt-loss numerator and the corresponding asset
  denominator. Rival iteration order can no longer partition normalized ratios.
- countedCities is local, so the old cross-agent-member identity leak is removed.

PRACTICAL 6285 / KI#456 -- FLIPPED-TILE LOSS/TOTAL ASSET DOMAIN -- CLEAN
- flippedTileAssetScore(eVictim) is one victim player's apportioned full
  OWN_EXCLUSIVE_RADIUS land-asset category: 125 / living victim-team members.
- lossesFromFlippedTiles derives its numerator from that exact category by applying
  the victim team's lost-land share. UWAICache::numPlotsLostAtWar is a nonnegative
  count of owner-team plots lost to the opponent; there is no hidden signed-gain
  interpretation.
- netLostAssetScore now adds the same full category to rTotal, so numerator and
  denominator share one coherent measurement domain and the original >100% ratio
  defect is removed.
- The inherited max(5, teamLand) damping for tiny land holdings changes only the
  fraction of that category considered lost; it does not reintroduce mixed units.

DEDUP / NEGATIVE CONTROL
- Repairs match original F132/KI#455 and F133/KI#456 boundaries; no independent
  repair-side defect found. F725 / provisional KI#1046 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP769
NEXT: practical 6284 from local commit-diff archive -> current-source proof ->
whole-album dedup -> consume F725 only for a concrete current repair-side defect.
===============================================================================

===============================================================================
C031-WIP770 CONTINUATION -- PRACTICAL 6284 KI#480 PYTHON QUEUE OWNERSHIP AUDIT
===============================================================================
STATUS: SETTLED CLEAN BOUNDARY. Primary 169/169 remains COMPLETE.
POST-PRIMARY: F708-F724 confirmed. F725 / provisional KI#1046 UNUSED.
NO GAMEPLAY/XML/PYTHON SOURCE FILES MODIFIED.

PRACTICAL 6284 / KI#480 -- PYTHON QUEUE GETTER OWNERSHIP -- CLEAN
- CySelectionGroupInterface and CyCityInterface still bind these getters through
  Boost.Python return_value_policy<manage_new_object>; the Python object therefore
  truly owns and deletes the pointer returned by the Cy wrapper.
- The Cy wrappers now allocate independent copies while the underlying native
  CvSelectionGroup/CvCity getters retain their old borrowed queue-node pointers.
  Native DLL call semantics are therefore unchanged.
- MissionData and OrderData are plain value records (enums/ints/bools only), with
  no nested pointer/heap ownership; the generated copy is self-contained and later
  queue mutation cannot dangle it.
- Current shipped Python contains no caller of either queue getter and therefore no
  supported write-through dependency on modifying live DLL queue storage through
  the returned object.
- The repair makes the existing owning binding truthful without creating a new
  ownership/lifetime or compatibility defect.

DEDUP / NEGATIVE CONTROL
- Matches original F157/KI#480 repair boundary. F725 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP770
NEXT: practical 6283 from local commit-diff archive -> current-source proof ->
whole-album dedup -> consume F725 only for a concrete current repair-side defect.
===============================================================================

===============================================================================
C031-WIP771 CONTINUATION -- PRACTICAL 6283 LIFETIME-REPAIR BUNDLE AUDIT
===============================================================================
STATUS: SETTLED CLEAN BOUNDARY. Primary 169/169 remains COMPLETE.
POST-PRIMARY: F708-F724 confirmed. F725 / provisional KI#1046 UNUSED.
NO GAMEPLAY/XML/PYTHON SOURCE FILES MODIFIED.

PRACTICAL 6283 / KI#475.2 -- CLEARUNITS PATHFINDER TEARDOWN -- CLEAN
- CvSelectionGroup's main/alternate static GroupPathFinders are allocated and
  destroyed together. During save/load teardown clearUnits can run after static
  finder destruction; testing m_pPathFinder therefore correctly distinguishes
  "path cache exists" from the reproduced null-ownership state.
- Ordinary initialized play still invalidates the reusable main finder exactly as
  KI#475 requires. The repair avoids only dereferencing a destroyed static owner.

PRACTICAL 6283 / KI#475.3 (+ LATER KI#548) -- DEAD-AT-LOAD REVIVAL -- CLEAN
- CvPlayer::setAlive updates team alive state first. If a formerly zero-alive team
  revives, CvTeam::changeAliveCount initializes team UWAI at that boundary.
- initRevivedPlayerInGame then initializes only the revived PLAYER UWAI object,
  preserving surviving teammate/team state. Later KI#548 reconstructs that newly
  initialized player's physical military inventory before processNewPlayerInGame
  performs the normal full cache update.
- This covers the unit that caused revival even though it was created while the
  player cache did not yet exist; no team reinitialization/double-count window is
  introduced by the later repair.

PRACTICAL 6283 / KI#478 -- GROUPMOVE REENTRANT MEMBERSHIP -- CLEAN
- Snapshot stores IDInfo values, not linked-list nodes/pointers. Every use performs
  a fresh object lookup and verifies pUnit->getGroup()==this.
- Units moved, deleted or regrouped by conquest/culture side effects are skipped;
  surviving original members retain AdvC's two-stage no-city-capture ordering.
- Native group semantics are unchanged apart from eliminating stale live-list-node
  lifetime across reentrant CvUnit::move side effects.

PRACTICAL 6283 / KI#479 -- UPDATETIMERS COMBAT REGROUPING -- CLEAN
- Same value-identity/fresh-membership pattern protects updateCombat from deleting
  a prefetched future node while preserving K-Mod's multiple queued-combat updates.
- A removed/split/dead original member is skipped; a still-current fighting member
  is updated once from the original snapshot.

DEDUP / NEGATIVE CONTROL
- Repairs match original F152 continuation crash findings and F155/F156 boundaries;
  later KI#548 composes correctly. F725 remains UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP771
NEXT: practical 6282 from local commit-diff archive -> current-source proof ->
whole-album dedup -> consume F725 only for a concrete current repair-side defect.
===============================================================================

===============================================================================
C031-WIP772 — F725 / KI#1046 CONFIRMED: KI#477 REPAIR DROPS AIR-CARGO AUTOMATION CANCELLATION
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F725 are now confirmed.
F726 / provisional KI#1047 is UNUSED.
No gameplay/XML/Python source was modified.

F725 / provisional KI#1046
--------------------------
TITLE
    Practical 6282's KI#477 repair reused the land-only KI#476 cargo helper in
    CvSelectionGroup::setAutomateType(NO_AUTOMATE), narrowing BtS's all-cargo
    automation-cancellation semantics and leaving ordinary automated AIR cargo
    active when its carrier automation is stopped.

STATUS
    CONFIRMED CURRENT ADVCIV-SAS REPAIR-SIDE REGRESSION / INCOMPLETE KI#477 REPAIR.
    F154/KI#477 remains the inherited Firaxis BtS cross-transport group-scope root.
    F725 owns the independent domain-narrowing defect introduced by the SAS repair:
    even an ordinary cargo group carried exclusively by ONE Carrier is skipped when
    the cargo domain is AIR.

ROOT / EXACT REPAIR MISMATCH
----------------------------
The inherited BtS/Base-AdvCiv cancellation block was domain-agnostic. Once a unit on
this transport group's plot was found to have a transport belonging to `this`, it did:

    pCargoUnit->getGroup()->setAutomateType(NO_AUTOMATE);
    pCargoUnit->getGroup()->setActivityType(ACTIVITY_AWAKE);

There was no `DOMAIN_LAND` admission test. This is important because BtS/K-Mod also
supports aircraft as real carrier cargo and uses group automation for carrier-launched
Explore behavior.

Practical 6282 correctly repaired F154/KI#477's cross-carrier ownership problem, but it
implemented that repair by replacing the generic BtS scan with:

    std::vector<CvSelectionGroup*> apLandCargoGroups;
    getExclusiveLandCargoGroups(apLandCargoGroups);

`getExclusiveLandCargoGroups` explicitly rejects every cargo member whose domain is not
`DOMAIN_LAND`. That restriction belongs to KI#476's AdvCiv `ACTIVITY_BOARDED` cycling
layer, which is deliberately about land cargo. It does not belong to BtS's older,
generic automation-cancellation convenience.

CURRENT LIVE AIR-CARGO PATH
---------------------------
Current `CvUnitAI::AI_update` makes the lost behavior directly reachable for normal
human Carrier automation:

1. A human sea Carrier group is set to `AUTOMATE_EXPLORE`.
2. When the Carrier has finished moving or is holding, its Explore update gathers the
   aircraft physically carried by that unit.
3. For each movable `DOMAIN_AIR` cargo unit, it executes:

       pCargoUnit->getGroup()->setAutomateType(AUTOMATE_EXPLORE);
       pCargoUnit->getGroup()->setActivityType(ACTIVITY_AWAKE);

   Thus an ordinary fighter group aboard one Carrier can genuinely be automated.
4. The human then issues the normal stop-automation command on the Carrier. That reaches
   `CvSelectionGroup::setAutomateType(NO_AUTOMATE)` for the sea group.
5. Current practical-6282 code asks only for `getExclusiveLandCargoGroups`, so the
   fighter group's `DOMAIN_AIR` members are never returned even though every one is
   carried by this Carrier group.
6. The Carrier becomes `NO_AUTOMATE`, but its fighter cargo group remains
   `AUTOMATE_EXPLORE`.
7. On a later fighter AI update, the special hold/skip guard applies only while the
   transport itself is still automated and moving. Since the Carrier is now no longer
   automated, the fighter can proceed into `AI_exploreAirMove()` and choose Recon,
   Air Patrol or Skip from the carrier.

This is therefore persistent gameplay behavior, not merely a stale flag. The explicit
player action "stop this Carrier's automation" no longer performs the inherited cargo
cancellation for the most important non-land cargo family.

WHY THE REPAIR'S EXCLUSIVITY IDEA IS STILL CORRECT
--------------------------------------------------
The defect does NOT invalidate practical 6282's carrier-ownership repair. A cargo
selection group can legitimately span members carried by different transports; applying
a group-wide transition after identifying only one member can still corrupt unrelated
cargo, exactly as F154 proved.

The missing distinction is DOMAIN versus OWNERSHIP:
- KI#476 `ACTIVITY_BOARDED` is an AdvCiv land-cargo convenience and should keep the
  land-only helper.
- KI#477 `NO_AUTOMATE` propagation is a BtS all-cargo convenience and needs the same
  exclusive-carrier-group invariant without filtering to `DOMAIN_LAND`.

ANCESTRY / CONTROL COMPARISON
-----------------------------
- Supplied Civ4CE BtS and K-Mod carry the original cancellation loop without a cargo
  domain filter.
- Base AdvCiv 1.14 also carries that generic loop unchanged.
- Practical 6282 introduces the `getExclusiveLandCargoGroups` replacement in SAS.
- Current SAS retains that land-only call in `CvSelectionGroup::setAutomateType`.
Thus F725 is not an inherited BtS/AdvCiv behavior; it is a repair-side regression caused
by combining KI#476 and KI#477 through a helper whose domain contract fits only KI#476.

WHOLE-ALBUM / KNOWN-ISSUE DEDUP
-------------------------------
- F154/KI#477 owns the older cross-transport spillover when stopping automation on one
  transport changes a mixed cargo selection group. It does not own the repaired code's
  new omission of ordinary exclusive AIR cargo.
- F153/KI#476 is the AdvCiv land-cargo `ACTIVITY_BOARDED` scope defect and intentionally
  remains land-only.
- No later numbered album finding or current Known Issues entry owns the all-cargo ->
  land-only narrowing introduced by practical 6282.
- The current KI#477 documentation says the intended convenience remains unchanged for
  ordinary exclusive cargo groups; that statement is false for exclusive AIR cargo and
  should be corrected when the repair is updated.

REPAIR BOUNDARY
---------------
Keep `getExclusiveLandCargoGroups` for `resetBoarded` / `handleBoarded`, but do not use a
land-only helper for BtS automation cancellation.

A narrow repair is to provide a generic exclusive-cargo-group collector for
`setAutomateType(NO_AUTOMATE)`:
- candidate cargo member: has a transport and that transport's selection group is
  `this`, with NO cargo-domain filter;
- exclusivity proof: every member of the candidate cargo selection group has a
  transport whose selection group is also `this`;
- only then apply group-wide `NO_AUTOMATE` and `ACTIVITY_AWAKE`.

This preserves practical 6282's cross-carrier safety while restoring the original BtS
behavior for fighters and any other legitimate non-land cargo. Do not weaken the
land-only contract of the AdvCiv `ACTIVITY_BOARDED` helpers merely to share code.

ADJACENT CONTROL STILL OPEN FOR LATER DEDUP
-------------------------------------------
While proving the Carrier reproduction, a neighboring inherited BtS/K-Mod path was
noticed: the Carrier Explore code itself sets `AUTOMATE_EXPLORE` on an AIR cargo unit's
entire selection group. If air cargo aboard different carriers can remain in one group,
that enabling transition may have a cross-carrier ownership sibling analogous to F154.
This is NOT needed for F725: F725 reproduces with one ordinary exclusive Carrier cargo
group. The neighboring old path has not been assigned a finding number here; preserve it
as a follow-up adversarial candidate rather than conflating it with practical 6282's
repair regression.

NEXT
----
WIP772 physical -> F725/KI#1046 CONFIRMED -> F726/KI#1047 UNUSED -> finish the adjacent
air-cargo enabling-path dedup/control -> classify/open practical 6281 from the local
commit-diff archive -> continue descending repair-side audit with current-source proof
and whole-album dedup.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP772
===============================================================================

===============================================================================
C031-WIP773 — F726 / KI#1047 CONFIRMED: BTS CARRIER-EXPLORE AIR-CARGO SCOPE MISMATCH
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F726 are now confirmed.
F727 / provisional KI#1048 is UNUSED.
No gameplay/XML/Python source was modified.

F726 / provisional KI#1047
--------------------------
TITLE
    Firaxis BtS Carrier Explore automation mixes unit-local transport cargo with
    selection-group-wide automation: only the sea GROUP HEAD's aircraft are admitted,
    while one admitted aircraft can automate a shared air group carried by another,
    unrelated transport group.

STATUS
    CONFIRMED CURRENT INHERITED FIRAXIS BTS DEFECT.
    Present in supplied Civ4CE BtS, K-Mod, Base AdvCiv 1.14 and current SAS; absent from
    supplied Vanilla Civ4 and Warlords, which do not contain the Carrier Explore
    aircraft-propagation block.

BROKEN SCOPE CONTRACT
---------------------
Human automation is selection-group state. `CvSelectionGroupAI::AI_update` chooses the
selection group's current head unit and invokes `pHeadUnit->AI_update()`; current
`CvUnitAI::AI_update` itself asserts `isGroupHead()`.

Inside `AUTOMATE_EXPLORE`, after the sea head performs `AI_exploreSeaMove()`, the
inherited Carrier convenience says, in effect:

    if (hasCargo() && domainCargo() == DOMAIN_AIR && sea-group done moving)
    {
        getCargoUnits(aCargoUnits);             // cargo of THIS UNIT only
        ...
        pCargoUnit->getGroup()->setAutomateType(AUTOMATE_EXPLORE);
        pCargoUnit->getGroup()->setActivityType(ACTIVITY_AWAKE);
    }

The trigger is therefore GROUP automation, admission is HEAD-UNIT cargo ownership, and
the mutation target is an entire AIR CARGO GROUP. Those three scopes need not coincide.

MANIFESTATION A -- SAME AUTOMATED SEA GROUP IS UNDER-COVERED
-------------------------------------------------------------
1. Group two human sea units together, with at least one Carrier that is not the group
   head (or use two Carriers and place aircraft on both).
2. Automate the sea selection group for Explore.
3. `CvSelectionGroupAI::AI_update` drives only the sea-group head through
   `CvUnitAI::AI_update`.
4. The post-explore cargo block calls the head UNIT's `hasCargo`, `domainCargo` and
   `getCargoUnits`; it never enumerates cargo carried by the other transport members of
   the same automated sea selection group.
5. Aircraft on a non-head Carrier therefore do not receive the intended
   `AUTOMATE_EXPLORE` convenience even though their transport belongs to exactly the
   same automated sea group.
6. If the head is a non-carrier escort while a grouped Carrier carries fighters, the
   whole sea group can miss the aircraft propagation because the head's `hasCargo()` is
   false.

This is a direct under-propagation caused by treating a selection-group event as if the
head transport unit owned all relevant cargo.

MANIFESTATION B -- AN UNRELATED CARRIER CAN BE OVER-COVERED
------------------------------------------------------------
Loading normally separates cargo units, but normal human grouping can regroup loaded
same-domain aircraft afterward. `CvUnit::canJoinGroup` requires owner, plot and domain
compatibility and has no carrier-affinity test. Thus fighter A aboard Carrier A and
fighter B aboard separate Carrier B on the same plot can legitimately share one AIR
selection group.

If Carrier A is the automated sea-group head and finishes an Explore update without
leaving the shared plot (for example, no useful exploration/patrol/safety move exists
and it ends in Skip/Hold), `getCargoUnits` correctly admits fighter A because A is
physically carried by Carrier A. The code then calls `setAutomateType` on fighter A's
ENTIRE shared air selection group. Fighter B is thereby marked `AUTOMATE_EXPLORE` even
though Carrier B belongs to a different, untouched sea selection group.

The cross-carrier group need not survive transport movement for this reproduction:
K-Mod's movement-time separation logic is irrelevant while both carriers remain on the
same plot. The same normal grouping reachability already proven for F154 applies to AIR
members because `canJoinGroup` is domain-generic.

BEHAVIORAL CONSEQUENCE
----------------------
Current air Explore automation is active gameplay state, not a UI-only bit.
`CvUnit::canAutomate(AUTOMATE_EXPLORE)` explicitly accepts `DOMAIN_AIR` when `canRecon`
is true; Fighters and Jet Fighters are non-suicide air units with positive air range.
For automated air cargo, `CvUnitAI::AI_update` holds only while its own transport remains
automated and still moving. Otherwise it enters `AI_exploreAirMove`, which can issue
Recon, Air Patrol or Skip from a Carrier.

Thus the under-covered same-group Carrier can fail to use its aircraft automatically,
while the over-covered unrelated Carrier's grouped aircraft can acquire automated air
behavior without the player automating that transport.

ANCESTRY
--------
- Supplied Civ4CE Vanilla and Warlords lack this post-sea-Explore air-cargo block.
- Supplied Civ4CE BtS contains the same `hasCargo/domainCargo/getCargoUnits` unit-local
  admission followed by `pCargoUnit->getGroup()->setAutomateType` group-wide mutation.
- K-Mod retains it; Base AdvCiv 1.14 and current SAS retain it.
- The group-head-only AI-update contract is likewise inherited/current.
Therefore F726 originates in Firaxis BtS rather than practical 6282 or a SAS change.

RELATION TO F154/KI#477 AND F725/KI#1046
----------------------------------------
F154/KI#477 is the opposite transition in `CvSelectionGroup::setAutomateType`: stopping
one transport group's automation can cancel another transport's grouped cargo.
F725/KI#1046 is the SAS repair regression that accidentally narrowed that cancellation
from all cargo domains to land only.

F726 is separate code and a separate enabling transition. Fixing cancellation correctly
with a generic exclusive-cargo helper does not fix Carrier Explore's head-unit omission
or its group-wide AIR overreach. The roots share the same underlying data-model warning
(selection groups are not owned by one carrier) but require distinct call-site repair.

WHOLE-ALBUM / KNOWN-ISSUE DEDUP
-------------------------------
The completed CvUnitAI.cpp pass and later deep reloop contain no owner for this Carrier
Explore scope mismatch. Current Known Issues KI#476/KI#477 discuss land-cargo Boarded
and transport-stop cancellation only. No existing numbered finding covers the
head-carrier under-propagation or this AIR enabling transition. F726 is therefore new,
not a renumbering of F154.

REPAIR BOUNDARY
---------------
Treat automated Carrier Explore as a SEA-SELECTION-GROUP event:
- discover `DOMAIN_AIR` cargo carried by ANY transport unit whose transport selection
  group is the currently automated sea group, not merely cargo of the head unit;
- before applying a group-wide air automation/activity transition, require that EVERY
  member of the candidate air selection group is carried by that same sea selection
  group, so an unrelated Carrier cannot be affected;
- deduplicate cargo selection groups before changing state.

A generic exclusive-cargo-group helper introduced for the corrected KI#477 repair can
likely provide the ownership proof; Carrier Explore can then filter the returned cargo
members/groups to `DOMAIN_AIR`. Keep AdvCiv's `ACTIVITY_BOARDED` helper land-specific.

NEXT
----
WIP773 physical -> F726/KI#1047 CONFIRMED -> F727/KI#1048 UNUSED -> practical 6281
(KI#472-KI#475 repair bundle) current-source proof -> whole-album dedup -> consume F727
only for a concrete current repair-side defect.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP773
===============================================================================

===============================================================================
C031-WIP774 — PRACTICAL 6281 KI#472-KI#475 REPAIR BUNDLE; F727 / KI#1048 CONFIRMED
===============================================================================

STATE
-----
Primary 169/169 C++ file album remains COMPLETE.
Post-primary F708-F727 are now confirmed.
F728 / provisional KI#1049 is UNUSED.
No gameplay/XML/Python source was modified.

PRACTICAL 6281 / KI#472 -- ORIGINAL FALSE-SUCCESS REPAIR WORKS, MIXED-CARRIER EXECUTION SIBLING FOUND
----------------------------------------------------------------------------------------------------
The KI#472 change correctly repairs F149's exact false-success reproduction:
- `canAllMove()` remains the readiness gate;
- before reporting a landing, the repaired code now requires a cargo member whose
  actual transport belongs to the current sea selection group AND whose unit-local
  `canMoveOrAttackInto(kPlot)` is true;
- a movable Chariot/Catapult/Trebuchet that cannot enter the SAS-impossible feature can
  no longer consume the deferred transport order merely by receiving a doomed mission;
- an otherwise-legal but temporarily immobile cargo group still leaves the sea mission
  deferred for a later turn.

However, the new ownership check applies only to QUALIFICATION. After one correctly
owned member qualifies, the executor still pushes the landing mission onto that
member's ENTIRE land cargo selection group. A supported mixed-carrier group therefore
retains a separate cross-transport execution defect, promoted below as F727/KI#1048.

F727 / provisional KI#1048
--------------------------
TITLE
    Amphibious execution admits one correctly carried land member, then pushes the
    landing onto its entire mixed-carrier selection group, so ordering transport group
    A can unload/attack with grouped cargo physically carried by untouched transport
    group B.

STATUS
    CONFIRMED CURRENT INHERITED FIRAXIS/K-MOD/ADVCIV CROSS-TRANSPORT SCOPE DEFECT,
    newly exposed while adversarially reviewing practical 6281's KI#472 repair.
    Practical 6281 did not create the group-wide mission push; it added an ownership
    check that fixes F149's qualification bug but leaves this independent execution
    scope mismatch intact.

BROKEN OWNERSHIP / EXECUTION CONTRACT
-------------------------------------
Current `groupAmphibMove` gathers distinct LAND cargo selection groups by enumerating
cargo physically carried by members of the CURRENT sea selection group.

Practical 6281 then establishes:

    pCargoUnit->getTransportUnit()->getGroup() == this

for at least one ready/legal member before setting `bCanLand=true`. That is the correct
carrier boundary for deciding whether THIS transport group has cargo able to perform
its landing.

But the successful branch executes:

    kCargoGroup.pushMission(MISSION_MOVE_TO, target, ...);

on the WHOLE cargo selection group. `CvUnit::canJoinGroup` has no carrier-affinity
requirement. As already proven by F153/F154, same-owner same-plot LAND cargo aboard
separate transports can legitimately share one selection group. Thus the mutation
scope can still exceed the sea group whose order triggered the landing.

DETERMINISTIC CURRENT-SOURCE REPRODUCTION
-----------------------------------------
1. Put separate human transport selection groups A and B on the same water plot
   adjacent to a legal coastal landing tile.
2. Load land cargo A on transport A and land cargo B on transport B. Regroup the two
   loaded land units through normal human grouping; same owner/plot/domain satisfies
   `canJoinGroup`, and their transport identities remain different.
3. Ensure both cargo members have movement and can legally enter/attack the adjacent
   land target. Transport A can be the sea group whose deferred MOVE_TO is currently
   resolving; transport B receives no order.
4. `groupAmphibMove` for sea group A discovers the shared cargo group because cargo A
   is physically carried by A.
5. The KI#472 ownership/legality loop finds cargo A, confirms A's transport group is
   `this`, and sets `bCanLand=true`.
6. The code pushes one `MISSION_MOVE_TO` onto the SHARED cargo selection group.
7. The cargo group's normal `groupPathTo/groupMove` execution considers every current
   group member. Cargo B is movable and can enter the target, so `CvUnit::move` moves B
   off its own transport B along with cargo A.
8. The player has therefore ordered only transport group A, yet cargo physically
   carried by untouched transport group B also lands/attacks because it happened to
   share A cargo's selection group.

The reproduction does not depend on F149's illegal-feature fixture. It works with
ordinary legal infantry cargo; F149's false-success repair can be completely correct
while F727 remains.

WHY PRACTICAL 6281 ITSELF CORROBORATES THE BOUNDARY
---------------------------------------------------
The repair comment explicitly says a mixed-carrier cargo group must not qualify through
another ship's unit. That establishes the intended event scope as the current sea
selection group. Once qualification is carrier-local, executing the resulting order on
members carried by a different sea group violates the same ownership boundary.

This is analogous to KI#476/KI#477's data-model warning but affects actual amphibious
movement/attack rather than Boarded or automation state.

ANCESTRY
--------
The group-wide amphibious cargo mission predates SAS:
- supplied Vanilla/Warlords already push a cargo selection group's MOVE_TO when a
  qualifying carried group head is found;
- BtS reorganizes this through cargo-group collection but still pushes the whole group;
- K-Mod practical 428 deduplicates cargo groups across carrier units, retaining the
  whole-group push;
- Base AdvCiv 1.14 retains the K-Mod structure;
- current SAS retains it with practical 6281's additional member/carrier legality test.
The exact ease with which cross-transport grouping persists differs by lineage, but
normal same-plot human regrouping is supported in current SAS and is sufficient.

WHOLE-ALBUM / KNOWN-ISSUE DEDUP
-------------------------------
- F149/KI#472 owns false SUCCESS accounting: an individually illegal but movable cargo
  group could cause the sea landing order to be consumed when nobody landed.
- F153/KI#476 owns AdvCiv `ACTIVITY_BOARDED` cross-carrier state.
- F154/KI#477 owns BtS `NO_AUTOMATE` cross-carrier state.
- F727 owns a separate group-wide amphibious MOVEMENT/ATTACK execution initiated by one
  sea group and applied to cargo carried by another.
No current Known Issues entry or later album finding owns this landing execution scope.

REPAIR BOUNDARY
---------------
Do not push a carrier-local amphibious order onto a cargo selection group unless every
member that will receive that group mission is carried by the current sea selection
group.

The least destructive policy is consistent with KI#476/KI#477:
- discover/deduplicate cargo groups belonging to the current sea group;
- require both current landing legality AND carrier-group exclusivity before issuing a
  group-wide cargo mission;
- if a cargo selection group spans another sea group, do not split it merely for this
  implicit transport convenience unless a deliberate split policy is chosen and all
  automation/mission consequences are handled explicitly.

A shared generic exclusive-cargo-group ownership helper can likely serve KI#477,
Carrier Explore F726 and this amphibious path, with call-site domain/legality filters.

PRACTICAL 6281 / KI#473 -- AIR PATROL MOVELESS-PARTICIPANT REPAIR -- CLEAN
---------------------------------------------------------------------------
- `startMission` now classifies a moveless member as left behind when the mission is
  `MISSION_AIRPATROL` before the generic `continue`.
- Movable members still reach the inherited `canAirDefend` classification, so an
  unsuitable movable Bomber is also split as before.
- Mission availability still requires at least one eligible movable patrol member;
  therefore the final K-Mod assertion that not EVERY selected unit is left behind is
  preserved for this path.
- The left-behind regroup occurs after classification and correctly removes a moveless
  head as well as a non-head member from the new Intercept group.
No repair-side sibling was found.

PRACTICAL 6281 / KI#474 -- UPGRADE ACTION CACHE -- CLEAN
--------------------------------------------------------
- The old one-representative-per-UnitType cache is replaced by every
  `isReadyForUpgrade()` member.
- Cached command visibility still invokes the authoritative per-unit
  `canDoCommand(COMMAND_UPGRADE, target, ...)`, so target legality, gold and promotion
  discounts are checked on the actual candidate rather than approximated in the cache.
- The cache remains a UI action-query snapshot created by `setupActionCache`; no native
  command execution uses it as authoritative state.
- Same-type promotion-discount heterogeneity from F151 is therefore repaired without
  broadening gameplay command semantics.
No repair-side defect found.

PRACTICAL 6281 / KI#475 -- GROUP PATH MEMBERSHIP INVALIDATION -- CLEAN IN CURRENT SOURCE
----------------------------------------------------------------------------------------
- `addUnit` invalidates the reusable MAIN `GroupPathFinder` immediately after the
  membership insertion.
- `deleteUnitNode` invalidates it after the actual removal; `removeUnit`, regrouping and
  split/merge paths reach that lower mutation boundary.
- `clearUnits` invalidates after clearing. Practical 6283 / KI#475.2 later added the
  necessary null-owner guard for save/load teardown; WIP771 already proved that current
  lifecycle repair clean.
- Not invalidating the alternate finder at ordinary membership mutation is deliberate:
  `getClearPathFinder` resets it before execution use, while group movement can mutate
  membership during an active alternate path whose caller still consults that path.
- The original F152 stale MAIN-node reproduction (Settler path -> equal-move restrictive
  Chariot joins same group -> second reusable path search) is therefore cut exactly at
  the membership mutation.
No additional current repair-side defect found.

PRACTICAL 6281 BUNDLE VERDICT
-----------------------------
KI#473, KI#474 and current KI#475 are clean at their intended boundaries. KI#472's
false-success repair is also effective for F149, but its new member-level ownership
check exposes a still-live older whole-group amphibious execution mismatch, now owned by
F727/KI#1048 rather than being folded into F149.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP774
F727 / provisional KI#1048: CONFIRMED
F728 / provisional KI#1049: UNUSED
NEXT: practical 6280 (`fix inherited K-Mod/BtS/AdvCiv selection-group defects
(KI#469-KI#471); defer KI#468`) from local commit-diff archive -> current-source proof
-> whole-album dedup -> consume F728 only for a concrete current defect.

No SAS/Base AdvC/K-Mod/Civ4CE gameplay source was modified during this audit.
===============================================================================
END C031-WIP774
===============================================================================


===============================================================================
C031-WIP775 APPEND -- practical 6280 adversarial repair audit (KI#469-KI#471)
===============================================================================

SCOPE
-----
Chronology cursor practical 6280 / commit 0a95fa1d5f2eb949bd26fe05f3973ba8db14b5b0:
    fix inherited K-Mod/BtS/AdvCiv selection-group defects (KI#469-KI#471);
    defer KI#468

This checkpoint re-challenges the three repairs against current SAS execution rather
than merely confirming that their original reproductions are cut.

PRACTICAL 6280 / KI#469 -- SPY SLEEP-WAKE MEMBER SCAN -- CLEAN
----------------------------------------------------------------
The repair replaces K-Mod's head-only Spy test with an all-member scan while preserving
the original pre-fortify-increment timing and exact `MAX_FORTIFY_TURNS - 1` threshold.

A possible equality-only late-join concern was checked and rejected. Human regrouping
through `CvUnit::joinGroup` forcibly sets the resulting multi-unit group to
`ACTIVITY_AWAKE`, cancels automation and clears its mission queue. A Spy already at the
wake threshold therefore cannot normally be inserted into a sleeping human group and
silently bypass the equality test. The original F146 non-head sleeping-Spy defect is
cut without a new reachable member-lifecycle hole found here.

PRACTICAL 6280 / KI#470 -- SENTRY HOSTILITY ACTOR SCAN -- CLEAN
----------------------------------------------------------------
The outer Sentry geometry still uses the maximum-range member, which is valid for the
shared same-plot/team LOS envelope. For every candidate unit, the repair then scans each
selection-group member that can personally see the candidate plot and calls:

    kPlot.isVisibleEnemyUnit(pGroupUnit, pLoopUnit)

Current `CvPlot::isVisibleEnemyUnit(attacker, candidate)` applies the supplied
attacker's `isAlwaysHostile(plot)` to `PUF_isEnemy`, while candidate invisibility is
checked against the attacker's team. Thus the exact unit-local property that made the
Privateer/Destroyer F147 reproduction fail is now evaluated through every relevant
member, without turning team-scoped invisibility into an incorrect per-unit concept.
No repair-side sibling was found.

PRACTICAL 6280 / KI#471 -- ORIGINAL HEAD-ONLY RECON DEFECT REPAIRED,
BUT PRE-MOVE PARTICIPATION PREDICATE IS NOT STABLE UNDER SHARED AIR CAPACITY
--------------------------------------------------------------------------------
The new loop correctly fixes the two original F148 directions in ordinary cases:
- an immobile head is no longer cleared merely because another aircraft can rebase;
- movable non-head aircraft that can enter the destination have their own unit-local
  recon state cleared instead of leaving stale remote sight behind.

The attack-vs-rebase discriminator is also sound at this boundary. `isRevealedAirBase`
is `isRevealedBase`; it requires a revealed city/ActsAsCity improvement whose revealed
team can be peacefully entered. An enemy air-strike destination therefore does not pass
this cleanup gate merely because both actions use MISSION_MOVE_TO.

F728 / provisional KI#1049
--------------------------
TITLE
    KI#471 repair clears grouped aircraft recon before movement using independent
    `canMoveInto` checks, so multiple aircraft competing for the destination's last
    air-capacity slot can have recon cleared even though a later member stays behind.

STATUS
    CONFIRMED CURRENT SAS REPAIR-SIDE REGRESSION / INCOMPLETE KI#471 REPAIR,
    introduced by practical 6280's pre-move all-participant cleanup loop.
    F148/KI#471 remains the inherited AdvCiv root; F728 owns the new shared-capacity
    participation mismatch in the repair.

BROKEN CONTRACT
---------------
At `CvSelectionGroup::startMission`, before any member has moved, practical 6280 does:

    FOR_EACH_UNIT_VAR_IN(pUnit, *this)
        if (pUnit->canMove() && pUnit->canMoveInto(*pDest))
            pUnit->setReconPlot(NULL);

For DOMAIN_AIR, `CvUnit::canMoveInto` checks the destination's CURRENT
`airUnitSpaceAvailable(team)` against that unit's `iAirUnitCap`. These independent
preflight checks do not reserve capacity for earlier members.

Later `groupPathTo` accepts an air move when ANY group member can enter the destination,
then `groupMove` visits members sequentially. Before each actual move it re-evaluates:

    pUnit->canMove() && pUnit->canMoveInto(*pPlot)

The first aircraft's arrival changes `countNumAirUnits` and therefore reduces
`airUnitSpaceAvailable`. A later aircraft that passed the earlier cleanup predicate can
then fail the execution-time predicate and be split into the behind group without
moving. Its recon plot has already been cleared.

DETERMINISTIC CURRENT-SOURCE REPRODUCTION
-----------------------------------------
1. Group two same-owner Fighters at one air base. Current `CvUnit::canJoinGroup` allows
   same-owner, same-plot, same-domain aircraft to share a selection group.
2. Give both live unit-local recon plots and movement remaining.
3. Choose a friendly/revealed destination air base with exactly ONE Fighter-sized air
   slot remaining. Current UNIT_FIGHTER is DOMAIN_AIR with `iAirUnitCap=1`; city air
   capacity is finite and `airUnitSpaceAvailable` subtracts already based aircraft.
4. The Rebase interface is exposed because `canDoInterfaceModeAt(INTERFACEMODE_REBASE)`
   requires only one member to pass `canMoveInto`; here both independently pass while
   the one slot is still free.
5. `startMission` runs before movement. Both Fighters see that same one free slot, so
   both pass `canMove && canMoveInto` and both execute `setReconPlot(NULL)`.
6. `groupPathTo` accepts the air mission because group `canMoveInto` is an ANY-member
   test, then calls `groupMove`.
7. The first Fighter moves into the destination and consumes the last slot.
8. When `groupMove` reaches the second Fighter, its fresh `canMoveInto` now fails because
   destination air capacity is exhausted. K-Mod/AdvCiv movement splits it into a
   left-behind group rather than moving it.
9. That second Fighter remains at the old base despite having lost the remote recon
   visibility that AdvCiv's advc.029 hook is explicitly intended to clear ONLY on
   rebase.

This is the same structural class as pending KI#468's independent finite-capacity
checks, but at a different execution site and with a destructive recon-state mutation.
KI#468 remains the COMMAND_LOAD admission defect; F728 owns KI#471's pre-move cleanup
using a non-reserving participation forecast.

WHY THIS IS A PRACTICAL-6280 REGRESSION RATHER THAN THE OLD F148 ALONE
---------------------------------------------------------------------
Base AdvCiv's head-only hook is wrong for grouped aircraft, as F148 established. But it
does not contain the new all-members preflight loop. Practical 6280 intentionally tried
to clear exactly the aircraft that will participate by predicting movement with
`canMove && canMoveInto` before execution. Shared destination capacity makes that
prediction unstable across sequential member moves. The current Known Issues wording
that the repair "matches group movement participation" is therefore too strong.

REPAIR BOUNDARY
---------------
The recon mutation should follow ACTUAL successful rebase participation, not forecast it
from an independently evaluated pre-move capacity check.

Strongest direction:
- clear `setReconPlot(NULL)` for each air unit immediately when that unit actually makes
  the non-combat air-base MOVE_TO/rebase, after the execution path has established that
  the member will move (or directly after its successful move while retaining enough
  old-state context);
- keep air attacks excluded;
- do not clear a member merely because it could enter the destination before another
  grouped aircraft consumes shared capacity.

If cleanup must remain in `startMission`, it would need a capacity-aware reservation /
assignment simulation matching `groupMove` order and every other mutable admission
constraint. That is substantially more fragile than attaching cleanup to actual
successful per-unit rebase execution.

WHOLE-ALBUM / KNOWN-ISSUE DEDUP
-------------------------------
- F148/KI#471 owns inherited AdvCiv head-only recon cleanup and the immobile-head /
  movable-non-head mismatch.
- F145/KI#468 owns pending AdvCiv COMMAND_LOAD independent finite cargo-capacity checks.
- F728/KI#1049 owns practical 6280's new recon cleanup based on independent pre-move
  air-capacity checks; it is not a duplicate because the mutated state, command path and
  regression ancestry differ.
No later album owner was found for this repair-side air-capacity/recon interaction.

PRACTICAL 6280 BUNDLE VERDICT
-----------------------------
KI#469 CLEAN at its intended boundary.
KI#470 CLEAN at its intended boundary.
KI#471's original F148 defect is materially repaired for ordinary grouped rebases, but
its pre-move participant prediction is not equivalent to actual execution when shared
air capacity changes during the group move. Promote F728/KI#1049 rather than reopening
F148 as though the original head-only diagnosis were wrong.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP775
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F728: confirmed through current chronology review.
F728 / provisional KI#1049: CONFIRMED.
F729 / provisional KI#1050: UNUSED.
NEXT: practical 6279 / commit aea34ce91dd437fcca7c295a35f8c76f0f7cd698
      (`fix inherited AdvCiv UWAI/naval group-state defects (KI#465-KI#467);
       reconcile C++ audit album C016-WIP01`)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP775
===============================================================================


===============================================================================
C031-WIP776 APPEND -- practical 6279 repair audit (KI#465-KI#467)
===============================================================================

SCOPE
-----
Chronology cursor practical 6279 / commit aea34ce91dd437fcca7c295a35f8c76f0f7cd698:
    fix inherited AdvCiv UWAI/naval group-state defects (KI#465-KI#467);
    reconcile C++ audit album C016-WIP01

Current source includes later compatible work, especially KI#456's flipped-tile asset-
domain repair; this pass therefore validates the maintained CURRENT contracts rather
than pretending practical 6279 remains byte-identical at every touched line.

PRACTICAL 6279 / KI#465 -- TEAM-OWNED FLIPPED-TILE NORMALIZATION -- CLEAN IN CURRENT SOURCE
-------------------------------------------------------------------------------------------
The original F142 mismatch was team-wide lost tiles divided by player-local land and
then divided by team membership. Practical 6279 moved threshold, land denominator and
apportionment into the same team domain. Later KI#456 made that alignment stronger:

    flippedTileAssetScore(eVictim)
        = 125 / MemberIter::count(eVictimTeam)

and `lossesFromFlippedTiles` now applies the lost share of TEAM land directly to that
matching apportioned category total.

Controls checked:
- `MemberIter` is explicitly `PlayerIter<ALIVE, MEMBER_OF>`; its count is the living
  members whose per-player callbacks are being apportioned.
- current `netLostAssetScore` call sites evaluate live/current major rivals, teammates
  and vassals rather than manufacturing a zero-living-member team callback;
- `flippedTileAssetScore` first rejects teams with no non-vassal team land;
- `lossesFromFlippedTiles` keeps one fixed four-tile significance threshold on the
  team-owned cached signal and uses `GET_TEAM(eVictimTeam).getTotalLand(false)`.

Thus changing how unchanged team land is partitioned among living teammates no longer
changes the physical-loss ratio. No separate divide-by-zero/dead-victim lifecycle edge
was established. KI#456 owns the later numerator/denominator asset-domain refinement;
it does not reopen KI#465.

PRACTICAL 6279 / KI#466 -- SEA PATROL MIXED-GROUP CLEANUP -- CLEAN
-----------------------------------------------------------------
The repair is internally consistent with AdvCiv's chosen mixed-group policy:
- mission admission uses `canSeaPatrol(pPlot, true)` and can accept a group when one
  member qualifies;
- persistent patrol is group activity `ACTIVITY_PATROL`;
- during unit `doTurn`, an individually invalid member now scans the group and wakes it
  only if no member remains individually capable of Sea Patrol.

A subtle API control was checked: the no-argument `canSeaPatrol()` used by the validity
scan has header default `bCheckActivity=false`. It therefore does NOT reject a valid
member merely because its group is already `ACTIVITY_PATROL`; the scan can actually
find the surviving warship. By contrast mission admission explicitly passes `true` to
prevent reissuing patrol to a group already patrolling.

If the underlying guarded-improvement geometry later becomes invalid for every member,
the last/any invalid member's scan finds no legal patroller and wakes the group, so
advc.004k's intended whole-patrol invalidation remains. No repair-side defect found.

PRACTICAL 6279 / KI#467 -- BLOCKADE SURVIVOR SCAN -- CLEAN
----------------------------------------------------------
The repair first clears only the current invalid unit's unit-local blockade flag, then
checks the remaining group for a member satisfying BOTH:

    pLoopUnit->isBlockading() && pLoopUnit->canPlunder(kPlot)

All normal group members share the current plot, while `canPlunder` evaluates the
candidate unit's own pirate/always-hostile status and current diplomatic legality.
Consequently:
- a still-valid hidden-nationality Privateer can preserve `ACTIVITY_PLUNDER` after a
  grouped ordinary warship becomes invalid;
- a stale or newly illegal blockader cannot preserve the activity merely by retaining
  membership;
- if no valid active blockader remains, `setActivityType(ACTIVITY_AWAKE)` performs the
  intended group-wide blockade cleanup through `CvSelectionGroup::setBlockading(false)`.

Unit-turn ordering does not reverse the invariant: whether the valid or invalid member
runs first, the valid member either collects first or remains discoverable by the
invalid member's survivor scan. No repair-side defect found.

PRACTICAL 6279 BUNDLE VERDICT
-----------------------------
KI#465 CLEAN in maintained current source, including later KI#456 interaction.
KI#466 CLEAN at the mixed-group persistent-activity boundary.
KI#467 CLEAN at the unit-local blockade / group-local activity boundary.
No new root consumed.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP776
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F728 remain confirmed.
F729 / provisional KI#1050: UNUSED.
NEXT: practical 6278 / commit 7e323809777443af4511886dff102992864ce868
      (`fix inherited AdvCiv UWAI/C++ identity and lifetime defects
       (KI#454, KI#458, KI#462, KI#464); reconcile C++ audit C016`)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP776
===============================================================================


===============================================================================
C031-WIP777 APPEND -- practical 6278 repair audit (KI#454, KI#458, KI#462, KI#464)
===============================================================================

SCOPE
-----
Chronology cursor practical 6278 / commit 7e323809777443af4511886dff102992864ce868:
    fix inherited AdvCiv UWAI/C++ identity and lifetime defects
    (KI#454, KI#458, KI#462, KI#464); reconcile C++ audit C016

This pass adversarially checks the maintained current source rather than merely proving
that the four original album roots were changed. One new repair-side lifetime mismatch
survives in KI#454; the other three repairs remain clean at their intended boundary.

F729 / provisional KI#1050 -- CONFIRMED -- PRACTICAL-6278 GREEDFORSPACE REPAIR REGRESSION
PERSISTENT RIVAL-SITE DEDUP STATE LEAKS ACROSS AGENT-TEAM MEMBER EVALUATIONS
-----------------------------------------------------------------------------------------
CONFIRMED current AdvCiv-SAS repair-side UWAI ownership/lifetime defect introduced by
practical 6278 while fixing F131/KI#454.

ROOT
----
F131/KI#454 correctly established that rival teammates can independently cache the same
physical settlement plot and that one plot must therefore be deduplicated across those
RIVAL-member passes. Practical 6278 implemented that physical identity with a member
container on the aspect object:

    class GreedForSpace : public WarUtilityAspect
    {
        ...
        std::set<PlotNumTypes> m_countedSites;
    };

and admits a visible recommendation only through:

    m_countedSites.insert(kSite.plotNum()).second

The lifetime of that set is broader than the ownership domain the repair intended.
`WarEvaluator` constructs ONE vector of aspect objects, then reuses those same objects
for every living member of the evaluating agent team:

    vector<WarUtilityAspect*> apAspects;
    fillWithAspects(apAspects);
    for (MemberIter itMember(m_kAgent.getID()); itMember.hasNext(); ++itMember)
        evaluate(itMember->getID(), apAspects);

`WarUtilityAspect::evaluate(MilitaryAnalyst const&)` changes `m_pAgentPlayer` and the
player-local UWAI cache for each member and calls `reset()` afterward, but base `reset`
clears only transient base pointers/rival state. It does NOT clear
`GreedForSpace::m_countedSites`.

Consequently a plot inserted while evaluating agent member A1 remains claimed when the
same GreedForSpace object is reused for agent member A2.

WHY THIS CHANGES UTILITY, NOT JUST LOGGING OR ATTRIBUTION
---------------------------------------------------------
GreedForSpace is not a team-uniform formula after the physical plot has been admitted.
The surrounding marginal value is explicitly player-local:

    ourCache().numAdjacentLandPlots(eThey)
    kWe.getNumCities()
    kWe.AI_getNumCitySites()
    kWeAI.amortizationMultiplier()

and the cap

    rExpectedSites.decreaseTo(iOurCities)

also depends on that current member's city/site state.

Therefore the first eligible agent teammate to encounter physical plot P does not just
record a harmless representative. It irrevocably chooses WHICH teammate's marginal
expansion formula gets to value P. A later teammate with a very different marginal
need can never see that opportunity.

Deterministic counterexample:
1. Agent team A has living members A1 and A2. Rival team B is eliminated in the
   scenario and exposes one visible recommended physical plot P.
2. Both A1 and A2 are otherwise eligible to value B/P.
3. A1 is evaluated first. A1 already has enough current city sites that adding 0.63 of
   one orphaned site reaches or remains behind a marginal/capped result near zero, but
   the code still inserts P into `m_countedSites` before the cap/utility calculation.
4. A2 has few current sites/cities and would assign substantial positive marginal value
   to the same released plot.
5. A2's later pass sees the persistent P and receives zero site count from it.

Changing only which teammate is encountered first can therefore change aggregate team
war utility although the physical map opportunity is unchanged. Even without a zero
A1 result, different city counts, current-site counts and amortization multipliers make
first-member capture numerically consequential.

DIRECT PRECEDENT -- KI#455
--------------------------
This is not a speculative objection to all persistent aspect state. The immediately
adjacent repaired F132/KI#455 documents the same lifecycle boundary explicitly:

    "Because one aspect object is also reused across the agent team's member
     evaluations, the persistent set can additionally make a later agent member skip
     city identities encountered for an earlier member."

The current `Revolts` repair removed that persistent member set and reconstructs the
relevant scenario-wide city union inside each agent member's `preEvaluate`, keeping its
uniqueness state aligned with the numerator/denominator formula that consumes it.

GreedForSpace's new `m_countedSites` was added in the same practical bundle but was not
similarly scoped, leaving exactly the cross-agent lifetime hazard already recognized
for KI#455.

ANCESTRY / DEDUP
----------------
- Base AdvCiv 1.14 has F131/KI#454's original duplicate-rival-site behavior, but not
  practical 6278's persistent `m_countedSites`; F729 is therefore a SAS repair
  regression/incomplete repair, not another inherited AdvCiv root.
- F131/KI#454 owns the original RIVAL-side duplicate physical plot: B1 and B2 can cache
  the same P and create two apparent opportunities.
- F132/KI#455 owns Revolts' own persistent-city-set/ratio lifetime mismatch. It provides
  a strong architectural precedent but cannot repair GreedForSpace because the classes,
  state and utility formula are separate.
- No later album GreedForSpace owner was found. The only post-F131 references are the
  original ledger summaries and this practical-6278 chronology cursor.

REPAIR BOUNDARY
---------------
Simply clearing `m_countedSites` for every agent member is not a complete answer: it
would prevent cross-agent suppression but could let the SAME one physical site be
valued independently by A1 and A2, recreating a physical-opportunity double count on the
agent side.

The correct ownership needs to be made explicit. Strong directions include:
- form the released-site UNION at a coherent team/scenario scope, then assign/value each
  unique physical opportunity once using a deterministic team-level marginal rule; or
- explicitly assign each unique plot to one eligible agent member according to a
  meaningful deterministic ownership/value criterion, then apply that member's
  player-local city/site formula.

Whichever policy is chosen, plot identity and the player-local marginal valuation must
share a deliberate lifetime. "Whichever MemberIter teammate happens to insert first"
is not such a policy. Mark this repair architectural unless a simple intended ownership
rule can be established from UWAI design/history.

PRACTICAL 6278 / KI#458 -- DECISIVE TARGET TEAM SCOPE -- CLEAN
--------------------------------------------------------------
The repaired `ThirdPartyIntervention` decisive-peace shortcut now asks whether the
EXACT configured target team capitulated to us; otherwise it requires every currently
living member of that target team to be simulated as eliminated. It no longer uses one
pre-simulation target leader player as a proxy and no longer treats an unrelated
capitulation elsewhere in the simulated war network as victory over `eTarget`.

Dead target members are naturally absent from `MemberIter`; surviving members must be
present in `MilitaryAnalyst::isEliminated`. No independent target-identity/lifetime edge
survived this pass.

PRACTICAL 6278 / KI#462 -- WAR-ON-FRIEND VICTIM TEAM DEDUP -- CLEAN
-------------------------------------------------------------------
`IllWill::evalAngeredPartners` now deduplicates `getWarsDeclaredBy(eWe)` by
`TeamTypes` before applying the observer's team-level `AI_disapprovesOfDoW` decision.
The dedup is limited to the victim side: separate defensive-pact target teams remain
separate diplomatic events, while multiple player representations of one declared-on
team cannot repeat the same war-on-friend penalty. Vassal exclusion and observer-team
exclusion are also team properties at this decision boundary. No new root found.

PRACTICAL 6278 / KI#464 -- POLYMORPHIC DESTRUCTION -- CLEAN
-----------------------------------------------------------
`WarUtilityAspect` now has a public C++03-compatible virtual destructor. `WarEvaluator`
allocates concrete derived aspect objects, stores them as `WarUtilityAspect*`, and
later deletes through that base pointer. The virtual base destructor therefore closes
the exact undefined-behavior contract and ensures derived containers such as the sets
owned by `KingMaking`, `Revolts` lineage and `GreedForSpace` are destructed correctly.
No independent lifetime defect remains at that delete site.

PRACTICAL 6278 BUNDLE VERDICT
-----------------------------
KI#458 CLEAN at exact-target decisive-state scope.
KI#462 CLEAN at victim-team diplomatic-event scope.
KI#464 CLEAN at polymorphic deletion scope.
KI#454's original rival-side physical-plot duplication is genuinely addressed, but the
new persistent set crosses the separate agent-member evaluation boundary and makes the
player-local marginal value of one site depend on first-member capture.
Promote F729/provisional KI#1050 rather than reopening F131 as if its original diagnosis
were wrong.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP777
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F729 remain confirmed.
F729 / provisional KI#1050: CONFIRMED.
F730 / provisional KI#1051: UNUSED.
NEXT: practical 6277 / commit d7387411d1... (exact local commit/diff title to be read)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP777
===============================================================================


===============================================================================
C031-WIP778 APPEND -- practical 6277 repair audit (KI#451 HiredHand)
===============================================================================

SCOPE
-----
Chronology cursor practical 6277 / commit d7387411d1b641ba3438a6a2bd2e84aaaf101221:
    fix inherited AdvCiv UWAI Hired Hand team obligations (KI#451);
    defer KI#447-KI#450 and reconcile C++ audit C015-WIP18

This pass adversarially rechecks the maintained current HiredHand repair rather than
merely verifying that the original target-member duplication was changed. The original
sponsor/bounty branch is genuinely canonicalized on the hostile target team, but two
independent aggregation boundaries remain wrong: historical-role utility still repeats
across AGENT teammates, and the separately retained hireling branch still contains a
team-keyed path that repeats across TARGET teammates.

The earlier candidate that simulated elimination of the current target-team leader
could suppress the canonical pass is REJECTED. `MilitaryAnalyst::getWarsContinued` is
constructed from the CURRENT war relation before invasion simulation and is not pruned
when a player is later predicted eliminated. `CvTeam::updateLeaderID` also keeps an
actually living team member as current leader whenever one exists. Simulated leader
loss therefore does not erase the canonical continuing-war membership gate.


F730 / provisional KI#1051 -- CONFIRMED -- PRACTICAL-6277 HIREDHAND REPAIR INCOMPLETE
HISTORICAL-ROLE TEAM WAR IS CANONICALIZED ONLY ON THE TARGET SIDE AND STILL REPEATS
ONCE PER AGENT-TEAM MEMBER
------------------------------------------------------------------------------------------------
CONFIRMED current AdvCiv-SAS repair-side/incomplete-repair UWAI aggregation defect.

ROOT
----
Practical 6277 repaired F128/KI#451's historical-role multiplication across target
teammates by adding:

    bool const bCanonicalTargetMember = (eThey == kTheirTeam.getLeaderID());
    ...
    if (bCanonicalTargetMember &&
        kOurTeam.AI_getAtWarCounter(eTheirTeam) >= m_kGame.getElapsedGameTurns())
    {
        rUtility += eval(NO_PLAYER, 50, 12);
    }

That fixes only one of the two player-partition dimensions through which the aspect is
executed.

`WarEvaluator` constructs one HiredHand aspect object and evaluates it for EVERY living
member of the agent team:

    vector<WarUtilityAspect*> apAspects;
    fillWithAspects(apAspects);
    for (MemberIter itMember(m_kAgent.getID()); itMember.hasNext(); ++itMember)
        evaluate(itMember->getID(), apAspects);

For each agent member, the generic rival loop reaches the canonical target-team leader.
The historical-role inputs are then identical:

    kOurTeam.AI_getAtWarCounter(eTheirTeam)
    m_kGame.getElapsedGameTurns()
    HiredHand::eval(NO_PLAYER, 50, 12)

`eval(NO_PLAYER,...)` again derives elapsed obligation from the same team-vs-team
`AI_getAtWarCounter`. No player-local state distinguishes A1's copy from A2's.

DETERMINISTIC REPRODUCTION
--------------------------
Let AI teammates A1+A2 have been at war with target team B since scenario turn 0 and
consider peace before the 12-turn historical-role obligation expires.

For A1:
    rival B is the canonical target member -> add historical-role utility H.

For A2:
    rival B is again the canonical target member -> add the same historical-role H.

A one-member agent team therefore contributes H while an otherwise identical two-member
agent team contributes 2H solely because the same team-owned war counter is visited
through two players.

This survives even with a one-player target team; it is independent of F128's original
B+C target-side multiplication.

ANCESTRY / DEDUP
----------------
- F128/KI#451 proved target-member repetition of the same historical-role and paid-war
  target-team obligation in inherited AdvCiv. Practical 6277's target-leader guard fixes
  that exact dimension.
- F730 is the separate AGENT-member dimension left by the repair. No later HiredHand
  owner in the album addresses it.
- F729/KI#1050 also concerns aspect objects reused across agent teammates, but its root
  is persistent physical-site identity crossing a player-local marginal formula.
  HiredHand needs no persistent container here; this is direct repeated evaluation of
  one purely team/global term.
- The album's prior "Sponsorship in multi-member teams" negative control remains valid
  for an ordinary freshly signed paid-war deal: `CvDeal` records that bounty in the
  specific signing player's cache, so the PA-free sponsorship branch does not
  automatically multiply merely because the agent team has two members. F730 does not
  rely on sponsorship state at all.

REPAIR BOUNDARY
---------------
Charge the historical-role term once per evaluating agent TEAM as well as once per
hostile target TEAM. A simple "agent team leader only" guard needs care because the
existing HiredHand top gate deliberately skips human `kWe`; mixed human/AI teams should
not accidentally lose an AI historical-role contribution solely because the current
team leader is human. A coherent per-team/pre-evaluation term or deterministic eligible
AI representative is safer.

Do not move the whole HiredHand aspect behind an agent canonical guard: the paid-war
cache and relationship-memory branches contain genuine player ownership.


F731 / provisional KI#1052 -- CONFIRMED -- PRACTICAL-6277 HIREDHAND SIBLING LEFT LIVE
"WE HIRED AN ALLY" OBLIGATION STILL REPEATS ONCE PER TARGET TEAM MEMBER THROUGH A
TEAM-KEYED SPONSOR PATH (AND NORMAL BOOKKEEPING BROADCASTS THE MEMORY PATH TOO)
------------------------------------------------------------------------------------------------
CONFIRMED current AdvCiv-SAS inherited/surviving UWAI target-team duplication defect
adjacent to the practical-6277 repair.

ROOT
----
Practical 6277 deliberately left the second HiredHand branch outside the target-team
canonical block because it contains player memories:

    // Have we hired someone to help us against eThey?
    for (... itAlly ...)
    {
        if (kAlly.uwai().getCache().sponsorAgainst(eTheirTeam) == eWe ||
            (kWe.AI_getMemoryCount(kAlly.getID(), MEMORY_ACCEPTED_JOIN_WAR) > 0 &&
             kThey.AI_getMemoryCount(eWe, MEMORY_HIRED_WAR_ALLY) > 0))
        {
            ...
            rUtility += eval(kAlly.getID(), 25, 10);
        }
    }

The first alternative is nevertheless fully TARGET-TEAM keyed:

    kAlly.uwai().getCache().sponsorAgainst(eTheirTeam) == eWe

For fixed agent A, ally D and hostile team B+C it has exactly the same truth value on
B's pass and C's pass. `eval(D,25,10)` then measures both A's and D's participation
against the same `eTheirTeam` through team at-war counters, so the resulting obligation
is also the same team-war quantity.

The memory alternative does not provide a reliable target-member uniqueness boundary
either. Normal `TRADE_WAR` bookkeeping explicitly iterates every attacked AI team
member and performs:

    itAttackedMember->AI_rememberEvent(eToPlayer, MEMORY_HIRED_WAR_ALLY);

The `DIPLOEVENT_JOIN_WAR` path likewise records `MEMORY_HIRED_WAR_ALLY` for every alive
member of the attacked team. Thus an ordinary hire against multi-member team B+C can
make the memory predicate true for BOTH B and C as well.

DETERMINISTIC REPRODUCTION
--------------------------
1. Agent A hires AI ally D to declare war on hostile team B+C.
2. D's cache records A as `sponsorAgainst(team B+C)`; ordinary trade/event bookkeeping
   also gives B and C the hired-war memory about A.
3. Later A considers peace while D still fights B+C and the 10-turn "fight along our
   hireling" obligation is live.
4. HiredHand evaluates rival B: the sponsor predicate is true and adds obligation H.
5. HiredHand evaluates rival C: the SAME sponsor predicate is true against the same team
   and adds the same H again.

The number of promises/hired allies has not changed; only the hostile team's player
partition has. One-member target B yields H; B+C yields 2H.

ANCESTRY / DEDUP
----------------
- F128/KI#451 explicitly said this neighboring "we hired someone" branch contained
  player-specific memory and should be reviewed on its own; it did NOT prove or own the
  branch. Practical 6277 therefore retained it intentionally.
- The present proof uses the branch's untouched team-keyed `sponsorAgainst(eTheirTeam)`
  alternative and the actual broadcast producer for `MEMORY_HIRED_WAR_ALLY`. It is not
  a restatement of F128's "we were hired" bounty branch.
- F228/KI#551 and later sponsorship lifecycle findings concern target-team key migration,
  overflow or stale cleanup. They can change whether sponsorship survives, but none
  prevents this per-target-member duplication while the state is valid.
- Trade-war coalition pricing/bookkeeping findings elsewhere concern redundant trade
  execution and payment valuation; F731 needs only one ordinary successful hired war.

ANCESTRY
--------
The branch and its team-keyed sponsor lookup are inherited from AdvCiv. Practical 6277
left them unchanged except for surrounding code movement. F731 is therefore an inherited
HiredHand sibling exposed by the repair audit, not a regression created by SAS.

REPAIR BOUNDARY
---------------
Separate the two ownership questions before canonicalizing:
- the fact that A hired D to fight target TEAM T and the 10-turn fight-along obligation
  are team-target events and should be charged once for T;
- genuinely target-player-specific memories, if they are intended to affect strength or
  eligibility, must be aggregated deliberately rather than allowing a full fixed
  obligation once for each member whose memory happens to be positive.

A target-team canonical pass can naturally own the team-keyed sponsor alternative. If
memory is meant to act as a fallback when no cache sponsorship exists, ask whether ANY
living target member carries the corresponding hired-war memory, then charge the team
obligation once. Do not simply leave the whole mixed OR expression inside each rival
player pass.


PRACTICAL 6277 BUNDLE VERDICT
-----------------------------
- Original KI#451's target-side canonicalization of the "we were hired" sponsor/bounty
  term is effective for an ordinary deal.
- The proposed simulated-target-leader-elimination failure is rejected because
  `getWarsContinued` is pre-simulation current-war state.
- F730/KI#1051: historical-role utility is still repeated across agent teammates.
- F731/KI#1052: the separate "we hired an ally" branch still repeats a target-team
  obligation across target teammates.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP778
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F731 remain confirmed.
F730 / provisional KI#1051: CONFIRMED.
F731 / provisional KI#1052: CONFIRMED.
F732 / provisional KI#1053: UNUSED.
NEXT: practical 6276 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP778
===============================================================================


===============================================================================
C031-WIP779 APPEND -- practical 6276 repair audit (KI#446 Bellicosity)
===============================================================================

SCOPE
-----
Chronology cursor practical 6276 / commit ecd30a6baa11145a3bddf0ed388005d64d377393:
    fix inherited AdvCiv UWAI Bellicosity team-loss accounting (KI#446);
    defer KI#445

Current maintained source was checked against F123/KI#446's original nonlinear
team-partition proof and the generic WarUtilityAspect/WarEvaluator member loops.

PRACTICAL 6276 / KI#446 -- CLEAN
--------------------------------
The repair now evaluates one hostile team through its current living team leader, but
does not use that representative player's reachability or losses as team proxies:

    if (... || eThey != kTheirTeam.getLeaderID())
        return;

    for (MemberIter itMember(eTheirTeam); ...)
        if (kWeAI.canReach(itMember->getID())) ...

and, per military branch:

    rOurLostPow = lostPower(eWe, branch);
    rTheirLostPow = sum(lostPower(member, branch) for living target-team members);
    min(2.35 * rOurLostPow, rTheirLostPow) - 0.75 * rOurLostPow;

This restores exactly the ownership boundary proved by F123: one current agent player's
loss budget is compared with the entire hostile TEAM's loss budget before the nonlinear
anti-third-party cap. The current agent's personality and present branch power remain
player-local, which is consistent with WarEvaluator deliberately summing distinct
agent-member utilities.

ADVERSARIAL CONTROLS
--------------------
- Simulated elimination does not invalidate the hostile-team canonical pass. The
  current `CvTeam::getLeaderID()` represents actual live game membership, while
  MilitaryAnalyst stores simulated losses separately; an actually living leader remains
  in the generic rival surface even if the scenario predicts its defeat.
- The target leader is not used as a reachability proxy. Any living target member can
  satisfy `kWeAI.canReach`, preserving the original player-local operational gate while
  avoiding representative dependence.
- Hostile member losses are physical/player-owned branch losses and are summed before
  the `min` cap. No second per-target-member cap survives.
- Repeating Bellicosity for distinct AGENT teammates is not the F123 defect: each pass
  uses that teammate's own personality, lost power, reachable target members and cached
  present military power. No team-uniform fixed quantity comparable to F730's
  historical-role term was found here.
- Current-war `getNumWars` gating remains outside the repaired loss aggregation and does
  not repartition the hostile team's credited losses.

No independent repair-side root survived practical 6276.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP779
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F731 remain confirmed.
F732 / provisional KI#1053: UNUSED.
NEXT: practical 6275 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP779
===============================================================================


===============================================================================
C031-WIP780 APPEND -- practical 6275 repair audit (KI#443-KI#444 startup clocks)
===============================================================================

SCOPE
-----
Chronology cursor practical 6275 / commit bd16fb397cec269fa99871050bd0186e3986c9d3:
    fix inherited AdvCiv UWAI startup clocks (KI#443-KI#444);
    reconcile C++ audit C015-WIP13

Both repaired clocks were checked against their actual game-time producers and refresh
lifecycle, not only against the practical diff.

PRACTICAL 6275 / KI#443 -- FAIRPLAY LATER-ERA BASELINE -- CLEAN
---------------------------------------------------------------
The repair reconstructs the ordinary start-era game turn as:

    iScheduledTurns = sum(CvGameSpeedInfo turn increments)
    iNormalEraStartTurn = iScheduledTurns * StartPercent(startEra) / 100

This is exactly the normal `CvGame::setStartTurnYear(0)` producer:

    iStartTurn = sum(kSpeed.getGameTurnInfo(i).iNumGameTurnsPerIncrement);
    iStartTurn *= getStartEra().StartPercent;
    iStartTurn /= 100;

The repair then keeps only:

    iExtraStartTurns = max(0, getStartTurn() - iNormalEraStartTurn)

and converts that exceptional offset through the same `rTrainMod` timebase used for
elapsed turns before applying the pre-existing map-density and StartPercent policy.
Thus an ordinary later-era start no longer receives its absolute calendar position as
an extra grace period, while Ancient/Advanced-Start or scenario-style positive extra
start offsets remain compensable in normalized turns.

The `max(0,...)` treatment also matches F120/KI#443's recorded repair boundary; the
finding never established a contract requiring negative compensation for a scenario
whose explicit absolute start turn lies earlier than the ordinary era baseline.

No arithmetic-order mismatch with game initialization and no new repair root found.

PRACTICAL 6275 / KI#444 -- TERMINAL-ERA STARTUP GRACE -- CLEAN
-------------------------------------------------------------
KingMaking and UWAICache threat rating now both use:

    rStartupSpeed = 2 / per100(gameSpeedPercent + trainPercent)
    if (currentEra <= startEra && elapsedGameTurns * rStartupSpeed < 25)
        suppress startup-sensitive signal;

This is the same normalized startup clock already used by `DramaticArc::preEvaluate`,
which F121 identified as the positive control. Era advancement still exits the guard
early; if no later era exists, elapsed normalized time ends it after the bounded grace.

For threat ratings, the surrounding `UWAICache::update` lifecycle recomputes
`updateThreatRatings()` every eligible turn after player history exists. The rating is
therefore not stuck at the zero cached during the grace once the 25-turn condition
expires.

KingMaking's `preEvaluate` runs afresh for each evaluation and clears its winner sets
before the guard, so it likewise begins normal computation immediately after the gate
expires. No stale startup state survives the repaired boundary.

PRACTICAL 6275 BUNDLE VERDICT
-----------------------------
KI#443 CLEAN against the exact `CvGame::setStartTurnYear` baseline producer.
KI#444 CLEAN against DramaticArc's normalized-clock control and UWAICache refresh
lifecycle.
No new root consumed.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP780
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F731 remain confirmed.
F732 / provisional KI#1053: UNUSED.
NEXT: practical 6274 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP780
===============================================================================


===============================================================================
C031-WIP781 APPEND -- practical 6274 repair audit (KI#441-KI#442)
===============================================================================

SCOPE
-----
Chronology cursor practical 6274 / commit 74a53043ed15c4e896fb99c4fe912d01593bdfc2:
    fix inherited AdvCiv UWAI revolt and capitulation defects (KI#441-KI#442);
    defer KI#440

PRACTICAL 6274 / KI#441 -- REVOLT ASSET SIGN -- CLEAN
-----------------------------------------------------
The repaired city value is:

    iCityAssets = max(0, pCacheCity->getAssetScore());

and the same nonnegative quantity enters both the total relevant-asset denominator and
expected revolt-loss numerator. Later KI#455 moved Revolts from persistent cross-rival
member state into one local scenario-wide city union per agent member, but retained that
same clamped value in both places.

Thus a maintenance-heavy negative-asset city can no longer become positive war utility
or cancel another city's threatened value, and the later ratio-lifetime repair did not
reintroduce a signed denominator mismatch. A zero-valued city may still be admitted to
the physical union, but contributes zero to both quantities and cannot suppress a
second city because plot identity is unique per city.

No new interaction root found.

PRACTICAL 6274 / KI#442 -- CAPITULATION TEAM VECTOR INDEX -- CLEAN
-----------------------------------------------------------------
`MilitaryAnalyst::prepareResults` now unions each player node's accepted-capitulation
set into:

    m_capitulationsAcceptedPerTeam[TEAMID(ePlayer)]

rather than indexing a TeamTypes-sized vector with the raw PlayerTypes value.

The producer confirms the ownership boundary. `InvasionGraph::Node::setCapitulated`
walks player nodes and, for every non-eliminated member of the accepting master team,
inserts the capitulating TEAM into that member node's `m_capitulationsAccepted` set.
`Node::getCapitulationsAccepted(TeamSet&)` then performs set insertion/union, so repeated
copies from multiple master members collapse naturally when `prepareResults` writes all
of them to the actual master-team slot.

Simulated player elimination does not create a missing-master result: the producer
records acceptance on every surviving master member node; one surviving member is
sufficient for the later union. No actual team identity is mutated by the invasion
simulation, so `TEAMID(ePlayer)` remains the correct storage key during result
preparation.

No independent repair-side root survived practical 6274.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP781
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F731 remain confirmed.
F732 / provisional KI#1053: UNUSED.
NEXT: practical 6273 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP781
===============================================================================


===============================================================================
C031-WIP782 APPEND -- practical 6273 repair audit (KI#437-KI#439)
===============================================================================

SCOPE
-----
Chronology cursor practical 6273 / commit e04fcd41a2b7a8a9bcfac37a5654a6f6baa9a1b8:
    fix inherited AdvCiv UWAI utility defects (KI#437-KI#439);
    reject KI#436 and reconcile C++ audit C015-WIP11

PRACTICAL 6273 / KI#437 -- FAIRPLAY TARGET-TEAM THIRD-PARTY EXCLUSION -- CLEAN
--------------------------------------------------------------------------------
The repair excludes every player whose `getTeam()` equals the current target team before
that player can enter either the potential-other-enemy denominator or enemy/ally
classification. This closes both original manifestations: a target teammate can no
longer dilute dogpile pressure and can no longer be misclassified through
`AI_shareWar(ownTeam)`.

The remaining third-party loop is intentionally player-based. A separate free-civ
player on another team represents additional economic/military capacity even when that
team has another member, so simple team deduplication is not established by KI#437's
invariant. No new root was promoted from that broader policy question.

PRACTICAL 6273 / KI#438 -- NORMALIZED PEACE DURATION -- CLEAN
-------------------------------------------------------------
The previously computed game-speed-adjusted peace duration is now used consistently for
both the long-peace gate and the subsequent linear decay:

    rAtPeaceTurns = 1.5 * rawPeaceTurns / (GoldenAgePercent/100 + 0.5)
    if (rAtPeaceTurns > threshold)
        rMult = max(1/3, 1 - (rAtPeaceTurns - threshold) / 30)

This preserves the historical expression that was already present but accidentally
discarded. Normal speed remains exactly unchanged; faster/slower speeds now move both
the threshold crossing and decay on the same normalized timebase. No mixed raw/scaled
term remains in that policy.

PRACTICAL 6273 / KI#439 -- DRAMATICARC EXACT WAR-HISTORY COUNTERS -- CLEAN
---------------------------------------------------------------------------
Current counter lifecycle confirms the repaired predicate:

    HasMetCounter > TurnsAtPeace

`CvTeam::declareWar` resets bilateral `TurnsAtPeace` to zero; `HasMetCounter` continues
to accumulate exact known-contact turns. During uninterrupted peace, `TurnsAtPeace`
continues accumulating and no false war history is introduced. After any counted war,
the reset makes HasMet strictly greater and the exact current postwar peace duration is
available immediately, including after a short war that the removed eight-turn tolerance
misclassified.

The separately repaired Permanent-Alliance counter reconciliation remains a broader
history-policy concern, not a defect in this exact current-war reset predicate.

PRACTICAL 6273 BUNDLE VERDICT
-----------------------------
KI#437 CLEAN at the independent-third-party boundary.
KI#438 CLEAN at game-speed normalization.
KI#439 CLEAN against exact counter producers/reset lifecycle.
No new root consumed.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP782
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F731 remain confirmed.
F732 / provisional KI#1053: UNUSED.
NEXT: practical 6272 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP782
===============================================================================


===============================================================================
C031-WIP783 APPEND -- practical 6272 repair audit (KI#433 KingMaking team state)
===============================================================================

SCOPE
-----
Chronology cursor practical 6272 / commit 038724dba92df55c2b5268de4341f72ab2ac47c2:
    fix inherited AdvCiv UWAI KingMaking team-state modeling (KI#433)

The repaired TeamSet winner representation, team Score/Time aggregation, accepted-vassal
score ownership and team-population Domination forecast were rechecked against their
actual game producers. Those conversions are coherent. Adversarial review nevertheless
found one deterministic repair-side representative leak in the new once-per-target-team
consumer, plus one older inherited Space4 prediction defect in a deliberately retained
player-local branch.


F732 / provisional KI#1053 -- CONFIRMED -- PRACTICAL-6272 KI#433 REPAIR-SIDE LEAK
ONCE-PER-TEAM KINGMAKING USES THE ADMINISTRATIVE TARGET LEADER'S PLAYER ATTITUDE
-------------------------------------------------------------------------------
CONFIRMED current AdvCiv-SAS repair-side UWAI aggregation/representative defect.

ROOT
----
Practical 6272 correctly changed the winner sets and several victory quantities from
player-owned to team-owned. To avoid charging that shared KingMaking state once for each
rival teammate, `KingMaking::evaluate` now keeps only:

    if (eThey != kTheirTeam.getLeaderID())
        return;

The very next behavioral input, however, is still the generic aspect's player-pair
attitude:

    int iAttitude = (kWe.isHuman() ? ATTITUDE_ANNOYED : towardThem());
    ...
    if (iAttitude >= ATTITUDE_FRIENDLY)
        return;

`WarUtilityAspect::evaluate(PlayerTypes)` defines `towardThem()` from exactly the current
agent PLAYER toward exactly the current rival PLAYER:

    kWe.AI_getAttitudeVal(eThey)

Thus team-owned victory threat/assets are evaluated once, but their entire attitude
multiplier is selected from whichever teammate happens to be `CvTeam::getLeaderID()`.

The engine does not define team diplomacy this way. `CvTeamAI::AI_getAttitudeVal(TeamTypes)`
explicitly averages member-to-member attitude values. Conversely `CvTeam::updateLeaderID`
chooses an administrative representative by a simple ordering rule: prefer an alive
member, prefer a human, otherwise retain the first/best qualifying player. Nothing in
that rule says the leader is the team's diplomatic-attitude representative.

DETERMINISTIC CONSEQUENCE
-------------------------
Let rival Permanent-Alliance/team T contain B and C with the same team-owned winner set,
score, projected asset change and war state. Let current agent A be FRIENDLY with B but
ANNOYED with C.

If B is T's current leader:

    eThey = B -> towardThem() == FRIENDLY -> KingMaking returns 0 for the whole team.

If C is T's current leader instead:

    eThey = C -> towardThem() == ANNOYED -> the same team-owned victory/asset change
    receives a substantial anti-winner utility.

Changing only the administrative representative can therefore change or erase the
coalition-wide KingMaking value. Human-member preference makes this especially visible
in mixed human/AI teams; leader replacement after an actual member death can also change
which pairwise attitude is sampled.

ANCESTRY / DEDUP
----------------
- Base AdvCiv 1.14 evaluated every rival player and therefore had the broader F110/KI#433
  player/team duplication/modeling defect, but did not contain this target-leader guard.
- Practical 6272 introduced the guard as the repair for repeated shared state. F732 is
  therefore a repair-side representative leak, not a restatement of F110.
- F730's target-leader guard is in HiredHand but its surviving defect is repetition over
  AGENT teammates of a fully team-keyed historical obligation; it does not own this
  player-attitude representative problem.
- Whole-album searches found no existing KingMaking leader-attitude owner.

REPAIR BOUNDARY
---------------
Keep once-per-target-team ownership, but do not silently equate team leader with team
attitude. A robust repair should define an explicit aggregation appropriate to the
CURRENT agent member (for example, aggregate that player's attitude toward the target
team's living members), or otherwise document and implement a deliberate team-attitude
policy. Simply replacing `towardThem()` with a full `kOurTeam.AI_getAttitude(...)` would
also change the agent-member dimension and should be reviewed rather than assumed.


F733 / provisional KI#1054 -- CONFIRMED -- INHERITED BASE ADVCIV KINGMAKING SPACE4 BUG
PREDICTED LOSS OF A PRE-LAUNCH CAPITAL FALSELY THWARTS THE SPACE4 CANDIDATE
-------------------------------------------------------------------------------
CONFIRMED inherited Base AdvCiv/K-Mod-era KingMaking prediction defect; not caused by
practical 6272, but exposed while adversarially reviewing the player-local Space branch
that KI#433 deliberately retained.

ROOT
----
For predicted stage-4 Space victory, `KingMaking::anyVictory` special-cases the current
capital:

    if ((eFlags & AI_VICTORY_SPACE4) && iStage == 4)
    {
        if (kPlayer.hasCapital() &&
            militAnalyst().lostCities(ePlayer).count(
                kPlayer.getCapital()->plotNum()) <= 0)
        {
            return true;
        }
    }

Space4 is excluded from the later `bSpaceValid` path. Therefore, if the military
scenario predicts loss of that current capital, this member's Space4 candidacy is
categorically dropped.

That implication is false before launch. Current SAS KI#190 already established both by
source and empirical testing that `CvCity::kill` calls `CvTeam::resetVictoryProgress`
when a capital falls, but `resetVictoryProgress` iterates only NONDEFAULT active victory
countdowns. Before launch there is no active countdown; completed spaceship components
are team Projects, the capital relocates, and those parts survive. The KI#190 prototype
actually chased successive English capitals while England retained 75% spaceship
completion.

PRE-LAUNCH SPACE4 IS REACHABLE
------------------------------
`CvPlayerAI::AI_calculateSpaceVictoryStage` can return stage 4 before launch. Once Apollo
exists, it derives a team project ratio and can return 4 when the ratio is at least about
2/3 and production is sufficient even while:

    GET_TEAM(getTeam()).getVictoryCountdown(eSpace) < 0

No launch/countdown requirement protects KingMaking's capital-loss shortcut.

DETERMINISTIC CONSEQUENCE
-------------------------
Take a living Space4 player/team with no active launch countdown, sufficient production,
and at least one other city. A contemplated war scenario captures the CURRENT capital
but does not eliminate the player/team.

Actual game mechanics:
- a replacement capital is selected;
- pre-launch spaceship Projects remain;
- the team can continue/launch its spaceship.

KingMaking prediction:
- sees the old capital plot in `lostCities(ePlayer)`;
- refuses the only Space4 success path for that member;
- can therefore remove the team from `m_winningFuture` and value the war as having
  thwarted an imminent Space threat when the decisive progress was not reset.

After launch the capital test is directionally correct because an active countdown makes
`resetVictoryProgress` erase the launch and spaceship Projects. The defect is the missing
pre-launch/active-countdown distinction.

ANCESTRY / DEDUP
----------------
- Base AdvCiv 1.14 contains the same unconditional Space4-capital-loss test.
- KI#190 owns city-assault TARGETING/EXECUTION of a launched spaceship capital and, as a
  factual control, documents why pre-launch capital capture is not decisive. It does not
  repair KingMaking's separate scenario prediction.
- F110/KI#433 owns team-victory state/modeling; practical 6272 intentionally retained
  player-local Space heuristics. F733 is a narrower independent false state transition
  that also reproduces for a solo team.
- No prior album root owns this KingMaking pre-launch-vs-launched distinction.

REPAIR BOUNDARY
---------------
The current-capital-loss shortcut should only categorically invalidate Space4 when that
team has an active Space-victory countdown (or when another independently proven
condition makes the trajectory truly invalid). Pre-launch capital loss may still deserve
a softer setback heuristic, but it must not be treated as equivalent to the launched
spaceship reset.


OTHER PRACTICAL-6272 CHALLENGES -- NO ADDITIONAL ROOT PROMOTED
--------------------------------------------------------------
- `adjustedContenderScore(TeamTypes)` sums each member's current/predicted member score,
  keeps the member-local commerce/overseas-peaceful adjustment inside that sum, then adds
  each accepted-vassal team's shared score once. This matches the repair's ownership
  split; no repeated accepted-vassal contribution remains.
- Predicted Domination now sums lost/gained city population over all current team members
  and compares against current team population, matching `CvGame::testVictory`'s
  team-population ownership.
- TeamSet winner cardinality and self-recognition now use TeamTypes consistently in the
  repaired catch-up/competition calculations.
- Split-team Culture candidacy was challenged because the real culture-victory test
  counts qualifying cities across all team members while `AI_calculateCultureVictoryStage`
  is player-strategy-oriented. That broader heuristic/model question is deliberately NOT
  numbered here: KI#433 explicitly preserved player-local Culture strategy inputs, and
  the stage code contains several intentional approximations. A stronger behavioral
  invariant would be required before treating that design choice as a new bug.

PRACTICAL 6272 BUNDLE VERDICT
-----------------------------
KI#433's principal team Score/Time, winner-set, accepted-vassal-score and Domination
repairs survive. F732 proves a new target-leader attitude representative leak in the
repair's once-per-team consumer. F733 proves an adjacent inherited Space4 forecast bug
that the review exposed but practical 6272 did not introduce.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP783
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6271 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP783
===============================================================================


===============================================================================
C031-WIP784 APPEND -- practical 6271 repair audit (KI#432 explicit asset victim)
===============================================================================

SCOPE
-----
Chronology cursor practical 6271 / commit e987f8de8fc28b24c5f854fedac9c9e61a0ea042:
    fix inherited AdvCiv UWAI Kingmaking ally/vassal asset attribution (KI#432)

PRACTICAL 6271 / KI#432 -- CLEAN UNDER CURRENT COMPOSITION
---------------------------------------------------------
The generalized `netLostAssetScore(eVictim,...)` contract survives the adversarial
caller/sub-helper audit.

- City losses, city gains, nuke losses, blockade pressure and flipped-tile losses all
  derive from the explicit `eVictim`; no surviving `eThey`, `kThey` or `kTheirTeam`
  dependency remains inside those victim helpers.
- Ordinary Loathing, Assistance and Rebuke callers explicitly preserve `eThey` as the
  victim. MilitaryVictory correctly keeps `eThey` fixed while varying the recipient
  across the agent and its vassals, so each recipient contributes only losses it caused.
- KingMaking now passes the actual teammate/vassal loop player. Existing vassals and
  teams predicted to capitulate both receive the intended 1/2 coalition weight.
- A predicted new vassal is treated as a change relative to the present coalition:
  half of its estimated remaining assets is subtracted as a coalition gain, while it is
  deliberately not inserted into the PRESENT-asset denominator. Existing vassals are
  already part of that baseline and therefore do enter the denominator at half weight.
- Later KI#456/KI#465 repairs compose correctly with KI#432: flipped-tile numerator and
  denominator use the explicit victim TEAM, one fixed team threshold and a category
  value apportioned through living-member callbacks. The old wrong-rival-team divisor
  does not survive current source.

The interaction with practical 6272's later target-leader-only KingMaking evaluation was
also challenged. `theirRelativeLoss` begins with `isPartOfAnalysis(eThey)`, but
`MilitaryAnalyst` populates that set by living PLAYERS whose TEAM is currently/future at
war. For a multi-member analyzed target team, the living administrative leader shares
that same team war relation and is included; the optimization does not silently skip the
coalition scan merely because another teammate is the operational participant.

No independent repair-side root survived practical 6271.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP784
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6270 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP784
===============================================================================


===============================================================================
C031-WIP785 APPEND -- practical 6270 repair audit (KI#434 AP owner, KI#435 war anger)
===============================================================================

SCOPE
-----
Chronology cursor practical 6270 / commit 94e2d279e8e473896374d06a9a55cbd57fb9b89b:
    fix inherited AdvCiv UWAI AP and war-anger defects (KI#434, KI#435)

PRACTICAL 6270 / KI#434 -- CLEAN UNDER CURRENT COMPOSITION
---------------------------------------------------------
The Apostolic-Palace conquest repair now tests the same player that the surrounding
calculation is actually valuing.

`MilitaryVictory::progressRatingDiplomacy` obtains the cached city's PRE-SIMULATION
owner as `eCityOwner`, then uses that old owner's attitude toward us when deciding how
much useful vote population the conquest removes. `CvPlayer::isFullMember(eVS)` is
player-local state: it checks that player's state religion, vote-source civic, loyalty
and voting membership. The inherited `kThey.isFullMember(eVS)` therefore really was a
wrong-object test when a partner/vassal conquest was attributed through a rival other
than the city's old owner.

The current repair consistently uses:

    PlayerTypes eCityOwner = kCacheCity.city().getOwner();
    CvPlayerAI const& kCityOwner = GET_PLAYER(eCityOwner);
    ...
    if (bSecular || kCityOwner.isFullMember(eVS))
        eOwnerTowardUs = kCityOwner.AI_getAttitude(eWe);

That ownership is also coherent with `addConquestsByPartner`: the weighted conquest map
can contain cities taken by partners, so the later old-owner lookup cannot safely be
replaced by the generic current rival/conqueror. The neighboring TEAM-human test is
inherited policy and not part of KI#434's wrong-player defect.

No repair-side AP membership root survived.

PRACTICAL 6270 / KI#435 -- CLEAN UNDER CURRENT COMPOSITION
---------------------------------------------------------
The war-anger divisor repair matches both producer and consumer domains.

`UWAICache::updateWarAnger` first calculates one anger contribution per ENEMY TEAM, then
stores a value for every living enemy PLAYER. Current source divides each team-keyed
contribution by:

    PlayerIter<CIV_ALIVE,MEMBER_OF>::count(itEnemy->getTeam())

which is the same living-member population over which the cache values are materialized.
`PublicOpposition` later consumes `angerFromWarWeariness(eThey)` once per rival player;
summing the living members of one enemy team therefore reconstructs that team's one
intended contribution. The old owner-team divisor could duplicate or dilute the result
whenever team sizes differed.

A stale-cache edge from the early returns was explicitly challenged and rejected.
Ordinary `UWAICache::update` begins with `clear(true)`, and `clear(true)` executes:

    m_arWarAnger.reset();

before `updateWarAnger()`. Therefore anarchy, zero current anger or zero current
war-weariness contribution leaves this turn's cache at zero rather than retaining a
prior turn's attribution.

The living enemy-member denominator also avoids the historical dead-assigned-member
mismatch and matches the exact live iterator that receives the split. No independent
repair-side root survived KI#435.

PRACTICAL 6270 BUNDLE VERDICT
-----------------------------
KI#434 and KI#435 both survive current-source proof, lifecycle pressure testing and
whole-album dedup. F734 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP785
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6269 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP785
===============================================================================


===============================================================================
C031-WIP786 APPEND -- practical 6269 repair audit (KI#430 coalition, KI#431 report)
===============================================================================

SCOPE
-----
Chronology cursor practical 6269 / commit 619d8711290d47538e35a610e1b63c2be5a7a340:
    fix inherited AdvCiv UWAI coalition and reporting defects (KI#430, KI#431)

PRACTICAL 6269 / KI#430 -- CLEAN UNDER CURRENT COMPOSITION
---------------------------------------------------------
The repaired PreEmptiveWar coalition snapshots now use matching ownership on the present
and predicted sides.

- `kOurTeam.getNumCities()` is a team-wide base, and current source subtracts predicted
  city losses for every alive team member rather than only `eWe`.
- Every EXISTING vassal contributes half of its current cities to `rOurCurrentCities`
  and half of its post-loss cities to `rOurPredictedCities`.
- A team predicted to capitulate to us contributes half weight only to the predicted
  side, which is the intended state transition.
- The inherited policy that our own/our-vassals' fresh conquests are too recent to count
  toward long-term productive power is preserved consistently; this is not an omitted
  symmetric gain under the model's stated assumption.

Later KI#460 composition was explicitly checked. `PreEmptiveWar::preEvaluate` resets a
per-agent-member rival-team set, and current `evaluate` consumes each rival TEAM forecast
once through any reachable member. The current/predicted coalition reconstruction is
therefore at the same target-team ownership scope rather than being re-multiplied through
target members. No later ownership change reopens KI#430.

PRACTICAL 6269 / KI#431 -- CLEAN
--------------------------------
The UlteriorMotives logging call now supplies exactly the missing third value requested
by its format string:

    log("Attitude level towards %s: %d, refusal thresh: %d",
        leaderName, towardThem(),
        kOurPersonality.getDeclareWarRefuseAttitudeThreshold());

The supplied threshold is the same value used immediately above to derive the suspicion
factor. `%d` is appropriate after ordinary variadic integral promotion of the attitude /
threshold enum/integer values. This is a bounded optional-report UB repair; no adjacent
format ownership issue survived the earlier whole-file mechanical audit or this reread.

PRACTICAL 6269 BUNDLE VERDICT
-----------------------------
KI#430 and KI#431 survive current-source proof, later-composition pressure testing and
whole-album dedup. F734 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP786
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6268 / commit 98142d25a5114685ee9a9aafc9923e8ddfba493d
      (KI#427-KI#429) -> current-source proof -> adversarial repair review -> dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP786
===============================================================================


===============================================================================
C031-WIP787 APPEND -- practical 6268 repair audit (KI#427-KI#429)
===============================================================================

SCOPE
-----
Chronology cursor practical 6268 / commit 98142d25a5114685ee9a9aafc9923e8ddfba493d:
    fix inherited AdvCiv UWAI defects in trade, Domination and Kingmaking
    (KI#427-KI#429)

PRACTICAL 6268 / KI#427 -- CLEAN
--------------------------------
`partnerUtilFromTrade` increments the qualifying resource-deal counter before the cap
check. Changing the inherited `>= 4` rejection to `> 4` therefore admits counts 1, 2, 3
and 4 and rejects only the fifth and later qualifying resource deals, matching both the
nearby `iMaxResourceTrades = 4` constant and the diagnostic wording "in excess of 4".
Dual-item/unsupported deals are skipped before this counter, so they do not consume a
slot. No repair-side off-by-one or stale-counter edge survived.

PRACTICAL 6268 / KI#428 -- CLEAN
--------------------------------
The Domination population-completion boundary matches the real game predicate.
`CvGame::testVictory` rejects population only when:

    100 * teamPopulation < totalPopulation * adjustedPopulationPercent

so exact equality satisfies that component. UWAI's integer target is the mathematical
ceiling of the same percentage product. `iPopToGo <= 0` is therefore the correct
completed-population branch, and exact zero must not flow to the division path.

The preceding `rPopGained.decreaseTo(iPopToGo)` can set the temporary gain to zero or a
negative value when the threshold is already met, but that value is not consumed by the
completed-population branch; that branch intentionally estimates only remaining
city/land progress. No new arithmetic edge survived.

PRACTICAL 6268 / KI#429 -- CLEAN UNDER LATER KI#433 COMPOSITION
--------------------------------------------------------------
The repair's essential invariant remains intact after the later team-victory rewrite.
Current `adjustedContenderScore(TeamTypes, bPredict)` first builds each member's
current/predicted score, applies that member's commerce and overseas peaceful-victory
adjustments, and sums the result into the team contender quantity. `addLeadingTeams`
then calls that SAME helper both when constructing `rBestScore` and when testing each
candidate against the human/AI margin.

Thus the original denominator/numerator mismatch has not reappeared at team scope. The
accepted-vassal predicted-score addition and the team-owned Score/Time conversion belong
to KI#433 and were separately adversarially audited at practical 6272; they do not
invalidate KI#429's same-quantity repair.

PRACTICAL 6268 BUNDLE VERDICT
-----------------------------
KI#427, KI#428 and KI#429 survive current-source proof, boundary testing, later-composition
review and whole-album dedup. F734 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP787
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6267 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP787
===============================================================================


===============================================================================
C031-WIP788 APPEND -- practical 6267 repair audit (KI#420-KI#421 PA state)
===============================================================================

SCOPE
-----
Chronology cursor practical 6267 / commit fe0e4abbe4ef49a910bff90d7db2f02143855c53:
    fix inherited Permanent-Alliance state defects (KI#420, KI#421)

PRACTICAL 6267 / KI#420 -- CLEAN UNDER CURRENT MERGE ORDER
---------------------------------------------------------
The exact bilateral HasMetCounter and TurnsAtPeace repair is coherent with the actual
Permanent-Alliance transaction order.

`shareCounters(eTeam)` runs BEFORE absorbed-player reassignment and no longer max-merges
these two exact fields. After reassignment, `iSecondTeamSize` is reconstructed from the
merged member count and each outsider's two pre-existing reverse relations remain
available in its old team-indexed arrays. Current source computes one team-size-weighted
value from outsider->survivor and outsider->absorbed, then assigns that same value to:

    outsider -> merged survivor
    merged survivor -> outsider

This restores the normal bilateral invariant and follows the neighboring exact
AtWarCounter reconciliation. The generic randomized/decaying AI relationship counters
remain under their separate inherited merge policy and are intentionally not pulled into
this repair.

The use of `getNumMembers()` (assigned team members, not merely currently alive members)
is the same long-standing K-Mod weighting domain used by all neighboring Permanent-
Alliance reverse merges; KI#420 does not introduce a new weighting population. No
repair-side timing/divisor/asymmetry root survived.

PRACTICAL 6267 / KI#421 -- CLEAN UNDER CURRENT VISIBILITY LIFECYCLE
-----------------------------------------------------------------
The stolen-visibility cleanup preserves the required stateful zero transition and its
ordering is correct.

1. `shareCounters` preserves/merges the survivor's outward stolen-visibility timer while
   both constituent identities exist.
2. Absorbed-team real plot visibility is copied into the survivor through
   `CvPlot::changeVisibilityCount`. That function automatically updates every ALIVE team
   currently stealing visibility from the survivor, so already-active survivor-directed
   espionage sight expands coherently to the newly merged real sight.
3. For each outsider, the intended post-merge outsider->survivor timer is then set from
   the weighted old outsider->survivor / outsider->absorbed timers.
4. The obsolete outsider->absorbed timer is cleared through
   `setStolenVisibilityTimer(eTeam, 0)`. Crossing positive->zero removes the associated
   plot-level `StolenVisibilityCount` from every plot still visible to the absorbed team.
5. The survivor's own obsolete survivor->absorbed relation receives the same stateful
   zero transition after all outsider transfers.

This avoids both failure modes: direct array reset would detach plot counts, while
clearing before the merged timer is calculated would destroy required input state.
Ordinary target elimination remains a separate negative control because normal sight
loss drains stolen counts through `changeVisibilityCount`; Permanent Alliance copies the
sight instead and therefore needs this explicit retirement.

Dead outsider team slots are not normal active consumers and are intentionally outside
the live cleanup guarantee; no ordinary gameplay resurrection path was found that turns
that bookkeeping detail into a current root.

PRACTICAL 6267 BUNDLE VERDICT
-----------------------------
KI#420 and KI#421 survive current merge-order, exact-counter and plot-lifecycle pressure
testing plus whole-album dedup. F734 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP788
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6266 / commit a95f6eec811dfacdee34b4a48eec1eff3bad81e0
      (KI#416-KI#417) -> current-source proof -> adversarial repair review -> dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP788
===============================================================================


===============================================================================
C031-WIP789 APPEND -- practical 6266 repair audit (KI#416-KI#417 team state)
===============================================================================

SCOPE
-----
Chronology cursor practical 6266 / commit a95f6eec811dfacdee34b4a48eec1eff3bad81e0:
    fix inherited AdvCiv team-state regressions (KI#416, KI#417)

PRACTICAL 6266 / KI#416 -- CLEAN FOR THE PROVEN REVIVAL CONTRACT
---------------------------------------------------------------
The current technology loops restore the persistent state population that AdvCiv's
alive-only refactor lost:

    PlayerIter<EVER_ALIVE,MEMBER_OF>

is used both for technology-driven era advancement and for persistent player-level
technology modifiers (feature-production, Worker speed, trade routes, health,
happiness, assets, power, tech score and commerce modifiers).

An eliminated member remains EVER_ALIVE and assigned to the same team. `setAlive(false)`
does not rebuild those persistent fields, and the normal revival path likewise does not
replay every technology already known by the team. Keeping the dead member synchronized
at acquisition/removal time is therefore the correct repair boundary.

A never-alive slot was challenged separately. Such a slot does not carry the retained
post-elimination player state that KI#416 repairs; ordinary midgame civilization creation
runs through `initInGame` / its own team initialization rather than the eliminated-member
revival contract. No normal-game path was found in which a never-alive member of an
already-established shared-tech team later activates while relying on stale persistent
technology fields. It is therefore not promoted as an incomplete-repair sibling.

The repair deliberately does NOT broaden unrelated alive-only technology side effects
such as notifications, religion founding or AI work; only persistent assigned-player
state is synchronized.

PRACTICAL 6266 / KI#417 -- CLEAN
--------------------------------
`getDefensivePower(eExcludeTeam)` now canonicalizes the prospective attacker to its
master team and skips every living player whose own master is that same team before
counting the target master or its Defensive-Pact partners.

That matches the war-obligation domain: a vassal of the attacking master belongs to the
attacking bloc and cannot simultaneously supply defensive power to the target merely
because the target master currently has a Defensive Pact with the attacker's master.
Passing a vassal ID as the exclusion also correctly canonicalizes to the master bloc;
passing `NO_TEAM` preserves the full defensive-coalition result.

The live callers use the parameter as the attacking/evaluating team, and no caller was
found that requires exclusion of only a direct team while retaining its vassals on the
defensive side. No repair-side coalition-scope root survived.

PRACTICAL 6266 BUNDLE VERDICT
-----------------------------
KI#416 and KI#417 survive current-source proof, revival/caller pressure testing and
whole-album dedup. F734 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP789
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F733 remain confirmed.
F734 / provisional KI#1055: UNUSED.
NEXT: practical 6265 / commit 007ac4a3db4bc38beee19e15285c9f1b8025ba39
      (KI#413, KI#415) -> current-source proof -> adversarial repair review -> dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP789
===============================================================================


===============================================================================
C031-WIP790 APPEND -- practical 6265 repair audit (KI#413, KI#415)
===============================================================================

SCOPE
-----
Chronology cursor practical 6265 / commit 007ac4a3db4bc38beee19e15285c9f1b8025ba39:
    repair forced-peace treaty discovery and dead-team war-weariness decay
    (KI#413, KI#415)

PRACTICAL 6265 / KI#413 -- CLEAN
--------------------------------
`CvTeam::turnsOfForcedPeaceRemaining` now first canonicalizes both queried sides to their
current master teams, then filters each live deal to exactly that unordered pair of
master blocs. Only after that identity filter does it scan BOTH complete trade lists for
`TRADE_PEACE_TREATY` and take the maximum `turnsToCancel()` among matching treaty deals.

That fixes both inherited assumptions behind KI#413: treaty position and treaty side.
Reparations or other annual items may precede the treaty without hiding it, and list
orientation no longer matters. The master-bloc filter also prevents an unrelated treaty
from being mistaken for the queried forced peace after vassal/master remapping. No
repair-side over-broad deal match or ordering root survived.

PRACTICAL 6265 / KI#415 -- ORIGINAL ROOT IMPROVED, REPAIR INCOMPLETE
-------------------------------------------------------------------
The current repair changes `CvTeam::doWarWeariness` from an alive-target iterator to:

    TeamIter<EVER_ALIVE>

That genuinely restores the inherited dead-TARGET decay branch for an ALIVE owner team.
If team A survives while former enemy B dies, A's stored A->B war weariness again receives
both ordinary decay and the stronger peace/dead-target multiplier while B is absent.

However, the repair only broadens the TARGET dimension. `doWarWeariness()` itself is
called from `CvTeam::doTurn()`, whose contract requires the OWNER team to be alive.
Therefore the reciprocal direction behaves differently:

    alive A: A->B continues decaying while B is dead
    dead  B: B->A receives no turns at all and remains frozen

`CvTeam::changeAliveCount(0)` ends wars but does not clear or rebuild
`m_aiWarWeariness`; `CvPlayer::setAlive(true)` / the normal revival path reuses the same
team object and likewise does not reset or age that array. The dead-owner value can thus
remain numerically unchanged for an arbitrarily long elimination interval.

This is gameplay-relevant even though peaceful revival does not immediately create city
anger: `CvPlayer::updateWarWearinessPercentAnger` sums stored weariness only against
CURRENT enemies. If revived B later fights surviving A again, B's old B->A value resumes
from the stale pre-elimination level, whereas A->B had been decaying for the whole dead
interval. The revived civilization can therefore re-enter the same rivalry carrying a
large obsolete asymmetric war-weariness burden.

F734 / provisional KI#1055 -- KI#415 repairs dead TARGET decay but leaves dead OWNER
war weariness frozen across elimination/revival
--------------------------------------------------------------------------
ROOT
----
The repaired iterator expands the per-owner target set, but the owner team's turn loop is
still alive-only by construction. Dead teams therefore cannot execute the decay function
for their own directional entries.

CONSEQUENCE
-----------
After B is eliminated, A->B ages correctly but B->A freezes. On a later B revival and
renewed A-B war, that obsolete directional value again contributes to B's live war-
weariness anger/AI state instead of reflecting the elapsed peace/death interval.

CONTROL / DEDUP
---------------
This is the same lifecycle family as F093/KI#415 but a distinct surviving dimension of
the attempted repair: KI#415's original report proved a dead-TARGET iterator regression;
F734 proves that target iteration alone cannot service a dead OWNER. No existing post-
primary F708-F733 root owns the reciprocal owner-lifecycle problem.

REPAIR DIRECTION
----------------
When a team is dead but retains persistent war-weariness state for possible revival,
age its own directional entries by elapsed turns as well (or equivalently normalize that
state on elimination/revival using the same decay semantics). Avoid simply clearing the
array unless loss of historical weariness on revival is explicitly desired, because the
existing model intentionally decays rather than instantly erases former-war weariness.

Classification: incomplete KI#415 repair / dead-team owner-lifecycle persistence bug.

PRACTICAL 6265 BUNDLE VERDICT
-----------------------------
KI#413 survives current-source proof and adversarial deal-order/master-bloc review.
KI#415's original dead-target root is improved, but its repair is incomplete on the
reciprocal dead-owner dimension; F734 / provisional KI#1055 is CONFIRMED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP790
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F734 remain confirmed.
F735 / provisional KI#1056: UNUSED.
NEXT: practical 6264 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP790
===============================================================================


===============================================================================
C031-WIP791 APPEND -- practical 6264 repair audit (KI#414, KI#418 Tech Share)
===============================================================================

SCOPE
-----
Chronology cursor practical 6264 / rewritten commit fd7ae43c621809d7ef820de98a5c1ea13483b7b1:
    fix inherited Tech Share defects in KI#414 and KI#418

PRACTICAL 6264 / KI#414 -- CLEAN
--------------------------------
The current repair removes Tech Share evaluation from the directional
`CvTeam::makeHasMet` half-transaction and performs:

    updateTechShare();
    kTeam.updateTechShare();

only after BOTH directional `makeHasMet` calls in `CvTeam::meet` have completed.
`OTHER_KNOWN_TO` can therefore no longer omit the newly encountered team merely because
its reverse has-met relation has not yet been written. The Internet's qualifying-holder
count is symmetric with respect to which team initiated contact.

The existing Python `firstContact` callback remains inside each directional `makeHasMet`
and therefore retains its historical timing; the earlier callback audit found no second
persistent Tech Share consumer/root there. Vassal/master propagation occurs later in
`meet` and recursively establishes each additional bilateral contact through the same
repaired transaction, so it does not reopen the initiator-order defect.

No double-grant issue survives either: `updateTechShare(TechTypes)` exits immediately once
the team already owns the technology, and setting a Tech Share technology on the first
post-contact scan makes the second scan for that same team harmless.

PRACTICAL 6264 / KI#418 -- CLEAN
--------------------------------
Kek-Mod intentionally changed Tech Share from qualifying-team cardinality to qualifying-
PLAYER cardinality. Current grant logic now adds:

    itOther->getAliveCount()

for each alive known team that owns the candidate technology, rather than assigned roster
size through `getNumMembers()`.

That is the correct population for the live feature: technology remains team-owned, so
one team-level `isHasTech` test is sufficient, while the amount contributed by that team
is exactly the number of living civilizations/players that presently constitute the
known holder population. Eliminated assigned teammates can no longer satisfy The
Internet threshold. This matches the feature's AI-side living-player interpretation and
does not disturb Permanent-Alliance/shared-tech ownership itself.

The iterator already restricts candidate teams to `CIV_ALIVE`, making `getAliveCount()`
strictly positive for each admitted team; no zero-member threshold or dead-team duplicate
edge survives.

PRACTICAL 6264 BUNDLE VERDICT
-----------------------------
KI#414 and KI#418 survive current-source proof, first-contact/vassal recursion pressure
testing, living-player cardinality review and whole-album dedup. F735 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP791
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F734 remain confirmed.
F735 / provisional KI#1056: UNUSED.
NEXT: practical 6263 / rewritten commit 403929fe273f0bce131fc6bfd2ea15d57f688e6c
      (KI#408-KI#411; KI#410 rejected in that practical)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP791
===============================================================================


===============================================================================
C031-WIP792 APPEND -- practical 6263 repair audit (KI#408-KI#411)
===============================================================================

SCOPE
-----
Chronology cursor practical 6263 / rewritten commit 403929fe273f0bce131fc6bfd2ea15d57f688e6c:
    fix inherited CvTeam defects in KI#408-KI#411; KI#410 rejected

PRACTICAL 6263 / KI#408 -- CLEAN
--------------------------------
`CvTeam::isInContactWithBarbarians` now floors the era-derived unit threshold at one:

    max(1, currentEra)

During the early unit-contact phase, an Ancient-era civilization must therefore have
positive local unit presence in an area before that area can qualify, eliminating the
old zero-threshold globalization where any Barbarian anywhere counted as contact. Later
era scaling is preserved, and the separate later city-contact phase is unchanged. No
new map-area or threshold boundary root survived.

PRACTICAL 6263 / KI#409 -- CLEAN
--------------------------------
Permanent-Alliance espionage merging now skips the two constituent team IDs when summing
the survivor's outgoing directional EP, then explicitly clears both obsolete constituent
slots. Outsider-directed EP is still summed, including stored state for nonliving team
slots. The reverse outsider->survivor merge in `addTeam` likewise sums the outsider's
former survivor and absorbed-team EP.

K-Mod's `getTotalUnspentEspionage` has also been hardened to iterate
`NOT_SAME_TEAM_AS`, so even inherited/external self-target residue cannot pollute the
usable-rival denominator. These two layers fix the original constituent-slot corruption
without discarding real outsider espionage. No surviving KI#409 repair-side root was
found.

PRACTICAL 6263 / KI#410 -- REJECTION STILL VALID
------------------------------------------------
The Advanced Start technology-removal dependency issue remains a dormant backend defect,
not a shipped reachable gameplay path. The Tech Chooser and AI purchase technologies but
do not expose/send the removal action on which KI#410 depends. The earlier rejection
therefore remains authoritative; no implementation review is required at this cursor.

PRACTICAL 6263 / KI#411 -- LIVE-OUTSIDER REPAIR WORKS, REVIVAL HOLE CONFIRMED
----------------------------------------------------------------------------
The current Permanent-Alliance repair explicitly unions K-Mod historical `m_abHasSeen`
in both observer directions before the absorbed team disappears, but it does so only for
`apOther`, which is populated with ALIVE outsider teams.

For live outsiders this is correct: survivor A inherits absorbed B's historical knowledge
of C, and C's historical knowledge of B is redirected to A. Current map ownership need
not reconstruct the bit.

The alive-only outsider domain is nevertheless too narrow for the persistent-history
contract. Team death does not erase `m_abHasSeen`, and normal elimination teardown even
calls `makeHasSeen(deadTeam)` for living teams so the dead civilization's identity is not
concealed. SAS/AdvCiv also supports later civilization/player revival.

A concrete surviving case is:

1. B has historically seen C (indeed, if C is eliminated while B is alive, elimination
   teardown explicitly ensures B.hasSeen(C)).
2. C is dead.
3. B is absorbed into surviving team A by Permanent Alliance while C remains dead.
4. KI#411's `apOther` loop skips C, so B.hasSeen(C) is not copied to A.hasSeen(C).
5. C later revives. A's identity-visibility checks now consult A.hasSeen(C), which can be
   false despite B's members having known C immediately before the alliance.

The reciprocal direction is also possible: dead C can retain historical C.hasSeen(B),
but because C is skipped during the merge, that knowledge remains keyed to obsolete team
B rather than the surviving team A.

F735 / provisional KI#1056 -- KI#411 Permanent-Alliance hasSeen migration skips dead
outsiders whose persistent identity history can later revive
--------------------------------------------------------------------------
ROOT
----
The repair correctly treats `m_abHasSeen` as persistent historical state but restricts
its migration to currently alive outsiders. Dead outsider slots retain exactly the kind
of history that can matter again after revival.

CONSEQUENCE
-----------
A revived civilization or the surviving Permanent-Alliance team can forget an identity
that one of the constituent teams historically knew, causing leader/civilization/color
concealment until fresh contact or revealed ownership reconstructs the bit.

CONTROL / DEDUP
---------------
This is an incomplete KI#411 repair, not the original omission itself: live outsiders are
now migrated correctly. It is distinct from F734/KI#1055's dead-owner war-weariness aging
and from KI#416's dead-member technology synchronization; all share revival as a lifecycle
control but act on different persistent state and transitions. No prior F708-F734 root
owns dead-outsider `hasSeen` migration through Permanent Alliance.

REPAIR DIRECTION
----------------
Migrate historical `hasSeen` across every relevant EVER_ALIVE outsider team, not only
currently alive outsiders, while retaining any alive-only restrictions required for
active attitude/war-plan side effects in neighboring code. The state transfer itself is
a pure persistent-bit union and does not require the outsider to be alive.

Classification: incomplete KI#411 repair / Permanent-Alliance persistent-history revival
hole.

PRACTICAL 6263 BUNDLE VERDICT
-----------------------------
KI#408 and KI#409 survive adversarial current-source review. KI#410 remains correctly
rejected as unreachable through shipped callers. KI#411 fixes live outsiders but leaves
a dead-outsider revival hole; F735 / provisional KI#1056 is CONFIRMED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP792
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F735 remain confirmed.
F736 / provisional KI#1057: UNUSED.
NEXT: practical 6262 / rewritten commit a19018fa021c1638c1dcf3e8a1ae7a46969fa587
      (docs-only clarification/rejections) -> chronology control, then practical 6261.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP792
===============================================================================


===============================================================================
C031-WIP793 APPEND -- practical 6262 chronology control + 6261 repair audit
===============================================================================

PRACTICAL 6262 -- DOCUMENTATION-ONLY CONTROL
--------------------------------------------
Rewritten commit a19018fa021c1638c1dcf3e8a1ae7a46969fa587 only clarifies KI#405 and records
KI#406-KI#407 rejection rationale. It introduces no C++ repair surface. The chronology
cursor therefore advances without manufacturing a code verdict; the existing
caller-based rejection evidence remains authoritative.

SCOPE -- PRACTICAL 6261
-----------------------
Rewritten commit 58563781c3401018e4d1aeab27df44d68acd83ee:
    fix inherited AdvCiv technology bookkeeping in KI#404-KI#405

PRACTICAL 6261 / KI#404 -- CLEAN
--------------------------------
On a genuine research completion crossing, current `setResearchProgress` computes player
overflow from the pre-clamp `progress - cost`, then normalizes the team's stored completed
technology progress through:

    m_aiResearchProgress.add(eTech, cost - currentProgress)

This sets the authoritative stored value to EXACTLY the technology cost regardless of the
size of the adjusted overflow. It therefore avoids AdvCiv's old arithmetic error where
an already-overflowing value had the overflow portion added again. The player receives
the intended unmodified overflow first; the bookkeeping clamp changes only the completed
technology's retained progress.

Repeat technologies compose correctly: after completion enters `setHasTech`, their
existing repeat-tech branch resets research progress to zero for the next iteration. No
residual-overflow or repeat-tech double-accounting root survived.

PRACTICAL 6261 / KI#405 -- CLEAN
--------------------------------
For non-repeat technologies, `setHasTech` returns immediately when the requested boolean
state already equals current ownership. Every execution reaching the cache update is
therefore a genuine state transition, and current source applies:

    m_iTechCount += (bNewValue ? 1 : -1)

The cached known-tech total consequently tracks acquisition and removal symmetrically.

The reachable True Starts regeneration path removes every currently known team technology
before `setInitialItems` reconstructs the new starting set. Each true->false transition
decrements the cache once; already-false technologies take the early return and cannot
drive the count negative. Reacquisition then increments normally. This restores the
cache invariant for gameplay consumers such as tech-progress comparisons and AI logic.

PRACTICAL 6261 BUNDLE VERDICT
-----------------------------
KI#404 and KI#405 survive current-source arithmetic/state-transition review and the
reachable True Starts removal/rebuild lifecycle. F736 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP793
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F735 remain confirmed.
F736 / provisional KI#1057: UNUSED.
NEXT: practical 6260 / rewritten commit 8d0fe661ecd772cd1b63f2b86050d7c747b2e497
      (KI#400-KI#403 Permanent-Alliance migration)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP793
===============================================================================


===============================================================================
C031-WIP794 APPEND -- practical 6260 repair audit (KI#400-KI#403 PA migration)
===============================================================================

SCOPE
-----
Chronology cursor practical 6260 / rewritten commit 8d0fe661ecd772cd1b63f2b86050d7c747b2e497:
    fix inherited Permanent Alliance state migration in KI#400-KI#403

PRACTICAL 6260 / KI#400 -- CLEAN UNDER CURRENT XML/STATE CONTRACT
---------------------------------------------------------------
Counterespionage is directional and currently has one live mission shape (+100 modifier
for a fixed duration). The repair migrates a coherent timer+modifier pair in both PA
directions and chooses the constituent effect with the longer remaining timer rather
than summing effects. Importantly, the underlying `shareCounters` loops range over all
team slots before their later alive-only active-AI branches, so stored effects involving
a temporarily dead outsider are not silently excluded from the state migration.

With the present single-modifier XML there is no equal-duration/different-modifier
ambiguity. No repair-side timer/modifier or dead-outsider root survived.

PRACTICAL 6260 / KI#401 -- LIVE-OUTSIDER TIMESTAMP REPAIR WORKS, DEAD-OUTSIDER CONTACT
MIGRATION STILL MISSING
-------------------------------------------------------------------------------------
For each ALIVE outsider, current `addTeam` snapshots the earliest nonnegative constituent
`getHasMetTurn` in both directions before calling `meet`, then restores those historical
dates after `meet` fills any missing contact with the alliance turn. That correctly fixes
KI#401 for live outsiders.

However, the entire contact/timestamp migration is inside `apOther`, and `apOther` is
constructed from outsiders satisfying `kOther.isAlive()`. Contact state is persistent
across elimination: team death ends wars/deals but does not erase `m_aiHasMetTurn`.
Revival likewise does not reconstruct all prior contacts from absorbed team identities.

A concrete surviving case is:

1. Absorbed team B and outsider C meet normally; B<->C therefore has real contact and
   historical first-contact turns.
2. C is eliminated. Its contact arrays persist while C is dead.
3. B forms a Permanent Alliance into survivor A while C remains dead.
4. Because C is absent from `apOther`, B<->C contact and timestamps are never redirected
   to A<->C before B becomes obsolete.
5. C later revives. A and C can now lack `isHasMet` entirely (and necessarily lack the
   inherited first-contact timestamp), despite B's members having known C before the PA.

This is stronger than merely resetting an Info Screen graph date: prior diplomatic
knowledge itself is stranded on the absorbed team ID and can affect contact-gated AI,
trade/diplomacy and Tech Share relationships after revival.

F736 / provisional KI#1057 -- KI#401 PA contact/timestamp migration skips dead outsiders
that can later revive
--------------------------------------------------------------------------
ROOT
----
The repaired contact migration inherits the alive-only `apOther` domain. Persistent
B<->C `hasMet` state and its historical turn are therefore not transferred when C is dead
at the moment B is absorbed.

CONSEQUENCE
-----------
After C revives, the surviving merged team A can behave as if it never met C at all,
until some new contact event reconstructs the relationship. This also loses the original
first-contact date that KI#401 specifically set out to preserve.

CONTROL / DEDUP
---------------
F735/KI#1056 is adjacent but distinct: it concerns the broader K-Mod `hasSeen` identity
bit, which may be learned without diplomatic first contact and chiefly controls identity
visibility. F736 concerns actual `hasMet`/first-contact state and therefore diplomacy,
trade/AI/contact semantics. Neither state implies the other strongly enough to merge the
roots.

REPAIR DIRECTION
----------------
Perform the pure persistent contact/timestamp migration for every relevant EVER_ALIVE
outsider, while keeping active-only attitude, war-plan, UI or deal side effects guarded
as necessary. Do not blindly call full `meet` on dead outsiders if that would trigger
active-contact side effects; the durable relation/timestamp fields can be unioned directly
for dead slots and normal `meet` retained for live ones.

Classification: incomplete KI#401 repair / Permanent-Alliance dead-outsider contact
revival hole.

PRACTICAL 6260 / KI#402 -- CLEAN
--------------------------------
A live Disengage deal follows its stored players when absorbed-team members are reassigned
to the survivor. For each live outsider in `apOther`, the repair preserves the bilateral
team enforcement flag whenever either absorbed-direction flag is live. This matches the
lifetime of the deal: player elimination kills deals, so a dead-outsider case cannot
retain a live Disengage transaction that needs migration. No repair-side root survived.

PRACTICAL 6260 / KI#403 -- CLEAN
--------------------------------
After project counts are merged, the repair chooses the minimum nonnegative victory
countdown across the two constituent teams and writes it to the survivor. Any live
countdown also forces `canLaunch=false`, undoing project-transfer recomputation that could
otherwise re-enable a second launch. This handles both an absorbed in-flight spaceship
and a survivor that was already in flight before receiving additional project counts.
No launch/countdown ordering edge survived.

PRACTICAL 6260 BUNDLE VERDICT
-----------------------------
KI#400, KI#402 and KI#403 survive current-source state-lifetime review. KI#401 correctly
repairs live outsiders but leaves persistent contact/timestamps stranded when the outsider
is dead during PA and later revives; F736 / provisional KI#1057 is CONFIRMED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP794
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F736 remain confirmed.
F737 / provisional KI#1058: UNUSED.
NEXT: practical 6259 / rewritten commit 95f1e653ee460fd89fab84d5d8d17b5329f0b9b7
      (KI#424-KI#426 UWAI team weighting/reach/trade-partner identity)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP794
===============================================================================


===============================================================================
C031-WIP795 APPEND -- practical 6259 repair audit (KI#424-KI#426 UWAI)
===============================================================================

SCOPE
-----
Chronology cursor practical 6259 / rewritten commit 95f1e653ee460fd89fab84d5d8d17b5329f0b9b7:
    fix UWAI KI#424-KI#426 team weighting, target reach and trade-partner identity

PRACTICAL 6259 / KI#424 -- CLEAN
--------------------------------
Each aspect object persists across the alive members of one agent team, but current
`WarUtilityAspect::evaluate` snapshots the prior cumulative utility, computes only the
current member's raw delta, applies the XML aspect weight to that delta once, and then
recombines it with the prior weighted total. Earlier members are therefore not reweighted
when later teammates are evaluated. The function also returns only the current member's
weighted delta, restoring per-player diagnostics without changing final aspect totals.

This composes with aspect-specific persistent helper state: legitimate team-wide
uniqueness state can affect which member produces a delta, while the wrapper still weights
whatever delta that member actually contributes exactly once. Separate lifetime mistakes
inside an aspect (e.g. the already-recorded GreedForSpace F729) remain independent roots.

PRACTICAL 6259 / KI#425 -- CLEAN
--------------------------------
The SAS faraway-war hard gate is now team-scoped. A one-shot `Risk` latch makes the gate
run once across all alive agent members; it finds the best qualifying land route or, when
naval transport is genuinely required, a route whose SAME teammate can also train cargo.
A remote teammate can no longer veto a reachable front and disconnected teammate caches
cannot manufacture a naval qualification.

The latch is initialized only in `Risk`'s constructor, which is sufficient: each
`WarEvaluator::evaluate` scenario call constructs a fresh aspect vector and deletes it at
the end. The recursive peace scenario therefore receives a fresh `Risk`; the latch only
persists across the member loop for the single scenario it is meant to deduplicate. No
cross-scenario stale-latch root survives.

PRACTICAL 6259 / KI#426 -- CLEAN
--------------------------------
`techRefuseThresh(PlayerTypes)` now resolves the queried player object and, for an AI,
reads THAT player's personality's technology-refusal threshold. The human special case
remains unchanged. Both directions of prospective partner technology trade therefore use
the willingness of the player whose willingness is being modeled, matching live trade
denial semantics. No team/leader identity leak survived.

PRACTICAL 6259 BUNDLE VERDICT
-----------------------------
KI#424, KI#425 and KI#426 survive current-source wrapper-lifetime, team-reach and
personality-identity pressure testing plus whole-album dedup. F737 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP795
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F736 remain confirmed.
F737 / provisional KI#1058: UNUSED.
NEXT: practical 6258 / rewritten commit 19325626df83dcb96953e57073094b69a92302f8
      (KI#412, KI#419, KI#422 reporting/state defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP795
===============================================================================


===============================================================================
C031-WIP796 APPEND -- practical 6258 repair audit (KI#412, KI#419, KI#422)
===============================================================================

SCOPE
-----
Chronology cursor practical 6258 / rewritten commit 19325626df83dcb96953e57073094b69a92302f8:
    fix inherited AdvCiv KI#412/KI#419 and BtS KI#422 reporting defects

PRACTICAL 6258 / KI#412 -- CLEAN
--------------------------------
The reparations announcement path now preserves the actual giving `PlayerTypes` from
`CvDeal` through `CvTeam::makePeace`, `announcePeace` and `tradeItemString`. `TRADE_CITIES`
therefore resolves its player-local city ID through the same giver that owns the deal-side
item, rather than through the giving team's leader.

A deal-side trade list belongs to one concrete giving player, so one retained player ID is
the correct identity for every city item in that reparations list. Both list orientations
in `CvDeal` pass their corresponding giver. Team-global item formatting continues to
derive the team from that player and is unchanged. No multi-member-team namespace leak
survived.

PRACTICAL 6258 / KI#419 -- CLEAN
--------------------------------
The Customizable Domestic Advisor's map-centering-aware X wrapper now returns
`city.getX()` when coordinates are revealed; the neighboring Y wrapper continues to
return `city.getY()`. Before map centering both intentionally return "?". This is the
complete correction for the inherited copy/paste reporting defect.

PRACTICAL 6258 / KI#422 -- CLEAN
--------------------------------
`CvTeam::changeProjectCount` now accepts an announcement-control flag. Permanent-Alliance
inheritance calls it with completion reporting disabled, then retains all real project
mechanics: team project count/art bookkeeping, interception/Tech Share/victory effects,
global unlocks and AI production invalidation still execute. Only the fresh completion
replay/message/sound block is suppressed for a project that was actually completed by
the absorbed team earlier.

The existing created-project-count compensation continues to cancel the temporary global
count increment caused by transferring an already-existing project. Suppressing reporting
does not suppress mechanical inheritance, so no false-event-vs-missing-effect tradeoff
survives.

PRACTICAL 6258 BUNDLE VERDICT
-----------------------------
KI#412, KI#419 and KI#422 survive current-source identity, UI and project-transfer review
plus whole-album dedup. F737 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP796
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F736 remain confirmed.
F737 / provisional KI#1058: UNUSED.
NEXT: practical 6257 (resolve exact commit/title from local rewrite map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP796
===============================================================================


===============================================================================
C031-WIP797 APPEND -- practical 6257 repair audit (KI#398 grouped Espionage)
===============================================================================

SCOPE
-----
Chronology cursor practical 6257 / rewritten commit a9988472a760b01915cf7076c848ff8605bbdb8a:
    fix inherited BtS KI#398 group Espionage; close CvTeam C014 audit album

PRACTICAL 6257 / KI#398 -- ORIGINAL MIXED NON-SPY ROOT IMPROVED, MISSION-SPECIFIC
ACTOR SELECTION STILL INCOMPLETE
--------------------------------------------------------------------------------
The repair correctly solves the documented Axeman+Spy case at the generic-action level:
`startMission` skips units that fail `canEspionage`, and popup/help paths centralize on
`CvSelectionGroup::getEspionageUnit()` instead of the arbitrary selection head.

However, `getEspionageUnit()` returns the first unit satisfying only:

    pUnit->canEspionage(pUnit->plot())

That predicate establishes generic Spy eligibility (right plot/owner, unmoved,
invisible, etc.); it does NOT establish eligibility or affordability for a particular
EspionageMissionTypes/target-data choice.

Mission cost is actor-specific. `CvPlayer::getEspionageMissionCostModifier` multiplies the
cost by the ACTING Spy's own `getFortifyTurns()` stationary discount. Consequently two
selected Spies on the same target plot can have different mission affordability even
though both pass generic `canEspionage`.

Concrete case:

1. Spy S1 is first in the selection group and has 0 stationary/fortify turns.
2. Spy S2 is later in the same selection group and has enough stationary turns to make a
   particular mission cheaper.
3. The team has enough EP for the mission with S2's discount but not enough for S1.
4. Both Spies pass generic `canEspionage`.

The first Espionage popup resolves S1 through `getEspionageUnit()` and calls
`canDoEspionageMission(..., pUnit=S1)`. The valid mission can therefore be omitted even
though selected S2 could execute it.

The executor remains incomplete too. For a specific `MISSION_ESPIONAGE`, `startMission`
reaches S1 first, checks only generic `canEspionage`, and calls `S1.espionage(...)`.
`CvUnit::espionage` performs the BEFORE_MISSION interception check before
`CvPlayer::doEspionageMission` finally calls mission-specific `canDoEspionageMission` and
can reject S1 for insufficient EP/other mission-specific reasons. Regardless of success,
`startMission` then takes the one-Spy-per-command `goto exit_unit_loop`; S2 is never tried.
Thus an ineligible first generic Spy can both block a later valid actor and potentially be
exposed to interception before the invalid mission is rejected.

F737 / provisional KI#1058 -- KI#398 resolves a generic Spy, not a Spy eligible for the
selected Espionage mission/target
--------------------------------------------------------------------------
ROOT
----
The repaired actor helper has no mission/target parameters and therefore cannot match the
mission-specific eligibility population used by `canDoEspionageMission`, whose cost and
validity depend on the concrete acting Spy.

CONSEQUENCE
-----------
Grouped Spies with different stationary discounts can hide executable missions in the
popup and can cause a chosen mission to abort on the first generic Spy instead of being
performed by a later eligible selected Spy. The first invalid actor can also undergo the
pre-mission interception check before mission-specific rejection.

CONTROL / DEDUP
---------------
This is an incomplete KI#398 repair, not a new generic popup-head bug: the non-Spy head
problem is fixed, but eligibility remains too weak within the population of actual Spies.
F055/KI#376 concerns espionage diagnostic aggregation of stationary Spies, not selection
of the executable mission actor. No F708-F736 root owns this mission-specific group actor
mismatch.

REPAIR DIRECTION
----------------
For popup/help/execution of a concrete mission, select an actual unit that passes
`canDoEspionageMission(eMission, target, plot, extraData, pSpy)` for that exact mission
(and, for phase-one mission listing, aggregate availability across selected eligible
Spies rather than binding all options to the first generic Spy). Preserve one successful
Spy action per command, but do not exit merely because an earlier generic Spy cannot
perform the chosen mission. Ideally validate mission-specific eligibility BEFORE any
interception roll.

Classification: incomplete KI#398 repair / mission-specific selected-actor identity and
eligibility bug.

PRACTICAL 6257 BUNDLE VERDICT
-----------------------------
KI#398 fixes the original arbitrary non-Spy-head/mixed-group defect but remains incomplete
for multiple selected Spies whose concrete mission eligibility differs. F737 /
provisional KI#1058 is CONFIRMED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP797
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F737 remain confirmed.
F738 / provisional KI#1059: UNUSED.
NEXT: practical 6256 / rewritten commit e95b703b659bce223a25312e872d530611246877
      (KI#393, KI#394 mission eligibility)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP797
===============================================================================


===============================================================================
C031-WIP798 APPEND -- practical 6256 + 6255 repair audit
===============================================================================

SCOPE -- PRACTICAL 6256
-----------------------
Rewritten commit e95b703b659bce223a25312e872d530611246877:
    fix inherited BtS/AdvCiv KI#393 and AdvCiv KI#394 mission eligibility

PRACTICAL 6256 / KI#393 -- CLEAN
--------------------------------
The repaired mission-help paths now derive specialized previews from units that can
actually execute the relevant mission at the current target. Ground Bombard excludes
spent/non-bombarding selected units; Air Bomb city previews require current target-valid
aircraft; target-structure help uses the executor's selected target-valid aircraft;
Paradrop/Lead/Discover and related mixed-selection help likewise retain or query the
eligible actor rather than falling back to the arbitrary selection head.

The repaired populations match the corresponding current executor/mission selectors for
the reviewed families. No remaining preview-only inclusion of a target-ineligible unit
survived this pass. F738 remains unused.

PRACTICAL 6256 / KI#394 -- CLEAN
--------------------------------
`CvSelectionGroupAI::AI_bestUnitForMission(MISSION_AIRBOMB, target)` now rejects every
candidate that fails `canAirBombAt(target)` before ranking city-defense damage or
improvement/route bombard strength. Group execution can therefore no longer select an
out-of-range or otherwise target-invalid aircraft, fail its `airBomb`, and abort before a
later valid aircraft is tried. The same target-valid selector is used by repaired help.

PRACTICAL 6255 / KI#399 -- CLEAN
--------------------------------
Rewritten commit b2b49c6674d9799de96672319dee7946c1089612 moves the Great-General XP remainder
index increment into the exact eligibility branch used to award experience. The division
denominator (`canGiveExperience`) and remainder-index population are therefore identical:
only same-owner, non-General units that can acquire a promotion consume a remainder slot.
Exactly `iRemainder` eligible recipients receive the extra +1 XP, preserving the full
`getStackExperienceToGive` pool without changing recipient order or scaling.

BUNDLE VERDICT
--------------
KI#393, KI#394 and KI#399 survive current-source eligibility/population review and
whole-album dedup. F738 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP798
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F737 remain confirmed.
F738 / provisional KI#1059: UNUSED.
NEXT: practical 6254 / rewritten commit 3857fe21439ff7733fb2a36ebbf62432bae22340
      (KI#395-KI#397 widget contracts; documents KI#393-KI#399)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP798
===============================================================================


===============================================================================
C031-WIP799 APPEND -- practical 6254 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 3857fe21439ff7733fb2a36ebbf62432bae22340:
    fix inherited BUG/AdvCiv KI#395-KI#397 widget contracts; document C013 KI#393-KI#399

PRACTICAL 6254 / KI#395 -- CLEAN
--------------------------------
The optional city-screen angry-citizen Chevron overlay now uses `WIDGET_ANGRY_CITIZEN`,
matching the underlying angry-citizen button instead of `WIDGET_CITIZEN`. It therefore
carries no bogus SpecialistTypes payload and inherits the same angry-citizen help/action
contract as the control underneath it. Current producers were checked; there is no
remaining specialist-Pedia path attached to this overlay.

PRACTICAL 6254 / KI#396 -- CLEAN
--------------------------------
The native Bonus/Tech trade-denial widgets now restore the Pedia action and advertise it
through `isLink`, while retaining their richer native hover parsers. Bonus payloads were
checked across all current producers. Ordinary Bonus cells pass the raw BonusTypes ID;
the Foreign Advisor Will Import column is the sole reviewed producer that signals import
semantics by adding +1000, and `executeAction` normalizes exactly that encoding before
Pedia navigation. Later city-screen reuse passes `data2=-1` with an ordinary Bonus ID and
is not mis-decoded. Tech denial widgets carry the raw TechTypes ID throughout.

PRACTICAL 6254 / KI#397 -- CLEAN
--------------------------------
`WIDGET_LEADERHEAD_RELATIONS` now restores the old leaderhead right-click contact action;
all current Foreign/Military Advisor producers keep the contacted rival in `data1`, which
is the contract consumed by `doContactCiv`. The scoreboard-specific trade-routes widget
is likewise restored to ordinary click-to-contact by remapping its rival from `data2` to
the contact widget's `data1` contract. The ordinary non-scoreboard `WIDGET_TRADE_ROUTES`
remains hover-only, preserving the intended Info/Foreign Advisor separation.

BUNDLE VERDICT
--------------
KI#395, KI#396 and KI#397 survive current-source producer/consumer and payload-contract
review. No repair-side or inherited sibling root was found. F738 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP799
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F737 remain confirmed.
F738 / provisional KI#1059: UNUSED.
NEXT: practical 6253 (resolve rewritten commit from local commit map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP799
===============================================================================


===============================================================================
C031-WIP800 APPEND -- practical 6253 repair audit
===============================================================================

SCOPE
-----
Rewritten commit a39bfdc68b5a84472dad4a7063a45cbb4829e69b:
    fix AdvCiv-SAS KI#129 and KI#388-KI#390 advisor/widget issues; close C013 audit album

PRACTICAL 6253 / KI#129 -- CLEAN
--------------------------------
The Military Advisor now applies `SAS_CV_MILITARY_ADVISOR_DEFAULT_TAB` only on the
advisor object's first opening and then retains the user's selected page across ordinary
close/reopen. `initDefines` validates the XML value against the fixed `PAGE_IDS` set
before assignment, page IDs themselves are stable, and full screen reopen still resets
only screen-lifecycle state such as minimap initialization. No stale/invalid page path
survived the current debug/perspective and screen-reconstruction review.

PRACTICAL 6253 / KI#388 -- ORIGINAL FIX CLEAN; NEW SIBLING F738 CONFIRMED
------------------------------------------------------------------------
The KI#388 changes do solve their documented problem: Score-tab hovers no longer import
scoreboard expansion/timer state. Score breakdown uses non-expansion data2=-1, trade
network uses ordinary `WIDGET_TRADE_ROUTES`, and the new
`WIDGET_POWER_RATIO_INFO_SCREEN` shares power help without belonging to the scoreboard
expansion set.

However, current Info Screen supports a selected advisor perspective that can differ from
the real active human (`self.iActivePlayer` is preserved/debug-selectable and can use an
allowed vassal perspective). The Score table computes visible power ratios from that
selected perspective:

    iActivePower = perspectivePlayer.getPower()
    ratio = perspectivePower / targetPower   (or inverse option)

The new C++ hover widget passes only the target player in data1 and the display color in
data2, then delegates to inherited `parsePowerRatioHelp`. That helper hardcodes:

    CvPlayer const& kActivePlayer = GET_PLAYER(GC.getGame().getActivePlayer());

Thus when the advisor perspective is B while the actual active human is A, a row for C
can visibly show B-vs-C while its hover text/percentage and espionage-intel explanation
are calculated as A-vs-C. The KI#388 widget separation exposes no payload through which
the parser can recover B.

F738 / provisional KI#1059 -- Info Screen power-ratio hover ignores selected advisor
perspective
-------------------------------------------------------------------------------------
ROOT
----
`WIDGET_POWER_RATIO_INFO_SCREEN` reuses a scoreboard parser whose implicit "you" is the
real active player, but the Info Screen Score table's "you" is a Python-selected advisor
perspective. The widget contract carries target + color, not target + perspective.

CONSEQUENCE
-----------
In debug-selected or supported alternate/vassal perspective, the visible PowR cell and
its hover can describe different player pairs. The hover's espionage thresholds are also
looked up from the real active player rather than the selected perspective.

CONTROL / DEDUP
---------------
The ordinary scoreboard widget is correctly tied to the real active player, so this is
not an inherited scoreboard defect. KI#388 itself concerns expansion-state side effects
and is genuinely fixed. No F708-F737 root owns advisor-perspective identity for power
help. Existing earlier C013 closure called the new widget structurally coherent but did
not adversarially compare its implicit active-player source with the Python screen's
explicit perspective source.

REPAIR DIRECTION
----------------
Give the Info-Screen-specific hover an explicit perspective-player payload and calculate
both ratio and espionage thresholds from it. If data2 is repurposed for the perspective,
the optional hover percentage color can be omitted or represented through a separate
contract; avoid silently falling back to `GC.getGame().getActivePlayer()` for this
advisor-specific widget.

Classification: AdvCiv-SAS Score-tab caller/widget-contract perspective bug, exposed
while reviewing KI#388.

PRACTICAL 6253 / KI#389 -- CLEAN
--------------------------------
The Specialist Extra Slots representative special-building icon now uses a neutral
widget because it has neither a concrete building nor a real technology context. This
removes the NO_TECH/TechPrereqAnyone false match without inventing another source object.
Concrete source rows retain their own specific help. No sibling contract leak found.

PRACTICAL 6253 / KI#390 -- CLEAN
--------------------------------
The dedicated Military Advisor combined Unit Cost + Supply hover validates data1 as a
PlayerTypes payload and appends exactly the existing post-inflation Unit Cost and Away
Supply breakdown builders. Current Summary callers pass the selected advisor perspective
player with data2>0, including debug-selected AI perspectives. It no longer imports City
Maintenance, Civic Upkeep or unrelated whole-expense terms.

BUNDLE VERDICT
--------------
KI#129, KI#389 and KI#390 survive current-source review. KI#388's expansion-state repair
survives, but the Score-tab power-hover reuse exposes independent F738 / provisional
KI#1059. F739 / provisional KI#1060 is UNUSED.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP800
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6252 / rewritten commit 7aaf0b6aa9fc0a5d7e7ff5a4d21490e6ce94b1df
      (KI#391-KI#392 repair bundle; documents KI#388-KI#390)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP800
===============================================================================


===============================================================================
C031-WIP801 APPEND -- practical 6252 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 7aaf0b6aa9fc0a5d7e7ff5a4d21490e6ce94b1df:
    Fix inherited BUG/K-Mod KI#391 and AdvCiv-SAS KI#392 from the C++ audit album;
    document KI#388-KI#390

PRACTICAL 6252 / KI#391 -- CLEAN
--------------------------------
Both current BUG Tech Splash implementations reviewed here now pass
`WIDGET_HELP_IMPROVEMENT` as `(TechTypes=self.iTech, BuildTypes=j)`, exactly matching
`CvDLLWidgetData::parseBuildHelp` -> `buildImprovementString`. This restores the correct
unlock text for route/feature-removal and other Build actions whose BuildInfo has no
ImprovementTypes result. The native alt action intentionally jumps only when the Build
has a concrete Improvement; non-improvement actions therefore remain hover-only on
right click, which is the correct contract rather than a remaining navigation loss.

PRACTICAL 6252 / KI#392 -- CLEAN
--------------------------------
The Extended/Detailed Tech Splash enabled-unit branch now references the existing
`WidgetTypes.WIDGET_PEDIA_JUMP_TO_UNIT` symbol. Current branch structure supplies a real
civilization unit ID whenever it emits the button. No sibling malformed WidgetTypes
reference survived the reviewed enabled-unit path; Wide already used the valid symbol.

BUNDLE VERDICT
--------------
KI#391 and KI#392 survive current-source caller/consumer review and whole-album dedup.
F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP801
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6251 / rewritten commit 8eae1ddf6c0d2f6346d0184bec5e6a009edb4fb0
      (KI#383-KI#384 and KI#386; KI#387 rejection)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP801
===============================================================================


===============================================================================
C031-WIP802 APPEND -- practical 6251 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 8eae1ddf6c0d2f6346d0184bec5e6a009edb4fb0:
    Fix KI#383-KI#384 and KI#386 widget regressions, reject KI#387, and reconcile
    C++ audit album WIP14

PRACTICAL 6251 / KI#383 -- CLEAN
--------------------------------
`WIDGET_LH_GLANCE` again appends AdvCiv's independent `parseWarTradesHelp` before the
SAS vassal-willingness explanation. The same `(rival=data1, viewer=data2)` payload feeds
both helpers, so the repair restores the missing fist explanation without replacing or
cross-wiring the newer vassal status. No duplicate status/action sibling survived.

PRACTICAL 6251 / KI#384 -- CLEAN
--------------------------------
`executeAltAction` no longer constructs an active-civilization reference before its
widget switch. Only `WIDGET_TRAIN` and `WIDGET_CONSTRUCT`, whose class IDs genuinely need
civilization-specific mapping, resolve `getActiveCivilization()` locally and explicitly
return unhandled when it is absent. Direct opening-menu Pedia actions therefore avoid
that unrelated game-state dependency. The remaining pre-switch locals are scalar widget
payloads/Python caller access and do not dereference an absent active civilization.

PRACTICAL 6251 / KI#386 -- CLEAN
--------------------------------
Infiltrate help now searches the selected units with the mission's own
`canInfiltrate(plot, true)` predicate and stops at the first eligible actor before showing
that actor's `getEspionagePoints` yield. This restores Great Spy help while preserving the
one-eligible-actor preview pattern used by Trade/Hurry/Great Work and the one-unit
Infiltrate execution behavior. No multi-Great-Spy aggregation mismatch was introduced.

PRACTICAL 6251 / KI#387 -- REJECTION STILL VALID
------------------------------------------------
The active `GET_TEAM(PlayerTypes)` overload still resolves through the player's actual
team (`TEAMID(ePlayer)`). No new evidence reopens the retracted enum-type false positive.

BUNDLE VERDICT
--------------
KI#383, KI#384 and KI#386 survive current-source review; KI#387 remains correctly
rejected. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP802
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6250 / rewritten commit f0ee784b6444e9a0ebedb67722fa4b89f9e29314
      (SASGameRecord KI#377-KI#382)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP802
===============================================================================


===============================================================================
C031-WIP803 APPEND -- practical 6250 repair audit
===============================================================================

SCOPE
-----
Rewritten commit f0ee784b6444e9a0ebedb67722fa4b89f9e29314:
    Fix AdvCiv-SAS SASGameRecord defects from C++ audit C012 (KI#377-KI#382)
Current-tree review includes later SASGameRecord lifecycle composition, notably KI#382.2.

PRACTICAL 6250 / KI#377 -- CLEAN
--------------------------------
Both `CvUnit::updateCombat` outcome branches pass the already-known combat target `pPlot`
through `CvEventReporter::combatResult` to GameRecord. It is the sole current internal
combatResult call contract. Exact battle coordinates, city-plot counters, pending-combat
matching, dependent death/context rows and Settler-combat context therefore use the
attacked tile rather than reconstructing it from the surviving/dead unit position.

PRACTICAL 6250 / KI#378 -- CLEAN
--------------------------------
The battle bucket stores an explicit inclusive start turn. Recorder state initializes it
from the actual current game turn for each new/load session; every real bucket flush
resets all matching counters and advances the start to `flushTurn + 1`. This remains
coherent for turn-0 combat, arbitrary load turns and extra victory/mid-interval flushes.

PRACTICAL 6250 / KI#379 -- CLEAN
--------------------------------
Golden Age/anarchy accumulators and emitted fields consistently describe *logged/session*
turns rather than civilization-lifetime totals. The counters reset with recorder state
and are updated at each enabled player-turn boundary before the game's own duration
counter decrements, matching the documented observation semantics.

PRACTICAL 6250 / KI#380 -- CLEAN
--------------------------------
Shared city-production helpers convert only CvCity's unavailable `MAX_INT` cost/turn
sentinel to recorder sentinel `-1`; finite production values remain unchanged. Current
ordinary/barbarian/snapshot call sites route the affected production-needed/turn fields
through those sanitizers.

PRACTICAL 6250 / KI#381 -- CLEAN
--------------------------------
Production-to-commerce diagnostic output now exits for `NO_PROCESS` or city disorder,
matching the engine's `CvCity::updateCommerce` suppression while leaving the selected
process represented by neighboring production fields. No diagnostic-only conversion is
reported while the actual city receives zero from that process.

PRACTICAL 6250 / KI#382 -- CLEAN AFTER LATER COMPOSITION
--------------------------------------------------------
Current non-constructor `CvGame::reset` finalizes the old recorder before teardown; the
map-read hook remains duplicate-safe insurance for load orders that replace the map
independently. Finalization flushes pending initialization actions, same-turn plot/map
revelation or city-bombard buffers, then records the authoritative old-session RNG
checkpoint before disabling tracking. The later KI#382.2 repair also makes level-3 plot
fingerprinting tolerate a stale city identifier during rollover rather than dereferencing
an already-destroyed city. No additional pending buffer reviewed here depends on newer
session state after this boundary.

BUNDLE VERDICT
--------------
KI#377-KI#382 survive current-source review including later SASGameRecord lifecycle
composition. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP803
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6249 (resolve from local commit map)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP803
===============================================================================


===============================================================================
C031-WIP804 APPEND -- practical 6249 repair audit
===============================================================================

SCOPE
-----
Rewritten commit f2d72e7f90a8640dce6600f1afe0c85b62cb3887:
    Fix AdvCiv-SAS SASGameRecord/BBAI diagnostic defects from the C++ audit album
    (KI#374-KI#376), and reconcile ChatGPT-5.6-Sol's durable C012 album
Current-tree review of both duplicated nearby-enemy helpers, the central GameRecord
varargs formatter and the espionage-preparation snapshot predicate.

PRACTICAL 6249 / KI#374 -- CLEAN
--------------------------------
Both affected helpers enumerate a concrete nearby `pLoopPlot` and now pass that plot to
`CvUnit::isEnemy`. This matches the callee contract exactly: `isEnemy(eTeam, kPlot)`
asks `getCombatOwner(eTeam, kPlot)` before the at-war test, so plot-sensitive combat
ownership/always-hostile behavior must be evaluated at the unit's actual plot rather
than at the search center. The independent invisibility gate is team-scoped and requires
no alternate plot argument. The duplicate BBAI Settler diagnostic and SASGameRecord
helper are now consistent.

PRACTICAL 6249 / KI#375 -- CLEAN
--------------------------------
The replacement `CvString::formatv(std::string&, ..., va_list)` removes the former
2047-character GameRecord ceiling and grows in 2048-byte increments up to its established
bounded retry limit. The apparent reuse of `va_list` across `_vsnprintf` retries was
adversarially checked against Civ4's supported Microsoft Visual C++ 7.1/x86 ABI: there
`va_list` is a pointer passed by value, so `_vsnprintf` advances its own pointer copy and
the formatter's argument position remains reusable on the next retry. This is also the
existing formatter used by BBAI/UWAI logging. Success produces a NUL-terminated complete
row; bounded formatter failure clears the output and the GameRecord caller asserts and
omits the row rather than emitting partial structured data. No second fixed-size buffer
was found on this central path.

PRACTICAL 6249 / KI#376 -- CLEAN
--------------------------------
The stationary-preparation fields now require `isSpy()`, positive fortify turns and
`canEspionage(&kPlot, true)`. `CvUnit::canEspionage` with `bTestVisible=true` still
checks the structural mission context -- ordinary Spy, espionage enabled, owned
non-Barbarian foreign plot and valid team/vassal relation -- while deliberately skipping
transient made-attack/moved/invisibility button-state checks. That matches the diagnostic
meaning of accumulated stationary preparation rather than immediate button executability.
Great Spies are excluded because `isSpy()` is false for them and remain represented by
the separate Great-Spy diagnostic fields. No interaction with F737's mission-specific
actor-affordability defect changes this aggregate preparation contract.

BUNDLE VERDICT
--------------
KI#374-KI#376 survive current-source adversarial review. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP804
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6248 / rewritten commit c366e5ea9df08e02de974f74df8558482187d0ce
      (KI#333, KI#336: emergency-production priority and map-heaviness lifecycle)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP804
===============================================================================


===============================================================================
C031-WIP805 APPEND -- practical 6248 repair audit
===============================================================================

SCOPE
-----
Rewritten commit c366e5ea9df08e02de974f74df8558482187d0ce:
    Fix AdvCiv-SAS emergency-production priority and map-heaviness lifecycle bugs
    (KI#333, KI#336)
Current-tree review of emergency-building ordering and every map-heaviness recomputation /
serialization boundary.

PRACTICAL 6248 / KI#333 -- CLEAN
--------------------------------
The Port-class rule now runs after the pre-existing strategic Walls/Castle branch while
retaining the shared `bEmergencyBuilding` latch. If strategic defense qualifies and a
building is legal, `SASTryEmergencyBuilding` pushes it and Port is skipped. If defense is
ineligible, already satisfied, obsolete or otherwise unconstructible, the latch remains
false and Port receives the same next-fallback opportunity as before. Harbor remains the
separately documented higher-priority economic emergency. The move therefore repairs the
ordering contract without creating an empty-production path or suppressing the only
buildable fallback.

PRACTICAL 6248 / KI#336 -- CLEAN
--------------------------------
`recomputeMapnameHeaviness` first resolves explicit script lists and otherwise classifies
the final major-civilization starting areas. New-game/regeneration setup performs an
early pass to clear stale state, then `setInitialItems` assigns and normalizes starts and
calls the shared `initFreeUnits`; that function recomputes immediately after free-unit
initialization. No current gameplay consumer of the cached land/naval flags executes
inside `CvPlayer::initFreeUnits`, so the provisional early neutral value for unspecified
scripts is replaced before ordinary city/unit AI decisions. Scenarios reach the same
shared post-start hook through the EXE's separate `initFreeUnits` entry. Save loads read
the two booleans from serialized `CvGame` state rather than requiring missing start-time
reclassification. Manual/automatic regeneration replaces the starts and subsequently
reaches the same post-start recomputation. No stale-map lifecycle path was found.

BUNDLE VERDICT
--------------
KI#333 and KI#336 survive current-source adversarial review. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP805
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6247 / rewritten commit 075f5cd9f4378f254d9a151191d5431ca618d05f
      (KI#368, KI#371: war-plan and WarSuccess coalition defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP805
===============================================================================


===============================================================================
C031-WIP806 APPEND -- practical 6247 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 075f5cd9f4378f254d9a151191d5431ca618d05f:
    Fix inherited AdvCiv war-plan and K-Mod WarSuccess coalition defects
    (KI#368, KI#371)
Current-tree review of master/vassal strategic-plan identity and coalition WarSuccess /
normalization populations.

PRACTICAL 6247 / KI#368 -- CLEAN
--------------------------------
Legacy war selection still values the concrete candidate team (including a voluntary
vassal as a tactical front), but now stores undeclared total/limited/dogpile strategic
preparation on that candidate's master. `AI_getWarPlanForPreparation` preserves a real
raw actual-team plan once present/at war and, only for undeclared preparation, falls back
to the target master's chosen-war plan. Area strategy, path, power/city/population,
air-power and technology-monopoly consumers use this contract, so a master-keyed plan
can see each vassal's actual assets without manufacturing duplicate raw plans. No current
producer/consumer was found that requires the old vassal key after selection.

PRACTICAL 6247 / KI#371 -- CLEAN
--------------------------------
WarSuccess remains stored by actual directional team pair. The new coalition helper sums
each distinct pairing between this team plus its vassals and the target master plus its
vassals exactly once; these are separate WarSuccess slots, not repeated reads of one
battle record. Coalition power remains one top-level `getPower(true)` term, and the
normalization unit count now spans the same evaluating master-plus-vassal player
population. The Force-Peace path likewise aggregates both success directions and its
peace-team unit scale over the same coalition. Resolution eligibility explicitly rejects
a force-peace target player that `isAVassal()`, so that caller supplies a free/master
peace team rather than invoking master-plus-vassal semantics on a raw vassal. No
coalition double count or omitted live pairing survives.

BUNDLE VERDICT
--------------
KI#368 and KI#371 survive current-source adversarial review. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP806
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6246 / rewritten commit 3c858051f0c56dc752543e3e66910814e4b59726
      (KI#372-KI#373: joint-war brokerage defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP806
===============================================================================


===============================================================================
C031-WIP807 APPEND -- practical 6246 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 3c858051f0c56dc752543e3e66910814e4b59726:
    Fix inherited AdvCiv and BtS/AdvCiv joint-war brokerage defects (KI#372-KI#373)
Current-tree review includes later KI#612 brokered-war coalition validation.

PRACTICAL 6246 / KI#372 -- CLEAN
--------------------------------
The supported non-UWAI/legacy target-selection branch now computes
`AI_declareWarTradeVal` at the same point where a candidate replaces `eBestTarget`, so
`iBestTeamPrice` is synchronized with the selected target before the shared compensation
balancer consumes it. Selection first passes `canTradeItem(..., true)`, so the cached
price belongs to a structurally legal/non-denied proposal rather than to a rejected
candidate. Legacy/background trade legality continues to reject all vassal targets; the
repair therefore does not expose that raw-target price function to the voluntary-vassal
master-coalition case handled only by full UWAI.

PRACTICAL 6246 / KI#373 -- CLEAN AFTER LATER COMPOSITION
--------------------------------------------------------
`TRADE_WAR` now admits a vassal target only when the declaring/hireling player has full
UWAI enabled and the target is not capitulated. That matches the UWAI branch that prices
and denies the target through the master-coalition-aware contract; legacy, disabled and
background-UWAI paths retain the inherited universal vassal exclusion. The later KI#612
repair additionally canonicalizes sponsor and target to their master teams and rejects
same-coalition or master-level Defensive Pact cascades. Foreign voluntary vassals remain
legal, while direct master/sibling/self-coalition and capitulated cases remain blocked.
The core war-declaration legality/met-team checks still run afterward. No policy widening
beyond the intended full-UWAI voluntary-vassal feature survives.

BUNDLE VERDICT
--------------
KI#372 and KI#373 survive current-source adversarial review including later brokered-war
hardening. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP807
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6245 / rewritten commit be1e58b394bd529fec88d32f6c878e821cbb7f05
      (KI#366, KI#369-KI#370: team relationship-state defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP807
===============================================================================


===============================================================================
C031-WIP808 APPEND -- practical 6245 repair audit
===============================================================================

SCOPE
-----
Rewritten commit be1e58b394bd529fec88d32f6c878e821cbb7f05:
    Fix inherited team relationship-state defects (KI#366, KI#369-KI#370) and
    close CvTeamAI audit album C011
Current-tree review includes later Permanent-Alliance migration repairs.

PRACTICAL 6245 / KI#366 -- CLEAN
--------------------------------
`AI_enmityValue` now rejects an enemy whose canonical master is the evaluating team's
master, matching the `KNOWN_POTENTIAL_ENEMY_OF` candidate iterator used by worst-enemy
replacement. This prevents an incumbent master, voluntary vassal or sibling vassal from
being grandfathered after entering the same political locus. The only live caller outside
`AI_updateWorstEnemy` uses the helper specifically to validate that the cached worst enemy
is still a meaningful enemy before applying rival-trade resentment; zeroing same-locus
teams is correct there too. No broader attitude/value caller is harmed by the centralized
eligibility rule.

PRACTICAL 6245 / KI#369 -- CLEAN
--------------------------------
`shareCounters` enumerates raw `MAX_TEAMS`, not only living outsiders, and now takes the
stronger absorbed-team `SharedWarSuccess` history for every surviving target identity.
Thus the absorbed->outsider direction is retained even for a currently dead outsider and
can survive later revival. This complements the existing outsider->merged weighted block
without duplicating one direction. The max/stronger-history policy matches neighboring
own-side relationship histories and does not sum two distinct teams into manufactured
extra shared-war contribution.

PRACTICAL 6245 / KI#370 -- CLEAN
--------------------------------
The outsider->merged relationship block also iterates all team slots and now folds
`OpenBordersCounter` with the same original-team/absorbed-team member-size weighting used
for neighboring reverse-direction histories. Consequently the later F735/F736 alive-only
identity/contact migration hole does not apply here: a dead outsider's stored reverse
Open-Borders history is still migrated. `shareCounters` separately preserves the
absorbed->outsider direction, so both directional histories survive with their intended
policies.

BUNDLE VERDICT
--------------
KI#366 and KI#369-KI#370 survive current-source adversarial review. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP808
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6244 / rewritten commit acf6f3baea1fb700645fcb5133e130b4e61e2316
      (KI#358 completion: AI_preDeclareWar memory population)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP808
===============================================================================

===============================================================================
C031-WIP809 APPEND -- practical 6244 repair audit
===============================================================================

SCOPE
-----
Rewritten commit acf6f3baea1fb700645fcb5133e130b4e61e2316:
    Complete inherited AdvCiv AI_preDeclareWar memory-population fix (KI#358)
Current-tree review of the restored all-major-player population, declaration cascades and
per-player memory multiplicity.

PRACTICAL 6244 / KI#358 -- CLEAN
--------------------------------
The completion restores every living major player to the common pre-declaration loop while
leaving each memory operation's own predicate intact. The resulting MemberAIIter x player
matrix is not an accidental team-size multiplier: original BtS/K-Mod declaration handling
explicitly recorded direct-victim and declared-war-on-friend memory separately for each
living declaring player against each eligible victim/observer player. AdvCiv's
`AI_rememberEvent` keeps that player-pair storage model. Thus a two-member declaring team
legitimately creates memories about both declaring players rather than one team-level
memory duplicated by the repair.

The all-demand reset likewise needs the broad population promised by advc.130o; the added
`kOurMember != kPlayer` guard replaces the self-exclusion that the removed potential-enemy
iterator used to provide. Secondary Defensive-Pact/vassal-alignment declarations can still
reach previously unmet direct victims before `meet()`, and those target members are now
processed as intended. `AI_disapprovesOfDoW` retains its own victim-contact, war-state and
master-locus rules after the broad outer population is restored.

UNNUMBERED CONTROL
------------------
The older AdvCiv war-despite-tribute branch predicates on team-averaged
`CvTeamAI::AI_getMemoryCount(..., MEMORY_MADE_DEMAND)` and then applies reputation memory
about each human declaring member. Multi-member teams therefore have some non-obvious
averaging/attribution behavior. This predates practical 6244 and no sufficiently firm
independent semantic invariant was established here, so it is retained only as a future
control, not promoted as F739.

BUNDLE VERDICT
--------------
KI#358's completion survives current-source adversarial review. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP809
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6243 / rewritten commit 6512cf0986df99c960b758abcfa3454f085653b5
      (KI#361-KI#363: team-state defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP809
===============================================================================

===============================================================================
C031-WIP810 APPEND -- practical 6243 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 6512cf0986df99c960b758abcfa3454f085653b5:
    Fix inherited K-Mod/BBAI and AdvCiv team-state defects (KI#361-KI#363)
Current-tree adversarial review including vassal, Defensive-Pact and relationship-counter
composition.

PRACTICAL 6243 / KI#361 -- CLEAN
--------------------------------
`AI_getEnemyPowerPercent` now stores one relevance fraction per actual defending team and
keeps the strongest applicable fraction when current/planned coalitions overlap. Current
war enemies are already enumerated as separate enemy teams, so master/vassal members that
are actually at war are represented once each. Planned non-vassal targets are expanded to
the target master locus plus every Defensive-Pact master locus (including those masters'
vassals through `getMasterTeam`), reproducing the population of the old defensive-power
term without repeating a shared ally for every chosen target. The target-level proximity
and other-war divisor remains intentionally common to that target's planned coalition,
matching the quantity that previously scaled `getDefensivePower` as a whole. No omitted or
doubled live coalition slot was established.

PRACTICAL 6243 / KI#362 -- CLEAN
--------------------------------
Both repaired callers now pass `AI_randomCounterChange` the accumulated counter's
non-negative remaining room. Because the helper can return only 0, 1 or 2 and clamps that
increment to the supplied room, a current value at or below the intended ceiling cannot
overrun it. A legacy/save value already above the ceiling receives no new increment and is
left to the existing decay path rather than being increased further. Open-Borders and
Bonus-Trade callers retain their original probability, speed and attitude-limit formulas.

PRACTICAL 6243 / KI#363 -- CLEAN
--------------------------------
War-plan counts deliberately exclude targets while they are vassals. Entry removes the
old target plan before `m_eMaster` is assigned, when the helper still accepts the target.
Independence clears `m_eMaster` first and only then restores the target's *current* stored
plan for every living AI team. This also handles a plan whose raw value changed while the
target was a vassal: intermediate transitions were intentionally excluded from the cache,
and the one current value is re-added on independence. Dead teams have their plans cleared
by their own lifecycle and do not require restoration. No remaining ordering/cache seam
was found.

BUNDLE VERDICT
--------------
KI#361-KI#363 survive current-source adversarial review. F739 remains unused.
Practical 6242 is audit-album reconciliation only and introduces no gameplay C++ repair to
adjudicate.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP810
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT SUBSTANTIVE: practical 6241 / rewritten commit ba9f4317ad0b3aca0493243edc7ce6278b50c907
      (KI#357-KI#360: CvTeamAI refactor defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP810
===============================================================================

===============================================================================
C031-WIP811 APPEND -- practical 6241 repair audit
===============================================================================

SCOPE
-----
Rewritten commit ba9f4317ad0b3aca0493243edc7ce6278b50c907:
    Fix inherited AdvCiv CvTeamAI refactor defects (KI#357-KI#360)
Current-tree review of war-weariness input units, capitulation timing and technology-rarity
population consistency. KI#358's later completion was adjudicated separately at WIP809.

PRACTICAL 6241 / KI#357 -- CLEAN
--------------------------------
`AI_warCommitmentCost` constructs `iTotalWW` in the same units used by the real player
war-weariness update (`getWarWeariness(..., true) / 100`). Every evaluating team member now
passes that raw coalition quantity to `getModifiedWarWearinessPercentAnger`, which applies
that member's game/handicap modifiers, while `iWWCost` remains solely the accumulating
output valuation. This restores the pre-1842 dataflow and removes the prior cross-domain
feedback from AI-value units into raw weariness. No member-order dependence survives.

PRACTICAL 6241 / KI#359 -- CLEAN
--------------------------------
The current positive condition is equivalent to the pre-refactor control flow: an
alternative free-major enemy can make `bMightCapToOther` true only once the surrendering
team's at-war counter has reached the era-adjusted minimum *and* the rival's WarSuccess is
sufficiently stronger. The repaired `>=` therefore restores the intended minimum-duration
gate rather than introducing a new maximum-duration boundary.

PRACTICAL 6241 / KI#360 -- CLEAN
--------------------------------
Pre-1842 source explicitly excluded only `isCapitulated()` teams before incrementing either
known-team or tech-owner counts; voluntary vassals remained eligible. The current repair
matches that contract exactly by continuing before both counts when capitulated and then
counting technology ownership and denominator membership over the same remaining known
alive-team population. Later tech-trade/locus helpers do not alter this rarity population.
No numerator/denominator asymmetry remains.

BUNDLE VERDICT
--------------
KI#357, KI#359 and KI#360 survive current-source adversarial review. KI#358 is already
covered by WIP809. F739 remains unused.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP811
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT: practical 6240 / rewritten commit 5f5b6105e4d672767be04ef277851703f6b53294
      (KI#353-KI#356: vote/war/air-power and multi-member-team defects)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP811
===============================================================================

===============================================================================
C031-WIP812 APPEND -- practical 6240 repair audit
===============================================================================

SCOPE
-----
Rewritten commit 5f5b6105e4d672767be04ef277851703f6b53294:
    Fix inherited CvTeamAI vote, war and air-power defects (KI#353-KI#356)
Current-tree adversarial review of vote-candidate identity, target-city population, rival
air-power averaging and multi-member legacy-war set accounting.

PRACTICAL 6240 / KI#353 -- CLEAN
--------------------------------
`AI_diploVoteCounterCandidate` now tracks both the vote totals and the teams owning first
and second place. Strict insertion plus the third-place closeness guard behaves coherently
for equal top-two totals, three-way ties, two-candidate games and a lone eligible team.
The function returns an opponent only when the evaluating team actually owns the other
top-two slot; no count/TeamTypes domain confusion survives.

PRACTICAL 6240 / KI#354 -- CLEAN
--------------------------------
`AI_haveSeenCities(eTeam,...)` now enumerates members of the requested target team and
applies the inherited deduction/primary-area/minimum-count tests to those cities. Current
legacy-war callers pass the concrete proposed target and separately handle master/vassal
war-plan identity, so the repair restores the intended object without introducing a
coalition-population mismatch.

PRACTICAL 6240 / KI#355 -- CLEAN
--------------------------------
Eligible rival teams are now filtered and counted once each, then all qualifying default
air unit classes are summed inside that team. The same rival inclusion set feeds both the
average denominator and rival-power numerator, while planned/current enemy power is an
intentional additional term. Later KI#368 composes by reading the preparation plan through
the target/master-aware helper and does not alter the repaired averaging population.

PRACTICAL 6240 / KI#356 -- CLEAN
--------------------------------
The legacy war gate now computes the actual non-Dagger population and its intersections
with Financial Trouble and Get Better Units. Financial opposition therefore cannot be
canceled by a healthy Dagger teammate, the denominator matches the documented non-Dagger
population, and the two-thirds upgrade postponement uses the corresponding intersection.
An all-Dagger team remains eligible for war checks, while the original total counts remain
available for the separate pro-war/pressure terms that intentionally use all members.

BUNDLE VERDICT
--------------
KI#353-KI#356 survive current-source adversarial review. F739 remains unused.
Practical 6239 is audit-album reconciliation only and has no gameplay repair to adjudicate.

CURRENT CURSOR
--------------
PHYSICAL CHECKPOINT: C031-WIP812
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F738 remain confirmed.
F739 / provisional KI#1060: UNUSED.
NEXT SUBSTANTIVE: practical 6238 / rewritten commit f0d50e744fdaf7299003c05971ba21f6c0a52df0
      (KI#48.3, KI#70, KI#74: CvCityAI corrections/clarifications)
      -> current-source proof -> adversarial repair review -> whole-album dedup.

No SAS/Base AdvCiv/K-Mod/Civ4CE gameplay/XML/Python source was modified during this
audit.
===============================================================================
END C031-WIP812
===============================================================================

===============================================================================
C031-WIP813 CONTINUATION - practical 6238 adversarial repair review
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6238 / f0d50e744fdaf7299003c05971ba21f6c0a52df0:
"Correct CvCityAI KI issues and complete and clarify the KI#74 CFC review and cure (KI#48.3, KI#70, KI#74)".
Current composed AdvCiv-SAS source was treated as authoritative and the historical diff as repair context.

RESULT
------
Practical 6238 survives the adversarial current-tree review. No new independent root was established. F739 / provisional KI#1060 remains UNUSED.

KI#70 - forced-specialist sanitizer
-----------------------------------
The repair independently validates the assigned count and the forced target. `CvCity::isSpecialistValid(eSpecialist, iExtra)` tests
`getSpecialistCount(eSpecialist) + iExtra <= getMaxSpecialistCount(eSpecialist)` unless the player has an unlimited specialist source or the specialist is the default specialist. Therefore
`iMissingForcedSpecialists = force - assigned` makes `isSpecialistValid(e, iMissingForcedSpecialists)` validate the intended forced-target total exactly. A city-limited stale target is capped to the current max; unlimited/default specialists remain valid. If the assigned count already exceeds the forced target there is no missing forced population to sanitize, and the independent assigned-count path handles any actual capacity violation.

KI#48.3 - building prefilter effective-food signal
--------------------------------------------------
`foodDifference(false, true)` computes `yield food - foodConsumption()` even while producing food-production units. `foodConsumption()` subtracts `healthRate`; `healthRate` is `min(0, goodHealth-badHealth)`, so unhealthiness already raises food consumption by the unhealthy amount. Subtracting `max(0,-iHealthLevel)` again therefore really did double-count unhealthy food loss. The current nonnegative net-food signal with the existing happiness-headroom gate matches the stated repair and its later threshold consumers.

KI#74 - AI_bestSpreadUnit NO_UNIT sentinel
-----------------------------------------
The root correction `return (*eBestSpreadUnit != NO_UNIT)` is correct for the enum output. The helper itself does not initialize the output parameter, so the contract was adversarially checked at every live caller rather than assumed safe. Both current callers explicitly initialize `UnitTypes eBestSpreadUnit = NO_UNIT` and `iBestSpreadUnitValue = -1` before the call. Consequently the no-candidate path is deterministic and false, while a selected candidate updates both outputs. Existing caller-side NO_UNIT guards remain defense in depth. No current uninitialized-output caller survives.

DEDUP / CONTROLS
----------------
The broader current `AI_bestSpreadUnit` missionary/team valuation issue is already represented elsewhere in the album and is not part of the KI#74 Boolean-sentinel repair. No new finding was manufactured from it here.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F738 CONFIRMED.
F739 / provisional KI#1060 UNUSED.
Latest physical checkpoint: C031-WIP813.
No repository source files modified.

NEXT
----
Continue backward from the next substantive practical before 6238 using the same repair-side adversarial and whole-album dedup pass.

===============================================================================
C031-WIP814 CONTINUATION - practical 6236 / F739 KI#1060 CONFIRMED
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6236 / b7535c87f11a33fd3864a99369d0155dc9ea5299:
"Fix inherited AdvCiv/BtS AP/UN replay privacy and voter grace bugs (KI#337, KI#343)".
Practical 6237 is reconciliation-only.

F739 / KI#1060 - CONFIRMED
--------------------------
TITLE:
Info Screen Timeline can reuse the real human's observer-filtered replay after switching to a vassal perspective, bypassing KI#337's historical AP/UN audience mask.

CLASSIFICATION:
Current AdvCiv-SAS cross-context / incomplete-repair defect. The historical audience mask introduced by KI#337 is correct; the defect is the Timeline consumer's cached-snapshot ownership contract when SAS's vassal-perspective advisor mode is used. This interaction is SAS-specific.

SOURCE PROOF
------------
KI#337 correctly stores one historical visibility bit mask beside every live `CvGame` replay message. `CvReplayInfo::createInfo(ePlayer)` copies an entry only when `isReplayMessageVisibleTo(i,ePlayer)` succeeds, except for the explicit NO_PLAYER reveal-all request.

However, `CvInfoScreen.buildTimelineCache` currently does:

    replayInfo = CyGame().getReplayInfo()
    if replayInfo.isNone() or self.bRevealAll:
        replayInfo = CyReplayInfo()
        ... replayInfo.createInfo(self.iActivePlayer or -1)

Thus a non-null `CvGame::m_pReplayInfo` is reused without proving that its observer is the Info Screen's currently selected `self.iActivePlayer`.

The mismatch is directly reachable in the normal end-game flow. `CvGame::showEndGameSequence` queues the Replay popup and then the Info Screen popup. `showReplay` calls `CyGame().saveReplay(humanPlayer)`, whose `CvGame::writeReplay` deletes/recreates the global `m_pReplayInfo` and calls `createInfo(ePlayer)`. The replay object therefore contains the historical-message subset visible to that real human.

The following Info Screen supports `bAllowVassalPerspective=True`. Outside Debug, `getAdvisorPerspectivePlayerIDs` explicitly offers the active player plus every alive vassal of the active player's team. When the user selects such a vassal, `self.bRevealAll` is false because the selected team differs from the real active team. `buildTimelineCache` then reuses the already non-null human-filtered `m_pReplayInfo` rather than rebuilding a snapshot for the selected vassal.

AP/UN voting membership is player-local: `CvPlayer::isVotingMember` is `getVotes(NO_VOTE,eVoteSource) > 0`. A master and its vassal can therefore have different historical voting-member status.

DETERMINISTIC CONSEQUENCES
--------------------------
Case 1 (privacy leak):
* real human A was a voting member when a passed resolution was reported;
* alive vassal B was not a voting member;
* end-game Replay builds `m_pReplayInfo` for A;
* subsequent Info Screen perspective is switched to B.

The passed-resolution row is already present in A's replay snapshot and is reused for B. The generic Timeline filters cannot reconstruct B's historical audience because this replay row is intentionally `NO_PLAYER` and coordinate-less -- precisely the original KI#337 problem. B therefore receives details that the historical mask excludes B from.

Case 2 (false omission): reverse A/B voting membership. The cached A snapshot lacks a row that B's own historical mask would admit, so B's selected perspective cannot see information it legitimately received.

This is not a failure of the mask serialization or `CvReplayInfo::createInfo`; it is stale observer identity on the reused replay snapshot.

REPAIR DIRECTION
----------------
Do not reuse a non-null `CyGame().getReplayInfo()` merely because it exists when Timeline perspective can differ. Rebuild an observer-specific `CyReplayInfo` for `self.iActivePlayer` whenever the selected perspective is not provably the observer for the cached snapshot; simplest robust Timeline behavior is to create a fresh snapshot for the selected perspective (and use NO_PLAYER only for reveal-all). Alternatively, carry explicit observer identity with the cached replay and require an exact match before reuse.

KI#343 CONTROL - CLEAN
----------------------
The repaired voter-grace path scans every current eligible major voter and changes every `NO_PLAYER_VOTE` to `NO_PLAYER_VOTE_CHECKED` in the same result pass. If any were newly marked, resolution postpones. On a later pass with no newly missing eligible voters, every still-checked eligible voter defaults to abstain together. Eligibility changes during the grace interval can legitimately create a new missing eligible voter and therefore a new grace interval for that newly eligible participant; iteration order no longer staggers players who were simultaneously missing. No sibling root was established.

DEDUP
-----
Whole-album searches found KI#337's original global replay-audience leak and the later general CvReplayInfo/CyReplayInfo audits, but no existing root for observer-specific replay reuse under Info Screen selected/vassal perspective. F738/KI#1059 is a different Info Screen perspective bug: PowR hover hardcodes the real active player. F739 is specifically replay-snapshot observer ownership and historical-message visibility.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP814.
No repository source files modified.

NEXT
----
Continue backward to practical 6235 / KI#346, then 6234 etc., adversarially checking each repaired current-tree contract and deduplicating before consuming F740.

===============================================================================
C031-WIP815 CONTINUATION - practical 6235 / KI#346 adversarial repair review
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6235 / 80a434cd895a6d2ad8e48da01bc1cdd72de0a85b:
"Fix inherited AdvCiv passable-area invalidation during K-Mod Ice melt (KI#346)".

RESULT
------
Practical 6235 survives the adversarial current-tree review. No new independent root was established. F740 / provisional KI#1061 remains UNUSED.

LIVE WATER-AREA MIGRATION
-------------------------
The repair detects an actual `isImpassable()` transition while global warming mutates plots, then performs one deferred refresh after the mutation batch.

`CvMap::recalculateWaterAreas()` is internally coherent for this live mutation:
* it collects and later deletes only old water CvAreas;
* water plots are detached with `setArea(NULL,false)`, deliberately avoiding process/decrement work on areas that are about to be destroyed;
* land plots and land CvAreas remain attached and alive;
* `calculateAreas_dfs()` only visits the now-area-less water plots and reattaches them through ordinary `setArea`, which invokes `processArea(+1)` and refreshes cached CvArea pointers on units occupying those plots;
* no external gameplay callback runs in the detach/delete/rebuild interval;
* `calculateReprAreas()` recomputes representative connectivity and calls `updateLakes()`, which in turn calls `computeShelves()`, so lake/yield/shelf topology is not left stale;
* after all water plots are attached, the helper restores the canonical primary adjacent-water-area city count for every city. Later album review independently established that this matches normal city area bookkeeping; `secondWaterArea()` is not a second ownership counter.

DERIVED STATE
-------------
The global-warming caller always resets step-path distance when passability changed. Water CvAreas are rebuilt only when `PASSABLE_AREAS` is enabled, which is correct because ordinary BtS-style area topology does not split on Ice when that option is off. Player `updatePlotGroups()` recalculates plot groups and then calls `verifyCityProduction()` and `updateTradeRoutes()`, closing the resource/trade/production consequences of the newly open channel.

DEDUP / CONTROLS
----------------
Later full-file album passes already revisited KI#346's shelf pointer lifetime, coastal-city counters, representative-area convergence and dynamic feature/passability invalidation and found no additional same-path current root. Scenario-start Ice topology is separately owned by F636/KI#958 and must not be duplicated here.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP815.
No repository source files modified.

NEXT
----
Continue to practical 6234 / KI#351 (river-edit derived state), then older practicals, with the same adversarial repair-side and whole-album dedup pass.

===============================================================================
C031-WIP816 CONTINUATION - practical 6234 / KI#351 dedup to existing F611 reopen
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6234 / d29d65355088fba9563f2a0b68ebe2cd145a75ec:
"Fix inherited BtS river-edit derived state (KI#351)".

RESULT
------
The practical-6234 repair remains INCOMPLETE in the current tree, but the surviving defect is already owned by the album's existing F611 / KI#351 REOPENED/BROADENED finding. No new independent root was established and F740 / provisional KI#1061 remains UNUSED.

EXISTING F611 / KI#351 REOPEN CONFIRMED
----------------------------------------
`updateRiverDerivedState` currently calls `updateIrrigated()` on the changed plot and each adjacent plot. This correctly handles a newly added river fresh-water source because `CvMap::updateIrrigated` can flood an eligible connected component from false to true.

The removal direction remains structurally different. If the last river fresh-water source is removed from a connected component whose potential irrigation plots are already `m_bIrrigated=true`, the inherited updater has no source-loss validation/teardown branch for an already-irrigated potential carrier. Calling the same local `updateIrrigated()` methods therefore leaves the source-less component irrigated. This exact current-tree defect was previously proven and frozen as F611 while deep-auditing CvPlot.cpp; do not consume F740 for it.

PORTIONS OF KI#351 THAT REMAIN VALID
------------------------------------
The finalized-game edge-change guard is appropriate. The helper's city fresh-water-health refresh over the local radius covers the plots whose `isRiver()` state can change from the edited edge. `GC.getGame().updatePlotGroups()` performs the intended nonlocal plot-group/resource/trade refresh. These portions are not reopened by F611.

REPAIR DIRECTION (UNCHANGED FROM F611)
--------------------------------------
Runtime loss of a fresh-water source needs a source-aware irrigation-component validation/rebuild rather than another call to the one-way per-plot updater. The same deeper source-loss primitive can support the separately owned F610/KI#933 AddsFreshWater-feature removal path without merging their distinct mutator roots.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
Existing earlier F611 / KI#351 REOPENED/BROADENED remains current and explains practical 6234's surviving defect.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP816.
No repository source files modified.

NEXT
----
Continue to practical 6233 / KI#350, then older practicals. Reuse existing finding ownership when chronology exposes a root already frozen elsewhere; consume F740 only for a distinct current defect.

===============================================================================
C031-WIP817 CONTINUATION - practical 6233 / KI#350 adversarial repair review
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6233 / 828658a349f47e87b766d873b63d21e4fab436e1:
"Fix inherited AdvCiv hidden-nationality leak in nuke reports (KI#350)".

RESULT
------
Practical 6233 survives the adversarial current-tree review. No new independent root was established. F740 / provisional KI#1061 remains UNUSED.

OBSERVER-RELATIVE IDENTITY
--------------------------
`NukeUnitEffect` snapshots `CvUnit::getVisualOwner(observerTeam)` while the affected unit still exists, before lethal damage/cargo destruction can remove it. `getVisualOwner` is explicitly team-relative and maps concealed hidden-nationality units to the Barbarian visual owner unless the observer is entitled to the real identity. One stored identity per observer team is therefore the correct privacy granularity; teammates do not need distinct values.

The later report loops visible effects and groups them by the stored visual owner, using that identity for civilization adjective, own-team/enemy classification and detailed-vs-count-only formatting. It no longer falls back to the destroyed object's real owner after lifetime end.

POPULATION / LIFETIME CONTROLS
------------------------------
The constructor initializes every team slot to NO_PLAYER, then fills all alive major-observer teams. The report itself is generated for the corresponding alive-major observer population, so no live reporting observer lacks a captured identity. Cargo killed through a lethal transport is snapshotted before transport damage destroys it. Plot visibility is still tested at report time, preserving the original visibility gate.

No current use-after-free, real-owner regrouping fallback, or sibling report path was found that re-exposes the concealed owner.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP817.
No repository source files modified.

NEXT
----
Continue to practical 6232 / KI#347-KI#349 + KI#352 and older chronology.

===============================================================================
C031-WIP818 CONTINUATION - practical 6232 / KI#347-KI#349 + KI#352 CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6232 / d12f1271d1ea2827db087f0c953f888f7571a521:
"Fix inherited BtS/AdvCiv CvPlot defects from audit C010 (KI#347-KI#349, KI#352)".

RESULT
------
All four repairs survive the adversarial current-tree review. No new independent root was established. F740 / provisional KI#1061 remains UNUSED.

KI#347 - Python found-value override caching
--------------------------------------------
`CvPlot::getFoundValue` now resolves the Python `-1` sentinel into the native value or keeps the real Python override, then writes either result into the same integer cache before area-best comparison. Current cache storage is full `int`, and the existing scenario randomization is applied only to the returned view, not accidentally baked into the authoritative cached value. No non--1 override path remains uncached.

KI#348 - foreign-team air capacity
----------------------------------
`airUnitSpaceAvailable(eTeam)` now passes `eTeam` to `CvCity::getAirUnitCapacity`, matching the team whose noncargo air units are counted. Every movement/rebase caller that supplies an aircraft/team context therefore receives the requested team's city capacity.

A tempting current `airUnitSpaceAvailable(getTeam())` call inside `CvPlot::canTrain` is not a repair bypass: `canTrain` is city/plot production legality and has no foreign unit-owner parameter. The producing city's plot team is exactly the correct capacity team there.

KI#349 - debugStr lifetime
--------------------------
`CvPlot::debugStr` now returns owning `CvWString` storage. The five live found-value logging callers pass `debugStr().c_str()` directly into the logging call; the temporary string remains alive until the end of that full expression, so its buffer is valid for the entire variadic call. No current caller stores the temporary pointer beyond the expression.

KI#352 - culture-decay player-domain buffer
-------------------------------------------
The protecting-radius lookup is now `MAX_PLAYERS`, matching the later `FOR_EACH_ENUM(Player)` domain including `BARBARIAN_PLAYER`. Nearby Barbarian cities remain deliberately excluded as radius-protection sources, while Barbarian culture can safely be looked up and decayed. The 48-civilization current DLL therefore no longer reads one slot past the old civilization-only array.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP818.
No repository source files modified.

NEXT
----
Continue to practical 6231 / KI#342 + KI#344, then practical 6230 / KI#341 + KI#345.

===============================================================================
C031-WIP819 CONTINUATION - practical 6231 / KI#342 + KI#344 CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6231 / 5dba4057e84e0052f1793fb45d4bfe05a837bc81:
"Fix inherited CvGame edge cases and reconcile audit C010 (AdvCiv KI#342, BtS KI#344)".

RESULT
------
Both repairs survive the adversarial current-tree review. No new independent root was established. F740 / provisional KI#1061 remains UNUSED.

KI#342 - global-warming retry exhaustion
----------------------------------------
`CvGame::getRandGWPlot` now resets `eTerrain=NO_TERRAIN` independently for every outer pool candidate and nulls `pTestPlot` after every rejected open-water/peak draw. A valid Ice draw sets frozen terrain and breaks with a live plot; valid nonpeak land sets its actual terrain and breaks; an exhausted sequence therefore leaves NULL and is skipped rather than scoring its final rejected plot against stale terrain.

A `syncRandPlot` NULL also breaks directly with NULL and reaches the same skip. No path was found where an unsuitable plot remains nonnull after the retry loop. The album's later whole-file global-warming regression control independently records the same repaired invariant.

KI#344 - Lock Modified Assets password
--------------------------------------
The replacement seven-character password alphabet contains only printable non-NUL ASCII and appends the terminator explicitly, so the first-byte-empty-password failure is eliminated without depending on localization or code page behavior.

`CvRandom::getInt` advances the synchronized RNG seed exactly once before mapping the current seed into the supplied range. The repaired code still performs exactly seven `getSorenRandNum` calls; changing the bound from the old arbitrary-byte range to 62 changes only the seven returned password symbols, not how many times or in what order the authoritative RNG state advances. ASCII `char` -> init-core password conversion is therefore safe for this internal token and synchronization position is preserved.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP819.
No repository source files modified.

NEXT
----
Continue to practical 6230 / KI#341 + KI#345, then older chronology.

===============================================================================
C031-WIP820 CONTINUATION - practical 6230 / KI#341 + KI#345 CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6230 / 946bb92966b5bdeb10552ac37309f61f98e18359:
"Fix inherited AdvCiv score and rare BtS Time Victory bugs (KI#341, KI#345)".

RESULT
------
Both repairs survive the adversarial current-tree review. No new independent root was established. F740 / provisional KI#1061 remains UNUSED.

KI#341 - initial score free-technology normalization
----------------------------------------------------
`CvGame::initScoreCalculation` now passes the technology-loop enum to `isCivilizationFreeTechs`, matching the original K-Mod/BtS contract. Each technology earlier than the selected start era is counted through the earlier-era branch. For later/current-era technologies, the playable-civilization scan counts the technology once if any playable civilization receives it free; the immediate `break` prevents duplicate initial-score credit when multiple civilizations share that starting technology.

The repaired structure therefore restores the intended union of possible civilization starting technologies without double-counting earlier-era or shared free technologies. No current enum-domain or start-era interaction reopens the AdvCiv refactor bug.

KI#345 - Time Victory tied-deadline fallthrough
-----------------------------------------------
`testVictory(eVictory,eTeam,&out)` deliberately resets and returns the End-Score property for that single tested victory. The outer `CvGame::testVictory()` loop needs a different aggregate fact: whether any enabled tested victory is End Score. The repair therefore uses a fresh per-call boolean and ORs it into `bEndScore`, eliminating dependence on later XML entries or later team iterations.

At a tied Time-Victory deadline, the End-Score victory is enabled and marks the aggregate true but no team passes the strict score-leader test; the generic max-turn fallback is therefore correctly suppressed so the enabled victory can remain unresolved. A unique score leader still reaches the ordinary winner path, and games without an enabled End-Score victory still retain the generic max-turn fallback.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F739 CONFIRMED.
F740 / provisional KI#1061 UNUSED.
Latest physical checkpoint: C031-WIP820.
No repository source files modified.

NEXT
----
Continue to practical 6229 / KI#338-KI#340, then older chronology.

===============================================================================
C031-WIP821 CONTINUATION - practical 6229 / KI#338-KI#340 + F740/KI#1061
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6229 / 6f136ab2d0c704463b3ecd69883276240db775a0:
"Fix inherited AdvCiv/BtS starting-position bugs from C++ audit album C009 (KI#338-KI#340)".
Also compared the current fallback against K-Mod and AdvC practical 2708
`9a623ad895c73c2ce0fe4d5f5cf668ab04848307`, which introduced the current
`abPlayerDone` refactor.

RESULT
------
KI#338, KI#339 and KI#340 survive the adversarial current-tree review. However, the same team-start fallback contains a distinct inherited AdvC regression not repaired by KI#340. Whole-album/current-KI dedup found no existing owner, so F740 / provisional KI#1061 is PROMOTED and CONFIRMED.

F740 / PROVISIONAL KI#1061 - TEAM-START FALLBACK CAN PERMANENTLY BLOCK A LATER TEAMMATE BEHIND A PREASSIGNED START
-------------------------------------------------------------------------------------------------------------------
ROOT FILE: `CvGameCoreDLL/CvGame.cpp`, `CvGame::assignStartingPlots` BtS-team-assignment fallback.
INHERITANCE: introduced by AdvC practical 2708's starting-position/SPI refactor; current Base AdvC 1.14 retains it. K-Mod's older fallback does not have this bookkeeping defect.

CURRENT DEFECT
--------------
For each alive team, the fallback scans `MemberIter` until it finds a member that can provide a starting plot. `abPlayerDone` is intended to make later passes advance to subsequent teammates. The current code sets that flag only inside the branch that began with a NULL starting plot:

    if (abPlayerDone.get(member))
        continue;
    if (member.getStartingPlot() == NULL)
    {
        member.setStartingPlot(member.findStartingPlot());
        abPlayerDone.set(member, true);
    }
    if (member.getStartingPlot() != NULL)
    {
        bStartFound = true;
        break;
    }

A member who already had a valid start before entering this fallback therefore triggers the final `break` but is never marked done. Every later outer pass reaches the same member first and breaks again. A later teammate with no starting plot is never visited.

MINIMAL INDEPENDENT REPRODUCTION
--------------------------------
Take one ordinary two-player alive team T=(P0,P1) on the fallback path. Let P0 already have a starting plot, e.g. one of the map's pre-marked starting plots collected and randomly assigned by the immediately preceding `assignStartingPlots` setup block; let P1 still have NULL.

Every pass does:

    P0: done=false, start!=NULL -> bStartFound=true -> break team
    P1: never reached

After all `2*MAX_PLAYERS` passes P1 is still NULL, so the Debug verification `FAssert(it->getStartingPlot() != NULL)` fails.

This does not depend on F626/KI#948's separate SPI unequal-team underfill. The very first block of `assignStartingPlots` deliberately supports a finite list of pre-marked map starts and stops assigning when that list is exhausted, after which the fallback is responsible for players still lacking starts. K-Mod's inherited fallback likewise explicitly searched for a member whose starting plot was NULL, so mixed preassigned/unassigned team members are part of the fallback's intended input domain. F626 can create another partial-start trigger, but is not required for F740.

RELEASE-PATH CONSEQUENCE
------------------------
The later team rearranger explicitly tolerates NULL starting plots while calculating its tentative permutation, but it only swaps/reassigns existing player->plot references and cannot create the missing start. `applyStartingLocHandicaps` likewise permits NULL start values through its comparator and ultimately emits an error/continues rather than guaranteeing recovery. The omitted teammate can therefore remain without a valid start in Release rather than this being Debug-only bookkeeping.

HISTORY PROOF
-------------
Before practical 2708, AdvC's own fallback used `abNewPlotFound` and marked a member after the member was known to have a non-NULL starting plot, whether that plot was pre-existing or newly found. The 2708 rewrite introduced `abPlayerDone` and moved the flag assignment into only the `startingPlot == NULL` branch. K-Mod's source similarly skips already-started players and selects a still-NULL team member. The current behavior is therefore a clear refactor regression rather than an intentional new team-start rule.

REPAIR DIRECTION
----------------
Mark the member done when that member actually has a valid starting plot, immediately before the successful `bStartFound/break`, instead of only because the function attempted `findStartingPlot`. That restores progression past preassigned members and also avoids declaring a failed NULL `findStartingPlot` attempt permanently done. Conceptually:

    if (start == NULL)
        setStartingPlot(findStartingPlot());
    if (start != NULL)
    {
        abPlayerDone.set(member, true);
        bStartFound = true;
        break;
    }

Keep KI#340's separate rotation over the compact `aeAliveTeams` vector.

REVIEWED 6229 REPAIRS
---------------------
KI#338 is sound: the volatility safeguard now measures `apStartingSitesByValue[i]`, exactly the site about to be assigned to the human's handicap slot; adjacent candidate sites are measured in the same sorted-site domain.

KI#339 is sound: the pre-existing random eligible-agent draw is translated to the chosen eligible agent's absolute source index, then full-list cyclic traversal preserves the relative cyclic order of that human/AI subset while skipping the opposite subset. Equal/colliding handicap placements therefore genuinely rotate instead of reverting to fixed input order.

KI#340 is sound: the fallback now constructs the actual alive-team ID vector and applies the random compact offset within that same vector. Sparse/high TeamTypes IDs no longer distort first-priority rotation. F740 is an independent per-member completion-state defect inside the repaired per-team traversal.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F740 CONFIRMED.
F741 / provisional KI#1062 UNUSED.
Existing earlier F611/KI#351 reopened and F626/KI#948 remain separately owned.
Latest physical checkpoint: C031-WIP821.
No repository source files modified.

NEXT
----
Continue to practical 6228 / KI#331 and older chronology. Keep F740 distinct from KI#340 and F626.

===============================================================================
C031-WIP822 CONTINUATION - practical 6228 / KI#331 CLEAN / SATURATED
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6228 / 2e35a0212c7aa2bdec7e2402f505646a69184205:
"Fix AdvCiv-SAS-only scrap-contract regression (KI#331)".
Re-ran a current whole-DLL caller sweep and reconciled against the album's later full-file KI#331 saturation controls.

RESULT
------
The repair remains sound at the current caller/callee contract. No new independent root was established. F741 / provisional KI#1062 remains UNUSED.

CURRENT CONTRACT
----------------
Routine `CvUnit::scrap()` now reports bool success after `canScrap()`. `scrapInternal()` performs the actual centralized logging and `kill(true)`. The explicit `scrapForced(bool)` path retains the fundamental fighting guard, respects the absolute AI-scrap switch for discretionary forced AI cleanup, and only bypasses that preference for the mandatory capitulation-disarm rule.

CALLER RECONCILIATION
---------------------
The live caller sweep remains consistent:
- upgrade cleanup marks `bKilled`, forces delayed death, clears upgrade state and recalculates cost only after `pLoopUnit->scrap()` succeeds;
- financial disband filters the initial candidate set by `canScrap`, advances its candidate index independently, increments the successful-disband quota only on true `scrap()`, and recalculates unit cost afterward;
- Worker, Explorer, trapped-Barbarian-ship, Escort Sea and excess Sea Explorer paths return only after successful routine scrap, so a preservation veto falls through to their ordinary fallback behavior;
- idle Barbarian Worker prechecks `canScrap()` immediately before the same unit's `scrap()` with no intervening mutation, so the untested return there is not a stale contract assumption;
- human `COMMAND_DELETE` is likewise admitted by `canDoCommand(COMMAND_DELETE)` through `canScrap` before `doCommand` executes the deletion;
- Settler-transport retirement unloads first and uses discretionary `scrapForced()`; doomed-city scorched-earth uses the same forced path only after its existing capture/evacuation/value checks; and capitulation nuclear disarm uses mandatory forced cleanup.

DYNAMIC FINANCIAL ELIGIBILITY
-----------------------------
The split `iDisbandCandidateIndex` / `iDisbandCount` design remains necessary and correct. `canScrap` can depend on state such as Worker totals/floors; a candidate that was eligible when the vector was built can become protected after an earlier successful removal. The subsequent `scrap()` recheck may then return false. Candidate traversal still advances, but the successful-disband quota does not, so the loop neither repeatedly retries that vetoed unit nor falsely spends its per-turn removal allowance.

WHOLE-ALBUM CONTROL
-------------------
Later Queue/full-file audits already re-grepped the current `scrap`/`scrapForced` surface and recorded old F002/KI#331 as repaired and saturated. The present chronology review found no new active caller outside that checked set. Do not allocate F741 to the old caller family absent a genuinely new contract violation.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F740 CONFIRMED.
F741 / provisional KI#1062 UNUSED.
Latest physical checkpoint: C031-WIP822.
No repository source files modified.

NEXT
----
Continue to practical 6227 and older chronology.

===============================================================================
C031-WIP823 CONTINUATION - practical 6227 / KI#330 + KI#332 CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6227 / b14b077d6782013b1e5850224819e0da6f8de5af:
"Fix SAS tech-contact and inherited BUG/Base AdvCiv leader-hover bugs (KI#330, KI#332)".
Reconciled the historical patch against the current CvPlayerAI.cpp relation logic, current XML option contract, the native WIDGET_PEDIA_JUMP_TO_LEADER parser, and every surviving Python leader-widget producer found in the current tree.

RESULT
------
Both repairs remain sound in the current composed tree. No new independent root was established. F741 / provisional KI#1062 remains UNUSED.

KI#330 - MASTER/VASSAL TECH-CONTACT POWER-BIAS EXEMPTION
-------------------------------------------------------
The current code computes the political relation independently of either configurable policy:

    const bool bVassalsOrMaster =
        kTheirTeam.isVassal(eOurTeam) ||
        kOurTeam.isVassal(eTheirTeam) ||
        (kOurTeam.isAVassal() && eMasterTeam != NO_TEAM &&
         kTheirTeam.isVassal(eMasterTeam) && eTheirTeam != eOurTeam);

The separately configurable preferential-contact branch may then multiply contact probability for master->vassal, vassal->master or sibling-vassal pairs. The later power-based contact modifier is gated only by `!bVassalsOrMaster`, exactly matching the XML promise that relative-power caution does not apply inside one master/vassal(s) locus even when the preferential-contact feature itself is disabled.

No missing supported topology was found. Civ4 vassalage is one master level: a prospective master cannot itself be a vassal, so there is no supported nested "vassal of a vassal" relation that the sibling-locus predicate would need to chase recursively. Master<->vassal is covered in both directions, and two distinct vassal teams of the same master are covered by the third branch. The equality guard merely prevents classifying the same vassal team as its own sibling.

The relation is computed within the existing CONTACT_TRADE_TECH path from the actual two player teams. It therefore controls only the intended contact-probability policy and does not alter tech-trade legality, valuation or research ownership.

KI#332 - LEADER-HOVER CIVILIZATION CONTEXT
------------------------------------------
The native C++ contract remains explicit: WIDGET_PEDIA_JUMP_TO_LEADER treats widget data2 as `CivilizationTypes` whenever data2 != -1, and passes that civilization to trait help so unit/building-class production modifiers can resolve civilization-specific replacements.

The current Python producer sweep found no surviving use of literal data2=1 as a generic enable flag. Concrete-player contexts now pass the displayed player's real civilization; Hall-of-Fame/replay and Battles contexts pass the civilization stored for that row. Generic Sevopedia leader links use the Leader page's existing canonical-civilization helper, which returns a civilization only when the leader has one unambiguous canonical civilization and otherwise returns -1. That deliberately suppresses civilization-specific replacement resolution instead of guessing an arbitrary civ for ambiguous generic leaders.

This also preserves the intentionally generic Trait-page behavior: a trait page describes the trait across civilizations, so it need not pretend that one civilization's replacement is canonical. The old Arabia leak therefore has not merely moved to another current widget producer.

WHOLE-ALBUM CONTROL
-------------------
Later full-file/widget audits already recorded old F001/KI#330 and F004/KI#332 as repaired. The present chronology pass independently rechecked the current caller/callee and option-composition contracts and found no new root in either family.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F740 CONFIRMED.
F741 / provisional KI#1062 UNUSED.
Latest physical checkpoint: C031-WIP823.
No repository source files modified.

NEXT
----
Continue to practical 6226 / KI#334 + KI#335 and older chronology. Pay special attention to observer-vs-subject visibility and replay/Timeline persistence, especially after the newer F739 alternate-perspective replay-cache finding.

===============================================================================
C031-WIP824 CONTINUATION - practical 6226 / KI#334 + KI#335 CLEAN
===============================================================================

SOURCE / CURSOR
---------------
Reviewed practical 6226 / 2763e46c4742a6f1757469dc56bc5332fbf7adcd:
"Fix inherited AdvCiv/AdvCiv-SAS battle-information leaks (KI#334, KI#335)".
Reconciled the patch against current CvUnit::kill, current replay/audience handling and Timeline filtering, current CvMilitaryAdvisor Battles consumers/cache keys, current vassal `hasMet` synchronization, and the later F739 alternate-perspective replay-cache finding.

RESULT
------
Both repairs remain sound at their current contracts. No new independent root was established. F741 / provisional KI#1062 remains UNUSED.

KI#334 - GREAT-GENERAL KILLER IDENTITY
---------------------------------------
Current transient reporting distinguishes three observer classes correctly:
- victim and killer receive their dedicated exact messages;
- spectators retain unrestricted exact reporting;
- ordinary third parties receive the exact two-civilization text only after `hasMet` succeeds for BOTH victim and killer teams. An observer that knows the victim but not the killer receives `TXT_KEY_MISC_GENERAL_KILLED_PUBLIC`, which omits the killer identity.

The persistent replay row is deliberately more conservative: it always stores the public victim-only text and has no coordinates. Therefore the replay payload itself no longer contains the two-party fact that caused old KI#334.

The later KI#337 replay-audience architecture does not reopen this root. Great-General rows use the victim as replay-message player and the ordinary Timeline additionally applies the selected perspective team's `hasMet` test to that player. F739 concerns observer-specific replay rows whose historical audience is represented only by the replay visibility mask (notably NO_PLAYER AP/UN rows) being reused from the real human's cached snapshot; the Great-General row remains present in ordinary snapshots but is independently hidden by the Timeline when the selected perspective has not met its victim. Thus F739 is not a second KI#334 failure.

A hidden-nationality killer was also checked as an adversarial identity boundary. Current SAS data has the Privateer as the supported hidden-nationality combat unit; the KI#334 message-producing `kill(ePlayer)` path is principally the noncombat/capture/damage kill path rather than ordinary lethal combat, and no supported Great-General/Privateer route was established that reaches this reporting branch while requiring the Privateer's true owner to remain concealed. Do not promote that theoretical identity concern without a concrete current route.

KI#335 - BATTLES SUBJECT VS OBSERVER MAP PRIVACY
------------------------------------------------
Current `getBattlePerspectiveColumns` keeps `self.iActivePlayer` as the selected battle-history SUBJECT, but passes stored X/Y and historical plot context through `getBattleObserverPlotContext` before any table/log/camera consumer receives them. Outside debug mode that helper uses the real active player's team as OBSERVER. An unrevealed plot becomes `(-1,-1,-1,-1,-1,0)`, simultaneously removing coordinates, terrain, feature, hill/peak and city context.

All relevant consumers use the sanitized tuple:
- table rows receive no Plot art/action when X/Y are hidden;
- copied LOG output receives the same sanitized coordinates/context;
- aggregate battle-stat consumers use the same perspective conversion but do not reconstruct hidden X/Y;
- the preserved-table cache key includes selected subject, real observer player and debug mode, so rows built under one privacy context are not reused under another.

The earlier concern that a selected vassal could reveal an opponent civilization unknown to the human master remains rejected. `CvTeam::setVassal` synchronizes `hasMet` relations between master and vassal across major third parties when vassalage begins; `hasMet` is persistent diplomatic knowledge. The remaining KI#335 contract is therefore specifically map/location/context privacy, and the current real-observer reveal gate closes it.

Debug mode intentionally bypasses the map sanitization and is not a privacy failure.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F740 CONFIRMED.
F741 / provisional KI#1062 UNUSED.
Latest physical checkpoint: C031-WIP824.
No repository source files modified.

NEXT
----
Practical 6225 is archaeology/album organization only; use it as a chronology control. Then continue to practical 6224 / rejected KI#327 vote-poll repair and older chronology, preserving rejected-vs-confirmed evidence levels.

===============================================================================
C031-WIP825 CONTINUATION - practical 6225 CONTROL + 6224 / KI#327 REJECTION HOLDS
===============================================================================

SOURCE / CURSOR
---------------
Practical 6225 is the archaeology/album organization commit and introduces no gameplay repair to adversarially re-open.
Reviewed practical 6224 / 6eb6978ecb7fe34cfe67c08b37aa56e316a8f224:
"Reject hidden inherited BUG/K-Mod vote-poll repair after runtime review (KI#327)".
Rechecked current BUG option exposure and current CvVictoryScreen reachability to ensure later work has not turned the formerly rejected/dormant path into an ordinary supported UI feature.

RESULT
------
The KI#327 rejection remains the correct classification. No new current root was established. F741 / provisional KI#1062 remains UNUSED.

CURRENT REACHABILITY
--------------------
`Assets/Config/BUG Advisors.xml` still defines `BugMembersTab` with default False, but `BugAdvisorOptionsTab.py` still has the visible checkbox commented out. No other current Python/settings surface exposes the option through normal UI. `CvVictoryScreen.showMembersScreen` enters the inherited advanced BUG poll only when `AdvisorOpt.isMembers()` is manually enabled; otherwise the supported plain Members view is used.

Thus later changes have not made the hidden poll an ordinary default path. A user can still edit the underlying BUG configuration manually, exactly as documented when KI#327 was rejected, but the album intentionally does not promote every dormant/manual hidden-option approximation to a gameplay defect.

SEMANTIC CONTROL
----------------
The retained advanced poll is still only an estimate built from visible attitude information and still differs from the DLL's true team vote decision in several ways. That mismatch is real source debt, but replacing it with exact `AI_diploVote` output would expose hidden strategic/vassal/tie/victory-stage state while the UI continues to present itself as a poll/statistical estimate. The previously attempted exact-repair API remains absent. No evidence changed the product/design decision that caused KI#327 to be rejected rather than fixed.

STATE
-----
Primary 169/169 COMPLETE.
Post-primary findings F708-F740 CONFIRMED.
F741 / provisional KI#1062 UNUSED.
Latest physical checkpoint: C031-WIP825.
No repository source files modified.

NEXT
----
Continue to practical 6223 / KI#328 and 6222 / KI#329. Preserve KI#329's lower evidence level: source-real defensive hazards are not to be promoted without a supported trigger or a distinct current regression.

================================================================================
C031-WIP826 - chronology continuation: practical 6223 / KI#328 and practical 6222 / KI#329
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP825.
No repository source files modified by this audit continuation.

Practical 6223 - KI#328 (Planet getGridSize Python->SDK integer-vector contract)
----------------------------------------------------------------------------------
Adversarial verdict: CLEAN in the current composed tree.

The current Planet_Generator_0_68.py repair explicitly returns
`(int(fW), int(fH))`. This closes the exact bridge contract exposed by the
archaeology: Planet's center-spacing arithmetic can leave `fW` as a Python
float, while CvDLLPythonIFaceBase::putSeqInArray asserts `PyInt_Check(item)`
before converting each getGridSize result with `PyInt_AsLong`. The native
CvPythonCaller::getGridSize path also requires the returned dimensions to be
positive.

The explicit `int` conversion preserves the release bridge's pre-existing
truncation semantics rather than changing the generated-map sizing rule; it
merely makes that effective conversion legal and deterministic in Debug too.
No later caller or alternate Planet return path bypasses the corrected final
return. No surviving sibling was found.

Practical 6222 - KI#329 (BTG Grid/Cross defensive retry + forced-Silver hardening)
----------------------------------------------------------------------------------
Adversarial verdict: the defensive hardening remains correctly classified as
source-level robustness work below the confirmed-defect threshold; no new F741.

Grid's retry state (`player`, `iRange`, `iPass`, `validFn`) now lives outside
the retry/area loops in both regional-start branches, so a failed pass can
actually advance toward the finite default-placement fallback and an empty
area list no longer strands initialization inside the body that never runs.
This is the intended state-machine repair and matches the finite Cross-style
contract.

For optional forced Silver, Grid and Cross no longer index an unproven
`plotsboundaries[0]`. If the narrow boundary population is empty, they use an
empty no-bonus land plot already collected in `plotsfurther`; if neither
population provides a candidate, optional normalization is safely left unmet.
The fallback population is built only from non-None, passable, non-water plots,
and the added no-bonus filter prevents overwriting another resource merely to
avoid an IndexError.

Adjacent control: Cross still contains inherited unchecked `plotsclose[0]`
and `plotsfurther[0]` forced fallbacks for Oil, Aluminum and Elephants. They
are visually similar source hazards, but this pass did not establish a
supported map-generation state in which those broader near-start land
populations are empty. KI#329 itself was intentionally retained as defensive
hardening because its old failures likewise were not runtime-reproduced. The
adjacent fallbacks therefore remain an unnumbered robustness observation, not
a promoted correctness root. Similarity alone is not enough to consume F741.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP826.
Next substantive chronology cursor: practical 6221 / KI#325.
No repository source files modified.

================================================================================
C031-WIP827 - chronology continuation: practical 6221 / KI#325 and practical 6220 / KI#321
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP826.
No repository source files modified by this audit continuation.

Practical 6221 - KI#325 (past city-name identity across language changes)
-------------------------------------------------------------------------
Adversarial verdict: CLEAN in the current composed tree.

The repair's dual representation is internally coherent:

* generated civilization/explicit city-name candidates are validated using their raw
  canonical XML text key;
* CvCity stores that same raw key in `m_szName` for generated names, while manual names
  are stored as literal strings;
* `CvCity::getName()` renders either representation through
  `gDLL->getObjectText(m_szName, ..., true)`;
* renamed/disbanded former names enter the past-name registry as the raw key/literal;
* validity tests compare raw identity first and current rendered display second.

The apparent literal-name risk closes from the engine's existing public contract itself:
manual city names have always been displayed through `CvCity::getName()`, which calls
`getObjectText` on `m_szName`. Therefore an unknown/non-key literal must pass through as
its visible literal under the `bNoSubs=true` object-text path; otherwise manual renaming
would already fail independently of KI#325. The same rendering path is exactly what the
new duplicate comparison uses.

This gives the intended two protections simultaneously. A canonical stock name remains
reserved after a language switch because its raw key matches. A literal/manual name also
cannot collide with the text currently visible for a stock or other literal city because
the rendered strings are compared. City acquisition remains safe because its initial
assignment intentionally bypasses the duplicate scan while preserving the acquired raw
name, and later rename/disband paths register the outgoing raw identity.

Independent later whole-file CvPlayer review already re-challenged
`getNewCityName`/`getCivilizationCityName`/`isCityNameValid` specifically for
language/manual-name collisions and reached the same clean conclusion. No new F741 or
migration shape change is established; SAS's current-version-only save policy also makes
an old translated-vector migration branch intentionally unnecessary.

Practical 6220 - KI#321 (Sevopedia class-default civilization availability label)
---------------------------------------------------------------------------------
Adversarial verdict: CLEAN; presentation-only correction remains accurate.

The default Unit/Building page branch no longer claims literal `All`. It uses the compact
`AllR` token, whose Sevopedia Legend defines the exact scope as all remaining
civilizations whose class mapping still resolves to the generic item, excluding
civilization-specific replacements and NONE mappings. Unique-item pages retain exact
civilization buttons. This matches Civ4's CivilizationInfo class mapping model without
turning the compact default panel into a redundant wall of icons.

No gameplay contract, enum payload or availability test was changed by this commit, and
later current-tree review/runtime validation found the compact token and Legend coherent.
No sibling correctness root survives.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP827.
Next substantive chronology cursor: practical 6219 / KI#313 + KI#314.
No repository source files modified.

================================================================================
C031-WIP828 - chronology continuation: practical 6219 / KI#313 + KI#314
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP827.
No repository source files modified by this audit continuation.

Practical 6219 - KI#313 (human BFC Artist provenance)
-----------------------------------------------------
Adversarial verdict: CLEAN for the repaired human-convenience contract.

The three-state city-local provenance (`AVAILABLE`, `FORCED`, `FINISHED`) records whether
the helper itself owns the one forced Artist. It is initialized for new cities, serialized
symmetrically in current saves, reset out of the human-only provenance domain for AI
cities, set to FORCED only when the helper itself increments the force target, and moved
to FINISHED when the helper-owned force disappears or when the cleanup completes.

The important manual-control producer is also covered: city-screen specialist interaction
routes through `TASK_CHANGE_SPECIALIST -> CvCity::alterSpecialistCount`, and that function
sets the provenance to FINISHED before applying any human Artist adjustment. Therefore the
helper neither re-adds nor later removes an Artist after the human has taken control.

`changeForceSpecialistCount` remains a lower-level Python-exposed mutator that does not
itself alter provenance, but there is no current shipped Python caller using it to bypass
the normal human adjustment path. A hypothetical external-script call is therefore API
extension debt, not a current supported reproduction. The separately discovered stale AI
BFC Artist lifetime problem is already owned elsewhere (F551/KI#874) and is not a failure
of KI#313's human provenance repair.

Practical 6219 - KI#314 (Earth Day / Trade Bloc presentation mismatch)
----------------------------------------------------------------------
Adversarial verdict: CLEAN; the repair is intentionally content/semantics-facing.

Current trigger XML requires `CIVIC_TRADE_BLOC`; `canApplyEarthDay2` tests legal Trade
Bloc civic trading; `applyEarthDay2` sets the Economy slot to Trade Bloc for the selected
AI recipients; and the visible English/help now presents the same mechanics as the World
Trade Forum and explicitly describes economic integration/Trade Bloc adoption. The old
EARTHDAY internal identifiers and callback names remain internal implementation names.

The event's existing gold/happiness effects, recipient ranking and adoption mechanics were
not meant to be rebalanced by KI#314. No current visible Environmentalism promise or
mechanical civic mismatch survives, so no new root is established.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP828.
Next substantive chronology cursor: practical 6218 / KI#296 + KI#312.
No repository source files modified.

================================================================================
C031-WIP829 - chronology continuation: practical 6218 / KI#296 + KI#312
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Existing older root F003 broadens/reopens KI#312 and must not be renumbered.
Previous physical checkpoint: C031-WIP828.
No repository source files modified by this audit continuation.

Practical 6218 - KI#296 (residual Farm progress != live Worker commitment)
-------------------------------------------------------------------------
Adversarial verdict: CLEAN in the current composed Worker code.

The repair centralizes `SAS_isWorkerBuildActivelyCommitted`: a Farm is promised only if
a same-owner unit is physically on the plot and its selection group's current head
mission is `MISSION_BUILD` with exactly the requested `BUILD_FARM`. Current
`AI_bestCityBuild` uses this predicate for BFC future-food credit, candidate reservation
and the irrigation-chain alternate-source logic. Interrupted Civ4 build progress can
therefore remain numerically positive without supplying imaginary future food or
blocking a real connector Farm.

Later SAS Worker rewrites retained the same helper rather than reintroducing raw
`getBuildProgress(BUILD_FARM) > 0` as commitment evidence. Group mission semantics also
fit the contract: all members of one Worker group share its active build mission, so an
eligible same-owner unit on the target is sufficient evidence that the build is live.
No new KI#296 seam survives.

Practical 6218 - KI#312 (master/vassal internal technology supply)
-----------------------------------------------------------------
Adversarial verdict: PARTIALLY REPAIRED, but the remaining defect is ALREADY OWNED by
F003 / existing KI#312; do not allocate F741.

The current helper trio materially repairs the original legality assumptions:

* `AI_isLocusMember` defines the full master/vassal/sibling locus;
* `AI_canTradeTechFrom` tests player-level `canTradeItem` across source/recipient team
  members, including global No Tech Trading, per-tech tradeability, no-broker/no-trade
  state and recipient legality;
* `AI_hasLocusTechTradeSource` reuses that contract, with denial required only for the
  immediate contact-redirection case.

Thus AI_techValue, outsider tech valuation and the ordinary randomly selected
`eBestReceiveTech` redirect no longer infer supply merely from `isHasTech`.

However the current `AI_doDiplo` still computes AdvCiv's independent
`eBestProgressTech` from the same outsider. After cluster-first can set
`eBestReceiveTech = NO_TECH`, the later advc.550f progress-for-gold path tests
`eBestProgressTech != NO_TECH && eBestProgressTech != eBestReceiveTech` and can buy that
outsider technology without consulting the locus-supply preference at all. Clearing
`eBestReceiveTech` can make that inequality trivially true.

This exact current survivor was already established near the beginning of the primary
album as F003 broadening existing KI#312 and repeatedly reconfirmed during the full
CvPlayerAI re-loop. It remains one older incomplete repair, not a new chronology finding.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Existing F003 / KI#312 remains current and incomplete through the outsider
progress-for-gold fallback.
Latest physical checkpoint: C031-WIP829.
Next substantive chronology cursor: practical 6217 / KI#300 + KI#322.
No repository source files modified.

================================================================================
C031-WIP830 - chronology continuation: practical 6217 / KI#300 + KI#322
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Existing F679 reopens KI#300 and must not be renumbered.
Previous physical checkpoint: C031-WIP829.
No repository source files modified by this audit continuation.

Practical 6217 - KI#300 (Timeline razed hidden holy-city name)
-------------------------------------------------------------
Adversarial verdict: INCOMPLETE IN CURRENT HEAD, but the survivor is ALREADY OWNED by
F679 reopening KI#300; do not allocate F741.

The repair correctly classifies localized religion-founding replay text independently
of the current city name by constructing sentinel-derived prefix/suffix templates and a
localized unknown-location replacement. It also intentionally keeps religion founding
globally visible while hiding the historical holy-city name when the plot is hidden or
no city survives.

However the final current test still uses:

    pPlot.getPlotCity() is None

for a valid map plot. Civ4's CyPlot wrapper returns `new CyCity(m_pPlot->getPlotCity())`;
when the underlying CvPlot has no city, Python therefore receives a non-None CyCity
wrapper whose inner CvCity pointer is NULL. The correct nullable-wrapper test is
`.isNone()`. Thus a holy city razed before the observer learns its name, followed by the
plot becoming revealed, has `bPlotHidden == false` and a non-None Python wrapper; the
intended no-city replacement is skipped and the historical replay name can survive.

This exact supported state was already proven in the later CyPlot audit as F679 and
explicitly attached to existing KI#300. Practical 6217 materially improved the bug but
did not close the managed-wrapper branch. No new finding number is consumed here.

Practical 6217 - KI#322 (Python 2.4 / Python 3 AI Personality normalization)
---------------------------------------------------------------------------
Adversarial verdict: CLEAN.

`round_current_runtime` no longer delegates exact half ties to version-dependent built-in
`round`. It truncates toward zero, inspects the signed fractional part, rounds magnitudes
past one half outward, and on exactly +/-0.5 moves outward only when the truncated
integer is odd. This implements ties-to-even for positive and negative values in syntax
compatible with Civ4's Python 2.4 while matching the Python 3 predump contract.

Focused tests cover 52.5, 53.5, -52.5, -53.5 and ordinary fractions; the external
predump workflow invokes those tests before its full comparison. Current shared
normalization routes use this helper rather than a separate built-in-round path. No
cross-runtime sibling survives.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Existing F679 / KI#300 remains current through the CyCity(NULL) Python-wrapper test.
Latest physical checkpoint: C031-WIP830.
Next substantive chronology cursor: practical 6216 / KI#285.
No repository source files modified.

================================================================================
C031-WIP831 - chronology continuation: practical 6216 / KI#285
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP830.
No repository source files modified by this audit continuation.

Practical 6216 - KI#285 (negative map-script minimum-starting-distance modifiers)
---------------------------------------------------------------------------------
Adversarial verdict: CLEAN.

The current native callback extractor clamps only the lower bound:

    return std::max(-100, toInt(lResult));

and its only native consumer applies:

    iRange *= 100 + minStartingDistanceMod();
    iRange /= 100;

This exactly restores the inherited BtS percentage contract that pre-SAS AdvCiv's
refactor accidentally broke: -95 means 5%, -10 means 90%, 0 means 100%, positive values
increase spacing, and values below -100 saturate at a zero multiplier before the ordinary
`MIN_CIV_STARTING_DISTANCE` floor is applied.

A whole-source caller search found no second C++ consumer retaining the old nonnegative
interpretation. Current map scripts continue to return their intended raw modifiers;
there is no separate wrap/team/normalization branch that rescales the callback in a
different unit. Local K-Mod ancestry provides the direct positive control: its original
`startingPlotRange` applies `max(0, lResult + 100)` before dividing by 100, algebraically
the same lower-bound contract now reconstructed by the split AdvCiv caller/extractor.

No new F741 candidate survives.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP831.
Next substantive chronology cursor: practical 6215 / KI#246, KI#256, KI#267 + KI#286.
No repository source files modified.

================================================================================
C031-WIP832 - chronology continuation: practical 6215 / KI#246, KI#256, KI#267 + KI#286
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP831.
No repository source files modified by this audit continuation.

Practical 6215 - KI#246 (SAS Longworld duplicate fallback starts)
----------------------------------------------------------------
Adversarial verdict: CLEAN in the current script.

Longworld now resets both its per-player assignment cache and used-plot set in
`beforeGeneration`. `findStartingPlot` returns a cached result on repeated callbacks,
records every successful nominal tile as used, and makes the trimmed/invalid nominal
fallback reject every tile already assigned earlier in the same generation. A fallback
selection is cached before return as well. Thus callback repetition cannot reshuffle a
player and one player's fallback cannot reuse another player's nominal or fallback start.
The configured-player list is recomputed from actual PlayerTypes rather than compact slot
positions. No later Longworld sizing changes invalidate the uniqueness state.

Practical 6215 - KI#256 (Equal Islands authoritative Ice cloning)
----------------------------------------------------------------
Adversarial verdict: CLEAN.

The region-copy operation now copies source plot type, terrain, feature (including source
Ice or absence of Ice), bonus and improvement in one authoritative direction, then copies
rivers after land state. The removed target-Ice exception was the sole reason independently
generated polar Ice could survive in a destination region or cover a cloned seafood bonus.
The current copy order gives each destination the same feature/bonus pairing as the chosen
source region, matching the map's equality contract.

Practical 6215 - KI#267 (sparse PlayerTypes in handcrafted regional starts)
---------------------------------------------------------------------------
Adversarial verdict: CLEAN for the completed family in current code.

Hub, Ring and Inland Sea now build a shuffled template pool of compact template IDs but
store the result in a dictionary keyed by each actual ever-alive PlayerTypes ID. Their
`findStartingPlot` callbacks reject only genuinely unmapped IDs and dereference the
actual-ID dictionary. Together with the earlier Lagoon, Cross and Team Battleground
repairs, the known family no longer indexes compact alive positions as if they were
PlayerTypes. The number of template IDs equals the ever-alive population, so the
actual-ID loop consumes the same number of shuffled slots without changing compact-ID
RNG population.

Practical 6215 - KI#286 (Wheel >18-player all-player fallback)
-------------------------------------------------------------
Adversarial verdict: CLEAN, including composition with KI#285.

For more than 18 players Wheel sets `bUseDefaultStartPlacement`; current
`findStartingPlot` now checks that flag before any handcrafted region validation and
returns `CvMapGeneratorUtil.findStartingPlot(playerID)` for every player. The same state
already controls Wheel's minimum-distance callback: handcrafted placement returns -95,
whereas default fallback returns -50. With KI#285 now correctly honoring negative
modifiers, the >18 path consistently means default placement for every player with its
intended 50% starting-distance multiplier, rather than a mixed 18-constrained/rest-default
population.

No new F741 candidate survives this four-map bundle.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP832.
Next substantive chronology cursor: practical 6214 / KI#257-KI#261.
No repository source files modified.

================================================================================
C031-WIP833 - chronology continuation: practical 6214 / KI#257-KI#261
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP832.
No repository source files modified by this audit continuation.

Practical 6214 - KI#257 (Planet static CyPlot alias + wrapped river delta)
-------------------------------------------------------------------------
Adversarial verdict: CLEAN.

Planet's feature-spread source is now obtained through managed `CyMap.plot()`, so later
transient `sPlot()` neighbor queries cannot retarget the retained source wrapper. The
river-crossing helper also normalizes dx/dy through the map's wrap settings before
choosing cardinal river direction, closing the X=0 vs X=width-1 seam error and remaining
generic if Y wrapping is ever enabled. Later CyMap/CyPlot wrapper audits explicitly
revisited the remaining Planet `sPlot()` surface and found the surviving calls transient
rather than a second retained-static-wrapper root. No new alias sibling survives.

Practical 6214 - KI#258 (Planet Smart Selection area units)
-----------------------------------------------------------
Adversarial verdict: CLEAN.

Smart Selection now classifies the same percentage-scaled terrain-cell dimensions used
by Planet's grid path and converts their area to plot-space with the inherited 4x4-cell
factor (`*16`) before comparing with plot-area thresholds such as 40x24, 48x28, etc.
This removes both old unit mismatches: unscaled WorldInfo dimensions and raw cell counts.
Spacing-driven grid enlargement occurs after distribution selection and depends on that
selection, so basing the distribution bucket on the common initial scaled grid avoids a
circular selection dependency and matches the documented repair.

Practical 6214 - KI#259 (Planet signed latitude inversion)
----------------------------------------------------------
Adversarial verdict: CLEAN.

Both callbacks derive one absolute polar latitude extent, clamp it to 90, then return
`+extent` for top and `-extent` for bottom. The symmetric climate band is retained while
the callback invariant top >= bottom is guaranteed, allowing AdvCiv True Starts latitude
logic to remain enabled in option combinations that formerly returned reversed signs.
No other current Planet latitude callback bypasses this helper.

Practical 6214 - KI#260 (low percentage grid safety floor)
----------------------------------------------------------
Adversarial verdict: CLEAN.

`getPlanetScaledGridSize` clamps the configured percentage to 1..100 and clamps the
resulting terrain-cell base grid to at least 6x4, the already-supported default Arena
minimum. Later continent-spacing expansion can only increase width and scales height
upward with it, so it cannot undo the safety floor. The final KI#328 integer conversion
then preserves the resulting positive dimensions at the Python->SDK boundary.

Practical 6214 - KI#261 (Planet frontier pruning indexes)
---------------------------------------------------------
Adversarial verdict: CLEAN; a tempting repair-side suspicion was explicitly rejected.

`_getPossibleGrowTiles` records positional indices of exhausted builders while iterating
the sorted `bArray`, then pops those positions from highest to lowest. Importantly,
`bArray.pop(index)` returns its internal `[power, value]` record, where `power` is the
world-offset key and `value` is `[x, y, scatt, scattdir]`. Therefore
`removedBuilder[0]` is the OFFSET stored in `builderList`, not the X coordinate. The
repair correctly removes that same offset from the creation-order FIFO. Highest-to-lowest
popping keeps all remaining recorded positions valid.

This closes both halves of the original defect: the actual exhausted builders are
removed rather than positions 0..N-1, and their creation-order bookkeeping is removed
with the same identity. No F741 candidate survives the Planet bundle.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP833.
Next substantive chronology cursor: practical 6213 / KI#248-KI#253.
No repository source files modified.

================================================================================
C031-WIP834 - chronology continuation: practical 6213 / KI#248-KI#253
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP833.
No repository source files modified by this audit continuation.

Practical 6213 - KI#248 (BTG Grid inverted 9-region rectangle)
-------------------------------------------------------------
Adversarial verdict: CLEAN.

The repaired one-line 9-region template now has contiguous west/east boundaries around
region 5 (`0.444 -> 0.555`, `0.555 -> 0.666`, `0.666 -> 0.777`). A current-source
structural scan of all literal normalized Grid template rectangles found 143 rectangle
records and no west>=east or south>=north rectangle. The documented 0.755 typo was the
only inverted static rectangle in this template family; no later template edit reopens it.

Practical 6213 - KI#249 (truthy bound `isFreshWater` methods)
------------------------------------------------------------
Adversarial verdict: CLEAN.

All nine repaired Grid/Cross/Archipelago/Islands/Equal-Islands sites call
`pPlot.isFreshWater()` rather than testing the bound method object. A current PrivateMaps
scan found no remaining `.isFreshWater` predicate in the same missing-parentheses form.
The corrected boolean feeds both custom-start scoring and the regional-yield/freshwater
classification paths; no neighboring caller keeps the old always-true semantics.

Practical 6213 - KI#250 (Grid Start Distance option index)
---------------------------------------------------------
Adversarial verdict: CLEAN.

Every physical-grid-size branch in current `getGridSize` selects its Normal/Far/Far-Mixed
table from custom option 7, the declared Start Distance option. Empty Land remains option
5 and is used later only for region/player-capacity behavior. No first branch or SAS-size
extension still reads option 5 as distance.

Practical 6213 - KI#251 (untuned supported region-count minimum distance)
------------------------------------------------------------------------
Adversarial verdict: CLEAN.

The legacy tuned `minimums` dictionary remains authoritative when it contains the current
`iNumRegions`; every otherwise-supported count falls back to Cross's conservative
`[0.05, 0.08]` pair. Dynamic KI#253 capacity can therefore introduce 1, 13, 14, 17 or
higher supported counts without a dictionary KeyError. The resulting pixel minima are
still floored to three tiles and then combined with the ordinary player start-range
checks, so the generic fallback does not bypass engine spacing legality.

Practical 6213 - KI#252 (persistent Grid mirroring state)
--------------------------------------------------------
Adversarial verdict: CLEAN.

`beforeGeneration` resets the completion flag plus source/target mirror geometry and all
region-use lists on every generation. `bGridRegionalGenerationCompleted` remains false
through setup and is set true only at the successful end of
`GridMultilayeredFractal.generatePlotsByRegion`; final layer mirroring is gated by both
the explicit mirror option and that flag. A no-player/generic generation path therefore
cannot consume undefined or previous-map geometry from the long-lived Python module.
The documented successive same-session high-player/script tests provide a runtime control
for the lifecycle reset.

Practical 6213 - KI#253 (Grid capacity beyond static 18-player layouts)
----------------------------------------------------------------------
Adversarial verdict: CLEAN, including composition with KI#250-KI#252.

Current `beforeGeneration` derives capacity from the actual ever-alive player count:
one-line uses exactly N regions, two-line rounds N up to an even number, and three-line
uses 1/2 rows for N<3 and otherwise rounds N up to a multiple of three. A structural
check for every supported population 1..48 confirms `iNumRegionsForShape` is divisible by
`iRows`; `buildGridTemplate` therefore always fills the requested capacity with equal,
nondegenerate rectangles. Static tuned templates are retained where available and only
missing capacities use the generated layout.

SAS world-size extrapolation separately keeps the selected 1/2/3-row height fixed while
scaling Huge's width by default-player count, so added capacity becomes horizontal columns
rather than an unintended aspect-ratio reshape. Empty-Land mode may leave one rounded-up
hub unused but never creates fewer physical hubs than actual players. Custom starting
placement checks `len(regions_in_use) >= iActualPlayers` before assigning; the later KI#274
unique-physical-region repair and KI#329 retry hardening compose with this capacity model.
The documented 48-player SAS48 three-line fully mirrored generation (`252x48`) is a direct
runtime high-end control.

No new F741 candidate survives the six-fix BTG Grid bundle.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP834.
Next substantive chronology cursor: practical 6212 / KI#243 + KI#244.
No repository source files modified.

================================================================================
C031-WIP835 - chronology continuation: practical 6212 / KI#243 + KI#244
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP834.
No repository source files modified by this audit continuation.

Practical 6212 - KI#243 (Water temporary river defines leaked/cached process-wide)
--------------------------------------------------------------------------------
Adversarial verdict: CLEAN in current composed code.

`CvMapGenerator::addRivers` now snapshots the three map-script-mutable globals before
calling Python, then reads their active post-callback values for this generation. If
Python fully handled rivers, changed values are restored before the early return. If
Python requested the default implementation, the active values are consumed throughout
the native river pass and restored at the common end. This preserves Water's intended
per-generation dense-river override while preventing either repeated Water generation or
a later different map from inheriting it.

The earlier SAS optimization that made these reads process-static is gone specifically
for the mutable three values. The independent `SAS_MAP_ADVCIV_RIVER_ROUTING_ENABLE`
toggle remains static because it is immutable configuration rather than a map-script
scratch override. A later full CvMapGenerator audit independently searched shipped
PrivateMaps and again found Water.py to be the only current mutator of:

    PLOTS_PER_RIVER_EDGE
    RIVER_SOURCE_MIN_RIVER_RANGE
    RIVER_SOURCE_MIN_SEAWATER_RANGE

so there is no second shipped mutation path omitted by the snapshot/restore contract.
Water's `allowDefaultImpl()` makes the Python-caller result take the native branch, where
the modified values are read before restoration. Runtime Water generation/turn-101
controls and same-process follow-up map tests therefore agree with the source lifetime.

Practical 6212 - KI#244 (Water one-tile lake cleanup wrapped Y incorrectly)
---------------------------------------------------------------------------
Adversarial verdict: CLEAN.

Water explicitly declares `getWrapX() == True` and `getWrapY() == False`. The repaired
cardinal-neighbor scan preserves modulo wrapping only for X; a north/south candidate
outside 0..height-1 is skipped instead of wrapping to the opposite edge. Interior
neighbors and the east/west seam still use the script's intended topology. The helper
reads its own generated `plotTypes` array, so no later CvMap area/cache behavior can
reintroduce the former false opposite-Y adjacency during this cleanup.

No new F741 candidate survives practical 6212.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP835.
Next substantive chronology cursor: practical 6211 / KI#310 + KI#319.
No repository source files modified.

================================================================================
C031-WIP836 - chronology continuation: practical 6211 / KI#310 + KI#319
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP835.
No repository source files modified by this audit continuation.

Practical 6211 - KI#310 (water-heavy empty-production fallback)
--------------------------------------------------------------
Adversarial verdict: CLEAN in current composed code.

Water-heavy AI cities first attempt the configured food, hammer and gold emergency
buildings. If none can be pushed, control now falls through into the same safe emergency
unit-selection fallback used by other empty AI cities instead of ending the turn with no
production. If a water building succeeds, `bEmergencyBuilding` blocks the unit fallback.
The later KI#333 priority change composes conservatively: strategic emergency defense gets
first refusal, then water economy, then the safe unit fallback. Current fallback-unit
selection retains its explicit no-Settler invariant and later military-cap logic. No
water-only branch can again strand a city merely because all three configured buildings
are unavailable/already built.

Practical 6211 - KI#319 (AI_update no-progress follow-up)
--------------------------------------------------------
Adversarial verdict: HISTORICALLY INCOMPLETE AT 6211, CURRENTLY CLOSED BY ALREADY-OWNED
LATER REPAIRS; no new F741.

The 6211 follow-up correctly added head-unit identity and UnitAI role to the original SAS
no-progress signature, preventing a legitimate head conversion/detachment from being
mistaken for a stuck iteration. Later systematic `CvSelectionGroupAI.cpp` auditing proved
four additional independent progress dimensions that the 6211 version still omitted:

    F199 / KI#522  group cargo changed by missionless transport unload
    F201 / KI#524  group membership changed by non-head bombard split
    F202 / KI#525  queued group-attack state intentionally consumed
    F203 / KI#526  non-head member movement spent by synchronous action

Those are existing album roots, not discoveries of this chronology pass. Current source
now snapshots head identity/role/plot/moves, mission-queue length, group size, total cargo,
queued-group-attack state and the sum of all current member moves before allowing the
fallback `MISSION_SKIP`. The later practical-6359 repair-side audit explicitly rechecked
the composed KI#319 signature and found no surviving same-membership movement transfer or
same-length mission replacement that remains ready/nonbusy while all other signature
components stay unchanged.

Thus practical 6211 is valuable ancestry evidence for an incomplete intermediate repair,
but every proved remaining defect is already owned and repaired later. No duplicate F741
is allocated.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP836.
Next substantive chronology cursor: practical 6210 / rejected KI#311 + KI#309.
No repository source files modified.

================================================================================
C031-WIP837 - chronology continuation: practical 6210 / KI#309 + rejected KI#311
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP836.
No repository source files modified by this audit continuation.

Practical 6210 - KI#309 (religion-value power weighting)
-------------------------------------------------------
Adversarial verdict: CLEAN.

`AI_religionValue` iterates each known major player, reads that same player's state
religion and attitude, and now weights the same object by `100 + player.getPower()`.
The removed bloc-power version instead charged a whole team/master-vassal bloc once for
each non-vassal member and skipped vassal players entirely, even though those skipped
players could have their own distinct state religions. The repaired granularity therefore
matches the quantity being classified.

The +100 floor preserves a nonzero diplomatic voice for weak known civilizations, while
the friendly 4/3 modifier remains player-specific just like the attitude query. All live
callers use the result as a player-local religion choice/conversion score; no caller
expects one value per team or master bloc. No later team/vassal rewrite reintroduces the
old duplication.

Practical 6210 - KI#311 (apparent GET_TEAM(PlayerTypes) enum misuse)
-------------------------------------------------------------------
Adversarial verdict: REJECTED FALSE POSITIVE remains correctly rejected.

In the active AI include context, `GET_TEAM` is intentionally overloaded for
`PlayerTypes`; that overload resolves the player's current team through `TEAMID`.
Consequently expressions such as `GET_TEAM(eFromPlayer)` are type-correct and are
semantically equivalent to explicitly caching `TEAMID(eFromPlayer)` for these uses.
Later KI#387/KI#436 audits independently rediscovered and then retracted the same textual
suspicion, reinforcing this as a durable negative-control pattern rather than a defect.

No new F741 candidate survives practical 6210.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP837.
Next substantive chronology cursor: practical 6209 / KI#304 + KI#306.
No repository source files modified.

================================================================================
C031-WIP838 - chronology continuation: practical 6209 / KI#304 + KI#306
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Previous physical checkpoint: C031-WIP837.
No repository source files modified by this audit continuation.

Practical 6209 - KI#304 (Python found-value int pipeline)
-------------------------------------------------------
Adversarial verdict: CLEAN.

The found-value Python bridge now has one consistent `int` contract in declaration and
implementation. Disabled callback returns retain the supported -1 sentinel; enabled
callbacks convert the Python numeric result with the ordinary long-to-int helper and no
longer assert/cast through signed short. Large supported custom found values therefore
reach the already-migrated int AI found-value pipeline without 16-bit wrap. Current
native/UI callers consume int values, and no second short-backed callback/storage seam
survives this bridge.

Practical 6209 - KI#306 (same-turn building-value cache invalidation)
-------------------------------------------------------------------
Adversarial verdict: ORIGINAL MAINTENANCE REPAIR CLEAN, BROADER FAMILY STILL OPEN BUT
ALREADY OWNED AS F008 / reopened KI#306; no new F741.

6209 correctly removes the government-center maintenance ranking cache and scans live
maintenance values for the rare gated candidate. This closes the proved case where an
earlier city grows during sequential city processing and immediately changes empire
maintenance without changing game turn or city count.

The sibling top-production-city cache deliberately remained. Later whole-file auditing
proved that cache has the same insufficient key: it is still keyed only by player, turn
and city count, while an earlier same-turn building completion can change other cities'
base production through `SpecialistYieldChanges`. A later Wonder gate can consequently
consume stale best/second/third production values and cross exact top-city/+5-hammer
thresholds. The current source still contains that cache.

This surviving current defect is already F008 / KI#306 reopened-broadened in the album.
The chronology pass therefore records the incomplete family boundary without allocating
F741 or duplicating the existing root.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED.
Latest physical checkpoint: C031-WIP838.
Next substantive chronology cursor: practical 6208 / KI#305 + KI#307.
No repository source files modified.

================================================================================
C031-WIP839 - chronology continuation: practical 6208 / KI#305 + KI#307
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F740: CONFIRMED.
F741 / provisional KI#1062: UNUSED entering this practical.
Previous physical checkpoint: C031-WIP838.
No repository source files modified by this audit continuation.

Practical 6208 - KI#305 (World Size chart cell-vs-plot labeling)
--------------------------------------------------------------
Adversarial verdict: CLEAN.

The chart now labels `CvWorldInfo.getGridWidth()/getGridHeight()` and their product as
WorldInfo grid CELLS, not playable map tiles/plots. Derived standard/largest ratios and
per-default-player values are consistently renamed and still operate on those same raw
XML units. This is the only honest static quantity because ordinary generation doubles
both dimensions while custom map scripts can replace the grid-size mapping entirely.
No gameplay or map-sizing calculation consumes these display fields.

F741 / provisional KI#1062 -- CONFIRMED
---------------------------------------
TITLE
    KI#307's Main Interface live-language refresh omitted four other persistent
    translated labels, leaving waiting/end-turn and Field-of-View text stale.

STATUS
    CONFIRMED current Main Interface language-cache family defect.
    Three instances are AdvCiv-SAS cache regressions introduced by the same optimization
    family; one Field-of-View instance is inherited from K-Mod/Base AdvCiv. KI#307's
    repair exposes the common missing invalidation boundary.

ROOT / CURRENT SOURCE
    KI#307 added `updateCachedLocalizedText()` and calls it from every `updateScreen()`.
    That helper refreshes ten translated fields only when `CyGame().getCurrentLanguage()`
    changes. A complete current `initState()` scan finds exactly four remaining
    `localText.getText` assignments outside that helper:

        self.szTextWaiting = ... localText.getText("SYSTEM_WAITING", ()) ...
        self.szTextEndTurn = localText.getText("SYSTEM_END_TURN", ())
        self.szTextWaitingForYou = ... localText.getText("SYSTEM_WAITING_FOR_YOU", ()) ...
        self.sFieldOfView_Text = localText.getText(
            "TXT_KEY_BUG_OPT_MAININTERFACE__FIELDOFVIEW_TEXT", ())

    All four are persistent object fields and are reused later without a fresh lookup.
    `updateScreen()` does not rerun `initState()` on a live language switch; that is the
    exact lifecycle KI#307 was introduced to repair.

BEHAVIORAL PROOF
    The three turn-state strings are selected directly from their cached fields when the
    interface displays Waiting, End Turn or Waiting for You. After changing language,
    the screen refresh continues but these strings remain in the language active when
    `initState()` last ran.

    The FoV slider similarly formats every redraw from `self.sFieldOfView_Text`; its XML
    contains explicit English/German/Italian/Spanish translations (e.g. German
    `Sichtfeld`), so the stale-language consequence is directly data-backed when the
    Field-of-View control is shown.

ANCESTRY / PROVENANCE
    Base AdvCiv/K-Mod perform live `localText.getText` calls for the three turn-state
    messages; SAS practical 5360 moved them into persistent `initState()` fields as a
    performance optimization. Thus those three are SAS cache-invalidation regressions.

    `sFieldOfView_Text` was already cached at initialization in K-Mod and remains so in
    Base AdvCiv 1.14, making that instance inherited. They are kept under one finding
    because the violated current contract and repair boundary are identical: all
    persistent translated Main Interface cache fields must participate in the same
    language-ID invalidation helper.

DEDUP / SCOPE
    Whole-album searches for these exact fields/keys found no earlier finding owner.
    Existing KI#307 documents only Strength/Air Strength/Movement/Range/Level/Experience,
    Starving/Stagnant/Maintenance and the dead-civilization tag; none of the four fields
    above are refreshed there.

    This is low-priority because AdvCiv-SAS officially targets English, matching KI#307's
    existing priority assessment. It is nevertheless a supported live-language-switch
    correctness regression under the same tested UI feature.

REPAIR DIRECTION
    Move/recompute these four translated fields inside `updateCachedLocalizedText()`
    alongside the existing ten. Preserve the SAS font-tag wrappers around Waiting and
    Waiting for You. No per-frame translation lookup is needed; the existing language-ID
    guard already provides the intended cache performance.

Practical 6208 - KI#307 original subset
---------------------------------------
Adversarial verdict: original ten-field repair itself is correct; F741 is the omitted
same-screen translated-cache family above.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F741: CONFIRMED.
F742 / provisional KI#1063: UNUSED.
Latest physical checkpoint: C031-WIP839.
Next substantive chronology cursor: practical 6207 / KI#303 + KI#308 + KI#308.2.
No repository source files modified.

================================================================================
C031-WIP840 - chronology continuation: practical 6207 / KI#303 + KI#308 + KI#308.2
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F741: CONFIRMED.
F742 / provisional KI#1063: UNUSED entering this practical.
Previous physical checkpoint: C031-WIP839.
No repository source files modified by this audit continuation.

Practical 6207 - KI#303 (Specialist Extra Yields building-wide commerce)
-----------------------------------------------------------------------
Adversarial verdict: CLEAN.

The new CvBuildingInfo Python exposure is a direct wrapper around the existing typed
`SpecialistExtraCommerce` Commerce enum map; current SevoPediaSpecialist iterates exactly
`CommerceTypes.NUM_COMMERCE_TYPES`, so it does not introduce an index/domain mismatch.
The page now combines the already-shown per-specialist Building
`SpecialistYieldChange(self.iSpecialist, yield)` with Building
`SpecialistExtraCommerce(commerce)`, which is intentionally building-wide: native
`CvPlayer::processBuilding` folds that value into the owning player's specialist-extra
commerce and `CvCity::getBaseCommerceRateTimes100` applies the resulting player value to
all specialist population plus settled Great People. Civic and Tech extra-commerce rows
use the same player-wide semantics. No duplicate storage or alternate building-specialist
commerce source remains omitted from this panel after the repair.

Practical 6207 - KI#308 (Specialist Breakdown exact GP modifier)
----------------------------------------------------------------
Adversarial verdict: CLEAN.

`CvCity::getGreatPeopleRate()` is exactly

    getBaseGreatPeopleRate() * getTotalGreatPeopleRateModifier() / 100

outside disorder. The exposed `getTotalGreatPeopleRateModifier()` includes the city
modifier, player modifier, state-religion modifier and Golden-Age modifier and clamps the
combined multiplier at zero. Using it directly therefore fixes the old non-invertible
reconstruction from the integer-truncated final rate. The changed `%+d%%` rendering also
correctly represents negative modifiers rather than hardcoding a plus sign.

The raw GP decomposition is consistent with current native base-GPP producers: buildings
and specialists are the normal sources of `changeBaseGreatPeopleRate`, and the existing
KI#101 policy intentionally includes obsolete-building GPP because that GPP remains in
the native base total. No second modifier reconstruction survives in this widget.

F742 / provisional KI#1063 -- CONFIRMED
---------------------------------------
TITLE
    City Screen Culture Breakdown still omits ordinary slider/process culture sources,
    so KI#308.2 can display an exact modifier beside an incomplete base decomposition.

STATUS
    CONFIRMED current AdvCiv-SAS City Screen display defect adjacent to KI#308.2.
    KI#308.2 fixes the modifier arithmetic itself; this finding is the broader source-
    accounting contract of the same Culture Breakdown.

ROOT / CURRENT SOURCE
    The current Culture Breakdown constructs row 1 from only:

        religion commerce
        corporation commerce
        building commerce
        trait free-city commerce
        specialist commerce + player SpecialistExtraCommerce

    Native `CvCity::getBaseCommerceRateTimes100(COMMERCE_CULTURE)` additionally starts
    with:

        getCommerceFromPercent(COMMERCE_CULTURE,
                getYieldRate(YIELD_COMMERCE) * 100)

    i.e. the ordinary Culture commerce-slider contribution. The Python breakdown never
    reads or displays that source.

    Native `CvCity::updateCommerce` then computes the final rate as:

        baseCulture * getTotalCommerceRateModifier(CULTURE) / 100
        + productionYield * getProductionToCommerceModifier(CULTURE)

    while the row-1 decomposition has no production-to-Culture/process term either.
    Current shipped XML has `PROCESS_CULTURE` at Drama with a 50% production-to-Culture
    modifier, so this is a live ordinary source rather than a dormant engine feature.

    Finally, under GAMEOPTION_NO_ESPIONAGE,
    `CvCity::getCommerceRateTimes100(COMMERCE_CULTURE)` adds the city's already-computed
    Espionage commerce rate to the returned Culture rate. The breakdown row and the
    displayed Culture modifier remain Culture-only, so the final row can contain a third
    unrepresented source in that supported option.

BEHAVIORAL PROOF
    After Drama, a city with positive commerce yield and any nonzero Culture slider can
    have positive base/final Culture even when every currently displayed row-1 source is
    zero. For example, commerce yield routed to Culture contributes through
    `getCommerceFromPercent`; row 2 reads the positive exact
    `getCommerceRateTimes100(CULTURE)`, but row 1 still shows zero for religion,
    corporation, building, trait and specialist categories.

    Likewise, a city running the shipped Build Culture process receives
    `productionYield * 50%` Culture after the normal commerce modifier. Row 2 includes
    that output while row 1 has no process/production source. Thus even after KI#308.2's
    exact modifier fix, the visible source arithmetic need not reconcile with the exact
    total.

ANCESTRY / PROVENANCE
    This is SAS-specific to the custom Culture Breakdown introduced with the Main
    Interface city-screen rework. Practical 6207 did not create the missing-source
    decomposition; it replaced only the old inferred modifier with
    `getTotalCommerceRateModifier(CULTURE) - 100`, exposing that the modifier is now exact
    while the base-source list remains partial.

DEDUP / SCOPE
    Whole-album searches for Culture Breakdown, slider culture, Build Culture/process
    conversion and No-Espionage culture found no earlier finding that owns this UI source
    decomposition. Existing Culture-process findings concern AI choice, order metadata,
    production-cache invalidation or GameRecord diagnostics, not this City Screen row.
    KI#308.2 owns only the truncated-base-rate modifier reconstruction.

REPAIR DIRECTION
    Prefer deriving row 1 from the same native source partition as
    `getBaseCommerceRateTimes100`, rather than maintaining an informal partial sum.
    At minimum add the Culture-slider commerce term and an explicit production-to-Culture
    process term, with a clear policy for GAMEOPTION_NO_ESPIONAGE's transferred Espionage
    rate. A stronger UI contract is to make the displayed components mathematically
    reconcile with the displayed final rate (or label the row explicitly as selected
    sources rather than a breakdown).

Practical 6207 - KI#308.2 original modifier repair
--------------------------------------------------
Adversarial verdict: exact-modifier repair CLEAN; broader Culture Breakdown source
accounting remains defective as F742 above.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP840.
Next substantive chronology cursor: practical 6206.
No repository source files modified.

================================================================================
C031-WIP841 - chronology continuation: practical 6206 / KI#299 + KI#301 + KI#302
================================================================================

Status entering this checkpoint
-------------------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED entering this practical.
Previous physical checkpoint: C031-WIP840.
No repository source files modified by this audit continuation.

Practical 6206 - KI#299 (Movie-to-Music No-Movies restoration)
--------------------------------------------------------------
Adversarial verdict: CLEAN.

The repair centralizes exactly the saved-state transition that must occur when leaving
Movie mode. Ordinary Movie close and Movie-to-Music both restore the user's saved
`GRAPHICOPTION_NO_MOVIES` value and clear the saved sentinel; Movie replay/Movie-to-Movie
intentionally retain the temporary override because Movie mode remains active. The Music
transition checks for a playable Music target before restoration, so a failed transition
also leaves the still-open Movie state coherent. No second supported exit path bypassing
this ownership handoff was established.

Practical 6206 - KI#301 (standard-Pedia Build namespace)
--------------------------------------------------------
Adversarial verdict: CLEAN.

The repaired compatibility branch now resolves the supplied valid `BuildTypes` through
`CvBuildInfo::getImprovement()` before passing an ID to the standard Civilopedia's
Improvement page. Improvement-producing Builds therefore cross the enum boundary
explicitly; route/feature-removal Builds with `NO_IMPROVEMENT` no longer reinterpret a
Build ID as an unrelated Improvement ID. Current shipped callers supply real BuildTypes
IDs, so no additional bounds/sentinel defect was found in this redirect.

Practical 6206 - KI#302 (Terrain Units / Improvement predicate)
---------------------------------------------------------------
Adversarial verdict: CLEAN for the documented contract.

The Units (Any Build) panel and Improvements panel now share the same
`isImprovementValidForTerrain` predicate. Hill and water were the documented mismatch:
Hill's synthetic plot-type page needs the existing KI#153 reconstruction across
underlying terrain/feature/bonus rules, while Coast/Ocean need the Improvement `isWater`
route. Both panels now consume those same rules, so an Improvement displayed for Hill or
water no longer loses its capable Worker/Workboat solely because the Units panel used the
old narrower direct-terrain test.

A broader candidate was challenged and rejected rather than promoted: ordinary terrain
pages historically use direct `TerrainMakesValid` semantics, while the special Hill page
must reconstruct unknown underlying state because Hill is not a real terrain layer.
Resource-only Improvements such as Pasture/Plantation therefore do not by themselves
prove a duplicate-predicate regression on ordinary terrain pages. Expanding that page to
'all improvements possible on some bonus/feature that can occur here' would be a UI scope
change unless a separate intended contract or runtime mismatch is established.

Checkpoint state
----------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP841.
Next substantive chronology cursor: practical 6205.
No repository source files modified.

================================================================================
C031-WIP842 APPEND -- practical 6205 repair audit (KI#318 + KI#320)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP842.
No repository source files modified.

PRACTICAL 6205 / KI#318 -- CLEAN
--------------------------------
Current source and ancestry confirm the zero-range WonderConstructRand repair is correctly
scoped. Practical 5092 had changed three opportunistic-Wonder branches from unguarded
SyncRandNum(getWonderConstructRand()) to an iWC > 0 guard in order to suppress invalid
negative ranges. That also suppressed valid zero ranges even though all three branches have
a fixed opportunity term outside the random contribution:

  * opportunistic wonder build 1: random / 5 + 7 turns;
  * opportunistic wonder build 2: random / 5 + 8 turns;
  * later production-rank branch: base probability 8 + random.

CvRandom::getInt(int) explicitly returns 0 for iNum <= 0, asserting only the negative case,
and returns before the underlying RNG is called. Therefore iWC == 0 is a deterministic zero
contribution and does not advance the synchronized RNG. Current shipped leader XML contains
real zero-valued playable leaders (including Montezuma and Sitting Bull), so the regression
is reachable without relying on historical negative/default data.

The remaining AI_bestBuildingThreshold iWC > 0 guard is intentionally different: that site
has no fixed term outside the random addition, so a zero range would contribute exactly zero
and skipping it is behaviorally equivalent. No second fixed-base branch remains under a >0
guard.

Verdict: KI#318 survives current-source, XML-reachability and RNG-stream review. No F743.

PRACTICAL 6205 / KI#320 -- CLEAN
--------------------------------
KI#40's high-happiness city-tile policy already contained both halves of one policy: multiply
food value by spare happiness and, from three surplus happiness upward, divide hammer value
by (surplus - 1), excluding food-production cities. The hammer divisor had simply been
placed after iProductionValue was normalized and added to iValue, making that local write
dead.

Current practical 6205 moves the existing divisor before the production normalization/add:

  iHappySurplus = happyLevel() - unhappyLevel(0)
  if (iHappySurplus >= 3 && !isFoodProduction())
      iProductionValue /= (iHappySurplus - 1)

The immediately following food block consumes the same iHappySurplus and has the same
food-production exception, so the moved calculation matches the documented paired policy
rather than introducing a new happiness definition. Production-emphasis scaling remains
before the divisor and ordinary production/base/special multipliers remain after it; the
change is therefore solely to make the pre-existing hammer de-emphasis participate in the
value that AI_yieldValue returns.

AI_yieldValue has multiple citizen/plot/Worker valuation callers, but this policy already
lived inside the shared function and was intended to affect those evaluations; practical
6205 does not broaden its caller population. Later exhaustive AI_yieldValue audits found and
numbered separate temporary-anger prediction defects but did not identify another surviving
order/sentinel defect in this high-happiness hammer calculation.

Verdict: KI#320 survives current-source and later-composition review. No F743.

CHRONOLOGY HANDOFF
------------------
Practical 6205 is closed clean. F743 / provisional KI#1064 remains unused.
Next substantive chronology cursor: practical 6204.

================================================================================
C031-WIP843 APPEND -- practical 6204 repair audit (KI#326)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP843.
No repository source files modified.

PRACTICAL 6204 / KI#326 -- CLEAN (DIAGNOSTIC-ONLY)
--------------------------------------------------
K-Mod's attitude-cache OOS diagnostic mixed a signed attitude value through `value << rivalID`.
SAS supports 48 civilization-player slots, so IDs 32-47 exceeded a 32-bit shift width, while
negative signed attitudes made the inherited expression nonportable even at lower IDs.

The current repair accumulates into `uint uiAttitudeHash`: each rival's signed attitude is
converted to unsigned, added, and multiplied by the unsigned constant 31u. Conversion from a
negative signed integer to unsigned and unsigned overflow are both defined modulo 2^32; no
invalid shift remains. Iterating rival IDs in order makes the polynomial recurrence
position-sensitive, while the inherited self-attitude exclusion is preserved. The resulting
hash is masked with MAX_INT before conversion back to int and folded into the pre-existing
checksum multiplier.

The checksum remains deliberately lossy and is diagnostic only. Hash collisions are not a
new simulation correctness contract, and this repair neither changes AI/game state nor hides
an identified game-state mismatch. No second high-player signed shift survives in this case.

Verdict: KI#326 is a correct diagnostic robustness cleanup. No F743.

CHRONOLOGY HANDOFF
------------------
Practical 6204 is closed clean. F743 / provisional KI#1064 remains unused.
Next substantive chronology cursor: practical 6203 / KI#315-KI#317.

================================================================================
C031-WIP844 APPEND -- practical 6203 repair audit (KI#315-KI#317)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP844.
No repository source files modified.

PRACTICAL 6203 / KI#315 -- CLEAN IN ITS DOCUMENTED DIMENSION
------------------------------------------------------------
AI_civicValue compares absolute civic scores. The generalized unlimited-specialist block
estimates an intrinsic positive benefit from the specialist types the candidate civic makes
unlimited. Multiplying that whole intrinsic benefit by iS made the same feature positive
when evaluating adoption and negative once the civic was active. Removing that iS is the
correct direction contract.

The later deep AI_civicValue saturation independently rechecked direct intrinsic effects and
explicitly confirmed that not every block should use iS: iS is needed only where a block
starts from current state and models a directional hypothetical transition. The unlimited-
specialist output is an absolute candidate property. Current anarchy handling remains a
rough heuristic but was separately reviewed and is not another sign failure.

Separate current/known defects inside the same generalized estimator (notably shared finite
specialist capacity being reused across multiple unlocked specialist types) are already
owned by earlier album roots such as F324/KI#647 and must not be duplicated here.

PRACTICAL 6203 / KI#316 -- CLEAN
--------------------------------
The ordinary 100% Great Person multiplier is the baseline multiplier for positive specialist
GPP. Subtracting 100 made all base GPP worth zero at the normal multiplier and valued only
bonus multiplier above baseline. The repaired expression uses the full nonnegative average
GP multiplier and leaves the existing output conversion/tuning unchanged.

Later AI_civicValue saturation explicitly rechecked the same block and found no independent
baseline-GPP arithmetic sibling beyond already-owned surrounding estimator defects.

PRACTICAL 6203 / KI#317 -- CITY-SELECTION REPAIR CORRECT; LATER DIRECTION SIBLING ALREADY OWNED
------------------------------------------------------------------------------------------------
The practical-6203 repair correctly replaced the pre-change `currently happy => skip` gate
with a before/after angry-citizen comparison. This admits a currently happy city when the
hypothetical anger change would actually cross its happiness cap while still ignoring cities
whose actual angry population remains unchanged.

However, the later deep CvPlayerAI audit proved an independent direction-normalization bug in
the same CivicPercentAnger block: the hypothetical welfare change was added directly to the
candidate civic's absolute score, so the harm of DROPPING an active pressure-preventing civic
could lower the score of KEEPING it. That defect is already F325 / KI#648, not F743. Current
source contains the later correction (`-12 * iAngryCitizenChange * iS`).

Thus KI#317's documented city-crossing/filter repair survives; the historical practical was
not the final repair of the broader family, but the remaining sign defect is already
independently numbered and repaired in current composition.

WHOLE-BLOCK DEDUP CONTROL
-------------------------
The later exhaustive AI_civicValue pass already saturated this area and owns additional
independent roots (e.g. F324/KI#647 shared specialist capacity and F326/KI#649 ordinary
commerce-modifier normalization). No fresh F743 candidate survives practical 6203.

CHRONOLOGY HANDOFF
------------------
Practical 6203 is closed as a mixed historical/current-composition boundary. F743 / provisional
KI#1064 remains unused.
Next substantive chronology cursor: practical 6202.

================================================================================
C031-WIP845 APPEND -- practical 6202 repair audit (KI#323 + rejected KI#324 + KI#324.2)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP845.
No repository source files modified.

PRACTICAL 6202 / KI#323 -- CLEAN
--------------------------------
The RFC/DoC-derived Unit Chart previously displayed only guaranteed first strikes. The repair
reads both UnitInfo.getFirstStrikes() and getChanceFirstStrikes(), showing guaranteed count
alone when no chance strikes exist and a minimum-to-maximum range when chance strikes do.
This matches the DLL's ordinary unit-help interpretation of guaranteed plus chance first
strikes. Chance-only units therefore correctly display e.g. 0-1 instead of blank.

PRACTICAL 6202 / KI#324 -- REJECTION REMAINS CORRECT
-----------------------------------------------------
The display of zero-base collateral rows with nonzero limit/max is intentional SAS capability
metadata, not false UI. Hiding those rows would conceal promotion-enabled capacity.

PRACTICAL 6202 / KI#324.2 -- CLEAN
-----------------------------------
The inherited CvUnitInfo::isPromotionValid veto rejected every promotion with a collateral-
damage change whenever the unit's BASE collateral damage was zero. SAS had broadened the
collateral-promotion design and configured eligible military units with nonzero collateral
limit/max, so retaining that base-damage prerequisite made the advertised progression
unobtainable.

Current runtime combat supports the repaired contract. CvUnit::collateralCombat computes base
collateral strength, but its early return is explicitly:

  if (iCollateralStrength <= 0 && getExtraCollateralDamage() <= 0) return;

Thus a positive promotion-derived extra-collateral value deliberately keeps a zero-base unit
in the collateral path. The later damage formula remains functional and is capped by the
unit's nonzero collateralDamageLimit(); collateralDamageMaxUnits() controls the target count.
Promotion application updates m_iExtraCollateralDamage through the existing promotion state.

The repair removes only the base-damage veto. Nonzero limit and max-unit guards remain, as do
UnitCombat eligibility, promotion prerequisites and the separate only-defense rejection.
Consequently it enables exactly the SAS-configured promotion path rather than collateral on
arbitrary civilians/unsupported units.

No new F743 survives this bundle.

CHRONOLOGY HANDOFF
------------------
Practical 6202 is closed clean (with KI#324 retained as a rejected finding). F743 / provisional
KI#1064 remains unused.
Next substantive chronology cursor: practical 6201.

================================================================================
C031-WIP846 APPEND -- chronology controls 6201-6198 + practical 6197 map-script repair audit
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP846.
No repository source files modified.

CHRONOLOGY CONTROLS 6201-6198
-----------------------------
Practicals 6201, 6200, 6199 and 6198 are documentation/release/binary-only chronology
controls in the reachable-history map. They introduce no independently auditable runtime
source repair, so this pass does not manufacture a gameplay finding from them.

PRACTICAL 6197 / KI#297 -- CLEAN
--------------------------------
Highlands.beforeInit() assigns the randomized north/south climate orientation to the module-
level shiftMultiplier. The missing `global shiftMultiplier` made those assignments local and
left the safe module default (0.0) permanently active. Current code declares the global before
assignment; getTopLatitude(), getBottomLatitude() and terrain-generation latitude calculations
all read that same module state. No second orientation variable or stale per-generation cache
survives.

PRACTICAL 6197 / KI#298 + KI#298.2 -- CLEAN
-------------------------------------------
Highlands now restores its inherited land-heavy base grid geometry (with Arena below Duel)
and feeds the table through the already-audited SAS world-size calibration helper. This removes
the much smaller shared compact profile that could leave small tiers without viable starts,
while preserving deterministic Huge-relative SAS24/32/40/48 scaling. No separate Highlands
consumer retains the old compact dimensions.

PRACTICAL 6197 / KI#295 -- CLEAN
--------------------------------
Inland Sea's inherited high-player templates include centers near 0.05/0.95. Current code
first clamps the template center into the two-plot safety envelope and only then expands and
clips the configured variability window. Therefore west<=center<=east and
south<=center<=north whenever the callback is used; the old small-grid inverted-window state
cannot be reconstructed by the repaired path. Player-template lookup is also keyed by actual
PlayerTypes by the separate KI#267 repair.

PRACTICAL 6197 / KI#294 -- CLEAN
--------------------------------
Mirror's special two-even-team path no longer assumes the participating TeamTypes are literal
0 and 1. It derives the two actual IDs from shuffled ever-alive players, partitions those same
players by the derived IDs and requires equal team membership before entering the mirror path.
Sparse/nonzero team IDs therefore no longer route players to the wrong side.

PRACTICAL 6197 / KI#294.2 -- CLEAN
-----------------------------------
Mirror restores its inherited land-heavy grid profile and adds Arena/SAS calibration. More
importantly, the two-team Start Together assignment now uses one shared primary/opposite loop.
Each primary start search is checked for `startPlot < 0`, each computed mirrored coordinate is
validated with map.isPlot(), and any failure clears every start provisionally assigned by this
special pass before delegating the *complete* default implementation.

The four inherited option transforms remain correct for the primary-left-side predicate:
Reflection/Inversion use x -> width-x-1; Copy/Opposite use x -> x+width/2, with the appropriate
y preservation/reflection. isValidForMirror keeps primary x within the left-side band, and the
new explicit map.isPlot() check protects the counterpart in all cases.

F740/KI#1061 control: the rollback does not create the DLL fallback's partial-preassigned-team
bug. In ordinary generated Mirror games the special path begins without externally pre-marked
partial starts; on failure it clears only its own provisional pairs and delegates a complete
assignment. A scenario/external partial-start state can still reach the separately owned F740
fallback defect, but that is neither introduced nor hidden by KI#294.2.

The older Start Anywhere branch still trusts findStartingPlot without its own negative-result
check. That is outside the KI#294.2 special-path repair; after the restored viable grid sizes no
supported deterministic failure was proved here, so it is retained only as a robustness watch,
not promoted to F743.

PRACTICAL 6197 / KI#293 + KI#293.2 -- CLEAN
-------------------------------------------
Oasis restores an inherited viable grid profile (plus Arena/SAS calibration) and hardens its
custom Nile-style river bounds. The outer loop and direction-specific y increments preserve
y+1 headroom; center and lateral shift are clamped so west/east probes remain within 0..W-1;
small heights force a positive vertical random range; and horizontal range is at least one.
`CvRandom.get(0)`-style zero ranges used for segment variation are valid deterministic draws in
this engine and do not reopen the boundary fault.

WHOLE-BUNDLE RESULT
-------------------
The eight 6197 repairs compose cleanly with the current map-generation and starting-location
code. The principal adversarial interaction with later F740 was explicitly rejected rather
than silently assumed. No unowned current-tree root survives this practical.

CHRONOLOGY HANDOFF
------------------
Practical 6197 is closed clean. F743 / provisional KI#1064 remains unused.
Next substantive chronology cursor: practical 6196 / KI#292.

================================================================================
C031-WIP847 APPEND -- practical 6196 repair audit (KI#292)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F742: CONFIRMED.
F743 / provisional KI#1064: UNUSED.
Latest physical checkpoint: C031-WIP847.
No repository source files modified.

PRACTICAL 6196 / KI#292 -- CLEAN
--------------------------------
Hub.py, Ring.py and Wheel.py shared the same inherited nine-player Buffered template typo:
the upper-middle region ended at longitude 0.677 while the adjacent buffer began at 0.667.
The corresponding lower region already ended at 0.667, making the intended symmetric boundary
clear. Current source changes all three copies to 0.667.

The actual float-to-grid conversion proves the repaired separation exactly. Region east bounds
are calculated as:

  east = int((width - 1) * eastFraction) - 1

while neighboring buffer west bounds are:

  west = int((width - 1) * westFraction)

With both fractions equal to 0.667, regionEast == bufferWest - 1 by construction for every
map width; integer rounding cannot recreate an overlap. With the old 0.677 east fraction,
wide enough maps can round the region beyond the 0.667 buffer start and generate both region
land and intended separation buffer on shared columns.

All three scripts use the same table semantics and conversion formulas. Their later high-player
fallback and world-size repairs do not modify the nine-player Buffered geometry. No second
script-specific boundary or stale copy survives current source.

CHRONOLOGY HANDOFF
------------------
Practical 6196 is closed clean. F743 / provisional KI#1064 remains unused.
Next substantive chronology cursor: practical 6195 / KI#267 + KI#288-KI#291.

================================================================================
C031-WIP848 APPEND -- practical 6195 Team Battleground audit; F743 / KI#1064
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP848.
No repository source files modified.

PRACTICAL 6195 / KI#288 -- CLEAN
--------------------------------
Team Battleground's Four Corners bridge offset and Donut hole radius are scalar geometry,
whereas the SAS calibrated-grid helper expects a `(width,height)` tuple. Current source gives
both scalars explicit Arena-through-SAS48 tables and uses the ordinary scalar world-size
lookup. Plot and terrain generation share the same Donut radius table, preventing the two
passes from drifting. No SAS-tier missing-key/unpack path survives.

PRACTICAL 6195 / KI#289 -- CLEAN FOR FRESH-GAME POPULATION
-----------------------------------------------------------
Round/Donut Start Separated now preserves randomized membership within teams but interleaves
one member from each team per circular circuit; Start Anywhere remains random and Start
Together remains grouped. The ordinary-shape validator likewise keeps >4-team games in the
Start Separated path and cycles successive same-team starts through four quarters instead of
falling through to unrestricted placement. The three proximity modes are therefore no longer
collapsed in the documented fresh-game state.

PRACTICAL 6195 / KI#290 -- CLEAN
--------------------------------
Circular starts now derive both phase and every player angle from one floating
`360.0 / player_num` step. The RNG call count/range is unchanged, but Python-2 truncation no
longer pushes the discarded angular remainder into the closing gap.

PRACTICAL 6195 / KI#291 -- CLEAN FOR ITS DOCUMENTED CAPACITY ROOT
-----------------------------------------------------------------
The full rounded circle is precomputed into `(player, plotNum)` pairs before any start is
written. A duplicate coordinate sets the per-assignment `bUseDefaultCircleStarts` latch and
causes every later findStartingPlot call plus normalization to delegate to the engine. Thus a
small high-player circle no longer writes duplicate/partial handcrafted starts. The latch is
reset at the beginning of each assignment pass.

PRACTICAL 6195 / KI#267 -- FRESH-GAME SPARSE-ID REPAIR CORRECT
---------------------------------------------------------------
The circle shuffle now enumerates actual ever-alive PlayerTypes rather than compact
`0..count-1` IDs, fixing the documented fresh-game sparse-slot omission/dead-slot substitution
when all participating ever-alive players are also alive, as they are during ordinary map
creation.

F743 / KI#1064 -- CONFIRMED CURRENT TEAM BATTLEGROUND REGENERATION POPULATION MISMATCH
--------------------------------------------------------------------------------------
The current script mixes EVER_ALIVE and ALIVE populations in state that is reused by
starting-plot assignment:

* beforeGeneration() builds `team_num` by compacting every `team.isEverAlive()` team;
* assignStartingPlots() sizes `assignedPlayers` to `countCivPlayersAlive()`;
* the non-circle findStartingPlot path indexes that alive-sized array with
  `team_num[thisTeamID]`;
* Round/Donut sets `player_num = countCivPlayersAlive()` but KI#267's repaired shuffle now
  enumerates every `player.isEverAlive()` PlayerTypes and then consumes only `player_num`
  sorted entries.

This mismatch is reachable in supported current engine lifecycle, not only a hypothetical
scenario. CvGame::regenerateMap is explicitly DLL-exported for WorldBuilder. It clears every
player starting plot and reruns `setInitialItems() -> assignStartingPlots()` but never revives
eliminated players. Eliminated teams/players therefore remain EVER_ALIVE while no longer ALIVE
when the map script is invoked again.

Deterministic ordinary-shape witness: suppose two players are alive but an eliminated
EVER_ALIVE team occurs before one surviving team in the compact team order. `assignedPlayers`
has length 2, while that survivor can receive `team_num == 2`; findStartingPlot executes
`assignedPlayers[2] += 1`, producing a Python out-of-range failure. More generally, any
EVER_ALIVE-team holes make the compact team index semantically incompatible with an ALIVE-player
count-sized array.

Round/Donut has a sibling manifestation: the shuffle array can contain dead ever-alive players
while `player_num` is only the alive count. Sorting/randomization can place a dead player among
the first `player_num` entries, giving that dead slot a handcrafted circle start and omitting a
live player. The subsequent circle-specific findStartingPlot path assumes the live player
already has such a start.

This is distinct from F740/KI#1061. F740 is the DLL's team fallback failing when the first
teammate already owns a pre-marked start. F743 is Team Battleground's own ALIVE/EVER_ALIVE
population mismatch during established-game map regeneration and can fail before or outside
that fallback family.

Repair direction should use one coherent participating population for all per-player/per-team
assignment structures during a generation pass -- practically ALIVE players/teams for starts,
with actual PlayerTypes/TeamTypes retained rather than compact-ID assumptions. If historical
EVER_ALIVE membership is needed elsewhere, it should not index ALIVE-sized assignment state.

WHOLE-BUNDLE RESULT
-------------------
KI#267 and KI#288-KI#291 remain valid repairs for their documented fresh-game defects. The
adversarial lifecycle pass adds one independently reachable current-tree root, F743/KI#1064,
through WorldBuilder regeneration after elimination.

CHRONOLOGY HANDOFF
------------------
Practical 6195 is closed with F743 / KI#1064 confirmed.
F744 / provisional KI#1065 remains unused.
Next substantive chronology cursor: practical 6194 / KI#283-KI#284.2 + KI#287.

================================================================================
C031-WIP849 APPEND -- practical 6194 repair audit (KI#283-KI#284.2 + KI#287)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP849.
No repository source files modified.

PRACTICAL 6194 / KI#283 -- CLEAN
--------------------------------
Donut builds its plot disk and its special terrain around the same fixed center coordinates.
Generic FractalWorld seam shifting could rotate plot types on wrapped maps while the later
terrain pass remained fixed, so disabling plot shifting for this handcrafted/ocean-margined
script restores their coordinate identity. On flat maps the generic shifter had no wrap seam
to choose anyway; on Cylindrical/Toroidal maps the disk's ocean boundary makes the shift
unnecessary. No second terrain-shift state survives.

PRACTICAL 6194 / KI#284 -- CLEAN
--------------------------------
Both Y seam kernels allocate/score by map or region HEIGHT, so their too-large-strip guards now
compare `2*stripRadius` with height rather than width, matching the symmetric X/width routines.
The strict `>` relation is correct: when strip size equals height, each source row participates
exactly once around the complete wrapped kernel; only sizes larger than height necessarily
repeat rows through modulo wrapping.

A nearby inherited line in shiftRegionPlots is suspicious:

  stripRadius = min(15, iStrip)
  stripRadius = max(3, iStrip)

The second assignment discards the apparent upper clamp. Repository-wide caller enumeration,
however, found every shipped caller passes iStrip <= 15 (typically 3, 5, 9 or 15), so the typo
is behaviorally dormant in current shipped code and is retained as a robustness watch rather
than promoted to F744.

PRACTICAL 6194 / KI#284.2 -- CLEAN
-----------------------------------
TerrainGenerator.processCustomizations() and FeatureGenerator.addFeatures() are inherited base
methods whose subclasses are allowed to replace __init__. Reading once-per-generation SAS
defines through `self.gc` therefore added an undeclared base-constructor dependency. Current
code reads those two global defines through a local CyGlobalContext(), restoring the inherited
subclass contract without changing ordinary base instances. The current subclass survey still
contains generators that replace constructors, so this remains the correct compatibility
boundary; no other state from these two define checks is required from the base constructor.

PRACTICAL 6194 / KI#287 -- CLEAN
--------------------------------
Hub, Ring and Wheel now compute east/north Evergreen transition boundaries by scaling the
actual distance from the center rectangle to the far map edge, exactly mirroring the inherited
west/south calculation. This keeps the Snowy center untouched while restoring a nonempty
Evergreen band on all four sides. The repair affects visual feature variety only and all three
shared copies use the same corrected formula.

CHRONOLOGY HANDOFF
------------------
Practical 6194 is closed clean. F744 / provisional KI#1065 remains unused.
Next substantive chronology cursor: practical 6193 / KI#278-KI#282.3.

================================================================================
C031-WIP850 APPEND -- practical 6193 map world-size repair audit (KI#278-KI#282.3)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP850.
No repository source files modified.

PRACTICAL 6193 / KI#278 + KI#278.2 + KI#278.3 -- CLEAN IN CURRENT COMPOSITION
-----------------------------------------------------------------------------
Rainforest's forced-bonus world-size modifier now uses the shared complete Arena-through-SAS48
scalar progression rather than a legacy Duel-Huge table that sent Arena upward and capped all
SAS tiers at Huge. Its grid profile also contains explicit viable Arena geometry and the later
KI#278.3 calibration follow-up is already composed into current source: Duel-Huge and SAS tiers
use the empirically tuned integer progression rather than either the too-small generic compact
Arena or the oversized first prepared calibration.

PRACTICAL 6193 / KI#279 -- CLEAN
--------------------------------
Global Highlands now has the same complete Mountain Pattern grain tiers as its Highlands
sibling: Arena shares the small [3,4,5] progression, ordinary middle sizes retain [4,5,6], and
SAS24-SAS48 use [5,6,7]. The value is obtained through the robust world-size helper before the
option-dependent reversal/index, so no added tier falls through to an unrelated Huge grain.

PRACTICAL 6193 / KI#280 + KI#281 + KI#282 -- CLEAN
---------------------------------------------------
Earth2's staged start predicate now implements its documented three stages directly:
low-player starts on the biggest landmass; middle-stage starts on that landmass OR inside the
Africa rectangle; full-world starts only at the configured high-player threshold. This removes
the old unreachable Africa condition and accidental admission of unrelated Old-World islands.

The grid lookup intentionally caps Huge and all SAS tiers at Earth2's already-large Huge
geometry. This composes with the staged-start policy: higher populations gain eligible
landmasses rather than progressively enlarging the Earth layout. Its regional grain table now
also includes Arena with Duel/Tiny grain while missing larger tiers intentionally retain Huge
grain through the ordinary lookup.

PRACTICAL 6193 / KI#282.2 -- CLEAN
----------------------------------
Pangaea's directly indexed regional grain table now contains a contiguous Arena-through-Huge
set. The existing `sizekey >= len(sizevalues)` branch therefore applies only to SAS/larger
sizes; Arena can no longer hit KeyError and Huge no longer falls into the larger-size fallback.

PRACTICAL 6193 / KI#282.3 -- CLEAN
----------------------------------
Hemispheres, Mixed Continents, not-too-Big-or-Small, Tectonics and Terra now include Arena in
their directly indexed percentage dictionaries, using each script's established Duel value.
Because WorldSizeTypes is contiguous Arena=0 through Huge=6, the repaired seven-entry tables
make the legacy `worldSize >= len(dict)` larger-size guard correct again: Huge 6 uses its stored
entry, SAS24+ 7+ use the intended fallback percentage.

Terra's second directly indexed regional-grain dictionary likewise contains Arena. A current
repository-wide sweep of shipped PrivateMaps direct `sizevalues[...]`, `sizeModifiers[...]` and
`worldsizes[...]` accesses found no remaining playable missing-Arena table of this family; all
reported direct accesses have an Arena entry in their local table.

WHOLE-BUNDLE RESULT
-------------------
Current world-size helpers and subsequent map edits preserve the 6193 repair contracts. No
unowned Arena/SAS lookup sibling or staged-start/grid-policy contradiction was established.

CHRONOLOGY HANDOFF
------------------
Practical 6193 is closed clean. F744 / provisional KI#1065 remains unused.
Next substantive chronology cursor: practical 6192.

================================================================================
C031-WIP851 APPEND -- practical 6192 Random Script Map audit (KI#275-KI#277 + KI#275.2)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP851.
No repository source files modified.

PRACTICAL 6192 / KI#275 -- CLEAN
--------------------------------
Random Script Map's combined coastal/Old-World validator no longer increments the undefined
`gl_isValidCalls` local before doing any work. No live definition/use of that obsolete counter
remains. The function now directly applies its intended candidate contract: reject polar rows,
require the biggest nonwater land area, require an adjacent water area and reject lakes. If no
positive-value candidate satisfies the best-effort validator, the existing engine fallback is
still allowed; that is intentional placement fallback rather than the former Python exception.

PRACTICAL 6192 / KI#275.2 -- CLEAN
----------------------------------
The option label now says `Coastal on Largest Continent`, matching the conjunctive validator
rather than inherited BTS's misleading `Coastal or Largest Continent`. This is presentation-
only and does not pretend the best-effort fallback can never choose another site.

PRACTICAL 6192 / KI#276 -- CLEAN
--------------------------------
Embedded Medium-and-Small rolls a binary first vertical orientation and a four-way second
roll. Values 2/3 are intentionally coerced to the orientation opposite the first; inherited
code wrote standalone `==` comparisons instead of assignments. Current source performs those
two assignments. RNG count, ranges and all later region formulas are unchanged, so only the
previously discarded orientation policy is restored.

PRACTICAL 6192 / KI#277 -- CLEAN
--------------------------------
Embedded randomized Terra later shifts the New World by +0.60 X or Eurasia by -0.40 X/Y.
Current random bases cap New World east at 0.39 and place Eurasia south in 0.40..0.46, so the
corresponding transformed limits remain <=0.99 and >=0.00. Other randomized margins retain
their existing safe intervals. The commit's exhaustive rectangle harness covered all relevant
roll combinations and Arena-through-SAS48 dimensions; current source preserves those ranges
and fixed shifts.

No later map-helper change reintroduces normalization of out-of-range region rectangles as an
implicit dependency; keeping the source coordinates in bounds remains the correct contract.

CHRONOLOGY HANDOFF
------------------
Practical 6192 is closed clean. F744 / provisional KI#1065 remains unused.
Next substantive chronology cursor: practical 6191.

================================================================================
C031-WIP852 APPEND -- practical 6191 repair audit (KI#247, KI#271-KI#274)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP852.
No repository source files modified.

PRACTICAL 6191 / KI#272 -- CLEAN
--------------------------------
BTG_Cross exposed a fourth Positioning label (Outside positions, crossed) but executed the same
assignment branch as Together. Current source restores the original three selectable policies
and removes the dormant duplicate label. The remaining branches map distinctly to Numerical,
Full shuffle, and Together behavior. No payload/index mismatch survives.

PRACTICAL 6191 / KI#274 -- CLEAN
--------------------------------
The duplicated/mirrored regional generation now consumes the chosen source from both region
pools, generates only the remaining N-1 open regions, records physical region IDs uniquely,
and ranks a copy rather than destructively aliasing the global `regions_in_use` list. The
source is therefore neither generated/recorded twice nor silently removed from later callbacks.
`remaining_regionsTwo.remove(thisRegion)` is valid because both pools are initialized with the
same complete region IDs before the source is selected.

PRACTICAL 6191 / KI#273 -- CLEAN FOR ITS DOCUMENTED MIRROR-BOUNDS ROOT
---------------------------------------------------------------------
Cross/Grid percentage rectangles can differ by one row/column after integer rounding. Current
source computes one common `min(width) x min(height)` footprint across every physical mirror
region and uses that same footprint for initial plot copying and all later mirror layers
(plot type, terrain, river IDs/rivers, features, bonuses and goodies). This removes the old
source-overread into a neighboring map rectangle and keeps every copied coordinate in both the
source and destination bounds.

The adversarial exact-symmetry challenge does not establish a new bug. `mirrorizeMap()`
explicitly strips bonuses/features/improvements/rivers outside the common source footprint;
the inherited code already stated the intent to prevent a larger rounded source region from
receiving extra bonuses/goodies. A one-cell resource-free terrain fringe can remain where the
percentage rectangles physically differ, but forcing equal plot geometry would be a map-design
change rather than a missing safety repair, and no caller promises equality outside the common
footprint.

PRACTICAL 6191 / KI#271 -- CLEAN
--------------------------------
BTG_Cross now falls back before any custom start assignment when the number of distinct
physical hubs is smaller than the participating player count. This prevents modulo reuse of a
hub/exact duplicate mirrored starts. The guard is evaluated before writes, so the default
implementation receives a clean starting-plot state.

PRACTICAL 6191 / KI#247 -- CLEAN, INCLUDING REGENERATION CONTROL
---------------------------------------------------------------
BTG_Grid compares distinct region capacity against the actual ever-alive player count rather
than legacy `iPlayers`, which may represent rounded regional capacity with an intentionally
empty hub. This is the correct population for Grid's own current assignment pipeline:
`player_list`, `shuffledPlayers`, and assignment bounds all use the same ever-alive population.
BTG_Cross likewise uses ever-alive players consistently.

WorldBuilder regeneration after elimination therefore does not reproduce F743/KI#1064 here.
Unlike Team Battleground, Grid/Cross do not combine compact EVER_ALIVE identities with an
ALIVE-sized indexing array. Dead historical slots may receive a regenerated historical start,
but every surviving ever-alive player remains represented and no alive-sized array is indexed
with an ever-alive compact team index. No omission or out-of-range witness analogous to F743
survives.

WHOLE-BUNDLE RESULT
-------------------
KI#247 and KI#271-KI#274 survive current-source, regeneration, rounded-geometry, and later-
callback review. No new F744 candidate survives practical 6191.

CHRONOLOGY HANDOFF
------------------
Practical 6191 is closed clean.
F744 / provisional KI#1065 remains unused.
Next chronology cursor: practical 6190 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP853 APPEND -- practical 6190 repair audit (KI#267-KI#270)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F743: CONFIRMED.
F744 / provisional KI#1065: UNUSED.
Latest physical checkpoint: C031-WIP853.
No repository source files modified.

PRACTICAL 6190 / KI#267 -- CLEAN FOR LAGOON/CROSS REPAIR
--------------------------------------------------------
BTG_Lagoon now enumerates actual ever-alive civilization PlayerTypes into `playerIDs`, shuffles
one template ID per actual player, and stores the mapping directly in `playerTemplateByID`.
`iPlayers` and the template-ID population both come from `countCivPlayersEverAlive()`, so sparse
civilization slots no longer index a compact assignment array with a raw PlayerTypes ID.

The WorldBuilder-regeneration control does not reproduce F743/KI#1064 here. The DLL's default
starting-plot assignment calls each map-script `findStartingPlot(playerID)` only for living
civilization players. Lagoon may retain extra historical template mappings for dead ever-alive
slots, but each surviving actual PlayerTypes still has a valid one-to-one template assignment;
there is no ALIVE-sized array indexed by an EVER_ALIVE compact identity. BTG_Cross's paired
KI#267 correction likewise preserves actual PlayerTypes through its ordered-player list.

PRACTICAL 6190 / KI#268 -- CLEAN; BARBARIAN-TEAM CANDIDATE REJECTED
------------------------------------------------------------------
The finite Top-v-Bottom repair correctly replaces the inherited unbounded random-retry loop:
it requires equal team sizes, builds the finite lists of members currently on the wrong half,
requires those lists to pair completely, and swaps one randomized pair at a time. Incompatible
or unequal arrangements return before any partial normalization rewrite.

Adversarial candidate rejected: the helper's first team-count loop explicitly tests
`not isBarbarian()`, while the later `listTeams` loop appears not to. This does NOT admit the
Barbarian team. Both loops are `for iI in range(gc.getMAX_CIV_TEAMS())`; in the DLL enum contract
`BARBARIAN_TEAM == MAX_CIV_TEAMS`, so the Barbarian slot is one position beyond the loop range.
It cannot enter either loop regardless of its alive/ever-alive initialization state. The first
loop's Barbarian predicate is therefore redundant defensive wording, not a population mismatch.
No F744 root survives this candidate.

PRACTICAL 6190 / KI#269 -- CLEAN
--------------------------------
Lagoon's land-band widths now use an explicit Arena-through-SAS48 world-size table instead of
subtracting the raw post-Arena enum value from a BTS-era bias. Duel-through-Huge therefore
recover the original BTG tuning, Arena uses the safe minimum, and the SAS tiers continue the
same monotonic progression. The bridge option remains a separate +1 tile addition. Current
`CyMap().getWorldSize()` values are all covered by the table in supported generated games.

PRACTICAL 6190 / KI#270 -- CLEAN
--------------------------------
The Lagoon `DonutFractalWorld` no longer relocates its completed handcrafted plot-type array
through the inherited seam shifter. All downstream fixed-coordinate systems -- starting
regions, center terrain/rivers, bridge/center-relative resources -- therefore remain aligned
with the same unshifted geography. No later callback in the shipped script applies a second
hidden plot-array shift.

WHOLE-BUNDLE RESULT
-------------------
KI#267-KI#270 survive current-source, sparse-ID, regeneration, team-population and coordinate-
alignment review. The open Barbarian-team hypothesis is conclusively rejected by the
MAX_CIV_TEAMS/BARBARIAN_TEAM enum boundary. No new F744 candidate survives practical 6190.

CHRONOLOGY HANDOFF
------------------
Practical 6190 is closed clean.
F744 / provisional KI#1065 remains unused.
Next chronology cursor: practical 6189 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP854 APPEND -- practical 6189 repair audit (KI#264-KI#265); F744 / KI#1065
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F744: CONFIRMED.
F745 / provisional KI#1066: UNUSED.
Latest physical checkpoint: C031-WIP854.
No repository source files modified.

PRACTICAL 6189 / KI#264 -- CLEAN FOR DOCUMENTED 48-PLAYER RANDOM-CONTINENT ROOT
-----------------------------------------------------------------------------
Custom Continents' random continent-count weighting now clamps the population lookup to the
nearest table-supported profile (2..18). The stock table intentionally tops out at six
continents; SAS 19-48-player games therefore reuse the 18-player weighting instead of raising a
KeyError and falling through to invalid/all-land behavior. RNG structure and the existing table
are preserved, and the lower clamp also makes the single-player edge safe.

PRACTICAL 6189 / KI#265 -- DOCUMENTED SPARSE/HIGH-ID REPAIRS CLEAN
-----------------------------------------------------------------
Archipelago, Islands and Tectonics now scan the DLL's full civilization-player ID range while
retaining their topology's <=18 actual-player gate, so a sparse/high PlayerTypes slot is no
longer omitted merely because its absolute ID exceeds 17. Their assignment populations and
shuffle lengths both use the same ever-alive player population.

Custom Continents' One-Per-Team bookkeeping likewise now builds `team_num` across the full
civilization-team ID range. For ordinary fresh games with <=6 participating teams, a sparse/high
TeamTypes ID therefore maps to the correct compact team index and then to the shuffled continent.
The later broadened KI#267 work owns other compact-ID scripts; no additional current hard-coded
0..17 start-assignment loop survives this practical's four repaired scripts.

F744 / KI#1065 -- CONFIRMED CURRENT CUSTOM CONTINENTS REGENERATION THRESHOLD MISMATCH
------------------------------------------------------------------------------------
Separate from KI#265's sparse-ID root, Custom Continents mixes two different team populations
across the producer and consumer of its One-Per-Team shuffle:

* `assignStartingPlots()` computes `iTeams = countCivTeamsEverAlive()`. It creates global
  `shuffledTeams` only when `iTeams < 7`; otherwise it allows the default implementation.
* `findStartingPlot()` independently computes `numTeams = countCivTeamsAlive()`. It enters the
  custom One-Per-Team path whenever `numTeams <= 6`, then indexes `shuffledTeams[teamID]`.

`CvGame::regenerateMap()` is an exported WorldBuilder-supported path. It clears starts/cities/
units and reruns random-map generation plus `setInitialItems()` without reviving eliminated
players, so EVER_ALIVE can legitimately remain >6 while ALIVE has fallen to <=6.

Deterministic witness: start a Custom Continents / One Per Team game with >6 teams, eliminate
enough teams to leave <=6 alive, then regenerate through WorldBuilder. The regeneration's
`assignStartingPlots()` still sees >6 ever-alive teams and does not create `shuffledTeams`.
The subsequent living-player `findStartingPlot()` calls see <=6 alive teams and consume that
missing global. In a game that originally began above the threshold, no earlier <=6 producer
was required to define it at all, yielding a Python callback failure/default fallback; in a
lifecycle where an old <=6 value exists and ever-alive membership later grew beyond six, the
value can instead be stale/too short for the freshly rebuilt `team_num` indices.

This is independently gameplay-visible from F743/KI#1064: F743 is Team Battleground's
ALIVE-sized array indexed by EVER_ALIVE-derived identities; F744 is a Custom Continents
producer/consumer threshold mismatch that crosses 6 only after lifecycle changes. Whole-album
search found no prior owner.

PRACTICAL 6189 RESULT
---------------------
KI#264 and KI#265 remain valid repairs for their documented fresh-game 48-slot/sparse-ID roots.
The adversarial lifecycle pass adds one independently reachable inherited/custom-script root,
F744/KI#1065, in Custom Continents One-Per-Team regeneration.

CHRONOLOGY HANDOFF
------------------
Practical 6189 is closed with F744 / KI#1065 confirmed.
F745 / provisional KI#1066 remains unused.
Next chronology cursor: practical 6188 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP855 APPEND -- practical 6188 repair audit (KI#262, KI#263, KI#266)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F744: CONFIRMED.
F745 / provisional KI#1066: UNUSED.
Latest physical checkpoint: C031-WIP855.
No repository source files modified.

PRACTICAL 6188 / KI#262 -- CLEAN
--------------------------------
The five shipped affected scripts now derive tiny-island Y from the independently rolled
`tinySouthLat`, not from `tinyWestLon`. A repository sweep finds no surviving
`tinySouthY = int(... tinyWestLon)` form in current PrivateMaps. RNG order is unchanged: both
longitude and latitude rolls still occur, only the intended latitude value is consumed for Y.

A tempting sibling in RandomScriptMap was checked separately. Its embedded Medium-and-Small
implementation already uses its own latitude roll for tiny-island Y and explicitly documents
that older copy/paste fix; it does not retain KI#262.

PRACTICAL 6188 / KI#263 -- CLEAN
--------------------------------
Standalone Medium_and_Small now passes `iContinentsGrainEast` to the eastern continent region.
The independently rolled west/east grains therefore both affect their intended halves.

Adversarial candidate rejected: RandomScriptMap's embedded Medium-and-Small generator contains
both `iContinentsGrainWest` and `iContinentsGrainEast` calls. The grep-visible west call is the
legitimate western continent; the later eastern continent already consumes
`iContinentsGrainEast`. That embedded Refar variant had independently corrected this typo, so
there is no F745 sibling there.

PRACTICAL 6188 / KI#266 -- CLEAN
--------------------------------
Custom Continents template `(6,3)` region 4 now gives its inner layer the same horizontal
`-0.63` binary-shift component as the outer layer and its mirrored lower partner. This restores
nested-layer movement on the intended axis. The surrounding six-continent template data show
the same outer/inner-axis pairing pattern; no second inner layer with the old vertical-only
copy/paste survives the targeted template family.

WHOLE-BUNDLE RESULT
-------------------
KI#262, KI#263 and KI#266 survive current-source and sibling-template review. The two plausible
repository-wide siblings (RandomScriptMap tiny islands and eastern continental grain) are
already correct in that embedded implementation. No new F745 candidate survives practical 6188.

CHRONOLOGY HANDOFF
------------------
Practical 6188 is closed clean.
F745 / provisional KI#1066 remains unused.
Next chronology cursor: practical 6187 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP856 APPEND -- practical 6187 repair audit (KI#254-KI#255)
================================================================================

AUTHORITY / STATUS
------------------
Primary C++ file audit: 169/169 COMPLETE.
Post-primary findings F708-F744: CONFIRMED.
F745 / provisional KI#1066: UNUSED.
Latest physical checkpoint: C031-WIP856.
No repository source files modified.

PRACTICAL 6187 / KI#254 -- CLEAN
--------------------------------
All four `isPeirceCrease()` quadrant tests now compute squared Euclidean coordinate deltas through
explicit multiplication. A current PrivateMaps sweep finds no remaining Peirce `^2` expression,
so Python bitwise XOR no longer participates in the circular crease predicate. The comparison
against the already-squared radius and all quadrant sign/eligibility gates are unchanged.

PRACTICAL 6187 / KI#255 -- CLEAN ACROSS BOTH DUPLICATED E-W IMPLEMENTATIONS
--------------------------------------------------------------------------
Each affected E-W companion arm now passes the freshly computed `iRefX2/iRefY2` anchor rather
than reusing its preceding landscape arm's `iRefX1/iRefY1`. The corresponding four cases are
fixed in both duplicated wrap-mode blocks, covering all eight bad calls identified in the
original Peirce source.

Adversarial same-variable call sites elsewhere in Peirce are not siblings of this root. In the
N-S/wrapped construction the code deliberately recomputes `iRefX1/iRefY1` between the two
parallel arms and never computes a discarded second anchor. The defining KI#255 signature --
compute distinct `iRefX2/iRefY2`, then accidentally pass the old first pair -- no longer survives.

WHOLE-BUNDLE RESULT
-------------------
KI#254 and KI#255 survive current-source, duplicated-wrap-block and sibling-call review. No new
F745 candidate survives practical 6187.

CHRONOLOGY HANDOFF
------------------
Practical 6187 is closed clean.
F745 / provisional KI#1066 remains unused.
Next chronology cursor: practical 6186 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP857 chronology checkpoint - practical 6186 / KI#241
================================================================================

Status: practical 6186 CLOSED after current-source, ancestry and deal-contract review.
Next unused finding slot remains F745 / provisional KI#1066.

Practical 6186 / KI#241 - Foreign Advisor Treaties mixed-resource disclosure
-------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repair fixes the documented mismatch without changing the row's cancellation
contract: resource-only deals remain intentionally omitted from the Treaties tab,
but once a deal is otherwise visible, resource terms are included in the same
one-row-per-CvDeal text that carries WIDGET_DEAL_KILL(deal.getID()). The widget
therefore still cancels the complete underlying deal and the visible mixed row now
states the complete cancellation scope.

Adversarial checks completed:
- `formatTreatyTrade` does not leave an unsupported TradeableItems hole. Explicit
  icon branches cover the main treaty/tech/civic/religion/resource/gold/map terms;
  the generic trade-icon fallback still calls native `CyGameTextMgr.getTradeString`,
  whose C++ switch covers the complete trade enum including AdvCiv TRADE_DISENGAGE.
- One-shot city/tech/map/etc. terms do not create a persisted hidden-term problem;
  ordinary `CvDeal::startTrade` saves only the continuing terms appropriate to the
  live deal, while the formatter remains defensive for mixed/custom cases.
- Duplicate display suppression remains confined to the old no-resource compact
  list. Resource-bearing rows preserve first-side and second-side entries separately,
  so their directional scope is not collapsed before localized grammar is applied.
- The apparent `listOurDealEntries` perspective reversal is intentional ancestry,
  not a regression. Base AdvCiv/K-Mod rendered each rival panel row with
  `getDealString(deal, iLoopPlayer)`, i.e. from that rival panel player's perspective.
  The KI#241 branch preserves that convention by treating the rival's side as the
  localized "our" side and the active leader as the named counterpart.
- Native WIDGET_DEAL_KILL hover/click continues to resolve the exact deal ID; no
  resource-specific cancellation path or alternate deal lookup was introduced.

No new root was promoted. F745 / provisional KI#1066 remains UNUSED.

Next chronology cursor: practical 6185 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP858 chronology checkpoint - practical 6185 / KI#239 + KI#240
================================================================================

Status: practical 6185 CLOSED after current-source, caller-direction and cancellation review.
Next unused finding slot remains F745 / provisional KI#1066.

Practical 6185 / KI#239 - dominated-strategic hard denial leaked onto human buyers
---------------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repair's `!bPlayerHuman` guard is on the correct semantic object. Inside
`CvPlayerAI::AI_bonusTrade`, `kPlayer = GET_PLAYER(eToPlayer)` is always the
recipient/buyer, regardless of which player object owns the seller-side method.
This remains true in the less-obvious cancellation direction where an AI checks a
human-supplied resource by calling the human seller's `AI_bonusTrade`: the
recipient passed in is still the AI, so the AI-buy optimization remains active.
Conversely, AI-to-human first-copy inventory and cancellation paths bypass the
hard substitution rule exactly as KI#239 intended.

The later ordinary duplicate-resource clause does not recreate the first-copy bug:
for a proposed first copy, availability plus `iChange == 1` does not exceed one;
for an existing first-copy import, the cancellation call uses `iChange == 0` and
one available copy likewise does not exceed one.

Practical 6185 / KI#240 - Medieval Copper and corporation value
---------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The era-alone Copper exclusion is gone; Copper becomes dominated by this hard gate
only when the AI buyer actually has Iron. Positive buyer corporation value is
queried before returning DENIAL_JOKING, so a militarily substitutable resource
with live corporation value reaches AdvCiv's ordinary valuation path. The
subsequent generic duplicate-resource filter already uses the same buyer-side
corporation safeguard, so the two layers compose rather than contradict each
other.

The explicit strategic-substitution table remains deliberately heuristic and is
documented as calibrated to current SAS XML; changing unit/resource relationships
in a modmod is therefore configuration/design maintenance rather than a surviving
repair bug in the present tree.

No new root was promoted. F745 / provisional KI#1066 remains UNUSED.

Next chronology cursor: practical 6184 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP859 chronology checkpoint - practical 6184 / KI#238 + KI#242 + KI#245
================================================================================

Status: practical 6184 CLOSED after current-profile, start-allocation and regeneration review.
Next unused finding slot remains F745 / provisional KI#1066.

Practical 6184 / KI#238 - Parallel Lines excess-player slot reuse
------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The explicit `iIndex >= sum(layout["line_slots"])` guard now delegates every
out-of-capacity player to Civ4's default start finder instead of falling through
with the last custom line/position retained. In-capacity nominal starts remain
unique; the script's waviness trims only line edges while the custom start uses
the line center, so the normal custom path does not depend on a duplicate-prone
fallback search. The player index is recomputed from the current sorted alive
population and per-generation assignment state resets in `beforeGeneration()`.

Practical 6184 / KI#242 - Large Facing Islands default capacity
---------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The current world profile provides exactly the intended default capacity at the
historically deficient sizes: Arena is 1x2 for 2 default players and Small is 1x5
for 5. Other sizes still meet or exceed their current XML defaults. Starts use the
current sorted alive-player list, one center slot per generated island, with a
clean DLL fallback for custom player counts beyond island capacity. Regeneration
after eliminations therefore does not reproduce the later ALIVE/EVER_ALIVE roots.

Practical 6184 / KI#245 - Spiky Avenues default house capacity
--------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Tiny now generates 4 houses for 3 default players and Small 6 houses for 5;
current Standard-through-SAS48 profiles likewise meet their XML default counts.
The per-generation slot/used-plot state resets, player indexing is ALIVE-based,
and excess custom-game players delegate to the DLL. Nominal house-center/outer-edge
slots are generated as land and the existing used-plot set protects the bounded
safety search as well.

Cross-check against F740/F743/F744:
- none of these three fixes uses an EVER_ALIVE-derived compact index to address an
  ALIVE-sized structure;
- none has a producer/consumer threshold split across alive vs ever-alive counts;
- Python overflow delegates after already assigned custom starts through Civ4's
  taken-start-aware default path rather than reusing the last custom slot.

No new root was promoted. F745 / provisional KI#1066 remains UNUSED.

Next chronology cursor: practical 6183 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP860 chronology checkpoint - practical 6183 / KI#237 + KI#237.2
================================================================================

Status: practical 6183 CLOSED after current-callsite/font-lineage review.
New confirmed root: F745 / KI#1066.
Next unused finding slot: F746 / provisional KI#1067.

F745 / KI#1066 - GP Maximum-types fitting measures a different font from the rendered SAS Label font
-----------------------------------------------------------------------------------------------------
Classification: current AdvCiv-SAS UI/font-migration regression; sibling exposed while adversarially
reviewing the otherwise-correct KI#237 + KI#237.2 repairs.

Current source contract:
- both live `GPUtil.getGreatPeopleText(...)` callers pass the actual runtime GP-bar width;
- after the helper returns, both callers wrap the complete text in `sasFontTagLabel`;
- `SAS_UI_FONT_LABEL` is a supported 1..4 define and currently defaults to 3;
- Maximum-types mode nevertheless calls `CyInterface().determineWidth(szNewText)` on an
  *untagged* candidate string.

Lineage proof:
1. K-Mod/BUG's old helper explicitly wrapped the appended GP-type suffix in `<font=2>` while
   performing the width measurement and while returning it.
2. SAS practical 5505 removed that local font tag because font ownership moved to the caller.
   The render path was indeed moved outward, but the measurement path was left untagged rather
   than being wrapped in the same configured font.
3. SAS practical 5626 then intentionally changed the Main Interface GP-bar text from the shared
   BODY font to the shared LABEL font. Current LABEL defaults to 3 and remains configurable.
4. Practical 6183 fixed two independent fitting defects: KI#237 now measures the *candidate*
   (`szNewTypes`) instead of the previous accepted list, and KI#237.2 supplies the real city-bar
   width instead of K-Mod's stale 230px constant. Neither repair changes the font used for
   `determineWidth`.

Internal current-tree control:
- width-sensitive Scoreboard code explicitly measures `SASTextScale.labelText(value)` or an
  already `sasFontTagLabel`-wrapped buffer when the corresponding text is rendered in Label font.
  This demonstrates the project's current contract that the font markup belongs in the string
  passed to `determineWidth`; the helper cannot infer a caller's later font tag.

Reachable consequence:
- with a larger configured Label font, Maximum-types can admit an icon/percentage suffix whose
  untagged measured width fits but whose actual Label-font render clips the GP bar;
- with a smaller configured Label font, it can stop early and hide a candidate that would fit.
- this affects both the top/map GP bar and city GP bar because both share the helper and both now
  render its result under `sasFontTagLabel`.

This is distinct from KI#237 (one-candidate-late acceptance) and KI#237.2 (stale physical width).
Those repairs remain correct; the missing dimension is font-consistent measurement after SAS's
font migration.

Surgical direction (not applied here): measure the same candidate under the same configured
Label font used by the callers, e.g. by applying the shared Label wrapper only for
`determineWidth` while retaining the helper's existing returned-text ownership contract. Avoid
reintroducing nested font tags into the returned text.

Whole-album dedup: no existing F-root owns GP-bar configured-font measurement. F745 is new.

Next chronology cursor: practical 6182 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP861 chronology checkpoint - practical 6182 / KI#235
================================================================================

Status: practical 6182 CLOSED after actual-unit/basic-help/executor and data-contract review.
Post-primary findings F708-F745 remain confirmed.
Next unused finding slot remains F746 / provisional KI#1067.
No repository source files modified.

PRACTICAL 6182 / KI#235 - actual-unit Bomber collateral hover
-------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repair changes only the actual-unit collateral-help eligibility predicate from
`pUnit->canFight()` to `pUnit->canFight() || pUnit->airBaseCombatStr() > 0` while preserving
its positive collateral-damage-or-limit gate. This is the same base-or-air combat distinction
already used by the basic/unit-info help.

Executor control:
- `CvUnit::canFight()` is strictly `baseCombatStr() > 0`;
- `CvUnit::canCombat()` is broader (`baseCombatStr() > 0 || airBaseCombatStr() > 0 || isNuke()`),
  so deliberately not using it avoids admitting nuclear-only units into this ordinary
  collateral-help family;
- `CvUnit::collateralCombat()` computes collateral strength from `airBaseCombatStr()` for
  `DOMAIN_AIR`, proving that positive-air/zero-base units are functional collateral actors;
- its early return also deliberately permits promotion-derived extra collateral, matching the
  separately audited KI#324.2 zero-base-damage capability policy.

Current-data control:
- Bomber: iCombat 0, iAirCombat 16, iCollateralDamage 100, limit 200, max 5;
- Stealth Bomber: iCombat 0, iAirCombat 20, iCollateralDamage 100, limit 200, max 6;
- Jet Fighter / Guided Missile have positive air combat with zero base collateral but retained
  nonzero capacity metadata, matching the already-audited KI#324.2 presentation policy;
- Tactical Nuke, ICBM and civilian Great Prophet have neither base nor air combat and therefore
  remain excluded despite inherited nonzero collateral-limit metadata.

Adversarial display-unit check:
`CvUnit::collateralDamageLimit()` returns XML percent scaled into MAX_HIT_POINTS, whereas the
basic/type-help branch explicitly converts UnitInfo's corresponding hit-point-scaled limit back
into percent. Supplied stock BtS has `MAX_HIT_POINTS = 100`, and the current SAS tree contains no
override of that inherited define. Thus the actual-unit accessor's value is numerically the same
percentage under the supported data contract; this does not produce a current display mismatch.

The existing runtime validation (Stealth Bomber displays 100% base / 200% limit / 6 defenders;
Settler remains free of collateral noise) is therefore consistent with source semantics rather
than merely a screenshot-specific success.

Whole-album control: later collateral/help and combat audits contain no surviving sibling owned
by KI#235, and no new root is promoted here.

CHRONOLOGY HANDOFF
------------------
Practical 6182 is closed clean.
F746 / provisional KI#1067 remains unused.
Next chronology cursor: practical 6181 (resolve exact KI bundle from local commit map).

================================================================================
C031-WIP862 chronology checkpoint - practical 6181 / KI#236
================================================================================

Status: practical 6181 CLOSED after Python-exposure, XML-population and native-eligibility review.
Post-primary findings F708-F745 remain confirmed.
F746 / provisional KI#1067 remains UNUSED.
No repository source files modified.

PRACTICAL 6181 / KI#236 - Executive corporation requirement in Sevopedia Unit
-----------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Current `SevoPediaUnit.placeRequires()` reads `CvUnitInfo.getPrereqCorporation()`, attaches the
required corporation's button with `WIDGET_PEDIA_JUMP_TO_CORPORATION`, and marks the Requires
panel nonempty. The getter is explicitly exported through `CyInfoInterface1`.

Native oracle:
`CvPlot::canTrain` independently checks the same UnitInfo field and rejects training unless the
plot has a city whose `isActiveCorporation(prereqCorp)` is true. The Pedia icon therefore reports
a real static unit-production prerequisite rather than inferring one from Executive naming or
spread behavior.

Current-data census:
exactly seven current unit rows have non-NONE `PrereqCorporation`, CORPORATION_1 through
CORPORATION_7; these are the seven Executive units documented by KI#236. All other unit rows use
NONE. Thus the added generic getter branch is complete for current XML and remains future-safe for
another unit that legitimately adopts the same schema field.

Adversarial controls:
- dynamic suppression of an otherwise present corporation (e.g. no-corporations state) is part of
  `isActiveCorporation`, not a second static UnitInfo requirement that belongs as another icon;
- existing tech/bonus/religion/building/project requirements retain their original independent
  handling and panel-empty bookkeeping;
- the corporation widget ID/data contract is the same one used by other Sevopedia corporation
  buttons, and the recorded runtime click/hover validation agrees with that source contract.

No surviving sibling of the omitted Unit `PrereqCorporation` display is present in the current
Requires panel. No new root is promoted.

CHRONOLOGY HANDOFF
------------------
Practical 6181 is closed clean.
F746 / provisional KI#1067 remains unused.
Next chronology cursor: practical 6180 / KI#234.

================================================================================
C031-WIP863 chronology checkpoint - practical 6180 / KI#234
================================================================================

Status: practical 6180 CLOSED after retained-target, city-lifetime and perspective-transition review.
Post-primary findings F708-F745 remain confirmed.
F746 / provisional KI#1067 remains UNUSED.
No repository source files modified.

PRACTICAL 6180 / KI#234 - persistent Foreign Advisor Espionage target state
-------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repair rebuilds the current alive/non-Barbarian/met rival list on every Espionage redraw and
validates the retained `ESP_iTargetPlayer` against that list. If the target is absent it clears
player, city and mission together before the existing first-valid-target selection. A still-valid
target remains persistent, which preserves the intended post-integration convenience.

Adversarial retained-city challenge - REJECTED:
A tempting concern was that a still-valid target could retain an `ESP_iActiveCityID` that is no
longer revealed under a changed advisor perspective. The supported Debug dropdown does not leave
that state intact: it explicitly sets `ESP_iTargetPlayer = -1` before rebuilding, and KI#234's
membership branch then clears city and mission too. Thus the same perspective-switch mechanism
used in the original runtime validation cannot carry a prior perspective's city through the repair.

With the active team unchanged, revealed map state is persistent. If the retained target loses or
destroys the selected city, `pTargetPlayer.getCity(oldID).isNone()` causes the city-list pass to
select the first current revealed city. The city ID/free-list contract prevents a stale destroyed
city object from remaining a valid city lookup under the old identifier.

`ESP_iSelectedMission` is not a hidden execution target: current code only resets it or assigns the
first active-mission ID when it is -1; no later reader executes or prices a mission from that field.
Consequently retaining it for a still-valid target does not produce a stale mission action.

The target list itself is rebuilt from the selected active player's team (`isHasMet`) and excludes
same-team, dead and Barbarian players before retained-state validation, so no alternate invalid-
target category bypasses the repair.

No existing or newly proven sibling survives. No new root is promoted.

CHRONOLOGY HANDOFF
------------------
Practical 6180 is closed clean.
F746 / provisional KI#1067 remains unused.
Next chronology cursor: practical 6179 / KI#231-KI#233.

================================================================================
C031-WIP864 chronology checkpoint - practical 6179 / KI#231-KI#233
================================================================================

Status: practical 6179 CLOSED after numeric-scaling, map-observer and native-action dispatch review.
New confirmed root: F746 / KI#1067.
Next unused finding slot: F747 / provisional KI#1068.
No repository source files modified.

PRACTICAL 6179 / KI#231 - Policy Advisor corporation generated-value units
--------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The current Policy Advisor applies the active world size's CorporationMaintenancePercent to the
stored corporation yield/commerce value while retaining the result in hundredths, then formats
that integer as signed units (`+1`, `+0.50`, `+1.50`, etc.). This matches native
`CvGameTextMgr::setCorporationHelp`: native code multiplies the XML value by the same world-size
percent and divides by 100 before interpreting the result as hundredths.

The Python helper explicitly emulates C++ truncation toward zero for negative intermediate values,
avoiding Python 2's floor-division difference. Its string formatter then uses integer assembly, so
no float-rounding mismatch is introduced. No surviving raw-hundredths or unscaled branch remains
in the Policy Advisor's generated-value row.

PRACTICAL 6179 / KI#232 - vassal Policy perspective map-knowledge leak
---------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Outside Debug mode, `getMapObserverTeam()` returns the real active player's team even when the
Policy Advisor subject is an inspectable vassal. Both Holy City and Corporation Headquarters
location/reveal tests use that observer team, while the selected vassal remains the policy/data
subject. In Debug mode the helper deliberately returns the selected player's team, matching the
screen's unrestricted debug-inspection semantics.

Thus subject identity and map-observer identity are separated exactly at the two global location
rows that can expose a hidden Holy City/HQ. No second old `self.iActivePlayer` reveal test survives
in those rows.

F746 / KI#1067 - restored Domestic Free Colony button is selected-player gated but active-player executed in Debug
------------------------------------------------------------------------------------------------------------------
Classification: current AdvCiv-SAS KI#233 repair-side perspective regression.

KI#233 correctly restored the missing visible Free Colony/Liberate control to the standard
Domestic Advisor. The new helper intends to show it only for the actionable real active-player
perspective, but its first guard is:

  if isAdvisorReadOnlyPerspective(self.iActivePlayer): return

The shared helper intentionally returns False for *all* perspectives while Debug mode is active.
The Domestic debug dropdown can therefore select any alive player B and `drawFreeColonyButton`
then computes visibility from B:

- `gc.getPlayer(B).canSplitEmpire()`;
- B's cities and each city's liberation recipient;
- the resulting `bCanLiberate` decides whether the button exists.

The button, however, is only a native `WIDGET_ACTION` carrying the action-info index for
`CONTROL_FREE_COLONY`; it carries no PlayerTypes payload.

Native executor proof:
- `CvDLLWidgetData::executeAction` dispatches WIDGET_ACTION through `doAction`;
- `CONTROL_FREE_COLONY` in `CvGameInterface` creates `BUTTONPOPUP_FREE_COLONY` without a selected
  player ID;
- `CvDLLButtonPopup::launchFreeColonyPopup` binds the popup to
  `GET_PLAYER(getActivePlayer())` and enumerates the real active player's split/liberation choices;
- popup acceptance sends `sendEmpireSplit(getActivePlayer(), ...)` or a TASK_LIBERATE command for
  the active player's city.

Deterministic consequence:
1. enter Debug mode and select alive AI B in the Domestic Advisor;
2. if B currently has a split/liberation option, the KI#233 helper displays DomesticSplit based on B;
3. click it;
4. the native popup and any resulting action operate on real active player A, not B.

The inverse mismatch also exists: selecting a B with no qualifying option hides the button even if
A has one. Thus the repair's UI eligibility and executor ownership are different players.

This is independent of existing Free-Colony roots F317/KI#640 (popup recipient recomputation) and
F447 (recycled dead-player/live-team colony slot lifecycle); neither covers advisor perspective or
the newly restored Domestic control.

Surgical direction (not applied here): gate this native action on
`self.iActivePlayer == CyGame().getActivePlayer()` even in Debug mode (or otherwise provide a
selected-player-specific executor, which the current WIDGET_ACTION contract does not support).
The former matches the repair comment's own "real active-player perspective" intent.

PRACTICAL 6179 / KI#233 ORIGINAL ROOT
-------------------------------------
The original missing-control repair remains valid for the real active-player perspective: its
runtime footer slot, native action index and ordinary conditional availability restore Base
AdvCiv's contextual affordance without reverting the SAS tabbed shell. F746 is an additional
Debug-perspective dimension of that new control, not a rejection of KI#233's main fix.

CHRONOLOGY HANDOFF
------------------
Practical 6179 is closed with F746/KI#1067 confirmed.
Next unused finding slot: F747 / provisional KI#1068.
Next chronology cursor: practical 6178 / KI#229-KI#230.

================================================================================
C031-WIP865 chronology checkpoint - practical 6178 / KI#229-KI#230
================================================================================

Status: practical 6178 CLOSED after typed-table and width-contract review.
Post-primary findings F708-F746 remain confirmed.
F747 / provisional KI#1068 remains UNUSED.
No repository source files modified.

PRACTICAL 6178 / KI#229 - Hall of Fame Date-header sort semantics
-----------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Base AdvCiv/K-Mod use Civ4's dedicated `screen.setTableDate(...)` for the formatted completion-date
cell. The SAS font-upscaling regression had replaced that with generic setTableText, losing the
engine's date cell type while leaving table-header sorting enabled. Current
`SASTextScale.setTableDateLabel` restores the same native `setTableDate` API and applies only the
shared Label font wrapper around the display text.

This is consistent with the project's established typed-cell scaling pattern: numeric sortable
cells likewise remain native `setTableInt` cells while carrying SAS font markup. The separate
Hall-of-Fame Sort-by-Date dropdown already and still orders by numeric `replayInfo.getFinalTurn()`,
so the repair neither changes replay chronology nor introduces a second competing sort key.

Runtime evidence recorded with the repair confirmed the table renders and the clickable Date
header toggles native sort state without error; the current-source/ancestry contract supplies the
missing multi-row date semantics even though that runtime fixture had only one stored game.

PRACTICAL 6178 / KI#230 - nonpositive Leader item-width cutoff
--------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Current `SAS_getLeaderItemsWidthByCurrentUIFont()` applies the wide-screen "return to base item
width" rule only when `SAS_SEVOPEDIA_LEADER_ITEMS_NO_REDUCE_MIN_WIDTH > 0` and the screen width is
at/above that threshold. This exactly matches the XML contract that <=0 keeps the custom per-font
width active at every resolution.

The independent per-font width values retain their own documented `<=0 -> base width` fallback,
so disabling the resolution cutoff does not disable that separate safety mechanism. The XML
wording and implementation both use `>=` for the positive threshold; no strictness mismatch
survives.

No new root is promoted.

CHRONOLOGY HANDOFF
------------------
Practical 6178 is closed clean.
F747 / provisional KI#1068 remains unused.
Next chronology cursor: practical 6177 / KI#228.

================================================================================
C031-WIP866 chronology checkpoint - practical 6177 / KI#228
================================================================================

Status: practical 6177 CLOSED after aligned-scoreboard population/order/lifetime review.
Post-primary findings F708-F746 remain confirmed.
F747 / provisional KI#1068 remains UNUSED.
No repository source files modified.

PRACTICAL 6177 / KI#228 - aligned scoreboard scrolling vs BUG Max Players
------------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repair establishes one authoritative aligned-scoreboard population before scroll state is
computed. `_scoreboardVisiblePlayers(scores)` fills the BUG Scoreboard with the current visible
teams/players and per-player part state; `scores.prepare()` then performs the renderer's native
rank assignment, vassal grouping/sort and positive `Max Players` cap, returning the resulting
PlayerTypes order.

`CvMainInterface` now derives all of the following from that same prepared list:
- total effective row count;
- active-player autocenter lookup/order;
- scroll-offset clamp;
- +/- and fast-scroll button availability.

It then calls `scores.draw(..., bPrepared=True)`, so the renderer does not independently repeat a
sort/cap against a different population. The remaining `draw` slice uses the already-prepared
list and mutates the object to the current visible window intentionally; `Scoreboard.hide(True)`
therefore re-shows only the rows actually drawn during hover expansion.

If a positive BUG Max Players cap excludes the active player, autocenter simply has no active
index and leaves the offset to the normal bounded clamp; this is consistent with the user's
explicit cap rather than a second ordering mismatch. Non-aligned drawing remains on its separate
uncapped traversal exactly as documented by KI#228.

No current second Max-Players/cap/order consumer survives. No new root is promoted.

CHRONOLOGY HANDOFF
------------------
Practical 6177 is closed clean.
F747 / provisional KI#1068 remains unused.
Next chronology cursor: practical 6176 / KI#225.

PHYSICAL CHECKPOINT: C031-WIP866.
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F746 remain confirmed.
F747 / provisional KI#1068: UNUSED.
===============================================================================
END C031-WIP866
===============================================================================


================================================================================
C031-WIP867 chronology checkpoint - practical 6176 / KI#225
================================================================================

Status: practical 6176 CLOSED after EventTrigger indirect-era threshold review.
Post-primary findings F708-F747 remain confirmed.
F748 / provisional KI#1069 is UNUSED.
No repository source files modified.

F747 / KI#1068 - EventTrigger corporation-era inference ignores SAS HQ-building tech gates
------------------------------------------------------------------------------------------
Classification: current AdvCiv-SAS incomplete KI#225 repair / Sevopedia grouping correctness.

KI#225 correctly repaired several indirect EventTrigger-era families by matching the DLL's
threshold semantics instead of maxing every listed alternative. The corporation branch, however,
still derives each corporation era from:

  gc.getCorporationInfo(iCorporation).getTechPrereq()

and does the same for the all-corporations threshold.

That is not the corporation availability contract in current AdvCiv-SAS. All seven corporation
rows in CIV4CorporationInfo.xml currently have `<TechPrereq>NONE</TechPrereq>`. Corporations are
instead gated by their HQ/founding building (`<FoundsCorporation>`), whose own PrereqTech/TechTypes
provide the real technology boundary. The same Sevopedia grouping module already documents this
and implements `SAS_getCorporationAvailabilityEra(iCorporation, iNumBuildingAndTechs)` specifically
to resolve the founding building and reuse the building-availability helper.

Current founding-building availability eras are:
- CORPORATION_1: Industrial (Theory of Evolution + Corporation)
- CORPORATION_2: Modern (Marine Technology + Corporation)
- CORPORATION_3: Industrial (Combustion + Corporation)
- CORPORATION_4: Modern (Radio + Corporation)
- CORPORATION_5: Industrial (Electricity + Corporation)
- CORPORATION_6: Industrial (Assembly Line + Corporation)
- CORPORATION_7: Industrial (Corporation)

So the sorted all-corporation threshold is five Industrial + two Modern entries. The live XML has
three EventTriggers with positive `iNumCorporations`, all with an empty CorporationsRequired list
and no competing direct/indirect technology gate:

1. EVENTTRIGGER_GREAT_DEPRESSION: `iNumCorporations = 2`
   DLL player-trigger semantics count distinct corporation types present in the player's empire.
   The second-earliest current corporation is still Industrial, so the earliest feasible era is
   Industrial.
2. EVENTTRIGGER_CORPORATE_EXPANSION: `iNumCorporations = 1`
   Earliest feasible era is Industrial.
3. EVENTTRIGGER_HOSTILE_TAKEOVER: `iNumCorporations = 1`
   Earliest feasible era is Industrial.

Current `_SAS_getEventTriggerEarliestEraAndSource`, however, converts every direct corporation
TechPrereq NONE to era -1. `_SAS_getThresholdEra` therefore contributes no corporation era, and
these otherwise ungated triggers fall into the Sevopedia `Any era` bucket even though they cannot
exist before Industrial corporation founding becomes possible.

This is not merely latent/config robustness: all three affected trigger families are current XML,
and no equal-or-later independent gate masks the error. Whole-album search found no prior finding
owning EventTrigger corporation availability / Great Depression era grouping.

Surgical direction (not applied here): in the EventTrigger corporation branch, use the existing
`SAS_getCorporationAvailabilityEra(iCorporation, gc.getNUM_BUILDING_AND_TECH_PREREQS())` (or a
cached equivalent) for both listed and all-corporation era vectors instead of direct
CvCorporationInfo TechPrereq. Preserve KI#225's existing threshold semantics: player-level empty
lists require the Nth-earliest distinct corporation; pick-city listed corporations combine the
city count gate with any-one-listed-type semantics.

PRACTICAL 6176 / KI#225 OTHER REPAIR DIMENSIONS
-----------------------------------------------
The initially suspected pick-city building threshold issue is rejected. `CvCity::canTrigger`
requires `getBuildingCount() >= iNumBuildings`, then, when a BuildingsRequired list exists,
requires that the city contain any one listed building class; it does not require
`iNumBuildings` distinct listed classes. Therefore KI#225's threshold-1 treatment for the listed
building alternatives matches the DLL contract.

The corresponding unit/religion threshold repairs remain coherent under the same current-source
check. F747 concerns only corporation availability-era resolution, not KI#225's threshold-family
rewrite in general.

CHRONOLOGY HANDOFF
------------------
Practical 6176 is closed with F747/KI#1068 confirmed.
Next unused finding slot: F748 / provisional KI#1069.
Next chronology cursor: practical 6175 / KI#224.

================================================================================
C031-WIP868 chronology checkpoint - practical 6175 / KI#224
================================================================================

Status: practical 6175 CLOSED after World Advisor observer/revealed-state revalidation.
Post-primary findings F708-F748 remain confirmed.
F749 / provisional KI#1070 is UNUSED.
No repository source files modified.

F748 / KI#1069 - KI#224 World Advisor Territory still filters live ownership under fog
--------------------------------------------------------------------------------------
Classification: current AdvCiv-SAS incomplete KI#224 repair / advisor map-information leak.

KI#224 correctly separated the World Advisor's selected SUBJECT from the real human map OBSERVER
outside Debug mode, and repaired BFC/Territory plot reveal, bonus, improvement and route reads to
use the observer team and revealed-state APIs. The Territory collector, however, still admits
plots with:

  pPlot.getOwner() == self.iActivePlayer

before counting the selected player's territory.

Live ownership is not the observer's remembered ownership contract in Civ4. `CvPlot` stores
`m_aiRevealedOwner[TeamTypes]`, exposes `getRevealedOwner(eTeam, bDebug)` to Python, and on
`CvPlot::setOwner` calls `updateRevealedOwner` for alive teams. That update changes the remembered
owner only when `plotThatRevealsOwner(eTeam)` finds the plot itself or an adjacent witness currently
visible. Thus a revealed-but-fogged border tile can intentionally retain its old revealed owner
while the live owner changes.

Deterministic consequence in the supported vassal perspective:
1. active human/master A has previously revealed a plot on selected vassal B's border;
2. the plot goes back under fog for A;
3. culture/ownership changes unseen, either away from B or newly to B;
4. A's `getRevealedOwner` remains the old owner until current visibility witnesses the change;
5. World Advisor Territory nevertheless uses raw `getOwner()` and immediately removes/adds that
   plot from B's territory counts.

The leak can therefore reveal hidden border changes through Total/terrain/feature/resource/
improvement territory counts even though KI#224 correctly protects the individual bonus,
improvement and route identities once a plot is admitted.

This is not the same as the initially challenged raw FeatureType use. Civ4 has no per-team
revealed-feature snapshot; feature symbols are deliberately visible/updated on any revealed plot
(`updateFeatureSymbolVisibility` keys off `isRevealed`), so current feature state matches the
engine's own revealed-map presentation. Ownership is explicitly different because Civ4 does
maintain remembered owner state.

Whole-album dedup found existing KI#224, KI#232 and KI#335 perspective/privacy roots, but no prior
root for World Advisor Territory's raw owner membership test. F748 is therefore kept separate as
an omitted revealed-state dimension of the KI#224 repair.

Surgical direction (not applied here): in `collectTerritoryData`, replace the raw owner membership
predicate with

  pPlot.getRevealedOwner(iObserverTeam, bDebug) == self.iActivePlayer

while retaining the existing reveal guard and observer-relative bonus/improvement/route reads.
Debug keeps live ownership through the wrapper's bDebug behavior.

PRACTICAL 6175 / KI#224 ORIGINAL REPAIR
---------------------------------------
The original repair remains valid: outside Debug, `getMapObserverTeam()` correctly keeps the real
active human as observer while vassal selection changes only the advisor subject. BFC and
Territory bonus/improvement/route calls use observer-relative APIs. No surviving direct
`self.iActiveTeam` fog/resource read was found.

CHRONOLOGY HANDOFF
------------------
Practical 6175 is closed with F748/KI#1069 confirmed.
Next unused finding slot: F749 / provisional KI#1070.
Next chronology cursor: practical 6174 / KI#223, KI#226, KI#227 (plus KI#214 composition).

================================================================================
C031-WIP869 chronology checkpoint - practical 6174 / KI#223, KI#226, KI#227
================================================================================

Status: practical 6174 CLOSED after UI/data-contract revalidation.
Post-primary findings F708-F748 remain confirmed.
F749 / provisional KI#1070 remains UNUSED.
No repository source files modified.

PRACTICAL 6174 / KI#223 - Domestic Advisor Founded numeric sorting
------------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The repaired cell now derives `CyGame().getTurnYear(getGameTurnFounded())` and inserts it through
the project's numeric `setTableIntLabel` wrapper. This removes the old localized BC/AD lexical
ordering while preserving the actual signed game-year ordering used by the existing World Advisor
convention. No parallel textual sort key survives in the same table.

PRACTICAL 6174 / KI#226 - Event pillage fields mislabeled as gold
-----------------------------------------------------------------
Verdict: CLEAN after runtime-semantics recheck.

Both `CvCity::applyEvent` and `CvPlayer::applyEvent` use MinPillage/MaxPillage to roll how many
eligible improvements to destroy. Native `CvGameTextMgr` likewise formats these fields as pillage
counts. Current Sevopedia therefore correctly labels them `Improvements Pillaged`, omits the gold
commerce glyph, and collapses equal min/max to one count. The fact that a roll can be bounded by
available eligible plots is inherent to the event effect, not evidence that the XML fields are
currency or a different unit.

PRACTICAL 6174 / KI#227 - Vote trade-route reward duplicated as requirement
---------------------------------------------------------------------------
Verdict: CLEAN after processVote/eligibility review.

`CvVoteInfo::getTradeRoutes()` is consumed by `CvGame::processVote` through global
`changeTradeRoutes(...)` when a resolution is applied/removed. Resolution eligibility does not
require that many pre-existing trade routes. Retaining the signed All-Cities Trade Routes line in
Effects while removing the Requirements duplicate therefore matches the actual vote contract.

No later whole-album finding reopens KI#223/KI#226/KI#227.

CHRONOLOGY HANDOFF
------------------
Practical 6174 is closed clean.
F749 / provisional KI#1070 remains unused.
Next chronology cursor: practical 6173 / KI#214.

================================================================================
C031-WIP870 chronology checkpoint - practical 6173 / KI#214
================================================================================

Status: practical 6173 CLOSED after irrigation-search state/dominance review.
Post-primary findings F708-F748 remain confirmed.
F749 / provisional KI#1070 remains UNUSED.
No repository source files modified.

PRACTICAL 6173 / KI#214 - irrigation-chain constrained shortest-path state
-------------------------------------------------------------------------
Verdict: CLEAN after adversarial algorithm recheck.

KI#214 replaced one best cost per plot with one best cost per `(plot, steps)` because route length
is independently capped: a cheaper but longer arrival at one plot must not prune a costlier shorter
arrival that can still reach irrigation inside the remaining step budget. Current source retains
that bounded state space (`numPlots * (iMaxPlots + 1)`) and generation stamps.

The separate `iOverwritePenalty` field does not require another state dimension. For any fixed
step count, current accumulated search cost is exactly the route overwrite penalty plus the fixed
350-per-extra-step term. Therefore, within one `(plot, steps)` state, minimizing cost also minimizes
overwrite penalty. Different step counts remain separate states, which is the dimension KI#214
needed to preserve.

Priority-queue stale-node rejection indexes the same `(plot,steps)` slot that was updated on push;
queued steps cannot exceed iMaxPlots; generation rollover clears stamps; old static storage is
ignored by the next generation even if map/max-route dimensions change. No predecessor state is
required because the helper intentionally returns the first source-side Farm that can be built now
and reruns after each completed connector.

Later independent worker/CvUnitAI audits in the album explicitly rechecked this helper and retained
KI#214's `(plot,steps)` model; no current route-state sibling is already open.

CHRONOLOGY HANDOFF
------------------
Practical 6173 is closed clean.
F749 / provisional KI#1070 remains unused.
Next chronology cursor: practical 6172 / KI#221-KI#222.

================================================================================
C031-WIP871 chronology checkpoint - practical 6172 / KI#221-KI#222
================================================================================

Status: practical 6172 CLOSED after Battle-History event-order/association review.
Post-primary findings F708-F748 remain confirmed.
F749 / provisional KI#1070 remains UNUSED.
No repository source files modified.

PRACTICAL 6172 / KI#221 - city-capture battle attribution
---------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

The current matcher requires same global turn, winner/new-owner and loser/previous-owner direction,
exact captured-city coordinates and a lethal (non-retreat) battle. Defended city acquisition
follows the final combat/move into that city plot, so newest-first identifies the battle that
actually cleared the entry. An ordinary undefended conquest has no such preceding lethal row at
that plot and therefore no longer relabels an unrelated same-turn battle elsewhere.

PRACTICAL 6172 / KI#222 - captured-civilian battle attribution
--------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

Current capturable Workers/Settlers do not fight; matching their old unit type to the defeated
escort was therefore fundamentally wrong. The repaired event uses the newly created captured
unit's exact plot and matches the newest same-turn lethal battle at that plot with the correct
capturing-player/old-owner direction. `unitCaptured` is emitted during the move/capture sequence
that follows clearing the plot, so the event ordering makes that newest row the enabling battle.
Several civilians captured by the same entry intentionally patch/accumulate on the same battle row.

Later Battle-History findings KI#334/KI#335 concern information visibility/persistence rather than
capture-row association and do not reopen these predicates.

CHRONOLOGY HANDOFF
------------------
Practical 6172 is closed clean.
F749 / provisional KI#1070 remains unused.
Next chronology cursor: practical 6171 / KI#218-KI#220.

================================================================================
C031-WIP872 chronology checkpoint - practical 6171 / KI#218-KI#220
================================================================================

Status: practical 6171 CLOSED after map-rasterization and Military-Summary executor review.
Post-primary findings F708-F748 remain confirmed.
F749 / provisional KI#1070 remains UNUSED.
No repository source files modified.

PRACTICAL 6171 / KI#218 - PerfectMongoose meteor boundary pairing
-----------------------------------------------------------------
Verdict: CLEAN after adversarial recheck.

`castMeteorUponTheEarth` depends on same-y circle boundary points retaining the opposite-side
insertion pairing before its stable y sort and pairwise row rasterization. The old identity-based
set removed that order without actually coordinate-deduplicating CirclePoint objects. Current
`getCirclePoints` restores the ordered list/append contract, while `getFilledCirclePoints` remains
the separate helper for callers needing filled/unique geometry. No sibling caller still converts
the meteor boundary list through an unordered set.

PRACTICAL 6171 / KI#219 - Military Summary anarchy support waiver
-----------------------------------------------------------------
Verdict: CLEAN after engine-wrapper recheck.

The detailed DLL breakdown overloads intentionally calculate free/paid populations and formula
subtotals regardless of anarchy. Their public `calculateUnitCost(...)` and
`calculateUnitSupply(...)` wrappers return zero immediately while `isAnarchy()`. The Summary now
retains the informative population/rate operands but zeros regular/military/extra/raw Unit Cost,
base/raw Supply and their inflated displayed totals during anarchy, with an explicit Anarchy label.
This matches what gameplay actually charges.

PRACTICAL 6171 / KI#220 - zero-gold Great-General upgrade observations
----------------------------------------------------------------------
Verdict: CLEAN after upgradePrice recheck.

`CvUnit::upgradePrice` can validly return zero after the unit's upgrade discount (including a 100%
leader/Great-General discount) and clamps ordinary results at zero. The Summary's candidate search
uses `-1` only as its no-candidate sentinel, so accepting/appending every `iPrice >= 0` correctly
includes free upgrades in min/average/max/total statistics. No invalid-target branch was found
where zero carries a different meaning.

CHRONOLOGY HANDOFF
------------------
Practical 6171 is closed clean.
F749 / provisional KI#1070 remains unused.
Next chronology cursor: practical 6170 / KI#215-KI#217.

===============================================================================
C031-WIP873 APPEND -- practical 6170 / KI#215-KI#217 adversarial closure
===============================================================================

Chronology cursor reviewed
--------------------------
Practical 6170 / 79410b2dba (local diff: SASBranch_6170_642bcd97e0.diff),
rechecking current composed source rather than only the historical patch.

Verdict
-------
CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#215 -- no-site Settler transport cleanup
--------------------------------------------
CONFIRMED FIXED.
- Current AI_settleMove calls this Settler's passenger-side `unload()` rather than
  `getTransportUnit()->unloadAll()` after a >20-turn no-valid-site condition.
- `CvUnit::unload()` first calls this unit's `canUnload()`, then detaches only this
  unit through `setTransportUnit(NULL)`.
- The old transport-side `unloadAll()` applies the same per-passenger `canUnload()`
  check once the transport itself is in an unload-all-valid location, but then
  detaches EVERY unloadable cargo unit. The repaired passenger call therefore
  preserves the intended legality while fixing the ownership/scope defect.
- If the Settler cannot unload (e.g. invalid revealed domain), it remains attached;
  the immediately-following `getTransportUnit() == NULL` condition stays false, so
  the no-site scrap is deliberately deferred rather than deleting cargo at sea.
- On successful unloading, only this Settler is detached; unrelated Settlers,
  Workers or military cargo remain aboard.
- `setTransportUnit(NULL)` awakens the unloaded unit/group as normal. No group-wide
  cargo mutation survives this path.
- Later explicit `AI_scrapSettlerWithNoValidSite` hardening is a separate cleanup
  contract and composes with KI#215 rather than reopening it.

KI#216 -- duplicate Work Boat demand water area
-----------------------------------------------
CONFIRMED FIXED.
- Current `AI_neededSeaWorkers()` obtains the primary pointer through
  `waterArea(true)` and counts that area once.
- It then obtains `secondWaterArea()` and adds that demand only when the pointer is
  non-null AND differs from the primary pointer.
- This exactly closes the inherited geometry where impassable Ice can make
  `waterArea(true)` and `secondWaterArea()` resolve to the same accessible sea.
- Availability-side accounting already uses the same pointer-identity dedup.
  Demand and availability therefore now agree in the collision case while two
  genuinely distinct accessible seas remain separately counted.
- Later Work Boat / Barbarian-demand work does not remove or bypass this guard.

KI#217 -- mixed-role attack-stack immediate-upgrade counting
------------------------------------------------------------
CONFIRMED FIXED for its documented root.
- Current `SAS_getBestUpgradeForLog(CvUnit const&)` derives
  `kUnit.AI().AI_getUnitAIType()` from EACH iterated stack member.
- Both the behavioral `iCanUpgradeNowUnits` count and its matching diagnostic cost
  pass call that one-argument helper; the attack-stack head role is no longer
  reused for heterogeneous members.
- This matches `CvUnitAI::AI_upgrade`, which likewise values candidates with the
  individual unit's own UnitAI.
- The caller subsequently applies real `canUpgrade(eBestUpgradeForLog)`, including
  current readiness, gold and SAS economic upgrade gates.

Adversarial control: deterministic 90-percent estimate
------------------------------------------------------
- The helper intentionally uses a deterministic `90 * AI_unitValue(candidate)`
  estimate whereas `AI_upgrade()` uses a synchronized 80..100 random factor.
- Therefore it is an estimate rather than an exact prediction of the next random
  upgrade roll. This was challenged because `iCanUpgradeNowUnits` is behavioral.
- Archaeology shows that deterministic 90% estimate is the original KI#156 design;
  practical 6170 / KI#217 explicitly changes ONLY member-role ownership and says
  the deterministic estimate is preserved.
- Current `canUpgrade` is still applied after a candidate is selected, so this is
  not the KI#217 wrong-member-identity defect.
- No hard correctness invariant was established that the ready-attack bypass must
  reproduce one future synchronized random roll exactly; classifying that policy
  approximation as a new bug would overstate the evidence. It remains a tuning /
  naming precision edge, not F749.

Dedup / controls
----------------
- Distinct from F151/KI#474 (same-type human upgrade action-cache representative).
- Distinct from KI#188.x upgrade-city routing and KI#160 upgrade-budget caps.
- Earlier whole-file CvUnitAI audit already found no surviving current root in the
  KI#215/KI#216/KI#217 paths; this chronology pass re-established the detailed
  action contracts rather than relying on that prior verdict.

Authoritative state after this checkpoint
-----------------------------------------
Primary queue: 169 / 169 COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
Next chronology cursor: practical 6169.
No repository gameplay/XML/Python/C++ source file modified by this audit.

===============================================================================
C031-WIP874 APPEND -- practical 6169 / KI#212 adversarial closure
===============================================================================

Practical 6169 / 56162f50f5 reviewed against current CvUnit::maxCombatStr,
CombatDetails declaration/export, Python consumers, and ancestry.

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#212 -- AI Barbarian attacker CombatDetails side field
-------------------------------------------------------
CONFIRMED FIXED.
- Current defender-is-Barbarian AI-handicap branch writes
  `iAIBarbarianCombatModifierTB` (This/defender is Barbarian).
- Current attacker-is-Barbarian AI-handicap branch writes
  `iAIBarbarianCombatModifierAB` (Attacker is Barbarian).
- A repository-wide assignment sweep finds exactly those two assignments for the
  AI Barbarian fields; no second copy/paste survivor writes AB/TB on the wrong side.
- Both fields remain separately declared in CombatDetails, separately exported in
  CyStructsInterface1.cpp, and separately consumed by CvUtil.py.
- The built-in combat message intentionally uses the same text key/sign handling
  for both, so visible logging remains equivalent while the exposed data contract
  is now semantically correct.
- Strength arithmetic uses iExtraModifier directly before the CombatDetails write;
  therefore the repair remains reporting/API-only as documented and introduces no
  combat-odds/RNG/balance change.

Dedup/control:
- KI#211 is the adjacent but independent terrain-attack strength-estimation bug;
  it is the next chronology cursor and is not conflated with this reporting field.

Authoritative state after this checkpoint:
Primary queue 169/169 COMPLETE; F708-F748 confirmed; F749/KI#1070 UNUSED.
Next chronology cursor: practical 6168 / KI#211.
No repository gameplay/XML/Python/C++ source modified.

===============================================================================
C031-WIP875 APPEND -- practical 6168 / KI#211 adversarial closure
===============================================================================

Practical 6168 / b4221a1bc7 reviewed against current CvUnit::maxCombatStr's
known-attacker and unknown-defender/effective-strength modes.

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#211 -- terrain attack sign in unknown-defender attacker strength
-------------------------------------------------------------------
CONFIRMED FIXED.
- `bAttackingUnknownDefender` temporarily treats `this` as the attacker and builds
  attacker-side plot modifiers in `iTempModifier` with the normal defender-side
  signs (e.g. attack bonuses enter as negative terms).
- At the end of the attacker block, unknown-defender mode reverses the complete
  subtotal with `iModifier -= iTempModifier`.
- City attack, Barbarian city attack, hills attack, feature attack, river attack,
  amphibious penalty and kamikaze already use that reversible subtotal.
- Terrain attack now does as well. Before KI#211 it alone wrote its negative term
  directly into `iModifier`, escaping the final reversal and turning a positive
  terrain attack bonus into an effective-strength penalty.
- A direct sibling sweep of this block finds no remaining attacker plot modifier
  that should be reversed but still bypasses `iTempModifier`.
- Known-defender semantics remain unchanged because that mode adds the subtotal
  normally (`iModifier += iTempModifier`).
- CombatDetails reporting continues to receive the conventional signed component;
  only the aggregate path used by the unknown-defender estimate changed.

Current AI consumers of effective/unknown-defender strength therefore receive the
intended terrain contribution; no second sign-asymmetry root established.

Authoritative state:
Primary queue 169/169 COMPLETE; F708-F748 confirmed; F749/KI#1070 UNUSED.
Next chronology cursor: practical 6167 / KI#210.
No repository gameplay/XML/Python/C++ source modified.

===============================================================================
C031-WIP876 APPEND -- practical 6167 / KI#210 adversarial lifecycle closure
===============================================================================

Practical 6167 / fcccb4f5e7 reviewed against doVoteSelection, initDiplomacy,
CvTeam::meet/makeHasMet, and midgame CvPlayer::initInGame colony creation.

Verdict: CLEAN CURRENT REPAIR. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#210 -- vote-selection self pair
----------------------------------
CONFIRMED SAFE/CORRECT, with a lifecycle nuance that refines the historical wording.

Game-start control:
- `CvGame::initDiplomacy()` deliberately executes `team.meet(teamID,false)` for
  every team, so ordinary initialized teams have `isHasMet(self)==true`.
- Therefore the practical-2502 full inner TeamIter self-pair was normally blocked
  by the later `if (!itTeam1->isHasMet(itTeam2))` even before KI#210. On ordinary
  game-start teams the lost self-exclusion was behaviorally inert.

Midgame-created-team reachability:
- A colony/new player can instead be created through `CvPlayer::initInGame` after
  game initialization.
- When this initializes a genuinely fresh team it calls `CvTeam::init`, resetting
  relation arrays, but does NOT reproduce `initDiplomacy`'s `meet(self,false)`.
- Such a team can later participate in a vote source after inheriting tech/state and
  receiving cities. Its own `isHasMet(self)` can therefore still be false.
- Without KI#210, the practical-2502 self pair could reach `meet(self,true)` in that
  supported lifecycle. `makeHasMet` sets the self relation and fires the Python
  firstContact event before its later `eOther == getID()` return guard.
- KI#210's explicit `team2 == team1` skip prevents that synthetic self first-contact
  and restores the original pre-refactor pair-enumeration invariant directly at the
  caller.

Adjacent robustness note (not promoted):
- Fresh midgame teams not receiving the normal game-start self-contact bit is itself
  an asymmetry. A repository/album check did not establish an independent concrete
  gameplay failure for the ordinary singleton-colony case once self vote contact is
  excluded. Many relation consumers explicitly compare different teams, and later
  team-merger paths have their own state migration.
- Keep this as lifecycle robustness debt rather than F749 unless a concrete consumer
  that requires a fresh singleton team's self HasMet state is proved.

The current repair is thus harmless for ordinary initialized teams and meaningful for
midgame-created teams; no caller elsewhere in vote selection bypasses the self guard.

Authoritative state:
Primary queue 169/169 COMPLETE; F708-F748 confirmed; F749/KI#1070 UNUSED.
Next chronology cursor: practical 6166 / KI#209.
No repository gameplay/XML/Python/C++ source modified.

===============================================================================
C031-WIP877 APPEND -- practicals 6166-6164 / KI#209-KI#207 adversarial closure
===============================================================================

Practicals reviewed:
- 6166 / 61c686a4c6 / KI#209 -- CvInitCore::reassignPlayer random-leader provenance.
- 6165 / 6b2c738295 / KI#208 -- CvTeamAI::AI_updateWorstEnemy sticky recompute flag.
- 6164 / d3b1b89822 / KI#207 -- CvTeam::getRandomMemberAlive filtered random index.

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#209 -- independent random civ / random leader state
-----------------------------------------------------
CONFIRMED FIXED.
- `reassignPlayer` saves the target slot's `m_abCivChosenRandomly` and
  `m_abLeaderChosenRandomly` into separate temporaries.
- Copying old->new likewise copies the two independent arrays separately.
- Restoring target->old writes `bRandomCiv` and `bRandomLeader` back to their matching
  arrays; the inherited copy/paste read from the civ flag is gone.
- A direct save/copy/restore comparison found no second random-provenance cross-wire.
- Later setup archaeology already uses current KI#209 as a positive control: the
  remaining True-Starts fixed-leader override root is independent precisely because
  these provenance flags now survive reassignment correctly.

KI#208 -- worst-enemy trade-memory recomputation request
-------------------------------------------------------
CONFIRMED FIXED.
- The tentative `eBestTeam` is selected before the enemy-trade-memory decay loop.
- If the loop changes that team's stored enemy grant/trade values, its attitude/
  enmity can change and the tentative winner must be recomputed.
- Current code sets `bUpdateWorstEnemyAgain = true` and never lets later unrelated
  teams overwrite it back to false.
- Other teams whose values are decayed only become less resentful; modifying them
  cannot create a stronger replacement candidate that independently requires this
  second pass. The trigger therefore has the correct narrow ownership.
- The recursive call uses `AI_updateWorstEnemy(false)`, so memory is not decayed a
  second time.
- Existing F045 remains separate: it concerns incumbent-vs-candidate eligibility in
  `AI_updateWorstEnemy`, not this sticky recalculation flag.

KI#207 -- eligible-member random selection
------------------------------------------
CONFIRMED FIXED.
- `MemberIter` is `PlayerIter<ALIVE,MEMBER_OF>`; its traversal population is living
  members of the requested team.
- Non-human selection counts `getAliveCount()` and traverses that exact population.
- Human-team selection counts `PlayerIter<HUMAN,MEMBER_OF>` and applies the same
  `isHuman()` filter to the MemberIter traversal. If the team is not human, the
  helper intentionally disables the human-only request first.
- `SyncRandNum(iValid)` returns a 0-based index; current `iValidIndex++ == iIndex`
  maps exactly onto the filtered population and makes the last eligible member
  reachable. It no longer uses `nextIndex()`, which counted underlying iterator
  positions/ineligible members and introduced the inherited bias/exclusion.
- Current callers request members only from live war/diplomacy teams, so the helper's
  pre-existing zero-population assumption is unchanged and separately audited.

Authoritative state after this checkpoint
-----------------------------------------
Primary queue: 169 / 169 COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
Next chronology cursor: practical 6163 / KI#206.
No repository gameplay/XML/Python/C++ source file modified by this audit.

===============================================================================
C031-WIP878 APPEND -- practical 6163 / KI#206 adversarial closure
===============================================================================

Practical 6163 / 179e5e1f3e reviewed against current CvPlayerAI::AI_eventValue,
current Event XML, game-speed ConstructPercent data, and later deep event-value audit
findings.

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

KI#206 -- direct random-event BuildingChange production-cost normalization
--------------------------------------------------------------------------
CONFIRMED FIXED.
- The direct BuildingChange valuation maps the BuildingClass to this civilization's
  BuildingType and starts from its production cost.
- Positive production cost is doubled as the inherited rough commerce-value heuristic;
  special `-1` cost retains the inherited fixed shrine-style estimate.
- Current code then multiplies by the active game speed's `ConstructPercent` and,
  crucially, divides by 100. This now has the same percentage normalization structure
  as the two analogous direct-unit event valuation paths (`TrainPercent / 100`).
- `kEvent.getBuildingChange()` is applied only after normalization, so positive
  creation and negative destruction use the same correctly scaled magnitude with the
  intended sign.
- Current shipped game-speed ConstructPercent values range from 33 through 400; the
  old omission therefore was a literal 100x unit error at every speed, not a Normal-
  speed-only coincidence.
- Current Event XML includes live negative BuildingChange choices (e.g. Forge loss),
  so both sign directions are meaningful under the repaired formula.

Dedup / current composition
---------------------------
- Later deep `AI_eventValue` auditing found independent event-valuation roots, notably
  dropped non-city BuildingExtraYield/BuildingExtraCommerce value. The album already
  records those as separate roots and explicitly excludes them from KI#206.
- No second direct BuildingChange ConstructPercent multiplication without `/100`
  survives in the current AI-event valuation path.

Chronology boundary
-------------------
- Practical 6162 is the merge of upstream AdvCiv 1.14 into the SAS line. It introduces
  no independent post-merge repair to adjudicate here.
- The post-SAS archaeology-fix chronology from practical 6282 down through 6163 has
  therefore reached its upstream-merge boundary.
- Next systematic chronology cursor is the newest AdvCivPreSAS history practical,
  practical 4343, continuing backward through the underlying AdvCiv ancestry.

Authoritative state after this checkpoint
-----------------------------------------
Primary queue: 169 / 169 COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
Next chronology cursor: AdvCivPreSAS practical 4343.
No repository gameplay/XML/Python/C++ source file modified by this audit.

===============================================================================
C031-WIP879 APPEND -- chronology-boundary correction + next comparative lane
===============================================================================

CORRECTION TO WIP878 CURSOR
---------------------------
WIP878 tentatively named AdvCivPreSAS practical 4343 as the next systematic cursor.
That would duplicate already-completed work and is therefore superseded here.

Authoritative repository evidence:
- `_1_AdvCiv-SAS/Docs/Source_Analysis/historical_source_archaeology.txt` records
  `Systematic historical commits reviewed: 4,343 / 4,343`.
- AdvCivPreSAS practicals 4343 -> 1250 are COMPLETE.
- K-Mod practicals 1249 -> 1 are COMPLETE.
- The separate AdvCiv-SAS runtime-source phase is also COMPLETE at 1,292 / 1,292
  source-touching SHAs.
- The practical-6282 -> practical-6163 pass just completed by this album was a
  distinct post-primary REPAIR-SIDE adversarial chronology over the resulting SAS
  fix commits, not the original historical discovery sweep.

Therefore:
- Do NOT restart the historical sweep at practical 4343.
- Post-primary repair-side chronology is now complete through its 6162 merge
  boundary.

NEXT LANE -- CIV4CE / VANILLA-LINEAGE DIFFERENTIAL CONTROL
----------------------------------------------------------
The same authoritative archaeology tracker explicitly notes that the supplied
`Civ4CE-main` checkout was discovered only AFTER the systematic AdvCiv/K-Mod/SAS
sweeps were complete and therefore did not certify those historical findings.

Use Civ4CE now as a new supporting comparison source rather than as ancestry proof:
1. compare concrete executable C++ semantics/fixes in Civ4CE's BtS DLL tree against
   current SAS/Base AdvCiv/K-Mod;
2. prioritize differences that look like correctness, lifetime, bounds, ownership,
   arithmetic, visibility or state-transition repairs rather than design choices;
3. for every candidate, prove a supported current SAS path and whole-album dedup
   before consuming a finding number;
4. do not report a Civ4CE difference merely because the implementations differ;
   current SAS behavior remains authoritative.

This lane makes direct use of the additional local source material the user supplied
without repeating the completed current-file or commit-history sweeps.

Authoritative state after this checkpoint
-----------------------------------------
Primary current-source queue: 169 / 169 COMPLETE.
Historical commit-oriented sweeps: COMPLETE.
Post-primary SAS repair-side chronology: COMPLETE through practical 6163 / merge 6162.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
Next cursor: Civ4CE differential control -- establish source/file inventory and begin
with high-signal DLL semantic differences, proving each against current SAS.
No repository gameplay/XML/Python/C++ source file modified by this audit.


===============================================================================
C031-WIP880 APPEND -- Civ4CE/Taurus comparative control tranche 1
===============================================================================

Scope
-----
New post-chronology comparison lane using the user-supplied Civ4CE and Taurus SDKs as
supporting controls. This tranche reviewed the highest-signal UOP/Taurus state-
transition fixes around team/player/game command plumbing rather than broad AI design
differences.

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

1. UOP potential-enemy forced-peace protection -> current CvTeamAI::AI_mayAttack
-------------------------------------------------------------------------------
CONFIRMED PRESERVED/SUPERSEDED CORRECTLY.
- Taurus/UOP changed global `isPotentialEnemy` so an undeclared sneak-attack target is
  considered attackable only when `canDeclareWar` also permits the declaration; this
  prevented invasion movement during forced peace followed by unit bumping.
- AdvCiv replaced that global strategic helper with `CvTeamAI::AI_mayAttack`.
- Current `AI_mayAttack(eDefender)`:
  * returns true immediately for actual war;
  * canonicalizes a vassal defender to its master for the war-plan lookup;
  * requires an imminent war plan;
  * rejects `isForcePeace(eDefenderMaster)` and `isForcePeace(eDefender)` before
    reporting the target attackable.
- The source retains the original UOP bugfix comment and explicitly explains why it
  can avoid the broader `canEventuallyDeclareWar` call: an impossible war plan has to
  be abandoned elsewhere.
- Current plot/unit wrappers establish a real defender team and the full CvTeamAI
  audit has already saturated `AI_mayAttack` caller semantics. No missing UOP guard
  survives here.

2. UOP CvPlayer::removeBuildingClass count semantics
---------------------------------------------------
CONFIRMED PRESERVED.
- Taurus/UOP fixes Firaxis `setNumRealBuilding(eBuilding,0)`, which destroyed all real
  copies in the selected city, to decrement exactly one copy.
- Current SAS maps the civilization's building class to BuildingType, finds the first
  city with a positive real count, and executes
  `setNumRealBuilding(eBuilding, getNumRealBuilding(eBuilding) - 1)` before breaking.
- This is exactly the UOP invariant and remains Python-exposed through the normal
  player wrapper.

3. UOP CvPlayer::canDoCivics(NO_CIVIC) MP crash guard
-----------------------------------------------------
CONFIRMED PRESERVED.
- Current SAS returns true for `eCivic == NO_CIVIC` before any `CvCivicInfo` lookup,
  matching the UOP simultaneous-turn MP crash fix.
- Later force-civic, Python override, tech/civic-option and no-slavery gates operate
  only on real civic IDs. No earlier dereference was reintroduced.

4. Permanent-Alliance tech-progress / no-tech-brokering UOP block
-----------------------------------------------------------------
CONFIRMED INTENTIONALLY SUPERSEDED, not a missing fix.
- Taurus/UOP avoided copying overflow progress for already-known techs and cleared
  NoTradeTech when the absorbed teammate had independently researched a tech.
- Current AdvCiv/K-Mod PA merge is structurally different:
  * research progress is transferred only when NEITHER constituent already has the
    tech;
  * incomplete progress is normalized proportionally across differing team research
    costs;
  * the old UOP NoTradeTech merge code is retained as commented archaeology because
    K-Mod/AdvCiv applies no-brokering state when techs are shared in `shareItems`.
- The just-completed repair-side PA chronology adversarially audited tech, project,
  contact, counter and revival migration in current composition. No Taurus-only
  correctness obligation is missing here.

5. Barbarian passive-research rounding UOP fix
----------------------------------------------
CONFIRMED SUPERSEDED MORE STRONGLY.
- Taurus/UOP changes the old nested integer truncation into one fraction and clamps a
  positive research award to at least one beaker.
- Current `CvTeam::doBarbarianResearch` uses `scaled` arithmetic, per-tech Barbarian
  modifiers, game-speed/start-era-aware base cost, a minimum of one, and an explicit
  no-overflow upper clamp. The original truncation root is therefore absent.

6. Water-city Python callback semantics
---------------------------------------
CONFIRMED PRESERVED after refactor.
- Taurus/UOP fixed the callback so a water plot remains invalid unless
  `canFoundCitiesOnWater` explicitly authorizes it.
- AdvCiv moved the logic into `CvPlot::canEverFound`; current code checks `isWater()`
  first, calls `canFoundWaterCity`, returns true only on explicit approval, otherwise
  false. The UOP comment remains in source.

7. Simultaneous-team-turn activation break
------------------------------------------
CONFIRMED PRESERVED.
- Current `CvGame` breaks only after the first ALIVE team is found and activated,
  matching the UOP fix rather than breaking after slot 0 regardless of liveness.

8. Recycled-player names/colors and upgrade-all pointer lifetime
---------------------------------------------------------------
CONFIRMED PRESERVED.
- Current add-player/recycled-slot logic retains the UOP identity cleanup / color
  collision semantics (with later AdvCiv/SAS initialization work layered above it).
- Current `CvNetDoCommand` stores the UnitType before iterating an Alt+all command,
  so deleting/upgrading the original unit cannot invalidate the comparison pointer.

Comparative-lane policy / next cursor
-------------------------------------
- Civ4CE remains useful as vanilla-lineage behavior, but its final DLL is close enough
  to the BtS/K-Mod ancestry that raw differences mostly reflect K-Mod/AdvCiv redesign.
- Taurus's explicit UOP markers provide the higher-signal finite checklist.
- Next tranche: labeled Taurus/UOP `CvCity` and `CvUnit` mechanics, prioritizing
  ownership/lifetime, movement/combat, build completion, cargo, visibility and
  arithmetic fixes. Each candidate must still be proved against CURRENT SAS and whole-
  album dedup before consuming F749.

Authoritative state
-------------------
Primary current-source queue: 169 / 169 COMPLETE.
Historical commit-oriented sweeps: COMPLETE.
Post-primary SAS repair-side chronology: COMPLETE.
Civ4CE/Taurus comparative lane: tranche 1 COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source file modified by this audit.


===============================================================================
C031-WIP881 APPEND -- Civ4CE/Taurus comparative control tranche 2A
CvCity/CvUnit mechanics -- high-signal first pass
===============================================================================

Verdict: CLEAN CURRENT DATA/COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Scope
-----
Reviewed the first high-signal Taurus/UOP CvCity/CvUnit/CvPlot/CvSelectionGroup
mechanics markers against current SAS, focusing on lifetime, cargo ownership, combat
context, building-count state and direct improvement-yield semantics.

Preserved/superseded UOP fixes
------------------------------
- `CvUnit::canLoadOnto` (renamed from canLoadUnit) still rejects loading a cargo unit
  onto its current transport, preserving the UOP anti-recursive-cargo invariant. It
  also rejects transports that are themselves cargo and retains normal cargo-space,
  team, hidden-nationality and movement checks.
- `CvUnit::maxCombatStr` still guards attacker-side plot modifiers behind both
  `pAttacker != NULL` and `pAttackedPlot != NULL`, preserving the UOP null-plot fix.
  The later KI#211 sign audit independently saturated the unknown-defender modifier
  block and found no surviving sibling.
- `CvCity::kill` still clears dead-city trade routes before detaching the plot and
  still restores deterministic river-required features (e.g. floodplains) under the
  current SAS define policy.
- `CvPlayer::removeBuildingClass` remains a one-copy decrement and the building
  completion path uses the building class's configured extra-player-instance count,
  preserving both related UOP count semantics.
- `CvCity::setBuildingHappyChange` and `setBuildingHealthChange` retain the UOP rule
  that stored class deltas affect city aggregates only while the civilization's
  corresponding building is active; positive and negative old/new contributions are
  removed/reapplied with the proper good/bad accumulator.
- K-Mod's iterative `CvSelectionGroup::continueMission` rewrite supersedes the old UOP
  human MOVE_TO queue workaround; current mission completion/activation logic has
  since received much deeper SAS queue-state auditing and KI fixes.

Taurus UOP `calculateImprovementYieldChange` clamp -- REJECTED AS CURRENT FINDING
-------------------------------------------------------------------------------
Taurus/UOP clamps the improvement's standalone delta so it cannot reduce the plot
below its current nature yield. K-Mod deliberately removed that local clamp and current
SAS inherits the raw-delta API, while `calculateYield()` clamps the final total to >=0.

Why this is NOT F749 in current SAS:
- current XML contains only one negative base improvement yield: Workshop `-1 food`;
- Workshop requires at least 1 natural food through `PrereqNatureYields`, so its raw
  negative delta cannot exceed the plot's available nature food;
- current tech/civic/route improvement-yield data does not introduce a stronger
  negative improvement delta that can violate the final-yield floor;
- therefore direct current AI/evaluator callers cannot presently observe an impossible
  over-negative improvement delta from shipped data.

Keep this as a mod-extension/API robustness difference: if future XML permits a more
negative improvement delta than the underlying nature yield, callers that consume
`calculateImprovementYieldChange` directly could rank an unrealizable negative value
while actual `calculateYield` floors at zero. That is not a supported current defect.

Next comparative cursor
-----------------------
Continue the remaining labeled Taurus/UOP mechanics markers in:
- CvUnit combat/movement/range-strike/heal/pillage/spread paths;
- CvPlot improvement/feature lifecycle;
- any CvCity production/culture state marker not already subsumed above.
Prioritize markers whose original correctness invariant is no longer visibly retained
in current source, and prove current reachability/data before consuming F749.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative lane: tranche 1 + tranche 2A COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP882 APPEND -- Civ4CE/Taurus comparative control tranche 2B
remaining CvUnit/CvPlot mechanics
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Confirmed retained/superseded invariants
----------------------------------------
- `CvUnit::canMoveInto` still evaluates terrain impassability independently from
  feature impassability. The UOP fix that removed the old mutually-exclusive `else`
  structure is preserved, with later AdvCiv own-border/loading refinements layered on
  top.
- The UOP consistency rule for post-attack movement remains incorporated in current
  `canMoveInto`: spent attacks block movement into a visible defended attack target,
  while defenseless capture movement and delayed-next-turn path checks retain the later
  K-Mod/AdvCiv exceptions.
- `CvUnit::canPillage` still rejects cargo units explicitly.
- `CvUnit::canAirDefend` still rejects cargo ground interceptors; current code also
  uses the observer-safe/revealed valid-domain predicate.
- LunarMongoose FeatureDamageFix remains active: `healTurns` subtracts feature turn
  damage and returns MAX_INT when net healing is nonpositive; `canHeal` rejects that
  state rather than offering a useless heal mission.
- `CvUnit::maxCombatStr` retains the UOP attacker-plot NULL guard; current unknown-
  defender handling and KI#211 terrain-sign correction compose with it.
- Range strike retains all three UOP correctness points reviewed here: target-plot
  visibility is required, execution validates from the attacker's real current plot,
  and the legacy defender-group mission-timer write remains removed because of OOS/
  animation-lifetime concerns.
- The religion-spread Python veto remains moved below inexpensive native rejection
  tests; this is an efficiency-only UOP marker and current SAS's Python-caller wrapper
  preserves the same result semantics.
- Natural feature disappearance/growth and improvement bonus discovery retain game-
  speed scaling under current `CvPlot` refactors. The exact helper names/scaled value
  source differ from Taurus but the per-turn probability invariant is preserved.
- Dead-city trade-route cleanup, floodplain restoration, active-world-wonder counting,
  building-instance completion and building happy/health updates are retained or
  structurally superseded by current CvCity code; no original UOP invariant was found
  missing.

Rejected differential watch retained from WIP881
------------------------------------------------
The K-Mod removal of Taurus/UOP's local negative-improvement-yield clamp remains an
intentional raw-delta API difference. Current shipped SAS XML cannot produce an
improvement delta more negative than the underlying natural yield, so no supported
current mis-ranking is proved. Keep as extension robustness only.

Next comparative cursor
-----------------------
Move to explicit Taurus/UOP AI correctness markers in CvPlayerAI / CvCityAI / CvUnitAI
and compare them against current K-Mod/AdvCiv/SAS rewrites. Prioritize small bugfix
invariants (wrong variable, missing legality gate, bad population, stale pointer,
arithmetic/bounds) rather than broad AI preference differences.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative lane: state tranche + mechanics tranches COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP883 APPEND -- Civ4CE/Taurus comparative control tranche 3A
CvPlayerAI high-signal UOP bugfix sample
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Reviewed high-signal Taurus/UOP AI corrections against current rewritten K-Mod/AdvCiv/SAS
code, prioritizing tiny correctness invariants rather than strategic preference changes.

Confirmed preserved/superseded examples
---------------------------------------
- Settler founding claim-threshold scaling retains the missing `/100` correction in the
  current CitySiteEvaluator-era implementation rather than using the old raw-percent
  magnitude.
- Target-city valuation now uses the stronger current `AI_cityWonderVal` / active-world-
  wonder helpers; the UOP omission of wonder value is not present.
- `AI_unitValue` retains the null-area guard before area corporation counts, preventing
  the Taurus/UOP mod-only crash case.
- Missionary culture-victory valuation uses nested Culture1 -> Culture2 -> Culture3
  stages, preserving the UOP copy/paste correction.
- Executive valuation no longer contains the assignment-in-condition typo (`=` versus
  `==`) from the Firaxis path; current corporation-spread valuation is substantially
  rewritten.
- `AI_civicValue(NO_CIVIC)` keeps the early crash-safe return and later civic scoring has
  received much deeper SAS auditing/fixes.
- `AI_getMinFoundValue` retains the UOP expense-sign correction:
  `calculateInflatedCosts() + max(0,-getGoldPerTurn())`, then layers K-Mod/AdvCiv
  available-income and corporation-maintenance adjustments above it.
- Espionage malicious-action attitude boundary retains the intended threshold semantics
  rather than the old `<50` equivalence mistake.
- Strategy detection restricts mobile anti-air/artillery capability to land units; sea
  or air units with movement/interception/bombard stats do not satisfy those land-blitz
  flags merely because they are mobile.
- AI diplomacy's tribute-demand gating retains the UOP principle that an already chosen
  pending war must not be accidentally blocked by accepted tribute/forced peace.

No sampled UOP invariant above is missing in current SAS, and no whole-album-unowned
current bug was proved.

Next comparative cursor
-----------------------
Finish labeled Taurus CvCityAI/CvUnitAI correctness markers (wrong accumulator/variable,
hurry/defender calculations, best-build state, settler/assault/paradrop/missionary
transport legality), then review any remaining small CvPlayerAI markers not already
subsumed by later full-file findings.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative lane: tranches 1, 2A, 2B, 3A COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP884 APPEND -- Civ4CE/Taurus comparative control tranche 3B
CvCityAI/CvUnitAI labeled correctness markers
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Representative UOP invariants re-proved in current rewritten AI
---------------------------------------------------------------
CvCityAI:
- Worker-production shortfall checks compare existing workers AGAINST needed workers in
  the correct direction; current SAS later refactors strengthen the ratio but do not
  revive the original reversed comparison.
- Building-specialist marginal value applies low-food / already-runnable penalties to
  the local `iTempValue`, not the function-wide accumulated value.
- Newly acquired-city defender logic has the original duplicate-condition/control-flow
  error removed; current K-Mod/AdvCiv defender logic is substantially rewritten.
- Financial weighting uses the corrected expense sign (`+ max(0,-GPT)`) where that
  inherited calculation survives; newer paths use player-level available-income helpers.
- Gold-hurry logic rejects nonpositive `hurryGold` before using it as a payback-cost
  numerator.
- Culture-pressure ownership tests use the city owner rather than a city-ID/player-ID
  cross-type comparison.
- Crush-strategy threat adjustment scales the current rival's temporary/civ factor,
  not the whole accumulated city-threat value.

CvUnitAI:
- Settler site scoring still requires a reachable safe-territory path (with later SAS
  search-range/quality refinements), preserving the UOP fix that stopped unreachable
  same-area sites from dominating.
- Assault Sea post-separation retreat checks the unit's UnitAI, not UnitType, and waits
  until the remaining group can all move before retreating.
- Paradrop bonus value uses a revealed/nonobsolete target bonus and preserves a positive
  tactical floor rather than letting ordinary resources become zero/negative targets.
- Special sea missionary/executive transport scoring doubles the local corporation/
  missionary candidate value for a one-turn path rather than accidentally doubling the
  unrelated outer accumulator.

Cross-control / dedup
---------------------
Many of these functions were rewritten far beyond Taurus by K-Mod and then audited in
current SAS for worker, production, threat, transport and city-site bugs. No Taurus
marker exposed a current unowned sibling after those later fixes.

Next comparative cursor
-----------------------
Finish the remaining Taurus/UOP non-AI utility/info/serialization markers (CvInfos,
Python bindings, network/message/setup and any small game-core helper not already
covered), then assess whether the comparative checklist has yielded any unreviewed
high-signal semantics before closing or extending the lane.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative lane: state/mechanics/AI tranches through 3B COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP885 APPEND -- Civ4CE/Taurus comparative control tranche 4
CvInfos / bindings / setup / message / utility + text-help labeled UOP markers
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Bindings / popup / network-message controls
--------------------------------------------
- CyInfoInterface1 still exports CvUnitInfo::isOnlyDefensive.
- CyInfoInterface2 binds CvImprovementInfo::getHappiness to the actual happiness getter,
  not the old erroneous defense-modifier getter.
- CvDLLWidgetData retains the Free-Tech popup/command state guard: a free-tech widget
  action is rejected unless the active player is currently choosing a free tech, and
  the state is cleared only on a legitimate choice.
- CvDLLButtonPopup retains the UOP liberation fixes: the liberation-player local is
  distinct and a city is not offered for liberation to a player currently at war with
  the popup owner.
- CvMessageData's Upgrade-All command snapshots the original unit type before iterating
  player units, so deleting/upgrading the selected unit cannot invalidate the type test.

Game/setup state controls
-------------------------
- Simultaneous-team-turn activation still breaks only after finding and activating the
  first ALIVE team; dead team slots do not terminate the search.
- addPlayer/recycled colony slots still clear stale custom leader/civilization names when
  the new identity differs, and an existing player does not invalidate its own default
  player color.
- The old CvGameCoreUtils potential-enemy forced-peace fix remains semantically covered
  by current CvTeamAI::AI_mayAttack (verified in WIP880).

CvGameTextMgr / UI bugfix controls
----------------------------------
Current help generation preserves the labeled UOP correctness fixes:
- negative civic military-unit, largest-city, building happiness/health and analogous
  building/improvement happiness values display absolute magnitudes with the correct
  unhappy/unhealthy glyph rather than a signed number plus negative glyph;
- non-state-religion civic happiness uses the corrected state-religion branch and passes
  the required numeric/symbol parameters;
- building spy-exposure text is conditional on positive espionage defense;
- commerce-happiness-per-level text uses an absolute divisor;
- FeatureDamageFix distinguishes turn damage from healing in feature help and preserves
  the plot-hover negative-damage/healing presentation;
- GameFontFix still omits the erroneous extra symbol increment that broke exact 49/74
  bonus-resource counts.

CvInfos efficiency-marker disposition
--------------------------------------
The Taurus CvInfos markers in this tranche (`m_bAnySpecialistYieldChange` and
`m_bAnyBonusYieldModifier`) are efficiency flags, not independent gameplay semantics.
AdvCiv later replaced the raw 2-D arrays/booleans with typed sparse/enum-map storage;
current help code iterates the actual non-default specialist/bonus yield maps. Their
removal therefore does not reopen a correctness bug.

Comparative-lane status
-----------------------
All explicit labeled Taurus/UOP marker families sampled across state transitions,
mechanics, CvPlayerAI/CvCityAI/CvUnitAI, text/UI, bindings, network commands and setup
have now either been re-proved in current SAS or identified as intentional later
K-Mod/AdvCiv redesigns. The one notable API divergence found during this lane -- raw
unclamped calculateImprovementYieldChange -- is not reachable as an over-negative
improvement delta in current shipped XML and remains extension robustness only.

No current whole-album-unowned defect was proved in this tranche.

Next comparative cursor
-----------------------
Broaden beyond explicit UOP labels: triage small executable Civ4CE/Taurus-vs-current-SAS
semantic differences (guards, bounds, ownership, iterator populations, arithmetic and
state transitions) that are not already marked by UOP comments. Exclude broad AI/design
changes. Every candidate still requires a supported current-SAS reproduction and whole-
album dedup before consuming F749.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus explicit-UOP comparative marker tranches: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP886 APPEND -- broader comparative lane: Python binding contract differential
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Method
------
Extracted Boost.Python `.def("pythonName", &CppClass::member)` bindings from current
SAS, Taurus and Civ4CE, then compared same-name mappings. Reference disagreements were
also used as controls for known UOP binding repairs. Finally stripped comments and
checked each current exposed Boost.Python class for duplicate live Python names mapped
to different C++ members.

Results
-------
No current SAS regression was found. Several differences instead demonstrate deliberate
AdvCiv/SAS API cleanup or correction of reference-tree copy/paste bindings:
- `getReligionRequired` now binds CvEventTriggerInfo::getReligionRequired; both older
  reference trees bind that Python name to getBonusRequired.
- `isPickOtherPlayerCity` now binds the matching predicate rather than isPickCity.
- `getProjectProductionModifier` now calls the project-specific city getter rather than
  generic getProductionModifier.
- the old duplicated recon/paradrop copy-paste registrations are gone; `canRecon` and
  `canReconAt` map only to recon methods, with paradrop exposed under its own names.
- `getGeneralProductionTurnsLeft` preserves the old Python API name while binding the
  single surviving equivalent production-turns method after AdvCiv removed redundancy.
- default unit/building Python names preserve compatibility while the C++ getters were
  type-safely renamed from *Index to getDefaultUnit/getDefaultBuilding.

Taurus-vs-Civ4CE binding differences line up with known UOP fixes: Taurus adds the
correct CvImprovementInfo::getHappiness and CvUnitInfo::isOnlyDefensive exposure (plus
an added player health binding); current SAS contains those corrected surfaces.

A live-class duplicate-name scan (comments stripped, Boost.Python classes separated)
found no Python method name bound to two different C++ members within one current class.
The apparent old `getHappiness -> getDefenseModifier` registration remains only as a
comment beside the corrected binding.

Next comparative cursor
-----------------------
Switch to another finite, high-signal cross-source contract: Python-event / network /
serialization argument and state-transition surfaces, prioritizing same event/message
names whose payloads or ownership differ between Civ4CE/Taurus and current SAS. Avoid
broad strategic/game-design differences; require current reachable proof + album dedup.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus explicit-UOP comparative markers: COMPLETE.
Reference-assisted Python binding differential: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP887 APPEND -- broader comparative lane: Python-event/network-message contracts
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Python event reporter differential
----------------------------------
Parsed the 74 event-reporting functions shared by current SAS, Taurus and Civ4CE.
- Shared event payload arity and ordering match across all three trees.
- SAS differences in event argument expressions are type-safe enum values where the old
  trees explicitly cast the same values to int; no PlayerTypes/TeamTypes swap or field
  reorder was found.
- postEvent retains the expected six common debug/modifier flags; SAS intentionally
  treats Debug mode as cheat-enabled for its final boolean as well.

Current network-message read/write symmetry
-------------------------------------------
Parsed all current CvMessageData subclasses with PutInBuffer/SetFromBuffer.
- 28/28 message classes have matching serialized field sequences in the same order.
- No class has a local write/read field-count or ordering skew.

Reference-schema differences
----------------------------
The shared classes whose SAS schemas differ from Civ4CE/Taurus are deliberate protocol
redesigns rather than dropped fields:
- CvNetPushOrder: K-Mod explicit save/pop/queue-position semantics replace BtS
  Alt/Shift/Ctrl queue encoding.
- CvNetPushMission: typed MovementFlags plus the AdvCiv modified-mission boolean are
  serialized and consumed symmetrically.
- CvNetUpdateCivics: AdvCiv typed CivicMap serializes itself instead of a raw looped
  fixed array.
- CvNetFPTest and CvNetCivLeaderSetup are explicit AdvCiv-added message classes; the
  latter transports the full civ/leader random-choice provenance maps used by setup.

No old shared payload field vanished without a corresponding semantic rewrite, and no
current Python/network whole-album-unowned defect was proved.

Next comparative cursor
-----------------------
Reference-assisted Cy* wrapper null-guard differential: identify shared wrapper methods
where Civ4CE/Taurus protect managed NULL objects but current SAS appears not to, then
prove/reject each candidate against AdvCiv's wrapper macros and actual Python callers.
This is especially relevant because managed CyCity/CyPlot NULL wrappers have produced
real findings elsewhere in the album.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus explicit-UOP markers, binding differential, and Python-event/network
contract differential: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP888 APPEND -- broader comparative lane: Cy* managed-NULL guard differential
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Method
------
Compared shared CyUnit/CyCity/CyPlot/CyPlayer/CyTeam/CyArea/CySelectionGroup/CyDeal
wrapper methods across Civ4CE, Taurus and current SAS, prioritizing methods where both
reference trees visibly guard the wrapper/native pointer but current code initially
appeared not to.

Results
-------
The small candidate set was false-positive/stronger-current behavior:
- CyCity::isConnectedTo and CyPlot::isConnectedTo now validate the owning wrapper, the
  argument CyCity wrapper, and its native CvCity pointer before dereference.
- CyPlot::isEnemyCity validates both CyUnit wrapper and native CvUnit pointer.
- CyPlot::movementCost validates the target plot, unit wrapper/native unit, and source
  plot wrapper/native plot.
The older reference implementations generally guarded only the owning wrapper and could
still dereference a managed-null argument wrapper.

Queue-object ownership control
------------------------------
CyCity::getOrderFromQueue and CySelectionGroup::getMissionFromQueue also illustrate a
stronger current contract: under the already-confirmed KI#480 repair they copy internal
OrderData/MissionData nodes into independent heap-owned objects, making the existing
Boost.Python owning policy truthful. The reference trees return internal queue storage.

No shared wrapper method with a reference-backed managed-NULL guard was proved to have
lost equivalent protection in current SAS.

Next comparative cursor
-----------------------
Systematically audit Boost.Python return-ownership policy, especially every current
`manage_new_object` binding and pointer-return wrapper. Verify that each returned pointer
is genuinely heap-owned (or otherwise compatible with the policy) rather than borrowed
engine/internal storage. Dedup against KI#480 and prior Cy* findings before consuming
F749.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus marker/binding/event-network/wrapper-guard comparative tranches: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP889 APPEND -- Boost.Python owning-return policy audit
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Scope / method
--------------
Enumerated every current Boost.Python method binding using
`return_value_policy<manage_new_object>` and mapped the bound C++ method to its concrete
implementation. Also checked the utility free functions registered with the same owning
policy.

Results
-------
- 54 method bindings under manage_new_object all return genuinely fresh heap objects or
  wrappers from their current implementation.
- The six utility object-return helpers checked (plot direction/cardinal/XY/city and
  IDInfo city/unit lookup) explicitly allocate new CyPlot/CyCity/CyUnit wrappers.
- CyCity::getOrderFromQueue and CySelectionGroup::getMissionFromQueue now allocate
  independent OrderData/MissionData copies under the already-confirmed KI#480 repair;
  they no longer expose internal queue nodes through an owning policy.
- Ordinary CyCity/CyPlot/CyUnit/CyArea/CySelectionGroup/CyDeal/CyReplayInfo returns use
  the expected `new CyX(nativePointer)` wrapper pattern; native engine storage itself is
  not transferred to Python.

No borrowed/internal pointer is currently exposed as Python-owned storage through a
manage_new_object binding.

Next comparative cursor
-----------------------
Audit the complementary ownership direction: object/pointer-returning Python bindings
that do NOT use an owning return policy. Look for implementations that allocate `new`
and therefore leak/mis-own, or for reference policies whose returned storage can expire
before Python. Dedup against KI#480 and prior wrapper lifetime findings.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative marker, binding, event/network, NULL-wrapper and owning-return
policy tranches: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP890 APPEND -- complementary borrowed/reference return-policy audit
===============================================================================

Verdict: NO NEW ROOT. Strong dedup control.
F749 / provisional KI#1070 remains UNUSED.

Systematic result
-----------------
Enumerated current `reference_existing_object` bindings and checked implementations for
fresh heap allocation versus borrowed/static storage.

Two structural fresh-allocation/reference-policy cases surfaced:
1. CyGame::getNextUnitInCycle -> `new CyUnit(...)` while the binding uses
   reference_existing_object.
   This is NOT new: whole-album dedup identifies it as already-confirmed F688, including
   the shipped Main Interface callers and the per-call wrapper leak proof.
2. CyArea::getTargetCity -> `new CyCity(...)` under reference_existing_object.
   This too was already examined by the primary wrapper audit. A complete shipped-Python
   census found zero callers; all current target-city consumers use native
   CvArea::AI_getTargetCity. It therefore remains documented latent/API robustness rather
   than a current gameplay finding.

All remaining borrowed-policy bindings match their storage lifetime:
- CyMap::sPlot/sPlotByIndex and cysPlot* helpers return deliberate static wrappers;
- CyGlobalContext returns persistent singleton/wrapper/info storage;
- ArtFileMgr/info getters return globally owned info records;
- EventTriggeredData and deal trade-list accessors return live engine-owned storage.
No other fresh `new` object is registered as borrowed.

This tranche independently re-proved F688 and the prior getTargetCity latent note but did
not consume F749.

Next investigation lane
-----------------------
The supplied Civ4CE/Taurus explicit marker, binding, event/network and wrapper-lifetime
surfaces are now substantially saturated. Pivot to current-SAS mechanical invariant
scans using small, finite static patterns: unsafe empty-container indexing, signed shift/
overflow, array/buffer bound mismatches, divide/modulo denominators, enum-domain mixups,
and nested-loop wrong-variable patterns. Every hit still requires supported reachability,
semantic proof and whole-album dedup before numbering.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus comparative surfaces through wrapper ownership/lifetime: SATURATED for the
high-signal contracts reviewed here.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.


===============================================================================
C031-WIP891 APPEND -- current-SAS mechanical invariant scan: empty-container indexing
===============================================================================

Verdict: CLEAN CURRENT COMPOSITION / STRONG DEDUP CONTROL. No new root consumed.
F749 / provisional KI#1070 remains UNUSED.

Scope / method
--------------
Systematically enumerated current top-level DLL `vec[0]`, `.front()` and `.back()` uses, then
triaged the dynamic-container cases for a supported zero/empty path. Fixed arrays, strings and
statically sized engine buffers were separated from std::vector/list/queue cases. Every surviving
candidate was checked against surrounding guards, producer/consumer population and whole-album
ownership rather than treating a debug FAssert as a release guard.

Previously open WIP890 cases closed
-----------------------------------
- SASGameRecordLog.cpp: all three material `.back()` candidates immediately follow their own
  `push_back` in the same branch (`g_aSASGameRecordWars`, plot-change groups and initial-tech
  groups). Palette `[0]` uses are fixed-size vectors constructed to the corresponding XML/info
  counts before parsing/legend use.
- CvInfo_Unit/CvInfo_Building Pass3: `m_aszExtraXMLforPass3[0]` is preceded by an executed
  `size() < 1` return guard. The XML read path always pushes the one deferred DefaultUnit /
  DefaultBuilding / VoteSource Civic string before Pass3.
- CvSelectionGroup temporary vectors: `apUnitsLeftBehind[0]` and `aAwake[0]` are explicitly under
  `!empty()`. The `apCanMove[0]` split path is entered only under `canAnyMove()`, whose exact
  implementation returns true iff at least one member has `canMove()`; that same predicate fills
  `apCanMove` immediately before indexing.
- CvTeam queued-war fronts: all six queues have one producer (`queueWar`) that pushes one field to
  every queue together and one consumer (`triggerWars`) that pops every queue together. No other
  mutation site exists, so testing `attacking_queue.empty()` is sufficient for all six fronts.
- RiseFall `chapters[0]`: initialization clamps chapter count to living civs, rejects
  `maxChapters < 1`, then unconditionally pushes the final RFChapter before any chapter-zero use.

Additional dynamic-container controls
-------------------------------------
- CvUnit failed missionary spread: the ranked-religion vector is nonempty on every legal spread
  attempt. `canSpread` requires the city not already to have the attempted religion; the ranking
  loop nevertheless always includes that attempted religion as a candidate unless the city is its
  Holy City. `CvGame::setHolyCity` always adds the holy religion to the city, so that excluded
  state contradicts `canSpread`'s prerequisite. Hence `partial_sort(begin,begin+1,end)` and `[0]`
  have at least one candidate on a legal call.
- UWAIAgent war-enemy `[0]` uses return before indexing when their vectors are empty. UWAICache's
  pairwise-duration `[0]` is preceded by an empty return; the land-only companion is under
  `!empty()`.
- CombatOdds position zero initializes its vector itself when empty before every later `[0]` use.
- CvGame SAS version-history `.back()` is under `!empty()`.
- CvGameTextMgr owner-unit `[0]` is used only when the selected owner bucket has size exactly 1;
  Font-symbol `.back()` calls all follow an immediately preceding row `push_back`.
- InvasionGraph `kCycle[0]` is called only after `simulateComponent` constructs a detected cycle,
  rejects size 1 and invokes `breakCycle` only when nonempty; the algorithmic cycle contract is
  therefore >=2.
- CvPlayerAI culture countdown `[0]` uses are under `iVictoryCities > 0 && getNumCities() >=
  iVictoryCities`, after one countdown value is pushed per city. Tech-path front/back uses occur
  only after `tech_paths.empty()` returns; each created path pushes its endpoint before any use.
  Deal queues use `.front()` only inside a while condition requiring both queues nonempty.
- PlayerHistory vector serialization has explicit `iSize > 0` guards.
- Unit/Building/Tech prerequisite-vector stream `[0]` calls are all under positive serialized/count
  guards.
- CvArtInfoFeature's failure fallback to first FeatureArtPiece is safe for current shipped data:
  every one of the eight FeatureVariety entries in CIV4ArtDefines_Feature.xml contains at least one
  FeatureArtPiece (minimum 1; normal tiling varieties contain 15). Empty-art XML remains extension
  robustness, not a current-data bug.
- `stats::*` vector helpers contain only FAssert preconditions internally, so their current callers
  were rechecked rather than trusted. The already-confirmed F687/SPaH takeover path remains the
  known reachable empty-distribution violation. Other sampled/current uses build at least one item
  or have explicit empty guards (UWAI distances/peace counters, building-count medians, True Starts,
  StartingPositionIteration, espionage target medians, etc.). No second whole-album-unowned empty
  `stats` caller was proved.

Known/deduplicated formal issues retained
-----------------------------------------
- CvMap replay texture serialization / pointer return can form `&m_replayTexture[0]` for an empty
  vector. This was already documented by the primary audit as formal C++ UB with no demonstrated
  supported-MSVC gameplay failure; no new root is consumed here.
- CvPlayerAI `m_aeBestTechs[0]` zero-length serialization has the same already-documented formal
  portability/hardening status.
- Existing F687 remains the consequential empty-vector finding in this family.

Next mechanical cursor
----------------------
Systematic denominator/modulo scan in current SAS: enumerate integer/fixed-point division and `%`
uses where the divisor is not a compile-time nonzero constant; prioritize values derived from
container sizes/counts, XML/configuration, game populations and dynamically computed differences.
Require a supported zero path plus actual execution consequence and whole-album dedup before
consuming F749. Treat already-guarded `std::max(1, ...)`, scaled constructors with protected
population, and assertion-only preconditions separately.

Authoritative state
-------------------
Primary source, historical and repair-side chronology sweeps: COMPLETE.
Civ4CE/Taurus high-signal comparative surfaces: SATURATED for the reviewed contracts.
Current-SAS empty-container mechanical invariant tranche: COMPLETE.
Post-primary confirmed roots: F708-F748.
F749 / provisional KI#1070: UNUSED.
No repository gameplay/XML/Python/C++ source modified.

PHYSICAL CHECKPOINT: C031-WIP891.
Primary 169/169 C++ files: COMPLETE.
Post-primary F708-F748 remain confirmed.
F749 / provisional KI#1070: UNUSED.
===============================================================================
END C031-WIP891
===============================================================================
